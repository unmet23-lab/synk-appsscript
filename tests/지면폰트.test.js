/* 지면 폰트 — 「발행하면 브랜드 서체가 사라지는」 자리를 기계가 잡는다 (2026-09-03).
 *
 * ══ 실사고 ═════════════════════════════════════════════════════════════════
 *   지면 56벌이 서체를 바깥에서 불렀다:
 *     <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/sun-typeface/SUIT/…">
 *   아티팩트 뷰어의 CSP 는 **스타일시트를 Google Fonts 에서만** 받는다(cdn.jsdelivr.net 은
 *   `/npm/` 아래 «스크립트»만). 그래서 발행된 지면은 **에러 없이** 대체 서체로 내려앉았고,
 *   경고는 발행 결과 한 줄에만 떴다 — **사람이 그 줄을 읽어야만 아는 실패**였다.
 *   09-03 에 지면 한 장만 손으로 고쳤고, 그건 도구가 다시 돌면 사라지는 수리였다.
 *
 *   🔴 그리고 **그게 절반이었다.** 세는 김에 나온 것 — 스택에 `'SUIT Variable'` 을 적어 놓고
 *      웹폰트를 **한 번도 안 부르는** 지면이 42벌 더 있었다(발행된 `마케팅_지금과새안` ·
 *      `SYNK_로드맵` · `이해대장` 포함). 막힌 적이 없어 경고조차 안 떴다. 09-03 에 같이 심었다.
 *
 * ══ 왜 이 여섯 검사인가 — 각자 «다른» 새는 방향을 막는다 ═════════════════
 *   ① 바깥에서 SUIT 를 부르는 지면이 0 인가        ← 새 지면이 옛 머리를 복사해 오는 길
 *   ② SUIT 를 «안»에 실은 지면은 바깥을 안 부르나  ← CSP 가 실제로 막는 그 자리
 *   ③ 펠트 스킨·자립형 지면은 서체를 «갖고» 있나   ← 굽기에서 서체가 조용히 빠지는 길
 *   ④ 심은 지면 52벌이 심긴 채로 남아 있나          ← 되돌려 놓고도 ①②가 초록인 길
 *   ⑤ 죽은 제외가 없나                              ← 목록이 낡아 분모가 조용히 작아지는 길
 *   ⑥ 자산 지문이 그대로인가 · 자가 무나(픽스처)    ← 위 다섯이 통째로 거짓 초록이 되는 길
 *
 *   🔑 ①②만 두면 **서체 선언이 통째로 사라진 지면이 초록으로 통과한다** — 「바깥을 안 부른다」가
 *      참이 되기 때문이다(0 이 성공 얼굴). 그래서 ③④ 가 «양성»을 따로 묻는다.
 *   🔑 ③ 의 분모는 등록부가 아니라 **파일 자신**이다(스킨 서명 · `_자립형` 이름). 등록부를 또 만들면
 *      지면방(`tools/lib/지면방.js`)과 같은 것을 두 곳이 알게 되고, 두 곳은 언젠가 갈린다.
 *
 * ══ 이 장치가 틀릴 때의 모습 ═══════════════════════════════════════════════
 *   제외 목록이 넓어져 «고쳐야 할 지면»을 삼키는 것 — 그러면 0건(초록)인데 분모만 조용히 작아진다.
 *   ⇒ 닫은 것 = 제외는 **경로마다 사유를 적는다**, 그리고 «제외인데 실은 안 걸리는» 줄(죽은 제외)을
 *      ⑤가 적색으로 낸다. 목록이 낡으면 목록이 운다.
 */
'use strict';
const { test } = require('node:test');
const assert = require('node:assert');
const fs = require('node:fs');
const path = require('node:path');
const { execFileSync } = require('node:child_process');

const 루트 = path.resolve(__dirname, '..');
const 브랜드폰트 = require(path.join(루트, 'tools', 'lib', '브랜드폰트.js'));

/** 추적 HTML 전량 = 분모. 작업본에만 있는 파일은 분모가 못 된다(다른 세션의 굽기 중간물이 섞인다). */
function 추적HTML() {
  return execFileSync('git', ['-C', 루트, 'ls-files', '-z', '--', '*.html'], { encoding: 'utf8', maxBuffer: 1 << 26 })
    .split('\0').filter(Boolean)
    .filter((p) => fs.existsSync(path.join(루트, p)));
}

const 읽기 = (rel) => fs.readFileSync(path.join(루트, rel), 'utf8');

/* ── 제외 — «왜 바깥에서 불러도 되나»를 경로마다 적는다 ────────────────────
 * 사유 없는 제외는 안 받는다. 넓은 와일드카드도 안 받는다(디렉터리 한 겹까지만). */
const 제외 = [
  [/^docs\/tools\//, '사람이 브라우저로 여는 «계기»다(팔레트·글리프 탐침·포스터 목업). 발행하지 않고, '
    + '`_mn_font_probe.html` 은 CDN 서체를 «비교 대상»으로 부르는 것이 그 지면의 일이다.'],
  [/^docs\/_archive\//, '보존 서가 — 그때 그 판 그대로가 값이다. 고치면 기록이 아니게 된다.'],
  [/^crewcard\/카드_(kr|mn)\.html$/, '🔴 학생이 여는 «라이브» 접수 폼(doGet 서빙)이다. 서체를 심으면 네 벌이 '
    + '각각 +0.8MB 가 되고, 이 폼은 몽골 학생 휴대폰이 연다. 발행 대상이 아니라 CSP 문제도 없다 — '
    + '심을지는 «학생 쪽 로딩»을 재고 유호님이 정하실 자리라 이 트랙에서 손대지 않았다.'],
  [/^docs\/크루카드\/크루카드_(한국어|몽골어)\.html$/, '위 라이브 폼의 docs 사본 — `크루카드사본.js` 가 '
    + '바이트 동일성을 강제한다. 정본이 안 바뀌었는데 사본만 바꾸면 그 검사가 빨개진다.'],
  [/^docs\/홈페이지_시안\/(병합판|시안\.tpl)\.html$/, '자립형 짝(`_자립형.html`)이 나가는 판이고, '
    + '이 원본은 그림도 상대경로라 애초에 «파일 하나»가 아니다. 서체는 `tools/시안굽기.js` 가 자립형에 심는다.\n'
    + '     ⚠ 09-03 에 기각본 넷(도전안판·도전안판_자립형·정석판·비교_0815)이 지워졌다'
    + '(유호 확정 「지운다」 · `6f8f3681f`·`bbeaf2d34`) — 이 정규식에서도 그 이름을 걷었다.'],
  [/^docs\/발표물\/_src_.*\.html$/, '🔑 **다른 통로가 이미 지킨다.** `tools/발표물빌드.js` 가 이 원고를 읽어 '
    + '산출물에 SUIT 정적 다섯 벌(400·500·600·800·900)을 심는다 — 산출물 여섯 전량이 `@font-face` 15벌을 '
    + '갖고 있는 것이 그 증거다. 그리고 그 임베드가 쓰는 가족 이름이 **`\'SUIT\'`** 라, 여기서 '
    + '`\'SUIT Variable\'` 로 바꾸면 심은 이름과 어긋나 **인쇄물이 통째로 폴백된다.** 원고가 이름만 '
    + '적는 것이 이 통로의 설계다(유호 확정 08-05 「임베드 통로에 태운다」).'],
  [/^docs\/브랜드_분위기_판정_0814\.html$/, '🔑 **폰트를 안 싣는 것이 이 지면의 설계다.** 머리에 스스로 적어 뒀다 — '
    + '「변수 격리: 색·글·레이아웃 전부 동결 — 바뀌는 것은 «재질»뿐 · 스크립트 0 · 외부 자원 0 '
    + '(폰트는 시스템 근사 — SUIT·DM Mono 실물 아님)」. 서체를 심으면 그 격리가 깨져 «재질만 비교한다»는 '
    + '이 판정 재료의 전제가 무너진다. 08-14 판정 재료라 지금 와서 조건을 바꾸지 않는다.'],
  [/^docs\/앱재설계_미니게임_아이디어\/시제품\/펠트놀이터_틀\.html$/,
    '🔑 **서체를 걷는 것이 이 지면의 설계다.** 이름 그대로 «틀»이라 그 자체로는 안 열린다 — '
    + '`시제품_자산.js` 가 자산을 data URI 로 끼워야 발행본이 된다(짝 README 가 절차를 쥔다). '
    + '그 묶는 단계에서 `@font-face` 줄을 **걷는다**: 자산만으로 2.3MB 이고 **5MB 판은 09-05 에 '
    + '클라우드플레어에 막혔다**(실측). 서체를 심으면 그 벽을 다시 넘어 발행 자체가 죽는다.\n'
    + '     ⚠ 틀에 남은 `@font-face` 세 줄은 src 가 `/*FONT_SUIT_M*/` 자리표시라 **실물 서체가 아니다** — '
    + '이 검사의 `/[\'"]SUIT/` 에 걸리는 것은 그 자리표시와 스택의 가족 이름뿐이다. '
    + '학생이 여는 폰은 내장 한글 서체로 떨어진다(README 에 그렇게 적혀 있다).'],
];

const 제외사유 = (rel) => (제외.find(([re]) => re.test(rel)) || [])[1] || null;

/* ── ④ 서체를 심은 지면 — «되돌려 놓아도 ①②는 초록»이라 여기가 따로 문다 ──
 * ①②는 「바깥을 안 부르나」만 묻는다. 서체가 통째로 빠진 지면은 바깥도 안 부르니
 * 그 둘을 그대로 통과한다(0 이 성공 얼굴). 여기가 «있나»를 이름째 묻는 자리다.
 *
 * 🔑 **손 지면과 생성 지면을 안 가른다** — 가르면 「지금 어느 쪽인가」를 이 파일이 또 알아야 하고,
 *    한 값을 두 곳이 알면 갈린다. 여기는 «결과»만 본다: 그 지면에 서체가 있나.
 *    ⇒ 빨개지면 처방은 둘 중 하나다.
 *       ⓐ 생성기가 있는 지면이면 **그 생성기**를 고쳐 서체를 싣게 하고 다시 굽는다(정본은 생성기다).
 *          09-03 실측: `docs/Loom_부품_시연.html`(tools/lib/loom.js) · `docs/이해대장.html`
 *          (tools/이해대장.js) · `docs/작동대장.html`(tools/작동대장.js) 셋이 그랬다.
 *          🔴 그중 이해대장은 **시험이 아니라 커밋 훅**이 잡았다(`tools/대장동봉검사.js`) —
 *             시험만 보면 초록이었다. 「자가 하나면 못 보는 자리가 남는다」의 실물.
 *       ⓑ 손 HTML 이면 그냥 다시 심는다: node tools/lib/브랜드폰트.js --심기 <그 파일>
 *
 * ⚠ 이 목록이 낡는 것(지면이 지워졌는데 줄이 남는 것)은 아래 검사가 스스로 문다. */
const 서체를심은지면 = [
  /* 생성기가 서체를 싣는 지면 셋 — 다시 구워도 안 빠진다. 그래도 여기 둔다(생성기가 되돌아가도 문다). */
  'docs/Loom_부품_시연.html',
  /* ── 09-03 2차 — 「스택에 SUIT 를 적어 놓고 한 번도 안 부르던」 지면 41벌 (유호 「42벌 쓸어줘」)
     이들은 CDN 이 막힌 적이 없다. 애초에 웹폰트를 «안 불러서» 지금껏 시스템 고딕이었다. */
  'bots/오버레이/전광판.html',
  'docs/12축이_못만드는것_2026-08-18.html',
  'docs/SHIFT/마케팅_정찰_2026-09-02.html',
  'docs/SHIFT/배너/시안.html',
  'docs/SHIFT/온라인마케팅_최종표.html',
  'docs/SHIFT/인증서_설명서_시안.html',
  'docs/SHIFT/채널단장_시안.html',
  'docs/SHIFT/프로필/시안.html',
  'docs/SHIFT/한차원_2026-09-02.html',
  'docs/SYNK_로드맵.html',
  'docs/SYNK_이야기.html',
  'docs/SYNK_철학_요약.html',
  'docs/tools/_bracket_probe/before_after.html',
  'docs/tools/_bracket_probe/index.html',
  'docs/게임_시안/게임_4종_화면.html',
  'docs/경쟁판정_2026-08-18.html',
  'docs/교실수집_선택지_2026-08-26.html',
  'docs/네온색_판정_2026-08-30.html',
  'docs/마스코트_동반자층.html',
  'docs/마케팅_지금과새안_2026-09-03.html',
  'docs/명품갭_판정_2026-08-26.html',
  'docs/발표물/로고/2차_k정제_AI추천_0818.html',
  'docs/발표물/로고/스플래시_잉크카드.html',
  'docs/발표물/로고/실물시트.html',
  'docs/발표물/로고/트렌디_개선안_대조.html',
  'docs/발표물/로고/후보시트.html',
  'docs/브랜드킷.html',
  'docs/설계대조_2026-08-18.html',
  'docs/심문표식_판정_2026-09-01.html',
  'docs/앱_방향설계_2026-08-18.html',
  'docs/오늘정리_2026-08-18.html',
  'docs/오답이_못보는것_2026-08-18.html',
  'docs/이해대장.html',
  'docs/작동대장.html',
  'docs/작명_판정표_0815.html',
  'docs/철학엔진_정합판정_2026-08-26.html',
  'docs/캐릭터/캐러셀_괜찮아요_다섯뜻_룸무대.html',
  'docs/캐릭터/판3종_대조_0820.html',
  'docs/판정대기_한자리_2026-08-26.html',
  'tools/lib/워프시연_템플릿.html',

  /* ── 09-03 3차 — 이름이 옛 판(`'SUIT'`)이라 스택 순서까지 같이 옮긴 여섯 (유호 「나머지 15벌도 쓸어줘」)
     정본 순서로 옮겼다: 'SYNK Bracket','Inter Tight','SUIT Variable',… (Inter Tight 가 «앞»이다 —
     폴백이 글자 단위라 라틴·키릴은 Inter Tight, 한글은 SUIT 가 그린다). 폐기 폰트 Pretendard 도 같이 걷었다. */
  /* 09-03 4차 — 자가 «첫 실전»에서 잡은 지면. 이 자를 세운 그날 새로 태어났고 ⑦ 이 짚었다. */
  'docs/자립형_빠질그림_판정_2026-09-03.html',
  'docs/다크팔레트_판정_0820.html',
  'docs/정본/SYNK LAB/자료/_src_오프라인_신규_등록서.html',
  'docs/캐릭터/세계관_첫시작_시연_0826.html',
  'docs/펠트코랄_0815.html',
  'docs/펠트코랄_0815_가벼운판.html',
  'docs/펠트코랄_0815_자립형.html',

  /* ── 09-03 1차 — CDN 이 실제로 막히던 지면들 */
  'docs/개인정보처리방침_게시용.html',
  'docs/교차검수_재설계_정찰_2026-09-02.html',
  'docs/깃허브_정찰_2026-09-03.html',
  'docs/말투후보_부리다_2026-09-03.html',
  'docs/몽글_물어보기_시안.html',
  'docs/운영_한눈에.html',
  'docs/컴퓨터를 잃어버렸을 때.html',
  'docs/허깅페이스_PRO가여는문_2026-09-03.html',
  'docs/허깅페이스_수확_2026-09-03.html',
  'docs/허깅페이스_정찰_2026-09-02.html',
  'docs/엔진/SYNK_엔진_지도.html',
  'docs/릴_재미_시안.html',
];

/* ── ① 바깥에서 SUIT 를 부르는 지면 ────────────────────────────────────── */

test('① 어떤 지면도 브랜드 서체를 «바깥»에서 부르지 않는다', () => {
  const 전량 = 추적HTML();
  const 걸린것 = [];
  let 잰것 = 0;
  for (const rel of 전량) {
    if (제외사유(rel)) continue;
    잰것 += 1;
    if (/sun-typeface/i.test(읽기(rel))) 걸린것.push(rel);
  }
  assert.ok(잰것 > 0, '분모가 0 이다 — 이 검사는 아무것도 안 쟀다');
  assert.deepEqual(걸린것, [],
    `바깥에서 SUIT 를 부르는 지면 ${걸린것.length}벌 (잰 것 ${잰것} / 추적 ${전량.length}).`
    + ' 아티팩트 CSP 가 이 줄을 «에러 없이» 막아 대체 서체로 내려앉는다.'
    + `\n   → 심는다: node tools/lib/브랜드폰트.js --심기 ${걸린것.slice(0, 3).join(' ')}`
    + '\n   → 굽기 산출물이면 그 생성기를 다시 돌린다(생성기는 이미 안 넣는다).');
});

/* ── ② SUIT 를 안에 실은 지면이 바깥을 부르나 = CSP 가 막는 그 자리 ──────── */

test('② 서체를 실은 지면은 Google Fonts 말고는 바깥 스타일시트를 안 부른다', () => {
  const 실은것 = 추적HTML().filter((rel) => !제외사유(rel) && 브랜드폰트.심겼나(읽기(rel)));
  assert.ok(실은것.length > 0, '서체를 실은 지면이 0벌 — 자가 아무것도 안 재고 있다');
  const 걸린것 = [];
  for (const rel of 실은것) {
    /* 축 = «서체·스타일»만. 그림·미디어는 «자립성» 축이라 여기서 안 문다 —
       섞으면 이 자가 「서체가 막히나」와 「그림이 같이 가나」 둘을 한 적색으로 뭉갠다.
       (그림 축은 굽기 통로의 `펠트문서.검사()` 가 잰다.) */
    const 흠 = 브랜드폰트.바깥것(읽기(rel), { 축: '서체' });
    if (흠.length) 걸린것.push(`${rel} — ${흠.map((x) => x.종류 + ' ' + x.url).join(' · ')}`);
  }
  assert.deepEqual(걸린것, [],
    `발행하면 조용히 막히는 자리 ${걸린것.length}벌 (실은 지면 ${실은것.length}벌 중).`
    + ` 아티팩트가 허용하는 곳은 ${브랜드폰트.허용호스트.join(' · ')} 뿐이다.`);
});

/* ── ③ «서체가 빠진 것»을 잡는다 — 분모는 파일 자신이 답한다 ───────────── */

test('③ 펠트 스킨을 입은 지면·자립형 지면은 서체를 «갖고» 있다', () => {
  const 과녁 = 추적HTML().filter((rel) => {
    if (제외사유(rel)) return false;
    if (/_자립형\.html$/.test(rel)) return true;
    return 읽기(rel).includes('SYNK 지면 「검은 무대」');    // 펠트 스킨 서명
  });
  assert.ok(과녁.length >= 18,
    `과녁이 ${과녁.length}벌뿐이다 — 스킨 서명이 바뀌었거나 지면이 사라졌다(09-03 실측 = 19벌).`
    + ' 분모가 조용히 작아지는 것이 이 검사가 새는 유일한 방향이다.');
  const 빈것 = 과녁.filter((rel) => !브랜드폰트.심겼나(읽기(rel)));
  assert.deepEqual(빈것, [],
    `서체가 빠진 지면 ${빈것.length}벌 (과녁 ${과녁.length}벌 중) — 바깥을 «안 부르는» 것만으로는`
    + ' 초록이 되지만 화면에는 브랜드 서체가 없다. 그 지면의 생성기를 다시 돌린다.');
});

test('④ 서체를 심은 지면은 심긴 채로 남는다', () => {
  const 없는것 = 서체를심은지면.filter((rel) => !fs.existsSync(path.join(루트, rel)));
  assert.deepEqual(없는것, [], `목록이 낡았다 — 없는 지면 ${없는것.length}벌. 지웠으면 이 목록에서도 지운다.`);
  const 빈것 = 서체를심은지면.filter((rel) => !브랜드폰트.심겼나(읽기(rel)));
  assert.deepEqual(빈것, [],
    `서체가 되돌아간 지면 ${빈것.length}벌 (${서체를심은지면.length}벌 중).`
    + `\n   → 다시 심는다: node tools/lib/브랜드폰트.js --심기 ${빈것.slice(0, 3).map((p) => JSON.stringify(p)).join(' ')}`);
});

/* ── ⑤ 목록이 낡는 것을 목록이 운다 ───────────────────────────────────── */

test('⑤ 죽은 제외가 없다 — 제외 줄은 실제로 무언가를 덮고 있다', () => {
  const 전량 = 추적HTML();
  const 죽은것 = 제외.filter(([re]) => !전량.some((rel) => re.test(rel))).map(([re]) => String(re));
  assert.deepEqual(죽은것, [],
    `아무것도 안 덮는 제외 ${죽은것.length}줄 — 지면이 옮겨졌거나 지워졌다.`
    + ' 죽은 제외를 두면 다음 사람이 「이미 판정된 자리」로 읽는다.');
});

/* ── ⑥ 전제 — 자산과 자가 실제로 서 있는가 ────────────────────────────── */

test('⑥ 서체 원본이 못박은 지문 그대로다 — 아니면 위 다섯이 전부 거짓 초록이다', () => {
  const buf = 브랜드폰트.woff2();                      // 지문이 다르면 여기서 던진다
  assert.equal(buf.length, 브랜드폰트.바이트);
  assert.ok(브랜드폰트.면().includes('data:font/woff2;base64,'), '심는 규칙에 data URI 가 없다');
  /* 탐지력 — 실저장소가 초록이어도 이 자가 «무는지»를 픽스처가 증명한다(맹점 ②). */
  const 병든 = '<html><head><title>t</title>'
    + '<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/sun-typeface/SUIT/x.css"></head></html>';
  assert.equal(브랜드폰트.심겼나(병든), false, '안 심긴 지면을 «심겼다»로 읽는다');
  assert.equal(브랜드폰트.바깥것(병든).length, 1, '바깥 스타일시트를 못 센다');
  const 나은것 = 브랜드폰트.심기(병든).html;
  assert.equal(브랜드폰트.심겼나(나은것), true);
  assert.equal(브랜드폰트.바깥것(나은것).length, 0);
  assert.equal(브랜드폰트.심기(나은것).html, 나은것, '멱등이 아니다 — 두 번 돌리면 서체가 두 벌 실린다');
  /* Google Fonts 는 통과해야 한다 — 여기서 물면 세 언어 스택이 통째로 막힌다. */
  const 구글 = '<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter+Tight">';
  assert.equal(브랜드폰트.바깥것(구글).length, 0, 'Google Fonts 를 막으면 라틴·키릴이 폴백된다');
  /* `<a href>` 는 이동이지 받아오기가 아니다 — 물면 바깥 링크 단 보고 지면이 전부 빨개진다. */
  assert.equal(브랜드폰트.바깥것('<a href="https://synk.im">홈</a>').length, 0, '바깥 «링크»를 자원으로 센다');
});

/* ── ⑦ 같은 사고가 세 번째로 나지 않게 — 「이름만 부르는」 자리를 막는다 ───
 *
 * 🔴 09-03 에 이 결함이 **두 번** 나왔다. 모양이 달라서 다른 사고로 보였을 뿐 뿌리가 같다.
 *   1차(39벌) — 바깥 CDN 을 부르는데 아티팩트 CSP 가 막았다. 그건 «부르긴 부르는데 못 받는» 것.
 *   2차(42벌) — 스택에 이름만 적고 **아무 데서도 안 불렀다**. 막힌 적이 없어 경고조차 안 떴다.
 *   3차(6벌)  — 위에 더해 이름이 옛 판(`'SUIT'`)이라 스택 순서까지 어긋나 있었다.
 *   ①은 1차만 잡고, ③④는 «이미 아는 지면»만 잡는다. **새로 태어나는 지면**은 셋 다 못 잡는다.
 *
 * 🔑 그래서 술어를 뒤집는다 — 「부르나」가 아니라 «부르면 싣나». 스택에 SUIT 를 적는 순간
 *    그 지면은 이 검사의 분모에 «스스로» 들어온다. 등록부가 없어 낡을 수가 없다.
 */

test('⑦ SUIT 를 «이름으로 부르는» 지면은 그 서체를 반드시 싣는다', () => {
  const 부르는것 = 추적HTML().filter((rel) => !제외사유(rel) && /['"]SUIT/.test(읽기(rel)));
  assert.ok(부르는것.length > 30,
    `SUIT 를 부르는 지면이 ${부르는것.length}벌뿐이다 — 자가 거의 아무것도 안 재고 있다(09-03 실측 = 79벌).`);
  const 빈것 = 부르는것.filter((rel) => !브랜드폰트.심겼나(읽기(rel)));
  assert.deepEqual(빈것, [],
    `이름만 적고 안 싣는 지면 ${빈것.length}벌 (부르는 지면 ${부르는것.length}벌 중).`
    + ' 이 지면들은 «막힌 적이 없어» 경고도 안 뜬다 — 그냥 시스템 고딕으로 떨어져 있다.'
    + `\n   → 심는다: node tools/lib/브랜드폰트.js --심기 ${빈것.slice(0, 3).map((p) => JSON.stringify(p)).join(' ')}`
    + '\n   → 안 심는 것이 맞으면 위 `제외` 표에 **사유와 함께** 줄을 넣는다(사유 없는 제외는 안 받는다).');
});
