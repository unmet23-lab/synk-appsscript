# 심문 P0 절단 — 「소급 불가」 축 (2026-08-07)

> 입력 = `docs/_ops/심문결과/` 6건(계약 3종 × 모델 2 · **P0 122건 · P1 33건**, 커밋 61ae7b4).
> 절단 기준 = **운영 첫 제출**. 개원(2027-02)이 아니라 실학생 첫 행이 `운영` DB 에 들어가는 순간이
> 마감이다 — 리허설 데이터는 버릴 수 있지만 운영 첫 행부터는 못 버린다.
> 아래 ①②는 **심문 지적을 그대로 옮긴 게 아니라, 오늘 실물 소스를 다시 열어 「아직 그런가」를
> 확인한 것만** 남겼다(심문 뒤 수리된 것이 섞여 있다 — 근거 줄번호를 각 항목에 붙였다).

---

## 판정

**①(14건)만 운영 배포 전 차단자다. 나머지는 뒤로 미뤄도 데이터가 안 상한다.**
그리고 ① 14건 중 **유호님 결정이 필요한 것은 1건**(음성 포맷)뿐이고, 13건은 저장소 안에서
끝나는 코드·계약 작업이라 지시 없이 진행할 수 있다.

계획 정원 85명 기준 S1 은 하루 한 문장 왕복 1회 = **하루 85건**. ① 을 안 닫고 운영을 열면
그 85건이 매일 손상된 채 쌓이고, 나중에 고쳐도 **이미 쌓인 것은 안 고쳐진다.**

### 진행 (2026-08-07 · ✅ 표시가 정본이다 — 아래 각 항목에 커밋·검증이 붙어 있다)

| 닫힘 | 남음 |
|---|---|
| **1**(음성 WAV · 유호님 확정) · **3**(`task_snapshot`) · **5**(멱등키) · **6**(빈 껍데기) · **7**(추정메타 물리 0 · ⏳리허설) · **8**(선택지 안정 id) · **9**(전사 오기) · **10**(`correlation_id`) · **11**(최초 제출 개입) · **13**(`skill_ids`) · **14**(IANA zone) | **2**(무발화 WAV ✅ · 관측 사건만 남음) · **4**(`client_event_id`) · **12**(전달 증거) |

🔴 **「남은 셋은 전부 c9 승인 대기」는 사실이 아니었다**(2026-08-07 실측 · 보드와 이 표가 같이 틀렸다).
유호님 앞에 놓인 c9 는 `발주_게임모듈.md` §6-5 #4 = **게임모듈 ③ 누락수리 하나**고, 그 안에 ①-2·4·12 는
**한 줄도 없다.** 승인이 나도 ① 은 안 열린다. 실측으로 다시 가르면:
- **2** — 다섯 값 중 넷은 c9 가 필요 없었다(둘은 WAV 파생 · 무발화 갈림은 **오늘 닫았다**). 남은 하나만 c9.
- **4** — `parent_event_id`·`turn_no` 는 계약·서버에 이미 섰고 **앱 생산자가 0**이다(S1 은 대화가 없다).
  즉 **오늘 잃는 것이 0**이고 차단자는 「운영 첫 제출」이 아니라 **챗봇 턴이 붙는 날**이다.
- **12** — 절반은 ①-7 이 닫았고(`inferred` 표기), 남은 절반은 **2 와 같은 이름 하나**를 기다린다.

👉 **① 전체가 실제로 기다리는 c9 는 「학생이 실제로 보고·들었다」 관측 사건 이름 1개뿐이다.** → §결정 요청.

⚠ 「닫힘」 = 저장소와 **리허설**에서 섰다는 뜻이다. **운영에는 아직 하나도 안 갔다** — 운영 반영은
유호님 판정 자리다(`SYNK-talk/docs/배포_경로.md` 과녁 게이트).
⏳ **7 은 저장소까지만 섰다**(talk `807e333`) — 조각을 리허설에 붓는 것은 `원격SQL.js --적용` 이고
그 도구가 스스로 「유호님 승인 뒤에」로 못박은 자리라 안 쐈다. 읽기로 과녁·전제는 재 뒀다:
대상 = `synk-core-rehearsal` · 현재판 = `20260807140000`(= 이 조각의 base) · 빠진열 = 이 조각의 네 열
정확히 그것뿐. 즉 **부으면 바로 서고**, 안 부으면 그 넷은 계속 계약에만 산다.

---

## ① 소급 불가 — 운영 첫 제출 전 (14건)

### A. 원본이 아예 안 남는다 (복원 경로 0)

1. **음성이 손실 압축이다** — `SYNK-talk/src/말하기화면.js:51` 이 `RecordingPresets.HIGH_QUALITY`
   (= m4a/AAC)다. 계약 정본은 PCM WAV 16kHz/16bit/mono(C0 §4-2). 압축이 지운 발음 신호는
   원본이 없어 되살릴 수 없다. 소스 46행이 이미 "정본 규격이 아니다"라고 자백해 뒀다.
   🔴 **유일하게 유호님 결정이 필요** — 아래 §결정 요청.
2. **S1 관측값이 기기에서만 산다** — `hesitation_ms`·`duration_ms`·재생 완료·자기 목소리
   되듣기·무발화 맥락. `lib/오늘과제.js:196-199` 가 스스로 적어 뒀다: "머뭇거림·발화길이·문턱은
   아직 안 나간다 … 기기 로그에는 남아 있으니 **c9 개정 때 함께 올린다**."
   🔴 **그 자기 진단이 틀렸다**(2026-08-07 실측) — 다섯 중 넷은 c9 가 필요 없었고, 그중 하나는
   **오늘도 매일 잃고 있었다.** 갈래를 세 층으로 가른다:
   - **`hesitation_ms`·`duration_ms` = 파생이라 필드가 없는 게 맞다.** `발주_게임모듈.md` §646 이
     이미 기각해 뒀다 — "`stt_segments` 첫 세그먼트의 `start` 가 곧 그 값이다(WAV 0초 = 녹음
     시작)". `wpm`·`pause_duration_sec` 와 같은 계열이다. 즉 원천은 payload 칸이 아니라 **WAV
     파일**이고, 파일이 서버에 있으면 c9 없이 산다. 👉 **새 필드 재제안 금지.**
   - 🔴 **그런데 무발화 행은 그 파일을 안 들고 갔다** — 이게 ①-2 의 진짜 소급 불가였다.
     무발화는 둘인데(ⓐ입을 안 뗌 = WAV 없음 / ⓑ소리는 났는데 문턱 미달 = **WAV 있음**),
     `src/제출API.js` 가 ⓑ 의 파일을 **이미 업로드까지 해 놓고** `제출사건` 이 `audio_ref` 를
     버렸다. 두 행이 바이트 단위로 같아지고 올라간 파일은 **버킷의 고아**로 남는다(③ 목록의
     「업로드 고아 파일」과 같은 자리). 겁먹은 학생과 포기한 학생이 한 행이 되어 이탈 예측
     (P0 S1-6)의 원신호가 죽는다 — ①-6 이 고친 접힘의 **한 층 아래**다.
     ✅ **해소**(talk `ca786a6`) — 무발화도 `audio_ref` 를 있으면 싣는다. **c9 불요**:
     `audio_ref` 는 계약이 이미 낸 이름이고 `lib/이벤트검증.js` 의 `선택필드` 에 사유와 함께
     등재돼 있다. 곁들여 서버가 `capture_meta.server` 에 헤더 실측을 얹어 준다(`functions/events`
     `헤더측정`). 회귀 2 짝(한쪽만 있으면 「늘 싣는다」·「늘 버린다」가 둘 다 통과한다)·변이 2/2·
     459/459. ⚠ `event_type` 은 그대로라 제출 수는 안 흔들린다(①-6 이 실측해 둔 자리).
   - ⏳ **진짜로 c9 인 것은 하나뿐이다 — 「학생이 실제로 보고·들었다」 관측 사건 이름 1개.**
     재생 완료·자기 목소리 되듣기는 파일에서 파생이 안 되고(오디오 **밖**의 화면 행동) 계약
     `event_type` 12값 어디에도 자리가 없다. 🔑 **그리고 그 이름이 ①-12 의 남은 절반과 같은
     것이다**(거기도 「학생이 실제로 봤다」는 관측 사건이 없어 열려 있다). 즉 **① 전체가
     기다리는 c9 는 이름 하나다.** → §결정 요청.
3. ~~**`task_snapshot` 이 필수가 아니다**~~ — `lib/이벤트검증.js:54-59` 는 `task_ref`·`task_format`
   만 요구했다. 문항·보기·지시문이 개정되면 학생이 그때 본 판을 복원할 수 없다.
   ✅ **해소**(talk `182d5e1`) — `submission.created` 필수로 올리고 **앱이 화면이 그린 판을
   되싣는다**(`화면과제` 제출재료 → `제출사건`). 🚫 서버가 배정 행에서 베끼는 길은 안 골랐다:
   그건 「앱이 배정대로 그렸다」는 추정이고, 추정을 관측 칸에 넣는 것이 아래 7번이다.
   실왕복 54/0(리허설 events v10 · 「판 없으면 거부」 포함).
4. **오프라인 사건끼리 못 잇는다** — 인과 참조가 서버 발급 `event_id` 뿐이라, 아직 업로드
   안 된 선행 턴을 가리킬 이름(`client_event_id`)이 없다. 한 배치 안의 대화 순서가 사라진다.

### B. 서로 다른 것이 같은 모양으로 접힌다 (사후 분리 불가)

5. ~~**멱등키가 UUID 가 아니라 결정론 문자열이다**~~ — `lib/오늘과제.js:214`
   `submission:{날짜}:{호흡}:{시도}`. 재설치·다중 기기·로컬 로그 초기화면 **다른 녹음이 같은
   키**가 되고, 서버는 `duplicate` + 원래 `event_id` 를 **성공으로** 돌려준다.
   👉 데이터 손실이 성공과 똑같은 모양이라 아무도 못 알아챈다. 6건 전부가 지적했다.
   ✅ **해소**(talk `06d00e5`) — 키를 **항목이 들고 간다**(`correlation_id` 와 같은 통로·같은
   발급기): `lib/제출로그.js` `항목추가` 가 만들 때 v4 uuid 로 **한 번** 짓고, `제출사건` 은
   그 값만 싣는다(없으면 사건을 안 만든다 — 여기서 지어내면 그 순간 좌표 조립으로 되돌아간다).
   🔑 **재시도 안전성은 키가 결정론이라서가 아니라 항목이 키를 들고 있어서다** — uuid 가
   위험해지는 것은 보낼 때마다 새로 짓는 경우뿐이고 그 통로는 없앴다. 서버 **배치**는 그대로
   결정론(`task:{learner_id}:{날짜}`) — 재실행이 같은 사실을 다시 쓰는 것이라 접혀야 맞다.
   🔴 계약(`C0 §4-1`)은 **처음부터 「UUID v4 · 한 번 생성하고 재시도해도 안 바꾼다」였다.**
   코드 주석이 "🔴 uuid 를 쓰면 안 된다"로 정반대를 적었고 **회귀가 그 반대쪽을 초록으로
   잠가** 두고 있었다(F179 와 같은 형태 · 여기선 테스트가 잠금장치였다).
   회귀 421/421(신설 4)·변이 4/4 — 그중 ③은 「발급은 하되 좌표에서 짓기」(시점만 옮긴 가짜
   수리)를 잡는다.
6. ~~**빈 껍데기 사건이 정상 행으로 저장된다**~~ — `이벤트검증.js:71-72` 가
   `preference.stated: []`·`session.abandoned: []`. 낭독 시작 전 이탈과 자유발화 중 포기가
   같은 행이 되어 끈기·난이도 모델의 원신호가 파괴된다.
   ✅ **해소**(talk `84275c8`) — 강제는 **새 이름 없이** `task_format` 으로 했다. 계약이 c5 에서
   **바로 그 구분을 위해** 만든 칸이라서다(「한 칸에 담으면 낭독 데이터로 회화 모델을 학습시키는
   사고가 조용히 성립한다」). 새 이름을 지으면 c9 이고, c9 를 기다리는 동안 쌓이는 무발화 행은
   영영 안 갈린다. 🔑 `submission` 밑인 이유 = `task_format` 의 물리 자리가 `engine.submissions`
   뿐이다(`learning_events` 엔 그 열이 없다) — 무발화도 **내용물 없는 제출물 행**을 남기고,
   그 행의 뜻이 「이 형식의 과제를 받고 아무것도 안 냈다」다. `task_ref` 도 함께 싣는다(분모).
   🔴 **기존 회귀가 반대쪽을 잠그고 있었다**: 「제출물 행을 만들지 않는다」로 제출 수 불변을
   지켰는데 그 대가가 이 손실이었다. 진짜 지렛대는 행의 부재가 아니라 `event_type` 필터다 —
   하류 5곳(progress·tasks·deliver 둘·corrections)이 전부 그렇게 세는 것을 소스로 확인했고
   `배달왕복시험` ⑥ 이 이미 그 자리를 지킨다. 검사의 의도는 남기고 지렛대만 바꿨다.
   🔑 곁들여 **「추가 필수 0」의 두 뜻**을 갈랐다(`문턱없음` 사유 장부 + 양방향 회귀):
   `session.abandoned` 는 **정해서 안 건 것**, `preference.stated` 는 **아직 안 정한 것**인데
   둘 다 `[]` 라 주석이 「같은 이유」로 묶어 놨었다. 후자의 사유 = 생산자 0 · payload 규격 정본 0 ·
   `발주_게임모듈.md` §593 이 「안 쓴다」로 못박음. 회귀 447/447(신설 4)·**변이 6/6**.
7. ~~**관찰과 추론이 물리에 안 갈린다**~~ — `source_kind`·`estimator_confidence`·`estimator_version`
   ·`evidence_refs` 는 검증기의 `서버칸`(`이벤트검증.js:20-24`)에만 있고, `supabase/` 전체에
   **0건**이었다(DDL 열도 INSERT 도 없다). 앱이 보내면 400 인데 서버도 저장하지 않는다 =
   조용히 버려진다. F185 가 계약층에서 잡은 고아 필드의 **물리층 판**이다.
   ✅ **해소**(talk `807e333` · ⏳리허설 적용은 승인 대기) — 조각 `20260807170000` 이
   `engine.source_kind` enum(계약 값목록 4종)과 네 열을 열고, 표 하나(`lib/사건출처.js`)를
   서버 통로 둘(`functions/events`·`deliver`)이 같이 읽는다. 각자 적으면 같은 사건이 한쪽에선
   관측, 다른 쪽에선 추정이 되고 증상은 몇 달 뒤 집계 불일치뿐이다.
   배달 두 행은 `inferred` 다 — 오늘 이 문장을 준 것은 관측이 아니라 판단이고, 판단이 틀린
   날의 저조를 학생 특성으로 읽지 않으려면 그 사실이 행에 남아야 한다.
   회귀 신설 9(전체 431/0) · 변이 8/8.
   🔴 **정직하게 — 이 커밋이 채우는 것은 `source_kind` 하나다.** 나머지 셋은 추정기가 서는 날
   첫 값을 받는다. 그때 열이 없으면 그 값이 버려지는 것이 이 항목이 지목한 상태이므로 열은
   지금 연다. 같은 이유로 `estimator_confidence` 범위 CHECK 는 **일부러 안 걸었다**(쓰는 자가
   없으면 아무 행도 안 재는 제약이 된다 — 강제와 함께 추정기 조각에서 건다).
   ⚠ **이 항목의 「소급 불가」 근거는 「지금 새고 있다」가 아니다** — 추정을 만드는 것이 아직
   없어 오늘 잃는 행은 0건이고, 오늘 기준으로는 `event_type` 에서 값을 되짚을 수도 있다.
   진짜 시한은 **엔진의 첫 추정 행**이다: 그 뒤로는 같은 `event_type` 에 관측 행과 추정 행이
   섞여 서서 사후에 못 가른다. 원문의 판정을 낮추지 않되 근거는 이것으로 바꿔 적는다.
   🔴 **곁들여 잡힌 것 — 열은 있는데 writer 가 0인 칸 셋**(같은 병의 다른 얼굴, 이 트랙 밖):
   `learning_events.model`·`prompt_ver`·`policy_ver` 는 DDL 에 **있는데** `supabase/` 어디서도
   INSERT 되지 않는다(검증기 `서버칸` 목록에만 산다). 즉 어느 판본의 규칙·프롬프트가 그날의
   개입을 만들었는지 행에 안 남는다 — 규칙이 바뀌면 이건 소급이 **안 된다**. 다만 판본의
   원천은 `lib/오늘과제.js` 라 그 파일을 든 세션 몫으로 남긴다(②로 승격할지도 그쪽 판정).
8. ~~**선택지에 안정 식별자가 없다**~~ — 표시 문구를 값으로 저장하면 번역·문구 수정 뒤 같은
   선택지를 못 잇고, 자리 효과와 의미 효과가 섞인다.
   ✅ **해소**(talk `7207b16`) — 계약(`C0 §156`)은 c6부터 **`{option_id, label}`**로 옳게 적혀
   있었고, 비어 있던 것은 **강제**다: `이벤트검증` 의 `있음()` 은 존재만 봐서 문구 문자열 배열이
   그대로 통과했고, **`L0` 예시와 회귀 픽스처가 그 틀린 모양을 정상으로 잠가** 뒀다
   (①-5 와 같은 형태 — 계약이 옳은데 검사가 반대쪽을 초록으로 굳혔다).
   🔑 **사건이 아니라 필드 이름으로 걸었다** — G2(`발주_게임모듈.md` §362)는 같은 선택로그 필드를
   `choice.selected` 가 아니라 `submission.created` payload 로 보내기로 이미 정해져 있어,
   이벤트별로 걸면 그 통로가 통째로 샌다. 검사 넷: 객체 모양 · `label` 동반 · `option_id` 중복 ·
   가리키는 값이 보여준 목록 **안**인가(라벨을 넣으면 여기서 걸린다).
   🔴 **생산자는 아직 0이다** — 첫 화면(S1-5·G2·G4)이 서기 전에 닫아서 잘못 쌓일 행이 0이다.
   회귀 28(신설 1·픽스처 1건 교체)·**변이 5/5**. 🔴 첫 변이는 3/5였다: 「중복 id」와 「분모 없음」
   픽스처가 **다른 이유로** 빨개져(소속 검사·`이벤트별필수`가 먼저 잡아) 규칙을 지워도 초록이었다.
9. **`transcript` / `transcript_verified` 의 뜻이 한 문서에서 거꾸로다** — 심문은 "두 정본이
   갈라졌다"고 했지만 실제로 세어 보니 **1 대 3**이다:
   - `L0_데이터계약.md:662` · `발주_수집파이프라인.md:62·111` · `발주_게임모듈.md:554` (3곳) =
     `transcript` 는 **기계(Whisper) 전사**, `transcript_verified` 는 사람이 **들린 대로** 되돌린 것.
     (목적이 「학생 오류 보존」이고, Whisper 가 오발음을 정타로 고쳐 듣기 때문이다.)
   - `P0_제품계약.md:112` (1곳) = "들린 대로 `transcript` / 의도 문장 `transcript_verified`"
     → **두 칸 다 거꾸로**다.
   👉 계약 판정이 아니라 **한 줄 오기**다. `P0_제품계약.md:112` 만 고치면 끝난다(비용 거의 0).
   그대로 두면 이 줄을 읽고 구현한 사람이 두 칸을 뒤바꿔 채우고, 그때부터 코퍼스의 라벨
   의미를 사후에 판별할 수 없다.

### C. 고리가 끊겨 원인을 못 되짚는다

10. ~~**`correlation_id` 가 계약상 선택이다**~~ — `이벤트검증.js:37` 공통필수가
    `idempotency_key·event_type·occurred_at·level_snapshot` 넷뿐이었다. 앱만 고쳤고(a9362e4)
    계약은 안 고쳤으니, 구 큐나 다음 소비자가 생략하면 한 앉음이 다시 흩어진다.
    ✅ **해소**(talk `182d5e1`) — 공통 필수로 올렸다. 없으면 앱이 사건을 **아예 안 만든다**
    (400 루프도, 지어낸 앉음도 만들지 않는다). 실왕복 54/0(「한 앉음 키 없으면 거부」 포함).
11. ~~**최초 제출이 개입을 안 가리킨다**~~ — `functions/events/index.ts:299-311` 은
    `retry_of_event_id` 가 있을 때만 `intervention_id` 를 계승한다. 즉시 성과가 어느 개입의
    결과인지 안 남는다.
    ✅ **해소**(talk `77f6fe2`) — 🔑 **새 필드 0 · c9 불필요 · 앱 무변경.** `task_ref` 는 배치가
    지은 이름이고 제출이 이미 들고 오므로, 서버가 `task.assigned` ↔ `submissions.task_ref` 로
    그 학생의 그날 배정을 찾아 잇는다(같은 자리 주석이 이미 "그 사건의 `intervention_id` 는
    서버가 잇는다"고 적어 둔 원리를 첫 제출로 넓힌 것). `session.abandoned` 도 같이 얻는다.
    🔴 **편향의 방향이 요지다** — 재시도한 학생은 고리가 남고 **한 번에 해낸 학생만** 안 남았다.
    즉 「어느 개입이 먹혔나」가 영영 재시도 쪽으로 기운다.
    🔴 못 찾으면 **거절하지 않고 null**(고정 도입 과제처럼 배정 행 없이 내는 갈래가 실재한다 —
    거기서 400 을 내면 그 발화가 큐에서 영영 재시도한다).
    실측 **실왕복 61/0**(리허설 `events` v15 · 신설 검사 4 = 양성 2 + **음성 2**) · **변이 2/2**
    (리허설에 실제 배포해서 쟀다: Ⓐ조회를 어긋나게 → 「물려받았다」만 ❌ · Ⓑ못 찾으면 거절 →
    ②정상 저장부터 무너짐). ⚠ **`learner_id` 필터는 미측정** — 이 시험이 학생 1명으로 돈다.
    ⛔ 운영 미배포.
12. **「실제 전달」의 증거가 없다** — `intervention.delivered` 를 전날 밤 배치가, 학생이 앱을
    열기 전에 쓴다. 네트워크 실패도 「전달 완료」로 학습된다.
    🔵 **절반은 ①-7 이 이미 닫았다**(2026-08-07 확인) — `lib/사건출처.js:47` 이 이 사건을
    `inferred` 로 박으므로 행이 스스로 「이건 관측이 아니라 추정」이라고 말한다. 남은 절반은
    「학생이 실제로 봤다」는 **관측** 사건인데 그건 새 이름이라 **c9** 다. 🚫 `inferred` 표시를
    다시 짓는 재제안 금지(이미 있다).
13. ~~**`skill_ids` 가 무검증이다**~~ — `events/index.ts:340` 이 존재·판본 대조 없이 그대로
    INSERT 했다. 어휘 지도의 축이 영구 오염된다.
    ✅ **해소**(talk `31f62d5`) — L0 §3-2 가 「서버 검증으로 막는다」고 **약속만 해 둔** 자리를
    지었다: 모양(문자열 배열) · 판본 동반 요구 · `engine.skills` 실재 대조(없는 id 를 지목).
    빈 배열은 정상으로 남긴다(축 없는 과업). 실왕복 54/0 — 거부 3 + **통과 2**(거부만 재면
    「전부 막는 검사」와 「옳게 막는 검사」가 같은 초록이다).
14. ~~**날짜 경계에 IANA zone 이 없다**~~ — 멱등키·`/tasks`·`/progress` 가 모두 "몽골 시간대"라고만
    쓴다. 배치와 Edge 가 다른 경계를 쓰면 결정론 키가 다른 날 사건을 접는다(→ 5번과 곱해진다).
    ✅ **해소**(talk `ac3f646`·`d2fdf26`) — 경계를 아는 곳이 **넷**이었다: `lib/오늘과제.js` 의
    `시간대`(정본) · `deliver`·`tasks` 의 SQL 리터럴 사본 · 그리고 `src/말하기화면.js` 의
    **기기 시계**. `progress` 만 정본을 가져다 쓰고 있었다. 셋을 `${시간대}`·`몽골날짜()` 로 모았고
    계약 산문(C0 §4-3 ①③ · 발주_게임모듈 §158)에 IANA 이름을 박았다.
    🔴 **원 지적보다 손실이 큰 자리는 리터럴 사본이 아니라 기기 시계였다.** 서버에서 과제를 못
    받은 갈래(토큰 없음·오프라인·빈 큐 — 코드 주석이 몽골 회선에서 "상시"라고 적어 둔 상태)에서
    `그날` 이 기기 날짜로 남고, 시간대가 어긋난 폰은 `제출로그.다음시도번호` 가 **남의 날 바구니**를
    센다. 그 수가 `attempt_no` 로 서버 행에 실려 `/progress` 의 `retry_count` 가 되므로 첫 시도가
    재시도로 저장되면 사후에 못 고친다. 같은 파일 6줄 아래 주석이 이미 "기기 시계로 끊으면 몽골
    아침에 어제 것이 오늘이 된다"라고 적어 두고 그 위에서 기기 시계를 쓰고 있었다.
    🔑 `tools/배달왕복시험.js` 는 **일부러 리터럴을 남겼다** — 대조자가 `시간대` 를 가져다 쓰면
    그 상수가 틀린 날 시험도 같이 틀려 초록이 된다(회귀의 리터럴 금지는 출하 코드에만 건다).
    회귀 4 신설(451/451 · 기준선 447) · **변이 5/5**. 🔴 그중 ④(`몽골날짜` 호출만 지우고 import
    는 남기기)가 **처음엔 통과**했다 — `/몽골날짜/` 가 import 줄에 매치됐다. `/몽골날짜\(/` 로
    좁혀 다시 잡았다(`d2fdf26`). ⛔ 리허설 재배포·실왕복 미실행 · ⛔ 운영 미배포.

---

## ② 지금 위험 — 소급은 되지만 사고가 먼저다 (6건)

데이터는 안 잃는다. 대신 열어 두면 사고가 난다.

15. ~~**폐기한 토큰이 살아 있다**~~ — `session_alive()` 는 **RLS 정책 안에만** 살았다.
    `events`·`tasks`·`uploads`·`corrections`·`progress` 는 `service_role` 이라 RLS 를 우회하므로
    다섯 곳 다 `auth_user_id` 만 보고 통과시켰다(같은 subquery 가 바이트 단위로 다섯 벌).
    비밀번호를 초기화해도 옛 토큰으로 읽기·쓰기·업로드 서명이 계속됐다.
    ✅ **해소**(talk `ddaf144`) — 조건을 `lib/토큰.js` 의 `살아있는학생()` 하나로 두고 다섯
    호출부의 `where` 를 갈아끼웠다(왕복 수 그대로). `iat` 는 **폐기된 적 있는 계정에서만** 보고,
    값이 있는데 못 읽으면 비교가 `null` 이라 거부된다 — 그 갈래는 JS 가 아니라 SQL 이 정한다.
    🔑 처방은 이미 `L0_스키마.sql:1335` 에 **주석으로** 있었다("Edge Function 은 같은 판정을
    자기 손으로 한 번 더 해야 한다"). 다섯 곳 다 안 했다 — 발화하지 않는 처방은 안 돈다.
    회귀 8(443/443)·변이 3/3 · `tests/세션폐기.test.js` 가 **SQL 정본을 파싱해 대조**한다.
    ⛔ 실왕복·TS 타입검사 미실행(리허설 배포 필요 · 이 기계에 deno 없음) · ⛔운영 미배포.
    🔴 선후 조건: `active`·`revoked_before` 는 `20260806234000_staff_c7.sql` 이 넣는다 —
    그 조각 없는 DB 에 이 함수들을 올리면 **전 요청이 500** 이다.
16. **철회 뒤 수집 0건이 미보장** — 기기 `occurred_at` 으로 동의를 판정하고 서명 URL 이 1시간
    산다. ✅ **코드는 이미 닫혔다**(talk `8ca48be` — `consent_id` 결속 포함). **적용만 남았다**
    → 세션보드 「외부 검수문 흡수」 줄의 🔵인계(원격 SQL 적용 + Edge 3종 배포).
17. ~~**검수자에게 원문 개인정보가 그대로 열린다**~~ — `display_name` 만 숨기고 `body_original`·
    `audio_ref`·`task_snapshot` 전문을 연다. L0 §3-1 이 "원문·음성에 이름·전화가 우발적으로
    들어온다"고 스스로 인정한다.
    ✅ **줄일 수 있는 만큼 해소**(talk `20260807190000_review_c8`) — 🔑 **좁히는 자리를 정책이
    아니라 판으로 옮겼다.** RLS 는 행 단위라 열을 못 좁히고, 게다가 검수 대시보드는
    `service_role` Edge Function 뒤에 서서 **RLS 를 우회한다**(②-15 와 똑같은 자리) — 즉
    정책만 손보면 고친 것처럼 보이고 아무것도 안 막는다. `engine.review_queue` 뷰가
    **12열만** 내보내고 넓은 옛 정책 `inspector_queue_submissions` 는 지웠다(정본이 둘이면
    넓은 쪽이 이긴다). 빠진 11열은 `tests/검수큐.test.js` 가 **사유와 함께** 든다 —
    새 열이 붙으면 「허용도 사유 붙은 제외도 아님」으로 빨개진다(반대방향 장부 · `182d5e1` 꼴).
    🔴 **그런데 이 항목은 열 목록으로 다 못 푼다** — `audio_ref`·`transcript` 는 **검수 대상
    그 자체**다. 학생이 거기서 제 이름을 말하면 검수자는 그걸 듣는다. 그래서 고친 것은
    「검수자가 개인정보를 안 본다」가 아니라 **그렇게 적혀 있던 문장**이다(L0 §4-5 에 ②-1 신설:
    안 여는 층 / 줄일 수 없는 층 / 그래서 남는 통제 셋으로 갈랐다). 남은 통제 = 동의(제3자
    열람 고지) · 수탁 계약(NDA) · 조회 감사 — **앞의 둘은 유호님 판정 자리다**(아래 §②-17 남은 반쪽).
    🔑 곁들여 셋: ①`redaction_result` 는 *가려낸 식별자 자체*를 담는 칸이라 그것을 검수자에게
    주는 것은 비식별을 안 한 것보다 나쁘다 ②철회분(`audio_deleted_at`)은 뷰가 아예 뺀다
    (⚠음성 축만 — 쓰기·번역 철회는 ②-16 몫) ③확인 쿼리의 「새는 권한」 검사가 `pg_tables`
    만 봐서 **뷰는 원리적으로 사각**이었다 — engine 첫 뷰를 내면서 `pg_views` 를 합쳤다.
    회귀 8·**변이 5/5** · 471/0 · ⛔리허설·운영 미적용(원격 SQL = 유호님 승인 자리).
18. ~~**비밀번호 초기화가 경쟁 상태에서 깨진다**~~ — GoTrue 와 DB 를 한 트랜잭션에 못 묶는데
    부분 실패 복구 상태가 미정의였다("감사는 성공·옛 비밀번호는 생존"이 가능).
    ✅ **해소**(talk `0a2aeb5`) — **순서를 뒤집었다.** GoTrue 를 먼저 갈고 DB 를 나중에 쓴다.
    묶을 수 없으면 남는 것은 순서뿐이고, 순서가 곧 「반쯤 실패했을 때 남는 상태」다. 이제
    실패는 「비번은 죽었고 임시번호는 안 나왔다」 — 원장이 한 번 더 누르면 복구된다.
    대가는 그 창에서 죽으면 감사 행이 **없는** 것이고, 「빠진 기록」이 「거짓 기록」보다 낫다.
    회귀 = 소스 순서 검사(변이 4/4 중 ②가 이 뒤집기를 잡는다).
19. **학생번호 5회로 원격 잠금** — `student_code` 는 비밀이 아닌데 뒤 4자리 5회 오답이면
    잠긴다. 학생마다 원장 수동 해제를 강제할 수 있다.
    🔴 **실측하니 더 나빴다**: 해제한다는 그 `/reset` 이 `auth_user_id is not null` 로 걸러
    **계정이 아직 없는 학생 = 잠긴 바로 그 학생만 정확히 빠져나갔다.** 즉 출구가 문서에만
    있었고 되돌리는 길은 DB 직접 수정뿐이었다(왕복시험도 `update` 로 풀고 있었다).
    ✅ **출구 해소**(talk `0a2aeb5`) — 계정 없는 학생에게는 임시번호 대신 잠금 해제로 답한다
    (`unlocked:true` · 감사 `learner.signup_unlock`). 실왕복 36/36 이 「해제 뒤 앱에서 다시
    등록된다」까지 잰다.
    ⏳ **남은 반쪽 = 유호님 판정**(아래 §②-19 남은 반쪽). 잠금이 여전히 **계정** 단위라
    공격자가 학생 수만큼 원장 손을 부를 수 있다 — 「영구 벽돌」은 사라졌고 「손이 든다」가 남았다.
20. ~~**`task_snapshot` 이 정답까지 학생에게 반환된다**~~ — `/tasks` 응답이 답안지가 된다.
    퀴즈가 붙는 순간 문제가 된다.
    ✅ **해소**(talk `82da5aa`) — 통로에 **허용 목록**을 놓았다(`lib/오늘과제.js` 의 `학생공개키`
    하나 · `/tasks` 가 그것을 부른다). 🔑 **차단 목록으로 잡지 않았다**: 앞으로 생길 키 이름을
    미리 알 수 없어 못 적은 이름이 그대로 새고 증상이 「통과」다. 허용 목록이면 새 키의 기본값이
    「안 나감」이고 증상은 **화면이 비는 것**이라 그날 사람이 온다.
    🔴 **오늘 새는 양은 0 이었다** — 생산자가 따라말하기뿐이라서지 통로가 막혀서가 아니다.
    막지 않으면 게임 모듈이 붙는 날 응답이 답안지가 되고, 그때 증상은 **정답률이 올라가는 것**
    뿐이라 아무도 안 짖는다(그 뒤 수집분은 「답을 볼 수 있었다」가 섞여 학습 신호로 못 쓴다).
    🔑 **소급 손실 0**: 앱이 되싣는 제출 행에서만 그 칸이 빠지고, 정답 원본은 **배정 행**
    (`task.assigned` 의 `submissions.task_snapshot`)에 그대로 남는다 — 제출은 `task_ref` 로
    그 행에 붙으므로(①-11) 채점 재계산은 그 원본으로 한다. ⛔ 이 목록을 넓혀 정답을 다시
    내보내는 것으로 「제출 스냅샷이 비었다」를 고치지 않는다.
    회귀 7(471/0) · 변이 6/6 · ✅**리허설 실측**(유호님 승인 · `tasks` **v5 ACTIVE** · 배달왕복
    **75/75** · talk `ad8adae`) — 정답을 든 배정을 **새로 심어** 실제 통로로 쟀다. 🔴 고쳐서는
    못 잰다: `engine.reject_original_overwrite()` 가 스냅샷 수정을 막는다(그 불변 트리거가
    리허설에서 살아 있다는 것도 이때 함께 확인됐다). ⛔운영 미배포.
    🤝 `local_b170e8dc` 와 동시 선언 → 둘 다 물러난 교착을 끊고 내가 받되 **그쪽 안(허용 목록)**
    을 그대로 썼다.

### §②-17 남은 반쪽 — 유호님 판정 1건 (검수자는 제3자다)

열 목록으로 지울 수 있는 것은 다 지웠다. 남은 것은 **검수자가 학생의 목소리를 듣는다**는 사실
자체고, 그건 검수의 정의라 기술로 못 없앤다. 지금 상태를 정직하게 적으면:

- 검수자는 **채용 예정**(몽골 검수자 파일럿 2027-02)이라 **오늘 듣는 사람은 0명**이다.
- 동의문은 유호님 확정으로 **두 문장**이다(D7). 거기에 「제3자가 듣는다」가 **없다.**
- 조회 감사(`staff_access_log`)는 표만 섰고 쓰는 주체(검수 Edge Function)가 아직 없다.

| 안 | 무엇을 하나 | 얻는 것 | 잃는 것 |
|---|---|---|---|
| **㉮ 채용 전에 둘 다 세운다** | ①동의문에 제3자 열람 한 조각 ②검수자 수탁 계약(NDA) 서면 | 첫 검수 전에 근거가 선다 — **동의는 소급이 안 된다**(이미 낸 음성은 다시 못 물어본다) | 두 문장 원칙에 한 조각이 붙는다(유호님이 지운 그 열거로 되돌아갈 위험) |
| ㉯ 채용 시점에 몰아서 | 지금 아무것도 안 한다 | 지금 손이 0 | 파일럿 학생의 **이미 쌓인 음성**이 동의 밖이 된다 |
| ㉰ NDA 만 | 계약만 맺는다 | 사람 쪽은 묶인다 | 학생이 **모른 채** 남에게 들린다 — 통제가 아니라 은폐다 |

**내 판정 = ㉮**, 단 동의문은 **열거를 늘리지 않는다.** 두 문장 체제를 지키면서 「SYNK LAB 과
검수를 맡은 사람이 듣습니다」 정도가 기존 문장 안에 들어가면 조각이 안 는다. 근거는 하나다 —
**동의는 소급이 안 되는 유일한 통제**고, 나머지 둘(NDA·감사)은 나중에 붙여도 과거분이 안 죽는다.

**도전안** — 검수 큐에 **비식별을 먼저 태운다**(`redaction_ver` 가 이미 계약에 있는 칸이다).
그러면 「사람이 원문을 듣는다」 자체가 줄어 동의 조각이 가벼워진다. **지금은 기각** — 음성
비식별기를 세우는 비용이 파일럿 규모(10~20명)의 몇 배고, 전사 오류를 비식별기가 한 겹 더
얹으면 **검수가 재려던 발음 오류를 그것이 지운다.** 학생 수가 수백이 되면 그때가 그 안의 자리다.

### §②-19 남은 반쪽 — 유호님 판정 1건 (잠금을 무엇에 걸 것인가)

출구는 생겼다(위 19). 남은 것은 **잠금이 계정에 걸린다**는 것 — 학생번호는 순번이라 아무나
뒤 4자리를 5번씩 틀려 학생 수만큼 잠글 수 있고, 그때마다 원장이 한 명씩 풀어야 한다.
개원 첫 주는 전원이 첫 등록이라 그 창이 가장 넓다.

| 안 | 무엇을 하나 | 얻는 것 | 잃는 것 |
|---|---|---|---|
| **㉮ 그대로 + 원장 통로를 넓힌다** | 잠금은 영구 유지, 원장 화면에 「잠긴 학생 목록 + 한 번에 풀기」 | 브루트포스 방어를 **한 톨도 안 깎는다** | 공격이 계속되면 원장이 계속 눌러야 한다(손은 들지만 학생은 안 막힌다) |
| ㉯ 시간 감쇠 | 5회 뒤 15~30분 지나면 자동 해제 | 원장 손이 아예 안 든다 | 뒤 4자리 1만 가지에 **하루 480회**를 내준다 — 한 학생을 3주면 뚫는다 |
| ㉰ 둘 다 | 감쇠 + 목록 | 편하다 | ㉯의 손실을 그대로 안는다 |

**내 판정 = ㉮.** 이 게이트가 지키는 값은 **뒤 4자리 하나뿐**이라(1만 가지) 시도 예산을
늘리는 순간 그게 유일한 방어선에서 깎여 나간다. ㉮ 의 비용은 「원장이 버튼을 누른다」이고
그건 되돌아오는 비용이지만, ㉯ 의 비용은 **계정 탈취 확률**이라 되돌아오지 않는다.
공격이 실제로 오면 그때 IP 층을 얹는 것이 순서고, 지금 그 층은 아직 필요 없다.

**도전안** — 잠금 자체를 없애고 뒤 4자리를 **학원이 발급한 비밀**로 바꾼다(크루카드에 이미
번호를 찍고 있다). 그러면 1만 가지 문제가 사라져 시도 제한이 급소가 아니게 된다. 대신
「학생이 아는 것」이 하나 늘어 개원 첫 주 창구 부담이 는다 — 지금 판단으론 개원 뒤 과제다.

---

## ③ 나중 — 소급 가능 (요약)

문서 판본 갈라짐(c3/c6/c8 세 갈래) · 동결 상태 표기와 실물 불일치 · 오류 HTTP 상태 3중
(`CONSENT_MISSING` 이 409/403/200) · 확인 SQL 이 열 **존재**만 보고 타입·정의를 안 봄 ·
업로드 고아 파일 정리 규칙 부재 · 이미지 업로드가 막다른 통로 · `exam.result` 빈 사건 ·
`data_use.granted` 승격 계보 · `pipeline_jobs`·`daily_activity` 가 소비자 없는 선행 구축.

## ④ 유호님 확정과 충돌 — 결정이 아니라 문서 오류 (즉시 정정)

> ✅ **2026-08-07 처리 완료** — 정본을 열어 대조한 결과 **두 항의 결론이 서로 달랐다.**
> 아래 첫 항은 **이 문서 쪽이 틀렸다**(정정 대상은 L0 가 아니라 이 줄이었다).

- ~~**L0 §4-5 가 검수 대시보드를 Expo 앱 역할 화면으로 확정**했다. 닫힌 정본은 「앱 밖 웹」이고
  🚫재제안 금지다.~~ → 🔴 **이 항이 오류다. L0 §4-5 가 맞다.**
  정본 `SYNK-talk/docs/P0_제품계약.md:56` 이 확정표에 그대로 싣고 있다 — 「**검수 대시보드는
  Expo** · 유호 08-06 "expo로 해줘" · 앱 안 역할 화면, PC 는 Expo web, **한 코드베이스**」.
  🚫재제안 금지가 걸린 쪽은 **「앱 밖 웹 도구」** 이고, 이 줄은 그 금지를 정확히 뒤집어 적었다.
  🔴 **F130 과 같은 실수의 3회차다**(원장 탭·검수 대시보드가 매번 같이 뒤집힌다) — 원인도 같다:
  메모리 인덱스의 요약 한 줄(`🚫…검수 대시보드「앱 밖 웹」`)에서 **🚫가 무엇에 걸렸는지**를
  거꾸로 읽고 정본을 안 열었다. 실행됐으면 유호님 확정을 뒤집는 개정이 L0 에 들어갔다.
- ✅ **L0 §4-1 ↔ §7 인증 코드 정체 충돌** — 이 항은 맞았다. §7 이 "코드는 `student_code`가
  **아니라** 별개의 고엔트로피 비밀"이라고 §4-1 을 인용하며 §4-1 을 부정하고 있었다.
  **정정 완료** — §7 ②를 §4-1(✅유호 확정 08-06)에 맞췄다. 🚫`student_code` 고엔트로피화는 기각.
- ✅ **곁들여 하나 더 나왔다 — L0 §4-2-3 ↔ §4-2-2**(심문 원문은 지목했는데 이 문서가 안 옮겼다).
  §4-2-3 의 복구 절차 ②가 "`updateUserById`로 **임시 비밀번호 설정**"이었다 — 바로 위 §4-2-2 가
  명시로 기각하고(「그건 그 계정에 영구히 발급된 두 번째 비밀번호」) 물리
  (`supabase/functions/auth/index.ts`)가 이미 해시 보관으로 선 것이다. **정정 완료**(ⓐGoTrue 난수
  → ⓑDB 해시 · ②-18 의 순서까지 못 박음). 셋 다 **문서만** 바뀌었다 — 코드·스키마 변경 0.

---

## ✅ 유호님 확정 (2026-08-07) — WAV 로 간다 · AGC off 도 같은 커밋

「**wav 로 하자**」 + **상시 지시**: 「모든 수집·기능 설계는 **당장 개원에 쓸 수 있도록**,
수집 방법도 **개원에 바로 쓸 수 있도록**」.
🚫 내가 낸 「먼저 업로드 성공률을 재고 그 다음 전환」 **순서는 기각**됐다 — 실측은 전환을
미룰 이유가 아니다. 재는 것과 만드는 것을 같이 한다.

실측 프리셋(`node_modules/expo-audio/src/RecordingConstants.ts:69-73`):
현행 `.m4a`·44,100Hz·**2ch**·128,000bps = **16 KB/s** → WAV 16k/16bit/mono = **32 KB/s** (정확히 2배).

아래는 확정 전 판정 재료로 남긴다.

**음성 포맷을 m4a/AAC → PCM WAV 로 바꿀 것인가.**

- **왜 지금인가**: ① 14건 중 유일하게 저장소 밖이 바뀐다. 나머지 13건은 제가 그냥 합니다.
- **비용**: Expo 기본 녹음기는 raw PCM 을 안 준다 → 새 네이티브 의존성 + **dev build**
  (Expo Go 로는 더 이상 못 열고, 폰에 개발 빌드를 한 번 설치해야 한다). 확정 경로는
  A-2(스트림 캡처 + 헤더 조립)이고 m4a 후처리는 🚫 — 압축이 이미 지운 신호는 안 돌아온다.
- **안 하면**: 발음 판정·음소 정렬의 원신호가 첫날부터 손실 압축본이다. 나중에 WAV 로 바꿔도
  그 전에 쌓인 것은 영구히 압축본이다.
- **판정**: **하라.** 단 착수 시점은 「운영 첫 제출 전」이면 되고 오늘일 필요는 없다 —
  ①의 나머지 13건이 dev build 없이 먼저 끝난다.

---

## ⏳ 결정 요청 (2026-08-07) — ① 이 실제로 기다리는 c9 는 **이름 하나**다

**한 줄로**: 「학생이 그날 나간 것을 **실제로 보고·들었다**」는 사건 이름 하나를 계약에 넣을까요.

**왜 유호님께 올리나** — 필드를 하나 늘리는 것은 제가 하지만, **계약 판을 c8→c9 로 올리는 것**은
두 저장소가 같은 바이트로 나눠 갖는 파일을 바꾸는 일이라 착수 승인이 필요합니다(`발주_게임모듈.md`
§6-5 #4 와 **같은 승인 한 번에 묶으면 됩니다** — 지금 거기 걸린 것은 게임모듈 ③ 하나입니다).

**이 이름 하나가 여는 것 — ① 의 남은 자리 전부**

| 어디 | 지금 | 이 이름이 서면 |
|---|---|---|
| ①-2 재생 완료·자기 목소리 되듣기 | 기기에만 있다가 사라진다 | 「들었는데 못 따라 했다」와 「듣지도 않았다」가 갈린다 |
| ①-12 전달 증거 남은 절반 | 전날 밤 배치가 「전달했다」고 쓴다(추정) | 네트워크 실패가 「전달 완료」로 학습되던 것이 멈춘다 |

**왜 지금인가** — 소급이 안 됩니다. 「그날 학생이 열었는가」는 그날에만 알 수 있고, 나중에 필드를
만들어도 그 전 기간은 영원히 빈칸입니다. 개원 첫 주가 정확히 이탈이 제일 많은 창입니다.

**비용** — 계약에 값 1개 · 앱 화면에 이미 있는 상태(`들었다`·「내 목소리 듣기」)를 사건으로 내보내는
배선. **새 화면 0 · 새 표 0 · dev build 0.** 서버는 이미 그 모양의 사건을 받습니다.

**안 하면** — ①-2 는 넷 중 셋만 닫힌 채 남고, ①-12 는 열린 채로 운영이 열립니다. 나머지 ① 은
오늘 기준 **차단이 아닙니다**(①-4 는 대화 사건 생산자가 0이라 챗봇이 붙는 날의 몫입니다).

> **도전안 — 이름을 새로 짓지 말고 `correction.viewed` 의 뜻을 넓힌다.** 이미 「학생이 봤다」는
> 사건이고 값 추가가 0입니다. 🔴 **제 판정은 기각** — 그 사건은 `correction_id` 를 필수로 물고
> 있어(교정 없는 날의 재생·되듣기를 가리킬 대상이 없습니다) 넓히면 필수를 풀어야 하고, 그 순간
> 「교정을 봤다」와 「오디오를 들었다」가 한 칸에서 섞입니다 — c6·§6-6 이 두 번 잡은 형태입니다.

---

## 도전안 — 14건을 하나씩 고치지 마라

심문 6건 중 **5건이 같은 대안을 냈다**: 계약을 산문이 아니라 **기계 정본 하나**로 만들고
거기서 검증기·SQL CHECK·픽스처를 **생성**하라.

지금은 같은 판정이 산문(C0)·코드(`이벤트검증.js`)·DB(CHECK) 세 곳에 따로 적혀 있다.
F179 가 정확히 그 형태였다 — 세 층이 서로 다른 계약을 믿었고, 12/12 초록이 거짓이었다.
개별 수리 14건은 그 갈라짐을 **14번 더 만든다.**

절충안(권장): 매트릭스를 새로 짓지 말고, `이벤트검증.js` 가 이미 선언한 "이 파일이 정본이다"를
**계약 JSON 쪽으로 한 칸 올린다.** 14건 수리를 그 한 파일에서 파생시키면 공사는 안 커지고
갈라짐만 막힌다.

## 한 수 더 — 반대 방향 검사 1개  ✅ **지었다**(talk `182d5e1`)

> `lib/이벤트검증.js` `선택필드` + `tests/이벤트검증.test.js`. 계약의 모든 필드는 ①어느 사건이
> 요구하거나 ②서버칸이거나 ③**사유와 함께 장부에** 있어야 하고, 셋 다 아니면 빨갛다.
> 실측: 걸어 보니 고아가 **23건**이었고 그중 셋이 위 3·10·13 이었다(나머지 21건은 사유와 함께
> 장부에 올렸다 — 그중 `valid_from`·`expires_at`·`user_confirmed` 는 「미구현(①-7)」이라 적혀
> 있어 장부가 **남은 구멍의 지도** 노릇도 한다). 변이 6/6 포획.

`이벤트검증.js:48-52` 는 이미 "없는 이름을 필수로 걸면 테스트가 빨개진다"를 기계로 강제한다.
그 짝인 **반대 방향** — 「계약에 있는데 **아무 사건도 요구하지 않는** 필드」를 빨갛게 하는 검사 —
를 같은 테스트에 걸면, 위 ①의 3·10·13(`task_snapshot`·`correlation_id`·`skill_ids`:
이름은 계약에 있는데 아무도 안 요구하는 상태)이 **구조적으로 다시 못 생긴다.**
새 도구 0개, 기존 테스트 파일 한 곳.
