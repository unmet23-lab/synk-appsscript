# 마찰 신호 장부

> **이 파일은 손으로 고치지 않는다** — `node tools/friction.js add|resolve`가 쓴다.
> 왜 있나: CLAUDE.md는 마찰 신호(유호님의 교정·거절, 내 반복 실수)를 "진화의 연료"로 쓰라고 하는데,
> 지금까지 그 신호는 **기록만 되고 세어지지 않았다.** 그래서 지침이 v6.10까지 오는 동안
> "어느 개정이 실제로 작동했나"를 잴 수단이 없었다(버전 충돌 9건을 스스로 "관측 합집합이라 하한"이라
> 적은 것이 그 증거 — 자기 사고 횟수를 정확히 모른다).
> 풀 eval 하네스는 1인 규모에 과하다. **세는 것**이 1인 규모의 eval이다.
>
> 종류 — `교정`(유호님이 결과를 고침) · `거절`(제안 기각) · `실수`(내 실수, 특히 반복) · `마찰`(절차·도구가 방해)
> 해소 칸이 비어 있으면 아직 살아있는 신호다. `/evolve` 1단계가 이 파일을 읽는다.

| ID | 날짜 | 종류 | 신호 | 해소 |
|---|---|---|---|---|
| F001 | 2026-07-17 | 교정 | 도구가 연결돼 있는데 실행 대신 가이드를 냈다 | memory/direct-execution-preference |
| F002 | 2026-07-28 | 마찰 | 정본 개정이 대외 문서에 전파되지 않는다(급여 v1.1→v1.3 동안 파생 3종이 v1.1을 인용한 채 남음) | 2026-08-03 tools/doc-graph.js + doc-propagation 훅 |
| F003 | 2026-07-31 | 교정 | "불가능" 프레임으로 답했다 — 무엇이면 되는지를 조사한 뒤 판정해야 한다 | memory/no-impossible-verdicts |
| F004 | 2026-07-31 | 실수 | 동시 세션 버전 충돌 9건(관측 합집합이라 하한) | tools/bump-version.js 채번 락 |
| F005 | 2026-07-31 | 마찰 | 결정 대기 큐 15건+ — 무엇부터 풀어야 할지 산출할 수단이 없다 | 2026-08-03 memory-graph --decisions(막힘> 위상정렬) |
| F006 | 2026-08-01 | 실수 | 내 미커밋 편집이 옆 세션 커밋에 쓸려 들어갔다 | CLAUDE.md v6.11 경로 지정 커밋 조항 + git-scope-guard 훅 |
| F007 | 2026-08-01 | 실수 | 가드가 조각만 검사해 200자 위반 2건이 그대로 통과(사람이 눈으로 잡음) | board-guard 결과 기반 검사 |
| F008 | 2026-08-02 | 실수 | 컨텍스트 1차 진단이 상시 로드분만 재서 진짜 범인을 놓쳤다 | CLAUDE.md v6.10 「어느 층을 잴지 먼저 정한다」 |
| F009 | 2026-08-02 | 실수 | 안전 테스트 368건 통과 중에 익명 공개 엔드포인트가 열려 있었다 | /deploy 보안 게이트(security-review 4단계 편입) |
| F010 | 2026-08-02 | 실수 | 월키 Date 오염 4번째 — 같은 원인이 네 번 재발 | [v9.144] 키 열 목록을 sheetSkeleton_에서 도출 — 손 목록이 사라져 새 시트가 자동으로 덮인다(회귀 10건) |
| F011 | 2026-08-03 | 실수 | 그래프 도구가 자기 설명 문서의 코드 스팬 예시를 진짜 엣지로 읽어 없는 노드 4개를 만들었다 | tools/memory-graph.js stripCode + tests/메모리그래프.test.js 회귀① |
| F012 | 2026-08-03 | 실수 | 인덱스에 판정 본문을 써서 MEMORY.md를 비대화시켰다 — 옆 세션이 압축 1순위로 지목(내가 만든 도구의 첫 실사용 신호) | memory-index-guard 훅(새 줄 250자 상한·실측 75분위) + 회귀 12건 |
| F013 | 2026-08-03 | 실수 | 옆 세션이 git add 해둔 3파일을 내 커밋(e2b522b)이 쓸어 담았다 — F006의 반복(방향만 반대). 원인=git 스테이징은 저장소당 1개인데 세션은 여럿 | git-scope-guard 훅(add -A/commit -a 차단) + 한 호출 안 커밋 규약 |
| F014 | 2026-08-03 | 실수 | 옆 세션 커밋에 내 스테이징 파일이 또 쓸려 들어갔다(F006 재발·2번째) — git add와 commit을 별개 호출로 나눈 사이에 피어가 커밋. 인덱스는 세션 간 공유 상태다 | CLAUDE.md v6.11 경로 지정 커밋(인덱스 무시) 1순위 명문화 |
| F015 | 2026-08-03 | 실수 | git add -A 가 내 작업이 아닌 untracked 파일 3건(유호님 git 밖 정본·자격증명 폴더 안)을 커밋에 담아 push까지 갔다 — F006/F013/F014와 원인 계열은 같으나 경합이 아니라 명령 자체의 범위 과잉. 자격증명은 .gitignore 한 줄이 막았다 | git-scope-guard 훅 + .gitignore로 지킨다 조항(의도는 명령을 못 이긴다) |
| F016 | 2026-08-03 | 실수 | git diff --cached --stat 을 tail -5 로 잘라 봐서 앞쪽에 있던 남의 파일 3건을 못 봤다 — v6.10 「컨텍스트도 자원」이 읽기 절약을 요구하는데 검사까지 자르는 습관으로 번졌다 | CLAUDE.md v6.11 「읽기를 좁히는 것과 검사를 자르는 것은 다르다」 |
| F017 | 2026-08-03 | 실수 | bump-version이 이력 체인을 base 기준으로 써서 옆 세션이 방금 추가한 항목을 덮는다 — v9.141(옆)과 v9.142(내) 동시 작업에서 Code.js SYNK_VERSION 주석의 [v9.141] 항목이 누락됐다. 충돌 해소 중 손으로 복원했으나, 충돌이 안 났으면 조용히 사라졌을 자리다. 채번기가 origin/master의 현재 이력 줄을 읽어 병합하도록 고칠 것 | bump-version mergeChain — origin 현재 줄과 병합(더하기만) + 삽입 기준을 체인 첫 항목으로 + 회귀 8건 |
| F018 | 2026-08-03 | 실수 | 마찰 장부 번호 충돌 — 두 세션이 각자 작업본만 보고 F015·F016을 동시 배정, 옆 세션이 F017로 재번호해 발각. 나지 않았으면 겹친 채 남았다 | friction.js nextId가 origin/master+로컬 브랜치를 훑는다(bump-version과 같은 범위) + 회귀 2건 |
| F019 | 2026-08-03 | 마찰 | 도구를 만들고 트리거를 안 걸었다 — 마찰 3건이 /evolve 기준(2건)을 넘겼는데 아무도 안 눌렀고, 훅 4종은 전부 PreToolUse(막는 게이트)라 무언가를 띄우는 층이 0이었다 | SessionStart 훅 + tools/rot-check.js(주기 스로틀) — 트리거 층 신설 |
| F020 | 2026-08-03 | 마찰 | F002를 절반만 해소하고 닫았다 — 파생 엣지는 심었지만 낡음을 판정할 시간축이 없어 급여 정본 v1.1 인용 3건이 그대로 살아 있었다(장치는 있는데 볼 눈이 없었다) | doc-graph 엣지 @버전(시간축) + 회귀 6건 — 낡은 인용 3건을 기계가 지목 |
| F021 | 2026-08-03 | 실수 | board-guard가 아무도 안 쓰는 표기(완료)로만 검사해 굵은 완료 줄을 전부 활성으로 셌다 — 활성 상한이 전체 상한처럼 굳어 새 세션이 자기 줄을 못 넣고 실제로 막혔다 | board-guard 장식 제거 판정 + 회귀 3건(실제 표기 **완료**로 검사) |
| F022 | 2026-08-03 | 실수 | doc-graph에 stripCode가 없어 표기법을 설명하는 문서의 코드 펜스 예시가 진짜 엣지로 샜다 — memory-graph에서 이미 밟은 함정을 다른 파일에서 두 번째로 밟았다(내가 쓴 문서가 내 도구에 걸렸다) | doc-graph maskCode(길이 보존) + 회귀 4건 — parseEdges·addEdge 양쪽에 적용 |
| F023 | 2026-08-03 | 실수 | 회귀 테스트를 실저장소의 낡은 인용 3건을 세도록 써서, 그 3건을 고치자마자 테스트가 스스로 깨졌다 — 회귀가 「버그가 아직 있을 것」을 요구하면 고치는 순간 빨간불이 되고 다음 사람은 테스트를 끈다 | 픽스처 기반으로 재작성(탐지 능력은 임시 파일로 못박고 실저장소는 거짓양성만 검사) |
| F024 | 2026-08-03 | 마찰 | 지침의 「git 밖」 예시가 네 번째로 낡았다(v6.4→v6.6→v6.9→08-03) — 이관이 한 걸음 나갈 때마다 목록이 반례가 되는데도 계속 목록으로 적었다 | CLAUDE.md v6.12 — 나열을 판별 규칙(git이 보느냐)+실측 명령(check-ignore/status --ignored)으로 교체 |
| F025 | 2026-08-03 | 실수 | 옆 세션(또는 도구)이 작업 트리를 clean 해 내 미커밋 테스트 파일과 codex/ 디렉터리가 함께 사라졌다 — 새 파일을 만들고 커밋까지의 노출 구간이 길었다 | git-scope-guard에 git clean 차단(dry-run만 통과·경로 예외 없음) + CLAUDE.md v7.1 「새 파일은 미추적 상태로 오래 두지 않는다」 + 회귀 8건 |
| F026 | 2026-08-03 | 마찰 | 「장치에 트리거 없음」 재발 — harness-export(이식 폴더 생성기)가 발동 장치 없이 남아, v6.12 개정 당일 바탕화면 이식 폴더가 v6.11인 채 하루 방치(그래프 3종 방치와 같은 유형, v6.12 조항 이전에 만들어진 백로그) | rot-check 주간 점검에 이식 폴더 스탬프 대조를 배선(warn) + 픽스처 회귀 3건 — 같은 커밋에 장치와 발동을 함께 담음 |
| F027 | 2026-08-03 | 실수 | 유호님이 의도적으로 배치한 폴더(ARC-AGI-3-Agents)를 확인 없이 저장소 밖으로 이동 — 「빈틈 선제 반영」을 유호님 작업 환경 변경에까지 적용했다. 파일 배치는 방향 결정이라 제안으로 올렸어야 함(가역이라 복구는 즉시) | CLAUDE.md v7.0 제안·선제 보완 조항 — 유호님이 배치한 파일·폴더·환경의 변경은 반영 말고 제안으로 |
| F028 | 2026-08-03 | 마찰 | /security-review가 내 미커밋 변경 대신 옆 세션이 방금 커밋한 문서 diff를 받았다 — 멀티 세션 저장소에서 「브랜치의 변경」이 origin 기준이라 남의 커밋이 잡히고 내 작업은 안 보였다. 초록이 나와도 내 코드를 본 초록이 아니다(층 착각) | 미해소 — 도구 결함이라 그 자리에서 못 고친다. 대책: 멀티 세션 저장소에서는 /security-review 결과를 받기 전에 그 diff가 내 파일인지 먼저 대조한다(이번엔 대조해서 잡았고 내 변경은 직접 검토했다) |
| F029 | 2026-08-03 | 교정 | 원인 진단을 포기하고 유호님께 넘겼다 — Chrome 확장 미연결에 8회 재시도 후 '경로를 접겠다'. 유호 교정 '게으른 판단이야'. 실제로 파니 30분 내 근본원인 특정: 브리지 방 주소=계정 UUID인데 앱은 새 계정(d70f4b79)·확장은 옛 계정(b63180e4). 교훈=재시도 실패는 '불가'가 아니라 '아직 안 판 것' | CLAUDE.md v7.1 「같은 방법의 재시도 실패를 불가로 번역하지 않는다 — 층을 바꿔 근본원인 후보 1개 특정」 (memory 프로즈로는 재발해 지침 승격) |
| F030 | 2026-08-04 | 실수 | Glide 조립 사전 안내가 문서만 보고 작성돼 틀렸다 — 「카드 4종을 지우면 화면이 빈다」고 했으나 실제 「내 기록」 상세엔 몬스터한마디·학업추세가 함께 있어 안 빈다(출석달력 추가 불필요). 교훈=조립가이드는 지도지 현재 상태가 아니다, 조작 전 라이브 컴포넌트 목록을 먼저 뜬다 | CLAUDE.md v7.15 조항 「현재 상태는 현재를 읽어 판정한다」 — 조립가이드는 지도지 현재 상태가 아니다. 조작 직전 라이브 컴포넌트 목록을 먼저 뜬다 |
| F031 | 2026-08-04 | 실수 | 다른 세션이 등록해둔 GitHub Secret(APPSSCRIPT_DEPLOY_KEY)을 확인 없이 덮어썼다 — 멀티 세션 조율 단위에 「repo 밖 공유 자원」이 있는데 GitHub 설정(Secret·배포키)은 그 목록에 없어 열기 직전 확인(gh secret list)을 안 했다. 기능은 안 깨졌으나(새 키 쌍 정합·수동 실행으로 발송 완료 검증) 남의 배포키가 고아가 됐다 | CLAUDE.md v7.2 — 조율 단위에 GitHub Secret·배포키 명시 + 덮기 전 gh secret list로 갱신 시각까지 확인 |
| F032 | 2026-08-04 | 실수 | 타 세션이 공유 GitHub 시크릿(APPSSCRIPT_DEPLOY_KEY)을 gh secret list 확인 없이 덮어써 배포키가 고아가 됐다. 시크릿은 존재 여부가 아니라 갱신 시각까지 봐야 덮어쓰기가 보인다 — 만약 TELEGRAM_BOT_TOKEN이 덮였다면 발송·수거 봇이 갈라져 증상이 「조용함」뿐이었을 것 | CLAUDE.md v7.2 — 같은 조항(존재 여부만 보면 덮은 게 안 보이고 증상은 「조용함」뿐) |
| F033 | 2026-08-04 | 실수 | Gemini Spark 가격·지역을 5월 기사 기준으로 단정(Ultra $100·미국만) — 유호님이 정정(Pro $20·한국 출시). 내 검색 결과 안에 이미 '인도 Pro 대상' 반증이 있었는데 대조하지 않았다. 빠르게 변하는 제품은 검색 결과가 아니라 그 날짜를 봐야 한다 | 같은 v7.15 조항 — 빠르게 변하는 제품은 검색 결과가 아니라 그 날짜를 본다(간접 증거로 현재를 단정하지 않는다) |
| F034 | 2026-08-04 | 실수 | 정본 판번호를 올리면서 그 버전을 리터럴로 단언한 tests/문서그래프.test.js를 같이 안 고쳐 master를 두 번(v1.5·v1.6) 빨갛게 만들고 옆 세션 v9.151 clasp 배포를 세웠다. 옆 세션 두 곳이 먼저 발견해 알려줬다. 대책 = 리터럴을 걷어내고 본문 대조로 바꾼 뒤(d26f6e4) 같은 파일에 재발 방지 가드 신설. 남은 교훈 = 문서만 바꾼 커밋도 CI를 탄다(08-04 경로 필터 제거) | 타 세션이 원인 제거(문서그래프 테스트의 정본 버전 리터럴을 본문 대조로 교체·d26f6e4)+같은 파일에 재발 가드 신설 · 「문서만 바꾼 커밋도 CI를 탄다」는 워크플로 경로 필터 제거로 성립 |
| F035 | 2026-08-04 | 실수 | git status --short로만 범위를 확인하고 커밋했는데, 그때 저장소가 다른 세션의 interactive rebase 진행 중이었다. --short는 'rebase in progress'를 표시하지 않는다 — 가장 위험한 저장소 상태에 대해 눈이 먼 채로 범위 검사를 한 셈. 커밋이 detached HEAD로 들어가 rebase --abort 시 유실될 뻔했다(branch 안전핀+패치로 구제). 대책 = 커밋 전 확인은 git status 전문(또는 .git/rebase-merge 존재 확인)으로. CLAUDE.md의 '공유 파일 편집 전 git status'가 --short를 허용하는 것처럼 읽히는 것도 개정 후보 | CLAUDE.md v7.2 — 「공유 파일 편집 전·커밋 전 git status 전문」(--short는 rebase 진행 중을 감춘다·일상 조회는 --short 무방) + 기계는 git-scope-guard 규칙④(진행 중 커밋 차단)로 선행 해소 |
| F036 | 2026-08-04 | 실수 | 내가 넣은 회귀가 repo 밖(~/.claude/.../memory) 경로를 읽어 CI를 깨뜨렸고 배포 세션을 차단했다. 로컬 638 pass를 초록의 증명으로 삼은 것이 오류 — 정본이 repo 밖에 사는 프로젝트에서 실저장소 테스트는 CI에서 반드시 깨진다. 대책=탐지는 픽스처가 지고 실저장소 검사는 존재할 때만(skip 노출)+가짜 HOME으로 CI 모사 검증 | tools/test-ci.js 신설(빈 HOME·TZ=UTC로 CI 모사) + /deploy 2단계 러너 교체 + CLAUDE.md v7.2 「repo 밖 환경에 기대는 검사는 CI에서 반드시 깨진다·실저장소 검사는 skip으로 드러낸다」 — 탐지 능력은 탐침 실험으로 실측(평소 러너 pass 1 / test-ci fail 1) |
| F037 | 2026-08-04 | 실수 | 타 세션 pull--rebase 시작→abort가 작업 트리를 HEAD로 되돌리며 내 미커밋 편집 2파일(contents_상담AI·세션보드)을 쓸어냄 — uncommitted-swept-by-peer의 새 변종(rebase-abort 스윕). 재적용+즉시 커밋(8e2204f)으로 봉합. 교훈=공유 체크아웃에서 편집 즉시 커밋은 코드에도 적용 | CLAUDE.md v7.2 — 「새로 만든 파일도 편집 중인 파일도 오래 미커밋으로 두지 않는다」(F025의 미추적과 F037의 추적-미커밋을 한 문장으로 병합·처방은 즉시 커밋으로 동일) |
| F038 | 2026-08-04 | 실수 | 리베이스 진행 중 다른 세션이 커밋해 detached HEAD 위에 얹혔고 인덱스가 꼬였다(F037). 범인은 부주의가 아니라 git status --short가 rebase 진행 중을 표시하지 않는 것 — 범위를 확인하는 습관이 오히려 상태를 가린다. 대책=git-scope-guard 규칙④(진행 중 커밋 차단)+회귀 4건 | git-scope-guard 규칙④(rebase 진행 중 커밋 차단)+회귀 4건 · CLAUDE.md v7.2가 프로즈 쪽 구멍(--short 허용처럼 읽힘)을 함께 막음 |
| F039 | 2026-08-04 | 실수 | 배포 보고에서 로컬 테스트 초록을 「초록」이라 진술했다 — v9.151 배포 시점 master CI는 실제로 빨간불(결정큐 ENOENT)이었다. 08-04에 두 세션이 같은 함정(로컬 홈엔 memory 폴더가 있어 통과)을 각각 밟음 = 개인 실수 아닌 시스템 결함. clasp-guard 배포 게이트는 로컬 스위트만 검사하므로 CI 초록을 보장하지 않는다. 처방 후보=테스트가 repo 밖 절대경로를 읽는지 검사하는 메타 회귀(원인 제거형) | tools/test-ci.js + /deploy 2단계 배선 + CLAUDE.md v7.2 「로컬 초록은 CI 초록이 아니다 — 초록이라 말하려면 그 층을 밝힌다」(clasp-guard는 로컬 스위트만 보므로 게이트가 아니라 이 층에서 잡는다) |
| F040 | 2026-08-04 | 실수 | 라이브 잔재 확인에 clasp pull을 써서 메인 작업본 Code.js가 라이브(v9.154)로 덮였다 — 옆 세션의 v9.155 커밋이 로컬에서 되돌아감(즉시 git checkout으로 복구). pull은 읽기가 아니라 작업본 덮어쓰기다. 확인은 편집기 파일 목록을 눈으로. | .claude/hooks/clasp-guard.js 규칙 0-A(clasp pull 차단)+CLAUDE.md v7.6 조항 |
| F041 | 2026-08-04 | 마찰 | 커밋으로 세션을 특정할 수단이 없어 타 세션 작업이 내 것으로 오귀속됐다(메모리 인덱스 압축 검수 건). author는 전 세션 공통 yuho·Co-Authored-By 모델명도 같은 모델 세션이 여럿이라 구분 불가·커밋 시각 인접성은 동시 4세션 환경에서 근거가 아님. 결과=엉뚱한 세션에 검수 회신이 가고 실제 작업자는 피드백을 못 받는다 | tools/githooks/prepare-commit-msg(Session-Id 트레일러)+CLAUDE.md v7.3 조항 |
| F042 | 2026-08-04 | 실수 | 마찰 장부 번호가 동시 발번으로 충돌했다 — 두 세션이 같은 순간에 seenElsewhere로 훑어 둘 다 F041을 만들었다(하나는 폐기·재번호). 훑기는 이미 기록된 번호만 보므로 「아직 아무 데도 안 쓰인 순간」을 원리적으로 못 덮는다. | tools/friction.js 채번 락(friction-F0NN 태그 예약·0caeafe) + 회귀 2건 |
| F043 | 2026-08-04 | 실수 | deploy-live 게이트를 contains(커밋 메시지, '[배포]')로 짜서 워크플로를 신설한 커밋 자신이 오발화했다 — 본문에 표식을 '설명'으로 적은 것을 시킨 것으로 읽었다. 말한 것과 시킨 것을 구별 못 하는 게이트는 게이트가 아니다 | startsWith(제목 첫머리)로 좁힘 + CLAUDE.md v7.4 「표식은 제목 첫머리만 인정한다」 |
| F044 | 2026-08-04 | 마찰 | `.claude/settings.json` 훅 등록 6건 전부가 로컬 절대경로(`C:/Users/…`·`/c/Program Files/nodejs/node.exe`)라 클라우드 세션(claude.ai/code)에서 가드 5종이 전부 안 돈다 — 훅 파일은 저장소에 있어 따라가므로 있는 것처럼 보이고, 실패 방향이 「통과」다(경로 부재=비차단 오류). v7.4는 조항으로만 막았고 이식성 수리는 미착수 | 훅 등록 6건을 CLAUDE_PROJECT_DIR+PATH node로 교체 · 실행 불가는 deny로 드러냄 · 회귀 tests/훅이식성.test.js 9건(변이 6/9 탐지) · CLAUDE.md v7.5 |
| F045 | 2026-08-04 | 실수 | 인코딩 원인을 charset 헤더로 단정했다가 재실측에서 뒤집힘(진짜 원인=Windows 셸이 인라인 문자열을 CP949로 인코딩). 조건 하나만 바꾼 대조를 처음부터 했어야 했다 | CLAUDE.md v7.10 조항(조건 하나씩 대조 + 전제 2) |
| F046 | 2026-08-04 | 실수 | 보드 줄을 아카이브로 옮기는 스크립트가 줄끝(CRLF) 차이로 삽입에 실패했는데 삭제는 성공해 줄이 통째로 유실됐다(즉시 복구). 지우기와 붙이기를 한 스크립트에서 할 때는 붙이기 성공을 확인한 뒤 지운다 — 파일 줄끝은 실측해서 쓴다. | tools/board-move.js(줄끝 실측·쓰기 전 검증·아카이브 먼저)+CLAUDE.md v7.8 조항 |
| F047 | 2026-08-04 | 실수 | 카드 제출 버튼이 죽어 있는 걸 회귀 10건이 전부 초록인 채 못 잡았다 — 문자열 존재만 검사하고 파싱·실행을 안 봤다. 유호님이 실사용 직전 물어봐서 발각 | tools/html-script-check.js + tests/HTML인라인스크립트.test.js(저장소 전체 일반화·옆 세션 crewcard분 353d5c4) |
| F048 | 2026-08-04 | 마찰 | rebase --continue가 unmerged 0인데도 계속 거부(상태 파일 어긋남) — status는 「all conflicts fixed」인데 continue는 거부. abort는 옆 세션 미커밋을 쓸어 금지라, 수동 커밋(bypass) + rebase --quit + branch -f 로 마무리했다 | 미해소 종결 — git 자체 결함(rebase 상태 파일 불일치)이라 훅이 낄 자리가 없다. 대책: abort 는 남의 미커밋을 쓸어 금지이므로, 수동 커밋(bypass) + rebase --quit + branch -f 로 마무리한다(이번에 그렇게 빠져나왔다) |
| F049 | 2026-08-04 | 실수 | v7.6 clasp pull 차단이 검색 명령을 막았다 — grep 인용 인자 안의 문구를 실행 명령으로 읽음(08-01 heredoc 오탐과 같은 계열인데 그때 heredoc만 덮고 인용 인자를 안 덮었다). 가드를 넣은 당일 내가 그 가드에 막혀 발견. | .claude/hooks/clasp-guard.js stripNonExecutedText 인용 스팬 치환 + 회귀 2건 |
| F050 | 2026-08-04 | 실수 | 파이썬 heredoc으로 테스트 파일을 편집하다 이스케이프가 꼬여 두 번 깨뜨렸다(같은 함정 반복) — 코드 파일 편집은 Edit 도구로, 스크립트 생성은 피한다 | 위와 같은 조항 — 코드 파일 편집은 스크립트가 아니라 Edit 도구로 |
| F051 | 2026-08-04 | 교정 | 지침에 이미 있는 「화면 확인은 텍스트 우선·스크린샷은 좌표 클릭용+최종 증명 1장만」(v6.10)을 어기고 한 세션에 20장+ 를 찍었다 — 유호님 교정. 07-31 448장에 이은 2회째라 프로즈로는 안 지켜진다는 것이 실증됐다 | screenshot-budget 훅(세션당 예산 사다리 4/9/차단)+회귀 8건(변이 2종 탐지)+CLAUDE.md v7.9 「기본값은 텍스트·클릭도 ref로」 |
| F052 | 2026-08-04 | 실수 | 인덱스 줄 길이를 awk(바이트)로 재고 훅 기준(JS 문자)과 다른 층에서 「250자 위반」이라 보고했다 — 한글 3바이트라 175자가 324로 보였다. 같은 사이클에 v7.10으로 「재는 층을 본다」를 박아놓고 몇 분 뒤 밟았다. | memory-index-guard --check 신설 — 훅이 쓰는 판정기를 그대로 CLI 로 노출해 재는 층을 하나로 묶었다(프로즈로 조심하라 대신 잘못 쓸 수 없는 통로). 실측 대조: 같은 MEMORY.md 가 훅 기준 3건 vs awk 바이트 기준 43건(14배). 회귀 3건 |
| F053 | 2026-08-04 | 실수 | 스크린샷 예산 훅이 있는데도 한 세션이 40장을 찍었다 — 판정층이 둘(settings.json case 필터+훅)이었고 둘 다 배치 도구(browser_batch·computer_batch)와 scroll을 몰랐다. 가드가 로직이 아니라 등록층에서 샌 F044와 같은 형태 | settings.json case 필터 제거로 판정층 단일화 + 매처에 배치 도구 등록 + 입력 트리 재귀 카운트 · 회귀 8건(매처 등록·판정층 이중화까지 검사·변이 4/4 탐지) · CLAUDE.md v7.11 「가드는 등록층에서 샌다」 |
| F054 | 2026-08-04 | 실수 | Bash 도구에 PowerShell here-string(@'...'@)을 써서 커밋 제목이 '@'로 깨졌다 — amend로 수리. F050(파이썬 heredoc 이스케이프)과 같은 계열: 셸이 둘인 환경에서 문법을 섞었다 | CLAUDE.md v7.11 세션 규약 「셸이 둘이다 — here-string은 PowerShell 전용, 코드 편집은 Edit 도구로」(F050과 한 조항으로 병합) |
| F055 | 2026-08-04 | 교정 | 권고를 「200~300턴에서 끊어라」로 줬는데 유호님이 두 번 되물었다 — 「턴」은 내가 일하는 단위지 유호님이 셀 수 있는 단위가 아니다(실측: 메시지 1개당 38턴). 비개발자 기준 = 유호님이 직접 셀 수 있는 단위(메시지 개수·화면에 보이는 값)로 환산해서 줘야 한다 | CLAUDE.md v7.13 소통 조항 — 판단 재료의 단위도 유호님이 직접 셀 수 있는 것으로 환산한다(턴 금지) |
| F056 | 2026-08-04 | 실수 | Bash에 PowerShell here-string을 보내 커밋 제목 첫머리에 @가 붙었다 (CLAUDE.md F054에 이미 적힌 함정을 밟음). 처방 = 멀티라인 커밋 메시지는 스크래치패드 파일 + git commit -F 로 통일한다 | 위와 같은 규칙6 — 처방(스크래치패드+commit -F)을 프로즈가 아니라 기계가 강제한다 |
| F057 | 2026-08-04 | 마찰 | context-budget 훅이 종료 조건 충족(미커밋 0)에도 매 턴 재발화 — 6턴 연속 동일 신호. 임계 초과만 보고 상태 변화를 안 봐서, 「끊을 지점」 알림이 오히려 컨텍스트를 늘린다(71%→72%). 처방 후보: 세션당 1회 스로틀, 또는 직전 발화 이후 커밋·상태 변화가 있을 때만 재발화. | 이미 해소돼 있었다 — context-budget.js 가 store.readStage 로 단계당 1회만 발화(장부만 안 닫힘) |
| F058 | 2026-08-04 | 실수 | 대조 단위를 「갈래」로 잡아 갈래 밖의 것을 3번 연속 놓쳤다 — 노션 대조 §2(자식 문서만)→§3 재대조(마케팅 갈래만)→삭제 직전 사이드바 전량 재독에서야 「이벤트 키트」 발견(저장소 적중 0/12·마케팅 인포 아래 3단). 목록·갈래가 아니라 트리 전량을 읽는다. | 같은 v7.15 조항 — 대조 단위를 갈래로 잡지 않는다. 목록·갈래가 아니라 트리 전량을 읽는다 |
| F059 | 2026-08-04 | 교정 | 대비 시각물에서 한쪽 축에만 색을 줘 「뭐가 뭔지 분간이 안 된다」는 교정 — 강조와 대비를 혼동했다 (memory viz-contrast-by-axis) | memory viz-contrast-by-axis 에 처방 존재(축의 양쪽에 각자 색·지목한 색은 값 고정하고 칠할 자리를 바꾼다). 디자인 판단이라 기계 불가 — 재료가 될 수 없으므로 닫는다 |
| F060 | 2026-08-04 | 실수 | Bash 커밋 메시지에 백틱을 넣어 코드 인용이 명령 치환으로 사라졌다(bash: mono: command not found). F056에 '멀티라인 커밋 메시지는 스크래치패드 파일 + git commit -F 로 통일한다'고 이미 적어 둔 함정을 그대로 밟았다 — F054·F056에 이은 3번째 셸 문법 사고. 고치려 했을 땐 이미 타 세션이 push한 뒤라 amend 불가(공유 브랜치 force-push는 메시지 한 단어보다 비싸다). 3번째이므로 프로즈가 아니라 기계로 막을 자리다 — 커밋 메시지에 백틱이 있으면 -F 를 강제하는 훅. | git-scope-guard 규칙6(커밋 메시지 안 백틱·명령치환·변수확장 차단, 원본 cmd 검사)+회귀 3건·변이 3/3 탐지 + CLAUDE.md v7.13 「셸에 맡기지 말고 파일로 준다」 |
| F061 | 2026-08-04 | 마찰 | clasp-guard가 clasp 프로젝트를 구분하지 못한다 — 크루카드(별도 프로젝트) 배포가 메인의 무관한 미커밋(엔진_수집.js)·빨간 테스트에 막혔다. 배포 집합은 그 프로젝트의 디렉터리가 정하는데 가드는 항상 메인 .claspignore로 검사한다. 결과=정당한 배포에 BYPASS를 쓰게 된다(가드가 우회 습관을 만드는 경로). | clasp-guard 가 배포 대상 프로젝트를 판별해 검사 범위를 맞춘다(구문·미커밋·보안·테스트) + lib/clasp-project.js 공용 통로 + 회귀 17건·변이 4/4 탐지. 오탐(무관한 미커밋·빨간 테스트)과 미탐(크루카드 코드가 보안 검사 밖)을 함께 수리 |
| F062 | 2026-08-04 | 실수 | 라이브 왕복 오독 2건 — ①curl -L이 302를 따라가다 411을 내자 「스크립트에 닿지 않았다」고 보고했으나 초기 POST는 이미 실행돼 있었다 ②시트 TZ가 America/Los_Angeles라 오늘 오전 UB 제출이 '2026-08-03 18:42'로 보였고 그걸 「어제 잔재」로 읽었다. 같은 TZ 함정에 v9.163에선 코드가, 이번엔 내 눈이 물렸다. | 같은 v7.15 조항 — 최종 응답코드는 실행 여부의 간접 증거다(302 뒤 411이어도 초기 POST는 이미 돌았다). 시트 TZ 표시도 마찬가지로 원본이 아니라 표시층이다 |
| F063 | 2026-08-04 | 실수 | 스크린샷 예산이 같은 자리에서 5번째로 샜다 — 이번엔 훅 안에서 목록이 갈라졌다(액션 판정은 scroll을 아는데 도구 이름 판정은 몰랐다) + 매처가 단건 도구 3종(computer-use scroll·teach_step, chrome gif_creator)을 안 잡아 훅에 닿지도 않았다. 앞의 넷과 달리 사고 전 코드 독해로 잡은 첫 건. 처방=IMAGE_NAMES 하나에서 양쪽 판정 파생 + 훅에 물어 매처를 자동 추적하는 회귀 | IMAGE_NAMES 하나에서 액션·도구 이름 판정 파생 + 매처 3종 등록 + 회귀 6건(훅에 물어 매처를 자동 추적) + CLAUDE.md v7.13 가드 조항 4번째 형태 |
| F064 | 2026-08-04 | 실수 | 인라인 Bash 스크립트에서 셸 변수를 잘못 이스케이프해 변수명이 확장되지 않고 리터럴 문자열로 넘어갔다(경로 자리에 변수명이 그대로 들어와 ENOENT) — 정규식·경로가 든 스크립트를 인라인으로 짠 것이 원인. F054·F056·F060(커밋 메시지)과 같은 계열의 4번째지만 통로가 다르다: 규칙6은 커밋 메시지만 막고 인라인 스크립트는 기계로 안 막혀 있다. 처방 후보=스크립트는 Write로 파일에 쓰고 실행 | shell-inline-guard 훅 신설(인라인 스크립트의 이스케이프된 달러·백틱 차단, 셸 오배송 here-string/heredoc 차단, 여러 줄은 경고) + 회귀 13건·변이 3/3 탐지 + CLAUDE.md v7.14 |
| F065 | 2026-08-04 | 실수 | 코드 파일을 스크립트로 변이시키다 중간에 죽어 파일이 변이 상태로 남았다 — python subprocess가 CP949로 자식 stdout을 디코드하다 예외를 던져 원복 라인이 실행되지 않았다. CLAUDE.md 「코드 파일 편집은 스크립트가 아니라 Edit 도구로」(F050)를 내가 어긴 자리다. 변이 검사도 Edit 도구로 하고, 스크립트를 쓸 땐 원복을 try/finally로 감싼다. | 옆 세션 code-edit-guard.js 훅(in-place 편집기·리다이렉션/tee/cp/mv·PowerShell 쓰기 cmdlet·인라인 스크립트 쓰기 전부 차단) + lib/shell-text.js 공용 통로 + tests/코드편집가드.test.js. 내 shell-inline-guard 중복 규칙은 물려 판정층을 하나로 |
| F066 | 2026-08-04 | 실수 | 남의 미커밋 파일에 git stash push를 걸었다 — 다른 세션이 121줄 수정 중이던 clasp-guard.js를, CI 실패 원인을 가리려고 잠시 치웠다. 그 명령이 2분 타임아웃에 걸려 stash pop이 실행되지 않을 뻔했고, 그랬으면 남의 작업이 stash에 갇힌 채 사라진 것처럼 보였을 것이다. 규칙: 남의 미커밋 파일은 진단 목적이라도 stash/checkout 하지 않는다. 원인 판별이 필요하면 git show HEAD:파일로 읽기만 한다. | git-scope-guard 규칙7 — 경로 없는 stash 는 더러운 트리에서 차단(트리 전체를 쓸어담아 남의 것이 반드시 함께 간다), 경로 지정은 통과하되 미커밋 목록으로 확인 요구, 복구·조회는 대상 아님. 안내에 「치우지 말고 git show HEAD:경로 로 읽어라」 명시. 회귀 5건 |
| F067 | 2026-08-04 | 실수 | 코드 파일을 sed -i로 고쳤다 (F050 재발). 변이 테스트를 빨리 돌리려고 SYNK-talk tools/eval-score.js에 sed를 걸었는데, 백업 cp가 /tmp로 가서 원복이 실패했고 파일이 변이 상태로 남았다. 지침이 코드 편집은 Edit 도구로라고 못박은 이유가 이것 — 변이 테스트도 예외가 아니다. 규칙: 일시적 변이도 Edit로 넣고 Edit로 되돌린다. | 위와 같은 code-edit-guard — sed -i 가 그 훅의 규칙1이다. 임시 변이도 예외가 아니라는 것이 그 훅의 명시 조항 |
| F068 | 2026-08-04 | 실수 | 편집 후 커밋 전에 느린 검증(clasp 조회 포함 수 분)을 돌려 미커밋 노출 시간을 늘렸다. 그 사이 옆 세션 stash 로 내 clasp-guard 편집이 잠시 작업트리에서 빠졌고, 나는 그 순간을 보고 「편집이 사라졌다」고 오판해 복구를 시작할 뻔했다(pop 후 온전히 돌아왔다). 관측이 시점에 따라 달라진다 — 내 관측끼리도. 처방=편집하면 검증 전에 먼저 커밋한다 | 위와 같은 규칙7 — 남의 stash 에 내 편집이 딸려가는 경로 자체를 막았다(오판의 원인 제거). 편집 후 즉시 커밋은 프로즈로 남는다 |
| F069 | 2026-08-04 | 실수 | .cmd 주석에 한글을 넣어 배치 파일이 깨졌다 — cmd.exe는 CP949로 파싱하는데 파일은 UTF-8이라 한글 바이트가 뭉개지고, 그 손상이 rem 안에서 멈추지 않아 cmd가 'daily'·'WHY'·'an' 을 명령으로 실행하려 했다. memory-push.cmd 머리말에 'Keep every line pure ASCII'라고 이미 적혀 있는데 주석은 예외겠지 하고 어겼다. 셸 문법 사고 5번째(F054 here-string·F056 제목 @·F060 커밋 백틱·F064 인라인 백틱). 2번째 재발이므로 프로즈가 아니라 회귀로 막는다 — tools/*.cmd 비ASCII 바이트 0 검사. | tests/배치파일인코딩.test.js 신설 — 배치 파일의 비ASCII 를 CI 가 배포 전에 잡는다(픽스처로 탐지력·실저장소는 거짓양성만·파일 0개면 skip 으로 드러냄). 훅이 아니라 회귀인 이유=표면이 .cmd 2개로 좁고 파일에 남는 종류의 사고라서. 표면이 넓어지면 훅으로 옮긴다 |
| F070 | 2026-08-04 | 마찰 | 같은 트랙이 두 세션에서 병렬로 돌았다 — 보드 완료줄의 '다음 트랙=코드편집 통로 기계 차단' 을 두 세션이 각각 집어 같은 가드를 따로 구현했다(c04e7b7 shell-inline-guard 안 vs b927369 새 훅). 하루 두 번째다(크루카드 포크에 이어). 시작 시 git log 는 스냅샷이라 남의 트랙이 세션 도중 착지하면 안 보인다 — 이번엔 예상 밖 테스트 적색을 오독으로 몰지 않고 파봐서 발견했다. 병합 비용은 회귀 14건 이관으로 끝났지만 구현 하나가 통째로 중복됐다 | track-collision 훅 신설 — 세션 첫 호출 HEAD 를 기준점으로 잡고, 내가 만진·선언한 파일과 표식(F0NN·버전태그) 겹침을 Edit 직전에 알린다(차단 아님). 회귀 18건·변이 10/10. 라이브 실증이 두 결함을 잡았다: 한글 경로 이스케이프(저장소 전량 미탐)·공용 장부 소음 11/13 |
| F071 | 2026-08-04 | 마찰 | 세션 직렬 전제가 실측으로 깨졌다 — 12분 안에 서로 다른 세션 ID 6개가 같은 master 에 커밋했다. CLAUDE.md 세션 규약은 「동시에 도는 대화 세션은 1개」를 전제로 쓰여 있다. 내 커밋이 조용히 no-op 이 되고서야 보였다 — F070 의 근본 원인이고, 증상은 언제나 「아무 일도 안 일어남」이다. | CLAUDE.md v7.16 「그 수를 규칙의 근거로 삼지 않는다·동시성은 Session-Id로 읽는다」 + 「커밋했다고 믿지 말고 결과를 본다」 + commit-noop-guard 훅(PostToolUse·이 저장소 첫 결과검사층) + 회귀 13건(등록층 4항 포함·라이브 발화 실측) |
| F072 | 2026-08-04 | 실수 | 해소된 ⏳를 지우지 않아 끝난 일이 매일 아침 결정 큐로 배달되고 있었다 — 노트북LM 업로드·Ask Gemini 육안 둘 다 완료됐는데 미결로 남아 유호님 폰에 갔다. 게다가 정리하면서 같은 함정을 두 번 더 밟았다: 한 줄에 「✅해소 … ⏳남은 것」을 같이 쓰면 큐가 끝난 쪽을 배달하고, 설명문 안에 쓴 ⏳ 글자까지 항목으로 잡힌다. 이 파일 자신이 L172에 「방금 끝낸 일을 미결로 밀어 올린다」고 적어둔 그 형태다(3번째). 처방=배달물에 ✅가 섞이면 빨간불(tests/결정큐). | e56db24 (타 세션) — 결정 큐 파서가 ✅완료 줄을 배달하지 않게 + tests/결정큐 회귀 29줄(「✅…완료(커밋)…⏳남은 건 X」 한 줄 픽스처 포함) · 18/18 초록 확인 |
| F073 | 2026-08-04 | 마찰 | 보드의 「진행중」 줄을 「내가 이어받을 미완」으로 읽었다 — 실제로는 살아 있는 남의 세션이었다. 유호님 「이어서 진행해」에 보드 활성 줄(궤적 연결 고리·진행중)을 집어 같은 파일을 편집했는데, 그 트랙은 local_dee95eb9 세션이 그 순간 돌리고 있던 것이었고 6분 뒤 [v9.178] 15a49e9 로 커밋됐다. 내 편집(멱등 회귀의 순서-only 검사를 분기·return까지 강화)이 남의 커밋에 그대로 실려 나갔다 — uncommitted-swept-by-peer 의 반대 방향이다. 🔑 원인은 소유자가 안 보이는 것이다: track-collision 훅은 **커밋만** 보고, 미커밋 작업본에는 Session-Id 가 없으며, 보드 줄에도 세션 식별자가 없다. 그래서 세션 시작 시의 git status 는 「전 세션이 남긴 미완」과 「지금 도는 남의 작업본」이 같은 모양이다. 같은 트랙 병렬 3번째(F070 가드 중복 · 크루카드 포크). CLAUDE.md 「보드에 작업중으로 선언된 파일은 편집하지 않는다」를 내가 어겼다. | tools/작업본소유자.js 신설 — track-collision 이 쌓는 touched+mtime 을 **읽어** 미커밋을 「내것/🔴살아있는 남의것/⚪끝난세션 유물/❔모름」으로 가른다 + SessionStart 훅(위험할 때만 발화) + 회귀 8건. 쓰는 쪽(훅)은 소유 세션 것이라 안 건드리고 조율 메시지로 분담 |
| F074 | 2026-08-04 | 실수 | 커밋 주인 판정에서 식별자를 잘못 골랐다 — Session-Id 트레일러는 CLAUDE_CODE_HOST_SESSION_ID 인데 스크래치패드 경로의 세션 UUID 와 대조해 내 커밋을 남의 것으로 판정할 뻔했다. v7.16 을 커밋하고 그 조항을 스스로 적용하다 드러났다. | CLAUDE.md v7.16 커밋주인 줄에 「내 id는 $CLAUDE_CODE_HOST_SESSION_ID」 명시 + commit-noop-guard 안내에 현재 세션 id 를 직접 박아 준다(회귀 2건: 변수명 안내·실제 id 주입) |
| F075 | 2026-08-04 | 마찰 | 컨텍스트 알림이 ⚫(500k) 뒤 영구 침묵 — 세 세션이 771~786k까지 무신호로 올라갔고 유호님이 「자동화가 적용 안 되는 것 같다」고 먼저 알아챘다. 경고 8번을 보고도 안 끊긴 건 알림 부족이 아니라 끊는 비용이었다. | context-budget ⚫ 위 STEP(200k) 재발화 + /close 스킬(끊기 1클릭) · 회귀 5건(재발화 구간·작은 창 소음 0·경고마다 실행 한 줄·스킬 실재·인계 3종) |
| F076 | 2026-08-04 | 마찰 | code-edit-guard가 대상 없는 rename(mv A B)까지 「덮어쓰기」로 오탐 — 바탕화면 사본 배포와 정본 자료 구판 표시에서 2회 연속 CODE_EDIT_BYPASS로 우회했다. 훅 메시지는 「백업→변이→검사→원복 중간사」를 근거로 드는데, 정본→사본 복사와 rename은 그 유형이 아니다. 오탐이 잦으면 우회가 습관이 되어 진짜 덮어쓰기도 같은 손짓으로 통과시킨다(가드가 사는 방식이 아니라 죽는 방식). | code-edit-guard ②-b 신설 — cp·mv 는 대상이 실제로 있을 때만 막는다(rename·새 사본 통과·모름은 fail-closed) + 인용 안 공백 보존으로 경로 조각남 수리 + 회귀 6건·변이 7/7 탐지 |
| F077 | 2026-08-04 | 실수 | 컴팩트로 컨텍스트가 내려간 세션의 억제 카운터가 그대로 남아 다음 사이클 400k 구간을 통째로 침묵시켰다. 카운터가 「가장 높이 갔던 곳」을 담고 있었는데 담아야 할 것은 「지금 어디인지」였다. 유호님 「다른 화면들에서도 되는지 확인해줘」가 아니었으면 안 보였다. | stage < prev 면 카운터를 따라 내린다(내려가는 길엔 침묵) + 회귀 760k→174k→310k 시나리오 |
| F078 | 2026-08-04 | 실수 | 버전 상수와 헤더 태그가 두 번에 나뉘어 쓰여 하루 두 번 CI가 빨개졌다(v9.180·v9.181). 검사는 엔진 7파일 합본의 최고 [v9.N]과 Code.js 상수를 대조하는데, 세션은 짜면서 엔진 헤더에 태그를 먼저 박고 채번은 나중에 했다. --desc 없이 돌리면 상수만 올라 반드시 빨개지는 구멍도 있었다. 그 적색이 남의 배포 게이트까지 막았다. | tools/bump-version.js — ①[vNEXT] 자리표를 채번과 같은 실행에서 엔진 전체 확정(창 자체를 없앰) ②--desc 를 채번 **전에** 강제(번호 안 태움) ③--check 신설(양방향+자리표 잔존) · /deploy 5단계와 CLAUDE.md 에 발동 조건 명시 · 회귀 6건 신설(픽스처 root 이음매 SYNK_BUMP_ROOT) |
| F079 | 2026-08-04 | 실수 | 충돌 감지가 워크트리 브랜치를 통째로 못 본다 — 세션 둘(dee95eb9·ba721067)이 같은 시각 F076을 각자 고쳤다. track-collision은 master 착지만 보고, 작업본소유자는 git status 미커밋만 본다. 워크트리의 별도 브랜치는 둘 다에 안 잡힌다. 이번엔 착수 전 list_sessions를 안 본 내 절차 누락이 겹쳤다(제목이 'Fix code-edit-guard rename false positive'라 한 번만 봤으면 즉시 보였다). 같은 트랙 병렬 4번째(F070 가드 중복·크루카드 포크·F073). | lib/worktrees.js 신설(공용 통로) + track-collision 신호④ — 다른 작업 트리의 살아있는 미커밋을 Edit 직전에 알린다. 🔑가장 깊은 층=상태 키를 cwd 해시가 아니라 메인 작업 트리로 잡아 워크트리 세션과 메인 세션이 서로를 보게 했다(메인 값은 그대로라 작업본소유자 계약 유지). 비용은 fs 선별로 68ms→0.01ms. 회귀 8건·변이 10/10 |
| F080 | 2026-08-04 | 실수 | profiles 진로 4열 자리를 코드 레지스트리에서 「비었다」고 읽고 라이브 시트를 안 봤다 — 131에는 이미 학교·동네가 있었다(타 세션 라이브 실측이 잡음). 레지스트리는 코드가 만드는 열만 알고 라이브에는 Glide의 Row ID·langColOf_의 학교/동네처럼 코드가 모르는 열이 자란다. 같은 판에서 129·130도 밟을 뻔했다(회귀가 잡음) = 한 커밋에 두 번. 처방=고정 번호 금지·이름 해석(profilesBlockAt_)+회귀 | profilesBlockAt_(이름 해석)+고정번호 금지 회귀 — 이번 세션이 실측으로 구멍 하나를 더 막았다: 선점열 레지스트리와 상담디테일 검사가 각자 손 목록을 들고 있다가 갈라져 4차 블록(SHARED4·130)이 검사 밖이었고, 라이브에만 있는 학교(131)·동네(132)도 레지스트리에 없었다. tests/_engine-source.js sharedBlocks() 하나에서 파생 + 라이브 실측 열 등재 + 블록 겹침 검사. 변이 8/8 탐지(옛 검사는 같은 변이 0/4) |
| F081 | 2026-08-04 | 실수 | 회귀 17건·변이 15/15가 전부 초록인 채로 라이브가 예외를 던졌다 — moveItem(item, toIndex) 오버로드는 제네릭 Item 을 받는데 addTextItem() 이 돌려주는 건 TextItem 이라 "The parameters (FormApp.TextItem,number) don't match the method signature" 로 죽었다. 🔑 재는 층이 달랐다: 이 저장소의 폼 검사는 **전부 소스 텍스트층**이고(tests/마감폼 머리말이 "시트·FormApp 의존이라 실행 불가"라고 적어 뒀다), API 시그니처는 실행해야만 보이는 층이다. 더 나쁜 건 실패 지점이다 — addTextItem 은 이미 성공한 뒤라 라이브 폼에 칸이 엉뚱한 자리에 남았고, 「새로 넣는 경로에서만 자리를 잡는」 원래 설계로는 그 폼이 영영 안 고쳐졌다(멱등이 수렴이 아니었다). 처방=인덱스 오버로드로 통일 + 양쪽 경로에서 자리 보정 + 회귀 2건(변이 18/18). 남는 교훈은 회귀로 못 막는다: **폼·드라이브처럼 실행층에서만 드러나는 API 는 배포 뒤 1회 실행이 유일한 검증이다.** | tools/실행층점검.js 신설 — 배포 diff 에서 실행층 전용 API(FormApp·DriveApp·DocumentApp·CalendarApp·Gmail/MailApp·ScriptApp)를 만진 **함수**를 뽑아 「배포 뒤 라이브 1회 실행」 목록을 만든다. 발동은 배포 통로 **둘 다**: clasp-guard 통과 직전(로컬·기준점=직전 버전 태그) + deploy-live 워크플로 단계(폰). 차단이 아니라 할 일 목록 — 막으면 실행 자체가 불가능하다. 회귀 10건(픽스처로 탐지력·실저장소는 거짓양성만·발동 조건까지)·변이 8/8. 만들면서 자기 결함 3개를 실측으로 잡았다: 한글 함수명 미탐(ASCII 정규식)·한글 경로 이스케이프 미탐(core.quotePath)·자기 주석 오탐 |
| F082 | 2026-08-04 | 실수 | F078을 닫으면서 세 번째 다리를 빠뜨렸다 — docs/버전_이력.md 도 SYNK_VERSION 과 어긋나면 CI 적색인데(safety [2026-08-03]) bump-version --check 가 그것을 안 본다. 자유서술이라 자동 기입은 못 해도 **탐지는 된다**. 「--check 로 잡는다」고 말해 놓고 안 넣었다(말과 산출물 불일치). 오늘 같은 창이 세 번째다. | bump-version.js --check 에 세 번째 다리 추가(docs/버전_이력.md 최고 태그 ↔ SYNK_VERSION 양방향 + 머리말 하드코딩) + 채번 직후 자동 발화(지시만 하던 줄이 이제 상태를 재서 말한다) + 회귀 4건(변이 4/4 교차오탐 0)·미검사는 「검사하지 않았다」로 드러냄 |
| F083 | 2026-08-04 | 실수 | 마찰을 「해소」로 닫으면서 신고에 적힌 명령을 그대로 다시 넣어 보지 않았다 — F076 을 닫은 뒤 실측하니 신고된 두 명령(둘 다 $HOME/OneDrive/Desktop/…)이 여전히 막혀 있었다. 홈 표기가 unknown 으로 떨어져 「모르면 막는다」에 걸렸다. 원인을 고쳤다는 확신과 신고자의 입력이 통과하는 걸 본 것은 다르다. 처방: resolve 하기 전에 신고 문구의 입력을 회귀에 그대로 넣는다. | code-edit-guard 홈 표기 펼치기 + 인용된 디렉터리 대상 + 회귀 4건(신고 명령 그대로 · 양방향) — 이 커밋 |
| F084 | 2026-08-04 | 실수 | 자격증명 화면에 form_input — 「읽지 않기」로 정해 놓고 쓰기는 했다. find가 「empty textbox for a property name」이라 답한 ref가 실제로는 CLAUDE_API_KEY의 값 칸이었고, 덮어쓰기가 이전 값을 응답으로 돌려줘 API 키가 트랜스크립트에 평문으로 남았다(저장 전 새로고침으로 파기·키 교체 필요). 교훈 ①find의 설명은 접근성 라벨이지 값이 아니다 — 「비었다」를 사실로 받고 쓰면 눈 감고 덮는 것 ②자격증명이 보이는 화면은 열어주고 손을 뗀다(읽기 금지는 쓰기 금지를 함의한다). 오타 1개 막으려다 키를 태웠다. | .claude/hooks/credential-guard.js 신설 — ①navigate 로 기록한 주소가 자격증명 화면이면 쓰기 도구(form_input·type·key·클립보드·업로드·페이지 스크립트)를 차단 ②값이 비밀 형태면 URL 무관 차단(사유에 값은 안 적는다) ③예외는 --allow-next 1회용(상시 해제 없음). 읽기·클릭은 통과 — 처방이 「열어주고 손 뗀다」라서. 회귀 12건(통과 목록 동일 무게·매처를 훅에서 파생해 서버 3종 전수)·변이 6/6. ⚠클릭으로 도달한 화면은 규칙①이 못 본다(훅 머리말에 명시·회귀로 못박음). ⏳유호=Anthropic API 키 교체는 사람 몫으로 남는다 |
| F085 | 2026-08-04 | 실수 | 같은 -uall 결함이 하루에 두 번째다 — 내 작업본소유자에서 고친 「git status --porcelain 이 새 폴더의 미추적을 접는다」가 공용 통로 .claude/hooks/lib/worktrees.js:145 에도 그대로 있다. 워크트리의 새 파일이 통째로 안 보이고 증상은 「0건」이다. 2번째 = 실수가 아니라 시스템 결함(같은 판정이 두 곳에 적혀 있다). 통로는 타 세션 소유라 조율 메시지로 넘겼다. | lib/worktrees.js othersDirty 에 -uall 추가 + 회귀(새 폴더 안 미추적 픽스처)·변이 11/11. 58ce9737 이 실측으로 지목 — 같은 자리를 오늘 작업본소유자(67fecc0)도 밟아 2번째라 양쪽 다 회귀로 잠갔다 |
| F086 | 2026-08-04 | 마찰 | .claude/worktrees/ 를 가리는 규칙이 .git/info/exclude 에만 있다(타 세션 dee95eb9 지적). 그건 커밋되지 않는 로컬 파일이라 다른 기계에선 워크트리가 통째로 커밋될 수 있다. .gitignore 로 옮겨야 하는데 그 파일이 지금 elated-bhaskara 워크트리 세션의 미커밋이라 두 세션 모두 안 건드렸다. | d8f28b7 — .git/info/exclude 에서 .gitignore 로 이관 + tests/무시규칙.test.js(픽스처 저장소에 .gitignore 만 놓고 재는 층 분리 · 실저장소 check-ignore 는 info/exclude 가 같이 먹혀 로컬에서만 초록이 된다) |
| F087 | 2026-08-04 | 실수 | 회귀 24건·변이 25/25가 전부 초록인데 적대 리뷰가 HIGH 3건을 잡았다(멱등 키 오프바이원·월키 Date 오염 5번째·크루카드 응답 유실). 공통 원인=회귀가 순서와 문자열 존재만 보고 값·동작을 안 쟀다. 변이도 같은 층이라 못 잡았다. 처방=시트 스텁 위 실행 층 회귀 7건 + 「초록의 개수가 아니라 재는 층을 본다」 | 82acb6e 실행 층 회귀(시트 스텁 위에서 실제 실행) — 변이 5/5 탐지로 실효 확인: 근거키 오프바이원·관측일 서식 고정 제거·되읽기 방어 제거·빈 슬러그 덮어쓰기·어휘 매핑 제거. 옛 소스 텍스트층 회귀는 값을 안 재서 전부 초록이었다 |
| F088 | 2026-08-04 | 마찰 | code-edit-guard 규칙②가 셸 변수 경로를 못 풀어 오탐 — mktemp 임시 디렉터리에 쓰는 $d/src/x.js 를 「코드 파일 덮어쓰기」로 막았다. 변수는 해석할 수 없으니 fail-closed 가 설계상 맞지만, 임시 경로 작업이 흔해 BYPASS 습관을 만들 위험이 F076 과 같은 계열이다. 대안: 변수가 섞이면 차단 대신 경고로 낮추거나, 실행 전 cwd 기준 해석 가능한 경우만 본다 | code-edit-guard 가 같은 명령 안에서 선언된 셸 변수를 풀어서 본다(POSIX·PowerShell 대입 + d=$(mktemp -d) 는 정의상 임시로 매핑) + 경로 정규화로 .. 를 접는다. 판정을 낮추지 않고 **모르는 것의 범위만 좁혔다** — 선언을 못 찾은 변수는 그대로 fail-closed. 회귀 2묶음(통과 5·차단 5)·변이 5/5. 라이브 재현 1회로 확인 |
| F089 | 2026-08-04 | 교정 | 로그인 계정을 확인하지 않고 원격 실측을 해 오진단했다 — 크롬 둘 중 77yuhbs 창에서 시트를 열고 「스토리지 15GB에 60.83GB 초과, 앱이 새 파일을 못 만든다」고 보고했는데, 유호님 지적으로 작업 계정(Browser 2)을 재니 5TB 중 0.07GB로 텅 비어 있었다. 같은 시트·같은 도구, 로그인만 달랐다. 🔑「잰다」는 규칙은 지켰는데 무엇을 재는지를 안 봤다 — 오진단이 실측의 모습을 하고 나온다. 증상 셋이 전부 계정을 가리켰는데 못 읽었다: 스토리지 배너·SYNK 메뉴가 …에 접힘·Gemini가 메뉴 아닌 광고 | CLAUDE.md v8.0 — 「잴 수 있는 것은 잰다」에 「재기 전에 무엇을·어느 층을 재는지부터 정한다 — 대상을 안 보면 오진단이 실측의 모습을 하고 나온다」를 붙여 흡수(조항 순증 0) |
| F090 | 2026-08-04 | 실수 | 작업본소유자가 살아있는 세션의 파일을 ⚪「끝난 세션이 남긴 것 — 이어받아도 된다」로 내놨다. 이 세션 SessionStart 의 실오판: Code.js 를 40분 전 끝난 세션 것으로 짚었는데, 실제로는 살아있는 세션이 node tools/bump-version.js(Bash)로 방금 [v9.184] 를 박은 것이었다. Bash 가 쓴 파일은 만진 기록이 안 남고(track-collision 은 Edit·Write 만 본다) 죽은 세션의 옛 기록이 그 자리를 이겼다. 🔑 기록은 「누가 언젠가 만졌다」만 말한다 — 그 뒤에 다른 통로가 덮어썼는지는 기록에 없다. 그래서 ⚪(이 도구에서 손대도 된다고 말하는 유일한 칸)는 시각으로 반증돼야 한다. 이어받아 커밋했으면 F073 재현이다. | tools/작업본소유자.js — ⚪ 유물 주장을 파일 mtime 으로 반증(심장박동이 멎은 뒤에 바뀐 파일은 그 세션 것일 수 없다 → ❔모름으로 내리고 왜 아닌지까지 적는다). 판정을 올리지 않고 모름의 범위만 넓혔다. 회귀 +4건(반증·거짓양성 0·훅 순서 여유·못 잰 경우)·변이 3/3 탐지. 곁들여 기존 픽스처가 파일 시각을 안 정해 실재하지 않는 상태를 시험하던 것도 수리 — 그 자리가 이 결함의 사각이었다. 커밋 52bc369(⚠그 메시지의 F089 는 오기, 같은 시각 옆 세션이 그 번호를 다른 신호로 채갔다). |
| F091 | 2026-08-04 | 실수 | 가드 두 곳이 구분자 상수를 날문자(보이지 않는 제어문자)로 들고 있었다 — 둘 다 바로 위 주석이 「반드시 이스케이프로 적는다」고 스스로 금지한 형태다(code-edit-guard 공백표 0x01 · track-collision SEP 0x1F). 지워지면 새는 방향이 서로 다른데 둘 다 나쁘다: 공백표가 비면 length !== 1 방어가 걸려 **모든 Bash 명령이 막히고**(전면 마비), SEP 가 비면 커밋 제목이 글자 단위로 쪼개진다(조용히 틀린다). 🔑 이게 프로즈로 안 지켜진다는 증거는 수리 과정 자체다 — 이 결함을 고치는 내 편집이 주석 안에 날문자를 **한 번 더** 심었다. 편집 통로가 이스케이프 표기를 날문자로 바꿔 놨고 Read 화면에는 아무 표시도 없었다(Edit 의 정확일치도 같은 이유로 실패했다). 규칙은 3년 치 주석으로 적혀 있었고 지키는 눈이 0이었다. | tests/훅이식성 ④ 날문자 금지 — .claude/hooks 아래 js 전수 스캔(탭·LF·CR만 허용, lib/ 포함) + 양방향 픽스처(날문자는 잡고 이스케이프 표기는 안 잡는다 · 거짓양성이면 다음 사람이 검사를 끈다). 변이 실측: 두 상수에 날문자를 되돌려 심으니 빨강, 원복하니 초록 — 실저장소에 실제로 닿는다. 곁들여 주석에서 표기 인용을 걷어냈다(인용하려던 것이 사고의 원인이었다). |
| F092 | 2026-08-04 | 실수 | 규칙을 뭉뚱그려 써서 유호님이 금지 이유를 알 수 없었다 - 마누스 페북 인스타 금지 조항이 비번 대리로그인과 공식 OAuth 커넥터를 한 줄로 묶었고 외부 사실도 낡았다 | 4aef3b3 — docs/AI_스택_가이드.md §1-6 을 둘로 갈랐다: 비번 대리로그인(정지 위험)과 공식 OAuth 커넥터(토큰만 받고 비번은 안 넘어간다 · 정지 위험 없음)를 각각 적고, 금지 근거도 「정지」가 아니라 「끊길 위험(인수 6개월 뒤집힘)」으로 바로잡았다. 외부 사실엔 날짜를 박았다(25-12 인수 → 26-04-27 제동 → 26-06-11 해제)+「인수 기간 얘기라 낡았다」 경고. ⚠장부는 그때 안 닫혀 살아있는 신호로 남아 있었다 — 이번 /evolve 1단계가 그걸 집어 들었다(고친 것과 닫는 것은 별개 동작이다). |
| F093 | 2026-08-04 | 실수 | 회귀 테스트 자신이 결함이라 판정이 거짓이 됐다 — ①execFileSync require 누락이 ReferenceError 를 catch 로 흘려 「실행 실패」를 「도구가 막았다」로 읽혔다(통과 쪽) ②경로 리터럴이 유호님의 정당한 파일 이동에 인질로 잡혀 master 5건 적색(적색 쪽). ②는 같은 파일이 「버전 리터럴은 인질이었다」를 이미 주석으로 배워 놓고 경로 축엔 안 옮겨 적어 재발했다. | tests/실행층점검.test.js catch 하드닝(status 없으면 throw) + tests/문서그래프.test.js 정본찾기(파일명·개수 1 강제) · 변이 5/5·4/4 |
| F094 | 2026-08-04 | 마찰 | board-guard 가 상태 칸이 완료로 시작하는 줄만 완료로 세어, 체크마크 접두를 붙인 종결 줄이 전부 활성으로 집계된다. 활성 상한 12 에 걸려 새 세션이 자기 줄을 못 넣는다 — 2026-08-03 에 굵게 표기를 같은 이유로 수리했는데 접두 기호 형태가 남아 재발했다. 처방을 따를 수 없으면 우회가 필연이라 표기가 갈라진다. | board-guard 판정을 「무엇으로 시작하는가」에서 **어휘 두 목록**(완료 3·활성 10)으로 바꿨다 — 접두 형태가 늘어도 안 깨진다. 활성 어휘가 우선(조용한 누수 방지) · 첫 구절만 본다(뒤쪽 서술이 뒤집지 않게) · 부정 접두(미완료) 방어. 🔑재발 원인이 「처방을 따를 수 없어 표기가 갈라진다」라 **안내 문구를 같은 목록에서 파생**시키고, 막을 때 **무엇을 활성으로 셌는지 보여준다**. 실측: 실보드 14줄에서 옛 판정 활성 11(상한 12 코앞) → 새 판정 3, 오분류 8건 전부 ✅ 접두. 회귀 +6·변이 7/7·CI 1177 pass |
| F095 | 2026-08-04 | 마찰 | 실저장소 이력에 기대는 검사가 CI 에서만 깨졌고, 그 적색이 **주인 없이** 남아 모두의 배포 게이트를 막았다. tests/실행층점검 의 세 검사가 `HEAD~1`·옛 커밋을 읽는데 CI 체크아웃은 fetch-depth 기본값(얕은 클론)이라 이력이 없다 — 로컬 14/14 초록, CI 만 적색. 더 나쁜 건 닫힌 방식이다: 그 트랙을 만든 세션이 보드에 **✅종결**로 적고 나갔는데 master 는 빨간 채였다 (b7ee0f7 이후 계속). CLAUDE.md 는 이미 「repo 밖 환경에 기대는 검사는 CI 에서 깨진다 — 실저장소 검사는 skip 으로 드러낸다」고 적어 뒀고, 그 조항이 적용되지 않았다. ⚠ 남은 관측 불일치 1건은 정직하게 적는다 — CI 에서 `HEAD~1` 은 못 읽는데 옛 커밋 `26ebb2f~1..26ebb2f` 는 **읽혔다**(그 검사는 통과했다). depth 1 클론을 재현해 보니 둘 다 못 읽혀 CI 조건이 단순 얕은 클론과 다르다. 원인 미특정 — 그래서 두 층을 다 손봤다. | 두 층 — ①.github/workflows/syntax-check.yml checkout 에 fetch-depth: 0(실저장소 검사가 CI 에서 실제로 돌게) ②범위를 못 읽으면 **통과가 아니라 skip** 으로 드러내고 사유에 git 오류 원문을 실어 보낸다(원인 미특정분을 CI 가 스스로 말하게 하는 계측). 실측 2환경: 이력 있으면 16/16 전부 실행, depth 1 클론 재현에서 0 fail·3 skip. 곁들여 한글경로 검사를 소스층/실이력층으로 갈라 탐지력의 본체(quotePath)는 이력 없이도 늘 돌게 했다. 로컬 CI 모사 1178/0. |
| F096 | 2026-08-04 | 교정 | 300k 인계 2차(유호: 아직도 문제) — ①깨워진 AI가 복붙용 지시문구를 화면에 안 내고 「창 닫으세요」만 냄 ②트랙경계 테스트가 cwd=실저장소로 돌아 CI마다 실인계문.md를 가짜 3블록(세션 block·UTC시각)으로 덮음 | 1bb186e 가 실물을 고쳤다(화면 실물 통로 tools/인계문.js + wake ④ + /close ⑥ · 시험 오염 봉쇄=임시 cwd·가짜 id 차단·카나리아), 뒤처리 d6c6d03. 장부는 그때 안 닫혀 이 세션이 이어받았다 — **고침과 닫음은 별개 동작**이다(F097 과 같은 축의 반대 방향: 저쪽은 안 고치고 닫았고, 여기는 고치고 안 닫았다). 닫기 전 F083 대로 재검증 — 격리 사본(git archive HEAD)에서 **변이 3/3 탐지**: ①인계문 도구 삭제 ②wake 의 인계문 호출 제거 → 둘 다 「처방-실행층 결속」 적색, ③실인계문에 가짜 세션 블록(세션 s1·block) 심기 → 카나리아 적색. 기준선 초록·원복 후 초록이라 사본 오염 0. ⚠ 닫으면서 **잔여 1건**을 실측으로 잡았다: wake 안내가 node tools/인계문.js --no-save 를 지시하는데 그 플래그가 **무동작**이라, 점검 실행 한 번이 공유 사본 3칸에서 남의 블록을 밀어낸다(내가 실제로 밟아 d35445de 블록을 밀어냈다). 지금 세션 45fed745 가 같은 자리를 수리 중(tools/인계문.js·tests/트랙경계·context-budget 미커밋)이라 **그 세션 몫으로 두고 조율만 남긴다** — 중복 수리 금지. |
| F097 | 2026-08-04 | 실수 | /evolve 1단계에서 살아있는 신호 F092 를 「이미 고쳐졌다」고 보고 닫았는데 **일렀다**. 내가 근거로 삼은 4aef3b3 은 원문 옆에 **정정 각주**를 단 커밋이라 낡은 문장이 **사본 3곳(docs 1·memory 2)에 그대로 살아남았고**, 각주 밑의 원문이 계속 읽혔다. 옆 세션이 그걸 잡아 3곳을 수리하고 tests/외부사실.test.js 를 신설했다(3번째 사본은 손이 아니라 가드가 잡았다). 🔑 두 겹의 실수다 — ①**정정을 각주로 달면 원문은 안 지워진다**(교체여야 한다) ②**「고쳤다」와 「닫아도 된다」는 다른 판정이다.** F083 이 「해소로 닫기 전에 신고 원문 그대로 다시 넣어 본다」고 이미 적어 뒀는데, 나는 그걸 문서 **본문에만** 적용하고 **사본에는 적용하지 않았다** — 같은 조항을 반쪽만 쓴 형태다. ⚠ 장부 F092 행은 도구가 재해소를 막아(이미 해소로 기록됨) 옛 근거만 담고 있다 — 이 행이 그 정정이다. | 옆 세션이 실물 수리 — 낡은 사본 3곳 교체 + tests/외부사실.test.js 신설(변이 4/4 · CI 1222/0), memory `correction-as-footnote-2026-08-04`. 절차 쪽 처방은 F083 의 재적용 범위를 넓히는 것이다: **해소로 닫기 전에 그 문구를 저장소 전체에서 grep 한다**(본문 1곳을 고친 것과 그 사실이 사라진 것은 다르다). 조항 신설은 하지 않는다 — F083 이 이미 있고, 재발하면 그때 기계(문구 grep 게이트)로 옮긴다. |
| F098 | 2026-08-05 | 실수 | 실행층점검이 「무엇만 봤는지」를 초록·적색 두 경로에 다른 문구로 적어 갈라졌다 — 회귀는 초록 문구만 알아, 실행층 API가 실제로 든 정상 커밋에서 도구는 옳은데 테스트가 빨개지고 그 적색이 배포 게이트를 막았다. 처방=범위주석() 한 함수 파생 + 두 경로 동일 문자열 회귀. | 범위주석() 단일 파생 + 두 경로 동일 문자열 회귀 2건 |
| F099 | 2026-08-05 | 실수 | 점검하러 node tools/인계문.js 를 한 번 돌린 것이 공유 사본 3칸에서 살아있는 옆 세션(0b9962e2)의 인계문 블록을 축출했다 — 그때 store 바통은 take()로 이미 비어 그 파일이 마지막 사본이었다. --no-save 로 점검 통로를 갈랐지만(98b46f5) 칸 3개 vs 살아있는 세션 10개라 정상 종료만으로도 축출은 계속된다 — 칸 수·세션별 파일은 유호님 판정 대기 | session-report.js 저장 구조를 세션별 파일(docs/_ops/인계문/<세션>.md)로 가르고 인계문.md 를 폴더에서 파생시키는 목차로 바꿨다(dfe7c2e) — 세션은 자기 파일만 써서 축출 경로가 구조적으로 없다. 점검용 --no-save 로 재출력이 목차를 흔드는 것도 막았다(98b46f5). 옛목차이주()로 착지 순간 증발 방지. 회귀 +3·변이 4/4·라이브 5파일 축출 0 |
| F100 | 2026-08-05 | 마찰 | F095 를 고친 그 형태가 **다른 검사에서 그대로 재발했다** — 실저장소 검사가 mtime 에 기대 CI 에서만 깨졌고, 그 적색이 master 를 막았다. tests/발표물임베드.test.js:95 「실저장소의 발표물이 전부 임베드본이다」가 소스와 산출물의 mtime 을 비교하는데, **git 은 mtime 을 보존하지 않는다.** 새 체크아웃에서는 쓰기 순서가 시각을 정하고 01_….html 이 _src_01_….html 보다 먼저 쓰이므로 소스가 **항상** 더 새로 보인다 — 플래키가 아니라 매번 걸린다. 실측: run 30924368506(5edc461) 1261 중 fail 1, 6종 전부 「소스가 더 새롭다」. 같은 파일이 로컬에서는 8/8 초록. 뒤이은 내 커밋 run 30924462120 도 같은 검사로 적색이라 이후 push 가 전부 이 적색을 물고 간다. ⚠ 보드 줄에는 「✅종결·push」로 적혀 있었다 — F095 와 같은 사인이다(고쳤다고 적은 시점과 master 가 초록인 시점이 다르다). 주인은 살아있다(5d53cc35 · 심장박동 0분) — 인수하지 않고 진단을 보내고 장부에만 남긴다. | 주인 세션(5d53cc35)이 **f0000ae 로 수리**했다 — 내가 보낸 진단(git 이 mtime 을 보존하지 않고, **체크아웃 쓰기 순서**가 시각을 정해 산출물이 소스보다 먼저 쓰이므로 소스가 **항상** 더 새로 보인다 — 플래키가 아니라 결정적이다)과 같은 자리이고, 처방은 내가 제안한 것보다 세다: **판정을 시각에서 내용으로 옮겼다**(소스·산출물 차이 자체로 「고치고 안 구웠다」를 잡는다). 픽스처도 둘 늘렸다 — 「소스를 고치고 안 구우면 잡는다(시각이 아니라 내용으로)」 + 「시각이 뒤집혀도 내용이 같으면 통과한다(CI 새 클론 재현)」. 닫기 전 F083 대로 **신고 조건 그대로 재현**했다: origin/master 를 새로 추출(= CI 체크아웃과 같은 조건, 소스·산출물 mtime 이 같은 초)해 그 위에서 돌리니 **9/9 초록**. 원격도 초록으로 돌아왔다 — 내 F100 커밋(74943ad) run 30924814879 success. ⚠ 후속 1건은 같은 축의 **다른 검사**다(7bb9413: 이주 회귀가 박힌 날짜에 기대 12시간 뒤 스스로 빨개질 참이었다) — 그 트랙 몫이라 여기서 닫지 않는다. 🔑 남는 교훈은 F095 와 같다: **실저장소 검사가 repo 밖 상태(파일 시각)에 기대면 로컬 초록·CI 적색이 된다.** 판정 재료를 내용으로 잡으면 환경이 안 낀다. |
| F101 | 2026-08-05 | 마찰 | AI 세션끼리의 내부 장부 정리 요청(F099 를 닫아달라)이 유호님 화면에 그대로 올라와 유호님이 「이게 뭔지 이해가 안 된다」고 하셨다. 게다가 그 요청은 이미 처리된 뒤라 낡은 메시지였다(45fed745 가 ee61363 로 닫음). 원인은 세션 규약의 탈출구다 — 「메시지는 전달이 보장되지 않으니 전달 불가 세션이면 유호님께」에 종류 필터가 없어, 유호님의 판단이 필요한 것(방향·비가역 승인)과 AI 내부 위생(장부 칸·커밋 정리)이 같은 통로로 올라온다. 후자는 유호님이 읽을 물건이 아니고, 유호님의 주의가 이 저장소에서 가장 비싼 자원이다. | |
