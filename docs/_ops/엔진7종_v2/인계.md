# 엔진 7종 상향 v2 — 이어 하는 세션에게 (2026-09-04 · 세션 1894d42f)

> 유호 확정 09-04 「B로 가자, 페이블로 v2 써줘」 → 결정.md 에 적혀 있다. **쓰는 모델 = Fable 5.1**(다른 모델 세션이 집으면 안 된다).
> 유호 지시 09-04 새벽 「마지막 묶음은 띄우지마. 지금 하는것만 마무리하고 대기해줘. 5시간 한도 풀리면 다시 요청할게」 — 그래서 §7·§8·§9·§10 은 **유호님이 다시 부르실 때** 띄운다.

## 1. 어디까지 왔나 — 파일이 정본이다
```bash
cd C:/Users/q1212/Documents/SYNK-appsscript && ls docs/_ops/엔진7종_v2/v2/ && node docs/_ops/엔진7종_v2/자.js 전부
```
- `v2/§N.md` 가 있고 자가 ✅ 이면 그 절은 끝났다(통과한 절은 커밋돼 있다 · `git log --oneline -- docs/_ops/엔진7종_v2/v2`).
- 없거나 🔴 이면 그 절은 다시 띄운다(🔴 인데 파일이 크면 «죽다 남은 반제품»이다 — 새 에이전트에게 「있는 v2/§N.md 를 이어 채우라」고 하면 값이 준다).
- 재료(`재료/을·갑·심사_§N.md`)가 없으면 `node docs/_ops/엔진7종_v2/v2준비.js` 로 다시 만든다(Opus 판은 스크립트가 `git show eb4d2cb24:docs/엔진7종_상향설계_v1.md` 로 꺼낸 임시 파일을 읽는다 — 스크래치 경로가 없으면 그 줄을 `git show` 로 바꾼다).

## 2. 절마다 띄우는 법 — Agent 도구(general-purpose · 백그라운드) 하나 = 절 하나
프롬프트 틀(§N 과 뼈대만 바꾼다 · 뼈대: 갑 = §0·§3·§4·§5·§10·§11·§12·§16 / 을 = 나머지):

```
저장소 C:/Users/q1212/Documents/SYNK-appsscript 에서 「엔진 7종 상향 설계 v2」의 절 §N 을 쓴다.
먼저 규칙 파일 docs/_ops/엔진7종_v2/규칙.md 를 전문으로 읽는다 — 지키는 것·옷(문장 자)·뼈대·쓰는 법이 전부 거기 있다.
재료 셋(전문으로 읽는다): docs/_ops/엔진7종_v2/재료/을/§N.md(정본 몸통 · 내용의 정본) · docs/_ops/엔진7종_v2/재료/갑/§N.md(Opus 판 · 문장 자의 본보기 · 가져올 것의 출처) · docs/_ops/엔진7종_v2/재료/심사_§N.md(가져올 것 · 틀림 목록).
이 절의 뼈대 = **갑/을**(…이 절의 가져올 것·고칠 틀림을 심사_§N.md 에서 두세 줄로 짚어 준다…).
산출 = docs/_ops/엔진7종_v2/v2/§N.md 하나. 다른 파일은 만들지도 고치지도 않는다. 큰 절이니 뼈대를 먼저 Write 하고 U 마다 Edit 로 채운다.
다 쓰면 저장소 루트에서 `node docs/_ops/엔진7종_v2/자.js §N` 을 돌려 ✅ 가 나올 때까지 고친다(🔴 목록이 곧 고칠 것).
최종 답(반환값) = 규칙 §4 의 한 단락 꼴. 인사·서론 없음.
```
남은 넷의 짚어 줄 것(심사_§N.md 에서):
- **§7 Trail(을 뼈대)** — 갑에서: 7-0 「회귀 삭제는 사고가 아니라 판단」 배경 문단(커밋 e75fc7fcf 본문 수치) · 장면 먼저 짧은 문장 · 7-3 표 · 7-4 쉬운 말. 을 틀림 고침: 「시트층 Supabase 참조 0」→엔진_운영배치.js roster-ingest 호출 실재(궤적 코드만 안 읽는다) · 「보장 (Ⅲ-6)」→Ⅰ-6 · 「tests/모델정책.test.js 를 짓는다」→08-05 부터 실재(«대조 검사 0»은 맞음). 갑 틀림(테스트 72 · 「열여섯 중」) 안 가져옴.
- **§8 Prism(을 뼈대)** — 갑에서: 8-0 「돈다/없다」 두 줄 · U 머리 한 줄 요약(「기계는 대조, 사람은 판정」) · 8-4 굵직 13줄 · U4 「걷지 않는 것」 표식 · 8-6 「기각 사유의 결 하나」 문장. 을 틀림 고침: 「§3-4 「CI 는 운영 자격증명을 쥐지 않는다」 유지」→§3 에 그 문장 없음(가리킴 고침) · 「배치는 노트북 밤 사슬(tools/밤샘.js)」→없는 파일 «짓는다» 표식 · 「수리 41건(치명 4·중대 29·경미 8)」→재료 태그로 4·27·10 · 「이해대장 절 이름 「학생 접점 참조 0」」→신설(없는 절) · 「모델정찰.yml 은 키를 안 읽는다」→없는 파일. 갑 틀림(「배치는 구름에서」 · 「91/118」) 안 가져옴.
- **§9 Temper(을 뼈대)** — 갑에서: 9-4 「하지 않는 것」 13줄 훑기 · 그 자리 뜻풀이(「역확률 가중(IPS) … 원리상 불성립」 · 「초안 변경률 = 감시 자」) · 9-1 「세는 자」 한 줄 · 9-6 «가장 큰 것 넷» 서술. 을 틀림 고침: 「talk 에 훅 0 · rot-check --hook 로 대체」→SYNK-talk .githooks 에 pre-commit·post-commit·prepare-commit-msg 실재(rot-check 는 appsscript 에만) · 「첫 울림 09-01 Fable 5.1」 삭제(모델정찰.jsonl 소견 [] · 울림 0). 갑 틀림(저장소 훅 「셋」 섞음 · 「행위자 값은 이미 계약에」 · 「중대 24」) 안 가져옴.
- **§10 Reed(갑 뼈대)** — 갑의 10-0 「돈다/짓고 있다/없다」 셋 · 10-1 「Reed 는 생산자가 아닙니다」 · U 의 짧은 줄·장면·「닫히는 문」 문장 · 10-3 한 줄 셀 · 10-6 산문을 따르되, U1~U9 내용·층 여덟·돈·「AI 두 배 자」·「유호님 자리」·정찰 URL 전량·§10-5 는 을 전량. 을 틀림 고침: U1 「live_chat_id 는 채팅쓰기 인자(방송상세 «칸» 아님)」→lib/라디오수집.js 방송상세가 그 칸을 반환 · 「heartbeat 열 다섯」→11(기본 6 + 09-02 마이그레이션 5 · insert 는 10열) · 「집중띠축 넷째 인자 청취들」→이미 인자 넷 · 새 인자는 다섯째(네 곳 전부) · 「학원달력·시간표 정본 0」→Code.js 「시간표 정본 v2(07-19 유호 확정)」 실재 · 「D8 ⑩·D8 ②·⑤ 뒤」→을 §12 D8 은 ①~④ 뿐. 갑 틀림(「돈다: 곡 성적표」 · 「스물여섯 중」) 안 가져옴.

한 번에 넷을 띄우면 ≈2M(큰 절 하나 ≈0.5M · 어림 ±30%). 창이 빠듯하면 둘씩.

## 3. 다 끝나면 — 조립·교체(본세션이 한다)
1. `node docs/_ops/엔진7종_v2/자.js 전부` 가 17절 전부 ✅.
2. 머리 = 스크래치 `v2_머리.md`(없으면 이 요지로 새로 쓴다: 정본 v2.0 · 파생 철학 v1.17 · 제작 사슬에 「집필 두 판 → 눈가림 심사 51 → 합친 판 v2(몸통 Fable · 문장 Opus 자 · 쓴 모델 Fable 5.1)」 · 상태 = 제안 · 번호 D1~D28+별항 · 대조 기준 · 시제 규약 · 어떻게 읽나) + `v2/§0.md` … `v2/§16.md` 를 순서대로 이어 `docs/엔진7종_상향설계_v2.md` 로 저장(LF).
3. `git rm docs/엔진7종_상향설계_v1.md`(정본 하나 · 보존은 git 3fada1e4b) · 참조 갱신: `docs/문서_지도.md`(엔진7종_상향설계_v1 줄 → v2) · `docs/_ops/트랙.md`(엔진 7종 묶음: 정본 경로 v2 · 「v2 누가 쓰나」 [유호] 줄 지움) · `docs/_ops/엔진7종_상향_전후비교_0904.md`·`모델비교_0903.md` 의 정본 경로 한 줄(「v1 은 v2 로 갈아입었다」 표기) · 기억 `model-compare-opus-vs-fable-0903.md`·`yuho-open-decisions.md`(v2 누가 쓰나 절 지움).
4. `node tools/doc-graph.js` 빨강 0 확인 · 문서 전체에 「판 ①·판 ②·앞 판은」 잔존 0 확인(`grep -c`).
5. 작업 폴더 `docs/_ops/엔진7종_v2/` 는 그 커밋에서 `git rm -r`(도구·규칙·인계는 git 이력) · 커밋 범위 못박기 · push.
6. 기억: `workflow-stage-split-session-limit.md` 또는 새 토픽에 한 줄 — 「600KB 문서 옷 입히기 = 절 17 × Agent 하나 · 절당 0.15~0.5M · 기계 자(U·D·이름·URL·해시 보존)가 내용 손실을 막았다」.
