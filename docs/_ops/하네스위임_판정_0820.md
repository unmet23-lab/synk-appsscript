# 하네스 위임 — 단계 판정과 집행 기록 (2026-08-20)

> **무엇을 정하나**: 유호님이 방향 긍정한 도전안 — 「운영 시스템은 Claude Code 하네스 표준 기능에 단계 위임하고,
> SYNK 손 유지분은 **자산 정본 4벌 + 안전 가드 3 + 결정.md** 로 수렴한다」(결정.md 08-20) — 의 **1단계 실행 범위**.
> 원칙: AI가 발달할수록 하네스 기본 기능이 손수 지은 운영 시스템을 흡수한다. 우리가 손으로 유지할 것은
> **하네스가 대체할 수 없는 것**(브랜드·철학·엔진·마스코트라는 자산, 비가역을 막는 안전, 유호님 확정 기록)뿐이다.

## 1. 스킬 13개 처분표

> 🔴 **이 표는 2026-08-20 «집행 기록»이다 — 현행 판정이 아니다.**
> 현행 정본 = [`하네스재판정_0901.md` §7](하네스재판정_0901.md)(스킬 **16종** 전수 재판정 · 유호 픽 09-01 ㉰).
> 아래 표를 근거로 「이 스킬은 은퇴 대상」이라 읽지 않는다.

| 스킬 | 판정 | 근거 |
|---|---|---|
| deploy | **유지** | 안전 4종의 하나 — 라이브 배포 유일 통로. 하네스가 대체 못 한다 |
| synk-brand · synk-content · synk-design | **유지** | 브랜드 자산의 규칙 그 자체 — 위임 대상이 아니라 지킬 대상 |
| 대장검토 | **유지** | 철학 정본·이해 대장 판정 절차 — 자산 축 |
| emil-design-eng · make-interfaces-feel-better | **유지** | 명품화(디테일) 도구 — 하네스에 없는 전문 지식 |
| forge | **유지** | 유호님이 부르는 결과물 도출 절차(A/B+게이트) |
| last30days | **유지** | 최신 정찰 게이트의 도구 |
| **evolve** | **✅ 은퇴 (08-20 유호 픽)** | 「지침 진화 사이클」은 v10.0(「규칙·훅·테스트를 새로 만들지 않는다 · 장치는 제안만」)과 결정.md 체계가 흡수했다. 마찰 기록(friction.js)과 개정 제안 문턱(rot-check 마찰 2건)은 남는다 — 걷은 것은 «개정 절차»뿐 |
| **missed** | **✅ 은퇴 (08-20 유호 픽)** | 여섯 갈래 중 둘(결정 큐)은 소멸, 나머지는 도구 직접 호출(`friction`·`rot-check`·`원격ci`·`gh pr list`) — 스킬 껍데기 없이 그 도구들을 돌면 된다 |
| **afk** | **✅ 은퇴 (08-20 유호 픽)** | 「밥 먹고 올게」는 자연어로 알아듣는다 — 정지선(승인 자리)은 CLAUDE.md 안전 절이 기계·상주로 이미 진다. 벽 기록 규약은 세션 끝 보고가 잇는다 |
| **close** | **유지 (몸통 교체)** | 몸통이던 보드·인계문 두 층이 08-20 에 걷혔다 — 그 자리에 PR 본문·`list_sessions` 를 넣어 스킬은 살렸다. 은퇴 여부는 ㉮ 픽 |

## 2. 보드·인계문 — 단계안

지금 선 자리: **PR = 작업 목록**(유호 확정 08-15)이 보드의 「선언」 역할 절반을 이미 흡수했다.
보드에 남은 고유 가치는 master 직발 단발 작업의 선언·세션 상태 칸이고, 인계문은 계정 갈아타기 바통이다.
하네스는 세션 목록·전사 검색·백그라운드 태스크를 이미 제공하지만 **다중 세션 조율은 아직 얇다**
(오늘도 로컬+클라우드 세션 4개가 초 단위로 master 를 경합했다 — 이 층을 지금 걷으면 그 경합이 무방비가 된다).

- **1단계 (✅ 2026-08-20 집행 · `8780e5f3`)** — **인계문 층**을 하네스에 넘겼다(도구 2벌 · 조각 22개 · 선언판 항목 2).
  이 문서가 권한 「스킬 은퇴 먼저」 대신 유호님이 인계문부터 잡았다 — **되돌리기가 제일 싼 층**이라서다(결정.md 08-20 집행 순서).
- **2단계 (✅ 2026-08-20 집행)** — **보드 층 철거.** 트랙 선언의 정본은 **열린 PR 본문** 하나, 세션 조율은 하네스가 진다.
  걷은 것: `docs/_ops/보드/` 11벌 · `docs/세션보드.md` · `docs/세션보드_아카이브.md`(761KB) · 보드 도구 8벌 ·
  대기열 2벌(자료 `작업대기열.md` 는 08-19 에 이미 삭제돼 그때부터 죽어 있었다) · `작업본소유자.js`.
- **3단계 (열림)** — §1 의 스킬 처분(㉮). 유호님 픽 전까지 스킬은 그대로 둔다.
- **앞당김 트리거**: 하네스가 세션 간 조율 기능을 더 내면 그 시점에 단계를 재판정한다(새 모델·하네스 업그레이드 재검토 관례와 같은 축).

## 3. 대가 — 이 방향이 틀릴 때의 모습

1. **하네스 종속** — Claude Code 기능 변경에 운영이 흔들린다. 완충: 자산 정본·결정.md·안전 가드는 전부 git 안이라 하네스와 무관하게 산다.
2. **한마디 트리거 상실** — afk·close 를 걷으면 「밥 먹고 올게」·「끊자」의 절차 보장이 약해진다(자연어로 알아듣긴 하나 순서 보장이 없다).
3. **조율 공백** — 보드를 너무 일찍 걷으면 다중 세션 경합(오늘 실측 4세션)이 무방비가 된다. 그래서 2단계를 관찰 뒤로 뒀다.

## 4. 도전안 병기 — 「단계 없이 한 번에」

운영 스킬 4개(evolve·afk·missed·close)를 **오늘 전부 삭제**하고 보드·인계문도 같이 걷는다.
문제가 생기면 git 이력에서 되살린다 — v10.0 「낡은 것은 남기지 않는다 · 보존은 git 이력」의 끝.
대가: 되살리는 날의 재작업 + 과도기 세션들이 조율 없이 경합. **권고하지 않는다** — 오늘 하루만도
반입 9건이 삭제 근거를 뒤집는 일이 있었다(실측 후 건별이 이 저장소에서 반복 검증된 속도다).

## 5. 유호님 픽 — 결과

- **㉮ 스킬 은퇴 범위**: **✅ 08-20 집행 — evolve·missed·afk 셋 은퇴**(유호 픽 「+afk 까지 셋」).
  close 는 유지 — 몸통을 PR 본문·`list_sessions` 로 교체해 살렸다. 걷은 셋의 남는 배선:
  rot-check 의 개정 문턱(마찰 2건)은 **산다** — 스킬이 아니라 「제안하라」는 신호였다. afk 의 정지선은 CLAUDE.md 안전 절이 이미 진다.
- **㉯ 선언 → PR 일원화**: **✅ 08-20 집행.** 「1~2주 관찰 뒤」 권고를 안 기다린 이유 = 관찰이 필요했던 질문에
  실측이 먼저 답했다 — 살아 있던 세션 3개 중 보드 줄을 가진 세션이 **0개**였고, board-guard 는 08-19 에
  이미 걷혀 보드는 아무것도 강제하지 않고 있었다. 관찰해야 할 「경합」의 재료 자체가 없었다.

다음 픽이 나오면 그 자리에서 집행하고 `docs/_ops/결정.md` 에 적는다.
