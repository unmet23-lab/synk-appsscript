# 워크트리 규약 — 세션마다 제 책상 (v1 · 2026-08-26)

> **왜 생겼나.** 세션 여섯이 **작업본 하나**를 함께 써서, 「내 몫만 커밋한다」고 해도 남의 미커밋이
> 함께 실려 갔다 — **여덟 번**(마지막 **넷이 08-26 하루**). 잃은 글자는 0이지만 장부가
> 「누가 왜 했나」를 틀리게 적었다.
> 🔴 **뒤 둘이 이 판정을 굳혔다**: 일곱째는 그 **대비 절차를 «실행하는 도중»에** 났고(`index.lock`
> 경합으로 뭉치 가르기가 밀리는 사이 남이 커밋했다), 여덟째는 **이 규약을 짓는 커밋에서** 났다.
> ⇒ 손 규율로는 못 이긴다 — 절차는 「내가 커밋할 때」만 듣는데 사고는 「남이 커밋할 때」 난다.
> 자리를 갈라야 위험 자체가 없어진다.
> 🔑 **손이 여럿이라는 것도 실측이다** — 다섯째는 다른 세션이 스스로 밝혔다(08-26 · 「내가 냈다 ·
> 그때 본 것은 `git status`(그 순간의 사진)와 `git diff --stat`(이름만 셈)이고 둘 다 원리상 남의
> hunk 를 못 본다」). 즉 조심의 문제가 아니다.
>
> 실측 판정(08-26 · 트랙 §1 ⏭)은 **「쓸 수 있다」**로 끝났다. 이 문서는 그 «어떻게»다.

---

## 0. 한 문장

**코드를 만지는 레인은 제 워크트리에서 짓고, 유호님이 여시는 것을 만드는 레인은 메인에 남는다.**

---

## 1. 누가 쓰고 누가 안 쓰나 — 기준은 하나다

> **「유호님이 이 산출을 커밋 «전»에 여실 일이 있는가」**

실측(08-26): 유호님 바탕화면 바로가기 **44개가 전부 `docs/` 를 가리킨다.** 코드(`Code.js`·`엔진_*.js`·
`lib/`·`tools/`)를 가리키는 것은 **0개**다. 그래서 갈림이 깨끗하다.

| 레인 | 자리 | 왜 |
|---|---|---|
| **엔진·코드 트랙**(`Code.js`·`엔진_*.js`·`lib/`·`tools/`·`tests/`·talk 연동) | 🟢 **워크트리** | 유호님이 커밋 전에 여실 일이 없다 ⇒ 가시성 대가 **0** |
| **청소·구조 트랙** | 🟢 **워크트리** | 위와 같다 |
| **굽기 레인**(시안 PNG·HTML·영상) | 🔵 **메인** | 아침에 바탕화면으로 **바로** 보셔야 한다 |
| **문서·판정 지면 레인**(`docs/**.md`·`docs/**.html`) | 🔵 **메인** | 같은 이유 — 만드는 중에도 여신다 |
| **배포**(`/deploy`) | 🔵 **메인** | 라이브에 나가는 것은 master 에서만 (§5) |

⚠ **애매하면 메인이다.** 워크트리는 편의고, 유호님이 못 보시는 것이 손해다.

---

## 2. 어떻게 들어가나 — 새 도구를 안 짓는다

하네스에 이미 있다(CLAUDE.md 「새 운영 장치를 짓기 전에 하네스에 이미 있는지 본다」).

```
EnterWorktree({ name: "엔진/저작신뢰" })     ← 세션 첫머리에 한 번
ExitWorktree({ action: "keep" })              ← 일이 남았으면 keep · 끝났으면 remove
```

- 🔴 **이름은 «영문·숫자·`.`·`_`·`-`» 뿐이다 — 한글이 안 들어간다**(08-26 실측: `엔진/4회전` 거절).
  ⚠ 이 문서 첫 판이 `엔진/…`·`청소/…`·`코드/…` 를 예로 들었는데 **셋 다 거절당한다.** 실물 오류:
  「each "/"-separated segment must be non-empty and contain only letters, digits, dots, underscores, and dashes」.
  ⇒ **이름 규칙 = `<lane>/<track>` 로마자** — `engine/round4` · `cleanup/deadwords` · `code/fsrs`.
  두 마디(`/`) 자체는 **된다**. 디렉터리는 `/` 가 `+` 로 바뀌어 앉는다(`.claude/worktrees/engine+round4`).
- 자리는 `.claude/worktrees/<이름>` — `.git/info/exclude` 에 있어 메인 눈에 안 띈다.
- 비용 = **954MB · 5.9초**(실측). 여섯이면 5.7G · 남은 디스크 110G.
- ⚠ 가지 기준은 `origin/master` 다(하네스 기본 `worktree.baseRef: fresh`). **안 민 로컬 커밋은 안 따라온다** — 들어가기 전에 `git log origin/master..master` 가 비었는지 본다.

---

## 3. 언제 합치나 — **커밋한 그 자리에서**

```
git push -u origin <내 가지>          # 내 것부터 남긴다
git fetch origin && git rebase origin/master   # master 를 당겨 얹는다
git push origin HEAD:master           # master 로 민다
```

🔴 **미루지 않는다.** 워크트리는 「내 것이 안 섞인다」를 사는 대신 **「안 합치면 아무 데도 없다」**를
같이 산다. 지금까지는 커밋만 하면 메인에 있었지만, 이제는 합쳐야 있다.
CLAUDE.md 「커밋과 push 는 한 벌이다」가 **커밋·push·합치기 한 벌**로 늘어난다.

---

## 4. 공용 원장(`트랙.md`·`결정.md`)은 어떻게 되나

**적는 방식은 그대로다.** 자기 트리에서 그 자리에 적고 합친다. 실측(08-26 · `git merge-file`):

| 두 세션이 | 결과 |
|---|---|
| **다른 절**에 적었다 | ✅ **자동으로 합쳐진다**(둘 다 살아남는다) |
| **같은 절, 떨어진 줄**에 적었다 | ✅ 자동으로 합쳐진다 |
| **정확히 같은 줄자리**에 적었다 | ⚠ **충돌 — 소리가 난다** |

🔑 오늘 사고 일곱 건은 **전부 다른 절**이었다 ⇒ 켰더라면 **일곱 건 다 자동으로 합쳐졌을 것**이다.
그리고 마지막 줄이 이 규약의 값이다 — 지금은 같은 자리를 만져도 **소리 없이** 한쪽이 실려 간다.

---

## 5. 검증 규율 — 이게 워크트리가 실제로 사 오는 것

**초록은 이제 «내 트리»에서 센다.** 지금까지는 남의 미커밋이 내 검사에 섞여 있었다. 실측(08-26):
`강사지식` 굽기 검사가 **남의 미커밋 `.txt` 14벌 때문에** 내 세션에서 빨갛게 떴고, 진단에 시간이 들었다.
🔴 **더 위험한 반대 방향** — 남의 미커밋이 내 결함을 **가려 초록으로 통과시킬 수 있다.** 증상이 없다.

- 워크트리에서는 그 오염이 **원리상 없다**. 초록이 내 것이면 정말 내 것이다.
- 그래서 「얼린 트리로 재기」(memory `frozen-tree-ci-measurement`)의 이유 절반이 사라진다.
- ⚠ 다만 **형제 저장소(SYNK-talk)는 여전히 한 책상**이다(§7).
- 🔴 **그리고 그 예외가 생각보다 넓다 — 형제의 «교차 검사»는 워크트리가 못 지킨다**(08-26 실측).
  talk 의 `tools/강사지식빌드.js` 는 원본을 `path.resolve(뿌리,'..','SYNK-appsscript')` 로 찾는다 —
  **내 워크트리가 아니라 «메인» appsscript 작업본**이다. 즉 appsscript 를 워크트리로 갈라도
  talk 에서 도는 교차 검사는 여전히 메인의 미커밋을 본다. ⇒ **§5 의 보증은 «한 저장소 안»에서만 산다.**
  🔑 **곁에서 값진 것 하나** — 그 빨강을 나는 「남의 미커밋 탓」으로 진단하고 있었는데 **틀렸다.**
  12벌 원본의 커밋·미커밋을 하나씩 재보니 미커밋 **0건**이고, 갈린 것은 **커밋된 정본 7벌**이었다
  (진짜 굽기 빚 · talk `aefb6a8` 에서 갚았다). ⇒ **「남의 오염이겠지」는 진단이 아니라 추측이다** —
  워크트리를 켜면 이 추측이 더 그럴듯해 보여서 오히려 위험하다. 세어서 답한다.

---

## 6. 배포 — master 에서만

`/deploy`·`clasp push` 는 **메인 트리에서 master 로** 한다. 워크트리에서 배포하면 그 가지의 코드가
라이브로 나가고, 그건 아무도 안 본 판이다. **합치고 → 메인에서 배포한다.**

---

## 7. 켜기 전에 고친 것 — 전수 훑기 (08-26)

규약만 세우고 켰으면 **조용히 틀린 답**이 열 자리에서 났다. 파일 **518개 / 여섯 표면**을 훑고,
점검 워크트리 **둘**(한 마디 · 두 마디)에서 실제로 재현해 **확정 45건**을 얻었다.

**🔴 차단자 열 — 전부 고쳤다**(커밋 `e3abce68`)

| 자리 | 무엇이 조용히 틀렸나 |
|---|---|
| `clasp-guard` BYPASS 순서 | `CLASP_GUARD_BYPASS=1 clasp push` 한 줄로 **워크트리에서 라이브로 밀 수 있었다** |
| `git-scope-guard` `input.cwd` | 가드가 **남의 트리**로 판정 — 되감기가 워크트리에서 오차단, 반대 방향은 조용한 소멸 |
| `git-scope-guard` stash | **stash 스택은 트리 공용**인데 「내 트리」만 재서, 남의 조각을 내 트리에 쏟는 길이 열려 있었다 |
| `install-githooks` | 어느 트리에서 설치해도 과녁이 **메인의 공용 `.git/hooks`** — 가지 판이 모두의 훅을 덮었다 |
| `test-ci` 스냅샷 | 워크트리 사본 21벌을 **두 벌로** 셌다 |
| `agents/reviewer.md` | `-C <메인 절대경로>` 라 리뷰가 **남이 고치는 중인 것**을 읽었다 |
| `agents/builder.md` | 저장소 자리를 절대경로로 못박았다 |
| `skills/deploy` ×2 | 작업 디렉터리 절대경로 고정 · `push origin master` 가 워크트리에선 **백업 없이 초록** |
| `skills/close` | `cd ../SYNK-talk` 가 워크트리에서 죽는다 |
| `결정.js` 첫 화면 | 확정 178건 대신 **175건**을 보여주며 낡았다는 표시가 **0**이었다 ⇒ 「N커밋 뒤처졌다」 한 줄 신설 |

**남은 확정 28건 — 대부분 원리상 안 닿는다**

| 갈래 | 수 | 처분 |
|---|---|---|
| 문서·굽기·내보내기 도구 | 18 | **메인 잔류 레인**이라 워크트리에서 안 돈다(§1). 그 레인을 워크트리로 옮길 때만 연다 |
| 설정·상태 | 6 | `settings.json` 권한 목록이 메인 절대경로 꼴 — **유호 판정 자리**(권한은 내가 안 고친다) · 나머지는 「성능만」·「안 깨짐」 |
| 형제(talk) 저장소 | 1 | `talk/tools/precommit.js` 가 `../../` 로 appsscript 를 찾는다 — talk 을 가를 때 |
| 코드 레인 | 3 | `clasp-guard.check.js` 워크트리 탐색(고침) · 나머지 둘은 「안 깨짐」 |

## 8. 알고 켜야 할 것 넷

1. **생 `grep -r` 은 오염된다** — 워크트리 사본이 **먼저** 나온다(실측: 첫 8건 전부 사본).
   하네스 Grep(ripgrep)과 저장소 도구(rot-check·테스트 580)는 **깨끗하다**(`.git/info/exclude` 를 읽는다).
   ⇒ 검색은 Grep 도구를 쓴다. 굳이 셸이면 `--exclude-dir=worktrees`.
2. **형제 저장소는 따로다** — appsscript 워크트리를 켜도 `SYNK-talk` 은 여전히 한 작업본이다.
   (talk 은 이미 워크트리를 하나 쓰고 있다 — codex 기준선 레인.) talk 을 함께 가를지는 별건.
3. **형제 자리를 «경로 모양»으로 짐작하지 않는다** — `.claude/hooks/lib/형제저장소.js` 하나가 답한다.
   08-26 에 아직 짐작하던 두 도구(`앱로고굽기.js`·`이해대장.js`)를 이전했다.
4. **굽기 산출은 메인에 있어야 한다**(§1) — 워크트리에서 구우면 유호님 바로가기에 안 뜬다.
5. 🔴 **워크트리 세션의 셸이 «좁아진다» — 08-26 첫 실사용에서 세 번 막혔다.**
   하네스가 「이 명령이 워크트리 밖으로 안 나가는가」를 못 확인하면 **실행 자체를 거절**한다:
   · `git -C <다른 저장소>` — **거절**(「redirects git to the shared checkout via -C」).
     ⇒ 워크트리에서 **메인 appsscript 의 git 상태를 못 읽는다.** 형제(talk)는 `git -C` 로 되지만
       그것도 **명령이 단순할 때만**이다.
   · **복합 명령이 거절된다** — `cd X && node …`, 심지어 `echo a; pwd` 도 「too complex」로 막혔다.
     ⇒ 한 줄에 하나씩 쪼갠다. 여러 줄이 필요하면 **스크립트 파일로 써서 `node <파일>`** 로 부른다
       (따옴표 탈출까지 같이 피해진다 — 오늘 세 번 그렇게 풀었다).
   · 읽기는 하네스 도구(Read·Grep·Glob)가 절대경로로 다 된다 — 셸을 우회하는 길이 그쪽이다.
   ⇒ **값**: 이 좁아짐이 곧 §5 의 보증이다(남의 트리를 못 만지니 오염도 없다). 다만 **일하는 손이
     바뀐다** — 「긴 한 줄」 버릇을 「짧은 여러 줄 + 스크립트 파일」로 바꿔야 한다.

---

## 9. 되돌리기

```
ExitWorktree({ action: "remove" })              # 세션이 스스로
git worktree remove --force .claude/worktrees/<이름>   # 밖에서
git worktree list                               # 지금 몇 개인가
```

규약을 접으려면 이 문서를 지우고 세션들이 메인에서 짓게 두면 된다 — **되돌리는 비용이 0인 변경이다.**
