# 엔진 지침 정리 — 2026-09-13

최근 작업·현재 원문·실제 소비 코드를 기준으로 일곱 엔진의 지침을 압축하고 발전을 막던 과거 조건을 제거한 결과다. 새로운 제품 기능이나 학생 효과를 구현·검증한 보고서는 아니다.

## 정리한 범위

정리 전 Git `8b80a507168d` 대비 관리 문서 23개는 **14,538→3,370행(76.8% 압축)**이다. 새 결과 보고서·코드 주석·개인 스킬은 이 분모에 넣지 않았다. 통합 설계는 **8,107→722행**이며 §0~16, 엔진별 U 59개, D1~D29와 별도 동의 질문을 포함한 질문 키 30개를 유지했다.

| 엔진·연결 | 제거·정정한 내용 |
|---|---|
| Core | E0 코드 0·파생 테이블/뷰 영구 금지·고정 연차. 추천 목표와 학생 응답의 현재 계약·소비 경로로 정리 |
| Loom | 사진 픽셀/특정 렌더러만 허용, 실시간 보류, AI 캐릭터 표현 전면 금지, 모든 진행 표시 금지, 고정 도입 순서 |
| Vellum | 당시 표정 수·말 걸기/대화의 포괄 금지·옛 반응과 검수 순서. 현재 재질 고정과 실제 상호작용 경계 보존 |
| Trail | 과거 학생 0·발급/재퇴소/시즌 연결 미구현 선언, 옛 회고 일정·판정 대기 |
| Prism | 고정 모델·조사 회차와 중복 설명. 오류 기회 분모·실제/합성·훈련/평가 분리 보존 |
| Temper | 미완료로 남은 호출 상태·재승인 서사. 개입 스냅샷·두 기준시각·관측·인과 해석 경계 보존 |
| Reed | MiniMax 신규 양산 지침·앱 개인 청취 수집 부활·라디오 도입 대기·옛 송출 방식 고정 |
| 공통 제작 통로 | 문서와 충돌하는 코드 주석·토큰 설명·스킬을 정리하고 조판 힌트의 임의 개수 제한을 제거 |

현재 기준은 [엔진 7종](../엔진7종_상향설계_v3.md)과 각 담당 정본이다. 최근 승인된 [Loom 움직임 표현](../Loom_실시간장면_표현기준.md)은 그대로 기본 적용한다. 완료된 비교·실험은 문서 안의 고정 Git 링크에서 확인하며 현재 지침으로 복제하지 않는다.

## 보존한 것

승인 원본·마스코트 정체성·의상 모델 지정, 색·재질·폰트 값, 표정 변화 영역 밖 픽셀·알파, 실제 동의·철회·인증·훈련/평가 분리·원본/당시 근거·정정 계보를 보존했다. 구현되지 않은 계약·소비자는 미구현으로 남겼다. 제품의 명시적인 현재 선택을 일괄 삭제하지 않았다.

## 확인

- Loom 상세 7문서를 다른 담당자가 소비자·CLI·원본 보호와 대조했고 계약 누락 지적은 없었다.
- 캐릭터 재질 고정 절은 정리 전과 동일하다. 디자인 토큰은 설명 문자열만 바뀌었고 값·자산·색과 생성 CSS는 유지됐다.
- 조판 힌트의 개수/현재 이름 고정 시험을 뜻·지원 형식의 구조 검사로 바꿨다. 모르는 힌트·미지원 형식·줄 누락 감지는 보존했다.
- Appsscript 통합 시험 112개, Core 관련 묶음 70개가 통과했다. 두 묶음의 철학 인용 시험은 중복이므로 합산하지 않는다.
- Talk 주석 정리는 관련 시험 37개와 학습자상태 시험 67개가 통과했고 주석을 제외한 실행 본문은 동일하다.
- 개인 스킬 `C:/Users/q1212/.agents/skills/synk-design/SKILL.md`는 짧은 UI 전환과 바람·캐릭터·영상의 시간을 구분하도록 좁게 수정했다. 스킬 검증과 독립 대조를 통과했다.
- 관리 문서의 로컬 링크 224개와 고정 Git 원문 20개는 대상이 존재한다. 학생 기록·실물 문서의 관련 조판 회귀 44개도 통과했다.
- 통합 설계 최종본은 Core·Trail·Prism·Temper 담당자가 D 결정·부록까지 독립 대조했다. 누락된 계약 지적은 없었고 현재 정본 링크와 미구현 표시를 보강했다.

## 통합과 확인 범위

다른 작업의 미커밋·스테이징을 보존했다. Talk 원격이 먼저 진행되어 격리 작업 사본에 이번 주석 수정만 합쳤다. 원격 반영 커밋은 `37fe129`·`0e20060`이며 기존 배포 장부 스테이징의 blob은 동일하게 유지했다. 로컬 원래 작업 사본의 다른 변경·분기 상태를 덮어쓰지 않았다.

앱 실행 동작·장면 이미지·배포는 바꾸지 않았다. 주석 파일도 동봉 지문에 포함되므로 기존 Talk 배포 대조 도구는 소스 차이를 표시할 수 있다. 이번 지침 정리를 서버 배포·실기기 확인으로 보고하지 않으며 검사·배포 도구를 우회하거나 삭제하지 않았다.
