# 인계 — 엔진7종 v3 ④ 소급 불가 절 심문 (2026-09-05 저녁 · 세션 맥락 910k 에서 씀)

> 🔴 모델은 Fable 5.1 · xhigh(유호 확정 09-05). 유호님은 «똑똑한데 이 분야를 처음 보는 사람» — 한 답에 주제 하나 · 쉬운 낱말 · 낯선 낱말은 그 자리에 괄호 뜻.
> 유호님 마지막 말씀(09-05 저녁): 「심문 끝나면 결과 읽고 정리해줘」. 그 일이 이 인계의 전부다.

## 지금 상태 한 줄
**엔진 설계 v3(`docs/엔진7종_상향설계_v3.md`)는 열일곱 절 합침이 끝났고(v2 삭제 · 커밋 856089ca8), ④ 심문 여섯 대상이 뒤에서 돌고 있다. §3 은 판정·고침까지 끝났고(커밋 6937ff7f9), §11 은 아스트라 결과만 있다(제미나이는 네트워크 실패 → 다시 던진다).**

## 실물 자리
- 정본 = `docs/엔진7종_상향설계_v3.md` · 합치기 장부 = `docs/_ops/엔진7종_v3/_합치기.md` · 심문 판정 = `docs/_ops/엔진7종_v3/_심문④_판정.md`(§3 끝 · 나머지 절은 여기에 이어 적는다).
- 심문 대상 발췌 = `docs/_ops/엔진7종_v3/심문_절{3,11,12,16}.md` + `docs/앱재설계_v2.md` + `docs/살아있는자리_설계_v1.md`. 결과 원문 = `docs/_ops/심문결과/심문_절N-<지문>-{astra-xhigh-1,gemini-high-2}.md`(앱 정본 둘은 파일 이름이 그 문서 이름).
- 사슬 로그 = `docs/_ops/엔진7종_v3/_심문④_로그.txt`(미추적 · 커밋 안 함). 도구 = `node tools/codex-review.js --심문 <문서> --timeout 1800`(같은 문서를 다시 던지면 끝난 회차는 이어받고 실패한 회차만 다시 돈다).
- 고침은 델타로: `docs/_ops/엔진7종_v3/델타_심문④_절N.txt` → `node tools/엔진7종_델타적용.js docs/엔진7종_상향설계_v3.md <델타> --시험 && … (적용)`.

## 남은 일(순서대로)
1. 사슬이 끝났나 = 로그 마지막 줄 `=== 끝 코드 …`가 대상 여섯에 다 있나. 안 끝났으면 기다린다(대상마다 10~15분).
2. **§11 제미나이 다시 던지기**: `node tools/codex-review.js --심문 docs/_ops/엔진7종_v3/심문_절11.md --timeout 1800`(아스트라 회차는 이어받음 · 제미나이만 다시).
3. 결과를 읽고 `_심문④_판정.md` 에 절마다 표(참/부분/틀림 · 채택/기각)를 잇는다. **P0 기각은 유호님께 한 줄**. 유호 자리로 올라갈 것은 §12 D 에 «권고»로 적는다(지금까지 하나 = 산문 콜드 보존 · D7 ⓑ).
4. v3 안의 자기모순·산수·번짐 누락은 델타로 닫는다(§3 는 닫았다 · §11 은 이 인계 아래 «§11 아스트라 판정 초안» 참고).
5. 커밋은 못박아서: `git commit -m "…" -- docs/엔진7종_상향설계_v3.md docs/_ops/엔진7종_v3/_심문④_판정.md docs/_ops/엔진7종_v3/델타_심문④_절N.txt`. 🔴 push 가 「remote end hung up」으로 죽는 중(16:37 · 네트워크) — `git status -sb` 가 ahead 면 다시 민다(http.postBuffer 를 512MB 로 올려 뒀다).
6. 다 되면 유호님께 한 답: «무엇을 고쳤나 · 유호님 자리(D7 권고 갱신 + P0 기각 한 줄) · 다음 = ⑤ 채택(각 정본 갱신)». 트랙 §0-재설계 39줄과 483줄의 「🔴 다음 = ④」를 ✅ 로 바꾸고 ⑤ 를 다음으로.

## §11 아스트라 판정 초안(내가 읽고 적은 것 · 제미나이 결과 오면 합쳐 표로)
- P0 ① goal_used(목표를 읽었나 스탬프)는 «배정 당시» 기록이라 첫 배정(02-11)부터 있어야 하는데 걸음 11 이 빛과 함께 개원 뒤로 뒀다 → 참 · 고침 = 스탬프 «생산»(코드 사실)은 02-11 전 · 빛 «화면»만 뒤.
- P0 ② D1 표에 후보 집합·배정확률·정책 분기 자리 없음 → 참 · **§3 고침(policy_trace)으로 이미 닫힘**.
- P0 ③ 맞아? 카드 첫 답 = 02-11 첫 주인데 관찰선택자·카드 동작은 걸음 15(첫 시즌 안) → 참 · 고침 = «최소 판»(짐작 하나 · 맞다/아니야 · 비어 있는 것부터 규칙)을 02-11 전 조각으로 떼고 관찰선택자는 그 위에.
- P1 ④ moment_id 값목록·학습개입 열이 c18 인데 첫 순간 반응(companion 개입)은 02-11 전 → 참 · 고침 = 값목록 «첫 벌 셋»(첫 제출·첫 녹음·첫 무오류)만 c17 로.
- P1 ⑤ 리허설 출구 9/9 를 요구하는데 V8·V9 는 걸음 17·13 것 → 참 · 고침 = «걸음 0~7 이 세운 검증점 n/n» · V8·V9 는 이름 예정으로 제외 명시.
- P1 ⑥ 재료 봉투가 걸음 10(개원 첫 주)인데 «나를 알고 챙기는» 첫 소비자는 02-11 → 참 · 고침 = 템플릿 판(LLM 0)만 02-11 전(걸음 4 앱 쌍) · LLM 판은 걸음 10.
- P1 ⑦ 일기 본체(작성·저장·전사·날짜 목록·어휘 지도 소비)의 걸음이 없다 → 참 · 고침 = 걸음 1(계약)+걸음 4(앱 쌍) · 자리는 D29.
- P1 ⑧ 걸음 7 최종 마감 = 개원일인데 첫 데이터는 02-11 → 참 · 고침 = 체험용 판의 E1~E4 는 02-11 전 · 개원 전 재확인 1회.
- P1 ⑨ 걸음 0 에 처리량 재는 항목이 없다 → 참 · 고침 = 걸음 0 ⑯ «처리량 자»(대표 작업 셋 완료 시간·재작업·외부 대기·가용 작업일).

## 밟은 함정(이 세션)
- 심문 도구는 «던지기» 없이 부르면 동기(끝날 때까지 기다림)로 돈다 — 배경 Bash 로 여섯을 순서대로 돌렸다(`_심문④_로그.txt`). 동시 두 프로세스(아스트라+제미나이)는 도구가 알아서 한다.
- 제미나이 3.1 Pro 호출이 「fetch failed」로 죽을 수 있다(16:24 §11) — 같은 문서를 다시 던지면 그 회차만 다시 돈다.
- 델타 파일 블록에 `===` 구분선을 빠뜨리면 「원문이 0번」으로 멈춘다(§14 에서 한 번 밟음).
- 인계문 「3.8 vs 3.1 을 ④ 첫 절에서 한 번 잰다」는 안 했다(도구 편성이 3.1 Pro 하나). 유호님이 원하시면 그때.
