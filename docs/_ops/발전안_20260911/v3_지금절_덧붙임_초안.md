# v3 「지금」 절 09-11 덧붙임 — 붙여 넣을 초안

> 2026-09-11 · 코드 기준 실측(git · 함수 이름) · 라이브 반영은 별도. 붙이는 자리 = `docs/엔진7종_상향설계_v3.md` 각 엔진의 「지금」 절 끝. 지금 절 본문은 고치지 않고 아래 인용 블록만 그 끝에 더한다. 절의 옛 문장이 이제 틀린 것은 블록 안 「지금 절에서 이제 틀린 문장」 줄이 가리킨다. 붙인 뒤 v3 머리 ⓪의 「09-09 철학 v2 영향 대조 범위」 줄에 「09-11 지금 절 덧붙임」을 한 줄 더한다.

## §4-1. Core

```
> **09-11 덧붙임(실측 09-11 · 코드 기준 · 라이브 반영은 별도).**
> - 돈다로 바뀐 것: 학생의 말 통로가 섰다 — 엔진_진단.js `진단멈춘까닭_`·`진단고침_`(922fed3e4)가 학생이 쓴 한 줄 200자를 진단세션에 적고, 엔진_수집.js `증언남기기_`·`증언맵_`(49eb76979)가 증언 그릇 testimony_log 를 세워 엔진_콘텐츠AI.js `aiStudioBatch_` 가 그 말을 되받는다.
> - 돈다로 바뀐 것: 학생 본인에게 나가는 자동 발송이 섰다 — 엔진_운영배치.js `시즌회고발송_`·`회고수신자_`(49eb76979)가 보호자 메일이 없으면 학생 본인 메일로 보내고, 엔진_진단.js `diagWriteBatch_`→`진단카드메일_`(ba9c319ae)가 진단자 본인 메일로 카드를 낸다.
> - 여전히 없다: 개입→효과 짝 되먹임 · 재심 실행 고리(`tools/모델정찰.js` 는 09-05 뒤 커밋 0 · 출처에 Anthropic 여전히 0) · `엔진점수.jsonl` 쓰는 코드 0 · 경로 B 승격 발행 0 · 실학생 0.
> - 지금 절에서 이제 틀린 문장: 「학생의 말 통로 0(엔진추천목표 코드 0)」 → 진단 두 통로와 증언 그릇이 돈다(922fed3e4 · 49eb76979).
> - 지금 절에서 이제 틀린 문장: 「수신자 = 자동 발송 53 중 학생 본인 0」 → 회고 편지 본인 무대와 진단 카드 메일이 본인에게 간다(49eb76979 · ba9c319ae).
> - 지금 절에서 이제 틀린 문장: 「시트 28종」 → 엔진_셋업확장.js 골격의 수집 표식 탭이 지금 31벌이다(49eb76979 가 「30→31」 · testimony_log · DIAG_SESSION_TAB_).
```

## 5-0. Loom

```
> **09-11 덧붙임(실측 09-11 · 코드 기준 · 라이브 반영은 별도).**
> - 돈다로 바뀐 것: 정본 4K 그림이 앱까지 들어왔다 — talk `afbae68`(마스코트 09-05 정본 교체 · 「재염색_」 접두 걷음) · `3ff18cc`(몽글 표정 넷) · `a432d8d`(마린) · `11cd442`(까몽 표시 배율).
> - 돈다로 바뀐 것: 몽글 탭 반응 08-25 봉인이 풀렸다 — talk `6a7b908` · src/마스코트.js · 놀람→기쁨·눈웃음·윙크 셋 중 하나 · 연타 3회부터 민망.
> - 돈다로 바뀐 것: 곡 굽기가 클라우드 창구(Vertex)로 옮겨졌다(talk `8e32d79`) · 노랫말 길이 상한으로 「제 발로 끝나는가」를 소리로 잰다(talk `40545fb`).
> - 여전히 없다: 만짐을 사건으로 보내는 코드 0(src/마스코트.js 사건보내기 grep 0 · 09-11 재측) · 반응 상태기계 0 · 학생 데이터 0줄 · 재심 발동은 사람 손.
> - 지금 절에서 이제 틀린 문장: 없다. 「탭·쓰다듬 핸들러는 있으나 사건보내기 0건」은 그대로 맞다 — 바뀐 것은 «핸들러가 무엇을 하는가»뿐이다(`6a7b908`).
```

## §6-1. Vellum

```
> **09-11 덧붙임(실측 09-11 · 코드 기준 · 라이브 반영은 별도).**
> - 돈다로 바뀐 것: 학생에게서 걷는 통로가 열렸다 — talk `0ed5ad5` 가 engine.stt_raw 를 세우고 functions/transcribe 가 원신호를 INSERT 한다. 그 고리는 `1a514b2` 가 learning_events.event_id 로 바꿨다(submissions.event_id 엔 유일키가 없어 리허설이 거절했다).
> - 돈다로 바뀐 것: 확인 답 「하루 두 장」 — talk `0ed5ad5` · 카드마다 하루 1행 색인 · functions/progress 가 오늘 답한 카드를 뺀다.
> - 돈다로 바뀐 것: 제출→답장 고리를 앱이 스스로 갱신한다 — talk `a524e25`(09-09) · lib/답장갱신.js · src/제출API.js · 재전송해도 사건 1개 · 늦게 온 교정을 앱 재시작 없이 받는다.
> - 여전히 없다: companion-bake 배치 0(talk supabase/functions/companion · src/몽글API.js · src/몽글문화면.js 09-05 뒤 커밋 0 — 「켜기만 남았다」 그대로) · 맑기·빛 산식 0 · 홈 화면 0.
> - 지금 절에서 이제 틀린 문장: 「학생에게서 걷는 통로 0」 → stt_raw 와 진단 한 줄이 돈다(`0ed5ad5` · 922fed3e4).
> - 지금 절에서 이제 틀린 문장: 「재학생 본인 자동 발송 0/53」 → 회고 편지 본인 무대가 학생 본인 메일로 나간다(49eb76979 `회고수신자_`).
```

## §7-1. Trail

```
> **09-11 덧붙임(실측 09-11 · 코드 기준 · 라이브 반영은 별도).**
> - 돈다로 바뀐 것: 입학 «전» 기록이 학생 번호에 이어진다 — 엔진_진단.js `진단잇기_`·`linkDiagnosisToStudent`·`진단시작점_`(922fed3e4).
> - 돈다로 바뀐 것: 제안 봉투 — 엔진_진단.js `제안봉투만들기_`·`제안봉투상태_`(`b1b2afc5f` · v9.332)가 제안 한 건의 «그때 값»(가격 · 보장 문안 판 · 환불 조건 판 · 잔여 정원 · 제안 시각 · 받은 사람)을 박고 결제·취소를 그 봉투에 잇는다. 원문 보존과 판 번호가 이 갈래에서는 선다. 궤적 엔진 «밖»이다.
> - 돈다로 바뀐 것: 인제스트 매핑 한 칸 — learners.entry_season. 엔진_운영배치.js `명부스윕_`·`입학시즌값_`(a5770b916)이 싣고 talk roster-ingest(`f75e718`)가 빈 자리에만 붓는다. 함수 판 = 리허설 v43 · 운영 v20(docs/_ops/결정.md 09-07).
> - 여전히 없다: 엔진_궤적.js 는 Supabase 를 안 읽는다(grep supabase = 0 · 09-11 재측 · 09-05 뒤 커밋 0) · tests/궤적.test.js·tests/면접궤적.test.js 둘 다 MISSING 그대로 · 졸업·중도이탈 읽는 코드 0 · 재관측 규칙 0.
> - 지금 절에서 이제 틀린 문장: 「학생 본인 수신 0 · 발급물 0장」 → 회고 편지와 진단 카드 메일이 본인에게 나간다(49eb76979 · ba9c319ae).
> - 지금 절에서 이제 틀린 문장: 「남은 것 = 인제스트 매핑 · 회귀 · 라우팅 0」 → 인제스트 한 칸은 붓는다(a5770b916 · talk `f75e718`). 회귀와 라우팅은 0 그대로.
```

## §8-1. Prism

```
> **09-11 덧붙임(실측 09-11 · 코드 기준 · 라이브 반영은 별도).**
> - 돈다로 바뀐 것: 재료 표가 둘 늘었다 — 진단세션(922fed3e4 · ba9c319ae · 문항 스냅샷 · 첫 시도 · AI 산출 · 기준판)과 testimony_log(49eb76979). 둘 다 엔진_셋업확장.js 골격에 수집 표식으로 들었다.
> - 돈다로 바뀐 것: 오류 태그가 첨삭 밖에서도 붙는다 — 엔진_진단.js `diagWriteBatch_` 가 HW_ERROR_TAGS 로 `aiCall_` 을 태우고 model·prompt_ver 를 산출에 박는다(`진단태깅판_`). 「태그 = 첨삭 LLM 자기보고」의 분모가 넓어졌다.
> - 돈다로 바뀐 것: 첨삭 서명 셋(확인자·확인시각·확인지문) — 3e52dfa3f · dbdf7de40 · 38f26191f. 무인 발행이 낸 노출과 사람이 격리를 푼 노출을 가른다.
> - 여전히 없다: 집계층 코드 0 · 사전분포 0 · 산출 소비자 0(09-11 grep · error_bank 읽는 코드 0) · 자동 발음 판정 자 0 · 자산층 규칙 ②③은 여전히 문서 규율뿐.
> - 지금 절에서 이제 틀린 문장: 「「누구=내부」 · 종점 = 사람 지면 · 학생에게 지정 낱말을 알리는 꼬리 한 줄 코드 0」 → 엔진_진단.js `진단카드메일_` 이 「다음 자리」 문형 이름을 진단자 본인 메일로 낸다(ba9c319ae). 그 통로는 자산층 «밖»이라 규칙 ②를 깬 것은 아니다.
```

## §9-1. Temper

```
> **09-11 덧붙임(실측 09-11 · 코드 기준 · 라이브 반영은 별도).**
> - 돈다로 바뀐 것: deliver 안에 개입 갈래가 하나 더 섰다 — talk `7c503ac` `자율일갈래` 가 engine.sunday_bundles 를 읽어 그날 배정 1건으로 낸다(일요일에 게임날이 겹쳐도 자율일이 이긴다). 받는 문 = functions/sunday-bundle(`16c2457`) · 완주 재료 GET(`9697525`).
> - 돈다로 바뀐 것: 짓는 쪽·판정 쪽 — 엔진_자율일.js `sundayBundleBatch_`(토요일 밤) · `sundayBundleJudge_`(월요일 판정) · `cohort1HwBatchWeekly_`(그 주 앱 제출자 전원을 hw_batch 한 행 · `8148456c8` · nightJobs 에서 expandHwBatch «앞»).
> - 돈다로 바뀐 것: 시즌 회고 편지 — 엔진_운영배치.js `시즌회고발송_`·`회고_알게된것_`(49eb76979). 층 셋은 ㉠실력·㉡증언·㉢삶이다.
> - 여전히 없다: 결과변수 0 · 개입 «종류» 칸 0 · policy_ver 칸 0 · 효과 구간 0 · 저작신뢰 등급 거름 0 · 시즌 회고에 Temper 절 0(편지는 섰으나 개입·효과 절은 없다) · talk lib/성과회수.js 는 09-05 뒤 커밋 0.
> - 지금 절에서 이제 틀린 문장: 「개입 행을 실제로 내는 통로 = 과제 배달 하나(deliver)」 → 문은 여전히 deliver 하나지만 그 안의 갈래가 둘이다(`7c503ac`). 겨냥 태그 칸은 09-05 «전»부터 있던 것이라 이 절의 「겨냥 태그 칸 0」은 안 재봤다.
```

## 10-0. Reed

```
> **09-11 덧붙임(실측 09-11 · 코드 기준 · 라이브 반영은 별도).**
> - 돈다로 바뀐 것: 어휘 갈림이 닫혔다 — talk `8d459ab` 가 곡장부의 장르 목록을 손 사본에서 떼고 결ascii 에서 받게 했다. 셋째 장르는 「차분」이고, lib/곡편성.js 의 새벽·밤도 '차분'이다.
> - 돈다로 바뀐 것: 장르 셋 → 일곱 — talk `e6c6a25`(도시 · 드림하늘 · 드림물 · 드림들판). 09-05 에 4K 로 구운 무대가 방송에 닿는 길이 생겼다.
> - 돈다로 바뀐 것: 앱에 라디오 자리가 섰다. 재생기는 아니다 — talk src/라디오화면.js(`0ed5ad5`) = 유튜브 영구 링크와 안내만 · 수집 0 · 사건 0(유호 확정 09-06).
> - 돈다로 바뀐 것: 곡 굽기 통로 Vertex(talk `8e32d79`) · 노랫말 길이 상한(talk `40545fb`). 무대·표지 쪽은 09-07~09-10 에 더 움직였다(추석 무대 6809296dd · K-LOFI 24 e3c5ac1c9 · 배경 선별 9d9a0f19d).
> - 여전히 없다: 저장·실배선 0 · 곡시계 0 · 앵커 열 0 · broadcast_segment 생산자 0 · 방송 0일 · 앱 재생기 0. (09-10 유튜브 라이브 13번 반영은 `docs/_ops/트랙.md` §0-라디오가 쥔다 · 이 절의 「방송 0일」은 v3 의 곡시계·세로 안전띠 그릇 기준이며 실제 송출 일수는 그 트랙 줄로 잰다.)
> - 지금 절에서 이제 틀린 문장: 「곡장부의 «결»은 시간대이고 장르 값은 '자연'입니다 … 같은 낱말이 두 뜻입니다」 → '차분'으로 이어 붙였다(`8d459ab`).
> - 지금 절에서 이제 틀린 문장: 「짓고 있다. 없습니다.」 → talk 쪽 장르·곡 굽기·앱 라디오 자리가 09-05~09-08 에 움직였다.
```

## 한 표로 — 이제 틀린 문장과 아직 0인 것

**이제 틀린 문장(원문 그대로).**
- Core 「학생의 말 통로 0(엔진추천목표 코드 0 · 이름 낮춤 09-02).」
- Core·Vellum 「수신자 = 자동 발송 53 중 학생 본인 0.」 / 「재학생 본인 자동 발송 0/53(08-29 손 실측 · 세는 도구 0)」
- Core 「시트 28종(수집탭워치독 350 · 09-02 portfolio_moments 편입)」 — 지금 골격 셈 31.
- Vellum 「학생에게서 걷는 통로 0.」
- Trail 「학생 본인 수신 0 · 발급물 0장」 / 「남은 것 = 인제스트 매핑 · 회귀 · 라우팅 0.」
- Prism 「학생에게 지정 낱말을 알리는 꼬리 한 줄 코드 0.」
- Temper 「개입 행을 실제로 내는 통로 = 과제 배달 하나(deliver).」
- Reed 「곡장부의 «결»은 시간대이고 장르 값은 '자연'입니다 … 같은 낱말이 두 뜻입니다.」 / 「짓고 있다. 없습니다.」

**일곱 절의 「없다」 중 아직 0인 것(한 줄).**
개입→효과 짝 되먹임 0 · 재심 실행 고리 0 · 엔진점수.jsonl 쓰는 코드 0 · 경로 B 승격 발행 0 · 실학생 0 · 마스코트 만짐→사건 0 · 반응 상태기계 0 · Loom 학생 데이터 0줄 · companion-bake 배치 0 · 맑기·빛 산식 0 · 홈 화면 0 · 엔진_궤적.js Supabase 읽기 0 · 궤적 회귀 시험 0(둘 다 MISSING) · 졸업·중도이탈 읽는 코드 0 · Prism 집계층·사전분포·산출 소비자 0 · 자동 발음 판정 0 · 결과변수 0 · 개입 종류 칸 0 · policy_ver 칸 0 · 효과 구간 0 · 시즌 회고 Temper 절 0 · Reed 저장·배선 0 · 곡시계 0 · broadcast_segment 생산자 0 · 앱 재생기 0.

**안 재봤다.** 위 전부 코드 기준이다. 라이브(Apps Script 배포판 · Supabase 운영 함수)에 무엇이 실렸는지는 roster-ingest 리허설 v43 · 운영 v20 말고는 안 재봤다. 실행 횟수와 실제 발송 건수도 안 재봤다. 이 초안은 09-11 조사 결과이며 v3 에 붙이기 전에 붙이는 사람이 커밋 해시를 한 번 더 연다.
