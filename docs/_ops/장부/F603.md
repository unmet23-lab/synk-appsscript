| F603 | 2026-08-18 | 마찰 | 차단문이 「아는 것은  뿐이다」로 나간다 — 아는 낱말이 0인 도구에서 문장 한가운데가 빈다 (SYNK-talk `lib/플래그.js`)  📏 실측 2026-08-18(#Q113 갈래③): 위치 인자만 받는 `tools/앱밟기.js` 에 인자 게이트를 달자    차단문이 이렇게 나갔다 — 「🔴 밟지 못했다 — 모르는 플래그 --전부 — 앱밟기 가 아는 것은  뿐이다.」    `아는것` 이 빈 배열이라 `join(' ')` 이 빈 문자열을 냈고, 그 자리가 문장 한가운데다.  🔑 왜 이제야 보였나: 그전엔 **선언이 빈 도구가 하나도 없었다.** 게이트를 진 15개가 전부 낱말을    하나 이상 가졌으니, 이 갈래는 코드가 아니라 «모집단» 때문에 원리상 안 보였다 — 회귀도    픽스처도 낱말이 있는 판만 먹였다. 부착이 모집단을 넓히는 순간 드러난 자리다.  ⚠ 이게 왜 가벼운 문구 문제가 아닌가: 낱말이 통째로 빈 문장은 「도구가 고장 났다」로 읽힌다.    그러면 사람은 **처방을 따르는 대신 우회를 찾는다** — 가드 맹점 ③(자기 처방)이 말하는 그 압력이고,    따를 수 없는 처방이 우회를 정상 통로로 만든 F103 과 같은 모양이다.  ✅ 해소: `인자게이트()` 가 빈 목록을 갈라 말한다 — 「<도구> 는 «--» 낱말을 하나도 안 받는다    (위치 인자만 받는다)」. 회귀 1건 신설(`tests/플래그게이트.test.js` ①) — 빈 목록이 문장에 구멍을    내면 그 자리에서 빨개진다. 실측 재확인: 앱밟기 차단문이 위 문장으로 나가고, `죽기()` 의 처방    줄(흐름 파일 이름 하나)이 그 아래 붙는다. | SYNK-talk lib/플래그.js 인자게이트() 가 빈 목록을 갈라 말한다 + 회귀 tests/플래그게이트.test.js ① 신설 |
