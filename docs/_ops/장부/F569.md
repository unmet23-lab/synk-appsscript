| F569 | 2026-08-17 | 마찰 | `대기열.js --집기` 의 거절 처방이 **낡아서 따를 수 없다** — 죽은 세션의 미완 줄을 승계할 때 (실측 2026-08-17 · #Q104 를 집다가).  무엇이 일어났나(세 통로를 그대로 돌린 실측): ① `node tools/대기열.js --집기 "#Q104" --파일 "…"` → `✗ 이미 남이 잡았다(근거 #Q104) … 죽은 세션이면 그 줄부터 치운다: node tools/board-move.js "<그 트랙 문구>"` (tools/대기열.js:896) ② 시킨 대로 `node tools/board-move.js "#Q104 시트층 도달 마지막 빚" --dry` → **원칙 ⑦ 로 거절**. 「주인이 죽은 것과 일이 끝난 것은 다르다 … 아카이브에 넣으면 다음 세션이 원리상 못 줍는다(F240)」 + 근거 후보 0건. 그 거절문이 내미는 답은 **`① 남은 일이 있으면 줍는다 — 내 보드 파일에 새 줄로 선언하고 이어서 한다(남의 줄은 고치지 않는다)`**. ③ 그래서 내 보드 파일에 새 줄을 Write → `board-guard` 가 막았지만 **따를 수 있는 처방을 내밀었다**: 「죽은 세션의 트랙을 그대로 이어받는 것이면 그 줄의 트랙 칸을 **바이트 그대로** 옮겨 적는다 — 트랙 칸이 같으면 ④는 승계로 보고 안 막는다」. 그대로 하니 통과했다(승계 선언 착지).  급소: **실제 문(board-guard)에는 승계 갈래가 이미 서 있는데, 앞단의 `--집기` 만 그걸 모른다.** 그래서 `--집기` 는 죽은 세션의 미완 줄에 대해 **원리상 조립을 못 내준다** — 이 도구의 존재 이유(보드 줄을 조립해 준다 · F415)가 바로 그 자리에서 도달 불가다.  새는 방향이 「통과」인 자리(F103 의 그 모양): `--집기` 의 처방을 **글자 그대로** 따르는 유일한 성공 경로가 `board-move … --미완확인 "…"` 로 **끝났다고 말해 버리는** 것이다. 그건 원칙 ⑦ 이 막으려던 번역(F397 — 미완이 완료의 얼굴로 굳는다) 그 자체다. 즉 처방을 어긴 쪽(내 파일에 새 줄)만 옳고, 따른 쪽은 장부를 오염시킨다.  닫는 방법(제안): `--집기` 의 점유 갈래에서 **주인의 생사 + 상태 칸의 「끝났다」 여부**를 board-guard 와 같은 판정으로 갈라, 죽은 주인 + 미완이면 거절 대신 **트랙 칸을 바이트 그대로 물린 승계 줄을 조립해 내민다**. 판정을 여기 다시 적으면 갈라지므로(board-move 원칙 ⑥ 과 같은 통로) `tools/lib/보드.js` 한 곳에서 파생해야 한다.  ⚠ 대가: 승계가 쉬워지면 **산 세션의 줄을 죽었다고 오판해 가로채는** 방향으로 샌다 — 그래서 생사 판정은 `작업본소유자` 한 곳에서만 와야 하고, 조립된 줄에는 「죽은 <지문> 승계」가 문구로 박혀야 한다(누가 언제 가로챘는지가 이력에 남는 유일한 재료). | 2026-08-18 as 198a11c6 — --집기 의 점유 갈래에서 죽은 주인의 «미완» 줄이면 거절 대신 승계 줄을 조립해 낸다. 행들 이 이미 활성행 으로 걸러져 있어 「미완」은 정의상 참이라, 새로 물은 것은 생사 하나뿐이고 그 판정은 작업본소유자(세션들·선언살았나·마감했나) 한 곳에서만 온다 — 여기 다시 안 적었다. 트랙 칸은 죽은 줄에서 바이트 그대로 물린다(board-guard ④ 가 승계로 보는 기준이 바이트다). 옛 처방 board-move 는 거절문에서 없앴다 — 원칙 ⑦ 이 미완 줄을 거절해 유일한 출구가 --미완확인 으로 「끝났다고 말하기」였다(F397·F103). 대가 셋을 문으로 걸었다: 모름은 승계 아님(마감 도장 요구 · F315) · 산 주인은 그대로 거절 · 조립된 줄에 「죽은 <지문> 승계」를 박아 오판이 이력에 남는다. 미머지 가지는 승계로 안 낸다(실물이 가지에 남는다 · F403). 막은 것=회귀 7 · 변이 5/5 포획 구멍0 미적용0(기준선 44 초록). 마지막 문은 그대로 board-guard — 이 도구는 프린터지 가드가 아니다. |
