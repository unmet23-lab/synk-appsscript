| F262 | 2026-08-08 | 마찰 | 정본 문서의 「⏳유호님 판정 대기」가 답이 온 뒤에도 안 닫혀, 새 세션이 그것을 열린 트랙으로 잡는다. 실측 2026-08-08: 아카이브·장부·인계문을 뺀 살아있는 정본에서 그 표식이 6자리 9항목인데 **전부 이미 답이 온 것**이었다 — 열린 것 0, 거짓 9/9. 자리: ①`docs/엔진도달_설계.md` §6(㉮/㉯ = 유호 08-08 「가 승인할게」로 확정) ②같은 문서 §3-2(「지금 없어서 못 만드는 소급불가 원신호 2건」 = 둘 다 c10 talk `87e7061` 로 닫힘) ③`docs/_ops/엔진도달_전수감사.md` §8(3건 — ㉮확정·n8n 지금 안 고름·마감시각 c10) ④`docs/앱기능_역할별_전수분석.md` §12-C(㉰㉱ = 유호 확정 + talk `67a282a` 구현 완료 · 그 커밋이 “정본 = …§12-C”라고 이 절을 «가리키면서» 이 절을 안 닫았다) ⑤같은 문서 456·457행 포인터 2개 ⑥`docs/이벤트_아이디어_v1.md` 제목(「유호님 판정 필요」인데 같은 절 결론이 「미결 0건」) ⑦`docs/디자인_컨셉_정본_v1.md` v1.8 행(Lime 2 ⏳ — 같은 문서 v1.9 가 답이다). 라이브 실증 = 이 세션이다: 인계문의 「새 트랙을 잡아라」를 따라 ①②를 순서대로 열어 트랙 후보로 잡았고, 메모리 정본을 열어서야 둘 다 죽은 질문인 것을 알았다(세션 전반부가 그 값이다). 뿌리는 통로다 — 대기의 **배달·종결 통로는 메모리 기반 결정 큐 하나**인데(F255 가 거기에 도장까지 달았다), 대기를 **문서에도** 적을 수 있고 문서 쪽에는 닫는 자가 없다. CLAUDE.md 가 「메모리·문서에 기록」이라 문서만 적어도 규칙은 충족되는데, 그렇게 적힌 것은 유호님께 배달되지도 않고 답이 와도 안 닫힌다. 같은 병의 4번째 층이다(F115 결정 큐 · F256 보드 · F260 정본 문서의 「발전시킬 것」 절 · 이번 = 정본 문서의 「판정 대기」 절). F260 은 자기가 밟은 한 자리(§6-1)만 고쳤고 나머지 6자리는 그대로였다 — 호출부마다 고치는 형태. | 6자리 전량 정정(`docs/엔진도달_설계.md` §3-2·§6 · `docs/_ops/엔진도달_전수감사.md` §8 · `docs/앱기능_역할별_전수분석.md` §12-C+포인터 2 · `docs/이벤트_아이디어_v1.md` 제목 · `docs/디자인_컨셉_정본_v1.md` v1.8 행). 표기를 통일했다 — 「열린 트랙으로 읽지 마라」 + **답이 착지한 주소**(메모리 슬러그 또는 커밋 sha)를 같이 적는다. 오늘 든 비용이 정확히 그 주소가 없어서였다(닫혔는지 알려면 메모리 전량을 뒤져야 했다). 원문은 지우지 않고 접어서 보존했다(설계 §6 `<details>`) — 당시 판단 근거는 이력이라 지우면 왜 ㉮인지가 사라진다. ⛔ **기계 통로는 안 세웠다(의도적)**: 「⏳+유호」 스캐너를 재 봤더니 `docs/_ops/인계문/`(세션마다 그 문구를 쓴다)·`docs/_ops/마찰신호.md`(F072·F114·F115·F118·F173·F194·F222 가 그 낱말로 적혀 있다)가 통째로 걸려 제외 목록이 폭발한다 — 규칙만 늘고 라우팅이 못 따라가는 모양(CLAUDE.md 가드 맹점 ③)이라 그 자리에서 접었다. → 대신 /evolve 제안 1줄을 올린다: 「대기는 메모리에만 산다 — 정본 문서엔 결과와 링크만」으로 CLAUDE.md 를 좁히면 대기를 쓸 자리가 하나가 되고 큐가 그것을 닫는다(문구 스캐너 없이 원인이 없어진다). |
