| F461 | 2026-08-15 | 마찰 | 대기열 `#Qnn` 채번에만 락이 없어 두 세션이 같은 번호를 발급했고, 그 결과 **저장소 전체 CI 가 주인 없이 빨갰다** — 이 저장소에서 번호를 매기는 세 곳 중 둘은 이미 이 병을 앓고 락으로 닫았다.  실측 2026-08-15(세션 `ddb6d85b` · `node tools/test-ci.js`): `tests/대기열점유.test.js` 가 `#Q84 (줄66 ↔ 줄87)` 로 fail. 도입 커밋 둘이 **7분 차이**다 — `2bdc3bb4`(02:24:29 · `066afeb6` · 마스코트 2단계 잔여) 와 `70e2f85f`(02:31:13 · `fdaec5ee` · 배포대조 본체 미측정). 둘 다 규약대로 「파일 전체 최대+1」을 읽었고, 둘 다 그때 최대가 83 이었다. 규약을 어긴 세션이 없는데 결과가 깨졌다 — 그게 이 신고의 전부다.  ■ 왜 마찰인가 — **같은 병을 이미 두 번 앓았고 두 번 다 락으로 닫았다.** 장부의 해소 수단 목록에 그대로 있다: 「tools/bump-version.js 채번 락」·「tools/friction.js 채번 락(friction-F0NN 태그 예약 · 0caeafe)」. 버전과 F-번호는 도구가 발급하니 락을 걸 자리가 있었는데, `#Qnn` 은 **사람(세션)이 Edit 로 손수 적는 번호**라 발급 통로 자체가 없다. 그래서 세 번째 재발이 「원인을 쓸 수 없게 만든다」(CLAUDE.md 신뢰성)의 대상인데, 여기서는 원인이 도구가 아니라 **통로의 부재**다.  ■ 사정거리: 탐지는 이미 있다(`대기열점유.test.js` 가 CI 에서 잡았다 — 그게 이 신고의 출처다). 없는 것은 **예방**이고, 그 사이의 비용이 「주인 없는 적색」이다: 겹친 두 세션 중 누구도 그 적색을 자기 것으로 안 읽고, 그동안 남의 배포 게이트가 막힌다. 이번엔 실측으로 CI 가 그것 하나 때문에 빨갰다.  ■ 지금 한 것: 규약(「늦게 도입된 쪽이 재번호 · 지운 번호는 재사용하지 않는다」)대로 `70e2f85f` 쪽을 `#Q86` 으로 옮겨 적색을 닫았다(`105b52a1` · 18/18). 어느 쪽도 보드에 그 번호를 트랙으로 안 들고 있어 조인 키는 안 깨졌다(실측: 보드 전체 `#Q84` 0건). **원인은 안 닫혔다.**  ■ 처방 후보(고르는 것은 이 신고 밖 — 되돌림 비용이 「모든 세션이 새 줄을 적는 방식」에 닿는다):   ㄱ) 발급을 도구로 옮긴다 — `tools/대기열.js --신설` 이 번호를 잡아 주고(bump-version 과 같은 원자 채번), 손 Edit 으로 `#Qnn` 을 새로 적는 것은 회귀가 막는다. ⚠ 대가: 새 줄 하나 세우는 데 도구 호출이 끼고, 지금 그 자리는 Edit 하나로 끝난다(「규칙보다 속도」에 정면으로 걸린다).   ㄴ) 발급은 그대로 두고 **커밋 순간에만** 막는다 — `tools/githooks/pre-commit` 이 스테이징된 작업대기열의 `#Q` 중복을 거부한다. 탐지 로직은 `대기열점유.test.js` 에 이미 있어 새로 지을 것이 없다. ⚠ 지금 그 훅 파일은 남의 미커밋이라(F454 트랙) 이번 세션이 못 건드렸다.   ㄷ) 그대로 둔다 — CI 가 잡으니 「하루 안에 누군가 고친다」. ⚠ 이번이 그 모양이고, 고친 사람이 겹친 두 세션이 아니었다. | 처방 ㄴ 착지 — `tools/대기열채번검사.js`(신설) 를 pre-commit 에 등재해 **겹친 `#Qnn` 이 커밋되는 순간** 거부한다. 발급은 손 Edit 그대로 두고(처방 ㄱ 은 「새 줄 하나에 도구 호출을 끼운다」라 유호 상시 「규칙보다 속도」와 부딪친다 — 안 골랐다), **막는 자리만 CI 에서 커밋으로 앞당겼다.** 그 사이가 이 신고가 잰 비용이었다: 겹친 두 세션 중 누구도 그 적색을 자기 것으로 안 읽고, 그동안 남의 배포 게이트가 막힌다.  ■ 판정은 한 곳이다 — `tools/대기열.js` 의 `채번()` 을 신설하고, **게이트와 사후 회귀가 같은 함수를 부른다.** 원래 판정은 `tests/대기열점유.test.js` 안의 인라인 사본 하나뿐이었는데, 게이트가 그 열몇 줄을 베꼈으면 둘이 갈리고 갈라지는 방향은 언제나 「통과」다(CLAUDE.md 맹점 ④). 그 사본은 지웠다 — 판정 자리 2 → 1.  ■ 착수 중 잡아 고친 것 1개(설계 오류): 처음엔 `--채번검사` 를 `대기열.js` 자신에 달았는데, 그 도구는 ROOT 를 `CLAUDE_PROJECT_DIR` 로 잡는다(픽스처 뿌리를 물리는 쓰임이 있다). 훅이 부르는 검사기가 그 변수를 물면 **워크트리에서 옆 저장소의 스테이징을 재고 조용히 통과한다** — `tests/게이트뿌리.test.js` 가 정확히 그 형태를 막고 있었고, 그대로 갔으면 그 회귀가 빨개졌다. 그래서 저장소 관용구대로 검사기를 별도 파일로 떼고 뿌리를 `path.resolve(__dirname, '..')` 로 잡았다. 그 갈래는 회귀 「급소 ②」가 픽스처로 못박는다.  ■ 실측: 회귀 `tests/대기열채번.test.js` 17건(판정층 7 + **진짜 `git commit` 배선 8** + 실저장소 거짓양성 2) · **변이 10/11 탐지**(대조군 별도 · 안 잡힌 1건 = 접두 경계 `(?!\d)` 제거 — `\d+` 가 greedy 라 채번 추출값이 안 바뀌는 자리다. 그 시험은 파서가 `/#Q(\d)/` 류로 좁아지는 미래 변이를 막는 값이라 남긴다) · `node tools/test-ci.js` 3373건 fail 0(pass 3356 · skip 12 · todo 5).  ■ 대가(새 장치의 값) — ① **틀릴 때의 모습 = 거짓 차단**: 내가 안 만든 중복이 이미 커밋돼 있으면, 대기열을 건드리는 다음 사람이 막힌다. 규약상 처방(늦게 도입된 쪽 재번호)은 맞지만 내 트랙과 무관한 손질이 커밋 앞에 끼어드는 모습이고, 그게 이 게이트가 낼 수 있는 가장 흔한 오답이다. 회귀 「급소 ③」이 **차단문을 읽어 그대로 따라 고치면 실제로 통과하는지**를 잰다 — 못 따를 처방은 우회를 정상 통로로 만든다(F103). ② **조용히 새는 자리 = `--no-verify`·훅 미설치 기계·병합 커밋** — 그래서 사후 회귀를 안 지웠다. 사전·사후 둘 다 필요하고, 대신 판정만 하나로 묶었다. ③ **닫은 것 1개** = 위 인라인 판정 사본(`tests/대기열점유.test.js`). ④ **안 닫은 것** = 「최대+1 을 읽는 순간과 커밋 사이」의 창 자체 — 여전히 열려 있다. 그 창을 닫으려면 발급을 도구로 옮겨야 하고(처방 ㄱ), 그건 속도 축의 판정이라 이 신고가 결정할 자리가 아니다. 대기열 머리말에 그 사실을 못박아 다음 사람이 「닫혔다」로 오해하지 않게 했다.  착지 = 세션 `local_452a975f` 2026-08-15. |
