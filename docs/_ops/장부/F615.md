| F615 | 2026-08-18 | 마찰 | 두 세션이 같은 마찰 번호를 **선언 10초 차**로 각자 잡았고, 알림은 **35분 뒤 커밋 시점에야** 왔다 — 그 사이 둘 다 같은 것을 지었다(F070 의 여덟째 · F585 와 층이 다르다).  실측 2026-08-18 (`local_9b74b64b` · 우연이 아니라 그 판이 그 상태였다): · 보드 선언 `007d99e` 06:55:36 `local_82d71975` — 「F605 … 처방 ㉠ 분모부터」 · 보드 선언 `98b78bd0` 06:55:46 `local_9b74b64b` — 「F605 append-only jsonl 보호 분모 측정」   → **10초 차**다. 둘 다 착수 전에 점유를 봤고 둘 다 「비었다」를 봤다. · 내가 알게 된 시각 = `track-collision` 이 07:31 경 PostToolUse 로 낸 것. 그때 그쪽은 이미   `7ea7ddcc`(07:06 · 막는 층+재는 층)까지 착지시키고 F605 를 해소·보드 ✅종결까지 끝냈다. · 그 사이 내가 지은 것 = 분모 실측(12벌 327행) + 전 스위트 변이 런 2회(약 14분) + 회귀 10건 +   변이 6/6. 그중 회귀 10건은 **버렸다**(그쪽 8건과 같은 자리라 중복 구현이다).  ■ 왜 신호인가 — 새는 방향이 「통과」이고, 그 통과가 **양쪽 다**에서 났다   이 저장소의 점유 장치 셋이 전부 이 판에서 옳게 돌았는데도 못 막았다:    · `board-guard`(Write 시점) — 내가 쓸 때 남의 줄이 **아직 없었다**. 옳게 통과시켰다.    · `보드.js`(다른 워크트리·origin/master 까지 읽는다 · F542) — 읽은 시점에 그 파일이 없었다.    · `track-collision`(PostToolUse) — **커밋에 반응한다.** 즉 지은 뒤에 운다.   세 층 다 「지금 잡혀 있나」를 재는데, 경합은 **「지금」이 서로 다른 두 순간**일 때 난다.  ■ F585 와 무엇이 다른가 (같은 트랙에 얹지 말 것)   F585 = 「가드가 **옳게 막았지만** 조사 한 벌을 치른 뒤에 막았다」 — 낭비는 조사분 1회.   이번  = 「가드가 **안 막았다**. 둘 다 통과했고 둘 다 끝까지 지었다」 — 낭비는 트랙 1개 전체.   F585 의 처방 ①(선언 전에 묻는 조회를 가드와 같은 함수로)은 이 판을 **못 막는다**: 그 조회도   「지금」을 잴 뿐이라 10초 뒤의 선언을 모른다. F585 가 스스로 적은 ⚠(「물어보고 나서 남이 먼저   선언하는 창은 없앨 수 없다」)가 여기서 **실제 비용으로 실현된 첫 실측**이다.  ■ 값이 0은 아니었다 — 그게 이 신고의 급소다   내 판이 그쪽 판에서 구멍 둘을 찾았다(`59f45507`): ㉠대상 목록이 `ls-files` 뿐이라 **장부 파일이   통째로 사라지면 조용히 통과**(그 축이 막으려던 F195 의 최대판이 그 모양이다) ㉡F605 자신의   실패 모양(문장↔과녁 갈라짐)이 안 봉인돼 있었다. 즉 병렬이 **이득을 냈다.**   그래서 「병렬을 없애자」가 자동으로 옳은 처방이 아니다 — 이 판에서 병렬은 교차검증이었다.   다만 그 이득은 **설계가 아니라 운**이었다(내가 뒤늦게 알아채 델타로 전환했기 때문이다).  ■ 처방 후보 — 안 지었다(이 신고는 재료다)   ㉠ 선언을 **origin 에 먼저 밀게** 한다(선언 커밋만 즉시 push). 그러면 「지금」이 한 층으로      모여 10초 창이 push 왕복만큼으로 줄고, 남의 워크트리 선언도 fetch 로 보인다.      ⚠대가 = F581(GCM 매달림)이 살아 있는 노트북에서 선언마다 push 왕복이 붙는다. 그리고        창이 0이 되지는 않는다(줄어들 뿐이다).   ㉡ `track-collision` 을 **커밋이 아니라 선언 시점에도** 돌린다 — 지금은 PostToolUse(Bash)라      커밋에만 반응한다. 선언 Write 직후 origin 을 한 번 보면 10초 창의 반대쪽이 잡힌다.      ⚠대가 = SessionStart 말고 매 선언마다 원격 조회가 붙는다.   ㉢ 충돌을 **못 막는 것으로 받아들이고, 늦게 알았을 때의 «전환 통로»를 정본에 적는다** —      이번에 내가 즉흥으로 한 것(남의 판을 머지 → 내 구현 버림 → 델타만 얹음 → 커밋문에 조율 1줄)      이 실제로 통했다. 지금 그 절차는 어디에도 안 적혀 있어 매번 즉흥이다.      ⚠대가 = 산문이라 안 지켜질 수 있다. 다만 ㉠㉡ 이 창을 0으로 못 만드는 한 이 통로는 필요하다.  ⚠ 안 잰 것 = 이런 경합이 **얼마나 자주** 나는지. 오늘 이 한 건은 실측이지만 빈도는 안 셌다.   세려면 「같은 F번호·같은 파일을 선언한 보드 줄이 서로 다른 지문으로 몇 쌍인가」를 이력에서   세면 된다(안 했다). 빈도를 모르면 ㉠㉡ 의 대가가 값어치를 하는지 못 정한다. | |
