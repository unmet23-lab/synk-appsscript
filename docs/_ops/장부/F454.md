| F454 | 2026-08-15 | 실수 | 코드의 «제어문자 구분자»를 산문으로 인용할 때마다 원시 바이트가 그대로 박힌다 — 한 세션에서 4번, 마지막 한 번은 **이 신고문 자신** 안에서 재발했다  실측 2026-08-15(세션 `7aa81186` · #Q78 잔여 배포 트랙). `원인지문_` 이 쓰는 구분자를 문서·커밋문에 설명하려고 「구분자 `\u0001` 로 둔다」를 쓰려 했는데, 세 번 다 **원시 0x01 바이트**가 파일에 들어갔다:   ① `docs/버전_이력.md` (v9.237 항목) ② 커밋 메시지 파일 ③ 판단 층 보안 검토문 전부 `cat -v` 로 `^A` 를 보고 잡아 `\u0001` 표기로 되돌렸다(치환 후 0건 실측). **라이브로 나간 바이트에는 0건** — 배포 파일은 애초에 `'\u0001'` 이스케이프였고 손댄 적이 없다.  ■ 왜 마찰인가 — 「2번째 = 시스템 결함」 조항의 실사고다   CLAUDE.md 신뢰성 절이 「같은 절차에서 2번째 = 실수가 아니라 시스템 결함 · 3번째 = 원인을 쓸 수   없게 만든다」고 못박는다. 이건 3번째까지 갔다. 그리고 셋 다 **같은 동작**이다 — 「제어문자를 쓰는   코드를 사람 말로 설명하기」. 이 동작은 이 저장소에서 드물지 않다: 구분자·센티넬·제어문자를 쓰는   자리마다 그 «왜»를 주석·이력·커밋문에 적는 것이 여기 규약이기 때문이다(`원인지문_` 주석 자신이   그 예다). 즉 **재발 조건이 상시**다.  ■ 하필 위험한 이유 — 선행 세션이 정확히 이걸로 빨갰다   인계문이 「직전 적색=내 원시 0x01, 고쳤고 미재검」이라 적어 놨다. 즉 `1ae6700f` 도 같은 자리에서   같은 병을 앓았다. 나는 그 사실을 **읽고 시작했는데도** 세 번 재발시켰다 — 지식으로는 안 막힌다는   증거다(그래서 프로즈가 아니라 기계로 옮겨야 하는 자리).  ■ 새는 방향 — 조용하다   원시 0x01 은 화면에서 **폭이 0**이라 눈으로 안 보이고, 에디터·diff·터미널이 대개 안 그린다.   `grep -P '\x01'` 이나 `cat -v` 로 «찾으러 가야» 보인다. 즉 안 찾으면 영영 안 보이고, 그 파일이   장부·이력이면 나중에 아무도 그 바이트의 출처를 모른다(F091 이 같은 축을 이미 적었다 —   「안 보이는 제어문자를 소스에 적으면 어느 편집이 지워도 티가 안 난다」).  ■ 이미 있는 것 / 없는 것 (실측)   · 있다: 옆 세션 `468ff5d7` 이 같은 날 **F450** 으로 `tools/자리표검사.js` + pre-commit 게이트를     세웠다. 다만 그건 **`[vNEXT]` 자리표**(확정 안 된 버전 표기)를 잡는 물건이고, 원시 제어문자는     과녁이 아니다 — 인접하지만 다른 축이다. ⚠그 도구의 회귀에 「④ 탐지력 — 날문자는 잡고,     이스케이프 표기는 안 잡는다(양방향)」가 있어 **이름은 겹쳐 보이나 재는 것이 다르다.**   · 없다: 「추적 파일에 원시 C0 제어문자(탭·개행 제외)가 들어오면 막는다」는 검사가 없다.     실측으로 지금 저장소 `*.js` 전량에 원시 0x01 은 0건이니, 픽스처 없이 실저장소만 보면     **거짓양성 0 = 상시 초록**이라 탐지력을 못 잰다(맹점 ②).  ■ 처방 후보(이번 세션에서 안 굳혔다 — 배포 트랙 중이었고, 훅·게이트는 되돌림 비용이 트랙 밖이다)   ㄱ) pre-commit 에 「원시 C0(0x00~0x08·0x0B·0x0C·0x0E~0x1F) 금지」를 얹는다. 짝 회귀 = 픽스처       2벌(원시가 든 파일은 빨갛고, `\u0001` 표기만 든 파일은 초록 — 양방향으로 재야 이빨이 산다).       ⚠ 예외 목록이 필요하다: 바이너리·`.png`·`.mp4`·`.pdf` 는 애초에 대상 밖이어야 한다.   ㄴ) 더 싼 판 — F450 이 이미 세운 `자리표검사.js` 에 검사 하나로 얹는다(새 도구를 안 만든다 ·       경량 원칙). 그쪽이 이미 pre-commit 에 배선돼 있어 등록층 구멍(F044 계열)을 안 새로 판다.   ㄷ) 안 막고 둔다 — 지금 실피해가 0이라는 근거로. ⚠권하지 않는다: 피해가 0인 이유는 내가       매번 «찾으러 갔기» 때문이고, 그건 사람 손이라 철학 ㉡ 에 어긋난다.   🔑 어느 쪽이든 **탐지력은 픽스처가 진다** — 실저장소는 지금 0건이라 거짓양성만 검사할 수 있다. | `tools/제어문자검사.js` 신설 + 발동 조건 둘(`pre-commit --cached` · **`commit-msg --메시지`** 신설) + 회귀 `tests/제어문자.test.js` 13건 · 변이 8/8 · test-ci 3347/0. 판정과 수집을 그 파일 하나가 지고(소비자가 둘뿐이라 lib 를 따로 안 판다) 훅은 발동 조건만 맡는다.  ■ **장부 권고 ㄴ 를 그대로 안 따랐다 — 그 처방은 사고 자리에서 영원히 초록인 가드를 만든다.** ㄴ 은 「`tools/자리표검사.js` 에 얹어라(이미 pre-commit 에 배선돼 있어 등록층 구멍을 안 판다)」인데, 그 도구의 범위는 `bump.배포파일들()` = Apps Script **16개**이고 자기 머리말이 「넓히면 안 된다」고 못박는다. 실측하면 **사고 3건이 전부 그 16개 밖**이다(`docs/버전_이력.md` · 커밋 메시지 · 판단 층 검토문). 얹었으면 통과 100%인 가드가 「닫았다」의 얼굴로 남는다 — CLAUDE.md 맹점 ④(맞는 얼굴로 틀린 값을 내는 자리). 권고를 쓴 세션이 배포집합의 크기를 안 열어 본 것이라, **이 행의 처방 칸도 「직접 열어 본 뒤 쓴다」에서 예외가 아니었다.**  ■ **옛글자 면제도 안 물려받는다.** `tools/lib/옛글자.js` 는 `docs/_ops/장부/`·`인계문/`·`evals/출력_*.json` 을 뺀다 — 사유는 「옛 사고를 인용하려면 그 글자를 적어야 한다」다. 그 사유는 제어문자로 **안 넘어온다**: 제어문자를 신고하는 글은 언제나 이스케이프 표기로 쓰지 원시 바이트를 적지 않는다. 그리고 F454 의 **세 번째 재발이 바로 그 면제 구역**(신고문 자신)이었다 — 물려받았으면 그 자리가 영영 안 잡힌다. 회귀가 이걸 못박는다(변이 「면제를 물려받는다」 → fail).  ■ 실측 2026-08-15(0 을 분모와 함께 · F207): 추적 **1323 = 텍스트 1137(옛글자 범위 752 + 면제 385) + 대상 밖 186**, 원시 C0 **0건**. 그래서 실저장소는 거짓양성밖에 못 재고 **탐지력은 픽스처가 진다**(맹점 ②) — 금지 바이트 30종 전수 · 허용 3종(탭·개행·복귀) · 처방을 따른 `\u0001` 표기는 초록(양방향).  ■ 🔑 **짓는 도중 F454 가 «두 번 더» 재현됐고, 새 판정이 둘 다 잡았다(첫 실물 2건 — 4번째·5번째 재발).** ㉠검사기 «자기 소스» 21줄 53칸(면제 없음을 설명하는 바로 그 문장) ㉡**이 해소문 자신** 7줄 358칸(「처방을 따른 표기는 초록」이라고 쓰는 문장). 둘 다 백틱 사이에 이스케이프 표기를 넣으려던 자리다. 나는 F454 전문을 읽고 그 병을 고치러 시작했는데도 두 번 더 냈다 — 「지식으로는 안 막힌다」의 결정적 증거이고, 이 결함의 재발 조건이 **상시**라는 신고문의 판정이 맞았다는 실측이다. 둘 다 눈으로 못 찾았고 `걸린줄()` 이 줄:칸을 짚어 고쳤다 — 이 가드가 없었으면 5번째는 이 장부 행에 박힌 채 커밋됐다(그리고 옛글자 면제 때문에 이 파일은 어느 기존 게이트에도 안 걸린다).  ■ 남은 구멍(닫지 않았다 · 다음 세션이 알아야 할 것): ①**형제 저장소 SYNK-talk 미배선** — 이 훅은 as 에만 설치했다. talk 의 `pre-commit` 은 이름만 같고 하는 일이 달라 `install-githooks.js` 가 `형제공유` 에서 뺀 목록이고(F300 이 통째 복사로 talk 게이트를 삼킨 자리), 그쪽 배선은 talk 소유 세션 몫이다. ②**설치본 드리프트** — 훅은 `.git/hooks` 에 살아 다른 세션은 `node tools/install-githooks.js` 를 돌려야 발화한다(드리프트는 `tests/커밋트레일러.test.js` 가 skip 으로만 드러낸다 — CI 에 훅이 없어서다). |
