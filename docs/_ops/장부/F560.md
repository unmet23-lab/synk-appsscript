| F560 | 2026-08-17 | 마찰 | citation-guard 가 저장소 «이름»을 문자열로 잘라 상대경로를 만들어, 워크트리에서 면제가 통째로 죽었다 — 코드 트랙에 워크트리가 «의무»가 된 뒤로는 보드·마찰신호·인계문을 고칠 때마다 맞는 행동에 거짓 경고가 떴다.  훅은 바로 두 줄 위에서 `ROOT = path.resolve(__dirname, '..', '..')` 를 이미 구해 놓고, 정작 면제 판정용 상대경로는 `rel.slice(rel.indexOf('/SYNK-appsscript/') + …)` 로 만들었다. 워크트리는 `…/SYNK-appsscript/.claude/worktrees/<가지>/` 아래라 **첫 조각이 걸려** `.claude/worktrees/<가지>/docs/_ops/보드/…` 가 나오고, 면제 목록은 `docs/_ops/보드/` 접두로 재기 때문에 한 건도 안 맞는다.  실측 08-17 (훅 사본을 고정하고 «경로만» 바꿔 대조 — 첫 회차엔 훅 사본과 경로를 동시에 바꿔서 판정이 흐렸다):   A 메인훅 × 메인 보드파일   → 조용 (정상)   B 워크훅 × 워크 «같은» 보드파일 → 경고 (결함)   C 워크훅 × 워크 마찰신호   → 경고 (결함)   D 워크훅 × 워크 비면제 문서 → 경고 (정상 · 대조군)  급소는 「가끔 틀린다」가 아니라 **이 가드가 막으려던 병을 스스로 키운다**는 것이다. citation-guard 는 「안 재고 적은 `파일:줄`」을 막으려고 있는데, 워크트리마다 거짓 경고를 뿌리면 그 경고를 안 읽는 습관이 붙는다 — 따를 수 없는 처방이 우회를 정상 통로로 만드는 F103 축이다.  왜 안 잡혔나 = 훅 29벌 중 **행동 회귀가 이 하나만 0** 이었다. 등록층(`훅이식성`·`훅등록`)은 settings.json 에서 훅을 «열거»해 이식성·실패 방향·죽은 참조를 이미 덮고 있었지만, 그 열거는 「무엇에 발동하는가」를 안 본다. 이름이 회귀에 한 번도 안 나오는 훅이 정확히 여기 하나였고(나머지 28벌은 4~92회), 그 사각에서 결함이 살았다.  덤으로 드러난 것: 훅 전체가 try/catch 라 `tools/인용검사.js` 가 사라져도 **조용히 통과**한다 — 그 죽음을 알리는 층이 어디에도 없었다. 신설 회귀 ③이 그 자리를 문다(변이 ⑤로 실증). | citation-guard 가 상대경로를 ROOT 에서 내도록 고침(저장소 이름 하드코딩 제거 + 워크트리 접두 벗기기) + tests/인용검사훅.test.js 신설 6건(훅 29벌 중 유일하게 행동 회귀 0 이던 자리) · 변이 6/6 구멍 0 |
