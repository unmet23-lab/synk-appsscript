| F602 | 2026-08-18 | 마찰 | 주석 제거 «통로» 가드가 사본만 세고 **「쓰는가」는 안 센다** — 비사용자는 그 가드에 원리상 안 보인다. 같은 부류 3번째다.  ■ 자리 talk `tests/소스검사통로.test.js` 의 실저장소 검사는 **「주석 제거기 지역 사본이 0벌이다」** 하나뿐이다 (`계수기.재기(ROOT).사본` 이 비었나). 즉 지키는 것은 **«또 짓지 마라»**이고, **«쓰라»**는 아무도 안 지킨다. 소스를 읽어 낱말을 세면서 `tests/lib/소스검사.js` 를 아예 안 부르는 시험은 사본이 0이므로 **언제나 초록**이다.  ■ 왜 3번째인가 (CLAUDE.md 신뢰성: 3번째 = 원인을 쓸 수 없게 만든다) ① F401(2026-08-13) — 사본이 갈라져 인라인 `//`·JSX·문자열 속 `//` 를 저마다 다르게 봤다 → 공용 통로 신설. ② #Q112·F594(2026-08-18) — 플래그 게이트가 주석 속 `자격증명.읽기(` 를 세어 도구를 «승격»시켰다 → 그 파일이 `코드만` 으로. ③ F533(2026-08-18 · 이 세션) — 왕복골격 가드가 주석 속 낱말을 위반으로 세 설계 주석을 못 적게 했다 → 그 파일이 `코드만` 으로. 세 번 다 **그 파일 하나만** 고쳤다. 통로는 F401 이 이미 세웠는데, 통로를 «안 쓰는» 쪽을 막는 기계가 0이라 같은 병이 파일을 옮겨 다닌다.  ■ 실측 2026-08-18 (`local_41b1ad2f` · talk) · tests/ 시험 파일 **150** · 그중 소스를 읽는 것 **123** · 렉서를 지나는 것 **63** · **안 지나는 것 52** · ⚠ 52 는 과녁이 아니다 — 「소스를 읽는다」만으로는 위험이 안 갈린다. 좁히기를 **두 번 틀렸다**:   ㉠1차(금지형 = 낱말이 없어야 한다)로 좁혀 15 → 열어 본 1건(`배포빚.test.js`)이 **거짓양성**이었다(positive assert + 런타임 객체).   ㉡그리고 과녁 자체가 틀렸다: 금지형은 **빨개져서** 그날 눈에 띈다(F533 이 그랬다). 조용히 새는 것은 반대쪽 —     **존재형**(「이 낱말이 코드에 있어야 한다」)이 주석 덕에 초록이면 아무도 안 본다. F401 의 **첫** 사례가 정확히 그것이다     (`for update` 를 코드에서 지웠는데 바로 위 주석이 그 낱말을 설명해 검사가 영원히 초록). · 과녁을 존재형으로 바꿔 다시 재니 — «JS 소스 파일»을 읽는 시험 **45** 중 렉서를 안 지나며 존재형으로 단언하는 자리는   **`tests/배포빚.test.js` 의 네 줄뿐**(:148 :149 :154 :155 · 대상 `tools/원격배포.js`·`tools/배포대조.js`). · 그 네 줄을 원문판/코드만판으로 대조 → **살아있는 거짓 초록 0건 · 잠재 4건**(넷 다 코드에도 실재한다).   ⇒ **오늘 깨진 것은 없다.** 부류는 실재하고 모집단은 작다.  ■ 처방 후보 (안 지었다 — 이 신고는 재료다 · 모집단이 작아 지금 지으면 싸게 끝난다) ㉠ `소스검사통로.test.js` 에 **«비사용» 축**을 더한다 — 소스를 읽어 낱말을 세는 시험은 `tests/lib/소스검사.js` 를 지나야 한다.    탐지력은 픽스처가 지고(맹점②), 실저장소는 거짓양성만 묻는다. 못 재는 날은 fail 아닌 skip(F296 · 지금 그 파일이 이미 그렇게 한다).    ⚠ 대가가 이 갈래의 전부다: **분모 정의가 어렵다.** 내 좁히기가 두 번 틀렸고(52→15→4), 거짓양성이 세면 F221 로 관습이 죽는다.    ⇒ 그래서 분모를 「소스를 읽나」가 아니라 **「읽은 소스에 대해 낱말 존재를 단언하나」**로 잡아야 한다 — 위 실측이 그 정의로 4를 냈다. ㉡ 먼저 `배포빚.test.js` 네 줄만 `코드만` 으로 옮긴다(모집단 → 0). ⚠ 이것만 하면 **네 번째 일회성 수리**라, ㉠ 없이는 다섯 번째가 온다. ㉢ 아무것도 안 한다 — 살아있는 결함이 0이므로 급하지 않다. 다만 그때는 「왜 안 하나」를 통로 파일 머리말에 적어 다음 사람이 또 재지 않게 한다.  ⚠ 착수 조건: talk `tests/소스검사통로.test.js`·`tests/배포빚.test.js`·`tools/가드계수.js` 를 만진다.    `tools/가드계수.js` 는 2026-08-18 지금 #Q113 갈래① 트랙(`local_31ea1e3b`)이 만지고 있었다 — 그 커밋이 착지한 뒤에 연다. | talk `153f8b4` — 계수기에 «존재형 × 언어» 축을 열고 `tests/소스검사통로.test.js` 에 잔여 래칫을 붙였다. 판정 자리 local_3e2c6a12 2026-08-18. ■ 무엇이 막나 ㉠ `tools/가드계수.js` 가 방향을 둘로 나눠 센다(`존재위험/존재안전/존재모름` 신설) — 옛 판은 부정 단언만 셌다. 두 방향은 눈멀 때의 증상이 정반대다: 부정형은 주석의 낱말을 위반으로 세어 **그날 빨개지고**(F533·F594 가 그렇게 잡혔다), 존재형은 **영원히 초록**이다(F401 첫 사례 = `for update`). 조용한 쪽을 안 세고 있었던 것이 이 신고의 뿌리다. 곁들여 `assert.match`·`doesNotMatch` 갈래가 열렸다 — 첫 인자가 곧 수신자라 술어 관문에서 통째로 떨어지고 있었고, 그래서 **부정 축에도** 사각이 있었다(그 갈래로 부정 위험 6자리가 새로 보인다). ㉡ `tests/배포빚.test.js` 네 줄을 `코드만(파일소스(…))` 으로 옮겼다. ■ 🔴 착수 실측이 이 신고의 전제를 뒤집었다 — 모집단은 4 가 아니라 **139 자리 / 36 파일**이다(존재형 × JS · ㉡ 처분 뒤). 신고문의 4 는 **파일 단위** 계수였다(「JS 소스를 읽는 시험 파일 중 `코드만` 을 한 번도 안 부르는 것」). 그런데 한 파일이 어떤 변수는 정제해 읽고 **다른 변수는 원문으로** 읽는다 — 그 갈래가 분모에서 통째로 빠져 있었다. 그래서 「모집단이 작아 지금 지으면 싸게 끝난다」가 틀렸고, 처방도 바뀌었다: **0 을 요구하지 않고 래칫으로 선다**(새 파일이면 적색 · 그 파일에서 늘면 적색 · 0 이 되면 「명단에서 빼라」 적색). 0 을 요구하면 그날 아무도 못 통과하고, 통과 못 하는 가드는 꺼진다(F221·F103). ■ 구멍을 실물로 증명했다(추정이 아니다) — 변이 ⑦ 「코드를 지우고 그 낱말을 주석에 남긴다」를 `tools/원격배포.js:458` 에 대고 **조건 하나만 바꿔** 두 번 돌렸다(F045): ㉡ 되돌림 → **구멍**(34건 전부 초록 · 배선이 죽었는데 아무도 안 빨개졌다) · ㉡ 적용 → **포획**. 신고문의 「살아있는 거짓 초록 0」은 맞았지만 그건 우연히 낱말이 코드에도 있었기 때문이고, 구멍 자체는 살아 있었다. ■ 잰 것 — 변이 **7/7 · 구멍 0 · 미적용 0**(`tools/변이.js` 공용 통로) · 회귀 11(신설 2) · talk 전체 **2375 통과 / 1 skip / 0 실패**. ■ 안 닫은 것(억지로 안 닫는다 — 안전 가드는 숫자를 맞추려고 닫지 않는다) ①잔여 139자리 36파일 = 별건 트랙(래칫이 늘지 못하게 쥐고 있다) ②면제 1건만 사유를 적었다 — `tests/강사지식.test.js` 는 겨눈 낱말이 생성물의 **머리말 주석**이라(`contents/강사지식.js:3`) 정제하면 과녁이 사라진다 ③**비JS 46자리**(SQL·md)는 이 검사가 안 진다 — SQL 주석(`--`)에 낱말이 숨는 축은 그대로 살아 있고 공용 통로 자체가 없다(저장소에 SQL 제거기 지역 사본 **18벌**) ④**부정형 js 6자리**(라디오낭독 1 · 말하기로직 5)도 안 닫았다 — 그쪽은 새면 «빨개지는» 방향이라 조용하지 않다. ■ as 저장소 실측(계수기를 다른 저장소에 대 보는 것 자체가 탐지다 — 이 도구 머리말 3차) — 훑은 파일 334 · **존재위험 js 404** · 사본 **13벌**. as 엔 `acorn`·`node_modules` 가 없어 그 저장소 CI 는 이 축을 원리상 못 돌린다(`tests/소스검사통로.test.js` 머리말이 이미 그렇게 적어 뒀다) — 래칫을 as 에 그대로 옮기면 «영원히 안 도는 장치»가 하나 느는 것이라 안 옮겼다. |
