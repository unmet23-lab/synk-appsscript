| F602 | 2026-08-18 | 마찰 | 주석 제거 «통로» 가드가 사본만 세고 **「쓰는가」는 안 센다** — 비사용자는 그 가드에 원리상 안 보인다. 같은 부류 3번째다.  ■ 자리 talk `tests/소스검사통로.test.js` 의 실저장소 검사는 **「주석 제거기 지역 사본이 0벌이다」** 하나뿐이다 (`계수기.재기(ROOT).사본` 이 비었나). 즉 지키는 것은 **«또 짓지 마라»**이고, **«쓰라»**는 아무도 안 지킨다. 소스를 읽어 낱말을 세면서 `tests/lib/소스검사.js` 를 아예 안 부르는 시험은 사본이 0이므로 **언제나 초록**이다.  ■ 왜 3번째인가 (CLAUDE.md 신뢰성: 3번째 = 원인을 쓸 수 없게 만든다) ① F401(2026-08-13) — 사본이 갈라져 인라인 `//`·JSX·문자열 속 `//` 를 저마다 다르게 봤다 → 공용 통로 신설. ② #Q112·F594(2026-08-18) — 플래그 게이트가 주석 속 `자격증명.읽기(` 를 세어 도구를 «승격»시켰다 → 그 파일이 `코드만` 으로. ③ F533(2026-08-18 · 이 세션) — 왕복골격 가드가 주석 속 낱말을 위반으로 세 설계 주석을 못 적게 했다 → 그 파일이 `코드만` 으로. 세 번 다 **그 파일 하나만** 고쳤다. 통로는 F401 이 이미 세웠는데, 통로를 «안 쓰는» 쪽을 막는 기계가 0이라 같은 병이 파일을 옮겨 다닌다.  ■ 실측 2026-08-18 (`local_41b1ad2f` · talk) · tests/ 시험 파일 **150** · 그중 소스를 읽는 것 **123** · 렉서를 지나는 것 **63** · **안 지나는 것 52** · ⚠ 52 는 과녁이 아니다 — 「소스를 읽는다」만으로는 위험이 안 갈린다. 좁히기를 **두 번 틀렸다**:   ㉠1차(금지형 = 낱말이 없어야 한다)로 좁혀 15 → 열어 본 1건(`배포빚.test.js`)이 **거짓양성**이었다(positive assert + 런타임 객체).   ㉡그리고 과녁 자체가 틀렸다: 금지형은 **빨개져서** 그날 눈에 띈다(F533 이 그랬다). 조용히 새는 것은 반대쪽 —     **존재형**(「이 낱말이 코드에 있어야 한다」)이 주석 덕에 초록이면 아무도 안 본다. F401 의 **첫** 사례가 정확히 그것이다     (`for update` 를 코드에서 지웠는데 바로 위 주석이 그 낱말을 설명해 검사가 영원히 초록). · 과녁을 존재형으로 바꿔 다시 재니 — «JS 소스 파일»을 읽는 시험 **45** 중 렉서를 안 지나며 존재형으로 단언하는 자리는   **`tests/배포빚.test.js` 의 네 줄뿐**(:148 :149 :154 :155 · 대상 `tools/원격배포.js`·`tools/배포대조.js`). · 그 네 줄을 원문판/코드만판으로 대조 → **살아있는 거짓 초록 0건 · 잠재 4건**(넷 다 코드에도 실재한다).   ⇒ **오늘 깨진 것은 없다.** 부류는 실재하고 모집단은 작다.  ■ 처방 후보 (안 지었다 — 이 신고는 재료다 · 모집단이 작아 지금 지으면 싸게 끝난다) ㉠ `소스검사통로.test.js` 에 **«비사용» 축**을 더한다 — 소스를 읽어 낱말을 세는 시험은 `tests/lib/소스검사.js` 를 지나야 한다.    탐지력은 픽스처가 지고(맹점②), 실저장소는 거짓양성만 묻는다. 못 재는 날은 fail 아닌 skip(F296 · 지금 그 파일이 이미 그렇게 한다).    ⚠ 대가가 이 갈래의 전부다: **분모 정의가 어렵다.** 내 좁히기가 두 번 틀렸고(52→15→4), 거짓양성이 세면 F221 로 관습이 죽는다.    ⇒ 그래서 분모를 「소스를 읽나」가 아니라 **「읽은 소스에 대해 낱말 존재를 단언하나」**로 잡아야 한다 — 위 실측이 그 정의로 4를 냈다. ㉡ 먼저 `배포빚.test.js` 네 줄만 `코드만` 으로 옮긴다(모집단 → 0). ⚠ 이것만 하면 **네 번째 일회성 수리**라, ㉠ 없이는 다섯 번째가 온다. ㉢ 아무것도 안 한다 — 살아있는 결함이 0이므로 급하지 않다. 다만 그때는 「왜 안 하나」를 통로 파일 머리말에 적어 다음 사람이 또 재지 않게 한다.  ⚠ 착수 조건: talk `tests/소스검사통로.test.js`·`tests/배포빚.test.js`·`tools/가드계수.js` 를 만진다.    `tools/가드계수.js` 는 2026-08-18 지금 #Q113 갈래① 트랙(`local_31ea1e3b`)이 만지고 있었다 — 그 커밋이 착지한 뒤에 연다. | ⏸ 이 세션에선 안 짓는다 — 판정 자리 local_41b1ad2f 2026-08-18. 이유 둘: ①착수 조건이 아직 안 섰다 — 처방 ㉠이 만질 talk tools/가드계수.js 를 지금 #Q113 갈래① 트랙(local_31ea1e3b)이 들고 있었고, 그 커밋 f4663cd 가 방금 착지했으니 다음 세션은 열 수 있다 ②살아있는 결함이 0건이라 급하지 않다(모집단 4자리 전부 잠재). 다만 «안 급하다»가 «안 한다»는 아니다 — 이건 같은 부류 3번째이고 CLAUDE.md 신뢰성 조항이 3번째에 요구하는 것은 또 한 번의 일회성 수리가 아니라 원인을 쓸 수 없게 만드는 것이다. 재료는 이 신고에 전부 실측으로 들어 있다(분모 정의까지: 「소스를 읽나」가 아니라 「읽은 소스에 대해 낱말 존재를 단언하나」 — 그 정의라야 52 가 아니라 4 가 나온다). 다음 주자는 처방 ㉠+㉡ 을 한 벌로 하면 싸게 끝난다. |
