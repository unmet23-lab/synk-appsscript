| F457 | 2026-08-15 | 실수 | 외부 계약을 우리 «출력»으로 기대값 삼은 회귀는, 그 출력이 계약 밖인 날 함께 초록이 된다 — 검사가 버그를 못박고 있었다  실측 08-15(#Q83 · talk `368603a`): AI 교정 배치 제출이 벤더에서 매번 400 이었고 대기 396건이 전량 멈춰 있었다. 원인은 `custom_id` 가 Batches API 규격(`^[a-zA-Z0-9_-]{1,64}$`) 밖이었던 것(구분자 `|`). 그런데 `tests/교정엔진.test.js` 는 `assert.equal(custom_id, `${제출id}/v1`)` 로 **우리가 만든 그 모양을 기대값으로** 적어 두어서, 실제 제출이 매번 튕기는 동안 스위트는 계속 초록이었다. 「검사가 있다」와 「계약을 잰다」가 갈린 자리다.  🔑 같은 함정이 두 형태다: ①우리 출력을 기대값으로 복사한다(이번 것) ②우리 상수를 그대로 빌려 검사한다(`assert.match(키, 배치키규격)`) — 둘 다 「우리가 우리를 지킨다」까지만 말하고, 정작 그 값이 외부 계약 밖인 날은 아무 말도 안 한다. 처방은 **외부 계약의 원문을 검사 파일에 따로 적는 것**(사본이 두 벌인 걸 알고 두는 자리 · 출처와 인용을 주석에 남긴다). 계약이 바뀌면 두 벌이 갈리지만, 갈리면 «빨개진다» — 지금은 갈려도 초록이었다.  🔑 그리고 이 결함이 며칠 늦게 잡힌 두 번째 이유: 벤더가 준 400 사유가 `console.error` 안에만 살고 응답 봉투에는 상태 코드만 실려서, 「400 이다」까지는 보였는데 «어디를 고칠지»가 로그를 따로 열어야만 나왔다. 실패 봉투는 상태 코드가 아니라 **처방으로 이어지는 문장**을 실어야 한다.  ⚠ CLAUDE.md 가드 맹점 ②(「버그가 아직 있을 것을 요구하는 회귀 금지」)와 축이 다르다 — 그건 «탐지력»을 실저장소에 기대는 것이고, 이건 «기대값 자체»를 검사 대상에서 복사한 것이다. ④(장치가 맞는 얼굴로 틀린 값을 낸다)의 한 갈래로 읽는 게 맞다. | ⏸ 이 «자리»는 닫혔다 — 교정 배치 custom_id 는 벤더 규격을 테스트에 따로 적어 물게 했고, 신고 원문(구분자 되돌림)을 변이로 다시 넣자 6건이 빨개졌다(talk 368603a). 안 닫는 이유는 F457 의 주장이 «부류»이기 때문이다: 외부 계약(벤더 규격·HTTP 계약·플랫폼 제약)을 우리 출력·우리 상수로 기대값 삼은 회귀가 저장소에 몇 개 더 있는지 안 셌다. 그 전수 대조는 #Q83 범위 밖이고 별도 일감이다 — 세는 법은 assert 의 기대값이 검사 대상에서 온 자리를 찾는 것. 판정 자리 = 세션 127e2164 · 08-15. |
