| F525 | 2026-08-17 | 마찰 | 드리프트 마스킹이 «접다 만» 자리가 있었다 — 형제 의존 블록 하나가 마스킹 밖에 붙어, 그 검사가 CI 에서만 빨갛고 로컬에선 영원히 초록이었다.  실측 2026-08-17 (`local_a49a26c1`): `tools/이해대장.js` 가 그리는 `<details class="사유">` 블록은 형제 저장소(SYNK-talk)의 사유 문장을 그대로 싣는데, `<div class="실측">` **밖**에 붙어 있었다. 드리프트 검사의 마스킹(`tests/이해대장.test.js` 의 `실측블록RE`)은 그 div 하나만 접으므로, 형제가 없는 환경에서는 그 줄이 통째로 빠진다.  추정이 아니라 쟀다 — 워크트리를 통째로 임시 경로에 떠서(조각 복사는 `tests/lib/소스검사.js` 에서 require 사슬이 끊겼다) 형제 없이 다시 그린 뒤, 시험과 **같은 정규식**으로 접어 대조했다: 갈린 줄 정확히 **1줄**. 즉 「실저장소 — 커밋된 화면이 지금 정본과 같다」가 Actions 러너에서 빨갛다(러너에 형제가 없다 — F364 가 이미 적어 둔 사실이다).  ■ 왜 안 보였나 — **로컬엔 형제가 늘 있다.** 그래서 이 결함은 원리상 로컬에서 안 나타난다. F364 는 같은 파일에서 「형제 있음/없음이 두 판정이 되는」 축을 이미 닫았는데, 그 처방(마스킹)은 **그 시점의 블록 하나**만 덮었고 나중에 추가된 형제 의존 블록은 덮이지 않았다. 장치가 안 도는 쪽이 아니라 **맞는 얼굴로 틀린 값을 내는 쪽**으로 샜다(CLAUDE.md 신뢰성 맹점 ④). 새는 방향은 여기서도 「통과」였다.  ■ 대가가 컸을 자리 — Actions 한도가 9/1 에 리셋되면 원격 CI 가 켜진다. 그 순간 이 적색은 고친 사람이 아니라 **그 뒤 커밋하는 모든 세션의 배포**를 막고(이해대장 게이트는 `/deploy` 2단계다), 처방(「다시 그려 커밋하라」)을 따르면 형제 있는 기계에서 반대 방향으로 빨개진다 — 따를 수 없는 처방(F103). 08-14 에 같은 모양으로 회수 커밋이 하루 넷이었다.  ■ 곁: 이 결함을 고치며 짠 내 회귀 자신이 처음엔 **커밋본**을 읽고 있어서, 생성기를 망가뜨려도 초록이었다(변이 둘이 구멍으로 잡았다). 다시 그려서 재도록 고쳤다 — 같은 맹점 ④의 두 번째 얼굴이 한 트랙 안에서 두 번 났다. | 형제 의존 블록 둘(사유·빨간칸)을 <div class="실측"> 안으로 옮겼고, 재발을 막는 회귀를 같은 커밋에 넣었다 — 「형제에서 온 사유 문장이 마스킹 밖에 있으면 빨개진다」(픽스처로 탐지력 · 실저장소는 거짓양성만). 고친 뒤 형제 없는 판을 다시 떠서 드리프트 0 을 확인했다. b6d9d2c4 |
