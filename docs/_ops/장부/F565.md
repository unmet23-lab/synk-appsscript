| F565 | 2026-08-17 | 마찰 | 가지 층의 «기준»이 낡은 로컬 master 라, 이미 머지된 남의 줄이 「아직 안 머지된 가지의 선언」으로 딸려 온다 — 소비자 셋이 함께 먹는 분모다.  실측 2026-08-17 (`local_a0a573fb` · F542 트랙에서 발각 · bare origin 을 세운 일회용 저장소로 재현): `EnterWorktree` 는 `origin/master` 에서 가지를 뗀다(하네스 `worktree.baseRef: fresh`). 그래서 열린 가지는 **로컬 master 가 아직 안 받은 커밋을 이미 들고 있다.** 그런데 `tools/lib/보드.js` 의 `가지끝()` 은 기준을 `가지기준 = ['master', 'origin/master']` 순으로 잡고 로컬 `master` 가 늘 먼저 성공하므로, `master...가지` 순diff 에 **그 가지가 더한 것이 아닌 줄**(= origin 이 더했고 내가 안 받은 남의 줄)이 섞여 온다.  재현 판 그대로: 열린 가지를 `origin/master` 에서 떼자 가지 층 1줄 중 1줄이 «이미 머지된» 줄이었다. 그때 인계문이 「🌿 아직 머지 안 된 가지 `열린가지` 안에 있다 … `gh pr list`」를 냈고, 그 PR 은 이미 머지돼 닫혔다 — **따라가면 빈손**이다(F103 무늬: 따를 수 없는 처방).  🔑 이건 F528(가지 층을 분모에 더한 수리)의 **대가 쪽**이지 그 수리의 결함이 아니다. 그 함수 머리말이 이미 적어 둔 대가 ㉠(「워크트리가 사라진 묵은 가지의 줄도 잡는다」)와 **다른 자리**다: 저건 죽은 가지, 이건 **산 가지가 남의 줄을 자기 선언으로 낸다**.  ■ 무게 — 소비자가 셋이라 증상이 셋으로 갈린다   · `대기열.js` 점유 판정(F521) — 이미 머지돼 «끝난» 트랙이 계속 자리를 물어 「지금 집을 수 있는 줄 0」 쪽으로 민다.   · `board-guard` ④ 겹침(F528) — 남이 이미 착지시킨 트랙을 「살아있는 선언」으로 읽어 새 세션을 비켜나게 한다.   · 인계문(F529) — 처방이 `gh pr list` 로 잘못 나간다. **이 갈래만** F542 트랙(`8cb3a96f`)이 닫았다 — 줄이 원격 층에 바이트 그대로 있으면 「받아라」로 고쳐 낸다(순증 · 회귀 `tests/인계문원격.test.js` · 변이 11/11).  ■ 처방 후보 — 안 지었다(이 신고는 재료다)   ㉠ `가지기준` 의 순서를 뒤집어 `origin/master` 를 먼저 쓴다. 기준이 신선해져 순diff 가 정확해지고, `git branch --no-merged origin/master` 가 「이미 원격에 머지된 가지」도 목록에서 뺀다 — 세 소비자가 한 번에 정확해진다. ⚠ 대가 = 원격이 없는 저장소·오프라인 클론에서 `origin/master` 가 없으면 지금처럼 `master` 로 내려가는데, 그 폴백이 **조용하다**. 신선/낡은 기준으로 잰 값이 같은 모양으로 나온다(새는 방향 = 「통과」).   ㉡ 기준을 로컬 master 로 두되 «로컬이 origin 보다 N 뒤처졌다」를 함께 내서 소비자가 그만큼 낮춰 읽게 한다. ⚠ 대가 = 판정이 아니라 주석이라, 세 소비자가 각자 해석한다(갈라진다).   ㉢ 아무것도 안 한다. ⚠ 대가 = 로컬이 뒤처진 트리에서 위 셋이 상시로 조금씩 틀린다. 이 저장소는 커밋 간격 중앙값 1분이라 «뒤처진 트리»가 기본 상태에 가깝다(이 세션도 열자마자 11 뒤였다).  ⚠ 손대는 순간 소비자 셋의 분모가 같이 움직이므로 F542 트랙에서는 **일부러 안 건드렸다** — 거기서 고친 것은 인계문이 내는 처방 하나뿐이다. | |
