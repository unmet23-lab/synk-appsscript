| F460 | 2026-08-15 | 마찰 | 가짜 시트 흉내가 라이브보다 «사나워» 거짓 적색을 낸다 — 같은 헬퍼에서 2번째다(계열 = 「가짜 시트가 라이브와 다르다」). 실측 2026-08-15(#Q81 변이 중): tests/발화퀄리티.test.js 의 `fakeLogSheet.setValues` 가 «부분 폭» 쓰기에서 `self.data[행] = row.map(...)` 로 행을 통째로 갈아치웠다. 실물 Sheets 는 지정 범위 안에만 쓴다. 그래서 한 열만 고쳐 쓰는 호출(`assignGroups` 의 시즌 열 정상화 = 폭 1)이 나머지 9열을 통째로 날렸고, 엔진은 멀쩡한데 시험만 빨개졌다. 1번째는 같은 파일 머리말에 적혀 있다 — 08-14 «Date 삼킴»을 안 흉내 내 「재실행이 행을 안 늘린다」가 초록인 채 라이브만 중복 적재였던 자리(그땐 흉내가 «착해서» 조용한 초록, 이번엔 «사나워서» 거짓 적색 — 방향만 반대고 원인은 하나다: 흉내의 계약이 어디에도 안 적혀 있다). 🔑 둘 다 «변이»가 잡았다 — 평소 초록으로는 원리상 안 보인다(맞는 얼굴로 틀린 값을 내는 자리 · 지침 v9.2 맹점 ④). | 수리 `0da8b380` — setValues 는 범위 안에만 쓰고, getLastRow 는 내용 있는 마지막 행까지, clearContent 는 행이 아니라 값만 비운다(+getNumberFormat 읽기 추가). ⚠ 회귀 장치는 «없다»(대가를 함께 적는다): 흉내의 계약을 기계로 물릴 자리가 지금 없고, 그걸 세우려면 「가짜 시트 ↔ 실물 Sheets 계약」을 픽스처로 못박는 별도 트랙이 필요하다 — 그건 이 줄의 몫이 아니라 대기열 몫이다. 지금 막는 것은 주석 한 벌(그 자리에 왜 이렇게 쓰는지)뿐이고, 다음 재발도 «변이를 돌리면» 잡힌다. |
