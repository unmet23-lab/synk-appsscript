| F311 | 2026-08-10 | 실수 | 바탕화면 경로 탐지가 세 파일에 3벌로 갈려 있었고, 회귀는 그중 한 벌만 물었다.  실측(2026-08-10 · 유호님 분리 반박 패스 M3 발견):   ① `tools/철학열람빌드.js` — 레지스트리 User Shell Folders → OneDrive → USERPROFILE 폴백   ② `tools/교재읽기본.js` — ①과 **글자 단위로 동일한 복사본**   ③ `tools/운영자료.js` — PowerShell 셸 확장(다른 방식) 회귀는 ③쪽만 물었다(`tests/운영자료.test.js` 「경로를 상수로 쓰지 말고 셸에 물어야 한다」). OneDrive 리디렉션이 바뀌는 날 ①②와 ③이 서로 다른 폴더를 가리켜도 **아무 데도 안 빨갛다.**  왜 아픈가: 2026-08-09 실사고의 재발 통로다 — `USERPROFILE\Desktop` 은 Test-Path 가 True 인 껍데기라 「맞게 썼다」로 읽히고, 그 자리에 폴더를 만들고 「바탕화면에 넣었습니다」로 보고했다 (유호님이 "안 보이는데?"로 잡음). 그 처방이 세 벌 중 한 벌에만 서 있었다. CLAUDE.md 신뢰성 「3번째 = 원인을 쓸 수 없게 만든다」에 정확히 해당한다 — 호출부마다 고치는 대신 잘못 쓸 수 없는 공용 통로를 만들고 옛 통로를 테스트로 금지해야 하는 자리.  곁들여 잡은 것: 단일화 중 `%없는변수%\Desktop` 이 빈 문자열 갈음으로 `\Desktop`(루트 상대경로)로 남는 구멍을 찾아 막았다. Windows 에선 그것도 path.isAbsolute 가 true 라 멀쩡한 절대경로의 얼굴을 하고, 그 폴더가 있는 기계에서는 엉뚱한 곳이 바탕화면이 된다(같은 사고의 재판). | tools/lib/바탕화면.js 공용 통로 + tests/바탕화면통로.test.js 옛 통로 금지 스캔(호출부 3곳 전부 통로 경유 · 런타임 링크 3/3 실측) |
