| F486 | 2026-08-15 | 실수 | 한 세션에서 「부정 판정을 층 하나만 보고」 두 번 냈다 — 유호님이 두 번 다 그 판정을 근거로 일감을 확정하셨다. ①「문항↔개념 매핑 0건」: 이 저장소 계약의 `skill_ids` 「미구현」 표기(c6 시점)만 보고 판단했는데 실물은 talk 에 100% 서 있었다(문항 100/100·스킬표 30·events 검증·L0 시드 30행). ②그 직후 「`skills.error_tags` 는 읽는 자리가 이미 있다」: 실은 소비자 0이다(`corrections.error_tags` 184곳과 이름만 같고 다른 물건인데 그 개수를 근거로 삼을 뻔했다). CLAUDE.md 에 이미 조항이 있다 — 「부정 판정은 값이 살 수 있는 층을 먼저 «열거»하고 그 전부를 연 뒤에만 쓴다」(F348·F403). 조항이 있는데도 2회 샜다는 것이 이 신호의 값이다: 그 조항은 「어디에도 없다」 꼴 문장에만 발화하는데, 실제로 샌 문장은 「0건이다」·「읽는 자리가 있다」처럼 긍정문 모양의 부정 판정이었다. 형제 저장소가 있는 이 프로젝트에서 「이 저장소에 없다 = 없다」는 상시 사각이다. 처방 후보(미확정) = 계약 파일의 「미구현」 목록이 낡음을 기계가 드러내게 하거나, 부정 판정 전 형제 저장소 grep 을 강제하는 가드. ⚠ 지금 짓지 않았다 — 유호 「최대한 압축」 지시대로 메모리 한 줄로 접었다(brain-learning-taxonomy 08-15 절). | ⏸ 판정 2026-08-15(세션 `22ef3c6d`) — **①의 원인은 기계로 닫았고 ②는 안 닫혔다. 그래서 해소가 아니라 보류다.**  ■ ① 「문항↔개념 매핑 0건」 — **닫힘**(as `67017c15` · talk `5573025`). 원인은 판단력이 아니라 **이름**이었다:    `계약/수집_교정_계약.json` 의 `learning_events.미구현` 목록은 구조상 «필드 − 라이브_대응» 의 여집합이라    (같은 파일의 분할 회귀가 그것을 강제한다) 애초에 **시트 열이 있나**만 아는 목록인데, 이름만 구현 여부를    말하고 있었다. 실측 = 58칸 중 층이 적힌 주석이 붙은 것은 35칸, **나머지 23칸이 층 없는 맨 부정**이고    `skill_ids` 가 그중 하나다. 처방 = 이름을 `라이브_대응없음` 으로 갈고(주의 → `라이브_대응없음_주석`),    `라이브_대응_축` 으로 축을 한 자리에 못박고, `tests/계약.test.js` 가 옛 이름(`^미구현`) 재등장과 축 설명    삭제를 fail 로 막는다. 변이 3/3(미적용 0) — 세 변이 모두 **의도한 검사**가 빨개진 것까지 이름으로 대조했다    (형제동기화 검사가 어느 변이에서든 함께 빨개지므로 포획 수만 세면 곁불을 내 탐지로 셀 뻔했다).    🔑 이 오독은 **네 번째**였고 앞의 셋은 처방이 전부 «주석 추가»였다(처분표 v3·v4 · 설계 §「오독이다」 ·    버전_이력 「계약 정본의 거짓말 정정」). 주석은 이름보다 늦게 읽힌다 — 지침 v9.2 「3번째 = 원인을 쓸 수    없게 만든다」를 그대로 집행했다.  ■ ② 「`skills.error_tags` 는 읽는 자리가 이미 있다」 — **안 닫혔다.** 기전이 다르다: ①은 저장소가 적어 둔    낡은 표기를 읽은 것이고, ②는 **같은 이름의 다른 물건**(`corrections.error_tags` 184곳)을 세어 소비자    개수로 옮긴 것이다. 고치려면 「부정 판정 전 형제 저장소 grep 강제」류의 가드가 필요한데, 그건 산문    판정문에 발화하는 장치라 거짓양성이 크고 이 저장소가 반복해 배운 「가드는 등록층에서 샌다」에 정면으로    걸린다 — **재료를 안 갖춘 채 지금 짓지 않는다.** 남은 축을 잃지 않게 이름을 박아 둔다:    «같은 이름 다른 네임스페이스를 한 분모로 세는 것». 집는 세션은 이 줄부터 읽는다.  ■ 이 보류가 값싸지 않은 이유 — ①의 처방이 ②에도 통하는지 물어봤고 답은 「안 통한다」였다. 통했다면    해소로 닫았을 것이다. 부분 수리를 전체 해소로 적는 것이 정확히 F092(안 고쳤는데 닫았다)다. |
