| F486 | 2026-08-15 | 실수 | 한 세션에서 「부정 판정을 층 하나만 보고」 두 번 냈다 — 유호님이 두 번 다 그 판정을 근거로 일감을 확정하셨다. ①「문항↔개념 매핑 0건」: 이 저장소 계약의 `skill_ids` 「미구현」 표기(c6 시점)만 보고 판단했는데 실물은 talk 에 100% 서 있었다(문항 100/100·스킬표 30·events 검증·L0 시드 30행). ②그 직후 「`skills.error_tags` 는 읽는 자리가 이미 있다」: 실은 소비자 0이다(`corrections.error_tags` 184곳과 이름만 같고 다른 물건인데 그 개수를 근거로 삼을 뻔했다). CLAUDE.md 에 이미 조항이 있다 — 「부정 판정은 값이 살 수 있는 층을 먼저 «열거»하고 그 전부를 연 뒤에만 쓴다」(F348·F403). 조항이 있는데도 2회 샜다는 것이 이 신호의 값이다: 그 조항은 「어디에도 없다」 꼴 문장에만 발화하는데, 실제로 샌 문장은 「0건이다」·「읽는 자리가 있다」처럼 긍정문 모양의 부정 판정이었다. 형제 저장소가 있는 이 프로젝트에서 「이 저장소에 없다 = 없다」는 상시 사각이다. 처방 후보(미확정) = 계약 파일의 「미구현」 목록이 낡음을 기계가 드러내게 하거나, 부정 판정 전 형제 저장소 grep 을 강제하는 가드. ⚠ 지금 짓지 않았다 — 유호 「최대한 압축」 지시대로 메모리 한 줄로 접었다(brain-learning-taxonomy 08-15 절). | |
