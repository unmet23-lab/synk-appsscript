| F308 | 2026-08-10 | 마찰 | 이종 검수(`tools/codex-review.js`)가 **남의 미커밋 바이너리 때문에 죽는다** — 그리고 그 죽음이 파이프 뒤에서 「통과」로 보였다. 2026-08-10 실측: 과녁 없이 부르면 작업본 미커밋 전체가 대상이 되는데(그 자체는 옳은 기본값이다), 그 순간 옆 세션 `747cc946` 이 발표물 **PDF 7벌**을 미커밋으로 들고 있어 대상이 「파일 31개」가 됐고 `spawnSync git ENOBUFS` 로 죽었다. 내 변경은 리터럴 3줄이었다.  두 겹의 문제다. ① **죽음이 통과와 같은 모양이었다.** 나는 `node tools/codex-review.js --버그만 2>&1 | tail -35` 로 불렀고, 하네스가 보고한 종료코드는 **파이프 마지막 명령(`tail`)의 0** 이었다. 스킬 문서는 「종료 2 = 확인 불가 · 2를 0으로 읽지 말 것」이라고 못박는데, **파이프를 한 번 끼우는 것만으로 그 구분이 사라진다.** 출력 본문에 🔴 가 찍혀 있어서 눈으로 잡았지 코드로는 못 잡았다(F207 계열 — 미실행이 통과와 같은 모양). ② **대상 범위가 내 배포집합보다 넓다.** 검수는 clasp 배포 파일을 보면 되는데 대상 diff 에는 남의 docs·PDF 가 통째로 들어간다. 세션이 10개 도는 시간대엔 「누군가는 바이너리를 미커밋으로 들고 있다」가 상시 조건이라, 이 죽음은 우연이 아니라 상수다.  우회한 방법(정상 통로여야 할 것): 내 트랙을 커밋하고 `--commit <sha>` 로 과녁을 좁혔다. 다만 자동커밋 훅이 먼저 파일 넷을 가져가 트랙이 두 커밋으로 갈려 있었다(F302 형태 · 이번엔 사람이 아니라 기계가 갈랐다).  처방 후보 ① 대상 diff 를 **배포집합 파일로 먼저 거른 뒤** git 에 넘긴다(남의 docs·PDF 는 애초에 검수 대상이 아니다). ② `execFileSync`/`spawnSync` 에 `maxBuffer` 를 주고, 넘치면 **죽지 말고 「확인 불가(2)」로 내려간다** — 지금은 예외로 터져서 사유가 안 남는다. ③ 파이프 뒤 종료코드 소실은 도구가 못 막으니, 호출 규약을 「파이프 없이 부르고 출력은 파일로」로 바꾼다. | tools/codex-review.js 가 git 자식에 maxBuffer 를 명시하고(257~268·322행) ENOBUFS 를 잡아 「--commit <sha> 로 과녁을 좁혀 다시 돌린다(미커밋 전체가 대상이면 남의 바이너리까지 들어온다)」를 처방한다(326행). 예외가 파이프 뒤에서 「통과」와 같은 모양이 되던 자리가 닫혔다. 2026-08-12 `local_e594c36e` 가 그 자리를 직접 열어 확인. |
