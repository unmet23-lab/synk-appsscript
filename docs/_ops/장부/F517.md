| F517 | 2026-08-16 | 마찰 | 「남의 지면에 얹을 때」라고 적힌 `부품만` 프리셋이 남의 지면을 때린다 — 그리고 얹으면 부품 0 이어도 전파 마커가 켜진다  Loom 전파 ③(발표물 6벌) 착수 실측. 인계문·정본 §5-2 는 이 칸을 「차단자 0」이라 적었는데, 재보니 차단자가 둘 있었다. 둘 다 「맞는 얼굴로 틀린 값」이다(지침 v9.2 맹점 ④).  ① **프리셋 이름이 약속하는 격리를 코드가 안 준다.** `tools/lib/loom.js:561` 은 `부품만: [...]  // 남의 지면에 얹을 때` 라고 적혀 있다. 그런데 그 산출 CSS 는 규칙 62 중 **28 이 맨요소 규칙**이다(`h2`·`ol`·`ul`·`li`·`p`·`a`·`td`·`th`·`blockquote`·`article`·`body`). 발표물 6벌은 mm 단위 인쇄 판을 자기 CSS 로 지닌 지면이라, 그대로 얹으면 **6벌 통틀어 34 자리**를 때린다(지면당 2~8). `바탕` 조각을 뺀 것만으로 「남의 지면에 얹어도 된다」가 되지 않는다 — 격리는 조각을 «빼는» 것이 아니라 선택자를 «가두는» 것이라야 선다.  ② **전파 마커가 「입었나」를 못 잰다.** `tools/lib/지면방.js:171` 의 `입은표시` 는 `/\/\*loom부품:/` — CSS 주석 하나다. 그래서 CSS 를 주입하기만 하면 부품이 한 개도 안 나와도 전파가 7/21 → 13/21 로 켜진다. 실측: 발표물 6벌의 마크업이 쓰는 Loom 부품 클래스는 **6벌 전부 0개**다(`.번호`·`.칩`·`.체크`·`.히어로수`·`.실금` 어느 것도 없다). 즉 지금 그대로 주입하면 「전파 13/21 초록 · 부품 0 · 인쇄 판 34자리 파손」이 된다 — 세 판 연속 갈렸던 분모(6/93·1/87·7/99 · F514)를 고친 바로 그 계기판이, 이번엔 **분모가 아니라 술어**로 같은 병을 낸다.  🔑 두 축을 갈라야 한다: F514 는 «무엇을 세는가»(분모)였고, 이건 «무엇을 세면 입은 것인가»(술어)다. 분모를 고쳐도 술어가 헐거우면 진척은 다시 거짓이 된다.  ⚠ 내 첫 실측도 틀렸다 — `:root` 를 6/6 충돌로 셌는데, 변수 이름이 서로 소라(Loom=`--단`·`--켜`·`--참` / 발표물=`--navy`·`--ink`) **덮어쓰기 0** 이다. 덮어쓰기는 «선택자가 같다»가 아니라 «선언하는 속성이 같다»에서 난다 — 선택자 층에서 멈추면 충돌을 부풀려 센다.  처방(이 트랙에서 집행): ㉠`css({범위})` — 맨요소 규칙만 그 범위 안에 가두고 한글 부품 클래스는 전역으로 둔다(클래스 이름은 이미 서로 소라 가둘 이유가 없다) ㉡컨테이너 선택자(`.글,article,body`)는 범위 «자신»으로 접는다 — `.룸 body` 는 영영 안 맞는다 ㉢마커만으로 「입었다」를 못 쓰게 부품 도달을 함께 센다. | ①은 닫았고 ②는 **빌드 층에서만** 닫았다(일반 해법은 남았다 — 그래서 반쯤 열어 둔다).  ① `css({범위})` — 맨요소 규칙만 `.룸` 안에 가둔다(`00eb3e99`). 한글 부품 클래스와 `:root` 변수는 전역(이름이 서로 소라 부딪지 않는다 · 실측 덮어쓰기 0). 컨테이너(`body`·`html`·`article`)는 범위 «자신»으로 접는다 — `.룸 body` 는 영영 안 맞아 counter-reset 이 죽는다. `@page` 는 가둘 수 없으니 **블록째 버린다**(통과가 곧 침범이라서 — 실제로 발표물 6벌의 `@page{size:A4;margin:0}` 을 덮을 뻔했다). 발표물 6벌 선택자 충돌 **34 → 0** · 통짜 프리셋 4벌 md5 master 동일.  🔑 판정 규칙을 짓다가 **두 번 샜고 둘 다 「안 가둔다」로**, 즉 남의 판을 때리는 쪽으로 샜다: ㉠뼈를 공백으로 잘라 판정 → `a:not(.민):not(.레일 a)::before` 의 괄호 «안» 공백에 속아 실패 → 실패가 「맨요소 아님」으로 떨어짐. ⇒ 물음을 「맨요소인가」에서 **「전역이어도 되는가」**로 뒤집었다(모르면 가둔다 — 새더라도 «안 맞는다» 쪽으로). ㉡`@keyframes` 안의 `from`·`to` 를 가두면 애니메이션이 죽는다(저장소에 오늘 0개인 것은 근거가 아니라 @-룰 스택으로 구조적으로 막았다).  ② **빌드 층**: `발표물빌드.js` 가 **훅이 있을 때만 얹는다**(`얹을까` · `e68e6ed4`). 어긋남은 양쪽 다 잡는다 — 훅 달고 안 구움(조용) · 훅 떼고 안 구움(**마커만 남아 「입었다」로 읽힌다** = 위험한 쪽). 그래서 발표물에 한해 «마커» = «부품» 이 성립한다. 🔴 **안 닫은 것**: `지면방` 의 술어(`입은표시` = `/\/\*loom부품:/`) 자체는 그대로다. 발표물 «밖»의 지면은 여전히 CSS 를 얹기만 하면 「입었다」로 켜진다. 일반 해법 = 술어에 «부품 도달»을 더하는 것인데, 이미 입은 7벌은 부품 클래스가 아니라 맨 `h2`·`ol` 로 입어서(전역 프리셋) 클래스만 세면 그 7벌이 거짓으로 강등된다 — 「닿는 훅이 마크업에 있나」로 재야 하고 그건 지면방 주인의 판이라 여기서 안 건드렸다.  곁가지로 **가드 둘이 「낡은 사본」이라 정본의 색을 틀렸다고 불렀다**(F520 계열 · 같은 커밋에서 원천 파생으로 고쳤다): ㉠`tests/로고정본색.test.js` 의 KIT 가 hex 19개 손 목록인데 킷은 **23색**이 됐다 — 정본이 정식으로 들인 `Slate #8A93AD`·`Slate 2 #5F657D` 를 「키트 밖 색」이라 불렀다(⇒ `디자인_토큰.json` 의 `색.킷` 에서 파생 · 토큰 파일 자신이 이미 `_킷주` 로 같은 교훈을 적어 뒀다) ㉡`tests/loom.test.js` ⑧ 이 `:root` 의 끝을 `*{box-sizing` 이라는 **랜드마크 문자열**로 잡아, 그 줄이 없는 프리셋에선 :root 안의 정상 선언까지 흠으로 냈다 — 그래서 프리셋 «이름»으로 예외를 파 뒀던 자리다(⇒ `:root{…}` 를 직접 걷어내 **이름 예외 0개**).  검증: 회귀 106 초록 · **변이 9/9 + 7/7 구멍 0 미적용 0** · CI 모사 pass 3759 / fail 2(둘 다 옆 세션의 `Code.js`·`이해대장.html` — test-ci 가 스스로 「이 적색은 못 믿는다」로 표시) · 전파 **7/21 → 10/21**. |
