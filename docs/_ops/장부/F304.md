| F304 | 2026-08-09 | 마찰 | board-guard 가 「남이 선언한 자리」를 막을 때 내주는 탈출구가 **승계 세션에게는 원리상 닫혀 있다**(F103 형태). 탈출구는 「죽은 세션의 줄이면 `node tools/board-move.js` 로 치운다」인데, board-move 는 `작업본소유자.살았나` 를 쓰고 그건 **파일 mtime 30분 창**이다(`SYNK_OWNER_ALIVE_MIN` 기본 30). 인계는 앞 세션이 끊긴 «직후»에 일어나므로 승계 세션이 그 문을 두드리는 시각은 **언제나 창 안**이다 — 처방이 가리키는 문이 승계 자리에서 30분간 항상 잠겨 있다.  실측 2026-08-09: `ddd51fde` 가 `content.viewed` 트랙(대기열 P1)을 선언만 하고 끊겨 `bfe0a73d` 에게 인계했다. `작업본소유자.js` 는 그 세션을 「살아있는 남의 작업본」으로 셌다(마지막 만짐 0분 전). 그런데 **세션 레지스트리에는 그 id 가 아예 없었다** — `list_sessions` 를 `include_archived` 까지 켜고 봐도 5개뿐이고 `ddd51fde` 는 그 안에 없다. 같은 출력이 센 「살아있는 세션 8개」도 실제로는 5개였다. 즉 **권위 있는 생사 정보가 있는데 30분 mtime 추정이 그것을 덮었다.**  열린 통로(우연이다): board-guard ④ 는 `트랙칸(남) === 트랙칸(새)` 이면 겹침으로 세지 않는다 — 앞 줄의 트랙 칸을 **바이트 그대로** 적어 승계를 선언하면 통과한다. 그 자리로 지나갔지만, 그건 안내문 어디에도 없어서 다음 승계 세션은 같은 벽에서 또 멈춘다.  처방 후보 ① `살았나` 가 mtime 추정 전에 **세션 레지스트리를 먼저 본다**(레지스트리에 없으면 죽음 · mtime 은 레지스트리를 못 읽을 때의 폴백). 안전 방향이 뒤집히지 않는다 — 레지스트리에 «있는데» 조용한 세션은 지금처럼 살아있음으로 남는다. ② board-guard 겹침 안내문에 「같은 트랙을 이어받는 것이면 **트랙 칸을 그대로** 적어 승계 선언한다」를 한 줄 넣는다(지금은 「비켜난다」만 있어 승계가 트랙 포기로 읽힌다). | 처방 ①②를 둘 다 넣었다 — `a970ab02`(판정) + `229a22f8`(안내). 🔑**신고문의 ① 은 그대로는 불가능했고, 급소는 다른 데 있었다**: 세션 레지스트리(`list_sessions`)는 MCP 도구에만 있어 훅·스크립트가 못 부르고, 내 세션 id(`local_8aa9d53a-…`)는 `~/.claude/sessions`·`projects` 어디에도 파일로 없다(실측 — 그 폴더의 `sessionId` 는 F074 가 말한 «다른 식별자»다). 그런데 **권위 정보는 이미 repo 도구 안에 있었다** — `작업본소유자.세션들()` 이 읽는 `끝남`(SessionEnd 죽음 도장 · F252)이 그것이고, `살았나` 가 그것을 **일부러 안 본다**(그 주석이 의도로 못박은 자리 · F025 방향 보존). 즉 「권위 정보가 없다」가 아니라 **있는데 안 읽는다**였다. 실측 08-12: 도장이 찍혔는데 `살았나` 가 「살았다」로 세는 세션 **4건** · 그 줄들이 안 치워져 보드가 상한 18 을 넘겨 20줄이 되며 **새 세션의 선언 자체가 막혔다**(board-guard 「완료 줄 6개는 board-move 가 전부 거절한다」). ①=`작업본소유자.마감했나`+`선언살았나` 1벌 신설 → **소비자 둘이 같은 것을 본다**(board-move 원칙⑥ · board-guard ⑧). 서로 다른 생사를 보면 한쪽은 막고 한쪽은 통과시키는 「따를 수 없는 처방」이 된다. 🔑판정은 **죽음 쪽으로만** 움직인다 — 도장이 없으면 옛 판정 그대로고, 되살아나면 track-collision 이 도장을 지운다. `살았나` 본체는 안 건드렸다(거기 도장을 요구하면 「도장 없는 강제 종료」가 영원히 살아있음이 되어 인계문이 안 거둬지고 미추적 소멸 · F025). ②=④ 겹침문에 승계 표기 안내 1줄(`트랙칸(남)===트랙칸(새)` continue 통로는 **이미 열려 있었는데 안내가 없어** 승계 세션이 트랙을 포기하거나 근거를 지워 우회했다) + 🚫산 세션 자리엔 쓰지 않는다를 같은 줄에. 회귀 `tests/작업본소유자.test.js` 3건(잠긴 문 · 도장 없을 때 방향 보존=F146 사고 축 · 나 자신 특례=F264) · **변이 2/2** 실측 · 85/85 + 보드·훅 69/69. 실동작 실측: 수리 전 exit 6 으로 거절되던 줄이 `--dry` 에서 통과했고, 이 세션이 실제로 2줄을 이관해 **전체 줄 위반이 사라졌다**(21→18 이하). ⚠남은 것: 활성 줄 13(상한 12)은 «작업중» 줄이라 board-move 대상이 아니다 — 그건 각 주인 몫이다. |
