| F381 | 2026-08-12 | 실수 | 같은 🎫 하나를 두 세션이 10분 안에 나란히 설계했다 — 보드 선언이 「먼저인 쪽」이 나였는데도 못 막았다. 실측 2026-08-12: `955e8a16` 이 남긴 🎫(「㉡ 한 벌 + 선생님 피드백 UX(반 단위)」)를 `1a1b47ad`(나)가 19:48 에 보드 선언(`843e3cd2`)하고 19:56 에 설계 v1 을 커밋했는데(`c7ed0d14` · `docs/강사_피드백_UX_설계.md`), `5c3f51e1` 이 19:58 에 같은 🎫 을 선언하며(`7790ce67`) 이미 끝난 설계(talk `e8b0631` · `docs/강사_반단위_피드백_설계.md`)를 들고 나왔다. 저쪽은 「선언보다 작업을 먼저」 했고, 그래서 내가 착수 전에 돌린 `node tools/board.js` 와 `node tools/작업본소유자.js` 어느 쪽에도 그 트랙이 없었다 — 두 도구 다 「커밋된 보드 줄」과 「이 저장소의 미커밋 작업본」을 보는데 저쪽은 그 순간 둘 다 아니었다(형제 저장소 SYNK-talk 에서 돌고 있었고 as 보드엔 줄이 없었다). 지금 충돌 탐지의 사각은 「형제 저장소에서 선언 없이 도는 트랙」이다 — F134·F137 이 미커밋 축에서 잡은 것과 같은 자리인데 축이 「보드」다. 손해 둘: ①설계 2벌(중복 구현 F070 의 문서판) ②그중 내 판이 틀린 채로 유호님께 갈 뻔했다 — 내 ⓑ 는 「Glide 판 화면이 지금 어디에도 없다」로 단정하고 유호님께 ⏳로 올렸는데, 그 화면은 `Code.js:3336` 부근에 살아 있었다(15·16열 `반카드요약`·`반몬스터이미지` + 주석에 유호 07-31 원문). 즉 「목록이냐 카드냐」는 물을 것이 아니라 **읽으면 되는 것**이었고 답은 카드였다(F376 과 같은 형태 — 묻기 전에 저장소를 먼저 뒤진다). 처방 후보 셋: ①「🎫 을 집었다」를 보드 선언보다 앞에 두는 원자적 통로 — 지금은 「집기」와 「선언」이 두 걸음이라 그 사이가 통째로 사각이다 ②`board.js` 가 형제 저장소의 최근 커밋 제목·신규 `docs/` 파일도 함께 훑어 「선언 없는 트랙」을 드러낸다 ③최소한 🎫 을 집을 때 그 **🎫 원문구**로 형제 저장소를 먼저 grep 한다(이번엔 문서 이름이 서로 다른 낱말이라 이름 대조로는 못 잡혔겠지만 🎫 문구로는 잡혔다). 처분: 정본은 `5c3f51e1` 것으로 하고 내 문서는 폐기 스텁으로 바꿨다(좌표 델타 4줄만 남김) · 대기열·메모리·인덱스 전부 그쪽을 가리키게 고쳤다. | `board-guard` ⑧-b 신설 — 트랙 칸의 **🎫 출처 지문**이 남의 활성 줄과 겹치면 알린다(회귀 4 · 스위트 94/94).  🔑 **처방을 측정이 골랐다 — 내가 처음 낸 것은 안 발화했다.** 신고 때 적은 후보 ②(「형제 저장소 최근 커밋을 훑는다」)를 실측으로 재니 **그날 0건**이었다: 저쪽 설계 커밋(talk `e8b0631`)이 **19:58:19** 로 내 보드 선언(`843e3cd2` · **19:48:18**)보다 **뒤**라, 착수 시점에 형제 저장소를 훑어도 볼 것이 없었다. 스스로 발화하지 않는 장치를 지을 뻔했다.  🔑 그리고 **장치는 이미 있었다** — `board-guard` ⑧ 트랙충돌(F340)이 바로 그 사고를 막으려고 08-12 에 섰다. 왜 안 울렸나를 계산으로 확정했다: 그 두 줄의 제목을 ⑧ 판정(`제목열쇠`→`겹침률`)에 그대로 먹이면 **0.375**(문턱 0.75)다. 이유는 문턱이 아니라 **구조**다 — 포함률의 분모가 `min(A,B)` 라 「같은 트랙 + 말 몇 개 더」는 잡지만, 한쪽이 🎫 를 **통째로**(「G2 ㉡ 서버판정 + 「맞음」 큐 자동하강 + 선생님 피드백 UX」) 적고 다른 쪽이 그 **한 조각**만 적으면 공유 낱말이 소수파가 되어 못 잡는다. 문턱을 낮추는 것은 답이 아니다(⑧ 자신이 「거짓양성이 곧 우회 손버릇」이라 적어 뒀다).  ✅ 그날 **울렸을** 축은 따로 있었다: 두 줄 다 트랙 칸에 `` `955e8a16` 🎫 `` 라고 출처를 적고 있었다 — 흐린 유사도가 아니라 「어느 일감을 물려받았나」의 **정확한 이름**이다. 실측 계산으로 확인했다(교집합 `955e8a16`).  ■ 못박은 방향 넷   ① **알림이지 차단이 아니다** — 하나의 🎫 를 둘로 갈라 맡는 것은 정상 통로다(그날 🎫 자체가 「㉡ 한 벌 + 피드백 UX」 두 벌이었고 실제로 갈라 맡으려 한 시도가 있었다 · talk `7f64606`). 막으면 따를 수 없는 처방이 된다(F103). 살아있는 상대와 **제목까지** 겹치면 ⑧ 이 이미 차단하니 이 축은 그 그물의 구멍만 덮는다.   ② **트랙 칸에서만 읽는다** — 상태 칸의 🎫 는 뜻이 반대다(「나는 이걸 안 한다」=내놓기). 섞으면 일감을 내놓는 줄마다 울어 경고가 통째로 무의미해진다(보드에 상시 8건). 회귀로 못박았다.   ③ **생사는 ⑧ 과 같은 곳(`산지문`)에서 온다** — 두 축이 다른 생사를 말하면 처방이 갈린다.   ④ **이 장치가 잡는 것은 언제나 «둘째 선언자»다** — 그날 나에게는 안 울었을 것이다(상대 줄이 10분 뒤에 생겼다). 그거면 족하다: 중복은 둘이 필요하고 둘째를 세우면 안 벌어진다. 「먼저 일하고 나중에 선언하는」 습관은 보드 층의 어떤 장치로도 못 막는다 — 그건 남는 구멍이라 여기 적어 둔다.  ■ 회귀 4 (`tests/board-guard.test.js` · 탐지력은 픽스처가 진다)   · 그날 두 줄을 **글자 그대로** 박아 알림이 뜨는지 · 그런데 **막지는 않는지**(처방 되먹임 F103) · 🎫 출처가 다르면 조용한지(거짓양성) · 상태 칸 🎫 로는 안 우는지. 기존 F340 3건은 한 글자도 안 바뀌었다(픽스처에 기본값 옵션만 더해 두 벌 복사를 피했다).  ⚠ 남은 절반은 F382(되넘김) — 그쪽 처방 셋은 아직 미착수다. 이 축이 둘째 선언자를 세우면 되넘김도 덜 생기지만 **막지는 못한다**. |
