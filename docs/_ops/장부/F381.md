| F381 | 2026-08-12 | 실수 | 같은 🎫 하나를 두 세션이 10분 안에 나란히 설계했다 — 보드 선언이 「먼저인 쪽」이 나였는데도 못 막았다. 실측 2026-08-12: `955e8a16` 이 남긴 🎫(「㉡ 한 벌 + 선생님 피드백 UX(반 단위)」)를 `1a1b47ad`(나)가 19:48 에 보드 선언(`843e3cd2`)하고 19:56 에 설계 v1 을 커밋했는데(`c7ed0d14` · `docs/강사_피드백_UX_설계.md`), `5c3f51e1` 이 19:58 에 같은 🎫 을 선언하며(`7790ce67`) 이미 끝난 설계(talk `e8b0631` · `docs/강사_반단위_피드백_설계.md`)를 들고 나왔다. 저쪽은 「선언보다 작업을 먼저」 했고, 그래서 내가 착수 전에 돌린 `node tools/board.js` 와 `node tools/작업본소유자.js` 어느 쪽에도 그 트랙이 없었다 — 두 도구 다 「커밋된 보드 줄」과 「이 저장소의 미커밋 작업본」을 보는데 저쪽은 그 순간 둘 다 아니었다(형제 저장소 SYNK-talk 에서 돌고 있었고 as 보드엔 줄이 없었다). 지금 충돌 탐지의 사각은 「형제 저장소에서 선언 없이 도는 트랙」이다 — F134·F137 이 미커밋 축에서 잡은 것과 같은 자리인데 축이 「보드」다. 손해 둘: ①설계 2벌(중복 구현 F070 의 문서판) ②그중 내 판이 틀린 채로 유호님께 갈 뻔했다 — 내 ⓑ 는 「Glide 판 화면이 지금 어디에도 없다」로 단정하고 유호님께 ⏳로 올렸는데, 그 화면은 `Code.js:3336` 부근에 살아 있었다(15·16열 `반카드요약`·`반몬스터이미지` + 주석에 유호 07-31 원문). 즉 「목록이냐 카드냐」는 물을 것이 아니라 **읽으면 되는 것**이었고 답은 카드였다(F376 과 같은 형태 — 묻기 전에 저장소를 먼저 뒤진다). 처방 후보 셋: ①「🎫 을 집었다」를 보드 선언보다 앞에 두는 원자적 통로 — 지금은 「집기」와 「선언」이 두 걸음이라 그 사이가 통째로 사각이다 ②`board.js` 가 형제 저장소의 최근 커밋 제목·신규 `docs/` 파일도 함께 훑어 「선언 없는 트랙」을 드러낸다 ③최소한 🎫 을 집을 때 그 **🎫 원문구**로 형제 저장소를 먼저 grep 한다(이번엔 문서 이름이 서로 다른 낱말이라 이름 대조로는 못 잡혔겠지만 🎫 문구로는 잡혔다). 처분: 정본은 `5c3f51e1` 것으로 하고 내 문서는 폐기 스텁으로 바꿨다(좌표 델타 4줄만 남김) · 대기열·메모리·인덱스 전부 그쪽을 가리키게 고쳤다. | |
