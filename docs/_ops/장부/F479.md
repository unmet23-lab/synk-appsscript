| F479 | 2026-08-15 | 교정 | 브랜드킷 첫 판에 마스코트를 구판(체리 젤리 렌더 누끼)으로 실었다 — 유호 교정 「이걸로 바뀌었는데 왜 예전버전으로?」. 원인=그림 경로를 토큰이 아니라 손 경로(마스코트_누끼)로 하드코딩 — 정본(정본사진.평상복=펠트 코랄 · 유호 픽 08-15)이 갈렸는데 안 따라갔다. 수리=조립 도구가 토큰에서 읽게 배선(F472 문법 · 같은 커밋) | 수리가 신고와 같은 커밋에 동봉(0be906db) — 조립 도구가 마스코트 그림을 토큰 정본사진.평상복에서 읽는다(하드코딩 제거). 검증=재조립 후 구판 경로(마스코트_누끼) 흔적 0 grep + 렌더 실측 · 정본이 갈리면 재조립이 따라간다 |
