| F506 | 2026-08-16 | 실수 | 결정큐 3형 훑기 1차가 «고치면서» 배포 게이트를 깨뜨렸다 — 저장소가 이미 그 자리에 경고문을 붙여 뒀는데 그대로 밟았다. 1차(08-16 local_96cbc032)는 낡은 표식을 고치려고 guard-routing-and-collisions.md 의 살아 있는 ⏳ 줄 «안»에 「✅활성 상한(12) 층은 이미 빼고 있다」를 적어 넣었다. tests/결정큐.test.js 의 「실저장소: 큐가 배달하는 항목 본문에 ✅가 없다」가 그 자리에서 빨개졌고, 이 적색은 주인 없는 적색이라 모든 세션의 배포 게이트를 막는다. 2차(08-16 local_8464c7f1)가 전량 훑기 전 회귀를 돌려 잡았다 — 1차는 메모리를 고친 뒤 이 테스트를 안 돌렸다. 급소는 조항 부재가 아니라 «발동 조건»이다: reviewer-console-ux.md:459 에 「위 ⏳ 줄과 아래 ✅ 줄을 한 줄로 합치지 마라 — 합치면 결정큐.test.js 가 빨개져 모든 세션의 배포 게이트가 막힌다(08-12 실측으로 실제 막혔다)」가 HTML 주석으로 이미 박혀 있다. 즉 같은 병의 2번째이고 처방문까지 있었는데, 그 처방문이 «그 파일 안»에만 있어서 다른 파일을 고치는 세션에게는 안 보였다. 그리고 메모리 편집은 repo 밖이라 pre-commit 훅 경로에 안 걸리고, 결정큐.test.js 를 돌리라고 말해 주는 자리가 어디에도 없다 — 장치는 있는데 발동하지 않는 형태(CLAUDE.md 신뢰성 「장치와 그 발동 조건은 같은 커밋에서 정한다」의 반례). | 2차 훑기가 그 줄을 「✅사실 줄 + ⏳질문 줄」로 갈라 복구했다(결정큐.test.js 47/47 초록 · 84건 배달 유지). 그리고 처방문을 그 자리로 옮겨 심었다 — guard-routing-and-collisions.md 의 ✅ 줄 바로 밑에 「이 ✅ 는 아래 ⏳ 줄과 한 줄로 합치지 않는다 · 합치면 모든 세션의 배포 게이트가 막힌다」를 박아, 다음에 이 줄을 고치는 세션이 «고치는 그 자리에서» 읽게 했다(reviewer-console-ux 에만 있던 경고가 안 보였던 것이 이 사고의 원인이라 사본이 아니라 이동·확산이다). 남은 구멍은 정직하게 열어 둔다: 메모리 편집이 결정큐.test.js 를 자동으로 호출하는 자리는 여전히 0이다 — 새 장치 0 이 이 트랙의 제약이라 짓지 않았고, 대신 이 신고가 그 빈칸의 기록이다. |
