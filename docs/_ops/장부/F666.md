| F666 | 2026-08-19 | 실수 | 훅 등록을 검사하는 두 회귀가 «스위치가 낀 명령»에서 과녁을 잃는다 — 그리고 새는 방향이 초록이다.  실측(2026-08-19 · `가드강등.js` 를 처음 **실제로** 켠 자리에서 드러났다 · fail 2→4):   그 도구는 훅 명령 앞에 존재 검사를 끼운다 —     `[ -f "${CLAUDE_PROJECT_DIR:-$PWD}/.claude/개원.json" ] || exit 0;`   이 절이 가리키는 것은 실행되는 스크립트가 아니라 **플래그 파일**이고, 미개원 동안 없는 것이 정상이다.    ① `tests/훅이식성.test.js` ③ 죽은 참조 — 경로를 `match()` 로 **첫 매치 하나만** 뽑았다.      스위치가 맨 앞에 끼자 첫 매치가 플래그 파일로 바뀌었고, 원래 보던 훅 스크립트를 **대신 잃었다.**      🔴 지금은 「없는 파일을 가리킨다」로 빨개져 드러났지만, 개원해서 `.claude/개원.json` 이      **생기는 순간** 이 검사는 초록인 채 **훅 스크립트를 한 건도 안 본다** — CLAUDE.md 맹점 ④      (맞는 얼굴로 틀린 값)의 교과서 형태다. 죽은 훅 등록을 잡으라고 있는 검사가 개원과 함께 눈이 먼다.    ② 같은 파일 ④ 스캔 범위 — `([^"'\s]+\.js)` 를 앵커 없이 걸어 `개원.json` 의 `.js` 를 물었다.      있지도 않은 `.claude/개원.js` 를 **지어내** 「등록이 죽은 경로를 가리킨다」로 울었다.    🔑 둘은 같은 판정을 각자의 정규식으로 적고 있었다 — 그래서 같은 자리에서 함께 샜다.  해소 — `참조경로()` 공용 통로 하나로 합쳤다(`tests/훅이식성.test.js`):   · 존재 검사 절을 **형태로** 걷어낸다(`|| exit 0` 로 끝나는 것만 · 파일명에 안 묶인다).     가드 자신의 실패 안전 절 `|| { … exit 2; }` 는 형태가 달라 안 걸린다.   · 첫 매치가 아니라 **전부** 뽑는다 · `.js$` 앵커로 `.json` 오독을 막는다.   · 탐지력은 픽스처 7건으로 못박았다 — 실저장소만 보면 「위반 0」과 「아무것도 못 뽑았다」가     같은 초록이라(F207), 뽑아야 할 것을 뽑는지와 죽은 경로를 여전히 잡는지를 양쪽으로 잰다. | tests/훅이식성.test.js 에 참조경로() 공용 통로 — 스위치 절을 형태로 걷어내고 경로를 전부 뽑는다(.js$ 앵커) + 탐지력 픽스처 7건. 두 검사가 각자 들던 정규식 2개를 하나로 닫았다. |
