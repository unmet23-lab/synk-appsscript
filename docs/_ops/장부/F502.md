| F502 | 2026-08-16 | 마찰 | 커밋 98건이 하루 종일 push 0건으로 노트북에만 있었다 — 「미커밋 0」은 지켰는데 그 층 위가 통째로 비었다  2026-08-16 실측(게이트 3단계 착수 전). `origin/master` 가 08-15 23:15 에 멈춰 있었고, 그 뒤로 16개 세션이 100커밋을 쌓는 동안 push 가 **한 번도** 없었다. 이 저장소는 08-01~08-15 매일 30~250회 push 했다(reflog 실측) — 즉 이날만 0이다.  ■ 왜 조용했나 · 훅 차단이 아니다 — `pre-push` 훅 자체가 없고, `git push --dry-run` 이 fast-forward 로 통과했다. · push 는 `/deploy` 6단계와 `/close` 에만 얹혀 있다. 즉 **스킬을 안 부르면 아무도 안 민다.** · 그런데 세션 규약의 감시 문장은 「미커밋을 오래 두지 않는다」까지다. 커밋은 다들 지켰다.   **지킨 채로 하루치가 무보호였다** — 감시가 한 층 아래에서 멈춰 있었다.  ■ 왜 굵직한가 어제 세운 서버측 잠금(Ruleset)도, 이번에 세운 PR 층도 **origin 에 올라온 것만** 지킨다. 안 민 하루는 그 층 전체가 없는 하루다. 게다가 화면상 아무 표시도 없다 — `git status` 는 「ahead 98」을 조용히 한 줄로 적을 뿐이고, 그 줄은 평소에도 늘 떠 있어서 눈에 안 든다.  ■ 처방 (같은 커밋에서 함께) · CLAUDE.md 세션 규약에 「커밋과 push 는 한 벌이다」 신설 — 트랙 닫을 땐 `작업가지 --닫기` 가 밀고,   master 직접 커밋이면 그 자리에서 `git push origin master`. · 3단계 통로(`--닫기`)는 머지 전에 반드시 push 하므로, 워크트리 트랙은 구조적으로 이 병에 안 걸린다. · ⚠ 아직 **기계 강제는 없다** — master 직접 커밋 경로는 여전히 프로즈다. 며칠 실측 뒤   「ahead N 이 임계를 넘으면 SessionStart 가 소리친다」를 세울지 판정한다(지금 세우면 분모를 모른다). | SessionStart 훅(tools/작업가지.js --훅)에 밀림 경보를 붙였다 — 안 밀린 커밋이 30건을 넘으면 소리친다. 문턱은 reflog 15일 실측(push 1610회·커밋 5779개 · 묶음 p99=29건)에서 왔고 과거 0.99%만 울렸을 값이다. 시간 축이 아니라 개수 축인 이유는 지키려는 것이 잃을 바이트의 양이라서다(1건이 12시간 묵는 건 정상 · 1시간 초과가 전체의 16%). 회귀 13/13 · 변이 7/7+10/10 구멍 0. 새 훅 0·새 파일 0·네트워크 0(로컬 ref만) · 스로틀 밖 · gh 없어도 돈다. 커밋 415e7809 |
