| F399 | 2026-08-13 | 마찰 | 대기열 줄의 「잔여=…」 목록이 형제 커밋으로 이미 닫힌 항목을 계속 이고 있다 — 다음 세션이 이미 선 것을 다시 짓는다. 실측: 대기열 P0 줄31 의 잔여가 「…·배정 갈래(초급→G2 인수조건)·여는 건 팩 검수확정」이었는데 배정 갈래는 형제 커밋 bec282f(talk lib/게임배정.js — 초급게이트 `초급인가` + G1G3G4 회전풀)에서 이미 서 있었다. 같은 줄이 그 커밋을 ✅로 적으면서 잔여 칸은 안 걷혀, 한 줄 안에서 두 칸이 서로 반대말을 한다. 0766bb39 가 착수 직전 lib 를 읽어서 갈랐기에 망정이지 그대로 집었으면 중복 구현이고 그건 되돌릴 수 없다. 뿌리: 대기열.js 의 「낡음 후보」 신호는 줄 단위(형제가 이 층을 걸고 커밋했나)라 줄 «안»의 잔여 항목은 원리상 못 본다 — 그리고 새는 방향은 여기서도 언제나 「할 일이 아직 남았다」쪽이라 조용하다(가드가 새는 네 형태와 같은 축). | ⏸ 안 고쳤다 — 이 세션이 닫은 것은 워크트리 낡은 사본 1건(relaxed-cartwright @ 9ea9462a)이고, F399 본문인 「대기열 줄의 잔여=… 목록이 형제 커밋으로 이미 닫힌 항목을 계속 인다」는 손도 안 댔다. 뿌리(대기열.js 의 낡음 신호가 줄 «단위»라 줄 «안»의 잔여 항목을 원리상 못 본다)는 도구 개선 트랙이라 이번 수거 범위 밖이다. 판정 자리: 유호 지시 08-13 「미커밋된 것 중 중요한 것들 이어서」 수거 세션(보드 b5fa8205 · 커밋 720e422b). ⚠ 이 번호를 달고 커밋이 들어간 이유는 tools/작업본소유자.js 가 워크트리 경고 4곳에 F399 를 인용하고 있어서다 — 그 인용은 이 행과 무관하다(별도 신고). ■ **[2026-08-13 · 세션 8807a562 일괄 수리 트랙] 보류를 유지한다 — 이번엔 「범위 밖」이 아니라 「재고 나온 판정」이다.** ①**순진한 탐지기는 소음이다**: 「한 줄에 커밋 인용 + 잔여 목록을 함께 인 줄」을 세니 열린 25줄 중 **17줄(68%)**. 그 비율이면 경보가 상시 켜져 있고, 상시 켜진 경보는 안 읽힌다 — `tools/대기열.js` 자기 머리말이 「세 줄만 낸다: 길어지면 전부가 소음이 되고 아무도 안 읽는다」로 이미 못박은 자리다. ②**더 결정적 — 그 탐지기는 F399 자신도 못 잡는다.** 신고 사례의 잔여는 「배정 갈래(초급→G2 인수조건)」로 백틱 식별자도 경로도 없고, 그것을 닫은 형제 커밋 `bec282f` 는 제목에 「대기열 P*n*」을 안 걸었다 — 대기열.js 가 2026-08-09 실측으로 고른 세 신호(①파일 경로가 형제에서 바뀌었나 ②백틱 식별자가 형제 diff 에 나왔나 ③형제 커밋 제목이 층을 인용했나) 중 **어느 것에도 안 걸린다.** 즉 뿌리는 신고문이 적은 「줄 단위라 줄 안을 못 본다」보다 한 겹 아래다: **잔여 칸은 자유 텍스트라 어떤 기계도 「이 항목이 닫혔나」를 못 판정한다**(F398 이 인계문 잔여에서 내린 결론과 같은 층 — 기계가 못 가르는 것은 사람에게 넘긴다). ③남은 좁은 후보 = F398 이 세운 `session-report.js` 의 `잔여조각()` 을 **고르는 순간에만** 재사용해 조각을 글자로 보이는 것. 그런데 대기열.js 엔 「한 줄을 고른다」는 표면이 없다(CLI 플래그가 `--확정대조` 하나뿐) — 표면 신설은 별도 도구 설계 트랙이고, 그 전엔 이 재사용이 세션 시작 **상시 출력**으로 갈 수밖에 없어 ①의 68%로 되돌아간다. ▶**재개 조건 둘**: 대기열에 「고르기」 표면이 생기는 날(그때 `잔여조각()` 재사용) · 또는 같은 사고가 2번째로 나는 날(그때는 표면부터 판다). 그때까지 서 있는 방벽은 사람 규칙 하나뿐이다 — **착수 직전 그 항목의 실물을 연다**(신고문에서 `0766bb39` 를 살린 그 행동). |
