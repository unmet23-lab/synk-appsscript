| F284 | 2026-08-09 | 실수 | 픽스처를 실물과 같은 모양으로 짰더니 세 축이 섞여, 앞 축이 뒤 축을 가려 두 축이 한 번도 실행되지 않았다 — 그런데도 스위트는 11/11 초록이었다. 실측 2026-08-09(F277 수리 중): 판정 재료를 셋(주석 제거 · 쓰기 구분 · 스코프 자르기)으로 가르고 거짓양성 픽스처 3개를 실물 deliver 모양 그대로 짰는데, 변이 ①(주석 제거를 끈다)·②(쓰기 구분을 끈다)가 둘 다 「아무도 안 잡는다」로 나왔다. 원인은 픽스처마다 세 축이 다 들어 있어 스코프 자르기가 먼저 걸러버린 것 — 뒤 축은 도달조차 못 했다. 즉 픽스처가 실물을 **닮는 것**과 그 축을 **시험하는 것**은 다른 일이고, 닮게 짤수록 실물의 다른 방어선이 함께 들어와 앞에서 막는다. 변이가 없었으면 「회귀 7건 · 11/11 초록」으로 종결했을 것이고 그 보고는 거짓이 됐다. 곁들여 변이 ②는 더 나쁜 것을 드러냈다 — 그 축에는 붙일 코드가 애초에 없었다(from/join 조건이 이미 쓰기를 거르고 있었다). 내가 만든 읽기부분() 은 아무 일도 안 하면서 잘못 고칠 여지만 만들었다(그 함수를 망가뜨리는 변이는 insert…select 탐지를 통째로 껐다). | tests/검수큐.test.js 픽스처를 축별로 다시 짰다(talk a2f595c · 변이 5/5) — 각 픽스처는 **자기 축만 남기고 나머지 축은 통과하도록** 준다(주석 축은 직원 함수 한 개 안에서, 쓰기 축도 직원 함수 안에서, 스코프 축만 함수 둘로). 그러면 그 픽스처가 정확히 한 가지를 못박는다. 그리고 하는 일 없던 읽기부분() 은 지웠다 — 없는 코드는 못 깨뜨린다. 🔑 이 결함을 드러낸 것은 스위트가 아니라 변이다: 초록은 분모와 함께만 읽는다는 조항의 픽스처 판 — **몇 건이 통과했나가 아니라 몇 축이 실제로 실행됐나**를 물어야 했다. |
