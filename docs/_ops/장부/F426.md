| F426 | 2026-08-14 | 마찰 | 배포 커밋이 `[vN]` 을 제목 «꼬리»에 달면 배포 기준선 탐지(`배포판점검.js 안나간변경()` — `git log --grep '^\[v'`)가 그 커밋을 못 보고, 그 순간부터 매 세션 시작의 🚀 「배포집합이 마지막 배포보다 앞서 있다」가 **상시 거짓 🔴** 로 발화한다 — F113 그대로(거짓 경보가 매번이면 진짜 드리프트 문장이 이미 무시당하고 있다). **2번째다**: ① `f28e3986` 「feat: 숙제 서클 인쇄물 … [v9.228] (대기열 P1)」(Code.js·contents_서클.js 접촉) — v9.230 이 제 표기로 착지하며 우연히 자가 치유 ② `015b9fd4` 「fix: 「골든셋」 → 「강사 정답 모음」 개명 … [v9.231]」(Code.js 접촉) — 라이브는 실측 바이트 동일(=HEAD)인데 기준선이 e61ef44(v9.230)에 묶여 오늘 세션들이 전부 거짓 🔴 를 받았다(`local_6c887e59` 실측 08-14). 탐지를 「제목 어디든 [vN]」으로 푸는 것은 오답 — 시작 아닌 [vN] 35건 중 33건이 버전을 «인용»한 docs 커밋이라 기준선이 진짜 배포 커밋을 지나쳐 앞으로 가고, 그쪽은 거짓음성(F244 ① — 영원한 침묵)이다. 처방 = 쓰는 순간 기계로: 배포집합 파일을 만지는 커밋의 제목에 [vN] 이 있는데 시작이 아니면 deny(버전 커밋이면 제목 시작으로 · 옛 버전 인용이면 괄호를 벗겨 v9.NNN 으로). | |
