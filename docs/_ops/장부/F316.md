| F316 | 2026-08-10 | 마찰 | git-scope-guard 의 diff 펼침이 **줄 수(40줄)로만** 잘라 바이트를 안 봤다. 발표물 HTML 은 minify 되어 한 줄이 최대 54,293 바이트라, 커밋 한 번에 2,544,098 바이트(실측 608,815 토큰)가 컨텍스트로 들어왔다 — 창(200k)의 4배다.  증상은 「가드가 막았다」가 아니라 **세션이 죽었다**로 나왔다. 발표물 18건 미커밋 트랙을 이어받은 세션 9개(f7a204ef·f2f20029·001d72cd·f9a760fc·9585e4a8·36112cad·8624e8f6·7b38c6ce·5f7c04c3)가 전부 같은 자리에서 컨텍스트 초과로 끝났고, 다음 세션이 또 이어받아 같은 커밋을 시도해 연쇄됐다. 유호님 신고 = 「작업 시키자마자 얼마 안 지나서 계속 컨텍스트 한도 초과」.  급소: 가드의 실패 모드에 **크기**가 없었다. 이 훅의 회귀는 「안 막음(거짓음성)/과하게 막음(거짓양성)」 두 축을 같은 무게로 지켜 왔는데, 세 번째 축인 **「막으면서 세션을 죽임」** 은 아무도 안 쟀다. 게다가 처방(`git diff HEAD -- <파일>` 전량)마저 그 자체로 컨텍스트를 터뜨리는 명령이라, 따르는 순간 또 죽는 F103 계열이기도 했다.  수리하다 나온 둘째 사각: 자르기를 넣으면 해시 재료가 잘린 글이 되어 **잘려 나간 구역의 변경이 해시를 못 흔든다**(「아까 봤다」로 조용히 통과). 해시는 원본 diff 로 잡았다. 그리고 그 회귀는 픽스처가 추적 파일이면 `index <blob>..<blob>` 줄이 앞 300바이트 안에 들어와 대신 흔들어 주므로 **탐지력 없이 초록**이 된다 — 변이로 실측해 미추적 신규 파일 픽스처로 바꿔 탐지력을 얻었다. | .claude/hooks/git-scope-guard.js 바이트 상한(줄 300B·파일 6,000B) + 해시를 원본 diff 기준으로 + 회귀 2건(tests/git범위가드.test.js — 변이 2종으로 탐지력 실측) · 실측 1,826,444B → 43,416B(42배) |
