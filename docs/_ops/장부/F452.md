| F452 | 2026-08-15 | 실수 | 측정하려고 생산자 Fn 을 부르면 그 호출이 곧 **벤더 유료 경로**다 — 「재는 행위가 값을 치른다」  08-15 `local_47d08bd3` 실측. 강사 반피드백 §8-5 실왕복을 지으며, 「맞음 제출이 AI 교정 분모에서 빠지는가」를 재려고 `POST functions/v1/correct` 를 5회 불렀다. 그 함수는 분모(`대기`)를 응답에 담아 주므로 계기로 쓰기 좋아 보인다 — 그런데 **분모를 센 다음 곧바로 대기 전량을 벤더 배치로 제출한다**(`correct/index.ts:243` 이 세고 `:365` 가 제출한다). 즉 측정 5회 = 제출 시도 5회였다.  그날 실제 피해는 0이었다 — 벤더가 400 으로 튕겨 `회수.배치 0`, 과금 0. **그건 운이지 설계가 아니다.** 제출이 성공했다면 대기 396건이 그대로 나갔다(리허설이라도 돈은 돈이다).  🔑 급소는 「부작용이 있는 함수를 부른 것」이 아니라 **그 함수가 계기처럼 생겼다**는 것이다.    응답에 분모가 들어 있고, 키가 없던 시절에는 실제로 안전한 계기였다(옛 왕복시험 머리말이    「생산자는 ANTHROPIC_API_KEY 를 기다리는 중」이라 적어 두었고, 그 문장이 낡은 줄 몰랐다).    조건이 바뀌자 같은 호출이 조용히 다른 일이 됐다 — **안전이 코드가 아니라 환경에 걸려 있었다.**  ▶ 이번에 쓴 처방: 생산자 **소스에서 `대기조건` 원문을 꺼내 DB 에 그대로 태운다**. 사본이 아닌   이유는 출처가 그 파일 자신이라 따라 바뀌기 때문이고, 값 끼움(`${…}`)이 생기면 조용히 0을   내는 대신 시험이 그 자리에서 죽는다(적색 방향).  ▶ 아직 안 한 것(다음 세션 몫): 이 처방은 **이 시험 하나에만** 섰다. 같은 유혹이 남아 있는 자리를   안 셌다 — 「응답에 계기값이 들었지만 부르면 부작용이 나는 Fn」 목록이 어디에도 없다.   기계로 막으려면 왕복도구가 부르는 Fn 을 걷어 「부작용 있는 통로」와 대조하는 회귀가 필요하다. | |
