| F269 | 2026-08-09 | 실수 | tools/로그인코드발급.js 가 만든 계정은 앱에서 영원히 로그인이 안 된다 — 합성 이메일 규칙이 갈라졌다. lib/로그인코드.js 는 발급코드로 이메일을 만들고(R3PC-YTGB → r3pcytgb@synk.invalid), 앱과 functions/auth 가 쓰는 lib/학생계정.js 는 학생번호로 만든다(SYNK-999 → synk999@synk.invalid). 2026-08-09 실측: 그 도구로 발급한 SYNK-999 로 앱 로그인 통로(/auth/v1/token)를 치니 invalid_credentials. 게다가 auth/index.ts 머리말은 「계정 생성은 여기(first-login) 한 곳에서만 일어난다」고 못박는데 이 도구가 두 번째 통로로 살아 있다. 여파 2건이 같은 날 실측됐다 — ①앱이 못 찾는 GoTrue 고아 계정 ②learners 에 손으로 선 번호가 왕복시험 채번의 max+1 을 끝으로 밀어 죽였다(local_515edda5 가 e05cb1e 로 수리). 개원 때 이 도구로 학생 계정을 만들면 전원이 로그인에 실패한다. | |
