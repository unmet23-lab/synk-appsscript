| F269 | 2026-08-09 | 실수 | tools/로그인코드발급.js 가 만든 계정은 앱에서 영원히 로그인이 안 된다 — 합성 이메일 규칙이 갈라졌다. lib/로그인코드.js 는 발급코드로 이메일을 만들고(R3PC-YTGB → r3pcytgb@synk.invalid), 앱과 functions/auth 가 쓰는 lib/학생계정.js 는 학생번호로 만든다(SYNK-999 → synk999@synk.invalid). 2026-08-09 실측: 그 도구로 발급한 SYNK-999 로 앱 로그인 통로(/auth/v1/token)를 치니 invalid_credentials. 게다가 auth/index.ts 머리말은 「계정 생성은 여기(first-login) 한 곳에서만 일어난다」고 못박는데 이 도구가 두 번째 통로로 살아 있다. 여파 2건이 같은 날 실측됐다 — ①앱이 못 찾는 GoTrue 고아 계정 ②learners 에 손으로 선 번호가 왕복시험 채번의 max+1 을 끝으로 밀어 죽였다(local_515edda5 가 e05cb1e 로 수리). 개원 때 이 도구로 학생 계정을 만들면 전원이 로그인에 실패한다. | 두 번째 계정 생성 통로를 지웠다(talk `a4df5e1`) — 규칙을 맞추지 않았다. 신고문은 「합성 이메일 규칙이 갈라졌다」로 읽히지만 실측하니 두 함수(`로그인코드.이메일`·`학생계정.이메일`)는 규칙이 같고 **먹이는 값이 달랐다**(코드 vs 학생번호) — 같은 입력엔 같은 답이 나온다. 그래서 규칙을 맞추는 처방은 성립하지 않고, 병은 도구가 **계정을 만든다**는 것 자체였다: §4-1 v1.2(유호 확정 08-06)가 아이디를 `student_code` 로 갈면서 계정이 서는 자리는 first-login 하나가 됐는데 이 도구만 옛 판(8자 코드)으로 남았다. 미리 만들면 ①주소가 코드에서 나와 앱이 못 찾고 ②전달할 초기 비밀번호가 생겨 「전달물 0」이 깨지고 ③`auth_user_id` 가 채워지는 순간 §4-1-1 첫 등록 경로가 그 학생에게 영원히 닫힌다. 세 개가 같은 뿌리라 통로를 지웠다. 남은 자리(명부에 학생을 올리는 것)는 `tools/명부등록.js` 가 잇는다 — 계정 0 · `--전화` 필수(뒤 4자리가 첫 등록 게이트의 유일한 대조값이라 비면 그 학생이 잠겨 증상이 F269 와 똑같아진다) · 학생번호를 발급기 규격으로 검사 · 중복 검사를 앱과 같은 방식(`upper(replace(student_code,'-',''))`)으로 접는다. 회귀는 **값이 아니라 호출자**를 못박는다(값 비교는 영원히 초록이라 이 병을 원리상 못 잡는다): `tests/학생계정.test.js` 가 `이메일`·`비밀번호` 를 가져다 쓰는 파일 목록을 고정하고(지금 = `tools/왕복시험.js --코드` 하나), `tests/명부등록.test.js` 가 이 도구의 계정 생성 재발을 본다. 585개 중 584 통과·1 skip(0 fail) · **변이 7/7**. 문서 4자리(L0 §8-2 ②·재발급 문단 · P0 §403·§537 ④)가 「발급 도구가 섰다」를 정본으로 들고 있던 것을 소급 개서 없이 「답이 바뀌었다」로 달았다. 🔴 남은 구멍(F269 밖): 명부에 학생을 올리는 운영 통로가 이 CLI 하나뿐이고 시트↔Supabase 명단 동기화는 0줄이다 — 개원 규모에서는 손으로 한 명씩이다. |
