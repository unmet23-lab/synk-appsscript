| F406 | 2026-08-13 | 마찰 | 대기열 〖지금〗 표식이 4건 중 4건 낡아 「지금 5건」이 실측 「지금 2건」이었다 — 표식은 붙인 시점의 사진이라 남이 집으면(줄36·71) 늙고, 그 사이 다른 세션이 이름이 비슷한 두 심문 통로(codex --심문 vs /fable심문)를 하나로 봐 살아있는 줄50 을 〖⏳유호〗로 접었다. 세션이 표식만 믿고 골랐으면 남이 0분 전까지 만지던 파일을 건드렸을 것이다. | 기계로 막았다 — `9d3df5a6`. 대기열.js 가 세션 시작마다 보드를 **실시간으로 읽어** 점유를 판정한다(사람이 표식을 갱신하는 통로를 없앴다). 신호 셋 전부 「그 세션 자신이 적은 것」: ①씨앗(옛 통로) ②「대기열 P1 줄36」 줄 참조+층 검증 ③파일 자리 ∩ 만지는 파일 칸(판 꼬리 _v5→_v6 를 떼고 본다). 실측 08-13(열린 25줄·보드 활성 17줄): ①3 ②4 ③8 · 합집합 8 — 〖지금〗 2줄이 **둘 다 이미 잡혀 있었는데** ①만으로는 1줄만 잡혔고 나머지 1줄이 곧 이 세션이 겪은 그 거짓 초록(줄50 ← eaa21f3f). ③이 홀로 그 줄을 잡는다. 조용히 지우지 않고 🔒(표식은 지금인데 잡힘 · 누가·어느 신호로)·🔓(표식은 남이점유인데 잡은 줄 없음)로 **드러낸다** — 첫 실행에서 🔓 1건(줄71)을 실제로 적발했다. 경로 뽑기는 `tools/lib/보드.js 파일자리()` 한 곳에 모으고 board-guard 자리들()이 그것을 쓴다(가드는 판 꼬리를 끈다 — v5/v6 를 같은 자리로 보면 정당한 새 판 선언이 막힌다). ⚠ 신고문이 제안한 `작업본소유자.js` 겹치기(주인 생사)는 **일부러 안 넣었다**: board-guard 는 활성 줄이면 주인 생사·커밋 여부를 안 가르고 막으므로(F203), 대기열만 「죽은 주인이니 비었다」로 부르면 그 처방을 따른 세션이 선언 순간 가드에 막힌다(F103 꼴). 죽은 줄의 처방은 그대로 board-move 다. 대가: ③은 「만질 파일」과 「인용한 파일」을 못 가른다 — 인용만 한 줄이 잡힌 것으로 보일 수 있고 방향은 「못 고름」(안전한 쪽 · 대기열.js 보드글자 머리말의 기존 판단 그대로)이며, 지우지 않고 근거와 함께 내므로 읽는 사람이 뒤집을 수 있다. 회귀 `tests/대기열점유.test.js` 13벌(탐지 5·거짓양성 5·실저장소 분모 3) · 변이 5/5 적발 · 보드·대기열 계열 171/171 · board-guard 104/104(순수 리팩터 증명). |
