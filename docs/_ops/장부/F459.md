| F459 | 2026-08-15 | 마찰 | ⏳유호 질문 하나가 두 세션에 «다른 라벨»로 나가서 답이 갈렸다 — 그리고 둘 다 그 답대로 착수했다. 실측 08-15: 같은 「㉠재염색 / ㉡네이티브」 픽에 대해 `local_b6bb5178` 은 「ㄱ 재염색 좋다」를, `local_968b273d` 는 「1.ㄴ」=㉡ 를 받았다(그쪽 커밋 f220bf53 · 내 커밋 56105294). 결과로 전 세트가 두 벌 파생됐고 한쪽은 ~380원이 든다.  🔴 왜 굵직한가 — 갈린 이유가 «유호님의 변심»이 아니라 **라벨링**일 수 있는데 그걸 가릴 재료가 어디에도 없다. 한쪽 답은 방법 이름을 직접 적었고(「재염색」) 다른 쪽은 위치 지시였다(「1.ㄴ」). 위치 지시는 그 세션이 선택지를 «어떤 순서로 냈는지»에 전적으로 기댄다 — 그런데 선택지 문구는 세션마다 각자 조립하고 어디에도 안 남는다. 그래서 나중에 누구도 「ㄴ 이 무엇을 가리켰나」를 복원할 수 없다.  ⚠ 새는 방향 = 「통과」. 두 세션 다 자기 답을 정상적으로 받아 정상적으로 착수했다. 보드도 둘 다 규약대로 새 선언을 썼고 파일 이름도 안 겹쳐(재염색_* / 코랄_*) 어떤 가드도 안 울린다. 충돌은 **커밋 로그를 사람이 읽을 때만** 보인다 — 나도 마무리 단계의 `git log` 에서 우연히 봤다. 늦게 봤으면 두 벌이 다 서고서야 알았을 것이다.  🔑 급소는 「같은 질문에 답이 둘」이 아니라 **답이 어느 질문의 어느 선택지였는지 되짚을 자리가 0** 이라는 것이다. decision-queue 는 질문을 배달하지만 «내가 유호님께 보여준 선택지 문구»는 보관하지 않는다.  ▶ 처방 후보(이번 세션 «안» 함 — 다른 트랙): ①⏳ 항목을 유호님께 낼 때 선택지에 **번호가 아니라 이름**을 쓴다(「ㄱ 재염색 / ㄴ 네이티브」처럼 낱말을 붙여, 위치가 아니라 뜻으로 답이 오게) ②답이 들어오면 그 항목을 장부·메모리에서 **닫힌 것으로 표시**해 다른 세션이 같은 질문을 다시 배달하지 않게 한다 ③decision-queue 에 「이 질문은 이미 답이 왔다」 상태 칸. ⚠대가: ②③은 유호님이 «마음을 바꿔» 다시 답하는 정상 경로까지 「중복」으로 읽을 수 있다 — 닫힘은 배달을 막는 게 아니라 **다른 답이 이미 있다고 드러내는** 쪽이어야 한다.  이번엔 갈림을 유호님께 올리고, 두 산출물은 둘 다 보존했다(파일명이 안 겹쳐 덮어쓰기 위험 0 · ㉠↔㉡ 실제 색차는 ΔE 0.49 라 어느 쪽이 남아도 색은 같다). | |
