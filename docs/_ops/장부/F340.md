| F340 | 2026-08-12 | 마찰 | 🎫 인계 일감에 «집었다»를 못 박는 자리가 없어 두 세션이 같은 것을 끝까지 나란히 했다 — 20~32분짜리 이종 검수가 두 번 탔다.  2026-08-12 실측. 9a710cd8 이 마감하며 「다음 세션이 --commit de6b34d5 재검수부터」로 🎫 를 놓았다. a0d3c91a(나)와 52a7abc6 이 몇 분 안에 각각 그 🎫 를 읽고, 각자 보드에 «거의 같은 문장»으로 선언하고, 각자 codex 재검수를 완주했다(둘 다 「2벌 0건」 — 같은 답을 두 번 샀다). 52a7abc6 이 먼저 배포까지 끝냈고, 나는 배포 직전 `bump-version --check` 가 낸 [vNEXT] 차단으로 «그제서야» 충돌을 알았다.  왜 안 보였나 — 대기열이 시키는 대조(`node tools/board.js`)는 읽기-그리고-쓰기 사이에 잠금이 없다. 둘 다 규약대로 「먼저 보드를 훑고」 선언했는데, 그 사이가 몇 분이라 서로가 서로를 못 봤다. 보드 파일은 선언 시점엔 대개 미커밋이라 `git log` 로도 안 보인다(F161 과 같은 자리, 다른 증상). 게다가 두 줄의 «트랙 제목이 사실상 동일»했는데도 board-guard 는 칸 길이·줄 수만 보지 제목 충돌을 안 본다.  비용은 검수 2배가 아니라 그 이상이다 — 나는 CI 모사 2회(각 5분)·라이브 대조 3회·배포 준비 전량을 더 태웠다. 그리고 위험한 쪽: 내가 충돌을 모른 채 clasp push 를 눌렀다면, 그 순간 남이 쓰고 있던 미커밋 (엔진_운영배치.js 의 [vNEXT] 명부스윕 + Code.js v9.215 채번)이 «반쯤 짜인 채» 라이브로 나갔다 (clasp 는 HEAD 가 아니라 작업본을 민다 · F309·F310). 막은 것은 규약 기억이 아니라 기계 둘이었다 — `bump-version --check` 의 [vNEXT] 차단과 clasp-guard 의 배포집합 미커밋 검사.  처방 후보(고르는 것은 유호님·도구 트랙 예산 결정 대기 중): board-guard 가 새 선언의 트랙 제목을 기존 활성 줄과 대조해 «겹치면 선언을 막고 그 세션 지문을 보여주는» 자리. 제목이 같을 때만 막으므로 거짓양성이 싸고, 막는 방향이 「나중에 온 쪽이 비켜난다」라 F073 과도 어긋나지 않는다. | board-guard ⑧ — 선언 순간 트랙 제목 겹침 차단(산 세션이면 deny·죽었으면 알림) + 회귀 3(신고 원문 두 제목 그대로 픽스처화) · 0f3cd515 |
