| F548 | 2026-08-17 | 실수 | 재심 배선 ㉠(모델 변경)이 «판정하는 자리에서 관측까지» 하려다 원리상 발동 0이었다 — 그 자리엔 값이 존재할 수 없다. 모델은 전사의 `message.usage` 줄에서 나오는데(lib/context-size.js) 그 줄은 어시스턴트가 한 턴을 마쳐야 찍힌다. 그런데 읽는 쪽인 대기열.js 는 SessionStart(matcher `startup|clear`)에만 등록돼 있어 늘 그 앞에서 돈다. 실측: 전사 12벌의 첫 usage 줄이 16~19번째(없는 것 2벌) · 장부 `docs/_ops/모델관측.jsonl` 은 PR #43 착지 이후 **파일 자체가 없다**(행 0). 그래서 `모델있음`이 영영 false → `적을줄` 영영 null → 장부가 빈 채 → `마지막관측` 영영 null → 모델바뀜 영영 null. F547(작동대장 `자동기록()` 호출 미검사)과 같은 병의 3번째다. 🔴 더 나쁜 것은 이 장치가 **정직하게** 「못 쟀다」를 낸다는 점이다 — 그 문구가 영구 상태인데 「곧 재면 된다」로 읽힌다(CLAUDE.md 맹점 ④ = 맞는 얼굴로 틀린 값). 회귀 17건이 전부 초록이었던 이유도 같다: 배선 검사의 전사 픽스처가 usage 줄을 **이미 갖고** 있어, 「통로가 도나」만 증명하고 「그 시점에 값이 있나」는 아무도 안 물었다. | 관측(쓰기)을 Stop 으로, 판정(읽기)을 SessionStart 로 갈랐다 — context-budget.js 가 모델을 적고 재심신호.판정()이 장부를 읽는다(장부바뀜: 재심 줄이 끄고, 중복 관측에도 안 증발). 회귀 신설 + 배선 검사 2건(실제 훅을 자식 프로세스로 돌려 장부 줄을 확인 · SessionStart 모양 전사에선 모델이 null 임을 못박음) · 변이 8/8 구멍 0 |
