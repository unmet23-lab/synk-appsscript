| F276 | 2026-08-09 | 마찰 | 배포본은 Deno 가 타입을 지운 판이라 `index.ts` 원문 전량 대조는 **방금 배포한 판도** 「다르다」로 낸다 — F274 처방이 왕복시험 5종을 영구 차단한다. **조건을 하나만 바꿔 갈랐다**: 리허설 `events` 를 HEAD 그대로 재배포(**v23**)한 직후에도 `배포대조` 가 그대로 「배포본에 없는 판: index.ts」다 — 「옛 판이 배포됐다」가 아니라 원리상 안 맞는다. 배포본 그 자리를 열면 `const { 헤더읽기, 길이초, 파일없음 } = 헤더모듈 as {…};` 에서 **`as {…}` 블록이 통째로 없고**, 반면 주석·`1_000_000`(숫자 구분자)·문자열 리터럴은 **원문 그대로 살아 있다**. 그래서 순수 JS 인 `.mjs` 만 재던 F274 이전 판은 초록이었고, 본체를 넣은 순간 **7종 전부** 「다르다」가 됐다(`auth` 만 `로그인코드.mjs` 가 더 붙는데 그건 진짜 뒤처짐이다). 급소는 회귀다 — `tests/배포대조.test.js` 의 `배포본흉내` 픽스처가 **타입이 하나도 없는 순수 JS**라 이 형태를 원리상 못 잡고, 그래서 F274 수리가 초록으로 통과했다. 처방 후보(주인 = F274 세션 `26b47e25`): 주석은 변환에 살아남으므로 `배포묶음` 이 `.ts` 에 **내용 전량의 해시**를 주석 한 줄로 실어 보내고 대조는 그 줄을 찾는다 — 표본추출로 퇴화하지 않는다(② 회귀가 못박은 자리). 픽스처에 타입 제거를 넣어 탐지력을 픽스처가 지게 한다. 곁들여 리허설 `events` 는 **실제로도** F273 반쪽이었다 — 그 v23 으로 `헤더읽기` 가 이제 실렸다. | |
