| F490 | 2026-08-15 | 실수 | 회귀가 「복제 ≤2」로 한 칸을 봐 줬는데, 사고는 정확히 그 봐 준 칸에서 났다 — tools/게이트.js 의 규칙 몸통이 세우기(상수 통째)와 바꾸기(gh 인자 손조립) 두 곳에 적혀 있었고, PUT 쪽이 exclude 를 빠뜨려 --풀기 가 422 로 죽어 있었다. 새는 방향이 「세우는 길은 살고 여는 길만 죽는다」라 잠금은 서고 장부도 화면도 초록인데 막혔을 때 열 열쇠만 없는 상태였다(문보다 열쇠를 먼저 짓는다는 이 도구의 존재 이유가 통째로 무효). 처방=몸통 조립을 요청몸통() 한 곳으로 + 회귀 ⑥ 을 ≤1 로 조이고 손조립 문자열 자체를 금지(⑩). | `e20ff91c` — 몸통 조립을 `tools/게이트.js:110 요청몸통()` **한 곳**으로 모았다(세우기·바꾸기 둘 다 여기서 파생). 회귀는 ⑥ 을 `<=2`→`<=1` 로 조이고(복제를 한 칸 봐 주면 갈라짐도 한 칸 봐 주는 것이다 — 실제 사고가 그 «허용된 2번째»에서 났다) ⑩ 을 신설해 **손조립 문자열 자체**를 금지했다(주석은 벗기고 본다 — 사고를 적으면 깨지는 검사는 다음 사람에게 「기록하지 마라」를 가르친다). ⑨ 는 «`exclude` 가 몸통에 살아 있나»를 두 목표(active·disabled) 각각에 대고 본다. 🔑 새는 방향이 「세우는 길은 살고 여는 길만 죽는다」라 잠금도 장부도 화면도 초록인 채 **막혔을 때 열 열쇠만** 없었다. 탐지력 실증 = **변이 3/3**(구멍 0·미적용 0 · `tools/변이.js` 통로): ①`exclude` 를 도로 빼면 ⑨ ②손조립을 되넣으면 ⑩ ③규칙 이름을 두 곳에 적으면 ⑥ 이 각각 빨개진다. 회귀 10/10. F092 대조 = `conditions[` 손조립은 저장소 어디에도 안 남았다. ⚠ 닫힌 것은 **08-15 이고 장부는 08-16 까지 열려 있었다** — 고친 커밋이 번호를 달았는데도(`friction-close-guard` 가 짖는 조건) 칸이 비었다. |
