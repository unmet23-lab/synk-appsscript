| F293 | 2026-08-09 | 마찰 | 대기열이 「상시공유」 목록에 없어, 새로 단 낡음 알림의 처방(「그 층을 갱신해 담는다」)이 실제로는 막힌다. 실측 2026-08-09: `docs/_ops/작업대기열.md` 는 상시 여러 세션이 자기 줄을 넣고 지우는 공유 선언판인데(이 시각 미커밋 보유 세션 5개 — dc40c8b2·4f05894e·97f86f0e·08deceb4·8192260e), git-scope-guard ⑧ 의 `상시공유` 목록은 `docs/세션보드.md`·`docs/_ops/마찰신호.md` 둘뿐이다. 그래서 대기열만 담으려 해도 「남의 미커밋이 섞인다」로 차단되고, deny 문의 두 갈래가 F229 때와 똑같이 둘 다 실행 불가능하다 — ①「위가 전부 내가 만든 것이면 다시 실행」은 남의 줄이 늘 섞여 영원히 거짓이고 ②「그 파일을 범위에서 빼라」는 대기열이 유일 파일이라 뺄 것이 없다. 3회차 탈출구가 있어 「불가」는 아니지만 F229 가 실측한 대로 규칙을 지키려는 쪽일수록 2회차에서 멈춘다. 새는 방향이 나쁘다 — 막히면 대기열 줄이 안 담기고, 그게 정확히 F289 가 낳은 낡음이다(장치를 달아 놓고 그 처방을 다른 가드가 막는 형태 · F103). ⚠ 단 보드·장부와 다른 점 하나를 같이 봐야 한다: 대기열은 **줄을 지우는 것이 정상 동작**이라(「끝나면 그 줄을 지우고 커밋」) ⑨(남의 줄 삭제)가 정상 조작에 발동한다. 그래서 목록에 한 줄 더하는 것으로 끝나지 않을 수 있고, 그 판단은 그 가드를 트랙으로 잡은 세션 몫이다. 처방 후보 ①`상시공유` 에 대기열을 넣고 ⑨ 가 대기열에서 무엇을 지켜야 하는지 따로 정한다 ②대기열을 층별 파일로 쪼갠다(F250 이 보드에 한 것 — 단 대기열은 순서가 뜻이라 표 조립 규칙이 더 필요하다) ③세션이 자기 줄만 담는 전용 통로를 board-move 무늬로 만든다. | |
