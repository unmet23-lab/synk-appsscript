| F631 | 2026-08-18 | 마찰 | 두 세션이 «같은 대기열 줄»을 19분 차로 각자 선언했고, 겹침 가드가 안 울렸다 — 둘 다 끝까지 갔다  2026-08-18 실측. #Q115 를 두 세션이 나란히 했다.   · 06:29:29Z `3dd52c55` 선언 (`e8e04858`) — 제목 「#Q115 「기계에 따라 답이 갈리는 검사」를     스위트 전수로 센다 — F617 의 셋은 «분모 없이 떨어진 것»이다」   · 06:48:44Z `197f1fe3` 선언 (`3480d706`) — 제목 거의 **글자까지 같다**(둘 다 큐 줄 제목을 그대로 옮겼다) 그리고 둘 다 ①축 토글 계수기를 짓고 ②**같은 시한폭탄**(`tests/검수런한도.test.js` 의 로컬 파싱 절대 시각)을 독립으로 찾아 ③각자 고쳤다. 그쪽이 먼저 머지했고(PR #129), 내 중복분은 접었다.  🔑 낭비는 「누가 잘못했나」가 아니라 **장치가 안 울렸다**는 데 있다:   ㉠ `board-guard` 의 트랙 겹침 검사는 「남의 **활성** 줄과 제목이 겹치나」를 보는데,      생사를 못 재면(`산지문` 이 비면) **막지 않고 통과**한다 — 클라우드 세션은 박동 재료가      없어서 늘 그 갈래로 떨어진다. 즉 이 환경에서는 그 가드가 원리상 안 운다.   ㉡ 대기열 줄에는 「누가 잡았나」 칸이 없다. `대기열.js` 는 보드를 훑어 «이미 잡힌 줄»을 내주는데,      그건 **고를 때 한 번** 보는 사진이라, 19분 뒤에 뜬 세션은 그 사이 갱신을 못 본다.   ㉢ 나는 선언 커밋을 **바로 밀었다**(06:29). 그쪽이 06:48 에 fetch 했다면 봤을 것이고,      안 봤다면 그건 「fetch 안 함」이 아니라 «볼 자리가 화면에 없었다»에 가깝다 —      SessionStart 의 대기열 카드가 「지금 집을 수 있는 줄」을 낼 때 **남의 보드까지 대조**하지만,      그 카드는 세션 «시작»에 한 번 뜨고 유호 지시로 트랙을 바꾸면 다시 안 뜬다.  ▶ 처방 후보(안 골랐다 — 재는 자리부터 정해야 한다):   ㉠ 큐 줄에 「점유」 칸을 두고 `--집기` 가 그 자리에 지문·시각을 **커밋으로** 박는다      (보드가 아니라 큐가 자물쇠가 된다 · 지금 큐는 읽기만 한다)   ㉡ 겹침 가드가 생사를 못 잴 때 **통과 대신 경고**로 내려간다(지금은 조용히 통과)   ㉢ 아무것도 안 한다 — 병행이 드물면 대가가 처방보다 크다. 단 그 판단의 재료가 지금 0이다      (「오늘 몇 건이 겹쳤나」를 아무도 안 센다).  ⚠ 이 신고는 «중복이 나쁘다»가 아니다. 두 판을 견줘 보니 그쪽 시한폭탄 수리가 내 것보다 나았고   (마커 파일의 `리셋시각` 을 ISO 로 직접 고쳐 달 이름·서수·로케일 왕복이 없다), 내 쪽은   「기준을 한 번만 뜨면 기준의 flaky 가 모든 축의 뒤집힘으로 샌다」를 잡아 그쪽 도구에 얹었다.   즉 병행이 «값»을 낳기도 한다 — 재야 할 것은 그 값과 낭비의 비다. | |
