| F631 | 2026-08-18 | 마찰 | 두 세션이 «같은 대기열 줄»을 19분 차로 각자 선언했고, 겹침 가드가 안 울렸다 — 둘 다 끝까지 갔다  2026-08-18 실측. #Q115 를 두 세션이 나란히 했다.   · 06:29:29Z `3dd52c55` 선언 (`e8e04858`) — 제목 「#Q115 「기계에 따라 답이 갈리는 검사」를     스위트 전수로 센다 — F617 의 셋은 «분모 없이 떨어진 것»이다」   · 06:48:44Z `197f1fe3` 선언 (`3480d706`) — 제목 거의 **글자까지 같다**(둘 다 큐 줄 제목을 그대로 옮겼다) 그리고 둘 다 ①축 토글 계수기를 짓고 ②**같은 시한폭탄**(`tests/검수런한도.test.js` 의 로컬 파싱 절대 시각)을 독립으로 찾아 ③각자 고쳤다. 그쪽이 먼저 머지했고(PR #129), 내 중복분은 접었다.  🔑 낭비는 「누가 잘못했나」가 아니라 **장치가 안 울렸다**는 데 있다:   ㉠ `board-guard` 의 트랙 겹침 검사는 「남의 **활성** 줄과 제목이 겹치나」를 보는데,      생사를 못 재면(`산지문` 이 비면) **막지 않고 통과**한다 — 클라우드 세션은 박동 재료가      없어서 늘 그 갈래로 떨어진다. 즉 이 환경에서는 그 가드가 원리상 안 운다.   ㉡ 대기열 줄에는 「누가 잡았나」 칸이 없다. `대기열.js` 는 보드를 훑어 «이미 잡힌 줄»을 내주는데,      그건 **고를 때 한 번** 보는 사진이라, 19분 뒤에 뜬 세션은 그 사이 갱신을 못 본다.   ㉢ 나는 선언 커밋을 **바로 밀었다**(06:29). 그쪽이 06:48 에 fetch 했다면 봤을 것이고,      안 봤다면 그건 「fetch 안 함」이 아니라 «볼 자리가 화면에 없었다»에 가깝다 —      SessionStart 의 대기열 카드가 「지금 집을 수 있는 줄」을 낼 때 **남의 보드까지 대조**하지만,      그 카드는 세션 «시작»에 한 번 뜨고 유호 지시로 트랙을 바꾸면 다시 안 뜬다.  ▶ 처방 후보(안 골랐다 — 재는 자리부터 정해야 한다):   ㉠ 큐 줄에 「점유」 칸을 두고 `--집기` 가 그 자리에 지문·시각을 **커밋으로** 박는다      (보드가 아니라 큐가 자물쇠가 된다 · 지금 큐는 읽기만 한다)   ㉡ 겹침 가드가 생사를 못 잴 때 **통과 대신 경고**로 내려간다(지금은 조용히 통과)   ㉢ 아무것도 안 한다 — 병행이 드물면 대가가 처방보다 크다. 단 그 판단의 재료가 지금 0이다      (「오늘 몇 건이 겹쳤나」를 아무도 안 센다).  ⚠ 이 신고는 «중복이 나쁘다»가 아니다. 두 판을 견줘 보니 그쪽 시한폭탄 수리가 내 것보다 나았고   (마커 파일의 `리셋시각` 을 ISO 로 직접 고쳐 달 이름·서수·로케일 왕복이 없다), 내 쪽은   「기준을 한 번만 뜨면 기준의 flaky 가 모든 축의 뒤집힘으로 샌다」를 잡아 그쪽 도구에 얹었다.   즉 병행이 «값»을 낳기도 한다 — 재야 할 것은 그 값과 낭비의 비다. | 🔴 **내가 적은 원인이 틀렸다 — 실측으로 정정한다.** 신고문 ㉠은 「겹침 가드는 생사를 못 재면 조용히 통과하고, 클라우드 세션은 박동 재료가 없어 늘 그 갈래로 떨어진다」였다. 그 자리에서 재 보니 `생사쟀나` 는 **true** 였다(`산지문` = Set(1) = 내 지문 · 내지문 정상). 즉 가드는 생사를 쟀고, 그 갈래는 이 사고와 무관하다. — 진짜 구멍은 한 칸 앞이다: **`board-guard` 의 `남의줄` 은 «작업본»의 보드 파일을 읽는다**(`보드.줄들(보드ROOT)`). 19분 전 origin 에 올라간 남의 선언은 내 체크아웃이 그것을 안 받았으면 **원리상 안 보인다.** 겹침 0 이 나온 게 아니라 **볼 것이 없었다.** 그리고 그 둘은 화면에서 **같은 모양**이다 — 「조용함」. 이 저장소가 반복해 당하는 그 형태(통과와 미실행이 같은 모양)다. ▶ **처방(착지)**: 판정을 못 고치더라도 **판정이 무엇을 못 봤는지는 말한다.** `board-guard` 의 트랙충돌이 표의 데이터 줄이 바뀌는 편집에서 `repo-staleness.js` 의 `재다()`(통로 하나 · `fetch:false` · 실측 23ms)를 불러 세 갈래를 **다르게** 낸다: ①origin 을 못 읽음 = 「낡았는지조차 모른다」 ②뒤처졌는데 보드 변경은 못 잼 ③뒤처짐 N · **보드 M번 바뀜** → 「이 판정은 그 선언들을 못 봤다」 + `git fetch origin master && git merge origin/master` 처방. **막지 않는다**(뒤처짐은 차단 사유가 아니다 · F192 와 같은 규율). ⚠ 대가: `fetch:false` 라 **마지막으로 fetch 한 origin ref 기준**이고 숫자가 «작게» 나올 수 있다 — 그래서 0 을 「최신」이라 단정하지 않고 세 갈래로 항상 가른다. ref 갱신은 같은 매처의 `repo-staleness` 가 스로틀 걸고 따로 한다. 회귀 `tests/board-guard.test.js` 4건(적색 1 + 거짓양성 3) · **변이 6/6 구멍 0**. 🔑 짓는 도중 **발화 조건에 안 실어** 장치가 안 도는 판을 한 번 만들었고(출력 게이트의 `||` 목록), 회귀가 그 자리에서 잡았다 — 「장치와 그 발동 조건은 같은 커밋에서 정한다」의 실물이라 그 주석을 게이트 옆에 남겼다. 🔑 변이 ④ 가 **내 회귀의 구멍**을 뚫었다: 「최신인데도 울린다」를 아무 검사도 안 잡아서(소음 방향이 통째로 안 재짐) 거짓양성 검사를 하나 더 세웠다. ⚠ **남은 것**: 이 처방은 「말한다」까지다. 큐 줄에 점유 칸을 두는 ㉠은 안 골랐다 — 「오늘 몇 건이 겹쳤나」를 아무도 안 세고 있어 그 판단의 재료가 0이다. |
