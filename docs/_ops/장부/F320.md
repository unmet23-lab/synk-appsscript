| F320 | 2026-08-10 | 마찰 | 발표물 넘침 검사가 같은 파일에서 회차마다 초록·적색으로 갈렸다 — 활자가 앉기 전(window.load)에 재서 폴백 활자 폭으로 조판된 판정이 나왔다. 실측 02_학부모_안내문_A4_kr 6회: 잰쪽 2·몸통 157 은 매 회차 동일한데 넘침만 0px↔18px(=한 줄) 로 3/3 갈렸다. 이 흔들림이 세션 둘을 연달아 잡아먹었다 — 286c22f7 은 초록 회차를 보고 「적색 해소·회귀 20/20」으로 적었고, 이어받은 f12f3c7d 는 적색 회차를 보고 「18px, 보드엔 초록으로 적혀 있었다」로 적었다. 둘 다 정직했고 둘 다 옳았다. 그 사이 유호 지시 「커밋으로 보호」가 두 번 다 커밋 직전에 멈췄다. 곁들여 도구 머리말이 시키는 명령(--넘침 docs/발표물/*.html)이 활자 미주입 원본 _src_ 까지 재서 자기 처방이 자기 거짓양성을 부르고 있었다. | tools/발표물린트.js — document.fonts.ready 를 기다리고 setTimeout 한 턴 넘겨 잰다(rAF 는 --virtual-time-budget 아래서 안 돌아 8회 중 6회가 죽었다). 측정기가 활자 상태를 함께 내고, 안 앉았으면 결과를 아예 안 낸다(조용한 초록 대신 시끄러운 실패). + 활자 미주입 원본은 「대상 아님」으로 가른다. + 마커·주입 판정 3중 복제를 tools/lib/활자주입.js 한 곳으로. 급소: 그 갈래는 주입됐나의 부정이 아니다 — 합치면 @font-face 없는 문서까지 원본으로 삼켜 넘침 탐지가 통째로 꺼진다(실제로 그렇게 짰다가 탐지 회귀 2건이 조용히 통과하는 것을 잡았다). 회귀 3(tests/발표물린트.test.js): 활자 loaded 여야 잰다 · 마커만 얹으면 대상 아님·마커만 떼면 같은 픽스처를 다시 잡는다 · 두 갈래 합치기 금지. 커밋 c0e08190. 실측 02_kr 13/13 결정적 초록·_src_02 5/5 적색 유지. |
