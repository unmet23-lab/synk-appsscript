| F538 | 2026-08-17 | 마찰 | 엔진도달 불변식③④ 의 순환 배제가 basename 이라, 소비자를 `.../index.ts` 로 적으면 자동층 전체가 눈에서 사라진다 — 무관한 소비자 둘까지 적색으로 나왔다 (talk `tests/이벤트검증.test.js:625` 안불리는소비자). 검사는 「소비자끼리의 참조는 근거가 못 된다」를 basename 집합으로 거르는데(`!소비자이름.has(이름(f))`), 장부에 `supabase/functions/teach/index.ts` 를 소비자로 적자 그 집합에 `index` 가 들어가 `functions/*/index.ts` 전량이 「부르는 자리」에서 제외됐다. 결과: 내가 안 건드린 `lib/학습자상태.js`·`lib/성과회수.js` 까지 「자동으로 도는 자리에서 안 불린다」로 빨개졌고, 처방 문구는 그 둘에 대해 따를 수 없는 것이었다(이미 동봉·import 돼 있다 · F103 계열). 2026-08-17 실측 — 기준선을 얼린 워크트리에서 51/51 초록을 확인해 거짓 적색임을 가렸다. 🔑 새는 방향은 「통과」가 아니라 「엉뚱한 적색」이지만, 그 적색이 **다른 트랙의 소비자**를 가리켜 원인 추적을 한 겹 늘린다. 처방 후보 = 경로 전체로 비교하거나(basename 대신), 소비자 지목을 `lib/` 로 제한해 index 계열이 애초에 못 들어오게 한다(후자가 설계 의도에 맞다 — 소비자는 lib 이어야 자동층에서 불릴 수 있다). | |
