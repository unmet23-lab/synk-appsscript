| F433 | 2026-08-14 | 마찰 | **죽은 세션이 남긴 board-move 반쪽(F226 잔재)이 «모든» 세션의 보드 정리를 잠근다 — 그리고 거절문이 내미는 처방은 원리상 못 따른다.** `board-move` 원칙 ⑤ 는 아카이브가 더러우면 아무것도 쓰지 않고 멈추는데, 그 거절문은 「**그 세션이 커밋한 뒤 다시 돌려라**」라고만 말한다. 주인이 죽었으면 그 문장은 영원히 실행 불가다 — F103 그 모양(따를 수 없는 처방은 우회를 정상 통로로 만든다). 🔴 **하루에 두 번 밟았다**(2026-08-14 · `local_0a83854d` 실측): ① `8981e74f`(#Q75) 의 잔재가 메인 아카이브에 미커밋으로 앉아 유호님이 지시한 「죽은 주인 5줄 처분」의 첫 삽부터 막았다 ② 그 다섯 중 넷을 옮긴 뒤, 이번엔 방금 죽은 `efd4bb2a`(릴스 4종) 의 잔재가 같은 자리에 앉아 마지막 한 줄을 또 막았다. 즉 세션 14~15개가 상시 도는 이 저장소에서 이 잠금은 예외가 아니라 **상시**고, 잠기는 대상은 신고자 한 명이 아니라 **보드 정리 전체**다(만석 19/18 이 안 풀린다 = board-guard 가 새 선언을 계속 막는다). 🔑 **출구는 있는데 처방에 안 적혀 있다** — 잔재로 남은 «그 줄»을 needle 로 board-move 를 한 번 더 돌리면 `내잔재뿐()`(HEAD 대비 차분 = 그 줄 하나·삭제 0)이 참이 되어 F226 경로로 이어서 커밋한다. 실제로 두 번 다 그렇게 풀었다(`ea07c75f` · 릴스분). 이것을 아는 사람만 풀 수 있고, 모르면 「남의 미커밋이니 기다린다」로 읽고 영원히 기다린다. 처방 = 거절문이 **죽음 여부를 재서** 갈라 말하게 한다: 잔재 줄의 주인이 `선언살았나` 로 죽었으면 「그 줄부터 `node tools/board-move.js "<그 줄 문구>"` 로 마저 옮겨라(F226 경로)」를 그 자리에 찍는다. 산 주인이면 지금 문구 그대로. ⚠ 회귀는 픽스처로 — 실저장소 잔재를 요구하는 시험은 F296(repo 밖 상태 의존)이다. | `3783b592` — board-move 원칙 ⑤ 거절문이 **생사를 재서 갈라 말한다**(신고 처방 그대로). 잔재 줄의 주인이 `선언살았나` 로 죽었으면 그 자리에 `node tools/board-move.js "<그 줄 문구>"` 를 찍는다(F226 경로 — 되먹이면 `내잔재뿐()` 이 참이 되어 이어서 커밋한다). 산 주인이면 옛 문구 그대로다. 곁들여 잔재 목록에서 **내 줄은 뺀다** — 지금 막힌 그 명령을 다시 돌리라는 말은 처방이 아니고 진짜 과녁을 가린다. 같은 판정을 두 번 안 적으려고 `산주인` IIFE 를 `산주인들(대상행, 파일지문칸)` 공용 함수로 올렸고, 아카이브엔 세션별 파일 좌표가 없으니 `''` 를 준다. 🔑 이 처방은 **어떤 게이트도 안 연다** — 가리키는 명령이 이 도구 자신이라 원칙 ⑥·⑦·F102 를 처음부터 다시 지난다(늘어난 것은 출구의 «가시성»뿐). 회귀 4(픽스처 · F296 — 실저장소 잔재 안 요구) · 변이 3: M1 생사 안 재고 늘 죽음 처방 → 1 빨감 · M2 내 줄 제외 삭제 → 1 빨감 · M3 **신고 원문 그대로 재현 → 3 빨감**(F083). ⚠ **첫 M2 는 안 물렸다** — 그 픽스처의 아카이브 차분엔 내 줄이 애초에 없어(내 줄은 보드에만 있다) 단언이 원리상 못 실패했다. 둘 다 차분에 올라오는 픽스처(F226 내 잔재 + 남의 삽입)를 새로 지어 다시 물렸다. ⚠ **못 재는 자리 1**: `파일지문칸` 인자는 변이를 심어도 안 빨개진다 — 원칙 ⑥ 이 먼저 돌아 두 인자가 갈리는 조합이 원리상 도달 불가다(주석에 적었다). 대가: 잔재 줄에 재료(지문·해시)가 0이면 산 주인에게도 F226 문구가 나가지만, 그 명령이 게이트에 다시 걸려 최악이 「한 번 더 거절당한다」다. F092 grep 실측 = 같은 꼴의 못 따를 처방은 master 에 더 없다(`:354` 는 산 주인일 때만 도니 따를 수 있다). 보드이관 54/54 · 보드 4벌 207/207 · board-move 소비자 8벌 226/226. |
