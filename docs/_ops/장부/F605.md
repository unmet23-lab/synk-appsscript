| F605 | 2026-08-18 | 마찰 | CLAUDE.md 가 지목한 장치가 그 대상을 **안 잰다** — 「append-only 장부(마찰신호.md·심문기록·결정록 류 jsonl)는 행 삭제 금지 — tests/장부유실.test.js 가 fail 로 잡는다」인데, 그 파일의 과녁은 두 경로뿐이다: docs/_ops/마찰신호.md(:64) · docs/_ops/장부(:65). jsonl 은 **한 줄도 안 본다**(실측 2026-08-18 local_a07b89a1 · F586 트랙을 닫다가 인접 확인). ■ 왜 신호인가 — 상주 텍스트가 «없는 보호»를 있다고 말한다. CLAUDE.md 는 매 턴 상주하므로 이 문장은 모든 세션이 읽는다. jsonl 행을 지우려는 세션이 「기계가 막아 줄 것」으로 읽고 지나가면, 그 행은 유실 회귀 어디에도 안 걸린다. 새는 방향이 「통과」다(맹점 ④ — 맞는 얼굴로 틀린 값). 그리고 이 문장은 **검증 없이 신뢰를 만든다**: F207 이 경고한 「분모를 안 밝힌 초록」의 산문판이다. ■ 안 잰 것을 명시한다(F348) — **다른 장치가 그 jsonl 들을 지키는지는 안 쟀다.** 부정 판정(「무보호다」)을 쓰려면 값이 살 수 있는 층을 전부 열거하고 열어야 하는데, 여기서 연 것은 tests/장부유실.test.js 하나와 tests/*.test.js 의 jsonl 낱말 grep(8벌 히트 · 내용 미확인)뿐이다. 즉 확정된 것은 **「그 문장이 지목한 파일이 그것을 안 잰다」** 하나이고, 「아무도 안 지킨다」가 아니다. ■ 처방 후보 — 안 지었다(이 신고는 재료다) ㉠ 먼저 **분모부터** 잰다: append-only 로 다뤄야 할 jsonl 이 몇 벌이고(심문기록·심문기록_fable·검수기록·검수기각·검수제안·게이트장부·모델관측·엔진점수…) 그중 몇 벌이 실제로 어떤 회귀에 걸려 있나. 세기 전에 장치를 지으면 어디를 겨눌지 모르는 장치가 된다(F526 ㉠ 이 같은 순서를 못박았다). ㉡ 문장을 사실에 맞춘다 — 가장 싸고 지금 당장 참이 되는 길이다. 단 그러면 jsonl 은 「기계 0 · 규율만」으로 드러나므로, 그 드러남 자체가 ㉠ 의 착수 근거가 된다. ㉢ 유실 회귀의 과녁을 jsonl 로 넓힌다. ⚠대가 = 그 파일들은 «행 단위 ID» 가 없어 마찰 장부처럼 번호로 못 잰다. 재는 축을 새로 정해야 하고(행 수 단조 증가? 해시 사슬?), 정상 편집(회전·압축)과 유실을 가르는 규칙이 필요하다 — F586 ㉢ 이 같은 이유로 기각됐던 축이다. 🔑 F586 과의 관계: 저기는 「번호가 살아 있는 채 행이 사라진다」였고 이건 「그 검사가 애초에 그 파일을 안 본다」다 — 둘 다 «분모»가 병이지만 층이 다르다. | 막는 층·재는 층을 한 벌로 세워 정본 문장이 참이 됐다 (7ea7ddcc · PR #105). 분모부터 쟀다(㉠) — jsonl 12벌·325레코드, 값이 살 수 있는 층 다섯(tests·hooks·gitattributes·workflows·tools)을 전부 열어 지키는 층 0을 확정했고 이력 삭제줄도 0이었다(사고 전이다). 행위로 잰 급소: 두 가지가 각자 append 하면 지정 없음=충돌 exit 1·표식 3줄(하나 고르기로 풀면 증발)·union=충돌 0·두 행 생존. ㉠막는 층=.gitattributes 의 docs/_ops/*.jsonl merge=union ㉡재는 층=장부유실.test.js 에 jsonl 과녁 8건. 신고의 ㉢ 대가(「행 단위 ID 가 없어 번호로 못 잰다」)는 실측으로 틀렸다 — 키는 번호가 아니라 «줄 전문»이고 append-only 라는 성질 자체가 축이다. 머지 커밋을 log -p 가 안 펼쳐도 사라진 줄은 부모 커밋에서 + 로 올라 있어 걸린다. ㉡ 안 함(CLAUDE.md 문구 수정) — ㉡이 서면서 문장이 저절로 참이 돼 정본을 안 만졌다. 회귀 20→28 · 변이 7/7 구멍0 미적용0 · CI 4478 fail 0. |
