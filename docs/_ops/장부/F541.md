| F541 | 2026-08-17 | 실수 | ①배포 검수의 `--기각 <키> --사유` 가 «다른 지적의 사유»로 기각되는 것을 못 막는다 — 진짜 P1 하나가 조용히 닫히고, 정작 틀린 지적은 게이트에 그대로 남았다  실측 (2026-08-17 · `docs/_ops/검수기각.jsonl` 마지막 행 · 검수 `--base 408b650d` #fp:192c099a):  기각된 키 `2b7e4da04edb` = 「알 수 없는 숫자형 장부 상태 거부」 · tools/rot-check.js:300 그런데 그 행의 `사유` 는 처음부터 끝까지 **tools/lib/loom.js 의 at-rule 스코프**를 논한다 (「범위씌우기 를 실제로 태우니 두 절반이 다 틀렸다 … keyframes 의 from·to 까지 접어 애니메이션이 조용히 죽는다」). 그 논증이 겨눈 실제 키는 `8d36529ad3e3`(loom.js:642)다. 즉 **키를 하나 밀려 쳤다.**  결과가 양쪽으로 샜다 — 두 방향 다 「통과」로 보인다: ① rot-check.js:300 은 **진짜 결함인데 영구 기각**됐다. 기각 이력은 다음 회차에 같은 지적을    내 앞에서 걸러내므로(그게 이 장부의 목적이다), 이 결함은 이제 **다시는 지적되지 않는다.**    실측으로 진짜임을 확인했다: `if (!o || typeof o.판정 !== 'number')` 가 «유일한 문」이라고    주석이 스스로 적어 뒀는데 값 집합이 안 닫혀 `판정:99` 가 통과하고, `장부판정` 은 1·2 만 보므로    적색도 메모도 안 내며, 훅은 `측정:true` 로 도장을 찍어 6시간 창을 리셋한다. ② 정작 틀린 지적 loom.js:642 는 **기각이 안 된 채 남아** 배포 게이트를 계속 막는다.    (`--확인` 이 지금도 차단급 2건으로 센다.)  왜 기계가 못 봤나 — 도구는 키만 보고 제목·파일을 자기가 채워 넣는다. 그래서 장부 행은 「제목: 알 수 없는 숫자형 장부 상태 거부 · 파일: tools/rot-check.js · 사유: (loom 이야기)」로 **겉보기엔 완벽하게 정상인 행**이 된다. 사람이 읽어도 제목과 사유를 나란히 놓기 전엔 안 보인다. CLAUDE.md 신뢰성 ④ 「맞는 얼굴로 틀린 값을 내는 자리」의 실물이고, 새는 방향은 언제나 「통과」다.  처방 = 사유가 지목하는 파일이 그 키의 파일과 어긋나면 `--기각` 이 거부한다. 지적마다 파일 경로가 장부에 이미 있으므로 재료는 다 있다(추가 수집 0). 대가: 사유에 남의 파일을 «참고로» 인용하는 정당한 기각이 막힌다 — 그래서 차단이 아니라 「어긋난다」를 말하고 확인 플래그로 지나가게 한다(따를 수 없는 처방을 만들지 않는다 · F103). | `tools/codex-review.js` 의 `사유_파일어긋남()` + 기각 등록부 배선 (`43ac5a13` · PR #42).  사유가 자기 파일을 한 번도 안 짚고 다른 파일만 논하면 `--기각` 이 exit 2 로 거부하고, 어느 파일을 논하는지와 지나갈 길 둘(맞는 키 찾기 = `--확인` / 참고 인용이면 `--딴파일`)을 함께 낸다. 지나간 행은 `딴파일확인: true` 로 장부에 남아 다음 사람이 같은 행을 F541 로 다시 의심하지 않는다.  F083 대로 **신고 원문을 그대로 되먹여 확인했다** — 회귀 `tests/이종검수.test.js` 의 실저장소 검사가 `검수기각.jsonl` 전건을 태워 그 행(`2b7e4da04edb`)을 실제로 잡는지 assert 한다. 잡는다.  대가와 닫은 것(새 장치 규약): 실측 73건 중 발화 4 = 진짜 1(그 행) + 남의 파일을 «근거로» 인용한 정당한 기각 3(상담AI.js↔Code.js 라이브 실측 · 엔진_셋업확장.js↔엔진_운영배치.js:3133 강사 문자열). 그 3건은 `--딴파일` 한 번으로 지나간다 — 차단으로 끝내면 우회가 정상 통로가 된다(F103). 닫은 것 = 「사유가 아무 파일도 안 짚으면 판정하지 않는다」(산문 기각은 그대로 통과). 틀릴 때의 모습 = 문턱이 헐거워지면 실저장소 발화가 4를 넘고, 회귀가 그 수를 못박아 잡는다.  회귀 120(신설 6) · 변이 5/5 구멍 0 · 미적용 0(새는 방향 둘을 다 겨눴다). 그리고 이 신고가 가리킨 «진짜 결함» rot-check.js:300 은 기각이 아니라 고쳤다(`427b327b` — 값 집합을 닫아 모르는 판정을 「못 잼」으로 낸다). 오탐 loom.js:642 는 맞는 키로 다시 기각했다(`5601f0b1`). |
