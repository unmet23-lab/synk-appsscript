| F420 | 2026-08-14 | 실수 | 유호님께 「지금 놓치고 있는 것」 판정 재료를 내면서, 시한이 걸린 6건 중 **2건을 열어보지 않고 메모리로 대신 판정해 틀리게 배달했다** — 그리고 그 둘이 하필 내가 스스로 「급소」라고 지목한 칸이었다. 실측 `local_4f668e62` 2026-08-14.  **무엇이 틀렸나** — 같은 세션 안에서 브라우저로 실제 화면을 열자 둘 다 뒤집혔다. - 「전자송달 **미신청** — 2클릭 필요 · 이게 급소」 → 실물은 **신청일자 `2026-08-12` · 해지일자 빈칸**(살아 있다). 요약 카드도 「사용중 입니다」. - 「근로장려금 2025 귀속분 **미확인** — 11/30 기한후신청(10% 감액) · 40~50만원 걸림」 → 실물은 **05월 정기신청 완료 · 심사중 · 지급예정 2026-08-27**. 놓친 것이 애초에 없었다. - 곁들여 분모도 틀렸다: 「폐업 사업자 2건」으로 세어 왔는데 `사업자 전환` 모달의 폐업 필터는 **3건**을 냈다(호유인터내셔날·블로우 기프트샵·키일리쉬). 08-12 에 신고한 2건만 세고 그 이전 폐업 1건이 빠져 있었다.  **왜 굵직한가 — 조항이 없어서가 아니라 있는 조항을 어겼다.** CLAUDE.md 판정·검증절이 정확히 이 자리를 못박고 있다: 「현재 상태는 현재를 읽어 판정한다 — 문서·목록의 한 갈래·응답 코드는 전부 간접 증거다(F030·F033·F058·F062·F130). 조작·판정·인용·이관 **직전에** 그 대상을 직접 연다.」 내가 인용한 것은 08-12 메모리 파일이었고, 그것은 **그때 참이었던 기록**이지 현재가 아니다. 축으로 보면 「잴 수 있는 건 잰다」(다른 층을 쟀다)가 아니라 정확히 「안 재고 자료로 대신했다」쪽이다.  **새는 방향이 이번에도 조용했다 — 그런데 모양이 뒤집혀 있다.** 가드가 새는 흔한 방향은 「없는데 있다고 통과」인데, 이번 오배달은 **「이미 됐는데 안 됐다고」**였다. 그래서 겉보기 손해가 없다(유호님이 시키는 대로 눌러도 이미 신청된 화면을 보고 끝났을 것). 바로 그 점이 위험하다 — **틀렸는데 아무도 안 다치는 오류는 교정 신호가 안 온다.** 이번엔 「원격으로 창 직전까지 가 달라」는 유호 지시가 우연히 실물을 열게 만들어 드러났고, 그 지시가 없었으면 이 두 줄은 다음 세션 인계문에 그대로 실려 계속 살았을 것이다. 실제로 08-12 → 08-14 이틀간 그렇게 살아 있었다.  **뿌리 — 메모리 파일에 「잰 시각」은 있는데 「유효기간」이 없다.** 이 저장소의 메모리 규약은 상대 날짜를 절대 날짜로 바꾸도록 이미 강제하고(그래서 「실측 08-12」라고 정확히 적혀 있었다) 나도 그걸 읽었다. 문제는 **날짜가 적혀 있어도 그것이 「다시 재라」는 신호로 읽히지 않는다**는 것이다. 특히 **저장소 밖 상태**(홈택스·특허로·은행·관공서)는 유호님이 세션 밖에서 손을 대므로 repo 안 값과 노화 속도가 완전히 다른데, 메모리는 둘을 같은 모양으로 적는다. 이번 건은 **유호님이 08-12 세션 직후에 직접 눌러 해결한 것**으로 보이고, 그 사실이 돌아올 통로가 없었다.  **▶ 닫으려면 — 후보 둘. 지금은 안 고친다(1번째).** ㉠ 메모리 규약에 「저장소 밖 상태」 표식 한 칸(예 `🌐`)을 두고, 그 줄을 판정 재료로 **인용할 때** 「직접 열었나」를 묻게 한다. 문제: 채팅층이라 훅이 볼 자리가 없다(F324 가 같은 벽에 부딪혔다 — 「권고는 채팅층이라 지금 걸 훅이 없다」). ㉡ 더 좁고 실현 가능한 자리 — **유호님께 배달되는 ⏳ 큐 항목 중 저장소 밖 상태에 기대는 것**에만 「마지막 실측일 + 며칠 지났다」를 같이 찍는다. 그러면 「08-12 실측」이 「이틀 전」이 아니라 화면에 숫자로 서고, 내는 쪽도 받는 쪽도 낡음을 본다. 이건 `decision-queue.js` 출력층이라 기계가 닿는다.  같은 형태가 2번째로 나오면 그때 ㉡을 판다 — 지금 조항을 더 쓰는 것은 v8.7 이 12건을 같은 사유로 안 닫은 축 그대로다(막을 조항은 이미 있고 지켜지지 않은 재발이다). | ⏸ 판정은 신고문 자신이 이미 적었다(`local_4f668e62` · 「▶ 닫으려면 — 후보 둘. **지금은 안 고친다(1번째)**」): ㉠ 메모리 표식은 채팅층이라 훅이 볼 자리가 없고(F324 와 같은 벽) ㉡ decision-queue 출력층의 「마지막 실측일 + 며칠」 표식은 **같은 형태 2번째에 판다**(1번째에 조항·장치를 더 쓰는 것은 v8.7 이 12건을 안 닫은 축 그대로). 장부만 「열림」으로 남아 있어 그 판정을 행에 옮겨 적는다 — `local_6c887e59` 08-14, 내용 재검토 후 동의(신고문이 처방 후보·발동 조건·비용까지 이미 셌다). 닫는 조건 = 같은 형태(저장소 밖 상태를 메모리로 대신 판정해 오배달) 2번째 발생 시 ㉡ 시공과 함께 resolve. |
