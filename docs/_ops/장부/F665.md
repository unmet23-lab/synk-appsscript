| F665 | 2026-08-19 | 마찰 | board-move 의 «반쪽 롤백» 잔재가 제3자에겐 정상 편집과 구별이 안 되고, CLAUDE.md 의 「즉시 커밋」이 그 제3자를 잔재 커밋으로 민다 — 그러면 F226 복구 신호가 죽는다.  실측 2026-08-19 (세션 `abee3c63` · 클라우드): ① SessionStart 의 `보드수거` 가 `cb62b1cb` 의 완료 줄을 이관하려다 `.git/index.lock` 충돌로 커밋에 실패했다. board-move 는 설계대로 **보드 파일만** 원문으로 되돌리고 **아카이브 삽입은 작업본에 남겼다**(F226 이 그 잔재를 알아보고 이어가라고 남긴 것). ② 그 결과 `git status` 에는 `docs/세션보드_아카이브.md` 한 줄 추가만 보인다 — 훅이 만든 다른 산출물(`작동대장_기록.jsonl` append)과 **모양이 완전히 같다.** ③ 나는 CLAUDE.md 세션 규약 「미커밋을 오래 두지 않는다 · 즉시 커밋」과 Stop 훅의 미커밋 경고를 따라 그 줄을 커밋했다(`536bd71`). 그 순간 ④ 줄이 아카이브와 살아있는 보드 **양쪽에** 남았고, F226 의 복구 신호(= 아카이브의 «미커밋» 추가행)가 사라졌다 — `잔재조사()` 는 `git show HEAD:아카이브` 와 작업본을 대조하므로 커밋되는 순간 「더해진 행 0 = 잔재 없음」으로 눈이 먼다.  무엇이 실제로 구했나: `보드수거 --실행` 이 여전히 그 줄을 거둘 수 있었다 — board-move 345·370행의 `이미있음` 검사가 「아카이브에 이미 있으면 다시 삽입하지 않는다」로 막아 둔 덕이다(F226 의 다른 절반). 그래서 실피해는 «보이는 중복 한 시점»에서 그쳤고 F102 의 「유실 대신 중복」 보장은 지켜졌다.  ⚠ 남는 구멍: 잔재를 커밋해도 **아무도 경고하지 않는다.** `commit-noop-guard` 는 no-op 만 보고, `git-scope-guard` 는 범위만 본다. 잔재가 커밋되면 ㉠F226 의 「다시 돌리면 이어간다」 처방이 조용히 무력해지고 ㉡보드에 줄이 남은 채 아카이브에만 실린 상태가 **영구화**된다(다음 `보드수거` 가 안 돌면 아무도 안 본다).  제안(판정 필요): `PreToolUse`(Bash·git commit) 나 `commit-noop-guard` 옆에서 **`docs/세션보드_아카이브.md` 에 추가행이 있는데 짝이 되는 보드 파일 삭제가 함께 담기지 않은 커밋**을 잡아 「이건 board-move 잔재다 — `node tools/보드수거.js --실행` 으로 마저 닫아라」로 돌려보낸다. 이미 `잔재조사()` 가 그 판정을 다 하고 있으니 새 로직이 아니라 **부르는 자리**만 늘리면 된다. 대가: 정당한 손 아카이브 편집(드묾)이 한 번 막힌다 — 처방이 그 자리에서 통로를 알려주므로 F103 의 「따를 수 없는 처방」은 아니다. | |
