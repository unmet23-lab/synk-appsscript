| F665 | 2026-08-19 | 실수 | 경량화 트랙이 도구를 짓고 **켜지 않은 채** 닫혔다 — 「재웠다」의 얼굴로 아무것도 안 재워졌다.  실측(2026-08-19 · 유호님 질문 「과감하게 제대로 경량화 하고있는거 맞아?」에서 재기 시작):   · `de35260` 이 `tools/가드강등.js` 140줄을 더하고 PR #150 으로 머지됐다. 삭제 0줄.   · 그런데 `node tools/가드강등.js --상태` 가 「스위치: 없음 → 절차 가드 10개는 지금 **돈다**」였다.     그 도구는 인자 없이 부르면 적용되는데(`else if (!인자) 적용()`), **아무도 안 불렀다.**     `.claude/settings.json` 은 그 커밋 뒤로 한 글자도 안 바뀌었다.   · 결과: 재워진 가드 0개 · 저장소만 140줄 무거워짐. 경량화 트랙이 순증으로 끝났다.   · 같은 날 작동대장은 이미 `"안불림":["tools/가드강등.js", …]` 로 잡아놨다 — 그 사진이     **미커밋으로 방치**돼 아무도 안 읽었다(장치는 봤는데 사람에게 안 닿은 자리).   · 덤: 그 커밋이 래칫(`게이트 없는 도구` 75→76)을 깨뜨린 채 머지됐다. CI 적색이 주인 없이 남았다.  왜 샜나 — CLAUDE.md 「장치와 그 발동 조건은 같은 커밋에서 정한다 · 스스로 발화하지 않는 장치는 안 돈다」가 **훅 등록**만 말하는 것으로 읽혔다. 이 도구는 등록이 아니라 «한 번 실행»이 발동 조건이라, 조항의 글자에는 안 걸리고 뜻에는 정통으로 걸린다.  처방(이 트랙에서 적용) — 상태를 바꾸는 도구를 짓는 트랙은 **그 도구를 켠 상태로** 닫는다. `--상태` 류가 「아직 안 걸렸다」로 나오면 그 트랙은 미완이다. 지표는 도구의 존재가 아니라 그 도구가 바꿔 놓은 **상태**다. | 스위치를 실제로 적용(가드 10개 재움) + tools/가드강등.js 에 인자게이트 → 래칫 76→75 초록. 규칙: 상태를 바꾸는 도구를 짓는 트랙은 그 도구를 «켠 상태»로 닫는다 — --상태 가 「아직 안 걸렸다」면 미완이다. |
