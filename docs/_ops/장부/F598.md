| F598 | 2026-08-18 | 마찰 | **「제자리 원복이라 바이트 무변경」이라는 부정 표기는 «사후»에만 참인데, 남은 «도는 중»에 잰다** — 변이 검사가 도는 십여 분 동안 그 파일은 다른 세션에게 「미커밋으로 들고 있다」로 보인다.  실측 2026-08-18 (`local_f6457d2a` · F582 ㉠ 재측정 트랙 · 우연이 아니라 **남이 실제로 그렇게 읽은 것**을 잡았다): · 나는 워크트리 `f582-liveness-gate-check4` 에서 `node tools/변이.js` 를 배경으로 돌렸다(변이 5 + 기준선 = 판당 ~2.5분 · 총 십여 분). 그 도구는 `.claude/hooks/board-guard.js` 를 **제자리에서 고쳤다가 바이트로 되돌린다**. · 같은 창에 내 보드 선언의 파일 칸은 `무변경=.claude/hooks/board-guard.js·tests/board-guard.test.js (… 변이는 워크트리서 바이트 원복)` 이었다 — `board-guard` 는 그 부정 표기를 읽고 자리를 안 세웠다(통과). · 그런데 옆 세션 `local_7dfdcffd` 는 같은 시각에 F589 판정문에 이렇게 적었다: 「그 파일을 **지금 다른 세션이 미커밋으로 들고 있다** — 워크트리 `f582-liveness-gate-check4` 의 `.claude/hooks/board-guard.js`(`작업본소유자.js` 실측 2026-08-18)」. 그리고 그것을 **안 짓는 이유**로 썼다(「내가 같은 자리를 지으면 중복 구현」). · 변이가 끝난 뒤 같은 명령을 다시 재니 `git -C .claude/worktrees/f582-liveness-gate-check4 status --porcelain` = **빈 출력**이다. 즉 그 관측은 지금 재현되지 않는다 — 창이 닫혔다.  ■ 기전 — 추정이 아니라 소스에서 읽었다 `tools/작업본소유자.js:183` 이 `git status --porcelain -uall` 를 돌리고 :199 에서 워크트리마다 같은 것을 돈다. 변이 도구는 fs 로 파일을 고치므로 그 창 동안 정확히 `M .claude/hooks/board-guard.js` 가 뜬다. 두 층이 **같은 순간에 반대로 답한다**: board-guard 는 선언문을 읽어 「안 만진다」, 작업본소유자는 디스크를 읽어 「만지는 중이다」.  ■ 왜 신호인가 — F589 가 이 부정 표기에 **정당성**을 부여해 뒀다 F589 는 자기가 ④를 지난 이유를 「부정 표기가 우회가 아니라 **사실**이어서 통과했다」고 적었다(변이 원복 보장을 근거로). 그 사실성이 **시간 의존**이라는 것은 어디에도 안 적혀 있다. 그래서 다음 사람은 그 표기를 상시 참으로 읽는다.  ■ 이번엔 손해가 안 났다 — 그게 이 신고의 급소다 `7dfdcffd` 가 내린 결론(안 짓는다)은 **결과적으로 옳았다**. 내가 실제로 그 트랙을 들고 있었기 때문이다. 즉 틀린 관측이 맞는 결론을 냈다 — 그래서 아무도 안 알아챈다. 새는 방향 둘을 적어 둔다: ㉠ 도는 중에 재면 **선언과 무관하게** 잡힌 것으로 보인다(낭비 쪽 · 이번 판) ㉡ 반대로 변이가 끝난 «뒤»에 재면 진짜로 그 트랙을 든 세션이 미커밋 0으로 보인다 — 그때 판정을 지는 것은 보드 선언 하나뿐인데, 그 선언은 `무변경` 이라 자리를 안 세운다. ㉡이 새는 방향은 **통과**다.  ■ 처방 후보 — 안 지었다(이 신고는 재료다) ㉠ 부정 표기에 **시간 축을 적게 한다** — `무변경(변이 중 일시 수정)` 처럼 창의 존재를 표기 자체가 말하게 한다. ⚠대가: 표기 문법이 늘고, 늘어난 문법은 안 쓰인다. ㉡ `변이.js` 가 도는 동안 **자리 표식 파일**(예: `.claude/변이중`)을 두고 `작업본소유자` 가 그 파일을 읽어 「변이 중(일시)」으로 갈라 낸다. ⚠대가: 도구가 죽으면 표식이 남는다 — 고아 잠금과 같은 병(F493·F562)을 하나 더 만든다. ㉢ 아무것도 안 한다 — 창이 십여 분이고 관측자가 보수적으로(비켜나는 쪽) 틀리므로. ⚠대가: ㉡ 방향은 그대로 남는다. | |
