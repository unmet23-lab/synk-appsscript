| F417 | 2026-08-14 | 실수 | 회귀 단언이 «같은 훅의 옆 게이트가 낸 말»에 걸려 초록이었다 — 내 게이트가 입을 다물어도 통과. 픽스처 저장소엔 옛글자·인용 검사기가 없어 pre-commit 이 스스로 「…못 했다」를 찍는데, 단언을 /못 했다/ 로만 걸어 그걸 내 탐지의 증거로 삼았다. 변이 ④가 살아남아 드러났다(단언을 [계약동봉] 태그에 못박아 수리 · 같은 파일의 다른 단언 하나도 빈 문자열에 대고 통과 중이었다). 남의 말로 내 탐지를 증명하지 마라 — 맹점 ④가 회귀 «자신»에서 난 자리다. | 실물은 `ce413b13`(신고 `760e731d` 보다 앞) 에 이미 고쳐져 들어와 있었다 — 단언을 `[계약동봉]` 태그에 못박은 판정기(`tests/계약동봉.test.js:159` 의 `이게이트가`)와, 성공 갈래의 stderr 를 살리는 `spawnSync` 호출부. 장부만 열려 있었다(F105·F117 과 같은 형태 — 고침과 닫음은 별개 동작). 이 세션은 **닫기 전에 F083 대로 격리 사본(`git worktree --detach HEAD`)에서 다시 쟀고, 그 과정에서 같은 형태가 다른 축에 살아 있는 것을 실측으로 잡아 함께 닫았다.**  **A축 — 신고된 자리(`tests/계약동봉.test.js`) 2×2 대조.** 바꾼 조건은 둘뿐이다: ㉠ 게이트의 「못 했다」 한 줄을 지운다(`tools/계약동봉검사.js:91`) ㉡ 단언을 옛 무태그 `/못 했다/` 로 되돌린다. · 기준선(손 안 댐) = 초록 12/0/0 · 게이트 침묵 × **지금** 단언 = **적색 10/2/0** — 정확히 「앵커가 죽으면 못 쟀다를 찍는다」·「앵커 끝이 없어도 못 쟀다다」 두 칸이 진다 · 게이트 침묵 × **옛** 단언 = **초록 12/0/0** — 신고 원문 그대로 재현된다(눈멂) · 옛 단언만(게이트 정상) = 초록 12/0/0 — 거짓양성 0 ⇒ 갈리는 것은 **태그 하나**뿐이고, 그 하나가 탐지의 전부다. A축은 닫혔다.  **B축 — 같은 형태가 옆 축에 그대로 살아 있었다(같은 형태 2번째).** `tests/옛글자훅.test.js:282` 가 같은 pre-commit 출력에 대고 무태그 `/못 했다/` 로 걸려 있었다. 이 훅은 게이트가 넷이고 그중 **셋**(`[계약동봉]`·`[옛글자]`·`[인용검사]`)이 검사기 부재 때 **같은 문구**를 찍는데, 그 픽스처엔 옛글자 검사기 말고는 애초에 아무것도 없다. 실측: · 옛 단언 × 게이트 정상 = 초록 13/0/2 · 옛 단언 × **게이트 침묵**(pre-commit 65행 echo 삭제) = **초록 13/0/2 — 한 칸도 안 진다** · 새 단언 × 게이트 정상 = 초록 13/0/2 (거짓양성 0) · 새 단언 × **게이트 침묵** = **적색 12/1/2** — 「검사기가 없으면 막지 않고 못 했다고 말한다」 칸이 진다 ⇒ 처분: 그 파일에도 `[옛글자]` 못박은 `이게이트가` 를 세우고 단언을 갈아끼웠다.  🔑 배운 것 — **한 축에서 배운 모양을 다른 축에 안 옮기면 그 축은 그대로 눈이 먼다.** 이 두 파일은 실제로 교훈을 «한 방향으로만» 주고받았다: `spawnSync`(성공 갈래 stderr 유실) 교훈은 옛글자훅 → 계약동봉 으로 옮겨져 두 파일 다 주석까지 달려 있었는데, 태그 못박기 교훈은 계약동봉에서 «났는데» 옛글자훅으로 안 갔다. 옮겨진 교훈과 안 옮겨진 교훈이 같은 두 파일 안에 나란히 있다.  ⚠ 대가·천장 (맹점 ④ — 새 장치엔 틀릴 때의 모습을 함께 적는다). ㉠ 못박은 태그는 **게이트가 자기 이름표를 찍는다는 것에 기댄다** — 훅의 `[옛글자]`·`[계약동봉]` 표기를 바꾸면 이 단언들이 적색이 된다. 다만 그건 조용히 새는 방향이 아니라 시끄러운 방향이라 그대로 둔다. ㉡ **공용 통로는 안 세웠다** — 실측 결과 이 형태의 자리는 저장소 전체에 둘뿐이고(`tests/계약훅.test.js:67` 은 픽스처가 심은 고유 카나리아 `불렸다` 라 애초에 안 겹친다), 둘 다 지금 닫혔다. 지침의 「3번째 = 공용 통로」 문턱에 아직 안 닿았고, 호출부 둘에 lib 한 벌을 세우는 것은 경량 원칙에 어긋난다. **3번째가 나오면 그때 `tests/lib/` 로 접는다** — 그 판정을 여기 적어 두는 이유는 다음 사람이 같은 대조를 처음부터 다시 하지 않게 하기 위해서다. ㉢ 닫은 것 1개 = `tests/옛글자훅.test.js` 의 눈먼 단언(위 실측). |
