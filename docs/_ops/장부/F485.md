| F485 | 2026-08-15 | 실수 | 해소로 «닫힌» F248 의 게이트가 작업본을 재고 있었다 — 그래서 같은 사고가 그 뒤로 두 번 더 났고, 장부는 내내 초록이었다.  F248(08-08)은 `7cb57e3` 로 해소 처리됐다: 「auto-commit 이 실을 후보 중 테스트 파일만 돌린다 · 적색·미측정은 그 파일만 떨어뜨리고 말한다 · 회귀 3·20/20·변이 6/7」. 그런데 그 뒤 **같은 모양이 두 번 더 났다** — F444(08-14 `594ef735`↔`01cb8dd0`)와 F444 2번째(08-15 `bea5da2f`↔`c3a283db` · HEAD 가 «회귀는 새 것 · 엔진은 옛 것»이라 **남의 /deploy 게이트를 막았다**). 즉 장부의 「해소」와 실물의 상태가 갈려 있었고, 갈린 쪽의 증상은 언제나 「통과」였다(CLAUDE.md 맹점 ④ — 맞는 얼굴로 틀린 값).  ■ 무엇이 빠져 있었나 (픽스처로 재현 · 추정 아님) 그 게이트는 실을 테스트를 **작업본** 상태로 돌린다. 커밋에 실리는 것은 `후보` 뿐이라, 짝이 이 커밋에서 빠지는 판에서는 그 초록이 「지금 디스크에 다 있다」를 잰 것이지 **「커밋 뒤 HEAD 가 서는가」를 잰 것이 아니다.** 재현: 짝 `구현.js` 를 무기록으로 빼고 그 짝에 기대는 새 테스트를 두면 → 작업본 초록 → 테스트만 착지 → HEAD 를 깨끗한 곳에 꺼내 돌리니 **적색**. 🔑 훅은 그때도 「구현.js 는 안 실었다」고 **말하고 있었다** — 뺀 사실은 알면서 그것을 자기 초록의 분모로 연결하지 않은 것이 구멍의 전부다(「초록은 분모와 함께만 읽는다」가 이 장치 자신에게는 안 걸려 있었다).  ■ 왜 이 행을 따로 세우나 F248 은 이미 「해소」라 되돌리지 않는다(그 판정을 뒤집는 것이 아니라, 그 처방이 **덜 닫았다**는 것이 이번에 잰 값이다). 재발 두 건은 F444 가 들고 있었는데 그 행의 판정은 「과녁이 Stop 훅이라 이 트랙 밖」으로 보류였다 — 그래서 어느 행도 이 구멍을 자기 몫으로 들고 있지 않았다. 그 빈자리를 이 행이 든다.  ■ 곁가지 — 따를 수 없는 처방이 서 있었다(F103) F342 가 처방으로 채택한 「트랙 여는 동안 `SYNK_AUTOCOMMIT_OFF=1`」은 **세션이 원리상 못 쓴다**(실측 08-15): Bash 한 호출에서 export 한 값은 다음 호출에서 빈값이고(셸 상태 비영속), 이 훅은 세션의 셸이 아니라 Claude Code 가 띄우므로 그 env 가 닿는 경로 자체가 없다. `settings.json` 의 env 는 저장소 전체 공유라 켜면 남의 세션 자동커밋까지 꺼진다. 훅 머리말이 그것을 끄는 법으로 안내하고 있어 실측대로 고쳤다. | auto-commit.js 의 「그 초록은 무엇을 잰 초록인가」 게이트 + 회귀 2건 — dbf08dda(수리+회귀 한 커밋) · 991ace96(근거 교정) · 대기열 #Q96.  ■ 왜 08-08 처방이 서 있는데도 두 번 더 났나 — 이번에 픽스처로 재현해 특정했다. F248 처방 ①(「이 커밋 하나로 판이 서는가」)은 auto-commit.js:161-220 에 이미 서 있었다. 그런데 그 게이트는 실을 테스트를 **작업본** 상태로 돌린다. 커밋에 실리는 것은 `후보` 뿐이라, 짝이 이 커밋에서 빠지는 판에서는 그 초록이 「지금 디스크에 다 있다」를 잰 것이지 **「커밋 뒤 HEAD 가 서는가」를 잰 것이 아니다.** 재현(픽스처 · 2026-08-15): 짝 `구현.js` 를 무기록으로 빼고 그 짝에 기대는 새 테스트를 두면 → 작업본 초록 → 테스트만 착지 → HEAD 를 깨끗한 곳에 꺼내 돌리니 **적색**. 이것이 신고문의 그 상태 그대로다. 🔑 급소는 훅이 **그때도 「구현.js 는 안 실었다」고 말하고 있었다**는 것이다 — 뺀 사실은 알면서 그것을 자기 초록의 분모로 연결하지 않은 것이 구멍의 전부였다.  ■ 무엇이 실제로 막았나 커밋에 안 실리는 «내용»(함께남김·깨짐·무기록·밖에서바뀜)이 작업본에 남아 있으면, 그 커밋에서 **테스트 파일만** 떨어뜨리고 짝까지 모으는 손 커밋 명령을 낸다. 나머지 파일은 그대로 싣는다(적색 게이트와 같은 「떨어뜨리기」 — 통째로 보류하면 F025 쪽으로 사고가 옮겨간다). 새 원칙이 아니라 이 훅이 이미 적어 둔 **「모름은 커밋하지 않는다」** 를 빠뜨린 자리에 적용한 것이다.  ■ 곁가지로 닫은 것 — 따를 수 없는 처방이 서 있었다(F103) 훅 머리말이 끄는 법으로 안내하던 `SYNK_AUTOCOMMIT_OFF=1` 은 **세션이 원리상 못 쓴다**(실측 08-15): Bash 한 호출에서 export 한 값은 다음 호출에서 빈값이고(셸 상태 비영속), 이 훅은 세션의 셸이 아니라 Claude Code 가 띄우므로 그 env 가 닿는 경로 자체가 없다. settings.json 의 env 는 저장소 전체 공유라 켜면 남의 세션 자동커밋까지 꺼진다. 그 안내를 실측대로 고쳤다 — F342 가 처방으로 적었던 형태가 바로 이것이라, 그 처방이 반복 재발을 못 막은 이유가 여기 있다.  ■ 검증 회귀 tests/자동커밋.test.js **22/22**(fail 0 · skip 0 · 340초) — 신설 2건은 양방향이다(짝 빠지면 보류 + 무관한 파일은 그대로 실림 / 적색뿐인 판에서 새 게이트는 조용함). 변이 **4/5 · 미적용 0**(공용 통로 tools/변이.js · 기준선 초록 5건). 안 잡힌 ⑤(방아쇠에 적색·미측정 추가)는 **회귀의 사각이 아니라 동치 변이**다: 적색·미측정이 차면 위 게이트가 그 배치의 테스트를 후보에서 통째로 빼므로 이 가지가 도달 불가라 관측 자체가 성립하지 않는다(그 사실을 코드 주석에 실측으로 적었다 — 991ace96).  ■ 대가(함께 적는다) 테스트와 **무관한** 파일 하나가 빠진 판에서도 그 턴엔 테스트가 안 실린다 — F025 노출이 한 턴 이어진다. 대신 조용하지 않고(처방 문구가 손 커밋 명령을 그대로 준다) 다음 턴에 배제가 걷히면 실린다.  ■ 안 닫은 것(범위 밖 — 기록으로 남긴다) ① 쪼갬 자체는 그대로다 — 한 트랙이 두 커밋으로 갈리는 것은 F444·F342 가 계속 든다(막은 것은 「쪼개지면 남이 다친다」쪽이다). ② **배포집합 파일**이 짝 없이 실리는 F302 형태는 이 게이트 밖이다(테스트 초록으로 잴 수 있는 축이 아니다). ③ 위 적색 게이트의 배치 단위 성질(하나가 빨가면 그 배치 테스트 전부 보류)은 보수적이라 새는 방향은 아니지만 훅 주석의 「떨어뜨리기지 보류가 아니다」보다 거칠다 — 파일 단위로 쪼개면 Stop 예산이 파일 수만큼 곱해져 모든 세션에 닿으므로 이 트랙에서 안 건드렸다. 대기열 #Q96 에 한계로 적었다. |
