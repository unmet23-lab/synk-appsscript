| F496 | 2026-08-16 | 마찰 | 장부가 실물과 갈라진 채 며칠을 산다 — 실측 2026-08-16: 열림 6건 중 **3건이 이미 고쳐진 것**이었다(F451·F490·F491 = 50%). 그중 둘은 **같은 세션 `ef8cdbc2` 가 같은 날 고치고 둘 다 안 닫았다**(`e20ff91c`·`3ccb4642`). `friction-close-guard` 는 **커밋 시점에 한 번 알리고 끝**이라 새는 방향이 둘이다: ①번호를 안 단 커밋에는 **원리상 눈이 먼다**(F491 이 그 자리 — 그 훅은 커밋 메시지의 `F\d{3}` 으로만 행을 찾는다) ②번호를 단 커밋에도 짖은 뒤 **세션이 그냥 끝나면 아무도 다시 안 본다**(F490 이 그 자리). 🔑 급소는 그 훅 머리말이 스스로 적어 뒀다 — 「절대 차단하지 않는다… **위험한 건 그 상태로 세션이 끝나는 것이다**」. 그런데 **끝나는 자리에 장부 검사가 0**이다: `/close` 6단계에도 · `session-end-handoff` 에도 · 인계문(`buildHandoff`)에도 없다. 🔴 그러면서 `/close` 는 **같은 논리로 두 단계를 이미 세워 뒀다** — 3-b 배포 대조(「`배포대조.js` 는 그때도 있었고 아무도 안 돌렸다 · 장치는 스스로 발화하지 않으면 안 돈다」)와 5-b 메모리 `⏳` 닫기(「새 파일을 쓰는 칸과 옛 줄을 닫는 칸이 갈라져 있는 것이 F115 의 나머지 절반」). 장부는 정확히 5-b 와 같은 병인데 그 칸만 비어 있다 — 신고(새 행)는 커밋마다 도는데 **닫기(옛 행)는 아무 데도 안 걸려 있다.** ⚠ 이 축은 F092(안 고쳤는데 닫음)·F096(고쳤는데 안 닫음)·F107·F132 에 이어 **여섯 번째**이고, 처방은 매번 «훅 판별식 기우기»였다(예외 둘을 덧대는 식) — 판별식이 아니라 **자리**가 비어 있었다. 대가는 둘: 다음 세션이 **이미 끝난 일을 다시 집거나**(F096 이 적은 바로 그것), 오늘처럼 코드를 열어 대조하는 비용을 문다(이 세션은 그 대조에 상당 부분을 썼다). 처방=`/close` 에 단계 하나 + `friction.js` 에 「이 세션 커밋이 만진 파일 ∩ 열린 행 신고문의 경로」를 가르는 갈래(새 훅 0·새 도구 0·새 상태 파일 0 · `friction.read()` 실측 28ms). | |
