| F496 | 2026-08-16 | 마찰 | 장부가 실물과 갈라진 채 며칠을 산다 — 실측 2026-08-16: 열림 6건 중 **3건이 이미 고쳐진 것**이었다(F451·F490·F491 = 50%). 그중 둘은 **같은 세션 `ef8cdbc2` 가 같은 날 고치고 둘 다 안 닫았다**(`e20ff91c`·`3ccb4642`). `friction-close-guard` 는 **커밋 시점에 한 번 알리고 끝**이라 새는 방향이 둘이다: ①번호를 안 단 커밋에는 **원리상 눈이 먼다**(F491 이 그 자리 — 그 훅은 커밋 메시지의 `F\d{3}` 으로만 행을 찾는다) ②번호를 단 커밋에도 짖은 뒤 **세션이 그냥 끝나면 아무도 다시 안 본다**(F490 이 그 자리). 🔑 급소는 그 훅 머리말이 스스로 적어 뒀다 — 「절대 차단하지 않는다… **위험한 건 그 상태로 세션이 끝나는 것이다**」. 그런데 **끝나는 자리에 장부 검사가 0**이다: `/close` 6단계에도 · `session-end-handoff` 에도 · 인계문(`buildHandoff`)에도 없다. 🔴 그러면서 `/close` 는 **같은 논리로 두 단계를 이미 세워 뒀다** — 3-b 배포 대조(「`배포대조.js` 는 그때도 있었고 아무도 안 돌렸다 · 장치는 스스로 발화하지 않으면 안 돈다」)와 5-b 메모리 `⏳` 닫기(「새 파일을 쓰는 칸과 옛 줄을 닫는 칸이 갈라져 있는 것이 F115 의 나머지 절반」). 장부는 정확히 5-b 와 같은 병인데 그 칸만 비어 있다 — 신고(새 행)는 커밋마다 도는데 **닫기(옛 행)는 아무 데도 안 걸려 있다.** ⚠ 이 축은 F092(안 고쳤는데 닫음)·F096(고쳤는데 안 닫음)·F107·F132 에 이어 **여섯 번째**이고, 처방은 매번 «훅 판별식 기우기»였다(예외 둘을 덧대는 식) — 판별식이 아니라 **자리**가 비어 있었다. 대가는 둘: 다음 세션이 **이미 끝난 일을 다시 집거나**(F096 이 적은 바로 그것), 오늘처럼 코드를 열어 대조하는 비용을 문다(이 세션은 그 대조에 상당 부분을 썼다). 처방=`/close` 에 단계 하나 + `friction.js` 에 「이 세션 커밋이 만진 파일 ∩ 열린 행 신고문의 경로」를 가르는 갈래(새 훅 0·새 도구 0·새 상태 파일 0 · `friction.read()` 실측 28ms). | `d5e04686` — **끝나는 자리에 검사를 세웠다.** `tools/friction.js --이세션` 이 「내 커밋(Session-Id 트레일러)이 만진 파일」 ∩ 「살아있는 행이 지목한 파일」로 맞추고, `/close` **5-c** 가 그것을 부른다(장치와 발동을 같은 커밋에 · 스스로 발화하지 않는 장치는 안 돈다). 🔑 **번호를 안 본다** — 그게 이 통로가 `friction-close-guard` 보다 넓은 유일한 이유다: F491 을 고친 `3ccb4642` 는 메시지에 번호가 없어 그 훅이 **원리상** 눈이 멀었다. 앞선 넷(F092·F096·F107·F132)이 전부 「훅 판별식에 예외 덧대기」였던 자리에서, 이번엔 **자리**를 채웠다. 새 훅 0·새 도구 0·새 상태 파일 0. 종료코드 셋을 가른다(0=없음 · 1=판정할 것 · 2=**못 쟀다** — git 실패를 0건으로 접지 않는다 F207). 기록물(장부·보드·인계문)은 「고친 파일」에서 뺀다 — 안 빼면 신고 커밋마다 살아있는 행 전부가 걸려 신호가 소음이 된다. **실저장소 실증**: 이 통로가 **자기 자신의 행(F496)을 잡았다** — 「내 커밋이 만진 `friction.js` 를 이 행이 지목한다 · exit 1」. 회귀 `tests/장부이세션.test.js` **14건**(탐지력은 전부 픽스처 · 실저장소는 거짓양성 검문만) · **변이 7/7**(구멍 0·미적용 0 — M1 파일명→경로 · M2 기록물 반쪽 · M3 보류 누락 · M4 git실패→0건 · M5 분모 삭제 · M6 침묵 사유 삭제 · M7 `/close` 호출 제거). 🔴 **착수 중 내 회귀가 CI 에서 깨질 자리를 하나 잡았다** — 스킬 파일명이 `SKILL.md`(대문자)인데 소문자로 적었다. Windows 는 대소문자를 무시해 로컬에선 초록이고 리눅스 CI 에서만 터진다(F296 계열). `git commit -- <경로>` 의 pathspec 에러가 그것을 잡아 줬고, 회귀에 그 경위를 주석으로 못박았다. ⚠ **남는 한계(닫지 않았다)**: ①신고문이 파일을 하나도 안 적은 행은 **원리상 안 걸린다** — 실측 08-16 분모가 「살아있는 54 = 지목 27 + **안 지목 27**」이라 **절반이 사각**이다. 그래서 출력이 늘 그 수를 함께 낸다(크면 `--open` 으로 직접 본다). ②파일명만으로 맞춰 동명이인 거짓양성이 가능하다 — 알림이지 차단이 아니라 새는 방향이 「한 번 더 본다」 쪽이다. ③`session-end-handoff`(창을 그냥 닫는 경로)엔 **안 얹었다** — 장치 개수를 늘리지 않으려는 판정이고, 그 경로로 새면 다음 세션이 `--open` 에서 본다. **닫을 것 = 없다**: `friction-close-guard` 는 커밋 시점을 계속 지켜야 한다(둘은 같은 축의 다른 시점이고 오늘 실측이 그 둘 다 필요하다는 증거다 — F490 은 훅이 짖었는데도 샜고, F491 은 훅이 아예 못 짖었다). |
