| F261 | 2026-08-08 | 실수 | 등록 검사가 「문자열이 있나」로만 봐서 **주석 처리한 호출도 통과**했다 — 야간 배치(nightJobs)에 새 함수를 거는 회귀를 `nj.includes("safeRun('selfDeclareLog', …)")` 로 짰는데, 그 줄을 `//` 로 주석 처리한 변이가 살아남았다. 등록층이 새는 **네 번째 형태**고 새는 방향은 여기서도 「통과」였다. 변이시험이 아니었으면 「걸어 뒀다」고 보고했을 것이고 실제로는 야간 배치가 그 함수를 영원히 안 불렀다 | 2026-08-08 줄머리 앵커로 「살아 있는 호출」만 센다(`/^\s*safeRun\('selfDeclareLog'/m` · tests/수집.test.js) — 문자열 포함은 주석·문서·인용까지 세므로 등록 검사에는 쓰지 않는다 |
