| F566 | 2026-08-17 | 마찰 | 인계문의 «직전 세션에서 한 것» 커밋 목록도 낡은 로컬 master 를 읽어 「커밋 없음」을 낸다 — F542 의 남은 반쪽(보드 줄 층만 고쳤다).  실측 2026-08-17 (`local_a0a573fb` · F542 를 착지시킨 그 세션이 자기 마감에서 그대로 겪었다): `--닫기` 로 머지·push 하고 `ExitWorktree(remove)` 한 뒤 `node tools/인계문.js` 를 돌리니 커밋 5건이 전부 origin/master 에 있는데도 **「· (커밋 없음 — 보드 줄과 메모리에서 직전 작업을 찾을 것)」**이 나왔다. 대조: `git log --grep a0a573fb` 가 로컬 master 에서 **0건** · `origin/master` 에서 **5건**.  기전은 F542 와 같은 뿌리다 — `.claude/hooks/lib/session-report.js` 의 `myCommits(cwd, sid)` 가 로컬 HEAD 의 이력만 훑는다. 그 트리의 로컬 master 가 뒤처져 있으면 방금 머지된 내 커밋이 그 이력에 아직 없다.  🔑 F542 트랙(`8cb3a96f`)은 **보드 줄 층만** 넓혔다(`보드.원격줄들()`). 그래서 그 판의 인계문은 트랙·상태 칸을 제대로 넘기고 「📥 `git merge --ff-only origin/master` 로 먼저 받아라」까지 주지만, 바로 위 커밋 목록은 여전히 「없음」이다 — **한 글 안에서 두 층이 서로 다른 판을 말한다.**  ■ 무게 — F542 본체보다 가볍다(그래서 같이 안 고쳤다)   · 문구가 「커밋 없음」 뒤에 **「보드 줄과 메모리에서 직전 작업을 찾을 것」**을 이미 달고 있어 확인을 금지하지 않는다(F529 가 고친 「다시 열 필요 없다」와 다른 자리다).   · 그리고 같은 글의 📥 줄이 «먼저 받아라»를 주므로, 그대로 따르면 다음 세션의 `git log` 는 정상이 된다.   · 다만 받는 세션의 첫인상이 「직전 세션이 아무것도 안 했다」라, 이어받기 판단의 재료가 한 겹 거짓이다.  ■ 처방 후보 — 안 지었다(재료다)   ㉠ `myCommits` 의 훑는 범위를 `HEAD` → `HEAD` + `origin/master` 로 넓히고, origin 에서만 나온 커밋은 «아직 안 받음»으로 표시한다(보드 줄 층이 이미 그 모양이다 — 이름표를 갈라 단다).     ⚠ 대가 = 스폰 1회 추가 · 그리고 「내가 안 받은 남의 커밋」이 섞이지 않게 **Session-Id 트레일러 필터는 그대로** 걸어야 한다.   ㉡ 커밋 목록은 그대로 두고, 📥 줄이 이미 있을 때는 「커밋 목록도 낡은 판이다」를 한 줄로 붙인다(값싸지만 판정이 아니라 주석이다).   ㉢ 아무것도 안 한다 — 📥 줄이 처방을 이미 주고 있고, 받은 세션이 그대로 따르면 자연히 해소된다.  ⚠ 뿌리를 공유하는 형제 = F565(가지 층의 기준이 낡은 로컬 master). 셋 다 「이 트리의 로컬 판이 진실이라고 가정한다」는 한 가정에서 나온다. | ⏸ 판정 2026-08-17 `local_1d940103` — **뿌리를 막았으나 이 행은 안 닫는다(보류).**  ■ 한 것 = 이 신고가 지목한 세 처방(㉠㉡㉢) 중 아무것도 아니다. 증상층(`myCommits` 의 훑는 범위)을   건드리지 않고, **로컬 master 가 낡는 것 자체**를 막았다 — 착지 `8885f70e`(PR #89).   생산자는 여섯이었다: `작업가지 --닫기` 가 origin 만 전진 + master 직접 커밋 도구 **5개가 전부 push 0**   (인계문수거·board-move·friction·보드껍데기정리·폰작업반입). 공용 통로 `tools/lib/master동기.js`   하나(`밀기`·`전진`)에 여섯 곳을 물렸고, 열거 회귀 ⑥-b 가 「master 에 커밋하면서 안 미는 도구」를   분모(≥5)와 함께 금지한다. 회귀 14 · 변이 15/15(구멍0·미적용0) · CI 모사 4303 fail 0.   🔑 왕복 실측: 그 `--닫기` 가 실제로 「로컬 master 3건 전진 · 뒤처짐 0」을 냈다.  ■ 그래도 안 닫는 이유 = **증상이 되살아날 창이 남아 있다.**   `전진` 은 `--ff-only` 고정이고, 남의 미커밋이 ff 를 막으면 **정상적으로 실패한다**(남의 바이트를   덮지 않는 것이 설계다 · 그 트리엔 상시 미커밋 11~20건이 있다). 그 세션의 트리는 그 순간부터   다시 뒤처지고, 거기서 인계문을 뽑으면 이 신고문의 증상이 그대로 나온다.   즉 갈라짐의 **기본값**은 뒤집었지만(안 갈라지는 쪽이 기본) **불가능하게 만들지는 못했다.**   닫힌 것처럼 적으면 그 남은 창이 장부에서 사라진다(F092 의 반대 방향).  ■ 다시 열 조건 / 남은 처방   · `전진` 실패가 실제로 얼마나 나는지 며칠 실측한다 — 실패가 드물면 ㉢(아무것도 안 한다)이 맞고,     잦으면 그때 ㉠(`myCommits` 범위를 origin/master 로 넓히고 «아직 안 받음» 이름표를 단다)을 짓는다.     ⚠ ㉠을 지을 땐 Session-Id 트레일러 필터를 그대로 걸어야 한다(남의 커밋이 섞인다).   · 실측 재료는 공짜로 쌓인다: `줄내기()` 가 실패 때 사유·처방을 한 줄로 찍으므로 화면에 남는다.  ■ 형제 = F565 는 다른 세션(`ac18eae8`)이 소비자 층에서 닫았다(PR #78 · `tools/lib/보드.js` 가지기준).   파일 겹침 0 을 대조했다 — 그쪽은 소비자, 이쪽은 생산자라 상보적이다. |
