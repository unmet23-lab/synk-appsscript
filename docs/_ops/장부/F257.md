| F257 | 2026-08-08 | 실수 | 「인계문 수거 — 죽은 세션 N건 (F111 통로)」 커밋이 실제로는 삭제를 싣는데 제목에는 그 말이 없다 — 실측 2026-08-08: 64d6b9b 는 제목이 「6건」인데 내용은 삭제 5 + 추가 1 이고, 40f763e 는 「3건」인데 삭제 2 + 수정 1 이다. 원인은 session-report.js writeHandoffFile ② 가 TTL 12시간 지난 세션 파일을 디스크에서 지우고, 그 D 가 인계문수거.js 의 커밋 범위(폴더+목차)에 그대로 함께 실리는 것이다 — 수거 도구 머리말이 그 TTL 을 이미 알고 있으면서 세는 것은 「거둘 것」뿐이라 삭제는 숫자에도 제목에도 안 나온다. 손해는 이력 오독이다: 커밋 목록만 보면 「N건을 보호했다」로 읽히는데 실측하면 로컬 master 의 인계문이 origin 보다 4개 적다(49 vs 53). 파일 자체는 삭제 이전 이력에 남으므로 소실은 아니지만, 「수거=보호」라는 이름이 「만료=삭제」를 가리는 것이 병이다. 처방 후보: ①커밋 제목을 두 수로 가른다(수거 N건 · 만료 M건) ②만료 삭제를 수거 커밋에 태우지 않고 별도 커밋으로 분리한다 ③최소한 보고문에 삭제 건수를 센다(지금 조사() 는 D 를 세지 않는다). | 3c64201 — 조사() 가 git status 항목을 파일명만 보고 갈랐다(xy 는 보고문에만 찍고 분류에 안 썼다). 그래서 session-report 의 12h TTL 이 지운 자국 D 가 「수거」에 들어갔고 제목의 숫자가 그 합이 됐다. 고친 자리는 분류 한 줄이고 동작은 그대로다 — 삭제도 커밋 범위에 그대로 넣는다(안 실으면 인계문 자리가 영영 더럽고, 기존 회귀 「추적-삭제도 거둔다」가 그 자리를 이미 못박는다 · 변이②가 그 회귀까지 빨갛게 만든다). 가른 것은 세는 이름뿐이라 제목·본문·보고문·완료문 네 자리를 함께 갈랐다 — 「거뒀다」와 「지워진 것을 정리했다」는 반대 사건이라 한 곳만 고치면 나머지가 옛말을 한다. 처방 ①(제목 두 수)을 택했고 ②(별도 커밋 분리)는 안 썼다: 만료만 있는 판에서 수거 커밋이 아예 안 나면 그 자리가 계속 더럽고, 그 사각을 회귀로 못박았다. 신고문 실측 둘을 정정한다 — 「로컬 49 vs origin 53」은 지금 49 vs 49 로 push 지연이었지 유실이 아니고, 유실 축 자체가 없다(--hook 도 실행()을 타 12h 안에 사실상 매번 커밋되고, git 의 D 는 추적 파일에만 생기니 실린 삭제는 전부 이력에 있다). 즉 이건 유실이 아니라 이름의 병이다. 회귀 2 신설·19/19·훅이식성 13/13·skip 0·변이 5/5. 🔴 첫 픽스처가 git 의 rename 감지에 걸렸다 — 두 파일에 같은 템플릿을 써서 내용이 거의 같았고 git 이 삭제+추가를 이름 변경으로 합쳐 --name-only 에 새 이름만 나왔다. 도구가 삭제를 제대로 실었는데도 안 실은 것처럼 보였다(재는 층이 값을 바꾼 자리). 내용을 다르게 줘서 갈랐고 주석에 못박았다. |
