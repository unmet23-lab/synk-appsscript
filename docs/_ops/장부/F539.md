| F539 | 2026-08-17 | 실수 | 계약을 형제(talk) 쪽에서 고치면 `tools/계약동기화.js` 가 **경고 없이 덮어 지운다** — 정본 방향이 파일 안에도 도구 출력에도 안 적혀 있다. 2026-08-17 실측: talk `계약/수집_교정_계약.json` 에 exam.result 규격 3벌(필드 그룹·라이브대응없음·주석 5칸·개정 절)을 넣고 회귀까지 돌린 뒤, 형제 초록 검사가 「두 저장소 계약이 같아야 한다」로 빨개져 동기화를 돌렸더니 **appsscript 판으로 통째로 덮여 편집이 전부 사라졌다**(talk 쪽은 그때까지 미커밋이라 git 이력에도 없다 — F025 형태). 도구 출력은 「형제에 정본을 넣었다」 한 줄이라 **덮은 사실은 말하지만 무엇을 지웠는지는 안 말한다**(diff 0줄). 계약 파일의 `고치는_법` 도 "두 저장소 모두에 같은 바이트로 넣는다" 까지만 적고 **어느 쪽이 정본인지**를 안 적는다 — 그래서 형제 쪽 파일을 열어 고치는 것이 자연스러운 첫 수가 된다. 🔑 새는 방향은 「조용한 소실」이다: 동기화가 성공으로 끝나고 회귀도 초록이 되어, 편집이 사라진 것을 **다음에 그 값을 찾을 때까지** 아무도 모른다. 처방 후보 = ①동기화가 덮기 전 대상 파일이 정본과 다르면 그 diff 를 stderr 로 내고 `--덮어쓰기` 없이는 멈춘다 ②계약 JSON 머리에 「정본 = appsscript · 사본 = talk」 한 줄 ③talk 쪽 파일을 읽기 전용 표식으로. | ⏸ 신고만 하고 안 고쳤다 — 처방이 계약 동일 ✓ 2개 (줄바꿈 제외) — 계약\문법급수_계약.json · 계약\수집_교정_계약.json 의 덮어쓰기 계약을 바꾸는 것이라 계약 트랙 몫이고, 이 세션은 잃은 편집을 정본에 다시 넣어 복구했다(as e5ad6ca4 = talk 4e6bf4a 와 같은 바이트). 다음에 계약을 고치는 세션이 같은 자리를 또 밟는다 — 그때 닫는다. |
