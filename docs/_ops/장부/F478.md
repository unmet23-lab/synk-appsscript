| F478 | 2026-08-15 | 마찰 | 운영에 민 Edge Function 이 **실제로 부팅하는지** 확인할 읽기 통로가 없다 — 바이트만 재고 「돈다」고 쓴다  실측 2026-08-15(세션 `bde22e62` · 조용한 실패 ③ 운영 배포 뒤).  ■ 무엇이 비었나 동봉 누락의 실패 모양은 이 저장소가 이미 아는 것이다 — 「배포는 ✅ 로 끝나고 **첫 호출의 import 에서 죽는다**」(`tools/원격배포.js:117·131·139` 가 세 번 적어 뒀다). 그런데 그 「첫 호출」을 **운영에서** 안전하게 해 볼 통로가 없다:   · `배포대조` 는 **바이트만** 잰다 — 파일이 거기 있다는 것이지 로드된다는 것이 아니다.   · `원격배포 --호출` 은 anon 키로 POST 하므로 `correct` 같은 service_role 함수에서는     **401 이 함수 코드에서** 나온다 = 모듈 그래프가 섰다는 완벽한 증거다(리허설에서 실측했다:     `{"error":"service_role 만 부를 수 있습니다"}`). 그런데 이 갈래가 **운영에서는 과녁 게이트에     막힌다** — `자격증명.읽기('원격배포', {읽기:true})` 선언이 없다.   · 그리고 그 자리 차단문은 「`--운영` 으로 우회하지 마십시오」라고 직접 적어 뒀다(F462). 옳다.  ■ 왜 도구 결함으로 접으면 안 되나 (처방이 안 나오는 이유) `--호출` 은 **읽기가 아니다.** `events`·`correct` 에 본문을 실어 보내면 그게 곧 쓰기·제출이다(F452). 그러니 `{읽기:true}` 를 통째로 다는 것은 틀린 처방이고, 게이트가 막은 것은 정상 동작이다. 비어 있는 것은 「**부작용 없는 부팅 확인**」이라는 갈래 자체다 — 지금은 그 갈래가 `--호출` 안에 숨어 있어서, 안전한 호출과 위험한 호출이 한 낱말을 쓴다.  ■ 이번엔 어떻게 넘겼나 (우회 아니고 추론이라고 밝힌다) 운영 `correct v6` 의 부팅은 **직접 안 쟀다.** 근거는 ①배포대조 바이트 8/8·앵커 138/138 로 리허설과 **바이트가 같다** ②그 같은 바이트가 리허설에서 401 을 냈다 ③import 성패는 번들 내용의 함수다. 강한 추론이지만 추론이다 — 「쟀다」로 적지 않는다.  ■ 처방 후보 (고르는 것은 이 신고 밖)   ㄱ) `--부팅확인` 갈래 신설 — **GET** 을 보낸다. `correct` 는 `req.method !== 'POST'` 를 맨 앞에서      405 로 끊으므로 본문도 인증도 필요 없고, 405 가 오면 모듈이 섰다는 뜻이다. 이 갈래만      `자격증명.읽기(..., {읽기:true})` 로 선언하면 운영에서도 쓰기 키 없이 지난다.      ⚠ 대가: 함수마다 「GET 을 어떻게 받나」가 달라서 405/404/200 중 무엇이 정상인지 표가 필요하다      — 그 표가 낡으면 「맞는 얼굴로 틀린 값」이 된다(맹점 ④).   ㄴ) 배포 직후 `원격배포` 가 스스로 GET 을 한 번 쳐 보고 결과를 배포장부에 적는다(도구 하나 안 늘어난다).   ㄷ) 안 한다 — 동봉 게이트가 앞을 막고, 바이트 대조가 뒤를 받치므로 잔여 위험이 작다.      ⚠ 다만 그러면 「운영 부팅은 영영 안 재는 축」으로 남는다는 것을 알고 두는 것이다. | ⏸ 이 세션은 신고만 하고 안 고쳤다. 처방 셋(ㄱ --부팅확인 GET 갈래 신설 / ㄴ 원격배포가 배포 직후 스스로 GET / ㄷ 안 한다)이 다 대가가 있어 픽이 필요하고, ㄱ은 함수별 「정상 응답 코드」 표를 새로 들어야 해서 낡으면 맞는 얼굴로 틀린 값이 된다(맹점 4). 지금 잔여 위험은 작다 — 동봉 게이트가 앞을 막고 배포대조 바이트가 뒤를 받친다. 경량 원칙상 새 장치는 실사용 실측 후 건별이라, 운영 배포에서 실제로 부팅 사고가 한 번 나면 그때 ㄱ 또는 ㄴ으로 연다. |
