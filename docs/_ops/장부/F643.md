| F643 | 2026-08-18 | 마찰 | 인계문 도구가 인계문을 지운다 — 클라우드 세션에서 `docs/_ops/인계문/` 43개가 한 번에 사라졌다(직전 세션 실측 · 그 세션이 복원했으나 미추적이었으면 유실이었다 · F025). 자리는 `session-report.js writeHandoffFile ②`의 TTL 청소다: 낡음 판정을 파일 **안**에 적힌 `at`(쓴 시각) 하나로만 하는데, 갓 클론한 저장소에서는 커밋돼 내려온 인계문 **전부**가 12시간을 넘긴 것으로 읽힌다 — 남이 버리고 간 유물이 아니라 git 이 방금 내려준 정본인데도. 하필 이 통로가 지문 없는 계정에서 제일 위험하다: 대화 맥락이 안 넘어가 **인계가 유일한 통로인 자리**(F096)를, 인계 도구가 스스로 지운다. 곁가지로 같은 함수에서 하나 더 실측됐다 — ponytail 가지(지문 모르면 쓰기마다 다른 파일)가 **안 돌고 있었다**: `/close` 통로가 `sessionId: sid || '?'` 로 부르는데 `id모름` 은 빈 값만 봐서, `파일용id('?')` 가 `_.md` 로 접혀 지문 없는 세션끼리 한 칸을 서로 덮었다(고친 줄이 자기 호출부 때문에 안 도는 자리 · 가드 맹점 ③). | `session-report.js` 의 TTL 청소를 **지우는 쪽만 fail-closed** 로 돌렸다 — 삭제는 조건 **둘 다** 넘어야 한다: ①내 지문을 안다(`'?'` 도 「모른다」로 친다 — 그 한 글자가 ponytail 가지를 통째로 안 돌게 하던 자리다) ②파일 **mtime 도** 낡았다(`낡은파일인가()` 신설 — 갓 클론·갓 체크아웃은 mtime 이 방금이라 이 조건이 혼자서도 막는다. ①만으로는 **지문이 있는** 클라우드에서 그대로 새는데, 그게 같은 사고의 두 번째 입구다). 안 걷은 것은 버리지 않고 목차에 링크로 남기고, 안 걷은 건수를 stderr 로 밝힌다(F207 — 미실행은 통과와 같은 모양이다). 회귀 `tests/인계문TTL.test.js` 4건이 **양쪽을 같은 무게로** 못박는다 — 안 지우는 쪽(모를 때·갓 내려온 파일)뿐 아니라 **여전히 지우는 쪽**(지문 있고 mtime 도 낡음)까지. 변이 실측: 삭제 가드를 빼면 4건 중 2건이 붉어지고, `'?'` 처리를 되돌리면 1건이 붉어진다(둘 다 되돌리면 초록이 아니다). 대가 = 걷기가 **늦어진다**(영구 누수 아님 — 지문 있는 세션이 다음에 걷고 mtime 도 12시간이면 다시 대상이 된다). 닫을 것 = 없다(삭제를 막는 안전 가드라 억지로 닫지 않는다). |
