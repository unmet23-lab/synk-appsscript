| F505 | 2026-08-16 | 실수 | 되감기의 「Stop 경계」 판정이 틀렸다 — 그리고 회귀가 그 버그를 통과시켰다(픽스처에 사람 턴이 0이었다)  이 축의 **네 번째** 거짓양성이다(F500 이 셋). 이번엔 내가 «오늘» 짜서 «오늘» 커밋한 도구다.  ■ 무엇이 틀렸나 훅은 `Stop` 에서 돈다 = 사람이 보낸 턴마다 한 번. 나는 그것을 「도구를 안 부르는 assistant 레코드」로 판정했다. 그런데 한 턴의 응답은 [텍스트]·[도구]·[텍스트] 로 **쪼개져** 레코드가 되므로 턴 «중간»이 전부 Stop 으로 걸린다. 그래서 한 턴 안에서 임계를 여러 개 뛰어넘은 세션의 발동 횟수가 부풀려졌다. 실측 정정: 발동 합계 **662 → 607**(-8.3%) · 세션당 최대 **16 → 14** · 중앙 302k → 343k. (결론인 「600k+ 19세션 침묵 0」은 안 바뀐다 — 최대 ctx 는 판정과 무관하다.)  ■ 🔑 어떻게 잡혔나 — **정답이 있는 표본으로 대조했다** 집계는 그럴듯했다(F500 의 세 판이 전부 그랬다). 잡은 것은 «내가 답을 아는» 표본 하나였다: **이 세션의 전사**. 사용자 메시지가 2개인데 내 판정은 **95**를 셌다. 47배다. 집계로는 영원히 안 보였을 오차다 — 47배가 나와도 표는 여전히 그럴듯하다.  ■ 🔴 급소는 도구가 아니라 **회귀**다 회귀 9건이 전부 초록이었는데도 이 버그가 살아 있었다. 까닭 = **픽스처에 사람 턴이 아예 없었다.** 전사를 `[턴, 턴, 턴]` 로만 지었으니 「턴 경계가 어디냐」는 질문 자체가 픽스처에 없었고, 그래서 어떤 판정을 넣어도 초록이었다. **픽스처가 실물의 한 축을 통째로 빼면 그 축의 버그는 구조적으로 안 걸린다** — 「탐지력은 픽스처가 진다」의 뒷면이다. 변이 7/7 도 이걸 못 봤다: 변이는 «있는 검사»의 탐지력만 재고, **없는 검사**는 못 만든다.  ■ 처방 (기계로) ① 판정을 `진짜사용자턴()` 하나로 — `tool_result` 는 role 이 user 라도 경계가 아니다. ② 픽스처 전부를 `대화()` 로 다시 지어 **사람 턴을 실제로 놓는다**. ③ 회귀 2건 신설: 「텍스트 전용 레코드를 Stop 으로 세면 발동이 부풀려진다」·    「도구 결과는 사람 턴이 아니다」 — 둘 다 옛 판정이면 빨개진다. 회귀 9 → 11 · 전부 초록. | |
