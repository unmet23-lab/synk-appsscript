| F651 | 2026-08-19 | 마찰 | Loom 의 「낮」 층이 통째로 @media print 안이라, **밝은 «화면» 지면이라는 자리가 아예 없었다** — 그래서 `인쇄부품` 을 밝은 지면에 얹으면 제목·링크가 사라지는데, 부품(유리 원판·칩)은 어두운 물체라 멀쩡히 보이므로 「얹혔다」가 초록으로 뜬다. 실측 2026-08-19(엔진 지도 렌더 전/후 대조): Chalk #E4E4E7 제목 on Paper #FAFAF9 = **1.21:1**(기준 3) — 밝은 칸 전부에서 h2 가 사라졌고, 지면방 전파 술어(마커)는 그대로 「입음」이었다. 발표물 3벌에도 같은 형태로 11+1건. 새는 방향이 언제나 「통과」인 자리다. 곁가지 둘도 같은 트랙에서 실측으로 드러났다: ①밝은 층이 남의 맨 글자(p·li·td)까지 Ink 로 만들면 밝은 지면 «안»의 어두운 섬(header 띠)이 통째로 사라진다 ②남의 blockquote 는 «판» 위에 앉을 수 있어(개인정보 게시본 = Chalk 판) Ash 2 로 흐리면 4.46:1 로 렌더린트가 문다 — 판 색을 모르면 «흐림»으로 대비를 보장할 수 없다. 그리고 발표물빌드에 `.룸` 을 씌울 때 어두운 칸을 클래스 이름(dark·cover)으로 가르려던 첫 판은 03 브로셔의 표지를 놓쳤다 — 이름 규칙은 다음 덱에서 조용히 샌다. | tools/lib/loom.js 에 «밝은글» 층 + «밝은부품» 프리셋 신설(값은 밝은글자() 한 곳에서 낮·밝은글 둘 다 읽는다) · 발표물빌드가 «휘도로» 밝은 칸에만 .룸 을 씌운다 · ol 손번호는 .민 옵트아웃 · 손 HTML 은 tools/지면얹기.js 가 등록층+낡음 적색을 진다 · 회귀 tests/지면얹기.test.js 10건(픽스처 8 + 실저장소 거짓양성 2) |
| F651 | 2026-08-19 | 실수 | master 를 안 당겨 본 채 착수해 **옆 세션이 이미 낸 공용 통로를 통째로 다시 지었다** — `tools/lib/룸입히기.js` 를 짓고 회귀 8건까지 붙인 뒤에야 `95761ba`(`tools/lib/loom얹기.js`)를 봤다. 이 트랙이 막으려던 사고(통로가 둘이면 갈라지고, 갈라지는 방향은 늘 「통과」)를 **내가 저질렀다.** 철거하고 master 판을 채택했다(그쪽이 넓다 — `범위진단()`·`</head>` 폴백·`얹힘()`/`내가얹음()` 분리). 🔑 staleness 훅은 편집 «시작» 때 「5 커밋 뒤」라고 이미 경고했는데, 그 경고를 **읽고도 트랙 판정을 다시 안 했다** — 경고를 「환경 안내」로 읽고 「지금 짓는 것이 이미 있나」를 안 물었다. 처방 후보: 새 `tools/lib/*.js` 를 **짓기 전에** `git log origin/master --since=1day -- tools/lib` 한 줄. | |
