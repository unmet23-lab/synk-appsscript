| F651 | 2026-08-19 | 실수 | master 를 안 당겨 본 채 착수해 **옆 세션이 이미 낸 공용 통로를 통째로 다시 지었다** — `tools/lib/룸입히기.js` 를 짓고 회귀 8건까지 붙인 뒤에야 `95761ba`(`tools/lib/loom얹기.js`)를 봤다. 이 트랙이 막으려던 사고(통로가 둘이면 갈라지고, 갈라지는 방향은 늘 「통과」)를 **내가 저질렀다.** 철거하고 master 판을 채택했다(그쪽이 넓다 — `범위진단()`·`</head>` 폴백·`얹힘()`/`내가얹음()` 분리). 🔑 staleness 훅은 편집 «시작» 때 「5 커밋 뒤」라고 이미 경고했는데, 그 경고를 **읽고도 트랙 판정을 다시 안 했다** — 경고를 「환경 안내」로 읽고 「지금 짓는 것이 이미 있나」를 안 물었다. 처방 후보: 새 `tools/lib/*.js` 를 **짓기 전에** `git log origin/master --since=1day -- tools/lib` 한 줄. | |
