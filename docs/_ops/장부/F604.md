| F604 | 2026-08-18 | 마찰 | 인계문수거가 **부분 적용해 놓고 「미커밋 그대로」라고 보고한다** — 장치가 안 도는 쪽이 아니라 「맞는 얼굴로 틀린 값」 쪽으로 샌 여섯 번째다.  실측 2026-08-18 (`local_96066cfd` 세션 시작 · 우연히 잡은 게 아니라 그 화면이 그 상태였다): · SessionStart 훅 출력 = 「[인계문수거] 거두지 못했다(git add 실패: … index.lock: File exists …) — 미커밋 그대로, 다음 실행이 다시 시도한다」. · 같은 순간의 `git status` = 인계문 경로 **15건이 스테이징돼 있었다.** 만료 삭제만 보면 알파벳 순으로 `02e398f7 1a1da987 1cc21d07 4af2d397 4e976ff7 524b5160` 여섯이 staged 이고 `54bf2c70` 부터 열여섯이 unstaged — **정확한 접두 절단**이다. 즉 `실행()` 의 경로별 add 루프가 앞 여섯을 앉힌 뒤 일곱 번째에서 잠금에 걸려 빠져나갔고, 보고문만 「그대로」라고 말했다. · 옛 회귀는 그 자리를 **실패«는» 봤다**(`tests/인계문수거.test.js:313` = 「사라짐 밖의 add 실패는 전과 같이 전체를 접는다」). 인덱스가 어떤 꼴로 남는지는 한 줄도 안 쟀다 — 새는 방향이 「통과」인 전형이다.  ■ 왜 신호인가 — 계보가 여섯이다 F400(배포대조) → F435·F436(원격SQL) → F493·F562(고아 잠금) → F543(분모 없는 ✅ 두 도구) → 이번. 전부 **쓰는 쪽이 관측 없이 문장을 확정**했다. 이번 자리의 특이점은 그 문장이 **부정 표기**라는 것이다: 「미커밋 그대로」는 「아무것도 안 했다」는 주장인데, 부정은 재기 전엔 알 수 없다(CLAUDE.md 「어디에도 없다」는 열 대상이 없다 · F348). 장치가 자기 부작용을 안 재고 부정을 말하는 자리는 여기 말고도 있을 것이다.  ■ 실측으로 갈린 것 — `git add` 의 원자성은 실패 사유마다 다르다 · 잠금(index.lock) → **0건 앉는다**(원자) · 경로 부재(`did not match any files`) → **0건 앉는다**(원자) · 무시 경로(.gitignore) → **나머지는 앉는다**(원자가 아니다) 그래서 「원자 add 로 바꾸면 이제 안 남는다」는 단정도 거짓이 된다. 이 셋을 안 갈랐으면 문장 하나를 다른 거짓 문장으로 바꿀 뻔했다.  ▶ 수리 = PR #102 (`04c74e33`). ㉠원인: 원자 add 를 먼저 쳐서 잠금 실패에 부분 적용이 원리상 안 생기게 하고, 경로별 루프는 F071(선점) 때만 돈다 ㉡대가: 그래도 남을 수 있는 판은 실패 뒤 인덱스를 **세어**(`인덱스잔여`) 보고하고, 못 재면 못 쟀다고 말한다(F207). 되돌리기(reset)는 안 넣었다 — 방금 실패한 그 잠금을 reset 도 똑같이 필요로 해서 못 미더운 층이 하나 느는 것뿐이다. 회귀 6 신설(27/27) · **변이 6/6 구멍0 미적용0**.  ⚠ 안 한 것 = **같은 모양 전수 감사**. 「장치가 자기 부작용을 안 재고 부정을 말한다」가 이 저장소에 몇 자리인지는 안 셌다 — 이 신고는 그 재료다. | |
