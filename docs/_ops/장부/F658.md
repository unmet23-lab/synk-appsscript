| F658 | 2026-08-19 | 교정 | **구판 마스코트를 또 썼다 — 같은 원인 세 번째.** 유호 교정 「왜 마스코트는 진짜 예전꺼를 왜 아직도 쓰는거야? 몇 번을 말해?」. 캐러셀 7컷 전부 `마스코트_누끼`(체리 젤리 = 구판)였고, `마스코트_동반자층` 머리도 `마스코트_렌더`(확정 «전» 판)였다. 정본은 토큰 `재질.펠트.정본사진.평상복` = 펠트 코랄(유호 픽 08-15 ㉠재염색)이고, 체리는 «특별한 순간»(무오류·기념일) 전용이다. 🔑**앞선 두 번의 수리가 «그 호출부»만 고쳤다** — F443 은 재질굽기, F479 는 브랜드킷 조립 도구를 고쳤을 뿐 **옛 통로는 살려 뒀다.** 그래서 아무도 안 본 지면(캐러셀·동반자층)에 그대로 남았다. 나 역시 캐러셀에 Loom 을 입히며 그림 경로를 «확인 대상»으로 안 봤다. | ✅ **옛 통로를 테스트로 금지**했다 — `tests/마스코트정본.test.js`: 지면방 「입는다」 전량에서 `마스코트_누끼`·`마스코트_렌더` 를 `src`/`url()` 로 가리키면 fail(주석 언급은 위반 아님). 정본 목록은 손으로 안 든다 — 토큰에서 읽어 정본이 갈리면 검사가 따라간다. 변이 검사로 탐지력 확인(구판으로 되돌리니 물었다). 실저장소 2건(캐러셀 7컷 + 동반자층 1컷) 수리. ⚠**남은 구멍**: 정본 펠트코랄 누끼는 표정이 **3종**(본체·눈감음·눈웃음)뿐이라, 캐러셀이 쓰던 「놀람」·「비니」는 정본에 없다 — 그 두 칸은 가장 가까운 표정으로 대체했고 유호님 판정 자리다. 그리고 피사체 비율이 달라(82.1%→55.7%) 상자를 47→69cqw 로 다시 쟀다. |

> **후기 08-23** — 위 처분의 가드 `tests/마스코트정본.test.js` 는 그 뒤 `ecd555ea` 에서 **삭제됐다**(그 커밋은 대가를 정직히 적었으나 이 장부는 갱신이 안 돼, 장부만 읽는 세션이 있지도 않은 가드를 신뢰했다 — 08-23 전수조사 실측). 그리고 같은 계열 **다섯 번째**가 08-23 터졌다(유호 「몇 번 말했는지 기억도 안 나」): 역대 분모가 전부 appsscript 안이라 **형제 저장소 talk 의 앱 컷**(08-13 변환본 6벌 · 원본 삭제 뒤에도 사본은 산다)은 다섯 번 다 검사 밖이었다. 처방 = 옛 컷 소각 + talk 회귀가 표정컷 전 값에 `재염색_` 접두 요구(talk `11c5848` · 결정.md 08-23 행).
