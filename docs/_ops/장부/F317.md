| F317 | 2026-08-10 | 마찰 | rot-check 가 「유호님이 세워 둔 것」과 「아직 안 고친 것」을 같은 🔴 로 뿜는다 — 매 세션 적색 27건 중 25건이 철학 정본 v1.0→v1.3 파생 전파였고, 그건 유호님이 보류시킨 것이다(정본 머리 명문 · memory smart-definition 「범위가 너무 커 토큰 낭비 · 일괄 리라이팅을 지시 없이 착수하지 않는다」). 사고 둘이 같이 난다: ①진짜 적색 2건(라이브 낡음·노트북LM)이 25건 소음에 묻혀 안 보인다 — 적색이 신호를 잃는다 ②세션이 그 25건을 「놓친 일감」으로 읽고 집는다 — 이번 세션(ceba5190)이 /missed 로 실제로 집어 5파일 7곳의 구판 화법까지 특정했다가 정본 머리를 읽고서야 되돌렸다(유호 확정을 지시 없이 뒤집을 뻔한 자리다). 「보류」를 말할 통로가 기계에 아예 없어서, 보류는 적색으로만 표현될 수 있었다. | tools/doc-graph.js 에 정본 자기선언 `<!-- 전파보류: vN -->` 신설(기존 `<!-- 정본: vN -->` 과 같은 계열 · maskCode 경유) — stale 을 staleHeld 로 갈라 rot-check 가 🔴 가 아니라 ⏸ 로 **세어서** 보인다(숨기지 않는다: 숨기면 「없는 것」이 된다). 실측 적색 27→2. 🔑 급소는 만료다 — 도장에 버전을 박아 보류는 그 판에서만 산다: 정본이 v1.4 로 오르면 도장(v1.3)이 낡아 파생이 자동으로 다시 적색이 된다(버전 없는 도장이었으면 이 통로 자체가 영구 은폐 장치가 된다). 보류는 파생 25곳이 아니라 정본 한 곳에만 적는다(형식이 흔들리면 파서가 조용히 놓친다). 회귀 5건 tests/문서그래프.test.js — 픽스처가 탐지력을 지고(보류 분류·만료 복귀·도장 없을 때 적색 유지·코드블록 예시 오탐) 실저장소에는 거짓양성만 검사(적색과 보류 교집합 0). |
