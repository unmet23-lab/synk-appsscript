| F641 | 2026-08-18 | 마찰 | **클라우드 세션(지문 없음)에서 `tools/인계문.js` 가 「못 잰 것」을 0으로 내고, 그 0을 근거로 세션 인계문 43개를 삭제한다** — 2026-08-18 `/close` 실측.  증상 셋(한 번 실행에서 동시에): ① 이 세션이 낸 커밋 6건을 하나도 못 찾고 **「커밋 없음」**을 낸다. ② 그 위에 **「트랙 없이 끝난 세션이다 — 추정이 아니라 실측이다」**라고 단정형으로 적는다. 못 잰 것을 잰 것처럼 말한다(F207 의 교과서 형태). ③ `docs/_ops/인계문/` 의 세션 파일 **43개를 지우고** 사본 `docs/_ops/인계문.md` 를 61줄 깎는다(45개 → 3개). 남의 세션 인계문까지 통째로 날아갔다.  원인 한 곳: `tools/인계문.js:60` `const sid = report.hostSessionId('')` — 커밋 트레일러에 박히는 그 값이다. 클라우드 세션은 `CLAUDE_CODE_HOST_SESSION_ID` 가 **빈 문자열**이라 ㉠내 커밋 판별과 ㉡「살아있는 세션」 판별이 **동시에** 눈이 먼다. 살아있는 세션이 0으로 보이니 TTL 정리가 전부 고아로 판정해 지운다. `prepare-commit-msg` 도 같은 환경에서 트레일러를 못 박아, 사후 대조로도 못 찾는다.  급소는 ③이다. ①②는 「빈 블록」이라 다음 세션이 보드·장부로 우회할 수 있지만, ③은 **되돌릴 재료를 없앤다**. 이번엔 전부 추적 파일이라 `git checkout -- docs/_ops/인계문/` 으로 43개 전량 복원했고 커밋은 안 했다. 미추적이었으면 영영 유실이다(F025).  처방 후보(판정 전) — ㉠ `hostSessionId` 가 빈 값이면 **삭제 경로를 통째로 건너뛴다**(지우는 쪽만 fail-closed · 출력은 계속). ㉡ 「커밋 없음」과 「못 쟀다」를 가른다 — 지문이 없으면 단정형 문장(「추정이 아니라 실측이다」)을 안 낸다. ㉢ 커밋 판별을 트레일러 단독이 아니라 `HEAD 대비 origin` 같은 층과 함께 본다.  ⚠ 안 잰 것: 다른 클라우드 세션들이 이 구멍을 이미 통과했는지는 못 쟀다. 오늘 06:38·06:43 의 미추적 인계문 2건이 남아 있는데 그것이 이 증상의 흔적인지 정상 산출인지 판정 안 했다(남의 것이라 안 건드렸다). | 도구가 사라져 이 구멍도 사라졌다 — tools/인계문.js 는 유호 확정 08-20 「위임 1단계」로 삭제됐다(8780e5f3 · 인계문 층을 하네스에 넘김). 빈 hostSessionId 로 삭제 경로가 눈머는 자리 자체가 코드에서 없어졌으므로 처방 ㉠㉡㉢은 지을 대상이 없다. ⚠ 이 해소는 «고쳐서»가 아니라 «층을 걷어서»다 — 하네스 인계에 같은 병(못 잰 것을 0으로 접기)이 있는지는 이 세션이 안 쟀다. |
