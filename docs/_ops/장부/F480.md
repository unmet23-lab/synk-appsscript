| F480 | 2026-08-15 | 실수 | 비용 실측을 **견줄 대상 없이** 적었더니 그 숫자가 하루짜리 차단자가 됐다 — 대기열 #Q87 이 「as 세션이 형제 배포빚을 못 본다」의 가장 자연스러운 처방(ㄱ-a: 형제 도구를 부른다)을 **「세션 시작 경로에 1.67초를 얹는다」** 하나로 기각하고 표식을 ⛔선행으로 내렸다. 오늘 다시 재니 둘 다 틀렸다: ①그 도구는 **390ms** 다(3회 = 352·380·441 · 1.67초의 4.3배 과장 — 옛 측정은 「2회 평균」이라 적혀 있었으나 재현 안 됨) ②그리고 **부르는 자리가 이미 10~14초다**(`작업본소유자 --hook` 3회 = 10.4·12.8·13.9초). 즉 얹는 값이 그 도구의 회차 편차(±5초)에 잡히지도 않는데, 절대값만 적어 두니 「1.67초」가 크게 읽혔다. 🔑 급소는 「낡은 숫자」가 아니라 **분모 없는 숫자**다 — 이 저장소는 「0은 분모와 함께 쓴다」를 이미 확정했는데(유호 08-14) 그건 «개수»에만 걸려 있었고, **비용·시간에는 안 걸려 있었다.** 비용을 판정 근거로 쓸 때 「무엇에 견줘 얼마인가」가 없으면, 재보지도 않은 채 값싼 처방이 기각되고 그 자리는 ⛔ 로 잠긴 채 남는다(실측 손실: #Q87 이 그 표식으로 하루 서 있었고, 그동안 리허설 낡음 5·운영 낡음 4 가 as 세션 눈에 한 줄도 안 떴다). | |
