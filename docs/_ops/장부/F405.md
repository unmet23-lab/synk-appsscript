| F405 | 2026-08-13 | 마찰 | 보드 승계 규약이 만드는 줄은 **아무도 못 거둔다** — board-guard ④는 「죽은 세션의 트랙을 이어받으면 트랙 칸을 **바이트 그대로** 옮겨 적어라」라고 시키는데(그래야 중복 구현으로 안 막힌다), `tools/lib/보드.js` 의 `이관문구()` 는 트랙 칸에서 **표 전체에서 유일한** 부분문자열을 찾는다. 승계로 바이트가 같아진 두 줄은 어떤 needle 도 유일하지 않아 `null` 이 되고, 그러면 `board-move` 가 그 줄을 특정 못 해 `보드수거` 가 「문구를 못 고른 것」으로 스킵한다 — **규약을 지킬수록 안 치워지는 줄이 는다.** 실측 2026-08-13: `보드수거` 가 `표 16줄 · 완료 4줄 → 거둘 수 있는 것 0건 · 문구를 못 고른 것 1건(34297acc)` 을 냈고, 그 줄은 5c55e5f5 의 트랙 칸을 규약대로 바이트 그대로 승계한 것이다(둘 다 「과제선택 5회차 «전건 처분» — 처분표 v5(원건 82 매핑)→채택분 v5.2 집행」). 처방 후보: 유일성을 **표 전체**가 아니라 **대상 파일 안**에서 재는 것 — `보드수거` 는 이미 `SYNK_BOARD` 로 어느 파일인지 넘기고 있고(그쪽 `옮기기()`), 한 세션 파일 안에서는 같은 트랙 칸이 두 번 나올 이유가 없다. ⚠ 손대는 쪽이 조심할 것: `board-move` 를 문구만으로 부르는 통로(사람·board-guard 처방문)에는 파일이 안 실리므로, 그 경우의 폴백은 지금 동작(전체 유일성)이어야 한다 — 좁히면 엉뚱한 줄을 옮긴다. 이 세션(F395 트랙)은 상한 분모만 고쳤고 이 자리는 범위 밖이라 새 트랙으로 넘긴다. | |
