| F555 | 2026-08-17 | 마찰 | git-scope-guard 가 인계문 조각에서 구조적 거짓양성을 낸다. 인계문 생성기는 파일을 통째로 다시 쓰는데(append 아님), 같은 세션이 나중에 갱신한 최신판을 커밋하면 가드가 그것을 «남의 커밋 이전 판에서 갈라졌다»로 읽고 4줄 삭제로 막는다. 처방(되살려라)을 따르면 낡은 판으로 되돌아가고 생성기가 다음에 또 덮으므로 따를 수 없는 처방이다(F103). 실제로 BYPASS 가 정상 통로가 됐다. | `.claude/hooks/git-scope-guard.js` ⑨ 가 「이름이 곧 주인인 파일」(인계문 조각)을 되돌림 판정에서 뺀다 — 판정은 새로 안 짓고 `작업본소유자.인계문지문` 하나를 부른다(같은 판정을 두 곳에 적으면 갈라진다). 회귀 3건 신설 · 변이 **3/3 구멍 0 · 미적용 0** · 기준선 초록 **시험 103건** — `564eef94` (PR #76).  🔑 근본 원인은 가드 «로직»이 아니라 **기록 층**이었다: ⑨ 의 F241 알리바이(`커밋뒤만졌나`)가 읽는 `dirtyChecked` 는 track-collision 이 **Edit·Write 도구 호출에서만** 찍는데, 인계문은 session-report 의 `원자쓰기` 가 Node 에서 fs 로 직접 쓴다. 그래서 그 알리바이가 **원리상 참이 될 수 없었고**, 생성기가 파일을 통째로 다시 쓸 때마다 옛 줄이 「사라진」 것으로 읽혀 매번 막혔다. 🔴 같은 사실이 `작업본소유자.js` 273~282행에 **이미 적혀 있었다**(그래서 그쪽은 «이름»을 주인 증거로 쓴다 — 「이름은 기록보다 약한 증거가 아니라 더 직접적인 증거다」). ⑨ 만 그 결론을 못 받은 것이라, 처방은 새 판정이 아니라 **파생**이다.  ■ 여는 범위를 두 겹으로 좁혔다(새는 방향은 언제나 「통과」다)  ① 인계문 조각만. 보드 조각(`docs/_ops/보드/<지문>.md`)은 Write 도구로 쓰므로 알리바이가 정상 작동한다 — 같이 열면 넓히는 쪽이다.  ② **파일 이름의 주인 == 그 커밋의 주인**일 때만. 남이 그 인계문에 적어 둔 줄을 내가 덮는 것은 생성기의 자기 갱신이 아니라 진짜 되돌림이다.  두 경계는 음성 회귀 2건이 각각 잡는다(변이 ②③ 이 그것을 증명했다).  ■ ⚠ 과녁은 **⑨ 하나**였다 — 처방 직후 첫 회귀가 여전히 빨간 것을 보고 「처방이 안 먹었다」로 접을 뻔했다. 층을 바꿔 사유 전문을 읽으니 막은 것은 ⑧(「내 것이라고 증명되지 않는 변경」)이었고 ⑨ 는 이미 비켜난 뒤였다. ⑧ 이 한 번 펼치는 것은 정상이고 그 처방(「같은 명령을 그대로 다시 실행」)은 **따를 수 있다**(F141) — F555 가 「따를 수 없다」고 신고한 것은 ⑨ 의 「되살려라」뿐이다. 그래서 검사를 `차단 false` 로 재지 않는다: 그렇게 재면 ⑧의 정상 동작까지 결함으로 세고, 그 검사를 초록으로 만들려면 **과녁이 아닌 ⑧을 깎아야** 한다. 대신 축을 둘로 갈랐다 — ⑨ 가 「되돌림」으로 안 짖을 것 + ⑧ 이 펼친 뒤 **재실행이 실제로 통할 것**(= 남는 출구가 BYPASS 가 아님).  🔑 이 트랙의 커밋 자신이 그 경로를 그대로 지났다(변이가 fs 로 쓰고 원복해 편집 지문이 어긋나 ⑧ 이 펼쳤고, diff 를 읽고 재실행하니 통과했다) — 처방이 실사용에서 따를 수 있음이 실측됐다.  ■ 대가 (틀릴 때의 모습)  · 인계문 조각에 한해 ⑨ 의 보호가 없다. 그 파일은 세션 상태에서 **파생**되고 주인이 하나라 「남의 줄을 잃는다」는 손해가 원리상 없다 — 다음 생성이 다시 만든다. 그래도 남이 그 파일에 손으로 적은 줄은 위 ② 가 계속 지킨다.  · 닫은 것 = **없다**(안전 가드는 억지로 닫지 않는다). 이 완화는 규칙을 지우는 것이 아니라 원리상 판정 불가였던 자리를 비켜서게 한 것이다. |
