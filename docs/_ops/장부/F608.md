| F608 | 2026-08-18 | 마찰 | `tools/보안검토과녁.js` 가 **붙여넣으라고 준 문장** 안의 기준선이 자기가 실제로 쓴 기준선과 다르다 — 그대로 따르면 검토가 «0줄 diff» 를 본다.  실측 2026-08-18 (`local_0f50ee2f` · 라이브 배포 밀림 트랙): · 요약줄은 맞게 적는다 — 「(기준 = 프로젝트마다 다르다 — (루트)=라이브 `600741b9`)」. · 그런데 바로 아래 «채팅에 그대로 붙여라» 블록은 「마지막 채번 `bbd6477` 이후의 변경을 본다」로 **채번을 박아 낸다**. · `bbd64770` 은 HEAD 의 **조상**이고 배포집합 diff 가 **0줄**이다(`git diff bbd64770..HEAD -- <배포 4파일>` = 빈 출력). · /deploy SKILL 4단계는 「출력된 목록을 **그대로 붙여**」라고 지시하므로, 지시를 지킬수록 틀린 과녁을 넘긴다.  🔑 새는 방향이 **「통과」**다 — 검토자는 「변경 없음 = 볼 것 없음」을 내고, 그 0건이 「취약점 0건」의 얼굴로 배포 보고에 실린다. `--대조` 는 **파일 이름이 언급됐는지**만 보므로 이 거짓 초록을 못 가른다(이번에도 exit 0 이 나왔다 — 내가 기준선을 손으로 갈아낀 뒤였는데도 같은 exit 였다).  🔴 이 도구는 **자기 출력에 F474 경고를 이미 싣고 있다**(「채번 뒤에 배포가 끊긴 자리는 이 기준선이 원리상 0건을 낸다」). 즉 아는 결함인데 **처방문 자신이 그 결함을 재생산한다** — 경고는 산문이고 붙여넣기 블록은 기계라, 사람이 읽는 쪽과 따르는 쪽이 갈렸다.  처방 후보: 붙여넣기 블록의 기준선을 요약줄과 **한 곳에서 파생**시킨다(프로젝트별 실기준선 문자열 1벌). 라이브 기준선인 프로젝트는 「라이브 `600741b9` 이후」로, 채번 기준선인 프로젝트만 채번 SHA 로 적는다. | 처방 후보 그대로 채택 — 기준선 문장을 **한 곳에서 파생**시켰다(`192e67b4` · 가지 `claude/synk-continue-cbjau8` · PR). 신고문이 지목한 「요약줄과 붙여넣기 블록이 각자 갈래를 나눈다」가 원인이 맞았고, 고친 것은 문구가 아니라 **자리**다. ① `기준선문(s)` 신설 = ㉡ 을 무엇에 견줬나를 내는 한 문장 — `기준표기()`(요약줄)와 새 `붙일문()`(블록)이 둘 다 여기서 파생한다. ② `붙일문(s)` 신설 = CLI 안의 손 문자열을 순수 함수로 꺼냈다. **이게 급소다** — 옛 자리에서는 이 결함을 잡는 검사를 «쓸 수가 없었다»: 요약줄은 순수 함수라 픽스처가 갈래 셋을 다 태웠는데(`tests/배포기준선.test.js`) 블록은 CLI 문자열 이어붙이기라 회귀가 **0건**이었다. 즉 이 파일이 자기 출력에 F474 경고를 이미 싣고도 샌 이유는 「사람이 읽는 쪽(산문)과 기계가 따르는 쪽(블록)이 갈렸다」가 맞다. ③ 곁가지로 하나 더 잡았다(신고문엔 없던 것) — 옛 요약줄은 갈래가 «라이브실측 전부»일 때 `기준선[0].sha` **하나만** 냈다. 프로젝트마다 sha 가 다르면 그 한 줄은 나머지 프로젝트에 대해 「맞는 얼굴로 틀린 값」이다(맹점 ④). 이제 프로젝트가 둘 이상이면 갈래가 같아도 프로젝트별로 적는다. ④ 블록에서는 기준선을 **줄을 갈라** 적는다 — 한 줄에 이으면 「섞임」일 때 마지막 프로젝트의 sha 하나가 전체 범위처럼 읽혀 고친 병이 문장 층에서 되살아난다. ■ 못박은 것 = **문구가 아니라 동일성**이다. 갈래마다 「블록의 기준선 == 요약줄의 기준선」을 건다 — 두 자리가 각자 갈래를 나누는 한, 문구를 아무리 고쳐도 갈래가 느는 날 또 갈라진다(요약줄 셋 : 블록 둘 이 정확히 그 모양이었다). 회귀 6건 추가(23→29) · **변이 7/7 구멍 0 · 미적용 0**(기준선 초록 46건) · 인접 스위트 `배포기준선`·`배포판점검`·`검수과녁부모` 62 pass·2 skip·0 fail. ■ **안 재는 것**을 밝힌다(F348) — 블록의 기준선 «줄»을 CLI 끝까지 태우는 검사는 못 짰다. CLI 를 픽스처로 모는 이음매는 `SYNK_REVIEW_TARGETS` 하나뿐인데 그건 기준선을 `null` 로 두므로 그 줄을 **원리상 안 지난다**(이음매를 넓히면 그게 곧 우회로다). 그래서 CLI 로는 **배선만**(`붙일문()` 을 실제로 부르는가 · 변이 ⑦ 이 그 자리를 판다) 태우고 기준선 갈래는 순수 함수가 진다. ■ CI 모사 = 4538건 중 pass 4401 · **fail 3 · skip 129**. 셋 다 **내 변경과 무관한 선재 적색**이고 추정이 아니라 실측이다 — `origin/master`(49703d30)를 임시 워크트리로 떼어 같은 CI 모사 환경에서 그 세 파일만 돌리니 **같은 이름 셋이 그대로 빨갛다**: ㉠`시트도달` 형제 저장소 후보(이 클론엔 `../SYNK-talk` 이 없다) ㉡`장부유실` — **F595 가 장부에서 실제로 사라졌다**(`docs/_ops/장부/F595.md` 가 master 에 없고 `마찰신호.md` 에도 0건 · 원본은 `6350f7d7` 에 산다 · 남의 트랙이라 손 안 댔다) ㉢`지도대장` 바탕화면 없음(CI 모사 HOME 에 `Desktop` 이 없다). ■ **정정(같은 세션 · 착지 뒤 실물 실행에서 내가 잡았다)** — 첫 판 `192e67b4` 는 프로젝트 «개수»만 보고 둘 이상이면 「프로젝트마다 다르다」를 냈다. 형제 저장소가 붙은 실판에서 (루트)·crewcard 둘 다 같은 채번 `bbf44ca2` 인데 「프로젝트마다 다르다 — (루트)=채번 bbf44ca2 · crewcard=채번 bbf44ca2」가 나왔다 — 참값은 「기준선이 하나」다. 없는 갈림을 말하는 것도 **맞는 얼굴로 틀린 값**(맹점 ④)이고, 그건 이 트랙이 고치려던 병 그대로다. 규칙을 「갈래와 sha 가 전부 같으면 한 문장으로 접고 하나라도 갈리면 프로젝트별」로 고쳤다(`55a21960`). ⇒ **최종 수치 = 회귀 23→30 · 변이 9/9 구멍0·미적용0**(기준선 초록 47건) — 위 본문의 「29 · 7/7」은 그 정정 전 값이다. |
