| F608 | 2026-08-18 | 마찰 | `tools/보안검토과녁.js` 가 **붙여넣으라고 준 문장** 안의 기준선이 자기가 실제로 쓴 기준선과 다르다 — 그대로 따르면 검토가 «0줄 diff» 를 본다.  실측 2026-08-18 (`local_0f50ee2f` · 라이브 배포 밀림 트랙): · 요약줄은 맞게 적는다 — 「(기준 = 프로젝트마다 다르다 — (루트)=라이브 `600741b9`)」. · 그런데 바로 아래 «채팅에 그대로 붙여라» 블록은 「마지막 채번 `bbd6477` 이후의 변경을 본다」로 **채번을 박아 낸다**. · `bbd64770` 은 HEAD 의 **조상**이고 배포집합 diff 가 **0줄**이다(`git diff bbd64770..HEAD -- <배포 4파일>` = 빈 출력). · /deploy SKILL 4단계는 「출력된 목록을 **그대로 붙여**」라고 지시하므로, 지시를 지킬수록 틀린 과녁을 넘긴다.  🔑 새는 방향이 **「통과」**다 — 검토자는 「변경 없음 = 볼 것 없음」을 내고, 그 0건이 「취약점 0건」의 얼굴로 배포 보고에 실린다. `--대조` 는 **파일 이름이 언급됐는지**만 보므로 이 거짓 초록을 못 가른다(이번에도 exit 0 이 나왔다 — 내가 기준선을 손으로 갈아낀 뒤였는데도 같은 exit 였다).  🔴 이 도구는 **자기 출력에 F474 경고를 이미 싣고 있다**(「채번 뒤에 배포가 끊긴 자리는 이 기준선이 원리상 0건을 낸다」). 즉 아는 결함인데 **처방문 자신이 그 결함을 재생산한다** — 경고는 산문이고 붙여넣기 블록은 기계라, 사람이 읽는 쪽과 따르는 쪽이 갈렸다.  처방 후보: 붙여넣기 블록의 기준선을 요약줄과 **한 곳에서 파생**시킨다(프로젝트별 실기준선 문자열 1벌). 라이브 기준선인 프로젝트는 「라이브 `600741b9` 이후」로, 채번 기준선인 프로젝트만 채번 SHA 로 적는다. | |
