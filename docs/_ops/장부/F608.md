| F608 | 2026-08-18 | 마찰 | `tools/보안검토과녁.js` 가 **붙여넣으라고 준 문장** 안의 기준선이 자기가 실제로 쓴 기준선과 다르다 — 그대로 따르면 검토가 «0줄 diff» 를 본다.  실측 2026-08-18 (`local_0f50ee2f` · 라이브 배포 밀림 트랙): · 요약줄은 맞게 적는다 — 「(기준 = 프로젝트마다 다르다 — (루트)=라이브 `600741b9`)」. · 그런데 바로 아래 «채팅에 그대로 붙여라» 블록은 「마지막 채번 `bbd6477` 이후의 변경을 본다」로 **채번을 박아 낸다**. · `bbd64770` 은 HEAD 의 **조상**이고 배포집합 diff 가 **0줄**이다(`git diff bbd64770..HEAD -- <배포 4파일>` = 빈 출력). · /deploy SKILL 4단계는 「출력된 목록을 **그대로 붙여**」라고 지시하므로, 지시를 지킬수록 틀린 과녁을 넘긴다.  🔑 새는 방향이 **「통과」**다 — 검토자는 「변경 없음 = 볼 것 없음」을 내고, 그 0건이 「취약점 0건」의 얼굴로 배포 보고에 실린다. `--대조` 는 **파일 이름이 언급됐는지**만 보므로 이 거짓 초록을 못 가른다(이번에도 exit 0 이 나왔다 — 내가 기준선을 손으로 갈아낀 뒤였는데도 같은 exit 였다).  🔴 이 도구는 **자기 출력에 F474 경고를 이미 싣고 있다**(「채번 뒤에 배포가 끊긴 자리는 이 기준선이 원리상 0건을 낸다」). 즉 아는 결함인데 **처방문 자신이 그 결함을 재생산한다** — 경고는 산문이고 붙여넣기 블록은 기계라, 사람이 읽는 쪽과 따르는 쪽이 갈렸다.  처방 후보: 붙여넣기 블록의 기준선을 요약줄과 **한 곳에서 파생**시킨다(프로젝트별 실기준선 문자열 1벌). 라이브 기준선인 프로젝트는 「라이브 `600741b9` 이후」로, 채번 기준선인 프로젝트만 채번 SHA 로 적는다. | 2026-08-18 `8b76a232` — 기준선 문자열을 **한 곳에서 파생**시켰다. 신설 `기준조각(b)`(기준선 한 항목 → 글자 · 채번은 7자로 잘라 `배포커밋` 과 **같은 글자**를 낸다)와 `견줌문(기준선)`(견준 판이 하나면 한 번만 말하고, 갈리면 이름과 sha 를 **전부** 낸다)을 세우고 `기준표기`·신설 `붙임문` 이 **둘 다 그것만** 쓴다. 곁가지 둘도 같이 닫혔다: ①옛 섞임 표기는 `crewcard=채번` 이라 **sha 를 아예 안 냈고**(무엇에 견줬는지 되짚을 재료 0) ②옛 라이브실측 표기는 `기준선[0]` 하나만 봐서 프로젝트가 둘일 때 나머지를 접었다 — 이 저장소의 clasp 프로젝트는 실제로 **둘**(`(루트)`·`crewcard`)이라 「섞임」은 예외가 아니라 상시 조건이다. 🔑 **3번째라 원인을 쓸 수 없게 만들었다**(F449→F474→F608 셋 다 「기준선을 두 곳에 적었다」가 원인): 공용 통로를 세우는 것으로 그치지 않고 **옛 통로를 회귀가 금지**한다 — `tests/배포기준선.test.js` 의 `🔒 [옛 통로 금지]` 가 CLI 블록 소스를 읽어 `기준.배포커밋`·`기준.기준선`·`라이브 실측 판`·`마지막 채번` 접근이 다시 생기면 빨개진다. 회귀 신설 5건(급소 = 갈래 **넷**에서 요약줄과 붙임 블록의 sha **집합**을 대조 — 문구가 아니라 재료를 잰다) + 배선 1건(`고정돌리기` CLI 가 붙임 블록을 실제로 내는가 — 순수 함수만 세우고 안 부르면 회귀는 초록인데 장치는 안 돈다). 관련 회귀 45/45 · **변이 7/7**(구멍 0 · 미적용 0 · 기준선 분모 45). ⚠남는 한계 = 이 트랙이 고친 것은 **두 문장이 갈라지지 않는 것**까지다 — 기준선 자체가 옳은지(F474 의 ㄴ 갈래가 원리상 0건을 내는 자리)는 그대로다. ⚠전체 `test-ci` 4501 중 fail 3 은 **내 변경 무관**이고 실측으로 갈랐다: 같은 3건이 `origin/master` 워크트리에서도 똑같이 빨갛다(형제 저장소 없음·실폴더 없음·git 이력 층 — repo 밖 환경에 기대는 검사 · F296). ■ **델타(`cloud_ddd2c370` · PR #114 · 2026-08-18)** — 같은 F608 을 두 세션이 «선언 15분 차»로 각자 잡았다(F615 가 이름 댄 경합의 두 번째 실측 · 내 선언 04:22 `58183ca3` / 그쪽 선언 04:11 `e41ad2e4`). 그쪽이 먼저 머지됐고(PR #112 `c7359032`) **그 판이 내 판보다 강하다** — 「옛 통로 금지」 소스 검사까지 있다. 그래서 F615 처방 ㉢ 그대로 **내 구현 전량을 버리고**(`192e67b4`·`55a21960` 은 이 가지에서 되돌렸다) 델타 하나만 얹었다: 🔑 **붙임 블록에서 기준선을 «줄을 갈라» 적는다.** `견줌문` 이 「프로젝트마다 다르다 — (루트)=… · crewcard=…」를 낼 수 있는데 그걸 한 줄에 이으면 꼬리의 「이후의 변경을 본다」가 **마지막 프로젝트의 sha 에 붙어** 읽힌다 — 붙여 받은 사람이 그 하나를 전체 구간으로 잡으면 나머지 프로젝트 diff 가 과녁 밖으로 떨어진다. 즉 이 절이 없애려던 실패가 **문장 층에서** 되살아나는 자리이고, 새는 방향은 여전히 「통과」다. 값은 그대로 `견줌문` 한 곳에서 오고 배치만 갈랐다. 회귀 2건 추가(`배포기준선.test.js` 45→47) — ①「이후의 변경」과 sha 는 **어느 갈래에서도** 한 줄에 안 온다(갈래별로 배치를 다르게 두면 그 판정이 두 번째 자리가 된다) ②섞임이면 프로젝트 목록이 한 줄에 모여 있다(기대값은 `견줌문` 에서 파생 — 자릿수 규칙을 두 번 적지 않는다). |
