| F543 | 2026-08-17 | 마찰 | ①배포 검수의 P1 2건이 서로 다른 도구인데 **같은 병**이었고, 둘 다 **그 파일 자신의 머리말이 그 병을 경고하는 바로 아래**에서 났다 — 산문으로 적은 규칙은 그 규칙을 적은 사람의 다음 20줄도 못 지킨다.  실측 2026-08-17 (검수-2026-08-17T0542-42dc04 · base 408b650d · local_fd7d2889 이 처분):  ① `tools/게이트.js:130` 상태보기() — `규칙찾기` 가 실패하면 「❔ 못 읽었다」를 찍고 `continue` 하는데 세는 칸이 없었다. 그래서 저장소 2곳의 ruleset 조회가 **전부** 실패해도 `어긋남 0` 문을 그대로 지나 `✅ 실측과 장부가 같다` 를 찍고 종료 0 이었다. 이 함수의 존재 이유가 「훅은 장부를 믿으니 여기서는 **실측**을 본다」인데, 실측을 한 줄도 못 읽은 판이 «대조해 보니 같더라» 와 같은 모양으로 나왔다. 그리고 그 파일 머리말 24~28행 「대가 (틀릴 때의 모습)」이 **거짓 초록**을 이미 예언하고 있었다 — 예언한 자리와 난 자리가 같다.  ② `tools/형제초록.js:127` 찍기() — `Number(값) || 0` 이라 `--실패 nope` 도, 값 자리에 다음 플래그가 온 `--실패 --건너뜀 1` 도 조용히 「실패 0」으로 도장에 앉았다. 바로 위 :106~109 의 `--실패` 필수 문이 **「안 재고 찍은 도장은 「초록」과 모양이 같다」** 라고 자기 입으로 적어 놓고, 그 다음 20줄에서 못 읽은 숫자를 0 으로 접었다. 도장은 sha 가 같은 한 계속 초록으로 읽히므로 이 거짓은 판정을 이월한다.  ■ 왜 신호인가 — 지침 v9.2 「가드·회귀의 네 맹점 ④」의 재발이고, 이번엔 **두 도구가 독립적으로** 같은 방향으로 샜다. 둘 다 신설 장치이고(F518 ㉡ · 게이트 3단계), 둘 다 저자가 F207 을 알고 있었고, 둘 다 새는 방향이 「통과」였다. 즉 이 저장소에서 「미실행·미측정을 통과로 접기」는 개인의 부주의가 아니라 **기본값**이다 — 손으로 안 막으면 그 모양이 나온다.  ■ 축이 F207 과 다른 지점: F207 은 「초록을 분모와 함께 읽어라」라는 **읽는 쪽** 규율이다. 이번 둘은 **쓰는 쪽**이 애초에 분모를 안 만들었다 — 세는 칸이 없거나(①), 못 읽은 값을 0 으로 메웠다(②). 읽는 사람이 아무리 조심해도 분모가 없으면 못 읽는다.  ■ 처방 후보 — 안 지었다(이 신고는 재료다).   ㉠ 코드 규약: 판정을 내는 함수는 «관측 목록 → 판정» 순수 함수로 분리하고 그 반환에 분모(전체·잰것)를 필수 칸으로 둔다. 이번 수리가 그 모양이다(`상태판정()` · `개수()`) — 두 자리 다 변이로 구멍 0 을 확인했다.   ㉡ 기계 검사: CLI 도구가 「✅」를 찍는 자리에 분모가 없으면 신고하는 린트. ⚠ 거짓양성이 셀 것 같고(축하·안내문도 ✅ 를 쓴다), 맹점 ② 「탐지력은 픽스처가 진다」를 지키려면 픽스처부터 필요하다.   ㉢ 아무것도 안 한다 — 이종 검수가 실제로 이 둘을 잡았다(그게 이 게이트의 값이다). 다만 잡힌 시점이 「배포 직전」이라 그 사이 두 장치는 각각 며칠씩 거짓 초록을 냈다. | |
