| F559 | 2026-08-17 | 실수 | 인계문의 ✅「진짜 후보」를 그대로 믿었으면 §8 함정에 그대로 걸렸다 — «생산자»까지만 따라가고 «착지점»을 안 봤다.  #Q99 5/5(`exit_log` 도달)를 이어받았다. 마감한 세션이 ①사유 실측을 하며 후보 둘을 갈라 뒀는데, ㉮(KPI 이탈률)는 「끝이 사람 화면」이라 §8 함정으로 기각하고 ㉯(리텐션 신호 → E5 AI 개입 멘트)는 ✅ **「진짜 후보다 — 학생에게 가는 문구가 바뀐다」**로 적어 넘겼다.  실측하니 ㉯도 **끝이 원장 홈 카드**였다. `리텐션멘트` → `aiCareMap`(Code.js:2528) → 쓰이는 자리가 Code.js:3126 하나뿐이고 그 블록 이름이 `{ // 원장 홈 카드 2종 }`(Code.js:3123)이다. 학생에게 가는 통로는 0건이다. 즉 인계문이 스스로 기각한 ㉮와 **같은 자리**를 ✅로 적은 것이다.  왜 갈렸나 = **따라간 거리가 달랐다.** 인계문이 연 자리(`엔진_콘텐츠AI.js:2677~2695`)는 그 문구를 «만드는» 자리다. 만드는 자리는 「학생별로 문구가 생긴다」처럼 보인다 — 그 문구가 «어디로 가는지»는 그 함수 안에 안 적혀 있다. 착지점은 `setState` 로 놓인 키를 **다시 grep 해야** 나온다.  ⇒ 이 저장소가 네 번 배운 「사유는 선언이지 실측이 아니다」(F531 계열)가 한 칸 더 간다:    **직전 세션의 «실측 결론»도 낡거나 틀린다.** 특히 ✅ 가 붙어 있으면 다시 안 열게 된다. ⇒ 일반 규율: **«끝»을 판정할 땐 생산자에서 멈추지 않고 소비자까지 간다.**    저장·전달 계층(app_state 키·시트 열·전역 맵)을 지나는 값은 생산자 코드만 봐서는 종착지를 모른다.  값: 이걸 안 열었으면 「학생에게 가는 문구가 바뀐다」를 근거로 도달 칸을 뒤집었을 것이고, 그건 §8 이 이름 대어 금지한 「사람 화면을 도달로 세기」다 — 래칫이 내려간 채 빚만 숨는다. | |
