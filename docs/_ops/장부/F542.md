| F542 | 2026-08-17 | 마찰 | 인계문·board 가 «로컬 master 가 origin 보다 뒤처진» 상태를 못 보고 「트랙 없이 끝난 세션」을 낸다 — F528 의 세 번째 얼굴. 실측 2026-08-17 local_b20caad9: 워크트리+PR 로 #Q99 3/5 를 종결하고(PR #39 머지 · 보드 줄 b3c192b4 가 origin/master 에 있다) ExitWorktree 로 워크트리를 거둔 직후 node tools/인계문.js 를 돌리니 «트랙 없이 끝난 세션이다 — 추정이 아니라 실측이다»가 나왔다. 원인은 본체 트리의 로컬 master 가 origin 보다 10 커밋 뒤였던 것뿐이고, 내 보드 파일은 그 10 커밋 안에 있었다. git merge --ff-only origin/master 뒤 다시 돌리니 정상이다. 🔴 새는 방향이 나쁘다 — 출력이 «모른다»가 아니라 «없다고 실측했다»고 단정한다(F348 의 모양: 값이 살 수 있는 층을 다 안 열고 부정 판정). 다음 세션은 그 말을 믿고 내 트랙을 이어받지 않는다. ▶후보 처방: 인계문·board 가 읽는 층에 이미 받아 둔 origin/master ref 를 더하거나, 최소한 «로컬이 origin 보다 N 뒤처졌다»를 감지해 단정을 «모름»으로 낮춘다. F528 이 워크트리 가지 층을 열었듯 이건 원격 ref 층이다. | tools/lib/보드.js 에 원격줄들() 신설(HEAD...origin/master 순diff · 파싱은 가지끝파싱 정본 공유 · 이름표만 갈아 단다) + session-report 의 네 자리(boardTrack 세 번째 층 · 무주줄 분모 · 못 잰 층 문구 · 낡음 판정)를 그 층으로 넓혔다 — 8cb3a96f(PR #65). 실물 재현(bare origin + fetch 만 한 트리)으로 전=거짓단정 후=트랙 넘김을 대조했고, 회귀 tests/인계문원격.test.js 20건 · 변이 11/11 구멍0. 뿌리 중 «가지 층 기준이 낡은 로컬 master» 갈래는 소비자 셋이 공유해 안 건드리고 F565 로 신고했다. |
