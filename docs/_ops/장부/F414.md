| F414 | 2026-08-14 | 실수 | 한 축에서 배운 «모양»을 옆 축에 안 옮겨 그 축이 그대로 눈이 멀었다 — `tools/가드계수.js` 4차(같은 병 네 번째).  무엇이: 08-13 에 ㉡(주석 제거기 사본) 축에서 「함수 «선언» 모양도 있다」를 실측으로 배우고 고쳤는데(그 커밋이 스스로 「첫 판은 화살표 모양만 봐서 13벌이 산 채로 초록이었다」고 적어 뒀다), **같은 계수기의 ㉠(원문 생산자) 축은 그대로 화살표·함수식만 봤다.** 그래서 `function section(){ return code.slice(…) }` 류가 원문함수로 못 서고, 그 파일의 부정 단언이 통째로 ❔모름으로 샜다(as 실측 24자리). 곁들여 같은 조임에서 나온 둘째 사각은 성질이 다르다 — 「이 파일만 봤다」: 원문 생산자를 지역 선언에서만 찾아 `const { engineSource } = require('./_engine-source')` 를 못 봤다(직접 17 + 파생 ~31).  왜 굵직한가: 이 계수기의 결함 4건이 전부 «같은 자리»에서 났다 — 1차 base64url 3벌, 2차 모양 셋 중 둘, 3차 별표 하나(`\*` vs `\/\*`), 4차 이번. 셋까지는 「한 축 안에서 모양을 덜 봤다」였고 4차는 **축 사이 전파 누락**이라 뿌리가 한 겹 위다. CLAUDE.md 신뢰성 조항의 「3번째 = 원인을 쓸 수 없게 만든다」를 이미 지난 자리인데, 처방이 매번 «그 축»에만 적용돼 왔다.  그리고 새는 방향은 이번에도 조용했다: 위험이 **84 로 보였고 실제는 181** 이었다. 「위험 84」는 적색이 아니라 그냥 작은 숫자라 아무도 안 본다 — 계수기가 덜 세면 그 결과는 언제나 「좋아 보인다」.  ⚠ 내 첫 처방도 같은 병에 걸렸다(그래서 이 신호에 같이 적는다): 「읽으면 원문 생산자」로 세어 talk 에 오탐 3건을 냈다(`세우기()` 는 파일을 읽지만 `module_.exports` 를 내준다 — 모듈 객체다). **읽었다고 «글»을 내주는 것은 아니다.** 숫자만 봤으면 「탐지 늘었다」로 읽고 넘어갔을 자리라, 움직인 자리를 전건 «이름»으로 대조해서 잡았다 — 숫자 대조로는 탐지와 오탐이 안 갈린다.  처방(이번 커밋 talk `c6ef43e`): ②-c 상대경로 require 한 겹 따라가기 · ③-b 함수 선언형 원문 생산자 · ⑤ 「읽었다고 글은 아니다」 경계(`원문글인가`) · ⑥ 정제 통로가 감쌌으면 읽기가 init 에 안 보여도 정제. 탐지력은 새 픽스처 뿌리 `tests/fixtures/가드계수불투명/` 가 지고(옛 census 무변경 · 변이 4/4), 스위트 2026/2026.  ▶ 남은 뿌리(이 신호가 닫히려면): 「축이 여럿인 판정기에 한 축의 교훈을 옮겼는지」를 사람이 기억하는 대신 기계가 묻게 하는 자리가 아직 없다. 지금은 축마다 모양 목록이 따로 산다. | 모양 어휘 하나(`모양`) + 축×모양 표(`축표` · ✗ 는 사유 필수) + 회귀 6건 — talk `f145f1a`. 어휘에 모양을 더하면 모든 축에 빈 칸이 생겨 적색이라, 새 모양을 들이는 사람이 **축마다 「이 축은 어떤가」를 대답해야** 통과한다(= 「옮겼는지」를 사람이 기억하던 자리를 기계가 묻는다). 어휘 밖 raw 타입 비교도 금지해 옛 통로를 막았다. 손 열거 7군데를 어휘로 돌린 것은 **순수 술어 추출이라 숫자가 안 움직였다**(전후 동일: talk 위험 19·사본 0·정제 132 / as 위험 181·사본 1·정제 15) — 계수기를 바꾸지 않고 전파만 기계화한 것이 이 처방의 핵심이다. F083: 변이 6/6이고 그중 ⑤가 08-14 4차 사각을 그대로 재현했는데 의도한 테스트가 잡았다. 스위트 2031/2032(skip 1). F092 대조: 저장소 전량 grep 결과 같은 모양의 «다중축» 판정기는 `tools/가드계수.js` 하나뿐이었다(as 0건 · talk 의 `tests/미정의심볼.test.js` 는 단일 사용이라 축 목록이 아니다). 남은 것 = ✓ 칸의 «옳음»은 이 표가 안 진다(픽스처 몫 · 표 주석 ㉢에 적었다). |
