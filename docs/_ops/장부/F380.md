| F380 | 2026-08-12 | 마찰 | 같은 승인 조건을 두 곳에 적어 갈라졌다 — SYNK-talk `tools/원격배포.js` 의 읽기 모드가 「라이브를 바꾸려면 **유호님 승인 뒤**」를 무조건 찍는다(실측 08-12 · 세션 1961779b 가 리허설 재배포 앞에서 멈춰 재유도했다). 두 겹으로 틀렸다: ①리허설 과녁엔 그 승인이 애초에 없었고(과녁을 안 보고 찍는다) ②운영 과녁의 승인은 08-12 유호님이 세션 자율로 위임했다(「운영 REF 이것도 자율주행에 맡길게」). 그때 고친 자리는 `lib/자격증명.js` 과녁차단문 하나뿐이었고(talk cc70e13), 그 커밋이 스스로 「안 고치면 위임이 코드에서 무효가 된다 — 다음 세션이 「유호님 명시 승인」을 읽고 멈춘다」고 적어 둔 위험이 같은 날 그대로 실현됐다. 뿌리는 낡음이 아니라 **판정을 두 곳에 적은 것**이다(CLAUDE.md 「목록은 하나에서 파생시킨다」의 문서·프로즈 판). 이 계열 2번째 = 시스템 결함 축. | talk `871580d` — 읽기 모드 힌트에서 승인 조건을 **지웠다**(칠 명령만 말한다). 조건의 정본은 과녁차단문 하나로 남기고, 운영 과녁이면 그 파일이 정본이라고만 가리킨다. 회귀 tests/원격배포.test.js 19/19. 🚫문구 스캐너는 안 만든다 — F243 에서 기각된 처방이고(제외 목록이 폭발한다), 여기 뿌리는 문구가 아니라 사본이다. |
