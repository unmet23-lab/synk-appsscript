| F369 | 2026-08-12 | 마찰 | 정본을 고친 커밋은 나갔는데 **그 정본을 가리키던 앵커와 그 앵커를 지키는 회귀가 함께 미커밋으로 남아**, HEAD 가 반쪽 게이트를 내보내면서도 아무도 못 봤다.  실측 2026-08-12: `docs/SYNK_철학.md` 는 v1.5 에서 ㉠ 을 폐번해 「그리고 우리가 하지 않는 것 **셋**」이 됐고 v1.6 에서 「4층」이 됐다(둘 다 커밋 착지). 그런데 HEAD 의 `tools/codex-review.js:448` 은 여전히 `/^>\s*\*\*그리고 우리가 하지 않는 것 넷/` 을 앵커로 찾는다 — 정본에 그 문자열은 **grep 0건**이라 그 자리가 안 뽑히고, 게이트가 «반쪽이다» 로 모든 검수·심문에 나간다. 판단 기준 없이 돈 심문의 산출은 「지적 없음」과 **같은 모양**이라 새는 방향이 침묵이다.  🔑 급소는 「앵커가 낡았다」가 아니라 **그것을 잡는 회귀(`tests/이종검수.test.js` 의 실저장소 게이트 시험)가 같은 미커밋 뭉치 안에 있었다**는 것이다. 그래서 작업본에서는 초록(92/92)이고 HEAD 에서만 빨간, 어느 층에서도 안 보이는 상태가 됐다 — `test-ci` 는 작업본을 재고, HEAD 를 재는 `원격ci.js` 는 Actions 한도로 0건이다(F247 가족: 죽은 세션의 미커밋은 CI 가 원리상 못 본다).  수리 5줄을 만든 세션들은 전부 죽어 있었고(작업본소유자 ⚪), 유물은 68분~19시간 떠 있었다. 초록을 먼저 재고(`node --check` OK · 92/92) `e9f62bc2` 로 착지시켰다.  ⚠ 남은 구멍(이 신고로 안 닫힘): 「작업본은 초록인데 HEAD 는 빨감」을 이 저장소는 지금 **잴 수단이 없다.** `test-ci` 는 작업본을 재고 원격 run 은 0건이다. 처방 후보 ①`test-ci` 에 「HEAD 만으로 한 번 더」 갈래 추가(임시 워크트리) ②Stop 훅이 미커밋 유물의 나이를 세어 알림 ③Actions 리셋(9/1) 뒤 원격 게이트 복귀. 재료가 갈리므로 판정은 별도 트랙. | 앵커는 e9f62bc2 로 착지했다(신고문 자신이 적고 있다). 지금 tools/codex-review.js:497 은 정본과 «같은» 문자열을 찾는다 — /^>\s*\*\*그리고 우리가 하지 않는 것 셋/ 이고 docs/SYNK_철학.md 는 v1.5 에서 ㉠ 을 폐번해 정확히 그 문구를 쓴다. 2026-08-12 `local_e594c36e` 가 양쪽을 직접 열어 대조. ⚠ 신고문이 스스로 갈라 둔 남은 구멍(「작업본은 초록인데 HEAD 는 빨감」을 잴 수단이 없다)은 「판정은 별도 트랙」이라 이 줄이 아니다. |
