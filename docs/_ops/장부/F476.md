| F476 | 2026-08-15 | 실수 | 감각층 시연 「생명감이 안 보인다」 신고가 2회 반복됐는데 진범이 매번 달랐다(v2→v3 rAF 스로틀, v3→v4 방치 타이머 — 패널·버튼 조작이 lastTouch 로 안 쳐져 검수하는 30초 사이 계속 잠들고 wake 직후 tick 이 도로 재움). 두 번 다 「장치가 발동하는가」만 실측하고 초록을 냈다 — 장치는 전부 발동했지만 «보는 사람의 시간 흐름» 속에서 다른 상태(잠)가 그 표현을 되삼켰다. 맞는 얼굴(발동 초록)로 틀린 값(안 보임)을 내는 자리(지침 맹점 ④). 교훈: 사용자 검수 재현 실측은 장치 단발 발동이 아니라 검수 시나리오의 시간 흐름(수십 초 연속 조작·방치 혼합)을 그대로 돌려야 한다. | v4 에서 수리(모든 접촉+wake 가 lastTouch 를 찍음) + 정본 §3① v4 교훈 ①로 규칙화 + 생명감 바로 보기 패널(검수 도구 자체를 시간 흐름 재현이 필요 없는 모양으로) |
