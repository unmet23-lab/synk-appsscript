| F544 | 2026-08-17 | 마찰 | 「원고가 정본」 굽기 가드가 «원고를 고친 정당한 경우»를 «산출물을 손으로 고친 사고»와 같은 모양으로 막는다 — 그리고 그 처방을 따르면 방금 한 수정이 사라진다(F103 과 같은 형태).  실측 2026-08-17 (엔진 작명 전파 트랙 · 몽글엔진 → SYNK Vellum): · docs/엔진/_원고/ 3벌(Vellum·Loom·SYNK_Core)을 고친 뒤 `node tools/펠트문서.js --전량` 을 돌리니   「원고가 낡은 지면 3벌 — 지금 구우면 산출물의 손 수정이 조용히 사라진다」 로 exit 1. · 가드가 내는 처방은 「먼저 --재현 을 돌린다(되메우기 명령을 그대로 낸다)」 인데,   되메우기는 **산출물 → 원고** 방향이라 그대로 따르면 방금 한 원고 수정이 통째로 사라진다.   즉 정상 통로(원고 수정 → 굽기)에 대해 **따를 수 없는 처방**이다. · --전량 에는 우회 플래그가 없다(소스 840~857행: 갈라짐이면 무조건 exit 1).   실제 통과 방법은 `--굽기 <입력> <출력>` 을 파일 수만큼 손으로 반복하는 것뿐이었다 —   우회가 정상 통로가 됐고, 그 손 반복은 철학 ㉡(사람 손을 늘리는 해법)에도 걸린다.   덤으로 그 커밋(43487c7d)이 없앴다고 적은 병이 바로 「6번 손으로 치는 것」이었다.  왜 두 경우를 못 가르나: 둘 다 「원고와 산출물이 다르다」는 **같은 관측**이다. 가르려면 «어느 쪽이 새것인가»(방향)를 알아야 하는데 지금 검사는 방향을 안 본다.  처방 후보 셋:  ① git 이 방향을 안다 — 원고가 산출물보다 나중에 수정·스테이징됐으면 「원고가 새것」이다.  ② `--전량 --원고우선` 같은 명시 플래그(사람이 방향을 댄다 · 기본값은 지금처럼 막는다).  ③ 최소한 차단문에 나가는 문을 적는다 — 「원고를 방금 고친 것이면 --굽기 로 개별 굽기」.     지금 차단문에는 그 문장이 없어서, 처방을 그대로 따른 세션은 자기 수정을 지우게 된다.  ⚠ 이 트랙은 ③의 자리를 손으로 지나갔다(개별 굽기 3회 → --재현 6/6 재현 · 갈라짐 0 확인).    탐지력 자체는 살아 있다(tests/펠트문서.test.js 14/14) — 문제는 «나가는 문»이 없다는 것. | |
