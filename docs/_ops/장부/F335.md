| F335 | 2026-08-11 | 마찰 | 게이트 none 안내의 처방이 그 게이트를 못 연다 — `--확인` 이 「검수 기록이 없다」면서 `--commit <과녁sha>` 를 처방하는데, 커밋 대상 검수는 지문을 기록하지 않는다(tests/이종검수.test.js 「옛 커밋을 검수하면 지문을 기록하지 않는다」가 그 계약을 못박고 있다). 그래서 처방을 그대로 따르면 20~40분을 태우고도 게이트는 같은 문구로 남는다 — F103 그 자체(따를 수 없는 처방은 우회를 정상 통로로 만든다). 실측 2026-08-11(81ffe463): P1 수리로 작업본 지문이 e6ac71ac 로 바뀌자 게이트가 none 으로 내려갔고 처방은 --commit 3d705b33 이었다. 게이트를 실제로 채우는 것은 **작업본(또는 작업본과 같은 HEAD)** 검수뿐이다. 처방 후보: none 일 때는 과녁커밋이 아니라 인자 없는 호출을 처방하고, 작업본에 배포 파일 변경이 있으면 그 사실을 함께 적는다. | |
