| F411 | 2026-08-14 | 마찰 | 심문 장부의 `금지지문` 이 «완료 시»에 다시 계산된다 — 회차가 도는 동안 `MEMORY.md` 를 고치면 장부에 남는 지문이 실제로 쓴 것과 갈린다.  실측 `tools/codex-review.js:1620` — 회차 완료 행을 append 할 때 `금지지문()` 을 **인자 없이 다시 계산**한다. 프롬프트 자체는 던질 때 한 번 조립되므로(`:1550`·`:1562` 주석이 그 이유를 적고 있다) 런의 «입력»은 안 흔들린다. 그런데 장부에 남는 값은 **완료 시각의 `MEMORY.md` 를 해시한 것**이라, 회차 대조(F281)의 조건 통제 증거가 사후에 조용히 무효가 될 수 있다. 그리고 그 상태는 정상과 **같은 얼굴**이다 — 지문이 들어 있으니 「쟀다」로 보인다.  지금 이 구멍을 막고 있는 것은 **사람 규칙**이다: 6회차를 던진 세션이 자기 보드 칸에 「⚠회차 도는 중 `MEMORY.md` 편집 금지」를 적어 뒀고, 그 줄을 읽은 다음 세션이 지켰다. CLAUDE.md 가 말하는 「프로즈로 막는 규칙」의 전형이라 세션이 갈리거나 그 줄이 아카이브로 넘어가면 안 지켜진다.  처방 후보: 던질 때 계산한 지문을 **런 레코드에 얼려 두고**, 완료 행은 그 값을 «옮겨 적는다»(재계산 금지). `대상지문` 은 이미 그렇게 다뤄지는지 함께 본다.  발견 경위: 6회차 전건 처분을 끝내고 7회차를 던진 뒤 메모리를 갱신하려다, 「이 편집이 도는 런을 오염시키나」를 묻고 그 자리를 직접 열어 쟀다. ⚠ 이번엔 `MEMORY.md` 를 안 고쳤으므로 **이 런의 지문은 아직 성하다** — 신고는 다음 번을 위한 것이다. | ⏸ 지금 안 고치는 이유가 F411 자신이다 — 7회차 심문이 «이 파일로» 도는 중이라(런 심문-2026-08-13T1514-88a14e · 자식이 회차마다 모듈을 다시 require 한다) 지금 codex-review.js 를 고치면 회차 사이에 도구가 갈려 대조가 무효가 된다. 판정 자리 = 6회차 처분 종결 세션 eaa21f3f. ▶ 7회차 결과를 수거하는 세션이 첫 일감으로 고친다(처방은 신고문에 적혀 있다: 던질 때 계산한 지문을 런 레코드에 얼리고 완료 행은 옮겨 적기). 그때까지 사람 규칙(보드 칸의 「회차 도는 중 MEMORY.md 편집 금지」)이 임시로 막는다. |
