| F640 | 2026-08-18 | 마찰 | `test-ci` 의 플랫폼 분모가 이 저장소에서 **영구 「모른다」** 가 됐다 — 내가 오늘 낸 변경의 대가다. 실측 2026-08-18: `runs-on: [self-hosted, linux]` 로 바꾼 뒤 `tools/lib/ci모사환경.js` 의 `러너판()` 이 배열 표기를 못 읽어 `판: null` 을 내고, 매 런 「CI 가 어느 판인지 못 읽었다 (본 라벨: [self-hosted, linux])」가 붙는다. 🔑 **틀린 초록은 아니다** — 과소주장이라 새는 방향이 「통과」가 아닌 드문 자리다. 그래서 위험한 건 값이 아니라 **닳음**이다: 늘 뜨는 경고는 곧 안 읽히고, 그러면 진짜로 판이 갈린 날에도 안 읽힌다(늘 적색인 목록이 무시당하는 것과 같은 병). 처방을 설계까지 정해 둔다 — ① `러너판()` 이 배열 표기 `[a, b]` 를 낱말로 갈라 읽는다 ② 판정을 **넷**으로 낸다: 지금의 「같다·다르다·모른다」에 **「라벨이 그렇게 말한다」** 를 더한다 = self-hosted 라벨에서 읽은 OS 는 `ubuntu-latest` 같은 GitHub 이미지와 달리 **등록자의 약속이지 GitHub 의 보증이 아니다**(그 약속을 거는 자리가 `tools/러너설치.sh` 의 `LABEL="linux"` 다) ③ 회귀는 두 소비자를 이미 못박고 있는 `tests/판정층정합.test.js` 에 얹는다. ⚠ 대가: 라벨은 사람이 등록할 때 정하는 값이라 윈도우 러너에 `linux` 를 달면 이 층이 거짓말을 한다 — 그래서 ②의 문구에서 「보증이 아니다」를 안 지우는 것이 이 처방의 조건이다. | ⏸ 세션을 닫는 자리라 새 장치를 안 짓는다 — 그리고 이 행은 **급하지 않은 쪽으로 샌다**. 지금 출력은 「모른다」(과소주장)라 거짓 초록이 아니고, 값이 틀린 게 아니라 **닳는다**는 것이 손해다. 처방은 신고문에 설계까지 박아 뒀으니(배열 표기 파싱 + 「라벨이 그렇게 말한다」 넷째 판정 + `tests/판정층정합.test.js` 회귀) 부를 때 바로 짓는다. ⏳ 착수 조건: 유호님이 `bash tools/러너설치.sh` 로 러너를 세운 **뒤** — 그때 라벨이 실제로 무엇으로 등록되는지 실물로 보고 짓는 편이 낫다(지금 짓는 넷째 판정은 아직 아무 러너도 안 선 상태의 추정 위에 선다). |
