| F273 | 2026-08-09 | 실수 | SYNK-talk `tools/원격배포.js` 가 반쪽만 배포했다 — 함수 본체는 **작업본**, 동봉 lib 은 **HEAD**. 본체와 lib 을 함께 고친 커밋 전에 밀었더니 새 `index.ts` + 옛 `음성헤더.mjs` 가 나가 events v21 이 배포는 ✅ 로 끝나고 첫 호출에서 `길이초 is not a function` 이 될 상태로 리허설에 섰다. 급소는 경고문이다 — lib 이름만 부르며 「미커밋 편집은 안 나간다」고 말해 **안전하다고 읽힌다**(새는 방향이 언제나 통과인 그 모양인데, 여기선 통과가 「막았다」의 얼굴을 하고 있다). 그 파일 머리말 61행이 이미 「배포되는 것은 언제나 커밋된 것이어야 한다」고 못박아 뒀는데 본체만 예외였고, 본체야말로 남의 미커밋 편집이 실릴 가장 큰 파일이다. | SYNK-talk tools/원격배포.js — 본체·동봉이 같은 통로(머리에서+미커밋검사)를 지난다. 판정은 순수 함수 작업본다름 으로 내리고 막는 것은 부르는 쪽(판뒤처짐 과 같은 자리) · 회귀 tests/원격배포.test.js 3건 |
