| F648 | 2026-08-18 | 마찰 | 오늘 되살린 원격 CI 가 2시간 만에 멈췄고, 멈춤의 모양이 «적색»이 아니라 «영원한 queued» 라 아무도 안 깨운다. 실측 2026-08-18T18:24Z (GitHub Actions API 직접 조회 · `gh` 가 없는 환경이라 `tools/원격ci.js` 는 「❔ 모름」을 냈고 MCP 로 갈아 재판정했다): 마지막으로 **성공**한 run 은 `#1801 @ 15:52:21Z`(claude/ubuntu-install-um1kob)다. 그 뒤 만들어진 run **6건이 전부 `queued` 인 채**고, 서로 **다른 가지 6개**이며 그중 하나가 **master(`#1802 @ 16:02:10Z` — 2시간 22분째)** 다. 가지가 갈리므로 concurrency 취소로는 설명이 안 되고, `if:` 스위치가 꺼졌다면 job 이 **아예 안 뜬다**(워크플로 주석이 스스로 적은 성질)는데 job 은 떴다 — 즉 **스위치는 켜져 있고 러너가 안 집는다**(유호님 노트북의 셀프호스티드 러너가 꺼졌거나 서비스가 죽었다). 🔑 이 자리가 위험한 이유는 값이 아니라 **모양**이다: 실패는 메일이 가지만 **queued 는 아무 알림도 안 간다.** 워크플로 주석이 08-14 중지 때 이미 같은 병을 적어 뒀다 — 「push 해도 CI 0건이 정상 얼굴로 굳는다」. 오늘 그 자리를 러너로 고쳤는데, **고친 판이 새 모양의 같은 병을 냈다**: 그때는 「run 0건」이었고 지금은 「run 은 있는데 영원히 안 끝난다」다. 둘 다 화면에서 초록도 적색도 아니라, **트랙 종료 게이트로 쓰면 그냥 영원히 기다린다**(오늘 PR #152 가 지침 v9.3 에서 원격 CI 를 종료 게이트로 «되돌리는» 중이라 사정거리가 바로 여기다). 🔴 덤으로 잰 것 — 이 적체는 내 PR #153 의 결함이 아니다: **base(master)도 같이 큐에 걸려 있다.** ⏸ 안 고쳤다 — 러너를 살리는 것은 유호님 노트북 쪽이라 이 세션이 못 한다. 저장소 쪽 처방 후보는 ①`timeout-minutes` 를 job 에 걸어 **안 집히면 실패로 떨어지게**(queued 는 timeout 이 아니라 job 시작 후에만 세므로 이것만으로는 부족하다 — 확인 필요) ②SessionStart 가 「queued 인 채 N분 넘은 run」을 적색으로 내는 층 ③러너 하트비트를 재는 통로인데, **어느 층이 「안 집힘」을 실제로 볼 수 있는지부터 재야 한다**(안 재고 지으면 또 하나의 맞는 얼굴로 틀린 값이다). | 저장소가 public 이 되면서 러너 의존이 사라졌다 — 실측 08-22: syntax-check.yml:75 이 `repository.private == false && 'ubuntu-latest' || self-hosted` 라 지금은 **GitHub 호스트 러너**가 집는다(gh 로 확인: isPrivate=false). 최근 run 5건이 전부 completed/success · 37~53초. 즉 「영원한 queued」의 원인(셀프호스티드가 오프라인)이 구조적으로 닿지 않는다. ⚠ 남는 조건: 저장소를 다시 private 로 돌리면 그 갈래가 되살아난다 — 처방 후보 ①timeout-minutes ②queued 나이를 재는 훅 은 그때 다시 꺼낸다. |
