| F512 | 2026-08-16 | 실수 | 마누스 폴러가 상태를 «메시지 목록»에서 찾았다 — 통로가 세워진 날부터 **어떤 런도 「끝났다」고 말할 수 없었다** (실측 2026-08-16 · `local_b489bd18`)  ■ 증상 런 `사실심문-2026-08-16T1057-07e8a7`(창업대출 6주장 적대 검증)이 155분째 「멈춤」으로 떠 있었다. 로그 154줄이 전부 `상태=(빈값)` — 단 한 번도 실제 상태를 읽은 적이 없다.  ■ 실측한 원인 (추정 아님 — 응답 원문과 작업 6건 이력을 직접 열었다) `task.listMessages` 응답 최상위 키는 여섯뿐이다: `has_more·messages·next_cursor·ok·request_id·task_id`. `task_status` 도 `status` 도 `data` 도 **없다.** 그런데 폴러는 그 셋만 봤다 —     String(j.task_status // j.status // (j.data && j.data.task_status) // '') 간헐이 아니라 **구조적으로** 늘 빈값이다(154/154). 빈값은 어느 분기에도 안 걸려 조용히 다음 회차로 흘렀고, 그래서 `stopped`·`error`·`waiting` **어느 종료 경로에도 도달할 수 없었다.** 도달 가능한 출구는 180분 뒤 상한초과 하나뿐 — 이 통로로 던진 모든 런의 운명이 그거였다.  ■ 실제 피해 그 작업은 마누스 쪽에서 `stopped` 였고 답도 다 써 있었다. 완료 시각은 작업 생성 **+659초(11분)**. 폴러는 그 뒤로 **144분을 더** 돌았다. 답은 도착해 있는데 회수 통로만 눈이 멀어 있었다.  ■ 왜 안 잡혔나 — 이게 F509·F510 과 같은 가족인 자리 F509 는 종료코드의 «이름»을 고쳤고(실패가 완주 칸에 앉던 자리), F510 은 «던지기»를 고쳤다(`agent_profile` 생략=200+유령). 둘 다 맞았다. 그런데 **«읽기»는 아무도 안 쟀다** — 응답 원문을 한 번도 안 열어 보고 필드 이름을 후보로 나열한 코드였고, 그 나열이 전부 빗나가도 증상은 「도는 중」이라 조용했다. 직전 세션은 던진 직후 `running` 스냅샷 하나를 보고 「✅ 첫 성공 던지기」로 트랙을 닫았다 — 던지기는 실제로 성공했으니 그 말은 절반만 맞았고, 나머지 절반(회수)은 그때 이미 불가능했다.  ■ 교훈 「응답에서 값을 못 찾음」을 **「아직 안 끝남」으로 접지 마라.** 그 둘은 완전히 다른 상태인데 한 칸으로 뭉치면 못 읽는 통로가 도는 통로의 얼굴을 하고, 사고는 조용히 상한까지 간다. 벤더 응답의 필드명은 **후보로 나열하지 말고 한 번 열어서 잰다** — 나열은 못 찾아도 아무 소리를 안 낸다. | tools/마누스.js 상태캐기(j) — 상태는 메시지 안 status_update.agent_status 에서 나온다(최신 하나·timestamp 로 고름·최상위 필드는 2차). 어휘 running/stopped 는 실측이고 task.list 의 status 와 같은 낱말이라 분기는 무변경. + 연속 빈값 상한 5회(종료코드 5) — 못 읽는 것을 도는 것으로 접지 않는다(근거: 첫 status_update 가 생성 +1~2초 · 6/6). + 로그에 출처= 를 함께 적어 「응답이 빈 건가 우리가 딴 데를 본 건가」를 사후에 가른다. 회귀 6(픽스처·네트워크 0) · 변이 11/11 구멍 0 — 372c9e2e |
