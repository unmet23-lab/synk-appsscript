| F484 | 2026-08-15 | 실수 | 플래그 게이트를 «부르고 답을 버린» 도구가 또 나왔다 — `SYNK-talk/tools/회차장부.js` 가 `인자게이트(...)` 를 맨 호출문으로 두어 모르는 `--낱말` 이 조용히 무시됐다(그 저장소 40개 도구 중 유일). 기존 등록층 회귀(`tests/플래그게이트.test.js` ③)는 「불렀나」만 봐서 **초록이었다** — 가드가 자기 등록층에 눈이 먼 자리(CLAUDE.md 맹점 ④: 맞는 얼굴로 틀린 값). 같은 계열 다섯 번째다(F400 배포대조 · F435 원격SQL 읽기 · F436 원격SQL 쓰기 · 08-14 「--운영 받는 11 중 die 는 1」 · 이번). 발견 경로도 기록한다: 회귀가 아니라 **변이 시험**이 잡았다(「--json 을 아는플래그에서 뺀다」 변이가 구멍으로 나왔다) — 사람이 읽어서는 안 보이는 자리였다. | `tests/플래그게이트.test.js` 에 ③-2 신설 — 게이트 호출이 ⓐ`if (인자게이트(…))` ⓑ`return …` ⓒ`const X = …` + 뒤에서 X 사용 셋 중 하나가 아니면 fail(자격증명 도구 전량 스캔 · 손 목록 0). 판정기 자체의 탐지력 픽스처 5건을 함께 둠. `회차장부.js` 는 나머지 12개 도구와 같은 모양으로 수리. ⚠ 한글 변수명에 `\b` 를 쓰면 JS 정규식이 원리상 안 붙는다(거짓양성 12/12 실측) — 그 함정을 주석에 적었다. |
