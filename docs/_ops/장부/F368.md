| F368 | 2026-08-12 | 마찰 | 보드 표가 「그 줄의 주인이 아직 있나」를 안 적어, 마감한 세션의 낡은 🎫 를 다음 세션이 집는다 — 한 세션이 트랙 **하나** 고르는 동안 세 번 낚였다(2026-08-12 `local_38c31cad` 실측).  낚인 셋: ①보드 🎫「다음=clasp push(215~217)」 두 줄 — 이미 `a668c152` 가 v9.215~220 을 완주한 뒤였다(커밋 `7a37a20d`·`8b909daa` 가 HEAD 16~18번째) ②보드 ▶「미착수 · `lib/멘탈게이지.js`·`lib/게임스냅샷.js` 신설」 — 둘 다 이미 형제 저장소에 있다 ③작업대기열 S1-5 항목 — 본문이 「✅2단계 종결」인데 체크박스는 `- [ ]` 로 열려 있다.  급소는 상한이 아니라 **모양이 같다는 것**이다. 산 세션의 줄은 그 세션이 갱신하니 이 병이 안 생기는데, 마감한 세션의 줄은 그 시점에서 굳고도 표에서 글자 하나 다르지 않다. 「지금 누가 뭘 잡았나」를 보라고 만든 화면에서 「그 누가 아직 있나」가 통째로 빠져 있었다. 비용은 살아있는 세션 수만큼 곱해진다(실측 12).  곁들여 나온 실물 둘: 실행 시점 표 17줄 중 **14줄이 마감한 주인의 것**(🎫 6건)이고, `board-guard` 는 「활성 16줄·상한 12」로 이미 위반 상태였다 — 새 세션이 자기 줄을 선언하려면 남의 완료 줄부터 옮겨야 했다. | |
