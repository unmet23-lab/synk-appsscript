| F368 | 2026-08-12 | 마찰 | 보드 표가 「그 줄의 주인이 아직 있나」를 안 적어, 마감한 세션의 낡은 🎫 를 다음 세션이 집는다 — 한 세션이 트랙 **하나** 고르는 동안 세 번 낚였다(2026-08-12 `local_38c31cad` 실측).  낚인 셋: ①보드 🎫「다음=clasp push(215~217)」 두 줄 — 이미 `a668c152` 가 v9.215~220 을 완주한 뒤였다(커밋 `7a37a20d`·`8b909daa` 가 HEAD 16~18번째) ②보드 ▶「미착수 · `lib/멘탈게이지.js`·`lib/게임스냅샷.js` 신설」 — 둘 다 이미 형제 저장소에 있다 ③작업대기열 S1-5 항목 — 본문이 「✅2단계 종결」인데 체크박스는 `- [ ]` 로 열려 있다.  급소는 상한이 아니라 **모양이 같다는 것**이다. 산 세션의 줄은 그 세션이 갱신하니 이 병이 안 생기는데, 마감한 세션의 줄은 그 시점에서 굳고도 표에서 글자 하나 다르지 않다. 「지금 누가 뭘 잡았나」를 보라고 만든 화면에서 「그 누가 아직 있나」가 통째로 빠져 있었다. 비용은 살아있는 세션 수만큼 곱해진다(실측 12).  곁들여 나온 실물 둘: 실행 시점 표 17줄 중 **14줄이 마감한 주인의 것**(🎫 6건)이고, `board-guard` 는 「활성 16줄·상한 12」로 이미 위반 상태였다 — 새 세션이 자기 줄을 선언하려면 남의 완료 줄부터 옮겨야 했다. | `node tools/board.js` 가 표 아래(**stderr**)에 「주인이 마감을 선언한 줄」을 세어 내고, 그중 🎫 를 남긴 줄을 따로 뽑아 **집기 전에 실물을 직접 열라**고 처방한다(`tools/lib/보드.js` 의 `주인없는줄들()` · 회귀 `tests/보드주인.test.js` 7).  못박은 것 넷: ①**「살아있음」은 작업본소유자에서 그대로 가져온다** — 여기 다시 적으면 두 도구가 서로 다른 생사를 보고 갈라진 쪽의 증상은 언제나 「통과」다(`board-move.js` 원칙 ⑥ 과 같은 통로 · 순환을 피하려 require 는 함수 안에). ②**새는 방향을 「주인 없음」으로 기울였다** — 「주인 있음」으로 틀리면 다음 세션이 낡은 🎫 를 집지만(이 사고), 반대로 틀리면 사람이 실물을 한 번 더 여는 비용뿐이다. 그래서 `유물-*.md` 처럼 파일지문이 `''` 인 것도 주인 없음으로 센다(그것을 「내 것」으로 세면 통째로 새는 자리 — board-id 머리 ⚠). ③**stdout 은 한 바이트도 안 바뀐다** — 이 표를 파싱하는 곳이 넷이라(session-report·track-collision·board-move·board-guard) 유령 경고가 지킨 계약을 그대로 지켰고, 회귀가 `텍스트()` 와 바이트 동일을 건다. ④**못 잼과 0건을 가른다**(F207) — 생사를 못 재면 `null` 이고 화면이 그 사실을 말한다.  두 축을 갈라 쓴다: 작업본 소유는 **심장박동**(`살았나` — 파일을 누가 들었나), 선언 생존은 **박동+마감 도장**(`선언살았나` — 그 줄이 아직 갱신되나). 실측에서 `528aebb5`·`d35f21df`·`070be212` 는 전자로 살아있고 후자로 죽어 있었다 — 그래서 화면 문구도 「끝났다」가 아니라 「**마감을 선언했다**」로 적는다(도장은 도구를 다시 부르면 지워진다).  변이 2/2 실측: 🎫 판정을 죽이면 그 테스트만, 못 잼을 0건으로 번역하면 F207 테스트만 빨개진다. 탐지력은 픽스처가 지고 실저장소에는 「내 줄이 안 잡힌다」만 건다(맹점 ②).  ⚠ 안 고친 것: 낡은 줄 자체는 이 수리가 안 지운다 — 남의 보드 줄 편집은 F073 이 금지한다. 화면이 「집기 전에 열어 봐라」까지만 하고, 완료 줄이면 `board-move` 처방을 같이 낸다(그 도구가 산 주인의 줄은 거절한다). |
