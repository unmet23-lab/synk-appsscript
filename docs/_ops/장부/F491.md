| F491 | 2026-08-15 | 실수 | 변이 공용 통로가 한 파일에 계약을 둘 들고 있었다 — 머리말 「쓰는 법」은 계획 안에서 변이검사()를 부르라 가르치고 CLI 는 module.exports 를 기대한다. 그 모양으로 돌리면 「변이 8/8 (구멍 0 · 미적용 0)」이 멀쩡히 찍힌 뒤 스택 트레이스가 붙어, 읽는 사람이 맞는 판을 실패로 접는다. 손 하네스를 없애려고 세운 통로가 머리말로 손 하네스를 가르치고 있었던 셈. 처방=머리말을 CLI 계약으로 통일 + CLI 가 시험 배열 없는 계획을 이름 대고 막음(exit 2) + 회귀 2(tests/변이통로.test.js). | `3ccb4642` — 머리말 「쓰는 법」을 **CLI 계약 하나로** 통일하고(계획은 `module.exports` 만 한다 · 스스로 `변이검사()` 를 부르지 않는다), CLI 가 `시험` 배열 없는 계획을 **이름 대고** 막는다(`tools/변이.js:261` · exit 2 · 「계획이 스스로 부르고 있지 않은지 본다」까지 인쇄한다). 🔑 새는 방향이 「초록을 적색으로」라 안전한 쪽이었던 것뿐이다 — 숫자는 맞는데 **화면이 틀렸다**: 「변이 8/8」이 멀쩡히 찍힌 뒤 스택이 붙어서, 읽는 사람이 맞는 판을 실패로 접는다. 오독을 없애려던 통로가 통로 자신의 머리말로 그 오독을 만들고 있었다. 회귀 2건 신설(`tests/변이통로.test.js` — ①CLI 가 막나 ②머리말이 CLI 와 **같은** 계약을 가르치나). 탐지력 실증 = **변이 2/2**(구멍 0·미적용 0): 그 둘을 각각 되돌리면 각각 빨개진다. 회귀 29/29. F092 대조 = 계획이 `변이검사()` 를 직접 부르는 자리는 저장소에 0(통로 자신과 그 회귀뿐). 🔴 **이 행이 열린 채 남은 이유는 따로 있다** — 고친 커밋이 **메시지에 F491 을 안 달아서** `friction-close-guard` 가 원리상 못 짖었다(그 훅은 커밋 메시지의 `F\d{3}` 으로만 행을 찾는다). 번호를 다는 것은 사람 손이고, 안 달면 가드는 눈이 먼다. |
