| F292 | 2026-08-09 | 실수 | 배포 게이트가 「이종 검수 기록이 없다」면서 처방한 명령(codex-review.js 인자 없이)이 그 게이트를 못 열었다 — 미커밋이 있으면 작업본을 검수하는데 이 저장소는 세션이 동시에 돌아 작업본에 늘 남의 미커밋이 있고, 그 diff 엔 배포 파일이 없어 범위 밖으로 기록된다. 나는 그 처방을 그대로 따라 50분을 태웠고 읽힌 것은 옆 세션들의 docs·tools·tests 였다(직전 세션의 1800초 타임아웃도 같은 원인으로 보인다 — 같은 방법 2회 실패). | tools/codex-review.js 게이트판정() 처방이 과녁을 댄다 — 그 프로젝트의 배포집합을 건드린 마지막 커밋(프로젝트별. 「최근 [v..] 커밋」으로 뽑으면 crewcard 에 루트 커밋을 처방해 따라도 범위 밖이 된다). 못 뽑으면 과녁 없이 낸다. 회귀 tests/이종검수.test.js 신설 3 |
