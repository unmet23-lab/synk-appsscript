| F249 | 2026-08-08 | 마찰 | ①배포 검수가 「확인 불가」만 내고 원인을 구조적으로 감춘다 — 오늘 3연속. 실측 2026-08-08(v9.196 배포 중): 1회차는 ~/.codex/models_cache.json 이 base_instructions 필드 없이 저장돼 codex 가 못 읽었고, 2회차는 codex-review.js 의 검수 경로 기본 타임아웃 900초를 max 추론이 넘겼다(같은 명령을 타임아웃 없이 직접 돌리니 15분을 넘겨 계속 돌았고 결국 완주해 P1 1건·P2 2건을 냈다 — 즉 안 도는 게 아니라 못 기다린 것이다). 두 실패가 화면에 **완전히 같은 모양**으로 떴다: codex-review.js:533 이 e.stderr 를 앞 3줄만 잘라 붙이는데 codex 는 그 자리에 배너(OpenAI Codex v0.146.0 / -------- / workdir: …)를 쓴다. 어떤 실패든 배너 3줄만 보이므로 원인 판별이 원리상 불가능하고, 그래서 1회차 뒤에 「로그인 만료인가」를 먼저 의심해 시간을 버렸다. F217 둘째 자리와 같은 형태다(병 + 못 본 이유가 짝) — 그쪽도 timeout 120초 < 실비용 358초였다. 새는 방향은 조용하진 않지만(2 = 확인 불가) 매번 15분을 태우고 아무것도 못 얻으며, 폰·급할 때 세션이 그냥 건너뛰게 만든다. 처방 후보 ①실패문에서 배너 줄(정규식 ^(OpenAI Codex/-{5,}/workdir:/model:/provider:/approval:/sandbox:/reasoning/session id:))을 걸러낸 뒤 남은 앞 3줄을 보인다 ②execFileSync 의 timeout 소진은 e.killed·e.signal 로 판별 가능하니 「타임아웃(N초) — --timeout 으로 늘려라」를 따로 낸다(처방이 실행 가능해진다) ③검수 경로 기본값 900초를 실측대로 올린다(제안 경로는 이미 1800초다 — 더 무거운 쪽이 더 짧은 역전). | tools/codex-review.js 실패요점() — codex 배너를 걷어내고 stderr 뒤에서 3줄 + 타임아웃은 signal 로 분기(268ca78). 타임아웃 문장이 늘릴 명령을 같이 주고, 그때는 로그인 힌트를 안 띄운다. 검수 기본 타임아웃 900→1800(심문보다 짧던 역전 제거). 회귀 7건(픽스처=실측 바이트) · 변이 7/7 |
