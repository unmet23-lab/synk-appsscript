| F396 | 2026-08-13 | 교정 | 운영자료.js 가 긴 줄표(—, U+2014)가 든 --이름 을 받으면 stderr 없이 exit 1 로 죽었다(유호 신고 2026-08-13). 유호님은 —를 빼고 다시 넣어 우회했다 — 그 흔적이 폴더 25번 `캐릭터 시안 v5 살아있음 시연` 이다(원래 주려던 이름은 `v5 — 살아있음 반응 시연`). 층을 하나씩만 바꿔 대조하니 인코딩(UTF-16LE base64 왕복)도 fs 도 멀쩡했고 WScript.Shell 의 CreateShortcut 만 죽었다: 그 층이 경로를 시스템 ANSI 코드페이지(이 기계 949)로 깎아 —가 `?` 가 되고, `?` 는 파일명 금지문자라 FileNotFoundException. 급소 둘. ①실패가 세 얼굴인데 둘이 조용하다 — 죽는 것(— – “ ✅ ⚠ 🔴)만 눈에 띄고, é 는 exit 0 로 `e` 로 깎여 딴 이름에 만들어지며(요청한 경로엔 파일이 없다), 그 이름 lnk 의 TargetPath 는 안 죽고 빈 문자열을 준다(「대상 없음」 오보의 씨앗). ②원인이 안 보인 이유는 ps() 가 stderr 를 stdio 로 통째로 버렸기 때문이다 — 버린 이유는 「CLIXML 이 결과에 낀다」였는데 execFileSync 는 stdout·stderr 를 따로 담아 섞일 일이 없었다. 버린 대가로 범인을 자백하는 문장(`Unable to save shortcut "…\x ? y.lnk"`)이 통째로 사라졌다. 처방을 「이름에서 특수문자를 지운다」로 안 잡은 근거: 코드페이지 안의 표기는 전부 멀쩡했다(― · → 「 ㉠ … ’ ① Ⅰ √ 한글 ß). 넓게 잡아 지우면 유호님이 화면에서 읽는 이름을 훼손하고, 코드페이지 표를 JS 에 베껴 두면 그 표가 갈라진다. | tools/운영자료.js: COM 에 ASCII 임시이름만 주고 fs.rename 으로 확정(바로가기만들기) + 빈 값이면 ASCII 사본으로 재독(바로가기대상읽기, 목록읽기·링크화 공용 통로) + ps 가 stderr 를 받아 PS오류읽기로 풀어 던짐 + 못만든이유가 링크·대상·층을 말함 · 회귀 tests/운영자료.test.js CP949 모사 셸 15건(변이 5/5 물림 · 실기계 1건은 win32 아니면 skip) |
