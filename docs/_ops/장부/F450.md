| F450 | 2026-08-15 | 마찰 | 확정 안 된 `[vNEXT]` 자리표가 **커밋되는 것**을 막는 기계가 0이라, 자리표 설계(F078)가 없애려던 「남의 배포를 막는 적색」이 자리표 자신으로 재현됐다. 실측 2026-08-15: 커밋 `694497ad`(세션 `1ae6700f`)가 `엔진_셋업확장.js` 의 `[vNEXT·검수 c59f24d9]` 2자리를 그대로 실었고, `tests/버전채번.test.js` 「실저장소에 [vNEXT] 자리표가 남아 있지 않다」가 빨개져 그 뒤 모든 세션의 `/deploy` 게이트가 막혔다(유호님이 다음 날 직접 신고). 급소는 그 세션이 규칙을 어긴 게 아니라 **규칙을 읽고 그대로 했다**는 것이다 — 커밋 본문이 「버전은 [vNEXT] 그대로 둔다 — 배포하는 세션이 채번한다(먼저 박으면 남의 배포를 막는다 F078)」이라고 F078 을 인용하면서 정확히 F078 의 증상을 냈다. 오독의 출처가 코드에 있다: ①`tools/bump-version.js:234` 「이 자리표는 `\[v9\.\d+` 에 안 걸려 **애초에 안 빨개진다**」 ②`.claude/hooks/auto-commit.js:39` 「`[vNEXT]` 헤더는 **그대로 커밋해도 안전** — bump-version 의 자리표 설계가 그 목적이다」. 둘 다 `tests/버전채번.test.js` 의 전용 검사가 생기기 전에 쓰인 문장이고, 그 뒤로 아무도 안 고쳐 낡았다. 판정이 세 층(bump 주석·auto-commit 주석·deploy SKILL.md:86)에 갈려 적혀 둘이 낡은 형태 — 갈라지는 방향은 여기서도 「통과」였다. 기계층은 통째로 비어 있었다: `.git/hooks/pre-commit` 의 게이트 넷(계약·계약동봉·이해대장·옛글자·인용)은 자리표를 아예 안 본다. 즉 사람이든 auto-commit 이든 자리표를 그대로 커밋할 수 있었고, 사후 검사(`test-ci`)만 있어 「고칠 수 있는 사람과 발견하는 사람이 반드시 달라지는」 F351·F379 와 같은 구조였다. ⚠ 이 자리에는 처방끼리 부딪히는 갈래가 실재한다 — 클라우드·폰 세션은 태그 push 가 거부돼 bump-version 이 「여기서 번호를 확정하지 않는다 — 헤더를 [vNEXT] 로 둔 채 커밋하면 PC 세션이 반입 때 채번한다」고 **시킨다**(F196). 그 처방과 커밋 게이트는 정면으로 부딪히므로 게이트에 탈출구를 열고 그 자리를 이름으로 적어야 한다(F103 — 따를 수 없는 처방은 우회를 정상 통로로 만든다). | `tools/자리표검사.js` 신설 + pre-commit 다섯째 게이트 등재(`205b3af8`). 판정을 `bump-version.자리표자리(내용)` **하나**로 모아 디스크를 읽는 `pendingPlaceholders` 와 커밋될 blob 을 읽는 검사기가 같은 함수를 쓰게 했다 — F339 가 같은 패턴을 네 곳에 손으로 적어 라벨 자리표를 통째로 놓쳤던 자리라 여기서 갈래를 원천 차단했다. 게이트는 **인덱스**를 읽는다(작업본이 아니다 · F302): `git add` 뒤 채번하면 작업본만 깨끗해지고 인덱스엔 자리표가 남는데, 작업본을 보는 게이트는 그 커밋을 통과시킨다. 범위는 `배포파일들()`(16개)을 그대로 받는다 — 손으로 안 적는다. 넓히면 `CLAUDE.md`·`deploy/SKILL.md`·회귀 파일처럼 자리표를 **설명하려고** 그 문자열을 적는 문서를 고치는 커밋이 전부 막히고, 따를 수 없는 처방은 우회를 정상 통로로 만든다(F103). 오독의 출처였던 두 문장(`bump-version.js` 「애초에 안 빨개진다」 · `auto-commit.js` 「그대로 커밋해도 안전」)은 「**미커밋인 동안만** 안전하다」로 고치고 각각 왜 틀렸는지를 실측 커밋(`694497ad`)과 함께 적었다. `deploy/SKILL.md` 에는 게이트가 뒤를 받친다는 한 줄을 달았다(`--check` 는 이력·상수 다리까지 보므로 여전히 먼저 돈다). 회귀 `tests/자리표게이트.test.js` 11건 — 탐지력은 전부 픽스처가 지고 실저장소엔 아무것도 안 묻는다(거짓양성 축은 `tests/버전채번.test.js` 잔존 0 검사가 그대로 맡는다). F302 는 **양방향**으로 잰다: 한쪽만 재면 「인덱스를 본다」와 「아무거나 본다」가 같은 초록이 된다. 변이 6/6 물림 — ①인덱스→작업본(F302 2건) ②라벨 없는 패턴(F339 축) ③배포 집합 밖까지 보기(거짓양성 축) ④빈 배포 집합을 통과로 읽기(F207 축) ⑤처방에서 `git add` 안내 빼기(F103 축) ⑥훅에서 게이트 빼기(등록 2건 — 「이름이 적혀 있다」와 「진짜 커밋이 막힌다」는 다른 층 · F044). E2E 는 실 훅을 태운 진짜 `git commit` 이 막히고 **처방대로 채번하면 같은 훅 아래서 통과**하는 것까지 잰다. 대가: ⓐ배포 집합을 못 읽는 날엔 exit 2 로 모든 커밋을 막는다 — 통과로 접지 않은 것은 의도다(빈 목록은 「자리표 0건」과 같은 모양이라 F207 그 자체) ⓑ클라우드·폰 세션은 태그 push 거부로 bump-version 이 「자리표를 둔 채 커밋하라」고 시키므로(F196) 그 갈래엔 `SYNK_SKIP_GUARD=1` 을 열고 차단문이 그 자리를 이름으로 말한다 ⓒ비용 실측 약 600ms(옆 게이트 529ms 와 같은 급 · 대부분 node 기동) ⓓ닫은 것은 없다 — 자리표를 막는 층이 애초에 0이라 대체할 옛 통로가 없었다. 곁가지 실측: 유호님이 신고한 그 적색 자체는 내가 안 고쳤다 — 주인 `local_7aa81186` 이 살아 있었고(박동 0분·죽음 도장 없음) 보드 줄의 다음 걸음이 「③bump→/deploy」였다. 그 세션이 `a074533c [v9.237]` 로 채번해 해소했다. |
