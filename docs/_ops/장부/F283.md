| F283 | 2026-08-09 | 마찰 | board-guard 이관처방이 백틱으로 시작하는 트랙 칸에서는 문구를 아예 못 뽑는다 — 이관문구() 가 따옴표·백틱 앞에서 자르는데(셸 명령치환을 피하려는 것이라 이유는 옳다), 트랙 칸이 백틱으로 시작하면 남는 게 굵게 표시 장식뿐이라 null 이 되고 그 줄은 처방 목록에 영원히 안 나온다. 실측 2026-08-09: 보드의 죽은 주인 완료 줄 7개 중 1개(audio_duration_sec 트랙)가 그 상태였고, 하필 손으로도 문구 고르기가 가장 어려운 줄이다 — 즉 기계도 사람도 못 치우는 자리다(F103 축: 따를 수 없는 처방). 처방 후보: 잘린 조각 중 **비어 있지 않은 첫 조각**을 쓴다(그 줄이면 submissions.audio_duration_sec 가 나오고 백틱이 없어 셸에도 안전하다). F278 과 같은 층이지만 축이 다르다 — F278 은 「어느 줄을 내미나」이고 이것은 「그 줄의 문구를 뽑을 수 있나」다. F278 주인(local_b11d2d9f)이 지금 그 자리를 만지는 중이라 함께 보라고 남긴다. | board-guard 이관문구() 가 비어 있지 않은 첫 조각을 쓴다 + 자르는 글자에 달러·역슬래시 추가(백틱 안쪽이 후보가 되므로) — 회귀 2건 · 변이 2/2 · 실판에서 막혔던 그 줄(audio_duration_sec)을 실제로 아카이브했다 |
