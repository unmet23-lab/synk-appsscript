| F567 | 2026-08-17 | 마찰 | 죽은 세션의 워크트리를 «이어받으면» 하네스가 그것을 안 거둔다 — 갇힘 처방의 마지막 한 칸이 비어 있다  실측 2026-08-17 (`local_950d2850`): 마감한 세션 `7f3c49db` 이 선언만 남기고 죽은 워크트리 (`q102-rescan-target-two-axis` · PR #63)를 `EnterWorktree({path})` 로 이어받아 트랙을 끝내고 `작업가지.js --닫기` 로 머지까지 했다. 그런데 마지막 정리에서 `ExitWorktree(remove)` 가 거절했다: 「이 세션은 그 워크트리의 주인이 아니다 — 직접 `git worktree remove` 해라」.  왜 문제인가 — **이 트랙이 고치는 병과 같은 모양이다.** F403(워크트리 갇힘)·F553(점유 판정)이 「죽은 워크트리를 이어받거나 지워서 닫아라」까지 세워 놨는데, 이어받는 쪽 길의 **마지막 한 칸** (그 워크트리를 거두는 일)은 도구가 안 진다. 내가 손으로 `git worktree remove` 를 친 것은 기억했기 때문이지 무엇이 시켜서가 아니다 — 안 쳤으면 **머지는 됐는데 워크트리만 남은** 상태가 되고, 그건 다음 세션의 SessionStart 에 「갇힘 후보」로 다시 떠서 같은 판정을 또 시킨다. (다행히 이번엔 커밋이 전부 origin 에 담겨 있어 「갇힘」의 실질 피해는 0이었다.)  ⚠ 하네스 도구의 거절 자체는 옳다 — 남의 워크트리를 지우는 것은 F073 계열의 사고다. 병은 「거절」이 아니라 **거절 뒤에 아무도 안 잡는 잔여**다.  처방 후보(판정 아님 — 다음 세션이 고른다):  ㉠ `작업가지.js --닫기` 가 머지 뒤 **자기 워크트리 거둠까지** 한다(주인 판정은 이미 그 자리에 있다).     지금 그 도구는 「로컬 워크트리 정리는 ExitWorktree(remove) 가 한다」고 말하는데, 이어받은     워크트리에서는 그 말이 **따를 수 없는 처방**이다(F103 모양).  ㉡ CLAUDE.md 워크트리 절에 「이어받기」 갈래를 한 줄 더한다 — 지금 절차는 `EnterWorktree` 로     새로 뜨는 갈래만 적혀 있어, 이어받는 길은 문서에 없다(내가 board-guard 안내를 보고 갔다).  ㉢ 그대로 둔다 — SessionStart 의 갇힘 경보가 결국 드러내므로 조용히 새지는 않는다(대가=중복 판정).  🔑 그리고 이번에 잘 돈 것도 같이 적는다: `board-guard` 가 겹침을 막으면서 **「죽은 세션 트랙을 이어받는 것이면 트랙 칸을 바이트 그대로 옮겨 적어라」**를 안내했고, `board-move` 는 남의 죽은 줄을 근거(`--미완확인`) 없이는 안 옮겼다. 이어받기의 **앞쪽 절반은 이미 기계가 진다** — 빈 것은 뒤쪽이다. | |
