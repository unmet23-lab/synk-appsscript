| F493 | 2026-08-16 | 마찰 | 고아 `.git/index.lock` 이 이 저장소의 커밋을 9시간 9분 막았는데, 그동안 장치들이 낸 말은 「다음 실행이 다시 시도한다」였다 — 경합일 때 참이고 고아일 때 거짓인 문장이다.  ■ 실측 2026-08-16(세션 `local_3dba4def` · 세션 시작에서 마주쳤다)   · 세션 시작 훅 3건이 실패했다 — `[인계문수거] 거두지 못했다(git add 실패: fatal: Unable to create '.../.git/index.lock': File exists. Another git process seems to be running...)` · `[보드수거] 거두지 못했다 — 실패 4건`.   · `.git/index.lock` mtime = **08-15 23:16** · `.git/index` mtime = 08-15 23:15 · 내가 잰 시각 08-16 08:24 ⇒ **9시간 8분 생존**.   · 그 창의 커밋 = **0건**. `git log` 로 못박았다: 직전 커밋 `639ee650`(08-15 **23:15:12**) → 다음 커밋 `9f4c80bb`(08-16 **08:24:24**). 잠금 mtime 이 마지막 커밋 1분 뒤다.   · 풀린 직후 **30초 안에 12커밋이 터졌다**(08:24:24~08:24:53 = 보드 4건 + `0d9a3ced` 인계문수거 1건 + …). 밀려 있던 훅들이 한꺼번에 성공한 것 — 「막혀 있었다」의 직접 증거다.   · 그 사이 미커밋이 **68건**까지 쌓였다(세션 시작 `작업본소유자` 실측 · 30분 뒤 18건으로 빠짐). 미추적은 유일한 무보호 상태다(F025).   · 메모리 `github-server-gate` 에 「✅열쇠 8/8 🔴커밋 미완(**남의 lock**)」이 적혀 있다 — 그 창에 실제로 막힌 세션이 최소 1개 있었다는 남의 기록이다.  ■ 무엇이 새나 — 이 저장소는 **경합만** 다뤘고, 고아를 가르는 자리가 0이다   · `.claude/hooks/auto-commit.js:45` 「index.lock 경합(옆 세션 훅과 동시 커밋)은 300ms 3회만 기다린다」   · `tools/friction.js:363` 「세션이 여럿 도는 저장소라 옆 세션의 훅과 index.lock 이 겹친다 — **짧게만** 기다린다」   · `tools/인계문수거.js:110` 「add 실패(index.lock 등)는 사라짐이 아니므로 전과 같이 **전체를 접는다**」   · `.claude/hooks/lib/session-report.js:43` 「`index.lock` 경합 … 이 계열 전체가 「못 읽음 → 0건」」   · F226(08-07)도 index.lock 을 **경합**으로만 다뤘다(board-move 가 남의 lock 에 막혀 죽은 잔재 처리).   ⇒ 전부 「곧 풀린다」를 전제한다. 그 전제가 참인 동안은 재시도가 옳고, 거짓이 되는 순간 **같은 문장이 9시간짜리 침묵을 「정상적인 다음 기회」로 포장한다.** 지침 v9.2 맹점 ④ 그 자리다 — 장치는 안 도는 쪽보다 **맞는 얼굴로 틀린 값**을 내는 쪽으로 더 자주 샌다.  ■ 왜 「사람이 알아서 지운다」가 답이 아닌가   · 이번엔 아무도 안 지웠다 — **9시간 뒤 저절로 사라졌다**(내가 재는 90초 사이에 없어졌다 · 원인 미특정). 즉 사람이 잡은 사건이 아니다.   · 밤에 났다는 것이 완화 사유가 아니라 **악화 사유**다: 이 저장소는 세션이 밤새 돈다(그 창에 살아있는 세션 3~10개). 유호님이 안 보는 시간대라 아무도 못 본다.   · 「9시간」은 이 저장소 기준으로 커밋 **40여 건**에 해당한다(직전 9시간 = 40커밋).  ■ 처방 후보(고르는 것은 이 신고 밖)   ㄱ) **나이로 가른다 — 말만 한다.** 잠금 mtime 이 문턱(예: 10분)을 넘으면 실패문에 한 줄 더 붙인다: 「경합이 아니라 고아일 수 있다(N분째) — 도는 git 이 없는지 본 뒤 치운다」. 판정을 두 곳에 안 적도록 공용 통로 하나에서 파생한다(CLAUDE.md 맹점 ④의 짝 조항). **자동 삭제는 안 한다** — 진짜 도는 git(대형 checkout·gc)을 죽이는 비가역 사고가 된다.   ㄴ) 자동 삭제. 🚫 위험이 이득을 넘는다 — 이 저장소는 세션 10개가 동시에 도는 판이라 「지금 아무도 안 쓴다」를 기계가 확신할 수 없다.   ㄷ) 안 한다 — 다음 고아 때도 9시간 침묵을 남긴다.  ■ 대가(새 장치엔 대가를 함께 적는다 · v9.2)   · **틀릴 때의 모습** = 정말 10분 넘게 인덱스를 쥔 긴 git 작업을 「고아」라고 잘못 부른다. 그래서 ㄱ은 **삭제를 안 하고 말만 한다** — 오진의 대가가 「사람이 한 번 확인한다」로 끝난다.   · **안전한 쪽으로 기운다** = 나이를 mtime 으로 재므로, 잠금을 쥔 채 인덱스를 계속 쓰는 프로세스는 mtime 이 갱신돼 **어려 보인다** ⇒ 경합으로 읽힌다(침묵). 새는 방향이 「조용함」이라 오경보를 안 만든다.   · **닫을 것** = 없다. 새 훅 0(기존 실패문 한 줄에 얹는다)이고 재시도·접기 동작은 바이트 그대로 둔다 — 안전 가드는 억지로 닫지 않는다. | 공용 통로 `tools/lib/git잠금.js` 하나가 잠금의 **나이**로 «경합»과 «고아»를 가르고, 네 호출부가 그것만 부른다(판정을 네 곳에 적지 않는다). 처방 후보 ㄱ 채택 — 말만 하고 **지우지 않는다**.  ■ 무엇이 실제로 막았나 (호출부 넷 = 자동으로 도는 셋 + 사람이 치는 하나)   ① `tools/인계문수거.js` — 「미커밋 그대로, 다음 실행이 다시 시도한다」 옆에 고아를 이름 댄다.   ② `tools/보드수거.js` — 이 도구는 board-move 의 «사유»를 안 들고 있어 「실패 N건」까지만 말한다(그래서 세션 시작이 원인을 안 냈다). 재시도로 **영영 안 풀리는** 사유 하나는 여기서 댈 수 있다. ⚠거절(원칙⑥)은 정상 판정이라 분모에서 뺐다 — 안 가르면 멀쩡한 회차마다 경보다.   ③ `.claude/hooks/auto-commit.js`(Stop) — **결과가 가장 나쁜 자리**다. 수거기가 못 거두면 다음 세션이 다시 보지만, Stop 훅이 매 턴 「이번 턴은 물러난다」만 하고 세션이 끝나면 남는 건 무보호 미커밋이다(F025). `한저장소()` 의 두 실패 반환에 `뿌리` 를 실어 형제 저장소도 **자기** 잠금을 재게 했다(없으면 본 저장소를 재고 있었다).   ④ `tools/friction.js` — 🔴 **여기서 신고문의 가정이 실측에 뒤집혔다**(아래).  ■ 🔴 실측이 신고를 고쳤다 — friction 은 «실패»로도 안 떨어졌다   신고문은 네 자리 모두 「다음 실행이 다시 시도한다」류의 문장을 낸다고 적었는데, friction 은 그보다 나빴다: `git add` 가 잠금에 조용히 지고 → `diff --numstat HEAD` 가 빈손 → **「커밋 건너뜀 — 커밋할 변경이 없다(장부는 저장됐다)」**. 맞는 얼굴인데 값이 틀렸다 — 실제로는 갓 태어난 장부 조각이 **미추적**으로 남는다(F025 무보호). 게다가 그 자리의 옛 처방(「손으로 커밋한다」)은 고아 잠금에서 **그것도 막힌다**(F103). 그래서 ④는 문장 한 줄이 아니라 **갈래 하나를 새로 세웠다**: 미추적이었던 경로가 `add` 뒤에도 `??` 면 「변경 없음」이 아니라 실패로 본다(가르는 자는 명령의 성패가 아니라 지금 파일 상태 — 그 파일이 이미 쓰던 원칙).  ■ 안 한 것 (계약으로 못박았고 변이가 문다)   · **지우지 않는다.** 세션 10개가 동시에 도는 판이라 「지금 아무도 안 쓴다」를 기계가 확신할 수 없다 — 진짜 도는 git(대형 checkout·gc)을 죽이면 비가역이다. 오진의 대가가 「사람이 한 번 확인한다」로 끝나게 뒀다(M4 가 이 계약을 문다).   · **막지 않는다.** 재시도·접기 동작은 바이트 그대로다. 이 통로는 오직 덧붙인다.   · **새 훅 0.** 기존 실패문 네 곳에 얹었다.  ■ 대가 (새 장치엔 대가를 함께 적는다 · v9.2)   · 틀릴 때의 모습 = 10분 넘게 인덱스를 쥔 긴 git 작업을 「고아」라 부른다 → 안 지우므로 대가가 한 줄의 잔소리다.   · 나이를 mtime 으로 재므로, 잠금을 쥔 채 계속 쓰는 프로세스는 **어려 보인다** ⇒ 경합으로 읽혀 침묵한다. 새는 방향이 「조용함」이라 오경보를 안 만든다.   · 문턱 10분의 근거 = 이 저장소의 실제 경합은 1초 미만이다(auto-commit 이 스스로 잡은 예산 300ms×3=0.9초). 10분은 그 600배. 비상구 `SYNK_GIT_LOCK_MIN`.   · 닫을 것 = **없다**(안전 가드는 억지로 닫지 않는다). ⚠ Stop 훅의 곁가지는 `require` 를 별도 try 로 감싸 실패 안전 — 이 훅이 못 뜨면 모든 세션의 미커밋 보호가 함께 꺼지므로 곁가지가 본체를 죽이면 안 된다.  ■ 검증   회귀 `tests/고아잠금.test.js` **19/19 신설**(fail 0 · skip 0) · **변이 15/15**(구멍 0 · 미적용 0).   탐지와 침묵을 같은 무게로 걸었다(가드 맹점 ②) — 19 중 **8건이 거짓양성 검문**이고, 「못 쟀다」는 셋째 칸으로 세워 「없음」으로 안 접는다(F207).   ⚠ 첫 변이 판은 「N/N」을 **안 냈다** — 미적용 3건(M4 찾기 2회 일치 · M5·M6 이스케이프된 백틱 불일치)을 도구가 세 번째 칸으로 찍어 세웠다. 그 셋을 고친 뒤에야 숫자가 섰다(F477 통로가 제 일을 한 자리).   기존 회귀 지킴: 인계문수거·보드수거 49/49 · 장부 4벌 96/96.   ✅ **CI 모사 초록 — 전 커밋 포함해 잰 판이다**(`6cb9e3a7` 뒤 실행 · **3633 중 pass 3616 · fail 0 · skip 12 · todo 5**). 🔑 여기 오는 데 3판이 걸렸고 그 자체가 실측이다: 1판은 `c4f8b8ea` 시점(3629 중 3612 초록), 2판은 **내가 도는 중에 `friction.js`·`git잠금.js` 를 고쳐 churn 거짓 적색**(도구가 「도는 동안 바뀐 파일」을 스스로 이름 대 줬다 — 고치러 가기 전에 재실행하라는 그 처방이 맞았다), 3판이 이 초록이다. ⇒ **CI 모사를 돌리는 동안 저장소를 건드리지 않는다**가 이 판의 교훈이다.   ⚠ 원격 CI 미검증(Actions 소진 · 9/1 리셋).  ■ ⚠ 안 닫은 것 (이 신고 밖 · 알고 남긴다)   · `board-move` 가 잠금에 죽으면 사유가 `보드수거` 까지 안 올라온다(종료 코드만 산다) — 사유 복원은 `tests/보드이관.test.js` 를 든 산 세션 영역이라 안 건드렸다. 지금은 ②의 한 줄이 그 자리를 대신 덮는다.   · **왜 그 잠금이 생겼는지**는 못 잡았다. 이 트랙이 답하는 것은 「났을 때 9시간 침묵하지 않는다」까지다 — 원인(어느 프로세스가 죽었나)은 재현이 없어 미특정으로 남긴다.   커밋 = `c4f8b8ea`(통로+수거기 둘) · `bb74e2ad`(Stop 훅) · `6cb9e3a7`(friction). |
