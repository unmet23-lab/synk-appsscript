| F493 | 2026-08-16 | 마찰 | 고아 `.git/index.lock` 이 이 저장소의 커밋을 9시간 9분 막았는데, 그동안 장치들이 낸 말은 「다음 실행이 다시 시도한다」였다 — 경합일 때 참이고 고아일 때 거짓인 문장이다.  ■ 실측 2026-08-16(세션 `local_3dba4def` · 세션 시작에서 마주쳤다)   · 세션 시작 훅 3건이 실패했다 — `[인계문수거] 거두지 못했다(git add 실패: fatal: Unable to create '.../.git/index.lock': File exists. Another git process seems to be running...)` · `[보드수거] 거두지 못했다 — 실패 4건`.   · `.git/index.lock` mtime = **08-15 23:16** · `.git/index` mtime = 08-15 23:15 · 내가 잰 시각 08-16 08:24 ⇒ **9시간 8분 생존**.   · 그 창의 커밋 = **0건**. `git log` 로 못박았다: 직전 커밋 `639ee650`(08-15 **23:15:12**) → 다음 커밋 `9f4c80bb`(08-16 **08:24:24**). 잠금 mtime 이 마지막 커밋 1분 뒤다.   · 풀린 직후 **30초 안에 12커밋이 터졌다**(08:24:24~08:24:53 = 보드 4건 + `0d9a3ced` 인계문수거 1건 + …). 밀려 있던 훅들이 한꺼번에 성공한 것 — 「막혀 있었다」의 직접 증거다.   · 그 사이 미커밋이 **68건**까지 쌓였다(세션 시작 `작업본소유자` 실측 · 30분 뒤 18건으로 빠짐). 미추적은 유일한 무보호 상태다(F025).   · 메모리 `github-server-gate` 에 「✅열쇠 8/8 🔴커밋 미완(**남의 lock**)」이 적혀 있다 — 그 창에 실제로 막힌 세션이 최소 1개 있었다는 남의 기록이다.  ■ 무엇이 새나 — 이 저장소는 **경합만** 다뤘고, 고아를 가르는 자리가 0이다   · `.claude/hooks/auto-commit.js:45` 「index.lock 경합(옆 세션 훅과 동시 커밋)은 300ms 3회만 기다린다」   · `tools/friction.js:363` 「세션이 여럿 도는 저장소라 옆 세션의 훅과 index.lock 이 겹친다 — **짧게만** 기다린다」   · `tools/인계문수거.js:110` 「add 실패(index.lock 등)는 사라짐이 아니므로 전과 같이 **전체를 접는다**」   · `.claude/hooks/lib/session-report.js:43` 「`index.lock` 경합 … 이 계열 전체가 「못 읽음 → 0건」」   · F226(08-07)도 index.lock 을 **경합**으로만 다뤘다(board-move 가 남의 lock 에 막혀 죽은 잔재 처리).   ⇒ 전부 「곧 풀린다」를 전제한다. 그 전제가 참인 동안은 재시도가 옳고, 거짓이 되는 순간 **같은 문장이 9시간짜리 침묵을 「정상적인 다음 기회」로 포장한다.** 지침 v9.2 맹점 ④ 그 자리다 — 장치는 안 도는 쪽보다 **맞는 얼굴로 틀린 값**을 내는 쪽으로 더 자주 샌다.  ■ 왜 「사람이 알아서 지운다」가 답이 아닌가   · 이번엔 아무도 안 지웠다 — **9시간 뒤 저절로 사라졌다**(내가 재는 90초 사이에 없어졌다 · 원인 미특정). 즉 사람이 잡은 사건이 아니다.   · 밤에 났다는 것이 완화 사유가 아니라 **악화 사유**다: 이 저장소는 세션이 밤새 돈다(그 창에 살아있는 세션 3~10개). 유호님이 안 보는 시간대라 아무도 못 본다.   · 「9시간」은 이 저장소 기준으로 커밋 **40여 건**에 해당한다(직전 9시간 = 40커밋).  ■ 처방 후보(고르는 것은 이 신고 밖)   ㄱ) **나이로 가른다 — 말만 한다.** 잠금 mtime 이 문턱(예: 10분)을 넘으면 실패문에 한 줄 더 붙인다: 「경합이 아니라 고아일 수 있다(N분째) — 도는 git 이 없는지 본 뒤 치운다」. 판정을 두 곳에 안 적도록 공용 통로 하나에서 파생한다(CLAUDE.md 맹점 ④의 짝 조항). **자동 삭제는 안 한다** — 진짜 도는 git(대형 checkout·gc)을 죽이는 비가역 사고가 된다.   ㄴ) 자동 삭제. 🚫 위험이 이득을 넘는다 — 이 저장소는 세션 10개가 동시에 도는 판이라 「지금 아무도 안 쓴다」를 기계가 확신할 수 없다.   ㄷ) 안 한다 — 다음 고아 때도 9시간 침묵을 남긴다.  ■ 대가(새 장치엔 대가를 함께 적는다 · v9.2)   · **틀릴 때의 모습** = 정말 10분 넘게 인덱스를 쥔 긴 git 작업을 「고아」라고 잘못 부른다. 그래서 ㄱ은 **삭제를 안 하고 말만 한다** — 오진의 대가가 「사람이 한 번 확인한다」로 끝난다.   · **안전한 쪽으로 기운다** = 나이를 mtime 으로 재므로, 잠금을 쥔 채 인덱스를 계속 쓰는 프로세스는 mtime 이 갱신돼 **어려 보인다** ⇒ 경합으로 읽힌다(침묵). 새는 방향이 「조용함」이라 오경보를 안 만든다.   · **닫을 것** = 없다. 새 훅 0(기존 실패문 한 줄에 얹는다)이고 재시도·접기 동작은 바이트 그대로 둔다 — 안전 가드는 억지로 닫지 않는다. | |
