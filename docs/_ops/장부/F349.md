| F349 | 2026-08-12 | 실수 | 인계문 화면이 유호님께 4번째로 지적됐다(08-12 스크린샷 2장 — 🟡 277k · 🔴 317k). 앞 세 판(463d6b7a·d180ade9·3307856a)은 전부 「전문을 systemMessage 에서 빼면 된다」로 갔고 전문은 실제로 빠졌는데, 그 자리에 인계 통로 «목차» 4줄이 들어앉아 화면은 그대로였다. `systemMessage` 는 줄마다 `Stop says:` 접두가 붙어 찍히므로 8줄이 9번 찍혔고, `\n\n` 이 만든 빈 줄은 접두만 덩그러니 남았다. 🔑 **새던 것은 「전문이냐 안내냐」가 아니라 줄 수였다** — 그리고 회귀는 「전문을 실었는가」만 봐서 안내가 4줄로 늘어도 초록이었다(가드가 막으려던 축과 실제 증상의 축이 달랐다). ▶ 처방 셋: ①안내 목차 통째 제거(8줄→3줄·빈 줄 0) ②회귀가 systemMessage 의 **줄 수 상한 3·빈 줄 0** 을 전 단계에서 잰다 — 상한 4(=현재+여유 1)로 뒀더니 「한 줄쯤」이 그대로 통과하는 것을 변이로 실측해 여유를 없앴다(여유 칸이 지난 네 판의 증식 경로 그 자체였다) ③F096(「파일 경로 안내는 실물의 대체가 안 된다」) 검사를 **문구에서 파일 실물로** 축 이동 — 옛 검사는 화면에 `인계문.md` 라는 글자가 있는지만 봐서 파일이 0 이어도 초록이었고, 그 글자를 지키려다 안내가 상주했다. | |
