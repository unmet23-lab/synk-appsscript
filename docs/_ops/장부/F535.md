| F535 | 2026-08-17 | 마찰 | 훅통로 회귀의 실저장소 검사가 «훅이 아닌 도구»를 스폰하는 시험을 위반으로 잡아 master 를 빨갛게 세워 뒀다 — 주인 없는 적색이라 남의 배포·트랙 종결 게이트를 상시로 막는다. 실측 2026-08-17: node tools/test-ci.js 가 tests/훅통로.test.js:188 에서 적색이고 지목 대상은 tests/형제초록.test.js:61 이다. 그런데 그 줄은 훅이 아니라 tools/형제초록.js(CLI 도구)를 띄우고, 「미실행은 판정이 아니다」도 이미 스스로 지킨다(r.error·status===null 이면 throw). 규칙이 오탐하는 기전은 위반들() 의 게이트 한 줄이다 — /hooks/i.test(주석뺀) 로 «파일 어딘가에 hooks 라는 낱말이 있으면» 대상으로 삼고, 그 뒤 spawnSync(process.execPath 를 전부 위반으로 센다. 형제초록.test.js 는 훅 등재를 확인하느라 그 낱말을 쓸 뿐이다. 즉 이 가드는 CLAUDE.md 가드 맹점 ①(사람이 실제로 쓰는 표기로 검사한다)과 ②(실저장소에는 거짓양성만 검사한다)를 자기가 어겼다 — 실저장소 팔이 거짓양성을 내는 순간 그 팔은 목적과 정반대로 도는데, 새는 방향이 「적색」이라 조용하지 않은 대신 **아무도 못 고치는 채로 남는다**(주인 세션 61c67fe7 은 죽었고, 고치려면 남의 트랙 파일을 만져야 한다). 처방 후보 ①게이트를 낱말이 아니라 «스폰 대상 경로»로 바꾼다(.claude/hooks 아래를 띄우는 호출만 위반) ②형제초록.test.js 쪽에 도구용 공용 통로를 준다 ③실저장소 팔을 fail 아닌 skip+보고로 내린다(F296 축). ⚠①이 정답으로 보이나 그 가드는 산 트랙이 아니므로 착수 전 board-guard·훅통로 소유부터 본다. | |
