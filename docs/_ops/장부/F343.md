| F343 | 2026-08-12 | 마찰 | 인계문이 「끝난 세션」이라 단언한 세션이 살아서 같은 🎫 를 스스로 처리했고, board-guard 는 그 겹침을 원리상 못 잡는다 — 새는 방향은 「통과」다.  실측 2026-08-12 (`local_ea9cfc54`): 인계문 머리가 「끝난 세션 `52a7abc6` 이 남긴 것」이라 단언하고 🎫 로 「`--commit 52eac5b2` 재검수 → 통과 시 clasp push(215+216 묶음)」을 넘겼다. 나는 규약대로 ①`git log` ②`node tools/board.js` ③메모리를 읽고 착수했다 — 그 시점 보드의 그 세션 줄도 같은 🎫 를 달고 있었다. 그런데 그 세션은 **인계문을 쓴 뒤에도 살아서 자기 🎫 를 직접 처리했다**: 재검수 2회전 완주(P1 4·P2 2) → `52eac5b2` 의 지적을 수리해 **v9.217 `c0f148aa`** 커밋(`Session-Id: local_52a7abc6` 트레일러로 확정) → 자기 보드 줄을 「🎫 다음=`--commit c0f148aa` 재검수」로 갱신. 즉 내가 이어받은 과녁은 **집는 순간 이미 낡아 있었다**.  내가 치른 값: 낡은 과녁으로 이종 검수를 던져 11분(codex API 실비 · `--런중단` 으로 껐다) · 보드 선언→철회 1왕복 · board-move 2건(그중 하나는 곧 다른 세션이 §7-1 로 이어받은 트랙이라 F337 이 경고한 자리를 나도 지났다). 알아챈 경위는 게이트가 아니라 **우연**이다 — 배포 전 라이브 기준선을 재다가 `git log` 에서 v9.217 을 봤다.  왜 안 막혔나 (급소): board-guard 의 겹침 검사는 **트랙 칸 문자열 비교**다. 인계문이 준 🎫 문구로 선언하면(「⓪`52eac5b2` 재검수 → clasp push」) 원 줄의 트랙 칸(「①v9.215 명부스윕 라이브 완주 + 대기열 P0 낡음 정정」)과 글자가 달라 **겹침으로 안 잡힌다.** F304 는 같은 검사의 정반대편을 관측해 뒀다 — 「트랙 칸을 바이트 그대로 적으면 통과한다(승계 선언 우회 통로)」. 두 관측을 합치면 결론은 하나다: **같게 적어도 통과하고 다르게 적어도 통과한다** — 이 검사는 승계와 충돌을 원리상 못 가른다. 그리고 실제 세션은 🎫 를 받으면 **문구를 바꿔 적는 쪽**이 자연스럽다(자기 트랙 이름을 새로 짓는다).  곁들여: 「인계문 = 그 세션은 끝났다」가 거짓일 수 있다는 것 자체가 안 적혀 있다. F332 는 인계문의 「너/나」를 갈랐지만 「끝났다/아직 산다」는 안 갈랐다. `/close` 는 인계문을 쓰지만 그 뒤 세션이 더 일하는 것을 막지 않는다(막을 이유도 없다 — 유호님이 그 창에 계속 말을 걸 수 있다).  처방 후보: ① **인계 사슬로 잇는다** — 선언 칸에 이미 관례로 적는 「`<지문>` 🎫 인계」를 board-guard 가 읽어, **그 지문의 줄이 아직 활성이면** 트랙 칸 글자와 무관하게 겹침으로 잡는다. 표기가 이미 있으니 새 규약이 아니고, 승계(원 줄이 완료·주인 죽음)와 충돌(원 줄이 활성)이 처음으로 갈린다. ② 값비싼 조작(`--던지기`·clasp push) 직전에 **원 세션의 보드 줄을 다시 읽는** 게이트 — 스냅샷이 아니라 현재를 본다(CLAUDE.md 「현재 상태는 현재를 읽어 판정한다」의 인계 판). ③ 인계문 머리의 「끝난 세션」 단언을 생성 시점 사실로 낮춘다(「이 글을 쓸 때 마감 중이던 세션 — 그 뒤로도 커밋했을 수 있다」) + 🎫 옆에 원 줄의 보드 파일 경로를 적어 집기 직전 대조 대상을 명시한다.  ①이 가장 값싸다(표기·데이터가 이미 있다). ②는 ①이 놓치는 「선언 후에 겹친」 경우를 잡지만 호출부가 여럿이다. | `.claude/hooks/board-guard.js` ④ `자리들()` 에 **repo 밖 공유 자원 축** 신설(`공유:clasp-라이브` ← `clasp push`) + 회귀 4 — 커밋 `1c580e17`.  ⚠ **신고문의 원인 진단은 오진이었다. 코드를 열고 정정한다.** 신고문은 「board-guard 의 겹침 검사는 트랙 칸 «글자» 비교라, 🎫 문구로 선언하면 원 줄과 글자가 달라 안 잡힌다」고 적었다. 실제 코드(`자리들()` · 793행)는 자리를 **①트랙 머리의 표식 ②만지는 파일의 경로** 둘로만 세고, 트랙 칸 글자 비교는 「같으면 승계로 통과」하는 예외 한 줄(833행)뿐이다. 즉 F304 가 관측한 「같게 적으면 통과」는 맞지만 「다르게 적으면 통과」의 원인은 글자 비교가 아니었다.  🔑 **진짜 급소**: 자리를 세는 두 축이 **전부 repo 안**을 가리킨다. 이 저장소에서 가장 비싼 공유 자원인 **라이브 Apps Script 에는 경로가 없어서** 원리상 자리로 안 잡혔다 — `clasp push` 는 HEAD 가 아니라 작업본을 통째로 민다(F310·F273). 실측 2026-08-12: 활성 줄에 `clasp push` 를 선언한 배포 트랙이 **동시에 3개**였는데 ④ 는 한 번도 안 울었다(내 철회분까지 4개). CLAUDE.md 세션 규약은 「조율 단위는 repo 밖 공유 자원(…배포키…)도 포함」이라 이미 못박아 두었으므로, 없던 것은 규약이 아니라 **가드의 그 축**이다.  검증: 회귀 4(탐지력 1 · 거짓양성 2 = `배포 0`·🚫 스팬 · 🎫 인계 1). **변이 검증 — 새 축을 끄면 탐지 1건만 빨개지고 거짓양성 3건은 그대로 초록**(버그가 아직 있을 것을 요구하는 회귀가 아니다). board-guard 89/89 · 보드 인접 합쳐 145/145. 거짓양성 축은 실측이 배제했다 — 보드의 비켜남 표기는 `배포 0`·`라이브 배포 0` 두 모양뿐이라 `clasp push` 와 글자가 안 겹친다(F221: 비켜났다고 적을수록 막히면 관습이 죽는다).  🔴 **안 고친 축이 하나 남았고, 그건 대기열 P3 로 넘긴다**(이 신고의 나머지 절반): 인계문 머리의 「**끝난** 세션 `<지문>` 이 남긴 것」이라는 단언은 그대로다 — 그 세션이 인계문을 쓴 뒤에도 계속 커밋할 수 있다는 사실은 어디에도 안 적혀 있고, `/close` 는 인계문을 쓰지만 그 뒤 세션이 더 일하는 것을 막지 않는다(막을 이유도 없다 — 유호님이 그 창에 계속 말을 걸 수 있다). 이번 수리는 **같은 자원을 다투면 막는다**로 사고의 «결과»를 잡지만 «원인»(낡은 🎫 를 집는 것)은 안 막는다. F332 가 인계문의 「너/나」를 갈랐듯 그 축은 「끝났다/아직 산다」를 갈라야 한다. |
