| F545 | 2026-08-17 | 마찰 | ①배포 검수의 **기각이 「바꿔 말하면」 무효**가 된다 — 같은 지적을 같은 날 두 번 기각했다. 기각 키가 사실상 «제목 해시»라 모델이 문장을 고쳐 쓰면 새 키가 되고, 「다음 검수 프롬프트에 기각됨으로 실려 다시 안 올라온다」는 그 도구의 약속이 그 자리에서 깨진다.  실측 2026-08-17 (`local_fd7d2889` · 검수 두 판 · base 408b650d 동일):   · 1판 `0542-42dc04` → 기각 `bd912adad8f5` 「기능 깨짐: 상태기반 과제선택」     2판 `0618-9dae45` → **재등장** `9c8d88902c66` 「기능 깨짐: 상태 기반 과제 선택 런타임」   · 1판 → 기각 `29e29e4a3bd2` 「기능 깨짐: 감각층 소리·햅틱의 학생 앱 배선」     2판 → **재등장** `7d2cb790d85e` 「기능 깨짐: 학생 앱의 소리·햅틱·물리 스프링 배선」   둘 다 근거로 든 문서·줄 번호까지 같다. 바뀐 것은 제목의 낱말뿐이다.  ■ 기전 — `tools/codex-review.js:200` `키(f) = sha256(파일 + '\0' + 제목정규화)`.   정규화는 소문자화·공백접기뿐이라 **동의어·어순·수식어에 무력**하다. 게다가 기능체크 갈래는   `파일` 이 상수 `(기능체크)` 라 키가 **오로지 제목**에서 나온다 — 그 갈래에서 가장 약하다.   (코드 지적은 `파일:라인` 이 붙어 있어 상대적으로 안정적이다 — 실제로 재등장한 넷 중 넷 다 기능체크다.)  ■ 무게 — 배포 게이트가 **원리상 안 닫힌다**. 기능체크는 「이 저장소의 기능이 다 지어졌는가」를   묻는데 진행중 트랙은 늘 있으므로 매 판 새 지적이 나오고, 기각은 다음 판에서 이름만 바꿔 되살아난다.   즉 「고치거나 기각하거나 둘 중 하나로 끝낸다」가 끝나지 않는다. 오늘 이 트랙은 그 루프를 두 바퀴   돌았고(각 20분 + 처분), 두 번째 바퀴는 첫 번째의 결론을 그대로 다시 적은 것이다.   🔑 축이 F541 과 다르다 — 그건 「사람이 키를 밀려 쳐서 엉뚱한 것을 기각」이고, 이건 **키가 맞는데도   같은 지적이 다른 키로 다시 온다**다. F541 가드(사유_파일어긋남)는 이 갈래를 원리상 못 본다   (파일이 `(기능체크)` 면 그 검사가 첫 줄에서 `return null` 한다).  ■ 처방 후보 — 안 지었다(되돌림 비용이 큰 게이트 규칙 변경이라 재료로 남긴다).   ㉠ **기능체크를 배포 게이트의 차단급에서 뺀다.** 배포 게이트가 묻는 것은 「나갈 바이트가 검수를      받았는가」이고 기능체크는 다른 질문이다. 기록·보고는 그대로 두고 «차단»만 뗀다.      ⚠ 대가 = 진짜 「배포될 기능이 깨짐」을 기능체크가 잡던 경로가 같이 죽는다. 그 축이 실제로      몇 건이었는지 먼저 세야 한다(오늘 표본으론 기능체크 P1 6건 전부가 «아직 안 지음»이었다).      ✅ **셌다 (08-17 `local_61ea8c4d` · 검수기록 62판 전량 · 이 대가는 0이 아니다)**: 차단급 137건 = 배포집합 «안» **54** + «밖» 19 + 기능체크 64. 그리고 그 기능체크 64건 중 **22건이 근거·수정방향에서 배포집합 파일을 지목한다**(`엔진_셋업확장.js` 12 · `엔진_콘텐츠AI.js` 6 · `엔진_운영배치.js` 3 · `엔진_수집.js`·`Code.js` — 인쇄 안전·출석 배치 복구·오류사전 배선 계열). ⇒ **㉠ 의 대가는 「오늘 표본 0건」이 아니라 「62판에서 22건」이다** — 하필 지금 밀린 배포집합 그 파일들이다. ⚠ 재는 층을 밝힌다: 지목은 «산문 안 파일명 일치»라 「지금 나갈 바이트의 결함」과 같은 뜻은 아니다(기능체크 갈래엔 기계가 읽을 `파일` 칸이 아예 없다 — 그게 F545 키 병의 뿌리이기도 하다). 그래도 **0 과는 분명히 다르다.**   ㉡ 기각 키를 제목이 아니라 **근거가 지목한 것**(파일 경로·문서:줄)에서 뽑는다. 기능체크도 근거에      문서 경로와 줄 번호를 달고 오므로 재료는 있다. ⚠ 대가 = 같은 문서를 짚은 «다른» 결함까지      한 번에 묻힌다 — 지금보다 넓게 접는 방향이라 새는 방향이 「통과」다.   ㉢ 기각된 지적을 프롬프트에 **사유와 함께** 실어 모델이 같은 부류를 다시 안 올리게 한다.      ⚠ 대가 = 프롬프트가 길어지고, 모델이 따를지는 보장이 아니다(오늘 이미 목록은 실렸는데 재등장했다).   ⏳ 셋 다 게이트의 «차단 범위»를 바꾸므로 유호님 판정 재료로 올린다.   ㉣ **(추가 08-17 `local_61ea8c4d`) `--대상아님` 이 «그 지문에 한해» 게이트를 열게 한다.** 지금 그 레버는 장부에만 남고 게이트는 못 연다(:1544 가 `기각`만 센다) — 즉 **참인데 범위 밖인 지적에는 정직한 출구가 0이다.** 지문 결속이면 배포집합이 한 바이트라도 바뀌는 순간 자동 무효라(검수기록이 이미 쓰는 그 기전) 다음 배포로 안 번지고, `걸러냄:false` 도 그대로여서 다음 회차엔 계속 올라온다. ⚠ 대가 = 「범위 밖」 판정이 그 지문에서 곧 통과가 되므로 **오분류 한 건이 그 배포를 연다** — 그래서 과녁이 진짜 배포집합 밖인지 기계로 함께 재야 한다(오늘 4건은 전부 `tools/` 라 그 검사가 성립했다). ⏳ 이것도 차단 범위 변경이라 유호님 픽에 함께 올린다. | ⏸ 안 닫는다 — 처방 셋(㉠기능체크를 차단급에서 빼기 ㉡기각 키를 근거 기반으로 ㉢사유와 함께 프롬프트에 싣기)이 전부 배포 게이트의 «차단 범위»를 바꾸는 비가역 결정이라 그 신고 자신이 ⏳유호 판정으로 올려 뒀다. 이 세션(local_41cc57e7)이 두 번째 실측을 얹었다: 라이브 배포 밀림 해소 트랙에서 ①검수가 낸 P1 4건이 **전부 배포집합 밖**(기능체크 3 + 남의 활성 트랙 1)이라 나갈 바이트와 무관한데 배포를 막았고, 앞선 회차의 P1 2건은 참이라 실제로 고쳤는데도(PR #86) 게이트가 새 4건으로 다시 닫혔다. 즉 F545 가 예언한 「고치거나 기각하거나로 끝나지 않는다」가 그대로 재현됐다. 판정 자리 = 유호님 픽  ▸ **세 번째 실측 = «기전» 특정 (2026-08-17 `local_61ea8c4d` · 배포 승계)** — 앞의 두 실측은 「기각이 이름만 바꿔 되살아난다」를 봤지만 **이번 차단은 그 축이 아니었다.** 전임이 P1 4건을 사유까지 달아 **전부 처분했는데도**(`--대상아님` ×4 · 14:25) 게이트는 그대로 `block` 이다. 기전: `게이트판정()`(`tools/codex-review.js:1544`)이 미해결을 `!기각키.has(f.키)` 로 세고, `기각키 = 걸러지는키들()` 은 **`처분==='기각'` 만** 담는다 — 그래서 08-17 에 함께 신설된 `채택수리`·`대상아님` 은 **원리상 게이트를 못 연다.** 게이트가 내미는 처방도 `①고친다 ②기각한다` 둘만 대므로(:1549-1552) 참인데 범위 밖인 지적에는 **정직한 출구가 0**이다: `기각` 은 「왜 틀렸는지」를 요구하는 **영구 필터**라, 열려면 다른 산 트랙이 소유한 지적(`board-move`·`보드수거`·`master동기` — 지금 두 워크트리 세션이 미커밋으로 들고 있다)을 **참인 채로 묻는** 값을 치러야 한다. 🔑 **회귀가 이 축을 안 본다** — `tests/검수처분.test.js` 는 `걸러지는키들` 필터축만 단언하고 `게이트판정` 레벨 단언이 **0건**이라, 이 막다른 길은 신설 당일 초록으로 나갔다(신뢰성 맹점 ④ 「맞는 얼굴로 틀린 값」의 정확한 모습 — 레버는 도는데 게이트가 안 열린다). ✅ 「전부 배포집합 밖」은 **기계로 재확인**했다(전임 주장 인용이 아니다): 4건의 과녁이 전부 `tools/` 아래이고 (루트) 배포집합은 루트 16파일(`Code.js`·`appsscript.json`·`contents_*`·`엔진_*` 등)이라 **`tools/` 경로는 구조상 못 든다.** 🔴 그래서 이 배포는 **가드를 고치지 않으면 안 나간다 — 그리고 나는 고치지 않았다**: 차단 범위 변경이 곧 위 ⏳유호 픽 자리이고, 내가 네 번째 변종을 몰래 넣으면 그 픽을 앞질러 무르게 한다(가드 완화 금지 · CLAUDE.md 「안전 가드는 다이어트 대상이 아니다」). `기각` 으로 열지도 않았다 — 참인 지적 3건을 영구히 묻는 대가라 장부 뜻이 깨진다 |
