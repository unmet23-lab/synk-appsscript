| F392 | 2026-08-13 | 실수 | 세션이 synk-memory 원격에 직접 push — 규약은 「세션은 읽기만」(스케줄러·Actions 몫). fast-forward라 피해 0이지만 통로 위반. 커밋까지만 하고 push는 자동화에 맡긴다 (local_9c45304e · e8909eb) | git-scope-guard 규칙⑨+⑨-b(381cbc92·77f892be): push 조각이 보이면 대상 저장소 remote -v 에 synk-memory 가 있을 때 deny(URL 직지정은 조각 글자로), 스케줄러 스크립트 tools/memory-push.cmd 의 손 호출도 같은 위반으로 차단(⑨ 처방문 「push 는 자동화 몫」이 우회 경로가 되는 되먹임 구멍 — 가드 맹점③). 커밋·읽기·언급은 통과(F103 되먹임 검사를 회귀에 포함). 회귀 ⑨ 4+⑨-b 2 · 실경로 되먹임 탐침 5/5 · 스위트 98/98 |
