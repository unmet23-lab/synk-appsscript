| F619 | 2026-08-18 | 마찰 | 점유 신탁이 **원격 전용 가지를 원리상 못 본다** — 그런데 `모름: false` 로, 즉 「재서 얻은 0」이라고 말한다. 새 클론·클라우드 세션에서는 이게 상시 조건이다.  ■ 실측 2026-08-18 (`cloud_ddd2c370` · 이 세션이 첫 10분에 실제로 걸렸다 · 우연이 아니라 동선이 그리로 낸다) · `tools/lib/보드.js:가지끝()` 이 후보 가지를 `git branch --no-merged <기준>` 로 뽑는다 — `-r` 도 `-a` 도 없다. 즉 **로컬 가지만** 본다. · 노트북에서는 안 샌다: 「워크트리+PR」 규약이 트랙마다 로컬 가지를 만들므로 미머지 가지가 전부 로컬에 있다(그 함수 머리말의 비용 실측도 「미머지 가지 10」= 전부 로컬을 전제한다). · **새 클론에는 그 가지들이 `origin/<이름>` 으로만 산다.** 이 컨테이너에서 잰 값: 로컬 미머지 가지 **2개**(내 가지 + master) · 원격 미머지 가지 **14개**. · 그래서 `보드.가지줄들('.')` = `모름: false · 줄수 1`(내 선언 하나) 이고, PR #107 이 방금 얹은 `보드.활성줄전량('.')` = `모름: false · 못본층: [] · 줄수 41 · 그중 가지 출처 **0**`. · 참값은 그게 아니다. 같은 판에서 원격 가지 14개의 `origin/master...<가지>` 순diff 를 직접 떠 보니 **활성 보드 줄 11개**가 거기 살아 있다 — 그중엔 `tools/lib/보드.js` 를 잡은 선언, `엔진_셋업확장.js` 를 잡은 선언, `../SYNK-talk` 트랙 둘이 들어 있다.  🔑 새는 방향이 **「통과」**다. 두 함수 다 `모름` 을 안 세운다 — `못본층: []` 은 「전부 봤다」는 **적극적 단언**이다. 소비자 셋(`board-guard` 겹침 분모 · `대기열.js` 점유 · `friction.js --open` 점유(F610 이 방금 배선했다))에게 그 0 은 「빈 자리다 · 집어라」로 읽힌다.  ■ 내가 실제로 걸린 자리 — 이 신고의 급소다 세션 시작 알림이 「🔴 지금 집을 수 있는 줄이 0이다 — 새 일감을 발굴해 추가한다」를 냈다(F609/F610 이 적은 그 동선 그대로). 장부 열림에서 F609 를 골랐고, 착수 전에 `가지줄들('.')` 로 점유를 물었다 — **`모름: false · 줄수 0`**. 그 말대로였으면 트랙 하나를 통째로 중복 구현했을 것이다. 실제로는 **PR #107 이 바로 그 축을 이미 끝내 놓고 머지 대기 중**이었다. 내가 안 걸린 이유는 도구가 말해줘서가 아니라 **GitHub MCP 로 열린 PR 목록을 따로 열어 봤기** 때문이다 — 그건 규약에 없는 내 임의 동작이다.  ■ F609/F610 · F585 · F615 와 축이 다르다 (같은 트랙에 얹지 말 것) · F610 = 「일감을 **고르는 화면**에 점유 값이 안 실린다」 → 처방이 `friction.js` 배선. 그 배선은 지금 master 에 섰는데, **그 배선이 부르는 판정 함수가 이 사각을 그대로 물려받는다.** 즉 F610 은 닫혔고 이 구멍은 그 «아래»에 남았다. · F585 = 「점유를 **경로**로 묻는 조회가 없다」 → 통로의 문제. 여기선 통로가 있고 돌았고 답까지 냈다 — **답이 틀렸다.** · F615 = 「두 선언이 **10초 차**라 어느 층도 못 봤다」 → 시간 창의 문제. 여기 11줄은 몇 시간~하루 된 선언이라 창과 무관하다. · F542/`원격줄들` = 「머지돼 origin/master 에 있는데 내 트리가 안 받은 줄」 → 그 층은 이미 있다. 이건 **아직 머지 안 된 남의 가지**라 그 층이 원리상 안 잡는다. ⇒ 그 파일 머리말이 스스로 「F529 와 같은 병의 **세 번째 얼굴**」이라 적었는데, 이게 **네 번째 얼굴**이다: 디스크 / 로컬 가지 / origin-master-미반입 / **origin-가지**.  ■ 왜 조용했나 · `가지끝()` 의 「대가」 절이 셋을 적어 뒀는데(묵은 가지 · 신원 · git 못 부름) **「원격 전용 가지」는 그 목록에 없다.** 안 적힌 대가는 안 재진다. · 게다가 같은 함수가 `if (!가지들.length) return '';` 에 「🔑 가지가 없으면 **재서 얻은 0** 이다 — `모름` 으로 안 올린다」를 명시로 적어 뒀다. 그 주석은 노트북에서는 참이고 새 클론에서는 거짓인데, **문장 어디에도 그 조건이 없다.** 맞는 얼굴로 틀린 값(맹점 ④)이 주석으로 못박힌 자리다.  ■ 처방 후보 — 안 지었다(이 신고는 재료다 · `tools/lib/보드.js` 는 `5653b6a5` 의 선언이 아직 서 있다) ㉠ `가지끝()` 이 로컬 + 원격추적(`git branch -r --no-merged`) **둘 다** 훑고 합친다. 파싱·중복제거는 이미 `가지끝파싱()` 이 줄 내용으로 지므로 **새로 지을 것이 없다**. ⚠대가 = 스폰이 가지 수만큼 는다(머리말 실측 = 가지당 약 73ms · 여기선 2 → 16). `board-guard` 는 Write 시점 훅이라 이 값이 사람 손에 붙는다 — 재고 나서 정한다. ㉡ 분모를 **돌려준다** — `{로컬: n, 원격: m}`. 지금은 몇 개를 훑었는지가 반환에도 출력에도 없어서, 0 을 받은 쪽이 「가지가 없다」와 「가지를 못 봤다」를 가를 재료가 아예 없다(F207). ㉢ 원격 ref 가 **낡은 것**은 여전히 못 고친다 — 이 컨테이너는 세션 시작 시 `origin/master` 와 내 가지만 받아 와 있었고, 내가 손으로 `git fetch '+refs/heads/*'` 를 친 **뒤에야** 14개가 보였다. 즉 ㉠ 만으로는 부족하고 「언제 fetch 하나」가 따라온다. 판정 함수 안에서 네트워크를 치는 것은 안 된다(Write 시점 훅이다) — 세션 경계(SessionStart)가 제자리로 보이는데, 거기서 fetch 하는지 안 하는지 **안 쟀다**(F348 — 명시한다). ㉣ 아무것도 안 한다 — 노트북 한 대만 쓰면 안 샌다. ⚠대가: 클라우드 세션은 이미 상시로 돈다(오늘만 `cloud_d427746a` 와 나 둘이다). 그 세션들에게 점유 판정은 **항상** 거짓 0 이다.  ■ 안 잰 것 — 명시한다(F348) · `board-guard` 의 겹침 분모가 이 사각으로 실제 사고를 냈는지는 **안 쟀다**(내 선언은 남과 안 겹쳤다). 위 11줄이 잡은 파일과 겹치는 선언을 클라우드에서 시도해 보면 재진다. · 노트북에서 워크트리를 지운 «뒤»(가지는 남고 워크트리만 사라진 판)에 로컬 가지가 계속 보이는지도 안 쟀다 — 보인다면 노트북 쪽 노출은 정말 0 이다. | 2026-08-18 `741f8c66`+`b7d2f5cc` — 처방 ㉠㉡ 채택(㉢·㉣ 은 안 함 · 아래). `가지끝()` 이 로컬 **과** 원격추적(`git branch -r --no-merged`)을 둘 다 훑고, 중복은 **sha 로** 접는다(이름으로 접으면 아직 안 민 로컬 커밋의 선언이 조용히 사라진다 — 새는 방향이 「그 트랙은 빈 자리다」라 이 함수가 고치려는 병 그 자체다). 노트북은 미머지 가지가 대개 로컬·원격 양쪽에 같은 커밋으로 있어 **비용이 안 는다**. ㉡ 분모를 돌려준다 — `가지줄들().가지분모 = {로컬, 원격, 훑음}` · `활성줄전량().분모.가지수` 로 실어 나르고, **`원격: null` 이면 `모름: true`** 다(반쪽만 본 0 을 「재서 얻은 0」이라 말하지 않는다). 실측(이 컨테이너): 수리 전 `모름 false · 못본층 [] · 가지 0` → 수리 후 `가지 29줄(활성 11) · 분모 {로컬 2, 원격 17, 훑음 17}` · 837ms. 🔴 **수리가 그 자리에서 자기 값을 실증했다**: 고친 직후 `friction.js --open` 이 **F585 를 🎫(집어도 된다) → 🔒(가지 `origin/worktree-f585-occupancy-query`)** 로 뒤집었고, **F619 자신도 🔒(가지 `origin/claude/synk-continue-cbjau8`)** 로 나왔다 — 즉 내가 이 트랙을 집을 때 이미 `ddd2c370` 이 06:52 에 선언해 뒀고 내 선언은 07:03 으로 **11분 늦은 경합**이었다(그쪽은 코드 0줄 · 내가 먼저 착지하므로 F615 ㉢ 의 전환 통로는 그쪽 몫이다 · 남의 줄 무변경). 그 사실을 사람이 아니라 **이 수리가** 드러냈다. 내가 착수 전 점유를 손 루프로 물으며 가지 안의 판을 안 연 것은 별건으로 신고했다(F627). ▶**변이가 내 구멍 2건을 잡았고 그 처방이 구조를 바꿨다**: 합치기·중복접기·분모 판정이 `가지끝()` **실행층 안**에 있어서 「원격 층만 못 봤다」와 「`origin/HEAD` 를 가지로 센다」 두 축을 **픽스처로 연출할 수 없었다**(같은 기준을 쓰므로 로컬이 지면 원격도 진다). 그대로면 「변이 6/8 · 구멍 2」였다 — 그래서 판정을 순수 함수 **`훑을가지()`·`가지목록파싱()`** 으로 떼고 실행층은 git 부르는 몫만 지게 했다(이 파일의 기존 정책 「판정은 순수 파서에」 그대로). 회귀 신설 **8건**(실저장소 픽스처 5 · 순수 3) · **변이 8/8**(구멍 0 · 미적용 0 · 기준선 분모 50) · `test-ci` **pass 4475 · fail 0 · skip 130**(linux). ⚠**㉢ 은 안 했다 — 안 잰 것을 명시한다(F348)**: 원격 ref 가 낡으면(이 컨테이너는 세션 시작 시 `origin/master` 와 내 가지만 받아 와 있었고 손으로 `git fetch '+refs/heads/*'` 를 친 뒤에야 16개가 보였다) 이 수리도 그만큼만 본다. 판정 함수 안에서 네트워크를 치는 것은 안 되고(Write 시점 훅이다) SessionStart 가 제자리로 보이는데 **거기서 fetch 하는지 안 쟀다.** ⚠㉣(아무것도 안 한다)는 기각 — 클라우드 세션이 상시로 돈다. ⚠남는 대가 = 순diff 가 가지당 약 50ms 라 원격 17개면 837ms 이고, 그 값이 `board-guard`(Write 시점 훅)에서 사람 손에 붙는다. 노트북은 이미 로컬 가지 수만큼 치르던 몫이라 **새 비용이 아니라 클라우드가 안 치르던 몫**이다 — 낮추려면 다시 재라. |
