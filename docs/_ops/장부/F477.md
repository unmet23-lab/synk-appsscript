| F477 | 2026-08-15 | 실수 | 변이 스크립트가 CRLF 파일에 `\n` 정규식을 써서 변이 2건이 **조용히 미적용**됐다 — 「변이 4/4」라는 맞는 얼굴의 틀린 숫자가 나올 뻔했다  `crewcard/크루카드.js` 는 CRLF 인데 변이 정규식이 `\n    \}\n` 처럼 LF 만 봤다. `String.replace` 는 안 맞으면 **원본을 그대로 돌려준다** — 예외도 경고도 없다. 그러면 변이본 = 원본이라 테스트가 초록이고, 그 초록은 「회귀가 못 잡았다」가 아니라 **「변이가 아예 안 걸렸다」**인데 둘이 같은 모양이다. 지침 맹점 ④(맞는 얼굴로 틀린 값)의 전형이고, 이 저장소의 변이 검사 전부가 밟을 수 있는 자리다.  해소 — 스크립트가 `변이본 === 원본` 을 먼저 보고 그 건을 **「⚠ 적용 안 됨」으로 따로 찍는다**(통과로 안 센다). 그래서 1차 실행이 「변이 4/6 · 미적용 2」로 나왔고, 정규식을 `조각내기()`(NL 상수 기준 인덱스 절단)로 바꿔 재실행해 7/7 이 됐다. 경계를 못 찾으면 `throw` 한다 — 조용한 미적용이 다시 생기지 않게 전제 붕괴를 실패로 드러낸다.  🔑 다음 사람에게: 변이 검사를 새로 짤 때 **①적용 여부를 먼저 단언하고 ②그 실패를 통과와 다른 모양으로 찍는다.** 이 둘이 없으면 「변이 N/N」은 검증되지 않은 숫자다. | tools/변이.js 공용 통로 신설 + tests/변이통로.test.js (커밋 56199a3d) — 세션마다 손으로 다시 쓰던 하네스를 통로 하나로 모았다. 신고 원문을 그대로 되넣어 확인했다(F083): 자기변이 M1 = 「줄끝 번역을 없앤다」가 정확히 F477 의 버그이고 회귀가 그 자리에서 빨개진다(포획). 통로가 막는 것 = ①찾기 문자열의 줄끝을 대상 파일에 맞춰 번역(호출부는 늘 `\n` 으로 쓴다) ②미적용(못찾음·모호·바이트 동일)을 포획도 구멍도 아닌 **세 번째 칸**으로 찍고, 하나라도 있으면 「N/N」 표기를 아예 안 만든다 ③판정 권위는 exit status·TAP 은 이름/분모 전용이고 둘이 어긋나면 미측정 ④기준선을 같은 통로로 먼저 돌려 초록+분모>0 을 요구(아니면 한 건도 안 돈다) ⑤「전부 skip」= 통과 0·실패 0 을 미측정으로(skip 은 분모에 그대로 세므로 전체만 보면 안 보인다 — 실측). 회귀 17/17 · 자기변이 15/15(구멍 0·미적용 0) · 탐지력 전부 픽스처. 자기적용이 내 회귀의 구멍 2개(M8 exit↔TAP·M10 기준선 미측정)를 잡아 `미측정판정` 순수 함수로 떼어내고서야 물렸다. ⚠남는 한계 = **옛 통로를 기계로 금지하지 못한다** — 변이 스크립트는 스크래치패드에 살아 저장소 훑기가 원리상 못 본다(훅띄우기가 `tests/훅통로.test.js` 로 닫은 자리를 여기선 못 닫는다). 그래서 다음 사람이 이 통로를 «아는가»에 기댄다 — 그 칸은 메모리 인덱스(매 세션 상주)로만 받쳤다. 통로를 안 쓰고 손으로 쓰면 다섯 모양이 그대로 돌아온다. |
