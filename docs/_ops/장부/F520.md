| F520 | 2026-08-17 | 마찰 | Stop 훅의 자동커밋이 「대장 동봉」 게이트를 지나쳤다 — 도구만 커밋하고 화면을 두고 갔다  실측(2026-08-17 00:16 · `local_62eb453e`): · `d17c95ed 자동커밋: 이해대장.js·이해대장.test.js — 미커밋 노출 차단(Stop 훅)` 이   `tools/이해대장.js` 를 커밋하면서 `docs/이해대장.html` 은 **안 실었다**(2 files changed). · 그 게이트는 실재한다 — `9cae299b feat: 정본이 이해대장을 두고 가는 커밋을 «커밋 전»에 막는다`   (유호 지시 08-14) · 검사기 = `tools/대장동봉검사.js` · 회귀 = `tests/대장동봉{,E2E}.test.js`.   그런데 자동커밋은 **통과했다**(커밋이 났다). · 결과: HEAD 에 «도구는 새 판, 화면은 옛 판» 이 잠시 서 있었다. 다음 커밋(`84ccb9c9`)이   사람 손으로 화면을 맞춰서 지금은 갈림 0 이다 — 즉 **아무도 안 맞췄으면 그대로 남았다.**  왜 아픈가: 그 게이트의 존재 이유가 정확히 이 모양이다 — 화면이 도구보다 낡으면 유호님이 보는 대장이 조용히 옛말을 한다. 그리고 새는 방향이 「커밋 성공」이라 눈에 안 띈다 (CLAUDE.md 신뢰성 맹점 ④ · 장치 자신의 결함).  🔴 «왜» 는 미판정이다 — 게이트 조건을 안 읽고 적는다. 후보 둘: ㉠자동커밋 경로가 pre-commit 훅을 안 지난다 ㉡게이트가 「정본(SYNK_철학.md) 변경」에만 발동하고 「도구 변경」은 안 본다. 가르려면 `tools/githooks/pre-commit` 과 `대장동봉검사.js` 의 발동 조건을 열어야 한다.  ⚠ 수리는 안 했다 — 하네스 봉인(유호 승인 08-16 · 「새 장치 0」)이고, 이건 수리 축이지만 조건을 안 읽은 채 고치면 오진이 처방의 얼굴을 한다. 여는 세션이 위 둘을 먼저 가른다. | 후보 둘을 갈랐고(㉠기각·㉡참) 참인 쪽을 고쳤다 — `3ba26d4c`(PR #27 · 커밋 `5b6c8631`).  ㉠ 「자동커밋이 pre-commit 을 안 지난다」 = 기각. `d17c95ed` 에 `Session-Id:` 트레일러가 박혀 있어 그 커밋에서 훅이 실제로 돌았고, `auto-commit.js` 에 `--no-verify` 가 0건이며, 설치본 `.git/hooks/pre-commit` 은 소스와 바이트 동일(12819B)이다.  ㉡ 「게이트가 정본 변경에만 발동한다」 = 참. 다만 결함이 아니라 **선언된 범위**였다 — `tests/대장동봉E2E.test.js` 가 「정본이 안 담긴 커밋에서는 게이트가 아예 안 선다(매 커밋 소음 0)」로 회귀에 못박아 두고 있었다. 참 명제는 **화면의 부모가 둘인데(정본=무엇을 그리나 · 생성기=어떻게 그리나) 게이트가 하나만 봤다**는 것이다.  무엇이 실제로 막나: `tools/대장동봉검사.js` 의 방아쇠를 «부모 둘 중 하나라도 담겼나»로 넓혔다. 판정은 여전히 `tools/이해대장.js --검사` 한 곳이 지므로 화면이 안 바뀌는 생성기 수정은 그대로 통과한다(전건 차단 아님). 처방의 «경로»는 이음매 `SYNK_대장_방아쇠` 로 넘겨, 생성기 커밋에 「정본을 담아라」고 시키던 못 따를 처방을 없앴다(F103·F302).  회귀 27(신설 7) · 변이 6/6 · 미적용 0 · CI 모사 3837건 fail 0. 신고 원문 되넣기(F083)는 변이 ①이 했다 — 방아쇠를 옛판으로 되돌리자 「생성기만 담은 커밋이 막힌다」가 그 자리에서 빨개졌다. 자기 처방 실측: 이 수리 커밋 자신이 생성기를 담아 그 게이트를 지나 통과했다(정본은 안 담겼는데 게이트가 말을 했다 = 새 방아쇠가 실물에서 섰다).  🔴 안 닫은 것: 그리는 «몸»은 여전히 작업본 생성기다(스테이징분을 임시 방에서 못 돌린다 — require·뿌리 때문). 고치지 않고 드러낸다 — 둘이 갈리면 「못 했다」라고 말하고 안 막는다.  ⚠ 이 판에서 낸 숫자를 한 번 틀렸다: 분모를 `git show --name-only | grep` 으로 재다 비ASCII 경로 이스케이프에 걸려 「합계 12 = 동봉 0 + 두고감 12」가 나왔고, 참값은 **동봉 8 + 두고감 4** 다. 별건으로 신고했다. |
