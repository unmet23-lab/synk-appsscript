| F520 | 2026-08-17 | 마찰 | Stop 훅의 자동커밋이 「대장 동봉」 게이트를 지나쳤다 — 도구만 커밋하고 화면을 두고 갔다  실측(2026-08-17 00:16 · `local_62eb453e`): · `d17c95ed 자동커밋: 이해대장.js·이해대장.test.js — 미커밋 노출 차단(Stop 훅)` 이   `tools/이해대장.js` 를 커밋하면서 `docs/이해대장.html` 은 **안 실었다**(2 files changed). · 그 게이트는 실재한다 — `9cae299b feat: 정본이 이해대장을 두고 가는 커밋을 «커밋 전»에 막는다`   (유호 지시 08-14) · 검사기 = `tools/대장동봉검사.js` · 회귀 = `tests/대장동봉{,E2E}.test.js`.   그런데 자동커밋은 **통과했다**(커밋이 났다). · 결과: HEAD 에 «도구는 새 판, 화면은 옛 판» 이 잠시 서 있었다. 다음 커밋(`84ccb9c9`)이   사람 손으로 화면을 맞춰서 지금은 갈림 0 이다 — 즉 **아무도 안 맞췄으면 그대로 남았다.**  왜 아픈가: 그 게이트의 존재 이유가 정확히 이 모양이다 — 화면이 도구보다 낡으면 유호님이 보는 대장이 조용히 옛말을 한다. 그리고 새는 방향이 「커밋 성공」이라 눈에 안 띈다 (CLAUDE.md 신뢰성 맹점 ④ · 장치 자신의 결함).  🔴 «왜» 는 미판정이다 — 게이트 조건을 안 읽고 적는다. 후보 둘: ㉠자동커밋 경로가 pre-commit 훅을 안 지난다 ㉡게이트가 「정본(SYNK_철학.md) 변경」에만 발동하고 「도구 변경」은 안 본다. 가르려면 `tools/githooks/pre-commit` 과 `대장동봉검사.js` 의 발동 조건을 열어야 한다.  ⚠ 수리는 안 했다 — 하네스 봉인(유호 승인 08-16 · 「새 장치 0」)이고, 이건 수리 축이지만 조건을 안 읽은 채 고치면 오진이 처방의 얼굴을 한다. 여는 세션이 위 둘을 먼저 가른다. | ㉡이 참이고 ㉠은 거짓이다 — 게이트의 발동 조건이 「정본이 담겼나」 한 칸이라, 정본이 안 든 커밋에선 아예 안 섰다. 발동 목록을 화면의 입력 다섯(정본·생성기·부품 둘·엔진점수)으로 넓혀 닫았다 (`tools/대장동봉검사.js` · 회귀 `tests/대장동봉{,E2E}.test.js` · 변이 6/6 구멍 0).  가른 방법 = 낡음의 원인을 고정하고 **스테이징 집합만** 바꾼 대조 4수(워크트리 — 형제 저장소가 보이는 층에서만 판정층이 산다): · 도구만 고침 → 화면 낡음(판정기 exit 1)   · 도구만 스테이징(= d17c95ed 모양) → 게이트 exit **0**  ← 이게 F520 · 같은 낡음에 정본만 집합에 더함 → 게이트 exit 1   · 그 정본 편집 단독으로는 화면이 안 낡는다(exit 0) ⇒ 두 경우 사이에서 바뀐 입력은 「정본이 담겼나」 하나뿐이다. ㉠ 반증(추정 아님): 평범한 `git commit` 이 pre-commit 을 그대로 지나 차단됐다. 자동커밋은 `--no-verify` 를 안 쓰고 `core.hooksPath` 도 안 걸려 있다 — **훅은 돌았다.**  🔑 오진할 뻔한 자리 — 좁은 발동 조건은 «실수»가 아니라 **설계된 맞교환**이고 회귀가 이미 못박고 있다(E2E 「매 커밋 소음 0」 · 출력에 `[이해대장]` 이 뜨면 실패). 그래서 「항상 돌린다」로 고치면 그 시험이 깨진다 — 고칠 곳은 판정층이 아니라 발동 목록이었다.  🔴 그리고 **내 회귀가 1차에 구멍 둘이었다**(변이 4/6 · 둘 다 「망가뜨렸는데 전부 초록」): · 교차검사가 게이트의 **소스 글자**를 grep 해서, 목록 한 줄을 주석 처리해도 문자열이 남아 통과했다 ⇒ 게이트를 `require` 해 **값**을 재도록 바꿨다(`require.main === module` 로 명령/모듈을 가름). · 게이트↔판정기 **이음매를 아무도 안 밟고 있었다** — 처방 시험이 env 를 판정기에 직접 넣어서, 게이트가 그 env 를 안 넘겨도 초록이었다(실물에선 판정기가 조용히 폴백해 옛 처방을 낸다) ⇒ 이음매 이름을 게이트 한 곳에서만 짓고, 판정기가 그 이름을 실제로 읽는지 잰다.  곁가지로 처방도 고쳤다(F103) — 전에는 정본을 안 고친 커밋에도 `git commit -- docs/SYNK_철학.md …` 를 내밀어, 그대로 따르면 **정작 담아야 할 생성기가 빠졌다.** 이제 이번에 담긴 입력을 그대로 댄다.  ⚠ 안 닫은 것 = **전이 입력.** `tools/lib/시트도달.js` 가 엔진 원본을 읽으므로 엔진 커밋도 이 화면을 바꿀 수 있는데, 그 층까지 발동 조건에 넣으면 사실상 매 커밋이라 「소음 0」과 부딪는다 — 그 축은 사후 회귀가 계속 진다. 게이트가 **작업본 생성기**로 판정하는 것(스테이징된 판이 아니라)도 그대로 남는 사각이다. |
