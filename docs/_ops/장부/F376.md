| F376 | 2026-08-12 | 실수 | 대기열 줄의 「⏳유호 N」이 낡아 **이미 답하신 것을 다시 여쭀다 — 한 세션에 2건**(2026-08-12 `local_52e7be3a` 실측). ①`ANTHROPIC_API_KEY`: 08-09 에 「지금 안 넣어도 된다 · 재질의 금지」로 판정됐는데 줄이 「유호님 몫(돈)」이라 적고 있어 유호님이 「저번에 결재한 것 같은데 또 필요하냐」고 되물으셨다 ②나침반 문항: 08-12 `local_83092ce5` 가 이미 확정했는데 줄이 「유호 3」이라 적어 그대로 여쭀고 유호님이 같은 답을 두 번 주셨다. 새 축 — 차단자 표식(08-12 신설)은 「무엇이 막나」는 세지만 **「그 판정이 이미 났나」는 안 본다**. 메모리에 확정이 있어도 대기열 줄이 대기로 남으면 그게 이긴다(다음 세션이 여는 것은 대기열이다). 처방 후보 = 표식을 달 때 그 항목의 메모리 토픽에서 확정·재질의금지를 대조. | |
