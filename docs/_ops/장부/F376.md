| F376 | 2026-08-12 | 실수 | 대기열 줄의 「⏳유호 N」이 낡아 **이미 답하신 것을 다시 여쭀다 — 한 세션에 2건**(2026-08-12 `local_52e7be3a` 실측). ①`ANTHROPIC_API_KEY`: 08-09 에 「지금 안 넣어도 된다 · 재질의 금지」로 판정됐는데 줄이 「유호님 몫(돈)」이라 적고 있어 유호님이 「저번에 결재한 것 같은데 또 필요하냐」고 되물으셨다 ②나침반 문항: 08-12 `local_83092ce5` 가 이미 확정했는데 줄이 「유호 3」이라 적어 그대로 여쭀고 유호님이 같은 답을 두 번 주셨다. 새 축 — 차단자 표식(08-12 신설)은 「무엇이 막나」는 세지만 **「그 판정이 이미 났나」는 안 본다**. 메모리에 확정이 있어도 대기열 줄이 대기로 남으면 그게 이긴다(다음 세션이 여는 것은 대기열이다). 처방 후보 = 표식을 달 때 그 항목의 메모리 토픽에서 확정·재질의금지를 대조. | tools/lib/확정대조.js + 대기열.js `--확정대조` — 「⏳유호 줄의 근거 토픽이 «다시 묻지 마라»를 적고 있나」를 세션 시작에 1줄, 조각은 명시 호출로 낸다. 회귀 tests/대기열확정.test.js 11건 · 변이 8/8. ■ 실측이 설계를 두 번 바꿨다: ①표기가 `🚫재질의` 하나가 아니다 — F376 ①의 실제 문장(vendor-api-billing.md:15)은 🚫 없이 「재질의 불필요」라 🚫 한 글자만 보는 검사는 그 건을 통째로 놓쳤다(F384 와 같은 뿌리 — 어휘를 하나에서 파생시킨다). ②반대로 낱말 「재질의」로 넓히면 F376 을 «기록한 문장»이 스스로 경보가 된다(season-review-vessel.md:114 「…재질의한 탓이다」). 그래서 패턴은 «🚫 가 가까이 붙은 재질의/질의» ∪ «재질의·질의 + 금지/불필요» 둘뿐이고, 둘 다 회귀가 양방향으로 못박는다. ■ 🔴 문구가 「낡았다」가 아니라 «대조하라»다 — 토픽 하나에 확정과 미결이 같이 산다(실측: 걸린 2건 다 그 모양). 판정은 여는 사람이 한다(`낡음후보` 와 같은 규약). 그래서 조각을 그대로 인용해 낸다 — 「N건 걸림」만 내면 결국 파일을 열어야 하고 그러면 아무도 안 연다. ■ 실측 08-12: ⏳유호 10건 중 2건 걸림(minigame-spec-4modules · parent-mail-mongolian) · 근거 링크가 없어 «못 잰» 것 2건(라디오24 둘 — 「깨끗하다」가 아니다). ■ 메모리는 저장소 밖이라 CI 엔 없다 → 측정 실패는 0건과 다른 모양으로 낸다(측정:false + 사유 + 분모). 실저장소 검사는 못 읽으면 skip(F296·F207). ■ 남은 자리(내 몫 아님): 대기열 파일 머리말의 표식 규약에 「줄 끝에 `근거=[[토픽]]`」을 적어야 링크 0 이 줄어드는데 그 파일은 `local_a92a1d2e` 가 잡고 있어 안 건드렸다(F073). |
