| F639 | 2026-08-18 | 실수 | **`git reset --hard HEAD~1` 로 «빈 커밋 하나»를 되돌리면서 미커밋 작업 트리 전부를 같이 날렸다.** 되돌리려던 것은 1커밋이었고 실제로 날아간 것은 **30파일·478줄**이다.  ■ 실측 2026-08-18 (`cloud_79415d5c` · 유호 픽 「발전안 1,2,3」 집행 중)  · 트레일러가 실제로 박히는지 재려고 `git commit --allow-empty` 로 빈 커밋을 냈다. 박혔다(`cse_01Scr…`).  · 그 빈 커밋을 치우려고 `git reset --hard HEAD~1` 을 썼다. **`--hard` 는 인덱스와 작업 트리도 함께 되돌린다** —    그 순간 미커밋이던 것이 전부 사라졌다:      `board-id.js` 축 세 갈래 신설 · 호출부 이관 33곳/26파일 · `code-edit-guard` 보드 갈래 ·      `settings.json` 훅 등록 2건. (미추적이던 `tools/환경눈.js`·`tests/지문축.test.js` 는 살았다 —      `--hard` 가 미추적을 안 건드린다는 성질이 우연히 절반을 구했다.)  · 복구에 걸린 것: 스크래치패드에 남아 있던 이관 스크립트 재실행 + 나머지 손 복원. **잃은 바이트는 0**이지만    그건 운이었다 — 이관을 스크립트로 짰기 때문이지, 절차가 지켜서가 아니다.  ■ 왜 신호인가 — 정본이 이미 이 자리를 두 번 가리키고 있었다  · CLAUDE.md 세션 규약: 「**미추적·미커밋을 오래 두지 않는다** — 미커밋 편집은 남의 `rebase --abort` 에    쓸린다(F037) — 즉시 커밋.」 F037 은 **남이** 쓸어가는 판을 적었는데, 이번엔 **내가** 쓸었다.    같은 문장이 막았어야 할 사고이고, 축은 「누가 쓰느냐」가 아니라 「미커밋으로 오래 들고 있었다」다.  · 나는 발전안 1(26파일)을 끝내고도 **커밋하지 않은 채** 2·3으로 넘어갔다. 커밋이 한 번만 끼어 있었으면    `--hard` 는 아무것도 못 가져갔다.  · 그리고 이 실수는 **되돌림 비용이 큰 명령을 「작은 정리」로 읽은** 것이다: 내 머릿속 과녁은 「커밋 1개」였고    명령의 과녁은 「HEAD + 인덱스 + 작업 트리」였다. CLAUDE.md 「되돌림 비용이 큰 판정은 상태를 한 번    바꿔보고 되돌린다 — 단 **되돌리는 절차를 먼저 정한다**」의 그 절차를 안 정하고 실행했다.  ■ 처방 후보 — 안 골랐다(재료다)  ㉠ 빈 커밋 실험은 `--hard` 가 아니라 `git reset --soft HEAD~1` 로 되돌린다(작업 트리 무변경).    더 나은 것은 **아예 안 만드는 것**: 트레일러 확인은 임시 클론이나 `--dry-run` 성 통로에서 한다.    ⚠대가: 사람 규율이라 안 지켜진다 — 이 신고가 그 증거다.  ㉡ `git-scope-guard` 에 `reset --hard` 갈래를 붙인다 — **미커밋이 있을 때만** 막고, 차단문에    `--soft`/`--keep` 과 「지금 날아갈 파일 N개」를 함께 낸다.    ⚠대가: `--hard` 는 충돌 해소·워크트리 정리에서 정당하게 쓰인다(F073 계열 절차에도 나온다) —      막는 방향이 넓으면 BYPASS 를 손버릇으로 만든다(F103). 그래서 **미커밋 0이면 통과**가 조건이다.    ⚠그리고 이 가드는 **자기 처방을 막지 않는지** 확인해야 한다: 차단문이 시키는 `--soft` 가 그 가드를      그대로 지나가는지 되먹여 본다(CLAUDE.md 맹점 ③).  ㉢ 아무것도 안 한다 — 이번에 잃은 바이트가 0이다.    ⚠대가: 0인 이유가 「스크립트로 짜 뒀다」라서다. 손으로 고친 판이었으면 그대로 잃었다.  ■ 안 잰 것 (F348)  · 이 저장소 이력에서 `--hard` 가 미커밋을 실제로 쓸어간 적이 있는지 안 셌다 — 그건 원리상 커밋에 안 남는다.  · `git-scope-guard` 가 지금 `reset` 을 어떻게 다루는지(아예 안 보는지, 다른 갈래로 보는지) 안 열었다. | ⏸ ⏸ **안 고쳤다 — 장치는 안 짓고 열어 둔다.** 신고 당일(2026-08-18) 처방 후보 셋을 적었고 그중 어느 것도 집행하지 않았다. 그 사실을 「닫힘」으로 위장하지 않는다.  ■ 왜 지금 안 닫나 ㉡(`git-scope-guard` 에 `reset --hard` 갈래)이 유일한 기계 처방인데, **막는 방향이 넓으면 BYPASS 를 손버릇으로 만든다**(F103). 안전하게 하려면 조건이 셋 다 서야 한다: ①미커밋이 0이면 통과 ②차단문이 `--soft`/`--keep` 과 「지금 날아갈 파일 N개」를 함께 낸다 ③**그 차단문이 시키는 `--soft` 가 그 가드를 그대로 지나가는지** 되먹여 본다(맹점 ③ 자기 처방 검사). 이 셋을 제대로 세우는 것은 별도 트랙 분량이고, 이 세션은 원격 CI 트랙 중이라 여기서 시작하면 두 트랙이 섞인다.  ■ 그동안 무엇이 이 자리를 지키나 — 정직하게: **사람 규율 하나뿐이다** CLAUDE.md 세션 규약의 「미추적·미커밋을 오래 두지 않는다 — 즉시 커밋」(F037). 이번 사고의 원인이 정확히 그 문장을 안 지킨 것(발전안 ①을 26파일 끝내고도 커밋 없이 ②③으로 넘어갔다)이라, **같은 문장이 다시 막아 줄 것이라는 근거는 없다.** 그래서 ⏸ 이지 ✅ 가 아니다.  ■ 다음 세션이 집을 때의 재료 · 잃은 바이트가 0이었던 이유는 절차가 아니라 **이관을 스크립트로 짜 뒀기 때문**이다(스크래치패드에 남아 재실행됐다). 손으로 고친 판이었으면 그대로 잃었다 — 즉 이 사고의 심각도는 «운»으로 측정됐다. · `--hard` 가 미추적 파일은 안 건드린다는 성질이 우연히 절반을 구했다(`환경눈.js`·`지문축.test.js` 생존).  ■ 안 잰 것 · 이 저장소 이력에서 `--hard` 가 미커밋을 실제로 쓸어간 적이 있는지 안 셌다 — 원리상 커밋에 안 남는다. · `git-scope-guard` 가 지금 `reset` 을 어떻게 다루는지(아예 안 보는지) 안 열었다. |
