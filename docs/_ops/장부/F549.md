| F549 | 2026-08-17 | 마찰 | master 가 「주인 없는 적색」으로 서 있다 — 방향 정본이 검수 프롬프트 상한을 넘었는데 그 사실이 커밋한 세션에 안 닿았다. 실측(2026-08-17 · 얼린 detached `origin/master` 워크트리): `docs/제품방향.md` **7,241자** vs `tools/codex-review.js` 의 `방향상한` **6,000** → `tests/검수제안.test.js` 「방향 정본 파일이 저장소에 실재하고 상한 안이다」가 적색. 내 트랙(재심 모델축)과 접점 0인데 내 PR CI 가 이것 때문에 빨갛다. 🔑 원인은 **두 트랙이 서로를 못 본 채 같은 날 붙었다**: PR #50(GPT 검수 3축)이 방향 텍스트를 프롬프트에 싣고 상한을 세웠고, PR #51(`80947a17` 「엔진 로드맵 신설」 · 유호 확정 08-17)이 그 파일을 키웠다. 둘 다 자기 회귀는 초록이었다 — 상한은 codex-review 쪽 시험이 지고, 방향 파일을 키운 쪽은 그 시험을 안 돌렸기 때문이다. 🔴 고치는 손은 기계가 못 정한다: 줄일 대상이 **유호님 확정 문구**(1년차 재정의·엔진 로드맵)라 CLAUDE.md 「유호님이 배치한 것의 변경은 반영 말고 제안으로」에 걸린다. 그래서 신고만 하고 판정안으로 올린다 — 갈래 셋: ㉠방향 파일을 6,000자 안으로 압축(무엇을 뺄지 = 유호 픽) ㉡프롬프트에 싣는 것을 파일 전문이 아니라 «머리 N자 + 로드맵 절»로 좁힌다(상한은 그대로) ㉢상한을 올린다(프롬프트 비대 — 그 상한이 막으려던 것 자체). | 이미 고쳐져 있었는데 장부만 열려 있었다 — 착지 a0a66d71 (유호 픽 ㉡ · 검수 프롬프트가 방향 정본을 «절 단위»로 싣는다). 실물 대조 2026-08-18: codex-review.js:692~716 이 상한 초과 시 절(##)을 골라 싣고 절이 없으면 잘랐다고 말한다 · docs/제품방향.md 7,134자 > 상한 6,000 인데 tests/검수제안.test.js 30/30 초록 = 주인 없는 적색 해소됨 · 보드 줄도 완료로 아카이브됐다. |
