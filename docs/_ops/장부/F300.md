| F300 | 2026-08-09 | 마찰 | 커밋 메시지에 `Session-Id` 를 손으로 적으면 훅이 비켜서고 **그 커밋은 영구히 주인 미상이 된다.** `tools/githooks/prepare-commit-msg` 는 `git interpret-trailers --if-exists doNothing` 이라, 사람·AI 가 이미 적어 둔 값이 있으면 **틀렸어도 그대로 둔다.** 2026-08-09 실측: 내가 커밋 메시지 파일 끝에 `Session-Id: local_c23ccb25`(보드 지문 8자리)를 손으로 적었고 — CLAUDE.md 가 「훅이 자동으로 박는다」고 이미 말하는데 중복으로, 그것도 축약형으로 적었다 — 훅이 doNothing 으로 비켜서 그 8자리가 그대로 박혔다(커밋 c80db3a6). 다른 커밋 5건은 전부 전체 UUID 다. 증상: `track-collision` 이 곧바로 「**남의 커밋 1건이 내 자리에 착지했다**」고 알렸다(그 훅은 `c.sid !== 내세션` 을 문자열 그대로 비교하는데 트레일러는 8자리, `$CLAUDE_CODE_HOST_SESSION_ID` 는 전체 UUID 라 절대 안 맞는다). 같은 재료를 쓰는 `작업본소유자.js`·인계문·`session-report` 도 같이 틀린다. 새는 방향이 둘이고 **둘 다 나쁘다**: ①내 커밋이 남의 것으로 보여 「새로 짓지 말고 물려라 / 내 작업본이 그 커밋을 안 담으면 남의 줄이 삭제로 실린다」는 잘못된 경고가 나간다(F187·F188 을 가리키는데 실제로는 내 커밋이다) ②track-collision 479줄의 「내 커밋 제목에서 표식 뽑기」는 조용히 0건이 된다 — 미검출은 통과와 같은 모양이다. 오진의 값도 실측됐다: 나는 처음에 이것을 **훅 결함으로 판정하고 유호님께 그렇게 보고했다가** prepare-commit-msg 원문을 열고서야 원인이 내 손이라는 것을 알았다(현재 상태는 현재를 읽어 판정한다 · 커밋 제목·훅 메시지는 간접 증거였다). F291 과 같은 축(소유 판별)인데 재료가 보드 줄이 아니라 **커밋 트레일러**라 그 수리가 안 닿는 자리다. 처방 후보 ①**손으로 적은 값이 내 id 의 진짜 접두사이면 교체한다** — 축약형만 정확히 집고 남의 완전한 UUID 는 안 건드린다(본선) ②`--if-exists replace` 통째로는 **안 된다**: cherry-pick 은 prepare-commit-msg 를 부르므로 원저자 트레일러를 내 것으로 덮어쓴다 ③회귀는 「축약형이 교정된다」와 「남의 완전한 UUID 는 안 덮인다」 둘을 같이 못박아야 한다 — 앞만 검사하면 ②의 사고가 초록으로 지나간다. | --파일 C:/Users/q1212/AppData/Local/Temp/claude/C--Users-q1212-Documents-SYNK-appsscript/1c424b7f-1748-4d08-8389-e808e770972a/scratchpad/해소.txt |
