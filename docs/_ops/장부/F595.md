| F595 | 2026-08-18 | 마찰 | `인계문.js` 가 **미머지 가지 안의 보드 선언을 못 본다** — 그래서 도는 트랙이 있는데도 「이 트랙은 종결로 적혀 있다」를 낸다. F528 이 `board-guard` 에서, F521 이 `대기열.js` 에서 닫은 것과 **같은 사각의 세 번째 자리**다.  실측 2026-08-18 (세션 `90a98dcc` · 마감 직전): - 내 보드 파일에 두 줄이 있다 — ①F583 `✅착지`(머지 완료) ②F585 `작업중`(**PR #93 미머지**). - 워크트리의 `docs/_ops/보드/90a98dcc.md` 에는 둘 다 있는데, `node tools/인계문.js` 는 ①만 집었다. - 상태 어휘를 세 번 바꿔 가며 대조했다: `⏸머지 대기` → `🔵진행 — 머지 남음` → `작업중 — 머지 남음`. **셋 다 결과가 같았다.** 즉 원인은 활성 어휘 판정이 아니라 **읽는 판**이다 — 08-15 확정 「코드 트랙은 워크트리+PR」 이후 선언이 가지 안에 앉는데, 이 도구는 master 판만 읽는다.   (어휘 대조 자체는 값이 있었다 — `진행` 은 활성 어휘가 아니다(`진행중` 이 어휘다). 그건 그 자리에서 고쳤다.)  왜 나쁜가 — **새는 방향이 「끝났다」**라 조용하다. 인계문의 그 문단은 다음 세션에게 「멈추지 말고 새 트랙을 잡아라」라고 지시한다. 그러면 미머지 PR 이 아무에게도 안 잡히고, 그게 정확히 F403(워크트리 갇힘)이 부르는 상태다 — 오늘 F583 이 그 경보의 정확도를 올려 놨는데, 인계 층에서 같은 트랙이 「종결」로 번역되면 그 경보가 뜰 자리조차 안 생긴다.  처방 후보(안 지었다 — 이 세션은 마감 중이고, 과녁이 남의 도구다): ㉠ `인계문.js` 의 보드 조회에 `보드lib.가지줄들()` 을 분모로 더한다 — F521·F528 이 이미 낸 통로라 새로 지을 것이 없다. 대가: git 스폰 1회(실측 226~486ms · 그 함수 머리말). ㉡ 최소한 「가지에 내 선언이 있는데 master 에 없다」를 **모름으로 드러낸다**(F207) — 종결로 단정하지 않는다. ⚠ ㉠이 맞아 보이지만 실측 없이 고르지 않는다 — `인계문.js` 가 master 판만 읽는 것이 **의도**일 수 있다(마감 시점의 정본을 박제하는 것). 그 의도라면 처방은 ㉡뿐이다. 그 머리말을 먼저 연다.  지금 판의 우회(다음 세션이 읽을 것): 이 세션의 실제 다음 할 일은 **PR #93 을 CI 확인 후 `작업가지.js --닫기` 로 머지하고 `ExitWorktree(remove)`** 다. 인계문 블록의 「종결」 문단이 그것과 어긋나면 이 행을 믿는다. | |
