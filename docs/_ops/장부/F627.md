| F627 | 2026-08-18 | 마찰 | **점유를 확인하면서 내 출력이 이름을 대 준 층을 안 열었다** — 그리고 그 자리가 하필 F619(그 층이 안 보인다는 신고) 착수 직전이었다. 실측 2026-08-18 (`local_f959b83b`): F619 를 집기 전에 점유를 물었고, 내가 돌린 루프는 `git ls-remote` 로 가지를 훑어 **`origin/claude/synk-continue-cbjau8` 를 포함한 8개 가지가 `ddd2c370.md` 를 들고 있다**고 화면에 찍었다. 그런데 나는 그 다음에 `grep -n "F619" docs/_ops/보드/ddd2c370.md` 만 돌렸다 — **작업 트리(=master 판)** 다. 거기엔 「🎫 신고만 하고 놓는다」가 있었고 나는 그것을 「집어도 된다」로 읽고 착수했다. 참값은 **그 가지 안**에 있었다: `0aa6139e`(06:52:00) 「보드 선언 ddd2c370 — F619 … 내 🎫 회수」. 내 선언은 07:03:35 로 **11분 늦었다**. 🔑 F348 그 자리다 — 부정 판정(「아무도 안 잡았다」)을 쓰면서 값이 살 수 있는 층을 **열거까지 해 놓고**(내 출력에 가지 이름이 그대로 있었다) 그중 한 층만 열었다. F623 과 같은 계열이되 얼굴이 다르다: 저쪽은 코드 주석을 정본으로 읽은 것이고, 이쪽은 **내가 방금 만든 목록을 읽고도 그 항목을 안 연 것**이다. ⚠ 그리고 이 실수를 잡아낸 것은 사람이 아니라 **내가 그 트랙에서 고친 장치 자신**이다: `가지줄들` 이 원격 가지를 훑게 된 직후 `friction.js --open` 이 F619 를 「🔒 이미 잡혔다 — 가지 origin/claude/synk-continue-cbjau8」로 냈다. 즉 결함의 실증과 수리의 검증이 같은 사건이었다. 같은 실행에서 **F585 도 🎫(집어도 된다) → 🔒(가지 origin/worktree-f585-occupancy-query)** 로 뒤집혔다 — 그걸 골랐어도 같은 사고였다. 처방 후보 ①점유 확인을 손 루프로 하지 않는다 — `node tools/friction.js --open`·`대기열.js` 가 이미 그 판정을 지므로, 손으로 grep 하면 그 도구의 층 목록과 갈라진다(같은 판정을 두 곳에 적으면 갈라진다) ②손으로 볼 수밖에 없을 때는 **작업 트리 판과 가지 판을 둘 다** 연다 — `git show <가지>:<경로>` 가 그 한 줄이다 ③이 실수의 모양을 회귀로 못박을 자리는 없다(사람 절차다) — 대신 ①이 성립하도록 도구 쪽 분모를 넓히는 것이 이번 트랙의 산출물이다. | |
