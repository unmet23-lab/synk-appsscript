| F584 | 2026-08-17 | 마찰 | 가드를 지은 세션은 그 가드의 **라이브 실증을 원리상 못 낸다** — `settings.json` 의 등록을 바꾸면 그 훅이 그 세션 동안 안 돈다. 실측 2026-08-17 (`local_67f76e1d` · F519 트랙): 등록 블록을 옮긴 뒤 저장소 **밖**(스크래치패드)과 **안**(docs/_ops/탐침) 양쪽에 250자 초과 인덱스 줄을 통째 쓰기로 넣어 봤는데 둘 다 통과했다. 같은 Write 에서 `repo-staleness` 는 정상 발동했고 앞서 `board-guard` 도 내 보드 쓰기를 실제로 막았다 — 즉 훅 통로 자체는 살아 있는데 **내가 등록을 바꾼 그 항목만** 안 돌았다. 훅 파일 로직은 멀쩡하다: 같은 페이로드를 새 판·옛 판 훅에 직접 물리니 둘 다 deny 를 냈다(워크트리·메인 트리 양쪽 실측). 원인 후보 1개로 좁혔다 — 하네스는 세션 시작에 훅 스냅샷을 뜨고 **바뀐 등록 항목은 그 세션에서 빠진다**(안 바뀐 항목만 계속 도는 관측과 정합). 확증은 못 했다(하네스 내부라 grep 대상이 아니다 · F184 계열). 무엇이 문제인가: 이 저장소는 가드 트랙마다 「라이브 실증」을 요구하는데(F200·F253 이 그 자리에서 죽은 가드를 잡았다), **등록을 만진 트랙에선 그 요구가 구조적으로 충족 불가**다. 그런데 화면에서 그 미실행은 「통과」와 같은 모양이라(F207) 다음 세션이 「라이브에서 확인했다」로 오독하기 쉽다. 처방 후보 — 안 지었다(재료다): ㉠ 훅 트랙의 종결 조건에서 라이브 실증을 빼고 «다음 세션 첫 확인»을 보드 줄에 남기는 통로를 규약화 ㉡ `tools/훅발동.js` 를 context-budget 전용이 아니라 「지금 세션에 이 훅이 살아 있나」를 재는 통로로 넓힌다(지금은 못 재고, 못 재는 것이 이 신호의 급소다) ㉢ 등록을 바꾼 커밋이 있으면 SessionStart 가 「이 세션엔 그 훅이 없다」를 그 자리에서 말한다. | `.claude/hooks/hook-registration-snapshot.js`(42ecc847 · PR #91) — SessionStart 에 등록 지문(이벤트·matcher·command)을 세션별로 도장 찍고, `settings.json`·`settings.local.json` 편집 **직후** 「내 세션이 뜬 판 vs 지금 파일」을 대조해 갈린 항목을 이름으로 짚는다. 처방 후보 ㉢을 **자리를 옮겨** 지었다 — 원문은 SessionStart 를 제안했지만 그 자리로는 원리상 못 잡는다(등록 변경은 세션 «도중»에 일어나고 SessionStart 는 그전에 끝났다). ⚠ 이 훅이 없앤 것은 결함 자체가 아니라 **오독**이다: 라이브 실증은 여전히 그 세션에서 불가능하고, 바뀐 것은 그 사실이 화면에서 「통과」와 같은 모양으로 지나가지 않는다는 것 하나다(F207) — 알림이 처방까지 준다(「종결 조건에서 라이브 실증을 빼고 보드 상태 칸에 ▶다음 세션 첫 확인 을 남겨라」). 회귀 `tests/훅등록스냅샷.test.js` 13건(탐지 4·거짓양성 3·미측정 2·범위 4) · **변이 12/12 구멍0 미적용0**. 메커니즘은 안 단정한다(하네스 내부는 확증 못 한 자리 · F184 계열) — 문구는 「안 돈다」가 아니라 「못 믿는다」까지만 간다. 원인 후보 ①의 간접 확증은 이 트랙이 먼저 냈다: 직전 세션이 F519 로 세운 memory-index-guard 를 **등록을 안 바꾼 다른 세션**(`local_fbfdb2e9`)에서 재니 네 층이 전부 발동했다(셸차단·3자대조·미측정알림·처방되먹임 · 실물 MEMORY.md 는 SHA256 전후 동일로 무접촉) — 훅 파일도 등록도 멀쩡하다는 뜻이라 경쟁 가설 둘(훅 로직 결함·등록 오류)이 죽는다. 🔴 이 장치 자신의 라이브 실증은 원리상 못 낸다(등록되는 순간 같은 이유로 못 믿는다) — 그래서 탐지력은 픽스처가 지고 라이브는 보드의 「▶다음 세션 첫 확인」이 진다. |
