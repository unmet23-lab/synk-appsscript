| F364 | 2026-08-12 | 마찰 | 형제 저장소가 없으면 「도달 실측」은 skip 인데 그 옆 「화면이 안 갈라졌다」는 hard fail 이다 — 같은 부재가 한 파일 안에서 두 판정으로 갈린다  실측(2026-08-12 · `local_a668c152` · 배포 완주 트랙): 배포 전 층을 가르려고 HEAD 를 임시 워크트리로 떠서 `node tools/test-ci.js` 를 돌렸다. 적색 **1건** — `tests/이해대장.test.js` 의 「실저장소 — 커밋된 화면이 지금 정본과 같다(안 갈라졌다)」. 단정문은 「docs/이해대장.html 이 정본보다 낡았다 — `node tools/이해대장.js` 를 돌려 다시 커밋하라」였다.  🔑 그런데 갈린 줄은 **딱 하나**였고, 그 한 줄이 형제 저장소 의존분이다:   - 커밋된 판: 「📏 엔진 도달 실측 — 도달이 0인 사건 3종 · 생산자는 섰는데 도달이 0인 사건 0종」   - 워크트리에서 다시 그린 판: 「📏 엔진 도달 실측 — ⚠ 못 쟀다 … 형제 저장소(SYNK-talk)를 못 읽었다」  즉 **처방을 따라도 안 낫는다.** 시키는 대로 `node tools/이해대장.js` 를 돌려 커밋하면 그 환경에서 「⚠ 못 쟀다」가 정본으로 박히고, 형제 저장소가 있는 기계에서는 그 순간 같은 테스트가 **반대 방향으로** 빨개진다. 따를 수 없는 처방은 우회를 정상 통로로 만든다(F103).  🔑 급소는 **한 파일 안에서 같은 부재를 두 판정으로 쓴 것**이다. 바로 위 형제 테스트는 이미 옳게 한다 — 「형제 저장소(SYNK-talk) 없음 — 이 환경에선 못 잰다」로 **skip** 한다(「빚 0」과 「안 재봤다」를 안 섞는다는 그 파일 자신의 원칙). 그 원칙이 옆 테스트에는 안 갔다. CLAUDE.md 가 이미 조항으로 든 그 형태다 — 「repo 밖 환경에 기대는 검사는 CI에서 깨진다 … 실저장소 검사도 fail 이 아니라 skip 으로 드러낸다」(F296 계열).  🔴 파급: GitHub Actions 러너에도 형제 저장소가 없다. 지금 Actions 가 한도 소진으로 정지해 **아무도 이걸 못 봤을 뿐**, 리셋(9/1) 뒤 원격 CI 를 켜면 이 한 건이 곧바로 남의 배포 게이트를 막는다. 주인 없이 남은 적색은 남의 게이트를 막는다.  ▶ 처방안(내가 안 고친 이유는 아래): 화면 비교를 형제 저장소가 **읽히는 환경에서만** 하고, 안 읽히면 skip 으로 드러낸다 — 또는 비교 전에 도달 실측 줄을 양쪽에서 같은 자리표로 지우고 나머지만 대조한다(그 줄은 위 형제 테스트가 이미 따로 지고 있다).  ⚠ 안 고치고 신고만 하는 이유: `tests/이해대장.test.js` 와 `docs/이해대장.html` 은 지금 **남의 미커밋 작업본**이고(`docs/SYNK_철학.md` 는 `528aebb5` 가 편집 중 · 이해대장은 그 정본의 파생물), 내 바이트가 남의 커밋에 실려 나간다(F073). 내 트랙은 배포 완주라 이 파일들의 편집권도 선언하지 않았다. | tests/이해대장.test.js — 드리프트 비교에서 «환경 의존 축»만 접고 나머지는 그대로 잰다(`b9c52282`). skip 이 아니라 마스킹을 골랐다: 못 재는 것은 `<div class="실측">` 한 블록뿐이고 그건 바로 위 「도달 실측」 검사가 이미 따로 진다 — 못 재는 한 칸 때문에 잴 수 있는 화면 전부를 놓지 않는다. 신고 원문 그대로 재현했다(F083): 형제 없는 임시 워크트리에서 **HEAD = pass 20·fail 1·산출물 오염** → **수리판 = pass 24·fail 0·산출물 깨끗**. 🆕 그 실측이 신고에 없던 결함 둘을 더 냈고 같이 닫았다: ②**줄끝도 repo 밖 환경이다** — `core.autocrlf=true` 인데 `.gitattributes` 가 `docs/*.html` 을 안 덮어, 새 체크아웃은 CRLF 179줄·도구 산출은 LF 179줄이라 **형제와 무관하게 전 줄이 갈린다**(로컬이 늘 초록이던 이유가 급소: 작업본은 도구가 마지막에 써서 이미 LF 였다 — 「로컬 초록은 CI 초록이 아니다」의 실물) ③**검사가 자기가 보는 것을 바꾸고 있었다** — 도구를 `cwd:ROOT` 로 돌려 커밋된 산출물을 덮어썼고, 형제 없는 환경에서 덮인 열화판을 auto-commit 이 master 로 실을 수 있었다. 되돌리기는 호출부마다 적지 않고 통로 하나(`도구돌리기`)로 뒀다 — 실제로 갈려 있었다(드리프트 검사만 고쳤더니 「몇 칸이 비었나」가 그대로 덮어써 워크트리가 계속 더러웠다 · 맹점 ④). 회귀 +3 전부 픽스처(실측 마스킹·줄끝 접힘·마스킹이 진짜 갈림을 삼키지 않는지) · 25/25 · **변이 4/4**(마스킹 제거·줄끝 접기 제거·되돌리기 제거·마스킹 확대 전부 적색). ⚠ 원격 run 층은 미검증(Actions 한도 소진) — 리셋 뒤 그 층에서 다시 봐야 한다. ⚠ `.gitattributes` 는 안 건드렸다: 저장소 재정규화는 남의 작업본을 흔든다(그 파일 머리말이 스스로 못박은 이유) — 검사 쪽에서 접는 것으로 족하다. |
