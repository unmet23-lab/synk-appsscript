| F481 | 2026-08-15 | 실수 | 가드를 「손 목록 → 정본 도출」로 갈아탈 때 **첫 실행이 기존 경보를 지우는 마이그레이션**이 될 수 있다 — 새 판정의 기준선(명부·HWM 류)이 빈 채로 시작하면 «그때 이미 나쁜 상태»가 전부 「아직 안 태어남/첫 관측」으로 접힌다. #Q89 에서 실제로 그럴 뻔했다(옛 판이 「누락 시트」로 외치던 35종이 정보 줄로 바뀔 참이었다). 내 자기검증·회귀 13·변이 10/10 이 전부 초록이었고 잡은 것은 ①배포 검수 P1 뿐이다. 처방: 기준선을 요구하는 새 가드는 **옛 판이 이미 판정해 둔 상태를 씨앗으로 깔고** 시작한다(씨앗은 역사라 안 낡는다). 그리고 씨앗 로직만 회귀로 덮으면 배선층이 샌다 — 호출부가 null 을 죽이는 변이(// 빈문자열)가 전건 초록을 통과했다(2차 변이가 잡음). | ⏸ 이 세션(e5950ca0)은 F481 을 «인용»만 했다 — 그 신고의 처방(씨앗 깔기)은 #Q89 가 이미 집행했고, 여기서는 그 교훈의 **뒷문장**만 빌려 썼다: 「씨앗 로직만 회귀로 덮으면 배선층이 샌다」. 그래서 F440 장치에 배선 변이 3건(exit 훅 삭제 · 성공이 아니라 실패에서만 울기 · 호출부가 파일칸을 빈 문자열로 죽이기)을 따로 세웠고 셋 다 판정 회귀로는 안 잡히고 board-move 를 프로세스로 띄우는 시험만 물었다. 즉 이 커밋은 F481 을 **적용**한 것이지 **해소**한 것이 아니다 — 닫는 것은 그 신고를 낸 트랙(#Q89 · 720133c1)의 몫이고 그 세션은 이미 마감했다. 닫는 조건 = 「기준선을 요구하는 새 가드가 옛 판의 판정을 씨앗으로 깔았는가」를 기계가 보는 자리가 서는 것(지금은 사람이 매번 대조한다). |
