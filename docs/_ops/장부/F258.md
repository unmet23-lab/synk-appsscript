| F258 | 2026-08-08 | 실수 | 배포 통로가 코드만 밀고 대상 DB 의 계약판을 안 봐서, HEAD 를 올리는 순간 공유 환경이 조용히 깨진다 — 배포 응답은 ✅ 이고 깨진 것은 다음 호출자가 본다. 실측 2026-08-08(내가 냈다): 결과 변수 실왕복을 재려고 SYNK-talk `deliver` 를 HEAD 에서 리허설(synk-core-rehearsal)에 올렸다. `원격배포` 는 v8 ACTIVE 로 성공했고, 이어 돌린 `배달왕복시험` 에서 배치가 전건 실패 — `column "due_at" of relation "submissions" does not exist`. 원인은 c10 커밋(87e7061)이 코드와 마이그레이션 `20260808010000_engine_c10.sql` 을 같이 냈는데 리허설 DB 는 c8 이었다는 것(c9·c10 미적용). 즉 저장소가 DB 보다 앞선 상태였고 배포 통로에는 그걸 볼 눈이 없었다. 기존 가드는 「배포본 ≠ 소스」(같은 파일이 나갔나)만 봤고, 「그 코드가 요구하는 스키마가 거기 있나」는 아무도 안 봤다. 급소 둘 — ①증상이 배포 시점에 안 나온다(배포는 초록, 죽는 건 다음 호출) ②공유 환경이라 그 다음 호출자가 남이다(리허설을 쓰는 다른 세션의 왕복시험이 전부 빨개진다). 해소 = tools/원격배포.js 에 순수 함수 `저장소판`·`판뒤처짐` 을 두고 `--적용` 앞에서 대상 DB 의 engine.schema_migrations 와 대조해 저장소가 앞서면 배포를 막는다(못 읽어도 막는다 — 읽기 실패·기록 없음·이름 깨짐 전부. 알고 넘길 때만 --판무시). 판을 손 상수로 안 두고 마이그레이션 파일 이름에서 뽑아 늘리는 사람이 두 곳을 고칠 일이 없게 했다. 회귀 4건(tests/원격배포.test.js)·변이 2/2·실환경 실측(같은 명령이 실제로 막았다: 저장소 c10 · DB 20260807170000_engine_c8.sql). 남은 것 = 리허설 DB 는 아직 c8 이라 그 환경의 deliver 는 깨진 채다. 되돌리는 길은 마이그레이션 적용뿐이고(원격배포는 HEAD 만 민다) `원격SQL --적용` 은 유호님 승인 몫이라 결정큐에 올렸다. | tools/원격배포.js 저장소판·판뒤처짐 — --적용 앞에서 대상 DB 의 engine.schema_migrations 와 대조해 저장소가 앞서면 차단(못 읽어도 차단) + 회귀 4건(변이 2/2) + 실환경 실측 차단 확인 · SYNK-talk 7c0768b |
