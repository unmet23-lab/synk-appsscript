| F255 | 2026-08-08 | 마찰 | 세션 시작 경고가 「걸러진 대기표식 89줄에 진짜 미결이 섞였는지 확인」을 5일간 매 세션 요구했다 — 89줄은 사람이 한 번에 볼 수 없는 분량이라 확인은 실제로 0회였다. cleanup:128 이 이미 「실행 불가능한 분량의 확인 요구는 확인 0회와 같은 모양이다」로 판정하고 「다음 세션은 전수가 아니라 새로 늘어난 줄만 본다」로 처방을 바꿔 놨는데, 무엇이 새 줄인지 세는 상태가 어디에도 없어 매 세션 같은 전량이 다시 떴다 — 따를 수 없는 처방이 우회(무시)를 정상 통로로 만든 F103 형태다. 실물 대가: 이번에 전수를 한 번 읽자 student-login:30 이 낡은 채 나왔다 — 「직원은 구글 로그인 유리 판정, 유호님 답 대기 중」이라고 적혀 있는데 inspector-auth 는 08-06 에 「직원도 학원 발급 코드 12자」로 뒤집어 운영 적용까지 끝냈다. 그 줄은 걸러진 쪽에 있어 아무에게도 안 보였다. | tools/decision-queue.js 검토 도장 — docs/_ops/결정큐_검토완료.txt 에 지문(토픽+본문 sha1)을 쌓고, 경고는 도장 없는 줄만 센다(분모는 같이 낸다). --버려진 은 새것만, --전체 로 전량, --확인 으로 도장. 회귀 3건(tests/결정큐.test.js: 도장은 분모를 안 지운다 · 본문이 바뀌면 다시 뜬다 · 도장 파일을 못 읽으면 전부 새것). |
