| F617 | 2026-08-18 | 마찰 | 이 저장소가 「CI 초록」이라 부르는 게이트(`node tools/test-ci.js`)가 **진짜 CI 환경에서 3건 적색**이다 — 셋 다 그 게이트 자신의 정본 조항(F296 「repo 밖 환경에 기대는 검사는 CI에서 깨진다 — 실저장소 검사는 fail 아닌 skip으로 드러낸다」)이 이름 대어 금지한 형태다. 그리고 **아무도 못 봤다**: 진실 층인 원격 run 이 2026-08-14 부터 꺼져 있다(Actions 소진 · 마지막 run 31794307388 은 job 2초 = 기동 실패).  ■ 실측 2026-08-18 (`197f1fe3` · ubuntu 컨테이너 · **세션이 나 하나뿐인 격리 환경**)   · `node tools/test-ci.js` 2회 — 둘 다 **4519건 중 fail 3**, 같은 3건. 재현된다.   · 같은 3건을 `master`(38612f2b) 워크트리에서 그 세 파일만 따로 돌렸다 → **똑같이 fail 3**.     ⇒ 브랜치 델타 0. 이 적색은 **주인이 없다**(CLAUDE.md 「주인 없는 적색은 남의 배포를 막는다」).   · 워크플로 `.github/workflows/syntax-check.yml` 은 `ubuntu-latest` 에서 `node --test tests/*.test.js`     를 `actions/checkout` **fetch-depth: 0** 으로 돈다 — 아래 ㉠의 조건이 CI 에 그대로 선다.   ⚠ 안 잰 것: 실제 GitHub runner 에서 돌려 보지는 **못했다**(Actions 0분). 위는 「같은 OS·같은 명령·     같은 체크아웃 옵션」의 재현이지 원격 run 그 자체가 아니다. 리셋(9/1) 뒤 #Q77 로 `push:` 를     되살리는 날 이 셋이 그대로 뜬다는 것이 이 신고의 예측이고, 그때가 검증 시점이다.  ■ 셋의 공통 뿌리 — **판정을 «두 층»에서 재고, 두 층이 이 기계에서만 우연히 일치한다**   유호님 노트북(Windows · 트랙마다 로컬 워크트리 · 지도 폴더 실재)에서는 두 층이 같은 답을 낸다.   그 우연이 깨지는 기계(CI · 클라우드 컨테이너 · 새 클론)에서는 갈라지고, 갈라진 답은 **언제나 fail** 이다.    ㉠ `tests/장부유실.test.js:457` — 「이력에 올라왔던 행이 지금도 전부 있다」      · 이력은 `이력번호(cwd, '--all')` = **모든 ref** 에서 모은다.      · 「사라진 게 아니라 아직 안 온 것」의 증거는 `살아있는후보()` = 상류 · 기본가지 ·        **로컬 워크트리들의 HEAD** 뿐이다(:96~119).      · 즉 재는 층이 «ref» 인데 사면하는 층은 «워크트리»다. 노트북엔 트랙마다 워크트리가 걸려 있어        두 층이 겹치지만, CI 는 워크트리가 0개이고 `fetch-depth: 0` 이라 origin 가지를 **전부** 본다.      · 이 컨테이너 실측 1회차(base 38612f2b): F595 · F610 · F612 · F614 넷이 「유실」로 떴다.        넷 다 **안 지워졌다** — 미머지 가지에 살아 있었다(F595 ← `origin/worktree-f585-occupancy-query`,        F610·F612·F614 ← `origin/worktree-f-open-ledger-occupancy`).      · 2회차(base 49703d30 · 같은 기계 · 같은 커밋의 검사 · 40분 뒤): **F595 하나**로 줄었다.        내가 고친 것은 0줄이다 — 그 사이 세션 `5653b6a5` 가 PR #107 을 머지했을 뿐이다.        🔑 **판정이 장부의 무결성이 아니라 「남이 언제 머지했나」를 따라간다.** 같은 검사·같은 장부에        대해 40분 사이 「4건 유실」→「1건 유실」이 나왔고 그 사이 장부의 안전성은 한 톨도 안 변했다.      🔴 **처방이 따를 수 없는 정도가 아니라 해롭다**(F103 의 최악판): 실패문은 「그 줄을 되돌려 넣어라」        라고 시킨다. 그대로 하면 **남이 지금 들고 있는 미머지 번호를 master 장부에 베껴 넣는 것**이고,        그 가지가 머지되는 순간 같은 번호가 둘이 된다 — 바로 위 `F586 조각이상` 검사가 「겹침」으로        무는 상태를 처방이 만들어 낸다.      ⚠ 강한 보증은 안 건드려도 된다: 「내 HEAD 이력에 있었는데 지금 없다」(`내가지움`)는 ref 와 무관하게        그대로 유실이다(F195·F469 축). 갈라지는 것은 «한 번도 내 가지에 온 적 없는» 갈래뿐이다.    ㉡ `tools/지도대장.js:61` — **죽은 폴백**(코드가 「없으면 대비했다」의 얼굴을 하고 있다)      · 그 줄: `const 바탕 = 바탕화면() || path.join(os.homedir(), 'OneDrive', 'Desktop');`      · 그런데 `바탕화면` = `lib/바탕화면.js` 의 **`경로`** 다(:43) — 못 찾으면 `||` 로 안 가고 **던진다**.        즉 `||` 뒤는 **원리상 도달 불가능한 죽은 코드**이고, 「없으면 이렇게 한다」는 안심이 가짜다.      · 그래서 `훑기()` 가 자기 문서화된 계약(:136 「폴더가 없으면 `있음:false` — 호출부가 **통과와        구분**할 수 있어야 한다」)을 **못 지킨다**. 던지는 쪽이 먼저다.      · 결과: `tests/지도대장.test.js:297` 은 `if (!결과.있음) t.skip(…)` 로 **이미 skip 을 준비해 뒀는데도**        그 줄에 닿지 못하고 fail 한다. 이 세션 SessionStart 의 `[검사기 고장] 지도 검사가 실패했다 —        바탕화면을 못 찾았다: /root/Desktop` 도 같은 뿌리다(같은 결함이 두 화면에서 다른 이름으로 뜬다).      ⚠ 읽는 쪽/쓰는 쪽이 갈린다: `경로()` 가 던지는 것은 **쓰는 쪽**(빌드 산출물이 없는 곳에 나가면 안 된다)        규율이라 옳다. `지도대장` 의 이 자리는 **읽기**다 — `훑기()` 가 `existsSync` 로 다시 거르고,        CLI 도 `있음:false` 면 exit 0 이라 추측 경로에 쓰는 통로가 0이다.    ㉢ `tests/시트도달.test.js:318` — 픽스처가 **한쪽 기계의 표기만** 경로로 성립한다      · 두 표기를 한 배열에 넣고 같은 단언을 건다:        `['/repo/SYNK-appsscript/.claude/worktrees/트랙이름', '\repo\SYNK-appsscript\.claude\worktrees\트랙이름']`      · POSIX 에서 뒤엣것은 **경로가 아니라 파일 이름 한 칸**이다(`\` 는 구분자가 아니다).        `path.resolve` 가 cwd 를 앞에 붙여 버려 후보 둘이 똑같이 `<cwd>/SYNK-talk` 로 접히고,        `/repo/SYNK-talk$` 단언이 원리상 못 선다. 반대로 Windows 에서는 둘 다 성립해 초록이다.      · 즉 이 검사의 답은 **코드가 아니라 OS 가 정한다**. 맹점 ①(「사람이 실제로 쓰는 표기로 검사한다」)의        거울상 — 각 기계에서 실제 표기는 하나인데 둘을 요구한다.      ⚠ 생산 코드(`이해대장.js:141` 의 `[\\/]` 정규식)는 이미 두 표기를 다 받는다. 결함은 픽스처 쪽이다.  ■ 왜 신호인가 — 새는 방향이 **「게이트를 끄게 만든다」**   · 이 셋은 «통과로 샌다»가 아니라 «상시 적색»이다. 그런데 상시 적색의 종착지는 결국 통과다:     빨간 것이 기본값이 되면 사람은 fail 개수를 외워 두고 「평소 그 3건」으로 접는다.     그 순간 **진짜 4번째가 같은 글자로 숨는다** — F616 이 「이 적색은 못 믿는다」로 잡은 것과 같은 축이고,     이번 것은 그보다 오래간다(F616 은 재실행이 처방이지만, 이건 재실행해도 계속 빨갛다).   · 계보: F296(환경 의존 검사) · F103(따를 수 없는 처방) · 맹점 ④(맞는 얼굴로 틀린 값).     ㉠은 셋을 한 자리에서 다 밟는다.   · 실측으로 이미 값을 치렀다. 같은 며칠 사이 세 판정이 전부 갈렸다:       `9b74b64b` 보드 「**CI 모사 4511 fail0**」(노트북) · `163e0921` 「4515 **fail1**=채번경쟁」(노트북)       · 나 「4519 **fail3**」(ubuntu). 같은 스위트다. 「CI 모사」라는 이름이 약속한 것의 정반대다.  ■ 안 잰 것 — 명시한다(F348)   · 스위트 전체에서 이런 「두 층 갈라짐」이 **몇 벌인지 안 셌다**. 이 셋은 이 환경에서 «떨어진 것»일 뿐이고,     Windows 에서만 빨개지는 반대 방향은 여기서 원리상 못 본다.   · 실제 GitHub runner 실행 0회(위 ⚠).   · `skipped 130` 의 내역을 안 봤다 — 그중 일부가 같은 병의 「이미 skip 으로 접힌」 판일 수 있다.  ■ 처방 — 지었다(이 신고는 재료가 아니라 수리 보고다). 각각 「틀릴 때의 모습」을 함께 적었다.   ㉠ `살아있는후보()` 가 **`--all` 이 읽는 층과 같은 층**을 읽게 한다(원격추적 ref·로컬 가지 포함).      그러면 유실 = 「지금 **어느 ref 도** 안 들고 있다」가 되어 기계와 무관해진다.      〖틀릴 때의 모습〗 버려진 가지에 남은 행이 영원히 「뒤처짐」(경고)으로만 남는다 — fail 은 안 난다.      그 대가를 받는 이유: 강한 보증(`내가지움`)은 그대로고, 뒤처짐은 이름과 함께 출력된다.   ㉡ `지도대장.js` 가 `찾기()`(안 던짐)를 쓰게 해 죽은 폴백을 **살린다**. 계약대로 `있음:false` 가 나온다.      〖틀릴 때의 모습〗 바탕화면이 없는 기계에서 「지도 0건」이 정상 출력이 된다 — 그건 skip 이지 초록이      아니라는 것을 `있음` 칸이 이미 들고 있다(리포트가 그 칸을 찍는다).   ㉢ 픽스처를 **그 기계의 실제 표기**로 짓는다(`path.sep` 기준). 반대쪽 표기는 그 OS 에서만 건다.      〖틀릴 때의 모습〗 Windows 전용 회귀가 리눅스에서 안 돌아 커버리지가 반이 된다 — 그래서 «판정 함수»      쪽 커버리지는 표기 무관 픽스처로 따로 못박는다. | 닫혔다 — CI 모사가 ubuntu 에서 **4546 fail 0**(착수 시 4519 fail 3 · master 실물 재확인 `d43c3623`).  ⚠ 먼저 밝힌다: **이 번호는 중복 접수다.** 같은 세 결함을 세션 `110a7190`(가지 `claude/f605-ci-check-jqme8b`)이 병렬로 잡아 F620 으로 신고했다 — 두 신고의 선언 시각을 서로 못 봤다(F615·F070 계열의 아홉째). 정본 처방 ㉢ 그대로 그 판을 반입하고 내 구현은 버렸다. 여기 적는 것은 «무엇이 실제로 막았나» 이지 내가 얼마나 했나가 아니다.  ■ 무엇이 막았나 — 셋 중 둘은 남이, 하나는 그 위에 얹은 델타가   ㉡ `tools/지도대장.js` 가 던지는 통로(`경로`)를 쓰던 것 → `찾기` 로 (`6e693005` · `110a7190`).      그쪽 회귀가 내 것보다 촘촘해 내 판은 **델타 0** 으로 접었다(계약·등록층·행동 3층).   ㉢ `tests/시트도달.test.js` 의 역슬래시 픽스처 → posix 에서 **미실행로 드러냄** (`d4ba6e8b`·`177896f2`).      구현을 안 고친 판정이 옳다 — 그 자리는 남의 활성 트랙이 잠갔고(래칫 `이해대장.js:140·142`),      진짜 수리는 `형제저장소.형제경로()` 이전이다.      델타 한 줄만 얹었다: 못 재는 것은 «경로 값»이지 «워크트리 꼴을 알아보는가»가 아니라,      후보가 하나 늘었는지는 두 표기 모두 어느 기계에서나 성립한다(리눅스 skip 이 영구 미실행로 굳는 몫을 덜었다).   ㉠ `tests/장부유실.test.js` — 그쪽 사면(`열린트랙가지`)이 `worktree-*` 를 걷어냈는데 **한 갈래가 남았다**:      `claude/*` 는 원리상 못 가른다. 지금 도는 클라우드 세션의 가지와 F123 의 «죽은 폰 가지»가 글자가 같다.      실측: 그 사면이 든 판에서도 **F619 가 그대로 「유실」**로 떴다(`origin/claude/synk-continue-cbjau8` =      지금 도는 세션의 신고 · 같은 날 `claude/*` 가지의 PR #113 이 머지됐으니 버려진 가지가 아니다).      ⇒ 이름으로 가르기를 멈추고 **세기**로 갈랐다 (`0a9414a3` · PR #117):        · `내가지움`(내 가지 이력에 있던 행이 지금 없다 = 삭제 · 기계 무관) → **실 저장소 게이트는 여기만**        · `밖에만`(내 계보에 온 적 없고 살아있는 판에도 없다 · 답이 «이 체크아웃»에 걸린다) → 이름과 함께 드러내기만      합(`유실`)은 안 건드렸다 → F123·F195·F215·F469 픽스처가 그대로 그 합을 보므로 탐지력 무손실.      회귀 +3(갈래 둘 + 등록층) · 변이 4/4 구멍 0 · 미적용 0(양쪽으로 쏠리는 것을 각각 못박았다).  ■ F620 도 같이 닫힌 것으로 읽어도 된다 — 남의 줄이라 여기서 처분하지 않는다.   그쪽이 ①②③으로 센 것과 이 번호가 ㉡㉠㉢으로 센 것이 같은 셋이고, 잔여였던 `claude/*` 갈래가 위 델타로 닫혔다.  ■ 안 잰 것 (F348 · 그대로 남는다)   · **실제 GitHub runner 실행 0회**다. 위 초록은 「같은 OS(ubuntu)·같은 명령·같은 `fetch-depth: 0`」의 재현이지     원격 run 그 자체가 아니다. Actions 는 08-14 부터 기동 실패(마지막 run `31794307388` · job 2초)고     워크플로의 `push:` 는 ⏸ 다. **리셋 9/1 뒤 #Q77 로 되살리는 날이 이 신고의 검증 시점이다.**   · 스위트 전체에서 「두 층 갈라짐」이 몇 벌인지는 여전히 안 셌다. 이 셋은 «이 환경에서 떨어진 것»일 뿐이고,     반대 방향(윈도우에서만 빨개지는 것)은 여기서 원리상 못 본다.   · `skipped 131` 의 내역을 안 봤다 — 그중 일부가 같은 병의 「이미 skip 으로 접힌」 판일 수 있다. |
