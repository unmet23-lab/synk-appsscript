| F580 | 2026-08-17 | 마찰 | `git push` 가 **자격증명 대화상자를 띄우려다 TTY 가 없어 멈춘다** — 그리고 `작업가지.js --닫기` 는 그 사유를 **빈 문자열로** 보고해 세션이 원인을 못 본다. 트랙을 닫는 마지막 한 걸음이 통째로 막히는데 화면엔 아무 단서가 없다.  ■ 실측 2026-08-17 (`local_ac18eae8` · F556 트랙 종결 중 · 약 15분 소모)   · `node tools/작업가지.js --닫기` → **`🔴 못 밀었다 — `** (사유 자리가 비었다).   · 손으로 `git push origin <가지>:<가지>` → **2분 타임아웃**. 배경으로 10분 줘도 안 끝났다.   · 같은 명령의 stderr 원문: **`fatal: User cancelled dialog.` / `bash: line 1: /dev/tty: No such device or address` / `error: failed to execute prompt script` / `fatal: could not read Username for 'https://github.com'`**.   · 즉 `credential.helper=manager`(Git Credential Manager)가 **GUI 대화상자**를 띄우려 했고, 이 세션엔 TTY 도 데스크톱 상호작용도 없어 영영 안 끝난다. **막힌 게 아니라 매달린 것**이라 타임아웃 전까지는 「도는 중」과 구별이 안 된다.   · `gh auth status` 는 **초록**이었다(`unmet23-lab` · keyring · scopes `repo`·`workflow`). 즉 인증 자체는 살아 있고 **git 쪽 통로만** 죽었다.   · 우회: `git -c credential.helper="!gh auth git-credential" push …` → **즉시 성공**(설정은 안 바꾼다 · 토큰은 세션을 안 지난다).   · ⚠ 같은 시각 `git ls-remote` 는 **되기도 하고 안 되기도 했다**(첫 호출은 같은 dialog 오류, 두 번째는 ref 를 냈다) — 그래서 「원격이 안 보인다」로 오진하기 쉽다.  ■ 두 층으로 샌다 — 둘 다 새는 방향이 「모른 채 멈춤」이다   ① **보고가 비었다.** `tools/작업가지.js:197` 은 `죽기(\`못 밀었다 — ${밀기.오류}\`)` 인데, 그 `오류` 가 빈 문자열이면 화면엔 사유가 0바이트로 나간다. 처방도 안 붙는다(:139 의 `--열기` 쪽엔 「직접 돌려 본다」가 있는데 `--닫기` 쪽엔 없다). 세션이 다음에 뭘 할지 모른다 — F103 무늬의 사촌(처방이 틀린 게 아니라 **아예 없다**).   ② **매달림을 실패로 안 읽는다.** 자격증명 대화상자는 종료하지 않으므로 `spawnSync` 에 타임아웃이 없으면 그 자리에서 멈춘다. 「느린 네트워크」와 「영영 안 끝남」이 같은 모양이다.  ■ 무게 — 트랙을 **닫는** 자리다   CLAUDE.md 는 「커밋과 push 는 한 벌」이라 못박고, 08-17 저녁의 F570/F577(로컬 master 갈라짐·밀림 게이지)이 전부 「안 민 것」의 대가였다. 이 결함은 그 대가를 **세션이 원인도 모른 채** 만들게 한다: 밀 의사도 있고 인증도 살아 있는데 통로 하나가 조용히 매달린다.  ■ 처방 후보 — 안 지었다(이 신고는 재료다)   ㉠ 저장소의 git 호출을 **비대화형으로 못박는다** — `GIT_TERMINAL_PROMPT=0`(+ `GCM_INTERACTIVE=never`)를 push/fetch 하는 통로에 얹는다. 그러면 매달리는 대신 **즉시 실패**하고 사유가 stderr 로 나온다. ⚠대가 = 정말로 사람이 로그인해야 하는 날에도 실패로 떨어진다(하지만 이 저장소의 세션은 어차피 그 대화상자를 못 본다 — 「전부 원격」 상시 지시와도 맞는다).   ㉡ `--닫기`·`--열기` 의 실패 보고가 **빈 사유를 그대로 내지 않게** 한다 — 비었으면 stdout·exit code·「stderr 0바이트」임을 밝히고, `gh auth status` 대조와 `-c credential.helper="!gh auth git-credential"` 재시도를 처방으로 낸다. ⚠대가 = 처방 문구가 늘어난다.   ㉢ git 이 **`gh` 를 자격증명 헬퍼로 쓰게** 영구 설정한다(`gh auth setup-git`). ⚠대가 = 유호님 환경 설정 변경이라 **내 몫이 아니다** — 올려서 판정받을 자리다.   ⚠ 어느 쪽이든 **자격증명을 코드·환경변수에 박는 갈래는 금지**다(CLAUDE.md: 자격증명만 예외로 영원히 git 눈 밖). | 두 반쪽을 다 닫았다. ㉠보고: 실패사유() 가 stderr·error·signal·종료코드를 순서대로 훑어 사유 칸을 «절대» 비우지 않고, 타임아웃이면 매달림이라고 이름을 대고 제한 초까지 적는다(옛 코드는 stderr 하나만 봐서 status=null·stderr 0바이트일 때 빈칸을 냈다). ㉡막힘: 자격증명 흔적 4종이 맞으면 gh 헬퍼로 한 번만 다시 민다 — 빈 헬퍼를 먼저 넣어 GCM 을 지우는 순서가 규약. 열기·닫기 둘 다 가지밀기() 한 통로를 지나고 처방도 함께 낸다. 회귀 ⑪⑫⑬ · 변이 M1~M8 전건 포획. 남은 것은 노트북 자체의 helper 수리 = F581(유호님 몫). |
