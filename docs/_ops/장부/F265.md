| F265 | 2026-08-09 | 마찰 | 검사가 **첫** 정의를 읽고 있어, 뒤 조각이 판을 올려도 영영 못 본다 — `tests/검수큐.test.js` 의 뷰 파서가 합본에서 `create or replace view engine.review_queue` 를 `.exec()` 로 **처음 한 번**만 집었다. 합본은 조각을 이어붙인 것이라 뒤 조각이 같은 뷰를 다시 정의하면 살아 있는 판은 마지막 것인데, 검사는 c8 의 12열을 계속 재고 초록을 낸다. 오늘 판을 22열로 올리며 드러났다(안 고쳤으면 새 판이 검사 밖에서 조용히 섰다). 같은 계열 3번째다 — F253(선점 탐지가 이주 뒤 **스텁**을 봤다) · F240 축B(배포 게이트가 **작업본**을 봤다)와 한 형태: **재는 대상이 낡았고 증상은 언제나 「통과」다.** | tests/검수큐.test.js `뷰정의()` — matchAll 로 모아 **마지막**을 쓴다(talk a5fb940). 곁들여 확인 쿼리 계수 앵커 넷을 `as <이름>(?![\w가-힣])` 로 못박았다: 변이 ⑦(계수를 `검수판열_지움` 으로 개명)이 앵커 없이 통과하는 것을 실측해서 잡았다. 변이 8/8. ⚠ 이 처방은 **이 파일 한 곳**이다 — 합본을 읽는 다른 검사에도 같은 형태가 있는지는 안 쟀다(talk tests/ 는 남의 세션이 동시에 만지는 중이라 전수는 그쪽 몫). |
