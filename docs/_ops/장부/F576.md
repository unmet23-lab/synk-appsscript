| F576 | 2026-08-17 | 마찰 | 대기열 점유의 ID 조인이 「만질 파일」 칸까지 읽어, 끝난 「지나가다 고침」 줄 하나가 빈 일감 둘의 주인으로 적혔다. 실측 2026-08-17: 보드 줄 ba795342 의 만질 파일 칸이 `docs/_ops/작업대기열.md`(충돌 해소 — #Q102 갱신본 + #Q103 신설 둘 다 살림) 인데, 그 줄은 남의 머지 충돌을 풀며 두 대기열 줄을 살렸다고 적었을 뿐이고 스스로 「내 트랙 아님 · 지나가다 고침」이라고 썼다. 그런데 조인 ⓪ 는 「나머지 신호는 볼 것도 없다」는 최강 신호라, #Q103 의 참 주인(파일 겹침 `tools/재질굽기.js` ← 재질 대조 트랙)을 통째로 가리고 그 줄을 주인으로 적었다. 새는 방향이 셋 다 조용하다: ①세션 시작 훅이 「지금 집을 수 있는 줄이 0이다」를 냈고(아무도 안 집으니 아무도 안 부딪혀 신고가 안 난다) ②`--집기 #Q103` 은 「죽은 세션이면 그 줄부터 치운다: board-move "master 가 갈라져…"」로 남의 끝난 줄을 지우라는 처방을 냈다(F103 모양) ③잡혔다/아니다가 아니라 「누가」만 틀리는 자리라 화면은 멀쩡했다. 같은 파일이 2026-08-13 에 이미 「인용을 선언으로 읽으면 빈 자리가 잠긴다」를 적고 상태 칸을 뺐는데, 세 칸 중 하나만 닫아 나머지 반쪽이 남아 있었다. 분모: 그날 활성 34줄 중 #Qnn 을 가진 줄 6 = 트랙 칸 5(전부 참) + 파일 칸만 1(전부 거짓) + 양쪽 0. | `tools/대기열.js` 의 `행()` 이 조인 키를 **트랙 칸에서만** 뽑는다 — 파일 칸은 「무엇을 만지나」이지 「무엇을 집었나」가 아니고, 정본 통로 `트랙칸()` 이 `#Qnn` 을 언제나 제목 머리에 박으므로(「조인 키라 절대 안 자른다」) 좁혀서 잃는 참이 0이다(그날 분모 트랙칸 5 · 파일칸만 1). 좁힌 쪽이 조용히 새지 않게 **대가를 같은 커밋에 적었다**: 새 `큐인용`(파일 칸에만 앉은 키)을 출력이 🪧 로 지목하고 「인용이면 그대로 두고 선언이면 트랙 칸으로 옮긴다」는 처방을 낸다 — 열린 번호만 본다(닫힌 번호까지 지목하면 매 세션 같은 소음이고 소음은 알림을 죽인다). ⓪ 만 좁혔고 ①씨앗·②줄참조·③파일자리는 그대로 파일 칸을 본다(한 번에 하나만 바꾼다 · F045). 회귀 4 신설(`tests/대기열점유.test.js` — 인용 픽스처·트랙칸 양방향·큐인용 재료·실저장소 거짓양성) · **변이 6/6 구멍 0 · 미적용 0**(기준선 초록 27건). 얼린 입력 위 전/후 대조(같은 보드·같은 대기열에서 조인 재료만 갈았다): 판정이 바뀐 줄은 **#Q103 하나** — 주인이 「끝난 지나가다-고침 줄」에서 「재질 대조 트랙(파일 겹침 `tools/재질굽기.js`)」으로 바로잡혔다. 🔴 **초록은 안 늘었다**(옛 0건 → 새 0건) — #Q103 은 그 파일 겹침으로 여전히 참되게 잠겨 있다. 이 수리가 판 것은 「집을 수 있는 줄」이 아니라 **「누가 잡고 있나」가 조용히 틀리던 자리**다. |
