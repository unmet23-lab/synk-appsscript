| F670 | 2026-08-19 | 마찰 | 펠트문서 스킨 var(--천-cream) 죽은 참조 — 중성축 개명(08-18)이 CSS 변수 소비처를 못 따라감·표지 오브 결이 조용히 빠진 채 렌더(CSS는 죽은 var를 에러 없이 삼킨다·F647 계열). 수리=목록[0] 파생 + 회귀 죽은천변수(탐지력 픽스처 포함) | 실측하니 죽은 참조가 0 이다 — 08-22 전수: `docs/엔진/SYNK_*_소개서.html` **6벌 전부 선언 1 · 사용 1** 로 짝이 맞는다(`--천-cream:` 선언과 `var(--천-cream)` 사용). 장부 밖에서 «선언 없이 쓰는» 자리 0건. ⚠ **장부의 🩹 착지 표식은 거짓이었다** — 그 번호를 단 커밋 3ec14c01 을 열어 보니 `.github/workflows/syntax-check.yml` 32줄만 고쳤고(node 20→22) CSS 는 안 건드렸다. F544 반례(번호만 달고 딴 걸 고친 커밋)의 재현이라, 착지 표식은 «열어 보라는 신호»이지 «닫혔다는 증거»가 아님을 다시 확인한다. 회귀 「죽은천변수」는 **안 섰다** — 지금 값이 맞아서 닫는 것이지 기계가 지켜서가 아니다. |
