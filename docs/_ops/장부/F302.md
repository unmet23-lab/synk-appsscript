| F302 | 2026-08-09 | 실수 | 한 트랙의 파일 변경을 두 커밋으로 나눴더니 그 3분 틈에 옆 세션이 배포했다. `local_33c711a0` 이 SYNK-talk 계약 관련 셋을 `d441d78` 로 커밋 → 그 커밋을 기다리던 `local_ddd51fde` 가 즉시 `events v25` 를 재배포해 `배포대조` 0 을 만들었다 → 3분 뒤 같은 세션의 둘째 커밋 `b0eee7f`(형제 저장소 회귀가 요구한 계약 `미구현` 분류)가 `events`·`correct` 를 **다시 드리프트**시켰다(둘 다 동봉 `계약.mjs`). 급소는 「누가 잘못 배포했나」가 아니다 — 배포는 언제나 HEAD 를 보내므로(F273) **첫 커밋만 담긴 배포본이 「✅ 방금 고쳤다」의 얼굴로 남는다.** 옆 세션의 초록 보고와 실제 배포본이 갈렸고 그 사이 왕복시험은 여전히 막혀 있었다(`local_33c711a0` 이 `events v26`·`correct v6` 으로 닫아 다름 0 복구). ⚠ **가드로 막을 자리가 아니다** — 커밋을 나누는 것 자체는 정상이고, 어느 커밋이 「그 트랙의 마지막」인지는 기계가 모른다. ▶ **처방(순서 규약)**: 동봉으로 나가는 파일(`lib/*.js`·`계약/*.json`)은 **한 커밋에 모은다**. 형제 저장소 회귀가 후속 커밋을 강요하는 경우(계약 동기화)엔 그 회귀를 **먼저** 돌려 무엇이 더 필요한지 확인하고 한 벌로 커밋한다 — `node tools/계약동기화.js` 뒤에 형제 `tests/계약.test.js` 를 돌리는 한 줄이 그것이다. | |
