| F404 | 2026-08-13 | 마찰 | 회귀의 판정이 「돌린 사람」에 기대 로컬만 빨갛고 CI 는 초록이었다 — 아무도 안 보는 적색이 모든 로컬 세션의 배포 전 게이트를 막았다. `tests/보드유령.test.js:179` 가 `board-move` 를 부르며 `CLAUDE_CODE_HOST_SESSION_ID` 를 고정하지 않고 주변 환경에서 물려받는다. 원칙 ⑦(F397)은 «남의» 죽은 미완 줄만 막는데 남인지 아닌지를 그 변수로 가르므로: CI(변수 없음)=원칙 ⑦ 안 돎=초록 / 로컬 세션(변수 있음)=픽스처 주인과 다름=적색. 같은 규칙을 시험하는 `tests/보드이관.test.js` 는 호출마다 지문을 명시로 넘긴다 — 갈린 것은 규칙이 아니라 이 한 자리다. F296 의 «거울상»이라 더 오래 안 보인다: 그건 「repo 밖 환경에 기댄 검사가 CI 에서 깨진다」이고 이건 「CI 에서만 «안» 깨진다」. 새는 방향이 반대라 원격 초록이 로컬 적색을 덮어 준다. | tests/보드유령.test.js 의 board-move 호출에 픽스처 주인과 같은 지문을 명시(d2aa4adc) — 이 검사가 재는 것은 원칙 7 이 아니라 유령이 후보에 드는가뿐이고, 원칙 7 자체는 tests/보드이관.test.js 가 진다. 12/12 · 변이 2/2 물림 |
