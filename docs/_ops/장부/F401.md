| F401 | 2026-08-13 | 마찰 | 가드가 파일 «원문»에 대고 금지 토큰을 재서 「그 금지를 설명하는 주석」을 위반으로 잡는다 — 같은 계열 2번째다. 실측: tests/마이크권한.test.js 의 「화면은 녹음 모드를 직접 켜지 않는다」가 src/말하기화면.js 전문을 includes('allowsRecording') 해서, 언마운트 마이크끄기 누락(0766bb39 적대리뷰 P2)을 고치며 그 이유를 주석에 적은 커밋 talk b41fce2 가 master 를 적색으로 만들었다 — 고친 사람은 코드를 한 줄도 안 어겼다. 이 계열은 2026-08-12 에 이미 한 번 걷혀 공용 통로(tests/lib/소스검사.js 의 코드만·코드만픽스처)가 섰는데 이 가드만 안 옮겨져 남아 있었다. 새는 방향이 「통과」가 아니라 「차단」이라 조용하지 않고 시끄럽게 샜지만, 처방이 「주석에 그 낱말을 쓰지 마라」가 되어 다음 사람이 이유를 못 적게 만드는 자리였다(F103 — 따를 수 없는 처방은 우회를 정상 통로로 만든다). 이 건은 그 가드를 코드만() 경유로 옮기고 탐지력 픽스처를 붙여 닫았다(talk 09ef832 · 1975/1975). ⚠ 남은 축 = 같은 모양의 다른 가드가 몇 개인지 **아직 못 쟀다** — 급조한 계수기는 긍정 단언과 비-소스 피연산자까지 세어 과대계상했고, 숫자를 지어내지 않으려 안 싣는다. 계수기를 제대로 세우는 것(부정 단언 × 피연산자가 실제 파일 원문인 것만)이 별도 트랙이고, 겸사겸사 실측된 것 하나: 주석 제거기 사본이 최소 5벌이다(tests/lib/소스검사.js 공용 + 가입문항·돌려주기·스폰통로 각자 + 견줌의 주석제거) — 판정이 다섯 곳이면 갈라진 쪽은 조용히 눈이 먼다. | |
