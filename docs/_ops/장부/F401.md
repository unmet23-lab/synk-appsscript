| F401 | 2026-08-13 | 마찰 | 가드가 파일 «원문»에 대고 금지 토큰을 재서 「그 금지를 설명하는 주석」을 위반으로 잡는다 — 같은 계열 2번째다. 실측: tests/마이크권한.test.js 의 「화면은 녹음 모드를 직접 켜지 않는다」가 src/말하기화면.js 전문을 includes('allowsRecording') 해서, 언마운트 마이크끄기 누락(0766bb39 적대리뷰 P2)을 고치며 그 이유를 주석에 적은 커밋 talk b41fce2 가 master 를 적색으로 만들었다 — 고친 사람은 코드를 한 줄도 안 어겼다. 이 계열은 2026-08-12 에 이미 한 번 걷혀 공용 통로(tests/lib/소스검사.js 의 코드만·코드만픽스처)가 섰는데 이 가드만 안 옮겨져 남아 있었다. 새는 방향이 「통과」가 아니라 「차단」이라 조용하지 않고 시끄럽게 샜지만, 처방이 「주석에 그 낱말을 쓰지 마라」가 되어 다음 사람이 이유를 못 적게 만드는 자리였다(F103 — 따를 수 없는 처방은 우회를 정상 통로로 만든다). 이 건은 그 가드를 코드만() 경유로 옮기고 탐지력 픽스처를 붙여 닫았다(talk 09ef832 · 1975/1975). ⚠ 남은 축 = 같은 모양의 다른 가드가 몇 개인지 **아직 못 쟀다** — 급조한 계수기는 긍정 단언과 비-소스 피연산자까지 세어 과대계상했고, 숫자를 지어내지 않으려 안 싣는다. 계수기를 제대로 세우는 것(부정 단언 × 피연산자가 실제 파일 원문인 것만)이 별도 트랙이고, 겸사겸사 실측된 것 하나: 주석 제거기 사본이 최소 5벌이다(tests/lib/소스검사.js 공용 + 가입문항·돌려주기·스폰통로 각자 + 견줌의 주석제거) — 판정이 다섯 곳이면 갈라진 쪽은 조용히 눈이 먼다. | 공용 통로 하나 + 옛 통로 금지 회귀 (talk `fbf4f26` · 보드 `local_8ad318ea`). 신고문이 못박은 닫는 조건 둘을 그대로 집행했다. ㉡**사본 7벌 → 0벌**(「최소 5벌」 추정을 실측이 정정): `tests/lib/소스검사.js` 를 일곱의 «상위집합»으로 갈고(줄 단위 filter → 훑는 판 · 인라인 블록·줄 주석까지 · **문자열·정규식 리터럴은 비껴간다** · 줄 수는 옛 판과 맞춘다 — 소비자가 `[\s\S]{0,N}` 으로 거리를 잰다) 일곱 자리를 전부 그 통로로. 옵션은 안 만들었다 — 갈라짐이 파일 안으로 이사할 뿐이다. ㉠**전수도 쟀다**: 부정 단언 × 파일 원문 = 위험 67 · 정제경유 80 · ❔모름 102(분모 = 훑은 167파일 · 못 읽은 0). 재는 물건 = `tools/가드계수.js`(acorn — 정규식으로 세면 계수기가 «세려는 병»에 스스로 걸린다). 🔑 그 계수기가 급조판과 같은 병에 **세 번** 걸렸고 그때마다 조인 것이 이번 소득이다: ①본문을 글자로 훑어 base64url 인코더 3벌을 사본으로 셈 → `.replace()` 첫 인자가 정규식 리터럴일 때만 그 source 를 본다 ②부분트리에 원문 변수가 보이면 위험으로 셈(문자열 상수를 재는 단언까지) → **수신자**만 본다 ③사본을 공용으로 돌린 직후 위험이 67→**69로 올라감** — 코드가 좋아졌는데 숫자가 나빠지면 계수기가 틀린 것이다(`() => 코드만(readFileSync(...))` 를 원문 생산자로 오분류). 막는 것 = `tests/소스검사통로.test.js`: 탐지력은 `tests/fixtures/가드계수/` 가 지고(사본·위험 + «세면 안 되는» base64url·문자열상수·긍정단언을 한 파일에 섞어 둠), 실저장소엔 「지역 사본 0벌」만 묻는다 — 분모(훑은 파일>100)를 먼저 못박고(F207), acorn 없으면 fail 아닌 **skip**(F296). 스위트 2014/2015(skip 1) · precommit 0 · **변이 6/6 물림**. ⚠ 남긴 것 = 위험 67 은 «세었을 뿐» 결함 판정이 아니다(그 자리가 주석을 안 볼 이유가 있을 수 있다) — 전건 처분은 이제 목록이 기계로 나오므로 대기열 P3 새 줄로 넘긴다. |
