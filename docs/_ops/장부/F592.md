| F592 | 2026-08-17 | 마찰 | 세션 시작 화면의 형제 배포빚 갚기 안내가 「운영은 --운영」을 권했는데, 실측하니 node tools/배포빚.js --운영 은 인자 없을 때와 출력이 한 글자도 다르지 않다(그 도구는 원래 두 판을 다 내고 모르는 플래그를 조용히 삼킨다). 게다가 F462 가 금지한 「읽기에 쓰기 승인 키 붙이기」를 정확히 권하고 있었다 — 도구가 스스로 인쇄하는 정본 명령과 정면으로 어긋나는 안내였다(F103). | as `b838efd8` — `tools/작업본소유자.js` 형제빚 절의 갚기 안내에서 `(운영은 --운영)` 을 지우고 「`node tools/배포빚.js` 가 **두 판(리허설·운영)을 한 번에** 내고 함수별 명령을 그대로 준다」로 바꿨다. 지운 이유를 그 자리 주석에 남겼다(둘 다 틀렸다: ①그 플래그는 아무 일도 안 한다 — 실측 출력이 인자 없을 때와 한 글자도 다르지 않다 ②F462 가 금지한 「읽기에 쓰기 승인 키 붙이기」를 정확히 권하고 있었다). 같은 커밋에서 F579 의 `미배포` 칸도 읽게 했다 — 「모름 2**(그중 라이브에 없음 2)**」 + 「배포된 적이 없다(HTTP 404) — 처방은 대조가 아니라 **배포**다」. 🔑 **회귀가 되살아남을 막는다**: `tests/작업본소유자.test.js` 에 「갚기 안내가 «--운영» 을 권하지 않는다」를 넣었다 — 그 문구는 「운영도 봐야 하니까」라는 **옳은 직관을 타고 돌아오는** 종류라, 지우기만 하면 다시 생긴다. 회귀 3 신설 · 126/126(기존 123 무변경). 🔑 **F083 재현**: 손 호출로 그 절을 다시 뽑아 `--운영` 이 사라지고 「두 판을 한 번에」가 나오는 것을 확인했다. 🔑 **F092 grep**: 저장소 전체에서 `배포빚.js --운영` 을 **시키는** 자리는 0 — 남은 두 자리는 이 수리의 「왜 지웠나」 주석과 이 장부 신고문이라 둘 다 설명이지 처방이 아니다. ⚠**안 잰 것**: 변이 검사 0 — `tools/변이.js` 는 «계획 파일»(`시험` 배열 exports)을 받는 구조라 테스트 경로를 바로 못 먹었고, 계획을 새로 쓰는 대신 안 돌린 것을 밝힌다(탐지력은 회귀의 명시적 문자열 assert 가 진다). 🔴 **뿌리는 안 닫혔다 — `#Q111` 로 등재**: `배포빚.js` 가 `lib/플래그.js` 의 `인자게이트` 를 안 지나 모르는 플래그를 조용히 삼킨다(exit 0). 그 게이트가 있었으면 이 거짓 안내는 애초에 못 살아남았다 — 누가 쳐 봐도 「돌긴 도네」로 보였을 것이다. |
