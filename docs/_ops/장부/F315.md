| F315 | 2026-08-10 | 마찰 | 세션 생존 판정이 「긴 외부 프로세스를 도는 세션」을 죽은 것으로 오판한다 — 심장박동은 파일 접촉으로 찍히는데, codex-review(12분)·CI 같은 도구를 부른 세션은 그 사이 아무 파일도 안 만져 박동만 식는다. 실측 08-10: f542de4f 는 멀쩡히 도는 중이었는데 「종료 도장 없음 + 박동 28분(재측정 2회에도 갱신 0)」을 강제 종료로 읽었다. 그 오판 위에서 남의 트랙을 이어받아 같은 codex-review 를 두 번째로 태우기 시작했고(12분×2), 당사자가 조율 메시지를 보내지 않았으면 bump-version 헛채번까지 갔다. 게다가 그쪽은 그 사이 P1 2건을 잡아 v9.205 로 넘어가 있었다 — 내가 이어받았으면 지적받은 판(4f26e507)을 라이브로 밀 뻔했다. F252 는 「도장 없으면 ⚪ 아닌 ❔」라고 적었지만 판정 도구(작업본소유자.죽은착수)는 박동 문턱만 보고 ❔ 를 못 낸다. 박동이 식었는데 도장이 없으면 「죽음」이 아니라 「모름」이고, 모름은 이어받기 금지다 — 프로즈가 아니라 그 함수가 세 값을 내야 한다. | tools/작업본소유자.js `죽은착수()` 가 두 값(살아있음/죽음) 대신 **세 값**을 낸다 — SessionEnd 도장(`끝남`)을 봐서 「죽음을 증명한 주인」만 ⏹ 로 내고, 박동만 식고 도장이 없으면 ❔ 로 내린다. 보고도 두 절로 갈랐다(처방이 정반대라 한 목록에 섞으면 그 자체가 사고다): ⏹=「이미 섰는지 열어 보고 board-move」 · ❔=「**이어받지 않는다** — 정상 종료하면 도장이 찍혀 ⏹ 로 내려온다」. 판정 규칙은 파일 판정의 `안찍힌` 과 같다(하나라도 안 찍혔으면 모름) — 둘이 갈리면 한 사건이 두 답을 낸다. 세션 기록이 아예 없어도 ❔다(「못 봤다」를 「죽었다」로 번역하지 않는다). 실측: 실저장소에서 5건 중 **2건이 ⏹→❔ 로 내려갔다**(그 2건은 전까지 「죽었다」로 단정돼 이어받기가 열려 있었다). 회귀 tests/작업본소유자.test.js 1건(변이 4) — 급소 assert 는 「도장 없는 주인이 ⏹ 절에 들어가면 빨개진다」이고, 픽스처 3개(도장 없음·도장 있음·기록 자체 없음)로 탐지력을 못박았다. 변이 실측: `증명됨: true` 로 되돌리면 F315 만 fail 1, F270 은 초록 유지(두 축이 안 겹친다는 증거). 82/82. |
