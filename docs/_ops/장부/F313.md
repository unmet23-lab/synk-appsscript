| F313 | 2026-08-10 | 실수 | 동봉 파일은 **주석만 고쳐도** 공유 배포 게이트가 빨개져 남의 왕복시험이 전부 막힌다 — 내가 냈다(2026-08-10 실측). SYNK-talk `lib/검수확정.js` 의 상수 머리말 주석을 실측으로 갱신하고 커밋했다(`e9edfdd`). 코드 동작 변경 0·회귀 1023/1023 초록이라 「문서 편집」으로 여기고 넘어갔는데, 그 파일은 `supabase/functions/review/동봉.json` 이 `검수확정.mjs` 로 실어 나가는 동봉이다. `tools/배포대조.js` 는 배포본 텍스트가 소스 내용을 **문자로 포함하는지**로 재므로(`빠진것 = 잰것.filter((이름) => !배포본.includes(정규(나갈것[이름]))))`) 주석 한 줄이 달라도 「다르다」가 된다. 그리고 `왕복전게이트` 는 다름이 하나라도 있으면 **exit 1 로 차단**한다 — 즉 그 순간부터 SYNK-talk 의 모든 왕복시험(배달·인증·교정·검수·업로드)이 **살아있는 세션 8개 전부에게** 막혔다. 증상이 내 쪽엔 안 나온다는 것이 급소다: 내 커밋은 초록이고, 빨개지는 것은 **남이 왕복을 돌릴 때**다. 판정 근거는 추정이 아니라 실측이다 — 배포본을 받아 내 커밋 전/후 판을 대조했더니 「전 판 포함 true · 후 판 포함 false」(옛 문구 「미측정 상수다」가 배포본에 살아 있었다). 즉 내 커밋 직전까지 `배포대조` 는 다름 0 이었고 내가 빨갛게 만들었다. 처방은 도구가 이미 알고 있었다 — `게이트판정.처방` 이 정확히 그 명령을 낸다. 리허설에 `review` 재배포(v2→v3 ACTIVE · 판 대조 ✅ 저장소 c10) 후 `배포대조` 다름 0 · 미측정 0 · 전체 10 으로 복구했다. 남는 교훈: 동봉 목록(`동봉.json`)에 오른 파일은 **「고치면 재배포까지가 한 벌」**이고, 그 한 벌은 주석·산문 편집에도 걸린다(F302 가 「한 커밋에 모은다」로 잡은 축의 다음 칸 — 커밋을 모아도 배포를 안 하면 게이트는 그대로 빨갛다). | |
