| F618 | 2026-08-18 | 마찰 | 래칫 목록의 키가 «파일:행번호»라, 그 위에 한 줄만 더해도 「위반이 늘었다」로 운다 (`tests/형제경로통로.test.js:90` `남은자리`)  ■ 실측 2026-08-18 (`cloud_d427746a` · F614 수리 중)   · F614 수리로 `tools/lib/보드근거.js` 위쪽에 29줄을 더했다. 코드 내용은 그대로인데 이미 «허용된» 빚 한 줄이 179 → 208 로 밀렸다.   · 래칫은 `새것 = 실제 - 기대` 를 「손조립하는 자리가 늘었다」로 낸다. 실제로 늘어난 자리는 **0** 인데 화면엔 위반 1건이 뜬다.     그리고 그 처방은 「`형제경로(root)` 를 써라」다 — 이미 그 판정을 지나 «남의 줄이 잠갔다»로 유예된 자리라, 처방을 따르면 유예를 깬다.  ■ 왜 신호인가 — **맞는 얼굴로 틀린 값**이다 (CLAUDE.md 가드 맹점 ④)   장치가 안 도는 것이 아니라 **정확히 도는 모양으로 틀린 답**을 낸다. 새는 방향은 「막는다」(거짓양성)라 사고는 안 나지만,   비용은 그 자리를 만지는 **모든** 트랙에 붙는다: 파일 위쪽을 건드릴 때마다 CI 가 빨개지고, 다음 사람은 매번 「내가 뭘 늘렸나」를 진단한다.   나는 이번에 그 진단을 한 바퀴 돌았다(실제 위반인지 확인 → 남의 잠금 줄 셋의 생사 확인 → 행번호 이동임을 확인).   ⚠ 그리고 이 사각은 **반복이 보장**돼 있다 — 행번호는 편집의 부산물이라 「안 늘렸다」를 증명해도 다음 편집에서 또 운다.  ■ 안 잰 것 (F348)   · 같은 «파일:행번호» 키 방식을 쓰는 다른 래칫이 이 저장소에 몇 곳인지 **안 셌다**. 이 파일 하나만 직접 증거다.   · 이 거짓양성이 과거에 실제로 CI 를 몇 번 빨갛게 했는지는 안 쟀다(이력 대조 안 함).  ■ 지금 한 것 / 안 한 것   ▶ 한 것 = 목록의 그 줄을 179 → 208 로 갱신하고, 왜 밀렸는지를 사유 칸에 적었다(다음 사람이 「빚이 옮겨다닌다」를 알게).   ▶ 안 한 것 = 키 방식 자체는 안 바꿨다. 되돌림 비용이 있는 자리라 재료만 남긴다.  ■ 처방 후보 — 안 지었다(이 신고는 재료다)   ㉠ 키를 **행 내용의 지문**으로 바꾼다(정규화한 줄 텍스트의 해시). 행이 밀려도 안 울고, 내용이 바뀌면 운다 — 래칫이 재려던 축 그대로다.     ⚠대가: 같은 파일에 같은 형태가 둘이면 구분이 안 된다(그때만 행번호를 덧붙인다).   ㉡ 키를 **파일 단위 건수**로 낮춘다(`보드근거.js: 1건`). 가장 싸다. ⚠대가: 같은 파일 안에서 한 건이 갚아지고 다른 한 건이 생기면 조용히 통과한다 — 새는 방향이 「통과」로 뒤집힌다.   ㉢ 아무것도 안 한다. ⚠대가: 그 파일들을 만지는 트랙마다 이 진단 비용을 다시 낸다(이번이 그 첫 실측이다). | |
