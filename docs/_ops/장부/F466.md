| F466 | 2026-08-15 | 실수 | 렌더가 «도는 중»인 폴더를 맨손 ffmpeg 로 구워, 앞뒤가 다른 영상을 만들고 그걸 근거로 «없는 결함»을 진단해 다음 세션에 인계까지 했다.  무슨 일이었나(2026-08-15 · 마스코트 3D LIVE 트랙): · 11:26 anim2 600프레임 재렌더 시작(좌 +75° 비대칭 큰 턴). 11:49 완료. · 11:30 — 아직 f≈270 까지밖에 안 구워진 시점에 그 폴더로 mp4 를 조립했다.   렌더 폴더는 덮어쓰기라 «렌더 머리» 뒤 번호는 전부 앞 렌더(v2)가 남긴 낡은 파일이었다. · 그 mp4 의 f450·f480 을 열어 「좌 턴이 안 착지한다」고 판정하고, 원인 후보 3개   (gest 부모 키 합성 · Bezier 보간 · keys() 의 setattr 왕복)까지 세워 보드·메모리·인계문에 남겼다. · 실측(다음 세션): 그 v3.mp4 의 f300·f450·f480 이 v2.mp4 와 바이트 동일(MAE 0.00)이고   f250 부터 갈린다. 완성된 프레임의 f450 은 처음부터 좌 +75°(눈 뜸)였다. 결함은 없었다.  왜 이게 「조심하자」로 안 끝나나: · CLAUDE.md 「현재 상태는 현재를 읽어 판정한다」를 어긴 게 아니다 — 파일을 직접 열었다.   급소는 «열었느냐»가 아니라 «언제 쓰인 것이냐»였다(F174 의 시점 축이 렌더 산출물에서 재현됐다). · 그리고 낡은 프레임이 섞인 폴더는 정상 폴더와 모양이 완전히 같다 — 사람 눈으로는 못 가른다.   가드 맹점 ④(맞는 얼굴로 틀린 값)의 전형이다. · 값이 비싸다: 없는 버그의 원인 후보 3개가 인계문·메모리·보드 세 곳에 「다음 세션의 첫 일감」으로   박혀서, 안 잡았으면 다음 세션이 블렌더 키 합성을 파는 데 통째로 들어갔다. | tools/마스코트인형리깅.py 에 `조립` 모드 — 굽기 «전에» 폴더가 한 벌인지 기계로 잰다.  잣대 둘: ①1~N 이 다 있는가 ②mtime 이 번호 순으로 단조증가하는가(역행 = 두 벌이 섞였다). 사고 재현 픽스처에서 정본은 exit 2, 검사를 죽인 변이본은 exit 0 — 탐지력이 그 검사에 있다는 것까지 확인.  원인을 쓸 수 없게 만든 부분: · 머리말의 맨손 ffmpeg 한 줄(그게 통로였다)을 지우고 조립 모드로 대체 —   tests/프레임한벌.test.js 가 그 줄이 되돌아오는 커밋을 잡는다(파이썬 없이도 도는 층). · 면제 --재굽기=a-b 는 «그 구간이 이웃보다 새 것일 때만» 통과한다.   안 그러면 낡은 꼬리의 첫 번호를 「내가 다시 구웠다」고 적는 것만으로 이번 사고가 그대로   세탁된다(나머지 낡은 판은 자기들끼리 단조증가라 통과) — 회귀 ⑤가 그 세탁을 픽스처로 못박았다.  대가(틀릴 때의 모습)도 도구 머리말에 적었다: · 거짓음성 — mtime 을 안 보존하고 폴더를 복사하면 시각이 평평해져 ②가 조용히 통과한다(①만 남는다). · 거짓양성 — 중간 구간을 일부러 다시 구우면 정당한 역행이 생긴다. 거절문이 조건과 함께 면제를 안내한다. · 「렌더가 지금 도는 중인가」는 따로 안 잰다 — 빈 폴더 위면 ①이, 낡은 한 벌 위면 ②가 잡아 중복이다. |
