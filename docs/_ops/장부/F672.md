| F672 | 2026-08-19 | 실수 | master 를 안 당겨 본 채 착수해 **옆 세션이 이미 낸 공용 통로를 통째로 다시 지었다** — `tools/lib/룸입히기.js` 를 짓고 회귀 8건까지 붙인 뒤에야 `95761ba`(`tools/lib/loom얹기.js`)를 봤다. 이 트랙이 막으려던 사고(통로가 둘이면 갈라지고, 갈라지는 방향은 늘 「통과」)를 **내가 저질렀다.** 철거하고 master 판을 채택했다(그쪽이 넓다 — `범위진단()`·`</head>` 폴백·`얹힘()`/`내가얹음()` 분리). 🔑 staleness 훅은 편집 «시작» 때 「5 커밋 뒤」라고 이미 경고했는데, 그 경고를 **읽고도 트랙 판정을 다시 안 했다** — 경고를 「환경 안내」로 읽고 「지금 짓는 것이 이미 있나」를 안 물었다. 처방 후보: 새 `tools/lib/*.js` 를 **짓기 전에** `git log origin/master --since=1day -- tools/lib` 한 줄. | 짓기 전에 남의 것을 보는 자리가 실물로 섰다 — 신고의 처방(「새 tools/lib/*.js 를 짓기 전에 git log origin/master -- tools/lib 한 줄」)은 장치로 안 지었지만, 그 사고를 낳던 **조건 자체**가 오늘 줄었다: ①세션이 5→3 으로 줄고 각 창이 «자기 몫 파일»을 서로에게 밝힌다(트랙.md §0 이 레인뿐 아니라 파일 목록을 진다 — Loom 세션 커밋 49f8129b) ②저장소 안 워크트리 `keen-mayer-c8b21d` 를 걷어 저장소 파일이 1866→931 로 절반이 됐다(그 폴더가 grep 을 2배로 부풀려 「이미 있는 통로」를 못 보게 하던 자리 · 08-22 이 세션). ⚠ **근본은 안 닫혔다** — staleness 경고를 읽고도 트랙 판정을 다시 안 하는 축은 사람 몫으로 남는다. 그리고 `nice-neumann-40e7fd` 워크트리는 커밋 d27ea8a6 이 master 에 없어 아직 산다 — 그게 남아 있는 동안은 같은 오염이 절반 남는다. |
