| F240 | 2026-08-08 | 마찰 | 배포 게이트가 저장소 전체를 재서, 남의 진행 중 작업이 무관한 배포를 상시로 막는다 — 새는 방향은 「라이브가 조용히 뒤처짐」이다. 실측 2026-08-08: 상담AI 의 채널 칸(`56e7d20` · 소급 불가 · 페북 학부모/인스타 학생 라벨)이 **24시간째 라이브에 없다**. 그 커밋 본문이 원인을 그대로 적고 있다 — 「라이브 미반영, clasp push 는 안 했다 … 게이트를 우회하지 않았다. 다음 배포에 버전 채번과 함께 나간다」. 오늘 내가 같은 자리에서 두 번 다시 막혔다: test-ci 1회차는 남이 편집 중이던 board-guard.js 의 ReferenceError, 2회차는 계약/수집_교정_계약.json 이 두 저장소 사이에서 갈라진 상태(15ab8497 이 c10 을 쓰는 중). 둘 다 내 배포집합 15개와 무관하고 둘 다 남의 살아있는 작업본이라 F073 으로 손댈 수 없다 — 처방은 「고쳐라」인데 고칠 권한이 없다(F103 축 · F235 를 배포층에서). 세션 13개가 동시에 도는 저장소에서 「전 저장소 초록」은 상시 조건이 아니라 가끔 열리는 창이다. 곁들여 둘째 축: 이 낡음을 재는 장치가 rot-check 의 네트워크 clasp pull 하나뿐이라(주간 · 7일 스로틀) 최대 7일간 안 보인다 — @HEAD 서빙 프로젝트에서는 deploy-freshness 가 구조적으로 침묵한다(배포판점검.점검() 이 라이브:false 면 측정:false → level ok → 훅 종료). 처방 후보 ①마지막 clasp push 시점의 배포집합 지문을 남겨 네트워크 0 으로 「저장소가 push 이후 바뀌었다」를 아무 세션이나 알게 한다 ②게이트 적색이 내 배포집합 밖 파일에서 났고 그 파일이 남의 살아있는 작업본이면 차단 대신 그 사실을 밝히고 통과시킨다(우회가 아니라 판정) ③rot-check 의 수리 필요 항목을 7일 스로틀 밖으로(이미 결정 큐 14번에 제안돼 있다). | 축 B 종결 — clasp-guard 검사 2 가 재는 대상을 작업본에서 HEAD 로 바로잡았다. 나갈 바이트는 HEAD 다: 같은 훅의 검사 3 이 배포집합 미커밋을, 검사 4 가 미push 를 막으므로 통과 시점의 작업본 배포집합 = HEAD 의 것 = origin 의 것이다. 그래서 원격 CI 가 이 HEAD 를 초록으로 통과시켰으면 로컬 스위트를 안 돌리고 그 사실을 알림으로 밝힌다(syntax-check.yml 은 node --test tests 전량을 돌므로 구문만 보는 층이 아니다). 우회가 아니라 재는 대상을 바로잡은 것이다. 못 재면(gh 없음=폰 클라우드 · run 0건 · 원격 적색 · 타임아웃 · 결과 없음) 전부 초록아님으로 떨어져 오늘까지처럼 작업본 전체를 도니 F103 을 안 만든다 — 새는 방향이 오늘과 같음이다. 기각한 대안 둘을 실측으로 남긴다: 1) 배포집합 파일 이름으로 테스트 소스를 훑어 관련 테스트만 고르는 안 — 107개 중 45개가 걸리고 그 안에 board-guard.test.js 가 있다(Code.js 가 보드 표 픽스처 문자열로 들어 있다). F240 실측 1번을 그대로 놓치는 판별자다. 2) TAP location 으로 실패 파일을 귀속시키는 안 — 파일은 정확히 나오지만 그 파일이 배포집합을 재는지 묻는 순간 다시 1번의 판별자가 필요해 같은 자리로 돌아온다. 곁들여 실측 하나: 작업본 전량 실행은 5분 20초로 훅 타임아웃 120초를 2.7배 넘겼다 — 이 검사는 실세션에서 완주한 적이 없을 수 있다. 원격초록 경로는 몇 초다. 라이브 재현: 이 수리 도중 게이트가 실제로 차단했고 적색 5건이 전부 남이 그때 편집 중이던 board-move.js 와 작업본소유자.js 의 것이라 배포집합 15개와 무관했다. 회귀 7 신설(tests/clasp프로젝트.test.js). 사람 층도 같이 고쳤다 — test-ci 적색 문구와 deploy 스킬 2단계가 같은 판정을 가리킨다(둘이 갈라지면 사람이 게이트에 닿기 전에 접는다). 축 A(가시성)는 F244 가 이미 덮었다. |
