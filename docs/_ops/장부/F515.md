| F515 | 2026-08-16 | 마찰 | 훅이 «자기에게 없는» 탈출구를 처방했다 — credential-guard 규칙①이 `CRED_GUARD_BYPASS=1` 을 시키는데 그 훅에 그 문자열을 보는 자리가 어디에도 없다. 같은 파일 머리말(:126)이 「MCP 도구에는 명령 앞에 `BYPASS=1` 을 붙일 자리가 없다」고 이미 적어 뒀으니, 훅이 자기가 부정한 것을 시킨 셈이다. 규칙②·③은 `--allow-next` 로 옳게 안내한다 = 한 훅 «안»에서 처방이 갈렸다(CLAUDE.md 신뢰성 ④). 기존 `tests/훅처방교차.test.js` 는 F212 축(처방을 다른 가드가 «막는다»)만 보고 수확도 `git ` 명령·repo-staleness 한 훅뿐이라 이 형태는 원리상 사각이었다. 실측: 훅 29 전수 = node 처방 27/27 ✅ · 도구 플래그 14/14 ✅ · BYPASS 6 중 깨진 것 1(나머지 5는 셸 가드가 env 아닌 «명령 문자열»에서 찾는 거짓양성 — 갈라서 봤다). | credential-guard 규칙① 처방을 --allow-next 로 통일 + tests/훅처방교차.test.js 에 축② 신설(훅이 시키는 탈출구가 그 훅에 실재하는가 · 양방향 픽스처 + 수확 미측정 방어) · 변이 7/7 구멍 0 미적용 0 — 412d3a2d |
