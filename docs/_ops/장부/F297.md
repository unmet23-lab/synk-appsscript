| F297 | 2026-08-09 | 마찰 | `tools/friction.js add` 가 **모르는 플래그를 신고문으로 접어** 쓰레기 행을 낳았다 — 그리고 그 우회를 만든 것은 도구 자신이었다. 두 구멍이 한 사건이다. ①**따를 수 없는 처방**: 지침은 「여러 줄이거나 코드·정규식을 인용하는 문자열은 셸에 맡기지 말고 파일로 준다」인데 이 도구엔 그 통로가 없었다 — 백틱이 든 신고문은 POSIX 셸에서 명령 치환으로 실행되고 따옴표는 말을 자른다(F103 축: 따를 수 없는 처방은 우회를 정상 통로로 만든다). ②**모르면 통과**: 그래서 없는 `--파일` 을 넘겨 봤더니 파서(520줄 `본문.push(남은[i])`)가 그 토큰과 절대경로를 그대로 신고문으로 접었고, 행이 태어나 커밋까지 갔다(F296 본문이 「--파일 C:/Users/.../friction-f278.txt」였다 · 복구 `78d4b910`). 증상이 없다 — 도구는 성공을 찍고 번호는 채번되며, 쓰레기가 들어간 줄은 사람만 안다. 장부는 `/evolve` 의 연료라 쓰레기 행은 다음 개정을 통째로 오염시킨다. 수리(`3c2ae04e`): `--파일 <경로>` 를 실제로 내고(줄바꿈·`|` 는 기존 `칸안전()` 이 이미 받는다 — 새 이스케이프를 짓지 않았다. 표.js 의 `\|` 는 이미 죽은 길이다), 모르는 `--플래그` 는 막고 말한다. 인자와 `--파일` 을 둘 다 주는 것도 막고(어느 쪽이 정본인지 갈린다), 못 읽으면 빈 행을 만들지 않는다. 회귀 4(`tests/마찰신호.test.js`) · 변이 3/3 · 그 파일 46/46. ⚠ 기록 정정: 커밋 `3c2ae04e` 는 제목에 F296 을 달았는데 그 번호는 **F278 거짓적색** 신고다 — 이 수리의 정본 번호는 이 행이다. | tools/friction.js 파서 — 모르는 플래그를 거절하고 파일 통로를 실제로 냈다 (회귀 4 · 변이 3/3) |
