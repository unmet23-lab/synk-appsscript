| F551 | 2026-08-17 | 마찰 | 정본 문서의 상한을 «쓰는 쪽은 stderr 경고 한 줄, 읽는 쪽은 하드 적색»으로 재서, 전체 CI 가 남의 손에 멈췄다 — docs/제품방향.md 가 origin/master 에서 이미 7,133자(상한 6,000)라 tests/검수제안.test.js 가 빨갛고, 그 하나가 스위트 4,073건의 유일한 fail 이다(3회 재현 · 커밋 80947a17 「엔진 로드맵 신설 — 유호 확정 08-17」 · 세션 7989fd41). 두 층이 갈린 것이 병이다: tools/codex-review.js 의 방향텍스트() 는 상한을 넘으면 stderr 로 「압축하라」고 «경고만» 하고 조용히 뒤를 잘라 쓰는데, 같은 상한을 tests/검수제안.test.js 는 assert 로 «막는다». 그래서 문서를 늘린 세션은 자기 커밋이 남의 CI 를 세운 줄 모르고 지나가고(그 경고는 codex-review 를 실제로 돌릴 때만 나온다 — 문서 편집만 하면 아예 안 나온다), 다음에 test-ci 를 돌린 사람이 남의 적색을 물려받는다. 지금 내가 그 자리다. 처방 후보 둘: ㉠ 문서를 쓰는 순간 재는 층을 둔다(pre-commit 또는 rot-check 축에 「상한 넘은 정본」을 얹어, 늘린 그 커밋에서 바로 보이게) ㉡ 잘라 쓰는 쪽과 막는 쪽 중 하나를 고른다 — 잘라 쓸 거면 시험도 「잘리는가」를 재고, 막을 거면 방향텍스트() 도 던진다. 지금은 같은 상한이 한쪽에선 권고, 한쪽에선 규칙이라 갈라져 있다. 내 트랙(#Q101 도구층 접기)과는 무관하고 나는 그 파일 무접촉이라, 압축은 그 문서 주인의 판정이 필요해 여기서 신고만 한다. | ⏸ 안 고쳤다 — 지금 안 닫기로 판정했다. 과녁 둘 다 내 트랙 밖이라서다. ㉠ 당장의 적색을 끄는 길은 docs/제품방향.md 를 6,000자 아래로 압축하는 것인데, 그 문서는 유호님 확정 로드맵의 정본이고 무엇을 덜어낼지는 내용 판정이다(세션 7989fd41 이 08-17 에 「엔진 로드맵 신설 — 유호 확정」으로 방금 늘렸다 · 남의 활성 영역이라 F073). 남의 정본을 내가 줄여 대신 커밋하지 않는다. ㉡ 뿌리 처방은 「같은 상한이 쓰는 쪽에선 권고(stderr 경고+조용한 절단), 읽는 쪽에선 규칙(assert)」인 갈라짐을 하나로 모으는 것인데, 그건 codex-review 의 방향텍스트() 계약을 바꾸는 별건 트랙이다(검수 프롬프트 층 · GPT 검수 3축 트랙이 그 파일을 쥐고 있다). 내 트랙(#Q101 도구층 표기 접기)은 그 파일 전부 무접촉이고, 곁다리로 손대면 이 커밋이 남의 검수 계약을 흔든다. 지금 남긴 것: 이 신고에 재현 근거(origin/master 7,133자 · 3회 재현 · 커밋 80947a17)와 처방 후보 둘을 적어 뒀으니, 그 문서나 검수 계약을 다음에 잡는 세션이 그대로 집을 수 있다. 판정한 자리 = 세션 local_7fbc725a · #Q101 종결 직후(보드 줄에 「CI 4073 fail1 = 내 것 아님 · F551」로 함께 적었다). |
