| F530 | 2026-08-17 | 마찰 | PR 을 닫기 전 게이트가 «가지»에서 재라고 적혀 있는데, 착지하는 것은 «머지 결과»다 — 그 둘이 갈리는 자리를 실측했다  실측(2026-08-17 · `local_7bddd6cc` · PR #25 인계): 인계문·보드가 남긴 처방이 「얼린 트리에서 `node tools/test-ci.js` 한 번 돌려 초록이면 `작업가지.js --닫기`」였다. 그대로 따랐으면 다음 둘이 **안 보인 채** master 로 갔다.  ① 래칫이 «둘 다 틀린 값»으로 착지할 뻔했다. 같은 칸(`시트도달상한_().도달0`)을 두 릴리스가    반대로 움직였다 — #Q100(가지)은 분모를 9→22 로 넓혀 5→7, #Q99(master)는 빚 하나를 갚아 5→4.    가지 단독으로 재면 7 이 초록이고, master 만 보면 4 가 초록이다. 참값은 **6**(넓힌 분모 위에서    하나가 갚아졌다)이고, 이건 두 판을 합친 트리에서만 잴 수 있다. 충돌 6건 중 이 한 자리만이    «값» 충돌이었고 나머지 다섯은 문서였다 — 즉 **눈으로 훑으면 지나칠 모양**이었다.  ② `tests/버전충돌.test.js` 적색이 **가지에서는 원리상 안 뜬다**. 병합 판의 Code.js 는 origin 의    v9.245 와 내용이 다른데(체인에 `[v9.244]` 가 더 붙는다) 번호가 같아진다 — 그 「같은 번호로    다른 내용」은 합치기 전에는 존재하지 않는 상태다. 전량 3897 중 유일한 적색이 이것이었다.  🔑 축이 「얼렸나」가 아니라 **「무엇을 얼렸나」**다. 얼린 트리 처방(F518 계열)은 «도는 동안 남이    편집해서 나는 거짓 적색»을 겨눈 것이고 그건 여전히 옳다. 여기서 샌 것은 다른 축이다 —    얼린 대상이 **가지**라서, 재는 대상이 착지할 것과 애초에 달랐다. 초록은 진짜였는데    그 초록이 답한 질문이 「이 가지가 성한가」였지 「이걸 master 에 얹으면 성한가」가 아니었다.  ▶ 처방 후보(아직 안 지었다 · 이 트랙은 PR #25 를 닫는 것이 과녁이라 섞지 않는다):    `작업가지.js --닫기` 가 머지 직전 `origin/master..HEAD` 뒤처짐을 **재서 말한다**.    ⚠ 막는 축으로 지으면 안 된다 — 이 저장소는 master 커밋 간격 중앙값이 1분이라 「0 뒤처짐」은    원리상 못 지키는 처방이고, 못 지키는 처방은 우회를 정상 통로로 만든다(F103). 보이는 축으로:    「이 CI 는 N 커밋 뒤처진 판에서 쟀다」를 숫자로 낸다(보드 상한 분모 건과 같은 축 뒤집기). | ⏸ 재현 1건 추가(local_524b5160 · PR #33): 가지에서 잰 래칫은 4→3, 머지 결과에서 다시 재니 6→5 였다 — F530 이 말한 갈림이 다른 트랙에서 그대로 났다. 여기서 안 닫는 이유: 처방은 작업가지.js --닫기 가 「머지 결과」를 재게 만드는 것이고 그건 이 트랙(#Q99 갚기) 범위 밖이며 신고자 세션의 몫이다. 대신 이 판에서는 손 산수 대신 tools/lib/시트도달.js 로 재서 옮겼다. |
