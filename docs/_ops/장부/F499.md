| F499 | 2026-08-16 | 마찰 | 유호님 결정큐가 **이미 답이 나온 질문**을 계속 배달한다 — 결정이 병목인 판에서 그 병목을 가짜로 부풀린다.  실측 2026-08-16 (`local_86d6d162` · #Q93 ㉠3차 중 발견): `node tools/decision-queue.js --all` 5번 항목 = `brand-kit-review-0814` 의 **⏳유호 P6 「마스코트에 아직 이름이 없습니다 — ㉠제가 후보를 좁혀 지금 정할까요(씨앗: 체리·톡톡·시니), ㉡첫 기수 학생들 공모로 맡길까요?」**. 그런데 `naming-canon` 메모리에 **「✅마스코트 = 몽글 유호 확정 2026-08-15 (「몽글로 확정하자」)」**가 박혀 있고, 같은 파일이 5차 재평가까지 거쳐 **몽글 유지**로 닫았다. 제시된 씨앗 셋(체리·톡톡·시니)은 전부 그 뒤 기각·폐기된 후보다. 즉 이 질문은 **하루 넘게 죽은 채** 유호님 화면에 서 있었다.  왜 안 걸렸나 — 축이 둘이다: ① **결정큐는 질문이 적힌 파일만 본다.** 답은 다른 토픽 파일(`naming-canon`)에 적혔고, 두 파일을 잇는 기계가 0이다.    `memory-status-guard` 는 **같은 파일 안**의 ⏳↔✅ 어긋남만 본다 — 파일이 갈리면 원리상 눈이 먼다. ② **답을 받은 세션과 질문을 세운 세션이 다르다.** 답을 들은 세션은 자기 토픽에 ✅를 적고 끝냈고,    남의 토픽의 ⏳를 내릴 이유도 통로도 없었다. 보드에서 F073 이 만드는 것과 **같은 모양**의 결함이다    (#Q93: 「끝낸 사람이 남의 줄을 못 닫는다」).  처방 후보(안 지었다 — 이 신고는 기록이고, 짓는 것은 결정큐 트랙 `6f4c6af5` 몫이라 비켜났다): ㉠ 배달 직전 그 ⏳ 문구의 **핵심 명사**가 다른 토픽에서 ✅와 함께 나오는지 훑어 「낡았을 수 있음」 표식만 붙인다    (자동으로 안 내린다 — 오탐이 진짜 미결을 지우는 쪽으로 새면 그게 더 나쁘다). ㉡ 결정에 번호를 주고 답이 그 번호를 인용하게 한다(#Qnn 이 대기열에서 하는 그 조인 키 역할). ⚠ 대가: ㉠은 훑는 값이 매 세션 시작에 붙는다 — 배달 직전 1회로 묶어야 한다.  ⚠ 이 신고에는 짝 하나가 더 있다: 나도 **같은 판에서 같은 층의 실수**를 했다 — 새로 지은 `tools/lib/보드근거.js` 가 「찾을 게 없었다(`[]`)」와 「못 쟀다(`null`)」를 뭉쳐, 경로 표기가 없는 줄에 「git 실패」를 냈다(같은 출력의 「못 본 곳」은 「경로 표기가 없다」라 두 말이 정면으로 어긋났다). CLAUDE.md 에 F207 로 박혀 있는데도 **새 장치 첫 판에서 그대로 재발**했다 — 실사용 첫 호출이 잡아 고치고 회귀·변이로 못박았다(`ebb255f9` · 변이 15/15). 즉 「0건 vs 미측정」은 프로즈 조항으로는 안 막히고 있다. | ⏸ 지금 기계를 안 짓는다 — 실측이 처방 ㉠을 기각했다. 잰 자리 local_41b1ad2f 2026-08-18. ①낱말 겹침 판별자: 미결 92건 중 82건을 후보로 뱉어 변별력 0(신고문이 예고한 거짓양성이 그대로 실측됐다 · 답 코퍼스=메모리 459파일+CLAUDE.md+철학 정본, 답으로 읽힌 줄 3320). ②좁힌 판별자(선택지 라벨 ㉮㉯㉰ 를 단 질문 ↔ 그 라벨을 인용한 픽·확정 줄): 모집단 11건뿐이고 후보 8건 중 열어서 확정된 낡음은 1건 — 나머지 81건은 이 판별자가 원리상 못 본다(0건이 아니라 안 쟀다 F207). 즉 지금 재료로는 표식조차 자동으로 못 붙인다. ③대신 사람이 회수했다: guard-routing-and-collisions.md:855 의 ㉮㉯㉰ 질문은 board-row-limit-denominator 에 유호 픽 08-16 ㉮ 로 답이 있고 fe56e332·PR #24 로 집행·CLAUDE.md 개정까지 끝났는데 이틀째 배달되고 있었다 → 그 자리에서 닫아 큐 92→91. ④덤으로 tests/결정큐.test.js 적색 1건을 해소했다(git-credential-manager-hang.md 의 배달 본문에 종결 표식 글자가 들어가 있던 자리 · 47/47). 다시 열 조건 = ㉡(결정에 번호를 주고 답이 그 번호를 인용) 이 서면 조인 키가 생겨 판별이 낱말 추측을 안 해도 된다. |
