| F521 | 2026-08-17 | 마찰 | 대기열 점유 판정이 워크트리 안의 보드 선언을 통째로 못 본다 — 「지금 집을 수 있는 것」이 전건 거짓 초록이었다. 실측 2026-08-17 세션 52393118(이 세션이 그대로 겪었다): SessionStart 대기열 훅이 🟢 「지금 집을 수 있는 것 2건」으로 #Q99·#Q100 을 불렀는데, 둘 다 이미 잡혀 있었다 — worktree-q99-talk-index-reach @ d8439590 「보드 선언 93a2d32e — #Q99」 · worktree-q100-collect-marker-scope @ c5e55666 「보드 선언 24b6c036 — #Q100」. 분모와 함께: 초록 2 = 진짜 빈 자리 0 + 워크트리에 가려진 것 2. 기전은 한 줄이다 — tools/대기열.js 보드행들() 이 보드.줄들(ROOT) 를 부르고, lib/보드.js 훑기() 는 <root>/docs/_ops/보드 한 디렉터리만 readdir 한다. 워크트리의 보드 파일은 원리상 안 보인다. 🔴 이 구멍은 08-15 「코드 트랙은 워크트리+PR로 짓는다」 확정이 넓혔다: 그 규약의 순서가 ①EnterWorktree ②보드 선언 커밋 이라 선언이 가지 안에 앉는다 — 즉 머지될 때까지 **모든 코드 트랙**이 대기열 눈에 「빈 자리」다. 규약이 바뀌면서 기존 장치의 분모가 조용히 줄어든 자리(가드 맹점 ④ — 맞는 얼굴로 틀린 값). F079 가 같은 병을 편집 시점(track-collision 신호④)과 미커밋(작업본소유자)에는 세웠는데 **트랙 고르는 층**엔 안 섰고, F221 은 그 층을 알고도 「공유 파일마다 울려 안 읽히는 경고가 된다」며 일부러 안 만들었다 — 그 판단은 Read·Grep 상시 경고 얘기였지 대기열 점유 판정(이미 있는 장치)이 아니다. 새는 방향이 「부른다」라 F406 이 스스로 「제일 나쁘다」고 적어 둔 그 방향이고, 마지막 문인 board-guard 는 선언 순간에만 막아 그 전까지의 조사·설계 비용은 이미 버려진다. | |
