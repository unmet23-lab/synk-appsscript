| F521 | 2026-08-17 | 마찰 | 대기열 점유 판정이 워크트리 안의 보드 선언을 통째로 못 본다 — 「지금 집을 수 있는 것」이 전건 거짓 초록이었다. 실측 2026-08-17 세션 52393118(이 세션이 그대로 겪었다): SessionStart 대기열 훅이 🟢 「지금 집을 수 있는 것 2건」으로 #Q99·#Q100 을 불렀는데, 둘 다 이미 잡혀 있었다 — worktree-q99-talk-index-reach @ d8439590 「보드 선언 93a2d32e — #Q99」 · worktree-q100-collect-marker-scope @ c5e55666 「보드 선언 24b6c036 — #Q100」. 분모와 함께: 초록 2 = 진짜 빈 자리 0 + 워크트리에 가려진 것 2. 기전은 한 줄이다 — tools/대기열.js 보드행들() 이 보드.줄들(ROOT) 를 부르고, lib/보드.js 훑기() 는 <root>/docs/_ops/보드 한 디렉터리만 readdir 한다. 워크트리의 보드 파일은 원리상 안 보인다. 🔴 이 구멍은 08-15 「코드 트랙은 워크트리+PR로 짓는다」 확정이 넓혔다: 그 규약의 순서가 ①EnterWorktree ②보드 선언 커밋 이라 선언이 가지 안에 앉는다 — 즉 머지될 때까지 **모든 코드 트랙**이 대기열 눈에 「빈 자리」다. 규약이 바뀌면서 기존 장치의 분모가 조용히 줄어든 자리(가드 맹점 ④ — 맞는 얼굴로 틀린 값). F079 가 같은 병을 편집 시점(track-collision 신호④)과 미커밋(작업본소유자)에는 세웠는데 **트랙 고르는 층**엔 안 섰고, F221 은 그 층을 알고도 「공유 파일마다 울려 안 읽히는 경고가 된다」며 일부러 안 만들었다 — 그 판단은 Read·Grep 상시 경고 얘기였지 대기열 점유 판정(이미 있는 장치)이 아니다. 새는 방향이 「부른다」라 F406 이 스스로 「제일 나쁘다」고 적어 둔 그 방향이고, 마지막 문인 board-guard 는 선언 순간에만 막아 그 전까지의 조사·설계 비용은 이미 버려진다. | tools/lib/보드.js 가지줄들() 신설 — git 한 스폰(log --branches --not master origin/master --source -p -U0 -- docs/_ops/보드/)으로 미머지 가지의 보드 표줄을 받고, 대기열 점유가 메인 보드와 **같은 잣대로** 그것까지 본다(14cdc8c4·e2616c5f · PR #29). 🔑 줄들()은 안 넓혔다 — 그 함수는 표 조립·board-guard 활성 상한·board-move 가 함께 쓰는데 거기 남의 트리 줄이 섞이면 12줄 상한이 남의 트리 때문에 막힌다. 넓혀야 하는 것은 점유 판정의 «분모» 하나뿐이라 그 자리만 더해 썼다. 🔑 못 부르면 모름:true 로 내고 대기열이 「미머지 가지를 못 훑었다 — 위 초록은 메인 보드 안에서만 참이다」를 화면에 밝힌다(빈 배열=없다 와 안 접는다 · F207). 🔑 층을 갈랐다: 잡혔다/아니다는 줄 «내용»으로만 판정하고, 출처(가지·지문)는 드러낼 때만 쓴다 — 회귀가 「메인이든 가지든 같은 결과」로 못박는다. ■ 재서 골랐다(추정 아님 · 실측 2026-08-17 작업 트리 12·보드 91파일): 트리별 보드 전량 읽기 = 1,162파일 392ms + worktree list 282~514ms / 크기 대조로 거르기 = 안 듣는다(980 중 899가 다르다 — 트리마다 뗀 시점이 달라 메인이 이미 움직였다) / 채택한 git 1스폰 = 226~486ms(회차 편차). 대기열 110ms → 약 340~600ms 가 되고, 그 값을 코드 주석에 못박았다(낮추려면 다시 재라). ■ 첫 판을 자기검증이 잡았다: %D 로 가지 이름을 붙였더니 실물 17줄 중 2줄이 «남의 가지» 이름을 달았다 — 문서에 「대가」로 적고 넘어가려다 --source/%S 로 커밋별 정확 귀속이 되는 걸 확인해 고쳤다. ■ 회귀 tests/보드가지.test.js 13 + tests/대기열점유.test.js 4 — 탐지력은 픽스처가 진다(파서가 순수 함수라 git 0으로 전부 잰다 · 실저장소는 계약만 보고 못 재면 skip · F296). 변이 12/12 · 구멍 0 · 미적용 0 — 첫 판은 11/12 였고 그 구멍이 **배선 층**(보드행들이 가지 줄을 안 붙인다)이라 「검사를 못 붙일 형태」가 원인이었다: 보드행들(옵션)에 자료 뿌리·가지 훑기 주입구를 내서 순수 픽스처로 닫았다. CI 모사 3846건 fail 0 · skip 31(원격 run 은 Actions 소진으로 0건 = 그 층 미검증). ■ 라이브 확인: 같은 명령이 이제 🟢 2건 대신 🔒 「이미 잡힌 줄」로 #Q99←worktree-q99-talk-index-reach(93a2d32e) · #Q100←worktree-q100-collect-marker-scope(24b6c036) 를 가지 이름과 함께 낸다. |
