| F586 | 2026-08-17 | 마찰 | 마찰 장부의 **번호 채번이 가지 사이에서 충돌**하고, 그렇게 겹친 두 신고 중 하나가 머지에서 조용히 사라진다 — 그리고 `장부유실` 회귀는 그것을 **원리상 못 본다**(번호 단위로 재기 때문). 새는 방향이 「통과」다.  ■ 실측 2026-08-17 (`local_e9524e5d` · F578 트랙을 닫다가 발견)  · 두 세션이 같은 **F578** 을 땄다 — `8aa76694`(`local_41cc57e7` · 배포 검수 과녁) · `f3ff9b70`(`local_67f76e1d` · git 자격증명 매달림). 서로 다른 사건이다.  · 둘 다 같은 파일 `docs/_ops/장부/F578.md` 에 각자 1행을 썼다. 서로의 존재를 모른다.  · `f9777da5`(`local_67f76e1d` · 「Merge origin/master — F519 가지에 반입」)에서 그 파일이 충돌했고 **자격증명 행이 버려졌다.** 실측: 부모1 `f3ff9b70` = 자격증명 1행 / 부모2 `42ede183` = 과녁 1행 / **결과 = 과녁 1행**.  · 즉 그 세션이 **자기 신고를 자기 머지로** 지웠다. 그 뒤 머지 5건이 전부 1행을 그대로 물려받았고(내 PR #87 머지 `060efb5f` 도 두 부모가 이미 1행이었다), 아무 데서도 소리가 안 났다.  ■ 왜 채번이 겹치나 — 락은 **동시성**을 막지 **분기**를 못 막는다  `tools/friction.js:86` 의 채번 락은 스스로 「번호만 지키고 파일 내용은 안 지킨다」고 적는다(F148). 그런데 번호를 정하는 재료는 **내 작업본의 장부 폴더**다. 이 저장소는 「워크트리+PR」이 정본 동선이라(CLAUDE.md 세션 규약) 가지가 상시 갈라져 있고, 갈라진 두 가지는 서로의 새 조각 파일을 못 본다. 락은 같은 작업본 안의 동시 호출만 직렬화하므로 이 자리에 원리상 닿지 않는다.  ⚠ 그리고 조각 폴더 설계의 전제가 여기서 깨진다 — `friction.js:26-29` 는 「새 행이 새 파일이면 부딪칠 자리 자체가 없다」고 적었는데, **번호가 겹치면 새 파일이 아니라 같은 파일**이다. 파일을 쪼개 없앤 줄 알았던 충돌이 채번 층으로 옮겨 앉았을 뿐이다.  ■ 왜 회귀가 안 잡았나 — **번호 단위로 재기 때문**  `tests/장부유실.test.js:76` 은 「이력에 `+| F0NN |` 로 올라왔던 **번호**가 지금 장부에 없다」를 잰다. F578 은 과녁 신고로 여전히 살아 있으므로 **번호로는 아무것도 안 사라졌다.** 행이 사라진 것을 번호로 재면 영원히 초록이다 — 가드 맹점 ④(맞는 얼굴로 틀린 값).  ■ 무게 — 이번 실해는 작지만 그건 **우연이다**  사라진 자격증명 신고는 같은 저녁 `local_ac18eae8` 이 **F580** 으로 같은 사건을 다시 겪어 신고해 두어 내용이 남았다. 장치가 지킨 게 아니라 두 세션이 같은 것을 밟았을 뿐이다. 그래서 이번 건은 **복구하지 않는다**(F580 이 정본 · 남의 신고 원문을 내가 새 번호로 재작성하는 것은 그 자체가 오염이다) — 이 판정을 여기 적어 다음 세션이 같은 대조를 다시 하지 않게 한다(F386).  ■ 처방 후보 — **안 지었다**(이 신고는 재료다. 채번은 장부 전체의 급소라 되돌림 비용이 크고, 과녁이 남의 활성 파일이다)  ㉠ 채번 직전 `git fetch` 해서 **origin 의 장부 폴더까지** 보고 딴다. ⚠대가 = 신고마다 네트워크 왕복이 붙고, F580 이 살아 있는 날엔 그 왕복이 매달릴 수 있다(신고를 못 하게 만드는 처방은 최악이다).  ㉡ 번호에 세션 지문을 섞는다(`F578-e9524e5d`). ⚠대가 = 기존 `F0NN` 인용 규약과 갈리고, CLAUDE.md·메모리·회귀에 박힌 참조 표기가 전부 두 갈래가 된다.  ㉢ 유실 검사를 **행 지문 단위**로 넓힌다. ⚠대가 = 해소 마크·오타 수정 같은 정상 편집이 전부 유실로 보인다 — 거짓양성이 곧 우회 손버릇이 되는 방향이라 그대로는 못 쓴다.  ㉣ **가장 싸고 드러내기만 하는 것** — 조각 파일 하나에 `| F0NN |` 행이 2개 이상이면 그 자리에서 알린다(읽기 한 번). 충돌을 막지는 못하지만 **사라지기 전에** 보인다. 회귀도 픽스처로 바로 선다.  ⚠ 어느 쪽이든 급소는 **충돌 해결에서 append-only 를 지키는 것**이다 — 그 두 행은 **둘 다** 살아야 했다. | ✅ 신고문의 급소(「그 두 행은 둘 다 살아야 했다」)를 기계로 닫았다 — ea797b1e · PR #101. **㉣ 단독이 아니라 두 층 한 벌이다**: ①막는 층 = .gitattributes 의 docs/_ops/장부/*.md merge=union — F578 사고를 그대로 재현해 실측했다(지정 없음 = add/add 충돌 exit 1·표식 3줄 → 사람이 하나를 버린다 / union = exit 0·표식 0·2행 전부 생존) ②드러내는 층 = 읽기 통로가 조각의 행을 «전량» 싣고 「한 파일 = 한 행」이 깨지면 그 자리에서 처방과 함께 배너를 낸다(판정은 순수 함수+분모 · 조각이상()). 🔴 **착수 중 발견 — 유실은 머지 «뒤»에도 한 번 더 있었다**: friction.js 조각행들() 이 .find() 로 첫 행만 집어, 머지가 두 행을 살려 놔도 도구가 둘째를 다시 버렸다. ㉣만 넣었으면 반쪽이었다(그 .find() 는 닫았다 · 옛 통로 0). ⚠ **대가도 실측했다**: union 은 양쪽이 «같은 행»을 다르게 고치면(해소 칸 채우기 등) 충돌 대신 행을 둘로 늘린다 — 즉 새는 방향이 「행이 는다」 하나뿐이고 그 모양이 ②의 탐지기 «안»이라 조용한 통로가 안 남는다. 🔴 **안 닫은 것 = 채번 충돌 자체**(㉠fetch·㉡지문 섞기는 그대로 미착수 · 대가가 서로 반대라 고르는 것이 판정이라는 이 신고의 말이 그대로 유효하다). 지금 닫은 것은 「겹치면 하나가 사라진다」이고, 겹침은 여전히 난다 — 다만 **무손실이고 시끄럽다**. 남은 창 하나: 머지를 도는 통로(작업가지 --닫기·master동기)가 friction.js 도 테스트도 안 지나므로, 겹침이 생긴 뒤 다음 friction.js 호출이나 test-ci 까지 «지연»이 있다(유실은 아니다). 검증 = 회귀 8 신설·장부유실 20/20 · **변이 7/7 구멍0 미적용0**(M7 = union 줄을 지우면 「머지하면 두 행이 산다」가 빨개진다 — 표기가 아니라 행위로 잰다 F590) · CI 모사 4454 fail0(원격 run 0건=미검증) · 실저장소 조각 364벌 겹침0·이름어긋남0. 🔑 자기검증에서 잡은 것: 이 결함을 재려던 내 첫 명령 grep -c '^/ F[0-9]' 가 F526.md(행이 '/  F526 /' · 공백 둘)를 「0행」으로 내 멀쩡한 파일을 깨진 것으로 읽었다 — 하필 F526 자신이 경고한 표기 축이라, 탐지기에 \s* 를 두고 픽스처로 못박았다(M1). |
