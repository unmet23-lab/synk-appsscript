| F398 | 2026-08-13 | 실수 | 인계문 도구가 «반쪽 종결»을 종결로 읽는다 — 상태 칸이 ✅로 시작하면(①판정 ✅ / ②집행 ▶) 「이 트랙은 종결로 적혀 있다 · 새 트랙을 잡아라」로 내보낸다. 5c55e5f5 실측: 남은 ②집행이 배경으로 밀릴 뻔했다. 우회=상태 칸을 ▶로 시작. 처방 후보=칸 안에 ▶가 있으면 종결로 안 읽기. | session-report.js `잔여조각()`+`종결문구()` — 종결 갈래가 잔여를 **단정하지 않고 조각을 글자로 보인다**(`73c7c680`). 신고문의 처방 후보(「칸 안에 ▶ 가 있으면 종결로 안 읽기」)는 **재 보고 기각했다**: 아카이브 ✅줄 540 중 **86줄이 ▶ 를 달고도 진짜 종결**이라(▶ 가 «다음 주자» 표기를 겸한다 — 실측 상위 `다음=새 트랙` 9·`다음=§8-4 화면(주인 없음)`·`다음=작업 대기열`) 후보대로 뒤집으면 종결 여섯 중 하나가 「이어라」로 나가고, 그건 `종결줄()` 주석이 일부러 피한 비싼 쪽이자 F169 가 실제로 당한 사고다. 그래서 고친 것은 **판정이 아니라 단정**이다 — 조각을 보이고 「원본 줄을 열어 가른 뒤 정하라」로 넘긴다(틀렸을 때 값=한 줄, 안 했을 때 값=조용히 사라진 잔여). F256 이 같은 자리의 **반대 방향**을 이미 실측했으므로(▶다음= 이 이미 끝난 일을 가리켜 오도) 어느 쪽 단정도 옳지 않고, 기계가 못 가르는 것은 사람에게 넘기는 게 맞다. 배경 목록(⏳·⏸·🎫·새 트랙·다음 없음)은 **좁게** 뒀다 — 넣는 순간 그 모양은 영영 안 보이므로 새는 방향을 「보인다」쪽에 뒀다. 회귀 `tests/인계문잔여.test.js` 15건(픽스처=실제로 났던 상태 칸 · 실저장소엔 거짓양성 불변식만 · 산 보드 ✅줄 8개 태움) · **변이 6/6** 격리 사본(①고치기 전 되돌림=F083 신고 원문 ②후보대로 뒤집기 ③④배경목록 비움·넓힘 ⑤단정을 조각과 같이 냄 ⑥상한 제거). F169 는 그대로 산다 — 그 잔여는 ⚠ 라 ▶ 가 없고 `tests/트랙경계.test.js` 픽스처가 옛 문구를 계속 못박는다(경계를 가른 것이지 무른 게 아니다). ⚠ 우회(상태 칸을 ▶ 로 열기)도 계속 통한다. |
