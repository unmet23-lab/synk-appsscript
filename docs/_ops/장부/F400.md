| F400 | 2026-08-13 | 실수 | SYNK-talk tools/배포대조.js 가 «모르는 플래그를 조용히 무시»한다 — 대상은 .env 의 SUPABASE_PROJECT_REF 하나로만 정해지는데 --운영 을 받고도 오류 없이 리허설을 다시 재서 「다름 0」 초록을 냈다. 라디오24 낡음 대조 중 실측(2026-08-13): 같은 14함수 초록을 두 번 받고 «운영도 최신»으로 30초간 오판했다 — 헤더의 「대상 ▸ synk-core-rehearsal」을 다시 읽어서야 갈렸다. 새는 방향이 「통과」다(CLAUDE.md 가드 맹점). 처방 후보 = 모르는 플래그면 die, 또는 --운영 을 실제 운영 REF 로 잇기. | SYNK-talk `7f312ae` — ①모르는 `--` 플래그 die ②`--운영`을 `자격증명.운영REF` 로 실연결(GET 전용 · 그 키가 곧 과녁판정의 승인 손이라 우회 아님) ③순수 `인자판정(args,env)` 분리로 탐지력이 네트워크에 안 기댐(F296). 회귀 4(⑩-a 모름 거절·⑩-b --운영이 실제로 ref 를 바꾼다·⑩-c 거짓양성·⑩-d 사용법↔아는플래그 등록층) · **변이 2/2 잡힘**(운영분기를 false 로 죽이면 ⑩-b 적색 · 모름검사를 죽이면 ⑩-a 적색) · 스위트 1996/1996. 🔴첫 실측이 바로 수확: 운영 **다름 7/14**(리허설은 0) — 이 구멍이 감추고 있던 드리프트다. |
