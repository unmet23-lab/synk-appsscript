| F659 | 2026-08-19 | 마찰 | **자산을 지우는 일은 「파일 지우기」가 아니라 «배선 걷기»였다** — 유호 확정 「체리 빨간 구판 아예 삭제」. 지울 파일은 26개(51MB)뿐인데, 그걸 거는 곳이 **도구 10벌·검사 3벌·토큰 2곳·지면 18벌**이었다. 그냥 지웠으면 ①`tests/마스코트자산.test.js` 가 그 폴더의 **존재를 요구**해 즉시 빨개지고 ②`재질굽기.js` 가 흐린 ENOENT 로 죽고 ③토큰 `정본사진.특별`·체리 램프가 없는 파일을 가리키고 ④브랜드렌더린트의 IP 색 판정이 빈 집합이 된 걸 «못 읽은 것»과 구분 못 했다. 🔑그리고 이 삭제는 **08-15 유호 확정(「평소=코랄·특별=체리, 대체가 아니라 역할 분리」)을 뒤집는** 일이라, 파일보다 그 조항을 먼저 접어야 했다. | ✅ 순서를 **배선→삭제**로 잡았다: 토큰(체리 램프·특별 슬롯·의상 조항) → 린트(MASCOT 빈 집합 · 바닥은 평상복) → 검사(존재 요구를 「알아보기 요구」로 갈라 `퇴역폴더` 신설) → 지면(주제가 «체리가 아닌» 시안 3벌은 정본으로 재조준 · 체리가 «주제»인 판정 기록 2벌은 그림만 죽이고 **퇴역 배너로 이유를 지면에 박음** — 기록을 고치면 그 판정이 거짓이 된다) → 그 뒤 삭제. ⚠**「미측정 방어」를 옮겼다** — 옛 검사는 `MASCOT >= 3` 으로 「안 실렸나」를 막았는데 체리가 비면 그 방어가 통째로 사라진다. 값이 남은 축(`MASCOT_바닥`)으로 이전했다. 전부 git 추적이라 되돌릴 수 있고, 되살리는 법을 토큰 조항·린트 주석·배너 세 곳에 적었다(파일만 되살리면 배선이 없다). |
