| F333 | 2026-08-11 | 마찰 | codex-review ③최종검수가 결과 기록 0으로 종료 — 4수 실측(파이프 경유 2수는 0바이트+exit0 위장: head/tail이 node 종료코드를 삼킴 · 순수 실행 2수도 헤더만 찍고 검수기록.jsonl 무기록·차단급 판정 불능). CLI 0.146.0 생존·--제안 패스는 정상 — 2패스 장주기 층 의심. v9.209는 그 층 미검증 명시로 나감 | tools/codex-review.js 진행 표식 + 소요 안내(825ad601) + 회귀 6(tests/이종검수.test.js 81/81 · CI 모사 2514/0).  🔴 신고문의 진단은 틀렸다 — 「2패스 장주기 층 의심」이 아니라 도구는 처음부터 멀쩡했다. 원인은 산술이다: ③최종검수 정상 완주 = 판단 패스 4번(회차 2 × 버그사냥+기능체크) × 5~8분 = 20~40분인데, 호출자(세션) 포그라운드 셸은 기본 2분·상한 10분이다. 즉 ③ 은 포그라운드에서 **완주할 수 없다** — 가끔 죽는 게 아니라 언제나 죽는다. F333 의 4수는 전부 그 상한 안에서 죽었다.  실측(08-11 · 조건 하나만 바꿨다): 같은 명령 `--commit 3d705b33` 을 **백그라운드로** 돌리니 버그사냥 2회차가 7.5분에 완주하고 기능체크로 넘어갔다. 바꾼 조건은 「호출자가 안 죽인다」 하나뿐이다.  왜 안 보였나: 밖에서 죽으면 catch 도 종료코드도 없어 남는 것이 이미 찍힌 헤더 몇 줄뿐이다 — 그래서 「도구가 결과 기록 0으로 종료」로 읽혔다(F207 계열 · 미실행이 통과와 같은 모양). 파이프 경유 2수가 exit 0 으로 보인 것은 F308① 의 재현이고 별개 층이다.  무엇으로 막았나: ①시작에 진행 표식을 남기고 `exit` 리스너로 정상 종료에 지운다 — 밖에서 죽으면(SIGTERM/SIGKILL) 리스너가 안 돌아 표식이 남고, 다음 실행이 「직전은 완주 못 했다(통과가 아니다)」를 대상·인자·pid·시작시각과 함께 말한다. 남는 것이 증거, 지워지는 것이 정상 — 새는 방향을 뒤집었다. ②시작 줄이 예상 분수와 **백그라운드 호출 명령 자체**를 낸다(조건 안 검 — 가장 빠른 `--버그만 --회차 1` 도 5~8분이라 기본 2분을 이미 넘는다). `--timeout` 을 늘리라는 옛 처방은 이 자리에선 해롭다: 도구가 더 버틸 뿐 호출자 상한은 그대로다(F103). ③거는 자리를 `진행걸기()` 하나로 두고 긴 경로 **셋 전부**(③최종검수·①선파악·②심문)가 탄다 — ③만 고치면 ①② 가 같은 모양으로 죽는 문으로 남는다.  실물 확인 2건: 표식이 긴 호출 **전에** 쓰이는 것과, 밖에서 죽인 뒤에도 남는 것을 실제 kill 로 확인했다(소스 검사로는 순서를 증명 못 한다). 기대는 전제 자체(`exit` 리스너가 정상 종료엔 돌고 외부 kill 엔 안 돈다)도 spawn+kill 픽스처로 못박았다.  게이트 영향 0 — 표식은 검수기록(배포 게이트 장부)과 섞지 않는다. 증거지 게이트가 아니다. |
