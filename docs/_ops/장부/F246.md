| F246 | 2026-08-08 | 마찰 | board-move 원칙⑥이 「내 작업 커밋」과 「배경으로 인용한 남의 커밋」을 안 가른다 — 줄 전체에서 해시를 긁어 하나라도 산 세션 것이면 이관을 막는다. 실측 2026-08-08: F243 종결 줄을 옮기려 하자 46897fe(314f9246·9분 전)를 근거로 차단됐는데, 그 해시는 내 트랙 칸에 적은 배경 설명이다(정본과 코드가 갈라진 것을 그 커밋이 고쳤다는 인용). 내 작업 커밋은 상태 칸의 5395056·6d8043a 이고 둘 다 내 것이다. F242 가 형제 저장소 해시를 못 읽던 자리를 고쳤는데 이건 그 옆 칸이다 — 그때는 재료가 없어 못 읽었고 이번엔 재료가 너무 많아 틀렸다. 새는 방향은 F242 와 반대라 덜 나쁘다(조용한 오이관이 아니라 시끄러운 미이관)지만, 처방이 「그 세션이 끝난 뒤 다시 돌려라」라서 내 세션에선 실행할 수 없고 종결 줄이 보드에 남아 활성 상한을 먹는다. 배경 인용은 F0NN 신고문이나 원인 설명에 흔하므로 드문 형태가 아니다. 처방 후보 ①해시 수집을 상태 칸으로 좁힌다(작업 커밋이 적히는 자리는 규약상 거기다) ②트랙 칸 해시는 소유 후보에서 빼고 상태 칸·세션 지문만 본다 ③차단 대신 「이 해시들은 배경 인용인가」를 묻고 지문이 내 것이면 통과. | tools/board-move.js 원칙⑥의 재료를 셋으로 갈랐다(f1979c1). ①**파일 이름** — F250 이후 보드 정본이 세션별 파일이라 「누가 이 줄을 썼나」는 추론이 아니라 좌표다. 셋 중 유일하게 정확하고, 커밋이 하나도 없는 트랙에도 있다. ②줄에 적힌 지문(`local_` 접두)은 그대로 둔다. ③커밋 해시는 **상태 칸만** 본다 — 작업 커밋이 적히는 자리는 규약상 거기고, 줄 전체를 긁으면 원인 설명의 배경 인용이 주인이 된다. 실측 2026-08-08(보드+아카이브 **333행 · 전부 4칸**): 지문이 상태 칸 밖에만 있는 행 **0**(=②는 오염 없음) · 해시 주인이 상태 칸 밖에만 있는 행 **18**(=그만큼이 오차단이었다) · 좁히면 재료가 0 이 되는 행 **4**. 그 4행은 옛 단일 파일 시절 작업 해시를 「만지는 파일」 칸에 적은 모양이라, 세션별 파일에서는 ① 이 그 자리를 대신한다 — 즉 좁히기의 미탐 방향을 새 장치가 아니라 **F250 이 이미 만들어 둔 좌표**로 메웠다(새 파일 0 · 새 상태 0 · 네트워크 0). 칸을 못 가르면 줄 전체로 떨어진다(폴백은 언제나 더 많이 보는 쪽). 칸 가르기는 tools/lib/표.js 한 통로만 쓴다(F119). 회귀 6 신설 · 보드이관 28/28 skip 0 · 표칸 8/8 · **변이 6/6**(①좁히기 원복 ②폴백 제거 ③파일이름 재료 삭제 ④조기반환에서 파일이름 뺌 ⑤내 지문 예외 삭제 ⑥죽음 무시 — 전부 빨개진다). 라이브 실측: 막혀 있던 종결 줄(「커밋만 되고 라이브에…」)이 주인 세션이 죽자 그대로 이관됐고, 주인이 살아 있는 줄(F250 후반 · 1f92d798)은 여전히 막힌다 — F146 을 안 열었다. 남은 틈: 세션이 **남의 파일에** 자기 줄을 쓰면 ① 이 엉뚱한 세션을 가리킨다. board-guard 는 「새 줄에 내 지문」까지만 요구하고 파일 소유를 막지는 않는다 — 그 경우 ②가 내 지문을 잡지만 막는 대상이 파일 주인 쪽이라 판정이 어긋난다. 곁들여 신고 F253(작업본소유자.보드선점 이 아직 날 split). |
