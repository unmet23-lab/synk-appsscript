| F612 | 2026-08-18 | 마찰 | 보드 상태 칸에 «분모를 보고하면» 그 글자가 표식이 된다 — 「🎫1」 을 `놓은줄()` 이 「이 트랙을 놓았다」로 읽었다 (`tools/lib/보드.js:262`)  ■ 실측 2026-08-18 (`local_5653b6a5` · F610 트랙 · 내가 만든 자리를 내가 밟았다)   · 내 보드 줄의 상태 칸에 실측 분모를 이렇게 적었다: `✅①분모 실측: 열림 10 = **남이 잡음 4** + 🎫1 + 빈자리 5`.     「🎫1」은 «놓인 줄이 1건» 이라는 **보고**다 — 내 트랙에 대한 말이 아니다.   · `놓은줄()`(`tools/lib/보드.js:262`)은 상태 칸 어디든 살아 있는 🎫 가 하나라도 있으면 **그 줄을 「주인이 놓은 줄」로 판정**한다.     그래서 방금 선언한 내 F610 트랙이 「🎫 놓인 줄 — 점유가 아니다 · 집어도 된다」로 나왔다.   · 🔴 급소는 그 함수의 **대가 주석 자신**이다: 「⚠ … 방향 = 「연다」라 새는 쪽이다. **지금 그 형태는 실측 0건**이고,     좁히려면 사람이 «놓았다»를 따로 적어야 하는데 그건 규약을 바꾸는 일이라 여기서 안 한다 — **알면서 남기는 값**이다.」     그 「실측 0건」이 **1건이 됐다.** 알면서 남긴 값이 실제로 청구된 첫 판이다.  ■ 왜 신호인가 — 새는 방향이 「연다」다   이 오독은 **막지 않고 푼다.** 내 활성 트랙이 「놓인 자리」로 보이면 남이 그 위에 선언해도 board-guard 가 안 막는다   (`놓은자리겹침` 으로 갈라져 처방이 「집어도 된다」쪽으로 간다). 즉 이 한 글자가 **중복 구현의 문을 연다** —   F610 이 막으려던 바로 그 사고다. 이번엔 내가 그 줄의 주인이라 손해가 안 났을 뿐이다.   그리고 이 형태는 앞으로 **늘어난다**: F610 이 세션시작·`--open` 에 「🎫놓임 N」을 정식 칸으로 세웠으니,   그 숫자를 보드 상태 칸에 옮겨 적는 것이 자연스러운 동작이 된다. 즉 신설 장치가 이 사각의 발생률을 올렸다.  ■ 안 잰 것 (F348)   · 실저장소의 다른 줄에 같은 오독이 몇 건인지 **안 셌다.** 내 줄 1건만 직접 증거다.   · `board-guard` 가 이 오독을 실제로 어떻게 처방하는지는 **코드에서 읽었지 돌려보지 않았다**     (`놓은자리겹침` 갈래 존재까지 확인 · 남의 줄로 실증하는 것은 남의 자리를 건드리는 일이라 안 했다).  ■ 지금 한 것 / 안 한 것   ▶ 한 것 = `tools/friction.js` 의 점유 갈래 **순서**로 방어했다(내 줄 > 🎫). 내가 놓았든 잡았든 그건 내 트랙이라     이 순서가 원래 옳고, 회귀·변이로 못박았다(`tests/장부점유.test.js` · 변이 ⑦).     ⚠ 그 방어는 **`friction.js` 안에서만** 참이다 — `놓은줄()` 자체는 그대로라 `board-guard` 는 여전히 오독한다.   ▶ 안 한 것 = `놓은줄()` 수리. 그 함수는 `board-guard` 의 판정을 지므로 좁히는 순간 **남의 활성 줄들의 판정이 바뀐다**.     지금 그 파일은 다른 세션의 선언에 잡혀 있고(F073), 되돌림 비용이 큰 자리라 이 트랙에서 안 만졌다.  ■ 처방 후보 — 안 지었다(이 신고는 재료다)   ㉠ 표식을 **자리로** 좁힌다 — 상태 칸 «첫 구절»의 🎫 만 표식으로 본다(`상태첫구절()` 이 이미 그 층을 가지고 있다).     ⚠대가: 「🔵착수 — … 🎫 이 갈래는 다음 주자에게」처럼 뒤에 붙여 놓는 실제 용례를 죽인다. 그 용례가 몇 건인지 먼저 세야 한다.   ㉡ 보고문에서 표식 글자를 **안 쓰기로** 규약을 정한다(「🎫1」 → 「놓임1」). 가장 싸지만 산문 규약이라 다음 사람이 모른다     — CLAUDE.md 가 매 턴 상주해도 그 아래 20줄을 못 지킨 자리가 이 저장소에 이미 여럿이다(F543).   ㉢ 아무것도 안 한다 — 실측 1건이고 그 1건은 자기 줄이었다. ⚠대가: 발생률을 올린 장치(F610)를 방금 세웠다. | |
