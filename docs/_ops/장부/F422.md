| F422 | 2026-08-14 | 실수 | 체크리스트의 미완 표식을 현재 상태로 읽어 「구직등록 15분이 최우선 · 480만이 걸렸다」를 1순위로 올렸는데, 고용24 마이페이지를 실제로 여니 08-12에 이미 완료돼 있었다(구직번호 K161132608120153). 그 표식은 08-12 시점의 사진이었다. 판정 직전에 대상을 직접 열었어야 했다 — 유호님이 급하지 않은 일을 급한 일로 알고 시간을 쓸 뻔했다. | ⏸ 실물은 고쳤다(§0-Z 를 실측값으로 교체 · §10-3 에 교훈). 닫지 않는 이유 = 기계 장치를 못 만든다 — 판정 대상이 repo 밖 웹사이트(고용24 마이페이지)라 가드·회귀가 닿지 않고, 억지로 만들면 「repo 밖 환경에 기대는 검사」(F296)가 된다. 같은 가족(F030·F033·F058·F062·F130)의 프로즈 조항이 CLAUDE.md 에 이미 있으니 중복 신설도 안 한다. 남는 쓸모 = /evolve 때 이 가족의 재발 횟수를 세는 재료. |
