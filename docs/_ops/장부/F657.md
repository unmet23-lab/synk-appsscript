| F657 | 2026-08-19 | 실수 | **파생 장부를 다시 구워 남의 같은 날 사진을 덮었다** — `작동대장.js` 를 여러 번 재생성했는데, 그 산출 jsonl 에 옆 세션이 이미 08-19 사진 한 줄을 넣어 둔 상태였다. 내 재생성이 그 날짜 줄을 «내 숫자»로 바꿔 그 줄이 사라졌고, `tests/장부유실.test.js`(F605)가 잡았다. 🔑append-only 장부인데 **생성기가 그것을 파생물로 취급**해 덮는다 — 사람이 지운 게 아니라 도구가 지운다. | ✅ 이력에서 그 줄만 찾아 **둘 다 남겼다**(테스트가 스스로 처방하는 「하나 고르기 금지」 그대로 · `git log --all -S`). 되살린 뒤 37/0. ⚠근본은 안 닫혔다 — 생성기가 같은 날짜 줄을 여전히 덮는다. 옆 세션(`5ff001ad`)도 같은 일을 겪고 같은 손 복구를 했다 = **2번째**라 시스템 결함 자리다. 처방 후보: 재굽기 전에 그 날짜 줄이 이미 있으면 덮지 말고 «한 줄 더» 쌓는다. |
