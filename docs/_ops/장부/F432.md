| F432 | 2026-08-14 | 마찰 | 이해대장 동봉 게이트(어제 신설)가 **조용히 통과**하던 자리 셋 — 판정층·등재는 초록인데 그 사이 배선은 아무도 안 쟀다. 셋 다 새는 방향이 「통과」다. ①스테이징된 삭제를 `--diff-filter=ACMR` 이 걸러, 화면을 지우는 커밋에서 게이트가 HEAD 사본(커밋 뒤엔 없는 파일)을 재고 통과했다 — 화면이 안 바뀌는 정본 개정과 겹치면 이 가드가 막으라고 있는 바로 그 피해가 그대로 난다. ②판정기(`tools/이해대장.js`)가 없으면 `node` 의 MODULE_NOT_FOUND 1이 그대로 차단이 돼, 부분 체크아웃·낡은 워크트리에서 정본을 한 글자도 못 고치게 했다(훅이 이 게이트에 대해 쓰는 규약은 「없으면 막지 않고 말한다」인데 게이트 안엔 그 갈래가 없었다 · F103). ③ROOT 를 `CLAUDE_PROJECT_DIR` 로 잡아, 그 변수가 다른 저장소를 가리키면 옆 저장소의 인덱스를 재고 통과했다 — 워크트리는 이 저장소가 실제로 쓰는 층이라 거기서 게이트가 통째로 눈이 먼다(F403 축). 급소는 셋이 아니라 **어떻게 안 보였나**다: 회귀가 판정층을 이음매에 직접 대고 재고 등재는 훅 파일을 grep 하는 두 층뿐이라, 그 둘이 다 초록인데 진짜 `git commit` 이 안 막히는 상태가 성립했다. 장치의 결함은 「안 도는 쪽」이 아니라 「맞는 얼굴로 틀린 값」 쪽으로 샌다는 CLAUDE.md 맹점 ④의 4번째 실측. | f34a765b — tests/대장동봉E2E.test.js(실 훅·실 게이트를 실은 1회용 저장소에 진짜 git commit · 10건) + 게이트 수리 셋: name-status 로 삭제·이름바뀜을 보고, 판정기 부재는 「못 했다」로 통과시키고, ROOT 를 git rev-parse --show-toplevel 로 뽑는다. 탐지력 실측 = 픽스 전 7/10(그 셋이 실패) |
