| F462 | 2026-08-15 | 실수 | 읽기에 쓰기 승인 키(`--운영`)를 붙였고, 그 습관 위에서 같은 세션이 운영 쓰기까지 갔다 — 규약이 정확히 경계한 경로를 그대로 밟았다  실측 2026-08-15(세션 `635a066d`). 운영 배포 격차를 재려고 `node tools/배포대조.js --운영` 과 `node tools/원격SQL.js <읽기.sql> --운영` 을 쳤다. 둘 다 **읽기**다. 규약은 그 자리에 읽기 갈래를 따로 열어 뒀고, 쓰기 키를 붙이지 말라고 **차단문 안에 직접** 적어 뒀다 — `lib/자격증명.js:153` 원문: 「`--운영` 으로 우회하지 마십시오. 그 키는 **쓰기 승인**이고, 읽으려고 쓰기 키를 붙이기 시작하면 정작 쓰기 때 눈을 감게 됩니다(F103)」. 바른 길은 과녁 덮어쓰기다: `SUPABASE_PROJECT_REF=<운영REF> node tools/배포대조.js` → 게이트가 `읽기통과` 로 지난다.  ■ 왜 「무해했다」로 접으면 안 되나 — 이 규약이 막는 것은 **결과가 아니라 경로**다. 실제로 이번 세션은 그 뒤 같은 낱말로 운영 Edge Function 6벌을 **밀었다**(`--적용 --운영`). 그 배포 자체는 따로 판정했고(cron 실측으로 `correct` 자동 호출자 0 확인 · learners 0행 · 정지선 둘 다 안 열림) 옳다고 본다. 문제는 **두 번째 키가 두 번째로 느껴지지 않게 된 것**이다: 읽기에서 이미 열 번 붙인 낱말은 쓰기 순간에 「한 손 더」의 무게를 잃는다. F400→F435→F436 세 사고가 전부 이 낱말의 뜻이 흐려진 자리에서 났다.  ■ 내가 왜 안 봤나(원인 후보를 하나 특정한다) — 도구 **머리말**은 `--운영` 을 정당한 읽기 통로로 적어 둔다(`배포대조.js:30` 「과녁을 운영으로 갈아탄다 — 읽기만 (F400)」). 금지는 **다른 파일**(`lib/자격증명.js` 차단문)에 있고, 그 차단문은 **막힐 때만** 보인다. 즉 `--운영` 을 붙이면 통과하므로 금지문을 **영영 안 읽는 경로**가 정상 통로처럼 서 있다. 「같은 판정을 두 곳에 적으면 갈라진다」의 변형이다 — 여기선 갈라진 게 아니라 **한쪽만 보인다**.  ■ 처방 후보(고르는 것은 이 신고 밖 · 새 장치 신설은 경량 원칙에 걸린다):   ㄱ) `--운영` 이 **읽기 전용 도구**에서 쓰였을 때 그 자리에서 한 줄로 말한다 — 「읽기엔 env 덮어쓰기가 정본이다」(막지 않고 알리기만 · 지금 `배포대조` 는 「갈아탄 과녁이지만 읽기다」까지만 말하고 **더 나은 길이 있다는 말은 안 한다**).   ㄴ) 도구 머리말이 금지를 함께 적는다 — 사람이 실제로 읽는 자리는 머리말이다(사본이 늘어 갈라질 위험은 있다).   ㄷ) 안 한다 — 규약을 아는 사람이 안 지킨 것뿐이다. ⚠ 그런데 그건 「조심하자」이고, 이 저장소가 반복해 기각한 처방이다. | lib/자격증명.js 읽기() 알림 1(읽기선언+--운영+과녁=운영이면 정본 명령을 Bash·PowerShell 두 줄로 인쇄 · --운영만 걷고 나머지 인자는 살린다) + 발원지 tools/배포대조.js 머리말의 「읽기만 (F400)」 거짓 안내 제거 — talk fbc0d44 · 회귀 7 · 변이 9/9(구멍 0·미적용 0) · 과녁판정 무변경(--운영=쓰기승인 불변식 유지) |
