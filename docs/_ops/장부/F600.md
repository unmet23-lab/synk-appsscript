| F600 | 2026-08-18 | 마찰 | 작동대장의 「마지막 손댐」 나이가 **매일 09시(KST) 이전엔 음수**로 나온다 — 주간 재설계 판정 화면이 맞는 얼굴로 틀린 값을 낸다. 그리고 **그 파일 자신의 머리말이 이 함정을 이미 이름 대어 경고하고 있다**(F543 과 같은 모양: 산문으로 적은 규칙이 그 규칙을 적은 사람의 다음 함수도 못 지켰다).  실측 2026-08-18 (`local_ba840681` · 손으로 잰 값 두 벌): · `node -e "new Date().toISOString()"` = `2026-08-17T21:11:16Z` ⇒ 작동대장의 `오늘`(:773 · UTC 자정 파싱) = **2026-08-17** · 같은 순간 `git -c core.quotepath=false log --date=short --format=@%ad` 는 `tools/대기열.js`·`tools/codex-review.js` 의 마지막 커밋을 **2026-08-18** 로 낸다(`%ad --date=short` = **작성자 타임존** 렌더 = KST). · ⇒ `:333` 의 `지난날 = Math.round((오늘 - Date.parse(날+'T00:00:00Z'))/86400000)` = **-1** · ⇒ 화면 Ⅳ절이 그대로 인쇄한다: `tools/codex-review.js … 2026-08-18 · -1일` · `tools/대기열.js … 2026-08-18 · -1일`(직접 눈으로 확인).  ■ 기전 — 시계가 둘인데 한 축으로 뺐다 `오늘` 은 **UTC 날짜**이고 `날` 은 **git 작성자 타임존 날짜**다. 한국은 UTC+9 라 **매일 00:00~09:00 KST 동안 두 축이 하루 갈린다** — 그 창에서 «오늘 손댄 파일»이 전부 -1일이 된다. 창이 하루 9시간(37.5%)이라 드문 자리가 아니다.  ■ 왜 신호인가 — 그 파일이 이 함정을 이미 적어 뒀다 `tools/작동대장.js:468~470` 머리말: 「🔑 **날짜로 잇지 않는다 — 이 저장소엔 「오늘」이 둘이다.** 작동대장은 UTC 날짜를 쓰고 … 한국은 UTC+9 라 **09시 이전엔 두 통로의 「오늘」이 하루 갈린다**」. 저자는 두 시계를 알았고 **기록 이음**(장부 append)에서는 못을 박았다. 그런데 **같은 파일 안 130줄 위의 `분류()` 나이 계산**은 그 못이 안 닿았다 — 거기서 git 이 «또 하나의 시계»를 들고 들어온다는 것을 안 본 것이다. F543 이 이름 댄 모양 그대로다: 「산문으로 적은 규칙은 그 규칙을 적은 사람의 다음 20줄도 못 지킨다」.  ■ 회귀가 원리상 눈이 멀어 있다 `tests/작동대장.test.js:64` 의 픽스처가 `손댐 = 모든 장치 → '2026-08-16'` 에 고정 `오늘` 이라 **날 > 오늘 인 경우를 한 번도 안 만든다.** 즉 탐지력을 진다는 픽스처가 «미래 날짜» 가지를 통째로 안 밟는다(맹점 ②의 다른 얼굴 — 버그를 요구하지 않는 것은 맞으나, **가지 자체가 없다**). 실저장소 검사(:300)는 손댐 지도가 «비었나»만 보고 값의 부호를 안 본다.  ■ 새는 방향 — 이번엔 「통과」가 아니라 「거짓 정보」다 날짜 축은 이 대장에서 **판정이 아니라 참고 열**이다(:632 · 유호 확정 08-17 「낡음=시간✗ 방향정합○」). 그래서 잘못된 값이 **아무 판정도 안 뒤집는다** — 그게 이 신고가 P0 가 아닌 이유이자, **아무도 안 알아채는 이유**다. 손해는 다른 데 있다: 주간 재설계 화면은 유호님이 여는 화면이고, 거기 「-1일」이 박혀 있으면 그 표 전체의 신뢰가 깎인다(그리고 「참고 열이니 괜찮다」는 판단은 읽는 사람이 아니라 짠 사람만 안다).  ■ 처방 후보 — 안 지었다(이 신고는 재료다) ㉠ **git 에서 축을 없앤다** — `--date=short` 대신 `--date=unix` 로 받아 JS 가 UTC 날짜로 접는다. 그러면 두 값이 **같은 축**이고 `TZ` 환경변수에도 안 기댄다(CLAUDE.md 「repo 밖 환경(시간대…)에 기대는 검사는 CI 에서 깨진다」 F296 — `--date=format-local` 은 그 함정을 새로 판다). ⚠대가: 08:00 KST 커밋이 화면에 «어제»로 적힌다 — 참고 열이라 감당 가능하나 그 뜻을 열 이름이 말해야 한다. ㉡ 나이를 **로컬 축**으로 통일한다(오늘도 로컬로). ⚠대가: 기록 장부가 UTC 라 이 파일 안에 시계가 둘 남는다 — 머리말이 금지한 바로 그 상태. ㉢ 음수를 **0 으로 접는다**. ⚠**하면 안 된다** — 이 저장소가 반복해 배운 「0 과 안 접는다」(F480·F207)를 정면으로 어긴다. 시계 어긋남·리베이스로 미래 날짜는 실제로 생기므로, 접으면 그때 아무 말도 안 하는 «맞는 얼굴»이 된다. | |
