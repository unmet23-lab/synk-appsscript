| F397 | 2026-08-13 | 마찰 | board-move 가 「주인 프로세스 사망」을 「완료」로 번역한다 — 상태 칸이 `▶작업중` 인 남의 줄을 「완료 줄 아카이브 이관」 커밋으로 옮긴다. 실측 2026-08-13: 세션 51a502a7 이 `18537b29` 로 내(0ba9d4da) 보드 줄을 옮겼는데 그 줄의 상태는 `▶작업중 — /deploy 진입` 이었고, 실제로 배포는 안 나간 상태였다(contents_서클.js 라이브 0 · Code.js·엔진_셋업확장 다름 — v9.228 서클 인쇄물이 통째로 미반영). 내가 다시 재선언해 이었기에 망정이지, 아카이브에 「완료」로 박힌 미완 트랙은 인계문·보드 어디에도 안 보이므로 **다음 세션이 원리상 못 줍는다** — F240(라이브가 조용히 뒤처짐)의 새 유입 경로다. 축이 F146·F165 와 다르다: 그건 「주인 표식이 없어 못 가른다」이고 이건 「표식도 상태 칸도 멀쩡한데 도구가 상태를 안 읽는다」다. 처방 후보 — board-move 가 대상 줄의 상태 칸을 읽어 ✅·완료 표식이 없으면 (ⓐ)거절하거나 (ⓑ)아카이브가 아니라 「미완 이관」 구역으로 보내고 대기열에 줄을 되살린다. 회귀는 픽스처로: `▶작업중` 줄 + 죽은 주인 = 이관 거절. | board-move 원칙 ⑦(`d36bbf9b`) — 남의 줄(파일 이름=좌표)이면서 `보드lib.활성행(row)` 이면 거절하고 아무것도 쓰지 않는다. 「끝났나」 판정을 다시 안 적고 `tools/lib/보드.js` 의 `활성행` 정본을 쓴 것이 급소다: board-guard(`완료줄들`=`!활성행`)·`tools/보드수거.js`(`완료행` 필터)가 같은 곳에서 판정을 받으므로 세 호출부 어디서도 갈라질 자리가 없고, 만석 처방·자동 수거가 이 거절에 죽지 않는다(F103·F278). 파일 주인 판정 정규식도 이 파일 안 한 곳(`파일의주인지문`)으로 모아 원칙 ⑥ 과 공유했다. 탈출구는 `--미완확인`. 회귀 `tests/보드이관.test.js` +6(탐지 2·거짓양성 3·자기처방 1) · 138/138. 변이 2/2 적색으로 탐지력 실측: ①가드 무력화(`false &&`) → 탐지 시험 2건 적색 = 신고 원문 그대로가 재현된다 ②활성 조건 제거(과잉 차단) → 내 거짓양성 시험 + 기존 F246 「죽은 주인 줄은 옮긴다」 동시 적색. 다른 아카이브 쓰기 경로 없음(grep 실측: auto-commit 은 보드·아카이브를 명시 제외, board-guard 는 읽기만, 보드수거는 board-move 위임). |
