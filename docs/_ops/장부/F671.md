| F671 | 2026-08-20 | 교정 | **구판 마스코트가 또 나왔다 — 같은 계열 네 번째. 이번엔 가드가 «초록인 채로» 났다.** 유호 교정 08-20 「예전 기억들이나 예전 정보들 … 또 예전 체리빛 글래시한 스타일의 마스코트를 가져와서 랜더링하더라고」. 🔑**앞선 셋(F443·F479·F658)의 처방은 옳았는데 축이 하나뿐이었다** — `tests/마스코트정본.test.js` 는 「지면이 구판을 «입는다»」(src·url())만 보고, 「문서가 구판을 «정본이라 부른다»」를 안 본다. AI 세션이 «무엇을 그릴지» 정할 때 읽는 것은 후자다: 그림을 한 장도 안 실어도 「그림 정본 = 마스코트_렌더」라고 적힌 문장 하나면 다음 세션이 그 스타일로 새로 굽는다. 📏실측 08-20: 그 가드가 **3/3 초록**인데 퇴역 3폴더 실로드가 **29건**이었고 **전부 그 분모 밖**이었다(「입는다」 23벌 · `bots/` **0벌**). 급소 셋 — ①`docs/브랜드킷.html` footer 가 「**정본 사슬**」이라는 이름으로 삭제된 폴더를 가리킨다(킷은 「마스코트 정본이 뭐냐」를 찾는 사람이 가장 먼저 여는 자리다) ②`bots/오버레이/마스코트.html` 은 08-19 퇴역 이후 그림이 **0/5** — 경로가 JS 상수라 매처에도, 지면방 등록층에도 두 겹으로 안 걸렸다(누구도 못 봤다) ③`캐릭터_생명감_설계.md` 가 「컷·조립 규격 정본 = `마스코트_렌더/README.md`」로 **삭제된 파일**을 정본이라 부른다. 🔴**그리고 내가 이 병을 그 자리에서 재현했다** — 로컬 master 가 `origin/master` 보다 **347 커밋 뒤**라 08-19 에 지워진 26개 자산이 작업본에 그대로 살아 있었고, 나는 그 README 를 「낡았다」고 판정해 낡음 표식을 달아 커밋했다(`77050991`). 즉 **유호님이 지운 것을 되살리는 커밋**이었다(머지가 삭제 쪽으로 풀어 트리에는 안 남았다). 뒤처진 판에서는 «삭제된 것이 살아 있는 것과 같은 모양»이라, 세션이 그것을 정본으로 읽는다 — 시동 훅은 347 뒤처짐을 「**못 민다**」(push 문제)로만 냈지 「**네 파일이 거짓말한다**」로는 안 냈다. ⚠부수 실측: 검사 초안이 `git ls-files`(한글 경로 8진 이스케이프)로 분모를 세어 **0/0** 이 나왔다 — 파일을 하나도 못 연 «거짓 초록»이다(`-z` 로 고침). | ✅ **축을 늘렸다 — 호출부를 또 고치지 않았다.** `tests/마스코트정본.test.js` 에 **축 ②「참조층 문서가 퇴역 자산을 «정본»이라 부르지 않는다」** 신설(분모 = `docs/`·`bots/`·`.claude/` **774벌** · 축 ① 분모 23벌의 34배). 신호 둘 = ㉠낱말 `정본` ㉡확장자 붙은 자산 포인터(`마스코트_렌더/본체.png`). 면제는 **파일 이름 목록이 아니라 «퇴역을 안다는 표시»** 로 준다 — 지면 위쪽 퇴역 배너(`퇴역`+`2026-08-19`) 아래는 「알고 남긴 기록」으로 통과하고, **배너보다 앞선 줄은 면제 안 된다**(바닥에 낱말만 뿌려 검사를 끄는 길을 막는다). 이름 목록이면 새 파일마다 낡지만 이 규칙은 안 낡는다. 실저장소 위반 **30 → 0**: 살아있는 문서 5곳은 현행 정본(토큰 `재질.펠트.정본사진.평상복`)으로 되돌리고(브랜드킷 footer·오버레이 README·오버레이 html·생명감 설계 2곳), 판정 «기록» 10곳은 F659 관례대로 퇴역 단서를 그 줄 곁에 달았다(기록을 고치면 그 판정이 거짓이 되므로 글은 안 건드렸다). 🎉**오버레이 봇 그림 0/5 → 5/5 실물 대조** — 기쁨 칸은 펠트 정본에 없어 눈웃음으로 덮고 ⏳유호 판정 자리로 표시했다(놀람은 합성 후보라 같은 표시). 🔒**닫은 것 = 목록 갈라짐**: 구판 목록을 테스트가 손으로 들고 있었고(2개) `tools/lib/마스코트자산.js` 의 `퇴역폴더`(3개)와 **갈라져 `마스코트_정본` 이 검사에서 빠져 있었다** — 라이브러리에서 파생하도록 바꿔 퇴역 폴더가 늘면 검사가 따라간다(맹점 ④). 그리고 **같은 병을 내 픽스처에서도 잡았다** — 탐지력 픽스처가 매처를 복사해 갖고 있어 진짜 매처가 망가져도 초록이었다 → 실저장소 검사와 **같은 함수**(`위반줄`)를 쓰게 합쳤다. 변이 **7/7 구멍 0 · 미적용 0**(신호 2 · 면제 2 · 목록 파생 1 · 실물 위반 2 — 실물 변이 둘이 실저장소 검사를 빨갛게 만든 것으로 「진짜로 스캔한다」가 증명됐다). ⚠**대가(틀릴 때의 모습)**: 신호가 낱말·확장자라 **둘 다 없는 맨 포인터는 통과한다** — 예: `path.join(ROOT,'docs','캐릭터','마스코트_렌더')` 처럼 조각으로 쪼갠 코드 경로. 새는 방향은 언제나 「통과」다. 넓히려면 낱말을 늘리지 말고 **분모를 늘려라**(낱말을 늘리면 거짓양성이 나고, 거짓양성은 우회를 정상 통로로 만든다 F103). ▶**남은 축 ③ = `tools/` 층**: `마스코트램프.py`·`마스코트비율.py`·`마스코트속비우기.py`·`마스코트속수리.py`·`마스코트영상합성.py`·`마스코트정본.py`·`마스코트누끼.js` 일곱이 삭제된 폴더를 읽는다(F659 가 `재질굽기.js` 한 곳만 퇴역 처리했다) — 이번 커밋은 참조층만 잡았고 코드층은 안 건드렸다(워크트리 규약). 죽은 도구는 흐린 ENOENT 로 죽는다. |

> **후기 08-23** — 위 처분의 축 ①② 가드 `tests/마스코트정본.test.js` 는 그 뒤 `ecd555ea` 에서 **삭제됐다**(이 장부의 ✅ 는 그날부터 실물 없는 도장이었다 — 08-23 전수조사 실측 · DESIGN.md:48 만 부재를 알고 있었다). 같은 계열 **다섯 번째**(08-23 · talk 앱 컷 6벌)의 처방과 남은 축 정리: ①talk 컷 소각·펠트 4벌 교체 + `재염색_` 접두 회귀(talk `11c5848`) ②위 「남은 축 ③ tools/ 층」 중 `마스코트누끼.js` 는 08-23 현행화(유래 정직화 — 08-13 체리 실측이 경로 치환으로 거짓이 돼 있었다) ③참조층 잔존 재청소 = 생명감 설계 컷표·시연 줄, synk-content 캐러셀 3파일, 오버레이 유령 명세, 토큰 `_출처`/`정본사진.특별` 오염 — 전부 08-23 같은 커밋에서 수리.
