| F390 | 2026-08-12 | 마찰 | 채번기의 [vNEXT] 치환 범위가 «엔진 7파일»뿐인데 그 자리표를 검사하는 층은 «저장소 전체»라, 배포 파일 일부가 자리표를 안은 채 남는다 — 새는 방향은 「배포로 나감」이다.  ■ 실측 2026-08-12 (v9.223 배선 중) `bump-version.js --desc … --종류 보강` 이 「[vNEXT] → [v9.223] 확정: 엔진_콘텐츠AI.js×4, 엔진_수집.js×1」이라고 보고하고 끝났다. 같은 커밋에서 내가 [vNEXT] 를 적은 파일은 **넷**이었다 — 나머지 둘(`상담AI.js` ×3 · `엔진_두뇌.js` ×2)은 그대로 남았고, 테스트 파일 둘(`tests/safety.test.js` ×3 · `tests/옛글자런타임.test.js` ×1)도 남았다. 도구는 「확정: (두 파일)」이라고만 말하지 「나머지는 안 봤다」고는 말하지 않는다 — **미실행이 완료와 같은 모양**이다(F207 축).  ■ 왜 굵직한가 — 남은 둘이 «배포 파일»이다 `상담AI.js`·`엔진_두뇌.js` 는 clasp 로 라이브에 올라가는 파일인데 `ENGINE_FILES`(`tests/_engine-source.js` 의 7개 목록)에는 없다. 그 목록은 원래 「테스트가 엔진을 한 문자열로 본다」는 다른 목적의 것이고, 채번기가 그것을 치환 범위로 재사용하면서 **「엔진 파일」과 「배포 파일」이 같다는 전제**가 조용히 들어왔다. 두 집합은 실제로 다르다. 그리고 이 사고는 이미 한 번 났다 — **F309·F310**: 미커밋 `contents_상담AI.js` 의 [vNEXT] 판이 clasp push 에 실려 **라이브에 자리표가 박혔다.** 그때 진단은 TOCTOU(미커밋 유출)였는데, 이번 실측은 같은 파일군이 «커밋돼 있어도» 자리표를 남길 수 있음을 보여준다 — 원인이 둘인데 한쪽만 처방됐다.  ■ 지금은 무엇이 막고 있나 (그리고 그것으로 충분한가) `safety` 의 「실저장소에 [vNEXT] 자리표가 남아 있지 않다」가 저장소 전체를 훑어 **잡긴 한다**(이번에도 그게 잡았다). 즉 그물은 있다. 문제는 그물이 **적색으로만** 말한다는 것이다 — 채번기는 「확정했다」고 말하고 돌아가고, 몇 분 뒤 CI 가 빨개지며, 그 적색은 그 사이 **남의 배포 게이트까지 막는다**(F078 이 못박은 바로 그 비용). 처방을 아는 도구가 처방을 안 하고 남에게 적색을 전가하는 모양이다.  ■ 처방 후보 (1번째라 지금은 안 한다 — 기록만) ①치환 범위를 「엔진 7파일」이 아니라 **배포 집합**(`.claspignore` 허용목록 = clasp 가 실제로 미는 것)으로 바꾼다 — 검사 층과 같은 범위가 되어 갈릴 자리가 없어진다. ②최소한 **분모를 밝힌다**: 「N개 파일에서 M개 치환 · 훑지 않은 자리 0」을 찍고, 저장소에 남은 [vNEXT] 가 있으면 그 목록을 그 자리에서 준다(사람이 CI 를 기다릴 이유가 없다). ③범위를 못 넓히면 채번 성공 직후 `--check` 를 스스로 돌려 실패 시 비영으로 끝낸다(지금은 사람이 따로 부른다). 🚫 ENGINE_FILES 에 배포 파일을 더 넣기 — 그 목록은 「엔진을 한 문자열로 본다」는 다른 계약이라, 얹으면 테스트 195건의 구간 자르기가 같이 흔들린다. 두 목록은 갈라 둔 채 채번기가 «배포 집합»을 따로 읽어야 한다. | |
