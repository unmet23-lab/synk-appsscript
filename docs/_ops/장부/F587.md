| F587 | 2026-08-17 | 마찰 | 세션 종료 게이트(/close 3-b)가 형제 배포를 「리허설 한 판」만 재고 초록을 냈다 — 실측 같은 순간 운영은 낡음 1. F455 로 두 판을 한 번에 내는 장부 층(배포빚.js)이 생겼는데 그것을 부르는 자리가 안 따라가 분모가 낡은 채 남았다(맹점 ④ = 맞는 얼굴로 틀린 값). 한 판의 초록이 두 판의 초록으로 읽혀 「종결」이 거짓이 되던 자리. | 두 겹으로 닫았다. ①**도구가 스스로 말한다** — talk `bcd4d08`: 집계 줄을 순수 함수 `집계문()` 으로 뽑아 **자기가 본 판과 «안 본 판»을 함께** 낸다(`[배포대조] 리허설 — 다름 0 … / ⚠ 이 실행이 **안 본** 판: 운영 — 위 초록은 «리허설» 하나의 초록이다. 두 판을 한 번에: node tools/배포빚.js`). 회귀 3 신설 · 25/25 · 급소를 겨눈 것은 **「초록일 때도 경고가 나오는가」**다(초록일수록 위험한 자리). 과녁을 못 가른 경우(`판이름` null)는 filter 가 자연히 **둘 다** 「안 본 판」으로 낸다 — 모름을 통과로 접지 않는다. ②**호출부** — as `bfaf3dc1`: `.claude/skills/close/SKILL.md` 3-b 를 `배포빚.js` 먼저로 바꿨다(두 판·장부·네트워크 0) + 낡은 판이 있을 때만 바이트 실측 + 운영은 과녁 덮어쓰기(F462) + F579 「모름 안이 또 갈린다」. 🔑 **프로즈만 안 고친 이유**: 안 읽으면 안 돈다 — 도구가 말하면 돈다(CLAUDE.md 신뢰성). 🔑 **F083 재현으로 확인**: 신고 원문의 그 명령(`node tools/배포대조.js` 인자 없이)을 그대로 다시 돌렸고, 아까 「다름 0 · 미측정 0 · 전체 16」으로 끝나던 자리가 이제 안 본 판을 지목한다. 🔑 **F092 grep**: as 에서 `배포대조.js` 를 부르는 자리는 `/close` **하나뿐**(전수 확인 · 다른 호출부 0) — 문구만 고치고 사실이 남는 자리는 없다. ⚠**안 잰 것**: 변이 검사 0(형제 저장소에 `tools/변이.js` 가 없다) · 그날 배포 없이 끝난 세션이 이 구멍을 통과했는지는 **못 쟀다**(3-b 를 돌렸다는 기록이 없다 — 안 돌렸으면 명령을 고쳐도 안 막힌다). 닫은 것은 「명령이 두 판 중 하나만 본다」 하나고, 그건 실측으로 참이었다. |
