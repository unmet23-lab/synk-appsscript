| F590 | 2026-08-17 | 마찰 | ⑤ 되감기가 `git checkout-index -a -f` 를 못 본다 — `git checkout -- .` 와 같은 해악인데 통과다  실측 2026-08-17 (`local_dad32674` · F573 트랙에서 «지나가며» 발견 · 격리 픽스처 · 더러운 트리 `M a.txt`):   · `git reset --hard`          → DENY   · `git checkout -- .`         → DENY   · `git clean -fd`             → DENY   · `git checkout-index -a -f`  → **pass** ← 여기  ■ 무엇이 같은가 `checkout-index -a -f` 는 인덱스 내용을 작업본에 **강제로 덮어쓴다**. 즉 남의 미커밋 편집이 소멸하는 성질이 `git checkout -- .` 와 같다 — F037 실사고가 정확히 그 손해였다(옆 세션 미커밋 2파일 소멸).  ■ 왜 안 걸리나 ⑤의 판정은 `(checkout|restore)<끝>[^&|;]*?\s--\s+\.` 다. `checkout-index` 는 ①이름이 다르고(대시 서브커맨드) ②`-- .` 표기를 쓰지 않는다(`-a`·`--all` 로 전량을 고른다). 그래서 **경계 수리 전에도 후에도 똑같이 통과**다 — F573 수리가 만든 구멍이 아니라 원래 있던 구멍이고, 같은 실행에서 대조로 확인했다(옛훅 pass · 새훅 pass).  ■ 안 고친 이유 (지금 판정 · 미루기가 아니라 범위 밖) F573 트랙은 「서브커맨드 이름을 반토막 읽는다」 하나를 닫는 트랙이고, 이건 「⑤의 표기 목록이 좁다」다. 축이 달라 같은 커밋에 실으면 어느 쪽 회귀가 무엇을 지키는지 갈라진다. 그리고 실측 재료가 하나 없다: **이 저장소에서 `checkout-index` 를 실제로 쓴 이력이 있는지 안 셌다** — 쓰지도 않는 명령에 규칙을 붙이면 가드가 무거워지고, 규칙만 늘리고 라우팅을 안 넓히는 형태(CLAUDE.md 맹점 ③ 계열)가 된다.  ■ 다음 주자가 열 층 ① `git log -S"checkout-index"` · settings.json 권한 목록 · tools/ 전체 — 실사용 0이면 이 행은 「안 닫는다」로 판정한다. ② 실사용이 있으면 ⑤의 표기 축을 «전량 선택 플래그»로 넓힌다(`-a`·`--all`·`-f`·`--force` 조합) —    단 `checkout-index <경로>` 는 한 파일만 꺼내는 정당한 통로라 **경로 지정형은 통과**여야 한다(과잉 차단=BYPASS 학습). | ⑤의 판정축을 «표기»에서 «행위»로 바꿨다 — `.claude/hooks/git-scope-guard.js` 의 `덮어쓰기꼴` 신설(전량 pathspec·강제 플래그·checkout-index 전량·read-tree -u) + `tests/git범위가드.test.js` 회귀 3(소멸형 16 전량 · 과잉차단 12 · 깨끗한 트리 16)  ■ ⏸ 보류 판정(`local_f6457d2a` · 5e9f7f03 · 같은 날)을 «지운 것이 아니라 이어받는다»  그 세션이 먼저 판정했다 — 「구멍은 안 닫는다」. 근거는 `checkout-index` 의 **언급 10곳 / 실행 0곳** (git 이력 4건 전부 이 결함을 신고·수리·인계한 문서 자신 · 추적 파일 5 · 설정 4층 전부 0)이고, 그 실측은 **참이다**(내가 독립으로 다시 재서 같은 값을 얻었다). 뒤집힌 것은 사실이 아니라 **분모**다. 그 세션이 판정문에 스스로 남긴 도전안 — *「실사용 0은 곧 거짓양성 비용도 0이라는 뜻이라, 안전 가드에서는 오히려 «닫기 싼 신호»로 읽을 수도 있다」* — 이 아래 실측이 그것을 재료로 확증한 것이다. 🔑 두 판정은 어긋난 관측이 아니다. 그 세션은 **명령 하나**를 쟀고, 이 트랙은 **행위 부류**를 쟀다.  ■ 신고문의 판정 기준을 그대로 따랐더니 답이 뒤집혔다 — 분모가 틀렸다  신고문은 「①`git log -S"checkout-index"`·settings.json 권한 목록·tools/ 전체 — **실사용 0이면 이 행은 「안 닫는다」로 판정한다**」를 게이트로 뒀다. 그 실측을 먼저 했고 **실사용은 0이 맞았다**:   · `git log -S"checkout-index" --all` → 4건, 전부 이 신고 자신·F573 해소문·인계문(=장부 텍스트)   · 저장소 소스 히트 → 주석 1·픽스처 1·장부 2 (실행되는 자리 0) 그런데 그 기준을 적용하기 전에 **부류의 분모**를 셌더니 그림이 달라졌다. 재는 대상을 「그 한 명령」이 아니라 「추적 파일의 미커밋 편집을 소멸시키는 git 명령」으로 잡고 실훅에 태웠다(격리 픽스처 `M a.txt` = 잃을 것이 있는 트리 · 24칸 · 대조군 동봉):    📏 **소멸형 16건 중 막힌 것 5 · 구멍 11**  구멍 11 중 급소는 `checkout-index` 가 아니었다:   · `git checkout .`   ← `--` 없는 표기. 옛 판정 `(checkout|restore)…\s--\s+\.` 이 원리상 못 본다.   · `git restore .`    ← git 2.23+ 이 **스스로 권하는** 현대 표기.   · `git restore --worktree .` · `git restore --source=HEAD --staged --worktree .`   · `git checkout -f` · `git checkout --force <가지>` · `git switch -f` · `git switch --discard-changes`   · `git checkout-index -a -f` · `--all --force` (신고분)   · `git read-tree -u --reset HEAD` (plumbing 되감기)  🔴 그리고 그 둘은 **권한 프롬프트조차 없다** — `.claude/settings.local.json` 허용목록에    `Bash(git checkout *)` · `Bash(git restore *)` 가 있다. 즉 F037(2026-08-04 · 옆 세션 미커밋    2파일 소멸)이 지키려던 자리가, 사람도 가드도 안 보는 채로 가장 흔한 표기로 열려 있었다.    신고문이 잰 「실사용 0」은 참이지만 그 명령의 값이었고, 부류의 값은 정반대다.  🔑 일반형 = **가드의 구멍을 「그 한 명령을 쓰나」로 판정하면 부류를 놓친다.** 가드가 재는 것은 행위이고,    같은 행위에 표기가 여럿이면 실사용은 표기마다 갈린다 — 안 쓰는 표기 옆에 제일 흔한 표기가 서 있었다.    (CLAUDE.md 「부정 판정은 값이 살 수 있는 층을 먼저 열거하고 그 전부를 연 뒤에만 쓴다」 · F348 의 형태다.)  ■ 무엇이 실제로 막았나 (기계)  · `.claude/hooks/git-scope-guard.js` — `덮어쓰기꼴` 신설, `되감기꼴` 이 그것을 부른다. 네 갈래:     ⓐ 전량 pathspec `\s(?:--\s+)?(?:\./?|:/)(?=\s|$)` — `--` 유무를 안 가린다(옛 판정을 포함한다)     ⓑ 강제꼴 `--force|--discard-changes|-[a-z]*f[a-z]*` + `끝` — `checkout`·`switch`     ⓒ `checkout-index` 의 `-a|--all`     ⓓ `read-tree` 의 `-u`   긴 플래그 끝은 전부 공용 상수 `끝`(F573)으로 닫았다 — `\b` 로 적으면 `--force-with-lease` 가 샌다. · 예외 하나 = `인덱스만()` — `--staged`(=`--cached`) 가 있고 `--worktree` 가 없으면 통과.   인덱스만 되돌리는 것은 작업본 내용을 남기므로 잃을 것이 0이고, 막으면 정당한 언스테이징이   BYPASS 를 배운다(F103). 회귀가 `--staged --worktree` 조합은 여전히 막히는지 함께 본다. · 발동 조건은 안 건드렸다 — ⑤는 여전히 **트리가 더러울 때만** 운다. 깨끗한 트리 16/16 통과를   회귀가 못박는다(과잉 차단은 우회를 가르친다). · `tests/git범위가드.test.js` 회귀 3 신설, 셋 다 **분모를 같은 test 안에** 둔다(F207):     ①소멸형 16 전량 차단 + `assert.equal(소멸형.length, 16)`(목록을 줄여 초록을 못 만든다)     ②통과해야 할 12건 무차단 + 분모 2(`git checkout .` 은 여전히 막히나 · `--staged` 예외가       `--worktree` 까지 풀지 않나)     ③깨끗한 트리 16 전량 통과  📏 실측 = 회귀 108→111 fail 0 · **변이 7/7 구멍 0 미적용 0** · 수리 후 분모 16/16 구멍 0(오탐 0/13) 🔑 변이 첫 판은 **6/7** 이었다 — 「강제꼴의 경계를 `끝`→`\b` 로 되돌리기」를 아무 검사도 못 잡았다.    잡을 실물이 있었다: `git switch --force-create` 는 **진짜 플래그**이고 작업본 수정을 그대로 들고    가므로 통과가 정답인데, `\b` 로 적으면 `--force` 뒤의 `-` 가 낱말 경계라 막힌다(F573 그 형태).    그 한 줄을 픽스처에 넣어 7/7. 경계 상수는 상상 속 플래그가 아니라 **실존 명령으로** 못박힌다. ⚠ 곁가지 1건(고쳤다 · 회귀 파일 머리말): 변이 격리 사본을 **파일 하나만** 임시폴더에 두면 이 훅이    깊은 경로에서 지연 require 하는 `lib/handoff-store.js` 가 안 잡힌다. 증상은 「그 검사들만 빨감」이라    변이 결과로 오독하기 쉽다 — 이번엔 `변이.js` 가 「기준선이 이미 적색」으로 멈춰 세워서 그 판의    숫자를 한 개도 안 믿었다(그 게이트의 값이 정확히 이것이다). 처방 = **`.claude/hooks/` 를 통째로 복사.**  ■ 대가 (틀릴 때의 모습 · CLAUDE.md 「새 장치엔 대가를 함께 적는다」)  · pathspec 은 `.`·`./`·`:/` 만 센다 — `git checkout -- '*'` 이나 최상위 폴더를 손으로 나열한 형태는   **여전히 통과**한다. 새는 방향은 「통과」다. 여기서 멈춘 이유는 나열형이 「범위를 사람이 골랐다」는   뜻이라 ①③⑤가 공유하는 범위 원칙 안이기 때문이다 — 넓히면 `git checkout master` 류까지 흔들린다. · 닫을 것: **없다.** 옛 판정을 대체한 것이지 병렬로 늘린 것이 아니다(`덮어쓰기꼴` 이 옛 표기를 포함).   장치 개수는 ±0 이다. · 이 수리는 신고문의 ②(「실사용이 있으면 전량 선택 플래그로 넓힌다」)를 **집행한 것이 아니다** —   ①의 게이트가 「안 닫는다」를 가리켰는데 분모를 바꿔 뒤집었다. 그 판단 근거를 위에 남긴다. |
