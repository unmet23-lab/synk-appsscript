| F590 | 2026-08-17 | 마찰 | ⑤ 되감기가 `git checkout-index -a -f` 를 못 본다 — `git checkout -- .` 와 같은 해악인데 통과다  실측 2026-08-17 (`local_dad32674` · F573 트랙에서 «지나가며» 발견 · 격리 픽스처 · 더러운 트리 `M a.txt`):   · `git reset --hard`          → DENY   · `git checkout -- .`         → DENY   · `git clean -fd`             → DENY   · `git checkout-index -a -f`  → **pass** ← 여기  ■ 무엇이 같은가 `checkout-index -a -f` 는 인덱스 내용을 작업본에 **강제로 덮어쓴다**. 즉 남의 미커밋 편집이 소멸하는 성질이 `git checkout -- .` 와 같다 — F037 실사고가 정확히 그 손해였다(옆 세션 미커밋 2파일 소멸).  ■ 왜 안 걸리나 ⑤의 판정은 `(checkout|restore)<끝>[^&|;]*?\s--\s+\.` 다. `checkout-index` 는 ①이름이 다르고(대시 서브커맨드) ②`-- .` 표기를 쓰지 않는다(`-a`·`--all` 로 전량을 고른다). 그래서 **경계 수리 전에도 후에도 똑같이 통과**다 — F573 수리가 만든 구멍이 아니라 원래 있던 구멍이고, 같은 실행에서 대조로 확인했다(옛훅 pass · 새훅 pass).  ■ 안 고친 이유 (지금 판정 · 미루기가 아니라 범위 밖) F573 트랙은 「서브커맨드 이름을 반토막 읽는다」 하나를 닫는 트랙이고, 이건 「⑤의 표기 목록이 좁다」다. 축이 달라 같은 커밋에 실으면 어느 쪽 회귀가 무엇을 지키는지 갈라진다. 그리고 실측 재료가 하나 없다: **이 저장소에서 `checkout-index` 를 실제로 쓴 이력이 있는지 안 셌다** — 쓰지도 않는 명령에 규칙을 붙이면 가드가 무거워지고, 규칙만 늘리고 라우팅을 안 넓히는 형태(CLAUDE.md 맹점 ③ 계열)가 된다.  ■ 다음 주자가 열 층 ① `git log -S"checkout-index"` · settings.json 권한 목록 · tools/ 전체 — 실사용 0이면 이 행은 「안 닫는다」로 판정한다. ② 실사용이 있으면 ⑤의 표기 축을 «전량 선택 플래그»로 넓힌다(`-a`·`--all`·`-f`·`--force` 조합) —    단 `checkout-index <경로>` 는 한 파일만 꺼내는 정당한 통로라 **경로 지정형은 통과**여야 한다(과잉 차단=BYPASS 학습). | ⏸ 판정했다 — **구멍은 안 닫는다.** F590 이 스스로 세운 기준(「실사용 0이면 이 행은 안 닫는다로 판정한다」)을 그대로 적용했다. 실측 2026-08-18 `local_f6457d2a` — **언급 10곳 / 실행 0곳**(0을 분모와 함께 쓴다): ①git 이력 `git log -S checkout-index --all` = 커밋 4건인데 **전부 이 결함을 신고·수리·인계한 문서·가드·시험 자신**이다(c19ec6b7 F590 신고 · 5ba2a2b0·568238f1 F573 수리 · f9e556b4 인계문 수거) — 그 명령을 «쓴» 커밋 0. ②추적 파일 5곳 = `.claude/hooks/git-scope-guard.js:73`(대시 서브커맨드를 열거한 주석) · `docs/_ops/인계문.md` · 장부 F573·F590 · `tests/git범위가드.test.js:228` — 마지막 것은 「경로 지정형(`git checkout-index a.md`)은 통과해야 한다」를 못박는 픽스처라 오히려 **안 닫는 쪽 재료**다. ③설정 4층 전부 0 — `.claude/settings.json` · `.claude/settings.local.json` · `~/.claude/settings.json` · `~/.claude/settings.local.json`(없음) · `package.json` 없음. ④워크트리 히트는 위 파일들의 사본이다. ⇒ 이 저장소에서 `checkout-index` 를 **실행한 자리는 한 번도 없었다** — F590 의 ②(표기 축을 전량 선택 플래그로 넓히기)로 가지 않는다. ⚠ **도전안(안 채택 · 다음 주자 재료)**: 「실사용 0」은 곧 **거짓양성 비용도 0**이라는 뜻이라, 안전 가드에서는 오히려 «닫기 싼 신호»로 읽을 수도 있다 — 막는 손해는 F037 실사고(옆 세션 미커밋 2파일 소멸) 그대로이고, 새는 방향은 언제나 「통과」다. 게다가 이 가드가 막는 상대는 사람의 습관이 아니라 **AI 세션이 그때그때 고르는 명령**이라, 과거 사용 0이 미래 사용 0의 근거로는 약하다. 그래도 안 닫은 이유는 **신고자가 세운 판정 기준을 새 근거 없이 뒤집지 않기** 위해서다. 뒤집을 근거 = 이 명령을 실제로 시도한 세션이 한 번이라도 잡히는 날(그때는 규칙 한 조각 추가 + 경로 지정형 통과 픽스처가 이미 서 있어 값이 싸다). |
