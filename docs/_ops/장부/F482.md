| F482 | 2026-08-15 | 마찰 | 🚀 배포뒤처짐 경보가 라이브=HEAD 인 날에도 매 세션 뜨고, 그 경보가 인쇄해 주는 처방(배포판점검 --라이브)을 그대로 돌려 초록을 받아도 안 꺼진다 — 재우는 도장을 만드는 자리가 rot-check(하루 1회) 하나뿐이라 수동 실측은 도장을 안 남긴다. 대기열 #Q96. | tools/배포판점검.js 도장찍기() — 잰 자리(--라이브)에서 배포실측 키만 병합(최상위 at 불변) + /deploy 7단계가 --라이브 로 부른다. 회귀 tests/배포도장.test.js 9건·변이 6/6(신고 원문 그대로인 «CLI가 도장을 안 찍는다» 변이가 포획됐다). 실측: --라이브 도장 2/2 → 직후 훅의 🚀 절 소멸. |
