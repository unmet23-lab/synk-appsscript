| F348 | 2026-08-12 | 실수 | 설계문의 실측 주장을 검증하면서 «한 층만 열고» 통과시켰다 — 「직접 열어 쟀다」가 「전 층을 쟀다」와 같은 모양이 된다  상태기반 과제선택 설계 v2 재심문 트랙(세션 d9336f79). v2 §4-1 이 「`출처` 는 지금 어디에도 안 착지한다」고 적었고, 나는 그것을 검증한다며 `deliver` 의 `task.assigned` INSERT 를 직접 열어 `payload = {ver:1}` 하나뿐인 것을 보고 **✅ 정확** 으로 통과시켰다. 유호님께 낸 중간 보고에도 「맞은 주장」 표에 넣어 올렸다.  틀렸다. 같은 트랜잭션 30줄 아래에 `insert into engine.submissions (… task_snapshot …)` 이 있고, 그 스냅샷의 `호흡[]` 각 칸에 `출처` 가 들어 있다(`오늘과제.js:172`·`:218` 호흡 8칸 목록). 즉 **`출처` 는 이미 착지한다.** 심문 sol 회차가 단독으로 이걸 잡아 「v2 의 실측이 틀렸다」로 냈고, 그 지적이 옳다. 내가 못 잡았으면 v3 는 payload 에 `출처` 를 또 만들어 **이중 정본**(학생이 본 스냅샷의 출처 ≠ 분석용 payload 의 출처)을 계약에 박았을 것이다.  ■ 왜 「간접 증거로 대신했다」가 아닌가 — 이건 다른 병이다 CLAUDE.md 는 두 축을 이미 갈라 뒀다: 「현재 상태는 현재를 읽어 판정한다」(문서·목록으로 대신하지 말고 직접 열어라)와 「잴 수 있는 건 잰다 — 단 무엇을·어느 층을 재는지부터 정한다」(F089·F183). 나는 첫째를 지켰다 — 실제로 코드를 열었다. 어긴 것은 둘째다. **어느 층을 재야 하는지를 안 정하고 먼저 열었고**, 처음 연 층에서 답이 나오자 거기서 멈췄다.  🔑 급소: 「직접 열었다」는 사실이 **멈춤의 알리바이가 된다.** 문서로 때웠으면 스스로 미심쩍지만, 코드를 열어 눈으로 본 값은 결론처럼 느껴진다. 그래서 이 실수는 게으를 때가 아니라 **성실할 때** 난다. 실제로 나는 이 세션에서 v2 주장 12건을 줄번호까지 대조하며 쟀고, 그 성실함의 한복판에서 이 한 건이 통과했다.  ■ 이 자리의 형태 — 「없다」는 주장은 「있다」보다 층을 하나 더 요구한다 `X 가 있다`는 한 자리를 찾으면 증명된다. `X 가 어디에도 없다`는 **X 가 살 수 있는 자리를 전부** 세어야 증명된다. v2 의 문장은 「어디에도」였는데 나는 한 자리만 보고 부정을 확인했다고 적었다. 사건 payload / 사건 열 / 스냅샷 / 별도 표 — 이 저장소에서 값이 착지할 수 있는 층은 최소 넷이다.  ■ 처방 (이 트랙에서 이미 집행) - 처분표 v2 §1 에 정정을 실측표 옆에 적었다(⚠ 표시 · 「처분 세션도 처음엔 한 층만 보고 ✅로   통과시켰다」). 커밋 `da199556` 메시지에도 같은 문장을 남겼다 — 조용히 고치면 다음 세션이   같은 자리에서 같은 방식으로 다시 통과시킨다. - 메모리 `task-selection-by-state` 에도 남겼다.  ■ 규칙 후보 (지침 개정 재료 — 지금 개정하지 않는다) 「**「어디에도 없다」는 판정은 값이 살 수 있는 층을 «먼저 열거하고» 그 전부를 연 뒤에만 쓴다.** 한 층에서 못 찾은 것은 「그 층에 없다」까지만 적는다.」 — 이 저장소에서 그 층 목록은 최소 넷이다(사건 payload · 사건 열 · `submissions.task_snapshot` · 별도 표). ⚠ 스캐너·CI 회귀로 만들 물건이 아니다(「없음」의 층 목록은 대상마다 다르다). 프로즈 규칙 자리다. | ⏸ 이 트랙(F350 처방)이 «한 축만» 덮었다 — 문서가 대는 file:line 인용은 tools/인용검사.js 가 기계로 잰다(cb0732e9). 하지만 F348 의 본질은 「«어디에도 없다»는 판정은 값이 살 수 있는 층을 먼저 열거하고 그 전부를 연 뒤에만 쓴다」는 사람 규칙이고, 그 층 목록은 대상마다 달라 스캐너로 못 만든다(F348 자신이 그렇게 적었다). 프로즈 자리라 /evolve 몫으로 남긴다. |
