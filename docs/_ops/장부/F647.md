| F647 | 2026-08-18 | 실수 | 정규식 `\b` 단어경계가 한글 앞뒤에서 안 생겨, 「고쳤다」가 조용히 안 고쳐진다 — 한 트랙에서 3번 밟았다(2026-08-18 ④ 지면 배선). JS 의 `\w` 는 ASCII 라 `룸`·`얹힘`·`사유` 같은 한글 토큰 옆에는 경계가 없다. 그래서 `\b룸\b` 는 `class="룸"` 에도 안 물리고, `r\.사유\b` 는 `${r.사유}` 를 못 바꾼다. 실측 3건: ①`sed`로 `r.얹힘`·`r.훅`·`r.사유`를 바꾸려다 전부 실패 — **구문검사는 통과**하고 발표물 Loom 이 3벌→0벌로 퇴행했다(값만 틀리는 형태) ②`loom얹기.범위진단()` 의 `class="[^"]*\b룸\b"` 가 `.룸` 이 있는 지면에도 「없다」를 내 거짓양성 ③`중성축_뒷정리.js` 의 구 이름 잔재 검사가 같은 축에서 흔들렸다. 새는 방향이 셋 다 다르다 — ①은 「안 바뀜」, ②는 「거짓양성」, ③은 「거짓음성」. 공통 원인은 하나고, 어느 쪽도 크래시가 아니라 **맞는 얼굴로 틀린 값**이다. 해소: `tools/lib/loom얹기.js` 에 `클래스있나(html, 낱말)` 를 두고 경계를 «찾지» 않고 속성값을 공백으로 쪼개 정확히 견주게 했다(회귀 `tests/loom얹기.test.js` ⑦ — `class="룸통"` 은 false 여야 한다까지 못박음). 코드 치환은 `sed` 정규식 대신 `Edit` 도구로 원문을 그대로 집는다. 남은 구멍: 저장소의 다른 정규식이 한글 토큰에 `\b` 를 쓰는지는 아직 안 셌다 — 세는 장치가 없다. | tools/lib/loom얹기.js 의 클래스있나() — 경계를 찾지 않고 class 속성값을 공백으로 쪼개 정확히 견준다 · 회귀 tests/loom얹기.test.js ⑦(class="룸통" 은 false) |
