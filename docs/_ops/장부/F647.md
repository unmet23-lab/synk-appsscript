| F647 | 2026-08-18 | 마찰 | 클라우드 세션이 대장을 다시 구우면 «커밋된 실측»이 «못 쟀다»로 덮인다 — 그리고 test-ci 가 그 굽기를 스스로 돈다. 실측 2026-08-18(클라우드 세션 · 형제 저장소 SYNK-talk 없음): 판정 재료를 모으려고 `node tools/이해대장.js` 를 돌렸더니 `docs/이해대장.html` 의 엔진 도달 절이 통째로 바뀌었다 — 커밋본은 「도달이 0인 사건 3종 + 손댈 것 0 · 빨간 칸 5 = 지금 1 · 학생 2 · 설계상 2」라는 **실측값**을 들고 있는데, 이 환경의 굽기는 그 자리를 「⚠ 못 쟀다 — 형제 저장소를 못 읽었다」로 바꿔 썼다. 도구의 화면 출력은 정직하다(「빚 0 이 아니라 안 재봤다」를 제대로 말한다) — 새는 자리는 **출력이 아니라 파일**이다: 못 잰 세션이 잰 세션의 값을 덮어쓰고, 그 파일은 git 에 들어간다. 🔑 위험한 건 이 한 번이 아니라 **자동성**이다 — `node tools/test-ci.js` 가 같은 굽기를 자기 런에서 돌리고(그 런의 알림: 「이 런이 스스로 다시 구운 산출물 2벌 … docs/이해대장.html」), test-ci 는 배포·트랙 종료 전에 **반드시** 도는 통로다. 즉 클라우드 세션이 규약대로 test-ci 를 돌린 뒤 `git add` 범위를 넓게 잡거나 대장 파일을 함께 담으면, **아무도 의도하지 않은 채 실측 퇴행이 master 로 간다.** 그리고 그 퇴행은 「빨간 칸이 사라진」 모양이라 화면에서 **좋아 보인다**(F348 의 파일층 판 — 부정 판정이 값을 덮는다). 이번엔 `git status` 전문을 읽고 되돌려 안 담았다. ⏸ 안 고쳤다 — 처방 후보는 ①굽기가 「못 잰 축」이면 **그 절만 기존 파일에서 보존**하고 덮지 않는다(fail-closed · F641 이 인계문에서 쓴 것과 같은 축) ②재료가 없으면 **파일을 아예 안 쓴다**(화면에만 낸다) ③생성물을 git 밖으로 — 인데, 어느 쪽이든 「어느 절이 형제 저장소 의존인가」를 도구가 스스로 알아야 해서 그 층을 먼저 정해야 한다. 지금 장치를 지으면 또 하나의 맞는 얼굴로 틀린 값이 된다. | ⏸ 지금 값은 맞지만 구조가 남아 안 닫는다 — 실측 08-22: `docs/이해대장.html` 의 «⚠ 못 쟀다» **0건** · 형제 저장소 SYNK-talk 존재 · 지금 도는 세션 셋이 전부 로컬이라 이 사고의 전제(형제를 못 읽는 클라우드 세션)가 지금은 없다. 그러나 **처방 ①(못 잰 절은 기존 파일에서 보존하고 덮지 않는다)은 안 들어갔다** — `tools/이해대장.js` 는 여전히 못 잰 축을 「⚠ 못 쟀다」로 **파일에 써서** 커밋된 실측을 덮을 수 있고(:511·:525·:564), `test-ci.js` 가 그 굽기를 자기 런에서 돈다는 통로도 그대로다. 안 짓는 까닭은 신고문이 스스로 적은 것과 같다 — 「어느 절이 형제 저장소 의존인가」를 도구가 알아야 하는데 그 층을 아직 안 정했고, 지금 지으면 또 하나의 «맞는 얼굴로 틀린 값»이 된다. **다시 볼 조건**: 클라우드 세션이 이 저장소에서 다시 돌 때 · 또는 이해대장.html 에 「못 쟀다」가 커밋으로 들어온 것이 관측될 때. |
