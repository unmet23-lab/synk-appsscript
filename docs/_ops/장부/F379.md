| F379 | 2026-08-12 | 실수 | F351 두 번째 실물 — 옛글자 규칙이 커밋 때 안 발동한다. 08-12: talk prompts/교정.md 에 「한자·가나 금지」 규칙을 넣으면서 그 보기로 U+52A9·U+8A5E 를, 이력 인용으로 U+683C 를 썼다. 그 파일은 전문이 그대로 모델 system 에 실리므로(lib/교정엔진.js) 문서 흠이 아니라 모델 입력에 금지 문자를 넣은 것이다. 커밋(10e63dd)은 조용히 지나갔고 as 저장소 test-ci 를 한참 뒤에 돌려서야 잡혔다 — F351 이 예고한 「남이 커밋한 뒤 발견」 모양 그대로다. 새 사실 둘: ①규칙의 «보기»가 그 규칙을 어기는 형태라 사람 눈에 특히 안 보인다(고치는 문장에서 같은 글자를 또 인용했고, 채팅에서도 또 해서 chat-glyph-guard 가 막았다 — 연속 3회) ②talk 세션은 as 의 test-ci 를 안 돌리므로 그 층에서는 원리상 안 잡힌다. 후보 처방은 F351 그대로(pre-commit --cached) + 검사 대상에 「모델 입력이 되는 파일」을 이름으로 넣기. 판정은 /evolve·유호님. | 이 행이 F351 에 더한 «새 사실 ②» 가 처방의 자리를 정했다 — talk 세션은 as 의 test-ci 를 안 돌리므로 그 층에서는 원리상 안 잡힌다. 그래서 as 만 고치면 이 행은 안 닫힌다. 처방: talk tools/precommit.js 가 형제(as)의 tools/옛글자검사.js --cached 를 부른다(talk 6fc5b93 · 옆 계약 게이트와 같은 축). 판정을 talk 에 복사하지 않는다 — 두 벌은 반드시 갈라지고 갈라지는 방향은 언제나 「통과」다. 형제가 없는 기계에서는 막지 않고 「못 했다」고 말한다(F103). 성공했을 때의 stderr 도 흘린다 — 그 한 줄이 분모다(몇 개를 열었나 · F207). execFileSync 는 성공 시 stdout 만 돌려줘 그 말을 버리는데, 그러면 talk 에서 「돌았는데 깨끗」과 「안 돌았다」가 같은 모양이 된다 — 이 행의 병이 정확히 그거였다. spawnSync 로 바꿨다. 새 사실 ①(규칙의 «보기»가 그 규칙을 어기는 형태라 사람 눈에 특히 안 보인다)은 사람 눈을 안 믿는 쪽으로 닫혔다: 커밋 층이 기계로 본다. 곁들여 프롬프트 v4 가 그 3자를 뺐고 A/B 로 옛글자 혼입 2/101 → 1/102(앞선 다섯 판 기저 0·0·0·1·0 으로 복귀 · talk 10316a9). F083 재주입: 이 행이 적은 원문 그대로(prompts/교정.md 에 U+52A9·U+8A5E·U+683C) 넣으니 진짜 git commit 이 세 자를 다 짚어 막았다. 회귀 talk tests/precommit.integration.test.js 3건 + as tests/옛글자훅.test.js 15건. |
