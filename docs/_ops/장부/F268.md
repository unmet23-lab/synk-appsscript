| F268 | 2026-08-09 | 실수 | 테스트 550/550 초록인데 앱 첫 화면이 실기기에서 튕겼다 — 선언 없는 이름(useState(오늘))을 아무 층도 못 봤다. 구문검사는 문법만, 나머지 테스트는 순수 함수만 부르고 화면을 그리지 않으며, Metro 번들링도 모듈 해석만 해 미정의 참조는 런타임까지 산다. 「초록은 분모와 함께 읽는다」의 새 형태 — 분모가 0 인 축(화면)이 있는데 총계가 그걸 안 드러냈다. | tests/미정의심볼.test.js — @babel/parser(metro 가 이미 깔아 둔 것, 새 의존성 0)로 JSX 를 읽어 선언 없는 참조를 잡는다. 실저장소 30파일은 거짓양성만 검사하고 탐지력은 픽스처 2개가 진다. 검사한 파일 수를 함께 낸다(F207). |
