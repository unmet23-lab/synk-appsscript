| F260 | 2026-08-08 | 마찰 | 정본 문서의 「발전시킬 것」 절이 이미 선 것을 계속 가리켜 새 세션이 그것을 트랙으로 잡는다 — 실측 2026-08-08: 트랙 없이 시작한 내가 유호님 상시 지시(수집=엔진 도달까지 한 벌)에 직결된 자리를 찾다가 `docs/_ops/엔진도달_전수감사.md` §6-1 「도달 장부 — 지금은 그 상태가 초록이다. 가장 큰 지렛대이고 가장 싸다」를 유일한 미실행 처방으로 읽고 착수 직전까지 갔다. 실제로는 그 장부가 talk `b66cd6c` 로 이미 섰고 교차 불변식 ②까지 붙어 있었다 — 코드를 열어 재고 나서야 알았다. 새는 방향은 오류가 아니라 「아직 할 일이 있다」는 조용한 오독이고, 안 재고 믿었으면 F070(같은 것을 두 세션이 따로 구현)이 됐다. F256(보드 종결 줄의 「▶다음=」)·F115(결정 큐 낡은 항목)와 같은 병인데 층이 **정본 문서**다 — 세 번째 층이라 개별 가드를 하나 더 짓는 것은 답이 아니다(CLAUDE.md 3번째 = 원인을 쓸 수 없게 만든다). 공통 원인: 「할 일」을 적는 자리가 셋인데 **끝났음을 그 자리에 되돌려 적는 통로가 어디에도 없다**(끝낸 세션은 자기 보드 줄과 커밋에만 적는다). 처방 후보: ①doc-graph 가 이미 정본→코드 엣지를 아는데(F243) 그 반대로 「처방 문장이 지목한 산출물이 실재하면 그 줄을 낡음으로 띄운다」 ②「발전시킬 것」 절에 산출물 좌표(파일·심볼)를 필수로 적게 하고 실재 검사를 건다 ③최소한 트랙 착수 전에 그 문장이 지목한 코드를 여는 것을 규약으로 못박는다(지금은 내 습관에만 있다). | 이번 판은 ③을 손으로 밟아 막았다(착수 전 코드 실측 → 낡음 발견 → §6-1 정정 커밋). 기계 통로는 없다 — 처방 후보 3개를 유호님 판정으로 올린다. |
