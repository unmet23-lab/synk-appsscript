| F516 | 2026-08-16 | 실수 | 답이 도착했는데 결과 파일의 「## 답」 칸엔 «못 찾았다» 가 앉았다 — F512 와 같은 병이 한 층 아래 또 있었다 (실측 2026-08-16 · `local_b489bd18`)  ■ 어떻게 드러났나 F512(상태 판독)를 고쳐 이 통로의 결과 파일을 **처음으로** 받아 봤다. 그런데 사람이 읽는 「## 답」 칸이 `_(assistant 메시지를 못 찾았다 — 아래 원문을 본다)_` 였다. 답은 파일 안에 있었다 — 원문 JSON 덤프 속에만.  ■ 원인 assistant 메시지는 `{ type:'assistant_message', assistant_message: { content: '…' } }` 인데 옛 코드는 `m.content || m.text || m.message` 를 훑었다. 타입 필터는 통과하고 본문만 전부 빈 문자열이 돼 `filter(Boolean)` 에서 사라졌다. 그래서 「메시지가 없다」가 아니라 「본문을 못 읽었다」인데 파일엔 전자로 적혔다.  ■ 왜 같은 병인가 F512 와 **한 글자도 안 다르다**: 벤더 응답의 필드명을 재지 않고 후보로 나열했고, 나열은 전부 빗나가도 아무 소리를 안 낸다. 한 응답에서 두 번 났다 — 상태는 「도는 중」으로, 본문은 「메시지 없음」으로. 두 번 다 새는 방향이 조용한 쪽이었다.  ■ 교훈 (F512 에 더한다) 같은 응답을 읽는 자리가 여럿이면 **한 자리를 고칠 때 나머지도 같이 잰다.** 하나가 필드명을 추측했다면 나머지도 십중팔구 같은 손으로 쓰였다. 그리고 이 커밋 짝에서 **변이가 같은 종류의 구멍을 두 번** 냈다(장치는 더했는데 「그 장치를 실제로 쓰는가」를 안 봤다 — 정의 줄에 맞는 정규식·호출부 미검사). 회귀에 「있다」와 「쓴다」는 다른 검사다. | tools/마누스.js 말본문(m) — 실측 자리(assistant_message.content)를 1순위로, 옛 후보는 뒤로(되던 것 무파손). 조각 배열은 펴서 잇고, 문자열이 아니면 빈 문자열을 낸다(String(객체)의 [object Object] 는 못 찾음보다 나쁘다 — 틀린 값이 맞는 얼굴로 앉는다). 회귀 3 신설 + ⑱ 발화검사에 호출부·옛식금지 증보 · 변이 5/5 구멍 0 · 라이브 실측: 결과 파일 18,885→29,323B 로 답이 들어왔다 — ba3661fb |
