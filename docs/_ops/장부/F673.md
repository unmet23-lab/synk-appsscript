| F673 | 2026-08-19 | 마찰 | **가두는 프리셋이 남의 지면 «변수»를 덮어썼다** — F652 와 같은 계열의 다른 문. 가두는 프리셋은 `바탕` 을 안 실어서 「쓰는 색만」 `:root` 에 채우는데, 그 이름이 `--ink`·`--coral`·`--font` 처럼 **흔한 낱말**이라 같은 이름을 쓰는 지면에 얹으면 Loom 블록이 뒤에 와서 **그 지면 변수를 통째로 덮는다.** 범위(`.룸`)는 «선택자»를 가뒀지만 «변수 이름»은 안 가뒀다. 실측: 어두운 캐러셀에 얹자 Loom 의 `--ink:#1B1B1A`(라이트용)가 지면의 `--ink:#E4E4E7`(Chalk)를 덮어 **제목·인용이 어두운 판 위에서 사라졌다.** 🔑22벌에서 여태 안 터진 이유는 «값이 우연히 같아서»다 — **안 터진 것은 안전의 증거가 아니다.** | ✅ `이름가두기()` — 가두는 프리셋의 **라틴** 변수만 `--룸-*` 로 바꾼다(선언·참조를 함께). 한글 이름(`--실`·`--숨`)은 그대로(이미 사실상 이름표). 통짜 프리셋은 무변경(자기가 곧 판). 안전 근거를 재서 확인: 가두는 프리셋이 참조하는 라틴 변수는 **전부 자기가 선언한다**(부품만 4/4 · 인쇄부품 9/9 · 「선언 안 된 참조」 0)이라 끊기는 고리가 없다. |
