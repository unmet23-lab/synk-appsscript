| F562 | 2026-08-17 | 마찰 | 고아 index.lock 처방이 «자기가 만든 사건»에서 원리상 도달 불가다 — ②의 조건이 영영 안 켜진다. 실측 2026-08-17 세션 local_a601a510: 세션 시작 훅이 `index.lock 26분째 — 🔴 고아 의심`을 옳게 냈다(F493 의 나이 게이트는 제 일을 했다). 그 다음이 막혔다. 처방은 ①`Get-Process git` 으로 도는 git 이 없나 본다 ②**없으면** 치운다 인데, `Get-Process git` 은 PID 24364 를 돌려줬다 — 살아있는 git 이 있으니 ②의 조건이 거짓이고, 지침대로면 기다리는 것 말고 할 게 없다. 그런데 그 24364 를 열어 보니 답이 정반대였다: 명령줄이 `git diff --cached --name-only -z --diff-filter=ACMR` 로 **이 저장소 `tools/githooks/pre-commit` 22행이 띄운 자식**이고, 부모 PID 30384 는 이미 죽었고, CPU 는 5초 창에서 0.0000초 증가(26분 누적 0초·스레드 1·WS 1.9MB)였다. 죽은 파이프에 막혀 영원히 사는 고아다 — 저 혼자로는 절대 안 끝난다. 즉 잠금을 만드는 주체가 `git commit` 이고 그 훅이 자식 git 을 띄우는 구조라, **고아 잠금 사건에서는 「도는 git」이 거의 언제나 하나 이상 남는다.** 처방 ①이 참을 답하는 바로 그 순간 ②가 영구히 거짓이 되는, F103(따를 수 없는 처방) 그 자리다. 모듈 89행이 `읽기 전용 CLI — 처방 ①이 실제로 실행 가능해야 한다(F103)` 라고 스스로 경계해 둔 자리인데, ①을 **실행 가능**하게만 만들고 **판정 가능**하게는 안 만들었다. 새는 방향이 나쁘다: 「기다려라」라서 조용하고, 조용한 채로 저장소 전체의 커밋이 계속 0이다(F493 실측 9시간 8분·그 창의 커밋 0건이 같은 병의 앞판). 대가는 더 나쁘다 — 처방이 도달 못 하면 사람은 결국 **확인 없이** 지우게 되고, 그건 🚫자동삭제를 금지하며 피하려던 바로 그 위험(진짜 도는 git 을 죽이는 비가역)을 기계 대신 사람 손으로 하는 것이다. 가름 재료는 이미 다 실측 가능했다: 부모 생사 · CPU 증가분 · 잠금 mtime 과 프로세스 시작 시각의 일치(둘 다 19:24:59 로 초 단위까지 같았다). 진짜 일하는 git(대형 checkout·gc)은 부모가 살아 있고 CPU 를 태우므로 이 셋이 정확히 갈라낸다. 덤으로 F493 토픽이 열어 둔 ⚠②(왜 잠금이 생겼는지 재현이 없어 미특정)에 첫 답이 생겼다 — pre-commit 훅의 파이프라인 자식이 부모 사망 시 고아로 남아 잠금을 문다. 처방은 지우는 자동화가 아니라 **가름의 자동화**다: CLI 가 도는 git 들을 열어 「고아다(치워도 된다)」·「살아있다(무엇을 하는 중이다·기다려라)」·「못 가렸다」 셋으로 답하게 한다. 🚫자동 삭제는 그대로 금지 — 도구는 여전히 안 지운다. | **첫 판정**(`local_61375a0b` 2026-08-18 · 커밋 `e6ab500`): 이미 고쳐져 있었는데 장부만 열려 있었다 — 착지 93b321a1 (PR #68 머지 · 보드 a601a510 종결). 신고의 급소는 「②의 조건이 영영 안 켜진다」였고, 그 커밋이 «가름»을 자동화해 처방을 판정 가능하게 만들었다(F103 계열 · 도는 git 이 남아 있어도 고아인지 가른다). ▸ **복구 주석 — 이 첫 판정은 한 번 지워졌다가 되살렸다.** `local_7dfdcffd` 가 **낡은 판 위에서** `friction.js resolve` 를 돌려 이 칸을 통째로 덮었고(`d15c1550`), 머지에서 그쪽이 남아 첫 판정이 HEAD 에서 사라졌다. 장부는 행 삭제 금지인데 **칸 덮어쓰기로 같은 손해가 났다**. 🔑 원인은 도구가 아니라 순서다 — `friction.js resolve` 는 **이미 해소된 행을 실제로 거부한다**(복구하려 다시 부르니 거부당해 확인했다). 그 가드가 안 선 이유는 그것이 **로컬 파일**을 읽는데 내 판이 낡아서 그 해소가 거기 없었기 때문이다. 같은 뿌리로 `friction.js --open` 이 낸 「열림 14건」도 낡은 목록이었다 — F562 는 그 시점에 이미 닫혀 있었다. 즉 **「열려 있다」는 관측이 아니라 낡음이었다.** ⚠ 세션 시작 뒤 `repo-staleness` 훅이 「내 판이 origin/master 보다 1 커밋 뒤다」라고 **경고했는데 그대로 안 했다** — 장부를 읽어 판정하기 전에 fetch 가 선행이다(이 저장소는 커밋 간격 중앙값 1분이라 «읽은 목록»이 곧 낡는다). ⇒ 일반형: **append-only 장부의 보호는 「행을 지우지 마라」로 부족하다 — 낡은 판에서 쓰면 칸 단위로 같은 일이 난다.** ▸ **실측 추가**(`local_7dfdcffd` · 첫 판정을 뒤집지 않는다 — 같은 결론에 라이브 근거를 보탠다): 해소 확인 2026-08-18 `local_7dfdcffd` — **처방대로 이미 지어져 있었고, 장부만 열린 채였다.** 고친 자리 = `93b321a1` 「고아 잠금 처방이 «자기가 만든 사건»에서 도달 불가였다 — 가름을 자동화한다 (F562)」(신고자 세션 `a601a510` 자신 · origin/master 조상 확인). 그 뒤 `095ee434`(`41cc57e7`)가 그 안의 P1 하나를 더 고쳤다 — 첫 표본이 비고 둘째에 git 이 있는 회차를 「없음」으로 접어 **잠금을 치우라고 권하던** 자리다.  ■ 처방과 실물 대조 — F562 가 요구한 세 갈래가 그대로 섰다(`tools/lib/git잠금.js`) · `프로세스판정()` 이 「고아」·「살아있다」·「못가렸다」 + 「없음」으로 답한다. 고아 판정은 **㉠부모 죽음 ∧ ㉡CPU 증가분 멎음**을 «둘 다» 요구하고, 어긋나면 「살아있다=기다려라」로 떨어진다. · 🚫자동 삭제는 지켜졌다 — 어느 갈래도 지우거나 죽이지 않고 명령을 사람에게 내밀 뿐이다(`가름문()`). · `if (require.main === module)` CLI 가 실제로 그 질문에 답한다 — F562 가 「①이 참을 답하는 순간 ②가 영구히 거짓이 된다」고 적은 그 막다른 길이 없어졌다.  ■ 내가 실측한 것 (인용이 아니라 다시 쟀다) · **라이브 실행** 2026-08-18 12:57 — 이 세션 시작 훅이 정확히 이 병으로 실패했다(`인계문수거` = `index.lock: File exists`). 그 판에서 재니 산 세션 8개가 붙어 `git.exe` 10+ 가 동시에 돌았다 — 옛 처방의 `Get-Process git` 이 **언제나** 참을 답하던 바로 그 조건이다. `프로세스판정()` 은 `살아있다`(산것 4 · 고아 0)를 냈고 그 잠금은 잠시 뒤 스스로 풀렸다 — **옳은 판정이다**(고아로 안 몰았다). · **회귀 재실행** — `tests/고아잠금.test.js` 36 fail0 · `tests/보드가지.test.js` 21 fail0.  ■ 닫으면서 남기는 정직한 한계 둘 · **고아 갈래는 라이브로 못 봤다** — 그 순간 진짜 고아가 없었다. 그 갈래를 지키는 것은 픽스처뿐이다(회귀에 있다). 「살아있다」 갈래만 실물로 확인했다. · **`명령줄` 이 안 읽히는 회차가 있다** — 라이브 4건 전부 `(못읽음)` 이었다. 도구는 그 문자열을 고아 갈래에서 「오진의 유일한 안전장치」라 부르므로 처음엔 구멍으로 의심했는데, **아니다**: 그 4건은 전부 `새로뜸`(둘째 표본에만 뜬 찰나 프로세스)이라 설계상 **고아 갈래에 못 간다**(무조건 산것). 진짜 고아는 수십 분을 앉아 있어 두 표본에 다 잡히고, 같은 CIM 조회로 장수 프로세스의 명령줄은 정상적으로 읽혔다(같은 판 raw 조회 10건 중 8건). ⚠ 다만 이건 «지금 판에서 관측된 것»이지 불변식이 아니다 — 고아인데 명령줄까지 못 읽는 회차가 나오면 그 안전장치는 빈 채로 「치워도 된다」가 나간다. 그때 여는 층 = `프로세스줄()` 이 명령줄 부재를 판정문 머리로 올리는 것(지금은 목록 안 한 줄이다).  ■ 곁: 이 행이 열린 채였던 이유가 F582·F589 계열이다 — 고친 세션도 그 안의 P1 을 고친 세션도 죽었고, 죽은 줄이 `tools/lib/git잠금.js` 를 잠가 다음 세션이 원리상 못 집었다. 그 줄은 이 판정과 함께 이관했다(근거: 커밋 095ee434 · 회귀 두 벌 재실행). |
