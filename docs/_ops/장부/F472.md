| F472 | 2026-08-15 | 실수 | L0 재질 토큰의 `울패치` 블록이 집행 코드와 «다른 것»을 말한다 — 라벨대로 새 코드를 짜면 눈알이 섞인 타일이 나온다. Loom L2b 패치 라이브러리 착수 첫 크롭에서 실측(맹점 ④ = 맞는 얼굴로 틀린 값).  갈래 ㉠ 「원점」인데 실제로는 「중심」: 토큰 `재질.펠트.울패치.원점 = [510,320]`. 집행 코드 `tools/마스코트인형리깅.py:167-168` 은 `x0 = cx - s//2` · `y0 = h - cy - s//2` 로 **중심**으로 읽는다. 라벨대로 왼위 원점으로 읽으면 `docs/캐릭터/펠트코랄_0815/재염색_본체.png` 에서 **검은 구슬 눈알**이 타일에 들어온다 — 실측 저채도(재염색이 원리적으로 안 닿는 픽셀 · C*≤14) **13.93%**(2,889/20,736), L* 5백분위 1.1(거의 검정). 중심으로 읽으면 같은 좌표가 **0.00%**(0/20,736 · C* 중앙 56.1)로 깨끗한 윗돔 양모다. 즉 숫자는 맞고 이름이 틀렸다.  갈래 ㉡ 좌표가 «한 벌뿐인데 정본사진은 두 장»: 같은 블록의 `정본사진` 은 평상복(재염색_본체.png · 1024²)과 특별(마스코트_렌더/본체.png · 2048²) 둘이다. 중심 해석을 그대로 특별 사진에 대면 저채도 **100.00%**(20,736/20,736 · L* 91.0 · C* 1.5) = 양모 0, **맨 배경**이다. 좌표는 1024² 평상복 전용인데 그 사실이 어디에도 안 적혀 있다.  왜 조용한가: 이 토큰 블록을 «프로그램으로 읽는 소비자가 아직 0» 이다(grep 실측 — 마스코트인형리깅.py 는 510·320·144 를 하드코딩하고 토큰을 안 읽는다). 그래서 라벨이 틀려도 아무 테스트도 안 깨졌다. 설계 정본 `docs/펠트엔진_설계.md` 3-2 가 예고한 「토큰↔코드 값 이중 상주」의 첫 청구서 — 갈라짐이 «값»이 아니라 «뜻»에서 먼저 났다.  대가: L2b 는 「새 오브젝트가 목표색 펠트를 0원으로 입는다」가 목적이라 패치 절단이 사슬의 첫 칸이다. 여기서 눈알·배경이 섞이면 그 뒤 재염색·타일링·검증이 전부 초록인 채로 오염된 라이브러리가 정본이 된다. | 토큰 키를 값에 맞춰 「원점」→「중심」으로 고치고(숫자 [510,320] 불변 · 파생 문서 전파 불필요 — 킷 색 축이 아니다), 같은 자리가 다시 갈릴 수 없게 세 겹으로 못박았다.  ①이름을 고쳤다 — `docs/디자인_토큰.json` 재질.펠트.울패치: `"중심": [510,320]` + `_주` 에 실측(저채도 13.93%→0.00%)과 왜 아무 테스트도 안 깨졌는지, `_사진` 에 「이 좌표는 1024² 평상복 전용」(갈래 ㉡)을 적었다.  ②옛 이름을 조용히 안 받는다 — `tools/펠트패치.py` 토큰읽기() 는 `중심` 이 없고 `원점` 이 있으면 **고칠 이름을 대며 죽는다**(『"원점" → "중심" (숫자는 그대로)』). 받아주면 라벨이 다시 갈리는 날 도구가 오염된 타일을 「통과」의 얼굴로 내므로, 원인을 쓸 수 없게 만드는 쪽을 택했다(F103 축 — 차단 사유가 시키는 그대로 고치면 통과한다).  ③오염 자체를 문으로 막았다 — `절단` 이 자른 뒤 스스로 검사한다: 저채도(C*≤14 · 재염색이 원리적으로 안 닿는 픽셀) 2% 상한 · 암부(L*<20) 1% 상한. 라벨이 또 어긋나도 눈알·스티치·그림자가 섞인 타일은 라이브러리 정본이 되지 못한다(맹점 ④ — 장치는 안 도는 쪽보다 「맞는 얼굴로 틀린 값」 쪽으로 샌다).  회귀 = `tests/펠트패치.test.js` 8건, 탐지력은 픽스처가 진다(맹점 ② — 실저장소에는 거짓양성만 검사). 변이 3/3 전건 검출: ㉠중심→원점 = 3건 적발(픽스처 ①·타일 ③·실저장소 ⑤) ㉡저채도 문 차단 = ②-b 적발 ㉢암부 문 차단 = ②-c 적발. ⚠변이가 실제로 구멍을 하나 팠다: 처음엔 ②(검은 눈알) 하나로 두 문을 다 덮는 줄 알았는데, 저채도 문을 껐더니 ②가 그대로 통과했다(암부 문이 대신 잡았다). 실물에서 가장 흔한 오염은 검정이 아니라 **크림 손스티치**(채도 0 인데 밝아서 암부 문에 안 걸린다)라 그 자리를 ②-b 로, 반대쪽(어둡지만 채도 살아 있는 접힘 그림자)을 ②-c 로 따로 세웠다 — 두 문이 각자 자기 몫을 지는지 이제 보인다.  🔑 남은 교훈(설계 정본 §7 에 등재): 정본에 값을 두는 것만으로는 정본이 아니다 — **누군가 그 값을 프로그램으로 읽어야** 살아 있다. 이 블록이 틀린 채로 조용했던 이유가 정확히 「읽는 소비자 0」이었고, L2b 가 첫 소비자가 되자마자 첫 크롭에서 드러났다. |
