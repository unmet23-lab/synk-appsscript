| F575 | 2026-08-17 | 마찰 | 대기열 점유 신호 ②(줄 참조)의 «좌표»가 밀려 **미착수 라이브 결함을 아무도 못 집게** 잠갔다 — 층(P1)이 같으면 기존 층 대조가 원리상 못 거른다.  실측 2026-08-17 (`local_ed2182cf` · 세션 시작 직후 트랙 고르는 자리): · `node tools/대기열.js --집기 "#Q106"` → **거절**: 「이미 남이 잡았다(근거 줄참조) — 그 트랙: #Q99 5/5 exit_log 도달…」 · 근거로 지목된 보드 줄 `docs/_ops/보드/7fbc725a.md` 는 자기 ID 를 **`#Q99` 로 적어 놓고** 「대기열 P1 **줄38**」도 함께 인용한다. · 그런데 지금 `docs/_ops/작업대기열.md` 의 **줄38 = #Q106**(영구 실패한 학생이 문법 판정 큐를 영영 막는다 · 🔴이미 라이브 · 미착수)이고, **#Q99 는 줄42·43** 으로 밀려 있다. · 즉 그 줄이 적은 좌표는 «적을 당시의 자리»다. 층까지 P1 로 같아 `점유()` 의 층 대조(`적힌층 !== 항목.층` 이면 버림)는 원리상 못 걸렀다.  ■ 새는 방향이 「잠근다」다 — 그래서 조용하다 · `#Q106` 은 대기열에서 〖지금〗 인데 도구가 「이미 잡힘」으로 접었고, 그날 세션 시작 알림은 **「지금 집을 수 있는 줄이 0이다」** 라고 그 거짓을 섞어 보고했다(실측 열림 22 · 남이 안 잡은 것 13 · 초록 0). · 「부른다」(F406 의 방향)와 반대 방향이라 기존 회귀가 전부 놓쳤다: `tests/대기열점유.test.js` 의 ② 검사 둘은 «층이 갈릴 때»만 본다. **같은 층 안에서 밀린 좌표**는 픽스처가 없다. · 잠긴 자리는 아무도 안 열어 보므로 스스로 드러나지 않는다 — 이 신고는 그 자리를 실제로 집으려다 막혀서 났다.  ■ 처방 — **안 지었다**(같은 함수를 `local_be03f4c7` 가 이미 잡고 있어 board-guard 가 막았고, 규약대로 비켜났다) 그 세션의 트랙 = 「대기열 점유가 «인용»을 «선언»으로 읽어 빈 자리를 잠갔다」(가지 `worktree-queue-occupancy-citation` · 신호 ⓪ ID 조인 갈래). **이 신고는 그 트랙과 같은 함수(`점유()`)의 다른 갈래(신호 ② 좌표)이니 한 벌로 가져가면 된다.**  ㉠ (검증된 패치 — 이 세션이 짜서 26/26 초록까지 확인하고 되돌렸다) 신호 ② 를 쓰기 전에 한 줄 세운다:     const 좌표낡음 = Number.isInteger(항목.id) && !!(행.큐 && 행.큐.size);   그 줄이 자기 ID 를 적었는데 그게 이 항목이 아니면(⓪ 가 위에서 이미 훑고 못 만났다는 뜻) 좌표 인용은 낡은 것으로 본다. ID 를 아예 안 적은 옛 선언은 폴백을 그대로 쓴다(그 줄엔 ⓪ 가 원리상 못 선다). ③(파일 자리)은 안 끈다 — 낡는 것은 «좌표»지 그 줄이 만지는 파일이 아니다.   ⚠대가 = 한 보드 줄이 「자기 ID 하나 + 남의 항목을 줄번호로」 동시에 선언하는 경우를 놓친다. 실측상 그런 줄은 0 이고, 방향은 「못 고름」이 아니라 「고를 수 있음」이라 반대쪽 위험(중복 구현)이 생긴다 — 그래서 ③ 과 board-guard 가 마지막 문으로 남는다. ㉡ 회귀 3(픽스처 · 탐지력은 픽스처가 진다):    ☠️ 자기 ID(#Q99)를 적은 선언의 「P1 줄38」이 #Q106 을 못 잠근다 · 🔑 좌표와 ID 가 같은 줄이면 그대로 잡힌다(가드가 정상 점유까지 끄면 안 된다) · 🔑 ID 를 안 적은 옛 선언은 줄참조 폴백을 그대로 쓴다. ㉢ 더 근본 — 보드 선언에서 **좌표 인용 자체를 은퇴**시킨다(`#Qnn` 만 남긴다). ⓪ 가 유호 채택 08-14 로 이미 정본이고, 좌표는 늙는 것 말고 하는 일이 없다. ⚠대가 = ID 없는 옛 줄이 남아 있는 동안은 폴백이 필요하다.  ⚠ 뿌리는 CLAUDE.md 의 이미 있는 조항이다 — 「행 번호·좌표는 근거가 아니다」(비가역 조준 절). 그 조항이 라이브 화면(승인 조준)에만 걸려 있고, **저장소 안의 장부 좌표**에는 안 걸려 있었다. | |
