| F573 | 2026-08-17 | 마찰 | git-scope-guard ④ 가 «머지를 마치는 처방»을 env 접두어 하나로 다시 막았고, 읽기 전용 조회(`git merge-base --is-ancestor`)도 같이 막힌다 — 그런데 **기전을 못 쟀다**(합성 payload 로는 그 훅이 안 돈다).  실측 2026-08-17 (`local_1d940103` · detached 워크트리에서 origin/master 를 반입하던 중):   · `GIT_EDITOR=true git merge --continue` → **deny**(「merge 진행 중 — 새 커밋을 만들지 않는다」).   · `export GIT_EDITOR=true` 를 앞 줄로 빼고 `git merge --continue` → **통과**, 머지 성사(6df8320b).     비대화 셸에서는 에디터를 죽여야 `--continue` 가 안 멈추므로, 처방을 그대로 따르려면     이 형태가 필요하다 — 즉 처방이 가리키는 명령이 **접두어를 붙이는 순간** 다시 막힌다.   · `git merge-base --is-ancestor A B` → **deny**(같은 문구). `merge\b` 가 `merge-base` 의     「merge」에도 걸린다(`-` 가 낱말 경계다). 이건 커밋을 만들 수 없는 **읽기 전용 조회**이고,     하필 인계문·F566 계열이 «착지 확인»에 쓰라고 지목하는 그 명령이다(직전 세션이 그걸로 8건을 확인했다).  ⚠ 다만 그 deny 가 **거짓 판정이라고는 말할 수 없다** — 그 순간 옆 세션이 실제로 메인 트리에서   머지를 돌고 있었다(`50a0f108`). 가드 안내문의 「다른 세션의 작업이면 끝날 때까지 기다린다」가   정확히 그 상황이라, ④는 설계대로 동작한 것일 수 있다. 갈리는 지점은 「읽기 조회까지 막을 일인가」다.  ■ 못 쟀다 — 이 신고의 급소다(그래서 처방을 안 지었다)   격리 픽스처(임시 repo + 인공 `.git/MERGE_HEAD` + 훅에 합성 payload 를 stdin 으로)를 세워   네 형태를 돌렸는데 **대조군 `git commit -m x -- a.txt` 까지 통과했다** — 즉 하네스가 가드를   아예 안 태웠다(deny 경로에 안 닿았다). `gitCwd` 는 명령 문자열의 `cd` 를 파싱하고(93줄)   거기까지 맞췄는데도 그렇다. 그래서 다음에 열 층은 **훅 payload 계약**이다:   합성 payload 로 이 훅을 태우는 정확한 꼴(필수 키·조기 return 조건)을 먼저 확정해야   회귀를 못 박을 수 있다. 그게 안 서면 이 가드는 회귀로 지킬 수 없는 상태다.  ⚠ 그리고 코드 주석 213~215줄은 「env 접두어(GIT_EDITOR=)를 매처가 못 읽는다」는 원인 후보가   **실측으로 반증됐다**(접두어를 떼도 똑같이 deny)고 적어 두었다. 내 실측은 그 반대 방향이다   (떼면 통과·붙이면 deny). 둘 중 하나가 낡았거나 조건이 다른데, 위 하네스 문제로 **내가 가르지 못했다** —   주석을 고치지 않고 이 어긋남만 적어 둔다(못 잰 것을 판정으로 쓰지 않는다). | git-scope-guard 에 공용 경계 상수 `끝 = '(?![-\w])'` 신설 + 전 서브커맨드 호출부 10곳 물림 + 회귀 2 신설(열거 8종·소스 축) — 단 신고의 두 축 중 «하나만» 참이었다  ■ 참이었던 축 (④ · 고쳤다) `merge\b` 가 `git merge-base --is-ancestor` 를 커밋 생성으로 읽어 deny 했다 — `-` 가 낱말 경계다. 급소는 신고문이 짚은 대로 「읽기 전용 조회가 막힌다」이고, 값이 비싼 이유는 막힌 것이 **확인 통로**라서다 (확인을 못 하면 판정이 추정으로 내려간다 · 하필 인계문·F566 계열이 착지 확인에 쓰라고 지목하는 명령이다). 실측 분모: `git help -a` 로 열거한 대시 서브커맨드 8종 × 2상태 = 16칸 중 **7칸이 DENY → pass** (8번째 `checkout-index` 는 ⑤의 표기 축에 애초에 안 걸려 전후 pass — 그 구멍은 별건으로 F590 신고).  ■ 참이 아니었던 축 (① · 재현되지 않는다 · 그래서 코드는 안 고쳤다) 신고문은 「`GIT_EDITOR=true git merge --continue` → deny · 접두어를 빼면 통과」라 적었다. 격리 픽스처(진짜 머지 충돌 + 인공 MERGE_HEAD 양쪽)에서 **기록된 문자열 그대로** 재니 둘 다 pass 다. 이 축은 완전 측정이 가능하다 — ④의 발동 판정(`커밋생성`)은 입력이 **명령 문자열 하나뿐**인 순수 계산이고, 그 구역은 신고 커밋(80be35e8) 이후 **바이트 무접촉**이다(그 뒤 유일한 변경 564eef94 는 ⑨ 축 17줄 추가). ⇒ 그날 본 deny 는 기록된 문자열이 낸 것이 아니다. 무엇이 냈는지는 모른다 — 그 이상은 쓰지 않는다. 그리고 코드 주석 213~215줄은 **고치지 않았다**: 그건 F103(08-05) 당시의 진단 이력이고 지금도 맞다 (마침꼴 예외가 생긴 뒤로 두 형태가 다 통과한다 — 내 실측이 그 주석과 같은 방향이다).  ■ 신고문이 「급소」라 지목한 것은 오진이었다 (다음 주자가 같은 벽을 안 만나도록) 신고문은 「합성 payload 로는 훅이 안 돈다 → 다음에 열 층은 훅 payload 계약」이라 적었다. 그게 벽이 아니었다. 계약은 이미 `tests/git범위가드.test.js` 에 서 있다 — stdin `{tool_input:{command}}` · `cwd` = 판정 대상 저장소. 그 하네스로 인공 `.git/MERGE_HEAD` 만 심어도 ④가 정확히 발동한다(대조군 DENY 재현). 어긋난 것은 **대조군 선택**이다: 신고문이 쓴 `git commit -m x -- a.txt` 는 ④가 머지 중일 때만 막는 명령이라, 머지 상태가 안 선 픽스처에서는 **통과가 정답**이다. 그 통과를 「하네스가 가드를 안 태웠다」로 읽었다. 🔑 일반형 = **대조군은 「막혀야 하는 것」이어야 하고, 그 명령이 막히는 «조건»까지 픽스처가 세워야 한다.**    조건을 안 세우면 정상 통과가 미실행과 같은 모양이 된다(F207 의 대조군 판).  ■ 무엇이 막았나 (기계) · `.claude/hooks/git-scope-guard.js` — 공용 상수 `끝` 신설. 호출부 10곳(①add ②commit ⑩commit·push   ③clean ⑥push ④동사4종 ⑤reset·checkout/restore ⑦stash 2곳)이 전부 이 상수를 지난다.   `\s+--abort` 로 이미 경계가 선 2곳(되감기꼴·탈출구꼴)은 그대로 뒀다 — 공백이 대시를 이미 배제한다. · `tests/git범위가드.test.js` 회귀 2 신설:   ①**열거 축** — 대시 8종이 MERGE_HEAD 상태에서 전부 통과 + **분모를 같은 test 안에** 둔다     (`git merge other`·`git commit -- a.md` 는 계속 차단 · 목록만 줄여 초록을 만들 수 없다).     `git mergetool`·`--tool=vimdiff` 도 넣었다 — 경계를 `(?!-)` 로 좁게 적으면 **충돌 해소 통로 자신이** 막힌다.   ②**소스 축** — 서브커맨드 바로 뒤에 `\b` 를 쓴 자리가 0인지 본다(옛 통로 금지). `HOOK` 을 읽어     변이 이음매를 따라간다 — 고정 경로를 읽으면 이 검사만 원본을 보고 변이가 조용히 통과한다. · 같은 계열이 다른 훅 둘에도 있다(`friction-close-guard:61`·`track-boundary:54` — `commit\b`).   **안 고쳤고 재제안도 필요 없다**: 둘 다 전 경로 `exit(0)` 인 알림 전용이고, 게다가 출력이 `[해시]`   모양이어야 통과해 `commit-graph verify` 는 그 앞에서 끊긴다 — 새는 방향이 잔소리다(F200 선례). |
