| F248 | 2026-08-08 | 마찰 | 자동커밋이 반쪽만 착지시켜 master 를 빨갛게 만든다 — 바이트는 지켰는데 공유 브랜치가 깨졌다. 실측 2026-08-08: `fb0bbce`(auto-commit · Stop 훅 · 세션 local_4859ee1e)가 tests/safety.test.js 의 [vNEXT] 테스트 129줄만 커밋했고, 그 테스트가 부르는 구현(엔진_운영배치.js 숙제·근태 집계)은 같은 세션 작업본에 미커밋으로 남았다. HEAD 에 해당 함수 0건 → 원격 run 31239587806 이 4건 실패하며 master 가 적색이 됐고, 그 상태는 남의 /deploy 게이트를 그대로 막는다(CLAUDE.md 신뢰성). 훅은 구문검사(구문깨졌나)까지만 보고 「이 커밋 하나로 판이 서는가」는 안 본다 — 테스트를 돌리기엔 Stop 이 너무 비싸다는 것이 원래 설계 이유다. 새는 방향은 침묵이 아니라 **남에게 전가된 적색**이라 F225(깨진 판 커밋)·F247(죽은 세션 미커밋의 초록·빨강 미측정)과 같은 가족이고, F247 과 다른 점은 주인이 **살아 있다**는 것이다(그래서 F073 상 내가 고칠 수 없고, 기다리는 것 말고 할 수 있는 게 없다). 처방 후보: ①auto-commit 이 「이번에 커밋할 파일들만으로 그 파일들을 부르는 테스트가 서는가」를 못 보면 커밋 대신 알림만 낸다 ②테스트 파일 단독 커밋은 짝(구현)이 같은 작업본에 미커밋이면 보류하고 사람에게 넘긴다 ③F247 트랙에 흡수 — 산 세션 갈래를 그쪽 판정층에 얹는다. | auto-commit 이 실을 후보 중 테스트 파일만 돌린다(실측 safety 1.7초 vs 전 스위트 313초 — 「Stop 에 비싸다」는 전 스위트 가정이었다). 적색·미측정은 그 파일만 떨어뜨리고 말한다(보류 아님 — F025 보호 유지). 자식 env 의 NODE_TEST_CONTEXT 제거 + 요약 줄(분모) 확인으로 미실행이 초록으로 안 온다. 회귀 3·20/20·변이 6/7(7cb57e3) |
