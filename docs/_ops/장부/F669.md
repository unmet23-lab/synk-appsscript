| F669 | 2026-08-19 | 실수 | 권한을 «다른 층에서» 읽고 할 수 있다고 단정했다 — 세 번째다(F662·F668 계열). 저장소 공개(3단계)를 원격으로 하겠다고 유호님께 말했는데, 근거로 삼은 admin:true 는 MCP 서버 토큰의 권한이었고 내가 실제로 쓰는 프록시 주입 토큰은 admin:false 였다. 게다가 프록시가 GitHub Actions 경로를 통째로 막아 러너 목록 조회도 안 된다. 두 토큰이 같은 얼굴(api.github.com 200)을 해서 구분을 안 했다. 세 번 같은 병이면 원인을 쓸 수 없게 만들어야 한다: 「할 수 있다」를 말하기 전에 그 통로로 «그 동작을» 재는 자리가 없다 — 도달(200)과 권한(admin)은 다른 층인데 한 번의 GET 으로 둘 다 봤다고 읽었다. | F662·F668 과 한 벌로 닫는다(축 3번째 · 「도달(200)과 권한(admin)은 다른 층인데 한 번의 GET 으로 둘 다 봤다고 읽었다」). 이 축의 원인은 정본이 진다 — CLAUDE.md v10.0 「실제로 한 것만 했다고 말한다」. 곁들여 이 신고가 가리킨 환경 조건 자체가 바뀌었다: 저장소는 이미 **public** 이고(실측 08-22) 프록시 토큰으로 GitHub Actions 를 못 읽던 자리는 이 로컬 세션에 해당하지 않는다(gh 가 정상 응답 · run 목록 조회됨). ⚠ **기계 가드 없음** — 셋 다 사람 규칙으로 닫는다. 4번째가 나면 그때는 「할 수 있다」를 말하기 전에 그 통로로 그 동작을 재는 장치를 유호님께 제안한다. |
