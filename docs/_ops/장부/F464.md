| F464 | 2026-08-15 | 마찰 | 인계문수거가 「스테이징된 삭제」를 «남이 먼저 거뒀다»로 읽어, 아무도 못 거두는 상태를 만들고는 「잃은 내용 없음」이라고 확신에 찬 얼굴로 거짓을 말했다  유호님 「미커밋된거 이어서 진행해」(08-15)로 미커밋을 갈랐더니, 만료 인계문 5건의 삭제가 **인덱스에 스테이징만 된 채** 굳어 있었다. `node tools/인계문수거.js --실행` 은 이렇게 답했다:      [인계문수거] 5건 전부 딴 세션이 먼저 거뒀다 — 커밋 안 함(잃은 내용 없음)  거짓이다. 아무도 안 거뒀고, 그 5건은 그때까지 며칠째 공유 인덱스에 남아 있었다.  ▶ 병인 — `git add -- <경로>` 의 "did not match any files" 가 **서로 다른 두 상태**를   같은 문장으로 나온다. 도구는 그 문장만 보고 한쪽으로 단정했다.    ⓐ 딴 세션이 이미 커밋했다 → 작업본·index·HEAD 어디에도 없다(원래 뜻 · F071 봉합).    ⓑ 삭제가 **이미 스테이징**됐다 → 작업본에도 index 에도 없지만 **HEAD 에는 있다**.   ⓑ 를 ⓐ 로 읽으면 그 경로는 매 실행 똑같이 건너뛰어져 **영영** 안 거둬진다 — 도구가   스스로는 절대 못 빠져나오는 자리다(자기 처방이 자기를 막는 꼴 · 맹점 ③).  ▶ 왜 굵직한가 — 지침 v9.2 맹점 ④ 그대로다: 장치는 «안 도는» 쪽보다 «맞는 얼굴로 틀린   값을 내는» 쪽으로 더 자주 샌다. 여기선 한 술 더 떠서, 틀린 값에 **「잃은 내용 없음」   이라는 안심 문구까지** 달려 나왔다. 읽는 세션은 확인할 이유를 못 찾는다.   번지는 자리도 있다 — 남은 스테이징은 모든 세션의 `git status` 에 남아 「미커밋 0」   판독을 흐리고, 그건 이 저장소의 인계 규약(끊기 전 미커밋 0)의 분모다.  ▶ 대가 — 판별자가 HEAD 조회에 기댄다. 빈 저장소·HEAD 분리처럼 조회가 실패하면 ⓐ 로   접어 **옛 동작 그대로** 건너뛴다(안전한 방향 · 내용 손실 0). 새로 닫은 것 1개 =   ⓑ 영구 누락. 새로 막는 면은 0이다(ⓐ 경로는 회귀로 따로 고정했다). | tools/인계문수거.js 판별자 HEAD에있나() — add 빈손의 두 까닭을 HEAD 존재로 가른다 + 픽스처 회귀 2건(tests/인계문수거.test.js · 변이 1/1) |
