| F597 | 2026-08-18 | 마찰 | 승계 문(트랙 칸 바이트 동일)이 **주인의 생사를 안 본다** — 그래서 «살아있는 세션의 자리»에 정당하게 들어가진다. F589 가 「㉡은 승계일 때만 열리는 좁은 문이고 그 좁음이 맞다」로 적은 판정의 반례다.  ■ 실측 2026-08-18 (`local_41b1ad2f`) · 인계문이 세션 `90a98dcc` 를 「**마감하며** 남긴 글」로 배달했고, 본문이 「PR #93 은 머지만 남았다」로 못박았다. · `node tools/board.js --점유 tests/보드점유조회.test.js F585` → 🔴 이미 잡혔다 1건 · `local_90a98dcc` · 가지 `worktree-f585-occupancy-query`.   **그 줄에 ⏹(주인이 죽었다) 표식은 없었다** — 도구는 「살아있다」를 옳게 알고 있었다. · 그런데 같은 출력이 바로 아래에서 **조건 없이** 이렇게 권한다:   「같은 트랙을 **승계**하는 것이면 그 줄의 트랙 칸을 **바이트 그대로** 옮겨 적는다 — 그때는 안 막힌다.」 · 그대로 했다. `board-guard` 는 **통과시켰다** — `docs/_ops/보드/41b1ad2f.md` 신설이 커밋 `e1990435` 로 origin master 까지 올라갔다. · 판정 자리 = `.claude/hooks/board-guard.js:1155` `if (트랙칸(남) === 트랙칸(새)) continue;` — 생사 조회가 **0회**다.   그리고 바로 아래 1196행이 **산문으로만** 「🚫산 세션의 자리엔 쓰지 않는다(그건 중복 구현이다)」라고 적는다.   즉 규칙은 있고 기계는 없다 — CLAUDE.md 「프로즈보다 기계 강제로 막을 수 있는 규칙은 기계로 옮긴다」 그 자리.  ■ 실제 손해 (가정이 아니다) · 나는 그 가지의 `tests/보드점유조회.test.js` 를 편집했다. 주인 `90a98dcc` 는 **살아서 같은 파일을 고치는 중**이었고,   내가 읽은 판과 디스크 판은 이미 갈려 있었다(그 세션이 `25425f6d` 로 같은 적색을 자기 통로로 고친 뒤였다).   내 편집은 그 새 코드가 쓰는 `BOARDJS` 상수를 지웠다 — 커밋 전에 발각해 `git checkout --` 로 되돌려 **손해 0**. · 즉 중복 구현이 **실제로 시작됐다**: 같은 적색(`tests/훅통로` 가 잡은 `보드점유조회.test.js:46`)을 두 세션이 각각   진단하고 각각 처방까지 지었다 — 그쪽은 `훅띄우기` 공용 통로, 내 쪽은 F535 면제형 인라인. · `track-collision` 훅은 안 막았다: 그 세션이 **방금 커밋을 마쳐** 미커밋이 아니었기 때문이다   (훅 문구 그대로 「그 세션이 만졌지만 지금은 미커밋이 아니다 — 커밋을 마친 자리라 막지 않는다」).   ⇒ 살아있는 주인이 «커밋을 자주 할수록» 이 층의 보호가 얇아진다. 잘한 관습이 가드를 끄는 모양이다.  ■ 왜 F582·F589 와 축이 다른가 F582·F589 는 **죽은 주인의 줄이 자리를 영구히 잠근다**(막지 말아야 할 때 막는다)를 잰다. 이건 정반대다 — **살아있는 주인의 자리가 승계 표기 하나로 열린다**(막아야 할 때 연다). 새는 방향이 「통과」라 조용하고, F589 가 ㉠(생사 게이트)을 권하며 붙인 근거 (「㉡이 정상 통로로 도는 것이 확인됐으니 ㉠을 붙여도 정직한 관습은 안 죽는다」)가 여기서 반쪽이 된다: ㉡은 정직한 관습이면서 **동시에** 산 세션의 자리로 들어가는 유일한 뚫림이다.  ■ 내 실수 몫 (가려 적는다 — 장치만 탓하지 않는다) 1차 원인은 인계문의 「마감하며 남긴 글」을 **생사 판정으로 번역**한 것이다. 정본은 이미 이걸 금지한다: 인계문 머리말 자신이 「그 뒤로도 더 일했을 수 있다」고 적어 뒀고, `node tools/작업본소유자.js` 한 번이면 갈렸다 (실제로 뒤늦게 그걸로 갈랐다 — `90a98dcc` 는 「살아있는 세션 15개」에 있었고 0분 전 파일 접촉). 「현재 상태는 현재를 읽어 판정한다」·「간접 증거로 대신하지 않는다」 그 자리를 그대로 밟았다.  ■ 처방 후보 (안 지었다 — 이 신고는 재료다 · 아래 파일 둘 다 지금 남이 살아서 고치는 중이라 내 트랙에서 못 만진다) ㉠ 승계 문에 생사 게이트 — `트랙칸(남) === 트랙칸(새)` continue 를 **주인이 죽었을 때만** 준다.    판정은 `작업본소유자.살았나` 한 곳에서(F589 ㉠ 과 **같은 자**를 쓴다 · 두 벌이면 갈라진다 · 맹점 ④).    ⚠ 대가가 거의 없다는 것이 이 갈래의 핵심이다: 산 세션이 «진짜로» 넘겨준 자리는 🎫 이고, 🎫 는 지금도 점유가    아니므로(F285) 원래 안 막힌다. 그래서 이건 F582 ㉠(⏳유호 판정 대기)과 달리 **지금 닫아도 되는** 축이다. ㉡ 조회 도구(`board.js --점유`)의 승계 안내를 **⏹ 일 때만** 찍는다 — 지금은 무조건이다.    산 주인 줄 바로 아래에 「승계하면 안 막힌다」가 붙는 것 자체가 F103 형 처방이다(따라 하면 사고가 난다). ㉢ 인계문이 「마감」이라 말할 때 **그 세션의 현재 생사를 함께 찍는다**(지금은 읽는 사람이 별도 도구를 불러야 안다).    F595(인계문이 미머지 가지 안의 선언을 못 본다)와 같은 파일·다른 칸이다.  ⚠ 착수 조건: ㉠㉡ 이 만질 `.claude/hooks/board-guard.js` · `tools/board.js` 는 **PR #93 이 지금 다시 쓰는 중**이다.    그 머지 뒤에만 손댄다 — 안 그러면 이 신고가 고발한 바로 그 중복 구현을 신고자가 반복한다. | ⏸ 지금 안 닫는다 — 처방 ㉠㉡ 이 만질 .claude/hooks/board-guard.js·tools/board.js 를 PR #93(주인 local_90a98dcc 생존)이 지금 다시 쓰는 중이라, 여기서 손대면 이 신고가 고발한 중복 구현을 신고자가 그대로 반복한다. 판정 자리 = 이 세션(local_41b1ad2f) · 착수 조건 = PR #93 머지 뒤. ㉠(승계 문 생사 게이트)은 F582 ㉠ 과 달리 대가가 거의 없어 ⏳유호 없이 닫을 수 있는 축이라고 판정했다 — 근거는 신고문 ㉠ 의 🎫 논증. |
