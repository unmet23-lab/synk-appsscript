| F616 | 2026-08-18 | 마찰 | `test-ci` 의 「도는 동안 바뀐 파일」 경고가 **자기 스위트 의 부작용을 「옆 세션」에게 돌린다** — 그래서 그 검사가 낀 적색은 **언제나** 「못 믿는다」로 나온다. 새는 방향이 「진짜 적색을 안 믿게 만든다」다.  ■ 실측 2026-08-18 (`local_163e0921` · 워크트리 `evidence-packet-unread-as-zero` · 우연이 아니라 그 화면이 그 상태였다) · 실행 직전 같은 호출 안에서 `git status --short` = **빈 출력**(트리 깨끗). 그 뒤 `node tools/test-ci.js`. · 끝나고 낸 문장:     「⚠ **이 적색은 못 믿는다** — 테스트가 도는 동안 2개 파일이 바뀌었다(**옆 세션이 편집 중**).        · docs\교재_읽기본\권1.html        · docs\이해대장.html      → 고치러 가기 전에 재실행하라. … 누구 파일인지는 `node tools/작업본소유자.js`.」 · 실행 직후 `git status --short` = **빈 출력**. 두 파일 다 무변경으로 돌아와 있다. · **이 워크트리는 남이 안 만진다** — 오늘 만든 내 전용 트리이고 다른 세션의 선언이 0건이다.   즉 「옆 세션」은 실재하지 않았다.  ■ 기전 — 소스에서 읽었다(추정 아님) · `tests/이해대장.test.js:495` = `if (원본바이트 !== null) fs.writeFileSync(산출, 원본바이트);`   → 산출물(`docs/이해대장.html`)을 **제자리에서 다시 굽고 바이트로 되돌린다.**   이 「다시 그려서 잰다」는 의도된 설계다(memory `ledger-mask-leak` — 화면 회귀는 다시 그려야 생성기를 지킨다). · `docs/교재_읽기본/권1.html` 의 생산자는 `tools/교재읽기본.js` 이고, 그것을 부르는 검사가 셋 있다   (`tests/교재읽기본.test.js` · `tests/바탕화면통로.test.js` · `tests/운영자료.test.js`). · `test-ci` 는 실행 전후로 스냅샷을 찍는다(그 배선 자체의 회귀가 이 런에서 초록으로 지나갔다:   「🔑 도는 동안 바뀐 파일을 잡는다」 · 「배선 — 실행 전후로 스냅샷을 찍고, **적색일 때만** 경고한다」).   탐지는 정확하다 — **틀린 것은 귀속**이다: 자기 스위트 가 만든 창을 제3자에게 돌린다.  ■ 왜 신호인가 — 무해한 오탈자가 아니라 **신뢰 신호를 상시 깎는다** · 경고는 «적색일 때만» 뜬다. 그런데 그 두 검사는 **거의 모든 전체 런에 낀다.**   ⇒ 전체 런이 빨개지면 사실상 **항상** 「이 적색은 못 믿는다 · 재실행하라」가 함께 나온다.   진짜 적색과 거짓 적색이 같은 글자로 나오는 자리다 — F207 의 「초록은 분모와 함께」의 거울상이고,   새는 방향은 통과가 아니라 **「빨간 것을 안 믿게 만든다」**다(더 나쁘다: 초록은 다시 재면 되지만   이건 재라는 처방이 매번 참이라 무한히 미룰 수 있다). · 처방문이 **따를 수 없다**(F103): 「누구 파일인지는 `작업본소유자.js`」라고 하는데, 그 목록엔 아무도 없다.   없는 세션을 찾다가 「그럼 내 탓이 아니군」으로 접는 것이 자연스러운 다음 수다. · 계보: F598 이 **같은 모양**을 다른 짝에서 잡았다(변이.js 의 제자리 원복 → `작업본소유자` 가   「미커밋으로 들고 있다」로 읽음). 거기선 **두 장치**가 어긋났고, 여기선 **한 장치가 자기 자신**과   어긋난다 — 그래서 이쪽이 더 닫기 쉽다(제 부작용은 제가 안다).  ■ 안 잰 것 — 명시한다(F348) · **그 런의 실제 실패가 무엇이었는지 못 봤다.** 배경 실행을 `| tail -12` 로 잘라 본문을 잃었다(내 실수).   같은 트리로 다시 돌린 판은 별도로 재고 있다 — 이 신고는 «귀속» 축이고 «그 적색이 진짜였나»는 다른 축이다. · 「전체 런에 거의 항상 낀다」는 두 검사가 기본 스위트에 있음을 근거로 한 것이고, **몇 %의 런에서 뜨는지는   안 셌다.** 분모를 세려면 여러 회차가 필요하다. · 스냅샷 대상에 든 다른 «제자리 굽기» 검사가 몇 벌인지도 안 셌다(이 둘은 관측된 것일 뿐이다).  ■ 처방 후보 — 안 지었다(이 신고는 재료다) ㉠ 귀속을 고친다 — 문구에서 「옆 세션이 편집 중」 단정을 빼고 **두 갈래로** 낸다:    「이 런이 스스로 굽는 산출물일 수 있다(생산자를 부르는 검사가 있나) / 밖에서 바뀌었다」.    ⚠대가: 문구가 길어지고, 「둘 중 하나」는 읽는 사람에게 판정을 떠넘긴다. ㉡ **되돌아온 파일은 빼고 센다** — 전후 스냅샷이 이미 있으니 「끝에 원래 바이트로 돌아온 것」은    창이 닫힌 것이라 경고에서 제외한다. 남는 것만 「밖에서 바뀌었다」로 낸다.    ⚠대가: 도중에 두 번 바뀌어 우연히 같은 바이트로 끝난 판은 못 본다(그 창은 실재한다 · F598 ㉡ 방향).    🔑 이게 제일 싸 보인다 — 지금 이미 뜨는 두 파일이 정확히 그 갈래다. ㉢ 굽는 검사를 격리한다(임시 디렉터리에서만 굽게) — 근본이지만 과녁이 남의 검사 여러 벌이라 크다.    그리고 「실물을 다시 그려서 잰다」는 설계 의도를 깎는다(memory `ledger-mask-leak` 가 그걸 지킨다). ㉣ 아무것도 안 한다 — 경고가 「재실행하라」이고 재실행은 실제로 답을 준다.    ⚠대가: 위 「진짜 적색을 안 믿게 만든다」가 그대로 남고, 이 판정이 사람 규율에만 얹힌다. | |
