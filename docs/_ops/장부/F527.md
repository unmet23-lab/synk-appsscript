| F527 | 2026-08-17 | 실수 | 손으로 잰 분모가 비ASCII 경로 이스케이프에 걸려 전건 빗나갔고, 그 틀린 값이 «맞는 얼굴로» 코드 주석·보드·커밋문까지 갔다  실측(2026-08-17 · `local_7f15ce3c` · F520 수리 중). 「`tools/이해대장.js` 를 만진 커밋이 화면을 같이 담았나」를 재려고 셸에서 이렇게 돌렸다:    git show --name-only --format= "$c" / grep -qx "docs/이해대장.html"  `git show` 는 기본값(`core.quotepath=true`)에서 비ASCII 경로를 `"docs/\354\235\264..."` 로 이스케이프해 낸다. 그래서 `grep -qx` 가 **한 건도 안 맞았고**, 출력은 「합계 12 = 동봉 0 + 두고감 12」였다. 참값은 `-c core.quotepath=false` 로 재니 **합계 12 = 동봉 8 + 두고감 4** 다.  ■ 왜 아픈가 — 새는 방향이 하필 «주장을 강화하는 쪽»이었다 「전량이 샜다(0/12)」는 내가 그때 세우려던 명제(게이트가 이 축을 못 본다)를 가장 세게 뒷받침하는 모양이다. 즉 틀린 값이 **의심을 부르지 않는 얼굴**로 나왔다. 그대로 코드 주석·보드 줄·커밋문 세 자리에 실었고, 그 뒤 origin 에 새로 착지한 커밋 하나(`b6d9d2c4`)를 같은 방법으로 재다 「파일 목록에 화면이 보이는데 판정은 두고감」이라는 **자기모순**이 눈에 걸려서야 발각됐다. 우연이다 — 그 커밋이 안 왔으면 그대로 나갔다.  ■ 🔴 급소는 「몰랐다」가 아니라 «아는 것이 도구 안에만 있었다»다 이 함정은 이 저장소가 이미 세 곳에 ☠️ 로 적어 두고 있다 — `tools/githooks/pre-commit`(「기본 출력은 비ASCII 경로를 이스케이프해서 한글 경로 대조가 **영원히 안 맞는다** = 검사가 안 도는데 초록」) · `tools/대장동봉검사.js` · `tools/계약동봉검사.js`. 셋 다 `-z` 를 써서 피한다. 그런데 그 앎은 **커밋되는 도구 안에만** 있고, 세션이 판단 재료를 만들려고 셸에서 손으로 재는 자리에는 아무 장치도 없다. 나는 바로 그 경고를 담은 파일을 고치면서 그 함정을 밟았다.  ■ 처방 후보 — 안 지었다(이 신고는 기록이다) ㉠ 손 측정도 도구로: 「커밋이 파일 X 를 담았나」를 재는 공용 함수 하나(`-c core.quotepath=false` 고정). 대가 = 도구가 하나 는다(경량 원칙과 정면 · 쓰는 자리가 몇 개인지 안 셌다). ㉡ 프로즈: CLAUDE.md 실행 절에 「git 출력으로 경로를 대조할 땐 `-c core.quotepath=false` 나 `-z`」. 대가 = 상주 한 줄이 늘고, 프로즈로 막힌 적이 없는 계열이다(이 함정은 이미 세 파일이 적어 뒀는데도 났다). ㉢ 아무것도 안 한다 — 손 측정은 늘 일회성이라 다음에도 각자 밟는다.  ⚠ 이 신고는 F522(「검증 안 한 숫자·문구를 판단 재료로 유호님께 올렸다」)와 **축이 다르다**: 저건 안 재고 올린 것이고, 이건 **쟀는데 재는 층이 값을 깨뜨렸다**(CLAUDE.md 「원인 판정」 ② 「재는 층이 값을 깨뜨리지 않는지 확인」의 정확한 실물). | |
