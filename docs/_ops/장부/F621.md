| F621 | 2026-08-18 | 실수 | 게이트를 «어디에 두느냐»가 게이트 자체보다 자주 틀린다 — 한 트랙에서 같은 뿌리로 세 번 밟았다. 2026-08-18 `local_9a6c0547` 실측(인자 게이트 부착 트랙 · 도구 76개에 붙이는 동안). ㉠ **최상위에 두면 남의 argv 를 읽는다**: `harness-export.js` 에 최상위로 달자 `node tools/rot-check.js --json` 이 죽었다 — rot-check 는 경로·정본 버전만 얻으려고 그 파일을 `require` 하는데(그쪽 208줄 주석이 「require 는 생성기를 실행하지 않는다」고 못박고 있다), 부모의 `--json` 이 자식 낱말로 읽혀 프로세스가 끊겼다. 나간 문장은 「harness-export 가 아는 것은 --dry --out 뿐이다」 — **멀쩡한 도구가 남의 이름으로 죽고**, 진단이 통째로 엉뚱한 파일로 간다. ㉡ **부작용·환경 점검 뒤에 두면 오진단을 낸다**: `마스코트누끼.js` 는 판정을 `findChrome()` 뒤에 뒀더니 오타가 「SKIP: 크롬을 못 찾았다」로 나갔다 — 사람은 «인자는 맞았는데 환경이 문제»라고 읽고, 크롬 있는 기계로 옮겨 가서야 진짜 사유를 본다. 같은 축으로 굽기·빌드·채번(재질굽기·펠트색굽기·인쇄본빌드·bump-version)은 부작용 앞에 둬야 한다 — 채번은 특히 비가역이다(태운 번호는 안 돌아온다). ㉢ **종료 규약이 도구마다 셋이라 「말만 하고 exit 0」이 난다**: `마누스.js` 는 게이트 문장을 찍고도 exit 0 이었다. 진입점이 `main().then((c) => { process.exitCode = c || 0; })` 라, 게이트가 `exitCode = 1` 을 적고 `null` 을 돌려주자 뒤에서 0 으로 덮였다. 부르는 쪽엔 «성공»으로 보인다. 🔑 셋의 공통 뿌리 = 게이트를 «붙였다»와 «그 자리에서 실제로 막는다»는 다른 사실인데, 코드를 읽는 검사(「답을 쓰나」·「선언이 자기 낱말을 덮나」)는 셋 다 **원리상** 못 본다. 셋 다 코드는 답을 받아 제대로 쓰고 있다. ⚠ 새는 방향이 갈린다 — ㉠㉡은 「멀쩡한 것이 죽는다」(시끄럽지만 엉뚱한 자리를 가리킨다), ㉢은 「통과」다(조용하다). 그래서 한 축으로는 못 막는다. | 같은 트랙에서 기계 둘을 세워 닫았다(`tests/도구인자게이트.test.js`). ③-3 **시점** — 게이트 호출의 중괄호 깊이가 0이면(=최상위) 적색. `if (require.main === module)` 한 줄 꼴만 예외. 깊이 세기는 문자열·정규식 리터럴을 건너뛴다(안 그러면 `/^\{/` 하나로 깊이가 부풀어 최상위 게이트가 「안전」으로 읽힌다 — 그 자리를 픽스처로 못박았다). ③-4 **실행층** — 게이트를 «건» 도구를 실제로 spawn 해서 ⓐexit ≠ 0 인지 ⓑ게이트 문장이 나왔는지를 잰다. ㉡㉢이 여기서 잡힌다(㉡은 「게이트 문장이 안 나왔다」로, ㉢은 「말은 했는데 exit 0」으로). 게이트를 안 건 도구는 안 돌린다 — 모르는 낱말을 주면 그 도구가 통째로 실행되고, 그게 이 트랙이 없애려는 병이라 검사 자신이 그 병을 앓지 않게 했다. 못 잰 것(스폰 실패·시간초과)은 fail 이 아니라 skip 으로 드러내고 분모를 함께 낸다(F296·F207). 변이로 확인: ③-3 은 「늘 안전이라 답한다」·「정규식/문자열 리터럴을 안 건너뛴다」 3건, ③-4 는 「말만 하고 exit 0」 1건 — 전부 포획(구멍 0). ⚠ 남는 대가 = 최상위 IIFE 안의 게이트는 깊이 1이라 ③-3 을 통과하는데 import 시점에 돈다(이 저장소 0건 · 생기면 그날 넓힌다). 자리 규칙은 각 도구 주석에도 남겼다 — 「부작용·환경 점검 앞」, 「종료 코드를 돌려주는 꼴이면 process.exit 대신 return」. |
