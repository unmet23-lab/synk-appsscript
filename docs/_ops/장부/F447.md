| F447 | 2026-08-14 | 마찰 | /close 가 인계문은 남기는데 죽음 도장은 안 찍어, 인계문이 시킨 일을 board-guard 가 막는다 — 인계 통로가 자기 처방을 스스로 차단한다  실측 2026-08-14(세션 `1ae6700f`): 세션 `379cb6f7` 이 `/close` 로 마감하며 ①보드 상태 칸을 `⏸인계` 로 놓고 ②`docs/_ops/인계문/379cb6f7.md` 에 「위 「상태/다음」에 적힌 다음 할 일부터 이어라」 + 이어받기 지점(`codex-review.js --commit cd2f5654` → 처분 → `/deploy`)까지 명시해 커밋했다(`5cc9c497`·`18f2d62c`). 그 인계문이 다음 창의 첫 메시지로 자동 주입됐다.  그런데 그 세션의 상태 파일에 **`끝남`(죽음 도장)이 없다** — `track-*.json` 을 직접 열어 실측: `끝남 = 없음`, 박동 10분. 그래서 board-guard 는 `379cb6f7` 을 **살아있다**로 읽고, 그 트랙을 내 보드 파일에 선언하려 하자 deny 했다: 「이 트랙은 살아있는 다른 세션(379cb6f7)이 이미 선언했다 — 나란히 지으면 둘 다 끝까지 간다(F340)」.  즉 **같은 파이프라인이 한쪽 입으로는 「이어받아라」라고 시키고 다른 쪽 입으로는 「비켜나라」라고 막는다.** 가드가 틀린 게 아니다 — F340 이 못박은 대로 🎫·⏸인계 표식으로는 못 가르고 가르는 것은 생사이며, 도장이 없으니 「살아있다」가 안전한 판정이다. 비어 있는 것은 **도장 쪽**이다.  ⚠ 이것은 CLAUDE.md 「가드·회귀의 네 맹점 ③ 자기 처방을 막지 않는지」의 실사고다: 차단 사유가 시키는 명령(=인계문이 지목한 다음 걸음)을 그 가드에 되먹이면 통과하지 못한다. 따를 수 없는 처방은 우회를 정상 통로로 만든다(F103) — 실제로 board-guard 의 탈출구(「트랙 칸을 바이트 그대로 옮겨 적으면 승계로 본다」)를 밟아도 ⑧-b 가 「산 세션」이라 다시 막았다.  비용: 이 세션은 인계문이 지목한 #Q78(모든 세션의 배포를 막고 있는 자리)을 못 집고 다른 트랙으로 비켜났다. `#Q79` 도 같은 이유로 막혔다(파일 `tests/발화퀄리티.test.js` 가 그 줄에 묶여 있다). 그 두 줄은 주인이 돌아오지 않으면 **아무도 못 집는다** — 죽음 도장이 없어 `board-move` 도 못 쓴다(F372 와 같은 자리).  ▶ 처방 후보(고르는 것은 이 신고 밖): ㄱ)`/close` 가 인계문 커밋과 **같은 걸음에서** 죽음 도장을 찍는다(지금은 SessionEnd 훅에만 있어 창을 그냥 닫으면 안 찍힌다) ㄴ)보드 상태 칸의 `⏸인계`·`🎫` 를 **도장의 대체 증거**로 board-guard 가 인정하되, 그건 F340 이 부정한 축이라 ㄱ 이 먼저다 ㄷ)도장 없는 침묵이 일정 시간 넘으면 `board-move` 만은 열어 준다(집기는 여전히 막고). | `tools/인계문.js` 저장 모드가 인계문과 **같은 걸음에서** 죽음 도장(`끝남`)을 찍는다(처방 ㄱ · 새 도구·훅 0건 — 이미 도는 통로에 세 줄). 회귀 `tests/트랙경계.test.js` 3건(저장은 찍는다 · `--no-save` 는 안 찍는다 · `track-boundary`(산 세션)는 안 찍는다) · 변이 2/2 탐지.  ⚠ **신고의 진단이 한 칸 틀렸다 — 도장은 「없는」 게 아니라 「늦는다」.** 2026-08-15 재실측(세션 `ddb6d85b` · 읽기만): 인계문 **57건 중 무도장 0건**(도장 53 + 만진 기록 자체가 없어 도장의 대상이 아닌 세션 4 · `markEnded` 는 기존 파일에만 찍는다). 신고가 「끝남 = 없음」으로 지목한 그 세션 `379cb6f7` 도 지금은 찍혀 있다. 즉 SessionEnd 는 돌았고, 다만 **창을 실제로 닫을 때** 돌았다. 그래서 진짜 구멍은 「/close 를 다 돌린 뒤 ~ 유호님이 창을 닫기 전」 구간이고, 그 구간의 길이는 세션이 아니라 **사람의 리듬**이 정한다 — 신고가 적은 비용(세션 하나가 #Q78 을 못 집고 비켜났다)이 정확히 그 구간에서 났다. 이 수리는 그 구간을 0 으로 만든다(/close 6단계가 곧 도장).  🔑 왜 공용 writer 가 아니라 이 자리인가 — 인계문 파일을 쓰는 통로는 넷이다: `tools/인계문.js`(/close) · `context-budget` 🔴 wake · `track-boundary` · `session-end-handoff`. 도장을 공용 `writeHandoffFile` 로 올리면 네 통로가 한꺼번에 「통과」하는데, 가운데 둘은 **살아서 계속 도는** 세션이 부르는 자리라 산 세션의 작업본이 ⚪(이어받아도 된다)로 떨어진다 — F252 가 막았던 그 사고가 반대 방향으로 되돌아온다. 그 「맞는 얼굴로 틀린 값」을 회귀 3번째가 잡는다(변이로 실측: 공용 writer 로 올리면 앞 둘은 초록인 채 3번째만 빨개진다).  대가: /close 를 돌고도 계속 일하는 세션은 다음 Edit·Write 전까지 죽은 것으로 보인다. 되돌리는 통로는 이미 있다 — `track-collision` 이 도구를 부르는 순간 도장을 지운다(F252 · 회귀 `tests/트랙충돌.test.js`). 사정거리도 좁다: 이 자리는 /close 가 「미커밋 0」을 보장한 뒤라 이어받을 유물이 애초에 없다. 닫을 것: **없다**(새 파일·훅·도구를 0개 늘렸다 — 물러날 자리가 이 세 줄뿐이다).  ⛔ 남는 것(이 수리 밖): `/close` 없이 강제 종료된 세션은 여전히 무도장이다(실측 81건). 그건 설계대로 ❔(확인하고 만져라)로 남는다 — 새는 방향이 「사람이 한 번 본다」라 맞다(handoff-store 머리말). 처방 후보 ㄴ·ㄷ(board-guard 가 `⏸인계`·`🎫` 를 도장 대체 증거로 인정 / 침묵 시간으로 board-move 만 열기)은 **집지 않았다** — 둘 다 F340 이 부정한 축(표식으로 생사를 가르기)이고, ㄱ 이 서면 그 둘이 필요해지는 빈도 자체가 줄어든다. |
