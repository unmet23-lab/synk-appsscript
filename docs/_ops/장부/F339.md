| F339 | 2026-08-12 | 마찰 | 채번기 [vNEXT] 치환·검사가 「[vNEXT·라벨]」 형태를 통째로 놓친다 — bump-version.js 가 정확히 '[vNEXT]' 만 보므로 라벨을 붙인 마커는 치환도 --check 도 통과한다(08-12 실측: 엔진_운영배치.js 의 [vNEXT·E²] 1건이 --check 초록 아래 살아남았다 · 손으로 발견). 가드가 로직 아닌 표기층에서 샜고, 새는 방향은 언제나 「통과」다. | tools/bump-version.js 자리표() — 판정을 함수 하나로 모으고 `[vNEXT]`·`[vNEXT·라벨]` 을 한 패턴으로 본다(치환은 라벨을 살리고 번호만 갈아 끼운다: `[vNEXT·E²]` → `[v9.N·E²]` — 사람이 손으로 확정할 때 쓴 그 모양이 정본이다 · 엔진_운영배치.js:383). 옛 코드는 같은 패턴이 네 곳에 손으로 적혀 있어 넓히면 갈라지는 자리였고, `/g` 정규식을 상수로 들고 있어 `lastIndex` 가 호출 사이에 남았다(그 자리에서 `lastIndex=0` 을 손으로 껐는데 한 번 잊으면 자리표가 「없다」로 읽힌다) — 매번 새로 만들어 그 자리를 없앴다. 회귀 4건(tests/버전채번.test.js): ①라벨 자리표 3형(민자리·`·E²`·공백 든 `·검수 <해시>`)을 다 잡고 라벨을 보존한다 ②`[vNEXTGEN]`·`[vNEXT9]`·확정 태그 `[v9.5]` 는 안 건드린다(거짓양성 0 — 넓힌 패턴이 멀쩡한 코드를 빨갛게 만드는 F287 축을 같이 막는다) ③두 번 연달아 불러도 같은 답(lastIndex 잔존 없음) ④`--check` 가 라벨 자리표에 적색을 낸다. 변이 3/3 — 옛 패턴(`/\[vNEXT\]/g`)을 그대로 먹여 세 assertion 이 전부 빨개지는 것을 확인했다(탐지력은 픽스처가 지고, 실저장소 검사는 「자리표 잔존 0」 거짓양성만 본다). CI 모사 2547/0. ⚠남긴 것: 확정된 `[v9.N·라벨]` 은 버전 태그 패턴 `\[v9\.(\d+)\]` 에 안 걸려 maxTagInEngines 가 안 센다 — 거짓 적색을 만드는 방향이 아니라 별건으로 둔다. 커밋 bcddc690. |
