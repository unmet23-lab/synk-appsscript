| F388 | 2026-08-12 | 마찰 | SQL 술어 텍스트를 lib 이 내보내고 TS 소비자가 끼우는 패턴에서, 정규식을 담는 순간 TS 템플릿 리터럴이 백슬래시를 조용히 먹는다 — '\s+' 가 's+' 로 접혀 「공백 정규화」가 「s 정규화」가 되는데 문법 오류가 안 난다.  ■ 어디서 났나 강사 반단위 피드백 설계 §8-2(G2 「맞음」 자동 하강)를 짓다가 밟았다. 술어 텍스트가 층을 셋 지난다: lib/보고서교정.js 의 JS 문자열 → supabase/functions/correct/index.ts 의 TS 템플릿 리터럴 → SQL 문자열 리터럴. 가운데 층에서 \s 는 「정의되지 않은 이스케이프」라 s 가 된다.  ■ 왜 굵직한가 — 증상이 없다 SQL 은 's+' 를 문법 오류로 안 본다. 그냥 s 를 접는 정규식으로 정상 동작한다. 그래서 나타나는 증상은 「어떤 맞음은 걸러지고 어떤 맞음은 안 걸러진다」뿐이고, 그건 이 설계 §7 이 급소로 지목한 바로 그 모양(판정이 두 벌)이다. 술어 정본화가 막으려던 사고를 이스케이프 층이 되살린다.  ■ 커밋 전에 잡았다 — 하지만 다음 사람은 못 잡는다 0be2fae 에서 POSIX 문자 클래스로 바꿔 막았다: \s→[[:space:]] · \.→[.]. 백슬래시가 0 이면 세 층 어디서도 뜻이 안 바뀐다. 회귀(tests/보고서교정.test.js ⑨)가 lib·소비자 양쪽에서 백슬래시 0 을 센다 — 다만 그 회귀는 맞음술어 하나에만 걸려 있다.  ■ 실측 — 지금은 아무도 안 샜다 저장소의 술어 패턴 전수 3벌을 원문자로 쟀다(JSON.stringify 로 재면 개행이 \n 으로 보여 거짓양성이 난다 — 처음에 그렇게 잘못 쟀다): 지금유효술어 0개·정규식 안 씀 / 풀술어 0개·정규식 안 씀 / 맞음술어 0개·정규식 씀. 기존 둘은 정규식을 애초에 안 써서 이 함정에 안 걸렸고, 정규식을 쓰는 첫 술어가 이번 것이다. 즉 이건 「재발」이 아니라 「첫 도달」이다.  ■ 처방 후보 (지금은 안 한다 — 1번째라서) 2번째가 오면 술어 export 전수에 백슬래시 0 을 강제하는 공용 회귀로 올린다. 그때는 호출부마다 고치지 말고 「정규식을 담는 술어는 POSIX 문자 클래스만」을 기계가 강제하는 자리를 하나 만든다. | |
