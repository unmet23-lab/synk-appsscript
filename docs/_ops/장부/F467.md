| F467 | 2026-08-15 | 교정 | 로그인 벽 있는 인스타를 내장 브라우저로 열었다가 유호님 교정(「구글 크롬으로 해줘 왜 굳이 이걸로」). 원격작업=작업계정 크롬(브라우저열기.js) 규약이 이미 있는데 내장 브라우저를 기본값으로 집었다. | `.claude/hooks/credential-guard.js` 규칙③ — **내장 브라우저**(`Claude_Browser`)로 「로그인해야 쓰는 화면」에 `navigate` 하면 deny. 🔑 **서버를 가르는 것이 이 처방의 전부다**: `claude-in-chrome` 은 유호님의 진짜 크롬이라 로그인 세션이 살아 있어 그대로 통과시킨다 — 안 가르고 막으면 남는 통로가 0이 되어 따를 수 없는 처방이 되고, 그건 우회를 정상 통로로 만든다(F103). 그래서 사유문이 **갈 길 둘을 먼저 준다**: ㄱ 유호님이 보고 클릭할 화면이면 `node tools/브라우저열기.js "<url>"`(작업 계정 크롬 · 폴백 없이 정지) · ㄴ 내가 직접 조작해야 하면 `mcp__claude-in-chrome__*`. 🔑 **막은 이동은 «간 것»으로 안 적는다** — 기록을 먼저 하면 규칙①이 가지도 않은 자격증명 화면에 있다고 판정한다(그 자리를 겨냥한 회귀를 따로 세웠다). 목록은 **실측된 5종만**(인스타그램=F467 실물 · 페이스북 · Gemini · 구글 작업 계정 자산 · Supabase 대시보드) — 넓히는 순간 localhost 프리뷰·공개 문서에서 매 세션 오탐이고, 오탐이 일상이면 BYPASS 가 손버릇이 된다. 예외는 기존 `--allow-next` 한 수를 그대로 쓴다(새 스위치 0). 회귀 17/17(신설 5 — 막는 쪽·통과하는 쪽·처방 되먹임을 같은 무게로) · **변이 7/7**(구멍 0 · 미적용 0). ⚠ **새 훅 0** — 이미 `navigate` 를 받아 URL 을 적던 훅에 판정만 얹었다(장치 개수 불변). ⚠ 변이 ②③(서버 판정을 무력화·반전)이 **기존 F084 검사**를 빨갛게 만든다 = 과잉 차단 쪽 방어가 이미 서 있다는 실측 증거다. ⚠ 남는 한계 = 이 훅은 도구 입력만 본다 — 클릭으로 도달했거나 이미 열려 있던 화면은 규칙③도 못 본다(규칙①과 같은 사각 · 머리말에 적혀 있다). |
