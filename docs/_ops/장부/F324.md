| F324 | 2026-08-11 | 실수 | 비가역 조작(토큰 삭제)을 화면을 보기 전에 추측으로 권고했다 — 「08-04 첫 토큰은 값을 아무도 모르는 죽은 토큰이니 지우시는 게 좋다」고 했는데 목록 화면에 「Last used within the last week」가 떠 있었다(살아서 쓰이는 중). 유호님이 그대로 눌렀으면 도는 기능이 죽었다. 삭제·폐기 권고는 대상을 직접 연 뒤에만 낸다 | ⏸ 「1번째」가 아니다 — 같은 축(대상을 안 열고 삭제·조작을 판정) 선례 F030·F058(마찰신호.md 08-04)이 있고, 그 사건들로 선 상주 조항 「현재 상태는 현재를 읽어 판정한다」 뒤에도 뚫렸다. 권고는 채팅층이라 지금 걸 훅이 없다(후보=비가역 권고에 「직접 연 증거」 요구 칸 — 미채택). 다음 재발은 검토가 아니라 기계화 착수 문턱이다 |
