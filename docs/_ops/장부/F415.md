| F415 | 2026-08-14 | 마찰 | 일감을 «재기 전에» 선언하지 않아 10분에 4번 자리를 잃었다 — 세션 시작 훅이 모든 세션에 같은 🟢 3건을 뿌리는데 점유 표식은 보드 선언 하나뿐이라, 「열고·재고·판단하는 동안」이 통째로 무보호 구간이다. 실측 08-14 local_71748fd0: #Q73(DESIGN.md 적색)의 절별 부피를 재는 사이 434254de 가 선언했고, 이어 #Q72·#Q49 를 선언하려 하자 f30e7914·f8fd4dd0 가 이미 있었고, 결정 큐 배달 결함(꼬리 조각 5·절 제목 2·걸러진 3)은 재료를 읽는 사이 옆 세션이 전부 닫아 0건이 됐다. 🔑 가드는 제대로 일했다 — 중복 구현은 0이고 새는 것은 컨텍스트다(그래서 적색도 경보도 없다). 처방 = 후보가 정해지면 «재기 전에» 보드 선언부터 하고 그 뒤에 연다(선언 30초 · 계측 5분). ⚠ F406(표식이 반대쪽으로 늙음)과 축이 다르다 — 그건 낡은 점유 표식이고 이건 표식이 서기 «전»의 창이다. | tools/대기열.js `--집기 #Qnn --파일 "…"` (커밋 ff20c805) — 「재기 전에 선언한다」를 한 줄로. 성격 = **프린터**다: 보드 파일을 도구가 직접 쓰면 board-guard(PreToolUse: Write)가 안 돌아 겹침 판정이 통째로 우회되므로, Write 할 «내용»만 조립해 내고 마지막 문은 그대로 board-guard 에 둔다. 「이미 남이 잡았나」는 새로 안 짓고 같은 파일의 점유() 를 그대로 부르며, 칸 상한은 board-guard 의 MAX_CELL 을 그 파일에서 읽는다(같은 값을 두 곳에 안 적는다). 발동 조건은 같은 커밋에서 훅 안내 줄에 동봉했다 — 「고르면 재기 전에 선언부터」(안 부르는 손잡이는 없는 것과 같다). 검증 = 회귀 tests/대기열.test.js 6건 추가(13/13 · 큐 4스위트 52/52) · 변이 1/1(집기가 파일을 쓰게 고치면 「안 쓴다」가 적색). 곁들여 잡은 것: 공용 lib 호출이 ROOT(=자료 뿌리 · CLAUDE_PROJECT_DIR 로 갈아끼운다) 기준이라 픽스처 뿌리를 물리는 순간 모듈이 통째로 사라졌다 → 코드는 __dirname 에서 부른다. ⚠ 천장 = 무보호 창을 0으로 만들지 못한다(선언 자체가 Write 라 그 왕복은 남는다) — 5분을 5초로 줄일 뿐이고, 두 세션이 같은 초에 조립하면 여전히 board-guard 가 뒤엣놈을 막는다(그게 설계다). 대가 3 = ①「만질 파일」은 도구가 못 적어 --파일 필수(비면 거절) ②제목은 상한에서 자르되 조인 키 #Qnn 은 불변 ③유령 선언은 새 장치 없이 board-move·보드수거·인계문이 받는다. 닫은 것 = 없다(손으로 조립하던 것은 도구가 아니라 사람 행위라 끌 통로가 없다). |
