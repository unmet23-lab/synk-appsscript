| F637 | 2026-08-18 | 마찰 | **F628 의 폴백이 클라우드 컨테이너에서 「도달 불가」다 — 훅 파일 자체가 없어서 폴백 줄이 실행되지 않는다.**  F628 은 08-18 에 「폴백 넣고 머지층은 포기」(유호 픽)로 닫혔고, 착지 커밋 `4a43530b` 가 `tools/githooks/prepare-commit-msg` 의 변수 조건에 폴백을 넣었다. 그런데 이 클라우드 세션에서 커밋해 보니 트레일러가 **여전히 안 박힌다.**  실측(이 세션 `45a428ae` · 2026-08-18): - 내 커밋 `96a5763e` 에 `Session-Id` 트레일러 **없음**(`git log -1 --format=%B` 로 직접 확인) - `.git/hooks/prepare-commit-msg` **없음**(`ls` = No such file or directory) - `git config core.hooksPath` **미설정**(빈 문자열) — 다른 경로로 우회 설치된 것도 아니다 - `CLAUDE_CODE_HOST_SESSION_ID` 는 빈 문자열이고 `CLAUDE_CODE_SESSION_ID` 는 살아 있다  기전: F628 재료 문서(`docs/_ops/커밋주인표식_공백_0818.md` §2)가 이미 이것을 **두 겹**이라 적었다 — ①설치 ②변수. 그 문서는 「설치만으로는 안 고쳐진다(깔아도 27행에서 exit 0)」고 썼는데, **역방향도 참이라는 것이 이번에 실측됐다: 변수 폴백만으로도 안 고쳐진다. 훅 파일이 없으면 그 폴백 줄에 도달할 방법이 없다.** 두 겹 중 한 겹만 닫고 트랙이 종결됐다.  그래서 지금 상태는 「고쳤다고 기록됐는데 안 도는」 장치다 — CLAUDE.md 신뢰성 절의 「스스로 발화하지 않는 장치는 안 돈다」와 「맞는 얼굴로 틀린 값을 내는 자리」에 동시에 걸린다. 그리고 F041(커밋 주인 판별)의 재료가 클라우드 커밋에서 계속 0인 채로 남는다.  ⚠ 안 잰 것: `tools/install-githooks.js` 가 클라우드에서 **안 돌아서** 없는 것인지, 돌았는데 실패한 것인지는 구분하지 않았다(두 경우가 같은 모양이다). 원인 특정 전이라 처방을 여기 적지 않는다 — 이 신고는 관측이다.  발견 경위: Slack 역할 판정 트랙(`45a428ae`)에서 문서를 커밋한 뒤 규약대로 트레일러를 확인하다 걸렸다. | |
