| F637 | 2026-08-18 | 마찰 | 🔴 **클라우드 컨테이너에서 셸(`Bash|PowerShell`) 매처 훅이 «안 돈다» — 등록은 멀쩡한데 발동이 0이다.** 이 저장소의 셸 안전 가드가 전부 그 매처에 달려 있다: `clasp-guard`(라이브 배포·`clasp pull` 차단) · `git-scope-guard`(`add -A`·`commit -a`·`clean` 차단, 커밋 범위 게이트) · `shell-inline-guard` · `code-edit-guard`(+ 그 안에 얹힌 **심장박동**). 새는 방향은 전부 「통과」다. ■ 실측 2026-08-18 (클라우드 `cse_014P5Hsr7yE5p9pdLqwj6X9x` · `e54f1033` · 저장소 `/home/user/synk-appsscript` · 기준 `origin/master@f0d6ed60`) — 서로 독립인 신호 **둘**이 같은 방향을 가리켰다: · ㉠ **`git add -A` 가 통과했다.** 손으로 그 페이로드를 훅에 물리면 `permissionDecision:"deny"` 를 정확히 낸다(`[git-scope-guard] git add -A · git add . 차단 …`). 즉 **로직은 멀쩡하고 발동이 없다.** · ㉡ **심장박동이 한 번도 안 찍혔다.** `code-edit-guard:163` 이 모든 셸 호출에서 `store.박동찍기(input)` 를 부르는데(조기 종료보다 **앞**에 두는 것이 그 훅의 설계다), 상태 폴더에 `track-*.json` 이 **0건**이다. 같은 폴더를 여러 번 지우고 셸 호출을 여러 번 낸 뒤 다시 봐도 0건이었고, 손으로 그 훅을 돌리면 `track-5530e60ed3-e54f1033-….json` 이 **즉시** 생긴다. 폴더가 딴 데 생긴 것도 아니다(`find / -name synk-context-budget` = `/tmp/synk-context-budget` 하나 · `track-*.json` 전역 0건). ■ 대조군 — **다른 매처는 산다.** 같은 `settings.json` 의 `SessionStart` 훅들은 이 세션 시작에 전부 출력을 냈고, `PreToolUse` 의 **`Edit|Write|MultiEdit`** 매처도 실제로 돈다: `repo-staleness` 가 09:17:51 에 `staleness-5530e60ed3-e54f1033-32fd-52a2-ace7-a9c62fddee36.json` 을 **라이브로** 썼다(내 F633 수리가 들어간 직후라 파일명이 그 증거다). 그래서 「settings.json 이 통째로 안 읽힌다」는 후보는 **기각**된다 — 셸 매처만 조용하다. ■ 왜 중대한가 · `clasp-guard` 0번이 **라이브 배포**를 막는 자리다(워크트리 `clasp push` 차단 · `clasp pull` 작업본 덮어쓰기 차단 F040). 클라우드 세션은 지금 그 문을 **열어 두고** 돌고 있다. · `git-scope-guard` 는 F015 실사고(사업문서 2종이 `add -A` 로 커밋·push 됨)의 수리다. 그 사고를 막는 장치가 이 환경에선 없다. · 심장박동이 0이면 `작업본소유자.세션들()` 에 내 기록이 **아예 없어** 「살아있는 세션 0개」가 되고, F633 수리로 자리 id 를 되찾아도 `board-guard` 의 `생사쟀나` 는 여전히 false 다(집합에 내가 없다). 즉 **F633 만으로는 그 사슬이 안 이어진다.** ■ 안 잰 것(F348) · **왜** 안 도는지는 못 특정했다. 후보 ㉠클라우드 하네스가 셸 도구를 다른 경로(샌드박스)로 태워 그 매처의 PreToolUse 를 안 부른다 ㉡도구 이름이 `Bash` 가 아니게 들어온다 ㉢권한 모드가 그 매처를 건너뛴다. 셋 다 **안 갈랐다** — 가르려면 훅 자신이 자기 호출을 기록해야 하는데 그건 별건 편집이다. · `PostToolUse` 의 셸 매처(`commit-noop-guard`·`friction-close-guard`·`track-boundary`·`deploy-freshness`)는 **따로 안 쟀다** — 침묵이 정상 동작인 훅들이라 부재를 부재로 못 읽는다. · 노트북(로컬)에서 같은 검사를 못 돌렸다 — 이 컨테이너 밖은 못 연다. 「로컬도 그렇다」고 말하지 않는다. | |
