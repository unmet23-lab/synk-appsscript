| F642 | 2026-08-18 | 실수 | 러너설치 스크립트가 릴리스에서 형제 파일(win-x64)의 체크섬을 집어, 멀쩡히 받은 215MB 를 남의 값과 대조해 검증이 터졌다 — 유호님 노트북 첫 실행이 여기서 막혔다. 릴리스 JSON 의 body 는 한 줄이라 grep -A2 가 아무것도 못 좁히고 head -1 이 목록 맨 위(win-x64)를 집었다. 안 도는 게 아니라 돌면서 남의 값을 냈고, 그때 낸 처방이 「지우고 다시 받아라」라 따를수록 같은 자리로 돌아왔다(F103) | tools/러너설치.sh 의 추출을 마커(BEGIN SHA linux-x64) 기준 sha_of_linux_x64 함수로 바꾸고 --sha추출 통로를 내 회귀가 실물을 그대로 재게 했다(로직 복제 없음 · F063) + tests/러너설치.test.js 4건 신설: 형제가 앞선 픽스처에서 linux 값을 집는가 · 옛 파이프라인 재현으로 픽스처의 탐지력 증명 · 마커 없으면 빈 값 · 읽기 전용 |
