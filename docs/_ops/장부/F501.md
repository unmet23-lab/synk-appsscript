| F501 | 2026-08-16 | 실수 | 관측층 손잡이가 비ASCII 환경변수 이름이라 POSIX 셸에서 원리적으로 못 선다 — bash export 가 not a valid identifier 로 거절한다(실측 08-16 · PowerShell 은 선다). 형제 저장소 lib/자격증명.js 31~34행이 이미 OS 와 맞닿는 이름은 ASCII 를 실측으로 못박아 뒀는데 08-15 신설 관측층이 MAESTRO_학생번호·EXPO_PUBLIC_합성밟기 로 그 규칙을 어겼다. 지금은 PowerShell 로 우회되나 CI 리눅스에 붙이는 날 통째로 막힌다. | talk 6073ecc — 셸을 건너는 이름 전부 ASCII 개명(MAESTRO_STUDENT_CODE·MAESTRO_PASSWORD·EXPO_PUBLIC_SYNTHETIC_RUN·SYNK_LEGACY_GLYPH_TOOL·SYNK_CONTRACT_TOOL) + 산문뿐이던 규칙을 기계로(lib/자격증명.js 셸이세울수있나) + 회귀 tests/앱환경변수.test.js(흐름머리·EAS키·소스 손잡이 · 분모 가드 포함) · 변이 7/7 구멍0 · 전체 2323/2323 · 재실측 결과 코드 자리 0(잔여는 주석·문서blob·Maestro 내부변수) |
