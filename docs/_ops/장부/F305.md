| F305 | 2026-08-10 | 실수 | 서브에이전트에 「훅이 여기서도 발동하나」를 재게 시키면서 **대조군을 안 줬다.** 그 에이전트는 git-scope-guard 가 원래 허용하는 드라이런(`git clean -n`)이 통과한 것을 보고 「훅이 서브에이전트에서 발동하지 않는다」고 단정했고, 나는 그 말을 받을 뻔했다. 내가 같은 명령을 내 세션에서 돌려 대조하니 **여기서도 안 막힌다** — 가드 규칙 ③ 이 `-n`·`--dry-run` 을 명시적으로 면제한다(`.claude/hooks/git-scope-guard.js:129-133`). 즉 그 프로브는 애초에 훅의 발동 여부를 가를 수 없는 명령이었다.  그 판정을 믿었으면 「서브에이전트엔 기계 벽이 없다」는 거짓 전제로 Fable 분리 설계(`.claude/agents/builder.md`)를 통째로 다시 짤 뻔했다 — 산문 벽 6개가 유일한 벽이라는 결론이 나온다.  재측정 2026-08-10: 차단이 확실하면서 통과해도 무해한 명령(전량 스테이징의 드라이런 · 규칙 ① 에는 드라이런 면제가 없다)으로 바꿔 내 세션과 서브에이전트에서 각각 돌리니 **같은 거절 원문**이 나왔다. 확정: **훅은 서브에이전트의 Bash 호출에도 발동한다.**  처방: 위임하는 프로브에는 「막히는 것」과 「안 막히는 것」을 **한 쌍으로** 주고, 대조군이 없으면 「판정 불가」를 반환하라고 프롬프트에 명시한다. 곁들여 — 낮은 등급 모델은 단정을 잘 하므로 판정이 섞인 위임에는 등급을 올리거나 판정 자체를 회수한다. | CLAUDE.md v8.8 모델 라우팅 조항 — 「판정을 위임할 땐 막히는 것/안 막히는 것을 한 벌로 준다」+「산출물은 호출자가 검증 결정표대로 받는다」 |
