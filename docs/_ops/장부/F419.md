| F419 | 2026-08-14 | 마찰 | 가드가 막기만 하고 처방을 안 줘, 그 자리가 나흘짜리 막다른 골목이 됐다 — 보드껍데기정리의 「미추적이라 안 건드린다」  08-10 에 생긴 0바이트 보드 파일 4개가 08-14 까지 미추적으로 앉아 있었다. 미추적은 이력·stash·reflog 어디에도 없는 유일한 무보호 상태다(F025).  껍데기정리는 그 4개를 매번 정확히 찾아냈다 — 「껍데기」 판정도 통과했고, 건너뛴 사유("미추적 — git 이 모르는 파일이라 안 건드린다")도 분모와 함께 정직하게 냈다. 차단 자체도 옳다(미추적 삭제는 비가역).  틀린 것은 그 옆자리다: 사유만 있고 **다음 손이 없었다**. 그래서 이 도구를 부른 사람은 요약의 마지막 줄 「→ 지울 것이 없다」만 읽고 돌아섰다. 실제로 그렇게 나흘이 갔다.  급소는 출력 순서였다 — 미추적만 남은 상태가 바로 「지울 것 0개」라 조기 종료로 빠진다. 처방을 그 아래 두면 **정작 필요할 때만 안 보인다**.  F103 과 같은 축이되 반대 모양이다: F103 은 「따를 수 없는 처방」이었고 이것은 「처방이 아예 없다」다. 둘 다 결말은 같다 — 사람이 절차를 새로 발명하게 되고, 발명이 필요한 절차는 아무도 안 따른다. | tools/보드껍데기정리.js — 미추적 껍데기가 있으면 그대로 붙여 넣으면 통과하는 add·commit 두 줄을 경로까지 짚어 낸다(출력 자리는 조기 종료 «위»). 회귀 tests/보드껍데기정리.test.js 2건 — 처방을 내는가 + 그 처방을 되먹이면 실제로 지워지는가(맹점 ③) · 변이 2/2 적색 · 실저장소 4건 회수 완료(2abd45ee·2f212d8d·b64c8d4c) — 0a185536 |
