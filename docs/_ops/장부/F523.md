| F523 | 2026-08-17 | 마찰 | 마찰 장부엔 점유층이 없다 — 열린 ▶ 하나에 세 세션이 동시에 붙었다 (대기열은 이미 푼 문제인데 장부로 안 갔다)  실측(2026-08-17 오전 · `local_07a6e4fd`): F520 이 「▶ 후보 둘(㉠자동커밋이 pre-commit 을 안 지난다 / ㉡게이트가 정본 변경에만 발동한다) — **여는 세션이 위 둘을 먼저 가른다**」로 열려 있었다. 10분 안에 **세 세션**이 각각 그것을 집었다:  - PR #27 `worktree-f520-daejang-gate-scope` — 「F520 — 대장 동봉 게이트의 «부모»가 하나뿐이다(정본만 보고 생성기를 안 본다)」 · 보드 선언 `28d70bde` **10:10** - PR #28 `worktree-f520-ledger-gate` — 「F520 «왜» 가르기 — 대장 동봉 게이트의 발동 조건이 생성기를 안 본다」 · 보드 선언 `a9564a05` **10:13** - 나(`07a6e4fd`) — 같은 진단에 도달해 트랙을 열기 직전 `작업가지.js` 출력에서 위 둘을 보고 **멈췄다**  셋 다 제목에 같은 판정이 적혀 있다. #27·#28 은 **보드 선언 1커밋뿐 코드 0줄**이라, 내가 안 멈췄으면 열 줄짜리 발동 조건 하나를 세 세션이 각각 고치고 셋이 같은 파일에서 충돌했을 것이다.  🔑 급소는 **같은 문제를 옆 층이 이미 풀어 뒀다**는 것이다. `tools/대기열.js` 에는 점유층이 있다(F406 · 회귀 `tests/대기열점유.test.js` · 머리말이 「점유 판정」을 절로 두고 「잡힌 줄은 «누가·왜»와 함께 드러낸다」까지 못박음). 이번 세션의 SessionStart 도 그 층 덕에 「🔒 〖지금〗 표식인데 **이미 잡힌 줄 4건**」을 정확히 걸러 줬다. 그런데 `tools/friction.js` 에는 점유 개념이 **0**이다(grep 히트는 전부 `friction-F041` 류 git 태그). 즉 **대기열의 ▶ 는 잠기고, 장부의 ▶ 는 안 잠긴다.** 같은 「열린 일감」인데 한쪽만 보호된다.  ⚠ 그리고 이 사고는 조용하다 — 셋 다 규약을 지켰다. 각자 보드에 선언했고, 남의 줄을 안 건드렸고, 워크트리+PR 로 갔다. 규약을 다 지켜도 나는 방향은 「중복 착수」다. 새는 방향이 「정상 통로」라 눈에 안 띈다(CLAUDE.md 신뢰성 맹점 ④).  🔴 **안 고쳤다 — «어떻게 세 세션에 닿았나»를 안 쟀기 때문이다.** SessionStart 등재 훅은 `session-handoff`·`session-decisions`·`philosophy-card`·`review-runs` 넷이고, **미해소 마찰을 띄우는 훅은 없다**(실측). 나는 `이해대장.js` 를 grep 하다 우연히 F520 을 만났다. #27·#28 이 무엇을 보고 갔는지는 모른다(인계문일 수도, 같은 grep 일 수도). 닿는 경로를 모른 채 점유층부터 지으면 **오진이 처방의 얼굴을 하고** 안 읽히는 자리에 장치가 하나 더 는다. 여는 세션이 먼저 잴 것: ①#27·#28 의 인계문에 F520 이 실려 있었나 ②실려 있었다면 점유는 장부가 아니라 **인계문 배달층**이 져야 하는가.  ▶ 그래서 이 신고에 «처방»은 안 적는다. 잰 것만 적는다 — 「대기열엔 있고 장부엔 없다」와 「10분에 셋」. | ⏸ ▶ 를 쟀다 — ①은 **아니다**로 닫히고, ②는 **전제가 안 선다**. 그리고 못 재는 이유가 갈렸다: 분모가 없는 게 아니라 **재는 층이 없다**. (실측 2026-08-17 · `local_c587271c` · 판정 자리 = 이 보류문 + 보드 줄 `docs/_ops/보드/c587271c.md`)  ■ ① #27·#28 의 인계문에 F520 이 실려 있었나 — **아니다.** `docs/_ops/인계문/*.md` 16벌 전량 grep. F520 신고 커밋 `7389c2cc` 를 실은 인계문은 **1/16**(`62eb453e.md`) 뿐이다. 그리고 바통은 1:1 이다 — `.claude/hooks/session-handoff.js:32` 의 `store.take(cwd)` 가 **집으면서 지운다**(08-04 실측 결함 ①의 처방이 거기 박혀 있다: 전에는 전역 1개라 셋 중 마지막이 이겼다). `7f15ce3c.md`·`61c67fe7.md` 안의 F520 언급은 전부 **자기가 낸 커밋**(보드 선언 `28d70bde`·`a9564a05` 이후분)이다 — 받은 것이 아니라 낸 것이다. → 그러므로 ②(「점유는 장부가 아니라 인계문 배달층이 져야 하는가」)는 **물을 수가 없다.** 배달층은 이미 1:1 이라 셋에 닿을 방법이 없다.  ■ ②′ 그러면 무엇이 셋에 닿았나 — **여전히 못 잰다. 못 재는 이유가 새로 잡혔다.** 「이 일감을 어떻게 찾았나」를 적는 자리가 **어느 층에도 0**이다(보드 칸에도, 커밋에도, PR 본문에도). F523 을 쓴 `07a6e4fd` 가 「`이해대장.js` 를 grep 하다 우연히」라고 **손으로** 적어 둔 한 줄이 이 축의 유일한 기록이다. 기록이 손글씨뿐이면 다음에도 손글씨가 있어야만 보인다.  ■ ③ 오늘 새 실측 — 나는 **두 번 튕겼고, 경로가 서로 달랐다.** · PR #24(변이 재실행 후 머지) ← **인계문 배달**. 내 첫 메시지가 그것을 ▶로 지목했다 → `local_02e398f7` 가 이미 선언 중이라 board-guard deny. · F501(비ASCII 환경변수 이름) ← **장부**. `node tools/friction.js --open` 손 호출 → `local_1cc21d07` 가 이미 선언 중이라 board-guard deny. 두 경로 다 점유를 **안 보고** 일감을 내밀었다. 그리고 **두 번 다 board-guard 가 선언 시점에 잡았다** — 중복 구현 0줄.  ■ ④ 그래서 과녁이 옮겨간다 — 점유층은 «없다»가 아니라 «맨 끝에 있다». 보드가 점유층이고 오늘 **2/2** 를 잡았다(F523 이 걱정한 「셋이 같은 파일에서 충돌」은 실제로는 안 난다). 새는 것은 중복 구현이 아니라 **고르기까지 태운 값**이다 — 두 번의 고르기에 도구 호출 26회를 쓰고 산출 코드 0줄로 튕겼다(오리엔테이션 몫이 섞여 있으니 26은 **상한**으로 읽는다 · 분모 = PR #24 쪽 13 + F501 쪽 13). 🔴 그리고 그 값은 **어디에도 안 남는다.** `board-guard` 의 deny 에는 기록층이 0이다(실측: `.claude/hooks/board-guard.js` 에 `appendFile`·`writeFile` 0건). F523 이 「10분에 셋」을 셀 수 있었던 것은 그 세션이 **우연히** 남의 PR 목록을 봤기 때문이고, 오늘 내 2건도 이 글을 안 쓰면 똑같이 사라진다. 즉 **충돌률의 분모가 상시 0**이라, 「점유를 앞당길 값이 있나」를 판정할 재료가 원리상 안 모인다.  ■ ⑤ 왜 지금 안 닫나 — **안 고쳤다. 사유가 F523 원문과 다르다.** 원문 사유 = 「닿는 경로를 안 쟀다」. 그건 위에서 쟀다(①은 아니다 · ②는 전제 붕괴). 남은 사유 = **재는 층부터 없다.** 순서는 ㉠ deny 를 세는 자리 → ㉡ 며칠 세기 → ㉢ 그 다음이 장치다. ㉠ 없이 장치부터 지으면 오늘 실측한 「2/2 를 이미 잡는다」를 못 보고 안 새는 자리에 장치를 하나 더 얹는다. ⚠ 대가를 적는다: **㉠도 장치다** — 「새 장치 0」 원칙과 정면이고, 세션 시작 출력이 한 줄 더 는다. 그래서 이 보류는 ㉠을 짓자는 제안이 **아니라**, 짓기 전에 무엇을 알아야 하는지를 못박는 기록이다. 여는 세션은 ④의 「26회·0줄」이 되풀이되는지부터 본다 — 한 번뿐이면 값이 없다. |
