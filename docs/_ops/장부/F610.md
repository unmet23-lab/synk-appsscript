| F610 | 2026-08-18 | 마찰 | 일감을 «고르는» 목록이 「이미 잡힌 것」을 안 가른다 — 오늘 세 세션이 같은 F605 를 동시에 집었고 내가 세 번째였다 (`tools/friction.js --open`)  ■ 실측 2026-08-18 (`local_5653b6a5` · 우연이 아니라 **내가 그 목록에 걸린 당사자**다)   · 세션 시작에서 새 트랙을 잡으려 `node tools/friction.js list --open` 을 열었다 — 열림 10건이 «점유 표기 없이» 나열된다.   · 그중 F605 를 골랐다. 근거로 `node tools/board.js | grep F605` 를 돌렸고 **선언 0** 이 나와 「아무도 안 잡았다」로 읽었다.   · 워크트리를 파고 보드 줄을 쓰는 순간 `board-guard` 가 막았다: 그 자리는 **두 세션이 이미 선언**했다     (`local_9b74b64b` 가지 `worktree-f605-jsonl-appendonly-denominator` · `local_82d71975` 가지 `worktree-f605-jsonl-ledger-guard`      · 뒤에 PR #105·#106 으로 확인). 내가 **세 번째**였다.   · `board.js` 가 0 을 낸 이유는 그 줄들이 **미머지 가지 안**에 있어서다 — 08-15 「워크트리+PR」 확정 뒤로 코드 트랙의 선언은 거기 앉는다(F521·F595).  ■ 분모 (전수 · 스크립트 실측 · 자기 줄과 🎫 놓음을 갈라 낸 뒤의 값)   열림 10 = **남이 잡음 4** + 놓인 줄(🎫) 1 + 빈자리 5.   🔴 남이 잡은 4건이 **전부 «가지에만»** 있다 — F543·F604(가지 `worktree-f604-negative-claim-audit`) · F598(가지 `…jsonl-appendonly-denominator`) · F605(가지 셋).   즉 그 목록이 부르는 10건 중 **4건은 부르면 안 되는 것**이었고(40%), 메인 보드만 읽는 층은 그 4건을 하나도 못 본다.   보류 79 = 남이 잡음 7(전부 메인) + 빈자리 72 — 이쪽도 표기는 0이다.   🔑 1차 셈은 4건이 아니라 다른 4건이었다. 둘이 거짓양성이라 갈라 냈다: ①**내 자신의 선언**(`줄들()` 은 워크트리 디스크를 읽으니 내 파일이 그대로 들어온다 — board-guard 는 F291 로 이미 가른 자리) ②**🎫 로 놓인 줄의 인용**(그 표식의 뜻은 정반대다 — 집어도 된다). 재는 층을 안 가르면 이 장치가 그대로 다음 F368 이 된다.  ■ 왜 신호인가 — **같은 병을 이 저장소가 이미 한 번 고쳤다**   F406 이 대기열에서 정확히 이것을 고쳤다: 「〖지금〗 표식은 붙인 시점의 사진이라 남이 집는 순간 늙는다」 → 점유 3신호(씨앗·줄참조·파일자리)를 세우고, 세션시작이 「열림 20 · **남이 안 잡은 것 12**」로 낸다.   그런데 **장부 목록엔 그 층이 0이다.** `tools/friction.js` 전문에 「점유」라는 낱말이 없다. 같은 저장소에서 한 목록은 가르고 다른 목록은 안 가른다 — CLAUDE.md 「2번째 = 실수가 아니라 시스템 결함」이 정확히 이 모양이다.   그리고 새는 방향이 **「부른다」**다. F406 이 자기 머리말에 「새는 방향이 『부른다』라 제일 나쁘다」고 이름 대어 적어 뒀는데, 그 경고가 옆 목록에는 안 옮겨 갔다.  ■ 손해가 어디까지 갔나 — **board-guard 가 막아 중복 구현 0**   마지막 문은 섰다(F585 가 세운 그 문). 버려진 것은 조사분뿐이다: 워크트리 하나 · 커밋 0 · 되돌림 1분.   ⚠ 그래서 이 병은 **안 아프게 재발한다** — 매번 「가드가 막았으니 괜찮다」로 끝나고, 다음 세션이 같은 목록에서 같은 자리를 또 판다. F585 가 「비켜난 뒤 남는 것이 조사분뿐이라 다음 세션이 같은 자리를 또 판다」고 적은 것과 같은 축인데, 저기는 **선언 전에 묻는 조회가 없다**(→ `board.js --점유`)이고 여기는 **고르는 목록 자체가 안 가른다**다. 물어야 답하는 문과, 부르기 전에 먼저 말하는 목록은 다른 층이다.  ■ 안 잰 것 (F348)   · F번호 문자열 매칭의 **거짓양성률은 안 쟀다** — 신고문을 인용만 한 줄도 잡힌다. 대기열 F406 이 같은 대가를 「방향은 『못 고름』이고 그쪽이 안전하다」로 이미 판정했으니 그 판단을 승계했지, 새로 잰 것이 아니다.   · 「목록을 보고 실제로 몇 번이나 잘못 집었나」의 이력은 안 쟀다 — 오늘 1건(나)만 직접 증거다. PR #105·#106 이 서로를 봤는지도 모른다.  ■ 처방 후보 (이 신고는 재료가 아니라 **이 트랙에서 짓는다**)   ㉠ `--open`·`--보류` 가 각 행에 점유를 **먼저 말한다** — 메인 + 미머지 가지(`보드.가지줄들` · F521)를 함께 보고, 자기 줄과 🎫 놓음을 갈라 낸다. 못 훑었으면 「0」이 아니라 **모름**이라고 말한다(F207).   ㉡ 「메인 + 가지를 합쳐 활성줄을 낸다」가 지금 **두 곳에 각자 적혀 있다**(`tools/대기열.js:191~206` · `.claude/hooks/board-guard.js:1190~`). 내가 세 번째가 되면 CLAUDE.md 「3번째 = 원인을 쓸 수 없게 만든다」에 걸린다 → 공용 통로 하나로 뽑는다.   ㉢ 아무것도 안 한다 — board-guard 가 막으니까. ⚠대가: 조사분 버리기가 세션마다 반복되고, 그 손해는 **아무 데도 안 적힌다**(오늘도 내가 신고를 안 썼으면 0으로 보였다). | ㉠㉡ 둘 다 섰다. ㉠ = `86e0b494`(PR #107 · `local_5653b6a5`) — `--open`·`--보류` 가 각 행에 점유를 먼저 말한다(메인 + 미머지 가지를 함께 보고 자기 줄·🎫 놓음을 갈라 낸다 · 못 훑으면 「0」이 아니라 모름 · 회귀 13 · 변이 9/9). ㉡ = 이 커밋(`cloud_d427746a` · 유호 지시 2026-08-18) — 신고문이 「두 곳에 각자 적혀 있다」고 지목한 `tools/대기열.js:보드행들` 과 `.claude/hooks/board-guard.js` 의 겹침 분모를 `tools/lib/보드.js:활성줄전량` 한 벌로 모았다. 이제 합치기·활성거르기·중복접기가 그 파일에만 있고, 소비자 셋(대기열 점유 · board-guard 겹침 · friction 장부점유)은 자기 «판정»만 진다. 두 소비자를 받으려 늘린 칸 셋은 각각 「없으면 무엇이 틀리는가」를 머리말에 적었다 — ㉠`가지제외파일`(가지 쪽에만 건다: 그 파일의 메인 줄은 접기 씨앗이라 남아야 한다 · F221) ㉡`분모`(활성 거르기 «전»의 층별 날수 — 「배열이거나 null」 계약이 활성 0건과 «못 읽음»을 못 가른다 · F207) ㉢`가지우선`(같은 줄이 두 층에 다 있을 때의 이름표 — 대기열은 가지 쪽, board-guard 는 메인 쪽이 옳고 후자는 F291 면제가 거기 걸려 있다). 접는 키도 `trim()` 으로 올렸다(메인은 파일을 쪼갠 것, 가지는 diff 를 판 것이라 꼬리 공백이 갈린다 — 날문자로 접으면 둘째가 「가지에만 있는 남의 선언」으로 읽혀 정당한 선언을 거짓 차단한다 · F221). 회귀 `tests/활성줄전량.test.js` 14 신설(기존 185 무변경 · 199 fail 0) · **변이 8/8 구멍 0**(옛 통로 원문 복원 두 형태 포함 — 되살리면 등록층 래칫과 실동작 회귀가 동시에 운다) · CI 모사 4567 pass 4432 **fail 0**. 래칫 키는 소스 내용이다(행번호 키는 무관한 편집마다 운다 · F618). ⚠ 안 잰 것 = ①글자가 «완전히 같은» 줄이 서로 다른 가지에 둘 있으면 `가지끝파싱` 이 이미 하나로 접는다(픽스처로 실측 · 옛 판도 같은 입력이라 이 트랙이 만든 변화가 아니고, 겹침 판정은 살아남은 줄이 내므로 방향은 「막는다」다) ②「목록을 보고 실제로 몇 번이나 잘못 집었나」의 이력은 신고문 그대로 여전히 안 쟀다. |
