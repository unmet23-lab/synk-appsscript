| F352 | 2026-08-12 | 마찰 | 던진 검수 런이 완주로 알리는데 회차 1 산출이 0이고, --런처분 은 검수기록 장부에 안 실어 배포 게이트가 닫힌 채 남는다 — 18분과 벤더 비용이 통째로 증발하고 미실행이 통과와 같은 모양이 된다 (F207 계열)  실측(`local_ed8b5124` · 런 `검수-2026-08-12T0514-5def98` · `--base c0f148aa` · 파일 104개 · 18분): 훅이 「✅ 완주했는데 아직 안 주운 런」으로 알렸고 `--런처분` 은 「도장」만 찍었다. 그런데 `docs/_ops/검수기록.jsonl` 은 **33줄 그대로**고 최신 항목은 내 런보다 2시간 앞선 `03:05:07`(대상 `52eac5b2`)이며, 현재 배포 지문 `166617a4` 를 가진 줄은 **0건**이다. 그래서 `--확인` 은 런 전과 똑같이 「이종 검수 기록이 없다」를 낸다 — 즉 18분과 벤더 비용을 쓴 검수가 **배포 게이트 관점에서 통째로 없던 일**이 됐다.  급소는 `tools/codex-review.js:1956` — `상태: 코드 === 0 ? '완주' : '실패'` 로 **종료코드만** 보고 완주를 적는다. 바로 아래 1957 주석은 「상태를 못 적어도 **결과는 장부에 있다**」를 전제로 깔지만, 이 판이 정확히 그 전제가 깨진 자리다(장부 append 는 `:1866`, 즉 `main()` 이 거기까지 갔어야 한다).  방증: 체크포인트 `synk-검수체크포인트/cec367f48f-9202d1d65a3f/` 에 `구조-2`·`산문-2` 만 있고 **`구조-1`·`산문-1` 이 없다** — 회차 1 산출이 0인데 부모는 0 을 받아 완주로 적었다. `로그-{검수,기능}-{1,2}.txt` 는 넷 다 0바이트.  처방 후보: 완주 판정에 「장부가 실제로 늘었나」를 넣는다(검수 갈래에 한정 — `--심문`·`--제안`·`--기각` 은 각자 다른 장부로 가니 같은 잣대를 대면 거짓 실패가 된다). 🚫**게이트를 배포집합으로 좁혀 우회 금지**([[deploy-drift-session-start]] 의 F240 처방 ② 재제안 금지와 같은 축).  ⚠ 이 판정은 「던지기」 갈래만 실측했다 — 포그라운드 런도 같은지는 **안 쟀다**(포그라운드는 20~32분이라 셸 상한 10분을 넘는다). | `tools/lib/검수런.js` 에 공용 통로 `자식환경()`·`자식인가()` 신설 + `tools/codex-review.js` 두 자리 적용. 🔑원인은 종료코드 판정(:1956)이 아니라 **환경 상속**이었다 — `병렬실행`(:947)이 `env: { ...process.env, SYNK_REVIEW_CHILD: '1' }` 로 회차 자식을 띄워 던진 런의 `SYNK_REVIEW_RUN_ID` 가 **손자에게 그대로 샜고**, 손자가 부모보다 먼저 끝나며 `마감()` 으로 부모 런에 도장을 찍었다. 부모는 그때 회차 1 폴백을 태우는 중이라 `검수기록.jsonl` append(:1866)에 못 갔다 — 그래서 「완주」인데 장부 33줄 그대로, 게이트는 닫힌 채. 실증=격리 픽스처(임시 `SYNK_REVIEW_RUNS`·벤더 호출 0)로 손자가 부모 런을 「진행」→「실패」로 덮는 것을 재현했다. 처방 두 층: ①`자식환경()` 이 런ID 키를 지운다(깨끗함) ②`마감()` 이 자식 표식을 보고 건너뛴다(등록층이 새는 날의 두 번째 벽 — 새 spawn 자리가 `...process.env` 를 펼쳐도 거짓 도장이 안 찍힌다). 회귀 `tests/검수런.test.js` ⑩ 2건이 **한 층씩 따로** 잰다(한 테스트로 묶으면 두 벽이 서로를 가려 둘 다 탐지력 0) · **변이 2/2** 실측 · 38/38. ⚠원 진단의 처방 후보(「완주 판정에 장부가 늘었나를 넣는다」)는 **안 넣었다** — 원인이 상속이라 그 자리엔 갈래별 장부가 갈리는 거짓 실패 위험만 남는다(검수/심문/제안이 각자 다른 장부). |
