| F366 | 2026-08-12 | 마찰 | 전제가 죽었는데 집행만 남아 나흘간 이관마다 커밋을 하나씩 더 썼다  `tools/board-move.js` 는 완료 줄 이관을 커밋 2개로 갈랐다(아카이브 선기록 → 보드 제거). 그 분리는 원칙 ④(F102)의 집행이고, 전제가 코드 바로 아랫줄에 적혀 있었다 — 「보드는 상시 여러 세션이 더럽히는 파일이다(실측 08-05 01:04: 한 순간에 6개 세션)」.  그 전제는 F250(2026-08-08 · 보드를 세션별 파일로 가름)에서 죽었다. 대상이 내 지문 파일 하나가 됐고, 쓸어갈 통로가 사라졌다 — `auto-commit` 은 `제외폴더` 에 `docs/_ops/보드/` 를 통째로 넣었고(F248), `git add -A` 계열은 `git-scope-guard` 가 막고, 남의 세션이 내 파일을 이름으로 집는 것은 F073 이라 `board-guard` 가 막는다.  즉 **F250 때 `auto-commit` 은 따라왔는데 `board-move` 는 안 따라왔다.** 같은 이주가 두 곳을 건드려야 했는데 한 곳만 갔고, 안 간 쪽은 「더 안전한 옛 거동」이라 아무 신호도 안 냈다. 새는 방향이 조용한 게 아니라 **비싼** 쪽이었다: 이관마다 커밋 1개 + 줄이 빠진 파일이 껍데기로 남아 262개 중 244개가 죽은 파일이 됐다. `board-guard` 는 PreToolUse 라 파일을 쓸 때마다 그 폴더를 통째로 훑으므로 그 값은 세션 수만큼 곱해진다. 실측 2026-08-12: 하루 커밋 449건 중 보드·인계문 장부가 290건(65%)이고, 세션당 장부 커밋이 08-07 의 2.2 에서 4.7 로 두 배가 됐다(세션당 «산출»은 2.0~2.6 으로 7일 내내 일정).  일반화: 「전제 → 집행」을 머리말에 적어 두는 것만으로는 전제가 죽는 날을 못 잡는다. 전제를 바꾸는 이주 커밋이 **그 전제를 인용한 자리들**을 찾아내게 해야 한다. 이번엔 사람이 숫자를 재다가 우연히 걸렸다. | board-move 를 원자 커밋(아카이브 삽입 + 보드 제거 한 커밋)으로 바꾸고 줄이 다 빠지면 파일째 지운다 — f9968565. 껍데기 판정은 tools/lib/보드.js 의 껍데기인가 하나로 올려 board-move 와 새 tools/보드껍데기정리.js 가 파생시킨다 — 47361523. 이미 쌓인 232개는 de5bd51b 에서 한 커밋으로 정리(선언 0줄 유실 실측). 회귀 18건(보드이관 +7 · 껍데기정리 11) · 변이 4/4 · CI 2730/0. |
