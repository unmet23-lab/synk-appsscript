| F295 | 2026-08-09 | 마찰 | 재질의금지 가드가 압축된 메모리 인덱스 한 줄에서 거짓양성을 낸다 — 🚫목록과 ⏳목록이 한 줄에 합쳐지면 해소표식(🚫재질의)이 주제어와 인접하지 않아 위반으로 잡힌다. 08-08 대압축이 만든 자리이고 증상은 이 기계 모든 세션의 test-ci 적색이다(CI 에선 메모리 폴더가 없어 skip 이라 안 보인다). 인덱스에 🚫를 붙여 풀었지만 근본은 가드가 줄 단위로만 본다는 것 | |
