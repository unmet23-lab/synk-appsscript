| F583 | 2026-08-17 | 마찰 | `워크트리갇힘` 이 **고유 내용이 0인 트리**에도 최고 경보(「완주한 트랙이 여기서 사라진다」)를 낸다 — 세는 값이 `rev-list --count master..HEAD` 라 **머지 노드 하나가 「1커밋」**으로 잡힌다.  실측 2026-08-17 (세션 `90a98dcc` · 시작 훅이 나에게 낸 그 경보): - 대상 = `…/2cbd5748-…/scratchpad/reconcile` @ `6df8320b` 「Merge origin/master — 좌초된 master 직접 커밋 6건 반입」 - `rev-list --count master..6df8320b` = **1** ⇒ 지금 훅이 쓰는 값. 「master 미포함 1커밋」 + 🔴 최고 경보. - `rev-list --count --no-merges master..6df8320b` = **0** ⇒ 내용을 지닌 커밋이 하나도 없다(두 부모가 이미 master 안). - `diff-tree --cc 6df8320b` = **빈 출력**(SHA 줄 하나뿐) ⇒ 손 충돌해결(evil merge)도 0. ⇒ 이 트리가 쥔 고유 내용은 **증명 가능하게 0**이다. 지워도 잃을 것이 없다. 그런데 화면은 F403 실사고(수리 163줄·회귀 245줄 유실)와 **똑같은 문구**로 나온다.  왜 결함인가 — 새는 방향이 「경보」라 조용하지 않고 시끄럽다. 그 시끄러움이 값이다: 경고는 사람이 무시해야만 남는 게 아니라 **읽히지 않아야 사라진다.** 도구 자신의 주석(`tools/작업본소유자.js:542`)이 이미 그 값을 적어 뒀다 — 「배경 소음이 된 경고는 진짜가 섞여도 안 읽힌다」. 지금 그 조건이 자기 함수 안에서 성립했다.  ⚠ 주석 544~546 의 「판정하지 않는다」는 **옳고, 유지한다.** 그 줄이 못 가른다고 적은 것은 「재타이핑된 같은 수리」(patch-id 휴리스틱)이고, 그건 여전히 기계가 못 가른다. 이 신고가 겨누는 것은 그 부류가 아니라 **DAG 구조로 결정되는 부분집합** 하나뿐이다 — 부모가 전부 master 안이고 combined diff 가 빈 머지. 휴리스틱이 0이라 오분류가 원리상 없다.  과녁 = `tools/작업본소유자.js:565` (`워크트리갇힘`). 갈래 = 「내용 0」을 세 번째 칸으로 갈라 조용한 처방(`git worktree remove`)만 내고, 나머지는 지금 문구 그대로 둔다. | `tools/작업본소유자.js` — 깨끗한 갇힘 후보를 **다시 둘로** 가른다: 🧊(유실 후보) / 🫧(볼 것 없음). 착지 `593ed7f3` (PR #90).  무엇이 실제로 막나 — 판정을 `고유내용()` 한 벌로 세웠다(같은 판정을 두 곳에 안 적는다): ① `rev-list --count --no-merges master..HEAD` = 0 ⇒ 내용을 지닌 커밋이 전부 이미 master 안 ② 그 머지들의 `diff-tree --cc` 가 전부 빔 ⇒ 손 충돌해결(evil merge)도 0 둘이 서야만 🫧 다. 한 번이라도 못 재면 `잼:false` 로 **시끄러운 쪽에 그대로 둔다**(F207 세 칸). 점유 판정이 **먼저**라 손이 붙은 트리는 내용이 0이어도 처분 처방을 못 받는다(F553 순서 보존). 🫧 는 훅에서 한 줄이고 `깨워야할갇힘` 에서 빠져 **그 칸만으로는 세션을 안 깨운다** — 내용 커밋이 하나라도 생기면 즉시 🧊 로 올라오므로 자기교정된다.  신고 원문을 그대로 되넣어 봤다(F083) — 변이 ①⑥ 이 정확히 옛 동작(커밋 수로만 세기 · 🧊 에서 안 빼기)이고 둘 다 회귀가 빨개졌다. 변이 **7/7 · 구멍 0 · 미적용 0** · 기준선 초록 분모 123.  실저장소 대조: `6df8320b`(reconcile 트리)가 🔴 9줄 「완주한 트랙이 여기서 사라진다」 → 🫧 조용한 칸 + `worktree remove` 처방으로 내려갔다.  ⚠ 닫으면서 남기는 것(억지로 안 닫는다): 그 머지가 «고른 조합»(어느 쪽을 취했나)은 **안 쟀다.** ①②가 서도 그 선택은 master 와 다를 수 있고, 잃는 것이 「새로 쓴 줄」이 아니라 「선택」이라 처방에 그렇게 적고 `git reflog` 를 준다. 이건 F583 이 신고한 결함이 아니라 새 장치의 대가다. |
