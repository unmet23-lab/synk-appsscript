| F558 | 2026-08-17 | 실수 | 초록을 분모 없이 읽고 「안 재고 있었다」로 번역했다. tests/트랙경계.test.js 42건이 전건 초록인 것만 보고 「그 초록이 F529 를 0건 재고 있었다」고 단정했는데, 실제로는 F529 검사가 6건 있었다(grep -c F529 = 11). 부정 판정은 값이 살 수 있는 층을 먼저 열거하고 그 전부를 연 뒤에만 쓴다는 F348 을 안 지켰다 — grep 한 번이면 됐다. 그 거짓 실측이 커밋 메시지와 회귀 파일 머리말에까지 박혔다(머리말은 정정 커밋으로 고쳤고 커밋 메시지는 SHA 라 못 고친다). | |
