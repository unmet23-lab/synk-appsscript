| F106 | 2026-08-05 | 교정 | 프로세스 목록을 「창(세션) 수」로 번역해 보고 — Get-Process claude 23개를 세션 24개로 읽고 그 위에 로그아웃 원인 추론(오래된 창 11개가 무효 토큰 보유)까지 쌓았다. 실측은 부모 1개(앱 본체)+부속 22개(gpu/renderer/utility/crashpad)로 앱은 하나였고 세션은 유호님 화면대로 8개. F089 재발 — 어느 층을 재는지 정하기 전에 셌다. | ⏸ /evolve v8.7(2026-08-09) 판정=안 닫음 — 막을 조항은 «이미 있고» 지켜지지 않은 재발이라, 프로즈를 더 쓰는 것이 처방이 아니다. 닫으려면 기계(훅·도구)가 필요하다. 근거=docs/지침_이력.md:492 (v8.7 개정 기록). 다시 재지 않는다 — 08-12 두 세션이 각각 이 12건을 「미측정」으로 재대조했다(F386). |
