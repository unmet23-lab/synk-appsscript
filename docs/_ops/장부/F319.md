| F319 | 2026-08-10 | 마찰 | 이종 검수가 「확인 불가」를 찍고도 종료코드 0 을 낸다 — 미실행이 통과와 같은 모양으로 나간다.  실측 2026-08-10 17:50~18:0x · `local_fe590634` · `node tools/codex-review.js --commit 39c65d1e`: 콘솔은 「🔴 확인 불가 — 기능체크가 **안 돌았다**(통과가 아니다): codex 기능체크 2/2회 타임아웃(900초 소진)」 을 찍었는데 프로세스 종료코드는 **0** 이었다. 스킬 정본(`.claude/skills/deploy/SKILL.md` 4-B)은 「종료 코드: 0=차단급 0건 / 1=P0·P1 있음 / **2=확인 불가** · **2를 0으로 읽지 말 것** — 안 돈 것이지 통과가 아니다」 라고 못박았는데 실제 코드가 그 계약을 안 지킨다.  새는 방향은 언제나 통과다: 종료코드로 게이트를 삼는 호출자(스크립트·훅·다음 세션의 `&&` 연쇄)는 「타임아웃으로 반쪽만 돈 검수」를 「차단급 0건」으로 읽는다. 사람이 콘솔 글자를 읽어야만 참이 되는 계약은 계약이 아니다.  ⚠ 배포 게이트 자체는 fail-closed 였다 — 그 뒤 `--확인` 이 여전히 「이 배포 내용(#fp:36ada333)에 이종 검수 기록이 없다」로 남았다(거짓 기록을 안 남겼다). 즉 지금 새는 곳은 게이트가 아니라 **종료코드를 읽는 쪽**이다. 그래서 「게이트가 막았으니 무해」로 닫지 않는다 — 두 판정이 갈라져 있는 상태 자체가 F207 계열(미실행이 통과 모양)이고, 갈라진 쪽은 언제든 먼저 소비된다.  ▶ 처방 후보(이 세션은 `tools/codex-review.js` 를 만지지 않았다 — F314 트랙 `62bb6ca8` 의 파일이다):    ① 확인 불가 경로가 exit 2 를 내도록 되돌리고 ② 회귀는 「타임아웃 픽스처 → exit 2」 로 못박는다    (`tests/이종검수.test.js` 에 등록층 검사가 이미 있다). | 철회 — 이 신호는 내 오독이었다(신고자 `local_fe590634` 가 같은 세션에서 반증).  종료코드 0 은 도구가 낸 게 아니라 **내가 `node tools/codex-review.js … 2>&1 | tail -60` 으로 파이프를 끼운 탓**이다. 파이프라인의 종료코드는 마지막 명령(`tail`)의 것이다. 실증: `node -e "process.exit(2)" | tail -1` → 0 · 파이프 없이 → 2.  도구는 계약을 지키고 있었다 — `tools/codex-review.js` 끝의 `process.exit(main(...))` 과 확인 불가 경로의 `return 2` 가 그대로 있고, **그 자리 주석이 이미 이 함정을 적어 두었다**: 「종료코드 2 는 파이프를 한 번 끼우면 사라지므로(F308 ①), 본문 문구가 마지막 방어선이다」. 곧 F319 는 F308 ① 의 중복 재발견이고, 재발한 쪽은 도구가 아니라 **읽는 사람(나)** 이다.  남는 교훈은 하나다: 긴 도구를 백그라운드로 돌릴 때 `| tail` 로 출력을 줄이면 그 순간 종료코드가 죽는다. 줄이려면 파이프 대신 출력 파일을 그대로 읽고, 종료코드는 파이프 없이 받는다.  ⚠ 이 철회는 「기능체크 900초 하드캡」과 **다른 건**이다 — 그쪽은 살아 있고 별도 신호로 남긴다. |
