| F532 | 2026-08-17 | 마찰 | master 가 빨간 채로 있다 — 새 회귀 파일이 「훅 회귀는 전부 새 통로를 쓴다」 검사를 어긴 채 머지됐다. 실측 2026-08-17: 02e398f7 이 PR #24 머지 전 test-ci 를 돌리자 tests/훅통로.test.js:188 이 tests/형제초록.test.js:61 을 짚어 fail 했고, 내 3커밋이 없는 master 에서 같은 검사를 돌려 대조하니 똑같이 났다(조건 하나만 바꾸기 — 내 트랙 것이 아니다). 그 파일은 bedfe9fb(feat: 형제 저장소가 빨간지 as 세션이 본다 · F518 ㉡ · Session-Id local_61c67fe7)가 방금 넣은 것이고, 61c67fe7 은 아직 산 세션이라 이 자리는 그 세션 몫이다 — 남의 파일이라 F073 이 내 손을 막는다(중복으로 집지 말라고 여기 적는다). 무게: 「주인 없는 적색은 남의 배포를 막는다」가 정본 조항인데 이건 주인이 있는 적색이라 더 조용하다 — 다음 세션들이 test-ci 를 돌릴 때마다 이 적색을 만나고, 매번 「내 것인지」를 가르는 대조 비용을 각자 치른다(02e398f7 은 그 대조에 1판을 더 썼다). 기전은 CLAUDE.md 가드 맹점 ③·④ 가 아니라 순서다: 공용 통로(tests/lib/훅띄우기.js)를 강제하는 검사는 있는데, 그 검사가 새 파일을 막는 자리가 커밋 게이트가 아니라 test-ci 뿐이라 머지 뒤에야 운다. | ⏸ 내 트랙 것이 아니다 — 같은 적색을 F535 로 신고한 세션이 worktree-f535-toolspawn-false-red 에서 잡고 있다(2026-08-17 · 내 CI 모사에서도 형제초록.test.js:61 그대로 재현). 내 커밋은 이 적색을 물려받았을 뿐이라 인용만 했다. |
