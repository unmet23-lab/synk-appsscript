| F384 | 2026-08-12 | 실수 | board-guard 의 「안 만진다는 명시」가 🚫 한 글자에만 걸린다 — 관례의 다른 표기는 선점으로 세어진다  F221 이 「🚫 로 시작하는 항목은 안 만진다는 명시지 선점이 아니다」를 못박았고, F354 가 그 사정거리를 조각에서 구절로 넓혔다. 그런데 부정을 **읽는 어휘가 `🚫` 하나뿐**이라, 보드의 실제 관례인 「⚠남의 산 작업본 `a`·`b` 무접촉」 표기는 여전히 선점으로 세어진다.  실측 2026-08-12 (local_c84a3b8a): 첫 보드 선언에서 만지지 «않을» 파일을 정직하게 적었다가 `tools/precommit.js` 겹침으로 차단됐다. 바로 앞 세션 5c3f51e1 의 살아있는 줄이 같은 표기를 쓰고 있다(그쪽은 그 파일들이 마침 남의 활성 줄과 안 겹쳐서 통과했을 뿐이다).  🔴 급소는 「막혔다」가 아니라 **내 첫 반응이 그 표기를 지우는 것이었다**는 데 있다 — board-guard 자기 주석이 F221 자리에서 정확히 그 우회를 기록해 뒀다("나는 그 표기를 지워서 통과시켰다 — 거짓 차단이 곧 우회다 F103"). 같은 우회가 다른 표기로 되살아난 것이라 같은 뿌리의 2번째다: 정직하게 적을수록 벌받으면 관습이 죽는다.  처방 후보: 부정 어휘를 목록 하나에서 파생시키고(🚫 · 무접촉 · 안 만진다 · 무변경 · 편집 0) 그 목록에서 차단 안내문까지 파생시킨다 — 가드가 무엇을 부정으로 읽는지 스스로 말하게 한다 (F094 가 완료/활성 어휘에서 이미 쓴 처방과 같은 모양). ⚠ 넓히다 반대로 새면 안 된다 — 부정어가 «그 조각 안»에 있을 때만 접는다(F354 의 경계 그대로). | tools/lib/보드.js 부정어휘[{패턴,보기,예}] 한 목록 + board-guard 차단문이 그것을 싣는다(aaec1818). 신고문 처방(어휘 늘리기)만으로는 안 닫혔다 — 실측(만지는 파일 칸 752개): 목록 누락은 불변 8·무접촉 1(그대로 3은 전건이 이미 🚫 스팬 안이라 얻는 게 0). 그리고 신고 원문 표기(⚠남의 산 작업본 a·b 무접촉)는 낱말을 더해도 마지막 구절만 걷힌다. 사정거리를 구절→조각으로 넓히는 길은 실측이 죽였다 — 조각 끝 부정 + 구절 2개 이상 303건 중 96건이 앞 구절에 진짜 경로를 인다(넓혔으면 그 96건이 통째로 「자리 없음」=새는 방향). 앞표식도 안 지어냈다: 「남의 작업본」 5건 중 4건이 이미 🚫…=남의 작업본 꼴(4:1). 그래서 고친 것은 어휘가 아니라 **가드가 스스로 말하게 한 것**이다 — 급소는 차단이 아니라 세션의 첫 반응이 표기를 지우는 것이었고, 지울 이유는 가드가 읽는 모양을 안 말해서였다(F103·F221 재발). 매처(부정꼬리RE)와 안내문(비켜남표기안내)이 한 목록에서 파생돼 「말한 표기가 안 통하는」 자리가 원리상 없다. 실저장소 델타: 752칸 중 바뀐 칸 7·경로가 떨어진 칸 1(= 신고 원문 그 자리 · 나머지 6은 경로 없는 산문). 회귀 tests/board-guard.test.js +5 · 104/104 · 보드·훅 계열 251/251 · 변이 4/4 물림(①낱말 제거=신고 재현 ②안내문 파생 끊기 ③차단문에서 안내 제거 ④불변→불변식? 넓히기). ⚠④는 처음 살아남았다 — 픽스처가 「불변식③」이라 꼬리 글자가 우연히 막아 줬고, 경계를 짚는 「교차 불변식」을 넣어서야 물렸다 |
