| F298 | 2026-08-09 | 실수 | 옛 글자 금칙이 「신고하는 행위」로 번식했다 — 내가 보드 줄에 U+5C3A 를 써서 저장소 적색을 냈고(Actions 결제 정지로 clasp-guard 가 로컬 전 스위트를 도는 동안이라 모든 세션의 push 게이트가 막혔다), 그것을 신고한 세션이 신고문에 그 글자를 그대로 인용해 같은 적색을 한 벌 더 만들었다(docs/_ops/보드/8bfd9bdb.md:1). 처방을 따르면 같은 위반이 되는 F103 계열. 처방: 신고문·보드·커밋에서 금칙 글자는 U+ 표기나 String.fromCodePoint 로만 짚는다 — tests/문서문자.test.js 의 실패 메시지가 이미 U+ 표기를 쓰니 그 형식을 그대로 인용하면 안전하다. | board-guard ⑦ 가 막는다(커밋 6c861df7) — 쓰는 순간 층이 새로 생겼다. F083 변이 실측(격리 사본, 신고 원문 그대로 재투입): ①원문 재투입 deny · 사유는 U+ 표기만(리터럴 0) ②「남의 위반을 신고하며 인용」한 줄도 deny — 번식 경로가 막혔다 ③처방대로 고친 줄은 allow(F103 통과). 정의는 .claude/hooks/lib/옛글자.js 하나로 모았고(소비자 3층) [단일 출처]·사본부활 회귀가 갈라짐을 막는다. 회귀 4건 신규. 🔴 잔여(숨기지 않는다): 이 층은 **보드 파일만** 막는다 — 실측 ④ 에서 docs/문서_지도.md 형태의 일반 문서는 allow 다. 실사고 3건 중 2건이 보드였고 3번째(문서_지도)는 여전히 커밋 뒤에야 잡힌다. 전역 쓰기 가드는 훅 신설·등록·라우팅 확장(라우팅은 훅보다 넓어야 한다)이 따로 필요해 여기서 끊었다. |
