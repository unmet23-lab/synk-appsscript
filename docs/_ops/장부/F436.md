| F436 | 2026-08-14 | 실수 | F435 가 **쓰기 통로에서도** 성립한다 — `--운영 --적용` 을 붙인 마이그레이션이 조용히 리허설에 떨어졌다  `tools/원격SQL.js` 는 `--운영` 을 모르는 낱말로 흘린다(F400 처방이 `배포대조.js` 한 파일에만 들어갔다 · F435 가 이 사실을 **읽기** 측면으로 이미 신고했다). 이 세션은 그 F435 조각을 **읽고 나서도** 쓰기에서 밟았다: `node tools/원격SQL.js supabase/migrations/20260812200000_class_c11.sql --적용 --운영` 이 종료코드 0 으로 성공하고, 대상은 `synk-core-rehearsal` 이었다.  왜 나쁜가 — 읽기판보다 한 단계 위다. 읽기가 밀리면 「낡은 숫자를 본다」로 끝나지만, 쓰기가 밀리면 **승인한 적 없는 프로젝트에 DDL 이 떨어진다.** 이것은 2026-08-07 실사고(앱·왕복은 리허설에 붙어 있는데 `--적용` 이 운영에 떨어졌고 그 사실을 적은 커밋도 보드 줄도 없었다)와 **방향만 반대인 같은 병**이다. 그때 세운 과녁 게이트는 이 형태를 못 막는다 — 게이트는 「지금 과녁이 운영인가」를 묻는데, 여기서는 과녁이 **애초에 안 바뀌었기** 때문에 게이트가 볼 것이 없다. 즉 승인 키가 승인만 하고 조준을 안 한다.  이번엔 무해했다. 그 조각은 `do $migration$` + `if not exists` 로 멱등이고 리허설엔 이미 같은 판이 서 있어 no-op 이었다(적용 뒤 조각수 28 불변으로 확인). 무해한 이유가 「도구가 막았다」가 아니라 「마침 멱등이었다」인 것이 이 신호의 요점이다.  무엇이 잡았나 — 도구가 **대상을 소리 내어 읽는 한 줄**뿐이다(`[원격SQL] 대상 ▸ synk-core-rehearsal`). `배포대조.js` 처럼 갈아탄 사실을 알리는 것도 아니고, 그냥 늘 찍는 줄이라 눈이 미끄러지기 쉽다. 실제로 이 세션은 출력을 읽고서야 알았다.  바른 호출(이번에 쓴 것) = 과녁 덮어쓰기 + 승인 키를 **둘 다**:   `SUPABASE_PROJECT_REF=<운영ref> node tools/원격SQL.js <파일> --적용 --운영` 하나만으로는 각각 다른 이유로 틀린다 — env 만 주면 게이트가 막고, `--운영` 만 주면 조용히 리허설에 쓴다.  처방 후보(고르지 않고 적어 둔다 · 되돌림 비용 순): ① `원격SQL.js` 가 `--운영` 을 `배포대조.js` 와 같은 뜻으로 받아 과녁을 갈아탄다 — F400 처방을 형제 도구로 전파하는 것이고, F435 가 이미 가리키던 자리다. ② 그게 부담이면 최소한 **모르는 `--` 플래그를 만나면 죽는다**(조용한 무시 금지). 오타 하나가 과녁을 바꾸는 통로에서 「모르는 낱말은 흘린다」는 그 자체로 결함이다. ③ ①을 하더라도 회귀는 「`--운영` 을 붙였을 때 대상이 운영인가」를 **도구마다** 물어야 한다 — 한 파일만 고치고 형제를 안 본 것이 F435 의 내용 그 자체다(같은 판정을 두 곳에 적으면 갈라진다 · CLAUDE.md 가드 맹점 ④). | 조준을 공용 통로로 올렸다 — talk `56f5e8e` (`lib/자격증명.js 읽기()`).  `--운영` 은 한 낱말에 두 뜻(①조준 ②쓰기 승인)인데 `과녁판정()` 은 ②만 봤고 ①은 도구마다 따로 이어야 했다. 이제 `읽기()` 가 ①까지 진다 — 도구 10개가 전부 반환된 env 의 `SUPABASE_PROJECT_REF` 한 칸에서 ref 를 꺼내므로 그 칸을 통로에서 맞추면 전부 맞는다. 호출부를 하나씩 고치지 않은 이유가 이것이다(F400 이 `배포대조.js` 한 파일만 고쳐서 F435·F436 이 났다).  실물 확인: `node tools/원격SQL.js <읽기.sql> --운영` 이 이제 env 덮어쓰기 없이 `Synk Core (qiwxeddw…)` 를 가리키고, 갈아탄 사실을 한 줄로 말한다. 플래그가 없으면 리허설 그대로다.  대가(같이 적는다): · F400 처방 ①(모르는 플래그는 die)은 **안 올렸다** — 도구마다 아는 낱말이 달라 공용 통로에서 세면 멀쩡한 인자를 죽인다. 그건 도구 몫으로 남는다. 그래서 「`--운영`의 오타」(`--운영ㅇ` 같은 것)는 여전히 조용히 흘러 리허설로 간다 — 이 해소가 안 닫는 자리다. · 틀릴 때의 모습: 갈아탄 줄이 안 찍히는데 대상은 바뀌어 있는 것(그 반대도). 회귀가 둘을 따로 잰다. · `배포대조.js` 의 중복 안내는 줄여서 그 도구만 아는 것(갈아타도 GET 만 친다)만 말하게 했다 — 같은 판정을 두 곳에 적으면 한쪽만 고쳐지는 날 갈라진다.  회귀 `tests/자격증명.test.js` 4건(+28 전체) · **변이 2/2 잡힘**(조준을 죽이면 「갈아탄다」와 「말한다」가 각각 빨개진다) · talk 스위트 2067→**2071/2071**(fail 0 · skipped 0). ⚠ 회귀가 기대값을 절대값으로 안 박는다 — ref 미지정이면 `파일읽기()` 가 실저장소 `.env` 를 물어와 CI 와 답이 갈린다(F296). 처음 그렇게 썼다가 로컬에서 빨개져 잡았고, 지금은 「같은 입력에 `--운영` 만 뺐을 때 안 움직이는가」라는 차이를 잰다. |
