| F633 | 2026-08-18 | 마찰 | **클라우드 세션에서 「내가 누구인가」가 통째로 빈다 — 쓰는 층엔 폴백이 이미 있는데 읽는 층 12곳이 `CLAUDE_CODE_HOST_SESSION_ID` 단일이다.** F628 의 형제이되 층이 다르다: 그쪽은 커밋 트레일러(연락용 id)였고, 이쪽은 **자리 식별 지문**(누구 줄인가·누구 락인가·살았나)이다. ■ 실측 2026-08-18 (클라우드 컨테이너 `cse_014P5Hsr7yE5p9pdLqwj6X9x` · 저장소 `/home/user/synk-appsscript` · 기준 `origin/master@f0d6ed60`) · 환경: `CLAUDE_CODE_HOST_SESSION_ID` **없음**(빈 문자열도 아니라 미정의) · `CLAUDE_CODE_REMOTE_SESSION_ID=cse_014P5Hsr7yE5p9pdLqwj6X9x` · `CLAUDE_CODE_SESSION_ID=e54f1033-32fd-52a2-ace7-a9c62fddee36`. ■ 기전 — **쓰는 쪽과 읽는 쪽이 서로 다른 질문을 한다** · 쓰는 층엔 폴백이 이미 있다: `.claude/hooks/lib/handoff-store.js:216 trackSessionId()` 와 `.claude/hooks/track-collision.js:79` 가 둘 다 `HOST || input.session_id` 다 — 그래서 **심장박동·만진기록은 클라우드에서도 `input.session_id`(=세션 UUID) 키로 제대로 쌓인다.** · 읽는 층은 폴백이 0이다. `process.env.CLAUDE_CODE_HOST_SESSION_ID` 를 **직접** 읽는 비-테스트 자리 12곳: `.claude/hooks/board-guard.js:607` · `tools/작업본소유자.js:65` · `tools/board-move.js` · `tools/대기열.js:1039,1087` · `tools/작업가지.js:178` · `tools/인계문수거.js:91` · `tools/friction.js:320,695,1073` · `tools/게이트.js:74` · `tools/bump-version.js:193` · `tools/판정예측.js:367,405` · `tools/마누스.js:226` · `tools/codex-review.js:229,2117`. · 왜 갈렸나 = **공용 통로가 훅 전용이었다.** `trackSessionId(input)` 이 그 통로인데 인자로 훅 stdin 을 요구한다. 훅이 아닌 도구는 그 `input` 이 없어서 부를 수가 없고, 그래서 12곳이 각자 `process.env` 를 직접 읽는 옛 통로로 남았다. 즉 「호출부마다 따로 적었다」가 아니라 **「공용 통로에 도구가 못 들어갔다」** 다. ■ 새는 방향은 전부 「통과」 아니면 「따를 수 없는 처방」이다 (직접 실행해 잰 것만 적는다) · `board-guard.js:607` `내지문 = 지문('')` = `''` → :608 `생사쟀나 = … && !!내지문 && …` 가 **영구 false** → 검사 ⑧(살아있는 남의 세션이 이미 선언한 트랙 겹침)이 deny 대신 「생사를 못 재 막지 않았다」 경고로 내려앉는다. 그 파일 601~606 행이 스스로 「가르는 재료는 나 자신이다 — 내 지문이 그 집합에 없으면 그 집합은 못 믿는다」라고 적어 뒀는데, 클라우드에선 그 재료 자체가 없다. · `작업본소유자.js:65` `나지금 = ''` → `살았나` 의 `(나지금 !== '' && s.sid === 나지금)` 절이 **꺼진다**. 그 절은 F264 실사고의 수리다(박동은 `Edit|Write` 에서만 뛰는데 읽기·조사만 30분 한 세션이 자기 파일을 자기 것으로 증명 못 해 `git-scope-guard` ⑧ 이 자기 커밋을 막았다). 클라우드 세션 전부에 **그 사고가 상시 재장전**돼 있다. · `대기열.js:1087` → `{오류:'세션 지문을 못 읽었다($CLAUDE_CODE_HOST_SESSION_ID) — 내 보드 파일을 못 고른다.'}` — 즉 클라우드 세션은 `--집기` 로 대기열을 **원리상 못 집는다**(처방이 「그 변수를 채워라」인데 채울 수 없다 · F103 형태). · `작업가지.js:178` → `null` · `인계문수거.js:91` → `''` · `friction.js:1073 --이세션` → `''` · `게이트.js:74` 지문 → `null`. · `bump-version.js:193` · `friction.js:320` 락 주인 = `'unknown/' + pid` → **모든 클라우드 세션이 같은 이름**을 쓴다(락 주인 표기가 세션을 못 가른다). ■ REMOTE 로 폴백하면 안 되는 자리다 — 실측으로 갈랐다 F628 이 트레일러에 넣은 값(`cse_…`)을 보드 지문으로 그대로 쓰면 **소비자가 거부한다**: `지문('cse_014P5Hsr7yE5p9pdLqwj6X9x')` = `'014p5hsr'` → hex 가 아니다 → `파일지문('014p5hsr.md')` = `''`(=주인 없음) · `줄의지문('… cse_014P5Hsr… …')` = `[]` · `줄이말하나(줄, sid)` = `false`. 훅 주석의 「보드 지문은 어느 쪽이든 같다 — `board-id.지문()` 이 접두를 벗긴다」는 **`지문()` 한 함수에 대해서만 참**이고, 그 값을 받는 `파일지문`·`줄의지문` 이 hex 를 요구한다는 것은 어디에도 안 적혀 있다. 반면 `CLAUDE_CODE_SESSION_ID` 는 UUID 라 앞 8자가 hex 이고(`e54f1033`), **쓰는 층이 이미 그 값으로 키를 만든다** — 읽는 층을 거기 맞추면 쓰는 층·읽는 층·파일명 규약 셋이 한 값으로 모인다. ■ 남는 자리(이 신고로 안 닫는다) — 연락용 id(트레일러 `cse_…`)와 자리 식별 지문(`e54f1033`)이 클라우드에서 **다른 값**이라, `tools/lib/보드근거.js:64` 가 트레일러를 8자로 잘라 만든 지문은 내 보드 지문과 영영 안 맞는다(= 내 커밋이 「남의것」으로 표시된다). 트레일러 축은 유호 픽 「폴백 넣고 머지층은 포기」로 이미 닫힌 자리라 재제안하지 않고 **관측만** 남긴다. ■ 안 잰 것(F348) · `input.session_id` 와 `process.env.CLAUDE_CODE_SESSION_ID` 가 같은 값인지는 이 신고 시점엔 **안 쟀다**(수리 트랙에서 상태 파일 이름으로 잰다). · 노트북(로컬) 환경은 이 컨테이너에서 못 연다 — 「HOST 가 있으면 옛 동작 그대로」는 코드로만 보장하고 회귀로 못박는다. | **공용 통로 신설 + 옛 통로 금지 래칫** — 착지 `2f16fac8`(본체) · `93a5c92b`(CI 적색 2건) · `16f2ec44`(변이 구멍 1) · PR #145(draft). 신설 `.claude/hooks/lib/세션식별.js` 가 판정을 하나로 진다 — **id 를 «쓰임»으로 가른다**(뭉치면 한쪽이 반드시 틀린다): `연락id()` HOST→REMOTE(트레일러·메시지 · **내부 UUID 로는 안 내려간다**) · `자리id()` HOST→훅stdin→`CLAUDE_CODE_SESSION_ID`(상태 키·보드 파일명 = 쓰는 층과 같아야 하는 값) · `지문()` 자리id 의 8자리인데 **hex 가 아니면 `''`** · `표기id()`(락·기록) · `진단()`(분모 보고) · `환경변수들`·`모름env()`. 읽는 층 **14곳**을 그 통로로 돌렸다(board-guard·git-scope-guard·repo-staleness·track-collision·auto-commit·edit-stamp·credential-guard·friction-close-guard·memory-status-guard·design-guard·commit-noop-guard·session-report / 작업본소유자·board·board-move·대기열·작업가지·인계문수거·friction·게이트·bump-version·판정예측·마누스·codex-review). 쓰는 층(`handoff-store.trackSessionId`)도 같은 함수를 부르게 해 **쓰는 쪽과 읽는 쪽이 한 값**이 된다. ■ 왜 REMOTE 가 아니라 UUID 인가 — 실측으로 갈랐다: `board-id.지문('cse_014P5Hsr…')`=`'014p5hsr'` 는 hex 가 아니라 소비자가 전부 거부한다(`파일지문('014p5hsr.md')`=`''` · `줄의지문()`=`[]`). ■ 실동작: 이 컨테이너에서 라이브 훅이 `staleness-…-e54f1033-32fd-52a2-ace7-a9c62fddee36.json` 을 실제로 썼고(수리 전엔 `…-nosid.json`), `작업본소유자` 가 「미커밋 0건」에서 **16건**으로 뒤집혔다. ■ 회귀 `tests/세션식별.test.js` **13건** — 층별 폴백 · 「연락은 UUID 로 안 내려간다」 · **소비자 계약**(board-id 가 그 지문을 실제로 받는가) · 쓰는 층↔읽는 층 동일성 · 옛 통로 금지(픽스처 8종이 탐지력을 지고, 실저장소는 거짓양성 0 + **훑은 파일 수**를 함께 낸다 · 못 읽으면 skip F296) · **자기 처방이 그 가드를 통과하는지**. **변이 8/8 구멍 0 · 미적용 0**(첫 판은 7/8 — `모름env` 목록이 낡아도 아무도 안 우는 구멍이었고 `16f2ec44` 로 닫았다). ■ 전량 CI 모사 **4656 · fail 0 · skipped 130**. ⚠ 원격 run 은 **0건**(Actions 소진 · 리셋 9/1)이라 진실 층은 **미검증**이다. ■ 기존 회귀 9건이 이 변경으로 깨졌다가 전부 고쳤다 — 원인이 둘이었다: ㉠테스트가 「모름」을 HOST 하나만 비워 만들었다(러너 의존 · `모름env()` 로 교정) ㉡**8자리 지문이 이 저장소에 둘**이다 — 보드 파일명(hex 요구)과 인계문 파일명(`session-report:951` · hex 무관). 인계문수거를 `지문()` 으로 바꿔 자기 파일을 못 찾았다. 합치지 않고 모듈 머리에 두 규칙을 명시했다. ■ 안 닫은 자리(재제안 아님 · 관측만) — 클라우드에선 트레일러(`cse_…`)와 보드 지문(`e54f1033`)이 **다른 값**이라 `보드근거.js:64` 의 커밋 주인 대조가 내 커밋을 「남의것」으로 읽는다. 트레일러 축은 유호 픽(F628 「폴백 넣고 머지층은 포기」)으로 이미 닫힌 자리다. ■ 그리고 **이 수리만으로는 사슬이 안 이어진다** — F637(클라우드에서 셸 매처 훅이 안 돈다) 때문에 심장박동이 0건이라, 자리 id 를 되찾아도 `board-guard` 의 `생사쟀나` 는 여전히 false 다. |
