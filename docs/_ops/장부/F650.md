| F650 | 2026-08-19 | 마찰 | 목차 재생성(session-report.js writeHandoffFile ②)이 만료 인계문을 무조건 지운다 — 클라우드 세션 하나가 `/close` 한 번에 42개를 지웠다(커밋 `74f3655` · 2026-08-18). 이번엔 전부 추적본이라 이력에서 복원했지만, 같은 삭제가 **미추적** 파일에 닿으면 이력·stash·reflog 어디에도 없어 영영 유실이다(F025). 하필 그 파일은 인계가 **유일한 통로인** 계정(폰·클라우드 — 대화 맥락이 안 넘어간다)의 유일한 사본이고, 이 함수는 훅이 부르는 조용한 보조 통로("실패해도 조용하다")라 지워도 아무도 못 본다. 🔑 인계문수거.js 머리말은 이 위험을 이미 적어 두고 있었다 — "커밋이 곧 보호고, 12시간이 시한이다" — 그런데 그 문장을 강제하는 장치가 삭제하는 쪽에 0이었다(적어만 두고 기계로 안 옮긴 자리). 🔑 직전 세션의 처방은 「지문이 빈 값이면 삭제를 건너뛴다」(클라우드만 막기)였는데, 조건을 하나씩 바꿔 대조하니 **지문이 있는 세션도 똑같이 4/4 를 지웠다** — 지문은 원인이 아니었다. 처방을 그대로 넣었으면 로컬 세션이 미추적 유일본을 지우는 본체가 그대로 열린 채 「닫았다」가 될 뻔했다. (직전 인계문이 이 건을 「F641」로 불렀으나 이 저장소 장부엔 그 번호가 없다 — 그 세션이 지문이 없어 기록 자체를 못 남겼다.) | writeHandoffFile ② 의 삭제 한 줄에 게이트 둘 — ㉠**지문을 모르면 삭제 통로를 안 연다**(제 이름도 못 대는 세션은 남의 유물을 거둘 자격이 없다 · git 도 안 부른다) ㉡**HEAD 에 없는 파일(미추적)은 안 지운다**(`HEAD인계문()` = `git ls-tree -r --name-only HEAD -- <폴더>`) · git 을 못 부르면 `null` 로 받아 **삭제 0**(못 물어본 것을 「없다」로 번역하지 않는다 F207·F348). 미추적본은 이제 인계문수거.js 가 커밋한 **뒤에야** 만료로 걷혀 두 도구의 순서가 비로소 맞는다. 회귀 `tests/인계문삭제.test.js` 9건(픽스처 저장소만 · 실저장소 검사 0). 변이 실측 2방향 — 게이트 제거 시 5건 적색, `ls-tree` 의 `-r` 만 빼면 2건 적색(`-r` 이 없으면 HEAD 목록이 디렉터리 한 줄뿐이라 이름이 하나도 안 잡혀 **조용히 삭제 0** 이 되는데, 그건 안전한 방향이라 보존 검사만으로는 전부 초록이다 — 그래서 「추적된 만료본은 실제로 걷힌다」를 같은 무게로 뒀다 · 맹점 ④). 곁들여 잡은 것: 회귀 자신이 플레이키였다 — 픽스처가 `Date.now()` 를 파일마다 다시 읽어 `at` 이 1ms 갈리면 목차 펼침/링크 배치가 뒤집힌다(같은 코드로 9/9 → 8/9 실측). 한 기준에서 벌려 박아 얼렸다(F281). 대가: git 이 없거나 느린 기계에선 삭제가 영영 0 이라 폴더가 **조용히** 자란다(안 도는 쪽이라 소리가 없다) — 읽는 눈금은 목차 꼬리의 「그 밖의 세션 (N개)」. 닫을 것: 없다(새 도구·훅 0 · 이미 도는 삭제 한 줄에 게이트 둘). |
