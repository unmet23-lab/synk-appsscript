| F570 | 2026-08-17 | 마찰 | 자동 커밋 통로 `tools/인계문수거.js` 가 push 를 안 해, 로컬 master 를 origin 과 «갈라뜨리는» 상시 생산자가 됐다 — F542·F565·F566 이 각자 고친 세 소비자의 공통 뿌리다.  실측 2026-08-17 (`local_cc593518` · 세션 시작 직후): · `git rev-list --left-right --count HEAD...origin/master` = **7 앞 · 57 뒤**. 관측 5분 사이 5→7 로 늘었다(지금도 생산 중). · origin 밖 커밋 7건이 **전부** 「docs: 인계문 수거 — 죽은 세션 N건 (F111 통로)」이고 `Session-Id` 트레일러가 **서로 다르다**(`local_eba8defb`·`local_ac18eae8`·`local_cc593518`(나)…) — 한 세션의 실수가 아니라 통로 결함이다. · `tools/인계문수거.js` 의 git 호출 전량 = ls-tree·rev-parse·symbolic-ref·status·add·commit·show. **push 가 없다.** · 그 커밋들이 「보호했다」는 인계문 중 4벌(`8e16b316`·`91e2b70a`·`e84e09f8`·`ee29b2cf`)을 `git cat-file -e origin/master:<경로>` 로 재니 **origin 에 0벌**. 이 노트북에만 있다.  ■ 두 층으로 샌다 — 둘 다 새는 방향이 「통과」다 ① **보호 안 된 것을 보호했다고 보고한다.** 이 도구의 존재 이유가 F111·F025 다 — 「미추적 인계문은 목차 재생성 TTL 12h 가 지우면 이력·stash·reflog 어디에도 없어 영영 유실」. 로컬 커밋으로 옮기면 TTL 은 피하지만 **노트북 하나에 묶인다.** CLAUDE.md 「서버측 잠금도 PR도 origin에 올라온 것만 지킨다 — 안 민 하루는 그 층 전체가 없는 하루다」가 그대로 적용된다. 세션 시작 알림은 그동안 「인계문 2건을 커밋했다(82c7556b) — F111 통로」라고 **성공의 얼굴로** 보고했다. ② **다음 주자에게 따를 수 없는 처방이 간다**(F103 무늬). F542 가 착지시킨 인계문 📥 줄은 「먼저 `git merge --ff-only origin/master` 를 친다」인데, 이 통로가 로컬 master 에 커밋을 낳는 순간 두 갈래가 **갈라져** ff-only 는 원리적으로 실패한다. 내가 이 세션 첫 손으로 그 처방을 받았고 못 따랐다.  ■ `auto-commit.js` 와 갈리는 지점 = **주인** auto-commit.js 줄 34 는 「push 는 하지 않는다 — 되돌리기 어렵고 CI 를 태운다. push 는 `/close` 의 몫이다」라고 의도를 적어 뒀고 **그 판단은 옳다**(매 턴 도는 통로). 갈리는 것은 담는 것의 주인이다: auto-commit 이 담는 것은 «내» 편집이라 내 `/close` 가 밀어 준다. 인계문수거가 담는 것은 «죽은 남»의 파일이라 **밀어 줄 주인이 없다** — 실측 7건이 그 빈자리다.  ■ 처방 후보 — 안 지었다(이 신고는 기록이다) ㉠ 커밋 직후 `git push origin master` 까지 한 벌로 간다. ⚠대가 = 세션 시작이 네트워크에 걸린다(타임아웃 30초 안) · origin 이 앞서 있으면 non-fast-forward 거절이니 **거절은 실패가 아니라 「다음이 받아 민다」로 조용히 물러나야** 한다(SessionStart 를 빨갛게 만들면 남의 시작을 막는다 · F568 무늬). ㉡ 커밋을 origin/master 기준 **가지**로 내고 그 가지만 push 한다(로컬 master 를 안 움직여 갈라짐 자체가 안 생긴다). ⚠대가 = 머지할 주인이 또 없다. ㉢ 세션 시작 알림이 「미푸시 N건」을 드러내고 명령을 준다. ⚠값싸지만 사람 손을 늘린다(철학 하지않는것 ㉡).  ⚠ 뿌리를 공유하는 형제 = F542(보드 줄)·F565(가지 기준)·F566(커밋 목록). 셋은 전부 **소비자**를 고쳤다. 이 신고는 그 셋이 읽는 «로컬 master» 를 낡게 만드는 **생산자** 쪽이다. | ⏸ 내가 신고했지만 내 트랙이 아니다 — 같은 자리를 local_1d940103 이 먼저 선언해 짓고 있다(보드: 「로컬 master 갈라짐이 «자란다» — 로컬 판이 진실 가정의 뿌리」 · tools/작업가지.js 닫기 + tools/인계문수거.js · 리셋셋 실측까지 마친 상태). board-guard 가 그 겹침을 막아 내가 비켜났고, 이 신고문은 그 트랙의 재료로 남긴다(실측: origin 밖 7커밋 전부 이 통로 · 서로 다른 세션 5 · 보호했다는 인계문 4벌이 origin 에 0벌). |
