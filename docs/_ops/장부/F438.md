| F438 | 2026-08-14 | 마찰 | 이해대장 «동봉» 게이트가 **줄끝 하나로** 저장소 전체 배포를 멈췄다 — 내용은 같았다. `tools/이해대장.js --검사` 가 커밋될 화면을 생성 결과와 **바이트 그대로** 대조(`지금 === html`)하는데, 이 저장소는 `core.autocrlf=true` 이고 `docs/이해대장.html` 엔 `.gitattributes` 지정이 없어 **체크아웃이 CRLF 로 내려준다**. 08-14 실측: 같은 내용을 LF 로 두면 「정본과 같다 — 통과」(exit 0), 그 파일을 CRLF 로만 바꾸면 「안 따라온다」(exit 1). 그 적색은 `/deploy` 2단계 게이트라 낸 사람이 아니라 **그 뒤 커밋하는 모든 세션의 배포**를 멈춘다 — 내가 [v9.233] 라이브 반영을 밀려다 여기서 막혔다. 급소는 셋이다: ①막으려던 사고(「주인 없는 적색이 남의 배포를 막는다」)를 **장치 자신이** 냈다(CLAUDE.md 가드 맹점 ④). ②차단문의 처방(`node tools/이해대장.js && git add`)은 실제로 통했지만 **다음 체크아웃마다 되살아난다** — 고쳐도 안 닫히는 처방이라 회수 커밋이 반복된다(이 파일에 이미 「fix: 이해대장 생성물 재생성 — 주인 없는 적색이 남의 배포를 막고 있었다」가 셋: 609042e9·6ff9505c·cc174e14). ③같은 축을 옆 파일은 이미 알고 있었다 — `tests/이해대장.test.js` 의 `[F364·줄끝]` 이 「새 체크아웃(CRLF)과 도구 산출(LF)이 같은 글자로 접힌다」를 못박는데, **정작 배포를 막는 판정기 쪽에는 그 접기가 없었다**(같은 판정이 두 곳에 있으면 갈라지고, 갈라지는 방향은 언제나 사고 쪽이다). | 두 층으로 닫았다(같은 커밋). ①판정기 — `tools/이해대장.js --검사` 가 대조 직전에 줄끝을 접는다(`줄끝접기 = s => s.replace(/\r\n/g,'\n')`). 잘못 쓸 수 없는 통로 쪽이라 체크아웃 정책이 무엇이든 판정이 안 흔들린다. ②파일 — `.gitattributes` 에 `docs/이해대장.html text eol=lf` 를 박아 체크아웃과 생성기 산출이 바이트로 같아진다(옆 `계약/**` 조항과 같은 이유·같은 좁은 범위). 덤으로 재생성이 `git status` 에 «내용 0의 M» 으로 뜨던 잡음이 사라진다 — 그 잡음은 `작업본소유자`·test-ci 의 「도는 중 바뀐 파일」 경보까지 흐렸다. 회귀 `tests/대장동봉.test.js` +1: 「줄끝만 다른 판(CRLF 체크아웃)은 막지 않는다」 — 기존 두 「막는다」 시험(내용이 낡으면 차단·화면이 없으면 차단)과 짝이라 **접는 것이 줄끝뿐**임을 양쪽에서 못박는다. 34/34 초록(빈 HOME·TZ=UTC). 대가: 줄끝«만» 다른 판을 「같다」로 읽는다 — 이 화면은 브라우저가 여는 HTML 이라 줄끝을 요구하는 소비자가 0이다. |
