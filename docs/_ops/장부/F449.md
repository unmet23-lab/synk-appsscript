| F449 | 2026-08-15 | 마찰 | `보안검토과녁.js` 가 「커밋했으면 라이브에 있다」를 전제해, 배포 전량이 커밋된 배포에서 과녁 0건을 내고 **「내보낼 바이트가 없다」고 단언한다** — 판단 층이 통째로 안 도는데 초록 얼굴이다  실측 2026-08-15(세션 `7aa81186` · #Q78 잔여 트랙 승계). 선행 세션 `1ae6700f` 이 `694497ad` [vNEXT] 로 `엔진_셋업확장.js` 를 고쳐 **커밋만 하고 배포 전에 죽었다**. 내가 이어받아 `/deploy` 4단계 과녁을 뽑았더니:      [보안검토과녁] 미커밋 배포 파일 **0건** — 이 배포가 라이브에 새로 내보낼 바이트가 없다.        판단 층 검토의 대상이 없다는 뜻이다(미실행이 아니다). 다른 게이트는 그대로 지난다.  **그 문장이 거짓이다.** 같은 세션 시작 훅이 정반대를 말한다 — 「배포집합이 마지막 배포보다 앞서 있다 · 커밋 50개 사이에 배포 파일 1개가 바뀌었다 · [(루트)] 엔진_셋업확장.js」. 실측 diff = `git diff cd2f5654..HEAD -- 엔진_셋업확장.js` = 24 insertions / 2 deletions(신설 함수 `원인지문_` + 그 호출부). 이 바이트는 다음 `clasp push` 가 **실제로 라이브에 내보낸다.**  ■ 기전 — 분모가 한 축 짧다   과녁 = `배포집합 ∩ 미커밋`(:23·:57). 즉 「아직 git 에 없는 바이트」만 센다. 그 정의는   **「커밋 = 라이브」일 때만** 「새로 내보낼 바이트」와 같아진다. 이 저장소는 그렇지 않다 —   배포는 늘 HEAD 이고(F273), HEAD 가 라이브보다 앞선 상태가 상시로 존재한다(F302 실측 3분 ·   이번엔 커밋 50개). 진짜 축은 **`배포집합 ∩ (HEAD Δ 라이브)`** 이고, 미커밋은 그 부분집합일 뿐이다.  ■ 왜 하필 지금 샜나 — 세션이 커밋과 배포 사이에서 죽는 것이 상시 조건이다   도구는 F306(2026-08-10)을 막으려 섰고 그때 과녁은 unstaged 였다. 전제된 동선은   「한 세션이 고치고 → 커밋하고 → 그 자리에서 민다」다. 그런데 이 저장소는 그 사이가 자주 끊긴다:   F444(Stop 훅 자동커밋이 트랙을 쪼갠다) · F447(/close 가 인계문만 남기고 죽음 도장을 안 찍는다) ·   컨텍스트 한계로 인계. **끊긴 뒤 이어받은 세션에게는 과녁이 언제나 0건으로 보인다** —   고칠 것을 커밋해 둔 것이 오히려 검토를 끄는 조건이 된다.  ■ 왜 마찰인가 — CLAUDE.md 신뢰성 맹점 ④ 의 정확한 사례다   「장치는 안 도는 쪽보다 **맞는 얼굴로 틀린 값을 내는 쪽**으로 더 자주 샌다.」 이 도구는 바로 그   모양이다: 종료 코드 0, 문장은 단정형, 근거까지 곁들인다(「미실행이 아니다」). 그 단정은 도구가   **재지 않은 축**(HEAD Δ 라이브)에 대한 주장이라 재는 것을 넘겨 말한 것이고(:33-34 가 스스로   경계한 바로 그 형태), 받아 적으면 그대로 「보안 검토 대상 없음 = 통과」로 배포 기록에 남는다.   기계 검사(`deploy-security-check.js`)는 아는 패턴만 보므로 **처음 보는 결함을 보는 층은 여기뿐**이다   (:15-16). 즉 이 자리가 0이면 그 배포는 판단 층이 통째로 안 돈 것이다.   ⚠ 이번 실피해는 0이다 — 델타가 dedup 서명용 MD5 지문 하나라 판단 층에 걸릴 것이 애초에 없었고,      학생 0명이다. 그러나 그건 **델타의 운이지 장치가 아니다**(도구 자신이 :19 에서 같은 말을 한다).  ■ 새는 방향은 여기서도 「통과」다   도구는 「못 잼(2)」과 「0건(0)」을 정직하게 갈라 놨다(:28-31 · F207 계열을 이미 의식했다).   구멍은 그 갈래 **밖**이다 — 못 잰 게 아니라 **다른 것을 정확히 쟀다**. 잰 축이 물음과 어긋난 자리라   종료 코드로는 영영 안 드러난다.  ■ 처방 후보(이번 세션에서 안 굳혔다 — 훅·도구 소유 트랙 밖이고, 되돌림 비용이 배포 게이트에 닿는다)   ㄱ) 과녁을 `배포집합 ∩ (HEAD Δ 마지막 배포)` 로 넓힌다. 재료는 이미 있다 —       `배포판점검.js` 가 마지막 배포 커밋과 배포집합을 둘 다 안다(세션 시작 훅이 그것으로 경고를 낸다).       미커밋은 그 합집합에 얹으면 된다(미커밋 ∪ 커밋됐지만 미배포).   ㄴ) 최소판 — 과녁이 0건일 때 **라이브 대비 앞선 배포 파일이 있으면 0 으로 끝내지 않는다**.       단정문(「내보낼 바이트가 없다」)을 지우고 그 목록을 과녁으로 내민다.   ㄷ) 그대로 두고 `/deploy` 스킬 쪽에서 잰다 — ⚠판정이 두 곳에 갈라지는 형태라 CLAUDE.md       「같은 판정을 두 곳에 적으면 갈라진다」에 정면으로 걸린다. 권하지 않는다.   짝이 될 회귀: 「미커밋 0 + 라이브보다 앞선 배포 파일 1」인 픽스처에서 과녁이 **비면 빨개진다**   (지금은 그 픽스처가 초록이다 — 변이로 이빨을 실측할 수 있는 자리다).  ■ 이번 세션의 자기 처방(장치 없이 손으로)   과녁을 손으로 계산해(`git diff cd2f5654..HEAD -- <배포집합>`) 그 델타를 판단 층에 넘겼다.   보고에는 「과녁 0건이라 통과」가 아니라 **「도구 과녁은 0이었고 실제 델타는 1파일이라 그것으로 돌렸다」**   로 적는다. | |
