| F474 | 2026-08-15 | 마찰 | tools/보안검토과녁.js 가 「채번은 났는데 배포가 끊긴」 인계 상황에서 「검토 대상 0건 — 대조할 것이 없다(미실행이 아니다)」를 단언한다. 실제로 라이브에 새로 나갈 바이트는 배포집합 5파일 · 31 삽입 13 삭제였다(Code.js·교재연동.js·엔진_셋업확장.js·엔진_운영배치.js·엔진_콘텐츠AI.js). 원인은 ㉡「안 나간 변경」의 기준선이 **마지막 채번 커밋**이라는 것이다 — 채번 «뒤»에 배포가 끊기면 그 채번 커밋 자신의 diff 가 과녁 밖으로 떨어진다. 실측 08-15: 기준선 dd8b4f41([v9.239]) 로 재면 0건, 라이브 실제 판 a074533c([v9.237]) 로 재면 5파일이다. F449 가 닫으려던 동선(「커밋과 배포 사이가 끊기는 것은 상시 동선이라 끊긴 뒤 이어받은 세션에게는 과녁이 언제나 0건으로 보였다」)이 한 층 안쪽에서 그대로 살아 있다 — 정상 순서가 채번→커밋→push→clasp 라 끊기는 자리는 거의 언제나 채번 «뒤»이고, 그때가 바로 기준선이 페이로드 자신이 되는 때다. 머리말이 이 한계를 ⚠로 적어 두긴 했으나(F244), 사람이 읽는 첫 줄은 「대조할 것이 없다」라는 긍정 단언이고 그 아래 분모줄도 「0건 = 미커밋 0 + 안 나간 변경 0」이라 두 갈래 모두 좋은 0으로 읽힌다 — 지침 v9.2 가드 맹점 ④(맞는 얼굴로 틀린 값)의 교과서 모양이다. 이 세션은 rot-check 의 「라이브 낡음」 관측이 따로 있었기에 알아챘고, 그 관측이 없었으면 「검토 대상 없음 = 통과」로 기록한 채 5파일을 라이브에 밀었을 것이다. | ✅ 해소 `local_c1f52699` — 기준선을 **라이브 실측 판**으로 갈아꼈다(신고문의 처방 ㉡ · ㉠`sha^` 는 안 썼다: 채번이 둘로 갈린 판에서 앞 채번의 파일을 놓치는 반쪽이라던 그 판정이 그대로 유효했다). `배포판점검.안나간변경` 이 프로젝트마다 기준선을 고르고, 규칙 둘을 코드에 못박았다 — ①**더 오래된 쪽만** 기준선이 된다(⇒ 새 과녁 ⊇ 옛 과녁 · 좁아지는 쪽은 원리상 못 난다) ②**채번이 도장보다 나중이면 도장을 안 믿는다**(/deploy 는 언제나 채번을 먼저 만드니 「도장 뒤에 난 채번」 = 그 사이 배포가 났을 수 있다 — 이 한 줄이 없으면 배포 직후부터 다음 rot-check 까지 매 세션 거짓 🔴 가 뜨고, 따를 수 없는 경보는 우회를 정상 통로로 만든다 · F103). 배선 네 칸(`라이브대조→판정→rot-check 배포도장→안나간변경`)을 한 커밋에 넣었다 — 재는 자리와 쓰는 자리가 갈리면 장치는 안 도는데 회귀는 초록이다. 출력도 고쳤다: 0건 첫 줄이 **기준선 갈래를 말한다**(채번이면 「**이 기준선으로는**」 + F474 한계 + 따를 수 있는 처방 `node tools/rot-check.js --hook --force` · 라이브 실측이면 한정어 없음). 🔑 착수 실측이 진단을 한 겹 바꿨다 — 신고 시점의 「5파일」은 오늘 **재현되지 않는다**(그 사이 배포가 나 라이브 = 저장소였다). 그래서 탐지력은 실저장소가 아니라 **전부 픽스처**가 진다(맹점 ②). 회귀 `tests/배포기준선.test.js` 16건(급소·⊇보장·안 믿는 자리 셋·접기 둘·배선 셋·문장 넷·실저장소 불변식) · **변이 10/10**(기준선 안 갈아낌·최신 판 고름·모름 건너뜀·시각 규칙 제거·더오래된 규칙 제거·판정 pass-through 제거·도장 미기록·한정어 제거·섞임 문장 합침·라이브에도 「원리상 못 잰다」). ⚠ 남는 한계 = 도장이 없는 판(새 클론·CI·라이브 미측정)에서는 그대로 채번 기준선이다 — 그때 **출력이 스스로 그렇게 말한다**는 것이 이번에 바뀐 전부다. |
