| F474 | 2026-08-15 | 마찰 | tools/보안검토과녁.js 가 「채번은 났는데 배포가 끊긴」 인계 상황에서 「검토 대상 0건 — 대조할 것이 없다(미실행이 아니다)」를 단언한다. 실제로 라이브에 새로 나갈 바이트는 배포집합 5파일 · 31 삽입 13 삭제였다(Code.js·교재연동.js·엔진_셋업확장.js·엔진_운영배치.js·엔진_콘텐츠AI.js). 원인은 ㉡「안 나간 변경」의 기준선이 **마지막 채번 커밋**이라는 것이다 — 채번 «뒤»에 배포가 끊기면 그 채번 커밋 자신의 diff 가 과녁 밖으로 떨어진다. 실측 08-15: 기준선 dd8b4f41([v9.239]) 로 재면 0건, 라이브 실제 판 a074533c([v9.237]) 로 재면 5파일이다. F449 가 닫으려던 동선(「커밋과 배포 사이가 끊기는 것은 상시 동선이라 끊긴 뒤 이어받은 세션에게는 과녁이 언제나 0건으로 보였다」)이 한 층 안쪽에서 그대로 살아 있다 — 정상 순서가 채번→커밋→push→clasp 라 끊기는 자리는 거의 언제나 채번 «뒤»이고, 그때가 바로 기준선이 페이로드 자신이 되는 때다. 머리말이 이 한계를 ⚠로 적어 두긴 했으나(F244), 사람이 읽는 첫 줄은 「대조할 것이 없다」라는 긍정 단언이고 그 아래 분모줄도 「0건 = 미커밋 0 + 안 나간 변경 0」이라 두 갈래 모두 좋은 0으로 읽힌다 — 지침 v9.2 가드 맹점 ④(맞는 얼굴로 틀린 값)의 교과서 모양이다. 이 세션은 rot-check 의 「라이브 낡음」 관측이 따로 있었기에 알아챘고, 그 관측이 없었으면 「검토 대상 없음 = 통과」로 기록한 채 5파일을 라이브에 밀었을 것이다. | ⏸ 지금 안 닫는다 — 처방이 「한 줄 고치기」가 아니라 **기준선을 갈아끼우는 일**이고, 이 세션의 선언 트랙(배포빚 닫기)을 먼저 닫아야 한다. 실측한 처방 후보 둘: ㉠`배포판점검.안나간변경` 의 diff 기준을 sha→sha^ 로(채번 커밋 자신을 포함) — 값이 싸고 가라앉힘(rot-check 도장)이 배포 뒤 자동으로 0건으로 되돌린다. **단 반쪽이다**: 채번이 둘로 나뉜 판(v9.238·v9.239)에서 앞 채번의 파일을 여전히 놓친다(실측: sha^..HEAD 는 엔진_운영배치.js 2줄을 못 본다). ㉡진짜 기준선 = **라이브 판**(`배포판점검.라이브판찾기` 가 이미 낸다 · rot-check 가 오늘 「라이브 = a074533c」로 실제로 뽑았다) — 도장에 라이브 커밋을 실어 그것을 기준으로 삼는다. ㉡가 정본이고 ㉠은 그 부분집합이다. 판정 자리 = 이 세션 보드 `71aa19d6` · 착수하는 세션은 회귀를 픽스처로 지고(맹점 ②) 「도장 없음」이 영구 소음이 되지 않는지(F103)를 함께 본다. |
