| F341 | 2026-08-12 | 마찰 | 같은 🎫 인계 하나를 **세 세션**이 동시에 집어 유료 재검수를 3벌 태웠다 — 보드는 「선언」만 하고 「선점」을 못 한다.  실측(08-12 · `04a341e1`(나)·`52a7abc6`·`a0d3c91a` · board-move 가 「세션 파일 3개에 걸린다」로 드러냈다): 9a710cd8 이 상태 칸에 놓은 🎫(「--commit de6b34d5 재검수→배포 210~214→as 명부스윕」)를 셋이 각자 집었다. 나는 09:20 에 내 보드 파일에 선언하고 커밋(96f345ae)했는데도 뒤에 온 둘이 같은 줄을 각자 선언했다. 결과: **약 5분짜리 이종 재검수를 셋이 각각 태웠고**(같은 커밋 de6b34d5 에 대해 · 장부에 같은 대상 기록 3줄), 배포 전 검증 전량도 각각 돌렸다. 라이브 결과물은 정상이다(라이브 = de6b34d5 실측 · 민 것은 52a7abc6).  ⚠ **첫 신고문의 과장을 정정한다**: 「clasp push 를 눌렀으면 남의 반쯤 지은 v9.215 가 라이브로 나갔다」고 적었는데 **틀렸다** — 같은 CI 로그 안에 clasp-guard 자신의 출력(「미커밋 배포 파일(엔진_운영배치.js): 커밋 먼저」)이 찍혀 있었다. 그 게이트는 무장돼 있었고 눌렀어도 막혔다. 이 건의 피해는 **낭비**이지 사고 직전이 아니다. 틀린 전제는 잘못된 처방을 낳으므로 남긴다.  가르는 축: 보드 선언은 「내가 이걸 한다」를 알리는 장치지 「남이 못 하게」 하는 장치가 아니다. 🎫 는 주인이 스스로 놓고 간 자리라 임자가 없어 이 구멍이 가장 크게 열린다. 기존 `track-collision` 훅이 이 축을 못 잡는 이유는 로직 결함이 아니라 **재료**다 — 그 훅은 남의 **커밋**(파일 착지·제목 표식 F0NN·[v9.NNN])을 보는데, 이번 겹침엔 커밋이 하나도 없었다(저쪽 보드 선언은 끝까지 미커밋이고 clasp push 는 커밋을 안 만든다).  처방(채택): **board-guard 가 내 보드 줄을 「쓰는 순간」 남의 활성 줄과 트랙 겹침을 본다.** 그 훅은 상한(활성 12·전체 18) 검사 때문에 이미 남의 보드 파일 전량을 읽고 있어 재료가 손에 있다 — 안 보고 있었을 뿐이다. 발동 시점이 40분 뒤가 아니라 선언 시점이라 먼저 잡은 쪽이 이기고 뒤에 온 쪽이 멈춘다. 🚫 기각: 「비싼 단계 직전에 다시 읽어라」 프로즈(발동 시점이 없다) · 「선언을 즉시 커밋하라」 단독(board.js 는 디스크를 읽으므로 커밋 여부가 원인이 아니었다). | |
