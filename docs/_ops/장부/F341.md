| F341 | 2026-08-12 | 마찰 | 같은 🎫 인계 하나를 두 세션이 동시에 집어 40분을 겹쳐 태웠다 — 보드는 「선언」만 하고 「선점」을 못 한다.  실측(08-12 · local_04a341e1 vs 52a7abc6): 9a710cd8 이 상태 칸에 놓은 🎫(「--commit de6b34d5 재검수→배포 210~214→as 명부스윕」)를 두 세션이 각자 집었다. 나는 09:20 에 내 보드 파일에 선언하고 커밋(96f345ae)했고, 52a7abc6 은 같은 트랙을 자기 파일에 선언했으나 **그 보드 파일이 미커밋**이라 내 `node tools/board.js` 조회에 안 잡혔다(반대로 그쪽은 내 커밋된 줄을 봤어야 하는데 안 봤거나 그 전에 읽었다). 결과: **재검수(약 5분·유료 왕복)를 양쪽이 각각 태웠고**, 배포 검증 전량을 양쪽이 각각 돌렸다.  더 위험했던 것은 겹침 자체가 아니라 **끝나는 방식**이다. 그쪽이 먼저 clasp push 를 끝내고 곧바로 다음 판(v9.215 명부스윕)을 작업본에서 짓기 시작했는데, 나는 같은 시각 「배포 직전」 자리에 서 있었다. 그대로 clasp push 를 눌렀으면 **남의 반쯤 지은 v9.215(Code.js·엔진_운영배치.js 미커밋)가 라이브로 나갔다** — clasp 는 HEAD 가 아니라 작업본을 민다(F310). 막은 것은 배포 게이트가 아니라 `test-ci` 가 뱉은 「테스트 도는 동안 10개 파일이 바뀌었다」 경고와 그 뒤의 `작업본소유자` 대조였다. 즉 **우연히 CI 가 늦게 끝난 덕에 봤다.**  가르는 축: 보드 선언은 「내가 이걸 한다」를 알리는 장치지 「남이 못 하게」 하는 장치가 아니고, 그 알림조차 **커밋돼야 남에게 보인다.** 🎫 는 주인이 놓고 간 자리라 임자가 없어 이 구멍이 가장 크게 열린다.  처방 후보(택일 아님 · 구현은 별도 판정): ①비싼 되돌릴 수 없는 단계(유료 재검수·clasp push·원격 배포) **직전에** 보드+작업본소유자를 다시 읽는 것을 훅으로 강제한다 — 「조작 직전에 그 대상을 직접 연다」(F030 계열)를 시간축이 아니라 **비용축**에 적용하는 것. ②보드 선언을 즉시 커밋하도록 board-guard 가 요구한다(미커밋 선언은 남에게 존재하지 않는다 — 지금 `작업본소유자` 가 「아직 커밋 안 된 보드 선언」으로 사후 보고만 한다). ③🎫 를 집을 때 그 줄에 집은 표식을 남기는 통로(board-move 계열 도구 한 벌)를 만들어 「집힘」이 커밋으로 드러나게 한다.  ⚠ 이번 건의 피해는 낭비(재검수 2회·검증 2벌)로 끝났고 라이브 결과물은 정상이다(라이브 = de6b34d5 실측). 그러나 같은 배치에서 **양쪽이 같은 순간에 clasp push 를 눌렀다면** 결과는 낭비가 아니라 사고였다. | |
