| F571 | 2026-08-17 | 마찰 | `tools/보드수거.js` 가 board-move 의 **사유와 처방을 통째로 버린다** — 세션시작 훅이 매 세션 「실패 5건」만 말해, 따를 수 있는 처방이 화면에 한 번도 안 왔다. 실측상 완료 줄 5건이 3시간 이상 갇혔고 같은 원인이 `git merge` 까지 막았다.  실측 2026-08-17 (`local_eba8defb` · 세션 시작 직후): · 세션시작 알림 = 「[보드수거] 거두지 못했다 — 실패 5건 · 🎫 3건은 남겼다」. **원인 0자.** · `node tools/보드수거.js --실행` 재현 = 같은 문장, exit 2. 사유 없음. · 같은 후보를 `board-move` 에 직접 물으니 **사유와 명령이 전문으로 나왔다**:     「아카이브에 이관 전부터 **남의** 미커밋이 있다 — … 아무것도 쓰지 않았다: M docs/세션보드_아카이브.md      그 잔재 줄의 주인은 **죽었다**(생사 실측) — 기다리면 영원히 안 풀린다(F433).      ▶ 그 줄부터 마저 옮겨라: node tools/board-move.js "작동 대장 기록 자동화 — …"」 · 즉 **F433 장치는 정상 작동했다.** 그 출력이 호출자에서 버려졌다.  ■ 어디서 버려지나 — 코드가 스스로 적어뒀다 `tools/보드수거.js:107` 이 `spawnSync(… encoding:'utf8')` 로 stdout·stderr 를 **받아 놓고**, `옮기기()` 는 종료코드만 보고 `'옮김'|'거절'|'실패'|'못받음'` 문자열로 **접는다**(112~113행). 166행 주석이 그 사실을 인정한다: 「이 도구는 board-move 의 사유를 안 들고 있어 「실패 N건」까지만 말한다(그래서 원인이 안 보인다)」. 재료가 손에 있는데 안 쓰는 자리다 — 없어서 못 낸 것이 아니다.  ■ 왜 이게 급소인가 — 잠금이 **셋**으로 번졌다 고아 미커밋 1줄(죽은 `6694ab98` 의 중단된 board-move)이 동시에 잠근 것:   ① board-move 원칙⑤ → 모든 세션의 줄 이관 거절   ② 보드수거 → 완료 줄 5건이 3시간째 안 거둬짐 (이 신고의 자리)   ③ `git merge origin/master` → 로컬 master 가 **57 커밋 뒤**로 고정      = F542·F565·F566(인계문·가지·대기열이 낡은 판을 읽는다)의 실제 방아쇠 ①은 옳은 거절이고 ③은 git 의 정상 동작이다. **셋을 몇 시간짜리로 만든 것은 ②의 침묵뿐이다** — 사유가 한 번만 화면에 왔으면 어느 세션이든 한 명령으로 풀었다.  ■ 가드 맹점 ④ 「맞는 얼굴로 틀린 값을 내는 자리」 그대로 이 도구는 안 도는 쪽이 아니라 **도는데 쓸모없는 값을 내는 쪽**으로 샜다. 훅은 매 세션 발화했고, 「실패 5건」은 실패를 **정확히** 보고한다 — 그래서 아무도 결함이라고 안 읽었다. 조용한 실패가 아니라 **시끄러운데 못 따를 실패**다(F103 무늬 · 처방이 없으면 우회가 정상 통로가 된다).  ■ 처방 후보 — 안 지었다(이 신고는 기록이다) ㉠ `옮기기()` 가 사유(stderr 마지막 die 블록)를 함께 돌려주고, `--실행`·`--hook` 둘 다    **실패 건마다 사유 첫 줄 + 처방 명령을 그대로 낸다**. board-move 가 이미 완성형 문장을    내므로 재가공 0 — 파이프만 잇는다. ⚠대가 = 훅 출력이 길어진다(실패 때만이라 상시 비용 0). ㉡ 실패 사유가 **재시도로 안 풀리는 종류**(고아 잠금·죽은 주인 잔재)면 훅이 적색으로 올린다.    ⚠대가 = 분류를 board-move 종료코드에 새로 새겨야 한다(계약 확장 = 두 파일이 갈릴 자리). ㉢ 아무것도 안 하고 사람이 매번 `--실행` 을 손으로 돌려 사유를 본다.    ⚠철학 「하지 않는 것 ㉡」 정면 — 사람 손을 늘리는 해법이다. 권고 = ㉠. 재료가 이미 손에 있고 계약을 안 건드린다. | tools/보드수거.js 의 옮기기() 가 {판정,사유} 를 돌려주고 출력부가 실패·못받음의 사유를 같은 사유끼리 묶어 낸다(부분 실패 포함 · 거절은 제외) — 사유 문장은 board-move 정본 그대로 파이프만 이었다. 회귀 tests/보드수거.test.js 42건(신설 15) · 변이 10/10 구멍0 · ①번 변이가 신고 원문의 옛 판(사유 안 싣기)을 그대로 되살려 회귀가 물었다(F083) · 0d8da887 |
