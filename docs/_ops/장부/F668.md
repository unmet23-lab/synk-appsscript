| F668 | 2026-08-19 | 실수 | 판정안을 내기 전에 그 대상을 안 열었다 — 두 번째다(F662 와 같은 계열). F605 적색의 원인을 「훅 파생물을 세션 가지에 담는 것 자체」로 짚고 ㉢(훅 수정)을 추천했는데, .gitattributes:68 에 이미 docs/_ops/*.jsonl merge=union 이 걸려 있어 머지되면 두 행이 다 산다 — 유실이 아니라 «아직 안 머지됨»이었고 훅을 고쳐도 그 적색은 안 없어진다. 원인은 검사의 과잉 신고 쪽이다. 같은 절차의 2번째라 실수가 아니라 시스템 결함으로 읽는다: 판정안 제출 직전에 「그 판정이 이름 댄 파일을 열었나」를 세는 자리가 없다. | F662 와 한 벌로 닫는다(축 2번째) — 「판정안 제출 직전에 그 판정이 이름 댄 파일을 열었나」. 답은 CLAUDE.md v10.0 솔직함 절(「확인 못 한 것은 「안 재봤다」로 쓴다」)이 지고, 새 장치는 안 짓는다(같은 문서의 「실수를 고칠 때 규칙·훅·테스트를 새로 만들지 않는다」). 실증·한계는 F662 해소문에 함께 적었다. ⚠ 이 신고 자신이 가리킨 .gitattributes merge=union 은 지금도 그대로다 — 그건 결함이 아니라 정상 동작이었다는 신고문의 정정이 맞다. |
