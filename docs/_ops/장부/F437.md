| F437 | 2026-08-14 | 실수 | 새 전역 상수 이름이 다른 엔진 파일과 겹쳐 라이브가 통째로 죽을 뻔했다 — `node --check` 는 파일 단위라 초록이었다. 발화 지수 로그 상수를 `TALK_LOG_HEADERS` 로 지었는데 `엔진_수집.js:327`(회화 로그 talk_log)이 같은 이름을 이미 쓴다. Apps Script 는 전역을 파일 순서대로 합쳐 초기화하므로 const 재선언 = 프로젝트 전체 SyntaxError = 모든 트리거 사망. 구문검사·발화퀄리티 회귀 39건 전부 초록이었고, **합본을 평가하는 `tests/월키원인차단.test.js` 하나만 빨갰다**. 하마터면 test-ci 의 「이 적색은 못 믿는다」(동시 편집 경고)를 방패로 넘길 뻔했다 — 경고가 가리킨 파일은 내가 방금 고친 이해대장.html 이었고, 실제로 어느 테스트가 빨간지 세어 보니 원인은 전혀 다른 곳이었다. ⇒ 처방: ①새 전역 상수는 짓기 전에 `grep -rn "const 이름"` 로 엔진 전량을 훑는다 ②적색은 파일 이름이 아니라 **실패한 테스트 이름**으로 가른다 ③회귀 신설(발화퀄리티 — 이름 겹침을 직접 셈). | `8b7adb34` — 처방 ③(회귀)을 **손 목록에서 전량 계수로** 올렸고, 그 과정에서 **아무도 안 보던 절반**을 찾아 닫았다. 신고의 `const` 겹침은 합본 평가(월키원인차단 등)가 예외로 이미 잡는다 — 그런데 **`function`·`var` 가 겹치면 예외가 «안» 난다**(뒤 파일이 앞을 조용히 덮는다). 실측으로 못박았다: `엔진_궤적.js` 에 `function 반키_`(엔진_셋업확장.js:2085 와 같은 이름 = 실사고 재현)를 심으니 **새 검사만 빨갛고 월키원인차단은 10/10 초록 그대로**였다(원복 확인 · 엔진 파일에 남은 바이트 0). 신설 `tests/전역이름겹침.test.js` — 재는 방법은 파싱이 아니라 **실행**(각 파일을 빈 컨텍스트에 혼자 돌려 그 파일이 만든 전역 키를 센다 · 파일이 도중에 던져도 호이스팅 덕에 이름 집합은 온전하고, 던진 사실은 분모 시험이 따로 말한다). 오늘 실저장소 = 7파일·톱레벨 이름 505·겹침 **0** ⇒ 실저장소 쪽은 거짓양성 전용이고 탐지력은 픽스처가 진다(F296). 회귀 6·변이 3(실파일 주입 → 1 빨감 · 접기 `>1`→`>2` → 1 빨감 · 이름 측정 비움 → 2 빨감). ⚠ **처음엔 실저장소 시험과 픽스처 시험이 딴 코드를 재고 있었다** — 실저장소는 오늘 겹침 0이라 어차피 초록이고 픽스처는 지역 filter 를 써서, 접기 로직 변이가 **안 물렸다**. `겹친이름들()` 하나로 모아 둘이 같은 통로를 지나게 고쳤다. ⚠ **못 보는 절반은 명시**: `const`·`let`·`class` 는 전역 객체에 안 붙어 이 방법으로 안 세진다 — 그쪽은 합본 예외가 지고, 그 통로가 살아 있는지를 ④번 시험이 대조한다(죽으면 거기서 알린다). 🔑 처방 ①(짓기 전에 `grep` 로 훑는다)은 이제 **기계가 대신 한다** — 사람 손을 늘리는 해법을 규약으로 남기지 않는다(철학 ㉡). 처방 ②(적색은 파일 이름이 아니라 실패한 테스트 이름으로 가른다)는 사람 몫으로 남고, 이 검사가 겹친 **두 파일 이름을 그 자리에 찍어** 그 판단을 싸게 만든다. 기존 `tests/발화퀄리티.test.js:561`(그 상수만 세는 좁은 판)은 남의 활성 줄이라 **무접촉**으로 뒀다 — 중복이지만 무해하고, 지우는 것이 이 신고의 몫이 아니다. |
