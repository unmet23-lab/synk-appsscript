| F625 | 2026-08-18 | 마찰 | **이어받은 트랙이 이미 master 에서 닫혀 있었다 — 구현 한 벌을 통째로 버렸다.** F615 의 아홉째지만 층이 다르다: 그쪽은 «선언 시점»의 경합이고, 이쪽은 «해소 시점»에 아무도 안 물어보는 자리다.  ■ 실측 2026-08-18 (`local_ad0673f3` · 클라우드 세션 · 지정 가지 `claude/continue-work-em060r`) · 세션 시작 시 `origin/claude/continue-work-em060r` 에 `197f1fe3` 의 **F616 착수 선언**이 있었다(69초 전 push).   유호님 지시는 「이어서 해」. 같은 가지로 넘어온 트랙이라 승계로 읽었다 — 그 판단 자체는 재료대로다. · SessionStart 훅이 「❔ 주인의 생사를 모르는 착수 줄 7건 · **이어받지 않는다**」를 냈고 그 목록에   `197f1fe3` 이 있었다. 그런데 **유호님 지시가 그 문구를 덮는다** — 그래서 훅은 이 판에서 아무것도 못 막았다. · 나는 착수 직후 `origin/master` 를 한 번 당겼다(그때 2커밋 뒤). 그 시점엔 F616 이 안 닫혀 있었다. · 구현·회귀·전량 CI(165초)·변이 14/14 를 끝내고 **커밋·push 까지 마친 뒤** master 를 다시 당겼더니   **30커밋 뒤**였고, 그 안에 `197f1fe3` 자신의 **PR #121 머지(`bc13702c`)** 가 있었다. 같은 F616 을,   더 나은 판으로(판정 3갈래 · 해시 재사용) 이미 닫아 놓았다. · 버린 것: `tools/lib/런중변경.js` 신설 + `test-ci` 개조 + 회귀 8건 + 변이 14/14 + 전량 CI 1회.   살린 것: 걸음 층 회귀 구멍 3(변이로 실측) — 그것만 델타로 얹었다. · 장부도 갈라졌다: 내 해소문이 붙은 `F616.md` 와 master 판이 병합되며 **같은 파일에 F616 행이 2개**가 됐다.   (`git checkout origin/master -- …` 로 되돌렸다.)  ■ 왜 신호인가 — 새는 방향이 「통과」다 · 이 저장소의 점유·경합 장치는 전부 **「지금 누가 잡고 있나」**를 잰다(board-guard · 보드.js · track-collision).   이 판에서 그 답은 끝까지 「내가 잡고 있다」로 **맞았다** — 잡은 사람이 나 하나였으니까(승계했으므로).   못 본 것은 «잡혔나»가 아니라 **«이미 끝났나»** 다. 그 질문을 던지는 층이 0이다. · `repo-staleness` 훅은 두 번 울었다(2커밋 뒤 · 25커밋 뒤). 하지만 그 처방은 「트랙을 **고르기 전에** 보라」다 —   나는 이미 골랐고 짓는 중이었다. 짓는 중에 남이 내 트랙을 끝내는 판을 그 문구가 안 다룬다. · `friction.js` 는 이미 master 를 읽는 배선(`lib/master동기.js`)을 가졌는데, **해소를 쓸 때 그 번호가   master 에서 이미 해소됐는지는 안 본다.** 그래서 중복 해소가 조용히 커밋됐다.  ■ 처방 후보 — 안 골랐다(재료다) ㉠ `friction.js resolve F0NN` 이 쓰기 전에 `origin/master` 의 그 조각을 읽어 **이미 해소면 멈춘다**    (「그쪽 해소문 앞 200자 + 내 것과 합칠지 물음」). 제일 싸고 과녁이 정확하다.    ⚠대가: 네트워크에 기댄다 — 못 읽으면 fail 아니라 **skip 으로 드러내야** 한다(F296). ㉡ 승계 선언(남의 지문을 인용하는 보드 줄)을 쓸 때 그 원 세션의 **최근 커밋·PR 상태**를 함께 낸다.    ⚠대가: 얕은 클론은 남의 가지를 다 못 본다 — 「모름」이 자주 나온다. ㉢ 긴 트랙(전량 CI 를 도는 급) 중간에 `origin/master` 를 강제로 한 번 더 당기게 한다.    ⚠대가: 상시 훅이면 소음이고, 사람 규율이면 안 지켜진다(그래서 이 판이 났다). ㉣ 아무것도 안 한다 — 승계 자체가 드물다.    ⚠대가: 이번에 버린 것이 구현 한 벌 + CI 165초 + 변이 런 2회다. 다음에도 같은 값을 낸다.  ■ 안 잰 것(F348) · 승계가 실제로 몇 %인지 안 셌다. 이번 1건이 분모의 전부다. · `197f1fe3` 이 언제 PR #121 을 머지했는지 정확한 시각은 안 봤다 — 내 착수 뒤라는 것만 안다. | ㉠ 채택 — `tools/lib/장부마스터.js` 신설 + `friction.js` 의 `resolve`·`defer` 가 **쓰기 전에** 그 관문을 탄다(`156ef7f4`·`a385cb48`). ㉠ 의 ⚠대가(네트워크)는 그 자리에서 닫았다: 못 읽으면 fail 이 아니라 **skip** 이고, 건너뛴 사실과 **사유**를 한 줄로 찍어 「쟀는데 깨끗하다」와 구별한다(F296·F207). 신고문이 안 적은 두 축을 더 닫았다 — ①**층이 둘**이다(조각 + 동결 아카이브): 조각만 열고 「master 에 없다」를 내면 한 갈래로 낸 부정 판정이라 F348 그 자리다(같은 번호는 조각이 이긴다 = `read()` 와 같은 규칙) ②**낡은 ref**: F625 의 실제 모양이 「착수 직후 한 번 당기고 30커밋」이었으므로 읽기 직전에 당긴다(회귀가 이 축만 따로 잰다 — 남이 origin 에 닫았지만 내 원격추적 ref 는 아직 옛 커밋인 픽스처). 판정은 지어내지 않는다 — 「해소냐」는 `friction.js` 의 `상태()` 하나가 지고 lib 는 `행상태` 를 주입받는다(행 파싱도 `행파싱()` 한 곳에서 파생 · 칸 가르기는 `lib/표.js` 한 통로). 차단문이 시키는 우회 `--마스터확인함` 은 회귀가 실제로 돌려 본다(가드 맹점 ③ · F103). 회귀 `tests/장부마스터.test.js` **16건 신설**(진짜 git 저장소 둘을 픽스처로 세운다 — 모사를 주입하면 경로 이음매가 검사에서 통째로 빠진다) · **변이 12/12 구멍 0** · **CI 모사 4651 pass 4516 fail 0**. 🔑 이 트랙이 스스로 잡힌 것 1건: 첫 판의 `마지막칸()` 이 날 `split('|')` 이라 백틱 안 파이프에서 그쪽 해소문이 잘렸다 — `tests/표칸.test.js` 의 옛통로 금지가 CI 모사에서 지목했고 회귀 1건을 얹어 닫았다. ⚠안 잰 것 = 이 관문이 실제로 몇 %의 중복 해소를 막는지(분모는 여전히 F625 그 1건) · `fetch` 타임아웃 없음(`master동기.전진` 과 같은 실행기를 쓴다 — 러너를 새로 만들면 git 호출 모양이 저장소에 둘이 된다) · 🔴원격 run 0건(Actions 소진 · 리셋 9/1)이라 초록은 모사 층뿐. 처방 ㉡㉢(승계 선언·긴 트랙 중간 당김)은 **안 지었다** — ㉠ 이 급소를 덮는지 한 판 살고 판정한다. |
