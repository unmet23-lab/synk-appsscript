| F435 | 2026-08-14 | 실수 | F400 의 처방이 형제 도구에 전파되지 않았다 — talk `tools/원격SQL.js` 는 **모르는 플래그를 조용히 무시한다.** 운영 스키마를 읽으려고 `--운영` 을 붙였는데(배포대조.js 에서 배운 낱말이다) 그 도구는 그 인자를 아예 안 읽어, 과녁이 리허설(synk-core-rehearsal)인 채로 실행됐고 결과는 「마이그 0 · 반판 없음 · learners 273행」이었다. 헤더의 `대상 ▸ synk-core-rehearsal` 을 다시 읽어서야 잡았다 — 안 읽었으면 리허설 상태를 **운영 판정으로 유호님께 보고**할 뻔했다(F400 의 실사고와 같은 모양·같은 하루).  08-13 에 F400 을 닫으면서 처방을 둘로 적었다: ①모르는 플래그는 die ②`--운영` 은 실제 운영 과녁으로 잇는다. 그 둘이 `tools/배포대조.js` **한 파일에만** 들어갔다. 같은 `.env` 과녁을 공유하고 같은 낱말을 받는 형제 도구(`원격SQL.js`·`원격배포.js`)는 그대로다 — 「같은 판정을 두 곳에 적으면 갈라진다」의 그 자리이고, 새는 방향은 언제나 「통과」다(모르는 플래그가 오류가 아니라 무시로 끝난다).  ⚠ 이번엔 «막은 것»이 가드가 아니라 **대상알림 한 줄**이었다. 그건 알림이지 게이트가 아니라, 출력을 안 읽으면 그대로 샌다. 고칠 자리는 `아는플래그` 목록과 die 를 세 도구가 **한 곳에서 파생**하게 하는 것(목록은 하나에서 파생 · CLAUDE.md 등록층 ④).  수리는 안 했다 — 지금 트랙(#Q38 반박 수리)이 아니고, 유호님 답 대기 중에 도구 셋을 건드리면 그 자체가 커밋 범위를 흐린다. | talk `lib/플래그.js` 신설(공용 판정) + 자격증명 통로를 쓰는 도구 **11 전부**에 `인자게이트` 등록 + 회귀 `tests/플래그게이트.test.js` 9건(변이 3/3 물림). 🔑 신고문의 과녁이 과소평가였다 — 「형제 도구 둘(원격SQL·원격배포)」이 아니라 **`--운영` 을 받을 수 있는 도구 11 중 die 하던 것이 1**(배포대조)뿐이었다(08-14 전수 실측: 교정확정·동의발급·명부등록·성과계기판·성적표·스토리지키·엔진뷰어·라디오시크릿까지 여덟이 더 샜다). 조준 절반은 F436 이 이미 `lib/자격증명.js 읽기()` 로 공용화했고 이 커밋이 나머지 절반(die)을 진다. 설계 = **목록은 도구 것·판정은 공용** — 자격증명.js 가 「도구마다 아는 낱말이 달라 여기 못 올린다」고 적은 그 벽을, 목록이 아니라 판정만 올려서 넘었다. `공용플래그`(=`--운영`)를 각 도구가 펴 넣는 형태라 가드가 자기 처방을 안 막는다(F103). 실행 층 실측: 열 도구 전부 `--진단` 에 **exit 1**(회귀 초록과 다른 층 — F435 는 실행 층에서 났다). 대가: 이 장치가 틀릴 때의 모습은 **거짓양성**(선언 누락 시 되던 명령이 죽음)이라, 회귀 ④가 선언이 소스의 `--낱말` 전량을 덮는지 기계로 센다(첫 판 추출기가 오류 문구·`--format=%cI` 를 낱말로 세 거짓양성 7건을 냈고 낱말 모양으로 좁혀 닫았다). 닫은 것 1 = `배포대조.js` 의 사본 필터를 공용 통로로 흡수(구현처가 1→1, 등록처만 11). 스위트 2081/2081. |
