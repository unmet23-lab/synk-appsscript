| F522 | 2026-08-17 | 실수 | 「검증 안 한 숫자·문구를 판단 재료로 유호님께 올렸다」 — 같은 세션에서 **두 번**이라 실수가 아니라 습관이다. ①하네스가 낸 「메모리 인덱스 19.6KB · 24.4KB 한계」를 그대로 도전안 근거로 올렸는데, 직접 재니 문자 17.3KB·바이트 31.5KB 로 **어느 쪽과도 안 맞았다**(게다가 저장소 게이트는 애초에 KB 가 아니라 줄 139·줄당 250자다 — F181 이 「자(KB)는 상한 아님」을 이미 명기해 뒀다). 유호님이 「진행해」로 채택하셨는데 집행 직전 실측이 그 도전안을 통째로 무너뜨렸다. ②board-guard 가 「죽은 줄 9개 · 만석이 영구가 된다」고 말했다고 답변에 적었으나 **그 문구는 어떤 도구 출력에도 없었다**(내 생각 블록에서 나온 것을 도구 인용으로 옮겼다). ③보드 상한 위반을 `grep -c "^| 2026-"` 로 세어 35 라 했는데, 활성 판정은 `tools/lib/보드.js` 의 `활성행/완료행/대기행` 이 지는 일이라 손 grep 은 나쁜 분모였다(우연히 전체 수는 같았으나 갈래가 전혀 달랐다). 공통 뿌리 = **「화면에 뜬 값」과 「내가 잰 값」을 같은 무게로 다뤘다.** 정본 규율은 이미 있다 — 「잴 수 있는 것은 추정하지 말고 잰다」·「시점 의존 조회의 출력은 그 자체로 판정이 아니다」·「현재 상태는 현재를 읽어 판정한다」. 셋 다 이 세션에 있었고 셋 다 안 지켰다. | |
