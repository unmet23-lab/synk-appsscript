| F272 | 2026-08-09 | 마찰 | C0계약 테스트 「값목록 통째 복사」 가드가 사람 산문의 낱말을 계약 값으로 센다 — learner_response 값이 「채택·무시·수정」이라 이 문서(교정 수용을 설명하는 문서)에서 세 낱말이 평범한 한국어로 쓰이는 것을 막을 수 없다. 실측 2026-08-09: 응답 필드 설명에 「모르는 칸을 무시하고」라고 쓴 한 낱말이 3/3을 채워 빨개졌다(문서엔 값목록이 없다). 위반 아닌 것을 막는 가드는 낱말 회피를 정상 통로로 만든다. 처방 후보=값이 백틱·목록 꼴로 키 이름 근처에 붙을 때만 세기. | talk a435f7d — 판정 축을 표기가 아닌 근접성으로 교체(키 곁 창 앞80·뒤400자 안의 서로 다른 값 수, 긴 값 우선 비겹침 매칭). 신고 원문 재현=옛 3/3 빨강·새 0/3, 저장소 실제 복사 모양 5종(L0식 나열·JSON·SQL CHECK·표 칸·행별 표)은 픽스처로 전부 빨강 못박음. 픽스처 8·변이 2/2·실문서 5키 초록·전 스위트 676/676. 1차 처방(백틱 표기만 세기)은 분리 반박이 탐지력 0으로 뒤집어 폐기 |
