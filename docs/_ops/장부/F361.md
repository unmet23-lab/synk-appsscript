| F361 | 2026-08-12 | 마찰 | 검수·심문이 10분마다 유호님 화면에 **빈 검은 창**을 띄웠다 — 정보는 0이고 방해만 됐다. `던지기()` 의 spawn 에만 `windowsHide` 가 붙어 있고 정작 회차마다 도는 `codex()` 의 execFileSync 에는 없었다(같은 판정을 두 자리에 따로 적어서 갈린 그 계열). 최악은 그 창이 **아무것도 안 찍는다**는 것 — stdio 가 파이프라 진행 신호조차 아니라서, 유호님이 「원래는 뭐가 써지더니 지금은 가만히 있다」고 멈춘 줄 아셨다. 도구가 스스로를 「고장난 것처럼」 보이게 만든 셈이다. | |
