| F361 | 2026-08-12 | 마찰 | 검수·심문이 10분마다 유호님 화면에 **빈 검은 창**을 띄웠다 — 정보는 0이고 방해만 됐다. `던지기()` 의 spawn 에만 `windowsHide` 가 붙어 있고 정작 회차마다 도는 `codex()` 의 execFileSync 에는 없었다(같은 판정을 두 자리에 따로 적어서 갈린 그 계열). 최악은 그 창이 **아무것도 안 찍는다**는 것 — stdio 가 파이프라 진행 신호조차 아니라서, 유호님이 「원래는 뭐가 써지더니 지금은 가만히 있다」고 멈춘 줄 아셨다. 도구가 스스로를 「고장난 것처럼」 보이게 만든 셈이다. | 자식 옵션을 자식옵션() 통로 하나로 모아 네 자리(git·codex CLI·회차 spawn·던지기)가 전부 그것을 지나게 했다 (15304b49). 실제로 막은 것은 한 줄이 아니라 **통로화**다 — 호출부마다 적는 방식이 애초에 갈린 원인이었다. 변이 실증: 회차 spawn 한 자리를 되돌리니 창숨김누락() 이 998 줄을 이름 대고 빨개졌다. 회귀 5. ⚠ 범위 밝힘 — 저장소 전역 자식 호출부 66자리 중 windowsHide 를 든 것은 이 넷 + 원격ci·track-collision 둘뿐이고, **나머지 60자리(대부분 훅의 git 호출)는 창이 뜨는지 미측정**이다. 유호님이 특정한 「10분마다」 증상은 codex 사슬이라 그 자리는 닫혔다. |
