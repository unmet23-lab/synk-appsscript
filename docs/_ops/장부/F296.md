| F296 | 2026-08-09 | 마찰 | board-guard 의 F278 검사 다섯이 **부하에 따라 거짓 적색**을 낸다 — 남의 CI·배포 게이트를 막는다. 급소: 그 다섯은 훅이 `board-move --dry` 를 자식 프로세스로 띄우고 8초(`검증제한` 기본 8000ms)에 끊는데, 타임아웃이면 훅이 「판정 불가 → 차단 유지」로 **다른 사유**를 내므로 `assert.match(사유, /첫 선언 1줄/)` 이 깨진다. 실측 2026-08-09: `node --test tests/board-guard.test.js` 단독은 75/75 초록(F291 회귀 5 포함)인데 `node tools/test-ci.js` 전 스위트(2278 통과)에서 「[F278] 첫 선언이라도 한 번에 2줄이면 막는다」 하나가 **15,046ms** 를 태우고 적색이 됐다 — 제한의 두 배다. 기계로 확정: `SYNK_BOARD_MOVE_TIMEOUT_MS=1` 을 주면 같은 가족 **다섯이 전부** 같은 모양으로 깨진다(70/75). 코드가 아니라 **자식이 제 시간에 못 돈 것**이고, 부하가 곧 판정을 바꾼다. 왜 나쁜가 ①CI 는 늘 부하가 높은 자리라 이 검사는 가장 필요한 데서 가장 잘 깨진다(「repo 밖 환경에 기대는 검사는 CI 에서 깨진다」의 실례 — 여기선 환경이 **시간**이다) ②주인 없는 적색은 남의 배포를 막는다 ③거짓 적색을 두 번 보면 다음 세션은 적색을 안 믿게 되고 그때 진짜 적색도 같이 죽는다. 처방 후보 ①타임아웃이면 fail 이 아니라 **skip 으로 드러낸다**(미실행과 통과를 가르는 그 저장소 규칙 그대로 — 지금은 미실행이 「막힘」으로 접혀 통과 사유와 섞인다) ②그 다섯에서 실 자식 프로세스를 픽스처로 갈아낸다(탐지 대상은 훅의 판정이지 board-move 의 속도가 아니다) ③제한 올리기는 **처방이 아니다**(부하에 상한이 없다 · 임계값 연장은 F249 에서 이미 죽은 길). 권고 = ②가 본선, ①은 그전까지의 안전판. 경위 = F291 수리 중 「이 적색이 내 것인가」를 가르려다 실측했다(내 변경은 ④ 겹침 가지뿐이라 스폰·타임아웃 경로에 안 닿는다). | |
