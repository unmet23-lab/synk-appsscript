| F430 | 2026-08-14 | 실수 | 「차단 마커 파일이 없다」를 「차단이 풀렸다」로 읽어, 이미 소진된 벤더 한도에 런을 하나 태웠다. 실측 2026-08-14 04:33Z `local_271b2a6d`: 대기열의 명부스윕 재검수 줄이 ⛔선행으로 「GPT 한도(리셋 8-18)」를 적고 있어 실물을 쟀는데 `한도상태: null` · `한도막나: false` 가 나왔다. 그 부재를 「풀림」으로 판정하고 그 줄을 〖지금〗으로 뒤집은 뒤 곧장 `--commit c0f148aa --던지기` 로 집행했고, 3분 뒤 런이 한도 소진으로 죽었다(1단계 분석 1/2 회차 차단 · 벤더 보고 리셋 2026-08-20 12:45). 오독의 정확한 자리는 이것이다 — **그 마커는 실패를 한 번 «본 뒤에만» 생긴다.** 그러니 부재의 뜻은 「한도가 있다」가 아니라 「마지막 실패 이후 아직 아무도 안 태웠다」뿐이고, 두 뜻이 같은 모양으로 나온다. 곁들여 나를 확신시킨 정황이 하나 더 있었는데 그게 오히려 반대 증거였다 — 같은 날 03:54Z 심문런이 성공해 마커를 자가 해제했고, 나는 그것을 「풀린 실측」으로 읽었지만 실제로는 **그 심문이 남은 한도를 태운 쪽**이었다. 즉 성공은 여유의 증거가 아니라 소진의 원인이었다. 이 계열의 조항은 정본에 이미 있다(현재 상태는 현재를 읽어 판정한다 · 시점 의존 조회의 출력은 그 자체로 판정이 아니다 · 「어디에도 없다」는 부정 판정에 열 대상이 없다 — F174·F348·F403). 그래서 이것은 새 발견이 아니라 **있는 조항을 한 번 더 어긴 것**이고, 어긴 형태가 특히 나쁘다: 부정 판정을 근거로 «비가역이 아닌» 실행을 골랐기에 값이 싸게 드러났지만, 같은 독법을 배포·삭제 층에 쓰면 그때는 안 싸다. 값한 것 하나 — 태워 본 덕에 표식이 참이라는 직접 증거가 나왔고, 덤으로 그 줄의 리셋 날짜가 낡았다는 것도 잡혔다(줄은 8-18 12:39, 벤더는 8-20 12:45). | ⏸ 장치 0 · 순서 규칙으로 닫는다. 도구는 이미 옳게 말하고 있었다 — 한도 차단 메시지 자체가 「풀렸는지 확인차 강행: --한도무시 (성공 1회면 마커가 스스로 지워진다)」라고, «태워 보는 것»이 유일한 증거임을 적어 둔다. 내 잘못은 그 도구를 우회해 내부값(한도상태)을 직접 읽고, 태우기 «전»에 판정을 문서에 적은 것이다. 그래서 처방은 새 장치가 아니라 순서다: 차단 표식 재측정은 「재고 끝」이 아니라 「재고 곧장 태워 본다」까지고, 되돌림 비용이 0에 가까운 층에서는 그게 가장 싼 검증이다(대기열_표식실측_0814.md 남은 몫 3에 못박음). 새 장치를 세우지 않는 이유 = 풀림은 원리상 사전 관측이 불가능해 어떤 검사도 「모름」밖에 못 낸다. 닫는 조건 = 이 계열 2번째 발생 시 한도상태를 3상태(막힘·모름·미확인)로 갈아 null 을 풀림으로 못 읽게 한다. 2026-08-14 local_271b2a6d 판정. |
