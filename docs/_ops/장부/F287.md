| F287 | 2026-08-09 | 실수 | 소스형 회귀에 「순서」 조건을 넣었다가 멀쩡한 코드를 세 번 빨갛게 만들었다 — 글자 위치는 실행 순서가 아니고, 정의와 호출은 글자로 안 갈린다. ①전역 검사에 「대상알림이 database/query 보다 먼저인가」를 넣자 교정확정·동의발급·명부등록이 걸렸다(그 문자열이 파일 위쪽 sql() 정의 안에 있다). ②기준을 「자격증명.읽기 뒤인가」로 바꾸자 원격SQL 이 걸렸다(대상알림 정의가 읽기 위에 있다). ③내 명부등록 회귀도 행동정하기( 로 찾아 정의를 쟀다. 셋 다 코드가 아니라 재는 층이 틀렸고, 증상은 매번 「멀쩡한 것이 빨개진다」였다. 처방 = 소스 문자열로 순서를 잴 때는 반드시 호출부만 골라내는 표기(인자까지 포함한 호출 문자열)로 못 박고, 그게 안 되면 순서는 전역이 아니라 그 파일을 아는 회귀에 맡긴다. | ⏸ /evolve v8.7(2026-08-09) 판정=안 닫음 — 회귀 작성법이라 그 층이 지침이 아니다. 근거=docs/지침_이력.md:492 (v8.7 개정 기록). 다시 재지 않는다 — 08-12 두 세션이 각각 이 12건을 「미측정」으로 재대조했다(F386). |
