| F649 | 2026-08-19 | 실수 | 러너설치 --확인 이 online 으로 job 을 돌리는 중인 러너를 「없다」고 말했다 — LABEL=linux(소문자)를 grep -q 로 찾는데 GitHub 가 붙이는 기본 라벨은 Linux(대문자)라 self-hosted,Linux,X64 에서 안 잡혔다. 워크플로는 멀쩡하다(Actions runs-on 매칭은 대소문자 무시) — 러너는 정상이고 확인 도구만 눈이 멀었다. 그 오판이 낸 처방이 「세우려면 설치하라」여서 따르면 멀쩡한 러너를 215MB 다시 깐다(F103 계열). 어제 F642 와 같은 형태다 — 안 도는 게 아니라 돌면서 남의 답을 낸다 | has_label() 을 awk 로 신설 — 라벨을 콤마 경계로 갈라 대소문자 무시 비교(부분일치 금지: linux-arm 오인 차단) + online 여부를 별도 축으로. --확인 을 세 갈래로 나눴다(🟢셋 다 섬 / ⏸등록됐지만 오프라인=«다시 설치할 것 없다» / ⬜아직) — 미검증과 미설치를 한 칸에 뭉치면 「기계를 켜라」 대신 「설치하라」가 나간다. --라벨판정 통로로 회귀가 실물 함수를 잰다(F063) + tests/러너설치.test.js 6건 신설: 실물 줄·옛 grep 이 못 찾음을 증명하는 탐지력·오프라인 분리·linux-arm 경계·0개 수렴·오프라인 갈래에 설치 처방 금지 |
