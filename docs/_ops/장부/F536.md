| F536 | 2026-08-17 | 마찰 | 이 저장소의 배포 게이트가 «형제 저장소의 작업본»에 매달려 있다 — 남이 편집 중이면 내 master 가 빨개진다. 실측 2026-08-17 local_ea5f83a4: 배포빚(라이브가 [v9.242]에 멈춰 다섯 판 미배포)을 갚으러 test-ci 를 돌리니 fail 4. 얼린 트리(detached origin/master)에서도 산 작업본에서도 «같은 4건»이라 F240 축 B 의 「커밋 안 된 작업 탓」 카브아웃이 안 먹었다 — 그래서 진짜 적색으로 읽고 고치러 갈 뻔했다. 다시 그려서 재니(화면 회귀는 다시 그려야 잰다 · F525) 갈린 대목이 딱 한 줄이었다: `exam.result` 의 도달0 3→2·빨간 칸 5→4. 그 한 줄의 출처가 형제 저장소 SYNK-talk 의 «미커밋» 파일 5벌(lib/이벤트검증.js·functions/teach/index.ts·동봉.json·계약/수집_교정_계약.json·새 lib/시험결과.js · 13:28 편집 = 살아있는 세션의 진행 중 트랙)이었다. 배선 둘이 형제의 **HEAD 가 아니라 작업본 파일**을 읽는다 — ①tests/계약.test.js 가 `../SYNK-talk/계약/*.json` 을 fs 로 읽어 대조 ②tools/이해대장.js 가 형제 실측을 화면에 굽고 tests/대장동봉.test.js 가 그 화면을 커밋본과 대조. 결과: 형제 세션이 저장하는 «순간» 이 저장소 master 가 빨개지고, 그 적색은 고친 사람이 아니라 **그 뒤 배포하려는 모든 세션**을 막는다(clasp-guard 가 tests/*.test.js 를 게이트로 쓴다). 이 저장소는 정작 자기 안에서는 같은 병을 이미 고쳤다 — F302 「작업본이 아니라 스테이징을 잰다」가 대장동봉·계약동봉 급소에 회귀로 박혀 있고, 형제 축만 그 규율 밖이다(같은 판정을 두 곳에 다르게 적은 자리). 처방 후보 = 형제 대조를 `git -C ../SYNK-talk show HEAD:<경로>` 로 읽어 남의 작업본이 내 게이트에 안 새게 하고, 형제가 dirty 면 fail 이 아니라 skip 으로 드러낸다(F296 「repo 밖에 기대는 검사는 skip 으로」 · F207 분모와 함께). ⚠ 대가를 함께 적는다 — HEAD 로 재면 형제가 커밋만 하고 push 안 한 창을 못 보고, dirty skip 은 「안 쟀다」가 초록과 같은 모양이 되지 않게 분모를 말해야 한다. 지금은 안 고쳤다(형제 트랙이 도는 중이라 그 파일들이 곧 바뀐다 · 남의 미커밋은 그대로 두고 보고한다) — 신고만 남긴다. | |
