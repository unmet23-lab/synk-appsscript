| F585 | 2026-08-17 | 마찰 | 점유 판정이 **조사 비용을 다 치른 뒤에야** 발동한다 — board-guard 는 Write 시점 게이트라, 「이 트랙이 비었나」를 묻는 값이 「조사 한 벌」이다. 실측 2026-08-17 `local_1290868d` 에서 연속 2회: ①미처분 검수(런 `6837fa`)를 「그 트랙 워크트리가 사라졌고 PR #86 이 머지됐으니 주인 없음」으로 읽고 지적 6건을 master 실물과 전건 재대조(P1 2 이미 수리·P2 2 생존)한 뒤 선언에서 차단 — 주인 `local_41cc57e7` 이 살아 있었다. ②`캐시성적()` 출력토큰 칸을 장부·코드·사슬까지 훑어 처방을 확정한 뒤 선언에서 차단 — `index.ts` 주인 `local_00554f3c` 이 살아 있었다. 두 번 다 **가드는 옳게 막았다**(중복 구현 0). 새는 것은 판정이 아니라 **순서**다: 워크트리 소멸·PR 머지·⏹ 부재는 전부 「주인 없음」의 간접 증거이고(세션은 master 에서도 일한다), 직접 증거인 보드 활성 줄은 **내가 쓰려 할 때만** 읽힌다. 그래서 비켜난 뒤 남는 것이 조사분뿐이라 다음 세션이 같은 자리를 또 판다(이번에 내가 판 자리 둘 다 앞 세션이 이미 팠을 자리다). 처방 후보 ①`대기열.js --집기` 처럼 **선언 전에 묻는 조회**를 가드와 같은 판정 함수로 연다(`node tools/board.js --점유 <경로…>`) ②세션시작 출력이 「지금 잡혀 있는 파일」을 경로 목록으로 낸다(지금은 트랙 문구만 나와 파일 대조가 사람 눈이다) ③차단문이 조사분을 버리지 않게 그 자리에서 「이 실측을 주인에게 넘길 통로」를 준다(F226 경로와 같은 축). ⚠①은 「물어보고 나서 남이 먼저 선언」하는 창이 남는다 — 없앨 수 없고, 지금 창(조사 한 벌)보다 짧다는 것이 이득이다. | |
