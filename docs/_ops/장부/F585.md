| F585 | 2026-08-17 | 마찰 | 점유 판정이 **조사 비용을 다 치른 뒤에야** 발동한다 — board-guard 는 Write 시점 게이트라, 「이 트랙이 비었나」를 묻는 값이 「조사 한 벌」이다. 실측 2026-08-17 `local_1290868d` 에서 연속 2회: ①미처분 검수(런 `6837fa`)를 「그 트랙 워크트리가 사라졌고 PR #86 이 머지됐으니 주인 없음」으로 읽고 지적 6건을 master 실물과 전건 재대조(P1 2 이미 수리·P2 2 생존)한 뒤 선언에서 차단 — 주인 `local_41cc57e7` 이 살아 있었다. ②`캐시성적()` 출력토큰 칸을 장부·코드·사슬까지 훑어 처방을 확정한 뒤 선언에서 차단 — `index.ts` 주인 `local_00554f3c` 이 살아 있었다. 두 번 다 **가드는 옳게 막았다**(중복 구현 0). 새는 것은 판정이 아니라 **순서**다: 워크트리 소멸·PR 머지·⏹ 부재는 전부 「주인 없음」의 간접 증거이고(세션은 master 에서도 일한다), 직접 증거인 보드 활성 줄은 **내가 쓰려 할 때만** 읽힌다. 그래서 비켜난 뒤 남는 것이 조사분뿐이라 다음 세션이 같은 자리를 또 판다(이번에 내가 판 자리 둘 다 앞 세션이 이미 팠을 자리다). 처방 후보 ①`대기열.js --집기` 처럼 **선언 전에 묻는 조회**를 가드와 같은 판정 함수로 연다(`node tools/board.js --점유 <경로…>`) ②세션시작 출력이 「지금 잡혀 있는 파일」을 경로 목록으로 낸다(지금은 트랙 문구만 나와 파일 대조가 사람 눈이다) ③차단문이 조사분을 버리지 않게 그 자리에서 「이 실측을 주인에게 넘길 통로」를 준다(F226 경로와 같은 축). ⚠①은 「물어보고 나서 남이 먼저 선언」하는 창이 남는다 — 없앨 수 없고, 지금 창(조사 한 벌)보다 짧다는 것이 이득이다. | `node tools/board.js --점유 <경로…>` — **선언하기 전에** 묻는 조회. 착지 `2192caf4`+`d0319878` (PR #93).  무엇이 실제로 막나 — 새 통로를 짓지 않고 **판정을 한 벌로 모았다**: `board-guard` ④ 의 `자리들`(표식 머리 · 파일 경로 · `#Q` · repo 밖 공유 자원) 몸통을 `tools/lib/보드.js` 로 옮기고, 가드는 그 한 벌에 위임한다(사본 0 — 안 쓰이게 된 `표식` require·`금지스팬빼기`·`파일칸` 은 지웠다. 남기면 그게 두 번째 통로다). 분모도 가드와 같다 — master 보드 줄 + 미머지 가지 안의 선언(F528·F521).  이 트랙의 계약이 회귀로 서 있다: **같은 픽스처를 조회와 가드에 함께 먹여 결론이 갈리면 빨개진다**(네 축 전부). 규칙을 베껴 두 벌이 되는 순간 그 검사가 잡는다 — 갈라지면 조회가 「비었다」고 답한 자리에서 가드가 막고, 그건 이 병을 고치는 척하며 그대로 두는 것이다.  거짓 🔴 를 안 만든다(이 도구는 선언 «뒤에도» 계속 불린다): 🎫 놓은 줄(F285)·내 줄(F291 축)·완료 줄은 점유로 안 센다. 첫 실사용이 「내 줄을 남의 것으로 읽는」 것을 잡아 고쳤다.  생사는 **라벨이지 판정이 아니다**: 죽은 주인의 줄엔 ⏹ 를 붙이되 결론은 가드와 동일하게 둔다 — 「죽은 줄을 자리에서 뺄까」는 ⏳유호 판정이라(F582 ㉠ · F589 가 빈도 재료) 도구가 정할 것이 아니다. 못 쟀으면 아무 말도 안 붙인다(F207).  🔴 신고 원문을 되넣어 봤다(F083) — 변이 ①(자리 매칭 죽이기)이 정확히 옛 상태(조회가 아무것도 못 본다)이고 회귀가 빨개졌다. 변이 **9/9 · 구멍 0 · 미적용 0** · 기준선 초록 분모 10 · 가드 회귀 116/116(이관이 가드를 안 깼다). 🔴 그 변이가 **회귀가 아니라 도구의 구멍**을 잡았다: 질의를 파일 칸에만 넣어서 «표식 머리» 축(`F583`·`②-20` · F203 이 닫은 자리)을 원리상 못 물었다. 토큰마다 트랙·파일 양쪽 칸에 놓아 닫고, 「표식이 두 번째 토큰일 때」까지 회귀로 못박았다(`d0319878`).  ⚠ 닫으면서 남기는 것(억지로 안 닫는다): · 창은 못 없앤다 — 「묻고 나서 남이 먼저 선언」하는 초 단위 창이 남는다. 이득은 그 창이 옛 창(**조사 한 벌**)보다 짧다는 것뿐이라 이건 **예약이 아니라 조회**다. 출력이 그렇게 말한다. · 홑 이름(`Code.js`)은 원리상 자리가 아니다(`파일자리` 가 2단 이상만 센다) — 물으면 exit 2 로 「못 뽑았다」라 답한다(초록 아님). · F585 처방 ②(세션시작이 잡힌 경로 목록을 낸다)는 **안 지었다** — 상주 출력이 늘어 경량 원칙과 정면으로 부딪치고, ①이 서면 그 값은 부를 때만 든다. ③(조사분을 주인에게 넘길 통로)도 안 지었다 — F226 축의 별도 설계라 이 트랙에 얹으면 둘 다 흐려진다. |
