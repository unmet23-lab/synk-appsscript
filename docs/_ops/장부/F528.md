| F528 | 2026-08-17 | 마찰 | board-guard 의 겹침 검사가 워크트리 가지 안의 보드 선언을 원리적으로 못 본다 — F521 이 소비자 한쪽(대기열)만 고치고 가드는 눈먼 채 남았다. 실측 2026-08-17 12:18: 나(a6dc3cbc)는 master 에 「PR #25 반입」을 12:18:14 에 선언했고, 7bddd6cc 는 같은 PR 을 12:18:43 에 **워크트리 가지 안에서** 선언했다(커밋 ff9de7bb, 가지 worktree-q100-collect-marker-scope). 두 선언 다 board-guard 를 지났는데 가드는 한마디도 안 했다 — 가드는 master 의 docs/_ops/보드/*.md 만 읽기 때문이다. F521 이 이걸 위해 만든 통로 `가지줄들()`(tools/lib/보드.js)을 쓰는 곳은 tools/대기열.js 와 tools/lib/보드.js 뿐이고 .claude/hooks/board-guard.js 에는 호출이 0건이다(grep 실측). 즉 「목록은 하나에서 파생」이 깨진 자리이고, 08-15 「워크트리+PR」 확정이 넓힌 구멍의 두 번째 얼굴이다(첫 얼굴이 F521). 새는 방향은 언제나 「통과」다 — 겹침을 못 본 가드는 조용하고, 대가는 되돌릴 수 없는 중복 구현이다. 지금 당장 안 고친 이유: board-guard.js 와 tests/board-guard.test.js 를 산 세션 02e398f7 이 PR #24 로 잡고 있어(보드 「작업중」) 같은 파일을 만지면 그 트랙과 충돌한다 — PR #24 머지 뒤에 집을 자리다. | ⏸ 안 고쳤다 — 고칠 파일을 산 세션이 쥐고 있어서다. 판정 자리: 2026-08-17 세션 a6dc3cbc, 착수 직전 실측. 고칠 곳은 `.claude/hooks/board-guard.js` 의 겹침 검사(④·⑧)를 `tools/lib/보드.js` 의 `가지줄들()` 위로 옮기는 한 자리인데, 그 파일과 `tests/board-guard.test.js` 를 산 세션 `02e398f7` 이 PR #24 로 「작업중」 선언해 들고 있다(보드 실측). 지금 만지면 CLAUDE.md 「보드에 '작업중'인 파일은 편집하지 않는다」를 정면으로 어기고, PR #24 가 담고 있는 유호 픽 ㉮ 수리와 같은 파일에서 충돌한다 — 겹침을 고치려다 겹침을 하나 더 만드는 모양이다. ▶여는 조건은 시간이 아니라 사건이다: **PR #24 가 머지되는 순간** 이 행을 집는다(그때 board-guard.js 의 임자가 사라진다). 그때 할 일도 이미 정해져 있다 — 겹침 판정의 분모를 master 보드에서 「master + 워크트리 가지 선언」으로 넓히고, 탐지력은 픽스처로 못박고(맹점②), 실저장소에는 거짓양성만 검사한다. ⚠ 이 보류는 「나중에」가 아니다 — 열린 채로 두는 대가를 적는다: 그동안 두 세션이 같은 워크트리·PR 을 이중 선언해도 가드는 계속 조용하다(오늘 실측 1건 = 나와 7bddd6cc, 29초 차). 즉 이 행이 열려 있는 동안의 위험은 「중복 구현」이고, 그것을 지금 막는 것은 기계가 아니라 사람이 세션 시작에 PR 목록을 읽는 습관뿐이다. |
