| F528 | 2026-08-17 | 마찰 | board-guard 의 겹침 검사가 워크트리 가지 안의 보드 선언을 원리적으로 못 본다 — F521 이 소비자 한쪽(대기열)만 고치고 가드는 눈먼 채 남았다. 실측 2026-08-17 12:18: 나(a6dc3cbc)는 master 에 「PR #25 반입」을 12:18:14 에 선언했고, 7bddd6cc 는 같은 PR 을 12:18:43 에 **워크트리 가지 안에서** 선언했다(커밋 ff9de7bb, 가지 worktree-q100-collect-marker-scope). 두 선언 다 board-guard 를 지났는데 가드는 한마디도 안 했다 — 가드는 master 의 docs/_ops/보드/*.md 만 읽기 때문이다. F521 이 이걸 위해 만든 통로 `가지줄들()`(tools/lib/보드.js)을 쓰는 곳은 tools/대기열.js 와 tools/lib/보드.js 뿐이고 .claude/hooks/board-guard.js 에는 호출이 0건이다(grep 실측). 즉 「목록은 하나에서 파생」이 깨진 자리이고, 08-15 「워크트리+PR」 확정이 넓힌 구멍의 두 번째 얼굴이다(첫 얼굴이 F521). 새는 방향은 언제나 「통과」다 — 겹침을 못 본 가드는 조용하고, 대가는 되돌릴 수 없는 중복 구현이다. 지금 당장 안 고친 이유: board-guard.js 와 tests/board-guard.test.js 를 산 세션 02e398f7 이 PR #24 로 잡고 있어(보드 「작업중」) 같은 파일을 만지면 그 트랙과 충돌한다 — PR #24 머지 뒤에 집을 자리다. | `.claude/hooks/board-guard.js` 의 겹침 검사(④)가 이제 `tools/lib/보드.js` 의 `가지줄들()` 을 분모에 함께 먹는다 — 커밋 `7806fe56`(PR #37). 통로는 하나에서 파생한다: F521 이 대기열용으로 낸 그 함수를 그대로 부르고, 여기서 정규식을 새로 적지 않았다. 🔑 가지 줄은 **겹침에만** 더했다 — `남의줄`(=`합치기`)은 상한 셈에도 쓰여서 거기 섞으면 master 에 없는 줄로 한도가 불고, 그 방향은 정당한 선언을 막는 거짓 차단이라 관습을 죽인다(F221). 🔴 짓다 잡은 것 하나를 함께 적는다(이게 실제로 막은 자리다): 가지 줄에 F291 면제(「내 파일에 있으면 내 줄」)를 그대로 주면, 세션이 **자기 보드 파일에 쓰는** 실사용의 기본 모양에서 이 검사가 통째로 침묵한다 — 즉 장치가 서 있는데 안 도는 상태가 된다(맹점 ④). `가지줄인가` 로 갈랐고 변이 ㉡ 가 그 자리를 지킨다. 처방도 갈랐다 — 가지 줄은 master 에 없어 `board-move` 로 못 치우므로 그 안내만 주면 따를 수 없는 처방이 되어 우회가 정상 통로가 된다(F103). 그래서 「`gh pr list` 로 주인·나이를 보고, 죽었으면 그 PR 부터 닫는다(`작업가지.js --닫기` 는 그 워크트리 안에서만 돈다)」를 가지 겹침일 때만 낸다. 회귀 = `tests/보드가지겹침.test.js` 7건(탐지력 2 · 거짓양성 4 · 모름 1) — 탐지력은 픽스처로 못박았다(`SYNK_BOARD_BRANCH_LOG` 주입구 · git 없이 서므로 CI 에서 안 깨진다 F296). 변이 **6/6 구멍 0 · 미적용 0**, 기준선 초록·분모 7. 그 변이가 실제로 잡아 고친 것 1건: 「내 파일이면 내 줄」 검사가 픽스처에 내 지문을 적어 둔 탓에 **파일 필터를 통째로 지워도 초록**이었다 — 엉뚱한 이유로 통과하던 것을 `ead12d63` 이 고쳤다. 기존 `tests/board-guard.test.js` 114/114 무변, 머지 후 합계 121/121. ⚠ 대가를 적는다: 새 선언이 있을 때만 `git log --branches` 를 한 번 부른다(상태 칸 갱신에는 안 든다). 못 훑으면 `모름` 으로 떨어지고 그때는 **그렇게 말한다** — 「가지에 선언 0」과 안 접는다(F207 · 변이 ㉤ 가 지킨다). ⚠ 안 닫은 것: 이 수리는 «분모»만 넓힌다. 두 세션이 **같은 초에** 각자 선언하는 경쟁(오늘 실측 29초 차)은 여전히 뒤에 쓴 쪽만 본다 — 그건 가드가 아니라 순서의 문제라 여기서 억지로 넓히지 않았다. |
