| F383 | 2026-08-12 | 마찰 | git-scope-guard 처방 「같은 명령을 그대로 다시 실행한다」가 신규(미추적) 파일에서는 따를 수 없다. 차단 시 앞선 git add 가 함께 풀려 재실행하면 pathspec 오류로 죽는다 — add 를 다시 해야 통과. 08-12 한 세션 2회(talk tests/eval대조.test.js · evals/출력_v6.json). 증상이 「처방대로 했는데 안 된다」라 우회를 정상 통로로 만든다(F103 계열). 후보 처방=차단문에 미추적 파일이 섞였으면 add 재실행을 처방에 함께 적기. | git-scope-guard ⑧ deny 처방문이 미추적 신규 파일을 관측하면(F383 갈래) 「git add -- 경로를 같은 호출 안에 앞세워라 · 차단은 실행 전이라 앞선 add도 무효」를 함께 적는다 — 5ea1d882 · 회귀 2(양성=미추적이면 문구·음성=추적뿐이면 문구 없음, tests/git범위가드.test.js) · 양성 시험은 문구 실재로만 통과하는 직접 결합이라 변이 동치 |
