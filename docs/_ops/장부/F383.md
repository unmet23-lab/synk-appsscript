| F383 | 2026-08-12 | 마찰 | git-scope-guard 처방 「같은 명령을 그대로 다시 실행한다」가 신규(미추적) 파일에서는 따를 수 없다. 차단 시 앞선 git add 가 함께 풀려 재실행하면 pathspec 오류로 죽는다 — add 를 다시 해야 통과. 08-12 한 세션 2회(talk tests/eval대조.test.js · evals/출력_v6.json). 증상이 「처방대로 했는데 안 된다」라 우회를 정상 통로로 만든다(F103 계열). 후보 처방=차단문에 미추적 파일이 섞였으면 add 재실행을 처방에 함께 적기. | |
