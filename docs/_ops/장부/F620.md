| F620 | 2026-08-18 | 마찰 | CI 적색 3건이 **한 원인의 세 얼굴**이었다 — 검사가 저마다 repo 밖 환경에 기대는데, 그 조항(F296)은 정본에 이미 있다. 실측 2026-08-18(리눅스 클라우드 세션이 `node tools/test-ci.js` 를 돌림 · 4496개 중 fail 3): ①`tests/지도대장.test.js:297` — 바탕화면이 없으면 `지도폴더()` 가 **던져서** 그 검사·`훑기()`·`시스템대장 --render` 의 「폴더 없음 → skip」 절 **셋이 한꺼번에** 무력화됐다(방어는 셋 다 이미 쓰여 있었다) ②`tests/장부유실.test.js:457` — 「살아있는 판」 후보가 **로컬 워크트리**만 세어, `origin/worktree-*` 에 멀쩡히 사는 F595·F610·F612·F614·F616 이 「유실」로 떴다(그중 넷은 30분 뒤 머지되자 저절로 사라졌다 — 사라진 게 아니라 안 온 것이다) ③`tests/시트도달.test.js:318` — 픽스처의 윈도우 경로 갈래를 `path.resolve` 가 이 기계 문법으로만 읽어 재료를 먼저 뭉갰다(윈도우에선 우연히 통과). 셋의 공통 모양은 **「유호님 로컬에서 초록 = CI 초록」이라는 암묵 전제**이고, 원격 Actions 가 0건(소진 · 리셋 9/1)이라 여태 아무도 안 쟀다. 그래서 「로컬 초록은 CI 초록이 아니다」가 정본에 있어도 **잴 층 자체가 없었다** — 조항이 안 지켜진 게 아니라 안 지켜진 것을 볼 눈이 없었다. ②는 처방까지 못 따르는 자리였다(F103): 배너가 「그 줄만 지금 장부에 되돌려 넣어라」라고 하는데, 따르면 남의 열린 트랙 신고를 내 가지에 복제해 머지 때 같은 번호가 둘이 된다(F586 그 사고). 신뢰성 조항의 「3번째 = 원인을 쓸 수 없게 만든다」에 정확히 닿은 자리라 처방 후보를 적는다 ①검사가 repo 밖(홈·워크트리·플랫폼·시각)에 닿는 통로를 **공용 게이트 하나**로 모으고, 그 게이트가 「없으면 skip · 못 세면 소리내기」를 대신 지운다(호출부마다 try/catch 를 다시 적는 지금 모양이 갈라지는 이유다) ②그 게이트를 안 거치고 `os.homedir()`·`worktree list`·`path.resolve` 를 직접 부르는 검사를 회귀가 금지한다(옛 통로 금지) ③리눅스 판을 재는 층을 되찾는다 — Actions 리셋 전까지는 클라우드 세션의 `test-ci` 가 그 층이므로, 트랙 종료 게이트에 「어느 OS 에서 쟀나」를 함께 적는다. 이번 세션은 셋을 각각 수리하고 회귀 7건·변이 10/10 을 붙였지만, 그건 세 얼굴을 지운 것이지 원인을 쓸 수 없게 만든 것이 아니다. | 2026-08-18 `a8b88e30`(③) + `bcdf0584`·`b9c3efb2`(②) — **원인을 쓸 수 없게** 만들었다(세 얼굴 수리는 신고 세션이 이미 했고, 여기 남았던 건 그 부분이다). ▶**진짜 원인은 처방 ③이었다**: `tools/test-ci.js` 가 지우는 축을 머리말에 **①시간대 ②홈** 둘로 못박아 놓고 「여기서 초록이면 CI 도 초록이다」라고 **단언**했다 — 플랫폼은 그 목록에 없다. CI 는 `ubuntu-latest`(linux · 워크플로 실측)이고 유호님 로컬은 win32 라, 그 문장은 **윈도우 세션에게 거짓 보증**이었고 F620 의 적색 3건이 그 증거다. F474·F608 과 **정확히 같은 병**(재지 않은 축에 대한 주장)이고 새는 방향은 「통과」다. 처방 = 지울 수 없는 축은 **이름 대고 말한다**: `lib/ci모사환경.js` 에 `러너판`·`CI판`(워크플로에서 읽는다 — 손으로 적으면 갈라진다)·`플랫폼줄` 을 세우고, `test-ci` 가 머리 줄에 `plat=` 를, 판정 **앞**에 플랫폼 줄을 낸다(초록에만 달면 「내 적색이 저쪽 판에도 나는가」를 못 가른다). 세 갈래를 **다르게** 낸다 — 같다/다르다/**모른다**(못 읽은 것을 「같다」로 접지 않는다). 머리말의 단언도 「**같은 플랫폼의** CI 도 초록이다」로 거뒀다. ▶처방 ②(옛 통로 금지) = `tests/소스검사통로.test.js` 가 「홈을 **방어 없이** 읽는 검사」를 **블록 단위**로 금지한다(파일 단위면 홈 읽는 그 검사가 무방비여도 초록이다 · 모듈 스코프도 분모에 넣는다 — 거기선 skip 할 수단이 아예 없다). 그러려면 렌즈가 하나 더 필요했다: `코드만` 은 리터럴을 보존해서 «나쁜 코드»를 문자열로 든 픽스처(`tests/메모리그래프.test.js`)가 위반으로 잡혔다 — 가드가 자기 픽스처에 걸리는 자리(맹점 ②). 그래서 `tests/lib/소스검사.js` 에 **`부름만`**(주석 + 리터럴 «속»까지 눕혀 「부르는가」만 남긴다)을 옵션 깃발이 아니라 **이름 둘로** 세웠다(그 파일 머리말의 정책 그대로). 실측 거짓양성 **0**(홈 읽는 3자리 전부 이미 방어 중). ▶**처방 ①(공용 게이트)은 안 했다 — 실측으로 기각한다**: `os.homedir()` 직접 호출이 **3자리**뿐이고 셋 다 이미 `existsSync`+`t.skip` 방어가 있다. 3개 호출부를 위한 라이브러리는 과잉이고, F620 ①의 병(방어가 있는데 앞이 **던져서** 안 닿음)은 층이 달라 그 게이트로 안 닫힌다 — 그건 `tests/지도대장.test.js` 의 「어느 기계에서도 안 던진다」가 이미 진다. 회귀 신설 **11건**(③ 5 · ② 6) · **변이 8/8 + 6/6**(구멍 0 · 미적용 0). ⚠**변이가 내 구멍 2건을 잡았고 고쳤다**: ㉠정규식 픽스처를 `/os\.homedir\(\)/` 로 써서 글자가 달라 그 축이 **안 재지고 있었다** ㉡따옴표 보존 사유를 「안 그러면 뒤가 문자열로 먹힌다」로 적었는데 **거짓**이었다(렉싱은 원문에서 이미 끝난다) — 진짜 축(「붙으면 없던 낱말이 생긴다」)으로 다시 썼다. `test-ci` **4583 중 pass 4448 · fail 0 · skip 130**(리눅스 판 · master 39커밋 병합 후). ⚠남는 한계 = 이 트랙은 **못 잰 축을 말하게** 했을 뿐 재게 하지 못한다 — 윈도우 세션은 여전히 리눅스를 못 재고, 그 층은 원격 Actions(소진 · 리셋 9/1)나 클라우드 세션이 진다. |
