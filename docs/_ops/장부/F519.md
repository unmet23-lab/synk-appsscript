| F519 | 2026-08-17 | 실수 | repo 밖 공유 파일(메모리 인덱스)에는 세션 조율 장치가 하나도 없어 두 세션이 같은 8차 압축을 동시에 했고, 내 설치가 남이 내린 5줄을 되살렸다 — 발각은 우연이었다(내 로그의 바이트 수가 22,776→20,101 로 안 맞은 것뿐).  저장소 안 파일은 층이 넷이다: git status · 작업본소유자 · board-guard · repo-staleness 훅. `~/.claude/.../memory/MEMORY.md` 는 **그 넷 전부의 밖**이라 남이 편집 중인지 볼 통로가 0이다. 08-16 실측: `local_24ba4d6b` 와 내가(`local_15ff9966`) 같은 유호 지시로 각자 8차를 돌렸고, 서로를 끝까지 몰랐다.  새는 방향은 언제나 「통과」다 — 내 조립은 103줄 base 위에서 초록이었고, 훅 `--check` 도 초록이었다(둘 다 «지금 디스크» 를 안 본다). 병합 뒤 실측: 17,323자·97줄·보호 플래그 유실 0.  곁가지 실사고 1건: 조립 산출물 `MEMORY.new.md` 를 메모리 폴더에 뒀더니 `decision-queue.js` 가 그걸 새 소스로 읽어 ⏳ 유령 34건을 만들었다(80→114 · 지우자 80 복구). 그 도구는 폴더 안 `*.md` 를 훑는다 — 임시 산출물은 스크래치패드에만 둔다. | |
