| F504 | 2026-08-16 | 마찰 | 아카이브로 걷힌 완료 줄이 보드에 되살아났는데 하루 동안 아무 장치도 안 울었다 — 「되붙이지 마라」는 프로즈로만 서 있다  실측 (2026-08-16 · 세션 162bb4da): · `docs/_ops/보드/f8cb9aaf.md` 가 스테이지에 「new file」로 떠 있었다. 그 줄(#Q38 §10-3 검수 콘솔)은   `0d2eb217`(08-15 22:12)이 이미 아카이브로 이관하며 파일을 지운 것이었다. 파일 mtime 은 08-15 22:33 —   **걷힌 지 21분 뒤에 되살아났다.** · 아카이브 `docs/세션보드_아카이브.md` 37번 줄과 **글자 그대로 같다**(trim 후 463자 일치, node 로 대조).   즉 같은 완료 줄이 아카이브와 보드에 **둘** 있었다. · `node tools/board.js` 는 그 줄을 그대로 렌더했다 — 활성 표에 24시간 떠 있었고 상한 분모를 먹었다.   (#Q93 「죽은 보드 줄이 자리를 영구히 잠근다」와 같은 자리를 다른 원인으로 먹은 것이다.)  왜 아무도 못 짚었나 — 장치는 있는데 **축이 다르다**: · `board-guard` 검사⑨ = 「조립기가 못 읽는 규격 밖 줄」(유령)만 막는다. 이 줄은 규격이 멀쩡해서 안 걸린다. · `tools/board.js:105` 는 반대 방향만 본다 — 「내 줄이 보드에 없는데 아카이브에 있다」(F368 되찾기).   거기 이미 이렇게 적혀 있다: 「아카이브 줄은 그때의 기록이다 — 되붙이면 상한을 먹고 「지금 잡은 것」과 섞인다」.   **그 문장이 정확히 일어난 일을 예언했는데, 막는 기계가 없어 프로즈로만 남았다.** · `작업본소유자` 는 이 파일을 「❔ 주인 모름」으로만 셌다 — 되살아난 중복이라는 판정은 아무도 안 한다.  이 자리의 모양 = 지침 v9.2 맹점 ④(맞는 얼굴로 틀린 값). 보드는 세션 간 조율의 정본 화면인데, 끝난 줄이 「진행중」의 얼굴로 앉아 있으면 다른 세션은 그 트랙을 산 것으로 읽는다.  처분(이번 세션): · 사본을 걷었다 — 아카이브에 글자 그대로 있음을 먼저 못박았으므로 잃은 내용 0.   (HEAD 에 없는 파일이라 커밋 없이 디스크에서 지우는 것으로 끝난다.) · **기계는 안 세웠다** — 하네스 한 달 봉인(유호 승인 08-16 · 「새 장치 0」)에 그대로 걸린다.   메모리 [[harness-deletion-verdict-0816]] 의 과녁이 「삭제」가 아니라 «신설 증가율»이라,   여기서 가드 하나를 더 세우면 그 판정을 정면으로 거스른다.  봉인이 풀리면 값을 재는 순서(그때의 재료로 남긴다): ① 보드 줄이 아카이브 줄과 **완전 일치**하면 board-guard 가 쓰는 순간 deny — 처방은 「되붙이지 말고    새 줄로 선언해라」(상태 칸이 달라지므로 자기 처방이 그 가드를 통과한다 · F103 검사). ② 대가: 아카이브를 못 읽으면 「중복 0」이 아니라 **판정 불가**로 낸다(F207 — 미실행과 통과가 같은 모양이면 안 된다). ③ 닫을 것 1개: `board.js` 의 F368 되찾기 경고문 중 「되붙이면…」 프로즈 절 — 기계가 서면 그 문장은 중복이다. | |
