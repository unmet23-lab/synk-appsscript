| F354 | 2026-08-12 | 마찰 | 보드 선언에서 「안 만진다」를 적으면 가드가 「만진다」로 읽는다 — 부정 표기를 못 가르는 자원 매칭  실측(2026-08-12 · `local_504cbcb1`): 새 트랙을 선언하며 만지는 파일 칸에 관례대로 「계약 JSON 편집 0 · talk 0 · clasp push 0 · 운영 DB 0」이라 적었다 — 전부 **안 만진다**는 뜻이다. board-guard 가 이것을 점유 선언으로 읽고 deny 했다: "공유:clasp-라이브 ← local_ed8b5124".  자리 = `.claude/hooks/board-guard.js:809`   { 자리: '공유:clasp-라이브', re: /clasp\s*push/i } 정규식이 낱말만 본다. 그런데 보드의 실제 표기 관례는 **안 건드리는 자원을 「… 0」으로 명시하는 것**이고 (현재 표 18줄 중 다수가 「배포 0 · 운영 DB 0 · talk 0」 형태다), 그 관례가 가드에는 안 보인다.  왜 나쁜가 — 새는 방향이 「통과」가 아니라 「과잉 차단」이라 덜 위험해 보이지만, 처방이 따를 수 없다: 실제로 안 겹치는데 「비켜나라」고 하니 세션이 할 수 있는 일은 **그 낱말을 선언에서 빼는 것**뿐이다. 내가 방금 그렇게 통과했다(「clasp push 0」 → 「라이브 내보내기 0」). 그 순간 「이 세션은 clasp push 를 하지 않는다」는 정보가 보드에서 사라진다 — 가드가 **정보를 지우는 방향으로 압력을 만든다**(F103). 한 번 그 회피가 관례가 되면 다음엔 진짜 겹침도 같은 모양으로 안 보인다.  ⚠ 고칠 때의 함정: 「0」만 예외로 빼면 이번엔 「clasp push 안 함」·「clasp push 금지」가 남는다. 사람이 실제로 쓰는 부정 표기부터 세고(보드 아카이브가 표본이다) 픽스처로 못박아야 한다 — 탐지력을 실저장소로 재면 표기가 하나 늘 때마다 조용히 샌다. | tools/lib/보드.js `만지는텍스트` — 부정의 사정거리를 조각(' · ')에서 **구절**로 좁혔다(`1ff96625`). 소비자 둘(board-guard 겹침 판정 · track-collision 내 자리 판정)이 서로 다르게 틀리고 있던 것을 이 한 곳으로 접었다. 🚫 는 그 자리부터 조각 끝까지 부정하고(앞은 살린다), 부정 낱말(0·없음·무변경·금지·제외·읽기만·안 함…)은 구절 끝에서만 센다. ⚠ 신고문이 경고한 함정을 실측으로 갈랐다 — 표본은 아카이브+보드 32파일·데이터행 663·토큰 1020이고, 토큰 바로 뒤 부정 꼬리는 **경로 축 0건 · `clasp push` 축 2건**이다(부정은 산문 낱말로 만든 자리에만 붙는다). 🆕 신고에 없던 **거울면 결함**을 같이 닫았다: 금지스팬빼기가 🚫 든 조각을 통째로 버려 「🚫 앞」의 진짜 선언이 사라지고 있었다 — 실측 **59건**(고유 경로 24 · 예: `tests/board-guard.test.js`), 새는 방향이라 증상이 조용했다. 전후 대조 = 되찾음 59 · 잃음 2(둘 다 `clasp push 0`). 회귀 `tests/부정표기.test.js` **17건**(탐지력은 픽스처 · 실저장소엔 거짓양성만) · **변이 6/6** — 그중 M5(구절 경계에서 괄호 제거)는 처음에 살아남아, 회귀에 없던 실보드 표기 `tools/사슬점검.js (배포 0` 를 찾아 넣어 잡았다. CI 모사 2813건 fail 0(원격 run 층은 Actions 한도 소진으로 미검증). ⚠ 접두형 「읽기만: <경로>」는 **일부러 안 뗐다**(같은 낱말이 접두·꼬리 두 모양이라 사정거리를 지어내면 진짜 자리를 놓친다) — 회귀가 그 경계를 못박는다. |
