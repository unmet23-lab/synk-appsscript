| F591 | 2026-08-17 | 실수 | 새로 만든 소스는 **커밋(정확히는 `git add`) 전까지 저장소의 소스 검사 전체가 안 본다** — 그 검사들이 `git ls-files`(추적 파일)로 범위를 잡기 때문이다. 그래서 «이미 회귀가 서 있는 병»을 짓는 동안 그대로 다시 만든다. 실측 2026-08-17(`local_fbfdb2e9` · F584 트랙): 새 훅의 복합키 구분자로 NUL 을 썼는데 그것이 소스 파일에 진짜 제어문자로 박혔고(3개), ripgrep 이 그 파일을 **바이너리로 취급**해 Grep 에서 통째로 사라졌다(`out.set` 검색이 "binary file matches" 한 줄만 냈다 — 즉 앞으로 그 파일은 코드 검색에 안 잡힌다). 그런데 `node --check` 초록 · 자체 회귀 13/13 초록 · 편집 화면에서도 공백처럼 보였다 — 어느 층도 안 짖었다. 발각은 **변이 검사의 「미적용」**이었다(찾기 문자열이 0번 맞아서 · 우연이다: 마침 그 줄을 변이 과녁으로 골랐기 때문이고, 안 골랐으면 그대로 머지됐다). 같은 병 2번째다 — `tests/훅이식성.test.js` ④ 가 정확히 이걸 막으려고 서 있고(F158 · cc51710 에서 복합키 구분자 날문자 2건), 검사 제목이 「구분자 상수는 이스케이프 표기로」다. 그 검사는 내 파일이 미추적이라 **범위 밖**이었다. 게다가 `git add` 뒤 그 회귀가 실제로 빨개졌는데 사유는 「훅으로 등록됐는데 날문자 스캔 밖」이었다 — **맞는 얼굴로 다른 것을 가리켰다**: 진짜 사유는 「미추적」이지 「날문자」가 아니었고, 그 시점엔 이미 NUL 을 손으로 고친 뒤였다. 즉 이 저장소에서 새 파일은 F025(미추적=유일한 무보호 상태)가 말하는 «git 이력 없음» 말고 **소스 품질 검사 전량이 안 도는 창**도 함께 연다. 처방 후보(안 지었다 — 재료다): ㉠ 날문자 판정을 `tests/` 안에서 공용 통로로 빼고 `Edit`·`Write` PostToolUse 에서 그 자리에 알린다(커밋까지 안 기다린다 — F584 훅이 방금 같은 형태로 섰다) ㉡ 스캔 범위를 `git ls-files` + 작업본 미추적 `.js` 로 넓힌다 ㉢ 「범위 밖」과 「위반」을 서로 다른 사유로 말하게 한다(지금은 둘이 같은 실패문으로 나온다). | |
