| F489 | 2026-08-15 | 마찰 | `작업본소유자 --hook` 이 부하로 `git status` 를 놓치면 **아무 말도 안 한다** — 그 침묵이 「미커밋 0 · 충돌 0」과 같은 모양이다. #Q98(fetch 만료)을 고치다 **옆에서** 실측한 이웃 자리다.  ■ 무엇이 새나 — `조사()` 는 `미커밋들()` 이 `null` 이면 `git못부름: true` 로 접고, `보고()` 는 그 갈래에서 **훅 모드일 때 빈 문자열**을 돌려준다(`tools/작업본소유자.js:863` — `return 훅 ? '' : '[작업본소유자] git 을 못 불렀다 — 판정 불가(0건 아님).'`). 손으로 부르면 「판정 불가」라고 말하지만 **세션 시작 훅에서는 한 줄도 안 나온다.** 그런데 이 도구의 침묵은 규약상 「위험 없음」이라는 뜻이다(같은 파일의 회귀 `--hook 은 🔴가 없으면 침묵한다` 가 그 계약이고, 다른 회귀는 아예 「침묵이 «깨끗함»으로 읽힌다」라고 적어 두었다). 즉 **못 잰 회차와 깨끗한 회차가 글자 그대로 같은 출력**이다.  ■ 왜 「git 이 없는 사람」 얘기가 아닌가 — 그 침묵은 git 미설치를 위해 만든 것이라 정당하다. 문제는 **같은 문에 타임아웃 만료가 함께 들어온다**는 것이다: `git()` 은 `spawnSync` 의 `timeout: 5000` 만료도 `null` 로 낸다. #Q98 에서 fetch 쪽은 갈랐지만(`git상세` 의 `못쟀다` 갈래) `미커밋들()` 은 여전히 옛 통로를 쓴다.  ■ 실측 2026-08-15(세션 `22ef3c6d`) — 닿는가:   · `git status --porcelain -uall` 3회 = **499ms · 259ms · 490ms** (예산 5,000ms 의 **10.0%**).   · #Q98 이 잰 부하 배율 = 같은 파일의 검사들이 0.3~2.9초 → **3~37초**(약 12배 · node 40+ 프로세스).   · 499ms × 12 ≈ 6초 > 5초 ⇒ **닿는다.** 평시엔 여유가 10배라 안 보이고, 노트북이 바쁜 회차에만 조용해진다 — 하필 세션이 여럿 도는 그때가 미커밋 충돌이 가장 잦은 때다.   ⚠ 「그 회차에 실제로 침묵이 났다」를 잡은 건 아니다. 여기서 잰 것은 **예산까지의 거리**이고, 12배는 #Q98 의 다른 검사에서 온 수다.  ■ 처방 후보(고르는 것은 이 신고 밖)   ㄱ) `미커밋들()` 도 `git상세` 로 갈아 만료를 **제3칸**으로 받고, 훅 모드에서 만료일 때만 한 줄 말한다 — 「지금은 못 쟀다(부하) · 조용할 때 다시」. git 미설치의 침묵은 그대로 둔다(그건 매 세션 나면 잔소리다).   ㄴ) 예산을 로컬 git 과 네트워크로 갈라 로컬만 넉넉히 준다 — ⚠ #Q98 이 이미 기각한 축이다(값은 조건을 못 이긴다). 다만 로컬 `git status` 는 네트워크가 없어 「오래 걸리면 곧 부하」가 참이라, fetch 때와 논거가 같지 않다.   ㄷ) 안 한다 — 침묵이 「깨끗함」으로 읽히는 것을 남긴다. 🚫 이 저장소가 반복해 기각한 모양이다.  🔑 계급 = 지침 v9.2 「맞는 얼굴로 틀린 값을 내는 자리를 적는다」 + 「0은 분모와 함께 쓴다」. 새는 방향은 여기서도 **통과**다. | |
