| F451 | 2026-08-15 | 실수 | 라이브2 v1 결함 2건이 유호 검수에서 발견 — ①웃음 눈 패치: 눈찾기 오검출(+25px)로 왼눈이 크롭 밖 잘림+페더 소거, 차이 마스크는 전면 포화(특징 선택 죽음) ②치마 팔로우스루 파티션이 시프트 때 알파 띠를 비워 몸통 분리로 보임. 내 정지컷 검수가 웃음 들썩임 구간(6.0~6.6s)을 안 떠서 놓쳤다. 수리=검출이탈→상대 강등·특징 합집합 마스크·연속 워프·스티치 심 페더 게이트(마스코트모션.py 08-15) | ⏸ ⏸ 판정 2026-08-16 `local_7563cbed` — **처방 4/4 가 코드에 서 있다. 남은 것은 「구운 결과를 눈으로 본 도장」뿐이고 그건 마스코트 트랙 세션 몫이다.** 실측 위치: ①검출이탈→상대 강등 = `tools/마스코트모션.py:442` ②특징 합집합 마스크 = `:480` ③연속 워프 = `:333` ④스티치 심 페더 게이트 = `:485~492`(주석이 「그날 실사고의 모양 그대로라 기계로 막는다」로 자기 근거를 적어 뒀다 · 임계 -85 = 스티치 심만 보고 · 대가까지 명기). ⚠ **회귀는 0이다** — 자산층(파이썬 이미지 파이프라인)이라 `tests/` 에 대응이 없고, 대신 ④가 굽는 순간 도는 **런타임 자가 게이트**다. 그래서 「고쳤다」의 마지막 근거인 «구운 프레임을 눈으로 봤다»를 내가 못 댄다 — 이 신고 자체가 「내 정지컷 검수가 6.0~6.6s 구간을 안 떠서 놓쳤다」라, 코드에 처방이 있다는 것은 **간접 증거**다(CLAUDE.md: 현재 상태는 현재를 열어 판정한다). 🔑 이 칸을 «해소»로 찍으면 그 트랙 세션이 다시 안 본다 — 그게 F092(안 고쳤는데 닫았다)의 모양이다. ▶닫는 조건 = 라이브2 v2 를 구워 6.0~6.6s 를 실제로 뜬 세션이 `resolve` 한다. |
