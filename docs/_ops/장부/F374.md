| F374 | 2026-08-12 | 마찰 | 보드의 🎫 가 「이미 나간 배포」를 계속 일감으로 내민다 — 낡음이 기계로 읽히는데도 사람이 매번 연다  실측(2026-08-12 · `local_d6cf3c72` 세션 시작): 보드 🎫 7건 중 **2건이 죽은 일감**이었다. · `52a7abc6` 🎫「`--commit c0f148aa` 재검수→통과 시 clasp push(215~217 묶음)」 · `9a710cd8` 🎫「`--commit de6b34d5` 재검수→배포(210~214)→as 스윕」 둘 다 이미 끝났다 — `local_a668c152` 가 08-12 16:20 에 **v9.215~220 을 한 묶음으로** 밀었고 (`docs/버전_이력.md` 말미), 내가 직접 잰 것도 같다: `node tools/배포판점검.js --라이브` = 「라이브 = 저장소(@HEAD · 배포집합 15개 바이트 동일 · 미커밋 0)」.  왜 나쁜가 — 새 세션이 트랙을 고르는 **첫 판단**에서 샌다. 보드는 스스로 「남이 이미 해버린 일과 내가 더 할 일은 이 표에서 같은 모양이다」라고 경고하지만, 처방이 「실물을 직접 연다」뿐이라 **모든 세션이 같은 확인을 다시 한다**(나는 커밋 로그·버전 이력·라이브 대조 3벌을 열었다). 7건 중 2건이면 29% 다 — 이 비율이면 확인 비용이 아니라 **오판 위험**이 문제다: 확인을 건너뛴 세션은 이미 나간 배포를 다시 밀거나(F070 중복), 반대로 산 일감을 죽은 것으로 보고 놓친다.  🔑 급소 = **이 낡음은 사람 판단이 아니라 기계가 읽을 수 있는 것이었다.** 「배포·clasp push· 재검수」를 말하는 🎫 는 `배포판점검 --라이브` 가 「라이브 = HEAD」라고 답하는 순간 **거의 확실히 낡았다**. 그런데 아무도 그 둘을 대조하지 않는다.  ⚠ 처방의 방향을 못박는다 — **`보드수거` 가 🎫 줄을 «거두게» 하는 것은 답이 아니다.** F368 이 「완료∩주인없음∩🎫없음」 세 조건을 동시에 요구한 이유가 바로 이것이고(🎫 를 자동으로 치우면 일감이 조용히 사라진다), 집을지 말지는 철학 ㉡ 이 사람 몫으로 남기라는 그 한 칸이다. 그래서 필요한 것은 **제거가 아니라 «알림»**이다: `board.js` 가 🎫 를 낼 때, 그 문구가 배포를 말하고 라이브가 HEAD 면 「⚠ 이 🎫 는 이미 나갔을 수 있다 — 버전 이력 말미부터 열어라」를 붙인다. 새는 방향이 「과잉 경고」라 안전하다(제거는 「조용한 유실」이라 반대다).  ⚠ 고칠 때의 함정 둘: ①**문구 매칭이 저장소를 못 가른다** — 🎫 가 말하는 배포가 Apps Script 인지 talk Edge Fn 인지 프로즈로는 안 갈리고, `배포판점검` 은 프로젝트별로 답이 다르다(루트 = @HEAD · crewcard 별도). 잘못 붙으면 산 일감에 「이미 나갔다」가 붙는다 → **경고 문구를 「낡았다」가 아니라 「대조하라」로 쓴다.** ②**라이브 대조는 자격증명이 필요해 클라우드 세션에선 못 잰다** — 못 재면 경고를 **안 붙이는** 쪽이 맞다(못 잰 것을 「최신」으로 접으면 F207 이 여기서 반대로 터진다). | |
