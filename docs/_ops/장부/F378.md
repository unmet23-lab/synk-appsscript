| F378 | 2026-08-12 | 마찰 | 앱 이름 가드가 별칭 재수출을 거짓양성으로 잡는다 — 실측 2026-08-12 lib/보고서교정제출.js:48 '스냅샷모양판'. tests/앱이름.test.js 가 `export { A as B }` 의 B 를 「선언 없이 참조된 이름」으로 읽는데, B 는 참조가 아니라 새 수출 이름이라 선언될 자리가 원리상 없다. 지금 저장소가 안 빨간 것은 우연이다 — 기존 재수출(lib/게임제출.js)이 전부 별칭 없는 꼴이라 그 문법을 안 밟는다. 비용은 작지만 고정이다: 새 파일에서 그 문법을 쓰는 세션마다 전 스위트 한 바퀴를 태우고 나서야 안다(내가 그랬다). 우회 = 지역 상수로 받아 재수출(그 자리에 사유 주석을 남겼다). 처방 후보 = 가드 파서가 ExportSpecifier 의 exported 이름을 참조 목록에서 뺀다 — 탐지력은 픽스처로 못박고 실저장소에는 거짓양성만 검사한다(CLAUDE.md 신뢰성 가드 세 맹점 ②). | talk tests/미정의심볼.test.js 걷기에 ExportNamedDeclaration(source면 두 이름 다 밖)·ExportSpecifier(local만 참조·exported 제외) 갈래 + 픽스처 3(거짓양성 0·declaration 탐지력·from 재수출) — talk f58e9ec · F083 변이: 갈래 뗀 격리 사본에서 신고 원문(스냅샷모양판) 거짓양성 재현 확인 · 미정의 local은 babel 파스층(ModuleExportUndefined)이 이미 거절함을 실측 |
