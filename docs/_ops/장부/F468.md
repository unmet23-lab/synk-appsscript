| F468 | 2026-08-15 | 실수 | DESIGN.md 가 read-budget BIG(16,384B)을 26B 넘겨 tests/디자인가드.test.js:173 이 주인 없는 CI 적색 — 같은 자리 2번째다(1번째 = #Q73, 수리 c93f8f1d 가 여유 873B 를 남겼는데 하루 만에 feat 40878852 의 +899B 로 재초과). 근본은 파일이 아니라 장치: 훅 처방 「파일을 문턱 아래로 되돌려라」와 회귀 「크기 < BIG」가 둘 다 사람이 바이트를 지키는 해법이라, 브랜드 결정이 날 때마다 자라는 정본과 양립할 수 없었다. 실측 덤: 문턱 아래로 되돌려도 초과 시점의 전문 읽기 처방은 일일 상한(이날 18/18 deny 실측)에 따로 걸린다 — 바이트 유지로는 F103 을 다 못 막는다. | design-guard 초과 분기 처방을 구역 읽기(limit는 CHUNK 이하 - read-budget 이 일부러 예산 밖에 둔 권장 통로)로 교체 + 회귀 축을 크기<BIG 에서 줄수<=CHUNK(실측 155줄 vs 400줄)로 교체 - 크기와 처방의 결합 자체를 절단. DESIGN.md 는 값 불변 정돈으로 16,410B->16,204B 동반 |
