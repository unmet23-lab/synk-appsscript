| F428 | 2026-08-14 | 마찰 | 「배포집합이 마지막 배포보다 앞서 있다」 🚀 경보가 **매 SessionStart 마다 거짓으로** 울고 있었다 — 드리프트가 실제로는 0인데 「커밋 203개 · 배포 파일 4개」로. 실측·수리 2026-08-14 `local_8981e74f`(#Q75).  **무엇이 틀렸나.** `tools/배포판점검.js` 의 `안나간변경()` 이 기준선(마지막 배포 커밋)을 **커밋 제목**에서 골랐다 — `git log -E --grep '^\[v'`, 즉 버전 태그가 제목 **머리**에 있을 때만. 그런데 실물 `015b9fd4` 는 「fix: 「골든셋」 → 「강사 정답 모음」 개명 — 뜻이 안 서는 낱말 **[v9.231]**」로 태그를 **꼬리**에 달았다. 그래서 기준선이 한 칸 낡은 `e61ef44`([v9.230])로 잡혔고, 그 사이에 낀 것이 정확히 `015b9fd4` 자신이라 그 커밋이 만진 배포 파일 4개(`Code.js`·`엔진_셋업확장.js`·`엔진_수집.js`·`엔진_콘텐츠AI.js`)가 영구히 「안 나갔다」로 떴다. 올바른 기준선으로 재면 `git diff --name-only 015b9fd4 HEAD -- <배포 4파일>` = **0건**이다.  **왜 굵직한가 — 이 장치가 자기 무덤을 미리 적어 뒀다.** `배포판점검.js:431`(옛 번호)이 이미 이렇게 적고 있었다: 「이 층은 **매 SessionStart** 마다 발화하므로 거짓 🔴 도 매번이고, 그러면 진짜 드리프트가 왔을 때 같은 문장이 이미 무시당하고 있다」(F113). 그리고 이 장치가 존재하는 이유는 F240 실사고 — **소급 불가 데이터가 걸린 하루짜리 손실**이다. 즉 거짓양성이 상시화되면 이 장치는 「있는데 안 도는」 것보다 나쁜 상태가 된다: 화면엔 매번 뜨는데 아무도 안 읽고, 진짜가 왔을 때 구별이 안 된다.  **뿌리 = 맹점 ① 「사람이 실제로 쓰는 표기로 검사한다」.** 채번 번호는 `bump-version.js` 가 **기계로** 쓰지만(`Code.js` 의 `const SYNK_VERSION` 줄 · 그 도구 `:37`·`:263` 「목록을 못 읽어도 Code.js 는 반드시 본다」), 제목의 `[vN]` **자리**는 사람이 정한다. 이력 전체 실측: 버전 태그를 단 커밋 **290 중 머리 255 · 그 밖 35**. 12%가 다른 자리에 단다 — 드문 일탈이 아니라 상시 갈리는 표기였고, 검사는 그중 한 모양만 봤다.  **새는 방향이 이번엔 시끄러웠다 — 그래서 오래 살았다.** 가드가 새는 흔한 방향은 조용한 「통과」인데 이번은 반대로 **상시 경보**였다. 시끄러운 고장은 「원래 저런 것」으로 읽혀서 아무도 신고하지 않는다. 실제로 이 경보는 매 세션 인계문·훅 출력에 그대로 실려 왔고, 그 자리를 지나간 세션들이 모두 그냥 읽고 넘겼다.  **처분(고쳤다).** 기준선 재료를 제목 → **내용**으로 옮겼다: `git log -G "^const SYNK_VERSION = '" -- Code.js`. 최근 `^\[v` 커밋 12/12 가 전부 그 줄을 바꿨음을 실측해 **엄격한 상위집합**임을 확인하고 갈아끼웠다(합집합·폴백 안 씀 — 경량). 🚫 **정규식을 아무 데나 매칭하도록 넓히는 것은 처방이 아니다**: 그 「그 밖 35」의 다수가 「docs: 보드 … ([v9.227] `dbfa38d2`)」처럼 버전을 **언급만** 하는 커밋이라, 그게 기준선이 되면 그 앞의 진짜 드리프트를 조용히 덮는다(그쪽이 위험한 방향). 회귀는 픽스처가 진다(맹점 ② · 실저장소는 이 수리로 깨끗해져 「안 뜬다」와 「안 돈다」가 같은 모양이다 · F207): ①꼬리 태그를 기준선으로 잡는가 ②「언급만」 커밋을 기준선으로 안 잡는가 ③대가 — 채번 상수가 `Code.js` 에 살아 있는가. **변이 2/2** — ①옛 판(머리만)으로 되돌리면 ①이 빨감, ②넓힌 판으로 바꾸면 ②가 빨감. 픽스처의 「뒤 커밋」이 `Code.js` 를 고칠 때 **채번 줄은 살려 두는** 것이 급소다(안 그러면 기준선이 뒤로 밀려 픽스처가 늘 「드리프트 0」을 내고 탐지력이 통째로 죽는데, 그 고장은 **초록의 모습**으로 온다).  ⚠ **대가(맹점 ④ — 틀릴 때의 모습).** 기준선이 `Code.js` 의 그 줄 하나에 걸렸다 — 상수를 옮기거나 파일을 개명하면 기준선이 `null` 이 되고, 호출부는 「배포 커밋이 없다」를 **말하지 않도록** 되어 있어서(`작업본소유자.js` 침묵 조건) 이 층이 **조용히 죽는다.** 그 고장은 「깨끗함」과 글자 하나 다르지 않다 — 그래서 회귀 ③으로 못박았다. 닫은 것 1개 = 그 침묵 자리.  ⚠ **이 신고는 「라이브가 저장소와 같다」는 주장이 아니다.** 이 층은 원리상 라이브를 못 읽는다(도구 자기 주석: 「라이브를 읽어서 하는 말이 아니다」). 고친 것은 **간접 증거의 기준선**이고, 라이브 실측은 `--라이브` 가 따로 진다. 옛 천장 둘(①`[vN]` 달고 clasp push 못 한 판 → 침묵 ②채번 없이 나간 배포 → 거짓양성)은 **그대로 서 있다** — 바뀐 것은 「채번을 아예 못 보던 세 번째 자리」이지 「채번 ≠ 배포」가 아니다. | tools/배포판점검.js 기준선 재료를 커밋 제목(--grep '^\[v') → 내용(git log -G "^const SYNK_VERSION = '" -- Code.js)으로 교체. bump-version 이 기계로 쓰는 줄이라 사람 표기에 안 기댄다. 회귀 tests/작업본소유자.test.js 3건(꼬리 태그 탐지 · 「언급만」 커밋 거부 · 대가=채번 상수 생존) · 변이 2/2 |
