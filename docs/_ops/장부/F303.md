| F303 | 2026-08-09 | 실수 | doc-graph 가 `.html` 을 스캔하지 않아, 파생 선언을 **이미 해 둔** 두 파일(docs/홈페이지_시안/시안.html·시안.tpl.html)이 그래프 밖에 있었다. 결과 = 철학 정본의 파생 엣지 0종 — 그런데 그 정본 머리는 그 엣지 수를 「전 문서 리라이팅 진행을 세는 유일한 자리」로 적어 두었다. 새는 방향이 「통과」도 아니고 **「0」**이라 「전파 안 됨」과 「계기판이 안 봄」이 같은 모양이었고, 철학을 편집해도 파생 알림이 아무에게도 안 갔다. 같은 결함 2번째(F243=루트 .js 가 스캔 밖이라 급여 v1.6 개정이 코드에 4일간 안 닿았다). 유호님 질문(「철학 전부 이해하나」)에 답하려 실측하다 드러났다. | tools/doc-graph.js `TEXT_EXT` 에 `html` 추가 + **계열 가드 신설**. 확장자를 하나 더 붙이는 데서 멈추지 않았다 — 같은 결함 2번째라 원인을 쓸 수 없게 만들었다: `tests/문서그래프.test.js` 의 「[회귀·계열] 파생을 선언한 docs·루트 파일은 전부 그래프 안에 있다」가 **doc-graph 의 walk 를 쓰지 않고** 직접 훑어(재려는 것이 그 walk 의 사각이라 같은 walk 로 재면 사각을 사각으로 검사하는 셈), 앞으로 `.svg`·`.json`·`.ts` 어디에 선언해도 열리지 않으면 빨개진다. 실패 메시지가 분모(20건)와 고칠 자리(TEXT_EXT·SCAN_DIRS)를 짚는다. 범위는 doc-graph 가 덮으려는 자리(`docs/**` + 루트 얕게)로 한정하고 의도적 제외는 `G.shouldSkip` 을 그대로 상속했다 — 하드코딩하면 도구와 가드의 판정이 갈린다. 탐지력 실측: 수리를 되돌리니 새 회귀 2건이 정확한 진단으로 빨개졌고 복원하니 초록(42/42). ➕곁들여 잡은 두 번째 결함 = `--add` 의 **자리**가 마크다운만 맞아(첫 `# ` 다음) `.txt`·`.html` 은 전부 파일 맨 첫 줄에 박혔다 — 유호님이 읽는 정본 첫 줄이 기계 표기가 되는 자리다(쓰기 경로의 사고는 조용하지 않고 문서에 남는다). 형식별로 갈랐다: `.txt`=꼬리 각주+무엇인지 밝히는 ※ 줄 · `.html`=doctype 뒤 · `.md`=기존 규칙 불변. 회귀 3건으로 못박음. 실물 결과: 철학 정본 파생 엣지 0 → **5종**(시안 2 + 기업마스터·기업소개서·집필 규범 — 셋 다 v1.0 반영을 grep 으로 확인한 뒤 스탬프했다. 구판 낱말은 전부 「구 마스터 … 교체 대상」 폐기 표기라 드리프트 아님), 그리고 `doc-propagation` 훅이 철학 편집 시 그 5종을 실제로 알린다(실측). |
