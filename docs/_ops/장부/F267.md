| F267 | 2026-08-09 | 실수 | 패키지 소스(src/)를 읽고 「설치본에 있다」고 판정했다 — 런타임이 읽는 것은 build/ 다. 앱 도입 커밋 b44c386 이 useAudioStream 을 「이미 설치된 expo-audio 57.0.3 에 들어 있다(src/ExpoAudio.ts:661 export)」로 근거를 적었는데, 57.0.3 은 그 re-export 한 줄이 build/ExpoAudio.js 에 빠진 채 배포됐다. 실기기에서만 죽고 테스트 550 은 전부 초록이었다(화면을 안 그린다). node_modules 안에 src/ 와 build/ 가 함께 있어 grep 이 소스를 먼저 물면 그대로 오판이 된다 — 「현재 상태는 현재를 읽어 판정한다」의 패키지 층 형태. | src/말하기화면.js 가 배럴 대신 expo-audio/build/AudioStream 에서 직접 가져오고, 그 자리 주석이 「런타임이 읽는 것은 package.json main 이 가리키는 build/ 다」를 못박는다. 판이 올라 배럴이 고쳐지면 되돌린다. |
