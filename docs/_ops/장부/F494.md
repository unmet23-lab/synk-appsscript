| F494 | 2026-08-16 | 마찰 | 종결된 결정이 ⏳ 표식을 단 채 남으면, 그게 「문장 속」이면 어떤 장치도 안 본다 — 한 파일에서 4줄 실측 (대기열 #Q99 착수 실측 · 08-16 `local_47a39ea2`)  🔴 **실측**: 메모리 `mascot-felt-liveness` 한 파일에 **이미 종결된 ⏳ 표식이 4줄** 있었다 — `:3`(프론트매터 description 「2단계 진입은 ⏳유호」 → :21 에서 승인됨) · `:88`·`:134`(「어느 벌 정본은 ⏳유호」 → :136 에서 「오케이 ㄱ 가자」로 닫힘) · `:137`(「②체리 대체·③Coral3 기계금지는 여전히 ⏳유호」 → 둘 다 닫힘). 넷 다 `tools/decision-queue.js` 가 **문장 속 지시어**로 걸러 큐에 안 갔고(설계대로다 — 그 판별이 없으면 문장 조각이 결정으로 배달된다), 폐기함(`--버려진`)에는 있었지만 **`--확인` 도장이 찍혀 조용**했다. 그래서 유일한 기록이 **대기열 줄 하나(#Q99)** 였다.  🔑 **새는 방향이 둘이고 서로 반대다**: ①진짜 미결이 문장 속이면 **배달 0**(유호님께 영원히 안 간다) ②종결분이 ⏳를 달고 있으면 **사람이 읽을 때 미결로 읽힌다** — 실제로 #Q99 는 「이게 미결인지 종결인지 원문만으로 못 가른다」로 세션 하나를 세웠다. 가른 증거는 결국 **메모리 밖**(커밋 `40878852` 의 커밋문 + `DESIGN.md:57-60`)에 있었다.  ⚠ **도구 결함이 아니다** — 폐기함 지문은 줄 번호가 아니라 **토픽+본문 sha1**(`decision-queue.js:338`)이라 본문이 바뀌면 다시 뜬다. 설계는 옳고, 어긋난 것은 **표기 규율**이다: 종결하면서 그 줄의 ⏳ 를 안 걷었다.  📏 **곁가지 실측 — 같은 통과에서 주인 없는 적색 하나를 찾아 닫았다**: `tests/결정큐.test.js:566`(실저장소 「배달 항목 본문에 ✅ 가 없다」)이 **4건으로 fail** 이었다(`equity-charter-design:47` · `fb-first-post-assets:95` · `mascot-companion-layer:32` · `policy-loan-industry-code:27` — 넷 다 한 줄에 해소와 미결을 같이 적은 것). 이 fail 하나가 `test-ci` 전체를 빨갛게 세워 **모든 세션의 `/deploy` 를 막고 있었다.** 08-16 에 그 검사의 처방대로 네 줄을 쪼갰다(질문은 한 자도 안 지웠다 · 결정큐 73/73 · test-ci 3611 중 pass 3594 · fail 0 · skip 12 · todo 5).  🔴 **내 문장이 같은 병을 한 번 더 만들었다** — 종결을 적으면서 원문을 그대로 인용했더니 그 인용의 ⏳ 가 폐기함에 **새로 4줄**을 만들었다(F255 의 「기호를 «쓰는» 자리와 «말하는» 자리는 다르다」 그대로). 마스킹은 백틱만 본다(`maskCodeSpans`) — 인용은 백틱으로 감쌌고 나머지는 낱말로 바꿔 4→0 으로 실측했다.  ▶ **안 닫는 이유**: 「종결됐는데 ⏳ 가 남았나」는 **의미 판정**이라 기계가 원리적으로 못 잰다. 값싼 두 축은 이미 서 있다(같은 줄에 ✅+⏳ = 빨강 · 문장 속 ⏳ 가 새로 생기면 폐기함에 「새것」으로 뜸). 남은 빈칸은 **「문장 속 ⏳ + 폐기함 도장이 이미 찍힌 뒤」** 한 칸뿐이고, 거기서 이 4줄이 살았다. 처방 후보 = **폐기함 도장에 만료(N일)를 붙인다** — 도장이 늙으면 다시 떠서 그때의 판정을 한 번 더 받는다(지문이 본문 기반이라 상태 파일 신설 0 · 줄 하나). 지금 안 짓는 이유는 경량 원칙 4차 과녁(「장치 개수」)이고, **같은 사고 2번째면 그 처방으로 착수한다.** | |
