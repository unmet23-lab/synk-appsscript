| F623 | 2026-08-18 | 교정 | 코드 주석에 박힌 낡은 전제를 실물과 대조 없이 **판정 근거로 승계했다** — 그리고 새 주석으로 재생산했다. 유호 교정 2026-08-18: 「폰으로 작업하는 게 없는데 왜 폰 관련 작업을 계속 하고 있어? 이거 내가 저번에 말했었는데」. 실측: `tests/장부유실.test.js` 는 5곳에서 「F123 = 폰 브랜치에만 남았던 사고(08-07)」라고 인용하는데, F123 정본(`docs/_ops/마찰신호.md` 의 `| F123 |` 행)은 memory-index-guard 의 부분 치환 문제(08-06)이고 조각 `docs/_ops/장부/F123.md` 는 없다 — 번호·날짜·내용이 다 어긋난 인용이 코드에 박혀 세션마다 읽힌다. 나는 그것을 그대로 믿고 F617 계열 수리에서 `claude/*` 를 「클라우드·폰 세션 가지 = 방치되니 유실로 경보해야 한다」로 갈라 **사면 목록에서 뺐고**, 그 전제를 주석 2곳(`tests/장부유실.test.js`)과 보드 1곳에 새로 적었다. 급소는 재료가 없었던 게 아니라 **있는 재료를 안 본 것**이다: 나는 같은 세션 초반에 `git for-each-ref refs/remotes/origin` 출력을 이미 봤고 거기 `claude/*` 7개가 전부 그날 커밋된 클라우드 세션(`synk-continue-*`·`f605-*`·`f610-*`·`vigilant-spence-*`)이며 폰 가지는 0개였다 — 즉 「현재 상태는 현재를 읽어 판정한다」의 대상을 손에 쥐고도 주석을 정본처럼 읽었다(F030·F033 계열인데 새 얼굴이다: 간접 증거가 문서·목록이 아니라 **코드 주석**이었고, 코드 안에 있다는 이유로 검증 대상에서 빠졌다). 새는 방향이 두 겹이라 나쁘다 — ①판정이 틀렸는데 회귀가 초록이라 아무도 안 잡는다(전제는 어느 회귀의 과녁도 아니다) ②주석이므로 **다음 세션이 같은 근거를 다시 읽는다**. 실제로 그 사이 다른 세션이 같은 갈래를 독립적으로 만나 `0a9414a3`(F617 델타)에서 「이름으로는 못 가른다, 세기로 가른다」로 옳게 풀었다 — 같은 자리에서 한 세션은 주석을 믿고 틀렸고 한 세션은 실물을 보고 맞았다. 처방 후보 ①**낡은 전제의 과녁을 만든다** — 유호님이 「안 쓴다」고 확정한 동선(폰 등)을 한 곳에 적고, 그 낱말이 **판정 근거**로 쓰인 자리를 회귀가 센다(설명·역사 서술은 통과 · 래칫이라 내려가면 통과) ②F0NN 인용을 기계가 대조한다 — 코드 주석의 `F\d{3}` 이 장부의 그 번호와 **날짜·주제가 맞는지** 훑는다(지금 이 오인용이 그 탐지력 픽스처가 된다) ③사람 쪽 규칙 — 남의 주석에서 「실측」·「사고」를 근거로 가져올 때는 그 원문을 열고, 못 열면 근거로 쓰지 않는다. ⚠ 이번 트랙에서 실제로 한 것은 주석 2곳·보드 1곳 회수뿐이다(유호 픽: 표현만 · 로직 무변경). 저장소 전반의 폰 배선(`tools/폰작업반입.js` + 참조 7곳 + `작업본소유자` 의 「대기 중인 폰 브랜치」 알림 + 위 F123 오인용)은 **조사해 제안만** 올린다(유호 픽). | |
