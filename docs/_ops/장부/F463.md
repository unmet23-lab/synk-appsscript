| F463 | 2026-08-15 | 실수 | 분모를 재는 스캐너가 «사람이 실제로 쓰는 표기»를 안 봐서 7을 5로 셌다 — 그것도 「잘못 재는 것」을 막는 장치(#Q85 시트 흉내 계약)를 짓는 중에 났다. 실측 2026-08-15: 시험 파일들의 쓰기 흉내를 세려고 `setValues\s*[:(]\s*(\(|function)` 로 «정의부»를 찾았는데, 이 저장소가 실제로 쓰는 표기는 메서드 축약형(`setValues(v) {`)이라 그게 통째로 사각이었다. 그래서 「쓰기 흉내 5파일」이 나왔고 — 거기엔 기준판인 `발화퀄리티.test.js` 자신이 빠져 있었다(그 파일의 흉내가 정확히 축약형이다). 이 숫자를 그대로 믿었으면 「흉내를 다 모았다」가 분모 2개를 빼먹은 채 초록으로 남았을 자리다. 고친 판정 규칙 = 정의부는 «앞에 점이 없는» 이름이다(호출은 `.setValues(`) → 7파일, 그중 되읽는 것 5. 🔑 이 실수의 계급 = 지침 v9.2 「가드·회귀의 네 맹점 ①(사람이 실제로 쓰는 표기로 검사한다)」 — 새 규칙이 아니라 **이미 적힌 조항을 내가 안 지킨 것**이고, 새는 방향은 여기서도 「통과」였다(적게 세면 목록이 짧아져 다 처리한 것처럼 보인다). | `tests/시트흉내계약.test.js` — 정의부 판정을 «표기 사전 3종»으로 넓히고(축약형 `setValues(v) {` · 콜론형 `setValues: (v) =>` · **할당형** `rng.setValues = (v) =>`), 그 탐지력을 픽스처로 못박았다. 🔑 **급소는 F463 이 「고쳤다」던 판정 규칙에 세 번째 표기가 통째로 빠져 있었다는 것이다** — 그때 고친 규칙(「정의부는 앞에 점이 없는 이름」)은 ①②만 가르고 ③은 원리상 못 본다(정의인데 앞에 점이 있다). 전수 실측 2026-08-15(tests/*.js + tests/lib/*.js 180개): 현행 3 → 할당형까지 4, 그 사각에 걸린 파일이 하필 **검사기 자신**이었다(변이 픽스처가 정확히 할당형으로 흉내를 뒤튼다 — 가드는 자기 전처리에도 눈이 먼다 · 맹점 ④). 분모도 같이 넓혔다: 옛 판은 `fs.readdirSync(TESTS)` + `.test.js` 라 **`tests/lib/` 이 통째로 밖**이었고 공용 통로 `시트흉내.js` 가 거기 산다. 이제 하위 폴더까지 재귀로 훑고, 면제는 2→4(넷 다 사유 명시 · 새로 든 둘 = 공용 통로 자신 · 검사기 자신). 회귀 6/6 · **변이 8/8**(구멍 0 · 미적용 0) — 표기 셋을 하나씩 죽이고, 호출/정의 가름을 지우고, 재귀와 `&&` 를 뒤집고, 면제 두 줄을 빼는 변이가 **전부** 빨개진다. ⚠ 남는 한계를 코드 머리말에 적었다 — **사전이지 파서가 아니다**: `Object.assign(rng, { setValues })`·구조분해·계산된 키는 여전히 못 본다. 넷째 표기가 실제로 쓰이는 날 고칠 자리는 그 사전 하나이고, 픽스처가 「무엇을 잡는가」를 셋으로 못박아 뒀으니 그 사실이 바로 드러난다. ⚠ 새 파일·새 훅 0 — 기존 검사기의 사각만 닫았다(장치 개수 불변 · 경량 원칙 4차 과녁). |
