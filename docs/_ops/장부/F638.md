| F638 | 2026-08-18 | 마찰 | 엔진 작명 규약이 **지도 문서 안에서만** 살아 있었고 지면·파일명엔 3일 동안 안 닿았다. `docs/엔진/SYNK_엔진_지도.html` v2.1(08-15)이 이미 「대외 표기는 `SYNK Trail` 꼴 회사명 결합」이라 규약을 적어 두었는데, 08-18 실측으로 소개서 6벌 중 회사명이 붙은 것은 **2벌**(Core·Vellum)뿐이었고 파일 이름은 **6 중 1벌**(`SYNK_Core_소개서.html`)뿐이었다 — 나머지는 `Loom_소개서.html`·`Trail_소개서.html` 꼴 맨몸. 유호님이 「우리 엔진 폴더에 모든 엔진 이름 앞에 synk 붙이자」고 **직접 말해서야** 닿았다. 🔴급소는 「규약이 틀렸다」가 아니라 **그 규약을 지면에 물리는 기계가 0이었다**는 것이다: `브랜드렌더린트`는 렌더 대비만 보고 이름 표기는 안 보며, 「대외 표기는」이라는 문구 자체가 내부 지면을 분모에서 빼 주는 얼굴을 하고 있었다(그래서 규약을 읽고도 안 고치는 게 정합해 보인다). 같은 계보 = #Q58 브랜드 «정합·전파»(확정이 접점에 안 닿은 자리) · 메모리 `brand-propagation-gap`. | docs/엔진 갈래 전량을 회사명 결합으로 옮겼다(원고 6·산출 6·지도 1 · 파일명 5벌 git mv) + 지도 v2.4 「작명 규약」 절에 접두를 다는 것/**안 다는 것**(층 이름 Amber·Patina · 얼굴 몽글 · 낱말 자체를 다루는 자리)을 명문화 + 회귀 tests/엔진이름.test.js 5건(이름 칸 title·h1·꼭지 + 파일명 · 픽스처가 탐지력을 지고 실저장소는 거짓양성만 · 변이 1/1 적색 확인). ⚠남는 층 = 엔진 폴더 **밖**(docs/Loom_엔진_설계.md·DESIGN.md·발표물)은 유호님 지시 범위 밖이라 안 건드렸다 — 제안으로 올렸다. |
