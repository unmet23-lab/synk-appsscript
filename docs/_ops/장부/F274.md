| F274 | 2026-08-09 | 마찰 | 배포판 대조가 함수 본체(index.ts)를 한 번도 안 봤다 — tools/배포대조.js 가 동봉묶기() 만 불러 동봉 lib 만 재고, 정작 가장 크고 가장 자주 바뀌는 본체는 비교 대상 밖이었다. 실측 2026-08-09: corrections 리허설 판 v5(08-07)에 몽골어 해설(1420b93)이 안 담겼는데 배포대조는 「같다(파일 1)」로 통과시켰다 — 배포는 2개를 보낸다. 그 판정이 왕복시험 5종의 발화점 게이트라 왕복 초록이 옛 판을 잰 것이 된다. F273 의 대조판: 조립이 반쪽이면 대조도 반쪽이고, 새는 방향은 언제나 통과다. | |
