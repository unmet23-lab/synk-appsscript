| F434 | 2026-08-14 | 실수 | ffmpeg 합성이 **글자층을 하나도 안 얹은 채 exit 0 으로 «성공»했다** — 그리고 그 산출물은 정상 mp4다(재생되고, 길이·해상도·마스코트 전부 맞다). 원인은 PNG 오버레이 입력에 `-loop 1` 이 없어 그 입력이 t=0 의 «단 한 프레임»이 된 것이다: `fade=in:st=7.2` 는 그 프레임의 타임스탬프(0)에서 아직 발화 전이라 알파 0 이고, overlay 의 eof_action=repeat 가 그 투명 프레임을 끝까지 반복한다. 즉 필터는 문법적으로 옳고 ffmpeg 는 경고 한 줄 없이 통과시킨다. 급소는 **실패가 실패의 모습을 안 한다**는 것이다 — 파일 크기(3.87MB)도, 재생 시간(10.0s)도, 프레임 수도 정상판과 구분이 안 됐다. 잡은 것은 오직 검증 단계였다(「완성 카드에서 Coral #FF6B5C 를 실제로 세어 본다」 → 0px). 검증을 「크롭했으니 지워졌겠지」를 안 믿으려고 짜 뒀는데, 정작 잡은 건 워터마크가 아니라 **글자층 전멸**이었다. 일반화: 오버레이·합성 파이프라인의 산출물은 「돌았다」가 아니라 **「얹으려던 것이 픽셀에 있나」로 잰다** — 얹힌 것의 색·화소 수를 산출물에서 직접 세고, 0 이면 fail. 덧붙여 같은 통로의 둘째 함정: `-loop 1` 만 걸고 입력 길이(`-t`)를 안 막으면 overlay 의 enable= 이 꺼진 구간에서 그 무한 스트림이 소비되지 않고 버퍼에 쌓여 **갇힌다**(실측: 메모리 1GB, 7분간 출력 2.62MB 에서 진행 0). 출력쪽 `-t` 는 인코딩 길이만 자를 뿐 입력 버퍼링을 안 막는다 — `-loop 1` 에는 반드시 «입력쪽» `-t` 를 짝지어 건다. | |
