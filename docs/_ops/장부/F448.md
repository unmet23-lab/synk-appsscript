| F448 | 2026-08-14 | 마찰 | `node --check` 가 ESM 파일의 구문 오류를 조용히 통과시킨다 — 구문검사가 초록인데 눈이 멀었다  실측(2026-08-14 · talk `src/말하기화면.js` 편집 중): JSX 가 들어 있는 화면 파일에 `node --check` 를 돌렸더니 **exit 0**. 같은 JSX 한 줄짜리 파일은 exit 1 로 잡는다. 가르는 것은 JSX 가 아니라 **모듈 판정**이었다 — 파일에 `import` 가 있으면 Node 가 ESM 으로 판정하고, 그 갈래에서 구문 오류가 보고되지 않는다(스크래치패드 대조: `import x from 'y'; const a = <div />;` → exit 0 / `const a = <div />;` → exit 1).  ■ 왜 마찰인가 — CLAUDE.md 신뢰성 맹점 ④ 「맞는 얼굴로 틀린 값을 내는 자리」의 정확한 사례다.   세션이 코드 파일을 고친 뒤 손에 잡는 첫 검사가 `node --check` 인데, talk 의 `src/*.js` 는 전부   `import` 를 쓰는 ESM 이라 **그 파일들에 대해서는 검사가 통째로 무의미**하다. 새는 방향은 언제나   「통과」고, 깨진 화면은 번들러를 돌리는 자리까지 가야 드러난다.  ■ F307 과 다른 기전이다(중복 신고 아님) — 그건 `<!--` 가 sloppy 스크립트의 Annex B 한 줄 주석이라   통과한 것이고, 이건 ESM 판정 갈래에서 아예 보고가 안 되는 것이다. 증상만 같다.  ■ 지금 사정거리: talk `tools/precommit.js:126` 은 «repo 도구에 --check 플래그»를 주는 것이라   이 결함과 무관하다. 즉 지금 이 구멍에 기대고 있는 파이프라인은 0이고, **사람(세션)이 손으로   돌릴 때만** 샌다. 이번엔 내가 그 초록을 한 번 믿었고, @babel/parser 로 다시 재서 알았다.  ■ 처방 후보(이번 세션에서 안 굳혔다 — 실사용 실측 없이 장치를 늘리지 않는다는 경량 원칙):   JSX·ESM 파일의 구문검사는 `node -e "require('@babel/parser').parse(…, {sourceType:'module',   plugins:['jsx']})"` 로 잰다. 굳힌다면 정본 1개(도구) + 회귀 픽스처(깨진 JSX 파일이 빨간지)이고,   「node --check 로 화면을 쟀다」를 금지하는 검사가 짝이다. | SYNK-talk `tests/구문검사.test.js` (`5895526`) — 다만 **신고가 진단한 구멍은 이미 닫혀 있었고, 실제로 빈 것은 「목록」이었다.**  ■ 먼저 신고를 실측으로 넓혔다(node v24.18.0): 눈머는 조건은 **JSX 가 아니라 `import`·`export` 다.**   `import x from 'y'; const a = (((;` → exit 0 · `export const a = 1; function f( {` → exit 0.   둘 다 출력 0줄. 즉 모듈로 판정된 파일은 **모든** 구문 오류가 안 보인다(신고문은 `import` 만 적었다).  ■ 그리고 신고의 처방(「@babel/parser 로 재는 정본 도구 + 회귀를 새로 짓는다」)은 **중복이었다.**   실측 2026-08-15(분모 전량 316/316 · 파일마다 임시 사본에 구문 쓰레기를 붙여 `node --check` 를 돌려   「깨뜨렸는데도 통과하는가」로 눈멂을 **추정 없이 만들었다**):     · 눈먼 파일 56 · 그 56 을 파서로 여는 회귀 **56** · 아무도 안 여는 것 **0**   `화면구문`(src + Edge Fn) · `미정의심볼`(App·index·src·lib·contents) · `화면렌더`(fixtures) 셋이   겹쳐서 이미 전부 덮고 있었다. as 쪽은 애초에 사정거리 밖이다 — 눈먼 10건이 전부 배포 밖 벤더   파일(`.claude/skills/last30days/.../bird-search/*`)이고, `clasp-guard` 가 `--check` 하는 Apps Script   디렉터리에는 `import`·`export` 를 쓰는 파일이 0이다.  ■ 그래서 지은 것은 «검사»가 아니라 «목록»이다 — 그게 진짜로 비어 있던 자리다.   위 셋은 대상을 **손으로 적은 층 목록**으로 고르고, 그 목록은 **이미 한 번 샜다**: `화면구문` 이   `src/` 만 보던 사이 `supabase/functions/<이름>/index.ts` 가 통째로 밖에 있었고, 회귀 370/370 이   초록인 채로 함수가 번들조차 안 됐다(그 파일 머리말이 적은 사고). 층이 하나 늘 때마다 사람이 세 곳을   맞춰야 하고 안 맞추면 **초록인 채로** 샌다 — CLAUDE.md 가드 맹점 ③(필터가 가드보다 좁다) 그 모양이다.   새 회귀는 대상을 `git ls-files` 추적 소스 **전량**에서 파생한다. 새 파일·새 폴더·새 진입점이   생겨도 다음 실행에 저절로 들어온다. 손 목록 0.  ■ 짓는 동안 내 계기가 두 번 틀렸고 둘 다 **분모를 찍어서** 잡았다(F207 이 실제로 일했다).   ① `git ls-files` 기본 출력이 비ASCII 경로를 `"docs/\354..."` 로 이스케이프해 내놓는다 — 그대로 읽으면      조용히 건너뛴다. 첫 판이 **316 중 25**만 재고 「샌 것 없음」을 낼 뻔했다. `-z` 로 고쳤고 그 이유를      `대상들()` 주석에 박았다(재는 층이 값을 깨뜨리는 자리 · CLAUDE.md 원인 판정 ②).   ② 파서 옵션에 `allowReturnOutsideFunction` 이 없어 **최상위 `return`** 을 쓰는 CJS 6벌이 「구문 오류」로      잡혔다(talk `tools/가드계수.js` · as 훅 5). Node 는 모듈을 함수로 감싸니 합법이고 `node --check` 도      통과시킨다 — 내 자가 좁았던 것이지 파일 결함이 아니다. 고친 뒤 두 저장소 전량 거짓양성 **0**.  ■ 검증: 회귀 2/2 · 전량 **2216/2216 fail 0** · 변이 양방향 실측 — 추적 파일에 깨진 ESM 을 심으니   `node --check` 는 **exit 0**(눈멂 재현)인데 새 회귀는 `fail 1` 로 파일·위치까지 지목했고   (`tests/_변이_임시.js — Unexpected token (2:20)`), 치운 뒤 `pass 2 · 317/317` 로 돌아왔다.  ■ 대가를 함께 적었다(맹점 ④): 시간 **0.30s**(전량 파싱) · 틀릴 때의 모습 = **거짓양성**(위 ②가 그 실례) ·   닫을 것 = **지금 없다**(「화면구문」은 분모 래칫 「화면 ≥4 · 함수 ≥3」을 따로 져서 여기 안 녹는다 —   그 래칫까지 파생으로 옮기는 날 닫는다).  ■ `node --check` 가 눈멀었다는 것은 **단언하지 않았다** — 버그가 아직 있을 것을 요구하는 회귀는 금지다   (맹점 ②). Node 가 고치면 이 파일은 그대로 초록이면 된다. 못박은 것은 우리 파서가 실제로 판다는   사실뿐이고 그건 픽스처 6단언(깨진 것 3 = 빨강 · 멀쩡한 것 3 = 초록)이 진다. |
