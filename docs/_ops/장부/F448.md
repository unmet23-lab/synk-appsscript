| F448 | 2026-08-14 | 마찰 | `node --check` 가 ESM 파일의 구문 오류를 조용히 통과시킨다 — 구문검사가 초록인데 눈이 멀었다  실측(2026-08-14 · talk `src/말하기화면.js` 편집 중): JSX 가 들어 있는 화면 파일에 `node --check` 를 돌렸더니 **exit 0**. 같은 JSX 한 줄짜리 파일은 exit 1 로 잡는다. 가르는 것은 JSX 가 아니라 **모듈 판정**이었다 — 파일에 `import` 가 있으면 Node 가 ESM 으로 판정하고, 그 갈래에서 구문 오류가 보고되지 않는다(스크래치패드 대조: `import x from 'y'; const a = <div />;` → exit 0 / `const a = <div />;` → exit 1).  ■ 왜 마찰인가 — CLAUDE.md 신뢰성 맹점 ④ 「맞는 얼굴로 틀린 값을 내는 자리」의 정확한 사례다.   세션이 코드 파일을 고친 뒤 손에 잡는 첫 검사가 `node --check` 인데, talk 의 `src/*.js` 는 전부   `import` 를 쓰는 ESM 이라 **그 파일들에 대해서는 검사가 통째로 무의미**하다. 새는 방향은 언제나   「통과」고, 깨진 화면은 번들러를 돌리는 자리까지 가야 드러난다.  ■ F307 과 다른 기전이다(중복 신고 아님) — 그건 `<!--` 가 sloppy 스크립트의 Annex B 한 줄 주석이라   통과한 것이고, 이건 ESM 판정 갈래에서 아예 보고가 안 되는 것이다. 증상만 같다.  ■ 지금 사정거리: talk `tools/precommit.js:126` 은 «repo 도구에 --check 플래그»를 주는 것이라   이 결함과 무관하다. 즉 지금 이 구멍에 기대고 있는 파이프라인은 0이고, **사람(세션)이 손으로   돌릴 때만** 샌다. 이번엔 내가 그 초록을 한 번 믿었고, @babel/parser 로 다시 재서 알았다.  ■ 처방 후보(이번 세션에서 안 굳혔다 — 실사용 실측 없이 장치를 늘리지 않는다는 경량 원칙):   JSX·ESM 파일의 구문검사는 `node -e "require('@babel/parser').parse(…, {sourceType:'module',   plugins:['jsx']})"` 로 잰다. 굳힌다면 정본 1개(도구) + 회귀 픽스처(깨진 JSX 파일이 빨간지)이고,   「node --check 로 화면을 쟀다」를 금지하는 검사가 짝이다. | |
