| F470 | 2026-08-15 | 실수 | F468 수리(0fc684fd)를 회귀 없이 커밋하고, 커밋 메시지엔 「처방의 정직성은 기존 회귀가 그대로 지킨다」고 단정했다 — 변이로 물려 보니 거짓이었다. 초과 분기 처방을 옛 문구(「문턱 아래로 되돌려라」)로 되돌리는 변이가 CI 를 그대로 통과했다(기존 검사는 「넘었다」만 봤고 옛 처방에도 그 낱말이 있다). 즉 내가 **바꾼 바로 그 축**(처방 내용)에만 회귀가 0이었고, 갈아끼운 다른 축(크기→줄수)은 멀쩡했다. 축을 교체할 때 옛 축의 회귀는 지웠는데 새 축의 회귀는 안 세운 형태다. 덤 2: ①변이 6벌 중 1벌이 치환 정규식 불일치로 조용히 미적용됐고 그 초록을 「구멍 없음」으로 읽을 뻔했다(전/후 바이트 비교를 넣고서야 갈렸다) ②후속 커밋 메시지에 test-ci 숫자를 재기 «전»에 적었다 — 총계는 우연히 맞았으나 분모 구성이 달랐다(워크트리 통과 3343·skip 36 / master 통과 3367·skip 12). | tests/디자인가드.test.js 에 「문턱을 넘으면 따라갈 수 있는 구역 읽기를 처방한다」 신설 (bbf4af33) — limit<=CHUNK 를 실제로 시키는지 + 옛 문구로 안 되돌아갔는지. 변이 재검 5/6 물림(1은 무해 판정). 변이 스크립트엔 전/후 바이트 비교를 넣어 조용한 미적용을 분모에서 가른다 |
