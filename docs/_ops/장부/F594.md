| F594 | 2026-08-18 | 마찰 | 플래그 게이트의 «등록층 회귀»가 자기 분모 밖을 원리상 못 본다 — talk 도구 16개가 그 사각에 산다 (#Q111 착수 실측)  talk `tests/플래그게이트.test.js` 는 「모르는 `--` 낱말을 조용히 무시하지 않는가」를 **등록층까지** 못박는 회귀다(F400·F435·F436 계열의 처방). 그런데 그 검사가 스캔하는 분모가 `자격증명도구들()` = 소스에 `자격증명.읽기(` 가 있는 도구뿐이다.  🔴 실측(2026-08-18 · talk `tools/`): `process.argv` 를 읽는 도구 **30개** 중    · 그 분모 «안» 13개 — 게이트 없는 것 **0**  (여긴 완벽하다)    · 그 분모 «밖» 17개 — 게이트 없는 것 **16**    `배포빚.js` 는 네트워크 0 · 자격증명 0 이라 그 16 중 하나였고, 그래서 #Q110·F592 의    거짓 안내(「운영은 `--운영`」)가 몇 달을 살아남았다 — 쳐 봐도 exit 0 이었기 때문이다.  🔑 이건 「가드가 안 돈다」가 아니라 **가드가 자기 등록층에 눈이 먼」** 자리다(CLAUDE.md 맹점 ④ ·    맞는 얼굴로 틀린 값). 검사는 늘 초록이었고, 초록의 뜻이 「13개 중 13개」였는데 읽는 쪽은    「이 저장소의 도구 전부」로 읽었다. 분모를 안 밝히는 초록의 전형이다(F207).  ⚠ 그래서 분모를 그냥 넓히면 안 된다 — **착수 때 실측으로 두 번 걸렸다**:    ㉠ ④ 검사(선언이 소스의 `--낱말` 전량을 덮는가)는 **자식 프로세스 인자와 도구 자신의 플래그를       못 가른다.** `배포빚.js` 만 해도 `--is-ancestor`·`--name-only` 는 git 에 넘기는 인자인데       ④가 그것들을 「선언에 없다」고 잡았다(실측). 시키는 대로 선언하면 그 낱말들이 CLI 에서       **통과**한다 — 처방을 따르면 병이 생기는 모양이다(F103 의 역방향).       사각 16건 중 같은 꼴이 여럿이다: `동봉신호`(`--no-commit-id`·`--root`)·`앱밟기`(`--version`)·       `마이그레이션_합본`·`BGM빌드`(`--check`).    ㉡ 스캐너가 **코드와 산문을 못 가른다.** 내가 주석에 `자격증명.읽기()` 를 글자로 쓰자       `배포빚.js` 가 자격증명 도구로 «승격»돼 ④가 위 ㉠을 요구했다(문구를 바꿔 풀었다).       즉 지금 분모는 정규식 하나에 매달려 있고, 그 정규식은 주석에도 걸린다.  ▶ 그래서 #Q111 에서는 **이 한 도구만** 닫았고(게이트 + 회귀 10 + 변이 7/7), 분모 자체는    대기열 #Q112 로 세웠다 — 넓히는 일이 「목록 손보기」가 아니라 **추출기 설계**이기 때문이다. | talk `tests/lib/CLI인자.js` 신설 — 분모와 추출기를 한 통로에서 판정한다(ca23b66·26875cb · 대기열 P3 #Q112). ①분모를 「소스에 `자격증명.읽기(` 가 있나」에서 **「코드로 `process.argv` 를 읽나」**로 갈았다 — 29개(옛 13). 자격증명 여부는 칸으로 내주고 쓰는 쪽이 거른다(두 분모를 따로 만들면 갈라진다). ②㉠ 해소: 자기 플래그를 「소스에 있나」가 아니라 **「argv 파생과 비교되나」**로 센다 — 자식 프로세스 인자(`--name-only`·`--root`·`--version`·`--적용 --판 --확정자`)가 선언 요구에서 빠졌다. 옛 ④는 그것들의 선언을 요구했고 시키는 대로 하면 CLI 에서 통과했다(병 만드는 처방 · F103). argv 세 모양(`const args =` · `main(argv)` 매개변수 · 직접)과 도우미 꼴(`인자값(argv, '--조각')`)을 따라간다 — 매개변수 꼴을 못 봐서 `eval-score`·`독립채점` 이 「자기 플래그 0」이던 거짓 안심도 같이 풀렸다. ③㉡ 해소: 모든 판정이 `tests/lib/소스검사.js` 의 `코드만` 을 지난다(렉서 사본 0 · F401). 실증 — `앱시작.js` 는 `process.argv` 를 **주석에서만** 언급하는데 옛 판 분모에 들어왔다. ④**잔여 14개는 장부가 아니라 기계가 진다**: 양방향 래칫 명단(없는 새 도구가 게이트 없이 들어오면 적색 · 게이트를 달고 줄을 안 지워도 적색)+갈래 라벨 검사+🎯 조준축 라벨 검사. 그래서 「닫으면 15개가 사라진다」던 보류 사유가 없어졌다 — 부착은 대기열 #Q112 갈래로 남고, 그 자리를 이제 CI 가 매번 센다. ⑤덤으로 실물 하나: `강사지식빌드.js` 가 조준 축 0(자격증명·env·판·네트워크 전무)인데 `--운영` 을 아는 낱말로 받고 있었다(F592 그 모양) — 빼고 새 검사 「③-3 반대 방향」이 그 결정을 지킨다. 회귀 17(신설 8) · 변이 15/15 구멍0 미적용0 · talk 전체 2364/2364 fail0. |
