| F253 | 2026-08-08 | 마찰 | 보드 선점 탐지가 표 칸을 날 split 으로 가른다 — F119 가 통로를 하나로 만든 뒤에도 이 자리만 옛 통로에 남았다. 실측 2026-08-08: tools/작업본소유자.js:363 의 보드선점() 이 diff 의 `+|` 줄을 `줄.slice(1).split('|')` 로 갈라 칸[2]=트랙 · 칸[4]=상태 를 읽는다. 그런데 보드 줄에는 백틱 인용이 빽빽하고(`local_xxx` · `2>&1|docs/…` · `git commit -a`), 백틱 **안**의 파이프가 하나만 있어도 그 뒤 칸이 통째로 밀린다 — 트랙 칸 자리에 상태 조각이 오고 상태 칸에 딴 칸이 온다. 새는 방향은 오류가 아니라 **조용한 오독**이라, F119 가 같은 원인으로 동시에 고친 네 곳(friction·session-report·track-collision·board-guard)과 모양이 완전히 같다. 이 파일만 그때 목록에 없었다. 곁들여 둘째 축: 옛 통로 금지 회귀(tests/표칸.test.js)의 대상이 **손으로 적은 목록 4개**라 표를 읽기 시작한 파일이 늘어도 조용히 빠진다 — 이번에 board-move.js 를 그 목록에 손으로 넣으면서 드러났다(F246 트랙). 처방 후보 ①tools/lib/표.js 의 칸나누기 로 바꾼다(양끝 파이프를 그 함수가 떼므로 slice(1) 을 빼고 칸 인덱스를 하나 당긴다) ②표칸 회귀의 대상을 손 목록이 아니라 「보드·장부 표 줄을 읽는 파일」에서 파생시킨다 ③최소한 tools/작업본소유자.js 를 그 목록에 넣어 적색으로 드러낸다(단 그 순간 남에게 전가된 적색이 되므로 ① 과 같은 커밋이어야 한다). 신고 시점에 이 파일은 **남의 살아있는 작업본**이라 F073 으로 손댈 수 없었다. | |
