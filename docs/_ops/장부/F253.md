| F253 | 2026-08-08 | 마찰 | 보드 선점 탐지가 표 칸을 날 split 으로 가른다 — F119 가 통로를 하나로 만든 뒤에도 이 자리만 옛 통로에 남았다. 실측 2026-08-08: tools/작업본소유자.js:363 의 보드선점() 이 diff 의 `+|` 줄을 `줄.slice(1).split('|')` 로 갈라 칸[2]=트랙 · 칸[4]=상태 를 읽는다. 그런데 보드 줄에는 백틱 인용이 빽빽하고(`local_xxx` · `2>&1|docs/…` · `git commit -a`), 백틱 **안**의 파이프가 하나만 있어도 그 뒤 칸이 통째로 밀린다 — 트랙 칸 자리에 상태 조각이 오고 상태 칸에 딴 칸이 온다. 새는 방향은 오류가 아니라 **조용한 오독**이라, F119 가 같은 원인으로 동시에 고친 네 곳(friction·session-report·track-collision·board-guard)과 모양이 완전히 같다. 이 파일만 그때 목록에 없었다. 곁들여 둘째 축: 옛 통로 금지 회귀(tests/표칸.test.js)의 대상이 **손으로 적은 목록 4개**라 표를 읽기 시작한 파일이 늘어도 조용히 빠진다 — 이번에 board-move.js 를 그 목록에 손으로 넣으면서 드러났다(F246 트랙). 처방 후보 ①tools/lib/표.js 의 칸나누기 로 바꾼다(양끝 파이프를 그 함수가 떼므로 slice(1) 을 빼고 칸 인덱스를 하나 당긴다) ②표칸 회귀의 대상을 손 목록이 아니라 「보드·장부 표 줄을 읽는 파일」에서 파생시킨다 ③최소한 tools/작업본소유자.js 를 그 목록에 넣어 적색으로 드러낸다(단 그 순간 남에게 전가된 적색이 되므로 ① 과 같은 커밋이어야 한다). 신고 시점에 이 파일은 **남의 살아있는 작업본**이라 F073 으로 손댈 수 없었다. | 815da28 — 축① 은 고쳤지만 그 자리에서 더 큰 것이 나왔다: 보드선점()이 F250 이주 뒤로 스텁 docs/세션보드.md 를 diff 해 원리상 상시 0건이었다(날 split 은 죽은 코드 안이라 증상조차 안 났다). 재는 대상을 docs/_ops/보드/ 폴더로 옮기고 미추적도 읽는다 — 세션당 파일 하나라 첫 선언이 곧 새 파일인데 git diff HEAD 는 미추적을 원리상 안 담고, 선점 경고가 가장 필요한 창이 정확히 거기다. 주인은 파일 이름에서 온다(F246 ①과 같은 재료) — 그래서 내 파일은 뺀다. 지문 규칙은 board-id 에서만 파생시킨다(손으로 다시 적었다가 local_ 접두를 안 떼서 내 줄이 그대로 떴다). 축②(손 목록)는 tools·hooks 의 모든 .js 를 훑는 것으로 갈았고, 훑은 개수부터 단언한다 — 경로가 어긋나면 0개를 훑고도 초록이던 자리다. 그 파생이 즉시 값을 했다: 변이③(날 split 복귀)을 행동 회귀와 이 금지가 동시에 잡았고, 같은 폴더의 날 split 3곳(toil·시스템대장·인쇄본빌드)도 함께 통로로 옮겼다. 라이브 실증: 고치기 전 0건 → 고친 뒤 옆 세션 6c04d8e5 가 방금 선언한 미추적 트랙 1건. 회귀 6 신설·74/74·표칸 8/8·시스템대장+인쇄본빌드 25/25·skip 0·변이 6/6. 🔴 변이⑤가 1차에 안 잡혔다 — 주인 표시 단언이 위쪽 미커밋 파일 목록에 뜬 같은 이름을 읽고 있었다(초록인데 아무것도 안 잰 단언). 옛 픽스처도 같은 병이라 스텁 좌표를 재며 도구가 죽은 내내 초록이었다 — 픽스처가 아무도 안 쓰는 자리를 재면 초록은 탐지력이 아니라 좌표 오류를 증명한다. |
