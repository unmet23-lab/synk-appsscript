| F582 | 2026-08-17 | 마찰 | `board-guard` 의 **파일 겹침 검사에 생사 게이트가 없다** — 죽은 세션의 «활성» 줄이 그 줄에 적힌 파일을 영구히 예약하고, 그 줄이 ⏳유호 결정을 들고 있으면 처방이 **하나도 안 돈다**(F103 그 모양). 실측으로 F572 수리의 보드 선언이 막혔다.  실측 2026-08-17 (`local_eba8defb` · 유호 지시 「F572 도 지금 고쳐줘」 수행 중): · `docs/_ops/보드/eba8defb.md` 에 F572 줄을 선언하려니 deny —   「이 자리는 **남이 이미 선언했다** — 보드의 활성 줄이다 · tools/board-move.js ← 「#Q93 죽은 보드    줄이 자리를 영구히 잠근다 — ㉠ 3차 처분 + ㉡ 판정재료 수집기…」」 · 그 줄(`86d6d162.md`)을 `tools/lib/보드.js` 로 직접 재니:   **활성행=true · 완료행=false · 이어받기(🎫)=false · 주인이 사나=false** · 즉 **주인은 죽었고 줄은 「활성」**이다. 그 조합이 이 검사에서 산 세션과 **같은 모양**이다.  ■ 처방 셋이 전부 막힌다 (거절문이 낸 그대로 따라가 봤다) ㉠ 「죽은 세션의 줄이면 그 줄부터 치운다: `node tools/board-move.js "그 트랙 문구"`」    → 그 줄은 **완료가 아니다**(활성). board-move 원칙⑦(미완 이관)이 거절한다. 탈출구      `--미완확인 "사유"` 는 있지만, 그 줄의 상태 칸은 「▶남은 과녁=상한 분모 불일치 ⏳유호      ㉮권고/㉯/㉰」다 — **유호님 미결 결정을 들고 있는 줄**이라 아카이브로 내보내면 그 결정이      화면에서 사라진다(조용한 누수 · 이 저장소가 🎫 를 자동 수거하지 않는 바로 그 이유). ㉡ 「죽은 세션의 트랙을 그대로 이어받는 것이면 트랙 칸을 **바이트 그대로** 옮겨 적어라」    → F572 는 #Q93 의 트랙이 **아니다**(#Q93 = 죽은 줄 처분·판정재료 / F572 = F433 처방이 튕김).      베껴 적으면 보드가 내 작업을 남의 트랙으로 **거짓 표기**한다. ㉢ 「위 자리를 안 만진다는 뜻이면 부정 표기를 붙여라」    → 나는 **실제로 그 파일을 만진다.** 붙이면 거짓이다. ㉣ `board-guard` 에는 BYPASS 가 없다(`grep BYPASS` = 주석의 «개념» 언급 3곳뿐 · env 손잡이 0). ⇒ 남는 문은 **선언 포기** 하나다. 그런데 가드 자신이 338~341행에 적어뒀다: 「선언 포기는   track-collision 의 첫 처방(보드에서 상대 줄을 읽는다)을 상대편에서 **원리상 불가능**하게 만든다」.  ■ 왜 이게 구조적인가 — 잠근 줄이 «그 문제 자체»다 막고 있는 #Q93 의 제목이 「**죽은 보드 줄이 자리를 영구히 잠근다**」다. 그 트랙은 ㉠㉡을 착지시키고 남은 ▶(상한 분모)을 ⏳유호로 넘긴 채 주인이 죽었다 — 그래서 **자기가 고치려던 증상으로 남아 다음 트랙을 막는다.** 상한 계산 쪽은 F372·F395(유호 픽 08-16 ㉮)로 「죽은 줄·대기 줄을 분모에서 뺀다」가 이미 들어갔는데, **파일 겹침 검사는 그 갈래를 안 받았다.** 즉 같은 「죽은 활성 줄」이 한 검사에선 분모에서 빠지고 다른 검사에선 산 세션과 동급이다 — 판정이 두 곳에서 갈렸다.  ■ 이 세션이 실제로 한 것 (재료로 남긴다) 선언을 포기하는 대신 **PR 을 진행 목록으로 세웠다**(CLAUDE.md 「PR 은 리뷰 게이트가 아니라 작업 목록이다 — 세션이 죽어도 안 사라지는 진행 목록」). 겹침 사실·구역 대조(`worktree-master-drift-guard` 가 같은 파일의 머리말·파일끝을 들고 있고 내 구역은 needle 조회부라 무교차)는 커밋 메시지와 PR 본문에 적었다. 보드 줄은 **못 적었다** — 그 사실을 여기 남긴다.  ■ 처방 후보 — 안 지었다(이 신고는 기록이다) ㉠ 파일 겹침 검사에 **생사 게이트**를 붙인다: 상대가 죽었으면 deny 대신 **알림**(검사⑧이 이미 그    원칙으로 돈다 — 「생사로 가른다, 표식으로 가르지 않는다」). 판정은 `작업본소유자.살았나` 한 곳에서.    ⚠대가 = 죽은 줄의 파일을 두 세션이 동시에 집을 수 있다. 다만 그건 track-collision·PR 목록이      잡는 층이고, 지금은 **아무도 못 집는다**(더 나쁜 쪽). ㉡ 「죽은 활성 줄」을 **대기 줄로 강등**하는 통로를 둔다(⏳를 잃지 않고 자리만 놓게). ⚠대가 = 새 상태를    하나 더 만든다 — 상태가 늘면 판정하는 곳마다 갈린다. ㉢ #Q93 의 ⏳를 유호님이 답해 그 줄을 정상 종결시킨다. ⚠대가 = 이 구조는 그대로라 다음에 또 난다. 권고 = ㉠. 이미 같은 파일 안에 그 원칙으로 도는 검사(⑧)가 있어 새 판정을 안 짓는다. | |
