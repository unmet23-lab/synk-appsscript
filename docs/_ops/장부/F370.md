| F370 | 2026-08-12 | 마찰 | 침묵하도록 설계된 가드가 닷새간 상시로 울었다 — 「위험 없으면 침묵」이 구조적 거짓양성에 죽어 있었다. `작업본소유자 --hook` 은 🔴(살아있는 남의 작업본)이 있을 때만 말하도록 만들어졌고 회귀까지 있다. 그런데 08-07 에 인계문을 ❔모름에서 제 주인에게 옮겨 붙인 수리(`인계문지문`) 뒤로, 세션이 끝날 때마다 쌓이는 `docs/_ops/인계문/<지문>.md` 가 전부 「살아있는 남의 것」으로 판정돼 🔴 칸이 **구조적으로 0이 안 됐다.** 실측 2026-08-12: 🔴 5건 = 5건 다 남의 인계문, 진짜 충돌 0, 매 세션 1,114자. 그리고 몇 분 뒤 실제 충돌 2건(`.claude/hooks/auto-commit.js`·`tests/자동커밋.test.js` ← 살아있는 655d027c)이 그 인계문 줄들 밑에 깔렸다 — 이 장치가 존재하는 이유가 바로 그 2건인데 노이즈가 그걸 묻었다. 🔑 교훈 둘: ①**판정을 옳게 고치면 경보도 옳아진다는 보장이 없다** — 08-07 수리는 주인을 정확히 붙였고, 그 정확함이 그대로 상시 경보가 됐다(수리의 부작용은 수리한 층이 아니라 그 판정을 쓰는 층에서 난다). ②**「이름이 곧 주인」인 파일에 「편집하지 마라」는 알려 줄 것이 없는 말이다** — 알려 줄 게 없는 경보는 침묵 예산만 태운다. 비용축은 실측으로 기각됐다: SessionStart 상주 전체가 4,837자(≈1.2k토큰)라 세션 비용의 1% 미만이고, 메모리에 적힌 08-12 숫자(작업본소유자 2,711 · philosophy-card 2,096)는 이미 뒤집혀 있었다(1,115 대 2,840). | tools/작업본소유자.js — 경보층에만 `이름이곧주인` 필터를 넣었다(`docs/_ops/{인계문,보드}/<지문>.md` 이고 **든 사람이 그 이름 하나뿐**일 때만 🔴 에서 뺀다). 판정층(`판정들`)은 한 글자도 안 건드렸다 — git-scope-guard ⑨ 와 track-collision ⑤ 는 계속 같은 값을 보고 커밋·편집을 막는다(맹점 ④: 같은 판정을 두 곳에 적으면 갈라진다). `every` 로 쓴 것이 급소다: `some`·`includes` 면 침입자가 주인 뒤에 숨고 새는 방향은 「통과」다(board-move 가 죽은 주인의 보드 파일을 쓰는 실경로가 있다). 손 호출에서는 뺀 것을 🔇 절로 그대로 보여 준다 — 남은 것만 내고 뺀 것을 안 밝히면 「이게 전부」로 읽힌다. 회귀 tests/작업본소유자.test.js +6(픽스처 전용 · 실저장소 인계문은 세션마다 달라 안 쓴다), 08-07 검사 1건은 관찰 창구를 🔴→🔇 로 옮기고 계약(「❔가 아니다」)을 더 세게 못박았다 · 91/91 · **변이 4/4**(필터 제거·every→some·보드폴더 누락·🔇 자수 삭제 전부 적색) · test-ci 2762/0(skip 10). ⚠ ⚪(유물)에 제 입을 주는 것은 **일부러 안 했다** — 「위험 없으면 침묵」은 이미 회귀로 못박힌 계약이라 다른 수리에 얹어 뒤집지 않는다. 그 결과 미추적 무보호(F025)가 SessionStart 어느 화면에도 안 남는 것은 사실이고, 이건 따로 판정할 열린 질문이다. |
