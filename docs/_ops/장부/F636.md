| F636 | 2026-08-18 | 마찰 | **F628 이 이름 대어 남긴 재발 조건이 그대로 살아 있었다 — 훅 「설치 층」에 발동 조건이 0개다.** F628 은 폴백을 넣어 「값이 없다」는 닫았지만, 그 훅이 **깔리는** 층은 유호 픽에서 빠져 「다음 픽 자리」로 남았다. 그 자리가 이것이다.  ■ 실측 — 클라우드 컨테이너 `cse_01RMwzh13hN8xA69dZTwuRxV` · 2026-08-18T09:0x · 가지 `claude/continue-work-9obneg` · `.git/hooks/` 에는 `*.sample` 15개뿐이고 **전부 컨테이너 생성 시각(04:04)** 이다 = `git clone` 이 깔아주는 템플릿 그대로다. `core.hooksPath` 미설정(우회 설치도 아니다). · `node tools/install-githooks.js --check` → `commit-msg`·`pre-commit`·`prepare-commit-msg` **3건 전부 미설치**, exit 1. · 저장소 전수 grep — `install-githooks` 를 **부르는** 자리 **0곳**(참조 14곳은 전부 `tests/`). `.claude/settings.json` 의 SessionStart 13항목 중 **0건**. · 이 컨테이너의 SessionStart 훅(보드수거)이 오늘 낸 커밋 8건 = `Session-Id` 트레일러 **0**.  ■ 🔑 F628 이 「안 쟀다」고 적은 칸을 **쟀다** F628 안 잰 것: 「클라우드 컨테이너에 훅이 왜 없는지(안 돌렸나·돌렸는데 실패했나)는 여전히 안 쟀다.」 → **안 돌렸다.** 부르는 자리가 0곳이라 실패할 기회조차 없었다. 샘플 15개가 clone 직후 상태 그대로인 것이 그 물증이다(설치기가 돌았다면 `.sample` 옆에 실패 흔적이든 파일이든 남는다). 즉 설치기는 「누가 기억하면 도는」 물건이었고, 어제 그것이 돈 유일한 자리는 직전 세션 `ad0673f3` 이 F628 을 **고치려고 손으로 부른** 한 번이다.  ■ 그래서 클라우드에서 한 번도 안 돈 게이트 셋 · `prepare-commit-msg` — 커밋 주인 표식(F041·F628). 정본이 「커밋 주인은 이걸로 읽는다」고 못박은 재료. · `commit-msg` — 커밋 **메시지**의 원시 제어문자 차단(F454). 그 훅 머리말이 스스로 「가장 비싼 자리」라 적은 것이다: 메시지에 박힌 바이트는 이력 재작성 말고 되돌릴 길이 없다. · `pre-commit` — 계약 동기화·계약 동봉·이해대장 동봉 게이트.  ■ 새는 방향 = 「통과」 훅이 **안 도는 것**과 훅이 **통과시킨 것**은 화면에서 같은 모양이다. `tests/커밋트레일러.test.js` 의 배선 검사가 이 상태를 정직하게 **skip** 으로 드러내고는 있었지만, 그 skip 을 **행동으로 옮기는 자리**가 없어 매 세션 조용히 지나갔다. CLAUDE.md 신뢰성: 스스로 발화하지 않는 장치는 안 돈다 · 장치와 그 발동 조건은 같은 커밋에서 정한다. | SessionStart 훅으로 **발동 조건을 만들어** 닫았다 — 착지 `23b35c36`.  ■ 무엇을 했나 · 신설 `.claude/hooks/githook-install.js` — `--check` 로 **먼저 묻고**, 깔 게 있을 때만 깔고 말한다(할 일 없으면 완전 침묵 · 매 세션 뜨는 줄은 그 자체가 비용이고 상시 소음은 진짜 경보를 죽인다). 깐 뒤 `--check` 로 결과를 **다시 재** 「썼다」와 「돈다」를 가른다. · 등록 = `.claude/settings.json` SessionStart **맨 앞**, matcher `startup|resume|clear`(장치와 발동 조건을 같은 커밋에 담았다). · 회귀 신설 `tests/훅설치발동.test.js` **7건** · CI 모사 **4651 · fail 0 · skip 126**.  ■ 판정을 두 곳에 안 적었다 무엇을 깔지·무엇을 안 덮을지(F391 보류)는 `tools/install-githooks.js` 하나가 그대로 진다. 이 훅은 **부르는 자리**와 **소음 정책**만 맡고, 거부 처방문은 설치기 것을 문자 그대로 흘린다(내가 다시 쓰면 두 처방이 갈린다 · F103). 새 이음매 **0개** — 회귀 픽스처는 설치기가 이미 가진 `SYNK_GITHOOKS_ROOT` 를 그대로 쓴다. 설치기에 새 플래그도 안 만들었다: PR #111 이 그 파일에 인자 게이트를 다는 중이라(아는 플래그 = `--check`), 새 플래그는 그 PR 이 착지하는 날 조용히 거부당한다.  ■ 변이 **6/6 · 구멍 0** — 그리고 변이가 실제로 구멍 하나를 뚫었다 M5 「설치기가 죽으면(`status === null`) 그것을 «최신» 으로 읽는다」로 망가뜨렸더니 훅이 **조용히 exit 0** 하는데 회귀 6건이 전부 초록이었다 — 죽은 프로세스의 침묵과 「깔 게 없다」의 침묵이 같은 모양이다(F207). 검사 ⑦(스스로 SIGKILL 하는 가짜 설치기)로 닫았다. 나머지 다섯: M1 사전 `--check` 생략(→상시 소음) · M2 거부를 통과로 · M3 사후 재확인 생략 · M4 설치기 부재 무시 · M6 분모 미보고 — 전부 잡혔다.  ■ 라이브 실증 (이 수리의 증거) · 이 저장소에서 훅 3개를 치우고 돌리니 **3건을 깔고 말했고**, 다시 돌리니 **침묵했다**. · 깐 뒤 낸 커밋 `639897a1` 에 `Session-Id: cse_01RMwzh13hN8xA69dZTwuRxV` 가 박혔다 — 이것이 **F628 폴백(HOST 없음 → REMOTE)의 첫 실환경 실증**이다. 직전 세션은 `CLAUDE_CODE_HOST_SESSION_ID` 가 **있는** 컨테이너였어서(그 세션 커밋의 트레일러는 접두 없는 UUID `ad0673f3-…`) 폴백 갈래는 픽스처에서만 돌았다. 이 컨테이너는 HOST 가 비어 있어 그 가지가 처음으로 실제로 탔다.  ■ 대가 (새 장치엔 대가를 함께 적는다) · **행동이 바뀐다**: `pre-commit` 이 깔리므로 지금까지 클라우드에서 그냥 통과하던 커밋이 계약·동봉 게이트에 **막힐 수 있다.** 그게 목적이지만 「어제는 되던 게 오늘 안 된다」로 보이는 자리다. · **순서는 계약이 아니다**: SessionStart 항목들의 실행 순서는 보장이 아니므로, **같은 SessionStart 안에서** 남의 훅이 내는 커밋(보드수거 등)까지 잡는다고 약속하지 않는다. 보장하는 것은 「SessionStart 가 끝난 뒤 이 세션이 내는 커밋」이다. 배열 맨 앞에 둔 것은 확률을 올린 것이지 증명이 아니다. · **닫을 것: 없다** — 이 장치가 대신하는 것은 「사람이 기억해서 부른다」라 끌 장치가 아니다. `tests/커밋트레일러.test.js` 의 배선 검사는 계속 **skip** 이어야 한다: CI 에는 훅이 애초에 없고, 거기서 fail 을 요구하면 repo 밖 환경에 기댄 검사가 된다(F296).  ■ 남는 것 · 안 잰 것(F348) · **GitHub PR 머지 커밋 층은 그대로 비어 있다** — 로컬 훅이 원리상 못 닿는다(F628 유호 픽 「머지층은 포기」). 그 커밋의 주인은 트레일러가 아니라 PR 이 말한다. · **`CLAUDE_CODE_HOST_SESSION_ID` 가 빈 컨테이너에서 보드·소유 판정 도구들이 내 지문을 못 읽는다** — 훅은 폴백을 얻었지만 `tools/`·`.claude/hooks/` 쪽 판정층은 아직 HOST 하나만 본다(전수 grep: `REMOTE_SESSION_ID` 를 읽는 코드는 훅 1곳뿐). 그 축은 **남의 PR #133 이 `tools/lib/세션지문.js` 로 짓는 중**이라 여기서 안 건드렸다(같은 축을 나란히 지으면 둘 다 끝까지 간다 · F340·F625). · 이 수리 뒤 실제로 몇 %의 클라우드 커밋이 트레일러를 갖게 되는지는 다음 회차들이 쌓아야 잴 수 있다. · 훅 설치가 **다른 클라우드 세션에서도** 실제로 발화하는지는 이 컨테이너에서 못 잰다(내 SessionStart 는 이미 지나갔다) — 다음 세션의 첫 커밋이 그 증거가 된다. |
