| F579 | 2026-08-17 | 마찰 | 형제 저장소 배포빚의 「모름」이 두 상태를 뭉친다 — 운영 companion·radio-round 는 HTTP 404(배포된 적 없음)인데 도구는 「안 잰 것」이라 부르며 「재려면 --도장」을 처방한다. 재면 또 404 → 또 모름. 따를 수 없는 처방이 영구히 나오는 자리(F103 재발). 오늘 아침 002076a 도 같은 실측을 커밋문에만 적어 두 번째다. | talk `f2da629` — 「모름」 안에서 «못 쟀다» 와 «라이브에 없다» 를 갈랐다. ①`배포대조.js` 가 404 만 `부재` 플래그로 가른다(503·끊김은 그대로 「못 쟀다」) ②`--도장` 이 부재를 장부에 남긴다(`출처:'부재'` · head=지금 HEAD → 배포하면 `출처:'배포'` 행이 뒤에 붙어 최신기준이 자동으로 덮는다 = 부재 기록은 스스로 낡지 않는다) ③`배포빚.js` 가 `미배포` 를 `모름` 의 **부분집합**으로 내고 처방만 「배포하라」로 갈린다. 🔑 상태값은 `미측정` 그대로 뒀다 — 새 상태를 신설하면 `게이트판정`(다름/못잼만 본다)·집계·출력의 기존 필터를 전부 빠져나가 **「통과」로 샌다**. 🔑 급소는 순서다: 부재 도장의 head 가 지금 HEAD 라, `빚판정` 이 부재를 먼저 안 가르면 변경 0 ⇒ 「최신」으로 샌다. 회귀 7 신설(48/48 · 기존 41 무변경 — 「분모 등식」 테스트가 그대로 통과) · 실측 끝단: 부재 2 도장 → 배포빚이 「그중 2 은 재봤더니 라이브에 없다 … 처방은 대조가 아니라 배포다」로 갈려 나온다. ⚠변이 검사 없음(형제 저장소에 `tools/변이.js` 가 없다 — 안 돌린 것을 밝힌다). |
