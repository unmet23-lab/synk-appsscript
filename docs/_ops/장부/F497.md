| F497 | 2026-08-16 | 실수 | 철회 가드를 부분 문자열로 짜서 「소프트웨어」가 「웨어」에 걸렸다 — 실저장소 거짓양성 검사는 그때도 초록이라(그 낱말이 마침 없었다) 오탐을 못 봤다. 탐지·오탐 둘 다 픽스처가 져야 한다 | `tools/발표물린트.js` 경계 검사(`5d6905b1`) + **영문 오탐 픽스처**(`299412c1`) — 두 커밋이 한 벌이어야 닫힌다.  ■ 왜 두 벌인가 — 신고 당시엔 처방이 절반만 집행돼 있었다 `5d6905b1` 이 부분 문자열을 경계로 바꾸고(한글 `/(?:SYNK|싱크)\s*웨어/` · 영문 `/Wear(?![a-z])/`·`/Pantry(?![a-z])/`) 오탐 픽스처를 심었는데, 그 픽스처는 **한글 셋**(소프트웨어·하드웨어·미들웨어)뿐이었다. 커밋 메시지는 「오탐 픽스처를 새로 심었다 · 26 pass」로 읽혀 다 덮은 것처럼 보였다.  ■ 실측으로 갈랐다 (2026-08-16 · `node tools/변이.js` 공용 통로) 경계를 지워 F497 이 신고한 그 부분 문자열 판으로 되돌리는 변이 4건:   · 한글 경계 제거      ✔ 포획   · `Wear` 경계 제거     ✖ **구멍** — 26건 전부 초록   · `Pantry` 경계 제거   ✖ **구멍** — 26건 전부 초록   · 철회 목록 항목 제거  ✔ 포획(탐지 쪽) → **변이 2/4 · 구멍 2 · 미적용 0**. 즉 누가 영문 경계를 「단순화」로 지워도 회귀가 한 건도 안 울었고, 그 초록은 통과와 **모양이 같았다**(지침 v9.2 맹점 ④ · F207).  ■ 메운 뒤 재측정 — **변이 4/4 · 구멍 0 · 미적용 0** · 기준선 초록 27건(26 → 27). 영문 픽스처 = Wearable·Wearables(에듀테크 발표물에 실재) + Pantryless(대칭 방어임을 주석에 명시 — 실재 오탐을 막는 게 아니라 경계를 지우는 편집이 조용히 통과 못 하게 모양을 못박는 것).  ■ 이 신고가 남긴 것 (F497 본문의 「탐지·오탐 둘 다 픽스처가 져야 한다」가 그대로 증명됐다) 실저장소 전량 통과는 **오늘의 사진**이라 오탐을 못 본다 — 그때도 초록이었고 지금도 초록이다. 그리고 픽스처를 심을 때 **고친 표면 전부**를 덮었는지는 초록이 말해 주지 않는다. 그걸 가르는 건 변이뿐이다.  새 장치 0 — 기존 회귀 파일의 픽스처만 늘렸다(하네스 한 달 봉인 준수 · memory harness-deletion-verdict-0816). |
