| F593 | 2026-08-18 | 마찰 | 가드·회귀가 낸 **사유가 실제 사유와 달라** 그 처방을 따라가면 헛돈다 — 두 자리를 실측했다(2026-08-17~18 `local_fbfdb2e9` · F591 정정 작업 중 나왔다). ㉠ `code-edit-guard` 의 처방 3 이 「대상이 임시 파일이면 스크래치패드 경로로 옮긴다(그 경로는 이 훅이 안 본다)」인데, **스크래치패드 안에서 상대 표기로 돌린 명령이 그대로 막힌다**(상대 경로를 cwd 로 안 푸는 것은 의도된 보수 판정이고 그 사실이 처방문에 없다). 실측에서 세 번 연속 막혔다 — ①상대 표기 ②경로를 node argv 로 조립 ③PowerShell 에서 `$env:CODE_EDIT_BYPASS='1'` 로 따옴표를 씌움. ③은 처방문이 적은 `CODE_EDIT_BYPASS=1` 이 **bash 문법**이라 이 저장소의 주 셸(PowerShell · CLAUDE.md 「셸이 둘이다」)에서 그대로 못 쓰기 때문이다 — 주 셸에서 못 쓰는 우회는 없는 우회고, 그 자리에서 「막을 수 없는 걸 막는 가드」가 되어 BYPASS 를 손버릇으로 가르친다(F103). ⚠ 이 훅은 같은 함정을 **이미 한 번 밟았다** — 머리말 F088 이 「3번 처방이 '스크래치패드로 옮겨라'인데 이미 스크래치패드였다」라고 적어 두고 변수 풀기로 한 갈래만 막았다. ㉡ `tests/훅이식성.test.js` ④ 스캔범위 검사가 「미추적이라 범위 밖」과 「날문자 위반」을 **같은 실패문**으로 낸다 — 새로 지은 훅이 미추적일 때 「훅으로 등록됐는데 날문자 스캔 밖」이 떴고, 진짜 처방은 `git add` 인데 날문자를 찾으러 가게 만들었다(맞는 얼굴로 다른 것을 가리킨다 · CLAUDE.md 맹점 ④). 축은 하나다: **가드가 «막았다»까지는 맞는데 «무엇을 고쳐야 하나»가 틀리면, 그 시간은 통째로 버려지고 결국 우회로 간다.** | `7e29ecfd` — ㉠ `code-edit-guard` 처방문에 두 줄을 넣었다: 「⚠**절대경로로 적어야 한다** — 상대 경로는 cwd 로 풀지 않으므로(풀면 temp 아래 저장소에서 가드가 통째로 꺼진다) 스크래치패드 «안에서» 돌려도 그대로 막힌다」와 「PowerShell 이면 `$env:CODE_EDIT_BYPASS=1;` 로, **따옴표를 씌우면 안 걸린다**」. 그리고 그 약속을 `tests/코드편집가드.test.js` 의 **자기 처방 되먹임 4건**이 지킨다(CLAUDE.md 가드 맹점 ③ — 차단 사유가 시키는 명령을 그 가드에 되먹여 통과하는지 본다): ①처방 3 을 따른 절대경로 쓰기는 통과 ②상대 표기는 막되 «왜»를 말한다 ③문서화된 BYPASS 표기가 **두 셸 모두**에서 실제로 통한다 ④처방문이 두 셸 표기를 둘 다 적고 있다. ㉡ `tests/훅이식성.test.js` ④ 는 「범위 밖」을 갈라서 말한다 — **죽은 등록**(파일이 없다 · F044)과 **미측정**(미추적이라 안 봤다 · F207·F591)은 처방이 정반대다. **변이 4/4 · 구멍 0 · 미적용 0.** 🔑 그 변이가 **내가 방금 쓴 회귀의 구멍**을 짚었다: 이 저장소의 실제 스크래치패드가 `AppData/Local/Temp/` 아래라 `temp` 규칙과 `scratchpad` 규칙이 겹치고, 겹친 자리만 재던 첫 판은 `/scratchpad/` 를 통째로 죽여도 36건이 전부 초록이었다 — 갈래를 나눠 다시 못박았다. ⚠ **㉡은 회귀로 탐지되지 않는다**(실저장소에 미추적 훅이 0이면 갈라진 두 갈래가 모두 빈 배열이라 어느 쪽을 지워도 초록이다) — 문구 층의 수정이고 그 한계를 커밋 메시지와 여기 함께 적는다. |
