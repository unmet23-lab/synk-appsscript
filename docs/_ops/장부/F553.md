| F553 | 2026-08-17 | 실수 | 「N분째 멈춤」을 주인 생사 판정에 썼다 — 처분(PR 닫기)을 먼저 하고 대상(워크트리 status)은 나중에 열었다. 순서가 거꾸로였다.  실사고 2026-08-17: F403 통로가 워크트리 `loom-docs-standing-pages` 를 「@468b0305 · master 미포함 8커밋 · 329분째 멈춤」으로 냈다. 나는 그 숫자를 「주인이 죽었다」로 읽고 ①내용 대조 ②회귀 실측(7/7 안 돎) ③PR #34 에 판정 코멘트 ④PR close 까지 갔다. **그 뒤에야** 워크트리 status 를 열었고, 거기 `UU .claude/hooks/lib/session-report.js` — **머지 충돌 미해결**이 있었다. 2분 뒤 다시 보니 HEAD 가 `f21c9fae` 로 올라가 있었다: 주인 세션은 **살아서 그 충돌을 풀고 있었다.** reopen 으로 되돌렸다(gh pr reopen 34 + 정정 코멘트).  무엇이 나를 속였나 — 셋이 겹쳤다: ① 박동은 **파일 접촉**으로 찍히는데, 머지 충돌로 멈춘 워크트리는 세션이 살아서 «판단 중»인 동안 파일을 안 만진다. SessionStart 훅은 이 함정을 「codex-review·CI 같은 긴 외부 프로세스」로만 적어놨다 — 충돌 대기는 그 목록에 없다. ② F403 처방문은 「반입하거나 remove」 두 길만 준다. **주인 생사를 가르는 칸이 처방 앞에 없다.** 그래서 「갇힘」이 곧 「주인 없음」으로 읽힌다. ③ `git worktree list` 의 `locked` 표식이 있었다 — 다른 워크트리 4개는 locked, 이건 아니었다. 나는 그걸 「주인 없음」의 방증으로 읽었지만, 실은 잠금을 안 건 세션도 있다는 뜻이었다.  이미 정본에 있는 규율이 이걸 막았어야 했다 — 「조작·판정·인용·이관 **직전에** 그 대상을 직접 연다」. 나는 워크트리를 «대상»으로 안 셌다. PR 을 닫는 조작의 대상은 PR 이 아니라 **그 가지가 사는 작업 트리**다.  처방 후보 ①(기계): F403 블록이 `git -C <워크트리> status --porcelain` 을 한 번 불러 `UU`·`MERGE_HEAD`·미커밋이 있으면 「멈춤」이 아니라 **「사람이 붙어 있는 자리」**로 낸다 — 처방문도 그때는 remove 를 안 준다. ②(규율): 워크트리·PR 처분 직전 status 를 여는 것을 「직접 연다」의 대상 목록에 명시. | tools/작업본소유자.js — 갇힘 처방 앞에 점유상태() 를 세웠다: 갇힌 워크트리마다 git status --porcelain -uall 을 한 번 불러 미커밋·충돌을 재고, 손자국이 있으면 🧊(처분 처방) 대신 🧑(처분 금지 · remove 를 안 준다)로 낸다. 못 쟀으면 참으로 기운다(안전 방향). 회귀 tests/작업본소유자.test.js 117/117(신설 4 · 충돌은 진짜 git merge 로 만든다) · 변이 7/7 구멍0 미적용0 · 실물 대조 3트리(2건이 옛 판에서 「지워라」를 받던 것). |
