| F611 | 2026-08-18 | 마찰 | E2E 픽스처가 «과녁 도구의 부품 목록»을 손으로 세고 있어서, 그 도구에 `require` 한 줄이 늘자 **남의 트랙 회귀가 통째로 죽었다** — 그리고 그것을 잡은 층은 트랙 자신의 회귀가 아니라 전량 CI 하나뿐이었다.  실측 2026-08-18 (`local_5c475120` · PR #104 인계 승계). F606 트랙이 `tools/대기열.js` 에 인자 게이트를 부착하며 `const { 인자게이트 } = require(path.join(__dirname, 'lib', '인자게이트.js'))` 를 최상위에 한 줄 더했다. 그 트랙의 자기 회귀(`tests/도구인자게이트.test.js` 14건)는 **초록**이었고 변이도 14/14 였다. 그런데 `node tools/test-ci.js` 는 빨갰다 — `tests/대기열채번.test.js` 가 `MODULE_NOT_FOUND: tools/lib/인자게이트.js` 로 죽는다.  ■ 한 갈래만 바꿔 대조했다(CLAUDE.md 원인 판정): 같은 파일을 master 에서 돌리면 **17/17 통과**, 이 가지에서 돌리면 fail. 바뀐 조건은 그 require 한 줄뿐이다.  ■ 왜 죽었나 — 그 시험은 임시 디렉터리에 «가짜 저장소»를 지어 진짜 `git commit` 으로 pre-commit 훅을 돌리는 E2E 다. 그 가짜 저장소에 실물 파일을 **손으로 네 개** 베껴 넣는다(`pre-commit`·`대기열채번검사.js`·`대기열.js`·`lib/확정대조.js`). 부품이 하나 늘어도 이 목록은 안 늘어난다. 새는 방향이 「통과」가 아니라 「크래시」라 조용하지는 않지만, **터지는 자리가 남의 트랙**이고 그래서 부착한 세션의 초록에는 아무 자국도 안 남는다.  ■ 그리고 이 목록은 이미 낡아 있었다(이번 줄이 처음이 아니다): `대기열.js` 는 `const 라이브 = (이름) => require(path.join(__dirname, 'lib', 이름))` 로 lib 를 **이름으로 늦게** 부르고, 실제로 `라이브('보드.js')`·`라이브('표.js')` 를 쓴다. 둘 다 픽스처에 실려 있지 않았다 — 그 갈래를 타는 시험이 아직 없어서 안 터졌을 뿐이다. 즉 손 목록은 이 도구에 대해 **원리상** 못 맞는다(정적 목록으로 동적 require 를 못 좇는다).  ■ 축: CLAUDE.md 신뢰성 「같은 판정을 두 곳에 적으면 갈라진다(목록은 하나에서 파생)」의 픽스처판이다. 여기서 갈라진 두 곳은 «도구가 실제로 요구하는 부품»과 «픽스처가 싣는다고 적어 둔 부품»이고, 갈라지는 쪽은 언제나 픽스처다(도구는 자기 require 를 늘릴 때 픽스처를 안 본다).  ■ 이 신고가 남기는 재료: 부착·통로 트랙에서 **「내 회귀 초록」은 분모가 자기 파일 하나**라는 것. 공용 파일(`tools/*.js`)에 require 를 더하는 트랙은 그 자체로 다른 스위트의 픽스처를 깨뜨릴 수 있고, 그 층은 전량 CI 에서만 보인다. F606 트랙의 보드가 「전량 CI 미완」을 스스로 적어 인계한 것이 정확히 이 사고를 막았다 — 인계문의 ⚠ 한 줄이 실제로 값을 냈다. | tests/대기열채번.test.js 의 lib 동봉을 손 목록 → tools/lib/*.js 통째 싣기로 (readdir 파생 · 빈 복사 방지 assert 동봉). 옆 «최상위 게이트» 를 일부러 안 싣는 기존 설계 의도는 그대로 — lib 는 옆 게이트가 아니라 과녁의 부품이라 축이 다르다. 실측 master 17/17 ↔ 가지 fail → 수리 뒤 17/17 |
