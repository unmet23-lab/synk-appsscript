| F533 | 2026-08-17 | 마찰 | 왕복골격 가드가 «주석 속 낱말»도 위반으로 센다 — 행동은 그대로인데 문구 하나로 빨개진다(F518 계열의 다른 자리). 실측 2026-08-17: talk tools/라디오왕복시험.js 에 「자격 모듈을 직접 안 든다」는 설계 주석을 적었더니 tests/왕복골격.test.js 가 그 낱말을 세어 fail. 픽스처가 의도적으로 주석 형태를 위반으로 못박아 뒀다(35~39행) — 즉 설계상 그런 것이지 실수가 아니라, 고치려면 그 계약을 여는 판정이 필요하다. 대가: 지금은 «금지 모듈을 왜 안 쓰는지» 를 코드 옆에 적을 수 없다(우회로 = 낱말을 피해 쓰기 = 다음 사람이 못 찾는다). 처방 후보 ㉠tests/lib/소스검사.js 의 코드만() 으로 주석을 벗기고 픽스처를 «코드 형태»로 바꾼다 ㉡그대로 둔다(주석까지 막는 게 의도라면 그 이유를 픽스처 옆에 적는다). 이 신고는 기록이다 — 남의 가드 계약이라 내 트랙에서 안 바꿨다. | tests/왕복골격.test.js 의 판정을 tests/lib/소스검사.js 의 코드만() 통로로 옮겼다 — talk f4663cd 계열 커밋 8acaf61. 계약을 연 근거는 산문이 아니라 선례다(#Q112 ㉡ 가 같은 저장소 같은 주에 같은 축을 그렇게 닫았다). 바꾸기 전에 쟀다: 왕복시험 도구 11개 전부 위반 0(전문·코드만 동일)이라 이 완화가 가리는 진짜 위반은 0건이고, tools/ 의 7개 파일이 이미 그 낱말을 주석에만 쓰고 있어 관습은 이미 이쪽이었다. 탐지력은 픽스처가 진다 — 금지 조각을 문자열 표기·날 표기 두 벌로 넣어 렉서가 삼키면 그 자리에서 빨개진다. 덤으로 변이가 뚫은 구멍 둘을 함께 닫았다(금지 목록이 조용히 줄어드는 방향 = 래칫 신설 · 목록 낡음 검사가 주석 언급을 「산다」로 세던 방향 = 픽스처 신설). F083 재주입 = tools/라디오왕복시험.js 가 우회로 「자격 모듈」이라 에둘러 적던 자리를 lib/자격증명.js 로 되돌렸고 초록을 확인했다. 잰 것 = 왕복골격 10/10(신설 3) · 변이 4/4 구멍0 미적용0 · talk 전체 2368/2368 fail0. F092 대조 = 같은 부류가 남아 있어 새 신고로 세웠다(통로 가드가 사본만 세고 비사용을 안 센다 · 살아있는 거짓 초록 0 · 잠재 4). |
