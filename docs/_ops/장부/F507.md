| F507 | 2026-08-16 | 마찰 | 「기계로 옮긴다」고 적은 처방이 **안 도는 층**에 적혀, 네 판 연속 재발한 결함을 한 판 더 통과시켰다 — 장치의 «발동 시점»을 안 적으면 「구현 뒤에 도는 검사」와 「오늘 도는 검사」가 같은 얼굴이다.  실측 2026-08-16 (`local_8369e570` · 상태기반 과제선택 13회차 처분 중): 12회차 처분표 §6 은 축 ㉡(형제 절 미수거)이 v10→v11→v12 세 판 연속 재발한 것을 두고 「세 판 연속이면 집행자의 부주의가 아니라 «절차에 검사가 없다»는 뜻이다 ⇒ 이번엔 문장이 아니라 **기계로** 옮긴다」로 닫고, 설계문 §12 항30 을 세우며 「🔑 **새 장치 0** — §12-21 이 **이미** 하는 일의 «대상»만 넓힌다」·「다음 판에서 ㉡가 또 나면 볼 곳은 **이 대조 목록**이다(덜 잡은 것이다)」라 적었다.  13회차에서 ㉡가 **또 2건** 났다(`input_hash` 대상 낱말 = 갈래 6 · `degraded` 생산자 = 갈래 7 — 둘 다 v5.9 가 「고쳤다」고 도장 찍은 자리이고, 형제 줄이 안 걷혔다). 그래서 항30 이 시킨 대로 「대조 목록」을 열러 갔더니 **목록이 없었다.**  두 층을 다 열어 쟀다: ①항30 이 이름 댄 기계는 설계문 §12-5 항5 가 「**설계문 숫자 회귀**」로 부르는 것인데 저장소 전량 검색 **0매치**다(as `tests`186·`tools`141·`hooks`39·`workflows`1 / talk `tests`164·`tools`45·`workflows`1 — 이 설계문을 읽는 기계는 `설계문_폐기어휘`·`인용검사`·`대기열점유` 셋뿐이고 **어느 것도 절 간 값을 대조하지 않는다**). ②그리고 항30 이 적힌 §12-21 은 **⛔착수 차단 중인 구현의 «인수 조건»**이다 — 구현이 서기 전엔 원리상 한 줄도 안 돈다.  ⇒ 처방은 옳았고(기계) **층이 틀렸다.** 같은 저장소에 **오늘 도는 문서층 가드**(`tests/설계문_폐기어휘.test.js` · 08-13 신설)가 이미 있었는데 아무도 그 주소를 안 봤다.  왜 안 걸렸나 — CLAUDE.md 신뢰성 조항이 「장치와 그 발동 조건은 같은 커밋에서 정한다」까지는 말하는데, **발동 조건이 «미래»인 장치를 지금 세는 것**을 안 막는다. §12-21 의 발동 조건은 「구현 착수 뒤」라 형식상 «적혀 있고», 그래서 조항을 지킨 얼굴로 통과했다. 「스스로 발화하지 않는 장치는 안 돈다」의 변종인데, 이건 **발화 시점이 무한정 미뤄진** 경우다.  처방(집행함): ①주소를 옮겨 **`tests/설계문값대조.test.js` 신설** — 대조 4벌(`input_hash` 낱말 · `degraded` 생산자 · `policy_ver` 자리수 · 재료 수 §5-1↔§12-5) · 픽스처 5 + 실저장소 2 = 7/7 · 실사용 첫 호출이 거짓양성 1건(항30 자신의 «결함 서술» 줄)을 잡아 판별어를 좁혔다 ②설계문 항30 에 「위 🔑 는 거짓이었다」를 명문으로 적고 주소를 정정 ③§13 에 🚫「없는 장치를 「이미 있다」로 적기」 등재.  남은 빈칸(안 지었다): 「이 처방은 언제부터 도나」를 **적게 강제하는 자리가 없다.** 지금은 규율(처분표 v13 §6)로만 남았고, 같은 실수를 다시 하면 또 한 판이 지나간다. 지침 층 후보 = 신뢰성 절의 「장치와 발동 조건은 같은 커밋」에 **「발동이 미래면 지금은 «없는 장치»로 센다」**를 붙이는 것. | ⏸ 판정 2026-08-16 `local_8369e570` (커밋 c20d6773) — 이 «건»은 고쳤다: 기계를 도는 층에 세웠고(tests/설계문값대조.test.js 7/7 · 폐기어휘와 11/11), 설계문 항30 의 거짓 전제를 명문 정정했고, §13 에 🚫「없는 장치를 「이미 있다」로 적기」를 등재했다. **그런데 «원인»은 안 닫혔다** — 「이 처방은 언제부터 도나」를 적게 «강제»하는 자리가 지금도 0이다. 남은 처방은 지침 층(신뢰성 절 「장치와 발동 조건은 같은 커밋」에 「발동이 미래면 지금은 «없는 장치»로 센다」를 붙이는 것)이고, 정본 개정은 /evolve 이며 그 호출은 유호님 몫이다(AI 는 제안만). ⇒ 해소가 아니라 보류다. 다시 재지 않는다 — 열 조건 = /evolve 가 이 항을 집는 날. |
