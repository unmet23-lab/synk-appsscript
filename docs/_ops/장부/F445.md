| F445 | 2026-08-14 | 마찰 | 심문 통로 둘의 이름이 겹쳐 대기열 줄이 잘못된 통로를 지목했다 — 08-13 재판정이 바로 이 혼동을 경고했는데 08-14 에 재발했다. #Q49 줄머리는 「집는 세션이 /fable심문 으로 돌린다」인데, 처분표 v9 의 실제 다음은 codex-review.js --심문(luna x2 + sol x1)이고 1~9회차가 전부 그 통로다. 적힌 대로 fable 만 돌리면 회차 간 대조가 원리상 무효다(F281 — 그 대조가 이 게이트의 존재 이유). 두 통로를 다 돌려 수습했으나 근본 원인은 이름이 겹치는 것이고, 줄을 쓰는 사람이 매번 옳게 고르기를 기대하는 구조다. | 이름이 겹치던 두 통로 중 하나가 **구조적으로 사라졌고**(08-14 유호 지시 「너무 낭비」 — 스킬·도구·`심문자.md` 삭제 · 실측 08-15: `.claude/skills` 에 fable 0 · `tools` 에 심문 도구 0), 남은 마지막 자리를 이번에 닫았다. 🔑 **실제로 막던 것은 삭제가 아니라 낡은 한 줄이었다** — 정작 모델 정본 `tools/모델정책.js:455` 가 폐지 뒤에도 `/fable심문` 을 「예외 하나」로 계속 적고 있었고, 이미 지워진 `심문자.md` 를 「그 통로라 유지」라고 말하고 있었다. 신고문이 지적한 「줄을 쓰는 사람이 매번 옳게 고르기를 기대하는 구조」가 바로 거기였다(사람이 읽는 정본이 둘이 있다고 말하면 대기열·보드는 계속 둘로 적는다). 처방: ①그 줄을 정정 — 「예외는 이제 없다 · 심문 통로는 `codex-review.js --심문` 하나뿐」 ②프로즈로 끝내지 않고 회귀를 세웠다 `tests/모델정책.test.js` 「[F445] 폐지된 fable심문 이 살아있는 지시문에 쓸 수 있는 통로로 남아 있지 않다」 — 살아있는 지시문(tools·.claude)만 훑고 docs·장부는 **기록**이라 뺀다(기록까지 금지하면 왜 폐지됐는지를 잃는다). 검증: 30/30 · **변이 3/3(구멍 0 · 미적용 0)** — M1 이 F083 그대로다(신고 원문의 그 낡은 줄을 되돌려 넣으니 빨개졌다) · M2 부활 금지 표식 제거 포획 · M3 자기변이로 분모 가드(볼곳>50)가 살아 있음 확인. 💸 대가: 「폐지·🚫」가 같은 줄에 있으면 통과시키므로 그 낱말을 달고 되살리는 문장은 못 잡는다 — 대신 F445 가 실제로 난 모양(「예외가 있다」고 태연히 적기)은 잡는다. 닫는 근거 = 이 신고의 급소였던 «이름 겹침»이 이제 원리적으로 못 산다: 부를 통로가 없고, 있다고 적으면 회귀가 빨개진다. |
