| F675 | 2026-08-20 | 마찰 | CI 러너가 죽자 저장소 전체가 멈췄다 — 열린 PR 20개·master 정체 16시간, 그런데 어느 층도 그것을 «적색»으로 내지 않았다. 실측 2026-08-20 05:0x UTC(유호님 상태 문의로 처음 드러남): ①`origin/master` 마지막 커밋이 08-19 13:39 KST(PR #150)에서 멈춰 있다 — 그 뒤 16시간 갱신 0건 ②그 사이 열린 draft PR 이 20개 쌓였다(#142~#165) ③syntax-check 의 queued run 5건 중 가장 오래된 것이 08-19 20:38 KST 로 **17시간 27분째 큐에 있다** — 셀프호스티드 러너(유호님 노트북 WSL2)가 그 시각 이후 오프라인이다 ④그래서 PR 들의 mergeable_state 가 `unstable` 이고, 검사가 안 끝나니 아무도 머지를 못 해 어제 하루 작업 전체(경량화 PR #166 · 지침 v9.3 #152 · v9.4+낡은문서 21종 삭제 #160 · 재제안금지 폐지 #157 · 자기검증층 철거 #158)가 **한 줄도 master 에 안 닿았다**. 저장소가 지금 읽는 지침은 여전히 v9.2(08-13)이고 「재제안 금지」 3자리가 그대로 산다.  새는 방향이 나쁘다: 「검증이 없으면 못 간다」가 「검증이 없으니 아무것도 못 갔다」가 됐다 — 안전 장치가 단일 장애점으로 뒤집혔고, 그 상태가 조용하다.  왜 안 보였나 (장치 자신의 결함 — 세 자리): ㈎ SessionStart 「눈먼 층」 훅이 원격 CI 0건의 이유로 세 갈래만 댄다 — ①`push:` 주석 ②러너 스위치 꺼짐 ③`gh` 없음. **실제 원인인 「스위치는 켜졌는데 러너가 오프라인」이 목록에 없다.** 그래서 훅이 「0건」이라 말할 때 읽는 쪽은 ②라고 읽는다(맞는 얼굴로 틀린 값). ㈏ 열린 PR 수를 못 잰다 — `tools/작업가지.js --훅` 이 `gh` 에만 기대는데 클라우드엔 `gh` 가 없다. GitHub MCP 로는 읽힌다(이 신고가 그것으로 쟀다). ㈐ master 정체 시간을 재는 층이 아예 없다 — 「마지막 master 커밋으로부터 N시간」은 아무 훅도 안 낸다. 이 사태의 가장 싼 단일 지표인데 분모에 없다.  F665(「도구를 짓고 켜지 않은 채 닫혔다」)의 같은 병 2번째다 — 그건 한 트랙 안의 스위치였고, 이건 저장소 전체의 착지다. CLAUDE.md 신뢰성 절 기준으로 2번째 = 실수가 아니라 시스템 결함.  처방 후보: ㈎ 눈먼층 훅에 「queued run 이 N분 넘게 쌓였다」 갈래를 넣는다(러너 오프라인의 직접 증거 — `conclusion` 이 아니라 `status: queued` 의 나이를 잰다) ㈏ `작업가지.js --훅` 에 GitHub MCP/토큰 폴백을 넣어 클라우드에서도 열린 PR 을 센다 ㈐ SessionStart 가 「master 마지막 커밋 이후 경과」를 한 줄로 낸다(임계 넘으면 적색) ㈑ 도전안 — 원격 CI 를 머지 게이트에서 빼고 로컬 `test-ci.js` 4,765건 초록으로 머지, 원격은 사후 감사로 돌린다(러너는 리눅스 축 하나를 더 볼 뿐인데 지금은 그것이 전체를 인질로 잡는다). | 사태의 조건이 전부 사라졌다 — 실측 08-22 18:0x: 열린 PR **0건**(신고 당시 20개) · origin/master 마지막 커밋 **82초 전**(당시 16시간 정체) · 오늘 master 커밋 **71건** · queued run **0건**(당시 17시간 27분째 큐). 뿌리인 단일 장애점도 없어졌다 — 저장소가 public 이라 워크플로가 GitHub 호스트 러너를 집는다(F648·F664 와 같은 해소). 즉 처방 ㈑(원격 CI 를 인질에서 푼다)는 «짓지 않고» 달성됐다. ⚠ **관측 층은 여전히 없다** — ㈐「마지막 master 커밋 이후 N시간」을 내는 훅은 안 섰다(rot-check 의 「정체」는 손일 장부용이라 다른 축이다). 지금은 잴 대상이 없어 안 짓는다(CLAUDE.md v10.0 「실수를 고칠 때 장치를 새로 만들지 않는다 — 필요하면 유호님께 제안한다」). **다시 볼 조건**: 저장소를 private 로 돌릴 때 · 또는 master 가 반나절 넘게 안 움직이는 것이 관측될 때. 그때 ㈐ 한 줄을 유호님께 제안한다. |
