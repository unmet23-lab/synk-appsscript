| F557 | 2026-08-17 | 실수 | 배포 절차 4-B(①배포 검수)를 건너뛴 채 라이브에 밀었다 — 스킬을 위에서 아래로 따라 읽으면서도 그 단계 하나만 지나갔고, 아무것도 나를 멈추지 않았다.  실측 2026-08-17 (`local_091160b8` · [v9.249] talk_log 문법 판정 라이브 반영 · 배포 지문 `#fp:aab8b1de`): · 돌린 것 = 1 오염확인 · 2 구문검사 + `test-ci` 4,116건 fail 0 · 3 appsscript.json · 4 `deploy-security-check` + 판단층 검토(과녁 3/3 · `--대조` 종료 0) · 6 push · 7 `clasp push --force` → `배포판점검 --라이브` 도장 2/2. · 안 돌린 것 = **4-B `codex-review`**. 배포가 나간 «뒤» `--확인` 을 부르고서야 「이 배포 내용(#fp:aab8b1de)에 이종 검수 기록이 없다」를 봤다.  ■ 왜 개인 부주의로만 접으면 안 되나 — 그 단계만 «새는 방향이 통과»다    절차의 다른 전부는 실패하면 **멈춘다**: 구문검사 exit 1 · test-ci fail>0 · deploy-security-check exit 1 · clasp-guard 5불변식.    4-B 만 「검수 기록 없음 = 알림」이다(스킬의 표가 그렇게 설계했다고 밝혀 뒀다 — 자격증명 없는 환경이 늘 있어서 차단으로 짜면 그 환경이 통째로 죽는다 · F103).    그 판단 자체는 옳다. 문제는 **알림이 실제로 사람 눈에 닿는 자리에 없었다**는 것이다:    `clasp push` 출력에 검수 관련 문장이 한 줄도 안 섞여 나왔다(훅이 알림을 안 냈는지, 냈는데 clasp 출력에 안 섞였는지는 **안 갈랐다** — 다시 재려면 배포를 한 번 더 해야 해서 여기서는 못 잰다).    즉 내 쪽에서 관측된 사실은 하나다 — **배포가 나가는 순간까지 아무 말도 없었다.**  ■ 축이 F103 과 다르다    F103 = 「따를 수 없는 처방이 우회를 정상 통로로 만든다」. 여기 처방은 따를 수 있었다(약 5분 · 읽기 전용 · `--던지기` 로 세션 밖 실행까지 준비돼 있다).    여기서 샌 것은 처방이 아니라 **발화**다 — 스스로 발화하지 않는 장치는 안 돈다(이 저장소가 F547 에서 방금 배운 그 문장).  ■ 처방 후보 — 안 지었다(이 신고는 재료다)    ㉠ `clasp-guard` 의 「검수 기록 없음」 알림을 **차단이 아닌 채로 더 크게** 낸다 — deny 가 아니라 push 출력 앞에 붙는 경고 블록으로. ⚠ 대가 = 알림 피로. 자격증명 없는 환경에서 매번 뜬다.    ㉡ `/deploy` 스킬 7단계(clasp push) **직전에** `codex-review --확인` 을 절차로 박아 넣는다 — 조회는 즉시 끝난다(방금 재 보니 1초 안). 사람이 「기록 없음」을 **보고 나서** 던지든 넘어가든 고르게 한다.    ㉢ 배포 뒤 검수가 지적을 내면 그건 이미 라이브다 — 그 경우의 처방(되돌리기냐 후속 수리냐)이 지금 어디에도 안 적혀 있다. 적는다.    ⚠ 셋 중 무엇을 고르든 «자격증명 없는 환경에서 죽지 않을 것»이 조건이다.  ■ 이번 판의 처분    배포 뒤 `--commit 16f8af78 --던지기` 로 런 `검수-2026-08-17T0949-e06d3a` 를 띄웠다. 결과는 다음 세션이 훅으로 받는다.    🔴 그때까지 이 배포는 **①배포 검수 미검증**이다 — 「검수 통과」라고 적지 않는다. | ⏸ 안 고쳤다 — 신고만 하고 판정을 미룬다. 후보 셋(㉠clasp-guard 알림 키우기 ㉡스킬 7단계 직전 --확인 박기 ㉢배포 뒤 지적의 처방 적기)이 전부 배포 게이트의 발화·차단 범위를 건드리는데, 고르는 조건인 「자격증명 없는 환경에서 안 죽는가」·「알림 피로가 몇 건 늘어나는가」를 안 쟀다. 재기 전에 고르면 F103 을 반대 방향으로 재생산한다. 이번 판의 실물 처분은 끝냈다 — 검수 런 e06d3a 를 던졌고 배포는 「검수 미검증」으로 보고했다(local_091160b8). |
