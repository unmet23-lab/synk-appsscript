| F492 | 2026-08-16 | 마찰 | 회귀 3벌이 «산 세션들이 공유하는 상태 폴더»에 픽스처를 써서, 전량 실행이 세션 붐비는 시간대에만 무작위로 빨갰다 — 그리고 그 적색이 진짜 적색과 모양이 같아 아무도 판정을 못 냈다. 실측 2026-08-15(세션 376f6598 · HEAD 9acb122d 를 worktree 로 핀): 전량 3583건 중 fail 1(`tests/보드이관.test.js` 「산 주인이면 기다린다」)인데 같은 파일 단독 재실행은 54/54 초록. 기전 = 픽스처가 심장박동을 `handoff-store` 의 STATE_DIR 에 쓰는데 기본값이 `os.tmpdir()/synk-context-budget` — 이 머신의 모든 세션 훅이 공유한다. `sweep()` 이 `if (!j.at || 나이>ttl) unlink` 라 `at` 없는 박동은 나이와 무관하게 즉시 지워지고(픽스처는 `{touched: []}` 만 쓴다), 그러면 「산 주인」이 「죽었다」로 보인다. 반대 방향으로도 샜다 — 내 가짜 세션이 남의 실물 판정에 섞인다. 🔑 기전은 2026-08-11 에 이미 저장소에 적혀 있었다(`tests/이종검수.test.js` 「표식은 남이 청소하는 폴더에 두지 않는다」 — F333 재발 경로). 그 파일은 자기 표식만 옮겼고 박동을 쓰는 회귀 3벌은 그대로 남았다 — 프로즈는 자기 파일만 지킨다. 🔴 피해는 이 저장소 전체다: 적색의 주인을 못 찾으니 배포 게이트가 「못 믿는 적색」으로 서고(세션 3a029050 이 보드에 그렇게 적고 판정을 비웠다), 주인 없는 적색은 남의 배포를 막는다. | 공용 통로 하나 + 기계 회귀로 닫았다 — `8c152472`(수리+회귀 한 커밋) · `8e00b03c`(변이가 잡은 내 시험 결함 수리).  무엇이 막나: ① `tests/lib/상태격리.js` 신설 = **잘못 쓸 수 없는 통로**. env 를 세우는 일과 `handoff-store` require 를 한 함수에 묶었다 — STATE_DIR 이 모듈 최상위 const 라 require «뒤»에 env 를 세우면 조용히 옛 좌표를 쓰고 증상은 「통과」뿐이라, 순서를 틀릴 수 없게 만든 것이 이 파일의 존재 이유다. 이미 굳었으면 넘어가지 않고 고치는 법을 적어 던진다. ② 회귀 3벌(`보드이관`·`board-guard`·`보드껍데기정리`)을 그 통로로 갈아 끼웠다. ③ `tests/상태격리.test.js` 신설 = 4번째가 못 생기게 하는 검사. 탐지력은 픽스처가 지고(위반·정상·주석뿐 세 갈래) 실저장소엔 거짓양성만 묻는다. 예외 1건(`이종검수` — stateDir 을 **읽기만** 한다)은 사유를 적게 하고, 파일이 없어지거나 더는 안 쓰면 빨개진다.  잰 것: 상태격리 7/7 · 보드이관+보드껍데기정리 67/67 · board-guard 112/112 · **변이 10/10(구멍 0 · 미적용 0)** — 그중 3건이 「3벌을 옛 통로로 되돌린다」라 실저장소 검사가 되돌림을 실제로 잡는 것까지 값으로 못박았다.  🔑 변이가 내 시험 결함을 하나 잡았다(그래서 첫 판은 9/10 이었다): 순서 검사를 `/던졌다/` 로 재고 있었는데 실패 경로가 찍는 `안던졌다` 에 그 낱말이 들어 있어 변이가 통과했다 — **부정형이 긍정형을 품는 것은 한국어의 기본값**이라, 서로의 부분문자열이 아닌 두 표식(`THREW:`/`MUTANT-SILENT`)으로 갈랐다.  ⚠ 대가(맹점 ④): `코드만()` 은 문자열 리터럴을 안 지우므로 «본문에 예시 문자열을 든 파일»이 걸린다. 저장소에서 그런 파일은 판정기 자신 하나뿐이라 자기를 분모에서 뺐고 그 사유를 파일에 적었다. 여기서 문자열 스트리퍼를 새로 짜지 않았다 — 그게 F401 이 사본 7벌을 걷어낸 자리다. |
