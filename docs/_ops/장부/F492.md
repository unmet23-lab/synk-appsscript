| F492 | 2026-08-16 | 마찰 | 회귀 3벌이 «산 세션들이 공유하는 상태 폴더»에 픽스처를 써서, 전량 실행이 세션 붐비는 시간대에만 무작위로 빨갰다 — 그리고 그 적색이 진짜 적색과 모양이 같아 아무도 판정을 못 냈다. 실측 2026-08-15(세션 376f6598 · HEAD 9acb122d 를 worktree 로 핀): 전량 3583건 중 fail 1(`tests/보드이관.test.js` 「산 주인이면 기다린다」)인데 같은 파일 단독 재실행은 54/54 초록. 기전 = 픽스처가 심장박동을 `handoff-store` 의 STATE_DIR 에 쓰는데 기본값이 `os.tmpdir()/synk-context-budget` — 이 머신의 모든 세션 훅이 공유한다. `sweep()` 이 `if (!j.at || 나이>ttl) unlink` 라 `at` 없는 박동은 나이와 무관하게 즉시 지워지고(픽스처는 `{touched: []}` 만 쓴다), 그러면 「산 주인」이 「죽었다」로 보인다. 반대 방향으로도 샜다 — 내 가짜 세션이 남의 실물 판정에 섞인다. 🔑 기전은 2026-08-11 에 이미 저장소에 적혀 있었다(`tests/이종검수.test.js` 「표식은 남이 청소하는 폴더에 두지 않는다」 — F333 재발 경로). 그 파일은 자기 표식만 옮겼고 박동을 쓰는 회귀 3벌은 그대로 남았다 — 프로즈는 자기 파일만 지킨다. 🔴 피해는 이 저장소 전체다: 적색의 주인을 못 찾으니 배포 게이트가 「못 믿는 적색」으로 서고(세션 3a029050 이 보드에 그렇게 적고 판정을 비웠다), 주인 없는 적색은 남의 배포를 막는다. | |
