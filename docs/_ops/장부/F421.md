| F421 | 2026-08-14 | 마찰 | 배포 게이트가 «고칠 수 있는 사람이 원리상 없는» 자리를 물어, 저장소 전체의 `/deploy` 가 아무도 풀 수 없는 채로 막혔다 — 그리고 그것을 신고하는 글이 위반을 하나 더 만들었다 (#Q74 · 08-14 실측)  무슨 일이었나. `tests/운영자료.test.js` 의 「바탕화면을 번호로 가리키는 줄이 없다」는 좋은 가드다(08-14 신설 즉시 메모리 18곳 중 16곳이 딴 문서를 가리키던 것을 잡았다). 그런데 그 가드가 무는 범위에 **세션 보드 파일**이 들어 있고, 보드 줄은 doctrine 상 남이 못 고친다. 세션 `f8fd4dd0` 가 자기 줄에 번호 참조를 적고 죽자 이렇게 됐다:   · 그 한 줄이 `test-ci` 를 적색으로 만든다 → 원격 CI 는 Actions 소진으로 0건이라 이 로컬 모사가 유일한 진실 층 → `/deploy` 2단계가 그걸 게이트로 쓰므로 **모든 세션의 배포**가 막힌다.   · 그런데 고칠 통로가 **전부 닫혀 있다**: 남의 보드 파일 편집은 F250·F073 금지 · `board-move` 는 원칙 ⑦(미완 이관 금지)로 아카이브도 거절(`--dry` 실측) · 고칠 수 있는 유일한 사람은 죽었다.   ⇒ 「주인 없는 적색은 남의 배포를 막는다」의 극단형이다. 옛 글자(F298)와 같은 병인데 **한 칸 더 나쁘다** — 옛 글자는 아무나 고칠 수 있지만 이 자리는 고칠 자격 자체가 한 사람에게 묶여 있다. 그 사람이 죽으면 적색은 **무관한 트랙이 끝날 때까지** 안 풀린다.  🔴 두 번째 얼굴 — 신고가 위반을 만든다(F103 축). `71748fd0` 이 이 적색을 대기열 줄68 에 신고했는데, 무엇이 금지인지 쓰려면 그 표기를 그대로 적어야 해서 **신고문이 위반을 둘 더 만들었다**(샌 자리 1곳 → 2곳). 그 줄이 스스로 적은 「새는 자리는 한 곳뿐이다」가 **저장한 순간 거짓**이 됐다. 옛 처방문은 「갈래 + 이름으로 적어라」 한 줄뿐 — 그건 그 자리를 «가리키는» 사람에게만 맞는 말이고 위반을 «신고·인용»하는 사람에게는 따를 수 없는 처방이었다. 따를 수 없는 처방은 우회를 정상 통로로 만들고, 여기서 그 우회는 「검사에서 `보드/` 를 빼서 초록 만들기」다 — 그러면 «지금 사는 선언»이 통째로 검사 밖으로 나간다(아카이브를 빼는 이유와 정반대).  🔑 배운 것 (다음 가드를 세울 때 물을 것). 새 가드에 **「이 적색을 누가 풀 수 있나」를 함께 적는다.** 탐지 범위를 정할 때 「여기 위반이 생기면 고칠 자격이 있는 사람이 항상 존재하나」를 묻지 않으면, 규칙이 옳아도 저장소가 잠긴다. 특히 **소유권이 걸린 파일 클래스**(보드 줄·남의 작업본)를 **공용 배포 게이트**로 무는 조합이 급소다 — 그 둘은 각각은 옳고 곱해질 때만 잠근다. 그리고 금지 «표기»를 무는 가드는 **신고 통로를 같이 열어 둬야** 한다(안 그러면 장부에 신고 자체가 못 들어간다 — 옛글자 lib 이 이미 같은 이유로 마찰장부·인계문을 면제했는데, 그 교훈이 이 가드에는 안 옮겨 왔다. **한 축에서 배운 모양을 다른 축에 안 옮기면 그 축은 그대로 눈이 먼다**).  처분(`a8da95d6`): ㉠ 신고문 두 자리 수리(샌 자리 2→1) ㉡ **board-guard ⑧ 신설** — 보드에 번호 참조를 «쓰는 순간» deny(⑦ 옛글자와 같은 통로·같은 규약: `incoming` 만 잰다. 결과 파일 전체를 재면 남의 옛 위반이 내 무관한 편집을 영원히 막아 이 사고를 가드가 재현한다) ㉢ 판정 1벌 = `.claude/hooks/lib/번호참조.js`(훅·스위트가 거기서 읽는다) ㉣ 처방을 따를 수 있게 — 접두를 떼면 안 물리는 것을 처방문에 적고 회귀로 못박았다(백틱 감싸기는 우회라 일부러 안 뚫었다). 회귀 훅 6/6 + 픽스처 2칸 · 변이 4/4.  ⚠ 남은 것은 `f8fd4dd0.md:5` 한 줄이고 그건 `58406896` 몫이다(#Q49 승계자 · 메시지로 통보). 그 트랙이 끝나 `board-move` 로 이관되면 아카이브는 검사 제외라 자동으로 풀린다. **앞으로의 재발은 ⑧ 이 쓰는 순간에 막지만, 이미 죽은 세션이 남긴 줄은 ⑧ 이전에 쓰인 것이라 이 통로로는 못 막는다** — 그래서 이 신호는 「닫혔다」가 아니라 「한쪽만 닫혔다」로 남긴다. | 남은 반쪽(⑧ 이전에 쓰인 죽은 세션 f8fd4dd0 의 보드 줄)이 닫혔다 — 실측 08-14 `local_6c887e59`: ① `docs/_ops/보드/f8fd4dd0.md` 부재 — 92d50b27 의 「주인 죽은 줄 8→0」 이관으로 아카이브 착지(d8c17eb1 확인 · 아카이브는 검사 제외라 신호가 예고한 「자동으로 풀린다」 경로 그대로 · 그 전에 a79ec866 이 유호 상시 지시 08-14 를 근거로 :5 번호 표기를 이름으로 직접 수리해 둠) ② `tests/운영자료.test.js` 단독 실행 67/67 초록(fail 0 · skip 0) ③ 대기열 #Q74 줄 삭제 `7265029f`. 쓰는-순간 재발은 board-guard ⑧(a8da95d6)이 막고 있으니 이제 양쪽 다 닫혔다. |
