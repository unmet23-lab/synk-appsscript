| F607 | 2026-08-18 | 마찰 | 명단의 갈래② 라벨이 도구 둘에 「자식 인자」라고 적었는데 그 둘은 자식을 한 번도 안 부른다 — 따랐으면 손잡이 여섯이 죽었다 (SYNK-talk `tests/lib/CLI인자.js`)  📏 실측 2026-08-18(#Q113 갈래② 착수): `eval-run.js`·`eval-score.js` 는 `child_process` 를    **들여오지도 않는다**(전수 grep 0). 그런데 #Q112 의 추출기가 「내가 못 따라간 낱말 = 자식 인자」로    접어서, 두 도구의 손잡이 여섯(`--출력 --프롬프트 --표본 --맥락` · `--fixture --대조`)이 전부    자식 인자 칸에 들어갔고 명단이 그 라벨을 그대로 실었다.    ▶ 명단의 처방은 「자식 인자는 선언에 넣지 마라」다. 그대로 따랐으면      `node tools/eval-run.js --원격 --출력 evals/출력_v1api.json` — **그 도구 머리말이 인쇄하는 명령** —      이 게이트에 막혀 죽는다. 병을 만드는 처방이 F594·F599 에 이어 **세 번째**로 날 자리였다(F103).  🔑 왜 못 봤나: 추출기가 쫓는 도우미 꼴은 「argv 를 **첫 인자로 받는** 것」(`인자값(argv, '--조각')`)    하나뿐이었다. 실제로는 도우미가 argv 를 **클로저로 쥐는** 꼴이 셋 더 있었다 —    `eval-run 인자()`(함수 · process.argv 직접) · `eval-score 인자값()`(main(argv) 안 화살표) ·    `엔진뷰어 값()`(화살표 + 자기 자신을 spawn). 셋 다 「자기 낱말 0」쪽으로 샜고, 새는 방향이    ④에서는 「덜 요구한다」라 **검사는 늘 초록**이었다.  🔴 그리고 이 신고를 닫는 과정에서 ④의 사각 하나가 **변이로** 잡혔다: 「선언이 자식 인자를 받아 주나」를    재는 검사가, 자식후보를 «선언까지 접은» 결과에서 빼고 있었다. 즉 자식 인자를 선언에 넣는 순간    그 낱말이 자식후보에서 사라져 검사가 눈이 먼다 — 가드가 자기 전처리에 눈이 머는 그 모양이다.  ✅ 해소(셋을 한 벌로 · talk `823f9ed` 다음 커밋):    ① `자식부름()` — 자식을 안 부르면 그 파일의 `--낱말` 은 전부 자기 것이다(도우미 꼴을 쫓지 않는 바닥).    ② `닫아쥔도우미들()` — argv 를 클로저로 쥔 지역 도우미를 중괄호 짝으로 찾아 그 호출의 낱말을 센다.    ③ 자식후보를 **선언 접기 전** 증거(argv 비교)와 **lib 이 읽는 낱말**로 뺀다 —       그래야 `왕복시험 --운영승인`(골격이 읽는다)이 거짓양성으로 안 걸리고, `--적용`(진짜 자식 인자)은 걸린다.    회귀 신설 2(④ 반대 방향 · ③-5 운영승인) + 픽스처 5 · 변이 12/12 구멍0 · talk 2373/2373.    📏 넣기 전 전수 실측: 이 규칙들로 **지금 게이트를 진 도구 중 새로 빨개지는 것 0건**. | SYNK-talk tests/lib/CLI인자.js — 자식부름() + 닫아쥔도우미들() + 자식후보를 «선언 접기 전» 증거와 lib 낱말로 뺀다 · 회귀 2 신설 · 변이 12/12 |
