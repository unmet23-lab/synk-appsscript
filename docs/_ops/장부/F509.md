| F509 | 2026-08-16 | 실수 | 던진 «사실심문» 실탄 하나가 3시간을 태우고 「✅ 완주」의 얼굴로 장부에 앉았다 — 실패가 성공 칸에 들어가는 자리 넷.  실측(2026-08-16 · 런 사실심문-2026-08-16T0713-eb8a58 · 대상 = 창업대출 업종 한 칸 6주장 적대 검증): · 로그 44,936바이트 전부가 `HTTP 404 not_found` 였다 — **167회 · 180분**. 결과 0바이트. · 그런데 런 장부는 `상태:"완주"` + `종료코드:3` 이고, 세션시작 훅은 그걸   「✅ 완주했는데 아직 안 주운 것 2건 — 결과가 이미 나와 있다. 지금 읽어라」 로 냈다. · 읽기 전용 실측: `task.list` 200 · 계정 작업 3건 · 그 id(k6AipjgY5G92NjauryCcZm) 는 **목록에 없다**.   제목에 창업/업종/대출이 든 작업 0건 · 크레딧 소모 0. 즉 생성이 200을 냈는데 그 id 는 유령이었다.  새는 자리 넷(전부 「맞는 얼굴로 틀린 값」 — 지침 v9.2 맹점 ④): ① 던지기가 자기가 캔 작업 id 를 **한 번도 안 써 보고** 폴러에게 넘긴다. 확인은 읽기 1회·크레딧 0인데,    안 하면 3시간이 조용히 탄다. ② 폴러가 조회 오류를 전부 「일시 오류」로 본다. 404 not_found 는 재시도해도 영영 같은 답인데 167회 돌았다.    주석은 「일시 오류로 폴러를 죽이지 않는다」고 전제를 적어 뒀지만, 그 전제가 안 맞는 오류를 안 가른다. ③ 마누스가 세 종료 경로(stopped·waiting·상한초과) 전부를 `상태:'완주'` 로 적는다 —    codex-review 는 같은 자리를 `코드===0 ? '완주' : '실패'` 로 가른다. **같은 판정을 두 곳에 적어 갈라졌다.** ④ 검수런 판정기가 '완주'와 '실패'를 **같은 바구니**(완주미처분)에 넣는다.    그래서 ③만 고치면 아무것도 안 변한다 — 두 층을 같이 고쳐야 한다. 이게 급소다.  곁가지: 상한 초과 경로가 append-only 장부에 종결 행을 안 적는다(사실심문기록.jsonl 이 「던짐」 한 줄로 끝).  대가: 유호님이 답을 기다린 돈 사안(창업대출 금리·한도 6주장)의 적대 검증이 **안 됐는데 됐다고 보였다.** | tools/lib/검수런.js 상태이름()으로 판정을 한 곳에 모으고 판정()이 상태·종료코드 어긋남을 비관 판독(옛 기록도 고쳐 읽힘) + 갈래 실패미처분 신설 · 읽는 곳 셋(review-runs 훅·codex-review --런목록·마누스 --런목록)을 같은 커밋에 · 던지기가 캔 id 를 1회 확인(3시간→1초) · 폴러 회복불가인가()로 4xx 조기 종료 · 회귀 변이 8/8 구멍 0 — 543f7dd3 |
