| F386 | 2026-08-12 | 마찰 | 장부의 「살아있음」이 신호이길 그만뒀는데 아무도 못 봤다 — 세는 자는 있고 «닫는 자»가 없다. 실측 2026-08-12 (`local_e594c36e` 전수 대조): 도구가 세는 40건 중 **폴더 행 28~32건을 직접 열어 재서 11건이 이미 죽어 있었다**(닫음 완료). 그중 9건은 «남이 그 사이 고쳤거나 스스로 반증된» 것이다 — F377 은 형제 저장소에서 5/5 초록, F358 은 F365 가 같은 코드의 정상 스폰을 실측해 반증, F369 는 제 본문에 「`e9f62bc2` 로 착지시켰다」라고 적고도 열린 채였다. 즉 «고친 세션이 자기가 고친 줄을 안 닫는다» — `add` 는 CLAUDE.md 에 발동 조건이 있는데(「발생 즉시」) `resolve` 는 아무 데서도 안 불린다.  🔑 그 낡음이 **왜 안 보였나 — 트리거는 있는데 포화됐다.** `tools/rot-check.js:454` 가 주간 점검에서 「/evolve 발동 조건 도달 · 살아있는 마찰 N건(기준 2건)」을 열린 줄 **전량의 id 와 함께** 낸다. 지침이 말하는 「굵직한 마찰 신호 2건」은 **새로 난** 신호 둘인데 구현은 «누적 열림»을 세므로, 40건이 쌓인 지금 이 알림은 영구히 켜져 있다 — 켜져 있는 알림은 안 읽힌다(F370 과 같은 병: 침묵하도록 설계된 자리가 상시로 운다). 「2건 더 났다」와 「40건이 안 닫혔다」가 한 문장에서 같은 모양이 됐다.  🔴 **기계로 낡음을 가르는 축은 없다 — 세 가설을 재서 전부 죽였다**(다음 세션이 같은 탐지기를 다시 짓지 않도록 수치를 남긴다):   ① 백틱 경로(보드가 쓰는 그 추출기 `tools/lib/보드낡음.js` 의 `경로들`) → 살아있는 29줄 중 **재료가 있는 줄이 4**. 장부는 경로를 산문에 맨몸으로 쓴다.   ② 맨 경로(확장자 요구) + 「신고 뒤 그 자리를 건드린 커밋」 → 재료는 12/28 로 늘지만 **가르지 못한다**: 내가 독립으로 초록을 확인한 F377 이 「그 뒤 커밋 0」으로 나오고(고친 자리가 신고문이 지목한 파일이 아니었다), 뜬 4건 중 3건은 뜨거운 공유 파일 잡음이다(`작업대기열.md` +103 · `Code.js` +14 · `CLAUDE.md` +1). 재현율도 정밀도도 동시에 죽는다.   ③ `F0NN` 인용(이 저장소가 실제로 쓰는 관행 — 고친 자리 주석에 번호를 박는다) → 23/28 이 「인용됨」으로 뜨지만 대부분 인계문·보드 아카이브·심문결과가 번호를 **되뇐** 것이고 코드가 인용한 것은 2건뿐이다. 한 건(F327)은 HTML 색상 리터럴 오탐이었다.   → 보드가 되는 이유는 보드 줄이 **만들 산출물과 검수할 커밋**을 적기 때문이고(F374), 장부 줄은 «고장난 행동»을 적는다 — 그 고침은 어디에나 착지할 수 있다. 그래서 이 자리의 처방은 탐지기가 아니라 **주기적 전수 대조**다. 탐지기를 지으면 4/12 짜리 초록이 「덮었다」의 얼굴로 남는다(F207).  ⚠ **내가 안 잰 분모**: 도구가 세는 40건 중 폴더(`docs/_ops/장부/F*.md`) 행만 쟀다. 동결 아카이브 `docs/_ops/마찰신호.md` 에 **열린 행이 14건** 더 있고(F106·F110·F114·F115·F120·F130·F131·F139·F166·F174·F240·F246·F248·F249) 그쪽은 훨씬 오래됐다 — 폴더 행은 전부 08-08~08-12 인데 아카이브 행은 그보다 앞이다. 「초록」이 아니라 «안 쟀다»로 적는다.  ▶ 처방 후보(하나를 고르는 것은 다음 트랙): ㉠ rot-check 의 /evolve 문장을 «지침 이력의 마지막 개정 뒤에 난 행»으로 세게 한다(그게 지침이 말하는 「2건」의 뜻이다 · 재료 = `docs/지침_이력.md` 날짜) ㉡ 같은 자리에서 「오래 열린 N건 — 닫혔는지 대조하라」를 갈라 낸다(나이는 기계가 정확히 안다 · 거짓양성 0) ㉢ 아카이브 14건 전수 대조. 🚫 ①②③ 탐지기 재신축. | |
