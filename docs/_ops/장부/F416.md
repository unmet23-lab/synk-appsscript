| F416 | 2026-08-14 | 마찰 | 가드가 자기 처방의 «대가»를 손으로 적어 둬, 대상이 자라자 정확히 반대를 말했다 — design-guard 가 「DESIGN.md 전문(≈4.5KB — read-budget 무료 구간)」이라 권하는 동안 실물은 17,507B 로 read-budget 「큰 파일」(16,384B)을 넘어 있었다. 예산이 막는 읽기를 «무료»라 부른 것이라 따를 수 없는 처방이 되고(F103), 그 사이 tests/디자인가드.test.js 는 주인 없는 적색으로 남의 배포를 막았다. 급소는 「파일이 자랐다」가 아니라 「장치가 맞는 얼굴로 틀린 값을 냈다」다(CLAUDE.md 맹점 ④) — 같은 줄의 「색 19」도 킷이 23색이 된 뒤로 낡아 있었다. 한 줄에 낡은 단정이 셋이었다. | design-guard 의 그 문장을 파생값으로 바꿨다 — 크기는 statSync 로 지금 재고 BIG 은 read-budget 소스에서 읽는다(같은 값을 두 곳에 안 적는다 · 회귀와 같은 통로). 문턱을 넘으면 문장이 「무료」에서 「예산에 걸린다 + 처분은 숫자 올리기가 아니다」로 뒤집힌다. 회귀 2건(지금 크기와 일치 · 초과 픽스처) 변이 2/2. 새 장치 0개 — 기존 훅의 낡은 단정을 고친 것뿐이다. DESIGN.md 는 중복 접기로 15,511B (c93f8f1d). |
