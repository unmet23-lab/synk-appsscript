| F526 | 2026-08-17 | 마찰 | 회귀가 «행동»이 아니라 «표기»를 재서 거짓 적색을 낸다 — F518 과 같은 병의 두 번째 자리다(이틀 새 둘).  실측 2026-08-17 (`local_70ba3cd3` · 두 저장소 master 초록 여부를 얼린 트리에서 재던 중 발각): `tests/소스검사통로.test.js` 의 「as 판이 talk 판과 «바이트로» 같다」가 얼린 워크트리에서만 빨갰다. 가른 것 셋 — ①산 as 와 산 talk 은 **바이트로 동일**(SHA256 일치·12114) ②얼린 판만 12317·CRLF 203 ③`core.autocrlf=true` 라 **새 체크아웃만** LF→CRLF 로 바뀐다. 내용은 한 글자도 안 갈렸다.  ■ 왜 신호인가 — 지침 v9.2 「가드·회귀의 네 맹점」 ① 의 재발이고, F518 과 **정확히 같은 축**이다:   F518 = testID 하나(표기)가 원문 정규식을 깨뜨렸다 · 이것 = 줄끝(표기)이 바이트 비교를 깨뜨렸다.   둘 다 «행동은 그대로인데 빨간»이고, 둘 다 남의 배포 게이트를 막는 자리다.   🔴 그리고 이 저장소는 **이 함정을 이미 알고 있었다** — 바로 옆 `이해대장.test.js` 가 F364 로   줄끝을 접어 뒀다. 즉 처방이 한 파일에만 서 있었고, 옆 파일은 그대로 노출돼 있었다.   같은 절차의 2번째다 — CLAUDE.md 대로라면 실수가 아니라 시스템 결함으로 다뤄야 한다.  ■ 수리는 했다: `d02ca1c7` — 줄끝만 접고 내용 비교는 그대로. 탐지력 픽스처 2벌로 못박았고   (줄끝만 다른 판은 접히고, 내용이 갈리면 접어도 잡힌다), 거짓 적색이 나던 **그 CRLF 트리에서**   13/13 초록을 확인했다. 실저장소 13/13.  ■ 안 한 것 (판정 재료):   ㉠ 같은 모양이 저장소에 몇 개인지 **안 셌다** — F518 때 잰 것은 「태그-속성 인접」 한 갈래뿐이고,      「정확일치 문자열·바이트 비교·들여쓰기 의존」 계열 전체의 분모는 여전히 0 이다.   ㉡ 장치는 안 지었다. 🔴 **정정(11:15)** — 처음 적은 사유 「하네스 봉인 · 「새 장치 0」」은 **낡은 표식이다**:      봉인은 08-16 밤 유호 확정 「봉인을 유지하지말자」로 **해제**됐고 하네스 검토·수리·발전은 세션 자율이다      (정본 = memory `harness-deletion-verdict-0816` §「봉인 해제」). 참 사유 = **분모를 안 재서**다 — ㉠ 이 열린 채로      장치를 지으면 어디를 겨눌지 모르는 장치가 된다. 순서는 ㉠(세기) → 그 다음이 장치다. 이 신고는 기록이다.   ⚠ 대가를 적는다: 접기는 «줄끝이 실제로 중요한» 검사에는 못 쓴다. 지금 이 검사는 목록 파생 여부를      보는 것이라 무관하지만, 규약으로 넓히면 그 구분이 필요해진다. | ⏸ ㉠(분모 세기)를 **절반 갚았다** — 그래서 닫지 않고, 지금 아는 수를 적어 둔다(`local_d8ce7307` · 2026-08-17).  ■ 무엇을·어느 층을 쟀나 (재기 전에 정했다 — CLAUDE.md 「잴 수 있는 것은 잰다」 단서)   과녁 = 검사·도구가 텍스트를 대조하는데 **행동이 아니라 표기**가 갈리면 판정이 뒤집히는 자리.   층 = `tools/` · `tools/lib/` · `tests/` · `.claude/hooks/` 의 325벌. 갈래 넷으로 나눠 셌다.   방법 = 스크래치패드 분류기(주석·문자열 프로즈를 걷어내고 **파일마다 git 헬퍼를 먼저 해석**한다).  ■ 잰 것 — 갈래 ②④는 분모가 섰다   ② 경로 이스케이프(quotepath) … **64 = 안전 51 + 「비었나」만 3 + 노출 10**      노출 10을 손으로 재검하니 **실질 1**이다(나머지 9는 거짓양성):        · `friction.js` 4건 = `/^\?\?/m` 는 상태 접두를 보고, `.trim()` 은 «빈지»만 본다 — 이스케이프가 판정을 못 바꾼다.        · `friction.js:209` 는 주석이 스스로 근거를 적어 뒀다 — 폴더명이 깨져도 `F0NN.md` 는 ASCII 라 정규식이 산다.        · 나머지는 픽스처가 ASCII 인 시험층.      🔴 실질 1 = `tools/인용검사.js` — 그리고 **두 과녁 중 «자동으로 도는» 쪽만** 눈이 멀어 있었다:         · `작업본문서들()` = 기본 과녁 + `--스테이징`(**pre-commit 이 부르는 그 길**) … git 출력을 읽어 **멀었다**.           실측: 한글 경로 .md 를 8벌 건드린 작업본에서 **0/8** 을 봤다(`quotepath=false` 로 재면 8/8).         · `전량문서들()` = `--전량` 보고용 … `readdirSync` 로 디스크를 훑는다 — **원래 멀쩡했다**(A/B 로 확인).         🔑 그래서 아무도 못 봤다 — 사람이 손으로 부르는 모드는 늘 맞는 수를 냈고(121벌),            조용히 도는 강제 모드만 틀렸다. 맹점 ④(맞는 얼굴로 틀린 값)의 교과서적 모양이다.         📏 크기: 면제를 뺀 검사 과녁 168벌 중 **143벌이 비ASCII 경로**다 — 그 문서를 건드린 커밋은            전부 이 가드를 그냥 통과했다(`docs/SYNK_철학.md` 같은 정본이 그 안에 있다).         수리+회귀 = PR #35 (변이 3/3 · 구멍 0 · 미적용 0 · 기준선 초록 분모 1).   ④ 비ASCII 식별자가 OS 경계에 닿는 자리(F501 계열) … **18건 — 전부 «잠재»다(지금은 안 터진다)**      실측으로 양쪽을 다 확인했다: Node→Node 전달은 **통과**(execve 는 셸 파서를 안 지난다),      `bash -c 'export SYNK_대장_정본=x'` 는 **not a valid identifier** 로 거절.      그리고 이 18건을 **셸에서 세우는 자리는 0이다**(`.sh`·`.yml`·워크플로 히트 0 — 전부 `.js` 와 산문).      ⚠ F501 은 형제 저장소 2건만 알고 있었다 — 이 저장소에 18건이 더 있다는 것이 새 수다.  ■ 아직 0인 것 — **그래서 닫지 않는다**   ① 줄끝(CRLF) … 후보 **32건을 「세기만」 했다. 손 재검 0.** 하필 이 갈래가 F526 자신의 갈래다.   ③ 공백·들여쓰기 의존 … 후보 1건(`tests/토큰실측.test.js:82`), 재검 0.   즉 「정확일치 문자열·바이트 비교」 계열의 참 분모는 여전히 미측정이다. 지금 닫으면 그 32건이   «세어졌다»는 얼굴로 사라진다 — 그게 이 신고가 애초에 겨눈 실패 모양이다.  ■ 장치 판정 (㉠ 다음이 장치라 했으니, 지금 아는 만큼만 적는다)   갈래 ②에 한해서는 **새 장치보다 린트 한 줄이 맞다** — 64자리 중 51이 이미 제대로 하고 있었다.   결함은 「공용 통로가 없어서」가 아니라 **헬퍼 하나가 규약을 안 따라서**였다. 즉 F527 ㉠(손 측정용   공용 함수를 짓자)의 전제는 이 수가 반박한다: 통로는 이미 있고 안 쓴 자리가 하나였다.   ▶ 다음 세션이 집을 것: ①갈래 ① 32건 손 재검 → 그 수를 보고 린트/가드를 정한다(순서 뒤집지 말 것). |
