| F526 | 2026-08-17 | 마찰 | 회귀가 «행동»이 아니라 «표기»를 재서 거짓 적색을 낸다 — F518 과 같은 병의 두 번째 자리다(이틀 새 둘).  실측 2026-08-17 (`local_70ba3cd3` · 두 저장소 master 초록 여부를 얼린 트리에서 재던 중 발각): `tests/소스검사통로.test.js` 의 「as 판이 talk 판과 «바이트로» 같다」가 얼린 워크트리에서만 빨갰다. 가른 것 셋 — ①산 as 와 산 talk 은 **바이트로 동일**(SHA256 일치·12114) ②얼린 판만 12317·CRLF 203 ③`core.autocrlf=true` 라 **새 체크아웃만** LF→CRLF 로 바뀐다. 내용은 한 글자도 안 갈렸다.  ■ 왜 신호인가 — 지침 v9.2 「가드·회귀의 네 맹점」 ① 의 재발이고, F518 과 **정확히 같은 축**이다:   F518 = testID 하나(표기)가 원문 정규식을 깨뜨렸다 · 이것 = 줄끝(표기)이 바이트 비교를 깨뜨렸다.   둘 다 «행동은 그대로인데 빨간»이고, 둘 다 남의 배포 게이트를 막는 자리다.   🔴 그리고 이 저장소는 **이 함정을 이미 알고 있었다** — 바로 옆 `이해대장.test.js` 가 F364 로   줄끝을 접어 뒀다. 즉 처방이 한 파일에만 서 있었고, 옆 파일은 그대로 노출돼 있었다.   같은 절차의 2번째다 — CLAUDE.md 대로라면 실수가 아니라 시스템 결함으로 다뤄야 한다.  ■ 수리는 했다: `d02ca1c7` — 줄끝만 접고 내용 비교는 그대로. 탐지력 픽스처 2벌로 못박았고   (줄끝만 다른 판은 접히고, 내용이 갈리면 접어도 잡힌다), 거짓 적색이 나던 **그 CRLF 트리에서**   13/13 초록을 확인했다. 실저장소 13/13.  ■ 안 한 것 (판정 재료):   ㉠ 같은 모양이 저장소에 몇 개인지 **안 셌다** — F518 때 잰 것은 「태그-속성 인접」 한 갈래뿐이고,      「정확일치 문자열·바이트 비교·들여쓰기 의존」 계열 전체의 분모는 여전히 0 이다.   ㉡ 장치는 안 지었다. 🔴 **정정(11:15)** — 처음 적은 사유 「하네스 봉인 · 「새 장치 0」」은 **낡은 표식이다**:      봉인은 08-16 밤 유호 확정 「봉인을 유지하지말자」로 **해제**됐고 하네스 검토·수리·발전은 세션 자율이다      (정본 = memory `harness-deletion-verdict-0816` §「봉인 해제」). 참 사유 = **분모를 안 재서**다 — ㉠ 이 열린 채로      장치를 지으면 어디를 겨눌지 모르는 장치가 된다. 순서는 ㉠(세기) → 그 다음이 장치다. 이 신고는 기록이다.   ⚠ 대가를 적는다: 접기는 «줄끝이 실제로 중요한» 검사에는 못 쓴다. 지금 이 검사는 목록 파생 여부를      보는 것이라 무관하지만, 규약으로 넓히면 그 구분이 필요해진다. | ㉠(분모 세기) 실측 종결 + 뿌리 하나 제거 — `tests/_engine-source.js` 의 `engineSource()` 가 줄끝 표기를 접는다(222f66ba·e71ea989 · 회귀 tests/엔진소스표기.test.js 9건 · 변이 7/7 구멍 0). ■ 잰 법: 얼린 detached 트리(origin/master@05adebf3 · autocrlf=false 로 LF 고정 · 형제 정션)에 **행동을 한 글자도 안 바꾸는 변형**만 얹고 전량 재실행. 판마다 원복 검산 + 「변형이 실제로 얹혔나」를 git status 로 확인(조용한 미적용은 「새 적색 0」과 모양이 같다). ■ 답 — 분모 3895(기준선 실패 0): 줄끝 LF→CRLF 1363파일 → 새 적색 **0** · 줄 끝 공백 372파일 → **40** · 머리 주석 1줄 372파일 → **3**. 🔑 P1 이 0인 것이 급소다 — 안전해서가 아니라 **2026-08-03 에 이미 터져서**(리베이스 직후 수집.test.js 9건 동시 사망) 호출부 세 곳이 각자 `.replace(/\r\n/g,'\n')` 을 붙였기 때문이다. 그 처방은 **축 하나만** 막았고 형제 축(줄끝 공백)은 열린 채 40 중 **27건**을 「섹션 끝 표식을 찾지 못함」으로 죽였다(수집 23·소급성 4). 즉 F518·F526 과 같은 병의 **네 번째** 자리이고, 셋 다 처방이 호출부별이었다. ■ 처방: 호출부마다 고치는 대신 이음매가 진다 — 그 파일 머리말이 이미 「표식 기반 검사 195건이 이 통로를 지난다」고 적어 두고 있었다. 손 접기 3곳은 거뒀고(같은 판정이 두 곳이면 갈라진다) 「옛 통로 금지」 검사를 붙였다. ■ 검증: 고친 판에 같은 P2 를 다시 얹어 재측정 — 총 실패 **40 → 12**, 「섹션 끝 표식」 계열 **27 → 0**(전건). 남은 12 중 1건은 버전 선점(내 가지가 origin 보다 뒤처져 나는 것 · 무변형 판에서도 난다 · 내 변경 무관). ■ 🔴 안 닫은 것 11건 — **이 값이 다음 세션의 분자다**(다시 「분모 0」에서 시작하지 않게): ㉮ 생성물 대조 5(대장동봉 3·이해대장 1·펠트문서 1) — 생성기 소스가 갈리면 산출물이 실제로 낡는 자리라 거짓 적색이라 부르기 어렵다. 단 `tests/대장동봉.test.js:71` 은 그 검사 문구가 「줄끝만 CRLF 인 같은 화면을…」이라 **CRLF 축만 접고 공백 축은 안 접은** 같은 반쪽 처방이 `tools/대장동봉검사.js` 에도 있다는 뜻이다. ㉯ 다른 이음매의 표식 절단 4(CI모사 2·리포트카드공개 2) — 같은 병, 각자 파일을 직접 읽는 통로. ㉰ 부정 단언 뒤집힘 1(수집 :790 계열). ㉱ 내 변형의 인공물 1(as/talk 을 한쪽만 변형해서 난 것 — 분자 아님). ■ ⚠ 이 측정이 «못» 보는 방향: 패턴이 표기 때문에 안 맞아 **조용히 초록**이 되는 쪽은 적색이 안 나므로 여기서 원리적으로 안 보인다. 그 축은 변이(tools/변이.js)가 지는 자리다. ■ 곁가지 실측(처방 후보 · 안 켰다): `.gitattributes` 에 `* text=auto eol=lf` 를 켜는 대가는 **0 파일**이다 — CRLF 작업본에서 켜고 `git update-index --really-refresh` 로 stat 캐시를 무효화해도 더러워진 파일은 자기 자신 1건뿐이었고, 재체크아웃은 CR 109→0 이 됐다. 그 파일 주석의 「저장소 전체 재정규화는 다른 세션의 작업본을 흔든다」는 `git add --renormalize` 얘기지 속성 한 줄이 아니었다 — 그 문장 때문에 근본 처방이 경로별로 세 번(훅·계약·이해대장) 미뤄졌다. **지금 안 켠 이유**: P1 이 0이라 당장 지킬 것이 없고, 내 측정은 깨끗한 트리에서만 했다(16세션이 도는 지금 더러운 작업본에서의 대가는 미측정). 조용한 창에서 켤 수 있게 값만 남긴다. |
