|  F526 | 2026-08-17 | 마찰 | 회귀가 «행동»이 아니라 «표기»를 재서 거짓 적색을 낸다 — F518 과 같은 병의 두 번째 자리다(이틀 새 둘).  실측 2026-08-17 (`local_70ba3cd3` · 두 저장소 master 초록 여부를 얼린 트리에서 재던 중 발각): `tests/소스검사통로.test.js` 의 「as 판이 talk 판과 «바이트로» 같다」가 얼린 워크트리에서만 빨갰다. 가른 것 셋 — ①산 as 와 산 talk 은 **바이트로 동일**(SHA256 일치·12114) ②얼린 판만 12317·CRLF 203 ③`core.autocrlf=true` 라 **새 체크아웃만** LF→CRLF 로 바뀐다. 내용은 한 글자도 안 갈렸다.  ■ 왜 신호인가 — 지침 v9.2 「가드·회귀의 네 맹점」 ① 의 재발이고, F518 과 **정확히 같은 축**이다:   F518 = testID 하나(표기)가 원문 정규식을 깨뜨렸다 · 이것 = 줄끝(표기)이 바이트 비교를 깨뜨렸다.   둘 다 «행동은 그대로인데 빨간»이고, 둘 다 남의 배포 게이트를 막는 자리다.   🔴 그리고 이 저장소는 **이 함정을 이미 알고 있었다** — 바로 옆 `이해대장.test.js` 가 F364 로   줄끝을 접어 뒀다. 즉 처방이 한 파일에만 서 있었고, 옆 파일은 그대로 노출돼 있었다.   같은 절차의 2번째다 — CLAUDE.md 대로라면 실수가 아니라 시스템 결함으로 다뤄야 한다.  ■ 수리는 했다: `d02ca1c7` — 줄끝만 접고 내용 비교는 그대로. 탐지력 픽스처 2벌로 못박았고   (줄끝만 다른 판은 접히고, 내용이 갈리면 접어도 잡힌다), 거짓 적색이 나던 **그 CRLF 트리에서**   13/13 초록을 확인했다. 실저장소 13/13.  ■ 안 한 것 (판정 재료):   ㉠ 같은 모양이 저장소에 몇 개인지 **안 셌다** — F518 때 잰 것은 「태그-속성 인접」 한 갈래뿐이고,      「정확일치 문자열·바이트 비교·들여쓰기 의존」 계열 전체의 분모는 여전히 0 이다.   ㉡ 장치는 안 지었다. 🔴 **정정(11:15)** — 처음 적은 사유 「하네스 봉인 · 「새 장치 0」」은 **낡은 표식이다**:      봉인은 08-16 밤 유호 확정 「봉인을 유지하지말자」로 **해제**됐고 하네스 검토·수리·발전은 세션 자율이다      (정본 = memory `harness-deletion-verdict-0816` §「봉인 해제」). 참 사유 = **분모를 안 재서**다 — ㉠ 이 열린 채로      장치를 지으면 어디를 겨눌지 모르는 장치가 된다. 순서는 ㉠(세기) → 그 다음이 장치다. 이 신고는 기록이다.   ⚠ 대가를 적는다: 접기는 «줄끝이 실제로 중요한» 검사에는 못 쓴다. 지금 이 검사는 목록 파생 여부를      보는 것이라 무관하지만, 규약으로 넓히면 그 구분이 필요해진다. | ⏸ **두 세션이 각자 ㉠을 쟀다** — 장부의 ▶ 에 점유층이 없어서다(F523 이 신고한 그 결함이 그대로 났다). 겹치지 않으므로 덮지 않고 합친다: [A]는 **정적 분류**(무엇이 몇 벌인가), [B]는 **행동 측정**(무엇이 실제로 빨개지는가). 그리고 [A]가 남긴 ▶(「갈래 ① 줄끝 32건 손 재검」)이 곧 [B]가 잰 축이라 **그 ▶ 는 갚혔다** — 손 재검이 아니라 행동으로. 아직 ⏸ 인 이유는 아래 «남은 것»이 실재하기 때문이다(지금 닫으면 그것들이 「세어졌다」는 얼굴로 사라진다).

■■ [A] `local_d8ce7307` — 정적 분류 ■■ ㉠(분모 세기)를 **절반 갚았다** — 그래서 닫지 않고, 지금 아는 수를 적어 둔다(`local_d8ce7307` · 2026-08-17).  ■ 무엇을·어느 층을 쟀나 (재기 전에 정했다 — CLAUDE.md 「잴 수 있는 것은 잰다」 단서)   과녁 = 검사·도구가 텍스트를 대조하는데 **행동이 아니라 표기**가 갈리면 판정이 뒤집히는 자리.   층 = `tools/` · `tools/lib/` · `tests/` · `.claude/hooks/` 의 325벌. 갈래 넷으로 나눠 셌다.   방법 = 스크래치패드 분류기(주석·문자열 프로즈를 걷어내고 **파일마다 git 헬퍼를 먼저 해석**한다).  ■ 잰 것 — 갈래 ②④는 분모가 섰다   ② 경로 이스케이프(quotepath) … **64 = 안전 51 + 「비었나」만 3 + 노출 10**      노출 10을 손으로 재검하니 **실질 1**이다(나머지 9는 거짓양성):        · `friction.js` 4건 = `/^\?\?/m` 는 상태 접두를 보고, `.trim()` 은 «빈지»만 본다 — 이스케이프가 판정을 못 바꾼다.        · `friction.js:209` 는 주석이 스스로 근거를 적어 뒀다 — 폴더명이 깨져도 `F0NN.md` 는 ASCII 라 정규식이 산다.        · 나머지는 픽스처가 ASCII 인 시험층.      🔴 실질 1 = `tools/인용검사.js` — 그리고 **두 과녁 중 «자동으로 도는» 쪽만** 눈이 멀어 있었다:         · `작업본문서들()` = 기본 과녁 + `--스테이징`(**pre-commit 이 부르는 그 길**) … git 출력을 읽어 **멀었다**.           실측: 한글 경로 .md 를 8벌 건드린 작업본에서 **0/8** 을 봤다(`quotepath=false` 로 재면 8/8).         · `전량문서들()` = `--전량` 보고용 … `readdirSync` 로 디스크를 훑는다 — **원래 멀쩡했다**(A/B 로 확인).         🔑 그래서 아무도 못 봤다 — 사람이 손으로 부르는 모드는 늘 맞는 수를 냈고(121벌),            조용히 도는 강제 모드만 틀렸다. 맹점 ④(맞는 얼굴로 틀린 값)의 교과서적 모양이다.         📏 크기: 면제를 뺀 검사 과녁 168벌 중 **143벌이 비ASCII 경로**다 — 그 문서를 건드린 커밋은            전부 이 가드를 그냥 통과했다(`docs/SYNK_철학.md` 같은 정본이 그 안에 있다).         수리+회귀 = PR #35 (변이 3/3 · 구멍 0 · 미적용 0 · 기준선 초록 분모 1).   ④ 비ASCII 식별자가 OS 경계에 닿는 자리(F501 계열) … **18건 — 전부 «잠재»다(지금은 안 터진다)**      실측으로 양쪽을 다 확인했다: Node→Node 전달은 **통과**(execve 는 셸 파서를 안 지난다),      `bash -c 'export SYNK_대장_정본=x'` 는 **not a valid identifier** 로 거절.      그리고 이 18건을 **셸에서 세우는 자리는 0이다**(`.sh`·`.yml`·워크플로 히트 0 — 전부 `.js` 와 산문).      ⚠ F501 은 형제 저장소 2건만 알고 있었다 — 이 저장소에 18건이 더 있다는 것이 새 수다.  ■ 아직 0인 것 — **그래서 닫지 않는다**   ① 줄끝(CRLF) … 후보 **32건을 「세기만」 했다. 손 재검 0.** 하필 이 갈래가 F526 자신의 갈래다.   ③ 공백·들여쓰기 의존 … 후보 1건(`tests/토큰실측.test.js:82`), 재검 0.   즉 「정확일치 문자열·바이트 비교」 계열의 참 분모는 여전히 미측정이다. 지금 닫으면 그 32건이   «세어졌다»는 얼굴로 사라진다 — 그게 이 신고가 애초에 겨눈 실패 모양이다.  ■ 장치 판정 (㉠ 다음이 장치라 했으니, 지금 아는 만큼만 적는다)   갈래 ②에 한해서는 **새 장치보다 린트 한 줄이 맞다** — 64자리 중 51이 이미 제대로 하고 있었다.   결함은 「공용 통로가 없어서」가 아니라 **헬퍼 하나가 규약을 안 따라서**였다. 즉 F527 ㉠(손 측정용   공용 함수를 짓자)의 전제는 이 수가 반박한다: 통로는 이미 있고 안 쓴 자리가 하나였다.   ▶ 다음 세션이 집을 것: ①갈래 ① 32건 손 재검 → 그 수를 보고 린트/가드를 정한다(순서 뒤집지 말 것).

■■ [B] `local_4e976ff7` — 행동 측정 + 뿌리 제거 ■■ ㉠(분모 세기) 실측 종결 + 뿌리 하나 제거 — `tests/_engine-source.js` 의 `engineSource()` 가 줄끝 표기를 접는다(222f66ba·e71ea989 · 회귀 tests/엔진소스표기.test.js 9건 · 변이 7/7 구멍 0). ■ 잰 법: 얼린 detached 트리(origin/master@05adebf3 · autocrlf=false 로 LF 고정 · 형제 정션)에 **행동을 한 글자도 안 바꾸는 변형**만 얹고 전량 재실행. 판마다 원복 검산 + 「변형이 실제로 얹혔나」를 git status 로 확인(조용한 미적용은 「새 적색 0」과 모양이 같다). ■ 답 — 분모 3895(기준선 실패 0): 줄끝 LF→CRLF 1363파일 → 새 적색 **0** · 줄 끝 공백 372파일 → **40** · 머리 주석 1줄 372파일 → **3**. 🔑 P1 이 0인 것이 급소다 — 안전해서가 아니라 **2026-08-03 에 이미 터져서**(리베이스 직후 수집.test.js 9건 동시 사망) 호출부 세 곳이 각자 `.replace(/\r\n/g,'\n')` 을 붙였기 때문이다. 그 처방은 **축 하나만** 막았고 형제 축(줄끝 공백)은 열린 채 40 중 **27건**을 「섹션 끝 표식을 찾지 못함」으로 죽였다(수집 23·소급성 4). 즉 F518·F526 과 같은 병의 **네 번째** 자리이고, 셋 다 처방이 호출부별이었다. ■ 처방: 호출부마다 고치는 대신 이음매가 진다 — 그 파일 머리말이 이미 「표식 기반 검사 195건이 이 통로를 지난다」고 적어 두고 있었다. 손 접기 3곳은 거뒀고(같은 판정이 두 곳이면 갈라진다) 「옛 통로 금지」 검사를 붙였다. ■ 검증: 고친 판에 같은 P2 를 다시 얹어 재측정 — 총 실패 **40 → 12**, 「섹션 끝 표식」 계열 **27 → 0**(전건). 남은 12 중 1건은 버전 선점(내 가지가 origin 보다 뒤처져 나는 것 · 무변형 판에서도 난다 · 내 변경 무관). ■ 🔴 안 닫은 것 11건 — **이 값이 다음 세션의 분자다**(다시 「분모 0」에서 시작하지 않게): ㉮ 생성물 대조 5(대장동봉 3·이해대장 1·펠트문서 1) — 생성기 소스가 갈리면 산출물이 실제로 낡는 자리라 거짓 적색이라 부르기 어렵다. 단 `tests/대장동봉.test.js:71` 은 그 검사 문구가 「줄끝만 CRLF 인 같은 화면을…」이라 **CRLF 축만 접고 공백 축은 안 접은** 같은 반쪽 처방이 `tools/대장동봉검사.js` 에도 있다는 뜻이다. ㉯ 다른 이음매의 표식 절단 4(CI모사 2·리포트카드공개 2) — 같은 병, 각자 파일을 직접 읽는 통로. ㉰ 부정 단언 뒤집힘 1(수집 :790 계열). ㉱ 내 변형의 인공물 1(as/talk 을 한쪽만 변형해서 난 것 — 분자 아님). ■ ⚠ 이 측정이 «못» 보는 방향: 패턴이 표기 때문에 안 맞아 **조용히 초록**이 되는 쪽은 적색이 안 나므로 여기서 원리적으로 안 보인다. 그 축은 변이(tools/변이.js)가 지는 자리다. ■ 곁가지 실측(처방 후보 · 안 켰다): `.gitattributes` 에 `* text=auto eol=lf` 를 켜는 대가는 **0 파일**이다 — CRLF 작업본에서 켜고 `git update-index --really-refresh` 로 stat 캐시를 무효화해도 더러워진 파일은 자기 자신 1건뿐이었고, 재체크아웃은 CR 109→0 이 됐다. 그 파일 주석의 「저장소 전체 재정규화는 다른 세션의 작업본을 흔든다」는 `git add --renormalize` 얘기지 속성 한 줄이 아니었다 — 그 문장 때문에 근본 처방이 경로별로 세 번(훅·계약·이해대장) 미뤄졌다. **지금 안 켠 이유**: P1 이 0이라 당장 지킬 것이 없고, 내 측정은 깨끗한 트리에서만 했다(16세션이 도는 지금 더러운 작업본에서의 대가는 미측정). 조용한 창에서 켤 수 있게 값만 남긴다.

■■ 남은 것(다음 세션의 분자 — 다시 「분모 0」에서 시작하지 않게) ■■ ⒜ [B]가 남긴 11건(생성물 대조 5 · 다른 이음매의 표식 절단 4 · 부정 단언 뒤집힘 1 · 측정 인공물 1). ⒝ [A]의 갈래 ③(공백·들여쓰기 의존) 후보 1건 손 재검 — 단 [B]의 P2 가 그 축을 **행동으로** 이미 훑었으므로 재검 대상은 「P2 에 안 걸리는 자리」로 좁혀졌다. ⒞ [A]의 갈래 ② 수리는 PR #35 (그 세션 몫 · 여기서 안 건드린다). ⒟ 두 측정 다 **조용히 초록이 되는 방향**은 못 본다 — 그 축은 변이(tools/변이.js)가 진다.

■■ [C] `local_57d592c2` — ⒜ 의 표식 절단 계열 전건 종결 ■■ ⒜ 를 **물려받지 않고 다시 재서** 절반을 닫았다 — 표식 절단 **5 → 0**(`ede5d5e4`·`7f7e754d` · talk 정본 `70d0d2c` · 회귀 `tests/엔진소스표기.test.js` 6건 신설 · 변이 11/11 구멍 0 미적용 0). ■ 잰 법: 얼린 detached 트리(HEAD=`e523506d` = 내 가지에 origin/master **반입**한 판 · 형제 정션)에 같은 P2(줄 끝 공백 361~362파일). 트리가 그 사이 움직여 분모가 3895→**3970** 이 됐다. 첫 판 기준선 fail 4 → P2 fail 14 = **새 적색 10**(표식 절단 5 · 생성물 대조 2 · 측정 인공물 3) — [B] 의 갈래는 맞았고 개수는 이 분모에서 10 이었다. ■ 🔴 **뿌리가 [B] 의 진단보다 하나 더 있었다 — 접기만이 아니라 「자르기」도다.** `소스.slice(i, 소스.indexOf(끝, i))` 는 끝앵커를 못 찾으면 `slice(i, -1)` 이 되어 **파일 나머지 전부**를 본문으로 삼는다. `수집.test.js:823` 의 적색이 그것이고(부정 단언이 뒤집혔다), 반대 방향이 더 나쁘다 — `리포트카드공개` 의 `voiceSweep_` 시험은 변형 아래서 **엉뚱한 구간을 보고도 통과했다**. 즉 [B] 가 「이 측정이 원리상 못 본다」고 적은 그 방향이 실물로 잡혔다. ■ 처방: [B] 는 접기를 `tests/_engine-source.js` 에 뒀는데 그러면 **엔진 소스를 읽는 호출부만** 안전하다. 엔진 밖 파일을 읽는 자리엔 지날 문이 없어 저마다 손으로 접었고 **실측 29벌 전부 CRLF 축만** 접은 반쪽이었다. 정의를 `tests/lib/소스검사.js` 하나로 모으고(그 파일이 원래 「소스를 검사 대상으로 삼는 통로」다) 문 둘을 냈다 — `파일소스()`(읽으면서 접는다) · `구간()`(앵커를 찾기 «전에» 접고, 못 찾으면 던진다). `_engine-source` 는 이제 require 만 한다. ⚠ 그 파일은 **talk 이 정본**이라 as 만 고친 판에서 `소스검사통로.test.js:105` 가 빨개졌다 — 검사가 제 일을 했고, talk 에 얹어(전량 2337 pass·fail 0) as 로 복사해 닫았다. ■ 🔑 **자기검증에서 잡아 고친 것 — 판별기를 되먹여 보고 두 번 틀렸다**: ③첫 판 `readFileSync\([^)]*\)\s*\.replace\(\/\\r` 는 **제가 금지한 되돌림을 못 잡았다**(`path.join(…)` 의 안쪽 `)` 에서 `[^)]*` 가 끊긴다) · ①넓힌 판은 이번엔 **자기 주석에 눈이 멀었다** — 「그전엔 손으로 이렇게 접었다」고 적어 둔 설명 문장이 위반으로 잡혀, 이 검사가 정확히 «가장 잘 적어 둔 파일»에서 깨졌다(F401 그 사고). 처방은 새 규칙이 아니라 같은 모듈이 이미 들고 있던 `코드만()` 이다. 그리고 넓힌 판별기가 **내가 못 본 손 접기 둘을 더 찾았다**(`수집.test.js:401`·`:411`). ■ 검증(분모와 함께): 얼린 트리 기준선 **3970 · fail 0** → 같은 P2 를 얹어 **fail 8**. 표식 절단 **5 → 0 전건** · 부정 단언 뒤집힘 **1 → 0**. 변이 11/11 — 첫 판의 구멍 1은 검사가 없어서가 아니라 **내 시험 목록이 좁아서**였다(`소스검사통로.test.js` 를 넣으니 닫혔다 · 사본을 새로 짓지 않았다). ■ 🔴 **측정 자체가 한 번 거짓말했다 — 다음 주자는 이것부터 읽는다**: 중간에 fail 37 이 나와 「내 수리가 회귀를 만들었다」로 보였는데 범인은 **내 변이기**였다. ①원복을 `git checkout -- .` 로 했더니 `autocrlf=true` 가 얼린 트리를 통째로 CRLF 로 바꿨고 ②변이기가 `split('\n')` 조각 **뒤에** 공백을 붙여 `\r 공백 \n` 이라는 **사람이 절대 안 쓰는 모양**을 만들었다. 그러면 재는 것이 「줄 끝 공백」이 아니라 「깨진 줄끝」이라, 접기가 멀쩡한데도 「섹션 끝 표식을 찾지 못함」이 22건 났다. F477 이 경고한 함정이 **변이기 쪽에서** 재발한 것이다. ⇒ 규율에 두 줄 더한다: **원복은 `-c core.autocrlf=false` 로 한다** · **변이는 줄끝 표기 «앞»에 넣는다**. ■ 🔴 **안 닫은 것 — 남은 8 = 생성물 대조 5 + 측정 인공물 3**: 인공물 3(`버전충돌:62` = Code.js 를 실제로 고쳤다 · `소스검사통로:105`·`크루카드:521` = as 만 변형했다)은 **분자가 아니다**(다시 세지 마라). 생성물 대조 5(`대장동봉` :71·:81·:139 · `이해대장:499` · `펠트문서:286`)는 산출물이 **실제로** 갈려서 나는 적색이라 거짓 적색이 아니다. ▶ **그 5의 노출층을 이번에 특정했고, [B] 의 지목을 정정한다**: `tools/대장동봉검사.js:144` 의 접기는 **실패 경로가 아니다** — 그건 「스테이징분과 작업본이 달라 못 했다」로 빠지는 가드라 애초에 막지 않는다. 실제 노출은 생성기의 **입력 스캐너**다: `tools/lib/시트도달.js:149` 가 엔진 소스를 원문 그대로 읽어 훑고, 그 훑기가 표기에 흔들리면 산출물의 «수»가 바뀐다. ⇒ 다음 처방은 접기를 **`tools/lib/` 로도 내는 것**이다(지금 정의는 `tests/lib/소스검사.js` 라 도구가 원리상 못 쓴다 — 방향이 거꾸로다). 그 이관은 pre-commit 게이트를 지나는 파일을 건드리므로 별건으로 세운다. |
