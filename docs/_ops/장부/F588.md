| F588 | 2026-08-17 | 마찰 | 검수 «처분» 레버 셋을 신설하면서 **게이트 축을 안 봤다** — 회귀가 그 축을 단언 0건으로 통과시켰다.  08-17 에 종결 레버가 `기각` 하나에서 셋(`기각`·`채택수리`·`대상아님`)으로 갈렸다. 그 트랙이 못 박은 불변식은 **필터 축 하나**다 — 「`기각` 만 걸러진다」(`tests/검수처분.test.js` 의 `걸러지는키들` 검사 4벌). 그런데 같은 집합이 **두 곳에서** 쓰인다: 필터(다음 회차에 안 올린다)와 **게이트**(`게이트판정()` 이 `!기각키.has(f.키)` 로 미해결을 센다 · `tools/codex-review.js:1544`). 후자는 회귀가 **한 줄도 단언하지 않는다.**  ■ 그래서 무엇이 샜나 — `대상아님`·`채택수리` 는 **원리상 배포 게이트를 못 연다.** 도구는 그 레버를 정상 통로로 안내하고(`--대상아님 <키> --사유 "왜"`), 사유를 요구하고, 장부에 처분까지 박는다. 사람이 보는 모든 신호가 「처분 끝」인데 게이트는 그대로 `block` 이다. 게이트가 내미는 처방도 `①고친다 ②기각한다` 둘만 대므로(:1549-1552) **참인데 범위 밖인 지적에는 정직한 출구가 0이다** — 열려면 `기각`(=「왜 틀렸는지」 + 영구 필터)으로 참인 지적을 묻어야 한다.  ■ 실측 (2026-08-17 · `local_61ea8c4d` · 배포 트랙 승계)   · 전임 `local_41cc57e7` 이 P1 4건을 14:25 에 `--대상아님` ×4 로 사유까지 달아 **전부** 처분했다.   · 그 뒤 `게이트판정()` 직접 호출 = `level=block · 미해결 4 · fp 0d994066` — **처분 전과 같은 4건**이다.   · 4건의 과녁은 전부 `tools/` 아래이고 (루트) 배포집합은 루트 16파일이라 `tools/` 는 **구조상 못 든다**(기계 확인).   ⇒ 라이브 배포 밀림 해소 트랙이 여기서 멈췄다. 게이트 5/6 통과 · 6번째만 닫힘.  ■ 축 — F545 와 **다르다**(같은 증상, 다른 층). F545 는 「기각이 이름만 바꿔 되살아난다」(키 변이)다. 이건 **키가 맞고 처분도 됐는데 그 처분이 게이트를 못 연다**다. F545 처방 ㉠㉡㉢ 은 셋 다 «차단 범위»를 바꾸는 ⏳유호 픽이라, 그 픽이 나기 전에도 이 축은 따로 산다.  ■ 왜 신뢰성 맹점 ④인가 — 「안 도는 쪽」이 아니라 **「맞는 얼굴로 틀린 값」**이다. 레버는 돌고(exit 0), 장부엔 남고, 화면은 처분됐다고 말하는데 게이트만 안 열린다. CLAUDE.md 가 「새 장치엔 대가를 함께 적는다: 틀릴 때의 모습」을 요구하는 자리에서, 이 장치의 틀린 모습이 안 적혔고 회귀도 그쪽을 안 봤다.  ■ 처방 후보 — 안 지었다(게이트 차단 범위는 ⏳유호 픽 자리라 손대지 않는다 · F545 에 ㉣ 로 올렸다).   ㉠ 회귀에 **게이트 축 단언**을 넣는다 — 처분 레버마다 `게이트판정` 레벨이 무엇이 되는지 픽스처로 못 박는다. 이건 차단 범위를 **안 바꾸고** 현재 동작을 그대로 기록만 하므로 픽과 독립이다. 지금 초록이 「의도된 설계」인지 「빠뜨린 축」인지를 이 검사가 문서화한다.   ㉡ 두 소비자(필터·게이트)가 **같은 함수를 쓰는 것 자체**를 다시 본다 — `걸러지는키들` 은 「다음 회차에 안 올린다」는 뜻이고 게이트가 묻는 것은 「이 지적이 처분됐나」다. 뜻이 다른 두 질문에 한 집합을 쓰면 한쪽을 고칠 때 다른 쪽이 조용히 갈린다(코드 주석 :111 이 「두 곳에 적으면 갈라진다」를 걱정했는데, 하나로 합쳐서 **반대 방향으로** 갈렸다).   ⚠ ㉡ 는 차단 범위를 건드리므로 F545 픽과 한 벌로 판정해야 한다. ㉠ 은 아니다. | |
