| F589 | 2026-08-17 | 마찰 | **F582 가 하루 만에 2번째로 발동했다 — 그리고 이번에는 «인계 자신»이 자물쇠 하나를 먹였다.** 죽은 세션의 활성 줄이 파일을 영구 예약하는 문제(F582)가, 그 신고를 낸 세션의 **바로 다음 세션**에서 다시 났다. 새로 드러난 것은 빈도만이 아니다 — 막는 줄이 둘로 늘었고 **그중 하나가 나에게 이 일감을 넘긴 세션 자신의 줄**이다.  실측 2026-08-17 (`local_cc683bbf` · 인계받은 트랙 = F572 미측정 변이 4건): · `docs/_ops/보드/cc683bbf.md` 첫 선언에 deny — 겹침 **2건**:   ① `tools/board-move.js` ← `86d6d162` 「#Q93 죽은 보드 줄이 자리를 영구히 잠근다…」 (F582 원 신고와 같은 줄)   ② `F572`·`tests/보드이관.test.js` ← `eba8defb` 「F572 후속 — 잔재 닫기의 변이 빚 4건…」 · ②의 주인 `eba8defb` 는 **마감하면서 「▶다음 할 일 = 내 보드 줄 2번째에 적혀 있다」로 이 일감을 넘긴 세션이다.** 넘긴 줄이 그대로 활성으로 남아 **넘겨받은 사람을 막는다.**  ■ 왜 이게 F582 의 단순 재발이 아닌가 — 자물쇠가 «인계에 비례해» 는다 F582 는 「죽은 줄이 자리를 잠근다」였다. 여기서 한 겹이 더 나온다: 인계가 **성립할수록** 잠금이 는다. 세션이 트랙을 끝내지 못하고 다음 사람에게 넘기는 것은 이 저장소의 정상 동작인데(인계문·보드 ▶다음), 그 정상 동작이 매번 활성 줄을 하나씩 남긴다. 죽은 활성 줄 12개는 사고의 잔해가 아니라 **인계의 부산물**이다.  ■ 이번에 실제로 통한 것 / 여전히 안 통하는 것 (처방 검증 재료) · ✅ ②는 ④ 거절문의 처방 ㉡(「죽은 세션의 트랙을 그대로 이어받는 것이면 트랙 칸을 **바이트 그대로** 옮겨 적는다」)로 **정당하게** 뚫렸다. F582 신고문은 이 처방을 「거짓 표기가 된다」로 기각했는데, 그건 그때 그 세션의 트랙(F572 수리)이 #Q93 과 달랐기 때문이다. 이번엔 내 트랙이 `eba8defb` 의 트랙과 **진짜로 같아서** 베껴 적는 것이 사실이었다. ⇒ ㉡은 틀린 처방이 아니라 **「승계일 때만 열리는 좁은 문」**이고, 그 좁음이 맞다. board-guard 도 「인계로 본다(🎫 인지 확인)」 알림을 냈다. · ❌ ①은 이번에도 출구가 없었다. 내가 지난 이유는 처방이 아니라 **사실**이다 — 이 트랙은 `tools/board-move.js` 를 변이로만 만지고 `tools/변이.js` 가 바이트 원복을 보장하므로, 파일 칸에 「제자리 원복이라 바이트 무변경」이라 적은 것이 참이었다. 즉 **부정 표기가 우회가 아니라 사실이어서** 통과했다. 그 파일을 **실제로 고치는** 다음 트랙에는 그 문이 없다 — F582 가 적은 ㉠~㉣ 막힘이 글자 그대로 그대로 남아 있다.  ■ 처방 — 새로 안 짓는다. F582 의 ㉠ 권고가 그대로 유효하고, 이 행은 **빈도 재료**다 ㉠ = `board-guard` 파일 겹침 검사 ④ 에 생사 게이트(같은 파일 검사 ⑧ 이 이미 쓰는 원칙 · 판정은 `작업본소유자.살았나` 한 곳에서 · 죽었으면 deny 대신 알림). 이번 판은 그 권고를 **강화**한다: ㉡(승계)이 정상 통로로 실제로 도는 것이 확인됐으므로, ㉠을 붙여도 「승계 표기」라는 정직한 관습은 안 죽는다 — 죽는 것은 **승계가 아닌데 막히는** 자리뿐이다. ⚠ 대가(F582 가 적은 것 그대로): 죽은 줄의 파일을 두 세션이 동시에 집을 수 있다. 다만 지금은 **아무도 못 집는다**(더 나쁜 쪽)이고, 동시 착수는 track-collision·PR 목록이 잡는 층이다. 판정 자리 = 유호님(F582 가 ⏳로 올려둔 그대로). 이 행은 「2번째 = 실수가 아니라 시스템 결함」(CLAUDE.md 신뢰성)의 근거를 대는 몫만 한다. | |
