| F307 | 2026-08-10 | 실수 | `doc-graph.js --add` 가 `.js` 파일에 **날 HTML 주석**을 심는다 — 저장소 규약은 `// <!-- 파생: … -->` 로 감싸는 것인데(`엔진_운영배치.js:3` 이 그 표기), `contents_상담AI.js` 에 심으니 `//` 없이 `<!-- 파생: docs/SYNK_철학.md@v1.0 -->` 한 줄이 파일 맨 앞(1행 빈 줄 + 2행)에 들어갔다.  🔴 **급소는 「가드가 이걸 통과시킨다」다.** `node --check` 가 exit 0 을 낸다 — `<!--` 는 sloppy 스크립트에서 **Annex B 레거시 한 줄 주석**이라 구문상 합법이기 때문이다. 즉 배포 전 구문검사(`/deploy` 1단계)는 이 자리에 **눈이 멀어 있다**. Apps Script 는 모듈이 아니라 스크립트로 돌아 라이브에서도 안 터지고, 그래서 증상이 「조용함」뿐이다 — 대외 카피 원천 파일 맨 앞에 HTML 주석이 박힌 채로 배포본에 실려 나간다.  실측 경위(2026-08-10 `local_a3993f21`): 철학 리라이팅 배치 1에서 파생 엣지 결손 5건을 심다가, 커밋 직전 `git diff` 를 눈으로 읽고 발견했다. `node --check` 도 CI 모사(2390/0)도 전부 초록이었다 — **잡은 것은 사람 눈이지 기계가 아니다.**  처방 후보: `--add`/`--stamp` 가 대상 확장자를 보고 주석 문법을 고른다(`.js`·`.ts` → `// <!-- … -->` · `.md`·`.html` → `<!-- … -->` · `.txt` → 현행). 곁들여 회귀 = 「추적되는 `.js` 에 `//` 로 안 감싼 `<!--` 가 없다」 — 이건 픽스처로 탐지력을 못박을 수 있고 실저장소엔 지금 위반 0이다. | tools/doc-graph.js — `--add` 에 `.js` 갈래를 신설했다. 표기 정본은 저장소가 이미 쓰던 `// <!-- 파생: … -->`(엔진_운영배치.js:3)이고, 자리는 머리 주석 블록 «바로 다음»(셔뱅·`'use strict'`·`//` 줄을 지나고 빈 줄에서 멈춘다 · 블록 주석 앞에서도 멈춘다 — 안으로 밀어 넣으면 엣지가 주석에 갇혀 파서가 영원히 0을 낸다). 심는 판정을 순수 함수 `엣지본문(text, line, docRel)` 으로 갈랐다 — 안 가르면 회귀가 이 판정을 재려고 실저장소 파일을 실제로 고쳐야 하고, 그러면 「버그가 아직 있을 것을 요구하는 회귀」로 굳는다(가드 3맹점 ②). 곁들여 개행을 파일에서 파생시킨다 — 옛 코드는 `\n` 으로 자르고 붙여 CRLF 파일(엔진_운영배치.js)에 끼워 넣은 줄만 LF 가 됐고, 그 오염은 다음 사람의 diff 에서야 드러난다. 회귀 6건(tests/문서그래프.test.js): ①`// ` 감싸개+머리 다음 자리+날 주석 줄 0+심은 뒤 파서가 그대로 읽음 ②셔뱅·use strict·머리 없음·블록 주석 네 모양에서 감싸개가 안 빠짐 ③CRLF 파일에 LF 가 안 섞임 ④`--stamp` 갈래(자리 갱신)가 감싸개를 안 벗김 ⑤실저장소 = 추적되는 `.js` 전량(분모 어서션 포함)에 줄 첫머리 날 `<!--` 0건 — 거짓양성만 본다. 🔑 이 회귀를 «구문검사로 짤 수 없다»는 것이 이 건의 핵심이다: `<!--` 는 sloppy 스크립트의 Annex B 한 줄 주석이라 `node --check` 가 exit 0 을 내고 Apps Script 라이브에서도 안 터진다 — 그래서 표기를 직접 못박았다. 변이 3/3 — 옛 동작(`.js` 가 떨어지던 마크다운 갈래)을 그대로 먹여 세 assertion 이 전부 빨개지는 것을 확인했고, 그 산출물이 실제로 「빈 줄 + 날 `<!-- 파생 -->` + 밀려난 첫 줄」이었다. 저장소 전수 grep — 엣지 줄을 쓰는 자리는 doc-graph 하나뿐이다(tools/시스템대장.js:207 은 `.html` 템플릿이라 날 주석이 맞다). CI 모사 2547/0. 커밋 bcddc690. |
