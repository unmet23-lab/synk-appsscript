| F391 | 2026-08-12 | 실수 | 훅 원본이 저장소에 있는데 «설치본»만 고치고 「이 층은 git 밖이라 안 퍼진다」고 커밋 메시지에 적었다 — 실측 2026-08-12 local_91cd3a7c. .git/hooks/pre-commit 을 먼저 발견해 거기 조각을 넣었고, 원본 tools/githooks/pre-commit + tools/install-githooks.js 를 안 찾았다. 그대로 뒀으면 다음 install-githooks 실행이 내 게이트를 조용히 지운다 — 증상은 「가드가 없다」가 아니라 「있었는데 사라졌다」라 아무 데도 안 남는다. F348 과 같은 형태다(한 층만 열고 «없다»를 판정). 🔑 이 저장소에서 «훅»이 사는 층은 셋이다: .claude/hooks(세션 훅) · tools/githooks(git 훅 원본) · .git/hooks(설치본). 새 게이트를 놓기 전에 셋을 다 열어야 한다. 발견 경로도 남긴다 — 메모리 토픽 guard-routing-and-collisions 의 옛 절이 install-githooks 를 지나가듯 언급해서 알았다(내가 찾은 게 아니다). 처방 후보=install-githooks 가 «설치본에만 있고 원본에 없는 줄»을 보고하게 한다(지금은 원본→설치본 한 방향만 본다). | install-githooks 가 설치본단독줄(원본↔설치본 줄 집합 차)을 재고 — --check 는 그 줄을 드러내고, write 는 덮지 않고 보류+exit1(.git/hooks 는 git 밖이라 덮이면 복구 0) · 처방 두 갈래(원본에 먼저 옮기기/설치본에서 지우기) 동봉 — 7c5edd15 · 회귀 4(tests/훅설치.test.js: 거부·check 노출·원본만 앞선 갱신은 통과(음성)·처방 되먹임 F103) · 실저장소 --check 최신 그대로(거짓양성 0) |
