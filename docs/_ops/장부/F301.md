| F301 | 2026-08-09 | 마찰 | F297 수리가 `add` 만 고쳐서 **바로 옆자리(`resolve`)에서 같은 사고가 그대로 재발했다.** 2026-08-09 실측: F300 을 닫으려고 `node tools/friction.js resolve F300 --파일 <경로>` 를 줬더니 `resolve` 의 파서(`args.slice(2).join(' ')`)가 그 토큰과 절대경로를 통째로 해소문으로 접었고, 「--파일 C:/Users/.../해소.txt」가 해소 칸이 되어 커밋까지 갔다(5fd6800a · 복구 이 커밋). F297 이 기록한 사고와 **글자 하나까지 같은 모양**인데 서브명령만 다르다. 왜 놓쳤나 — F297 은 「신고문 통로가 없다」를 고쳤고 그 자리에서 `add` 파서에 두 장치(모르는 플래그 거절 · `--파일`)를 넣었는데, **같은 파일 40줄 아래의 `resolve` 는 같은 결함을 그대로 갖고 있었고 아무도 안 봤다.** 새는 방향은 여기서도 「통과」다: 도구는 성공을 찍고 행은 닫히며, 쓰레기가 들어간 칸은 사람만 안다. 더 나쁜 것은 **장부가 `/evolve` 의 연료**라는 점이다 — 해소 칸의 쓰레기는 「무엇이 실제로 막았나」를 통째로 지우므로, 다음 개정은 그 수리가 무엇이었는지 모른 채 돈다(F092 「안 고쳤는데 닫았다」와 결과가 같다). 수리: 두 장치를 **모듈 스코프 함수 둘**(`모르는플래그`·`본문또는파일`)로 뽑아 `add` 와 `resolve` 가 같은 것을 부른다 — 호출부마다 고치면 세 번째 서브명령이 생기는 날 또 갈라진다(CLAUDE.md 신뢰성: 3번째 = 원인을 쓸 수 없게 만든다). 회귀 4건을 `add` 쪽 넷과 **짝**으로 넣었다(`tests/마찰신호.test.js` 50/50): 모르는 옵션 거절 · `--파일` 이 그대로 칸이 됨(백틱·따옴표 포함) · 인자와 파일 둘 다면 막음 · 못 읽으면 **행을 닫지 않음**. 한쪽만 초록이면 갈라진 것이다. 🔑 이 신고문 자체가 새 통로(`--파일`)로 들어왔다 = F083 재투입 증명. ⚠ 남은 천장: 이 도구의 다른 진입점(`report`·`--open`)은 텍스트 인자를 안 받아 같은 결함이 없지만, **새 서브명령을 더할 때 이 두 함수를 안 쓰면 다시 갈라진다** — 그건 리뷰 말고 층이 없다. | |
