| F655 | 2026-08-19 | 실수 | **낡은 스냅샷을 재고 「값이 0이다」라고 판정할 뻔했다** — 카드 환경층의 효과를 재려고 격리 카드 HTML 을 떠서 픽셀을 샀는데, 그 파일은 환경층을 «넣기 전»에 뜬 것이었다. 세로 최대 채널 차 **0.0/255** 가 나왔고 하마터면 「이 층은 값이 없다」로 접을 뻔했다. 다시 떠서 재니 실제로는 값이 있었다. 🔑CLAUDE.md 「재는 층이 값을 깨뜨리지 않는지 확인」이 정확히 이 자리다 — **파생물을 재면 원본이 아니라 파생 시점을 잰다.** | ✅ 그 자리에서 재생성 후 재측정 + A/B(조건 하나만 바꾼 대조판)로 다시 쟀다. 처방: 격리 픽스처는 «재기 직전»에 원본에서 다시 뜬다(떠 놓고 쓰지 않는다). |
