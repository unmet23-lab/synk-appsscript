| F606 | 2026-08-18 | 마찰 | 형제 저장소가 다섯 번 배운 가드가 **이 저장소엔 한 번도 안 왔다** — 트랙을 닫는 도구가 모르는 플래그를 조용히 삼킨다. 실측 2026-08-18 (`local_88739beb` · 렉서 `코드만` 을 지나 셌다): `tools/` 에서 `process.argv` 를 «코드로» 읽는 도구 **77 중 인자 게이트를 건 것 0**. 내가 직접 밟은 자리 둘 — `node tools/이해대장.js --빨강` 과 `node tools/대기열.js --상세 "#Q113"` 이 **오류 없이 전량 판을 냈다**(좁혀 달라고 부탁한 사람은 그 출력을 좁혀진 것으로 읽는다). 급소는 읽기 도구가 아니라 **트랙을 닫는 도구**다: `작업가지.js --닫기` 는 머지·원격 가지 거둠인데 `--단기` 오타 하나면 아무 말 없이 «현황»만 그렸다. 직전 세션(`local_61375a0b`)이 바로 그 파일의 조용한 실패 셋(F567·F577·F580 — 「트랙을 닫는 마지막 걸음이 조용히 실패한다」)을 고친 자리라, 삼켜지는 플래그는 **같은 병의 네 번째 입**이고 새는 방향이 셋과 똑같이 「통과」다. ■ 이 신고가 남기는 재료는 수리가 아니라 **분모를 넓히는 일이 「목록 손보기」가 아니라 «추출기 설계»라는 것**이다 — 형제 코드를 그대로 옮기니 이 저장소에서 값이 통째로 뒤집혔다(git 인자가 «자기 낱말»로 올라오고 진짜 낱말 `--닫기` 가 자식후보로 밀렸다). 뒤집은 것은 둘: ㉠**클로저 도우미**(`const 있나 = (f) => 인자.includes(f)`)가 이 저장소의 주력 표기인데 형제 판은 `값(인자, '--열기')` 꼴만 봤다 ㉡**이름 가림** — `function gh(인자, 옵션 = {})` 의 **정의부**가 「argv 를 넘겨받는 도우미」로 읽혀 `gh(['--version'])`·`git(['status','--porcelain'])` 의 git 인자가 승격됐다(한글 식별자엔 `\b` 가 원리상 안 붙는다). 그대로 선언했으면 ⓐ`--닫기` 가 게이트에 막혀 트랙을 못 닫고(F103) ⓑgit 낱말이 CLI 에서 통과했다 — 두 방향이 반대라 한쪽만 막으면 나머지가 그대로 샌다. ■ 그리고 **원문 grep 이 나를 두 번 속였다**: `grep -c -- '--운영' tools/작업본소유자.js` = 2 인데 둘 다 주석이었고(믿었으면 없는 조준축을 설계에 넣었다), `grep -l 인자게이트 tools/*.js` 가 낸 그 파일의 낱말도 주석이었다(믿었으면 분모가 부풀어 「덜 남았다」로 읽혔다). 형제가 같은 병을 두 번 겪고 렉서 통로로 옮겼는데, 이쪽은 그 교훈도 안 와 있었다. ■ 남은 빚 = **미게이트 75/77**. 래칫이 늘어나는 것만 막고 부착은 갈래로 나눈다(71개를 한 커밋에 달면 하나만 틀려도 되던 명령이 죽고, 그게 이 장치가 틀릴 때의 모습이다). | tools/lib/인자게이트.js 공용 판정 + tests/lib/도구인자.js(분모·게이트·자기 낱말을 렉서 뒤에서 한 통로로) + tests/도구인자게이트.test.js 회귀 14(양방향 ④ · 래칫) + 부착 둘(작업가지·대기열). 변이 14/14 구멍0 미적용0. 남은 빚 = 미게이트 75/77 은 래칫으로 늘지만 못하게 묶었다 |
