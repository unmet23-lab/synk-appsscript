| F350 | 2026-08-12 | 실수 | 「file:line 을 인용한 실측 주장」이 세 번째로 거짓이었다 — 이번엔 «정정하는 쪽»이 틀렸다  F348 은 「설계문의 실측 주장을 한 층만 열고 통과시켰다」였다. 그 정정으로 나온 처분표 v2 가 스스로 독립 실측이라며 낸 N 항목이 또 같은 병이었다: 「`estimator_version`·`estimator_confidence`· `as_of`·`창일수` 는 `learning_events` 에 물리 열로 이미 있다(L0_스키마.sql:2881~2883)」.  그 줄을 열어 보니 실제로 여는 열은 `source_kind`·`estimator_confidence`·`estimator_version`· `evidence_refs` 넷이고, `as_of` 는 스키마 파일 전체에 0건이다(`창일수`·`window_days` 도 0건). 넷 중 둘만 맞았고, 정작 그 자리에 있는 `evidence_refs` 는 안 셌다.  같은 문서의 S 항목도 같은 꼴이었다 — 「계약이 `cited_refs` 를 「생성에 들어간 입력」으로 정의했다」. 실제 계약 정의는 「어느 지식 노트가 그 답에 실렸나」(RAG)이고, 「생성에 들어간 입력」은 `evidence_refs` **주석이 자기와 구분하려고 부른 별칭**이었다.  왜 위험한가: 두 주장 다 「file:line 을 달고 있어서」 다음 독자가 재지 않고 그대로 인용한다. 실제로 처분표 §5 의 「면적 1칸」 산술이 그 위에 서 있었고, 그 갈래를 그대로 골랐으면 갈 곳 없는 `as_of` 를 「이미 열이 있다」고 믿고 설계했을 것이다.  3번째라 프로즈로는 못 막는다. 기계화 후보: 문서의 `파일:줄` 인용을 훑어 ①그 파일에 그 줄이 있는지 ②그 줄(±2)에 인용된 식별자가 실제로 있는지 검사하는 가드. 탐지력은 픽스처로 못박고 실저장소에는 거짓양성만 검사한다(CLAUDE.md 신뢰성 ②). | tools/인용검사.js + tests/인용검사.test.js(16벌) + pre-commit 조각(cb0732e9). F083 재현 검증: 신고 원문(처분표 v2)을 그대로 재니 as_of·창일수가 「파일 전체에 없는 이름」으로 잡혔다 — 손으로 잡은 것과 같다. 곁들여 F350 이 못 본 자리 하나를 더 찾았다(lib/성과회수.js 의 prompt_ver). 거짓양성 5원인을 실측 제거해 전량 🔴0(분모 234). ⚠ 발동층은 .git/hooks/pre-commit 이라 git 밖 — 이 기계에서만 돈다. |
