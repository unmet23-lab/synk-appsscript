| F423 | 2026-08-14 | 실수 | board-guard 가 **자기가 통과시킨 이유**를 「물려받은 보드 위반」 목록에 담아, 위반이 0건인 판에서도 모든 세션이 위반 1건을 안고 시작했다 — 장치가 「맞는 얼굴로 틀린 값」을 낸 자리(맹점 ④)이고, 딸린 처방은 따를 수도 없었다(F103). 08-14 실측 · 대기열 #Q66.  무슨 일인가. 상한 ①이 재는 수는 «도는 트랙»이다 — 가드 스스로 머리말(`:265~278`)에 그렇게 적었고, F395(`⏳`·`⏸`)와 F372(주인 사망)가 그 분모에서 두 갈래를 뺐다. 그러면 도는 트랙이 상한 안일 때 상한 ① 위반은 **0건**이다. 그런데 그 뺄셈을 알리는 문장이 `물려받은`(=위반 목록 배열)에 push 돼 있어서, 출력층이 그것을 「⚠ 물려받은 보드 위반 1건 — 내 편집은 막지 않았다」로 렌더했다. 즉 **통과 사유가 위반 건수로 세어졌다.**  왜 굵직한가 — 이 소음이 진짜 초과를 묻는다. 이 저장소의 세션은 보드에 선언하며 시작하므로 이 문장을 «매번» 본다(이 세션도 자기 편집에서 두 번 밟았다 — 활성 15→17 · 도는 3→5 · 둘 다 상한 12 안). 진짜 물려받은 위반(과길이 칸·트랙 충돌·전체 상한 초과)이 오면 **같은 머리글·같은 건수**에 섞여 나오는데, 읽는 쪽은 이미 「늘 뜨는 그것」으로 학습돼 있다. 대기열 #Q66 이 「그 소음 때문에 진짜 초과도 안 읽힌다 — 상한이 있으나 마나」로 적어 둔 자리가 정확히 이것이고, 그 줄은 원인을 「죽은 줄이 분모에 든다」로만 지목해 **분모를 고친 뒤에도 절반이 남아 있었다**(F372 수리는 08-14 에 회귀 4건과 함께 이미 착지해 있었다 — 셈은 옳았고 말이 틀렸다).  곁들여 F103. 위반 목록에 붙는 처방은 「그 세션이 살아있으면 그쪽 몫 · 죽었으면 board-move 를 실행하면 사라진다」인데, 뺀 줄에는 **원리상 안 듣는다** — `⏳`·`⏸` 는 과녁이 저장소 밖(유호님 답·시점)이라 어느 세션도 못 닫고, 주인이 죽은 활성 줄은 F073 이 편집을, board-move 원칙⑦이 이관을 각각 막는다. 그 셋은 가드 자신이 다른 자리에 적어 둔 사실이다. 따를 수 없는 처방을 매 세션에 내밀면 그 목록 전체가 「그냥 뜨는 것」이 된다.  처분 `db4cf6f5` — **가른 것은 말뿐이고 셈·결정은 한 글자도 안 건드렸다.** `셈설명` 채널을 따로 두고 🧮 머리글로 낸다(처방 없음 — 뺀 줄에는 없는 게 맞다). 뺀 사실 자체는 계속 말한다(침묵하면 「상한이 헐거워졌다」로 읽힌다 — F395 가 세운 축이고 기존 회귀 둘이 `알림`에서 그 문구를 계속 잰다).  변이 실측(조건은 채널 하나 · `셈설명.push` → `물려받은.push` 되돌림): 기준선 **초록 112/0/0** → 되돌리면 **적색 111/1/0**, 정확히 새 시험 한 칸만 진다. 회귀 2 = ㉠#Q66(뺀 사실은 말하되 위반으로 안 센다) ㉡F234 짝(**진짜** 물려받은 위반은 그 머리글로 계속 나온다 — 가르다 이쪽까지 지우면 빨개진다).  ⚠ 대가 (새 장치엔 틀릴 때의 모습을 함께 적는다). ㉠ 채널이 둘이 되었으니 **다음 사람이 새 알림을 어느 쪽에 담을지 틀릴 수 있다** — 가르는 기준을 한 문장으로 못박아 두었다: 「치울 것이 있으면 위반, 셈이 이래서 통과했으면 🧮」. 틀리는 방향은 조용하지 않다(위반 건수가 눈에 띄게 늘거나 준다). ㉡ 닫은 것 1개 = 대기열 #Q66 을 지웠다(양쪽 절반이 다 닫혔다). ㉢ **더 세지 않았다** — 「물려받은 목록에 또 무엇이 잘못 담겨 있나」는 전수로 안 봤다. 지금 담기는 것은 과길이 칸·유령·트랙 충돌·🎫 충돌·도는 트랙 초과이고 이 넷은 전부 「치울 것이 있다」 쪽이 맞다. | db4cf6f5 — 셈설명 채널 분리(🧮 머리글·처방 없음). 셈·결정 무변경 · 뺀 사실은 계속 말한다(F395 축) · 변이 1/1(채널만 되돌리면 111/1/0) · 회귀 2(#Q66 + F234 짝 탐지력) |
