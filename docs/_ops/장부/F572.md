| F572 | 2026-08-17 | 마찰 | `board-move` 의 F433 처방(「잔재 줄부터 마저 옮겨라」)이 **오늘 실물로 튕겼다** — 도구가 540~542행에 스스로 「남는 사각」이라 적어둔 그 판이 실재했고, 그 결과 따를 수 있는 유일한 출구가 따를 수 없었다.  실측 2026-08-17 (`local_eba8defb`): · board-move 거절문이 준 명령을 **그대로** 복붙했다:     `node tools/board-move.js "작동 대장 기록 자동화 — 자동이 기본, 재심이 분모를 덮는다"` · 결과 = exit 1 · 「[board-move] 보드에서 "…" 가 든 줄을 못 찾았다.」 · 원인 = 잔재를 남긴 죽은 세션 `6694ab98` 이 **보드 삭제까지 작업본에 쓰고** 죽어, needle 이   가리키는 보드 줄이 이미 없었다. 아카이브 +1 / 보드 −1 이 **둘 다 미커밋**인 모양이다.  ■ 도구는 이 사각을 알고 있었다 — 확률 판단이 틀렸다 `tools/board-move.js:540~542` 주석: 「⚠ 남는 사각: 잔재 줄의 **보드 줄이 이미 사라진** 경우(손 편집 등)엔 이 명령이 「못 찾았다」로 튕긴다. F226 실물은 언제나 보드 줄이 남아 있는 모양이라 (아카이브 커밋 «전»에 죽는다) 그쪽을 기본으로 뒀고…」 → 「F226 실물은 언제나 …」가 **오늘 반례를 만났다.** 사각을 적어두는 것은 조용히 틀리지 않게    하지만, 처방이 못 따를 때 **대안 출구를 안 준다**는 점은 그대로다(F103).  ■ 실제로 무엇으로 풀었나 (재료로 남긴다) board-move 원칙②(쓰기 전 메모리 검증)를 손으로 다시 재고, 통과한 뒤 두 파일을 **한 커밋**으로 닫았다 — `707d2c92`:   · 아카이브 1024 → 1025 (더해진 1 · 지워진 0) / 보드 2 → 1 (지워진 1 · 더해진 0)   · 옮겨진 줄이 양쪽에서 바이트 동일 · 아카이브에 정확히 1번 · 새 보드엔 없음   · 줄 주인 생사 = board-move 의 `산주인들()` 실측으로 죽음 즉 **상태는 이미 「옮김 완료, 커밋만 안 됨」이었다** — 필요한 것은 이관이 아니라 «닫기»였다.  ■ 처방 후보 — 안 지었다(이 신고는 기록이다) ㉠ 거절문의 처방을 **상태로 갈래낸다**: 잔재 줄이 보드에 있으면 지금처럼 「마저 옮겨라」,    없으면 「이미 옮겨졌다 — 원칙② 를 재검증하고 두 파일을 한 커밋으로 닫아라」+그 명령.    ⚠대가 = 닫기 통로를 새로 짓는 것이니 F102(아카이브 먼저·원자)를 그 통로도 지어야 한다. ㉡ `--잔재닫기` 같은 전용 손잡이. ⚠머리말이 「탈출구가 곧 우회다」로 이미 기각한 모양이다. ㉢ 그대로 두고 이 신고를 재료로 남긴다. ⚠다음 사람이 같은 3시간을 다시 쓴다. 권고 = ㉠. 사각이 실물로 발동한 이상 「드문 모양」 근거는 만료됐다. | ⏸ 이 세션에서 판정: 안 닫는다. 과녁이 board-move.js(F046·F102 사고 이력이 있는 도구)이고 권고 처방 ㉠은 «잔재 닫기» 통로를 새로 짓는 것이라 그 통로도 원칙②·F102(아카이브 먼저·원자)를 함께 져야 한다 — 보드수거 트랙(F571)에 얹으면 두 도구를 한 커밋에 섞게 된다. 대가는 한정적이다: F571 착지로 board-move 의 거절문(처방 포함)이 이제 화면까지 오므로, 다음 사람은 처방을 «보고» 그것이 튕기는 것까지 확인할 수 있고 이 장부 행에 실제 해소 절차(원칙② 재검증 후 두 파일 한 커밋 · 707d2c92)가 적혀 있다. 다음 주자: 처방을 상태로 갈래내는 ㉠. |
