| F510 | 2026-08-16 | 실수 | 마누스 통로가 **세워진 날부터 성공한 런이 0건**이었다 — 원인은 코드 주석이 «근거»로 적어 둔, 한 번도 안 잰 전제였다.  실측 2026-08-16(조건 하나씩만 바꾼 대조 · 크레딧 1300→1300 = 0 소모):   agent_profile 없음   → HTTP 200 + task_id + task_url … task.list 에 **없다**(유령)   agent_profile 1.6    → HTTP 200 … 역시 **유령**(무료 등급이 못 쓰는 프로필인데 에러를 안 낸다)   agent_profile 1.6-lite → HTTP 200 … **실재한다** ✅   오타·없는 이름        → HTTP 400 `Valid profiles are: manus-1.6, manus-1.6-lite, manus-1.6-max`  즉 **틀린 이름은 400 으로 시끄럽고, 등급 밖 프로필은 200 으로 조용하다.** 조용한 쪽이 통로를 죽였다.  🔑 급소는 코드가 그 선택을 **일부러** 했고, 이유까지 적어 뒀다는 것이다:    「agent_profile 은 일부러 안 보낸다 — 등급마다 쓸 수 있는 프로필이 달라(무료=lite)      박아 두면 등급이 바뀌는 날 조용히 깨진다. 계정 기본값에 맡긴다.」    걱정은 옳았다(조용히 깨지는 게 위험하다). 답이 틀렸다 — **계정 기본값이라는 것이 없다.**    그리고 그 전제를 한 번도 안 쟀다. 「잴 수 있는 것은 추정하지 말고 잰다」가 안 걸린 자리인데,    ⚠주석이 실측처럼 생겨서(옆 줄의 `prompt` 주석은 진짜 실측이었다) 아무도 다시 안 봤다.    같은 파일 안에서 **잰 것과 안 잰 것이 같은 글꼴로** 나란히 있었다.  곁가지(같은 파일 · 같은 「맞는 얼굴로 틀린 값」 축):   던지기가 실제로 통했는데 종료코드가 0xC0000409 로 나갔다 — fetch 두 번 뒤 process.exit() 를   즉시 부르면 libuv 가 닫히는 핸들에 uv_async_send 를 쏴 abort 한다(Windows · node v24.18).   부르는 쪽이 그 코드를 읽으면 «실패했다»고 보고 재던진다 = 크레딧을 또 태운다.   갈래 대조로 갈랐다: GET 1회 exit 0 · POST(400) 1회 exit 0 · 200 뒤 두 번째 fetch 에서만 abort.  대가: 유호님이 08-16 에 「Codex 심문처럼 자동화도구로 만들어달라」고 지시해 세운 통로가,   회귀 8·변이 5/5 로 초록이었는데도 **실물에서는 한 번도 안 돌았다.** 회귀가 순수 함수만 봤고   「작업이 실제로 생겼나」를 보는 층이 0이었다. 첫 실탄이 그걸 3시간 404 로 드러냈지만   그마저 「✅ 완주」로 앉았다(F509). | tools/마누스.js 생성몸통이 agent_profile 을 반드시 보낸다(기본 manus-1.6-lite · MANUS_AGENT_PROFILE 로 갈아끼움) + 던지기가 캔 id 를 한 번 써 본다 + CLI 가 process.exitCode 만 적는다 · 회귀 tests/마누스통로.test.js ①-b ①-c ⑫ |
