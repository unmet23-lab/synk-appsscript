| F327 | 2026-08-11 | 실수 | Apps Script 편집기의 실행 버튼은 자동화로 조준할 수 없다 — 「실행 직전 조준 확인」조차 안 통했다. 실측 3회(08-11 · 실행 기록으로 확정): 드롭다운에서 checkGoldenPush 를 고르고 툴바 라벨이 checkGoldenPush 로 바뀐 것을 확대해서 눈으로 확인한 뒤 실행을 눌렀는데, 실제로 돈 것은 migrateHwFormV9138 이었다. 즉 이 화면에서 **표시된 선택은 실행 대상의 증거가 아니다**. 곁들여 클릭이 조용히 안 먹기도(파일 목록·옵션 ref 클릭 3회) 두 번 먹기도(실행 1클릭 → 실행 기록 2건) 했고, 반환값은 셋 다 똑같이 Clicked on element 였다. 처방=편집기 Run 을 자동화로 누르지 않는다. 라이브 함수를 원격으로 돌려야 하면 층을 바꾼다(임시 doGet 러너 절차 · 메모리 synk-tooling-paths). 값어치보다 위험이 큰 확인은 접는다 — checkGoldenPush 의 대상인 토큰 연결은 08-04 라이브 실측 ✅ 가 있고 v9.176 감시가 이미 기계로 진다. | |
