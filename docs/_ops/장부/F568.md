| F568 | 2026-08-17 | 마찰 | 검수 런 종료코드 1(=P0·P1 지적 있음)을 세션시작 알림이 「실패로 끝난 것 — 주울 답이 없다(로그를 열어도 답이 아니라 오류다)」로 낸다. 그 문구대로 접었으면 라이브에 살아 있던 P1 을 못 봤다: 문법 판정 커서가 masteryApply_ 보다 먼저 찍혀, 야간이 삼킨 예외 뒤로 학생 전사문·대화문이 영구 미판정이 되는 결함(v9.248·v9.249 · 이미 라이브였다). 그 런은 «완주»해 지적 14건을 장부에 남긴 상태였다 — 실패가 아니라 결과다. 게다가 도구 자신의 --확인 게이트도 그 P1 을 안 막았다: 지문이 바뀌면 옛 검수 기록이 무효라는 규칙이, 실재하는 미해결 P1 을 게이트에서 조용히 풀어 준다(v9.250 이 다른 파일을 건드려 지문이 갈렸다). 두 층 다 새는 방향이 「통과」다. ▶수리 = v9.251 로 착지(회귀 3·변이 4/4) · 잔여 P1 은 #Q106. ⚠ 세션 91e2b70a 가 worktree-q104-review-exit-code-meaning 으로 같은 축을 이미 잡고 있다 — 중복 구현 금지, 그 트랙에 이 실측을 재료로 준다. | **열린 채 남아 있던 것이지 안 고쳐진 것이 아니었다** — 실물을 열어 확인하고 닫는다(장부의 「열림」이 거짓이면 다음 세션이 그 자리에서 헛돈다 · 계보 F614). 확인한 것 넷, 2026-08-18 `local_9a6c0547`: ①`tools/lib/검수런.js:종료코드뜻` 이 codex **1 = 「미결」·결과 true** 로 적혀 있다(「실패」가 아니다) — 그리고 표에 **없는** 코드는 「모름」이지 「결과 없음」이 아니라고 같은 자리에 못박혀 있다(이 신고가 지목한 새는 방향 그대로). ②`.claude/hooks/review-runs.js` 에 **🟠 「결과가 나왔고 처리할 것이 남은 것」 칸**이 🔴 실패 칸과 **따로** 서 있고, 그 주석이 이 사건을 실측으로 인용한다(64분 완주 · 지적 13건 · P1 2건이 「주울 답이 없다」로 광고된 자리). ③🔴 칸도 이제 「없다」와 「모른다」를 가른다 — 표에 든 코드에서만 단정한다. ④회귀가 그 판정을 문다: `tests/검수런.test.js` 에 미결미처분 갈래를 잡는 단언 **4개**(차단 지적 런 · 마누스 error · waiting · 알림 파일이 그 칸을 읽는가) — 57건 초록. 그리고 이 신고가 남긴 잔여도 닫혀 있다: **#Q106 은 ✅종결**(`[v9.253]` · PR #79)이다. ⚠ 안 잰 것: 이 신고문이 말한 두 번째 층(「`--확인` 게이트가 지문이 갈리면 실재하는 미해결 P1 을 조용히 풀어 준다」)은 **여기서 다시 안 쟀다** — 그 축은 신고문이 `v9.251` 착지로 적었고 이번 확인의 과녁이 아니다. 그 층이 다시 새면 그건 새 신고다(이 해소가 그것까지 덮는다고 읽지 않는다). |
