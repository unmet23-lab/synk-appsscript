| F418 | 2026-08-14 | 실수 | 탐색 목적의 `--help` 를 `tools/인계문.js` 에 줬는데 도구가 모르는 인자를 「실행하라」로 읽어, 산 세션의 가짜 「마감」 인계문(커밋 0 · 트랙 없음 · 종료사유 /close)을 세션 파일로 굽고 파생 목차 맨 위 — 「여기를 복사하라」 자리 — 에 앉혔다(F096 증상의 새 입구: 점검 통로는 --no-save 로 닫았지만 «모르는 인자» 문은 열려 있었다). 같은 계열 가드가 friction.js 에는 이미 있다(F297·F301) — 이 결함 모양의 두 번째 도구라 수정만으로 안 끝내고 회귀를 동반했다. | 인자 층 가드 — `--no-save` 외 인자는 아무것도 안 쓰고 exit 1(사유·올바른 두 통로를 stderr 로). 회귀 = tests/트랙경계.test.js 「모르는 인자」 1건, 변이 1/1(가드를 빼면 빨감 — 픽스처가 탐지력을 못박는다). 실물 원상복구 = 가짜 세션 파일 삭제 + 파생 목차에서 내 블록 제거(다음 close 재파생이 어차피 덮는 자리). 대가: 부르는 쪽이 새 플래그를 먼저 쓰면 거짓 정지 — 시끄러운 방향(exit 1)이라 바로 보인다. |
