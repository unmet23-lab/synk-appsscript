| F635 | 2026-08-18 | 마찰 | **board-guard 의 판정은 건강한데 «닿는 통로»가 Write/Edit 뿐이다 — Bash 로 쓴 보드 줄은 가드를 원리상 안 지난다.** 그리고 이 세션의 하네스 기본 지시가 「가능하면 Bash 로 편집하라」라서, 알려진 좁은 매처가 **상시 우회 통로로 승격**됐다.  ■ 실측 2026-08-18 (클라우드 세션 `cloud_79415d5c` · 유호 지시 「어제 구성한 시스템 점검」)  · 등록층을 `settings.json` 에서 직접 셌다 — board-guard 등록: **Write/Edit 매처 1건 · Bash 매처 0건.**    같은 PreToolUse 에 `Bash|PowerShell` 매처가 셋 있으나(git-scope · shell-inline · code-edit) 전부 **다른 과녁**이다.    `code-edit-guard` 의 경로 정규식은 코드 확장자만 본다(`.js` 등) — 보드 파일은 `.md` 라 셋 중 어느 것도 안 잡는다.  · 상태를 바꿔 보고 되돌렸다(되돌림 절차 = `rm` 한 번 · 대상은 미추적인 내 파일):    `printf … > docs/_ops/보드/79415d5c.md` → **종료코드 0 · 143바이트 생성 · 가드 발동 0.** 그 뒤 `rm` · 작업트리 깨끗.  · 같은 위반을 Write 로 내면 막힌다(대조군): 디스크에 짧은 칸 기준선을 두고 215자 칸을 Write 로 얹으니    `permissionDecision = deny` + 「**내가 이 편집으로 바꾼 줄**이다 · 4번째 칸 215자」. **판정 로직은 정확하다.**  ■ 내가 두 번 틀렸고 실측이 두 번 다 죽였다 (이 신고의 절반은 이것이다)  ㉠ 첫 미끼가 192자였다 — 상한 200 미달이라 안 막힌 것을 하마터면 「매처가 안 잡았다」로 옮길 뻔했다.    칸 길이를 세고서야 미끼 설계가 틀린 것을 알았다.  ㉡ 다음엔 263자 칸이 통과하는 것을 보고 **「빈 지문 탓에 가드가 내 줄을 못 가린다」**는 기전을 세웠다.    `CLAUDE_CODE_HOST_SESSION_ID` 를 ㉮빈값 ㉯내 파일명과 일치 ㉰남의 지문 셋으로 바꿔 대조하니 **판정이 전부 같았다** — 반증.    진짜 기전은 719~720행에 이미 적혀 있었다: 가드는 「내 편집이 위반을 **늘렸나**」만 막는다(F234·F235).    내가 먹인 입력이 디스크와 **같은 내용**이라 증분이 0이었다 — 즉 내 시험이 과녁을 안 재고 있었다.  🔑 CLAUDE.md 「원인 판정은 조건을 하나씩 바꿔 대조한다」의 ②번 대가 그 자리다 — **재는 층이 값을 깨뜨렸다.**    디스크 기준선을 안 갈아 놓고 두 번 쟀고, 두 번 다 「통과」가 나와서 두 번 다 가드를 의심했다.  ■ 왜 신호인가 — 새는 방향이 「통과」이고, 이번엔 **하네스가 그 방향으로 민다**  · CLAUDE.md 세션 규약이 「그대로 Write — 써야 board-guard 가 겹침을 본다(F415)」고 못박아 뒀다.    즉 이 좁은 매처는 **알려져 있었고, 사람 규율로 메워 두었다.**  · 그런데 이 세션의 하네스 기본 지시는 「Bash 로 할 수 있는 일은 Bash 로 하라(파일 수정은 sed·heredoc 으로)」다.    사람 규율과 하네스 기본값이 **정면으로 반대**를 가리키고, 규율 쪽이 프로즈이고 기본값 쪽이 상시 문구다.    CLAUDE.md 신뢰성 절이 정한 처분은 「프로즈보다 기계 강제로 막을 수 있는 규칙은 기계로 옮긴다」 —    지금 이 규칙은 정확히 반대로 서 있다(기계는 Write 만 보고, 나머지는 프로즈가 진다).  · 맹점 ③ 의 회귀는 **있고, 옳다** — `tests/훅등록.test.js:137` 「라우팅은 훅보다 넓다 — 훅이 막는 입력을    matcher·case 필터가 전부 통과시킨다」. 열어서 확인했다. 그런데 그 회귀는 **등록된 매처가 훅보다 좁지 않은가**를    묻는 것이라, 이 구멍에 원리상 눈이 멀어 있다: 그 입력표(:90)에 board-guard 는 `'Edit'` 한 줄로만 실려 있고,    Bash 통로는 애초에 등록이 없으니 「좁아졌다」고 말할 대상 자체가 없다.    🔑 즉 이 회귀가 못 보는 것은 「매처가 좁아졌나」가 아니라 **「이 훅이 저 도구에도 붙었어야 하나」**다 —    그건 라우팅 질문이 아니라 설계 질문이고, 지금 그 질문을 지는 층이 0이다.  ■ 처방 후보 — 안 골랐다(재료다)  ㉠ board-guard 를 `Bash|PowerShell` 에도 등록하고, 셸 문자열에서 `docs/_ops/보드/*.md` 로 향하는 쓰기(`>`·`>>`·`tee`·`sed -i`·`cp`·`mv`)를 뽑아 같은 판정으로 보낸다.    ⚠대가: 셸 문자열에서 대상 경로를 뽑는 일은 `code-edit-guard` 가 이미 치르는 값이고, 그 파일 머리말이      거짓양성 사례(임시 디렉터리·변수 대입)를 길게 적어 두었다 — 같은 오탐을 하나 더 만든다.      그리고 board-guard 는 보드 폴더 전량을 파싱한다(실측 판정 1회 ~100ms) — 모든 Bash 호출에 얹으면 상시 비용이다.  ㉡ 매처는 그대로 두고, **보드 파일을 Bash 로 건드리는 것 자체를 `code-edit-guard` 과녁에 넣는다**(경로 1개 추가 · 판정은 「Write 로 하라」 한 줄).    싸고 과녁이 좁다. ⚠대가: 두 훅이 같은 대상을 나눠 보게 되어 「같은 판정을 두 곳에 적으면 갈라진다」에 걸린다 —      과녁 목록을 한 곳에서 파생시켜야 한다.  ㉢ 아무것도 안 한다 — 지금까지 실사고 0건이다(보드 파일을 Bash 로 쓴 커밋을 안 찾아봤다 · 아래 「안 잰 것」).    ⚠대가: 하네스 기본값이 Bash 를 미는 한 이 우회는 **점점 자주** 지나간다. 그리고 board-guard 가 막는 것은      길이만이 아니라 **트랙 충돌·유령 줄·남의 줄 편집**이라, 이 통로로 새면 F073(남의 바이트를 내 커밋에 싣기)까지 열린다.  ■ 안 잰 것 (F348)  · 실제로 보드 파일이 Bash 통로로 쓰인 커밋이 이력에 있는지 **안 셌다** — 「사고 0건」은 관측이 아니라 미측정이다.  · PowerShell 통로(노트북)는 이 컨테이너에 없어 못 쟀다.  · `code-edit-guard` 가 `.md` 를 정말 하나도 안 보는지는 정규식을 읽어 판정했고, 실제 입력을 먹여 보지는 않았다. | **`code-edit-guard` 에 세션보드 갈래를 얹어 닫았다** — 셸에서 `docs/_ops/보드/*.md` 로 향하는 쓰기(리다이렉트·`sed -i`·`tee`·`cp`·`mv`·`rm`·python `open(…,'w')`·node `writeFileSync`)를 잡아 「Write 로 써라」로 막는다. 착지 `410905f9`.  ■ 왜 새 훅을 안 세웠나 `code-edit-guard` 가 **모든 셸 호출에 실제로 도는 유일한 훅**이고, 셸 문자열에서 대상 경로를 뽑는 어려운 값을 이미 치르고 있다. 판정을 두 곳에 적으면 갈라진다(F063).  ■ 무엇이 실제로 막았나 — 실측 6판 전건 정확 막을 것 3(리다이렉트·append·`sed -i`) · 통과할 것 3(`cat`·`grep`·`git add`). git 은 `git-scope-guard` 가 따로 지고, 보드 파일을 커밋하는 것은 막을 일이 아니다.  ■ 이 신고의 절반은 내가 두 번 틀린 기록이다 ㉠ 첫 미끼가 192자라 상한 200 미달이었는데 「매처가 안 잡았다」로 옮길 뻔했다. ㉡ 다음엔 「빈 지문 탓」이라는 기전을 세웠다가, 지문 3종으로 대조하니 판정이 전부 같아 **반증**됐다. 진짜 기전은 「내 편집이 위반을 늘렸나」만 막는 설계(F234·F235)였고, 내 시험 입력이 디스크와 같아 증분이 0이었다 — **재는 층이 값을 깨뜨린** 판이다.  ■ 안 잰 것 · `node tools/변이.js` 를 안 돌렸다. · PowerShell 통로(노트북)는 이 컨테이너에 없어 못 쟀다 — 낱말(`Set-Content`·`Add-Content`·`Out-File`)은 목록에 넣어 뒀지만 실입력을 먹여 보지 못했다. · 보드 파일이 실제로 셸로 쓰인 커밋이 이력에 있는지 안 셌다 — 「사고 0건」은 관측이 아니라 미측정이다. |
