| F395 | 2026-08-13 | 마찰 | board-guard 처방 불능 실측: 활성 상한 12를 남의 줄만으로 14 초과 · 첫선언 예외 소진 · 처방인 board-move 는 완료줄 전용, 보드수거.js 는 활성줄 거절(거둘 것 0건) — 유호 직접 지시 트랙(경량화 2·3차)을 선언할 통로가 없어 무선언 진행(커밋 메시지 조율). 상한 초과가 전부 남의 줄일 때 내 1줄 통로가 규정에 없다(F103 축) | `4ecc72b1` — board-guard 상한 ①의 **분모를 고쳤다** — 「활성」이 아니라 「도는 트랙(활성 − 유호·시점 대기)」을 잰다(`.claude/hooks/board-guard.js` countRunning·countWaiting · 판정 정본 `tools/lib/보드.js` 대기기호()·대기행()). 신고문의 「상한 초과가 전부 남의 줄일 때 통로가 없다」는 실측하니 **원인이 통로가 아니라 분모**였다: 2026-08-13 실보드 표 18줄 · 활성 15(상한 12)인데 그중 **5줄이 `⏳`·`⏸`** 였다(상표 접수 대기·카피 갈래 확정 대기·킷 A/B 처분·FB 압축안·영문명 정정) — 과녁이 유호님 답·외부 기관에 있어 **어느 세션도 못 닫는 줄**이다. 빼고 세면 도는 트랙 10이라 상한 안이고, 상한은 「트랙이 너무 많다」가 아니라 「유호님 답이 밀렸다」에 대고 발화하고 있었다. 그래서 처방이 원리상 0이었다(끝난 트랙 상태 갱신=남의 줄이라 F073 금지 · 내 줄 합치기=합칠 줄 없음) — 가드가 스스로 「새 세션이 따를 수 있는 처방이 없다」고 적어 두었던 그 자리 = F103. 신고문이 든 「첫선언 예외 소진」은 **반증**이다: F278 예외는 살아 있고 이 세션의 첫 선언을 실제로 통과시켰다(그 세션은 이미 자기 줄이 있어 예외 밖이었던 것). 못박은 방향 셋 — ①**완료 판정은 안 물들인다**(대기를 완료로 접으면 `보드수거` 가 유호님 답 기다리는 트랙을 조용히 아카이브로 가져간다) ②**전체 상한 18에는 대기 줄도 그대로 센다**(보드가 대기 줄로만 부푸는 자리는 그쪽이 막는다) ③**뺀 사실을 말한다** — 대기 제외가 판정을 바꾼 실행에서만 알림 1줄(늘 말하면 잔소리, 안 말하면 「상한이 헐거워졌다」로 읽힌다). 표식으로만 가른다(F374 ① — 프로즈 「유호 판정 대기」만 적힌 줄 1건은 일부러 안 잡는다: 좁게 틀리면 시끄럽고 넓게 틀리면 조용하다). 곁들여 board-guard 주석 1줄 정정(「활성 상한은 면제하지 않는다」가 F278-b 뒤로 바로 아래 코드와 반대였다). 검증 = 회귀 +4(tests/board-guard.test.js — 대조쌍은 조건 하나만 다르다: 작업중 10 vs 11) · **변이 5/5**(대기행 무력화 · 상태칸→줄전체 · 완료행 오염 · 전체상한에서도 빼기 · 알림 침묵) · 보드 4벌 173/173 · 인용검사 16/16. **F083 재주입 = 변이 ①이 그것이다** — `대기행` 을 무력화하면 신고문의 상태가 글자 그대로 돌아오고 F395 회귀 4벌이 전부 적색이다(즉 이 장치가 없으면 신고가 재현된다). 실보드 실발화도 확인 — 「활성 13줄(상한 12)이지만 **도는 트랙은 6줄**이다 · 대기 7줄을 분모에서 뺐다」. ⚠CI 모사는 **미검증**이다: 두 회차 다 옆 세션이 도는 중 편집한 파일(`엔진_콘텐츠AI.js` 등 8개) 탓에 적색이고 test-ci 스스로 「이 적색은 못 믿는다」를 냈다 — 내 변경 범위 밖이라 재실행은 다음 세션 몫으로 둔다. |
