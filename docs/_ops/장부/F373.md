| F373 | 2026-08-12 | 실수 | 회귀 1593 초록 · 장부 「✅종결」 — 그런데 그 SQL 은 «파서에 먹인 적이 0회» 라 어느 DB 에도 앉은 적이 없었다  실측 2026-08-12. 리허설에 밀린 마이그 6조각을 붓다가 ④에서 42601 로 죽었다. `season_c11`·`season_review_c11` 두 조각이 주석 안에 슬래시+별표를 적어(「`compass/…` 두 경로」의 꼬리) **Postgres 블록 주석이 중첩되는 성질** 때문에 파일 끝까지 안 닫혔다. 즉 파싱 자체가 실패한다.  무게는 「오타」가 아니라 **무엇이 그것을 못 잡았나**에 있다:   · 두 조각은 저장소에 「✅종결」로 커밋돼 있었다(메모리 season-review-vessel: 「①②③④ 전건 종결 ·     회귀 60 · 1518/1518」).   · talk 회귀 1593개가 전부 초록이었다.   · 그런데 리허설 19조각 목록에도 운영 16조각 목록에도 **없다** — 한 번도 앉은 적이 없다.  축이 F371(종결 선언이 배포와 갈렸다)과 다르다. 거긴 「배포를 안 했다」였고 여긴 **「배포가 원리상 불가능했다」**다. 회귀는 파일의 «내용»을 검사했지 그 파일을 **실제 소비자 (Postgres 파서)에게 먹여보지 않았다.** 그래서 초록과 「먹힌다」가 갈렸고, 둘이 같은 모양이었다.  일반화 — 이 저장소에 같은 모양이 더 있다:   산출물이 «다른 엔진»에 먹히는 것들(SQL·JSON 계약·appsscript 매니페스트·HTML)은 우리 회귀가   읽을 수 있다는 것과 그쪽 파서가 받는다는 것이 다른 사건이다. 우리 층에서 초록인데   저쪽에서 죽는 자리는 **배포 시점에야 보인다** — 그때는 이미 「종결」로 적힌 뒤다.  해소분(같은 커밋 `54d04d4`):   · 두 조각 수리 + 체크섬 2슬롯×2 재계산 + 합본 재생성 → 리허설 6/6 착지(19→25조각).   · 회귀 `tests/마이그레이션주석.test.js` 신설 — Postgres 규칙 그대로(줄주석·문자열·달러 인용     존중, 주석은 주석끼리만 중첩) 깊이를 세고 조각 25 + 합본을 잰다. 변이 3/3 잡힘.     ⚠ grep 개수 세기는 안 된다 — `cron_c10` 이 실측 거짓양성이다(닫는 글자가 하나 많은데 정상).  📏 이 함정의 밟기 쉬움이 같은 세션 안에서 **3회** 실측됐다: 경고문을 조각에 적으며 두 번,    회귀 파일 머리말에서 한 번(JS 가 죽었다). 프로즈 「조심하라」가 왜 처방이 아닌지의 표본.  남은 축(이 신고로 닫지 않는다): 운영 DB 는 16조각 · Edge Function 은 다름 7·미측정 6 — 운영 배포는 승인 밖이라 손대지 않았다. | |
