| F306 | 2026-08-10 | 마찰 | `/security-review` 가 **내 변경이 아니라 옆 세션의 문서를 읽고** 판정을 내려 했다. 2026-08-10 실측: 내 배포 대상은 `Code.js`·`엔진_두뇌.js`·`상담AI.js` 의 미커밋 리터럴 3줄이었는데, 스킬이 넘긴 DIFF CONTENT 본문에는 그 셋이 **한 줄도 없었고** 대신 세션 `local_ed0a2064` 가 방금 커밋한 인계문·보드 마크다운 5벌이 들어 있었다.  원인: 그 통로는 「브랜치가 origin 보다 앞선 커밋」을 diff 대상으로 잡는다. 나는 앞선 트랙을 이미 push 했고(`fad4da0f`), 그 사이 남이 3커밋을 얹었다. 그래서 「내 앞선 커밋 = 남의 것」이 됐고, **정작 내 배포 대상은 unstaged 라 본문 밖**이었다. `git status` 절에는 내 파일이 보이므로 겉보기엔 정상 실행이다.  왜 위험한가: 이건 `/deploy` 4단계의 **판단 층 게이트**다. 기계 검사(`deploy-security-check.js`)는 배포 표면을 따로 보지만, 처음 보는 결함을 보는 층은 여기뿐이다. 남의 문서를 읽고 「취약점 없음」이 나오면 나는 그것을 **내 코드의 보안 검토 통과로 기록한다** — 미실행이 통과와 같은 모양으로 나오는 F207 계열이고, 새는 방향은 언제나 「통과」다. 세션이 10개 도는 시간대엔 우연이 아니라 상시 조건이다.  이번엔 리터럴 3줄이라 실해가 0이었지만, 그건 운이지 장치가 아니다.  처방 후보 ① `/deploy` 4단계가 **검토 대상 파일 목록을 스킬에 명시로 넘긴다**(브랜치 추론에 맡기지 않는다). ② 검토 결과를 받을 때 **그 보고서가 내 배포집합 파일을 실제로 언급했는지** 대조하고, 안 했으면 「미실행」으로 적는다(0건과 미실행을 가른다 — 배포 보고에 층을 밝히는 것과 같은 축). | tools/보안검토과녁.js 신설 + deploy SKILL 4단계 ㉠㉡ 배선(bc68d225). 신고문 처방 후보 둘을 그대로: ①과녁을 명시로 뽑는다(배포집합 ∩ 미커밋 = clasp 가 실어 나갈 아직 git 에 없는 바이트 · 배포집합은 tools/배포판점검.js 정본에서 파생 · 출력이 /security-review 에 붙일 문장까지 낸다) ②보고서가 그 과녁을 실제로 언급했는지 대조해 0건과 미실행을 가른다(종료 3=미실행). 새는 방향 넷을 못박았다: 종료 코드가 계약(0 돌았다·2 못 쟀다·3 미실행 · 글자로 안 가른다) · 못 잼은 0건이 아니다(git 실패·보고서 못 읽음 전부 2) · 과녁 0건은 미실행이 아니다(F103 — 여기서 적색을 내면 정상 배포가 상시 막힌다) · 재는 것은 「봤나」까지고 안 언급된 과녁은 통과 판정 안에서도 이름을 대고 못 가른다고 적는다. 언급 판정은 경로+파일이름 둘 다 받는 느슨한 쪽(좁게 틀리면 거짓 적색이 배포를 막는다). 테스트 이음매 SYNK_REVIEW_TARGETS 는 켜지면 스스로 밝힌다. 회귀 tests/보안검토과녁.test.js 신설 14건 · 14/14 · 변이 4/4 물림(①못잼을 0건으로 ②미실행을 통과로 ③이름 언급을 거짓 적색으로 ④이음매 조용히). ⚠③은 처음 살아남았다 — 픽스처 과녁이 Code.js(최상위)라 경로==이름이어서 그 갈래를 안 지났고, tools/lib/보드.js 를 넣어서야 물렸다. 층: test-ci 적색 1건은 내 것 아님(엔진_콘텐츠AI.js 의 [vNEXT] = 옆 세션 ade4741d v9.228) · 이 커밋은 배포집합 무접촉 · 원격 CI 는 Actions 소진으로 미검증 |
