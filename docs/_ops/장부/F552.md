| F552 | 2026-08-17 | 마찰 | F181 의 교정이 **과교정**이라 반대 방향으로 새고 있었다 — 게이트가 둘인데 인덱스 머리말이 하나를 「없는 것」으로 적었다.  무슨 일: 유호님이 「MEMORY.md 가 20KB 다 · 훅이 24.4KB 읽기 한계·17.1KB 목표를 경고한다」며 9차 압축을 지시했다. 그런데 MEMORY.md 머리말 4행은 **「자(KB)는 훅이 「상한 아님」 명기(F181 오역)」** 이라 적혀 있었고, `memory-index-guard.js:47-48` 도 「17.1KB 를 내라는 용량 훅은 저장소에 없다(hooks·tools·tests 전량 grep)」라고 못박고 있었다. 저장소 전체를 grep 해도 24.4/17.1 은 안 나왔다. 그래서 나는 **「전제가 틀렸다 · KB 게이트는 없다」로 판정했다** — 훅 `--check` 도 「위반 0건(110줄/139 · 250자 초과 0줄)」이라 그 판정을 뒷받침했다.  틀린 지점: KB 게이트는 **저장소가 아니라 하네스**에 있다. MEMORY.md 를 Edit 한 순간 PostToolUse 로 실물 문구가 떴다 — "The memory index at MEMORY.md is 19.8KB, approaching the 24.4KB read limit. Compact it to under 17.1KB now". 자÷1024 로 재는 진짜 게이트고, 유호님 말이 처음부터 정확했다.  왜 샜나: F181 은 「17.1KB 를 내라는 **용량 훅이 저장소에 있다**」는 오해를 고친 것이었는데, 교정문이 **「KB 는 상한이 아니다」** 라는 더 넓은 문장으로 굳었다. 참인 명제(저장소에 없다)를 거짓 명제(그런 상한이 없다)로 일반화한 것이다. 저장소 안만 grep 하면 영원히 확증되는 모양이라 **자기교정이 안 된다** — 실물은 저장소 밖(하네스)에 있고 Edit 를 쳐야만 나타난다. CLAUDE.md 맹점 ④(장치가 맞는 얼굴로 틀린 값을 내는 자리) 그대로다.  대가: 압축 트랙의 첫 판정을 통째로 뒤집어야 했고, 「분모를 밝히라」는 지시에 **없는 게이트를 근거로 「할 일 없음」에 가까운 답**을 낼 뻔했다.  곁가지 실측(같은 자리에서 나온 것): 머리말이 8차를 「22.2→17.1KB」로 적었는데 지도.md 의 8차 스냅샷 제목은 「20101자/99줄 → 18223자/102줄」이다. 두 숫자가 서로 안 맞는다 — KB 표기가 어느 층에서 왔는지 아무도 모르는 채 인용되고 있었다는 증거다. | MEMORY.md 머리말 4행을 「상한 둘(층이 다르다)」로 다시 씀 — 훅=줄 139·줄당 250자(memory-index-guard.js --check) · 하네스=17.1KB 아래(24.4KB 읽기 한계 · 자÷1024). 한쪽만 적혀 있어서 다른 쪽이 「없는 것」이 됐던 자리라, 두 층을 같은 줄에 나란히 두는 것이 처방이다. |
