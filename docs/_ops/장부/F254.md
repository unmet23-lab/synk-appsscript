| F254 | 2026-08-08 | 마찰 | 배포 게이트가 값싼 검사보다 비싼 검사를 먼저 돌려, 「git 한 줄이면 알 답」을 주는 데 5분을 쓰고 그 탓에 자기 회귀가 한 번도 완주하지 못했다. 실측 2026-08-08: clasp-guard 의 검사 순서가 1)구문 2)전 스위트 3)배포 파일 미커밋 4)미push 였다. 검사 2 는 루트 프로젝트에서 tests 107개를 전량 도는데 실측 5분 20초다 — 훅 등록층의 timeout 120초를 2.7배 넘긴다(즉 실세션에서 완주한 적이 없을 수 있다). 그 뒤에 오는 검사 3 은 git status 한 줄이다. 결과가 두 겹으로 나쁘다: 첫째, 커밋만 하면 풀릴 차단을 알려주는 데 5분을 쓴다. 둘째, tests/clasp-guard.check.js 의 미커밋 차단 검사가 훅 한 번당 5분을 물어 600초 하네스 타임아웃마저 넘겨 죽었고, 그래서 배포 파일 15종에 대한 그 검사가 한 번도 안 돌았다 — F236·F238 이 「전체 점검 완주 미확인」으로 남긴 자리가 정확히 이것이다. 새는 방향은 「안 돈 검사가 초록과 같은 모양」이다. 곁들여 같은 실측에서 내 F240 축 B 수리의 결함도 하나 나왔다: 원격초록이 못 잰 이유 세 가지(타임아웃·빈 출력·spawn 실패)를 「gh 조회 실패」 한 문구로 뭉개, 게이트 483초 실행에서 진짜 원인(원격ci 가 60초를 넘김 — git fetch 가 남의 세션과 경합한 것으로 보인다)을 못 가리키고 gh 인증을 보라는 처방을 냈다. 미실행 두 종류가 같은 모양이면 안 된다는 F210·F215 자리를 내가 새로 하나 만든 셈이다. | 순서를 1)구문 3)미커밋 4)미push 2)전 스위트 로 바꿨다. 텍스트를 옮기는 대신 검사 2 를 함수로 묶고 검사 4 뒤에서 부른다(브레이스 구조가 그대로라 diff 가 작다). 맨 앞에 problems.length 면 즉시 반환 — 이미 막힐 것이 확정됐으면 스위트를 아예 안 돈다. 잃는 것은 없다: 커밋한 뒤 재시도할 때 그 스위트가 돈다. 실측이 증거다 — tests/clasp-guard.check.js 가 4분 52초에 완주했고 전 22건 ok 다(배포 파일 15종 포함). 직전 실행은 첫 파일에서 ETIMEDOUT 으로 죽어 그 15건이 한 번도 안 돌던 것이다. 순서 자체에는 회귀를 안 붙였다 — 시간 단언은 플레이크가 되고, 이 완주 실측이 더 강한 증거다. 두 번째 갈래(뭉개진 사유)는 셋을 갈라 각자 다른 말을 하게 했다: 결과가 통째로 없다 · N초 안에 안 끝났다(신호명 포함) · 출력 없이 종료(exit 코드). 회귀 3 추가로 셋이 서로 다른 문구인지 못박았다(25/25 · 변이 6/6). |
