| F537 | 2026-08-17 | 마찰 | `docs/이해대장.html` 이 원리적으로 초록에 못 머문다 — 생성기가 «수렴하지 않는다». 그래서 `tests/대장동봉.test.js` 3건이 주인 없이 빨갛고, 그 적색은 정본을 건드리는 **모든 세션의 커밋**을 pre-commit 게이트에서 막는다. ■ 실측 2026-08-17 (`local_4e976ff7` · F526 트랙 반입 뒤 master CI 모사에서 발각): ①as 작업본은 깨끗했다(`docs/이해대장.html`·`계약/`·`tools/이해대장.js` 미커밋 0) — 즉 이 적색은 «커밋된 판»의 것이지 남의 미커밋 탓이 아니다. ②`node tools/이해대장.js --검사` 가 그대로 재현했다. ③다시 그려 담고 push 했다(`31af876d` — 도달 0인 사건 3→2종 · 빨간 칸 5→4 · 지금 2→1 · #Q99 2/5 갚음 `07791965` 이 화면에 안 따라온 분). ④**그런데 곧바로 같은 명령을 한 번 더 돌리니 값이 되돌아갔다** — 2종/4칸 → **3종/5칸**. 그 사이 내가 한 일은 없다. 즉 연속 두 회차의 출력이 다르다. ■ 왜 아픈가 — 「다시 그려서 담아라」라는 처방이 **따를 수 없는 처방**이다(F103 의 모양). 담는 순간 값이 이미 다른 쪽일 수 있고, 다음 세션이 또 다시 그려 담으면 그 커밋이 다시 뒤집는다. 실제로 커밋 이력에 「화면 재생성」이 반복해 등장할 근거가 여기 있다. 그리고 새는 방향이 나쁘다 — **정본을 건드리는 남의 커밋이 막힌다.** ■ 원인은 «후보»다(안 쟀다 — 여기서 단정하지 않는다): 화면 본문이 「실값 정본 = SYNK-talk/lib/이벤트검증.js」라고 스스로 적고 있고, 그 형제 저장소는 지금 미커밋이 있다(`계약/수집_교정_계약.json` M). 생성기 입력에 **남의 저장소 작업본**이나 매분 갱신되는 장부가 섞여 있으면 이 모양이 정확히 난다. ▶ 여는 세션이 먼저 잴 것: ㉠ `tools/이해대장.js` 의 입력 목록을 열거하고 그중 **repo 밖·형제 작업본·시각 의존**이 있는지 본다(F296 의 축) ㉡ 있으면 처방은 「다시 그리기」가 아니라 **입력을 커밋된 것으로 못 박기**다(형제는 `git show origin/master:<경로>` 로 읽는 식). ㉢ 못 박을 수 없는 입력이면 그 값은 화면에서 빼거나 「측정 시각」과 함께 낸다 — 지금은 그 값이 판정처럼 보인다. ⚠ 내가 민 `31af876d` 은 두 값 중 한쪽일 뿐이다. 되돌리지 않았다 — 어느 쪽도 「맞는 값」이 아니라서 되돌려도 같은 상태다. | ⏸ 내 트랙(#Q99 3/5)이 아니라 안 닫는다 — 대신 재현 근거 하나를 더 붙인다. 2026-08-17 local_b20caad9: 내 래칫 변경(시트층 도달0 5→4) 때문에 화면을 다시 그렸는데, 같은 생성기가 앱 사건 층을 «2종/빨간 칸 4» → «3종/5»로 **되돌렸다**(31af876d 가 민 값의 반대쪽). 즉 진동은 F537 이 적은 대로 재현되고, 이번엔 «내 축과 남의 축이 한 파일에 묶여» 나왔다 — 생성기가 파일을 통째로 쓰므로 내 축만 담을 방법이 없어 git-scope-guard 를 BYPASS 로 지났다(근거 = F537 신고자 본인의 「어느 쪽도 맞는 값이 아니라 되돌려도 같은 상태다」). 📏 여는 세션에 재료 하나 추가: **얼린 detached 워크트리에서는 이 검사가 초록이다(37건 fail 0)** — 그 트리가 형제 저장소에 못 닿아 6건이 skip 되고, 문제의 값이 바로 그 skip 구간에서 나온다. 즉 ㉠의 «repo 밖 입력»은 실재하고, 얼린 초록을 이 축의 근거로 쓰면 안 된다. |
