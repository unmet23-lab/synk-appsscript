| F328 | 2026-08-11 | 마찰 | 새 에이전트 정의(.claude/agents/심문자.md)는 그걸 만든 세션에서 안 잡힌다 — 같은 세션에서 만든 스킬은 잡히는 비대칭이라 「등록 끝」으로 읽고 바로 호출했다가 not found 로 튕겼다. 폴백을 조용히 쓰면 그 판이 「심문자 정의로 돌았다」로 기록된다. | ⏸ 원인(하네스 비대칭 — 새 에이전트 정의는 만든 세션에서 미인식)은 저장소에서 못 고친다. 피해(조용한 폴백이 「심문자로 돌았다」로 기록됨)는 F375 던짐 행 9칸의 모델·효력 칸이 절반을 진다 — 실사용 subagent 칸은 미착지(재발 시 착수 후보). 남는 규칙=새 정의는 만든 세션에서 부르지 않는다(신고문 그대로) |
