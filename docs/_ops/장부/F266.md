| F266 | 2026-08-09 | 마찰 | git-scope-guard 8의 기억 파일(commitscope-*)이 실사용에서 한 번도 안 써져, 처방 「같은 명령 재실행하면 통과」와 F141 3회차 탈출구가 둘 다 영영 안 열린다 — 남는 문이 GIT_SCOPE_BYPASS 하나뿐이라 우회가 정상 통로가 된다(F103 재현 · F141→F229→F264 에 이은 4번째). 실측 2026-08-09: SYNK-talk 코드 3파일을 같은 명령으로 2회 커밋 시도했는데 둘 다 「지금 이 명령이 실어 갈 내용」(=회차 1)이 떴다. 상태 폴더 247개 중 commitscope-* 실사용 파일 0건이고 존재하는 것은 전부 테스트 픽스처(scope8-state-*/local_me)뿐이다. 쓰기 경로 자체는 멀쩡하다 — 같은 코드(stateDir+projectKey+safeId)를 스크래치패드 스크립트로 재현하면 쓰기 OK. 원인 미특정: 훅 프로세스가 그 블록에 도달하지 않거나 훅 안에서 stateDir 이 다른 곳을 가리킨다. 곁들여 3파일 전부 이 세션의 Edit 산출인데 소유가 「모름」으로 떨어졌다(형제 저장소 talk 경로) | |
