| F266 | 2026-08-09 | 마찰 | git-scope-guard 8의 기억 파일(commitscope-*)이 실사용에서 한 번도 안 써져, 처방 「같은 명령 재실행하면 통과」와 F141 3회차 탈출구가 둘 다 영영 안 열린다 — 남는 문이 GIT_SCOPE_BYPASS 하나뿐이라 우회가 정상 통로가 된다(F103 재현 · F141→F229→F264 에 이은 4번째). 실측 2026-08-09: SYNK-talk 코드 3파일을 같은 명령으로 2회 커밋 시도했는데 둘 다 「지금 이 명령이 실어 갈 내용」(=회차 1)이 떴다. 상태 폴더 247개 중 commitscope-* 실사용 파일 0건이고 존재하는 것은 전부 테스트 픽스처(scope8-state-*/local_me)뿐이다. 쓰기 경로 자체는 멀쩡하다 — 같은 코드(stateDir+projectKey+safeId)를 스크래치패드 스크립트로 재현하면 쓰기 OK. 원인 미특정: 훅 프로세스가 그 블록에 도달하지 않거나 훅 안에서 stateDir 이 다른 곳을 가리킨다. 곁들여 3파일 전부 이 세션의 Edit 산출인데 소유가 「모름」으로 떨어졌다(형제 저장소 talk 경로) | .claude/hooks/git-scope-guard.js ⑧ 기억에 at: Date.now() 한 칸. 원인은 회차 로직이 아니라 기억이 매번 사라지는 것이었다 — sweep(handoff-store 309행)은 at 없는 파일을 나이와 무관하게 지우고, 그 sweep 은 세션이 열릴 때(take)와 닫힐 때(session-end-handoff 40행)마다 돈다. 이 저장소는 늘 세션 여럿이 굴러서 그 기억이 분 단위로 증발했고, 그래서 deny 문이 시키는 「같은 명령을 그대로 다시 실행한다」가 실행 불가능했다(남는 문이 BYPASS 뿐 · F103). 실측은 조건 하나만 바꿔 했다(격리 폴더 A/B): at 없음 = sweep 이 1건 지움·파일 사라짐 / at 있음 = 0건·생존. 신고문의 「commitscope 실사용 0건」은 폴더 오독이 아니라 이 증발의 결과다 — 진짜 상태 폴더는 os.tmpdir()/synk-context-budget 이고 지금은 산 세션들이 쓴 4건이 보인다. 클래스 전수: STATE_DIR 에 쓰는 12곳 중 at 이 없던 건 이 한 곳뿐이고(나머지는 전부 찍는다) tools/ 쪽은 읽기만이라 이 하나로 닫힌다. 회귀 1 신설(tests/git범위가드.test.js) — 처방을 그 가드에 되먹이는 모양이다: 첫 창은 막고, 그 사이 세션 하나가 열렸다 닫히는(=sweep) 조건만 바꿔 재실행이 통하는지 본다. 이 구멍이 숨은 이유는 픽스처가 실사용과 갈라져 있어서다 — 옛 검사는 sweep 을 한 번도 안 불렀고 만진기록도 at 을 안 찍었다(실물 track-collision 107행은 찍는다). 둘 다 실물에 맞췄고, 청소가 픽스처를 지우면 「남을 못 봐서」 초록이 되는 거짓양성이라 sweep 반환값 0 을 함께 못박았다. 84/84 skip 0 · 변이 2/2(①at 제거 ②at=0) — 변이는 격리 사본(SYNK_TEST_SCOPE_HOOK)에만 걸어 실훅은 무손상(F225). 잔여: 신고문 곁들여의 「형제 저장소 talk 경로 소유 모름」은 다른 층이고 F264 수리 뒤 미측정이라 이 건으로 닫지 않는다. |
