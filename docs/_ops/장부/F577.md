| F577 | 2026-08-17 | 마찰 | 밀림 게이지가 「밀 수 있나」를 안 본다 — **갈라진 master 는 문턱 아래에 조용히 앉고, 그 사이 배포 게이트는 따를 수 없는 처방을 낸다.** F570 이 신고한 «생산자»(인계문수거가 push 안 함)의 **소비자 쪽**이고, 그 트랙(`local_1d940103`)이 잡은 파일과 겹쳐 나는 신고만 한다.  ■ 실측 2026-08-17 21:22 (`local_ac18eae8` · 세션 열자마자)   · `안밀린것()` = **7건** · `안밀린_문턱` = **30** → 경보 **안 운다**.   · 나이 눈금 = **0.63시간**(최고령 커밋 20:42) → 이것도 초록. **두 눈금 다 정상으로 읽힌다.**   · 실제 = `git rev-list --count master..origin/master` = **57 뒤처짐**.   · `git push --dry-run origin master` → **`! [rejected] master -> master (non-fast-forward)`**.   그 7건은 「아직 안 민 것」이 아니라 **「밀 수 없는 것」**이었다. 개수가 7이든 1이든 못 민다.  🔑 두 상태가 한 게이지의 같은 구간에 겹쳐 있다   ㉠ 「밀 수 있는데 안 밀었다」 — 문턱 30 은 **이걸** 재도록 실측된 값이다(2026-08-16 · reflog 15일 · push 1610회 · p50=1 · p99=29).   ㉡ 「밀 수 없다(non-ff)」 — **개수·나이와 무관**하다. 그런데 0~29 구간에 앉아 ㉠ 의 침묵을 그대로 물려받는다.   분포로 뽑은 문턱은 ㉠ 의 분포다. ㉡ 에는 문턱이 원리적으로 없는데 같은 문턱을 쓴다 — 맹점 ④(맞는 얼굴로 틀린 값).  🔴 처방이 따를 수 없다 (F103 무늬) — 두 자리에서, 그리고 한쪽은 **배포를 막는다**   · `밀림경보`(`tools/작업가지.js`) 마지막 줄 = `→ git push origin master` **하나뿐**. 문턱 30 에 닿을 무렵이면 이미 뒤처져 있어 그 명령은 반드시 거절된다. 「받아라」는 어디에도 안 나온다.   · `clasp-guard` 검사 4(`.claude/hooks/clasp-guard.js:295`) = `origin/master..master ≠ 0` 이면 **배포 차단** + 처방 `git push origin master 먼저`. 같은 파일 :261 의 「정상 통로다(BYPASS 아님): git push origin master → node tools/원격ci.js」도 같다. 갈라진 동안 배포하는 **유일한 길이 `CLASP_GUARD_BYPASS=1`** 이 된다 — 그 훅 머리말이 스스로 경계한 「우회 습관」(:38 · :60)을 가드가 자기 손으로 만든다.  ■ 층을 열거해 확인했다(「어디에도 없다」를 안 쓰려고 · F348)   · ahead(`origin/master..master`) 를 재는 곳 3 — `tools/작업가지.js`(안밀린것) · `tools/백업.js:123` · `.claude/hooks/clasp-guard.js:296`.   · behind(`HEAD..origin/master`) 를 재는 곳 1 — `.claude/hooks/repo-staleness.js:74`. **본다.** 그런데 그 훅이 내는 처방은 읽기뿐(`git fetch && git log HEAD..origin/master`)이고 「갈라졌다·못 민다」는 말을 안 한다.   · **두 축을 곱해 「갈라짐」이라 판정하는 자리 = 0.** git 상태로서의 diverge 를 말하는 코드가 저장소에 없다(`갈라` 검색 히트는 전부 「판정을 두 곳에 적으면 갈라진다」라는 다른 뜻이다).  ■ 처방 후보 — 안 지었다(이 신고는 재료다 · 과녁 파일이 남의 활성 트랙이다)   ㉠ `안밀린것()` 이 뒤 축도 함께 재서 `{앞, 뒤, 갈라짐}` 을 내고, `갈라짐` 이면 **문턱·나이와 무관하게** 소리치고 처방을 「받아라」로 낸다. ⚠대가 = 판정이 한 곳에서 나야 하므로 `clasp-guard` 검사 4 도 같은 통로를 써야 한다(두 곳에 적으면 갈라지고, 갈라진 쪽은 「통과」다).   ㉡ `clasp-guard` 검사 4 의 처방만 갈라짐일 때 「받아라」로 바꾼다. ⚠대가 = 값싸지만 밀림경보는 그대로 조용하다.   ⚠ 어느 쪽이든 **차단 자체는 유지한다** — 안전 가드는 다이어트 대상이 아니다(CLAUDE.md 머리말).  ■ F570 과 갈리는 지점 = **층**. F570 은 생산자(`tools/인계문수거.js` 가 push 를 안 해 갈라짐을 «만든다»). 이건 소비자(갈라진 뒤 게이지·게이트가 그 사실을 «못 읽는다»). 생산자를 고쳐도 master 직접 커밋이 있는 한 갈라짐은 다시 나므로 이 자리는 안 닫힌다. | |
