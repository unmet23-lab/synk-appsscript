| F351 | 2026-08-12 | 마찰 | 마찰 · 「쓰는 문자 셋뿐」 규칙이 «커밋 때» 안 발동한다 — 위반은 늘 남이 커밋한 뒤 남이 발견한다  08-12 하루에 옛 글자 적색이 두 갈래로 났고 둘 다 «이미 커밋된 뒤»에 드러났다. ①형제 evals/출력_v2재시도.json:33 U+683C — 모델이 낸 기록이라 그 파일을 고치는 것 자체가 금지인 자리. ②이 저장소 docs/상태기반_과제선택_설계.md:24 · docs/_ops/심문결과/상태기반과제선택_처분표_v2.md:18 U+7AE0 — 한국어 낱말에 한자 뜻풀이를 괄호로 단 자리(「장(U+7AE0)」 — 이 장부에도 그 글자를 «적지 않는다»: `docs/_ops/장부/` 는 옛글자 검사의 면제 밖이라, 신고문에 그 글자를 그대로 실으면 신고가 스스로 위반을 하나 더 만든다. 실측: 이 행이 태어난 순간 적색이 2→3 이 됐다). 커밋 e3cf9614 로 그날 새로 들어왔다.  축은 규칙이 아니라 «발동 시점»이다. 이 규칙을 지키는 장치는 tests/문서문자.test.js 하나이고 그건 스위트를 돌릴 때만 발화한다 — 커밋은 그냥 통과한다. 그래서 위반은 늘 「남이 커밋한 뒤 남이 발견」이 되고, 발견자는 자기 파일이 아니라 손댈 수 없으며(보드에 작업중으로 선언된 파일), 그동안 모든 세션의 test-ci 가 빨갛다. 빨간 게이트는 「남의 미커밋 탓」으로 오독되기까지 한다 — 대기열 P2 줄이 그 오독을 실측으로 적고 있다.  ▶ 후보 처방(판정은 /evolve·유호님 몫): 이 검사를 pre-commit 훅으로 앞당긴다. 스위트 전체가 아니라 «그 커밋에 담기는 경로만» 훑으면 싸다(git diff --cached --name-only). 지금 구조는 「고칠 수 있는 사람」과 「발견하는 사람」이 반드시 다른 사람이 되게 만든다. | 발동 시점을 커밋으로 앞당겼다 — as c6b8bc3a + talk 6fc5b93. 축은 「규칙」이 아니라 「언제 발화하나」였고, 그 축만 옮겼다(규칙·면제는 그대로). ① 판정 1벌: 문자 클래스 정본은 이미 .claude/hooks/lib/옛글자.js 였는데(F298) 파일 층 정의만 tests/문서문자.test.js 안에 살아서, 커밋 층을 세우려면 사본을 만들 수밖에 없는 구조였다 — 그게 이 마찰의 뿌리다. tools/lib/옛글자.js 가 그 정본에서 source 를 물려받고(g 플래그는 뗀다 — 전역 정규식의 .test() 는 lastIndex 를 들고 다녀 호출마다 답이 뒤집힌다), 스위트도 커밋 층도 같은 모듈에서 읽는다. 소비자 넷 · 정의 하나. ② 호출부 tools/옛글자검사.js --cached — 뿌리를 git 으로 실측해 어느 저장소에서 돌려도 된다. 작업 트리가 아니라 인덱스를 읽는다(커밋될 실물이 그쪽). 면제(장부·인계문·evals/출력_*)를 그대로 물려받는다 — 안 그러면 이 가드가 「옛 글자가 샜다」는 신고 자체를 불가능하게 만든다(이 행이 태어날 때 실측한 그 자리). ③ 등록: as tools/githooks/pre-commit(+설치본) · talk tools/precommit.js. 없으면 막지 않고 「못 했다」고 말하고, 있는데 0 아닌 값이면 막는다 — 부분 체크아웃에서 차단으로 바꾸면 그 세션은 아무것도 커밋 못 한다(F103). ④ 비용: 파일마다 git show 를 띄우니 200파일 커밋 6.7초였다(Windows 실측). cat-file --batch 한 프로세스로 접어 176ms · 파일 수 무관. 느린 가드는 결국 꺼진다. 회귀 tests/옛글자훅.test.js 15건(변이 2/2) — 진짜 git commit 이 막히는 것까지 밟는다(종료코드가 훅 밖으로 전해지는가). talk tests/precommit.integration.test.js +3. F083 재주입 4/4: 이 행이 적은 두 자리(docs/상태기반_과제선택_설계.md · docs/_ops/심문결과/…_v2.md 의 U+7AE0)는 «막힘», 모델 출력 기록(evals/출력_v2재시도.json 의 U+683C)은 «통과»로 갈렸다. CI 모사 2859/0. |
