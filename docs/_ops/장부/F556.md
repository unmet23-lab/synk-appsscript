| F556 | 2026-08-17 | 마찰 | 🎫 로 «놓인» 줄을 `node tools/대기열.js --집기` 가 여전히 「이미 남이 잡았다」로 읽는다 — 정본이 낸 유일한 예외가 도구층에서 막힌다(F103 무늬).  실측 2026-08-17 (`local_779db4c2`): CLAUDE.md 세션 규약은 「남의 줄을 내 트랙으로 이어받지 않는다 — **예외는 상태 칸에 `🎫` 가 붙은 일감 하나뿐**(주인이 「나는 이걸 안 한다」고 스스로 놓은 것)」이라고 못박는다. 그런데 `#Q99` 의 주인(`7fbc725a`)이 PR #56 에서 상태 칸에 「🎫**①사유 실측만 하고 놓는다** — 나는 ②부터 안 한다(코드 0줄 · 갚음 아님)」를 찍고 마감했는데도, `--집기 "#Q99"` 는 그대로 점유로 판정하고 비켜나라고 낸다.  원인은 결함이 아니라 **층이 갈린 것**이다: `tools/대기열.js` 의 `행()` 은 점유를 **선언 칸(트랙·파일)만** 보고 상태 칸은 일부러 안 본다(2026-08-13 실측 — 상태 칸의 «인용»을 선언으로 읽으면 빈 자리가 잠긴다). 🎫 는 바로 그 안 보는 칸에 산다. 즉 board-guard ④ 는 이미 「상태 칸의 🎫 는 놓은 자리지 선점이 아니다」라고 판정해 뒀는데, 대기열의 점유 판정만 그 결론을 못 받는다.  그래서 지금은 정본을 따르는 길이 **도구를 우회해 보드 줄을 손으로 쓰는 것**뿐이다 — 따를 수 없는 처방이 우회를 정상 통로로 만드는 그 무늬다.  ⚠ 처방을 낼 때 갈라야 할 것: 🎫 는 이 저장소에서 **두 뜻**으로 쓰인다 — ㉠「내가 안 한다, 남이 집어라」(놓은 자리) ㉡「이건 유호님 손이 필요하다」(🎫발행=유호 손 · 🎫유호=Tripo 키). ㉡ 을 「비었다」로 읽으면 세션이 유호님 몫을 집는다. 상태 칸을 통째로 열지 말고 ㉠ 만 좁게 여는 것이 안전한 방향이다. | 판정을 `보드lib.놓은줄()` **통로 하나**로 모으고 `대기열.js` 가 그 결론을 받게 했다(PR #81 · `local_ac18eae8`). 신고가 짚은 그대로 원인은 결함이 아니라 **사본**이었다 — `board-guard` 는 F285 로 이미 「놓은 자리는 선점이 아니다」라고 판정해 뒀는데 그게 훅 안의 사본이라, 같은 규약을 읽는 대기열 점유까지 오지 않았다. 이제 훅 쪽은 껍데기(`function 놓은줄(line) { return 보드lib.놓은줄(line); }`)만 남는다.  ■ 신고가 「갈라야 한다」고 경고한 축은 **실측이 다르게 말했다** — 그래서 안 갈랐다   보드 표줄 **1097**(아카이브 + 세션별 조각 전량) · 상태칸 🎫 스팬 **70** 을 전수로 세니:   · ㉠「내가 안 한다, 남이 집어라」 = **69**(「🎫다음=…」·「🎫잔여=…」·「🎫이어받아도 된다」).   · ㉡「이건 유호님 손이 필요하다」 = **0**. 신고가 든 두 예(`🎫발행=유호 손`·`🎫유호=Tripo 키`)는 **메모리 인덱스 줄**이지 보드 상태 칸에 온 적이 없다. 층이 다르다. 없는 결함에 장치를 세우면 그 장치가 다음 오탐이 되므로 ㉠/㉡ 을 가르지 않았다.   · 대신 **진짜 오탐축이 따로 있었다**: 부정 「🎫없음」 **1건**(실데이터). 사본의 `includes('🎫')` 가 이걸 「놓았다」로 읽었고 새는 방향이 「연다」였다 — 남의 산 줄이 열린다. 표식 **바로 뒤**의 부정만 좁게 뺐다(넓히면 「🎫 다음=… 없다」 같은 정상 문장이 죽는다 · 회귀로 양쪽을 못박았다).  ■ 규칙 하나를 더 세웠다 — **산 선언이 이긴다**   놓은 줄과 산 줄이 같은 자리를 물면 답은 「잡혔다」다. 한 사람이 놓았다고 다른 사람의 선언까지 열리면 그게 중복 구현이고, 그건 못 되돌린다. 놓은 줄만 걸렸을 때만 연다.  ■ 조용히 열지 않는다(F278)   대기열 출력에 「🎫 주인이 놓은 자리 N건 — 점유가 아니다」 절을, `--집기` 에는 「그 줄을 고치지 마라 · 내 파일에 새 줄」 안내를 냈다. 비켜 준 자리에 침묵을 두면 다음 세션이 또 헤맨다.  ■ 잰 것   · 회귀 신설 9(대기열점유 7 · board-guard 2) · `tests/대기열점유.test.js` **34 pass · skip 0** · `tests/board-guard.test.js` 116 pass(F285 3건 그대로 산다).   · **변이 8/8 · 구멍 0 · 미적용 0.**   · 🔴 **변이가 내 회귀의 결함 둘을 잡았다** — `--집기` 검사가 **닫힌 번호(#Q99)** 를 상수로 써서 「열린 항목에 없다」로 조기 반환했고, 그래서 망가뜨려도 초록이었다(미실행이 통과와 같은 모양 · F207). 열린 번호를 목록에서 가져오게 고치고 `집기()` 에 보드 주입구를 냈다.   · CI 모사 전량 fail 0. 🔴 원격 CI 는 0건(Actions 소진 · 리셋 9/1) — 그 층은 **미검증**이다.  ■ ⚠ 착지 시점 효과는 0이다 — 숨기지 않는다   실측: 활성 표줄 40 중 놓은 줄 **1**(`7fbc725a` 의 #Q99 — 신고가 재현한 바로 그 줄) · 그 줄이 지금 여는 대기열 자리 **0** · 주인이 「놓은 줄」→「산 줄」로 정정된 것 **0**. 신고 시점의 사례가 그 사이 닫혔기 때문이다. 구조는 남는다: 다음에 누가 🎫 를 놓는 순간부터 도구가 그걸 읽는다.  ■ 안 한 것 — 「🎫 를 **말하는**」 상태 칸(🎫 자체를 논하는 줄)은 여전히 놓음으로 읽힌다. 방향이 「연다」라 새는 쪽이고, 실측 0건이라 지금은 남긴다. 좁히려면 사람이 «놓았다»를 따로 적는 규약 변경이 필요하다 — **알면서 남기는 값**이라 코드 머리말에 적어 뒀다. |
