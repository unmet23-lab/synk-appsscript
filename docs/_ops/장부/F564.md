| F564 | 2026-08-17 | 마찰 | 형제 저장소(SYNK-talk)를 가리키는 거짓 인용이 워크트리에서는 조용히 통과했다 — 새는 방향이 「통과」라 증상이 없었다.  `tools/lib/보드낡음.js` 의 `저장소들()` 이 형제를 `root/../SYNK-talk` 에서만 찾았다. 그런데 워크트리는 `<주저장소>/.claude/worktrees/<가지>/` 에 살아서 그 자리엔 형제가 없다. 그래서 워크트리에서는 목록이 `[as]` 로 줄고, talk 파일 인용이 「못잼」으로 접힌다. citation-guard 는 **적색만** 알리므로 그 접힘은 아무 소리도 안 낸다.  실측 08-17 — 같은 문장 네 벌을 ROOT 만 바꿔 재기:   메인 ROOT     as맞는 조용 / as거짓 경고 / talk맞는 조용 / talk거짓 **경고**   워크트리 ROOT  as맞는 조용 / as거짓 경고 / talk맞는 조용 / talk거짓 **조용 ← 구멍**  CLAUDE.md 가 코드 트랙에 워크트리를 「의무」로 만든 뒤라 이 통로는 상시 열려 있었다. 이 저장소 문서는 talk 파일을 수시로 인용한다(대기열 줄 여럿이 `talk docs/발주_게임모듈.md` 식으로 근거를 댄다) — 그 인용이 워크트리에서 쓰이면 한 번도 검사되지 않았다.  처방 = 형제를 「내 옆」이 아니라 **주저장소 옆**에서 찾는다. `as` 뿌리는 워크트리 그대로 둔다 — 그 세션이 지금 고치는 판이 거기라, 주저장소로 밀면 아직 커밋 안 된 새 파일이 「어디에도 없다」로 적색이 되어 고치려던 것보다 나쁜 방향이 된다. 꼬리는 통째로 벗긴다(워크트리 이름은 `feat/foo` 처럼 마디가 여럿일 수 있다).  대가를 적는다: 판정이 **경로 모양**에 기댄다. 워크트리를 `.claude/worktrees/` 밖에 손으로 만들면 이 규칙은 그 판을 못 알아보고 수리 전으로 되돌아간다 — 그때도 조용하다. 지금 통로(EnterWorktree·`작업가지.js`)가 전부 그 자리에 만들기 때문에 경로로 뒀고, 통로가 늘면 이 줄이 먼저 낡는다.  ■ 짓는 중 실제로 잡아 고친 것 — 빈 판을 판정으로 접을 뻔했다 첫 재측정에서 `q99-exit-log-reach-5of5` 워크트리를 ROOT 로 잡았는데 그 워크트리는 **이미 거둬진 뒤**였다. 없는 디렉터리라 색인이 비었고 네 문장이 전부 「못잼」으로 나왔다 — 자기 저장소 파일의 거짓 인용까지 못잼이라 앞선 실측과 대놓고 모순됐는데, 그 모순을 안 봤으면 「워크트리에선 아무것도 안 잡힌다」는 더 큰 거짓 판정을 낼 뻔했다. `git worktree list` 로 산 워크트리를 확인하고 다시 쟀다. 회귀 픽스처에는 그래서 「빈 판이면 던진다」를 넣지 않고 **애초에 픽스처를 직접 짓는** 쪽으로 갔다(실저장소를 재면 이 기계에 형제가 실제로 있어 옛 형태로도 초록이다). | 저장소들() 이 형제를 주저장소 옆에서 찾도록 고침(워크트리 꼬리를 통째로 벗김 · as 뿌리는 워크트리 그대로) + tests/형제저장소해석.test.js 신설 5건 · 변이 4/4 구멍 0 · CI 모사 4123 fail0 |
