| F425 | 2026-08-14 | 실수 | 운영 배포 「돈다」를 재려고 만든 탐침이 **게이트웨이의 답을 함수의 답으로 읽었다** — 12/12 초록이 나왔는데 워커는 한 번도 안 떴다 (#Q68 · 2026-08-14 실측)  무슨 일이었나. talk Edge Fn 12벌을 운영에 재배포한 뒤, 「매니페스트 일치는 돈다의 증거가 아니다」에 따라 부팅을 따로 재려고 인증 없이 각 함수를 GET 했다. 12개 전부 `401 {"code":"UNAUTHORIZED_NO_AUTH_HEADER","message":"Missing authorization header"}` 가 왔고, 탐침은 그것을 「함수 자기 코드가 답했다 = import 사슬 통과」로 찍었다. 실제로 그 401 은 **Supabase 게이트웨이**가 낸 것이다 — `verify_jwt` 가 켜져 있으면 워커를 **띄우기 전에** 자른다. 즉 부팅은 하나도 증명되지 않았고, 동봉이 깨졌더라도 같은 12/12 초록이 나왔을 것이다.  🔑 급소 — **잰 층이 다르다**(F089·F183 계열)인데 «잰 값이 그럴듯해서» 안 보였다. 401 은 이 저장소에서 이미 「살아서 인증 거는 중」의 신호로 쓰인 적이 있어(08-13 correct 운영 배포 기록) 낯익은 얼굴이었다. 낯익은 값이 나오면 어느 층에서 왔는지를 안 묻게 된다.  🔑 가른 방법 = **응답 «몸의 모양»으로 층을 가른다.** 게이트웨이 꼴(`UNAUTHORIZED_NO_AUTH_HEADER`)과 함수 봉투 꼴(`contract_ver`·`CONTRACT_VER_MISSING`·`POST 만 받는다`)은 키가 다르다. anon 키로 게이트웨이를 지나 다시 재니 12/12 가 함수 자기 봉투로 답했다(그때가 진짜 부팅 확인이다).  ⚠ 다음에 물을 것 — 「이 응답을 낸 것이 **내가 배포한 코드**인가, 그 앞에 선 무엇인가」. 플랫폼 앞단(게이트웨이·CDN·프록시)이 있는 층을 잴 때는 **성공 코드가 아니라 응답의 «출처 지문»**을 봐야 한다. 상태 코드만으로는 앞단과 뒷단이 같은 모양이다. | 탐침을 응답 «몸의 모양»으로 층을 가르게 고쳤다(게이트웨이 꼴 → 🟡 못 가름 · 함수 봉투 꼴 → ✅) + anon 키로 게이트웨이를 지나 재측정 — 운영 12/12 가 자기 봉투로 답했다 |
