| F412 | 2026-08-14 | 실수 | 처방이 그 처방을 «검사하는 도구»의 눈을 멀게 했는데 나는 못 봤다 — 검수가 잡았다.  숙제 서클 넘침 수리에서 `circleFitCard_`(안 들어가면 보조 줄부터 걷는다)를 세웠더니, 그 조판을 검사하는 `tools/서클조판.js --넘침시험` 이 **4/4 → 0/4** 로 떨어졌다. 그 시험은 카드의 `kept` 를 길게 만들어 `circleSheetHtml_` 에 넣는 방식이었는데, 새 걷기가 렌더 «전»에 그 긴 줄을 먼저 빼 버려 DOM 에 넘치는 줄이 남지 않은 것이다. 나온 빨강은 「측정기가 눈이 멀었다」와 **글자 그대로 같은 모양**이라 원인을 못 가른다.  내가 그 사이에 회귀 42/42 와 변이 12/12 를 초록으로 봤는데도 못 봤다 — 그 검사들은 «엔진»을 겨눴고, 눈이 먼 것은 «도구»였다. 세 회차 뒤 GPT 검수가 잡았다.  전이되는 축: **어떤 처방이 「검사 대상의 모양」을 바꾸면, 그 모양을 전제로 삼던 검사·픽스처가 같이 죽는다.** 픽스처가 「버그가 아직 있을 것」을 요구하면 안 된다는 CLAUDE.md 가드 맹점 ②의 거울상이다 — 여기서는 픽스처가 「내가 방금 없앤 상태」를 요구하고 있었다. 처방을 넣을 때 그 기능을 «검사하는 도구»가 무엇을 전제하는지 같이 본다.  해소: 부풀리기를 브라우저 «안»으로 옮겨 렌더가 끝난 DOM 에 직접 긴 줄을 넣는다(제품 코드에 시험용 우회 0). 4/4 복구 · 회귀 `tests/숙제서클.test.js` 가 「어느 층에서 부풀리는가」를 못박고 변이 6/6 이 그 검사를 되묻는다. 커밋 e61ef44c [v9.230]. | 부풀리기를 브라우저 안으로 옮겨 렌더 후 DOM 에 직접 긴 줄을 넣는다(제품 코드에 시험용 우회 0) — 4/4 복구 · 회귀가 '어느 층에서 부풀리는가'를 못박고 변이 6/6 이 그 검사를 되묻는다 · e61ef44c |
