| F367 | 2026-08-12 | 마찰 | 🎫 로 넘겨진 처방이 **죽은 근거 위에 서 있었다** — 「보드 자기 줄 91커밋/일을 auto-commit 이 흡수하게 하자」는 실행했으면 얻는 것 0 · 잃는 것 > 0 이었다.  `.claude/hooks/auto-commit.js` 의 `제외폴더` 에 `docs/_ops/보드/` 가 있고, 적힌 근거는 「보드 줄의 이동은 board-move.js 의 의식이라 중간 판이 실리면 반쪽만 착지한다(F248 계열)」였다. 그 근거는 이미 죽어 있다 — `board-move.js` 는 `fs.writeFileSync` 로 쓰므로 편집 도장이 없고, 이 훅은 도장 있는 것만 싣는다(139~141행 무기록 버킷). 즉 제외가 없어도 board-move 의 중간 판은 원리상 안 실린다. 그래서 다음 세션이 「F248 은 죽었으니 제외를 떼면 91커밋/일이 줄겠다」로 읽을 수 있는 상태였고, 실제로 인계 🎫 가 그렇게 적혀 있었다.  실측으로 그 처방을 반증했다 (2026-08-12 · 오늘 커밋 481건 전수): - 이 훅이 오늘 낸 커밋 = **3건**. 보드만 담은 손 커밋을 낸 세션 47개 중 자동커밋을 낸 세션은 3개뿐이다. - 이유: 세션이 CLAUDE.md 대로 즉시 손 커밋하니 Stop 시점엔 더러운 보드 파일이 없다. 제외를 떼도 **흡수될 커밋이 없다** — 보드 손커밋 184건은 그대로 남고, 자동커밋이 그 자리를 대신 집을 뿐이다(1:1 교환). - 잃는 것은 있다: 갱신을 자동커밋이 먼저 집으면 `docs: 보드 …` 서술 메시지가 「자동커밋: 655d027c.md — 미커밋 노출 차단」 익명 메시지로 바뀐다. 그 서술 메시지는 **인계문이 다음 세션에 넘기는 재료**다(이 세션이 받은 인계문도 `docs: 보드 d35f21df — 다음 할 일 = …` 줄로 직전 트랙을 알았다).  보드 계열 251건의 갈래(실측): 갱신 69 · 이관 132 · 선언 49 · 기타 1. 갱신 69 중 53건(77%)이 같은 세션의 다른 커밋 ±5분 안에 있어 「그 커밋에 보드 경로를 같이 담기」로는 줄일 수 있으나, 그건 **모든 커밋에 거는 가드**가 되어 가장 뜨거운 경로에 마찰을 더한다 — 11% 절감에 그 값은 안 맞는다(자기반박으로 기각).  곁들여 F045 재현: 첫 측정이 `git show --name-only` 기본 quotepath 때문에 한글 경로를 이스케이프해 **「보드 커밋 0건」**으로 나왔다. 재는 층이 값을 깨뜨려 초록이 된 자리다 — `-c core.quotepath=false` 로 다시 재서 218/184 를 얻었다. | `.claude/hooks/auto-commit.js` 의 제외폴더 주석에서 **죽은 근거를 걷어내고 산 근거로 갈아끼웠다**(`05c92fa2`) — 「board-move 의 중간 판이 실린다(F248 계열)」 대신 ①board-move 는 `fs.writeFileSync` 라 도장이 없어 이 훅이 원리상 못 본다는 사실 ②오늘 481커밋 중 이 훅이 낸 것은 3건뿐이라 떼도 흡수될 커밋이 없고 `docs: 보드 …` 서술 메시지(인계문의 재료)만 잃는다는 실측을 적었다. `tests/자동커밋.test.js` 의 F102 회귀 주석에도 「이 회귀를 지우려는 다음 세션에게」로 같은 내용을 박아, 근거가 갈렸어도 막는 방향이 같다는 것을 읽는 자리에 뒀다.  막는 것은 주석이 아니라 회귀다 — `보드는 절대 자동커밋하지 않는다(F102)` 가 제외를 실제로 못박는다. **F083 변이로 탐지력을 실측했다**: 실물 훅을 건드리지 않고(살아있는 세션 6개가 매 턴 쓰는 파일이라) `.claude/hooks/` 안에 확장자를 `.js` 밖으로 둔 사본을 만들어 `제외폴더` 에서 `docs/_ops/보드/` 만 빼고 `SYNK_TEST_AUTOCOMMIT_HOOK` 이음매로 물렸더니 **19 pass · 1 fail**, 빨개진 것이 정확히 그 보드 회귀였다(변이 1/1). 사본은 시험 직후 삭제 확인.  F092 대로 닫기 전 저장소 전체를 grep 했다 — 죽은 문구가 남은 자리는 `.claude/worktrees/` 스냅샷 2개뿐이고 master 추적분에는 없다.  곁들여: 처방의 대안이었던 「갱신 69건을 같은 세션의 다른 커밋에 같이 담게 하는 가드」는 **자기반박으로 기각**했다 — 11% 절감을 위해 모든 커밋(가장 뜨거운 경로)에 가드를 거는 값이 안 맞고, 따를 수 없는 처방은 우회를 정상 통로로 만든다(F103). |
