| F518 | 2026-08-17 | 마찰 | 원문(소스 텍스트) 정규식 회귀가 «행동은 그대로인데» testID 하나에 깨져 master 를 5시간 빨갛게 두었다 — 그 사이 아무도 몰랐다.  실측 2026-08-16~17 (`local_738aae09` · 라디오시크릿 트랙 중 전체 스위트를 돌리다 발견): SYNK-talk master 가 `tests/말하기로직.test.js` 의 「녹음카드: 배경으로 나가면 끊고…」 1건으로 적색이었다. 원인은 `b58ed02`(08-16 19:13 · 기기 관측 고리 · Maestro 자동 밟기)가 `src/말하기화면.js:1196` 의 `<Pressable onPress={넘어가기}` 에 **`testID="말하기-무발화-넘김"` 을 끼운 것**이다. 회귀는 `/\{!끊김 && \(\s*<Pressable onPress=\{넘어가기\}/` 로 **붙어 있음**을 요구하고 있어서 속성 하나가 들어오자 그대로 죽었다. 화면 «행동»은 한 글자도 안 변했다.  ■ 왜 이게 신호인가 — 지침 v9.2 「가드·회귀의 네 맹점」 ① 의 정확한 재발이다:   «사람이 실제로 쓰는 표기로 검사한다». 원문 정규식은 **표기**를 재는데, 표기는 기능과 무관하게   늘 바뀐다(testID·접근성 라벨·포맷터·prettier). 그래서 이 계열 회귀는 «행동이 멀쩡한데 빨간»   거짓 적색을 주기적으로 낸다. 그리고 그 적색은 CLAUDE.md 가 적어 둔 대로 **남의 배포 게이트를 막는다**.  ■ 🔴 더 아픈 축 — 아무도 5시간 동안 몰랐다.   `b58ed02` 를 낸 세션은 그 커밋을 「흐름 3/3 통과」로 닫았고(에뮬레이터 실측은 진짜로 통과했다),   그 뒤 두 커밋(`c7f3b15`·`06e8b3b`)이 더 얹혔는데 셋 다 스위트를 안 돌렸거나 안 보고 지나갔다.   즉 이 저장소에서 «master 가 빨간지»를 말하는 자리가 **사람이 npm test 를 손으로 돌릴 때뿐**이다.   (형제 저장소라 as 의 `test-ci.js`·SessionStart 훅이 원리상 안 본다 · 원격 CI 는 Actions 소진으로 0건.)  ■ 처방 후보 — 안 지었다(하네스 한 달 봉인 · 「새 장치 0」 · memory harness-deletion-verdict-0816).   이 신고는 기록이다. 판정할 때 재료로 쓸 것:   ㉠ 원문 정규식을 «속성 관대»로 바꾸는 규약 — `<Tag[^>]*attr=` 형태. 이번 수리가 그 모양이고      변이 2/2 로 불변식이 안 느슨해졌음을 확인했다(가드 제거·다른 버튼 감싸기 둘 다 포획).      단 이건 **한 자리만** 고친 것이고, 같은 모양의 원문 정규식이 저장소에 몇 개인지 안 셌다.   ㉡ 형제 저장소의 적색을 as 세션이 «보게» 하는 자리 — 지금은 통로가 0 이다.      ⚠ 대가: SessionStart 에 항목이 하나 늘고, 그 값은 매 세션 붙는다(경량 원칙과 정면).   ㉢ 아무것도 안 한다 — 손 npm test 에 계속 기댄다(이번처럼 우연히 발견되기를 기다린다).  ■ 수리는 했다: talk `fa24979` — 정규식만 완화, 행동 무변경, 2320/2320 초록 복구.   내 트랙(#Q48 라디오시크릿) 밖이지만 «주인 없는 적색»이라 걷었다. | |
