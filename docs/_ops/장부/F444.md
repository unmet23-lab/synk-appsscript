| F444 | 2026-08-14 | 마찰 | Stop 훅의 「미커밋 노출 차단」 자동커밋이 한 트랙을 두 커밋으로 쪼갠다 — F302 와 정면 충돌한다.  실측 08-14(#Q78 → [v9.235] 트랙 · 세션 local_379cb6f7): 코드(`엔진_셋업확장.js`)와 그 회귀 (`tests/발화퀄리티.test.js`)를 한 커밋에 모으려고 test-ci 를 기다리는 동안 중간 보고로 턴을 끝냈다. 그 순간 Stop 훅이 `594ef735 자동커밋: 발화퀄리티.test.js — 미커밋 노출 차단` 을 만들었고, 뒤이어 내가 `01cb8dd0` 로 코드를 커밋해 **한 트랙이 두 커밋으로 갈렸다**. 커밋 결과가 「5 files / 56 insertions」로 기대(6 files / 137)와 어긋나서야 알아챘다 — 훅은 조용히 성공하므로 그 전까지 아무 신호가 없다.  이번엔 두 커밋 사이에 낀 커밋이 0이라 피해가 없었다(실측 `git log 594ef735..01cb8dd0` = 1건, 즉 사이 0). 그러나 F302 가 적은 위험은 그대로 산다 — 배포는 늘 HEAD 라 그 틈에 남이 `/deploy` 하면 **회귀만 담기고 코드는 안 담긴 배포본**이 「방금 고쳤다」의 얼굴로 남는다(F302 실측 3분이 이미 있다). 이번 트랙에서는 쪼개진 쪽이 tests/ 라 clasp 배포 집합 밖이었던 것이 운이었지, 설계가 아니다.  두 장치가 각자 옳다: F025(미추적·미커밋은 유일한 무보호)가 Stop 훅을 세웠고, F302(한 트랙은 한 커밋)가 그 반대를 요구한다. 충돌 자리는 **작업 중간에 턴이 끝나는 순간**이다 — 보고·질문·긴 백그라운드 대기 때문에 턴을 끊는 것은 정상 동선인데, 그때마다 훅이 미완성 트랙을 쪼갠다. 그리고 그 시점은 세션이 고르는 게 아니라 대화 리듬이 고른다.  처방 후보(내가 고르지 않는다 — 훅 소유 트랙 밖이고, 되돌림 비용이 배포 게이트에 닿는다):  ㉠ 세션이 「이 트랙은 아직 한 커밋으로 안 모였다」를 선언하면 Stop 훅이 그 경로를 자동커밋에서     빼고 경고만 낸다(보드 트랙 칸의 표식 하나로 충분할 수 있다 — 새 장부를 안 만드는 쪽).  ㉡ 자동커밋에 「트랙 미완」 트레일러를 박고, 다음 커밋이 그것을 흡수했는지 기계가 사후 대조한다     (지금은 갈라진 것을 아무도 못 본다 — 이번에도 커밋 통계가 안 맞아서 눈으로 잡았다).  ㉢ 그대로 둔다 — 대신 세션 규약에 「트랙 중간에 턴을 끊지 말고 커밋을 먼저 한다」를 적는다.     ⚠ 이건 사람(세션) 손을 늘리는 해법이라 철학 ㉡ 과 어긋나고, 잊으면 그대로 재발한다.  당장의 자기 처방: 중간 보고·긴 대기로 턴을 끊기 전에 **커밋을 먼저 한다**. 아직 트랙이 안 끝났으면 그 커밋 제목에 미완임을 적어 다음 커밋이 이어받는 것으로 읽히게 한다. | ⏸ 2번째 실측 08-15(local_d8038154 · #Q79 [v9.238] 트랙) — 이번엔 «쪼갬»에서 그치지 않고 HEAD 를 빨갛게 만들었다. Stop 훅 자동커밋 bea5da2f 가 8파일 중 테스트 2 + 버전_이력만 담아 가, 그 사이 HEAD 는 «회귀는 새 것 · 엔진은 옛 것»이라 새 검사가 실저장소를 빨갛게 보고 남의 배포 게이트까지 막는 상태였다(F248 과 같은 모양 — F444 를 F248 가족으로 읽어야 한다는 것이 이번에 는 것). 회수는 나머지 5파일 즉시 커밋(c3a283db)으로 끝났고 창은 짧았다. 지금 안 닫는 이유 = 과녁이 Stop 훅(.claude/hooks)이라 이 트랙 밖이고, 처방 갈래(커밋 보류냐 · 짝 검사냐)가 F248 처방 후보와 겹쳐 따로 고치면 같은 판정이 두 곳으로 갈라진다. 두 행을 한 트랙으로 묶어 처분해야 한다. |
