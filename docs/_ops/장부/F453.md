| F453 | 2026-08-15 | 실수 | 산출물 HTML 을 보냈는데 «렌더가 아니라 소스 코드»로 떠서 유호님이 두 번 못 보셨다. 원인을 «4.5MB 라 인라인 렌더가 못 받았다»로 단정했다가 유호님 화면(줄번호 붙은 코드 뷰어)을 받고서야 갈랐다 — 구조 실측(data URI 6·doctype·Chrome 렌더 요소 183)까지 맞게 하고도 남은 후보를 열거하지 않았다. 처방=보여줄 페이지는 «렌더되는 자리»(아티팩트)로 내고, 로컬 .html 은 뷰어가 소스로 열 수 있음을 전제한다. | ⏸ 이번 세션에서 «안 닫기»로 판정했다(커밋 738acbcb 에서 판정 · 코랄 전 세트 트랙).  왜 안 닫나: 나는 이 트랙 산출물을 아티팩트로 냈고 유호님 화면에서 렌더되는 것까지 확인했지만, 그건 «내가 그 통로를 골랐다»는 것이지 기계가 막은 게 아니다. F083 대로 신고 원문을 그대로 다시 넣어 보면(= 로컬 .html 경로를 산출물로 건넨다) 지금도 똑같이 소스 코드로 뜬다. 막는 층이 0건이다. 프로즈로만 남으면 다음 세션이 같은 자리를 다시 밟는다 — 실제로 이 신고는 «두 번» 밟힌 뒤에 났다.  닫을 조건(다음에 집는 세션이 그대로 쓸 것): 산출물로 건네는 경로가 .html 인데 그 자리가 아티팩트가 아니면 드러내는 층 하나. 후보는 SendUserFile 계열 PreToolUse 훅 — .html 을 보내려 하면 「이건 유호님 화면에서 소스로 뜬다 · 아티팩트로 내라」로 deny. ⚠새 장치의 대가를 함께 적어야 한다: 틀릴 때의 모습 = 아티팩트로 낼 수 없는 정당한 .html 전달(예: 남이 열 코드 리뷰용 파일)까지 막는 거짓양성. 그래서 «산출물로 보여주려는» 것과 «파일로 넘기는» 것을 가르는 기준을 먼저 정해야 하고, 그 기준이 안 서면 훅이 우회를 정상 통로로 만든다(F103).  여기 안 넣은 이유(범위): 이번 세션 지시는 유호 픽 집행이다. 훅 신설은 다른 트랙이고, 기준을 안 정하고 지금 넣으면 위 거짓양성을 그대로 들인다. |
