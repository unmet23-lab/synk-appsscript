| F326 | 2026-08-11 | 실수 | 라이브 폼을 고치는 함수를 「읽기 전용을 누른다」고 믿고 실행했다 — 드롭다운이 파일 전환 때 첫 함수로 되돌아갔고, 나는 「고른 것」만 확인하고 「선택된 것」을 실행 직전에 다시 안 봤다. checkGoldenPush(읽기 전용) 대신 migrateHwFormV9138 이 돌아 라이브 숙제 폼에 문항 2개가 실제로 추가되고 URL 틀 2종이 갱신됐으며 유호님께 메일 1통이 나갔다. CLAUDE.md 「승인은 행위에 대한 것이지 조준이 아니다 — 실행 직전 선택된 대상의 내용을 다시 읽어 대조한다」를 정확히 어겼다. 급소는 클릭 실패가 조용하다는 것 — 같은 페이지에서 파일 목록 ref 클릭도 세 번 조용히 안 먹었고(좌표로 우회), 옵션 클릭도 같은 방식으로 안 먹었는데 실행 버튼만은 먹었다. 「안 먹은 클릭」과 「먹은 클릭」이 반환값에서 같은 모양(Clicked on element)이라 조준 실패가 통과처럼 보인다. | CLAUDE.md 조항이 받았다 — 「승인은 행위에 대한 것이지 조준이 아니다 — 라이브·공유 화면은 승인 뒤에도 대상이 밀리므로 실행 직전 선택된 대상의 내용을 다시 읽어 대조한다(행 번호·좌표는 근거가 아니다)」. ⚠ 이 조항«조차» Apps Script 편집기에선 안 통한다는 것이 F327 의 실측이라 그 줄은 열어 둔다 — 여기서 닫는 것은 「조준을 안 다시 봤다」는 이 신고문의 몫뿐이다. |
