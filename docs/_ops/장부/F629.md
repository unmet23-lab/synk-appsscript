| F629 | 2026-08-18 | 마찰 | `대기행()` 이 상태 칸의 대기 기호(⏳·⏸)를 **«다는 것»과 «논하는 것»으로 안 가른다** — F612 가 `놓은줄()`(🎫)에서 닫은 병의 **쌍둥이**인데 그 수리가 여기엔 안 왔다. 두 판정은 같은 파일에 이웃해 있고(`tools/lib/보드.js`), 같은 모양으로 상태 칸을 통째로 `includes` 한다.  ■ 어떻게 나왔나 — 내가 이 사각에 **직접 낚였다**(그게 이 신고의 재료다) 다른 트랙(F628 · 점유의 origin/master 층)의 값을 재다가 「뒤처진 트리가 놓치는 **활성** 줄」을 세었는데, 세는 층이 `활성행()` 이었다. 처음 낸 수(30커밋 뒤 → 활성 4)를 열어 보니 그 4 중 둘이 **`✅**종결**` 로 시작하는 줄**이었다 — 상태 칸 끝머리에 「⏸F612 안 집음(유예 유지)」 같은 **남의 대기를 인용한 서술**이 있어서 `대기행()` 이 참을 냈고, `활성행()` 이 그걸 그대로 「활성」으로 올렸다. 즉 **내 분모가 내가 모르는 채로 부풀어 있었다.** CLAUDE.md 「②재는 층이 값을 깨뜨리지 않는지 확인」 그 자리다.  ■ 기전 (추정이 아니라 소스) `대기행(line)` = 상태 칸을 뽑아 `status.includes('⏳'||'⏸')` 뿐이다. 그리고 `활성행()` 이 `if (대기행(line)) return true` 로 **완료 어휘를 덮는다**(F554 가 일부러 그렇게 만들었다 — 「완료 어휘가 있어도 과녁이 저장소 밖이라고 스스로 말하면 활성이다」). 그 설계는 옳다. 새는 것은 **「스스로 말한다」와 「남의 것을 인용한다」를 안 가르는 것**이다. `대기행` 의 머리말은 이 구분을 **이미 알고 있다**: 「⚠ 상태 칸만 본다: 트랙·파일 칸의 ⏳ 는 «이 트랙이 대기다»가 아니라 딴 줄을 가리키는 인용이다」. 경계를 **칸 단위**로만 그었고, 인용은 상태 칸 **안에서도** 일어난다.  ■ 실측 2026-08-18 (`local_9a6c0547` · 보드 조각 전량 + `docs/세션보드_아카이브.md`) · 표줄 **1096** · `활성행` 참 **390** · `대기행` 발동 **222** · 상태 칸 대기 기호 스팬 **230**. · 스팬 230 을 형태로 갈랐다: 「⏳유호…」 = **169**(자기 대기 · 정상) · 나머지 **61**. · 그 61 도 대부분 압축된 자기 대기다(「⏸ **머지 대기**」·「⏳CI 모사 미완」·「⏳F115 는 연 채」·「⏸잔여=…」). · **오탐으로 판정한 것은 5~8 스팬**이고 형태가 셋이다:   ㉠ **셈·보고** — 「활성 15 중 5줄이 ⏳·⏸(아무도 못 닫는다)」·「(⏳유호 11·⛔선행 4·⏸시점 1)」·「파일에 ⏳ 2줄인데 `--check` 0건」. 기호 뒤가 **수**로 시작한다.   ㉡ **기호를 명사로 부름** — 「`f6457d2a` ⏸**과** 갈린 건」·「🚫「⏳+유호」 스캐너」. 기호 뒤에 **조사**가 붙거나 인용부호 안이다.   ㉢ **남의 상태 인용** — 「⏸F612 안 집음」·「⏸F612 는 여전히 유예」(그 줄 자신은 ✅종결이다). · 살아 있는 보드 조각에서 지금 뒤집혀 있는 줄 = **3**(`73b368d5` 1 · `d427746a` 2) — 활성 49 중 **6%**.  ■ 왜 신호인가 · 방향이 둘 다 나쁘다. 뒤집힌 줄은 `완료행 = 데이터행 && !활성행` 이라 **`보드수거` 가 원리상 못 치운다** — #Q93 「죽은 보드 줄이 자리를 영구히 잠근다」가 바로 이 모양이고, 점유 화면에는 **유령 점유**로 뜬다(F585·F615 가 값을 이미 치른 자리). · 그런데 **고치는 방향도 위험하다** — 넓게 좁히면 진짜로 기다리는 줄이 완료로 접히고, 그건 F554 가 이름 대어 경고한 사고다(「보드수거가 아카이브로 가져가면, 유호님 답이 오는 날 그 트랙은 보드에도 인계문에도 없다」). 그래서 **싼 수리가 아니다.** · 이 신고의 값은 숫자 자체가 아니라 **「측정층이 조용히 부푼다」는 실증**이다. 나는 F628 의 분모를 이 함수로 셌고, 처음 낸 수(176)가 통째로 틀렸다.  ■ 처방 후보 — 안 지었다(이 신고는 재료다) ㉠ **F612 가 이미 검증한 두 규칙을 그대로 이식**한다 — 기호 뒤가 수면 셈(`놓음셈RE`), 백틱에 싸이면 인용(`인용된표식`). ⚠대가: 지금 저장소에서 백틱 인용은 **0건**이라 그 절반은 과녁 없는 규칙이다(장치를 위한 장치). 셈 쪽은 정상 용례 중 기호 뒤가 수로 시작하는 것이 **0건**이라 안전하다. ㉡ **조사 규칙을 더한다** — 기호 바로 뒤에 「과·와·는·은·를·가·이·도·만」이 오면 기호를 **명사로 부르는** 것이다. ⚠대가: 한국어 조사 목록은 낡는다. 현재 과녁 1건. ㉢ **㉢형(남의 상태 인용)은 기계로 못 가른다** — 「⏸F612 안 집음」과 「⏸F612 대기」는 글자로 구분이 안 된다. 사람 규약(「남의 대기는 기호 없이 번호로만 쓴다」)이거나, 아예 안 가르거나다. ⚠대가: 산문 규약은 안 지켜진다. ㉣ 아무것도 안 한다 — 현재 오염이 활성 49 중 3줄이고, 고치다 F554 사고를 내면 더 비싸다. ⚠대가: `보드수거` 가 그 3줄을 영원히 못 치우고, 세는 자리마다 이 부풂이 조용히 따라간다.  ■ 안 잰 것(F348) · 「오탐 5~8」은 **내 손분류**다 — 기계 판정이 아니다. ㉢형은 사람이 읽어야 갈리므로 그 수는 자동으로 재확인되지 않는다. · 같은 «칸 통째 includes» 모양을 쓰는 다른 판정이 이 파일에 더 있는지는 **안 셌다**(`놓은줄` 은 F612 가 닫았고 `대기행` 이 이번 것 — 셋째가 있는지 모른다). | 좁힘 규칙을 `논해진표식(앞, 뒤)` **한 벌**로 모으고 `놓은줄()`·`대기행()` 이 그것만 부른다 — `70069fcd`. 셈 규칙의 사본(`놓음셈RE`)은 지웠다(안 도는 규칙을 다음 사람은 «도는 것»으로 읽는다). 처방 후보 ㉠(F612 검증 두 규칙 이식)+㉡(조사)을 채택했고, 세 규칙 다 **실측 0건 손실**을 확인하고 넣었다: 스팬 231 중 걸리는 것 10(셈 9·조사 1·백틱 0), 그로 인해 «활성→완료» 로 뒤집히는 줄 9인데 **9건 전부 첫 구절이 완료 어휘**다 — 즉 F554 가 경고한 사고(기다리는 트랙이 조용히 사라진다)는 이 규칙 집합에선 안 난다. 🎫 쪽에 새로 더해지는 조사 규칙은 74 스팬 중 0건이라 기존 판정 무변경. 회귀 11 신설(`tests/표식논함.test.js` — **정상 용례를 지키는 검사가 좁힘 검사보다 많다**, 새는 방향이 조용한 쪽이라) · **변이 9/9 구멍 0 · 미적용 0**(좁힘을 지우는 5 + 넓히는 4 — 양방향) · CI 모사 4680 pass 4545 **fail 0**(linux) · 🔴원격 run 0건(리셋 9/1)이라 초록은 모사 층뿐이다. ⚠ **㉢(남의 상태 인용)은 안 닫았다** — 「⏸F612 안 집음」과 「⏸F612 대기」는 글자로 구분이 안 되고 프로즈 매칭은 F374 ① 이 금지한다. 산 보드에 그 꼴이 2줄 있고(`d427746a`) 이 트랙은 못 고친다 — 사람 규약(「남의 대기는 기호 없이 번호로만 쓴다」)이 필요한 자리다. 같은 이유로 못 닫은 갈래 둘을 더 적는다: 수가 기호 «앞»에 오는 셈(「활성 15 중 5줄이 ⏳·⏸」)과 혼합 보고문(「(⏳유호 11 · ⏸시점 1)」). **셋 다 회귀로 못박았다** — 침묵으로 남기면 다음 사람이 이 좁힘을 「전부 잡는 것」으로 읽는다. 🔑 이 신고가 나온 경위 자체가 값이다: 나는 F628 의 분모를 `활성행()` 으로 세다 낚였고(첫 수 176이 통째로 틀렸다), 회귀를 짓다 내 단언 둘이 또 실측과 어긋난 것을 스스로 잡았다 — 그 둘이 지금은 「안 닫았다」를 지키는 검사가 됐다. |
