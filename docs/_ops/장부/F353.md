| F353 | 2026-08-12 | 마찰 | 수리가 「다른 회귀가 읽는 자리」를 비웠고, 두 처방이 서로 모순인 채 master 에 남았다 — 적색 2건은 어느 보드·대기열에도 없었다  실측 2026-08-12 (세션 `4588ddf9` · 새 트랙을 잡기 «전» 규약대로 `node tools/test-ci.js` 를 돌리다 드러났다). 적색 4건 중 2건이 보드·대기열·장부 어디에도 없었다.  원인 = F349 수리(`f7e89e3c` · 세션 `1e5496ab` · 46분 전)가 `.claude/hooks/context-budget.js` 의 `인계칸` 4줄을 지웠다. 그 블록 «안»에 `node tools/인계문.js` 와 `--no-save` 재출력 처방이 살고 있었고, 그 두 글자를 전제로 재는 회귀가 `tests/트랙경계.test.js` 에 2건 있었다 — ①「수동 재출력」 안내는 `--no-save` 를 준다 ②wake·/close·블록 지시가 가리키는 인계문 도구가 실재한다(검사 대상 파일 3벌에 `context-budget.js` 포함).  🔑 급소는 「한쪽이 낡았다」가 아니라 **두 회귀가 같은 파일을 반대 방향으로 잡고 있다**는 것이다. F349 는 그 화면을 «3줄 · 빈 줄 0 · 인계 목차 부재»로 기계 고정했고(회귀 신설 · 여유 0), F096/F099 회귀는 같은 파일이 그 목차를 **가지고 있으라**고 요구한다. 그래서 어느 쪽도 「고치면 초록」이 아니고, 힘으로 초록을 만들면 둘 중 하나의 탐지력이 죽는다.  🔑 곁들여 **처방 자체가 실물에서 사라졌다** — 저장소에서 사람에게 `--no-save` 를 시키는 자리가 **0**이 됐다(남은 것은 `tools/인계문.js` 자기 주석과 마찰장부의 역사 인용뿐). F099(점검하러 한 번 돌린 것이 살아있는 남의 인계문을 밀어냄)를 막던 «말»이 없어지고, 도구가 그 플래그를 지키는지 재는 회귀 1건만 남았다. 회귀가 「처방이 사라졌다」를 정확히 신고했는데 그 신고를 아무도 안 읽었다.  축은 F225·F351 과 같다 — `test-ci` 는 **커밋 때 안 발동해서** 적색이 그대로 master 로 가고, 그 뒤 모든 세션의 배포 게이트를 막는다. 다른 점은 이번 적색이 「남의 미커밋」도 「낡은 검사」도 아니라 **방금 들어온 정상 수리의 낙진**이라는 것이다. | 처방의 «자리»를 옮겨 두 회귀의 모순을 없앴다 — `35a22eff`.  수동 재출력 처방(`node tools/인계문.js --no-save`)을 줄 수로 기계 고정된 컨텍스트 경고 화면이 아니라 `.claude/skills/close/SKILL.md` §6, 즉 사람이 끊을 때 실제로 읽는 문서에 두고, `tests/트랙경계.test.js` 의 검사 2건 과녁을 그 문서로 옮겼다. 이제 F349 회귀(화면에 그 글자가 «있으면» 적색)와 F096 회귀(처방이 «있어야» 초록)가 서로 다른 파일을 재므로 둘이 동시에 초록일 수 있다 — 모순 자체가 사라졌다.  되돌림 방지는 프로즈가 아니라 두 자리에 박았다: 검사 주석에 🚫훅으로 복귀 · 🚫코드 주석으로 통과(주석은 유호님이 읽는 자리가 아니라, 그러면 검사가 영구히 무의미해진다)를 이유와 함께 적었고, 탐지력은 변이 4/4 로 못박았다(①처방 문장 삭제 ②`--no-save` 를 맨 명령으로 ③앵커 제거 ④도구 이름 전량 제거 — 넷 다 적발). 트랙경계 32/32 · 컨텍스트예산 38/38.  ⚠ 이 조각이 닫은 것은 **그 한 쌍의 모순**이다. 「수리가 다른 회귀의 전제를 지우고 커밋된다」는 상위 축 — `test-ci` 가 커밋 때 안 발동해서 적색이 그대로 master 로 가는 것 — 은 F351·F225 에 그대로 열려 있다. 여기서 닫았다고 읽으면 안 된다. |
