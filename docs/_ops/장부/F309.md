| F309 | 2026-08-10 | 실수 | 배포 직전에 배포집합 미커밋을 다시 재지 않아 남의 미커밋(contents_상담AI.js 의 [vNEXT] 판)이 clasp push 에 실려 라이브로 나갔다. 두 층이 동시에 눈이 멀었다 — ①clasp-guard 가 통과시켰다 ②push 직후 배포판점검 --라이브 는 작업본과 비교하므로 「바이트 동일」 초록이 떴다. 미커밋 유출이 초록의 모습으로 지나간다. ⚠**진단 정정(같은 세션 · 소스 실독)**: ①은 가드 결함이 **아니다** — `clasp-guard.js:263` 이 `core.quotepath=false` 로 한글 파일명 함정까지 이미 막았고, 검사 시점엔 실제로 깨끗했다. 급소는 **훅 통과와 clasp 가 파일을 읽는 사이의 레이스(TOCTOU)** 라 훅만으로는 원리상 못 막는다(세션 5개가 같은 작업본을 공유). 그래서 실효 처방은 사전이 아니라 **사후층**이다: push **직후** 배포집합 미커밋 0 을 확인해야 「라이브=작업본」이 비로소 「라이브=HEAD」를 뜻한다 — 그 한 줄이 없으면 유출이 초록과 같은 모양이 된다(08-10 실측 2회 · F310 은 같은 자리를 다른 각도로 본 것). ✅재push 로 정합 확인(미커밋 0 + 15개 바이트 동일). | 배포판점검 미커밋 축(판정 전 갈래가 배포집합미커밋을 받아 유출=stale·push직후=warn·초록은 미커밋0 실측 때만 라이브=HEAD) + deploy-freshness warn 갈래 · 회귀 tests/배포판점검.test.js ②-c 12건 |
