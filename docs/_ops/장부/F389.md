| F389 | 2026-08-12 | 마찰 | test-ci 초록이 배포 게이트 통과를 뜻하지 않는다 — 메모리층 검사 둘(재질의금지·결정큐 실저장소)은 test-ci 가 HOME 을 비워 CI 를 모사하는 탓에 메모리 폴더를 못 찾아 skip 되고, clasp-guard 는 HOME 이 있는 환경에서 같은 스위트를 돌려 그 둘이 빨갛다. 그래서 「CI 모사 초록 2895/fail 0」인데 배포가 막혔고, 갈라 줄 원격 CI 는 Actions 한도로 job 미기동이라 판정층이 통째로 없었다. 게다가 그 적색은 내 트랙 밖 (남이 메모리에 ⏳ 와 ✅ 를 한 줄에 적은 것)이라 주인이 자기 적색을 볼 통로가 없다 — 그 세션의 test-ci 도 초록이었을 것이다. | tools/lib/ci모사환경.js 한 벌 + 같은 스위트 세 러너 정합(test-ci·clasp-guard 안전테스트·auto-commit 실을테스트 — 1a131b7e·1e46a926): 게이트가 실 HOME 으로 재던 갈래를 제거해 남의 메모리 적색이 배포를 못 막는다. 메모리 위생(재질의금지)은 tools/lib/재질의금지.js 로 떼어 rot-check 재질의 절(warn·실물 있는 기계=주인이 본다)로 발동 이전. 회귀 판정층정합 5·재질의금지 2 · 변이 3/3(env 제거·인라인 부활·등록 삭제 전부 적발) · CI 모사 2958/0 |
