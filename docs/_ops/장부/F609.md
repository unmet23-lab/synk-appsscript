| F609 | 2026-08-18 | 마찰 | 일감 발굴의 **두 통로 중 한쪽만** 점유를 본다 — `대기열.js` 는 가지를 읽고(F521), `tools/friction.js --open` 은 점유 로직이 **0줄**이다. 그래서 오늘 아침 네 세션이 **같은 장부 번호 하나(F605)** 에 동시에 들어갔다.  ■ 실측 2026-08-18 06:50~06:57 (`local_163e0921` · 내가 그 네 번째였다) · 워크트리 실물 **셋**이 F605 하나에 서 있다(`git worktree list`):   - `.claude/worktrees/f605-jsonl-appendonly-denominator` @ `98b78bd0` (locked)   - `.claude/worktrees/f605-jsonl-ledger-guard`           @ `007d99e3` (locked)   - `.claude/worktrees/f605-jsonl-loss-gate`              @ `2ab10676` (locked · 커밋 0 = 선언 전) · 그중 **선언 커밋 둘이 10초 차**다:   - `007d99e3` 2026-08-18 06:55:36 +0900 — 세션 `82d71975` · 만질 파일 `tests/장부유실.test.js`   - `98b78bd0` 2026-08-18 06:55:46 +0900 — 세션 `9b74b64b` · 만질 파일 `CLAUDE.md` · `tests/장부유실.test.js`   두 선언의 파일 칸은 `tests/장부유실.test.js` 에서 **겹친다**. · 나는 06:56 경 `EnterWorktree(name: f605-jsonl-ledger-guard)` 를 쳤고 하네스가   「A worktree with this name already existed and was resumed as-is」 로 답했다 — **이름까지 같았다.**   손 안 대고 `ExitWorktree(keep)` 로 물러났다(내 커밋 0 · 남의 트리 무변경).  ■ 왜 네 세션이 같은 자리로 갔나 — 우연이 아니라 **동선이 그리로 낸다** · SessionStart 대기열 알림이 오늘 이렇게 났다: 「🔴 **지금 집을 수 있는 줄이 0이다** — 아래 차단자를   풀거나, **새 일감을 발굴해 추가한다**」(열림 20 · 남이 안 잡은 것 12 · 전부 차단). · 대기열이 비면 남는 발굴 통로는 `node tools/friction.js --open` 하나다. 그 출력은 열림 10건을   **점유 표시 없이** 나열한다. 목록 맨 아래가 그날 가장 새 번호(F605, 06:38 신고)이고,   네 세션이 각자 그것을 골랐다. · 즉 이건 개인의 부주의가 아니라 **분배기가 없는 큐**의 정상 출력이다.  ■ 기전 — 통로 둘이 같은 자를 안 쓴다 (grep 실측) · `tools/lib/보드.js:615 가지줄들()` = 미머지 가지의 보드 선언을 읽는 정본 통로(F521·F528 이 낸 것).   지금 이 저장소에서 **실제로 돈다**: `node -e "…가지줄들('.')"` → `모름:false · 줄 수 7`,   그 7줄 안에 위 F605 선언 **둘 다** 들어 있다. 도구는 답을 갖고 있다. · 소비자 실측(`grep -rn 가지줄들`): `.claude/hooks/board-guard.js` · `.claude/hooks/lib/session-report.js` ·   `tools/대기열.js` — 셋. · `tools/friction.js` 는 **0회**다(`grep -n "보드\|점유\|가지줄들" tools/friction.js` 9히트는 전부   주석·무관 낱말이고 조회 코드가 아니다). 그래서 `--open` 은 「누가 이미 이 번호를 잡았나」를   구조적으로 못 말한다 — 없는 기능이지 고장이 아니다.  ■ 왜 F585·F595·F597 과 축이 다른가 · F585 = 점유를 **경로**로 묻는 조회가 없다 → 처방이 `board.js --점유 <경로…>`(PR #93). · F595 = `인계문.js` 가 미머지 가지 선언을 못 본다 → **읽는 판**의 문제. · F597 = 승계 문이 주인의 **생사**를 안 본다. · 이건 셋 다와 다르다: 조회 도구도 있고 판정 함수도 돌고 생사와도 무관한데,   **일감을 고르는 화면**에 그 값이 안 실린다. 세 자리 전부 「선언하려 할 때」 말하고,   아무도 「고르려 할 때」 말하지 않는다. 그래서 조사분이 넷씩 겹쳐도 조용하다. · ⚠ 그리고 F585 의 처방(`--점유`)은 지금 **PR #93 에 갇혀 master 에 없다** — 오늘 내가 점유를   물으려 했을 때 쓸 수 있는 것은 `grep docs/_ops/보드/*.md` 뿐이었고, 그건 정의상 미머지 가지를 못 본다.  ■ 손해 — 이번엔 안 났다. 그게 급소다 · 넷 중 셋은 아직 분모 측정(㉠) 단계라 산출물이 안 갈렸다. 나는 물러났다. · 하지만 둘은 **같은 파일**(`tests/장부유실.test.js`)을 선언했고, 하나는 거기에 `CLAUDE.md` 까지 얹었다.   그대로 가면 같은 회귀를 두 벌 짓고 정본 문장을 서로 다르게 고친다. · F585 가 적은 「비켜난 뒤 남는 것이 조사분뿐이라 다음 세션이 같은 자리를 또 판다」가   **동시에 넷**으로 나온 판이다.  ■ 안 잰 것 — 명시한다(F348) · **`board-guard` 가 왜 둘째 선언을 통과시켰는지 안 쟀다.** 두 선언의 파일 칸이 겹치는데도 섰다.   후보 셋을 갈라야 한다: ㉠Write 시점(≈06:55:40)에 첫 가지가 아직 안 보였다(커밋 06:55:36 · 창 4초)   ㉡`가지끝()` 이 `origin/master` 기준이라 **push 전 로컬 가지**를 못 봤다   ㉢겹침 판정이 파일 칸을 그렇게 안 읽는다(F574 계열).   셋 중 무엇인지 모른 채로는 처방을 못 고른다 — 이건 이 신고가 아니라 트랙의 첫 일이다. · 「다른 발굴 통로가 또 있나」도 안 셌다. 내가 연 것은 SessionStart 출력·`대기열.js`·`friction.js` 셋이다.  ■ 처방 후보 — 안 지었다(이 신고는 재료다) ㉠ `friction.js --open`(과 `--보류`)이 `보드lib.가지줄들()` + master 보드를 분모로 각 행에    「🔒 잡힘 — 세션 N · 가지 …」를 찍는다. **새로 지을 것이 없다** — `대기열.js` 가 이미 그 모양이고    그 통로를 쓴다. 대가 = git 스폰 1회(실측 226~486ms · `가지줄들` 머리말) + 오프라인·픽스처에서    `모름:true` 를 **모름으로** 내야 한다(0으로 접으면 F207).    ⚠ 잡힘 판정은 **장부 번호**를 키로 해야 하는데, 지금 선언문의 번호는 트랙 칸 산문 안에 산다    (`**F605 — …**`). 번호 추출이 새 판정이면 그게 갈라질 자리다(맹점 ④) — 파서를 새로 짓지 말고    `대기열.js` 의 표식 규칙(F406 · 제목 첫머리)과 같은 자를 쓸 수 있는지부터 본다. ㉡ SessionStart 의 「새 일감을 발굴해 추가한다」 문구가 **가는 곳을 지목**한다 —    지금은 어디서 고르라는 말이 없어 모두가 같은 목록의 같은 줄로 간다.    ⚠ 이건 산문이라 ㉠ 없이는 「따를 수 없는 처방」이 된다(F103). ㉢ 아무것도 안 한다 — 네 세션이 겹치는 것은 오늘처럼 대기열이 통째로 막힌 날에만 난다.    ⚠대가: 막힌 날이 드물지 않다(오늘 열림 20 중 집을 수 있는 줄 0). | ⏸ 중복 접수다 — 같은 결함을 세션 local_5653b6a5 가 F610 으로 신고하고 수리 커밋까지 냈다(가지 worktree-f-open-ledger-occupancy · 제목: 일감을 고르는 목록이 점유를 먼저 말한다 — 장부 --open·--보류). 처방 주인은 그쪽이므로 여기서 안 짓는다. 이 행은 지우지 않고 재료로 남긴다: F610 이 못 담은 실측 셋이 여기 있다 — ①워크트리 실물 3개와 선언 2건의 10초 차(06:55:36 / 06:55:46) ②내가 EnterWorktree 로 남의 워크트리 이름에 그대로 재진입한 네 번째 사례 ③board-guard 가 왜 둘째 선언을 통과시켰는지는 아직 아무도 안 쟀다(후보 셋을 신고문에 적어 뒀다). 판정 자리 = 이 세션 local_163e0921 · 2026-08-18. |
