| F601 | 2026-08-18 | 마찰 | 낡은 판에서 장부를 읽으면 「열림」이 거짓으로 뜨고, 그 위에 판정을 쓰면 **먼저 닫은 사람의 판정이 지워진다** — F596 과 기전이 반대이고, 손해가 한 칸 더 크다.  실측 2026-08-18 (`local_7dfdcffd` · 트랙을 고르려고 열림 목록을 훑던 자리): · `node tools/friction.js --open` = 14건. 그중 **F562 를 골라 트랙으로 삼았다.** · 그런데 F562 는 그 시점에 **이미 닫혀 있었다** — `local_61375a0b` 가 `e6ab500` 로 해소를 적었고 그것은 `origin/master` 에 있었다. · 내 판에는 그게 없었다(실측: `git merge-base --is-ancestor e6ab500 d15c1550` = 거짓 — 내 해소 커밋이 그 커밋의 자손이 아니다). · 그래서 `friction.js resolve F562` 가 통과했고, 머지에서 내 쪽이 남아 **첫 판정이 HEAD 에서 사라졌다**(복구 = `eed8cb55`).  ■ 도구는 결백하다 — 가드는 실재하고, 낡음이 그것을 무력화했다 `friction.js resolve` 는 **이미 해소된 행을 실제로 거부한다**(복구하려 다시 부르니 거부당해 확인했다). 그 가드가 안 선 이유는 하나뿐이다: 그것이 **로컬 파일**을 읽는데 내 판이 낡아 그 해소가 거기 없었다. 즉 이 결함은 로직 층이 아니라 **신선도 층**에 있다.  ■ F596·F496 과 무엇이 다른가 (겹치지만 같지 않다) · F596(같은 날 `61375a0b`) = 「행이 **실제로 안 닫혀** 열림이 거짓」 → 손해 = 다음 세션이 헛돈다. · 이 행 = 「행은 **닫혔는데** 내 판이 낡아 열림으로 보인다」 → 손해 = 헛돌기 **+ 먼저 닫은 사람의 판정 소멸**. 🔑 방향이 반대다. F596 의 처방(닫히지 않은 행을 찾아내기)은 이 자리를 **원리상 못 본다** — 여기서는 행이 이미 닫혀 있고, 못 본 것은 장부가 아니라 **내 판**이다. · F496 = 커밋 시점 `friction-close-guard` 의 사각(한 번 알리고 끝). 그것도 로컬을 읽으므로 같은 낡음에 걸린다. · CLAUDE.md 의 「append-only 장부는 행 삭제 금지」도 이 자리를 안 덮는다 — **행은 안 지웠고 칸만 덮었다.** 회귀 `tests/장부유실.test.js` 13건은 이 변경에 초록이었다(행 수가 그대로라서다).  ■ 경고는 있었는데 안 통했다 (이게 급소다) 세션 시작 뒤 `repo-staleness` 훅이 **「내 판이 origin/master 보다 1 커밋 뒤다」라고 정확히 경고했다.** 나는 그것을 읽고도 fetch 하지 않았다. 그 훅은 **Write 시점**에 붙어 있어 「편집을 시작하기 전에」를 말하는데, 나는 그 전에 이미 `--open` 으로 **일감을 골라 버린 뒤**였다 — 즉 경고가 붙은 자리가 결정이 내려진 자리보다 **뒤**다(F585 와 같은 형태: 값이 조사 뒤에 온다).  ■ 처방 후보 (안 지었다 — 이 신고는 재료다) ㉠ **읽는 목록에 신선도를 붙인다**: `friction.js --open`·`대기열.js`·`board.js` 가 목록을 찍기 전에 `origin/master` 와의 거리를 재서, 뒤처졌으면 목록 머리에 「이 목록은 N커밋 낡았다 — fetch 먼저」를 찍는다(자동 fetch 는 안 한다 — 읽기 명령이 네트워크에 매달리면 F581 에 걸린다). ㉡ **쓰는 쪽에서 한 번 더**: `resolve`·`defer` 가 쓰기 직전 그 행만 `git show origin/master:<장부파일>` 로 대조해, 원격에 이미 값이 있으면 덮지 말고 **병합을 권한다**. ⚠ 대가 = 네트워크 접촉이 쓰기 경로에 들어온다(F581 매달림 위험) — 실패는 fail 이 아니라 「못 쟀다」로 드러내야 한다. ㉢ 아무것도 안 한다 — 사람이 훅 경고를 지키면 된다. ⚠ 실측이 그 반대다: 경고가 떴는데 내가 안 지켰고, 새는 방향이 「성공」이라 조용했다. 🔑 고르기 전에 잴 것: 이 낡음이 실제로 얼마나 자주 나는가(세션 8개 동시·커밋 간격 중앙값 1분이면 상시일 수 있다). ㉠은 값이 싸고 거짓양성이 없다(거리는 로컬 ref 로만 잰다). | ⏸ 안 닫는다 (판정 2026-08-18 `local_7dfdcffd` · 신고자 본인) — **재료는 다 냈고 고르는 조건을 아직 안 쟀다.**  ■ 왜 지금 안 짓나 ㉠(읽는 목록에 신선도 표기)은 값이 싸고 거짓양성이 없어 유력하지만, 고르는 기준인 **「이 낡음이 실제로 얼마나 자주 나는가」를 안 쟀다.** 내 판은 1회(N=1)다 — 상시라면 ㉠이 맞고, 드물다면 ㉢(아무것도 안 한다)이 맞는데 N=1 로는 못 가른다. 재기 전에 고르면 F103 을 반대 방향으로 재생산한다(안 도는 장치를 하나 더 얹는 것).  ■ 그리고 지금 이 읽기 경로에 손이 몰려 있다 같은 축의 이웃 셋이 오늘 동시에 움직였다 — F596(`61375a0b` · 「열림이 거짓」의 반대 기전) · F582/F589(`f6457d2a` 가 `2569856`·PR #98 로 생사 게이트 착지) · F585(`41b1ad2f` · PR #93 점유 조회). 여기에 내가 네 번째 장치를 같은 목록 출력부에 얹으면 갈라진다 — 「같은 판정을 두 곳에 적으면 갈라진다」(CLAUDE.md 맹점 ④). 신선도 표기를 짓는다면 **그 셋 중 하나에서 파생**시켜야 하고, 그건 그 트랙들이 가라앉은 뒤다.  ■ 이번 판에서 실제로 한 것 (신고를 닫는 일이 아니다) · 내가 지운 `61375a0b` 의 첫 판정을 되살렸다(`eed8cb55` · 경위 주석 포함 · `tests/장부유실.test.js` 13 fail0). · 그 회귀가 이 사고에 **초록이었다**는 사실도 함께 적었다 — 행 수만 세므로 «칸 덮어쓰기»를 원리상 못 본다. 회귀를 늘리진 않았다(그것도 위의 파생 판정에 걸린다).  ■ 닫는 조건 ① 같은 낡음이 2번째로 관측되면(그때 = 시스템 결함) ㉠을 짓는다 — 분모는 그때 세면 된다. ② 또는 위 이웃 트랙 중 하나가 목록 출력부를 공용 통로로 모으면, 신선도 한 줄을 거기 얹는다. |
