| F574 | 2026-08-17 | 마찰 | board-guard 겹침 검사가 「⏳유호 대기」 줄을 분모에서 안 뺀다 — 그 줄의 「만질 파일」 칸에 적힌 파일이 **영구히** 잠긴다. 상한 계산은 이미 뺀다(유호 픽 08-16 ㉮ · 오늘 실측 출력 「도는 트랙 2줄 · 대기 22 · 죽은 11 제외」)는데 겹침 검사만 `isActiveRow` 로 남아 `대기행` 을 안 본다(`.claude/hooks/board-guard.js:1182` 의 `남의선언`).  실측 2026-08-17 `local_ed3b362b`: 🎫 승계로 `tools/lib/보드근거.js` 를 고치려다 `86d6d162` 의 줄에 막혔다. 그 줄은 상태 칸이 **✅착지**(`ebb255f9` = origin/master 조상 확인)이고 주인은 죽었으며(마지막 커밋 08-16 15:59) 남은 과녁은 **⏳유호 픽 ㉮/㉯/㉰** 하나뿐이다. 처방이 0이다 — ①어느 세션도 못 닫고(과녁이 저장소 밖) ②`board-move` 는 원칙⑦로 정확히 거부하고(남은 일이 실재한다) ③남의 활성 줄이라 F073 이 편집을 막는다. 그 칸에 적힌 파일 4개가 그날부터 아무도 못 고치는 자리가 됐다.  새는 방향이 「막힘」이라 조용하지는 않다 — 대신 F103 이 말하는 **따를 수 없는 처방**이 되어 우회를 정상 통로로 만든다(오늘 나는 그 두 파일을 트랙에서 빼는 것으로 우회했고, 그래서 소스 래치에 예외 2건이 남았다).  축이 같은 트랙이 지금 돈다 — `be03f4c7` 「대기열 점유가 「인용」을 「선언」으로 읽는다(실측 파일칸만 1건 = 전부 거짓 잠금)」. 같은 결함의 대기열 판이라 처방을 나눠 짓지 말고 한쪽에서 파생시킨다. | ⏸ 안 고쳤다 — 과녁이 board-guard 겹침 검사( 의 )라 이 트랙(형제 경로 정본)과 다른 층이고, 같은 결함의 대기열 판을  이 지금 돌고 있다(「대기열 점유가 인용을 선언으로 읽는다」). 처방을 두 곳에 나눠 지으면 갈라진다 — 한쪽에서 파생시켜야 한다. 판정 자리 = local_ed3b362b 2026-08-17 · 우회 흔적은  의 래칫 3건에 이름으로 남겨 뒀다. |
