| F596 | 2026-08-18 | 마찰 | 장부의 «열림»이 거짓이다 — 남이 고쳐 놓고 안 닫은 행이 다음 세션들을 헛돌게 한다. 그리고 그걸 잡으라고 지은 장치(F496)는 **원리상** 이 자리를 못 본다.  실측 2026-08-18 (`local_61375a0b` · 트랙을 고르려고 열림 목록을 훑던 자리):  · `node tools/friction.js --open` = **14건**. 그 14건에 대해 「이 번호를 단 `fix:` 커밋이 origin/master 에 있나」를 세니 **3건**이 나왔다.  · **F549** ← `a0a66d71 fix: 검수 프롬프트가 방향 정본을 「절 단위」로 싣는다 (유호 픽 ㉡)`. 보드 줄까지 완료로 아카이브된 상태였다.  · **F562** ← `93b321a1 fix: 고아 잠금 처방이 «자기가 만든 사건»에서 도달 불가였다 — 가름을 자동화한다` (PR #68 머지 · 보드 종결).  · **F544** ← `876d18f9 fix: 굽기 차단문에 «나가는 문»…`. ⚠ 이건 **닫으면 안 되는 것**이었다 — 그 커밋이 처방 셋 중 ③만 넣고 ①②(방향 자동 판정)를 소스 주석에 「F544 에 남겼다」고 적어 뒀다.  ⇒ 즉 열림 14 = **닫아야 할 2** + 정당하게 열린 1(부분 수리) + 진행중 3(F585·F589·F590 은 지금 남의 워크트리가 돈다) + 진짜 열림 8.  ■ 왜 안 잡혔나 — 자리가 아니라 **분모**가 비어 있었다 F496 이 지은 `이세션분`은 「이 세션이 고쳤을 법한 살아있는 행」을 `/close` 에서 잡는다. 그런데 그 고르는 방법이 **`Session-Id` 트레일러로 내 커밋만 모으는 것**이다. 그래서 세션 A 가 고치고 안 닫으면, B·C·D 의 `이세션분`에는 그 행이 **영원히 안 뜬다** — 자기 커밋이 아니기 때문이다. `friction-close-guard` 도 마찬가지로 커밋 시점의 **그 세션**만 본다. 이 저장소는 세션이 십수 개 동시에 돌고(2026-08-18 실측: 살아있는 세션 15~17 · 워크트리 6~8), 인계가 정상 동작이다. 그러면 「남이 고쳤다」가 예외가 아니라 **기본값**이고, 두 장치가 다 그 기본값에 눈이 멀어 있었다.  ■ 값 — 이건 소음이 아니라 조사비다 열림 목록은 「다음에 뭘 집을까」를 고르는 자리다. 나는 이 판에서 F575·F569·#Q99·#Q100·F590·F585·F582 를 차례로 열어 보고서야 「이미 남이 잡았거나 이미 끝났다」를 알았다. 그 조사비를 **모든 세션이 매번** 낸다(F585 가 보드 축에서 신고한 것과 같은 병의 장부 축).  ■ 무엇을 지었나 (같은 커밋) `friction.js --open` 이 행마다 「🩹 이미 닫혔을 수 있다 — <sha> 가 이 번호를 달고 master 에 착지했다」를 붙인다. 새 도구 0 · 새 훅 0 · 새 장부 0 — **있던 목록이 진실을 말하게** 한 것뿐이다.  · 🔴 **자동으로 안 닫는다**(F092: 안 고쳤는데 닫은 실측이 이미 있다). F544 가 그 반례를 같은 판에서 실물로 대 줬다.  · `fix:`·`feat:`·`perf:`·`refactor:` 제목만 센다 — `docs: 마찰 … 신고`·`보류`·보드 커밋은 번호를 달고도 안 고친 것들이라 넣으면 전부 걸려 소음이 된다.  · **`origin/master` 에 담긴 것만** 본다. 안 그러면 남이 지금 워크트리에서 도는 트랙이 「이미 착지」로 읽혀, 진행 중인 남의 일을 닫으라고 권하게 된다.  · git 을 못 부르면 **못 쟀다(null)** 로 낸다 — 0건과 다르다(F207). 표식 수는 늘 분모와 함께 낸다.  ■ 대가 (틀릴 때의 모습) 번호를 안 단 fix 커밋은 **원리상 못 잡는다**(F568 이 그 예다 — 수리는 v9.251 로 갔는데 커밋 제목에 번호가 없다). 그래서 이 표식의 0 은 「고쳐진 것이 없다」가 아니라 「번호를 단 fix 가 없다」이고, 출력이 그 문장을 그대로 쓴다. | friction.js --open 이 행마다 착지 표식을 붙인다(9e8725d8 · PR #99 머지). 새 도구 0 · 새 훅 0 · 새 장부 0 — 있던 목록이 진실을 말하게 한 것. 고침 제목(fix/feat/perf/refactor)만 · origin/master 에 담긴 것만 · 번호 경계로 F54↔F549 오탐 차단 · git 실패는 null(F207). 자동으로 안 닫는다 — F544 가 반례 정본이라 표식이지 판정이 아니다(그 행은 보류로 판정을 적었다). 회귀 7 신설 66/66 · 변이 11/11 구멍0 미적용0(M4 가 F207 접기를, M11 이 자동닫기를 각각 잡는다). |
