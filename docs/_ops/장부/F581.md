| F581 | 2026-08-17 | 마찰 | 이 노트북의 git 이 원격에 못 닿는다 — `git-credential-manager` 가 응답을 안 하고 매달린다. 실측 08-17 22:35~22:42: `git push` 120초 타임아웃 · `git ls-remote origin HEAD` 45초 타임아웃(exit 124) · GIT_TRACE 로 멈추는 지점이 `git-credential-manager get` 바로 뒤로 특정됐다. 원격·인증은 멀쩡하다: `gh api rate_limit` 즉답 · curl 로 github.com 의 git-upload-pack 이 0.3초에 401. 즉 **막힌 층은 네트워크도 토큰도 아니고 git 이 부르는 자격증명 헬퍼 하나**다. 이것이 「커밋과 push 는 한 벌」 규약과 `tools/작업가지.js --열기/--닫기` 를 통째로 죽인다(--열기 가 실제로 죽었다). 일회 우회는 확인됐다(설정 무변경): `git -c credential.helper= -c credential.helper="!gh auth git-credential" push …` — 빈 값으로 먼저 지워야 갈린다(그냥 -c 로 주면 헬퍼가 «덧붙어» GCM 이 그대로 먼저 돈다 · 이 함정에 나도 한 번 걸렸다). ⚠ 영구 수리(`git config --global credential.helper` 교체 · GCM 재설치)는 유호님이 배치한 환경이라 손대지 않고 제안으로 올린다. ⚠ 워크트리 세션에선 GIT_CONFIG_COUNT 환경변수 우회가 하네스에 막히므로(실측) 도구 내부 git 은 못 덮는다 — `--열기` 는 gh pr create 로, `--닫기` 는 그 우회를 쓰는 손 절차로 대신해야 한다. | ⏸ 지금 안 닫는다 — 뿌리(이 노트북의 git credential helper)는 저장소 밖이라 세션이 못 고친다. 다만 F580 트랙에서 «작업가지.js 의 push 가 그 매달림에 걸리면 gh 헬퍼로 자동 우회»하게 만들어 트랙이 막히는 일은 없앴다(회귀 ⑫ · 변이 M4~M7). 즉 아픔은 줄었고 병은 그대로다. 닫는 조건 = 유호님이 Windows 자격 증명 관리자에서 github.com 항목을 지우고 다시 로그인하는 것. 판정 자리 = 2026-08-18 local_61375a0b. |
