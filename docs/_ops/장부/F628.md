| F628 | 2026-08-18 | 마찰 | **커밋 주인 표식(`Session-Id`)이 두 층에서 안 박힌다 — 정본이 「이걸로 읽는다」고 못박은 재료가 지금 안 쌓이고 있다.** 재료 전문 = `docs/_ops/커밋주인표식_공백_0818.md`(독립 검증 1패스 · 초판 9건 정정).  ■ 정본 문장 (CLAUDE.md 세션 규약)   「커밋 주인은 추정하지 말고 **`Session-Id` 트레일러**를 읽는다(`prepare-commit-msg` 훅이 박는다 — author·모델명·시각 인접성은 구분자가 아니다 · F041).」  ■ 실측 — 기준 `origin/master @ ef6b0a41` · 2026-08-18T07:39:59Z (시점 의존이라 기준을 함께 적는다) · **최신 194커밋 연속 트레일러 없음** · 기간 2026-08-18T04:04:47Z → 07:39:59Z (약 3.6시간). · 그 194건의 작성자: `noreply@anthropic.com` 163(그중 **비머지 133**) · `unmet23@gmail.com` 31(**전부 머지 · 비머지 0**). · 이 클론이 보는 332커밋: 트레일러 있음 126 · 없음 206. **있는 126건은 전부 `local_` 접두** — 클라우드가 박은 적이 한 번도 없다. · 마지막 트레일러 = `38612f2b`(`local_9b74b64b`).  ■ 「방치」가 아니라 **커버리지 공백**이다 — 그 갈래가 이 신고의 급소다   유호 계정 31건이 **전부 머지 커밋**이라는 것이 증거다: 노트북 훅은 안 깨졌다. 안 닿는 층이 둘 생겼을 뿐이다.   ① 훅이 없는 **클라우드 컨테이너**(비머지 133) ② 로컬 훅이 **원리상** 못 닿는 **GitHub PR 머지**(31).   ⇒ 「누가 훅을 껐나」를 찾으면 영원히 못 찾는다. 찾을 것은 **닿지 않는 층**이다.  ■ 기전 — 두 겹이라 하나만 고치면 안 고쳐진다 (소스에서 읽었다) · 설치: 이 컨테이너에 `.git/hooks/prepare-commit-msg` 가 **없고** `core.hooksPath` 도 **미설정**(우회 설치도 아니다). 원인은 안 쟀다. · 변수: `tools/githooks/prepare-commit-msg:27` = `[ -n "$CLAUDE_CODE_HOST_SESSION_ID" ] || exit 0`. 클라우드엔 그 변수가 **비어 있다**.   ⇒ **훅을 깔아도 27행에서 그대로 exit 0 한다.**  ■ 실제 손해 — 사람 손이 늘어 있다(철학 ㉡ 위반)   이 세션은 보드·장부 도구를 부를 때마다 `export CLAUDE_CODE_HOST_SESSION_ID="$CLAUDE_CODE_SESSION_ID"` 를 손으로 붙였다.   안 붙이면 `board.js:113` 은 내 보드 줄을 「내 것」으로 못 알아보고, `board-move.js:506` 은 내 완료 줄을 내가 못 옮긴다   (그 도구가 `:580` 에서 스스로 「비면 **내 커밋도 남의 것으로 보인다**」고 말한다). `friction.js:316`·`bump-version.js:193` 은 주인이 `unknown/pid` 다.  ■ 🔑 재료 — 쓸 수 있는 id 가 이미 환경에 있다(아무도 안 쓴다)   `CLAUDE_CODE_REMOTE_SESSION_ID=cse_01Fi49nN7q4mpXF7RsLDkqeZ` — 그 본문이 이 세션 URL `…/session_01Fi49nN7q4mpXF7RsLDkqeZ` 의 본문과 **같다**.   저장소 전수 grep 결과 이 변수를 쓰는 코드는 **0곳**이다.   ⚠ 이 자리에서 초판이 틀렸다 — 「진짜 클라우드 세션 id 는 셸에서 못 읽는다」고 적어 그 갈래를 비싼 쪽으로 매겼는데, 독립 검증이 뒤집었다.  ■ 처방 후보 — 안 골랐다(유호 픽 · 문서 §5·§6 에 3지선다) ㉠ 훅 27행에 폴백 사슬(`HOST` → `REMOTE`(cse_) → `SESSION_ID`).    ⚠대가: 트레일러 값의 형태가 셋이 된다 — 8자리 지문은 서지만 「어느 기계 것인가」를 새로 가려야 한다. ㉡ ㉠ + SessionStart 에서 `install-githooks.js` 를 한 번 돌린다. ⚠대가: 매 세션 스폰 1회 · 설치 실패 원인은 아직 안 쟀다. ㉢ GitHub 머지 커밋 층은 **포기하고 명시**한다(로컬 훅이 원리상 못 닿는다). ⚠대가: 주인 판별 분모가 영구히 「비머지만」이 된다. ㉣ 아무것도 안 한다. ⚠대가: 정본의 그 규칙이 **글자로만 남는다** — 클라우드가 주 통로인 지금, 가리키는 재료가 계속 0이다. 추천 = ㉠+㉢.  ■ 안 잰 것(F348) · 설치가 왜 없는지 안 쟀다(「안 돌렸다」와 「돌렸는데 실패했다」는 같은 모양이다). · 얕은 클론이라 332커밋까지만 봤다. · `cse_` 접두를 `send_message`·`list_sessions` 가 그대로 받는지는 **저장소 밖 계약이라 못 쟀다** — ㉠을 고르면 그것부터 확인해야 한다.  ■ 어떻게 걸렸나 · 겹침   F623 잔여(폰 배선 전수 조사) 중에 나온 **별건**이다. 그 폰 축 자체는 `110a7190` 이 PR 로 먼저 닫아 내 조사분은 폐기했다(F625 계보의 **두 번째 실측**).   이 자리는 그 트랙이 만진 파일과 겹치지 않는다 — `tools/githooks/` 를 만진 커밋이 0건임을 확인했다. | |
