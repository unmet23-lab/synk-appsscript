| F628 | 2026-08-18 | 마찰 | 점유 판정이 **「이미 `origin/master` 에 있는데 내 작업 트리엔 아직 없는 선언」을 원리상 못 보는데**, `못본층: []` 로 «전부 봤다»고 답한다. 그 층은 이미 지어져 있다 — `보드.원격줄들()`(F542) — 인계문 하나만 부르고 **점유 소비자 셋은 안 부른다.**  ■ 라이브 실측 2026-08-18 (`local_9a6c0547` · 이 컨테이너) · 보드 선언을 쓰기 **직전에** 쟀다: `활성줄전량(".")` = 활성 49줄 · **`못본층: []`** · 분모 `{메인 65, 가지 8, 가지출처{로컬 1, 원격 8}}`. · 같은 순간 `원격줄들(".")` = 내가 놓친 표줄 **5** · 그중 **활성 1**(`197f1fe3.md` #Q115). · 그때 내 트리는 `origin/master` 보다 **41 커밋 뒤**였고, 그 뒤처짐은 **17분 만에** 생겼다(직전 측정은 0 뒤였다). 즉 이 사각은 예외 상황이 아니라 **이 저장소의 상시 상태**다. · 뒤처짐 깊이별 실측(같은 이력에서 `~K...origin/master` 로 재현): 2커밋 뒤 → 놓친 활성 0 · 5 → 1 · 10 → 2 · 20 → 3 · 30 → 4 · 50 → 5.  ■ 픽스처로 기전 확정(재현 절차 · 실측이 아니라 원리임을 못박는다) 베어 원격 + 클론 둘. 남이 `origin/master` 에 활성 선언을 올리고 나는 fetch 만 한 상태(ref 는 있고 트리는 1커밋 뒤): `활성줄전량` = 줄 1 · **`못본층: []`** · `가지출처 {로컬 0, 원격 0}` / `원격줄들` = 그 선언 1줄. 가지 층이 못 잡는 이유는 구조적이다 — `가지끝()` 은 `branch -r --no-merged origin/master` 라 **`origin/master` 자신은 정의상 제외**된다. 즉 «가지에도 없고 내 디스크에도 없는» 자리가 통째로 빈다.  ■ 왜 신호인가 — 새는 방향이 「집어라」다 세 소비자가 전부 이 값을 「지금 누가 뭘 잡았나」로 읽는다: `board-guard` 겹침 검사 ④ · `대기열.js` 점유 · `friction.js --open` 잡힘. 이 층이 비면 그 답은 **「빈 자리」**로 나오고, 그건 다음 세션에게 「집어라」다. 그리고 `못본층: []` 이 **분모까지 초록으로 만든다** — F207 이 이름 댄 「미실행이 통과와 같은 모양」 그 자리이고, 여기선 자기 입으로 「전부 봤다」고 **단언**까지 한다. `friction --open` 이 화면에 내는 문구가 그 단언의 얼굴이다: 「🔒 점유는 **메인 보드 + 미머지 가지**를 함께 본다(F521)」 — 두 층을 정직하게 적어 놓고, 세 번째 층이 존재한다는 사실은 어디에도 없다.  ■ F619 와 무엇이 다른가 (같은 트랙에 얹지 말 것) F619 = 「**원격 전용 가지**를 못 본다」 — 아직 머지 안 된 남의 가지. `가지끝()` 이 로컬 가지만 훑던 것을 원격까지 넓혀 닫았다(`099961e3`). 이번 = 「**이미 머지된** 선언인데 내 트리가 안 받았다」 — 가지 층이 `--no-merged` 로 **일부러** 빼는 자리다. 처방도 정반대다: 앞은 `gh pr list`, 뒤는 `git merge --ff-only origin/master`(F542 가 인계문 쪽에서 이미 그렇게 갈라 뒀다).  ■ 값과 대가 · 값 = 이 저장소가 반복해서 치른 가장 비싼 실패(중복 구현)의 한 갈래다 — F585(조사 한 벌) · F615(트랙 1개 전체) · F625(구현 한 벌 + CI 165초 + 변이 런 2회). · 대가 = git 스폰 1회. 실측 `활성줄전량` 230ms → 243ms(**+13ms · +6%**). 부르는 자리가 세션 경계·선언 시점이라 초당 도는 것이 없다. · 틀릴 때의 모습 = `origin/master` ref 는 **마지막 fetch 만큼만** 새것이다. 안 받은 판은 여전히 안 보이고 방향은 「못 봄」이다. 이 층이 좁히는 것은 **「이미 손에 있는데 안 열어 본 것」**이다(F542 가 같은 대가를 이미 적어 뒀다 · fetch 시점 자체는 F619 ㉢ 로 열려 있다).  ■ 안 잰 것(F348) · 이 사각 때문에 실제로 중복 선언이 난 건수는 **안 셌다.** 오늘 관측한 경합 둘(F616 · F619)은 둘 다 **원격 가지** 모양이라 F619 가 이미 닫은 갈래다 — 이 신호를 설명하지 않는다. 즉 지금 근거는 「원리 + 라이브 사각 1건(활성 1줄)」이지 「사고 N건」이 아니다. · 세는 법은 있다: 보드 이력에서 「같은 F번호·같은 파일을 선언한 서로 다른 지문의 줄」 쌍 중, 뒤에 쓴 쪽의 트리가 앞선 선언을 `origin/master` 에 두고도 안 받은 상태였는지 대조하면 된다(F615 가 적어 둔 빈도 측정과 같은 자리 · 안 했다). | `보드.원격줄들()`(F542) 을 점유 공용 통로(`활성줄전량`)에 배선했다 — `9dca60a3`. 새로 지은 것이 아니라 **이미 있던 층을 소비자 셋에 잇는 것**이다(인계문만 부르고 있었다). ①정규화를 `원격몫()` 한 벌로 모았다(부르는 쪽이 둘이 된 순간 = 갈라지기 직전) ②이름표·차단문을 «가지»와 갈랐다 — 처방이 정반대다(가지=PR 을 봐라 / 원격=`git merge --ff-only` 로 받아라 · 뭉치면 없는 PR 을 뒤진다 F103) ③소비자 셋이 전부 읽는다(board-guard 겹침 ④ · 대기열 초록 옆 · friction --open) — 안 읽히는 분모는 없는 것과 같다. 값 = git 스폰 1회(`활성줄전량` 230→243ms · **+6%**). 회귀 18 신설 + 훅 회귀 2(`SYNK_BOARD_REMOTE_LOG` 주입구를 같은 커밋에 냈다 — 발동 조건 없는 장치는 안 돈다) · **변이 15/15 구멍 0 · 미적용 0**(첫 판 구멍 2는 둘 다 진짜였다: `저장소인가` 갈래가 픽스처에서 도달조차 안 했고, `점유()` 가 몫을 나르는 자리에 회귀가 0이었다) · CI 모사 4666 pass 4531 **fail 0**(linux). 🔴 원격 run 0건(Actions 소진 · 리셋 9/1)이라 초록은 모사 층뿐이다. ⚠ **㉢ 은 안 닫았다** — `origin/master` ref 는 마지막 fetch 만큼만 새것이라 안 받은 판은 여전히 안 보인다(F619 ㉢ 과 같은 자리). 이 층이 좁히는 것은 「이미 손에 있는데 안 열어 본 것」이고, 그래서 화면 처방에 fetch 를 함께 적는다. ⚠ 덤으로 잡은 것: friction 의 분모 인쇄가 CLI 안에 **한 벌 더** 있었고 정본이 칸 이름을 바꾼 뒤라 값이 멀쩡한데도 「❔ 못 쟀다」가 떴다(F608 과 같은 병) — 사본을 지우고 정본을 부르게 했다. |
