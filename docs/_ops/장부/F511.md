| F511 | 2026-08-16 | 마찰 | 세션 시작 훅이 멈춘 런의 「이어받기」로 **빈 명령**을 내줬다 — 게다가 그 도구는 그 런을 이어받을 수 없는 도구였다. 런 장부에 새 종류(`사실심문`)가 들어왔는데 읽는 쪽 셋이 그걸 모른 채 갈라져 있다.  실측(2026-08-16 · 런 레코드 22건 전수): · `사실심문` 2건만 `인자` 칸이 아예 없다(대신 `작업id`·`질문`·`생성응답`) — 마누스 통로가 적는 레코드 모양이 codex 통로와 다르다. · `.claude/hooks/review-runs.js:114` = `(x.런.인자 || []).join(' ')` 라 **안 죽고** `이어받기: node tools/codex-review.js ` (인자 빈칸)을 낸다. 꼬리말도 「끝난 회차는 체크포인트에서 그대로 돌아온다」인데 사실심문엔 체크포인트가 없다(원격 작업id 에 다시 붙는 것이다). 맞는 얼굴로 틀린 값 — 세션을 여는 **모든 세션**이 이 줄을 본다. · `tools/codex-review.js:1801` = `x.런.인자.join(' ')` 라 **TypeError 로 죽는다**(exit 2). 멈춤 절에서 끊겨 그 뒤 「도는 중」 목록이 통째로 안 나온다. · `tools/마누스.js:386` 만 맞다 — 종류로 거르고 `node tools/마누스.js --이어받기 <런ID>` 를 낸다.  같은 병의 3번째다. F509 가 **상태** 판정을 `상태이름()` 한 곳에 모았는데, **이어받기 명령**은 아직 세 곳에 손으로 적혀 있었다. 종류가 하나 더 늘 때마다 세 곳이 또 갈린다.  새는 방향: 언제나 「통과」. 훅은 안 죽고 그럴듯한 명령을 주고, 사람은 그걸 그대로 붙여 넣는다. | `검수런.무응답분(런)` — 멈춤 판정 시계를 `재개 || 시작` 으로 갈랐다(화면의 「N분」은 `시작` 기준 그대로) + `마누스.재개덧(pid)` — 다시 붙을 때 재개 도장을 찍고 죽은 폴러가 남긴 `끝`·`종료코드`·`비고` 잔재를 지운다. 이어받은 뒤 다시 무응답이면 도로 멈춤이고 사유에 「이어받은 뒤 N분 무응답」이 적힌다(시계 초기화가 영구 면죄부가 되지 않게).  커밋 `6036774b`(PR #15) · 회귀 68/68 · 변이 7/7(구멍 0 · 미적용 0 — 시계 되돌리기·판정이 안 부르기·화면 분까지 바꾸기·꼬리 지우기·도장 안 찍기·잔재 안 지우기·분기가 재개덧을 안 부르기). 실물 확인: 살아 있는 런 `사실심문-2026-08-16T1057-07e8a7` 에 이 코드로 다시 붙였더니 두 소비자가 「멈춤 · 이어받아라」 → 「⏳ 도는 중 193분째」로 바뀌었다.  ⚠ 이 신고의 **명령 층은 `b489bd18` 이 F508 로 먼저 닫았다**(`372c9e2e` · 종류 «이름» 이 아니라 `작업id` 구조로 가른다 · `인자: []` 를 「모름」이 아니라 「기본 인자」로 읽는다). 그쪽 판이 내 것보다 나아 충돌 5파일을 전부 그쪽으로 취하고 내 중복 구현은 철회했다. 이 해소가 덮는 것은 그 뒤에 남은 층 하나 — **처방을 따랐는데도 따랐다는 사실이 안 보이던 자리**다. 명령이 맞아진 뒤 실제로 이어받아 봐야 나오는 층이라 F508 없이는 볼 수 없었다. |
