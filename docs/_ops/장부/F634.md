| F634 | 2026-08-18 | 마찰 | **「내가 누구인가」의 정답이 층마다 다른데 정본 통로는 하나뿐이다 — 클라우드 세션은 정본 통로에서 «빈 지문»을 받는다.** F628 이 트레일러 축만 보고 보드 축을 안 봤다.  ■ 실측 2026-08-18 (클라우드 세션 `cloud_79415d5c` · 컨테이너 `container_019Z…` · 유호 지시 「어제 구성한 시스템 점검」)  · 보드 정본 파일명 규격 = hex 8자리다 — `board-id.파일지문()` 이 `/^([0-9a-f]{8})\.md$/` 로 못박는다.  · 이 컨테이너의 세션 id 후보 3종을 `board-id.지문()` 에 그대로 넣어 쟀다:      HOST(`CLAUDE_CODE_HOST_SESSION_ID`)    = **unset** → 지문 `''`        → 보드 파일명 불가      REMOTE(`CLAUDE_CODE_REMOTE_SESSION_ID`) = `cse_01Scr7GJ3A…` → 지문 `01scr7gj` → **hex 아님** → 보드 파일명 원리상 불가      SESSION(`CLAUDE_CODE_SESSION_ID`)       = `79415d5c-36d0-…` → 지문 `79415d5c` → **유일하게 유효**  · 그런데 F628 회귀(`tests/커밋트레일러.test.js`)는 SESSION 축으로 내려가는 것을 **명시적으로 금지한다** —    근거는 「그건 메시지를 못 보내는 내부 id 라, 거기까지 폴백하면 트레일러의 뜻이 «연락 가능한 id» 에서    «그냥 구분자» 로 내려앉는다」. 트레일러 축에선 그 판단이 옳다.  · 실물 대조 — 이 저장소의 클라우드 세션 보드 파일 4벌은 **전부 SESSION 축**이다:    `ad0673f3.md` · `d427746a.md` · `110a7190.md` · `cc593518.md` (모두 UUID 앞 8자리 · hex).    그리고 `ad0673f3` 이 실제로 낸 커밋의 트레일러는 `ad0673f3-a839-5087-…`(UUID)이지 `cse_…` 가 아니다.    즉 **F628 이 넣은 REMOTE 폴백은 실사용 커밋에 한 번도 안 박혔다**(아래 ■2 가 그 이유다).  · 결과 — 클라우드에서 이 여덟 자리가 전부 빈 지문으로 돈다:    `board.js:113`(내 줄 못 찾음) · `board-move.js:535`(내 줄 못 옮김) · `대기열.js:1087`(`--집기` 가 오류로 거절)    · `작업본소유자.js:65`(내가 산 세션인지 판정 불가) · `인계문수거.js:91` · `게이트.js:74` · `friction.js:1073`(`--이세션`)    · `bump-version.js:193`. 여덟 자리 전부 `process.env.CLAUDE_CODE_HOST_SESSION_ID` 를 **각자** 읽는다.  ■2 곁가지 실측 — F628 폴백이 이 컨테이너에서 원리상 발화하지 않는다  · `.git/hooks/` 가 **비어 있다**(`core.hooksPath` unset · `install-githooks` 는 어디에도 등록 안 됨 — 수동 도구).  · 그래서 이 세션의 시작 훅이 낸 커밋 8건의 `Session-Id` 트레일러가 **0/8** 이다.  · 분모: `origin/master` 최근 200커밋 중 **비머지 137건에 트레일러 11건(8%)** · 머지 63건은 0(원리상 못 닿음 — F628 기록대로).    CLAUDE.md 세션 규약이 「커밋 주인은 추정하지 말고 `Session-Id` 트레일러를 읽는다(F041)」고 못박은 재료가    주 통로에서 **92% 비어 있다.**  · F628 커밋이 이 한계를 스스로 적어 뒀다(「설치 층은 유호 픽에서 제외됐다」) — 새 것은 **분모**다. 「구멍이 있다」가 아니라 「8%」다.  ■ 왜 신호인가 — 새는 방향이 「통과」이고, 이미 3회차다  · 같은 뿌리(HOST 만 읽는다)가 커밋 트레일러 층에서 한 번(F628), 보드·대기열 층에서 다시 났다.    CLAUDE.md 신뢰성 절이 정한 처분은 **3번째 = 원인을 쓸 수 없게 만든다** — 호출부마다 고치는 대신    잘못 쓸 수 없는 공용 통로 + 옛 통로를 테스트로 금지. 지금은 호출부가 여덟이고 통로가 없다.  · 그리고 `board-id.js` 머리말이 이 새는 방향을 **이미 예언해 뒀다**: 「빈 문자열끼리 `===` 로 비교하면    주인 없는 파일이 통째로 내 것이 된다 · 새는 방향은 여기서도 「통과」다」. 예언한 자리와 난 자리가 같다.    (`board-move.js:529~536` 은 그 함정을 실제로 막아 뒀다 — 막은 자리가 있다는 것이 나머지가 안전하다는 뜻은 아니다.)  · 축이 F628 과 다른 지점: F628 은 **한 값**(연락 가능한 id)을 못 구한 문제였다. 이건 **두 값이 서로 다른    규격을 요구하는데 그것을 뽑는 함수가 하나**인 문제다. 폴백 사슬을 아무리 늘려도 한 함수로는 못 푼다.  ■ 처방 후보 — 안 골랐다(이 신고는 재료다 · 유호 픽 자리)  ㉠ `board-id` 를 **두 함수로 가른다**: `연락지문()`(HOST→REMOTE · 트레일러 몫 · F628 규율 그대로)와     `보드지문()`(HOST→SESSION · hex8 을 요구 · 파일명 몫). 호출부 여덟을 그 둘 중 하나로 보내고,     `process.env.CLAUDE_CODE_HOST_SESSION_ID` 직독을 테스트로 금지한다.     ⚠대가: 「지문」이라는 낱말이 둘로 갈린다 — 갈린 낱말은 잘못 고른다. 그래서 이름에 용도를 박아야 하고,       그래도 옛 커밋·옛 보드 줄은 한 축으로 적혀 있어 **과거 판정이 갈릴 수 있다**(안 쟀다).  ㉡ `install-githooks` 를 SessionStart 훅으로 등록해 클라우드에서도 트레일러가 박히게 한다.     ⚠대가: 시작 훅이 **저장소 상태를 바꾸는** 열넷째가 된다(지금 열셋 중 상태를 바꾸는 것은 보드수거 하나).       그리고 트레일러가 `cse_…` 로 박히면 보드 파일명(`hex8`)과 **다른 축**이라 F041 이 여전히 두 값을 본다 —       ㉠ 없이 ㉡만 하면 주인 판별이 오히려 더 갈린다.  ㉢ 아무것도 안 한다 — 클라우드 세션이 보드를 손으로 hex8 로 적는 관행이 실제로 돌고 있다(4벌 실측).     ⚠대가: 관행은 기계가 아니다. `대기열.js --집기` 는 지금도 오류로 거절하므로 클라우드 세션은       **일감을 규약대로 집을 수 없다**(손으로 보드 줄을 쓰는 우회가 정상 통로가 된다 · F103 그 모양).  ■ 안 잰 것 (F348)  · 노트북 세션에서 HOST 가 실제로 어떤 값인지 안 봤다 — 이 컨테이너엔 그 층이 없다.  · 빈 지문으로 도는 여덟 자리 중 **실제로 오판을 낸 자리**는 `대기열.js --집기` 하나만 확인했다(오류로 거절 = fail-closed).    나머지 일곱이 fail-closed 인지 fail-open 인지는 각각 안 열었다.  · `cse_` ↔ `session_` 접두 대응(F628 이 표본 1건으로 관측)이 이 컨테이너에서도 같은지 안 봤다. | |
