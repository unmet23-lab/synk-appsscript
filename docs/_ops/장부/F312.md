| F312 | 2026-08-10 | 실수 | F244 를 닫을 때 「발화 자리를 `작업본소유자.js` 에서 rot-check 로 **옮긴 것**이 이 수리의 핵심」이라고 적어 놓고, 실제로는 옛 자리를 안 지웠다 — 옮긴 게 아니라 **복제**였다. 그래서 같은 판정이 두 층에 남았고, 옛 층은 F244 자신이 「양방향으로 틀린다」고 못박은 `[vN]` 기준선을 그대로 쓴다. 실측 2026-08-10 `local_a621f018`: 같은 시각 `안나간변경()` 은 「엔진_셋업확장.js 가 안 나갔다」(기준선 `e601b25` · 커밋 30개), `라이브대조()` 는 「라이브 = 저장소 · 배포집합 15개 바이트 동일」을 냈다. 정확히 F244 메모리가 예언한 거짓양성 ②(채번 없이 나간 배포 뒤의 커밋이 계속 「안 나갔다」로 잡힌다)다. 무게가 다른 이유는 **주기**다 — rot-check 는 하루 1회지만 이 층은 **매 SessionStart** 발화라 거짓 🔴 도 매번이고, 매번 틀리는 경고는 진짜 드리프트가 왔을 때 이미 무시당하고 있다(rot-check 배포 절 머리말이 스스로 「하루에 한 번 거짓말하는 가드는 곧 무시당한다」고 적어 둔 그 자리다). 실제 피해도 났다: 이 세션은 그 경보와 보드의 「⛔라이브 미배포」 줄을 근거로 「v9.203 이 라이브에 없다」고 읽고 배포 트랙을 열 뻔했고, 라이브를 직접 재고서야 이미 나가 있음을 알았다.  교훈은 「옮겼다」를 **커밋에서 확인하지 않고 메모리에 적었다**는 것이다. 옛 자리를 지웠는지는 grep 한 번이면 갈렸는데, 수리 노트가 의도를 완료형으로 적는 순간 다음 세션들은 그것을 실측으로 읽는다. | 배포판점검 라이브도장(지문 동반) + rot-check 배포도장 — 라이브 실측이 초록이고 그 뒤 배포집합이 안 바뀐 프로젝트만 재운다(회귀 6·변이 4/4) |
