| F529 | 2026-08-17 | 마찰 | 인계문 생성기가 워크트리 가지 안의 보드 선언을 못 봐서 「트랙 없이 끝난 세션이다 — 추정이 아니라 실측이다 · 보드를 다시 열 필요 없다」고 거짓 단정한다 — F521(대기열)·F528(board-guard) 에 이은 세 번째 소비자이고, 대가는 앞의 둘보다 크다. 실측 2026-08-17: 세션 24ba4d6b 는 10:04:51 에 워크트리 가지 안에서 보드를 선언하고(a81e7775), 10:26:32 에 수리를 착지시키고(fe56e332), 10:28:30 에 「⚠ 미검증 1건: 변이 검사는 아직 안 돌았다 … 다음 세션이 재실행한 뒤 머지한다」를 커밋 본문에 남긴 채(4008ad34) PR #24 를 열어 두고 마감했다. 그런데 4분 뒤인 10:32 에 쓰인 그 세션의 인계문(docs/_ops/인계문/24ba4d6b.md)은 「▶ 트랙 없이 끝난 세션이다 — 추정이 아니라 실측이다: 보드(node tools/board.js)의 줄이 전부 세션별 보드 파일 이름·지문·커밋 해시로 주인을 밝히고 있고 그중 그 세션(24ba4d6b)의 줄은 없다. 보드를 다시 열 필요 없다.」고 적었다. 원인은 F528 과 같은 자리다 — 생성기가 master 의 docs/_ops/보드/*.md 만 읽고, F521 이 이걸 위해 만든 통로 가지줄들()(tools/lib/보드.js)을 안 쓴다. 새는 방향은 여기서도 「통과」인데 이 얼굴이 가장 위험한 이유는 인계문이 다음 세션의 첫 입력이라는 것이다: 「보드를 다시 열 필요 없다」는 문장이 확인 자체를 금지해서, 못 보는 것을 넘어 안 보게 만든다. 이 인계문만 읽은 세션이었다면 유호 픽 08-16 ㉮ 의 집행이 변이 미검증 1건만 남긴 채 워크트리와 함께 사라졌을 자리다 — F403 실사고의 재현 조건이 그대로 갖춰져 있었다. 발견 경위 자체가 증거다: 02e398f7 은 그 인계문이 아니라 SessionStart 의 워크트리 갇힘 경보와 커밋 메시지를 읽어서 잡았고, 인계문에 없던 🎫 는 커밋 본문에만 있었다. 처방 자리는 F528 과 한 벌이다(목록은 하나에서 파생) — 인계문 생성기·board-guard 둘 다 가지줄들() 을 지나게 한다. | session-report.js 의 보드 읽는 세 통로(boardTrack·무주줄·낡음)를 전부 보드.가지줄들()(F521 통로) 뒤로 보냈다 — 고르는 규칙은 함수 하나를 master/가지에 두 번 부른다(갈라짐 방지). 찾은 줄엔 가지 이름+gh pr list(F403)를 달고, 못 잰 회차(모름)엔 「다시 열 필요 없다」를 아예 안 쓴다. 읽는 쪽 낡음은 가지에서 찾았을 때만 '가지' 판정으로 바꾼다(순증 — '사라짐'은 이미 확인을 시키는 문구라 안 내렸다). 실측 boardTrack 옛 0/10 → 새 10/10 · 회귀 tests/트랙경계.test.js 42/42(신설 6) · 변이 7/7 · 커밋 5c270fc2 |
