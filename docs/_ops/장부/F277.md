| F277 | 2026-08-09 | 마찰 | SYNK-talk 적색 1건이 주인 없이 남았다 — `70f8692`(cd81c070)가 `deliver?점검` 에 `current_staff()` 를 넣자 `tests/검수큐.test.js` 가 deliver 를 「직원 통로」로 분류했고, 원래부터 있던 배달 조회(`s.task_ref`·`task_snapshot`·`due_at`)가 그 규칙에 걸렸다(거짓양성). 그 세션은 「변이 3/3」으로 종결했는데 **전 스위트를 안 돌려** 자기 커밋이 만든 적색을 못 봤다 — 남의 배포 게이트가 그날부터 막힌다. 판정(검사를 좁히나 / deliver 를 뷰로 돌리나)은 검수 콘솔 트랙(`0482f24b`) 몫. | |
