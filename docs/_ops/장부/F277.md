| F277 | 2026-08-09 | 마찰 | SYNK-talk 적색 1건이 주인 없이 남았다 — `70f8692`(cd81c070)가 `deliver?점검` 에 `current_staff()` 를 넣자 `tests/검수큐.test.js` 가 deliver 를 「직원 통로」로 분류했고, 원래부터 있던 배달 조회(`s.task_ref`·`task_snapshot`·`due_at`)가 그 규칙에 걸렸다(거짓양성). 그 세션은 「변이 3/3」으로 종결했는데 **전 스위트를 안 돌려** 자기 커밋이 만든 적색을 못 봤다 — 남의 배포 게이트가 그날부터 막힌다. 판정(검사를 좁히나 / deliver 를 뷰로 돌리나)은 검수 콘솔 트랙(`0482f24b`) 몫. | tests/검수큐.test.js ⑤ — 판정 재료를 셋으로 갈랐다: ①주석을 지우고 sql 리터럴 안쪽만 본다(TS 타입 선언은 쿼리가 아니다) ②쓰기는 from/join engine.submissions 조건이 이미 거른다 ③「직원 통로」를 파일이 아니라 톱레벨 함수 스코프로 판정한다. 실측: 지목된 6열 중 4개가 insert 열 목록·2개가 주석과 TS 타입이고, 진짜 select 는 배달하기() 하나인데 그 경로는 service_role 전용이라 원장이 도달할 수 없다 — 원장이 닿는 점검하기() 는 submissions 를 한 번도 안 읽는다(count 3개가 전부). ②에 읽기부분() 을 한 벌 만들었다가 지웠다: 변이가 「아무도 안 잡는다」로 드러냈고 하는 일 없는 코드는 잘못 고칠 여지만 만든다. 좁히는 수리라 대가를 적었다 — 직원 확인을 A함수, 원표 읽기를 B함수로 나누면 못 본다(그 자리는 1차 방어선인 뷰 열 목록·정책 부재 검사가 진다). 회귀 7(거짓양성 3+탐지력 4)이고 세 축을 한 픽스처에 섞지 않았다 — 섞으면 앞 축이 걸러 뒤 축이 실행조차 안 되고 첫 판이 그래서 변이 ①②를 못 잡았다. 검수큐 11/11 · 전 스위트 675/675 적색 0 · 변이 5/5 · F083 재현: 스코프 자르기를 끄면 실저장소 검사가 다시 빨개진다. talk a2f595c |
