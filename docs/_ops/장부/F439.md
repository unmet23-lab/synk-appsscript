| F439 | 2026-08-14 | 실수 | 확인과 행위를 **같은 셸 명령에 묶어** 남의 미커밋 파일을 덮었다 — 확인은 돌았는데 게이트가 아니었다. `git status --short -- docs/_ops/작업대기열.md && node <제거스크립트> …` 를 한 호출로 보냈다. status 는 정직하게 ` M`(남의 미커밋)을 찍었지만 **그 출력을 내가 읽기 전에 뒷명령이 이미 파일을 고쳤다.** 결과로 내 편집이 옆 세션 `6e8dd8b4` 의 커밋 `b160253a` 에 실려 나갔다(F073 이 경고하는 바로 그 모양). 이번엔 피해가 0이었다 — 지운 것이 겹치지 않는 줄 하나였고 그쪽 편집도 그대로 살아 대기열이 온전하다(#Q70 `[x]` + #Q72 제거 둘 다 착지) — 그러나 그건 **운이지 절차가 아니다.** 급소: 여러 명령을 한 호출로 묶는 것은 턴을 아끼는 정상 습관이고 이 저장소는 세션이 6개씩 도는 시간대가 있어, 「확인 && 행위」 묶음은 **상시 조건**이다. 닫으려면 「확인의 출력을 사람이 읽고 나서야 행위가 가능한 형태」로 바꿔야 한다 — 예: 편집 스크립트가 스스로 대상 경로의 `git status` 를 물어 남의 미커밋이면 멈추는 것(작업본소유자.js 판정을 그 자리에서 부르는 형태). 지금은 장치를 안 지었다. | ⏸ 안 고쳤다 — 장치는 여전히 0이고, 이번엔 **손 규율로만** 지났다(세션 `ddb6d85b` 08-15 · 대기열 #Q84 중복 해소 `105b52a1`). 판정 자리 = 그 커밋.  이번에 지나간 방식: 편집 대상(`docs/_ops/작업대기열.md`)의 `git status` 를 **읽기 전용으로 따로** 부르고, 그 출력을 읽은 «뒤» 별개 호출로 편집했다(확인 && 행위를 한 호출에 안 묶었다).  🔑 이번 실측이 신고의 처방을 한 칸 더 좁힌다 — **그 파일의 깨끗함이 몇 분 사이에 두 번 뒤집혔다.** 처음 볼 때 깨끗 → 다음에 볼 때 남의 미커밋 → 그다음엔 다시 깨끗(그 세션이 커밋했다). 즉 이 저장소에서 「남의 미커밋인가」는 **상태가 아니라 사진**이고, 신고가 지목한 「한 호출에 묶는 것」의 진짜 해악은 순서가 아니라 **간격**이다: 확인과 쓰기 사이가 벌어질수록 사진이 낡는다. 그래서 처방은 「사람이 출력을 읽고 나서 행위」보다 **「쓰기 직전에 쓰는 쪽이 스스로 다시 묻는다」**가 정확하다(작업본소유자 판정을 편집 통로 안에서 부르는 형태 — 신고가 괄호로 적어 둔 그 갈래). 사람이 읽는 방식은 이번처럼 통하지만, 통한 이유가 「내가 두 번 봤다」라 재현이 사람에게 달려 있다.  지금 안 닫는 이유: ①장치를 지을 자리(`tools/githooks/pre-commit` 또는 공용 편집 통로)가 지금 **남의 미커밋**이다(`0f422273` 이 F454 제어문자 작업으로 잡고 있다) — 그 파일을 건드리면 이 신고가 경고한 바로 그 사고를 내가 낸다 ②그리고 이 자리는 「가드 하나 더」보다 **어디를 공용 통로로 삼을지**가 먼저다(호출부마다 status 를 부르면 3번째 재발 때 또 흩어진다 — CLAUDE.md 「원인을 쓸 수 없게 만든다」). |
