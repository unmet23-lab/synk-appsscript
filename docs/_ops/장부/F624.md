| F624 | 2026-08-18 | 마찰 | 보드 «선언»을 셸로 쓰면 점유 가드가 통째로 안 돈다 — 등록 매처가 `Edit|Write|MultiEdit` 뿐이다. 2026-08-18 실측(`local_9a6c0547`): 트랙을 고르고 보드 줄을 `cat > docs/_ops/보드/9a6c0547.md <<EOF` 로 썼다. 그 순간 `board-guard` 는 **한 번도 안 돌았다**(그 훅은 settings.json 에서 `Edit|Write|MultiEdit` 에만 걸려 있다). 그래서 ①트랙 충돌(남의 활성 줄과 제목 겹침) ②칸 200자·활성 12줄·전체 18줄 상한 ③줄에 내 지문이 적혔나 — 셋이 전부 건너뛰어졌다. 결과: 세션 `197f1fe3` 이 06:30:23 에 선언한 F616 트랙을 내가 06:31:45 에 다시 선언했다(**82초 차**). 둘 다 「비었다」를 봤고 둘 다 지었다 — 나는 통로 파일까지 짜고 나서야 알았고, 그 구현을 버렸다(오늘 네 번째 경합 · 장부 610·612·615 계보). ⚠ 그 82초가 **막을 수 있던 창**이었다는 것이 이 신고의 급소다: 그쪽 선언은 06:31:27 에 origin/master 에 착지했고 내 선언은 그 **18초 뒤**다. `board-guard` 가 돌았으면(그 훅은 다른 워크트리·origin 까지 읽는다) 겹침을 봤을 것이고, 최소한 `repo-staleness` 가 「3커밋 뒤처졌다」를 그 자리에서 말했을 것이다 — 실제로 그 훅은 **나중에** 내가 `Write` 로 코드 파일을 만들 때 그 문장을 냈다. 즉 장치는 멀쩡했고 **선언 경로만 그 밖에 있었다.** 🔑 CLAUDE.md 「가드는 로직보다 **등록층**에서 샌다 — 매처·필터가 훅이 막는 것을 다 잡는지 회귀로 검사한다」가 이미 있는데, 이 짝(Bash ↔ board-guard)만 비어 있었다. 그리고 이 저장소는 셸 쓰기를 막는 훅(`code-edit-guard`)을 이미 갖고 있지만 그 과녁이 **코드 확장자**뿐이라 `.md` 인 보드 조각은 통과였다 — 재현: `{"tool_name":"Bash","tool_input":{"command":"cat > docs/_ops/보드/deadbeef.md <<EOF…"}}` → exit 0(통과), 같은 모양으로 `tools/aaa.js` 를 주면 deny. ⚠ 안 잰 것: 같은 등록층 사각이 다른 훅에도 몇 곳인지 안 셌다(이 짝 하나만 직접 증거다). | `code-edit-guard` 의 과녁에 **보드 정본 조각**(`docs/_ops/보드/<지문>.md`)을 넣었다 — 새 훅을 안 세운 이유는 그 파일 자신이 적어 둔 대로 「모든 셸 호출에 실제로 도는 유일한 훅」이라서다(프로세스 +0). 통로가 둘이라 **둘 다** 넓혔다: 문자열 스캔(`코드경로들` — 리다이렉트·힙독·tee·인라인 스크립트)과 토큰 판정(`코드대상인가` — cp·mv), 그리고 인용 안을 살리는 `keepQuoted`. 한쪽만 넓히면 나머지가 조용히 새는데, 그 구멍을 변이가 실제로 잡았다(첫 판은 cp·mv 갈래가 열려 있었다). 🔑 **처방을 갈랐다** — 코드는 「`Edit` 로 고쳐라」지만 보드는 **「`Write` 로 써라」**다. 같은 처방을 주면 따라도 `board-guard` 는 여전히 안 돈다(F103). 차단문이 무엇이 안 도는지(겹침·상한·지문)와 조립 명령(`대기열.js --집기`)·이관 도구(`board-move.js`)까지 함께 낸다. 차단 이름도 갈랐다(「코드 파일」 → 「보드 정본 조각」) — 맞는 얼굴로 틀린 이름은 다음 사람이 메시지를 안 읽게 만든다. ⚠ **안 막는 것**을 같은 무게로 못박았다(거짓양성이 BYPASS 를 손버릇으로 만든다): 읽기(`cat`) · 노드 도구(`board-move.js`·`보드수거.js` — 셸 파서 눈에 안 보이고 그게 옳다) · 다른 `.md`(장부·인계문) · 스크래치패드. 회귀 3건(`tests/코드편집가드.test.js`) · 변이 **6/6 구멍0**(문자열 갈래·토큰 갈래·인용 살리기·처방 삭제·이름 뭉개기·과녁을 모든 md 로 넓히기). ⚠ 남는 대가: 셸이 아닌 경로로 보드를 쓰는 방법(예: 파이썬 도구를 새로 만들어 쓰기)은 여전히 이 훅 밖이다 — 과녁은 「셸 쓰기」이지 「모든 쓰기」가 아니다. 그리고 이 수리는 **경합 창 자체를 0으로 만들지 않는다**(장부 615 가 그 축이다): 막는 것은 「가드가 아예 안 도는」 자리이고, 「돌았는데 10초 차로 늦은」 자리는 그대로 열려 있다. |
