| F458 | 2026-08-15 | 마찰 | board-guard 의 트랙 겹침 차단에 **「진 쪽이 이미 끝냈다」 갈래가 없다** — 끝낸 세션이 자기 줄을 ✅종결로 못 고쳐 «끝났는데 착수»로 영구히 남는다  실측 2026-08-15(세션 `local_c1c17692` · #Q82). 순서가 이랬다: ① 내가 `docs/_ops/보드/c1c17692.md` 에 #Q82 를 선언했고 board-guard 가 **통과**시켰다(그때 겹치는 활성 줄이 없었다 — 같은 세션이 직전 #Q48 선언은 `7c9ab428` 겹침으로 정확히 deny 했으니 가드 자체는 돌고 있었다). ② 그 뒤 살아있는 `7aa81186` 이 같은 #Q82 를 자기 보드에 선언했다. ③ 일을 다 끝내고(코드+회귀 23/23+변이 6/6+`test-ci` 3332 초록) 내 줄의 상태 칸을 `🔵착수` → `✅종결` 로 바꾸려 하자 board-guard 가 **deny** 했다: 「이 트랙은 살아있는 다른 세션(7aa81186)이 이미 선언했다 — 비켜난다」.  ■ 급소 — 가드가 막은 것이 «중복 구현»이 아니라 «완료 기록»이다 차단문의 처방(「비켜난다 — 다른 것을 골라 내 파일에 선언한다」)은 **아직 시작 안 한 세션**에게만 따를 수 있는 말이다. 나는 이미 끝냈고 커밋(`824f5343`)까지 했으므로 비켜날 것이 없다. 남은 유일한 행위가 「내 줄에 끝났다고 적기」인데 그게 정확히 막힌 것이다. ⚠ CLAUDE.md 「가드·회귀의 네 맹점 ③ 자기 처방을 막지 않는지」의 실사고다 — 차단 사유가 시키는 명령을 되먹여도 통과할 수 없다(F103·F447 과 같은 계열, 다른 자리).  ■ 그래서 무엇이 남나 — 아무도 못 치우는 줄이 하나 더 는다 내 줄은 `🔵착수` 인 채로 남는다. 이건 세션 시작 훅이 매번 경고하는 그 모양이고(F270 「주인이 죽은 채 착수로 남은 보드 줄 — 이 줄을 『아직 안 된 일』의 근거로 쓰지 않는다」), 내가 죽으면 남은 세션들은 그 줄을 보고 **이미 끝난 #Q82 를 다시 집을 재료로 읽는다**. 남의 줄이라 F073 이 편집을 막고 board-move 원칙⑦이 거절하므로 아무도 못 치운다 — 보드 상한(전체 18)이 만석인 채 영구가 되는 그 경로에 한 줄을 보태는 셈이다(같은 세션의 board-guard 알림이 이미 「진짜 분모는 주인이 죽었는데 끝났다고 안 말하는 줄 2개」라고 적고 있었다).  ■ 왜 이 갈래가 비어 있나 (추정 아님 — 코드가 아니라 «축»의 문제다) ⑧ 은 「나중에 온 쪽이 비켜난다」를 구현한 것이고(F340·F341 의 채택 처방) 그 방향은 옳다. 비어 있는 것은 **선언과 종결을 같은 문으로 받는다**는 점이다: 트랙 칸이 같으면 그 쓰기가 «새로 집는 것»인지 «이미 한 것을 닫는 것»인지 가르지 않는다. 상태 칸이 `✅`·`⏸` 로 «열리는» 쓰기는 새 착수가 아니다. ⚠ 왜 ①이 통과하고 ③이 막혔는지(=②가 어떻게 내 줄을 안 보고 섰는지)는 **못 가렸다.** 후보 둘: ㉠`7aa81186` 의 박동이 내 ① 시점엔 식어 있어 죽은 줄로 읽혔다(그 세션은 `isRunning:false` 인데 파일은 계속 만졌다 — F315 가 적은 그 상태) ㉡board-guard 등록층이 `Edit|Write|MultiEdit` 뿐이라 Bash 로 쓴 보드 줄은 겹침 검사를 원리상 안 지난다(설정 실측 — Bash 매처에 걸린 것은 clasp-guard 하나다). **어느 쪽인지 안 재고는 못 쓴다** — ㉡ 이면 그건 맹점 ③(훅보다 좁은 필터)의 별건이라 따로 신고할 자리다.  ■ 처방 후보(내가 고르지 않는다 — 훅 소유 트랙 밖이고, 잘못 넓히면 F340 이 막던 중복 구현이 되살아난다) ㄱ) **상태 칸이 종결형(`✅`·`⏸인계`)으로 열리는 쓰기는 겹침 검사에서 뺀다** — 새 착수가 아니기 때문이다. 가장 좁고, 되살아나는 위험이 없다(종결 표기로는 남의 일을 못 가로챈다). ㄴ) 차단문에 **탈출구를 적는다**: 「이미 끝냈으면 `node tools/board-move.js "트랙 문구"` 로 곧장 아카이브로 보내라」. ⚠ 지금 그 처방이 실제로 도는지는 안 쟀다 — 원칙⑦이 남의 활성 줄을 막는데 내 줄이 겹친 상태에서 어떻게 판정하는지 모른다. 적기 전에 재야 한다. ㄷ) 겹침을 만든 **뒤에 온 쪽**에게 「먼저 온 쪽이 이미 끝냈다」를 알린다(세션 간 메시지) — 이번엔 내가 손으로 보냈다(`local_7aa81186` 앞으로). 장치가 아니라 손이라 다음에도 잊힌다.  ■ 이번 세션의 자기 처방(장치 없이 손으로) `7aa81186` 에게 직접 메시지를 보내 비켜나 달라고 했고, 그 줄이 정리되면 내 줄도 갱신할 수 있다고 적었다. 보고에는 「보드 ✅종결」이 아니라 **「커밋 `824f5343` 로 끝났고 보드 줄은 가드에 막혀 🔵착수 그대로다」**로 적는다. | |
