| F336 | 2026-08-11 | 실수 | F335 신고문 정정 — 「커밋 대상 검수는 지문을 기록하지 않는다」는 **너무 세게 적었다**. 유효지문()을 읽으면 조건부다: 그 커밋과 HEAD 사이에 배포집합 변경이 없고 작업본에도 미커밋 배포 파일이 없으면 **지문을 기록한다**(실측 08-11: 내 수리를 18018fc0 으로 커밋한 뒤 --commit 18018fc0 이 정상적으로 게이트 대상이 됐다). F335 가 실제로 가리키는 결함은 더 좁다 — **작업본에 미커밋 배포 변경이 있는 상태**에서 --확인 이 none 을 내며 --commit <과녁> 을 처방하는데, 바로 그 상태에서는 그 처방이 지문을 못 남긴다(대상과 작업본이 어긋나므로 설계상 안 적는다 — 그건 옳은 동작이다). 즉 틀린 것은 도구의 판정이 아니라 **안내문이 전제를 안 말하는 것**이다. 교훈: 코드 한 조각(테스트 제목)만 읽고 일반 명제를 세웠다 — 그 함수 본문을 먼저 읽었으면 조건이 보였다. | 정정만 — F335 본체(안내문이 전제를 안 밝힌다)는 열린 채로 둔다 |
