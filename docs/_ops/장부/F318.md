| F318 | 2026-08-10 | 마찰 | notebooklm-drive.js 의 `--dry` 가 드라이브 마운트가 없어도 「대상: G:\내 드라이브\SYNK_노트북LM」을 찍고 정상 종료한다 — 그 경로는 찾은 자리가 아니라 DEFAULT_OUT 의 **안내용 기본값**이고(마운트 미검출 시의 폴백 문자열), 드라이런은 상위 폴더 검사에 닿기 전에 return 하기 때문이다. 08-10 실측: rot-check 적색 「노트북LM 자동 갱신 실패(Drive mount missing)」의 원인이 아직 살아있는지 재려고 `--dry` 를 돌렸고, 그 출력을 「마운트가 살아있다 = 이유가 사라졌다」는 증거로 읽어 실사 실행(.cmd)에 들어갔다가 그제야 막혔다(로그에 FAILED 한 줄 추가). 드라이런이 실사와 다른 판정을 내면 그건 예행연습이 아니라 오도 장치다 — 「드라이런 신뢰」는 이미 기각분인데(memory 08-05) 도구 쪽에 그 함정이 남아 있었다. | |
