| F318 | 2026-08-10 | 마찰 | notebooklm-drive.js 의 `--dry` 가 드라이브 마운트가 없어도 「대상: G:\내 드라이브\SYNK_노트북LM」을 찍고 정상 종료한다 — 그 경로는 찾은 자리가 아니라 DEFAULT_OUT 의 **안내용 기본값**이고(마운트 미검출 시의 폴백 문자열), 드라이런은 상위 폴더 검사에 닿기 전에 return 하기 때문이다. 08-10 실측: rot-check 적색 「노트북LM 자동 갱신 실패(Drive mount missing)」의 원인이 아직 살아있는지 재려고 `--dry` 를 돌렸고, 그 출력을 「마운트가 살아있다 = 이유가 사라졌다」는 증거로 읽어 실사 실행(.cmd)에 들어갔다가 그제야 막혔다(로그에 FAILED 한 줄 추가). 드라이런이 실사와 다른 판정을 내면 그건 예행연습이 아니라 오도 장치다 — 「드라이런 신뢰」는 이미 기각분인데(memory 08-05) 도구 쪽에 그 함정이 남아 있었다. | 두 자리를 갈랐다. ① tools/notebooklm-drive.js — `--dry` 가 DRIVE_ROOT 미검출이면 「⚠ 드라이브 마운트 미검출 — 위 경로는 안내용 기본값이다(찾은 자리가 아니다)」를 대상 줄 바로 밑에 찍는다(`--out` 을 준 경우는 제외 — 그때는 기본값이 아니다). ② tools/rot-check.js — 적색 처방이 로그만 보고 「도구를 돌려라」로 고정돼 있던 것을 **지금 마운트가 있나**로 갈랐다: 없으면 「도구를 돌려도 같은 자리에서 멈춘다 · 유호님만 풀 수 있다(드라이브 데스크톱을 켜면 예약 작업이 매일 도니 손은 안 간다)」, 있으면 종래 처방. 판정은 생성기의 findDriveRoot() 를 export 해 **호출 시점에** 부른다 — 드라이브 문자 목록을 rot-check 에 베끼면 한쪽만 고쳐진다. ⚠ 회귀는 안 붙였다: 두 자리 다 출력 문구뿐이라 검사가 문구에 묶이는데 「문구 스캐너」는 기각 계열이다(F243). 대신 마운트 판정이 한 곳(findDriveRoot)에 살아서, 갈라짐 자체가 구조로 막힌다. 실측: 마운트 꺼진 상태에서 --dry 가 경고를 내고 rot-check 가 🔴 갈래 문구를 낸다. |
