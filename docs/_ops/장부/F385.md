| F385 | 2026-08-12 | 마찰 | CLAUDE.md 의 「커밋은 `git commit -m "..." -- 경로들` 로 범위를 못 박는다」가 **새 파일에는 그대로 안 먹는다** — 미추적 경로를 주면 git 이 `pathspec ... did not match any file(s) known to git` 로 거절한다(실측 08-12 · 보드 파일 신설 때). 지침 문장만 따르면 새 파일은 영원히 커밋이 안 되고, 그 자리에서 `add -A` 로 우회하고 싶어진다 — 그건 `git-scope-guard` 가 막는 바로 그 명령이다(F103: 따를 수 없는 처방은 우회를 정상 통로로 만든다). 실제 통로는 「좁은 `git add <그 경로>` → `git commit -- <같은 경로>`」이고, 범위는 `--` 가 계속 못 박으므로 그 틈에 남의 스테이징이 얹혀도 안 실린다. 🔑 이미 **도구는 그렇게 하고 있다** — `tools/friction.js:320` 이 미추적이면 `git add -- 경로` 를 먼저 부르고 커밋한다. 즉 구멍은 코드가 아니라 지침 문장 하나다. 처방 = /evolve 로 그 조항에 「새 파일은 좁은 add 를 먼저」 반절 추가(집행은 유호님 호출 몫). | 지침 v9.2 — 커밋 조항에 「새 파일은 add 먼저」 반절 착지(CLAUDE.md 커밋 절). 신고문이 남긴 문구 재료(git-scope-guard 안내문·friction.js)를 그대로 옮기지 않고 격리 저장소 프로브로 다시 쟀고, 실측이 신고문보다 나쁜 모습을 냈다: 미추적이 하나라도 섞이면 범위 커밋은 그 파일만 빠지는 게 아니라 pathspec 에러로 커밋 전체가 no-op 이라 같이 지정한 추적 파일까지 안 들어간다. 즉 정본 73줄의 처방(범위를 못 박는 commit)을 글자대로 따르면 새 파일이 있는 트랙은 매번 조용히 실패한다 — 따를 수 없는 처방이 아니라 따라도 새는 처방이라 F103 의 사촌 자리다. commit-noop-guard 가 잡기는 하나 사후 알림이고, 이 개정은 사전 처방 쪽을 고친 것이다. |
