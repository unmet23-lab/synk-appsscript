| F385 | 2026-08-12 | 마찰 | CLAUDE.md 의 「커밋은 `git commit -m "..." -- 경로들` 로 범위를 못 박는다」가 **새 파일에는 그대로 안 먹는다** — 미추적 경로를 주면 git 이 `pathspec ... did not match any file(s) known to git` 로 거절한다(실측 08-12 · 보드 파일 신설 때). 지침 문장만 따르면 새 파일은 영원히 커밋이 안 되고, 그 자리에서 `add -A` 로 우회하고 싶어진다 — 그건 `git-scope-guard` 가 막는 바로 그 명령이다(F103: 따를 수 없는 처방은 우회를 정상 통로로 만든다). 실제 통로는 「좁은 `git add <그 경로>` → `git commit -- <같은 경로>`」이고, 범위는 `--` 가 계속 못 박으므로 그 틈에 남의 스테이징이 얹혀도 안 실린다. 🔑 이미 **도구는 그렇게 하고 있다** — `tools/friction.js:320` 이 미추적이면 `git add -- 경로` 를 먼저 부르고 커밋한다. 즉 구멍은 코드가 아니라 지침 문장 하나다. 처방 = /evolve 로 그 조항에 「새 파일은 좁은 add 를 먼저」 반절 추가(집행은 유호님 호출 몫). | ⏸ 처방 확정·집행만 남음 — 커밋 조항에 「새 파일은 좁은 add 먼저」 반절 추가는 /evolve 몫(발동=유호 호출 기본). 문구 재료는 기계층에 이미 있다 — git-scope-guard.js:97 안내문(한 호출 안에서 add 와 commit 을 잇는 형태)·tools/friction.js:356(미추적이면 좁은 add 먼저). /evolve 재료로 대기 |
