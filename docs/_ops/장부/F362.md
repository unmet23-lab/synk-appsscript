| F362 | 2026-08-12 | 실수 | 메모리 백업(`SYNK_MemoryPush`)이 08-08~08-12 나흘 동안 12번 실패했는데 **아무도 몰랐다** — 그동안 메모리 정본은 클라우드로 안 올라갔다. 두 겹으로 조용했다: ①실패 핸들러가 자기 문장("possible conflict")만 적고 git 의 실제 오류를 버려서, 로그가 나흘 내내 **추측**만 쌓았고 그 추측이 틀린 방향이라 아무도 못 고쳤다. ②진짜 원인은 충돌이 아니라 **더러운 작업본**이었다 — 세션 ~19개가 메모리 폴더를 계속 써서 `add`→`commit` 과 `pull` 사이에 새 편집이 내려앉고, 그 상태에서 rebase 는 시작 자체를 거부한다. 실측 08-12: 인증·네트워크 정상 · 교착 없음 · 로컬이 정확히 1커밋 앞 — 충돌할 것 자체가 없었다. 발견 경로가 더 나쁘다: 이 트랙을 아무 장치도 안 알렸고, 유호님이 **다른 창**을 묻다가 곁가지로 나왔다. | tools/memory-push.cmd 에 --autostash + 실패 시 git stderr 를 %LOG% 에 붙임 · 회귀 tests/백업통로.test.js ③절 4건 · 격리 픽스처 대조 실측(옛 명령 128 'cannot pull with rebase: You have unstaged changes' vs 새 명령 0 + 클라우드 커밋 반입 + 남의 편집 보존) — 688a7a3d |
