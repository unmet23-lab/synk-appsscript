| F513 | 2026-08-16 | 마찰 | 대장의 빨간 칸 «AI 게이트웨이»가 「지금 채울 수 있는 일감」의 얼굴을 하고 있었다 — 착수 조건이 repo 밖(학생 도래)인데 장부 사유가 그것을 안 적었다.  실측 2026-08-16 (`local_24ba4d6b`): 세션 시작 훅이 매 세션 「이해 12칸 중 3칸이 비었다 · 엔진에 안 닿는 층 ㉡사람·㉢삶」을 낸다. 그 기계적 실체는 형제 저장소 `SYNK-talk/lib/이벤트검증.js` 의 셋째 래칫 `행동안바뀜상한 = 2` 이고, 빈 부품 둘의 사유는 이렇게 적혀 있었다: · '학습자 모델'   — "상태 기반 과제 선택 설계가 착지하면 이 칸이 파일이 된다" → repo 안 일감(#Q49)이라 지금 집을 수 있다 · 'AI 게이트웨이' — "결정이 그 계수를 읽는 날 파일이 된다"                  → **누가 언제 그 날을 정하는지가 없다**  두 칸이 대장에서 같은 빨강이고 사유 문구도 같은 모양이라, 「대기열·보드가 전부 막혔으니 대장의 빨간 칸을 직접 줄이자」로 착수했다. 되먹임을 짜기 직전에 `functions/deliver:563` 이 **이웃 칸에서 같은 이유로 이미 거절해 둔 것**을 발견하고 되돌아왔다("상태가 오늘의 과제를 고르지는 않는다 … 학생 0명이라 그 규칙이 맞는지 잴 방법도 없다" · 제품방향 불변식 5 의 「죽는 쪽」).  되돌아온 근거는 추정이 아니라 실측이다(`tools/원격SQL.js` 읽기): · 운영   — 학생 0 · 개입 0 · 관측 0 · 전체 사건 0행 · 리허설 — 개입 1225 대 관측(`content.viewed`) 1 · 학생이 낸 행 782 · 제출 671 · 「1225 대 1」은 결함이 아니다 — 왕복시험은 서버 API 를 직접 치고, `content.viewed` 는 `src/말하기화면.js` 두 자리에서 사람이 화면을 눌러야 난다(`tools/배달왕복시험.js` 는 그 사건을 만들지 않고 앱이 `parent_event_id` 재료를 싣는지만 잰다). 개원 전 정상 상태다.  즉 장부는 「이 칸이 비었다」를 **맞게** 말하면서 「지금 채울 수 있다」로 **읽히게** 했다 — CLAUDE.md 신뢰성 맹점 ④(맞는 얼굴로 틀린 값을 내는 자리)의 한 형태다. 새는 방향은 여기서도 「통과」였다: 아무 검사도 안 울렸고, 착수해서 표본 0 짜리 되먹임 규칙을 박았어도 회귀 50/50 은 그대로 초록이었을 것이다(래칫은 파일이 «생기면» 오히려 내려가므로 축하했을 것이다).  ⚠ 곁가지 실측 하나 — `deliver:428` 의 감시기는 `사슬.닿음 === 0` 일 때만 운다. 지금 리허설은 1225 대 1 이라 **침묵한다**. 분모 없이 0 만 보는 자리라 [report-zero-with-denominator] 축에 걸리지만, 개원 전에는 연결률이 언제나 0 에 가까워 고치면 매 배치가 빨개진다(따를 수 없는 처방 · F103). **지금 안 고친다** — 학생이 온 뒤에 분모 축으로 다시 본다. | `SYNK-talk/lib/이벤트검증.js` 의 'AI 게이트웨이' 사유와 그 위 주석에 **차단자의 성질**을 실측과 함께 못박았다 — ①분모 두 판(운영 0/0/0 · 리허설 1225 대 1) ②「1225 대 1」이 결함이 아니라 개원 전 정상인 이유(왕복시험은 그 사건을 안 만든다) ③`deliver` 가 이웃 칸에서 같은 이유로 이미 거절했다는 대조 ④**착수 조건은 repo 안에 없다 — 학생이 와야 열린다** ⑤짝인 '학습자 모델' 과 차단자의 성질이 다르다(저쪽은 설계 착지=repo 안 일감). 회귀 `tests/이벤트검증.test.js` 50/50.  🔑 **대가 — 새 장치 0.** 검사를 안 늘렸다. 「사유에 차단자를 적었는가」를 기계로 재려면 사유 문자열의 «뜻»을 봐야 하는데 그건 지금 층에서 못 잰다(현행 강제는 30자 하한이 전부고, 그 하한은 이 결함을 통과시켰다 — 옛 사유도 30자를 넘었다). 억지로 닫을 기존 검사도 없어 **닫은 장치 0**. ⚠ 틀릴 때의 모습 = 개원해서 재료가 생기는 날 이 문단이 낡아 「아직 못 한다」로 남는 것이다. 다만 그때는 대장의 빨강이 실제 일감으로 바뀌므로 이 줄을 지우는 손이 그 자리에 저절로 온다 — 낡아도 조용히 새지는 않는다. |
