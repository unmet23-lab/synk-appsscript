| F613 | 2026-08-18 | 마찰 | 걷기 루프가 **못 지운 파일을 「🗑 걷었다」로 인쇄**하고 부분 삭제를 종료코드 0 으로 낸다 — F604 계보 일곱째이고, 새는 «방향»이 처음으로 반대다.  실측 2026-08-18 (`local_21d5b829` · F604 가 「안 했다」고 남긴 전수 감사에서 나왔다 · PR #108 `85b36790`).  ■ 실물 — `tools/운영자료.js` 옛판정리 옛 모양: `console.log('🗑 …')` 를 찍고 **그 다음 줄에서** `fs.rmSync(…, {force:true})`. 로그가 행위보다 먼저다. rmSync 가 던지면(Windows 에서 파일이 열려 있으면 EBUSY·EPERM 이 실제로 난다) 못 지운 그 파일까지 「🗑 걷었다」로 인쇄된 채 루프가 통째로 빠져나간다. 이 함수는 try 밖이라(호출부 :946 의 try 는 갈래 계산만 감싼다) 프로세스가 그대로 죽고 앞의 N 건은 **이미 지워진 뒤**다. 그리고 무조건 `return 0` 이라 **부분 삭제가 완주와 같은 종료코드로 나갔다**. :1054 는 넣기 루프 «안»에서 부르므로 이중 부분 적용(복사 일부 + 삭제 일부)이 된다.  ■ 왜 F604 보다 나쁜가 — 삭제 대상이 저장소 **밖** 유호님 바탕화면이라 되돌릴 이력이 없다(F025). F604 는 인덱스라 되돌릴 수 있었다.  ■ 🔑 이 신고의 값은 «축»에 있다 — F604 의 병은 낱말이 아니라 구조였다  F604 를 「부정 표기」로 읽으면 이 자리는 **원리상 안 걸린다**: 저기는 했는데 「안 했다」, 여기는 안 했는데 「했다」 — 방향이 반대다. 실제로 문장 축 감사(150벌 · 부정 낱말 출력 275줄 → 실패+부작용부정 동거 9건)는 **손 판정 9/9 전건 참**이었다. 살아 있는 결함 0. 그 0 이 축을 바꾸라고 말한 것이다. 축을 «구조»(루프 안 비원자적 부작용)로 옮기니 **49자리**(파일 39·git 10 · 국소 try 없는 것 32)가 나왔고 그 안에 이 결함이 있었다. F604 원문이 이미 그 축을 말하고 있었다 — 「경로별 add 루프가 앞 여섯을 앉힌 뒤 일곱 번째에서 잠금에 걸려 빠져나갔다」. 문장은 그 구조의 «증상»일 뿐이라, 증상을 세면 이미 고쳐진 것만 안 나오고 아직 안 난 것은 영영 안 나온다.  ■ 계보 일곱 — F400(배포대조) → F435·F436(원격SQL) → F493·F562(고아 잠금) → F543(분모 없는 ✅ 두 도구) → F604(인계문수거) → 이번. 전부 «쓰는 쪽이 관측 없이 문장을 확정»했다.  ■ 대가 (틀릴 때의 모습) — `rmSync({force:true})` 는 없는 파일에도 안 던지므로 `지움` 은 「지웠다」가 아니라 **「지운 뒤 없다」의 수**다. 목록은 바로 위 `목록읽기` 가 방금 실측한 것이라 지금은 둘이 같지만, 남이 동시에 지우면 그 한 건이 «내가 지운 것»으로 셈해진다.  ■ 🔴 안 한 것 (F348) — 구조 축 32자리 중 **손 판정은 3자리뿐**이고 나머지 29 는 안 읽었다. 대부분 산출물 굽기(재실행하면 되니 무해)로 «보이지만» 그건 판정이 아니라 인상이다. 린트 장치도 안 지었다 — 29 가 미판정인 채로 켜면 따를 수 없는 경보가 된다(F103·F526 ㉠ 이 같은 순서를 못박았다). 전문·재현 축·다음 사람이 집을 것 = `docs/_ops/부정단정_감사표.md`. | tools/운영자료.js 옛판정리 — ①행위 뒤에 찍는다(못 지운 파일은 「🗑」로 안 나온다) ②성공·실패를 갈라 «센다» ③한 건이 막혀도 나머지를 계속 시도한다(걷기는 멱등이라 접는 것보다 낫다) ④부분 적용 보고에 분모를 함께 낸다(「대상 N건 중 M건 지웠고 K건 남았다」) ⑤부분 적용이면 종료코드 1 — 옛 무조건 `return 0` 을 닫았다. 회귀 tests/운영자료.test.js 6건 신설(76건 fail 0 · skip 1=win32 COM) · **변이 7/7 구멍 0 · 미적용 0**(①은 F083 대로 원본 버그를 그대로 재주입해 포획) · CI 모사 4476 fail 0(⚠원격 run 0건 = 미검증 · Actions 소진) · `옛판정리` 를 exports 에 추가(회귀가 부를 수 있어야 한다) — PR #108 `85b36790`. ⚠ 이것이 닫은 것은 **이 한 자리**다: 감사 자체는 구조 축 32 중 29 가 미판정이라 F604 의 「전수」는 아직 안 끝났다(전문 = docs/_ops/부정단정_감사표.md §4·§5). |
