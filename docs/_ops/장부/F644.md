| F644 | 2026-08-18 | 마찰 | 인계문 생성기가 「못 쟀다」를 「없다」로 번역한다 — 이 환경(클라우드)에서 `node tools/인계문.js` 를 실제로 돌려 보니, 커밋 4건을 낸 세션의 인계문에 「(커밋 없음 — 보드 줄과 메모리에서 직전 작업을 찾을 것)」 과 「▶ **트랙 없이 끝난 세션이다** — 추정이 아니라 실측이다」 가 나란히 찍혔다. 원인은 `myCommits` 가 커밋 주인을 `Session-Id` 트레일러로만 읽는데(F041 통로), 이 환경엔 `prepare-commit-msg` 훅이 안 걸려 트레일러가 애초에 안 박히는 것이다 — SessionStart 가 이미 그 눈멂을 「눈먼 층 5/6」으로 알리고 있는데, 인계문은 같은 사실을 **단정문**으로 바꿔 다음 세션의 첫 입력에 싣는다. 그래서 「보드를 다시 열 필요 없다」까지 함께 나가, 다음 세션이 앞 세션의 작업을 못 찾고 새 트랙을 잡게 만든다(F348 — 부정 판정은 층을 열거하고 전부 연 뒤에만 쓴다 · 인계는 지금 클라우드 계정의 유일한 통로라 사정거리가 넓다). ⏸ 안 고쳤다 — 처방 후보는 「트레일러를 못 읽는 환경이면 커밋 절을 «못 쟀다»로 내리고 보드·git log 를 열라고 시킨다」인데, 그 판정을 어느 층에서 낼지(훅 등재 여부 실측 vs 트레일러 0건)를 안 재고 장치를 지으면 또 하나의 맞는 얼굴로 틀린 값이 된다. | F641 과 같은 뿌리 — tools/인계문.js 가 유호 확정 08-20 「위임 1단계」로 삭제됐다(8780e5f3). 「커밋 없음」을 단정문으로 내던 생성기가 없어졌으므로 처방(트레일러를 못 읽는 환경이면 «못 쟀다»로 내린다)은 지을 대상이 없다. ⚠ 인계는 이제 하네스 몫이라 같은 병이 그쪽에 있는지는 이 세션이 안 쟀다. |
