| F632 | 2026-08-18 | 마찰 | **「내가 누구인가」를 읽는 통로가 호출부마다 따로고, 도구 층엔 폴백이 0이라 지문 없는 기계에서 규약이 통째로 우회된다.**  ■ 실측 2026-08-18 (이 세션 · 클라우드 · `CLAUDE_CODE_HOST_SESSION_ID` 가 **빈 문자열**) · 분모: `CLAUDE_CODE_HOST_SESSION_ID` 를 읽는 자리 **205곳/55파일**(tools 12 · hooks 14 · tests 29). 정규화가 **세 갈래**다 — `store.safeId(...)` · `보드id.지문(...)` · 손 `replace(/^local_/,'').slice(0,8)`. 폴백은 **네 갈래**다 — `input.session_id`(훅 층) · `'unknown'`/`'nosid'`/날짜 · `''`(도구 층 전부). 즉 훅은 훅 입력에서 세션 id 를 주워 계속 도는데 **도구는 주울 자리가 없어 눈이 먼다.**  ■ 그래서 무엇이 났나 (내가 실제로 걸린 것부터) · ①**`tools/대기열.js --집기` 가 따를 수 없는 처방으로 끝난다** — `세션 지문을 못 읽었다($CLAUDE_CODE_HOST_SESSION_ID) — 내 보드 파일을 못 고른다.` 한 줄이 전부고 **다음 명령이 없다.** 정본은 「고르면 재기 전에 선언부터: `--집기` 가 보드 줄을 조립해 준다(그대로 Write — 써야 board-guard 가 겹침을 본다 · F415)」인데, 그 유일한 통로가 닫히면 규약을 따르는 길은 **보드 파일을 손으로 쓰는 것**뿐이다. 나는 이번 세션에서 실제로 그렇게 했다 — 우회가 정상 통로가 된 그 무늬다(F103). · ②`tools/board.js` 의 «되찾기»(마감 뒤 아카이브로 거둬진 내 줄 알림)가 `if (내지문)` 아래라 **조용히 꺼진다.** 「내 줄 없음」과 「내가 누군지 못 읽음」이 **같은 침묵**이다(F207). · ③`대기열.js:1098-1099` 는 선언 줄에 접두를 **`local_` 로 박는다.** 클라우드 세션이 그 통로로 선언하면 공유 선언판이 provenance 를 잘못 말한다(읽는 쪽은 접두를 전부 떼므로 파손은 안 나고 — 그래서 **증상이 없다**). · ④`friction.js --이세션`(=`/close` 5-c)·`작업본소유자` 의 「나」·`board-guard` 의 주인표식 요구가 전부 같은 이유로 꺼진다. ④의 board-guard 는 **의도된 면제**다(F103 배려가 이미 적혀 있다) — 문제는 그 배려가 **훅에만** 있고 도구엔 없다는 것.  ■ 왜 신호인가 — 새는 방향이 「통과」이자 「침묵」이다 이 저장소가 F044·F053·F063 에서 배운 것은 **가드는 로직보다 등록층에서 샌다**였고, 그 처방이 「클라우드에선 변수가 비어 **폴백이 실제로 일한다**」였다. 그 처방은 훅 등록층에만 적용됐고 **도구 층은 안 지났다.** 그리고 이건 「폰은 안 쓴다」(F623 유호 교정)와 다른 축이다 — 폰이 아니라 **클라우드 세션**이고, 이 저장소는 지금 클라우드 세션으로 굴러간다(원격 가지 `claude/*` 가 그 증거다).  ■ 안 잰 것 (F348) · 이 세션의 빈 변수가 **모든** 클라우드 세션의 상태인지는 **안 쟀다.** 반례가 저장소 안에 있다 — 보드 줄에 `cloud_d427746a`·`cloud_ddd2c370` 처럼 접두가 붙은 지문이 실제로 산다. 즉 **어떤 클라우드 세션에는 변수가 있었다.** 이 신고가 세는 것은 「빈 기계가 존재한다」이지 「클라우드는 늘 비어 있다」가 아니다. · 205곳 중 실제로 해악이 나는 자리가 몇인지 전수로 안 쟀다 — 위 ①~④는 **내가 이번 세션에 직접 부딪히거나 코드를 열어 확인한 것**이고, 나머지는 안 열었다. · 훅이 받는 `input.session_id` 가 이 기계에서 실제로 값이 있는지 **안 쟀다**(훅을 직접 못 띄웠다).  ■ 처방 후보 — 안 골랐다(재료다) ㉠ **공용 통로 하나**(`tools/lib/세션지문.js`)로 「내 지문」을 뽑고, 출처(호스트변수·명시지정·훅입력·없음)를 **함께 낸다** — 「못 읽음」이 「없음」과 다른 값이 된다. 그 위에 명시 지정(`SYNK_세션지문=<8자리>` 또는 `--지문`)을 두어 **빈 기계에도 따를 수 있는 길**을 낸다. ⚠대가: 지정은 사람이 틀리게 줄 수 있다(남의 지문을 적으면 남의 파일에 쓴다) — 그래서 지정값도 board-guard 의 「내 파일에만」을 그대로 지나야 한다. ㉡ 205곳을 전부 공용 통로로 옮긴다. ⚠대가: 남의 활성 트랙과 정면 충돌한다(지금 `대기열.js`·`board-guard.js`·`보드.js` 는 남이 만지는 자리다) — 한 판에 할 일이 아니다. ㉢ 아무것도 안 한다. ⚠대가: 지문 없는 기계는 매번 규약을 손으로 우회하고, 그 우회는 **기록에 안 남는다**(내가 이번에 쓴 보드 파일이 그렇다 — 도구가 조립한 줄과 구별이 안 된다). | |
