| F632 | 2026-08-18 | 마찰 | 훅 등록 회귀의 파서가 «소문자 영문 파일명만» 읽어, 한글 훅을 등록해도 「등록이 없다」는 거짓 실패를 낸다 — 반대 방향(미등록 한글 훅이 조용히 통과)이 더 위험하다. 정규식 /hooks 슬래시 a-z 하이픈 플러스 .js/ 가 원인. 이 저장소는 tools 를 이미 한글로 짓는데(작업가지·대기열·보드수거) 훅만 영문을 강제할 근거가 없었다 — 가드 맹점 ① 「사람이 실제로 쓰는 표기로 검사한다」 실물. 실측 2026-08-18 remote-capability 훅 신설 중 발견, 같은 커밋에서 파서를 넓히고 변이 시험으로 탐지력 확인. | |
