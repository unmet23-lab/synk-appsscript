| F614 | 2026-08-18 | 마찰 | 「열린 장부 번호」를 **사람이 읽을 출력에서 긁어** 온다 — 실측 36개 중 24개가 헛것이다 (`tools/lib/보드근거.js:89` `열림장부()`)  ■ 실측 2026-08-18 (`local_5653b6a5` · F610 트랙에서 «내가 출력 형식을 바꿔도 되나» 를 확인하다 나왔다)   · `열림장부(root)` 는 `node tools/friction.js --open` 을 **자식 프로세스로 돌린 뒤** 그 stdout 에서 `\bF\d{2,4}\b` 를 전부 긁는다.   · 그런데 그 출력은 **신고문 전문**을 싣는다. 그리고 이 저장소의 신고문은 서로를 상시 인용한다(F526 이 F518 을 들고, F604 가 F400·F435·F436·F493·F562·F543 을 든다).   · 실측: 긁힘 **36** · 진짜 열림 **12** · **헛것 24**     `F181 F518 F207 F529 F348 F531 F226 F582 F589 F493 F562 F400 F435 F436 F071 F586 F526 F521 F595 F291 F368 F406 F073 F596`     이 24개는 **전부 이미 해소·보류된 번호이거나 안내문의 근거 표기**다. 세 배로 부푼 목록이 「열린 장부」라는 이름을 달고 판정에 들어간다.  ■ 왜 신호인가 — **답이 코드 안에 있는데 텍스트로 돌아갔다**   같은 파일이 이미 `require('tools/friction.js')` 를 할 수 있는 자리에 있고, 그 모듈은 `read()`·`열렸나` 를 **export 하고 있다**   (`tools/friction.js` 머리말: 「상태 판정 — 소비자(rot-check · friction-close-guard)는 자기 정규식을 쓰지 않고 이것을 쓴다」).   즉 규약이 이미 있고 다른 소비자 둘은 그것을 따르는데, 이 한 자리만 **자식 프로세스 + 정규식**으로 우회했다.   그 결과 이 자리는 **출력 문구가 바뀔 때마다 조용히 값이 변한다** — 오늘 내가 안내문에 근거 표기(F521·F610·F406)를 더하자   긁히는 수가 그만큼 늘었다. 판정 입력이 «사람에게 읽어 줄 글»에 매달려 있다.  ■ 손해의 방향 — 「안 치운다」(보수적) 이지만 조용하다   소비자는 「상태 칸의 F번호가 아직 열려 있나」로 그 보드 줄의 처분을 가른다. 목록이 부풀면 **이미 처분된 번호가 「아직 열림」으로** 읽혀   그 줄을 살아 있는 근거로 본다. 막는 쪽이라 사고는 안 나지만, 아무도 안 울고 「치울 게 없다」로 보인다 —   이 저장소가 F372·F395 로 이미 아픈 「죽은 줄이 자리를 영구 예약한다」와 같은 방향이다.  ■ 내가 악화시킨 몫 (숨기지 않는다)   24 중 **3건 정도가 오늘 내가 더한 것**이다(`F521`·`F610`·`F406` — F610 수리가 안내문에 근거를 적었다). 안 뺐다:   근거 표기는 이 저장소의 규약이고 빼면 읽는 사람이 왜 그런지 모른다. **원인은 인용이 아니라 «인용을 긁는 것»이다.**  ■ 안 잰 것 (F348)   · 이 부푼 목록이 실제로 **어떤 보드 줄의 처분을 뒤집었는지**는 안 쟀다 — 방향(안 치움)만 코드에서 읽었고 실물 대조는 안 했다.   · `열림장부()` 의 다른 호출부가 있는지는 이 파일 안에서만 봤다.  ■ 처방 후보 — 안 지었다(이 신고는 재료다)   ㉠ **모듈로 직접 묻는다** — `const f = require('.../friction.js'); f.read().rows.filter(f.열렸나).map((r) => r.id)`.     자식 프로세스 하나가 사라져 더 빠르고, 출력 문구와 판정이 영구히 갈라진다. ⚠대가: `보드근거.js` 는 `board-move` 의 처분 판정을     지므로 목록이 36→12 로 줄면 **지금 「못 치운다」로 막혀 있던 줄들이 치울 수 있게 바뀐다** — 되돌림 비용이 있는 변화라     「상태를 한 번 바꿔보고 되돌리는」 절차가 붙어야 한다(CLAUDE.md). 그래서 F610 트랙에서 안 집었다.   ㉡ 긁는 범위를 **행 머리**로 좁힌다(`^  (F\d+)  `). 싸지만 여전히 출력 문구에 매달린다 — 같은 병이 다음 형식 변경에서 재발한다.   ㉢ 아무것도 안 한다 — 방향이 보수적이라. ⚠대가: 판정 입력이 사람 글에 매달린 상태가 남고, 그 사실이 어디에도 안 적힌다(이 신고가 그 자리다). | ㉠ 채택 — `tools/lib/보드근거.js` 의 `열림장부()` 가 자식 프로세스 + stdout 정규식을 버리고 장부 모듈에 직접 묻는다(`read()` + `열렸나` — friction.js 머리말이 이미 못박은 규약이고 rot-check·friction-close-guard 는 따르고 있었다). 추출(`열림뽑기`)과 적재(`열림장부`)를 갈라 픽스처가 탐지력을 진다. 실측 2026-08-18: 긁힘 49 → 14 · 헛것 35 → 0 · 놓침 0(장부 모듈 판정과 글자 그대로 일치). 회귀 `tests/보드근거.test.js` 5건(인용 부풀기 · 보류≠열림 · 옛 통로 소스 금지 · 못 잼≠0건 · 실저장소 일치) · 변이 5/5 구멍 0. 신고문이 적은 되돌림 비용(「막혀 있던 줄들이 치울 수 있게 바뀐다」)은 **코드로 대조해 과대평가로 판명** — board-move 의 처분 게이트는 `보드lib.활성행()` 이고 ⑤ 장부 갈래는 거절문 안의 안내 문구다(`tools/board-move.js:609,616`). 즉 바뀐 것은 사람이 읽는 근거뿐이고 도구가 허용하는 범위는 그대로다. ⚠ 안 한 것 = 부푼 목록이 과거에 실제로 어떤 줄의 처분을 뒤집었는지는 여전히 안 쟀다(신고문의 미측정 그대로). |
