| F578 | 2026-08-17 | 마찰 | ①배포 검수 게이트가 내미는 과녁이 **머지 커밋일 때 원리상 안 돈다** — 처방을 그대로 따랐는데 종료 2(확인 불가)로 끝났다. 따를 수 없는 처방이라 F103 계열이고, 새는 방향은 「배포가 검수 없이 나간다」다.  실측 2026-08-17 (`local_41cc57e7` · 라이브 배포 밀림 해소 트랙):  · `node tools/codex-review.js --확인` 이 낸 처방 = `node tools/codex-review.js --commit fb84d011   ((루트) · 약 5분)`  · 그대로 던졌더니 1분 만에 **종료코드 2** — 로그: 「대상: commit fb84d011 · 파일 2개 / 검수 범위(clasp 프로젝트): 없음 — 배포 파일이 아닌 변경」  · 원인: `fb84d011` 은 **머지 커밋**이다(`Merge remote-tracking branch 'origin/master' into worktree-q106-grammar-quarantine`).    과녁 계산은 「배포집합을 마지막으로 건드린 커밋」을 `git log -- <배포파일들>` 로 뽑는데, 머지가 그 파일들을 «들여왔으므로» 머지 SHA 가 뽑힌다.    그런데 대상 diff 는 `git show --name-only <sha>`(:398) 라 **첫 부모 기준**이고, 첫 부모가 워크트리 가지 쪽이면 배포 파일은 「안 바뀐 것」으로 나온다 → 배포 파일 0건 → F314 가드가 돌기 전에 멈춘다.  · 즉 게이트가 **자기가 뽑은 과녁으로 자기 게이트를 못 연다.** 그리고 이 저장소는 「워크트리+PR」이 정본 동선이라(CLAUDE.md 세션 규약) 배포집합을 마지막으로 만진 커밋이 머지인 경우가 **드문 일이 아니라 상시 조건**이다.  ■ 왜 신호인가 — 새는 방향이 「통과」다.  검수 기록 없음은 게이트에서 **차단이 아니라 알림**이다(F103 대비 · 자격증명 없는 환경 보호). 그래서 이 처방이 튕겨도 배포는 그대로 나간다 — 세션이 처방을 한 번 따라 보고 종료 2 를 받으면, 「돌렸는데 안 되네」로 접고 배포하는 것이 자연스러운 동선이다. 지침 v9.2 가드 맹점 ③(자기 처방을 막지 않는지 — 차단 사유가 시키는 명령을 그 가드에 되먹여 통과하는지 본다)의 정확한 재발이고, 이번엔 되먹임이 **막히는** 게 아니라 **빈손으로 통과**한다.  ■ 우회(이번에 쓴 것 — 처방이 아니라 재료다)  `--base <라이브 기준선 sha>` 는 `git diff <sha>...HEAD`(:403·:504) 라 범위를 받는다. `배포판점검 --라이브` 가 파일마다 내주는 라이브 기준선 중 가장 최신을 골라 `--base cd331cf0` 로 던지니 정상 기동했다.  덮이는지 실측으로 확인함: `--base cd331cf0` = 276 insertions / 16 deletions 이고, 파일별 라이브 기준선 delta 합(1+123+22+41+89 / 1+5+4+5+1)과 **정확히 일치**한다.  ■ 처방 후보 — 안 지었다(이 신고는 재료다. 과녁 계산은 게이트의 급소라 되돌림 비용이 크다).  ㉠ 과녁이 머지 커밋으로 뽑히면 `--commit <머지>` 대신 `--base <라이브 기준선>` 을 내민다. 재료는 이미 있다 — `배포판점검` 이 파일마다 라이브 기준선 sha 를 들고 있다.    ⚠ 대가 = 기준선이 파일마다 갈릴 때 「가장 최신 하나」로 접으면 더 오래된 기준선 파일의 앞 구간이 빠질 수 있다. 이번엔 안 빠진 것을 실측으로 확인했지만 그것이 언제나 참이라는 증명은 아니다 — 접기 전에 파일별 delta 합과 대조하는 검사가 같이 서야 한다.  ㉡ 과녁을 뽑을 때 머지 커밋을 건너뛰고 `--no-merges` 로 실커밋을 잡는다.    ⚠ 대가 = 배포집합 변경이 여러 실커밋에 흩어져 있으면 마지막 하나만 잡혀 앞 구간이 통째로 빠진다(지금보다 **조용히** 좁아지는 방향이라 더 나쁘다).  ㉢ 과녁이 안 돌 것을 `--확인` 이 **미리** 말한다 — 처방을 내밀기 전에 그 sha 의 `git show --name-only` 에 배포 파일이 있는지 본다(싸다). 못 돌 처방이면 「이 과녁으로는 안 열린다」를 그 자리에서 밝힌다.    ⚠ 이건 고치는 게 아니라 **드러내는** 것이다 — 그래도 「따라 해보고 종료 2」보다 낫다(5분 + 오독을 아낀다).  ⚠ 짝 신호 하나 — 같은 판에서 나도 같은 층의 실수를 했다: 첫 `배포판점검 --라이브` 를 origin 동기화 **전**에 찍고, 그 낡은 목록을 근거로 「`교재연동.js` 는 나갈 바이트 0」이라고 보안 보고서에 **단정**했다. 그 사이 남의 머지로 `[v9.253]` 이 들어와 실제로는 +128 줄이 나갈 참이었다. 배포집합은 세션이 도는 동안 남의 머지로 바뀌므로 「직전에 다시 연다」가 여기에도 그대로 적용된다. 재실측해서 5/5 로 잡고 전량 정독했다. | |
