| F561 | 2026-08-17 | 마찰 | 검수 런의 «종료코드 1 = 차단 지적을 찾았다»가 「실패 = 주울 답이 없다」로 접혀, 진짜 지적 13건이 버려질 뻔했다.  `codex-review.js:2453` 은 `return 차단.length ? 1 : 0` — **1 은 검수 게이트의 정상 성공 경로**다(차단급 지적을 찾았다는 뜻). 그런데 `lib/검수런.js:236` 의 `상태이름(종료코드) = 코드===0 ? '완주' : '실패'` 가 **1비트**라 그 1을 오류와 못 가르고, `판정()` 이 `실패미처분`(계약 = 「주울 결과가 **없다**」)으로 찍는다. 결과로 `review-runs` 훅이 매 세션 시작에 「🔴 실패로 끝난 것 1건 — **주울 답이 없다**(로그를 열어도 답이 아니라 오류다)」를 광고하고, 처방은 「다시 던졌거나 **접었으면** 도장」이다.  실측: 런 `검수-2026-08-17T0949-e06d3a` (--commit 16f8af78)은 64분을 **완주**해 지적 13건(P1 2 · P2 2 · P3 9)을 `docs/_ops/검수기록.jsonl` 에 정상으로 썼다. 로그 끝줄도 `기록: docs/_ops/검수기록.jsonl` 이다. 그런데 장부는 「실패」, 훅은 「주울 답 없음」이었다 — P1 「mastery_log 쓰기 실패 시 포인터 보존」이 그 안에 있다.  F509 픽스의 **거울상**이다. 그때 고친 것은 「실패를 완주로 적었다」(마누스 종료코드 3이 ✅완주로 앉음)였고, 처방이 「0 아니면 전부 실패」로 비관 일반화되면서 **결과가 있는 종료까지 실패로** 접혔다. 종료코드의 뜻은 도구마다 다른데(codex 1=지적있음 / 2=확인불가, 마누스 3=상한초과) 판독은 도구를 안 본다.  CLAUDE.md 신뢰성 맹점 ④ 그 자리 — 장치가 안 도는 게 아니라 **맞는 얼굴로 틀린 값**을 냈다. 그리고 같은 판정 자리의 3번째 결함이라(F508·F509) 목록만 고치지 말고 잘못 쓸 수 없는 통로로 바꾼다. | |
