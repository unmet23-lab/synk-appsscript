| F630 | 2026-08-18 | 마찰 | **`tests/보드주인.test.js` 의 «실저장소 거짓양성» 검사가 자기 런의 «길이» 때문에 빨개진다** — 도는 세션이 살아 있는데도 「내 보드 줄에 주인이 없다」로 잡힌다. F616·F598 과 같은 계보(장치가 자기 실행의 부작용을 재고 있다)인데, 여기선 부작용이 «파일»이 아니라 **«시간»**이다.  ■ 실측 2026-08-18 (`local_ad0673f3` · 이 클라우드 컨테이너 · **트리 동일 · 결과 갈림**) · 4회차 `node tools/test-ci.js` → 4638건 중 **fail 1**     `not ok 2229 - ☠️ 실저장소 거짓양성 — **내 줄**은 주인 없음에 안 잡힌다`     `error: '내 줄이 주인 없음으로 잡혔다: ad0673f3'` (`tests/보드주인.test.js:87`) · 5회차 — **같은 트리에서 아무것도 안 고치고** 다시 돌렸다 → 4638건 **fail 0**. · 그 사이 바꾼 것은 없다. 바뀐 것은 **내 심장박동의 나이**뿐이다. · 그 검사 파일만 단독으로 돌리면 변수 유무와 무관하게 7/7 초록이다(변수 없이도, 손으로 넣고도).   즉 「검사가 틀렸다」가 아니라 **「전 스위트와 함께 돌 때만」** 빨개진다.  ■ 기전 — 소스에서 읽었다(추정 아님) · `tools/lib/보드.js:1010 주인없는줄들()` = 「보드 표의 줄 중, 파일지문이 **살아 있는 세션 집합**에 없는 것」. · 그 집합은 `작업본소유자.선언살았나(s) = 살았나(s) && !마감했나(s)` 로 만든다 — **시간 의존**이다. · 전 스위트는 이 기계에서 **약 3분 반**이 걸리고, 그동안 이 세션은 도구를 한 번도 안 부른다.   박동은 **파일 접촉**으로 찍히므로(F315 가 이미 이름 붙인 자리) 그 사이 식는다.   ⇒ **런 자신이 판정 재료를 낡게 만든 뒤, 같은 런 안에서 그 재료를 읽는다.**  ■ 왜 신호인가 — 새는 방향이 「빨간 것을 안 믿게 만든다」다 · 이 적색은 **진짜 적색과 모양이 같다.** 게다가 처방이 없다 — 화면에는 「내 줄이 주인 없음으로 잡혔다」만 뜨고,   읽는 사람은 자기 보드 파일을 의심하러 간다(내가 그랬다). 실제로 고칠 것은 **0** 이다. · 그리고 **재실행하면 초록**이라, 「아 flaky 네」로 접히기 쉽다. 그건 진단이 아니다(CLAUDE.md: 「flaky」는 진단이 아니다). · 이 검사의 의도는 옳다 — 실저장소 거짓양성을 막는 자리다. 틀린 것은 **재는 시점**이다. · ⚠ 이 자리는 **분모가 크다**: 앞으로 전 스위트를 도는 모든 세션이 자기 보드 줄을 가진 채로 이 함정을 밟는다.   오늘 그 검사가 새로 들어왔으니(파일 222벌 · 며칠 전 219) 아직 아무도 안 밟았을 뿐이다.  ■ 처방 후보 — 안 지었다(재료다) ㉠ 그 검사를 **박동에 안 기대게** 한다 — 「내 파일이 있으면 통과」가 아니라, 「내 지문이 산 목록에 없으면 **skip**」으로 내린다.    ⚠대가: 탐지력이 준다. 다만 탐지력은 이미 같은 파일의 픽스처 4벌이 지고 있고, 그 자리는 이 검사가 스스로 「탐지력은 위 픽스처가 진다」고 적어 뒀다. ㉡ 스위트가 도는 동안 **박동을 한 번 찍는다**(러너가 시작·중간에 상태 파일을 touch).    ⚠대가: 테스트 러너가 런타임 상태를 쓰게 된다 — 「도는 동안 바뀐 파일」 경고(F616)의 새 원천을 만든다. 서로 물린다. ㉢ 박동 나이 문턱(`SYNK_OWNER_STALE_MIN`, 기본 2분)을 스위트 길이 위로 올린다.    ⚠대가: 죽은 세션을 산 것으로 보는 창이 그만큼 길어진다 — 그 판정을 쓰는 다른 자리(이어받기·이관)가 같이 둔해진다. ㉣ 아무것도 안 한다. ⚠대가: 위 「분모가 크다」가 그대로 남는다.  ■ 안 잰 것(F348) · **몇 %의 런에서 뜨는지 안 셌다** — 관측은 2회(4회차 적색 · 5회차 초록)뿐이다. 박동 나이가 문턱을 넘느냐에 달렸으니   「도구를 마지막으로 부른 뒤 몇 분 지나 스위트를 시작했나」가 진짜 변수인데, 그걸 회차별로 안 기록했다. · `살았나()` 의 실제 문턱값을 소스에서 끝까지 안 따라갔다(`선언살았나` 까지만 읽었다). · 다른 세션의 줄에서도 같은 일이 나는지 안 쟀다(내 줄 하나가 분모의 전부다). | ⏸ ⏸보류 — **기전을 못 특정했다.** 관측 가능성만 올리고 열어 둔다(행동 무변경). 착지 `3fd23934`·`6dc12043`.  ■ 🔴 먼저 철회한다 — 이 신고가 「기전 — 소스에서 읽었다(추정 아님)」로 적은 설명은 **서지 않는다** · 내가 근거로 든 것은 `선언살았나 = 살았나 && !마감했나` 가 **시간 의존**이라는 것뿐이었고,   그 위 두 줄을 안 읽었다: `살았나` 에는 **자기면제**(`s.sid === 나지금`)가 있고 문턱은   **30분**(`SYNK_OWNER_ALIVE_MIN` 기본값)이다. 그 런은 **3분 반**짜리였다.   ⇒ 「3분 반 도는 동안 박동이 식었다」는 이 소스에서 **나올 수 없는 이야기**다. · CLAUDE.md 「현재 상태는 현재를 읽어 판정한다」의 자리인데, 나는 **함수 이름 한 줄**을 읽고   기전이라 적었다. 「소스에서 읽었다」는 말이 그 자체로 담보가 아니다 — 어디까지 읽었는지가 담보다.  ■ 재현 시도 — **세 갈래 다 실패**(그래서 「간헐」이 관측의 전부다) · ①이 파일 단독(`CLAUDE_CODE_HOST_SESSION_ID` 있음·없음 둘 다) → 7/7 초록 · ②`ci모사환경` 을 씌운 자식 프로세스에서 상태 폴더·레코드 확인 → 내 레코드 살아 있음(박동 10분) · ③같은 모사 env 로 그 파일만 → 7/7 초록 · 그 뒤 전 스위트 3회(4638·4642·4644) 전부 fail 0. **적색은 1/4 회차에서 한 번만 났다.**  ■ 남은 기전 후보 — 하나로 좁혔다(확인은 안 됐다) `작업본소유자.세션들()` 은 상태 파일마다 `readFileSync`+`JSON.parse` 를 **try/catch 로 감싸고 실패하면 그 레코드를 조용히 건너뛴다**(`continue`). 훅이 그 파일을 비원자적으로 다시 쓰는 순간과 겹치면 내 레코드가 목록에서 사라지고, 그러면 내 보드 줄은 정확히 「주인 없음」이 된다. ⚠ 이건 **후보**다 — 관측으로 확인 안 했다. 확인하려면 그 순간의 목록이 필요하고, 그게 아래 처방이다.  ■ 한 것 — 행동은 안 바꿨다(기전을 모르는 채 판정을 느슨하게 하면 고친 게 아니라 가린 것이다) · 실패문이 **스스로 진단을 들고 온다**: 상태 폴더 이음매(env) · 내 env id · 세션 레코드 **전량**   (sid·박동 분·끝남·선언살았나) · 주인 없다고 잡힌 지문 목록. 그리고 갈래를 가르는 한 줄을 붙였다 —   「내 레코드가 목록에 **없으면** 건너뜀 쪽, 있는데 `선언살았나=false` 면 박동 쪽. 둘은 처방이 다르다.」   ⇒ 다음 적색은 재현 없이 갈래가 갈린다. 나는 그게 없어서 재현부터 세 번 헛돌았다. · 관측은 **적색일 때만** 조립한다(초록 런에 읽기를 안 붙인다). · ⚠ 진단문에서 `stateDir()` 호출은 **뺐다** — 읽기만 해도 `tests/상태격리.test.js` 가 「공유 폴더에   쓰는 회귀」로 잡는다. 그게 맞다(가드는 사람이 쓰는 **표기**로 검사한다). 기본 경로를 베끼는 대신   이음매만 보인다.  ■ 곁가지에서 진짜 구멍을 하나 닫았다 (이 트랙의 실질 산출) · 변이 실측: 「생사를 안 재고 **전부 산 것으로**」 망가뜨렸는데 그 파일 7건이 **전부 초록**이었다.   기존 픽스처의 지문이 상태 폴더에 아예 없는 값이라, 생사 판정을 통째로 지워도 여전히   「주인 없음」으로 잡혔기 때문이다 — 그 검사들은 «필터가 있다»를 **원리상 못 잰다.**   그런데 그 파일 머리 ①이 못박은 대로 **새는 방향은 「주인 있음」**이고(그쪽으로 틀리면 다음 세션이   죽은 주인의 낡은 🎫 를 집는다) 그 방향을 재는 픽스처가 **0**이었다. · 신설 `tests/보드주인생사.test.js` — 산 세션 1·죽은 세션 1(mtime 200분)을 세우고 「죽은 쪽만 잡히는가」를 잰다.   🔑 파일을 **가른** 이유: 격리는 `격리된store` 를 require 앞에 둬야 해서 **파일 전체**에 걸리는데,   `보드주인.test.js` 에는 실저장소를 재는 검사가 함께 있어 그 파일을 격리하면 그쪽이 빈 임시 폴더를   보고 내 줄을 「주인 없음」으로 잡는다 — 한 파일 안에서 두 요구가 서로를 깨뜨린다. · 검증: 회귀 7 → **9건**(신설 파일 2) · **변이 3/3 구멍 0 · 미적용 0** · CI 모사 **4644 fail 0**.  ■ 왜 지금 안 닫나 · 다음 사람이 할 일 기전이 미특정이라 닫으면 「고쳤다」가 거짓이 된다. **다음 적색 1회**가 곧 다음 재료다 — 그 실패문의 「세션 레코드 N건」과 내 지문의 유무만 보면 갈래가 갈리고, 거기서 처방이 정해진다. 그때까지 이 행은 열어 둔다.  ■ 안 잰 것(F348) · 몇 %의 런에서 뜨는지 안 셌다(관측 4회차 중 1회 · 분모가 너무 작다). · 상태 파일이 실제로 비원자적으로 쓰이는지(쓰는 쪽 코드)는 **안 봤다** — 후보를 좁혔을 뿐이다. · 다른 세션의 줄에서도 같은 일이 나는지 안 쟀다(내 줄 하나가 분모의 전부다). |
