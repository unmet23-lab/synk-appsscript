| F630 | 2026-08-18 | 마찰 | **`tests/보드주인.test.js` 의 «실저장소 거짓양성» 검사가 자기 런의 «길이» 때문에 빨개진다** — 도는 세션이 살아 있는데도 「내 보드 줄에 주인이 없다」로 잡힌다. F616·F598 과 같은 계보(장치가 자기 실행의 부작용을 재고 있다)인데, 여기선 부작용이 «파일»이 아니라 **«시간»**이다.  ■ 실측 2026-08-18 (`local_ad0673f3` · 이 클라우드 컨테이너 · **트리 동일 · 결과 갈림**) · 4회차 `node tools/test-ci.js` → 4638건 중 **fail 1**     `not ok 2229 - ☠️ 실저장소 거짓양성 — **내 줄**은 주인 없음에 안 잡힌다`     `error: '내 줄이 주인 없음으로 잡혔다: ad0673f3'` (`tests/보드주인.test.js:87`) · 5회차 — **같은 트리에서 아무것도 안 고치고** 다시 돌렸다 → 4638건 **fail 0**. · 그 사이 바꾼 것은 없다. 바뀐 것은 **내 심장박동의 나이**뿐이다. · 그 검사 파일만 단독으로 돌리면 변수 유무와 무관하게 7/7 초록이다(변수 없이도, 손으로 넣고도).   즉 「검사가 틀렸다」가 아니라 **「전 스위트와 함께 돌 때만」** 빨개진다.  ■ 기전 — 소스에서 읽었다(추정 아님) · `tools/lib/보드.js:1010 주인없는줄들()` = 「보드 표의 줄 중, 파일지문이 **살아 있는 세션 집합**에 없는 것」. · 그 집합은 `작업본소유자.선언살았나(s) = 살았나(s) && !마감했나(s)` 로 만든다 — **시간 의존**이다. · 전 스위트는 이 기계에서 **약 3분 반**이 걸리고, 그동안 이 세션은 도구를 한 번도 안 부른다.   박동은 **파일 접촉**으로 찍히므로(F315 가 이미 이름 붙인 자리) 그 사이 식는다.   ⇒ **런 자신이 판정 재료를 낡게 만든 뒤, 같은 런 안에서 그 재료를 읽는다.**  ■ 왜 신호인가 — 새는 방향이 「빨간 것을 안 믿게 만든다」다 · 이 적색은 **진짜 적색과 모양이 같다.** 게다가 처방이 없다 — 화면에는 「내 줄이 주인 없음으로 잡혔다」만 뜨고,   읽는 사람은 자기 보드 파일을 의심하러 간다(내가 그랬다). 실제로 고칠 것은 **0** 이다. · 그리고 **재실행하면 초록**이라, 「아 flaky 네」로 접히기 쉽다. 그건 진단이 아니다(CLAUDE.md: 「flaky」는 진단이 아니다). · 이 검사의 의도는 옳다 — 실저장소 거짓양성을 막는 자리다. 틀린 것은 **재는 시점**이다. · ⚠ 이 자리는 **분모가 크다**: 앞으로 전 스위트를 도는 모든 세션이 자기 보드 줄을 가진 채로 이 함정을 밟는다.   오늘 그 검사가 새로 들어왔으니(파일 222벌 · 며칠 전 219) 아직 아무도 안 밟았을 뿐이다.  ■ 처방 후보 — 안 지었다(재료다) ㉠ 그 검사를 **박동에 안 기대게** 한다 — 「내 파일이 있으면 통과」가 아니라, 「내 지문이 산 목록에 없으면 **skip**」으로 내린다.    ⚠대가: 탐지력이 준다. 다만 탐지력은 이미 같은 파일의 픽스처 4벌이 지고 있고, 그 자리는 이 검사가 스스로 「탐지력은 위 픽스처가 진다」고 적어 뒀다. ㉡ 스위트가 도는 동안 **박동을 한 번 찍는다**(러너가 시작·중간에 상태 파일을 touch).    ⚠대가: 테스트 러너가 런타임 상태를 쓰게 된다 — 「도는 동안 바뀐 파일」 경고(F616)의 새 원천을 만든다. 서로 물린다. ㉢ 박동 나이 문턱(`SYNK_OWNER_STALE_MIN`, 기본 2분)을 스위트 길이 위로 올린다.    ⚠대가: 죽은 세션을 산 것으로 보는 창이 그만큼 길어진다 — 그 판정을 쓰는 다른 자리(이어받기·이관)가 같이 둔해진다. ㉣ 아무것도 안 한다. ⚠대가: 위 「분모가 크다」가 그대로 남는다.  ■ 안 잰 것(F348) · **몇 %의 런에서 뜨는지 안 셌다** — 관측은 2회(4회차 적색 · 5회차 초록)뿐이다. 박동 나이가 문턱을 넘느냐에 달렸으니   「도구를 마지막으로 부른 뒤 몇 분 지나 스위트를 시작했나」가 진짜 변수인데, 그걸 회차별로 안 기록했다. · `살았나()` 의 실제 문턱값을 소스에서 끝까지 안 따라갔다(`선언살았나` 까지만 읽었다). · 다른 세션의 줄에서도 같은 일이 나는지 안 쟀다(내 줄 하나가 분모의 전부다). | |
