| F413 | 2026-08-14 | 마찰 | 가드끼리 물렸다 — design-guard 의 처방(「킷 정본 DESIGN.md 를 읽어라」)이 read-budget 의 「큰 파일」 상한에 걸린다. 실측 2026-08-14 `local_92d50b27`: HEAD 의 `DESIGN.md` = **16,772B** ≥ read-budget `BIG` = 16,384B → `tests/디자인가드.test.js:173`(「처방이 시킨 DESIGN.md 읽기가 read-budget 에 막히지 않는다」)이 **적색**이다. 이 검사는 지침 v9.2 맹점③(자기 처방을 막지 않는지 — 차단 사유가 시키는 명령을 그 가드에 되먹여 통과하는지 본다)을 그대로 기계화한 자리이고, 지금 그것이 **실제로 발화했다**(설계대로 일한 것이지 검사의 결함이 아니다). 넘긴 것은 `28182531`(앱 모드 = 다크 단일 · DESIGN.md §2-c 신설)로 보인다 — 그 변경이 틀린 게 아니라 **경계를 넘는 순간 아무도 안 울었다**(파일이 커지는 것 자체엔 발화 지점이 없다). ⚠ 범위가 이 트랙 밖이다: `/deploy` 2단계가 `tools/test-ci.js` 를 게이트로 쓰므로 이 적색은 **저장소 전체의 배포를 막는다**(원격 CI 는 Actions 소진으로 0건이라 이 로컬 모사가 유일한 진실 층이다). 🔴 **주인 있는 적색이다 — 내가 못 고친다**: `DESIGN.md` 는 살아있는 세션 `8aff2ee6` 의 작업본이다(`작업본소유자` 실측 0분 전 접촉 · 작업본 17,453B 로 더 커지는 중)이라 F073 이 편집을 금지한다. 집는 세션을 위한 재료 셋: ㉠ `DESIGN.md` 를 「요약 + 정본 링크」로 되돌려 상한 밑으로(그 파일의 원래 형태이고, 킷 정본은 `docs/디자인_컨셉_정본_v1.md` 에 따로 있다) ㉡ 처방 문구를 「그 절만 `Read offset/limit`」으로 바꿔 큰 파일을 통째로 읽지 않게 한다 ㉢ read-budget 의 `BIG` 을 올린다 — 🚫 이건 상한을 올려 푸는 길이라 F249 에서 이미 죽었고, 올리는 순간 이 검사는 어떤 크기에도 안 울게 된다. ⚠ 판정 전에 `8aff2ee6` 의 보드 줄과 생사를 먼저 본다(그 세션이 지금 그 파일을 짓고 있으면 크기는 더 움직인다). | ㉠ 갈래로 착지했다 — `DESIGN.md` 를 상한 밑으로 줄였다(신고문이 적어 둔 세 후보 중 첫째 · ㉢「BIG 을 올린다」는 F249 에서 이미 죽은 길이라 안 갔다). 실측 2026-08-14 `local_4f668e62`: 작업본 `DESIGN.md` = **15,511B** < read-budget `BIG` = 16,384B(HEAD 판 15,361B) → `tests/디자인가드.test.js` **18/18 pass · fail 0**. 즉 신고 당시 적색이던 `:173`(「처방이 시킨 DESIGN.md 읽기가 read-budget 에 막히지 않는다」)이 초록으로 돌아섰고, 그 적색이 막고 있던 저장소 전체 `/deploy` 게이트(`tools/test-ci.js`)도 이 축에서는 풀렸다. 실제로 막은 것 = `c93f8f1d`(중복 접기로 파일을 줄이고, design-guard 의 「큰 파일」 문장을 상수로 적지 않고 statSync + read-budget 소스에서 읽는 **파생값**으로 바꿔 같은 값을 두 곳에 안 적게 했다) + 대기열 #Q73 종결분. ⚠ 남는 것은 신고문이 지목한 뿌리 하나 — **파일이 커지는 것 자체엔 발화 지점이 없다**(경계를 넘는 순간 아무도 안 울었다). 지금은 회귀가 «넘은 뒤에» 적색으로 알리는 구조라, 다음에 DESIGN.md 가 다시 자라면 같은 모양으로 또 배포를 막는다. 그 발화 지점 신설은 이 신고의 범위 밖이라 닫으면서 남긴다(재발 시 착수 문턱). |
