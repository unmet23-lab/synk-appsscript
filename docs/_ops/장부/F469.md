| F469 | 2026-08-15 | 실수 | tests/장부유실.test.js 가 워크트리 가지에서 master·남의 워크트리의 새 장부 조각을 「유실」로 오진한다 — F215 의 워크트리판. 이력은 `--all`(모든 ref)에서 모으는데, 뒤처짐/유실을 가르는 재료는 `@{upstream}` 하나만 봤다. Claude Code 워크트리 가지엔 상류가 안 걸리니 빈 판 = 후보 전부가 유실. 워크트리가 6개씩 도는 지금은 누가 장부에 행 하나를 더할 때마다 나머지 전부의 test-ci 가 빨개진다(주인 없는 적색 양산기 — 주인 없는 적색은 남의 배포를 막는다). 실측 08-15: master `7785ce3c`(F467) 와 남의 산 워크트리 `c51a52db`(F468) 둘 다 이 모양으로 적색을 냈고, 임시 처치는 master 원문을 그 워크트리에 그대로 수렴시킨 것이었다(행 삭제 0). | tests/장부유실.test.js — 가르는 재료를 상류 하나에서 **세 갈래**(상류 · 기본 가지 master/main·origin · 지금 체크아웃된 워크트리들의 HEAD)로 넓히고, 「가지」가 아니라 **워크트리**를 세도록 했다(F403 과 같은 판정 — 미병합 가지 셋 중 둘은 낡은 사본이다). F123 경계는 그대로다: 죽은 가지에만 남은 행은 여전히 유실. 넓히기가 F195(반입 덮어쓰기) 탐지를 삼키지 않게 가르는 축을 하나 더 뒀다 — 후보가 **내 가지 이력**(`git log HEAD`)에 있었으면 남이 들고 있어도 유실이다(내가 지운 것이지 안 온 것이 아니다). 「내 HEAD 의 조상인 판은 재료가 아니다」(merge-base --is-ancestor)로 내 워크트리 자기제외까지 대신해, Windows 경로 비교가 새던 자리를 없앴다. 닫은 것 = 구 `상류번호()` 를 폴백으로도 안 남기고 삭제. 회귀 6건 신설(총 13) · 탐지력 실측: 구판(상류 하나)에 대고 돌리면 신설 3건 + 실 저장소가 적색, 반대로 경계를 과대하게 넓히면(가지 전부를 살아있다고) F123 경계 3건이 적색 — 양방향으로 못박혔다. |
