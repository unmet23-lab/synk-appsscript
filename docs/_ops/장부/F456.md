| F456 | 2026-08-15 | 마찰 | 같은 그림의 「코어색」이 도구마다 다른 값으로 나온다 — 자(ruler)가 넷인데 전부 자기 이름을 안 찍는다. 실측 08-15(재염색_본체.png 한 장): `펠트색갈이.py` = #D65649(Lab 채도 C*>30 · 원본 해상도 3px 격자) · `자립형가볍게.py` = #E55D50(HSV 채도>0.25 & max>60 · 512px 축소본) · `상태컷정합.py` = #EC584A(배경 걷은 마스크 안) — 자간 ΔE2000 3.62(구판 3.45 · 네이티브 2.43).  🔴 왜 굵직한가 — 자의 오차가 판정 재료보다 크다. 유호님이 고르던 ㉠재염색↔㉡네이티브의 실제 색차는 ΔE 0.49(자A)/0.90(자B)인데, 자 둘의 불일치는 2.43~3.62 다. 즉 「어느 쪽이 킷 코랄에 가까운가」는 그 숫자들로는 애초에 결정 가능한 질문이 아니었다. (픽 자체는 무효가 아니다 — 보드 A/B 는 두 판에 «같은 자»를 썼으니 그 비교는 유효했고, 유호님은 그림도 함께 보셨다. 무효가 되는 건 «문서 간 인용»이다.)  🔴 그리고 장부에 틀린 단정이 남아 있다 — 커밋 e1547baa 가 「측정 정의는 `펠트색갈이.py` 와 같은 것을 쓴다(정의가 갈리면 대조가 판정을 못 낸다)」고 적었는데 실제로는 Lab 채도 vs HSV 채도 · 원본 vs 512px 축소로 다르다. 정의를 맞췄다고 적어 둔 탓에 다음 세션은 대조해 볼 이유를 잃는다.  ⚠ 새는 방향 = 언제나 「통과」. 각 도구는 자기 안에서 일관돼 전부 초록으로 나온다. 어긋남은 숫자가 문서를 건너갈 때만 생기고(메모리는 #E55D50, 그 파일을 만든 도구는 #D65649 를 찍는다), 그 자리를 보는 검사는 0건이다. 다음 세션이 재염색본을 다시 재면 「자산이 바뀌었다」는 오판이 정상적인 실측의 얼굴로 나온다.  ▶ 처방 후보(이번 세션은 «안» 한다 — 되돌림 비용): ①`tools/lib/코어색` 1벌로 파생 ②각 도구가 값 옆에 자 이름을 찍기. ⚠소급 통일은 이미 인용된 숫자를 통째로 무효화한다 — 15e2290d 가 같은 이유로 기존 굽기 도구 둘을 안 건드렸다. 그래서 안전한 순서는 «새로 찍는 값부터 자 이름을 붙이는 것»이고, 옛 숫자는 어느 자로 잰 것인지만 주석으로 남긴다. CLAUDE.md 「같은 판정을 두 곳에 적으면 갈라진다(목록은 하나에서 파생)」와 맹점 ④의 실물 사례. | |
