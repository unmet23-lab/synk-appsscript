| F410 | 2026-08-13 | 마찰 | 새 회귀가 공용 통로(tests/lib/소스검사.js)를 못 썼다 — 그 통로가 아직 미추적이라 기대면 내려받는 쪽에서 검사가 통째로 죽는다. 그래서 tests/상담철학정합.test.js 는 코드 모양 패턴으로 자체 방어했고, 「내가 남긴 주석을 위반으로 읽는」 거짓양성을 한 번 겪고서야 잡았다(형제 tests/상담지식.test.js 가 머리말에 이미 적어 둔 그 함정). 미추적 공용 통로는 «있는 것처럼 보이지만 남이 못 쓰는» 상태다 — F025 의 회귀 버전. | 공용 통로 `tests/lib/소스검사.js` 가 **이제 양쪽 저장소에 추적된다**(as·talk `git ls-files` 둘 다 실측) — 신고가 지목한 차단자가 사라졌다. 그래서 `tests/상담철학정합.test.js` 가 통로로 갈아탔다(커밋 `4df2f684`): 지식·엔진을 `코드만` 판으로 보고, 주석을 피하려고만 있던 자체 방어(`… ' +` 꼬리)를 걷었다. 그 모듈이 요구하는 탐지력 픽스처(`코드만픽스처`)도 ⓪ 으로 한 번 돈다. 뜻인 앵커는 남겼다 — ①(금칙 «배열의 원소»인가)·④(리터럴이 그 낱말로 «시작»하는가)는 자체 방어가 아니라 판정이다. 변이 5/5로 갈랐다: ①위반이 코드로 되살아나면 잡는다 ②같은 말이 주석에만 있으면 안 잡는다(신고된 거짓양성) ③HEAD 깨끗 ④**대조 — 옛 판이었으면 ②에서 거짓양성이 났다** ⑤대조 — 옛 판도 ①은 잡았다. 즉 바뀐 것은 탐지력이 아니라 «거짓양성 부류 하나»다. 회귀 6/6. 🔴 그 과정에서 새로 실측한 것 하나를 함께 남긴다 — **시야는 하나가 아니다**: 전부 `코드만` 으로 돌렸더니 ⑤가 빨개졌다(doc-graph 파생 스탬프는 «주석에 사는 것이 정상»이라 주석 지운 판엔 원리상 없다). `지식원문`/`지식` 둘로 갈라 이름으로 못박았다(깃발 하나로 두면 다음 사람이 또 통째로 돌린다). 이것이 공용 통로의 대가다 — 「주석을 지운다」가 언제나 옳은 게 아니라 시야는 «무엇을 보는 단언인가»로 고른다. 남은 것 = 신고문이 곁들여 적은 「상담지식.test.js 와 합치기」는 이 신호의 뿌리가 아니라 별건이고 그 파일은 아직 남의 작업본이라 안 건드렸다(F073). |
