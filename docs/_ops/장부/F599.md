| F599 | 2026-08-18 | 실수 | 분모를 넓히면서 «옛 분모의 텍스트 판정»을 그대로 물려받아, 내 첫 판이 도구 둘에 「병을 만드는 처방」을 적었다 — 부착 전에 잡았지만 새는 방향은 「그럴듯한 명단」이었다.  🔴 실측(2026-08-18 · talk #Q112): F594 를 닫으려고 플래그 게이트의 분모를 `자격증명.읽기(` 텍스트 대조에서 argv 기준으로 넓혔다. 그런데 **같은 파일 안의 «다른 축»**(조준축 = `--운영` 이 그 도구에서 뜻을 갖나)은 옛 텍스트 판정을 그대로 물려받았다. 그래서 `lib/왕복골격.js` 의 `열기()` 를 지나 조준하는 도구 **다섯**(`왕복시험`·`교정1건왕복`·`세그먼트재료`·`업로드왕복시험`·`eval-run`)이 「조준축 없음」으로 판정됐고, 그중 위치 인자 전용인 둘(`세그먼트재료`·`업로드왕복시험`)에 명단이 **「선언 = 빈 목록」**이라고 적혔다 — 따르면 `--운영` 이 죽는다(F103 의 그 모양). 회귀는 전부 초록이었다: 그 축엔 검사가 없었으니까.  🔴 그리고 고치려고 만든 «한 겹 추적»의 첫 판도 틀렸다 — 반대 방향으로. `lib/자격증명.js` 169행이 도움말로 `자격증명.읽기('<도구>', …)` 를 **문자열에** 담는데(`코드만` 은 문자열을 정당하게 지킨다), 그 파일을 「부여자」로 세자 그 파일을 들여오되 `파일읽기` 만 쓰는 `앱밟기.js` 가 조준축으로 승격됐다(거짓양성). ㉡(코드↔산문)이 주석이 아니라 **문자열**에서 재발한 자리다.  🔑 교훈 둘 — 다음에 분모를 넓힐 때 이 두 줄만 먼저 읽으면 된다:   ㉠ **분모를 넓힐 때는 그 파일이 쓰는 «판정 전부»를 세어 본다.** 한 축만 넓히면 나머지 축이      옛 좁은 판정으로 남고, 그 어긋남은 적색이 아니라 **«처방이 틀리는» 쪽으로** 샌다.      장치가 「안 도는」 것보다 이쪽으로 더 자주 샌다(v9.2 맹점 ④).   ㉡ **오판 두 방향을 같은 픽스처 묶음에 담는다.** 미탐(한 겹을 못 봄)과 오탐(문자열을 부여자로      봄)은 방향이 반대라, 한쪽만 막으면 나머지가 그대로 산다. 그래서 판정 함수가 파일 읽기를      **주입**받게 짓고(파일 없이 시험된다), 다섯 모양을 값으로 못박았다.  ⚠ 잡아 고친 경로가 「코드 독해」가 아니라 **실측**이었다는 것도 기록해 둔다 — `왕복골격.열기()` 를 직접 열어 `자격증명.읽기(도구)` 를 본 것이 첫 오판을 깼고, `앱밟기.js` 를 직접 열어 `파일읽기` 만 쓰는 것을 본 것이 둘째 오판을 깼다(둘 다 「그 대상을 직접 연다」 조항). | 같은 파일의 두 판정을 «한 축씩» 넓히지 말고 함께 세었다 — 조준축 판정을 talk `tests/lib/CLI인자.js` 의 `조준축()` 으로 옮기고, 그 함수가 lib 읽기를 **주입**받게 지었다(파일 없이 시험된다 — 그래야 두 오판이 못 돌아온다). 미탐·오탐 두 방향을 픽스처 다섯에 함께 담고, 「정의 파일은 조준축을 부여하지 않는다」를 규칙으로 못박았다(한글 lookbehind 로 `파일읽기(` 와 `읽기(` 를 가른다 — JS 의 `\b` 는 ASCII 경계라 원리상 안 붙는다). 명단의 🎯 라벨을 기계가 실측과 대조하고(라벨이 틀리면 적색), 한 겹 추적이 죽으면 「한겹 ≥ 5」 단언이 빨개진다. 변이 ⑫~⑮ 네 건이 두 방향을 각각 물었다(15/15 구멍0). ⚠ 이 신고를 담는 명령 자체도 같은 계열을 한 번 더 밟았다 — 해소문을 `--해소 "…"` 인라인으로 줘서 백틱이 셸 명령 치환에 먹혔고(F060 계열), 위 문장의 파일 경로가 통째로 사라진 채 기록됐다. 그 자리를 이 줄로 복원했다(행 삭제 아님). 긴 글·백틱은 `--파일` 통로로 준다. |
