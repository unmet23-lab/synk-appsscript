| F289 | 2026-08-09 | 마찰 | 대기열 항목이 08-08 에 닫혔는데 08-09 까지 열린 채 남아, 새 세션이 「막히지 않은 유일한 P1」로 골라 착수했다가 실측으로 되돌렸다. 실측: 대기열 P1 「retry_of_event_id 목록이 event_id 를 안 준다」는 talk 7c0768b(08-08)이 사슬 4마디(deliver 집기박기 → tasks 싣기 → lib/오늘과제 제출사건 되싣기 → src/제출API 전송)로 이미 닫았고, 그때 「목록에 event_id 추가」 길은 오히려 명시 기각됐다. 그런데 그 줄이 근거로 단 메모리가 learning-data-layer-audit(08-07 판)이라 낡은 진단을 그대로 들고 있었다. 새는 방향이 나쁘다 — 대기열은 「다음에 집을 것」의 정본이라 낡은 줄은 조용한 오답이 아니라 세션을 통째로 헛돌린다(이번엔 내가 코드를 열어 봐서 멈췄지만, 그대로 착수했으면 이미 있는 사슬을 두 번째로 지었다 = F070). 급소: 닫은 세션(local_f3b14f3e)이 talk 저장소에서 일을 끝냈고 대기열은 appsscript 에 있어, 「끝나면 그 줄을 지운다」 규칙이 저장소 경계를 넘지 못했다. 처방 후보 ①대기열 줄이 근거로 단 메모리의 수정 시각이 줄보다 새로우면 rot-check 가 알린다 ②줄에 talk 커밋 해시를 적게 하고 그 해시가 이미 master 에 있으면 낡음 후보로 띄운다 ③형제 저장소 커밋이 대기열 낱말을 건드리면 알린다 | tools/대기열.js 낡음후보() — 형제 커밋 제목이 「대기열 P*n*」을 걸었는데 그 층이 그 뒤로 안 바뀌었으면 세션 시작에 층 단위로 알린다(회귀 tests/대기열낡음.test.js 8건 · 변이 4/4). 재료는 추론이 아니라 일한 세션 자신의 선언 — 실측으로 경로대조(F289 그 줄엔 경로 0개라 못 잡음)·식별자대조(4발 1적중, 도메인 낱말이 아무 커밋에나 걸림)를 제치고 골랐다. 1초 밀기는 rot-check 것을 빌려 써 복사본을 안 만들었다(변이 M4 가 두 스위트를 동시에 빨갛게 만들어 빌림이 산 것을 증명). 못 보는 것 = 제목에 「대기열」을 안 적은 커밋·같은 층 다른 줄(그래서 문구가 「낡았다」가 아니라 「대조하라」). |
