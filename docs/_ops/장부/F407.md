| F407 | 2026-08-13 | 실수 | board-move 원칙⑦ 거절문이 「남은 일이 없다고 실측했으면 그 근거를 달아 다시 돌려라: board-move '문구' --미완확인」 이라 적어 놓고, 정작 근거 문자열을 붙이면 needle 조립(:95 args.filter(...).join(' '))이 그것까지 검색 문구로 삼아 「줄을 못 찾았다」로 튕긴다. --미완확인 은 :94 에서 boolean 으로만 읽히고 사유를 받는 자리가 없다(:92 주석은 「사유를 말로 받는 이유는 F103」이라 적었는데 구현이 안 따라왔다). 처방을 그대로 따르면 실패하고, 성공하는 통로는 근거를 버리는 쪽이다 — 근거가 어디에도 안 남는다. 정확히 F103(따를 수 없는 처방은 우회를 정상 통로로 만든다). | ⏸ 안 고쳤다. 수리 자체는 작다(board-move.js:95 needle 조립에서 --미완확인 뒤 문자열을 사유로 떼어내고, 아카이브 줄이나 커밋 메시지에 그 사유를 남긴다). 다만 tools/board-move.js 는 모든 세션이 종료 때 쓰는 공용 통로라 지금 고치면 도는 세션 10개의 마감 절차를 흔든다 — 회귀(tests/보드유령.test.js 계열)와 한 커밋으로 묶어 별건으로 집는다. 판정 자리 = 세션 0c6fbc37 정찰 종결 보고. |
