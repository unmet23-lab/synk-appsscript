| F540 | 2026-08-17 | 마찰 | 대장동봉 게이트의 방아쇠 목록이 전부 as 경로다 — 형제(SYNK-talk)가 커밋하면 화면이 조용히 낡고 master 가 남의 트랙 때문에 빨개진다.  실측 2026-08-17 (`local_57d592c2` · #Q101 의 기준선을 재던 중 발각 — 이 트랙이 겨눈 것이 아니다): 얼린 트리(HEAD=7617f5ac · autocrlf=false · 형제 정션)에서 전량 3945 중 fail 4 였다. 셋이 대장동봉 계열이고 (`tests/대장동봉.test.js` :71 줄끝판 · :81 자기처방판 · :139 실저장소판) 산 워크트리에서도 똑같이 셋이었다 — 얼음 탓이 아니다. 다시 그려 대조하니 갈린 줄은 정확히 **1줄**, `exam.result` 의 사유 문장이었다. 그 문장의 출처는 형제 저장소이고, 형제는 13:47 에 `4e6bf4a`(exam.result 규격 · `lib/시험결과.js`)를 커밋했다.  ■ 급소 — 게이트는 잘 돌고 있는데 **부를 사람이 없다**   `tools/대장동봉검사.js:52` 의 `방아쇠목록` 은 다섯 줄 전부 as 경로다   (정본 `docs/SYNK_철학.md` · 생성기 `tools/이해대장.js` · `tools/lib/표.js` · `tools/lib/시트도달.js` · `docs/_ops/엔진점수.jsonl`).   화면의 부모는 그 다섯이 아니라 **여섯**이다 — 형제 저장소가 여섯 번째인데 목록에 없다.   그래서 형제 쪽 커밋은 이 게이트를 **원리상** 못 울린다: 그 커밋의 스테이징에 as 경로가 하나도 없으니   `방아쇠.length === 0` 으로 조용히 0을 낸다. 화면은 낡고, 다음에 as 에 커밋하는 세션이 그 적색을 물려받는다.  ■ 왜 F364·F525 가 이걸 안 막았나 — **둘 다 반대 방향이다**   F364 = 형제가 **없는** 환경에서 hard fail 이 나던 자리 → 마스킹으로 접었다.   F525 = 그 마스킹이 «접다 만» 자리(형제 의존 블록 하나가 마스킹 밖) → 블록을 안으로 옮겼다.   둘 다 「형제가 안 읽힐 때」를 다뤘고, 이것은 「형제가 **움직였을 때**」다. 마스킹은 여기서 도움이 안 된다 —   두 기계 다 형제가 읽히고, 값이 실제로 갈렸으니 그 적색은 **참**이다. 못 고칠 사람에게 갔을 뿐이다.  ■ 지금 상태 — 열린 채로 둔다(내가 안 고친 이유)   `docs/이해대장.html` 은 지금 두 세션이 들고 있다: `24b6c036` 이 「만질 파일」로 선언했고(보드),   낡게 만든 커밋의 주인 `ae5b475e` 는 13:47 에 살아 있었다. 내 바이트가 남의 커밋에 실린다(F073).   다시 그리는 것 자체는 한 명령(`node tools/이해대장.js`)이라 주인이 곧 갚을 수 있다.  ▶ 처방 후보(고르지 않았다 — 재고 나서 정한다)   ㉠ 형제 HEAD 를 방아쇠에 넣는다 — as 커밋 때 형제 SHA 가 화면에 박힌 것과 다르면 막는다.      대가: as 트랙이 형제 진행에 인질이 된다(남이 커밋할 때마다 내 커밋이 막힌다). 아마 못 쓴다.   ㉡ 형제 쪽 post-commit 이 as 화면 낡음을 알린다 — 막지 말고 드러낸다(silent-failure-ledger ④ 계열).   ㉢ 화면의 형제 의존 블록을 검사에서 **자리표로 접고**(F364 마스킹의 확장), 그 축은 별도 신선도      검사가 형제 HEAD 로 따로 진다 — 「못 재는 한 칸 때문에 잴 수 있는 화면 전부를 놓지 않는다」의 승계.   ⚠ 셋 다 「어느 쪽이 얼마나 자주 도는가」를 안 재고는 못 고른다. 재기 전에 무엇을 잴지부터 정한다. | |
