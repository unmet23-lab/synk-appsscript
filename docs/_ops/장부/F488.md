| F488 | 2026-08-15 | 마찰 | 테스트가 «못 쟀다»를 fail 로 낸다 — tests/작업본소유자.test.js 가 git fetch 5초 타임아웃에 적색. 도구 자신은 그 출력에 「실패가 아니다」라고 찍는데 회귀는 /방금올라온것/ 매치를 요구해 CI 를 통째로 빨갛게 세운다(F296 그 자리: repo 밖 환경에 기댄 검사는 fail 아니라 skip 으로 드러내야 한다). 주인 없는 적색이 남의 배포 게이트를 막는다. | `tests/작업본소유자.test.js` 의 #Q95 스로틀 검사 4건이 「원격을 **못 쟀다**」를 만나면 fail 대신 **skip** 한다(F296 그대로 — repo 밖 환경에 기댄 검사는 실패가 아니라 미측정으로 드러낸다). 새는 방향(조용한 초록)은 두 조건으로 막았다: ①도구가 **말했을 때만** 건너뛴다(아무 말 없는 회차는 여전히 fail — 진짜 회귀를 덮지 않는다) ②문구를 `원격을` 로 못박아 `git status` 쪽 만료(F489)까지 삼키지 않는다. ⚠ 이 skip 문은 평시엔 한 번도 안 타서 그대로 두면 「안 도는 가드」라, 탐지력을 픽스처로 못박았다 — 진짜 만료엔 열리고 · 정상 회차엔 안 열리고 · `git status` 만료엔 안 열린다를 **도구가 실제로 찍은 세 문장**으로 잰다(문구를 손으로 베끼면 도구가 말을 바꾼 날 이 검사만 초록으로 남는다). 113/113(skipped 0 = 조용히 발화하지 않았다는 분모) · **변이 8/8** 중 ⑥⑦⑧ 이 이 문의 양방향·폭을 전부 문다 — 커밋 77476d79. |
