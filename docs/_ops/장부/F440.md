| F440 | 2026-08-14 | 교정 | 산출물을 만들고 커밋했는데 유호님께 «보내지» 않았다 — 자개 캐러셀 예시 HTML 2벌이 12ae23e9 로 커밋되고 보드에 ✅로 적혔지만 운영자료 반출도 직송도 0이라 유호님 화면엔 아무것도 안 왔다(유호 08-14 「자개 캐러셀이 아직 안온것같아」). 보드 ✅·렌더린트 초록·커밋은 전부 «만들었다»만 증명하고 «닿았다»는 아무도 안 쟀다 — 유호님이 보는 산출물의 완결은 반출까지다. | 트랙을 «끝났다»고 선언하는 자리에 「닿았나」 한 칸을 세웠다 — 판정 정본은 `운영자료.반출도달` 하나이고, 발화는 `board-move` 의 성공 종료(exit 0) 한 곳이다(종료 자리가 셋이라 `process.on('exit')` 로 묶었다 — 세 곳에 적으면 한쪽만 낡는다). 🔑 **재기 전에 대상을 어떻게 정할지부터 정했다**: ①어느 파일인가 = 그 보드 줄의 「만질 파일」 칸에서 도출(`lib/보드.js 산출물경로들`) ②어느 종류인가 = 유호님 폴더 실측에서 도출(`화면확장자`) — 둘 다 손 목록이 아니다(#Q89 가 방금 닫은 죄를 다시 짓지 않는다). 🔴 **①에서 이 신고를 낳은 그 줄이 원리상 안 잡히는 것을 발견했다**: 원문이 `docs/캐릭터/자개_캐러셀_예시_0814{,_자립형}.html` 인데 `만지는텍스트` 의 구절 경계에 `{`·`}` 가 들어 있어(그건 그 판정에는 옳다) 조각이 셋으로 쪼개지고 `.html` 이 홀로 남는다 — 실측: 중괄호를 안 펼치면 그 칸에서 경로가 **1개**(딴 파일 `.md`)만 나오고, 펼치면 **3개**가 나오며 늘어난 둘이 정확히 유호님이 못 받은 그 두 파일이다. 새는 방향이 「검사할 게 없다」라 조용히 통과한다(가드 맹점 ① — 사람이 실제로 쓰는 표기로 검사한다). ⇒ `중괄호펼치기` 를 세웠다. 🔑 **②는 실측이 규칙을 정했다**: 확장자별 도달률(분모=`정본후보()`) `.html` 26/86=30.2% · `.pdf` 8/25=32.0% · `.xlsx` 1/2=50.0% vs `.md` 7/96=7.3% · `.txt` 1/30=3.3% — `.md` 는 이 저장소가 설계·장부를 적는 형식이라 넣으면 경보가 벽이 된다(실측: 보드 활성+아카 152줄에 돌려 **우는 줄 43~60**). 문턱 10%로 좁히니 **11~12줄**이 됐고 남은 것이 전부 진짜였다(자개 캐러셀·체리젤리 킷대조 AB·브랜드 명품판·홈페이지 병합판 — 유호님이 봐야 하는데 안 간 것들). 문턱은 비율이라 스스로 따라온다 — 유호님이 `.md` 를 정기적으로 받기 시작하면 그날부터 저절로 켜진다. 그리고 **되짚을 수 있는 것만** 센다: `--이름` 으로 갈아입고 나간 사본은 어느 repo 경로에서 왔는지 알 길이 없다(실측: 폴더의 `.png` 3·`.mp4` 1 이 전부 갈아입은 사본이고 저장소 `.png` 109개 중 이름으로 되짚히는 것 **0개**) — 넣으면 그 종류는 전건이 「안 닿았다」로 나오는데 그건 참이 아니라 못 재는 것이다. ⚙ **부수로 통로 하나가 살아났다**: `.lnk` 대상 읽기가 파일마다 PowerShell 을 띄워 `--목록 --갈래 운영`(52건)이 **9.0~9.8초**였다 — 그 값으로는 어느 도구도 이 판정을 부를 수 없다. `경로+mtime+size` 캐시로 **9,838ms → 147ms**(실측 3회 · 67배), 그리고 「빨라졌는데 틀리면 최악」이라 **냉(캐시 빌드)=온=온 바이트 동일**을 대조했다(69항목·바로가기 47). 캐시는 **성공만** 담는다 — 실패를 담으면 유호님이 깨진 바로가기를 고쳐도 도구가 옛 실패를 되풀이한다. 🔑 **회귀가 진짜 버그 둘을 잡았다**: ①`중괄호펼치기` 재귀판이 상한에 걸리면 중괄호가 **반쯤 남은** 문자열을 냈다(`a111{1,2}….html` — 원문도 아니고 펼친 것도 아닌 제3의 값) → 반복으로 바꿨다 ②캐시 키에 공백을 적었는데 기록 층에서 **날 NUL 바이트**로 박혔고(`tests/운영자료.test.js` 「소스에 날 제어문자가 없다」가 잡았다 · 같은 자리 세 번째) 구분자를 안 쓰는 꼴(`JSON.stringify([…])`)로 바꿨다. 회귀 `tests/반출도달.test.js` **14건**(전부 픽스처 — 실저장소=유호님 바탕화면이라 CI 에 없다 · 모사 셸로 `.lnk` 를 흉내내 PowerShell 없는 판에서도 돈다), **변이 14/14**(구멍 0 · 미적용 0). 그중 **배선 변이 3건**이 F481 이 겨냥한 자리다 — 「exit 훅을 지운다」·「성공이 아니라 실패에서만 운다」·「호출부가 파일칸을 빈 문자열로 죽인다」 셋 다 판정 회귀로는 안 잡히고 `board-move` 를 프로세스로 띄우는 배선 시험만 문다. ⚠ **대가(새 장치엔 함께 적는다)**: ①**새는 방향은 「통과」다** — 보드 파일 칸이 경로를 안 적었거나(「스크래치패드」·「운영자료 41~43」 처럼 이름만 적은 줄이 실제로 있다) 한 번도 안 나가 본 종류면 **한 건도 안 뜬다**. 그래서 출력이 「본 경로 N = 닿음 + 안닿음」을 분모로 함께 내고 「경로를 안 적은 조각은 원리상 안 세진다」를 그 자리에 적는다(F207) ②**비용** — 캐시가 찬물이면 ~10초(그때만) ③**닫은 장치 0** — 이 자리에서 닫을 옛 장치가 없다(«닿았나» 를 재던 것이 애초에 0개다). 대신 스스로 조용해진다: 반출하면 다음 실행부터 안 뜬다 ④**막지 않는다** — 반출은 유호님 화면을 바꾸는 일이라 기계가 대신 누를 자리가 아니고, 여기서 막으면 「닫을 수 없는 트랙」이 생겨 우회가 정상 통로가 된다(F103). 🔴 **그리고 이 신고의 그 파일은 오늘도 안 닿아 있다** — `node tools/운영자료.js --닿았나 "docs/캐릭터/자개_캐러셀_예시_0814.html"` 실측 08-15: 어느 갈래에도 없다(신고 이튿날). 반출 자체는 **유호님이 배치한 폴더를 바꾸는 일이라 제안으로 올렸다**(CLAUDE.md 제안·선제 보완) — 안 닿은 11~12줄 목록과 함께. |
