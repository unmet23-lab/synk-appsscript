| F645 | 2026-08-18 | 마찰 | 대기열 채번이 가지 사이에서 안 잠긴다 — 오늘 `#Q116` 이 **둘에게 발급**됐다: `claude/continue-work-8yo3bz` 의 `9f3acdec`(09:57 · F637 셸 매처 훅)와 `claude/distributed-ai-tutor-analysis-5nfc0t` 의 `59367b00`(13:55 · 엔진 축 A 회피 지도 · #Q116~#Q121 여섯 줄). 둘 다 master 에 안 들어와 있어 서로의 존재를 못 본다 — 반입되는 날 같은 번호가 두 가지를 가리키고, 대기열은 「#Q118+#Q119 는 한 커밋으로」처럼 **번호로 서로를 참조**하므로 그 참조가 통째로 어긋난다(소급 정정이 사람 손이다). 원인은 채번이 origin 태그 선점에 기대는데 이 환경에서 그 push 가 막힌 것과 같은 뿌리다 — `friction.js` 도 오늘 「번호를 예약하지 못했다 — F643/F644 로 진행한다」를 두 번 냈다(F196 계열). 즉 **장부·대기열 두 축의 채번이 같은 자리에서 동시에 샌다.** ⏸ 안 고쳤다 — 처방 후보는 ①채번을 `origin/master` + **원격 가지 전량** 대조로 넓히거나(friction.js 는 이미 로컬 가지까지 훑는다) ②가지에서는 번호 대신 임시 표식을 쓰고 반입 시 확정하는 것인데, 어느 쪽이든 「가지가 많은 날」의 실측(오늘 열린 가지 수·충돌 실발생률)을 먼저 재야 한다. 오늘 실측된 충돌은 1건이다. | |
