| F455 | 2026-08-15 | 마찰 | 운영 배포본 5개가 소스보다 낡았는데 아무 게이트도 안 걸리고, 그 사실을 「다름 0」으로 적은 대기열 숫자가 하루 만에 낡았다 — 「실측 8-14」라는 꼬리표가 그 숫자를 계속 사실처럼 살려 둔다  실측 2026-08-15(세션 04974b76 · 읽기만 · 운영 쓰기 0): `SUPABASE_PROJECT_REF=<운영REF> node tools/배포대조.js`(자격증명 「읽기통과」 갈래 — `--운영` 안 붙임) 결과 = **다름 5 · 미측정 1 · 전체 15**.   · deliver  — 배포본에 없는 판: 오늘과제.mjs · 라디오태스크.mjs · 학습자상태.mjs (15/18)   · teach    — 학습자상태.mjs · 라디오태스크.mjs (10/12)   · tasks    — 오늘과제.mjs (12/13)   · progress — 오늘과제.mjs (11/12)   · radio-promote — 라디오태스크.mjs (8/9)   · companion — 미측정(HTTP 404 · 운영 붓기 전이라 정상) 근원은 하나다: talk `49d4feb`(2026-08-14 23:28 · master) 가 lib 셋을 신설해 함수 다섯에 물렸는데 **운영 재배포가 안 왔다**. 리허설·회귀·보드는 전부 초록이라 이 격차를 말하는 자리가 0이다.  ■ 왜 마찰인가 — 두 겹이다. ① **낡음을 세는 눈이 손호출뿐이다.** `배포대조` 는 부르면 정확히 답한다. 그런데 아무도 안 부르면 「운영이 소스보다 낡다」는 어디에도 안 뜬다 — 커밋도 회귀도 보드도 배포 격차를 모른다. 철학 정본 v1.5 가 못박은 자리가 정확히 여기다(배관은 배포 안 되면 없는 것과 같다 · ④완결의 급소). 지금 이 다섯은 「짓고 커밋했으니 됐다」의 얼굴로 서 있다. ② **한 번 잰 숫자에 꼬리표가 붙어 사실로 굳었다.** 대기열 #Q44 줄은 「`배포대조 --운영` 실측 08-14 = corrections 포함 14/14 ✅같다 · 다름 0」을 근거로 표식을 〖⏳유호〗→〖지금〗으로 올렸다. 그 숫자는 **쟀을 때 맞았다** — 위 `49d4feb` 이 그날 23:28 에 들어왔으니 측정이 먼저였을 뿐이다. 문제는 「실측 08-14」라는 꼬리표가 낡음을 **막아 주는 것처럼 읽힌다**는 것이다: 날짜를 적었으니 정직한데, 읽는 쪽은 그걸 「검증된 값」으로 쓴다. F406 은 «표식»이 사진임을 적었고, 이 줄은 **표식의 근거로 인용된 실측 숫자**도 똑같이 사진이라는 것이다. 하루가 뒤집었다.  ■ 사정거리: 다섯 중 넷(deliver·tasks·progress·teach)은 학생·강사가 매일 닿는 통로다. 운영 `learners` 0행이라 지금 피해는 0 — 그러나 그건 미개원이라서지 장치가 막은 게 아니다.  ■ 처방 후보(고르지 않는다 — 배포 소유 트랙 밖이고, 재배포는 늘 HEAD 라 남의 진행 중 트랙까지 싣는다 F273·F302):   ㄱ) 낡음을 «인용 시점»과 함께만 읽게 한다 — 대기열·보드에 배포 숫자를 적을 때 그 숫자를 만든 명령과 HEAD 커밋을 같이 박아, 뒤에 커밋이 붙으면 기계가 「이 숫자 뒤에 배포 대상 파일이 n건 바뀌었다」를 드러낸다. 새 계기를 안 만들고 기존 `배포대조` 출력에 HEAD 를 물리는 쪽.   ㄴ) `/deploy` 가 끝날 때 그 배포가 덮은 함수 목록을 장부에 적어, 다음 세션이 「그 뒤 바뀐 lib」를 대조할 재료를 남긴다.   ㄷ) 그대로 두고 재배포만 한다 — ⚠ 격차는 오늘 닫히고 내일 다시 열린다(이번이 그 모양이다).  ■ 지금 당장의 사실만 남긴다: **운영은 talk master 보다 다섯 함수만큼 낡아 있다.** 누가 재배포할지는 이 신고 밖이다. | talk `3c61579` 배포빚 장부 + 재배포 10벌(운영 6·리허설 4 → 양쪽 다름 0).  🔴 **이 신고의 진단은 실측이 기각했다** — 「낡음을 세는 눈이 손호출뿐이다」는 틀렸다. 눈은 있었고 **정확히 물었다**: `동봉신호`(post-commit)를 그 커밋(`49d4feb`)에 다시 돌리면 `deliver·progress·radio-promote·tasks·teach` 다섯을 이름까지 대며 알린다 — 이틀 뒤 네트워크로 잰 집합과 **완전히 같다**. 빠진 것은 탐지가 아니라 **보존**이다: 그 말이 「그 순간의 stdout」에만 살았고, 배포는 승인 자리라 그 자리에서 못 갚는 일이 흔한데 못 갚은 빚이 어느 장부에도 안 남았다(`368603a` 는 리허설만 갚았고, `49d4feb` 는 메모리에 「▶잔여=배포 둘」만 적고 세션이 끝났다).  🔴 **이 신고가 못 본 것 둘**(운영만 쟀다):   ① **리허설도 초록이 아니었다** — 실측 다름 4(deliver·progress·tasks·radio-promote). 신고문의 「리허설·회귀·보드는 전부 초록이라 이 격차를 말하는 자리가 0이다」는 리허설을 안 재고 쓴 것이다.   ② **자동 호출자가 낡은 판을 때리고 있었다** — 운영 cron 실측 4잡 전부 active: `deliver-daily`(매일 16:05)·`deliver-check`(16:35)·`radio-promote-hourly`(매시 21분)·`transcribe-batch`(10분마다). 「피해 0」의 근거는 미개원(learners 0행)이지 「아무도 안 부른다」가 아니었다.  🔑 **그리고 이 신고의 숫자 자체가 몇 시간 만에 낡았다** — 신고는 다름 5·전체 15, 내 재실측은 **다름 6·미측정 2·전체 16**(`correct` 가 `368603a` 로 새로 벌어졌고 `radio-round` 가 신설됐다). 신고문의 논지(「실측 08-14」 꼬리표가 낡은 숫자를 사실로 굳힌다)가 그 자리에서 자기증명됐다.  ■ 처방 — ㄱ·ㄴ 을 합쳐 **한 장치**로 냈다(ㄷ 재배포도 같이 했다):   · `tools/배포빚.js` 신설 — **네트워크 0**. `원격배포` 가 「작업본이 아니라 HEAD 에서 읽어」 올린다는 규약이 이미 있어, 「배포한 HEAD」 한 칸이면 낡음이 휴리스틱이 아니라 **등식**이다.   · 도장 두 통로: `원격배포 --적용` 성공(같은 걸음) · `배포대조 --도장`(「같다」에만 · 미측정엔 안 찍는다). 원격은 여전히 GET 만.   · 발화는 **기존 자리 재사용** — `동봉신호` 가 커밋마다 「방금 진 빚 + 아직 안 갚은 빚」을 함께 낸다. 새 훅 0 · 새 SessionStart 항목 0(경량 원칙).   · 「0은 분모와 함께」: 합계 = 낡음 + 최신 + 모름. **장부에 없는 함수는 절대 「최신」이 아니다** — 이 장치가 샐 유일한 방향이라 별도 칸이다.  ■ 검증: 회귀 16 · **변이 8/8** · talk 전체 2232 중 실패 0 · 재현 픽스처(`49d4feb^` 를 기준으로 두면 다섯 함수를 그대로 짚는다). 변이가 실제로 잡아 고친 것 = 등록층 검사가 `배포빚.도장(` 의 **존재**만 봐서 `if (false)` 로 닿지 않게 만들어도 초록이었다 → 순수 `도장거리()` 로 빼고 배선 **모양**을 못박았다.  ■ 남은 한계(적어 둔다): 이 장치는 **알리기만 하고 안 막는다**(배포는 승인 자리라 막으면 우회가 정상 통로가 된다 · F103). 그리고 발화가 talk 커밋 자리라, talk 을 안 건드리는 세션은 못 본다 — 대기열 P3 로 세웠다. |
