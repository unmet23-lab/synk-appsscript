| F495 | 2026-08-16 | 마찰 | 보드 파일 이름이 지문 꼴이 아니면 그 줄은 **주인 판정에서 통째로 빠지고, 그 방향은 「영구 잠금」이다.** 실물 1건: `docs/_ops/보드/local_008484b1.md` (접두 `local_` 을 안 뗀 이름 · 지금 그 세션은 살아 있다). `.claude/hooks/lib/board-id.js:파일지문()` 은 `^[0-9a-f]{8}\.md$` 만 인정하고 아니면 `''` 을 낸다 — 주석이 그 빈 문자열을 「주인 없음이지 내 것이 아니다」로 정확히 경고해 뒀는데, **소비자 쪽에서 그 값이 「죽지 않았다」로 접힌다**: `board-guard.js:원칙7처방()` 이 `if (!지문) return false` 로 그 줄을 죽은-활성 목록에서 빼고, 같은 자를 쓰는 상한 셈(F372 죽은 주인 빼기)도 산 것으로 센다. 그래서 지금은 「맞는 답을 틀린 이유로」 내고 있다 — 그 세션이 살아 있으니 결과가 우연히 맞는다. 🔴 그 세션이 죽는 순간 값이 뒤집히는 게 아니라 **아무것도 안 바뀐다**: 여전히 산 것으로 세어져 죽은 줄 목록에 영영 안 뜨고, `원칙7처방` 이 내미는 board-move 명령에도 안 실린다. 그러면 F073(남의 줄 편집 금지)·원칙⑦(상태 칸이 끝났다고 안 말한다)·`보드수거`(완료행만 본다) 셋이 그대로 걸려 **아무도 못 치우는 줄**이 된다 — #Q93 이 적은 병인데 경로가 다르다(#Q93 의 11건은 적어도 처방 목록에 «뜨기라도» 한다). 🔑 급소는 등록층이다(CLAUDE.md 「가드는 로직보다 등록층에서 샌다 · 새는 방향은 언제나 통과」): board-guard 는 「내 줄은 내 파일에만」을 기계로 강제하면서 **그 파일 이름이 지문 꼴인지는 한 번도 안 본다.** 실측 분모 = 보드 폴더 80파일 중 79가 8자리 hex, 어긋난 것 1(=이 건). ▶처방 = 만드는 순간 막는다(파일이 아직 없을 때만 deny · 이미 있는 어긋난 파일은 경고로만 — 산 세션을 트랙 중간에 세우면 그게 F103 이다) + 회귀. 대가: 지문 꼴이 아닌 정당한 보드 파일(픽스처·유물)이 있으면 거짓양성 — 실저장소 분모가 79/80 이라 지금은 0이고, 픽스처는 `SYNK_BOARD_ROOT` 로 갈라져 있다. | |
