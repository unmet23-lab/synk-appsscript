| F495 | 2026-08-16 | 마찰 | 보드 파일 이름이 지문 꼴이 아니면 그 줄은 **주인 판정에서 통째로 빠지고, 그 방향은 「영구 잠금」이다.** 실물 1건: `docs/_ops/보드/local_008484b1.md` (접두 `local_` 을 안 뗀 이름 · 지금 그 세션은 살아 있다). `.claude/hooks/lib/board-id.js:파일지문()` 은 `^[0-9a-f]{8}\.md$` 만 인정하고 아니면 `''` 을 낸다 — 주석이 그 빈 문자열을 「주인 없음이지 내 것이 아니다」로 정확히 경고해 뒀는데, **소비자 쪽에서 그 값이 「죽지 않았다」로 접힌다**: `board-guard.js:원칙7처방()` 이 `if (!지문) return false` 로 그 줄을 죽은-활성 목록에서 빼고, 같은 자를 쓰는 상한 셈(F372 죽은 주인 빼기)도 산 것으로 센다. 그래서 지금은 「맞는 답을 틀린 이유로」 내고 있다 — 그 세션이 살아 있으니 결과가 우연히 맞는다. 🔴 그 세션이 죽는 순간 값이 뒤집히는 게 아니라 **아무것도 안 바뀐다**: 여전히 산 것으로 세어져 죽은 줄 목록에 영영 안 뜨고, `원칙7처방` 이 내미는 board-move 명령에도 안 실린다. 그러면 F073(남의 줄 편집 금지)·원칙⑦(상태 칸이 끝났다고 안 말한다)·`보드수거`(완료행만 본다) 셋이 그대로 걸려 **아무도 못 치우는 줄**이 된다 — #Q93 이 적은 병인데 경로가 다르다(#Q93 의 11건은 적어도 처방 목록에 «뜨기라도» 한다). 🔑 급소는 등록층이다(CLAUDE.md 「가드는 로직보다 등록층에서 샌다 · 새는 방향은 언제나 통과」): board-guard 는 「내 줄은 내 파일에만」을 기계로 강제하면서 **그 파일 이름이 지문 꼴인지는 한 번도 안 본다.** 실측 분모 = 보드 폴더 80파일 중 79가 8자리 hex, 어긋난 것 1(=이 건). ▶처방 = 만드는 순간 막는다(파일이 아직 없을 때만 deny · 이미 있는 어긋난 파일은 경고로만 — 산 세션을 트랙 중간에 세우면 그게 F103 이다) + 회귀. 대가: 지문 꼴이 아닌 정당한 보드 파일(픽스처·유물)이 있으면 거짓양성 — 실저장소 분모가 79/80 이라 지금은 0이고, 픽스처는 `SYNK_BOARD_ROOT` 로 갈라져 있다. | ⏸ 판정 2026-08-16 `local_a92e1e36`(대기열 #Q93 트랙 · 커밋 `019b12d7`) — **절반은 기계로 닫았고, 나머지 절반은 «지금 안 짓는다»고 판정했다. 그래서 해소가 아니라 보류다.** ■ **닫은 것 = 「못 쟀다」가 제3칸으로 섰다.** `board-guard` ⑧-c 신설 — 보드 폴더에 이름이 지문 꼴이 아닌 파일이 있으면 **이름을 대고** 드러내고 `git mv` 처방까지 함께 낸다(비차단 알림). 신고문이 지목한 급소(빈 문자열이 「죽지 않았다」로 접혀 **아무도 그 사실을 모른다**)는 이 층에서 닫혔다. 회귀 2건 신설 · `tests/board-guard.test.js` 123/123 · **변이 4/4**(구멍 0 · 미적용 0 · 기준선 114건 초록) — M1 안 모은다 · M2 이름 뺀다 · M3 `git mv` 처방 뺀다 · M4 전부 못 잰 것으로 센다(거짓양성 방향까지 물렸다 · 그 변이는 #Q66 소음 회귀도 함께 빨갛게 했다). ■ **안 닫은 것 = 신고문이 적은 처방 「만드는 순간 막는다」 그 자체.** 실측으로 기각했다: ①**남의 회귀를 깬다** — `tests/보드폴더.test.js:127` 이 비지문 이름 `mine0002.md` 를 **「아직 없는 파일 + 통과 기대」**로 이 훅에 넣는다(F233 축 검사). 생성 deny 는 그 검사를 죽인다 — 가드가 남의 검사를 깨는 자리라 CLAUDE.md 맹점 ③ 정면이다 ②**빈도가 1이다** — 보드 80파일 중 어긋난 이름 1(79/80 이 지문 꼴). #Q92 가 같은 자리에서 세운 기준이 「1건짜리면 ㉢(안 한다)이 정답」이고 F473 도 같은 저울로 판정했다. 경량 원칙 4차 과녁이 「장치 개수」인 것도 같은 방향이다. ■ **그래서 무엇이 남았나(정직하게)** — 이름이 어긋난 줄을 **기계가 치우지는 못한다.** `board-move` 는 여전히 그 파일의 주인을 못 재고, `원칙7처방` 의 죽은 줄 목록에도 안 실린다. 달라진 것은 **그 사실이 조용하지 않다**는 것뿐이다: 알림이 파일명과 `git mv` 를 함께 내므로 사람·세션이 한 수로 고칠 수 있고, 고치고 나면 그 줄은 보통의 죽은 줄이 되어 기존 처방 통로에 합류한다. ■ **다시 여는 조건** = 지문 꼴이 아닌 보드 파일이 **2번째로** 생기는 날. 그때는 실수가 아니라 시스템 결함이라(CLAUDE.md 「같은 절차에서 2번째 = 시스템 결함」) 생성 deny 를 짓는 것이 맞고, 그 판에서는 픽스처 이름 6개(`mine0002`·`mine0003`·`peer0001`·`me00`·`sess`·`abc`)를 지문 꼴로 갈아 ①의 대가부터 없앤다. 그 순서를 여기 적어 두는 이유는 다음 사람이 같은 저울질을 처음부터 다시 하지 않게 하기 위해서다(F386). ■ **실물 1건은 안 건드렸다** — `docs/_ops/보드/local_008484b1.md` 의 주인은 지금 살아 있다(0분 전 편집). 트랙 중간에 이름을 갈면 그 세션이 자기 줄을 잃는다(F073·F103) — 그 세션 몫이고, 죽은 뒤에는 이제 알림이 매 세션 그 이름과 명령을 내민다. |
