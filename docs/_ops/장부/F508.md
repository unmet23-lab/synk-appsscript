| F508 | 2026-08-16 | 실수 | 멈춘 «사실심문» 런이 `--런목록` 을 통째로 죽인다 — 그래서 이어받기 처방이 «빈 명령»으로 나간다  실측 2026-08-16 (`local_9fd726ac` · 배포빚 트랙 중 곁가지로 밟았다):  `node tools/codex-review.js --런목록` 이 **exit 2 + `Cannot read properties of undefined (reading 'join')`** 로 죽는다. 재현 100%.  원인: `tools/codex-review.js:1790` 이 멈춘 런마다 `x.런.인자.join(' ')` 로 이어받기 명령을 조립한다. 그런데 «사실심문» 런의 장부(`docs/_ops/사실심문기록.jsonl`)는 칸이 `때·일·런ID·작업id·라벨·질문` 뿐이고 **`인자` 칸이 아예 없다** — 검수·심문 런에만 있는 칸을 «전 런종에 있다»고 가정했다. 런종마다 장부 스키마가 다른데 한 코드가 하나로 읽는다.  새는 방향은 «통과»가 아니라 «폭발»이라 F044 계열은 아니다. 대신 두 갈래로 샌다:  ① `--런목록` 이 죽으면 **그 뒤에 올 다른 런이 한 줄도 안 보인다** — 멈춘 런 하나가 목록 전체를 가린다.    지금 이 저장소엔 「완주했는데 안 주운 런 1건」이 그 뒤에 깔려 있었다.  ② SessionStart 훅은 안 죽고 대신 「이어받기: `node tools/codex-review.js `」 를 **인자 없이** 찍는다.    그대로 붙여 넣으면 이어받기가 아니라 **새 검수가 처음부터 돈다**. 게다가 그 줄은    「↩ 이어받기는 **처음부터 다시가 아니다**」라고 스스로 약속한 바로 아래에 붙는다.    따를 수 없는 처방이라 F103 가족이다. 실측: 이 세션 시작 브리핑에 그 빈 명령이 그대로 떴다.  **지금 안 고친다** — 내 트랙(배포빚)이 아니고 1번째다. 기록만 남긴다 (「알고도 두지 마라」 = 미루기는 되고 **기록 없이 넘어가는 것만** 금지).  고칠 때 잴 것 셋: ㉠ 인자 없는 런의 이어받기는 «명령»이 아니라 «못 준다 + 왜»로 낸다 — 빈 명령을 찍느니 안 찍는다.    (`(x.런.인자 || []).join(' ')` 식 방어는 **오답**이다: 빈 명령이 그대로 살아남아 ②가 안 닫힌다.) ㉡ 회귀는 픽스처로 진다 — `인자` 칸 없는 런 레코드 하나를 넣고 `--런목록` 이 exit 0 으로    살아남는지, 그리고 그 줄에 «빈 명령»이 안 찍히는지 둘 다 본다(실저장소는 거짓양성만 검사). ㉢ 같은 가정(런종마다 다른 장부 스키마를 한 코드가 하나로 읽는다)이 다른 자리에도 있는지 훑는다 —    장부가 셋(`심문기록`·`사실심문기록`·검수)인데 읽는 코드가 한 벌이면 이 자리가 유일할 리 없다. | |
