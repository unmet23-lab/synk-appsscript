# AI 계정 정리와 Talk 품질 재검토 · 2026-09-11

> 최신 후속: **Apps Script 표준 전환·실제 Opus 5 응답과 Talk 운영/리허설 Anthropic 결제 조직 통일·각 1회 실제 Sonnet 5 응답까지 완료**. 전체 자동화 조사에서는 14개 기대/13개 등록, 시간 초과 2건, 월간 발송 재시도 결함 2곳을 발견했다. 현재 결과·미확인은 [자동화·키 통일 검증](AI_자동화_키통일_20260911.md)을 따른다. 아래 과거 관측·품질 비교와 구분한다.

## 결론

현재 운영 모델은 Talk 받아쓰기 Whisper-1, 교정 Sonnet 5를 유지한다. 품질 문제가 전혀 없다는 인증이 아니라, SYNK의 학습자 음성·한국어 교정·몽골어 해설에서 새 모델의 확실한 우월성을 아직 측정하지 못했기 때문이다. 모델 교체 전에 확인된 신뢰도·교정 태그의 실제 결함을 먼저 수리했다.

사용자 확정은 Anthropic API 결제 계정 단일화다. 실제 선택 계정·조직 ID·잔액은 저장소 밖 개인 런타임 설정에만 기록한다. 이 기록은 계정 선택과 관측일을 남기는 자료이며 API 키를 물리적으로 가두는 기능이 아니다.

## 계정·구독 확인

| 항목 | 실제 확인 | 남은 일 |
|---|---|---|
| Anthropic 지정 결제 조직 | 사용자 충전 완료 보고 뒤 Console의 유료 충전·양수 잔액 확인. 자동 충전 꺼짐 유지 | 충전 장애 해소. 에이전트는 구매하지 않음. 제품별 키 소속 증명은 별도 |
| Apps Script Claude 키 | 사용자가 새 `synk-appsscript` 키를 입력·저장. 재조회 후 저장 유지, 지정 계정 생성 키의 앞 16·뒤 4 식별자 일치·외곽 공백 없음. 표준 전환 후 고정 배포 48의 합성 요청 1회에서 실제 Opus 5·HTTP 200·정답 일치 | 통신 복구 완료. 전체 키 동일성·API 응답 조직 헤더·청구 명세 대조는 아님. 과거 Google 403과 후속 성공을 구분 |
| Talk Anthropic 시크릿 | 후속 승인으로 지정 조직에서 운영/리허설 전용 키 각각 발급·저장. 전체 전달값·저장 SHA-256 대조와 실제 Sonnet 5 단회 응답 각각 성공 | 최종 청구 명세 대조·옛 키 소비자 조사·최신 품질판 운영 배포는 별도. 과거 기존 키 E01/E02 품질 1/2 결과를 새 키 연결 검증으로 덮지 않음 |
| Claude Code 구독 | 현재 auth status: claude.ai / firstParty / Max, 기본 Opus 5/xhigh. 개인·프로젝트 설정과 Process/User/Machine 환경에 API-key·Vertex·Bedrock 자동 전환 설정 없음 | API 계정 통일 때문에 기존 유료 구독 로그인을 강제로 바꾸지 않음. CLI 계정의 서버 초과 사용 설정은 여전히 미확인 |
| Codex 개인 설정 | 현재 Astra/max, forced_login_method=chatgpt | 이전 Luna/medium 관측 갱신. 이번 후속에서 사용자 모델 설정을 덮어쓰지 않음 |
| Vertex | 앞선 명시 무료 체험 계정·프로젝트의 실제 호출 성공 유지 | 신규 Transcribe 모델이 같은 크레딧 대상인지는 별도 확인 필요. Gemini 정액제와 Vertex는 서로 다른 통로 |

지정 계정 잔액 확보와 Apps Script 연결에 이어 Talk 운영·리허설 키 통일도 완료했다. 새 키 발급·저장·실제 호출의 후속 증거는 [키 통일 결과](AI_자동화_키통일_20260911.md)에 있다. 원래 키의 다른 소비자를 확인하기 전 일괄 폐기하지 않는다. OpenAI 받아쓰기 키는 별도 공급자로 이번에 바꾸지 않았다.

## 이번에 고친 품질 결함

1. 받아쓰기: `Number(null)` 등 암묵 변환 때문에 미제공 확률이 숫자 0으로 바뀌고, `exp(0)`으로 확실한 결과처럼 계산될 수 있었다. segment JSON 확률은 실제 유한 숫자만 받고 null·빈 문자열·boolean 등을 미측정으로 남긴다. 진짜 숫자 0의 의미는 보존한다.
2. 검수 확정: 같은 미측정 값을 낮은 신뢰도 숫자로 잘못 처리하던 소비자를 맞춘다. 기존 최소 청취·실제 재생 기록 게이트는 유지한다.
3. 교정: `오류없음`과 실제 오류 태그가 섞인 응답은 상충 판정으로 거부한다. 부분적으로 태그를 지워 정상 응답으로 꾸미지 않는다.
4. 평가: 기대 오류 태그를 포함했다는 이유로 `미검출=true`와 `통과=true`가 동시에 서던 모순을 막는다. 기존 20문항 보관 출력 4벌의 18/20 점수는 모두 그대로다.
5. 검수 화면: scalar `stt_confidence`의 null·빈칸·boolean은 기존 ‘전사 신뢰도 미측정’ 문구로 표시한다. 이 scalar는 DB가 십진 숫자 문자열로 반환할 수 있어 `"0.000"`은 정상 표시하고 자릿수를 보존한다. segment JSON 확률의 실제 숫자 계약과 구분한다. `synk-design` 기준에 따라 기존 스타일·배치를 유지했으며 로컬 렌더 107/107과 독립 합성 19/19를 확인했다. 실기기·라이브 화면의 픽셀 검증은 하지 않았다.

신뢰도는 음성 인식 토큰 확률에서 나온 참고값이며 실제 정답률·발음 점수가 아니다. 과거 DB에 잘못 저장된 값은 이번 코드 변경으로 소급 복구하지 않았다.

STT 4파일 독립 검토에서 새 P0/P1 회귀 없음, 관련 61/61 통과. STT 확장 회귀 175/175, 교정 태그 관련 91/91. 이 숫자는 서로 겹치는 시험을 포함하므로 합쳐 총 시험 수로 제시하지 않는다. 통합·리허설 최종 상태는 아래 실행 기록에 별도로 갱신한다.

## 받아쓰기: 유지 이유와 후보

| 모델 | 공식 단가 | 1,000분 예시 | 판단 |
|---|---:|---:|---|
| Whisper-1 | US$0.006/분 | US$6 | 현행 유지. 현재 구간 시작·끝·평균 로그 확률 계약과 맞음 |
| gpt-transcribe | US$0.0045/분 | US$4.50 | 우선 비교 후보. 25% 저렴하지만 구간 타임스탬프와 현재 확률 계약의 대체 불충분 |
| Gemini 3.5 Transcribe | 오디오 입력 US$2/M + 텍스트 출력 US$12/M | 약 US$5.10 | 공식 예시 토큰량으로 계산한 추정. verbatim·단어 시간 유망, 실제 학습자 품질과 Vertex 크레딧 적용 미확인 |

[OpenAI 전사 가격](https://developers.openai.com/api/docs/pricing#transcription-and-speech), [Gemini 전사 가격](https://ai.google.dev/gemini-api/docs/pricing#gemini-3.5-transcribe).

`gpt-transcribe`는 공식 신규 권장 모델이지만 현재 Whisper용 `verbose_json`을 그대로 보낼 수 없다. `language`도 `languages[]`로 바뀌며 타임스탬프를 제공하지 않는다. 현재 코드의 400/422 영구 실패 분류와 구간 검수까지 포함해 별도 어댑터가 필요하다. [공식 전환 안내](https://developers.openai.com/cookbook/examples/migrating_from_whisper_to_gpt_transcribe), [전사 가이드](https://developers.openai.com/api/docs/guides/speech-to-text#timestamps).

Gemini는 학생이 한 실수·반복을 보존하는 `verbatim` 방식만 후보로 삼는다. `smart` 방식의 문법 정리·말 고침 제거는 한국어 학습자의 실제 오류를 지울 수 있다. 단어 타임스탬프는 정확도에 영향을 줄 수 있고 Cloud 버전은 Preview이며 크레딧 적격성도 확인되지 않았다. 무료 AI Studio 경로에 학생 음성을 보내지 않는다. [Gemini 가이드](https://ai.google.dev/gemini-api/docs/transcribe), [Cloud 모델 상태](https://docs.cloud.google.com/gemini-enterprise-agent-platform/models/gemini/3-5-transcribe).

저장소의 30개 합성 대본과 비교 도구만으로는 정확도를 측정할 수 없다. 대응 평가 음성이 없으며, 읽으라고 준 대본과 실제로 발화한 내용도 같다고 가정하면 안 된다. 아래 조건의 작은 비교시험을 먼저 권한다.

- 동의받은 비식별 음성 또는 별도 시험 음성, 사람이 실제 들은 그대로 적은 정답.
- 몽골인 한국어 발음·조사/어미 실수·한몽 혼용·반복·자기 수정·침묵/소음 포함.
- 문자 오류율뿐 아니라 학생 오류 보존, 없는 말 생성, 구간 시간, 미측정 처리, 지연을 따로 측정.
- 원본 음성과 사람이 확정한 transcript_verified 보존. 기존 시험 도구가 영구 행·암호·대기열을 바꾸는 경로는 이 평가에 사용하지 않음.

1,000분에서 월 US$1.50 차이이므로 지금은 비용보다 학습 오류를 보존하고 선생님이 정확한 구간을 들을 수 있는지가 더 중요하다.

## 교정: 유지 이유와 후보

| 모델 | 입력/출력 100만 토큰당 USD | 1,000건 단순 예시 | 권고 |
|---|---:|---:|---|
| Sonnet 5 | 2 / 10 | 9.00 | 운영 유지. 구조화 응답과 기존 검수 계약 유지 |
| Opus 5 | 5 / 25 | 22.50 | 품질 향상 우선 비교 후보. 문맥·의도 보존·몽골어 해설을 함께 대조 |
| Gemini 3.1 Pro | 2 / 12 | 10.00 | 기존 구독 평가 후보. 제품 API와 구독 묶음 응답을 같은 성적으로 취급하지 않음 |
| Gemini 3.8 Flash | 0.75 / 3.75 | 3.375 | Vertex 크레딧으로 비교할 비용 후보. 교체 우월성 미확인 |
| Fable 5.1 / GPT-6 Astra | 10 / 50 | 45.00 | 비싸다는 사실이 이 업무의 우월성 증거는 아님. Fable은 사고 설정도 현재 요청과 달라 단순 치환 불가 |

예시는 건당 입력 2,000 + 가시 출력 500토큰, 사고·캐시·배치·세금 제외다. 실제 비용이나 예산 상한이 아니다. 사고 출력 1,000토큰을 추가하면 Sonnet은 US$19/1,000건, Opus는 US$47.50/1,000건이 된다. Sonnet의 9월 가격 인상 예정 정보는 취소됐으므로 현재 공식 US$2/10을 사용했다. Flash 단가는 현재 공식 한시 가격으로 이후 재확인해야 한다. [Anthropic 가격](https://platform.claude.com/docs/en/about-claude/pricing), [Google 가격](https://cloud.google.com/gemini-enterprise-agent-platform/generative-ai/pricing), [Astra](https://developers.openai.com/api/docs/models/gpt-6-astra), [Fable 5.1](https://platform.claude.com/docs/en/models/fable-5-1/overview).

개선 우선순위는 모델명보다 다음 세 가지다.

1. 맞는 문장을 불필요하게 고치지 않고 학생 의도를 보존하는지 측정한다. 현재 엄격 문자열 정답에는 ‘제가/저는’처럼 문맥상 둘 다 가능한 답을 오답 처리할 여지가 있다.
2. 교정 문장·오류 태그·몽골어 설명을 각각 평가한다. 현재 몽골어 검사 통과는 문자/존재 조건이지 해설 의미가 맞다는 증거가 아니다.
3. 기존 합성 102문항(23태그)과 문맥 최소쌍을 동일 단건 요청으로 Sonnet/Opus에 대조한다. Sonnet 사고량도 대조해 더 비싼 모델만의 이득인지 분리한다. 몽골어 의미는 블라인드 사람 검토가 필요하다.

이 비교에서 반복적으로 분명한 우월성이 나올 때 Opus 전환 또는 어려운 사례의 선별 재검토를 권한다. 현재 18/20×4는 Gemini 구독 모델에 같은 20문항을 묶어 보낸 선별 결과로, Sonnet/Opus 제품 API 간 비교 성적이 아니다. 잔액 부족 무응답도 모델의 교정 오류율로 세지 않는다.

## 역할 장부 정정

`f7045e761` 원격 반영. 고유 역할 36개·가동 표기 28·미배선 8이라는 수는 유지하되 실제 부분 배선을 정확히 적었다. 과제 생성 큐, Apps Script 칭찬/미션 저장, 실제 오류 태그 소비, 학습 목적/활용 맥락 반입이 존재한다. 역할 전체의 자동 완료까지 증명되지 않은 상태를 ‘코드 없음’으로 설명하지 않는다. 채점·발음·성향·일기 등 새 제품 계약을 임의로 발명해 가동 수만 올리지 않았다.

## 실행 기록

- Talk 소스: `22d9804` STT 미측정 보존, `d7fab09` 교정 상충 태그 거부, `30736cf` 신뢰도 표시. 격리 worktree에서 자기 변경만 통합.
- 통합 회귀 19파일 353/353 통과. 최종 `30736cf`의 서버 본체·동봉 입력 77파일은 시험판 `d7fab09`와 바이트 동일.
- 리허설 8함수 배포 완료: events v81, transcribe v36, review v39, teach v52, correct v64, companion v46, deliver v105, deliver-one v50 모두 ACTIVE.
- 사후 배포대조 8/8 ‘같다’. 동봉 모듈 140/140 원문 바이트 일치, 변환되는 TypeScript 본체는 기존 도구의 주석 앵커·배포 시각을 대조했다. 본체 전체 바이트 일치나 실제 API 성공으로 확대하지 않는다.
- 기존 배포장부 615행 바이트·SHA-256 보존, 자기 배포 8행만 추가. UI 표시 수정은 소스·로컬 렌더 검증이며 앱 배포 완료가 아니다.
- Talk 소스 3커밋과 실행증거 `a61feea`를 origin/master에 push 완료. 별도 보고는 Talk의 docs/_ops/Talk_AI품질수리_20260911.md. 두 저장소의 다른 담당 작업본·staging을 자동 덮어쓰지 않음.
- 충전 후 최소 원격 시험: 기존 리허설 키, `correct?평가=1`, 합성 E01/E02만 실행. 요청 묶음 1회·Messages 2건·추가 재시도 0. 두 항목 모두 실제 반환 `claude-sonnet-5`, HTTP 200, 사유·검증사유 없음. 학생 데이터·대기열·운영 호출 없음.
- 위 시험의 고정 품질 채점은 **1/2**다. E01은 `제가 몽골 사람입니다.`/`맞춤법:기타`를 반환해 `저는` 포함·주격 조사 태그를 요구하는 시험지와 달랐다. 기대값을 바꾸거나 성공할 때까지 재호출하지 않았다. API 복구와 교정·태그·몽골어 해설 품질 승인은 별개다. 근거: Talk 격리 사본 `evals/출력_v7q1_billing_smoke_20260911.json`(실행일 필드는 UTC, 작업일은 한국 시간 9월 11일).
- Apps Script 키 저장 후 `aiText_`를 합성 입력·출력 상한 128로 `clasp run` 1회 시도했으나 Google 실행 API 권한 오류(403). CLI 종료값 0을 성공으로 세지 않는다. 재시도·추가 Anthropic 성공 응답 없음. 호출 전 리허설 만료 속성 부재를 확인했다.
- **전환 전 관측:** Apps Script Cloud 연결은 `기본값`이고, clasp 기본 로그인은 지정 계정이었다. 당시 기본 프로젝트는 공식 `scripts.run`의 동일 표준 프로젝트 요건을 충족하지 못했다. [Google 실행 요구사항](https://developers.google.com/apps-script/api/how-tos/execute). 후속 승인으로 표준 연결·PC 전용 OAuth 로그인·원격 실행을 완료했다.
- `상담AI_점검` 대체 실행은 하지 않음: 고정 3문항이어도 실제 상담로그 조회·진단카운터·로그 영구 기록이 있어 순수 합성 단건 검증과 다르다. 전환 전에는 새 helper·배포도 하지 않았고, 이후 소유자 전용 고정 점검만 별도로 구현·배포했다. **이 단계 당시** Talk 시크릿 변경은 미실행이었으며, 이후 별도 승인으로 통일·연결 확인을 완료했다. 기존 키 폐기는 하지 않았다.
- **전환 전 보류와 후속 해소:** 기본 프로젝트 삭제·기본 복귀 불가·재승인 영향을 사용자에게 설명했고, 이후 “사전 점검후 표준형으로 가자”라는 명시 승인을 받았다. 실제 실행 계정에서 트리거 13개 모두 소유 확인 후 표준 연결했다. [Google 전환 영향](https://developers.google.com/apps-script/guides/cloud-platform-projects).
- **후속 실제 변경:** 전용 표준 프로젝트·OAuth Production 설정·Drive/Apps Script API 활성화·Cloud 연결 완료. 생성 시 자동 연결된 결제는 새 프로젝트에서만 해제하고 재조회했다. 기존 고정 배포에 API executable ‘나만’이 있음을 UI로 확인했다. 전환 전 46 → 다른 동시 작업 47 → 이번 소유자 점검 48로 같은 배포 ID를 갱신했고 실행 계정·접근 범위는 보존됐다. 키 수정·트리거 재생성은 하지 않았다.
- **후속 코드·실호출:** `dea2bf791`의 [원격 CI 34527494194](https://github.com/unmet23-lab/synk-appsscript/actions/runs/34527494194) 성공 후 점검 파일·호출 수 주석만 추가했다. HEAD·고정 48 각각 배포 자료 23/23 원문 바이트 동일. `aiConnectionCheck --nondev` 1회는 HTTP 200, 실제 `claude-opus-5`, 정답 일치, 입력 42·출력 18·캐시 0이었다. 학생 데이터·재시도 없음. 이 결과는 연결 확인이며 학습자 교정 품질 비교·청구 조직 헤더 증명이 아니다. 상세 증거와 잔여는 [전환 결과](AppsScript_표준Cloud_전환_20260911.md)에 기록했다.
- 전체 초기 최적화·원격 CI·Vertex 연결 증거는 [기존 보고](AI_최적화_20260911.md)에 보존.
- 후속 원격 통합 `8313ab602`의 [CI 34510104370](https://github.com/unmet23-lab/synk-appsscript/actions/runs/34510104370) 성공 확인. 이 커밋에는 다른 엔진·홈페이지 작업도 포함되므로 그 변경을 본 작업의 구현으로 주장하지 않는다. 본 후속 문서·결정 파서/모델 정책 검사 79/79 통과.
