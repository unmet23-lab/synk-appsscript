# 엔진 7종 상향 설계 — 인계문 (2026-09-02~03 · 세션 85b643a8 → 8e3e0911 → 다음 세션)

> **이 폴더는 «작업 중» 폴더다.** 산출물(`docs/엔진7종_상향설계_v1.md`)이 서면 **그 커밋에서 이 폴더를 통째로 지운다**
> (CLAUDE.md 「낡은 것은 남기지 않는다」 — 재료는 git 이력이 보존한다). 지우기 전까지는 이어 하는 세션의 유일한 입구다.
>
> **유호님 원지시(09-02)**: 「철학정본을 전부 훑어보고 지금 7종의 모든 엔진 시스템을 최신정보에 기반해서
> **한차원 업그레이드 된 시스템 설계**로 발전시켜줘. 다시한번 말하지만 **기회비용 생각하지 마**」
> 뒤이어 「A단 끝나면 B단·C단 바로 이어서 돌려줘」 · 「지금까지 했던 작업 절대 빠뜨리지말고 전부 인계할수있도록 해줘」.
>
> **왜 또 인계하나** — 09-03 05:38 에 **여섯 번째로 한도**(모델을 몇 시간 안에 얼마나 쓸 수 있는지의 상한)에 닿았다.
> 이번엔 여섯 벌 중 넷이 살아 파일로 남았고, 죽은 것은 둘(엔진 하나 = 서버 오류 · 통합 하나 = 한도)뿐이다.
> **남은 것은 세 걸음뿐이다 — 수리 둘 · 집필 하나.**

---

## 0. 이어 하는 세션이 첫 10분에 할 일

1. **이 파일을 끝까지 읽는다**(5분). 철학 정본 95KB 를 다시 읽을 필요는 없다 — `재료/철학.json` 이 그 독해다.
2. **세팅** = **유호님이 고르신 모델 + 울트라코드 켬**(여러 갈래 일을 한꺼번에 돌리는 모드). 본세션 추론 강도는 기본(high)으로 둔다 —
   에이전트별 강도는 스크립트가 이미 정한다(설계·반박·수리 = high · 통합·집필 = max).
   본세션을 max 로 올리면 같은 창의 토큰을 오케스트레이션(일 나눠주는 몫)이 먹어 에이전트 몫이 준다.
   ※ 지금까지 전 단계가 **Fable 5.1** 로 돌았다(사실 기록 · 모델 픽은 유호님 몫이다).
3. **§4-1 로 «지금 어디까지 왔나»를 파일로 확인**한다(이 문서의 표가 아니라 `재료/` 폴더가 정본이다).
4. **§3 의 남은 명령**을 위에서부터 돌린다. 단계마다 결과를 `재료/` 로 나눠 저장하고, 한도에 걸리면 «빠진 것만» 다시 돈다.

---

## 1. 무엇을 어디까지 했나 (전부 파일로 있다 · 다시 하지 않는다)

| 단계 | 내용 | 결과 파일(`재료/`) | 상태 |
|---|---|---|---|
| 1차 독해 | 엔진별 현행 설계·코드·트랙 독해 7 + 철학 대조 1 + 횡단(유호 확정·계약·래칫·큐) 1 | `<엔진>_독해.json` ×7 · `철학.json` · `횡단.json` | ✅ 09-02 05:17 |
| 1차 정찰 | 2026-09 기준 웹 정찰(엔진별 33~58회 검색 · 게이트 3 통과분만 · 출처 URL·날짜) 7 + 공통 AI 스택 1 + 세계 1등 대조 1 | `<엔진>_정찰.json` ×7 · `공통스택.json` · `세계1등.json` | ✅ 05:17 |
| A 설계 | 엔진별 렌즈 셋(A 「AI가 세질수록 세지는 구조」 · B 「학생 하루·명품」 · C 「세계 1등 대조·종류 변화」) 독립 설계 21안 | `설계_<엔진>.json`(배열) | ✅ |
| A 심사 | 엔진별 심사 2(철학·차원 우선 / 실현·명품·세로줄 우선 · 7축 채점 → winner·graft·fatal_flaws) 14 | `심사_<엔진>.json`(배열 2) | ✅ |
| A 종합 | 엔진별 종합 설계 1 = 7(승자 뼈대 + 이식 + 치명 결함 수리 · 상향 항목 6~9개씩) | `종합_<엔진>.json` ×7 | ✅ 09-02 19:01 |
| B1 반박 | 종합마다 반박 4갈래(철학 충돌 · 실현성 · 진짜 한 차원인가 · 유호 확정 충돌·실물 낡음) = 28 | `반박_<엔진>.json`(배열 4) | ✅ 09-02 22:47 |
| B1 통합 | 일곱을 한 시스템으로(세로줄 v2 · 공유 계약 23 · 충돌 해소 19 · 순서 20걸음 · Atlas 정의 · 유호 판정 18 · 걷어냄 9 · 자 33) | `통합_수리전.json` | ✅ 09-02 23:17 |
| B1 통합 반박 | 통합 설계 반박 4갈래 | `반박_통합.json`(배열 4) | ✅ 09-03 04:28 |
| **B2a 엔진 수리** | 엔진별 수리 = 반박 4벌을 고치거나 근거로 기각(change_log) | `수리_<엔진>.json` | 🟡 **6/7**(09-03 05:38) — **남은 하나 = Loom** |
| **B2a 통합 수리** | 통합 설계를 반박 4벌로 수리 | `통합.json` | ⬜ **0/1**(두 번 다 한도로 죽었다 · §5) |
| ~~B2b 재반박~~ | 수리본 재반박 2갈래×7 | — | 🚫 **뺀다**(09-03 판정 · §3-1) — 그 몫은 집필 비평 ⑧ «실물 대조»가 진다 |
| C 집필 | 정본 문서 집필 → 완결 비평 → 수리(최대 3회전) | **`docs/엔진7종_상향설계_v1.md`** | ⬜ 남음 |
| 마감 | 사람 검토 · 트랙 §1 줄 갱신 · **이 폴더 삭제** · 커밋·푸시 | — | ⬜ 남음 |

**수리가 실제로 한 일**(자 = `수리_<엔진>.json` 의 `change_log` 길이 · 09-03 05:40 실측):

| 엔진 | 고친 곳 | 근거 대고 기각 | 상향 항목 |
|---|---|---|---|
| Core | 53 | 4 | 9 |
| Vellum | 46 | 2 | 8 |
| Prism | 41 | 5 | 9 |
| Trail | 39 | 2 | 8 |
| Reed | 34 | 2 | 9 |
| Temper | 32 | 4 | 7 |
| **Loom** | — | — | **아직 안 돌았다**(반박은 4벌 다 있다 · 치명 2 · 중대 35) |

**태운 값**: 서브에이전트 토큰 합계 ≈ **30.3M**(1차 4.3 · A 9.0+1.5+0.3 · B1 5.8+4.7 · B2a 3.8 + fill 1.3).
한도에 여섯 번 걸렸지만 **「처음부터」는 한 번도 없었다** — 끝난 것은 그때마다 파일로 남겼다.

---

## 2. 재료 파일 읽는 법 (스키마)

- `<엔진>_독해.json` — 현행 지도: `current_state`(돈다/짓고 있다/없다) · `principles` · `inherited_constraints`(🚫의 «기술적 이유» — 승계 대상) · `open_gaps` · `philosophy_served/unserved` · `numbers`(자·날짜 동반) · `yuho_confirmed` · `upgrade_seeds` · `files_read`.
- `<엔진>_정찰.json` — `findings`(이름·날짜·URL·근거·게이트 ㉠㉡㉢·효력 시점·비용·확신도) · `rejected` · `ceiling_reference`(세계 최상 · URL).
- `철학.json` — `rulers`(자 21) · `prohibitions` · `gates` · `three_layers` · `vertical_line` · `engine_to_clause_matrix` · `clauses_no_engine_serves`(주인 없던 조항 9) · 🔑 **`one_dimension_up_definition`**(여섯 축 — 이 설계 전체의 판정문).
- `횡단.json` — `yuho_confirmed_engine_wide` 16(다시 여는 조건 포함) · `cross_engine_gaps` 12 · `open_queue_items` · `asset_layer_rules` · `contracts_and_ratchets` · `measured_state`.
- `종합_<엔진>.json` / `수리_<엔진>.json` 의 `design` — DESIGN 스키마: `thesis` · `kind_before/kind_after` · `architecture`(layers·inputs·outputs·stores) · `upgrades`[]( 이름 · 무엇을(장면→구조) · 왜 한 차원인가 · 복무 조항 · 정찰 근거 · **걷어낼 것** · 제약 처리 · 대가 · 닫을 것 하나 · **자** · 효력 시점 · 닫히는 문 · 유호 질문) · `io_with_other_engines` · `what_not_to_do` · `build_order` · `open_yuho_decisions`.
- `반박_<엔진>.json` — 배열 4: `refuted`[](item · severity 치명/중대/경미 · why · **evidence 파일:줄** · fix) · `kept` · `overall`.
- `수리_<엔진>.json` — `design`(수리된 «전체») · `change_log` · `rejected_refutations`(근거 있는 기각).
- `통합_수리전.json` / `통합.json` — SYSTEM 스키마: `thesis` · `vertical_line_v2` · `shared_contracts` · `conflicts_resolved` · `engine_order` · `atlas_definition` · `what_changes_for_student_day` · `what_changes_for_yuho` · `measures` · `open_yuho_decisions` · `removed_list`.
- `설계_*` · `심사_*` — 과정 기록(종합의 근거). **집필엔 안 쓴다.**

---

## 3. 남은 명령 — 위에서부터 그대로

경로 = `C:/Users/q1212/Documents/SYNK-appsscript/docs/_ops/엔진7종_상향_인계` (아래 `$H`).
**스크립트의 재료 기본 경로는 이미 이 폴더를 가리킨다** — `args`(넘겨주는 값) 없이 그냥 돌리면 된다.
분배 스크립트(워크플로가 남긴 기록을 재료 파일로 나눠 담는 것)는 **cwd(지금 서 있는 폴더)의 `재료/` 에 쓴다** — 반드시 `$H` 에서 실행한다.
저널(워크플로가 에이전트마다 남긴 결과 기록) 경로 = 워크플로 결과 알림의 «Transcript dir» + `/journal.jsonl`.

```text
① 수리 마저 — «그냥 돌리면 되게» 미리 만들어 뒀다 (남은 엔진 하나 Loom + 통합 수리 하나)
   Workflow({ scriptPath: "$H/scripts/wf2b2af3_fill.js" })
   끝나면: cd $H && node scripts/분배2.js "<이 런의 저널>" B2
   ⚠ 분배는 파일을 덮어쓴다 — 이미 있는 수리 6벌은 이 런에 없으니 안 건드린다(이름이 달라 안전)
   ⚠ 이 런은 에이전트 둘뿐이라 ≈1M · 창 앞머리가 아니어도 든다

②-a 통합 수리만 또 죽었으면 — 그것 하나만 도는 스크립트가 따로 있다
   Workflow({ scriptPath: "$H/scripts/wf2b2ag_system.js" })
   🔴 이건 강도 max 하나짜리라 40~60분 걸리고 두 번 연속 한도로 죽었다 — **창 맨 앞에서 혼자 돌린다**
   끝나면 같은 분배 명령(B2)

②-b (건너뛴다) B2b 재반박 — 09-03 판정으로 뺐다. §3-1 이 근거다.
   되살리고 싶으면 scripts/wf2b2b_reverify.js 가 그대로 있다(ENGINES 를 좁혀 쓴다).

③ C — 집필 → 완결 비평 → 수리(최대 3회전)
   Workflow({ scriptPath: "$H/scripts/wf2c_write.js" })
   산출 = docs/엔진7종_상향설계_v1.md (집필 에이전트가 직접 Write 한다)
   ⚠ 이 단계가 가장 무겁다(≈2.5~3M) — 창 앞머리에서 시작한다
   ⚠ 이 스크립트는 통합.json 과 수리_<엔진>.json 일곱을 읽는다 — ① 이 끝나야 돈다

④ 사람 검토(본세션이 직접 읽는다)
   · §0 만 읽고 «무엇이 달라지는지 · 무엇을 정해야 하는지»가 보이나
   · 시제 §0(없는 것을 있다고 쓰지 않았나) · 숫자에 자·날짜가 있나
   · 「0건」과 「안 재봤다」를 갈랐나 · 유호 판정 자리가 정지선 넷만인가

⑤ 마감 커밋(범위를 못 박는다)
   git add -- docs/엔진7종_상향설계_v1.md docs/_ops/트랙.md
   git rm -r -q -- docs/_ops/엔진7종_상향_인계
   git commit -m "..." -- docs/엔진7종_상향설계_v1.md docs/_ops/트랙.md docs/_ops/엔진7종_상향_인계
   git push
   · 트랙 §1 의 「🔵 엔진 7종 상향 설계」 줄을 «완료 + 정본 경로 + 유호 판정 개수 + 첫 걸음 셋»으로 갈아끼운다
   · 유호님 판정거리가 있으면 node tools/결정.js 로 올리지 말고 «문서 §12» 로만 둔다(유호님이 정하시는 자리)

⑥ 기억: memory/workflow-stage-split-session-limit.md 에 C단 실측 한 줄 추가
```

**한도에 걸리면**(모델을 얼마나 쓸 수 있는지의 상한에 닿아 남은 에이전트가 전부 실패할 때):
알림의 실패 목록으로 «빠진 것만» 도는 fill 스크립트를 만든다 —
본보기 = `scripts/wf2a2_fill.js` · `scripts/wf2b1f_fill.js` · `scripts/wf2b2af_fill.js` · `scripts/wf2b2af3_fill.js`.
만드는 법 = 앞 스크립트의 **머리(스키마·프롬프트 정의)를 그대로 두고 맨 끝 실행 블록만 갈아끼운다**(`TODO` 배열을 빠진 것만으로).
🔴 **한도로 죽은 에이전트는 «산출 0»으로 죽는다** — 자료를 다 읽고 맨 끝에 한 번에 쓰기 때문이다. 주울 반제품이 없다.


### 3-1. 🔴 남은 양과 «재반박을 뺀다»는 판정

**남은 토큰 ≈ 3.5~4M · 창 한 번**(재반박을 뺀 뒤). 자 = 지금까지 런들의 실측(에이전트 하나당 25~45만).

| 단계 | 에이전트 | 예상 |
|---|---|---|
| 수리 마저(Loom 1 + 통합 1) | 2 | ≈1M |
| 집필 C(집필 1 + 비평·수리 2~4) | 3~5 | ≈2.5~3M |

#### 🚫 재반박(B2b)을 뺀 근거 넷 — 유호님 물음 「의미가 있을까 걱정이야」에 답한 것

1. **첫 반박의 수확이 컸고 수리가 그것을 다 먹었다.** 첫 반박 = 치명 32 · 중대 240.
   수리가 실제로 고친 양 = 여섯 엔진 합쳐 **245곳**(Core 53 · Vellum 46 · Prism 41 · Trail 39 · Reed 34 · Temper 32 · 근거 대고 기각은 19건뿐 · 기각도 파일:줄 근거를 댔다).
   보기: 「TOPIK 성적을 학생 앱 카드로 받는다」는 안이 «강사가 넣는 자리»라는 실물 확인으로 통째로 뒤집혔다.
2. **집필 단계에 이미 둘째 그물이 있다** — 완결 비평이 최대 3회전 돌며 철학 조항 전량·시제·빠짐·읽힘을 다시 본다.
3. **이 산출물은 코드가 아니라 «유호님이 보고 정하실 제안서»다** — 사람 관문이 뒤에 하나 더 있다.
4. **값이 안 맞는다** — 재반박 5~6M 은 남은 예산보다 큰데, 그 돈은 유호님이 실제로 읽는 «문서 자체»에 쓰는 편이 낫다.

#### 대신 넣은 것 (0원)

집필 비평 검사 축에 **⑧ «실물 대조»**를 넣었다(`scripts/wf2c_write.js`) —
문서가 「있다」·「없다」·「0건」·「코드 0」이라 말한 실물을 저장소에서 최소 15곳 표본으로 grep(글자 찾기) 해 확인한다.
**재반박이 잡았을 딱 그 실패**(수리가 크게 고치면서 새로 들어온 «없는 실물»)를 이것이 대신 잡는다.

⚠ 유호님이 「그래도 재반박 하자」고 하시면 `scripts/wf2b2b_reverify.js` 가 그대로 있다.
치명이 많던 셋(Core 9 · Vellum 7 · Trail 5)에만 걸면 ≈2.5M 이다.

**첫 반박이 잡은 것**(수리가 처리하는 재료): Core 치명 9·중대 42 / Vellum 7·38 / Trail 5·32 / Prism 4·34 / Temper 3·32 / **Loom 2·35** / Reed 2·27.

---

## 4. 런 장부

저널 위치는 **세션마다 폴더가 다르다** — `C:/Users/q1212/.claude/projects/C--Users-q1212-Documents-SYNK-appsscript/<세션ID>/subagents/workflows/<런>/journal.jsonl`.
(이 기계 · 그 세션 폴더에만 있다 — **다른 기계·다른 계정이면 없다. 그땐 `재료/` 만 믿는다.**)

| 런 | 세션 | 무엇 | 저널 | 결과 |
|---|---|---|---|---|
| 1차 | 85b643a8 | 독해 9 · 정찰 9 | `wf_efa03659-963` | 18/18 ✅ |
| A | 85b643a8 | 설계 21 · 심사 14 · 종합 3(Core·Vellum·Trail) | `wf_43bf49bf-771` | 35/42 (한도) |
| A2 | 85b643a8 | 심사 3 · 종합 3(Prism·Temper·Reed) | `wf_abed6fb6-d7f` | 6/6 ✅ |
| A3 | 85b643a8 | 종합 Loom | `wf_ca322459-de9` | 1/1 ✅ |
| B1 | 85b643a8 | 반박 13 | `wf_ac14c07e-6a5` | 13/29 (한도) |
| B1-fill | 85b643a8 | 통합 1 · 반박 15 | `wf_ff9053dd-34a` | 16/16 ✅ |
| B2a | 85b643a8 | 수리 7 · 통합 반박 4 · 통합 수리 1 | `wf_ec0a2c87-1d2` | 2/12 (한도) |
| B2a-fill | 85b643a8 | 수리 7 · 통합 반박 2 · 통합 수리 1 | `wf_933911c2-e53` | 4/10 (한도 · Trail·Reed 수리 + 통합 반박 2) |
| **B2a-fill2** | **8e3e0911** | 수리 5 · 통합 수리 1 | `wf_768da2bf-f19` | **4/6** — 산 것 = Core·Vellum·Prism·Temper · **죽은 것 = Loom(서버 오류)·통합(한도)** · 1.28M · 59분 |

### 4-1. 🔴 지금 어디까지 왔나 — 파일로 확인한다 (이어 하는 세션의 첫 일)

```bash
cd C:/Users/q1212/Documents/SYNK-appsscript/docs/_ops/엔진7종_상향_인계 && ls 재료/수리_*.json 재료/통합.json
```

**09-03 05:40 현재 답**(다시 재보고 이 줄과 다르면 파일을 믿는다):
- `수리_*.json` **6벌**(Core · Prism · Reed · Temper · Trail · Vellum) — **없는 것 = 수리_Loom**
- `통합.json` **없음**

- **`수리_<엔진>.json` 7벌 + `통합.json` 이 다 있으면** → B2a 끝. §3 ③(집필)으로 간다.
- **빠진 것이 있으면** → §3 ①을 돌린다. 빠진 것의 재료는 `종합_Loom.json`(설계)과 `반박_Loom.json`(반박 4벌)이고 둘 다 이미 있다.
- **`통합.json` 만 없으면** → §3 ②-a 를 창 맨 앞에서 혼자 돌린다. 입력 = `통합_수리전.json` + `반박_통합.json`(4벌 다 있다).

---

## 5. 이 세션들이 배운 것 (같은 함정을 안 밟게)

- 🔴 **통합 수리(강도 max 하나짜리)가 «두 번 연속» 한도로 죽었다** — 09-02 23:40 · 09-03 05:38.
  다른 에이전트와 같이 띄우면 걔들이 먼저 창을 먹고, 이건 오래 걸려서(40~60분) 끝에 걸린다.
  **처방 = 창 맨 앞에서 혼자 돌린다**(`scripts/wf2b2ag_system.js` 를 그래서 따로 만들어 뒀다).
- 🔴 **한도로 죽은 에이전트는 산출 0** — 읽기값만 태우고 회수할 것이 없다. 그래서 단계를 잘게 쪼갠다.
- 🔴 **진행 화면에 토큰이 찍혀 있어도 «살았다»가 아니다** — 살았는지는 `journal.jsonl` 의 `{"type":"result"}` 줄이 판정한다.
- **한도 말고 «서버 오류»로도 죽는다** — 09-03 Loom 수리가 그랬다(「Server error mid-response」). 이건 다시 돌리면 살 확률이 높다(한도와 달리 계정 사정이 아니다).
- `resumeFromRunId`(끊긴 워크플로를 이어 붙이는 기능) 캐시가 **병렬 결과를 안 되살렸다**(5안 중 4안 재실행) — 재개 대신 «빠진 것만» 스크립트.
- **pipeline 첫 단계가 `null` 을 내면 그 항목이 조용히 버려진다**(Loom 종합이 그렇게 빠졌다) — 자리표시는 비-null 로.
- **분배 스크립트는 파일을 덮어쓴다** — 두 런을 합칠 땐 저널을 콤마로 같이 넣는다.
- 한 단계 = 15~30 에이전트(≈3~6M)로 잡으면 한 창에 든다. 통합·집필처럼 max 강도 하나짜리는 40~60분 걸린다.
- **fill 스크립트 만드는 법**: 앞 스크립트를 그대로 두고 «맨 끝 실행 블록»만 갈아끼운다. 머리(스키마·프롬프트)를 건드리면 재료 규격이 어긋난다.
  문법 검사는 `node --check` 로 안 된다(최상위 return 때문) — async 함수로 감싸 `new vm.Script(...)` 로 잰다.
- 정본 기억: `memory/workflow-stage-split-session-limit.md`.

---

## 6. 이 세션들의 판단 맥락 (재료 파일 밖에 있던 것 — 잃으면 다시 못 만든다)

- **「한 차원 위」의 자** = `재료/철학.json` 의 `one_dimension_up_definition` 여섯 축(①모델 축의 부호 ②재료의 출처 ③흐름의 종점 ④수신자 ⑤재심의 갱신 방식 ⑥자). 설계·심사·반박이 **전부 이 문장을 자로 썼다.** 집필도 §1 에 이걸 인용한다.
- **유호님이 정할 것은 정지선 넷만**(돈 · 모델 픽 · 소급 불가 · 유호 확정 충돌) 문서 §12 로 간다. 나머지는 「내가 진행한다」로 적는다. 엔진별 13~18건이 올라왔고 통합이 합쳐 줄이는 몫을 맡았다.
- **시제 규약(철학 §0)** — 없는 것을 있다고 쓰지 않는다. 반박이 가장 많이 잡은 것이 이것이다(예: 캐릭터 세 축 중 «맑기·빛»은 아직 짓기 전인데 라이브처럼 쓴 장면 · 09-03 재측정에서도 코드 0으로 확인).
- **학생 화면에 수·분모·비율을 싣지 않는다**(앱 게임층 규격 3 · 철학 Ⅱ-8 「계기판이 아니다」) — 수는 강사·원장 계기판에만. 반박이 이걸 치명으로 여러 번 잡았다.
- **자산층 셋(Trail·Prism·Temper)은 «읽기만»** — 자기 수집 통로를 새로 만들지 않는다. 학생 화면에 직접 안 닿는다.
- 작업본에 **다른 세션의 미커밋이 계속 있었다**(철학 정본 v1.14~16 개정 등) — 손대지 않았다. 이어 하는 세션도 임자를 가른다.
  ※ 09-03 05:30 기준 작업본은 깨끗했다(git status 0줄).
