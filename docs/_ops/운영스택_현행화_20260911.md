# 운영 스택 현행화 — 2026-09-11

요청: 폐기된 Notion의 기능을 현재 서비스로 압축·대체하고, 9월 11일의 최신 기능과 실제 SYNK 소비 경로를 대조해 품질이 확인되는 변경을 적용한다. 단순 최신 버전 번호와 SYNK에서 검증된 성능은 구별한다.

## 적용과 실제 확인

- **Notion:** 폐기된 `SYNK 앱 동기화` 연결을 삭제했고 개발자 연결 목록의 `No connections yet`를 확인했다. Apps Script 소유자 전용 실행으로 `NOTION_TOKEN` 속성 부재를 22:22:05 KST에 확인했다. 임시 실행 파일은 제거했고 원래 23개 파일 모두 보존됨을 API 재조회로 대조했다. Notion 작업공간 전체를 이번에 삭제한 것은 아니다.
- **잘못된 과거 보고:** `자격증명_교체_완료_20260911.md`의 Notion 재발급·만료 대기·전체 완료 서술을 실제 정리 상태로 바로잡았다.
- **운영 진단 v9.345:** 설정됨·일부 누락·폐기 잔존·조회 실패를 구분한다. 키나 템플릿 ID가 있다는 것만으로 연동 작동을 선언하지 않는다. 진단은 비밀값을 반환하거나 외부로 전송하지 않는다. GitHub 학생 자료 반출은 기존 목적별 동의 확인 전 제한 상태를 유지한다.
- **현행 구조:** 접수와 자유서술은 Apps Script 상담시트, 정량 기록은 profiles/calc와 Supabase, 정성 관찰은 SYNK-talk 강사 관찰 통로, 회사 문서는 저장소 정본과 기존 열람 도구로 연결한다. `SYNK_동기화_구조.md`와 연관 설계 7개를 실제 코드에 맞췄다. 기존 데이터 열 `📝자유서술→노션`은 호환용 키라 보존했으며 Notion 전송을 뜻하지 않는다.
- **앱 패치:** SYNK-talk 직접 의존 13곳을 현행 Expo 57 호환판으로 올렸다. Expo 57.0.22, Skia 2.11.2, ts-fsrs 5.4.2, sharp 0.35.4 및 관련 Expo 패치다. 누락된 expo-audio의 expo-asset 필수 의존도 보완했다. 복습 60일 상한의 안정도·난이도 등 계산 보존을 확인했다.
- **Gemini:** 공식 지원표에 맞춰 Gemini 3.8의 medium 선택을 허용한다. 과거 503은 당시 가용성 실패이며 기능 미지원의 증거가 아니다. 기본 high는 유지한다. 이번 medium 실호출은 하지 않았다.
- **노션 대체 학생 기록:** 기존 강사 화면 안에 담당 학생의 교실 관찰·학습 제출 메타·강사 피드백 조회를 추가했다. 종류별 최근 10건, 날짜·출처·빈 기록·실패·이전 기록 존재를 구별한다. 학생 원문·음성 경로·연락처·위험 신호는 반환하지 않는다. 독립 검토에서 동의 철회 중 읽기 결함을 발견해 수정했고 122/122 관련 시험과 PC·모바일 폭의 실제 컴포넌트 검수를 마쳤다. 서버는 리허설 v55·운영 v27에 반영했다.

## 검증

- Apps Script 이번 진단·반출 선언·모델정책·기존 안전 시험: **241개 중 236 통과, 실패 0, 기존 TODO 5**.
- 전체 test-ci 최초 실행: 2,484개 중 통과 2,470, 실패 5. 이번 진단 기대값과 실제 Instagram 출구 선언 2건을 고쳐 관련 시험을 통과시켰다. 나머지는 detached 최신상태 관찰, 비전 생성물의 혼합 줄바꿈, 다른 작업의 Loom 생성기/자산 불일치다. 전체 통과로 보고하지 않는다.
- `deploy-security-check` 통과. `보안검토과녁`에서 넓은 미커밋과 간접 미배포 후보를 확인했다. 이번 라이브 변경은 원격 원문과 일치하는 기준에서 Code·콘텐츠AI·폼리포트 3개만 바꾸며 나머지 20개 원격 파일은 그대로 보존한다.
- 앱 패치 관련 **65/65 통과**, 웹·Android·iOS 번들 생성 성공. Expo doctor **20/21**이며 남은 항목은 기존 Skia와 Expo 권장판 차이다. 네이티브 컴파일·기기 설치·녹음·OTA·스토어 배포 성공을 뜻하지 않는다.
- 앱 학생 기록과 패치 상세: `C:/Users/q1212/Documents/SYNK-talk/docs/_ops/학생기록_노션대체_20260911.md`, `C:/Users/q1212/Documents/SYNK-talk/docs/_ops/스택_패치_검증_20260911.md`.

## 아직 바꾸지 않은 대상과 이유

- Opus 5·GPT-6 Astra는 현재 사용 가능한 현행 모델이다. 상위 추론 후보를 모든 상담에 넣으면 실제 품질이 좋아지는지는 동일 과제로 비교해야 한다. 가격·발행일로 최고 품질이라고 단정하지 않는다.
- 앱 Whisper 전사와 Apps Script Google 전사는 최신 전사 후보가 있다. 그러나 학습자의 실수·머뭇거림 보존, 한·몽 혼합 발화, 구간 시각·검수 계약을 확인하지 않고 바꾸면 학습 자료를 왜곡할 수 있다. 승인 음성 자산 교체 작업과도 구별한다.
- Supabase 새 키 체계는 cron·모바일·JWT 소비자를 함께 이관해야 한다. Postgres.js 19개 연결 지점의 패치는 연결·재시도·트랜잭션 회귀와 리허설까지 필요한 별도 반영 범위로 남는다.
- 차기 React Native·React·Sentry 개별 최신판은 현재 Expo 지원 묶음을 벗어나므로 일괄 올리지 않았다.
- 직원 최초 등록·회의/업무 보드의 미구현 요구는 기존 앱과 다른 역할이다. 새 SaaS를 만들거나 구매한 것으로 처리하지 않는다. 현재 구현/미구현 구분은 동기화 구조 문서에 남겼다.

## 공식 근거

[Expo SDK 57](https://expo.dev/changelog/sdk-57), [Expo 업그레이드](https://docs.expo.dev/workflow/upgrading-expo-sdk-walkthrough/), [Skia 2.11.2](https://github.com/Shopify/react-native-skia/releases/tag/v2.11.2), [ts-fsrs 5.4.2](https://github.com/open-spaced-repetition/ts-fsrs/releases/tag/v5.4.2), [sharp 0.35.4](https://github.com/lovell/sharp/releases/tag/v0.35.4), [Gemini 3.8 지원표](https://ai.google.dev/gemini-api/docs/models/gemini-3.8-flash), [Claude 모델](https://platform.claude.com/docs/en/models/overview), [OpenAI 전사](https://developers.openai.com/api/docs/guides/speech-to-text), [Chirp 3](https://docs.cloud.google.com/speech-to-text/docs/models/chirp-3), [Supabase 키 이관](https://supabase.com/docs/guides/getting-started/migrating-to-new-api-keys), [Postgres.js 릴리스](https://github.com/porsager/postgres/releases).

## 배포·인수

- Apps Script: 로컬 자기 변경 `1f782056d`, 원격의 다른 완료 작업을 보존한 통합 커밋 **`ab7d158c7`**을 master로 push했다. 원격 CI `34605899509` 통과. 소스 **23/23**이 통합 Git 원문과 일치하며, 실제 프로젝트 HEAD와 기존 고정 배포 **@53**을 API로 다시 받아 같은 내용임을 확인했다. 기존 배포 ID와 배포 수 2개 유지. 내용 지문 `db42dbd1`.
- 공식 배포판점검은 루트 작업본과 라이브 23개 내용 일치를 확인했으나 공유 작업본에 다른 작업의 미커밋 9개가 있어 로컬 HEAD 동일로 표시하지 않았다. 별도의 통합 커밋-배포 소스 전량 대조로 이 한계를 보완했다. crewcard 조회의 일시적 네트워크·임시폴더 오류도 API 재조회로 확인했으며 프로젝트 HEAD와 고정 **@34**의 **5/5** 파일이 현재 작업본과 일치한다. crewcard 코드는 이번에 바꾸지 않았다.
- SYNK-talk: 로컬 자기 변경 `d56ae61`, 원격 통합 **`8b341d4`**를 master로 push했다. 직원 기록 서버는 기존 도구로 리허설 **v55**, 운영 **v27 ACTIVE**에 배포했다. 각 환경 동봉 **20/20 바이트 일치**, 본체 **400/400 앵커 일치**, 새 두 경로의 무인증 요청 **401**을 확인했다. 본체 앵커 검사는 TypeScript 원문 바이트 동일의 증거가 아니며 실제 직원 세션 조회는 별도다.
- 운영 PostgreSQL에서 존재하지 않는 합성 UUID와 0·1·2개 시각 배열로 최종 동의 조회문을 읽기 전용 실행했다. 학생 부재와 동의 결과 false를 확인했으며 학생·동의 생성은 없다. 실제 동시 철회 실험이나 실학생 인수로 확대하지 않는다.
- 학생 기록을 포함한 최종 앱 웹 번들 생성 성공. 앱의 네이티브 컴파일·기기 설치·스토어/OTA 반영은 미실행이다. 이 검증 없이 Expo 밖의 차기 버전이나 전사 모델까지 최고 성능이라고 선언하지 않는다.
- SYNK-talk 최초 원격 CI는 새 화면과 기존 공통 규칙의 연결 4건을 발견했다(직원 뒤로가기 목록, 시간대 정본, 미리보기 인자 검사 2건). 원격 수정 커밋 **`4939eb4`**에서 해결했고 관련 **150/150**, 최종 CI **34607822363**의 verify·Supabase 검사가 통과했다. build는 조건상 건너뛰어 실제 기기 빌드 성공을 뜻하지 않는다. 최종 UI 소스로 미리보기와 웹 번들도 다시 생성했다.

이번에 확인하지 못한 모든 회사 계정·기기·제작물까지 최신화했다고 주장하지 않는다. 새 구매·구독·자동충전·학생 자료 외부 전달은 하지 않았다.
