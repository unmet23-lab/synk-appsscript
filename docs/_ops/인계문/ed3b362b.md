<!-- at:1786974974518 -->
## 2026-08-17 22:56 · 세션 ed3b362b · 컨텍스트 374k

```
SYNK 이어서 작업한다 — 이 글은 세션 `ed3b362b` 이 **마감하며** 남긴 것이라(그 뒤로도 더 일했을 수 있다) 아래의 「나·내」는 전부 그 세션이다. **너는 다른 세션이다** — 네 지문은 `$CLAUDE_CODE_HOST_SESSION_ID` 앞 8자리이고 보드 줄은 **네 지문 파일에만** 쓴다(F250·F332). 직전 세션에서 한 것:
· eb4c332b Merge 밀어둔 옛 판 — rebase 로 갈라진 내 가지의 pre-rebase 사본 2건(둘 다 local_ed3b362b)
· 6d5b119a docs: 보드 ed3b362b — 형제 경로 정본 착지 (손조립 15→3 · 워크트리 skip 12→1 · 변이 7/7)
· 829cd82f fix: 픽스처 둘이 «판정을 못 가르고» 있었다 — 변이 검사와 CI 모사가 각각 잡았다 (변이 7/7 구멍0)
· 59828f64 fix: 형제 저장소 경로를 «모양으로 짐작»하던 네 벌 → git 에 묻는 정본 하나 (F564 후속 · 🎫 91e2b70a)
· 90daa1a4 docs: 보드 선언 ed3b362b — 형제 저장소 경로 규칙 네 벌을 git 에 묻는 정본 하나로 (F574 동반 신고)
· 044f665e docs: 마찰 F574 신고 — board-guard 겹침 검사가 「⏳유호 대기」 줄을 분모에서 안 뺀다 — 그 줄의 「만질 파일…
· efbd6a5d docs: 보드 선언 ed3b362b — 형제 저장소 경로 규칙 네 벌을 git 에 묻는 정본 하나로 (F574 동반 신고)
· ab39c115 docs: 마찰 F574 신고 — board-guard 겹침 검사가 「⏳유호 대기」 줄을 분모에서 안 뺀다 — 그 줄의 「만질 파일…
· 저장소 미커밋 12건 — **내 것인지 남의 것인지 `node tools/작업본소유자.js`로 먼저 가른다**(남의 작업본을 커밋하면 사고다)
▶ 그 세션의 보드 줄 (원본 `docs/_ops/보드/ed3b362b.md` — 마감 뒤 바뀌었을 수 있다): **형제 저장소 경로를 «모양으로 짐작»하는 규칙이 네 벌 — git 에 묻는 정본 하나로**(🎫 승계 `local_91e2b70a` · 실측: 지금 살아있는 `head-ci` 워크트리에서 네 벌 **전부** 눈이 먼다 · 새 선언 · 남의 줄 무변경)
   상태/다음: ✅**착지** (`local_ed3b362b` · PR #77) — 손조립 **15→3** · 워크트리 skip **12→1** · 회귀 14 · **변이 7/7 구멍0** · CI 4298 fail1(남의 채번 경쟁 · 단독 초록) · 🎫**남은 3=`보드근거.js:179`·`이해대장.js:140,142`**(남의 줄이 잠금 · F574) · ④해당없음
▶ **이 트랙은 종결로 적혀 있다**(상태 칸이 ✅ 로 열린다) — 위 「상태/다음」의 ⚠·⏳ 잔여는 **배경이지 지시가 아니다.** 「남이 배포하면 따라 나가는 것」과 「내가 더 할 일」이 거기선 같은 모양이다. **멈추지 말고 새 트랙을 잡아라** — 장부 미해소·보드 ⏳·결정 큐에서 고르고, 방향이 굵직하면 유호님께 판정안으로 올린다. 남의 줄을 내 트랙으로 이어받지는 않는다(F073 · F165 · F170) — **예외는 상태 칸에 `🎫` 가 붙은 일감 하나뿐**(주인이 「나는 이걸 안 한다」고 스스로 놓은 것). 집을 땐 남의 줄을 고치지 말고 **`docs/_ops/보드/<네 지문>.md` 에 네 줄**을 새로 선언한다.
```
