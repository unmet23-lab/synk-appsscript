# 트랙 — 무엇이 남았나

> **결정.md 의 짝이다.** 결정.md 는 「무엇을 정했나」, 이 파일은 「그중 무엇이 아직 안 끝났나」.
> 만든 까닭(유호 지시 08-22 「지금 우리 할것들 전부 까먹지말고 제대로 인계문 부탁해」):
> 트랙 선언의 정본은 «열린 PR» 인데 **지금 열린 PR 이 0건**이라 남은 일을 적을 자리가 없었다.
>
> **낡음 방지 규율 — 이 파일을 살리는 유일한 조건이다**
> - 트랙을 끝낸 세션이 **그 커밋에서** 이 줄을 지운다. 「나중에」는 없다.
> - 새 트랙은 유호님이 정한 그 자리에서 한 줄 넣는다(결정.md `--추가` 와 한 벌).
> - **엔진에 닿는 트랙의 종결 선언에는 ④완결 3칸을 붙인다** — 표기 `④모✗닿✗늘✗`(모였나·닿았나·늘었나 ·
>   철학 Ⅰ-3④ 「보드 상태 칸」의 현행 자리 = 이 파일 · 유호 확정 08-22 D4). 한 낱말 「종결」이 ✓✓✗ 와
>   ✗✗✗ 를 뭉개지 않게 — 「늘었나」는 첫 실학생 전엔 ✗ 가 정직값이다(F207).
> - **여기 적힌 상태와 실물이 어긋나면 실물이 이긴다.** 이 파일은 색인이지 증거가 아니다.
>   증거는 커밋·렌더·`node tools/결정.js` 다.
> - 열린 PR 이 있으면 **PR 본문이 이 파일보다 우선**한다(유호 확정 08-15 「PR = 작업 목록」).
>
> 마지막 갱신 = 2026-08-24 16:2x (§2-F 갱신 — 요소 156·화면 14 완주 뒤 «색으로 갈린» 라이브러리 52장 착수)

---

## 0. 지금 도는 세션 — 남의 자리는 손대지 않는다

`list_sessions` 로 다시 센다. **08-22 17:33 실측 — 셋이다.** 한때 여섯까지 늘었다가
유호 지시(「끊는거 니가 해줘」)로 굽기 중복 셋을 끊어 정리했다(17:27). 셋 다 **메인 저장소 한
작업본을 함께 쓴다** — 워크트리가 아니라 같은 디스크다.

| 세션 | 레인 | 이 세션 몫인 파일 |
|---|---|---|
| ① Loom — 화면·요소 | **요소 닫힘 · 오늘 탭 v1 판정 대기(08-22)** · 다음 = 소식 탭 | `tools/lib/loom.js` · `docs/디자인_토큰.json` · `docs/이해대장.html` · `docs/Loom_부품_시연.html` · 트랙 §0·§2 |
| ② 엔진 — 상태기반·deliver | 상태기반 §12 · 나침반 배선 · Ⅱ 자물쇠 · Ⅲ⑤ FSRS(talk `lib/복습스케줄.js`·`lib/복습파생.js` — 옛 `엔진_복습스케줄.js` 이식판은 08-22 폐기) | 트랙 §1 |
| 배치 정리 | 없음(세션 수만 잰다) | 없음 |

⚠ **블렌더는 한 줄로만 돈다** — iGPU 하나에 블렌더 1벌이 3.4GB 를 쥔다. 실측 17:33 여유 **0.8GB**.
둘째를 띄우면 둘 다 느려지는 정도가 아니라 스왑으로 넘어간다.

### ⚠ 겹침 규율 — 08-22 에 실물로 배웠다 (유호 지시 「겹치지않게 조심해」)
같은 굽기가 **세 세션에서 동시에** 돌았다(17:08). 내장 GPU 하나라 장당 **149초** 걸렸고(혼자면 31초)
셋이 같은 PNG 를 겨눴다. 그다음이 더 나빴다 — 두 세션이 «서로의 프로세스를 죽여» **둘 다 멈췄다**(17:16).

- **죽이기는 수렴하지 않는다.** 남의 굽기를 죽이지 않는다. 규칙은 하나뿐이다 —
  **이미 돌면 걸지 않고 물러난다.** 산출은 같은 폴더에 쌓이니 누가 굽든 결과는 하나다.
- **거는 쪽이 먼저 센다**: `Get-CimInstance Win32_Process -Filter "Name='node.exe'" | ? { $_.CommandLine -like '*굽기*' }`
  🔴 `tasklist | grep` 도 `tasklist /FI` 도 **쓰지 마라** — 한글 로케일·MSYS 경로 변환 때문에 **늘 0건**이다.
  블렌더 셋이 도는 중에도 「없음」이라고 답했다(그 오판이 이 사고의 방아쇠였다).
- 자리를 잡았으면 `send_message` 로 **먼저 알린다.** 알리기 전에 거는 것이 겹침의 시작이다.
- **끊긴 굽기는 성공처럼 보인다.** 도구 백그라운드로 건 굽기는 «남이 끊어도·잘려도» 알림이
  `completed · exit 0` 이고 로그 끝줄이 ✅ 라 완주와 구별이 안 된다(두 세션이 각각 겪었다:
  한쪽은 2분 반 뒤 한 장만 굽고, 한쪽은 130초 뒤 로그 안 `EXIT=127`).
  🔴 **판정은 파일 «시각»으로 한다 — 수로는 못 잡는다**(08-24 · 두 세션이 각각 실물로 밟았다).
  옛 판 파일이 그 자리에 남아 있으면 `existsSync && size>2048` 도, 「파일 몇 장인가」도 **전부
  성공으로 읽는다** — 수가 그대로이기 때문이다. 86 세션은 68초 만에 죽은 굽기가 로그에
  「✅ 68초 · 3547KB」로 찍히는 것을 봤고(수리 = `세트굽기.js` 판정에 `mtimeMs >= 항목 시작 −1초` ·
  `644716a4`), 나는 「버린다」고 말만 하고 산출을 남겨 둬 다음 굽기가 그걸 집을 자리를 만들어 뒀다.
  ⇒ ①판정에 mtime 을 넣거나 ②굽기 전에 산출 폴더를 비운다. 「버린다」는 **파일을 지워야** 참이 된다.
  🔑 **「돌고 있다」와 「났다」는 다른 사실이다**(08-24 · 86 자기 정정). 프로세스 목록의
  `출력=…/lead-back.png` 는 **의도**를 말하지 산출을 말하지 않는다 — 그 줄을 보고 「파일이 났다」로
  건너뛴 판정이 세션 셋에 틀린 보고를 돌렸다. 프로세스는 «살았나»만 답하고, «났나»는 파일만 답한다.
  🔑 **굽기와 «판정 지면»은 한 벌이다**(08-24 · 86 실측 `5e1c47a6` — 같은 병의 셋째 얼굴).
  `요소_시안.html` 이 23:11 생성인데 그 뒤 03:44 까지 156장이 다시 구워져 **지면과 실물이 네 시간 반
  어긋나 있었다** — 그대로 올렸으면 유호님이 «옛 그림으로 새 판을 판정»하셨을 것이다.
  ⇒ 굽기 도구는 ④지면 그리기까지 한 벌로 돌고, 지면 판정도 mtime 으로 건다.
  🔴 **이건 시안 시트를 손으로 만드는 자리에도 그대로 걸린다**(NPC 갈래 실측): 컷 몇 장을 다시
  굽고 시트를 안 다시 만들면, 유호님이 보시는 격자에 옛 컷과 새 컷이 **섞인 채로** 선다.
  ⇒ **컷을 하나라도 다시 구우면 시트를 다시 만든다** — 「보여드리는 것」이 「있는 것」과 같아야
  판정이 판정이 된다. 세 얼굴의 뿌리는 하나다: **지금 보는 것이 지금 것인지 시각으로 확인한다.**
- **분리 실행의 인코딩은 «입력»과 «출력»이 따로 논다**(08-23 실측 · 둘 다 밟았다):
  · 입력 — `cmd /c` 배치는 한글을 CP949 로 읽어 명령이 통째로 깨진다(「'--?비'는 명령이 아닙니다」).
    ⇒ PowerShell `.ps1` 을 **UTF-8 BOM** 으로 저장한다.
  · 출력 — 그렇게 띄워도 **로그는 여전히 CP949** 다. 그래서 `grep -q "전부 끝"` 같은 «한글 표지»
    판정이 원리상 안 된다(52장 굽기에서 완주를 못 잡고 STALL 로 끝났다).
    ⇒ 완주 판정은 **파일(수·해상도·시각)** 로 한다. 로그 표지는 보조로만.
- 🔴 **bash 는 한글 변수명을 확장하지 못한다** — `$수`·`IFS='|' read -r 방 스 그` 가 조용히 실패한다.
  더 고약한 것은 **실패한 자리에 「PASS」가 찍힌다**는 점이다(재생성이 하나도 안 돌았는데 옛 파일끼리
  비교해 「최대차 0」이 나왔다 · 08-23). ⇒ 스크립트 변수는 **영문**으로, 회귀는 파이썬으로 몬다.
- 공유 작업본이라 **남의 미커밋이 내 `git status` 에 뜬다.** 커밋은 언제나 `-- 경로` 로 못 박는다
  (지침 그대로 · `add -A`·`commit -a` 는 남의 반쯤 된 일을 통째로 담는다).

✅ `tools/요소굽기.py` 미커밋은 **해소됐다**(08-22 · 커밋 996f09aa + b2a19949). 그 299줄은 남의 것이
아니라 요소 트랙 본인의 것이었다 — `직물결()` 수리·조작 여섯·투명 모드.
⚠ 그래도 굽기 전에 `git status -- tools/요소굽기.py` 는 지킨다: **세트굽기는 항목마다 이 파일을
새로 읽는다.** 배치가 도는 중에 이 파일을 고치면 앞 렌더와 뒤 렌더가 갈리고, 갈린 세트는 못 알아본다.

---

## 1. 엔진 — 가장 큰 트랙 (유호 확정 08-20 「전부 다 진행」·「Ⅰ 먼저」)

두 레인이다. 코드 레인은 세로줄(순서 있음), 대기 레인은 병렬.

### 코드 레인 — 08-22 저녁 전수 실측으로 다시 적음 (옛 표의 Ⅰ③·Ⅰ④ 「남음/미착수」는 낡은 기록이었다)
| | 트랙 | 상태 |
|---|---|---|
| Ⅰ① | 행동층(상태기반 과제선택) | ✅ **엔진 몫 종결**(08-22 · §12 도장 + ⓔ-22 서버 반쪽 talk `572e671` · 리허설+운영 5벌 재배포·배달왕복 122/122) — 남은 것은 아래 4절 «엔진 판정 6» 뒤의 실행뿐 · **④모✗닿✗늘✗**(활성 전 무동작 — 코드 완비와 별개) |
| Ⅲ⑤ | FSRS-6 복습 스케줄러 | 🟡 **«배선만» 전량 완료 — 남은 것은 유호 판정(교체) 하나**(08-24 · §8 명세 v0.3 + 파생층 + 병행 도구 = talk `6e7e631`+`e781798` · 계산기 08-22 `2e5c6e9`). 급소 둘 소화: S2 미상=버림(파생층 「모르면 안 보낸다」 — 계산기 버린수 0 이 건강 지표) · **S1 「맞았다」 다리 = R2 재제출(retry_of) + R3 문항축(G4 빈칸 `재는축` — 계약 개정 0 · 이미 실물이었다)** · S4 왕복 모양 §8-7 못박음(즉석 발명 차단). 병행 실측(리허설): 행 989 → 리뷰 3 + 버림 444 갈래 셈(`node tools/복습병행.js`) · 계약 밖 픽스처 태그 22건 차단. 리허설·운영 8Fn 재배포 · 배달왕복 122/122 · talk 2559 초록. **교체(14일 창 대체)는 유호 판정 뒤 한 커밋(ⓓ) — 자연 판정 시점 = 실학생 리뷰가 쌓인 뒤 · ⓓ 판정날 mastery `grammar_id`↔S6 표 합류도 한 판정으로 묶는다(유호 처분 08-24 · 결정.md — 매핑 초안·잇기/백지 비교표는 그날 AI 완성형)** · **④모✗닿✗늘✗**(병행은 보여만 준다 · 소비자 = 회귀+병행 도구) |
| Ⅲ⑥ | 성향 확인 되돌려주기 | ✅ **한 벌 완료**(08-22 밤 · 유호 세부 픽 「리듬부터·새 사건·이 설계로」 — talk `8f92fc8`+`650b6ef` **계약 c13** `estimate.responded` · 마이그 65 CHECK 리허설+운영 착지(_c12 0·_c13 65) · progress 오늘의확인(하루 1회·부정 재노출 금지) · 교수멘탈 확인 카드+즉시 반응 · 학습자상태 v13 ⑫확인축 · 회귀 전벌 · 배달왕복 122/122). 카피도 확정됐다(08-22 「그대로 확정」 · talk `55e1d94` `카피확정=true`). 남은 것 = 둘째 축 이후(리듬 실측 뒤) · ⚠**정정(08-24 G11 정찰)**: 「한 벌 완료」였지만 **답의 앱 통로가 죽어 있었다** — 게임로그 접두 표 부재 + 조립기 payload.ver 누락(서버에 닿아본 적이 없어 lib 회귀로는 안 보였다). G11 커밋(talk `4fb4c6a`)에서 수리 + 첫 실왕복 통과 · **④모✗닿✗늘✗**(실학생 0 — 사건 0) |
| Ⅲ⑦ | 확신도 전 경로 확장 | ✅ **완료**(08-22 밤 · talk `c819d95` — G2 짚음·G4 빈칸에 기호식 토글 · 계약 개정 0(실물이 이미 열려 있었다) · 학습자상태 v12 자기인식 확장(표본 이중 방지 `표본n`) · 막아 둔 자리 둘 걷음 · 배포·왕복 122/122) · **④모✗닿✗늘✗**(실학생 0) |
| Ⅲ⑧ | 앱 오류 감시 | 기시공(08-15) — 남은 것은 Sentry 웹 조회 토큰(유호님 · 08-22 「나중에」) 하나 |

끝나서 지운 줄(실물 증거 — 다음 검토가 낡은 문서로 재보고하지 않게):
· **Ⅰ③ deliver·검수 수리** = task_snapshot 배선 `238bf21`(08-20) + 검수 P1 2건 `bfb587d`(**08-14** — 08-20
  전수 검토가 낡은 대기열 항목을 재보고한 것) + 낡은 자백 주석(학습자상태.js) 수리 talk `572e671`(08-22).
· **Ⅰ④ 상담 장부 편입** = `b085e705`(08-21 · 「엔진검토 Ⅰ-④」 명기 — 골격 57→58종 · 도달 분모 23 ·
  leads 는 ㉡ 탈락으로 존재·머리글 감시만). 잔여 «상담로그 도달 0» 의 차단자는 코드가 아니라 **봇 개통**
  (Meta 토큰 = 유호님 몫 · `docs/상담AI_개통_지금할일.md` · 기한 2026-12-10).
· **Ⅱ 자물쇠·장부·정본 수리** = 마지막 별항이던 문서층 기계 대조 2벌(`tests/설계문값대조`·
  `tests/설계문_폐기어휘`)을 08-22 저녁 부활 — `e75fc7fc^` 판 바이트 그대로, 현 설계문(v5.13-e)에서
  21/21 초록(픽스처 탐지력 포함 · Ⅱⓐ `cac7ffe9` 의 11벌 부활과 같은 방식). 이로써 Ⅱ 는 닫혔다.
· **Ⅰ② ㉢ 나침반 배선** = 08-22 저녁 deliver(생성) 통로까지 완주(talk `379cd26` · 리허설 v77/v26 ·
  운영 v17/v5 · 배달왕복 122/122) — correct 배선(08-20 `d0dd8c9`)의 짝. 시즌목표가 요약 목표
  조각으로 실리고(§6-2 v5.13-f 명문 · 말의 정본 = `시즌줄` 하나 · 쓸축수 밖) 프롬프트 v2 가
  첨삭 v7 동형 두 축(소재로만 · 언어 불변)을 못박았다. 시험지·스냅샷풀은 무접촉(#6 자리 보존).
  잔여 = 시즌 «있는» 실데이터 실측 — 나침반 답이 처음 쌓이는 날(실학생)의 몫이고 correct 와 같은 상태.

### 대기 레인 (병렬 · 유호님·외부가 쥔 것)
Meta 심사 · **몽골어 검수** · 계정 · 운영 배포

### 🔵 지금 도는 것 — #6 착수 (유호 지시 08-22 「#6 지금 착수할게」)
**#6 = §8-B 평가·크레딧 충전·사람 채점 80회** — 첫 생성 행 «직전» 관문(유호 확정 08-21 · 사람 채점은 한국어 교육 축이라 유호님 몫).

**✅ 기계 쪽 한 벌 완주 — 08-22 밤. 남은 것은 사람 채점 하나다.**
- ✅ 유호님 크레딧 **$6 충전** · `GENERATION_MODEL=claude-sonnet-5` 리허설 시크릿 착지(유호 위임
  「하이쿠나 소넷으로 골라줘」 — 픽 근거 = **재채점 리스크**: 미달 시 유호님 하루가 다시 드는데 모델 차액은 $0.21).
- ✅ **실물 80회 착지**(talk `beedd9c` · `evals/과제생성_결과.json` · 러너 `--원격` · 배포판=소스 게이트 통과):
  **79건 응답 · 1건 검문탈락(P022#1 문장 62자 > 60 상한)** — 후자는 §8-B 「호출 실패·검문탈락 0점 고정·재시도 없음」
  규율대로다(런타임이면 학생에게 안 나갔을 문장). 기계 계약 ✅.
- 📏 **실측 단가가 추정을 닫았다**: 입력 104,826토큰(호출당 1,310) · 출력 6,894(호출당 **86** — 상한 300의 1/3).
  ⇒ **80회 = $0.28**(추정 $0.41보다 쌌다 · 출력이 짧아서). 3벌 재실행해도 $0.84.
- ▶ **남은 것 = 유호님 채점 — 진행 «10/80»**(08-25 현재 · 미채점 70 · 0점 고정 0 · **다음 카드 P021#1**).
  통로가 **채점 화면**이 됐다(08-25 유호 채택 · talk `4b0acb3`·`d928e44`·`5669e6f`): 바탕화면
  「SYNK 자산\사업·운영」의 **「과제 채점 화면 열기」** 더블클릭(= `node tools/과제채점.js --열기`) →
  카드마다 축 여덟 O 초안, 흠만 X 로 뒤집고 저장(X 는 이유 필수 · 행마다 즉시 저장 · 완주하면 집계 표까지).
  상단 분모 클릭 = 매긴 것 목록 → 되돌아가 다시 매기기. 규칙은 talk `lib/과제채점.js` 한 벌 —
  CLI 채점·`--판정` 과 같은 파일·같은 계약. 옛 안내 Artifact 「과제 문장 채점표」의 CLI 시작 명령 절은
  이 줄로 대체됐다. **이어받는 세션은 talk `git pull` 부터** — 채점 진행은 `evals/과제생성_결과.json` 이 쥔다.
  - 첫 10건 경향(표본 작음): 전부 O 5 · 흠 5 · X 최다는 **⑥정확성 4회**(v3 최악 축과 같은 자리) ·
    ⑦목표활용·⑧상태활용 각 3. 유호님 메모에 교정문이 실려 있어 **다음 판 프롬프트 재료**가 된다
    (P002#2 「자전거는 어디 두고 걷나」 = 문장 내 논리 · P003#1 「이어 쓰면 맥락 구조가 안 맞는다」 =
    1~3문장 완화의 부작용 후보). 완주 날 정리해 §8-B 3회전 판정에 붙인다.
  - P001#2 는 메모(어휘 지적·교정문)와 점수(전부 O)가 어긋나 있던 것을 **⑥정확성 X 로 교정**(유호 지시
    08-25 · talk `4a58344` · 눈금 근거 = 같은 구조의 P002#1 판정). 그 자리를 만든 「되돌아갈 문 부재」는
    `5669e6f` 가 닫았다.
- 🔴 **채점 CLI 가 ⑧상태활용을 «원리상 못 매기게» 돼 있던 것을 고쳤다**(talk `321c3b5` · 유호 「어떻게
  하는건지 이해가 안가네」에 답하다 실측): 화면이 급수·목표·기술만 보여주고 **모델이 본 상태 요약을
  안 보여줬다** — 그 축의 정의가 「요약에 실린 값이 산출에 쓰였나」인데 요약을 못 보면 판정 불가다.
  같이 고친 넷 = 미채점 행만 묻기(0점 고정·기채점 자동 제외) · 진행 `[n/총]` · 「11110111」 붙여쓰기 ·
  0점인데 이유가 비면 되돌리기(80개 다 매긴 «뒤»에 파일 전체 무효를 발견하는 참사를 그 자리에서 막는다).
- ⚠ **`--판정` 은 모델을 env 로 줘야 한다** — `$env:GENERATION_MODEL='claude-sonnet-5'` 를 앞에 붙인다.
  현행판이 `process.env.GENERATION_MODEL` 을 읽고 **미설정이면 빈 값 = 비교축 「다름」**이 설계 의도라
  (`lib/과제생성현행판.js:7`) 결함이 아니라 절차다. ⚠ Actions 리셋(9/1) 뒤엔 CI env 에도 그 칸이 필요하다.
- ⚠ **회귀 `과제생성게이트.test.js ③` 적색은 «의도»다** — 결과 파일이 실재하는데 채점이 안 끝났으니
  집계 0(러너가 파일 판에 적은 「실행과 채점은 한 벌이다」 그대로). **채점이 서면 사라진다 · 재진단 금지.**
- 🔄 **§8-B 2회전 진행(08-23)** — 1회전(v3·Sonnet) 유호 전수 채점 79건 = **전 축 미달**(⑥ 0.23 최악).
  피드백 패턴 여섯 → 프롬프트 v4 + 계약 완화 둘(유호 확정: 1~3문장 8~120 · 후속 질문 ?≤2 —
  검문·설계 §8 v5.14 한 벌) + **모델 Opus 픽**(유호). v4+Opus 80건 재착지(\$1.53 · 탈락 0 ·
  talk `97b13bd`+`2e15cc0`). v3 채점은 `_v3판보관.json`(패턴의 원천). **다음 = 유호님 2회전 채점.**
  유호 발견 「자기성찰」이 v4 머리 기준이 됐다 — 채점표 축은 불변(⑤재미가 그 얼굴을 잰다).
- ▶ **따라 말하기 «문장 단위 끊기» 어댑터** — 계약이 1~3문장이 되면서(위 완화) 앱 ②따라 걸음이
  긴 이야기를 통으로 읽게 된다. 문장 단위로 끊어 듣고-따라 말하게 하는 처리(src/말하기화면.js
  듣기·녹음카드 층) — **활성 전 필수**(한 호흡 원칙의 새 형태). 완화 확정(08-23)과 한 몸의 일감.
- ✅ **게임 4모듈 «자기 설명» 검수 — 집행(08-23 · talk `1a6fade`)** — 실측: 네 화면 모두 단계 안내는
  좋았으나 «전체 그림» 이 전부 공백(말하기 공백의 복제) · G1 은 «연결»도 끊김(고른 전략 소실).
  처방 = 첫 카드 개요 한 줄 ×4 + G1 「내가 고른 방법」 재표시. 화면 회귀 6벌 83/83.
  ⚠ 개요 문구 4벌은 초안 — **카피 확정 = 유호님 몫**(성향확인 카피와 같은 절차).
- 🔄 **몽글 «그 자리 연기» — 숙제 인트로 연출(유호 확정 08-23 · 결정.md 그 줄 · talk `9a7493b`+`7db47ea`)** —
  말하기 1벌 착지: lib 연출대본 3벌(세 걸음 예고 · 답하기 질문을 @질문 슬롯으로 몽글이 물어봄 ·
  완료축하 — 지금 마친 순간만·기쁨 합법 유일 대본) + 마스코트 시퀀서(다가옴/사라짐·탭 건너뛰기·
  녹음/줄임 게이트). 인트로 말투 = «같이-권유형» 유호 픽으로 확정. 상주는 유호 해제에도 안 올림
  (녹음 중 얼어붙은 몸 — 연기 등장·퇴장이 정답 · 결정.md). 전체 2533 초록.
  **남은 몫**: ①유호님 눈검수 — 연기 박자(총 ~9초) + 완료축하 문구(「와아, 다 했다!」 초안) 확정
  ②눈검수 통과 뒤 **게임 4모듈 확장**(G3 사장님 전화 = 몽글이 전화기 들고 역할 연기 등 — 소품 연출).
- ▶ **펠트 표정판 두 벌 — 깜찍(기쁨)·별눈** (08-23 옛 몽글 소각의 남은 자리 · 결정.md 그 줄) —
  앱 보상 표정(기쁨)이 지금 눈웃음으로 접혀 있다. 펠트코랄 정본에 깜찍·별눈판을 만들면(합성 또는
  굽기 — 놀람처럼 원본 픽셀 합성이 선례) talk 표정컷에 한 줄씩 복귀. 별눈은 발동 신호(진화·시즌)도
  아직 0 이라 판이 서도 배선은 그 신호가 설 때.
- ▶ **08-23 전수조사 잔여 — 둘 남음** (유호 지시 「제대로 전수조사」 · A급 13곳 + ①②③은 수리 완료):
  ✅①NPC 16종 양모 밤 재채색 + 팔레트 «이름·값 쌍» 토큰 대조 회귀(talk `abb3e5c` — ⚠대조 시트
  16쌍 눈검수 = 유호님 몫) ✅②퇴역 색 이름 15자리 스윕(talk `59a93ef`) ✅③소개서 옛 컷 체계
  서술 현행화(「3D LIVE」 자체는 현행 자산으로 실측 — 낡은 건 「컷 14·표정 10」 조각뿐이었다).
  ④`git-scope-guard` ⑦⑧ 철거/재정본화 판정 — 정본(작업본소유자.js)이 08-20 보드 철거로 소멸해
  ⑦은 비활성·⑧은 「전부 모름」만 돈다(표식은 달았다 · **유호님 판정 자리**: 걷을지 하네스식으로
  다시 세울지) ⑤오버레이(bots/) 무대 색·명세 현행화 — 방송 재개 전까지면 된다.

### 엔진심문(0822) 소화 현황 — 유호 확정 08-22 「잔여 전부 진행해」 · 08-23 집행분
✅ **12건 종결**(정본 = docs/엔진심문_0822_fable5.md 취소선 · talk `2e33ed9`+`b3a5ef6`+`dfdd077`+`490dc85`+`624ae47`
등 · 배포 완료 = events·radio-promote·progress·deliver·deliver-one·tasks 리허설+운영 · 배달왕복 122/122):
T1 재호출 맥락 · T2 상태오류 값 폴백 · T3 vault A0 · T4 공갈 소멸 · **T5 워커 성공 경로 첫 실측**(성공 2 ·
≈$0.01) · T9 표식+기각 · T12 감시 늑대소년 · G1 장부 현행화+**④«늘었나» 칸 신설**(B[22] — 늘지않음상한
래칫) · G7 소비자 명부 · G12 예산 시계 서버 축 · G13 부정키 한 원천 · S2 미상=버림 · S3 판올림 감시.
✅ **잔여 4건도 종결(08-23 · 유호 지시 「이어서 진행해줘」)**: T6 감시 7→**9항**(⑧산출 전멸 —
target>0·성공 0·분포 동봉 / ⑦빈 배정) · T7 산술 ㉱ 등재(108명 상한 · 활성조각 머리말) ·
T10 **이미 착지 실측**(어댑터 사슬 전체가 살아 있었다 — 낡은 자인·주석만 현행화) · T11 빈 배정
**자가치유 + 리허설 재현 실측 통과**(duplicate 전 제출 보충 · deliver 리허설 v83/운영 v22).
⇒ **엔진 레인 심문 16건 전건 종결.** ✅ **G11 잔여도 종결(08-24 · 유호 해제 「G11 잔여도 이어서
진행해」 — 08-23 「채점 연기·잠금 유지」를 그 자리에서 다시 열었고 결정.md 등재)**: 처방 둘 다
집행 — ⓐ물리 방벽 `estimate_daily_once_c13`(engine.ub_date · progress 게이트와 같은 자 ·
리허설·운영 적용 ✅ 운영 «전부 통과») + events 늦은 쪽 duplicate 접힘 ⓑ학습자상태 **v14**
확인축 «키별 마지막 답»(접힘·뒤집힘 드러냄 · 표본n 으로 신뢰도 불변 · 모양 회귀 4 신설).
🔑 **잠금의 근거였던 §8-B 는 재서 닫았다**: 평가 풀·시험지에 확인축 0건 → v14 아래 80행
input_hash 전부 동일(결과검증 0 사유) — **갈리는 것은 이름표(estimator_version)뿐**이고, 유호
채점 완주 날 게이트 ③이 그 이름표 하나로 빨개지면 그때의 처분(재생성 $1.5 vs 동봉 재검증
갱신)이 유호 판정 자리다(입력 동일 실측이 그 판정의 재료). 🔴 정찰이 그 앞의 **죽은 통로 둘**을
먼저 잡았다 — Ⅲ⑥ 확인 답이 게임로그 접두 표에 없어 서버에 아예 안 닿았고(quiz.answered 병의
재발) 조립기 payload.ver 누락은 첫 실왕복(왕복시험 ⑯ 신설 · 87/87)이 잡았다. talk `4fb4c6a`.
✅ **T8 도 재판정+집행됐다(08-24 · 유호 픽 「C 조준 생성」)** — 창 안 «생성 진행 중»은 구제가 안 잡아챈다:
「생성중」 카드 + 대기 잡이면 그 학생만 조준 생성(deliver-one?learner_id → jobs_claim(_learner) — 이미
실물이던 슬롯) · 비용 상한 불변(claim 유일성) · 마감 뒤·전멸일 구제 그대로 · §3-1 재판정 명문 +
순수 판정 회귀(창안생성중). ⚠활성 «전»엔 잡이 0이라 무동작 — 첫 발현은 생성 활성 날이다.
✅ **판정 자리 셋(S5·S6·G4)도 08-24 소화 완료**(유호 「이어서 진행해」 · talk `75ddcf0`): S5 = 계약 동기
탐지 양쪽 스위트 부활 · S6 = 태그↔기술 표 유호 확정(8쌍+15 대응없음 — 카드 키 교체·퀴즈 합류까지) ·
G4 = 죽어 있던 CI 복원 예약 재발행 + 형제 탐지(CI 러너 교차 대조만 PAT = 유호 칸).
✅ **G2·G3 도 08-24 소화**(유호 「g2·g3 도 이어서 진행해」): G2 = §16-1 #6 판정을 «파일 존재»→«결과
1벌»로(talk `lib/과제생성평가.결과한벌` 신설 — 차단기 회귀·채점 CLI·왕복시험 A7 셋이 한 함수 · A7
«운영 붓기 차단» 문구 복귀 실측 · ✗ 사유 = 미채점 80행) · G3 = §8-B 장부 신설(talk `evals/생성결과.md`
— 세 판 누적 $2.24 · 유호 채점 두 회전 · v3 전수 79건 8축 전멸 실측(⑥ 0.23) · estimator 이름표 처분까지
— 그간 전부 커밋 메시지에만 있었다). 🔴 곁에서 선존 결함 셋을 또 잡았다: ⓐ`estimate_responded_c13`
조각의 재적용 비멱등 — 같은 저녁 다른 세션이 CI 복원 빨강으로 **독립 동시 발견**해 합본 «멱등화»에
규칙 ①제약사슬·②이력insert 를 세웠고(talk `51bbd7f`+`4aeb36a`), 그 처방이 조각 «직접» 재적용
통로(생성왕복시험 A1)엔 안 닿아 A1 이 같은 함수를 지나게 배선 ⓑ생성왕복시험 A2 의 c12 이름 박제
(c13 개명 뒤 «제약 34개 전부 부재» 헛빨강 — 이름별 마지막 사건 · 사용자 스키마 전체 기준으로 수리)
ⓒ활성조각 감시 ②-a 의 `text[]∥unknown` 배열 캐스트 결함(T6 08-23 개정이 심었다 — **활성 날 감시
아홉 항 전체가 죽는** 급소를 A9 재실행이 잡음 · `::text` 확정).
✅ **잔여 4건(G8·G9·G10·S7)도 08-24 밤 종결**(유호 「이어서 해 · 더 좋은 설계 있으면 알아서」 — 각 건을
심문 문구가 아니라 실물 재조사로 재판정): **G8** = 검사 분모를 `.claspignore` 파생 `liveFiles()` 로
가르고(ENGINE_FILES 는 합본·순서 정본으로 존치) `tests/두뇌.test.js` 12검사 부활(08-19 대철거가 걷은
제품 몫 — S5 계보 · 현행 코드 12/12 초록) · **G9** = systemWatchdog 미개통 «정보» 채널(감시 4곳이 요구를
접어 침묵이던 자리 — 적색 아님) · **G10** = 성과계기판 ⑥승격 격차 계수 신설(**첫 실측 의사 16·발행 0·
격차 16 🔴**) + [자동] 4단 주체 명문(talk 검수 뒷단 — appsscript 0 은 맞는 0) · **S7** = «하루 갈림»
전제 기각(같은 실행) 대신 진짜 병 둘: 급수 «한 번도 없었음» → 스윕 아홉째 열(「현재급수」 이름 찾기) +
서버 급수정규화·급수갱신 배선(talk `fbf3d2b`) · 반비움 잔존은 계수로 노출.
⇒ **심문 0822 문서 실물 기준 전건 종결.** 이번에 새로 선 유호 판정 자리 셋은 **같은 날 밤 처분 완료**
(유호 확정 「그대로 진행」 · 결정.md 08-24 등재 — 축 = 손 최소·나중 문제 0·최신 기능):
ⓐ반 «비움» = 현행(빈 칸=모름 · 잔존 계수) **정식 확정·종결** — 실물상 빈 반 칸의 정상 경로가 없다
(퇴소=행 삭제 v9.34 · 이동=값 교체)라 빈 칸은 실수 후보가 맞다 · «반 없이 재원» 첫 실사례 날 그 자리에서 명시어
ⓑmastery `grammar_id`↔S6 합류 = **FSRS 교체 판정 ⓓ에 묶음**(Ⅲ⑤ 줄이 든다 — 그날 최신 모델로 매핑 초안+
잇기/백지 비교표 AI 완성형 → 유호 픽 1회) ⓒ`data_use.granted` 생산자 = **개원전_일괄판정_큐 Q8 등재**
(당김 트리거 = 첫 실학생 승격 의사 · 그때까지 격차 계수가 든다). 기존 유호 칸 둘 = §8-B 채점(절차 = talk `evals/생성결과.md` 꼬리 명령 한 줄) ·
CI 교차 PAT 그대로. 곁 관찰 셋(적재실패 outcome='내부오류' 이름 재사용 · 죽은 왕복의 활성 잔존 ·
**포인트 사유 «리그승리» 1건 미인식** — v9.262 워치독 원격 실측 08-24 밤: reason 분류 표(도전·성장·
숙제·업적)에 없는 문구라 버튼 문구/분류 표 어느 쪽이 낡았는지 게임·포인트 레인이 판정할 것.
같은 실측의 다른 ⚠ 다섯(트리거 실종·백업 폴더·폼 2 열기 실패)은 **교차 계정 관점 경보**다 —
unmet23 로 편집기 실행하면 원장 계정 소유 트리거·폴더·폼이 안 보인다. 실물은 대시보드 실측
(트리거 12 · 오류 0%)이 반박 · 진짜 판정은 월요일 원장 계정 자동 워치독 메일이 진다).
- ✅ **「9/1 요금 2배」는 소멸했다**(08-22 벤더 문서 실측 — Sonnet 5 $2/$10 이 표준가가 됐고 인상은 취소).
  ⇒ 서두를 값 근거 0 · 개원 전 점검 목록의 그 칸도 같이 죽는다.

**개원 후 조건**: ⑨ 숙련 추정 — 데이터가 쌓여야 시작한다.

---

## 2. Loom — 앱 UI 를 펠트 실물로 (유호 확정 08-20 「전부 대체」)

CSS 목업은 임시 비계다. 화면·부품을 블렌더로 굽는다.

### 2-A. 화면 굽기
| 화면 | 상태 |
|---|---|
| 퀴즈(조합판) | ✅ v1 채택·이식 (4a3c70a8 · 보조줄 수리 3f830220) |
| 나의 성장(성장판) | ✅ v1 채택·이식 (ffc2faca) |
| **오늘 탭** | ✅ **v1 채택**(유호 08-23) · 넷째 줄 「다시쓰기(학3)」 포함 · 대비 13블록 AAA |
| **소식 탭** | ✅ **v1 채택**(유호 08-23) · 대비 18블록 AAA |
| **학부모 · 우리 아이** | 🟡 **v1 시안 — ⏳유호님 판정 대기**(08-23 · 새 부품 0 · 대비 20블록 AAA) |
| **학부모 · 리포트** | 🟡 **v1 시안 — ⏳유호님 판정 대기**(08-23 · 대비 16블록 AAA) |
| **학부모 · 소식** | 🟡 **v1 시안 — ⏳유호님 판정 대기**(08-23 · 대비 19블록 AAA) |

| **강사 · 오늘 수업 / 학생 / 검수 / 기록** | 🟡 **v1 시안 넷 — ⏳유호님 판정 대기**(08-23 · 전부 AAA) |
| **원장 · 오늘 / 학생 / 경영** | 🟡 **v1 시안 셋 — ⏳유호님 판정 대기**(08-23 · 전부 AAA) |

✅ **화면 열넷이 다 섰다**(08-23) — 학생 넷(퀴즈·오늘·나의 성장·소식) · 학부모 셋 ·
   **강사 넷**(오늘 수업·학생·검수·기록) · **원장 셋**(오늘·학생·경영). 네 역할이 각자 화면으로 하루를 돈다.
🔑 **열넷 내리 새 부품이 0개다.** 늘어난 것은 «자리»와 «본문»뿐이다.
🔑 탭을 가른 잣대가 역할마다 다르다 — 학생·학부모는 **시제**(현재·누적·도착), 강사는 **행동**
   (지금 교실에서·한 사람을 본다·AI 를 가르친다·남긴다), 원장은 **「팔 화면인가」**(유호 확정 §12-2).
⚠**강사만 탭이 넷**이라 탭바에 네 칸 자리를 새로 뒀다(x −1.02/−0.34/0.34/1.02).
   세 칸 값은 손대지 않았다 — 다섯 화면이 이미 채택됐다.
🔴 **강사 「검수」 탭이 이 앱에서 가장 중요한 화면이다** — Glide 가 폐기되면 강사 판정의 유일한
   입구가 죽는다(강7 · 전수분석 §5). 다른 화면이 다 늦어도 이것만은 파일럿 게이트 «전»에 선다.
공방 인계 = `docs/캐릭터/{강사,원장}공방_0823/인계.md`
📄 **판정 지면 = `docs/화면_시안.html`**(`node tools/화면시안.js` · 446KB · 자립형) — 열넷이
   공방 일곱 곳에 흩어져 있어 «네 역할이 한 세계로 읽히는가»를 볼 자리가 없었다.
   **판정은 한 곳에서 해야 판정이다.** 요소·오브 시안과 같은 통로(`<!--펠트스킨-->` + 펠트문서.js).
⚙ 08-23 정리: 같은 `단추()` 가 다섯 벌이던 것을 `화면부품`·`화면단추`·`화면배경`·`화면탭바`
   넷으로 모았다(80줄 감소). **회귀로 확인** — 리팩터링 전/후 최대차 1/255(기준 잡음과 같다).
   곁들여 ①조합판·성장판의 커밋된 렌더가 `직물결()` 수리 이전 판이라 다시 구웠고
   ②`성장덧씌움.py`·`조합덧씌움3.py` 가 **없어진 `렌더/` 폴더**를 보고 있어 통째로 안 돌던 것을
   고쳤으며 ③채택판 이름 규약을 세 공방에서 통일했다(층별 = 꼬리 없음).

공방 인계 = `docs/캐릭터/조합공방_0821/인계.md` · `docs/캐릭터/성장공방_0821/인계.md` ·
`docs/캐릭터/오늘공방_0822/인계.md` · `docs/캐릭터/소식공방_0823/인계.md`
(채택값·함정 각주·명품 렌더 4계가 거기 있다 — 새 화면 굽기 전에 연다)

🔑 **학생 탭 셋이 다 섰고 08-23 전부 채택됐다**(결정.md). 세 탭을 가른 잣대는 하나 — **시제**다:
성장 = «어디까지 왔나»(과거의 누적) · 오늘 = «오늘 무엇을 하나»(현재의 할 일) ·
소식 = **«밖에서 나에게 온 것»**(도착물). 겹치면 탭이 셋일 까닭이 없어진다.
네 화면 내리 **새 부품이 0개**다 — 늘어난 것은 «자리»와 «본문»뿐이고, 그게 이 세계가
싸게 넓어지는 방식이다(소식 탭을 오늘 탭과 실제로 가르는 「날짜 열」조차 굽지 않고 PIL 이 붙인다).
세 탭 나란히 = `docs/캐릭터/소식공방_0823/탭셋_나란히.png`

🔑 **그 시제 잣대를 학부모 탭에 그대로 댄다**(08-23) — 오늘↔**우리 아이** · 나의 성장↔리포트 ·
소식↔소식. **역할이 달라도 시제가 같으면 부품을 하나도 새로 안 만들어도 된다**:
학부모 앱은 «다른 앱»이 아니라 같은 학원의 두 번째 창이다.
다섯 화면 나란히 = `docs/캐릭터/학부모공방_0823/다섯화면_나란히.png`

⚠**블렌더는 상대 출력 경로를 «드라이브 루트»로 해석한다** — `출력=docs/…` 를 주면 렌더가
`C:\docs\…` 로 떨어진다. 고약한 것은 **칸 JSON 은 제자리에 생긴다**는 점이라
「JSON 은 있는데 PNG 가 없다」로 나타난다(실측 08-23 · `C:\docs\` 에 옛 굽기가 쌓여 있어 같이 치웠다).
⇒ **화면 굽기의 출력은 언제나 절대 경로로 준다.**

### 2-B·2-C. 요소 트랙 — ✅ **닫혔다** (08-22 · 유호 지시 「마쳐야하잖아」·「전부 해줘」)

네 갈래 전부 끝났다 — ①김밥 ②천(결) ③부품 배선 ④조작 여섯.

| 무엇 | 실물 |
|---|---|
| 세트 굽기 | **138/138장** (숫자26·자모31·넘버쿠키10·기호8·요일7·살림6·**음식19**·**초밥19**·되굽기2·조작10) · 1000px · 전수 디코딩 통과·실패 0 |
| 음식 확장 | 3 → **19종**(유호 08-22 「음식이 조금 더 많이」 → 「삼겹살같은 한식도 퀄리티있게」 → 「게장같은건 어렵겠지?」·「계란찜도 있으면」). 끼니 역할이 안 겹치게: 구이2·밥1·튀김1·면2·전1·찌개1·찜1·해물1·떡1·분식3·과일2·음료1 |
| 초밥 신설 | **19종**(니기리10·군함2·마키4·유부1·모둠1·엔가와1). 곁들이는 두 갈래 — **낱개 = 초밥 + 생강 하나 · 모둠 = 한 상 전부**(젓가락·받침·레몬·차·물수건·간장) |
| 명품화 3차(08-23) | 유호 「전부 자연스럽게 윤기있게 정말 맛있어보였으면. 배고파지도록말야!」 → 식탁 조명 + `윤기등` · 국물 거칠기 0.54→0.34 · 털 걷기 셋(파전·호떡·붕어빵) · 건더기로 국물 덮기 · 김치 14판 |
| 부품 굽기 | **10종 20장**(몸+접지 알파 두 층) → `구운재질.json` **18종** · 지면에 실리는 무게 **314KB** |
| 지면 배선 | 자리를 얻은 다섯 = **체크·별·하트·도장·말풍선**. 탭 얼굴 넷은 자산까지(앱은 형제 저장소 SYNK-talk · 같은 JSON 을 읽으면 된다) |
| 판정 지면 | `docs/요소_시안.html` — **10세트 138장 · 1083KB** · 자립형(인라인 안 된 그림 0). 대조판 두 장 = `docs/캐릭터/요소공방_0822/{음식,초밥}_대조판.png` |

안 배선한 것과 까닭: **숫자·자모·요일** = 글자마다 한 장이라 CSS `counter` 로 못 고르고 본문 활자는
불가침이다(교보재 카드·인쇄물에서는 그림 그대로 쓴다 — 그건 배선이 아니라 편집이다).
**불릿**은 레진 무채로 남는다 — 7px 자리에 보풀을 깔면 얼룩으로만 읽힌다.

굽기 통로의 배움은 §0 「겹침 규율」로 옮겼다(끊긴 굽기가 성공처럼 보이는 것 · 분리 실행 · 파일 수 판정).

**음식 갈래에서 여덟 번 되풀이한 실패 하나** — 작은 덩이(어묵·김치·치킨·송편·초밥샤리·비빔밥밥·
파전·호떡·붕어빵)에 털을 심으면
**실루엣이 죽는다.** 길이를 내리고 뭉침을 올려도 안 됐고, 개수를 올리자 이번엔 iGPU 드라이버가
죽었다(26000×3덩이 · 5분 뒤 크래시 — 한계는 «오브젝트당»이 아니라 «장면 전체»다).
⇒ 바삭함·쫀득함은 털이 아니라 **잔 범프**(직물결 지름 0.30·세기 0.95)가 낸다. 털이 값을 하는 것은
귤 껍질처럼 **털이 곧 재료일 때**뿐이고, 그때도 큰 덩이 하나에서만이다.
⚠비균등 스케일은 **구에도** 걸린다 — `scale=(1,1,0.34)` 는 같은 길이 값을 축마다 다른 길이로 만든다.

**08-23 에 새로 선 규율 둘** (음식 명품화 3차에서 값을 치르고 얻었다)
1. 🔑 **식탁 조명(키 2000) + AgX 에서 «밝은 색»은 전부 흰색이 된다.** 하루에 세 번 같은 자리를
   밟았다 — 양념가루(Coral 3) · 김치 보시기(Paper→Stone 도 하얬다) · 김치 잎맥(Coral Soft).
   ⇒ **신호는 밝음이 아니라 «어둠»으로 낸다.** 팔레트에서 두 단 내려도 이 조명엔 한 단도 안 된다.
2. 🔑 **국물이 국물로 보이는 것은 국물이 «많이» 보여서가 아니라 건더기 «사이로» 보여서다.**
   떡볶이·라면·김치찌개가 「붉은 팬케이크」였던 진짜 까닭은 색이 아니라 **면적**이었다
   (건더기가 국물의 절반도 못 덮었다). 색 탓으로 보고 세 판을 헤맨 뒤에야 실물을 크게 열어 봤다.
3. 🔴 **색 판단은 «재고» 말한다 — 나란히 놓고 보는 것으로는 못 가른다.**
   나는 배율 1.00/0.58/0.34 세 판을 눈으로 보고 「거의 같다 → 배율은 안 먹는다」고 **유호님께
   보고까지 했다.** 픽셀로 재니 (227,155,144)→(211,142,132)→(194,134,123) 로 분명히 움직이고
   있었다(08-23 철회). 4계 ④ 가 색에도 그대로 걸린다.
   ⇒ 손잡이는 둘이고 **반사(Specular IOR Level)가 배율보다 세다** — 젖은 면의 붉음은 절반이
     «반사»이고 반사는 바탕색을 안 탄다. `매끈재질(…, 반사=)` · `국그릇(…, 배율=)` 로 열어 뒀다.
   ⇒ 그리고 **색은 홀로 정해지지 않는다**: 국물을 어둡게 하자 찌개김치·뚝배기·돌솥·반찬김치가
     차례로 어긋났다. 한 접시의 값을 바꾸면 «옆 것»을 다시 본다.

### 2-E. 부품 사전 미이행분 — 새 부품 **열넷 / 18장** ✅ **08-23 닫혔다**
(유호 지시 08-23 「ui ux 요소들이 충분히 있는거 맞아? … 무심코 디테일하게 명품화 못하고 지나간
 요소들이 있다면 한번씩 체크해서 발전시켜줘」)

🔴 **큐가 빈 게 아니라 큐를 세는 방식이 틀려 있었다.** 사전(`docs/양모공방_요소사전.md`) §5 는
「▶다음 굽기 후보: 지금은 비어 있다」고 적고 있었는데, 기계로 재니 **자기 표의 부품 줄 45 중
12줄에 실물이 없었다.** 판정을 「✅ 를 손으로 다는 것」에 걸어 둔 탓이다.
⇒ **판정을 «등록부에 함수가 있는가»로 옮기고 `node tools/부품대조.js` 가 매번 돌린다.**
  반대 시험까지 했다(등록부에서 한 형태를 빼면 잡는가 · 대응표에 없는 줄이 오면 잡는가 — 둘 다 잡는다).

| 갈래 | 부품 | 통로 |
|---|---|---|
| 1-a 조작 | 단추위계(주·보조·비활성 한 판) · 라디오 · 스와이프 · 풀다운 · 드래그 | `--세트 조작2` |
| 1-b 표시 | 뱃지(1·3·12) · 코치마크 · 시접자 | `--세트 표시2` |
| 1-c 구조 + 새 자리 | 파이핑(헤더·섹션) · 주머니 · **녹음 3상태** · 내려놓은바늘 · 고르개 · 접힘 | `--세트 구조2` |

🔑 **표에 «줄조차 없던» 자리 넷을 찾아 세웠다** — 녹음 · 오류/오프라인 · 드롭다운 · 아코디언.
   그중 **녹음이 가장 큰 구멍**이었다: 사전 §2 가 「별도 트랙 소관(`1cfa648d` 계열)」으로 넘겼는데
   그 트랙에 자산이 **0** 이었다. 우리는 «말하기» 학원이고 학생이 하루에 가장 여러 번 누르는 단추다.
   ⇒ **남에게 넘긴 자리는 아무도 안 본다.** 넘길 때는 넘긴 곳의 실물을 같이 재야 한다.
🔑 녹음의 은유는 새로 짓지 않았다 — 사전 §0 의 바느질 사다리를 **시간축으로** 읽었다:
   대기 = ⓪빈 구멍 · 말하는중 = ②시침(성긴 긴 땀 + 바늘까지 이어진 실) · 맺음 = ③꿰맴 + 끝매듭.
   **말한 것이 곧 한 줄의 땀**이라, 오늘판의 「말한 학생」·달력의 V·성장 게이지가 한 물건이 된다.

**네 판을 돌려 잡은 것 — 셋 다 이 저장소가 이미 적어 둔 함정이었다**(사전 §5 함정 7·8·9 로 이식)
1. 🔴 **무채는 보풀을 입으면 «밝기와 무관하게» 흰 솜이 된다.** 1판에서 Ash Wool 이 씻겨 Deep Wool 로
   내렸는데 **Deep Wool 보풀도 똑같이 씻겼다**(네 판이 같은 병 · 단추위계 보조가 코랄보다 밝아 **위계가
   뒤집혔다**). ⇒ 가족 규율 = **무채 면은 «결», 유채만 «털».** 탭 전환의 「지금 탭만 털을 입는다」와 같은 명제다.
2. **구도는 「거리」와 「중심」이 따로 논다.** 「거리 ≈ 5.1×반폭」은 가로 판 전용이다(센서 AUTO 가 긴 변에
   붙어 세로 판은 보이는 «폭»이 1/비율 로 준다). 카메라 z 오프셋도 위·아래 끝을 그만큼 옮긴다.
   ⇒ 잘림은 «내용 bbox» 가 아니라 **테두리 한 줄의 최대 밝기**로 잰다(배경 1 · 잘림 100~173 을 실제로 잡았다).
3. 🔴 **`Rx(90°)` 는 원기둥 축(+Z)을 −Y = 카메라 쪽으로 보낸다** — 바늘 둘이 길이 1.6 짜리인데 «점»으로
   찍혔고, 네 판을 「바늘이 작다」로 오진하며 두 번 키웠다. 눕히는 것은 **`Ry(90°)`** 다(게장 주석의 그 함정).

⚠ **1판 18장 중 11장이 못 쓸 상태였다** — 그걸 눈이 아니라 «재서» 알았다(여백%·유채%·최밝·결σ²).
   4판까지 돌린 뒤 잘림 0 · 최소 여백 1.7% · 모든 유채 판에서 **코랄이 최밝**을 되찾았다.

**🎞 현실감 — 손잡이 셋을 따로 켜서 재고 둘을 채택**(유호 08-23 「조금 더 현실감있게」)
| 손잡이 | 판정 | 실측 근거 |
|---|---|---|
| **속살**(서브서피스) | ✅채택 | 🔴 이 저장소에 서브서피스가 **한 건도 없었다** — 재질이 순수 확산이라 물리적으로 «무광 플라스틱». 실루엣 띠 밝기 35→40 · 35→43 · 97→102(셋 다 오름) |
| **심도** f/0.9 | ✅채택 | f/2.2 는 심도가 물체보다 깊어 **무효**였다. 열어서 재니 깊이 있는 부품에서만 값을 한다 — 폼폼 바깥띠 결 698→621, 평평한 단추위계 441→439 |
| **섬유**(Hair BSDF) | ✅조건부 채택(08-25) | 08-23 엔 기각(**코랄이 하얗게 씻긴다** — 뱃지 최대차 106·평균 15.1). 08-25 디테일 프로브 11판 뒤 유호 「조합판 최고다」로 승격 — 단 «유채도 문턱»(크로마 ≤0.16, `요소굽기.py`) 아래 무채·연한 털만. Soft 몸 넷은 미검증 → 착수 때 확인 1장 |
| **잔털**(보풀 둘째 층) | ✅채택(08-25) | 같은 확정. 본털에 비례(길이 ×1.05 · 개수 ×0.126)로 `짧은퍼`에 상주 — 절대값 이식은 «벌레 다리»(유호 반려 8판) 재발 |

⏳ **남은 격차 = 해상도.** 퍼프로브 23 + 오브 26 + 글자 3 = **52장이 1800px** 인데 요소 156장·화면 14장은
1000px 이다. 전량 1800px+속살+심도로 맞추는 값 = 실측 기준 **≈13시간**(1800px 한 장: 결만 29초 ·
털 하나 119초 · 긴 보풀 폼폼 **572초** — 값은 털이 정한다). 노이즈는 128 샘플로 충분(잔차 3.63).
**08-23 진행분**: 조작 10 + 새 부품 18 = **28장 1800px+속살+심도 완료**(1시간 42분 · 실패 0).
🔴 그러면서 **소급 누락**이 드러났다 — 함정 7 을 새 부품에만 걸고 08-22 조작 여섯을 안 봤다:
모달 카드 · 토스트 태그 · 슬라이더 트랙이 흰 덩어리였고(무채에 보풀), 셋 다 결로 내려 다시 구웠다.
**규율을 새로 세운 세션이 옛 부품을 다시 훑지 않으면 규율은 절반만 산다.**
✅**요소 세트 전량 1800px 완료**(08-24 실측: 156/156 · 미달 0 — 옆 세션이 「세트 전부 --너비 1800 --다시」로 완주).
🔄 **그런데 그 판은 유호 검수에서 반려됐다**(08-24 「156장+화면이 전체적으로 많이 아쉬워」) —
   **해상도가 아니라 색이 문제였다.** 아래 2-F 참조.
✅**명품 재굽기 완주**(08-24 15:41 · `2f928ba4`) — 요소 156 + 화면 14 + 덧씌움 14 를
   색관리 PBR중립 · 노출 +0.25 · 샘플 256 · 1800px 로 다시 구웠다.
✅**화면 14장 ⛔ 해제** — 유호 직접 지시 08-24 「모든 요소들 제대로 1800px 명품화 굽기 재 진행해줘 ·
   9시간 이상 걸려도 좋아」. 위 도구가 요소 뒤에 이어서 굽는다(통로가 이제 표로 서 있다 — 인계문
   예시 한 줄에 기대던 자리를 `tools/명품재굽기.js` 의 «화면들» 표가 대신한다).
⚠기록 정정: 커밋 92782bb3 메시지가 「트랙에 ⛔를 못 박는다」고 적었으나 그 편집은 이스케이프 오류로
실제로는 안 실렸었다(문서_지도만 실림) — 이 줄이 그 정정이다.

### 2-F. 명품 재굽기 — 「색이 씻긴다」의 진범을 08-24 에 잡았다 🔴**2차 반려 — 새 자가 «쨍하다»(08-24 밤)**

🔴 **이 저장소는 색 관리(뷰 변환)를 한 번도 안 만지고 있었다.** 블렌더 기본 AgX 가 그대로 걸려
   채도를 눌렀고, 우리는 그 결과와 **재료를 바꿔가며** 싸웠다 — 「밝은 색은 전부 흰색이 된다」·
   「연어핑크로 씻긴다」·**김치 15판**·「두 단 내려도 한 단이 들어올려진다」. 전부 같은 병의 증상이다.
   **자를 안 보고 열다섯 번 잰 것이다.**

실측 A/B(대표 넷 × 네 벌 · 나란히 보고 고르지 않고 «재서» 골랐다 — 08-23 색 오보의 그 배움):

| 변주 | 채도 | 대비 | 판정 |
|---|---|---|---|
| **Khronos PBR Neutral** | **+55~73%** | **+3.7~29.3%** | ✅채택 — 대비가 오른 유일한 변주 |
| AgX - Punchy | +25~30% | −2~12% | 기각 |
| Filmic | −4~10% | −3~13% | 기각 |
| 노출 +0.25 | (중립의 −12~18% 밝기를 되민다) | | ✅채택 |
| 샘플 256 | 또렷함 **+25.8%**(털에서 특히) | | ✅채택 · 512 는 2배 값에 +6.9% 뿐 |

⚠**색을 조명에 맞춰 손으로 내려 둔 자리들**은 이제 «두 번» 보정된 상태다. 재굽기 뒤 훑고
  과보정된 것만 팔레트 값으로 되돌린다. 🔑그때 규율은 「신호는 어둠으로 낸다」가 아니라
  **「자를 먼저 본다」**로 바뀐다.
  **눈대중 말고 이 여덟을 짚는다**(08-24 기계 추출 · 「조명이 들어올려서 내렸다」고 주석이 자백한 자리):

| 형태 | 줄 | 손으로 내린 것 |
|---|---|---|
| 떡볶이 | 1967 | Coral 3 → **Coral Rim**(한 단 깊게 · 「연어핑크로 씻긴다」) |
| 김치 | 2122·2124 | Paper → Stone → **두 단**(그래도 새하얬다 · 10판) |
| 비빔밥 | 2387 | Graphite 4 가 「밝은 시멘트」로 씻겨 손봄 |
| 치킨 | 2463 | Butter → **Butter Deep**(두 단 · 튀김옷) |
| 파전 | 2548 | **두 단**(「부친 색」) |
| 송편 | 2647 | 두 단 + **배율로 마저** |
| 양념가루 | 4184 | Coral Rim `#941F19` — 노출·거칠기·배율·스페큘러 다 재보고 기각한 자리 |
| 국그릇 | 4216 | 뚝배기·돌솥 Graphite(「회색 시멘트」) |

  🔑 여덟 중 일곱이 **식탁 조명**이다. 새 자에서 이 조명이 얼마나 덜 들어올리는지부터 재고,
    그 값 하나로 여덟을 같이 판정한다 — 하나씩 눈으로 고치면 김치 15판이 여덟 번 되풀이된다.

🔑 **옛 판(AgX)은 `4a4d2472` 에 보존돼 있다** — 옆 세션이 재굽기 «직전»(08-24 06:5x)에 156장을
  커밋해 뒀다. 그래서 이번엔 덮어써도 안 사라지고 **A/B 대조 재료로 남는다**(전에는 덮으면 끝이었다).
  ⚠**「이미 커밋됐네」로 재굽기를 건너뛰지 마라** — 그 커밋은 «새 색 이전» 판이다(피어 지적 08-24).

✅ **1차 완주 = 요소 156 + 화면 14 + 덧씌움 14**(07:12 착수 → 15:41 · 커밋 `2f928ba4`).
  「두 번 보정된 여덟 자리」 우려는 `굽기대조.py` 실측으로 **해소**됐다 — 채도 +39~61% 오르는 동안
  밝기 최대 하락이 삼겹살 −7.7% 뿐이라, 팔레트로 되돌릴 자리가 없었다.

#### 🔴 2차 반려 (08-24 22:1x) — 완주한 그 자(PBR 중립)가 «쨍하다»

유호: 「조명이 너무 밝아서 Loom 의 가장 큰 장점인 **현실에서 사는 느낌**이 안 나. 그냥 AI로 만든
쨍한 느낌밖에」. **오늘 새 자로 구운 222장 전부**(요소 156 · 화면 14 · 오브 26 · 퍼프로브 23 · 글자 3)
+ NPC 검증 4장이 이 판이다. 어제까지 판은 옛 자(AgX)라 해당 없다.

🔑 **병인 = 판정 축.** 아침 반려(「씻긴다」)를 받고 새 자를 **숫자 둘(채도·대비)**로 골랐다 —
  「채도 오르면 이긴다」는 잣대 자체가 쨍함을 최적화한 것이고, «현실감(눈)» 축이 판정에 없었다.
  기술적으로도 그렇다: **PBR 중립은 본래 제품사진용 변환**(색을 «정확히» 보존 = 제품샷·AI렌더 느낌)이고,
  필름의 하이라이트 감쇠로 «현실에서 찍은» 느낌을 내던 것이 바로 AgX 였다. 씻김을 고치려다 반대 극단으로 갔다.
⇒ **색·질감 채택은 숫자만으로 하지 않는다**(결정.md 08-24 등재). 새 자는 두 극 사이 — AgX 계열에서
  채도를 회복하는 후보 셋(Punchy · Punchy+노출0.15 · 중고대비)을 대표 5장으로 구워 **유호님 눈**이 고른다.
✅ **확정(08-24 23:0x) = AgX + Punchy 룩 + 노출 0** — 색시안판(대표 5 × 후보 3 + 반려 양끝) 뒤,
  «같은 3판 형상·자만 바꾼» NPC 증명판에서 유호 픽 「아래 버전이 훨씬 낫네」. `요소굽기.py` 기본값 교체 완료.
  🔄 **밤샘 전량 재굽기 착수 23:22** — 명품재굽기 전부(222장 + 지면 3벌) → NPC 20장 + 시트.
  🔴 **그 밤샘은 세션 사망과 함께 죽었다**(08-25 01:0x — 세션이 띄운 백그라운드는 세션과 같이 죽는다).
  잔여 ≈ 요소 197 + 화면 14 + 라이브러리 52 + NPC 20. 잇는 통로 = 패드 `재개.js`(한 프로세스 · NPC 제외).
  ✅ **08-25 아침 — 털 디테일 정본이 그 사이 «③조합판»으로 확정됐다**(디테일 프로브 11판 · 결정.md):
  섬유(유채도 가드)+잔털이 `요소굽기.py` 기본실감으로, 전진(−0.248)이 `NPC세트굽기.py`로 학습 완료.
  ⏸ **전량 착수는 유호 보류 지시**(「일단 시작하지말고 loom엔진에 학습시켜줘」) — 착수 신호가 오면
  ①Soft 몸색 확인 1장(섬유문턱 재판정) → ②재개.js 차례 → ③NPC 20장(몸색 확정 동반) 순서다.

#### ✅ 갈래별 자 분리 — **화면 열넷만 옛 AgX 맨판** (유호 픽 08-24 밤 「화면만 옛 판으로 가자」)

첫 화면 시험(오늘판 본문까지)을 내가 「통과」로 보고했는데 유호님이 **왼쪽(옛 판)이 낫다**고 하셨다.
🔴 그때 낸 비교판이 **공정하지 않았다** — 옛 1080px·새 1800px 를 같은 높이로 눌러 담아 새 판만
두 배 세게 축소됐다(0.43배 대 0.26배). 같은 화소 크기로 다시 내고서야 판단 재료가 정직해졌다.
🔑 **같은 자가 갈래에 따라 반대로 작동한다** — 렌더는 «밝은 물건이 검은 무대에» 서니 하이라이트
  감쇠가 디테일을 살리고, 화면은 «판 자체가 어두운 것»이라 살릴 것 없이 더 눌러버린다.
  실측(그릇만): 밝기 22.3→5.9(**−74%**) · 대비 18.4→16.3(**−11%**) — NPC 에서 털이 산 것과 정반대.
⇒ `명품재굽기.js` 의 `화면자` 상수 한 곳(`색관리=AgX 룩=None 노출=0`)을 화면굽기·빠진것다시가 같이 쓴다.
⚠**밤샘 프로세스는 23:22 에 뜬 옛 코드를 메모리에 들고 있어 화면을 Punchy 로 한 번 굽는다** —
  끝나면 패드 `아침화면.js` 감시가 `--화면만`(새 코드 = 옛 자)으로 덮는다. 그 뒤가 정본이다.
⚠재검토 요청 뒤에도 유호 재확인 「그대로 가자」(트렌디함은 자가 아니라 모션·글로우·햅틱에서 가져온다).

#### 🌙 08-25 밤샘 — **털 정본 확정 뒤의 전량**이 돈다 (유호 07:0x 「30분 뒤 시작 · 나머지 전부」)

🔑 **왜 또 전량인가**: 08-24 밤엔 «색»이 바뀌었고, 08-25 아침엔 «털»이 바뀌었다(③조합판 · `8ab57c20`).
  섬유+잔털이 기본실감이 됐으니 어제 구운 것도 전부 옛 털이다 — `--빠진것`(mtime)은 이걸 못 본다.
🔑 **세션에서 떼어내 띄웠다**(07:03:20 · PID 고아화 확인). 08-24 밤 세션 사망으로 밤샘이 같이 죽은
  자리의 대책이다. ⚠**패드는 세션마다 다르므로 절대경로로 적는다**(다음 세션은 자기 패드를 본다):
  `C:\Users\q1212\AppData\Local\Temp\claude\C--Users-q1212-Documents-SYNK-appsscript\4604e744-bd4f-4470-b9b6-bf481615665e\scratchpad\`
  안의 `밤샘_0825.js`(통로) · `밤샘_0825.log`(단계·종료코드) · `밤샘_0825.out`(자식 도구 출력).
  순서 ①30분 대기(07:33:29 착수) → ②명품재굽기 전량(222+지면+검사) → ③NPC 몸색 재료 8 → ④NPC 20 → ⑤시트.

🌓 **08-25 낮 방침 전환 — 「잘 때만 굽는다」**(유호 「다른 작업 하고 잘때만 굽는게 제일 효율적일것같아」)
  굽기가 낮에 돌면 GPU·RAM 을 다 먹어 다른 작업이 같이 느려진다. ⇒ **요소 156 이 끝나는 자리에서
  1차를 멈추고 커밋**하고, 나머지는 취침 전에 건다. 2차 통로 = 패드 `밤굽기_2차.js`
  (①화면 14+덧씌움 → ②라이브러리 52 → ③NPC 몸색 재료 8 → ④NPC 20 → ⑤시트 → ⑥검사).
  쓰기: `node 밤굽기_2차.js [대기분]` · 세션 분리로 띄운다(Start-Process 고아화 확인까지).
⏱ **시간은 판정 축이 아니다** — 유호 08-25 「총 시간은 영향없어 12시간 넘게 해도 돼」.
  그래서 샘플 256·1800px 를 안 깎았고, 몸색 재료도 5장 → **8장**(A안 넷 × 가드켬/섬유강제)으로 늘렸다.
  ✅실측 완주: 폼폼(최악 = 긴 보풀) 1800px·256 = **934초(15.6분)** · 옛 판 572초 대비 **1.63배**.
  ⚠단 이 배수를 전량에 그대로 곱하면 과대추정이다 — 잔털은 «털 있는 것»에만 붙으므로
  결만 있는 부품(옛 29초)은 안 늘어난다. 평균 1.3~1.5배로 보아 전량 **17~20시간** 안팎.
  정확한 값은 밤샘 로그의 단계별 「끝 · N분」이 말한다(추정 대신 그걸 인용한다).

🔴 **오진 1건과 그 정정 — 「전후」의 «전»이 무엇인지 먼저 묻는다 (08-25 14:3x)**
  14:30 에 요소 넷(붕어빵·도장·자모·토스트)을 «전후»로 재고 「채도가 6~15% 떨어졌다, 잔털이
  면에 그림자를 드리운다」고 유호님께 보고했다. **틀렸다.** 꺼내 쓴 «전» 네 장이 전부
  커밋 `2f928ba4` = **반려된 PBR 중립 판**이었다 — 즉 잰 것은 «털 전후»가 아니라
  «반려색 대 확정색»이었고, 그 하락은 유호님이 물리신 **쨍함이 빠진 값**이었다.
  🔑 진범을 가른 것은 **털이 없는 물건**이었다: 도장 «참»은 직물결·분필재질만 쓰는데(짧은퍼 0회)
    똑같이 −25.6% 가 났다. 못을 박으려고 세 판을 같은 자로 구워 재니 **소수점까지 동일**했다 —
    지금(섬유+잔털) · 잔털 뺀 · 섬유 뺀 셋 다 밝기 148.510 · 채도 0.20316(차이 0.000000).
    털 손잡이가 원인이면 원리상 나올 수 없는 결과다 ⇒ −25.6% 는 **전량이 색 교체의 값**이다.
  ✅ **못을 두 번 박았다 — 털 «있는» 요소를 색 맞춰 다시 쟀다**(토스트 · 둘 다 AgX Punchy):
    옛 털(속살+심도) 밝기 51.653·채도 0.27234 → 새 털(③조합판) 51.554·0.27166
    ⇒ **밝기 −0.2% · 채도 −0.3%** (앞서 틀리게 잰 값 −22.6%/−15.1% 와 대조하라).
    **새 털은 색을 안 해친다.** 털이 주인공인 물건에서만 밝기가 내려가고(폼폼 −19.6% = 부피),
    털이 일부인 물건은 사실상 무변화, 털이 없는 물건은 완전 무변화다 — 셋이 한 방향으로 맞는다.
  ⇒ **대조판을 만들기 전에 «전»의 출처 커밋을 먼저 확인한다**(`git log -1 -- <경로>`).
    같은 폴더에 있다고 같은 자로 구워진 것이 아니다 — 이 저장소는 08-24~25 에 색이 두 번,
    털이 한 번 갈렸다. 그리고 **의심 갈래를 못 쓰는 물건**(여기선 털 없는 도장)이 가장 싼 검산이다.

✅ **유채 요소에서도 색은 안 씻긴다 — 폼폼(Coral) 실측 08-25 07:1x**
  유호님 판정은 «흰 몸(Oat) NPC»에서 났으므로 **유채 요소는 그때 안 본 자리**였다. 눈으로는
  새 판이 «조금 탁해» 보였는데, 공용 마스크로 재니 진단이 뒤집혔다:
  | 축 | 이전(털 옛판) | 지금(③조합판) | 변화 |
  |---|---|---|---|
  | 밝기 | 106.04 | 85.27 | **−19.6%** |
  | 가중채도 | 0.6611 | 0.6620 | **+0.1%** |
  ⇒ 채도가 그대로니 **씻김(하얘짐)이 아니다.** 어두워진 것은 잔털이 표면에 드리운 그림자,
  곧 **부피감**이다 — 유호 반려 「너무 쨍하다」와 **같은 방향**이라 되레 맞다.
  🔑 「탁하다」는 눈의 말이 명도와 채도를 안 가른다 — 그 둘을 갈라야 씻김과 부피를 구분한다.
  🔑 **이 측정이 유효한 까닭 = 폼폼은 어제 라이브러리 굽기에서 이미 AgX Punchy 로 구워져 있어
    «색이 양쪽 같았다»**(커밋 `02bf7523`). 위 오진과 정확히 반대 조건이라 털만 남아 잰 것이다.

| 순서 | 아침에 할 것 | 확인법 |
|---|---|---|
| 1 | 밤샘 어디까지 갔나 | 패드 `밤샘_0825.log` — 단계마다 「끝 · N분 · 종료코드」가 찍힌다 |
| 2 | 기계 검사 | `node tools/명품재굽기.js --검사 "2026-08-25 07:33"` → ✅ 0 이어야 |
| 3 | 유호님께 낼 것 | 시안 지면 3벌(요소·화면·오브) + `docs/NPC_시안.html` + 장당 시간 실측 |
| 4 | **NPC 몸색 판정** | `docs/캐릭터/NPC공방_0824/_몸색시안/` 5장 — A안 넷 + Butter Soft 섬유강제 1 |
| 5 | 섬유 문턱 재판정 | 그 5장에서 씻김이 없으면 `요소굽기.py` 섬유문턱 0.16 을 올린다(A안 넷은 0.165~0.31) |
| 6 | 전후 지면 다시 짓기 | `python tools/재굽기_전후.py` — 보류 상태(옛 프레임)라 최종 자 기준으로 재생성 |
⚠**NPC 20장은 현행 정본(몸색 Oat)으로 굽는다** — A안은 유호 미판정이라 표를 안 움직였다.
  판정이 A안이면 20장만 다시 굽는다(그 비용을 알고 고른 것이다 · 미확정을 전량에 박는 쪽이 더 비싸다).
⇒ 잃은 것은 없다 — 옛 판 222장 전량이 git 에 있다(`4a4d2472` · `2e60e210`). 비용은 재굽기 기계 시간뿐.
⚠`docs/재굽기_전후.html` 은 «채도 올랐다 = 이겼다» 프레임이라 **보류** — 새 자 확정 뒤 다시 짓는다.

#### 🔴 그 완주 «뒤에» 기계로 세니 세트가 **색으로 갈려 있었다** (08-24 16:1x)

폴더별로 「새 자(07:12 이후)로 난 PNG 가 몇 장인가」를 세자 이렇게 나왔다:

| 갈래 | 장수 | 새 색 | 자 |
|---|---|---|---|
| 요소공방·화면 열넷 | 170 | **170** | PBR 중립 ✅ |
| **오브공방_0821** | 26 | **0** | AgX(08-23 17:20) |
| **퍼프로브_0819** | 23 | **0** | AgX(08-23 17:39) |
| **글자공방_0820** | 3 | **0** | AgX(08-23 16:25) |

🔑 **까닭 = `명품재굽기.js` 가 그 셋을 «안 불렀다».** 통로는 따로 있었고(`오브굽기.js` ·
  `요소전량굽기.js`) 08-23 에 1800px 로 맞춘 뒤 손이 떠났다. **해상도로 갈리는 것을 막자고 세운
  도구가 색으로 갈리는 것은 못 막고 있었다** — 도구가 「전량」이라 이름 붙었는데 전량이 아니었다.
⇒ **`명품재굽기.js` 에 ③ 라이브러리를 못 박았다**(`--라이브러리만`). 이제 3D 로 굽는 자산 중
  이 도구가 안 부르는 것은 없다. 🔑안 굽는 둘은 일부러다 — `펠트코랄_0815`(마스코트) ·
  `펠트패치_0815` 는 **사진**이라 다시 구울 통로 자체가 없다.
⇒ `--검사` 도 그 52장을 같이 센다. 안 그러면 **「전량이 새 자로 났다」가 156/208 을 두고 참**이 된다.

🔴 **같은 굽기 판정 함정이 두 도구에 더 살아 있었다** — `오브굽기.js`·`요소전량굽기.js` 가 둘 다
  `existsSync && size > 2048` 로 성공을 쟀다. 재굽기에서는 **옛 판이 늘 그 자리에 있어 언제나 참**이다
  (`644716a4` 가 세트굽기에서 고친 바로 그 병 · 죽은 굽기가 「✅ 3547KB」로 찍힌다).
  ⇒ 둘 다 **「이 굽기가 새로 썼나」**(mtime)로 옮겼다. 규율을 세운 세션이 옆 도구를 안 훑으면
  규율은 절반만 산다 — §2-E 의 그 줄이 여기서 한 번 더 참이 됐다.

🔄 **도는 중** — `node tools/명품재굽기.js --라이브러리만` (16:19 착수 · 52장 · 1800px · 샘플 256).
  퍼프로브+글자 26 → 오브 26 순서. 끝나면 `오브_시안.html` 까지 한 벌로 다시 그린다.
  ⚠샘플을 256 으로 올린 것은 세트 표준을 따라간 것이다 — `요소전량굽기.js` 부품표는 96/128 에
  머물러 있었고(08-22 「해상도 하나만 바꾼다」의 잔재), 그대로 두면 **또 갈린다.** `--샘플` 로 덮는다.

### 2-G. NPC 배지 세트 — 유호 반려 08-24, 실측이 그 말을 그대로 확인했다

유호 지적: 「모든 npc가 다 똑같이 생겼고 특정부분은 부자연스럽고 전체적으로 표현력이 좀 약하다」.
자로 재니 정확했다(옆 세션 산출 20장 중 boss·prof·lead·insp × calm 실측):

| 잰 것 | 값 | 뜻 |
|---|---|---|
| 역끼리 다른 화소(32/255 초과) | **0.6~1.5%** | **네 역이 98.5~99.4% 같은 그림**이다. 가장 비슷한 lead↔insp 는 99.4% |
| 몸 평균 채도 | **0.066~0.076** | 거의 무채 — **함정 7 그대로**(무채 + 털 = 흰 솜). 흰 화소 11% |
| 주인공이 쓰는 화면 | **31%** | 나머지 69%가 판·배경. 표현이 실릴 자리가 없다 |

🔑 까닭은 코드에 그대로 있다 — **몸이 넷 다 같다**(같은 반지름 0.74 · 같은 Oat · 같은 털 19000 ·
   같은 22땀 · 같은 눈 자리 ±0.25). **다른 것은 소품 하나뿐**이다(나비넥타이/안경/넥타이/모자챙).
   ⇒ 처방 셋: ①역마다 **몸 색을 유채로 갈라** 함정 7 을 벗는다 ②크기·비례를 갈라 실루엣부터
   구분되게 한다 ③눈썹·볼 등 표정 요소를 얹는다(지금은 눈 둘 + 입 하나뿐).
🔄 **08-24 저녁 — 유호님이 이 갈래를 되살렸다**: 「굽기 끝나면 NPC 20장도 이어서 구워줘」.
  ⇒ **통로를 저장소가 쥔다**: `tools/NPC세트굽기.py`(장면 · 처방 ② 집행분) + `tools/NPC굽기.js`(배치 20장).
  패드에만 있던 것을 반입하며 `REPO` 하드코딩을 `__file__` 기준으로 뗐다. 산출 = `docs/캐릭터/NPC공방_0824/`.
  **20장 = 무대 4(벨·책·클립보드·도장) + 배지 4역 × 4상태(calm·lean·win·back).**
  🔄**08-24 18:16 — 이어굽기 감시가 걸려 있다**(유호 지시 「굽기 끝나면 검증하고 바로 본굽기 진행해」).
  스크래치패드 `4604e744…/NPC이어굽기.js` 가 라이브러리 52장이 GPU 를 놓기를 기다렸다가
  **①검증 4장(900px·96) → ②재기 → ③문턱 통과하면 본굽기 20장** 을 사람 없이 잇는다.
  문턱 = `lead↔insp` 실루엣 겹침 ≤88% **그리고** `lead÷insp` 실루엣 크기 ≥1.30배(2판 = 91.8% · 1.003배).
  ⚠**이 감시가 NPC 자리를 쥔다** — 손으로 따로 `NPC굽기.js` 를 부르지 않는다(두 벌이 겹치면 둘 다 느려진다).
  ⚠**옛 시안은 900px 이었다** — 새 판은 세트 표준 1800px · 샘플 256 이라 장당 값이 크게 오른다.
  첫 장을 재서 잔여 시간을 다시 낸다(어림으로 적지 않는다).

🔴 **굽기 «전»에 형상 2판을 재서 약한 자리를 잡았다** — `tools/NPC형상대조.py`(신설).
  반려 근거였던 그 잣대로 다시 재니 2판은 분명히 나아졌다(가장 비슷한 쌍 0.58%→4.35% · 5.8배).
  **그런데 실루엣만 떼어 재자 다른 게 보였다:**

| 잰 것 | 1판(반려) | 2판 | 3판(지금) |
|---|---|---|---|
| 실루엣 겹침 · 가장 많이 겹치는 쌍 | 99.4% | 91.8% (`lead↔insp`) | 재는 중 |
| 실루엣 겹침 · 평균 | 98.9% | 86.2% | |
| 가장 큰 역 ÷ 가장 작은 역 | 1.01배 | 1.16배 | |
| 몸 평균 채도 | 0.076 | 0.141 | |

  🔑 **`lead` 와 `insp` 의 실루엣 화소가 33561 대 33449 — 1.003배, 즉 «같은 크기»였다.**
    크기배를 0.88 대 0.78 로 갈라 놨는데도 그렇다. 까닭은 **모서리가 면적을 되돌려주기** 때문이다 —
    정원이 버리는 네 귀를 둥근사각이 도로 채운다. **크기배 하나로는 원과 사각을 못 가른다.**
    ⚠그리고 이건 「다른 화소 %」로는 **원리상 안 보인다**(배경이 69%라 희석한다). 실루엣은 따로 잰다.
  ⇒ 유호 지시 08-24 「lead 랑 insp 더 갈라서 굽자」. 3판은 손잡이 **셋을 한꺼번에** 건다:
    `lead` 0.88→**0.94** 정원 · `insp` 0.78→**0.70** + 가로세로 0.88/1.02→**1.02/0.86**(가로 블록) +
    크리스 0.52→**0.76**(모서리를 세운다). 하나만 걸면 또 상쇄된다.
  ⇒ 각짐(크리스)을 역형상 표로 끌어올렸다 — 값이 코드에 박혀 있으면 다음 사람이 손잡이인 줄 모른다.

✅ **집행자 = 스크래치패드 `3c750abe…` 세션(이 줄을 적는 세션이 그 세션이다 · 08-24 자진 신고).**
  ⚠**옛 줄 정정**: 「`NPC세트굽기.py` 는 저장소가 아니라 그 패드에 있다」는 위 반입으로 해소됐다.
  통로는 `tools/요소굽기.py` 를 **실행마다 런타임으로 읽어** 재질·조명·무대를 빌린다(베끼기 0) —
  그래서 2-F 의 색 기본값 변경(`16445cea` 07:12:20)이 이 갈래에도 원리상 걸린다.
  ✅ **다만 실현되지는 않았다**(08-24 파일 시각 실측): NPC 마지막 산출이 **07:09** 라 색 변경보다
  먼저다 — 새 자로 구워진 NPC 는 **0장**이고, 유호님이 보신 그림과 남은 산출은 **같은 자**(AgX)다.
  🔴 내가 피어 보고를 받고 「07:18 재개분은 새 색이라 버린다」고 이 줄에 적었던 것은 **틀렸다** —
  받은 말을 파일 시각으로 대조하지 않고 옮겼다(계보 실측 = 패드 `npc세트/색계보.md`).
  ⇒ **굽기는 07:2x 에 세웠고**(86 배치와 GPU 를 다투지 않는다 · 자리는 그쪽 것), 앱 반입용 투명
  산출 4장은 **지웠다** — 형상이 반려돼 못 쓰는 데다, 남겨 두면 다음 굽기가 `existsSync && size>2048`
  류 판정에서 「이미 있음 = 성공」으로 **오독한다**(피어 86 이 자기 배치에서 실물로 밟았다 ·
  파일 «수»로는 못 잡고 **시각**으로만 잡힌다 · `644716a4`). 시안 20장은 대조군이라 남긴다.
  🔑 **틀린 지목은 「담당자가 있다」는 착시를 만들어 그 자리를 아무도 안 잡게 한다**(피어 지적 08-24) —
  그 착시를 만든 것이 나였으므로 정정도 내 몫이다. 다만 **집행자가 정해졌다고 착수한다는 뜻은 아니다**:
  형상 처방 ①②③ 은 유호님 판단 자리이고, 그때까지 나도 안 굽는다(병렬 재설계 = 마스코트 10벌 공존의 재발).
🔑 **소각 지시는 보류 중이다 — 옛 평면 SVG 16종은 그대로 산다.** 시간선이 이렇다: 반려 «전» 에
  「이걸로 확정, SVG 소각하고 배선까지」 지시가 있었고 그 **뒤** 에 반려가 왔다. 자산 반입 직전에
  멈춘 덕에 되돌릴 자리가 남았다 — 다시 태울지는 새 판 확정 뒤 유호님이 가르실 자리다.
✅ **판정층은 먼저 섰다**(talk `888dc53` · `lib/NPC연출.js`+회귀 7). 형상과 «무관한» 부분만 골라 낸 것이다 —
  규격 3 이 정한 압박 3단 문턱·전이 자리·어휘는 몸을 다시 빚어도 그대로다. 화면 배선분(NPC 컴포넌트 ·
  4모듈)은 자산이 없으면 빌드가 깨지므로 **커밋하지 않고 패드에 패치로 보존**했다(트리는 되돌렸다 —
  미커밋 0). 🔑 착수 때 **다시 짓지 말고 그 패치를 꺼내 쓴다.**
  ⏳ 그 커밋이 정한 것 하나 — 규격 3 이 ⏳로 남겼던 「경과 시간이냐 남은 비율이냐」는 **비율**로 정했다
  (초로 두면 상한 30초에 돌아섬 문턱 30초가 되어 그 단이 원리상 0회 노출이다 · 첫 판이 그랬다).
⚠**순서 주의**(피어 지적 08-24 · 받아들임): 2-F 의 새 색은 이 반려를 **못 푼다.**
  「98.5~99.4% 같은 그림」은 뷰 변환이 만든 문제가 아니라 **형상**이 만든 문제다. 새 자로 다시 구우면
  «새 색으로 칠한 같은 그림»이 나올 뿐이다. ⇒ **처방 ①②③(형상)을 먼저 확정하고, 그다음 새 색으로 한 번에.**
  색은 형상의 대체재가 아니라 그 «위에 얹히는 층»이다.

### 2-H. 몽글 친구(투슬리스풍) — 시안 채택(유호 08-25 「너무 귀여워 마음에 들어」) · 표정 세트 진행 중

**선 것**: 본체 정본판 = `docs/캐릭터/친구공방_0825/몽글친구_v2.png`(1024·96샘플 · 평소 눈).
  굽기 통로 = `tools/몽글친구굽기.py`(요소굽기 앵커 치환 · NPC세트굽기와 같은 문법). 시드 고정이라 재현 가능.
  캐릭터 문법: Ink 펠트 몸 · Meadow 구슬눈(동공 70%·캐치라이트·«평소 크기» — 눈 반려 2회를 거친 확정값,
  코드 주석에 전말) · 꼬리 지느러미 한쪽 = **몽글이가 기워 준 Coral 시트 + Stitch 실땀 4점**(서사 핵심).
  표정 5종 코드는 **구현 완료**(`표정=본체|눈감음|눈웃음|우34|좌34` — 마스코트 정본 5종 문법 · 표정 실은 Stitch).

**남은 것** (유호 지시 08-25 「명품화 + 몽글이처럼 많은 표정」):
- ① 표정 4종(눈감음·눈웃음·우34·좌34) **조율판 검수 — 아직 한 장도 실렌더 검증 안 됨**(배치가 세션 종료로 중단).
  호출: `& blender --background --python tools/몽글친구굽기.py -- 출력=... 표정=눈감음 너비=700 비율=1.0 샘플=40 품질=조율 조리개=2.8`
- ② 통과 후 최종 4장: `너비=1024 비율=1.0 샘플=96 조리개=2.8`(품질 인자 없이) — 각 ~40분 CPU(GPU 는 470만 가닥 OOM → 되물림).
- ③ 세트 정리: 다섯 장을 마스코트 문법 이름(본체·눈감음·눈웃음·우34·좌34)으로 개명 · 커밋.
- ④ 누끼판(`투명=1` · 본체·눈감음·눈웃음) — 몽글이 누끼 세트와 한 벌. 앱에 얹는 단계의 재료.
- ⑤ 이름 — 유호님 미정(후보 제안: 까뭉이·먹물이·밤이).

⚠굽기는 기계 1벌 상한 — **밤샘 전량 재굽기 배치(§결정 08-25 07:33 착수)와 겹치지 않게** 블렌더 프로세스 확인 후 착수.
⚠PowerShell 로 굽기 로그를 거를 때 `Select-Object -First` 금지(파이프 중단이 블렌더를 죽인다 — -Last 만) · 파일명 보간은 `${변수}`. 전말 = 메모리 blender-bake-hardware-settings.

### 2-D. 퍼프로브 26종 + 오브 26색 — 전량 1800px 재굽기 **52장** ✅ **08-23 닫혔다** · 🔄**08-24 색으로 다시 열렸다**

⚠**이 갈래는 «해상도»로 닫혔지 «색»으로 닫힌 게 아니다.** 08-24 에 색관리가 바뀌자 이 52장이
  통째로 옛 자에 남았다 — 집행은 §2-F 아래쪽(`--라이브러리만`)이 쥔다. 아래는 08-23 해상도 판의 기록이다.

🔴 **08-23 — 이 갈래가 못 돌던 «진짜» 까닭이 드러났다. 재굽기 시간이 아니라 병합이었다.**
이 트랙은 명령줄로 `node tools/요소전량굽기.js --다시` 를 적어 뒀는데 **그 파일이 master 에 없다.**
지웠나 찾아보니 삭제 이력이 아니라 — **브랜치 `claude/nice-neumann-40e7fd` 에만 있고
master 에 한 번도 병합되지 않았다.** 거기 갇힌 커밋이 **일곱**이다:

| 커밋 | 무엇 | master 에 |
|---|---|---|
| `b1ad7fc3` | 요소 26종 **1800px 재굽기** | ❌ (master 는 1152px 24장 + 900px 1장) |
| `6888cbec` | 오브 26색 1800px | ❌ (master 는 900px 26장) |
| `02bff815` | 요소 7종 구도 잘림 수리 | ❌ |
| `95f77777` | 오브 중복 정의 161줄 제거 | ✅ 다른 경로로 이미 반영(`def 오브` 1개 확인) |
| `f5918797` | 「직물결」 무동작 수리 | ✅ 다른 경로로 이미 반영(Distance 배선 확인) |
| `aab973fd` | 낡은 프로브 2장 삭제 | ❌ (master 에 아직 있었다 — 08-23 에 지웠다) |
| `d27ea8a6` | 인계문 | ✅ 08-22 에 구조됨 |

⇒ **인계문만 구조하고 도구와 렌더는 브랜치에 두고 왔다.** 문서는 「도구가 있다」고 말하는데
실물이 없으니, 트랙을 읽은 세션이 명령을 그대로 쳐도 «파일 없음»만 만난다.
🔑 **「워크트리/브랜치에 갇힌 것」을 구할 때는 문서가 아니라 «문서가 가리키는 것»을 센다.**

## 08-23 집행
· 브랜치에서 도구 넷을 구조했다 — `요소전량굽기.js`(신규) · `오브굽기.js`·`오브시안.js`
  (기본 너비 900 → **1800**) · `퍼프로브.py`(선대 도구 표기).
· 낡은 프로브 2장(`오브_v2_짧은퍼.png`·`코랄사각_v1.png`)을 지웠다(유호 승인 08-22 · git 이력에 남는다).
· **브랜치의 1800px 렌더는 안 가져왔다** — 그 판도 결 수리 «이전»이고, 지금 코드로 다시 구우면
  결 수리 + 08-23 수리까지 얹힌 더 나은 판이 나온다. 중간 산물을 옮기는 대신 새로 굽는다.
· 52장 전량 굽기 **완주**(요소 26 · 52분 → 오브 26 · 55분 → 오브시안 · 실패 0).
  전수 실측: 퍼프로브 23 + 글자 3 + 오브 26 = **52장 모두 1800×1476**(1152/900 잔재 0).

## 🔴 굽고 나서야 드러난 것 — 구도 수리 7종도 안 왔었다
도구를 구조할 때 `요소굽기.py` 의 수리 둘(오브 중복·직물결)은 master 반영을 확인했는데
**`02bff815`(요소 7종 구도 잘림 수리)는 확인하지 않았다.** 대조판에서 아이콘·판3종·단추토글이
화면 끝에 잘려 있는 것이 보여 재보니 **7종 전부 옛 카메라 값**이었다.
⇒ 파일을 통째로 가져오면 08-23 작업(화면 열넷·음식·초밥)이 날아가므로 **그 7종의 카메라 값과
  주석만** 옮기고 7장을 다시 구웠다(16분). 값은 눈이 아니라 «탐침 렌더의 bbox 를 화각으로 역산»한 계산이다.
🔑 **「갇힌 것」을 구할 때는 커밋 목록이 아니라 «그 커밋이 고친 자리»를 하나씩 대조한다** —
   커밋 셋 중 둘만 확인하고 하나를 빠뜨렸고, 그 하나가 그림 일곱 장을 잘랐다.

## 실측 — 두 갈래 효과가 갈려서 보인다
같은 폭(900px)으로 줄여 잰 또렷함(라플라시안 분산):
| 대상 | 전 | 후 | 변화 | 무엇이 바꿨나 |
|---|---|---|---|---|
| 폼폼 | 127.0 | 177.4 | **+39.7%** | 해상도(털 한 가닥이 서브픽셀을 벗어난다) |
| 와펜 | 123.5 | 174.1 | **+41.0%** | 해상도 |
| 오브 Coral | 112.5 | 212.9 | **+89.3%** | 해상도 |
| 직조라벨 | 161.5 | 1080.1 | **+568.8%** | **결 수리** — 옛 판은 「직조」라벨인데 표면이 통째로 매끈했다 |
⚠**구도가 바뀐 7종은 전/후 비교에서 뺐다** — 카메라를 뒤로 뺐으므로 물체가 화면에서 작아졌고,
  같은 폭으로 줄여 재면 결이 촘촘해져 분산이 «내려간다». 선명도가 나빠진 게 아니라
  **같은 것을 안 재는 것**이다(판3종에서 −3~7% 로 실제로 그렇게 나왔다).
⚠인계의 「+83.4%」는 **원본 크기 그대로** 잰 값이라 «해상도»를 잰 셈이었다. 위 표는 같은 폭으로 줄여 잰다.

## 소비자 갱신
· `docs/오브_시안.html` — 굽기 스크립트가 자동 갱신(727KB · 26장).
· `docs/캐릭터/판3종_대조_0820.html` — 그림은 상대 경로라 따라오지만 **밝기 수치가 코드에 박혀
  있었다.** 옛 수치(208.2/110.0/223.3)는 **잣대가 코드에 안 남아 재현 불가**였다(여섯 영역 전부
  «일정하게» 6~7 낮았다 — 영역이 아니라 잣대 차이다). ⇒ 잣대를 코드에 못 박고 다시 쟀다:
  패턴지 **201.7** · 유리 **102.8** · 다린천 **218.0** · 카드 **97.4**.
  ⚠밝기는 재굽기로 안 바뀐다 — 값이 달라진 까닭은 «새로 구워서»가 아니라 «잣대가 달라서»다.
  ⚠**생존율은 재현 못 했다**(판 안/밖 띠 좌표가 코드에 없다) — 옛 측정으로 남기고 그렇게 밝혔다.
  판정 자체는 안 바뀐다: 유리 102.8 vs 카드 97.4 — 여전히 «거의 같다».

실측 변화폭(1/255 초과 픽셀 비율): 다린천판 3.43/255 · 37.1% · 패턴지판 1.67 · 36.9% · 오브 Coral 2.47 · 40.6%.

| 범위 | 시간 | 남는 것 |
|---|---|---|
| **전량 52장** (권장) | 4~5시간 | 규격이 한 번에 맞는다 |
| 요소만 26장 | 2~3시간 | 오브 받침만 결 없이 남는다 |
| 판 3종만 | 15분 | 나머지 13곳이 불일치로 남는다 |

⚠**여기 적혀 있던 `node tools/요소전량굽기.js --다시` 는 «없는 도구»다**(08-23 실측).
`tools/` 에 있는 것은 `오브굽기.js`·`오브시안.js` 둘뿐이고, 퍼프로브 24종은 세트굽기의 세트에도
안 들어 있다(세트는 숫자·넘버쿠키·자모·기호·요일·살림·음식·음식되굽기·초밥·조작 열이다).
⇒ 이 갈래를 실제로 집행하려면 **세트굽기 표에 「퍼프로브」 세트를 한 벌 더하는 것이 먼저**다.
```
node tools/오브굽기.js   ·   node tools/오브시안.js        (오브 26색 — 이쪽은 도구가 있다)
```
레시피·함정·실측 전문 = `docs/캐릭터/퍼프로브_0819/인계.md`
(1800px 이 선명도만이 아니라 **킷 색**을 되살린 실측 · 구도 잘림 7종 수리 계산법이 거기 있다)
⚠ 굽는 중 «정지»가 실제로 있었다 — 털실진행바 1회차가 2시간 멈췄다(CPU 시간이 안 늘고 워킹셋 0MB).
살았는지는 **CPU 시간이 느는가**로 판정하고, 멈췄으면 그 프로세스만 끊고 다시 부른다.

---

## 2-정찰. 08-24 수용 트랙 (유호 상시 강화 「정찰은 수용까지가 한 벌 · 기회비용 금지」)

정본 = `docs/AI기능_정찰_2026-08.md`(조사일 기준 실검색 · 모델 픽 발언 없음).

- **S1 온디바이스 STT 검증** ✅착수 — Expo 온디바이스 전사(iOS 26 SpeechAnalyzer·Android ML Kit ·
  API 키·과금·클라우드 왕복 0)를 현행 클라우드 전사와 **같은 음성 30개 기계 대조**(WER · 비원어민 억양 축).
  자리 = SYNK-talk · 개발 빌드 필요(Expo Go 불가). 통과 시 이득 = 전사비 0 · 오프라인 · **원음성 미반출(㉣ 강화)**.
  ⚠끝낸 세션이 이 줄을 지우고 결과를 정찰 문서에 적는다.
- **S2·S3 → 📦개원전 일괄 판정 큐**(유호 확정 08-24 「나중-효력은 개원 전에 그 시점 정찰과 한 번에 모아서」).
  정본 = `docs/개원전_일괄판정_큐.md`(Q1 실시간 음성 · Q2 Workspace + 당김 트리거 = **Meta 인증이 도메인
  메일을 요구하는 순간** · Q3 passkey · Q4 스킬화 · Q5 시각 산출). 판정일 = 가오픈 준비 착수 선언 날 ·
  늦어도 개원 4주 전. 큐가 저장하는 것은 결론이 아니라 «그날 다시 물을 질문»이다 — 판정일에 새 정찰 필수.
- **S1 온디바이스 전사 — 지을 수 있는 것 전부 착지**(talk `ddebefd`+어댑터 커밋): ①채점표 `tools/전사대조.js`(한국어
  CER · ≤5%p 규약 · 회귀 5/5) ②어댑터 `src/온디바이스전사.js`(파일 전사 · ko-KR · 미반출 플래그 · 규격 궁합
  16kHz PCM WAV 일치) ③낭독 대본 30문장 `docs/전사대조_대본_v1.json`(몽골 화자 급소 설계) ④`--뼈대` 표본 생성.
  ⛔남은 재료 둘 다 사람이다: 개발 빌드 1회 + **몽골 화자의 낭독 30문장**(첫 학생 오는 날 또는 말가이 부탁 —
  유호님·원어민이 읽으면 재려는 축(비원어민 억양)을 못 잰다).
- **파생 27곳 v1.11 인용 현행화 스윕** — 다음 세션 자율 일감(전파보류 도장 ⓐ는 안 올림 — 스킬 규약상
  유호 자리 · rot-check 가 27곳을 세고 있다).

## 3. 브랜드·로고 — 거의 닫혔다

✅ 로고 다섯 자리 전부 실물 확보(08-22) · ✅ B2 신호만 펠트 지면 실적용 · ✅ 꺾쇠 v2 좌표 전 층 통일
✅ **정본 등재 확인됨**(08-22 실측) — `_브랜드킷.md` §3 「렌더판」 표에 다섯 자리가 통로까지 적혀 있다.
곁들여 낡음 셋을 걷었다: 킷 §2 의 「워드마크 = DM Mono」(§3 실물은 Inter Tight 600/−1) ·
§3 「남은 것」의 🔴 충돌 경고(08-18 에 이미 해소) · 폰트 정본이 §3 을 `700/−4` 로 잘못 인용하던 줄.
✅ **로고 두 락업 확정·집행 완료**(08-25 밤 · 커밋 `37095067`+`7da2de0` · CI 초록) — 대외 `synk`
(어센더 k) · 앱 `syn<`(꺾쇠 T4 = 직교 10.0) · 재질 통일(신호도 펠트) · 색갈래 셋(코랄·단색·알록)
· 알록 대안 서랍 16조합. 소비자 전량 재굽기 완료. 정본 = `tools/lib/로고정본.js` + 킷 §3.
**이 갈래는 닫혔다** — 남은 것은 유호 픽 하나(«>< 찡긋» 배지)뿐이고 그건 §4 판정 대기 몫이다.

---

## 4. 판정 대기 — 유호님이 정해야 움직인다

| 무엇 | 왜 멈춰 있나 |
|---|---|
| **엔진 판정 6 — ✅ 전부 답 받음**(08-22 저녁 · 결정.md 4줄 등재) | 답과 집행: ① **#6 = 보류 → 🔵 08-22 «착수»**(유호님이 그날 저녁 다시 열었다 — 실측·막는 것 둘은 §1 「지금 도는 것」) ② 문구 2벌 = **확정·집행**(시스템 말투 상시 규칙 — talk `acf8e50` · synk-brand 등재 · 전수 26건 재프레임) ③ eval 재실행 = **#6 때 같이** ④ FSRS = **권고 채택·계산기 집행**(talk `2e5c6e9` · 잔재 폐기 — §1 Ⅲ⑤) ⑤ 질문 예산 = **하루 1회·둘 다 착수**(§1 Ⅲ⑥⑦ — 구현 남음) ⑥ Sentry 토큰 = **나중에**. 유호님 손이 다시 필요한 것은 #6 여는 날(크레딧·모델 픽·채점 80회 — 클릭 안내 준비됨)과 Sentry 토큰뿐 |
| **몽골어 보조줄 층별 규칙** | 기본값만 박았다(08-22). 「UI 라벨은 반복으로 익는다」가 가설이라 **실학생 첫 달**에 잰다. 잴 것 = 초급 학생이 UI 라벨에 막히는가 |
| 알약에 실땀을 두를까 | 킷 운용 ②를 따르면 둘러야 하나 화면에 실땀 넷이 서면 명품 4계 ②가 깨진다(조합공방 §5) |
| 몬스터 최소형(학16) | 「단순화 수위」 미확정 — 지금 그리면 그 결정을 앞지른다 |
| 목소리 성장카드(학14) | 부품 하나가 더 필요 |
| 유리(Loom 재질) 존속 | 패턴지·다린 천 굽기 결과 판정 전까지 존속(08-20 정정) |
| **요소 해상도 — 1000px 로 마침** | 103장을 1000px 로 마쳤다. 털 선명도의 진짜 손잡이는 해상도지만(900→1800px = +83.4% · 샘플 4배는 +15%뿐) 먼저 구운 67장이 1000px 이라 섞으면 세트가 갈린다. 시안 보시고 «털이 뭉갠다» 하시면 전량 `--너비 1800` 재굽기(≈4시간) |
| **도장의 천 결이 안 보인다** | Stone 으로 내리고 빛도 결광2 로 내렸지만 이 노출에서 밝은 천은 범프 명암이 갈 자리가 없다. 「손으로 판 인장」으로는 읽혀서 거기서 멈췄다 — 더 갈지는 유호님 몫 |
| **김치 — 목록에서 가장 약하다** | **15판** 갔다(보시기 어둡게 · 잎 말기 · 잎맥 어둡게 · 잎 배율 0.34 로 「고기」에선 벗어났다). 원리적 한계다: **형태가 정체인 음식**(초밥·게·송편·김밥)은 펠트가 잘 내는데 **젖고 구겨진 것이 정체인 김치**는 어렵다. 더 갈지 여기서 멈출지가 유호님 몫 |
| ~~알록 라이트판 — 눈 검증 전~~ | ✅ **닫힘 08-25** — 흰 배경판 시트에서 첫 렌더·확대 실측. 잉크 몸 위 색실이 다크판보다 오히려 **더 선명**하다(어두운 몸 ↔ 밝은 실의 대비). 쓰기 금지 해제 |
| **검정 바탕 halo 접지 — 필요해지면** | 정본 락업의 접지 그림자는 Ink Deep 이라 **검정 바탕(#080605)에서는 바탕색과 같아 사라진다**(킷 §3 ①-e). 지금 크기(스플래시 202dp)에선 흠이 아니지만 큰 판(포스터·간판)에서 글자가 «놓인» 게 아니라 «잘라 붙인» 느낌이면, 그림자를 옅은 광(halo)으로 뒤집는 판을 따로 만든다. 착수 조건 = 그 큰 판을 실제로 만들 때 |

---

## 4-B. AI 활용 가이드 — 만들 것 셋 (유호 확정 08-23 · 개원 전까지)

정본 = `docs/AI활용_가이드_v2.md`. 확정: 바깥 AI는 «숙제별로 갈라 가르친다» · 학부모 A4 «만든다» · 네 낱말 «맡기기·부탁하기·가려보기·책임지기» 확정.

- [x] **A 수업 1회 교안** — 실물 08-24 `docs/AI활용_교안_v1.md`(카드 6장·오답은 «그날 실물 채집» 절차로 — 지어낸 오답 금지 · 몽골어 병기는 검수 뒤)
- [x] **C 학부모 A4** — 실물 08-24 `docs/발표물/11_AI활용_학부모안내_A4_kr.html`(린트 ✅ · mn판은 검수 통로 뒤)
- [~] 네 낱말 몽골어 — AI 초안 + 기계 대조(문법 4/4 정상) 08-24 · 정본 = `docs/AI활용문안_몽골어_검수표.md` · 남은 것 = 사람 확정 하나(검수 큐 Q · 채용 뒤)

⏳ 급하지 않다 — 실학생이 앱을 쓰기 시작해야 쓰이는 물건이다(개원 2027-02).

---

## 4-C. 쌓는 셋(Trail·Prism·Temper) — **트랙에 0건이던 자리** (08-24 신설)

정본 = `docs/엔진/SYNK_{Trail,Prism,Temper}_소개서.html`. 세 소개서가 각자 「지금 어디까지 왔나」를 정직하게 싣고 있는데 **트랙·결정 어느 쪽에도 한 줄이 없었다**(08-24 실측 0/0). 이건 08-15 에 궤적에서 이미 난 병의 재발이다 — 그때는 「엔진 대장에 없어 반년째 아무도 안 키웠다」였고, 이번엔 층만 «트랙»으로 바뀌었다. **코드가 서 있어도 남은 일 목록에 없으면 안 자란다.**

- [x] **Temper §10 「개입 로그 규격」 — 규격 확정 + 코드 착수 완주 08-24**(정본 = `docs/Temper_개입로그_규격_v1.md` · 새 열 0). 코드 걸음 셋 전건 소화: ⓐ는 재판정 기각(승격표 전건 학생 사건 — 죽은 칸 대신 **진입 잠금**) ⓑ두 층으로 이행(talk `tests/개입스탬프.test.js` 5검사 — 통로 장부 3파일·미탑재 상한 0/1 · `tools/성과계기판.js` 「개입 스탬프」 누적 계수) ⓒ실측 완료(corrections·correct·teach 전부 insert 0 — delivered 생산 없음). 첫 계기판 실측: 리허설 delivered 4468 = 완비 4169 + 미비 299(전건 스탬프 배선 08-09 «전» 옛 행 + 왕복시험 직접 SQL 행 — 실손실 0)
- [x] **Trail 「선배 모드」 = «명예 졸업생 카드» 확정**(유호 08-25 · 결정.md) — 궤적 평생 열람 + 근황 한 줄 자동 수집 · 학습 잠금 · ③선배의 방 계단은 개원 후 표본 위에서 재판정. 남은 것 = **코드 착지(2027 1기 졸업 전)** — 잠금 화면 + 근황 한 줄→outcome_log 통로. 데이터 0행은 그때까지 정상
- [~] **Vellum 강사판 — 남은 것은 «키»가 아니라 «앱 입구»다**(08-25 원격 실측으로 판정 뒤집힘 · 앞선 「붓기 승인」·「키 주입」 줄은 이 줄이 대체):
  - ✅ **실답 모드 증명 완료**(리허설 · 유호 클릭 0): 강사 토큰으로 `companion/ask` 3문 실호출 — 진짜 출처를 달고 답하고(숙제서클·수업규칙), 급여 질문은 안 흘린다(문서에 없다고 되묻는다 · 출처 0 행이 곧 §7-4 빈칸 신호). `companion_qa` 3행 적재 = 「모였나」 게이트의 첫 실물. 탐침 = 스크래치패드 `몽글강사탐침.js`(강사 계정 `probe-teacher` 재사용 · 리허설만)
  - 🔴 **앱에서 물을 자리가 0** — `companion/ask` 를 부르는 앱 코드가 전수 0(`lib/마스코트강사말.js` 는 정형 발화 ㉮ 전용 · fetch 없음). 서버 문은 섰는데 문고리가 없다
  - ⚠ **운영 키 주입은 지금 무의미** — 앱 dev·preview·staging 빌드가 전부 **리허설**을 본다(`eas.json` · 운영은 production 빌드 하나). 입구가 서고 운영 빌드를 쓰는 날이 키의 자리 → 「가오픈 때」 원칙 그대로 (유호 08-25 「지금 켠다」 픽은 «키가 시험의 차단자»라는 내 전제 위에서 나왔고, 그 전제가 틀렸다 — 재판정 재료로 올림)
  - 남은 일 = **강사 화면의 「몽글에게 물어보기」 입구 한 벌** — 시안 v1 나감(08-25 · `docs/몽글_물어보기_시안.html` · 리허설 실답 3벌 · 렌더 검증 완료) → **유호 보류 08-25**(「구성은 나쁘지 않은데 다음에 체크」 — 반려 아님 · 결정.md). **당김 트리거 = 몽글 친구들(캐릭터 공방 레인) 완성** — 그날 시안 재체크(픽 셋: 입구 모양 · 넘버쿠키=큐 순번 · 대기 모션=실패 스피너) → talk 착지 · 완결 게이트 ✗✗는 그 뒤 2단계
- [ ] **Prism 집계층** = 코드 0 · 재료 생산자는 라이브. 세우는 시점 = **개원 직후**(첫 실데이터로 검증하며) — 지금 앞당길 이유 없음, 다만 잊지 않게 여기 둔다
- [ ] **Core 훈련 승격(⑥) 통로 코드 0** — 08-24 실측 보강: 검수확정에 **의사 칸은 이미 실물**(lib/검수확정.js `promote` · 기본값 승격 안 함). 남은 배선 = «promote=true 행 → 비식별 공정 → 불변 산출물 → data_use.granted 발행»(경로 B 설계 그대로 · granted 생산자는 사건출처·검증기에 정의만 있고 함수 0). **처분 = 개원전 큐 Q8**(유호 08-24 · 당김 트리거 = 첫 실학생 promotion_intent — 이 줄은 «잊지 않게» 남는 거울이고 절차 정본은 큐가 쥔다)
- [ ] 🔴 **몽골어 검수자 채용 — 이 목록의 어느 줄도 아닌데 여러 줄의 공통 차단자다**(08-24 실측: 트랙 전체에 채용 줄 0). 미니게임 4모듈 검수확정(㉡ 성향의 심장) · 라디오24 문항 0/100 · 몽글 이름 20곳 전파 · 엔진 이름 4종 현지 검수 · AI가이드 네 낱말이 전부 이 하나 뒤에 서 있다. 확정 정본 = memory mongolian-reviewer-hire(08-13 「현지 언어 파트너 채용」·발주서·2명 테스트 1명 채용) — **확정만 있고 집행 트랙이 없었다** · 🔑요청 전량은 `docs/몽골어검수_요청_큐.md`(08-24 — 유호 지시로 한 자리에 모음)
- [x] **Temper retry_of — 전수 실측 완료 08-24 밤 · 끊김 0**(생산 4통로 전부 잇는다 — events 고리풀기·deliver 배정 2벌·radio-promote 부모라운드). 남은 것은 끊김이 아니라 과잉 1(오늘과제 step 무분기 — corrections :211 소비층 3중 게이트가 이미 해소 · 생산을 좁히면 오프라인 지연 발화의 고리가 죽어 기각). 세는 자 = 개입스탬프 ①③(통로 장부 + retry_of 전수) · 판정 전문 = 규격 문서 §4
- [ ] **개원 전 리허설에 «수집면 전부 켜짐» 확인 항목**(Core §10 첫째 문 — 첫 학생의 첫 주는 한 번뿐) — 개원 리허설 체크리스트에 등재만 해두면 되는 한 줄. **출구 짝은 이미 섰다** = 관통왕복시험(talk `tools/관통왕복시험.js` · 08-24 첫 실측 ✓✓✓✓✓ 23/23 — 켜짐이 입구, 관통이 출구 · 명세 = `docs/관통왕복_명세.md`)
- [x] **소개서 6벌 상태표 얼음 — 08-24 저녁 전량 해빙 + 장치화**(진찰 세션): ①원고 6벌+지도 08-22~24 완주 전량 반영(Temper §10 종결·Amber 초안·화면 14벌·나침반 완주·학습자상태 v13·점수 88/102 등 — 사실 뒤집힘 7곳·판 모순 5곳·형제 불일치 3건 수리) ②Core 원고↔산출물 «양방향 갈라짐» 해소(08-24 새벽 정정이 산출물에 직접 가해져 있었다 — 원고로 흡수 후 전량 재굽기 · `--재현` 6/6) ③**rot-check 에 «소개서 대조» 절 신설** — 원고↔산출물 재현·판 자기모순·엔진점수 실값 대조·철학판 인용·지도 판을 매 실행 기계로 잰다(반대 시험 통과 · 거짓 양성 0). 「엔진 트랙을 만질 때 같은 커밋에서 갱신한다」 규율은 이제 어기면 그날 빨개진다

---

## 5. 유호님만 풀 수 있는 것

| 무엇 | 지금 상태 |
|---|---|
| 🔴 **제미나이LM 자동 갱신 실패** | 구글 드라이브 데스크톱이 꺼져 있다(G:~K: 전부 없음) — 유호님이 메모리 절약 위해 의도적으로 꺼둠(08-22). 켜서 「내 드라이브」가 보이면 예약 작업이 매일 알아서 돈다 |
| ▶ **지침 개정 제안 조건 도달** | 마지막 개정 08-13 뒤 마찰 신호 15건(기준 2건). 개정할지 유호님 승인 자리 |
| **상표 출원** `40-2026-0165300` | 기한 **2027-02-11** · 간판은 키릴 의무 |
| **Supabase 운영 재배포·vault 교체** | 2건 미결(신형 `sb_secret_` 키 전환) |
| **몽골어 검수** | 미니게임 G1~G4 가 이것 때문에 멈춰 있다. 🔑**요청 전량 한 자리 = `docs/몽골어검수_요청_큐.md`**(08-24 신설 · 회차 셋 + 상시 · 선행 차단자=채용 §0 · S1 낭독 30문장 포함) |

---

## 6. 기술 부채 — 알고 있고 안 고친 것

- ~~본문 층이 해상도에 묶여 있다~~ → ✅ **08-23 풀었다.** 칸의 «자리»는 JSON 이 이미 상대적으로
  주고 있었고, 묶여 있던 것은 칸 «안»의 여백과 글자 크기뿐이었다. 카드 폭에서 배율을 뽑아
  (`배 = 카드폭 / 945`) 그 둘에만 곱한다 — **덧씌움 열넷 전부**.
  회귀: 1080px 산출 **열넷 다 픽셀 그대로**(배 = 1.0) · 700px 흉내판에서 **열넷 다 제자리**.
  ⚠곁들여 드러난 것 둘: ①`조합덧씌움3.py` 의 힌트가 **화면 절대 좌표**(1910)라 700px 에서
  통째로 사라졌다 — 「손좌표를 안 쓴다」를 유일하게 어긴 자리였고 바닥 기준(`H − 픽(142)`)으로
  고쳤다. ②대비 실측이 «못 쟀다»를 조용히 건너뛰고 있었다(조합판은 가드가 없어 0 나눗셈) —
  이제 한 줄로 드러난다. **그 한 줄이 ①을 잡았다.**
  ⚠남는 한계: 아주 작은 판(700px)에서는 출석 격자·태그의 «이름»이 서로 겹친다. 1080px 이 정본이라
  실사용엔 안 걸리지만, 더 작은 판이 필요해지면 그건 «글자 크기»가 아니라 «배치»를 다시 잡을 일이다.
- **판 갈래(게이지=판)는 코드에 살아 있되 미채택** — 「오늘」 탭에서 게이지를 작게 재사용할 일이 생기면 그때 꺼낸다.
- **PIL 합성 목업이지 Expo 실장이 아니다** · AMOLED 실기 미확인.
- 화면 안 숫자·문구는 표본이다(12 / 7 / 3급) — 실데이터 배선 전.
- 🔴 **계약 «판 번호»를 지키는 기계가 이 저장소엔 0** (08-24 실측 · 새 장치 안 짓기로 함).
  `엔진_수집.js` 의 `SCHEMA_VER` 는 `계약/수집_교정_계약.json` 의 `버전` 을 손으로 베낀 값이고
  (GAS 가 JSON 을 import 못 한다), 그 둘을 대조하던 `tests/계약.test.js` 는 **08-19 자기검증 층 철거**
  (`e75fc7fc` · 유호 지시)로 걷혔다. 그 뒤 계약이 c12(08-21)·c13(08-22)로 오르는 동안 상수는 c11 에
  머물렀고 — 판 번호는 **행에 박히는** 값이라 그 사이 쓰인 행이 있으면 영원히 c11 이다(소급 기입 금지 ·
  개원 전이라 라이브 시트에서 실제 몇 행이 그랬는지는 **안 재봤다** — 저장소 안에서 잴 수 없는 값이다) —
  08-24 에 손으로 `c13` 까지 올렸다. 남은 계약 검사 `tools/계약동봉검사.js` 는 `픽스처_항목필드` ↔ 조립부만
  보고 판 번호는 안 본다 · `tools/계약동기화.js` 는 파일을 형제와 같은 바이트로 맞출 뿐 이 상수는 안 본다.
  **지키는 방법은 지금 하나뿐이다 — 계약 판을 올리는 손이 같은 커밋에서 그 상수를 고친다**(근거 전문은
  `엔진_수집.js` 그 상수 머리말). 또 벌어지면 그때가 장치를 유호님께 «제안»할 자리다(짓는 게 아니라).
- 🔴 **계약 c13 이 자기 분할을 안 지켰다 — 정본이 talk 이라 그쪽 몫으로 남긴다** (08-24 실측 · 이 저장소 영향 0).
  계약은 `learning_events.필드` 전량이 `라이브_대응`(시트 열 있음) ∪ `라이브_대응없음`(없음) 이라고 스스로
  못박았는데(`라이브_대응_축`), c13 이 새로 낸 「확인」 5칸(`trait_axis`·`shown_key`·`shown_text`·`response`·
  `estimate_as_of`)이 **어느 쪽에도 없다**(85칸 중 5칸 미분류 · 나머지 80 = 14+66 정상). 그 축 문장은
  「`tests/계약.test.js` 가 그 분할을 강제한다」고 적었지만 **두 저장소 어디에도 그 검사는 없다** —
  `라이브_대응` 을 읽는 코드는 talk `docs/C0_API계약.md` 산문 한 곳뿐(양쪽 `tests/`·`lib/` 전수 0).
  ⚠ 이 저장소 판정에는 영향이 없다(그 5칸은 이 저장소가 생산하지 않는 사건의 payload 다) — 다만
  「미분류」를 「시트 열 있음」으로 오독하면 c11 때 `skill_ids` 오독(계약이 직접 적은 실사고)이 반복된다.
- ✅ **talk CI 의 `push:` 트리거를 08-24 에 되살렸다 — 예약된 9/1 복원이 아니라 «사유 소멸»이다.**
  유호 확정 08-24 「공개전환으로 하고 개원 직전에 비공개로 바꿀꺼야」 → SYNK-talk 을 **public 으로 전환**했다.
  public + GitHub 표준 러너 = Actions 분을 **안 센다.** 08-14 중지 사유가 통째로 사라졌다(talk `5b0f13a`).
  경로 조건은 **안 붙였다** — 그건 분 아끼기가 목적이었고, 안 붙이는 쪽이 매 push 마다 마이그 회귀를 받는다.
  예약 `talk-ci-push-restore-0901` 은 **비활성**(그 예약이 띄운 세션이라 삭제는 일반 세션 몫).
  ⚠ **개원 직전 비공개 복귀는 유호님 몫이다**(유호 08-24 「내년 2월 개원 · 이건 내가 알아서할게 체크하지않아도 돼」)
  — 예약·리마인더를 걸지 않는다. 복귀하는 그 손이 알아야 할 절차(supabase job 경로 조건 + 분 잔량 확인을
  한 벌로 · 분 재는 법 = 손 실행 1회 뒤 check-run 주석 · 「분 소진」과 「결제 실패」가 같은 문구)는
  talk `ci.yml` 머리말에 전부 박아 두었다.
- ✅ **talk supabase 열흘치 청구서 — 켜자마자 나온 빨강 «넷»을 같은 날 다 갚았다**(유호 「제대로 3번으로 고쳐줘」 08-24).
  복원 첫 run `32714974980` 부터 연쇄로 드러난 결함 전부가 CI 사각(08-11 기동불능 · 08-14 공식 중지)에서
  들어온 것들이다. **「push 해도 CI 0건이 정상 얼굴로 굳는다」의 실물 넷**:
  ① 제약 65벌 재실행 죽음(08-22 c13 조각 · 자기 이름 drop 0개) → **합본 생성기에 멱등화 층**(talk `51bbd7f`) —
    조각은 봉인(checksum)이라 굽는 자리에서 고쳤다. 규칙은 「drop 있는 alter 사슬의 add 앞에 자기 drop」.
  ② 같은 조각 꼬리의 이력 insert 도 가드 밖(duplicate key) → 멱등화 규칙 ② «if not exists 감싸기»(talk `4aeb36a`).
    두 병의 뿌리는 하나다 — 머리 가드의 return 은 do-블록만 빠져나가고 몸이 밖에 있었다.
  ③ 검증기 «처음부터 다시»가 engine 만 지움 → ops·radio 잔존이 재건을 죽임(cannot drop columns from view)
    → `drop_synk` 로 셋 다 지운다(talk `698deea` · 「배치 도구가 이름보다 좁다」의 재현).
  ④ c3/c4 씨앗의 시대착오(08-10 진입 · recorded_by·consent_id 는 그때 없던 열) → 씨앗을 시대 정확하게 +
    동의귀속 가드에 «시대이전» 표식 예외(talk `7f065c6`·`1767b3f`) + 씨앗 스킬 자국 걷기(talk `11d2291`).
  종착: run `32719815827` **전체 초록**(verify+supabase · 시험 2577). 곁수확 둘 — 사후 대조 ❌ 가 실측을
  안 찍던 것을 고쳤고(진단 재료가 없었다), 검수뷰·잡없는제출 «불일치»는 내 자리 오독이었다(기대 튜플은
  case 순서 · 실측 행은 CTE 파일 순서 — **이름으로 대조해야 한다**).
  ⚠ 운영 착지판 `supabase/DB착지판.json` = **c11** — c13 이 운영에 갔는지는 저장소만으로 확정 못 한다
  (안 재봤다 · 운영 DB 조회 필요). 멱등화 층은 운영 적용과 무관하게 안전하다(빈 DB 도 재실행도 no-op 방향).


---

## 7. 다음 세션이 먼저 볼 것

1. `node tools/결정.js` — 유호님 확정 52건(이 파일보다 이게 정본이다)
2. `list_sessions` — 지금 도는 세션이 어디를 만지는지
3. `node tools/rot-check.js` · `node tools/friction.js --open` — 낡음과 마찰
4. 이 파일의 ⛔ 표시 — 그게 안 끝난 것이다
