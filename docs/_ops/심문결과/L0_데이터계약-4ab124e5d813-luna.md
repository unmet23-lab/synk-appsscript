판정: 동결 불가. P0 16건, P1 6건이다.

### P0

- [P0] 인증 코드 정체 충돌 — 닫힌 결정은 학원 발급 코드가 `SYNK-001`과 다르다고 했지만, §4-1은 예측 가능한 `student_code`를 로그인 ID로 삼고 별도 발급 코드를 받지 않는다. §4-1의 “앞 판은 비밀번호만 막았다”는 해명은 정본 충돌을 해결하지 못한다.

- [P0] 공개 가입으로 합성계정 선점 — §4-2·§8-2는 공개 가입 OFF를 요구하면서 실제 상태를 `disable_signup=false`로 기록한다. `auth/index.ts`는 합성 이메일이 이미 있으면 그 Auth 계정을 그대로 학생에게 연결하므로, 공격자가 `synk042@synk.invalid`를 먼저 만들고 비밀번호를 정하면 정상 학생의 계정을 선점할 수 있다.

- [P0] 폐기된 토큰이 실제 API에서 살아 있다 — §4-2-3은 `active`·`revoked_before`를 매 요청 검사한다고 하지만 `events`, `tasks`, `uploads`, `corrections`, `progress`는 `auth_user_id`만 조회하고 `session_alive()`를 호출하지 않는다. 직접 `SUPABASE_DB_URL` 연결은 RLS를 우회하므로, 폐기된 access token으로 새 이벤트 저장·자료 조회·업로드 서명 발급이 가능하다.

- [P0] 닫힌 제품 결정과 직원 화면 충돌 — 제공된 닫힌 결정 목록은 검수 대시보드를 앱 밖 웹으로 두었는데, §4-5는 검수자를 Expo 앱 내부 역할 화면으로 고정한다. 화면 표면이 바뀌면 인증·세션·번들·감사 경로가 모두 달라지므로 직원 인증 설계는 동결할 수 없다.

- [P0] c8 적용 상태가 판정 불능 — §9-4는 운영·리허설 DB가 `20260807060000`까지 섰다고 하고 §12-6은 원격 적용과 물리 왕복이 아직이라고 한다. 닫힌 인덱스도 c8 운영 반영 종결이라고 기록하므로, §8의 삭제 후 재구축 절차를 어느 상태에 적용해야 하는지 문서만으로 결정할 수 없다.

- [P0] 제출 이벤트가 `task_snapshot`을 버린다 — §3-3·§7-4는 스냅샷을 필수 원본으로 규정하지만 DDL은 nullable이고 `이벤트검증.js`는 `task_ref`만 요구한다. 실제 `lib/오늘과제.js`의 제출 payload에도 `task_snapshot`이 없어서 첫 제출이 성공 저장된 뒤 학생이 본 문항을 복원할 수 없다.

- [P0] 이벤트별 payload 검증이 빈 껍데기를 허용한다 — §3-2는 이벤트별 검증을 약속하지만 `quiz.answered`는 `task_type`만, `preference.stated`·`session.abandoned`는 추가 필드 없이 통과한다. `events/index.ts`도 payload가 비어 있으면 `payload.ver`조차 요구하지 않으며, 선택 payload의 타입·범위·상호 일관성도 검사하지 않는다.

- [P0] 멱등키 형식 미검증 — C0는 앱 멱등키를 UUID v4로 규정하지만 실제 앱은 `submission:날짜:단계:시도` 문자열을 만들고 서버는 임의 값을 `String(key)`로 저장한다. 오타·객체·짧은 문자열이 서로 같은 키로 접히면 정상 제출이 `duplicate`로 조용히 사라진다.

- [P0] 회화 세션 고리가 선택 사항이다 — 계약 봉투와 C0는 `correlation_id`를 세션 고리로 설명하지만 `공통필수`에는 없고 앱도 없으면 `null`을 보낸다. 첫 발화부터 한 앉음의 경계가 빠지면 아침 낭독과 저녁 자유발화를 사후에 구분할 수 없다.

- [P0] 추정 메타데이터의 물리 저장소가 없다 — 코어엔진·계약은 `source_kind`, `estimator_confidence`, `evidence_refs`, `estimator_version` 등을 요구하지만 실제 L0에는 `source_kind` 컬럼·타입과 추정 컬럼이 없다. `events/index.ts`도 이 값을 INSERT하지 않으며, 실제 enum조차 문서의 `system`을 포함하지 않는다.

- [P0] skill 축이 재현 불가능하다 — §3-5는 `skill_taxonomy_ver`와 `superseded_by`로 의미를 보존한다고 하지만 `skills` 행은 mutable이고 불변 트리거가 없다. `skill_ids` 배열에는 FK가 없고 이벤트 서버도 존재 여부를 검증하지 않으므로, 과거 이벤트에 오타·삭제된 ID·변경된 정의가 섞여도 저장된다.

- [P0] pipeline_jobs가 성공 제출을 받지 않는다 — §9-2는 큐를 핵심 물리 자리로 만들었지만 현재 함수·도구 어디에도 `pipeline_jobs` INSERT나 submissions 연동 트리거가 없다. 제출은 저장되었다는 200만 받고 큐에는 없으므로, 처리되지 않은 제출과 정상 처리된 제출을 구분할 수 없다.

- [P0] 교정 골든 고리가 실제 입력되지 않는다 — §3-4·§9-1은 `reviewed_correction_id`, `verdict`, `verdict_reason`, 안정적인 검수자 ID를 요구하지만 `tools/교정확정.js`는 교정문·태그·자유 문자열 `reviewer`만 INSERT한다. 첫 교정부터 AI 교정에 대한 승인인지 독립적인 사람 교정인지, 누가 확정했는지 복원할 수 없다.

- [P0] 철회 표식이 파일 삭제를 증명하지 않는다 — §9-3-2는 사용자가 Storage 폴더를 삭제한 뒤 SQL로 `audio_deleted_at`을 기록하지만 둘 사이에는 원자성이 없고 실제 잔존 객체를 검사하지 않는다. 이미 발급된 signed upload URL도 철회 시 폐기되지 않으므로, 표식은 남았는데 파일이 남거나 철회 뒤 업로드가 완료될 수 있다.

- [P0] 승격·시험 결과 사건이 빈 사건이다 — §6-2가 요구하는 `artifact_ref`, `content_hash`, `serializer_ver`, `dataset_ver`, `consent_id`, 승인자·평가·롤백 계보의 저장 자리가 없다. `data_use.granted`와 `exam.result`는 값목록에만 있고 payload 필수 규격·작성 함수가 없어서, 중요한 사건을 공통 봉투만으로 저장할 수 있다.

- [P0] 삭제 규칙이 문서 안에서 반대다 — §3-6의 `consents` FK는 `on delete cascade`인데 §10은 `restrict`로 고쳤다고 선언하고 실제 생성 DDL도 `restrict`다. 구현자가 §3 SQL을 따르면 학생 삭제 한 번으로 동의·사건이 연쇄 삭제되어 append-only와 철회 계보가 무너진다.

### P1

- [P1] 버전 권위가 세 갈래다 — §2는 현재 계약판을 c3라고 쓰고, 계약 JSON·Edge Function·실제 합본은 c8이며, `schema_ver`에는 요청한 API 판이 아니라 DB 최신 판이 저장된다. `pipeline_jobs`, `daily_activity`, `staff`, `staff_access_log`에는 아예 `schema_ver`도 없어 재현성 규칙이 전체 행에 적용되지 않는다.

- [P1] 고정 문장이 AI 개입으로 위장된다 — `deliver`는 실제 모델·prompt 없이 고정 도입 문장을 만들면서 `actor_kind='ai'`를 기록하고 첫날에는 `degraded=false`다. 엔진은 스크립트·강등·실제 AI 답변을 같은 개입으로 학습한다.

- [P1] 미래 동의가 수집을 승인할 수 있다 — 배달 배치는 `revoked_at is null`만 보고 동의를 고르며 `agreed_at <= now()`를 검사하지 않는다. 미래 시각으로 잘못 입력된 동의가 있으면 동의 시작 전에도 배정·개입 사건이 기록된다.

- [P1] 검수자에게 원문 속 개인정보가 그대로 열린다 — §4-5는 학생 신원을 차단한다고 하지만 inspector 정책은 `body_original`, `audio_ref`, `task_snapshot` 전체를 읽게 한다. 학생이 원문·음성에 이름·전화번호를 말할 수 있다고 §3-1에서 인정했으므로, 표시 이름을 숨기는 것만으로 비식별이 되지 않는다.

- [P1] 복합 FK가 의미 오류를 막지 못한다 — `retry_of_event_id`·`parent_event_id`는 같은 학생인지밖에 검사하지 않아 다른 세션·미래 사건·`task.assigned`를 가리킬 수 있고 순환도 만들 수 있다. 구조적으로 유효한데 학습 사슬은 틀린 사건이 생긴다.

- [P1] daily_activity가 부재의 증거가 아니다 — `expected`가 nullable이고 이를 쓰는 배치·함수가 없으며 `task.assigned`와 FK로 연결되지 않는다. 행 없음, 배치 실패, 실제 접속 0일이 모두 같은 상태로 남는다.

### 이 문서보다 나은 대안 구조 1개

`증거 코어`와 `운영·승격 층`을 동결 게이트부터 분리한다. 먼저 `learning_events`·`submissions`·`corrections`·`consents`에 대해 이벤트별 엄격 검증, 세션·멱등키·스냅샷·불변·폐기 토큰 검사를 실제 API 왕복으로 통과시키고, 인증·직원·큐·daily_activity·훈련 승격은 각각 작성자·필수 payload·실측 테스트가 선 뒤 별도 마이그레이션으로 동결한다.