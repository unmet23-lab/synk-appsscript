판정 범위: 저장소 대조 명령이 실행 환경에서 차단되어, 아래는 제공된 후보 전문과 그 안에 인용된 사실만으로 심문했다.

[P0] `max+1`은 번호 예약이 아니다 — §5 ㉠은 번호 계산과 `app_state` 갱신을 두 단계로 적었지만, 시트·카운터·DB 기록의 원자성이 없다. 발급 직후 중단·재시도·다른 발급 경로가 발생하면 번호만 예약되거나, 행만 남거나, 같은 번호가 재발급될 수 있다. `LockService`만으로는 수동 편집과 다른 실행 경로를 막지 못하며 충돌 시 중단·복구 규칙도 없다. 문제 위치: §2 ①②, §5 ㉠.

[P0] `on delete restrict`는 학생 행 불변식을 보장하지 않는다 — 자식 행이 없는 `learners` 행은 `restrict`만으로 삭제되지 않는다고 보장할 수 없다. 또한 상담시트의 학생번호 삭제·수정도 기계적으로 차단하지 않는다. §2 ⑥과 §5 ㉣의 “행을 지우지 않는다”는 운영 규칙이지 강제되는 물리 계약이 아니다.

[P0] 한 행으로는 휴학·복귀·재등록을 표현할 수 없다 — `left_at`과 `exit_reason`이 `learners`에 한 개씩만 있으면 휴학 후 복귀, 중도이탈 후 재등록, 복수 과정 참여의 이력이 덮어써진다. 특히 휴학을 종료사유로 저장하면서 복귀일이나 재학 회차가 없으므로 현재 상태와 과거 상태를 구분할 수 없다. 문제 위치: §5 ㉡·㉣.

[P0] 날짜의 의미가 서로 충돌한다 — `enrolled_at`을 “첫 반배정일”이면서 “상담시트 등록일”이라고 정의했고, `left_at`은 실제 퇴소일이 아니라 `퇴소감지일`로 정의했다. 감지 지연이 있는 경우 학생이 실제로 언제 재학·학습했는지 복원할 수 없으며, 시즌별 학습 기간과 졸업 시점을 계산할 수 없다. 문제 위치: §5 ㉣.

[P0] 동일 사실의 원천이 네 군데로 갈라진다 — `payStatus`, 상담시트 `처리상태`, `exit_log.종료사유`, `learners.exit_reason`이 모두 상태를 표현한다. 어느 값이 정본인지, 변경 순서와 동기화 실패 시 우선순위가 무엇인지 없다. 한 곳은 `졸업`, 다른 곳은 `퇴소`, 또 다른 곳은 빈 값인 상태가 그대로 통과할 수 있다. 문제 위치: §2 ⑧, §5 ㉡·㉣.

[P0] 퇴소 인제스트에 데이터 계약이 없다 — “같은 자리, 같은 시크릿 방식”은 실행 위치만 말할 뿐, 원천 행 ID·불변 이벤트 ID·대상 `learner_id`·멱등키·재시도·지연 도착·미매칭 처리 규칙을 정의하지 않는다. 기존 `roster-ingest`의 `on conflict ... do nothing`도 충돌을 조용히 삼키는 구조인데, 이를 실패·경고로 바꾸겠다는 처방이 없다. 문제 위치: §2 ⑨, §5 ㉣, §7.

[P0] `완주시즌 0~6`은 계산 규격이 아니다 — 시즌의 고유 ID, 시작·종료일, 부분 수료, 휴학 후 복귀, 교재 버전과의 연결이 없다. “자동 산출 가능하면 자동”은 구현자마다 다른 결과를 내므로 2031년 졸업생 명단을 재현할 수 없다. 문제 위치: §5 ㉡, §6.

[P0] `recovery_email`은 문이 아니다 — §5 ㉢은 로그인은 계속 번호+비밀번호라고 하면서 이메일을 단순 컬럼으로만 추가한다. 전화번호가 바뀐 뒤 이메일을 이용해 어떤 API·계정 연결·복귀 절차가 실행되는지 없으므로, 제시된 실패 상황을 해결하지 못한다.

[P0] “문은 여러 개”라는 설계가 현재 스키마와 충돌한다 — §2 ⑤의 `auth_user_id uuid unique`는 한 `learner`에 하나의 Auth 계정만 연결되는 구조다. 이메일 기반의 졸업생 계정과 학원 계정, 향후 B2C 계정을 함께 연결할 계정 관계 테이블이나 이력 구조가 없다. 문제 위치: §3·§4, §2 ⑤.

[P0] `learner_id`를 사람 자체로 선언했지만 사람 모델이 없다 — 현재 `learner_id`는 `engine.learners`의 행 식별자일 뿐, 재등록·중복 생성·계정 추가·동일인 병합 시 같은 사람이라는 판정 규칙이 없다. 따라서 “10년 뒤에도 같은 사람”이라는 핵심 주장을 UUID 하나가 자동으로 보장하지 않는다. 문제 위치: §3·§4·§6.

[P0] 번호 변경을 허용하면서 번호 이력이 없다 — §3은 이름표가 바뀔 수 있어야 한다고 하고, §4·§5 ㉤은 기존 번호를 졸업 후에도 유지한다고 한다. 번호를 교체할 경우 과거 시트·인쇄물·외부 입력을 새 번호로 연결할 별칭 테이블이나 변경 사건이 없어, 옛 번호가 끊어진다. 문제 위치: §3, §4, §5 ㉠·㉤.

[P0] 동결 가능한 물리 계약이 제시되지 않았다 — 새 필드의 nullable/default/check/index, 기존 행 backfill 규칙, 시트 헤더와 DB 필드 매핑, API payload, migration 순서·재실행·부분 실패·rollback이 없다. §7은 실제 DB 형상도 확인하지 않았다고 명시한다. 이 상태에서 “자리”를 만들면 미적용이나 부분 적용이 정상처럼 보일 수 있다. 문제 위치: §5 전반, §7.

[P1] 빈 값과 미처리 상태가 구분되지 않는다 — `exit_reason = null`, `left_at = null`, 인제스트 미실행, 아직 재학 중인 상태를 판별할 `status`나 처리 watermark가 없다. 누락된 데이터와 실제로 값이 없는 데이터가 같은 모양이 되어 완결 게이트의 “모였나”를 검증할 수 없다. 문제 위치: §5 ㉡·㉣, §6.

[P1] 엔진 학습 사슬이 끊겨 있다 — 이 문서는 새 필드가 이해 ㉠·㉡·㉢ 중 어디에 기여하는지, 어떤 이벤트가 어떤 엔진 부품으로 읽히는지, 언제 회수하는지 이름을 대지 않는다. 2031년 명단과 영업 활용만 제시되어 판단 정본의 “한 사람을 깊이 이해하는 교육 시스템”으로 이어지는 배선이 없다. 문제 위치: §1, §5 ㉥, §9.

[P1] 동결 전에 사업적 선택이 남아 있다 — 복구 이메일의 선택·필수 여부와 대상 범위가 §8에서 미결정이다. 선택으로 두면 장기 복귀 경로가 없는 학생이 생기고, 필수로 두면 수집 시점과 실패 처리가 달라지므로 현재 문서는 두 서로 다른 시스템을 동시에 기술한다. 문제 위치: §5 ㉢, §8 ②·③.

[P1] 졸업 카드가 학생에게 자기설명되지 않는다 — §5 ㉤은 번호를 인쇄한다고만 하며, 그 번호가 무엇인지, 언제 어디에 쓰는지, 분실·오독 시 무엇을 해야 하는지 안내하지 않는다. 이는 ⑤ 자기설명 게이트와 ① 실제 사용 도움을 통과했다는 근거가 없고, 학생이 카드를 보관·재사용할 이유도 설계되지 않았다.

[P2] 필수 자리와 후속 기능의 경계가 흐리다 — §6은 불변 데이터 수집과 졸업 카드·명단 뷰를 같은 처방 흐름에 놓는다. 카드 발행과 명단 화면은 단순한 “자리”가 아니라 학생·운영 기능인데, 지금 동결할 최소 스키마와 나중에 구현할 제품 기능의 경계가 없다.

이 문서보다 나은 대안 구조는 1개다.

`person`(불변 사람 ID) — `enrollment_episode`(등록 회차·시작일·종료일·종료사유·완주시즌) — `account_identity`(복수 계정 연결) — `student_code_history`(번호 이력) — `lifecycle_event`(불변 이벤트 ID·효력 시각·원천 행 ID·멱등키)로 분리한다. `learners`는 이 원본들의 현재 상태를 보여 주는 파생 뷰로 두고, 모든 시트·인제스트·엔진 이벤트는 `person_id` 또는 회차 ID를 반드시 전달하게 해야 한다.