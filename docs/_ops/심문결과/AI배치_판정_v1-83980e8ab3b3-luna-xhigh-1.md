[P0] 미실행된 이종 심문을 동결 가능한 상태로 표시한다 — §3 #3·§4·§5는 심문을 최우선 소급불가 게이트로 두지만, 제미나이 심문은 실제로 한 번도 실행되지 않았다. 그런데 `tools/모델정책.js:286`은 이 자리를 `돈다`로 표시하고, 실행 전 동결을 막는 조건도 없다. 재현 입력: 제미나이 심문 장부가 없는 상태에서 #3 동결을 요청하면, 미검증 상태가 정상 배치처럼 남는다.

[P1] 현재 GPT와 아스트라의 점수를 바꿔 끼웠다 — §0은 현재 GPT를 luna, 아스트라를 대기 상태라고 하면서 §3 #8·#9의 근거로 아스트라의 Terminal-Bench 점수 57.7을 사용한다. 실제 배치표도 #8은 GPT로 `돈다`, #18은 아스트라 대기로 분리되어 있어 현재 실행 모델에 대한 근거가 없다(`tools/모델정책.js:292,304`). 재현 입력: 현재 GPT luna로 대규모 코드 작업을 선택하고 57.7의 원 모델을 추적하면 아스트라만 남는다.

[P1] 근거 수치가 정본과 갈리고 재현 재료가 없다 — §1은 외부 숫자를 고정했다고 주장하지만 출처 URL, 시험 분모, 모델·효력, 과제 분할, 채점법이 없다. 후보 문서는 73.7을 쓰는데 `tools/모델정책.js:801-806`에는 여전히 73.8이 남아 있고, `docs/_ops/GPT_세팅_재판정_0904.md:150-152`는 다시 73.7로 적는다. 재현 입력: 저장소에서 `73.7`과 `73.8`을 함께 검색하면 서로 다른 값으로 #11·#21·#22의 배치 근거가 갈린다.

[P1] 측정 전의 핵심 학생 산출물을 최종 배정했다 — §3 #21·#22·#23·#31·#32는 각각 미측정·미배선·크레딧 대기인데도 제미나이로 얼려져 있고, §4에서는 그중 여러 자리를 사업 가치 최상위로 올린다. `tools/모델정책.js:308-321`과 `834-848`도 이 상태를 확인하며, 이는 “가장 잘하는 쪽”이라는 ① 규칙을 적용할 근거가 없다는 뜻이다. 재현 입력: “왜 #23·#31·#32가 제미나이인가”를 물으면 측정값이 아니라 “아무도 안 쟀다”, “유일 후보”, “대량”만 나온다.

[P1] ④번 자는 일감 분류 규칙이 아니라 사후 벤치마크 서사다 — §2 ④는 DeepSWE와 Terminal-Bench의 서로 다른 시험을 “한 번에 답하기/도구 오래 붙들기”로 번역하지만, 실제 일감에 필요한 도구 호출 수·실행 시간·환경 변경·출력 형식의 판정 칸이 없다. 같은 Terminal-Bench도 2.1과 4.0에서 결과가 뒤집힌다고 스스로 인정한다(§2 ④, `tools/모델정책.js:856-861`). 재현 입력: 도구를 쓰는 코드 수정 1건을 #8과 #11 중 어디로 보낼지 요청하면 문서 안에는 결정 가능한 규칙이 없다.

[P1] 32자리 표에서 학습 사슬이 끊긴다 — §3의 대부분 항목은 손과 이유만 있고 `기능→이벤트→필드→엔진 소비자→회수 시점`이 없다. `tools/모델정책.js:280-281`도 의도적으로 손·상태만 보관하며, #21·#22·#32에는 계약 필드와 착지 소비자가 연결되어 있지 않다. 재현 입력: #21 또는 #32에 대해 어떤 사건을 어떤 필드로 저장하고 어느 엔진 부품이 언제 읽는지 물으면 제미나이라는 벤더명 외의 답이 없다.

[P1] #32의 “성향 추출 설계 완료” 주장은 근거 고리를 생략한다 — `docs/코어엔진_설계.md:190,281,286`은 성향 추정에 근거 이벤트, 추정 버전, 유효기간, 확신도, 학생 확인 사건을 요구하고 라벨은 원본이 아닌 파생값으로 규정한다. 후보 문서 §3 #32·§4 3번에는 이 필드·버전·재계산·소비자 연결이 전혀 없다. 재현 입력: 같은 숙제 신호를 새 추정 버전으로 다시 계산할 때 기존 추정의 근거와 유효기간을 복원할 경로가 배치 문서에 없다.

[P1] 학생에게 가는 과제·채점이 기본 게이트를 측정하지 않는다 — §5의 #21·#22 측정 네 가지는 사실 오류·수준 적합·사람 수정 시간·결국 사용 비율뿐이다. `docs/SYNK_철학.md:92,94`의 재미, 실제 산출이 엔진에 닿는가, 신선함, 자기설명은 빠져 있어 정확하고 한 번 쓰고 버리는 콘텐츠도 통과할 수 있다. 재현 입력: 정확하지만 재미없고 학생 산출 이벤트가 생기지 않으며 첫 화면에서 사용법을 설명하지 않는 퀴즈를 넣으면 네 지표만으로는 실패하지 않는다.

[P1] “32자리 전수”가 돈이 걸린 외부 사실 적대 검수를 누락한다 — 후보 §3 #19는 GPT의 넓은 웹 조사 하나로 끝나지만, `docs/AI_스택_가이드.md:280-306`은 법령·업종코드·상표·시세 같은 외부 사실에 독립 조사원 둘의 반박과 판정 장부가 별도로 필요하다고 명시한다. 이 누락은 단순 웹 검색 품질 문제가 아니라 반박받지 않은 답과 판정완료 답을 구분하지 못하는 운영 결함이다. 재현 입력: 돈·기한이 걸린 외부 사실을 #19에 보내면 독립 반박, 불일치 판정, 확인 못 한 항목의 장부 착지가 없다.

[P1] 라이브 시트 점검은 제미나이 배정만 있고 실행 통로가 없다 — §3 #20은 제미나이를 지정하지만 `tools/모델정책.js:306,835-837`은 이를 `미배선`, “설계만·미구현”으로 표시한다. 실제 가이드는 시트 안 `Ask Gemini`를 사람이 직접 쓰는 경로로 설명하므로 모델 배치와 수동 조작이 같은 자리에 섞였다(`docs/AI_스택_가이드.md:265,426`). 재현 입력: #20을 자동 실행하라고 요청하면 호출 명령·입력·결과 저장·실패 상태 중 어느 것도 정해져 있지 않다.

[P2] 정확히 여덟씩 나눈 것은 위험 예산이 아니라 자리 수 맞추기다 — §0·§3의 8·8·8은 호출량, 작업 난도, 학생 영향, 토큰 소모, 되돌림 비용을 전혀 세지 않는다. 80요청이 필요한 #21·#22와 단일 문서 심문을 같은 한 자리로 세어도 구조 시험은 통과한다(`tests/모델정책.test.js:625-635,666-671`). 재현 입력: #21·#22의 80요청 배치와 #1의 문서 1건을 비교해도 배치표의 부담 분모는 동일하게 1이다.

[P1] 아스트라 교체의 영향면을 한 줄로 묶어 검증을 생략한다 — §6은 `검수선택지` 한 줄을 바꾸면 심문·검수·실행이 모두 따라온다고 한다. `tools/모델정책.js:104,113-115,175-185`에도 같은 결합이 있어, 비용과 회귀 영향이 큰 모델 교체가 세 파이프라인에 동시에 퍼지지만 역할별 수용시험·롤백 조건이 없다. 재현 입력: 아스트라가 열리는 날 한 줄을 옮긴 뒤 설계 심문, 배포 검수, 실행 레인의 실제 모델과 효력을 각각 대조하면 세 값이 함께 바뀌지만 어느 하나라도 실패했을 때 독립적으로 되돌릴 수 없다.

읽은 파일: `docs/AI배치_판정_v1.md`, `docs/SYNK_철학.md`, `docs/코어엔진_설계.md`, `docs/엔진도달_설계.md`, `docs/AI_스택_가이드.md`, `docs/_ops/GPT_세팅_재판정_0904.md`, `계약/수집_교정_계약.json`, `tools/모델정책.js`, `tools/몽골어대조.js`, `tools/ai스택점검.js`, 관련 모델정책 시험.

안 본 것: 형제 저장소 `SYNK-talk` 전체, 실제 라이브 시트·DB, 인용된 외부 벤치마크 원문.

이 문서보다 나은 대안 구조가 있으면 1개: **32자리 목록을 동결하지 말고, 각 일감마다 `근거·현재 상태·입력 계약·출력 이벤트·엔진 소비자·회수 시점·학생 게이트·독립 검수자`를 한 행에 묶은 증거 행렬로 재구성하라. 모든 칸이 채워지기 전에는 모델 배정을 `후보`로만 두고 동결하지 않는 구조가 낫다.**