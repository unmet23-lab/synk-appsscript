# C0 동결 심문 97건 — 세 갈래 처분표

> 🔑 **이 문서는 「누가 처분하나」 층이다** — 지적을 **축**으로 가른 것은 같은 폴더의
> **`C0_API계약-d82f81ec45dc-종합판정.md`**(4판 합집합 **65건** · 원본 122건 · `local_f5700cd3` 가
> 나와 **동시에** 완성했다). 그쪽이 **지적의 정본**이고 여기는 **처분의 정본**이다. 겹치면 종합판정이 이긴다.
> **원문** = `C0_API계약-d82f81ec45dc-{luna-1,luna-2,sol-3,sol-1}.md`(35·29·33·25건 · 편성 luna×2+sol×2).
> ⚠ **아래 A/B/C 는 앞 3판(97건) 위에서 갈랐다** — 4판째(sol-1)가 새로 낸 것은 종합판정 축 A~E 에 있고,
> 그중 처분이 갈리는 것은 **C4**(정답 유출 = 오독)로 아래에 편입했다.
> **판정** = 4판 만장일치 `failed_p0`(동결 불가).
> **대상 정본** = SYNK-talk `docs/C0_API계약.md`.
> ⚠ **금지 지문이 3회차 다 갈렸다**(`2d9b13a8`·`1d926474`·`921f6726` · 장부 F281) — 심문이 도는 20분
> 사이 `MEMORY.md` 를 고쳤다. **합집합은 유효하나 「모델만 다름」 대조로는 못 쓴다.**

## 처분 원칙

| 갈래 | 무엇 | 누가 처분하나 |
|---|---|---|
| **A** | 문서가 구현을 못 따라갔다 — **코드 0**, 문서만 고치면 사라진다 | AI(가역 내부 편집) · **✅ 2026-08-09 반영 완료** |
| **B** | 진짜 구멍 — 코드·계약 개정이 필요하다 | ⏳**유호 판정**(개원 전 범위) |
| **C** | 심문자 오독 — 반영하지 않는다 | AI(근거를 여기 남긴다) |

🔑 **A 를 먼저 턴 이유** = B 판정의 재료가 되기 때문이다. 문서가 세 판본으로 갈라져 있으면
「진짜 구멍」과 「문서만 낡음」이 같은 모양으로 보이고, 유호님께 올라가는 B 목록이 부풀려진다.

---

## 갈래 A — ✅ 반영 완료 (talk `docs/C0_API계약.md` · 코드 0)

각 줄의 처분은 **코드를 먼저 열어 실측한 뒤** 문서를 맞춘 것이다(반대가 아니다).

| # | 지적 | 실측 | 처분 |
|---|---|---|---|
| A1 | 한 문서 안 판본이 **c6·c9·c10 세 갈래**(3벌 전부) | 계약 `버전 = c10` | §0 표 `c6`→`c10` · §4-1 payload `c9`→`c10` · §9 를 **「이력 절」**로 명시(현행 규범 아님) |
| A2 | 「엔드포인트 둘」인데 실제 **8개**(3벌 전부) | 쓰기 2 + 조회 3 + 인증 3 | §4 머리에 **8개 표 + 동결 축**을 세웠다 — 왕복 6이 재는 것은 **쓰기 2뿐**임을 못박음 |
| A3 | §10 「`progress` 남았다」 vs §4-3 「구현 완료」 | 셋 다 구현·리허설·⛔운영 미배포 | §10 줄 교체(렌더 증명 `f4437f0` 포함) |
| A4 | 발화 길이 정본이 갈라졌다(2벌) | `events/index.ts:471` 이 `audio_duration_sec` 를 쓴다 | §4-2 에 **정본 = `submissions.audio_duration_sec`(초)** 병기 · 봉투 `duration_ms` 는 같은 값의 관측 기록 |
| A5 | §5·§7 이 「앱에서 동의 화면이 뜬다」(2벌) | 닫힌 결정 = 앱 안 동의 화면 🚫 · 실제는 **막힘 카드** | §5 `CONSENT_MISSING` 칸 · §7 표 5 둘 다 교체 — **큐 정지**(`보낼것(로그,막힘)` · talk `f8c8758`)까지 명시 |
| A6 | 서버 채움 표에 `consent_ver` 만 있고 `consent_id` 가 없다 | `events/index.ts:299·446` 이 **둘 다** 적는다 | §4-1 서버칸을 두 칸으로 — 「철회는 행 단위」 근거 병기 |
| A7 | `goal_snapshot` 이 「앱 필수」인데 검증기는 선택(2벌) | `이벤트검증.js` `선택필드` 에 사유와 함께 있다 | §4-1 에 **실측 병기**(필수 아님 + 키존재·null 미구분) — 처방 자체는 **B7** |
| A8 | 「이미 등록됨」이 401 게이트 실패에 섞였다 | `auth/index.ts:469` = `200 { already: true }` | §4-4 표 401 줄에서 뺐다(바로 위 성공 줄이 이미 정확했다) |
| A9 | `AUTH_EXPIRED` 계약이 스스로 거짓(2벌) | `verify_jwt=true` — 게이트웨이가 함수 **앞에서** 자른다 | §5 표에 「이 코드를 내는 서버 코드는 0줄」 + 앱 분기는 **HTTP 401 자체** · 「`code`로만 분기」의 **유일한 예외**로 명시 |
| A10 | 머리말 「초안이라 구현 금지」 아래 라이브 기록 | 인증 3종 라이브 · 조회 3종 리허설 | 머리말 교체 — 위험은 「미승인 구현」이 아니라 **문서 뒤처짐**이고, 고칠 때 **코드를 먼저 연다** |
| A11 | 오프라인 큐 정지가 문서에 0(2벌) | talk `f8c8758` 이 세웠다 | §5·§7 에 반영(**귀속** 규약 자체는 **B19**) |
| A12 | `session_id`·`content_id`·`task_schema_ver` 가 필드 정본에 없다(2벌) | 아래 **C2** 참조 — 절반은 오독, 절반은 참 | §3 에 실측 주석(무엇이 틀렸고 무엇이 참인지) · 처방은 **B4** |

---

## 갈래 B — ⏳ 유호 판정 대기

### 내 안 = 개원 전에는 **셋만** 친다 (「데이터가 지금 잘못 쌓이는 것」만)

| # | 구멍 | 왜 개원 전인가 |
|---|---|---|
| **B1** | **역할별 `event_type` 허용표가 0** — 학생 토큰으로 `task.assigned`·`exam.result`·`intervention.delivered`·`data_use.granted` 를 주장할 수 있다(3벌) | 오염이 **append-only 에 섞여 들어간다** — 나중에 표를 만들어도 이미 쌓인 행은 못 가른다 |
| **B2** | **멱등키 충돌이 손실을 「성공」으로 위장** — 내용 비교 없이 `duplicate` + 기존 `event_id`, 앱은 큐에서 지운다(3벌) | 사라지는 쪽이 **학생 발화**다. 증상이 「조용함」뿐이라 개원 후엔 발견도 못 한다 |
| **B3** | **첫날 발화가 사건으로 안 남는다** — 배정 0이면 고정 도입 과제 ↔ `task_ref` 없으면 보내지 마라(2벌) | **개원 첫날에만** 나는 기준선이다. 그날이 지나면 소급 0 |

자기반박: B1 은 「개원 전엔 학생이 없으니 위조도 없다」로 미룰 수 있다 — 그러나 막는 표를 나중에 세우면
**그 전까지 쌓인 행에 신뢰 등급이 없어** 전량을 의심해야 한다. 그래서 셋에 남겼다.

### 나머지 B (개원 후 후보 · 유호님이 범위를 넓히면 편입)

- **B4** 필드 정본 **역방향 회귀 0** — 저장하는 열이 계약에 있는지 아무도 안 본다(이번 실측 발견 · A12·C2 의 뿌리)
- **B5** `data_use.granted` 빈 껍데기(`artifact_ref`·`content_hash`·`dataset_ver`·승인자 0 · 3벌) — 생산자도 0
- **B6** 철회 뒤 서명 URL 생존 · 고아 파일 정리 0(3벌)
- **B7** `goal_snapshot` 「키 존재 + null」 검사 없음 — 앱 결손과 「몰랐다」가 같은 `null`(2벌)
- **B8** `degraded` 가 제출 INSERT 에 안 실려 DB 기본값 false — 강등 과제 발화가 정상으로 기록
- **B9** 부분 실패 보장이 `null` 원소 하나로 깨진다(루프 밖 `사건.idempotency_key`)
- **B10** 타학생 `correction_id` 가 **재시도 가능 5xx** — 영구 오류가 큐를 잡는다(3벌)
- **B11** `attempt_no` 오버플로 → `/progress` 전건 5xx
- **B12** `corrected_text is not null` 이라 `''` 가 빈 교정 카드로 샌다
- **B13** 25MB 가 클라이언트 주장뿐 — 직접 PUT 뒤 실제 크기 미검증
- **B14** `occurred_at` UTC 강제 0(`Date.parse` 가 날짜 전용·오프셋을 받는다)
- **B15** `temp-login` 1회용이 **동시 요청**에 안 깨진다(조건부 소비·잠금 0 · 2벌)
- **B16** `first-login` 이 `첫배정세우기` 실패에도 200 — 첫날 폴백으로 내려간다
- **B17** `content.viewed` 가 앱 종료 전 미전송이면 소멸
- **B18** 매 요청 `session_alive()` 미검사 — 만료 전 access token 생존(2벌 · ⏳기존 유호 대기 「토큰 폐기」)
- **B19** 오프라인 큐 **계정 귀속** 규약 0 — 기기 공유·계정 전환 시 남의 사건이 된다(3벌)
- **B20** `learning_events` ↔ `submissions` 원자성 경계 미정의 — 반쪽 행이 `duplicate` 로 굳는다
- **B21** Storage 원본 **불변 증거 0**(전체 바이트 해시·덮어쓰기 금지 없음)
- **B22** `RATE_LIMITED` 가 이름뿐(키·시간창·상한 0)
- **B23** 격리 큐가 복구 계약이 아니다(저장 위치·보존·재처리 0) — 앱 삭제 = 영구 삭제
- **B24** RLS 우회 직결이라 `where learner_id` 가 유일 방어선인데 **전 경로 부정 테스트 0**(3벌)
- ~~**B25** `/tasks` 가 정답을 학생 기기로 내린다~~ → **C4 로 이동**(실측 반증 · 아래)
- **B26** WAV/AGC — 손잡이 없음 + 실기기 2대 미측정(3벌 · ⏳기존 유호 대기)
- **B27** 웹 통로 CORS 0(F275)
- **B28** 이미지 업로드에 소비자 0 — `image_ref` 가 이벤트 계약에 없다
- **B29** `task_ref` 가 인과 식별자가 아니다(FK·유일성·배정 `event_id` 요구 0)
- **B30** `task_snapshot` 이 두 뜻 — 정답 포함 요구 vs `/tasks` 가 제거해서 준다
- **B31** skill taxonomy 가 **판본 존재**만 확인(같은 id 의 판본 간 재사용을 못 막는다)
- **B32** 하위호환 426 정책이 거짓 — 구 앱은 변환도 426도 아닌 **영구 `rejected`**
- **B33** `schema_ver` 가 저장 시점 판을 찍어 **생산 판을 거짓말**한다(오프라인 혼합 큐)
- **B34** 최초 제출이 배정·개입에 안 묶인다(`intervention_id` 계승이 `retry_of_event_id` 있을 때뿐 · 2벌)
- **B35** `retry_of_event_id` 의미 미강제(제출 사건인지·같은 학생인지·교정이 확정됐는지)
- **B36** 개입 귀속이 원 개입 A vs 오늘 배달 B 로 **미결인 채 행이 쌓인다**
- **B37** `/progress` 날짜 원천 미정(`occurred_at` vs `ingested_at`)
- **B38** 교정 목록이 **확정 교정만**을 보장하지 않는다(actor_kind 필터 0 — AI 초안이 최종처럼 보일 수 있다)
- **B39** 엔진 도달 0(3벌) — 정본은 appsscript `docs/엔진도달_설계.md` 이고 C0 의 몫이 아니다

---

## 갈래 C — 오독 (반영하지 않는다)

- **C1** sol-3 「§2·§4-4~4-6 이 **폐기된 인증 통로를 부활**시켰다(F269 충돌)」 — **틀렸다.**
  F269 가 지운 것은 `tools/로그인코드발급.js`(**도구**)이고 `first-login` 은 **그 대신 세운 정상 통로**다.
  같은 지적의 「재발급 E1 대기와 충돌」도 축이 다르다 — 닫힌 결정의 「재발급」은 **`student_code` 재발급**이고
  §4-5·§4-6 은 **비밀번호** 초기화·임시로그인이다. 심문자가 🚫 목록을 반대로 읽었다.
- **C2** luna-1 #5 · sol-3 「`session_id`·`content_id`·`task_schema_ver` 를 앱이 보내면 **미등록 필드로 거절**된다」
  — **틀렸다.** `functions/events` 가 넷(+`consent_id`)을 읽어 `insert` 하고(`index.ts` 235·419·426·470·477),
  `lib/이벤트검증.js` 에는 **미등록 필드를 거절하는 로직이 아예 없다.**
  🔑 **틀리지 않은 절반** = 정본이 뒤처졌다(계약 JSON 필드 목록에 그 넷이 없다) → **B4** 로 옮겼다.
- **C4** sol-1·sol-3 「`task_snapshot` **정답이 학생 기기로 유출**된다」 — **라이브 누출은 틀렸다.**
  `SYNK-talk/supabase/functions/tasks/index.ts:160` 이 `학생판스냅샷()` 으로 **허용 키만** 내보낸다
  (luna 두 판은 정반대로 정확히 읽었다). 🔑 **sol 두 판이 나란히 틀리고 luna 두 판이 나란히 맞았다** —
  회차를 늘려도 안 잡히는 오독이고, 유호님이 편성에 **모델 축**을 넣은 근거의 실측이다.
  🔑 **절반은 유효** — §4-1 은 `task_snapshot` 에 정답을 넣으라 하면서 **학생판 필터의 존재를 어디에도 안 적었다.**
  문서대로 구현하면 누출된다 → **P0 기각 · 문서 결함으로 채택**(종합판정 B7 · **B30 과 같은 자리**).
- **C3** 세 벌 공통 「C0 를 **기계계약 매트릭스로 재구성**한다」 — **기각.**
  개발 분업 **§8-1 「기존 L0·C0·M0 재작성 금지」**이고 개원 전 크기가 아니다.
  ⏳ 대신 낸 **도전안**(유호 판정 대기) = 재작성이 아니라 **정본의 자리를 옮긴다** — 계약 JSON +
  `lib/이벤트검증.js` 를 정본으로 승격하고 C0 문서를 사람용 안내로 격하하면 **갈래 A 가 원리상 사라진다.**

---

## 남은 절차

⏳ **유호 판정 4** — 앞 둘은 3판 시점에, 뒤 둘은 4판 종합이 올렸다. **넷 다 이미 큐에 있다(재질의 금지).**

1. 갈래 B 중 **개원 전 범위**(내 안 = **B1·B2·B3**).
2. **도전안**(C3 · 정본의 자리를 옮긴다) 채택 여부.
3. **동결 범위 절단** — 여덟으로 넓히나 / `events`+`uploads/sign` 둘로 자르나(sol-3 권고 = 자르는 쪽).
4. **미측정 축을 동결 밖으로 명시** — 음성 AGC·실기기 · 인증 재발급 · 철회 전파.

**AI 몫(판정 없이 가는 것)**

- ✅ **판본 통일**(종합판정 게이트 2의 앞 절반) — 2026-08-09 완료(talk `98e6529`).
- 🔴 **그 뒤 절반 = 회귀가 아직 없다.** 지금 `tests/C0계약.test.js` 는 `contract_ver` **예시**는 계약과
  대조하지만 **산문의 판본 표기**(「현행 c6」 같은 말)는 안 본다 — **그래서 c6·c9·c10 이 갈라진 걸 아무도
  못 봤다.** 고쳐만 두면 같은 자리에서 또 갈라진다.
  ⚠ **그 파일은 지금 `2bcd8620`(F272 트랙)의 살아있는 작업본이라 손대지 않았다**(F073).
  → **다음 세션 몫**: 그 트랙이 커밋되면 「문서 산문의 `cN` 표기 = 계약 JSON `버전`」 회귀를 그 파일에 얹는다.
- 🔴 **B4 의 역방향 회귀**도 같은 성격이다 — 「저장하는 열이 계약 필드 목록에 있는가」를 보는 검사가 0이다.
- 판정이 오면 **2차 심문**을 던진다 — 🔑 **던지기 전에 입력(금지 지문·정본)을 얼린다**(F281 · 이번 런은
  네 판이 서로 다른 금지 지문을 들고 돌아 「어느 모델이 더 잡았다」를 못 쟀다).
