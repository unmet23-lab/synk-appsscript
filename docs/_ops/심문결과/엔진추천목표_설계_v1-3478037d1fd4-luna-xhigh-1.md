동결 불가. P0가 다수이며, 현재 문서는 구현 발주서가 아니다.

1. [P0] c14 목표 왕복을 c13 확인 사건으로 오배선한다 — 후보 §3-2·§6·§8 (`docs/엔진추천목표_설계_v1.md:174-180,261-262,308`)은 `estimate.responded`와 `맞다|아니다`를 사용한다. 정본은 `goal.responded`와 `해냈다|아직`, `class_date`, `card_version`을 요구한다(`docs/교실수집_목표왕복_설계_v1.md:27-30`, `SYNK-talk/lib/이벤트검증.js:162-168`). 재현 입력: `shown_key=goal_id`인 목표 카드에서 학생이 `맞다`를 탭하면 c13 확인축에 들어가거나 거부되고, c14 목표축에는 사건이 생기지 않는다.

2. [P0] v1에서 금지된 과녁 스냅샷을 몰래 v2로 넣는다 — 후보 §3-1·§3-2 (`docs/엔진추천목표_설계_v1.md:143-160,173-177`)은 `goal_text`를 저장하고 카드 문구에도 목표 내용을 넣는다. 기존 정본은 v1에서 과녁 텍스트를 저장·노출하지 않으며, 스냅샷은 별도 v2 판정으로 남겼다(`docs/교실수집_목표왕복_설계_v1.md:10,70`, `docs/_ops/결정.md:153,156`). 재현 입력: 현행 c14 카드와 이 후보를 같은 버전으로 배포하면 한쪽은 「오늘 아침에 말한 목표」, 다른 쪽은 실제 문법 태그를 물어 카드 의미와 재현 기준이 갈린다.

3. [P0] 추천 행의 저장 장소·사건 봉투·멱등성이 미결이다 — 후보 §3·§3-1·§6 (`docs/엔진추천목표_설계_v1.md:117-129,143-157,259-260`)은 `system` 값이 있는지조차 확인하지 않았고 `learning_events`와 엔진 전용 표 중 하나를 고르지 않았다. 현재 출처 값에는 `system`이 없고, 공통 사건에는 `event_id`, `occurred_at`, `ingested_at`, `correlation_id`, `idempotency_key` 등이 필요하다(`SYNK-talk/lib/사건출처.js:27-29,69-79`, `SYNK-talk/lib/이벤트검증.js:87`). 재현 입력: Drive 파일 생성 후 DB 저장이 타임아웃되고 작업을 재시도하면, 중복 추천 행·종이만 있고 행이 없는 상태·행만 있는 상태를 구분할 수 없다.

4. [P0] `goal_text`가 학생이 실제 본 문장이라는 보장이 없다 — 후보 §3-1·§6 (`docs/엔진추천목표_설계_v1.md:146,160,259`)은 굽기 성공 후 저장하면 스냅샷이 맞는다고 가정한다. 그러나 `엔진_셋업확장.js:3276-3278`의 `circleFitCard_`는 공간 부족 시 `next_one`을 제거하고, 실제 렌더는 `엔진_셋업확장.js:3328-3341`의 fitted card를 사용한다. 재현 입력: 긴 제출문으로 `next_one`이 제거되는 학생을 굽고 원본 카드에서 행을 만들면, 밤에는 학생이 보지 못한 목표를 물어보게 된다.

5. [P0] 목표와 관측 결과를 제출·검수 단위로 연결하지 않는다 — 후보 §3-3·§3-4 (`docs/엔진추천목표_설계_v1.md:200-219`)은 `goal_tag`와 전체 `error_tags`의 단순 일치만 요구한다. `submission_id`, `correction_id`, 검수 판본·시각, 목표 문법이 실제로 사용된 기회(opportunity)를 어떤 필드로 판정하는지 없다(`SYNK-talk/lib/이벤트검증.js:155-156,349-350`). 재현 입력: 같은 날 첫 제출에는 `-(으)로` 오류가 있고 재제출에는 없을 때, 어느 제출을 관측으로 삼는지와 「정답」인지 「문법을 사용하지 않음」인지 복원할 수 없다.

6. [P0] 카드 도달 범위가 G1 제출 완료자에 한정된다 — 후보 §6·§9 (`docs/엔진추천목표_설계_v1.md:262,317-319`)은 `교수멘탈화면`의 「보냈어요」 꼬리에만 붙이고, 숙제 미제출일은 카드가 없다고 인정한다. 실제 앱은 `교수멘탈화면.js:368-392`가 `단계==='대기'`일 때만 카드를 그리고, `말하기화면.js:730-742`에서 G1만 이 화면으로 보내며 G2~G4는 별도 화면이다(`SYNK-talk/src/부품.js:18-23`). 재현 입력: 학생이 종이 목표를 받은 뒤 숙제를 제출하지 않거나 G2 숙제를 제출하면 노출·무응답·전달 실패가 모두 빈 상태가 되어 그날의 자기보고를 영구히 잃는다.

7. [P1] 추천의 입력 근거와 시점 스냅샷이 없어 재현되지 않는다 — 후보 §3-1·§4 (`docs/엔진추천목표_설계_v1.md:143-160,223-237`)에는 `picker_version`만 있고 선택에 사용한 사건 ID, `evidence_refs`, `as_of`, tie-break 규칙이 없다. 실제 `circleShakyPick_`는 당시의 누적 `rs`를 다시 읽어 선택한다(`엔진_셋업확장.js:2970-3006`), 코어 정본은 추정·추천 계산에 근거와 시점 절단을 요구한다(`docs/코어엔진_설계.md:190-194`). 재현 입력: 인쇄 뒤 늦게 교정 행 하나가 들어오면 같은 `picker_version`으로 다른 태그가 선택되며, 최초 선택의 근거를 설명할 수 없다.

8. [P1] 이력은 있지만 태그가 없는 학생의 처리 규칙이 없다 — 후보 §3-1은 이력 0일 때만 행을 생략한다고 하고, §8은 이력 0 외에는 반 인원수만큼 행이 있어야 한다(`docs/엔진추천목표_설계_v1.md:163,307`). 실제 `circleShakyPick_`가 인식 불가능한 태그에서 `null`을 반환해도 `circleMemberCard_`는 fallback `next_one`을 만들 수 있다(`엔진_셋업확장.js:2970-3006,3036-3067`). 재현 입력: 학생 기록이 `오류없음`뿐이거나 문구 매핑이 없는 태그를 가지면 종이에는 fallback이 나오지만 `goal_tag`가 없어, 행을 건너뛰거나 가짜 null을 채우게 된다.

9. [P1] “다음 날 같은 태그 두 번”이 배정 엔진에 연결되지 않았다 — 후보 §7-㉢ (`docs/엔진추천목표_설계_v1.md:291-297`)은 `origin_goal_id`와 `attempt_no`라는 이름만 제시하고 다음 수업·휴일·무응답·카드 미노출의 상태 전이를 정의하지 않는다. `circleShakyPick_`에도 이 사슬을 소비하는 규칙이 없다(`엔진_셋업확장.js:2970-3006`). 재현 입력: 금요일에 `아니요`를 누르고 월요일에 새 오류가 생기면, 월요일 추천이 재시도인지 새 추천인지 판정할 수 없다.

10. [P1] 30일이라는 수치가 보고 기준이 아니다 — 후보 §7-㉣ (`docs/엔진추천목표_설계_v1.md:299-301`)은 “관측이 30일 쌓인 뒤”만 말하고 유효한 paired observation 수, 노출 분모, `잴수없음` 비율을 정하지 않는다. 이 문제는 기존 심문에서도 채택됐지만 아직 문서에 남아 있다(`docs/_ops/심문결과/학생의말_설계_v1-전건판정.md:118`). 재현 입력: 1건만 첫날 관측되고 이후 29일간 카드가 없던 학생도, 30건의 유효 관측을 가진 학생도 같은 문구를 만족해 학부모 보고 게이트가 재현되지 않는다.

11. [P1] ⑤ 자기설명과 학생 경험을 기존 꼬리 카드로 면제한다 — 후보 §3-2·§6 (`docs/엔진추천목표_설계_v1.md:173-190,261-262`)은 한 번의 이진 탭 외에 전체 기능, 아침 목표와 밤 응답의 연결, 답변 후 학생에게 보이는 변화가 없다. 실제 화면은 `몽글의 관찰`이라는 일반 카드로 그려진다(`SYNK-talk/src/교수멘탈화면.js:389-407`). 재현 입력: 처음 온 학생이 G1 숙제 뒤 카드를 보고 `맞아요/아니에요`를 누르면, 이것이 엔진 추천 목표의 회수인지, 다음 학습이 어떻게 달라지는지 알 수 없고 저녁 카드 자체에는 한국어 산출이나 새 경험이 없다.

12. [P1] 하루 한 칸 선점은 동시성 계약이 아니다 — 후보 §3-2·§7-㉠ (`docs/엔진추천목표_설계_v1.md:198-199,276-282`)은 목표 카드를 정렬 밖에서 선점한다고만 하고 원자적 예약·우선순위·중복 응답 규칙을 정의하지 않는다. 기존 확정은 리듬 축과 목표 회수가 하루 질문 예산을 공유한다고 못박았다(`docs/_ops/결정.md:395-396`). 재현 입력: 같은 학생이 두 기기에서 목표 카드와 리듬 카드를 동시에 요청하면 어느 카드가 유효한지, 다른 요청을 duplicate로 할지, 하루 뒤 사라진 목표를 어떻게 복구할지 결정할 수 없다.

읽은 파일: `docs/엔진추천목표_설계_v1.md`, `docs/코어엔진_설계.md`, `docs/숙제서클_설계.md`, `docs/교실수집_목표왕복_설계_v1.md`, `docs/_ops/결정.md`, `docs/_ops/검수기각.jsonl`, 관련 심문결과, `엔진_셋업확장.js`, 그리고 SYNK-talk의 수집 계약·이벤트 검증·출처·학습자 상태·성향 확인·교수멘탈 화면·말하기 화면·부품·C0 계약.

안 본 것: 실제 Supabase/Apps Script 라이브 실행 결과, 원격 화면 실측, 전체 테스트 실행 결과, 위 목록 외 저장소 전체 파일.

대안 1 — 추천은 별도 `goal.recommended` 사건/표로 고정하고 `goal_id`, 출력 artifact ID, 선택 근거 사건, picker 판, `as_of`, 멱등키를 함께 저장한다. 학생 카드는 기존 c14의 `goal.responded`와 `해냈다|아직`, `class_date`, `card_version`을 사용하며 과녁 텍스트는 v1에서 노출하지 않는다. 관측은 검수된 제출·교정 ID와 목표 사용 기회를 기준으로 `재발·미재발·잴수없음`을 파생하고, 유효 paired observation N건 전에는 외부 보고를 차단한다.