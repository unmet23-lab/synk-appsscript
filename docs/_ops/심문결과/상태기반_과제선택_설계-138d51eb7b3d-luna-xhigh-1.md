동결 불가. 지적 12건.

1. [P0] 되돌림 예외가 스냅샷 불변을 무력화한다 — 파일: `docs/상태기반_과제선택_설계.md:1535`. `적재실패→대기`이면 `return NEW`가 불변 필드 검사보다 먼저 실행되어 `branch_snapshot`, `event_draft`, `snapshot_as_of`, `batch_run_id`를 바꿀 수 있다. 재현 입력: 적재실패 job을 대기로 바꾸면서 다른 학생의 draft와 run_id를 함께 UPDATE하면 통과하며, 이후 append-only 사건의 계보를 복구할 수 없다.

2. [P0] `event_draft`에 학생 결속 재료가 없다 — 파일: `docs/상태기반_과제선택_설계.md:2709`, `2738`. 문서는 draft와 원소의 학생을 대조한다고 주장하지만 draft에는 `learner_id`를 싣지 않도록 명시한다. 재현 입력: 같은 날짜의 학생 B draft를 학생 A 원소에 넣으면 날짜 검사는 통과하고 A에게 B의 과제 스냅샷이 착지한다.

3. [P0] `task_snapshot` 내부 검사가 사실상 비어 있다 — 파일: `docs/상태기반_과제선택_설계.md:2717`. 실제 강제는 `호흡[]` 배열과 `차례===3` 행 존재뿐이라 문장·출처·프롬프트 등 착지에 필요한 내부 계약이 적재 시 검증되지 않는다. 재현 입력: `task_snapshot: {"호흡":[{"차례":3}]}`인 draft를 넣으면 적재는 통과하고 마감 착지에서야 malformed가 드러나 학생은 폴백도 못 받는다.

4. [P0] draft 있는 `적재실패`의 마감 복구가 자기 RPC에 없다 — 파일: `docs/상태기반_과제선택_설계.md:1992`, `2448`, `2461`. ⓓ-15는 `event_draft`가 있는 행을 마감 스윕이 닫는다고 하지만 `jobs_finalize_due`는 `대기·claimed`만 열거하고 `적재실패`를 명시적으로 제외한다. 재현 입력: draft가 있는 적재실패 job을 마감 뒤 남기면 스윕·전이표 어디에도 `적재실패→마감폴백` 경로가 없어 영구 잔류한다.

5. [P0] draft 없는 적재실패는 학생 접속 전까지 무한 고아다 — 파일: `docs/상태기반_과제선택_설계.md:1989`, `1996`, `2001`. 문서가 인정하듯 마감 스윕은 이를 닫지 않고, 재적재는 오케스트레이터 재실행 또는 `/tasks` 학생 접속에 의존한다. 재현 입력: 조립 실패 후 학생이 그날 앱을 열지 않으면 `task.assigned`와 `intervention.delivered`가 영원히 생기지 않아 그날의 학습 증거가 소급 불가능하게 사라진다.

6. [P0] `policy_ver` 지문이 4번째 감시 잡을 누락한다 — 파일: `docs/상태기반_과제선택_설계.md:3186`, `3206`. 표는 cron 4잡이라고 정정했지만 실제 지문 입력은 여전히 잡 3종이며 `deliver-check`가 빠져 있다. 재현 입력: `deliver-check`의 주기·쿼리·등록을 바꿔도 나머지 세 잡이 같으면 같은 `policy_ver`로 기록되어 감시가 사라진 날과 정상 날을 판으로 분리할 수 없다.

7. [P0] `GENERATION_MODEL` 누락이 실패가 아니라 무접촉으로 사라진다 — 파일: `docs/상태기반_과제선택_설계.md:4598`, `5555`. 키 없음은 `키없음` 행을 남기도록 했지만 모델 미설정은 오케스트레이터가 행을 전혀 건드리지 않게 되어 스케줄 실패·대상 없음과 구분되지 않는다. 재현 입력: 프롬프트와 API 키는 있고 `GENERATION_MODEL`만 비운 뒤 배치를 실행하면 생성 대상 job·실패 outcome·학생 분모가 남지 않는다.

8. [P0] 현행 게이트 상태가 문서 내부에서 충돌한다 — 파일: `docs/상태기반_과제선택_설계.md:1`, `5455`, `5772`, `5854`. 머리와 집행 도장은 v5.13·#5/#7 완료·#6만 잔여라고 하지만 §16-1은 v5.8이 현재이고 #5도 실물 0이라고 적으며, 다른 절은 §12 회귀가 한 벌도 실행되지 않았다고 한다. 재현 입력: 작업자가 머리와 §16-1 중 하나를 따라 첫 생성 행을 허용하면, 같은 문서가 반대 결정을 내리며 잘못 쌓인 append-only 행은 되돌릴 수 없다.

9. [P1] `loaded_count`에 음수 방어가 없다 — 파일: `docs/상태기반_과제선택_설계.md:1694`, `1725`. 제약은 `loaded_count <= enrolled_count`만 검사하므로 음수 값이 계정식을 통과한다. 재현 입력: `enrolled=17,target=0,loaded=-1,partial=0,skipped_game=9,skipped_existing=9`를 넣으면 합계와 범위 검사를 모두 통과해 실행 장부가 거짓 보고를 저장한다.

10. [P1] 00:00 워커의 정상 종료와 B3 재호출이 동시에 규정된다 — 파일: `docs/상태기반_과제선택_설계.md:760`, `791`. 워커는 00:00에 큐가 없어 끝나는 것이 정상이라고 하지만, 같은 조건인 실행 행 0건은 B3가 즉시 `deliver` 재호출 대상으로 삼는다. 재현 입력: `deliver-daily`가 00:05에 예정된 시각에 00:00 워커가 먼저 실행되면, 정상 종료인지 오케스트레이터 재호출인지 구현자가 임의로 결정한다.

11. [P1] `assignment_status`와 `data`의 일관성 계약이 없다 — 파일: `docs/상태기반_과제선택_설계.md:631`, `645`. 네 상태값은 정의했지만 `있음`이면 data가 있어야 하는지, `오류`이면 stale data를 금지하는지, `생성중`에 어떤 data를 허용하는지 정하지 않았다. 재현 입력: `assignment_status='있음', data=[]` 또는 `assignment_status='오류', data=[이전 과제]`를 보내면 API와 앱 어댑터가 서로 다른 사실을 학생에게 표시한다.

12. [P1] 학생 자기설명 ⑤가 “다른 트랙이 맡는다”는 지목에서 멈춘다 — 파일: `docs/상태기반_과제선택_설계.md:5046`, `5071`, `5515`. 테스트는 카드가 비어 있지 않고 브랜드 바닥을 갖는지만 보며, 실제 문구는 `카피확정=false`이고 전체 그림·재료 연결·첫 행동 설명을 검증하지 않는다. 재현 입력: 처음 보는 학생에게 생성중 카드나 생성 숙제를 띄우면 테스트는 통과해도 학생은 “이게 무엇이고 무엇을 해야 하는가”를 첫 화면에서 알 수 없다(`docs/SYNK_철학.md:92`의 자기설명 ⑤ 위반).

이 문서보다 나은 대안 구조 1개: 기존 `generation_batch_runs`를 불변 실행 매니페스트로 고정하고, job에는 실행 ID·학생/날짜 결속 draft hash·입력 hash만 둔다. 재적재·마감·구제는 허용 갱신 열을 각각 명시한 RPC로 분리하고, draft 전체 JSON 계약과 `status↔data` 매핑표를 적재·API 양쪽에서 동일하게 검사하며, 평가 결과가 없으면 첫 생성 행을 실제로 거절한다.

읽은 파일: [후보 문서](C:/Users/q1212/Documents/SYNK-appsscript/docs/상태기반_과제선택_설계.md), `CLAUDE.md`, `docs/AI_스택_가이드.md`, `docs/SYNK_철학.md`, `docs/제품방향.md`, `docs/코어엔진_설계.md`, 관련 심문 결과 파일.

안 본 것: SYNK-talk 실제 소스·마이그레이션·라이브 DB·원격 CI 실행 상태. 개인정보·동의·보안 축은 요청대로 지적하지 않았다.