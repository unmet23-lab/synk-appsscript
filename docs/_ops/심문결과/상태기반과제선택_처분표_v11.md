# 상태기반 과제선택 — 처분표 v11 (11회차 · 대상 설계 v5.7)

> **런** 대상지문 `6df13955f843`(= v5.7) · 편성 **luna/max 1벌**(유호 08-14 `4ed6ebea` 로 편성 교체 후 **첫 판**) ·
> 결과 = `docs/_ops/심문결과/상태기반_과제선택_설계-6df13955f843-luna-1.md` · 던짐·완료 모두 세션 `ef5a9aa4`.
> **원건 13** — P0 **8** · P1 **5**. 통로 하나·벌 하나라 **합집합 병합이 없다**(v10 은 codex 3벌+fable 1벌 = 91).
> **게이트**(유호 확정 08-12): ①판정 = 원건 하나도 빠짐없이 채택/부분/기각/범위밖 + 근거 · ②집행 = 채택분이 본문에 들어왔다.
> 🚫 이 표로 v1~v10 을 덮지 않는다(회차 대조). 🚫 **원건 수를 앞 회차와 직접 빼지 마라** — 편성이 3벌→1벌로 바뀌었다(F281).
> ⚠ **「만장」 축이 이 판엔 없다** — 1벌이라 «몇 벌이 같은 말을 했나»가 원리상 0이다. 가중치가 사라진 자리를 **실측이 진다**(§3).
> **①판정 = ✅ 종결 — 13/13 매핑 · ✅6 · ◐7 · ❌0 · ⧉0.** **②집행 = ✅ 종결 13/13 — 설계 v5.8**(§8 도장 · 집행자 조정 = §7).

## §1. 조건 지문 — 편성이 바뀐 첫 판이라 «대조»가 아니라 «기준점»이다

| 회차 | 편성 | 원건 | P0 |
|---|---|---|---|
| 9회차 | codex 3벌 | 76 | — |
| 10회차 | codex 3벌 + fable 1벌 | 91 | 47+ |
| **11회차(이 판)** | **luna/max 1벌** | **13** | **8** |

🔴 **13 을 「좋아졌다」로 읽지 마라.** 분모가 셋에서 하나로 줄었다 — 91→13 의 대부분은 **편성 차이**다.
읽을 수 있는 것은 두 개뿐이다: ①**P0 비율이 8/13 = 62%** 로 여전히 높다 ②**13건 중 8건이 v5.7 이 «이번 판에 새로 지은» 자리**다
(1·2·4·5·9·10·11 + 6의 ⑦) — 즉 **고친 자리에서 다시 났다.** 이것이 이 판의 축이다(§4).

## §2. 갈래 표 — 원건 13 → **13갈래**(1벌이라 합칠 것이 없다) · 판정 확정

표기: **L-n** = luna-1 지적 n. 판정 = ✅채택 · ◐부분 · ❌기각 · ⧉범위밖. 줄번호는 v5.7 본문 실측(§3).

| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| **1** | **`적재실패` 에 자동 복구 통로가 «원리상» 없다** — 배치가 완주한 날의 조립 실패는 아무도 다시 안 집는다 | L-1 (P0) | ✅ **채택 · 실측 확인** — 세 통로가 전부 못 닿는다: ㉢`jobs_claim` 은 `대기` 만(`:1557`) · ㉧ 마감 스윕은 **명시 제외**(`:1776` 「`적재실패` 는 여기서 안 집는다」) · B4 재적재는 **오케스트레이터 재실행**이 방아쇠인데 그 재실행의 조건이 「집을 것 0 **그리고** 그날 정본 실행이 미완」이고(`:626~627`) **바로 다음 줄이 「완주면 재호출이 안 난다」를 못박는다**(`:629`). `적재실패` 1건이 있어도 그 배치는 `finished_at` 이 있는 **완주**다 ⇒ 재호출 0. 남은 통로는 ㉨ 구제 하나인데 방아쇠가 **학생 접속**이라, 철학 ㉡(「사람 손을 늘리는 해법을 쓰지 않는다」)이 «학생 손»으로 옮겨 붙은 형태다. 🔑 **감시는 이미 «본다»**(§3-2-a ① 이 `적재실패` 를 센다 `:655`) — 없는 것은 «고치는» 자리다. 처방 = B3 재호출 조건에 **셋째 갈래**: 「그 날짜에 `적재실패` 행이 있고 **마감 전**이고 `load_retry_count <` 상한이면 재호출」. 🔑 **새 잡 0 · 새 칸 0** — 상한 재료 `load_retry_count` 는 v5.7 A13 이 이미 세웠고(`:881`) **지금 아무도 안 읽는다**(소비자가 생긴다) |
| **2** | **구제가 «남의 날짜»의 실행판을 오늘 사건에 찍는다** — 그날 시작 행이 0이면 가장 최근 시작 행에서 읽는다 | L-2 (P0) | ◐ **부분** — ㉠전제 실측 확인(`:1795~1796`·`:1819` — 「그날 시작 행이 아예 0이면 **가장 최근 시작 행**에서 읽는다」 = v10 §7 조정 5). append-only 라 잘못 찍힌 계보는 소급 불가라는 지적이 맞다. ㉡**그러나 「학생마다 다른 `snapshot_as_of`」는 결함이 아니다** — 구제는 정의상 각자 다른 시각에 오고, A6 가 「구제 호출 시점 `now()`」로 이미 판정했다(`:1805`). 배치 «안»의 균일성 요구가 배치 밖에 안 걸린다 ⇒ **그 갈래는 기각.** ⇒ 처방 = **차용을 폐지한다**: 그날 시작 행이 0이면 **`deliver` 가 ⓪`batch_run_start` 를 먼저 부르고** 그 `run_id` 로 ㉨ 을 부른다. 🔑 근거는 D3 자신이다 — 판독기를 돌릴 수 있는 층은 「저장소 산출물을 들고 있는 Edge Fn」인데(`:1791~1792`) **구제 호출자 `deliver` 가 바로 그 층**이다. 새 RPC 0(A3 가 이미 있다)·새 칸 0 ⇒ 구제 job 이 `batch_run_id` 를 «갖고»(복합 FK 가 날짜까지 결속한다) 실행판이 **그 시점의 진짜 판**이 된다. **§7 조정 5 철회** |
| **3** | **`&맥락=구제` 가 안 붙으면 학생 화면 조회가 벤더 경로로 떨어진다** — 누락 기본값이 `배치` 다 | L-3 (P0) | ◐ **부분** — ㉠「실제 호출부가 `learner_id` 만 넘긴다」는 **구현 미착수**다(⛔착수 차단 중 · 결함으로 안 센다). ㉡「기본값 `배치`」 자체는 §3-1 이 근거를 달아 판정한 자리다(`:453~454` — 기본을 `구제` 로 두면 배선을 잊은 날 **배치 전원이 조용히 폴백**이고 그게 더 나쁘다). **그러나 셋째 갈래를 문서가 안 봤다**: 두 실패가 둘 다 「잊었을 때」의 이야기인데, **잊을 수 없게 만들 수 있다.** 실측 = `deliver` 의 호출자는 **①`tasks` Edge Fn ②cron 둘뿐이고 둘 다 우리가 배포하는 서버 층**이다(`:449~450`) — **앱은 `deliver` 를 직접 안 부른다**(앱 → `/tasks` → `deliver`). 즉 구앱 호환(D2)이 필수화를 막지 않는다. ⇒ **`맥락` 을 필수 인자로 올린다**: 누락도 목록 밖과 **같이 400**. cron 호출부엔 같은 마이그에서 `&맥락=배치` 를 박고 §12-21 이 이미 그 등록 SQL 본문을 대조한다 ⇒ **fail-open 이 fail-closed 로 바뀌고 잊으면 시끄럽게 죽는다** |
| **4** | **`payload.ver` 를 «생산자 표식»으로 쓴다 — 같은 문서의 🚫 목록이 금지한 그것** | L-4 (P0) | ✅ **채택 · 자기모순 실측** — 세 자리가 정면이다: §3 ②' 조인이 「**그리고 `payload.ver` = 이 계약의 판**」(`:325~326`) ↔ §11-2 「`payload.ver` 는 사건의 «모양» 판이지 「과제 생성 통로를 지났다」는 표식이 **아니다**」(`:3530`) ↔ §13 「🚫 **`payload.ver>=2` 를 「과제 생성 통로」 표식으로 쓰기**」(`:4210~4211`). **v5.7 의 C3 집행이 자기 판의 🚫 목록을 밟았다.** 처방 = **`출처='생성'` 으로 갈아끼운다** — 이 트랙이 새로 만드는 리터럴이 정확히 그것이고(`:242`), §11-3 이 **이미 정본 삼항**을 적어 뒀다: 「생성분은 `출처='생성'`+`degraded=false`+`generation_outcome='성공'`」(`:3590`). 🔑 **쿼리 면적 +0** — 그 조인은 §11-2 대로 `engine.submissions` 를 이미 타고(`:333~335`) `출처` 는 `task_snapshot.호흡[].출처` 에 산다(`:3484`). §13 의 🚫 를 「조인에도 적용된다」로 넓혀 못박는다 |
| **5** | **`roster_hash` 는 «명단»을 기록할 뿐 «누구를 처리했나»에 안 묶인다** — 학생을 바꿔치기해도 완주다 | L-5 (P0) | ✅ **채택 · 실측 확인** — 굳는 것은 `roster_hash`(정렬 id 목록의 SHA-256 `:1238`)·`enrolled_count`·`level_distribution` 셋이고, 등식은 **개수뿐**이다(`batch_runs_roster_equation` `:1305~1308`). 그래서 학생 A 를 빼고 B 를 넣어도 `loaded_count` 가 같으면 통과하고 `finished_at` 이 정상으로 남는다 — A2 가 「전원 누락이 네 항을 다 통과」를 닫으며 **「전원」은 세고 「누구」는 안 세는 상태**를 남겼다. 처방 = **집합 대조를 ㉠ 이 진다**: `_skipped_game int` 를 **id 목록**으로 넓히고, ㉠ 이 「`_targets` ∪ `_skipped_game` ∪ `existing` 의 learner 집합」을 정렬·해시해 시작 행의 `roster_hash` 와 **같은지** 본다(다르면 예외 = 「전량 아니면 0」 그대로). 🔑 **새 칸 0 · 새 표 0 · 인자 타입 1** — 개수는 `cardinality` 로 파생하므로 등식 CHECK 도 그대로 선다. ⚠ 물리 CHECK 로는 못 건다(서브쿼리 불가 — v10 §7 조정 3 의 그 사정) ⇒ **함수 검사가 곧 물리다**(RPC 밖 쓰기가 권한으로 막혀 있다 `:1197`) |
| **6** | **상태 근거(`evidence_refs.events`)가 학생·시간창에 결속되지 않는다** | L-6 (P0) | ◐ **부분** — ㉤ 착지 대조 여덟 중 ⑦이 **`evidence_refs.as_of` = `job.snapshot_as_of` 하나만** 닫는다(`:1705~1707`). 나머지는 안 닫힌다: 그 `event_id` 들이 **같은 학생**인지 · **이중 cutoff**(`occurred_at <= as_of` **AND** `ingested_at <= as_of` — §11-4 `:3608` 이 정본으로 못박은 그 경계)를 만족하는지. append-only 라 착지 뒤 정정이 없다는 ㉤ 자신의 논거가 이 칸에도 그대로 선다. ⇒ **⑨ 근거 사건 결속** 신설: `evidence_refs.events` **전량**이 ⓐ그 job 의 `learner_id` 사건이고 ⓑ`occurred_at <= job.snapshot_as_of` ⓒ`ingested_at <= job.snapshot_as_of` — 어긋나면 예외. 🔑 조인 한 번(`learning_events`)·새 칸 0. ㉡**`axes_used` ↔ 요약 문자열 일치는 «안 한다»** — SQL 이 자연어 요약을 파싱하게 만드는 일이고(§6-2 「목록의 정본은 코드」와 정면), 그 대조는 §12 회귀·검문(㉭)이 지는 층이다 ⇒ **§13 등재** |
| **7** | **`ingested_as_of` 조회층 경계가 아직 물리 배선이 아니다** | L-7 (P0) | ◐ **부분** — ㉠「지금 없다」는 §16-1 **선행 작업 3**으로 이미 서 있다(`:4518`)·§11-4 가 실측까지 적어 뒀다(`:3619` 「`ingested_at` **0회**」) ⇒ 사실 재론이라 그 자체로는 새 결함이 아니다. ㉡**그러나 새 사실이 하나 있다**: 그 선행 표에서 **「첫 생성 행 «전»에 반드시 서야 한다」가 #1 에만 적혀 있다**(`:4516` — 「→ ② 검증기·마이그 → ③ 그다음 첫 생성 행」). #3(`ingested_as_of`)·#5(판독기)는 그 표식이 **없다** — 그런데 둘 다 **안 서면 첫 행부터 계보가 거짓**이고 append-only 라 소급 불가다. 즉 순서가 «있는 것처럼» 읽히는데 강제는 한 줄뿐이었다. ⇒ 선행 표에 **「첫 생성 행 전 필수」 칸**을 더하고 #1·#3·#5 를 ✓ 로 못박는다(칸 1 · 새 표 0) |
| **8** | **§8-B 평가 게이트가 실제 배포를 못 막는다** — 결과가 0인데 켤 수 있다 | L-8 (P0) | ◐ **부분** — ㉠활성 방아쇠 자체는 잘 골랐다: `prompts/과제생성.md` 가 있으면 차단이 돈다(`:3903~3905`) — 그 파일이 없으면 생성이 원리상 불가라 방아쇠와 능력이 같은 자리다. ㉡**그러나 「어디서 도는 차단인가」가 문서에 0이다** — §12-19 는 눈금만 적고 실행 층(회귀 파일·통로)을 안 정했다. 실행 층이 없으면 「미실행」과 「통과」가 같은 얼굴이고(F207), **원격 CI 는 지금 0건**(Actions 소진 · 리셋 9/1 · 메모리 `github-actions-billing-outage`)이라 「초록」이라 쓸 수도 없다. ⇒ 셋을 못박는다: ①차단은 **회귀로 산다**(로컬 `node tools/test-ci.js` 가 도는 층 — 그래야 `/deploy` 가 진다) ②§16-1 선행 표에 **#6 「현재 `quality_ver` 의 §8-B 결과 1벌 존재」**를 세우고 «첫 생성 행 전 필수 ✓» ③**원격 미검증**임을 §8-B 에 그대로 적는다(F103 정직화 — 「막는다」와 「막을 «수 있게» 지었다」를 안 뭉갠다) |
| **9** | **완주 등식이 음수·재적 초과를 통과시킨다** | L-9 (P1) | ✅ **채택 · 반례 재현 확인** — v5.7 A2 가 연 두 칸에 **부호·상한 CHECK 가 0**이다(`:1266~1267`). 심문자 반례를 제약 전량에 넣어 봤다: `enrolled=17·target=0·loaded=0·partial=0·skipped_game=-1·skipped_existing=18` ⇒ `counts_pair`✓ `counts_order`✓ `enrolled_nonneg`✓ `partial_range`✓ `finished_cols`✓ `roster_equation`(0-1+18=17)✓ — **전부 통과**한다. 🔑 **v5.6 A2 의 기소문이 「음수 검사도 0이었다」**(`:1276`)인데 **v5.7 A2 가 새로 연 칸이 그 규율을 안 받았다.** 처방 = `skipped_game_count >= 0 and skipped_existing_count >= 0 and skipped_game_count <= enrolled_count and skipped_existing_count <= enrolled_count`(null 허용 — 완주 전엔 빈다). 🔑 등식과 «다른 층»이라 둘 다 둔다(등식은 합, 이건 각 항) |
| **10** | **`skill_ids` 가 `engine.skills` 에 안 묶인다 — 「적재에서 거절」이 가리키는 검사가 없다** | L-10 (P1) | ✅ **채택 · 실측 확인** — §6-0 은 「**존재 검증** = `engine.skills` DB 대조(없는 ID 는 **적재에서 거절**)」이라 적었는데(`:2691`), ㉠ 의 **적재 의미 검사 열거 ①~⑥ 은 전부 `branch_snapshot` 키에 대한 것**이고(`:1443~1453`) `event_draft` 만 별도 검사를 받는다(`:1454~1459`) — **`skill_ids` 항이 0이다.** 물리 제약도 개수 1~2(`:997`)와 `skill_taxonomy_ver` 비공백뿐이라, 오탈자·삭제된 ID 가 «성공» 행에 그대로 앉고 후속 기술 조인이 0행이어도 생성은 초록이다. 🔑 **v10 축 ①(「규칙이 가리키는 정본 칸이 비어 있다」)의 여섯 번째 자리**다. 처방 = 적재 의미 검사 **⑦ 신설** — 원소의 `skill_ids` 전량이 `engine.skills` 에 실재하지 않으면 거절(비대상·`적재실패` 는 `'{}'` 라 자동 면제). ⚠ **「그 분류판에 속하는가」는 지금 안 적는다** — `engine.skills` 에 판 칸이 있는지 «안 열어 봤다» ⇒ 열 이름을 지어 적으면 v10 §7 조정 10 과 같은 형이다. **집행 시 실물 확인 뒤 별항**(§7 등재) |
| **11** | **`attempt_open` 멱등이 «벤더가 이미 불렸는가»를 못 가른다** | L-11 (P1) | ✅ **채택 · 자기모순 실측** — A16 은 「같은 fence·같은 `_request_body` 해시의 **열린** 시도가 있으면 그 `attempt_id` 를 돌려준다」다(`:1605~1609`). 그런데 **같은 문서가 그 «열린 채»의 뜻을 이미 정했다**: 「응답 직후 죽은 워커의 행은 `raw_response is null · result is null` 로 남고, 그 «열린 채»가 **「받았는지 모른다」의 증거**다」(`:1621~1623`). ⇒ A16 은 **「벤더가 불렸는지 모르는 행」을 재사용해 워커를 그 호출 경로로 다시 들여보낸다** — 막으려던 중복 과금이 그 통로로 되돌아온다. 처방 = **재사용을 거절로 바꾼다**: 같은 fence·같은 해시의 열린 시도가 있으면 `reject_reason='중복열림'`. 워커 계약은 이미 사유로 갈리므로(B9 `:1612~1615`) `판불일치` 와 **같은 처분** — 벤더를 안 부르고 그 자리에서 ㉤ 폴백 착지. 🔑 **새 칸 0**(사유 값 1) · 그 열린 행은 그대로 남아 감시 ④(B7)가 센다 — 돈이 나갔을 수 있는 자리가 «적색»으로 뜨는 것이 정확한 상태다. ⚠ 대가 = 응답이 유실된 회차의 그 학생은 그날 폴백을 받는다. 「돈이 두 번 나가는 것보다 폴백 한 번이 싸다」가 이 판정이다(A16 자신이 「돈이 나가는 자리」라 적은 축 그대로) |
| **12** | **구앱은 `생성중` 을 계속 `없음` 으로 그린다** | L-12 (P1) | ◐ **부분** — ㉠전제 실측 확인(`:514~518` — 「`assignment_status` 를 안 읽는 클라이언트는 `data` 만 보고 «있음/없음»으로 그린다」). ㉡**그러나 「호환 규칙을 없앤다」는 기각**이다 — 그 한 줄이 없으면 구앱이 미지 값에 예외를 내거나 빈 화면을 그리고, 그건 **학생 화면에서** 처음 발견된다(같은 절이 이미 그 대가를 적었다). 신·구 동시 운영은 스토어 심사 때문에 **정상 상태**다. ㉢**노출 창을 재 봤더니 좁다**: 학생이 앱을 여는 시간대는 **전부 마감(06:00 UB) 뒤**이고(`:1715` — B1 이 그 실측으로 섰다) 마감 뒤에도 `생성중` 이 남는 날은 감시 ①이 **적색인 사고 날**이다. 즉 구앱 학생이 이 차이를 만나는 창 = 「사고 난 날 + 구제가 claim 을 못 잡은 순간」이고, 그 상황의 구앱 화면은 **현행 라이브와 같다**(후퇴 0). ⇒ 처방 = ①그 좁은 창을 §11-2 에 **명문으로** 적어 다음 회차가 다시 집지 않게 하고 ②**활성 시작일 상수의 «정하는 규칙»에 「c12 신앱 배포 뒤」를 1줄** 더한다(새 상수 0 — v5.4 C5 가 세운 그 상수의 정의를 채우는 것) |
| **13** | **학생에게 쓸 이유가 있는 생성문인지 «증명»하는 시험이 비었다** | L-13 (P1) | ◐ **부분 · 8과 합류** — 알맹이(「§8-B 가 아직 안 돌았고 배포를 안 막는다」)는 **8과 같은 사실**이라 처방도 8이 진다(중복 처방 금지 — 두 곳에 적으면 갈린다). 고유분 = §12-29 의 **표시**다: 그 항은 「생성분이 ③답하기 통로를 탄다」 **종단 배선**을 재는 것이 목적이고(`:4097`), 그 목적 자체는 옳다. 문제는 **그 초록이 «품질의 초록»으로 읽힌다**는 것 ⇒ 그 항 머리에 한 줄: 「**이 항은 배선만 잰다 — 품질은 §8-A·§8-B 가 진다. §8-B 결과가 0인 동안 이 초록은 철학 ① 게이트의 초록이 아니다**」(F207 정직화 · §12-19 가 이미 쓴 것과 같은 형태) |

## §3. 실측 — 인용이 아니라 열어서 쟀다 (11건)

| # | 무엇을 | 어디를 | 결과 |
|---|---|---|---|
| 1 | B3 재호출 조건이 완주 배치를 집나 | `:626~629` | ❌ **못 집는다** — 「전원 게임일·방학의 «정상 0-job 완주»는 `finished_at` 이 있어 재호출이 안 난다」가 **명시**다. `적재실패` 가 있는 완주도 같은 얼굴 |
| 2 | 마감 스윕이 `적재실패` 를 집나 | `:1776` | ❌ 「`적재실패` 는 여기서 안 집는다 — `event_draft` 가 없어 조립할 재료가 없다」 |
| 3 | `load_retry_count` 의 소비자 | 전문 검색 | **0** — v5.7 A13 이 세웠고(`:881`) 읽는 자리가 어디에도 없다(갈래 1의 처방이 첫 소비자다) |
| 4 | 구제의 실행판 출처 | `:1795~1796`·`:1819` | 「그날 시작 행이 아예 0이면 **가장 최근 시작 행**」 — 남의 날짜 차용이 계약이다 |
| 5 | `deliver` 의 호출자 | `:449~450` | **둘뿐**(`tasks` Edge Fn · cron) — 앱은 `deliver` 를 직접 안 부른다 ⇒ 갈래 3 필수화가 구앱을 안 깬다 |
| 6 | §3 ②' 조인 ↔ §13 🚫 | `:325~326` ↔ `:4210~4211` | **정면 충돌** — 같은 판이 금지한 것을 같은 판이 집행했다 |
| 7 | 생성분 판별의 정본 | `:3590`·`:242`·`:3484` | `출처='생성'`+`degraded=false`+`generation_outcome='성공'` — 이미 있고 `출처` 는 이미 조인하는 행의 칸이다 |
| 8 | skip 2칸 CHECK | `:1266~1267`·`:1275~1308` 전량 | **부호·상한 0** — 반례를 여섯 제약에 전부 넣어 재현했다(§2 갈래 9) |
| 9 | 적재 의미 검사 열거 | `:1443~1459` | ①~⑥ 전부 `branch_snapshot` · `event_draft` 별항 — **`skill_ids` 항 0** |
| 10 | ㉤ 착지 대조 ①~⑧ 중 `evidence_refs` | `:1679~1710` | ⑦의 **`as_of` 하나**뿐 — 학생·이중 cutoff 결속 0 |
| 11 | A16 ↔ 「열린 채가 증거다」 | `:1605~1609` ↔ `:1621~1623` | **정면 충돌** — 재사용 대상이 「받았는지 모른다」의 그 행이다 |

🔑 **집행 중 추가로 하나 더 났다**(심문자가 못 본 것 · §7 조정 6): DDL 주석 `:855` 가 아직 「㉨ 구제만 `_target` 값을 쓴다」인데,
v5.7 A6 이 「`_target` 에 그 키가 **없어서** 함수가 `now()` 로 스스로 굳힌다」로 바꿨다(`:1805~1808`) — **같은 판이 형제 절을 안 걷은** 그 형태다.

## §4. 판정 분포 — 그리고 이 판의 축

| 판정 | 수 | 비고 |
|---|---|---|
| ✅ 채택 | **6** | 1·4·5·9·10·11 |
| ◐ 부분 | **7** | 2·3·6·7·8·12·13 — 전제 일부를 기각하고 알맹이만 |
| ❌ 기각 | **0** | — |
| ⧉ 범위밖 | **0** | — |
| **계** | **13갈래 / 원건 13** | 빠짐·중복·잉여 **0** |

🔑 **이 판의 축은 하나다 — 「고친 자리에서 다시 났다」.** 13건 중 **8건이 v5.7 이 이번 판에 새로 지은 자리**다:
- **A2 가 두 번 샜다** — 명단을 «기록»만 하고 «대조»를 안 했고(5), 새로 연 두 칸이 자기 판의 음수 규율을 안 받았다(9).
- **A16 이 자기 문서의 다른 줄과 정면**이다(11) — 「열린 채 = 받았는지 모른다」를 그대로 두고 그 행을 재사용한다.
- **C3 가 §13 의 🚫 를 밟았다**(4) — 금지 목록이 «있는데도» 밟혔다. 목록이 있는 것과 집행자가 그 목록을 지나는 것은 다른 일이다.
- **A13 이 칸만 세우고 소비자를 안 붙였다**(1·실측 3) — 「부활 횟수」를 세는 칸이 있는데 그 수를 읽는 자리가 0이라, 복구 자체가 안 돈다.

⚠ **v10 이 세 축(①빈 정본 칸 ②감시가 자기 대상을 못 봄 ③형제 절 미수거)을 적고 닫았는데, 이 판에서 ①은 한 자리(10), ③은 두 자리(4·§7 조정 6)에서 재발했다.**
축을 적는 것으로는 안 닫힌다 — **집행 «절차»에 그 축의 검사가 들어가야 한다**(§6 에 증보로 등재).

## §5. 집행 지도 — 이 순서로 연다 (②집행 = 설계 v5.8)

**ⓐ 자기모순 먼저** (금지 목록·형제 절을 밟은 자리 — 새 면적 0)
1. **§3 ②' `payload.ver` → `출처='생성'`**(4) · §13 🚫 를 「조인에도 적용」으로 넓힘
2. **§3-5-b ㉢ `attempt_open` 재사용 → `중복열림` 거절**(11) · 워커 계약 = `판불일치` 와 같은 처분
3. **DDL 주석 `:855` 수거**(§7 조정 6 — `snapshot_as_of` 구제 갈래)

**ⓑ 물리 결속** (검사·CHECK 추가 · 새 표 0)
4. **㉠ 명단 집합 대조**(5) — `_skipped_game` id 목록화 + 해시 대조
5. **㉠ 적재 의미 검사 ⑦ `skill_ids` 존재**(10)
6. **㉤ 착지 대조 ⑨ 근거 사건 결속**(6)
7. **skip 2칸 부호·상한 CHECK**(9)

**ⓒ 복구·경로**
8. **B3 재호출 셋째 갈래 = `적재실패` + 마감 전 + 재시도 상한**(1)
9. **㉨ 실행판 차용 폐지 — 구제도 ⓪로 시작 행을 만든다**(2) · §7 조정 5 철회
10. **`맥락` 필수화**(3) — 누락도 400 · cron 에 `&맥락=배치` 명시

**ⓓ 게이트·표시**
11. **§16-1 선행 표에 「첫 생성 행 전 필수」 칸**(7) + **#6 §8-B 결과 존재**(8)
12. **§8-B 차단이 사는 층 명문 + 원격 미검증 정직화**(8)
13. **§11-2 구앱 노출 창 명문 + 활성 시작일 정의 1줄**(12) · **§12-29 「배선만 잰다」 1줄**(13)

**ⓔ 마지막** — §13 에 v5.8 증보(§6) · 머리말·§16-1 게이트 갱신

## §6. 🚫 설계 §13 v5.8 증보 — 다음 회차가 다시 집지 않게 (집행 시 전건 등재)

- 🚫 **`payload.ver` 를 생산자 «조인 조건»으로 쓰기** — 갈래 4. 기존 🚫 는 「검증기 규칙」만 겨눴고 조인에서 재발했다. 생산자 판별 정본 = §11-3 삼항.
- 🚫 **`evidence_refs.axes_used` ↔ 요약 문자열 일치를 DB 가 검사하기** — 갈래 6 반쪽. SQL 이 자연어를 파싱하게 되고 §6-2(「목록의 정본은 코드」)와 정면이다. 그 대조는 검문(㉭)·§12 회귀 몫.
- 🚫 **구앱 호환 규칙(「`assignment_status` 를 모르면 `data` 만 본다」)을 걷기** — 갈래 12. 걷으면 구앱이 학생 화면에서 깨진다. 대신 활성 시작일이 순서를 진다.
- 🚫 **`맥락` 누락 기본값을 `구제` 로 두기** — 갈래 3. §3-1 근거 그대로(배선을 잊은 날 배치 전원이 조용히 폴백). 필수화가 답이지 기본값 뒤집기가 아니다.
- 🚫 **`적재실패` 를 마감 스윕·`jobs_claim` 이 집게 하기** — 갈래 1 처방과 혼동 금지. `event_draft` 가 없어 조립 재료가 없다(`:1776`) — 처방은 **재적재 방아쇠**이지 종료 상태를 큐로 되돌리는 것이 아니다.
- 🚫 **구제의 `snapshot_as_of` 를 배치처럼 «한 벌»로 묶기** — 갈래 2 기각분. 구제는 각자 다른 시각에 오고 A6 가 이미 판정했다.
- 🚫 **`attempt_open` 재사용을 되살리기** — 갈래 11. 「열린 채 = 받았는지 모른다」와 양립 불가. 재시도는 새 fence 로 온다.

## §7. 집행자 조정 자리 (②집행 때 채운다 — 다음 회차 재심 대상)

> v10 §7 과 같은 규약: 집행 중 처분표의 처방을 바꿨으면 **여기에 전건 적는다**(안 적으면 다음 회차가 「처분표대로 안 했다」로 다시 집는다).

| # | 갈래 | 조정 | 근거 |
|---|---|---|---|
| 1 | 1 | 재시도 상한 값 = **3**(`load_retry_count < 3`) · 방아쇠는 **워커 1항**(새 잡 0) | 상한 없이 열면 조립이 원리적으로 깨진 학생에서 재실행이 무한이다 · 3 = 마감까지 남은 워커 회차(10분 × 36) 대비 충분히 작고 일시 장애 1~2회를 덮는다 |
| 2 | 2 | 구제가 ⓪를 부르는 자리 = **`deliver`(Edge Fn)** 이고 `jobs_load_one` 시그니처에 **`_run_id uuid` 를 연다** | 판독기는 Edge Fn 층 산출이라는 D3 규정 그대로 — SQL 안에서 ⓪를 부르면 D3 이 걷은 그 「DB 가 스스로 읽는다」로 되돌아간다 |
| 3 | 5 | `_skipped_game int` → **`_skipped_game uuid[]`** · `skipped_existing` 은 인자가 아니라 ㉠ 내부 산출이므로 **그 id 목록도 함수가 자기 손에 쥔다**(인자 추가 0) | 개수만으로는 집합 대조가 원리상 불가 · `existing` 은 유일키 충돌로 함수가 «누구»를 이미 안다 |
| 4 | 10 | 「그 분류판에 속하는가」는 **적지 않는다** — 존재 대조만 · 판 결속은 **`engine.skills` 실물 확인 뒤 별항** | v10 §7 조정 10 의 규약(「안 연 열 이름을 지어 적으면 같은 형이다」) 그대로 |
| 5 | 8 | 「차단이 사는 층」 = **회귀 파일**로 적되 **파일 이름은 talk 저장소 실물 확인 뒤** — 이 문서는 이름을 «약속»하지 않는다 | §12-19 가 「이름 없는 칸은 대조를 못 한다」(B4)의 반대 실수를 하지 않게 — 지어 적으면 그것이 다음 회차의 빈 정본 칸이다 |
| 6 | (신규) | DDL 주석 `:855` 「㉨ 구제만 `_target` 값을 쓴다」를 **v5.7 A6 대로 수거** — 심문자가 못 본 미수거 | 갈래 2 집행이 그 줄을 지나므로 같은 항에서 함께 걷는다(축 ③ 재발 방지) |
| 7 | 12 | 활성 시작일 정의 = 「**c12 신앱이 스토어에 나간 뒤**」 — «구앱 비율 0» 이 아니라 «신앱 도달 가능» | 구앱 비율 0 은 학생 0명인 지금 못 재는 값이다(못 재는 것을 게이트에 넣으면 게이트가 안 쓰인다 · 철학 ④ 주의) |

## §8. 도장

- **①판정** = ✅ **종결** — 원건 **13/13** 매핑(빠짐·중복·잉여 0) · **13갈래** ✅6·◐7·❌0·⧉0 · 실측 **11건 + 집행 중 추가 1**.
- **②집행** = ✅ **종결 13/13 — 설계 v5.8**(§5 지도 13단계 · 조정 7건 = §7).
- **런 도장** — 대상지문 `6df13955f843` ✓ 처분 완료 · **편성 luna/max 1벌 첫 판** · 🚫 같은 판에 재던짐(이중 소각).
- ⛔ **구현 착수 차단 유지** — 유호님 확정 규칙상 새 회차 지적은 다시 전건 처분을 지난다. ▶ 다음 = **12회차 심문을 v5.8 에**.
- ⏳ **유호님 자리 2**(그대로 · 이 표가 새로 만든 것 없음) — ①**G3 면적 자르기**(§14) ②**E2 급수→기술 매핑을 이 트랙이 지을 것인가**.
