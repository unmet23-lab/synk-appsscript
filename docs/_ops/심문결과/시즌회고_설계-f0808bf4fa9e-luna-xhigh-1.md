동결 불가. 지적 20건.

[P0] 시즌 식별자가 교재 정본과 연결되지 않는다 — §9-0·§8은 시즌을 교재 1권이라고 하면서 `season_id`를 날짜 범위 행이라고만 정의한다. `textbook_id`·교재 버전·학습자 수강 연결·경계 확정 및 변경 규칙이 없어 서로 다른 교재를 같은 시즌으로 묶거나, 교재 지연 후 이미 기록한 구간을 바꿀 수 있다. 첫 `season_compass`와 회고 기준선은 소급 복원할 수 없다.

[P0] 입학일과 시즌 시작의 관계가 비어 있다 — §8의 시한은 입학일이고 §9-2 ㉤의 입력 시점은 시즌 시작인데, 학생이 교재 중간에 입학하는 경우의 규칙이 없다. 입학 행에 3문항을 기록해도 실제 시즌 앞 절반은 비어 있는 상태로 회고된다. 첫 시즌의 기준선이 학생마다 다른 길이로 만들어진다.

[P0] 회고 라벨의 과녁이 회고 행에 고정되지 않는다 — §3-2의 `record_snapshot`은 오른쪽 실기록만 담고, `season_goal`은 별도 `season_compass` 행에 남긴다. 과녁의 복사본·content hash·불변 버전·강제 FK가 없다. 나침반 행을 정정하거나 덮어쓰면 동일한 `verdict`가 다른 목표를 향한 판정으로 조용히 바뀐다.

[P0] 두 표로 분리했지만 회고 표 자체는 덮어쓰기 구조다 — §3-2는 학생과 강사가 같은 `season_review` 한 행을 순차적으로 수정하게 하고 `decided_by`·`decided_at`도 단일 값이다. 판정 수정·재검토·재제출을 append-only 사건으로 남기는 규격이 없다. 강사의 후속 수정이 최초 라벨을 지우면 엔진이 실제로 학습한 판정도 알 수 없다.

[P0] 학생 선행 순서가 화면 설명에만 있다 — §6은 학생 판정 후 강사 판정을 요구하지만 API·DB 차원의 상태 전이, CAS/version 검사, 선행 판정 시각 검증이 없다. 두 탭, 직접 API 호출, 오프라인 재시도로 강사 판정이 먼저 저장되거나 학생 값이 나중에 `null`로 덮일 수 있다. `verdict_by_self`를 대조군으로 쓰겠다는 전제가 무너진다.

[P0] 멱등성·동시성·부분 실패 계약이 없다 — §6·§8은 한 세션과 자동 저장만 말할 뿐, 클라이언트 멱등키·재시도 규칙·초안/확정 상태·충돌 처리·트랜잭션 경계를 정의하지 않는다. 저장 성공 후 화면 종료, 재시도 중복, snapshot 저장 후 라벨 저장 실패가 모두 가능하다. 유일키는 중복 요청을 막는 것이 아니라 정상 데이터를 서로 덮어쓰게 만들 뿐이다.

[P0] 빈 기록과 유효한 `same`이 같은 모양이다 — §6은 학생의 `null`만 설명하고, `n=0`, 축 계산 실패, 나침반 미작성, 증거 부족을 위한 회고 상태가 없다. 실기록이 하나도 없어도 강사는 세 갈래 중 하나를 눌러야 하므로 실패한 수집이 `same`으로 저장될 수 있다. 엔진과 운영자는 미적용과 유효한 라벨을 구분하지 못한다.

[P0] 엔진 도달을 의도적으로 뒤로 미룬다 — §8 ⑤는 라벨→엔진 배선을 여러 행이 쌓인 뒤로 미룬다. 이는 판단 정본 ④의 `닿았나`와 `늘었나`, 그리고 §7의 “유일한 부호 생산자” 주장과 정면 충돌한다. 첫 시즌 행을 저장할 때 outbox·ack·재전송·replay가 없으므로, 나중에 배선을 붙여도 누락된 라벨을 확실히 회수할 수 없다.

[P0] 엔진 학습 승격 사건이 생략됐다 — §0·§7·§8은 `season_review` 행을 곧바로 엔진 학습 입력처럼 취급한다. 그러나 인계 정본의 훈련 승격은 원본 event_id, 불변 artifact_ref, content hash, serializer/dataset 버전, 사람 승인, 평가 참조를 별도 사건으로 남기게 되어 있다. 이 문서에는 해당 사건과 엔진 수신 주체가 없어 어떤 행이 어떤 학습셋에 들어갔는지 재현할 수 없다.

[P0] `record_snapshot`은 구현 가능한 계약이 아니다 — §3-2·§4-1은 `axes_전반`·`axes_후반`·`season_totals`라는 껍데기만 제시한다. 중첩 필드, 단위, null 의미, 근거 ref, 계산 함수 버전, 모델·prompt·schema 버전, 늦게 들어온 사건의 처리 규칙이 없다. `추정판` 문자열 하나로는 동일한 회고를 다시 계산할 수 없다.

[P0] 전·후반 구간 계산이 비결정적이다 — §4-2는 변수 길이인 교재 시즌을 `as_of`와 정수 `창`으로 나누면서 정확한 `[from,to)` 규칙, 중간일 반올림, 끝점 포함 여부를 정하지 않았다. 59일·61일 시즌에서는 하루가 겹치거나 빠질 수 있고, 함수가 시즌 바깥 사건까지 포함할 수 있다. 전·후반 대조가 구현자마다 달라진다.

[P0] 하나의 enum에 서로 다른 판단 축을 접었다 — §5의 `closer/same/redirected`는 시즌 목표에 대한 진전과 5년 방향 변경을 동시에 표현한다. 방향을 바꾸면서 가까워질 수도 있고, 가까워지지 않았지만 방향만 바꿀 수도 있는데 우선순위 규칙이 없다. §3-1의 `self_in_5y_changed=true`를 `redirected`의 직접 근거로 삼으면 실제 진전 정보가 강제로 소실된다.

[P1] `season_goal`은 과녁이 아니라 해석 대상이다 — §9-2 ㉤은 한 줄 자유문만 남기고 성공 기준·목표 유형·연결 역량을 금지한다. 그러면 `closer`가 제출 수 때문인지 발음 때문인지 목표 변화 때문인지 필드로 식별할 수 없다. 엔진은 학생별 자유문과 교사의 주관적 라벨만 받아 비교 가능한 학습 신호를 만들기 어렵다.

[P1] `self_in_5y_changed`는 변화의 증거가 아니다 — §3-1은 버튼 상태 `true`를 곧바로 `redirected`의 근거로 취급하지만 이전 답변의 ref/hash와 새 답변의 필수 조건이 없다. 학생이 [바꿀래]를 누르고 같은 문장이나 빈 문장을 제출해도 방향 전환으로 저장될 수 있다. 버튼 의도와 실제 답변 변화가 구분되지 않는다.

[P1] 답변의 작성자와 기록자가 섞인다 — §3-1의 `recorded_by`는 행 전체에 한 명만 둔다. 강사가 학생 답변을 촉진·번역·대필하는 정상 경로라면 문항별 작성자, 기록 시각, 세션, 수정 버전이 필요하지만 `answers`에는 없다. 나중에 엔진은 학생의 원문인지 강사의 paraphrase인지 판별할 수 없다.

[P1] 목적 enum이 문서 안에서 갈린다 — §3-1은 유학·취업·K컬처를 말하고 §9-2 ㉦은 `study`·`work`·`culture`를 사용한다. canonical enum과 매핑 버전이 없어 목적별 `topik_use` 문구를 잘못 보여주거나 빈 응답을 만들 수 있다. 시즌 간 동일 필드 비교도 불가능해진다.

[P1] 장학 심사 연결이 학생 비교 금지와 충돌한다 — §9-1-b는 `season_review`를 여러 학생 중 전액 프리패스 1명을 고르는 심사표로 사용한다. 판단 정본 ㉢은 등수·평균·다른 학생보다를 어느 화면과 문서에도 두지 말라고 했으므로, 한 명을 선정하는 순간 내부 비교가 발생한다. 동점, 회고 누락, 불완전 라벨의 처리 규칙도 없다.

[P1] “2시즌=4개월” 계산이 시즌 정의와 충돌한다 — §9-0은 교재 종료가 정본이고 달력 2달은 근사값이라고 한다. 그런데 §9-1-b는 이를 고정 4개월·연 3회·2027년 6월 지급·132만₮ 예산으로 계산한다. 교재 지연이나 시즌 길이 변동이 허용되면 지급일과 연간 횟수는 더 이상 이 표에서 도출되지 않는다.

[P1] 회고 기준표가 교사마다 달라진다 — §5·§6은 세 코드와 한 줄 사유만 두고 `closer/same/redirected`를 어떤 증거 조합으로 선택하는지 규정하지 않는다. `n=0.8`과 목표 미달성, 제출 증가와 시험 하락처럼 충돌하는 근거의 판정 규칙도 없다. `decided_by=teacher`는 기준 부재를 사람에게 떠넘기는 필드일 뿐이다.

[P1] 학생 접점을 학생 접점으로 취급하지 않는다 — §0은 회고를 학생에게 보여주는 화면이 아니라고 하지만 §6에서는 학생이 직접 문장을 쓰고 판정 버튼을 누른다. 따라서 ① 기본 게이트와 ⑤ 자기설명 게이트 대상인데, §9-2 ㉤에는 왜 다시 열어야 하는지, 무엇이 다음 학습에 반영되는지, 즉시 어떤 보상을 받는지가 없다. 한 줄과 클릭은 편의 설명이지 재미·실사용 한국어 도움·신선함의 근거가 아니다.

[P1] 첫 화면 자기설명이 정의되지 않았다 — §6은 학생이 무엇을 먼저 보고 어떤 순서로 누르는지만 적고, 기능 전체가 무엇인지와 네 재료가 어떻게 이어지는지를 설명하지 않는다. 특히 학생 판정이 강사 판정과 어떻게 다른 데이터인지 화면에서 설명되지 않으면 학생의 클릭은 의미 없는 설문이 된다. 이는 판단 정본 ⑤가 명시한 실패 사례와 같은 유형이다.

이 문서보다 나은 대안 구조가 있으면 1개: `불변 사건 → 불변 회고 표본 → 파생 projection/outbox` 3층 구조다. 시즌은 교재 ID·버전·학습자 연결·고정 경계를 가진 별도 엔터티로 만들고, 나침반·학생 판정·강사 판정을 각각 멱등키가 있는 append-only 사건으로 남긴다. 회고 표본에는 목표 답변의 복사본/hash와 전·후반 feature snapshot, 계산 버전을 함께 고정하며, `progress_verdict`와 `direction_changed`는 별도 필드로 둔다. 엔진과 장학은 이 불변 표본을 직접 수정하지 않고 승인·전달 상태가 있는 projection/outbox를 읽게 한다.