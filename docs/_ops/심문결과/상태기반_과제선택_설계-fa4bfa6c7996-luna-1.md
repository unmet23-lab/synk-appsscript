공격 결과: P0 9건, P1 5건, P2 1건. 동결 불가다.

- [P0] c12가 실재하지 않는다 — §11-2는 현행 계약이 c11이고 c12는 “개정 요청”이라고 인정한다. 그런데 §3·§12는 이미 c12 필드와 검증을 전제로 쓴다. 계약 JSON·검증기·마이그레이션이 없으므로 첫 append-only 행의 허용 규격이 정본으로 존재하지 않는다.

- [P0] `batch_run_id` 전량 기입은 물리 계약이 아니다 — §3-5-b DDL에서 `batch_run_id uuid`는 `NOT NULL`이 아니며, 복합 FK도 NULL을 허용한다. “구제 포함 전량 채움”은 주석일 뿐이다. 실행 계보 없는 job이 정상 저장될 수 있어 0-job·재실행 진단이 소급 불가능해진다.

- [P0] 실행판이 두 입력으로 갈라진다 — `batch_run_start`가 실행판을 저장하지만 `jobs_load`는 같은 판을 `_batch`로 다시 받고, `jobs_load_one`은 `calendar_game_day`를 별도 인자로 받는다(§3-5-b). 저장된 run의 값과 호출자가 넘긴 값이 다를 때 이를 거절하거나 run에서 읽는 규칙이 없다. 한 job에 “run 판”과 “실제 적용 판”이 동시에 생긴다.

- [P0] `policy_ver` 생성 규격이 없다 — §5-1은 13개 이질적 재료를 기존 판지문 직렬화로 해시하라고 하지만, 실제로 인용한 직렬화 코드는 게임 판정 3값용이다. 파일 해시·cron·`pg_get_functiondef`를 어떤 순서와 바이트 규격으로 합치는지 정본이 없다. §16-1 #5는 첫 생성 전 필수라고 했으므로 구현자가 임의 직렬화를 짓는 순간 첫 계보가 갈린다.

- [P0] 평가 `input_hash`의 대상이 서로 다르다 — §8-B 실행층은 렌더된 요청 본문 전체를 해시한다고 적지만, §8-B E2와 §12-19는 `generation_input_text`를 “요약 문자열”로 해시한다고 적는다. 그런데 §11-2는 `generation_input_text`를 모델에 실제로 간 요청 본문 전체로 정의한다. 프롬프트 포장·옵션이 바뀌어도 낡은 평가 결과가 살아남을 수 있다.

- [P0] 근거 원래 개수가 저장되지 않는다 — §6-1은 원래 사건 수가 `학습자상태.js`의 `잘림`에 있다고 하지만 그 상태 객체는 메모리 산출물이고 이벤트에 저장되지 않는다. 저장되는 것은 최대 50개의 `events`와 `truncated` 불리언뿐이다. 50/50과 50/200을 나중에 구분할 수 없으므로 학습자 모델 입력의 완전성 계보가 끊긴다.

- [P0] `degraded` 생산자가 세 갈래로 갈린다 — §3-5-b의 봉투 표는 `degraded`를 호출자 값으로 두지만, v5.7 D1은 draft에서 제외하고 착지 모드가 함수 몫으로 정한다고 한다. §5-4는 다시 호출자 값을 대조한다고 쓴다. `jobs_finalize`에는 별도 `degraded` 인자가 없고 이 충돌을 해소하는 단일 산출 규칙도 없다. 첫 행부터 정상 생성과 폴백의 의미가 뒤집힐 수 있다.

- [P0] 기존 부분 착지는 탐지만 하고 복구하지 않는다 — §3-5-b는 `task.assigned`만 있고 개입·submission이 없으면 `partial`로 세고 “고치지는 않는다”고 못박는다. 이후 재실행은 기존 배정으로 건너뛰므로 job도 닻도 새로 만들지 않는다. 감시 행만 남고 학생의 실제 개입·성과 사슬은 영구 결손이다.

- [P0] 롤백 금지가 상위 물리 계약과 충돌한다 — §12-21·§12-27은 빈 DB 재적용을 롤백의 갈음으로 삼고 역마이그레이션을 만들지 않는다. 그러나 L0·개발 분업 기준은 중간 실패와 롤백 경로 기록·검증을 요구한다. 빈 DB 재적용은 이미 일부 적용된 개발 DB나 기존 데이터가 있는 DB를 되돌리지 못한다.

- [P1] 오케스트레이터의 150초 상한을 측정하지 않았다 — §3은 벤더 호출이 0이므로 학급 17명이 한 호출에 들어간다고 결론낸다. 그러나 학생별 상태 조회·이력 조회·기술 선택·draft 조립·DB 적재는 여전히 외부 대기와 CPU를 발생시킨다. §12-20은 워커만 측정하며 `deliver` 자체의 시간 예산과 실패 복구 눈금이 없다.

- [P1] 새 fence가 벤더 중복 호출을 다시 허용한다 — `attempt_open` 중복 방지는 같은 fence에서만 작동한다(§3-5-b). 응답 후 `attempt_close` 전에 워커가 죽으면 reclaim 뒤 새 claim이 fence를 올리고, 새 fence에서는 같은 요청을 다시 호출한다. 응답 유실이 반복되면 한 학생에게 여러 벤더 호출과 여러 산출이 생긴다.

- [P1] `중복열림` 처분이 RPC 계약표에서 사라졌다 — 본문은 `reject_reason`에 `중복열림`을 추가하고 `내부오류` 폴백을 정의하지만, §3-5-b의 최종 RPC 반환표에는 세 기존 사유만 적혀 있다. 구현자는 이를 예외·중단·폴백 중 임의로 처리할 수 있다. 최신 중복 과금 방지 규칙이 계약표에서 검사 불가능하다.

- [P1] 학생의 재미는 실제 학생 경험으로 검증되지 않는다 — §8-A의 재미 런타임 칸은 비어 있고 §8-B는 가상 40사례의 사람 채점만 한다. 첫 학생 뒤에도 “왜 한 번 더 열었는가”를 첫 시즌 회고에서 다시 연다고만 했지, 생성 기능의 재방문·완료·자발 발화가 어떤 상태로 기록되는지 없다. 학생 접점 기능이 수집·평가 통과만으로 재미있는 기능의 지위를 얻는다.

- [P1] 목표 활용과 목표 도움을 혼동한다 — §8-B의 ⑦은 출력이 `goal` 맥락을 사용했는지만 본다. 그것이 실제 목표에 도움이 되는지, 실제 한국어 산출을 만들었는지는 §12-3·§16-1에서 첫 학생 뒤로 미뤄진다. 목표를 자연스럽게 언급한 무용한 과제도 사전 게이트를 통과할 수 있다.

- [P2] 관측 전 물리 면적이 과도하다 — §6-0은 `skill_ids` 소비자가 0이고 ④완결도 `✗✗✗`이며 생성 코드도 0줄이라고 명시한다. 그 상태에서 10개 RPC, 다수 상태 전이, 13재료 판지문, 29개 인수 조건을 동결한다. 실제 학생 실패보다 문서 자체의 미관측 분기와 유지 비용을 먼저 고정하는 구조다.

대안 구조: 없음. 위 P0를 처분하지 않은 상태에서는 구조 대안보다 정본·실행·소급성 결함을 먼저 닫아야 한다.