판정: `failed_p0` — P0 22건, P1 10건, P2 1건. 동결 불가다. 현재 공용 계약 c10과 후보 문서 전문을 대조했다.

[P0] 닫힌 엔드포인트 범위 위반 — 닫힌 결정은 엔드포인트 2개인데 §4는 쓰기 2개 외에 조회 3개와 인증 3개를 추가해 실제로 8개를 정의한다. §4 첫 문장의 “둘”과 §4-3~4-6이 서로 모순되므로 동결 대상 범위부터 확정되지 않았다.

[P0] 폐기된 인증 통로 부활 — §2·§4-4~4-6은 `first-login` 계정 생성, 원장 초기화, 임시번호 로그인을 라이브로 선언한다. 이는 F269의 두 번째 계정 생성 통로 제거, 재발급 E1 대기, 직원 인증 선행 설계 대기라는 닫힌 결정과 정면충돌한다.

[P0] C0 요청 필드가 필드 정본에 없다 — §3·§4-1의 `session_id`, `content_id`, `task_schema_ver`는 현재 `수집_교정_계약.json` c10 필드 목록에 없다. 앱이 보내면 미등록 필드로 거절되거나 조용히 버려지고, 보내지 않으면 세션·콘텐츠 대상·문항 규격판을 소급 복구할 수 없다.

[P0] 발화 길이 정본과 충돌 — §4-2는 길이를 `capture_meta.server.duration_ms`에만 넣는다. 닫힌 정본은 봉투 밖 `submissions.audio_duration_sec`이므로 현재 소비자가 읽는 열은 계속 비고, 두 단위가 독립 정본으로 갈라진다.

[P0] 원본 음성 규격이 실행 불가능한 채 동결된다 — §4-2는 AGC·노이즈 억제 off를 정본으로 선언하면서 실제 라이브러리에는 손잡이가 없고 매 행이 `unknown`이라고 인정한다. 실기기 측정도 0이므로 첫 수집부터 처리된 음성을 원신호로 오인할 수 있고, 지워진 발음 정보는 복원되지 않는다.

[P0] 학생이 서버·강사 사건을 위조할 수 있다 — §4-1은 학생 토큰이 보내는 `event_type`에 생산자 역할별 허용표를 두지 않는다. 따라서 `task.assigned`, `exam.result`, `intervention.delivered`, `data_use.granted`, `data_use.revoked`까지 학생 공개 통로에서 주장할 수 있어 학습·평가·승격 계보가 오염된다.

[P0] 훈련 승격 사건이 이름뿐이다 — §4-1의 허용 payload에는 `artifact_ref`, `content_hash`, `serializer_ver`, `redaction_ver`, 목적, 정확한 `consent_id`와 검사 시각, 승인자·승인 시각, `dataset_ver`가 없다. `data_use.granted`가 저장돼도 무엇을 어떤 동의와 판본으로 승격했는지 증명하거나 철회할 수 없다.

[P0] 철회 사건과 동의 게이트가 서로 막는다 — §4-1은 모든 사건에 현재 유효한 동의를 요구하므로 별도 예외·순서가 없으면 철회 뒤 `data_use.revoked`를 기록할 수 없다. §5는 사후 동의가 과거 사건을 살리지 못한다고 했는데 §7은 막힌 큐를 보존해 재동의 뒤 보내려 하므로 둘 중 하나는 반드시 거짓이다.

[P0] 서명 URL이 철회 뒤 수집을 다시 연다 — §4-2는 동의를 서명 발급 시점에만 검사한다. 서명 발급 후 철회·폴더 삭제가 일어나도 만료 전 PUT이 뒤늦게 성공하면 이벤트 없는 음성이 삭제 뒤 다시 생기며, 업로드와 이벤트 사이의 장애도 추적 불가능한 고아 파일을 만든다.

[P0] 오프라인 큐가 학생에게 귀속되지 않는다 — §2·§4-1은 요청 본문에 학생 식별자를 금지하고 전송 시점 토큰으로 소유자를 결정하지만, 로컬 큐를 생성 당시 계정에 묶는 규약은 없다. 로그아웃·계정 전환·세션 갱신 경쟁이 생기면 이전 학생의 텍스트 사건이 새 학생 토큰으로 정상 저장된다.

[P0] 멱등 충돌이 데이터 유실을 성공으로 위장한다 — §4-1은 같은 키면 내용 비교 없이 `duplicate`와 기존 `event_id`를 반환한다. 같은 키에 다른 본문·스냅숏이 실리거나 결정론적 일일 키로 과제가 수정되면 새 사건이 조용히 삼켜지며 앱은 큐에서 삭제한다.

[P0] 사건과 하위 행의 원자성이 정의되지 않았다 — `learning_events`와 `submissions`를 한 건으로 저장하는 트랜잭션 경계가 §4-1에 없다. 사건만 저장되고 제출 행이 실패하면 재시도는 `duplicate`로 성공 처리되어 영구적인 반쪽 행이 남는다.

[P0] 사건 참조가 모호하고 타학생 소유권 검사가 없다 — §4-1은 `retry_of_event_id`·`parent_event_id`에 진짜 `event_id` 또는 `idempotency_key`를 같은 문자열 칸으로 받는데, 앱 키도 UUID라 어느 이름공간인지 구분할 수 없다. 원시 `event_id` 경로에는 대상이 같은 학생인지 확인한다는 규약도 없어 타학생 사건 연결과 `intervention_id` 오계승이 가능하다.

[P0] 정답이 제출 전에 학생 기기로 유출된다 — §4-1의 `task_snapshot`에는 `정답`이 있고 §4-3 `/tasks`는 그 스냅숏을 그대로 앱에 돌려준다. 학생이 네트워크·로컬 저장소를 보면 정답을 알 수 있어 이후 채점·재시도 데이터가 학습 증거로서 무효화된다.

[P0] 제출이 정확한 배정과 결합되지 않는다 — `/tasks`는 정확한 `task.assigned` 사건인 `task_id`를 주지만 제출 사건이 이를 `parent_event_id`로 되돌려 보내도록 요구하지 않는다. 대신 변조 가능한 클라이언트 `task_snapshot`과 재사용 가능한 `task_ref`만 믿으므로 실제 배정·표시 내용과 제출을 증명할 수 없다.

[P0] 고정 도입 과제는 제출할 수 없다 — §4-3은 배정이 없으면 앱의 고정 도입 과제로 내려가라고 하면서, 같은 절에서 서버가 준 `task_ref`가 없으면 발화를 보내지 말라고 한다. 빈 응답에는 `task_ref`가 없고 앱의 작명도 금지됐으므로 첫날 기준선 발화가 화면에서 끝나고 사건으로 남지 않는다.

[P0] 목적 스냅숏의 누락과 `null`을 구분하지 않는다 — §4-1 앞부분은 `goal_snapshot`을 앱 필수라고 선언하지만 §4-3은 공통 필수가 아니며 빠져도 무해하다고 뒤집는다. `level_snapshot`처럼 “키 존재 + null” 검사를 두지 않아 앱 결손이 “당시 목적을 몰랐다”로 위장되고 목적 변경 전 맥락이 영구 손실된다.

[P0] 개입 성과 귀속 의미가 미결인 채 행이 쌓인다 — §4-3은 교정 재발화의 `intervention_id`를 원 개입 A에서 계승하면서 오늘 실제 전달된 개입 B도 있다고 인정하고, 어느 쪽에 공을 돌릴지는 첫 소비자 때 다시 보겠다고 미룬다. 최상위 자산인 실제 전달→성과 연결의 의미를 동결 시점에 정하지 않으면 동일 필드가 두 인과 의미로 축적된다.

[P0] 기존 access token 폐기가 구현되지 않았다 — §4-5는 `revoked_before` 기록만으로 기존 세션을 막았다고 주장하지만 §2·§5의 일반 호출은 게이트웨이 서명·만료 검사만 사용하고 매 요청 `session_alive()` 검사를 요구하지 않는다. 비밀번호를 바꿔도 만료 전 access token은 계속 학생 데이터에 접근할 수 있다.

[P0] 인증의 외부 시스템 경계가 원자적이지 않다 — §4-4는 GoTrue 계정 생성과 learner 연결·가입문항 기록 사이의 실패 복구를 정의하지 않아 Auth에만 남은 고아 계정이 생길 수 있다. §4-6도 임시 해시 확인→GoTrue 비밀번호 변경→해시 삭제를 선점·잠금 없이 수행해 동시 두 요청이 모두 “1회용” 번호를 사용할 수 있다.

[P0] Storage 원본이 불변이라는 증거가 없다 — §4-2에는 객체 덮어쓰기 금지, 전체 바이트 해시, 객체 버전 또는 업로드 완료 지문이 없다. DB의 `audio_ref`와 첫 4KB 측정값은 그대로인 채 실제 음성 바이트가 바뀌어도 탐지할 수 없다.

[P0] 동결 시험이 문서 표면을 덮지 않는다 — §7의 왕복 6은 주로 `/events`, UI 동의 게이트, 토큰 재시도만 판정하며 조회 3개·인증 3개·업로드 철회 경쟁·실기기 WAV/AGC를 동결 조건으로 묶지 않는다. §8의 “여섯 칸 전부 초록”은 확장된 C0 전체의 통과 증거가 아니다.

[P1] 여러 판의 상태가 한 본문에 겹쳤다 — §0은 현행 c6, payload 절은 c9, 응답은 c10이며 §10은 `progress`가 남았다고 쓰지만 §4-3은 구현 완료라고 쓴다. “초안이라 구현 금지”라는 머리말 아래 라이브 구현 기록도 있어 현재 규범과 변경 이력을 구분할 수 없다.

[P1] 필수 규칙 정본이 둘이다 — §0은 필수 필드 정본이 JSON이라고 하지만 §4-1은 실제 필수 조합을 `lib/이벤트검증.js`가 정한다고 한다. payload 허용 목록도 문서에 다시 복사해 이미 여러 차례 뒤처졌으므로 Apps Script·n8n·새 소비자는 한 정본만 읽어 유효 요청을 만들 수 없다.

[P1] 401 오류 계약이 스스로 거짓이다 — §5는 앱이 `AUTH_EXPIRED` 코드와 `retryable`로 분기한다고 정하지만 §7은 만료 JWT가 함수 앞에서 잘려 그 봉투와 코드가 오지 않는다고 밝힌다. “message가 아니라 code로만 분기”와 “모든 401로 분기”가 동시에 정본이다.

[P1] 이미 등록된 계정의 응답이 두 갈래다 — §4-4 성공 표는 이미 계정이 있으면 `200 already:true`라고 하고, 바로 아래 게이트 실패 표는 “이미 등록됨”을 다른 실패와 함께 401로 묶는다. 구현자는 어느 응답도 계약 위반 없이 선택할 수 있어 계정 존재 노출과 앱 분기가 실행마다 달라진다.

[P1] anon 인증 함수 안의 원장 경로 검증이 명시되지 않았다 — 같은 `auth` 함수가 anon `first-login`·`temp-login`과 director 전용 `reset`을 함께 맡으면 함수 단위 JWT 게이트만으로 둘을 나눌 수 없다. §4-5는 claims를 `session_alive()`에 넣는다고만 하고 서명·발급자·만료를 먼저 검증하라는 계약이 없어 보호 경계가 구현자 추정에 맡겨졌다.

[P1] 진행 집계의 날짜 원천이 없다 — §3은 기기 `occurred_at`을 못 믿고 미래값도 허용하지만 §4-3 `progress`는 오늘·어제 및 “첫 사건 시각”에 `occurred_at`과 `ingested_at` 중 무엇을 쓰는지 정하지 않는다. 같은 행이 구현에 따라 서로 다른 날에 집계된다.

[P1] 타학생 `correction_id`가 영구 오류인데 5xx다 — §4-1은 교차 학생 연결을 트리거가 막지만 이를 서버 오류로 내보낸다고 명시한다. 고칠 수 없는 입력을 재시도 가능 5xx로 분류하면 백오프·쿼터 소모 뒤 격리될 뿐 원인을 정확히 보고하지 못한다.

[P1] 격리 큐가 복구 계약이 아니다 — §4-1은 rejected 항목을 “격리 보관 후 보고”한다고만 하고 저장 위치·계정 귀속·보존 기간·보고 성공 확인·수정 후 재처리 규칙을 정의하지 않는다. 앱 삭제나 기기 분실이 일어나면 격리가 곧 영구 삭제다.

[P1] `postgres` 직결은 영향 범위 0이라는 주장과 모순된다 — §1의 Edge Function은 RLS를 전부 우회하는 `postgres` 역할을 사용하므로 SQL 한 줄의 누락이 engine뿐 아니라 프로젝트 전체에 미친다. 연결 풀·동시 연결 상한·트랜잭션 정리 규약도 없어 Edge 인스턴스 증가가 DB 연결 고갈로 이어질 수 있다.

[P1] RATE_LIMITED는 이름만 있다 — §5에 429 코드가 있지만 `/events`, `/uploads/sign`, anon 인증 각각의 키·시간창·상한·전역 제한이 없다. 특히 25MB 무제한 보존 업로드와 공개 학생번호 인증은 버그나 반복 호출 하나로 비용·잠금 피해를 계속 키울 수 있다.

[P2] 이미지 업로드는 끝까지 이어지지 않는다 — §4-2 제목과 `kind`는 이미지를 허용하지만 응답은 `audio_ref`만 주고 이벤트 계약에는 `image_ref`가 없다. 이미지 기능은 업로드 뒤 어느 사건에도 연결되지 않는 선행 구축이다.

대안 구조 1개: C0 동결본을 닫힌 결정대로 `events`와 `uploads/sign` 두 통로만 남긴 “학생 데이터면”으로 다시 자르고, 조회·인증·운영 이력은 별도 비동결 계약으로 분리한다. 하나의 기계 계약에 이벤트별 wire 경로, producer 역할, 키 존재/null 규칙, 앱·서버 소유권, 동일학생 참조, 멱등 내용 충돌, 트랜잭션 경계를 넣고 앱·Edge·n8n 검증기와 문서 표를 여기서 생성해야 한다. 승격 사건은 최소 계보 필드가 서기 전까지 허용 값에서 막고, 오프라인 큐는 요청 본문과 별개인 로컬 계정 소유자에 묶으며, 업로드는 기존 멱등키와 전체 바이트 지문에 결합해야 한다.