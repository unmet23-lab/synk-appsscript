동결 불가다. 다음 결함은 구현 전에 닫히지 않으면 소급 복구가 어렵거나, 초록인데 실제로는 작동하지 않는 상태를 만든다.

[P0] 배달 순간에 가짜 `submission.created`를 만든다 — §3-0·§3-5-b·[학습자상태.js](C:/Users/q1212/Documents/SYNK-talk/lib/학습자상태.js)

`_event` 안의 `submission` 객체가 `event_type: "submission.created"`로 정의되어 있다. 하지만 `engine.submissions`는 학생이 본 과제 스냅샷이고, 실제 제출 사건은 학생 행동 뒤에 별도로 생겨야 한다. 이를 그대로 실행하면 모든 배정이 제출 완료로 집계되어 리듬·실력 이해 데이터가 영구 오염된다.

[P0] ⑥′ 비대상 착지가 정의된 RPC 가드와 충돌한다 — §3-4·§3-5-b·§3-6

문서는 `대기 → 대상아님`을 `jobs_finalize(..., _mode='정상')`로 처리하라고 한다. 그런데 정상 착지의 가드는 `status='claimed'`이고, `대기`를 받을 수 있는 것은 마감 모드뿐이다. 비대상 행을 만들 `_event`의 완전한 입력도 `_targets`에 없어서, 이 경로는 현재 규격만으로 실행할 수 없다.

[P0] 임대 만료 회수는 전이만 있고 실행 주체가 없다 — §3-5·§3-5-b·§12-15·§12-18

상태표에는 `claimed → 대기`가 있지만 다섯 RPC 어디에도 회수 함수가 없다. `jobs_claim`은 `대기`만 집도록 적혀 있으므로 워커가 죽으면 해당 일감은 06:00까지 다시 집히지 않는다. “재진입”의 핵심 경로가 문서상 존재하지 않는다.

[P0] 시도 결과와 학생에게 간 결과의 계보가 갈라진다 — §3-5-b·§3-6

`attempt_close`는 `result is null`만 검사하고 job의 상태·펜스·종료 여부를 검사하지 않는다. 마감 폴백 뒤에도 오래된 워커가 시도를 성공으로 닫을 수 있고, `winning_attempt_id`도 같은 job인지·성공 시도인지·같은 펜스인지 보장하지 않는다. 학생은 폴백을 받았는데 장부에는 성공 생성으로 남는 모순이 발생한다.

[P0] 닫힌 값목록이 물리적으로 닫혀 있지 않다 — §4-1·§3-5-b·§12-7·§12-21

`generation_jobs.outcome`에는 CHECK가 없고, `gate_failed_reasons` 배열의 원소도 허용목록 검사가 없다. §12-21의 “DDL과 값목록 대조”는 DDL에 값목록이 없으므로 아무것도 강제하지 못한다. 임의 문자열이 저장되어도 정상 행처럼 보이며, append-only 사건과 연결된 뒤에는 정규화할 수 없다.

[P0] `degraded`의 뜻이 문서 안에서 충돌한다 — §1·§4-1·§5-4·§3-5-b

§1은 첫날·교정문을 `degraded=false`로 두지만 §5-4는 생성 갈래가 아니면 `true`라고 정의한다. 초급·미정·구제경로·게임날도 각각 성공인지 범위 밖인지 규칙이 한 의미로 닫히지 않는다. 게다가 새 `_event` 스키마에는 `degraded` 자체가 없어 기존 기본값에 조용히 의존한다.

[P0] 입력 스냅샷은 선언만 있고 배선되지 않았다 — §6·§11-4·§3-5-b·§12-21

문서는 `occurred_at`과 `ingested_at`을 모두 `as_of`로 자르라고 하지만 `generation_jobs`에는 `snapshot_as_of`만 있다. 현재 `학습자상태()`는 `ingested_as_of`를 받지 않고, 생성 워커가 job의 시각을 상태 계산에 전달하는 계약도 없다. `branch_snapshot` 역시 버전·필수키·구조 검사가 없는 임의 `jsonb`라서 시각을 고쳐도 당시 판정을 재현할 수 없다.

[P0] `evidence_refs`의 정본 모양이 현재 생산자와 충돌한다 — §0-3·§6-1·§6-2·§4-3

§6-1은 사건 최대 50개·`잘림=true`·`쓴축`을 요구한다. 그러나 §4-3과 현재 `학습자상태.js`는 근거 상한 200, `잘림` 숫자, `쓴축` 없음의 형태다. 후보 문서는 현재 코드를 생산자로 인정하면서 동시에 다른 JSON 모양을 요구하므로 첫 생성 행부터 서로 다른 계보가 쌓인다.

[P0] `policy_ver`의 값 형식과 회귀 기준이 갈린다 — §5-1·§5-4·§12-5·§16

§5-1은 여섯 재료의 해시를 말하고, §5-4는 값이 `'오늘과제.v4'`라는 고정 문자열이라고 말한다. §12-5와 §16은 일곱 재료·일곱 회귀라고 적어 현재 절의 여섯 재료와도 충돌한다. 구현자는 해시를 저장할지 버전명을 저장할지, 여섯 개를 검사할지 일곱 개를 검사할지 결정해야 한다.

[P0] `policy_ver`가 실제 생성 정책을 모두 덮지 않는다 — §5-1·§6-0·§8

지문 재료에는 `갈래판정`, `skill_ids` 생산 규칙, 검문기의 정규화·오류순서가 완전하게 포함되어 있지 않다. 반면 큐 순서·워커 상한·마감 시각 같은 배관 규칙은 포함한다. 그러면 교육적으로 다른 생성 정책이 같은 `policy_ver`에 묶이고, 운영 상수만 바뀐 동일 정책은 불필요하게 다른 판으로 갈린다.

[P0] 배치 전체의 불변 버전 봉투가 없다 — §5-1·§3-2-a·§3-5-b

문서는 배치 시작 시 하나의 판을 계산한다고 하지만 `generation_jobs`에는 `model`, `prompt_ver`, `policy_ver`, 요약판, 선택기판이 없다. 00:10부터 05:50까지 코드나 설정이 바뀌면 같은 배정일 안에서 서로 다른 규칙으로 생성된다. 시도 행에 나중에 기록하는 것만으로는 이미 다른 입력으로 만들어진 job을 되돌릴 수 없다.

[P0] c12와 생성 필수 필드는 미래 작업으로 남아 있다 — §11-2·§12-7·§12-21

현재 계약은 c11이고 c12는 구현자가 나중에 만들 diff다. 검증기는 키의 존재가 아니라 값이 있을 때만 검사하고, 우리 생산자가 필수 필드를 넣는지는 회귀 픽스처에 위임한다. 픽스처가 빠지거나 다른 경로가 호출되면 c11 모양 또는 필드 누락 행이 append-only 원본에 저장된다.

[P0] 원자 착지 입력이 자기 조인 계약을 만족하지 못한다 — §3-0·§3-5-b

§3-0은 `task.assigned`와 `intervention.delivered`가 같은 `intervention_id`를 가져야 한다고 한다. 그러나 §3-5-b의 `_event.task_assigned`에는 `intervention_id`가 없고, 이를 어디서 파생하는지도 없다. RPC가 숨은 기본값에 의존하면 구현자마다 세 사건의 연결 그래프가 달라진다.

[P0] cron 등록만으로는 큐가 살아 있다는 증거가 아니다 — §3-2-a·§12-21

문서는 잡 이름과 시간표만 제시하고 워커가 어느 함수를 어떤 인자로 호출하는지, 실패 시 어떻게 이어지는지는 닫지 않았다. §12-21도 등록 SQL과 스케줄 문자열만 검사하므로 잘못된 대상이나 무작동 잡이 초록이 될 수 있다. 그 상태에서는 `generation_jobs`가 영원히 `대기`여도 문서상 인수 조건을 통과한다.

[P0] ④완결의 ‘늘었나’가 이름만 있다 — §6-0 H3·§12-17

현재 `성과회수.js`는 `skill_ids`를 읽지 않으며, 문서는 확장하라고만 한다. “겨냥 대비 산출”이 어느 반환 필드·집계·회수 시점·이해 대장 칸으로 기록되는지 계약이 없다. 따라서 `skill_ids`는 모였나에만 남고 닿았나·늘었나는 검증할 수 없다.

[P1] claim 실패 시 학생에게 빈 화면을 정상 반환한다 — §3-1·§12-11

워커가 `claimed`인 동안 구제 경로가 claim을 못 잡으면 HTTP 200과 빈 배정을 반환한다. 앱은 오류 카드를 보여 주고 학생이 다음에 다시 열 때까지 과제를 제공하지 않는다. 학생 접점의 ‘실제로 사용할 수 있는 한국어 도움’이 이 정상 경쟁 경로에서 끊긴다.

[P1] 초급·미정 학생을 기능 밖으로 밀어낸다 — §7-1·§12-8

문서는 급수 1~2와 미정을 생성 대상에서 제외하고, 초기에는 이 기능의 도달 학생이 0명일 수 있다고 인정한다. 비율을 보고하는 것은 학생에게 유용한 과제를 제공하지 못하는 문제를 해결하지 않는다. 핵심 초급 학습자에게 기존 일반 과제만 나간다면 그 학생의 목표와 상태를 이해하는 기능이라는 기본 게이트가 비어 있다.

[P1] 재미와 재사용 이유가 평가표 밖에 있다 — §8-A·§8-B

문서는 재미를 오프라인 1인 채점으로 미루고, “왜 학생이 한 번 더 여는가”를 범위 밖으로 둔다. 한 문장이 채점자에게 재미있어 보이는 것과 실제 학생이 다시 열 이유가 있는 것은 다르다. 캐릭터·성과감·목표 맥락과 생성 과제가 연결되는 학생 경험 증거가 없어 재미·신선함 게이트가 닫히지 않는다.

[P1] `skill_ids`는 학생 약점이 아니라 급수별 회전이다 — §6-0·§6-0 H1·§6-0 H2

기술 선택의 원천은 급수 매핑과 최근 미선택 회전이며, 실제 오류·약점은 선택 입력에 들어가지 않는다. 문서 스스로 `skill_ids`를 처치 의도라고 정의하므로, 사후 오류 조인은 오늘 과제가 학생에게 맞았다는 보장이 아니다. §8-B의 TOPIK 적합성도 급수 채점으로만 적혀 있어 문항팩·문항·TOPIK 기술과의 지속 가능한 연결이 없다.

[P1] 생성 평가셋의 표본과 채점 규칙이 재현되지 않는다 — §8-B·§12-19

12개 셀에 셀당 3~4개를 넣어 N=40이라고 했지만 어느 셀에 4개를 넣는지는 정해져 있지 않다. 재미·목표 활용·연결성의 0/1 판정 기준도 구체적인 채점 규칙이 아니며, `채점표 판`은 결과에 기록될 뿐 그 내용이 닫혀 있지 않다. 같은 시험지 이름으로 서로 다른 표본과 판정을 내도 회차 비교가 불가능하다.

[P1] `쓸축수`가 생성 분기인데 정의가 없다 — §4-3·§6-2

`쓸축수===0`이 생성과 `상태없음`을 가르는 핵심 분기지만, 하위 지표가 일부 null일 때 축을 세는 규칙이 없다. 현재 상태 객체에 명시적인 `쓸축수` 생산 필드도 없고 요약 코드에 추론을 위임한다. 같은 학생이 구현자에 따라 생성 또는 폴백으로 갈라질 수 있다.

[P1] 폐기된 규격이 현행 규격과 함께 상주한다 — §7-2·§12-5·§12-7·§16

§7-2는 오늘 조립기를 다시 돌리라고 한 뒤, 바로 아래에 전날 스냅샷을 통째로 복사하는 옛 규칙을 전문으로 남긴다. 여섯/일곱 재료와 14/13값 산술도 현행과 역사 규격이 한 문서 안에서 반복된다. 전문을 읽는 구현자와 도구가 어느 문장을 선택할지에 따라 소급 불가능한 첫 행이 달라진다.

대안 구조 1개 — `generation_batch` 불변 봉투 + `jobs/attempts/finalize` 4층 구조.

배치 시작 시 cutoff, 모델·프롬프트·정책·선택기 판, 상한을 불변 `generation_batch`에 저장하고 job에는 당시 분기·상태 입력을 고정한다. `attempts`에는 명시적 회수·펜스·같은 job 제약을 두며, 최종 RPC는 `task.assigned`·`intervention.delivered`·과제 스냅샷만 원자적으로 만들고 실제 `submission.created`는 학생 행동 뒤에만 허용한다. c12 계약, DDL 제약, cron 실행 검증, 성과 소비자, 평가 결과를 같은 변경 묶음으로 닫기 전에는 학생 노출을 열지 않는다.