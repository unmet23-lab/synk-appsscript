# 상태기반 과제선택 — 처분표 v9 (9회차 · 대상 설계 v5.5)

> **런** `심문-2026-08-14T0354-d790ae` · 대상지문 `475ef906f5e1`(= v5.5) · 편성 luna×2+sol×1(모델정책 고정값 · 효력 max)
> **원건 76** — luna-1 **19**(P0 11·P1 8) · luna-2 **30**(P0 21·P1 9) · sol-3 **27**(P0 20·P1 7) · 결과 3벌 = 같은 폴더 `...-475ef906f5e1-{luna-1,luna-2,sol-3}.md`(커밋 `da68eccd`)
> **게이트**(유호 확정 08-12): ①판정 = 원건 하나도 빠짐없이 채택/부분/기각/범위밖 + 근거 · ②집행 = 채택분이 본문에 들어왔다. 둘 중 하나라도 비면 ⛔착수 차단 유지.
> 🚫 이 표로 처분표 v1~v8 을 덮지 않는다(회차 대조). 🚫 「몇 벌이 같은 말을 했나」는 가중치지 증명이 아니다.
> **①판정 = ✅ 종결 — 76/76 매핑 · 53갈래 ✅32 · ◐15 · ❌5 · ⧉1.** 갈래층 기각이 처음으로 5건이다(1~8회차는 0~4) — 다섯 다 「지적이 겨눈 자리에 기존 명시 처리·명시 기각이 이미 서 있음」을 실측으로 확인한 경우다(§2 해당 칸).

## §1. 조건 지문 — 회차 «안» 통제 2번째 성립 · 회차 «간» 대조 첫 성립

| 회차 | 상태 | 방향 | 철학 | 금지 |
|---|---|---|---|---|
| 9회차 던짐(03:54Z) | 3벌 동일 | `fdce7ffafff1` | `486e20d3af4d` | `c878a65c95a3` |
| 9회차 완료(04:14Z) | 3벌 동일 | `fdce7ffafff1` | `486e20d3af4d` | `c878a65c95a3` |
| (대조) 8회차 던짐·완료 | 3벌 동일 | `4fb5cafa7c07` | `47da58ed6659` | `beeec9a4fa92` |

던짐↔완료 짝 6/6 동일 — F411 수리가 **2번째로** 성립(회차 «안» 통제). **회차 «간» 대조 첫 성립**: 8↔9회차의 조건지문 3종이 전부 갈렸고 그 갈림이 장부에 «보인다». 원인은 위반이 아니라 8회차 처분 자체다 — ①금지: 8회차 처분이 §13 에 v5.5 증보(🚫 10건)를 넣었다 ②철학·방향: 철학 정본 0814 재도장(`792218d1`). **따라서 회차 간 지적 수 증감(64→76)을 조건 동일 전제로 읽으면 안 된다.**

## §2. 갈래 표 — 원건 76 → **53갈래** · 판정 확정

표기: L1-n = luna-1 지적 n · L2-n = luna-2 · S-n = sol-3(등장 순서 번호). 판정 = ✅채택 · ◐부분 · ❌기각 · ⧉범위밖. 근거 줄번호는 §3 실측.

### A군 — 실행 장부 물리 계보 (15)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| A1 | 실행 인스턴스 정체성 부재 — `batch_runs` PK=`assign_date`·미완 행 덮어쓰기·jobs 에 `batch_run_id` 없음 | L1-18·L2-1·S-8 | ✅ **3벌 만장** — `run_id` 신설·jobs `batch_run_id` FK·시작 행 append(PK 재편)·「그 날 정본 실행」 규칙. v8 A4 의 알맹이(시작 증거 보존)는 유지·강화라 재론 아님. `run_id` 는 엔진 내부다 — `_event` 봉투·payload 에 안 싣는다(:3137 correlation 발급 금지와 층 분리 · c12 면적 불변) |
| A2 | `batch_runs` CHECK 부분 상태·음수 허용 | S-9 | ✅ 쌍 채움 CHECK(`(target is null)=(loaded is null)`)+`>=0`+finished 정합 |
| A3 | 승자 계보 — fence·세대·현재성 미검증 | L1-3·L2-5·S-4 | ✅ winning 대조를 「현재 fence 의 성공 시도」로 물리화(+㉤ 현재성 항) |
| A4 | 결정 계보 — result 미대조·NULL 3값·비종결 부착 | L2-4·S-5 | ✅ scope 3값 안전 개서 + `jobs_nonterminal_cols` 에 deciding 비움 + 결정 시도 result↔outcome 정합 대조 |
| A5 | ㉤ 바이트 대조 부재(`generation_input_text`↔`request_body`·산출↔raw 파싱) | L2-10 | ✅ ㉤ 에 ⑤⑥ 두 항 추가(실측 = §3-1 S-4 와 동일 실물 1171-1188) |
| A6 | 스냅샷 메타↔job 결속 — 배치 기준시각 부재·`estimator_*` 일치 무검사 | L1-4·L2-2·L2-3 | ✅ `batch_runs.snapshot_as_of` 신설(배치 한 벌)·jobs 상속·㉤ 에 메타 일치 항. L2-3 의 degraded 반쪽은 **기실재**(v5.5 A10 갈래표 — 실측) |
| A7 | `event_draft` 스키마 계약 부재 | L1-5·L2-9 | ✅ 적재(jobs_load) 의미 검사에 필수 키·타입·learner/assign_date 결속 — 물리 jsonb CHECK 전면화는 안 함(v8 A11 「전조합 물리 과잉」 판정 유지) |
| A8 | 마감 봉투 원천 모순 — 미claim job 의 필수 메타 조립 불가·계산 주체 이원 | S-2 | ✅ draft 에 조립 시점 상태 메타 동봉(오케스트레이터 일원화 — A6 기준시각과 한 벌)·㉠↔㉧ 주석 정리 |
| A9 | `status×outcome` 역방향 부재 | S-6 | ✅ 양방향 열거 CHECK |
| A10 | `대기`+`not_target_reason` 저장 가능·claim 무시 | L2-8 | ❌ **기각 — v5.4 명시 기각 재론**(`:810-814` 실측: 양방향이면 적재 트랜잭션 내 전이가 깨지고, claim 이 사유를 읽으면 «오배제»(정상 job 영구 제외)가 «오포함»보다 크다는 논거로 닫은 자리 · 오염 행은 §12-21 회귀가 센다 · 9회차 신규 사실 0) |
| A11 | `branch_snapshot` 의미 검사 편방향·`level` 정합 무검사 | L1-6·S-14 | ◐ 적재 의미 검사에 역방향(사유='첫날'→`is_first_day=true`)·level 값목록 등재·`초급/미정`↔level 정합 추가 — 물리 CHECK 전면화는 v8 판정대로 안 함 |
| A12 | 생성 대상의 빈 `skill_ids` 통과 | S-15 | ✅ 물리 CHECK(비대상 예외 · 대상 job 은 1~2개) — §6-0 「[] 금지」 절차 규칙의 물리화 |
| A13 | `적재실패` 최소 봉투 행이 대상 분모에 | S-13 | ✅ §3-6 분모에서 «판정 불능» 별도 셈(분모 밖·별도 카운트 보고) |
| A14 | 실행판 5칸 빈 문자열 허용 | L2-28 | ✅ `<> ''` CHECK 5칸 |
| A15 | 착지 못한 시도의 입력 계보 부재 | S-3 | ◐ 별도 칸 신설 없이 A6 집행으로 닫힌다 — attempt→job(기준시각·메타 결속)→상태 사슬을 판정문·주석으로 명시(중복 저장 안 함) |

### B군 — 시간·감시·경로 (10)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| B1 | 배치 전멸=초록 — `deliver-check` 가 `batch_runs` 를 안 봄 | L1-1·L2-17 | ✅ 감시 기준에 `batch_runs`(행 부재=적색·`finished_at` null=적색) 편입 — §3-2-a C4 표 갱신 |
| B2 | `partial` 영속 부재 — 1080 산문(「C4 가 알린다」)에 배선이 없다(자기모순 실측) | L1-2·L2-6·S-11 | ✅ `batch_runs.partial_count` 신설 + C5 감지 결과 저장 + deliver-check 가 읽는 배선(산문의 실물화) |
| B3 | 06:00 마감이 cron 시간표일 뿐 | L1-7 | ✅ `jobs_claim`·정상 `jobs_finalize` 에 마감 시각 조건(경계 상수는 cron 과 같은 원천 명시) |
| B4 | `적재실패` 자동 재시도 부재(학생 접속 의존) | L1-19 | ◐ 재실행 계약(990-993)에 「`적재실패` 행도 재적재 대상」 1항 명시(새 통로 0 — 이미 합법인 오케스트레이터 재실행이 집게 한다) + 감시는 기실재(535 목록에 `적재실패` 포함)임을 명시. 「스윕이 최소 봉투로 닫기」는 안 쓴다 — draft 없이는 과제를 조립할 수 없다(A3 최소 봉투 실물) · cron 이 구제를 부르는 방식도 안 쓴다 |
| B5 | 게임날 구분이 원리상 못 선다(실측: A11 이 true 를 거절해 늘 false) | L2-7 | ✅ C5 판별을 `branch_snapshot` 참조에서 «게임날 달력 판별(§3-4 식) + job 유무»로 재배선 |
| B6 | 구제 경로 날짜·실행판 미정 | L2-16 | ✅ ㉨ 에 UB 날짜 식 재사용 명문 + `_batch` 는 인자 아닌 「DB 가 현행 판을 스스로 읽는다」 규정 |
| B7 | `jobs_load_one` 반환형이 기존 배정 갈래를 못 담음 | S-1 | ✅ 반환형 재설계(갈래 봉투 — kind + job/assigned_event 복합) · 시그니처·반환 표 1301~1305 정합 |
| B8 | 구제 조립 실패가 「없음」으로 위장(행 0·상태 0) | S-12 | ✅ ㉨ 예외 시 그 자리에서 실패 행을 남긴다(큐 없던 날도 감시가 셀 행이 서게) — 응답 값은 D2 와 한 벌 |
| B9 | `attempt_open` 실패 사유 무분화 — 판 불일치가 `예산소진` 으로 오기록 | S-20 | ✅ 거절 사유 반환(판·상태·펜스) + outcome `판불일치` 값 추가(잔류 job 이 그 값으로 닫히게 · c12 개정 요청에 동승) |
| B10 | 열린 attempt(`result` null) 감시 부재 | L2-29 | ✅ deliver-check 에 「열린 attempt(lease 만료)」 1항 |

### C군 — 결정성·입력 조립 (5)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| C1 | 기술 회전 후보 순서 미정 | L1-10·L2-14 | ✅ 후보 조회에 `order by skill_id` 명문(§6-0) |
| C2 | 근거 원천 순서 미정·절단 메타·`"창일수":30` 리터럴 충돌 | L2-13·S-7 | ◐ §11-4 원신호 질의에 `order by (occurred_at, event_id)` 명문 + 리터럴을 「예시·정본은 코드」로 정정 — 「원래 개수 안 남긴다」 반쪽은 **기실재**(실측 2083: 잘림 칸이 원래 개수를 담는다) |
| C3 | 중복 검사 원천 호흡 미정의 | L2-11 | ✅ 「호흡[] 중 ③답하기 행의 프롬프트만」 필터 명문(281 자리) |
| C4 | `쓸축수`↔목표 조각 관계 미정 | L2-12 | ✅ 명문: 쓸축수 = 상태 축(성향+축밖)만 · 목표 조각은 개수에 안 센다 — 목표만 있고 상태 축 0이면 `상태없음`(목표는 2단 폴백의 급수·목적 재료로 산다) |
| C5 | `쓴축` 이 축밖을 안 적음 | L2-27 | ✅ 정의를 「실린 축 이름 전부(성향 5+축밖 6)」로 수정 |

### D군 — 계약·이름·앱 접점 (5)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| D1 | `evidence_refs` 중첩 한글 키 vs snake_case 규약 | S-26 | ✅ 내부 키 영문화(`events`·`window_days`·`axes_used`·`truncated`) — 8회차 D1(필드명 영문·값은 한글)과 같은 축 · c12 검증기 갈림 방지 |
| D2 | 앱 상태 계약 — 오류 상태 없음·구앱 전환 조건 없음 | L1-11·L2-23 | ✅ `assignment_status` 에 오류 값 1개 추가(c12 개정 요청 목록 동승) + 구앱 전환 규칙 1줄(c12 전 앱은 「없음」으로 그린다를 명시적 계약으로) |
| D3 | 폴백 조립 규칙 상충 — §7-2 「재호출」 ↔ C1·㉧ 「draft 만」 | S-23 | ✅ **§7-2 를 C1 에 정렬**(재호출 문구 삭제 · 단일 정본 = §3-5-b C1) — 실측: 재호출 0 스파이(3103)까지 C1 쪽이 정본 |
| D4 | `model`·`prompt_ver` 의미 이원(0회 호출 행에도 실림) | S-19 | ◐ 뜻 명문(「적용하려던 판」 — 실호출 여부는 `generation_outcome` 이 가른다) + 소비자 규칙(모델 효과 집계는 outcome='성공' 필터 필수) — 칸 신설·제거는 안 함 |
| D5 | `generation_input_text` 정의 이원(§11-2 원문 ↔ §8-B 요약) | S-17 | ✅ 정본 = §11-2 「렌더된 요청 본문 원문」(요약이 박힌 전체 · 1787) — §8-B 의 `input_hash` 재료 문구를 그 정의로 수리 |

### E군 — 판·평가 게이트 (7)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| E1 | 평가 비교축에서 `policy_ver` 제외 — 검문 변경 뒤 옛 초록 재사용(**3벌 만장**) | L1-8·L2-20·S-18 | ◐ **「되넣기」는 v8 §6 금지 유지**(배관 커밋마다 80회 재채점) — 대안: 품질 직결 **2재료(검문·파서)** 만 묶은 부분판 `quality_ver` 를 비교축에 신설·편입. 프롬프트는 안 넣는다(:3208 금지 — `prompt_ver` 정본 칸+E6 대조가 이미 지고, D5 집행 뒤엔 `input_hash` 가 원문 층에서 잡는다). 실측(2929-2930: 검문 변경은 `input_hash` 로도 안 잡힘)이 구멍을 실증했고, 부분판은 «품질이 실제로 바뀐 날»에만 재채점을 세운다 |
| E2 | 평가 결과 JSON 기계 계약 미비 | L2-21 | ◐ 키 집합 7개 명문·누락/미지 키=파일 무효·셀 집계식·`grader_note` 규정 — 「자료형 전무」 반쪽은 기실재(0/1·7축 명시 · 실측 2435) |
| E3 | 평가 해시의 증거 참조 부재 | L1-16 | ◐ **`attempt_id` 되살리기는 v8 §6 금지 유지** — 대안: 평가 결과 파일에 `raw_response` **원문 동봉**(해시의 원본 · 불변 파일 층이라 금지의 두 논거(운영 오염·끊긴 UUID)에 안 닿음) |
| E4 | N=40 층화 시험의 재현 절차 부재 | L2-22 | ✅ 시험지 «생성» 절차 명문 — 고정 가상 스냅샷 N벌 + 회전 시작점 열거(결정적) |
| E5 | 미검증 32/72 조합의 학생 노출 무차단 | L1-12·S-16 | ◐ **전조합 채점은 v8 §6 금지 유지 · 차단도 안 함**(폴백 강제=신선함 하락 · ⏳유호 E2 얽힘) — ⓐ시험지 40칸 구성을 「재적 학생 급수 분포 우선」 규칙으로 ⓑ미검증 셀에서 생성이 실제로 나가면 커버리지 보고가 «미검증 노출»로 센다(계기판 적색) |
| E6 | 「상태 기반」 검증 축 부재(1794 의 「②⑦이 잰다」가 정의와 어긋남 — 실측) | L1-13 | ◐ 새 축 신설 없이 ⑦(목표 활용) 채점 정의를 「입력 요약의 축 값이 산출에 반영됐나」로 보강 + 1794 문구 정정 |
| E7 | `policy_ver` 산출 통로 실물 부재 + 지문 함수 위치 오지목(심문 밖 발견) | L2-19 | ◐ 판독기(12재료→지문)를 §12 선행 작업에 실물 등재(트랙 자체가 ⛔미착수라 「구현 없음」 자체는 상태) + **1749-1750 정정: 직렬화 정본 실물은 `tests/오늘과제.test.js:1040`**(lib 지목은 거짓) — 「Vault 까지 재료」 반쪽은 틀림(재료 12종에 없음 · 실측) |

### F군 — 인수·마이그·수집 사슬 (5)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| F1 | 재실행 가드 — 한 블록 예외가 부분 실패 고착 | L2-18·S-10 | ✅ 제약별 개별 가드(존재 검사 개별화) + 신설 칸 `add column` 가드 전량(`winning_result` 하나뿐인 자리) |
| F2 | 롤백 규정이 상위 정본과 충돌 | L1-9 | ❌ **기각 — 기실재**(실측 C-1: §12-27 이 두 정본을 줄번호째 인용하며 경계·L0 §8 기록 6종·만료(=첫 학생)를 이미 명시 · L0:642 표가 「빈 DB 재실행」 갈래 허용 · v8 §6 「역마이그레이션 신설 🚫」 유지) |
| F3 | 발화 관측 사건 계약을 첫 산출 «전» 동결 | L2-15·S-21 | ◐ §12-17·§16-1 의 「발화 관측 사건」을 계약 실물 낱말로 동결 — `submission.created` + task_type(`발화녹음`·`출석발화`) + 조인 키 `assigned_event_id`(새 event_type 신설 안 함) — 「연결 전무」 반쪽은 과장(실측 C-7: `content.viewed[parent_event_id]`→성과회수 닻 사슬 실재) |
| F4 | 재개 조건 실행 규격 부재 | L2-26 | ❌ **기각 — 기실재**(직접 실측: §12-3 이 약속한 «첫 학생 뒤 항»은 v5.5 F4 가 §16-1(3424-3429)에 실측 조건까지 실물화 · 닫는 기준·주체는 그 항이 열리는 날의 판정 재료 — 못 재는 것을 게이트에 안 넣는다(2297)) |
| F5 | corrections 조회 계약 미지정 | S-22 | ◐ 공급자·통로·창을 실물 이름으로 명문(성과회수 층 · `content.viewed` 닻과 같은 통로 · 창 = 성과회수 기존 1·7·30일 재사용) — 「운영 조인을 인수 조건처럼 요구」 반쪽은 기실재(v8 G3 가 픽스처 공급·별도 눈금으로 분리 · 실측 2852-2856) |

### G군 — 구조·완결 (3)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| G1 | §13 배제 ↔ 코어엔진 §10(`valid_from` 등) 충돌 | L1-17 | ❌ **기각 — 층 혼동**(실측 C-3: 코어엔진 §10=추정 «저장» 규격 · §13 배제=상태 «읽기» 재료 · 두 열은 talk 실물에도 DDL 0건이라 「없는 열을 버렸다」가 성립 안 함 · §13 기존 🚫 유지) |
| G2 | 한 문서 다중 정본 — 반복 절이 회차마다 새 공격면 | S-27 | ◐ 문서 «분리»는 기각(복제=갈림의 원천 — G2 이관 원칙 그대로) — 알맹이는 채택: 이번 집행이 갈린 두 절 쌍(D3·D5)을 단일 정본+참조로 수리하고, 반복 자리는 §12-25 계열 기계 대조가 정본 |
| G3 | 구현 0줄에 동결 과다 — 경량 원칙 충돌 | L2-30 | ❌ **기각 — 유호 확정과 정면 충돌**(08-12 「게이트 = 심문 지적 전건 처분」·⛔착수 차단이 확정 절차 그 자체 — 세션이 못 뒤집는다) · 문서 팽창 비용은 G2 이관 트랙이 계속 진다 |

### H군 — 학생 접점·품질 (3)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| H1 | 재미 게이트 앵커 부재 + 종단 인수 시험 부재 | L1-14·S-24 | ◐ ⑤(답하고 싶어지나)에 0점 조건·앵커 2~3사례 명문(**오프라인 산출물 채점 규격** — 학생 단위 측정 아님 · v8 §6 금지에 안 닿음) + §12 에 「생성분이 ③답하기 통로를 타는」 종단 픽스처 1개(2305 「별도 배선 0」 주장의 검증 장치) |
| H2 | 폴백 반복이 신선함 게이트 우회 | L2-24 | ⧉ **범위밖 — 별건 기실재**(실측 C-6: 실물 차단 0은 맞으나 §7-2(2229-2231)가 「이 게이트 밖·현행 라이브 그대로·별건」으로 명시 처리 · 별건 실물 = 대기열 #Q49 줄 「말하기 문장 팩」 후속) |
| H3 | 최신 기능 정찰 게이트 부재 — **3벌 만장** | L1-15·L2-25·S-25 | ✅ **채택**(철학 `SYNK_철학.md:66` + 유호 확정 08-14 「정찰=필수·트리거=만들 때」 · 실측: 설계문 정찰 0매치) — §12 선행 작업에 「구현 착수 «직전» 정찰 1회 산출물(본 것·채택/기각·대가 — 모델·프롬프트·생성 방식)」 1항. 상시 게이트·구독은 안 만든다(확정 「시도 많이·상주 적게」) |

## §3. 실측 — 인용 않고 열어서 쟀다 (컨텍스트 독립 측정 3벌 위임 · 줄번호 동봉 보고 · +직접 2건)

### §3-1. 설계문 §3-5-b·§3-6 (측정 A · 27건) — **맞다 26 · 부분 1 · 틀리다 0**

| ID | 판정 | 근거 줄 | 핵심 실물 |
|---|---|---|---|
| L1-3 | 맞다 | 923-925·933-934 | 승자 FK=같은 job+성공만·결정 FK=같은 job만 — fence·최신시도·결과·바이트 대조 어디에도 0 |
| L1-4 | 맞다 | 1396·1171-1188 | ㉤ 대조 4항뿐 — `snapshot_as_of`·`as_of`·근거사건·창일수 일치 검사 전 문서 0 |
| L1-5 | 맞다 | 716·783·1060 | 정확히는 nullable+조건 CHECK(적재실패 예외)나 내용·타입·결속 검사 0 |
| L1-6 | 맞다 | 1054-1059·1263-1271 | A11=게임날·교정ref·첫날 편방향 셋뿐 · level 값목록이 표에 없어 「값목록 밖 거절」 작동 불가 |
| L1-7 | 맞다 | 1095·1156-1157 | claim 조건=`status='대기'` 하나 · ㉤ 정상 가드=상태+펜스 — RPC 시각 조건 0 |
| L1-18 | 맞다 | 953·990-993 | `assign_date date primary key` · 재실행이 미완 행을 명시적으로 덮음 · jobs 에 `batch_run_id` 0 |
| L2-1 | 맞다 | 693-816·994-995 | `_batch`↔`batch_runs` 갈림을 제약이 아니라 «사후 대조(탐지)»로만 설계 |
| L2-2 | 맞다 | 1018·952-981 | `snapshot_as_of` 원소별 호출자 입력 · 균일성 제약 0 · 배치 기준시각 열 없음 |
| L2-3 | 부분 | 1396·1184-1188 | `estimator_*`·`evidence_refs` 무검사는 맞다 — **`degraded` 는 v5.5 A10 갈래표 대조가 이미 있다**(반쪽 틀림) |
| L2-4 | 맞다 | 786-789·932-935 | scope=outcome↔칸 유무만 · attempt.result 와 outcome 일치 대조 0 |
| L2-5 | 맞다 | 923-925·919-920 | `(attempt_id, job_id, result)` — fence·attempt_no·현재성 0 |
| L2-8 | 맞다 | 815·810-814·1095 | 「편방향이다(쌍조건 아님)」 — **양방향화는 v5.4 가 본문에서 명시 기각해 둔 자리** · claim 은 「안 읽는다」가 의도 |
| L2-9 | 맞다 | 1219-1221·1226 | draft 스키마 검사 전 문서 0 — `branch_snapshot`(A8/A11)과 비대칭 · 스윕은 재실행 금지라 draft 가 유일 원천 |
| L2-18 | 맞다 | 917-926 | 승자 3제약이 한 ALTER+한 `duplicate_object` 블록 — 첫 제약 기존 시 나머지 둘 스킵 |
| L2-28 | 맞다 | 709·727-729·963-968 | 실행판 5칸 형식·비공백 CHECK 0 |
| S-1 | 맞다 | 1229·1251-1252·1305 | `returns engine.generation_jobs` 인데 기존 배정이면 「job 안 만들고 배정 재조회 반환」 — 반환형이 원리상 못 담음 |
| S-2 | 맞다 | 1067·1111-1115·1408 | draft 에 estimator 칸 없음+㉧ 다른 원천 규정 0 — 미claim job 사건은 필수 칸을 못 채움 · 계산 주체 이원(1025↔1113) 실재 |
| S-3 | 맞다 | 821-851 | attempts 에 `estimator_confidence`·`evidence_refs`·쓴축 칸 0 |
| S-4 | 맞다 | 923-925·1171-1188 | ㉤ ②는 봉투 «내부» 대조 — `generation_input_text`↔`request_body`·저장문↔raw 파싱 대조 목록에 없음 |
| S-5 | 맞다 | 786-797·932-935 | `=` 비교라 outcome null→UNKNOWN 통과 · nonterminal 목록에 deciding 없음 · 벤더오류 attempt 승인 |
| S-6 | 맞다 | 777-781 | 정방향 2개뿐 — 역방향 제약 0(문서 자신이 「안 건다」 자인) |
| S-8 | 맞다 | 953·990-993 | run_id·fence 열 자체 없음 — 겹침 시 metadata 후발 값 승 |
| S-9 | 맞다 | 974-980·986-987 | 한쪽만 채우면 UNKNOWN 통과 · `>=0` 검사 0 · 동시 채움은 산문뿐 |
| S-10 | 맞다 | 917-926·916 | v5.5 신설 칸이 CREATE 본문 안이라 기존 표 미복구 · add column 가드는 `winning_result` 하나 |
| S-13 | 맞다 | 1555·1042·971 | 최소 봉투(ver 만) 행이 대상 분모·`target_count` 에 포함(「만 허용」이 아니라 「최소 허용」이 정확) |
| S-14 | 맞다 | 1059·1269 | ③ 편방향 · `초급/미정`↔`level` 정합 검사 어디에도 0 |
| S-15 | 맞다 | 708·1951-1954·1393 | 1~2개는 절차 규칙뿐 물리 CHECK 0 — 성공 착지는 job 값 그대로 사건에 실림 |

### §3-2. 외부 정본·코드 (측정 C · 7과제) — 층 못 연 것 0

| ID | 판정 | 실물 | 핵심 |
|---|---|---|---|
| C-1(L1-9) | **부분** | talk `개발_분업.md:187`·`L0_데이터계약.md:636~637` ↔ 설계 3085~3100 | 정본 요구는 실재하나 **§12-27 이 두 정본을 줄번호째 인용하며 경계·L0 §8 기록 6종·만료(=첫 학생)를 이미 명시** — L0:642 표 자체가 「빈 DB 재실행」 갈래 허용. «미해결 충돌»은 틀리다 |
| C-2(H3) | **맞다** | 철학 `SYNK_철학.md:66` ↔ 설계문 전문 Grep 0 | 「만들 때마다 정찰」 유호 확정 실재 · 설계문에 「정찰」·「최신 기능」 0매치 — §8·§12 에 정찰 산출물 없음 |
| C-3(G1) | **부분** | `코어엔진_설계.md:172` ↔ 설계 3249~3252 · talk `이벤트검증.js:230~232` | 코어엔진 §10=추정 «저장» 규격 · §13 배제=상태 «읽기» 재료 — 두 열은 talk 실물에도 DDL 0건이라 직접 충돌 미성립 |
| C-4(E5) | **맞다** | talk `20260812120000_engine_c11.sql:140~170` | 시드 30행(grammar 18·expr 5·vocab 7) — **급수(레벨) 칸 0**(테이블·시드·c11 어디에도) |
| C-5(E7) | **부분** | 설계 1732~1757 ↔ talk `스키마_지문.sql:31`·`tests/오늘과제.test.js:1040` | 12재료 판독 함수·RPC 실재 0(맞다 — 단 트랙 자체가 ⛔미착수) · «Vault까지 재료»는 틀리다 · ➕심문 밖 발견: **설계 1749~1750 이 직렬화 정본을 `lib/오늘과제.js` 로 지목하나 실물은 `tests/오늘과제.test.js:1040`** |
| C-6(H2) | **맞다** | talk `lib/오늘과제.js:299~303` ↔ 설계 2229~2231 | 폴백 반복 차단 실물 0 — 단 설계문 자신이 「이 게이트 밖 · 현행 라이브 그대로 · 별건」으로 명시 처리 |
| C-7(F3) | **부분** | talk `계약/수집_교정_계약.json:74·90·156`·`이벤트검증.js:178`·`성과회수.js:143~152` ↔ 설계 2824~2825 | 발화 전용 event_type 부재는 맞다(발화 = `submission.created`+task_type 이 짐) · `delivered.parent_event_id` 비움은 계약 뜻(설계도 🚫 명시) · **「연결 계약 전무」는 과장** — `content.viewed[parent_event_id]`→성과회수 닻 사슬 실재 |

### §3-3. 설계문 나머지 절 (측정 B · 33건) — **맞다 29 · 부분 4 · 틀리다 0**

| ID | 판정 | 근거 줄 | 핵심 실물 |
|---|---|---|---|
| L1-1 | 맞다 | 535·946~997·3106 | 감시 목록 = `('대기','claimed','적재실패')` — `batch_runs` 연결 전 문서 0 |
| L1-2 | 부분 | 1079~1080·535 | 반환만·수리 없음은 맞다 — 단 1080 「C4 가 알린다」 산문이 있는데 **배선이 없다**(문서 자기모순을 지적이 포착) |
| L1-8 | 맞다 | 1732·2437·2452~2453 | 「policy_ver 는 비교에서 뺐다(기록만)」·CI 정지 자인·「실재하면 활성」 전부 실재 |
| L1-10 | 맞다 | 1945~1949 | 시작 위치가 가리킬 후보 배열의 정본 순서·ORDER BY 문서 전체 0(1105 order by 는 학생 큐 것) |
| L1-11 | 맞다 | 2540~2549·417~418 | 「c12 는 다음 판이고 아직 없다」 · 클라이언트 전환 조건 문서 전체 0 |
| L1-12 | 맞다 | 2379·1946 | 「나머지 32 조합은 검증 없이 운영에서 처음 만난다」 자인 · §6-0 「급수 필터 0」 |
| L1-13 | 맞다 | 3041~3046·2386~2392·1794 | 7축 정의에 성향·습관·약점 «사용» 검사 축 0 — 1794 는 ②⑦이 잰다고 주장하나 정의는 급수·목표뿐 |
| L1-14 | 맞다 | 2305~2312 | 「같은 통로라 그대로 붙는다 — 별도 배선 0」 · §12 에 도달 픽스처 0 |
| L1-16 | 맞다 | 2435 | 「attempt_id 를 뺐다」 — 원응답·실제 입력은 해시만·불변 참조 0 |
| L1-19 | 맞다 | 1033~1035·1421·535 | 전이표상 유일 복귀 = 구제 — deliver-check 는 세어 알릴 뿐 |
| L2-6 | 부분 | 1083·1080·535 | 저장 0은 맞다 — 「읽지 않는다」는 C4 기준 층에선 맞고 1080 산문과는 어긋남(배선 없는 주장) |
| L2-7 | 맞다 | 588·1508·1081~1082·1056~1057 | 게임날 jobs ❌라 저장 행엔 그 칸이 없고 A11 이 true 를 거절해 손안 스냅샷도 늘 false — **갈래가 원리상 못 선다** |
| L2-11 | 맞다 | 279~281·2262 | 호흡 배열 어느 행(③답하기)인지 필터 0 |
| L2-12 | 맞다 | 1691~1696·2124 | 목표 조각을 축으로 세는지 네 절 전부 무언 |
| L2-13 | 맞다 | 2076~2079·2686~2688 | §11-4 원신호 질의에 ORDER BY 0(문서 전체 order by 는 1105 하나) |
| L2-14 | 맞다 | 1945~1948 | L1-10 과 동일 자리 |
| L2-16 | 맞다 | 504~505·1229·1245 | ㉨ 호출자의 날짜 산출식·`_batch` 조달 규정 0 |
| L2-17 | 맞다 | 982~989·970·535 | 「finished_at null = 도중에 죽었다」 증거는 표에 남되 deliver-check 가 본다는 계약 0 |
| L2-20 | 맞다 | 1732·2437·2929~2930 | 검문(재료2) 변경은 입력 문자열을 안 바꿔 `input_hash` 로도 안 잡히는데 2437 은 「잡는다」 서술 |
| L2-21 | 부분 | 2435·2386 | 값역 0/1·축수 7 명시라 「자료형 전무」는 아님 — 키 집합·누락 처리·집계식·note 필수는 부재 |
| L2-22 | 맞다 | 2381·1949~1950 | 「스냅샷 동봉 — 재추출 없음」 · 회전 입력 미정의라 실행 가능한 생성 절차 아님 |
| L2-23 | 맞다 | 401~404·1305 | 값 셋뿐 — 구제 규격 위반을 표현할 응답 값 0 |
| L2-27 | 맞다 | 2057·2092~2095 | 실물은 성향5+축밖6인데 쓴축은 성향 이름만 |
| L2-29 | 맞다 | 1126~1128·535 | 「열린 attempt 를 안 닫는다」 — result null 감시 규칙 문서 전체 0 |
| S-7 | 맞다 | 2074~2076·2052·1676~1677·2083 | ORDER 0 + 「창을 상수로 안 적는다」(1676)와 예시값 30 충돌 — 단 잘림 칸은 원래 개수를 담는다(2083) |
| S-12 | 맞다 | 1305·401·535·1230~1232 | ㉨ 예외 시 행 0·상태 0·전용 값 0 — 큐 없던 날 셀 행 자체가 없다 |
| S-17 | 맞다 | 2565·2435·1787 | §11-2 「요청 본문 원문」 ↔ §8-B 「요약 문자열」 — 갈림 실재 |
| S-18 | 맞다 | 2437·2921~2949 | §12-19 에 현행 입력 재해시↔결과 행 대조 규칙 0 |
| S-20 | 맞다 | 1120·1123·1299·781·1422 | 실행판 거절 반환도 null 하나 — 잔류 job 은 마감폴백=`예산소진` 으로 닫혀 판 불일치가 가려짐 |
| S-21 | 맞다 | 2846~2849·3427~3430·353 | 발화 관측의 event type·생산자·연결 키 동결 0(353 은 `content.viewed` 것) |
| S-22 | 부분 | 2004~2009·2852~2856 | 층 이름·키 미지정·창 미정은 맞다 — 「운영 조인 요구」 반쪽은 G3 가 픽스처 공급·별도 눈금으로 이미 분리 |
| S-23 | 맞다 | 2210~2211·1063~1068·1221·3103~3104 | 「오늘과제() 재호출」 ↔ C1 「한 번 굳힌다」·㉧ 재호출 금지·재호출 0 스파이 — **정면 상충 실재**(§7-2 미수거) |
| S-24 | 맞다 | 2390·2386·2305~2307 | 2386 「무엇이 0인가를 적는다」 선언과 달리 ⑤엔 0점 조건·앵커 0 + 동일 통로 인수 시험 0 |

### §3-4. 직접 실측 2건 (위임 목록 밖 국소)

| ID | 판정 | 근거 줄 | 핵심 실물 |
|---|---|---|---|
| L2-26(F4) | **틀리다(절반)** | 3424-3429·2297·175 | §12-3 이 약속한 «첫 학생 뒤 항»은 v5.5 F4 가 §16-1 에 **이미 실물화**(실측 조건 = 생성 성공 배정 ≥1 + 발화 관측 사건 · 여는 날 = 첫 제출 뒤) |
| S-19(D4) | **맞다(뜻 겹침 실재)** | 1393·1596·1604·2032 | 성공 착지는 job 실행판 그대로 사건에 실리고 `키없음`(시도 자체를 안 함)·`상태없음` 행에도 같은 칸이 실린다 |

주(중복 실측 대체): **A5(L2-10)** 의 사실 = 측정 A 의 S-4 실물(1171-1188)과 동일. **B2(S-11)** 의 사실 = L1-2·L2-6 실물과 동일. **A10 기각 근거** = 직접 실측 `:810-814`(v5.4 기각 주석 원문).

## §5. 집행 지도 — 절 단위 · 이 순서대로 (②집행 = 설계 v5.6 · 물리 먼저)

1. **§3-5-b DDL** — A1(run_id·batch_run_id·PK 재편·append 계약) · A2 · A9 · A12 · A14 · A3·A4(계보 CHECK/FK 강화) · F1(가드 개별화)
2. **§3-5-b 봉투·draft** — A7(적재 의미 검사) · A8(draft 상태 메타 동봉) · A6(`snapshot_as_of` 한 벌) · A15(사슬 명시)
3. **§3-5-b RPC** — B3(마감 가드) · B7(㉨ 반환형) · B6(㉨ 날짜·판) · B8(예외 행) · B9(open 사유+`판불일치`) · A5(㉤ ⑤⑥)
4. **§3-6·§3-2-a** — A13(분모 분리) · B4(적재실패→스윕 갈래) · B5(게임날 재배선) · B1·B2·B10(deliver-check 기준 확장 — C4 표)
5. **§6·§11-4** — C1(order by) · C4(쓸축수 정의) · C5(쓴축 전부) · D1(키 영문화) · C2(원신호 ORDER·리터럴 정정)
6. **§5·§8-B** — D5(input_text 단일 정본) · E1(`quality_ver` 부분판) · E2(키 집합·집계) · E3(원문 동봉) · E4(시험지 절차) · E5(급수 분포 우선·미검증 노출 셈) · E6(⑦ 정의·1794 정정) · E7(판독기 선행 등재·1749 정정) · H1(⑤ 앵커+종단 픽스처)
7. **§7-2·§11-2·§12** — D3(§7-2 를 C1 에 정렬) · D2(오류 값·전환 규칙 — c12 요청 동승) · D4(뜻 명문) · F3(발화 낱말 동결) · F5(공급자 실물) · H3(정찰 선행 1항)
8. **§13·머리말·게이트** — §6 재제안 금지 등재 · 판 계보 v5.6 · 게이트 표 9회차 줄 · ④완결 ✗✗✗ 유지

## §6. 재제안 금지 (이 회차 — 근거는 §2 해당 칸)

- `jobs_nontarget_cols` 쌍조건화·`jobs_claim` 의 사유 읽기(A10 — `:810-814` v5.4 기각 논거: 트랜잭션 내 전이 파괴 · 오배제 > 오포함)
- 역마이그레이션·롤백 경로 신설(F2 — §12-27 기실재 · v8 §6 유지)
- «첫 학생 뒤 항» 재실물화 요구(F4 — §16-1:3424-3429 에 이미 서 있다)
- `valid_from`·`expires_at` 를 이 트랙에 편입(G1 — 층 혼동 · DDL 0 · §13 기존 🚫)
- 「동결 과다 — 게이트 축소」·「문서 분리」(G3·G2 — 유호 확정 08-12 게이트 · 복제 금지)
- 폴백 반복 차단을 이 트랙에 편입(H2 — 별건 = 말하기 문장 팩)
- «전체» `policy_ver` 를 §8-B 비교축에 되넣기(E1 — v8 §6 유지 · 이번 처방은 품질 3재료 부분판 `quality_ver` 뿐)
- 평가 결과 행에 `attempt_id` 되살리기(E3 — v8 §6 유지 · 이번 처방은 원문 동봉뿐)
- 급수×기술 전 조합 사람 채점·미검증 셀 «차단»(E5 — v8 §6 유지 + 차단=폴백 강제 · ⏳유호 E2 얽힘)

## §7. 게이트 현황

| 회차 | 판 | 지적 | ①판정 | ②집행 |
|---|---|---|---|---|
| 9회차(`475ef906f5e1` = v5.5) | v5.5 | **76**(P0 52·P1 24) | ✅ **이 표 — 76/76 매핑 · 53갈래 ✅32·◐15·❌5·⧉1** | ▶다음 = 설계 v5.6(§5 지도 8단계) |

- ①판정 = ✅ 종결(2026-08-14 · 세션 `a4946442`) — 기각 5는 전부 «기존 명시 처리의 재론»을 실측으로 확인한 것(신규 사실 0).
- ⛔구현 착수 차단은 그대로다 — ②집행(v5.6)이 끝나야 이 회차분 전건 처분이 닫힌다.
- ④완결(이 트랙) = **✗✗✗**(변동 없음 — 구현 0줄).
