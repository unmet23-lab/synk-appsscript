# 상태기반 과제선택 — 처분표 v9 (9회차 · 대상 설계 v5.5)

> **런** `심문-2026-08-14T0354-d790ae` · 대상지문 `475ef906f5e1`(= v5.5) · 편성 luna×2+sol×1(모델정책 고정값 · 효력 max)
> **원건 76** — luna-1 **19**(P0 11·P1 8) · luna-2 **30**(P0 21·P1 9) · sol-3 **27**(P0 20·P1 7) · 결과 3벌 = 같은 폴더 `...-475ef906f5e1-{luna-1,luna-2,sol-3}.md`(커밋 `da68eccd`)
> **게이트**(유호 확정 08-12): ①판정 = 원건 하나도 빠짐없이 채택/부분/기각/범위밖 + 근거 · ②집행 = 채택분이 본문에 들어왔다. 둘 중 하나라도 비면 ⛔착수 차단 유지.
> 🚫 이 표로 처분표 v1~v8 을 덮지 않는다(회차 대조). 🚫 「몇 벌이 같은 말을 했나」는 가중치지 증명이 아니다.

## §1. 조건 지문 — 회차 «안» 통제 2번째 성립 · 회차 «간» 대조 첫 성립

| 회차 | 상태 | 방향 | 철학 | 금지 |
|---|---|---|---|---|
| 9회차 던짐(03:54Z) | 3벌 동일 | `fdce7ffafff1` | `486e20d3af4d` | `c878a65c95a3` |
| 9회차 완료(04:14Z) | 3벌 동일 | `fdce7ffafff1` | `486e20d3af4d` | `c878a65c95a3` |
| (대조) 8회차 던짐·완료 | 3벌 동일 | `4fb5cafa7c07` | `47da58ed6659` | `beeec9a4fa92` |

던짐↔완료 짝 6/6 동일 — F411 수리가 **2번째로** 성립(회차 «안» 통제). **회차 «간» 대조 첫 성립**: 8회차↔9회차의 조건지문 3종이 전부 갈렸고 그 갈림이 장부에 «보인다». 원인은 위반이 아니라 8회차 처분 자체다 — ①금지: 8회차 처분이 §13 에 v5.5 증보(🚫 10건)를 넣었다 ②철학·방향: 철학 정본 0814 재도장(`792218d1` · §0 위반 2곳 수리). **따라서 9회차 지적은 8회차와 «다른 금지목록» 아래서 나온 것이고, 회차 간 지적 수 증감(64→76)을 조건 동일 전제로 읽으면 안 된다.**

## §2. 갈래 표 — 원건 76 → **53갈래** (판정은 §3 실측 뒤)

표기: L1-n = luna-1 지적 n · L2-n = luna-2 · S-n = sol-3(등장 순서 번호). 판정 = ✅채택 · ◐부분 · ❌기각 · ⧉범위밖.

### A군 — 실행 장부 물리 계보 (15)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| A1 | 실행 인스턴스 정체성 부재 — `batch_runs` PK=`assign_date`·미완 행 덮어쓰기·jobs 에 `batch_run_id` FK 없음(동시 재실행 계보 섞임) | L1-18·L2-1·S-8 | (§3 뒤) |
| A2 | `batch_runs` CHECK 가 부분 상태·음수 허용(UNKNOWN 통과) | S-9 | (§3 뒤) |
| A3 | 승자 계보 — `winning_attempt_id` FK 가 fence·세대·현재성 미검증(옛 성공 시도를 승자로 기록 가능) | L1-3·L2-5·S-4 | (§3 뒤) |
| A4 | 결정 계보 — `deciding_attempt_id` 가 result 미대조·`outcome=null` 3값 통과·비종결 행에 부착 가능 | L2-4·S-5 | (§3 뒤) |
| A5 | ㉤ 바이트 대조 부재 — `generation_input_text`==`attempt.request_body`·저장 문장==raw response 파싱 결과 | L2-10 | (§3 뒤) |
| A6 | 스냅샷 메타↔job 결속 — `snapshot_as_of` 임의 입력·배치 기준시각 부재·`estimator_*`/`evidence_refs`/`degraded` 일치 검사 없음 | L1-4·L2-2·L2-3 | (§3 뒤) |
| A7 | `event_draft` 스키마 계약 부재(필수 키·자료형·학생·배정일 결속) — 마감 폴백의 유일 원천인데 무검증 | L1-5·L2-9 | (§3 뒤) |
| A8 | 마감 스윕 봉투 원천 모순 — 워커 안 돈 job 의 `estimator_confidence`·`evidence_refs` 조립 불가·계산 주체 이원(㉠ 주석↔워커) | S-2 | (§3 뒤) |
| A9 | `status×outcome` CHECK 역방향 부재(`착지`+`대상아님` 저장 가능) | S-6 | (§3 뒤) |
| A10 | 금지된 중간 상태 — `대기`+`not_target_reason` 저장 가능·`jobs_claim` 이 사유 무시 | L2-8 | (§3 뒤) |
| A11 | `branch_snapshot` 의미 검사 편방향 — 역방향(`is_first_day=false`+사유 첫날)·`level`↔`초급` 정합 미검사 | L1-6·S-14 | (§3 뒤) |
| A12 | 생성 대상의 빈 `skill_ids` 통과(1~2개 CHECK 없음·존재 검사는 빈 배열에 공허 성공) | S-15 | (§3 뒤) |
| A13 | `적재실패` 최소 봉투 × 대상 분모 — 판정 불능 행을 대상으로 센다 | S-13 | (§3 뒤) |
| A14 | 실행판 5칸이 `text not null` 뿐 — 빈 문자열 거부 없음 | L2-28 | (§3 뒤) |
| A15 | 착지 못한 시도의 입력 계보 — attempt 에 요청을 만든 상태 메타가 없다 | S-3 | (§3 뒤) |

### B군 — 시간·감시·경로 (10)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| B1 | 배치 전멸=초록 — `deliver-check` 가 `batch_runs` 행 부재·`finished_at=null` 을 감시 안 함 | L1-1·L2-17 | (§3 뒤) |
| B2 | `partial` 영속 부재 — 반환값뿐·장부 칸 없음·감시 못 셈·복구 경로 없음 | L1-2·L2-6·S-11 | (§3 뒤) |
| B3 | 06:00 마감이 cron 시간표일 뿐 — `jobs_claim`/정상 `jobs_finalize` 에 마감 시각 가드 없음 | L1-7 | (§3 뒤) |
| B4 | `적재실패` 자동 재시도 부재 — 학생 접속(`/tasks`)에만 매달림 | L1-19 | (§3 뒤) |
| B5 | 게임날 부분실패 구분의 물리 근거 부재 — job·`branch_snapshot` 없는 행을 `is_game_day` 로 구분하라 함 | L2-7 | (§3 뒤) |
| B6 | 구제 경로의 날짜 산출·실행판 입력 미정(`jobs_load_one`) | L2-16 | (§3 뒤) |
| B7 | `jobs_load_one` 반환형이 기존 배정 갈래를 표현 못함(`returns generation_jobs`) | S-1 | (§3 뒤) |
| B8 | 구제 조립 실패가 「오늘 과제 없음」으로 위장(상태 안 남김·셀 행 없음) | S-12 | (§3 뒤) |
| B9 | `attempt_open` 실패 사유 무분화 — 판 불일치가 `예산소진` 으로 오기록 | S-20 | (§3 뒤) |
| B10 | 열린 attempt(`result is null`) 감시 부재 — 정상 폴백과 미확인 벤더 호출 구분 불가 | L2-29 | (§3 뒤) |

### C군 — 결정성·입력 조립 (5)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| C1 | 기술 회전 후보 순서 미정(ORDER BY 부재 — 같은 학생·날짜가 다른 `skill_ids`) | L1-10·L2-14 | (§3 뒤) |
| C2 | 근거 절단 — 원천 SQL 순서 미정·전체 개수/나머지 미보존·`"창일수":30` 리터럴이 「코드만 정본」 규칙과 충돌 | L2-13·S-7 | (§3 뒤) |
| C3 | 중복 검사의 원천 호흡 미정의(듣기 안내 vs 답하기 질문) | L2-11 | (§3 뒤) |
| C4 | `쓸축수`↔목표 조각 관계 미정(목표만 있는 요약의 `상태없음` 판정 갈림) | L2-12 | (§3 뒤) |
| C5 | `쓴축` 이 축밖 지표·목표를 안 적음(입력 계보 불완전) | L2-27 | (§3 뒤) |

### D군 — 계약·이름·앱 접점 (5)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| D1 | `evidence_refs` 중첩 한글 키 vs snake_case 규약 | S-26 | (§3 뒤) |
| D2 | 앱 상태 계약 — `assignment_status` 에 오류 상태 없음·c12 미동결·구앱 전환 조건 없음 | L1-11·L2-23 | (§3 뒤) |
| D3 | 폴백 조립 규칙 상충 — §7-2 「`오늘과제()` 재호출」 ↔ C1·㉧ 「draft 만·재호출 금지」 | S-23 | (§3 뒤) |
| D4 | `model`·`prompt_ver` 의미 이원 — 0회 호출 outcome 에도 실행판 복사(실사용 vs 계획값) | S-19 | (§3 뒤) |
| D5 | `generation_input_text` 정의 이원 — §11-2 「렌더된 요청 본문」 ↔ §8-B 「요약 문자열」 | S-17 | (§3 뒤) |

### E군 — 판·평가 게이트 (7)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| E1 | 평가 비교축에서 `policy_ver` 제외 — 검문·폴백 변경 뒤에도 옛 평가 초록 재사용 | L1-8·L2-20·S-18 | (§3 뒤) |
| E2 | 평가 결과 JSON 기계 계약 부재(`axis_scores` 키 집합·누락 처리·집계식) | L2-21 | (§3 뒤) |
| E3 | 평가 해시의 증거 참조 부재(`attempt_id` 제거로 원문 재검토 불가) | L1-16 | (§3 뒤) |
| E4 | N=40 층화 시험의 재현 입력 부재(시험지 생성 절차 미정) | L2-22 | (§3 뒤) |
| E5 | 미검증 32/72 조합의 학생 노출 무차단 | L1-12·S-16 | (§3 뒤) |
| E6 | 「상태 기반」 검증 축 부재 — 7축에 상태 실사용 축 없음 | L1-13 | (§3 뒤) |
| E7 | `policy_ver` 산출 통로 실물 부재(pg_get_functiondef·cron 을 읽을 장치) | L2-19 | (§3 뒤) |

### F군 — 인수·마이그·수집 사슬 (5)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| F1 | 재실행 가드 — 여러 제약을 한 `DO`/예외 블록에 묶어 부분 실패 고착 | L2-18·S-10 | (§3 뒤) |
| F2 | 롤백 규정이 상위 정본(개발_분업:187·L0:636~)과 충돌 주장 | L1-9 | (§3 뒤) |
| F3 | 발화 관측 사건 계약(event type·조인 키) 을 첫 산출 «전» 에 동결해야 | L2-15·S-21 | (§3 뒤) |
| F4 | 재개 조건의 실행 규격 부재(닫는 기준·주체·기록) | L2-26 | (§3 뒤) |
| F5 | corrections 조회 계약 미지정(호출 파일·링크 키·관측 창) | S-22 | (§3 뒤) |

### G군 — 구조·완결 (3)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| G1 | 추정 메타 유효기간 — §13 배제 ↔ 코어엔진_설계 §10(`valid_from`·`expires_at`·`user_confirmed`) 충돌 주장 | L1-17 | (§3 뒤) |
| G2 | 한 문서 다중 정본 — 같은 값의 반복 절이 회차마다 새 공격면 | S-27 | (§3 뒤) |
| G3 | 구현 0줄에 인프라 동결 과다 — 경량 원칙 충돌 주장 | L2-30 | (§3 뒤) |

### H군 — 학생 접점·품질 (3)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| H1 | 재미 게이트 실행 규격 부재(앵커·0점 조건) + 생성분 종단 인수 시험 부재 | L1-14·S-24 | (§3 뒤) |
| H2 | 폴백 반복이 신선함 게이트를 우회(같은 일반 과제 반복 무차단) | L2-24 | (§3 뒤) |
| H3 | 최신 기능 정찰 게이트 부재 — **3벌 만장** | L1-15·L2-25·S-25 | (§3 뒤) |

## §3. 실측 — 인용 않고 열어서 쟀다 (컨텍스트 독립 측정 3벌 위임 · 줄번호 동봉 보고)

(측정 A = 설계문 §3-5-b·§3-6 · 27건 / 측정 B = 설계문 나머지 절 · 33건 / 측정 C = 외부 정본·코드 · 7과제 — 결과 수거 후 기입)

## §4. 원건 → 갈래 전량 매핑 (76/76)

| 원건 | 갈래 | | 원건 | 갈래 | | 원건 | 갈래 |
|---|---|---|---|---|---|---|---|
| L1-1 | B1 | | L2-1 | A1 | | S-1 | B7 |
| L1-2 | B2 | | L2-2 | A6 | | S-2 | A8 |
| L1-3 | A3 | | L2-3 | A6 | | S-3 | A15 |
| L1-4 | A6 | | L2-4 | A4 | | S-4 | A3 |
| L1-5 | A7 | | L2-5 | A3 | | S-5 | A4 |
| L1-6 | A11 | | L2-6 | B2 | | S-6 | A9 |
| L1-7 | B3 | | L2-7 | B5 | | S-7 | C2 |
| L1-8 | E1 | | L2-8 | A10 | | S-8 | A1 |
| L1-9 | F2 | | L2-9 | A7 | | S-9 | A2 |
| L1-10 | C1 | | L2-10 | A5 | | S-10 | F1 |
| L1-11 | D2 | | L2-11 | C3 | | S-11 | B2 |
| L1-12 | E5 | | L2-12 | C4 | | S-12 | B8 |
| L1-13 | E6 | | L2-13 | C2 | | S-13 | A13 |
| L1-14 | H1 | | L2-14 | C1 | | S-14 | A11 |
| L1-15 | H3 | | L2-15 | F3 | | S-15 | A12 |
| L1-16 | E3 | | L2-16 | B6 | | S-16 | E5 |
| L1-17 | G1 | | L2-17 | B1 | | S-17 | D5 |
| L1-18 | A1 | | L2-18 | F1 | | S-18 | E1 |
| L1-19 | B4 | | L2-19 | E7 | | S-19 | D4 |
| | | | L2-20 | E1 | | S-20 | B9 |
| | | | L2-21 | E2 | | S-21 | F3 |
| | | | L2-22 | E4 | | S-22 | F5 |
| | | | L2-23 | D2 | | S-23 | D3 |
| | | | L2-24 | H2 | | S-24 | H1 |
| | | | L2-25 | H3 | | S-25 | H3 |
| | | | L2-26 | F4 | | S-26 | D1 |
| | | | L2-27 | C5 | | S-27 | G2 |
| | | | L2-28 | A14 | | | |
| | | | L2-29 | B10 | | | |
| | | | L2-30 | G3 | | | |

검산: L1 19 + L2 30 + S 27 = **76/76** · 빠짐 0 · 한 원건 = 한 갈래(중복 0) · 갈래 합 24+13+7+6+10+7+3+6 = 76 ✓.

## §5. 집행 지도 — (①판정 종결 후 작성)

## §6. 재제안 금지 — (판정 후 작성)

## §7. 게이트 현황

| 회차 | 판 | 지적 | ①판정 | ②집행 |
|---|---|---|---|---|
| 9회차(`475ef906f5e1` = v5.5) | v5.5 | **76**(P0 52·P1 24) | ▶작성 중 | — |
