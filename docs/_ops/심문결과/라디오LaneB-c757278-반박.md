# 분리 반박 1패스 — talk `c757278` (라디오24 Lane B · 2026-08-12 · 컨텍스트 독립 리뷰어)

> 발주 세션 = `local_4a7d40f9`. 리뷰어는 커밋 diff + HEAD 원문을 읽고 **주장 전부를 실행으로 재현**해 확인했다(저장소 무변경).
> **판정: 치명 7 · 경미 8.** 배포 게이트 최소 셋 = ①(리듬·끈기 오염 — 개입 행 스탬프라 소급 불가) · ②(계약판 앞서감 — 앱 전건 426) · ④(5000 굶주림 — 영구 손실).
> ⚠ 이 파일은 리뷰어 보고 원문의 보존이다 — 수리 세션은 여기서 골라 고치고, 항목별 처분(수리/기각+사유)을 이 파일에 덧붙인다.

## 치명

### 1. 라디오 `!출석`·`!목표` 승격 행이 앱 숙제 제출률·완주율로 읽힌다
- 파일: talk `lib/라디오승격.js:236-254`(submission.created 갈래) + `lib/학습자상태.js:158-159, 197-220, 228-236`
- 리듬축은 `task.assigned`×`submission.created`를 시간 구간으로 짝짓는다. 승격 행은 event_type 이 같아 필터를 그대로 통과하고, task_type(라디오퀴즈·목표선언·자습체크인)을 보는 소비자가 한 곳도 없다.
- 재현(실행함): 배정 1건(due 16:00) + 라디오 자습체크인 승격 행(04:00) → `리듬={"제출률":1,"마감여유분_중앙":720,"지각":0,"n":1}` · `끈기={"완주율":1,"n":1}`. **앱을 한 번도 안 연 학생이 「숙제를 12시간 여유 두고 냈다」가 된다.** deliver 가 개입 행에 스탬프하므로 되돌릴 수 없다.
- 같은 파일이 작성과정.n 표본 제외로 이 계열을 이미 못박아 놨는데 새 생산자에 적용 안 함.
- 방향: 리듬·끈기축 입력을 앱 통로 task_type 으로 거르거나, 라디오 제출을 별도 축으로.

### 2. 앱 계약판만 c11 로 올라가 c10 DB 에 대해 전건 426
- 파일: talk `src/계약판.js:16` vs `functions/events/index.ts:197-202` · `functions/tasks/index.ts:162-167`
- 서버는 선언판 > db판 이면 `426 CONTRACT_VER_UNSUPPORTED, retryable:false`. 이 커밋은 「⛔ DB 적용 0」이라 적으면서 클라이언트 상수를 c11 로 올렸다 — **클라이언트가 서버보다 앞서는 첫 사례.** HEAD 로 Expo 빌드하면 과제 조회·사건 업로드 전건 실패.
- 방향: 앱 상수 승격을 마이그 적용 뒤 별도 커밋으로 떼거나, 순서 제약(마이그 먼저→앱 빌드)을 못박는다. ⚠ `tests/계약.test.js` 가 상수=계약 정본 lockstep 을 강제하므로 되돌리기만으로는 안 풀린다 — 검사 축(「계약 정본」 vs 「DB 착지 판」)의 판정이 필요.

### 3. 원장 jsonb 를 무검증으로 계약 칸에 복사한다 — 검증기는 그 결과를 거절한다
- 파일: talk `lib/라디오승격.js:181-195` + `functions/radio-promote/index.ts:145-175`(insert 경로에 `검증` 호출 0)
- 재현: `task_snapshot.보기=['이','에','를']`(문자열 배열) → 계획 통과, `selected_option:"undefined"`. 중복 id 보기 → 자리 2인데 첫째 보기 id. 둘 다 검증기(주체 server)는 거절하는 모양인데 Fn 은 검증을 안 지난다.
- quiz_round.task_snapshot 생산자(P3)가 아직 없어 모양 보증이 0 — learning_events 불변이라 오염은 정정 불가.
- 방향: 승격계획에서 보기 원소 모양·중복 id 검사(`스냅샷불량`) 또는 Fn insert 전 `검증` 통과.

### 4. `limit 5000` 굶주림 — 창이 차면 새 메시지는 영원히 승격되지 않는다
- 파일: talk `functions/radio-promote/index.ts:64-71`(order by sent_at asc limit 5000 · 되돌아보기 7일)
- 7일 창 명령이 5000건을 넘으면 매 실행이 가장 오래된 5000건만 집는다 → 전부 `이미승격`, 승격 0. 새 메시지는 창을 벗어날 때까지 안 집히고 벗어나면 소급 절차가 없어 **영구 손실**. 잘림 신호도 응답에 없다.
- 방향: 커서(마지막 sent_at+message_id) 진행 또는 잘림 플래그+「제외 100%=이미승격」 경고.

### 5. 시점 조회 넷 중 둘이 정렬 없이 「입력의 마지막 행」을 고른다
- 파일: talk `lib/라디오승격.js:95-103`(활성링크) · `:124-130`(카드찾기) — filter 후 [length-1], 정렬 없음(라운드찾기·유효동의는 정렬함). Fn 질의에도 order by 없음.
- 재현: 겹치는 링크 이력(해제 미래시각 예약·confirmed_at 백데이트 정정이면 겹침 발생 — 부분 유일 인덱스는 못 막음) → 행 순서에 따라 **사건이 남의 학생에게 붙고 그 학생의 consent·급수로 저장**. 카드도 동일.
- 방향: confirmed_at·shown_from 내림차순 정렬 후 첫 행 + 겹침 건수 분모 노출.

### 6. 원신호 상한 500 의 전제가 깨졌는데 상한을 안 봤다
- 파일: talk `functions/deliver/index.ts:86-88, 279`(order by occurred_at desc limit 500) + `lib/학습자상태.js` 쓰는사건 +3
- 공개 채팅 명령엔 하루 상한이 없다(선호·정서·제출 무제한). `!빗소리` 하루 20번 → 30일 600행 → 오래된 task.assigned·submission 이 잘려 리듬 분모가 조용히 준다. 재현: preference 50행만으로 estimator_confidence 0.83.
- 방향: 사건 종류별 상한/창 분리 + 승격기에 명령별 하루 접기(선호·정서는 하루 마지막 선언).

### 7. 승격기를 부르는 것이 없다 — 배포해도 도달 0 유지
- 파일: talk `functions/radio-promote/index.ts:8-9`(「pg_cron 이 부른다」) vs `migrations/20260809070000_cron_c10.sql:71-104`(잡 셋뿐 — radio-promote 없음)
- cron 조각도 배포 목록의 스케줄 항목도 없다. 다 배포해도 아무도 안 불러 승격 0행 = 「라디오 활동 없음」과 구분 불가(조용한 0).
- 방향: cron 조각(또는 배포 절차 명시 단계)을 같은 트랙에서.

## 경미

8. **동의 술어 다섯 번째 사본** — `라디오승격.js:105-115` vs `lib/동의게이트.js`(「새 자리는 둘 중 하나를 고른다」). 오늘은 `그때유효` 와 뜻 동일(대조함)이나 텍스트 대조 회귀가 이 사본을 안 봐 갈라지는 날 조용함.
9. **교정 큐 상한 없음 + 주석 거짓** — `functions/correct` 대기조건은 event_type+body 뿐, task_type/format 안 봄. 「형식으로 거른다」는 거짓(퀴즈가 안 걸리는 건 event_type 덕). `!출석` 30번=벤더 과금 30건. 퀴즈 submissions 의 pipeline_jobs 는 소비자 없이 queued 영구 적재.
10. **집단 되돌림 호출자·입력 생산자 0** — `라디오편성.js` 호출 0건. 입력 「오답」은 같은 커밋이 「정오 결합 못 한다」로 못박은 값이라 지금 아무도 못 만든다. 「되돌림 경로 0」은 실질 그대로.
11. **correlation 회귀가 몽골 날짜 경계를 안 잼** — 픽스처 셋 다 UTC 날짜로 잘라도 같은 판정. 경계 쌍(15:59Z vs 16:01Z)이 없다. 주석 「다음날」도 실제 이틀 뒤.
12. **CHECK 이름 c10 사본 둘 잔존** — `lib/검수확정.js:19` · `src/검수API.js:32` 가 `pipeline_jobs_discard_reason_c10` 을 정본으로 지목(적용 후 없는 이름).
13. **제외 사유 command_kind '답' 하드코딩** — `index.ts:128`.
14. **5000건 단일 tx** — 백로그 크면 Edge 시간 초과로 매 실행 0건 고착 가능. 멱등이라 청크 커밋이 자연스럽다.
15. **계약판 조회 취약 셋** — 0행 TypeError · 접미 없는 조각이 뒤에 붙으면 500(기존 Fn 공통) · schema_ver 가 코드 판이 아니라 DB 최신 판.

## 공격 실패(검증됐고 문제 없음)
CHECK 14개 drop/add 짝 전수 대조 ✓ · 마이그 골격(checksum·base·재실행·시드 conflict) ✓ · 확인_적용후상태 14개 `_c11` ✓ · SQL 인젝션·캐스트·배열 파라미터·17열 순서 ✓ · 라운드짝 키 형 갈림 없음 ✓ · 마감·경계 판정 ✓ · 결정적 uuid 모양 ✓ · 계약↔검증기 정합(3필수·affect_kind·position↔⑦ 구조적 일치) ✓ · 회귀가 버그 존치를 요구하지 않음 ✓ · payload 에 channel_id/표시명 0 ✓ · 배포 등록(디렉터리 스캔)·동봉 그래프 ✓.

## 검증 못 한 층(정직)
Fn 본체 테스트 0줄(DB 왕복으로만 재짐 — §9 승격 왕복 도구 부재) · 마이그 실 DB 미적용(텍스트 층 대조).

---

## 처분 (수리 세션 `local_3d3af5e4` · 2026-08-12 · talk `8f0e35f` 한 커밋 · 스위트 1441/1441)

| # | 처분 | 내용 |
|---|---|---|
| 1 | ✅수리 | 통로 축의 첫 소비자를 세웠다 — 정본 `lib/라디오태스크.js`(3종) 신설, `학습자상태.v6` 리듬·끈기가 그 목록을 **뺀다**(앱 신호 보존은 회귀 양방향), deliver 원신호에 `task_type` 동봉. 라디오 행 자체는 자기 축(선호·정서·자기인식)으로 이미 닿으므로 별도 축은 안 세웠다 |
| 2 | ✅수리 | 검사 축 판정 = **「DB 착지판」**. `supabase/DB착지판.json` 신설(c10), `src/계약판.js` c10 복귀, `tests/계약.test.js` 를 「상수=착지판 ≤ 정본판, 틈은 대기 마이그 조각이 설명」으로 재작성. 올리는 커밋 = 마이그를 부은 확인 커밋(cron 조각 확인 ③이 절차를 지목). 낡는 방향은 안전(앱이 옛 판 선언=서버 수용) |
| 3 | ✅수리 | 승격계획이 검증기 ⑦의 `보기id들` 을 **그대로** 재사용(export 추가) — 문자열 배열·중복 id·label 누락 = `스냅샷불량` 제외. 회귀 3모양 |
| 4 | ✅수리 | (sent_at, message_id) 키셋 커서 + 배치 1000·실행당 20배치 + **배치별 tx** + 응답 `잘림`·`배치` 명시. 커서는 저장 안 함(멱등이 대신) |
| 5 | ✅수리 | 링크 겹침 = 고르지 않고 `링크겹침` 제외(오귀속=남의 학생 consent·급수로 소급 불가 — 정렬로 하나를 «고르는» 것도 넘겨짚기라 제외가 옳다고 판정). 카드 = shown_from 최근 우선 정렬(그때 화면에 있던 것). 입력 순서 무관 회귀 |
| 6 | ✅수리 | 두 겹: ㉠승격기 하루 접기 — 선호·정서는 학생×몽골날짜×갈래(차원/affect_kind) **첫 선언만**(`하루접힘` · DB 기존 행 `이미하루키` 대조 · 키 조립은 `선호정서키` 하나). ⚠리뷰 방향은 「마지막 선언」이었으나 **첫 선언으로 바꿔 채택** — 마지막은 열린 날 동안 미확정이라 재실행마다 판정이 뒤집히거나 하루 지연이 필요하고, 첫 선언은 즉시·결정적·멱등 재실행 안정. 원장 보존이라 소급 재산출 가능 ㉡deliver 원신호 상한을 전체 500→**종별 150**(row_number partition — 한 종의 폭주가 다른 종을 못 민다) |
| 7 | ✅수리 | `20260812130000_cron_radio_c11.sql` — `radio-promote-hourly` 매시 21분(vault 두 시크릿·리허설 제외 정책 cron_c10 그대로). 확인 블록·CHECK 기대 목록 승계(마지막 조각 의무) · 합본 재생성 |
| 8 | ✅수리 | `동의게이트.그때유효평가`(평가형) 신설 — 승격기 메모리 필터가 그것을 쓴다. 의미 사례 5종 회귀(사후 동의·기철회·미래 예약 철회) |
| 9 | ◐부분 | radio-promote 머리말의 거짓 주석(「형식으로 거른다」)을 진실로 정정(실제 차단자 = correct 대기 술어의 event_type). **잔여 = 퀴즈 submissions 의 pipeline_jobs queued 영구 적재** — 잡 생성 트리거(c7)·폐기 어휘(CHECK)는 검수 계약 소유라 이 트랙이 손대지 않는다. 검수 큐 뷰가 텍스트 제출을 어떻게 그리나와 한 자리(반박 W 기존 지적)로 검수 콘솔 트랙 몫 |
| 10 | ✅인정·코드 무변경 | 「되돌림 경로 0은 실질 그대로」 인정 — `라디오편성.js` 는 계산기까지고 호출자는 P3 라운드 생성기 몫(파일 머리말에 이미 명시). 입력 「오답」은 정오 결합 판(§9 조인)이 서야 생긴다. 지어낼 호출자가 없으므로 무변경, 과대 서술은 메모리·대기열에서 정정 |
| 11 | ✅수리 | correlation 회귀에 몽골 경계쌍 2방향(15:59Z/16:01Z 같은 UTC 날짜 · 전날 16:01Z 같은 UB 날짜) — UTC 절단 구현이 양방향에서 죽는다 |
| 12 | ✅수리 | `tests/폐기사유.test.js` 제약 이름을 합본 최고판 파생으로(손값 c10 제거) · 산문 사본 둘(`검수확정.js`·`검수API.js`) 정정. 런타임은 원래 `like 'c%'` 패턴이라 무사 |
| 13 | ✅수리 | Fn 의 `command_kind:'답'` 손값 자리 자체를 제거(제외 분모는 사유 합만 나감) · 계획 행이 `command_kind` 실값을 든다(필요해지는 날의 재료) |
| 14 | ✅수리 | =④ 배치별 tx |
| 15 | ◐부분 | 계약판 조회 0행 가드 추가. 「접미 없는 조각이 붙으면 500」은 기존 회귀 `tests/마이그레이션이름.test.js` 가 파일명 규칙으로 이미 막는다(합본 도구도 거절). schema_ver=DB 최신 판은 설계 의도라 무변경 |

▶ 다음 = 이 수리 커밋(`8f0e35f`)에 대한 **분리 재반박 1패스**(컨텍스트 독립) — 결과는 이 파일 아래에 잇는다.
