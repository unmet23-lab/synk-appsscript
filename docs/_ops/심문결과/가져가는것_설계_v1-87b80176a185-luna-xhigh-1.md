총 21건이다.

1. [P0] 원본 저장소를 잘못 골랐다 — `docs/가져가는것_설계_v1.md:135-147, 369-375`는 `portfolio_moments`를 시트에만 만들지만, `docs/제품방향.md:44-47`은 Supabase/Postgres를 핵심 원본으로 확정한다. 시트 행은 `engine.learning_events`나 학습자 모델로 들어가는 계약·어댑터가 없어 엔진 원본이 아니다. 재현 입력: 순간 폴더를 만들고 `momentSweep_()`를 실행하면 시트만 늘고 Postgres·엔진에는 사건이 0건이다.

2. [P0] 최소 원본 이벤트 필드가 없다 — `docs/가져가는것_설계_v1.md:139-147`에는 `session_id`, `occurred_at`, `ingested_at`, `schema_ver`, `correlation_id`, 복합 `idempotency_key`가 없다. `SYNK-talk/docs/L0_데이터계약.md:42-48`의 필수 계약을 전부 놓쳤고, `담긴날` 하나로 발생 시각과 적재 시각을 구분하지 못한다. 재현 입력: 3월 14일 사진을 3월 15일 올리면 실제 발생일과 적재일을 재구성할 수 없다.

3. [P0] `class_snapshot` 하나로는 출석 조인이 성립하지 않는다 — `docs/가져가는것_설계_v1.md:155-167`은 반 스냅샷만 추가하지만, 실제 `엔진_셋업확장.js:142`, `엔진_운영배치.js:1842-1863`은 여전히 네 칸만 읽고 같은 학생·같은 날 출석을 하나로 제거한다. 같은 반의 보강·복수 차시·다른 세션을 구분할 사건 키가 없다. 재현 입력: 같은 학생이 같은 날 정규 수업과 보강에 참석하면 두 순간이 어느 출석에 속하는지 판정할 수 없다.

4. [P0] 미디어 목록을 단일 파일 칸으로 축소했다 — `docs/가져가는것_설계_v1.md:145, 220-223, 244-260`은 사진 두 장과 영상을 예시로 들면서 저장 칸은 `사진_file_id` 하나뿐이다. 영상 종류·순서·원본 참조·복수 파일 목록이 없어 한 순간의 미디어 관계가 소실된다. 재현 입력: 한 폴더에 JPG 두 장과 MP4 하나를 넣으면 한 파일만 남거나 순간 행이 세 번 생긴다.

5. [P0] 시즌 밖 사건의 식별 키가 없다 — `docs/가져가는것_설계_v1.md:141, 201`은 시즌을 날짜에서 유도하지만 12월 파일럿과 2월 체험은 시즌 밖이라고 명시한다. 이때 `시즌`은 빈 값 또는 임의값이 되고 파일럿·체험 사건을 정식 학습 이력과 연결할 비시즌 키가 없다. 재현 입력: 2027-02-11 체험 첫날 순간을 적재하면 어느 코호트·경험 회차·시작 사건인지 복원할 수 없다.

6. [P0] `crew_projects` 자동 파생 주장이 거짓이다 — `docs/가져가는것_설계_v1.md:241-242, 395`는 폴더와 출석만으로 프로젝트명·결과물 링크·공개일·참여크루를 만든다고 하지만, 폴더 문법에는 그 필드가 없다. 실제 `엔진_셋업확장.js:170`의 9칸은 수동 입력용이며 출석은 참석 사실이지 프로젝트 참여·완성 사실이 아니다. 재현 입력: 한 반에서 두 프로젝트를 만들거나 일부 학생만 참여하면 어느 프로젝트 행과 참여자에 귀속할 수 없다.

7. [P0] 멱등성과 재시도 계약이 없다 — `docs/가져가는것_설계_v1.md:135, 145, 231-237, 372`는 `file_id` 중복 제거만 말하고 원본 폴더 ID, 처리 상태, 유일 제약, 락, 복사 전후 매핑을 정의하지 않는다. 동시에 두 트리거가 돌거나 실패 후 학원 계정 폴더로 복사하면 새 파일 ID가 생겨 중복을 막을 기준이 사라진다. 재현 입력: 같은 `momentSweep_()`를 동시에 두 번 실행하면 동일 순간이 두 행으로 append된다.

8. [P1] 생산자에서 화면 렌더링으로만 끝난다 — `docs/가져가는것_설계_v1.md:372-376`의 소비자는 BY·BX·원장 알림뿐이고, 엔진 이벤트·어휘 지도·학습자 모델·평가셋 소비자가 없다. `docs/엔진도달_설계.md:329-345`와 `docs/코어엔진_설계.md:57-59` 기준에서 화면 문구는 엔진 도달이 아니다. 재현 입력: 순간 행을 넣고 제안된 소비자를 실행해도 학생 카드 문구만 바뀌고 이해 대장의 엔진 칸은 늘지 않는다.

9. [P0] 사람 판정을 자동 문구로 바꿨다 — `docs/가져가는것_설계_v1.md:555-565`는 증서 세 줄 중 하나를 “나침반 목표 달성 여부”로 자동 채운다. 판단 정본은 목표에 가까워졌는지를 시즌 회고에서 사람이 판정한다고 못 박았으므로, `season_goal`의 존재만으로 달성 여부를 출력하면 의미 계약 위반이다. 재현 입력: `season_goal`은 입력됐지만 시즌 회고 판정이 비어 있는 학생에게 증서를 굽는다.

10. [P0] “보존”이라고 쓴 나침반 값이 수정 가능하다 — `docs/가져가는것_설계_v1.md:1005-1016`은 유일키와 삭제 방지만 근거로 첫 답을 보존한다고 주장한다. 실제 `SYNK-talk/supabase/migrations/20260812140000_season_c11.sql:158-166`은 개서를 허용하며, 확정 후 스냅샷은 별도 회고 계층의 일이다. 재현 입력: 촉진 세션에서 첫 시즌 답을 수정한 뒤 기록장을 다시 굽으면 “첫 답 그대로”가 바뀐다.

11. [P0] 졸업 원본 파일 약속을 이행할 저장 구조가 없다 — `docs/가져가는것_설계_v1.md:250-260, 297-305`는 시스템에는 압축본만 두고 원본은 강사 휴대폰에 남긴다고 하면서 졸업 때 원본 파일을 직접 건넨다고 한다. 중앙 원본 목록·보존 상태·누락 검증이 없어 휴대폰 분실이나 파일 교체 시 약속을 판정할 수 없다. 재현 입력: 졸업 전 강사 휴대폰의 원본이 사라지면 기록장과 직접 전달 파일 모두 복구할 수 없다.

12. [P0] 완결 게이트의 네 칸을 기록하지 않는다 — 판단 정본은 모였나·닿았나·늘었나·누구에게를 각각 `✓/✗`로 보드에 적으라고 했지만, `docs/가져가는것_설계_v1.md:151, 1060-1064`에는 구현 계획과 요약만 있다. 일부만 구현돼도 문서상 완료 여부를 판정할 상태값이 없다. 재현 입력: 시트는 생겼지만 엔진 소비자가 없는 커밋을 보면 “시트 1장 추가”와 “완결”을 구분할 수 없다.

13. [P0] 이미 확인한 거짓말을 선택사항으로 남겼다 — `docs/가져가는것_설계_v1.md:658-673, 945`는 지급 후에도 “곧 전달”이 계속 뜨는 결함을 확인하고도 두 소비자를 같이 고칠지 유호님 판단으로 남긴다. `jacket_grants.지급상태=지급완료`를 읽는 코드가 0곳이라는 사실도 문서 안에 있다. 재현 입력: 지급상태를 완료로 바꾸고 목표 카드를 열면 학생에게 계속 “곧 전달해 드려요”가 표시된다.

14. [P1] 닫힌 범위를 다시 열었다 — `docs/가져가는것_설계_v1.md:940-946`은 이미 거절된 앱 상시 포트폴리오와 Glide 경로를 다시 선택지로 올린다. `docs/제품방향.md:44-47`은 Glide 폐기와 Expo 전환을 확정했으므로 이 문서는 확정 범위를 보존하지 않는다. 재현 입력: §14-⑦의 “둘 다 둔다”를 택하면 폴더와 `crew_projects`가 동시에 기록되는 이중 경로가 생긴다.

15. [P1] 학생 접점이 기록 알림에 머물러 기본 게이트를 못 지난다 — `docs/가져가는것_설계_v1.md:271-289, 372-376`의 학생 경험은 최근 순간 한 줄과 “오늘 하나 담겼어”라는 일회성 알림뿐이다. 학생이 무엇을 말하거나 쓰는지, 자기 목표에 어떻게 도움이 되는지, 왜 다시 열어야 하는지에 대한 행동 루프가 없다. 재현 입력: 처음 접한 학생이 BY/BX를 보면 포트폴리오의 정체와 다음 행동을 알 수 없고 한국어 산출도 발생하지 않는다.

16. [P1] `contents_순간.js`는 학습 콘텐츠 계약이 아니다 — `docs/가져가는것_설계_v1.md:376`은 종류별 문구 뱅크만 요구하고 수준·목표·학생 행동·한국어 산출 필드를 정의하지 않는다. 따라서 같은 순간도 단순 축하 문구로 끝나며 실제 사용 한국어와 신선함을 검증할 수 없다. 재현 입력: 교사가 몽골어 또는 의미 없는 한 줄을 넣어도 어떤 학습 문구·연습으로 변환할지 계약상 결정할 수 없다.

17. [P1] 채움 사다리의 “개인 재료” 전제가 거짓이다 — `docs/가져가는것_설계_v1.md:324-332`는 첨삭·나침반·출석·업적·카드가 결석과 무관하게 항상 있다고 가정한다. 실제로 숙제 제출·업적·카드·시즌 진입은 조건부라 고유 재료가 없는 학생이 생긴다. 재현 입력: 체험 후 등록하지 않았거나 숙제·업적이 없는 학생의 시즌 두 쪽을 굽는다.

18. [P1] 공식 TOPIK과 모의 급수를 섞었다 — `docs/가져가는것_설계_v1.md:311, 531-539`는 `TOPIK·모의`를 한 급수 재료로 묶고 표지에는 “마지막 급수”만 쓴다. 닫힌 결정인 공식 TOPIK 4급 게이트와 모의 점수를 구분하는 필드·문안 규칙이 없다. 재현 입력: `academic_log`의 최근 값이 모의 4급이면 기록장 표지에 공식 4급처럼 출력된다.

19. [P1] 비교 금지가 동적 산출물에서는 무방비다 — `docs/가져가는것_설계_v1.md:545, 565, 844-848`은 정적 금칙어 검사만 제안한다. Apps Script·시트·폴더에서 들어오는 `league_history`, 업적, 교사 한 줄은 `발표물린트.js`가 검사하지 않는다. 재현 입력: 업적 원문에 “TOP10”을 넣고 기록장을 굽으면 비교 문구가 그대로 학생 산출물에 들어간다.

20. [P1] 월간 봉투는 판형이 아니라 희망사항이다 — `docs/가져가는것_설계_v1.md:622-646, 794-803`은 A4 한 장에 네 기능을 넣는다고 하지만 접힘 위치·비인쇄 여백·학생별 배치·양면 정합을 정의하지 않았다. `@page margin:0`은 실제 프린터의 비인쇄 영역을 제거하지 않으며 프린터 사양도 아직 미측정이다. 재현 입력: 현재 `printMonthlyCards()`를 그대로 실행해 일반 프린터로 출력하면 사진·리포트·절취선이 잘리는지 자동 판정할 수 없다.

21. [P1] 실패 경로가 실제 복구 경로가 아니다 — `docs/가져가는것_설계_v1.md:231-239`는 파싱 실패를 “미분류”로 알리지만 `portfolio_moments` 스키마에는 원본 폴더 ID·오류 사유·처리 상태·재시도 시각이 없다. 폴더를 고친 뒤 어떤 행을 갱신해야 하는지 알 수 없어 재시도는 중복 또는 누락으로 끝난다. 재현 입력: 한 줄이 없는 폴더를 먼저 스윕한 뒤 이름을 고쳐 다시 스윕한다.

22. [P1] 일정 기준이 현재 날짜와 이미 어긋난다 — `docs/가져가는것_설계_v1.md:858-899`은 2026-08-26 기준 D-N을 유지하면서 2026-09-02 심문 결과만 덧붙였다. 2027-01-29는 9월 2일 기준 D-149이지 D-156이 아니며, 파일럿 시작일도 12-01과 12-07~18 중 하나로 닫히지 않았다. 재현 입력: 오늘 기준 D-156을 남은 작업일로 계산하면 실제 여유를 7일 과대평가한다.

23. [P1] 몽골어 미검수 상태로 학생·학부모 물건을 동결하려 한다 — `docs/가져가는것_설계_v1.md:397, 541, 785-797, 897`은 mn 문안을 “검수 대기”로 비워 두고 검수자도 0명이라고 적는다. 그런데 1월 5일·2월 11일에 필요한 인쇄물의 발주·조판 순서는 이미 확정된 것처럼 작성되어 빈 문안과 검수 완료를 구분하지 않는다. 재현 입력: `contents_증서.js`의 mn 칸과 월간 봉투 문안을 실제 인쇄물로 굽는다.

읽은 파일: `docs/가져가는것_설계_v1.md` 전문, `docs/제품방향.md`, `docs/엔진도달_설계.md`, `docs/코어엔진_설계.md`, `엔진_셋업확장.js`, `엔진_운영배치.js`, `Code.js` 관련 구간, `SYNK-talk/docs/L0_데이터계약.md`, `SYNK-talk/supabase/migrations/20260812140000_season_c11.sql`. 개인정보·동의·권한·보안 축은 지적하지 않았다. 문서가 인용한 `공간설계`, `강사채용_부트캠프`, `파일럿_체험2주`, `몽골어_검수_발주서`, `개원재무`, `GLIDE_조립가이드`, `마케팅_개편` 전문은 이번 대조에서 열지 않았다.

이 문서보다 나은 대안 구조가 있으면 1개: `Supabase 불변 moment_event 원본 → 시트·기록장용 버전된 파생 뷰 → 발행·전달 사건`의 3층 구조로 분리한다. 원본에는 발생일·세션·미디어 목록·원본 참조·멱등키를 넣고, 각 파생 뷰에 모임/도달/성장/대상을 별도 상태로 기록해야 한다.