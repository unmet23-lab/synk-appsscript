판정요약: P0 3 · P1 6 · P2 5

<!-- 대상 docs/상태기반_과제선택_설계.md v5.6 · 대상지문 4a98a0a91252 · 범위 3 · 회차 10
     모델 claude-fable-5 · 심문자 에이전트 정의로 돎(폴백 아님) · 2026-08-14 -->

실측 범위: 대상 문서 v5.6 전문 + `C:\Users\q1212\Documents\SYNK-talk`의 `src\교정API.js`·`lib\성과회수.js`·`lib\게임배정.js`·`lib\학습자상태.js`·`lib\오늘과제.js`·`supabase\functions\deliver\index.ts`·`tasks\index.ts`·`supabase\L0_스키마.sql`·`migrations\20260812120000_engine_c11.sql`·`20260809070000_cron_c10.sql`·`계약\수집_교정_계약.json`·`tests\오늘과제.test.js`·`docs\C0_API계약.md`, appsscript의 `tools\계약동기화.js`·`docs\SYNK_철학.md`를 직접 열었다. 문서가 인용한 실측 다수(게임요일 [2,5]·grammar 18/전체 30·`35 16` cron·`as_of` 물리 0건·추정메타 4열·c11·삼항 사슬 degraded 갈래·결정지문 `tests/오늘과제.test.js:1039~1041` 등)는 실물과 일치함을 확인했다 — 아래 지적은 그 위에서 남은 것들이다.

---

**[P0] 마감 게이트(B3)가 구제 경로 전체를 벽돌로 만든다 — 배치 전멸일에 학생이 하루 종일 빈손이 된다** — §3-5-b ㉡(B3)·㉤ 끝줄("_mode='정상'은 마감 시각 이후 거절") vs §3-1·㉨(B4·B7·B8)·§12-11. 마감은 배정일 아침 06:00 UB이고 학생이 앱을 여는 시간대는 전부 그 «뒤»인데, 구제는 `jobs_claim`(B3: 마감 뒤 0행)과 `jobs_finalize('정상')`(마감 뒤 거절) 둘 다에 막혀 폴백을 착지시킬 수 없다 — ㉨가 지어진 바로 그 날(오케스트레이터 전멸일)과 B4의 적재실패 재적재(방아쇠=학생 접속)가 정확히 이 시간대에 온다. 결과: 그 학생은 `task.assigned`조차 0인 채(그날 분모 행이 영구 부재 — 소급 불가) '오류' 카드만 보며, 현행 라이브(`tasks/index.ts:212`→동기 조립)보다 명백히 후퇴한다. §12-11의 "job 0건에서 구제를 부르면 폴백이 착지한다" 픽스처는 시계를 마감 전으로 고정해야만 통과하므로 픽스처 초록·운영 사망이 같은 얼굴이 된다(렌즈 6) — B3 집행이 자기 형제 절(구제 계약 전체)을 안 걷었다.

**[P0] A8 「상태 메타 draft 동결」이 degraded를 포함해 — 성공 또는 폴백 중 한쪽 착지가 원리상 예외로 죽는다** — §3-5-b ㉠ A8 vs ㉤ ④(A10 갈래표)·§5-4·봉투 전량 표. A8은 `degraded`를 조립 시점에 draft에 굳히고 "착지 봉투의 상태 메타는 어느 모드든 draft 값 하나다(워커도 재계산하지 않는다)"라 못박았는데, ㉤ ④는 호출자 값이 갈래표(`성공`→false · 실패·마감·구제→true)와 다르면 예외로 죽이고, A10은 outcome에서의 파생을 금지한다. draft는 outcome을 모르는 시점의 한 값이므로 — true로 굳히면 성공 착지가, false로 굳히면 모든 폴백 착지가 ④에서 예외다. 세 규칙(A8 동결·A10 파생 금지·④ 대조)을 동시에 지키며 `degraded=false`인 성공 행을 만들 합법 경로가 문서 안에 없다 — 주 경로(성공)가 동결된 자기모순 위에 선다.

**[P0] F5의 corrections 공급 통로(`parent_event_id`)는 실물 교정 사건이 싣지 않는 키다 — 조인이 영원히 0행이다** — §6-0 H3 F5 표(통로 행). 실측: `correction.viewed`·`correction.responded`의 유일한 생산자 `SYNK-talk\src\교정API.js`의 봉투(`:90~102`)는 `idempotency_key`·`event_type`·`occurred_at`·`correlation_id`·`level_snapshot`·`correction_id`·`payload`뿐이고 파일 전체에 `parent_event_id`가 0회다(`functions/corrections/index.ts`도 0회 · 계약 `:156`의 그 칸 뜻은 「대화 선행 턴」·`:116` 미구현 목록 등재). F5가 인용한 실측(`성과회수.js:36~37`)은 `content.viewed` 통로의 것을 `correction.*`에 옮겨 적은 것으로, 생산자를 안 열었다 — 그 주석이 경고한 바로 그 사고(「관측이 영원히 0건·증상은 아무도 안 본다 하나뿐」)가 corrections 쪽에서 동결된다. 실제 사슬은 `correction_id`→corrections 행→원 제출 사건→배정(event_id)의 다단 조인이라, 이 통로대로 구현하면 「닿았나」 배선이 서류상으로만 서고 F5 자신의 「운영 생산자 실재 눈금」(호출부가 싣는가만 본다)으로는 키 불일치를 못 잡는다.

**[P1] §12-19가 같은 판(v5.6)의 §8-B 개정에서 미수거 — 「비교 6칸·실행판 9칸」이 세 자리에 그대로다** — §12-19(첫 불릿 "실행판 9칸"·"옛 초록 재사용 차단: 비교 6칸"·I2 항 "현행 6칸") vs §8-B(차단기 비교 7칸·동봉 10칸·"비교 7+존재 2+기록 1"). E1이 `quality_ver`를 더하며 §8-B 세 자리는 고쳤지만 눈금 절(§12-19)은 안 걷었고, §12-21의 기계 대조는 "세 칸(차단기·동봉·이 산술)" — 전부 §8-B 안 — 만 겨눠 §12-19는 대조 밖이다. 구현자가 눈금 절을 따르면 quality_ver 차단이 조용히 부재해, E1이 닫으려던 「검문을 느슨하게 고친 날 옛 초록이 산다」가 눈금 층에서 되살아난다. 문서 스스로 "이 산술은 판마다 갈렸다(네 번째)"라 적은 그 자리의 다섯 번째 갈림이며, 이번엔 한 판 안에서 났다.

**[P1] §8-B 시험지에 생성 절차가 «두 벌»이다 — E4·F2·H3·§12-19가 「균등 5」 전제인 채 E5-ⓐ가 그 전제를 걷었다** — §8-B E4 절차 2("8칸에 칸당 5벌") vs E5-ⓐ("재적 급수 분포 우선 배분·칸당 최소 3"). 같은 판이 두 절차를 나란히 신설하며 앞 문장을 안 걷어, 두 사람이 같은 이름·다른 시험지를 만든다(E4가 막으려던 바로 그것 — F281 축). goal 3·2 번갈아(F2)와 기술 층화 산술(18×2=36≤40·서로 다른 급수 칸)은 칸 크기 5에서만 정의되고 가변 칸에서 재계산이 없으며, §12-19 표본 재현 픽스처도 "칸당 5개(N=40)"로 못박혀 세 번째 사본이다. 덧붙여 배분 재료인 「enrolled_count를 낸 그 명단의 급수 구성」은 어느 표에도 저장되지 않아(수만 굳는다) 유도 재현이 안 된다. E2의 기계 계약 예시 「⑦: n=20」도 같은 판의 E6("n=40이 된다")과 정면이다.

**[P1] 감시 ④(열린 시도)의 판정식이 미규정 칸 위에 선다 — 3값 구멍을 자기 신설 감시에 냈다** — §3-2-a B10 표 ④. 적색 조건이 "그 job의 `lease_until`이 지난 행"인데, 전이표는 회수·반납에서만 `lease_until` 비움을 규정하고 착지·마감폴백 «뒤»의 처분은 미규정이다. 회수를 지났거나(비움→null) 종료 시 비우는 합법 구현에서 null 비교는 UNKNOWN이라 그 행이 안 세어진다 — 이 문서가 A2·A4·S-9에서 세 번 기소한 바로 그 형태다. 새는 대상이 B10의 표적 그 자체(응답 뒤 죽어 회수된 job의 버려진 호출 — 돈이 나간 자리)라, 감시가 자기 대상의 부재를 초록으로 읽는 ①축의 재발이다.

**[P1] ㉨의 실행판 「DB가 현행 판을 스스로 읽는다」(B6)는 실행 불가능한 규격이다** — §3-5-b ㉨(B6) vs §5-1(재료 12 중 6개=JS 파일 내용 해시)·§16-1 #5(판독기 실물 0 자인). `policy_ver` 재료 1·2·7·8·11·12는 저장소 JS 파일의 내용 해시인데 Postgres RPC가 저장소 파일을 읽을 길이 없고(읽을 수 있는 건 재료 10의 `pg_get_functiondef`뿐), 현행 판을 담는 DB 표·설정의 신설은 이 문서가 금지한다. 그래서 B6이 `_batch` 인자를 걷으며 세운 대안은 원천이 없고, 구현은 호출자 산출로 되돌아가거나(걷은 논거와 정면) 임의 저장 통로를 발명하게 된다 — §12-25가 재는 「설계문이 자기 자신과 맞나」의 위반이다.

**[P1] 기술 회전의 「안 겨냥 우선 × 시작 위치」 결합 규칙이 비결정이다** — §6-0 표(우선순위·동률 처리·개수). 안 겨냥 후보가 3개 이상일 때 «어느 2개»인지: ⓐ정렬된 18열 위에서 시작 위치부터 순회하며 겨냥분을 건너뛰기, ⓑ안 겨냥 부분집합을 만들어 그 크기로 mod — 둘 다 표의 문장(우선·결정적·개수)을 만족하면서 다른 기술을 낸다. D6이 스스로 적은 「«해시»라고만 적으면 구현이 각자 정하고 첫 처치 의도는 append-only 사건에 남아 못 고친다」가 해시 아래 층(선택 함수)에 그대로 남았다. C1·C2로 목록의 앞단 결정성은 닫고 정작 선택 규칙 자체가 열려 있어, §8-B 층화 채움(회전 순서 그대로)과 운영 겨냥 이력이 구현자마다 갈린다.

**[P1] §12-13 격리 회귀의 허용 목록이 v5.6 구조와 안 맞는다 — 「과제생성.js·과제배치.js 둘뿐」** — §12-13·§3-5 셋째 불릿. v5.6에서 두 장부를 실제로 만지는 층은 deliver Edge Fn(오케스트레이터 — `jobs_load`·`jobs_finalize('적재')`), tasks 구제(`jobs_load_one`·`jobs_claim`), deliver-check SQL(4항), §12-8 진단, §8-B E5-ⓐ(batch_runs 명단)까지 늘었는데 목록은 v4 시절 그대로이고, `과제배치.js`라는 파일은 문서 어디에도 정의가 없다(오케스트레이터는 JS lib이 아니라 Edge Fn이다). 이 목록대로 회귀를 세우면 정당한 구현을 거절하거나 거절을 피해 목록을 넓히며 격리를 무너뜨린다 — 이 항이 스스로 인용한 F103 형이 목록 층에서 재발한다.

**[P2] B4의 재적재 전이가 상태 전이표에 미수거다** — §3-5-b 전이표의 `적재실패`→`대기` 행은 주체가 「구제 `jobs_load_one`」뿐인데, v5.6 B4는 같은 전이를 `jobs_load`(오케스트레이터 재실행)에도 열었다. §12-25 대조는 RPC 이름 실재만 보므로 이 누락을 못 잡고, 전이표를 정본으로 읽는 구현이 재실행 되살림을 거절할 수 있다.

**[P2] deliver-check 신기준의 활성 시작일 공백** — §3-2-a. 워커·마감 잡은 「활성 시작일부터 등록」이 명문인데 deliver-check의 기준 교체(4항)는 시작일 규정이 없어, 배포~활성 사이 매일 ②(batch_runs 행 0)가 거짓 적색이다 — C4가 스스로 인용한 「매일 빨간 화면은 곧 안 보게 된다」를 자기 손으로 만든다.

**[P2] `quality_ver` 산출 자리가 소비자와 안 맞는다** — §5-1-a. 산출을 "배치 시작 1회"로 못박았는데 쓰이는 곳은 오프라인 차단기 하나뿐이고 행·payload 저장은 금지라, 런타임 산출값은 소비자 0인 죽은 계산이다. 차단기(회귀)가 자체 계산하는 것인지 어딘가 저장된 값을 읽는 것인지 한 줄이 없다.

**[P2] `jobs_claim` 회전의 `hashtext`·`abs`** — §3-5-b ㉡. `hashtext`는 비문서화 내부 함수라 PG 판 간 안정성 보장이 없고 `abs(int4)`는 INT_MIN에서 예외다 — 같은 문서가 §6-0 회전에는 SHA-256·엔디안·BigInt까지 못박은 규율과 층이 갈린다(실사용 확률은 낮다).

**[P2] `중복창_일`(14)의 물리 원천이 미지정이다** — §3 ②'·§8·§6-0 세 절이 같은 상수를 쓰는데 정본 파일이 지목되지 않았다. 검문(재료 2)과 기술선택(재료 8)에 상수가 각자 서면 두 사본이 갈리는 날 창이 축마다 달라지고, 판지문은 어느 한쪽 해시만 오른다.

---

**대안 구조 1개**: **마감을 「벤더 마감」과 「배달 마감」 둘로 가른다.** B3가 막으려던 사고는 「마감 뒤 새 벤더 호출이 스윕과 경합」 하나뿐이므로 — `jobs_claim`·`jobs_finalize`의 마감 거절은 «벤더 경로»에만 걸고, 벤더 0이 계약인 구제(§3-1)와 적재실패 되살림에는 넷째 모드(`_mode='구제'` — 가드: attempts 0행 강제 + `event_draft`/신규 조립만 착지 가능 + outcome ∈ `구제경로`)를 연다. 이러면 P0 1번이 닫히고 B3의 원 목적(마감 뒤 벤더 호출 0)은 물리 가드로 오히려 더 강해지며, 새 표·새 칸 0으로 문서의 면적 원칙과도 양립한다.
