확인 불가 — 라이브 가동과 SYNK-talk의 L0·C0·M0·분업 원문은 확인하지 못했습니다. 아래는 제공 전문, 로컬 정본의 관련 절, 격리한 코드의 합성 재현에 근거한 지적입니다.

**[P0] 삶 이해의 도장이 검토한 판본에 묶이지 않는다** — §4-삶은 기존 부품과 별도 상수로 성립 조건을 충족할 수 있다고 결론내리지만, [엔진_수집.js:105](/C:/Users/q1212/Documents/SYNK-appsscript/엔진_수집.js:105)의 `노출카드_`는 상태 문자열만 비교합니다. 재현 조건은 초안 A를 승인한 뒤 재생성한 B가 같은 노출 상태를 이어받는 경우이며, 함수는 내용이 바뀐 사실을 판별할 수 없습니다. 승인한 출력의 판본·근거와 재생성 시 도장 무효화 규칙이 없으면, 강사가 보지 않은 삶 판정이 승인된 결과로 나갑니다.

**[P1] 시계열 보호가 원신호와 재계산 계약에 연결되지 않는다** — §2⑥·§3은 모델 변경을 곧 영구 왜곡으로 설명하고 출력 규격·앵커·추정 판 번호만 선행 조건으로 둡니다. 그러나 [코어엔진_설계.md:281](/C:/Users/q1212/Documents/SYNK-appsscript/docs/코어엔진_설계.md:281)는 근거 사건, 추정 출처, 유효기간을 요구하고 원신호로 상태를 재계산하도록 정했습니다. 같은 답안을 다른 판으로 재채점하는 경우를 넣으면, 후보 문서의 조건만으로는 원래 관측·당시 판정·새 판정을 연결할 수 없으며 `채점분류`·`오류태깅`에 이 규율을 적용하는 구체적 조건도 빠져 있습니다.

**[P1] 사건과 소비자를 적기만 해도 ‘닿았다’가 된다** — §4의 배선 판정은 실제 저장·자동 소비·이해 대장 갱신을 확인하지 않습니다. [tests/모델정책.test.js:748](/C:/Users/q1212/Documents/SYNK-appsscript/tests/모델정책.test.js:748)는 `남기는사건`과 `엔진소비자`가 참으로 평가되는지만 검사하므로, 둘에 각각 `"미정"`을 넣고 상태를 `"섰다"`로 바꿔도 해당 검사를 통과할 수 있습니다. 이 정의로 동결하면 판단 정본의 모였나·닿았나·늘었나·누구에게를 충족하지 않고도 배선 완료가 됩니다.

**[P1] 담당 지정과 성능 입증이 다시 같은 도장으로 섞였다** — §5는 시험지가 서기 전에는 손 도장을 올리지 말라고 하지만, §3은 `몽골어검문`·`채점분류`의 손을 지정했고 §7은 몽골어 성능을 아직 재지 않았다고 적습니다. 기계 정본도 [모델정책.js:1042](/C:/Users/q1212/Documents/SYNK-appsscript/tools/모델정책.js:1042)에서 채점 성능 근거를 철회한 상태입니다. 소유자가 담당을 정한 자리를 조회하는 것만으로 성능 검증까지 끝난 듯 읽히므로, 소유자 지정과 시험으로 입증된 적합성을 구분해야 합니다.

**[P1] 긴 작업 분류 규칙을 실행할 수 없다** — §2④의 “도구를 몇 번 부르나”는 횟수 질문인데 판정은 예·아니오를 요구하고, “한 번에 끝나나”에 예라고 답할수록 오래 붙드는 일로 분류됩니다. 환경을 바꾸고 한 번에 끝나는 작업은 오래 붙드는 일이 되지만, 같은 환경에서 반복하는 긴 작업은 분류가 불명확합니다. 여기에 대규모이면서 오래 읽힐 엔진 코드처럼 §3의 담당이 겹치는 입력을 넣으면, 어느 규칙이 우선하는지도 결정할 수 없습니다.

**[P1] 독립 검수자 계산이 전체 자리의 결과를 다루지 못한다** — §4는 모든 자리의 독립 검수자를 `검수자(지은쪽)`로 계산한다고 주장하지만, [모델정책.js:245](/C:/Users/q1212/Documents/SYNK-appsscript/tools/모델정책.js:245)의 표는 단일 작성 집안만 받고 주로 코드 검수 통로를 반환합니다. `우리 도구`·`바깥 도구` 또는 공동 제작 결과를 입력하면 적용할 작성자와 검수 통로가 정해지지 않습니다. 그림·소리·종이·합쳐진 답까지 검수자가 계산된다는 문서의 보장이 실물 함수보다 넓습니다.

**[P1] 시험지 목록을 자동 판정 장치로 취급한다** — §5에는 본보기와 경계 사례의 수만 있고, 새 출력의 채점 주체·항목별 실패 기준·판정불능 처리·검사기 자체의 검증 방법이 없습니다. 본보기와 다른 과제 형식이나 새 오류 유형을 입력해도, 기존 시험지를 통과했다는 사실만으로 실제 산출물을 통과시킬 수 있습니다. “한 번 만든다”를 유지하려면 이후 사례를 자동으로 회수하고 검사 범위 밖을 드러내는 경로가 필요한데 그 책임도 비어 있습니다.

**[P1] 몽골어의 반복 사람 검수가 ‘한 번’ 계산에서 사라졌다** — §5는 몽골어에서 사람 손이 시험지 작성 때만 든다고 적지만, [몽골어대조.js](/C:/Users/q1212/Documents/SYNK-appsscript/tools/몽골어대조.js)는 기계 검문에 통과한 대외 확정본도 사람 검수를 받도록 명시합니다. 새 대외 문안을 발행할 때마다 실물을 따르면 반복 검수가 발생하고, 후보 문서를 따르면 그 절차가 생략됩니다. 사람 손을 시험지로 옮겼다는 변경 설명이 실제 운영 절차와 맞지 않습니다.

**[P1] 무료 창구 규칙이 정본의 요청량 계산과 충돌한다** — §6은 몽골어 검문을 공짜 창구로 고정하지만, [모델정책.js:1032](/C:/Users/q1212/Documents/SYNK-appsscript/tools/모델정책.js:1032)는 검문 한 판을 무료 하루 몫 대비 **80/20요청**으로 기록하고 돈 열쇠가 필요하다고 계산합니다. 검문 한 판을 문서대로 보내면 해당 창구의 하루 몫을 넘습니다. 충전 결정을 재론할 문제가 아니라, 이미 확보한 통로를 문서가 잘못 안내하는 결함입니다.

**[P1] 학생 글 통합에 처리량과 장애 시 동작이 없다** — §2⑤·§8⑤는 학생 글을 한 손으로 모으면서 주간 몫의 절반이라는 제한만 제시합니다. 과제 생성·맞장구·되돌림말·검사 작업이 같은 기간에 몰리는 입력에 대해 우선순위, 허용 지연, 재시도, 대기 중 학생에게 보일 상태가 없습니다. 담당 통합 결정은 유지하더라도, 실제로 몇 건을 감당하고 한도에 닿았을 때 무엇이 일어나는지는 동결 전에 정해야 합니다.

**[P1] 뜻 대조는 큰 차이를 읽고도 ‘같다’를 반환한다** — §3의 `뜻대조`가 사용하는 [tools/lib/뜻대조.js:122](/C:/Users/q1212/Documents/SYNK-appsscript/tools/lib/뜻대조.js:122)는 차이 목록과 개수가 어긋나도 마지막 개수를 믿습니다. 합성 답 `큼: 무료를 유료로 바꿨다`와 `판정: 큰것 0`을 함께 넣자 `어긋남=true`이면서 `같다=true`가 반환됐습니다. 실행이 비정상 종료했지만 출력에 판정 줄이 남은 경우도 통과해, 두 실패 재현 **2/2**가 정상 판정으로 접혔습니다.

**[P1] 글 검사기의 파싱 실패가 지적 없음으로 바뀐다** — §3의 `글검사` 실물인 [tools/글검사.js:137](/C:/Users/q1212/Documents/SYNK-appsscript/tools/글검사.js:137)는 파싱 실패에 빈 지적 배열을 반환하고, 호출부는 실패 표시를 검사하지 않습니다. 검사기 응답을 깨진 JSON으로 바꾼 합성 재현 **1/1**에서 종료 코드가 성공이고 화면에는 “새 지적 0건”이 나왔습니다. 학생 글 담당을 통일해도 검사 불능인 글이 검사 완료처럼 처리됩니다.

**[P1] 종이물의 목표 기간 계산이 교과 정본 밖의 약속을 만든다** — §3의 `종이물`이 사용하는 [상담결과조판.js:347](/C:/Users/q1212/Documents/SYNK-appsscript/tools/상담결과조판.js:347)는 TOPIK 6급도 Lv6에 대응시킵니다. `한국어수준=완전초보, TOPIK목표=6급`을 넣은 합성 재현 **1/1**에서 목표 6급·5시즌이라는 정상 값이 나왔지만, [커리큘럼_정본_v1.md:23](/C:/Users/q1212/Documents/SYNK-appsscript/docs/커리큘럼_정본_v1.md:23)는 Lv6의 6급 표기를 폐기했습니다. 대외 확정 문안·목표 수치·보장으로 읽힐 표현을 **봤다 → 문제다**: 조판 성공만으로 교육적 판단과 기간 안내가 검증됐다고 볼 수 없습니다.

**[P1] 과제의 말투를 통일해도 학생이 다시 열 이유는 검증되지 않는다** — §2⑤·§5의 `과제글` 기준에는 학생이 왜 한 번 더 열겠는지, 실제 한국어를 말하거나 쓰는 동작이 있는지, 반복 속에서 어떤 흥미 변화가 생기는지를 판정할 항목이 없습니다. 같은 말투로 비슷한 문제를 반복하고 정답 번호만 고르게 하는 과제도 현재 적힌 기준에서는 탈락 사유가 분명하지 않습니다. 빠진 층은 학생 접점의 **재참여 이유·실사용 산출·신선함 검증**이며, 이를 학생 글 담당자의 이름으로 대신하고 있습니다.

**[P1] 자기설명 게이트의 책임자가 없다** — §3의 `앱글`은 브랜드 말투, `글검사`는 쉬운 낱말과 문체를 담당하지만 판단 정본⑤의 전체 그림과 연결을 누가 검사하는지 없습니다. 첫 화면에 “계속” 버튼과 단계 안내만 있고 과제의 목적·재료 관계·제출할 것이 빠진 입력도 두 자리의 검사를 통과할 수 있습니다. 빠진 층은 **처음 본 학생이 무엇을 왜 해야 하는지 이해하는가**이며, 학생 산출 자체가 시작되지 못하는 결함입니다.

읽은 파일: 제공 전문 **7/7**, 후보 문서, `AGENTS.md`, 모델정책·관련 시험·엔진 수집 함수, 철학·제품방향·코어엔진·수집 계약·학생일기·삶이해 현황·커리큘럼·AI 스택 가이드·결정·검수기각의 관련 부분입니다. 안 본 것: SYNK-talk 계약·분업 원문, 이름만 제공된 대형 파일의 나머지 본문, 실제 시트·앱 화면·라이브 호출입니다. 합성 재현 **4/4**에서 위 반환값을 확인했으며 전체 회귀는 실행하지 않았습니다.

**대안 구조 1개 — 이름표별 ‘근거 연결표’로 바꾸십시오.** 담당 지정·품질 검증·실제 가동을 각각 기록하고, 각 자리에서 기존 시험과 실행 통로, 실패 동작, 계약 필드, 생산자·소비자, 기존 트랙의 완결 도장으로 연결합니다. 학생 접점에는 기본·자기설명 게이트를 연결하고, 삶 이해에는 승인한 판본과 근거를 연결합니다. 새 엔진 인프라를 선행 구축할 필요 없이, 무엇을 확정했고 무엇이 아직 미완인지 검증 가능한 구조가 됩니다.