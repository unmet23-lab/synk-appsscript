판정: **동결 불가 — 18건.**

[P0] **실측 상태가 커밋으로 재현되지 않는다** — §전건 판정은 `season_review`가 없다고 하지만, SYNK-talk `HEAD a596522`에는 시즌 관련 파일이 없고 작업본에만 관련 파일이 있다. 깨끗한 체크아웃에서는 시즌·회고 물리물이 사라지므로 동결 근거가 재현되지 않는다. 문서: `docs/시즌회고_설계.md:19-29` · 재현 입력: `git ls-tree -r HEAD | rg "season|회고|나침반"`.

[P0] **append-only 선언과 실제 나침반 행이 충돌한다** — §2-1·§3-1은 모든 답을 남긴다고 하지만, `season_compass`는 `(learner_id, season_id)` 유일행이고 삭제만 막으며 수정은 가능하다. 같은 행의 `answers`를 수정하면 최초 답은 복구할 수 없다. 문서: `docs/시즌회고_설계.md:87-91, 110-118` · 재현 입력: 기존 행의 `answers`를 A에서 B로 `UPDATE`.

[P0] **answers의 내용 검증이 없다** — §3-1과 §9-2는 서술형·한 줄 답을 요구하지만 키 집합만 확인하면 빈 문자열, `null`, 배열, 객체도 통과한다. 이후 `closer` 판정을 저장할 수 있어 “답 없음”이 정상 목표와 같은 모양이 된다. 문서: `docs/시즌회고_설계.md:110-118, 481-506` · 재현 입력: `{"why_learning":"","self_in_5y":null,"topik_use":[],"season_goal":{}}`.

[P0] **`판정불가`가 전건 판정에만 있고 본문 규격에는 없다** — §전건 판정은 `판정불가` 갈래가 필요하다고 적었지만 §3-2와 §5의 `verdict`는 여전히 `closer/same/redirected` 세 값뿐이다. 근거가 비었을 때 강사가 `same`을 누르거나 행을 남기지 않는 두 선택만 남아 오라벨과 누락을 구분할 수 없다. 문서: `docs/시즌회고_설계.md:23-27, 120-129, 201-219` · 재현 입력: 시즌 실기록·나침반·축 값이 없는 학생에게 회고 실행.

[P0] **진척과 방향 변경을 하나의 라벨로 접는다** — `closer`와 `redirected`는 서로 독립적인 사실인데 단일 3값 열에 넣었다. 학생이 시즌 목표에는 가까워졌지만 5년 목표는 바꾼 경우를 동시에 표현할 수 없어 엔진이 어느 사실을 학습할지 결정되지 않는다. 문서: `docs/시즌회고_설계.md:201-219, 450-459` · 재현 입력: `self_in_5y_changed=true`와 `season_goal` 달성.

[P0] **버튼 클릭을 실제 목표 변경의 증거로 오인한다** — `self_in_5y_changed=true`는 학생이 [바꿀래]를 눌렀다는 사실일 뿐, 이전 답과 새 답이 실제로 달라졌다는 증거가 아니다. 같은 문장을 다시 제출해도 `redirected`가 되어 방향 변경 라벨이 오염된다. 문서: `docs/시즌회고_설계.md:114-118, 201-208` · 재현 입력: 이전 답과 동일한 `self_in_5y`를 제출하면서 변경 버튼만 선택.

[P0] **회고 행에 판정 대상인 나침반 사본이 없다** — `record_snapshot`은 오른쪽 실기록만 굳히고, 회고 당시의 `season_goal`·5년 목표·답 해시·나침반 행 식별자를 저장하지 않는다. 나침반 행이 나중에 수정되면 같은 `closer` 라벨이 다른 목표에 붙어 학습 라벨의 과녁이 바뀐다. 문서: `docs/시즌회고_설계.md:120-151, 254-267` · 재현 입력: 목표 A로 회고 저장 후 같은 시즌 나침반을 목표 B로 수정.

[P0] **season_review도 사람 판정을 덮어쓸 수 있다** — §7은 학생·강사의 판정 차이를 귀중한 신호라고 하지만, 학생·강사의 원시 제출 사건이나 판정 이력을 정의하지 않고 한 행에 최종값만 둔다. 강사가 나중에 값을 바꾸면 최초 판단과 변경 이유가 사라진다. 문서: `docs/시즌회고_설계.md:120-133, 223-244, 254-267` · 재현 입력: `verdict=closer` 저장 후 `same`으로 수정.

[P0] **중도 입학생의 시즌 기준선이 없다** — 시즌은 교재 경계라고 하면서 학생별 입학일·시즌 참여 시작일·부분 시즌 처리 규칙을 정의하지 않았다. 시즌 중간 입학생에게 시작 나침반을 언제 받는지, 첫 회고를 제외할지, 짧은 기간을 비교할지 결정할 수 없다. 문서: `docs/시즌회고_설계.md:105-118, 271-290, 304-316` · 재현 입력: 시즌 시작 20일 후 입학한 학생에게 첫 회고 실행.

[P0] **전·후반 구간의 시간 규칙이 결정되지 않았다** — §4-2는 중간 시점과 앞·뒤 창을 말하지만 `[from,to)`인지, 중간일의 사건을 어느 쪽에 넣는지, 날짜·시간대·윤년·늦게 들어온 사건을 어떻게 처리하는지 정의하지 않았다. 같은 사건 배열로도 구현자마다 다른 스냅샷과 라벨이 나온다. 문서: `docs/시즌회고_설계.md:159-197, 287-290` · 재현 입력: 59일 시즌의 중간 시각에 발생한 제출 사건 1건.

[P0] **스냅샷 기준 시점과 지연 수집 기준이 없다** — “회고를 연 순간”을 굳힌다고 했지만 `opened_at`, `ingested_as_of`, 사건 확정 기준을 표준 필드로 정의하지 않았다. 시즌 안에 치른 시험 결과가 회고 직후 도착하면 같은 회고를 다시 계산할 때 포함 여부가 달라진다. 문서: `docs/시즌회고_설계.md:120-151, 223-236` · 재현 입력: 10:00에 스냅샷 생성 후 10:05에 시즌 내 시험 결과 수집.

[P0] **멱등성·재시도·동시성이 설계에서 빠졌다** — 두 표에 요청 멱등키, 상관키, 저장 결과 식별자, 타임아웃 후 재조회 규칙이 없고, §8은 계약·마이그레이션을 문서 밖으로 미룬다. 저장 성공 후 응답이 끊긴 상황에서 재시도하면 유일키 오류인지 중복 저장인지 정상 완료인지 판정할 수 없다. 문서: `docs/시즌회고_설계.md:105-133, 223-281` · 재현 입력: 저장 요청 후 네트워크 타임아웃을 발생시키고 동일 요청 재전송.

[P0] **엔진 도달을 시즌 2 이후로 미룬다** — §8 ⑤는 라벨이 몇 개 쌓인 뒤에야 엔진 배선을 세우도록 되어 있어 첫 시즌의 라벨이 저장만 되고 회수되지 않을 수 있다. 이는 정본의 “모였나·닿았나·늘었나” 중 닿음이 ✗인 상태를 의도적으로 허용하는 것이며, 엔진이 어느 부품에서 언제 읽는지도 없다. 문서: `docs/시즌회고_설계.md:33-36, 271-281` · 재현 입력: 첫 `season_review` 저장 후 엔진 소비 경로 조회.

[P1] **season_totals의 계산 계약이 없다** — 제출 수, 열람률, 최신 시험 결과, 급수의 이름만 있고 대상 이벤트, 분모, 경계 포함, 최신의 기준(`occurred_at`인지 시험일인지), 누락값 표현이 없다. 동일한 원본을 넣어도 구현자별 집계가 달라져 회고 스냅샷을 재현할 수 없다. 문서: `docs/시즌회고_설계.md:137-177` · 재현 입력: 시즌 경계 직전 제출과 늦게 수집된 `exam.result`를 함께 입력.

[P1] **기록자 단위가 너무 거칠다** — `recorded_by`를 행 하나에만 두어 학생이 직접 쓴 답과 강사가 대필·요약한 답을 구분하지 못한다. 한 세션에서 일부 답만 강사가 바꿔 적으면 답별 원문성과 출처가 사라져 사람 이해·삶 이해 학습 데이터가 섞인다. 문서: `docs/시즌회고_설계.md:110-118` · 재현 입력: 네 답 중 `season_goal`만 강사가 요약해 저장.

[P1] **`topik_use`라는 동일 키에 서로 다른 질문을 넣는다** — `study/work` 학생에게는 TOPIK 용도를 묻고 `culture` 학생에게는 일반적인 한국어 사용처를 묻도록 했지만 저장 키는 같다. 엔진이 키별로 묶으면 동일 필드 안에 서로 다른 의미가 섞여 비교와 학습이 깨진다. 문서: `docs/시즌회고_설계.md:450-455, 539-545` · 재현 입력: `goal_track=study`와 `goal_track=culture`의 `topik_use` 답을 같은 집합으로 학습.

[P1] **학생 접점인데 자기설명 게이트가 비어 있다** — 문서는 회고를 학생 화면이 아니라고 하면서 학생에게 버튼과 한 줄 입력을 요구한다. 처음 본 학생에게 이 활동의 전체 목적, 이전 답과 현재 교재 목표의 연결, 제출 후 얻는 결과를 첫 화면에서 설명하는 규격이 없으므로 반복 조사처럼 느껴질 수밖에 없다. 문서: `docs/시즌회고_설계.md:33-36, 223-244, 481-522` · 재현 입력: 처음 회고 화면을 본 학생에게 안내 없이 [그대로]/[바꿀래]와 한 줄 입력만 노출.

[P1] **학생의 `null`이 실패와 자발적 미응답을 합친다** — §6은 학생이 건너뛰면 `null`을 허용하지만, 시도 기록·실패 상태·네트워크 재전송 상태를 저장하지 않는다. 학생이 일부러 안 누른 경우와 화면 오류로 제출되지 않은 경우가 같은 대조군에서 처리된다. 문서: `docs/시즌회고_설계.md:234-244` · 재현 입력: 학생 판정 요청 중 네트워크 단절 후 강사 판정만 저장.

더 나은 대안 구조 1개: `learner_season`으로 참여 구간을 별도 관리하고, `season_compass_revision`과 `season_review_event`를 append-only 사건으로 분리한다. 회고에는 나침반 사본·해시, `[from,to)` 구간, `as_of/ingested_as_of`, 계산판·집계판 버전, `판정불가`를 함께 굳히며 `progress_verdict`와 `direction_changed`를 별도 필드로 둔다. 모든 저장은 멱등키를 가진 한 트랜잭션으로 엔진 소비 경로까지 같은 세로 슬라이스로 연결해야 한다.

검수 범위: `docs/시즌회고_설계.md`, 관련 코어엔진·철학·홈페이지·장학·교수법 문서, SYNK-talk의 `L0_데이터계약.md`, `docs/개발_분업.md`, 관련 작업본 마이그레이션·회고 코드·테스트를 대조했다. 라이브 Supabase 상태, 실제 배포 환경, 전체 C0 계약 전문은 확인하지 않았다.