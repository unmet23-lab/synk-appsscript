결론: 동결 불가. 현재값·생산 경로·사건 의미·학생 경험이 동시에 닫히지 않았다.

[P0] A-8의 스키마 판본이 현재 정본과 충돌한다 — [docs/코어엔진_설계.md:261,270](<C:/Users/q1212/Documents/SYNK-appsscript/docs/코어엔진_설계.md:270>)은 현재 판을 `c15`라고 하지만 실제 계약과 `엔진_수집.js`는 `c16`이다. `schema_ver`는 행에 영구히 남으므로 잘못 기록된 판본은 소급 수정할 수 없다. 재현 입력: `계약/수집_교정_계약.json:3`, `엔진_수집.js:63`, `tests/계약.test.js` 존재 여부를 확인하면 각각 `c16`, `c16`, 없음이 나온다.

[P0] §9의 훈련 승격은 사건명만 있고 생산 경로가 없다 — [docs/코어엔진_설계.md:141,272-280](<C:/Users/q1212/Documents/SYNK-appsscript/docs/코어엔진_설계.md:141>)은 `data_use.granted`를 c4에서 완료했다고 적지만, `docs/엔진도달_설계.md:34,84-89`는 생산자와 구현이 여전히 0이라고 판정한다. 검수→산출물 저장→승격 사건→데이터셋의 자동 원자적 흐름이 없으므로 행과 필드만 쌓이고 회사 훈련 재료는 계속 0이다. 재현 입력: `rg -n --glob '*.js' 'data_use\.granted' .`에서 실행 생산자를 찾을 수 없다.

[P0] 개입-효과 고리가 측정 규격 없이 선언되어 있다 — [docs/코어엔진_설계.md:218-220](<C:/Users/q1212/Documents/SYNK-appsscript/docs/코어엔진_설계.md:218>)은 즉시·1·7·30일 성과를 말하지만 어떤 사건이 각 성과인지, 미관측과 실패를 어떻게 구분하는지 정의하지 않는다. 그러면 `intervention_id`는 있어도 무엇이 효과였는지 재현할 수 없어 최상위 자산이 단순 연결 로그로 끝난다. 재현 입력: 개입 시각을 T0, 제출을 T+23시간과 T+31시간에 발생시키면 어느 성과 창에 들어가는지 이 문서로 판정할 수 없다.

[P0] 미래 정보 차단 규칙이 이 문서의 원신호 규격에 없다 — [docs/코어엔진_설계.md:189,218-220,280](<C:/Users/q1212/Documents/SYNK-appsscript/docs/코어엔진_설계.md:280>)은 `valid_from`·`expires_at`와 발생 시각만 말하고, 개입 당시 상태를 계산하는 `as_of` 절단을 명시하지 않는다. 현재 `docs/엔진도달_설계.md:70`의 `as_of ≤ intervention.delivered.occurred_at` 규칙과 갈리며, 사후 성과가 과거 추천의 근거로 섞일 수 있다. 재현 입력: 1월 개입을 2월 성과까지 포함해 다시 계산해도 이 문서만으로는 이를 거부할 수 없다.

[P0] 추정 신뢰도 필드명이 문서 내부에서 충돌한다 — [docs/코어엔진_설계.md:189](<C:/Users/q1212/Documents/SYNK-appsscript/docs/코어엔진_설계.md:189>)은 추정 신뢰도를 `confidence`라고 하고, [A-0/A-10](<C:/Users/q1212/Documents/SYNK-appsscript/docs/코어엔진_설계.md:261>)은 학생 확신도와 분리해 `estimator_confidence`라고 한다. 한 생산자가 앞 절을 따르면 학생 자기인식 값에 AI 추정치를 기록하게 되어 두 학습 축이 영구적으로 오염된다. 재현 입력: 동일한 추정값 `0.68`을 `confidence`와 `estimator_confidence` 중 어디에 기록해야 하는지 절마다 답이 다르다.

[P0] 선택 로그가 선택의 과정을 복원하지 못한다 — [docs/코어엔진_설계.md:131,193,274](<C:/Users/q1212/Documents/SYNK-appsscript/docs/코어엔진_설계.md:274>)은 최종 선택만으로 부족하다고 하면서도 `changed_selection` 불리언만 둔다. 이전 선택값·선택 사건 순서·이유를 물었는지 여부가 없어 A→B와 B→A, 미질문과 질문 후 무응답이 같은 모양으로 남는다. 재현 입력: 같은 후보와 최종값으로 두 사용자의 선택 경로를 A→B, B→A로 만들면 동일한 필드 행이 된다.

[P0] 교정 반응의 무응답·무시·지연 응답이 구분되지 않는다 — [docs/코어엔진_설계.md:173,273](<C:/Users/q1212/Documents/SYNK-appsscript/docs/코어엔진_설계.md:173>)은 `채택/무시/수정`을 요구하지만 `correction.viewed`와 `correction.responded` 사이의 종료 시각·응답 기한·미응답 판정이 없다. 교정을 아예 전달하지 못한 경우, 보고도 답하지 않은 경우, 며칠 뒤 답한 경우가 빈 값 또는 동일한 반응으로 집계될 수 있다. 재현 입력: 교정을 본 뒤 앱을 종료하고 10일 후 답하는 세 경우를 입력하면 이 문서의 규칙으로는 어느 시점의 반응인지 결정할 수 없다.

[P0] 멱등키와 오프라인 재시도의 동작 계약이 비어 있다 — [docs/코어엔진_설계.md:280](<C:/Users/q1212/Documents/SYNK-appsscript/docs/코어엔진_설계.md:280>)은 `idempotency_key`가 중복을 막는다고만 하고 유일성 범위, 다른 payload 충돌 시 처리, 동시 삽입 승자, 사건 순서 기준을 정하지 않는다. 그 결과 같은 사건을 중복 집계하거나 정상적인 두 시도를 하나로 삼켜도 데이터가 통과할 수 있다. 재현 입력: 오프라인 기기 두 대가 같은 키로 서로 다른 `selected_option`을 재전송했을 때 결과가 정의되어 있지 않다.

[P0] `user_confirmed`는 이름만 있고 학생 확인 사건의 물리가 없다 — [docs/코어엔진_설계.md:189,206-214](<C:/Users/q1212/Documents/SYNK-appsscript/docs/코어엔진_설계.md:206>)은 학생 확인을 최고 품질로 선언하면서도 생산·전달·저장 방식과 빈도 결정을 미결로 남긴다. 첫 학생이 확인 답을 남기기 전에 이 경로가 없으면 잘못된 추정이 맞는지 틀리는지 영원히 복원할 수 없다. 재현 입력: 첫 학생에게 성향 카드를 보여주고 “아니에요”를 눌렀을 때 어떤 사건과 어떤 학습자 상태 갱신이 발생하는지 문서에 없다.

[P1] 강등 동작이 정상 성공으로 기록될 수 있다 — [docs/코어엔진_설계.md:264-266](<C:/Users/q1212/Documents/SYNK-appsscript/docs/코어엔진_설계.md:266>)은 기능표에 기본 동작을 적으라고 하지만 fallback 시 `degraded`를 어느 사건에 필수로 기록하고 성공·실패와 어떻게 결합할지 정하지 않는다. 서버가 기본 답을 반환하고 표식 기록을 빠뜨리면 엔진은 모델 개입이 성공한 것으로 학습한다. 재현 입력: AI 타임아웃 뒤 기본 문장을 반환하는 `intervention.delivered`를 `degraded` 없이 저장하면 정상 개입과 구분되지 않는다.

[P1] 지식 그래프의 링크와 인용이 동일한 역사 버전을 보장하지 않는다 — [docs/코어엔진_설계.md:129,133,267](<C:/Users/q1212/Documents/SYNK-appsscript/docs/코어엔진_설계.md:129>)은 선행·관련 링크와 `cited_refs`를 말하지만 링크 ID, 링크 종류, 노트 판본·해시를 정의하지 않는다. 같은 노트 ID의 내용을 수정하면 과거 답변의 인용이 현재 내용으로 바뀌어 회수와 재현 모두 흔들린다. 재현 입력: 답변 후 동일 지식 노트의 내용을 수정하면 이전 답이 어느 버전을 사용했는지 복원할 수 없다.

[P0] “학생 0명인 마지막 창” 날짜가 현재 결정과 충돌한다 — [docs/코어엔진_설계.md:243-247](<C:/Users/q1212/Documents/SYNK-appsscript/docs/코어엔진_설계.md:245>)은 첫 데이터 시점을 개원일 이후로 전제하지만 `docs/_ops/결정.md:93`은 실학생 파일럿을 2026-12-07~21로 확정한다. 이 문서를 따르면 파일럿보다 늦게 수집·배선해도 된다는 잘못된 일정 판단이 발생하고, 그 사이의 원본 증거는 소급할 수 없다. 재현 입력: “첫 실학생 데이터 earliest”를 조회하면 개원일이 아니라 12월 파일럿 날짜가 나온다.

[P1] 운영·완결 게이트가 현재 판정표로 존재하지 않는다 — [docs/코어엔진_설계.md:82-87](<C:/Users/q1212/Documents/SYNK-appsscript/docs/코어엔진_설계.md:82>)은 철학의 ③운영·④완결 눈금이 없으며 E1에서 만들겠다고 미룬다. 따라서 각 기능의 모였나·닿았나·늘었나·누구에게와 사람 화면의 자기설명 통과 여부를 동결 시점에 판정할 장소가 없다. 재현 입력: `intervention.delivered` 한 건의 네 칸 상태와 사용자 설명 상태를 이 문서에서 찾으면 정의된 보드 칸이 없다.

[P1] 학생 확인 UI가 기본 게이트와 자기설명 게이트를 통과할 근거가 없다 — [docs/코어엔진_설계.md:202-214](<C:/Users/q1212/Documents/SYNK-appsscript/docs/코어엔진_설계.md:202>)은 한 줄 문구와 두 단추만 제안하고, [228-241](<C:/Users/q1212/Documents/SYNK-appsscript/docs/코어엔진_설계.md:228>)은 `?`·`??` 기호 재사용만 제안한다. 처음 본 학생이 이것이 무엇인지, 답하면 다음 학습이 어떻게 달라지는지, 실제 한국어 연습에 어떤 이득이 있는지 알 수 있는 흐름이 없다. 재현 입력: 신규 학생이 “요즘 저녁에 더 집중이 오르는 것 같아요—맞아요?” 카드를 처음 보면 기능의 목적과 다음 행동을 문서만으로 설명할 수 없다.

이 문서보다 나은 대안 구조 1개: 역사·판정 서술과 별도로 현재 동결 레지스트리를 둔다. 레지스트리의 사건별 한 행에 생산자, 필수·선택·금지 필드, 스냅샷, 멱등 범위, `as_of`, 효과 창, fallback, 소비자, 회수, 네 가지 완결 칸과 학생 첫 화면 설명 상태를 함께 두고 CI가 계약 판본·문서·코드와 자동 대조하게 해야 한다.

검수 범위 — 읽은 파일: `docs/코어엔진_설계.md`, `계약/수집_교정_계약.json`, `docs/엔진도달_설계.md`, `docs/제품방향.md`, `docs/_ops/결정.md`, `엔진_수집.js`, `tests/safety.test.js`, `tools/계약동봉검사.js`.

안 본 것: 이 저장소에 없는 SYNK-talk의 C0/L0 원문·실제 Supabase DB 상태. 해당 자료만을 근거로 한 별도 지적은 하지 않았다.