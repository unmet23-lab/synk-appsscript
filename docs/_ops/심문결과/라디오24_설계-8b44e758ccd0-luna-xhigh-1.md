판정: 동결 불가. P0 10건, P1 7건이다.

1. [P0] “P0 완료”인데 DB 적용·실측 검증이 실행셋에서 빠졌다 — `docs/라디오24_설계.md:3,7,154-177,321-327`은 남은 일이 채널·OAuth·VPS뿐이라고 쓰지만, `:177`은 실제 DB가 미적용이라고 인정한다. `SYNK-talk/supabase/functions/radio-ingest/index.ts:121-169`는 스키마가 없으면 실패한다. 재현 입력: 마이그레이션 미적용 상태에서 채널→OAuth→VPS만 완료한 뒤 채팅 1건을 보내면 원장 저장이 500으로 끝난다.

2. [P0] 500건 절단이 다음 페이지 커서를 건너뛴다 — `radio-ingest/index.ts:47-49,95-97`은 500건만 남기지만 `bots/라디오수집봇.js:384-385`는 절단 전 API 응답의 `nextPageToken`을 저장한다. 재현 입력: 600개의 `textMessageEvent`와 다음 토큰 `P`를 반환하면 500건만 저장되고 다음 조회는 `P`부터 시작해 나머지 100건을 영구히 건너뛴다.

3. [P0] 저장한 커서가 재시작 복구에 전혀 쓰이지 않는다 — `docs/라디오24_설계.md:158-164`는 c15가 커서를 닫았다고 주장하지만 `bots/라디오수집봇.js:214-223,326-340`은 재시작 때 `다음쪽=null`로 초기화하고 heartbeat를 읽지 않는다. 재현 입력: 봇을 중단한 동안 2,000건 이상 채팅을 발생시킨 뒤 재시작하면 최신 페이지만 다시 읽고 공백 구간은 복구되지 않는다.

4. [P0] 원문 누락을 `{}`로 위장한다 — `docs/라디오24_설계.md:38,262`는 원 API 원문 보존을 필수로 하지만 `radio-ingest/index.ts:63-76`은 `raw`가 없으면 `{}`를 넣는다. 재현 입력: 필수 메시지 필드만 보내고 `raw`를 생략하면 `chat_message.raw NOT NULL`을 통과하며, 이후 재파싱 불가능한 행이 완전한 원문처럼 남는다.

5. [P0] `broadcast_segment`는 표만 있고 생산자가 없다 — `docs/라디오24_설계.md:53,182-192,262`는 모든 사건의 맥락 조인 열쇠라고 정의하지만 실제 인제스트 `radio-ingest/index.ts:145-169`는 채팅과 heartbeat만 기록한다. 재현 입력: 곡 또는 시간대가 바뀌어도 `radio.broadcast_segment`에는 행이 생기지 않아 1년차 운영·3년차 개인화의 맥락 축이 첫날부터 비게 된다.

6. [P0] 동시 시청자 수가 매 60초 측정값이 아니라 방송 발견 시점의 낡은 값이다 — `docs/라디오24_설계.md:54,410-417`은 heartbeat마다 그때의 인원을 기록한다고 쓰지만 `bots/라디오수집봇.js:107-116`에서 한 번 읽은 값을 `:280-287`의 모든 heartbeat·라운드 판단에 재사용한다. 재현 입력: 발견 시점 1명 뒤 실제 시청자가 20명 또는 0명이 되어도 불빛·라운드 개시·분모는 계속 1명으로 남는다.

7. [P0] `video_id`를 추가했지만 방송 경계를 실제로 사용하지 않는다 — `docs/라디오24_설계.md:154-164,235-244,266-281`과 달리 `bots/라디오수집봇.js:232-253`의 라운드 ack에는 video id가 빠지고, `radio-promote/index.ts:104-112,149-158`와 `lib/라디오승격.js:130-137`은 시간만으로 라운드와 응답을 찾는다. 재현 입력: 같은 채널에서 수업 방송과 라디오 방송을 동시에 열고 수업 채팅에 `!답 2`를 보내면 라디오 문항 응답으로 승격될 수 있다.

8. [P0] 채팅 출제의 성공·실패 상태가 원장에 확정되지 않는다 — `docs/라디오24_설계.md:162-164,235-244,421-425`는 `posted_message_ids`·`post_status`를 근거로 삼지만 `radio-round/index.ts:273-279`는 이를 쓰지 않고, `bots/라디오수집봇.js:294-312`는 전송 실패 뒤에도 상태를 완료로 진행한다. 재현 입력: 출제 성공 후 ack를 실패시키거나 마감 메시지만 실패시키면 라운드가 없거나 `closed_at`이 비어 늦은 `!답`이 정상 응답처럼 승격된다.

9. [P0] 7일 배치창과 “v1 소급 없음”이 장기 장애 데이터를 영구 폐기한다 — `docs/라디오24_설계.md:19,266-273`은 원장 보존을 손실 0처럼 설명하지만 `radio-promote/index.ts:61-63,149-158`은 최근 7일만 조회한다. 재현 입력: 승격기가 8일 중단된 동안 발생한 링크 학생의 채팅은 원장에 남아도 다시 승격할 통로가 없어 `learning_events`에 영영 도달하지 않는다.

10. [P0] 급수 스냅샷이 발생 시점 값이 아니라 승격 시점 현재값이다 — `docs/라디오24_설계.md:277-280`은 `occurred_at` 당시 값을 요구하지만 `radio-promote/index.ts:170-172,234-246`은 배치 실행 시점의 `engine.learners.level_current`를 모든 과거 사건에 넣는다. 재현 입력: Lv2에서 발생한 사건을 학생이 Lv3으로 바뀐 뒤 승격하면 불변 행에 Lv3이 기록되어 당시 학습자 상태를 복원할 수 없다.

11. [P1] 제거한 학생 표면의 명령·표·승격 규격이 남아 있다 — `docs/라디오24_설계.md:456-467` 스스로 롤링 자막 제거 후 `subtitle_card_log`와 `!출석`·낭독 의존성이 남았고, ASMR 페이지 제거 후 `!asmr` 승격만 남았다고 인정한다. 재현 입력: `!asmr`를 실행하면 갈 목적지가 없고, 자막 카드가 없는 날 `!출석`은 실제 오늘의 표현이 아닌 fallback 과제로 처리되어 문서의 학습 연결과 달라진다.

12. [P1] 라운드 생산자와 승격자의 계약 판정이 갈린다 — `docs/라디오24_설계.md:19,293-302`는 c11을 전제로 하지만 `radio-round/index.ts:154-162`는 c10이면 라운드를 쓰고 `radio-promote/index.ts:81-99`는 c11 미만을 거절한다. 재현 입력: 최신 스키마 판본이 c10인 상태에서 라운드 ack를 하면 원장만 쌓이고 승격은 실패한다.

13. [P1] 날짜 경계 정본이 문서와 코드에서 두 갈래다 — `docs/라디오24_설계.md:40`은 `lib/오늘과제.js` 재사용을 요구하면서 `:166-171`은 `lib/몽골날짜.js`를 정본이라고 한다. 실제 `SYNK-talk/lib/오늘과제.js:23-25`는 몽골날짜로 위임하지만 `bots/라디오수집봇.js:225-228`은 UTC+8을 직접 계산한다. 재현 입력: 날짜 경계 테스트에서 IANA 모듈과 봇의 직접 계산을 각각 실행하면 서로 다른 접힘 규칙을 허용하는 구조가 된다.

14. [P1] YouTube 쿼터 “9,000 유닛 안전” 계산이 완전한 상한이 아니다 — `docs/라디오24_설계.md:330-331`과 `lib/라디오수집.js:69-74`는 폴링+출제만 합산하고 `search.list` 재발견 비용을 제외한다. `lib/라디오수집.js:113-136`은 재발견만 최악 4,800유닛으로 계산한다. 재현 입력: 하루 중 방송 종료·재발견을 두 번 반복하면 9,000에 재발견 비용이 더해져 10,000 한도를 넘고, 수집과 출제가 함께 조용히 멈출 수 있다.

15. [P1] 몽골 학생용 채팅 안내가 구현되지 않았다 — `docs/라디오24_설계.md:324,436-449`는 문안 몽골어 병기를 요구하지만 `SYNK-talk/lib/라디오라운드.js:257-265`의 `mn` 값은 모두 `null`이다. 재현 입력: 라운드 안내·마감·재도전 문구를 생성하면 한국어만 출력되어 첫 이용자가 `!답` 절차를 이해하지 못한다.

16. [P1] 첫 화면 자기설명이 성립하지 않는다 — `docs/라디오24_설계.md:405-449`는 화면에 음악·불빛·마스코트와 시간대별 명령 하나만 둔다. `docs/SYNK_철학.md:92-96`의 ⑤가 요구하는 “이게 무엇인지·무엇을 할지·재료가 어떻게 연결되는지”를 설명하는 상시 요소가 없다. 재현 입력: 광고 명령이 바뀌는 시간대 밖에 처음 접속하면 학생은 음악과 불빛은 보지만 라디오의 목적, 채팅 참여법, 앱 낭독과의 연결을 알 수 없다.

17. [P1] 회수 시점이 폐기된 연차 어휘로만 적혀 있다 — `docs/라디오24_설계.md:35,53,66,77,128,277`은 “1년차·3년차 회수”를 반복하지만 `docs/AI_스택_가이드.md:12-20`과 `docs/제품방향.md:18-32`는 기존 1·2·3·4년차 제품 표기를 폐기하고 +6개월·2027-08 등의 기준으로 바꿨다. 재현 입력: 구현자에게 “3년차 개인화”의 실제 날짜·게이트를 물으면 이 문서와 현행 로드맵에서 단일 답을 만들 수 없다.

값비싼 변경 점검: 봤다 → 문제다. DB 마이그레이션 적용, OAuth·채널 개설, VPS 결제, BGM 생성·인코딩은 모두 실제 비용 또는 비가역성이 있는데, 이 문서는 파일 존재와 승인만으로 실행 가능 상태처럼 취급한다.

이 문서보다 나은 대안 구조가 있으면 1개:

`한 방송 한 세로줄`만 먼저 동결한다. 고정된 `video_id`로 DB 적용·실DB 재검증 → 원문/heartbeat/segment 수집 → 출제·ack·마감 → 당일 승격 → 첫 이용자 화면·몽골어 안내까지 한 번에 왕복 검증한 뒤, 통과한 명령과 표면만 확장한다.

읽은 파일: `docs/라디오24_설계.md` 전문, `CLAUDE.md`, `docs/AI_스택_가이드.md`, `docs/제품방향.md`, `docs/SYNK_철학.md`, 관련 `SYNK-talk` 마이그레이션·수집·라운드·승격 코드.

안 본 것: 원격 Supabase 실제 DB, 실제 YouTube/OAuth/VPS 상태, 테스트 파일 전문, 오버레이 HTML의 현재 렌더링 결과, Reed 콘텐츠 팩 실물. `SYNK-talk` 코드는 현재 작업 트리 기준으로 대조했다.