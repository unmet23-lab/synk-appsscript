동결 불가. 결함 18건이다.

1. [P0] 정본·채택·계약판이 충돌한다 — 동결 후보 문서 머리말은 “채택 전에는 정본 아님”인데 본문 상태는 “채택·배선 완료”다. §10은 c15를 다음 판으로 지목하지만 현재 계약은 [수집_교정_계약.json](C:/Users/q1212/Documents/SYNK-talk/계약/수집_교정_계약.json:3) c16이고, c15 설명은 reads 판1([수집_교정_계약.json](C:/Users/q1212/Documents/SYNK-talk/계약/수집_교정_계약.json:17)), 구현은 판2([읽기기록.js](C:/Users/q1212/Documents/SYNK-talk/lib/읽기기록.js:37))다. 재현 입력: 같은 문서를 기준으로 c15 마이그레이션과 c16 계약을 각각 적용하면 서로 다른 이벤트 모양이 나온다.

2. [P0] “모든 task.assigned에 reads”가 거짓이다 — 후보 문서 §3·§10-1은 전 경로 기록을 주장하지만 게임 경로와 일반 폴백은 여전히 `{ver:1}`만 저장한다([deliver/index.ts](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/deliver/index.ts:766), [deliver/index.ts](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/deliver/index.ts:936)). 생성 경로만 reads를 draft에 넣는다([생성모드.ts](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/deliver/생성모드.ts:159)). 재현 입력: 비생성·게임·비대상 배치를 실행하면 task.assigned에 읽기 기록이 없다.

3. [P0] reads 내부 구조가 검증되지 않는다 — 계약은 `reads`라는 최상위 키만 허용하고([수집_교정_계약.json](C:/Users/q1212/Documents/SYNK-talk/계약/수집_교정_계약.json:125)), 검증기도 최상위 키만 검사한다([이벤트검증.js](C:/Users/q1212/Documents/SYNK-talk/lib/이벤트검증.js:899)). 집계기는 임의의 `picker`·`source`가 truthy이면 읽음으로 센다([읽기기록.js](C:/Users/q1212/Documents/SYNK-talk/lib/읽기기록.js:98)). 재현 입력: `{"reads":{"skill":{"picker":"bogus"},"life":{"source":"bogus"},"person":{"source":"bogus"}}}`를 넣으면 3층 읽음으로 집계된다.

4. [P0] 빈 사유 값록이 문서 안에서 충돌한다 — §3은 현재 정본이 네 값뿐이라고 하면서 예시에는 `no_confirmed_preference`와 `no_stage_yet`를 추가한다. 실제 정본 배열은 네 값뿐이다([읽기기록.js](C:/Users/q1212/Documents/SYNK-talk/lib/읽기기록.js:44)). 재현 입력: 학생에게 확정 무대가 없을 때 새 생산자가 어느 값을 써야 하는지 계약상 유효한 답이 없다.

5. [P0] 무대·확정 성향을 읽는 생산자가 없다 — 후보 문서 §4·§5는 `stage`와 `confirmed_preference`를 약속하지만 `읽기기록()`은 시즌 목표와 행동축만 입력받는다([읽기기록.js](C:/Users/q1212/Documents/SYNK-talk/lib/읽기기록.js:64)). 생성 호출부도 그 두 재료를 넘기지 않는다([생성모드.ts](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/deliver/생성모드.ts:159)). 재현 입력: 학생이 무대 A와 성향을 확정한 뒤 과제를 생성하면 reads에는 시즌 목표와 behavioral_axes만 남는다.

6. [P0] 무대 노출 사건의 이벤트 계약과 답 연결이 없다 — §4는 별도 노출 사건을 요구하지만 event_type 이름·생산자·검증 규칙을 정하지 않았고, 현재 event_type 목록에도 없다([수집_교정_계약.json](C:/Users/q1212/Documents/SYNK-talk/계약/수집_교정_계약.json:101)). `preference.stated`에도 후보집합 ID나 선택 후보 ID가 없고 필수 필드는 기존 세 개뿐이다([이벤트검증.js](C:/Users/q1212/Documents/SYNK-talk/lib/이벤트검증.js:190)). 재현 입력: 후보집합 A와 B를 같은 세션에 보여준 뒤 학생이 같은 문장으로 답하면 어떤 집합에서 선택했는지 복원할 수 없다.

7. [P0] 무대 후보의 원입력과 생성 판이 보존되지 않는다 — §4.2 노출 사건은 후보 원문과 목표판만 적고 원래 목표 문장, model, prompt_ver, schema_ver를 요구하지 않는다. 목표가 수정되거나 모델·프롬프트가 바뀌면 후보 품질과 생성 근거를 재현할 수 없다. 재현 입력: 목표 “간호사”로 후보를 만든 뒤 목표를 “개발자”로 수정하고 사건을 조회하면 후보가 어느 목표에서 나왔는지 확인할 수 없다.

8. [P0] `goal_used`는 현재 즉사하고 의미도 부족하다 — 현재 출력 스키마는 `sentence`·`question`만 허용하고([과제생성.js](C:/Users/q1212/Documents/SYNK-talk/lib/과제생성.js:44)), 파서는 미지 키를 응답파손으로 거절한다([과제생성.js](C:/Users/q1212/Documents/SYNK-talk/lib/과제생성.js:136)). 설령 키를 추가해도 `true` 하나로 시즌 목표를 썼는지 확정 무대를 썼는지 구분할 수 없다. 재현 입력: `goal_used:true`를 포함한 유효 JSON은 현재 `응답파손`이 되고, 두 무대 A/B도 같은 boolean으로 뭉개진다.

9. [P0] 분모가 null인데 `늘✓`를 찍는다 — §3과 §11은 시도 수를 아직 모른다고 인정하지만 도장에는 읽기 커버리지 `늘✓`를 적는다. 실제 함수도 시도 수가 없으면 `시도수`와 `미착지`를 null로 낸다([읽기기록.js](C:/Users/q1212/Documents/SYNK-talk/lib/읽기기록.js:142)). 재현 입력: 생성 실패로 task.assigned가 생기지 않은 밤을 포함해 계기판을 실행하면 실패는 분모에서 빠진 채 완료 표시가 가능하다.

10. [P0] 기아 수리 실패가 알려진 결함으로 되돌아간다 — 후보 문서 §5는 축별 마지막 답 정렬을 수리했다고 하지만, SQL 실패 시 progress는 오류를 기록하고 빈 객체를 넘긴다([progress/index.ts](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/progress/index.ts:223)). 그러면 고정 우선순위로 내려앉아 리듬 카드만 반복한다([성향확인.js](C:/Users/q1212/Documents/SYNK-talk/lib/성향확인.js:120)). 재현 입력: 축별 마지막날 조회를 실패시키면 작성과정·집중띠는 “미응답”처럼 보이며 영구 라벨이 생기지 않는다.

11. [P0] 확인 카드의 노출 분모가 없다 — §6은 노출 수가 필요하다고 하지만 §4의 후보집합 노출 사건은 무대용이고, 성향 확인 카드 자체에는 표시·전달 사건이 없다. 현재 progress는 답변 행만 세고 있다([progress/index.ts](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/progress/index.ts:214)). 재현 입력: 카드가 반환된 뒤 앱이 닫히거나 네트워크가 끊기면 “한 번도 묻지 않음”, “물었지만 무응답”, “조회 실패”가 모두 n=0이 된다.

12. [P0] 무대 거부가 이전 확정을 취소하지 못한다 — §4.3은 학생·시즌별 마지막 `맞다`만 남기고 `아니다`에 대한 철회 규칙을 금지한다. A를 `맞다`로 확정한 뒤 A를 `아니다`라고 답해도 A가 계속 현재 무대로 남는다. 재현 입력: 같은 무대에 `맞다 → 아니다`를 순서대로 넣고 접으면 미래 과제가 학생이 거부한 무대를 계속 사용한다.

13. [P1] 무대 카드의 슬롯과 하루 상한이 충돌한다 — §4는 무대 후보를 오늘의확인 레일에 태우지만 §5는 되묻기 카드를 하루 최대 2장, 성향 1장과 목표 1장으로 못박는다. 실제 progress 응답도 `오늘의확인`과 `오늘의목표` 두 칸뿐이다([progress/index.ts](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/progress/index.ts:271)). 재현 입력: 성향 카드·목표 카드·무대 후보가 모두 필요한 날 무대가 어느 칸을 차지하는지 계약상 정해져 있지 않다.

14. [P1] ⓓ·E2 판정 카드는 실행 설계가 아니다 — §7은 정책별 배정, 관찰창, 비교 단위가 없으면 판정 근거가 아니라고 인정하면서도 실제 카드의 배정 규칙·정책 ID·성과 필드·결정 규칙을 정의하지 않는다. 같은 카드가 두 정책을 겪지 않는다는 문제를 문장으로만 지적했을 뿐이다. 재현 입력: 고정창 학생군과 FSRS 학생군의 결과를 모아도 어느 정책·관찰창에서 나온 값인지 사건으로 연결할 수 없다.

15. [P1] `goal_used` 정확도를 잴 라벨 저장소가 없다 — §11은 회전 채점표로 자기 보고 정확도를 재겠다고 하지만 채점자·시각·루브릭 판·판정값·task.assigned 연결 필드가 없다. 그러면 정확도 표본이 생겨도 자기 보고와 사람 판정을 재현 가능한 사건으로 만들 수 없다. 재현 입력: 5회전 뒤 사람이 “실제로 무대를 썼다”고 판정해도 그 값을 어느 과제에 붙일지 정의되어 있지 않다.

16. [P1] 학생에게 선택의 효과가 돌아오지 않는다 — §4는 `goal_used`의 학생 문구를 표본이 생길 때까지 보류한다. 따라서 학생이 무대를 골라도 다음 과제에서 그 선택이 반영됐는지 알 수 없어, 판단 정본의 ⑤ 자기설명과 ①기본 게이트 중 `재미`·`실사용 도움`에 답이 없다. 재현 입력: 학생이 “병원 접수처”를 선택한 뒤 과제를 열면 내부 boolean 외에는 선택의 결과를 확인할 방법이 없다.

17. [P1] “실제로 쓸 한국어”의 학생용 품질 게이트를 미래로 미뤘다 — §4는 무대가 장소만 제공하고 실제 한국어 품질은 이 설계 밖의 미래 회전 채점표로 넘긴다. `goal_used:true`는 목표 문맥의 자연성·유용성·문법 정확성을 보장하지 않는다. 재현 입력: 병원이라는 단어만 포함한 부자연스러운 문장이 목표 사용으로 보고되어도 현재 설계상 차단할 자가 없다.

18. [P2] §9의 수치와 최신 근거가 감사 불가능하다 — FSRS의 20~30% 절감·90% 리텐션, 맥락 개인화 `g≈0.89`를 적었지만 출처·표본·설계·측정값이 없다. 문서가 스스로 요구한 “수는 자와 함께” 규칙을 위반한 상태다. 재현 입력: 독립 검수자가 해당 수치의 1차 연구와 적용 조건을 추적할 수 없다.

검수 범위: 동결 후보 문서 전문, [수집_교정_계약.json](C:/Users/q1212/Documents/SYNK-talk/계약/수집_교정_계약.json), [읽기기록.js](C:/Users/q1212/Documents/SYNK-talk/lib/읽기기록.js), [이벤트검증.js](C:/Users/q1212/Documents/SYNK-talk/lib/이벤트검증.js), [과제생성.js](C:/Users/q1212/Documents/SYNK-talk/lib/과제생성.js), [성향확인.js](C:/Users/q1212/Documents/SYNK-talk/lib/성향확인.js), progress·deliver·생성모드 코드, c15 마이그레이션을 읽었다. 실DB 상태·원격 배포 상태·§9 외부 원문은 확인하지 않았다.

이 문서보다 나은 대안 구조 1개: 기능 서사와 동결 계약을 분리하고, 기능마다 `생산자 경로 → 불변 필드 → 저장 위치 → 소비자 → 집계식 → 학생 화면 → 실패 fixture`를 한 행으로 둔다. 한 칸이라도 실물 코드·계약·회귀 fixture가 없으면 `✗`로 고정하며, “예상 ✓”는 허용하지 않는다.