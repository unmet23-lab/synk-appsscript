# 상태기반 과제선택 — 7회차 심문 처분표 v7

> **정본** · 2026-08-14 · 세션 `3bc38188` · 대상 = 설계 **v5.3**(대상지문 `09400290eafe`) ·
> 결과 3벌 = `상태기반_과제선택_설계-09400290eafe-{luna-1,luna-2,sol-3}.md`
> (런 `심문-2026-08-13T1514-88a14e` · 던진 세션 `eaa21f3f` · 수거 `3bc38188`).
> **게이트 = 「전건 처분」**(유호 확정 08-12 「통과라는 상태가 없다」): ①판정 = 원건 매핑 수 = 결과
> 파일 지적 수 **69** ②집행 = 채택분이 본문(v5.4)에 들어왔다.
> 🚫 v1~v6 처분표를 덮지 않는다(회차 대조 · F281). 🚫 이 표를 6회차 위에 «세지» 않는다 — 판이 다르다
> (v5.2 ↔ v5.3). **숫자가 60→69 로 늘어난 것을 「나빠졌다」의 근거로 쓰지 않는다.**

## §0. 게이트 산술

| 벌 | 원건 | P0 | P1 |
|---|---|---|---|
| luna-1 | 23 | 18 | 5 |
| luna-2 | 25 | 20 | 5 |
| sol-3 | 21 | 11 | 10 |
| **계** | **69** | 49 | 20 |

**처분 = 40갈래**(채택 33 · 부분 채택 7 · 기각 0) · **원건 매핑 69/69**(빠짐 0 · 중복 0 · 잉여 0 —
손으로 세지 않았다: 추출·매핑·대조를 스크립트가 했다). **3벌 만장일치 10갈래.**

⚠ **갈래 층 기각이 0인 것은 이 회차의 «성적»이 아니라 «층»이다.** v1~v2 는 「장이 통째로 없다」였고
v5.3 은 「닫았다고 적은 자리가 안 닫혔다」다 — 후자는 문서 자신의 문장과 대조하면 판정이 갈리지 않는다
(그래서 반박할 것이 적다). 대신 **처방 층에서 1건을 기각했다**(A4 — 아래).

## §1. 회차 조건 (F281)

한 런(`88a14e`)의 3벌이라 **대상지문·방향·철학·금지 지문이 세 벌 동일**(`09400290eafe` ·
`4fb5cafa7c07` · `1b4c3bdc50b1` · `ff23c161104b`) — 이 판 «안»의 3/3 합의는 가중치로 쓸 수 있고
(증명은 아니다), 회차 «간» 산술 대조는 금지다.

🔴 **회차 «간» 조건 대조는 이번에 «불가»로 판정한다 — 장부가 그 증거를 잃었다(F411).**
수거 중 실측: 던짐↔완료 짝 9 중 **6이 갈려 있었다.** 완료 행이 `방향·철학·금지` 를 다시 계산해서,
프롬프트가 조립된 뒤 정본이 바뀌면 장부에 «완료 시각의 값»이 남았다.

| 회차 | 대상지문 | 던짐 철학 | 완료 철학 | 판정 |
|---|---|---|---|---|
| 5회차 | `f0b56546c486` | (동일) | (동일) | 금지만 갈림 |
| 6회차 | `e5fa329c11d6` | `afc4bbfb6bfb`(v1.7) | `1b4c3bdc50b1`(v1.8) | **철학·금지 둘 다 갈림** |
| 7회차 | `09400290eafe` | `1b4c3bdc50b1` | `1b4c3bdc50b1` | ✅ 성하다 |

6회차 완료 행이 적힌 23초 전에 `24a222c4`(철학 v1.8)가 들어왔다. ⇒ **6·7회차를 대조하면 장부는
「철학 동일」로 읽히지만 실제로는 v1.7 대 v1.8 이다.** 조건 통제를 증명하라고 둔 칸이 정반대로
«조건이 갈린 것을 감췄다». 🔑 도구는 고쳤다(`823a462b` · 회귀 4 · 변이 3/3 · F411 해소 `8339f8fd`)
— **고친 것은 다음 회차부터 산다.** 옛 6짝은 append-only 라 못 고치고, 래칫이 신규만 막는다.

## §2. 실측 — 인용이 아니라 열어서 쟀다

지적이 편 «사실 주장» 중 일곱을 직접 열어 쟀다. **일곱 다 지적이 맞다** — 이번엔 심문자를 정정한 건이
하나도 없다(5·6회차와 갈린다). 대신 **설계가 스스로 적은 문장과 어긋난 자리**가 넷이었다.

| # | 잰 것 | 결과 |
|---|---|---|
| 실1 | `engine.skills` 의 `domain='grammar'` 가 30행인가 | 🔴 **아니다 — 18이다.** 두 시드가 동일(`L0_스키마.sql:6685`·`20260812120000_engine_c11.sql:140`): **문법 18 · 표현 5 · 어휘 7 = 계 30.** 설계 §6-0(`:1311`)의 「`domain='grammar'` 전량(**30행**)」은 **총계를 한 갈래에 붙인 것**이다 ⇒ S3-12 가 맞다 |
| 실2 | `attempts_result_gate` 가 빈 배열을 막는가 | 🔴 **못 막는다.** `array_length('{}',1)` = `NULL` → 쌍조건 우변이 `NULL` → `TRUE = NULL` 도 `FALSE = NULL` 도 `NULL` → CHECK 는 `NULL` 을 거절하지 않는다. **양방향 다 샌다**(검문탈락+빈배열 · 성공+빈배열). `attempts_gate_values` 의 `<@` 도 빈 배열엔 참이라 못 잡는다 ⇒ L1-3·L2-6·S3-2 가 맞다. 🔑 **저장소는 이미 `cardinality()` 를 쓴다**(`L0_스키마.sql:319`) — 이 설계만 그 표기를 벗어났고, 벗어난 그 자리가 구멍이다 |
| 실3 | `generation_attempts` 에 `estimator_version` 이 있는가 | 🔴 **없다**(`:647~672` — `model`·`prompt_ver`·`policy_ver` 셋뿐). 그런데 같은 절 `:605~608` 주석은 「배치 실행판 봉투 **넷**」을 적고 「attempts 의 **같은 이름 칸은 그대로 둔다** — 두 값의 대조가 곧 배포가 갈렸다의 탐지기」라 적었다 ⇒ **탐지기가 4칸 중 3칸만 본다.** 문서가 자기 주석과 어긋났다 ⇒ L1-9·L2-5·S3-5 가 맞다 |
| 실4 | `jobs_winner_present` 가 정상 폴백을 통과시키는가 | 🔴 **거절한다.** §4 폴백 표는 단2 = 「그 밖 **전부**」이고, §3-6 종료 상태는 `착지`·`마감폴백`·`대상아님` **셋뿐**이며 `마감폴백` 은 마감 스윕 전용이다 ⇒ 평시 폴백(`키없음`·`타임아웃`·`상태없음`·`구제경로`…)은 `착지` 로 앉을 수밖에 없는데 CHECK 는 거기에 `winning_attempt_id` 를 요구한다. 성공 시도가 없거나(대개) 아예 attempt 행이 없다 ⇒ 트랜잭션이 깨지거나 **실패 시도를 거짓 승자로 적어야** 한다. 복합 FK 는 「같은 job 인가」만 보고 `result='성공'` 은 안 본다 ⇒ L1-2·L2-2·S3-1 이 맞다. 🔑 `:635` 주석이 범인을 자백한다 — 「생성이 **성공해** 착지한 날은」이라 쓰고 조건은 `status <> '착지'` 로 걸었다(뜻은 `outcome`, 글자는 `status`) |
| 실5 | 현행 `오늘과제()` 의 `degraded` 가 §5-4 와 맞는가 | 🔴 **초급·미정에서 어긋난다.** 실물(`lib/오늘과제.js:299~303`)은 `첫날→false · 교정문→false · 전날→true · 도입→true`. §5-4(`:1224~1226`)는 `대상아님`(첫날·교정문·**초급·미정**)을 「**라이브 그대로** `false`」라 적었는데, 초급·미정 학생은 평일에 전날/도입으로 나가므로 라이브는 **`true`** 다 ⇒ S3-17 이 맞다. 「라이브 그대로」라는 말이 라이브를 안 열고 쓰였다 |
| 실6 | `deliver-check` 를 §3-2-a 가 건드렸는가 | 🔴 **안 건드렸다.** c10 에 `cron.schedule('deliver-check', '35 16 * * *', …)`(`20260809070000_cron_c10.sql:87`)로 살아 있고 §3-2-a 는 새 잡 셋만 더한다. `워커학생상한` = **3**(§3-2 표 `:361`)이라 3회 호출로 최대 9명 ⇒ 대상이 그보다 많은 날은 정상 배치도 미달로 보고된다 ⇒ S3-19 가 맞다. ⚠ **단 S3-19 가 적은 시각 「00:35」는 그 벌의 말이고 실물 cron 은 `35 16 * * *` 다** — 지적의 몸통은 살고 숫자 한 칸만 그쪽 것이다 |
| 실7 | `generation_jobs`·`generation_attempts`·`lib/과제*.js` 가 실재하는가 | **전부 없다**(SYNK-talk 전량 검색 0). ⚠ 그러나 이 트랙은 **⛔구현 착수 차단 중**이라 코드 부재는 «정상»이다 ⇒ L1-1 의 「코드가 없다」는 결함이 아니고, **살아남는 몸통은 「RPC 본문·트랜잭션 SQL 이 없어 §12 인수 조건이 아직 규격이 아니다」** 쪽이다(G3 부분 채택의 근거) |

🔑 **이 회차 최대 발견은 실1 이다 — 그리고 그 오류의 계보가 6회차 자신에게 있다.**
6회차 처분표 v6 의 실2 는 `engine.skills` 를 열고 「ID 는 `skill-ko-…` 꼴 **30행**」이라 적었다.
그 문장 안에서 30 은 **참**이다(표 전체가 30행). 그런데 v5.3 §6-0 이 그 숫자를 **`domain='grammar'`
전량**이라는 «좁힌 자리»로 옮겨 적으면서 거짓이 됐다. **참인 숫자가 좁혀지면서 거짓이 된 것**이고,
좁힌 쪽에는 아무 표시도 안 남았다. 6회차가 「매핑이 실재하지 않는다」를 잡아 원천을 갈아끼웠는데,
**그 갈아끼운 원천의 크기를 다시 안 열어봤다** — 같은 실패 모드(간접 증거로 현재 상태를 판정)가
한 회차 만에 한 칸 안쪽에서 재발했다.

---

## §3. 처분 — 40갈래

표기: ✅채택 / ◐부분 채택 / ❌기각 · 「원건」은 그 갈래로 접힌 지적 · **굵은 갈래 = 3벌 만장일치**

### A군 — 물리 계약(DDL·RPC)이 안 닫힌 자리 (12갈래 · 원건 20)

| # | 갈래 | 원건 | 처분 | 수리 방향 |
|---|---|---|---|---|
| **A1** | `착지` 승자 필수가 정상 폴백을 거절한다 | L1-2·L2-2·S3-1 | ✅ | CHECK 의 키를 `status` 에서 **`outcome`** 으로 옮긴다: `outcome <> '성공' or winning_attempt_id is not null`. 그리고 승자가 **성공 시도**임을 DB 가 지게 한다 — CHECK 는 남의 표를 못 보므로 `attempts` 에 `result` 를 포함한 **복합 FK 확장**(`attempt_id, job_id, result`)으로 건다(실2 계열 처방과 같은 축: 문장이 아니라 물리) |
| **A2** | 검문 사유 빈 배열이 CHECK 를 통과한다 | L1-3·L2-6·S3-2 | ✅ | `array_length(x,1) >= 1` → **`cardinality(x) >= 1`**(빈 배열에서 0 을 낸다 · 저장소 기존 표기 `L0_스키마.sql:319`). §12-21 회귀에 **빈 배열 픽스처 2벌**(검문탈락+`{}` 거절 · 성공+`{}` 거절)을 못박는다 |
| **A3** | `estimator_version` 이 attempts 에 없다 | L1-9·L2-5·S3-5 | ✅ | `generation_attempts` 에 칸을 더하고 `attempt_open` 시그니처에 넣는다. **job 값과 다르면 거절**(`:607` 이 이미 model·prompt_ver·policy_ver 에 대해 약속한 것 — 넷째 칸만 빠졌다) |
| A4 | `jobs_terminal_cols` 가 개별 칸을 안 막는다 | L1-4 | ◐ | **앞부분 채택**: 쌍조건은 세 칸의 «곱»만 봐서 `대기` 행에 `outcome` 하나만 얹는 것은 통과한다 ⇒ 비종료 상태에 종료 칸이 **하나라도** 들어가면 거절하도록 칸별로 건다.<br>❌ **뒷부분(`not_target_reason` 양방향화) 기각**: `:640` 이 편방향인 이유를 이미 적었다 — 비대상 행은 적재 «트랜잭션 안»에서 `대기`→`대상아님` 으로 지나가고, CHECK 는 문장마다 평가되므로 양방향으로 만들면 **그 전이 자체가 깨진다.** 지적이 든 위험(대상 job 에 사유가 붙어 claim 에서 영구 제외)은 실재하나 처방이 반대다 ⇒ 대신 `jobs_claim` 이 `not_target_reason` 을 **안 읽게** 못박고 그 이유를 절에 적는다 |
| A5 | 원응답 없이 영구 종결할 수 있다 | S3-3 | ✅ | `attempt_close` 에 사후 조건: `result in ('성공','검문탈락','응답파손','응답초과')` 이면 `raw_response`·`responded_at` **필수**(`타임아웃`·`입력초과`는 null 허용 — 못 받았거나 안 불렀다). 1회 채움이라 놓치면 소급 복구 불가 |
| A6 | job.outcome 과 payload 가 갈려도 저장된다 | L1-13·L2-15 | ✅ | `jobs_finalize` 가 `_outcome` 과 `_event.payload.generation_outcome` 의 **동일성을 트랜잭션 안에서 검사**하고 어긋나면 예외. 두 곳에 같은 사실을 적는 구조 자체는 유지(사건은 append-only 라 job 을 못 읽는다) |
| A7 | 선점 job 을 자발 반납할 RPC 가 없다 | L2-4 | ✅ | `jobs_release(job_id, fence)` 신설 — 워커 예산이 마르면 **즉시** `claimed→대기`(펜싱 그대로 · ㉥ 회수와 같은 전이). 지금은 1800초 임대 만료까지 고착돼 다음 10분 회차가 못 집는다 ⇒ §3-2-a 의 「10분마다 이어받는다」가 실질 30분이 된다 |
| A8 | `branch_snapshot` 규격이 안 닫혔다 | L1-5 | ✅ | 필수 키·자료형·값목록을 §3-5-b 에 표로 못박는다(첫날·교정문·급수·목표·게임 판정 + `ver`). `jobs_load` 가 그 키 부재를 거절 |
| A9 | 전량 원자 적재가 한 명 오류를 전원 장애로 | S3-8 | ◐ | **격리는 채택**: 학생별 조립 실패는 그 학생 job 을 `outcome='내부오류'` 로 만들고 나머지는 적재한다.<br>원자성 «폐기»는 **안 한다** — `unique(learner_id, assign_date)` + 한 트랜잭션이 「재실행이 큐를 두 배로 안 만든다」를 지는 자리다(§3-5). 둘은 양립한다(실패 학생도 «행은» 만든다) |
| A10 | 롤백 부재가 물리 인수 조건과 충돌 | L1-20·L2-17 | ◐ | **개원 전이라 빈 DB 재적용이 실질 롤백**이고 그 사실을 §12-21 에 명시한다(지금은 「롤백 안 만든다」만 있고 «왜 그래도 되는가»가 없다). ❌ 역마이그레이션 신설은 안 한다 — 학생 0명 구간의 되돌림 비용이 그 유지비보다 싸다. ⚠ **첫 학생이 온 뒤에는 이 판정이 죽는다** — 그 조건을 절에 적는다 |
| A11 | 빈 job 집합에서 회전식이 0 나눗셈 | L2-24 | ✅ | `jobs_claim` 이 `그날 job 수 = 0` 이면 **회전 계산 전에** 빈 결과를 반환한다. 무대상일이 cron 오류로 바뀌면 그날 사고가 정상에 묻힌다 |
| A12 | `attempt_open`↔외부호출 사이 유실 창 | L1-19 | ✅ | `attempt_open` 이 null 을 내면 **워커는 반드시 중단**(계약으로 못박음). 응답 직후 사망은 `raw_response` 유실로 남고, 그 자리를 `result='타임아웃'` 과 구분하기 위해 회수 시 **열린 attempt 를 닫지 않고 그대로 둔다**(열린 행 자체가 「받았는지 모른다」의 증거) |

### B군 — 사건 봉투(L0 계약) (4갈래 · 원건 6)

| # | 갈래 | 원건 | 처분 | 수리 방향 |
|---|---|---|---|---|
| **B1** | `_event` 가 L0 필수 필드를 못 만든다 | L1-6·L2-9·S3-4 | ✅ | §3-5-b 에 **봉투 전량 표**를 넣는다 — `learner_id`·`actor_kind`·`schema_ver`·`occurred_at`·`idempotency_key`·`task_type`·`level_snapshot`·`goal_snapshot`·`degraded`·`retry_of_event_id`, 그리고 `submission_row` 의 `occurred_at`·`schema_ver`·`due_at`·`due_ver`. **무엇을 job 이 채우고 무엇을 호출자가 넣는지 칸마다 표시**한다. 🔑 `idempotency_key` 는 세 사건이 **각자** 갖는다(`task.assigned` 키 재사용 금지 — 재사용하면 세 사건이 한 키로 뭉쳐 부분 착지를 못 가른다) |
| B2 | `task_type`·`task_format` 이 정본과 갈린다 | L2-10 | ✅ | `submission_row.task_type` 을 라이브 값(`발화녹음`)에 맞춘다. `task_format` 은 **호흡별 칸**과 사건 최상위 값을 절에서 갈라 적는다(지금 한 낱말이 두 층을 가리킨다) |
| B3 | 상태 메타 일부를 조용히 버린다 | L2-19 | ◐ | **`source_kind` 는 채택**(뜻이 다른 축이라 §4-1 이 이미 갈라 적었으니 「왜 안 싣는가」를 명시). ⚠ **`valid_from`·`expires_at` 은 전제가 틀렸다** — 6회차 실4 가 이미 쟀고 `learning_events` 에 **그 열이 없다**. 없는 열을 「버렸다」고 할 수 없다 ⇒ 그 부분은 기각하고, 정정을 절에 남겨 다음 회차가 또 집지 않게 한다 |
| B4 | 배정 행을 학생 산출로 오인한다 | L1-16 | ✅ | §12-3 인수 조건에서 「제출」을 뺀다 — `submissions` 는 **배정 사건에 딸린 보조 행**이고 배정 트랜잭션 안에서 선다(6회차 실1). 「학생이 말하거나 썼다」의 증거는 **녹음·전사 사건**이지 이 행이 아니다. 🔑 이건 ④완결 「모였나」의 급소다 — 지금 인수 조건은 배정만 성공해도 초록이다 |

### C군 — 마감·구제·전환 경로 (5갈래 · 원건 8)

| # | 갈래 | 원건 | 처분 | 수리 방향 |
|---|---|---|---|---|
| **C1** | 06:00 마감 스윕을 정의된 RPC 로 못 부른다 | L1-7·L2-3·S3-7 | ✅ | 두 가지가 함께 빈다: ①마감 대상을 **열거**하는 단위 ②그때 쓸 **폴백 입력**. ⇒ `jobs_load` 가 job 마다 **`event_draft`(폴백 봉투 전량)를 미리 저장**하고(지금은 비대상 건에만 있다), 마감 스윕은 `jobs_finalize_due(assign_date)` **집합 RPC** 하나로 돈다. 🔑 마감 때 `오늘과제()` 를 **다시 돌리지 않는다** — 다시 돌리면 최초 배정과 다른 문장이 학생에게 가고 그건 소급 불가다 |
| C2 | 큐가 없으면 구제가 구제하지 못한다 | L1-8 | ✅ | 구제 경로가 **그 학생 job 을 없으면 만든다**(`jobs_load_one`). 지금은 `jobs_claim` 만 해서 오케스트레이터가 죽은 날엔 집을 것이 0 ⇒ 「오케스트레이터 장애를 구제한다」가 큐가 이미 있을 때만 참이다 |
| C3 | 구제 호출부 미배선 + 대기 학생 빈 화면 | L1-21·L2-20 | ✅ | ①`/tasks` → `deliver` 호출에 **맥락 인자**(`배치\|구제`)를 배선하고 기본값·오류값을 계약에 적는다 ②claim 실패·생성 중은 **HTTP 200 + 빈 배정**이 아니라 «생성 중» 상태 필드로 답한다. 🔑 학생 화면 문구는 이 트랙이 정하지 않는다(카피 정본 소관) — 계약 칸만 연다 |
| C4 | `deliver-check` 가 새 구조에서 거짓 경보 | S3-19 | ✅ | §3-2-a 에 **기존 잡 처분**을 적는다 — `deliver-check` 를 마감 스윕 «뒤»로 옮기거나, 판정 기준을 「착지 수」가 아니라 「대기로 남은 수」로 바꾼다. 안 적으면 정상 배치가 매일 빨갛게 뜨고, 그 화면은 곧 안 보게 된다 |
| C5 | 라이브 경로 → 새 큐 전환 경계가 없다 | L2-16 | ✅ | **활성 시작일**을 못박고, 그날 이전 사건은 흡수하지 않으며, 같은 학생·날짜에 기존 사건이 있으면 `jobs_load` 가 그 행을 **`이미배정`으로 건너뛴다**. 지금은 배포 시점에 따라 한 날짜에 두 판이 공존할 수 있다 |

### D군 — 스냅샷·재현·판지문 (8갈래 · 원건 15)

| # | 갈래 | 원건 | 처분 | 수리 방향 |
|---|---|---|---|---|
| **D1** | `ingested_as_of` 가 원신호 조회에 미배선 | L1-10·L2-14·S3-6 | ✅ | §11-4 는 `학습자상태()` **옵션만** 더했다. 실제 `deliver` 원신호 SQL 이 `ingested_at` 을 **가져오지도 않고** `now()-창일수` 로 먼저 자르므로 하류가 검사할 값이 없다 ⇒ SQL 에 칸을 싣고 cutoff 를 **조회 층**으로 내린다. 갈래 판정·기술 선택의 이력 조회도 같은 경계를 쓴다 |
| D2 | `policy_ver` 가 행동 입력을 다 안 묶는다 | L1-17·L2-8 | ✅ | 재료에 **`폴백여유_MS`·cron 주기·`jobs_finalize` 상태 전이·오류 분류·폴백 draft 조립 규칙**을 더한다. 지금 여덟은 «생성»만 묶고 «마감·분류»를 안 묶어서, 그 셋이 바뀌어도 같은 판 아래 분포가 섞인다 |
| D3 | `policy_ver` 형식이 §5-4 에서 미수거 | L2-7·S3-18 | ✅ | §5-1(V6-15)이 이미 `'오늘과제.v4+<해시12>'` + **접두 판독**으로 닫았는데 §5-4(`:1209`)가 「`policy_ver='오늘과제.v4'` **자체가** 표시」인 채로 남았다 ⇒ 그 한 줄을 접두 판독으로 개서. 🔑 **형제 절 미수거** — v5.2 집행 세션이 「v5 낙방의 뿌리 그 자체」라 적은 그 형태가 또 났다 |
| D4 | 요약 생산자가 없어 분기가 구현자에게 열렸다 | L1-11 | ✅ | `lib/과제요약.js` 의 **단위·순서·널 처리·절단 규칙**을 §6-2 에 못박는다. `policy_ver` 해시는 «바뀐 것»을 알릴 뿐 «첫 구현»을 안 정한다 — 그런데 이 분기는 생성/폴백을 가르므로 소급 불가다 |
| **D5** | 순수 검문과 이력 조회가 충돌·미배선 | L1-12·L2-11·S3-14 | ✅ | 순수 경계는 **지킨다** — 최근 14일 생성문·기술 겨냥 이력을 **명시적 입력**으로 넘긴다(함수가 DB 를 안 읽는다). 그리고 그 이력을 **조회해 넘기는 생산자·쿼리·`as_of`** 를 §3 파이프라인에 적는다. 지금은 픽스처에만 넣고 운영은 빈 배열이어도 전 검사가 초록이다 |
| D6 | 기술 선택 해시가 재현 규격이 아니다 | L1-23 | ✅ | 해시 알고리즘·문자열 인코딩·1개/2개 선택 기준·최근 이력 조회 범위를 못박는다. 첫 처치 의도는 append-only 사건에 남아 나중에 못 고친다 |
| D7 | 재적 분모가 과거를 재현하지 못한다 | L2-25 | ✅ | 배정일의 재적 수를 **그날 job 적재 시점에 저장**한다(지금은 현재 `engine.learners` 를 읽어서, 학생이 추가·비활성화되면 과거 배치의 진단이 바뀐다) |
| D8 | 세 분모가 사고를 진단하지 못한다 | L2-13·S3-9 | ✅ | 🔴 §3-6 V6-27 의 「재적 분모가 **0 이면 배치가 죽은 것이다**」는 **자기모순**이다 — 재적 분모는 `engine.learners` 를 세므로 배치가 안 돌아도 N 이 나온다. ⇒ 「전원 게임일」과 「오케스트레이터가 시작 전에 죽은 날」은 여전히 `(재적=N, job=0, intervention=0)` 으로 **같은 얼굴**이다. 처방 = **배치 실행 표식**(`batch_runs` 행 또는 heartbeat)을 세워 「돌았는데 0」과 「안 돌았다」를 가른다. 곁들여 §12-18 이 `status` 필터 없이 job 전량을 세어 **대상 분모의 세 번째 정의**를 만든 것을 정정 |

### E군 — `skill_ids` 사슬 (4갈래 · 원건 9)

| # | 갈래 | 원건 | 처분 | 수리 방향 |
|---|---|---|---|---|
| E1 | **skills 원천 실측이 틀렸다 — grammar 는 18 이다** | S3-12 | ✅ | §6-0 의 **30 → 18** 정정(실1). 「후보 = 그 30행 전량」·「`[]` 는 시드가 깨진 것」도 함께 갈린다. `skill_taxonomy_ver` 의 **정확한 값과 생산 규칙**도 못박는다(지금 `c11`·`skills.v1`·`kt.2026-08` 중 구현자가 고른다). 🔑 이 갈래가 이 회차 최대 발견이다 — §2 의 계보 설명 참조 |
| **E2** | 회전이 학생의 약점·급수와 무관하다 | L1-15·L2-22·S3-13 | ◐ | **급수 층화는 채택** — §8-B 시험 40개를 기술×급수로 층화해 「그 급수에 안 맞는 기술」이 검증 없이 나가는 것을 막는다.<br>❌ **「학생 약점으로 후보를 좁혀라」는 이번 판에서 기각** — 6회차가 급수→기술 매핑이 **실재하지 않음**을 실측했고(그래서 회전으로 갔다) 되돌리면 그 낙방 자리로 회귀한다. ⏳ **유호님 대기 중인 「급수→기술 매핑을 이 트랙이 지을 것인가」가 이 자리의 열쇠다**(결정큐) |
| E3 | `skill_ids` 가 사전 의도를 처치로 위조한다 | L2-12·S3-10 | ✅ | 폴백이 나간 건의 사건에는 `skill_ids` 를 **안 싣는다**(겨냥은 job 에 남기고, 사건에는 실제로 나간 것만). 안 그러면 학생이 못 본 기술이 후속 제출의 «효과»로 조인되고 회전에서도 「이미 줬다」로 세어진다 — 둘 다 append-only 라 소급 정정 불가 |
| **E4** | `늘었나 ✗` 인데 게이트가 통과시킨다 | L1-14·L2-1·S3-16 | ✅ | §12-17 의 「skill 조인이 0행이 아니면 통과」를 **`닿았나`의 증거로 이름을 바꾼다**(지금 `늘었나` 자리에 서 있다). `늘었나` 는 §6-0 H3 가 스스로 적은 대로 **✗ 이고**, 그 칸을 채우려면 `error_tags` 시드·기회·분모·관찰 창이 먼저다 ⇒ **④완결은 `✓✓✗` 로 정직하게 두고 그 상태를 보드 상태 칸에 적는다**(철학 정본 v1.8). 🔑 문서가 이미 정직하게 ✗ 를 적었는데 **인수 조건만 그걸 안 읽는다** — 고칠 것은 판정이 아니라 게이트다 |

### F군 — 출시 평가 게이트 §8-B (4갈래 · 원건 8)

| # | 갈래 | 원건 | 처분 | 수리 방향 |
|---|---|---|---|---|
| **F1** | 평가 초록이 채점한 산출물에 안 묶인다 | L1-18·L2-18·S3-11 | ✅ | 결과 행에 **실제 `sentence`·`question`·원응답(또는 그 해시)·입력 해시·attempt 연결**을 싣는다. 생성은 비결정이라 지금 행(`case_id`·`axis_scores`·`grader_note`)만으론 «무엇을 보고 준 점수인가»를 못 되살린다. 그리고 **기대 case ↔ 결과 행 일대일·중복 금지**를 회귀가 검사한다(지금은 누락을 중복으로 메운 거짓 초록을 못 막는다) |
| F2 | 목표 미설정 학생이 임의 처분된다 | L2-21·S3-15 | ✅ | `goal_snapshot=null` 은 **개원 첫 주의 정상 상태**다(C0). ⇒ ①생성은 목표 없이 진행(맥락을 지어내지 않는다) ②§8-B 의 「목표 활용」 축은 그 사례에서 **분모에서 뺀다**(0점 아님 — 0점은 없는 것을 못했다고 세는 것이다) ③시험 8칸에 `goal` 유/무를 **변수로 넣는다**(지금은 급수 4×상태 2 뿐이라 목표를 무시하는 프롬프트도 통과한다) |
| F3 | 출시 게이트가 사람 손을 폭증시킨다 | S3-20 | ◐ | **산술은 그쪽이 맞다**(40×2×7=560 축 판정). 다만 채점자는 한 사례를 한 번 열어 7축을 함께 매기므로 **사람이 여는 횟수는 80** 이다 ⇒ 「560개 확정」은 과대 표현. ✅ **후단은 채택** — 「평가 없이 켜는 것이 «불가능»하다」는 지금 **거짓**이다(Actions 정지 · 손 배포 경로가 기계로 안 막힌다) ⇒ 그 문장을 「회귀가 결과 파일을 검사한다」로 낮추고, 진짜 차단은 **생성 경로 활성 플래그**가 진다고 적는다 |
| F4 | 신선함 검사가 질문을 놓친다 | L1-22·L2-23 | ✅ | 중복 검사를 **문장+질문 쌍**으로 넓히고, 완전 일치 대신 정규화(NFC·공백·문장부호) 후 비교. ⚠ **「재미」의 학생 단위 판정은 이번 판에서 안 연다** — 학생 0명이라 못 재는 칸이고, 못 재는 것을 게이트에 넣으면 게이트 전체가 안 쓰인다(철학 정본 ④ 주의). 그 사실을 절에 적는다 |

### G군 — 문서 자체 (3갈래 · 원건 3)

| # | 갈래 | 원건 | 처분 | 수리 방향 |
|---|---|---|---|---|
| G1 | `degraded` 「라이브 그대로」가 실물과 다르다 | S3-17 | ✅ | §5-4 의 「`대상아님`(첫날·교정문·초급·미정)은 **라이브 그대로 `false`**」를 정정(실5) — 라이브는 **첫날·교정문만 `false`** 이고 초급·미정은 평일에 `true` 다. 🔑 「라이브 그대로」라는 말이 라이브를 안 열고 쓰였다 |
| G2 | 동결 문서가 경량 정본 원칙을 위반한다 | S3-21 | ✅ | §0 판 역사·§15·§16 옛 처분표 대조표를 **`지도.md` 로 이관**하고 본문엔 현행 규격만 남긴다(유호 확정 「개발 중엔 가볍게」). 🔑 이 지적은 **이 문서가 여러 판 연속 숫자·어휘가 갈린 원인을 정확히 짚었다** — 복제가 갈림의 원천이고, 실1·D3 가 바로 그 형태다. ⚠ 금지목록(§13)은 **안 옮긴다**(재제안 차단이 현행 기능이다) |
| G3 | 물리 계약이 주석에만 있다 | L1-1 | ◐ | ❌ **「코드가 없다」는 기각** — ⛔구현 착수 차단 중이라 부재가 정상이다(실7).<br>✅ **몸통은 채택** — RPC **본문**(트랜잭션 경계·반환 계약)이 없어 §12 인수 조건이 아직 «규격»이 아니다. 시그니처만으론 원자성·펜싱·마감 동작을 구현자가 정한다 ⇒ 여섯 RPC 각각에 **반환 계약과 실패 시 상태**를 표로 적는다(SQL 본문 전량은 구현 커밋 몫) |

---

## §4. 원건 매핑 69/69

기계 대조(추출·매핑·빠짐/중복/잉여 검사) — **빠짐 0 · 중복 0 · 잉여 0.**

| 벌 | 원건 → 갈래 |
|---|---|
| L1 | 1→G3 · 2→A1 · 3→A2 · 4→A4 · 5→A8 · 6→B1 · 7→C1 · 8→C2 · 9→A3 · 10→D1 · 11→D4 · 12→D5 · 13→A6 · 14→E4 · 15→E2 · 16→B4 · 17→D2 · 18→F1 · 19→A12 · 20→A10 · 21→C3 · 22→F4 · 23→D6 |
| L2 | 1→E4 · 2→A1 · 3→C1 · 4→A7 · 5→A3 · 6→A2 · 7→D3 · 8→D2 · 9→B1 · 10→B2 · 11→D5 · 12→E3 · 13→D8 · 14→D1 · 15→A6 · 16→C5 · 17→A10 · 18→F1 · 19→B3 · 20→C3 · 21→F2 · 22→E2 · 23→F4 · 24→A11 · 25→D7 |
| S3 | 1→A1 · 2→A2 · 3→A5 · 4→B1 · 5→A3 · 6→D1 · 7→C1 · 8→A9 · 9→D8 · 10→E3 · 11→F1 · 12→E1 · 13→E2 · 14→D5 · 15→F2 · 16→E4 · 17→G1 · 18→D3 · 19→C4 · 20→F3 · 21→G2 |

## §5. 집행 순서 (v5.4)

되돌림 비용이 큰 것부터 — **물리 계약이 먼저다**(append-only 라 한 번 저장되면 못 고친다).

1. **A군 물리** — A1·A2·A3·A4·A5·A6·A7·A8·A11·A12 (DDL·RPC 절 재작성)
2. **B군 봉투** — B1·B2·B4 (+B3 정정 1줄) — 사건은 소급 불가라 A군과 같은 무게
3. **C군 경로** — C1·C5 먼저(마감·전환은 데이터가 갈린다) → C2·C3·C4
4. **D군 재현** — D1·D3·D8 먼저(자기모순·형제 절) → D2·D4·D5·D6·D7
5. **E군** — E1 정정이 선행(숫자가 다른 절로 번진다) → E3·E4 → E2 급수 층화
6. **F군** — F1·F2·F4 → F3 문장 낮추기
7. **G군** — G1 정정 → G3 반환 계약 표 → **G2 이관은 맨 마지막**(본문이 다 선 뒤에 옮긴다)

⛔ **구현 착수 차단 유지** — 이 표는 문서 층이다. A9·A10 은 **첫 학생이 온 날 판정이 죽는다**(조건을
절에 적었다). ⏳ **유호님 자리 = E2**(급수→기술 매핑을 이 트랙이 지을 것인가 — 결정큐에 이미 서 있다).

## §6. 🚫 다음 회차가 다시 집지 않게 (정정 기록)

- 🚫 **`valid_from`·`expires_at` 이 「버려졌다」** — `learning_events` 에 **그 열이 없다**(6회차 실4). B3 참조.
- 🚫 **「`deliver-check` 는 00:35」** — 실물 cron 은 `35 16 * * *` 다(실6). 지적의 몸통은 살고 시각만 그쪽 것이다.
- 🚫 **「코드 파일이 없으니 결함」** — ⛔구현 착수 차단 중이라 부재가 정상이다(실7 · G3).
- 🚫 **「560개 사람 확정」** — 사람이 여는 횟수는 80 이다(F3).
