확인 불가 — 라이브 배포·학생 화면·서버 왕복은 확인하지 않았다. 아래는 문서·로컬 코드 대조와 순수 함수 재현에서 잡은 결함이다.

소급 불가 스키마·무료 연장 판정·금칙 경계 변경을 **봤다 → 문제다.** 아래 절·행 번호는 [동결 후보 문서](C:/Users/q1212/Documents/SYNK-appsscript/docs/자율일_설계_v1.md)를 기준으로 한다.

**[P0] 배정 스냅샷을 보존한다면서 같은 식별자로 덮어쓴다** — §③-㉠·§⑥·§⑧, 67·164·218행. 같은 배정ID를 나중 내용으로 교체하면, 먼저 받은 문항을 오프라인에서 푼 제출이 교체된 목표·분모에 붙는다. 항목ID까지 바꾸면 옛 제출이 고아가 되고, 유지하면 서로 다른 문항이 같은 항목이 된다. 발행 이후 불변 규칙과 정정본의 별도 식별자가 없어, 이 문서가 막겠다는 과거 복원 불능을 스스로 만든다.

**[P0] 항목 식별자를 끝까지 보존하는 제출 규격이 없다** — §⑥·§⑧, 164·197·218행. 「배정ID·항목ID를 싣는다」고만 하고 기존 `task_ref`·`payload`·`task_snapshot` 중 정확히 어디에 어떤 판으로 넣는지 정하지 않았다. 기존 `학생판스냅샷()`에 배정ID·항목ID·목표를 넣은 재현에서는 **보존 0/3필드**였으며, `답하기{n}` 형식 이름을 추가하는 것만으로는 이 손실이 해결되지 않는다. 같은 자유발화 형식의 제출들이 쌓인 뒤에는 어느 물음의 답인지 믿을 만하게 복원할 수 없다.

**[P0] 진단 회차 번호를 그대로 연결하면 중간에 끝 문항이 나온다** — §③-㉡·§⑧, 81·214행. 문서는 중간을 `2`, 끝을 `3`으로 보내지만, 실물 `contents_진단문항.js:140–153`은 시작·중간·끝을 각각 `0·1·2`로 받는다. 실제 함수에 문서 번호를 넣은 **입력 2/2가 모두 끝 문항으로 해석됐다**. 변환 규칙 없이 동결하면 끝 진단 문항을 중간에 노출하고, 무료 연장 판단에 쓰는 재측정이 같은 문항 반복으로 오염된다.

**[P0] 일요일 우선순위가 첫날 원본과 미소비 교정문을 없앤다** — §③-㉣, 108행. 첫 배정일이 일요일인 학생에게 문형을 우선하면 고정 도입 과제가 남겨야 하는 첫날 비교 원본이 빠지고, 이후에는 배정 이력이 생겨 첫날 갈래로 돌아가지 않는다. 교정문도 「월요일에 다시 온다」는 선언과 달리, 현재 `deliver/index.ts:354`는 마지막 배정 이후에 생성된 교정만 조회한다. 일요일 배정 전에 생성됐지만 일요일에 사용하지 않은 교정은 월요일 조회에서 탈락하므로, 미뤄진 교정을 보존하는 규칙부터 필요하다.

**[P1] 오답을 talk에 넣는지조차 서로 반대로 적었다** — §⑦-㉠·§⑧, 180·197·213행. §⑦은 오답이 talk에 안 들어가므로 금칙 밖이라고 주장하지만, §⑧은 talk 화면과 요청 본문에 오답을 명시한다. 같은 오답 항목을 입력하면 한 절을 따른 구현은 talk에서 제외하고 다른 절을 따른 구현은 표시한다. 금칙 적용 경계와 구현 범위를 동시에 동결할 수 없는 상태다.

**[P1] 되돌림 통로가 완료 여부만 돌려줘 학습 답안을 운반하지 못한다** — §⑥·§⑧, 159·198행. talk에서 선택형 답을 받아도 명시된 되돌림은 「배정ID별 항목 충족」뿐이며, 고른 답·확신도·시도·응답 시각을 `quiz_log`로 보내는 규격이 없다. 기존 `quizSweep_()`도 문항을 `contents`·`ai_daily`에서 찾고 `자율일배정`은 읽지 않는다. 따라서 학생이 모든 항목을 제출해도 완주 집계만 성공하고 재출제·약점 분석에 필요한 답안은 도착하지 않는 구현이 문서상 허용된다.

**[P1] 원오답 행 ID와 재제출 사건 ID는 호환되지 않는다** — §⑥·§⑧, 160·197·213행. 요청은 `quiz_log_id`를 주는데 제출은 `retry_of_event_id`를 사용하라고 한다. C0에서 후자는 실제 사건 UUID 또는 그 사건의 멱등키이며, 기존 퀴즈 로그 행 ID를 사건으로 바꾸는 대응표·변환 규칙은 없다. 퀴즈 행 ID를 그대로 넣으면 거절되고, 고리를 생략하면 어떤 오답에 대한 재도전인지 사라진다.

**[P1] 문형별 오답을 고를 열쇠가 기존 퀴즈 기록에는 없다** — §③-㉡·㉢·§⑥, 80·96·161·164행. 기존 `quiz_log` 헤더에는 문형 번호가 없으므로 「그 주 문형 우선」과 「8주 동안 놓친 문형 셋」을 무엇으로 계산하는지 정해지지 않았다. 묶음에서 문형이 따라온다는 설명은 새 자율일 제출에만 적용되며 평일의 기존 퀴즈까지 해결하지 않는다. 또한 오답 공급원은 `quiz_log`로 한정하면서 진단 약점도 다음 오답 자리로 간다고 하므로, 진단 세션을 연습 문항으로 바꾸는 소비 경로가 비어 있다.

**[P1] 다시 맞힌 결과를 읽어 약점 상태를 바꾸는 소비자가 없다** — §③-㉡·§⑥, 80·159행. 명시된 `퀴즈오답맵_()`는 후속 시도를 제외하고 첫 오답·찍맞만 읽으며, 자율일 선택 규칙에도 해결된 원오답을 구별하는 조건이 없다. 최초에는 틀렸지만 재도전에서 맞힌 입력을 넣어도 옛 오답은 계속 후보에 남는다. 원오답 고리를 저장하는 것과 그 고리로 학습자 이해를 갱신하는 것은 다른 일인데, 후자가 빠졌다.

**[P1] 이월 우선 규칙이 이번 주 문형 학습을 밀어낸다** — §③-㉡·㉢·§⑤, 83·90–96·143행. 지난주 굳히기를 **0/8항목** 수행한 학생에게 이월을 우선하면 이번 주 굳히기 **8/8자리**가 지난주 문형으로 채워진다. 이는 「그 주 문형 둘로 문항을 짓는다」 및 「지난주와 같은 일요일은 원리상 없다」와 충돌한다. 상한을 지킨다는 이유로 이번 주 목표를 포기해도 되는지, 이월 항목의 성취를 어느 주에 귀속할지도 정해지지 않았다.

**[P1] 14일 창은 미배정 오답의 다음 주 생존을 보장하지 않는다** — §③-㉡, 80·83행. 첫 토요일에 이미 열흘 된 오답 후보 중 **5/12문항**을 배정하면, 다음 토요일에는 나머지가 발생 후 열이레가 되어 창 밖으로 나간다. 미배정 **7/12문항**은 「지난주에 못 한 배정 항목」도 아니므로 이월 규칙으로 구제되지 않는다. 「나머지는 버려지지 않는다」는 보장은 현재 규칙으로 거짓이다.

**[P1] 배치가 아예 실행되지 않은 공급 실패는 집계에서 사라진다** — §③-㉠·§④·§⑧, 60·121·196·202행. 공급 실패를 묶음 행에만 적고 묶음별로 집계하므로, 토요일 배치가 시작되지 않거나 행 생성 전에 죽으면 실패를 담을 행 자체가 없다. 다리 설정 누락도 「0초 스킵」 이후 어떤 실패 상태를 반드시 남기는지 연결되지 않았다. 배정 대상 명부와 실제 발행 결과를 대조하는 규칙이 없으면, 전원 미공급과 집계할 학생 없음이 같은 모양으로 끝난다.

**[P1] 일부 공급 장애가 정상적으로 수행한 항목까지 가린다** — §③-㉢·§④·§⑤, 100·121·144행. 굳히기만 생성 실패해도 나머지는 내보내지만, 묶음이 공급 실패이면 학생 쪽 판정을 하지 않는다. 학생이 공급된 말하기와 필수진단을 모두 마쳐도 전체 완주 상태에 의존하는 표 채움·편지·후속 집계에서 그 수행이 가려질 수 있다. 공급 충족과 학생 수행을 독립적으로 표현하지 않아 우리 장애가 학생의 성취까지 지운다.

**[P1] 화요일 재집계로 월요일의 ‘전송 대기’를 알 수는 없다** — §④·§⑨, 122·128·228행. 월요일 서버에서 관측되지 않은 제출은 앱의 오프라인 큐에 있는지, 아직 수행하지 않았는지 구별할 수 없다. 화요일에 도착한 것을 재집계하는 것은 사후 정정이며, 월요일에 큐 상태를 확인한 증거가 되지 않는다. 집계 문구에서도 전송 대기를 빼고 있어, 확인되지 않은 부재가 학생의 「빈 날」로 먼저 전달되고 그 정정이 어디까지 전파되는지도 없다.

**[P1] 필수진단 항목이 어느 진단 세션으로 충족되는지 정하지 않았다** — §③-㉡·§④·§⑧, 81·118·214행. 항목에는 회차만 있고 수강 기간·진단 역할·고정 기준판·해당 세션을 연결하는 규격이 없다. 동일 학생에게 같은 회차의 연습·재시도 세션이 함께 있으면 어느 완주를 인정할지 구현자가 결정하게 된다. 또한 다음 날 판정만 정의돼 있어, 차시 정본이 요구하는 당일 미완료 재안내와 마지막 날 안에 끝 진단을 받는 흐름도 비어 있다.

**[P1] AI 응답 성공과 문항 발행 가능을 구별하지 않는다** — §③-㉢·§⑨·§⑩, 98–100·226·239행. 실패 조건은 키 없음·응답 실패이고 사람의 훑기는 첫 주뿐이라, 이후에 새로 생성되는 문항의 발행 판정이 없다. 형식은 맞지만 정답이 복수인 문항이나 진단 문항을 그대로 되풀이한 응답도 이 규격에서는 공급 성공으로 들어갈 수 있다. 생성 지시문과 별개로 문항 검증·진단 문항 중복 배제·실패 처리 기준이 있어야 하며, 매주 원장 확인으로 메우면 운영 정본과 다시 충돌한다.

**[P1] 제출 존재를 실제 한국어 산출로 간주한다** — §②·§③-㉡·§④, 45·77–79·118행. 문서는 무음도 녹음 길이에 포함된다는 사실을 인정하면서, 낭독·답하기의 충족 조건은 제출 존재만 둔다. 말하기 **4/4항목**에 무음 제출이 남고 선택형도 제출되면 문서상 완주 조건을 만족하지만, 학생이 실제로 말하거나 쓴 한국어는 확인되지 않는다. 완료가 정답률을 요구할 필요는 없어도, 실제 산출과 무발화를 구별하는 조건은 있어야 판단 정본의 도움 축을 지킬 수 있다.

**[P1] ‘학생마다 짓는다’면서 학생의 목표를 생성 재료에서 뺐다** — §③-㉠·㉢·㉣, 68·98·104행. 묶음의 목표는 문형 번호이고 생성 입력은 문형 이름·설명·출제 규칙이라, 드림맵과 학생이 한국어로 실현하려는 일이 어느 입력에 실리는지 없다. 같은 문형을 배우는 학생들에게 문장만 다르게 생성해도 이 설계를 충족하므로, 학생별 배정이 학생별 목적에 맞춘 학습을 보장하지 않는다. 필요한 것은 목표 도달 자동 판정이 아니라, 이미 확정된 목표 맥락을 문장과 물음에 반영하는 연결이다.

**[P1] 다시 열 이유로 내세운 누적물이 1기 학생에게는 보이지 않는다** — §⑤·§⑧, 144·149·200행. 반복 방문의 근거를 재료 변화와 「열둘 한 장 표」로 제시하지만, 학생은 표를 열어 볼 수 없고 채워졌다는 문구만 받는다. 차시 정본은 매 차시 끝에 줄을 더해 마지막 토요일 수업에서 쓰도록 했는데, 후보는 일요일 완주 조건으로 바꿔 마지막 줄을 그 수업 이후에야 판정한다. 학생이 쌓은 것을 다시 보고 싶어 돌아오는 경험과 교실에서 써야 하는 산출물을 함께 뒤로 밀었다.

**[P1] 일요일 묶음의 전체 그림과 시작 동작이 없다** — §③-㉣·§⑧, 104–108·197행. 기존 편지의 말하기 흐름에 선택형·오답·필수진단을 더하면서 첫 화면에서 무엇을 먼저 하고 어디까지 하면 되는지 설명하는 규격이 없다. 필수진단 주에 접속한 학생이 진단의 필수 여부, 나머지 활동과의 관계, 중단 후 이어갈 위치를 어디서 아는지도 없다. 이는 화면 장식의 미구현이 아니라 판단 정본 ⑤가 요구하는 전체 그림·연결 설명의 누락이다.

**[P1] 이미 확정한 화면 방 운영을 문서가 다시 선택 사항으로 바꿨다** — §⑤·§⑩, 147·238행. `docs/_ops/결정.md:74`의 확정은 1기 일요일에 화면 방을 열어 두는 것이지만, 후보는 이를 「있으면 더 좋다」로 내리고 다시 결정할 질문으로 올렸다. 자동화 층을 보완했다는 사실은 소유자의 운영 결정을 변경할 근거가 아니다. 변경 확정 없이 동결하면 같은 일요일 운영을 두 정본이 다르게 지시한다.

읽은 파일: 후보 문서 전문과 제공된 진단 문항 전문, 양 저장소 작업 지침, 관련 절·함수 범위의 `1기_차시대응표_v1.md`, `급수진단_설계_v1.md`, `제품방향.md`, `코어엔진_설계.md`, `살아있는자리_설계_v1.md`, `_ops/결정.md`, `_ops/검수기각.jsonl`, `엔진_수집.js`, `엔진_콘텐츠AI.js`; talk의 L0·C0·P0 계약, `개발_분업.md`, `오늘과제.js`, `이벤트검증.js`, `말하기화면.js`, `deliver/index.ts`, `tasks/index.ts`.

안 본 것: 라이브 시트·DB·앱 화면, M0 별도 실물, 아직 작성되지 않은 자율일 수신·집계 구현. 재현은 로컬 순수 함수 범위다.

**대안 구조 1개 — 초안·불변 발행본·원본 응답·파생 집계를 구분한다.** appsscript는 재료를 만들고, talk의 기존 배정 사건을 학생에게 실제 발행한 묶음의 정본으로 삼는다. 발행본에 항목 식별자·스냅샷 판·목표·출처 고리·진단 세션 연결을 확정하고, 원본 응답에서 퀴즈 기록과 완주 집계를 파생시키며 공급·수행·도착 상태를 따로 계산한다. 이월은 이번 주 목표와의 우선순위를 명시하고, 학생에게는 기존 편지 안에서 실제 제출한 문장과 누적 결과를 다시 볼 수 있게 돌려준다.