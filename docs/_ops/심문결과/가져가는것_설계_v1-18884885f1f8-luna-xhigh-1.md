[P0] 새 그릇이 엔진에 닿는 배선이 없다 — §2-c·§6은 Drive/시트 적재와 BY·BX·인쇄 소비자만 적고, Supabase 원본·L0 이벤트·C0 API·엔진 소비자를 정의하지 않는다. 시트에 행이 생기는 것과 엔진이 읽고 이해 대장 칸을 채우는 것은 다르며, `portfolio_moments`의 A-1 귀속도 없다.

[P0] 학생 연결 키가 불충분하다 — §2-d의 `class_snapshot`만으로는 같은 반의 여러 차시, 보강 수업, 같은 날짜의 복수 수업을 구분할 수 없다. `portfolio_moments`와 `attendance` 사이에 `session_id`·실제 수업일시·이벤트 키가 없어 여러 학생에게 잘못 붙거나 아무 학생에게도 붙지 않는다.

[P0] 사건일과 적재일이 분리되지 않았다 — §2-c의 `담긴날`은 자동값인데, §3-b는 사진을 다음 날이나 나중에 올리는 상황을 전제로 한다. 그러면 「그날의 순간」, `BX` 오늘 알림, 월간 봉투의 월 분류가 업로드일인지 실제 사건일인지 결정되지 않아 늦게 올린 순간을 복원할 수 없다.

[P0] 미디어 스키마가 실제 대상을 담지 못한다 — `portfolio_moments`에는 `photo_file_id` 하나뿐인데 §2-c 예시는 사진 두 장이고 §3-c는 영상까지 같은 폴더에 넣는다. 폴더 단위 순간 ID, 미디어 종류·순서·목록·영상 참조가 없어 재실행 때 한 순간을 여러 행으로 만들거나 사진·영상을 조용히 버리게 된다.

[P0] 멱등성이 성립하지 않는다 — §6의 `file_id` 중복 제거는 동시 실행을 막는 유일 제약이나 잠금이 아니다. §14-a⑨의 복사본은 새 `file_id`를 만들지만 원본 ID를 저장하지 않으므로, 재실행 때 같은 파일을 계속 복사하고 중복 행을 만든다.

[P0] 체험 기간의 순간에 유효한 시즌 키가 없다 — §3-a는 2월 11~24일이 시즌 밖이라고 못 박았지만, §2-c의 새 행은 날짜에서 `시즌`을 유도하도록 되어 있다. 체험 첫날 이름표를 저장할 `experience_id`나 비시즌 키가 없으므로 첫 소급불가 증거부터 현재 스키마에 들어가지 않는다.

[P0] `crew_projects` 자동 파생 주장이 거짓이다 — §3-b는 폴더명과 출석으로 프로젝트명·결과물 링크·공개일·참여크루까지 채운다고 하지만, 폴더 문법에는 날짜·반·차시·종류·한 줄만 있다. 출석은 반 출석자일 뿐 프로젝트 참여자나 완성 여부를 증명하지 못하므로 §6 걸음 3은 실행 불가능하다.

[P0] 목표 달성 여부를 자동 출력하는 것은 판단 정본과 충돌한다 — §9-b는 증서 세 줄 중 하나를 `나침반 목표 달성 여부`로 자동 채운다고 한다. 판단 정본은 목표에 가까워졌는지를 시즌 회고에서 사람이 판정한다고 했으며, 그 사람 판정 필드나 승인 사건은 이 문서에 없다.

[P0] 이름표를 개인 기록에 귀속할 방법이 없다 — §2-d·§5-b는 학생별 폴더와 이름 태깅을 금지하면서, 반 전체 순간 사진을 각 학생 기록장에 넣는다. 반 사진 속 어떤 손글씨 이름표가 누구 것인지 시스템은 알 수 없으므로 「그 아이의 첫 이름표」를 재현할 수 없다.

[P0] 졸업 때 약속한 원본 파일을 보장하지 못한다 — §3-d는 시스템에 긴 변 2,000px로 줄인 파일만 남기고 원본은 강사 휴대폰에 둔다. 그런데 §5-a는 졸업 때 사진 원본을 직접 건넨다고 약속하며, 중앙 보관·백업·파일 목록·전달 검증 절차가 없다.

[P0] 완결 게이트의 네 칸이 실제로 기록되지 않는다 — 판단 정본은 모였나·닿았나·늘었나·누구에게를 각각 ✓/✗로 보드에 쓰라고 했지만, 이 문서에는 어느 산출물에도 그 네 상태가 없다. BY·BX·인쇄는 화면 소비자일 뿐 엔진 성장이나 실제 학생 전달의 증거가 아니므로 동결 후 완료로 위장될 수 있다.

[P0] 이미 발견된 거짓 화면을 선택사항으로 남겼다 — §10-c와 §16-⑥은 `jacket_grants` 지급 후에도 목표 카드가 계속 「곧 전달」이라고 거짓말하는 결함을 인정한다. 유호님이 별건을 선택할 수 있게 두면 이 트랙은 물건을 배포하면서 학생 화면의 사실오류를 함께 출시하게 된다.

[P0] 자기 선언의 최초값이 사라진다 — §2-b의 `self_declare_log`는 「바뀐 것만」 야간 append한다. 처음 입력한 값이 이후 바뀌지 않으면 로그에 없고, §5-b의 「1시즌의 나 → 6시즌의 나」를 재구성할 수 없다.

[P1] 시즌 회고 병치의 API 계약이 없다 — §6 걸음 4가 근거로 드는 `지난5년답`은 단일 답 반환의 근거일 뿐, 여러 시즌의 `season_compass`·`season_review` 이력 목록이 아니다. 과거 시즌 배열의 응답 형식·정렬·빈 값·버전이 정의되지 않아 「한 줄 추가」라는 설명만으로는 구현할 수 없다.

[P1] 사진 수집은 자동화가 아니라 반복 수작업이다 — §3-b는 강사가 휴대폰에서 사진을 골라 업로드하고 폴더를 찾고 이름 끝에 문장을 붙이는 과정을 전제로 한다. 오프라인 업로드 큐, 성공 확인, 재시도 상태, 수정된 폴더의 재처리 규칙이 없으며, 다음 날 목록은 잃어버린 사진을 복구하지 못한다.

[P1] 비교 금지 계약이 런타임에서 강제되지 않는다 — `achievements`, `season_review`, 폴더의 한 줄, 교사 문구는 모두 동적 텍스트인데 §12-c의 `발표물린트`는 정적 HTML만 검사한다. 새 `기록장조판`, `증서조판`, Apps Script 인쇄 결과의 숫자·등수·평균·상위 표현을 막는 검증이 없다.

[P1] TOPIK 공식 결과와 모의 결과를 섞는다 — §2-b는 `TOPIK·모의`를 한 항목으로 묶고, §5-b 표지는 `마지막 급수`만 출력한다. 판단 정본의 실제 게이트는 공식 TOPIK 4급이고 3급·EPS-TOPIK은 별개이므로, 이 기록장은 모의 4급을 공식 4급처럼 보이게 하거나 시험 종류를 지워 버린다.

[P1] 학생 경험에 반복 사용 이유가 없다 — §4는 BY에 최근 한 줄을 넣고 BX에 「오늘 하나 담겼어」를 띄우지만, 학생이 말하거나 쓰거나 선택하거나 다시 열어 볼 행동이 없다. 사진은 화면에 표시하지 않으므로 새 기능이 실제 한국어 사용·재미·신선함 중 무엇을 만들어 내는지 답하지 못한다.

[P1] 자기설명 게이트를 통과하지 못한다 — 「오늘 하나 담겼어」는 무엇이 담겼는지와 다음에 무엇을 해야 하는지를 설명하지 않는다. §4-a에는 상세 보기·확인·회고 같은 행동 경로가 없어서 처음 본 학생이나 학부모가 기능의 정체를 알 수 없다.

[P1] 채움 사다리의 개인 재료 보장이 틀렸다 — §5-b-2는 첨삭, 나침반, 해낸 일, 카드가 결석과 무관하게 항상 있다고 단정한다. 실제로는 숙제 제출·업적·카드 획득이 조건부이고, 순간도 0일 수 있으므로 고정 2쪽이 빈칸이나 반 공유 사진으로 채워질 수 있다.

[P1] 몽골어 검수는 일정이 아니라 미확정 차단자다 — §11-e는 검수자 0명이고 새 물건 11종이 기존 발주서 밖이라고 적었다. 그런데 §13은 2026-09-10 계약과 11월 마감을 확정된 것처럼 사용하며, 담당자·수락 기준·실패 시 대체 경로가 없어 학부모용 물건을 검수 대기 상태로 동결한다.

[P1] 일정 정본이 현재 시점과 서로 맞지 않는다 — 문서는 오늘을 2026-08-26으로 계산하지만 현재 기준일은 2026-09-01이라 모든 D-N 표기가 이미 6일 낡았다. 더구나 §13-a의 2027-01-05~01-29 평일은 월~금 기준 19일인데 18일로 계산했고, 파일럿 시작일도 12-01과 12-07~18 사이에서 미해결이다.

[P1] 월간 봉투는 기존 함수의 메뉴 추가로 끝나지 않는다 — §10-b는 리포트카드·카드 절취면·사진·접힘면을 한 판에 배치한다고 하지만, §6 걸음 2에는 학생별 페이지 조합·양면 위치·접힘선·프린터 여백 검증이 없다. `@page margin 0`은 하드웨어의 비인쇄 영역을 제거하지 않으므로 §11-f의 프린터 미측정 상태에서는 핵심 월간 산출물이 깨질 수 있다.

[P1] 「직접 건네는 파일」은 산출물 계약이 아니다 — §5-a·§6 걸음 5는 USB나 본인 기기에 직접 준다고만 하고, 포함 파일 목록·정렬·파일 누락 검사·전달 완료 기록을 정의하지 않는다. 전달 시점에 무엇이 실제로 넘어갔는지 재현할 수 없다.

[P2] 소급 레일과 장식 생산을 한 동결 범위에 묶어 과설계했다 — §6의 최소 필수 작업은 사건 원본과 엔진 배선인데, 문서는 동시에 두 생성기·월간 봉투·시즌 증서·태그·기록장·11종 번역·색 로테이션까지 끌어들인다. 예산·프린터·크루 정책·저장소가 미정인 상태에서 15개 품목을 얼리면 핵심 레일보다 운영 실패면적만 먼저 고정된다.

이 문서보다 나은 대안 구조 1개: `불변 사건 원본(Supabase) → 버전 있는 학생별 파생 뷰 → 발행물·전달 사건`의 3층으로 분리한다. 먼저 `event_id`, `session_id`, 실제 사건시각, 미디어 목록, 참석 스냅샷, 원본 참조를 가진 최소 원본과 C0 소비자만 동결하고, 기록장·봉투·증서·굿즈는 그 파생 뷰를 검증한 뒤 별도 발행한다.