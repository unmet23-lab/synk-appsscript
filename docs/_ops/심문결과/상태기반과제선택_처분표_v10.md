# 상태기반 과제선택 — 처분표 v10 (10회차 · 대상 설계 v5.6)

> **런** `심문-2026-08-14T1332-cf896f`(codex 3벌 · 36분) · 대상지문 `4a98a0a91252`(= v5.6) · 편성 luna×2+sol×1(모델정책 고정값 · 효력 max)
> **원건 91** — **두 통로 합집합**: codex **77**(luna-1 **24**·luna-2 **34**·sol-3 **19** · P0 47·P1 30 · 커밋 `71c8062d`) + fable **14**(P0 3·P1 6·P2 5 · 커밋 `6b18e681`). 결과 4벌 = 같은 폴더 `...-4a98a0a91252-{luna-1,luna-2,sol-3,fable-10}.md`.
> **게이트**(유호 확정 08-12): ①판정 = 원건 하나도 빠짐없이 채택/부분/기각/범위밖 + 근거 · ②집행 = 채택분이 본문에 들어왔다. 둘 중 하나라도 비면 ⛔착수 차단 유지.
> 🚫 이 표로 처분표 v1~v9 를 덮지 않는다(회차 대조). 🚫 「몇 벌이 같은 말을 했나」는 가중치지 증명이 아니다.
> 🚫 **모델에게 병합시키지 않았다** — 키 = 파일+제목 합집합 · 등급이 갈리면 **나쁜 쪽 승**(P0>P1>P2).
> **①판정 = ✅ 종결 — 91/91 매핑 · 58갈래 ✅43 · ◐11 · ❌3 · ⧉1. ②집행 = ✅ 종결 54/54 — 설계 v5.7**(§8 도장 · 집행자 조정 10건 = §7).

## §1. 조건 지문 — 회차 «안» 통제 3번째 성립 · 편성이 이 회차를 마지막으로 바뀐다

| 회차 | 상태 | 방향 | 철학 | 금지 |
|---|---|---|---|---|
| 10회차 던짐(13:32Z) | 3벌 동일 | `c5ee401c21db` | `0e7e473e648e` | `1b8e0780e704` |
| 10회차 완료(14:08Z) | 3벌 동일 | `c5ee401c21db` | `0e7e473e648e` | `1b8e0780e704` |
| (대조) 9회차 던짐·완료 | 3벌 동일 | `fdce7ffafff1` | `486e20d3af4d` | `c878a65c95a3` |

던짐↔완료 짝 **6/6 동일** — F411 수리가 **3번째로** 성립(회차 «안» 통제). 9↔10회차 지문 3종은 **전부 갈렸다**(원인 = 9회차 처분 자신이 낸 §13 v5.6 증보 12건). **따라서 76→77 증감을 조건 동일 전제로 읽으면 안 된다.**

⚠ **원건 수를 앞 회차와 직접 빼지 마라 — 이 표만 분모가 둘이다.** 91 = codex 3벌(77) + fable 1벌(14)이고 1~9회차는 codex 3벌만이다. 그리고 **11회차부터 편성이 luna/max 1벌**로 바뀐다(유호 08-14 `4ed6ebea`) — 「지적 수가 안 준다」를 다음 판에서 이 표와 비교하면 편성 차이를 품질 변화로 읽게 된다.

⚠ **`/fable심문` 통로는 08-14 유호 지시로 폐지됐다**(「너무 낭비」 · 스킬·도구·`심문자.md` 삭제 `4ed6ebea`). 결과·장부는 증거라 보존한다. 🚫부활 재제안.

🔑 **이종 계보 교차확인 5건** — fable 판이 값한 자리는 「codex 가 못 본 것」이 아니라 **둘이 독립으로 같은 급소를 짚은 것**이다(그 5건은 「심문자 오독」 가설이 닫힌다): D1(A8 draft) · B1(마감 게이트) · B7(lease null) · C1(회전 결합) · E3(§12-19 미수거).

## §2. 갈래 표 — 원건 91 → **58갈래** · 판정 확정

표기: L1-n = luna-1 지적 n · L2-n = luna-2 · S-n = sol-3 · **F-n = fable**(등장 순서 번호). 판정 = ✅채택 · ◐부분 · ❌기각 · ⧉범위밖. 근거 줄번호는 §3 실측.

### A군 — 실행 장부 물리 계보 (16)

| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| A1 | `batch_run_id` FK 가 `assign_date`·스냅샷을 결속 안 함 → 「그날 정본 실행」이 단수로 존재하지 않는다 | L1-5·L1-13·L2-30·S-5 | ✅ **3벌 만장** — `batch_runs` 에 `(run_id, assign_date)` 유니크 + jobs 복합 FK · 「그날 정본 실행」 = 그 날짜의 `finished_at` 이 있는 **최신 run** 하나로 규칙 명문(감시가 고르는 자리) · `적재실패` 부활 갱신표에 `batch_run_id` 편입. v5.6 A1 이 `run_id` 를 신설하며 **결속을 안 걸었다** |
| A2 | 재적 전량 커버리지가 증명되지 않는다 — `enrolled_count` 는 숫자뿐, 명단·급수 분포 미저장 | L1-6·L2-1·S-4 | ✅ **3벌 만장** — 실측: `target_count=0·loaded_count=0·partial_count=0·finished_at≠null` 이 재적 17에서 **합법**(:1163) ⇒ 전원 누락이 네 감시 항을 다 통과한다. 처방 = 시작 행에 **명단 스냅샷**(학생 목록 해시 + 급수 분포 jsonb) + `target + 대상아님 + 적재실패 = enrolled` 등식 CHECK. 🔑 E8 의 분포 «출처»도 이 칸이 진다(세 심문자 대안이 모인 자리 = 매니페스트) |
| A3 | `batch_runs` 시작 행의 물리 쓰기 통로가 없다 | L2-7 | ✅ 아홉 RPC 어디에도 시작 행 생성 함수가 없는데 v5.6 A4 는 「별도 트랜잭션으로 먼저 커밋」을 요구한다 ⇒ **열째 RPC `batch_run_start(_assign_date, _batch)` 신설**(멱등 반환 규칙 포함) · 판지문 재료 10 을 「아홉」→「열」로 |
| A4 | `deciding_attempt_id` 를 채울 **인자가 없다** | L2-4 | ✅ **실측 확인** — DDL `jobs_deciding_scope` 는 여섯 실패값에서 `not null` 을 요구(:886~892)하는데 `jobs_finalize` 시그니처에 그 인자가 **없다**(:1434~1437) ⇒ 실패 착지가 원리상 불가. v5.6 A4 는 «CHECK 경유»만 닫고 **값이 들어오는 통로**를 안 열었다. 처방 = `_deciding_attempt_id uuid default null` + 「어느 시도가 결정을 냈나」 = 그 fence 의 마지막 닫힌 시도 |
| A5 | 실행판이 jobs·batch_runs 는 6칸인데 attempts·`attempt_open` 은 **4칸** | L2-5 | ✅ **실측 확인** — attempts 는 `model`·`prompt_ver`·`policy_ver`·`estimator_version` 넷뿐이고 `schema_ver`·`skill_taxonomy_ver` 가 없다 · `attempt_open` 인자도 넷(:1240~1242) ⇒ 「한 배정일에 판이 안 섞인다」가 시도 층에서 안 선다. 처방 = 2칸 + 인자 2 + job 값 대조 거절(B9 사유 반환에 `판불일치` 이미 있다) |
| A6 | 구제 RPC 가 `NOT NULL` 값을 만들 수 없다(`snapshot_as_of`) | L1-3·S-2 | ✅ ㉠ 이 `_targets` 에서 그 칸을 걷었고 ㉨ 은 `_target` 이 같은 스키마라 했는데 물리 열은 `not null` ⇒ 삽입 단계에서 막힌다. 처방 = `jobs_load_one` 이 구제 시점 값을 **스스로 굳힌다**(:1554 「새로 굳힌다」 규칙의 물리 대응) + 시그니처에 명시 |
| A7 | 「별도 트랜잭션」이 PostgreSQL RPC 에 **없다** | L2-3·S-1 | ✅ 자율 트랜잭션이 없으므로 예외가 그 행까지 롤백한다 ⇒ 구제 실패가 다시 「행 0·상태 0」(B8 이 막으려던 바로 그것). 처방 = **예외를 안 올린다** — 같은 트랜잭션에서 `적재실패` 행 + typed 결과(B7 갈래 봉투 `kind='조립실패'`)를 커밋(sol-3 대안 그대로). ⚠ **A3 와 가른다**: 시작 행은 오케스트레이터가 **별도 «호출»**로 커밋하므로 A3 는 그대로 선다 — 불가능한 것은 ㉨ «안»의 자율 트랜잭션뿐이다 |
| A8 | 재실행 가드(`if not exists`·`duplicate_object` 무시)가 스키마 불일치를 숨긴다 | L1-9·L2-16 | ✅ 같은 이름·다른 타입·다른 조건이 그대로 통과하고 「오류 없이 끝났다」만 검사한다 ⇒ 첫 append-only 행이 약한 옛 계약 아래 저장될 수 있다(소급 불가). 처방 = §12-21 에 **계약 «동일성» 검사**(열·타입·제약 집합을 기대값과 대조) — 재적용 성공은 동일성의 증거가 아니다 |
| A9 | `대기`+`not_target_reason` 조합을 물리로 막고 `jobs_claim` 이 사유를 읽게 하기 | L1-11 | ❌ **기각 — 네 판째 재론.** v5.4 명시 기각(`:810-814`) → v9 A10 기각 → §13 v5.6 증보 **첫 항**이 그 기각을 문서에 박았다. 논거: 양방향 CHECK 면 적재 트랜잭션 «안»의 전이가 깨지고, claim 이 사유를 읽으면 **오배제**(정상 job 영구 제외)가 오포함보다 크다. 오염 행은 §12-21 이 «센다». **10회차 신규 사실 0** |
| A10 | `level` **값목록이 실제로 없다** — 규칙이 가리키는 정본 칸이 비어 있다 | L1-17·L2-20·S-7 | ✅ **3벌 만장 · 실측 확인** — 적재 검사 ⑤ 가 「아래 `branch_snapshot` 표의 `level` 칸이 **정본 목록을 진다**」고 가리키는데(:1293~1294), 그 표 칸(:1591)은 「`level_snapshot` 과 **같은 값**을 쓴다」뿐 **리터럴이 0개**다 ⇒ ⑤·⑥(초급·미정 정합)이 원리상 작동 불가. 🔑 **v9 A11 이 「약속」만 하고 안 채운 자리** — v8 F4(§12-3 이 약속한 §16-1 별도 항이 없다)와 **같은 형태가 두 판 연속**이다. 처방 = 그 칸에 값목록을 실물로 적는다(⚠ **집행 시 talk 의 `level_snapshot` 실물 값을 열어 확정** — 문서에서 베끼지 않는다) |
| A11 | `target_count` 의 뜻이 둘 — DDL 주석이 정본 식과 다르다 | L2-17·S-13 | ✅ **실측 확인** — DDL 주석은 `status <> '대상아님'`(:1149)이라 `적재실패` 를 **포함**하는데 §3-6·§12-8 정본 식은 제외한다 ⇒ 저장 계기와 보고 계기가 다른 수를 낸다. 🔑 **v5.6 A13 집행이 §3-6 만 고치고 DDL 주석을 안 걷었다**(미수거) |
| A12 | 비공백 CHECK 가 공백 문자열을 허용한다 | L2-32 | ✅ **실측 확인** — `jobs_ver_nonempty`·`batch_runs_ver_nonempty` 가 `<> ''` 만 본다(:928·:1169) ⇒ `"   "` 통과. 처방 = `btrim(x) <> ''` 로 6칸(+A5 확장분 2칸) |
| A13 | `적재실패` 재처리가 최초 실패의 계보를 덮어쓴다 | L1-12·L2-8 | ◐ 갱신표(:1554)가 새 값으로 덮고 `load_error` 200자만 남긴다. **전체 이력 보존은 면적이 크다** ⇒ 최소 3칸으로 닫는다: `load_failed_at`·`load_fail_run_id`·`load_retry_count`. 「최초 `_target` 과 실패 당시 전체 입력을 불변 행으로」는 **안 쓴다**(표 하나가 더 선다) |
| A14 | `branch_snapshot` 이 영구 증거가 아니다(job 갱신·삭제 시 복원 불가) | L2-15 | ◐ 알맹이 채택 · 새 표 0 — **job 보존 계약 1항**(적재 후 `branch_snapshot` 불변 · 삭제 금지 · 재적재는 A13 이력으로). 「아카이빙 계약」은 안 짓는다(개원 전 · 첫 학생이 만료 조건) |
| A15 | 추정기 판 계보가 강제되지 않는다 — `policy_ver` 재료에 상태 추정기가 **없다** | L2-22 | ✅ **실측 확인** — 재료 열둘(:2107~2131)에 `lib/학습자상태.js` 가 없고, `estimator_version` 은 코드가 내는 문자열일 뿐 증가 강제가 0이다 ⇒ 추정기가 바뀌고 문자열이 그대로면 **같은 판·같은 추정 버전 아래 다른 상태 입력**이 쌓인다(소급 분리 불가). 처방 = **재료 13 신설 = `lib/학습자상태.js` 파일 해시**. ⚠ 대가 = 그 파일의 무관한 수정에도 판이 오른다 — 재료 12(`오늘과제.js`)가 이미 받아들인 「안전한 실패」 논거가 그대로 선다 |
| A16 | `attempt_open` 이 재시도에 멱등하지 않다 | L1-18 | ✅ `attempt_no = max+1` + `(job_id, attempt_no)` 유일키뿐이라 응답 유실 후 재호출이 **중복 attempt = 벤더 중복 호출**이 된다(돈이 나가는 자리). 처방 = 같은 fence·같은 `_request_body` 해시의 **열린 시도가 있으면 그것을 반환**(새 행 0) + 반납 상태 규칙. B7 감시와 한 벌 |

### B군 — 마감·구제·감시 경로 (12)

| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| B1 | **마감 게이트(B3)가 구제 경로 전체를 벽돌로 만든다** — 배치 전멸일에 학생이 하루 종일 빈손 | F-1·L2-2 | ✅ **P0 · 이종 교차 · 실측 확인(전 세션)** — 마감은 06:00 UB 이고 학생이 앱을 여는 시간대는 **전부 그 뒤**인데 구제는 `jobs_claim`(마감 뒤 0행 `:1483`·`:1487`)과 `jobs_finalize('정상')`(마감 뒤 거절 `:1542`) **둘 다**에 막힌다 · `_mode` 열거 실측 = 정상·마감·적재 **셋뿐**(구제 모드 없음) ⇒ 그 학생은 `task.assigned` 조차 0(그날 분모 행 **영구 부재 · 소급 불가**)이고 현행 라이브보다 **후퇴**한다. 처방 = **fable 대안 그대로** — 마감을 「벤더 마감」과 「배달 마감」으로 가른다: 마감 거절은 **벤더 경로에만** 걸고, 벤더 0이 계약인 구제에 **넷째 모드 `_mode='구제'`**(가드 = attempts 0행 강제 + `event_draft`/신규 조립만 착지 + outcome ∈ 구제경로). 🔑 B3 의 원 목적(마감 뒤 벤더 호출 0)은 물리 가드로 **더 강해지고 새 표·새 칸 0**이다. ⚠ §12-11 픽스처가 시계를 마감 «전»으로 고정해야만 통과한다 — **픽스처 초록·운영 사망이 같은 얼굴**이라 그 픽스처도 같이 고친다 |
| B2 | 마감 스윕의 단건 실패가 영구 고아 job 을 만든다 | L1-8 | ✅ 워커는 **오늘 날짜만** 집으므로 과거 `assign_date` 의 `대기`·`claimed` 를 아무도 다시 안 집는다. 처방 = `jobs_finalize_due` 의 대상을 「오늘」→**「마감이 지난 미종결 전량」**으로 + 감시에 1항 |
| B3 | 전체 배치가 죽어도 감지만 하고 복구하지 않는다 · 오케스트레이터가 여전히 150초 단일 장애점 | L1-7·S-3 | ✅ 재실행 cron·RPC 가 없고 `jobs_load` **전** 타임아웃이면 시작 행만 남고 전원 job 0인 채 자동 재시도가 0이다. 처방 = **새 잡 0** — 이미 36회 도는 `generate-worker` 에 1항: 「큐가 비었는데 그 날짜 `batch_runs` 가 미완이면 오케스트레이터를 재호출한다」(멱등 재실행 · A3 시작 행이 그 판정 재료) |
| B4 | `generate-worker` 의 **실제 호출점이 없다** | L2-6 | ✅ cron 표(:553)가 잡 이름과 RPC 순서만 적고 어떤 Edge Fn·URL 이 워커 코드를 도는지 정하지 않는다 ⇒ §12-21 이 「함수명·인자 대조」를 시키는데 **대조할 이름이 없다**(cron 등록만 초록·벤더 호출 0건이 가능). 처방 = cron 표에 **실행 주체 칸** 추가 + §12-21 대조 대상 확정 |
| B5 | 「달력 게임일 ∧ job 없음」은 게임 배정의 **증거가 아니다** | L2-9·S-6 | ✅ 그 식은 「그날이 화·금」만 말한다 — 같은 날 첫날·교정문으로 게임을 안 받은 학생의 결손이 **정상 게임으로 묻혀** `partial` 경보가 사라진다. 처방 = C5 판별에 셋째 재료 = **그 학생의 `task.assigned` 가 실제 게임 행인가**(`task_type` 실측). ⚠ **§13 v5.6 의 「`is_game_day` 로 되돌리기 금지」와 다른 처방이다** — 되돌리는 것이 아니라 달력(필요조건)에 실물 확인을 더하는 것 |
| B6 | 0-job 배치의 원인이 영구히 구분되지 않는다 | L1-14 | ◐ 대부분 **A2(명단 스냅샷) 집행으로 닫힌다**. 남는 것 = 읽고 버리던 `calendar_game_day` 를 시작 행에 **굳히기**(새 칸 1) |
| B7 | 감시 ④(열린 시도)가 `lease_until` **null 에 눈이 멀다** | L2-18·F-6 | ✅ **P1 · 이종 교차 · 실측 확인** — 판정식이 「job 의 `lease_until` 이 지난 행」(:606)인데 회수(:1492)·반납(:1503)·회수 전이(:1871)가 그 칸을 **비운다** ⇒ `null < now()` 는 UNKNOWN 이라 **그 행이 안 세어진다.** 새는 대상이 **B10 의 표적 그 자체**(응답 뒤 죽어 회수된 job 의 버려진 벤더 호출 = 돈이 나간 자리). 🔑 v5.6 이 「감시가 자기 대상의 부재를 정상으로 읽는다」 축을 닫으며 **새로 지은 감시에서 같은 형태가 났다** — 이 문서가 A2·A4·S-9 에서 세 번 기소한 3값 구멍이다. 처방 = 「`result is null` **그리고** (job 의 `lease_until` 이 지났거나 **null 이거나** job 이 종결됐다)」 |
| B8 | 라이브 배달과 야간 배치 사이의 경쟁이 닫히지 않았다 | L2-10 | ◐ 알맹이 채택 · **새 락 0** — 기존 멱등키 `task:{learner}:{날짜}` 가 이미 예약키다(v2 확정 · §3-5). 그 키를 「기존 사건 조회 → job 생성」 구간에 거는 규칙을 명문화한다. 「격리 수준·별도 락」은 안 짓는다 |
| B9 | `deliver-check` 신기준에 **활성 시작일이 없다** | F-11 | ✅ 워커·마감 잡은 「활성 시작일부터 등록」이 명문인데 기준 교체(4항)엔 그 단서가 없다 ⇒ 배포~활성 사이 **매일 거짓 적색**이고, C4 가 스스로 인용한 「매일 빨간 화면은 곧 안 보게 된다」를 자기 손으로 만든다. 처방 = 4항 전부에 같은 단서 |
| B10 | B4 재적재 전이가 **상태 전이표에 미수거** | F-10 | ✅ 전이표의 `적재실패`→`대기` 행 주체가 「구제 `jobs_load_one`」뿐인데 v5.6 B4 는 같은 전이를 `jobs_load`(오케스트레이터 재실행)에도 열었다 · §12-25 대조는 RPC 이름 실재만 봐서 이 누락을 못 잡는다. 처방 = 전이표에 주체 추가 |
| B11 | 마감 «전» `적재실패` 의 `assignment_status` 가 없다 + `claimed` 를 lease 무관하게 「생성중」으로 | L2-23 | ✅ 앱이 `없음` 으로 그리거나, **죽은 워커의 job 을 학생에게 「곧 온다」**고 알린다. 처방 = 마감 전 `적재실패` → `생성중` 편입 + `claimed` 는 **lease 가 유효할 때만** 생성중(만료면 재시도 대기) |
| B12 | `생성중` 이 학생 화면의 막다른 상태다 | L1-22 | ◐ **상태값 전달은 기실재**(§11-2 가 `assignment_status` 를 싣고 신앱이 다르게 그린다) · 남는 알맹이 = 「재시점·다음 행동 문구」인데 그건 **학생 접점 카피**라 이 트랙이 확정할 자리가 아니다 ⇒ §14 에 별건으로 적고(synk-brand 눈금) 인수 조건엔 「문구 칸이 비어 있지 않다」만 |

### C군 — 결정성·입력 조립 (8)

| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| C1 | 기술 회전의 「안 겨냥 우선 × 시작 위치」 **결합 규칙이 비결정** | L2-27·F-8 | ✅ **P1 · 이종 교차** — ⓐ정렬된 목록 위를 시작 위치부터 순회하며 겨냥분 건너뛰기 / ⓑ안 겨냥 부분집합을 만들어 그 크기로 mod — **둘 다 표의 문장을 만족하면서 다른 기술을 낸다.** C1·C2 가 목록의 «앞단» 결정성은 닫았는데 **선택 함수 자체가 열려 있었다.** 처방 = ⓐ를 의사코드로 못박는다(첫 처치 의도는 append-only 라 못 고친다) |
| C2 | `skill_ids` 가 생성 «입력»에 연결되어 있지 않다 — 겨냥이 장식 필드가 된다 | L2-14·S-12 | ✅ §6-0 은 기술을 먼저 고르는데 §5-3 요청 계약에 **기술 ID·이름·정의를 렌더링하는 규칙이 0**이다 ⇒ 기술은 job·사건에 기록되지만 모델 산출엔 영향이 없고, 그러면 **성과를 기술별로 조인하는 학습 사슬 전체가 거짓**이다. 처방 = §5-3 에 렌더링 규칙 신설. ⚠ **7축에 「겨냥 적중」을 새 축으로 넣지 않는다**(사람 채점 면적) — 겨냥 기술 언급 여부는 **기계 검사**로 셀 보고에 싣는다 |
| C3 | 중복 이력 조회가 다른 생산자를 섞을 수 있다 | L2-26 | ✅ ②' 조인이 `intervention.delivered`+`generation_outcome='성공'` 만 보는데 c12 는 다른 생산자도 그 키를 선택적으로 갖게 한다 ⇒ 남의 개입 문장이 중복 이력에 들어온다. 처방 = 조인에 생산자 범위(`task_type='발화녹음'` + `payload.ver`) |
| C4 | `evidence_refs` 가 절단된 **실제 분량**을 잃는다 | L2-24 | ◐ **기각 아님 — 층이 다르다.** §6-1 이 「`truncated` 는 원래 개수를 대신하는 값이 아니라 별개 사실이라 둘은 겹치지 않는다」로 닫았고(:2555~2558) 상류 `잘림` 이 개수를 담는 것도 맞다(v9 실측) — **그러나 그 값이 `evidence_refs` 에 안 실리므로 사건 소비자는 못 본다.** 처방 = **새 칸 0** — 「원래 개수는 상류 상태 객체의 `잘림` 이 지고 소비자는 거기서 읽는다」를 §6-1 에 한 줄로 적어 통로를 확정 |
| C5 | 학생에게 저장된 **질문**이 원응답과 대조되지 않는다 | L1-10 | ✅ §7 은 응답 `question` 을 ③답하기의 `프롬프트` 로 넣으라는데 ㉤ A5 대조 목록은 **문장만** 검사한다 ⇒ 원응답의 질문 A 와 학생이 본 질문 B 가 동시에 착지할 수 있고, 중복 검사·평가셋·학생 화면이 **서로 다른 질문**을 읽는다. 처방 = ㉤ 대조에 `question` 을 문장과 같은 규격으로 추가 |
| C6 | 벤더 호출 실패의 입력 저장이 물리적으로 강제되지 않는다 + 실호출 판정이 자기모순 | L2-25·S-16 | ✅ §5-2 는 「성공+시도 실패 6종은 실호출」이라는데 §4-1·§11-2 는 `입력초과` 가 **호출 «전»** 실패라고 명시한다 · `attempt_open` 도 전송 전에 생기므로 **시도 행 자체가 실호출의 증거가 아니다.** 처방 = 실호출 정의를 `attempt_close` 의 응답 유무로 다시 적고, `generation_input_text` 물리 강제를 성공 외 갈래에도 |
| C7 | `중복창_일`(14)의 물리 원천이 미지정 | F-14 | ✅ §3 ②'·§8·§6-0 세 절이 같은 상수를 쓰는데 정본 파일이 없다 ⇒ 두 사본이 갈리는 날 창이 축마다 달라지고 판지문은 한쪽 해시만 오른다. 처방 = `lib/과제검문.js` 상수를 정본으로 지목(재료 2 가 이미 그 파일 해시라 **판은 자동으로 선다**) |
| C8 | 검문 사유 배열의 **순서**가 저장 계약에 없다 | L2-34 | ✅ `attempts_gate_values` 가 허용 원소 «집합»만 검사해 `{길이,중복}` 과 `{중복,길이}` 가 둘 다 저장된다 ⇒ `generation_gate_failed` 가 갈린다. 처방 = 저장 순서를 §8 의 행 순서로 못박고 CHECK 에 정렬·중복 금지 |

### D군 — `degraded`·생산자·판독기 (5)

| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| D1 | **A8 「상태 메타 draft 동결」이 `degraded` 를 포함해 성공/폴백 중 한쪽이 원리상 예외로 죽는다** | F-2·L1-1 | ✅ **P0 · 이종 교차 · 실측 확인(전 세션)** — A8 은 `degraded` 를 조립 시점에 굳히고 「어느 모드든 draft 값 하나」라 못박았는데 ㉤ ④ 는 갈래표(성공→false · 실패·마감·구제→true)와 다르면 **예외로 죽이고** A10 은 outcome 파생을 금지한다. draft 는 outcome 을 모르는 시점의 한 값이라 **true 로 굳히면 성공 착지가, false 로 굳히면 모든 폴백 착지가 ④에서 예외**다. 🔑 실측 교정: 「합법 경로 부재」가 아니라 **`:1311`(성공하면 draft 를 안 쓴다)과 A8(어느 모드든 draft 값)이 같은 절 안에서 배타**다. 처방 = **`degraded` 만 동결 대상에서 뺀다** — 착지 시점에 **모드**가 갈래표대로 정한다(A10 의 「`outcome` 에서 파생 금지」와 안 부딪힌다: 모드는 outcome 이 아니다) · `estimator_version`·`evidence_refs` 동결은 유지 · ㉤ ④ 대조도 그대로 산다 |
| D2 | 상태 메타의 **생산자**가 오케스트레이터와 워커로 갈린다 | L1-2 | ✅ §3 은 워커가 claim 뒤 상태를 읽는다 하고 §3-5-b A8 은 오케스트레이터가 미리 만든다 — **전달 통로도, 두 결과를 비교하는 검사도 없다.** 처방 = 생산자를 **오케스트레이터 하나**로 못박고(A8 유지) §3 흐름도의 그 줄을 고친다 + 워커 재계산 금지 명문 |
| D3 | `policy_ver` **판독기가 원리상 못 선다** — DB 가 저장소 JS 해시를 읽을 길이 없다 | L1-4·F-7 | ◐ **P0 · 이종 교차 · 실측 확인** — 재료 열둘 중 **여섯이 저장소 JS 파일 내용 해시**(1·2·7·8·11·12)이고 DB 가 읽을 수 있는 것은 재료 10(`pg_get_functiondef`)뿐이다. **「아직 안 지었다」 반쪽은 기실재** — §16-1 #5 가 「지금 실물 **0**」을 자인하고 DB 진입점까지 그 항에 얹었다. **새 반쪽 = ㉨ B6 「DB 가 현행 판을 스스로 읽는다」가 «수행 불가능한 규격»**이라는 것 ⇒ 구현은 호출자 산출로 되돌아가거나(B6 이 걷은 논거와 정면) 임의 통로를 발명한다. 처방 = **새 표 0** — B6 을 「**배치 시작 1회** 판독기가 산출한 값을 `batch_run_start` 가 시작 행에 굳히고, 이후 모든 RPC 는 **그 행에서 읽는다**」로 갈아끼운다(A3 와 한 벌 · B6 이 걷으려던 「원소마다 갈리는 호출자 값」은 배치 한 벌 상속으로 닫힌다) |
| D4 | `jobs_claim` 회전의 `hashtext`·`abs` | F-13 | ✅ `hashtext` 는 비문서화 내부 함수라 PG 판 간 안정성 보장이 없고 `abs(int4)` 는 INT_MIN 에서 예외다 — §6-0 회전이 SHA-256·엔디안·BigInt 까지 못박은 규율과 **층이 갈린다**(실사용 확률은 낮다). 처방 = 문서화된 해시 + `mod` 후 양수화 |
| D5 | `quality_ver` 산출 자리가 소비자와 안 맞는다 | F-12 | ✅ §5-1-a 가 산출을 「배치 시작 1회」로 못박았는데 쓰이는 곳은 **오프라인 차단기 하나**뿐이고 행·payload 저장은 금지다 ⇒ 런타임 산출값은 **소비자 0인 죽은 계산**이다. 처방 = 산출 주체를 **차단기(회귀)가 자체 계산**으로 못박고 런타임 산출 조항을 지운다 |

### E군 — §8-B 평가 게이트 (9 · 원건 19)

> ⚠ **이 군이 원건의 21%(19/91)다** — 그리고 §8-B 는 **9회차가 이번 판에 새로 지은 절**이다. 「새로 지은 면적이 다음 회차 지적을 만든다」는 관측 자체는 G3 이 지고(⏳유호), 아래는 그것과 무관하게 **선 자리의 결함**만 처분한다.

| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| E1 | **시험지 생성 절차가 «두 벌»** + ⑦ 분모가 두 값 | L1-15·S-11·F-5 | ✅ **P0 · 3벌(이종 포함) · 실측 확인** — E4 는 「칸당 **정확히 5개**(8칸×5 ⇒ N=40)」(:2862)인데 E5-ⓐ 가 「분포 우선·칸당 **최소 3**」으로 그 전제를 걷었고(:2901~2904), E4 의 **종속 산술이 칸당 5 위에서만 정의된다**: 목표 유무 3·2 번갈아 ⇒ 20/20(:2868) · 기술 층화 18×2=36≤40(:2867). 게다가 ⑦ 분모가 **`n=40`(:2957) vs `n=20`(:2931·:3020)** 로 같은 절 안에서 갈렸다. 처방 = **E5-ⓐ 를 정본**으로 삼고 종속 산술을 **가변 칸에서 재계산하는 규칙**으로 다시 적는다 + `n=40` 으로 세 자리 통일 + 시험지 파일이 칸당 수를 명시 |
| E2 | 평가 결과가 실제 산출물과 **결속되지 않는다** | L1-16·S-14 | ✅ 기계 계약이 `raw_response`·`raw_response_hash`·`sentence`·`question`·`input_hash` 의 **존재와 자료형만** 검사한다 ⇒ 해시가 원문과 달라도, 문장이 파서 산출과 달라도 유효 JSON 이고 **사람이 수정한 낡은 결과가 출시 게이트를 통과**한다. 처방 = 세 일치 검사(`sha256(raw_response)==raw_response_hash` · 문장·질문은 같은 파서 산출 · `input_hash` 는 렌더된 요청 본문의 해시) |
| E3 | **§12-19 가 같은 판의 §8-B 개정을 미수거** | L2-11·F-4 | ✅ **P0 · 이종 교차 · 실측 확인** — §12-19 는 「실행판 **9칸**」·「옛 초록 재사용 차단: 비교 **6칸**」(:3520~3530)인데 §8-B 는 비교 **7칸**(:2996 `quality_ver` 편입)·동봉 **10칸**이다 ⇒ 눈금 절을 따라 구현하면 **`quality_ver` 차단이 조용히 부재**해 E1 이 닫으려던 「검문을 고쳐도 옛 초록이 산다」가 되살아난다. 🔑 문서 스스로 「이 산술은 판마다 갈렸다(네 번째)」라 적은 자리의 **다섯 번째** 갈림이고, 이번엔 **한 판 «안»에서** 났다. 처방 = §12-19 의 수를 **지우고** 「§8-B 표의 비교축 전량」으로 파생시킨다(같은 수를 두 곳에 안 적는다) |
| E4 | 「두 회차 모두 통과해야 1점」이 집계식에서 **평균으로 바뀐다** | L2-12·S-10 | ✅ **실측 확인** — 결과 파일은 `case_id#회차` 행이고 셀 집계식은 「축 점수 합 ÷ 유효 분모」(:3020)라, 36사례가 두 번 통과하고 4사례가 한 번만 통과하면 명세는 36/40=**0.90** 인데 구현식은 76/80=**0.95** 를 낸다(거짓 초록). 처방 = 집계 단위를 **사례**로 — 두 회차를 한 행에 묶고 축별 AND 를 파생(sol-3 대안 그대로 · 행 구조는 그대로 두고 파생 규칙만 적는다) |
| E5 | **⑦축이 「목표 활용」이 아니라 「무엇이든 하나 존재」** — 판단 정본의 목표 게이트가 빈다 | L1-20·L2-13·S-15 | ✅ **3벌 만장** — v5.6 E6 이 ⑦ 정의를 「목표 맥락」→「입력 요약의 사용」으로 넓히며 **목표를 다른 축에 흡수시켰다** ⇒ 목표가 있는 학생의 목표를 무시하고 리듬만 반영해도 1점이고, 반대도 통과한다. 🔑 **철학 ① 「그 학생의 목표에 도움」 게이트가 이 축 하나에 걸려 있다.** 처방 = ⑦을 **둘로 가른다** — ⑦목표 활용(`goal` 있는 사례만 · 분모 20) · ⑧상태 활용(분모 40). ⚠ 축이 7→8 이어도 **사람이 여는 횟수는 그대로 80**(한 사례를 한 번 열어 전 축을 매긴다 · §13 F3 산술) — ㉡ 위반이 아니다 |
| E6 | 재미 축의 판정 규격이 안 닫혔다 · 통과선 0.7 이 「30% 재미없음」을 승인한다 | L1-21·S-19 | ◐ **통과선 0.7 재론은 기각** — v4 가 명시로 정한 자리다(가장 주관적인 축에 가장 높은 문턱을 걸면 게이트가 채점자 취향으로 흔들린다). **앵커·불일치 규격은 채택** = 앵커 사례를 시험지 파일에 **식별자로 고정** + 중간 사례 기준 3줄 + 1인 채점의 불일치 처리 |
| E7 | 평가 활성화가 파일 «존재»에만 매달린다 · 출시 게이트에 차단자가 없다 | L2-19·S-9 | ◐ **반쪽 기실재** — v5.5 E6 이 「실재가 «읽힌다»를 대신하지 않는다」로 이미 닫고 §12-24 ④ 실물 대조 두 벌을 세웠다(:2997). **남는 알맹이 = Actions 정지 중이라 그 회귀가 «안 돌 수 있다»**(sol-3) ⇒ 「기계적으로 불가능」 단정이 사실과 부딪힌다. 처방 = **F207 축으로 정직화** — 「회귀가 돌면 불가능 · 안 돌면 초록이 아니라 **미실행**」 + 배포 전 손 호출 1항(원격 CI 0건은 메모리 `github-actions-billing-outage`) |
| E8 | 「실제 급수 분포에 가까운 시험지」가 **실행 규칙이 아니다** + 재적 변화마다 사람 80회 | L2-31·S-17 | ✅ 거리 함수·동률 처리·재생성 «시점»이 없고, 배분 재료인 **급수 구성이 어디에도 저장되지 않는다**(수만 굳는다) ⇒ 같은 분포에서 다른 시험지가 나온다. 그리고 입학·급수 변경이라는 **정상 운영 사건**마다 80회 사람 채점이 서면 ㉡ 상시 위반이고 **우회가 정상 통로가 된다**(F103). 처방 = ①거리 함수·동률·재생성 시점 명문 ②재생성 트리거를 **분포가 임계 이상 갈릴 때**로(매 변화 ✗) ③재료는 **A2 의 명단 스냅샷**이 낸다 |
| E9 | 평가 결과 파일의 기계 계약이 끝까지 안 닫혔다 | L2-29 | ✅ 행 키 «집합»만 적고 자료형·형식·null 규칙이 없으며 `axis_scores` 의 축 키에 **기계 이름이 없다** ⇒ malformed 결과가 「키는 있다」로 통과한다. 처방 = 키 이름·자료형·null 규칙 표 1개(E2·E5 의 8축과 한 벌) |

### F군 — 성과 회수·학습 사슬 (3)

| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| F1 | **F5 의 corrections 공급 통로 키(`parent_event_id`)가 실물에 0회** — 조인이 영원히 0행 + 기술별 성과 회수의 입력 계약이 비어 있다 | F-3·L1-19 | ✅ **P0 · 이종 교차 · 실측 확인(전 세션)** — `correction.viewed`·`correction.responded` 의 유일한 생산자 `SYNK-talk/src/교정API.js` 봉투(`:90~102`)는 칸 7개뿐이고 `parent_event_id` 가 **파일 전체 0회**(`functions/corrections/index.ts` 도 0회) · F5 가 인용한 실측(`성과회수.js:36~37`)은 **`content.viewed` 통로의 것을 옮겨 적은 것**이라 생산자를 안 열었다 ⇒ 「닿았나」 배선이 서류상으로만 서고, 그 주석이 경고한 사고(관측 영원히 0건)가 corrections 쪽에서 동결된다. 처방 = 통로를 **`correction_id`→corrections 행→원 제출 사건→배정(`event_id`) 다단 조인**으로 갈아끼운다 + 이벤트 목록·중복 교정 선택·기술 사용 «분모» 명문(L1-19) |
| F2 | 학생 산출(`submission.created`)과 배정 사이의 **인과 키가 없다** | S-8 | ◐ **허브 조인 반쪽은 기실재** — v4.1 이 `submissions.event_id = 배정 event_id` 로 확정했고 라이브 실측까지 섰다. **새 반쪽 = 그건 «배정 시점에 만든 보조 행»이고 후일 발생하는 `submission.created` «사건»은 다른 행**이라는 것 — sol-3 가 그 구분을 정확히 짚었다. 처방 = 그 사건 봉투에 `parent_event_id` = 배정 `event_id` 를 싣는 규칙(**F1 과 같은 형태의 사고를 여기서 미리 막는다**). ⚠ **집행 시 실물 생산자를 열어 확인**한다 — F1 이 바로 그 「생산자를 안 열고 적었다」로 났다 |
| F3 | 재미 배선 셋 중 **하나만** 종단 검사한다 | S-18 | ◐ §8-A 는 재발화 꼬리·achieve 효과음·캐릭터 반응이 다 붙는다는데 §12-29 는 ③답하기 행과 꼬리 판정만 본다. ⚠ §13 의 「학생 단위 재미 **측정** 기각」과 **층이 다르다**(이건 배선 «연결» 검사다). 처방 = **효과음 연결 1항만 지금** 인수 조건에 · **캐릭터 반응은 마스코트 트랙 착지 후**(⏸ · 메모리 `mascot-companion-layer`) |

### G군 — 문서·게이트·면적 (5)

| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| G1 | **최신 기능 정찰이 서류 게이트에 머문다 — 본 것·채택/기각·대가의 실제 기록이 0** | L1-23·L2-21 | ✅ §16-1 0번·§14 선행 0 이 정찰을 «반드시» 요구하는데 본문에 결과가 없고, 저장 경로·배포된 `model`·`prompt_ver`·생성 방식과의 연결도 없다 ⇒ 「최신 방식을 채택했다」와 실제 코드가 달라도 검출 불가. 🔑 luna-2 의 몸통이 정확하다 — **정찰 결과가 JSON 출력 계약이나 모델 선택을 바꿀 수 있는 상태에서는 동결을 진행할 수 없다.** 처방 = 집행 때 **실제로 정찰하고** §14 에 3줄(본 것·채택/기각·대가)을 적는다 — 메모리 `latest-ai-scouting-gate`(유호 확정 08-14)의 게이트 ㉠㉡㉢ 그대로 · 소스는 `latest-feature-scouting-sources` |
| G2 | 롤백 금지가 상위 물리 계약과 충돌한다 | L1-24 | ❌ **기각 — 네 판째 재론.** §13 v5.5(두 판째) → v5.6 F2(세 판째)에서 닫혔고, §12-27 이 두 정본을 **줄번호째 인용**하며 경계·L0 §8 기록 6종·만료(=첫 학생)를 이미 명시했다. 저장소 마이그 27벌 전부 롤백이 없고 재실행 안전으로 갈음한다 — **이 트랙만 없는 관행을 새로 짓지 않는다.** 10회차 신규 사실 0 |
| G3 | **선행 구축 면적이 검증 결과보다 앞서 있다** — 생성 행·학생 산출 0인 상태에서 이 많은 물리 표면을 동결하면 관측되지 않은 실패 경로를 대규모로 고정한다 | L2-33 | ❌ **세션 권한 밖 기각 — 두 판째 · ⏳유호 이관.** §13 v5.6 G3 가 이미 닫았다: 「게이트는 **유호 확정 08-12**(심문 지적 «전건 처분»)라 **세션이 못 뒤집는다**」. 🔑 **그러나 지적 자체는 살아 있다** — 10회를 돌았는데 원건이 안 준다(82→60→69→64→76→**91**) ⇒ **판정 자리를 §14 로 올린다**(갈래·권고·대가 완성형). 이 표는 그 답과 **무관하게** 성립한다: 아래 §5 지도가 「선 자리의 결함 수리」와 「새 면적 추가」를 갈라 놨으므로, 면적을 자르기로 하면 후자만 접힌다 |
| G4 | 학생이 다시 열 이유가 없다(학생 접점 계약 공백) | L2-28 | ⧉ **범위밖** — §8-A·§8-B 가 학생 단위 재미·재방문을 **첫 시즌 회고로 명시로 미뤘다**(학생 0명 = 못 재는 칸 · §13 v5.5 H1 이 그 기각의 정본). 「생성물이 어떤 행동으로 이어지는가」는 ③답하기 꼬리로 이미 서 있다. **못 재는 것을 게이트에 넣으면 게이트 전체가 안 쓰인다** |
| G5 | §12-13 격리 회귀의 허용 목록이 v5.6 구조와 안 맞는다 — **정의되지 않은 파일을 가리킨다** | F-9 | ✅ **실측 확인** — 목록이 「`과제생성.js`·`과제배치.js` 둘뿐」인데(:733·:3432~3433) **`과제배치.js` 는 문서 어디에도 정의가 없다**(오케스트레이터는 JS lib 이 아니라 Edge Fn 이다). v5.6 에서 두 장부를 실제로 만지는 층은 deliver Edge Fn·tasks 구제·deliver-check SQL·§12-8 진단·§8-B E5-ⓐ 까지 늘었다 ⇒ 이 목록대로 회귀를 세우면 **정당한 구현을 거절하거나, 거절을 피해 목록을 넓히며 격리가 무너진다**(이 항이 스스로 인용한 F103 형이 목록 층에서 재발). 처방 = 목록을 v5.6 구조로 갱신 + 없는 파일 제거 |

## §3. 실측 — 인용이 아니라 열어서 쟀다 (이 세션 12건 + 전 세션 3건)

> 규약: 「현재 상태는 현재를 읽어 판정한다」 · 심문자 주장을 그대로 옮기지 않는다. 줄번호는 `docs/상태기반_과제선택_설계.md` v5.6.

| # | 잰 것 | 결과 |
|---|---|---|
| 실1 | A10 — `level` 값목록 실재 | 🔴 **지적이 맞다.** 적재 검사 ⑤(:1293)가 「아래 표의 `level` 칸이 정본 목록을 진다」고 가리키는데 그 칸(:1591)엔 「`level_snapshot` 과 같은 값을 쓴다」뿐 **리터럴 0개** |
| 실2 | A4 — `jobs_finalize` 시그니처 | 🔴 **지적이 맞다.** `(_job_id, _fence, _outcome, _event, _winning_attempt_id, _mode)`(:1434~1437) — `_deciding_attempt_id` **없음**. DDL 은 여섯 실패값에서 not null 요구(:886~892) |
| 실3 | A5 — attempts 실행판 칸수 | 🔴 **지적이 맞다.** attempts 는 `model`·`prompt_ver`·`policy_ver`·`estimator_version` **넷** · `attempt_open` 인자도 넷(:1240~1242) · jobs·batch_runs 는 **여섯**(:928·:1169) |
| 실4 | B7 — 감시 ④ 가 세는 행 | 🔴 **지적이 맞다.** 판정식 = 「job 의 `lease_until` 이 지난 행」(:606)인데 회수(:1492)·반납(:1503)·전이표(:1871)가 그 칸을 **비운다** ⇒ null 비교 UNKNOWN 으로 **표적이 빠진다** |
| 실5 | A11 — `target_count` 정의 두 곳 | 🔴 **지적이 맞다.** DDL 주석 `status <> '대상아님'`(:1149) ↔ §3-6 정본 식(`적재실패` 제외) — v5.6 A13 집행이 §3-6 만 고쳤다 |
| 실6 | A12 — 비공백 CHECK | 🔴 **지적이 맞다.** `<> ''` 만(:928·:1169) — `"   "` 통과 |
| 실7 | A15 — `policy_ver` 재료에 상태 추정기 | 🔴 **지적이 맞다.** 재료 열둘(:2107~2131) = 과제요약·과제검문·폴백순서·타임아웃·워커상한군·큐회전·갈래판정·기술선택·cron·아홉RPC·과제생성·오늘과제 — **`학습자상태.js` 없음** |
| 실8 | E1 — 시험지 칸당 수 | 🔴 **지적이 맞다.** E4 「정확히 5개(8칸×5=N40)」(:2862) ↔ E5-ⓐ 「분포 우선·칸당 최소 3」(:2901~2904) · 종속 산술(목표 3·2 → 20/20 :2868 · 기술 층화 18×2≤40 :2867)이 **칸당 5 위에서만** 정의 |
| 실9 | E1 — ⑦ 분모 | 🔴 **한 절 안에서 갈렸다.** `n=40`(:2957 · v5.6 E6) ↔ `n=20`(:2931·:3020) |
| 실10 | E3 — §12-19 실물 | 🔴 **지적이 맞다.** 「실행판 9칸」·「비교 6칸」(:3520~3530) ↔ §8-B 비교 **7칸**(:2996) |
| 실11 | D3 — 판독기 실물·재료 계보 | ◐ **반쪽은 기실재.** §16-1 #5 가 「지금 실물 **0**」을 자인하고 DB 진입점까지 그 항에 얹었다 — 단 재료 열둘 중 **여섯이 저장소 JS 파일 해시**라 **B6 의 규격 자체가 수행 불가능**한 것은 새 사실 |
| 실12 | G5 — 격리 허용 목록 | 🔴 **지적이 맞다.** 「`과제생성.js`·`과제배치.js` 둘뿐」(:733·:3432~3433) — **`과제배치.js` 는 문서 전체에 정의 0** |
| (전) 실13 | B1 — 마감 게이트 | 🔴 확인 — `jobs_claim` 마감 뒤 0행(`:1483`·`:1487`) · `jobs_finalize('정상')` 마감 뒤 거절(`:1542`) · `_mode` 열거 = 정상·마감·적재 **셋뿐** |
| (전) 실14 | D1 — A8 draft | 🔴 확인 — **「합법 경로 부재」가 아니라** `:1311`(성공하면 draft 안 쓴다)과 A8(어느 모드든 draft 값)이 **같은 절 안에서 배타** |
| (전) 실15 | F1 — corrections 통로 키 | 🔴 확인 — `교정API.js:90~102` 봉투 칸 7개에 `parent_event_id` **0회**(`corrections/index.ts` 도 0회) |

🔑 **심문자 정정 0건 · 잰 것 15/15 「지적이 맞다」**(D3 만 반쪽). 5·6회차와 갈리고 7·9회차 패턴이 재현됐다 — **물리층 지적은 거의 전부 실물**이다.
⚠ **A10 처방은 집행 때 talk 실물(`level_snapshot` 값 형태)을 열어 확정한다** — 문서에서 베끼면 이 갈래가 난 그 자리를 반복한다.
⚠ **F2 처방도 집행 때 실물 생산자를 연다** — F1 이 정확히 「생산자를 안 열고 적었다」로 났다.

## §4. 판정 분포

| 판정 | 수 | 비고 |
|---|---|---|
| ✅ 채택 | **43** | A 13 · B 9 · C 7 · D 4 · E 7 · F 1 · G 2 |
| ◐ 부분 | **11** | 반쪽이 기실재이거나 면적을 깎아 알맹이만 |
| ❌ 기각 | **3** | A9(네 판째) · G2(네 판째) · G3(세션 권한 밖 · ⏳유호 이관) |
| ⧉ 범위밖 | **1** | G4(첫 시즌 회고 닻 기실재) |
| **계** | **58갈래 / 원건 91** | 빠짐·중복·잉여 **0** |

🔑 **이 판의 축 셋**(설계 머리말과 같은 낱말로 적는다):
- **① 규칙이 가리키는 «정본 칸»이 비어 있었다** — A10(`level` 값목록: 규칙이 「저 표가 목록을 진다」는데 표가 비었다) · A4(CHECK 는 값을 요구하는데 인자가 없다) · B4(대조하라는 함수명이 없다) · G5(허용 목록이 없는 파일을 가리킨다) · C7(상수의 정본 파일이 없다). **v8 F4 와 같은 형태가 다섯 자리로 늘었다** — 「약속을 적고 그 자리를 안 채운다」.
- **② 새로 지은 감시·게이트가 자기 대상을 못 본다** — B7(회수가 `lease_until` 을 비워 표적이 빠진다 · **v5.6 이 그 축을 닫으며 지은 감시에서 재발**) · A2(전원 누락이 네 항을 다 통과) · E3(눈금 절이 같은 판의 개정을 미수거) · E4(AND 규칙이 집계식에서 평균) · E7(회귀가 안 돌면 「미실행」인데 「불가능」이라 적혔다).
- **③ 한 판 «안»에서 형제 절을 안 걷었다** — B1(B3 집행이 구제 계약 전체를 안 걷어 **소급 불가 사고**) · D1(A8 이 ㉤ ④ 와 배타) · E1(E5-ⓐ 가 E4 종속 산술을 안 걷음) · A11(A13 이 DDL 주석을 안 걷음) · B10(B4 가 전이표를 안 걷음). **v5 낙방의 뿌리가 그것이었는데 같은 형태가 다섯 자리다.**

## §5. 집행 지도 — 이 순서로 연다 (②집행 = 설계 v5.7)

> 🔑 **두 덩이를 갈라 놨다** — ⓐ**선 자리의 결함 수리**(면적 안 늘림) / ⓑ**새 면적 추가**. G3 의 ⏳유호 답이 「면적을 자른다」로 오면 **ⓑ만 접히고 ⓐ는 그대로 간다.**

**ⓐ 선 자리 수리 (면적 0 · 34갈래)**
1. **§3-5-b 마감·구제** — B1(넷째 모드 `_mode='구제'`) · B2 · B10 · A7(예외 대신 typed 결과) · A6
2. **§3-5-b 물리 계보** — A1(복합 FK·정본 실행 규칙) · A4(인자) · A5(2칸+인자) · A11(주석) · A12(`btrim`) · A9 는 기각이라 손대지 않는다
3. **§3-5-b `degraded`·생산자** — D1(동결에서 `degraded` 만 뺀다) · D2(생산자 일원화) · A14
4. **감시** — B7(null 갈래) · B9(활성 시작일) · B5(셋째 재료) · B11 · B3(워커 1항)
5. **§5·§6 결정성** — C1(의사코드) · C3(생산자 범위) · C4(통로 한 줄) · C5(질문 대조) · C6 · C7 · C8 · D4 · D5
6. **§8-B 내부 정합** — E1(정본 일원화·n=40) · E3(§12-19 파생화) · E4(사례 단위 AND) · E2 · E9 · E6(앵커) · E7(F207 정직화)
7. **§12 눈금** — A8(동일성 검사) · G5(목록 갱신) · B12(문구 칸)

**ⓑ 새 면적 (칸·RPC 신설 · 9갈래)**
8. **A2 명단 스냅샷**(학생 목록 해시 + 급수 분포 jsonb + 등식 CHECK) → **E8** 이 그 재료를 쓴다 · **B6**(`calendar_game_day` 굳히기)
9. **A3 `batch_run_start` 열째 RPC** → **D3**(판독기 산출값을 시작 행에 굳혀 RPC 들이 읽는다) · 재료 10 「아홉→열」
10. **A13** 3칸 · **A15** 재료 13 · **A16** 멱등 반환
11. **C2** §5-3 기술 렌더링 규칙 · **E5** ⑦→⑦⑧ 분리
12. **F1** corrections 다단 조인 · **F2** 사건 봉투(⚠실물 확인) · **F3** 효과음 1항

**ⓒ 마지막**
13. **G1 정찰을 «실제로» 하고** §14 에 3줄 — 🔑 luna-2 대로 **동결 전에** 한다(정찰 결과가 출력 계약·모델 선택을 바꿀 수 있다)
14. **G3 판정 자리를 §14 에** 완성형(갈래·권고·대가)으로 올린다 → 결정 큐
15. §13 에 **v5.7 증보**(아래 §6) · 머리말·§16-1 게이트 갱신

## §6. 🚫 설계 §13 v5.7 증보 — 다음 회차가 다시 집지 않게 (집행 시 전건 등재)

- 🚫 **`대기`+`not_target_reason` 물리 차단 · `jobs_claim` 이 사유를 읽게 하기** — A9(**네 판째**). 오배제 > 오포함 · §12-21 이 센다.
- 🚫 **역마이그레이션·롤백 경로 신설** — G2(**네 판째**). §12-27 이 경계·L0 §8 기록 6종·만료를 줄번호째 인용.
- 🚫 **「동결 과다 — 게이트·면적 축소」를 세션이 판정하기** — G3(**두 판째**). 게이트는 유호 확정 08-12 라 **세션이 못 뒤집는다** ⇒ 판정 자리는 §14(⏳유호)다. **지적을 「기각됐다」로 읽지 마라 — 자리를 옮긴 것이다.**
- 🚫 **학생 단위 재미·재방문을 이 트랙에서 측정** — G4(**세 판째** · ⧉). 학생 0명 = 못 재는 칸 · 재개 닻 = 첫 시즌 회고.
- 🚫 **§8-B ⑤재미 통과선 0.7 을 올리기** — E6 반쪽(두 판째). 가장 주관적인 축에 가장 높은 문턱을 걸면 게이트가 채점자 취향으로 흔들린다(v4 명시 판정).
- 🚫 **§8-B 7축에 「겨냥 기술 적중」을 새 «사람 채점» 축으로 신설** — C2 처방 중 안 쓴 갈래. 사람 손 면적이 늘고, 겨냥 언급은 **기계 검사**로 셀 보고가 진다.
- 🚫 **`적재실패` 의 최초 `_target`·전체 입력을 불변 «행»으로 보존** — A13 반쪽. 표가 하나 더 선다 · 최소 3칸으로 닫았다.
- 🚫 **C5 게임날 갈래를 `branch_snapshot.is_game_day` 로 되돌리기** — B5 처방과 혼동 금지(v5.6 증보 그대로 유효). 이번 처방은 **달력 + job 유무 + `task_type` 실물** 셋이다.
- 🚫 **`policy_ver` 판독기를 위해 새 DB 표·설정을 짓기** — D3. 시작 행(A3)에 굳혀 읽는 것으로 닫았다.
- 🚫 **`evidence_refs` 에 원래 개수 칸을 새로 열기** — C4. 상류 `잘림` 이 정본이고 §6-1 은 **통로 한 줄**로 닫는다.

## §7. 집행자 조정 자리 (②집행 때 채운다 — 다음 회차 재심 대상)

> v9 §7 과 같은 규약: 집행 중 처분표의 처방을 바꿨으면 **여기에 전건 적는다**(안 적으면 다음 회차가 「처분표대로 안 했다」로 다시 집는다).

| # | 갈래 | 조정 | 근거 |
|---|---|---|---|
| 1 | A2 | 등식을 세 항(`target+대상아님+적재실패=enrolled`) → **다섯 항**(`loaded + skipped_game + skipped_existing = enrolled` · skip 2칸+㉠ `_skipped_game` 인자 1 신설) | 세 항은 **물리로 성립 불가** — ⓪게임날·`existing` 은 job 이 없다(§3-4) ⇒ 완주 배치마다 CHECK 위반 |
| 2 | B11 | 「`claimed` 는 lease 유효할 때만 생성중」을 **값 신설 0**으로 — 만료 `claimed` 도 실리는 값은 `생성중`, 판정식이 lease 를 읽는 것만 명문 | 다섯째 값을 열면 c12 값목록이 늘고 어댑터가 「곧 온다/잠시 뒤」를 가를 실익 0 — 회수 계약(10분 회차)이 「곧 온다」를 사실로 만든다 |
| 3 | C8 | 순서·중복 금지를 CHECK 가 아니라 **㉣ `attempt_close` 함수 검사**로 | 물리 CHECK 는 서브쿼리 불가 — attempts 는 RPC 밖 UPDATE 가 권한으로 막혀 있어 함수 검사가 곧 물리다 |
| 4 | D2 | 전달 통로를 확정: 요약 «입력» 재료(축값)를 **`event_draft` 상태 메타 옆에 동봉** | 처방이 「생산자 일원화」만 적고 통로를 안 정했다 — 통로 없는 일원화는 워커 재계산으로 되돌아간다 |
| 5 | D3 | 그날 시작 행이 **0**인 날의 구제 실행판 = **가장 최근 시작 행** | 처방 미규정 갈래 — 구제가 서는 날은 배치가 죽은 날이라 완주 행이 없을 수 있고, 마지막 배포 판이 가장 정직한 값이다 |
| 6 | A1 | 「그날 정본 실행」을 **완주 최신 run** 으로 «재정의»(v5.6 의 「jobs 의 `batch_run_id` 행」 규칙은 계보로 강등) | B4 재적재가 합법이 되며 v5.6 규칙이 **단수가 아니게 됐다**(한 날짜의 jobs 가 서로 다른 run 을 가리키는 것이 정상) |
| 7 | A3 | 멱등 반환 규칙 = **「재호출은 언제나 새 행」**으로 특정 | HTTP 재시도와 재실행은 함수가 구분 불가 · ④ A1 append 가 죽은 행 잔존을 이미 정상으로 정했다 · 큐 이중 생성은 jobs 유일키 몫 |
| 8 | E1×E5 | ⑦ 분모 「n=40 통일」은 **E5 축 분리로 대체 집행**(⑦목표=20 · ⑧상태=40) | E1 처방은 분리 «전» 상태의 통일이었다 — 분리 후엔 두 값이 각자 옳다 |
| 9 | B4 | 실행 주체 실물 이름 = 워커 Edge Fn **`deliver-one`**(§3 흐름의 그 이름) · deadline·check 는 SQL 직접 | 처방은 칸 신설만 — 이름 없는 칸은 대조를 못 한다 |
| 10 | F1 | 다단 조인 홉을 「`correction_id` → corrections 행 → 원 제출 → 배정 `event_id`」 수준으로 적음(열 이름 확정은 구현 시 실물 확인) | F1 자신이 「생산자를 안 열고 적었다」로 났다 — 안 연 열 이름을 지어 적으면 같은 형이다 |

## §8. 도장

- **①판정** = ✅ **종결** — 원건 **91/91** 기계 매핑(빠짐·중복·잉여 0) · **58갈래** ✅43·◐11·❌3·⧉1 · 실측 15건.
- **②집행** = ✅ **종결 54/54 — 설계 v5.7**(08-15 · 두 세션): 지도1 9갈래 `784eda23`(e6527a70) +
  지도2 17 `4b15ef5c` · 지도3 14 `a3e1f803` · 지도4·5 14 `791f1566`(ef5a9aa4 승계).
  집행 중 실물 재확인 3(교정API 봉투 7칸·게임제출 봉투·events 접수층 `parent_event_id` 기실재) ·
  G1 정찰 실집행(구조화 출력 채택·Batch API 기각 — §14 기록) · 조정 10건 = §7.
  ⛔ **구현 착수 차단 유지**(새 회차는 다시 전건 처분을 지난다 — ▶다음 = 11회차 심문을 v5.7 에 ·
  ⚠편성 luna/max 1벌).
- **런 도장** — `심문-2026-08-14T1332-cf896f` ✓ 처분 완료 · fable 판 `6b18e681` ✓ 합집합 편입(통로는 폐지 · 결과는 증거로 보존).
- ⏳ **유호님 자리 2** — ①**G3 면적 자르기**(§14 로 올림 · 이 표와 무관하게 성립) ②**E2 급수→기술 매핑을 이 트랙이 지을 것인가**(기존 결정 큐 줄 · 그대로).
