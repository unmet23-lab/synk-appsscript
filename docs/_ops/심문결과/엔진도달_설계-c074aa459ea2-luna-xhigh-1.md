판정 근거는 제공된 동결 후보 전문과 인계문 자체다. 문서 안의 “실측”·“리허설” 주장은 독립적인 운영 DB 검증으로 인정하지 않는다.

- [P0] `as_of`가 미래정보 누출을 막지 못한다 — §3 경로 A는 `occurred_at`만 개입 시각 이전인지 확인한다. 늦게 도착한 STT·시트 이관·재처리 결과가 과거 발생시각을 가지면, 개입 당시에는 없던 정보가 상태 계산에 들어간다. `available_at`·`ingested_at` 같은 지식 가능 시점과 그 절단 규칙이 없어 첫 상태를 재현할 수 없다.

- [P0] 승격 사건의 필드와 생성 순서가 모순된다 — §3 경로 B는 산출물 저장 후 `data_use.granted`, 그 뒤 데이터셋 조립 순서인데 §6은 승격 사건에 이미 `dataset_ver`와 `train/eval 목적`을 요구한다. 동시에 §3은 검수자가 목적을 고르지 않고 조립 공정이 정한다고 한다. 생성 주체·시점·트랜잭션이 정의되지 않아 어느 구현도 세 규칙을 동시에 만족하지 못한다.

- [P0] `event_id` 하나만으로는 재검수와 수정본을 구분하지 못한다 — §3은 원본 사건당 승격 1회만 허용하지만, 같은 원본의 교정문·라벨이 승격 의사 뒤에 바뀌는 경우를 막거나 연결하는 `correction_version`이 없다. 재시도 중 산출물이 일부 저장되면 중복이 생기고, 수정본은 같은 `event_id` 때문에 승격되지 않는다. 첫 수정본의 원래 상태는 소급 복구할 수 없다.

- [P0] 회사 훈련 경로가 미구현인데 첫 학생을 막는 게이트가 없다 — §2는 `data_use.granted` 생산자 0, §3의 자동 산출물·데이터셋 조립도 구현 0이라고 적는다. §2의 ⑦ 역시 리허설·미배포 상태다. 그럼에도 학생 접점과 검수 의사가 먼저 들어갈 수 있으며, “미구현이면 해당 기능을 실행하지 않는다”는 기계적 출시 조건이 없다.

- [P0] `⑦ 엔진이 배운다`가 개인화 조회와 학습을 혼동한다 — §5는 읽힌 것을 실제 소비로 정의하지만, 판단 정본의 `늘었나`는 부록 A-1 이해 대장의 어느 칸이 채워졌는지 이름을 요구한다. `deliver`의 프롬프트 변경, `성과회수` 호출, 동적 상태 계산은 서비스 동작이지 특정 엔진 부품의 증가 증명이 아니다. 경로별 A-1 칸과 그 값을 쓰는 원자적 사건이 없다.

- [P0] 도달 장부는 실행 증명이 아니라 문자열 선언이다 — §4의 검사는 소비자 파일에 부품명 문자열이 있는지만 확인하고, 문서 스스로 “읽어서 바꾸는지”는 못 가른다고 인정한다. 호출되지 않는 함수·죽은 분기·실패를 삼킨 호출도 초록이 된다. §8의 일부 시트 회귀만으로 13개 앱 사건 전체의 런타임 도달을 보장할 수 없다.

- [P0] “학생이 와야 한다”는 `self_declare_log`·`scene_log`의 안전한 예외가 아니다 — §8은 `self_declare_log` 생산자·소비자 0으로 두고, §8-0은 `scene_log`를 먼저 열고 소비자는 훗날 세운다고 한다. 학생의 첫 자기선언·장면 기록이 바로 소급 불가능한 최초 증거다. 첫 이벤트 전에 생산자·필드·소비자·실패 재시도 계약이 서 있지 않으면 그 순간부터 손실이 시작된다.

- [P0] 시트 이관은 범위만 있고 물리 계약이 없다 — §2-1과 §7은 실제 데이터가 시트에 쌓인다고 인정하면서 이관 어댑터 구현을 미래 과제로 남긴다. `correlation_id`·`task_snapshot` 결측을 “이관 표기와 manifest”로 넘긴다는 규칙만 있고, 사건별 스키마·시간·식별자·소비자별 강제 제외가 없다. 아카이빙이나 `wipe` 뒤에는 누락 필드를 복원할 방법이 없다.

- [P0] `dataset_item`이 미룬 DDL 위에 서 있다 — §3의 훈련·평가 누출 방지는 `dataset_item.group_id`에 의존하지만, 인계문은 `dataset_item DDL`을 의도적으로 미룬 항목으로 둔다. `group_id` 생성 알고리즘·버전·재배정 금지·데이터셋 간 유일성 제약이 없으므로 묶음 단위 상호배타를 실행할 수 없다. 경로 B의 핵심 안전장치를 설계만으로 동결한 셈이다.

- [P0] 문법·어휘의 정식 식별자가 없다 — §8-2는 `개인퀴즈`라는 무의미한 유형을 문제 원문 앞머리로 대체한다. 원문 표현이 바뀌면 같은 문법이 여러 항목으로 갈라지고, 우연히 같은 앞머리를 쓰면 다른 문법이 합쳐진다. `hw_feedback`·음성·대화의 `masteryApply_`가 동일한 학습 대상을 연결할 안정적인 `skill_id` 계약이 없다.

- [P0] 조 편성이 학생 간 비교 금지를 정면으로 위반한다 — §8-1은 여러 학생의 값을 모아 중앙값 `quietHi`를 만들고 “조용한 절반”을 분산한다. 판단 정본 ㉢은 학생끼리 비교하지 않고 어제의 같은 학생과만 비교한다고 명시하며, 내부 운영 알고리즘 예외도 없다. 화면에 등수를 숨겼을 뿐 비교 로직 자체를 설계에 넣었다.

- [P1] 기본 게이트를 프롬프트 변화로 대체했다 — §8-2~8-6 회귀는 프롬프트 문자열이나 `mastery_log`가 달라지는지만 잰다. 학생이 실제로 말하거나 쓰는지, 그 산출이 학생의 목표에 도움이 되는지, 다시 열 이유가 생기는지는 검증하지 않는다. 데이터가 엔진에 닿아도 판단 정본 ①의 “실제로 사용할 한국어 도움”은 미판정이다.

- [P1] 자기설명 게이트 ⑤가 전부 빠졌다 — 반복 퀴즈·복귀 학생용 재료·음성 판정·오늘의 한 문장이 첫 화면에서 무엇이며 무엇을 해야 하는지 설명하는 UI 계약이 없다. 관련 테스트도 모델 입력과 프롬프트만 확인한다. 처음 본 학생·학부모가 이유와 행동을 모르면 백엔드가 작동해도 `사람에게 닿나`에서 실패한다.

- [P1] 승격 격차 계수는 통제가 아니라 단순 숫자다 — §5는 `promotion_intent=true` 행 수와 `data_use.granted` 사건 수만 뺀다. 원본 `event_id`, 교정 버전, 시도 번호로 대조하지 않으므로 잘못된 승격 하나와 누락 하나가 서로 상쇄되어 격차 0이 될 수 있다. 어떤 행을 재처리해야 하는지도 산출하지 않는다.

- [P1] 개입-성과 회수 시점이 재현 불가능하다 — §3·§5는 즉시·1·7·30일과 개입 ID만 적고, 성과 사건의 정확한 종류·시간대·중복 개입 귀속·지연 도착·관측 중단 규칙을 정의하지 않는다. 같은 학생에게 여러 개입이 겹치면 어떤 개입의 효과인지 달라진다. 첫 성과 원본이 쌓인 뒤에는 동일한 연결률을 다시 계산할 수 없다.

- [P1] 분모 정본이 문서 안에서 하나가 아니다 — §8에는 22·23·24종, §8-0에는 골격 58·표식 23종이 병존하고, 선언된 범주 수는 9+13+15+13+7+3=60으로 골격 58과도 맞지 않는다. 날짜를 붙였다는 이유로 현재 판정과 감사 이력을 한 표에 섞었다. 이미 세 차례 표가 낡았다고 기록한 방식 자체가 동결 문서의 수동 복사 결함을 입증한다.

- [P0] 강사 골든 경로가 미결정 상태다 — §1은 ㉠ 재검수·㉡ teach 승격칸·㉢ 평가 전용 중 하나를 유호님에게 남겨 두었고, §2-1은 자동 배치를 금지한다. 동시에 `G001` 재채번으로 원본 사건 연결이 끊겼다고 적었다. 동결 시점에 골든 행이 훈련인지 평가인지, 나중에 어떤 사건으로 계보를 만들지 결정되어 있지 않으며 기존 행을 완전히 복구할 수 없다.

- [P1] 성취 경로에는 이미 알려진 미검증 구멍이 있다 — §8-6은 `Math.min` 폭 클램프를 삭제한 변이 43건이 모두 초록이었다고 명시한다. 구형 시트 폭에서 예외가 나면 `aiStudioBatch_` 전체가 중단되는데, 결과가 “성취 없음”과 구분되지 않는다. 학생에게 보이는 오늘의 한 문장 경로를 실제 `Range` 형태로 검증하지 않은 채 닫을 수 없다.

- [P2] 감사 장치가 제품·원본 계약보다 앞서 있다 — §8·§8-0은 수십 개 탭의 분류·래칫·수동 표를 확장했지만, 핵심 식별자·이관 어댑터·승격 파이프라인·학생 화면은 미완성이다. 수동으로 복사하는 분모 표가 반복해서 낡는 것은 장치가 또 하나의 관리 원장이 되었다는 증거다. 앱 우선·E0 원칙에 맞추려면 동결 대상은 자동 생성되는 최소 교차표여야 한다.

대안 구조 1개: 문서를 `현재 물리 계약`, `원본 사건 상태기계`, `학생 화면·경험 계약`, `날짜별 감사 기록`으로 분리하고, 각 사건에 `event_id·skill_id·occurred_at·available_at·source_version`을 고정한다. 승격·산출물·데이터셋·소비는 각각 상태와 멱등키를 갖게 하며, “늘었나”는 반드시 명명된 A-1 칸의 실제 값 변화와 학생 화면의 행동·자기설명 E2E 테스트가 함께 통과할 때만 ✓로 둔다.