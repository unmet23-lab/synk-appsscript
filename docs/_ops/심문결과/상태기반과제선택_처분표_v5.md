# 상태기반 과제선택 — 5회차 심문 처분표 v5

> **정본** · 2026-08-13 · 세션 `local_5c55e5f5` · 대상 = 설계 **v5.1**(대상지문 `f0b56546c486`) ·
> 결과 3벌 = `상태기반_과제선택_설계-f0b56546c486-{luna-1,luna-2,sol-3}.md`(런 `심문-2026-08-13T0344-f769e2` · 수거 `local_4bbeced4`).
> **게이트 = 「전건 처분」**(유호 확정 08-12 「통과라는 상태가 없다」): ①판정 = 원건 매핑 수 = 결과 파일 지적 수 **82** ②집행 = 채택분이 본문(v5.2)에 들어왔다.
> 🚫 v1~v4 처분표를 덮지 않는다(회차 대조 · F281). 🚫 이 표를 4회차 위에 «세지» 않는다 — 판이 다르다(v4/v4.1 ↔ v5.1).

## §0. 게이트 산술

| 벌 | 원건 | P0 | P1 | P2 |
|---|---|---|---|---|
| luna-1 | 18 | 14 | 4 | 0 |
| luna-2 | 25 | 20 | 5 | 0 |
| sol-3 | 39 | 22 | 16 | 1 |
| **계** | **82** | 56 | 25 | 1 |

**처분 = 46갈래**(채택 38 · 부분 채택 7 · 기각 1 · 범위밖 0) · **원건 매핑 82/82**(§4 표 — 빠짐 0 · 중복 0).

## §1. 회차 조건 (F281)

한 런(`f769e2`)의 3벌이라 **대상지문·금지지문(`d3c73c9e511f`)이 세 벌 동일** — 이 판 «안»의 3/3 합의는 가중치로 쓸 수 있다(증명은 아니다 · F348·F350). 회차 «간» 산술 대조는 금지(1~4회차와 판이 다르다).

## §2. 실측 — 인용이 아니라 열어서 쟀다 (컨텍스트 독립 에이전트 2벌 + 문서 전문 대조)

| # | 잰 것 | 결과 |
|---|---|---|
| 실1 | `lib/학습자상태.js` 판·축 | **`학습자상태.v9`(`:89`) · 축 11개**(성향 5 + 축밖 6 — 작성과정·선호·정서·집중띠·탐지·강제산출) · 창 30 · 근거상한 200 · `잘림`=원래 개수. **v5 §4-3 의 「실물=v7·9축」 정정 자체가 낡았다.** ⚠판번호≠축수(v7=9축·v8=10축·v9=11축 — 「v9=9축」은 우연의 겹침) |
| 실2 | `ingested_at` 절단 | 학습자상태 창안 필터는 `occurred_at` 만 자른다(`:206~209`) · `ingested_at` 은 그 파일 0회(열 실재는 `이벤트검증.js:21`) |
| 실3 | `출처` 리터럴 | `'도입'·'교정문'·'전날'`(오늘과제 `:299~303`) + 화면층 `'고정'·'서버'`. **`'폴백'` 리터럴은 저장소에 없다** |
| 실4 | ③답하기 호흡 | `문장` 키 없음 · `프롬프트`만(`:159~174`) · 선택지 없으면 **키 생략**(`:172` · null 아님 — 근거 주석 `:166~168`) · `task_schema_ver` 는 호흡이 아니라 **사건 행 최상위**이고 오늘과제 경로는 **안 싣는다** |
| 실5 | `output_text` 사슬 | deliver `:596` 이 개입 payload 에 `{ver:1, output_text: 따라말하기문장(스냅샷)}` 을 쓰고 tasks `:184→:259` 가 그걸 학생에게 낸다 — **①듣기의 실물 사슬** |
| 실6 | cron 배선 | pg_cron 4개뿐: `deliver-daily`(16:05Z)·`deliver-check`(16:35Z)·`transcribe-batch`·`radio-promote-hourly`(`migrations/20260809070000_cron_c10.sql` 등). **`deliver-one`·마감 스윕류 = 저장소 전체 0매치** |
| 실7 | 계약·스키마 | 현행 `c11`(계약 `:3`) · `c12`/`generation_*`/`generation_jobs·attempts·claims` **전부 0매치** · payload 화이트리스트 **런타임 강제 없음**(계약 `:86` 자인) · `payload.ver` 규범은 C0 문서(`:175`)에 있고 계약 JSON 엔 없음 · **스키마 한글 열 0개**(규약 `:70` 존중돼 있음) |
| 실8 | 구제·순서 | `구제할까` = 배정0 AND 동의 AND 오늘(3조건 · `오늘과제.js:766~771`) · deliver 는 완전 직렬(`:370~384` · Promise.all 0) · **게임배정이 오늘과제보다 먼저**(`:442`→`:513` · 게임날 조기 return) |
| 실9 | `skill_ids` 소비 | `성과회수.js` 에 0회(주석 `:36~39`는 개입↔관측 경고). 읽는 곳 = 라디오승격·라디오편성·문법급수·events 검증뿐 |
| 실10 | 문서 자기충돌 | §3-5(claims·시도·landed·포기)와 §3-5-b(jobs·attempts·착지·마감폴백)가 공존 · §3 도식 `:212`·`:361`·`:375` 는 「5명·⌈17/5⌉」인데 §3-2 표는 **3** · §5-1 재료1(프롬프트 템플릿)↔§6 E군 표(promp트=prompt_ver 몫)↔§11-2 `:1191`(모델 ID까지 policy_ver 재료) 3절 충돌 · §4-1 값목록 **15**인데 §3-6·§12-7 산술은 「행 13」 · 머리말 `:26`(차단자 1 남음)·`:39~40`(4회차 «통과» 선행)이 §16-1(해제·게이트=전건 처분)과 모순 |

## §3. 갈래 처분 — 뿌리 10군 46갈래

> 표기: ✅채택 · ◐부분 채택 · ❌기각. 처방의 집행 자리 = v5.2 절 번호. **v5 낙방의 뿌리 하나**: v5 는 §3-5-b·§7-2 등 «새 절을 얹고» 형제 절(§3-5·§3-6·§4·§5-1·§15·머리말)을 안 걷었다 — 46갈래 중 12갈래가 그 한 동작(형제 절 스윕 누락)에서 나왔다.

### A군 — 실행 장부·상태기계 (7)

- **A1 ✅ 상태기계 두 정본** 〔L1-1·L2-1·S3-1 · 3/3〕 §3-5 가 폐기됐어야 할 `generation_claims`·`시도·landed·포기` 판을 그대로 싣고 §3-6 표·§12-13·§12-15·§15-D 가 그 어휘를 전제한다(실10). ⚠v5 는 §3-6 머리에 «§3-5-b 가 정본» 주의만 달았다 — 번역 없는 면책은 두 정본이다. **처방**: §3-5 를 «보장의 뜻»(최소 1회 호출·착지 1회 + 펜싱 논증)만 남기고 물리 서술 전부 §3-5-b 로 · §3-6 표를 jobs 어휘(대기·claimed·착지·마감폴백·대상아님 + attempts 행)로 재작성 · §12-13·§12-15·§15-D 참조 갱신.
- **A2 ✅ 문서 상태·머리말 자기충돌** 〔S3-38〕 머리말 `:26`「차단자 하나」·`:39~40`「4회차 통과 선행」 vs §16-1「해제·게이트=전건 처분」 · 머리말 `:6`「v7·9축」(실1로 이중 낡음) · §15 표의 D(claims)·Z(`용량초과`)·AB(`position` 기록)·AF(통째 재사용) 행이 폐기된 규격을 「닫힌 자리」로 가리킴. **처방**: 머리말을 v5.2 상태로 재작성(게이트=전건 처분·5회차 처분 반영) · §15 해당 행에 「(vN 에서 대체 — 역사)」 표기. ➕곁들임: §14-1 대조군 항에 08-12 자율주행 판정(0% — Ⅰ-8 눈금이 다른 형태 · 재제안 금지)을 병기해 낡음을 걷는다.
- **A3 ✅ attempts append-only 자기모순** 〔L1-2·L2-2·S3-2 · 3/3〕 「갱신·삭제 없음」인데 `원응답·응답시각·결과` 를 나중에 채우라 하고, RPC 셋에 시도 기록 전이가 없다. **처방**: RPC 를 다섯으로 — `attempt_open`(호출 «전» INSERT · 요청본문) · `attempt_close`(응답 칸 **null→값 1회만** 채움 · `where raw_response is null` 가드) 추가. append-only 의 정확한 뜻을 「행 삭제·값 덮어쓰기 금지 — 응답 칸의 1회 채움만 허용」으로 재정의(시도 이력 보존이라는 원 목적 그대로).
- **A4 ✅ 승리 시도 식별 불가** 〔S3-3 · 단독〕 재시도 뒤 어느 응답이 학생에게 갔는지 계보가 없다. **처방**: jobs 에 `winning_attempt_id uuid` 1칸 — `job_종료` 가 착지 트랜잭션에서 채운다.
- **A5 ✅ 회수·펜싱 전이 구멍** 〔L2-3 · 단독〕 §3-5(`:477` 회수가 토큰 올림)와 §3-5-b 전이표(`:561` 안 올림)가 충돌하고, 착지 가드의 **상태 조건**이 없어 회수 직후(`대기`·낡은 토큰 일치) 좀비 착지 창이 남는다. **처방**: 한 목소리로 — 토큰은 **집기에서만** +1(회수는 안 올림 · 마감 스윕만 빼앗으며 +1) · 정상 착지 가드 = `상태='claimed' AND 펜싱=$내토큰` **둘 다**(상태 조건이 회수 후 좀비를 거절한다) · §3-5 옛 문장 삭제.
- **A6 ✅ RPC 계약 불완전** 〔L2-8·L1-15·S3-16·S3-17〕 `_사건 jsonb` 구조·이벤트별 필수 칸·반환 계약·멱등 재호출 답·회수/attempt/학생지정 전이가 미정의. **처방**: §3-5-b 에 `_사건` 스키마 전문(세 사건 각각의 event_type·payload(ver·generation_* 3칸·`output_text` 포함)·task_snapshot·occurred_at·멱등키) · 반환 = 성공→`assigned_event_id` / 펜싱·상태 불일치→**0행·null** / 이미 종료→기존 `assigned_event_id`(= HTTP 유실 뒤 재호출의 답 — 착지·미착지가 반환값으로 갈린다) · `jobs_적재` 유일키 충돌 = 기존 행 무변경(첫 적재가 정본 · 신규/기존 건수를 반환이 가른다 — 내용 대조는 안 한다).
- **A7 ✅ 배정일↔사건 시각 경계 미정** 〔L2-6 · 단독〕 **처방**: §11-4 에 1줄 — 세 사건 `occurred_at` = 착지 실시각 · 날짜 귀속·조회 닻 = `task_ref`(=`task-${배정일}` · G트랙 ⑩ 확정 재사용) · 마감 스윕 착지도 배정일로 귀속(06:00 경계에서 리듬 창 오차는 실시각 그대로 둔다 — 소급 조작 금지).

### B군 — 값목록·물리 제약 (4)

- **B1 ✅ 값 산술 15↔13 · 마감폴백/예산소진 불일치** 〔L1-13·L2-7·S3-4 · 3/3〕 §4-1 은 15값(v5 E군이 `응답초과` 를 더함), `이미배정`=응답 전용 → **행 14**인데 §3-6·§12-7·§11-2 는 「행 13」인 채다. §3-5-b `:564`「`마감폴백` 은 상태이자 종료사유」는 자기 `:507`(종료사유=§4-1 값목록)과 모순 — 그 목록에 `마감폴백` 이 없다. **처방**: 산술을 **행 14 + 응답 1 + 행 0** 으로 세 절 일괄 정정 · `:564` = 「상태=`마감폴백` · 종료사유=`예산소진`」.
- **B2 ✅ `응답초과` 판정값 부재** 〔L2-10·S3-5〕 **처방**: 상한 = **8000 코드포인트**(입력과 같은 상수 하나) · 측정 시점 = 파싱 «전» 원문 · attempts `결과='응답초과'` + `원응답`=앞 8000 그대로(결과값 자체가 「절단본」 표시 — 입력과 달리 응답은 비결정이라 조각이 증거다. 입력초과의 null 규칙과 다른 이유를 명시).
- **B3 ✅ DDL 이 불가능 상태 허용** 〔L1-10·S3-26〕 종료사유·결과·검문실패사유 자유 text · 상태 결합 제약·FK 없음. **처방**: CHECK 3벌(job 종료사유 ∈ §4-1 행 14값 · attempts 결과 ∈ 7값 · 상태별 결합 — `착지⇒assigned_event_id·종료시각 not null`·`대기⇒owner·lease null`) + `assigned_event_id` FK→`learning_events(event_id)`+unique. 값·픽스처는 §4-1 한 곳에서 파생(사본 금지).
- **B4 ✅ `대상아님` 원인 소실** 〔S3-9 · 단독〕 `_대상` 예시의 `대상아님사유?` 가 DDL 에 칸이 없다 — §7-1 이 약속한 「초급 도달률 분포」를 못 센다. **처방**: jobs 에 `not_target_reason` text 1칸(`첫날|교정문|초급|미정` · CHECK) — §12-8 분포가 이 칸으로 갈라 센다.

### C군 — 입력 스냅샷·상태 실물 (6)

- **C1 ✅ job 에 판정 당시 입력이 없다** 〔L1-3·L2-5·S3-8 · 3/3〕 `snapshot_as_of`+`skill_ids` 만으로는 「왜 대상이었나」(첫날·교정문·게임·급수·목표)를 소급 못 한다. **처방**: jobs 에 `branch_snapshot jsonb` 1칸(갈래·급수·goal 스냅샷·교정문 ref·게임 판정 — 선판정 «출력» 봉투) + 규칙 1줄 「적재 뒤 도착한 재료는 오늘 판에 안 끼어든다(다음 배정일 몫)」. 상태 «값»은 따로 안 싣는다(§6-2 유지 — `generation_input_text`+`evidence_refs` 가 진다).
- **C2 ✅ 입력초과 시 축값 소실** 〔S3-24 · 단독〕 + **C3 ✅ 입력초과 감사 증거 부재** 〔S3-25 · 단독〕 **처방(한 벌)**: attempts 행 생성 시점을 「부를 때」→「**조립할 때**」로 — 입력초과도 attempts 에 행을 만들어 `요청본문` **전문**을 표에 보존한다(표는 payload 가 아니라 8000 상한 무관 · 호출은 안 하므로 원응답 null·결과='입력초과'). payload 의 `generation_input_text=null` 규칙은 그대로(계약 면적 불변 · 증거는 장부가 진다).
- **C4 ✅ `ingested_at` 경계가 명령만 있고 배선이 없다** 〔L1-7·L2-17〕 §11-4 는 두 경계를 걸라는데 실물 필터는 `occurred_at` 뿐(실2)이고 §12 에 그 픽스처가 없다. **처방**: §3-3 과 같은 «선행 작업» 형식으로 — 학습자상태 창안 필터에 `옵션.ingested_as_of` 추가(미전달 = 현행 동일 · 생성 경로만 전달 · 추정판 올림) + §12 에 늦적재 배제 픽스처 1벌.
- **C5 ✅ 상태판 서술 삼중 낡음** 〔L1-6·L2-16·S3-23 · 3/3〕 실물 v9·11축(실1)인데 머리말·§4-3 은 「v7·9축」, §6-1 예시는 「창일수 14」, §6-2 는 「축밖 3」. §4-3 스스로 「판 이름을 문서에 박지 않는다」고 적고 **바로 위에서 박았다**. **처방**: 세 자리의 판·축수·창 숫자를 전부 「실물 = 코드 정본(지금 v9·11축)」 참조로 바꾸고 «판번호≠축수» 1줄 · 예시 JSON 의 14→30. ⚠talk `학습자상태.js:32~33` 머리말도 낡았다(축밖 셋) — 구현 커밋 몫으로 §12 에 기록.
- **C6 ◐ 벤더 감사 정보 부족** 〔S3-35 · 단독〕 지연시간은 `요청시각/응답시각` 차로 이미 나오고, usage·request-id 는 벤더 응답 원문 안에 있다. **채택분**: 「`원응답` 은 성공·실패 **모두** 보존한다(usage·request-id 가 그 안에 산다) · 벤더오류 시 오류 응답 원문이 `원응답`」 명시. **기각분**: 전용 칸 신설(면적 — 경량 원칙 · 필요해지면 원응답 파생으로 충분).

### D군 — 갈래·폴백·degraded (4)

- **D1 ✅ 비대상 원자 착지 입력 부족** 〔L1-4·S3-18〕 `jobs_적재` `_대상` 에 착지 자료(스냅샷·교정문·output_text)가 없어 §3-4 ⑥' 와 물리 계약이 안 이어진다. **처방**: 비대상 착지 = **같은 `job_종료`** 를 쓴다(적재와 한 트랜잭션 · `_사건` 규격은 A6 이 정의) — 별도 경로 0 · 「착지도 마감도 이 하나」가 ⑥' 까지 확장.
- **D2 ✅ 폴백 `출처` 3값 분열** 〔L1-5·L2-21·S3-19 · 3/3〕 §4=`전날|도입` · §7-2=`'폴백'` · §11-3=`전날` · 실물 리터럴엔 `'폴백'` 이 없다(실3). **처방**: **`'폴백'` 리터럴 철회** — 폴백 행의 `출처` = 오늘 조립기가 낸 값 그대로(`교정문|전날|도입` · 재호출 처방은 유지), 「왜」는 `generation_outcome`+`degraded` 가 진다(새 리터럴 = `'생성'` 하나뿐). §4·§7-2·§11-3 세 절 동축 정정. ◐L2-21 후반(폴백 반복이 중복 검사를 우회): 중복 검사 범위=생성문은 설계 의도가 맞다 — 「폴백 반복은 이 게이트 밖(현행 라이브 동작 유지 · 별건)」 경계 1줄만 명시.
- **D3 ✅ `degraded` 뜻 오염** 〔L2-19·S3-20〕 §5-4 「v4 이후 true=AI 가 오늘 실패했다」는 구제경로(의도적 미호출)·초급 제외(범위 결정)를 장애로 읽게 한다. **처방**: §5-4 재기술 — true 의 뜻 = 「그날의 최상 갈래(생성)가 아닌 것이 나갔다」(원뜻과 연속) · 실패/의도/범위는 `generation_outcome` 이 가른다 · 원장 화면은 두 칸을 함께 읽는다.
- **D4 ✅ `payload.ver>=2` 필수화가 딴 생산자를 깨뜨림** 〔S3-21 · 단독〕 ver 는 사건 모양 판이지 「과제 생성 통로」 표식이 아니다. **처방**: 검증층은 **값 검증만**(`generation_outcome` 키가 «있으면» §4-1 값목록 검사) · «존재» 강제는 §12-7 픽스처가 우리 생산자(deliver 말하기 갈래)에 진다 — 다른 개입 생산자는 ver=2 를 자유로 쓴다. §11-2 경계 문구 교체.

### E군 — 구제 경로 (2)

- **E1 ✅ 집기에 학생 선택자가 없다** 〔S3-14 · 단독〕 `jobs_집기(_배정일,_소유자,_개수,_임대초)` 로는 구제가 «그 학생» claim 을 못 잡는다 — 남의 job 을 가져간다. **처방**: `_learner uuid default null` 인자 추가(지정 시 그 학생 job 만 · 미지정 = 현행).
- **E2 ✅ `claimed` 중 구제 응답 규격 부재** 〔L2-4·S3-15〕 워커가 생성 중이면 배정이 아직 없어 재조회가 빈손 — 대기·응답 규칙이 없다. **처방**: 규격 명문화 — claim 못 잡고 배정도 없으면 **현행 그대로 빈 배정 반환**(200 · 재조회 1회 유지 · 계약 변경 0) + 앱은 기존 「오늘 과제를 읽지 못했어요」 폴백 카드(다음 열기가 워커 착지분을 본다) · §12-11 픽스처에 이 갈래 추가. 밀어붙인 실시간 대기(폴링·소켓)는 §3-1 근거로 안 한다.

### F군 — 운영 배선·계측 (4)

- **F1 ✅ 반복 워커·마감 스윕 배선 0** 〔L1-12·L2-20〕 실물 cron 4개에 워커·스윕이 없다(실6) — 스케줄러가 없으면 큐는 영원히 `대기`다. **처방**: §3-2 에 배선 표 신설(전부 안전선·pg_cron·`cron_c10.sql` 선례 형식) — `deliver-daily` 16:05Z = 오케스트레이터(현행 잡 재사용) · **`generate-worker`** `10-50/10 16-21 * * *`Z(00:10~05:50 UB 10분마다 — 17÷3=6회 필요 대비 여유·겹침은 claim 이 막는다) · **`generate-deadline`** `0 22 * * *`Z(06:00 UB 스윕 모드) · 등록 마이그는 멱등(unschedule 후 재등록)·Vault 자격증명 선례 그대로 · §12 에 「잡 3종 등록 SQL 실재」 픽스처.
- **F2 ✅ 워커 상한 3↔5 동시 존재** 〔L1-11·S3-6〕 §3-2 표=3 인데 도식 `:212`·`:361`(⌈17/5⌉=4회)·`:375` 는 5 인 채다(실10). 상한 3으로 4회면 12명 — 5명이 마감폴백으로 밀린다(sol 산술이 맞다). **처방**: 세 자리에서 숫자를 걷고 「`워커학생상한`(§3-2 표 · 두 상수에서 계산 파생)」 참조로 통일 · 최소 회차 = ⌈재적수÷상한⌉(지금 ⌈17/3⌉=**6회**)로 재계산.
- **F3 ✅ CPU 계측 누락** 〔S3-36 · 단독〕 §3-2 가 「CPU 상한은 별도 축」이라 적고 §12-20 은 벽시계 3항만 잰다. **처방**: §12-20 에 항 4 「active CPU(호출당)」 추가 — 숫자 판정은 실측 후(우리 실측이 아닌 문서 링크 값을 상수로 안 박는다 · ㉮ 교훈).
- **F4 ◐ 마이그레이션 인수 절차 부재** 〔S3-37 · 단독〕 **채택분**: §12 에 신설 항(§12-21) — 빈 DB 적용·재적용 멱등·기존 판 공존(c11 행 무결)·함수 시그니처 재독증. **기각분**: 롤백 절차 — append-only 원칙과 기존 마이그 관행(27개 전부 롤백 없음 · 재실행 안전으로 갈음)이 근거.

### G군 — 계약·판·매핑 (7)

- **G1 ◐ c12 실물 부재인데 착수 차단만 해제** 〔L1-9·L2-9〕 **기각분**: 「c12 원문이 설계 시점에 없다」 자체 — §11-2 J군이 순서(①c12 개정→②검증기·마이그→③첫 생성 행)를 이미 못박았고, 계약 JSON 은 구현이 **정본과 한 커밋**으로 내는 물건이다(`:1198` · F302). 문서가 미리 만들면 그게 이중 정본이다. **채택분**: ①「§11-2 의 3칸 표가 곧 c12 diff 정본」임을 명시(구현이 이 표를 «옮겨 적는다») ②§12-21(F4 와 한 항)에 c12 검증기 왕복 픽스처 — ver2+3칸 통과 · 목록 밖 거절 · ver1 무결 · **값 검증**(D4).
- **G2 ✅ 한글 DDL vs 영문 규약 정면 충돌** 〔L2-25·S3-27〕 §11-2 가 「필드명=영문 snake_case·한글 🚫」를 확정해 놓고 §3-5-b DDL·RPC 는 `배정일·상태·펜싱…` 전부 한글이다. 실물 스키마는 한글 열 0(실7) — 규약이 살아 있다. **처방**: §3-5-b 식별자 전면 영문화(assign_date·status·fence·owner·lease_until·outcome·assigned_event_id·attempt_no·request_body·requested_at·raw_response·responded_at·result·gate_failed_reasons·jobs_load/claim/finalize·attempt_open/close) — **값은 한글 유지**(`'대기'`·`'착지'` … — `task_type='숙제제출'` 선례·§11-2 ㉴ 와 동축).
- **G3 ✅ 판 계보 3절 충돌** 〔S3-7·L1-16〕 §5-1 재료1(프롬프트 템플릿)·§11-2 `:1191`(모델 ID·옵션까지 policy_ver 재료) vs §6 E군 표(프롬프트·옵션=prompt_ver · 모델=model) — §5-2·§6 자신이 「policy_ver 에 모델 금지」다(실10). **처방**: §6 E군 표를 유일 정본으로 — §5-1 재료를 **6개**로(프롬프트 템플릿 행을 prompt_ver 파생으로 이동) · `:1191` 문구 정정 · §12-5 회귀 벌수 7→6 동축 · 지문 산출 규격 1줄(재료 정규 직렬화 → 기존 `판지문` 장치 방식 그대로 · 산출 자리 = 배치 시작 1회).
- **G4 ◐ 요약 입력이 계약으로 안 닫힘** 〔L2-12 · 단독〕 **기각분**: 하위 지표·단위·순서를 문서에 베끼는 것 — §6-2 의 「사본은 갈라진다」가 옳고, 드리프트는 판지문이 잡는다. **채택분**: 그 판지문의 해시 대상을 명시 — 「재료 2 = `lib/과제요약.js` **파일 내용 해시**(프롬프트 파일과 같은 방식)」 1줄(이게 없으면 「규칙이 바뀌었는데 판이 안 오른다」가 실제로 가능했다).
- **G5 ✅ `output_text` 사슬 단절** 〔L2-11·S3-13〕 ①듣기가 개입 payload `output_text` 를 읽는데(실5) §7 매핑·§11-2 신설 3칸 어디에도 그 칸의 유지 규칙이 없다 — ver2 에서 빠지면 학생 첫 듣기가 빈다. **처방**: §11-2 에 「ver=2 는 **추가**이지 대체가 아니다 — 기존 c11 출처 묶음(`output_text` 등)을 그대로 싣는다 · 생성 성공 시 `output_text` = 따라말하기 문장(=sentence · 라이브 `:596` 형태 유지)」 · A6 의 `_사건` 규격에 동봉.
- **G6 ✅ §7 매핑표가 조립기와 어긋남** 〔L1-14 · 단독〕 `선택지·선택차원=null` 은 조립기의 **키 생략** 규칙(실4 · 주석이 null 을 명시 거부)과 충돌 — §7-2 가 「null/생략 갈림」을 결함이라 불러놓고 §7 표가 null 을 박았다. `task_schema_ver` 는 호흡 칸이 아니고 오늘과제 경로는 안 싣는다(실4). **처방**: 표 정정 — 선택지·선택차원 = **키 생략**(조립기 규칙 그대로) · `task_schema_ver` 행 삭제(스냅샷 최상위 `ver:1` 이 이미 모양 판 · 생성분 모양 불변이라 새로 안 만든다).
- **G7 ✅ 큐 회전이 SQL 계약에 없다** 〔L2-24·S3-28〕 §11-5 회전을 확정했는데 `jobs_집기` 는 `created_at` 인덱스뿐 — 실행됐는지 검증 불가. **처방**: 집기 정렬을 RPC 계약 문장으로 — 「`order by` = `learner_id` 오름차순을 `해시(배정일) mod 재적수` 시작점으로 회전(동률 없음 — learner_id 유일)」 · 인덱스에 learner_id 포함 · §12 픽스처(같은 배정일 두 번 집기 = 같은 순서 재현).

### H군 — skill_ids (3)

- **H1 ✅ 생산자 규칙 부재** 〔L1-8·L2-13·S3-10 · 3/3〕 「배치가 먼저 고른다」뿐 — 원천·후보·우선순위·빈 값 뜻이 없고, `default '{}'` 가 생산 실패와 「기술 없음」을 같은 모양으로 만든다. **처방**: §6-0 에 규칙 명문화 — 입력 = **급수**(`lib/문법급수.js` 의 급수→기술 매핑 실재 · 실9)와 `engine.skills` 시드 · 선택 = 그 급수 후보 중 「최근 `중복창_일` 안 겨냥 안 한 것」 우선의 결정적 회전(해시(learner_id·배정일)) · 개수 1~2 · 존재 검증 = events fn 의 DB 대조 선례 · **상태·오류태그는 입력이 아니다**(계약 v3.6 조건 3 — 시점 충돌도 이걸로 소멸: 급수는 오케스트레이터 선판정 재료라 상태 계산 전에 있다 · S3-10 후반의 구조 모순 해소) · `[]` = 「그 급수 후보 목록이 비었다」 하나로 정의(생산 실패는 `내부오류`).
- **H2 ✅ 관찰/처치 혼동** 〔L2-14·S3-11〕 겨냥한 기술은 **개입 의도**지 학생 오류 증거가 아니다 — §6-0 이 「착지가 A-1 ㉠(무엇을 틀리는가)을 채운다」고 적으면 사전 가설이 실력 이해에 굳는다. **처방**: §6-0 정정 — 「`skill_ids` 는 ㉠의 **값이 아니라 조인 키**다. ㉠이 자라는 지점 = 후속 산출(제출·교정·오류태그)과의 **조인**(겨냥 기술별 후속 오류율)」 · 관찰/처치 분리 1줄 명시.
- **H3 ✅ ㉠ 소비자 미지목** 〔L2-15·S3-12〕 성과회수는 skill_ids 를 안 읽는다(실9) — 「늘었나」 주장이 `✓✓✗` 다. **처방**: 소비자를 이름으로 — `lib/성과회수.js` 확장(개입 행 skill_ids × 후속 `submission.created`·교정 결과 조인 → 겨냥 기술별 성과 칸) · §12-17 사슬 픽스처에 「skill 조인이 실제로 값을 낸다」 추가 · 완결 3칸을 그 기준으로 다시 적는다(모였나=행 · 닿았나=조인 소비자 · 늘었나=㉠의 「겨냥 대비 산출」 칸).

### I군 — 품질 게이트 §8·§8-B (8)

- **I1 ✅ 평가셋 재현 값 미확정** 〔L1-17·L2-22·S3-34 · 3/3〕 §8-B·§12-19 가 「갈래 목록·반복·불일치 처리를 적어야 한다」고만 하고 값을 안 냈다 — 다음 실행자가 다른 시험을 같은 이름으로 친다. **처방**: 값을 §8-B 에 박는다 — 갈래 = **급수(3~6 · 4) × 상태(상태없음·표본적음=쓸축수 1~2·표본풍부=3+ · 3) = 12칸**, 칸당 3~4 스냅샷 = N=40 유지 · 갈래당 생성 **2회**(비결정 — 1회는 표본이 아니다) · 채점 = **채점자 1인 확정제**(1인 학원 — 불일치 조항은 채점자가 2인이 되는 날 그 판에서) · seed = 시험지에 스냅샷 자체를 동봉(재추출 없음 — 그게 「시험지 해시」의 뜻).
- **I2 ✅ 게이트 실행층 부재** 〔S3-22 · 단독〕 결과 저장·실행판 연결·노출 차단기가 없어 평가 없이 열 수 있다. **처방**: 결과 정본 = `evals/과제생성_결과.json`(`출력_v1api.json` 선례 모양 · 실행판 8칸 동봉) · 차단기 = **회귀 테스트**가 「생성 경로 활성이면 현행 model+prompt_ver+policy_ver 조합의 통과 결과가 그 파일에 실재」를 검사(§12-14 에 기계 형태로 편입 — 골대 파일이 없으면 빨강).
- **I3 ◐ 평균이 취약 구간을 숨김** 〔S3-29 · 단독〕 축별 «각각» 통과선은 이미 §8-B 에 있다(합산 금지). **채택분**: 급수 구간 분해 추가 — 「축×급수 셀로 나눠 보고하고, **한 급수가 전 축 미달이면 전체 미통과**」 1줄. **기각분**: 셀별 통과선 상수 신설(N=40 에서 셀당 3~4건 — 셀 단위 문턱은 표본이 못 받친다 · 첫 분포 후 별건).
- **I4 ✅ 한국어 정확성 축 부재** 〔S3-30 · 단독〕 연결성·답변 가능성이 있어도 «틀린 한국어»가 초록을 받는다 — 어학원 산출물에서 실사용 도움의 바닥이다. **처방**: 축 6 「**정확성**(문법·자연스러움 · 0/1)」 신설 · 통과선 **≥ 0.95**(주관성이 낮은 축이라 높게 — ⑤재미를 낮게 둔 것과 같은 논리의 반대 방향) · AI 초벌+사람 확정.
- **I5 ✅ 목표 정합 축 부재** 〔L1-18·L2-23·S3-31 · 3/3〕 목표(goal)가 계약 v3.6 의 셋째 입력 축인데 §8-B 가 그 활용을 안 잰다 — 판단 정본 ①「그 학생의 목표」가 게이트에 없다. **처방**: 축 7 「**목표 활용**(질문이 그 학생 목표 맥락을 실제로 썼나 · 0/1 · 사람)」 신설 · 통과선 **≥ 0.7**(재미와 같은 급 — 주관 축) · ⚠런타임 판정이 아니라 오프라인 채점이므로 철학 ①의 ⚠(실시간 자동 판정 금지)와 충돌하지 않음을 명시. TOPIK 직결은 축 ③(수준 적합성)의 채점 기준에 「그 급수 문형·어휘 범위」로 편입(새 축 아님).
- **I6 ◐ 재미 ⓒ층 재개 트리거 부재** 〔S3-32 · 단독〕 **채택분**: 재개 닻 1줄 — 「ⓒ층은 **첫 시즌 회고**(철학 Ⅰ-8 의 판정자·주기)에서 연다 — 눈금은 그때 실데이터로 정한다」(숫자를 지어내지 않는 규율 그대로). **기각분**: 지금 재방문·완료율 눈금 상수화(학생 0명 — 못 재는 것을 게이트에 넣지 않는다).
- **I7 ◐ 신선함 라이브 드리프트 재개 부재** 〔S3-33 · 단독〕 **채택분**: I6 과 같은 닻(시즌 회고에서 상투구 비율을 실생성물로 재고 필요시 §8 중복 검사 정교화를 별건으로 연다) 1줄. **기각분**: 지금 정교화(재료 0 — v5 H군 판정 유지).
- **I8 ✅ 검문 적용 대상 문자열 모호** 〔S3-39 · P2 · 단독〕 **처방**: §8 표에 열 추가 — 각 항목의 대상: 길이=각각(문장 8~60·질문 6~60 이미 분리) · 한국어 비율·금칙 서식·식별자 역유입 = **문장·질문 각각**(둘 중 하나라도 걸리면 탈락 · 결합 검사 아님) · 질문 형태 = 질문만 · 중복 = 문장만. 실패 라벨 = §8 행 순서 첫 항목(기존 규칙 재확인).

### J군 — 기각 (1)

- **J1 ❌ 「원자 착지가 실패한 날을 상태에서 지운다」** 〔L2-18 · 단독〕 **기각(오독)**. 주장 = 워커가 마감 전 죽으면 「과제가 있었지만 배달 실패한 날」이 학습자 원본에서 사라진다. 실물: ①재진입 — 다음 워커 호출이 큐를 이어받는다(§3-2) ②그마저 못 하면 **06:00 마감 스윕이 임대를 빼앗아 폴백을 «착지»한다**(§3-5-b 전이표) — 세 사건이 서므로 그날은 `task.assigned`+`degraded=true`+`generation_outcome` 으로 상태·성과 원본에 남는다. 사라지는 날은 스윕 자체가 죽은 날뿐이고 그 감시는 §12-8 두 분모 차이 + F1 배선(신설 잡 실패 알림)이 진다. jobs 를 상태 계산에서 빼는 것은 자기 오염 방지(§3-5 의 원 논거)라 옳다.

## §4. 원건 82 전건 매핑 (게이트 ① — 빠짐 0)

| 원건 | 갈래 | 처분 | | 원건 | 갈래 | 처분 | | 원건 | 갈래 | 처분 |
|---|---|---|---|---|---|---|---|---|---|---|
| L1-1 | A1 | ✅ | | L2-1 | A1 | ✅ | | S3-1 | A1 | ✅ |
| L1-2 | A3 | ✅ | | L2-2 | A3 | ✅ | | S3-2 | A3 | ✅ |
| L1-3 | C1 | ✅ | | L2-3 | A5 | ✅ | | S3-3 | A4 | ✅ |
| L1-4 | D1 | ✅ | | L2-4 | E2 | ✅ | | S3-4 | B1 | ✅ |
| L1-5 | D2 | ✅ | | L2-5 | C1 | ✅ | | S3-5 | B2 | ✅ |
| L1-6 | C5 | ✅ | | L2-6 | A7 | ✅ | | S3-6 | F2 | ✅ |
| L1-7 | C4 | ✅ | | L2-7 | B1 | ✅ | | S3-7 | G3 | ✅ |
| L1-8 | H1 | ✅ | | L2-8 | A6 | ✅ | | S3-8 | C1 | ✅ |
| L1-9 | G1 | ◐ | | L2-9 | G1 | ◐ | | S3-9 | B4 | ✅ |
| L1-10 | B3 | ✅ | | L2-10 | B2 | ✅ | | S3-10 | H1 | ✅ |
| L1-11 | F2 | ✅ | | L2-11 | G5 | ✅ | | S3-11 | H2 | ✅ |
| L1-12 | F1 | ✅ | | L2-12 | G4 | ◐ | | S3-12 | H3 | ✅ |
| L1-13 | B1 | ✅ | | L2-13 | H1 | ✅ | | S3-13 | G5 | ✅ |
| L1-14 | G6 | ✅ | | L2-14 | H2 | ✅ | | S3-14 | E1 | ✅ |
| L1-15 | A6 | ✅ | | L2-15 | H3 | ✅ | | S3-15 | E2 | ✅ |
| L1-16 | G3 | ✅ | | L2-16 | C5 | ✅ | | S3-16 | A6 | ✅ |
| L1-17 | I1 | ✅ | | L2-17 | C4 | ✅ | | S3-17 | A6 | ✅ |
| L1-18 | I5 | ✅ | | L2-18 | J1 | ❌ | | S3-18 | D1 | ✅ |
| | | | | L2-19 | D3 | ✅ | | S3-19 | D2 | ✅ |
| | | | | L2-20 | F1 | ✅ | | S3-20 | D3 | ✅ |
| | | | | L2-21 | D2 | ◐ | | S3-21 | D4 | ✅ |
| | | | | L2-22 | I1 | ✅ | | S3-22 | I2 | ✅ |
| | | | | L2-23 | I5 | ✅ | | S3-23 | C5 | ✅ |
| | | | | L2-24 | G7 | ✅ | | S3-24 | C2 | ✅ |
| | | | | L2-25 | G2 | ✅ | | S3-25 | C3 | ✅ |
| | | | | | | | | S3-26 | B3 | ✅ |
| | | | | | | | | S3-27 | G2 | ✅ |
| | | | | | | | | S3-28 | G7 | ✅ |
| | | | | | | | | S3-29 | I3 | ◐ |
| | | | | | | | | S3-30 | I4 | ✅ |
| | | | | | | | | S3-31 | I5 | ✅ |
| | | | | | | | | S3-32 | I6 | ◐ |
| | | | | | | | | S3-33 | I7 | ◐ |
| | | | | | | | | S3-34 | I1 | ✅ |
| | | | | | | | | S3-35 | C6 | ◐ |
| | | | | | | | | S3-36 | F3 | ✅ |
| | | | | | | | | S3-37 | F4 | ◐ |
| | | | | | | | | S3-38 | A2 | ✅ |
| | | | | | | | | S3-39 | I8 | ✅ |

계 = L1 18 + L2 25 + S3 39 = **82**. ◐도 채택분이 있으므로 집행 대상이다(기각분은 각 갈래에 근거 명시).

## §5. 집행 지도 (게이트 ② — v5.2 가 이 절들을 연다)

머리말(A2·C5) · §1(D2) · §3 도식(F2) · §3-1(E2) · §3-2(F1·F2) · §3-4(D1) · §3-5(A1·A5) · §3-5-b(A3·A4·A5·A6·B1·B3·B4·C1·C2·C3·E1·G2·G7) · §3-6(A1·B1) · §4(D2) · §4-1(B1) · §4-2(B2·C6) · §4-3(C5) · §5-1(G3) · §5-4(D3) · §6-1(C5) · §6-2(C5·G4) · §6-0(H1·H2·H3) · §7(G5·G6) · §7-2(D2) · §8(I8·I7) · §8-B(I1·I2·I3·I4·I5·I6) · §11-2(B1·D4·G1·G3·G5) · §11-3(D2) · §11-4(A7·C4) · §12(다수 — 7·13·15·20 정정, 21 신설) · §14(A2 곁들임) · §15(A2) · §16-1(전건 처분 반영).

## §6. 🚫 재제안 금지 (v5 처분 신설)

- 🚫 attempts 를 「응답 칸 1회 채움」 넘어 자유 UPDATE 로 되돌리기(A3 의 재정의가 상한이다).
- 🚫 `'폴백'` `출처` 리터럴 되살리기(D2 — 새 리터럴은 `'생성'` 하나뿐).
- 🚫 `payload.ver>=2` 를 「과제 생성 통로」 표식으로 다시 쓰기(D4 — ver 는 모양 판이다).
- 🚫 §3-5-b 식별자를 한글로 «편의상» 되돌리기(G2 — 값 한글·이름 영문).
- 🚫 프롬프트 템플릿을 `policy_ver` 재료에 다시 넣기(G3 — §6 E군 표가 유일 정본).
- 🚫 `skill_ids` 착지를 ㉠실력 이해의 «값»으로 세기(H2 — 조인 키다).
- 🚫 「원자 착지가 실패한 날을 지운다」 재제안(J1 기각 — 마감 스윕 착지가 남긴다).
- 🚫 셀(축×급수)별 통과선 상수 지금 신설(I3 — 표본이 못 받친다 · 첫 분포 후).

## §7. 남는 것 (이 처분이 안 닫는 것)

- **재심문(6회차)** — v5.2 집행 후 «그 시점의 현재 판»을 던진다(F281 · v5.1 지문 고집 금지). GPT 한도는 계정 교체로 열려 있음(5회차 완주가 증거).
- **⛔구현 착수 차단은 유지** — 게이트 ①(이 표)+②(v5.2 집행)가 서면 «이 회차분»은 처분 완료지만, 유호 확정 규칙상 새 회차 지적은 다시 전건 처분을 지난다.
- talk `학습자상태.js:32~33` 낡은 머리말(축밖 셋) 수리 = 구현 커밋 몫(C5 ⚠).
