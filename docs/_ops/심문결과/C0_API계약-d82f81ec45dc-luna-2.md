지적 29건. 결론: 동결 불가.

[P0] 동결 판정이 실측을 가장한다 — §7은 왕복 6을 동결 기준으로 삼지만 §8-2는 1~4가 리허설, 5·6이 회귀임을 인정한다. 운영 미배포·실제 철회·실제 만료를 검증하지 않은 초록은 동결 증거가 아니다. 문제 절: §7·§8·§4-3.

[P0] 저장을 학습으로 오인한다 — C0는 `learning_events` 저장만 정의하고 엔진 부품·소비자·도달 확인·회수 경로를 정의하지 않는다. `SYNK-appsscript`의 엔진 도달 감사는 13종 도달 0, 학습자 상태 파생 0, 승격 행 0이다. 문제 절: §4-1·§10, `docs/_ops/엔진도달_전수감사.md`.

[P0] 계약 버전이 한 문서 안에서 세 갈래다 — §0은 현행 계약을 c6이라 쓰고, 헤더·응답은 c10, payload 필드 목록은 c9라고 쓴다. 실제 계약 JSON은 c10이므로 구현자는 필수 필드와 DB 제약 중 어느 판을 따라야 하는지 결정할 수 없다. 문제 절: §0·§3·§4-1·§6.

[P1] 닫힌 C0 범위를 위반한다 — 인계문은 C0 엔드포인트가 2개뿐이라고 닫았지만 이 문서는 events·uploads·조회 3종·인증 3종을 한 문서에 포함한다. §4의 “지금 만드는 것은 둘”도 본문과 모순되어 승인 대상 자체가 불명확하다. 문제 절: §4-3~§4-6 및 인계문 C0 결정.

[P0] payload 스키마가 문서에만 있다 — §4-1은 `payload.ver`와 이벤트별 필수·허용 필드를 요구하지만 실제 검증은 payload가 비어 있을 때 `ver`를 허용하고, 임의 키와 타입을 완전 검증하지 않는다. `{}` 또는 잘못된 구조가 불변 원본으로 저장되면 나중에 판독·재계산할 수 없다. 문제 절: §4-1 “payload 규격”, `lib/이벤트검증.js`, `functions/events`.

[P0] `goal_snapshot` 누락이 정상으로 위장된다 — §4-1은 목적이 소급 불가라며 앱 필수라고 적었지만 §4-3과 검증기는 선택 필드로 둔다. 서버는 누락과 실제 목적 미상을 모두 `null`로 저장하므로 앱 결손을 복구할 방법이 없다. 문제 절: §4-1·§4-3·`lib/이벤트검증.js`.

[P0] 하위호환 정책이 거짓이다 — §6은 구 앱을 계속 받거나 필요할 때만 426을 내린다고 하지만 서버는 선언된 버전과 무관하게 현행 c10 검증기를 적용한다. 구 앱의 c9 이후 필드 누락은 변환도 426도 아닌 영구 `rejected`가 되어 데이터 구멍이 된다. 문제 절: §6·`functions/events`.

[P0] 재시도·부모 고리의 의미를 검증하지 않는다 — §4-1·§4-3은 `retry_of_event_id`를 원 제출, `parent_event_id`를 특정 배달·제출 사건으로 정의한다. 실제 `고리풀기`는 같은 학생의 사건이 존재하는지만 보므로 `task.assigned`나 임의 사건을 가리킬 수 있고, 결과 변수와 개입 계승이 오염된다. 문제 절: §4-1·§4-3 ①·`functions/events`.

[P0] 멱등키 충돌이 데이터 손실로 둔갑한다 — DB는 `(learner_id, idempotency_key)`만 유일하게 만들고 요청 본문·원문·음성 참조의 동일성을 비교하지 않는다. 키를 재사용한 새 사건도 원래 사건의 `duplicate`로 응답되어 새 증거가 사라지며, UUID v4 형식조차 서버에서 강제하지 않는다. 문제 절: §4-1 응답·`functions/events`.

[P0] `task_ref`가 인과 식별자가 아니다 — §4-1·§4-3은 이를 통해 배정과 개입을 연결하지만 FK·유일성·배정 `event_id`를 요구하지 않는다. 실제 서버는 같은 학생의 동일 `task_ref` 중 최신 행을 임의 선택하거나, 못 찾으면 연결 없이 저장한다. 문제 절: §4-1·§4-3 ①·`functions/events`.

[P0] `degraded`가 제출 사건에서 사라진다 — C0는 서버가 채운다고 적고 `/tasks`는 값을 내려보내지만 앱 제출 재료에는 없으며 `events` INSERT도 해당 열을 쓰지 않아 DB 기본값 false가 들어간다. 고정 도입·강등 과제의 발화가 정상 AI 과제로 기록되어 처방 효과가 영구적으로 왜곡된다. 문제 절: §4-1·§4-3 ①·`functions/events`.

[P0] 정확한 동의 고리가 C0에서 빠졌다 — 서버 채움 표에는 `consent_ver`만 있고 실제 L0·c10·events 구현은 `consent_id`를 함께 요구한다. 같은 버전으로 재동의한 경우 버전만으로는 어느 동의 행이 근거였는지 식별할 수 없어 철회·승격 계보가 끊긴다. 문제 절: §4-1, L0 §3-6·§6-2.

[P0] `data_use.granted`가 빈 승인 플래그다 — C0는 승격 사건 이름만 두고 `artifact_ref`, `content_hash`, `serializer_ver`, `dataset_ver`, 승인자·평가·롤백 계보를 계약하지 않는다. 생산자도 0이므로 “무엇을 어떤 동의로 승인했는가”를 나중에 재구성할 수 없다. 문제 절: §4-1 payload 목록·§10, L0 §6-2.

[P0] 서명 URL이 철회 후에도 살아 있다 — §4-2는 서명 발급을 동의 게이트로 삼고 600초짜리 직접 PUT URL을 준다. 발급 뒤 동의가 철회되어도 PUT 자체는 막히지 않고, 이후 events가 거절해도 이미 올라간 원본은 남는다. 문제 절: §4-2·§5·§10.

[P0] WAV·AGC 축이 열린 채 동결 후보로 남아 있다 — §4-2가 AGC 제어 불가, `unknown` 상시 기록, iOS·Android 실기기 미측정을 명시한다. 샘플레이트·자동처리 차이는 음성 원본에서 소급 복원할 수 없으므로 이 상태의 동결은 오염된 음성 의미를 고정한다. 문제 절: §4-2 음성 인수 조건.

[P0] `content.viewed`가 디스크에 보존되지 않는다 — §4-3은 실제로 들었는지를 판정하는 유일한 사건으로 쓰지만 `열람사건` 자체가 앱 종료 전에 전송되지 않으면 사라진다. 합성 리허설은 통과해도 실제 청취 관측은 영구 누락된다. 문제 절: §4-3 ①·`lib/오늘과제.js`.

[P0] 첫 로그인 단일 통로와 동시성 보장이 모두 깨졌다 — §2·§4-4는 서버의 유일 통로와 패배 요청의 비밀번호 불변을 선언하지만 공개 GoTrue 가입이 실제로 열려 있고, 기존 합성 이메일이면 비밀번호를 덮어쓴다. 두 first-login 요청이 동시에 오면 둘 다 GoTrue를 통과한 뒤 한 요청이 다른 요청의 비밀번호를 바꿀 수 있다. 문제 절: §2·§4-4·`functions/auth`, L0 §8-2.

[P0] 임시번호가 동시 실행에서 1회용이 아니다 — §4-6은 검증 후 해시를 지운다고만 하고, 구현은 유효성 조회·GoTrue 비밀번호 변경·해시 삭제를 잠금이나 조건부 소비 없이 수행한다. 같은 임시번호를 두 요청이 동시에 사용하면 둘 다 성공하고 마지막 새 비밀번호가 승자가 된다. 문제 절: §4-6·`functions/auth`.

[P0] 첫 로그인 성공이 첫 발화 보존을 보장하지 않는다 — 실제 `첫배정세우기`가 실패해도 first-login은 HTTP 200을 반환하고, 당일 앱은 제출 재료 없는 폴백으로 내려간다. 다음 배치까지 기다리는 동안 첫 목소리는 사라지며 `ok:true`가 성공과 손실을 구분하지 못한다. 문제 절: §4-3 ①·§4-4·`functions/auth`.

[P0] E1 인증 미완 상태를 라이브로 선언한다 — 인계문은 옛 refresh/access token 차단과 강사·원장·운영자 인증을 E1/N10 전제 미완료로 남겼다. 그런데 C0 §4-5·§4-6은 원장 reset과 임시로그인을 라이브 계약처럼 확정해 동결 상태를 서로 다르게 표시한다. 문제 절: §4-5·§4-6 및 인계문 §6·§10.

[P1] 부분 실패 보장이 비정상 배치에서 깨진다 — §4-1은 어떤 한 건이 틀려도 나머지를 저장한다고 하지만 외부 루프가 원소별 `try` 전에 `사건.idempotency_key`를 읽는다. `null` 원소 하나면 전체 배치가 예외로 끝나 head-of-line 방지가 무너진다. 문제 절: §4-1 응답·`functions/events`.

[P1] 타학생 `correction_id`가 재시도 가능한 5xx가 된다 — §4-1은 교차 학생 참조가 서버 오류라고 명시하고 §5는 5xx를 재시도 대상으로 둔다. 실제 트리거 메시지는 events의 400 변환 목록에 없어 잘못된 항목이 영구 재시도 큐가 된다. 문제 절: §4-1·§5·L0 스키마 교차학생 트리거·`functions/events`.

[P1] UTC 형식이 강제되지 않는다 — §3은 `Z`가 붙은 ISO UTC를 요구하지만 구현은 `Date.parse`만 호출해 날짜 전용·오프셋·로컬 형식을 받아들인다. 이 불변 시각이 동의 유효성·몽골 날짜·progress 계산에 그대로 사용되어 입력 표기 하나로 분류가 달라진다. 문제 절: §3·`functions/events`.

[P1] `task_snapshot`의 의미가 두 개다 — §4-1/L0는 정답 포함 스냅샷을 요구하고 앱이 GET 응답을 되싣는다고 하지만 실제 `/tasks`는 정답과 허용목록 밖 필드를 제거한다. 제출 스냅샷과 배정 스냅샷이 달라져 “학생이 본 판”과 “채점 원본”을 계약 하나로 증명하지 못한다. 문제 절: §4-1·§4-3 ①·`functions/tasks`·`lib/오늘과제.js`.

[P1] RLS가 API 보안 경계가 아니다 — §1은 직접 Postgres 연결을 쓰고 조회 절도 `service_role` 우회 때문에 수동 `where learner_id`가 유일한 방어선임을 인정한다. 엔드포인트 하나의 누락된 조건으로 타학생 데이터가 전부 노출되지만 중앙 subject-scope나 전 경로 부정 테스트가 없다. 문제 절: §1·§4-3.

[P1] 빈 교정 필터가 빈 문자열을 막지 못한다 — §4-3은 교정문과 태그가 모두 비어 있으면 제외한다고 하지만 SQL은 `corrected_text is not null`을 사용한다. `''`는 통과하고 빈 카드가 실제 교정처럼 노출된다. 문제 절: §4-3 ②·`functions/corrections`.

[P1] `attempt_no` 하나로 progress 전체가 500이 된다 — §4-3은 숫자꼴만 읽는다고 하지만 정규식 통과 뒤 임의로 긴 숫자를 `::int`로 변환한다. 불변 payload 하나가 오버플로를 일으키면 조회 전체가 5xx가 되어 앱이 무한 재시도한다. 문제 절: §4-3 ③·`functions/progress`.

[P1] 25MB 제한은 클라이언트 주장에 불과하다 — §4-2는 서버가 크기와 형식을 검증한다고 하지만 서명 단계에서 앱이 보낸 `byte_size`만 검사하고 직접 PUT 후 실제 크기를 거부하지 않는다. 작은 크기로 신고한 대용량·다른 MIME 원본이 무제한 보존 경로에 들어간다. 문제 절: §4-2·`functions/uploads`.

[P1] skill taxonomy 검증이 판본 존재만 확인한다 — §4-1은 ID와 판본을 함께 검증한다고 하지만 구현은 판본 문자열의 존재와 `skill_id` 존재만 확인한다. 같은 ID가 판본 간 재사용되면 다른 지도 축의 사건이 현재 지도에 섞여 소급 불가능한 오염이 된다. 문제 절: §4-1·`functions/events`.

대안 구조 1개 — 현재 엔드포인트를 늘리지 않고, `event/endpoint manifest` 하나를 기계 정본으로 둔다. 각 사건에 버전별 요청 스키마, 허용 대상 사건 타입, 멱등 fingerprint, 동의·Storage 수명, 서버 파생 필드, 엔진 소비자·회수 경로, 실왕복 테스트를 함께 선언하고 C0 문서는 여기서 생성해야 한다.