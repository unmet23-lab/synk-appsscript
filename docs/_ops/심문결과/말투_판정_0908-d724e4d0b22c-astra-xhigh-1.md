확인 불가 — 감소한 46/102건 전체가 오탐이었다는 판정과 문안 개선 효과는 입증되지 않았습니다.

[P1] 검사 실패가 정상 통과로 바뀐다 — §4는 ‘거짓 초록’을 막는다고 하지만, [글검사.js:155](/C:/Users/q1212/Documents/SYNK-appsscript/tools/글검사.js:155)는 실행 오류를 빈 지적 배열로 돌려주고 집계에서는 오류를 버립니다. 검사기의 오류 종료, 잘린 JSON, 필수 항목 없는 `{}`를 주입한 재현에서 실패 조건 3/3개가 모두 성공 종료와 지적 0건으로 바뀌었습니다. 이 상태에서는 실제로 검사하지 못한 문서도 판정 자료에서 깨끗한 문서와 구별되지 않습니다.

[P1] 경계를 줘도 인용과 동작 순서를 인과로 오인한다 — §2㉡의 경계는 ‘이라고·이라는’만 피할 뿐, [글검사.js:77](/C:/Users/q1212/Documents/SYNK-appsscript/tools/글검사.js:77)의 의미 판별을 보장하지 않습니다. 재현 입력은 “우리는 이것을 교육이라 부릅니다” 또는 “문장을 골라서 읽습니다” 뒤에 “교실은 넓습니다. 수업은 짧습니다. 숙제는 많습니다”를 붙인 문단입니다. 비인과 사례 2/2개에서 보정 전 잡히던 두 규칙이 보정 후 모두 사라졌으므로, 현재 표본의 ‘거짓 통과 0/8건’으로 이 패턴을 동결할 수 없습니다.

[P1] 표본의 예외를 모든 HTML 산문의 면제로 확대했다 — §2㉠·§6은 실제 흠의 비율을 확인하지 않고 홍보물 전체에서 규칙을 끄며, 실제 [분류 코드:71](/C:/Users/q1212/Documents/SYNK-appsscript/tools/lib/대외문안.js:71)는 HTML이면 내용과 관계없이 홍보물로 취급합니다. “교실은 넓습니다. 수업은 짧습니다. 숙제는 많습니다. 교재는 얇습니다”라는 동일 산문도 Markdown에서는 두 규칙에 걸리고 HTML에서는 모두 사라졌습니다. 게다가 ‘다음 문단의 까닭을 못 본다’는 근거는 `endingRepeat`와 어긋납니다. 실제 [규칙 구현:16](/C:/Users/q1212/Documents/SYNK-appsscript/node_modules/hanlint/src/rules/sentence/endingRepeat.js:16)은 문단들을 이어 검사하며, 재현에서도 다음 문단에 까닭을 넣자 지적이 사라졌습니다.

[P1] 남은 지적의 종류와 위치를 잘못 판정했다 — §5의 ‘전부 산문이며 두 규칙이 옳다’는 선언은 실물과 다릅니다. 대상 파일 17/17개를 전량 재측정하면 남은 지적 중 11/56건은 홍보물에 있고, 19/56건은 긴 문장·명사 쌓기·제목 건너뜀 등 이번 두 규칙 밖의 지적입니다. 이들을 ‘그래서·때문에로 이을 산문’으로 넘기면 다른 결함에 엉뚱한 수정을 지시하게 됩니다. §6의 ‘홍보물에서 endingRepeat가 낸 42건’도 잘못된 귀속으로, 실제 구성은 endingRepeat 24/42건과 factListParagraph 18/42건입니다.

[P1] 자동 복구가 판정 자료의 JSON을 깨뜨린다 — §4가 보장하려는 설치 후 자동 복구에서 [글검사.js:246](/C:/Users/q1212/Documents/SYNK-appsscript/tools/글검사.js:246)은 복구 안내를 표준 출력에 먼저 씁니다. 이음말이 없는 상태로 `--json --전량`을 실행하도록 재현하면 안내문 뒤에 JSON이 붙어 전체 출력의 JSON 파싱이 실패하지만, 종료 코드는 성공입니다. 판정 지면이 사용하는 입력 통로가 복구가 필요한 바로 그 실행에서 깨집니다.

[P2] 자동 보정이 매일 쓰는 검사 경로에는 연결되지 않았다 — §4는 사람이 기억할 일을 없앴다고 하지만, [plain-words-guard.js:62](/C:/Users/q1212/Documents/SYNK-appsscript/.claude/hooks/plain-words-guard.js:62)는 보정 함수가 있는 도구를 거치지 않고 hanlint를 직접 실행합니다. 재현 순서는 패키지 재설치로 추가 표지가 없어진 상태에서 답변 훅부터 실행하는 것입니다. 별도로 `글검사.js`를 실행할 때까지 훅은 이전 목록을 쓰므로, 검사 결과가 실행 순서에 의존합니다.

수치와 검사 규칙 완화를 봤다 → 문제다. 위 지적의 대상은 검사 배관이며, 여기에 학생 경험 게이트를 억지로 적용하지 않았습니다.

읽은 파일: 후보 문서, 판정 HTML, `글검사.js`, 홍보물·대외문안 설정, 대외문안 분류 코드, 관련 훅과 hanlint 규칙·목록·로더. 결정·검수기각 기록은 관련 항목을 검색했고, 검사 대상 17/17개 파일은 자동 재측정했습니다. 안 본 것: 대상 문안 전체의 의미 적합성, 실제 화면, 배포 환경.

대안 구조 1개: **규칙별 근거와 미결 항목을 구분한 판정표**로 바꾸십시오. 각 규칙에 적용할 글의 형태, 오탐·정탐 재현문, 변경 전후 결과, 미확인 범위를 적고, 실행 결과에서는 ‘검사 완료·검사 실패·규칙 제외’를 분리해야 합니다. 지적 감소량은 오탐 해결량으로 부르지 말고, 남은 지적은 실제 규칙과 문서별로 넘기십시오.