지적 29건

[P0] 검수 위치가 닫힌 결정과 정면 충돌한다 — §0과 §3은 검수 대시보드를 SYNK-talk Expo 앱 내부 역할 화면으로 고정한다. 이미 닫힌 결정은 원장탭은 앱이 아니며 검수는 앱 밖 웹이다. 인증·번들·배포 경계가 달라지므로 이 상태로 동결할 수 없다.

[P0] 잠금 해제 선언이 문서 내부에서 반박된다 — 머리말은 failed_p0 5건 전부 처분 및 CHK-5만 잔여라고 하지만 §3은 검수 Edge Function 3구멍과 화면을 남은 차단으로 적는다. §4 #4·#5·#13·#14는 그 미구현 기능을 수용 조건으로 요구한다. 현재 검수 테스트도 직원 Edge Function 부재를 건너뛰므로 “전부 통과”는 판정이 아니다.

[P0] c6·c5를 현재 계약으로 오기재한다 — §1은 L0 스키마를 c6, §5는 오류축을 c5로 고정하지만 현행 계약·스키마는 c9이고 `content.viewed` 등 c9 사건이 존재한다. 이 문서를 따른 구현자는 낡은 사건·필드·검사 기준을 사용하게 된다. §4 #1의 SQL 원샷 성공은 계약 버전 불일치를 검출하지 못한다.

[P0] WAV 헤더로 AGC 해제를 증명한다고 거짓말한다 — §0과 §4 #8은 AGC·노이즈 억제 off를 수집 사실로 선언하지만 현행 C0는 iOS·Android에서 이를 강제·검증할 수 없고 `agc_verified`도 `unknown`이다. WAV 헤더에는 AGC 처리 여부가 없다. 첫 녹음의 손실 신호는 나중에 복원할 수 없다.

[P0] 저장 버킷 이름이 현행 업로드 통로와 다르다 — §1은 비공개 `homework` 버킷을 선언하지만 C0·L0·`lib/업로드경로.js`의 현행 버킷은 `learner-media`다. 잘못 적용하면 signed URL과 삭제 경로가 서로 다른 저장소를 가리킨다. §4 #6은 파일 존재만 검사하므로 이 오류를 통과시킨다.

[P0] 비식별화 단계가 존재하지 않는다 — §2는 원본 음성과 STT 원문을 Whisper와 Gemini에 바로 전달하지만 비식별화 규칙·결과·버전·실패 처리가 없다. §3의 비식별 산출물은 저장 직전에 처음 등장한다. 식별 정보가 외부 모델과 검수자에게 노출된 뒤에는 어떤 비식별 상태였는지 소급 재구성할 수 없다.

[P0] n8n 선점 명세가 원자적 실행 명세가 아니다 — §2 #1은 Select에서 `for update skip locked`를 한 뒤 별도 update를 하라고 적지만 SQL 함수·RPC·트랜잭션 경계가 없다. 두 워커가 같은 행을 읽고 각각 `processing`으로 바꿀 수 있다. `attempt_id`를 생성한다는 문장만으로 중복 처리가 차단되지 않는다.

[P0] 늦은 워커와 부분 실패를 막지 못한다 — §2 #5·#6은 전사 update, AI 교정 insert, 상태 update를 서로 다른 단계로 실행한다. `attempt_id` 조건·작업별 멱등키·원자 트랜잭션이 없어 lease가 만료된 옛 워커가 새 워커 뒤에서 기록할 수 있다. 전사만 성공하면 재시도 시 불변 전사 거부 또는 AI 교정 중복이 발생한다.

[P0] `task.assigned` 행이 AI 큐에 섞인다 — 현행 트리거는 모든 `engine.submissions` 행에 `pipeline_jobs`를 생성하고, `task.assigned`도 그 표에 저장된다. §2의 폴링 조건에는 `learning_events.event_type='submission.created'` 필터가 없다. 오디오 없는 배정 행이 재시도 횟수를 소모하고 큐를 오염시킨다.

[P0] 3회 실패 항목이 조용히 사라진다 — §2 오류 처리는 상태를 다시 `pending`으로 만들고 `attempt_count`를 올리지만, 폴러는 `<3`만 읽는다. `failed` 전환·실패 큐·알림·사람 검수 경로가 없다. 3회 실패 행은 새 항목이 없는 것과 같은 모양이 된다.

[P0] AI 결과 스키마가 DB와 프롬프트를 동시에 어긴다 — §2 #5는 `corrections.schema_ver`를 쓰지 않는데 현행 열은 NOT NULL이고 기본값이 없다. 출력은 `error_types`, `mongolian_explanation`을 요구하지만 계약·DB는 `error_tags`, `explanation`을 사용하고 현행 프롬프트 정본은 한국어 키를 낸다. 변환기가 없어 insert가 실패하거나 오류 태그가 빈 배열로 저장된다.

[P0] 검수 화면이 요구하는 데이터가 `review_queue`에 없다 — §3은 읽는 곳을 `engine.review_queue` 하나로 제한하지만 현행 뷰에는 AI 교정문, `correction_id`, 오류 태그, 해설, 모델·프롬프트 버전이 없다. 같은 절은 그 필드들의 프리필과 `reviewed_correction_id` 기록을 요구한다. 단일 뷰 제한과 화면 명세가 동시에 성립하지 않는다.

[P0] 폐기 항목이 큐에서 제거되지 않는다 — §3은 폐기 시 `pipeline_jobs.status='discarded'`만 기록하지만 현행 `review_queue`는 pipeline 상태를 보지 않고 AI correction 존재와 오디오 삭제 여부만 본다. 폐기한 행이 다시 검수 큐에 나타난다. 또한 뷰에 `is_audit_sample`이 없어 감사 우선 순서도 구현할 수 없다.

[P0] 검수 인증·오디오 서명 통로가 없다 — §3은 `service_role` Edge Function과 비공개 오디오 플레이어를 요구하지만 실제 검수 Function, 직원 검증, signed URL 발급, 조회 감사 기록의 실행 계약이 없다. C0상 `engine`은 PostgREST로 노출되지 않고 `service_role`은 RLS를 우회한다. 앱이 직접 접근하면 전 학생 데이터 노출이고, 직접 접근하지 않으면 오디오를 재생할 수 없다.

[P0] 청취 게이트가 화면 장식에 머문다 — §3과 §4 #5는 Enter를 UI에서 막는 것만 정의한다. 서버가 `review_listened_ms>0`, 활성 직원, 대상 AI 교정, 동일 학생 연결을 재검증하는 조건이 없다. 직접 호출·네트워크 재전송으로 0초 청취 승인 행을 만들 수 있다.

[P0] 오프라인 재시도와 업로드-이벤트 연결이 없다 — §2는 이미 생성된 `pipeline_jobs`부터 시작하며 앱의 업로드 outbox, 이벤트 멱등키, 재연결 시 reconciliation을 정의하지 않는다. 업로드만 남고 이벤트가 사라지거나 오프라인 재시도로 중복 제출이 생길 수 있다. §4 #7은 n8n 중단만 시험하고 앱 중단·오프라인을 시험하지 않는다.

[P0] 훈련 승격은 설계 문장뿐이다 — §3과 §4 #13은 비식별 artifact 저장, `data_use.granted`, dataset 조립을 자동화한다고 하지만 artifact 저장소·writer·`data_use.granted` 생산자·dataset_item·소비자가 없다. 현행 엔진 도달 설계에서 AI 1차 처리·검수·승격·엔진 소비자는 모두 0이다. §4 #10의 “반출 도구가 없으면 첫 조건으로 적는다”는 차단 장치가 아니다.

[P0] 철회가 원본 오디오에서 끝난다 — §4 #6은 `audio_deleted_at`과 Storage 파일 삭제만 검증한다. §3·§4 #13에서 만든 artifact·dataset·모델 계보의 삭제·제외·롤백 경로와 검증이 없다. 승인 후 철회된 데이터가 파생 산출물에 계속 남을 수 있다.

[P0] `style_shift`가 잘못된 축에 들어간다 — §8 A-2는 `task_type='style_shift'`를 요구하지만 현행 `task_type` 계약에는 그런 값이 없다. 기존 축에서 맞는 값은 `task_format='높임전환'`이다. §1의 축 분리 원칙을 §8이 스스로 위반하므로 DB 거부 또는 분류 손실이 발생한다.

[P0] 과제·목표 스냅샷이 필수 구조로 강제되지 않는다 — §1은 값을 `task_snapshot` JSONB에 넣는다고만 하고 내부 필드와 `task_schema_ver` 검증을 정의하지 않는다. C0가 요구하는 질문·선택지·정답·지시문과 이벤트 시점의 `goal_snapshot`도 누락되어 있다. 현재 목표값만 저장하면 목표 변경 뒤 과거 발화의 의도와 과제 맥락을 복원할 수 없다.

[P0] 금지된 `mishearing` 축을 다시 도입한다 — §5는 mishearing을 `transcript`와 `transcript_verified`의 차이로만 보며 태그가 아니라고 한다. 그런데 §8 마지막은 c5에 `politeness`와 `mishearing`을 `error_types`로 추가하라고 적는다. 이는 현행 c9의 `화용:공손성`과 mishearing 비태그 결정을 동시에 뒤집고 ASR 불확실성을 학습자 오류로 오염시킨다.

[P0] `daily_activity.expected` 생산자가 없다 — §4 #9는 pg_cron과 `expected` 생성을 요구하지만 SQL·스케줄·시간대·배정 원천·멱등키가 없다. 현행 엔진 설계에도 `daily_activity.expected` producer는 0이다. `expected`가 없으면 “낼 일이 없었음”과 “내지 않음”을 구분할 수 없고, deadline 시각·버전 누락은 소급 복구할 수 없다.

[P0] 검수에서 원본 사건 식별자가 끊긴다 — §3의 유일한 큐 뷰에는 `event_id`가 없지만 §4 #13의 승격은 원본 `event_id`를 요구한다. 큐에서 사건으로 가는 join·writer·검증 경로가 문서에 없다. `content.viewed`, `intervention.delivered`, `correlation_id`, 후속 성과와의 연결도 없어 교정이 전달·열람·효과로 이어졌는지 알 수 없다.

[P1] 모델 계보가 재현 불가능하다 — §2의 “flash 3.6”과 `correct-v1`은 정확한 모델 ID·Whisper 버전·생성 설정·워크플로 버전·입출력 지문을 고정하지 않는다. `model`과 `prompt_ver` 두 열만 채워도 동일 결과를 재현할 수 없다. 학습셋의 라벨 품질 비교가 불가능해진다.

[P1] `Z`가 복구가 아니라 중복 라벨 생산자가 된다 — §3은 직전 확정을 새 teacher `corrections` 행으로 다시 만든다고 한다. `supersedes`, 최신 행 선택, 철회·충돌 규칙이 없고 `reviewed_correction_id`는 이전 교사가 아니라 AI 행만 가리킨다. 서로 다른 teacher 판정이 모두 개인화·훈련 경로로 흘러갈 수 있다.

[P1] 5% 감사 표본을 재현할 수 없다 — §0·§2는 `random()` 기반 5%를 말하지만 표본 버전·seed·모집단 시점·재처리 규칙이 없다. 백필과 재시도 시 표본과 분모가 달라진다. 감사율과 감사 대상의 사후 검증이 불가능하다.

[P1] 수용 기준이 실행 판정이 아니라 자기 선언이다 — §4 #1은 명령·assertion·프로젝트 식별자 없이 꼬리의 판정값을 “✅ 전부 통과”로 요구한다. #3·#4·#13은 아직 없는 AI·검수·승격 경로에 의존하고 #10은 반출 도구가 없어도 조건만 적어둘 수 있게 한다. 체크리스트를 채우는 행위가 실제 왕복 성공과 같은 모양이 된다.

[P1] 미확정 기능이 동결 범위에 남아 있다 — §3 마지막의 검수 0단계는 “제안·유호 답 대기”인데 §5는 CHK-5만 남았다고 선언한다. 저신뢰 자기확인은 새 이벤트·동의 경계·생산자를 바꾸는 기능이다. 포함인지 제외인지조차 문서에서 판정할 수 없다.

[P2] 핵심 수집 계약보다 UI 세부를 먼저 동결한다 — §3은 단축키, 세그먼트 재생, 프리로드, `Z`, 세션 요약을 고정하지만 핵심 writer와 엔진 소비자는 없다. 이 세부는 원본 증거 보존에 필수적이지 않으면서 동결 범위와 테스트 부담만 늘린다. 변경 가능한 UX가 미구현 P0 경로를 가리는 구조다.

이 문서보다 나은 대안 구조 1개 — 단일 발주서를 `c9 입력 계약`·`원자적 파이프라인 계약`·`앱 밖 검수 웹/Edge Function 계약`·`승격·철회·계보 계약`의 4개 실행 묶음으로 분리한다. 각 묶음은 생산자, 불변 필드, writer, 읽기 경로, 실패 상태, 멱등키, 실측 테스트를 한 표에 가져야 하며, 현행 L0/C0와 정확한 커밋만 참조해야 한다. 네 묶음의 부정 경로와 실제 왕복이 모두 통과하기 전에는 잠금 해제 판정을 쓰지 않는다.