판정: `failed_p0` — P0 17건, P1 2건. 동결 불가다.

[P0] 정본 경계가 뒤집혔다 — 머리말은 “이 문서가 설계 정본이고 구현은 이 문서를 따른다”고 선언하지만, 현행 P0는 이 문서의 소유 범위를 세 호흡의 학습 의미로 제한하고 필드·API·DDL은 각각 c8·C0·L0에 맡긴다. 게다가 C0는 아직 소비자 승인과 왕복 6 통과 전인 초안이다. 이 상태로 동결하면 낡은 산문이 기계 계약을 역으로 고치게 된다.

[P0] 배정과 실제 개입이 사건에서 사라진다 — §6~§7 흐름에는 `task.assigned`와 `intervention.delivered`가 없다. 전자가 없으면 “학생이 안 냄”과 “낼 과제가 없었음·배치가 죽었음”이 같은 0건이 된다. 후자의 `intervention_id`·`output_text`·`model`·`prompt_ver`·`policy_ver`·`degraded`가 없으면 무엇이 어떤 반응을 만들었는지 재현할 수 없다.

[P0] 통제 발화와 자유 발화의 쌍 고리가 없다 — §6은 같은 날의 ②·③ 쌍이 데이터 가치의 핵심이라고 주장하지만 §7에는 `correlation_id`·세션 ID·슬롯 ID가 없다. 학생·날짜·주제만으로 묶으면 재시도, 오프라인 지연 전송, 하루 복수 실행에서 어느 ②와 어느 ③이 한 쌍인지 복원할 수 없다. `task_format`만으로는 여러 시도 사이의 대응 관계를 확정하지 못한다.

[P0] 오류→교정→재발화 3점 사슬이 끊겼다 — §6의 교정문 슬롯에는 원 제출 `event_id/submission_id`, `correction_id`, 교정 열람 사건, `retry_of_event_id`, 원 개입 ID가 없다. “교정이 나갔다”와 “학생이 실제로 봤다”도 구분하지 않는다. c8이 `correction_id`를 소급 불가 고리로 추가했는데 후보 문서는 이를 필수로 만들지 않아 교정 효과를 교정 단위로 판정할 수 없다.

[P0] 통제 발화의 통제조건이 저장되지 않는다 — §6의 ②는 문장 텍스트만 말하고 `mission_id/challenge_id`·목표 음소·문항판·prompt seed·스킬 ID·taxonomy 판을 요구하지 않는다. 억양 평가의 실제 정답은 텍스트가 아니라 학생이 들은 목표 음성인데, TTS 음성 참조·보이스·속도·엔진판도 없다. 같은 문장이라도 다른 음성을 들었다면 학생 간·시점 간 비교 조건이 같지 않다.

[P0] “원본 음성”이 기술적으로 정의되지 않았다 — §3-2·§7에는 codec·sample rate·bit depth·channel·AGC·노이즈 억제·마이크 경로·기기/OS·앱 빌드의 실측값이 없다. 실제 HEAD 화면은 현재 `HIGH_QUALITY` m4a/AAC를 사용하므로 “원본”이라는 단어가 무손실 원신호를 보장하지도 않는다. `capture_meta.server/app`가 없으면 학생 발음과 기기 가공을 영원히 분리하지 못한다.

[P0] 음성 생명주기가 동의·철회 계약과 충돌한다 — §3-2와 §7의 “영구 보관”은 현행 계약의 “보존기간 무제한이지만 철회 시 실제 삭제”와 모순된다. §4의 자기 목소리 재생은 동의의 증거가 아니며, §9도 유효한 `consent_id`·동의판·동의시각을 녹음과 업로드 서명 전에 검사하도록 요구하지 않는다. `data_use.revoked`·삭제 표식·Storage 객체 삭제·계보 보존도 없어 동의 없는 파일이나 철회 후 파일이 남을 수 있다.

[P0] 학생 병기 텍스트를 STT 정답으로 오인한다 — §3-3의 학생 입력은 말하기 전 작성, 말한 뒤 회상, 재생 후 수정, 맞춤법 오류가 모두 가능한 “의도 주장”이지 검증 전사가 아니다. 입력 시점·수정 여부·입력 언어도 없다. `body_original/intended_text`, 기계 `transcript`, 사람이 들은 `transcript_verified`를 분리하지 않으면 STT 평가셋의 정답 쪽이 오염된다.

[P0] `hesitation_ms`는 계약에도 없고 확신도 대체물도 아니다 — §5의 필드는 c8 필드 목록과 C0 허용 payload에 없어서 현재는 기기 로컬 로그에만 남고 서버 사건에는 실리지 않는다. 측정값은 VAD 문턱, 배경 소음, 마이크 초기화, 기기 지연, 문장 길이, 언어 처리시간을 함께 잰다. 이를 자기 확신도와 같은 축이라고 고정하면 명시적 확신도는 잃고 교란된 관측값만 남는다.

[P0] 무발화를 학습 곤란으로 잘못 확정한다 — §3-4는 `abandoned`를 곧바로 “이 문장에서 막힘”으로 해석한다. 권한 거부, 전화 수신, 앱 백그라운드 전환, 녹음기 오류, 저장공간 부족, 주변 소음, 실수 탭도 같은 모양이다. 종료 주체·reason code·호흡·녹음기 상태·권한·문턱판이 없으면 관찰과 추론을 다시 분리할 수 없다.

[P0] 되듣기와 재시도의 인과 고리가 없다 — §4는 자기 목소리 재생이 재시도를 만든다고 주장하지만 재생 시작·완료·실패를 남길 사건이나 필드가 없다. `attempt` 증가만으로는 재생 후 자기수정인지, 소음 때문에 다시 녹음했는지, 더 나빠진 재녹음인지 알 수 없다. 따라서 이전·다음 음성은 원본 쌍일 뿐 “공짜 정답 레이블”이 아니다.

[P0] 초급의 음성 선택 퀴즈는 STT 없이 실행 불가능하다 — §6은 선택지를 소리로 들려주고 학생도 소리로 답하게 하면서 §7은 STT를 나중으로 미룬다. 그러면 제출 순간 `selected_option`을 확정할 수 없고 객관식 채점도 할 수 없다. 또한 안정 `option_id`, 표시 문구 스냅숏, 순서, 추천안, 전량 거절, 선택 이유가 빠져 현행 선택로그 계약과 충돌한다.

[P0] 급수 미정 학생의 첫 발화를 잃는다 — §6은 “미정 0”을 1~2급과 함께 처리하지만 현행 C0는 `level_snapshot`을 공통 필수로 요구하고 배달 응답은 급수 미정 학생에게 `null`을 돌려줄 수 있다. 이미 알려진 실제 실패 방향은 “배정은 보이지만 제출은 400”이다. `0`이 계약값인지, `null`을 어떻게 변환하는지 정의하지 않아 개원 첫 기준선이 통째로 빠진다.

[P0] 물리 출석과 학습 출석을 연결할 키가 없다 — §2의 물리 출석은 Apps Script/시트, 말하기 사건은 Supabase에 사는데 `class_instance_id`·attendance 행 ID·예정 수업·안정 학생 조인·정정판이 없다. 날짜와 학생만으로 대조하면 보강, 온라인 제출, 지각 입력, 강사 사후 정정이 뒤섞인다. 더구나 “위축/온라인·보강”은 관찰 사실이 아니라 추론인데 문서가 사실처럼 등치한다.

[P0] 업로드와 사건 저장의 실패 경계가 없다 — §7은 음성 저장과 제출을 화살표 하나로 표현하고 멱등키·발생/수신 시각·업로드 소유권·객체 해시·큐 상태·영구 실패 격리·고아 객체 회수를 정의하지 않는다. 업로드 성공 후 사건 저장 실패면 개인정보 파일만 남고, 재시도마다 새 경로를 발급하면 중복 객체가 쌓인다. 반대 순서면 존재하지 않는 파일을 가리키는 성공 행이 남는다.

[P0] 오류태그 정본과 물리구조가 낡았다 — §8은 `HW_ERROR_TAGS`와 같은 “23종”이라고 쓰지만 현행 배열은 오류유형 23개와 `오류없음`을 합친 24개 값이다. c5에서 추가된 `화용:공손성`을 포함한 계약 c8이 정본이며, L0에는 별도 `error_tags` 테이블도 없다. 후보대로 테이블을 신설하면 기존 계약 검증·`corrections.error_tags`·`skills`와 병렬 정본이 생긴다.

[P0] 실학생 선행조건 목록이 거짓으로 완결돼 있다 — §9은 몽골어 검수·Storage·EXPO_TOKEN 세 가지만 열거한다. 현행 출시 조건인 C0 동결과 왕복, 앱 내 동의 게이트, 타학생 접근 0건, 멱등·오프라인 실측, RLS, 백업 복구시험, M2 사람 라벨 회로, 정확한 산출물에 대한 운영 승인 등이 빠졌다. 이 제목을 운영 체크리스트로 읽으면 미완성 시스템을 실학생에게 열 수 있다.

[P1] 급수 분기와 도전안이 실험 오염을 만든다 — §6은 “매일 통제+자유 발화 쌍”을 주장한 뒤 5~6급은 ②를 선택적으로 생략하게 하고, 같은 절에 순서 반전 미채택안까지 둔다. 노출 여부·생략 선택·적용 정책판·실험 variant·배정 단위가 없어서 쌍의 결측과 기능 실패를 구분할 수 없다. 구현 정본 안의 미채택 분기는 작업자마다 임의 적용되는 통로가 된다.

[P1] 교정 효과라는 결론이 식별되지 않는다 — §6의 같은 문장 다음날 재발화는 단기 모방·반복 연습 효과까지 모두 포함한다. 비교군, 교정 열람 전 기준, 자유발화 전이, 1·7·30일 지연 성과가 없으므로 “교정 엔진의 성적표”라는 결론은 성립하지 않는다. 측정 가능한 것은 해당 문장 재연 수행이지 교정의 인과효과나 장기 정착이 아니다.

이 문서보다 나은 대안 구조 1개 — 「세 호흡 사건사슬 명세」로 교체한다. 첫 장은 이 문서가 UX·학습 의미만 소유하고 필드=c8, API=C0, 물리=L0임을 고정한다. 본문은 `동작 → event_type → 필수 필드 → 불변 스냅숏 → 조인 ID → 관찰/추론 구분 → 엔진 부품 → 회수 시점 → 실패 판정` 열을 가진 행렬로 만들고, `task.assigned → intervention.delivered → ①노출 → ②·③ 시도들 → 자기재생 → 제출 → correction.viewed/responded → retry_of_event_id → 즉시·지연 성과`를 한 사슬로 적는다. 끝에는 음성 상태기계와 동의·오프라인·고아 파일·철회·급수 미정·타학생 차단의 실측 게이트를 두며, 현 계약에 없는 필드는 계약 개정 전까지 “수집됨”이라고 쓰지 않는다.