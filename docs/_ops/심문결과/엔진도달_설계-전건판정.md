# 엔진 도달 설계 — 심문 19건 전건 판정

> 판정자 = 이 세션 · 2026-09-01 · 과녁 문서 지문 `c074aa459ea2`
> 심문 원본 = `엔진도달_설계-c074aa459ea2-luna-xhigh-1.md`(gpt-5.6-luna / xhigh / 1회)
> 유호 지시 09-01 「여덟 벌 판정 전부 진행」의 **첫 벌**.

---

## 0. 판정 규약

🔴 **심문은 저장소를 못 봤다** — 스스로 *「판정 근거는 제공된 동결 후보 전문과 인계문 자체다」*라고
머리에 적었다. 그래서 이 판정의 몫은 하나다: **지적을 실물에 대보는 것.**

⚠ **이 판정에서 실제로 연 실물**: `엔진_셋업확장.js`(`quietHi` :2451 · `buildGroupPlan_` · 도달 장부 ·
`시트도달상한_`) · `계약/수집_교정_계약.json` · `docs/SYNK_철학.md` ㉢ · `docs/엔진도달_설계.md` §7·§8.
**안 연 것**: talk 저장소 코드 · 라이브 DB · §3 승격 파이프라인 실물 · `masteryApply_` 본문.
그 축은 아래에서 **조건부**로 남기고 «재는 자리»를 못 박았다.

등급 = 발음 판정과 같다(채택 / 채택(문면) / 부분 채택 / 부분 기각 / 유호님 몫 / 조건부).

---

## 1. 셈

**원본 19건** = P0 12 · P1 6 · P2 1

| 처분 | 건수 |
|---|---|
| 채택 | 4 |
| 부분 채택 | 3 |
| 부분 기각 | 2 |
| 유호님 몫 | 2 |
| 조건부(안 재봤다 · 재는 자리 못 박음) | 8 |

🔑 **조건부가 8건인 것을 숨기지 않는다** — 이 문서의 절반은 talk 저장소·라이브 DB·승격 파이프라인
실물에 걸려 있고, 이 세션은 그것을 안 열었다. **「안 재봤다」를 「기각」으로 접지 않는다.**

---

## 2. 실물에 대서 답이 난 것 (9건)

### ⑩ 문법·어휘의 정식 식별자가 없다 → **부분 기각** 🔑 *같은 오독이 기록돼 있다*
계약에 **이미 있다** — `계약/수집_교정_계약.json` 「주체·맥락」에 `skill_ids`·`skill_taxonomy_ver`.
그리고 그 파일이 **바로 이 오독의 이력을 적어 뒀다**:
> *「🔴 그 오독이 실제로 났다 — 2026-08-15 에 `skill_ids` 가 여기 있다는 이유로 「문항↔개념 매핑
> 0건」 판정이 나갔고 유호님이 그걸 근거로 일감을 확정하셨다. **실물은 talk 에 문항 100/100**」*
⇒ 심문이 이 저장소 문서만 보고 **같은 자리를 다시 밟았다.** 식별자 계약은 있고 실물도 talk 에 있다.
⚠ **참인 부분**: 시트 층(`hw_feedback`·`voice_log`·`talk_log`)에서 `masteryApply_` 가 같은 학습 대상을
잇는 안정 키는 **안 재봤다** — 아래 조건부 ⑳-b 로 넘긴다.

### ⑪ 조 편성이 학생 간 비교 금지를 위반한다 → **부분 채택 + 유호님 몫**
**실물은 있다.** `엔진_셋업확장.js:2451` 이 여러 학생의 값을 모아 **중앙값 `quietHi`** 를 내고,
`buildGroupPlan_` 은 `m.skill` 로 정렬해 순위를 매긴다(`rankOf`). 「모아서 견준다」는 사실이다.
**그런데 철학 ㉢ 의 문면은 산출물 기준이다**:
> *「학생끼리 비교하지 않는다 — 비교는 어제의 그 학생과만. **등수·평균·「다른 아이보다」는 어느
> 화면에도, 어느 문서에도 두지 않는다**」*
⇒ 금지된 것은 **두는 것**(등수·평균을 화면·문서에 노출)이고, 조 편성은 **학생을 돕는 배치**다
(조용한 학생만 한 조에 모이면 그 조가 침묵한다). 「정면 위반」은 과한 판정으로 본다.
🔴 **다만 심문이 옳게 짚은 것 하나**: 철학에 **「내부 운영 알고리즘은 예외」라는 경계가 없다.**
그래서 다음 사람이 이 코드를 보고 같은 물음을 다시 던진다(실제로 이 심문이 그랬다).
⇒ ✅ **유호님이 닫으셨다 (2026-09-01) — 「학생에게 보이는 것까지야」.**
내부 배치·운영 알고리즘이 학생들의 값을 모아 견주는 것은 **위반이 아니다.** 가르는 자는 하나 —
**그 견줌의 결과가 학생 화면·문서에 등수·평균·「다른 아이보다」로 나가는가.**
⇒ 조 편성은 **위반이 아니다**(학생이 보는 것은 조 번호·이름·역할뿐 · `groupBoardRender_`).
경계를 철학 ㉢ 밑에 한 줄로 박았고 결정 원장에 등재했다 — **그래야 이 물음이 다시 안 온다.**

### ⑯ 분모 정본이 문서 안에서 하나가 아니다 → **채택** 🔴 *그리고 오늘 내가 넷째를 만들었다*
실측: `엔진도달_설계.md` 안에 **22·23·24가 병존**한다(:219 «표식 23종» · :221 «표식 24종» ·
:405 «분모 22종»). 그리고 **09-01 에 내가 실물을 26 으로 올려 놓고 이 문서를 안 고쳤다** — 그래서
지금은 **넷**이 병존한다.
⚠ 문서 :219 가 이미 처방을 적어 뒀다(*「어느 문단도 믿지 말고 도구로 다시 재라」*) — 그 처방이
맞지만, **「믿지 마라」는 낡음을 막지 못한다**(네 번째가 그 증거다).
▶ **처방**: 이 문서에서 «현재 값»을 지우고 **도구 출력을 가리키기만** 한다(값은 `시트도달상한_()`·
`수집장부탭_()` 하나에서만 나온다). 날짜 붙은 이력은 남기되 «현재»와 섞지 않는다.

### ⑥ 도달 장부는 실행 증명이 아니라 문자열 선언이다 → **채택(문서가 이미 자백)**
§4 스스로 *「읽어서 바꾸는지는 못 가른다」*고 적었다. 호출 안 되는 함수·죽은 분기·삼킨 실패가
초록이 되는 것은 사실이다.
⚠ **그런데 이건 이 장부의 «설계된 한계»다** — §8 이 *「스캐너는 여전히 안 짓는다 … 선언은 사람이
하고 기계는 빠뜨림·거짓 지목·증가만 막는다」*로 그 선을 명시했다.
⇒ **채택하되 처방은 「런타임 증명을 붙인다」가 아니다**: 장부가 **자기 한계를 출력에 함께 낸다**
(지금은 문서에만 있다). 「초록 = 도달함」으로 읽히는 것이 이 결함의 실체다.

### ⑧ 시트 이관은 범위만 있고 물리 계약이 없다 → **채택 · ✅ 09-01 처분 시작**
발음 규격 심문 P0-⑦ 이 **독립으로 같은 곳을 짚었다**(겹침 자체가 신호).
▶ 09-01 처분: ㉠ 재료 정리 한 장(`docs/시트이관_재료정리_2026-09-01.md`) ㉡ **자를 세웠다**
(`tools/L0대조점검.js` — 헤더가 자랐는데 talk L0 §5 대조표가 그대로면 운다) ㉢ talk 발주문 작성.
⚠ **남은 것은 그 표를 실제로 고치는 일**이고 그건 talk 정본이라 이쪽 세션이 못 한다.

### ⑦ `self_declare_log`·`scene_log` 의 「학생이 와야 한다」 예외 → **부분 채택**
지적의 핵심(첫 이벤트 전에 생산자·필드·소비자·재시도 계약이 서 있어야 한다)은 **옳다** —
학생의 첫 자기선언·장면은 소급 불가다. 다만 **`scene_log` 는 이미 소비자가 섰다**
(`엔진_셋업확장.js` 도달 장부: `scene_log` → `Code.js:calcAll`). 심문이 본 문서는 그 이전 판이다.
⇒ **`self_declare_log` 만 남는다** — 그쪽은 도달 0 이 맞다.

### ⑲ [P2] 감사 장치가 제품·원본 계약보다 앞서 있다 → **부분 채택**
「수동 복사 분모 표가 반복해 낡는다」는 ⑯ 이 실측으로 뒷받침한다(네 번째 낡음).
⚠ 다만 **「감사 장치를 줄여라」로 읽지 않는다** — 오늘 그 장치들이 실제로 값을 냈다(래칫이 내
추측 상한을 막았고, 회귀가 정규식·NUL 결함 둘을 잡았다). 참인 것은 **「값이 손으로 복사된다」**
하나이고 처방은 ⑯ 과 같다: 값을 도구 하나에서만 낸다.

### ①⑤ — 문서가 스스로 한계를 적은 두 자리 → **조건부**로 내림(§3)

---

## 3. 안 재봤다 — 조건부 8건 (재는 자리를 못 박는다)

| # | 지적 | 왜 조건부인가 | **재는 자리** |
|---|---|---|---|
| ① | `as_of` 가 미래정보 누출을 못 막는다(`available_at`·`ingested_at` 없음) | §3 경로 A 실물이 talk 층이고 안 열었다 | talk `docs/C0_API계약.md` + 상태 계산 코드 |
| ② | 승격 사건 필드·생성 순서 모순 | 승격 파이프라인 **구현 0**이라 코드로 못 잰다 | 그 구현이 서는 커밋 |
| ③ | `event_id` 만으로 재검수·수정본 구분 불가(`correction_version` 없음) | 계약 파일에 `correction_id`·`retry_of_event_id` 는 있다 — **버전 축이 따로 있는지** 안 봤다 | `계약/수집_교정_계약.json` 전수 + talk `corrections` |
| ④ | 훈련 경로 미구현인데 첫 학생 막는 게이트 없음 | 「출시 조건」이 어디에 사는지 안 찾았다 | P0 제품계약 · 개원 체크리스트 |
| ⑤ | `⑦엔진이 배운다` 가 개인화 조회와 학습을 혼동 | A-1 이해 대장의 칸↔경로 대응을 안 열었다 | `docs/이해대장.html` + 철학 부록 A-1 |
| ⑨ | `dataset_item` 이 미룬 DDL 위에 서 있다 | DDL 은 talk `supabase/` 몫 | talk `supabase/L0_스키마.sql` |
| ⑰ | 강사 골든 경로 미결정(㉠㉡㉢) | ✅ **계보 부분은 09-01 에 재서 닫았다**(아래 §4-b) — 남은 것은 순수 판정이라 유호님 몫 | — |
| ⑳-b | 시트 층의 `skill_id` 연결(⑩ 잔여) | `masteryApply_` 본문을 안 읽었다 | `교재연동.js:masteryApply_` |

---

## 4. 남은 P1 셋 — 게이트 축 (한 묶음으로 본다)

⑫ 기본 게이트를 프롬프트 변화로 대체 · ⑬ 자기설명 게이트 ⑤ 전부 빠짐 · ⑱ 성취 경로 미검증 구멍

**⑫⑬ = 채택(문면).** 셋 다 같은 뿌리다 — **회귀가 「엔진이 읽었나」만 재고 「사람에게 닿았나」를
안 잰다.** 철학이 그 둘을 나란히 세워 뒀는데(④ 완결 = 엔진까지 · ⑤ 자기설명 = 사람까지) 이 문서의
검사는 ④ 쪽만 있다. ⇒ 규격에 **「⑤ 를 재는 자가 없다」를 명문화**한다(없는 것을 있는 척하지 않는다).
⚠ **회귀를 지금 짓지 않는다** — 학생 0명이라 「다시 열 이유가 생겼나」를 원리상 못 잰다. 못 재는
것을 게이트에 넣으면 게이트 전체가 안 쓰인다(철학이 「늘었나」에서 쓴 그 논리 그대로).

**⑱ = 조건부.** 「`Math.min` 폭 클램프 변이 43건이 모두 초록」은 §8-6 이 **스스로 적은 것**이라 사실로
받는다. 다만 그 구멍을 닫는 회귀(실제 `Range` 형태 검증)를 **이 세션이 안 지었다** — 재는 자리 =
`aiStudioBatch_` 의 시트 폭 처리.

---

## 4-b. 🔑 강사 골든 «계보» — 재봤더니 안 끊겼다 (유호 지시 09-01 「계보 복원 먼저」)

심문은 *「`G001` 재채번으로 원본 사건 연결이 끊겼다 … 기존 행을 완전히 복구할 수 없다」*고 했다.
**실물을 열어 보니 틀렸다 — 그리고 그 오해의 출처는 문서 문장이었다.**

| | 실물 | 어디 |
|---|---|---|
| 골든 행의 새 번호 | `GD20260901-1` 꼴(문서가 적은 `G001` 은 옛 형식) | `엔진_수집.js:1087` |
| **원본 고리** | **`fb_id` 열에 `hw_feedback.id` 를 그대로 넣는다** ✅ | 같은 줄 `p.id` |
| 그 고리가 실제로 쓰이나 | ✅ **쓰인다** — 표본 중복 검사가 **2열(`fb_id`)** 을 읽어 이미 뽑힌 것을 거른다 | `:1067` |
| 동의 근거 | 행에는 없지만 **`student_id` 로 조회된다**(`voiceConsentMap_` 이 상담시트에서 학생 단위 맵을 만든다) | `엔진_폼리포트.js:444` |

🔴 **문서 :183 의 「고리가 끊긴다」는 «픽스처 산출물» 이야기였다.** `골든픽스처_()` 는 밖으로 내보낼 때
`student_id`·`fb_id`·강사명·날짜를 **일부러 버린다**(`엔진_수집.js:1130` — 의도된 비식별).
즉 **나간 픽스처엔 고리가 없고, 시트 원본에는 있다.** 심문이 그 맥락을 놓치고 「원본 연결이 끊겼다」로
읽었고, 문서가 옛 채번 형식(`G001`)을 그대로 둔 것이 그 오해를 도왔다.

⇒ **계보 복원 작업은 필요 없다.** ㉠㉡㉢ 판정의 선행 조건이 사라졌다.

**그래서 ㉡(teach 문에 승격 칸)을 고를 때 실제로 필요한 것은 둘뿐이다**:
1. **승격 의사 칸** — `teacher_gold` 에 `promotion_intent` 를 둘지, 검수 문을 재통과시킬지(=갈래 자체)
2. **동의 근거를 «그때 값»으로 박을지** — 지금은 조회식이라 「이 행을 만들 때 동의했나」는 재현이 안 된다.
   ⚠ 이건 ㉠㉡ 어느 쪽을 골라도 같이 서는 물음이고, **소급 불가 축**이다(옛 행에 지어 넣을 수 없다).

---

## 5. 🔴 유호님께 — 두 건

1. **철학 ㉢ 의 범위**(⑪) — 「학생끼리 비교 금지」가 **학생에게 보이는 것**까지인가, **내부 배치
   알고리즘**까지인가? 지금 조 편성은 여러 학생의 값을 모아 중앙값을 내고 순위를 매긴다.
   전자면 철학에 경계 한 줄을 적어 이 물음이 다시 안 오게 하고, 후자면 조 편성을 다시 짜야 한다.
2. **강사 골든 경로**(⑰) — ㉠재검수 / ㉡teach 승격칸 / ㉢평가 전용 중 하나가 아직 안 골라졌다.
   문서가 유호님께 남겨 둔 자리 그대로다.

---

## 6. 이 판정이 낳은 실행 항목

- [ ] ⑯ 이 문서의 «현재 분모»를 지우고 도구 출력을 가리키게(내가 오늘 만든 네 번째 낡음 포함)
- [ ] ⑥ 도달 장부 출력에 **자기 한계 한 줄**(「선언이지 실행 증명이 아니다」)
- [ ] ⑫⑬ 규격에 「⑤ 자기설명을 재는 자가 없다」 명문화
- [ ] ⑧ talk 에 L0 §5 발주(재료 정리 §6 그대로)
- [ ] 조건부 8건 — 각 「재는 자리」가 열리는 날 다시 판정

> ⚠ 이 판정은 **처분이지 수리가 아니다.** 위 항목은 아직 반영되지 않았다.
