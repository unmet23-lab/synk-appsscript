동결 불가. P0 11건, P1 9건이다.

[P0] 동결 기준 코드가 두 판이다 — 머리말은 기준 코드가 `v9.276`이라고 하지만 §3·§4의 실물 반영은 `v9.277`이다. 어느 커밋과 배포 산출물이 정본인지 특정할 수 없어 잘못된 열 규격으로 배포될 수 있다. 문제 절: 머리말, §3, §4, §5-4.

[P0] 한 녹음과 여러 목표 낱말의 연결 카디널리티가 없다 — `HW306`은 여섯 행인데 `voice_log`에는 숙제ID와 단일 `목표발화`만 정의돼 있다. 한 녹음에 여섯 낱말을 넣는지, 낱말마다 녹음하는지, 어느 낱말이 실제 지정됐는지 복원할 `assignment_id`·`item_id`가 없다. 문제 절: §2-3, §2-4, §3, §6-2.

[P0] 같은 필드의 역사적 의미를 바꿔 놓았다 — `미션ID`는 미션명에서 숙제ID로, `목표발화`는 문장에서 낱말로 바뀐다. 기존 행과 신규 행을 가르는 호환 규칙·이관표·수집 시작 시점이 없어 장기 집계에서 같은 열이 서로 다른 의미를 갖는다. 문제 절: §1, §2-4, §3.

[P0] `시즌`은 스냅샷이 아니다 — 제출 뒤 `voiceSweep_`가 사람의 현재 시즌 경계로 채우지만 `season_id`, 경계 버전, 적용 시각을 저장하지 않는다. 사람이 시즌 시작일을 고치면 과거 녹음이 조용히 다른 시즌으로 재분류된다. 문제 절: §3, §6-2.

[P0] 미션 0개와 폼 미연결 상태가 정상처럼 남는다 — 문서가 미션 목록 0개와 `목소리폼ID` 미확인을 인정하고, 코드도 빈 값이면 조용히 대기한다고 적는다. 사전검증·차단·경보가 없어 학생이 유도 없는 음성을 제출해도 수집 성공처럼 쌓일 수 있다. 문제 절: §1-⑤, §4, §6, §5-4.

[P0] 이벤트 식별자와 멱등성 계약이 없다 — `file_id`는 파일 참조일 뿐 제출 이벤트의 고유키인지, 재시도 시 같은 시도인지 정의돼 있지 않다. 폼 재전송·동시 실행·파일 생성 후 행 기록 실패·STT 실패를 구분할 방법이 없어 중복과 유실을 복구할 수 없다. 문제 절: §1의 12칸 설명, §3, §5-4.

[P0] 시트를 원본처럼 다룬다 — 문서는 `voice_missions`와 `voice_log` 시트 열만 계약으로 세우고 Supabase 원본 이벤트, L0/C0/M0 매핑, 어댑터 경계를 제시하지 않는다. 이는 “Supabase가 원본이고 시트는 운영 어댑터”라는 확정 스택과 충돌하며, Prism이 읽을 정본이 없다. 문제 절: §2-4, §3, §6-2.

[P0] 추론 라벨과 전달 사건을 원본 음성 행에 섞었다 — 빈 `발음태그`를 나중에 같은 `voice_log` 행에 채우고, `돌려준날`도 같은 행에 기록한다. 관찰 원본·AI 추론·학생 전달 사건을 분리하지 않아 라벨 버전·판정 사건·전달 대상의 계보가 끊긴다. 문제 절: §3, §5-6, §6-2.

[P0] `늘었나`가 프롬프트 증가를 실력 이해로 오인한다 — `목표발화`는 학생이 무엇을 들었는지만 기록하고, 무엇을 틀렸는지는 기록하지 않는다. STT는 발음 오류를 보정하고 `발음태그`도 비어 있으므로 §6-2의 `목표발화 → ㉠ 실력` 매핑은 엔진 학습이 아니라 과제 배포량을 실력 데이터처럼 세게 만든다. 문제 절: §1-⑥, §3, §5-6, §6-2.

[P0] 수업 6축의 실패는 영구적으로 사라진다 — P1·P2·P5·P6·P7·P8은 녹음도 구조화 관찰도 없이 진행된다. 학생이 그 자리에서 실패했는지, 연습하지 않았는지, 잘했는지를 나중에 구분할 수 없다. 문제 절: §2-1, §2-2, §6-2.

[P0] 축 enum이 활성 설계와 충돌한다 — §2-4는 `P1~P9`만 허용하지만 §2-1은 실제 수집축으로 `P10`을 사용한다. 예시에는 `HW318·P9·있어요`가 있고 본문 표에는 `HW318·P10·책상·옷장`이 있어 시드나 검증기가 서로 다른 분류를 만들 수 있다. 문제 절: §2-1, §2-3, §2-4.

[P1] 낱말 수와 겹받침 수가 서로 다르다 — §2-3은 낱말 11개라고 하지만 §5-1은 반복해서 10개라고 한다. 또한 “겹받침 셋”이라고 적었지만 최종 목록에는 `읽었어요`, `많았어요` 두 개만 보이며 뒤에서는 3개에서 2개로 줄었다고 한다. 문제 절: §2-3, §4, §5-1, §5-3.

[P1] 복수 축 의도가 기계적으로 남지 않는다 — 한 낱말이 P3와 P9를 동시에 일으킨다고 하면서 `축`에는 하나만 쓰고, `비고`는 생략 가능하게 했다. P4의 하위 현상도 별도 필드가 없어 나중에 어느 축을 의도적으로 노린 녹음인지 판별할 수 없다. 문제 절: §2-2, §2-3, §2-4, §5-6.

[P1] 한 회분의 수치를 고정 배분으로 과대해석한다 — TOPIK I 20발화·234자라는 작은 수동 표본에서 곧바로 6·3·2개 배분을 확정했다. 축별 노출 분모·미사용·시도 여부가 없으므로 P3의 높은 건수는 학생 난이도가 아니라 설계자가 여섯 개를 넣은 결과일 수 있다. 문제 절: §2-1, §2-3, §6-2.

[P1] 라벨 유예가 일정과 판정으로 닫혀 있지 않다 — “첫 소리 수백 건”의 분모, 품질 기준, 책임자, 마감일, 태그 버전, 평가셋, 재판정 규칙이 없다. 발음태그는 빈 칸인 채 무기한 남을 수 있고, 그동안 Prism의 지도는 생성되지 않는다. 문제 절: §3, §4, §5-6, §6.

[P1] `전사엔진판`은 측정 장치의 판이 아니다 — 문서 스스로 GCP가 실제 서빙 판을 반환하지 않아 요청한 모델명만 남는다고 인정한다. 그런데 §6-2에서는 이를 시계열 판정의 전제로 둔다; 같은 모델명 뒤에서 제공자가 갱신하면 학생 변화와 모델 변화를 여전히 구분할 수 없다. 문제 절: §1-⑧, §3, §6-2.

[P1] `전사신뢰도`의 집계 단위가 없다 — STT 응답의 여러 `results[]`와 여러 낱말을 하나의 신뢰도로 줄이는 평균·최솟값·가중치 규칙이 정의되지 않았다. 낱말별 위치·구간·정렬도 없어 어느 목표 낱말에서 문제가 났는지 연결할 수 없다. 문제 절: §1-⑦, §3, §6-2.

[P1] 학생에게 돌려줬다는 증거가 빈 껍데기다 — `돌려준날`은 `buildVoiceGrowthCards_`가 쓴 날짜일 뿐 카드ID·카드판·내용·렌더링 결과를 가리키지 않는다. 발음태그가 비어 있는 시점에는 학생이 실제로 얻는 교정 정보가 무엇인지도 정의되지 않아 `누구=학생` 도장이 전달 성공처럼 부풀 수 있다. 문제 절: §3, §5-6, §6-2.

[P1] 학생 기본 게이트가 빠졌다 — `HW306`의 20초 자유 답변에 여섯 낱말을 반드시 넣게 하면서 자연스러운 대체·선택·즉시 피드백을 정의하지 않았다. 학생이 왜 한 번 더 열어야 하는지, 그 학생의 목표에 어떻게 맞는지, 무엇이 신선한지에 대한 답이 없어 재미·목표 도움·신선함 층이 비어 있다. 문제 절: §2-3, §4, §5-6.

[P1] 18열의 최종 순서와 구버전 호환이 불명확하다 — 기존 규약은 `schema_ver`를 끝에 둔다고 했는데 새 칸을 어디에 붙이는지 명시하지 않았다. 열 위치를 쓰는 기존 코드와 머리글 이름을 쓰는 코드의 호환 여부를 964개 정적 검사만으로 확인할 수 없다. 문제 절: §1의 12칸 목록, §3, §4, §5-4.

[P1] Prism 연결은 계약이 아니라 미래 서술이다 — §6-2는 Prism과 `aiStudioBatch_`가 읽는다고 적지만 입력 API, 출력 스키마, 읽기 시점, 커서, 실패 상태가 없다. `닿았나✗`를 적는 것만으로는 다음 수집분이 실제 엔진에 도달하는지 검증할 수 없다. 문제 절: §5-5, §5-6, §6-2.

이 문서보다 나은 대안 구조가 있으면 1개 — Supabase에 불변 `voice_attempt` 이벤트를 두고 `assignment_id·assignment_version·item_id·target_snapshot[]·season_id·season_rule_ver·audio_ref`를 한 번에 기록한다. STT 결과·발음 라벨·학생 카드 전달은 별도 사건으로 연결하고, `voice_missions`는 버전 테이블로 관리하며 시트는 조회 어댑터로만 둔다.