# 상태기반 과제선택 — 처분표 v8 (8회차 · 대상 설계 v5.4)

> **런** `심문-2026-08-14T0028-ac77d5` · 대상지문 `731680160550`(= v5.4) · 편성 luna×2+sol×1(모델정책 고정값)
> **원건 64** — luna-1 **21**(P0 16·P1 5) · luna-2 **19**(P0 15·P1 4) · sol-3 **24**(P0 17·P1 7) · 결과 3벌 = 같은 폴더 `...-731680160550-{luna-1,luna-2,sol-3}.md`(커밋 `39d69237`)
> **게이트**(유호 확정 08-12): ①판정 = 원건 하나도 빠짐없이 채택/부분/기각/범위밖 + 근거 · ②집행 = 채택분이 본문에 들어왔다. 둘 중 하나라도 비면 ⛔착수 차단 유지.
> 🚫 이 표로 처분표 v1~v7 을 덮지 않는다(회차 대조). 🚫 「몇 벌이 같은 말을 했나」는 가중치지 증명이 아니다.

## §1. 조건 지문 — F411 수리 뒤 첫 회차, 처음으로 «얼었다»

| 회차 | 상태 | 방향 | 철학 | 금지 |
|---|---|---|---|---|
| 8회차 던짐(00:28Z) | 3벌 동일 | `4fb5cafa7c07` | `47da58ed6659` | `beeec9a4fa92` |
| 8회차 완료(00:52Z) | 3벌 동일 | `4fb5cafa7c07` | `47da58ed6659` | `beeec9a4fa92` |

던짐↔완료 짝 6/6 동일 — **회차 «안» 조건 통제가 장부로 증명되는 첫 판**이다(옛 6짝은 append-only 라 못 고침 · 7회차까지는 「불가」 판정). 회차 «간» 대조는 다음 회차(9회차)부터 이 판과 짝지어 성립한다.

## §2. 갈래 표 — 원건 64 → **46갈래** (판정은 §3 실측 뒤)

표기: L1-n = luna-1 지적 n · L2-n = luna-2 · S-n = sol-3. 판정 = ✅채택 · ◐부분 · ❌기각 · ⧉범위밖.

### A군 — 실행 장부 물리 계약 (14)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| A1 | `event_draft`·`load_error` 물리 칸 부재(+`_targets` 선택/필수 충돌) | L1-1·L2-1·S-1 | ✅ DDL 에 두 열 신설 + `_targets` 필수/선택 정리(실측 §3-1) |
| A2 | 구제(`jobs_load_one`) 계보 보존 — 갱신 칸 미정·`load_error` 저장 없음 | L1-16 | ✅ 갱신 칸 전량 표로 못박고 최초 실패 원인 보존 규정 |
| A3 | `적재실패` 행을 못 만드는 실패 구간(not null 충돌) | S-2 | ✅ 실패 행 최소 봉투 규정(배치 상수는 채우고 `branch_snapshot` 은 부분 허용+A8 검사 면제) |
| A4 | `batch_runs` 같은 트랜잭션 롤백 모순 · ON CONFLICT 부재 | L1-2·S-24 | ✅ 시작 행을 «선행 별도 트랜잭션»으로 가르고 재실행 계약 명문(luna-1 대안 채택) |
| A5 | 0-job 배치의 실행판 소실 | S-10 | ✅ batch_runs 에 실행판을 싣되 «그 실행이 쓴 판»으로 뜻을 갈라 V6-3(두 표 갈림)과 양립 |
| A6 | status×outcome 조합 미강제 | L1-11·L2-11 | ✅ 값 단위 CHECK 추가 + §3-6 표에 outcome 열(실측: 지금은 `적재실패` 하나뿐) |
| A7 | `attempt_close` 가 `result=null` 로 닫히는 길 | L2-7 | ✅ `_result` null 이면 예외(시그니처 규정) |
| A8 | 실패 outcome 의 결정적 attempt 계보 부재 | S-6 | ✅ 결정적 시도 칸(성공 전용 승자와 별개) — 시각·순번 추정 불가 논거 승인 |
| A9 | 검문 실패 사유 사건↔시도 파생 미대조 | L2-8 | ✅ ㉤ 대조 목록에 파생 규칙 추가 ➕`:1690` 한글 열명 잔재 수리(심문 밖 발견) |
| A10 | `degraded`×`generation_outcome` 미대조 | L2-9 | ◐ 물리 CHECK 는 원리상 불가(파생 금지 `:1207`·갈래 의존) — ㉤ 대조 목록에 갈래별 규칙로 편입 |
| A11 | `branch_snapshot` 의미 교차 검사 없음 | L2-10 | ◐ 전 조합 물리 강제는 과잉 — `jobs_load` 적재 검사에 의미 규칙(게임날 job 금지 등) 추가 |
| A12 | `_event` 작성자·검증 범위(대조가 `outcome` 하나) | L1-12 | ✅ ㉤ 대조 목록 확장(learner·assign_date·intervention_id·스냅샷 투영) |
| A13 | `task_snapshot`·`output_text`·요청본문 복수 정본 | L2-2·S-11 | ✅ 투영·동일 바이트 대조 규정(둘째는 첫째의 기계 대조되는 사본으로 명시) |
| A14 | 실행 봉투에 `schema_ver` 부재 | S-7 | ✅ 봉투에 `schema_ver` 고정(적재 시점) — 배치 중 계약 배포 혼입 차단 |

### B군 — 시간·배선 (6)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| B1 | `assign_date` 시간대 계약(UB 날짜 변환식 부재) | L1-3·S-9 | ✅ §3-2-a 에 UB 날짜 SQL 식 명문(`(now() at time zone 'Asia/Ulaanbaatar')::date`) |
| B2 | `deliver-check` 00:35↔06:05 충돌 · 기존 잡 미검사 | L1-4 | ✅ 표 칸 갱신 + §12-21 에 기존 잡 변경 검사 편입 |
| B3 | cron 「10분마다」가 실제로 20분 공백 | S-22 | ✅ 식을 `*/10 16-21` 로(30회→36회 · 문구 함께 갱신) |
| B4 | 기존 `task.assigned` 무조건 스킵 은폐 · `jobs_load_one` 사건 경합 | L1-5·L2-14 | ✅ C5 에 부분 실패 감지(개입·submissions 결손 검사 · 게임날 갈래 명시) + 구제의 기존 «사건» 경합 규정 |
| B5 | 큐 회전 분모 모순(jobs ↔ 재적) | L2-13 | ✅ §11-5 의 식 삭제(㉡ 단일 정본 — §11-5 자신의 선언 집행) |
| B6 | 기술 선택이 자기 입력보다 먼저 실행(오케스트레이터↔워커 순서 역전) | S-3 | ✅ 이력 조회를 가른다: 겨냥 이력은 오케스트레이터가 적재 «전»(같은 as_of), 중복 이력만 워커 — ②′·§6-0 재배선 |

### C군 — 스냅샷·이력 (4)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| C1 | 이력 조회 이중 cutoff 미구현(②′·§6-0 ↔ §11-4) | L1-6·L2-5·S-4 | ✅ 두 조회에 `occurred_at<=as_of AND ingested_at<=as_of` 명문(산문 `:2422` 를 쿼리로) |
| C2 | 중복 검사 자기모순(쌍↔OR) · 질문 미공급 · `성공` 필터 부재 | L1-7·L2-6·S-5 | ✅ 판정식 하나로(문장·질문 각각 정규화 완전일치 · 어느 하나 일치=중복) + 질문 조인 배선(assigned_event_id→submissions) + `성공` 필터 |
| C3 | `evidence_refs` object↔array 충돌 | L2-3 | ✅ `_event` 쪽을 §6-1 객체 모양으로 정정 |
| C4 | `evidence_refs` 변환 규격이 실물 상태 객체로 불가능 | L2-4 | ✅ 「as_of 근접 순」 철회 — 원천 배열 순서 그대로 앞 50(실측: 사건별 시각이 원천에 없다 `:213`·`:520`) |

### D군 — 계약·이름 (5)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| D1 | API 응답 한글 키(`배정상태`·`신규`·`기존`) 자기 규칙 위반 | L1-8 | ✅ 필드명 영문화(`assignment_status` 등 · 값은 한글 유지 = ㉴ 규칙 그대로) |
| D2 | `배정상태` 가 C0·앱 사이 고아 | S-14 | ✅ 응답 자리·앱 어댑터 인수 조건 명시 + C0 §4-3 개정을 c12 요청에 동봉 + 마감 뒤 잔존 `대기\|claimed`=오류로 읽는 규정 |
| D3 | c12 선행 — 지금 판은 실행 가능한 동결 계약이 아님 | L1-13 | ◐ 순서는 이미 정본(§11-2 「요청 판」·c12→검증기→첫 행) — 선행 작업 1번임을 §16-1 표에 등재. 「동결 불가」 처방은 기각(게이트=전건 처분으로 이미 개정 · 유호 확정 08-12) |
| D4 | 계약 정본 저장소 방향(talk↔appsscript) | S-12 | ✅ 머리 정본 관계 정정 — 문서(P0·C0) 정본=talk · 계약 JSON 정본=**appsscript**(`계약동기화.js:9` · 단방향) — c12 개정 작업 자리 명시 |
| D5 | c12 「미지 키 거절」의 기계 원천 부재(`choice_dimension` 누락) | S-13 | ✅ 「현행 검증기가 거절한다」 거짓 전제 정정 — 거절은 c12 검증기 «신규 동작»이고 목록 원천=계약 JSON(기계) · C0 §177 갱신 동봉 |

### E군 — 판·지문·평가 (6)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| E1 | `policy_ver` 전이 함수 누락 · 정규 직렬화 규격 부재 | L2-12·S-8·L1-21 | ✅ 재료 10 을 아홉 RPC 전부로 확장 · 직렬화는 판지문 장치 코드가 정본임을 명문(◐ — 새 규격 신설 대신 장치 위임 강화) |
| E2 | 품질 초록의 판 축 오배선(`skill_taxonomy_ver` 누락·`policy_ver` 과잉) | S-17 | ✅ 차단기 조합에서 `policy_ver` 제거 + `skill_taxonomy_ver` 추가(품질을 바꾸는 축만 묶는다) |
| E3 | 기술 회전 해시 endian·정밀도 미규정 | S-23 | ✅ big-endian·BigInt 명문 |
| E4 | §8-B 자기모순(3→7↔8칸 · case_id · 해시 규격) | S-15 | ✅ E5 와 함께 해소 — `attempt_id` 를 빼면 7칸이라 「3→7」이 참이 된다 + case_id 를 base#회차로 분해 + 두 해시 규격(SHA-256·UTF-8·대상 바이트) 명문 |
| E5 | 오프라인 평가 `attempt_id` 고리 불성립 | S-16 | ✅ 결과 행에서 `attempt_id` 삭제 — 재현 고리는 `raw_response_hash`·`input_hash` 가 진다(운영 장부 오염·끊긴 UUID 두 갈래 다 차단) |
| E6 | 평가 활성화 기준이 실행 경로와 무연결 | L2-19 | ◐ 「파일 실재=활성」은 유지(V6-23 새 플래그 금지) — 활성 시 실행 경로가 그 파일을 읽는 배선+`prompt_ver` 일치를 §12-24 형 실물 대조로 추가 |

### F군 — 인수 조건·마이그 (5)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| F1 | §12-21 「RPC 여섯」 낡음 | L1-9 | ✅ 여섯→아홉 정정(2곳) — §12-25 전이표↔시그니처 기계 대조가 있는데 산문 숫자가 새는 자리 |
| F2 | 값목록 15↔14(`이미배정`) 회귀 통과 불능 | L1-10 | ◐ §4-1 이 이미 15/14 를 갈라 적음(`:1408~1409`) — 구멍은 §12-21 눈금이 어느 집합인지 무언 ⇒ 「DDL == 행에 남는 14」로 명문 |
| F3 | 마이그 재실행 가드·롤백 계약 충돌(상위 정본 포함) | L1-15·L2-15·S-19 | ✅ §3-5-b DDL 에 가드 직접 삽입(§12-21 이 요구를 이미 적었는데 본문이 안 걷힌 형태 — v5 낙방 동작 재발) + §12-27 에 L0 §8 기록 6종 정합·「첫 학생 뒤 재개정」 조건(◐ — 역마이그 신설은 계속 안 함) |
| F4 | 실학생 산출 게이트 부재·§12-17 「제출」 중의성 | L2-16 | ✅ §12-3 이 약속한 «첫 학생 뒤 항»을 §16-1 에 실제로 신설(미집행 실측) + §12-17 낱말을 「학생 실제 산출(발화 관측)」로 정정 |
| F5 | 상태·목표 «사용» 증거가 입력 차이에만 머묾 | L2-17·L1-18 | ◐ 조립 규칙에 「`goal_snapshot` 있으면 요약에 목표 조각 필수」(결정적 규칙이라 가능) + §12-22 에 그 존재 검사. 런타임 산출-상태 검증은 기각(품질 런타임 문턱 금지 · §8-B ②⑦이 자리) |

### G군 — 완결·정합 (3)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| G1 | ④완결 ✓✓✗ ↔ ✗✗✗ — 문서 안 상충 | L1-14·S-20 | ✅ **✗✗✗ 로 통일**(세 자리 전부) — ④의 「모였나」는 실저장이지 설계가 아니다(철학 ④ 원문 · 「설계상 ✓」는 §16-1 스스로 자인한 무리) · 7회차 E4 의 ✓✓✗ 를 이 판이 뒤집는다 |
| G2 | 철학 정본 A-1 이 v5.2·5회차로 낡음 | L1-20 | ✅ `SYNK_철학.md:177` 한 줄 갱신(②집행 커밋에 동승 · 국소 — 파생 일괄 재도장 아님) |
| G3 | 「닿았나」 소비자의 데이터 계약 부재(`성과회수` 실물 시그니처) | S-18 | ✅ H3 처방을 실물로 다시 쓴다 — `성과회수(개입,사건들,옵션)`(옵션.기준시각) · corrections 공급자(누가·어떤 키·어떤 창)를 이름으로 · 픽스처가 조인을 대신 못 하게 「운영 생산자 실재」 눈금 분리 |

### H군 — 학생 접점·품질 (3)
| # | 갈래 | 원건 | 판정 |
|---|---|---|---|
| H1 | 재미·재방문 층의 연결 계약 부재 | L1-17·L2-18 | ◐ §8-A 에 «연결 표» 1절(이미 선 실물을 이름으로: 재발화 꼬리·achieve 효과음·캐릭터 반응 — 생성 과제 화면이 그 층에 «어떻게 실리나») + 시즌 회고 전달 필드. 학생 단위 «측정» 요구는 기각(닫힌 결정 「재미를 학생 단위로 안 잰다」·학생 0명 — luna-2 스스로 그 축을 갈랐다) |
| H2 | `skill_ids` 회전 = 학생 상태 아닌 사전 가설 기록 | L1-19 | ◐ §6-0 H2(조인 키 규정)에 「사전 겨냥(처치 가설)이지 실력 판정이 아니다」 뜻 한 줄 보강 + ⏳유호 E2 연결 명시. 적합성 재설계는 기각(🚫학생 약점으로 좁히기 — E2 답이 열쇠) |
| H3 | 급수 적합성 시험이 런타임 조합(72)을 안 덮음 | S-21 | ◐ 「수준 적합성 앞에 반드시 선다」 주장을 실제 보장 범위(기술당 두 급수 칸)로 축소 + 셀 보고에 급수×기술 커버리지 명시. 전 조합 사람 채점은 기각(㉡ 사람 손 폭증 — sol-17 자신의 경고와 정면) |

## §3. 실측 — 인용 않고 열어서 쟀다 (컨텍스트 독립 측정 3벌 위임 · 줄번호 동봉 보고)

### §3-1. 설계문 §3-5-b·§3-6 (측정 A)
- **A1 참** — `generation_jobs` DDL(`:655~746`·ALTER `:836`)에 `event_draft`·`load_error` 열 0. 규정은 참조한다: `:898`(jobs_load 인자 `event_draft?, load_error?`) · `:922`(C1 「전량 저장」) · `:1047`(마감 스윕이 「저장된 event_draft」로 조립) · `:1064`(구제가 새로 넣음) · `:1225`(전이표 `load_error` 전제). `_targets` 물음표(선택)와 C1 「전량」 충돌도 실재(`:898`↔`:922`).
- **A4 참** — batch_runs 는 jobs_load 와 같은 트랜잭션(`:872`) · `finished_at null=도중에 죽었다`(`:869`) ⇒ 롤백되면 그 null 행 자체가 안 남는다. ON CONFLICT·멱등 문장 0(멱등 문장 `:906`·`:934`는 jobs 쪽만). 실행판 4칸은 명시 배제(`:874` V6-3).
- **F2 참** — `outcome` CHECK 리터럴 **14**(`:690~692`) · §4-1 표 **15**(`:1388~1402` · `이미배정` 포함 · `:1408~1409`가 「전체 15/행에 남는 14」를 이미 갈라 적음) — 즉 §12-21 의 「DDL 집합 = §4-1 집합」 눈금이 어느 쪽 집합을 가리키는지가 구멍.
- **A6 부분참** — status↔outcome 값 단위 CHECK 는 `jobs_load_failed_cols`(`:717~719` 적재실패→내부오류) **하나뿐**. `대상아님`·`마감폴백`(→`예산소진` 은 산문 `:1239`)은 미강제. §3-6 표(`:1302`)에 outcome 열 자체가 없다.
- **A7 참** — `attempt_close(_result)` 비-null 요구 0(`:979`) · `result` 열 nullable(`:775`) · 가드는 `where result is null`(`:981`) ⇒ null 로 「닫으면」 열린 채 남아 재닫기 가능.
- **A8 참** — 승자 칸은 `성공` 전용(`:735`·`:737`·`:839~843`) · 실패 outcome 의 결정적 attempt 칸 0.
- **A11 참** — `branch_snapshot` 검사는 키·자료형·값목록(`:1073`·`:917~918`·표 `:1076~1083`)뿐, 분기 의미 교차 0.
- **A12 참** — `jobs_finalize` 의 `_event` 대조는 ①outcome 쌍(`:1010~1011`) ②필수 칸 부재 예외(`:1218`) 둘뿐.
- **A13 참** — `_event` 에 `task_assigned.task_snapshot`(`:1132`)과 `submission_row.task_snapshot`(`:1179`) 둘 다 실재 · 투영/일치 규정 0.
- **A14 참** — `schema_ver` = 호출자 몫(`:1205` 「job 에 없는 값이다」).
- **A3 참** — `branch_snapshot`·`skill_taxonomy_ver`·실행판 4칸 전부 `not null`(`:664~685`) · A9(`:903~904`)는 4칸만 정하고 조립 실패 건을 무엇으로 채우는지 규정 0.
- **A2 참** — 구제 갱신 칸 = 종료 2칸 비움+`branch_snapshot`·`event_draft` 신규+펜싱 그대로(`:1063~1065`·`:1231`)뿐 · `snapshot_as_of`·`skill_ids`·`skill_taxonomy_ver`·실행판 4칸 갱신 여부 미기재.
- **B4 참** — C5(`:929~930`)는 기존 «사건» 있으면 통째 스킵 · 게임날/부분 실패(배정만 남음) 구분 0 · `jobs_load_one` 은 기존 **job 행만** 본다(`:1062` — 기존 «사건» 확인 문장 0).
- **B5 참** — ㉡ 회전 분모 = 그날 job 수(`:954~955`) ↔ §11-5 = 재적수(`:2448~2449`) · §11-5 스스로 「규칙을 다시 안 적는다」(`:2452~2453`)면서 식을 적어 갈렸다.
- **B6 방향확정** — 조립(기술 선택 포함)은 오케스트레이터 학생별 루프·RPC 밖·생성 전(`:669`·`:897`·`:900`).
- **F3(DDL) 참** — 이 절 DDL 에 `if not exists`·`or replace` 0 — §12-21 이 스스로 지적까지 한다(`:2710`).
- **A9 참** — 「시도엔 배열 전부·사건엔 첫 사유」는 §3-5-b «밖»(`:1690~1691`·`:2028~2029`)에 있고 파생 대조 제약 0. ➕**심문 3벌이 못 본 것 1**: `:1690` 열 이름이 `검문실패사유`(한글) ↔ DDL `:777` `gate_failed_reasons` — G2 영문화 잔재.
- **A10 참(층 정정)** — `degraded` 는 jobs 열이 아니라 `_event` 칸(`:1149`)이고 「outcome 에서 파생하지 않는다」가 명시(`:1207` — 첫날·교정문은 성공 아닌데 false) ⇒ 물리 CHECK 는 원리상 못 걸고, 걸 자리는 ㉤ 대조 목록이다.
- **A5 방향확정** — batch_runs 에 실행판 4칸이 없는 것은 V6-3(두 표 갈림 방지)의 «의도»였다(`:874`) — 0-job 날 소실은 그 의도의 대가.

### §3-2. 외부 정본·코드 (측정 C)
- **D4 참** — `tools/계약동기화.js:9` 「정본 = 이 저장소. 형제(SYNK-talk)는 언제나 사본」 · `:78` 단방향 쓰기 · talk 에 역방향 통로 0.
- **D5 참** — talk `lib/이벤트검증.js:578~687` 에 payload 전수 화이트리스트 루프 0(서버칸 **차단**목록 `:600~604` + 특정 키 정합 `:646~647`만). C0 §177(`C0_API계약.md:177`)의 닫힌 목록에 `choice_dimension` **0회** — 그런데 그 키는 계약 JSON·`선택로그.js:165` 에 산다(문서만 낡음 = §177 자신이 적은 재발 조건).
- **G2 참** — `SYNK_철학.md:177` 부록 A-1 = 「v5.2·5회차·⛔6회차 심문 전 착수 차단」 — 두 판 낡음.
- **G3 부분참** — `성과회수(개입, 사건들, 옵션)`(talk `lib/성과회수.js:99` — 셋째는 옵션 객체·`옵션.기준시각` `:108`) · 순수 함수 참 · corrections 를 회수로 넘기는 배선 talk 전체 0(호출 2곳 다 `learning_events` 행만).
- **C4 부분참** — 학습자상태.js: 상한 200(`:119~121`) 참 · `잘림`=원래 개수(`:255~260`) 참 · **사건별 시각·쓴축 없음 참**(`:213` 한 자루 · `:520` 자인) ⇒ 「as_of 근접 순 정렬」은 상태 객체만으로 불가.
- **E6 재료** — `prompts/과제생성.md` 는 appsscript·talk **둘 다 없음**(talk `prompts/` = `교정.md` 하나 · `오늘과제.js:16` 이 부재 자인).
- **F3(상위 정본) 참** — talk `개발_분업.md:187` 「마이그레이션 검증(빈 DB·기존 데이터·**중간 실패·롤백**)」 · `L0_데이터계약.md:636~637` 「롤백·전진 경로의 기록 6종」. ⚠둘 다 **talk 저장소** 문서다(appsscript 에 없음 — 심문자 경로 표기만 부정확, 내용 참).
- **C2 재료** — 질문은 `호흡[].프롬프트` 로 실린다(talk `오늘과제.js:159~173`·화이트리스트 `:206`) — 계약 JSON 은 `호흡` 0회(그릇까지만 계약).
- **D2 재료** — C0 §4-3(`:264`) `data:[]` = 「오늘 배정 없다 → 고정 도입 · 오류 아님」 — `배정상태` 는 C0 에 없는 개념.

### §3-3. 설계문 나머지 절 (측정 B)
- **B2 참** — cron 표 칸은 여전히 `deliver-check` 00:35(`:472`) · 06:05 는 표 아래 산문뿐(`:500~501`).
- **B3 참(정밀화)** — 워커 식 `10-50/10 16-21 * * *`(`:470`) = 30회는 맞으나 분 00 이 빠져 **시 경계마다**(16:50→17:10) 20분 간격 — 「10분마다」 문구와 어긋난다.
- **B1 참** — `오늘`→UB 날짜 변환식 0(문서 전체 `Asia/Ulaanbaatar` SQL 은 `:1211` due_at 파생 하나뿐 · `:658` 주석뿐).
- **D1 참** — `배정상태 ∈ 있음|없음|생성중`(`:389`) 위치 규정 없음 · `{"신규","기존"}`(`:934`·`:1110`) · §11-2 는 영문 snake_case·한글 🚫(`:2293`·`:2299`·`:2311`).
- **D3 재료** — §11-2 가 c11 현행·c12 「요청 판」을 이미 명시(`:2281~2282`) · §12 는 c12 3칸 전제 인수조건 실재(`:2502~2505`·`:2727~2728`).
- **C1 참** — §11-4 이중 cutoff 요구(`:2392`) ↔ ②′(`:269~270`)·§6-0 이력 조회(`:1741`)는 `occurred_at` 하한 하나뿐(산문 확장 `:2422` 만).
- **C2 참** — 같은 줄 `:2023` 안에 「완전 일치」와 「문장 일치 **또는** 질문 일치」 공존 · ②′ 봉투 = `output_text`·`skill_ids` 둘(`:269~270`) · `성공` 필터 0(E3 의 「degraded=true 엔 skill_ids 안 실림」은 기술 축만 덮는다).
- **E1 참** — §5-1 재료 12 중 함수 정의는 재료 10 의 `jobs_finalize`·`jobs_finalize_due` 둘뿐(`:1538`) · 「정규 직렬화」는 기존 판지문 장치 위임(`:1542~1543`) · 절단 규격은 값 형식 `<해시12>`(`:1550`)뿐.
- **C3 참(자리 정정)** — §6-1 정본 = 객체(`:1818`) · 50개·`잘림:true`·as_of 근접 순은 §6-2 가 아니라 **§6-1**(`:1840~1841` · 변환 주체 = `과제요약.js` `:1847`) · §6-2 의 50 은 딴 것(축 문자열 50자 `:1884`).
- **E3 참** — D6 해시(`:1740`): UTF-8·SHA-256·앞 8바이트까지 있고 **endian·BigInt 규정 0**.
- **G1 참** — §6-0 H3 표 = ✗✗✗(`:1773~1775` · 산문 `:1769`는 ✓✓✗) ↔ §12-17 ✓✓✗(`:2592`) ↔ §16-1 ✓✓✗+「전부 설계상 판정」 자인(`:3090~3092`) — 한 문서 안 세 값.
- **E4 참** — 「3→7칸」(`:2179`·머리 `:19`) vs 실제 열거 **8**(`case_id·axis_scores·grader_note·sentence·question·raw_response_hash·input_hash·attempt_id`) · 두 해시 알고리즘·대상 바이트 규정 0(§5-3 에 0회).
- **E2 재료** — 차단기 6칸 = `model·prompt_ver·policy_ver·estimator_version·시험지해시·채점표판`(`:2181`·`:2648`) — `skill_taxonomy_ver` 없음.
- **H3 재료** — 기술 층화(`:2125`): 18기술 ≥2회 · 같은 기술은 다른 급수 칸 — 즉 72 조합 중 최대 40.
- **E6 참** — 「활성」= `prompts/과제생성.md` 실재(`:2182`·`:2652`) · 실행 경로 연결 검사 0.
- **F1 참** — §12-21 「RPC 여섯」 2회(`:2720`·`:2723~2724`) ↔ §3-5-b 는 아홉.
- **F3 재료** — §12-21 은 이미 「v5.2 DDL 이 자기 조건을 못 지났다 ⇒ if not exists」(`:2709~2711`)를 적었는데 **§3-5-b 본문 DDL 은 무가드 그대로**(측정 A 15) — 「새 절을 얹고 형제 절을 안 걷었다」 그 형태. 롤백 = §12-27 「빈 DB 재적용이 실질 롤백」(`:2793~2795`).
- **F5 재료** — §12-22 는 입력 문자열 차이만 재고 스스로 「반영 여부는 §8-B ②·⑦의 일」 자인(`:2754~2758`).
- **F4 참** — §12-3 「별도 항으로 §16-1 에 적는다」(`:2472~2473`) — **§16-1 에 그 항이 없다**(3050~3121 전수 · 미집행 실측). §12-17 「제출」(`:2576`)은 사전 생성 보조 행으로 읽힌다(§12-3 자신이 `:2469~2470` 에서 경고한 그 낱말).
- **A13 재료** — §7 두 자리(`:1906`·`:1928~1934`) · §3-0 물리 자리=submissions·무증설(`:294~296`·`:1935`).
- **A10 재료** — degraded 정본 뜻 = 「최상 갈래 아님」(`:1625`·`:1631`) · 값 대응 §4 표(`:1370~1371`) · §12-7 에 degraded 0회.
- **D4 참** — 머리 `:65~66` 「계약 정본은 SYNK-talk …·`계약/수집_교정_계약.json`」 ↔ 실물 동기화 방향은 appsscript 정본(§3-2 측정 C).
- **B6 참** — ②′ = 워커 claim 직후(`:245~247`·`:268`) ↔ `jobs_load` 인자에 `skill_ids`(`:896~898` · 큐 만들 때 고정 `:1714`) — 회전 입력(최근 겨냥 이력)이 소비보다 «뒤»에 읽힌다. §6-0 의 시점 해명(`:1752~1754`)은 급수 축만 덮는다.
- **B5 재료** — §11-5 재적수 식(`:2448~2449`)과 「여기 규칙을 다시 안 적는다」(`:2452~2453`) 공존.

## §4. 원건 → 갈래 전량 매핑 (64/64)

| 원건 | 갈래 | | 원건 | 갈래 | | 원건 | 갈래 |
|---|---|---|---|---|---|---|---|
| L1-1 | A1 | | L2-1 | A1 | | S-1 | A1 |
| L1-2 | A4 | | L2-2 | A13 | | S-2 | A3 |
| L1-3 | B1 | | L2-3 | C3 | | S-3 | B6 |
| L1-4 | B2 | | L2-4 | C4 | | S-4 | C1 |
| L1-5 | B4 | | L2-5 | C1 | | S-5 | C2 |
| L1-6 | C1 | | L2-6 | C2 | | S-6 | A8 |
| L1-7 | C2 | | L2-7 | A7 | | S-7 | A14 |
| L1-8 | D1 | | L2-8 | A9 | | S-8 | E1 |
| L1-9 | F1 | | L2-9 | A10 | | S-9 | B1 |
| L1-10 | F2 | | L2-10 | A11 | | S-10 | A5 |
| L1-11 | A6 | | L2-11 | A6 | | S-11 | A13 |
| L1-12 | A12 | | L2-12 | E1 | | S-12 | D4 |
| L1-13 | D3 | | L2-13 | B5 | | S-13 | D5 |
| L1-14 | G1 | | L2-14 | B4 | | S-14 | D2 |
| L1-15 | F3 | | L2-15 | F3 | | S-15 | E4 |
| L1-16 | A2 | | L2-16 | F4 | | S-16 | E5 |
| L1-17 | H1 | | L2-17 | F5 | | S-17 | E2 |
| L1-18 | F5 | | L2-18 | H1 | | S-18 | G3 |
| L1-19 | H2 | | L2-19 | E6 | | S-19 | F3 |
| L1-20 | G2 | | | | | S-20 | G1 |
| L1-21 | E1 | | | | | S-21 | H3 |
| | | | | | | S-22 | B3 |
| | | | | | | S-23 | E3 |
| | | | | | | S-24 | A4 |

기계 검사: L1 21 + L2 19 + S 24 = **64/64** · 빠짐 0 · 중복 0 · 잉여 0.

## §5. 집행 지도 — 절 단위 · 이 순서대로 (②집행 = 설계 v5.5)

> 집행자는 심문 결과 3벌을 다시 안 읽어도 된다 — 이 표가 절 단위 지도다.
> **규율 셋**(5~7회차 교훈 그대로): ①한 절을 고치면 그 값을 쓰는 형제 절을 같은 커밋에서 전수 grep ②`(실재)`·`(실측)` 표기는 «파일:줄» 동봉 ③「집행했다」≠「돈다」 — 넣은 것이 자기 요구를 만족하는지 그 자리에서 재확인.

1. **§3-5-b DDL** (물리 먼저 — 저장되면 못 고친다): A1(`event_draft jsonb`·`load_error text` 열+`_targets` 정리) · A14(`schema_ver` 열) · A6(CHECK — 대상아님→대상아님·마감폴백→예산소진) · A8(결정적 시도 칸) · F3(DDL 재실행 가드 직접 삽입 — `if not exists`·조건부 constraint) · A3(적재실패 최소 봉투 — 배치 상수 4칸+`skill_taxonomy_ver` 는 채워지고 `branch_snapshot` 부분 허용·A8 스키마 검사 면제) · A5+A4(`batch_runs` — 시작 행 선행 별도 트랜잭션·`jobs_load` 가 갱신·재실행 ON CONFLICT 계약·실행판 4칸+`schema_ver`+`skill_taxonomy_ver` 을 «그 실행이 쓴 판» 뜻으로 싣기)
2. **§3-5-b RPC 규정**: A7(`attempt_close` `_result` null=예외) · A2(`jobs_load_one` 갱신 칸 전량 표 — `snapshot_as_of`·`skill_ids`·실행판은 «새로 굳힌다» 명시 · 최초 `load_error` 보존) · B4(C5 부분 실패 감지+`jobs_load_one` 기존 사건 경합) · A12+A13+A9+A10(㉤ `jobs_finalize` 대조 목록 확장 — learner·assign_date·intervention_id 일치 · task_snapshot 투영 동일 바이트 · output_text=문장 · `generation_gate_failed`=결정 시도 `gate_failed_reasons` 첫 항목 · outcome×degraded 갈래표 · ➕`:1690` `검문실패사유`→`gate_failed_reasons`) · A11(`jobs_load` 의미 검사 — `is_game_day=true` job 거절 등)
3. **§3 ②′·§6-0** (경로): B6(겨냥 이력 조회를 오케스트레이터 적재 «전»으로 — 같은 as_of · 중복 이력만 워커) · C1(두 조회에 이중 cutoff 명문) · C2(중복 판정식 단일화+질문 조인 배선+`성공` 필터) · E3(해시 big-endian·BigInt)
4. **§3-2-a** (배선): B1(UB 날짜 SQL 식) · B2(표 06:05 갱신) · B3(`*/10 16-21`·36회 문구)
5. **§4-1·§3-6**: F2 는 §12-21 쪽에서 · A6 의 outcome 열을 §3-6 표에
6. **§5-1·§5-3**: E1(재료 10→아홉 RPC 전부 · 직렬화=판지문 장치 코드 정본 명문)
7. **§6-1**: C3(`_event` 를 객체로) · C4(정렬 철회 — 원천 순서 앞 50)
8. **§6-0**: H2(사전 가설 뜻 보강+⏳E2 연결) · G3(닿았나 처방을 실물 시그니처·corrections 공급 계약으로)
9. **§8-A·§8-B**: H1(연결 표 1절+회고 전달 필드) · E2(차단기 `policy_ver` 제거·`skill_taxonomy_ver` 추가) · E4+E5(`attempt_id` 삭제→7칸·case_id 분해·해시 규격) · E6(실행 경로 실물 대조 추가) · H3(주장 축소+커버리지 셀 보고)
10. **§11**: D1(응답 키 영문화) · D2(자리·어댑터·마감 뒤 잔존 규정+C0 §4-3 개정 동봉) · D4(머리 정본 관계 — JSON 정본=appsscript) · D5(검증기 전제 정정+목록 원천=계약 JSON+§177 갱신 동봉) · B5(§11-5 식 삭제)
11. **§12·§16-1**: F1(여섯→아홉) · F2(눈금=행에 남는 14) · F3(§12-27 L0 §8 정합+첫 학생 뒤 재개정) · F4(§16-1 «첫 학생 뒤 항» 신설+§12-17 낱말) · F5(목표 조각 필수+존재 검사) · D3(§16-1 선행 작업 표 — c12 개정=1번)
12. **머리말·정합** (맨 마지막): G1(④완결 ✗✗✗ 통일 — H3 표·§12-17·§16-1·보드) · G2(`SYNK_철학.md:177` 한 줄 — 같은 커밋 동승) · 판 v5.4→**v5.5** · §16-1 회차 표 갱신

## §6. 재제안 금지 (이 회차 — 근거는 §2 해당 칸)

- `degraded` 를 `generation_outcome` 에서 **파생**시키기(A10 — 파생 금지가 정본 `:1207` · 대조는 ㉤ 갈래표만)
- 평가 결과 행에 `attempt_id` 되살리기(E5 — 운영 오염/끊긴 UUID 두 갈래 다 막힌 자리)
- §8-B 차단기 조합에 `policy_ver` 되넣기(E2 — 배관 변경마다 80회 사람 채점)
- §11-5 에 회전 식 다시 적기(B5 — ㉡ 단일 정본)
- 급수×기술 **전 조합** 사람 채점(H3 — ㉡ 사람 손 폭증)
- 학생 단위 재미 «측정» 게이트(H1 — 닫힌 결정 · 학생 0명 · 재개 닻은 §8-B)
- `skill_ids` 후보를 학생 약점으로 좁히기(H2 — 기존 🚫 재확인 · ⏳유호 E2 가 열쇠)
- batch_runs·jobs 의 실행판을 「같은 사실 두 표」로 부르기(A5 — «실행이 쓴 판»과 «job 에 적용된 판»으로 뜻을 갈랐다)
- `_targets` 의 `event_draft` 를 다시 선택(`?`) 표기로(A1 — C1 「전량」이 정본)
- 역마이그레이션 신설(F3 — 개원 전=빈 DB 재적용 유지 · 단 L0 §8 기록 6종은 따른다)

## §7. 게이트 현황

- **①판정 = ✅ 종결**(이 표 · 원건 64/64 매핑 · 46갈래 ✅37·◐9·갈래층 기각 0 — 기각은 처방층 4: D3 동결불가·F5 런타임검증·H1 측정·H2 재설계·H3 전조합)
- **②집행 = ✅ 종결**(46/46 · 2026-08-14 · 세션 `185657ef`) — 설계 **v5.5**. §5 지도 12단계 순서
  그대로 커밋 5벌: `212f7a8c`(1~2단계 · 물리) · `c2d3bd98`(3~5 · 경로·배선) · `97f281b4`(6~9 ·
  판·평가) · `fcd0900c`(10~11 · 계약·인수) · `6f69621f`(12 · 머리말·G1·G2 동승 · 판 v5.5).
  ⛔**구현 착수 차단은 그대로다** — 새 회차 지적은 다시 전건 처분을 지난다(유호 확정 08-12).
  ▶ 다음 = **9회차 심문을 v5.5 에**(회차 «간» 조건 대조가 이 회차와 짝지어 처음 성립).
- ④완결(이 트랙) = **✗✗✗**(G1 판정 — 구현 0줄 · «설계상 ✓» 폐기 · v5.5 가 세 자리 통일 집행)
