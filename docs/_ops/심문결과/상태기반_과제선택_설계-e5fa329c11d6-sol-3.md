동결 불가: P0 11건, P1 10건.

[P0] 닫힌 `pg_cron` 결정 위반 — §3-2-a는 워커·마감 잡을 “등록 마이그레이션”으로 만들지만, [지도.md](</C:/Users/q1212/.claude/projects/C--Users-q1212-Documents-SYNK-appsscript/memory/지도.md:102>)의 닫힌 결정은 `pg_cron=마이그레이션 아닌 도구`다. 프로젝트별 함수 URL·런타임 과녁이 다른데 이 판을 동결하면 금지된 배포 경로가 물리 정본이 되고, 미등록·오과녁 시 큐는 영원히 `대기`로 남는다.

[P0] 마이그레이션 인수 조건이 스스로 실행 불가능하다 — §12-21은 같은 마이그레이션의 재적용 멱등을 요구하지만 §3-5-b DDL은 `CREATE TABLE`, `ADD CONSTRAINT`, `CREATE INDEX`에 재실행 가드가 없어 두 번째 실행에서 바로 충돌한다. 같은 절의 “롤백 절차는 안 만든다”도 [개발_분업.md](</C:/Users/q1212/Documents/SYNK-talk/docs/개발_분업.md:187>)와 인계문의 중간 실패·롤백 검증 의무를 정면으로 거부하므로, 부분 적용 뒤 복구 가능한 상태가 정의되지 않았다.

[P0] `generation_outcome` 닫힌 목록이 DDL에 없다 — §3-5-b는 CHECK 리터럴과 §4-1 목록을 기계 대조한다고 선언하지만 실제 `generation_jobs.outcome`은 제약 없는 `text`다. 자유 문자열이 장부에 저장될 수 있어 잘못된 값이 append-only 계보에 남고, §12-21의 “DDL CHECK 집합 대조”는 대조할 집합 자체가 없다.

[P0] 임대 만료 회수 전이가 존재하지 않는다 — §3-5-b는 “다섯 RPC 밖에서 전이 없음”을 선언하면서 `claimed→대기` 회수를 표에만 넣었다. `jobs_claim`은 `status='대기'`만 집고 `jobs_finalize`는 종료만 하므로 임대 시각이 지나도 상태는 저절로 바뀌지 않으며, 죽은 워커의 일감은 재시도되지 않고 06:00 마감폴백까지 고착된다. `_lease_sec`의 초기값도 어느 정본에도 없다.

[P0] 승리 시도가 다른 일감에 붙을 수 있다 — §3-5-b의 FK는 `winning_attempt_id → attempts.attempt_id`만 검사하고 `attempt.job_id = generation_jobs.job_id`를 보장하지 않는다. 따라서 학생 A의 job이 학생 B의 attempt를 승리 시도로 가리켜도 DB가 승인하며, 모델·프롬프트·원응답 계보가 영구 오염된다.

[P0] 원자 착지 입력이 자기 조인을 표현하지 못한다 — §3-5-b의 `_event.task_assigned`에는 §3-0이 필수라고 한 `intervention_id`가 없고, `submission` 블록에는 물리 `engine.submissions`에 존재하지 않는 `event_type='submission.created'`가 들어 있다. 실제 [L0_스키마.sql](</C:/Users/q1212/Documents/SYNK-talk/supabase/L0_스키마.sql:434>)에서 submissions는 보조 행이지 이벤트가 아니므로, 구현자는 그 필드를 무시하거나 학생이 제출하기도 전에 가짜 `submission.created` 이벤트를 만들게 된다.

[P0] 배치 시작 실행판이 job에 고정되지 않는다 — §5-1은 배치 시작에 판지문을 한 번 계산해 그날 모든 job이 같은 값을 쓰라고 하지만, `generation_jobs`와 `jobs_load._targets`에는 `model`, `prompt_ver`, `policy_ver`가 없다. 이 값들은 나중에 워커의 `attempt_open`에서 다시 들어오므로 오케스트레이터와 워커 사이에 배포가 바뀌면, 옛 규칙으로 만든 `branch_snapshot`과 새 실행판으로 생성한 결과가 같은 배정일에 섞인다.

[P0] `skill_ids` 생산 원천이 실재하지 않는다 — §6-0 H1은 `lib/문법급수.js`에 “급수→기술 매핑”이 있다고 단정하지만, 실제 [문법급수.js](</C:/Users/q1212/Documents/SYNK-talk/lib/문법급수.js:24>)는 G문법 ID의 급수표와 레벨 밴드만 제공하며 `engine.skills`의 30개 ID로 이어지는 매핑을 내지 않는다. 파일은 어휘 7종·조사 2종의 급수를 모른다고 명시하므로, 구현하면 일부 기술이 영구 누락되거나 생산 실패가 `[] = 후보 없음`으로 오기록된다.

[P0] 겨냥 기술별 오류율은 계산 불가능한 가짜 `늘었나`다 — §6-0 H3과 §12-17은 `skill_ids × 후속 제출·교정` 조인만 이름 붙였을 뿐, 학생이 해당 기술을 실제 사용한 기회·관측 창·오류태그 대응·분모를 정의하지 않았다. 생성물이 겨냥 기술을 실제로 요구했는지 검사하는 축도 §8-B의 7축에 없고, 현행 `engine.skills.error_tags` 시드는 비어 있어 “오류 없음”과 “그 기술을 쓰지 않음”을 구분할 수 없다. 이 값을 ㉠ 이해 대장에 넣으면 처치 의도가 관찰된 실력으로 위조된다.

[P0] 출시 차단기의 활성 조건이 정의되지 않았다 — §8-B I2와 §12-19의 “생성 경로가 활성이면”이 어떤 설정·DB 값·빌드 플래그를 뜻하는지 없다. 테스트 구현이 해당 조건을 false로 해석하면 평가 결과 파일이 없어도 초록이 되므로, 미실행과 통과가 정확히 같은 모양이 된다.

[P0] 평가 결과의 실행판 일치 규칙이 서로 모순된다 — §8-B는 결과에 8칸을 동봉하고 하나라도 다르면 옛 초록을 거절한다고 하지만, 실제 차단기 조건은 `model+prompt_ver+policy_ver` 세 칸만 비교한다. 그러면 옛 `estimator_version`·시험지·채점표 결과가 재사용될 수 있고, 반대로 채점자·시각까지 “현재값과 일치”시킬 기준은 존재하지 않아 8칸 완전 일치 규칙은 구현할 수도 없다.

[P1] `jobs_finalize` 반환 계약은 SQL 시그니처와 맞지 않는다 — `returns uuid`인 스칼라 함수는 `SELECT` 시 한 행의 UUID 또는 NULL을 돌려주지, 문서가 요구하는 “0행·null”을 동시에 표현하지 못한다. §3-5-b의 낡은 워커·정상 착지·이미 종료 세 갈래를 호출자가 동일하게 해석할 수 있는 판별 계약이 닫히지 않았다.

[P1] 시도 장부 CHECK가 절반만 막는다 — `attempts_result_gate`는 `검문탈락`일 때 배열이 non-null인지만 검사한다. 빈 배열, 목록 밖 사유, 성공·타임아웃 행에 붙은 실패 사유를 모두 허용하므로 §4-1의 “검문탈락일 때만 7값”과 충돌한다.

[P1] 초과 응답의 유일한 원본을 스스로 버린다 — §4-2는 `raw_response`가 성공·실패 원문 전부를 보존한다고 선언하면서 `응답초과`에서는 앞 8,000 코드포인트만 남긴다. 비결정 응답의 뒤쪽과 usage·응답 ID가 소실될 수 있어, 비용·벤더 이상·파싱 실패의 원인을 나중에 복원할 수 없다.

[P1] 평가셋에 런타임 도달 불가능한 셀이 들어 있다 — §4-3은 `쓸축수===0`이면 벤더를 호출하지 않고 `상태없음`으로 내리는데, §8-B는 `상태없음` 셀도 두 번 생성하도록 한다. 평가 표본의 3분의 1이 실제 경로에서 절대 노출되지 않는 입력이어서, 도달 가능한 표본적음·표본풍부 품질이 약해도 전체 게이트가 통과할 수 있다.

[P1] 급수 취약 구간 차단 규칙이 무효다 — §8-B I3은 한 급수가 “전 축 미달”일 때만 전체 실패로 친다. 특정 급수가 정확성·수준·답변 가능성·재미 등 6축에서 무너지고 한 축만 넘겨도 이 규칙은 통과하므로, “평균이 취약 급수를 숨기는 것을 막는다”는 목적을 달성하지 못한다.

[P1] 상태판과 규칙판의 소유권이 갈린다 — §6 표는 `policy_ver`가 상태 축을 진다고 하지만 §5-1의 여섯 재료에는 `학습자상태.js` 계산 규칙이 없고, 다른 곳에서는 `estimator_version`이 그 판을 진다. 여기에 §6-1은 현행 [학습자상태.js](</C:/Users/q1212/Documents/SYNK-talk/lib/학습자상태.js:117>)의 `200개·잘림=원래 개수`를 `50개·잘림=true·쓴축 추가`로 바꾸면서 어느 판을 반드시 올릴지도 정하지 않아, 같은 판 이름 아래 서로 다른 증거 모양이 섞일 수 있다.

[P1] 현행 인수 숫자가 다시 두 정본이다 — §5-1은 판지문 재료와 픽스처를 6개라고 고쳤지만 §12-5는 여전히 7벌·일곱 재료를 요구한다. §4-1·§12-7은 `행 14 + 응답 1 + 행 0`인데 활성 §13은 다시 `13 + 응답 1 + 행 0`이라고 적어, 구현자가 어느 실패 경로를 생략해도 한쪽 문장으로 정당화할 수 있다.

[P1] 부분 실패 분모가 같은 절에서 다시 갈린다 — §3-6과 §12-8 앞부분은 `generation_jobs` 대상 분모와 실제 닻 분모 두 개를 함께 내라고 한다. 그런데 §12-8 뒤에서는 다시 “분모 = intervention.delivered 건수”라고 단수 정본을 선언해, 대상 분모가 빠지고 사라진 학생이 통계에서도 사라지는 구현을 허용한다.

[P1] 워커 계측은 합격 게이트가 아니다 — §12-20은 CPU·콜드 스타트·claim·finalize 시간을 재라고만 하고 허용 상한이나 `워커학생상한` 계산식을 정의하지 않는다. 현재 Supabase Edge Functions의 문서상 요청 idle 한도는 150초이고 CPU 시간 한도는 2초인데, 숫자만 출력하는 검사는 한도를 넘겨도 판정상 빨강이 아니다. [Supabase 공식 제한 문서](https://supabase.com/docs/guides/functions/limits)

[P1] 학생이 왜 다시 여는지가 설계에서 제거됐다 — §8-B는 이 질문을 범위 밖으로 밀고 첫 시즌 회고까지 미룬다. 지금 학생이 받는 것은 문장 하나와 질문 하나뿐이며, 선택권·즉시 반응·재도전 변주·캐릭터 반응·완료 보상이 없어 “답하고 싶나”를 원장 1인이 오프라인 채점하는 것만으로 재방문 이유를 대신한다. 이는 학생 접점에 즉시 적용해야 하는 ①의 재미 게이트를 사후 측정으로 미룬 결함이다.

이 문서보다 나은 대안 구조가 있으면 1개: **“동결 실행봉투를 가진 DB 큐” 구조**다. `jobs_load`가 배정일·스냅샷뿐 아니라 `model/prompt/policy/estimator`와 기술 겨냥 규칙을 한 번에 고정하고, `claim_expired`를 포함한 명시 RPC·동일 job 복합 FK·닫힌 outcome CHECK·상태가 있는 반환형으로 전이를 구성한다. 착지는 정확히 `task.assigned`와 `intervention.delivered` 두 이벤트 및 submissions 보조 행 하나만 만들고, cron은 닫힌 결정대로 프로젝트 확인형 배포 도구가 등록한다. 기술 효과는 “겨냥 기술 사용 기회→실제 사용 관측→교정 태그 대응” 계약이 생기기 전까지 `늘었나`로 승격하지 않으며, 출시 게이트는 명시적 활성 플래그·도달 가능한 셀·급수별 필수축을 검사한다.