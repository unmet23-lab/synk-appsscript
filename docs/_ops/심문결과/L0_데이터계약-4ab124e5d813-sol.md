동결 판정: 실패 — P0 17건, P1 3건.

[P0] 동결 상태가 사실과 어긋난다 — §7은 payload 검증과 왕복 테스트를 아직 `⏳`로 두고, §12-6도 c8 원격 적용·물리 왕복이 미완료라고 적었다. 반면 닫힌 정본은 c8 실왕복·운영 반영 종결이고, 실물에는 §12가 언급하지 않은 `20260807060000_engine_c8.sql`까지 있다. 이 문서는 동결 후보도 현행 적용 기록도 아니다.

[P0] 인증 방식이 문서 안에서 양립 불가능하다 — §4-1은 `student_code + 학생이 정한 비밀번호`를 확정하지만 §7은 로그인 코드가 `student_code`가 아닌 별도 고엔트로피 비밀이라고 단언한다. §4-2-2는 임시번호를 Auth 비밀번호에 넣지 않는다고 했는데 §4-2-3은 `updateUserById`로 임시 비밀번호를 설정하라고 한다. 구현자는 어느 계약을 따라도 다른 절을 위반한다.

[P0] 관찰과 추론의 분리가 물리에 존재하지 않는다 — §2·§3-4는 `source_kind`와 추정 메타를 필수 구분으로 설명하지만 실제 DDL에는 `source_kind`, `estimator_confidence`, `evidence_refs`, `estimator_version`, `valid_from`, `expires_at`, `user_confirmed` 열이 없다. Edge 검증기는 앱이 이 값을 보내는 것은 막으면서 서버 INSERT도 저장하지 않아 조용히 버린다. `system`도 문서 enum에만 있고 실제 배치의 `task.assigned`는 `actor_kind='ai'`로 오염된다.

[P0] 빈 껍데기 이벤트가 정상 행으로 저장된다 — §3-2는 이벤트별 payload 검증과 `payload.ver` 필수를 선언하지만 실제 검증기는 `quiz.answered`에 `task_type`만 요구하고 `preference.stated`·`session.abandoned`에는 내용 필드가 하나도 없다. `choice.selected`도 빈 선택지, 음수 지연시간, 선택과 전량거절의 동시 참 같은 모순을 막지 않는다. 서버 사건인 `intervention.delivered`·`data_use.granted`·`exam.result`도 전용 검증 규격이 없다.

[P0] 훈련 승격 사건이 계보 없이 성립한다 — §6-2가 `artifact_ref`, 해시, serializer·dataset 버전, 정확한 `consent_id`, 승인자와 평가 참조를 최소값으로 못박았지만 `data_use.granted`는 `payload={}`로도 저장 가능하다. §9-3의 `data_use.revoked`도 이 계보를 가리키지 않는다. 승격·철회 행이 있어도 무엇을 어느 데이터셋에서 빼야 하는지 복원할 수 없다.

[P0] 개념 축이 무검증 입력이다 — §3-5는 배열 FK 부재를 서버 검증으로 막겠다고 하지만 실제 events 함수는 `skill_ids`와 `skill_taxonomy_ver`를 존재·판본 대조 없이 그대로 INSERT한다. 앱도 임의 `skill_ids`를 주장할 수 있고 `skills` 정의 자체도 서비스 역할에서 덮어쓸 수 있다. 잘못된 개념 ID가 쌓이면 어휘 지도와 학습자 모델의 축이 영구 오염된다.

[P0] 과제 스냅숏과 제출 사이의 정확한 고리가 없다 — §3-3은 `task_snapshot`을 소급 불가 필수값으로 선언하지만 제출 검증은 이를 요구하지 않고, 실제 앱은 배정에서 받은 `task_id`를 제출 사건에 싣지 않은 채 자유 문자열 `task_ref`만 돌려준다. `parent_event_id`도 비어 있고 DB FK·유일성도 없다. 같은 `task_ref`가 재사용되거나 배정행이 어긋나면 학생이 본 정확한 판을 확정할 수 없다.

[P0] “실제 전달”과 “생성·배정”이 거짓으로 합쳐졌다 — §3-2의 `intervention.delivered`는 실제 전달 사건이어야 하지만 구현은 야간 배치 시점에, 학생이 앱을 열기 전에 이를 쓴다. 최초 제출도 배정 사건이나 `intervention_id`를 직접 가리키지 않아 서버의 개입 계승이 일어나지 않는다. 후보→추천→실제 노출→학생 반응→성과 사슬에서 실제 노출과 성과 고리가 모두 끊겼다.

[P0] S1의 소급 불가 측정값이 로컬에서 소멸한다 — P0 제품 계약은 재생 완료, `hesitation_ms`, 발화 길이·문턱, 무발화 맥락, 자기 목소리 되듣기를 수집 대상으로 정했다. 화면은 이 값을 로컬 로그에 만들지만 `lib/오늘과제.js`는 `hesitation_ms`·`duration_ms`를 계약 밖이라 명시적으로 전송하지 않고, 되듣기에도 사건이 없다. 앱 기능이 데이터를 낳는다는 §1의 전제가 이미 깨졌다.

[P0] 앱 멱등키가 서로 다른 제출을 합친다 — §2와 C0는 앱 사건마다 UUID v4를 한 번 생성해 재시도에도 유지하도록 정했지만 실제 제출은 `submission:{날짜}:{호흡}:{시도}`를 쓴다. 다른 기기·앱 재설치·같은 날 별도 세션에서 시도 번호가 다시 시작되면 서로 다른 제출이 충돌한다. 서버는 이를 정상 `duplicate`로 답하므로 데이터 손실이 성공과 같은 모양이다.

[P0] 원본과 스냅숏이 불변도 단일 정본도 아니다 — `submissions.event_id`에 UNIQUE가 없어 한 사건에 여러 제출물이 붙을 수 있고, `task_type`·`occurred_at`·`schema_ver`는 이벤트와 제출물 양쪽에 중복되지만 일치 제약이 없다. §10-2의 트리거는 `body_original`과 `transcript`만 보호해 `task_snapshot`, `task_schema_ver`, `audio_ref`, `capture_meta`, `transcript_verified`는 덮어쓸 수 있다. “그때 본 것”과 검증 전사가 사후 개서 가능한 구조다.

[P0] 철회 뒤 수집 0건을 보장하지 못한다 — §3-6은 클라이언트가 정하는 `occurred_at`만으로 동의 유효성을 판정하므로 철회 뒤 요청도 과거 시각으로 보내면 저장된다. 이벤트는 정확한 `consent_id`가 아니라 문자열 `consent_ver`만 들고, 동의 행은 중첩·개서가 가능하다. §9-3 철회 SQL은 `schema_ver='c5'`를 하드코딩하고 운영 사건을 기본값 `actor_kind='learner'`로 기록하며, 같은 날 철회 키도 하나로 충돌한다.

[P0] 미디어 원본과 삭제 완료를 증명할 수 없다 — §3-3의 `audio_ref`·`image_refs`에는 콘텐츠 해시, 바이트 수, 객체 버전이 없어 같은 경로의 파일이 교체돼도 DB 증거는 그대로다. §9-3-2는 Storage 삭제 후 DB 표식을 남기는 비원자적 손절차이고 오디오·이미지 전체를 `audio_deleted_at` 하나로 뭉갠다. 부분 삭제·DB 실패·참조 없는 고아 업로드가 모두 정확한 완료와 구분되지 않는다.

[P0] 세션 폐기가 실제 API 경로에서 우회된다 — §4-2-3·§4-5는 `active`·`revoked_before`가 만료 전 access token까지 막으며 Edge Function도 같은 판정을 반복한다고 주장한다. 실제 events·tasks·corrections·progress·uploads는 `auth_user_id`로 learner만 찾고 `session_alive()`를 호출하지 않는다. 폐기된 옛 토큰으로 읽기·쓰기·업로드 서명 경로가 계속 열린다.

[P0] 공개 가입 계정 선점이 첫 등록을 망가뜨린다 — §8-2는 공개 가입이 열린 현재 위험을 낮다고 판정하지만 합성 이메일은 학생번호로 예측 가능하다. 실제 첫 등록은 같은 이메일의 기존 `auth.users` 행을 발견하면 소유권·비밀번호·확인 상태를 검증하지 않고 그 UID를 learner에 연결한다. 선점된 계정이면 실제 학생에게 성공을 보여준 뒤 로그인이 막히고, 확인된 선점 계정이면 타인 계정에 learner가 연결된다.

[P0] 비밀번호 초기화의 30분·5회·1회용 보증이 경쟁 상태에서 깨진다 — reset은 DB에 임시 해시·폐기 시각·감사행을 먼저 커밋한 뒤 GoTrue 비밀번호를 바꾸므로 후자가 실패하면 옛 비밀번호로 새 토큰을 받을 수 있다. temp-login은 GoTrue 비밀번호를 먼저 바꾸고 임시 해시를 나중에 지워 중간 실패나 동시 요청에서 임시번호가 재사용된다. 시도 횟수도 조회와 증가가 분리돼 병렬 추측 요청이 5회 문턱을 동시에 통과한다.

[P0] 골든 판정과 직접 교정이 같은 모양으로 오염된다 — §3-4·§4-5는 강사 판정이 특정 AI 교정과 특정 `staff_id`를 가리킨다고 하지만 DB는 actor별 필수 조합, 검토 대상의 `actor_kind='ai'`, reviewer의 staff FK를 강제하지 않는다. 실제 확정 도구는 자유 문자열 reviewer를 쓰고 `reviewed_correction_id`를 생략한다. `transcript_verified`도 검수자·검수 시각·규칙 버전 없이 가변이라 평가셋 라벨 계보가 없다.

[P0] 사후 검증이 잘못된 스키마를 초록으로 만든다 — §8·§10-3의 확인 SQL은 열의 존재만 보고 타입·NULL·기본값을 검사하지 않으며, 제약은 이름만 보고 대상 표와 정의를 보지 않는다. 정책도 8개라는 개수만 셀 뿐 이름·명령·`USING`·`WITH CHECK`를 대조하지 않고, 트리거는 대상 표·함수·발화 조건을 검증하지 않는다. 같은 이름과 개수를 유지한 누출 정책이나 무효 제약이 `✅ 전부 통과`가 될 수 있다.

[P0] 닫힌 화면 결정과 정면 충돌한다 — §4-5는 검수 대시보드를 Expo 앱 역할 화면으로 확정하고 “앱 밖 웹”을 재제안 금지로 뒤집었다. 제공된 닫힌 정본은 원장탭은 앱이 아니며 검수 대시보드는 앱 밖 웹이라고 명시한다. 이 문서대로 동결하면 배포 경계와 권한 표면을 잘못 구축한다.

[P1] 작성·적용 책임선이 뒤집혔다 — 머리말과 §8은 Claude가 L0 물리 계약과 원격 SQL 적용을 맡도록 하지만 확정 분업은 Codex가 DDL·마이그레이션·기술 시험을 쓰고 Claude가 교육 의미를 승인하는 구조다. §9-3-2는 Storage 삭제라는 물리 작업까지 유호님에게 넘긴다. 작성자·검수자 분리와 “유호님은 결정만”이라는 운영 규칙을 동시에 위반한다.

[P1] `pipeline_jobs`와 `daily_activity`는 소비자 없는 선행 구축이다 — §9-2가 두 표를 만들었지만 마이그레이션·검사 밖에는 쓰는 코드가 없고 `daily_activity.expected`도 nullable이다. 행이 없으면 여전히 배치 실패와 실제 부재를 구분하지 못하며, 이미 `task.assigned`가 분모 역할을 해 두 번째 정본이 된다. E0 코드 0과 앱 우선 원칙을 어기면서도 주장한 문제를 해결하지 못한다.

[P1] 검수자 비식별 주장이 거짓이다 — §4-5는 inspector가 학생 신원을 보지 않는다고 하지만 §3-1은 원문과 음성에 이름·전화·주소가 우발적으로 들어온다고 인정한다. 검수 큐는 원본 제출물과 음성을 그대로 내주며 검수용 불변 비식별 산출물이나 정확한 외부 반출 영수증이 없다. 구조화된 `display_name`만 숨긴 것을 신원 비노출로 기록했다.

이 문서보다 나은 대안 구조 1개 — `현행 전용 계약 매니페스트 + append-only 인과 사슬`로 재구성한다. 변경 이력은 별도 문서로 떼고, 하나의 c9 기계 정본에 이벤트 생산자·필수 타입·상호배타 조건·정확한 `consent_id`·배정/개입 부모·미디어 해시를 선언해 Edge 검증, DB 제약, 사후 catalog 서명을 생성한다. 기존 테이블 안에서 `task.assigned → intervention.presented → submission/retry → outcome`을 정확한 ID로 잇고 `submissions.event_id` 1:1·스냅숏 불변·공통 `session_alive` 검사를 강제하며, writer가 없는 큐·일일활동 표는 동결 대상에서 제외한다.