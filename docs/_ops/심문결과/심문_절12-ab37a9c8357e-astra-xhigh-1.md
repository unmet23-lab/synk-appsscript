확인 불가 — 운영·실기기 동작은 실측하지 않았습니다. 아래는 제공하신 `856089ca8` 후보본 기준이며, 줄 번호도 해당 커밋 기준입니다.

**[P0] 동결 목록에서 일기의 실제 계약 변경이 빠졌다** — §12 D1·부록 표1·표2는 그릇과 허용 키의 유일 정본을 자처하지만, 일기에는 사건 이름만 싣고 실제 필드 목록을 비워 둡니다(`docs/엔진7종_상향설계_v3.md:7228`). 일기 정본 §6이 요구하는 `input_lang`, `pipeline_jobs.job_kind`, 유일 제약 교체와 기존 교정 워커의 작업 선별도 물리표에 없습니다. 이 표대로 계약을 만들고 일기 작업을 넣으면, 일기 작업을 기존 교정 작업과 구별해 처리해야 한다는 요구가 동결 범위에서 누락됩니다.

**[P0] 만짐 기록으로 약속한 자국의 위치를 복원할 수 없다** — §12 부록의 `mascot.touched`에는 횟수·시간만 있고 쓰다듬은 위치가 없습니다(`docs/엔진7종_상향설계_v3.md:7210`). 왼쪽만 쓰다듬은 경우와 오른쪽만 쓰다듬은 경우는 같은 기록이 되지만, 소비자인 Patina는 다른 위치에 자국을 내야 합니다. §5 U1도 위치 집계 칸을 D1에서 정하라고 넘겼는데, D1은 이를 받지 않은 채 기존 목록을 동결하려 합니다.

**[P0] 화용 능력의 ‘행동할 기회’가 계약에 없다** — D1 곁은 기회가 있었던 경우만 판단하겠다고 하지만, 부록은 기존 사건에 붙일 화용 표식만 정합니다(`docs/엔진7종_상향설계_v3.md:6709`, `:7227`). 문제가 없었던 수업과 문제가 있었지만 학생이 알리지 않은 수업은 모두 보고 행동 기록이 없다는 같은 상태로 남습니다. 기회의 발생·대상 행동·실제 반응을 연결하는 계약이 없으므로, 나중에 판단 코드를 만들어도 첫 시즌의 분모는 복원되지 않습니다.

**[P0] 강사가 고른 개입 종류가 저장 목록에 없다** — D6은 강사가 개입 종류를 고르게 하지만, 부록의 개입 기록은 전부 `teacher_classroom`으로 묶습니다(`docs/엔진7종_상향설계_v3.md:6786`, `:7219`). ‘짝 바꿈’을 고른 경우와 ‘개별 설명’을 고른 경우를 구별할 필드·닫힌 값목록이 없고, 전체 값목록의 지문인 `policy_ver`로도 그날 선택한 항목은 알 수 없습니다. §9 U2가 이 누락을 이미 D1로 넘겼는데도, 정작 동결표에는 해결되지 않았습니다.

**[P0] 리포트의 전달 증거를 기록할 계약이 없다** — D25는 제작·발송·학생 열람·부모 열람을 구별하라고 요구하면서, 부록에는 `intervention_id`, `recipe_ver`, `loom_ver`만 둡니다(`docs/엔진7종_상향설계_v3.md:7116`, `:7220`). 같은 리포트를 재인쇄하고 부모가 열었지만 학생에게는 건네지 않은 경우, 전달 경로·확인 종류·대상 리포트 판을 무엇으로 고정할지 정해져 있지 않습니다. 기존 `content.viewed`의 연결만으로는 종이 교부 증거까지 생기지 않으므로, ‘누구에게 닿았나’ 도장을 계약에서 판별할 수 없습니다.

**[P0] AI 도움 표식을 제출 뒤에 받아 원 제출과의 연결을 잃는다** — 부록은 `ai_assist`를 ‘보냈어요’ 카드에서 받으면서 저작신뢰 등급과 Temper 분모에 사용합니다(`docs/엔진7종_상향설계_v3.md:7217`). 제출 직후 앱을 닫거나, 재전송 중 다른 제출의 카드에 응답하면 어느 제출의 선언인지 고정하는 규격이 없습니다. 제출과 함께 저장할지, 후속 사건으로 연결할지, 응답이 끝내 없으면 어떻게 구별할지를 동결하지 않아 같은 산출물이 처리 순서에 따라 다른 학습 근거가 됩니다.

**[P1] 강사 카드 탭이 실제 수업 개입으로 둔갑한다** — D6·부록의 손 카드 탭은 §9 U3에서 서버의 `intervention.delivered(teacher_classroom)` 생성으로 연결됩니다(`docs/엔진7종_상향설계_v3.md:6797`, `:7228`; §9 U3 `:5024`). 강사가 가이드를 읽으려고 카드를 눌렀지만 학생에게 말을 걸지 않은 경우에도 개입이 전달된 모양이 됩니다. ‘추천을 확인했다’와 ‘교실에서 실행했다’의 증거를 구별하지 않으면 Temper는 하지 않은 수업 방법의 효과를 계산합니다.

**[P1] 빛의 뜻을 지킨다는 권고가 여전히 목표 읽기를 목표 반영으로 바꿔 센다** — D7의 수정 권고는 `reads.life.source === 'season_goal'`를 빛의 조건으로 삼지만, 이 값은 과제에 목표가 실제로 반영됐다는 증거가 아닙니다(`docs/엔진7종_상향설계_v3.md:6817`). 발음 목표를 읽은 뒤 무관한 쓰기 과제를 배정해도 조건을 만족하며, 실제 읽기 함수도 과제 내용을 받지 않고 해당 표식을 생성했습니다(합성 재현 1/1). 학생에게 돌아가야 할 ‘내 목표를 알아봐 주는 경험’이 단순한 입력 조회에 붙습니다. 게다가 요약표는 여전히 반대 선택인 **(b)**를 권고해, 표와 본문을 따라 승인한 결과까지 갈립니다(`:6631`).

**[P1] 성공 지표가 어려운 한국어를 피하는 정책을 유리하게 만든다** — D12의 주 결과변수는 첫 시즌 폴백 과제에서 ‘오류 확인 제출 수/제출 수’이며, 과제 난이도·새 문장 여부를 맞추는 조건이 없습니다(`docs/엔진7종_상향설계_v3.md:6917`). 합성 예로 익숙한 보기 문제만 반복해 오류가 0/30인 정책이, 새로운 문장을 시도하게 해 오류가 12/30인 정책보다 좋은 방향으로 나타납니다. 빠진 층은 **새 상황에서 한국어를 혼자 써 보는 능력**이며, 이를 후보 지표로만 남긴 채 주 지표를 동결하면 쉬운 과제 공급과 학습 향상을 구별하지 못합니다.

**[P1] 곡 사이 낱말에는 학생이 직접 써 볼 차례가 없다** — D8 ①은 낱말을 들려주고 답장 꼬리로 안내하지만, 그 표현을 학생이 말하거나 쓰게 하는 후속 과제 배선을 요구하지 않습니다(`docs/엔진7종_상향설계_v3.md:6824`). 관련 §10 U5도 이후 제출에서 표현이 자연스럽게 다시 나오기를 기다리는 구조여서, 학생이 계속 듣기만 해도 송출·노출 기록은 쌓입니다. 빠진 층은 **실사용 산출의 차례**이며, 청취와 체류 A/B만으로는 판단 정본의 ‘학생 산출이 엔진에 닿는다’를 통과할 수 없습니다.

**[P1] 사람 손을 세는 결정에 정책 자동 적용까지 끼워 넣었다** — D20은 처리 부담을 세는 질문 안에 ‘판올림 섀도 + 강사 일치’를 기본값 자동 대상으로 넣습니다(`docs/엔진7종_상향설계_v3.md:7053`). 자동화 대상이 후보 계산인지 활성 정책 교체인지 닫지 않아, 강사 일치가 나온 뒤 원장이 카드를 읽지 않은 상태에서도 정책을 바꾸는 구현이 이 선택지에 들어맞습니다. 이는 D4와 §9가 남긴 사람의 정책 선택 자리를 조용히 없애므로, 클릭 수 계산에 대한 승인이 정책 적용 승인으로 확장됩니다.

**[P1] 비용 상한의 계산 대상이 무료 케어 약속과 충돌한다** — D13은 무료 체험생·졸업 뒤 케어 대상까지 비용에 넣으라지만, D28은 총액을 ‘상한 × 재원 수’로 고정합니다(`docs/엔진7종_상향설계_v3.md:6949`, `:7148`). 유료 재원생은 없고 무료 케어 대상만 서비스를 쓰는 상황에서는 허용 예산이 계산상 사라집니다. 계산 대상과 유지할 서비스 묶음을 맞추지 않고 금액만 승인받으면, 같은 승인으로 한쪽은 서비스를 중단하고 다른 쪽은 상한 밖 지출을 하게 됩니다.

소급 불가 스키마·돈·확정 뜻 변경은 **봤다 → 문제다**입니다.

읽은 파일은 후보 전문과 관련 절·필드 기준으로 다음과 같습니다.

- SYNK-appsscript: `AGENTS.md`, `엔진7종_상향설계_v3.md`, `코어엔진_설계.md`, `제품방향.md`, `SYNK_철학.md`, `학생일기_설계.md`, `앱재설계_v2.md`, `살아있는자리_설계_v1.md`, `Temper_개입로그_규격_v1.md`, 결정·검수기각 장부, 수집·교정 계약.
- SYNK-talk: `AGENTS.md`, 저장소 규약·공용지침, L0·C0 계약, 개발 분업, 이벤트검증·사건통로·읽기기록·저작신뢰 코드.
- 안 본 것: 운영 DB·시트 값, 실기기 동작, 실제 청구, 외부 서비스 현재 조건. 지정하신 개인정보·보안·동의 등의 축은 지적에서 제외했습니다.

**더 나은 대안 구조 하나:** 기능별 **증거 계약표**를 유일 정본으로 두십시오. 각 행에 ‘학생에게 돌아가는 경험 → 발생 조건 → 당시 기회·행동·산출물 연결 → 필수 필드와 빈 값의 뜻 → 생산자·소비자 → 회수 시점 → 실패·재시도 시험 → 선행 결정’을 고정하고, D 선택지·계약 어휘·물리표를 여기서 파생해야 합니다. 지금처럼 절마다 해결을 D1에 넘기고 D1에는 이름만 모으는 구조로는 누락이 반복됩니다.