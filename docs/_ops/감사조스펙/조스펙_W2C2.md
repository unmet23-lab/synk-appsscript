# 시공 스펙 W2C2 — 항목 4건 (App.js·테마 군단)

**이 조가 소유한 파일(이 밖은 수정 금지)**:
- talk:App.js
- talk:src/오프라인카드.js
- talk:src/저장.js
- talk:src/인증화면.js
- talk:src/말하기화면.js
- talk:src/답장화면.js
- talk:src/몽글문화면.js
- talk:src/도착확인.js
- talk:tests/몽글문화면.test.js
- (신설 허용) talk:tests/감사회귀_W2C2.test.js

⚠ 이 군단에서는 talk:App.js 와 talk:src/테마.js 수정이 «허용»된다(소유 목록에 있을 때만). 항목 순서대로 시공하라 — 구조가 먼저 서야 뒤가 앉는다.

---
## D7-3

**현장(감사 원문)**: 앱 시작 오프라인 갈래 (SYNK-talk/App.js:62-85 세션 복원 · src/인증화면.js)

**시공 지시서**: ① 신설 talk:src/오프라인카드.js — App.js 로딩 화면(162-167행)과 같은 문법: 빈 Navy 바탕(색.바탕) + <기호 크기={56}/>(src/기호.js · 폰트 안 탐) + 본문 「인터넷이 연결되면 자동으로 들어가요」(문구 초안 · 확정 유호님) + 「다른 계정으로 들어가기」 작은 글자 출구(잉크_보조 · 기기 넘김용, onPress는 prop 손으로가기). 색·폰트 전부 킷 토큰. ② App.js: 상태 한 칸 const [복원막힘, set복원막힘] = useState(false); 재시도용 const [복원세대, set복원세대] = useState(0); 복원 효과(64-89행) deps를 [복원세대]로, catch(78-83행)를 「if (e && !e.retryable) { await 세션지우기(); } else { set복원막힘(true); }」로, 효과 머리에서 set복원막힘(false). AppState 리스너 하나: 'active' && 복원막힘 && !세션 이면 set복원세대(n=>n+1) — 갱신 자동 재시도. 렌더: !세션 && 복원막힘 이면 인증화면 대신 <오프라인카드 손으로가기={() => set복원막힘(false)} /> (손으로가기가 로그인 폼으로 내려보냄 — 인증 로직 무변경). 세션읽기가 null인 첫 사용 기기는 복원막힘이 안 서므로 현행 그대로.

**실물 근거(정찰)**: App.js:78-85 실물 — retryable(NETWORK 포함) 실패는 토큰을 남기지만 세션 null·복원중 false로 빈 로그인 폼에 떨어진다(전용 카드·자동 재시도 0). App.js가 피어 영토라 지금 시공 불가 — 오프라인카드.js 신설만 먼저 두면 소비자 0 고아라 한 벌로 미룬다.

**시험**: 값싼 회귀 1건: 복원 catch가 retryable 갈래에서 세션지우기를 안 부른다는 소스 정규식(tests/기기비우기.test.js 곁)

---
## D7-12

**현장(감사 원문)**: 만료로 재로그인하는 날의 로그인 폼 (SYNK-talk/App.js:77-81 · src/인증화면.js:45)

**시공 지시서**: ① 저장.js(208-250 세션 절): 새 함수 둘. export async function 만료시지우기() { if (웹) return; const s = await 세션읽기_원시(); await SecureStore.setItemAsync(세션키, JSON.stringify({ 학생번호: (s && s.학생번호) || '' })); } — refresh_token만 버리고 학생번호는 남긴다(기기 밖으로 안 나가는 값). export async function 학생번호읽기() — 세션키 JSON에서 학생번호만 돌려줌(refresh_token 유무 무관 · 세션읽기는 240행 규약대로 refresh_token 없으면 null 그대로라 로그인 폼으로 떨어지는 흐름 불변). 기기비우기(279행)는 현행 세션지우기() 유지 — 기기 넘김 정문은 전부 지운다. ② App.js:83 「if (e && !e.retryable) await 세션지우기()」 → await 만료시지우기(); 이어 const 남은번호 = await 학생번호읽기().catch(() => null); 를 상태(초기번호)로 들고 인증화면에 prop으로 내림. ③ 인증화면.js:43 서명에 초기학생번호 = null 추가, 45행 useState('') → useState(초기학생번호 || '') (로그인 단계 초기값일 뿐 — 40-42행 「토큰 prop 금지」 규율과 무관한 표시값).

**실물 근거(정찰)**: App.js:83 실물(bb904ab 반영 후) = 비-retryable이면 세션지우기() 전량 삭제, 인증화면.js:45 = 학생번호 useState('') 빈칸 시작 — 항목 미해결. App.js가 피어 영토(다른 세션 미커밋 작업 중)라 지금 시공 불가, 저장.js·인증화면.js 몫만 먼저 앉히면 반쪽이라 한 벌로 미룬다.

**시험**: tests/기기비우기.test.js(저장.js 세션 함수 덮음) + 1건: 만료시지우기 뒤 세션읽기()===null 이면서 학생번호읽기()가 번호를 돌려준다

---
## D4-1

**현장(감사 원문)**: 인증화면 첫등록 + App.js/저장.js + 마스코트 소비 전 자리 (신앱)

**시공 지시서**: ① src/저장.js — 마스코트기록읽기(116행) 무늬 그대로 가이드읽기()/가이드쓰기(이름) 신설(guide_choice.json · 깨짐·부재는 '몽글' 폴백 — 마스코트 실패 모드는 조용함). 기기비우기(270행)에 이 파일 지우기 합류(학생 귀속). ② src/인증화면.js 첫등록 — 문항 map(172행) 아래 「누구랑 같이 공부할래요?」 보기줄: 보기 고르기 컴포넌트(262행) 재사용, 몽글·까몽 둘 활성 + 마린 칸은 비활성 「곧 와요」. 답은 서버 첫등록 페이로드에 안 넣는다(lib/가입문항.js 25행 🚫 새 문항 금지 · auth 동봉) — 실행(106행)에서 로그인성공 직전 가이드쓰기. ③ App.js — 세션 복원·세움 자리에서 가이드읽기→state, 말하기화면(211행)·답장화면 렌더에 캐릭터 prop 전달. ④ 두 화면 — prop 받아 <마스코트 캐릭터={캐릭터}> (말하기화면 734·743·753행 · 답장화면 298행). 강사화면 437행은 몽글 고정 유지. 마스코트.js·가이드목소리.js·마스코트생명.js는 캐릭터 인자를 이미 받는다(87행 기본값 몽글) — prop 만 흘리면 소리·혼잣말이 함께 갈린다.

**실물 근거(정찰)**: grep '캐릭터=' src/ App.js = 0건(exit 1) · src/저장.js export 17종에 가이드 키 0(실측) · src/검수문.js 150-153행 주석 「고르는 화면이 없어 앱 어디서도 안 난다」 · 혼잣말.json 까몽150·마린150 카운트 실측

**시험**: tests/마스코트배선.test.js·tests/인증API.test.js 기존 + 새 회귀: 저장.js 가이드 왕복(쓰기→읽기 · 깨진 파일→'몽글' 폴백) 1건

---
## G1-5

**현장(감사 원문)**: C:/Users/q1212/Documents/SYNK-talk/src/몽글API.js (물어보기) — 호출처 0줄

**시공 지시서**: ① 새 화면 src/몽글문화면.js({토큰,돌아가기}): 원장초기화.js의 wrap/카드/줄버튼 스타일 무늬 재사용(색·폰트 킷토큰만). 질문 TextInput은 lib/몽글물음.질문판정으로 보내기 버튼을 잠그고 상한 근접 때만 남은수를 보인다. 보내기 → import { 물어보기 as 몽글묻기 } from './몽글API.js'(도착확인.js:48 동명 로컬 함수와 충돌 예약 — 별칭 필수) → 몽글묻기(토큰,{질문,화면:'몽글문'}). 갈래 셋 렌더: 답함=글+출처 목록+근거없음 배지 / 넘김=색.잉크_보조 안내(「원장님께 넘겼어요」 — 오류색 금지: 인계는 설계된 정상 결과) / 못받음=오류칸. 자동 재시도·답 캐시 금지(몽글API 머리말 🚫 그대로). ② 도착확인.js 155행 「교실 관찰 남기기」 줄버튼 아래에 같은 무늬로 「몽글에게 묻기 (원장·강사)」 onPress={()=>가기('몽글')} — 숨기지 않는다(권한은 서버 403이 정한다). ③ App.js 287행 관찰 라우트 다음에 {화면==='몽글' && <몽글문화면 토큰={세션.access_token} 돌아가기={()=>set화면('시스템')} />}. ②③이 피어 영토(도착확인.js·App.js 미커밋 작업 중)라 시공은 대기.

**실물 근거(정찰)**: grep '몽글API' App.js·src/·lib/·tests/ = lib/몽글물음.js 주석 1건뿐 — import 하는 화면 0개(오늘 재실측, 감사와 일치). 도착확인.js:48에 동명 로컬 물어보기 실재.

**시험**: 신규 tests/몽글문화면.test.js — 화면이 lib/몽글물음.질문판정을 쓰고 넘김 갈래에 색.신호가 없으며 setInterval/재시도 0건임을 소스로 잰다

