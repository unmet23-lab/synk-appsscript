# 시공 스펙 W2C1 — 항목 3건 (App.js·테마 군단)

**이 조가 소유한 파일(이 밖은 수정 금지)**:
- talk:src/오류경계.js
- talk:App.js
- talk:contents/문구_오류.js
- talk:tests/오류경계.test.js
- (신설 허용) talk:tests/감사회귀_W2C1.test.js

⚠ 이 군단에서는 talk:App.js 와 talk:src/테마.js 수정이 «허용»된다(소유 목록에 있을 때만). 항목 순서대로 시공하라 — 구조가 먼저 서야 뒤가 앉는다.

---
## G2-3

**현장(감사 원문)**: ② ErrorBoundary 실측 0건 — componentDidCatch·getDerivedStateFromError grep 0 (src·lib·App.js). 화면 하나의 렌더 예외가 곧 앱 전체 크래시다.

**시공 지시서**: ① 신설 src/오류경계.js(~30줄 · 클래스): static getDerivedStateFromError → {깨짐:true}; componentDidCatch(오류)에서 관측보고(오류, { spot: 'error_boundary', screen: this.props.화면이름 }) — 키는 ASCII(밖으로 나가는 키 규약), 값은 한글 OK. render: 깨짐이면 fallback — 문구_오류.js 새 키(진행형·명랑 초안 「화면이 잠깐 넘어졌어요 — 다시 세울게요!」 · 확정은 유호님) + 「말하기로 돌아가기」 버튼 = this.props.되세우기() 호출 후 깨짐 해제. 색·폰트는 src/테마.js 킷 토큰만. ② App.js: 화면 스위치 묶음(210~292행)을 <오류경계 화면이름={화면} key={화면} 되세우기={() => set화면('말하기')}>로 감싼다 — 겉테 상주 링크(294행~)는 밖에 둔다. key={화면}으로 화면 전환 때 경계 상태가 자연 리셋.

**실물 근거(정찰)**: grep componentDidCatch|getDerivedStateFromError|ErrorBoundary = App.js·src·lib 0건(08-31). 관측보고는 src/관측.js:129에 완성 — 미켜짐이면 false 반환이라 새 배선 0(감사 그대로).

**시험**: 신규 tests/오류경계.test.js — 화면렌더.test.js 무늬로 오류 던지는 자식 → fallback 렌더·관측보고 호출 확인

---
## D5-2

**현장(감사 원문)**: App.js 라우팅 — 화면 전환 전부(말하기↔답장↔어제↔시스템↔직원 6화면)가 조건부 렌더 즉시 교체(전환 모션 0, 소스 확인)

**시공 지시서**: App.js 안에 화면틀 컴포넌트를 세운다: `function 화면틀({ children }) { const 등장 = use등장({ 시간: 240, 올라옴: 8 }); return <Animated.View style={[{ flex: 1 }, 등장]}>{children}</Animated.View>; }` — import는 react-native `Animated` 와 `./lib/모션.js` 의 use등장. 라우팅 조건부 렌더 묶음(현재 211행 <말하기화면 ~ 288행 어제의나)을 `<화면틀 key={화면}>…</화면틀>` 하나로 감싼다 — key={화면} 이라 화면이 바뀔 때마다 remount 되어 등장 한 박자가 돈다. 겉테(294행 SYSTEM 링크, 답장·어제 링크 등 상주 층)와 인증·로딩 분기(라우팅 앞 return)는 밖에 남긴다 — 테는 서 있고 내용만 자리 잡는다. reduce-motion 은 use등장이 스스로 접는다(lib/모션.js:53-58). 새 눈금·새 의존성 0.

**실물 근거(정찰)**: App.js 211~288행이 {화면 === '…' && <…화면 />} 즉시 교체 렌더(grep: App.js에 Animated·use등장 0건). App.js는 피어 세션 미커밋 영토라 지금 수정 금지.

**시험**: tests/앱시작.test.js·tests/화면렌더.test.js가 App 골격을 덮는다. 회귀 1건: 소스층 — 라우팅 묶음이 화면틀(key={화면}) 안이고 SYSTEM 링크는 밖임을 assert.

---
## G2-1

**현장(감사 원문)**: ① Android 백버튼 — BackHandler 실측 0건 (src·lib·App.js·index.js grep 0 · android/MainActivity.kt는 Expo 기본 템플릿 = 백버튼이 태스크를 백그라운드로 보낸다)

**시공 지시서**: App.js에 안드로이드 백버튼 배선. ① 2행 react-native import에 BackHandler 추가. ② 41행 `const [화면, set화면] = useState('말하기')` 아래 useEffect 하나: `const 구독 = BackHandler.addEventListener('hardwareBackPress', () => { if (화면 === '말하기') return false; const 시스템행 = ['검수','강사','문구감수','반피드백','나침반','회고','관찰']; set화면(시스템행.includes(화면) ? '시스템' : '말하기'); return true; }); return () => 구독.remove();` 의존성 [화면]. 지도는 각 화면의 기존 돌아가기 prop과 동일하게(245~286행 실측: 직원 7화면→'시스템' · '답장'·'어제'·'시스템'·'비번변경'·'초기화'·'검수문'→'말하기'). '말하기'에서 false = 기본 동작(태스크 백그라운드) — 감사 원안 그대로. 내비 라이브러리 안 넣는 설계와 무충돌.

**실물 근거(정찰)**: grep BackHandler = App.js·index.js·src·lib 전체 0건(08-31 · HEAD 20d4455). android/app/src/main/java/lab/synk/talk/MainActivity.kt 존재(Expo 기본 템플릿). App.js 41행 화면 state·210~292행 조건부 렌더 실측.

**시험**: 신규 소스 회귀 1건(tests/백버튼.test.js 또는 기존 화면구문 곁) — App.js에 hardwareBackPress 등록과 직원화면→'시스템' 지도 문자열 존재

