# 라디오 생동·소리 끊김 재수정 — 2026-09-09

## 실제 반영

2026-09-09 19:50:13 KST, 기존 `radio-live`에 v2 무대와 무손실 반복 소리·송출 복구 보완을 함께 반영하고 한 번 재시작했다. 공개 방송 주소는 https://www.youtube.com/watch?v=eI-5vSy5mjY 그대로다. 서버에는 git이 없으므로 필요한 파일만 업로드하고 SHA256을 대조했다. 승인된 다리 없는 까몽의 기존 DJ 차림/HTML은 유지했고, 다른 작업의 의상·털 고정 후보는 배포하지 않았다.

## 배경 조정

- 달의 원형 윤곽과 위치는 보존하고 안쪽 재질의 느린 회전을 강화했다.
- 양쪽 긴 구름의 움직이는 영역을 넓혔다. 구름 변위가 달 가장자리를 끌어당기는 중간 후보 결함은 제거한 뒤 최종판을 다시 렌더했다.
- 감나무 윗가지는 바람에 흔들리고, 몸통·아래 가지·기와는 고정했다.
- 네 창의 밝기가 다른 박자로 부드럽게 변한다. 점멸/완전 소등/카메라 이동은 없다.
- `synk-design`·`emil-design-eng` 기준은 재질·구도 보존, 실제 시청 크기에서 보이는 운동, 장면 전체 고정에 적용했다. 일반 홍보 영상식 확대·효과음은 차분한 라디오 요청에 적용하지 않았다.
- 최종 60초/30fps/1280×720, 원본 렌더 0초=60초 픽셀 차이 0. 최종 MP4 SHA256: `c0e984f2ce771cabd1704c5ac25335c1b05519731566de7dfa98cf6622827df7`.

## 소리와 실제 송출 장애를 분리해서 수정

1. 기존 AAC 파일의 반복 경계에서 약 25~27ms 무음이 재현됐다. 이전 200ms 검사로는 놓쳤다. 11곡의 기존 PCM 혼합을 그대로 24bit FLAC으로 옮기고 방송 출력에서 연속 인코딩해 매 바퀴 AAC 패딩이 다시 붙지 않도록 했다. 곡 순서·6초 겹침·음원 원본은 바꾸지 않았다. 새 곡 `오늘 밤 제일 환한 사람`과 `The Phantom's Lament`를 포함한 11곡/28분21초 반복 구성이다.
2. 18:29~18:32 KST에 송출 시계 정체 뒤 Broken pipe로 재연결된 실제 장애도 있었다. 반복 경계의 작은 틈만으로 설명할 수 없다. 네트워크 쓰기 15초 기한, 기존 감시의 10초 점검/30초 무진전 판정으로 복구 대기를 줄였다. 초기 60초는 기다리고, 느려도 시간이 진행 중이면 재시작하지 않는다. 새로운 서비스나 감시 데몬은 만들지 않았다.
3. 진행 기록은 줄 단위로 모아 읽고, 방송 주소가 조각으로 잘려도 로그로 새지 않게 했다. 실제 최초 연결 정체 원인이 네트워크/상대 수신/FFmpeg 내부 중 무엇인지는 소급 확정하지 않는다. 복구 개선은 무중단 보장이 아니다.

## 확인 증거와 한계

- 관련 자동시험 20/20, 수정 JavaScript 구문 및 diff 공백 검사 통과.
- 로컬 새 소리 3바퀴(약 85분 분량) 전체: 219,794 AAC 패킷/디코딩 프레임, 큰 시각 공백·중복 0, −45dB/5ms 이상 무음 0. 서버에서 85분 장애 주입/전체 부하 시험을 했다는 뜻은 아니다.
- 반영 후 동일 PID 140581, 추가 재시작 0, 실제 출력 시간 지속 전진, 버린/겹친 프레임·캡처 실패 0. 시간별 최종 증거는 `실방송확인.json`.
- 19:57 KST 공식 YouTube 계정 API: 입력 active, health good, 공개 방송 live.
- 실제 YouTube 수신본 `유튜브수신_수정후.mp4`: 120.000726초, 1280×720, SHA256 `8024fe5f22a77d34327db21f4e1c8012159871f6f48128e3b4dd496142d8aad9`. 로컬 미리보기가 아니다.
- 실제 수신 오디오 패킷/디코딩 프레임 각 5,168개: 1.1ms 이상 시각 공백·중복 0, 디코딩 경고 0, −45dB/3ms 이상 무음 0. 이것은 수신한 2분의 신호 분석이며 사람이 청취한 평가나 장시간 무중단 보장은 아니다.
- 실제 수신 영상 0→3초: 달 4.54px, 왼 구름 10.85px, 윗가지 9.78px 특징점 이동; 같은 구간 지붕 0px. 20~110초에도 움직임 지속. 압축 잡음만으로 운동을 판정하지 않았다.
- 수신 영상 61.0·61.2초 눈뜸 → 61.4~61.8초 눈감음 → 62.0초 눈뜸, 81.2초 눈웃음 등을 확인했다. 원래 표정 시스템이 실제 방송에 들어온다.
- 실제 수신본 50초부터 약 30초를 재압축 없이 추출한 확인 영상: `실제방송_30초.mp4`.

## 재현·복구 자료

- 무대 생성: `tools/라디오자연무대.py`, 최종 자산 `docs/라디오/무대영상/추석_자연생동_20260909_v2.mp4`.
- 소리 원인·3바퀴 검증: `output/radio-audio-20260909/recheck/findings.md`, `gapless-continuous-report.json`, `tools/라디오반복소리검사_20260909.cjs`. FLAC은 원래 `loop-pcm.wav`를 `ffmpeg -i loop-pcm.wav -c:a flac -sample_fmt s32 -bits_per_raw_sample 24 gapless.flac`으로 보관한 파일이며 디코딩 PCM 해시가 일치했다. 302MB 음원은 git에 중복 적재하지 않았다.
- 좁은 반영 도구: `tools/라디오생동반영_20260909_v2.cjs`; 원격 백업 네 경로와 반영 해시는 `라이브반영.json`.
- 실제 방송 수신 도구: `tools/라디오수신검수.cjs`; 서명 재생 URL은 저장·출력하지 않는다.
- 방송 출력 정체의 과거 근거와 로컬 무응답 시험은 `../재확인/stream-findings.md`, `stream-timeout-proof.json`에 있으며 같은 서버에서 장애를 일부러 일으키지는 않았다.
