# 작업 대기열 — AI가 다음에 집을 것

> **상태**: ✅**확정** · 2026-08-09 — 자율 진행 모드의 실행 순서 정본(메모리 `autonomous-build-mode`).
>
> **왜 이 파일인가**: 유호님이 새 세션에 「이어서 해」만 입력해도 굵직한 것부터 지어지게 하려면, **다음에 무엇을 집을지가 한 곳에 있어야 한다.** 인계문은 「직전에 뭘 했나」, 보드는 「지금 누가 뭘 잡았나」, 결정큐는 「유호님이 답할 것」이라 셋 다 이 자리를 못 채운다.
>
> **쓰는 법(세션)**: ①맨 위부터 훑어 **아직 아무도 안 잡은 것**을 고른다(`node tools/board.js` 로 대조) ②내 보드 파일에 트랙 선언 ③끝나면 여기서 그 줄을 지우고 커밋. **남의 보드 줄과 겹치면 다음 것으로 내려간다.**
>
> **고치는 법**: 새 일감을 발견하면 근거와 함께 해당 층에 **추가**한다(자율 모드 — 유호님께 안 묻는다). 순서를 크게 뒤집는 것은 유호님 판정.
>
> 🚫 유호님이 답해야 하는 것을 여기 넣지 않는다(그건 `node tools/decision-queue.js`) · 🚫 완료한 줄을 ✅로 남겨 두지 않는다(지운다 — 이력은 git).

## 층 — 위가 먼저다

**P0 학생이 앱을 실제로 쓰기까지 막는 것** → 개원 직결([[build-for-opening-now]])
**P1 수집→엔진 도달** → 평생 계획의 심장이고 지금 물리 도달 0([[collection-engine-automation-mandate]])
**P2 재미·쉬움·신선함** → 유호 품질 기준(「안 쓰이고 재미없고 어려우면 이 시대엔 가치 없다」)
**P3 도구·하네스** → 위 셋을 더 빨리·안전하게 만드는 것만

---

## P0 — 개원을 막는 것

- [ ] **운영 `engine.learners` 적재 0** — 통로는 섰지만 운영 명부는 여전히 비어 있다(`local_4cd29e93` 실측 · 리허설 156행이라 왕복시험은 내내 초록이었다). ⚠ **선행 = 유호님의 실제 명부 한 장**(학생번호·전화) — 개원 전엔 채울 재료가 없다. 근거=[[l0-auth-code]]

## P1 — 수집이 엔진에 닿는 자리

- [ ] **AI 교정 생산자 — 코드는 섰고 「키·배포·cron」만 남았다**(⑥ 의 **선행** · `local_f0fb6e57` 08-09). 원래 진단: 큐 뷰가 `submission.created` **+ AI 교정 있음**을 둘 다 요구하는데 저장소 전체에 `engine.corrections` **INSERT 가 0줄**이라 큐가 구조적으로 0행이었다(리허설 실측 제출 704 · AI 교정 462 · 큐 0 — 462는 전부 `task.assigned` 에 붙은 것으로 `deliver:269` 설계지 결함이 아니다).<br>✅ **지었다** — talk `6379aa2`: `lib/교정엔진.js`(순수) + `functions/correct`(service_role 배치) + 회귀 25 · **변이 6/6** · 780/780. 동의는 `engine.consents` 를 직접 본다(`pipeline_jobs.status='revoked'` 는 값목록만 있고 **writer 0** 이라 거짓 가드다).<br>▶ **남은 셋**(08-09 `local_4f05894e` 실측 갱신): ①**`ANTHROPIC_API_KEY` 미설정 = 유호님 몫**(돈 · 없으면 배치가 행을 안 건드리고 `no_api_key` 만 낸다) ②✅**리허설 배포 완료** — `correct v1 ACTIVE`(`원격배포 --목록` 실측 · 판 대조 통과 `저장소 c10 · DB 20260809060000` · 호출 시 `401 service_role 만 부를 수 있습니다` 로 인증 게이트도 실측). ⛔**운영 배포는 0** — 라이브라 유호님 승인 자리이고, 로컬 `.env` 가 리허설 REF 를 물어 `--운영` 을 붙여도 대상이 안 바뀐다. ③`pg_cron` 잡 = **지금 부을 자리가 없다**: 리허설은 `20260809070000_cron_c10.sql` 머리말이 ⛔ 로 금지(스케줄러가 돌면 **옆 세션 왕복시험의 배정 상태를 흔든다**) · 운영은 ②·①이 선행. 🔑 **키 오는 날 운영 배포와 한 벌로 짓는다** — 지금 마이그레이션 파일만 미리 지으면 `base_version` 이 남의 새 조각에 밀려 낡고, 그 조각은 부을 때 `raise` 로 죽는다. 🚫 「큐가 비었으니 뷰를 넓히자」(AI 교정 없는 행을 검수자에게 내리면 검수가 아니라 채점이다) · 🚫 프롬프트·값목록을 코드에 베끼기(동봉으로 온다)
- [ ] **⑥ 검수 콘솔 — 남은 것은 §5(승인·폐기)와 화면이다.** 착수 순서 정본 = talk `docs/검수_내부계약.md` §6.<br>✅ **1 판 22열**(talk `a5fb940`) · ✅ **2 `review` 함수 + §3 큐 읽기 + §4 오디오 서명**(talk `ad17aef` · 회귀 28 · ⛔**배포 0 · 실왕복 0**). 「4구멍」은 능력 목록이지 배포 단위가 아니라 함수 **하나 + 하위 경로**로 섰다(조회 감사는 엔드포인트가 아니라 ①②의 부수 효과).<br>✅ **3 c11 물리 4자리 — 섰다**(talk `a1b45b3` · 847/847 · 변이 2/2 · 판은 **c10 유지**). 🔑 **이름 둘이 위 목록과 다르다** — 채택된 정본은 `corrections.supersedes` · **`promotion_intent`** · **`transcript_at_review`** · `pipeline_jobs.discard_reason` 다. `_verified_` 를 뺀 것이 판정이다: 검수자가 판정하는 시점에 화면에 있는 것은 보통 **기계 전사**이고 `transcript_verified` 는 그 판정의 **산출물**이라, 이름에 `verified` 를 넣으면 행마다 거짓을 적게 된다(사유 = 그 조각 머리말).<br>✅ **4 착수 순서가 전부 닫혔다** — §5 승인·폐기(talk `0a38e58` · 911/911 · 변이 12/12) + Expo 검수 화면(talk `7e51fcd` · 960/960 · 변이 22/22). ▶ **남은 것은 코드가 아니라 배포다**: `review` 함수가 리허설에 **미배포**(`배포대조` HTTP 404 실측 08-09 · `local_ddd51fde`) · 실왕복 0 · c11 조각(`20260809090000`) 어느 DB 에도 미적용.<br>🔴 배포 순서가 위 P3 와 **한 매듭**이다 — c11 조각의 체인도 `20260809070000_cron_c10` 에서 끊긴다. 그 하나가 풀리면 ⑥ 배포·실왕복이 같이 열린다.<br>🔴 실 PG 검증층 = CI `supabase` job 인데 Actions 결제 차단으로 안 돈다 — 대신 `tools/원격SQL.js`(읽기)가 그 자리를 진다.<br>🚫 `dataset_item(group_id)` 는 **일부러 안 냈다**(발주서가 넷째로 적었지만 새 표 = ㉮ 가 미룬 C 계층 그 자체 · `엔진도달_설계.md` §6 표 3행 · 멤버십은 소급 불가가 아니다). c11 이 낼 때 `(dataset_ver, group_id) → 목적` 을 **PK 로** 잡아 훈련셋⟂평가셋 혼입을 표현 불가능하게 할 것 — 트리거는 꺼진다.<br>🚫 열 없이 §5 요청/응답 지어내기(구현 불가능한 채로 정본이 된다) · 🚫 `event_id` 를 검수 화면에 주기 · 🚫 함수 4개로 쪼개기·`action` 파라미터. 근거=[[reviewer-console-ux]]
- [ ] **`policy_ver` 가 행에 안 실린다 — 원인 확정·처방 실행, 실증 1회만 남았다**(`local_d73330a1` 08-09). 계기판 실측이 개입 **281건 전부 미기록**을 드러냈고, 원인은 **리허설 `deliver` 가 옛 판**이었다(v10 = 08-08T23:56Z 배포 · 생산자 커밋 talk `af750e7` = 08-09T07:32Z). 🔑 **코드가 선 것과 행에 값이 있는 것은 다르다** — 그 차이는 계기판을 돌리기 전까지 아무 데서도 안 빨갰다. ✅ **v11 재배포 완료**(리허설 · 판 대조 통과) — 다음 배달부터 값이 실린다.<br>▶ 남은 것 = **실증**: 새 배달 1회를 만들어 행에 값이 찍히는지 본다. 지금은 아래 「배포 뒤처짐」이 `배달왕복시험` 을 막아 못 한다(`원격배포 --호출` 은 anon 이라 배치 권한이 없다 — 설계대로다). 근거=[[policy-ver-producer]]
- [ ] **배달 281건 중 「눈이 닿았다」가 1건뿐**(같은 실측). `content.viewed` 생산자가 화면 하나(`lib/오늘과제.js`)뿐이라 그 화면을 안 거친 배달은 원리상 관측 0이고, 그래서 지금은 **네트워크 실패와 「학생이 안 봤다」가 같은 모양**이다 — 계약 c9 가 이 사건을 만든 이유가 정확히 그것이다. 개입 열람 자리를 앱 화면에 세우면 그 구분이 되살아난다.<br>🚫 관측이 0이라고 뷰·분모를 손보기(미배선을 「무시했다」로 바꿔 적는 짓이다) · 근거=[[learner-state-consumer]]

## P2 — 재미·쉬움·신선함

- [ ] **미니게임 4모듈 착수** — ⚠**선행 = 유호님 판정 2건**(게이트 AND/OR · G4 원안). 그 답이 오면 즉시 P0급으로 올린다. 착수 시 품질 기준 = BGM·효과음·마이크로인터랙션까지(유호 지시 08-09 · 「띡 던져놓기」 금지). 근거=[[minigame-spec-4modules]]<br>⚠ **`quiz.answered` 생산자를 세우면 도달을 같이 내야 선다**(래칫이 이제 2 — 3번째부터 막는다). ✅ 08-09 부터 그게 **가능하다**: 소비자 `lib/학습자상태.js` 가 실재하므로 그 파일에 **③자기인식 축**(확신도 vs 정답률 격차)을 더하고 `쓰는사건` 에 이 사건을 넣으면 도달이 선다. 전에는 적을 소비자가 없어 「적는 것 자체가 불가능」했다(F103). 같은 통로가 `choice.selected`(⑤관심·성향 축의 입구)에도 그대로 열렸다.
- [ ] **강6 관찰태그 자동화** — 앱 UI 대개편에서 유일하게 남은 신규 항목. 근거=[[app-ui-style-directive]] 🔴

## P3 — 도구·하네스

- [ ] 🔴 **리허설 배포 뒤처짐 — 7종 중 6종 재배포 완료, 남은 하나가 왕복시험 전체를 잡고 있다**(`local_ddd51fde` 08-09). ✅ `uploads v10`·`transcribe v3`·`corrections v8`·`progress v11`·`tasks v10`·`correct v3` 재배포(판 대조 전건 통과) → `배포대조` **다름 7 → 1**.<br>🔴 남은 하나 = **`events`**, 그리고 그것이 `배달·인증·교정왕복시험` **전부**를 막는다(왕복시험은 함수 하나가 아니라 배포대조 **전체**를 게이트로 삼는다 — 6종을 고쳐도 0종이 돈다).<br>🔑 **막는 것은 배포가 아니라 열이다** — `functions/events` 의 INSERT 열 목록에 `request_hash` 가 들어 있는데(`index.ts:431`) 리허설 DB 에 그 열이 **없다**(`information_schema` 실측 0). 지금 밀면 **모든 사건 POST 가 500** 이다.<br>🔴 **그 열을 다는 조각(`20260809080000`)의 `base_version` 이 `20260809070000_cron_c10`** 인데, 그 조각은 머리말이 「⛔ 리허설엔 일부러 안 건다」로 막아 둔 것이라 **체인이 거기서 끊겨 있다**(리허설 최신판 `20260809060000` 실측). 즉 **한 정책 줄이 이 저장소의 주 검증층 전체를 잡고 있다.**<br>⏳ **유호님 승인 자리** — `--적용` 은 비가역이라 `원격SQL.js` 머리말이 명시 승인을 요구한다. 권고안·리스크·되돌림은 [[reviewer-console-ux]] ⏳ 줄. 🚫 `--판무시` 로 뚫기(막는 방향이 안전한 방향이다) · 🚫 `080000` 의 `base_version` 손보기(운영엔 이미 그 체크섬으로 부어져 있다)

- [ ] **역방향 회귀를 `engine.corrections` 까지 넓힌다** — 08-09 에 선 talk `tests/계약역방향.test.js` 는 `learning_events`·`submissions` **두 표만** 잰다(그 파일 `대상표` 한 줄). 계약은 `corrected_text`·`error_tags`·`model`·`prompt_ver` 같은 교정 칸도 이름으로 들고 있는데 그 표에 저장하는 통로(`functions/correct`·`deliver`)는 검사 밖이라, **같은 병이 그 표에서 다시 자란다**. 넓히면 새 열마다 계약 등재 xor `물리칸` 장부 사유가 필요해진다. 🔴AI 몫

---

## 지금 막혀 있어 여기 못 올리는 것 (유호님 답이 열쇠)

`node tools/decision-queue.js` 가 정본. 그중 **대기열을 실제로 막고 있는 것**만: 미니게임 게이트 AND/OR · C0 동결 범위 · Expo 결제(실기기 빌드) · 몽골어 검수자 1인 · **LAB철학 판정 9건**(`docs/LAB철학.md` §6 — 철학이 확정돼야 P2 콘텐츠·미니게임 서사가 그 위에 선다).
