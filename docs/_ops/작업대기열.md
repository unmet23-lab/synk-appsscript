# 작업 대기열 — AI가 다음에 집을 것

> **상태**: ✅**확정** · 2026-08-09 — 자율 진행 모드의 실행 순서 정본(메모리 `autonomous-build-mode`).
>
> **왜 이 파일인가**: 유호님이 새 세션에 「이어서 해」만 입력해도 굵직한 것부터 지어지게 하려면, **다음에 무엇을 집을지가 한 곳에 있어야 한다.** 인계문은 「직전에 뭘 했나」, 보드는 「지금 누가 뭘 잡았나」, 결정큐는 「유호님이 답할 것」이라 셋 다 이 자리를 못 채운다.
>
> **쓰는 법(세션)**: ①맨 위부터 훑어 **아직 아무도 안 잡은 것**을 고른다(`node tools/board.js` 로 대조) ②내 보드 파일에 트랙 선언 ③끝나면 여기서 그 줄을 지우고 커밋. **남의 보드 줄과 겹치면 다음 것으로 내려간다.**
>
> **고치는 법**: 새 일감을 발견하면 근거와 함께 해당 층에 **추가**한다(자율 모드 — 유호님께 안 묻는다). 순서를 크게 뒤집는 것은 유호님 판정.
>
> 🚫 유호님이 답해야 하는 것을 여기 넣지 않는다(그건 `node tools/decision-queue.js`) · 🚫 완료한 줄을 ✅로 남겨 두지 않는다(지운다 — 이력은 git).

## 층 — 위가 먼저다

**P0 학생이 앱을 실제로 쓰기까지 막는 것** → 개원 직결([[build-for-opening-now]])
**P1 수집→엔진 도달** → 평생 계획의 심장이고 지금 물리 도달 0([[collection-engine-automation-mandate]])
**P2 재미·쉬움·신선함** → 유호 품질 기준(「안 쓰이고 재미없고 어려우면 이 시대엔 가치 없다」)
**P3 도구·하네스** → 위 셋을 더 빨리·안전하게 만드는 것만

---

## P0 — 개원을 막는 것

- [ ] **운영 `engine.learners` 적재 0** — 통로는 섰지만 운영 명부는 여전히 비어 있다(`local_4cd29e93` 실측 · 리허설 156행이라 왕복시험은 내내 초록이었다). ⚠ **선행 = 유호님의 실제 명부 한 장**(학생번호·전화) — 개원 전엔 채울 재료가 없다. 근거=[[l0-auth-code]]

## P1 — 수집이 엔진에 닿는 자리

- [ ] **AI 교정 생산자 — 코드는 섰고 「키·배포·cron」만 남았다**(⑥ 의 **선행** · `local_f0fb6e57` 08-09). 원래 진단: 큐 뷰가 `submission.created` **+ AI 교정 있음**을 둘 다 요구하는데 저장소 전체에 `engine.corrections` **INSERT 가 0줄**이라 큐가 구조적으로 0행이었다(리허설 실측 제출 704 · AI 교정 462 · 큐 0 — 462는 전부 `task.assigned` 에 붙은 것으로 `deliver:269` 설계지 결함이 아니다).<br>✅ **지었다** — talk `6379aa2`: `lib/교정엔진.js`(순수) + `functions/correct`(service_role 배치) + 회귀 25 · **변이 6/6** · 780/780. 동의는 `engine.consents` 를 직접 본다(`pipeline_jobs.status='revoked'` 는 값목록만 있고 **writer 0** 이라 거짓 가드다).<br>▶ **남은 셋**(08-09 `local_4f05894e` 실측 갱신): ①**`ANTHROPIC_API_KEY` 미설정 = 유호님 몫**(돈 · 없으면 배치가 행을 안 건드리고 `no_api_key` 만 낸다) ②✅**리허설 배포 완료** — `correct v1 ACTIVE`(`원격배포 --목록` 실측 · 판 대조 통과 `저장소 c10 · DB 20260809060000` · 호출 시 `401 service_role 만 부를 수 있습니다` 로 인증 게이트도 실측). ⛔**운영 배포는 0** — 라이브라 유호님 승인 자리이고, 로컬 `.env` 가 리허설 REF 를 물어 `--운영` 을 붙여도 대상이 안 바뀐다. ③`pg_cron` 잡 = **지금 부을 자리가 없다**: 리허설은 `20260809070000_cron_c10.sql` 머리말이 ⛔ 로 금지(스케줄러가 돌면 **옆 세션 왕복시험의 배정 상태를 흔든다**) · 운영은 ②·①이 선행. 🔑 **키 오는 날 운영 배포와 한 벌로 짓는다** — 지금 마이그레이션 파일만 미리 지으면 `base_version` 이 남의 새 조각에 밀려 낡고, 그 조각은 부을 때 `raise` 로 죽는다. 🚫 「큐가 비었으니 뷰를 넓히자」(AI 교정 없는 행을 검수자에게 내리면 검수가 아니라 채점이다) · 🚫 프롬프트·값목록을 코드에 베끼기(동봉으로 온다)
- [ ] **⑥ 검수 콘솔 — 착수 순서 넷 + 배포 + 실왕복이 전부 닫혔다. 남은 것은 미측정 상수 둘과 운영 배포다.** 착수 순서 정본 = talk `docs/검수_내부계약.md` §6.<br>✅ **1 판 22열**(talk `a5fb940`) · ✅ **2 `review` 함수 + §3 큐 읽기 + §4 오디오 서명**(talk `ad17aef` · 회귀 28 · ⛔**배포 0 · 실왕복 0**). 「4구멍」은 능력 목록이지 배포 단위가 아니라 함수 **하나 + 하위 경로**로 섰다(조회 감사는 엔드포인트가 아니라 ①②의 부수 효과).<br>✅ **3 c11 물리 4자리 — 섰다**(talk `a1b45b3` · 847/847 · 변이 2/2 · 판은 **c10 유지**). 🔑 **이름 둘이 위 목록과 다르다** — 채택된 정본은 `corrections.supersedes` · **`promotion_intent`** · **`transcript_at_review`** · `pipeline_jobs.discard_reason` 다. `_verified_` 를 뺀 것이 판정이다: 검수자가 판정하는 시점에 화면에 있는 것은 보통 **기계 전사**이고 `transcript_verified` 는 그 판정의 **산출물**이라, 이름에 `verified` 를 넣으면 행마다 거짓을 적게 된다(사유 = 그 조각 머리말).<br>✅ **4 착수 순서가 전부 닫혔다** — §5 승인·폐기(talk `0a38e58` · 911/911 · 변이 12/12) + Expo 검수 화면(talk `7e51fcd` · 960/960 · 변이 22/22).<br>✅ **실왕복이 섰다 — `tools/검수왕복시험.js` 63/63 · 2회차도 63/63**(talk `dd849bb` · 08-09 `local_741d0fb6`). 배포·조각 매듭이 P3 에서 풀린 뒤 남아 있던 마지막 칸이다. 🔑 **「배포됨」과 「돈다」 사이가 통째로 비어 있었다** — `staff_access_log` 의 action 이 `learner.*` 둘뿐이라 `review.*` 넷이 전부 0행이었고(실측), 그 구간의 결함은 증상이 없다. 잰 것: ⓪물리 · ①문 10(anon·비직원·`teacher`·해임 전부 403) · ②§3 큐 11(keyset 이음 · `event_id` 미노출 · 0건도 감사 1행) · ③§4 5(**그 주소로 파일이 실제로 내려온다**) · ④승인 게이트 6 · ⑤원자성 넷 11 · ⑥`Z` 5(409 계보 충돌) · ⑦폐기 7(어휘를 DB CHECK 에서 읽어 안내문 대조). 사후 실측: `pipeline_jobs.status` 가 처음으로 `pending` 밖으로 나갔다(verified 2 · discarded 2). 🚫 큐 픽스처를 새로 짓기(없는 것은 AI 교정 한 칸뿐이라 그것만 심는다) · 🚫 서명 응답 칸을 `url ?? signed_url` 로 느슨히 받기(계약이 갈리는 날 시험이 초록으로 덮는다) · 🚫 절대 개수로 재기(감사·교정이 append-only 라 2회차부터 영원히 빨갛다).<br>✅ **게이트 ①의 재료가 섰다** — `submissions.stt_segments` 생산자(talk `8bb6f35` · 회귀 20 · 변이 12/12 · `local_99032bdf`). 소비자 셋(구간 칩 · `청취문턱` · `세그먼트펴기`)이 다 선 채로 **쓰는 코드가 0줄**이라 문턱이 언제나 하한 3초였다. 🔑 재료가 없어서가 아니었다 — `transcribe` 가 이미 `verbose_json` 을 요청해 세그먼트를 **받고 있었고** `전사값()` 이 버렸다. ⚠ `검수의뢰_엔진수집설계` §228 모양엔 `confidence` 가 없는데 `세그먼트펴기` 는 그 키**만** 읽는다 — 문서대로 저장했으면 생산자가 서도 저신뢰가 원리상 0이고 증상은 「초록인데 아무것도 안 걸린다」뿐이었다(상위집합으로 냈다). ✅ **배포됐다**(08-09 실측 · 판 대조 다름 0). 🔴 **그런데 아직 한 행도 안 만들어졌다** — `submissions` 중 `stt_segments` 가 있는 행이 **0**이다(08-09 `local_741d0fb6` 실측). 코드가 선 것과 행에 값이 있는 것은 다르고, 실왕복에서도 `listen_gate.measured` 가 **false** 로 나왔다(그 미측정을 응답이 드러내도록 설계돼 있다). ▶ **다음 = 새 전사를 한 번 태워 세그먼트 행을 만들고**, 그 위에서 미측정 상수 둘을 잰다 — `저신뢰문턱 0.7` 경계값 · 세그먼트 시간 단위(`lib/검수확정.js` 가 지목). 그전까지 문턱은 언제나 하한 3초다.<br>✅ **재검수 청취 게이트가 닫혔다 — ㉮ 구현**(08-10 `local_50528b03` · talk `19e27f2` · 실왕복 **71/71**). 실왕복이 냈던 구멍(612초 된 옛 서명 기록으로 재검수 200)과 프리로드 누수를 **한 처방**으로 닫았다: 감사 종류를 `review.audio`(발급) ↔ **`review.audio.played`**(재생)로 가르고 게이트 ②를 `max(at)` 갈래 판정으로 바꿨다(첫 확정=시간 무관 · 재검수=`마지막청취 > supersedes.created_at`). 마이그 0(`action` 은 CHECK 없는 text). 🔑 **10분 창이 시간 상수 0으로 성립한다** — 확정분엔 새 서명이 안 나가니 창을 만드는 것은 서버 상수가 아니라 **서명 수명이라는 물리**다. 🚫 게이트에 시간 리터럴 박기 · 🚫 §4 가 확정분에 서명 내주게 하기(㉯) · 🚫 발급을 다시 게이트 증거로.<br>🔴 실 PG 검증층 = CI `supabase` job 인데 Actions 결제 차단으로 안 돈다 — 대신 `tools/원격SQL.js`(읽기)가 그 자리를 진다.<br>🚫 `dataset_item(group_id)` 는 **일부러 안 냈다**(발주서가 넷째로 적었지만 새 표 = ㉮ 가 미룬 C 계층 그 자체 · `엔진도달_설계.md` §6 표 3행 · 멤버십은 소급 불가가 아니다). c11 이 낼 때 `(dataset_ver, group_id) → 목적` 을 **PK 로** 잡아 훈련셋⟂평가셋 혼입을 표현 불가능하게 할 것 — 트리거는 꺼진다.<br>🚫 열 없이 §5 요청/응답 지어내기(구현 불가능한 채로 정본이 된다) · 🚫 `event_id` 를 검수 화면에 주기 · 🚫 함수 4개로 쪼개기·`action` 파라미터. 근거=[[reviewer-console-ux]]
- [ ] **`policy_ver` 가 행에 안 실린다 — 원인 확정·처방 실행, 실증 1회만 남았다**(`local_d73330a1` 08-09). 계기판 실측이 개입 **281건 전부 미기록**을 드러냈고, 원인은 **리허설 `deliver` 가 옛 판**이었다(v10 = 08-08T23:56Z 배포 · 생산자 커밋 talk `af750e7` = 08-09T07:32Z). 🔑 **코드가 선 것과 행에 값이 있는 것은 다르다** — 그 차이는 계기판을 돌리기 전까지 아무 데서도 안 빨갰다. ✅ **v11 재배포 완료**(리허설 · 판 대조 통과) — 다음 배달부터 값이 실린다.<br>✅ **실증 끝 — 이 줄은 닫혔다**(08-09 `local_33c711a0` · 배포대조가 0 이 된 직후 `배달왕복시험` 을 돌려 새 배달을 만들고 `원격SQL`(읽기)로 쟀다): 개입 **291건 중 판기록 10** · 그 **10건이 전부 최근 2시간**(= 오늘 만든 배달)이고 판 이름은 `오늘과제.v1` · 옛 **281건은 그대로 null**. 소급이 없는 것이 설계다(그때 무슨 규칙이 골랐는지 지금 알 길이 없고, 규칙을 소급 적용하는 것은 복원이 아니라 날조다). 🔑 이 줄이 남긴 교훈은 그대로 유효하다 — **코드가 선 것 ≠ 행에 값이 있는 것**이고, 그 차이는 계기판을 돌리기 전까지 아무 데서도 안 빨갰다. 근거=[[policy-ver-producer]]
- [ ] **배달 281건 중 「눈이 닿았다」가 1건뿐**(같은 실측). `content.viewed` 생산자가 화면 하나(`lib/오늘과제.js`)뿐이라 그 화면을 안 거친 배달은 원리상 관측 0이고, 그래서 지금은 **네트워크 실패와 「학생이 안 봤다」가 같은 모양**이다 — 계약 c9 가 이 사건을 만든 이유가 정확히 그것이다. 개입 열람 자리를 앱 화면에 세우면 그 구분이 되살아난다.<br>⚠ **실측 갱신 08-09**(`local_99032bdf` · 이 줄을 집기 전에 대조한 결과): **생산자는 죽어 있지 않다.** `src/말하기화면.js:311 열람알리기` 가 `expo-speech` **낭독 완료**에서 실제로 사건을 낸다(둘째 생산자 = 되듣기 · 부모는 제출 사건). 즉 1/281 은 「미배선」이 아니라 **리허설엔 그 화면을 지나간 사람이 없었다**가 더 큰 몫이다. 🔴 그리고 위 처방(「화면에 세운다」)은 **이미 기각된 판정과 충돌한다** — `lib/오늘과제.js:384` 가 *"화면에 글자가 떠 있는 것은 관측이 아니다 … 낭독이 끝까지 간 것만 센다"* 로 못박았다. 화면 표시로 사건을 내면 「들었다」가 「띄웠다」로 뜻을 갈아탄다. ✅ **재서 다시 썼다 — 이 줄은 「지어라」가 아니라 「지금은 잴 수 없다」로 닫는다**(08-09 `local_bfe0a73d` · 리허설 읽기 SQL 5벌).<br>①**짝은 붙는다 — 실측**: 그 1건의 주인은 **SYNK-999(유호님 실기기)**이고 `parent_event_id` 가 `intervention.delivered` 로 정확히 해석된다(20:13 배달 → **20:45:39 열람** → 20:46 제출 · 08-08 실기기 관통). 배선 결함이 아니다.<br>②**분자와 분모가 다른 층을 센다**: 분모 294 = 야간 배치가 **픽스처 학생**에게 만든 배달(배달 받은 118명 중 100명이 `tms*` 생성 코드)이고, 분자는 **앱 세션에서만** 나온다 — 리허설 제출 **735건 중 앱 기기에서 온 것은 2건**(`submissions.capture_meta.platform='android'` · 둘 다 SYNK-999 · 나머지 = `node-rehearsal` 7 + capture_meta 없는 옛 픽스처 726). 즉 **1/281 은 「리허설에 사람이 한 명」을 잰 값**이지 배선을 잰 값이 아니다.<br>③**낭독을 안 거치는 «정당한» 열람 경로가 셋이고 전부 구조적 관측 0**: ⓐ`말하기화면.js:456 onError` — TTS 실패·한국어 음성 없는 기기는 `들었다=true` 로 흐름만 열고 사건은 안 낸다(웹 미리보기는 **항상** 이 경로) ⓑ`:476 「듣지 않고 넘어가기」` — ♪ 를 안 눌러도 본문은 화면에 있고 학생은 그날 과제를 끝낸다 ⓒ`오늘과제.js:308` 폴백·고정 과제 — `intervention_event_id: null` 이라 `열람사건()` 이 null(원리상 불가). ⓓ 둘째 생산자 **되듣기는 0행** — 한 번도 안 뛰었다.<br>🔑 **화면을 더 세워도 안 풀린다** — ⓐⓑ 는 「낭독 완주만 센다」(`오늘과제.js:384`)는 **살아 있는 판정**이 막고 ⓒ 는 가리킬 부모가 없다.<br>🔴 **남는 진짜 구멍 하나**: c9 가 만들어진 이유였던 *「네트워크 실패」와 「학생이 안 봤다」를 가른다*가 **지금 안 된다** — ⓐⓑ 가 「안 봤다」와 같은 모양이라서다. 가르려면 «기기에 닿았다»는 별도 관측이 필요한데 그건 서버가 `/v1/tasks` 를 내줄 때 **이미 아는 사실**이고, 🚫 새 `event_type` 은 기각돼 있으므로 **계약 판정 자리**지 이 줄이 혼자 지을 것이 아니다.<br>▶ **다시 열릴 조건 = 실학생 앱 세션이 생기는 날(개원).** 그전에 이 비율을 움직이는 것은 전부 분모 조작이다.<br>▶ **도전안**: 통념은 「관측이 1건뿐이니 더 모으자」인데, 정작 «배달이 닿았나»의 실용적 답은 **제출**이다 — 제출한 학생에게 그날 배달이 닿은 것은 확실하다. 그러면 `content.viewed` 가 혼자 답하는 질문은 **「제출까지 안 간 학생」**뿐이고 그건 개원 후 **이탈 진단**용이다. 지금 지을 것이 없다는 결론이 그쪽에서도 같게 나온다.<br>🚫 관측이 0이라고 뷰·분모를 손보기(미배선을 「무시했다」로 바꿔 적는 짓이다) · 🚫 화면 표시로 `content.viewed` 내기 · 🚫 `onError` 에 열람 붙이기 · 🚫 「개입 열람 화면을 세운다」 재제안(ⓐⓑⓒ 어느 것도 화면으로 안 풀린다) · 근거=[[learner-state-consumer]]

## P2 — 재미·쉬움·신선함

- [ ] **미니게임 4모듈 착수** — ⚠**선행 = 유호님 판정 2건**(게이트 AND/OR · G4 원안). 그 답이 오면 즉시 P0급으로 올린다. 착수 시 품질 기준 = BGM·효과음·마이크로인터랙션까지(유호 지시 08-09 · 「띡 던져놓기」 금지). 근거=[[minigame-spec-4modules]]<br>⚠ **`quiz.answered` 생산자를 세우면 도달을 같이 내야 선다**(래칫이 이제 2 — 3번째부터 막는다). ✅ 08-09 부터 그게 **가능하다**: 소비자 `lib/학습자상태.js` 가 실재하므로 그 파일에 **③자기인식 축**(확신도 vs 정답률 격차)을 더하고 `쓰는사건` 에 이 사건을 넣으면 도달이 선다. 전에는 적을 소비자가 없어 「적는 것 자체가 불가능」했다(F103). 같은 통로가 `choice.selected`(⑤관심·성향 축의 입구)에도 그대로 열렸다.
- [ ] **강6 관찰태그 자동화** — 앱 UI 대개편에서 유일하게 남은 신규 항목. 근거=[[app-ui-style-directive]] 🔴
- [ ] **낱말 교체가 코드층에 안 닿았다 — 학부모 카드의 「왕관/Титэм」**(08-09 `local_734d4614` 가 배포 스윕에서 발견 · v9.200 이 슬로건만 상환하고 남긴 자리). 철학 리라이팅 장부 §1 은 「왕관」→**「도전 배지」**(학부모 접점·문서 표기 — **앱 내부 기능명은 유지**)로 못박았는데, 리라이팅 트랙의 대상 목록이 `docs/` 뿐이라 **배포 코드가 통째로 사각**이었다. 실측: 배포 파일에 「왕관」 109자리·「Титэм」 11자리(대부분은 주석·시트 열·강사 HUD = 손댈 대상 아님).<br>🔴 **실제 학부모 접점 2자리**: `Code.js:2900` 주간 리포트 카드 `👑 Титэм: N удаа` · `Code.js:2913` 왕관 배너 `титэм авлаа! Гэртээ магтаж өгөөрэй` + 한국어 병기 「오늘 왕관을 받았어요! 집에서 칭찬해 주세요」. 🔑 **선례가 이미 있다** — `Code.js:2915` 가 v9.74 에서 같은 카드의 「몬스터」를 **성장 파트너(хамтрагч)** 로 이미 바꿨다. 「왕관」만 안 된 것이다.<br>⚠ **지금 못 미는 이유 = 몽골어**(장부 §6 「몽골어 병기는 원어민 검수 대기 — 리라이팅해도 확정 아님」). 몽골어 주문장을 못 바꾸는 채로 한국어 병기만 바꾸면 **한 카드 안에서 말이 갈린다**(지금은 둘 다 「왕관」이라 최소한 일치한다). 선행 = 몽골어 검수자 1인(유호님 채용 몫 · 결정큐).<br>⚠ **경계 판정이 남아 있다** — 학생 접점(`Code.js:2922` 오늘의알림 푸시 · `:2933` 나의 기록실 「첫 왕관」)은 장부가 「학부모 접점」만 적어 대상인지 미확정이고, `.claude/skills/synk-brand/SKILL.md:24-25·77` 은 **세계관 정본**으로 「몬스터 진화·왕관」을 정의하며 모범 답안까지 「숙제를 내면 왕관이 하나 올라가요」로 준다 — 학생 접점을 대상으로 잡으면 그 정본과 `voice-guard.js:56` 처방까지 같이 가야 한다. 🚫 **전면 개명은 기각돼 있다**(메모리 「몬스터 전면 개명(학부모 접점만 교체)」)므로 **집는 세션은 경계부터 판정하고 그 판정을 장부 §1 에 적는다.**<br>✅ 슬로건 축은 닫혔다 — `tests/은퇴문구.test.js` 가 배포 파일의 구 슬로건 재유입을 기계로 막는다. 🚫 그 검사를 낱말(「왕관」·「몬스터」)로 넓히기(경계를 기계가 못 갈라 거짓양성이 쏟아지고, 그러면 검사를 끄게 된다) · 🚫 몽골어를 검수 없이 지어 넣기 · 🚫 한국어만 먼저 바꾸기. 근거=[[app-ui-style-directive]]

## P3 — 도구·하네스

*(비어 있다 — 2026-08-09 두 항목 다 닫혔다: 리허설 배포 뒤처짐 `local_ddd51fde` · 역방향 회귀 `local_33c711a0`. 검증층은 지금 **전부 돈다** — `배포대조` 다름 0 · `배달왕복시험` **88/88** · `인증왕복시험` 41/41 · `교정왕복시험` 12/12.)*

---

## 지금 막혀 있어 여기 못 올리는 것 (유호님 답이 열쇠)

`node tools/decision-queue.js` 가 정본. 그중 **대기열을 실제로 막고 있는 것**만: 미니게임 게이트 AND/OR · C0 동결 범위 · Expo 결제(실기기 빌드) · 몽골어 검수자 1인 · **LAB철학 판정 9건**(`docs/LAB철학.md` §6 — 철학이 확정돼야 P2 콘텐츠·미니게임 서사가 그 위에 선다).
