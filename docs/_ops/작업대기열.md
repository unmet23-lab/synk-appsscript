# 작업 대기열 — AI가 다음에 집을 것

> **상태**: ✅**확정** · 2026-08-09 — 자율 진행 모드의 실행 순서 정본(메모리 `autonomous-build-mode`).
>
> **왜 이 파일인가**: 유호님이 새 세션에 「이어서 해」만 입력해도 굵직한 것부터 지어지게 하려면, **다음에 무엇을 집을지가 한 곳에 있어야 한다.** 인계문은 「직전에 뭘 했나」, 보드는 「지금 누가 뭘 잡았나」, 결정큐는 「유호님이 답할 것」이라 셋 다 이 자리를 못 채운다.
>
> **쓰는 법(세션)**: ①맨 위부터 훑어 **아직 아무도 안 잡은 것**을 고른다(`node tools/board.js` 로 대조) ②내 보드 파일에 트랙 선언 ③끝나면 여기서 그 줄을 지우고 커밋. **남의 보드 줄과 겹치면 다음 것으로 내려간다.**
>
> **차단자 표식(필수)**: 새 줄을 추가하거나 차단자를 판정했으면 `- [ ] ` 바로 뒤에 `〖지금〗`·`〖⏳유호〗`·`〖⛔선행〗`·`〖재료0〗`·`〖⏸시점〗`·`〖남이점유〗` 중 하나를 단다. **「열림」과 「지금 집을 수 있음」은 같은 말이 아니다** — 2026-08-12 실측에서 열림 23 중 지금 집을 수 있는 것은 **3건**뿐이었고(나머지는 열자마자 막힌다), 그 차이가 어디에도 안 보여 새 세션이 P0 부터 훑다 시간을 태웠다. `node tools/대기열.js` 가 이 표식을 세서 「지금 집을 수 있는 것」을 맨 위에 낸다. 표식이 없으면 「지금 가능」이 아니라 **「미표기」로 따로 센다** — 모름을 통과로 세지 않는다. 차단자가 풀리면 표식을 `〖지금〗` 으로 바꾼다(그게 다음 세션에게 보내는 신호다).
>
> **영구 ID(필수 · 유호 채택 08-14)**: 표식 바로 뒤에 `#Qnn` — 새 줄은 **파일 전체 최대+1** 을 받고, 지운 번호는 재사용하지 않는다. 줄이 움직여도 ID 는 안 변한다(첫 발급만 그날 줄번호를 씨앗으로 썼다 — 이후 번호와 위치는 무관하다). **집으면 내 보드 선언의 트랙 칸에 그 `#Qnn` 을 그대로 적는다** — `tools/대기열.js` 점유 판정의 조인 키이고, 같은 `#Qnn` 을 두 세션이 선언하면 `board-guard` 가 선언 순간 막는다. 왜 생겼나: 점유를 제목·줄번호·파일 겹침 세 갈래로 **추정**하던 동안, 트랙 이름이 회차마다 갈려 잡힌 줄이 🟢로 광고됐다(F406 · 08-13 실사고). 누락·중복은 `tests/대기열점유.test.js` 가 fail 로 잡는다. 안 집는다는 뜻으로 남의 ID 를 적을 땐 `🚫#Qnn` (그건 자리가 아니다).
>
> **고치는 법**: 새 일감을 발견하면 근거와 함께 해당 층에 **추가**한다(자율 모드 — 유호님께 안 묻는다). 순서를 크게 뒤집는 것은 유호님 판정.
>
> 🚫 유호님이 답해야 하는 것을 여기 넣지 않는다(그건 `node tools/decision-queue.js`) · 🚫 완료한 줄을 ✅로 남겨 두지 않는다(지운다 — 이력은 git).

## 층 — 위가 먼저다

**P0 학생이 앱을 실제로 쓰기까지 막는 것** → 개원 직결([[build-for-opening-now]])
**P1 수집→엔진 도달** → 평생 계획의 심장이고 지금 물리 도달 0([[collection-engine-automation-mandate]])
**P2 재미·쉬움·신선함** → 유호 품질 기준(「안 쓰이고 재미없고 어려우면 이 시대엔 가치 없다」)
**P3 도구·하네스** → 위 셋을 더 빨리·안전하게 만드는 것만

---

## P0 — 개원을 막는 것

- [ ] 〖재료0〗 #Q26 **운영 `engine.learners` 적재 0** — 통로는 섰지만 운영 명부는 여전히 비어 있다(`local_4cd29e93` 실측 · 리허설 156행이라 왕복시험은 내내 초록이었다). ✅ **통로 결함 2건은 잡혀 고쳐졌다**(talk `8d37608` 08-09 · 낡음 대조 반영 08-10 `local_7a7ce3cf`): ①`user_id` 별칭 누락 — 유호님이 내려받아 먹일 **바로 그 시트**를 통째로 거절했다 ②`role` 열을 안 봐 강사가 학생 명부에 설 행으로 잡혔다. 처방은 거절이 아니라 **건너뜀**(강사=고칠 대상이 아니라 대상 아닌 행 · 건너뛴 행은 역할별로 세어 보인다 · 열이 전량 비면 무시·쓰는데 그 줄만 비면 거절) · 회귀 44 · 변이 28/28 · 실측 4줄→학생 2+대상 아님 2. **이제 남은 것은 재료뿐이다.** ⚠ **선행 = 유호님의 실제 명부 한 장**(학생번호·전화) — 개원 전엔 채울 재료가 없다. 근거=[[l0-auth-code]]

- [x] 〖✅종결〗 #Q28 **✅유호 확정 08-14 「외부로 나가도 돼, 너무 이거에 목메이지마. 당연히 모든 시스템을 구축하고 단단하게 만들때 내가 알아서 처리할게」 → 이 줄을 닫는다** — 상시 지시 [[consent-not-a-decision-input]](「보안=전부 완성 후 개원 때 유호님이 직접」)의 재확인이다. 🚫재제안 · 🚫「소급 불가라 지금 해야」 논거로 되살리기(유호님이 그 축을 알고 판정했다) · 아래 실측(도는 자리 6·처방 3선례)은 **완성 후 유호님이 집을 때의 재료로만** 보존한다. 원 진단 보존 ↓ **Apps Script 층이 학생 «이름»을 외부 모델로 내보내고 있다 — 방향 불변식 4 정면 위반**(①배포 검수 P3 `2a691faf9a43`·`8a166fbac772` 가 짚음 · `local_f542de4f` 확인 08-10). 정본 원문이 안 나가는 것을 **「식별자(이름·연락처·`student_id`·생년·학교)」**로 못박았는데(`docs/제품방향.md:62`), `aiText_` 호출부가 이름을 프롬프트에 그대로 싣는다: 웰컴 스토리(`엔진_콘텐츠AI.js:2228` `s.n`+목표+비전) · 「미래의 나」 편지(`:2317` `s.n`+목표+TOPIK+포인트) · **몽골어 레벨 진단**(`:2544` `nm`+점수) · 첨삭(`callClaudeFeedback_:1884` `stu.name`+급수). ⚠ **앞의 둘은 `STORY_AI_ON=false` 라 지금 안 돈다 — 뒤의 둘은 돈다.** 🔴 **소급 불가 축이다**(나간 것은 되돌릴 수 없다) — 그래서 P0 에 둔다. 개원 직결은 아니지만 학생이 늘수록 매일 샌다. ▶ 판정 갈래가 하나 있어 **재료를 같이 낸다**: 편지·스토리는 이름을 부르는 것이 산출물의 핵심이라 「이름 제거」가 곧 품질 손실이다 — ①호출부에서 가명·이니셜로 치환하고 **반환 후 되돌리기**(품질 유지·통로 하나) ②이름 없이 짓고 템플릿이 이름을 끼운다(누출 0·문장이 뻣뻣해진다) ③레벨 진단·첨삭만 먼저(도는 자리부터). 🚫 「구조·통계만 전송」 복원(08-06 유호 A안 확정으로 폐기된 문구) · 🚫 소급 삭제 시도(벤더 쪽이라 못 한다 — 앞으로만 막는다).<br>🔴 **전수 재측정 08-10 `local_5bdf4e0a` — 「4자리」는 과소평가다. `aiText_` 호출부만 세고 `aiCall_` 계열을 안 셌다.** 지금 **도는 자리는 6개**이고 그중 **셋은 학생 여럿을 한 번에** 보낸다: ①**개인화 튜터**(`:2108` — `s.n`+급수+약점+**최애**+**목표**를 한 줄로 묶어 `AI_DAILY_BATCH_SIZE` 단위 **전원** · 매일) ②첨삭(`:1885` `stu.name`+급수 · 매일 1명씩) ③**리텐션 멘트**(`:2197` 이름+반+사유 · 여러 명) ④**칭호 작명**(`:2281` 이름+월P+주활동+몬스터 · 여러 명 · 월 1회) ⑤레벨 진단(`:2549` 이름+점수) ⑥**주간 운영 리포트**(`엔진_셋업확장.js:1147` — 리포트 본문 **6000자를 통째로** 싣는데 그 안에 「🔇 4주차 침묵 학생」 섹션이 있다 · 주 1회). 위가 짚은 편지·스토리 둘은 그대로 안 돈다 — **즉 우선순위가 뒤집힌다.**<br>🔑 **처방은 이 저장소 안에 이미 세 번 실물로 있다** — 오류은행(`:2149` system 이 *"개별 문장에서 개인 정보를 제거하고"* 로 명시) · 반 브리핑(`:2176` 반 이름·인원·평균만) · SNS 초안(`:2428` 집계 숫자만). 할 수 있다는 것이 증명돼 있고 **안 한 자리만 안 된 것이다.**<br>🔑 **갈래 ①(가명 치환→되돌리기)은 절반이 이미 구현돼 있다** — 튜터·칭호가 **인덱스 `i`** 로 응답을 되맞춘다(`out.items[].i`). 이름은 매칭에 안 쓰이고 **품질에도 안 쓰인다**(첨삭 산출 4칸·칭호·퀴즈 어디에도 이름을 부르는 칸이 없다). 레벨 진단은 **폴백(`:2550`)이 이미 템플릿으로 이름을 끼운다** — 갈래 ②의 실물 예다. ⚠ **리텐션만 예외로 이름을 응답 키로 쓴다**(`map[it.n]`) → 인덱스로 바꾸는 것이 그 자리의 일감이다.<br>▶ **판정안 = 갈래 ③+②**(도는 6자리부터, 이름을 빼고 인덱스로 매칭). 품질 손실이 **관측되지 않는다** — 부를 칸이 없다. 이름이 산출물의 핵심인 편지·스토리 둘은 지금 안 도니 갈래 ①/② 판정을 유호님 몫으로 남긴다. ⚠ 착수는 `엔진_콘텐츠AI.js` 를 만지므로 **`f542de4f` 의 v9.205 clasp push 완료 후**(F310 — clasp 는 작업본을 민다).<br>✅ **도는 6자리가 닫혔다**(08-10 `local_1b8a040b` · 판정안 갈래③+② 그대로): 튜터·첨삭·칭호 = 프롬프트에서 이름 삭제(매칭은 원래부터 인덱스) · 리텐션 = 응답 키를 이름→인덱스로(소비자 `리텐션멘트` 모양 불변) · 레벨진단 = 이름 없이 짓고 템플릿이 머리에 끼움(폴백과 같은 꼴 · 갈래②) · **주간리포트 H7 = AI 입력을 집계 화이트리스트 6섹션으로**(15섹션 전수 매핑 실측 — 이름 확정 6(순위·미납·재등록·문의·만료·이수율)+조건부 3(워치독 유령행·설문 자유서술·교안 URL) 제외 · **새 섹션 기본값 = 제외**라 새는 방향이 기본 닫힘 · 6000자 절단은 방어가 아니었다 — 이름 섹션이 본문 앞쪽). 첨삭 사용자 틀은 `FB_USER_TEMPLATE` 상수로 빼 `fbPromptVer_` 지문에 편입(틀 변경=이름 재유입이 prompt_ver 에 보인다). 회귀 = `tests/이름유출.test.js` 5종 — **호출부 전수 스냅샷 12자리**(새 벤더 호출부는 등재 전까지 빨강 — 「전수를 사람 눈으로 세다 두 번 틀린」 그 자리를 기계로) + 자리별 조립식 검사 + 탐지력 픽스처 6모양. 선행이던 v9.205 push 는 이 트랙 커밋과 함께 HEAD 로 한 번에 나간다(배포는 늘 HEAD).<br>✅ **라이브 반영 종결 2026-08-10 22:09 `local_fe590634`** — 커밋만 돼 있고 **라이브는 v9.202 였다**(주인 둘 다 배포 전에 죽었다: `1b8a040b` ▶검수→push→clasp 미완 · `f542de4f` ▶push→clasp 미완). 실측→게이트→배포로 닫았다: 이종 검수 **차단급 0**(버그 사냥 1회 0건·2회 0건 · 기능체크 P3 13건은 알림급) · CI 모사 2477/0 · 보안 통과 · `배포판점검 --라이브` = **「라이브 = 저장소 · 배포집합 15개 바이트 동일」**. 첫 실전 = 그날 22시 `nightJobs`(첨삭·튜터·리텐션·레벨진단) · H7 주간리포트 = 08-17(월) 07시 · 칭호 = 09-01. ⚠ 도중에 **게이트 자체가 막혀 있던 것**을 고쳤다(`82318707`) — 기능체크만 `Math.min(초*1000, 900000)` 로 눌려 도구가 내미는 `--timeout` 처방이 그 자리엔 원리상 안 닿았다(F103 · 08-05 도입 땐 기본이 900 이라 no-op 이었는데 08-08 F249 가 기본을 1800 으로 올리며 구속조건이 됐다) · 회귀 2건(픽스처가 탐지력·실저장소는 거짓양성).<br>🔴 **남은 것 = STORY_AI_ON 두 자리(웰컴 스토리·미래의 나 편지)의 갈래①/② 판정 = 유호님 몫** — 켜기 전 판정이 선행이라 지금 새는 것은 없다. 근거=[[core-engine-obsidian]]

- [ ] 〖재료0〗 #Q30 **✅유호 답 08-14 「미안해 아직 몽골어 검수자가 없어.. 미안해」 → 표식 ⏳유호 ⇒ 재료0** — 유호님이 «낼 답»이 없다(팩 검수확정의 차단자는 판정이 아니라 **사람 1인**). 🚫재촉·🚫재질의 · 검수표는 이미 서 있다(운영 「G1 문항팩 몽골어 검수표」·40조각) — 검수자가 오는 날 확정 커밋 1벌. ⚠남은 ⏳유호 4(강사 계정·저장 자리·배정 이력·운영 배포)는 **G2 피드백 통로** 몫이라 이 표식과 축이 다르다(별도 등재). 원줄 ↓ **미니게임 4모듈 착수 — ✅선행 판정 4건 해소(유호 확정 08-10 · `ee0d0ed6` 수령 · P0 B-3 종결=talk `eb35ab0`): 게이트 3축=OR · G4=서류 관문 유지 · 숨은 시계=채택 · 적대자=새 NPC 계열 신설.** 원줄의 「답 오면 즉시 P0급」 규칙으로 P2→P0 승격 + 두 세션의 같은 항목 두 줄을 여기 한 줄로 병합(`21f0c571` 08-10). ✅**게임층(재미·연출·NPC) 설계 정본 = `docs/미니게임_게임층_설계.md`**(`local_21f0c571` · 분리 반박 2패스 — v1 치명 5 철회: 재도전율 식·G2/G4 자동채점 오판·G3 녹음 중 소리·검사표 조작·인내 게이지 정본 침범). ⚠08-10 동시 진행: `e2aebfa1`=발주서 본문(작업본 집필 중 — §1-1 OR·§1-4 NPC·§6-8 스냅샷 표·§6-9 지시문 · 반입 참조는 그 커밋 뒤) — **집기 전 보드 대조**. ⚠**남은 선행**: S1 완성(발주서 §6-4 — G1·G2부터) · ~~c9 ③ 승인~~ ✅**종결 08-10 `local_da1166a0`**(talk `82d6802` — 검증기에 태우니 차원을 실어도, 계약이 모르는 아무 키도 **통과**한다. 「없으면 C0 가 거절한다」가 거짓이라 승인 자리가 아니었고, 진짜 차단자였던 생산자를 세웠다: 조립기·배정·화면·소비자 한 커밋 · 회귀 10 · 근거=발주 §6-6 ⑦) · §6-7⑧ 처분 규칙 · ~~⏳유호 신규 3건~~ → ✅**NPC 확정 3벌 08-11**(`089723ff` · 유호 답 수령): 이름=**㉠ 직함만 유호 확정** · ㉯=**ⓐ 상급자 신설 위임 판정**(결정록 #23 · 게임층 설계 §4 규격 5 개서 · 발주서 §6-9 「상급자께」 갱신=반입 커밋 몫) · 성격=**반응 규칙 4벌 위임 판정**(결정록 #24). 실물=**`docs/캐릭터/NPC_시안_v2.html`** 운영 「NPC 시안 v2 (확정판 전체 실물)」(수위 B · 4역 전부 · 상태 3단×4 · 보상 얼굴 4 · 환상층/현실층 대비 시연). ✅**v2 유호 확정 08-11 「이대로 확정」**(수위 B·4역·성격 채택 종결 — NPC 축 유호 답 완료) · ✅**자산팩 반입**(talk `8de8951` — 4역×4상태 16종+회귀) · ✅**㉰ 소리층 집행 08-11**(유호 「기본안+효과음·햅틱·양질」 · talk `2461d9b` — BGM 통로(생성물 git 밖·`측정템포BPM` 1곳·결정록 #25)+`소리게이트` 전역 게이트+SFX 3종+expo-haptics · 회귀 8) — ✅**BGM 선정 종결 08-11**(유호 「너무 마음에 들어. 23번으로」 → `synk-bgm-measure.wav` 승격 · talk `4f84653` · `bgm재생()` 기본 트랙 · 🚫튀는 층 복원 — **소리층 유호 대기 0** · 잔여=G3 녹음 경계 배선=게임 화면 커밋 몫 §7 ⓐ) · ⚠G3 녹음 경계 배선·§7 회귀 ⓐ = 게임 화면 커밋 몫(`089723ff`). 품질 기준 = BGM·효과음·마이크로인터랙션까지(유호 08-09 「띡 던져놓기」 금지 — 기준 정본=위 설계 문서). ⚠`quiz.answered`·`choice.selected` 생산자를 세우면 소비자 도달을 같은 커밋에(`lib/학습자상태.js` ③자기인식·⑤관심축이 소비자 자리 — 래칫이 막는다).<br>✅ **첫제출-전 판정 2건이 실측으로 닫혔다 — 착수를 막는 계약 자리는 하나도 안 남았다**(08-11 `local_6efd76f3` · talk `47449fa` = 발주 §6-6 ⑨ 신설). ①**c9 ① `compose_meta` 도 «계약» 차단자가 아니었다** — ⑦이 ③에 한 것을 ①에 그대로 했다(검증기+서버에 5사례를 실제로 태웠다 · 전건 통과). 🔑 그중 3번이 **⑤가 되돌리며 막으려던 «반쪽 객체» 바로 그 형태**라, 계약에 넣든 안 넣든 똑같이 통과한다 — 되돌림 사유는 옳지만 **계약이 못 막는 것**이었고 막는 자리는 **조립기 하나**다(③과 같은 결론 · 승인이 아니라 코드 자리). ②**`learner_response=무시`** ⏳ 종결 — 「구현 착수 시 앱 1곳에서 확정」이라던 그 한 곳은 S1-8 `src/답장화면.js:18` 이고 이미 G1 규칙(버튼 둘)을 이 발주 §153·§191 근거로 못박고 있었다. 안 누른 것은 `viewed` 있고 `responded` 없는 모양으로 갈리므로 손실이 아니다.<br>✅ **1단계 = 조립기가 섰다**(talk `65ff9a6` · `lib/작성과정.js` · 회귀 15 · 선택로그 1단계 선례). 네 칸을 **한 벌로만** 내고 한 칸이라도 못 재면 객체를 통째로 안 만든다(0 으로 채우면 「안 쟀다」가 「0회였다」로 적히고 소급이 없다) · `first_keystroke_ms`(회피 지연)와 `total_compose_ms`(붙든 시간)는 **겹치지 않게** 정의해 더하면 전체가 된다 · `revision_count` 는 횟수가 아니라 **구간** 수(막힘 vs 다듬기를 가른다) · `input_burst_max` 는 정수이고 **라벨이 아니다**(검수 우선순위 신호 전용). 한글 조합에서 길이가 안 변하는 전이는 일부러 안 센다. ⚠ **아직 생산자가 아니다** — 화면이 서는 커밋에서 엔진 도달을 같은 커밋에.<br>▶ **다음 = 2단계**(G1 화면 + 생산자 + 소비자 한 커밋). 🚫 `paste_used` 불리언 복귀 · 🚫 `latency_ms` 로 `first_keystroke_ms` 흡수 · 🚫 「c9 승인 대기」로 되살리기(되살리려면 검증기에 태워 거절을 실측으로) · 🚫 조립기만 또 짓기.<br>⚠ **트랙 겹침**: `de44763a` 가 같은 칸(순수 로직 3벌)을 보드에 선언했으나 파일이 0개인 채 박동이 식었다 — 겹친 첫 벌만 **그쪽 선언 이름**(`lib/작성과정.js`)으로 커밋해 두 이름이 서는 것을 막았고, **남은 2벌(`멘탈게이지`·`게임스냅샷`)은 안 건드렸다.**<br>✅ **순수 로직 3벌이 다 섰다 — 남은 2벌 종결**(08-11 `local_9e34a18f` · talk `e364872` · 회귀 12 · 스위트 1257/1257). `lib/멘탈게이지.js`=5칸(인사·사과·이유·요청·맺음) 규칙 판정 — **연출·안내 전용**(채점 아님·저장 0·서버 0 · G1 §4-5): 문형 어간·어미 정규식(부분일치 채점 0 · 소스 `.includes(` 0 을 회귀가 기계로), 이유 어미의 글자 이웃(에서·께서·-면서·여기서·보고서) 제외, 맺음은 **끝 15자 창**(30자로 두면 본문 중간 감사를 오탐 — 픽스처가 잡아 좁혔다), 칸 목록은 팩 `요구문형` 키 파생(판정기 없는 칸=전체 null · 반쪽 판정 금지), `같은판정()`이 리렌더 판단(한글 조합 보호). `lib/게임스냅샷.js`=§6-8 G1 6키를 팩 `모듈상수`·`펴기` 재사용으로 조립(값 사본 0 · 보기·정답·mn 키 없음 · 빈 재료면 스냅샷 통째 null · **배정 쪽도 같은 함수** · 검수확정 게이트는 배정 통로 몫). ⚠**셋 다 아직 생산자 0** — ▶**다음=2단계**(G1 화면+생산자+소비자 한 커밋 · 조립기만 또 짓기 🚫). 조율: `a6fb9632` 가 같은 인계문을 받고 같은 트랙 선언 → `9e34a18f` 선점 확인 후 철회(as `6abb02a7` · 중복 구현 0 — 같은 인계문이 두 세션에 배달된 충돌).<br>✅ **2단계 종결 — G1 화면·생산자·소비자가 섰다**(08-11 `local_9e34a18f` · talk `9c03338`+`ecb59a7` 한 트랙 · builder 위임+컨텍스트 독립 적대 리뷰 1패스 · 변이 12/12 · 1323/1324). `src/교수멘탈화면.js`(전략 3장→쓰기→「교수님이 읽고 있어요」 · uncontrolled TextInput·숨은 시계·게이지=빠진 칸 이름·즉답 0·성취음=소리 게이트 경유) · `lib/게임제출.js`(§6 표 그대로 실계약 실태우기 · compose_meta 한 벌 아니면 키 없음 · **검수확정 fail-closed**) · `lib/게임큐.js`(단일 직렬 통로 — 동시 쓰기 소실 재현 픽스처 선행) · `저장.못보낸수()`(나가기 게이트가 전 큐 합산) · `학습자상태` 작성과정축(추정판 v4 · 표본 이중 계상 제거). 리뷰가 잡은 차단 3=전부 **학생 산출물 영구 소멸 경로**(게이트 미합산·배출구 1곳·flush 경쟁) — 수리 완료. H1: 제목 칸은 c8 에 자리가 없어 **뺐다**(계약 판정 후 복원 — 「제목: 」 합성은 계측·게이지·body_original 을 소급 불가로 오염). ⚠적색 1=be9063b(남의 트랙 「구문검사만」). ▶**다음=배정 배선 커밋**(새벽 배치+검수확정 게이트=인수 조건 · **이월 H2** 이탈 문턱·시계 혼용 · **H3** 재제출 대기 점프 — 둘 다 소급 불가 신호라 그 커밋 전 판정) · deliver 재배포=밀린 묶음 합류(전까지 라이브 추정판 v3 · ⏳유호).<br>🔴**배정 배선은 한 커밋 배선이 아니다 — 명세 v1 이 컨텍스트 독립 적대 반박에 기각됐다**(08-11 `local_5c2158b4` · 치명 5 전부 파일:줄 실측): **C1** `/v1/tasks` `학생판스냅샷` 허용목록(최상위 `ver·날짜·호흡` — `오늘과제.js:207-242`)이 G1 6키를 전부 벗겨 앱은 게임을 원리상 못 보고, 그날 발화 폴백도 빈손(「오늘 과제를 읽지 못했어요」·제출재료 null) — 수리는 `tasks` 동봉이라 **deliver 와 한 커밋·한 배포**(F302) · **C2** 동봉 기전이 CJS 전용(`원격배포.js` require풀기·.mjs 껍데기)이라 **ESM 인 팩·게임스냅샷·게임제출을 못 싣는다** — 배포는 초록·배치는 부팅 전사(선행 = 동봉기 ESM 축 or 게임 lib CJS 통일 + 「동봉 전량 CJS」 회귀) · **C3** 게임 배정이 교정 워터마크(`deliver:196-204` 「마지막 task.assigned 뒤 확정분만」)를 밀어 **미배달 교정문이 영구 증발**(주2회면 확정 교정 ~2/7 — 결과 변수 급소 · 처방 = 교정문 있으면 게임 미루기) · **C4** task_ref 를 챌린지 상수(`g1-교수멘탈`)로 박으면 둘째 게임날부터 대기 점프로 잠긴다 — 발주 :167 표는 **제출 행** 규격이지 배정 행이 아니다 · 날짜 스코프 task_ref(기존 픽스처 `task-2026-08-11` 축)가 H3 의 진짜 키 · **C5** H2 「다음 마운트 발견」은 재료가 없다 — 선택사건·큐 항목에 task_ref 칸이 없어 오귀속 아니면 0건(선행 = 큐 항목 칸 신설). W 굵은 것: 급수 조항은 「일부러 없음」이 아니라 **침묵**(G3·G4 는 명시 제외 · 초급 보호 선례 `오늘과제:132` — 판정안=초급(1~2·미정) 제외 · 유호 이의 자리) · G1 은 `intervention.delivered` **🚫**(발주 :226 — 개입 행 없이 배정만 · 성과회수 닻 오염 방지) · 새 판정은 학생별 try **안**으로(`deliver:285` 오늘과제 호출은 try 밖 — 한 학생 예외가 배치 전체 500) · 요일 파생은 `getUTCDay`(`골든표본:51` 선례 · 로컬 tz 는 하루 밀린다) · 검수 큐 뷰가 쓰기 원문·지시문을 못 그린다(`review_c10.sql` — 검수확정 뒤집기 전에 검수 콘솔 몫 선행). ▶**순서** = ✅**①C2 종결**(08-11 `local_5c2158b4` · talk `8948c1a` — 게임 lib 2벌 CJS 통일+`스냅샷모양판` 게임스냅샷 이사+REQUIRE문 상대경로 전부·**유니코드 식별자**(`\w` 가 한글 이름 require 를 조용히 통과 — 같은 날 실측)+로더 바꾼소스 지름길 구멍 수리+`동봉CJS.test.js` 회귀 5 · 스위트 1328/1329, 적색 1=기준선 be9063b 몫) ✅**②C4+H3 종결**(08-11 `local_35a08d49` · talk `b578600` — 발주 §6-6 ⑩ 신설: 배정 행 task_ref=`task-${날짜}` 확정(챌린지 식별=`task_snapshot.challenge_id` · 상수면 `제출항목` 대기 점프에 영구 잠김=retry_of 행 원리상 0) · H3 재제출 판=**다음 게임날 «새» 배정 행**(새 task_ref+retry_of_event_id 동봉 · 같은 task_ref 재배정 금지 · §4-11 「다시 보낼래요」=`수정` 의사 표시로 정정 — 즉시 재작성 아님) · attempt_no=같은 배정 안 눈금 분리 · G3 §6-3 task_ref 셀 동축 정정 · ④ 인수 조건 3갈래(날짜 축·retry 동봉·같은 키 재배정 0) 등재 · 회귀 1(`게임로그.test.js` 잠김 기전 양방향) · 1330/1330 — 앱 쪽 배선·회귀는 이미 완비 실측이라 ②는 배정 규칙 문면+회귀로 닫혔다. ⚠전제=그날 배정 1건 — ④가 두 배정을 내는 쪽으로 가면 재판정) ✅**③C5+H2 종결**(08-11 `local_35a08d49` · talk `576a13e` — H2 실측: cleanup 이탈 생산자는 「자정을 넘겨 산 채로 언마운트」에서만 발화=사실상 0 · 처방=**「다음 마운트 발견」 수거**: 고름 항목에 로컬 닻(`task_meta`=task_ref+스냅샷 2 · 전송 안 됨 · 제출로그 무늬)을 붙이고, 고름만 남고 그날이 지난 배정을 이탈 항목으로 세운다(조립=`이탈사건` 하나 · 실배선 `src/게임큐.js` 주입) · 시계 혼용 제거=판정 기준이 **서버 task_ref 끼리 대조**(오늘 배정 모르면 수거 쉼 — 오귀속보다 0건) · 수거 자리 둘=게임 마운트+말하기 공용 배출구(밀기 직전) · 이중 계상=항목 id 접기+완결 배정 제외 · cleanup 생산자 유지(W3 즉시층) · 닻 없는 옛 항목 안 걷음 · 회귀 6 · 1336/1336) ✅**④C1+C3+배정 본체 종결**(08-11 `local_fb0d32ac` · talk `38f3f77` **한 커밋** · 발주 §6-6 ⑪ — deliver 게임 갈래(판정 정본=`lib/게임배정.js` 순수 · 검수확정 fail-closed→게임날 화·금[판정안]→첫날→C3 교정문 우선→H3 재제출→초급 제외[판정안·재제출은 우회]) · C1=`학생판스냅샷`→`학생판게임스냅샷` 위임(6키 공개·자체 허용목록·모르는 챌린지=전부 벗김) · C3 둘째 층=②슬롯 교정 조인 `task_type='발화녹음'` 한정(메일 교정문이 낭독 문장으로 나가는 사고) · H3 조인 닻=retry 배정의 부재(워터마크 아님) · 개입 행 0 · 멱등키 같은 자리라 「그날 1건」 유일 제약 · 시드=학생×날짜 결정적 해시 45벌 · 회귀 20(`tests/게임배정.test.js` — ⑩ 3갈래 동봉) · 1362/1363 skip1). **배정 배선 ①~④ 전부 닫혔다.** ⚠남은 것: ⓐ~~deliver·tasks·progress 재배포 밀림~~ → ✅**해소 실측 08-12**(배포대조 다름 0 — 12함수 같다·radio 2종만 404 미배포(⏳유호 배포 자리) · Lane B 재배포로 이미 닿아 있었다 · F313 적색 원인 소멸) ⓑ~~SQL 두 조인+게임 INSERT 리허설 실왕복 0회~~ → ✅**실왕복 종결 08-12**(talk `9686cfa` · `local_7201c1e2` — 배달왕복시험 ⑫⑬: C3 격리(메일 교정문 낭독 유출 0)·H3 거울 1행→재배정 뒤 소등·같은날 멱등 접힘(그날 1건=유일 제약)·미래 게임날 성립·/tasks G1 6키+retry 관통 · 109/109 · 왕복 게이트 함수목록 스코프 신설+회귀) ⓒ✅**검수 재료 종결 08-12 `26fb0070`**(`local_a92d3a05` — 정본 `docs/G1문항팩_몽골어_검수표.md`·운영 「G1 문항팩 몽골어 검수표」 · 40조각 팩 직접 조립 · AI 초안 칸 없음=발주 §3 🚫 · 발주서 §L·§M 반입=08-04 판이 8일치 물량 미포함 · 🔑급소=「조립 확인 5문장」(조각을 몽골어에서 붙였을 때 깨지면 팩 개정 v2) · 핵심어휘 번역 금지 · `mn` 키 모양 판정안 4벌) → **잔여 = 검수자 오는 날 확정 커밋뿐**(mn 병기+true+문항판 인상 한 벌) ⓓ⏳유호 2=게임날 화·금·초급 제외 판정안 이의 여부. cleanup 이탈 생산자는 지우지 않는다(로컬 항목id `abandon:{correlation_id}` 가 접는다 — W3 이 명세 v1 의 삭제 근거를 반증). 기록: M3 게임 로그 본문 무기한 보관 · M4 「홈 카드 1장」과 early return 갈림 · M6 코랄 1점 기계 검사 부재 · L3 소리 플레이어 누수(남의 트랙 파일 — 보고만).<br>✅**확정대조 판정 08-13 `local_8eafc289` — G2 축 반영 + 「지금」 몫 분리**: ①G2 1~3단계 **전부 종결 08-12**(팩 63문항·로직·생산자·소비자 탐지축 v8·3단계 ①배선 talk `0a9242e`·②화면 `eaa9976` — 이 줄이 G2 를 한 자도 안 적고 있었다 · 정본=메모리 §G2 세 절) ②ⓓ의 「초급 제외」 판정안은 3단계 ①배선 ⑥이 **「초급→G2 로 보낸다」로 대체**(이의 자리도 그 판이 정본) ③**G3·G4 «짓기» 몫은 아래 〖지금〗 줄로 분리** — 이 줄에 남는 것 = ⛔팩 검수확정(몽골어 검수자 — G1 검수표=운영 「G1 문항팩 몽골어 검수표」 준비 완료)·ⓓ게임날 화·금 이의·G2 피드백 통로 ⏳유호 4(강사 계정·저장 자리·배정 이력·운영 배포 — talk `docs/강사_반단위_피드백_설계.md` §10)뿐이라 〖⏳유호〗 표식은 그대로가 맞다. 근거=[[minigame-spec-4modules]]
- [ ] 〖재료0〗 #Q31 **미니게임 잔여 2모듈 — G3 「알바 변명」(말하기 20~30초)·G4 「서류 관문」(빈칸 강제 산출) 구현**(08-13 `local_8eafc289` 확정대조가 위 줄에서 분리 — 〖⏳유호〗 표식이 «지을 수 있는» 몫까지 가리고 있었다 · 「지금 집을 수 있는 줄 0」의 실원인 하나). 패턴은 선례 둘 그대로 = **팩→로직→생산자·소비자→화면 · `검수확정=false` fail-closed 로 짓는다**(몽골어 검수는 «여는» 게이트지 «짓는» 게이트가 아니라는 것이 G1·G2 두 선례로 증명 — G1 조립기 `65ff9a6`→화면 `9c03338` · G2 1~3단계 `71392a0`→`eaa9976`). 규격 정본 = talk `docs/발주_게임모듈.md` **§G3(:426)·§G4(:667)**(G3 §4-0·§4-3 「없다」는 검증자 거짓양성 판명 — 실재 · G4=원안 문서 대조 생략 유호 확정 08-10 🚫재요청) + `docs/발주_게임콘텐츠팩.md` · 검수 예산 병목은 G3 뿐(발주 §6-2). 유의(메모리에서 승계): G3 녹음 경계 배선 §7 ⓐ=화면 커밋 동봉 몫 · G3 녹음 구간 소리·햅틱 🚫(마이크 되먹임) · G4 스냅샷 `보기`·`정답` 키 금지(§6-8) · `corrections.error_tags` 설계 라벨 적재 🚫(teacher 행만). ⚠「G2~G4 팩 미리 짓기 🚫」는 모듈 트랙 **없이** 팩만 쌓는 것(G1 검수표에 얹기)을 막은 조항 — G2 가 자기 트랙 1단계에서 팩 63문항을 지은 선례가 그 경계를 실증한다. ⚠G2 재제출(deliver H3 조인 G1 한정 넓히기 · Fn 재배포 동봉)은 이 줄이 아니라 별개 소트랙(메모리 §3단계② 말미). 착수 = `node tools/board.js` 대조 후 talk 저장소에 선언. 근거=[[minigame-spec-4modules]] · ✅**진행 08-13**: G3 1단계 talk `d6f0279` · G4 팩 `b36bdbc`+등재 `575413b`(`25a36462`) · G2 재제출 `2ebf585` · **게임 3Fn 재배포 리허설(deliver v24·tasks v23·progress v21)+운영(각 v5 · 판 c11 통과 · `--운영` 자율키=유호 확정 08-12) 배포대조 6/6 같다 + 배달왕복시험 117/117(⑫·⑬·⑫-G2 배포판 실측 · `ac1af92c`)** · ✅**코드 사슬 종결 08-13 — 화면 2벌 «검증»까지**(`local_0766bb39`): 51a502a7 이 사망하며 남긴 자동커밋 talk `9654cbc`(화면 2벌+라우팅 1468줄)가 **스위트·리뷰 미실측 상태로 착지**해 있던 것을 닫았다 — 스위트 **1968/1968** · `tools/precommit.js` 통과 · `사슬점검` ①~⑥ 초록 · 적대리뷰 1패스(차단 0). 유의 4건 전건 대조 초록: G3 §7 ⓐ 녹음경계 배선 ✅(열림/닫힘/언마운트) · 녹음 중 소리·햅틱 0 ✅(신호음은 녹음 «전» + 300ms 무음) · G4 스냅샷 `보기`·`정답` 키 0 ✅(`빈칸.정답집합` 은 §6-0-4 가 «학생공개»로 명시한 값이고 `채점기판본` 은 비공개) · error_tags 적재 0 ✅. **배정 갈래(초급→G2)는 이미 서 있다** — `lib/게임배정.js` 초급게이트+G1G3G4 회전풀(`bec282f`). · ✅**적대리뷰 P2 1건 처분 종결(같은 날 · 조율으로 닫혔다)**: `src/말하기화면.js` 언마운트 cleanup(≈900줄 「게이트 균형」)이 `녹음끝()` 만 부르고 **`마이크끄기` 를 안 불렀다** — `끝()` 에는 있다. 녹음 중 언마운트로 나가면 `allowsRecording: true` 가 남아 그날 **재생 전부**(BGM·사장님 TTS·되듣기·효과음)가 계속 작게 들린다(G3 자동시작이 이 경로를 손 탭 시절보다 훨씬 자주 밟는다). 처방=그 cleanup 에 `마이크끄기({ 오디오모드: setAudioModeAsync }).catch(()=>{})` 한 줄 + `tests/알바변명화면.test.js` ⑤-c 에 소스층 회귀 1줄. 파일을 산 세션(aecdc2fd 카피교체)이 들고 있어 0766bb39 는 **못 댔고**(F073 — 경로를 못 박아도 저쪽 4 헝크가 같이 나간다) 좌표·처방만 여기 남겼는데, **aecdc2fd 가 그것을 읽고 그대로 집행**했다(talk `b41fce2` · 처방 그대로+회귀 ⑤-c 1줄 · 커밋 제목에 「0766bb39 P2 인계」). 🔴**그 커밋이 master 를 적색으로 만들었다** — 주석에 적은 `allowsRecording` 을 `tests/마이크권한.test.js` 가 원문 `includes` 로 재서 잡았다(코드는 한 줄도 안 어겼다). 0766bb39 가 그 가드를 **공용 `코드만()` 경유로 옮기고 탐지력 픽스처를 붙여** 닫았다(talk `09ef832` · **1975/1975**) — 신고 F401(같은 계열 2번째 · 2026-08-12 에 이미 걷힌 계열의 잔류 가드). · ✅**운영 4Fn 재배포 종결 08-13**(유호 지시 「4Fn 재배포 해줘」 · `local_0766bb39`): **deliver v7 · tasks v6 · progress v6 · corrections v4 ACTIVE**(판 대조 c11 ✓ 넷 다). 사전 게이트=스위트 **1996/1996**·작업본 깨끗. 도착을 **3층**으로 쟀다 — ①배포대조 `--운영` **다름 7→3** ②남은 3이 정확히 «내가 안 쏜 그 셋» ③라이브 HTTP **401**(살아서 인증 거는 중 — 매니페스트 일치는 「돈다」의 증거가 아니라 따로 쟀다). 🔑과녁 갈이 실측: `--운영` 은 과녁 스위치가 아니라 **안전키**이고 과녁은 `.env` 의 `SUPABASE_PROJECT_REF` 가 정한다(`lib/자격증명.읽기` 가 `{...파일읽기(), ...process.env}` 라 셸 환경변수가 이긴다) — 이름만 믿고 쐈으면 리허설에 떨어진다. ⛔**안 쏜 3건과 그 이유**: `roster-ingest`=유호 보류(반피드백 트랙 몫 · 드리프트 아님 · memory deploy-target-gate) · `radio-ingest`·`radio-promote`=라디오 트랙 몫. 🔴**그 radio 2벌은 기록과 어긋난다** — 메모리 radio24-design 은 「라이브 착지 완결 08-13(운영 cron 200)」인데 운영 배포본엔 `라디오파서.mjs`·`라디오승격.mjs`·`라디오채팅파서.mjs`가 **없다**. 둘 중 하나가 틀렸고 내 트랙이 아니라 안 건드렸다 — 라디오 트랙이 실측할 자리(이 줄이 그 통보다). ✅**해소 08-13 `local_ffb86589` — 시점 유물이었다**: `배포대조.js --운영` **전량** 재실측 = `radio-ingest` ✅같다(1/1) · `radio-promote` ✅같다(**8/8** — `라디오승격.mjs`·`라디오채팅파서.mjs` 포함) · 전체 **다름 1**(roster-ingest 뿐 = ⏳유호 보류 · 드리프트 아님). 메모리 `radio24-design` 이 맞았다 — 이 신고는 `867298eb` 의 radio-promote 운영 배포 **전에** 잰 값이다. · 잔여=여는 건 팩 검수확정(몽골어 검수자 오는 날)<br>🔍**표식 재판정 08-13 `local_ffb86589` — 〖지금〗 → 〖재료0〗**: 이 줄이 선언한 «구현»은 자기 꼬리에 **✅코드 사슬 종결**(화면 2벌 검증·1968/1968·리뷰 차단 0)로 적혀 있고, 같은 꼬리가 「잔여=여는 건 팩 검수확정(몽골어 검수자 오는 날)」이라 못박았다 — 세션이 지을 것이 아니라 **사람이 올 것**이다. 〖지금〗 으로 두면 새 세션이 P0 라서 맨 먼저 열고, 열어야만 막혔음을 안다.
- [ ] 〖재료0〗 #Q32 **✅유호 답 08-14 「아직없어..」(몽골어 검수자) → 표식 ⏳유호 ⇒ 재료0** — 21조각 검수표는 완성돼 있고(운영 「몽골어 검수표 21조각」) 확정 회수만 남았다. 🚫재촉·🚫재질의. 🔴**그동안도 미검수 AI 번역이 매주 라이브로 나간다**(설계 §5 ③) — 검수자가 없는 채로 이 상태가 이어지는 것을 유호님이 아신다는 뜻으로 읽는다. 원줄 ↓ **주간 리포트 본문 몽골어판 — 개원 전 필수**(철학정합 §3-F · 유호 채택 08-11 · `local_339b2c3e` 등재). ✅ **설계 종결 08-12** — 정본 = `docs/주간리포트_몽골어_설계.md` v1(`local_b5cf7ae1` · `7a0b0279` · 반박 1패스 4건 반영 · 메모리 [[parent-mail-mongolian]]). **아래 원 진단 3곳이 실측으로 정정됐다.** ①줄번호 `1934-1941` 은 낡음 — 그 자리는 지금 `parentWeeklyDigestRetry_` 이고, 실제 자리는 본문 `2032-2045`·발송 `2049`·태그표 `2102` 다. ②**「본문 몽골어 생성」이 아니다** — 전수로 세면 검수 대상은 **고정 문안 21조각**(주간 13 + **왕관 메일 8** — 왕관 `2239-2251` 도 같은 결함인데 이 줄이 안 적고 있었다)이고 **한 번 검수하면 영구**다. 가변은 둘뿐: ⓐ「이번 주 배운 것」=`weekly_topics` **E열 `배운내용_mn` 배선이 이미 서 있다**(`translateTopics_` · 다이제스트가 E 우선 읽음 `2019`) ⓑ칭찬 태그=`TAG_MN[t] || t`(`2037`) 라 매핑 밖이면 한국어가 몽골어 자리로 샌다(1줄 처방). ③규약은 발명 대상이 아니다 — 결석(`677-681`)·생일(`1045-1050`) 메일이 이미 「몽골어 먼저+한국어 괄호+몽골어 서명」으로 나간다(v9.28). 🔴 **반박이 연 구멍** = ⓐ의 AI 번역 출력을 아무도 안 본다 — 미검수 몽골어가 **지금도 매주 나가는 중**(설계문 §5 ③). 🔴 **발송 대상 수 미측정**(운영 시트 미열람) — 0이 아니면 순서가 앞당겨진다. ✅ **검수표 종결 08-12 `be81d35d`**(`local_a92d3a05`) — 정본 `docs/주간리포트_몽골어_검수표.md`(운영 「몽골어 검수표 21조각」 · 초안 21칸·확정 21칸 빈칸). 그 대조가 **줄번호를 또 정정**했다: 위 ①의 `2032-2045`·`2049`·`2102`·`2239-2251` 도 v9.21x 착지로 밀렸다 → **실측 = 본문 `2071-2084`·제목 `2088`·`TAG_MN` `2141`(누출 `2076`)·왕관 `2278-2289`**(문구는 21/21 그대로). 🔴 **그리고 설계가 「검수 대상 아님」이라 한 왕관 몽골어 3줄이 실은 한국어보다 짧다** — `#20`「어제의 나보다 한 걸음…」은 몽골어로 **아예 안 나가고** `#17` 은 반 이름이 빠졌다 → ✅**유호 확정 08-12 = ㉡ 한국어와 1:1**(압축본 유지 기각 · `9cff04f1` · 🚫재질의). ▶ **다음 = 확정 회수**(검수자 1인 선행) → 상수 1벌 → 배선 2자리 → `TAG_MN` 누출 1줄 → 회귀. 원 진단(보존): 몽골어는 칭찬 태그 4종 병기+꼬리 요약 1줄뿐, 제목·서술 본문 전량 한국어(측정일 08-11) — 철학 Ⅲ-1 「매주 몽골어 리포트」 약속과 갭이 크다. **몽골어 검수 선행**(산출물 검수 원칙 · 선행=검수자 1인 — 결정큐 기존 항목·새 결정 아님). 서기 전까지 **대외 문안의 현재형 표현 조정이 한 벌**(§3-G — 부록 A 는 08-11 개서 완료 `f7a4c5a5` · 본문 Ⅰ-5 「매주 도착하는 몽골어 리포트」류 현재형은 이 트랙에서 함께 — 본문은 뜻 층이라 판 올림·전파 절차를 탄다). 🚫검수 없이 몽골어 지어 넣기 · 🚫태그 병기를 「몽골어판 있음」으로 세기. 🔍**확정대조 판정 08-13 `local_8eafc289`: 낡음 아님** — ㉡ 확정(한국어와 1:1 · 🚫재질의)은 이 줄에 이미 실렸고, 잔여 = 검수자 1인 선행(결정큐 기존 항목)·발송대상 실측뿐이라 〖⏳유호〗 그대로. 근거=[[philosophy-alignment-audit-0811]] · [[parent-mail-mongolian]]

## P1 — 수집이 엔진에 닿는 자리

- [x] 〖✅종결〗 #Q78 **[v9.234] 재검수 차단급 2건 처분 → 그 뒤 `/deploy`**(08-14 `local_4d41f7d9` 등재 · 대상 지문 `#fp:892d8574` · `node tools/codex-review.js --확인` 로 원문을 본다). 🔴 **[v9.234] 는 커밋(`97e82d6f`)만 됐고 라이브에 안 나갔다** — 그 커밋이 닫은 것은 그 앞 회차의 P1 6건이고(오탐 0 · 변이 6/6), 재검수가 새로 둘을 냈다. ㉠**[P1] 반 스냅샷 실패가 `safeRun` 에 안 전해진다**(`엔진_셋업확장.js:1755`) — 내 수리는 실패 반 이름을 **요약 문자열**까지만 올렸는데 그 문자열은 `Logger.log` 로만 가서(`:1094`) 실패 메일도 재시도도 안 돈다. 즉 「조용히 빠진다」의 절반만 닫았다 — 처방은 실패가 있으면 그 회차를 `safeRun` 의 오류 경로로 올리는 것(단 **한 반 때문에 나머지 반 적재를 통째로 버리면 안 된다** — 모으고 마지막에 던지는 형태). ㉡**[P1] 반명 조인이 정확 일치다** — `talkIndexSnapshot_` 이 `profiles` 의 반 문자열을 그대로 넘기고 `groupBoardOf_`(`:2057`)는 `String(r[1])===cls` 로 맞춘다. 표시명이 한 글자라도 다르면(예: 「정규반2(9시)」 vs 「정규반2」) 그 반이 **빈 후보로 조용히** 빠진다. ⚠**이건 [v9.234] 가 만든 게 아니라 [v9.233] 부터 있던 자리로 보인다** — 집기 전에 운영 `profiles` E열과 `groups` B열의 실제 문자열을 **직접 대조**해서 「지금 갈리는 반이 몇 개인가」부터 재라(0이면 기각 사유가 그 실측이다 · 갈리면 조인 정규화가 이 트랙 밖 선행 결함이다). 🔑 배경 = 이 저장소는 **차단급 미해결이 있으면 모든 세션의 배포가 막힌다** — 둘 다 「고치거나 사유 달아 기각」 중 하나로 끝내야 열린다. 근거=[[fluency-result-storage]] · 학생 0명이라 실피해는 0이고 첫 실전 작동은 월요일 07시다. ✅**종결 08-14 `local_379cb6f7`** — 차단급 2건 **기각 0으로 전건 수리**([v9.235] `01cb8dd0`) + 그 재검수가 낸 것까지 처분([v9.236] `cd2f5654` · 수리 2·기각 2) · 최종 검수 **차단급 0**(#fp:cb501388) · ✅**라이브 착지**(라이브=HEAD · 배포집합 16파일 바이트 동일 · `배포판점검.js --라이브` 실측). 🔑㉡ 은 이 줄이 예고한 「운영에서 0이면 기각」 갈래를 **안 탔다** — 학생 0명이라 지금 재면 0이 나오는데 그건 «안 갈린다»가 아니라 «잴 게 없다»다(분모 0). 대신 구조가 답했다: 그 접기는 **이미 저장소 관용구(5곳)**였고 새 자리 하나가 통로를 안 탄 것이라, 트랙 밖 선행 결함이 아니라 이 트랙이 낸 누락이었다. ▶잔여 = #Q79(운영배치 2곳 이관) · #Q80(실패한 주가 영영 빔 + 행별 계약 버전) · 마찰 F444 신설.


- [ ] 〖⏳유호〗 #Q37 **카피 전수 감사 — 「따뜻하되 직관적」 전면 교체(유호 확정 08-13 · F394 defer 를 닫는 자리)**(등재 08-13 `local_5f22db27` · 규칙 층은 `3db8e737` 로 닫힘: voice-guard·synk-brand·집필 규범·매뉴얼 §4-2 — 확정 표기 「내일의 필살기」→**「다음에 맞힐 문제」**). 남은 것 = **콘텐츠 층**: ①repo 정본 .txt(소개서·FAQ·기업마스터·수업규칙 3곳·커리큘럼·05운영)·교재 4문서(디자인시스템 「스킬 뱃지」·목차골격·앱연동 「필살기 노트」·시냅스코어 원고) ②SYNK-talk 앱 화면 문구 전량 ③`contents_*.js` 학생 대면 문자열. 절차 = 문구 전량 표로 뽑아 **「첫 독해에 뜻이 서나」** 기준 3갈래(㉠고침 / ㉡살림 / ㉢기능명=코드·교재 연쇄라 신중) → 유호 확정 → 일괄 교체 + 몽골어 대응 동시 확정. ⚠낱말 교체 경계(장부 §1-b)와 「왕관/Титэм」 줄(P2)의 몽골어 검수 선행 조건을 승계 — 한·몽 병기 카드는 ㉢(한국어만 먼저 바꾸면 한 카드 안에서 말이 갈린다). 🚫「내일의 필살기」 재사용 · 🚫은유 단독 대체어. ✅**감사표 완성 08-13 `local_660dcb6e`** — 정본=`docs/_ops/카피_전수감사_표.md`(독립 추출 3벌→적대 반박 1패스 P0 1·P1 5·P2 6 **전건 수리** · 분모 3층 전수: A 42파일+정본 잔여 20 미감사 선언·B src29/lib55/팩7·C 루트 16/16 — 만족도팩 학부모 병기 3벌 ㉢ 편입) · 집계 ㉠93곳(무조건 61+갈래종속 32)·㉢68·㉡~2,710 · 반박 기록=`docs/_ops/심문결과/카피전수감사-047e9416-반박.md` · **잔여=유호 확정(갈래1-가 일괄 승인 + 갈래2 픽 4: 슬롯명·필살기노트 ⓐ/ⓑ·뒤표지 슬로건·「무기」) → 교체 집행(A=문서 커밋·B=talk 커밋·C=/deploy) → F394 resolve**. 근거=[[offline-class-20min-structure]]
- [ ] 〖⏳유호〗 #Q38 **숙제 서클 실물화 — 조별 인쇄물 생산자 + 검수 콘솔 「오늘 수업 반」 뷰**(등재 08-13 `local_5f22db27` · 유호 확정: 수업 첫 20분=서클(4인조 발화)∥몽골쌤 순회 검수 · 정본=`docs/숙제서클_설계.md`+매뉴얼 §1-1·§1-1b). ①인쇄물 생산자: 엔진이 학생별 「잘한 것 1·다시 만날 문제 1」+레벨 문장 틀을 골라 **조별 1장** 조립(기존 「앱이 매 차시 인쇄」 통로 확장 — 발화퀄리티 v9.99 조 편성 기계 재사용) ②검수 콘솔: **오늘 수업 반 단위 대기열**(AI 초안→확정/수정 1탭 · 16명/20분 = 학생당 ~1분 · 조 단위 묶음 화면) — 기존 ⑥ 검수 콘솔 트랙에 얹는 요구지 새 화면 신축 아님 ③과제 포인트(15) 부여=검수 확정 시 자동(매뉴얼 §1-2 개정 반영됨). ⚠겹침: ⑥ 검수 콘솔 〖재료0〗(키 오는 날)과 한 벌로 착지 가능. 🚫새 수집 채널 신설(수집은 옵션 — 유호 명시) · 🚫서클 화면·인쇄물에 점수·등수(㉢). ✅**진행 08-13 `local_e7707d28` — 인수조건 §10 의 1·2·4·5 종결**(`f28e3986` [v9.228]): ①조 단위 조립자 `circleSheetOf_`→`circleSheetHtml_`→`printCircleSheets`(읽기 전용 · 새 시트·폼·event_type·쓰기 0 · 조·좌석·역할은 `groupBoardOf_` 재사용) ②A4 조판 **실측** — 8줄이 59.4mm 칸에 들어간다(여백 5.1mm · `tools/서클조판.js --실측` 헤드리스 크롬 · 라이브와 같은 함수). 실측이 연 것 둘 = ⓐ`scrollHeight` 가 「딱 맞다」와 「한참 남는다」를 같은 숫자로 냈다(재는 층이 값을 깨뜨림 → 마지막 블록 실제 바닥으로 교체) ⓑ긴 제출문은 넘친다 → **잘림 순서를 CSS 로 못박음**: 학생 문장(`.keep`) 불가침·강사 틀(`.frames`)만 잘림(`--넘침시험`이 탐지력 되물음 4/4) ④ROLE_TALK **가중 유지 · 범위를 이름에 박음**(브리핑은 역할 무관 균등이라 지수에 안 든다 → 브리핑 문구 교정 · 🚫평탄화 · ⏸서클 발화 합산은 첫 시즌 실측 뒤 — 지금 합치면 못 재는 상수를 지어내는 것) ⑤질문 카드 **96장**(결 4종×밴드 3×8 · `contents_서클.js` · 줄만 더하면 늘어난다). 회귀 `tests/숙제서클.test.js` 25. ✅**§10-3 「오늘 수업 반」 뷰 — 코드 층 종결 08-14 `local_63180e5f`**(유호 집행 지시 · 구 표식 「키 오는 날」은 ⑥의 옛 차단자를 잘못 물려받은 것 — ⑥ 자신은 08-13 에 「실학생 발화」로 갈아 적었다): talk 한 벌 = 마이그 `20260814100000_review_class_c11`(learners 조·좌석 거울 2칸 + `review_queue_class` 판 26열 — 정체를 여는 판정 근거는 순회 동선 §4) + `review` Fn `?class=` 갈래·`GET /v1/review/classes`(반커서 = 조→좌석 정렬 · 감사 review.queue 동일 분모 + review.classes 신설) + 검수화면 반 칩·조 칩 묶음·이름줄 + 다리 = as `명부스윕_` 조편성 동봉 → `roster-ingest` 스냅샷 갱신(미언급 비움 · 막힘 제외 · 범위 못 1~20 결속 회귀). talk 스위트 2062/2062 · as 명부스윕 회귀 +3. 계약 정본 = talk `검수_내부계약.md` §3-2. ✅**운영 도달 — 착지 08-14 `local_f8cb9aaf`**(as `756c6323` · 「⏳유호 승인 묶음」은 낡음이었다: 그 표기의 출처는 AI가 만든 질문 목록이고 운영 배포 승인은 08-12 유호님 원문으로 이미 자율주행이다 · 정지선 둘 다 안 열림 — 파괴형 0건·운영 `learners` 0행 붓기 전후 동일): 마이그 3조각(운영 조각 25→**28** `20260814100000`) → Fn `review` v4·`roster-ingest` v3(순서 강제 — 거꾸로면 아침 스윕 전체 500) → 배포대조 **다름 0 · 미측정 0 · 전체 14**(운영·리허설 둘 다 · 리허설도 같은 3조각이 없어 양쪽을 부었다). 운영 실물 = anon 자기 봉투로 확인(안내문이 `GET /v1/review/{queue,classes}` 를 스스로 부른다 · 부팅을 401 로 안 쟀다 F425). ▶잔여 = **운영 `ROSTER_INGEST_SECRET` 미설정**(운영 503 `ingest_secret_unset` / 리허설 401 = 시크릿이 있어 판정하고 거절 — 붓기가 만든 게 아니라 **드러낸** 것이고 자격증명이라 세션이 안 넘는다 ⏳유호) + §10-6 ⏳유호(조 이름 장치 — ✅08-14 자율주행 「자동부여」 판정 별도). ⚠왕복(라이브 HTTP) 층의 반 모드 커버리지 **0** — `검수왕복시험` 71/71 은 새 경로를 하나도 안 잰 초록이다(소스 회귀는 46건). 근거=[[offline-class-20min-structure]]

- [ ] 〖재료0〗 #Q40 **✅유호 08-14 「이거에대해 자세히 설명해줘」 → 설명 제출(세션 `0f1f094e`)** — 표식 불변(차단자=학생 0명·키 오는 날). 원줄 ↓ **AI 교정 생산자 — 코드는 섰고 「키·배포·cron」만 남았다**(⑥ 의 **선행** · `local_f0fb6e57` 08-09). 원래 진단: 큐 뷰가 `submission.created` **+ AI 교정 있음**을 둘 다 요구하는데 저장소 전체에 `engine.corrections` **INSERT 가 0줄**이라 큐가 구조적으로 0행이었다(리허설 실측 제출 704 · AI 교정 462 · 큐 0 — 462는 전부 `task.assigned` 에 붙은 것으로 `deliver:269` 설계지 결함이 아니다).<br>✅ **지었다** — talk `6379aa2`: `lib/교정엔진.js`(순수) + `functions/correct`(service_role 배치) + 회귀 25 · **변이 6/6** · 780/780. 동의는 `engine.consents` 를 직접 본다(`pipeline_jobs.status='revoked'` 는 값목록만 있고 **writer 0** 이라 거짓 가드다).<br>▶ **남은 셋**(08-09 `local_4f05894e` 실측 갱신): ①**`ANTHROPIC_API_KEY` 미설정 = 개원 날 넣는다 — 유호님께 다시 묻지 않는다**(유호 판정 08-09 `4f05894e` · 🚫재질의 · [[vendor-api-billing]]): 종량제라 안 쓰면 0원이고 **학생 0명이라 교정할 발화가 0**이라, 이건 돈 판정이 아니라 **재료 부족**이다(2026-08-12 유호님 「저번에 결재한 것 같은데 또 필요하냐」 재질의로 드러난 낡음 — 판정 다음 날부터 이 줄이 「유호님 몫(돈)」이라 적고 있었고, 08-12 차단자 분류가 그 글자를 그대로 믿었다 · 구독료 ≠ API 키는 사실이고 그것도 그 판정에 이미 적혀 있다). 없으면 배치가 행을 안 건드리고 `no_api_key` 만 낸다(오류 아님) ②✅**리허설 배포 완료** — `correct v1 ACTIVE`(`원격배포 --목록` 실측 · 판 대조 통과 `저장소 c10 · DB 20260809060000` · 호출 시 `401 service_role 만 부를 수 있습니다` 로 인증 게이트도 실측). ⛔**운영 배포는 0** — 라이브라 유호님 승인 자리이고, 로컬 `.env` 가 리허설 REF 를 물어 `--운영` 을 붙여도 대상이 안 바뀐다. ③`pg_cron` 잡 = **지금 부을 자리가 없다**: 리허설은 `20260809070000_cron_c10.sql` 머리말이 ⛔ 로 금지(스케줄러가 돌면 **옆 세션 왕복시험의 배정 상태를 흔든다**) · 운영은 ②·①이 선행. 🔑 **키 오는 날 운영 배포와 한 벌로 짓는다** — 지금 마이그레이션 파일만 미리 지으면 `base_version` 이 남의 새 조각에 밀려 낡고, 그 조각은 부을 때 `raise` 로 죽는다. 🚫 「큐가 비었으니 뷰를 넓히자」(AI 교정 없는 행을 검수자에게 내리면 검수가 아니라 채점이다) · 🚫 프롬프트·값목록을 코드에 베끼기(동봉으로 온다)
- [ ] 〖재료0〗 #Q41 **⑥ 검수 콘솔 — 착수 순서 넷 + 배포 + 실왕복이 전부 닫혔다. 남은 것은 미측정 상수 둘과 운영 배포다.** 착수 순서 정본 = talk `docs/검수_내부계약.md` §6.<br>✅ **1 판 22열**(talk `a5fb940`) · ✅ **2 `review` 함수 + §3 큐 읽기 + §4 오디오 서명**(talk `ad17aef` · 회귀 28 · ⛔**배포 0 · 실왕복 0**). 「4구멍」은 능력 목록이지 배포 단위가 아니라 함수 **하나 + 하위 경로**로 섰다(조회 감사는 엔드포인트가 아니라 ①②의 부수 효과).<br>✅ **3 c11 물리 4자리 — 섰다**(talk `a1b45b3` · 847/847 · 변이 2/2 · 판은 **c10 유지**). 🔑 **이름 둘이 위 목록과 다르다** — 채택된 정본은 `corrections.supersedes` · **`promotion_intent`** · **`transcript_at_review`** · `pipeline_jobs.discard_reason` 다. `_verified_` 를 뺀 것이 판정이다: 검수자가 판정하는 시점에 화면에 있는 것은 보통 **기계 전사**이고 `transcript_verified` 는 그 판정의 **산출물**이라, 이름에 `verified` 를 넣으면 행마다 거짓을 적게 된다(사유 = 그 조각 머리말).<br>✅ **4 착수 순서가 전부 닫혔다** — §5 승인·폐기(talk `0a38e58` · 911/911 · 변이 12/12) + Expo 검수 화면(talk `7e51fcd` · 960/960 · 변이 22/22).<br>✅ **실왕복이 섰다 — `tools/검수왕복시험.js` 63/63 · 2회차도 63/63**(talk `dd849bb` · 08-09 `local_741d0fb6`). 배포·조각 매듭이 P3 에서 풀린 뒤 남아 있던 마지막 칸이다. 🔑 **「배포됨」과 「돈다」 사이가 통째로 비어 있었다** — `staff_access_log` 의 action 이 `learner.*` 둘뿐이라 `review.*` 넷이 전부 0행이었고(실측), 그 구간의 결함은 증상이 없다. 잰 것: ⓪물리 · ①문 10(anon·비직원·`teacher`·해임 전부 403) · ②§3 큐 11(keyset 이음 · `event_id` 미노출 · 0건도 감사 1행) · ③§4 5(**그 주소로 파일이 실제로 내려온다**) · ④승인 게이트 6 · ⑤원자성 넷 11 · ⑥`Z` 5(409 계보 충돌) · ⑦폐기 7(어휘를 DB CHECK 에서 읽어 안내문 대조). 사후 실측: `pipeline_jobs.status` 가 처음으로 `pending` 밖으로 나갔다(verified 2 · discarded 2). 🚫 큐 픽스처를 새로 짓기(없는 것은 AI 교정 한 칸뿐이라 그것만 심는다) · 🚫 서명 응답 칸을 `url ?? signed_url` 로 느슨히 받기(계약이 갈리는 날 시험이 초록으로 덮는다) · 🚫 절대 개수로 재기(감사·교정이 append-only 라 2회차부터 영원히 빨갛다).<br>✅ **게이트 ①의 재료가 섰다** — `submissions.stt_segments` 생산자(talk `8bb6f35` · 회귀 20 · 변이 12/12 · `local_99032bdf`). 소비자 셋(구간 칩 · `청취문턱` · `세그먼트펴기`)이 다 선 채로 **쓰는 코드가 0줄**이라 문턱이 언제나 하한 3초였다. 🔑 재료가 없어서가 아니었다 — `transcribe` 가 이미 `verbose_json` 을 요청해 세그먼트를 **받고 있었고** `전사값()` 이 버렸다. ⚠ `검수의뢰_엔진수집설계` §228 모양엔 `confidence` 가 없는데 `세그먼트펴기` 는 그 키**만** 읽는다 — 문서대로 저장했으면 생산자가 서도 저신뢰가 원리상 0이고 증상은 「초록인데 아무것도 안 걸린다」뿐이었다(상위집합으로 냈다). ✅ **배포됐다**(08-09 실측 · 판 대조 다름 0). 🔴 **그런데 아직 한 행도 안 만들어졌다** — `submissions` 중 `stt_segments` 가 있는 행이 **0**이다(08-09 `local_741d0fb6` 실측). 코드가 선 것과 행에 값이 있는 것은 다르고, 실왕복에서도 `listen_gate.measured` 가 **false** 로 나왔다(그 미측정을 응답이 드러내도록 설계돼 있다). ▶ **다음 = 새 전사를 한 번 태워 세그먼트 행을 만들고**, 그 위에서 미측정 상수 둘을 잰다 — `저신뢰문턱 0.7` 경계값 · 세그먼트 시간 단위(`lib/검수확정.js` 가 지목). 그전까지 문턱은 언제나 하한 3초다.<br>✅ **재료가 섰다 — 907건 만에 첫 `stt_segments` 행**(08-10 `local_19269335` · talk `ccee460` = `tools/세그먼트재료.js`). 🔑 **막던 것은 배선이 아니라 재료였다** — 오디오가 붙은 행은 전부 `업로드왕복시험` 의 **0.1초 무음 WAV** 라 Whisper 가 낼 구간이 없었다(그 행들의 전사가 죄다 「고맙습니다.」인 것이 무음 환각의 자국이다). 한국어 TTS 로 실말소리를 만들어 **정상 통로**(sign→PUT→events→transcribe)로 두 번 태웠다(16.2초 → 4조각 · 59.6초 → 14조각). ▶ **미측정 상수 둘 중 하나는 확정되고, 하나는 「문턱값」의 문제가 아니었다**: ① **시간 단위 = 초** — 소비자(`세그먼트펴기`)가 `start_ms` 없으면 초로 읽는 해석이 생산자 출력과 맞다(59.6초 오디오 → 편 총길이 59000ms · 1000배 틀린 문턱은 없다). ② **`저신뢰문턱 0.7` 은 값의 해상도가 30초다** — 14조각의 `avg_logprob` 이 **3개 값**뿐이고 경계가 27초·56초로 Whisper 의 **30초 디코딩 윈도우**와 대응한다(같은 윈도우의 모든 조각에 같은 값이 복사돼 온다 · `no_speech_prob` 도 같고 `confidence` 는 그 파생이라 같이 간다). → 과제 발화 대부분(30초 미만)은 **한 윈도우**라 전부 저신뢰이거나 전부 고신뢰고, `청취문턱` 은 실질적으로 「발화 전체 길이」 또는 「하한 3000ms」 **두 값만** 갖는다. 문턱을 0.7 에서 어디로 옮겨도 이 성질은 안 바뀐다 — 옮길 값이 아니라 **해상도**의 문제다. ⚠ 합성 발화라 저신뢰 쪽은 못 쟀다(0.87~0.90 만 봤다) — 더듬는 실학생 발화가 와야 한다(못 잰 것을 잰 것처럼 적지 않는다). ⚠ **이 실측은 UX ②(「이 구간만 들으면 된다」고 구간을 칠하기)의 전제를 흔든다** — 30초 미만 발화에서는 칠할 구간이 전부 같은 색이다. **판정은 안 했다**(재료만 낸다). 🚫 상수(`lib/검수확정.js`)는 안 건드렸다 — `833da425` 가 보드에 선언한 자리다.<br>✅ **그 상수 칸이 닫혔다 — 그리고 그 자리에서 회귀가 새고 있던 것을 잡았다**(08-10 `local_e38dd4c5` · talk `e9edfdd` · 죽은 `833da425` 트랙 회수). ①실측 둘을 상수 머리말에 못박고 ②「튜닝하면 되겠지」로 문턱을 만지는 손을 먼저 멈춰 세우는 회귀를 신설했다(값 옹호가 아니라 **진짜 손잡이가 재료 쪽임을 읽게 하는** 자리). 🔴 **급소는 따로 있었다** — 「생산자가 서면 빨개져 사람을 부른다」던 덫(`tests/검수확정.test.js`)의 판별식이 `set\s+stt_segments\s*=` 라 **실제 생산자를 못 봤다**: `transcribe/index.ts:158` 이 `set` 과 떨어진 조각(`` sql`, stt_segments = ${...}` ``)으로 쓴다. 생산자가 08-09 에 섰는데 그 검사는 **계속 초록**이었다(옛/새 판별식 실측: **옛 0건 · 새 1건** · 거짓양성 0). 처방 = 판별식을 `쓰기표기` 한 자리로 빼 사람이 쓰는 표기로 다시 짜고, 방향을 **「0줄이다」 → 「생산자가 있다」**로 뒤집고(사라지면 게이트가 조용히 하한 3초로 되돌아간다), 탐지력은 픽스처가 진다(쓴다 3형·읽는다 5형 양방향). 회귀 1023/1023. 🚫 문턱 옮기기로 해상도 문제 풀기 · 🚫 실학생 저신뢰 발화 전에 0.7 을 「검증됐다」로 인용하기.<br>✅ **재검수 청취 게이트가 닫혔다 — ㉮ 구현**(08-10 `local_50528b03` · talk `19e27f2` · 실왕복 **71/71**). 실왕복이 냈던 구멍(612초 된 옛 서명 기록으로 재검수 200)과 프리로드 누수를 **한 처방**으로 닫았다: 감사 종류를 `review.audio`(발급) ↔ **`review.audio.played`**(재생)로 가르고 게이트 ②를 `max(at)` 갈래 판정으로 바꿨다(첫 확정=시간 무관 · 재검수=`마지막청취 > supersedes.created_at`). 마이그 0(`action` 은 CHECK 없는 text). 🔑 **10분 창이 시간 상수 0으로 성립한다** — 확정분엔 새 서명이 안 나가니 창을 만드는 것은 서버 상수가 아니라 **서명 수명이라는 물리**다. 🚫 게이트에 시간 리터럴 박기 · 🚫 §4 가 확정분에 서명 내주게 하기(㉯) · 🚫 발급을 다시 게이트 증거로.<br>🔴 실 PG 검증층 = CI `supabase` job 인데 Actions 결제 차단으로 안 돈다 — 대신 `tools/원격SQL.js`(읽기)가 그 자리를 진다.<br>🚫 `dataset_item(group_id)` 는 **일부러 안 냈다**(발주서가 넷째로 적었지만 새 표 = ㉮ 가 미룬 C 계층 그 자체 · `엔진도달_설계.md` §6 표 3행 · 멤버십은 소급 불가가 아니다). c11 이 낼 때 `(dataset_ver, group_id) → 목적` 을 **PK 로** 잡아 훈련셋⟂평가셋 혼입을 표현 불가능하게 할 것 — 트리거는 꺼진다.<br>🚫 열 없이 §5 요청/응답 지어내기(구현 불가능한 채로 정본이 된다) · 🚫 `event_id` 를 검수 화면에 주기 · 🚫 함수 4개로 쪼개기·`action` 파라미터.<br>🔍**차단자 재판정 08-13 `local_2c180209`: 〖⛔선행〗→〖재료0〗** — 미측정 상수 둘은 08-10 에 이미 실측 종결(단위=초 확정 · 0.7=옮길 값이 아니라 해상도 문제)이고, 남은 「운영 배포」도 닫혀 있다: `review` 운영 «같다» 실측(08-12 `388660c5` 11종 붓기 · 08-13 배포대조 `local_2c180209`). 세션이 지금 할 일 0 — 남은 차단자는 **실학생 저신뢰 발화 하나**(개원 후 재료 · 그때 0.7 재검). 근거=[[reviewer-console-ux]]

- [ ] 〖⛔선행〗 #Q43 **✅유호 승인 08-14 「웅 오케이」 = 안전핀 4종을 다음 마이그 조각에 «넣는다»** — 판정 축은 닫혔다. 표식이 ⛔선행 그대로인 이유는 유호 답이 아니라 **물리**다: 마이그 파일을 미리 지으면 `base_version` 이 남의 새 조각에 밀려 낡고 부을 때 `raise` 로 죽는다(아래 ⚠). ▶다음 조각이 확정되는 그 커밋이 이 4종을 동봉한다 — 🚫따로 짓기. 원줄 ↓ **DB 안전핀 4종 — 다음 마이그레이션 조각과 한 벌로 붓는다**(전층감사 08-10 묶음 ① · 정본=`docs/설계전층감사.md` §2 — 학생 0명 + 붓는 날 전인 지금이 설계 비용 최소 구간). ①`submissions.event_id` UNIQUE — 지금은 비유일 인덱스뿐(`_c6.sql:618` · 조인들이 1:1 가정) · **오늘 새는 경로는 없다**(두 INSERT 다 부모 멱등키 유일이 앞에서 막음 — 반박 실측) → 심층 방어로만 정직하게 ②AI 교정 부분 유일 인덱스(`actor_kind='ai'` per submission) — `correct/index.ts:120-121` 이 자칭하는 「두 번째 INSERT 0행」 보장을 READ COMMITTED 의 `not exists` 는 못 준다 ③`consent_id` NOT NULL + `recorded_by` 강제 트리거 — 조각이 「다음 조각 몫」으로 예약해 둔 자리(`20260807120000:15` · `20260807140000:20-23,78`) · ✅**선행 해소**(08-10 talk `8576a8f` · 쓰는 통로 전량이 스탬프한다 — consents 8자리 · learning_events 13자리 · 회귀 `tests/동의귀속통로.test.js` 가 폴더째 훑어 재발을 막는다). ⚠조일 때 **`recorded_by` 는 `before insert` 트리거로**(`not null`·`check not valid` 는 옛 행의 **철회를 영구히 막는다** — 20260807140000:26-35 가 그 판정을 이미 냈다). ⚠남은 층 하나 = 값이 옳은지는 실왕복이 진다(회귀는 열 이름까지만 본다) ④`estimator_confidence` 0~1 CHECK — 유예 조건(「추정기가 서는 조각에서」)이 `deliver/index.ts:369` 쓰기 시작으로 충족됨. ⚠**마이그 파일을 미리 짓지 않는다** — `base_version` 이 남의 새 조각에 밀려 낡는다(위 AI 교정 생산자 줄과 같은 이유 · 다음 조각이 확정되는 시점과 한 벌). 근거=[[design-audit-full-stack]]
- [ ] 〖지금〗 #Q44 🔍**표식 재판정 08-14 `local_7c9e319e` — 〖⏳유호〗→〖지금〗 · 차단자가 사라졌다**: 이 줄이 적은 차단자 둘이 **둘 다 낡았다**. ①「도구에 대상 전환 플래그가 없다」 → F436(talk `56f5e8e`)이 `--운영` 조준을 공용 통로로 올렸고 F435(`edae7e3`)가 11 도구 전부로 넓혔다 ②「유호님 승인 자리」 → **08-12 유호 원문으로 운영 쓰기 자율**(정지선 둘 = 운영 데이터 삭제 · 실학생 행 들어온 뒤 쓰기 — 둘 다 안 열림, 운영 `learners` 0행 실측). ▶그리고 **기다리던 배포가 이미 있다**: `배포대조 --운영` 실측 08-14 = `corrections` 포함 **14/14 ✅같다 · 다름 0**. 남은 것은 배포가 아니라 **착지 확인(실왕복)**이다 — 집는 세션이 그 층부터 연다. · **동의 철회 학생의 교정 사건이 영구 제외되는 사슬 — ✅코드·회귀 종결. 남은 것은 배포뿐**(전층감사 08-10 §2-5 · 08-10 `local_b00d7deb` · talk `bf00ce8`+`3bbd1af`). 원래 진단: ⓐ서버 `corrections` 에 동의 게이트 0줄(통로 규약 밖 **유일한 조회 함수** — 파일 전체 `consent` 매치 0) ⓑ앱 교정 큐에 `blocked` 게이트 없음 → `CONSENT_MISSING(retryable:false)` 가 `send_final` 영구 제외로 떨어진다(C0 §7 왕복 5 가 **제출 큐에서** 잡은 그 병이 교정 큐에 그대로 남아 있었다).<br>✅ **새 모양을 안 지었다** — ⓐ는 `tasks` 와 같은 꼴(`동봉.json`+`지금유효`→봉투 `blocked`), ⓑ는 `lib/제출로그.보낼것` 을 `교정로그` 가 **재수출**(지은 줄 0 · 전송 자리가 `흘려보내기` 하나뿐이라 호출부마다 안 묻는다). 계약 개정 0·마이그 0(응답 필드 추가는 판올림이 아니다).<br>🔑 **막혔다고 `data` 를 비우지 않는다** — 그러면 「교정이 아직 없다」와 「막혔다」가 같은 모양이 되고 빈 카드 금지 때문에 화면이 안 떠 학생은 이유를 못 듣는다(F176). 카드는 그대로 두고 **버튼 자리에만** 막힘 카드를 세운다 — 큐를 멈추면 `send_final` 이 안 붙어 「답이 저장되지 않았어요」 경고까지 사라지는데, 화면은 로그에서 파생한 「답했어요」(서버 도착을 함의)를 말한다.<br>✅ 회귀 = `동의게이트.test.js`(조회 통로 둘 다 · `blocked` 적재까지) · `교정로그.test.js`(막힘 5) · `교정API.test.js`(중계 층 2 — **아무도 안 재던 칸**) · `화면렌더.test.js` ⑦-b(막힌 화면을 실제로 그린다). 스위트 **1015/1015**.<br>✅ **리허설 배포 + 실왕복까지 닫혔다**(유호님 승인 08-10 · talk `e98a3be` · `corrections` **v8→v9** · 판 대조 ✅ · `배포대조` 다름 0). 회귀는 **소스에 게이트가 적혀 있는지**까지만 보므로 배포본이 옛 판이면 그 초록은 라이브에 대해 말이 없다 — 그 사이를 `배달왕복시험` **⑫**(88→**94/94**)가 진다: 철회한 A 도 200+`CONSENT_MISSING` · **목록 20건 그대로** · `tasks` 와 **같은 코드**. 🔑 ⑫ 가 파일 맨 끝인 것이 설계다 — **철회는 되돌릴 수 없고**(DB 트리거 `protect_consents`: 「재동의는 새 행이다」 L0 §9-3 · 실행이 잡았다) ⑩ 이 `지금유효id식(A)` 를 계속 쓴다. 🚫 `revoked_at=null` 원복(트리거가 막고, `where learner_id` 통째면 **원래 철회된 행까지 되살린다**).<br>🔴 **남은 칸 = 운영 배포 하나.** ⛔ 로컬 `.env` 의 `SUPABASE_PROJECT_REF` 가 리허설(`synk-core-rehearsal`)을 물고 **도구에 대상 전환 플래그가 없다** — 운영 REF 는 자격증명이라 세션이 지어낼 수 없다(같은 벽 = 위 AI 교정 생산자 줄). ⚠ 순서는 **서버 먼저** — 앱만 새 판이면 `blocked` 가 안 와서 게이트가 조용히 없다. 🚫 막혔다고 목록 비우기 · 🚫 `보낼것` 에 기본값 주기(안 넘긴 호출부가 조용히 옛 동작으로 샌다) · 🚫 앱에 「동의하기」 버튼. 근거=[[design-audit-full-stack]]

- [ ] 〖지금〗 #Q46 🔍**표식 재판정 08-14 `local_7c9e319e` — 〖⏳유호〗→〖지금〗 · 줄 자신이 반증을 들고 있었다**: 본문이 이미 「⏳유호님 승인 자리이던 것은 **08-12 유호 확정으로 세션 자율**이다(운영 포함)」라고 적는데 줄머리 표식만 옛날 것이었다(표식은 붙인 시점의 사진이라 늙는다 · F406). 두 번째 차단자 「재배포 4종」도 **08-12 해소 실측**이 같은 줄에 적혀 있고, `배포대조 --운영` 08-14 재실측 = **14/14 ✅같다 · 다름 0**. ▶남은 것은 착지 확인이다. · **S1-5 「골라서 답하기」 — 화면이 없어 성향 축 ⑤가 영원히 `null` 이다**(개원 최소 셋 S1 의 미완 칸 · P0 §275 · 08-10 `local_19269335`). `choice.selected` 는 ⑤관심·목표의 **유일한 입구**인데 생산자가 0줄이라 `lib/학습자상태.js` 가 그 축을 못 낸다. 🔑 **미니게임 판정(유호님 대기)과 무관하다** — P0 가 이 칸의 엔진 무응답 동작을 「고정 선택지 2개」로 적어 둬서 **서버 변경 0으로 앱만으로 설 수 있다**.<br>✅ **1단계 = 순수 조립기 + 회귀**(talk `1c2ebcc` · `lib/선택로그.js` · 회귀 8 · 1032/1032). 자리를 섞고 그 순서를 그대로 남기는 것이 이 파일의 값이다 — 추천을 늘 첫째에 두면 「1번을 누르는 손버릇」과 「그것을 좋아함」이 영원히 같은 모양이 된다. `latency_ms` 는 **단조 시계**로만 받는다(벽시계 차는 음수·몇 시간짜리를 만들고 그 행은 «오래 망설인 학생»으로 읽힌다).<br>🔑 **회귀가 실계약 검증기를 직접 먹여 둘을 잡았다**: ①`position` 은 **1부터 센다**(L0 §140 정본 예시 — 0 부터 셌으면 전 행이 한 칸씩 밀리고 그 오독은 어디서도 안 빨개진다) ②🔴 **계약이 「안 골랐다」를 원리상 못 받는다** — `skipped`·`rejected_all` 을 택1로 허용해 놓고 같은 계약이 `payload.position` 을 무조건 필수로 건다(`널허용` 에도 없다). 안 고른 행은 가리킬 자리가 없어 `null` 인데 그게 거부된다. **「무관심」과 「뚜렷한 거절」은 성향 축에서 정반대 신호**인데 둘 다 통로가 0이다.<br>✅ **2단계 종결 — 성향 축 ⑤가 `null` 을 벗어났다**(talk `7487034` · 08-10 `local_680f686e` · 스위트 1060/1061, 남은 1은 미커밋 경고라 그 커밋이 풀었다). ⓐ선택 화면(`Pressable` · **조립기가 준 순서 그대로** 그린다 · `latency_ms` 는 단조 시계 · 고름은 **앉음당 한 번**만 싣는다 — 시도마다 실으면 사건이 시도 수만큼 나가고 멱등키가 항목마다 달라 서버도 못 접는다 · 고름은 **제출과 독립**으로 나간다: 부모를 달면 WAV 가 못 올라가는 날 그날의 「무엇에 끌렸나」까지 죽는다) ⓑ장부 둘을 사유→파일·도달로(`도달0상한` 6→**5**) ⓒ`lib/학습자상태.js` ⑤관심축(고른것 분포·추천따름률·자리별·망설임 중앙·바꿈률·안고름 · `추정판` v1→v2). 보기가 `{option_id,label}` 이 되며 `선택판` v1→**v2**, 옛 문자열 보기는 **id=문구로 살린다**(리허설 배정 행이 그 모양이고 `task_snapshot` 은 불변이라, 새 모양만 받으면 그 행들의 선택지가 조용히 사라져 화면이 자유발화로 내려간다). ②-20 허용 목록에 **보기 한 층**을 냈다(게임 모듈이 보기와 «정답»을 한 벌로 낼 예정이라 그날 자동으로 막힌다).<br>🔑 **이 판이 잡은 것 = 「밀지 않은 날」은 계약을 원리상 못 지났다.** 1단계 회귀는 추천이 «있는» 갈래만 실계약에 먹였는데, S1-5 는 고정 보기 2개라 늘 `recommended_option: null` 이고 그 값이 **전건 거부**된다 — 즉 화면을 다 지어도 행이 **0**이고 그동안 모든 검사는 초록이었을 것이다. 처방 = `널허용` 에 `payload.recommended_option`(그 목록의 뜻이 정확히 「모른다/안 밀었다」와 「앱이 그 칸을 빠뜨렸다」를 가르는 것이고, **키는 여전히 필수**라 앱 결손은 그대로 거부된다). 곁들여 널허용 판정이 **공통 필수에만** 걸려 있던 것을 이벤트별 필수와 **같은 함수**로 합쳤다(같은 판정이 두 곳에 살면 갈라진다). 🚫 없는 추천을 지어 넣어 통과시키기.<br>~~🔴 남은 것 = 리허설 재배포 4종(`deliver`·`events`·`progress`·`tasks`)~~ → ✅ **해소 실측 08-12**(`local_1961779b`): 리허설 `deliver`·`progress`·`tasks`·`teach` 재배포 → `배포대조` **다름 0** · `배달왕복시험` **109/109** · `골든왕복시험` **43/43**. `events` 는 이미 「같다」라 안 밀었다(밀린 것은 넷이되 같은 넷이 아니다). **운영도 같은 4종을 밀어 다름 5→1** — 남은 1 = `correct`(살아있는 세션이 프롬프트 A/B 중이라 **일부러 안 밀었다** · F302). ⚠운영은 「섰다」까지만이다 — 실왕복은 리허설에서만 돈다(학생 0명). 기록으로 남길 위험: 배포 전까지 서버는 **옛 계약으로 판정하고 옛 모양 스냅샷을 만든다**(F313 · 내 화면은 초록이라 증상이 0이다). ⏳유호님 승인 자리이던 것은 **08-12 유호 확정으로 세션 자율**이다(운영 포함 · 정지선 둘 = 운영 데이터 삭제 · 실학생 행 들어온 뒤 쓰기).<br>✅ **별건 관찰 2건이 한 벌로 닫혔다 — 「안 골랐다」의 통로가 섰다**(08-10 `local_671a0bce` · talk `c090ef7`). 실측이 먼저다(조립기 출력을 검증기에 그대로 태웠다): `skipped`·`rejected_all` 둘 다 **`필수 누락: payload.position` 하나로 전건 거부** — 계약이 택1로 허용해 놓고 같은 계약이 막고 있었다. 🔑 **가드를 푼 게 아니라 자리를 옮겼다**: `널허용` += `payload.position`(키는 여전히 필수 — 「안 골랐다」와 「앱이 빠뜨렸다」가 갈린다) + 「골랐다고 적었으면 자리도 맞아야 한다」를 ⑦(**필드 이름 축**)으로 이사. 오히려 세졌다 — 값의 존재가 아니라 `options_shown` 의 **그 자리와 일치**해야 하고(0·-1·딴 자리 전부 거부), **안 골랐는데 자리가 적힌 행**도 막는다. ⑦ 에 둔 것이 판정이다: 그 검사는 사건이 아니라 필드 이름으로 걸려 **G2 의 `submission.created` 통로까지 덮는다**(`이벤트별필수` 에 두면 그 통로가 통째로 샌다 · 발주 §6-3).<br>🔑 **같은 판에서 함께 고쳤다 — 하나만 고치면 방어가 사라진다**: 택1이 `있음()` 으로 재서 **`false` 도 「있다」로 셌다**(`selected_option:null, skipped:false, rejected_all:false` 인 행이 통과 · 실측). 오늘까지 새는 양이 0이던 것은 `position` 필수가 그 조합을 **우연히** 막아서였고 이 판이 그 우연을 없앤다 → 택1 전용 `참인가()`(「실려 있나」가 아니라 「그렇다고 말했나」).<br>🔑 **생산자~소비자 한 벌**: 화면 `마침 && 보기 && 고른것` → `마침 && 보기`(보기를 봤는데 안 고른 앉음도 관측) · 소비자 `안고름` 한 수를 **무관심/거절**로 가름(합은 유지 · `rejected_all === true` 로만 센다) · `추정판` v2→**v3**(⑤축 **분모**가 바뀌었다 — `바꿈률`·`자리별`·`망설임_중앙` 이 전부 새 행을 포함한 분모 위에서 난다). ⚠ `거절` 생산자는 오늘 0(S1-5 에 「둘 다 아니에요」 버튼 없음 — G2 몫).<br>🔑 **새 가드가 그 자리에서 잡은 것 1건**: `tests/이벤트검증.test.js` 선택 픽스처가 `selected_option:'o2'`(둘째)인데 `position: 1` 이었다 — 검사가 스스로 거짓을 들고 있었다.<br>계약 JSON 개정 0(`position` 은 이미 `필드.선택로그` 에 있다 — **언제** 값을 요구하느냐는 검증기 층 판정이고 `lib/이벤트검증.js` 가 그 정본) · 마이그 0 · 새 event_type 0 · 회귀 +9 · 249/249(관련 5벌). ▶ 재배포 4종은 위 줄과 **같은 묶음**이다(동봉 신호가 같은 넷을 지목 — 새 차단자가 아니다). 🚫 0·-1 로 지어내기 · 🚫 「지금은 거부된다」를 회귀로 못박기 · 🚫 `안고름` 을 한 수로 되돌리기 · 🚫 `있음()` 으로 택1 재기 · 🚫 문구 문자열로 싣기(c6 에서 이미 정정 — `{option_id,label}`). 근거=[[mvp-eta-measured]]

- [ ] 〖지금〗 #Q48 **✅유호 승인 08-14 「오케이」 = P1 채널 게이트 → 표식 ⛔선행 ⇒ 지금** — ⚠**해석을 명시한다**: 이 줄의 유일한 잔여는 ③P3 배선이고 그 선행이 「P1 채널 게이트(⏳유호)」 하나였으므로, 「오케이」를 **채널 승인**으로 읽었다(다르면 한 마디 주시면 되돌린다 · 🚫재질의 조항 때문에 되묻지 않았다). ▶여는 순서 = **오버레이(P3) 착수 → 그 뒤 `quiz_round` 배선**(순서를 뒤집으면 「안 보여준 것을 보여줬다고 적는 행」이 남는다 — 아래 ⛔절). 🎫**송출·외부 발행은 별건 승인**(오버레이 «짓기» 만 자율). 원줄 ↓ **라디오24 Lane B — c11 소개정+승격기+소비자 «한 커밋»**(래칫 `생산자섰는데도달0상한=0` — S1-5 가 통과한 방식 talk `82d6802` 그대로).<br>✅ **코드 «한 커밋» 종결 08-12**(`local_4a7d40f9` · talk **`c757278`** · 계약 정본 as `1e08a981` · 설계 v1.3 `477bb8fd`=§4-4 c11 번호 정정+§4-5 집단 되돌림+§7-3 다이제스트 판정안+폴링 60초 확정(자율기록 #26) · 회귀 +26 · 스위트 1430/1430): 계약 c11(task_type +3 · `affect.reported` · 선호 규격 — **선언 칸은 `stated_option` 신명**: `selected_option` 은 선택로그 어휘라 ⑦ 검사(options_shown·position 한 벌)와 충돌 실측 · 퀴즈 payload 에는 position=학생이 친 자리를 그대로 싣는다) · CHECK 14개 `_c10`→`_c11` + `engine.skills` 시드 30(시드↔스킬표 텍스트 대조 회귀 · ⚠verdict CHECK 는 한 문장 분리 — 검수확정 회귀 추출이 「));」 종결을 문다, c12 작성자 주의) · `lib/라디오승격.js`(판정층 순수 — 링크·동의·학생×라운드 첫응답·마감·답해석·결정적 correlation=학생×몽골날짜) + `radio-promote` Fn(service_role · c11 게이트 · skill 실재 대조 · 분모 응답 · 제외는 표 없이 사유별 셈 — 이력에서 재산출 가능이 근거) · 소비자 학습자상태 **v5**(③자기인식=확신 표기·선호=차원별·정서=빈도 — **도달0상한 5→3 래칫 하향**) · `lib/라디오편성.js`(§4-5 — 미노출 우선 1차·w=1+오답률·표본하한 5·난수 없음).<br>🔴 **분리 반박 1패스 완료 08-12 — 치명 7·경미 8**(정본=`docs/_ops/심문결과/라디오LaneB-c757278-반박.md` — 「공격 실패」 목록 포함이라 그 렌즈들은 재검증 불요). 배포 게이트 최소 셋: ①리듬·끈기축이 라디오 제출(`목표선언`·`자습체크인`)을 **앱 숙제로 센다**(deliver 가 개입 행에 스탬프 — 소급 불가) ②앱 계약판 c11 이 c10 DB 상대로 **전건 426**(수리 전 앱 빌드 금지 · `tests/계약.test.js` lockstep 이라 단순 되돌림 불가 — 검사 축 판정 필요) ④`limit 5000` 굶주림(창 넘치면 영구 손실). 나머지 치명=③스냅샷 무검증 ⑤정렬 없는 링크·카드 귀속 ⑥원신호 상한 500 ⑦cron 부재(승격기를 부르는 것이 0).<br>🔴 **잔여(순서 재판정 08-12)**: ⓪✅**수리 3회전 종결 08-12 — 리뷰어 「새 결함 0」**(`local_3d3af5e4` · 1차 talk **`8f0e35f`**=치명7·경미8(`radio-promote.v2`+`학습자상태.v6`+`lib/라디오태스크.js`+`DB착지판.json`+cron 조각) → 재반박 새 치명 2 → 2차 **`7b05e56`**(라디오 제출 fetch 전 제외·시간예산 커서) → 3차 **`18eb83f`**(2차가 심은 SQL 3값논리 — task_type NULL 앱 제출 증발 수리) · 처분 전량=반박 md · 1442/1442) ①⏳유호=c11 마이그 **2조각**(engine+cron_radio — 리허설은 cron 제외 정책) 적용+`radio-promote` 신규 배포+동봉 밀린 재배포(F313 — 동봉신호 7종: corrections·deliver·events·radio-promote·review·tasks·teach)+**적용 확인 커밋에서 `DB착지판.json`·`src/계약판.js` 를 c11 로**(회귀가 짝을 묶는다) — **수리 커밋과 한 묶음으로 승인** ②§9 승격 왕복 도구(배포 뒤 — 부르기 인자화·링크/라운드/동의 시드) ③P3 라운드 생성기 ↔ `라디오편성.추첨가중` 배선(경미 10 의 해소 자리 — 입력 「오답」 생산자 판정 포함). c11 = ①`task_type` +'라디오퀴즈'(✅유호 §7-5) ②+'목표선언'·'자습체크인'(ⓒⓓ) ③`preference.stated` payload 규격(ⓐⓑ — 계약이 「첫 생산자가 정한다」고 예고한 자리) ④`event_type` +`affect.reported`(ⓔ · ⚠**14번째 사건이라 `도달0상한=5` 래칫에 걸린다 — 도달까지 동봉 필수**). 절차 = 계약 판올림 + `tools/계약동기화.js`(두 저장소 같은 바이트) + 살아있는 CHECK 전량 `_c10`→`_c11` 개명(c7 접미 통일 선례 · radio 쪽 `broadcast_segment_kind_c10` 포함). 승격기 = talk `supabase/functions/` 내부 생산자(§4-3 표 — `sent_at` 시점 활성 링크·동의 조회·학생×라운드 첫 응답 1건·`skill_ids`·`confidence`·60초 재도전 `retry_of_event_id`). 🚫엔진이 원장 직독(원칙 12) · 🚫상한 올려서 풀기(래칫은 내리기만).<br>➕**「집단 되돌림」 방향 채택 — Lane B 설계에 반영할 것**(유호 08-11 · 철학정합 §3-D · `local_339b2c3e` 등재): 승격기 설계에 **집단 되돌림 규격** — 링크된 시청자들의 약점 분포로 라운드 문항 가중(개인 문항은 1:다 방송 구조상 불가 — 집단 단위가 정직한 한계). 엔진→라디오 되돌림 경로 0(문항 회전=재고 기준)의 해소 자리. ➕같은 커밋의 판정안 자리 = **질문 다이제스트 자동화**(§3-E-1 — AI 선별 자동 확정+다음날 원장 거부권 1클릭 · 「매일 클릭」 상시 손 해소 · 남길 이유=큐레이션 질 — 판정안으로 유호님께).<br>✅**잔여 ①·② 닫힘 08-13 `local_867298eb`**: ①한 묶음은 08-12 `388660c5` 가 마이그 9조각+11종을 운영에 부었고(착지판 c11) **라디오 2종만 빠져 cron 이 매시 404**(48h 6건 실측) — radio-promote 운영 배포로 수리, **01:21 틱 200 실측**(채널 게이트는 «송출»이지 승격 회로가 아니다 · 리허설 잔존 cron 은 조각 정책대로 unschedule) ②`tools/라디오승격왕복시험.js` 신설 **33/33**(원장 16→승격 6 · 제외 10사유 각 1 — 재도전 쌍·correlation 결정성·하루접힘·링크겹침·멱등 실측). 남은 것=③P3 배선(오버레이와 한 벌)·radio-promote 재배포 1회(시각 대조 — G트랙 게임스냅샷 커밋 뒤).<br>🔍**확정대조 판정 08-13 `local_67ac851b`: ⏳유호 표식이 낡았다 → 〖지금〗** — 유호 몫이던 ①(c11 마이그+배포 «한 묶음» 승인)은 08-12 `388660c5` 운영 붓기 + 08-13 radio-promote 운영 배포(`867298eb`)로 닫혔고, 잔여 둘은 세션 몫이다: ⓐradio-promote 재배포 1회 — 선행이던 G트랙 게임스냅샷 커밋(talk `575413b`)이 08-13 착지했고 talk HEAD `6066d51` 이 동봉 2줄도 닫아 **지금 가능**(단 8c60cc61 의 4Fn 재배포 선언과 시각 조율 · F302) ⓑ③P3 배선(오버레이와 한 벌 — 송출 «켜기» 아님 · 채널 게이트는 송출만 막는다).<br>✅**ⓑ 계산 사슬 착지 08-13 `local_deb526ff`**(talk **`37f0486`** · 회귀 +18 · 스위트 **2010/2010** · 변이 **6/6** 물림): 착수 실측이 문서와 갈렸다 — `추첨가중` 의 **호출부가 회귀 하나뿐**이라 「되돌림 경로 0 해소」로 적힌 자리가 실은 아직 0이었다(게이트 ④ 닿았나 ✗ · 계산기는 08-12 에 다 지어졌다). ①**「오답」 생산자 판정 종결** — 정오는 저장값이 아니라 **파생값**이다: 계약이 `is_correct` 를 막았고 승격 행이 싣는 것은 «학생이 고른 것»뿐이라(`payload.selected_option`) 정답은 `submissions.task_snapshot.정답` 에 있다 → `라디오편성.승격요약(행들, {기준시각, 창일수=28})` 이 그 두 칸을 맞댄다(둘 다 엔진 산출층이라 원장 직독 아님). **못 가르는 행은 «응답»에도 안 넣고 뺀 사유별 셈을 같이 낸다** — 맞음으로 접으면 오답률이 조용히 낮아지고 오답으로 접으면 높아진다(둘 다 편성을 기울인다). 기준시각은 인자다(여기서 현재 시각을 읽으면 회귀가 돌린 날에 따라 갈린다). ②**`lib/라디오라운드.js` 신설 = 추첨가중의 첫 실호출부** — 난수 대신 **씨앗 sha256**(결정적이라 회귀가 분포 아닌 값으로 잰다) · 후보는 `문항id` 정렬 후 누적(정렬을 빼면 같은 씨앗이 날마다 다른 문항을 뽑는데 **증상이 없다**) · `제외`+`축한정` = 재도전 짝(같은 skill 다른 문항 · 스키마 `retry_of_round_id`). ⚠변이 시험이 처음 **4/6** 이었고 못 잡은 둘이 소득이다(씨앗 1개짜리 순서 테스트·중복축 픽스처 0). 🔴**잔여 = 마지막 한 칸** — 문항 팩·노출 이력을 읽어 `radio.quiz_round` 에 적는 Fn(P3 · 오버레이와 한 벌). **그 칸이 서기 전까지 게이트 ④ 는 여전히 「닿았나 ✗」다** — 이 착지는 사슬을 닫았지 도달을 완성하지 않았다. 🚫송출 켜기(채널 게이트).<br>✅**ⓐ radio 2종 «운영» 배포 종결 08-13 `local_97a424b2`**(유호 지시 「전부 한번에 잘 진행」 · 구 줄47 ⓐ 「radio-promote 재배포 1회」가 이 자리다): `radio-ingest` **v3**·`radio-promote` **v4** ACTIVE(판 대조 ✅ c11) — 운영 배포대조 **다름 3→1**(남은 1=roster-ingest=반피드백 트랙 ⏳유호 보류 · 리허설은 0). 🔑 **이 층은 그전까지 «미측정인데 초록»이었다** — `배포대조.js` 가 `--운영` 을 조용히 무시하고 리허설을 다시 재고 있었다(F400 · 봉인 talk `7f312ae` · 회귀 4·변이 2/2). F302 조율 실측 = HEAD `37f0486`(P3 배선)이 만진 `라디오라운드.js`·`라디오편성.js` 는 **어느 Fn 동봉에도 안 든다** + 그 주인 `deb526ff` 사망 + HEAD 스위트 2010/2010. ⚠**매니페스트 일치는 「돈다」의 증거가 아니다** — 운영 cron 틱 200 은 이번에 안 쟀다(승격기 실호출은 사건을 쓰므로 일부러 안 침). 근거=[[radio24-design]]·[[deploy-target-gate]]<br>⛔**표식 재판정 08-13 `local_ffb86589` — 〖지금〗 → 〖⛔선행〗**: 잔여 셋 중 ⓐ·ⓑ 는 닫혔고(ⓐ=`배포대조 --운영` 전량 재실측 **다름 1** — radio 2벌 포함 ✅같다 · ⓑ=talk `37f0486`), **남은 ③P3 배선만 열려 있는데 그것이 지금 못 선다**. 근거 둘: ①설계 §4-5 말미가 「라운드 생성기가 그것을 부르는 배선은 **P3(오버레이와 한 벌)**」인데 오버레이는 **저장소에 코드 0벌**이다(`grep -rl 오버레이` = 주석·문서뿐 · 실측) ②`radio.quiz_round` 는 「그때 **화면에 나간** 문항의 스냅샷」이고 `shown_at` 이 그 시각이라, 송출·오버레이 없이 적으면 **안 보여준 것을 보여줬다고 적는 행**이 된다 — 「부르는 곳 없는 생산자」를 한 층 위에 또 세우는 자리다(래칫 `생산자섰는데도달0상한=0` 이 사건 층에서 막는 바로 그 모양). ▶여는 조건 = 오버레이(P3) 착수 · 그 선행 = P1 채널 게이트(⏳유호 · 🚫재질의).
- [ ] 〖지금〗 #Q49 **✅유호 08-14 「심문 한도 채웠어」 → 표식 ⛔선행 ⇒ 지금** — 차단자였던 벤더 한도가 풀렸다. ▶**다음 = 10회차 심문을 설계 v5.6 에**(9회차 전건 처분 종결·v5.6 착지가 08-14 에 끝나 있다 — 근거 메모리 [[task-selection-by-state]]). ⛔**구현 착수 차단은 그대로**(심문 게이트를 통과해야 열린다).<br>⚠ **이 줄이 통로를 잘못 적었었다 — 08-13 재판정이 경고한 그 혼동이 하루 만에 재발했다**(`local_950cd77e` 08-14): 줄머리가 「집는 세션이 `/fable심문` 으로 돌린다」로 적혀 있었는데, **처분표 v9 의 ▶다음은 `codex-review.js --심문`(luna×2+sol×1)** 이고 1~9회차가 전부 그 통로다. 이름이 겹치는 두 통로를 뭉치면 회차 «간» 대조가 원리상 무효다(F281 — 그 대조가 이 게이트의 존재 이유). ⇒ 두 통로를 **둘 다** 돌렸고 장부도 둘로 갈랐다: <br>  · ✅**fable 판 완주 08-14 `6b18e681`** — 적힌 대로 `/fable심문` 범위③(대상지문 `4a98a0a91252` · 금지 `ee763135f508`) · **P0 3 · P1 6 · P2 5** · 결과 `docs/_ops/심문결과/상태기반_과제선택_설계-4a98a0a91252-fable-10.md` · 장부 `심문기록_fable.jsonl`(fable 회차 **1** — 이 통로로는 첫 판이라 회차 «간» 대조 재료가 아직 없다). **P0 셋 전부 실측 확인**(보고 전 실측 규약): ①마감 게이트(B3)가 구제 경로를 벽돌로 — `jobs_claim` 이 06:00 뒤 0행인데 구제가 그걸 부른다(`:1483`·`:1487` vs `:1542` · `_mode` 열거 실측 = 정상·마감·적재 셋뿐, 구제 모드 없음 · 🔴소급 불가) ②A8 draft 상태 메타 동결 × ㉤④ degraded 갈래표(`:1312~1317` vs `:1469~1473`) — 🔑실측 교정: 「합법 경로 부재」가 아니라 `:1311`(성공하면 draft 안 쓰인다)과 A8(어느 모드든 draft 값)이 **같은 절 안에서 배타**다 ③F5 의 corrections 통로 키 `parent_event_id` 가 실물에 **0회**(`SYNK-talk/src/교정API.js` 봉투 칸 7개·`functions/corrections/index.ts` 둘 다 0 — 설계가 근거로 든 `성과회수.js:36~40` 은 `content.viewed` 얘기다) ⇒ 조인이 영원히 0행. 기각 장부 46건 대조 재탕 0.<br>  · 🔵**codex 10회차 던졌다 08-14** — 런 `심문-2026-08-14T1332-cf896f`(대상 v5.6 · 한도는 유호님 08-14 「심문 한도 채웠어」로 풀림). 세션 밖에서 돌고 **결과는 다음 세션 시작 훅이 알린다** — 던진 세션이 죽었다고 **다시 던지지 마라**(같은 판을 두 번 태운다).<br>▶**따라서 다음 = ⓐcodex 런 수거 → ⓑ두 판(codex 원건 + fable 14) 합집합 처분표 v10 → ⓒ설계 v5.7 집행.** 🚫모델에게 병합시키지 않는다(키 = 파일+제목 합집합 · 등급 갈리면 나쁜 쪽 승). 원줄 ↓ **상태 기반 과제 선택 — ②집행(설계 v5.4) — 처분표 v7 채택분 40갈래**(⛔구현 착수 차단 · 표식 08-13 `eaa21f3f`: 지금 집을 것이 **없다** — 7회차 런이 세션 밖에서 도는 중이고, 결과가 떨어지면 **SessionStart 훅이 알린다** ⇒ 그때 〖지금〗으로 돌린다). 🔴 **설계 v1 은 심문 게이트에서 떨어졌다** — 3벌 **만장일치 「동결 불가」**(원 지적 53건 → 중복 접어 20갈래 · 채택 P0 11·P1 10 · 기각 4는 처방만이고 알맹이는 채택). **처분표 정본 = `docs/_ops/심문결과/상태기반과제선택_처분표.md`** · 원문 3벌은 같은 폴더 `...-9de9a33e6e29-{luna-1,luna-2,sol-3}.md`. ✅ **v2 착지 08-12** (`dc3969a2` — 채택분 전건 반영: 통로 역전(선판정→예약→조건부 생성)·게임 갈래 편입·`생성결과` 9종·입력 스냅샷·`model_ref` 철회→정본 칸·arm 학생별 30일 고정·생성 예산(순차 루프 보호)·상태없음=생성금지·검문 판정표·폴백 2단 고정·인수 조건 10). 🔴 **재심문 종결 08-12 — v2 도 3벌 만장일치 「동결 불가」**(`local_d9336f79` · 지문 `8323cf86c665` · 원 지적 53건 P0 30 → 중복 접어 23갈래 · **처분표 정본 = `docs/_ops/심문결과/상태기반과제선택_처분표_v2.md`** · 원문 3벌 같은 폴더 `...-8323cf86c665-*`). 조건 통제 실측 ✅ 금지·방향 지문이 3회차 동일(F281). 🔑 **v2 는 후퇴가 아니다** — v1 20갈래 중 되돌아온 것은 3뿐이고 지적의 층이 「장이 통째로 없다」→「규격이 안 닫혔다」로 갈렸다. 🔴 **v3 의 축 = 닻 옮기기**: 3벌이 **독립으로 같은 대안**을 냈다 — 새 메타를 `task.assigned` 에만 두면 성과 소비자(`성과회수.js`·계기판)가 `intervention.delivered` 를 읽으므로 **효과 소비자에 영원히 안 닿는다**. 실측 5/5 사실 + **v2 자신의 실측 1건이 거짓**(`출처` 는 이미 `submissions.task_snapshot.호흡[].출처` 로 착지한다 — sol 단독). 단독 지적 4건(sol 2·luna-2 2)이 또 값했다. ▶다음 = **v3 개정**(처분표 v2 §5 순서) → 그 뒤 재심문. ⚠**고친 판 ≠ 통과한 판** · 🚫 v2 상태로 구현 착수(지적 중 5건은 코드로 확인된 **사실**이다: `deliver` 가 `오늘과제()` 보다 **먼저** `게임배정()` 을 돈다 `index.ts:369`·`:436` / 상태는 5축이 아니라 성향 5축+축밖 3이고 `estimator_confidence` 는 **표본 충분도**다 / `:326` 순차 루프라 한 호출이 멈추면 뒤 학생 전원 미배달 / `payload.model_ref` 는 계약의 최상위 `model`·`prompt_ver` 를 **우회**한다 / `출처` 는 「이미 행에 남는 칸」이 아니다 — `source_kind` 는 뜻이 다른 enum 이다). ✅**㉠ 유호 확정 08-12 「㉠으로 개정해」 → `P0_제품계약.md` v3.6 개정 완료**(talk `b35bc7c` — `학습자상태` 축 요약이 셋째 입력 · 조건 3 동봉: 🚫문턱 분기(상태는 짓는 재료지 고르는 잣대가 아니다)·🚫①②우선순위 변경·🚫오류태그를 입력으로 · ⛔SRS 조항과 옛 근거 원문은 그대로 유지) · 🚫재질의. **따라서 착수를 막는 것은 이제 처분표의 P0 10·P1 10 갈래뿐이다** — v1 §8 의 「㉢을 골라도 구현은 시작할 수 있다」는 자기 §5-1(판지문이 요약 조립 규칙을 해시한다)과 모순이라 철회됐다.<br>⚠ **심문 통로는 둘이고 이름이 겹친다 — 문서에서 같은 낱말로 쓰지 않는다**: **②설계 심문** = `node tools/codex-review.js --심문 <문서>`(luna×2+sol×1 · 배포 파이프라인의 상시 층 · 세션이 돌린다 · 이 게이트가 그것이다) / **페이블 재심** = `/fable심문` 스킬(Fable · **유호님이 부르실 때만** · 08-12 현재 실행 0회). 옛 문구가 둘을 뭉쳐 「게이트를 열 방법이 세션에 없다」로 읽혔다(F103 꼴). 채우는 자리는 **하나** = P0 §6-1 셋째 갈래(급수·목적으로 문장 1개 생성) — 오늘 라이브의 모든 평일 배정이 `degraded=true` 인 이유가 그 자리가 비어서다. ①②(첫날·교정문)는 계약이 정한 소급 불가 자리라 안 건드린다. 물건 셋 = `lib/과제요약.js`(순수·비식별)·`lib/과제생성.js`(벤더 호출 유일 자리)·`lib/과제검문.js`(순수·길이/급수/금칙/식별자 역유입) + `lib/오늘과제.js` 갈래 넷→다섯(생성은 **교정문보다 뒤** — 앞에 두면 ②슬롯 규칙이 깨진다) + 판올림 `오늘과제.v4`. 🔑 판지문의 대상을 「출력」에서 「입력 조립」으로 옮긴다(생성 출력은 비결정이라 행동 지문을 못 찍는다 — 프롬프트 템플릿은 `prompts/과제생성.md` 파일로 두고 해시). 🚫`policy_ver` 에 모델 이름(별도 `payload.model_ref`) · 🚫상태 5축 문턱 분기(불변식 5 죽는 쪽) · 🚫검문 실패 재시도. ⏳유호 2 = P0 §6-3 「입력 둘뿐」 개정 여부·대조군 시점(⚠**㉢을 골라도 구현은 시작할 수 있다** — 요약 모듈 인자 하나가 갈릴 뿐). ▶후속(별건) = **말하기 문장 팩**(`contents/` 넷 중 말하기 팩만 없다 — 있으면 1단 실패일의 품질이 「어제 것 재탕」에서 오른다 · 사람이 문장을 채우는 콘텐츠 일감이라 이 트랙 인수 조건에 넣지 않는다).<br>🔍**낡음 정정 08-13 `local_67ac851b` — 〖남이점유〗아님(점유 세션 전부 마감) · 상태는 v2 가 아니라 v5.1 이다**: v3 `e3cf9614` → v4.1 `d58614ad` → **v5.1 §12(18·19·20) 착지 = 착수 차단 «해제»**(⚠해제≠초록 — 픽스처 0벌 실행 · 정본=memory). 5회차 심문(F281 도구 조립 · `5f7e5d76`)은 던졌으나 **GPT 한도 소진으로 미실행** — 런 `심문-2026-08-13T0104-87f15a` 종료코드 2 · 처분 도장 08-13 「통과 아님 · 재던지기=한도 리셋 후」. ▶~~잔여 = 8-18 GPT 리셋 후 5회차 재던지기~~ → ✅**5회차 «완주» 08-13**(수거 `local_4bbeced4` · 런 `심문-2026-08-13T0344-f769e2` `--한도무시` · 장부 완료 3행 04:03Z): **3벌 «동결 불가»** — 원 지적 82건(P0 56 · luna-1 18·luna-2 25·sol-3 39) · 결과 3벌 커밋 `docs/_ops/심문결과/...-f0b56546c486-*`. ✅**게이트 ①판정 종결 08-13 `local_5c55e5f5`**(`73f2ac9b`+`0ddccfb7` — **처분표 v5 = `docs/_ops/심문결과/상태기반과제선택_처분표_v5.md`** · 원건 **82/82 기계 매핑**(빠짐·중복·잉여 0) · 46갈래 ✅38·◐7·❌1 · 실측 10건이 심문자 주장 셋을 정정: **실물 `학습자상태.v9`·11축**(v5 의 「v7·9축」 정정 자체가 낡음 · 판번호≠축수) · **`'폴백'` 리터럴 저장소에 없음** · `deliver-one`·`generation_*`·c12 **0매치**). ✅**게이트 ②집행 «종결» 08-13 `local_34297acc`**(`e16fbb45`+`58b782af`+`f1564834`+`4e745055` — **설계 v5.2** · 45/45 기계 대조 · §3-5-b 전면 재작성(RPC 3→5 `attempt_open/close` · CHECK 4 · FK 2 · **식별자 전면 영문화**(값은 한글) · `branch_snapshot` · `not_target_reason` · 반환 계약 3행) · §8-B **축 5→7**(⑥정확성 ≥0.95 · ⑦목표 활용 ≥0.7)+I2 실행층·I3 셀 보고 · **§3-2-a cron 3잡 배선**(`generate-worker`·`generate-deadline` 신설) · §12-21 신설 · §13 에 🚫8 · 머리말·§16-1 게이트 재작성). 🔑집행 중 잡은 것 = §13 의 「🚫회수에서 펜싱토큰 안 올리기」가 이번 판 A5 처방과 **정면 반대**라 철회 — 형제 절 미수거가 v5 낙방의 뿌리 그 자체다. ▶**다음 = 6회차 심문을 v5.2 에 던진다**(F281 «그 시점의 현재 판» · 🚫v5.1 지문 고집) → 수거 → 전건 처분. ⛔구현 착수 차단 유지 · 🚫처분표 §6 재제안 8건. 근거=[[task-selection-by-state]]<br>🔍**표식 재판정 08-13 `local_ffb86589` — 〖지금〗 → 〖⏳유호〗**: 이 줄의 ▶다음이 「**6회차 심문**을 v5.2 에 던진다」인데 심문 통로 `/fable심문` 은 **유호님이 모델을 이름으로 부를 때만** 도는 별도 통로다(CLAUDE.md 실행 절 · 스킬 설명 「유호님이 원할 때만」). 세션이 자기 판단으로 편성하면 F281(회차 간 대조 무효)에 걸린다. ⛔구현 착수 차단도 그대로라 세션이 지을 것이 이 줄에 0이다.<br>🔍**재판정 뒤집음 08-13 `local_0c6fbc37` — 〖⏳유호〗 → 〖지금〗**(윗 판정을 지우지 않고 병기 — 갈린 지점을 남긴다): 윗 판정은 **이름이 비슷한 두 통로를 하나로 봤다.** 5회차를 실제로 돈 것은 `/fable심문`(유호님 전용)이 아니라 `tools/codex-review.js --심문` 이다 — `docs/_ops/심문기록.jsonl` 마지막 3행이 회차1·2=`gpt-5.6-luna`, 회차3=`gpt-5.6-sol`(04:03:31Z)이고, 이 편성은 `node tools/모델정책.js` 의 «②심문 편성» 고정값과 **글자 그대로 같다**(fable 통로였다면 모델명이 fable 이어야 한다). CLAUDE.md 실행 절도 둘을 갈라 적었다 — 「예외=유호님이 모델을 이름으로 부르는 `/fable심문` · **GPT 검수·심문 편성**·제미나이 픽은 **유호님 고정값 그대로** `tools/모델정책.js`」. 즉 세션이 모델을 고르는 것이 아니라 유호님이 박아 둔 편성을 그대로 돌리는 것이라 F281 에 안 걸린다 — F281 이 금지하는 것은 «회차 사이 입력이 갈리는 것»이고, 그래서 진짜 처방은 통로를 막는 것이 아니라 **회차 도는 중 `MEMORY.md`(금지목록 원천) 편집을 얼리는 것**이다. ⛔구현 착수 차단은 맞지만 이 줄의 ▶다음은 구현이 아니라 문서 층(6회차 심문→처분표 v6→설계 v5.3 집행)이라 그 차단의 대상이 아니다.<br>✅**6회차 «전건 처분» 종결 08-13 `eaa21f3f`** — ①판정 `a592476b`(**처분표 v6** · 원건 **60/60** 매핑 · 36갈래 채택24·부분8·기각4) + ②집행 `b2c5f42c`(**설계 v5.3** · 채택 32갈래 착지). 🔴최대 발견 = §6-0 H1 이 「(실재)」라 단정한 **급수→기술 매핑이 실재하지 않았다**(`문법급수.js` 는 G문법ID→급수 · `engine.skills` 는 `skill-ko-*` 30행 · 안 겹친다) ⇒ 원천을 `engine.skills`+회전으로 갈아끼우고 `lib/기술선택.js` 를 선행 작업으로. 곁들여 `_event` 의 **가짜 `submission.created` 제거**(실측: `engine.submissions` 에 `event_type` 열 없음 — 그대로 뒀으면 리듬 축 전량 오염) · 회수 RPC 신설 · `outcome` CHECK · 복합 FK · ④완결 「늘었나」를 ✗ 로 정직화. ~~▶다음 = 7회차 수거 → 처분표 v7~~ → ✅**7회차 «수거 + ①판정» 종결 08-14 `3bc38188`**: 수거 `8d46148e`(3벌 + 장부 · 대상지문 `09400290eafe` = v5.3 직접 해시 대조) + **처분표 v7 `7fa27627`**(정본 = `docs/_ops/심문결과/상태기반과제선택_처분표_v7.md` · 원건 **69/69 기계 매핑** 빠짐·중복·잉여 0 · **40갈래** 채택 33·부분 7·갈래층 기각 0(처방층 1=A4) · 3벌 만장일치 **10**). 🎫F411 = ✅수리 `823a462b`+해소 `8339f8fd`(회귀 4·변이 3/3). 🔴**실측 7건 전부 «지적이 맞다»**(5·6회차와 갈린다 — 심문자 정정 0). 최대 발견 = **`domain='grammar'` 는 30이 아니라 18**(문법18·표현5·어휘7=계30 · 두 시드 동일) — v6 실2 의 「30행」은 표 «전체»라 그 문장 안에선 참이었는데, v5.3 이 좁힌 자리로 옮겨 적으며 거짓이 됐다(6회차가 갈아끼운 원천의 크기를 다시 안 열어봤다). 곁들여 `array_length('{}',1)=NULL` 로 검문 CHECK 양방향 누수(저장소는 이미 `cardinality()` 를 쓴다)·`jobs_winner_present` 가 평시 폴백 전량 거절(뜻은 outcome, 글자는 status)·`estimator_version` 이 attempts 에만 없어 탐지기가 4중 3·§5-4 「라이브 그대로 false」가 실물과 반대(`오늘과제.js:299~303`). ⚠**회차 «간» 조건 대조는 이 판에서 «불가»**(F411 — 던짐↔완료 짝 9 중 6 갈림 · 6회차는 던짐 철학 v1.7 → 완료 v1.8 이라 7회차와 「동일」로 읽힌다 · 고친 것은 다음 회차부터 산다). ▶**다음 = ②집행(설계 v5.4)** — 순서는 처분표 §5(물리 A군 → 봉투 B군 → 경로 C군 → 재현 D군 → E·F → G2 이관은 맨 마지막). ⛔구현 착수 차단 유지 · ⏳유호=E2(급수→기술 매핑을 이 트랙이 지을 것인가 — 결정큐 기존 줄).<br>✅**7회차 ②집행 «종결» 40/40 — 설계 v5.4 착지 08-14 `local_e1abd26f`**(커밋 5벌: `455eea05` A군9+B군4 · `f9a1d5d9` C군5+§12 눈금4 · `cd2f3e6c` D군7 · `8dc0967b` E군3+F군4+G3 · `34e07b9a` G2 이관+머리말·게이트 갱신). **①판정 ✅(69/69·40갈래) + ②집행 ✅(40/40) = 이 회차분 전건 처분 종결.** 굵직한 것 넷 — ⓐ**RPC 여섯→아홉**(`jobs_release`·`jobs_finalize_due`·`jobs_load_one`): 셋 다 「전이표에 그려 놓고 함수가 없던」 **같은 형태**라(V6-1 이 회수에서 겪은 것) §12-25 가 전이표↔시그니처를 **양방향 기계 대조**한다 — 구현 착수 «전»에도 도는 유일한 종류의 검사다 ⓑ**`적재실패` 상태 + `generation_batch_runs` 신설**: 한 학생 조립 실패가 배치 전체를 죽이던 자리(A9)와, 「돌았는데 0」·「안 돌았다」가 같은 얼굴이던 자리(D8 — §3-6 의 「재적 분모 0이면 배치가 죽은 것이다」는 **자기모순**이라 철회) ⓒ**봉투 전량 표**(B1): `learning_events` 의 `not null` 칸을 실측해 채웠다 — 그 블록만 보고 구현하면 **첫 insert 부터 죽었다**. 곁들여 `task_type` 이 `'숙제제출'`(게임 통로 값)이던 것을 실물 `'발화녹음'`으로(B2 · `deliver:118`) ⓓ**판지문 재료 8→12**(D2 — 여덟은 «생성»만 묶고 «마감·분류»를 안 묶었다) + `ingested_as_of` 경계를 **조회 층**으로 내림(D1 — v5.2 의 배선은 한 층 위 SQL 이 `now()` 로 먼저 자르고 `ingested_at` 을 안 실어 **검사할 값 자체가 없었다**). 🔴**앞 세션이 넣은 여섯 중 «둘이 반쪽»이라 이 판이 마저 닫았다**(새 갈래 아님 · 「집행했다」가 「돈다」는 아니다): A2 의 `cardinality` 쌍조건은 「성공+빈 배열」을 여전히 통과시켜 **A2 자신이 요구한 픽스처 2벌 중 둘째가 원리상 못 지나갔고**, A3 은 `estimator_version` 을 `not null` 로 세우고 `attempt_open` 시그니처에 인자를 안 열어 **첫 insert 가 불가능**했다. 🔑**G2 이관** = 머리말 판 역사 두 절 + §15·§16 대조표를 `memory/지도.md` 로(복제가 갈림의 원천이고 이 판의 거짓 셋이 전부 그 형태였다 · 금지목록 §13 과 §16-1 현황표는 **안 옮겼다** — 현행 기능이다). ✅**8회차 «던졌다» 08-14 `local_f8fd4dd0`** — 런 `심문-2026-08-14T0028-ac77d5`(대상 **v5.4** · F281 «그 시점의 현재 판» 지킴 · 🚫v5.3·v5.3-a 지문 고집). ▶**다음 = 수거**(`--런목록` → 결과 읽고 `--런처분`) → 전건 처분. ⚠던진 런은 세션이 끝나도 살아 있고 **결과는 다음 세션 시작 훅이 알린다** — 던진 세션이 죽었다고 다시 던지지 마라(같은 판을 두 번 태운다). 🔑 **8회차는 F411 수리(`823a462b`) 뒤 첫 회차라 회차 «간» 조건 대조가 처음으로 성립한다.** ⛔구현 착수 차단 그대로 · ~~④완결 ✓✓✗~~(8회차 G1 이 ✗✗✗ 로 뒤집음) · 🚫§13 v5.4 증보 7건 재제안.<br>✅**8회차 «수거+①판정» 종결 08-14 `58406896`**(죽은 `f8fd4dd0` 승계 · 결과 3벌 `39d69237` + **처분표 v8 `8bbe9bcc`** — 원건 **64/64** 매핑 · **46갈래** ✅37·◐9·갈래층 기각 0 · 런 도장 ✓). 🔑 **던짐↔완료 조건지문 6/6 동일 — F411 수리가 처음 성립**(회차 «안» 통제가 장부로 증명 · 9회차부터 «간» 대조 가능). 🔑 실측 = 컨텍스트 독립 3벌 위임(줄번호 동봉) — 굵은 확정: **B6** 기술 회전 입력이 소비(적재 인자)보다 늦게 읽힌다(sol 단독) · **F4** §12-3 이 약속한 「§16-1 별도 항」이 §16-1 에 없다(약속 미집행 실측) · **G1** ④완결 **✗✗✗ 통일**(「설계상 ✓」 폐기 — 철학 ④ 원문) · ➕심문 밖 발견 1(`:1690` 한글 열명 잔재). ~~▶다음 = ②집행(설계 v5.5)~~ → ✅**8회차 ②집행 «종결» 46갈래 — 설계 v5.5 착지 08-14 `185657ef`**(커밋 5벌: `212f7a8c` 물리 1~2단계 · `c2d3bd98` 경로·배선 3~5 · `97f281b4` 판·평가 6~9 · `fcd0900c` 계약·인수 10~11 · `6f69621f` 머리말·G1·G2 동승 12). **①판정 ✅(64/64·46갈래) + ②집행 ✅ = 8회차분 전건 처분 종결**(처분표 v8 §7 도장). 굵직한 것: ⓐ규정 네 곳이 참조하던 두 열이 DDL 에 «없었다»(A1 3벌 만장 — `event_draft`·`load_error` 실물화·«전량» 필수 · `적재실패` 최소 봉투 A3 는 자기 not null 에 걸려 원리상 못 서던 채) ⓑ`batch_runs` 시작 행을 **별도 트랜잭션**으로(A4 — 같은 트랜잭션이면 롤백이 「죽었다」의 증거를 같이 지운다) ⓒ겨냥 이력 조회를 **오케스트레이터 적재 «전»**으로 재배선(B6 — 회전 입력이 자기 출력(적재 인자)보다 늦게 읽히던 역전) ⓓ차단기 축 교체 `policy_ver`→`skill_taxonomy_ver`(E2 — 배관 커밋마다 80회 사람 재채점이 서던 자리 · 조립 변경은 `input_hash` 가 잡는다) ⓔ**④완결 ✗✗✗ 세 자리 통일**(G1 — 「설계상 ✓」 폐기 · 보드 상태 칸 포함) ⓕ재실행 가드를 DDL 본문에 직접 삽입(F3 — §12-21 요구를 본문이 안 걷어 복사한 구현이 재적용에서 즉사하던 자리). ~~▶다음 = 9회차 심문을 «v5.5» 에 던진다~~(`node tools/codex-review.js --심문 docs/상태기반_과제선택_설계.md --던지기` · F281 «그 시점의 현재 판» · 🚫v5.4 지문 고집 · 🔑**회차 «간» 조건 대조가 8회차와 짝지어 처음 성립하는 회차다** — 처분표 v8 §1). ⛔구현 착수 차단 유지(새 회차는 다시 전건 처분을 지난다) · ⏳유호 E2 그대로 · 🚫처분표 v8 §6 재제안 10건(설계 §13 v5.5 증보에도 등재).<br>✅**9회차 «던졌다» 08-14 `local_a4946442`** — 런 `심문-2026-08-14T0354-d790ae`(대상 **v5.5** 지문 `475ef906f5e1` · 던짐 3행 03:54Z 장부 커밋 · 편성 luna×2+sol×1 max 동일). 🔑조건지문 3종(방향 `fdce7f`·철학 `486e20`·금지 `c878a6`)이 8회차와 **다른 값으로 «던짐»에 박혔다** — 8회차 처분이 낳은 §13 증보와 철학 0814 재도장(`792218d1`)이 원인이고, F411 수리 덕에 이 갈림이 처음으로 장부에 «보인다»(= «간» 대조 재료 — 갈렸다는 사실이 기록됐지 갈림이 위반은 아니다). ~~▶다음 = 수거~~ → ✅**9회차 «수거+①판정» 종결 08-14 `local_a4946442`(던진 세션이 그대로)** — 결과 3벌 `da68eccd`(원건 **76** — L1 19·L2 30·S 27 · P0 52·P1 24) + **처분표 v9 `57ccd058`**(정본=`docs/_ops/심문결과/상태기반과제선택_처분표_v9.md` · 원건 **76/76 기계 매핑** 빠짐·중복·잉여 0 · **53갈래** ✅32·◐15·**❌5**·⧉1 · 런 도장 ✓). 🔑던짐↔완료 조건지문 6/6 동일(F411 2번째 성립) + **회차 «간» 대조 첫 성립** — 8↔9회차 지문 3종이 전부 갈렸고 그 갈림이 장부에 «보인다»(원인=8회차 처분 자체 · v9 §1 — 64→76 증감을 조건 동일 전제로 읽지 마라). 🔑**갈래층 기각 5가 처음 나왔다** — 다섯 다 «기존 명시 처리의 재론»을 실측으로 확인(A10 `:810-814` v5.4 기각 논거 · F2 §12-27 기실재 · F4 §16-1 첫학생항 기실재 · G1 층혼동 · G3 유호 확정 게이트와 정면 충돌). 🔑실측 = 컨텍스트 독립 3벌 위임(A 27·B 33·C 7 · 줄번호 동봉) + 직접 2 — 맞다 62·부분 6: 물리층 지적 거의 전부 실물(7회차 패턴 재현) · 심문자 틀림도 5(§12-27·Vault·잘림 칸 개수·「연결 전무」·§16-1). ➕심문 밖 발견 1: §5-1:1749 가 직렬화 정본을 `lib/오늘과제.js` 로 지목하나 실물은 `tests/오늘과제.test.js:1040`(E7 집행분). ~~▶다음 = ②집행(설계 v5.6)~~ → ✅**9회차 ②집행 «종결» 47/47 — 설계 v5.6 착지 08-14(두 세션 인계)**: 지도1~3 = `local_68de272b` `55bbef4b`(§3-5-b 물리·봉투·RPC — A1~A9·A11·A12·A14·A15·B3·B6~B9·F1) · 지도4~8 = `local_bd025ef9` `54bcf866`+`ac465321`(§3-6·§3-2-a·§6·§11-4·§5·§8-B·§7-2·§11-2·§12·§13·머리말). **①판정 ✅(76/76·53갈래) + ②집행 ✅(47/47) = 이 회차분 전건 처분 종결**(처분표 v9 §7 도장). 굵직한 것 넷 — ⓐ**감시가 자기 대상의 부재를 정상으로 읽던 자리 넷**: `deliver-check` 가 jobs 만 세어 **배치 전멸이 초록**이었다(B1) ⇒ 기준 1→4(남은 큐·배치 실행 행·`partial_count`·열린 attempt) · 결손 수는 저장 자리가 없어 「C4 가 알린다」가 배선 0인 산문이었다(B2 ⇒ `batch_runs.partial_count` 신설) · `적재실패` 가 대상 분모에 섞여 **조립 사고가 생성 실패의 얼굴**을 썼다(A13 ⇒ 「판정 불능」 별도 칸) ⓑ**결정적이라 적힌 것의 앞단이 비결정**: 회전 후보(C1 `order by skill_id`)·근거 앞 50(C2 `order by (occurred_at, event_id)`)·중복 이력(C3 호흡[] 중 ③답하기 행만) ⓒ**한 이름 두 뜻 넷**: 폴백 조립이 §7-2「재호출」↔§3-5-b「draft 만」으로 정면 반대였다(D3 ⇒ C1 단일 정본 · 실측: §12-28 이 「재호출 0회」를 스파이로 재고 있어 v5.5 대로 구현하면 자기 인수 조건에서 빨개진다) · `generation_input_text`(D5) · 실행판 뜻(D4) · `쓸축수`(C4) ⓓ**평가 게이트**: `quality_ver` 부분판 신설로 「검문을 고쳐도 옛 초록이 사는」 구멍을 닫고(E1 3벌 만장) 결과 파일 기계 계약·원문 동봉·시험지 생성 절차·⑦ 정의 확장(E2~E6). 🔑**실측이 처방을 «싸게» 만든 자리 둘**: `게임날인가()` 가 요일만 보는 순수 함수라(`게임배정.js:73·78~81·315`) B5 를 **새 칸 0**으로 닫았고, `학습자상태.js` 의 `잘림` 이 원래 개수를 이미 담아 C2 반쪽은 처방이 필요 없었다. 🔑**심문 밖 발견 정정**: 직렬화 정본은 `tests/오늘과제.test.js:1039~1040` 이고 `lib/오늘과제.js:264` 의 `판지문` 은 «장부»다(E7). 집행자 조정 5건은 처분표 v9 §7 에 전건 등재(A4·A3 / B5 `_calendar_game_day` 인자 / A13·G2 세 절 복제 → 정본+참조 / F3 조인 키 이름 / E1 재료 수가 처분표 §2·§6 에서 갈림). CI 3268 = 통과 3251·실패 0·건너뜀 12·todo 5. ▶**다음 = 10회차 심문을 «v5.6» 에 던진다**(`node tools/codex-review.js --심문 docs/상태기반_과제선택_설계.md --던지기` · F281 «그 시점의 현재 판» · 🚫v5.5 지문 고집). ⛔**구현 착수 차단 유지**(새 회차는 다시 전건 처분을 지난다) · ⏳유호 E2 그대로 · 🚫**설계 §13 v5.6 증보 12건 재제안**(A10·F2·F4·G1·G3·H2 기각분 + E1·E3·E5 두 판째 + B5·C4·E7 신규).<br>🔍**표식 재판정 08-14 `local_13e17007` — 〖지금〗 → 〖⛔선행〗**: ▶다음이 **codex 심문 던지기 하나**인데 GPT(codex) 한도가 소진이다(리셋 **2026-08-20 12:45** · SessionStart 도장 08-14 13:35 실측). 표식을 그대로 두면 새 세션이 초록으로 읽고 집었다가 첫 명령에서 막힌다(F406). 한도가 풀리면 〖지금〗으로 되돌린다 — 그 외 차단자는 없다(①판정·②집행 전건 종결).
- [x] ✅**종결** #Q50 **유호 08-14 「웅 자율적으로 진행해줘. 퀄리티 좋게」 → 집행 종결 08-14 `aa2a6632`**(`local_950cd77e`) — 🔑**이 줄의 「남은 3」이 낡아 있었다**: ①나침반 문항·②장학 주기는 **08-12 에 유호님이 이미 확정**하셨고(설계 §9 「2·5 는 ✅유호님 확정 2026-08-12」 · 커밋 `d216f5d9`) 남은 것은 ③문서 정합뿐이었다 — 아래 원문의 「⏳유호 남은 3」·「②만 자율 밖(돈)」은 그 확정 «전»에 쓰인 사진이다(F406). 집행분: ⓐ**「분기」 자리가 둘이 아니라 넷**(홈페이지는 `7b4c569f` 로 **이미 서 있었다** — 열어보지 않았으면 두 번 고쳤다 / 매뉴얼 §4-5 「시즌 2달 ≠ 분기 3달」+`[운영 확정]` 딱지 해제 / `02B` **v4.3** 3자리+개정이력 / **`02_학생_복지_프로그램.txt:65` = 주기의 정본** — 02B 가 「어긋나면 02가 이긴다」라 적어 둬서 02B 만 고치면 조용히 되돌아간다) ⓑ**장학 ② 「종합 평가 = [확인 필요]」 빈칸을 채웠다** → 그 구간의 시즌 회고 2건(`engine.season_review`) ⓒ상담AI Q11 나침반 **2문항 → 3문항**(「5년 뒤의 나」 누락 · ⚠🚫「대외 FAQ 문장 수정」은 「갱신」 낱말 축으로 읽고 빠진 하나만 끼웠다 — 해석 다르면 그 한 줄만 되돌린다). 안 한 것 = ③교통비 「월말 평가」 [확인 필요](주기가 월이라 회고 2달로 못 덮음 · 별건) · 버전 bump(v9.235 가 #Q78 배포 대기 스탬프 · F078). 회귀 3285 · fail 0. ⚠라이브 도달은 다음 배포에 얹힌다(`contents_상담AI.js` 는 clasp 대상).<br>원줄 ↓ 원줄 ↓ **시즌 회고 그릇 — ✅설계 v3 + ✅①②③④ «구현» 전건 종결. 남은 것 = 붓기 + 실왕복 + ⑤(시즌 2 이후)**(철학정합 §3-C · 유호 채택 08-11 · `local_339b2c3e` 등재 → 설계 08-12 `local_2b84a2bb` → ①② 08-12 `local_2297601e` → ③④ 08-12 `local_6333d084`). 정본 = **`docs/시즌회고_설계.md` v3**(분리 반박 1패스 4건 반영).<br>✅ **①② 구현 종결 08-12** — talk `aac998a` 한 커밋(계약 조각+통로+화면+회귀 · 인수 조건 「①② 한 세션」 그대로). 선 것 = `20260812140000_season_c11.sql`(`engine.season`·`engine.season_compass` · 겹침 배제 gist · 학생×시즌 유일 · **회차별 문항 키 집합 CHECK** · 삭제 금지 트리거) · `lib/나침반문항.js`(문항 4 · 목적별 문구 갈래 · 답검사) · `functions/teach` 의 `compass/open`·`compass/save` · `src/나침반API.js`+`src/나침반화면.js`(**학생번호 `SYNK-001` 로 연다** — uuid 를 요구하면 화면이 원리상 못 쓰이고 나침반이 「나중에 옮겨 적기」가 된다) · L0 계약 §13 · 회귀 22 · **전체 1478/1478**.<br>🔑 **계약판은 안 올렸다**(c11 유지 — 새 값목록 0·`learning_events` 0 · `radio_c10` 선례). 🔑 lib 의 키 묶음과 DB CHECK 의 키 묶음을 **회귀가 기계로 대조**한다(변이로 이빨 실측 — 갈리면 JS 통과·DB 거절이고 그 거절은 화면에서 「저장이 안 된다」로만 보인다).<br>🔴 **아직 아닌 것(정직 표기)**: ⓐ**원격 적용 0** — c11 두 조각이 ⏳유호 승인 대기라 이 조각도 **같은 승인에 얹혀 한 번에** 부어진다(먼저 부으면 `base_version` 검사가 중단 · 안전 방향) ⓑ**물리 실왕복 0** — 지금 초록은 파일 층이다(리허설 5갈래 = L0 §13-6) ⓒ**몽골어 라벨 0** — `라벨_mn` 전부 null(검수 선행 · 통로는 이미 섰고 채우면 화면은 안 고친다) ⓓ**첫 `season` 행은 운영이 손으로 연다**(교재 1권 단위) — 없으면 통로가 409 `SEASON_NOT_OPEN` 으로 정직하게 멈춘다.<br>✅ **③④ 구현 종결 08-12** — talk `2ec0f2c` 한 커밋(인수 조건 「③④를 나누지 않는다」 그대로 · 회귀 38 · **전체 1518/1518** · 변이 2/2). 선 것 = `20260812170000_season_review_c11.sql`(`engine.season_review` · 3갈래 CHECK 둘 · 「판정·사유·주체·시각 한 벌」 CHECK · 트리거 둘 = **굳힌 근거 불변 + 강사 판정 뒤 학생 칸 개서 금지** · 삭제 금지) · `lib/회고.js`(판정 어휘·문구 2벌·**구간을 반으로 갈라 축을 두 번 부르는 자리 하나**·굳히기) · `functions/teach` 의 `retro/open`·`retro/self`·`retro/judge` · `src/회고화면.js`+`src/회고API.js` · L0 계약 §14.<br>🔑 **여는 순간 굳히고 다시 열면 재계산 0** — 판정할 때 다시 계산하면 창이 밀려 「가까워졌다」 옆 숫자가 **그 판정을 낸 사람이 본 적 없는 숫자**가 된다. 🔑 **통로가 셋인 것이 곧 「학생 먼저」**(합치면 순서가 화면 규약으로만 남는다 · DB 트리거가 한 번 더 진다). 🔑 **회고할 시즌도 서버가 고른다**(나침반이 있는데 회고 미확정인 **가장 오래된** 시즌 · 건너뛰기 없음). 🔑 `verdict`·`note` **개서는 허용**(삭제가 막힌 자리에서 개서까지 막으면 남는 통로가 0 · F103). 🔑 굳힌 근거의 `evidence_refs` 는 **행엔 남고 응답에선 벗긴다**(`event_id` 를 브라우저로 안 보낸다).<br>📏 **실측이 잡은 것**: 기존 회귀 **넷**이 「재는 층」이 넓어 새 핸들러에 거짓 적색을 냈다(파일 끝까지 자르는 슬라이스 셋 + `select` 목록 전량 스캔 하나 — F287 계열 · ①② 커밋이 같은 자리에서 이미 두 번 겪었다). 끝을 다음 함수로 못박고, 「동봉했는데 안 쓴다」를 **본체 import ∪ 동봉끼리의 require** 로 다시 정의했다(전이 사슬을 「낡았다」로 읽던 자리). 곁들여 동봉 사슬을 **커밋 전에** 풀어 실측했다(안 하면 배포는 초록이고 함수는 첫 import 에서 죽는다).<br>🔴 **③④ 도 아직 아닌 것**: ⓐ원격 붓기 0(c11 승인에 얹힌다) ⓑ물리 실왕복 0(리허설 6갈래 = L0 §14-7) ⓒ몽골어 0 — `라벨_mn`·`라벨_mn_학생` 전부 null이고 **학생이 직접 누르는 칸**이라 이 검수는 나침반 문항보다 **먼저** 끝나야 한다 ⓓ**teach 재배포 ⏳유호 승인**(동봉 lib 이 바뀌었다 — 안 밀면 남의 왕복시험이 빨개진다 F313).<br>▶ **다음 = ⑤라벨→엔진 배선인데 «시즌 2 이후»다**(설계 §8 — 행이 몇 개 쌓인 뒤). 지금 선 것은 그릇이지 소비자가 아니다.<br>✅ **유호 확정 08-12**(🚫재질의): **①시즌 = 2달**(연 6시즌 · `season_id` 행의 데이터지 코드 상수 아님) **②도전안 채택** — `verdict_by_self`(학생 자기 판정 칸)를 둔다: **학생이 먼저** 누르고 제출한 뒤 강사 칸이 열린다(뒤집히면 학생 칸이 강사 판정의 메아리가 되어 대조군이 죽는다) · null 허용(안 눌러도 강사 판정은 진행 — 회고는 밀리면 라벨 0) · 🚫null 과 「눌렀는데 같다」 접기 **③판정 주체 = 강사**(원장 아님 → `decided_by`).<br>🔴 **「2달」이 확정 뒤에 깬 것 2**(둘 다 v2 반영): ㉠**정본 충돌** — `기업철학_홈페이지_v1.md:75` 가 「시즌(**분기**)마다」로, 매뉴얼 §4-5 가 크루를 「분기·반 단위」로 적었다. ▶권고=**시즌만 2달로 떼고 나머지 「분기」(장학 최우수·분기 평가 300P·학부모 상담)는 3달 유지** = 홈페이지 1자리 + 매뉴얼 한 줄 · 🚫장학·평가도 2달(연 4→6회 = 장학 예산 1.5배) · ⏳유호 「그래」 대기. ㉡**창 30일 < 시즌 2달** — 8축이 덮는 건 시즌 후반 절반이라 「초반에 잘하다 말았다」와 「초반에 못하다 살아났다」가 같은 그림으로 뜨고 강사가 그걸로 시즌 전체를 판정한다. ▶처방=`record_snapshot` **2층**(`axes_30d` 기존 정의 그대로 + `season_totals` 시즌 구간 전체) · 🚫두 층 평균 · 🚫`학습자상태` 창 30일 건드리기(다른 소비자가 갈린다).<br>🔴 **시한이 「첫 시즌 종료 전」이 아니다 — 「첫 학생의 «입학일» 전」이다**(실측이 앞당겼다): 회고가 병치할 왼쪽 = 나침반 3문항 답인데 **두 저장소 전수 검색 0건**(08-12 · `나침반`·`compass`·`season` 코드·계약·SQL 전량)이고, 대외로는 상담AI FAQ Q11(`확정: true`)이 「**입학할 때**, 그리고 시즌마다 나침반 세션을 엽니다 … 드림맵 카드로 남아 시즌마다 **갱신**됩니다」로 **이미 약속 중**이다. 그날 통로가 안 돌면 그 학생의 왼쪽은 영구히 빈다(소급 불가).<br>🔑 **급소 낱말 = 「갱신」** — 대외 문장을 구현 규격으로 그대로 옮기면 **덮어쓰기**가 되고 그게 정확히 `learners.goal_track` 의 병이다. 규격 = 보이는 것은 최신 한 장, **남는 것은 전부**(append).<br>▶ **남은 구현 = ①②만**(③④⑤는 첫 시즌 끝 전·그 뒤): ①`engine.season_compass` 행 하나(나침반 답 적는 화면까지 한 벌 — 「강사가 나중에 옮겨 적기」가 되면 자동화 축이 그 자리에서 죽는다) ②`season_id`=날짜 범위를 가진 행(🚫주기 상수 박기 — 주기는 [운영 확정]). 인수 조건 = ①② 한 세션(계약 조각+통로+회귀) · ③④는 나누지 않는다(창 30일이 밀려 판정이 근거를 잃는다).<br>✅ **유호 추가 확정 08-12 (3)**: ⓐ**시즌의 정의 = 교재 1권**(달력 2달이 아니다 — 「교재 1권당 2달 걸리니 2달에 한 번 돌아보며 점검」 · 🚫달력으로 시즌 자동 생성 · 교재가 늦게 끝나면 시즌도 늦게 끝난다) ⓑ**층 = P1 맨 아래**(이동 완료) ⓒ나침반 업그레이드 지시(「간편하게·실용적으로」) → 아래 ㉤ 반영.<br>🔑 **v3 판정 2 (유호 물음에 답)**: ㉠**성향 창은 30일 그대로 둔다** — 회고가 같은 함수를 **구간만 달리해 두 번** 부른다(`axes_전반`·`axes_후반`). 실측 = `lib/학습자상태.js:147` 이 `{as_of, 창}` 을 **이미 인자로 받는다**(30 은 안 넘길 때 기본값 `:154`)라 코드 변경 0. 🔴창을 60 으로 늘리면 **평균으로 접혀** 「초반에 잘하다 말았다」와 「초반에 못하다 살아났다」가 같은 값이 된다 — 회고가 판정할 게 정확히 그 방향이라 문제가 안 풀린다. 그리고 창 30 은 과제 배정·답장 맥락이 **매일** 쓰고 거기선 최근성이 값이다. 🚫기본 `창일수` 변경 · 🚫회고 전용 축 함수 신설 · 🚫두 층 평균. ㉡**나침반에 시즌 문항 `season_goal` 하나 추가**(「이번 교재에서 하고 싶은 것 한 가지」) — 🔴없으면 **2달 회고 판정이 원리상 안 선다**(과녁이 5년짜리뿐이면 강사가 매번 `same` 을 눌러 라벨이 전부 같은 값 = 엔진 학습 0). 입학 3문항은 그대로, 시즌마다는 **한 줄 1 + 클릭 1**(`self_in_5y` 는 [그대로/바꿀래] 버튼 + `self_in_5y_changed` 칸이 `redirected` 를 직접 말한다). 🚫④를 지표·마감·달성기준으로 쪼개기(계획서가 된다) · 🚫자동 채점.<br>🔴 **곁들여 발견 — 장학 ② 「전액 프리패스」의 심사 근거가 정본에서 비어 있다**(`02B_장학제도_운영규칙_내부용.txt`: 「분기별 **종합 평가** 최우수자」인데 종합 평가 = **[확인 필요 — 평가 종류·기록 위치 미정]**). **시즌 회고가 그 물건이다**(회고 그릇의 두 번째 소비자 · 개원 직결). 그래서 주기가 시즌 정수배여야 한다 — **분기(3달)=1.5시즌이라 심사일에 시즌 경계가 영영 안 온다.** ▶권고=**2시즌(4개월)·연 3회**(회고 2개가 재료 · 분기 대비 예산 25%↓ 176만→132만₮ · 6개월은 첫 지급이 2027-08 이라 개원 초 이탈 구간에 당근이 안 보인다) · 대안=반기(연 88만₮·가장 쌈) · 🚫연 6회(264만₮·희소성 붕괴). ⏳**돈이라 유호님 판정**. ⚠별건=③교통비 세이브의 「월말 평가 점수」도 기록 위치 미정(주기가 월이라 회고로 못 덮음 — 따로 등재).<br>⏳**유호 남은 3** = ①**나침반 문항 최종 확정**(권고=입학 3 + 시즌 `season_goal` 1)(재료 전량=설계문 §9-2 · 실측=정본이 이미 갈렸다: 철학·홈페이지 **3문항** vs 상담AI Q11 **2문항**(「5년 뒤의 나」 누락=요약 누락) · 권고=**3 그대로 굳힌다** — 셋이 회고의 서로 다른 자리에 착지: `why_learning`→끈기·리듬축 / `self_in_5y`→**다음 시즌의 같은 답**(=`redirected` 의 유일한 근거) / `topik_use`→`exam.result`·급수 · ⚠③이 `culture` 목적 학생에겐 안 맞는다 → 문항키 하나·문구만 목적별(🚫문항 4개로 늘리기) · ⚠몽골어 라벨=검수 선행이나 통로는 먼저 지을 수 있다) ②**장학 ② 주기**(돈 · 권고=2시즌·연 3회) ③**「분기」 두 자리 문구 정정**(홈페이지 1 + 매뉴얼 1 · 「그래」 한 마디). ✅층 P2→P1 맨 아래 = 확정·이동 완료.<br>🚫목표 런타임 자동 판정(유호 확정 그대로) · 🚫포트폴리오·드림맵 카드 등재 재론(경량 구조로 대체 확정) · 🚫판정 눈금 5단계·점수화 · 🚫나침반 답을 `preference.stated` 에 앉히기(c11 ⑦ 가드를 깎아야 하고 그 가드는 G2 통로까지 덮는다) · 🚫새 `event_type` 신설(엔진이 세는 축이 없다) · 🚫대외 FAQ 문장 수정(뜻이 안 틀렸다 — 맞출 것은 구현). 근거=[[philosophy-alignment-audit-0811]]

- [ ] 〖지금〗 #Q52 **✅유호 08-14 「채웠어 (아이디 바꿨음 한도 100%짜리)」 → 표식 ⛔선행 ⇒ 지금 · 🔵집행 착수**(세션 `0f1f094e` 08-14 11:49 — `node tools/codex-review.js --commit c0f148aa --던지기 --한도무시` · 런 `검수-2026-08-14T1149-5711bc` · 세션 밖에서 돈다). ▶결과가 떨어지면 훅이 알린다 → 지적 있으면 `엔진_운영배치.js` 명부스윕 구역 수리 → `--런처분` 도장. 원줄 ↓ **명부스윕 수리 커밋 `c0f148aa`([v9.217]) 재검수 1건 — 죽은 세션 52a7abc6 의 🎫 잔여**(08-13 `local_f0129532` 실측 등재 · 보드 줄은 아카이브 이관). 실측: `검수기록.jsonl` 에 대상 `c0f148aa` 항목 **0건**(직전 `52eac5b2` 재검수는 33행에 있고 그 지적을 수리한 커밋이 바로 `c0f148aa`) · 🎫의 「통과 시 clasp push(215~217)」 배포 다리는 a668c152 가 라이브 밀림 수리로 v9.215~220 한 묶음 완주(`7a37a20d` · 라이브=저장소 바이트 동일)라 이미 라이브 — 남은 것은 **사후 재검수뿐**(수리 커밋이 독립 검수를 한 번도 안 받았다 · 「codex 층 미검증은 보고에 밝힌다」의 그 층). ⛔선행 = GPT(codex) 한도(리셋 8-18 12:39 · 또는 c4fcf299 계정 교체 프로브 성공=마커 자가 해제 — 집기 전 `한도상태` 실측). 실행 = `node tools/codex-review.js --commit c0f148aa` → 지적 있으면 수리(대상 파일=`엔진_운영배치.js` 명부스윕 구역). ⚠짝 🎫(9a710cd8 「de6b34d5 재검수→배포(210~214)→as 명부스윕」)은 세 다리 전부 후속 세션들이 이행 완료 실측(장부 de6b34d5 3건 · 배포 같은 묶음 완주 · 스윕=v9.215 신설로 52a7abc6 가 구현·라이브 `엔진_운영배치.js:383`) — **일감 없음 · 재수색 금지**. 근거=[[collection-engine-automation-mandate]]

## P2 — 재미·쉬움·신선함

*(미니게임 4모듈 줄은 08-10 P0으로 승격 병합 — 위 P0 절)*
- [ ] 〖⏳유호〗 #Q57 **✅유호 08-14 「이거에대해 자세히 설명해줘」 → 설명 제출(세션 `0f1f094e`)** — 표식 불변(차단자=계약 판정 1건, 아래 §5). 원줄 ↓ **강6 관찰태그 자동화 — 설계 정본 섰다. 남은 것 = 유호님 계약 판정 + M2 구현**(08-10 `local_ed0a2064` · 정본 = `docs/관찰태그_자동화_설계.md` · 분리 반박 1패스 치명 8 반영). 방법 확정 = 「강사 원문 → AI 구조화 초안 → 강사 확정」·**확정된 관찰만 `source_kind='teacher'` 사건으로 적재**(초안은 화면 상태 — 사건출처 단일값 규약이라 행 분기 불가)·어휘 2층(영역 9유형 유지+오류태그는 텍스트 축만·억지 태그 금지). ⚠**선행 = 유호님 판정 1건**(다음 계약 판에 `observation.noted`+물리칸 2(관찰자 식별·draft_modified)+RLS 개정(학생 self-select 제외 — 안 하면 강사 관찰 원문이 학생에게 노출) · 🚫c11에 묶기). 구현 자리 = M2 talk 강사 화면(관찰 전용 문 신설 — `review` 함수 `검수역할`에 teacher 넣기 🚫)·라이브는 0줄이 판정. 🚫㉯ AI 선그리기(관찰 화면에 앱 축 표시 포함)·후분류 별건 게이트 건드리기. 근거=[[app-ui-style-directive]]
- [ ] 〖지금〗 #Q58 **🔴유호 지시 08-14 가 이 줄의 «축»을 바꿨다 — 「왕관 낱말 이것도 왕관이 뭔지 도저히 이해가 안가 직관적으로 재구성해야할것같다」**(표식 ⏳유호 ⇒ 지금 · 세션 `0f1f094e` 착수). 🔑**낱말 교체(왕관→도전 배지)가 아니라 제도 재구성**이다 — 그래서 **몽골어 검수 차단자가 이 판정을 안 막는다**(검수는 «최종 문구»에만 걸리고, 무엇을 왜 주는가는 지금 정한다). 실측이 유호님 말을 뒷받침한다 — **한 낱말이 다섯 층을 가리킨다**: ①MVP 왕관(기량·참여 · 반당 매일 1명 강제 `dailyGuard`) ②시냅스 왕관(노력·성장 · 반당 매일 1명) ③왕관 컬렉션(누적 AW·AX) ④왕관 액자(FRAME_CSS 7단계 중 최고 등급명) ⑤「왕관의 왕」(몬스터 M07 — 08-14 「몬스터 7단계 삭제」로 소멸 중 `2a14d761`) + 왕관 시상식(월초 실물)·왕관 밸런스(강사 공정성 12열). 배포 코드 실측 = `Code.js` 64줄 · `엔진_운영배치.js` 27 · `엔진_콘텐츠AI.js` 11 · `엔진_셋업확장.js` 6 · `엔진_폼리포트.js` 1. 🔴**곁들여 나온 급소 — ①MVP 는 철학 ㉢(학생끼리 비교 금지)과 정면으로 부딪힌다**(반당 1명 강제 = 매일 반에서 1등을 뽑는 제도 · 「등수는 어느 화면에도 두지 않는다」). ▶판정안 정본 = `docs/왕관제도_재구성_판정안.md`. 원줄 ↓ **낱말 교체가 코드층에 안 닿았다 — 학부모 카드의 「왕관/Титэм」**(08-09 `local_734d4614` 가 배포 스윕에서 발견 · v9.200 이 슬로건만 상환하고 남긴 자리). 철학 리라이팅 장부 §1 은 「왕관」→**「도전 배지」**(학부모 접점·문서 표기 — **앱 내부 기능명은 유지**)로 못박았는데, 리라이팅 트랙의 대상 목록이 `docs/` 뿐이라 **배포 코드가 통째로 사각**이었다. 실측: 배포 파일에 「왕관」 109자리·「Титэм」 11자리(대부분은 주석·시트 열·강사 HUD = 손댈 대상 아님).<br>🔴 **실제 학부모 접점 2자리**: `Code.js:2900` 주간 리포트 카드 `👑 Титэм: N удаа` · `Code.js:2913` 왕관 배너 `титэм авлаа! Гэртээ магтаж өгөөрэй` + 한국어 병기 「오늘 왕관을 받았어요! 집에서 칭찬해 주세요」. 🔑 **선례가 이미 있다** — `Code.js:2915` 가 v9.74 에서 같은 카드의 「몬스터」를 **성장 파트너(хамтрагч)** 로 이미 바꿨다. 「왕관」만 안 된 것이다.<br>⚠ **지금 못 미는 이유 = 몽골어**(장부 §6 「몽골어 병기는 원어민 검수 대기 — 리라이팅해도 확정 아님」). 몽골어 주문장을 못 바꾸는 채로 한국어 병기만 바꾸면 **한 카드 안에서 말이 갈린다**(지금은 둘 다 「왕관」이라 최소한 일치한다). 선행 = 몽골어 검수자 1인(유호님 채용 몫 · 결정큐).<br>⚠ **경계 판정이 남아 있다** — 학생 접점(`Code.js:2922` 오늘의알림 푸시 · `:2933` 나의 기록실 「첫 왕관」)은 장부가 「학부모 접점」만 적어 대상인지 미확정이고, `.claude/skills/synk-brand/SKILL.md:24-25·77` 은 **세계관 정본**으로 「몬스터 진화·왕관」을 정의하며 모범 답안까지 「숙제를 내면 왕관이 하나 올라가요」로 준다 — 학생 접점을 대상으로 잡으면 그 정본과 `voice-guard.js:56` 처방까지 같이 가야 한다. 🚫 **전면 개명은 기각돼 있다**(메모리 「몬스터 전면 개명(학부모 접점만 교체)」).<br>✅ **경계 판정이 끝났다 — 장부 §1-b 에 성문화됐다**(08-10 `local_a3993f21` · `866c28c8`). 가르는 축은 「누가 읽는가」가 아니라 **「무엇과 갈리는가」**다: ㉠**바꾼다**=제도를 설명하는 카피·안내문(앱 화면을 인용하지 않으니 갈릴 것이 없다) · ㉡**유지+낱말 안내**=앱 실물 기능명을 기록하거나 **강사가 학생 앞에서 낼 말을 규정하는 문서**(05 운영 시스템·강사 교수법 매뉴얼 §4-3 — 문서만 앞서 바꾸면 강사의 말과 학생이 보는 화면이 갈리고 그 갈림은 교실에서 즉시 드러난다) · ㉢**지금 손대지 않는다**=이 줄이 가리키는 `Code.js` 학부모·학생 접점(한·몽 병기라 한국어만 바꾸면 한 카드 안에서 말이 갈린다).<br>🔑 **그래서 이 줄에 남은 것은 판정이 아니라 몽골어 검수 1인뿐이다** — 검수자가 오면 ㉢ 4자리(`:2900`·`:2913` 학부모 + 경계 미확정이던 `:2922`·`:2933` 학생 접점)를 한 벌로 민다. 학생 접점은 §1-b ㉢ 이 「배포 코드의 병기 카드」로 함께 묶어 판정했다 — `synk-brand` SKILL.md 세계관 정본·`voice-guard.js:56` 도 그날 같이 간다.<br>✅ 슬로건 축은 닫혔다 — `tests/은퇴문구.test.js` 가 배포 파일의 구 슬로건 재유입을 기계로 막는다. 🚫 그 검사를 낱말(「왕관」·「몬스터」)로 넓히기(경계를 기계가 못 갈라 거짓양성이 쏟아지고, 그러면 검사를 끄게 된다) · 🚫 몽골어를 검수 없이 지어 넣기 · 🚫 한국어만 먼저 바꾸기. 근거=[[app-ui-style-directive]]
- [ ] 〖지금〗 #Q59 🔍**표식 재판정 08-14 `local_7c9e319e` — 〖⏳유호〗→〖지금〗 · 남았다던 「배포 둘」이 둘 다 착지했다**: ①talk 운영 Fn → `배포대조 --운영` 실측 08-14 = `correct` 포함 **14/14 ✅같다 · 다름 0** ②Apps Script 라이브 v9.223 → 라이브가 **[v9.233]**(08-14 착지 · 라이브=HEAD·16파일 바이트 동일 실측 · 메모리 `fluency-result-storage`)이라 v9.223 은 그 안에 실려 있다. ▶남은 것은 **다섯 층 장부의 ①~⑤ 칸을 착지 실측으로 갱신하는 것**뿐이다. · **「쓰는 문자는 셋뿐」의 층 장부 — 다섯 층 중 넷이 닫혔고 남은 것은 «배포» 둘이다**(원 제목: AI 교정 해설이 몽골어 문장 안에 «한자»를 섞어 낸다 — 실측 1자)(08-12 `local_ea9cfc54` 가 `test-ci` 적색을 가르다 발견 · 아무도 안 세던 축). 자리 = `SYNK-talk/evals/출력_v2재시도.json:33` — 「목적격」의 **격**이 한자(U+683C)로 나와 키릴 문장 한복판에 박혔다(`тусах[한자]ийн нөлөө`). 형제 저장소 **전량 실측 = 이 1자뿐**(eval 출력 10벌 중 1벌 · ~1% 빈도)이라 흔하진 않지만 **학생이 읽을 해설에 그대로 나가는 경로**다(철학 게이트 ① 실사용 도움). 🔑 **거짓양성이 아니다** — `tests/문서문자.test.js` 의 형제 검사(「학생이 보는 글자는 형제에 산다」)가 정확히 겨눈 자리다. 🚫 **그 파일의 글자를 고쳐 초록 만들기** — eval 출력은 **모델이 실제로 낸 것의 기록**이라 고치면 실측 조작이고, 고칠 자리는 프롬프트다. 처방 후보는 둘이었고 **둘 다 닫혔다**: ✅**②eval 채점기 문자 혼입 축**(08-12 `local_92ad82e6` · talk `246c3d9` · as `5e76e792`+`b8ffbec7` — 판마다 `옛글자 혼입 n/분모`를 내고, as 검사는 `evals/출력_*.json` 을 면제하되 그 면제의 근거가 살아 있는지를 기계로 대조한다. 이걸로 **상시 적색·「남의 미커밋 탓」 오독도 같이 끝났다**). ✅**①교정 프롬프트 1줄**(08-12 `local_8231d25c` · talk **`10e63dd`** = 프롬프트 **v3** 「쓰는 글자는 한글·키릴·라틴과 숫자·문장부호뿐 — 한자·가나 금지」 · 일부러 ② 뒤로 미뤄 뒀던 몫이다: 프롬프트를 건드리면 판이 하나 더 생기는데 이 트랙이 이미 「판 간 비교가 잡음에 묻힌다」를 적었고, 이제 넣기 전/후를 같은 눈금으로 잰다). 🔑 **이 줄은 이제 「고칠 것」이 아니라 «층 장부»다**(F387 의 처방 — 확정 하나를 지키는 장치가 층마다 따로인데 한 층을 세우고 확정 전체를 닫은 것으로 적은 사고가 이 줄에서 났다). 유호 확정(2026-08-07) 「쓰는 문자는 한글·키릴·라틴 셋뿐」이 사는 층은 **다섯**이다:<br>· ①우리가 쓴 글 — 저장소 파일 스캔 ✅(F351) · ②우리가 쓴 글 — 커밋 층 ✅(F379) · ③**모델이 학생에게** — talk 런타임 ✅(`5caa617` = `lib/옛글자.js`+`교정값` · 행 버림=재시도 공짜 · 리허설 `correct` 에 `옛글자.mjs` 실려 있음 **실측**) · ④**모델이 학생에게** — Apps Script 런타임 ✅(**v9.223** `2e732c3f` — 모델 깔때기 여덟 자리) · ⑤eval 채점 축 ✅(`246c3d9`).<br>🔴 **남은 것은 배포 둘뿐이고 둘 다 이 줄 밖이다**: talk 운영 Fn(=위 「AI 교정 생산자」 줄 · ⏳유호) · Apps Script 라이브(v9.223 을 담은 clasp push).<br>✅ **v7 실측 종결**(08-12 `local_6d54a17f` · talk `b24cd7b` · 리허설 `correct` **v21** 재배포 — 그 낡음이 남의 왕복시험도 막고 있었다 F313): 옛글자 **0/102**인데 `사유 0` 이라 **게이트는 한 번도 안 걸렸다** — 이 판은 게이트의 「돈다」를 증명하지 못했다(증명은 픽스처 회귀 쪽 `tests/옛글자.test.js`). 프롬프트 v3~v7 실측 **2·1·0·1·0** = 510행에 4자 ≈0.8%.<br>➕ **축이 조용히 눈머는 자리를 막았다**(talk `ce485f0` · 회귀 2 · 변이 1/1): 게이트가 «버린» 행이 파싱된 칸을 잃으면 채점기가 그 행을 문자 축 **분모에서 뺀다** → 걸린 행이 전부 분모 밖으로 빠져 축이 영원히 `0/n` 초록이 된다(새는 방향이 「통과」).<br>🔴 **곁들여 나온 것 — 같은 프롬프트 두 회차가 7건 움직였다**: v6→v7 에서 전체 92→89 · 주체 높임 **4/4→3/4**. ⑨의 「판 통틀어 처음 4/4」는 회차 운이 섞인 값이라 **한 회차로 4칸짜리 축을 판정하지 않는다**(다음 프롬프트 개정은 같은 판 2회차를 기준선으로). `C11` 은 돌아와 2판 연속이 아니라 규칙 1 은 안 연다.<br>🚫 이 줄을 「끝났다」로 지우기(배포 둘이 남았다) · 🚫 `evals/출력_*.json` 의 글자 고치기(실측 기록이다) · 🚫 한 판의 0 을 「고쳤다」로 · 🚫 프롬프트 문장만으로 갈음(v3~v6 네 판이 못 막았다). 근거=[[philosophy-alignment-audit-0811]]

- [ ] 〖지금〗 #Q61 🔍**표식 재판정 08-14 `local_7c9e319e` — 〖⏳유호〗→〖지금〗 · 「적용 안 됐다」가 실측으로 뒤집혔다**: 이 줄은 「남은 차단자는 마이그 1벌 적용 하나 · 적용은 ⏳유호님 승인 자리」라고 적는데, **그 조각은 이미 운영에 있다** — `engine.schema_migrations` 직접 조회 08-14 = `20260812210000_teacher_notes_c11.sql` **적용 2026-08-14 08:19:58+00**(운영 조각 28개 · 학생 0행 · 사건 0행). 승인 축도 낡았다(08-12 유호 원문 = 운영 쓰기 자율). ▶남은 것은 그 줄이 스스로 적은 **§8-5 실왕복**이고 인수 조건 둘이 이미 문서에 있다. · **선생님 피드백 UX(반 단위 전용 메뉴) — ✅ §8-1·2·3·4 «구현 전건 종결». 남은 차단자는 마이그 1벌 적용 하나**(유호 신규 지시 08-12 「반을 보기 쉽게 정해서 해당 반에 들어가서 피드백하기 쉽도록 · 니가 판단해줘」 · `955e8a16` 🎫 을 **두 세션이 10분 안에 나란히** 집었다 · **F381**). 🔴 **정본 = talk `docs/강사_반단위_피드백_설계.md`** — `SYNK-appsscript/docs/강사_피드백_UX_설계.md` 는 **폐기 스텁**이고 인용하지 않는다. ✅ **§8-1 반 원본+다리**(`local_c84a3b8a` · 마이그 `20260812200000_class_c11` · 명부 열별칭 1줄로 「이미 오는 것을 받는다」) · ✅ **§8-2 G2 「맞음」 술어**(talk `0be2fae` — `correct` 대기조건에 술어 하나) · ✅ **§8-3 라우트 셋 + `teacher_notes`**(`local_df3c7c6d` · talk `5557df7` · 마이그 `20260812210000_teacher_notes_c11`) · ✅ **§8-4 화면**(08-12 `local_62036356` · talk `ff5de3a` — `src/{반피드백화면,반피드백API}.js` 신설 · `App.js`·`도착확인` 진입 줄 · 회귀 35 · 변이 12/12 + 재조립 뒤 4/4 · 전량 1813/0). 🔑 **§8-4 가 짓기 전에 §4 통로의 구멍을 냈다** — `feedback/queue` 가 「누가·언제·무슨 과제」만 내고 §5 ③ 이 화면에 놓기로 한 «학생이 쓴 문장»을 안 줬다(재료 없는 판정 칸은 안 쓰이고, 안 쓰이는 칸의 생산량은 0이다) → 재료 다섯 칸(`body_original`·`transcript`·AI 교정 3)을 더했다. 술어(`기다림질의`)에는 안 매단다 — 카드의 셈과 목록이 같이 쓰는 하나뿐인 정의라, 본문을 붙이면 카드 한 장 그리려고 내 반 전체의 대기 글을 끌어온다(설계 §4-b). ✅ **리허설 `teach` v8 재배포 종결** — `배포대조` **다름 1→0**(§8-3 이 안 밀어 남아 있던 `반피드백.mjs` 누락도 같이 닫혔다 · 그 다름은 남의 왕복시험을 막고 있었다 F313). 🔴 **남은 차단자 하나 = 리허설 DB 에 `20260812210000_teacher_notes_c11` 이 «미적용»이다** (실측 = 배포 도구가 읽은 DB 최신 조각이 `20260812200000_class_c11` 이다). 표가 없으므로 `feedback/*` 세 라우트는 지금 호출하면 500 이다 — ⚠ **실호출로 재지 않았다**(근거는 DB 최신 조각 실측이고, 「표 없음 → SQL 오류 → INTERNAL」은 코드에서 읽은 귀결이다). 적용은 ⏳유호님 승인 자리: `node tools/원격SQL.js supabase/migrations/20260812210000_teacher_notes_c11.sql --적용`. ▶ 그 뒤가 **§8-5 실왕복**이고 인수 조건 둘이다: ⓐ담당 반 **밖** 학생이 **0행**으로 막히는 것을 일부러 심어 증명 ⓑ「맞음」 제출을 심고 **AI 행 0 · 검수 큐 0행**. 🔴 실왕복 0 · 운영(라이브) 0 — 지금 초록은 **파일 층 + 리허설 배포 층**까지다. 🚫 `corrections` 에 강사 한 마디 쓰기 · 🚫 `roster-ingest` 전체 upsert · 🚫 점수·정답률 정렬(정렬 축이 곧 비교다) · 🚫 술어를 두 곳에 적기 · 🚫 강사 탭 두 메뉴 합치기. 근거=[[reviewer-console-ux]]

## P3 — 도구·하네스

- [ ] 〖재료0〗 #Q80 **발화 지수 스냅샷 — 실패한 주가 영영 비는 자리 + 행별 계약 버전**(08-14 `local_379cb6f7` 등재 · [v9.235] 재검수가 낸 P1 1·P3 4의 잔여). 🔑**차단자 = 학생 0명**(첫 실전 작동은 개원 후 월요일 07시 · 지금은 실패할 반 자체가 0이라 잴 수도 고칠 수도 없다). ㉠**실패한 주는 다시 안 채워진다**(검수 2606da3bf3d5 · [v9.235] 에서 **기각**했고 그 사유가 이 줄이다) — `talkIndexSnapshot_` 의 멱등 키는 `시즌|주차|sid` 라 다음 주 실행은 «다음 주» 행만 쓴다. 실패한 반의 그 주는 영영 빈다. ⚠**`safeRun` 에 재시도 큐를 넣는 처방은 쓰지 않는다** — v9.32 부터 있는 전역 공용 통로라 그걸 타는 모든 자동 작업의 동작이 함께 바뀐다(되돌림 비용이 트랙 밖). ▶설계 갈래 둘: ㄱ)스냅샷이 «빠진 주차»를 메우게 한다(주차 루프 + 멱등 키가 이미 있어 중복은 안 난다) ㄴ)실패 반만 보류 마커에 적고 다음 실행이 그것부터 재시도한다. **ㄱ이 권고** — 마커는 새 상태층이고 ㄱ은 있는 키를 그대로 쓴다. ㉡**행별 계약 버전이 없다**(검수 be9fcc2793dc·edb11b7c906f) — `TALK_INDEX_LOG_HEADERS` 10열에 `schema_ver`·계산 알고리즘 버전·`measured_until` 이 없어 과거 `pct` 를 나중에 못 복원한다. ⚠열 추가는 append 형 로그의 기존 행과 폭이 갈리니 마이그레이션과 한 벌로 본다. ㉢**엔진 도달**(검수 5bb802a81aaf·11fbba99916d) — 적재는 Apps Script 시트까지고 `engine.*` 자동 소비 배선은 이 트랙 밖이다(④ 판정은 [v9.233] 선례 `✗✓✓` 승계). ㉣**change_id 계보**(2ddf40845e54·f98cf3ce4bd6) — 기능지도·제안·판정·검수를 잇는 change_id 가 안 남는다. 이건 `tools/codex-review.js` 소유라 이 트랙이 못 고친다.

- [ ] 〖지금〗 #Q79 **반 키 접기(`반키_`)를 `엔진_운영배치.js` 2곳에도 태운다 — 갈라짐 예방**(08-14 `local_379cb6f7` 등재 · #Q78 에서 갈라져 나온 잔여). #Q78 이 `talkIndexSnapshot_` 의 조인 누락을 고치며 **공용 통로 `반키_`**(`엔진_셋업확장.js` · `groupBoardOf_` 앞)를 세우고 그 파일 4곳(`:1737`·`:1988`·`:3439`·`:3490`/`:3545`)을 전부 그 통로로 모았다. 남은 것은 `엔진_운영배치.js:3056`(`const bare = raw.split('(')[0].trim()`)·`:3169`(`const cls = rawCls.split('(')[0].trim() || rawCls`) 둘. ⚠**지금 동작은 옳다** — 둘 다 접고 있다. 이건 결함이 아니라 **같은 판정이 두 파일에 흩어진 상태**이고, 한쪽만 바뀌는 날 조용히 갈라진다(#Q78 이 실제로 그렇게 났다). 🔑 **이번에 못 옮긴 이유는 소유다** — 보드에서 `local_63180e5f`·`local_7b188c7f` 두 세션이 그 파일을 「만질 파일」로 들고 있었다(F073). ▶집을 때 ①보드로 그 파일이 비었는지 먼저 보고 ②`:3169` 의 `|| rawCls` 폴백은 «빈 문자열이면 원문 유지»라 뜻이 달라 그대로 남긴다(`반키_(rawCls) || rawCls`) ③회귀 `tests/발화퀄리티.test.js` 「반 키 접기가 «한 곳»에서만 산다」의 검사 범위를 그 파일까지 넓힌다 — 지금은 `엔진_셋업확장.js` 만 잠근다(합본으로 세면 남의 커밋이 막힌다).




- [ ] 〖⏸시점〗 #Q77 **✅유호 08-14 「굿 고마워 9월1일전까지 소음 없게해준거 너무 좋은 선택이야」 — 판정 승인·표식 불변**(복원 시점 9/1 그대로 · 🚫앞당기기). 원줄 ↓ **syntax-check `push:` 트리거 복원 — Actions 리셋(9/1) 뒤**(08-14 `local_7c9e319e` 등재 · 지도 `github-actions-billing-outage` ▶push자동→손호출 집행의 짝). 08-14 에 `push:` 를 주석으로 내렸다(`.github/workflows/syntax-check.yml` — 소진 상태에선 run 이 기동 없이 실패하며 실패 알림만 하루 ~16통 · 텔레그램 답 수거를 죽이던 자리). 복원 = yml 의 `# push:` 주석 해제 한 줄 → 첫 push 뒤 `node tools/원격ci.js` 로 **run 이 실제로 생기는지 실측**(「run 0건」은 화면에서 「초록」과 같은 모양 — 그 파일 머리 주석의 08-06 실사고) → CLAUDE.md 「리셋 뒤 원격ci.js 를 트랙 종료 게이트로 되돌린다」와 한 벌로 닫는다. 근거=[[github-actions-billing-outage]]

- [x] #Q70 **「부정 단언 × 파일 원문」 처분 — ✅3차 «전건 종결» 08-14 `local_6e8dd8b4` · talk `4a2a804`** (위험 67 → 39 → 23(재계측·새 9 포함) → **1** — 잔류 1 = C0계약:204 ㉢비JS 마크다운·«왜» 주석으로 의도 잔류 · 사본 0벌(참) 유지 · 계수기 결함 3 수리 = SQL 정제 별건 판정·매개변수 그늘→모름·별칭 재수출 인식 · 픽스처 `tests/fixtures/가드계수경계/`·변이 3/3 · 「한 이름 두 뜻」 3곳은 이름 가르기 · 스위트 2072/2072)(08-13 등재 `local_8ad318ea` · 처분 `local_9f7d765b` · talk `fe0a440`·`340fd91`·`96da2f1`). 🔑 **가장 큰 건은 계수기 자신이었다** — ㉡ 사본 탐지가 «함수 모양»(`const f = (s) => …`) 하나만 봐서, 저장소 사본의 다수인 «식» 모양(`const 주석뺀소스 = 소스.replace(…)`)과 «함수 선언» 모양(`function 주석없이(s){…}`)을 통째로 못 봤다. 그래서 「지역 사본 0벌」이 나왔고, 그것을 값으로 못박은 `tests/소스검사통로.test.js` 가 **16벌이 산 채로 초록**이었다 — 새는 방향은 언제나 「통과」다. ✅ **처분한 것**: ㉠ 사본 16벌 → 0벌(11개 파일을 공용 `tests/lib/소스검사.js` 경유로) · ㉡ 못 합치는 3자리는 «왜»를 1줄로 남겼다(`검수큐` = 주석이 sql`` **안**에 산다 → `SQL주석없이` 로 층 분리, 덤으로 `--` 도 이제 뗀다 · `나침반문항`·`회고` = SQL 파일이라 언어가 다르다, 블록만 떼던 것에 `--` 추가 · `스폰통로` = 위반을 **행 번호로** 짚어 주는데 `코드만` 은 주석만 있던 줄을 버려 번호가 밀린다 → 렉서는 공용으로 두고 공백 정책만 나눈 **`줄맞춰코드만`** 신설, 옵션 깃발 아님) · ㉢ 계수기 3차 조임(모양 셋 · **JSON 경계** = `JSON.parse(readFileSync(…))` 는 구조라 주석이 원리상 없다, 그 파생인 «생성된 SQL» 까지 위험으로 번지던 자리 · **「한 이름 두 뜻」** = 한 스코프 근사가 상수 선언까지 원문으로 물들이던 자리, 못 가른 것은 위험이 아니라 ❔모름). **변이 8/8** · 스위트 2017/2017. ▶ **남은 것 = 위험 39 건별 처분.** 각 단언의 정규식을 그 대상 파일의 «주석 글»에 대고 실제로 재봤더니 **지금 주석에 걸리는 것은 0건**이다 — 즉 지금 눈먼 가드는 없다(그래서 P3 이지 급하지 않다). 남은 39 는 «앞으로» 눈멀 수 있는 자리라 ㉠(공용 통로 경유)/㉡(왜 안 감싸는지 1줄)로 건별 판단이 남았다. 재는 법 = talk `node tools/가드계수.js .`(`--json` 있음). 🚫 위험 숫자만 0으로 만들기(감싸면 숫자는 줄지만 그 검사가 무엇을 재는지는 안 본 것이다) · 🚫 사본을 새로 지어 우회(`tests/소스검사통로.test.js` 변이 8/8 이 막는다) · 🚫 ❔모름을 「깨끗하다」로 읽기. 근거=[[guard-routing-and-collisions]]<br>🔍**표식 재판정 08-13 `local_0c6fbc37` — 〖지금〗 → 〖남이점유〗**: 산 세션 `ffb86589` 가 22:53 에 이 줄을 그대로 집어 선언했고(`5ff3f5b7` · talk `4f9c9f0` 「위험 39→33」), **0분 전까지** `SYNK-talk/tests/교정배치.test.js` 를 미커밋으로 들고 있다. 같은 talk 테스트 파일 묶음을 만지므로 병행하면 정면 충돌이다.<br>➕**이 줄이 안 다룬 별건(as 는 한 번도 안 재졌다)**: 같은 계수기를 as 에 대면 **사본 44·위험 78** 인데 공용 통로 `tests/lib/소스검사.js`·계수기 `tools/가드계수.js`·회귀 `tests/소스검사통로.test.js` 가 **talk 에만** 서 있다(as 엔 셋 다 없다). 「사본 0벌」은 talk 안에서만 참이다. as 단독이라 `ffb86589` 와 충돌 0 — P3 새 줄로 아래에 세운다.<br>✅ **2차 처분 08-13 `local_ffb86589` — 위험 39 → 14 (25건)** (talk `4f9c9f0`·`0ceb20e`·`7514027`·`aa4a052` · 정제경유 99→124 · **스위트 2020/2020** · 사본 0벌 유지). 처분한 25건은 전부 같은 얼굴이었다 — **«금지»를 설명하는 주석이 그대로 위반으로 잡히는 자리**라, 그 검사는 정확히 «가장 잘 적어 둔 파일»에서 깨진다(F401 그 사고). ➕통로를 새로 든 파일 전부에 **탐지력 픽스처**를 함께 세웠다 — 제거기가 죽는 날의 증상은 적색이 아니라 **초록**이다.<br>🔑 **두 가지를 실측으로 배웠다(다음 주자는 이것부터 읽는다)**: ①**통째로 감싸면 깨지는 파일이 있다** — `검수통로.test.js` 는 `함수본문()` 의 구간 앵커로 `/* ── §5 승인·폐기` 같은 **주석 배너**를 쓴다. 정제하면 앵커가 사라져 「이 검사가 통째로 미실행」으로 떨어진다(실제로 적색을 봤다). 거기서 주석은 설명이 아니라 «구조»다 → 원문은 남기고 «금지» 단언만 돌린다. ②**같은 파일을 여는 자리는 «전부» 같이 돌린다** — `가드계수` 는 파일 하나를 한 스코프로 근사하고 **첫 선언이 이긴다**. 한 이름(`src`·`통로`·`소스`)이 어떤 자리는 원문·어떤 자리는 정제면, 하나만 감쌌을 때 남은 원문이 「정제됐다」로 세어진다 — **숫자는 좋아지는데 사각은 남는** 가장 나쁜 결말이다. 못 합치면 이름을 가른다(`가입문항` 이 그 처분).<br>▶ **남은 14 = 성질이 다섯이고, 「감싸면 된다」는 하나뿐이다**(그래서 세다 만 것이 아니라 갈래가 갈린 것이다): ㉠**SQL 6건**(반피드백 :215·:222·:228 · 반배정 :135·:141 · 꼬리 :345) — `코드만` 은 **JS 렉서**라 SQL `--` 를 모른다. 공용 통로에 SQL 판이 **없고**, 계수기는 SQL 제거기 14벌을 「언어가 달라 합치는 대상이 «아니다»」로 이미 분류해 뒀다 — 그 분류를 뒤집는 일이라 **별건 판정이 선행**이다(감싸기 아님). ㉡**계수기 오탐 1건**(사건위조 :101) — `r.오류들.some(m => m.includes(…))` 는 **런타임 결과 배열**이지 파일 원문이 아니다. 처분은 코드가 아니라 계수기 쪽(🔴위험이 아니라 ❔모름이어야 한다). ㉢**파생 구간 3건**(계약 :111 `src.slice(i)` · 왕복시험모드 :88 한 줄 추출 · 꼬리 :345) — 자르는 **앵커가 주석인지** 먼저 본다. 주석이면 ①의 처분(원문 유지+금지만 정제), 코드면 그냥 감싼다. ㉣**마크다운 1건**(C0계약 :202 `스펙`=발주 문서의 ``` 코드펜스) — JS 주석 제거기의 대상이 아니다. ㉡처럼 «왜 안 감싸는지» 1줄이 답일 수 있다. ㉤**바로 집을 수 있는 JS 4건**(꼬리 :368 `답장화면.js` · 동의게이트 :96·:218·:248 — `글`=`함수(이름)` 추출이라 ㉢ 판정을 먼저) — **다음 세션은 여기부터**. 재는 법은 그대로 talk `node tools/가드계수.js .`(`--json`).


*(전부 닫혔다 — 2026-08-10 **F313 동봉 신호**(talk `post-commit`+`동봉신호.js` · talk `7dc50bb` · 회귀 14) · `동의발급.js` 술어 사본(검사 목록 편입 · talk `bf00ce8`) · 2026-08-09 두 항목: 리허설 배포 뒤처짐 `local_ddd51fde` · 역방향 회귀 `local_33c711a0`. 검증층은 지금 **전부 돈다** — `배포대조` 다름 0 · `배달왕복시험` **88/88** · `인증왕복시험` 41/41 · `교정왕복시험` 12/12.)*

---

## 지금 막혀 있어 여기 못 올리는 것 (유호님 답이 열쇠)

`node tools/decision-queue.js` 가 정본. 그중 **대기열을 실제로 막고 있는 것**만: C0 동결 범위 · Expo 결제(실기기 빌드) · 몽골어 검수자 1인. *(미니게임 게이트 AND/OR 는 08-10 OR 확정으로 빠졌다.)* *(LAB철학 판정 9건은 뺐다 08-10 — 차단 사유 「철학이 확정돼야」가 해소됐다: 철학 축은 유호 「다 유지」 08-09 + 3벌 확정. ⚠단 §6-5 무상/포인트(ST21·22 sink)·§6-6 참관 빈도·캐파의 **실행 수치 2건은 미확정으로 남아 있다** — 정본이 「빈도·무상/포인트·캐파는 실행지에서 확정」으로 넘겼다(SYNK_철학.md). 원문 `docs/_archive/LAB철학.md` §6 · 추적=메모리 marketing-philosophy-overhaul ⚠⑤⑥)*
