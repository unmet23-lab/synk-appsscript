<!-- 세션 보드 정본 조각 — 이 파일에는 내 줄만 쓴다(F250). 표는 `node tools/board.js` 가 조립한다. -->

| 2026-08-17 | **F584 — 등록을 바꾼 세션엔 그 훅이 없고, 그 미실행이 「통과」와 같은 모양이다**(인계 `67f76e1d` ▶첫확인 승계 · 새 선언 · 남의 줄 무변경) | `.claude/hooks/`(신설) · `.claude/settings.json` · `tests/` · 이 줄 (Code.js 0 · talk 0 · 운영 DB 0 · 배포 0 · 외부발행 0) | 🔵착수 (`local_fbfdb2e9`) — ✅선행 실측: memory-index-guard 네 층(셸차단·3자대조·미측정알림·처방되먹임) 전부 **다른 세션에서 발동** → F584 원인후보① 간접 확증 · ▶처방 ㉢은 자리를 옮긴다(등록 변경은 세션 «도중»이라 SessionStart 로는 못 잡는다) · ④해당없음(배관) |

