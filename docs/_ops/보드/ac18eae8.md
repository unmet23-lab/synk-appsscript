<!-- 세션 보드 정본 조각 — 이 파일에는 내 줄만 쓴다(F250). 표는 `node tools/board.js` 가 조립한다. -->

| 2026-08-17 | **F556 — 「놓은 자리」 판정이 사본으로 갈려 `--집기` 가 그 결론을 못 받는다**(정본의 유일한 이어받기 예외가 도구층에서 막혀 «손으로 보드 줄 쓰기»가 정상 통로가 됐다=F103 · board-guard 는 F285 로 이미 놓은 줄로 읽는다 · 새 선언 · 남의 줄 무변경) | `tools/lib/보드.js`(놓은줄 신설) · `.claude/hooks/board-guard.js`(사본→파생) · `tools/대기열.js`(점유) · `tests/대기열점유.test.js`·`tests/board-guard.test.js` · 이 줄 (운영 0 · 배포 0 · clasp 0 · talk 0) | ▶착수 (`local_ac18eae8`) — 실측 표줄 1097·상태칸 🎟 스팬 70: ㉡「유호 몫」 **0건**(그건 메모리 인덱스 층이다) · 부정 「없음」 **1건**이 진짜 오탐축 |
