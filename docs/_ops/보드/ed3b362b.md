<!-- 세션 보드 정본 조각 — 이 파일에는 내 줄만 쓴다(F250). 표는 `node tools/board.js` 가 조립한다. -->

| 2026-08-17 | **형제 저장소 경로를 «모양으로 짐작»하는 규칙이 네 벌 — git 에 묻는 정본 하나로**(🎫 승계 `local_91e2b70a` · 실측: 지금 살아있는 `head-ci` 워크트리에서 네 벌 **전부** 눈이 먼다 · 새 선언 · 남의 줄 무변경) | 신설 `.claude/hooks/lib/형제저장소.js`·`tests/형제경로통로.test.js` / 배선 `tools/lib/보드낡음.js`·`tools/형제초록.js`·`.claude/hooks/lib/handoff-store.js` + `tools/`·`tests/` 형제경로 호출부 12곳 (🚫`보드근거.js`·`이해대장.js` 제외) | 🔵착수 (`local_ed3b362b`) — 처방=`worktrees.mainWorktree()`(선 모듈 · 0.16ms) · 운영·clasp·배포·talk 쓰기 0 · ▶①정본 ②배선 ③래칫 래치 · ④해당없음(배관) |
