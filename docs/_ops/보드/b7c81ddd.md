<!-- 세션 보드 정본 조각 — 이 파일에는 내 줄만 쓴다(F250). 표는 `node tools/board.js` 가 조립한다. -->
<!-- ⚠ 줄 순서 = 인계 순서다 — `tools/인계문.js` 가 **마지막 줄**을 다음 주자에게 넘긴다.
     그래서 종결(✅) 줄을 위에, 아직 도는 줄을 **맨 아래**에 둔다. -->

# 보드 — b7c81ddd

| 날짜 | 트랙 | 만질 파일 | 상태/다음 |
|---|---|---|---|
| 2026-08-18 | **엔진 여섯 이름에 회사명 결합 — 「SYNK Loom·SYNK Prism·SYNK Temper…」**(유호 지시 08-18 「우리 엔진 폴더에 모든 엔진 이름 앞에 synk 붙이자」 · 새 선언 · 남의 줄 무변경) | `docs/엔진/_원고/*`(정본) → 굽기 산출 `docs/엔진/*` · `docs/엔진/SYNK_엔진_지도.html` · 경로 참조 `tools/브랜드렌더린트.js`·`tests/펠트문서.test.js` · 이 줄 (`Code.js` 무접촉 · talk 0 · 운영 DB 0 · clasp 0 · 배포 0) | ✅**착지 `accbe3aa`** (`local_b7c81ddd` · 가지 `claude/engine-synk-prefix-w8b4up`) — 원고 6벌+지도 v2.4 치환 → 재굽기 **재현 6/6** · 파일명 5벌 `SYNK_` 접두 `git mv`(원고·산출) · 규약 절 신설(접두를 **안 다는** 셋 = 층 이름·몽글·낱말 다루는 자리) · 회귀 신설 `tests/엔진이름.test.js` 5건(**변이 1/1 적색**) · **CI 모사 4649 fail 0** · 🆕F638 신고·해소 `972d30c3` · ⚠크롬 없어 `브랜드렌더린트` 렌더 검사는 **SKIP**(통과 아님) · ▶다음=PR · ⏳유호=엔진 폴더 **밖** 전파(설계 정본·DESIGN.md·발표물) 픽 · 완결 ④해당없음(문서층) |
