<!-- 세션 보드 정본 조각 — 이 파일에는 내 줄만 쓴다(F250). 표는 `node tools/board.js` 가 조립한다. -->
<!-- ⚠ 줄 순서 = 인계 순서다 — `tools/인계문.js` 가 **마지막 줄**을 다음 주자에게 넘긴다.
     그래서 종결(✅) 줄을 위에, 아직 도는 줄을 **맨 아래**에 둔다. -->

# 보드 — f959b83b

| 날짜 | 트랙 | 만질 파일 | 상태/다음 |
|---|---|---|---|
| 2026-08-18 | **F620 — 검사가 repo 밖(홈·워크트리·플랫폼)에 닿는 통로가 호출부마다 따로라 갈라진다**(세 얼굴은 남이 수리 완료 · 여기 남은 건 「원인을 쓸 수 없게」 · 새 선언 · 남의 줄 무변경) | tests/lib/* · repo 밖에 닿는 검사 13벌 · docs/_ops/장부/F620.md · 이 줄 (배포집합 무접촉 · 운영 0) | ✅**종결 `a8b88e30`(③)·`bcdf0584`(②)**(장부 해소 `23c12bf9`) — 원인은 ③이었다: `test-ci` 가 지우는 축을 ①②로 못박고 「초록이면 CI 도 초록」을 **단언**했다(CI=linux·로컬=win32 · F474·F608 과 같은 병). 이제 세 갈래(같다/다르다/**모른다**)를 이름 대고 낸다 + `부름만` 렌즈로 「홈을 방어 없이 읽는 검사」 금지 · ①공용 게이트는 **실측 기각**(호출부 3자리·전부 방어 중) · 회귀 11 · **변이 8/8+6/6**(구멍 0 — 내 구멍 2건은 변이가 잡아 고쳤다) · CI 모사 4583 fail 0(linux) · 완결 ✗모였나/✗닿았나/✗늘었나(배관) |
| 2026-08-18 | **F619 — 점유 신탁이 원격 전용 가지를 못 보는데 「재서 얻은 0」이라 말한다**(🎫 `ddd2c370` 이 놓은 줄 · 그 세션과 내 F608 이 15분 차 경합한 것이 이 값의 실증이다 · 새 선언 · 남의 줄 무변경) | tools/lib/보드.js · tests/보드*.test.js · docs/_ops/장부/F619.md · 이 줄 (배포집합 무접촉 · 운영 0) | ✅**종결 `741f8c66`+`b7d2f5cc`**(장부 해소 `7d862d75`) — 로컬+원격 합산 · 중복은 **sha 로** 접음 · 분모 `{로컬,원격,훑음}` 반환 · `원격:null` 이면 **모름**. 🔴수리가 그 자리에서 자기 값을 실증: F585 가 🎫→🔒 로 뒤집혔고 **F619 자신도 🔒** — `ddd2c370` 이 06:52 선언, 내 선언 07:03 = **11분 늦은 경합**(그쪽 코드 0줄 · 내가 먼저 착지 · 남의 줄 무변경 · 내 점유 확인 실수는 F627 로 신고). 회귀 8 · **변이 8/8**(구멍 0 — 변이가 잡은 내 구멍 2건 처방이 판정을 순수 함수로 뗐다) · CI 모사 4475 fail 0(linux) · ⚠㉢ fetch 시점은 **안 쟀다** · 완결 ✗모였나/✗닿았나/✗늘었나(배관) |
