<!-- 세션 보드 정본 조각 — 이 파일에는 내 줄만 쓴다(F250). 표는 `node tools/board.js` 가 조립한다. -->

| 2026-08-15 | **장부유실 검사가 워크트리 가지에서 거짓적색을 낸다 — F215 의 워크트리판**(유호 신고 08-15 · 새 선언 · 남의 줄 무변경) | tests/장부유실.test.js · docs/_ops/장부/F0NN.md (Code.js·talk·운영 DB·배포 전부 0) | 🔵**착수** (`local_d250230c`) — 실측: 이 워크트리에서 `F468`(남의 산 워크트리에만 있는 행)이 「유실」 적색 · 재료를 상류 하나 → 상류+기본가지+산 워크트리로 넓히고 「내 가지 이력에 있던 것은 유실」로 F123·F195 를 지킨다 · ④해당없음(배관) |
