<!-- 세션 보드 정본 조각 — 이 파일에는 내 줄만 쓴다(F250). 표는 `node tools/board.js` 가 조립한다. -->
<!-- ⚠ 줄 순서 = 인계 순서다 — `tools/인계문.js` 가 **마지막 줄**을 다음 주자에게 넘긴다.
     그래서 종결(✅) 줄을 위에, 아직 도는 줄을 **맨 아래**에 둔다. -->

| 2026-08-17 | **F578 — 배포 검수 게이트가 «자기가 뽑은 과녁»으로 자기를 못 연다**(머지 커밋에서 부모 기준이 갈린다 · 새는 방향=배포가 검수 없이 나감 · 새 선언 · 남의 줄 무변경) | `tools/codex-review.js`(과녁커밋·확인 처방) · `tests/` 회귀 신설 · 마찰 장부 · 이 줄 (`Code.js` 0 · talk 0 · 운영 DB 0 · clasp 0 · 배포 0) | 🔵착수 (`local_e9524e5d`) — 급소=`과녁커밋`:422 `git log --`(전부모)↔`대상결정`:398 `git show`(첫부모) · ▶①`--first-parent` 로 기준 일치 ②`--확인` 이 못 열 과녁을 미리 밝힘 ③회귀 · ④해당없음(배관) |
