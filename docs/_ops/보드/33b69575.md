<!-- 세션 보드 정본 조각 — 이 파일에는 내 줄만 쓴다(F250). 표는 `node tools/board.js` 가 조립한다. -->
<!-- ⚠ 줄 순서 = 인계 순서다 — `tools/인계문.js` 가 **마지막 줄**을 다음 주자에게 넘긴다.
     그래서 종결(✅) 줄을 위에, 아직 도는 줄을 **맨 아래**에 둔다. -->

# 보드 — 33b69575

| 날짜 | 트랙 | 만질 파일 | 상태/다음 |
|---|---|---|---|
| 2026-08-18 | **F575+F569 대기열 점유가 «밀린 좌표»로 빈 자리를 잠그고, `--집기` 는 죽은 주인의 미완 줄에 조립을 원리상 못 내준다**(같은 파일 한 벌 · `e9524e5d` 🎫 발굴분 · 새 선언 · 남의 줄 무변경) | `tools/대기열.js`(`점유()`·`--집기`) · `tests/대기열점유.test.js` · 이 줄 (읽기만=`tools/lib/보드.js`·`tools/작업본소유자.js` · 운영 0 · talk 0 · clasp 0 · 배포 0) | 🔵**착수** — ①F575 좌표낡음 한 줄(신고에 26/26 검증된 패치) ②F569 죽은주인+미완이면 승계 줄 조립 · 회귀는 픽스처 · ④해당없음(배관) |
