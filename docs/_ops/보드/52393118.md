<!-- 세션 보드 정본 조각 — 이 파일에는 내 줄만 쓴다(F250). 표는 `node tools/board.js` 가 조립한다. -->

| 2026-08-17 | **대기열 점유가 워크트리 보드 선언을 못 본다 — 코드 트랙 전건이 「빈 자리」로 불린다**(F521 신설 · 08-15 「워크트리+PR」 확정이 넓힌 구멍 · 새 선언 · 남의 줄 무변경) | PR 예정 · `tools/대기열.js`·`tools/lib/보드.js`·`tests/대기열점유.test.js` (엔진 0 · talk 0 · 운영 0 · clasp 0 · 외부발행 0) | ▶착수 — 실측 **초록 2 = 빈 자리 0 + 워크트리에 가려진 것 2**(#Q99←`93a2d32e` · #Q100←`24b6c036`) · ▶다음=공용 통로 `worktrees.all` 로 점유 분모 넓히기 · ④해당없음(배관) |
