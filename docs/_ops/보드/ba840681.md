<!-- 세션 보드 정본 조각 — 이 파일에는 내 줄만 쓴다(F250). 표는 `node tools/board.js` 가 조립한다. -->
<!-- ⚠ 줄 순서 = 인계 순서다 — `tools/인계문.js` 가 **마지막 줄**을 다음 주자에게 넘긴다.
     그래서 종결(✅) 줄을 위에, 아직 도는 줄을 **맨 아래**에 둔다. -->

| 2026-08-18 | **F600 — 작동대장의 「마지막 손댐」 나이가 매일 09시(KST) 이전엔 «음수»로 나온다**(주간 재설계 화면이 유호님께 -1일을 인쇄한다 · 그 파일 머리말이 「오늘이 둘이다」를 이미 경고했고 회귀 픽스처는 미래 날짜 가지를 원리상 안 밟는다 · 새 선언 · 남의 줄 무변경) | `tools/작동대장.js`(:270 손댐지도 · :333 지난날) · `tests/작동대장.test.js` · 이 줄 (talk 0 · 운영 DB 0 · clasp 0 · 배포 0 · `docs/작동대장.html`=파생) | 작업중 (`local_ba840681`) — ▶처방 ㉠=git `--date=unix` 로 받아 JS 가 UTC 로 접는다(축 하나 · TZ 환경 무의존 F296) · 🚫음수를 0으로 접기(F480·F207) · ④해당없음(배관) |
