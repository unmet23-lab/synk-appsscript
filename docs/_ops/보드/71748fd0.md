# 보드 — 71748fd0

| 날짜 | 트랙 | 만질 파일 | 상태/다음 |
|---|---|---|---|
| 2026-08-14 | **F415 처방 기계화 — `node tools/대기열.js --집기 #Qnn` 한 줄로 보드 선언**(무보호 창 「열고·재고·판단하는 5분」 → 5초 · 프로즈보다 기계 강제 · 앞 두 선언은 옆 세션이 먼저 닫아 무효 · 새 선언 · 남의 줄 무변경) | `tools/대기열.js` · 회귀 `tests/대기열*.test.js` · `.claude/hooks/board-guard.js`(판정을 **파생**한다 — 필요하면 export 한 줄만 · 사본 금지) · `DESIGN.md`·훅 라우팅 무접촉 · talk 0 · 운영 DB 0 · 배포 0 · 이 줄 | 작업중 (`local_71748fd0`) — ▶board-guard 의 겹침 판정을 부를 수 있는지부터 실측 → 없으면 거기서 파생 · 대가=유령 선언(보드수거·인계문이 이미 받는 자리) · 🚫판정 사본 짓기·가드 우회로 쓰기 |
