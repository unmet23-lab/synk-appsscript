| 2026-08-08 | **board-move 원칙⑥이 형제 저장소 커밋을 못 읽어 산 세션의 줄을 옮긴다**(해시가 talk 것이면 `rev-parse` 가 실패해 그냥 건너뛴다 · 실측: 「게임 4모듈」 줄은 지문 0 + 해시 2개 전부 talk → 산주인 0 인데 주인은 25분 전 박동 · F165 가 메운 사각의 옆 칸) | **appsscript: tools/board-move.js · tests/보드이관.test.js · 장부(F242)**(🚫훅층 전부=남 7세션 · 🚫board-guard=`f4639dff` · 🚫작업본소유자=`85ae6b62` · talk 0) | ✅**종결·원격초록**(**1d9edc0**·run 31234589836·장부 `ad88fb0`·`local_9c1fe204`) — 해시 조회가 `store.siblings` 순회(새 의존 0) · 회귀 2·22/22·변이 **4/4** · ➕회귀 구멍(죽은-주인 픽스처에 산 세션 0=미측정) · memory `board-move-sibling-hash` |
