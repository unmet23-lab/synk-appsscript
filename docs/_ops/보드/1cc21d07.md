<!-- 세션 보드 정본 조각 — 이 파일에는 내 줄만 쓴다(F250). 표는 `node tools/board.js` 가 조립한다. -->

| 2026-08-17 | **F501 — 비ASCII 환경변수 이름이 POSIX 셸에서 원리적으로 못 선다**(장부 열림 6건 중 「알고도 둔」 실결함 · 실측 `export MAESTRO_학생번호=900` → exit 1 not a valid identifier · 새 선언 · 남의 줄 무변경) | talk `.maestro/*.yaml`·`eas.json`·`src/관측.js`·`tools/앱밟기.js`·`tools/합성계정세우기.js`·`tools/precommit.js`+회귀 · 이 줄 (as 코드 0 · 운영 DB 0 · clasp 0 · 외부발행 0) | ▶**착수** (`local_1cc21d07`) — 9e243aa3 관측 줄은 ✅종결이라 무접촉 · 순서=전량 실측→ASCII 개명→`자격증명.js` 규칙에 회귀 얹기→변이 · ④해당없음(배관) |
