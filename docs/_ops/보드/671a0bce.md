# 보드 — 671a0bce

| 날짜 | 트랙/작업 | 만지는 파일 | 상태 |
|---|---|---|---|
| 2026-08-10 | **「안 골랐다」가 계약을 원리상 못 지난다 — `payload.position` 필수가 `skipped`·`rejected_all` 행을 전건 거부**(소비자 `안고름` 은 이미 서 있다 · S1-5 가 남긴 ➕별건 관찰 2건이 한 몸) | **talk**: `lib/이벤트검증.js`(널허용·택1)+회귀 · `lib/선택로그.js`·`src/말하기화면.js` 주석 · **as**: 이 줄·대기열 | 🔵**착수** (`local_671a0bce`) — ①검증기에 실제로 태워 거부를 실측 ②택1 `있음()` 이 `false` 를 「있다」로 세는 것 동반 수리(하나만 고치면 방어가 사라진다) |
