| 2026-08-09 | **C0 심문 B3 — 배정 0인 날 발화가 통째로 사라진다**(대기열 P0 마지막 · 소급 불가 · `8f826858` 이 B1·B2 종결 후 처방째 인계) — 배치가 죽은 날·첫배정 실패 날 `/tasks` 가 빈 배열 → 앱이 폴백으로 내려가 **서버로 안 보낸다** | **SYNK-talk: supabase/functions/tasks/index.ts · tests 신설분**(🚫`functions/events`·`lib/{요청해시,이벤트검증}`·`src/사건통로`=`8f826858` · 🚫`deliver` 본체 · 운영 DB 쓰기 0 · 배포 0) · **appsscript: 이 줄 + 대기열** | ▶**착수** (`local_47093f7d`) — 처방=배정 0이면 기존 단건 `deliver?learner_id=` **1회**만 부르고 다시 읽는다(새 엔드포인트 🚫 · 루프 🚫 · 앱이 `task_ref` 지어내기 🚫) |
