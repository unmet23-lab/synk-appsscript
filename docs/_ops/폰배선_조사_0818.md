# 「폰」 배선 전수 조사 → 제안 (F623 잔여)

> 유호 교정 2026-08-18: **「폰으로 작업하는 게 없는데 왜 폰 관련 작업을 계속 하고 있어? 이거 내가 저번에 말했었는데」**
> F623 이 남긴 몫 = 저장소 전반의 폰 배선을 **조사해 제안만** 올린다(처분은 유호 픽).
> 조사자 `local_ad0673f3` · 2026-08-18 · 이 문서는 **결정 재료**다. 코드는 한 줄도 안 고쳤다.

---

## 한 줄 결론

**「폰」이라는 낱말이 서로 다른 세 가지의 이름으로 쓰이고 있고, 유호님이 없앤 것은 그중 하나뿐이다.**
나머지 둘은 **지금 이 대화가 돌고 있는 바로 그 층**이라 지우면 이 창이 죽는다.
그리고 그 오해 때문에 **살아 있는 고장 하나가 147커밋째 방치**돼 있다 — 아래 §3.

---

## 1. 재는 층부터 고쳤다 (첫 판이 틀렸다)

`grep 폰` 을 그대로 돌린 첫 판은 **폰트(font)** 와 **스폰(spawn)** 을 같이 셌다.
그 판에서 1위는 `tests/브랜드폰트.test.js` **37건**, 2위 `tools/지도대장.js` **33건**이었는데 **전부 폰트**다.
그 숫자로 「폰 배선이 90파일」이라고 올렸으면 그게 곧 F522(검증 안 한 숫자를 재료로 올림)의 재발이다.

→ 경계를 박고(`스`·`아` 뒤 아님 · `트` 앞 아님) 다시 셌다. **아래 숫자는 그 판이다.**

## 2. 「폰」이 덮는 네 갈래 — 파일 52 · 줄 236 (그중 실행되는 줄 124)

| 갈래 | 뜻 | 줄 | 파일 | 살아 있나 |
|---|---|---:|---:|---|
| **㉠ 폰 «세션»** | 유호님이 폰에서 클로드를 돌린다 | 131 | 32 | ✗ **죽었다** (유호 확정) |
| **㉢ 제약 «환경»의 약칭** | 「CI·폰」 = 메모리 없음·자격증명 없음·codex 없음·세션 id 없음 | 24 | 14 | ✓ **살아 있다** — 오늘 그 조건을 지는 건 «클라우드 세션» |
| **㉡ 폰 «화면·기기»** | 학생·유호님이 폰으로 보고 녹음하고 인쇄물을 연다 | 14 | 9 | ✓ **살아 있다** — 이름도 맞다 |
| ❔ 규칙이 못 가름 | 대부분 `폰작업반입` 도구·회귀 **내부 이름**(변수 `폰판` · 픽스처 `폰산출물.md` · 브랜치 `폰`) | 67 | 13 | 그중 **44줄(66%)이 `폰작업반입` 두 파일** |

### ㉠ 이 실제로는 무엇을 하는가 — 이름만 「폰」이다

`tools/폰작업반입.js` 의 과녁은 **`claude/*` 브랜치**다. 오늘 그 브랜치의 주인은:

```
origin/claude/continue-work-em060r            64 seconds ago   <noreply@anthropic.com>   ← 이 세션
origin/claude/synk-continue-drmdf4             4 minutes ago   <noreply@anthropic.com>
origin/claude/synk-continue-cbjau8             5 minutes ago   <noreply@anthropic.com>
… (claude/* 9개 · 전부 noreply@anthropic.com = 클라우드)
origin/worktree-f-open-ledger-occupancy        3 hours ago     <unmet23@gmail.com>       ← 유호님 노트북
```

**폰 가지 0개.** 즉 그 도구는 폰 일을 하는 게 아니라 **클라우드 세션 일을 하고 있고**, 유호님은 지금 그 통로로 저와 대화하고 있다.
같은 얼굴이 `작업본소유자.js:1222`(「☁ 원격에 대기 중인 **클라우드(폰)** 브랜치 N개」)·`repo-staleness.js:107`·`friction.js:347` 에 있다 — 셋 다 **이 세션에서 실제로 화면에 떴다.**

### ㉡ 는 건드리면 안 된다

`Code.js:1533`(학생 휴대폰 폰트 폴백) · `엔진_콘텐츠AI.js:4634`(「30초 혼잣말을 폰에 녹음」) · `tools/이해대장.js:284`(「표는 폰에서 깨진다」) · `notebooklm-export.js:55`(휴대폰번호 = 개인정보 마스킹 패턴).
몽골 학생의 주 기기가 폰이다. 이 갈래는 **이름도 내용도 맞다.**

---

## 3. 🔴 이 오해가 지금 고장 하나를 덮고 있다 — 그게 이 조사의 급소다

CLAUDE.md 는 이렇게 못박는다:

> 커밋 주인은 추정하지 말고 **`Session-Id` 트레일러**를 읽는다(`prepare-commit-msg` 훅이 박는다 — author·모델명·시각 인접성은 구분자가 아니다 · F041).

**그 재료가 지금 0이다.** 실측(2026-08-18 · 이 컨테이너):

| 잰 것 | 값 |
|---|---|
| 이 클라우드 컨테이너의 `CLAUDE_CODE_HOST_SESSION_ID` | **undefined** (있는 건 `CLAUDE_CODE_SESSION_ID` 뿐) |
| `.git/hooks` 의 `prepare-commit-msg` | **없다** — `tools/install-githooks.js` 를 이 컨테이너에서 아무도 안 돌렸다 |
| 훅 원본 `tools/githooks/prepare-commit-msg:27` | `[ -n "$CLAUDE_CODE_HOST_SESSION_ID" ] \|\| exit 0` — 변수가 비면 **조용히 아무것도 안 한다** |
| master 최신 **147 커밋 연속** | `Session-Id` **없음**. 그 앞 첫 유효 트레일러 = `38612f2b`(`local_9b74b64b` = 노트북 세션) |
| 얕은 클론이 보는 285 커밋 | 트레일러 있음 126 · 없음 159 |

**왜 아무도 안 고쳤나** — 코드가 이 조건을 「폰에서만 산다」고 적어 뒀기 때문이다.
`.claude/hooks/lib/session-report.js:961~966`:

> *「폰 클라우드엔 `CLAUDE_CODE_HOST_SESSION_ID` 가 없어 그게 빈 문자열이다 … 하필 **인계가 유일한 통로인** 계정(폰)에서만 산다 … 진짜 세션 id 가 **폰에도** 생기면 이 가지는 저절로 안 탄다」*

폰이 아니라 **모든 클라우드 세션**이 그 조건이다. 「폰은 안 쓴다」로 읽고 넘긴 사이, 실제 주 통로에서 장치가 꺼져 있었다.

**오늘 제가 실제로 치른 값**: 보드·장부 도구를 부를 때마다 `export CLAUDE_CODE_HOST_SESSION_ID="$CLAUDE_CODE_SESSION_ID"` 를 손으로 붙였습니다. 안 붙이면 `board.js` 는 제 보드 줄을 「내 것」으로 못 알아보고, `board-move.js` 는 제 완료 줄을 제가 못 옮깁니다(그 도구가 스스로 :580 에서 「내 줄인데 막혔다면 이 환경변수가 비었는지 본다」고 말합니다). CLAUDE.md 의 「사람 손을 늘리는 해법을 쓰지 않는다」에 정면으로 걸리는 자리입니다.

---

## 4. 제안 — 세 갈래 (추천은 乙)

### 甲. 이름만 바로잡는다 · **로직 무변경 · 삭제 0**
㉠㉢ 의 「폰」을 실제 과녁으로 고쳐 적는다 — 「클라우드 세션」/「제약 환경(CI·클라우드)」.
`폰작업반입.js` 는 하는 일이 `claude/*` 반입이므로 이름이 곧 오해의 원천이다(파일명 변경은 참조 7곳 동반).
- **얻는 것**: 다음 세션이 같은 오해를 안 한다. 유호님이 화면에서 「폰」을 다시 안 본다.
- **대가**: 문구 변경이 넓다(줄 155 · 파일 46). 회귀 문구 앵커가 몇 개 같이 바뀐다. 행동은 안 바뀐다.

### 乙. 甲 + §3 의 고장 1건 수리 ← **추천**
클라우드 세션에서도 `Session-Id` 가 박히게 한다. 갈래 둘 중 하나를 골라야 한다:
- (ㄱ) 훅이 `CLAUDE_CODE_HOST_SESSION_ID` 가 비면 `CLAUDE_CODE_SESSION_ID` 로 **폴백**한다 — 가장 싸다.
  ⚠대가: 훅 머리말이 「그 변수는 메시지를 못 보내는 내부 id 라 쓰지 말 것」이라 적어 뒀다. 즉 **트레일러의 뜻이 「연락 가능한 id」에서 「구분자」로 바뀐다.**
- (ㄴ) 클라우드의 진짜 세션 id(`session_01…`)를 박는다 — 뜻이 안 바뀐다.
  ⚠대가: 그 값을 셸에서 못 읽는다(도구를 거쳐야 한다). 훅이 무거워진다.
- 어느 쪽이든 **훅이 이 컨테이너에 설치돼 있지 않은 것**은 따로 풀어야 한다(SessionStart 에서 설치기를 한 번 돌릴지).
- **얻는 것**: F041(커밋 주인 판별)의 재료가 147커밋 만에 돌아온다. 손 `export` 가 사라진다.
- **대가**: 훅 통로를 만진다 = 되돌림 비용이 있는 자리. 회귀와 함께 가야 한다.

### 丙. 폰 배선을 지운다
- **대가**: **지금 이 대화가 도는 층을 지운다.** ㉠ 의 실물은 클라우드 반입 통로이고 유호님이 그걸 쓰고 있다. 비추천.

---

## 5. 유호님이 고르실 것 (하나만)

> **「폰」 배선을 어떻게 처분할까요?**
> - **① 乙 (추천)** — 이름 바로잡기 + Session-Id 고장 수리. 수리 갈래는 (ㄱ)싼 폴백 / (ㄴ)뜻 보존 중 하나를 같이 골라 주세요.
> - **② 甲만** — 이름만 바로잡고 고장은 별건으로 남긴다.
> - **③ 지금은 안 한다** — 재료만 남기고 닫는다.

무엇을 고르시든 **삭제는 0**입니다. 이 조사에서 지운 파일·줄은 없습니다.

---

## 6. 안 잰 것 (F348 — 명시한다)

- **실사용을 못 쟀다.** `폰작업반입.js` 의 「transcript 393세션」(도구실사용_실측_0813)은 **실행 증거가 아니다** — `작업본소유자.js:1239` 가 SessionStart 마다 `node tools/폰작업반입.js` 를 화면에 찍기 때문에 세션 수에 수렴한다. 진짜 실행 횟수는 이 컨테이너에 transcript 가 없어 못 잰다.
- **얕은 클론이라 285커밋까지만 봤다.** 그 밖의 트레일러 상태는 안 쟀다.
- **❔ 67줄**은 규칙이 못 가른 것이다. 파일 분포로만 성격을 말했다(44줄이 `폰작업반입` 두 파일).
- **㉢ 환경 축이 몇 곳에서 «판정 근거»로 쓰이는지**는 안 셌다 — F623 처방 후보 ①(낡은 전제의 과녁을 회귀로 센다)이 그걸 세는 장치다. 그건 이 조사가 아니라 그 처방의 몫이다.
