# 자격증명 교체 및 Instagram 상담 연결 완료 — 2026-09-11

이 문서는 2026-09-11에 실제로 반영하고 확인한 범위를 기록한다. 비밀번호, API 키, 토큰, 검증 문자열 원문은 포함하지 않는다.

> **같은 날 정정:** 최초 보고는 남아 있던 Notion 토큰을 현행 기능으로 오인했다. Notion은 8월에 사용 종료됐으며, 토큰 갱신은 불필요한 작업이었다. 9월 11일 후속 작업에서 해당 통합을 삭제하고 Apps Script 속성도 제거했다. 사용자 지적 전에 “사업자 인증만 남았다”고 보고한 완료 판정은 잘못됐다. 설정 확인과 업무 사용처 검증을 함께 해야 한다.

## 최종 판정

- Anthropic, GitHub, Supabase 명부 수신, Meta 웹훅, Instagram 상담 토큰을 교체했다. Notion은 사용 종료에 맞춰 통합과 속성을 제거했다.
- 실제 API 연결과 실사용 여부는 아래 표의 검증 범위로 구분한다. GitHub 연결 시험은 현재 중지된 학생 자료 반출의 허용이나 업무 성공을 뜻하지 않는다.
- Apps Script 원격 프로젝트는 운영 파일 23개이며 임시 검증 파일은 0개다.
- Instagram @synkbrief에서 @synk.mn으로 보낸 실제 DM에 상담 봇이 자동 응답했다.
- 신규 사업자등록과 Meta 사업자 인증은 진행하지 않았다. 현재 새 사업자등록증이 없다는 사용자 확인에 따른 범위다.

## 서비스별 반영과 확인

| 대상 | 반영 | 확인 증거 | 이전 자격증명 처리·기한 |
|---|---|---|---|
| Anthropic | Apps Script Claude 키 교체 | `aiConnectionCheck()` 성공 | 이전 Apps Script 키 삭제 |
| Gemini | 현재 Apps Script 원격 소스의 미사용 키 정리 | 원격 소스에서 키 참조 0건 | 이전 API 키 삭제, 죽은 Script Property 제거. API 키와 별개인 서비스 계정은 유지 |
| GitHub | SYNK-talk 한 저장소의 Contents 읽기·쓰기만 허용한 fine-grained PAT 연결 | `골든전송점검_()` GET 성공. 목적별 동의 확인 전 학생 자료 반출은 중지 상태 | 이전 PAT 삭제, 새 PAT 만료 2026-12-10 |
| Notion | 잘못 갱신했던 `SYNK 앱 동기화` 통합 삭제, `NOTION_TOKEN` 제거 | 공식 연결 목록 `No connections yet`; 소유자 실행 22:22 KST `notionPropertyAbsent:true`; 운영 소스 23개에 Notion 전송 함수/API 0건 | 통합 삭제로 접근 권한 폐기. 이전에 적었던 9월 18일 토큰 유예 관리 일정도 종료 |
| Supabase | 운영·리허설 양쪽의 `ROSTER_INGEST_SECRET` 교체, Apps Script 수신 키 교체 | 인증된 빈 요청이 `bad_body`로 도달해 인증 통과 확인 | 운영·리허설 모두 새 값 반영 |
| Meta 웹훅 | Instagram callback URL 키와 verify token 교체 | Meta 설정 저장 후 재조회, 외부 challenge 요청 HTTP 200 및 challenge 일치 | `messages` 관련 구독과 앱 게시 상태 유지 |
| Instagram | @synk.mn Instagram Login 토큰 교체 | Graph API에서 username `synk.mn` 확인, 기존 발급시각 기록 함수 성공 | 발급시각 기록 완료, 오래된 갱신 시도 상태 제거 |

## 상담 봇 실사용 확인

실제 Instagram DM으로 서로 다른 두 질문을 확인했다.

1. `SYNK SHIFT는 어떤 경험과 과정을 보여주나요?`
   - 상담 봇임을 먼저 고지하고 사람이 답하도록 연결했다.
   - 현재 상담 봇은 SYNK LAB 공개 상담 전용이며 SHIFT·PULSE의 내부 또는 별도 사업 내용을 공개하지 않는 경계와 일치한다.
2. `SYNK LAB에서는 AI를 어떻게 활용하나요?`
   - 한국어로 자동 응답했다.
   - AI를 금지하는 방식이 아니라 맡길 일과 스스로 판단할 일을 구분하고, 첫 시도와 마지막 확인은 학생이 맡는다는 현재 교육 원칙을 설명했다.
   - 가격·일정·성과 보장·민감한 운영 정보는 답변에 포함하지 않았다.

이 검증은 연결, 언어 선택, 공개 정보 답변, 범위 밖 질문의 인계 동작을 확인한 것이다. 미래의 모든 자연어 질문이 항상 정확하다는 보장은 아니다.

## 정리 상태

- Apps Script 원격 소스: 23개 파일, `__SYNK*` 임시 파일 0개.
- 교체 과정에 만든 로컬 임시 스크립트·비밀 파일 9개를 삭제했고 잔존 0개를 확인했다.
- 브라우저 자동화 세션에 보관한 임시 비밀값도 해제했다.
- 이 보고서와 저장소 어디에도 새 자격증명 원문을 기록하지 않았다.

## 다음 날짜

- 2026-12-10: GitHub fine-grained PAT 만료 전에 교체 필요.
