# 카피 전수 감사 표 — 「따뜻하되 직관적」 콘텐츠 층 (F394 닫는 자리)

> **작성 2026-08-13 · 세션 660dcb6e · 상태 = 조립 중(층 A·B·C 추출 진행)**
> 근거: 유호 확정 08-13(F394) · 규칙 층 커밋 `3db8e737` · 낱말 교체 경계 = `docs/_ops/철학_리라이팅_장부.md` §1-b · 대기열 P1 줄36.
> **이 표는 판정 재료다 — 어떤 교체도 유호 확정 전에 실행하지 않는다.**

## 0. 기준 (규칙 층에서 그대로 승계 — 여기서 재발명 안 함)
- 감사 기준 = **「첫 독해에 뜻이 서나」**(처음 보는 몽골 학생과 한국인 둘 다).
- 확정 표기: 오답 리프레이밍 = **「다음에 맞힐 문제」**. 「내일의 필살기」·「다음 필살기」 = 구 표기(재사용 금지).
- 대체어 3조건: ①첫 독해에 뜻 ②몽골어 역번역 생존 ③온기 = 관점(잃는 쪽 대신 얻는 쪽 세기). 은유·별칭 단독 금지.
- 3갈래: **㉠고침**(제안 교체문 병기) / **㉡살림** / **㉢신중**(기능명·한몽병기·데이터 연쇄 — 연쇄를 명시).
- 한·몽 병기 카드 = 무조건 ㉢(몽골어 원어민 검수 선행 — 한국어만 먼저 바꾸면 한 카드 안에서 말이 갈린다).
- 몽골어는 검수 없이 지어 넣지 않는다.

## 1. 범위 (분모)
- **층 A** 문서 정본·교재: 정본 .txt 6벌(소개서·FAQ·기업마스터·수업규칙·커리큘럼·05운영) + 교재 4문서 + 커리큘럼 2벌 + 교수법 매뉴얼(학생 대면부) + synk-content references.
- **층 B** SYNK-talk 앱 화면 문구 전량(src 전 파일 + lib 문구 생산자 · 말하기화면.js는 HEAD 기준 — 살아있는 세션 704d62b2 작업본 회피).
- **층 C** contents_*.js 4벌 + 엔진 층(Code.js·교재연동.js·엔진_콘텐츠AI.js·엔진_운영배치.js·엔진_셋업확장.js) 학생 대면 문자열 — ⚠엔진 층은 대기열 원문에 없던 확장(실측: 「필살기」가 엔진 층에 19곳)이라 **판정만** 싣고 교체는 /deploy 트랙.
- 제외: docs/_archive/**(구판) · docs/_ops/** 장부(append-only) · 세션보드·버전이력.

## 2. 세션이 직접 실측한 ㉢ 연쇄 — 「필살기 노트」 (기능명 + 라이브 데이터)
| 자리 | 실측 | 연쇄 |
|---|---|---|
| 라이브 시트 열 108 `필살기노트` | `tests/safety.test.js:425` 예약 열 대장 · 교재연동.js 소유(v9.58) | **라이브 데이터 열 이름** — 개명은 마이그레이션 사안 |
| `.claspignore:6` 주석 「목소리 타임랩스+필살기 노트 엔진」 | 개발자 주석 | 낮음(주석) |
| `docs/GLIDE_조립가이드_v947.md:260` 열 대장 | Glide 폐기 확정(app-rebuild-native) — 구판 문서 | 없음(문서 자체가 은퇴 대상) |
| `docs/교재_앱_연동_매핑_v1.md` 「필살기 노트」 | 교재 4문서의 하나 — 층 A 표에 합류 | 교재 연쇄 |

**판정 갈래(유호 몫)**: ⓐ기능명 유지 + 학생 대면 설명 카피만 직관화(연쇄 0 · §1-b ㉡의 원칙) / ⓑ개명(예: 「다시 도전 노트」류 직설명 — 시트 열·교재·코드 3연쇄 + 몽골어 동시 확정 필요). 권고는 층 A·B 추출이 끝난 뒤 노출 빈도를 보고 낸다.

## 3. 층 A — 문서 정본·교재 (조립 대기)
## 4. 층 B — 앱 화면 (조립 대기)
## 5. 층 C — 코드 콘텐츠 (조립 대기)
## 6. 집계·판정안 (조립 대기)
