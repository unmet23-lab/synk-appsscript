# 라디오 굶음 — ✅ 닫혔다 (2026-09-07 21:47 KST)

> 이 문서는 09-07 저녁의 «인계문»이었다. 그 일이 끝나서 **결과판으로 갈아 썼다.** 가설·갈래 셋은 지웠다(git 이력이 쥔다).
> 파일 = `bots/오버레이/겹쳐송출.js` · 커밋 `c08a894e2`(①) → `60ccec54f`(②) · 서버 = `synk@34.71.111.97` · 서비스 = `radio-live`

---

## 0. 결론

**방송이 실시간을 정확히 따라간다.** 실시간대비 0.803 → **1.0**(천장). 방송에 들어가는 그림 초당 8.0 → **10.0/10**. 13분 내리 흔들림 없고 그 사이 안 죽었다.

**기계는 하나도 안 고쳤다.** 「불린 박자」는 지금도 초당 8.0 이다 — 시계는 여전히 10번이 아니라 8번 울린다. 느린 기계 위에서 방송만 제 속도를 되찾았다.

실측(12:34:20 판 · 13분):

```
12:35  1x  내보낸 것 초당 10.0/10  불린 박자 8.0/10  거른 박자 0  버린장 0  겹친장 0
12:41  1x  내보낸 것 초당 10.0/10  불린 박자 8.0/10  거른 박자 0
12:47  1x  내보낸 것 초당 10.0/10  불린 박자 8.0/10  거른 박자 0
```

---

## 1. 무엇이 굶기고 있었나 — 두 겹이었다

**겉의 겹.** 한 장이 216KB 인데 Node 의 기본 물그릇은 16~64KB 다. 그래서 `ff.stdin.write()` 가 **한 번도 빠짐없이** 「가득 찼다」를 냈고, 옛 코드는 그 답을 보고 그 박자를 건너뛰었다.

**속의 겹(진짜).** 게이트를 걷어도 초당 8.0 그대로였다. 그런데 그때 같이 심은 **「거른 박자」가 0** 이었다. 「막지도 않았는데 8장뿐이다」가 서면서 답이 갈렸다 — **박자를 부르는 시계 자체가 안 울린다.** 100ms 로 맡긴 것이 실제로는 125ms 마다 온다.

까닭은 기계다(11:25 실측).

| 잰 것 | 값 |
|---|---|
| 기계 | e2-medium (코어 둘을 **나눠 쓰는** 값싼 등급) · nproc 2 |
| 크로미움 | 81.7% (43.7 + 24.4 + 13.6) |
| ffmpeg | 41.7% |
| 합 | **200% 중 123%** |
| 일감이 CPU 를 기다리는 비율 | **33.3%** (`/proc/pressure/cpu` some avg60 · full = 0) |

시계 지연 25% 와 그 33% 가 같은 곳을 가리킨다.

`-framerate 10` 은 ffmpeg 에 «한 장 = 0.1초»라는 약속이다. 그래서 8장만 들어가면 층의 시계가 **0.80배**로 흐르고, overlay 가 그 느린 시계에 output 을 묶어 방송 전체가 0.8배가 된다. **실측 0.805 = 8.0 ÷ 10 · 세 자리가 맞았다.**

---

## 2. 고친 것 셋

1. **흘려보내는 쪽** — 「가득 찼다」는 답 대신 «쌓인 양»으로 판정한다(여덟 장 = 0.8초까지 그냥 쓴다 · 램은 1.7MB 에 묶인다).
2. **벽시계가 장 수를 정한다** — 「틱 한 번 = 한 장」을 버렸다. 시계가 늦게 울려도 그 사이 흘러간 시각만큼 몰아 쓴다(한 번에 1초치까지). **이것이 값을 1.0 으로 올린 걸음이다.**
3. **방송 열쇠 가리기** — ffmpeg 이 끊길 때 내는 문장에 rtmp 주소 전체가 들어 있었고 그걸 그대로 일지에 옮기고 있었다(24시간 일지에 2줄 실측). 이제 찍기 전에 가린다.

보고에 **「불린 박자 초당 N/10」**과 **「거른 박자」**를 실었다. 이 둘이 「못 삼켰다」와 「안 불렸다」를 가른다.

---

## 3. 곁들여 닫힌 것 · 앞 인계문에서 바로잡은 것

- ✅ **곡 종류는 범인이 아니다.** 전자 → 시티팝 전환을 두 번 봤는데 실시간대비가 0.803 에서 미동도 안 했다(12:23:44 · 12:32:24).
- ✅ **압축(preset)도 범인이 아니었다.** `veryfast` → `ultrafast` 로 기계 부담은 절반이 됐는데 값은 0.82 → 0.84 로 거의 안 움직였다.
- 🔴 **「20시간에 14번 죽었다」는 과장이었다.** 죽은 무늬를 다시 세어 보니 **굶어 죽은 것은 10:40:33 하나**(유튜브가 `End of file` 로 끊음)이고, 나머지는 우리가 끊은 것(SIGKILL)이거나 다른 까닭이다. 05:51~09:18 은 3시간 27분을 살았다.
- 🔴 **「내보낸 것」이 늘어나는 것은 좋은 얼굴이 아니다.** 죽은 판에서 그 수가 분당 480 → 580 으로 오르는 «동안» 실시간대비가 0.82 → 0.456 으로 처졌다. ffmpeg 의 입력 대기줄(`-thread_queue_size 512` = 51초치)이 밀린 그림을 쓸어 담기만 했기 때문이다.

---

## 4. 🔴 유호님 앞에 걸린 것

- ✅ **방송 열쇠 재발급 — 09-08 02:40 에 갈았다**(유호 「전부 이어서 해줘」). 09-07 22:0x 에 내가 서버 일지를 가리지 않고 불러 대화에 찍힌 값은 죽었다. 길 = `node tools/라디오방송열쇠.js --갈기`(새 스트림 「상시 3」 · 방송 자리 `l6MiHzDNwKQ` 그대로 · 서버 .env 두 칸만 갈아 넣음 · 옛 스트림 삭제 · 단계 testing 되돌림). 🔑 옛 스트림을 지워도 돌던 ffmpeg 은 스스로 안 끊긴다 — 송출은 `fuser -k -n tcp 8790` 으로 되살렸다(sudo 없이). 같은 시각 옆 세션의 곡 인코딩(팩 11~13)이 서버 부하를 12 까지 올려 몇 분 굶었고, 그 배치가 끝나자 0.98x 로 돌아왔다.

---

## 5. 남은 것 — 굶음과는 «다른 자»다

- **층 그림이 초당 3.5장만 뜬다**(목표 10). 크로미움이 화면을 뜨는 속도 자체가 모자란다. 코드가 「못 뜬 순간엔 직전 장을 다시 쓴다」로 되어 있어 **굶음과는 무관하고**, 화면이 뚝뚝해 보이는 문제다. 🔴 유호님 멀미 이력이 있는 자리라(기억 `mascot-motion-makes-yuho-sick`) «덜 움직이는» 쪽은 오히려 안전하다 — 고칠지 말지는 화면을 보고 정한다.
- **유튜브 쪽 화면을 직접 안 봤다.** 시청자에게 어떻게 보이는지는 서버 쪽 수로만 판정했다.
- **`ultrafast` 가 화질을 얼마나 내줬는지 안 재봤다.** 눈으로 안 봤다.
- **`마스코트.html` 이 «다 된 판»인지 안 봤다.** 09-07 오전에 끊긴 세션이 유호 지시(18:34 「몽글이랑 까몽이 셋 다 지금 마린처럼 눈 깜빡이거나…」)를 받아 14번 고치던 중 한도로 끊겼다. 커밋은 됐지만 화면 확인은 안 했다.

---

## 6. 재는 명령

지금 굶고 있나(1분마다 한 줄):

```bash
ssh -i ~/.ssh/synk_radio synk@34.71.111.97 'journalctl -u radio-live --since "-30m" --no-pager | grep 실시간대비 | tail -20'
```

몇 번 다시 섰나:

```bash
ssh -i ~/.ssh/synk_radio synk@34.71.111.97 'systemctl show radio-live -p NRestarts -p ExecMainStartTimestamp'
```

고친 판 올리기(재시작은 `sudo` 라 **내 손이 안 닿는다** — 유호님 터미널 몫):

```bash
scp -i ~/.ssh/synk_radio bots/오버레이/겹쳐송출.js synk@34.71.111.97:/opt/synk-radio/지면/bots/오버레이/겹쳐송출.js
```

> 🔴 **일지를 부를 때는 열쇠를 가린다.** `| sed -E 's#(live2/)[^ "]+#\1<가림>#g'` 를 뒤에 붙인다. 09-07 에 이걸 빼먹어 열쇠가 대화에 찍혔다.
> 🔴 **유호님 터미널에서 `ssh` 가 죽으면** 서버가 아니라 열쇠 파일 권한을 먼저 본다(기억 `ssh-key-perms-block-user-terminal`).

---

## 7. 09-08 새벽 — 화면 손질(유호 지적 여섯 · 0원)

유호님이 미리보기를 보고 지적하신 것 = 「배경이 별로 안 움직인다 · 부스 옆에 뭐가 삐져나왔다 · 부스 위에 꽃이 붙어 있다 · 깜빡일 때 헤드셋이 안 따라온다」. 판정과 고친 것:

| 지적 | 뿌리 | 고친 것 |
|---|---|---|
| 배경이 안 움직인다 | 무대 층의 움직임은 **방송에 안 나간다** — 배경은 팩 영상이다 | 무대 층에 결별 빛 지도(가로등·창·네온·물비침) → `tools/무대영상굽기.js` 로 60초 루프 영상 → 전자 팩 넷 다시 구워 갈아 끼움 |
| 부스 옆 삐져나옴 | 부스 그림 왼쪽의 흰 종이 잔재 | `tools/부스다듬기.py` — 몸통만 남기고 네모로 잘랐다 · 폭 22vw · 디스크 자리 재측정 |
| 부스 위의 꽃 | 마린 가슴 꽃이 부스 윗선 위로 삐죽 | 부스가 서는 결에서는 가슴 조각을 안 입힌다(`부스뒤옷`) |
| 깜빡일 때 헤드셋 고정 | 눈감음 컷이 본체보다 5칸 아래(눈웃음 11 · 놀람 26) | 윤곽선 상호상관으로 되밀고, 눈만 바뀌는 컷은 «본체 몸 + 눈»으로 구워 몸이 바이트까지 같다(마린 6컷) |

- 층은 **재시작 없이** 새로 열었다 — 서버 크로미움 CDP(9222) `Page.navigate` + 결 사건 다시 던지기(기억 `radio-layer-reload-without-sudo`). 방송은 안 끊겼다(초당 10/10 · 1x).
- 겹쳐송출.js 기본값(`--부스 1` · house → 전자네온물가)은 서버에 올려 두었고, 같은 밤 열쇠 교체 때의 되살리기(§7-2)에 섰다 — 유호님 재시작은 필요 없어졌다.
- 🔴 `라디오무대만팩.sh` 에 한글 변수 `층=` 을 썼다가 넷을 옛 영상으로 구웠다(머리글 경고를 내가 밟았다) — `layer` 로 고쳐 다시 구웠다.
- 남은 것 = 까몽 응원 컷 22% 큼(마린만 서는 동안은 화면에 안 나온다). 시티팝 나무·파도와 열쇠 재발급은 §7-2 에서 닫혔다.

### 7-2. 같은 밤 이어서 — 시티팝·차분도 층 영상 · 열쇠 교체 (유호 「전부 이어서 해줘」 · 0원)

- **시티팝**(유호 지적 09-07 「나무 흔들리는거랑 파도치는거 부자연스러워」) — Veo 영상을 내리고 층 영상으로. 나무는 안 움직이고, 부두 오른쪽 바다만 다각형으로 오려 일렁이게 하고, 등대 불이 4.4초로 숨 쉰다. 그림은 세로 44% 로 조금 위로 잘라 등대 꼭대기가 숨 쉬는 확대에 안 잘린다(옛 팩도 40% 였다). 옛 Veo `citypop.mp4` 는 지웠다(git 이력).
- **차분** — 정지 그림이던 것을 층 영상으로. 창틀 안 호수만 아주 여리게(네온의 절반) 일렁이고 달·달 비침이 30초로 숨 쉰다.
- 곡 여덟(시티팝 6 · 차분 2)을 다시 구워 서버 `팩/` 에 갈아 끼웠다(옛 판 = `팩_옛_citypop_Veo영상/` · `팩_옛_calm_정지그림/`).
- **방송 열쇠 교체**는 §4 ✅ 줄. 실측 셋: ① 옛 스트림을 지워도 돌던 ffmpeg 은 스스로 안 끊긴다 ② `fuser -k -n tcp 8790` 이 sudo 없는 재시작이다(systemd Restart=always) — 새 열쇠와 겹쳐송출.js 새 기본값이 이 재시작에 섰다 ③ 방송 자리를 «제목»으로 찾으면 제목이 바뀐 날 죽는다 → 묶인 스트림으로 찾는다.
- ⚠ **옆 세션**이 같은 시각 서버에서 팩 11~13(수노 시티팝)을 구웠다(부하 12/2코어 · 몇 분 굶음 · 끝나자 0.98x). 그 셋은 `playlist.txt` 에 아직 없다 — 그 세션 몫이라 손대지 않았다. 재생목록이 바뀌면 송출을 한 번 되살려야 한다(`fuser -k -n tcp 8790`).
