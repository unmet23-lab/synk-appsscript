## 새로 더할 것

- **철거 완료의 증거.** 무엇: 각 항목에 저장소·`파일:줄`·삭제할 정확한 문장/심볼·검색 범위·대체 경로의 실행 결과를 붙인다. **낱말 미검출만으로 완료 처리하지 않는다.** 왜: 산출물에서 지워도 원고가 남으면 다음 굽기가 복원한다. 실물은 [tools/펠트문서.js:731](C:/Users/q1212/Documents/SYNK-appsscript/tools/펠트문서.js:731)의 `지면방`·`지면짝들()`·`재현대조()`다. 원고와 산출물의 재현 대조까지 철거 조건에 포함한다. 붙일 곳: §13 머리의 「적는 규약」 뒤.

- **점수 장부의 옛 읽기 방식도 걷는다.** 무엇: [tools/이해대장.js:343](C:/Users/q1212/Documents/SYNK-appsscript/tools/이해대장.js:343)의 `!Number(r.무응답)`·전체 장부 마지막 행 선택·`판`만 같은 직전 행 비교와, [tools/rot-check.js:189](C:/Users/q1212/Documents/SYNK-appsscript/tools/rot-check.js:189)의 별도 `실점수` 판정을 통합 대상에 넣는다. `engine`·`kind`·`판`·시험지와 분모가 맞는 기록끼리 비교하고, `null`·필드 누락을 미측정으로 처리한다. 왜: 기존 함수를 메모리에서 실행한 합성 입력 **2/2건**에서 서로 다른 엔진끼리 비교하거나 `전체:null`을 점수 하락으로 계산했다. 붙일 곳: §13-1(가) 「별도 점수 장부」 행 바로 아래.

- **일시적 보류를 영구 처분으로 만드는 경로.** 무엇: 재측정 실패·일시 장애를 기존 `--처분`으로 닫는 관행을 걷는다. 재개 조건과 그 조건을 실제로 확인하는 호출자가 서야 감시 통합을 완료한다. 왜: [tools/모델정찰.js:504](C:/Users/q1212/Documents/SYNK-appsscript/tools/모델정찰.js:504)의 `처분짝들()`은 `종류`·`모델`·`신판`만 보고 영구 집합을 만들며, 같은 파일 652행은 그 짝을 계속 `처분된신판`으로 보낸다. 사유에 재시도를 써도 재개되지 않는다. 붙일 곳: §13-1(가) 「감시기·재심 방아쇠」 행 아래.

- **감시 실행 자리 변경으로 사라지는 출처.** 무엇: CI 통합 전에 로컬 출처의 승계 방법을 명시한다. 왜: [tools/모델정찰.js:328](C:/Users/q1212/Documents/SYNK-appsscript/tools/모델정찰.js:328)의 `코덱스캐시읽기()`는 로컬 캐시가 없으면 `확인불가`를 던지고, 692행은 전체 `코드`를 확인 불가로 만든다. 현재 호출자는 [.claude/settings.json:140](C:/Users/q1212/Documents/SYNK-appsscript/.claude/settings.json:140)에 있다. 공개 페이지를 읽는 CI가 섰다는 사실만으로 로컬 출처까지 승계됐다고 셀 수 없다. 붙일 곳: 같은 감시 통합 행의 철거 조건.

- **금지패턴의 실제 사본.** 무엇: [tools/혼잣말검사.js:27](C:/Users/q1212/Documents/SYNK-appsscript/tools/혼잣말검사.js:27)의 `한글수사`·`단위`·`금지패턴` 사본을 실명으로 등재한다. 왜: [SYNK-talk/tests/lib/금지패턴.js:18](C:/Users/q1212/Documents/SYNK-talk/tests/lib/금지패턴.js:18)에 같은 정의가 있고, 본문 §6 U4도 이 사본 철거를 명시하지만 §13에는 빠졌다. **금칙 완화 여부를 봤다 → 사본만 걷고 탐지 규칙과 탐지력 시험을 승계해야 한다.** 붙일 곳: §13-4의 발화검문 항목 뒤.

- **계약 사건과 과거 기록은 철거 대상에서 제외한다.** 무엇: Core U3의 `content.viewed` 재사용안을 걷더라도 기존 사건 자체는 유지한다고 명시한다. `source_kind` 정리도 기존 `learning_events`의 갱신·삭제로 집행하지 않는다. 왜: [SYNK-talk/lib/이벤트검증.js:210](C:/Users/q1212/Documents/SYNK-talk/lib/이벤트검증.js:210)은 `content.viewed`에 `parent_event_id`를 요구하고, [L0_데이터계약.md:46](C:/Users/q1212/Documents/SYNK-talk/docs/L0_데이터계약.md:46)은 `learning_events`를 append-only로 규정한다. **소급 불가 스키마를 봤다 → 설계안 폐기를 과거 사실 삭제로 확대하면 문제다.** 붙일 곳: §13-10.

## 지금 판이 틀린 것

- **어디: §13 첫 문장.** 무엇이 틀렸나: 「채택되는 날」을 현행 상태 문장까지 지우는 공통 시점으로 삼았다. 설계 채택과 구현 착지는 다르다. 무엇을 봤나: [본문 6371행](C:/Users/q1212/Documents/SYNK-appsscript/docs/엔진7종_상향설계_v2.md:6371)은 착지 커밋에 문서 갱신이 동행한다고 하고, 2795행은 정본 통로가 서기 전에는 「짓는다」라고 쓰도록 한다. **미채택안은 채택일에 폐기하고, 현행 동작 설명은 대체 동작이 서는 날 교체해야 한다.**

- **어디: §13-7 「판올림 = 커밋 즉시 라이브」(D4 뒤).** 무엇이 틀렸나: D4의 어느 선택지를 택하든 걷는 것으로 적었다. 무엇을 봤나: [D4 6539행](C:/Users/q1212/Documents/SYNK-appsscript/docs/엔진7종_상향설계_v2.md:6539)은 **② ⓑ가 현행 유지**이고, 6541행은 그 선택지를 권고한다. 철거 조건은 `D4 ② ⓐ 채택`으로 좁혀야 한다.

- **어디: §13-1(가) 「돈 규칙 여섯 → 값 둘」.** 무엇이 틀렸나: 본문에 남아 있는 돈 통제를 누락한 채 전량 통합을 선언했다. 무엇을 봤나: [D13 6725행](C:/Users/q1212/Documents/SYNK-appsscript/docs/엔진7종_상향설계_v2.md:6725)은 월 구독을 별도 갈래로 남기고, 6730행은 회전당·분기 상한을 다시 둔다. **돈 규칙을 봤다 → 현재 문장은 문제다.** 기존 규칙별로 D13·D28에 흡수되는지, 별도 유지되는지 대응을 먼저 닫아야 한다. 값 자체는 제안하지 않는다.

- **어디: §13-1(가) 「발화 검문·예산 상수 → 함수 하나」.** 무엇이 틀렸나: 검문과 예산을 같은 책임으로 합쳤다. 무엇을 봤나: [공유 계약 854행](C:/Users/q1212/Documents/SYNK-appsscript/docs/엔진7종_상향설계_v2.md:854)은 문안 검증을, 바로 다음 855행은 접점 상한·겹침을 별도 계약으로 규정한다. **각 책임 안의 중복을 걷는 것**으로 고쳐야 한다. 한 함수로 합친다는 근거는 없다.

- **어디: §13-4 「가이드 정본 = 기기 파일」 철거 대상.** 무엇이 틀렸나: 실제 정본 역할을 하는 저장 어댑터가 빠졌다. 무엇을 봤나: [SYNK-talk/src/저장.js:178](C:/Users/q1212/Documents/SYNK-talk/src/저장.js:178)의 `가이드읽기()`·`가이드쓰기()`가 `guide_choice.json`을 읽고 쓰며, 쓰기 실패도 삼킨다. [App.js:207](C:/Users/q1212/Documents/SYNK-talk/App.js:207)은 여전히 그 값을 읽는다. 주석만 교체하면 서버가 정본이라는 설명과 실제 동작이 갈린다. **로컬 정본 읽기·쓰기의 교체와 캐시 유지 범위**를 철거 대상으로 함께 적어야 한다.

- **어디: §13-10의 본문화 조건.** 무엇이 틀렸나: `D2 채택`과 `도달 실측`으로 줄이면서 선택지와 측정 대상을 잃었다. 무엇을 봤나: [D18 6805행](C:/Users/q1212/Documents/SYNK-appsscript/docs/엔진7종_상향설계_v2.md:6805)의 정확한 조건은 **`D2 B 채택 ∧ 관통왕복 V7 ✓ ∧ 이해대장 listen.session 도달 실측`**이다. D2 A는 현행 유지이므로 같은 조건으로 처리할 수 없다.

## 안 재봤다

- **대체물의 실행 가능성.** 설계상 경로인 `tools/밤샘.js`, `tools/학생시뮬.js`, `.github/workflows/모델정찰.yml`, `SYNK-talk/lib/오늘의한가지.js`, `SYNK-talk/lib/접점예산.js`, `SYNK-talk/lib/발화검문.js`는 확인한 경로에 없었다. 부재는 확인했지만 대체 기능은 시험할 수 없었다. 구현물과 실제 호출 경로가 필요하다.

- **조건부 철거의 운영 충족 여부.** [docs/관통왕복_명세.md:3](C:/Users/q1212/Documents/SYNK-appsscript/docs/관통왕복_명세.md:3)은 현재 v0.4다. V7의 구현·실행 결과, 서버 정본 전환 후 오프라인 가이드 동기화, 실행 중인 밤샘 프로세스·예약, Blender 설치본, 실제 청구는 안 재봤다. 각각의 실행 결과와 운영 상태가 필요하다.

- **검증 범위.** 실행한 검증은 `엔진점수(root)`를 격리한 합성 입력 2/2건이다. 나머지는 코드·문서 대조이며 저장소 시험 전체를 실행한 것은 아니다. 읽은 파일은 위에 인용한 파일의 해당 구간과 `AGENTS.md`, `docs/_ops/결정.md`, `docs/_ops/검수기각.jsonl`, `계약/수집_교정_계약.json`, `tools/밤샘_0831.js`, `tools/밤샘_0901.js`, `tools/밤샘_0902.js`, `tests/엔진이름.test.js`, `SYNK-talk/src/인증화면.js`, `SYNK-talk/lib/사건출처.js`다. §13 전 항목의 실물·호출자를 전수 확인한 결과는 아니다.