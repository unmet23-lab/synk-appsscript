@@@
- **「보냈어요」 카드** = 숙제 제출 직후 뜨는 카드입니다(talk 교수멘탈화면). ai_assist 1탭·답장 꼬리 한 줄이 여기 실립니다.
===
- **「보냈어요」 카드** = 숙제 제출 직후 뜨는 카드입니다(talk 교수멘탈화면). 답장 꼬리 한 줄이 여기 실립니다. ai_assist 1탭(「AI 랑 같이 했어?」)은 이 카드가 아니라 보내기 «직전»에 있습니다 — 그래야 제출 사건 안에 같이 저장됩니다(심문④ 3-1).
@@@
개원일 학생 전원이 Lv1~2(여섯 단계 중 초급 둘)이고, Lv1~2 는 AI 생성 대상 밖입니다.
===
개원일 학생 전원이 Lv1~2(여섯 단계 중 초급 둘)이고, Lv1~2 는 AI «숙제 생성» 대상 밖입니다(🔴 숙제 생성만입니다 — 마스코트 말(재료 봉투 · 템플릿 기본) · 옷 가게 아주머니(템플릿 · LLM 0) · 흥정 확정(서버 채점) · 일기 맞장구(LLM · 학생·일 1회)는 이 문장과 무관한 통로라 첫 시즌에도 돕니다 · 심문④ 제미나이 3-9 는 이 문장을 «전부 침묵»으로 읽었다).
@@@
시험 학습자(리허설·시뮬)는 사건이 아니라 학습자 표식 `learners.is_test` 로 가릅니다.
===
시험 학습자(리허설·시뮬)는 사건이 아니라 학습자 표식 `learners.is_test` 로 가릅니다. 🔴 그 표식은 «바뀔 수 있는 값»이라(심문④ 제미나이 3-10) 규칙 셋을 D1 물리에 붙입니다 = is_test 는 생성 때 고정 · 바꾸는 갱신은 DB 트리거가 거절 · 시험 계정은 learner_account.linked(실계정 연결) 대상에서 제외. payload 에 같은 표식을 복제하지는 않습니다(한 값 두 곳).
@@@
재현은 summary_hash 로 캐시 표에서 되찾고, 시효가 지나 없으면 «재현 불가 · 근거 만료»로 정직하게 적습니다(0 이나 빈 글로 접지 않는다). 전환 전까지는 «지금 통로가 산문을 저장한다»를 §4 D1 별항에 적어 두고, 소비자 커밋에서 닫습니다.
===
재현은 summary_hash 로 되찾습니다. 🔴 «캐시에서만 되찾는다»로는 재심 계약이 깨집니다(심문④ 아스트라 3-2 + 제미나이 3-2 · 둘 다 P0): 두 시즌 시효가 지나거나 주간 덮어쓰기(A 로 개입 → 다음 주 B 로 덮음)가 되면 «같은 입력에 새 모델» 재심도 30일 회고 대조도 못 하고, summary_hash 는 복원물이 아닙니다. 그래서 권고 하나 더(D7 ⓑ) = **개입 사건이 가리킨 산문 판만 원장 밖 «콜드 저장»(비공개 버킷 · 키 = summary_hash · learning_events 엔 본문 0 그대로)에 보존**합니다. 시효는 재심 창이 아니라 «학생이 빼기(atlas.excluded)를 누른 근거는 즉시 삭제 · 나머지는 졸업 뒤 보존 기간(D_consent)까지». 그 전까지의 «재현 불가 · 근거 만료»는 정직하게 적습니다(0 이나 빈 글로 접지 않는다). 전환 전까지는 «지금 통로가 산문을 저장한다»를 §4 D1 별항에 적어 두고, 소비자 커밋에서 닫습니다.
@@@
→ correct key_takeaway · Vellum 카드 · Reed word_between_songs · Loom 시즌 책 |
===
→ correct key_takeaway · Vellum 카드 · Reed word_between_songs · Loom 시즌 책 · 🔴 Reed 가 들려준 표현은 다음 숙제 문항 하나가 «쓰게» 한다(오늘의 한 가지 1문항과 같은 통로 · 그 문항이 재출현률의 «기회» 분모 · 심문④ 아스트라 3-8 — 안 그러면 들려주기만 하고 써 볼 차례가 없다) |
@@@
- ai_assist = 「보냈어요」 카드 안 1탭(declared: none|between|unsure · 예산 밖). 실물 칸 = `submission.created payload.ai_assist{declared,note}`.
===
- ai_assist = 보내기 «직전» 1탭(보내기 단추 곁 · declared: none|between|unsure · 안 고르면 «미표식»(null) · 예산 밖). 실물 칸 = `submission.created payload.ai_assist{declared,note}` — 제출 사건과 «같이» 저장됩니다. 🔴 v2 의 「보냈어요 카드 안 1탭」은 제출 «뒤»라 덧붙이기만 하는 원장의 옛 사건을 못 고칩니다(심문④ 아스트라 3-1 · §0-1 은 처음부터 「보내기 전」이었다). 뒤늦은 표식 사건은 만들지 않습니다(원장 갱신 0 · 후속 응답 규격 0).
@@@
- rot-check 48h 칸은 jsonl 커밋 시각을 읽습니다.
===
- rot-check 의 신선도 칸은 «출처별 마지막 OK 정찰의 때»를 읽습니다(🔴 v2 의 「48h · jsonl 커밋 시각」은 주간 실행과 안 맞고 실패 장부 커밋도 최신으로 읽는다 · 심문④ 아스트라 3-5 · §14-5 와 같게) · 주간 실행이면 상한 = 8일 · 감지 건수와 읽기 성공을 갈라 셉니다.
@@@
- ② 첫 시즌 뒤 = Prism 귀 라벨 정답지 · S16 · 조준 낱말 · Trail 20문장(3기부터).
===
- ② 첫 시즌 뒤 = Prism 귀 라벨 정답지 · S16 · 조준 낱말 · Trail 20문장(3기부터).
- 🔴 «설계된 침묵»(검수 전 문구는 안 나간다 · 유호 확정)은 그대로이되, 침묵을 «감시»합니다(심문④ 제미나이 3-11 · 폴백 노출은 기각): 검수 큐에 «지금 침묵 중인 자리 수 · 가장 오래 기다린 일수»를 세고 상한(값은 유호 자리)을 넘으면 원장 아침 브리핑 한 줄로 알립니다(§14-5 몽골어 행).
@@@
  → `submission.created payload.ai_assist{declared,note}` explicit · 「보냈어요」 카드 안 1탭 · 소비자 다섯 ⓑ 같은 커밋.
===
  → `submission.created payload.ai_assist{declared,note}` explicit · 보내기 «직전» 1탭(제출 사건과 같이 저장 · 심문④ 3-1) · 소비자 다섯 ⓑ 같은 커밋.
@@@
V2·V5 는 픽스처 atlas_summary 캐시 1건을 insert(LLM 0)한 뒤 deliver·correct 가 그 글을 무절단으로 실어 나르나를 봅니다. 산문 «품질»은 요약채점기가 따로 잽니다(별 자).
===
V2·V5 는 픽스처 atlas_summary 캐시 1건을 insert(LLM 0)한 뒤 deliver·correct 의 «생성 입력»이 그 캐시의 지문(summary_ver·summary_hash) + 슬롯 값으로 조립되어 픽스처와 일치하나를 봅니다(🔴 v2 의 「산문 전문 무절단 운반」 합격 조건은 §3-3 ② 의 «지문+슬롯» 규칙과 충돌했다 · 심문④ 아스트라 3-3 · 둘을 다 지킬 수 없었다). 산문 «품질»은 요약채점기가 따로 잽니다(별 자) — 거기에 «산문 vs 슬롯 표현» 예측력 짝 한 열을 더해, 산문으로 잰 예측력이 소비자가 실제로 받는 슬롯 표현에서도 유지되는지 봅니다(§14-2).
@@@
부채 계기판의 칸 넷(졸업 n 중 4급↑ 확인/미확인/미달)이 이 조항의 실물이고
===
부채 계기판의 칸 넷(약속한 판정일이 도래한 n 중 4급↑ 확인/미확인/≤3 확인/케어 개시 · 🔴 v2 의 「졸업 n」 분모는 §7 U5 와 갈렸다 · 심문④ 아스트라 3-6)이 이 조항의 실물이고
@@@
저장 = «지울 수 있는 캐시». 학생당 두 판 · 덮어쓰기 · excluded 뒤 즉시 삭제·재생성 · 학생 RLS 밖.
===
저장 = «지울 수 있는 캐시». 학생당 두 판 · 덮어쓰기 · excluded 뒤 즉시 삭제·재생성 · 학생 RLS 밖. 개입 사건이 가리킨 판은 덮어써도 남습니다 — 원장 밖 콜드 저장(§3-3 ② · D7 ⓑ 권고 · 심문④ 3-2).
@@@
재현은 지문으로 캐시에서 되찾고, 시효가 지났으면 «재현 불가 · 근거 만료».
===
재현은 지문으로 콜드 저장에서 되찾고(캐시는 «지금 판»만), 없으면 «재현 불가 · 근거 만료».
@@@
권고 = 생성 입력에 산문 전문 대신 **판 지문(summary_ver·summary_hash) + 슬롯 값**만 넣는다(재현은 지문으로 · 시효 지나면 «재현 불가»로 정직하게). D1 의 c17 과 같은 커밋에서 정하실 자리입니다.
===
권고 = 생성 입력에 산문 전문 대신 **판 지문(summary_ver·summary_hash) + 슬롯 값**만 넣는다(재현은 지문으로 · 없으면 «재현 불가»로 정직하게). 🔴 심문④(09-05 저녁 · 아스트라·제미나이 둘 다 P0)가 그 위에 하나를 더 올렸습니다: 시효·덮어쓰기 뒤엔 «같은 입력에 새 모델» 재심이 깨지므로, **개입이 가리킨 산문 판만 원장 밖 콜드 저장(비공개 버킷 · 키 = summary_hash)에 보존**합니다. learning_events 는 여전히 본문 0 · 빼기 근거는 즉시 삭제 · 나머지는 졸업 뒤 보존 기간까지. 08-05 「추정을 사실처럼 저장 않음」과의 관계 = 저장 자리가 «사실 원장»이 아니라 «재현용 창고»라는 것 — 그래도 예외 범위가 넓어지니 유호님 자리입니다. D1 의 c17 과 같은 커밋에서 정하실 자리입니다.
@@@
- 선택지: A) ⓐ+ⓑ. 저장 0 원칙의 예외 = 시효 있는 파생 캐시 하나.
===
- 선택지: A) ⓐ+ⓑ. 저장 0 원칙의 예외 = 시효 있는 파생 캐시 하나 + 개입이 가리킨 판의 콜드 보존(심문④ 권고 · 예외 둘째 · 원장 밖).
@@@
상시 = deliver + companion + 인코더 + Prism 원인 호출(D13 에서 옮김 · ≈$16/월 추정) + 09-05 로 더해진 상시 둘(강사 손 카드 AI 가이드 = 반·일 1회 · 일기 맞장구 = 학생·일 1회 · 값은 안 재봤다). 검산 ≈$0.8/명/월 추정 · 정원 상한 16 이면 ≈$13/월 · 192명이면 ≈$150/월. 단가는 첫 회전 계측기가 냅니다.
===
상시 = deliver + companion + 인코더 + Prism 원인 호출(D13 에서 옮김) + 09-05 로 더해진 상시 둘(강사 손 카드 AI 가이드 = 반·일 1회 · 일기 맞장구 = 학생·일 1회 · 값은 안 재봤다). 🔴 검산을 분모와 함께 다시 적습니다(심문④ 아스트라 3-7 · v2 의 «≈$0.8/명/월 · 16명 ≈$13»은 포함 항목보다 작았고 「Prism ≈$16/월」은 월 4,000제출 기준이라 16명 규모가 아니었다): 16명 기준 인코더 ≈$10~16(§4 U4) + Prism 원인 호출 ≈$1~2(≈320제출 × $0.004) + 손 카드 가이드 ≈$1(반 2~3 · 일 1회) + 일기 맞장구 ≈$2(16명 · 일 1회 · Sonnet 급) + deliver·companion(LLM 선택 통로를 안 켜면 0) ⇒ **≈$14~21/월 ≈ $0.9~1.3/명/월**(토큰 산수 · 실청구 전). 192명이면 ≈$170~250/월. 단가는 첫 회전 계측기가 냅니다 — 아래 «예 $1/명/월»은 낮을 수 있으니 값은 계측 뒤.
@@@
| `ai_assist`(explicit · 제출 절차) | declared ∈ {none · between · unsure} | Vellum 「보냈어요」 카드 1탭 → 저작신뢰 등급 · v16(능력 진술 승격 금지) · Temper 결과변수 분모 |
===
| `ai_assist`(explicit · 제출 절차) | declared ∈ {none · between · unsure} · 안 고름 = null(미표식) | Vellum 보내기 «직전» 1탭(제출 사건 payload 에 같이 · 심문④ 3-1) → 저작신뢰 등급 · v16(능력 진술 승격 금지) · Temper 결과변수 분모 |
@@@
· companion 만 moment_id · cited_refs(companion) · 반 키·근거 셀 키(Prism 카드) | 생산자 = §3-4 개입 규격 행(kind 별)
===
· companion 만 moment_id · cited_refs(companion) · 반 키·근거 셀 키(Prism 카드) · 🔴 policy_trace{candidates, probs, branch}(후보 집합 · 후보별 배정확률 · 거친 정책 분기 · §4 U5 가 요구하는데 v2 표엔 자리가 없었다 · 심문④ 아스트라 3-4 · 확률 정책 활성 «전»엔 null · 이름은 D1 확정 때) | 생산자 = §3-4 개입 규격 행(kind 별)
