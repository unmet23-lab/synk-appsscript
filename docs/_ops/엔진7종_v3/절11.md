## 새로 더할 것

1. **외부 대기의 시작을 구현 순서에서 분리한다.**  
   **무엇:** NIKL 신청과 D16 검수 발주는 각각의 선행 조건이 충족되면 대기를 시작하고, 접수일·회수 예정일·회수 뒤 연결할 걸음을 적는다. 걸음 9 전체의 착수까지 신청을 기다리는 순서로 읽히지 않게 한다.  
   **왜:** NIKL 신청이 걸음 9의 첫 항목인데 승인 대기가 가장 길다고 적혀 있다. 구현 완료 순서와 외부 대기 시작 순서는 다르다. 근거: [docs/엔진7종_상향설계_v2.md:6226](C:/Users/q1212/Documents/SYNK-appsscript/docs/엔진7종_상향설계_v2.md:6226), 같은 파일 `:6769`.  
   **붙일 곳:** 표 앞 「표 읽는 법」과 걸음 0·9.

2. **c17의 완료 판정을 어휘별 생산·소비 증거로 닫는다.**  
   **무엇:** D1 목록을 복제하지 않고, 각 항목에 계약 허용 → 생산자 → 저장 → 소비자 질의 → 계산·반환 → 시험 증거를 연결한다. 해당 걸음에서 아직 사용하지 않는 칸은 적용 조건을 명시한다. 성공한 항목 수의 분모는 그 마감에 필요한 항목 전체다.  
   **왜:** `ⓐ 뒤 ⓑ 0이면 무효`만으로는 빠진 쌍을 식별하지 못한다. `lib/학습자상태.js`를 고쳐도 호출부 SQL이 재료를 공급하지 않으면 소비가 성립하지 않는다. 실제 공급 지점은 [deliver/index.ts:419](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/deliver/index.ts:419), 호출 지점은 같은 파일 `:897`이다.  
   **붙일 곳:** 걸음 1의 출구 조건, 걸음 6의 합류 조건.

3. **계약 배포 안의 선후관계를 명시한다.**  
   **무엇:** 사람의 배포 절차에 DB 수용 준비 → 서버 수용 확인 → DB 착지 기록 확인 → 앱 `X-Contract-Ver` 상향·생산자 활성 순서를 넣는다. 구판 앱의 기존 봉투와 신판 앱의 신규 봉투를 각각 검증한다. `supabase/migrations/` 작업은 단일 레인으로 계산한다.  
   **왜:** 앱 선언판이 DB보다 앞서면 `CONTRACT_VER_UNSUPPORTED`, `retryable:false`가 반환된다. 앱 통로도 이를 `끝:true`로 접는다. 계약 파일을 올렸다는 사실만으로 앱 판을 올리면 첫 사건의 재시도 경로가 닫힌다. 근거: [src/계약판.js:16](C:/Users/q1212/Documents/SYNK-talk/src/계약판.js:16), [events/index.ts:213](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/events/index.ts:213), [src/사건통로.js:155](C:/Users/q1212/Documents/SYNK-talk/src/사건통로.js:155), [docs/이_저장소_규약.md:37](C:/Users/q1212/Documents/SYNK-talk/docs/이_저장소_규약.md:37).  
   **붙일 곳:** 걸음 1 「배포(리허설→운영)」 바로 아래.

4. **시즌 식별자 확인에 실제 시즌 실행 조건을 붙인다.**  
   **무엇:** `app_state`의 `key='시즌시작일'`과 `engine.season`의 `season_id`·`code`·`starts_on`·`ends_on` 사이 대응 및 파일럿·체험 기간 처리 방식을 확정한다. 정규 시즌 시작일을 바꿨다가 복원하는 방식으로 리허설을 통과시키지 않는다.  
   **왜:** `seasonLabelOf_()`는 시작일을 문자열 키로 사용하지만 `engine.season.season_id`는 UUID다. 또한 `lessonNoOf_()`는 시작일 이전 날짜에 `0`을 반환하고, 인쇄 경로는 `session_no`가 없으면 중단한다. 식별자 규격 유무만 확인해서는 걸음 5의 첫 서클이 열리지 않는다. 근거: [엔진_셋업확장.js:2235](C:/Users/q1212/Documents/SYNK-appsscript/엔진_셋업확장.js:2235), 같은 파일 `:2242`, `:3580`; [20260812140000_season_c11.sql:89](C:/Users/q1212/Documents/SYNK-talk/supabase/migrations/20260812140000_season_c11.sql:89).  
   **붙일 곳:** 걸음 0의 ⑭ 다음, 걸음 5·7의 입구 조건. 09-04 확정의 `textbook` 필수값 해제도 함께 대조한다(결정 원장의 09-04 항목(줄 번호·낱말은 안 재봤다)).

5. **「같이 한 번」을 같은 검증 묶음 안의 순차 실행으로 정의한다.**  
   **무엇:** 관통왕복시험 → `engine.gen_active_from()` 잔존 없음 확인 → 배달왕복시험 순서를 명시한다. 각 결과에는 대상 환경과 실제 실행한 코드·DB 판을 붙이고, 리허설 출구 결과와 운영 입구 결과를 구분한다.  
   **왜:** 관통왕복시험은 생성 활성 함수를 임시로 설치한다. 그동안 배달왕복시험을 실행하면 해당 시험의 `deliver`가 실패한다. 또한 V 검증은 리허설 전용이고 E4는 운영 도달을 요구하므로 결과가 서로를 대신하지 못한다. 근거: [tools/관통왕복시험.js:24](C:/Users/q1212/Documents/SYNK-talk/tools/관통왕복시험.js:24), 같은 파일 `:294`; [docs/관통왕복_명세.md:29](C:/Users/q1212/Documents/SYNK-appsscript/docs/관통왕복_명세.md:29), 같은 파일 `:89`.  
   **붙일 곳:** 걸음 6의 재실측과 걸음 7의 출구.

6. **합성 리허설과 실학생 효과 판정의 분모·시계를 분리한다.**  
   **무엇:** 리허설 통과는 배선·재현·실패 처리의 증거로만 사용한다. 실학생 효과를 계산할 때는 `engine.learners.is_test`로 합성 자료를 제외하고, `기준시각`에 따라 `미도래`·`표본0`·`측정`을 구분한다. 효과 판정 시점은 `잴수있는때`와 실제 표본 충족 여부로 정한다.  
   **왜:** 현재 `tools/성과계기판.js`의 질의에는 `is_test` 제외가 없다. `성과회수()`는 아직 끝나지 않은 창을 `미도래`로 반환한다. 12-21 리허설 판정이 실학생의 30일 효과까지 확인했다는 뜻이 될 수 없다. 근거: [tools/성과계기판.js:216](C:/Users/q1212/Documents/SYNK-talk/tools/성과계기판.js:216), [lib/성과회수.js:233](C:/Users/q1212/Documents/SYNK-talk/lib/성과회수.js:233).  
   **붙일 곳:** 걸음 3의 사전분석계획 조건, 걸음 7의 판정 범위.

7. **교실 쌍에 ‘없음’과 응답 유실 시험을 추가한다.**  
   **무엇:** 관찰만 확정한 경우, 개입까지 확정한 경우, 학생 목표까지 확인한 경우를 각각 시험한다. 선택·확인하지 않은 `intervention.delivered`·`goal.stated`는 생성하지 않는다. 저장 중 실패에는 부분 기록이 남지 않고, 저장 뒤 응답이 유실된 재전송에는 `idempotency_key` 기준으로 중복이 생기지 않아야 한다.  
   **왜:** 「셋을 같은 트랜잭션」은 원자성 조건이지 항상 사건 셋을 만들어야 한다는 뜻이 아니다. Temper U3도 개입의 기본값을 「없음」으로 정한다. 현재 관찰 생산자는 멱등 충돌을 처리하지만 확장될 사건 묶음의 동작은 별도로 검증해야 한다. 근거: [docs/엔진7종_상향설계_v2.md:4832](C:/Users/q1212/Documents/SYNK-appsscript/docs/엔진7종_상향설계_v2.md:4832), [teach/index.ts:1671](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/teach/index.ts:1671).  
   **붙일 곳:** 걸음 5의 시험 확장.

## 지금 판이 틀린 것

1. **D23을 미정으로 취급하는 전제가 낡았다.**  
   **어디:** `docs/엔진7종_상향설계_v2.md:6021`, `:6041`.  
   **무엇이 틀렸나:** D23 확정을 기다려야 날짜가 참이 된다는 문장을 유지할 수 없다. A안의 조건문을 확정문으로 바꾸고, 리허설 마감과 실학생 데이터 마감을 각각 적용해야 한다. 공간 확보도 12월 리허설의 선행 조건으로 재도입하면 안 된다.  
   **판단 근거:** [docs/_ops/결정.md:77](C:/Users/q1212/Documents/SYNK-appsscript/docs/_ops/결정.md:77)은 12-07~12-18·판정일 12-21·합성 학생·강사 1을 확정했다. 같은 파일 `:71`은 이 확정으로 공간이 파일럿 앞에 설 필요가 없어졌다고 명시한다.

2. **걸음 9와 11의 일부 마감이 첫 실학생보다 늦다.**  
   **어디:** `docs/엔진7종_상향설계_v2.md:6233`, `:6265`.  
   **무엇이 틀렸나:** 걸음 9의 사전등록을 02-25까지, 걸음 11의 Amber 즉시 반환을 03 첫 주까지 허용하면 02-11 첫 제출·첫 녹음이 먼저 발생한다. 사전등록과 해당 최초 반응의 생산·반환 경로만 먼저 떼어 02-11 이전에 닫아야 한다.  
   **판단 근거:** 같은 설계 `:4053`은 `등록일 < 첫 실학생 제출 사건 시각`, `:6253`은 첫 제출·첫 녹음의 그 자리 반환을 요구한다. 첫 실학생 데이터 마감은 [docs/_ops/결정.md:77](C:/Users/q1212/Documents/SYNK-appsscript/docs/_ops/결정.md:77)이다. **재현 상황:** 학생이 02-11 제출하고 해당 기능은 표의 마감에 맞춰 나중에 완성되는 경우.

3. **첫 개입 스탬프의 생산을 걸음 16에 남겨 두었다.**  
   **어디:** `docs/엔진7종_상향설계_v2.md:6326`과 D1 `:6450`, `:6465`.  
   **무엇이 틀렸나:** D1은 `arm_id`를 첫 주 어휘로, `seed`를 첫 개입부터 찍을 값으로 정했는데 실제 스탬프 생산은 첫 판올림 직전인 걸음 16에 배치했다. 어휘 예약으로는 이전 개입의 값을 복원하지 못한다. 첫 개입에 필요한 스탬프 생산은 걸음 3·5의 해당 생산자에 붙이고, 그림자 대조는 걸음 16에 남겨야 한다.  
   **판단 근거:** 현재 [deliver/index.ts:935](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/deliver/index.ts:935)의 직접 배달 경로는 `payload`에 `ver`·`output_text`를 기록한다. 이후 변경할 미래 코드가 과거 행의 실제 선택을 증명해 주지는 않는다. **재현 상황:** 걸음 3 이후 개입을 시작하고 걸음 16 전에 정책을 그대로 운영하는 경우.

4. **둘째 창의 범위가 뒤 걸음의 선행 마감을 빠뜨린다.**  
   **어디:** `docs/엔진7종_상향설계_v2.md:6035`, `:6037`, `:6304`, `:6344`.  
   **무엇이 틀렸나:** 걸음 8~11의 합을 둘째 창의 작업량으로 삼으면서, 걸음 17이 직접 요구하는 「통로·문턱은 개원 전」을 제외했다. 걸음 14도 D15 A의 「코드 개원 전」과 달리 개원 뒤로 분류했다. 걸음 단위가 아니라 앞당겨야 하는 하위 산출물까지 포함해 다시 합산해야 한다.  
   **판단 근거:** 같은 설계 `:6344`, `:6754`, `:6758`. 또한 [docs/개원전_일괄판정_큐.md:27](C:/Users/q1212/Documents/SYNK-appsscript/docs/개원전_일괄판정_큐.md:27)의 Q8 트리거는 첫 실학생 데이터의 `promotion_intent`다. 「개원 셋째 주쯤」을 보장하는 날짜 조건은 아니다. D15 선택에 따른 분기를 명시해야 한다.

## 안 재봤다

- **실제 처리량과 수정 후 소요량.** ≈33세션은 걸음 0~7의 합, ≈29세션은 걸음 8~11의 합으로 산술을 확인했다. ≈63덩이의 걸음별 배분과 세션 환산 근거는 확인하지 못했다. 걸음 0의 조사 목록만으로 구현→시험→검수→수리→배포 확인까지의 처리량을 추정할 수 없다. 대표 작업의 완료까지 걸린 시간과 외부 대기 시간을 따로 재야 한다. 09-05부터 12-01까지는 달력상 87일이며, 가용 작업일은 미측정이다.

- **현재 라이브 상태.** DB·시트·트리거·학생 빌드·실기기·RLS 조회는 실행하지 않았다. `supabase/DB착지판.json`은 저장소의 확인 기록으로만 읽었다. 걸음 7을 닫으려면 각 환경의 실제 배포판과 같은 판의 실행 증거가 필요하다. 로컬 시험도 이번 검토에서는 실행하지 않았다.

- **외부 조건과 비용.** NIKL 처리 기간·이용 조건, Dual stream의 현재 제공·중단 조건, 모델 별칭, 토큰 처리 한도, 실제 검수비·회수 기간은 확인하지 않았다. 공식 원문과 계정별 조회·견적이 필요하다. 돈·반출·TOPIK 보장 관련 문구는 봤으며, 기존 판정 대기와 출처 미확인을 유지하는 것은 괜찮다. 확정값으로 올릴 근거는 확보하지 않았다.

- **계획된 실물의 구현 여부.** 지정 경로 확인에서는 `docs/Temper_사전분석계획_v1.md`, `tools/계약판올림.js`, `tools/시험지굽기.js`, `tools/설명대조.js`, `../SYNK-talk/lib/이름살균.js`, `../SYNK-talk/lib/관찰선택자.js`가 없었다. 다른 작업본의 존재와 구현 내용은 확인하지 않았다. 미구현 자체를 이번 설계의 오류로 세지는 않았다.

- **읽은 파일·안 본 것.** 본문에서 파일·줄로 인용한 파일의 관련 구간을 읽었다. 추가로 양 저장소 `AGENTS.md`, `docs/_ops/검수기각.jsonl`, `docs/파일럿_체험2주_설계_v1.md`, `docs/시즌회고_설계.md`, `계약/수집_교정_계약.json`, talk의 `docs/공용지침.md`, `lib/학습자상태.js`, `lib/이벤트검증.js`, `lib/계약판.js`, `tools/배달왕복시험.js`, `tests/계약판.test.js`, `tests/시험학습자표식.test.js`, 관련 migration을 대조했다. 저장소 전체 구현과 §11 밖 모든 설계의 전수 검수는 하지 않았다.