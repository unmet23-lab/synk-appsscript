# 엔진7종 v3 · ④ 소급 불가 절 심문 — 결과 판정 (2026-09-05 저녁)

> 무엇 = 채택 순서 ④(결정 원장 09-05 「되돌릴 수 없는 것에 닿는 절만 심문」). 대상 여섯 = v3 §3(한 시스템 · 계약·급소) · §11(순서 · 닫히는 문) · §12(유호님 결정) · §16(대가) + 09-05 앱 정본 둘(`docs/앱재설계_v2.md` · `docs/살아있는자리_설계_v1.md`). 발췌 = `심문_절N.md`(정본 v3 커밋 856089ca8 에서 뽑음).
> 심문자 둘 = 아스트라(gpt-6-astra · xhigh) + 제미나이 3.1 Pro(high). 같은 문서를 따로 봤다. 결과 원문 = `docs/_ops/심문결과/심문_절N-<지문>-{astra-xhigh-1,gemini-high-2}.md`.
> 읽는 법 = 지적마다 **참 / 부분 / 틀림** 과 **채택(무엇을 고치나) / 기각(왜)** 을 적는다. P0 을 기각하면 유호님께 한 줄로 알린다(조용한 봉인 차단). 고치는 곳은 정본 v3(델타 `델타_심문④.txt`)이고, «결정이 필요한 것»은 §12 D 자리로 올린다.
> 🔴 «채택»은 v3 안의 «고침»이지 유호님의 채택(⑤)이 아니다. ⑤ 는 이 판정 뒤 유호님 자리다.

## §3 한 시스템 — 아스트라 8(P0 2 · P1 6) + 제미나이 5(P0 2 · P1 2 · P2 1)

| # | 누가 | 등급 | 지적 | 판정 | 처분 |
|---|---|---|---|---|---|
| 3-1 | 아스트라 | P0 | ai_assist(「AI 랑 같이 했어?」) 1탭을 «제출 뒤» 「보냈어요」 카드에서 받아 이미 저장된 submission.created 의 payload 에 넣는다 — 덧붙이기만 하는 원장에서는 못 고친다 · 응답 유실·재시도 규격도 없다 | **참**(v3 안 자기모순 · §0-1 은 「보내기 «전» 1탭」이고 §3-4 ⑥·읽는 법 ⑥ 은 「보냈어요 카드 안」) | **채택 = 제출 «직전» 1탭**(보내기 단추 곁 · 선택 안 하면 unsure 가 아니라 «미표식») → submission.created payload 에 실려 «같이» 저장된다. 「보냈어요」 카드에는 답장 꼬리만. 후속 응답 사건은 안 만든다(원장 갱신 0). §3-4 (가)·읽는 법 ⑥ · §6 카드 규격 고침 |
| 3-2 | 아스트라 + 제미나이 | P0 | 산문(atlas_summary)을 원장에 안 넣고 캐시(두 시즌 시효)로만 두면, 시효 뒤 «같은 입력에 새 모델» 재심도 30일 회고 대조도 못 한다 — summary_hash 는 복원물이 아니다 · 주간 덮어쓰기가 시효 «전»에도 근거를 지운다(A 로 개입 → B 로 덮음 → A 재심 불가) | **참**(둘 다 같은 자리 · v3 §3-3 ② · §4 U4 가 「시효 지나면 재현 불가 · 근거 만료」라 적어 둔 그 구멍) | **채택(권고) = 개입에 «쓰인» 산문 판만 원장 밖 콜드 저장에 해시 키로 보존** — learning_events 엔 여전히 본문 0 · 저장 자리 = 비공개 버킷(제미나이 안 · Prism 이 쓰는 learner-media 와 같은 결) · 키 = summary_hash · 덮어써도 개입이 가리킨 판은 남는다 · 시효 = 재심 창(두 시즌)이 아니라 «학생이 빼기(atlas.excluded)를 누른 근거는 즉시 삭제 · 나머지는 졸업 뒤 보존 기간(D_consent)까지» · 🔴 결정 자리 = **D7**(산문 Atlas 형태 · 권고를 ⓑ 에서 «ⓑ + 콜드 보존»으로 올린다 · 유호님께 한 줄) |
| 3-3 | 아스트라 | P1 | 생성 입력 = «지문 + 슬롯»(§3-3 ②)인데 관통왕복 V2·V5 는 «산문 전문 무절단 운반»이 합격이다 — 둘을 다 지킬 수 없다 · 산문 예측력이 슬롯 표현에도 유지되는지 비교도 없다 | **참**(v3 §3-5-1) | **채택 = V2·V5 정의를 «지문+슬롯으로 조립된 생성 입력이 픽스처와 일치하나»로** · 요약채점기 Brier 짝에 «산문 vs 슬롯 표현» 열 하나 더(§4 U4 · §14-2) |
| 3-4 | 아스트라 | P1 | Core U5 가 사건에 «후보 집합 · 후보별 배정확률 · 거친 정책 분기»를 남기라 하는데 D1 허용 키 표엔 그 자리가 없다 | **참**(§12 D1 표 intervention.delivered v2 행) | **채택 = D1 표에 `policy_trace{candidates, probs, branch}` 한 칸(이름은 D1 확정 때 · 지금은 «짓는다»)** · 확률 정책 활성 «전»엔 null |
| 3-5 | 아스트라 | P1 | §3-4 ⑫ 「rot-check 48h 칸은 jsonl 커밋 시각」 — 주간 실행인데 48h 로 재고, 실패 장부 커밋도 «최신»으로 읽힌다 · §14-5 의 「출처별 마지막 OK 때」와 갈린다 | **참**(§14 고침이 §3 에 안 번졌다) | **채택 = §3-4 ⑫ 를 §14-5 와 같게**(출처별 마지막 OK 정찰의 «때» · 주간 실행이면 상한 = 8일 · 감지 건수와 읽기 성공 분리) |
| 3-6 | 아스트라 | P1 | §3-6 Ⅰ-6 행이 보장 계기판 분모를 «졸업 n»으로 적는다 — 판정일이 지났는데 아직 졸업 못 한 학생이 빠진다 · §7 U5 는 졸업을 조건으로 삼지 말라 한다 | **참**(§7 고침이 §3-6 에 안 번졌다) | **채택 = §3-6 행을 «약속한 판정일이 도래한 n 중 4급↑ 확인/미확인/≤3 확인/케어 개시»로** |
| 3-7 | 아스트라 | P1 | D28 검산 «≈$0.8/명/월 · 16명 ≈$13/월»이 포함 항목(Prism ≈$16 · 인코더 ≈$10~16)보다 작다 | **참**(산수 어긋남 · Prism «≈$16»은 월 4,000제출 기준이라 16명 규모가 아니다) | **채택 = D28 검산을 분모와 함께 다시 적는다**: 16명 기준 인코더 ≈$10~16 + Prism 원인 호출 ≈$1~2(≈320제출 × $0.004) + 손 카드 가이드 ≈$1(반 2~3 · 일 1회) + 일기 맞장구 ≈$2(16명 · 일 1회 · Sonnet 급) + deliver·companion(LLM 선택 통로 0이면 0) ⇒ **≈$14~21/월 ≈ $0.9~1.3/명/월**(토큰 산수 · 실청구 전) · 「예 $1/명/월」은 낮을 수 있으니 값은 첫 회전 계측기 뒤 |
| 3-8 | 아스트라 | P1 | 곡 사이 낱말은 «들려준 뒤 실제로 써 보는 차례»가 없다 — 후속 과제가 전부 다른 표현이면 재출현률 기회가 0 | **참**(더할 것 · §10 U5 재출현률의 «기회 분모»와 같은 구멍의 앞면) | **채택 = 들려준 표현을 다음 숙제 문항 하나가 «쓰게» 한다**(오늘의 한 가지 1문항과 같은 통로 · §3-4 오늘의한가지 소비자에 Reed 낱말 추가 · 재출현률의 기회 분모 = 그 문항) |
| 3-9 | 제미나이 | P0 | 개원일 학생 전원 Lv1~2 는 「AI 생성 대상 밖」이라 일기 맞장구·옷 가게 흥정·재료 봉투가 첫 시즌 내내 침묵한다 | **틀림(기각) · 문장은 고친다** — 「AI 생성 대상 밖」은 «숙제(과제) 생성»(D22)만이다. 재료 봉투 = 템플릿 기본(LLM 0 · §6 U8) · 옷 가게 아주머니 = 템플릿 6 + LLM 0(옷입히기 §3-2) · 흥정 확정 = 서버 채점(교정 통로 error_tags · 옷입히기 §3-3) · 일기 맞장구만 LLM 인데 D22 와 무관(학생·일 1회 · 검문 뒤). 제안한 «템플릿 폴백»은 이미 설계다 | **기각(P0 → 유호님께 한 줄)** · 고침 = §3-2·§0-1 의 「초급은 AI 생성 대상 밖」에 «(숙제 생성만 · 마스코트 말·가게·일기는 다른 통로)» 붙임 |
| 3-10 | 제미나이 | P1 | is_test 는 계정 표의 «바뀔 수 있는» 값이라 훗날 풀리거나 실계정으로 전환되면 옛 사건의 시험/실학생 구분이 원장만으로 영영 안 된다 → payload 에 is_simulated 불변 표식을 | **부분**(위험은 참 · 처방은 «한 값 두 곳») | **채택(형태 바꿔) = is_test 는 생성 때 고정 · 바꾸는 갱신은 DB 트리거가 거절 · 시험 계정은 learner_account.linked 대상에서 제외**(§3-3 ⓪ · D1 물리) · payload 복제는 안 한다 |
| 3-11 | 제미나이 | P1 | 몽골어 검수 지연으로 «빈 mn 은 화면에 안 나간다»가 «조용한 실패»가 된다 → 영어·한국어 폴백 노출 또는 데드라인 경보 | **부분**(침묵은 «설계된 침묵»(유호 확정 · 검수 전 문구는 침묵)이라 폴백 노출은 안 한다 · «감시 없는 침묵»은 참) | **채택(경보만) = 검수 큐에 «침묵 중인 자리 수 · 대기 일수 상한» 경보**(원장 아침 브리핑 한 줄 · §3-4 ⑬ · §14-5 몽골어 행) · 폴백 노출은 기각 |
| 3-12 | 제미나이 | P2 | 학생 0명에 모델 정찰·재채점 자동화는 과설계 → 외부 감시를 걷고 모델 ID 를 손으로 적어라 | **틀림(기각)** — 철학 Ⅰ-9①(사건이 부르는 재심 · §1 축 ⑤)이 이 설계의 축이고, 09-05 유호 확정 「재심 결과는 아침 브리핑 한 줄」은 재심 자체를 살린 것이다. 손으로 적는 모델 ID 는 「한 값 두 곳」·「사람 손에 건 장치는 0번 돈다」로 되돌아간다 | **기각** |

**§3 결과 요약.** 참 8 · 부분 2 · 기각 2(P0 하나 = 제미나이 3-9 → 유호님께 한 줄). 유호님 자리로 올라가는 것 하나 = **3-2 산문 콜드 보존(D7 권고 갱신)**. 나머지 고침은 v3 안의 자기모순·산수·번짐 누락이라 델타로 닫는다.

**3.8 vs 3.1 한 번 재기(인계문 「④ 첫 절에서」).** 이 회차는 3.1 Pro 만 돌았다(도구 편성이 3.1 Pro 하나). 같은 §3 을 3.8(공짜 문)로 한 번 더 돌려 견주는 것은 «안 했다» — 공짜 몫(하루 20발)과 크레딧 시한(11-13)을 보고 유호님이 원하시면 그때 잰다.
