## 새로 더할 것

1. **셋이 서로를 숨기는 연쇄 실패**
   - **무엇:** 「감시감 때문에 응답을 피함 → 자료가 비어 감 → 응답한 학생만 남아 동의율이 좋아 보임」을 공통 실패로 추가한다. 기존 동의율의 `n = 맞다 + 아니다`는 유지하고, 별도로 **응답한 대상/실제 노출 대상**, **실제 노출 대상/전체 대상**을 함께 본다. 노출을 못 재면 관계가 괜찮다는 판정도 닫지 못한다.
   - **왜:** [SYNK-talk/lib/동의율.js:19](C:/Users/q1212/Documents/SYNK-talk/lib/동의율.js:19)는 무응답을 분모에서 제외하며, `동의율표()`의 `총학생수`도 응답 행에서만 나온다(:43·:93). 응답자의 동의율 상승과 전체 학생의 관계 악화가 동시에 가능하다.
   - **붙는 자리:** 「셋에 공통인 한 줄」 앞. 세 대가를 독립적으로 통과시켜도 남는 구멍이다.

2. **착수 금지 다음에 ‘고장 중에도 수업이 이어지는가’를 붙인다**
   - **무엇:** 각 대가에 **중단할 소비 → 유지할 기본 과제 경로 → 재개 증거**를 적는다. 걸음 7에서는 해당 기능을 중단한 상태에서도 `intervention.delivered`·`task.assigned`가 남고, 그 배정의 `submission.created`까지 이어지는지 잰다. 화면에 과제가 보이는 것만으로 수업 유지라고 치지 않는다.
   - **왜:** [SYNK-talk/lib/오늘과제.js:474](C:/Users/q1212/Documents/SYNK-talk/lib/오늘과제.js:474)의 `화면과제()` 폴백은 `intervention_id: null`, `제출재료: null`이다. 빈 화면은 막지만 발화를 서버로 보내지 못한다. 또한 D22 ⓒ의 장애 독립성도 따로 필요하다. `aiWeakMap_()`은 약점 로더이고, 실제 재출제는 [엔진_콘텐츠AI.js:3317](C:/Users/q1212/Documents/SYNK-appsscript/엔진_콘텐츠AI.js:3317)의 `aiCall_()`을 거친다. AI 통로 장애에서 함께 멈출 수 있다.
   - **붙는 자리:** 각 「닫을 것 하나」 끝과 걸음 7 출구. 지금 절은 착수·판올림을 막지만, 이미 켜진 기능을 닫은 뒤의 수업은 증명하지 않는다.

3. **판 대조에 ‘그때 알 수 있었던 자료’의 경계를 추가한다**
   - **무엇:** 개입 당시 판단 재현은 `occurred_at <= as_of`와 `ingested_at <= ingested_as_of`를 함께 적용한다. 늦게 들어온 사실까지 포함한 사후 재산출은 당시 판단 재현과 구분한다. `회수판`과 기준시각이 같다는 이유만으로 두 결과를 같은 측정으로 취급하지 않는다.
   - **왜:** [SYNK-talk/lib/학습자상태.js:258](C:/Users/q1212/Documents/SYNK-talk/lib/학습자상태.js:258)에 이 구분이 이미 구현되어 있다. 반면 [SYNK-talk/lib/성과회수.js:230](C:/Users/q1212/Documents/SYNK-talk/lib/성과회수.js:230)의 기준선 계산은 `as_of`와 `창`만 전달한다. 합성 입력으로, 개입 전에 발생했지만 개입 뒤 적재된 사건을 추가하자 동일한 회수 기준시각의 `이전.표본충분도`가 달라졌다. `ingested_as_of`를 함께 적용한 대조에서는 제외됐다. 관련 단언 **2/2 통과**다.
   - **붙는 자리:** 대가 ②의 `summary_ver·prompt_ver·classifier_ver` 대조 항목 뒤.

4. **‘빼기’ 이후 늦게 끝난 작업이 근거를 되살리는 순서를 시험한다**
   - **무엇:** §4 U6의 설계 어휘 `atlas.excluded{axis, evidence_ref}`에 다음 실행 순서를 추가한다. **제외 전 자료로 캐시 생성 시작 → 제외 사건 도착 → 캐시 삭제 → 앞선 생성 작업 완료 → 소비자 읽기.** 마지막 읽기에서도 제외한 근거가 산문·되묻기·과제요약·답장 꼬리에 재등장하지 않아야 한다.
   - **왜:** [docs/엔진7종_상향설계_v2.md:1457](C:/Users/q1212/Documents/SYNK-appsscript/docs/엔진7종_상향설계_v2.md:1457)의 삭제·재생성만으로는 이미 실행 중인 작업의 뒤늦은 쓰기를 설명하지 못한다. 같은 문서 :1461은 야간 Batch를, :1578은 영구 제외를 명시한다. **삭제 성공과 재등장 방지는 다른 검증점**이다.
   - **붙는 자리:** 대가 ③ 마지막 「빼기를 첫 카드부터」 바로 뒤. 실제로 이 경쟁 상태가 발생한다는 판정이 아니라, 구현이 닫아야 할 재현 순서다.

## 지금 판이 틀린 것

1. **D23을 미확정으로 둔 설명**
   - **어디:** `docs/엔진7종_상향설계_v2.md:7902`·`:7905`·`:7907`·`:8013`·`:8050`.
   - **틀린 것:** D23을 아직 제안이라 적고, 그 코호트의 답이 학생 관계 판정을 닫을 것처럼 연결했다. **수치·유호 확정 충돌을 봤다 → 문제다.**
   - **근거:** [docs/_ops/결정.md:77](C:/Users/q1212/Documents/SYNK-appsscript/docs/_ops/결정.md:77)은 2026-09-05에 **12-07~12-18, 판정일 12-21, 합성 학생, 강사 1명**으로 확정했다. 이 리허설의 참가 구성은 실학생 효과·감시감의 분모가 아니다. D23 확정과 실학생 표본 미측정을 분리해야 한다.

2. **첫 시즌 회고의 `도래 0`을 정직값으로 미리 확정한 것**
   - **어디:** `docs/엔진7종_상향설계_v2.md:7917`·`:7919`·`:7920`.
   - **틀린 것:** 첫 개입 시점, 회수 창 도래, 기준선 유효성을 섞었다. 기준선이 잘렸다고 30일 창까지 미도래가 되지는 않는다.
   - **근거:** [SYNK-talk/lib/성과회수.js:234](C:/Users/q1212/Documents/SYNK-talk/lib/성과회수.js:234)는 `기준 < 개입때 + 일수`일 때만 `미도래`를 낸다. 합성 `intervention.delivered.occurred_at='2027-02-11T00:00:00Z'`와 다음 날 `quiz.answered`를 넣으면, `기준시각='2027-03-13T00:00:00Z'`에서 `창별['30일'].측정`은 **1/1**, `미도래`는 **0/1**이다. 이는 효과 입증이 아니라 **도래 0을 고정할 수 없다는 반례**다.

3. **계약 동일성 검사에 V6 판정을 ‘한 줄’로 맡긴 것**
   - **어디:** `docs/엔진7종_상향설계_v2.md:7974`부터의 훅 설명.
   - **틀린 것:** 계약 파일의 동일성과 생산자·소비자 왕복 성공은 다른 판정이다. 현재 발동 조건으로는 소비자만 바꾼 커밋도 검사 밖이다.
   - **근거:** [.git/hooks/pre-commit:128](C:/Users/q1212/Documents/SYNK-appsscript/.git/hooks/pre-commit:128)은 staged `계약/수집_교정_계약.json` 변경에만 발동한다. [tools/계약동기화.js:74](C:/Users/q1212/Documents/SYNK-appsscript/tools/계약동기화.js:74)는 정규화한 파일 내용을 비교하고, 형제가 없으면 :64에서 종료코드 `0`을 반환한다. **양쪽 계약은 같지만 V6 소비자가 없는 입력**도 이 검사의 성공 조건을 만족한다. 새 훅 없이 진행하더라도 발동 범위와 V6 실행 증거의 귀속을 추가 설계해야 한다. 왕복시험은 기존 [SYNK-talk/lib/왕복골격.js:43](C:/Users/q1212/Documents/SYNK-talk/lib/왕복골격.js:43)의 배포판 게이트와 연결해야 한다.

4. **Patina를 구현보다 앞선 걸음에서 닫은 것**
   - **어디:** `docs/엔진7종_상향설계_v2.md:8008`·`:8010`.
   - **틀린 것:** 걸음 4 개발 빌드로 Patina 앱 축의 최종 철회 여부를 판정한다.
   - **근거:** 같은 문서 [걸음 4, :6160](C:/Users/q1212/Documents/SYNK-appsscript/docs/엔진7종_상향설계_v2.md:6160)는 Loom U1·U2를 포함하지만, **Loom U3(Patina)는 걸음 15**다([:6066](C:/Users/q1212/Documents/SYNK-appsscript/docs/엔진7종_상향설계_v2.md:6066)). 걸음 4에서 가능한 것은 후보 표현 검수다. 실제 누적 상태를 가진 Patina의 나란히 비교는 걸음 15 구현 뒤, 학생 노출 전에 다시 닫아야 한다.

5. **넣은 주·뺀 주 비교로 관계 위험을 닫을 수 있다는 전제**
   - **어디:** `docs/엔진7종_상향설계_v2.md:8014`·`:8015`.
   - **틀린 것:** 기능을 끈 주가 이전 학습·노출 경험까지 없는 대조가 되지는 않는다. 또한 `반영 제외 수/학생`은 켜고 끌 기능이 아니라 관측지표다.
   - **근거:** 같은 문서 [Temper U7, :5137](C:/Users/q1212/Documents/SYNK-appsscript/docs/엔진7종_상향설계_v2.md:5137)이 이미 이월효과를 실패 원인으로 적었다. 낱말을 들은 뒤 기능을 꺼도 배운 낱말은 남고, 감시로 느꼈던 경험도 기능 중단만으로 사라졌다고 볼 수 없다. 비교 단위·순서·이전 노출·결과변수를 정하기 전에는 **탐색 관찰**로만 취급해야 한다. `빼기`는 비교 기간 모두에서 유지해야 한다.

6. **`sttSweep_`를 실행 함수로 적은 것**
   - **어디:** `docs/엔진7종_상향설계_v2.md:7940`·`:7941`.
   - **틀린 것:** 눈 실측 대상 함수명이 실물과 다르다.
   - **근거:** [교재연동.js:168](C:/Users/q1212/Documents/SYNK-appsscript/교재연동.js:168)의 실제 호출은 `voiceSweep_(ss)` 다음 `voiceTranscribe_(ss)`다. `voiceTranscribe_` 정의는 [:897](C:/Users/q1212/Documents/SYNK-appsscript/교재연동.js:897)에 있다. 두 저장소의 JS·TS 검색에서 `sttSweep_`는 실행 정의·호출 없이 주석에만 남았다. 실측 이름은 `voiceSweep_·voiceTranscribe_`로 고쳐야 한다.

## 안 재봤다

- **첫 실학생 유입일과 실제 분모.** [docs/_ops/결정.md:133](C:/Users/q1212/Documents/SYNK-appsscript/docs/_ops/결정.md:133)에 개원 전 판매도 확정되어 있다. 실제 데이터 유입이 체험 2주보다 앞서는지는 확인하지 않았다. 실제 최초 입학·개입 시각과 관측 대상 수가 필요하다.
- **중단 후 수업 유지, 제외 이후 캐시 재등장, 학생의 감시감.** 각각 기능 중단 상태의 배정→제출 실왕복, 실행 순서를 제어한 캐시 시험, 실제 사용 언어로 진행한 학생 검수가 필요하다. 유호님 눈 검수와 학생 관계 판정은 별도다.
- **제안 부품의 구현.** `SYNK-talk/docs/Temper_사전분석계획_v1.md`, `SYNK-talk/lib/요약채점기.js`, `SYNK-talk/lib/관찰선택자.js`는 현재 경로가 없는 것을 확인했다. 위 `atlas.excluded{axis, evidence_ref}`도 인용한 설계 어휘이며, 구현 완료로 취급하지 않았다.
- **원격·실기기·과거 실측.** E1~E4, V1~V9, 배달왕복, 실제 시트·트리거·빌드, 당시 테스트 파일 수와 철거 커밋의 수치는 재실측하지 않았다. 이번 실행 검증은 메모리상의 합성 입력을 사용한 순수 함수 호출이다.
- **읽은 파일 — 해당 구간 기준.** 지침과 결정·기각 이력 외에 appsscript의 `docs/엔진7종_상향설계_v2.md`, `docs/관통왕복_명세.md`, `docs/Temper_개입로그_규격_v1.md`, `계약/수집_교정_계약.json`, `tools/계약동기화.js`, `.git/hooks/pre-commit`, `교재연동.js`, `엔진_콘텐츠AI.js`; talk의 `tests/킷대조.test.js`, `tools/관통왕복시험.js`, `lib/왕복골격.js`, `lib/성과회수.js`, `lib/학습자상태.js`, `lib/동의율.js`, `lib/오늘과제.js`, `lib/갈래판정.js`를 대조했다. 전체 저장소 검수는 하지 않았다.