## 새로 더할 것

1. **회수 함수 앞에 ‘입력 완전성’ 조건을 둔다.**  
   **무엇:** 개입별로 기준선 시작부터 후행 창 끝까지 원신호가 공급됐는지 확인한다. `engine.corrections.submission_id → engine.submissions.submission_id → engine.learning_events.event_id`로 교정을 원 제출에 연결하고, 저작신뢰도 그 제출에서 계산한다. `저작판정()`에는 `compose_meta`뿐 아니라 `본문길이`도 필요하다. 조회 상한에 걸렸거나 필요한 고리가 빠지면 결과변수를 계산하지 않는다.  
   **왜:** 현재 입력 공급이 서로 다르다. [성과계기판.js:216](C:/Users/q1212/Documents/SYNK-talk/tools/성과계기판.js:216)은 `learning_events`만 조회하고 전체 `LIMIT`를 건다. [deliver/index.ts:476](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/deliver/index.ts:476)은 현재 상태의 `창일수`로 자른다. [teach/index.ts:919](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/teach/index.ts:919)은 시즌 내부만 조회하며 개입의 `intervention_id`, `policy_ver`, `skill_ids`도 투영하지 않는다. `lib/회고.js`만 고쳐서는 Temper 재료가 도착하지 않는다. [저작신뢰.js:71](C:/Users/q1212/Documents/SYNK-talk/lib/저작신뢰.js:71)은 본문길이가 없으면 `못잼`을 반환한다.  
   **붙일 곳:** U1 「구조」, U5 「생산자」, §9-6 걸음 3·4. ‘계산층 변경’에 조회부·동봉 변경을 함께 명시한다.

2. **‘당시 알던 상태 재현’과 ‘나중에 모인 결과 회수’를 분리한다.**  
   **무엇:** 선택 당시 상태는 `evidence_refs.as_of`와 `evidence_refs.ingested_as_of`로 재현한다. 후행 결과는 개입의 `occurred_at`으로 창을 정하되, `ingested_at`은 평가의 `기준시각`으로 제한한다. 늦게 들어온 원신호를 반영한 재계산과 당시 판단의 재현을 같은 결과로 부르지 않는다.  
   **왜:** [학습자상태.js:258](C:/Users/q1212/Documents/SYNK-talk/lib/학습자상태.js:258)에 이미 이중 절단의 이유와 구현이 있다. 반면 [성과회수.js:230](C:/Users/q1212/Documents/SYNK-talk/lib/성과회수.js:230)은 `ingested_as_of`를 넘기지 않는다. 합성 재현에서는 `기준시각=2027-04-30`인데 `ingested_at=2027-06-05`인 후행 사건이 포함됐다. U3의 `as_of ≤ occurred_at` 검사만으로는 이 누출을 잡지 못한다.  
   **붙일 곳:** U1 「미래정보」, U3 test ⑤, U4·U6 「옛 짝 재계산」.

3. **개입 수와 독립된 관측 수를 가른다.**  
   **무엇:** 같은 `event_id`·`correction_id`의 반복 열람과 같은 후행 제출을 공유하는 여러 개입을 독립 성공으로 누적하지 않는다. 같은 학생의 후행 창에 사람·기계 개입이 겹치면 그 겹침을 표시하고, 단독 개입의 근거와 분리한다. U7에는 개입 건수뿐 아니라 서로 다른 관측·학생·실제 관측된 개입 갈래의 수를 함께 제시한다.  
   **왜:** [성과회수.js:293](C:/Users/q1212/Documents/SYNK-talk/lib/성과회수.js:293)은 개입마다 같은 사건 배열을 다시 읽는다. 이 구조 위에 개입별 `방향 일치`를 그대로 Beta 성공 횟수로 더하면 동일한 제출이 여러 번의 독립 증거가 된다. 인용한 Bähni 논문도 반복 측정의 상관과 다층 구조를 다룬다. 단순 Beta 사후만으로 그 의존성이 해소되지는 않는다. [논문 본문](https://arxiv.org/html/2507.14848v2)  
   **붙일 곳:** U1 「사전분석계획」, U7 「원신호·셀 표본 하한」. 반복 열람을 추가해도 결과변수의 분자·분모가 증가하지 않는 회귀를 `tests/성과회수.test.js`에 요구한다.

4. **사람 개입의 원자성에 멱등 고리를 추가한다.**  
   **무엇:** `observation.noted`와 `intervention.delivered`는 서로 다른 `idempotency_key`를 갖되, 같은 확정 요청에서 재현 가능한 짝이어야 한다. 기존 `parent_event_id`로 원 관찰을 가리키는 계약을 검토한다. 재전송·동시 확정·중간 실패에서도 관찰과 개입이 각각 한 행만 남는지 잰다.  
   **왜:** [teach/index.ts:1637](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/teach/index.ts:1637)은 이미 트랜잭션을 쓰지만, [같은 파일:1671](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/teach/index.ts:1671)의 충돌 기준은 `(learner_id, idempotency_key)`다. 두 insert에 같은 키를 복사하면 한쪽이 사라지고, 재시도마다 새 키를 만들면 개입이 중복된다. ‘같은 트랜잭션’만으로는 짝이 보장되지 않는다.  
   **붙일 곳:** U3 「구조」와 `tests/관찰통로.test.js`·`tests/개입스탬프.test.js`의 검증 요구.

5. **재심 완료를 ‘판 이름 존재’에서 ‘같은 대상의 계산 완료’로 바꾼다.**  
   **무엇:** 기존 `docs/_ops/엔진점수.jsonl`에 재심 결과를 남길 때 대상 범위·기준시각·사용한 판 조합·완료 분자/대상 분모가 확인돼야 완료로 센다. 같은 원신호를 고정하고 선택 규칙, 측정 규칙, 심판을 각각 바꿔 재심한다. 과거 `record_snapshot`은 갱신하지 않는다.  
   **왜:** 판 이름만 있으면 일부 실패한 재계산도 완료로 오인한다. 또한 `성과회수()`는 `학습자상태()`에 의존하고, 저작신뢰 임계 변경도 결과 분모를 바꾼다. ‘값 상수 객체’만의 지문으로 측정 전체의 판을 대표할 수 없다. [성과회수.js:42](C:/Users/q1212/Documents/SYNK-talk/lib/성과회수.js:42), [저작신뢰.js:21](C:/Users/q1212/Documents/SYNK-talk/lib/저작신뢰.js:21), [teach/index.ts:913](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/teach/index.ts:913).  
   **붙일 곳:** U1 「회수판」, U4 「래칫」, U6 「재채점」. 완료 행의 구체적 필드명은 장부 계약을 확인한 뒤 정한다.

6. **사실 한 줄의 근거를 강사 판정과 별도로 검증한다.**  
   **무엇:** 선택한 개입이 있었다는 사실, 그 시즌에 반복됐다는 사실, 강사가 통했다고 판단했다는 사실을 구분한다. 학생 문장에 사용한 사실은 해당 시즌의 원 사건에서 검증돼야 한다. 강사의 선택만으로 빈도 표현을 생성하지 않는다.  
   **왜:** U5의 「통한 개입」 선택은 효과에 대한 판정이다. 그것만으로 예시 문장 「예문을 하나 더 받은 날이 많았어」의 빈도가 증명되지 않는다. 숫자를 숨겨도 `많았어`는 수량 주장이다.  
   **붙일 곳:** U5 「학생 한 줄 문장 규격」, §9-4 Vellum 출력 계약.

## 지금 판이 틀린 것

1. **[P1] U1 ㉯ 「교정 건수 / 제출 건수」는 오류 결과변수가 아니다.**  
   **틀린 점:** 정상 문장도 교정 처리 결과 행이 생긴다. 이 비율은 교정 처리의 도달률을 나타낼 수 있지만 오류 감소를 뜻하지 않는다.  
   **근거:** [correct/index.ts:148](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/correct/index.ts:148)은 유효한 결과를 `engine.corrections`에 저장한다. [교정엔진.js:265](C:/Users/q1212/Documents/SYNK-talk/lib/교정엔진.js:265)은 `error_tags=['오류없음']`을 정상 판정으로 구분한다. 재현 입력은 동일한 제출 수에 대해 전부 `오류없음`인 경우와 전부 오류가 있는 경우다. 처리 완료 행 수가 같으면 제안한 비율도 같다.  
   **수정:** 처리 완료율은 측정 가능성 지표로 내리고, 오류 결과변수는 판정 완료된 적격 제출의 `error_tags`로 별도 정의한다. 교정 미착을 오류 없음으로 세면 안 된다.

2. **[P1] U5·§9-5·§9-6의 「첫 시즌 도래 0」은 산술 오류다.**  
   **틀린 점:** 기준선 14일과 후행 창 30일은 총 44일이다. 8주 시즌 안에 도래할 수 있다. 기준선 14일이 직전 30일 조건을 대체하는지 별도 조건인지도 U1에서 충돌한다.  
   **근거:** [결정.md:138](C:/Users/q1212/Documents/SYNK-appsscript/docs/_ops/결정.md:138)의 현행 시즌은 달력 8주다. `2027-02-25 + 14일 = 03-11`, 그 개입의 30일 창은 `04-10`에 닫힌다. [성과회수.js:235](C:/Users/q1212/Documents/SYNK-talk/lib/성과회수.js:235)도 기준시각이 창 끝에 도달하면 미도래로 분류하지 않는다.  
   **수정:** 첫 시즌 확정을 일괄 금지하지 말고, 각 개입의 기준선 충족·창 도래·적격 관측 충족 여부로 결정한다. 실제 도래 건수는 아직 모른다.

3. **[P1] U2의 계약 목록으로는 U3를 직렬화할 수 없다. — 소급 불가 스키마: 봤다 → 문제다.**  
   **틀린 점:** `expected_ver`·`shadow` 추가와 teacher 명문화만으로는 사람 개입 종류, 제안한 `payload.confirmed_at`, 세 힘 축의 저장 위치가 정해지지 않는다.  
   **근거:** 현행 c16의 [계약/수집_교정_계약.json:112](C:/Users/q1212/Documents/SYNK-talk/계약/수집_교정_계약.json:112)에 이 필드들은 없다. 기존 `tags`는 오류태그다. [관찰초안.js:156](C:/Users/q1212/Documents/SYNK-talk/lib/관찰초안.js:156)의 `확정검사()`에 세 힘 문자열을 넣으면 계약 밖 태그로 거절된다. `actor_kind='teacher'`는 행위자를 구분할 뿐 개입 유형을 구분하지 않는다.  
   **수정:** c17 작업 범위에 저장 위치·값목록·누락 의미·생산자·소비자를 전부 정하는 일을 추가한다. 미정 필드명을 확정된 실물처럼 쓰지 않는다.

4. **[P1] U6 「쌍 수 = 좋음×나쁨」은 제시한 심판 자격시험의 분모가 아니다.**  
   **틀린 점:** 서로 다른 원문에 붙은 좋음·나쁨 답장을 모두 곱하면 동일 입력의 대안 비교가 아니다. 같은 답장을 여러 비교에 재사용한 수도 독립 시험 수가 아니다.  
   **근거:** 인용한 보상모형은 같은 입력 `x`에 대한 `y⁺`, `y⁻`의 선호를 모델링한다. [논문 §4.2](https://arxiv.org/html/2603.24375v1). 가령 좋음 30건과 나쁨 30건으로 비교 900건을 만들더라도 원 답장은 여전히 60건이며, 독립 근거가 900건으로 늘지 않는다.  
   **수정:** 같은 원문·맥락에 대한 답장 대안으로 쌍을 만들고, 원문 단위로 분리한다. 교정 답장과 관찰 구조화 초안은 자격을 따로 보고한다. `정확도 + 95% 구간 하한 > 0.5`라는 표현도 덧셈으로 읽히므로 **「정확도 구간의 하한 > 0.5」**로 통일한다. 구간 계산법은 별도 확정이 필요하다.

5. **[P1] U4의 파일 지문 설명과 활성 무변 약속이 맞지 않는다.**  
   **틀린 점:** `오늘과제.js`는 재료 7이 아니라 재료 12다. 해석기를 같은 파일에 추가하면 생성 경로의 `policy_ver` 지문 재료가 변한다. ‘판 인자 없이 호출한 결과 동일’만으로 지문 불변까지 증명할 수 없다.  
   **근거:** [판독기.js:36](C:/Users/q1212/Documents/SYNK-talk/lib/판독기.js:36)은 재료 7을 `갈래판정`, [같은 파일:41](C:/Users/q1212/Documents/SYNK-talk/lib/판독기.js:41)은 재료 12를 `폴백조립`·`lib/오늘과제.js` 파일 해시로 정의한다.  
   **수정:** 학생에게 나가는 결과의 동일성과 `policy_ver` 변경을 구분한다. 「해석기가 재료 열셋 어느 항목에도 안 드는지」라는 검사는 삭제하고, 어느 재료가 바뀌었으며 활성 결과가 동일한지를 각각 잰다.

6. **[P2] §9-1 「소비자 = 로컬 HTML」「본체 집계 코드 0줄」은 현재 코드와 다르다.**  
   **틀린 점:** 효과 판정은 없지만 런타임 회수 집계는 이미 있다.  
   **근거:** [성과회수.js:273](C:/Users/q1212/Documents/SYNK-talk/lib/성과회수.js:273)의 `회수요약()`을 [deliver/index.ts:635](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/deliver/index.ts:635)이 호출하고, 같은 파일 720행에서 회수 계수를 출력한다.  
   **수정:** ‘없다’의 대상을 결과변수·효과 판정으로 좁힌다. 기존 배치 소비자를 U1 변경 영향과 회귀 범위에 포함한다.

7. **[P2] U3 「초안 변경률 = 초안이 원문을 얼마나 바꿨나」는 다른 지표다.**  
   **틀린 점:** 현재 지표는 원문 변경량이 아니라 AI 구조화 초안과 강사 확정값의 불일치 여부다.  
   **근거:** [관찰초안.js:140](C:/Users/q1212/Documents/SYNK-talk/lib/관찰초안.js:140)의 `초안지문()`은 `area`와 정렬된 `tags`만 사용한다. `고쳤나()`는 `true|false|null`을 반환한다. [teach/index.ts:1624](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/teach/index.ts:1624)이 이를 `draft_modified`로 저장한다.  
   **수정:** 분자는 `draft_modified=true`, 분모는 `draft_modified IS NOT NULL`로 명시한다. 원문 수정량을 재려면 현재 지표와 별개라는 판단이 먼저 필요하다.

8. **[P2] §9-5 「새 파일 0」과 실물 검증점 참조가 틀렸다.**  
   **틀린 점:** 본문은 새 계획 문서·섀도 표·시험지를 요구하면서 전체 파일 신설을 금지한다. `관통왕복 V9(개입 스탬프)`도 현재 실물 이름이 아니다.  
   **근거:** [관통왕복시험.js:12](C:/Users/q1212/Documents/SYNK-talk/tools/관통왕복시험.js:12)의 개입 스탬프 검증점은 **V1**이다. 본문의 V6·V9는 현재 구현된 검증점으로 확인되지 않았다.  
   **수정:** 금지 대상을 기존에 의도한 `새 lib 파일·새 도구 파일·새 장부`로 한정하고, 새 문서·표·시험지는 예정 산출물로 센다. 현행 V1과 확장 예정 검증점을 분리한다.

## 안 재봤다

- **운영 효과·도래 수·갈린 결정 수·강사 작업시간·비용:** 라이브 DB와 실학생 데이터는 조회하지 않았다. 실행 날짜·계약판·원신호 범위가 고정된 집계, 실제 API 사용량, 강사 시연이 필요하다. 유료 호출과 `0원`·`40~60분` 주장도 실측하지 않았다.
- **예정 실물:** `../SYNK-talk/docs/Temper_사전분석계획_v1.md`, `../SYNK-talk/contents/처방판_섀도.json`, 양 저장소의 `evals/temper_시험지.json`은 확인한 경로에 없었다. 새 함수·필드·파일의 확정 이름으로 취급하지 않았다.
- **U6의 운영 가능한 심판:** 인용 논문의 보상모형과 쌍대 비교 정의는 확인했다. 한국어 교정·강사 초안의 자격, 로컬 실행 환경, 처리속도는 재지 않았다. 고정된 입력 묶음과 라벨링 단위부터 필요하다.
- **검증 범위:** 기존 `tests/성과회수.test.js`·`tests/성과계기판.test.js`·`tests/개입스탬프.test.js`는 **36/36 통과**했다. 별도 합성 입력으로 날짜 계산과 늦적재 포함을 확인했다. 이는 §9 상향안의 통과 판정이 아니다.
- **읽은 파일:** 위 근거 링크의 해당 구간, 양 저장소 `AGENTS.md`·수집 계약, talk의 `docs/이_저장소_규약.md`·`docs/공용지침.md`·`lib/오늘과제.js`·`lib/회고.js`·`lib/이벤트검증.js`·`tests/관찰통로.test.js`·teach/deliver `동봉.json`, appsscript의 `docs/Temper_개입로그_규격_v1.md`·`docs/시즌회고_설계.md`·`docs/_ops/결정.md`·`docs/_ops/검수기각.jsonl` 관련 검색·`tools/모델정찰.js`·`tools/rot-check.js` 관련 구간. **안 본 것:** 운영 DB 적용 상태, 실제 화면 왕복, 전체 인용문헌, 전체 저장소 회귀. 파일 수정·배포는 하지 않았다.