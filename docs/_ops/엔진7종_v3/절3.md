## 새로 더할 것

1. **판정 전체에 같은 입력 시점과 근거 집합을 묶는다.**
   - **무엇:** `as_of`뿐 아니라 `ingested_as_of`를 산문 생성·선택·개입 스탬프까지 승계한다. 과거 판정 재현은 당시 입력으로, 새 모델 재심은 같은 입력에 다른 판을 적용한 것으로 구분한다. `evidence_refs.잘림`이 있으면 `evidence_refs.사건`을 전체 근거 목록으로 취급하지 않는다.
   - **왜:** 실물은 이미 늦적재를 차단한다. [../SYNK-talk/lib/학습자상태.js:258](C:/Users/q1212/Documents/SYNK-talk/lib/학습자상태.js:258)은 발생 시각만 자르면 같은 `as_of`에도 입력이 바뀐다고 명시한다. `deliver/생성모드.ts:117`도 두 시각을 함께 넘긴다. 이 경계를 산문에서 잃으면 새 모델의 개선과 뒤늦게 들어온 데이터의 효과가 섞인다.
   - **붙일 곳:** §3-3 ① 파생, §3-4 읽는 법 ②·③·④. 수용 조건은 **기준 시각 뒤에 적재된 과거 사건을 추가해도 당시 판정의 입력 집합이 바뀌지 않음**이다.

2. **‘빼기’의 완료 조건을 캐시 재생성에서 최종 전달 차단까지 올린다.**
   - **무엇:** `atlas.excluded` 이후에는 해당 근거를 사용한 미발송 산출물도 무효화한다. 재생성에 실패해도 제외 전 산문으로 복귀하지 않는다. `shown_key`·`shown_text`·`evidence_refs`를 대조해, 제외 대상이 표시 문장인지 그 문장의 근거인지 D1에서 확정한다. 문장을 바꿔 쓰거나 모델을 바꿔도 동일한 제외 대상이 되살아나면 실패다.
   - **왜:** [../SYNK-talk/supabase/functions/deliver/생성모드.ts:154](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/deliver/생성모드.ts:154)은 요약을 `event_draft`에 굳힌다. `deliver-one/index.ts:408`은 그것을 재계산하지 않고 사용한다. 따라서 캐시만 지우면 이미 대기 중인 작업이 제외 전 글을 전달할 수 있다.
   - **붙일 곳:** §3-3 「학생의 ‘빼기’」, §3-4 읽는 법 ④·⑥. **작업 준비 → 학생 제외 → 옛 작업 완료** 순서를 관통왕복의 필수 반례로 넣는다. 필요한 무효화 계약의 물리 이름은 아직 정하지 않는다.

3. **학생판과 강사판의 경계를 생성 입력부터 검증한다.**
   - **무엇:** 학생판에서는 금지 원천뿐 아니라 그 원천에서 계산한 중간 요약·태그도 생성 입력에서 제외한다. 같은 허용 근거를 둔 채 `observation.noted`만 바꿨을 때 학생판 생성 입력이 동일한지 검사한다.
   - **왜:** `evidence_refs`에서 참조를 지워도 이미 생성된 문장에 관찰 내용이 남을 수 있다. 실물 c14는 원장 열람에서 `observation.noted`를 제외한다([../SYNK-talk/supabase/migrations/20260831130000_classroom_loop_c14.sql:378](C:/Users/q1212/Documents/SYNK-talk/supabase/migrations/20260831130000_classroom_loop_c14.sql:378)). 산문을 거치는 경로에서도 같은 경계가 성립해야 한다.
   - **붙일 곳:** §3-4 읽는 법 ④, §3-5-1의 V2·V5. 검사 대상은 참조 목록뿐 아니라 **학생판에 실제로 전달되는 생성 입력과 조립 결과**다.

4. **접점 예산에 동시 실행·재시도의 소비 규칙을 붙인다.**
   - **무엇:** 하루 예산의 조회·예약·소비 확정을 하나의 처리 단위로 정의한다. 생성 완료와 실제 노출을 구분하고, 같은 `idempotency_key`의 재시도가 예산을 다시 소비하지 않게 한다. 몽골 날짜 경계와 미노출 예약의 해제 조건도 함께 정한다.
   - **왜:** §3-4의 상수 한 곳은 값 불일치를 막지만, 서로 다른 생산자가 동시에 남은 슬롯을 읽어 각각 내보내는 일은 막지 못한다. 실물 [../SYNK-talk/src/사건통로.js:106](C:/Users/q1212/Documents/SYNK-talk/src/사건통로.js:106)은 사건의 `idempotency_key`를 승계한다. 이 재시도 규칙과 예산 소비가 연결돼야 한다.
   - **붙일 곳:** §3-4 읽는 법 ⑧. 수용 조건은 **생산자 동시 실행·응답 유실·날짜 경계 재시도에서도 정해진 하루 상한을 넘지 않음**이다. 예약을 저장할 표·열 이름은 D1에서 확정해야 한다.

5. **30일 짝에서 ‘전달 확인’과 ‘효과 귀속’을 별도로 닫는다.**
   - **무엇:** `intervention_kind`마다 실제 노출을 입증하는 사건과 연결 키를 지정한다. `content.viewed.parent_event_id`가 없는 갈래는 관측 통로 미완성으로 남긴다. 같은 후속 사건이 여러 개입의 창에 들어갔을 때, 그것을 여러 개입의 독립된 성공으로 세지 않는 규칙도 사전분석계획에 추가한다.
   - **왜:** [../SYNK-talk/lib/성과회수.js:154](C:/Users/q1212/Documents/SYNK-talk/lib/성과회수.js:154)의 관측 짝은 `content.viewed`만 읽는다. 같은 파일 `:164`는 미열람과 생산자 미배선을 하나의 `false`에 사유로 병기한다. `창별성과():230`·`:241`은 개입 전후 상태를 계산하므로, 서로 다른 개입이 같은 후속 변화를 공유할 수 있다. 스탬프가 완비돼도 어느 개입 때문에 달라졌는지는 자동으로 정해지지 않는다.
   - **붙일 곳:** §3-2 ⑥, §3-4 읽는 법 ③, V9. 분모를 **전체 개입 / 관측 통로가 있는 개입 / 실제 노출이 확인된 개입 / 결과 창이 도래한 개입**으로 구분한다. 섀도 산출에는 실행하지 않은 개입의 실학생 효과를 붙이지 않는다.

6. **재심의 완료 조건을 ‘발동’에서 ‘결과 회수’까지 늘린다.**
   - **무엇:** 기존 재심 큐에서 감지된 변경과 시험지·실행판의 조합을 중복 없이 추적한다. 로컬 종료·예산 소진·일부 시험지 실패 후에는 미완료분을 재개한다. 큐 등록, 실행 시작, 결과 수신, 사람 판정을 각각 구분한다.
   - **왜:** [tools/모델정찰.js:499](C:/Users/q1212/Documents/SYNK-appsscript/tools/모델정찰.js:499)의 장부 기록은 정찰 기록의 저장이다. §3의 「재심 발동/감지」만으로는 큐에 들어간 뒤 끝나지 않은 재심이 성공처럼 보일 수 있다.
   - **붙일 곳:** §3-2 「재는 자」, §3-4 읽는 법 ⑫. 추가 눈금은 **완료된 재채점 / 실행 대상 재채점**, 그리고 미완료 사유다. 모델 픽과 재채점 완료는 별개로 유지한다.

## 지금 판이 틀린 것

1. **§3-3 ②·소유 경계, §3-5의 「옛 요약 잔존 문제도 원리상 0」**
   - **무엇이 틀렸나:** 산문을 캐시 표에 두는 것만으로 사건 원장에 본문이 남지 않는다는 결론은 성립하지 않는다. 현재 생성 통로를 그대로 연결하면 산문이 요청 본문에 포함돼 다시 저장된다.
   - **근거:** `../SYNK-talk/lib/과제생성.js:198`은 `{{요약}}`에 요약 전문을 넣는다. [../SYNK-talk/lib/착지봉투.js:30](C:/Users/q1212/Documents/SYNK-talk/lib/착지봉투.js:30)은 요청을 `payload.generation_input_text`에 담는다. `supabase/migrations/20260821120000_generation_c12.sql:912`는 그것이 `engine.generation_attempts.request_body`와 같은지 강제한다.
   - **재현 입력·수정 방향:** 합성 문자열을 `착지봉투()`의 `input_text`로 넘기면 개입 사건 봉투에 그대로 남는다. 따라서 **AI 추정 저장 금칙과 기존 요청 본문 보존 계약의 충돌을 먼저 해결해야 한다.** 캐시 삭제만으로 해결됐다고 쓰면 안 된다.

2. **§3-4 읽는 법 ③의 `actor_kind∈{machine,teacher}`**
   - **무엇이 틀렸나:** `machine`은 실물 `engine.actor_kind`의 값이 아니다.
   - **근거:** [../SYNK-talk/supabase/migrations/20260806150000_engine_c6.sql:379](C:/Users/q1212/Documents/SYNK-talk/supabase/migrations/20260806150000_engine_c6.sql:379)의 enum은 `('learner', 'ai', 'teacher')`다. `lib/사건출처.js:11`과 `lib/이벤트검증.js:340`도 같은 어휘를 쓴다.
   - **재현 입력·수정 방향:** `actor_kind='machine'`인 행은 현재 enum과 맞지 않는다. 기존 어휘를 승계하는 개입 생산자는 `ai`·`teacher`로 적어야 한다.

3. **§3-3 ⓪의 [기존]/[새] 표기**
   - **무엇이 틀렸나:** `goal.responded`와 `observation.noted`를 [새]로 적었고, 기존 원장에 `(c17)`을 붙여 현재 계약판과 목표 계약판을 섞었다.
   - **근거:** [계약/수집_교정_계약.json:3](C:/Users/q1212/Documents/SYNK-appsscript/계약/수집_교정_계약.json:3)은 `c16`이다. 같은 파일 `:101`에 두 사건이 이미 있으며, `../SYNK-talk/lib/학습자상태.js:311`·`:313`에 소비자도 있다.
   - **수정 방향:** 현재 원장은 `[기존 c16 → 목표 c17]`, 두 사건은 `[기존 c14]`로 적고 **이번에 추가할 payload·생산 경로·소비 범위만 [새]**로 구분한다. 이는 라이브 배포 확인과는 별개다.

4. **§3-6 읽는 법 첫째 행의 세 힘 하위 갈래 `(observed)`**
   - **무엇이 틀렸나:** 강사가 관찰 원문에서 선택한 태그를 `observed`로 옮기면 같은 근거의 출처가 파생 과정에서 바뀐다.
   - **근거:** [../SYNK-talk/lib/사건출처.js:64](C:/Users/q1212/Documents/SYNK-talk/lib/사건출처.js:64)는 확정된 `observation.noted`를 언제나 `teacher`로 둔다. `lib/학습자상태.js:933`은 그 이유로 관찰축을 `표본제외`에 둔다.
   - **재현 입력·수정 방향:** `observation.noted.payload.tags`에 세 힘 태그가 붙어도 출처는 `teacher`다. **태그별 분모를 새로 만드는 것과 학생 행동 표본으로 승격하는 것을 구분해야 한다.**

5. **§3-4 읽는 법 ⑨의 「학생 답이 굳은 뒤에만 강사 칸이 열린다」**
   - **무엇이 틀렸나:** 기존 「학생 먼저」 규칙을 학생 응답 필수 규칙으로 강화했다. 무응답이면 회고가 막히는 변경인데, 기존 확정의 복원처럼 설명한다.
   - **근거:** [../SYNK-talk/supabase/migrations/20260812170000_season_review_c11.sql:40](C:/Users/q1212/Documents/SYNK-talk/supabase/migrations/20260812170000_season_review_c11.sql:40)은 `verdict_by_self=null`이어도 강사 판정이 진행된다고 명시한다. `:159`의 차단 대상은 **강사 판정 뒤 학생 답을 변경하는 것**이다. `src/회고화면.js:15`에도 「학생 없이 진행」이 있다.
   - **재현 입력·수정 방향:** 학생 무응답 상태에서 강사가 회고하려는 경우다. 기존 규칙을 승계하려면 **학생 응답 기회 먼저 → 응답 또는 미응답 확정 → 강사 판정**, 미응답은 `null`로 남긴다. 응답 필수로 바꾸려면 별도 판정 사항이다.

6. **§3-6 읽는 법 열째 행의 「알렸나/안 알렸나가 이미 사실로 남는다」**
   - **무엇이 틀렸나:** 개입 도장은 시스템·강사가 무엇을 했는지의 기록이다. 학생이 문제를 알아차리고 알렸는지의 기록이 아니다. 세 힘 태그 행 수/관찰 확정 수도 관찰 기록의 구성비이지 성장 자체가 아니다.
   - **근거:** §3-4 읽는 법 ③의 스탬프 목록에는 학생의 문제 인지·보고 행동을 식별하는 계약이 없다. [../SYNK-talk/lib/학습자상태.js:944](C:/Users/q1212/Documents/SYNK-talk/lib/학습자상태.js:944)의 관찰축은 `area`·`tags`의 빈도를 집계한다.
   - **재현 입력·수정 방향:** 학생이 아무 말도 하지 않은 날에도 강사의 개입 행은 존재할 수 있다. 반대로 학생이 자발적으로 알렸어도 개입 행이 없을 수 있다. **행동할 기회가 있었는지와 그때 실제로 무엇을 했는지의 짝**이 서기 전까지 SHIFT 자의 측정은 미완료다. 해당 표식의 필드명은 지어내지 않는다.

## 안 재봤다

- **라이브 상태:** 실제 Supabase enum·CHECK·RLS·예약 실행, 시트 수집과 배포 상태는 안 재봤다. 위 판단은 작업본 계약·코드·마이그레이션을 근거로 한다. 라이브 충족 여부에는 배포판과 읽기 전용 실측이 필요하다.
- **새 계약의 물리:** 제외 대상의 식별·승계, 미발송 산출물 무효화, 접점 예약, 라디오·교실 개입의 관측 짝은 표·열까지 확정된 구현을 확인하지 못했다. D1 확정본과 생산자·소비자 양쪽 구현이 필요하다.
- **효과·비용·보장:** 보장 의무를 내부에서 추적하는 방향은 **봤다 → 괜찮다**. 실제 보장 이행, Brier 예측력, 개입 효과, 월비용 상한 충족은 안 재봤다. 사전 고정한 평가 규칙과 실학생 결과·실행 비용 기록이 필요하다. AI 추정 저장 금칙과 소급 불가 계약은 **봤다 → 위 오류의 충돌이 문제다**.
- **이번 실행:** 파일 생성·수정 없이 합성 반례 **2/2**를 확인했다—늦적재 경계의 입력 차이, 요청 본문의 사건 봉투 잔존. 전체 회귀·관통왕복·DB 시험은 실행하지 않았다.
- **읽은 파일:** SYNK-appsscript의 `AGENTS.md`, `계약/수집_교정_계약.json`, `docs/엔진7종_상향설계_v2.md`, `docs/관통왕복_명세.md`, `docs/SYNK_철학.md`, `docs/_ops/결정.md`, `docs/_ops/검수기각.jsonl`, `tests/수집탭워치독.test.js`, `tools/모델정찰.js`의 관련 구간. SYNK-talk의 지침 파일과 `lib/학습자상태.js`, `lib/성과회수.js`, `lib/회고.js`, `lib/사건출처.js`, `lib/이벤트검증.js`, `lib/과제생성.js`, `lib/착지봉투.js`, `src/사건통로.js`, `src/회고화면.js`, `supabase/functions/deliver/생성모드.ts`, `supabase/functions/deliver/index.ts`, `supabase/functions/deliver-one/index.ts` 및 위에서 인용한 마이그레이션의 관련 구간을 읽었다. 저장소 전량 검수는 아니다.