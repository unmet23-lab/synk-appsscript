## 새로 더할 것

1. **추정 대상과 독립 표본의 계약**

   **무엇:** 산출마다 「누구의·어떤 과업에서·어떤 조건 아래의 분포인가」를 고정한다. 제출 수와 별개로 독립 학습자 수·학습자별 기여 편중을 계산하고, 카드의 최소 표본 조건에도 적용한다. 식별자는 백엔드 계산에만 사용한다.

   **왜:** 한 학습자가 낸 제출 30/30과 서로 다른 학습자의 제출 30/30은 같은 증거가 아니다. 반복 제출만으로 문턱을 넘으면 소수 반 보호와 분포의 대표성이 함께 깨진다. 현재 계약에는 `learner_id`, `task_type`, `task_format`, `level_snapshot`이 있지만 이들을 이용한 표본 단위 규칙은 §8에 없다. 근거: [계약/수집_교정_계약.json:79](C:/Users/q1212/Documents/SYNK-appsscript/계약/수집_교정_계약.json:79).

   **붙는 곳:** U0 산출 계약·U3 셀 정의·U8 문턱. 학원 관측을 「몽골어 화자 일반」으로 확대할 조건이 충족되기 전에는 학원·과업·관측 기간으로 주장 범위를 제한한다.

2. **사전등록에 ‘결과를 가르는 규칙’과 등록 시점 증거 추가**

   **무엇:** 가설마다 예측 방향, 분자·분모의 계산식, 제외 조건, 측정 실패 처리, 비교 구간, 반복 대조 처리, 조준 발동 조건까지 관측 전에 동결한다. 변경은 원등록을 보존한 후속 개정으로 남긴다. CI에는 첫 실학생 제출의 원문 대신 백엔드가 확정한 비식별 시점 증거만 전달한다.

   **왜:** 지금의 「재는 자·조준 가능 시즌·등록일」만으로는 결과를 본 뒤 분모나 제외 조건을 바꿔도 사전등록처럼 보인다. 또한 `occurred_at`과 `ingested_at`은 다른 시계다. 첫 제출의 기준을 기기 발생 시각으로 할지 서버 수납 시각으로 할지 정하지 않으면 오프라인 제출이 등록의 선후를 뒤집는다. 근거: [계약/수집_교정_계약.json:78](C:/Users/q1212/Documents/SYNK-appsscript/계약/수집_교정_계약.json:78), [이벤트검증.js:81](C:/Users/q1212/Documents/SYNK-talk/lib/이벤트검증.js:81).

   **붙는 곳:** U1 「CI 자」 바로 뒤. 키를 읽지 않는 CI가 무엇으로 첫 제출을 아는지도 이 계약에서 닫는다.

3. **오류별 ‘기회’ 정의와 분석 실패의 상·하한**

   **무엇:** 각 오류태그에 대해 기회를 원문에서 셀지, 교정문에서 셀지, 과업 조건에서 셀지 정의한다. 누락·대치 때문에 원문에서 사라진 기회도 다룬다. 분석 실패는 제출 분모에는 남기되, 기회 수를 모르는 상태를 정상 기회나 오류 없음으로 계산하지 않는다. 실패분 처리에 따라 조준 방향이 바뀌면 조준을 보류한다.

   **왜:** `조사:여격(에게·한테)`의 기회를 실제 등장한 `에게·한테` 토큰만으로 세면, 그 조사를 빠뜨리거나 다른 조사로 바꾼 오류가 분모에서 빠진다. 반대로 `corrected_text`에 추가된 표현을 모두 필수 기회로 보면 교정자의 문체가 분모를 만든다. 근거: [계약/수집_교정_계약.json:25](C:/Users/q1212/Documents/SYNK-appsscript/계약/수집_교정_계약.json:25), [계약/수집_교정_계약.json:80](C:/Users/q1212/Documents/SYNK-appsscript/계약/수집_교정_계약.json:80).

   **붙는 곳:** U2 「기회 수 열」의 정의·U3 「미달」 조건. 기회 수의 완비율과 기회 정의의 타당성을 별도로 검증한다.

4. **측정 오차를 ‘대조’에 전파하는 규칙**

   **무엇:** 태거 정밀도 표를 보여주는 데서 끝내지 않고, 그 오차를 고려해도 예측 안/밖이 유지되는지 검사한다. 교정 모델·태거·발음 인식기를 교체할 때는 동일 원문 또는 동일 원음을 구판과 신판으로 교차 측정한다. `model`, `prompt_ver`, `estimator_version`이 바뀐 효과와 학생 산출이 바뀐 효과를 분리한다.

   **왜:** 결정론 정렬기라도 입력 `corrected_text`가 모델 교체로 달라지면 오류율이 변한다. 태그별 이진 판정에서는 관측 양성률이 민감도·특이도·실제 오류율에 함께 의존하므로, 정밀도 하나를 고정된 「잡음 상한」처럼 사용할 수 없다. 기존 계약도 측정 규칙과 선택 규칙을 구별한다. 근거: [계약/수집_교정_계약.json:90](C:/Users/q1212/Documents/SYNK-appsscript/계약/수집_교정_계약.json:90), [이벤트검증.js:32](C:/Users/q1212/Documents/SYNK-talk/lib/이벤트검증.js:32).

   **붙는 곳:** U3 대조기·U5 채택 자·U7 모델 교체 인계. 동일 시험지의 모델 성적과 장기 분포의 측정 연속성을 별도로 통과시킨다.

5. **조준 전후의 비교 가능성 검증**

   **무엇:** 다음 시즌과의 비교는 동일 낱말·동일 `task_format` 등 공통 조건이 남아 있는 셀에서 먼저 한다. 과업 구성까지 바뀐 전체 오류율은 별도로 표시한다. 조준의 선택 과정이 복원되지 않거나 비교할 공통 조건이 없으면 개선·악화 방향을 내지 않는다.

   **왜:** 자연/조준을 갈라 저장하는 것만으로 선택 편향이 제거되지는 않는다. 어려운 낱말을 쉬운 낱말로 바꿨다면 오류율 감소는 학습 효과 없이도 발생한다. 적응적으로 수집한 자료의 단순 평균이 편향될 수 있다는 연구에 비춰도 필요한 구분이다. [Confidence intervals for policy evaluation in adaptive experiments](https://pmc.ncbi.nlm.nih.gov/articles/PMC8054003/). 과업 형식과 당시 과업은 이미 별개 계약 필드다. [계약/수집_교정_계약.json:79](C:/Users/q1212/Documents/SYNK-appsscript/계약/수집_교정_계약.json:79).

   **붙는 곳:** U4 「등장률 처리」·U3 시즌 대조·Temper 인계. 확정된 축별 낱말 수를 바꾸지 않고 비교의 유효 범위를 좁히는 장치다.

6. **재검수·철회·지연 도착을 처리하는 산출 계보**

   **무엇:** 산출이 사용한 입력 집합과 교정 계보를 보존한다. `supersedes`로 대체된 교정을 중복 집계하지 않고, 재검수 시 관련 셀을 다시 계산한다. 시즌 종료 당시의 산출과 뒤늦은 자료를 반영한 재계산본은 구별한다. `data_use.revoked`가 오면 원음 사본·노트북 캐시·평가 자료까지 영향을 추적하고 재복사를 막는다.

   **왜:** 생산자별 중복 제거만으로는 같은 제출의 재검수 전후를 가르지 못한다. 실제 검수 통로에는 `supersedes`가 있으며, 철회 계약은 보관 중인 녹음 삭제를 요구한다. 해시 이름으로 사본을 늘리면서 원본과의 고리를 버리면 그 약속을 집행할 수 없다. 근거: [review/index.ts:628](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/review/index.ts:628), [L0_데이터계약.md:736](C:/Users/q1212/Documents/SYNK-talk/docs/L0_데이터계약.md:736).

   **붙는 곳:** U2 짝 스윕·U3 재계산·U5 사본 통로·U7 표집. 기존 L0처럼 행·통계·계보의 보존과 녹음 내용 삭제를 구별한다.

7. **개발용 정답과 최종 평가용 정답의 분리**

   **무엇:** 사상표·원인 규칙·발음 판정 기준을 고치는 데 사용한 정답은 최종 성적의 독립 평가분에서 제외한다. 같은 제출의 재교정·재제출과 같은 화자의 반복 녹음은 분할 경계를 함께 넘지 못하게 한다. 평가자는 모델 초안을 보기 전에 자기 라벨을 확정한다.

   **왜:** 정답지를 보며 규칙을 고친 뒤 같은 정답지에서 높은 일치율을 얻는 것은 일반화 검증이 아니다. 파일 SHA가 같아도 평가 자료가 개발 과정에 노출된 문제는 남는다. 기존 `retry_of_event_id`, `correction_id`, `supersedes`는 그 중복 계보를 추적할 실물이다. 근거: [계약/수집_교정_계약.json:79](C:/Users/q1212/Documents/SYNK-appsscript/계약/수집_교정_계약.json:79), [review/index.ts:707](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/review/index.ts:707).

   **붙는 곳:** U2 검산·U5 안의 귀 라벨·U6 원인 정답지·U7 bake. 새 정답지 수량은 제안하지 않고 기존 발주분의 사용 목적부터 나눈다.

## 지금 판이 틀린 것

1. **U2 — 「결정론 18종 + 해석 6종」은 계약의 분할이 아니다**

   **틀린 점:** 현재 계약은 전체 24/24 항목 중 오류가 23/24, `오류없음`이 1/24다. 오류를 18종과 6종으로 나누면 정상 상태까지 오류 종류로 센다. 또한 편집의 결정론적 검출이 그 편집의 교육적 필요성까지 결정론적으로 증명하지 않는다.

   **판단 근거:** [계약/수집_교정_계약.json:22](C:/Users/q1212/Documents/SYNK-appsscript/계약/수집_교정_계약.json:22)의 목록과 `오류없음` 항목. Korean ERRANT 논문도 조사·어순·생략 등에서 복수의 유효한 교정이 성립함을 설명한다. [논문 §5.3–5.4](https://arxiv.org/html/2605.30545v1).

   **수정:** 계약 항목별로 편집 검출 가능 여부와 오류 해석 가능 여부를 따로 대응시킨다. `오류없음`과 분석 실패는 그 분할 밖의 상태로 다룬다. 원문 유지가 가능한 문장을 자연스럽게 고친 경우를 반드시 거짓양성 검산에 넣는다.

2. **U1·U3 — NIKL 위치 빈도와 학원 기회 대비 오류율은 바로 대조할 수 없다**

   **틀린 점:** 오류 주석의 위치별 빈도·구성비에서 기회 대비 오류 확률이 곧바로 나오지는 않는다. 분모 미상 표식만 붙여 수치 prior로 사용해도 이 단위 차이는 해소되지 않는다. `몽골` 국적과 `몽골어` 언어권도 같은 필터가 아니다.

   **판단 근거:** [NIKL 통계 화면](https://kcorpus.korean.go.kr/service/goErrorAnnotArea.do)은 오류 주석 빈도 화면이며 국적·언어권 선택이 별도로 존재한다. 이번에 읽힌 화면의 어절 수/표본 수는 실제 수치가 채워지지 않은 상태였다. 또한 U8 예시는 제출 38건을 소개한 뒤 같은 38을 기회 분모로 사용한다. [엔진7종_상향설계_v2.md:4451](C:/Users/q1212/Documents/SYNK-appsscript/docs/엔진7종_상향설계_v2.md:4451).

   **수정:** 분모의 의미가 일치하는 자료만 수치 대조에 넣는다. 위치 빈도만 확보되면 그 범위의 구성비·순위 가설로 제한한다. 카드에서도 제출 수와 조사 기회 수를 별도로 적는다.

3. **U0·U4·U6 — 「어느 형태로도 학생에게 안 닿음」과 실제로 허용한 파생 통로가 충돌한다**

   **틀린 점:** U4의 지정 낱말은 학생에게 도달하고, U6의 사전 파생 `prompts/교정.md`는 교정 출력에 영향을 준다. 그런데 `prism_` 문자열 참조만 검사하면 파생 프롬프트를 통한 전달은 검사 밖이고, 전부 금지하면 U4 자체가 막힌다.

   **판단 근거:** §8 내부 입출력의 충돌이다. 철학의 실제 금지는 학생·학부모에게 집단 비교를 제시하는 것이다. [SYNK_철학.md:48](C:/Users/q1212/Documents/SYNK-appsscript/docs/SYNK_철학.md:48).

   **수정:** 분포값·집단 비교 설명의 전달 금지와 과업 재료로의 변환을 구별한다. 허용 변환의 출력까지 검사해야 한다. U6의 원인 어휘가 학생 답장에 들어가지 않는다는 약속은 프롬프트 파생 이후에도 검증해야 한다.

4. **U4 — 「옛 행 비고」와 입구 셋으로는 낱말 교체가 완결되지 않는다**

   **틀린 점:** `voiceMissionTexts_`는 `비고`를 보지 않고 `미션ID`·`목표발화`가 있는 모든 행을 읽는다. 옛 행을 비고만 바꿔 남기면 계속 지정 낱말에 포함된다. 또한 `voiceSweep_`는 수집 실행 당시의 목록·시즌을 읽는다. 이미 저장된 `voice_log`가 불변이어도, 교체 전에 배정되고 교체 후 수집된 녹음에는 새 목표가 붙을 수 있다.

   **판단 근거:** [교재연동.js:247](C:/Users/q1212/Documents/SYNK-appsscript/교재연동.js:247), [교재연동.js:378](C:/Users/q1212/Documents/SYNK-appsscript/교재연동.js:378), [교재연동.js:414](C:/Users/q1212/Documents/SYNK-appsscript/교재연동.js:414).

   **수정:** 활성 낱말을 실제로 읽는 `voiceMissionTexts_`까지 범위에 넣고, 총수 검사는 보존 이력을 포함한 행 수와 구별한다. 당시 `task_snapshot` 또는 그에 대응하는 배정 증거로 목표를 확정해야 한다. 재현 입력은 「옛 과업 배정 → 목록 교체 → 지연 제출·스윕」이다.

5. **U5 — 강제정렬을 목표 낱말의 존재 판정처럼 사용할 수 없다**

   **틀린 점:** `Qwen3-ForcedAligner-0.6B`는 음성과 주어진 텍스트를 정렬한다. 정렬에 성공했다는 사실만으로 학생이 목표 낱말을 실제 발음했다고 결론낼 수 없다. 정렬 입력 텍스트의 출처도 현재 순서에 빠져 있다. `PER` 역시 인식기의 오류와 학생 발음의 차이를 구별하지 않고 쓰였다.

   **판단 근거:** [공식 ForcedAligner 사용 예](https://huggingface.co/Qwen/Qwen3-ForcedAligner-0.6B)는 `audio`, `text`, `language`를 받는다. [음소 인식기 모델 카드](https://huggingface.co/slplab/wav2vec2-XLSR-300m_KoreanPhonene_spoken_by_foreigners)는 사람이 들린 대로 전사한 음소열과 예측 음소열을 구별한다.

   **수정:** 정렬용 텍스트의 출처, 목표 존재의 독립 판정, 정렬 실패를 명시한다. 인식기 PER은 기계열 대 사람 귀 라벨로 재고, 학생 발음의 차이는 사람 귀 라벨 또는 검증된 인식열 대 기대 발음으로 잰다. 목표를 말하지 않은 녹음과 다른 낱말을 말한 녹음을 음성 대조 입력에 넣는다.

6. **U6 — 원인 ID 일치가 L1 원인의 검증은 아니다**

   **틀린 점:** 사람과 모델이 같은 원인 ID를 골라도, 그 오류가 몽골어 전이 때문에 발생했다는 인과 설명까지 검증된 것은 아니다. 집단 단면만으로 「쓸 때 맞고 말할 때 무너진다」는 같은 사람의 교차 양상도 증명할 수 없다. 합성 정확도를 실문장 정확도의 「상한」이라고 부를 근거도 없다.

   **판단 근거:** 계약은 `l1_source_phrase`를 검수자의 사후 추정으로 명시하며 학생의 실제 L1 의도와 구별한다. [계약/수집_교정_계약.json:11](C:/Users/q1212/Documents/SYNK-appsscript/계약/수집_교정_계약.json:11). U6 자체의 교차 규격은 「집단 단면만」이다.

   **수정:** 현재 가능한 출력은 원인 규칙과의 양립 여부·주석자 간 일치다. 발달 과정의 오류 등 경쟁 설명을 가를 증거가 없으면 원인 확정으로 승격하지 않는다. 집단 교차는 두 분포의 차이로 표현하고, 합성 성적은 합성 조건의 성적으로만 적는다.

7. **U7 첫 커밋·U2 합성 벌 — 이미 있는 기능과 없는 기능을 잘못 묶었다**

   **틀린 점:** `게이트함수들 = ['correct']`와 벤더 응답의 `model` 수집은 이미 구현돼 있다. 반면 `correct?평가=1`은 받은 문장을 기존 교정 프롬프트로 처리하는 통로이며 합성 오류 문장을 생성하는 통로가 아니다.

   **판단 근거:** [eval-run.js:45](C:/Users/q1212/Documents/SYNK-talk/tools/eval-run.js:45), [correct/index.ts:217](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/correct/index.ts:217), [correct/index.ts:233](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/correct/index.ts:233), [correct/index.ts:252](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/correct/index.ts:252).

   **수정:** 새 구현은 후보 선택 인자, 허용 조합 검증, 실측 모델의 결과 보존, «못 쟀다» 기록에 한정한다. 합성 오류의 생성과 생성된 문장의 평가를 별도 단계로 명시한다. 모델·효력 값은 제안하지 않는다.

8. **U2·U6·U7 — 재사용할 ‘살균 규칙’이 본문 비식별화를 보장하지 않는다**

   **틀린 점:** 식별 열을 빼는 것과 자유서술 안의 식별 내용을 제거하는 것은 다르다. 현재 `역소독_`는 비식별화 함수가 아니라 시트 수식 방어용 아포스트로피를 되돌리는 함수다.

   **판단 근거:** [엔진_수집.js:1096](C:/Users/q1212/Documents/SYNK-appsscript/엔진_수집.js:1096), [엔진_수집.js:1158](C:/Users/q1212/Documents/SYNK-appsscript/엔진_수집.js:1158). [SYNK_철학.md:76](C:/Users/q1212/Documents/SYNK-appsscript/docs/SYNK_철학.md:76)도 자유서술 이름 필터가 없음을 명시한다.

   **수정:** 기존 함수 재사용으로 본문 비식별화까지 완료됐다고 쓰지 않는다. 내부 보관 사본과 외부 모델·검수자에게 전달할 텍스트의 통과 조건을 구별해야 한다. 비공개 버킷에 저장했다는 사실은 본문 비식별화의 증거가 아니다.

9. **U8 — 강사·반 사건은 현재 `intervention.delivered` 계약에 바로 들어가지 않는다**

   **틀린 점:** 현재 사건 표의 `learner_id`는 `NOT NULL`이고 멱등키도 학습자와 묶인다. 수신자=강사·반 키만으로 사건을 만들 수 없다. `policy_ver`의 뜻도 단순 카드판이 아니라 개입을 고른 선택 규칙의 판이다. 카드 없음까지 같은 전달 사건으로 세면 기존 전달 건수의 뜻이 달라진다.

   **판단 근거:** [20260806150000_engine_c6.sql:404](C:/Users/q1212/Documents/SYNK-talk/supabase/migrations/20260806150000_engine_c6.sql:404), [이벤트검증.js:32](C:/Users/q1212/Documents/SYNK-talk/lib/이벤트검증.js:32). `payload_허용필드`에도 제안한 수신자·반·근거 셀 이름은 등재돼 있지 않다. [계약/수집_교정_계약.json:112](C:/Users/q1212/Documents/SYNK-appsscript/계약/수집_교정_계약.json:112).

   **수정:** c17의 반 스냅샷만으로 닫히지 않는 별도 계약 과제다. 강사·반 귀속, 선택판과 내용판, 전달·미전달, 기존 소비자의 집계 범위를 먼저 정한다. 임의 학생에게 귀속하거나 반 학생마다 사건을 복제해 우회하면 안 된다.

10. **U8 — 응답 꼬리만으로 수신·관찰 순서·대조군이 성립하지 않는다**

    **틀린 점:** 현재 `관찰보내기`는 응답 필드를 골라 반환하므로 서버가 카드 필드를 추가해도 화면까지 자동 전달되지 않는다. 서버 응답 생성은 앱 수신의 증거도 아니다. 또한 현재 완료 화면에는 「이 학생에게 하나 더 남기기」가 있어 한 관찰 뒤의 카드가 바로 다음 관찰 앞에 놓인다. 문턱 미달로 카드가 없는 주는 카드가 있는 주와 표본 조건부터 달라 인과 대조군이 아니다.

    **판단 근거:** [관찰API.js:107](C:/Users/q1212/Documents/SYNK-talk/src/관찰API.js:107), [관찰화면.js:162](C:/Users/q1212/Documents/SYNK-talk/src/관찰화면.js:162), [teach/index.ts:1671](C:/Users/q1212/Documents/SYNK-talk/supabase/functions/teach/index.ts:1671).

    **수정:** 서버 응답→API 변환→완료 화면을 함께 변경하고, 관찰 재전송 시 카드가 중복 배달되지 않게 한다. 수신 증거가 없으면 서버 응답까지만 센다. 카드 장애가 이미 확정된 관찰을 실패시키지 않도록 분리한다. 이후 관찰은 이전 카드 노출을 고려하고, 「없음 주」는 기술적 비교군으로 제한한다.

## 안 재봤다

- **확인 불가 — 라이브 상태·성능·비용.** 시트 값, 실제 버킷 설정·객체, 예약 작업, 배포판, 벤더 호출, 음성 추론은 실행하지 않았다. `docs/_ops/트랙.md:114`에는 이후 수동 수집 기록이 있으므로 §8의 09-02 현황을 현재 상태로 그대로 재사용하면 안 된다. 통과 판정도 하지 않았다.

- **새 실물의 이름·형상.** 제시된 `tools/prism.js`, `tests/prism계약.test.js`, `tools/밤샘.js`, `.github/workflows/모델정찰.yml`은 확인한 경로에 없다. `prism_map`, `prism_delivery`, `prism_tag_ver`의 구현도 확인한 계약·코드 범위에서 찾지 못했다. 이들은 신설 제안으로만 남겨야 한다. `lib/자격증명.js`는 appsscript 루트가 아니라 `../SYNK-talk/lib/자격증명.js`에서 존재만 확인했다. 내용은 열지 않았다.

- **NIKL·발음·정답지의 실제 적합성.** 몽골어 필터를 적용한 통계값·기회 분모·원시 자료 이용 조건, 형태소 분석기 선정, 제시된 픽스처의 태그별 구성, 몽골어 화자 음성에서의 인식 성능은 미측정이다. 해당 자료와 독립 정답, 실패 사례별 실행 결과가 필요하다. 인용된 모든 논문·가격표를 재검증한 것도 아니다.

- **값비싼 변경은 봤다 → 현재 서술에 문제가 있다.** 대외 일반화 문안, 본문 비식별화 주장, 강사·반 사건의 스키마, 과업 스냅샷의 소급 불가 조건은 위 지적에 포함했다. 금액은 실청구 근거가 없어 승인 가능한 확정 비용으로 보지 않았다.

- **읽은 파일·범위:** appsscript의 `AGENTS.md`, `package.json`, `계약/수집_교정_계약.json`, `엔진_수집.js`, `교재연동.js`, `엔진_셋업확장.js`, `docs/엔진7종_상향설계_v2.md`, `docs/SYNK_철학.md`, `docs/발음데이터_규격.md`, `docs/_ops/결정.md`, `docs/_ops/검수기각.jsonl`, `docs/_ops/트랙.md`, `tools/모델정찰.js`, `tools/모델정책.js`의 관련 구간을 읽었다. `tools/이해대장.js` 등은 관련 참조를 검색했다. SYNK-talk에서는 지침 문서, 동일 수집 계약, `lib/이벤트검증.js`, `lib/작성과정.js`, `lib/동의게이트.js`, `lib/성과회수.js`, `src/관찰API.js`, `src/관찰화면.js`, `supabase/functions/{teach,correct,review}/index.ts`, `tools/eval-run.js`, `docs/L0_데이터계약.md`, 관련 DDL을 대조했다. 저장소 전체와 각 파일 전량을 검수한 것은 아니다.