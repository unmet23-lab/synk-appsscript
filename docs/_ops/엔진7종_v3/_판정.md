# 엔진7종 v3 — 절별 판정 장부

> 자 = `_판정자.md` 의 축 넷(지어냄 · 더할 것 · 틀림 참 · 안 재봤다). 아스트라가 낸 `절N.md` 는 «제안»이다.
> 여기 적힌 것만 실물 대조를 지난 것이고, v3 로 합칠 때 이 표를 본다. 어느 절이 끝났나는 `_진행.json` 이 안다.

| 절 | 지어냄 | 더할 것 | 틀림 참 | 안 재봤다 | 쉬운말(§0·§12 만) | 판정 |
|---|---|---|---|---|---|---|
| §7 Trail | 0 | 7 | 검증한 것 전부 | 5 | 자 아님 | ✅ 통과 (판을 연 시험 · 09-05) |
| §0 요약 | 0 | 4 | 4 참 + 1 반참 / 5 | 5 | 지적 32건 전부 긴 줄표 = 출력 형식이 시킨 것 · 낯선 낱말 0 | ✅ 통과 (09-05) |
| §1 자 | 0 | 4 | 7 참 / 7 | 5 | 자 아님 | ✅ 통과 (09-05) |
| §2 세계 눈높이 | 0 | 6 | 7 참 + 2 안 재봄 / 9 | 5 | 자 아님 | ✅ 통과 (09-05) |
| §3 한 시스템 | 0 | 6 | 5 참 + 1 반참 / 6 | 5(+합성 반례 2/2) | 자 아님 | ✅ 통과 (09-05) |
| §4 Core | 0 | 6 | 되짚은 4/8 참 · 나머지 인용 실물 | 4(+시험 95/95 재현) | 자 아님 | ✅ 통과 |
| §5 Loom | 0 | 8 | 되짚은 4/9 참 | 7(+토큰빌드 정합 재현) | 자 아님 | ✅ 통과 |
| §6 Vellum | 0 | 6 | 되짚은 6/8 참 | 6(+합성 4/4) | 자 아님 | ✅ 통과 |
| §8 Prism | 0 | 7 | 되짚은 2/10 참 | 5 | 자 아님 | ✅ 통과 |
| §9 Temper | 0 | 6 | 되짚은 4/8 참 | 5(+시험 36/36 재현) | 자 아님 | ✅ 통과 |
| §10 Reed | 0 | 6 | 되짚은 5/13 참(둘은 직접 돌려 재현) | 7 | 자 아님 | ✅ 통과 |
| §11 순서 | 0 | 7 | 되짚은 3/4 참 | 5 | 자 아님 | ✅ 통과 |
| §12 유호 결정 | 0 | 6 | 되짚은 5/11 참 | 5 | 낯선 낱말 0 · 긴 줄표만 | ✅ 통과 |
| §13 걷어냄 | 0 | 6 | 되짚은 2/6 참 | 3(+합성 2/2) | 자 아님 | ✅ 통과 |
| §14 재는 자 | 0 | 5 | 되짚은 2/6 참 | 4(+시험 37/37) | 자 아님 | ✅ 통과 |
| §15 정찰 장부 | 0 | 8 | 되짚은 3/9 참 | 6 | 자 아님 | ✅ 통과 |
| §16 대가 | 0 | 4 | 되짚은 3/6 참 | 5(+단언 2/2) | 자 아님 | ✅ 통과 |

> **17/17 통과(09-05).** 지어냄은 인용 검사기(`tools/인용검사.js --파일`)로 302건 전부 ⚪(파일·줄·식별자 실재). 「틀림 참」은 절마다 무게 있는 것만 원문·코드로 되짚었고(위 «되짚은 n/m»), 나머지는 «인용이 실물»이라는 것까지만 확인했다 — 논리까지 참이라고 적지 않는다. 더할 것은 §0·§1·§16 만 5 아래이고 그 셋은 요약·자·대가 절이라 성격이 다르다.

## §4~§16 묶음 상세 — 09-05 병렬 13절(§7 은 시험 때 끝) · 12 분 안에 전부 도착

**절마다 급소 하나씩.**
- **§4 Core** — D1(6472 「동승 6」)과 부록(6986 「c18」)이 같은 어휘를 서로 다른 판에 둔다 → 어느 쪽으로 구현해도 다른 절을 어긴다. compose_meta 완비 5 중 실제 compose_meta 는 3(점검표 17). 시트 «27종»은 이미 28(워치독 350). `portfolio_moments` 는 사건이 아니라 시트. 모델정찰 코드 1 은 출시 신호가 아니다.
- **§5 Loom** — `도착확인.js` 는 답장 화면이 아니라 «배포 도착 확인» 화면(23). 이해대장 `엔진점수()` 가 engine·kind 를 안 가려 Loom 행을 넣으면 남의 점수가 된다(326 · 합성 2/2). 이미지 모델 상수는 09-03 기록이 아니라 `gemini-3-pro-image`(이미지굽기 31). 룸조판이 원문 속 `<!--조판:-->` 을 지시로 먹는다(195). Patina 자리는 `tap_n` 여섯 칸으로 사분면을 못 낸다.
- **§6 Vellum** — 🔴 계약 이름 일곱이 실물과 다르다: `preference.stated` 의 실제 칸은 `payload.preference_dimension`(이벤트검증 71) · `intervention.delivered` 는 «서버사건»이라 앱이 못 쓴다(20) · `correction.responded='수정'` 은 재작성 완주가 아니다(답장화면 204) · `(learner_id, moment_id)` 는 하루 상한이 아니다(실물 상한은 `ub_date` 날짜식) · `reads.life` 는 객체(읽기기록 64) · `aiWeakMap_` 은 태그 목록이 아니라 메모 섞인 문자열 · 첫날 「어제」는 null(App.js 420) · 관찰·몽글·나침반·회고는 강사 화면(도착확인 127·143 · 재기 A 와 일치).
- **§8 Prism** — 24종 목록에 `오류없음` 1 이 있어 「18+6」 분할이 계약과 안 맞는다. NIKL 위치 빈도와 기회 대비 오류율은 단위가 다르다. `voiceMissionTexts_` 는 비고를 안 보고 전 행을 읽어 «옛 행 비고»로는 낱말 교체가 안 닫힌다(교재연동 247·378). 강제정렬 성공 ≠ 목표 낱말 발음. 원인 ID 일치 ≠ L1 원인 검증.
- **§9 Temper** — 「교정 건수/제출 건수」는 오류 결과변수가 아니다(`오류없음` 도 교정 행 · 교정엔진 265). 「첫 시즌 도래 0」은 산술 오류(14+30=44일 < 8주 · 결정 8주 시즌). c16 에 없는 `expected_ver`·`shadow`·세 힘 저장 위치를 실물처럼 씀. `오늘과제.js` 는 재료 7 이 아니라 12(판독기 41). `관통왕복 V9` 는 실물 이름이 아니고 스탬프 검증점은 V1(관통왕복시험 12).
- **§10 Reed** — 🔴 빈 시청자 수 `null` 이 `0` 으로 바뀌어 `이탈률({시작:17, 끝:null}) = 1`(전원 이탈)이 된다 — 직접 돌려 재현. 봇은 시청자 수를 발견 때 값으로 반복 전송(수집봇 114·388). `사건출처('listen.session')` = null 이라 값목록만 더해선 배선이 안 된다. 앞당김 회귀 부등호가 반대(5513). 세로 안전띠 «폭의 9/16» 은 틀리고 «높이의 9/16»(=폭의 81/256). 「무대 4컷 다시 안 굽는다」는 09-05 4K 재굽기 확정과 충돌.
- **§11 순서** — D23 을 「제안」으로 둔 문장(6021·6041)은 09-05 A안 확정으로 낡았다. 걸음 9·11 마감(02-25 · 03 첫 주)이 첫 실학생 데이터(02-11)보다 늦다. 첫 개입 스탬프 생산이 걸음 16 에 있어 첫 개입 값을 못 남긴다. 앱 판이 DB 보다 앞서면 `CONTRACT_VER_UNSUPPORTED · retryable:false` 로 재시도 문이 닫힌다(계약판 16 · events 213).
- **§12 유호 결정** — 「답이 없으면 권고대로 짓는다」는 정지선 안 항목엔 못 쓴다. D23·구현 시점은 이미 확정(원장). D1 별항 마감은 02-25 가 아니라 첫 실학생 02-11. D3 「안 만진 아이 = 빈 값」은 «0 관측»과 «미상»을 섞는다. D7 빛의 뜻(철학 184 「그 목표가 실린 과제」)을 바꾼다. D12 「교정 건수/제출 건수」 재등장. 자 넷 재료 「전혀 없다」는 틀림(`화용:공손성` 실재). 돈 표 분류 어긋남 · D11 음성 반출 범위 불명(교재연동 876 STT 통로 실재).
- **§13 걷어냄** — 「채택되는 날」에 현행 설명까지 지우면 안 된다(채택 ≠ 착지). 「판올림 = 커밋 즉시 라이브」 철거는 D4 ②ⓐ 채택으로 좁혀야. 돈 규칙 여섯 → 값 둘은 D13 6725·6730 을 빼먹었다. 「가이드 정본 = 기기 파일」 철거 대상에 `저장.js:178` 어댑터가 빠졌다. 처분짝들(모델정찰 504)이 일시 장애를 영구 처분으로 만든다.
- **§14 재는 자** — 이해대장 「닿는 층」은 실행 실측이 아니라 문서 문자열 집계(도달색 365). `연결률` 이 두 함수에서 다른 분모(계기판 124 vs 성과회수 319 · 같은 입력에서 2/2 vs 0/2). `동의율표()` 는 맞다·아니다만 세고 노출 분모가 없다(45). 「무응답 null」이 출력 부재와 규칙 거부를 섞는다(eval-score 99·127). 모델정찰 마지막 커밋 시각 ≠ 출처 읽기 성공 시각.
- **§15 정찰 장부** — Gemini 파일 전사는 `custom_vocabulary` 와 낱말 타임스탬프가 양립 안 하고 Live 엔 타임스탬프가 없다(공식 명세). `stt_model` 열은 이미 있다(마이그 69) · `whisper-1` 도 실제 쓴다(transcribe 44). Edge 400초 ≠ 응답 전 150초(생성상수 24). Khan +5.09 는 인지 참여(§2 와 같은 지적). 「처지 = 채택」 셀은 0/47 — 자유문장이라 기계 집계가 안 선다. 표 아래 문장 여섯이 표의 최종 처분을 되돌린다. 세로 안전띠 산식(§10 과 같은 지적).
- **§16 대가** — D23 미확정 전제(7902…) 낡음. 「첫 시즌 회고 도래 0」은 반례가 선다(성과회수 234). 계약 동일성 훅(pre-commit 128 · 계약동기화 74)은 V6 왕복을 대신 못 한다. Patina(Loom U3 = 걸음 15)를 걸음 4 에서 닫을 수 없다. 넣은 주·뺀 주 비교는 이월효과로 대조가 아니다. `sttSweep_` 는 실행 정의 0 · 주석에만 2(실물 = `voiceSweep_`·`voiceTranscribe_`).

**열일곱 절을 가로지르는 결 여덟 — v3 합칠 때 공통 규칙으로 올린다.**
1. **시제가 낡았다.** 09-04·09-05 확정(D23 A안 · 구현 최대한 늦춤 · 8주 시즌 · 무대 4K 재굽기 · 4K 굽기 통로 = 제미나이)이 v2 여러 절에 «제안·미정»으로 남아 있다(§11 §12 §13 §16 §10).
2. **as_of 하나로는 재현이 안 선다.** `ingested_as_of` 를 산문·선택·개입 스탬프·성과회수까지 승계해야 한다(§3 §4 §5 §6 §9 §10 §16 이 각자 짚었다 · 실물 학습자상태 258 은 이미 둘 다 받는다).
3. **서버사건과 앱사건의 경계.** `intervention.delivered`·`source_kind`·`policy_ver` 는 서버칸 · 앱은 못 쓴다 · 새 사건은 값목록 + `앱사건` 허용 + `사건출처()` 배선까지 한 벌(§6 §9 §10).
4. **분모와 표본 단위.** 독립 학습자 수 · 반복 관측 중복 · 미도래/표본0/고리없음 분리 · `is_test` 제외 · 노출 분모(§8 §9 §10 §14 §16).
5. **🔴 산문이 원장에 저장된다**(§3 급소) — `{{요약}}` → `generation_input_text` → 사건 봉투. 「AI 추정은 시효 캐시뿐」과 c12 계약이 부딪힌다. D1 자리에서 먼저 푼다.
6. **실물 이름 오기 열둘.** actor_kind `machine`(실물 learner·ai·teacher) · `sttSweep_` · `도착확인.js` 뜻 · 시트 27→28 · `portfolio_moments` · V9→V1 · 오늘과제 재료 12 · `preference_dimension` · `reads.life` 객체 · `nano-banana` 상수 · `aiWeakMap_` 성격 · 재료 파일 `세계1등.json`·정찰 JSON 9벌 부재.
7. **산술·논리 오류 넷.** 세로 안전띠 9/16(§10 §15) · 첫 시즌 도래 0(§9 §16) · 앞당김 부등호(§10) · 필요 사건 종 비율(§5).
8. **아직 코드 0 인 이름을 실물처럼 쓴 자리.** `lib/접점예산.js`·`발화검문.js`·`요약채점기.js`·`관찰선택자.js`·`처방정책.js`·`이름살균.js`·`룸눈.js`·`곡시계.js` 등 — 재기 A 의 「설계만 있고 코드 0」과 같은 목록. v3 는 «짓는다»로 시제를 못박는다.

**합치기 전에 유호님 자리에 걸리는 것.** 없음 — 여덟 결은 설계자가 정리할 일이고, 돈·모델 픽·소급 불가·확정 충돌은 이미 원장에 있는 확정을 따르면 된다. 단 하나, **TOPIK 기출 «탑재»**(09-05 확정)와 8월 기각(신선함)의 충돌은 그 차례에 되묻는다.

## §3 상세 — 09-05 · 9.3분 · 13,867자 (원문 39,265자)

**대조한 실물 — 전부 맞았다.** 형제(SYNK-talk) 19곳: `supabase/functions/deliver/생성모드.ts:117`(as_of·ingested_as_of 둘 다 넘김)·`:154`(event_draft) · `deliver-one/index.ts:408`(요약은 draft 에서 읽기만) · `migrations/20260831130000_classroom_loop_c14.sql:378`(learner_self_events 가 observation.noted 제외) · `src/사건통로.js:106`(idempotency_key 승계) · `lib/성과회수.js:154·164·230·241` · `lib/과제생성.js:198`({{요약}}) · `lib/착지봉투.js:30`(generation_input_text) · `migrations/20260821120000_generation_c12.sql:912`(request_body 동일 강제) · `migrations/20260806150000_engine_c6.sql:379`(enum learner·ai·teacher) · `lib/사건출처.js:11·64` · `lib/이벤트검증.js:340` · `lib/학습자상태.js:311·313·933·944` · `migrations/20260812170000_season_review_c11.sql:40·159` · `src/회고화면.js:15`. 이쪽 6곳: `docs/엔진7종_상향설계_v2.md:837`(actor_kind∈{machine,teacher})·`:1013`(잔존 원리상 0)·`:1029`(학생 답이 굳은 뒤 강사 칸)·`:1082`(알렸나/안 알렸나) · `계약/수집_교정_계약.json:3·101` · `tools/모델정찰.js:499`(장부적기).

**틀림 지적 여섯 — 다섯 참 · 하나 반참.**
- ① 🔴 **급소.** 「파생 캐시로 옮기면 옛 요약 잔존 문제도 원리상 0」(1013)은 틀렸다 — 지금 생성 통로는 요약 «전문»을 `{{요약}}` 으로 요청 본문에 넣고(과제생성 198), 그 본문을 `generation_input_text` 로 사건 봉투에 담으며(착지봉투 30), c12 계약이 그것이 `request_body` 와 같기를 강제한다(sql 912). 즉 산문이 원장에 저장된다. → v3 는 «AI 추정 저장 금칙»과 «요청 본문 보존 계약»의 충돌을 D1 자리에서 먼저 푼다. 참.
- ② `actor_kind∈{machine,teacher}`(837) — 실물 enum 은 `learner·ai·teacher`. `machine` 은 없는 값. 참.
- ③ §3-3 ⓪ 에서 `goal.responded`·`observation.noted` 를 [새]로 적었는데 둘 다 c14 에 이미 있고(계약 101 · 소비자 학습자상태 311·313), 원장에 `(c17)` 을 붙여 현행(c16)과 목표(c17)를 섞었다. 참.
- ④ §3-6 세 힘 태그를 «observed»로 옮기면 출처가 바뀐다 — 코드는 확정 관찰을 언제나 `teacher` 로 둔다(사건출처 64 · 학습자상태 933 표본제외). 코드 근거는 참인데 v2 1071~1072 에 «(observed)» 표기가 그대로 있는지는 못 찾았다(반참).
- ⑤ 「학생 답이 굳은 뒤 강사 칸」(1029)은 기존 규칙을 «학생 응답 필수»로 강화한 것 — c11 은 `verdict_by_self` null 허용(강사 판정 진행 · 40), 차단 대상은 «강사 판정 뒤 학생 답 변경»(159), 화면에 「학생 없이 진행」(회고화면 15). 참.
- ⑥ 「알렸나/안 알렸나가 이미 사실로 남는다」(1082)는 개입 도장을 학생의 «알림 행동»으로 읽은 것 — 스탬프 목록에 학생 인지·보고 계약이 없고, 관찰축은 area·tags 빈도 집계다(944). 참.

**더할 것 여섯.** ①판정 전체에 as_of + ingested_as_of 를 승계(늦적재 경계 · 생성모드는 이미 둘 다 넘긴다) ②빼기 = 캐시 재생성만이 아니라 «미발송 산출물 무효화»까지(event_draft 에 굳은 요약이 제외 전 글을 전달할 수 있다 · 관통왕복 반례로) ③학생판/강사판 경계를 «생성 입력»부터 검사(참조 목록만으로는 부족) ④접점 예산에 동시 실행·재시도 소비 규칙(idempotency_key 와 연결) ⑤30일 짝에서 «전달 확인»과 «효과 귀속»을 따로(분모 넷: 전체 / 관측 통로 있음 / 노출 확인 / 창 도래) ⑥재심 완료 눈금 = 완료 재채점 / 실행 대상.

**내가 이번에 안 잰 것.** 아스트라의 합성 반례 2/2(늦적재 입력 차이 · 요청 본문 봉투 잔존) 재현 · v2 1072 의 observed 표기 · 아스트라 한도.

## §2 상세 — 09-05 · 6.8분 · 14,666자

**대조한 실물 — 전부 맞았다.** `docs/엔진7종_상향설계_v2.md` 줄 27곳(467·471·474·476·480·493·508·515·521·522·524·535·554·561·574·580·584·587·588·612·614·776·850·1462·1465·7327·7333) · `계약/수집_교정_계약.json` 줄 9곳(11·59·78·89·101·188·190·196·207) + 필드 18개(retry_of_event_id · skill_ids · task_ref · task_snapshot · task_schema_ver · correction.viewed · content.viewed · l1_source_phrase · body_original · corrected_text · transcript_verified · 픽스처_항목필드 · exam.result · exam_kind · exam_date · exam_level · exam_round · section_scores) 전부 실재 · `엔진_궤적.js:14·17·28·373`. 🔴 **`세계1등.json` 은 메인 트리에 없다** — 아스트라가 「못 찾았다」고 정직하게 적었고 맞다. 작업 폴더 삭제(3f3b3fc03) 때 같이 갔고 워크트리 둘에만 남아 있다 → v3 에서 §2 의 재료 위치를 git 이력으로 가리키거나 되살린다.
**바깥 수치 둘을 원문으로 확인.** Khan 블로그: 최근 풀이 이력 +3.4%(608,000 스레드) · 선행기술 +2.7%(1.36M) · 평문+24h = **+5.09% 는 인지 참여**(정답률 아님) · 출시 조건 이길 확률 >.95 · 다음 문항 = 같은 skill·같은 세션·Khanmigo 도움 없음. LearnLM 논문: 무수정 승인 **74.4%**(2,691/3,617 · 원문의 76.4% 는 틀림) · 165명·5개교 · 66.2 vs 60.7 · 부록 D.1 이 활동·오개념·예측 성과를 «호출마다» 넣었다고 명시 · 부록 H 는 처리량을 «시뮬레이션»으로.

**틀림 지적 아홉 — 일곱 참 · 둘 안 재봄.**
- ① 표본 부재 ≠ 지표·계산기까지 못 만듦 · 요약 Brier(7327) ≠ KT 타당성 → 참.
- ② Khan 수치를 합쳤다(+6.1 = 3.4+2.7 · 5.09 는 다른 지표) → **참**(§15 1465 는 바르게 갈라 적었는데 §2 가 뭉쳤다).
- ③ Khanmigo RCT 0.06~0.08 SD 가 «AI 자체 효과»가 아니다(18개교 · AI 없는 Khan 과 유사) → 안 재봤다(논문 원문 미확인 · 방향은 그럴듯).
- ④ 「우리만 = 한 것 · 사람 강사」 → **참**(Khan 도 실제 풀이 이력을 쓴다 · 원문 확인). 한국어 발음 평가 공백의 반례(ETRI KSI) → 안 재봤다.
- ⑤ LearnLM 76.4% → 74.4% · 전이 +5.5pp 의 불확실성 · 상태 동봉은 그들도 한다 → **참**(원문 확인).
- ⑥ Alpha 「선발 효과가 모델보다 컸다」는 분해 없는 단정 → 참(낮춰 적는다).
- ⑦ MAE ≠ 캘리브레이션 · 「공식 채점 API 없음 → 아무도 못 갖는다」는 비약 → 참.
- ⑧ Atlas 「지우기」= 캐시 재생성·원천 삭제 0(776) + atlas.excluded(850) 로 좁혀야 → 참.
- ⑨ 「시한 = 2027 1기 졸업 전」 ↔ 궤적.js 가 2027 1기를 근거 없는 날짜로 정정(첫 졸업 2028-02) → 참.

**더할 것 여섯 — 첫 절로 시험선(5)을 넘었다.** ①벤더 주장마다 «SYNK 로 옮겨도 성립하는 조건»(대상·개입·대조·결과변수·분모) ②전이 지표를 실제 사건(retry_of_event_id · 새 task_ref 첫 시도 · 도움 노출 부재)에 묶는 판정 규격 ③Atlas 가치 = «같은 근거·다른 형식» 대조 + 근거 없는 주장 수 ④몽골어 원인층(l1_source_phrase)을 반증 가능한 가설로 ⑤강사 코파일럿 비교 단위 = 완료 개입당 사람 시간 ⑥졸업 뒤 성과에 재관측률(재관측 수/도래 수)과 미관측·진행중·좌절 구분.

**내가 이번에 안 잰 것.** Oreopoulos 논문 · ETRI 논문 · 그 밖의 벤더 수치 전량 · 아스트라 한도 소모.

## §1 상세 — 09-05 · 9.5분 · 11,508자

**대조한 실물 — 전부 맞았다.** 두 저장소 줄 인용 22곳. 이쪽: `docs/SYNK_철학.md:48·92·94·141` · `docs/엔진7종_상향설계_v2.md:355·374·395·857·913·6696` · `docs/_ops/결정.md`(09-04 「AI 판정 초안 → 강사 도장」 · 09-02 「비교 금지 = 학생에게 보이는 것」 — 그 뒤 내가 넷을 더해 줄이 +4 밀렸다) · `tools/lib/시트도달.js:207·228`(거짓 지목 차단 · 래칫은 총수 비교) · `엔진_셋업확장.js:408`(mastery_log 의 '도달' 소비자 둘 살아 있고 '연습' 만 걷힘) · `tools/모델정찰.js:745·783` · `docs/개원전_일괄판정_큐.md:26`(첫 울림 09-01). 형제(SYNK-talk): `lib/과제요약.js:87·115`(axes_used → 쓸축수) · `lib/성과회수.js:227`({이전, 이후} · 뺄셈 칸 없음) · `evals/픽스처.json:5`(출처 = 합성 · 102 = 오류 62 + 정상 40) · `lib/학습자상태.js:258·275`(ingested_as_of 절단) · `tests/학습자상태.test.js:60`. 아스트라가 «메모리 안에서» 시트도달 변조 시험(미도달 5/28 유지 · 검사() 1/1 통과)을 했다는 것은 `검사()` 가 원문을 인자로 받는 구조라 가능한 일이지만 내가 되돌려 재보진 않았다.

**틀림 지적 일곱 — 전부 참.**
- ① 게이트 ①의 「도움 = 여섯 축 · 신선 = 정찰 게이트」는 철학 정본 정의(실사용 도움 = 학생이 실제로 말하거나 쓰고 그 산출이 엔진에 닿는가)를 종류 변화·도구 채택으로 바꿔치기한 것. 적용 대상 가름(접점 ①②③④ / 배관 ③④)도 승계해야 한다.
- ② 판정문 「여섯 중 하나면 종류 변화」와 §1-2 끝 「안 커지면 종류 변화 아님」이 서로 다른 판정. (f) 「도구가 잰다」는 ⑥ 의 종점(래칫 · 퇴행 탐지)보다 약하다. → 모델 동반 성장(a)과 b~f 를 따로 판정.
- ③ 「우리 학생이 실제로 틀린 문제로 만든 시험지 102」는 틀렸다 — 픽스처 출처가 「합성 · 실학생 문장이 아니다」. §0-보 는 「지금은 합성 102」로 바르게 적었는데 §1-2 가 어긋났다.
- ④ 「미래정보 누출 → as_of 절단」만으로는 부족 — 실제 코드는 `ingested_at` 도 자른다(늦적재 경계). 요약이 코드보다 뒤처졌다.
- ⑤ ㉢ 비교 금지를 «모든 수치 금지»로 넓혔고, 철학 정본의 «학생·학부모가 보는» 범위에서 학부모를 뺐다. 숫자 0 은 별도 규약(§0 「보는 숫자 0」)으로 따로 지목.
- ⑥ 09-04 확정 「AI 가 판정 초안 → 강사가 읽고 고쳐 도장」이 §1-3 「관찰 = 시스템 · 판정 = 사람」에 안 실렸다. 실시간 자동 «최종» 판정 금지는 그대로.
- ⑦ 「재미 = 다음 날 재방문율」은 반복 사용의 관측값일 뿐 — 의무 숙제도 재방문을 만들고, 시즌 회고처럼 다음 날 올 이유가 없는 접점도 있다.

**더할 것 넷 — 합칠 때 받을 만하다.** ①축 표기마다 «틀리면 실패하는 대조»(자산 있음/없음 × 모델 구/신) ②래칫이 총수만이 아니라 «기존에 닿던 통로»를 지킨다 ③「재심 대상이 생겼다」와 「재심을 끝냈다」를 따로 센다(완료/필요 · 대기 시간) ④③축 종점에 「도움 뒤 혼자 해냈는가」(D12 ⓓ 와 연결). 더할 것이 4 로 두 절 연속 시험선(5) 아래인데, §0·§1 은 요약·자 절이라 본문 절(§4~§10)과 성격이 다르다 — §4 에서 다시 본다.

**내가 이번에 안 잰 것.** 시트도달 변조 시험 재현 · 아스트라 한도 소모(9.5분 한 발).

## §0 상세 — 09-05 · 6.0분 · 8,621자

**대조한 실물 — 전부 맞았다.** 줄 인용 8곳: `docs/SYNK_철학.md:141`(도움 뒤 혼자 해내는가) · `docs/엔진7종_상향설계_v2.md:1579`(고침·반영 제외) · `:1403`(집중띠 = 동반 사건 시간만) · `:884`(되묻기 2 + 별도 20초 문항 + ai_assist 예산 밖) · `:170`(Supabase 완료 문장) · `엔진_콘텐츠AI.js:2950`(`aiWeakMap_` 정의) · `:3317`(`aiCall_` 로 「같은 문법의 새 문제」) · `docs/_ops/결정.md:116`(구현 최대한 늦춤). 파일 6 + 형제 저장소 `SYNK-talk/lib/학습자상태.js` 전부 있다.

**틀림 지적 다섯의 판정.**
- ⓐ 「시트층 재출제는 AI 없이 시트 규칙」 → **참.** `aiWeakMap_` 은 약점만 모으고, 문제는 `aiCall_` 이 만든다. v3 에서 §0-1 머리와 §0-보 「시트층 재출제」를 「시트에 모인 약점으로 AI 가 다시 낸다」로 고친다. 앱 초급 숙제 생성이 꺼진 것과는 다른 통로다.
- ⓑ 「집중띠 = 실제로 앉아 있던 시간대」 → **참.** 본문은 제출·퀴즈·미니게임이 같이 있는 시간만 집중띠로 치고, 순수 청취는 후보로 격리한다. §0-2 Reed 줄을 낮춘다.
- ⓒ 「묻힘은 하루 2장·20초」 → **반참.** 원문은 «두 장 + 20초 문항» 나열로도 읽히지만, 합친 시간을 잰 적이 없는 것은 사실이다. 아스트라 문장(「되묻기는 최대 두 장 · 합친 시간은 안 재봤다」)이 더 안전하다.
- ⓓ 「검증기·이해대장 도구 절, 이건 제가 지금 합니다」 → **참.** 🔴 v2 는 09-04 결정(구현 최대한 늦춤) «전»에 쓰였다. v3 §0-3 걸음 0 은 «읽고 확인하는 일»만 남기고, 짓는 일은 착수 시점이 정해진 뒤로 뺀다.
- ⓔ 「Supabase 만료 미루기 닫혔다 · 자 = `.env` 되읽음」 → **참(근거 부족).** `트랙.md:463` 도 같은 근거뿐이다. 서비스 쪽 만료일은 안 재봤다 → 합칠 때 「닫혔다」를 「작업 파일만 미뤘다 · 서비스 쪽은 안 재봤다」로 낮추거나, 서비스에서 확인한 뒤 되돌린다.

**더할 것 넷 — 합칠 때 받을 만하다(확정은 합치는 자리에서).**
1. 「혼자 해내게 됐는가」를 성과 축으로 — 철학 v1.14 눈금 후보와 같은 축 · D12 로 잇는다 · §0-2 표 앞.
2. 잘못 이해를 고친 «다음 날» 장면 — §0-1 20:40 뒤.
3. 강사가 새로 맡는 일과 덜 하게 되는 일을 나란히 — §0-1 하루 끝 · 숫자는 측정 뒤.
4. 장면마다 «준비 · 켜짐 확인 · 막힌 날 학생이 받는 것» — §0-3 첫 걸음 앞.

**내가 이번에 안 잰 것.** 서비스 쪽 Supabase 만료일 · 아스트라 한도 소모량(6분 한 발).

**경위.** §0 은 두 번 띄워졌다. 다른 세션이 08:47 에 먼저 띄운 것을 유호님 지시로 08:51 에 끊고(4.5분치 버림) 이 세션이 다시 띄웠다. 결과 파일은 이 세션 것 하나다.
