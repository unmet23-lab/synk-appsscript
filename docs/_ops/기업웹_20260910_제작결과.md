# SYNK 기업 홈페이지 제작 결과 · 2026-09-10

## 현행 결과 · 다섯 번째 시안: SYNK는 펠트, 사업명에만 실땀

사용자 실제 말: “synk를 빼고 나머지 shift,lab,pulse에만 스티치를 넣는거지”, 이어서 “웅 전부 반영해줘”. SYNK의 실땀을 덜고 기존 펠트 몸체·두께·그늘·y/k 형태를 유지하며, LAB·SHIFT·PULSE는 기존 색과 실땀을 유지하는 범위로 적용했다.

- **공개·비공개 반영 완료:** https://synk.im/ 및 https://synk-conversations.unmet23.chatgpt.site . 공개 사이트의 변경 파일 9개는 HTTP 200이고 검증본과 내용이 모두 일치한다. 실제 공개 브랜드 소개 화면에서 새 4개 조합과 설명을 확인했고, 비공개 사이트도 기존 로그인 후 같은 현행 설명을 확인했다.
- 원본 `홍보물/마케팅실행_20260909/브랜드킷`의 Ink/Paper 2종, 단독·LAB·SHIFT·PULSE 배치용 8조합과 배경확인 8종, 정밀 SVG 2종, 공유 WebP 2종, 사용규칙·브랜드잠금·킷 안내·배치명세를 갱신했다. `DESIGN.md`와 `로고_중립색_정본_v1.md`의 현행 기준도 맞췄다.
- 내장 ImageGen으로 실땀 없는 펠트 표면을 만들고 원래 투명도와 글자 윤곽에 맞춰 조립했다. 생성 응답은 격자가 들어간 RGB였으므로 그대로 배포하지 않았다. 선택 원본·정확한 프롬프트·복원 방식·해시는 킷 `실땀수정/`에 보존했고 `실땀역할수정.cjs`로 재현한다. 3840px는 출력 캔버스이며 생성 원해상도라고 표시하지 않는다.
- 독립 대조: Git `6b3dffab88dc0a868e1b4794b76d50da0ceb8beb` 대비 원본 2종과 배치 8종의 캔버스·알파 차이 0픽셀. 사업명 6종 영역의 RGBA 차이 0픽셀. SYNK 내부 표면은 새로 편집된 재질이며 전체 픽셀 보존을 주장하지 않는다. 배경확인 8종에서 실땀 잔존·격자·눈에 띄는 외곽 잔상 없음, 사업명 스티치 보존을 직접 확인했다.
- `tools/lib/로고정본.js`는 외부 SYNK 워드마크의 실땀 층만 제외했다. 20개 해당 변형의 변경은 실땀 제거에 한정되고 범위 밖 101개 출력은 이전과 같다. 관련 시험 4개 통과. 배너 생성기는 옛 코랄 k 원본 대신 현행 Ink/Paper PNG를 읽도록 갱신하고 구문검사를 통과했다.
- 킷 조립기의 `--synk-only` 경로는 옛 전체 자산 재복사를 피하며 8개 조합과 직접 파생 자산만 갱신한다. 현재 영상 제작용 로컬 공유 WebP 사본도 갱신했다. 기존에 구운 영상·PDF·카드·ZIP과 과거 게시물은 재제작·재게시하지 않았으며 현재 원본과 웹 반영을 그 과거 산출물까지 확대한 것으로 표시하지 않는다.
- 홈페이지 헤더·답변·사업 소개·브랜드 사례·푸터의 승인 자산 5개와 파비콘을 갱신하고 화면·공개 답변의 실땀 설명을 동기화했다. 로고·스크립트·공개 지식 요청에 버전 표식을 적용했다. 관련 답변 시험 29개 통과, 1440px·390px 실제 화면과 모바일 가로 넘침·이미지 누락 없음 확인.
- 제작 소스 `SYNK-website` 커밋 `00feb70bc1ab2fcb4f988aeb7258b696f3106875` 원격 반영 완료. Sites 버전 5, 배포 `appgdep_6aa277ded51081919f00ca0e8bda65bd` 상태 `succeeded`. 공개 `synk-policy` 커밋 `7e8ab090b79ef64ca016233d7927ad8466647823` 원격 반영 및 GitHub Pages 상태 `built`. 기존 접근 범위와 하위 페이지는 유지했다.
- 사용 기준의 현행 해시는 홈페이지 `qa/redesign-source-manifest.json`에 갱신했다. 작업 중 `마케팅_정본.md`에 추가된 영상 컷 호흡 규칙은 이번 정적 홈페이지 내용에 영향을 주지 않는 변경으로 대조했다. 남은 회사 안내 언어판·LAB 신청 폼·실제 고객 피드백은 아래 네 번째 시안의 후속 범위와 같다.

## 네 번째 시안: 회사 철학과 SHIFT 최신 방향 리디자인

사용자 요청: 변경된 SHIFT 방향·제공물과 각 회사가 추구하는 철학을 반영해 `synk.im` 홈페이지를 전체 검수하고 리디자인. 이어서 완성하라는 요청까지 같은 작업으로 수행했다.

- **공개 반영 완료:** https://synk.im/ . GitHub Pages의 기존 `main` 루트에 기업 홈페이지를 반영했고 배포 상태 `built`, 공개 화면 및 SHIFT 질문 답변을 확인했다. 비공개 검토 주소 https://synk-conversations.unmet23.chatgpt.site 에도 같은 화면이 게시돼 있다.
- 구조: 질문 중심 입구 → 공통 철학 → LAB 교육 → SHIFT 실제 회사 제작 과정과 방법 → PULSE 음악·이야기 → 자체 브랜드 디자인과 선택 이유 → 실제 이메일 문의. 이전 세 언어 LAB 소개를 한국어 기업 소개로 교체한 범위를 명시한다.
- SHIFT는 유호의 실제 AI 회사 제작 문제·시도·실패·수정·사람의 선택·결과·제공 가치를 중심으로 설명한다. 그 안의 방법을 강의와 적용 지원으로 발전시키며, 유학·기관·강사·제작·자격 사업은 준비 범위로 구분했다. 미확정 일정·가격·고객 성과·협약을 만들지 않았다.
- 승인 사진형 로고 5개·마스코트·색·서체를 현행 명세와 재대조했다. 제목과 본문의 읽는 순서, 섹션 간 밀도, 모바일 공책과 문구 겹침·한글 오브젝트 잘림·마스코트 접지 그림자를 수정했다. 마스코트 원본·털·표정은 변경하지 않았다.
- 사용 기준: 현행 `DESIGN.md`, `docs/SYNK_철학.md`, `docs/마케팅_정본.md`, `docs/마케팅/SHIFT.md`, `docs/마케팅/SHIFT_공개수업.md` 등 원문 9개의 SHA256을 `C:/Users/q1212/Documents/SYNK-website/qa/redesign-source-manifest.json`에 기록하고 게시 직전 모두 일치함을 확인했다. 회사 운영 원칙 작업파일 SHA256은 `f16234c9e1dcc5818e7023acf3a9c4b70cdb9e6934ea49abee8cb5ec72d85c7f`다.
- 독립 구조안 2개를 병합하고 사실·과장 검토에서 나온 구 SHIFT 안내/답변, 사업 범위 미응답, 성인 안내 불일치를 수정했다. 공개 문서 6개·답변 27개를 동기화했고 화면 추천 질문 12개와 답변 근거를 포함한 관련 시험 **29개 통과**.
- 실제 브라우저에서 1440·1024·768·390·320px를 검수했다. 가로 넘침·깨진 내부 링크·누락 이미지 0. 문서 열기·Escape 닫기와 초점 복귀·복사·새 대화·키보드 전송·미확인 질문 거절·지식 로드 실패 후 복구·움직임 감소 환경을 확인했다. 상세 증거는 소스 `qa/RESULTS.md`에 있다.
- 제작 소스: `C:/Users/q1212/Documents/SYNK-website`, 검증 커밋 `6514f58918ab6530a2cc9eab4a611527fb6be32d`. Sites 비공개 버전 4 배포 `appgdep_6aa26c4951748191bf7a8f2e042891b4` 성공 및 실제 게시 화면 확인.
- 회사 도메인 소스: `C:/Users/q1212/Documents/synk-policy`. 분리 작업본 `C:/Users/q1212/Documents/SYNK-home-release`에서 준비한 `bc2e7042f09694027c38e7458de1cf004f7b8f49`를 기존 GitHub Pages 원격 `main`에 반영하고 원래 작업본도 같은 커밋으로 맞췄다. `CNAME`, `/privacy/`, `/name/`와 가족 페이지·영상, `/diag/`, `/retro/`, `/preview2/` 및 기존 도구 자산을 보존했다.
- 공개 배포 뒤 `synk.im`의 기업홈 파일 25개와 기존 하위 HTML 6개, 합계 **31개 응답이 HTTP 200이며 검증본과 내용 일치**함을 확인했다. 텍스트는 줄바꿈 차이를 정규화했고 바이너리는 SHA256으로 대조했다. 기존 도구의 학생별 인증·데이터 쓰기 흐름을 다시 실행했다는 뜻은 아니다.
- 남은 범위: 현재 기업 홈은 한국어다. 회사 안내의 다른 언어판, LAB 신청 폼, 실제 방문자 피드백·교육/지원의 확정 일정·고객 효과는 별도 후속 작업이다. 문의는 기존 `hello@synk.im` 이메일 작성 링크이며 실제 메시지는 발송하지 않았다. 생성형 AI 연결이나 신청·결제를 구현한 것으로 표시하지 않는다.
- 기록 보존 상태: 홈페이지 두 저장소의 소스 커밋·원격 반영·배포는 완료했다. 본 결과와 트랙의 문서 수정은 작업파일에 저장했으며, SYNK-appsscript의 기존 Git 인덱스 잠금과 다른 Git 작업이 확인돼 이번 문서 커밋은 보류했다. 잠금·다른 작업의 스테이징은 변경하지 않았다. 로컬 확인 서버와 임시 배포 압축은 정리했다.

## 최초 제작 기록

- 비공개 게시 성공: https://synk-conversations.unmet23.chatgpt.site
- 사용자 요청: 챗봇을 메인으로 두고 스크롤 아래에 간결한 사업 탐색을 배치. 기존 홈페이지 기록·시안을 디자인 근거로 재사용하지 않고 현재 회사 자산을 최대한 활용.
- 구현 위치: `C:/Users/q1212/Documents/SYNK-website`. 별도 저장소이며 원격 Sites 소스 main에 `d452fa07e4ac679627e4b7e0ecc9b44a5e3e4d8d`를 push했다.
- 첫 화면 질문, 공개 문서 근거 보기, 후속 질문, LAB·SHIFT·PULSE 이동, 대화 초기화와 모바일 화면을 구현했다.
- 배포 응답의 `succeeded`를 확인했다. Codex 브라우저 열기는 `queued` 반환으로 실제 탭 표시 여부는 확인 불가.

## 실제 사용한 근거

- 자산: [자산 명세](기업웹_20260910_자산/asset-manifest.json), [자산 실물](기업웹_20260910_자산/contact-sheet.png).
- 회사 사실: [공개 지식 근거](기업웹_20260910_지식/source-notes.md). 이 초안의 24개 레코드에 구현에 맞는 사용 안내·마스코트 문항을 더해 최종 공개 답변은 26개다.
- 현행 `DESIGN.md`와 디자인 토큰, 공식 SYNK 로고 생성기, 승인된 마스코트·사물 자산을 사용했다. Paper/Ink를 중심으로 사업별 색상을 사용했다.
- 지식 조사 기준 HEAD는 `b031ed4ef460a09591ee61335e6415585506057e`. 관련 원문의 내용 지문은 위 근거 파일에 기록했다. 이후 저장소의 다른 작업은 본 구현에 섞지 않았다.
- 기존 홈페이지 설계·과거 승인/기각 디자인은 이번 사용자의 명시 요청에 따라 참고하지 않았다. 이번 결과에 대한 사용자 만족·승인을 추정하지 않는다.

## 검증과 범위

- 구문 및 답변 계약 시험 25개 통과. 독립 검토에서 발견한 전체 회사 질문, 사업 전환과 후속 질문, 없는 세부 정보 오응답을 수정했다.
- PC 1440×1000, 모바일 390×844와 320px 대화 화면을 실제 렌더로 확인. 질문·근거 문서·Escape 닫기·새 대화·내부 링크 확인. 브라우저 오류·리소스 누락·가로 넘침 0.
- 화면과 시험 증거는 별도 소스의 `qa/RESULTS.md`, `qa/*.png`, `qa/knowledge.test.mjs`에 있다.
- 현재 챗봇은 공개 문서를 찾아 정해진 문단을 보여주는 검색형이다. 생성형 AI 연결·모델 학습·자유로운 문서 추론을 구현했다고 표시하지 않는다. 문서 밖 질문에는 확인 필요를 안내한다.
- 기존 `synk.im` 도메인 교체, 공개 접근 전환, 문의 전송, 신청·결제, 생성형 AI 비용 발생 작업은 하지 않았다. 최종 공개 지식 확정과 실제 방문자 반응은 남아 있다.
- 게시 후 생성한 임시 배포 파일은 정리했다. 로컬 C 드라이브 여유 공간 0으로 브라우저 자동화 연결이 실패했지만 원격 게시 성공은 별도로 확인했다.

## 두 번째 시안 · 마스코트 접지와 정보 밀도 보강

사용자 실제 말: “생각보다 상당히 괜찮네”, “몽글이랑 까몽이는 약간 떠보여”, “폰트가 약간씩 심심”, “전체적으로 조금 비어있는 느낌”, “조금 더 알차게 어필”. 전체 디자인이나 세부가 모두 승인됐다고 해석하지 않았다.

관찰·판단: 기존 화면에서 몽글 밑단은 잘리고 까몽은 기울어져 공중에 놓였다. 세 파일은 투명 배경이며 흰 사각 배경 문제가 아니다. 까몽의 꼬리 끝과 몸 밑단을 구분해 그림자 위치를 맞췄다. 정보 부족은 사업 대상과 구체적인 경험이 추상적인 소개에 가려진 부분에서 보완했다.

- 마스코트 원본 파일·털·색은 유지. 몸 전체가 보이는 크기와 위치, 부드러운 공통 바닥 음영과 개별 접지 그림자를 적용했다.
- 기존 서체의 굵기·크기·행간·강조로 제목과 질문창 위계를 보강했다.
- 사업별 대상·준비하는 경험·준비 중인 작업과 직접 이동 링크를 추가했다. 회사의 작업 기준을 세 항목으로 설명했다.
- 1440·1024·768·390·320px 렌더에서 가로 넘침 0. 변경 후 답변 계약 시험 25개 통과, 대화·문서·초기화·앵커 동작과 이미지 누락·브라우저 오류 없음 확인.
- 자산 배치와 문안은 독립 검토를 받았다. 최종 시각 판단은 현재 첫 화면·모바일 사업 화면을 직접 비교했고 사용자 만족은 아직 확인하지 않았다.
- 검증 소스 `be13ed75c30ba9be87f835491a1630c96dfd0f05`로 비공개 버전 2 게시 성공. 같은 주소를 새로고침해 변경 문구가 표시되는 것을 실제 브라우저에서 확인했다. 공개 접근·생성형 AI 연결은 변경하지 않았다.
- 적용 범위: 이번 홈페이지의 구성과 이미지 배치. 다른 마스코트 자산·다른 산출물의 전면 교정으로 확대하지 않는다. 다음 피드백이 오면 해당 화면과 목적에서 다시 판단한다.

## 세 번째 시안 · 승인 로고 전체 적용과 까꿍 구성

사용자 실제 말: “몽글이 가운데있고 그 옆에 까몽이랑 마린이 까꿍 하는 느낌”, “왜 synk 마크를 바꾼버전을 안쓰고 예전버전을 쓰는거야”, “전부 최신화해주고 명품화 브랜딩 기법 제대로 적용된거 맞아”. 이번 대화에서 직접 받은 피드백이며, 수정 결과의 만족이나 승인은 아직 확인하지 않았다.

관찰·원인: 색상 토큰 48개와 서체는 현행 기준에 맞았지만, 홈페이지의 로고는 평면 SVG였고 사업명은 일반 활자로 조립했다. 승인된 펠트·스티치 사진형 완성 조합을 인수하고 화면에서 대조하는 작업이 빠졌다. 최신 자료가 존재한다는 사실이 홈페이지 자동 동기화를 뜻하지 않는다.

- 현행 원본: `docs/홍보물/마케팅실행_20260909/브랜드킷/배치용`의 SYNK Ink/Paper, LAB·SHIFT·PULSE Ink 완성 조합 5개. 원본 명세의 SHA256과 5개 파일이 일치함을 직접·독립 검토로 확인했다. 회사 운영 원칙 작업파일 SHA256은 `e062c7964da828ddc43f0e61a85864f668e2ec2539fb5283f4c451bcea6b5868`이었다.
- 홈페이지 헤더·사업 세 곳·답변 표시·브랜드 사례·푸터에 사진형 완성 조합을 사용했다. 파비콘은 같은 현행 킷의 정밀 SVG다. 이전 웹용 SVG 2개와 참조를 제거했다. `C:/Users/q1212/Documents/SYNK-website/sync-brand-assets.mjs`는 명세와 원본을 대조해 비율 축소·WebP 인코딩만 수행하며, 출처·원본·산출물 해시는 `qa/brand-assets-manifest.json`에 남는다.
- 몽글을 중앙 전경에 놓고 까몽·마린을 양옆 뒤로 겹쳤다. 기울기·크기·바닥 음영을 조정했으며 승인된 마스코트 파일·털·몸·표정은 바꾸지 않았다.
- 실제 SYNK 브랜드 조합과 먹색 중심·사업별 색·공통 재질을 선택한 이유를 보여주는 소개를 더했다. 고객 성과나 손으로 제작한 실물이라는 주장으로 확대하지 않는다. 브랜드 일관성·읽는 순서·실제 결과와 선택 이유를 적용했으며, 모든 명품 브랜딩 기법의 완성을 선언하지 않는다.
- 구문·답변 계약 시험 25개 통과. 1440px PC, 390px 모바일 실물과 320px 가로 넘침, 대화·근거·초기화·앵커를 확인했다. 오류·이미지 누락·가로 넘침 0. 원본과 화면 자산 및 브랜드 서사는 두 독립 검토 결과를 확인해 통합했다.
- 검증 소스 `c925d73be8ca1446afd857b8cbe753c937f2bc5f`를 Sites 소스 main에 push하고 비공개 버전 3 게시 성공을 확인했다. 실제 게시 탭 새로고침 후 SYNK LAB·SHIFT·PULSE 완성 이미지와 “세 개의 이름에, 하나의 결을.” 내용이 표시되는 것을 확인했다. URL은 기존과 같다.
- 로컬 저장 공간 0으로 배포 압축 생성이 실패해, Sites의 원격 소스 빌드로 게시했다. 검증 뒤 중복 화면 이미지 일부를 정리했으며 로컬 확인 서버도 종료했다.
- 기록 시 `docs/_ops/트랙.md`가 이미 0바이트인 미커밋 상태였다. 다른 작업 내용을 복원하거나 덮어쓰지 않기 위해 이 파일에는 쓰지 않았고, 이번 완료 증거는 본 결과 문서에만 갱신한다.
- 적용 범위는 현재 기업 홈페이지다. 다른 회사 산출물의 전면 최신화, 공개 접근 전환, 회사 도메인 변경, 생성형 AI 연결은 포함하지 않는다. 공개 안내 6개·답변 26개를 검색하는 기존 기능을 유지한다.

## 2026-09-10 후속 — 최신 SHIFT 순서와 사업별 제공 가치

사용자 요청: 현재 디자인의 퀄리티는 유지하면서 다른 작업에서 변경한 SHIFT 브랜딩 방식·순서, 유호가 추구하는 것, 각 사업의 제공 가치와 선택 이유를 홈페이지 전반에 반영한다. 최신 `마케팅/SHIFT.md`·`SHIFT_공개수업.md`·`SYNK_철학.md`와 각 사업 원문을 다시 읽었다. 실제 회사 일 → AI와 시도·수정·사람의 판단·결과 → 도움이 된 방법 → 신뢰와 유호의 AI 강사 활동·교육지원 순서를 적용했다.

- 서로 다른 A안(실제 제공물 중심)과 B안(창업자의 실제 판단 중심)을 병렬 작성하고, 별도 검토자가 사실·준비 상태·유호의 실제 말·조직·음성보류를 검토했다. 통합 문안의 수정 필요 지적 0개. 로고 변경 이유는 사용자가 직접 말한 “서로가 서로를 죽이는것같더라고”와 SYNK 실땀 제거 제안에 근거한 편집 서술이다.
- 기업 홈은 **실제 로고 작업 → LAB·SHIFT·PULSE 제공 경험 → 선택할 때 살필 근거 → 철학 → 질문과 협업**으로 바꿨다. 첫 화면은 회사가 만드는 경험을 소개하고 바로 사례와 사업으로 이동한다. 기존 질문 기능은 뒤로 옮겼으며 질문을 해도 첫 회사 소개는 유지된다. 공개 안내 6개·답변 29개도 같은 내용으로 갱신했다.
- LAB은 학생이 고른 상황과 다음 연습을 준비하는 교육, SHIFT는 실제 작업의 판단과 적용 방법, PULSE는 한 곡과 장면의 완결된 감상으로 설명한다. 현재 한글 이름 경험, 직접 작성 자료, 61.6초 전곡에 연결하고 개원·강의·지원·제작 협업의 준비 범위를 구분했다.
- Field Notes는 실제 로고 수정 기록을 앞에 두고 기존 실습을 관련 도구로 재배치했다. SHIFT 관련 소개 13곳을 맞추고, 이전 공개수업은 접힌 참고 실습으로 정리했다. LAB 전용 페이지에는 SHIFT 안내를 적용하지 않았다. 기존 공예 필름은 첫 화면에서 내려 현재 작품·실제 로고를 보여준다. 미디어 원본은 삭제하거나 다시 인코딩하지 않았다.
- 경쟁사 공식 안내에서도 실습·예시·결과물·피드백·감상 제공을 확인했다. 따라서 해당 기능을 SYNK의 독점이라고 쓰지 않고 **실제 작업과 선택 이유, 자기 조건으로 바꿔 쓸 자료, 다시 찾을 작품, 협업의 결과·범위·완료 기준**으로 선택 근거를 구성했다. 확인 자료: [패스트캠퍼스 실무 교육](https://b2b.fastcampus.co.kr/service_aicamp_rndresearch), [스파르타 업무 자동화](https://spartaclub.kr/product/20), [TTMIK 교육 과정](https://courses.talktomeinkorean.com/course-curriculum), [Lofi Girl 감상](https://www.youtube.com/watch?v=jfKfPfyJRdk). 경쟁사 전체 조사나 정량 우월성 검증은 아니다.

검증: 기존 응답 계약 시험 29개 통과. 기업 홈 37개 ID·20개 로컬 파일 참조, Field Notes 244파일·561참조 검사 통과. 기업 홈 1280·390px와 Field Notes 700·390px에서 실제 화면을 확인했고, 가로 넘침·중복 ID 없음 및 질문 답변 후 첫 소개 유지, 음성 자동 재생 없음, 참고 수업 접힘을 확인했다. 지연 로딩 완료 후 기업 홈 누락 이미지 없음. 공개 목적지 4곳 HTTP200. 기존 WebMCP 등록 알림은 확인했으나 직접 도구 호출은 이번에 재검증하지 않았다.

반영 완료:

| 위치 | 소스 | 확인 |
|---|---|---|
| [공개 기업 홈](https://synk.im/) | `db27ecf95c552e336fdeebb1b09a88b594ba62da` | GitHub Pages built, HTML·JS·JSON·CSS HTTP200 및 줄바꿈 정규화 후 원본 일치 |
| [기존 비공개 검토 홈](https://synk-conversations.unmet23.chatgpt.site/) | `b79a7a30c3cbf8a3f6d03eeb24200d79c0a6f7c5` | Sites v6 succeeded, 실제 브라우저에서 새 본문·질문 영역 확인 |
| [공개 작업 기록과 자료](https://synk-field-notes.unmet23.chatgpt.site/) | `47db14dfe71ec2e24429143d0c652dd13e4784e7` | Sites v7 succeeded, 실제 브라우저에서 새 제목·작업 기록 확인, CSS 일치 및 HTML 본문 뒤 CDN 검사 스크립트 추가 확인 |

기존 로고·마스코트 자산과 영상·음성은 변경하지 않았다. 다른 작업에서 선정 중인 목소리를 반영한 교체, SNS 게시·프로필 수정, 교육 모집·실제 고객 이용 효과 확인은 이번 완료에 포함되지 않는다. 원문과 적용 증거는 [내용 개편 검증](기업웹_20260910_내용개편검증.json)에 남긴다. 기억은 이번 요청에서 갱신하지 않았다.

## 2026-09-10 후속 — 브랜드 패널의 반복 제거와 중앙 정렬

사용자 실제 요청: 처음에는 SYNK의 S 시작선을 맞춰 달라고 했고, 이어 이 패널에서는 검정 SYNK 세 개를 제거하고 LAB·SHIFT·PULSE만 남기도록 구체화했다. 이후 이름만 남은 구성에서는 각각 중앙 정렬하고 크기를 조금 키우며, “중심은 차분하게. 각자의 이름은 또렷하게.” 문구 때문에 늘어난 공간을 줄여 달라고 했다. 최신 요청을 우선했다.

- 위 종이색 SYNK는 유지하고 아래는 승인 로고 원본의 사업명 영역만 SVG 표시 범위로 노출했다. 로고 이미지를 재생성하거나 파일 픽셀을 수정하지 않았다. 사업명마다 중앙선이 같고 높이는 32→40px, 작은 화면은 28→35px로 25% 확대했다.
- Field Notes 패널의 설명 문구와 해당 문구 여백을 제거했다. 같은 패널이 있는 synk.im과 비공개 검토 홈도 반영했다. 사업 소개 영역의 전체 회사 로고는 이번 패널 교정 범위와 구별했다.
- 처음 접두어 제거를 Field Notes에만 반영해 synk.im이 남아 있었고 사용자가 지적했다. 이번에는 공개 기업 홈까지 실제 게시 화면을 확인했다. 원본 픽셀 독립 검수에서 사업명 누락·검정 접두어 노출 0개. 공개 브라우저 700px에서 세 이름 중앙선 오차 0.01px 미만, 가로 넘침 없음, Field Notes 설명 문구 없음과 패널 높이 432px를 확인했다.
- 최종 공개 기업 홈: `ec6609c37fed29c0390bc55257fa3368e2a7ff73`, GitHub Pages built, HTML·CSS HTTP200 및 줄바꿈 정규화 일치. 비공개 검토 홈: `3e8f2358b235253c1c18b3e47840b5880b08664e`, Sites v8 succeeded (`appgdep_6aa29af93e34819197557d0a9600c75f`). Field Notes: `edbbb91f2e8b71d6f848939e3b25fd31a721325c`, Sites v9 succeeded (`appgdep_6aa29b454dfc81919459d84645be4845`), 공개 브라우저에서 최종 배치 확인.
- 사업 내용 개편 때 확인한 정본 11개 지문은 최종 교정 시에도 일치했다. 기존 음성 보류와 준비 범위는 그대로다.

## 2026-09-10 후속 — 몽글 외곽 교정과 연관 산출물 최신화

사용자 요청: 메인 몽글의 4K 출처와 떠 보이는 테두리를 조사해 최상의 품질로 교정하고, 앞으로 모든 수정의 실제 사용처를 찾아 정본·제작 경로·현재 결과까지 함께 최신화한다. 정확한 ‘GPT 2.5’ 선택 기능이 없는 점을 설명한 뒤 사용자는 “시간이 걸려도 좋으니까 뭐든 최상의 퀄리티로 구현해줘”라고 답했다.

- **출처:** 홈페이지의 세 마스코트는 1000×1000 WebP이며 기존 4096×4096 RGBA 정본을 Sharp로 축소·WebP90 인코딩한 결과와 바이트가 같았다. `tools/마스코트4K굽기.js`의 Gemini 4K 제작 및 흰 배경 후처리 경로는 확인했지만, 개별 이미지의 생성 응답 로그가 없어 정확한 모델 버전은 확인 불가다. 웹에 4K 파일을 직접 게시했다고 표시하지 않는다.
- **원인과 선택:** 몽글의 밝은 띠는 반투명 부분뿐 아니라 원본의 불투명 외곽 픽셀에도 있었다. 흰 배경의 색 잔류와 일치하는 현상이며 CSS 테두리만의 문제가 아니었다. 기본 ImageGen 교정 후보는 1254×1254 RGB에 격자가 들어 있어 채택하지 않았다. 이를 GPT 2.5 결과나 투명 4K로 표시하지 않는다.
- **교정:** `tools/몽글외곽정리.cjs`로 캔버스 바깥과 연결된 외곽 24px 안에서 흰색 잔류만 추정·감산했다. 가장 가까운 색으로 덮어 생기는 줄무늬는 독립 검수 후 폐기하고, 픽셀별 털의 명암 차이를 유지하는 방식으로 확정했다. 구멍·봉제선·얼굴 안쪽은 보호하고 홈페이지의 몽글 위치·접지 그림자도 함께 조정했다.
- **정본 검증:** 17개 모두 4096² 유지, 알파 변화 0, 외곽 교정 범위 밖 RGBA 변화 0. 정면 15개의 변경 좌표 199,139개와 그 좌표의 최종 RGBA가 모두 같음을 별도 검증했다. 좌34·우34도 실물 확인했다. 최종 본체 SHA256은 `6fd4e6b3946006d54d7059e60cc135b27874128bd8e7dfa6e6a62f1a1532f56c`다. 이전 원본은 Git `b06809376a292ae8a9912007fdc988cdf6b85c96`에 보존돼 있다.
- **현재 파생물:** 1024px 표정 17개와 판 기록을 갱신했다. 정렬·눈 영역·알파를 유지했고 다른 캐릭터 33개와 기록은 변경 0이다. 기업 홈, 이름 경험·진단 화면의 WebP 4개, 소개서 20PNG·4PDF, 캐러셀 14PNG, 계정 카드 2종·마케팅 카드 1종·첫게시물 6계정의 정적 이미지와 현재 공유 사본을 갱신했다. 앱용 8개 표정의 반영·커밋 상태는 아래 검증 파일에 별도 기록한다.
- **묶음 확인:** 마케팅 ZIP 20개와 관련 ZIP 6개를 다시 검증했다. 후자에 든 기존 영상·오디오 16항목은 바이트를 보존했다. 파일별 변경 전후 지문과 실제 경로는 [마케팅 갱신 목록](기업웹_20260910_자산/marketing-sync-files.json)에 있다. 다운로드 묶음 갱신을 SNS 게시나 이미 전달한 파일의 회수·재전달로 확대하지 않는다.
- **앱 소스:** SYNK-talk의 336px 표정 8개와 검수 시트만 갱신해 `42eddf4460b98e62016f2aab0d75f758a68579bc`를 origin/master에 반영했다. 관련 시험 36개와 실제 시트 검수 통과. 새 출력 8개의 알파는 현행 정본을 축소한 알파와 같다. 이전 앱의 7표정은 현행 정본과 윤곽이 달라 이번에 맞췄으므로 이전 앱 8개 전체의 알파가 보존됐다고 주장하지 않는다. 다른 작업의 스테이징은 보존했다.
- **앱 미리보기까지 갱신:** 뒤이어 `dist/first-flow-preview`의 실제 번들이 구 몽글 8개를 참조하는 것을 발견했다. `tools/첫흐름미리보기.js`가 HTML과 실제 번들에 연결된 마스코트 지문을 검사하고, 다르면 기존 export로 다시 만든 뒤 서비스하도록 수정했다(`659434f`, 회귀 시험 6개 통과). 실물 로그인에서 발견한 웹의 CanvasKit 초기화 오류도 기존 의도대로 이미지 표시 방식으로 대체하도록 고쳤다(`f4ef384`). 다른 미커밋을 섞지 않은 해당 커밋의 깨끗한 사본에서 82파일을 다시 생성하고 현재 미리보기에 반영했다. 390×844 합성 로그인·답장 화면에서 최신 몽글 실제 로드와 브라우저 오류 0을 확인했다. 이 로컬 확인은 앱 운영 배포·학생 실사용과 다르다.
- **다음 작업의 실행 경로:** 기업 홈의 `npm run build`가 `sync-mascot-assets.mjs`를 실행해 현행 정본에서 배포 자산을 만든다. 이름·진단 사본은 `node tools/기업웹마스코트동기화.cjs`, 화면 표정은 `python tools/마스코트층작게.py --누구 몽글`을 쓴다. 소개서 자산 생성기는 파일 존재 여부 대신 원본·출력 지문을 비교한다. `--keys=mong,smile,curious --검사만`로 범위를 좁혀 검사할 수 있다. 마케팅 생성기도 현재 원본을 읽도록 고쳤다.
- **공통 기준:** `AI_운영원칙.md`에는 무엇을 수정하든 실제 참조를 검색해 정본·코드·생성기·공유 사본·파생물·현재 게시본을 함께 조사·반영하도록 명시했다. `DESIGN.md`와 `명품_기준_v1.md`에는 실제 크기·배경·겹침·재생에서 부자연스러움을 먼저 찾아 교정하고, 교정만으로 완성할 수 없으면 허용된 이미지 통로로 재제작·재검수하도록 반영했다. 새 중앙 큐·강제 훅을 만들지 않았으며, 이미 실행 중인 모든 다른 작업에 지침이 자동 전달됐다고 주장하지 않는다.

게시 상태:

| 위치 | 소스 | 반영 확인 |
|---|---|---|
| 공개 기업 홈·이름·진단 | `0a92e7f7c0a1c263eedbad6d8b9b389ac1aa24b2` | GitHub Pages built, 기업 홈 공개 화면 1440·1280·700·390·320px에서 오류·넘침·미로드 0 |
| 기존 비공개 검토 홈 | `1af37b9719ed02ebe46d3b9ccd7a23700a89e06b` | Sites v9 succeeded, 배포 `appgdep_6aa2a45d76c08191ab596477931a1116` |
| 공개 Field Notes | `6d134ad0e7507d8f54f35aa5504fa204366e41d4` | 244파일·561참조 빌드 통과, Sites v10 succeeded, 배포 `appgdep_6aa2a6233dd48191b52f222e3eaf53e8` |

기존 응답 계약 시험 29개도 통과했다. 현재 웹 자산에 버전 표식을 붙여 이전 캐시와 구별했다. 공개 응답의 파일 대조·실제 하위 화면 확인·정본과 파생물의 상세 증거는 [마스코트 검증](기업웹_20260910_마스코트검증.json)에 모았다. 이전 시안의 원문 지문은 당시 기록이며 이번에 수정한 공통 기준과 혼동하지 않는다.

공개 재조회에서 synk.im 9개 응답은 HTTP200이었다. 이미지 5개는 바이트 지문이 같았고, HTML3개·CSS는 CRLF/LF 줄바꿈 차이만 있었다. 이름·진단 첫 화면과 이미지 표시, Field Notes의 갱신 이미지 2개 지문 및 실제 카드 표시를 확인했다. 하위 화면의 JavaScript 예외는 없었으나 별도 `/favicon.ico` 요청의 404는 남아 있다. 이번 이미지 표시 오류와 구별한다.

**남은 범위:** 첫게시물의 기존 영상 03/04/05/06/07/09는 현재 음성 원고와 길이가 달라 전체 납품 검증이 미완료다. 이번에는 정적 48장·62화면·418링크를 확인했고, 음성·영상은 다른 작업의 현행 목소리 선택과 적용 순서에 따라 교체할 대상으로 남겼다. 앱 운영 배포·기기 실사용·SNS 재게시·이미 배포한 인쇄물/전달물 재전달은 이번에 수행하지 않았다. 해당 범위까지 전부 최신화됐다고 보고하지 않는다.

앱 미리보기 최종 검증은 마스코트 24참조 지문 일치, 현재 미리보기 82파일과 깨끗한 생성본의 바이트 일치, 캐시 회귀 6개·웹 표시 대체 2개·기존 배선 8개 시험 통과다. 작업용 18769 서버는 종료했고 포트가 닫힌 것을 확인했다. 자동 승인 검토가 임시 `node_modules` 연결 삭제를 `blocked by policy`로 거절했으며 구체 사유는 제공하지 않았다. 해당 작업을 더 시도하지 않았고 `.synk-talk-mongle-preview-20260910` 격리 사본과 `SYNK-talk/dist/first-flow-preview-before-mongle-20260910`, `first-flow-preview-before-webfallback-20260910` 백업 2개는 남겨뒀다. 현재 사용 경로인 `dist/first-flow-preview`는 새 생성본이다.

## 2026-09-10 후속 — 까몽과 몽글 사이의 밝은 띠 재제작

사용자는 이전 외곽 교정 뒤에도 몽글 왼쪽과 까몽 사이에 흰색 테두리가 남아 이질적이라고 지적했다. 실제 4K PNG에서 불투명 픽셀 안쪽까지 밝은 분홍색 잔류가 이어지는 것을 다시 확인했다. 경계만 깎는 방식은 충분하지 않아, 기본 ImageGen으로 세 캐릭터를 한 장면에 통합하고 같은 빛·바닥·접촉 그림자를 다시 만들었다. 결과는 `docs/캐릭터/웹_대표장면/정본.json`과 `세친구_20260910.png`에 기록했으며 1586×992 RGB다. 정확한 GPT 2.5 선택 기능은 확인되지 않아 그렇게 표기하지 않는다. 개별 4K·표정 정본은 유지한다.

실제 영향을 받는 사용처 조사 결과는 기업 홈 두 곳뿐이었다: `SYNK-website/dist/index.html`과 `synk-policy/index.html`. Field Notes와 앱에는 이 세 친구 장면 사용처가 없었다. 기업 홈 생성기는 장면 원본·레이아웃 참조·세 캐릭터 정본의 SHA-256을 검사하고, 변경되면 시각 검수 없이 낡은 장면을 재생성하지 않도록 갱신했다. 홈페이지는 `companions-scene.webp` 한 장을 Paper 바탕에 multiply로 합성한다.

로컬 실제 렌더에서 1440·1280·700·390·320px 이미지 로드·가로 넘침·스크립트 오류·배경 일치·합성 모드를 확인했고 모두 통과했다. 1440·390px 장면과 첫 화면을 직접 보고 흰 상자·두꺼운 띠·겹침 이질감·부유·캐릭터 잘림·본문 충돌이 없음을 확인했다. 독립 검수도 같은 결과로 채택을 권고했다.

게시 상태 갱신: 공개 `https://synk.im/`은 GitHub Pages 소스 `f7bb29c3af5a151209357f5b797fadcb2ee39bf9`로 push했고 Pages 빌드 재조회가 남아 있다. 기존 비공개 홈 `https://synk-conversations.unmet23.chatgpt.site/`은 Sites v10, 배포 `appgdep_6aa2b74aba348191bf836821fb92be2f`, `succeeded`까지 확인했다. Field Notes는 이 장면을 사용하지 않아 재배포하지 않았다.
