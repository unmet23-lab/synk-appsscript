# 인계 — 일요일 자율일 설계 (2026-09-07 밤)

> **이 문서는 다리다.** 정본을 베끼지 않는다. 다음 세션이 **바로 손댈 것**과 **내가 틀린 자리**만 적는다.
> 정본 셋: 자율일 = `docs/자율일_설계_v1.md`(초안 v0.9) · 차시 = `docs/1기_차시대응표_v1.md`(v2.0) · 달력·값 = `docs/개원전판매_설계_v1.md`(v1.8) · 남은 일 = `docs/_ops/트랙.md` §0-판매
> ⏳ 시한 = **12-05**(첫 자율일이 12-06 이다)

---

## ① 첫 손 — 이것부터

```
node tools/codex-review.js --런목록
```

**주울 것 = `심문-2026-09-06T1814-de5ab1`**(자율일 설계 1회차 · 아스트라 15 · 제미나이 5). 나머지 둘(옷 얹기 · 개원 입지)은 **남의 세션 것이니 손대지 않는다.**

결과 전문:
- `docs/_ops/심문결과/자율일_설계_v1-2e7f661dea5a-astra-xhigh-1.md`
- `docs/_ops/심문결과/자율일_설계_v1-2e7f661dea5a-gemini-high-2.md`

🔴 **판정 한 줄: 「동결을 막는 결함이 있다」.** 이 초안은 지금 상태로 못 굳힌다.

---

## ② 🔴 내가 틀린 자리 셋 — 여기부터 고친다

### ㉠ 「엔진이 꺼져도 학생이 낸다」가 **코드와 반대**다 (아스트라 P0)

내가 계약 문서(`SYNK-talk docs/P0_제품계약.md` §6-4)만 읽고 사실로 적었다. **안 재고 적었다.**

실측은 반대다 — `SYNK-talk/lib/오늘과제.js:474` 가 빈 배정에서 `제출재료:null` 을 내고, 그 재료로 `제출사건()` 을 부르면 사건도 `null` 이다. 아스트라가 합성 입력으로 재현했다. **그 상태에서는 학생이 녹음해도 서버에 안 모이고 월요일 집계도 안 선다.**

▶ **다음 세션이 할 것**: 그 파일을 직접 열어 확인하고, 자율일 설계 §⑤-㉠ 의 「우리 잘못」 줄을 사실로 고친다. 계약 문서와 코드가 갈린 것이면 **어느 쪽이 정본인지도 같이 정한다.**

### ㉡ 주간문형 시트를 고치면 **옛 과제의 목표가 사라진다** (아스트라 P0)

내가 「유호님이 배포 없이 고치실 수 있게 시트에 둔다」를 장점으로 적었는데, 고치는 순간 **지난주 과제가 무엇을 겨눴는지 복원이 안 된다.** 2기에 같은 차시 행을 바꾸면 1기 기록이 죽는다.

아스트라가 기존 `학생판스냅샷()` 에 차시 판·문형 키를 넣어 봤더니 **0/2 개만 보존**됐다. 「기존 계약을 쓴다」로는 안 닫힌다.

▶ **배정할 때 그 판을 제출 행에 박는 규칙**이 필요하다.

### ㉢ 오답 지도는 **재출제할 문항 목록이 아니다** (아스트라 P1이지만 급소다)

내가 `퀴즈오답맵_`·`aiWeakMap_` 을 「재출제 재료」로 썼다. 실제로 그 둘은 **문자열을 돌려준다**(`엔진_콘텐츠AI.js:3128`). 문항 식별자·발생일·문형 연결이 안 남는다. 그래서 §③-㉢ 의 「그 주 것을 앞에 놓는다」를 **수행할 재료가 없다.**

▶ 오답을 다시 내려면 **문항 단위로 남기는 자리부터** 있어야 한다.

---

## ③ 나머지 지적 — 갈래별로

### 계약과 부딪히는 것 둘

| 무엇 | 왜 |
|---|---|
| 🔴 **SRS 금지가 「오류태그 기반 재출제」까지 금지한다** | 내 §⑦-㉠ 은 「간격을 계산 안 하니 안 걸린다」로 넘겼다. 실제 문구(`SYNK-talk docs/P0_제품계약.md:354`)는 **오류태그 재출제도 금지**다. 유호 확정(자율일에 오답을 낸다)과 기존 계약이 **정면으로 부딪힌다** — 문서끼리 맞추는 일이 먼저다 |
| **사람 손을 늘리는 해법 금지 위반**(제미나이 P0) | 「남은 것을 다음 주 평일 과제에 얹는다」와 월요일 한 줄이 사람 손에 기댄다는 지적 |

### 셈이 안 맞는 것 셋

- **매주 문형 둘**이라는 그릇에 **첫 주(문형 0)와 마지막 주(전체 복습)가 안 들어간다.** 8주 중 2주가 규칙 밖이다.
- **필수 진단이 완주 조건에 없다.** 중간(12-20)·끝(01-24) 진단이 자율일에 있는데, 일반 과제만 내도 「완주」가 된다. **끝 진단은 무료 연장을 재는 자다.**
- **제출 행 수로 완주를 세면** 같은 문장을 두 번 녹음한 학생과 목표를 다 한 학생이 같아 보인다.

### 내가 걷은 줄 알았는데 남아 있는 것

**시간 가정이 근거에 그대로 살아 있다.** 「말하기 주 35분」은 걷었는데, §① 의 「일요일을 쓰면 50시간」은 같은 종류의 안 잰 값이다. 그걸 설계의 필요성 근거로 계속 썼다.

그리고 **녹음 길이를 「실제로 말한 시간」이라 부른 것도 틀렸다.** 무음 1초도 길이에 들어간다(아스트라가 합성 무음으로 재현).

### 학생 쪽 (제미나이 + 아스트라가 같이 짚음)

**재미·신선함 축이 통째로 없다.** 내가 「1기 전엔 못 짓는다」로 넘겼는데, 심문은 **「이미 있는 화면을 재사용한다는 사실이 새로 얹는 과제 묶음의 게이트를 대신 통과시키지 않는다」**로 그 넘김 자체를 기각했다.

---

## ④ 아스트라가 낸 대안 — 다음 판의 뼈대로 쓸 만하다

> 기존 배정·제출 체계 안에서 **「그날 실제로 배정한 묶음」**을 기준으로 삼는다. 차시표의 일반 주·문형 없는 주·전체 복습·필수 진단을 **구분**하고, 배정할 때 항목별 목표·문항 판·원 오답 고리를 **고정**한다. 완주는 그 묶음의 고유 항목 충족으로 세고, 공급 실패와 전송 대기는 따로 표시한다. 이월은 평일에 무조건 더하지 않고 **대체 규칙**으로 처리한다. 각 항목의 제출이 기존 엔진 소비자와 학생의 다음 행동까지 이어지는 경로를 **함께 적은 뒤** 굳힌다.

🔑 이 대안이 내 초안의 구멍 다섯(㉠㉡㉢ · 그릇 · 완주 세기)을 한꺼번에 닫는다. **다음 판은 이 축으로 다시 쓰는 것을 권한다.**

---

## ⑤ 이 세션이 한 것 — 커밋만

| 커밋 | 무엇 |
|---|---|
| `7daad3cdb` | 차시 대응표 첫 벌(40차시 = 8과 × 5차시라 「고르기」가 아니라 「덜어내기」) |
| `6ab7f3583` | 심문 1회차 반영 v1.1(내 오류 넷) |
| `19baa2e62` | **v2.0 — 토요일 8차시 + 일요일 자율**(유호 확정 09-07 ⓒ) · 판매 설계 v1.8 · 파일 이름 옮김 |
| `5c9a85a39` | 옛 인계문(판매 달력) 걷음 |
| `3cdf97abf` | **자율일 설계 초안 v0.9** |
| 기억 둘 | `teaching-toward-the-ruler-kills-it` · `shell-eats-backticks-in-double-quotes` |

전부 push 했다.

---

## ⑥ 🔴 유호님 앞에 걸린 것

| 무엇 | 언제까지 | 지금 |
|---|---|---|
| **일요일 그 시각에 화면 방을 켜 두실까** | 12-05 | 안 켜면 학생이 올 까닭 셋 중 둘이 죽는다(자율일 설계 §⑤-㉢) |
| **값을 다시 보실까** | 진단 공개 전 | 물건이 반으로 줄었는데(교실 주 2회 → 1회) 값은 15만원 그대로다. 확정 ⓐ 를 지키는 쪽으로 갔고, 근거는 다시 썼다(판매 설계 §④-㉡) |
| 검수 갈래에 딸린 **포인트** | 12-05 | 과제 포인트가 검수 확정에 매여 있다(차시 대응표 §⑤-㉣) |
| 주간문형 시트를 유호님이 고치실까 | 1기 뒤 | 🔴 **심문 P0 가 이 자리를 쳤다**(②-㉡) — 고치면 옛 기록이 죽는다. 갈래를 다시 짜야 한다 |

---

## ⑦ 밟은 함정 — 다음 사람이 같은 자리에서 안 멈추게

1. 🔴 **셸 큰따옴표 안의 백틱은 파이썬 문자열 안이어도 셸이 먼저 먹는다.** 파일 경로 네 자리가 조용히 빈칸이 됐다. `assert` 는 «옛 문자열»만 보므로 안 운다. ▶ 본문 고치는 파이썬은 **스크래치패드에 파일로 쓰고** `python <파일>` 로 돌린다. 기억 = `shell-eats-backticks-in-double-quotes`.
2. **한글 이스케이프(`\uXXXX`)로 도망치다 두 번 더 틀렸다.** 파일로 쓰는 게 답이다.
3. **줄끝이 파일마다 다르다.** 이 저장소 문서는 대개 CRLF 인데 트랙·인계문은 LF 였다. ▶ 스크립트가 **그 파일의 줄끝을 읽어 따라가게** 짠다.
4. **트랙 불릿은 900바이트가 상한이다**(`node tools/트랙위생.js` 가 잰다). 넘으면 걷는 게 아니라 **정본으로 옮기고 포인터만** 남긴다.
5. **남의 병합이 도는 중엔 커밋이 막힌다.** 기다린다. 대신 마치지 않는다.

---

## ⑧ 안 한 것 — 정직하게

- **심문 15건을 안 반영했다.** 이 인계문이 그 목록이다.
- **2회차 심문을 안 돌렸다.** 1회차 반영 뒤에 돈다.
- **차시 대응표를 자율일 심문에 맞춰 안 고쳤다.** 최소 두 자리가 걸린다 — §④-㉡ 의 「앱 미션 주 35분」(이미 자율일 설계가 철회했다)과 §⑥ 의 일요일 셋.
- **준비 시간을 안 쟀다.** 화면 한 장의 빈 틀이 먼저다(차시 대응표 §⑧-㉢).
- **`오늘과제.js:474` 를 내 눈으로 안 봤다.** 아스트라 말을 옮겼을 뿐이다 — 다음 세션이 직접 연다.
