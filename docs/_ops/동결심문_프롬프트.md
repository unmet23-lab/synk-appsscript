# 설계 심문 프롬프트 템플릿 (정본)

> **정식 이름 = 「설계 심문」**(08-06 유호님 이름 분리 · 옛 이름 「동결 심문」과 같은 것 — 파일명은 참조가 깨지지 않게 그대로 둔다).
> 형제 둘과 헷갈리지 않게: **①배포 검수**=코드·매 배포 자동 / **②설계 심문**=이 파일 / **③물리 작성**=GPT가 직접 씀. 지도 = `docs/AI_스택_가이드.md` §1-2.
>
> ⏸ **꺼짐** (2026-08-06 유호님: *"앞으로 심문은 하지마. 일단 지금부터 심문 하는 기능 꺼놔 내가 필요할때 킬수있도록 다시 말할게"*)
> — 유호님의 명시 재개 지시 없이는 **어떤 세션도 이 심문을 실행하지 않는다.** 이 파일은 재개 때 쓸 보존 자산이다. 분업 정본 §8-2에 같은 표식.
> ⚠**되켤 때 먼저 고칠 것**: 이 심문은 **도구가 없다**(템플릿을 손으로 조립해 먹인다). 꺼진 실제 이유가 그 조립+대기 20분이 **유호님 앞에서 흘렀기** 때문이라, 도구화·백그라운드 없이 그냥 켜면 F121이 그대로 재발한다.
>
> **자리**: 계약·설계 정본을 동결하기 직전, codex(luna max 기본)에게 먹이는 적대 심문의 고정 템플릿.
> 분업 정본 = SYNK-talk `docs/개발_분업.md` §8-2. 판정 이력 = memory `engine-role-allocation-2026-08-05`.
> **이 파일이 repo에 커밋돼 있는 이유**: 심문 입력을 세션이 회차별로 선별·각색하면 독립성이 모델 이름에만 남는다(심문 P0 실측).
> 변경은 diff로 보이게 — 개정하려면 이 파일을 고쳐 커밋한다.
>
> **호출 규칙(실측 08-05)**: Git Bash에서 codex.cmd 직접 호출 금지(생성 없이 exit 0) — `cmd.exe /c` 경유(`tools/codex-review.js`의 `codex()` 패턴)·cwd=repo. 읽기 전용 3플래그 필수. 빈 출력·타임아웃 = 확인 불가 = **동결 불가**.

---

당신은 독립 반대심문자다. 아래 「동결 후보 문서」를 공격만 하라 — 칭찬·양비론 금지, 애매하면 결함으로 본다.

## 배경 (사실로 전제하라)
몽골 게임화 한국어 학원(1인 창업·비개발자 원장)이 「회사의 뇌」 = 내부 학습 엔진을 앱(SYNK-talk)과 병행 설계 중이다. 목표 축 = **「엔진이 학습할 수 있도록 설계」**: 앱의 모든 기능이 학습 데이터를 낳고, 스키마·계약은 소급 불가라 동결 전에 결함을 잡아야 한다.

## 소유자(유호님)가 이미 닫은 결정 — 재론하지 말라. 단, 문서가 이것들과 모순되면 그 모순은 지적하라.
[여기에 대상 문서와 관련된 재제안 금지 목록을 삽입한다 — 출처는 memory 인덱스의 🚫 항목과 정본 문서의 기각 인용. **줄이지 말고 관련분 전부**.]

## 동결 후보 문서 (공격 대상 — 전문)
[대상 문서 **전문**을 삽입한다. 요약·발췌 금지 — 발췌는 심문자를 눈멀게 한다.]

## 공격 렌즈 (전부 적용)
1. **소급 불가 구멍**: 이 규격으로 데이터가 쌓이기 시작한 뒤에는 영원히 복원할 수 없는 누락 필드·스냅샷·식별자가 있는가?
2. **학습 사슬 단절**: 「기능→이벤트→필드→엔진 부품(어휘 지도·학습자 모델·평가셋)→회수 시점」 사슬이 끊긴 곳. 지도 질문에 답하는 데 필요한 필드가 계약에 실재하는가?
3. **문서 간 모순**: 이 문서가 다른 정본(제품방향·코어엔진 설계·L0/C0/M0 계약·분업)과 갈리는 곳.
4. **닫은 결정과의 충돌**: 위 금지 목록과 문서 내용이 모순되는 곳.
5. **기술 실행 가능성**: 실제로 수집·저장·마이그레이션·검증 가능한가? 멱등성·동시성·오프라인 재시도·동의 철회 전파가 정의됐는가?
6. **조용한 실패 방향**: 실패·미적용·빈 값이 「통과」와 같은 모양이 되는 자리.
7. **과설계·선행 구축**: 지금 필요 없는 테이블·인프라를 만들고 있지 않은가? (앱 우선·E0 코드 0줄)

## 출력 형식
발견 항목마다: [P0|P1|P2] 제목 — 주장 2~4문장, 문서의 어느 절이 문제인지 지목. P0=동결하면 사고가 나는 결함(소급 불가 포함), P1=중대 약점, P2=개선 여지. 마지막에 「이 문서보다 나은 대안 구조가 있으면 1개」를 제시하거나 「없음」이라고 명시하라. 한국어로 답하라.
