# 걸음 0 실측 장부 — 「안 재봤다」를 닫는다

> 2026-09-04 세움 · **2026-09-05 저녁 16/16 답함**. 자 = `docs/엔진7종_상향설계_v3.md` §11 「걸음 0 목록」 실측 14건 + 심문이 더한 둘(⑮·⑯). 세울 땐 v2(git e371f0010) — 09-04 것의 «N행» 지목은 v2 기준, 09-05 것은 09-05 저녁 작업본 기준(줄은 움직이니 낱말로 찾는다).
> **원칙**: 잰 것만 적는다. 못 잰 것은 「없다」가 아니라 「안 재봤다」로 남긴다.
> 근거 줄번호는 이 저장소(또는 형제 저장소 talk) 실물을 직접 열어 확인한 것이다(에이전트 보고를 그대로 옮기지 않는다).
> ✅ **09-05 저녁 · 잰 답을 정본 v3 에 옮겼다** — 델타 `docs/_ops/엔진7종_v3/델타_걸음0_ⓑ.txt`(26블록 · 「안 재봤다·미실측·두 값」이라 적혀 있던 자리 전량 + §11 걸음 0 목록 끝에 상태 줄). 이 장부는 «근거»를 쥐고 정본은 «결론»을 쥔다.
> **잰 손**: 09-04 여섯(①③④⑦⑫⑭) = Opus 5 세션 · 09-05 열(②⑤⑥⑧⑨⑩⑪⑬⑮⑯)과 정본 수리 = Fable 5.1 세션(모델은 유호님이 앱 바에서 고르신 것). 실측은 grep·ls·조회라 자가 갈릴 자리가 작지만, 판정이 갈리면 이 줄을 먼저 본다.
> 🔴 **걸음 0 의 «짓는 일» 둘은 안 지었다** — Prism U0 검증기(발주서 v2 `docs/_ops/발주/prism_U0_검증기.md` · 범위 넷 실물 0) · `tools/이해대장.js` 절 넷(실물 0). 까닭 둘: 정본 v3 §0·§11 이 「«짓는 일»(검증기 · 도구 절)은 착수 시점이 정해진 뒤」로 못 박았고(09-04 유호 확정 「구현은 최대한 늦춘다 · 홍보 먼저」) · 검증기 «던지기»는 트랙 §0-코덱스 「세팅 판정」(09-04 유호 「엄청 큰 작업이니 최대한 세팅을 제대로 하고나서」)이 아직 안 닫혔다. **착수 시점은 유호님 자리** — 정하시면 워크트리 `engine/step0` 에서 둘을 짓는다.

## 실측 16건 — 16/16 답함(닫힘 12 · 없다 4 · 안 재봤다 0 · 항목 «안»의 곁 「안 재봤다」 다섯은 각 절 끝에)

| # | 무엇을 재나 | 답 |
|---|---|---|
| ① | 횡단.json 전량 재grep | ✅ 닫힘 · 저장소에 없던 장부를 09-04 에 들였다(`docs/_ops/횡단.json`) · 사본 대조 90/90 실재 |
| ② | 포트폴리오 걸음 1 라이브 배포 여부 | ✅ **닫힘 · 나갔다**(09-05 · 라이브 = HEAD 바이트 동일) |
| ③ | 학생ID 생애 칸 실물 목록 | ✅ 닫힘 · 어긋남 셋 |
| ④ | `intervention.delivered` arm ID 실재 | ✅ **없다**(정의 0 · 사용 0) |
| ⑤ | `season_review` 실 DB 적용 | ✅ **닫힘 · 적용됐다**(리허설·운영 둘 다 · 09-05) |
| ⑥ | 시즌 회고 화면 칸 | ✅ 닫힘 · 사람 칸 셋 실재(학생 자기판정 · 강사 판정 · 사유) · 「통한 개입」·「실현」 0 |
| ⑦ | `exam.result` 문항별 정오 | ✅ **없다** · 영역 셋까지 |
| ⑧ | 되묻기 정렬 SQL 라이브 | ✅ 닫힘 · 코드 실재 + 배포판에 실려 있다(리허설·운영 둘 다) · 실행 횟수는 안 재봤다 |
| ⑨ | ffconcat 재읽기 | ✅ 닫힘 · **목록 파일 자체는 안 다시 읽는다 · 목록 안의 하위 목록은 매 바퀴 읽는다**(로컬 ffmpeg 8.1.2) |
| ⑩ | nano-banana 별칭 `models.get` | ✅ 닫힘 · **살아 있다**(목록·조회 둘 다 200 · 09-05) |
| ⑪ | Reed 곡 물량 | ✅ 닫힘 · 지금 팩 8벌 = **68.8분** · 보류 2벌 더하면 85.5분 · 옛 두 수(69/68.2)가 다 설명된다 |
| ⑫ | `portfolio_moments`→talk 통로 실물 | ✅ **없다**(통로 0) |
| ⑬ | topik.go.kr 원문 1회 | ✅ 닫힘 · 「시험 성적은 성적 발표일로부터 2년간 유효합니다」 원문 확인(교육부 공고 제2025-317호) |
| ⑭ | 커리큘럼 정본 시즌 «식별자» 규격 유무 | ✅ **없다** · 다만 딴 자리에 둘이 있고 서로 어긋난다 |
| ⑮ | 시즌 식별자의 «실제 실행 조건» | ✅ 닫힘 · 시트 ↔ DB ↔ 파일럿 대조표 · 어긋남 다섯 |
| ⑯ | «처리량 자» | ✅ 닫힘 · 장부가 섰다 = `docs/_ops/처리량_장부.md` · 첫 표본 = 이 걸음 0 |

---

## ① 횡단.json — 이 저장소에 없다 (09-04)

`git ls-files` 와 `find` 둘 다 0건이다. 실물은 워크트리 두 곳(옛 인계 폴더의 재료)에만 있고 둘은 내용이 같다(54,318바이트).

그 사본을 파싱하면 최상위 키 8 · 항목 합 100. 항목이 가리키는 실물을 뽑으면 파일 경로 83 · 함수 이름 7 = **90건이고, 90건 전부 실재한다(없음 0)**. 이 저장소 35 · talk 저장소 42 · 기억 파일 8 · 함수 7.

**뜻**: 09-01 grep 이 잡은 것은 지금도 전부 살아 있다. 다만 **장부 자체가 커밋 밖**이라 워크트리를 걷으면 사라진다.
→ **처분**: 걸음 0 안에서 이 저장소로 들였다(`docs/_ops/횡단.json` · md5 동일).

## ② 포트폴리오 걸음 1(`d8758119d`) — 라이브에 나갔다 (09-05)

- 자 = `node tools/배포판점검.js --라이브`(라이브를 **임시 폴더**로 받아 저장소와 바이트로 견준다 · 작업본은 건드리지 않는다 · 손 `clasp` 아님).
- 결과 원문: 「(루트): 라이브 = 저장소 (@HEAD · 배포집합 18개 바이트 동일 · 미커밋 0 — 라이브 = HEAD)」.
- `git merge-base --is-ancestor d8758119d HEAD` = 참(그 커밋은 HEAD 의 조상). 라이브 = HEAD 이므로 그 커밋이 만진 배포 파일(Code.js · 엔진_폼리포트.js · 엔진_운영배치.js · 엔진_셋업확장.js · 엔진_콘텐츠AI.js · contents_순간.js)이 라이브에 있다.
- 곁(이 물음 밖 · 안 건드렸다): 같은 실측에서 **crewcard @28 이 옛 코드를 서빙한다**(배포 지문 63fe9b64 ≠ 현재 bf97564c). 배포는 `/deploy` 자리다.

## ③ 학생 ID 생애 칸 — 실물이 설계 문서를 앞섰다 (09-04)

시트층 정본은 `엔진_운영배치.js:168` 의 `EXIT_LOG_HEADERS` 이고 **일곱 칸**이다.
값 목록도 `엔진_운영배치.js:172` 의 `EXIT_REASONS_` 여섯으로 서 있다(결정.md 08-30 확정).
DB 층 다섯 칸은 talk 저장소 마이그레이션 `20260902600000_learner_lifecycle_c16.sql` 80~84행에 `add column if not exists` 로 서 있다.

**어긋남 셋**:
- ⓐ `learner_lifecycle` 이라는 낱말은 이 저장소 코드에 0건이다(문서에만 넷).
- ⓑ 시트 쪽 이름은 한글로 따로 산다. DB 의 `enrolled_at`·`lifecycle_status` 에 대응하는 시트 칸은 없다.
- ⓒ 🔴 **설계 문서가 실물보다 뒤처져 말한다** — 아래 정본 수리 ㉯(09-05 에 고쳤다).

## ④ `intervention.delivered` 의 «arm ID» — 정의 0 · 사용 0 (09-04)

「어느 처방 갈래를 받았나」를 가리키는 칸이 계약에도 코드에도 없다.

- talk 계약 정본 `docs/L0_데이터계약.md` 119행 정의 블록(payload 예시 122~131행)에 arm 계열 0.
- talk 값목록 정본 `계약/수집_교정_계약.json` 90행 「출처」 묶음에도 0. 101행 `event_type` 17종에 사건 이름은 있다.
- 실제 생산자 talk `supabase/functions/deliver/index.ts` 899~912행 insert 열 18개에 arm 계열 0.
- `arm_id`·`policy_arm`·`처방갈래` grep → talk 전 폴더 0건 · 이 저장소 `*.js` 전량 0건.
- 낱말이 사는 곳은 설계도 하나뿐이고, 거기서도 「앞으로 낼 이름」으로 적혀 있다.

**뜻**: 설계도가 「없다」고 적은 것이 참이다. 걸음 1 에서 새로 내는 어휘가 맞다.

## ⑤ `season_review` — 실 DB 에 적용됐다 · 리허설·운영 둘 다 (09-05)

- 자 = talk `tools/원격SQL.js`(읽기 전용 · **표 존재와 마이그레이션 장부만** 물었다 · 행 내용 0 · 학생 식별 데이터 0).
- 리허설(synk-core-rehearsal): `engine.season` · `engine.season_compass` · `engine.season_review` 셋 다 있다 · `engine.schema_migrations` 에 `20260812140000`(season_c11) · `20260812170000`(season_review_c11) · `20260902600000`(learner_lifecycle_c16) 셋 다 적용 참.
- 운영(Synk Core): 같은 결과(셋 다 있다 · 셋 다 적용 참).
- 마이그레이션 실물 = talk `supabase/migrations/20260812170000_season_review_c11.sql`(455행 · 표 정의 111~121행).
- 곁: 운영 읽기에 `--운영` 을 붙였더니 도구가 「읽기의 정본은 과녁 덮어쓰기(`SUPABASE_PROJECT_REF=<운영 ref>`)」라고 일렀다(F462). 결과는 같고, 다음엔 그 길로 간다.

## ⑥ 시즌 회고 화면 — 사람 칸 셋은 실재 · 「통한 개입」·「실현」은 0 (09-05)

- 표(`season_review_c11.sql` 111~121행): `verdict`(강사 판정 · null = 아직 안 눌렀다) · `verdict_by_self`(학생 자기판정 · null = 안 눌렀다) · `note`(사유) · `opened_by/at` · `decided_by/at` · `record_snapshot`.
- 화면(talk `src/회고화면.js` · `src/회고API.js`): 학생 판정을 **먼저** 받고(`자기판정하기`) → 강사 판정 + 사유(`판정하기`). 갈래 셋(closer · same · redirected)과 사유 글자수 상한은 서버가 준다(`lib/회고.js` 굳힌판 = '회고스냅샷.v1').
- v3 걸음 13 「굳힌판 v2」의 **사람 칸 넷** = 학생 자기판정 ✅ · 강사 verdict ✅ · **통한 개입 0** · **실현 0** ⇒ 둘은 짓는다(v3 §7 「season_review 강사 「통한 개입」 확정 열 = 마이그 _c17」 자리). 전제는 참 — 「둘이 있고 둘이 없다」를 알고 짓는다.

## ⑦ `exam.result` 문항별 정오 — 없다 · 영역 셋까지 (09-04)

- talk `lib/이벤트검증.js` 234행 필수 칸 셋 = 시험 종류·날짜·급수.
- talk `계약/수집_교정_계약.json` 89행 「시험」 다섯 칸(종류·날짜·급수·회차·영역점수).
- talk `lib/시험결과.js` 56행이 최소 단위를 얼린다 — 듣기·읽기·쓰기 셋, 각 0~100 정수.
- **총점도 일부러 안 받는다**: 같은 파일 54~55행이 「영역 합이라 파생이고, 파생을 저장하면 둘이 어긋나는 날 어느 쪽이 참인지 아무도 못 정한다」고 적었다.
- 시트 쪽도 같다. `엔진_셋업확장.js:19` 의 `ACADEMIC_LOG_HEADERS` 는 「값」이 한 칸이고, 그 칸의 뜻은 `Code.js:429` 주석이 「유형 level(급수 1~6)·mock(점수 0~100)」으로 적는다.
- 실제 행 수는 **안 재봤다**(DB 조회 권한 밖).

## ⑧ 되묻기 정렬 — 코드 실재 · 배포판에 실려 있다 · 실행 횟수는 안 재봤다 (09-05)

- **코드**: talk `lib/성향확인.js` `축후보들()` 이 축을 «마지막으로 답한 날»로 정렬한다(고정 우선순위는 동률 깨기로 내렸다 · 09-02 심문 전건판정 ㉮ 「기아」 처방). 그 날짜를 대는 **질의문**은 talk `supabase/functions/progress/index.ts` 223~249행(`e.payload->>'trait_axis'` 로 축별 마지막 답 날) — 정렬은 JS 이고 SQL 은 재료를 댄다. 커밋 = talk `8efa5ad`(2026-09-02 02:00 KST).
- **라이브**: talk `docs/_ops/배포장부.jsonl` 의 배포 도장 = progress **리허설 v67** head `f293bbf3`(09-02 03:35) · **운영 v27** head `ab5a0b41`(09-02 03:45). `git merge-base --is-ancestor 8efa5ad <head>` 둘 다 **참** ⇒ 정렬 코드가 든 판이 둘 다 나갔다. 그 뒤 progress·성향확인 커밋 0 (HEAD = 배포판). 원격 함수 목록(`원격배포.js --목록` · 읽기)도 progress v67 ACTIVE(09-02 03:42Z 갱신).
- **안 재봤다**: 그 질의문이 라이브에서 «몇 번 돌았나»(요청 로그). 실학생 0 인 지금은 리허설 학생 요청뿐이라 재도 뜻이 작다.

## ⑨ ffconcat 재읽기 — 목록 파일은 안 다시 읽고, 목록 «안의» 하위 목록은 매 바퀴 다시 읽는다 (09-05)

자 = 로컬 실험(ffmpeg 8.1.2 · 2초 사인파 셋 A/B/C · `-f concat -safe 0 -stream_loop -1 -re` · 5초에 파일을 바꿔 치우고 debug 로그의 「Opening 'X' for reading」 줄을 셌다 · 바퀴 4초 · 13초 출력). 스크립트·결과 = 이 세션 스크래치 `ffconcat_reread.js` · `ffconcat_result.json`.

| 물음 | 결과 | 셈 |
|---|---|---|
| Q1 `playlist.txt` 자체를 바꾸면(A,B → C) 다음 바퀴에 읽나 | ❌ **안 읽는다** — 열 때 한 번 파싱 | playlist 1회 · A 4 · B 3 · **C 0** |
| Q2 목록의 마지막 항목이 **하위 목록** `tail.ffconcat` 이고 그 파일을 바꾸면(B → C) | ✅ **먹는다** — 닿을 때마다 다시 연다 | tail 3회 · 바꾼 뒤 **C 2** |
| Q3 곡 파일을 같은 이름으로 바꿔 치우면 | ✅ 파일은 바퀴마다 다시 연다 — 내용 교체가 먹는다 | B 3회 열림 |

**뜻**(Reed D8 ③ · 24시간 펼침): «평판 목록 하나 + 루프»면 편성을 바꾸려면 **하루 1회 재시작**이 필요하다(재시작 = 새 video_id · v3 5776). 대신 **«머리 목록 + 하위 목록» 두 층**이면 하위 목록 파일만 바꿔 치워 **재시작 없이** 편성을 갈 수 있다 — v3 5791 ㉡ 의 「재읽기 되면 무재시작」이 이 길로 참이 된다.
**안 재봤다** = VPS 의 ffmpeg 판(다르면 그 판으로 다시 잰다) · 24시간 타임스탬프 누적(v3 5576 · 장시간).

## ⑩ nano-banana 별칭 — 살아 있다 (09-05)

- 자 = AI Studio 문 `models.list` + `models.get`(무료 열쇠 · 조회 둘 · 생성 0 · 돈 0). 모델 54 중 `nano-banana-pro-preview` 있음 · `models.get` 200(displayName 「Nano Banana Pro」 · description 「Gemini 3 Pro Image Preview」 · version 3.0). GA(정식판) 이름 `gemini-3-pro-image` 도 200(같은 displayName · description 「Gemini 3 Pro Image」).
- 그림 모델 여섯 = gemini-2.5-flash-image · gemini-3-pro-image(-preview) · gemini-3.1-flash-image(-preview) · gemini-3.1-flash-lite-image.
- **뜻**: 별칭은 살아 있지만 코드는 이미 GA 이름을 쓴다(`tools/lib/이미지굽기.js` 31 · `SYNK_IMAGE_MODEL || 'gemini-3-pro-image'` · 09-05). «지금 실행 전제»는 GA 이름이다(v3 §5 [틀림 ⑥]). 변경 0.

## ⑪ Reed 곡 물량 — 지금 팩 8벌 68.8분 · 옛 두 수는 «다른 묶음»을 잰 것 (09-05)

- 자 = ffprobe · 실물 = `C:\Users\q1212\OneDrive\Desktop\SYNK 자산\라디오팩`(mp3 10 = 팩 8 + `보류_마린대기` 2) · `…\라디오팩_ts`(.ts 8 + playlist.txt + 재생목록.json). `곡장부.json` 은 두 저장소에 **없다** — 장부 실물은 talk `bots/송출/크레딧.md`(곡 10 행).

| 묶음 | 벌 | 합 |
|---|---|---|
| 지금 팩(01~06 · 09 · 10) | 8 | **4,129초 = 68.8분** |
| 보류(07 · 08 house · 마린 대기) | 2 | 998초 = 16.6분 |
| 전부 | 10 | 5,127초 = 85.5분 |
| 옛 첫 팩(01~08 · 07·08 이 아직 팩 안에 있던 때) | 8 | 4,090초 = **68.2분** ← 기억 `lyria-music-generation-pitfalls` 의 수 |

- 트랙 §0-라디오 「팩 8벌 69분」 = 지금 팩 68.8 의 반올림 · 기억 68.2 = 옛 8벌. **정본 지위**: 손 숫자 둘 다 아니고 **폴더를 ffprobe 로 재는 것**이 자다(v3 6141 「곡 물량을 트랙·memory 에 다시 적지 않는다」와 같은 결).
- **안 재봤다** = VPS 에 올라간 팩(송출 0 이라 아직 없을 수 있다).

## ⑫ `portfolio_moments` → talk 통로 — 0 (09-04)

표는 실물이다. `Code.js:1059` 가 탭 이름과 14칸 헤더를 든다. 생산자는 `엔진_폼리포트.js:5196` 의 `momentSweep_` 이고 21시 배치가 부른다.

소비자 넷이 **전부 이 저장소 안에서 끝난다**:
- `엔진_폼리포트.js:5261` `momentRowsView_`
- `엔진_폼리포트.js:5283` `momentBoardRows_`
- `Code.js:2215` `momentLine_` (걸어온길 카드 2202행 · 오늘알림 3158행이 부른다)

건너가는 코드를 찾은 자리: talk 전 폴더에 `portfolio_moments` 0건 · `moment_id` 0건. 이 저장소 `*.js` 에서 `supabase` 는 시험·도구 둘뿐이고 엔진 호출 0.

라이브 시트 실제 행 수는 **안 재봤다**.

## ⑬ topik.go.kr 원문 — 「성적 발표일로부터 2년간 유효」 확인 (09-05)

- 원문(교육부 공고 제2025-317호 「2026년 한국어능력시험 토픽 I, 토픽 II, 토픽 말하기 평가 시행계획 공고」 · 2025-10-10 교육부장관 · PDF 12쪽 · **7쪽**):
  「**나. 시험 성적은 성적 발표일로부터 2년간 유효합니다.**」
  「다. 성적증명서는 홈페이지를 통해서 발급하며 우편, 팩스 및 이메일 등으로 발송하지 않습니다. 단, 국외의 경우 시행기관의 요청이 있을 시 응시자 1인당 1매의 성적증명서를 …」
- 출처 = https://exam.topik.go.kr/nasdata/webnas/raonkeditordata/BBS//2025/10/20251031_093545671_74607.pdf (topik.go.kr 도메인 · 09-05 받아 pypdf 로 읽었다).
- HTML 안내 페이지(`www.topik.go.kr/TWGUID/TWGUID0060.do`)는 본문이 스크립트로 그려져 글을 못 받았다 — **안 재봤다** = 그 페이지의 문장(공고가 상위 문서라 판정은 선다).
- **뜻**: v3 §7 Trail 의 「2년 창 = 미확인 · low」 → **확인 · 기준일 = 성적 발표일**. 정본에는 델타 두 블록으로 옮겼다(정본 수리 ㉮ 와 같은 델타 파일).

## ⑭ 시즌 «식별자» 규격 — 커리큘럼 정본엔 없다 · 딴 자리에 둘이 있고 어긋난다 (09-04)

- `docs/커리큘럼_정본_v1.md` 에서 「시즌」이 나오는 줄은 31행 하나뿐이고, 내용은 진도 구조(1권 = 1시즌 8주 = 40차시)이지 식별자 규격이 아니다.
- 코드가 그 사실을 이미 알고 적어 두었다. `엔진_운영배치.js:164` 원문: 「`마지막_완료_시즌ID` 는 **일부러 없다** — 커리큘럼 정본에 시즌 «식별자» 규격이 없다 … 규격 없이 적으면 뜻 모를 숫자가 쌓인다」.
- 🔴 **딴 자리에는 둘이 있다.** talk `supabase/L0_스키마.sql` 7404~7405행이 `season_id` 를 uuid 로, `code` 를 사람이 부르는 이름표(`'2027-S1'`)로 박았다. 형식 검사는 안 걸려 있다(유일·비움불가만).
- 🔴 **그리고 길이가 서로 다르다** — 정본 수리 ㉰(09-05 에 고쳤다 · 답은 달력 8주).
- 실제 쓰이는 값 둘: 엔진 시즌 라벨은 시작일 `yyyy-MM-dd`(`엔진_셋업확장.js:2235` 의 `seasonLabelOf_`) · talk 쪽은 `'2027-S1'` 꼴.

**뜻**: 걸음 1 의 선행 조건이 「규격 한 줄」인데, 답은 「없다」가 아니라 **「둘이 있고 어긋난다」**다. 하나로 세우는 것이 걸음 1 의 실제 일이다. 실행 조건까지는 ⑮ 가 잰다.

## ⑮ 시즌 식별자의 «실제 실행 조건» — 시트 ↔ DB ↔ 파일럿 대조표 (09-05)

| 축 | 시트 엔진(이 저장소 `엔진_셋업확장.js`) | DB(talk) | 어긋남 |
|---|---|---|---|
| 시즌 이름 | `seasonLabelOf_` = 시작일 `yyyy-MM-dd`(2235) · 원천 = `app_state` key `시즌시작일`(`seasonStartOf_` 2187 · `setSeasonStart` 2208) | `engine.season.code` text unique(`'2027-S1'` 꼴 · 형식 검사 0) + `season_id` uuid(season_c11) | ⓐ 이름 규격 둘 · 서로 잇는 코드 0 |
| 길이 | **코드 상수 `SEASON_WEEKS = 8`**(2164) — 끝 = 시작 + 8주 − 1일(2210) | 행 데이터 `starts_on`·`ends_on`(null = 진행 중) · 「주기는 데이터이지 상수가 아니다」(season_c11 주석) | ⓑ 같은 값을 두 곳이 안다 · 시즌회고_설계 「🚫 코드 상수」와 시트 엔진이 어긋난다 |
| 「지금 시즌인가」 | `lessonNoOf_`: 시작 «전» = 0 · 시작 + 8주 + 14일 지나면 0(2242~2248) · `seasonWeekOf_` 는 상한 없음(2259~2265) | teach 665~668: `starts_on <= today and (ends_on is null or ends_on >= today)` · 없으면 `SEASON_NOT_OPEN`(247) · 겹침은 gist 제약으로 막는다 | ⓒ 시트는 «상한 10주» · DB 는 «열림(ends_on null)» — 끝의 정의가 다르다 |
| 시즌 밖이면 | 인쇄 멈춤 「시즌 기간 밖입니다(setSeasonStart 확인)」(3580 · 3665) | 나침반·회고 통로 닫힘(SEASON_NOT_OPEN) | — |
| 교재 칸 | 없음 | `textbook text not null`(season_c11) — 결정 09-04 「교재 칸 필수값 해제」 뒤 **마이그 0**(`drop not null` 없음) | ⓓ 결정은 섰고 코드는 안 따라왔다(걸음 1) |
| 파일럿·체험(12-07~12-18 · 2월 2주) | 정식 시즌1 시작 = 2027-02-25 ⇒ 둘 다 「시즌 기간 밖」= 종이 0(`docs/파일럿_체험2주_설계_v1.md` §A-3-3) · 갈래 셋 중 권고 ⓑ 「리허설 시즌 라벨 한 갈래」(코드 한 벌 · 10월 안) | 파일럿 시즌 행을 넣으려면 `code`·`textbook` 필수 · 겹침 제약이라 `ends_on` 을 박아야 정규 행이 선다 | ⓔ **갈래 ⓑ 는 결정 원장·트랙에 0건 — 미결**(설계 권고 · 걸음 1 에서 세운다) |

- 「정규 시즌 시작일을 바꿨다 되돌리는 식」(갈래 ⓐ)은 차시 해석을 갈라 놓는다(그 문서가 「위험」이라 적었다) — 쓰지 않는다(v3 ⑮ 원문과 같다).
- 이 걸음은 «잰다»까지. 하나로 세우는 것(규격 한 줄 · 상수 걷기 · 교재 칸 해제 · 리허설 라벨)은 걸음 1.

## ⑯ 처리량 자 — 장부가 섰다 (09-05)

`docs/_ops/처리량_장부.md` · 첫 표본 = 이 걸음 0(읽기 부분 · 세션 3 · 돈 0 · 재작업 0) · 대표 작업 셋의 칸은 비워 두었다(그 일을 할 때 적는다). 가용 작업일은 안 재봤다.

---

## 정본 수리 — 6건 처리 결과 (09-05 저녁)

| # | 무엇 | 결과 |
|---|---|---|
| 1 | 결정.md 「자기 3년 궤적 평생 열람」 행 grep 병기 | ✅ 근거 칸에 정정 병기(유호 확정 문장은 그대로 · `grep -o '3년 궤적'` = 원문 1 + 병기 1 · 근거 = 결정 09-02 「책임의 눈금 = 1년에 TOPIK 4급」). v3 §7 U8 이 말한 「08-25 정정 행」은 결정 원장에서 **못 찾았다**(grep) — 그래서 09-02 행을 근거로 댔다 |
| 2 | 시즌회고_설계 「회고 표는 없다」 | ✅ 09-05 저녁에 이미 고쳐짐(`grep '표.*없다'` 머리말 = 0) · 「실 DB 적용」은 ⑤ 가 닫았다 |
| 3 | 소개서 낡음 통보 6건 | ⏭ **걸음 19(착지 커밋)로 이월** — 09-05 채택 D18 A 「소개서 ⑦⑩ 은 걸음이 착지할 때 같은 커밋에서」 |
| ㉮ | v3 §7 Trail 「문항별 정오」 자기모순(구조 = 넣는다 · 걷어내는 것 = 뺀다) | ✅ 델타(`docs/_ops/엔진7종_v3/델타_걸음0_㉮.txt` · 적용기 `tools/엔진7종_델타적용.js`) — 「구조」 줄에 시제 «짓는다(미래 · topik_mock 에만 · 지금 실물 0)»를 못 박고, 「걷어내는 것」의 「뺀다」는 «지금 official payload» 라고 갈랐다. 같은 델타에 ⑬ 「2년 창 확인」 두 블록을 실었다 |
| ㉯ | `docs/학생ID_종단_설계.md` 205·352행이 실물보다 뒤처져 말한다 | ✅ 7칸·`종료사유`·`엔진_운영배치.js` 정의로 정정 · 처방 표 세 줄에 ✅실재/⏳ 표식 · §6 「지금」 줄의 소급 불가 목록에서 ㉡ 을 뺐다(`마지막_완료_시즌ID` 만 규격 뒤) |
| ㉰ | `docs/시즌회고_설계.md` 「한 시즌 = 2달 ✅유호 확정」 vs 「달력 8주」 | ✅ 결정 원장 09-04 「시즌의 끝을 «교재 1권»에서 «달력 8주»로 옮긴다」(08-12 「2달」을 대체)로 276행·289행을 고쳤다 — **유호님께 다시 묻지 않았다**(답이 원장에 있었다) · 다른 「2달」 언급(139·155·193행 …)은 근사 문맥이라 두었다 · ⑮ⓑ 의 `SEASON_WEEKS = 8` 상수도 289행 옆에 적었다 |

### 배경 — 09-04 에 잡힌 셋(왜 수리 대상이 3→6 이 됐나)
- ㉮ 같은 U 안에서 열세 줄 사이에 두 줄이 반대로 섰다(v2 3641행 「구조 ⓐ 문항별 정오는 topik_mock 에만」 ↔ 3652행 「걷어내는 것 payload 문항별 정오」). ⑦ 실측(실물 0)으로 「뺀다」가 현행이고 「구조」는 «세우는 것»이라 시제로 갈랐다.
- ㉯ 205행 「5칸 · 왜 나갔는지가 없다 · `엔진_셋업확장.js:70`」 셋이 다 틀렸다 — 그 줄은 주석이고 정의는 `엔진_운영배치.js:168`(7칸) · `종료사유` 와 `EXIT_REASONS_` 여섯(결정 08-30)이 서 있었다. 🔑 「소급 불가」로 적힌 자리가 이미 닫혀 있었다.
- ㉰ 276행 「2달 ✅유호 확정」과 287행 「달력 8주」가 한 문서에서 갈렸다. 09-04 쪽이 나중이고 결정 원장이 그 뒤집기를 08-12 대체로 명시하고 있어 유호님께 되묻지 않고 고쳤다.
