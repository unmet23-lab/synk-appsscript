# 자개(나전) 캐러셀 표지 — 갈림 2안 생성 프롬프트 (v3 · 2026-08-14)

> **유호 아이디어 08-14**: 「캐러셀 첫 시작이나 마지막에 «자개» 바탕으로 구성해보고 싶다」 → 판정 = 좋다,
> 단 **「바탕」이 아니라 「나전(인레이)」으로**. 유호 지시 = 「이거 다 적용해서 시안 2장 · 비교되게」.
>
> **왜 자개가 이 자리에 맞나** — 08-14 접수한 레퍼런스 3벌의 공통 문법이 *순흑 다크 무대 + 실물 재질 렌더
> 오브 1점 + 반투명*이었다(메모리 `brand-mood-axis-0814`). 자개는 그 셋을 그대로 만족한다: 검은 옻칠이
> 순흑 무대고, 자개가 반투명 간섭광 오브다. **재질 픽(글래시 젤리 / 펠트)이 어느 쪽으로 나도 안 싸우는
> 유일한 후보** — 유리 쪽이면 같은 광학 가족, 펠트 쪽이면 「무른 재질 위 단단한 광택 1점」으로 대비가 선다.
> 그리고 한국어 학원인데 지금 시각 언어에 「한국」이 0이다. 자개는 설명 없이 한국을 나르고, 몽골 학생에겐
> 자개장 문화가 없어 **「전통」이 아니라 「처음 보는 광택」**으로 읽힌다(신선함 축).

## 🔴 1차 실패 — 유호 판정 「그냥 유리가 깨져있는 것 같다」 (실물 = `자개_0814/1차_실패_흩어진파편.png`)

**유호님이 실물을 보고 기각했고, 판정이 정확했다.** 나온 그림은 자개가 아니라 **검은 테이블에 유리 조각을
흩뿌린 사진**이었다. 내 프롬프트가 세 자리에서 자개를 죽였다 — 셋 다 내 설계 결함이다.

| # | 무엇이 잘못됐나 | v3 처방 |
|---|---|---|
| ① **흩어짐** | 내가 낸 「조각이 모여 문양이 된다」 서사에 맞추려고 조각을 떼어놨다. 자개의 아름다움은 조각이 아니라 **조각들이 맞물려 이룬 문양**에 있다 — 흩어진 나전은 「예쁜 자개」가 아니라 **작업 중인 재료**, 곧 깨진 유리다. **서사가 재질을 이겼다.** | 맞물린 **하나의 연속 곡선**. `NOT shattered / NOT loose pieces lying on a table` 를 명시 네거티브로 |
| ② **색 제한**(가장 뼈아픔) | 브랜드색과 안 싸우게 하려고 「청록–은–연보라만 · 금빛 금지」로 좁혔는데, **그게 자개의 생명인 무지개 간섭광을 없앴다.** 실물이 탁한 청록 유리로 나온 결정타. | **간섭광 해방** — 분홍·초록·파랑·은빛이 각도 따라 흐르게. 충돌은 **면적으로** 통제 |
| ③ **상감 누락** | 「박혀 있다」를 안 써서 옻칠 «위에 올려둔 재료»가 됐다 — 공예가 아니라 부품으로 읽힌다. | `inlaid INTO / flush / polished to the same mirror plane / never resting on top` + 빛을 낮은 각도로 스쳐 |

🔑 **②의 교훈이 이 트랙 밖으로 나간다** — 색 충돌을 **색으로** 막으려 한 게 오판이었다. 올바른 처방은
**면적 제한**이다. 「강렬함의 원인은 색이 아니라 면적」은 캐러셀에서 이미 실측해 둔 결론인데
(메모리 `mascot-cherry-jelly-kit` 파생 산출물 절) 이 자리에서 안 썼다. 재질의 본질을 깎아 충돌을 피하면
충돌은 사라지지만 **그 재질을 쓸 이유도 같이 사라진다.**

## 갈림 — 변수는 **재질 해석 하나**뿐

| | A안 · 정통 나전 | B안 · 자개 × 젤리 셸 |
|---|---|---|
| 무대 | 순흑 옻칠판 | 순흑 무대 |
| 자개가 있는 곳 | 옻칠에 **박힌**(상감) 문양 | 반투명 셸 **안에** 봉인된 문양 |
| 계보 | 실물 나전칠기 문법 그대로 | **도전안 선반영**(레퍼런스 분해 ①«안이 보인다» · 패키지 §5 encased 문법) |
| 회수 | 캐러셀 표지·마감 2장 | 앱·SNS·굿즈 전부에 같은 언어가 깔린다 |
| 대가 | 세계관 확장은 안 됨(표지 장식에서 멈춤) | 재질 픽 전이라 셸을 다시 구우면 두 번 일할 수 있음 |

⚠ **나머지는 전부 같게 굽는다**(구도·조명·문양·글자 여백) — 조건을 둘 이상 바꾸면 모르는 채
「알아냈다」고 쓰게 된다(CLAUDE.md 판정·검증). 눈으로 갈리는 것이 재질 해석 하나여야 픽이 선다.
한 응답에 둘을 같이 굽게 해서 **시드까지 공유**시킨다.

## 서사 — 「부분 → 전체」 (v3에서 수정)

구안은 「흩어진 조각 → 모임」이었는데 그 첫 항이 곧 1차 실패의 원인이었다. **자개 문법을 안 깨면서
같은 서사를 내는 형태로 바꾼다**:

- **첫 장** = 완성된 곡선 문양의 **일부**가 화면 아래 1/3에서 흐르고, 프레임 밖으로 이어진다(이어질 것을 암시).
- **마지막 장** = 그 곡선이 **닫힌 하나의 형태**(원 · 로고 `<` · 마스코트 셸 실루엣)로 완성된다.

낱낱이 모여 하나가 된다는 뜻은 그대로 살고, 어느 컷에서도 「깨진 상태」가 화면에 안 나온다.
▶ **이번 2컷은 둘 다 첫 장(부분)** — 마감 컷은 픽이 난 뒤 그 안의 짝으로 굽는다(픽 전에 4컷 구우면 두 번 일한다).

## 참조 이미지를 **안 넣는다** (판단 근거)

명품판 통로는 「마스코트 렌더를 참조로 넣으면 재질 일치 ◎」였지만 이번엔 안 넣는다 —
① 색이 딸려온다(색은 지금 **가결이지 확정이 아니고**, 판단 근거로 쓰지 말라는 유호 지시 08-14)
② 클립보드 붙여넣기가 유일 참조 통로인데 유호님이 클립보드를 실사용 중이라 덮어쓰기 사고 벡터다
③ 이번 대조의 변수는 재질 해석이지 색이 아니다.
→ 대신 셸 재질을 **문장으로** 명품판 패키지와 같은 가족에 맞춘다(`glass-jelly, glossy wet, soft internal glow`).

## v3 프롬프트 (한 덩어리 · A·B 동시)

```
Generate TWO separate square images (A and B) in one reply. They must share IDENTICAL
composition, camera angle, lighting and motif. The ONLY difference is the material
interpretation.

COMMON SPEC: Square 1:1 composition. Cinematic studio product photography of a fine
Korean craft object, ultra high resolution, quiet luxury, extreme restraint. Light from
the upper left, soft and single-source, raking across the surface at a low angle so the
nacre lights up. The upper 55% of the frame is empty pitch-black negative space reserved
for a title. Black dominates the frame; the inlay occupies only the lower third, small
and jewel-like. No text, no letters, no logo, no watermark, no face, no people.

THE MOTIF (identical in both): a refined flowing curved motif of mother-of-pearl inlay in
the tradition of Korean najeon lacquerware. The pieces are precisely cut and fitted edge
to edge so they form ONE continuous ribbon-like curve, like a single calm brushstroke or
a drifting cloud line. CRITICAL - this is a finished museum-quality inlay: intact,
deliberate, every piece belonging to the pattern. Absolutely NOT shattered glass, NOT
broken shards, NOT cracks, NOT scattered debris, NOT loose pieces lying on a table.

THE MATERIAL: real nacre with its full living iridescence - the shimmer shifts along the
curve through soft pinks, greens, blues, silvers and pale violets as the angle changes,
with that deep watery depth real mother-of-pearl has. Let it be genuinely beautiful and
luminous, the way an antique najeon box catches light.

IMAGE A - traditional najeon lacquer: the motif is inlaid INTO a pitch-black Korean
lacquer surface (ottchil), deep and softly reflective like still black water. The nacre
sits perfectly flush with the lacquer, polished to the same mirror plane, embedded and
seamless - inlaid, never resting on top.

IMAGE B - the same motif encased in jelly: the identical nacre curve is suspended INSIDE
a single translucent colourless glass-jelly form floating in the lower third against the
same pitch-black stage - glossy and wet-looking with a soft internal glow, small bright
specular highlights, no ground shadow. The curve is seen through gentle refraction,
intact and continuous, sealed like a ribbon in clear ice.
```

## ✅ v3 결과 — 「깨진 유리」 탈출 · **유호 확정 = A 갈래** (실물 = `자개_0814/v3_AB대조.png`)

Gemini 가 A·B 를 **한 장의 4분할 시트**로 냈다(요청은 2장 · 대조용으로는 오히려 편하다).

- ✅ **원인 ①③ 해소** — 하나로 이어진 곡선 문양이 나왔고, A 는 옻칠에 박혔고 B 는 젤리 안에 봉인됐다. 파편은 사라졌다.
- ✅ **유호 확정 08-14 = 「A로 가되 문양 밀도 올려서 한 번 더」** → B 갈래(젤리 셸) 종료.
  🔑 내 판정도 A 였다 — **B 의 껍질이 젤리가 아니라 «비눗방울»로 나왔다**(얇은 물집 막). 마스코트의 쫀득한 젤리와 다른
  가족이라 고급감이 떨어지고, «안이 보인다» 문법의 값(수집물이 셸 안에 쌓인다)도 이 얇은 막에서는 안 선다.
- 🔴 **②(간섭광)는 v3 에서도 안 풀렸다** — 색 제한을 풀었는데도 은빛+옅은 분홍에 그쳤고, 문양이 곡선 하나뿐이라
  **나전 공예가 아니라 스티커**로 읽혔다.

### 🔑 v4 재진단 — 「광택 약함」과 「밀도 없음」은 **같은 문제**였다
자개의 무지갯빛은 **조각마다 나전 결(grain) 방향이 달라서 조각별로 다른 색이 터지는** 현상이다.
v3 의 곡선은 **통짜 한 조각**이라 색도 하나일 수밖에 없었다 — 즉 밀도를 안 준 것이 광택을 죽인 원인이고,
둘은 별개 항목이 아니었다. **조각을 수십 개로 쪼개면 둘이 동시에 풀린다.**

### v4 프롬프트 (A 단독 · 1장)
```
Generate ONE square image.

Square 1:1 composition for an Instagram carousel cover. Cinematic macro product
photography of a museum-quality Korean najeon lacquer object, ultra high resolution,
quiet luxury, extreme restraint.

THE SURFACE: pitch-black Korean lacquer (ottchil), deep and softly reflective like still
black water. The upper 55% of the frame is empty black negative space reserved for a
title. Black dominates the frame.

THE INLAY: across the lower third, a flowing cloud-and-wave motif inlaid in
mother-of-pearl. CRITICAL - the motif is built from MANY individually hand-cut pieces of
shell: dozens of small shaped fragments fitted together edge to edge like a fine mosaic,
their seams visible as delicate hairlines. This density is the whole point - it must read
as painstaking handcraft, never as one flat sticker shape. Every piece is set perfectly
flush with the lacquer and polished to the same mirror plane.

THE LIGHT: raking light from the upper left at a very low angle. Because each piece is
cut with its nacre grain running a different way, EACH PIECE FLASHES A DIFFERENT COLOUR -
one fragment glows green, its neighbour rose-pink, the next blue-silver, another pale
gold - so the whole motif shimmers as a mosaic of shifting iridescent colour with deep
watery depth. Intense, alive and jewel-like, the way an antique najeon box erupts with
colour when it catches the light.

No text, no letters, no labels, no watermark, no people. Nothing shattered or broken -
every single piece belongs to the pattern.
```
⚠ v3 에서 A/B 라벨 글자가 이미지에 박혔다(`no text` 를 썼는데도) — 대조 시트라 라벨이 붙은 것으로 보이며,
1장 단독 요청인 v4 에서는 재발 여부를 검수 축에 넣는다.

## 🔴 접는 기준 (미리 못 박는다)

v3 에서도 「깨진 유리」로 나오면 **자개는 접는다.** 원인 셋을 다 고쳤는데도 안 나온다면 그건 프롬프트
문제가 아니라 **생성 모델이 나전을 못 만든다**는 뜻이고, 그러면 실물 자개 촬영이나 벡터 작화라는
훨씬 비싼 길만 남는다 — 캐러셀 표지 하나에 치를 값이 아니다.

## 통로 실측 (2026-08-14 · 다음 세션은 이걸로)

- 생성 = **Gemini 웹**(유호 Pro · 지출 0 · 유호 픽 08-14). 반입 = `docs/캐릭터/자개_0814/`
- 🔑 **입력은 뚫린다 — `document.execCommand('insertText', false, TEXT)`**. Quill 정상 경로를 통과해
  Angular 모델까지 들어간다. 🚫안 되는 것: `computer type`·`computer key`(CDP) · 합성 `KeyboardEvent` ·
  DOM 직접 주입(+ `innerHTML` 은 **Trusted Types** 로 막힘 — `replaceChildren` 을 쓴다) · 합성 `paste`.
- 🔴 **전송은 못 뚫는다 — 사람 손이 필요하다.** 프로그램 `click()` · **CDP `left_click`** · CDP Enter 전부
  무반응(전건 실측). 근본원인 = Gemini 가 전송에 **신뢰된 사용자 제스처(user activation)** 를 요구하고,
  입력은 그걸 안 요구해서 층이 갈린다. 이전 세션들이 「⏳유호 = Gemini 전송 클릭」에서 멈춘 것과 같은 벽인데
  이번에 **이유를 특정**했다. → 통로 설계 = **AI 가 프롬프트를 세팅 · 유호님은 Enter 한 번**.
- 🔑 **수거 = canvas 경유 앵커 다운로드**: `drawImage` → `toBlob` → `a[download].click()` → Downloads.
  🚫 `fetch(blob:)` 는 실패한다(`Failed to fetch` 실측) — 옛 기록의 「blob 앵커」보다 이 경로가 확실하다.
- AI Studio(`aistudio.google.com`)는 **주입·전송 모두 자동화 가능**하지만 `Paid` 표시 = 과금
  (이미지 ≈ 장당 55원). 유호 픽 08-14 = **지출 0 통로 유지**.

## 수거·검수 규격

- 검수 4축: ①검정 지배(자개가 «면»이 아닌가) ②상단 55% 글자 자리가 실제로 비었나 ③문양이 **하나의 곡선**으로
  읽히나(파편이면 실패) ④자개가 **박혀** 있나(위에 놓였으면 실패)
- 발행 전 게이트 = 몽골어 병기 · 말가이 검수(캐러셀 공통 규약)
