<!-- 이 문서의 자리 = 「셋의 몸이 몇이고 어떤 비율인가」 하나. 유호 지시 2026-09-08 「마스코트 설계를 숫자로 적어두는 거 지금 해줘」.
     ⚠ 겹치지 않게 가른다 — 여기서 다시 쓰지 않는다:
       · 누구인가(성격·말투·관계·혼잣말) = docs/캐릭터/가이드_정본.md
       · 언제 어떤 표정이 뜨는가 = docs/캐릭터/캐릭터_생명감_설계.md
       · 표정 «어휘»(이름·컷) = docs/디자인_토큰.json 「캐릭터」 축
       · 브랜드 색 «킷» = docs/디자인_토큰.json 「색」 축 (여기는 그 킷과의 «거리»만 든다)
       · 그림 파일 경로 = tools/lib/마스코트자산.js
     이 문서가 지는 것 = 몸의 치수 · 비율 · 눈의 기하 · 부분 색 · 재질 판정 · 안 잰 것. -->

# 마스코트 치수 정본 v1 — 몽글 · 까몽 · 마린

> v1 · 2026-09-08 · 유호 지시 「마스코트 설계를 숫자로 적어두는 거 지금 해줘」
> **왜 숫자인가** — 그림 파일은 눈앞에 화면을 씌우는 기계(VR)로 넘어가지 않는다. 넘어가는 것은 **비율·색·눈의 규칙**이다. 그리고 그전에도 값이 있다: 다시 구울 때 «같은 아이»가 나왔는지 **재서** 알 수 있다.
> **다시 뽑는 자** = `python tools/마스코트치수재기.py`(그림까지 보려면 `--그림 <폴더>`). 이 문서의 숫자는 그 자가 낸 값이다.
> 🆕 **09-08 갱신 ①** — §3 판정 2 가 닫혔다(킷 밖으로 나갔던 셋을 킷 `09 Character` 로 올렸다 · 유호 확정). §7 에 남은 것은 하나다.
> 🔴 **09-08 갱신 ② — 눈 자를 고쳤다. §2 의 숫자가 통째로 바뀌었다.** 유호님이 지면에서 「몽글이 눈이 짝짝이로 체크돼 있다 · 까몽이 직사각형이 이상하다 · 마린 오른쪽 상자가 왼쪽으로 와야 한다」를 잡으셨고, 셋 다 맞았다. 밟은 함정은 §0 아래에 적어 뒀다. **09-08 낮 판의 §2 숫자는 쓰지 마라.**
> ⚠ **여기 있는 것은 «잰 값»이지 «정한 값»이 아니다.** 지금 구워져 있는 그림에서 뽑았다. 어느 것을 앞으로 지킬지는 §6 이 묻는다.

---

## §0. 잰 방법 — 자를 먼저 밝힌다

| | |
|---|---|
| **표본** | `docs/캐릭터/정본_4K/<이름>_본체.png` 셋. 4096×4096 그림. |
| **몸의 경계** | 투명도 128 초과인 화소. 그림자·잔털은 투명도가 낮아 자동으로 빠진다. |
| **눈을 찾는 자** | 셋이 **다르다**. 몽글 = 밝기 70 아래 **그리고 붉은기(r−g)가 40 아래**(검은 구슬) · 까몽 = 초록이 도는 화소 전부(홍채 고리) · 마린 = 노란 화소(렌즈). 셋 다 **가장 큰 덩어리 둘**만 눈으로 친다. |
| **색의 거리** | 사람 눈에 가까운 색 공간에서 잰 차이(ΔE, CIE76). **2 아래면 구별하기 어렵고, 5 아래면 같은 색 계열, 10 을 넘으면 다른 색으로 보인다.** |
| **못 미더운 자리** | §5 에 「안 쟀다」로 모아 뒀다. |

### 🔴 09-08 에 밟은 함정 셋 — 유호님이 지면에서 잡으셨다

| 무엇이 틀렸나 | 왜 그랬나 | 어떻게 고쳤나 |
|---|---|---|
| **몽글의 오른쪽 눈이 44% 크게 잡혔다** | 밝기만으로 찾으니 **눈 둘레의 그늘까지 눈으로 삼켰다.** 오른쪽 눈은 그늘이 더 넓어서 상자가 더 커졌다 | 가르는 자는 밝기가 아니라 **붉은기**다 — 구슬은 r−g ≈ 2 이고 코랄 그늘은 r−g ≈ 116 이다. 새 자로 재니 두 눈 차이가 **9.7%** 로 줄었다 |
| **까몽의 눈이 실제의 3분의 2로 잡혔다** | 초록 문턱이 높아 **홍채 고리의 밝은 쪽만** 걸렸다. 그래서 상자가 눈 한쪽에 붙은 좁은 사각형이 됐다 | 초록 문턱을 낮춰 고리 전체를 잡는다. 눈 폭이 169 → **259 화소**가 됐다 |
| **마린의 오른쪽 상자가 오른쪽으로 밀려 보였다** | 숫자가 아니라 **지면 문제**였다. 잴 때는 「투명도 128 초과」로 몸을 잘랐는데 그림은 「투명도 0 초과」로 잘라, 그림이 79 화소 더 넓었다. 그 위에 백분율로 그리니 상자가 통째로 밀렸다 | 그림도 **같은 기준**으로 자른다 |

🔑 **남는 교훈**: 자가 틀리면 «숫자가 이상하다»가 아니라 **«그림이 이상하다»로 먼저 보인다.** 그래서 잰 값 옆에 늘 그림을 같이 낸다.

---

## §1. 몸 — 셋의 실루엣

| | **몽글** | **까몽** | **마린** |
|---|---|---|---|
| 몸 폭 × 높이 (화소) | 3138 × 3086 | 3236 × 3052 | 2028 × 3233 |
| **가로세로비** | **1.02** — 거의 정사각 | **1.06** — 가로로 살짝 넓다 | **0.63** — 세로로 길다 |
| **가장 넓은 자리** | 아래에서 **바닥 쪽(위에서 90~95%)** | 가운데 **날개(위에서 60~65%)** | 머리 **투구(위에서 30~35%)** |
| 실루엣 한 마디 | 아래로 퍼지는 종 | 가운데가 벌어진 십자 | 위가 무거운 못 |

🔑 **셋의 실루엣이 서로 다른 자리에서 넓어진다.** 이것이 멀리서 봐도 셋이 갈리는 까닭이고, 입체로 다시 지을 때 **가장 먼저 지켜야 할 한 줄**이다. 얼굴을 지워도 이 셋은 안 헷갈린다.

---

## §2. 눈 — 셋의 «눈 문법»이 숫자로 갈린다

> 🔴 아래는 **09-08 저녁에 자를 고친 뒤의 값**이다(§0 아래 함정 표 참조).

| | **몽글** | **까몽** | **마린** |
|---|---|---|---|
| 잰 것 | 검은 구슬 | 초록 홍채 고리 전체 | 노란 렌즈 |
| 눈 폭 (화소) | 313 | 259 | 503 |
| **눈 폭 ÷ 몸 폭** | **0.100** | **0.080** | **0.248** |
| 세로 ÷ 가로 | 1.03 | 1.12 | 1.16 |
| **두 눈의 폭 차이** | 9.7% | 4.5% | **0.8%** |
| **눈 사이 ÷ 몸 폭** | 0.338 | 0.244 | **0.482** |
| 눈 사이 ÷ 눈 폭 | 3.38 | 3.05 | **1.94** |
| **두 눈 틈 ÷ 눈 폭** | 2.35 | 1.79 | **0.95** |
| **눈의 높이**(위에서) | 34.2% | 28.9% | **40.5%** |
| 두 눈 높이 어긋남 | 6 화소 | 11 화소 | 11 화소 |

🔑 **한 줄로 읽으면**: 눈 크기는 **마린 ≫ 몽글 > 까몽** 순이다. 갈리는 자리는 크기보다 **붙어 있는 정도**다 — 마린은 두 눈 틈이 **눈 하나보다 좁고**(0.95), 몽글·까몽은 눈 두 개쯤 벌어져 있다(2.35 · 1.79). 얼굴의 인상이 여기서 갈린다.

⚠ **몽글의 두 눈은 실제로 조금 다르다**(폭 297 · 329 화소 = 9.7% 차이). 그림이 살짝 비스듬해서 오른쪽 눈이 더 가깝기 때문이다. 마린은 0.8% 라 사실상 같다.

### 반짝임 — 마린만 «무광»이다

| | 몽글 | 까몽 | 마린 |
|---|---|---|---|
| 흰 반짝임 | **있다** — 지름이 눈 폭의 **15.8~18.9%** · 자리는 눈 안에서 가로 29~31% · 세로 25~26%(왼쪽 위) | **있다** — 지름 **21.9~26.1%** · 세로 26~28% | **없다** |
| 읽히는 재질 | 유리 구슬 | 유리 구슬 + 초록 홍채 | **무광 펠트 원반** |

🔴 **셋의 눈은 «재질»이 갈린다.** 마린의 눈만 빛을 안 튕기는 펠트 조각이고, 몽글·까몽은 빛을 튕기는 구슬이다. 입체로 다시 지을 때 이걸 모르면 셋이 같은 눈을 달고 나온다.
⚠ 09-08 낮 판에는 「까몽은 반짝임이 안 잡혔다」고 적혀 있었는데, 그건 눈을 좁게 잡아서 반짝임이 상자 밖에 있었기 때문이다. **까몽에도 반짝임이 있다.**

---

## §3. 색 — 잰 값과 브랜드 킷의 거리

| 잰 자리 | 잰 색 | 가장 가까운 킷 | 거리 | 읽기 |
|---|---|---|---|---|
| 몽글 밝은 면 | `#F99480` | Coral 2 `#FD9C87` | **3.1** | ✅ 킷 위 |
| 몽글 그늘 | `#A73A36` | Coral 3 `#AE322A` | **8.9** | ✅ 킷 램프 안 |
| 몽글 밑단 실 | `#FBCAAA` | Coral Soft `#FBB7A3` | **10.6** | ⚠ 아래 참조 |
| 까몽 어두운 털 | `#271B1C` | Ink `#2B2320` | **4.7** | ✅ 킷 위 |
| 까몽 밝은 털 | `#986D60` | Ash Wool `#8D857A` | **17.0** | ✅ 킷 승격 → **Chestnut** |
| 까몽 홍채 | `#799459` | Meadow Deep `#3F6B2E` | **18.8** | ✅ 킷 승격 → **Moss** |
| 마린 투구 | `#283158` | Lapis Deep `#24448C` | **21.6** | ✅ 킷 승격 → **Indigo** |
| 마린 렌즈 | `#F5CD58` | Butter `#F5C445` | **6.9** | ✅ 킷 위 |
| 마린 몸 | `#CBB7A8` | Stone `#C7BFB2` | **5.3** | ✅ 킷 위 |

**판정 셋**

1. ✅ **몽글은 브랜드 코랄 위에 그대로 서 있다.** 밝은 면 3.1 · 그늘 8.9 로, 킷의 코랄 램프 2~4 사이에 정확히 앉는다. 마스코트와 화면이 같은 색을 쓰고 있다는 뜻이다.
2. ✅ **킷 밖으로 나갔던 셋은 09-08 에 킷으로 올라갔다** (유호 확정 「킷 밖으로 나간 색 셋은 킷에 새로 올리자」). 새 팔레트 **09 Character** 에 이름을 달았다 — **Indigo**(마린 투구 `#283158`) · **Moss**(까몽 홍채 `#799459`) · **Chestnut**(까몽 밝은 털 `#986D60`). 킷은 42 → **45색**이 됐고 개수 잠금(`tests/브랜드값.test.js`)과 화면 변수(`docs/tools/synk-tokens.css`)를 같은 커밋에서 갱신했다.
   - 🔴 **이것은 08-13 판정을 좁힌 것이다** — 그때 「캐릭터 색은 킷이 아니다」(마리오 빨강이 닌텐도 UI 색이 아닌 문법)로 정했었다. 09-08 확정은 **까몽·마린 셋에만** 걸리고, **몽글의 체리 램프는 그대로 킷 밖**이다.
   - ⚠ **자가 둘이라 갈라 적는다.** 위 표의 거리는 **ΔE76**(옛 자)이고, 「킷에 넣어도 새 색으로 서는가」는 **ΔE2000**(새 자)으로 다시 쟀다: Indigo 10.2(Lapis Deep) · Moss 10.5(Meadow) · Chestnut 15.3(Ash Wool).
   - 🔑 **토큰 파일이 근거로 들던 「킷 색들은 전부 18.8~65.4 로 멀다」는 킷 23색 시절 값이라 지금은 사실이 아니다.** 지금 킷의 실측 최소 이웃거리는 **1.9**(Graphite ↔ Ink Deep)이고 **중앙값은 6.6** 이다. 새 셋(10.2 · 10.5 · 15.3)은 오히려 킷 평균보다 **멀리** 선다.
3. 🔴 **한 값이 두 곳에서 갈렸다** — 킷의 `Stitch #F0E3C8` 은 직책에 「마스코트 실물 밑단 실측」이라 적혀 있는데, 지금 4K 판의 밑단 실은 `#FBCAAA` 로 그 색과 **16.1** 만큼 떨어져 있다(오히려 Coral Soft 가 10.6 으로 더 가깝다). 옛 굽기에서 잰 값이 새 굽기와 어긋난 것이다. **어느 쪽이 정본인지 정해야 한다.**

---

## §4. 캐릭터별 — 그 아이만 있는 치수

### 몽글
| | |
|---|---|
| **밑단 물결 꼭짓점** | **5개** |
| 물결 깊이 | 몸 높이의 **7.0%** |
| 밑단 실 높이 | 바닥에서 몸 높이의 **10.2%** |
| 실의 굵기 | 약 10 화소(몸 폭의 0.3%) |

### 마린
| | |
|---|---|
| **투구 폭 ÷ 몸 폭** | **0.99** — 몸에서 제일 넓은 것이 투구다 |
| 투구 높이 ÷ 몸 높이 | **0.61** |
| 투구가 끝나는 자리 | 위에서 **61.1%** |
| 투구 색 · 렌즈 색 · 몸 색 | `#283158` · `#F5CD58` · `#CBB7A8` |

### 까몽
| | |
|---|---|
| 가장 넓은 띠 | 위에서 60~65%(날개를 편 자리) |
| 그 아래 잘록한 곳 | 그 폭의 **57%** — 날개가 몸통보다 **1.75배** 넓다 |
| 홍채 색 | `#799459` |
| 동공 | **안 쟀다** — 털과 밝기가 같아 기계로 못 가른다 |

---

## §5. 🔴 안 쟀다 — 지어내지 않는다

- **뒤통수와 옆면.** 정본 컷은 앞과 좌우 34도뿐이라 **뒤가 없다.** 입체로 지을 때 정확히 여기가 빈다.
- **까몽의 동공 크기.** 털이 동공만큼 어두워 밝기로는 못 가른다. 사람 손으로 짚거나 다른 자가 필요하다.
- **실물 크기(센티미터).** 정해진 적이 없다. 펠트 인형으로 만들거나 입체로 지을 때 «키»가 있어야 한다.
- **몸의 두께(앞뒤).** 앞에서 본 그림뿐이라 못 잰다.
- **표정 컷마다 눈이 얼마나 흔들리는가.** 굵은 자(8화소 간격)로 몽글의 일곱 컷을 재보니 **몸 크기는 0.3% 안에서 지켜졌다**(3137~3145 화소). 눈 쪽은 같은 굵은 자로 ±4% 흔들렸는데, 자가 굵어서 그 값을 판정에 못 쓴다. 🔑 **몸이 지켜진다는 것만 확인됐다.**
- **까몽·마린의 다른 컷 전량.** 본체 하나만 쟀다.
- **재질의 물성**(털 길이 · 결의 방향 · 빛을 튕기는 정도의 수치). 눈으로 「유광/무광」만 갈랐다.

---

## §6. 다시 구울 때 지킬 것 — 숫자로 쓴 점검표

이 표가 이 문서의 실제 쓸모다. 새로 구운 그림이 나오면 `python tools/마스코트치수재기.py` 를 돌려 아래와 견준다.

| 지킬 것 | 몽글 | 까몽 | 마린 | 얼마나 어긋나면 다시 굽나 |
|---|---|---|---|---|
| 가로세로비 | 1.02 | 1.06 | 0.63 | ±5% |
| 가장 넓은 자리 | 90~95% | 60~65% | 30~35% | 한 띠(5%) 이상 옮겨지면 |
| 눈 폭 ÷ 몸 폭 | 0.100 | 0.080 | 0.248 | ±10% |
| 눈 사이 ÷ 눈 폭 | 3.38 | 3.05 | 1.94 | ±10% |
| 두 눈 틈 ÷ 눈 폭 | 2.35 | 1.79 | 0.95 | ±10% |
| 눈의 높이(위에서) | 34% | 29% | 41% | ±3%p |
| 눈의 재질 | 유리(반짝임 있음) | 유리 + 홍채 | **무광(반짝임 0)** | 하나라도 바뀌면 |
| 몸 색과 킷의 거리 | 3.1 | 4.7 | 5.3 | 10 을 넘으면 |
| 몽글 밑단 꼭짓점 | 5개 | — | — | 개수가 바뀌면 |

---

## §7. 유호님 자리 — 정해 주셔야 닫히는 것 하나 (하나는 09-08 에 닫혔다)

1. ✅ **킷 밖으로 나간 색 셋** — **킷에 올렸다**(유호 확정 09-08). §3 판정 2 참조.
2. ⏳ **몽글 밑단 실의 색** — 킷의 `Stitch #F0E3C8` 과 지금 그림의 `#FBCAAA` 중 **어느 쪽이 정본인가**. 되돌아오는 자리 = 다음에 몽글을 굽는 날.

---

**관련**: `docs/캐릭터/가이드_정본.md`(누구인가) · `docs/캐릭터/캐릭터_생명감_설계.md`(언제 어떤 표정) · `docs/디자인_토큰.json`(표정 어휘 · 색 킷) · `tools/마스코트치수재기.py`(다시 뽑는 자) · `docs/VR시대_자산판정_v1.md` §5-③(이 문서를 부른 자리)
