# 정본 폴더 — 지도는 한 장뿐이다

> **이 폴더의 안내는 `docs/문서_지도.md` 로 병합됐다**(2026-08-09 압축 ㉠ 지도 4→1 — 자리별 읽는 경로·층 규칙·잠금값·대기 항목 전부 그쪽이 정본. 여기 새 안내를 쓰지 않는다).

- **이 폴더가 정본이다.** Desktop 바로가기는 여기를 가리킨다. 편집 전 `git status`, 편집 후 커밋.
- 라이브에는 안 올라간다 — `.claspignore` 허용목록 방식이라 Apps Script는 이 폴더를 모른다.
- 🔴 **자격증명은 영원히 git 눈 밖** — 실물은 repo 밖 `C:\Users\q1212\SYNK_보안\`, `.gitignore`(`**/로그인.txt` 등)는 재유입 방지용. 상세 = 문서_지도 §자격증명.
