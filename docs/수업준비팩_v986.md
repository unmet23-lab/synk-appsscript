# 수업준비 팩 v9.86 — 2026-07-31 (유호님 "1,2,3,4 전부 진행" 채택분)

> 9번 아이디어 4건 전량 구현. **Glide 조립 0클릭** — 전부 이미 바인딩된 카드(반상세HTML·수업준비HTML)에
> 서버 렌더로 들어가거나(A·B·C), 앱 밖 산출물(D)이다. 실렌더 4시나리오 검증 완료.

## 무엇이 생겼나

| # | 기능 | 어디에 | 언제 |
|---|---|---|---|
| A | **오늘·내일 결석 예정**(학부모 사전신고) 행 | 반 상세 HUD 「오늘의 미션」 절 | 매 calc(14/22시)부터 자동 |
| B | **조 편성 절**(조·역할·오늘 발표 콜아웃·임시 조 필) | 반 상세 HUD (미션 절 아래) | 시즌 시작+편성 후 자동 — 그 전엔 절 자체가 안 보임(정상) |
| C | **제출 현황 — 검사 동선**(반별 n/전체·미제출 5명 캡) | 강사 수업준비 카드(DJ114) | 매 calc부터 자동 (어제 0시 이후 제출 기준) |
| D | **주간 교안 초안 자동 생성** | Drive 폴더 `SYNK_교안초안` + 월요일 주간 통합 리포트에 링크 | 매주 월 07시 (시즌 기간에만) |

- A: 수업 12분 전 브리핑 메일에만 있던 사전신고가 앱에 상설 — 12번(결석 미리 알리기)과 한 회로가 됨
  (학부모가 알리면 → 카드 ✅ → 강사 화면까지 관통).
- B: v9.80 조 편성이 메일 텍스트로만 가던 것을 HUD에 상설. 역할·발표는 차시 번호에서 계산(쓰기 0),
  메일 편성표와 같은 원천(roleOfSeat_)이라 어긋날 수 없음. groups 시트는 배치 1회 읽기.
- C: hw_feedback 기존 읽기에 편승(추가 읽기 0). "낸 학생부터 검사" 동선 — 담당 반별로 강사마다 카드가 다름.
- D: 양식 v2.0의 14항 틀에 **앱이 아는 칸만** 채움 — 기간·인원·담당·지난주 이월('더연습' 태그)·지난주 배운 것·
  **3주차부터 오류 톱10 실측**(student_errors 21일)·**4주차부터 지명 명단**(발화 실측 전엔 최근 2주 포인트
  무활동 대체임을 문서에 명시)·조 편성 참조·**과업 은행 20종 차시×주차 로테이션 추천**(8주 동안 같은 차시에
  같은 과업 안 옴). 강사 몫 9칸은 그대로 빈칸(상한 30분). **같은 이름 문서가 있으면 절대 재생성 안 함**
  (강사·원장 편집 보존). 콘텐츠 정본 = `contents_교안.js`(과업 은행 T-01~T-20).

## 유호님 몫

1. **⚠ 배포 후 아무 함수 ▶ 1회 + 권한 승인** — 이번에 문서 생성(DocumentApp)이 프로젝트 최초로 들어가
   재승인 창이 한 번 뜹니다. **승인 전까지는 자동 트리거가 "Authorization required"로 실패**하니
   (실패해도 데이터 손실 없음 — 다음 실행에 자동 복구) 배포 직후 바로 눌러주세요.
   어차피 하실 `setupStore` ▶와 같은 클릭 세션에서 승인되면 끝.
2. (시즌 시작할 때) `setSeasonStart("2027-02-01")` → `assignGroupsAll` ▶ — B(조 편성 절)와 D(교안의
   조 블록·주차 계산)의 선행 조건. 개원 전엔 안 해도 됨 — 그동안 B 절과 D는 조용히 보류.
3. 끝. Glide는 만질 것 없음.

## 회귀 장치
- tests [v9.86] 1건 신설: A 행·B 절 3태(렌더/생략/임시 조)·C 캡·하위 호환·D 멱등("기존 유지")·
  contents_교안.js 미배포 가드·과업 20종·filePushOrder — 전 스위트 166종 그린.
- D의 미배포·시즌 미설정·기간 밖은 전부 조용한 보류(weeklyJobs 섹션에 사유 1줄) — 어떤 경우에도 리포트를 죽이지 않음.
