# 발표물 브랜드 키트 — 인쇄물 10종 공통 (v2.1 · 2026-08-09 · 색표 파생 선언)

<!-- 파생: docs/디자인_컨셉_정본_v1.md, docs/디자인_토큰.json -->

> ⚠ **색 값은 여기서 편집하지 않는다 — §1 색표는 파생(사본)이다**(2026-08-09
> 사본 정리 · 문서압축_지도 ㉠ — 색표 3중 사본의 하나). 원천은
> `docs/디자인_컨셉_정본_v1.md`(역할·매핑 규칙)와 `docs/디자인_토큰.json`(hex)뿐이다.
> 색이 바뀌면 원천 → 이 표 → 발표물 전 HTML `:root` 순으로 내려온다 —
> 발표물 HTML은 **자립형 1파일** 규칙이라 CSS를 외부 파일로 뺄 수 없어
> 사슬이 세 층이고, 이 표를 고치면 전 HTML의 `:root`를 같이 고친다.
> ⚠ §3 로고 SVG **도형(좌표)**은 반대로 **이 문서가 정본**이다(`tools/로고주입.js` ·
> `tools/인쇄본빌드.js`가 여기서 복사 — 좌표 재작도 금지). 단 §3 안의
> fill/stroke hex는 색이라 원천이 바뀌면 여기서 **함께 갱신**한다 —
> 편집 금지는 「원천 없이 값 바꾸기」 금지지 동기화 금지가 아니다.

## 0-b. 재질 — 펠트 단독 (2026-08-20 유호님 확정)

- **글래시(체리 젤리 유리 재질)는 완전 기각** — 발표물·소개서 어디에도 넣지 않는다.
  구판 글래시 자산(글래시 본체·글래시 오브·체리 젤리 캐러셀 계열)은 인용 금지 — 유물이다.
- **UI·UX 레이아웃 디자인이 들어가는 모든 자리의 장식 재질 = 펠트(양모 공방)뿐이다.**
  요소 실물은 `tools/요소굽기.py` 산출물을 쓴다(2026-08-20 현재 8종+추가 굽는 중 ·
  `docs/캐릭터/퍼프로브_0819/`) — 재질을 손으로 새로 그리지 않는다.
- **펠트의 하한은 「명품」이다**(유호님 원문 08-20: 「대충 만든 펠트 저급한 느낌이 아니라
  지금처럼 여러 번 굽고 정말 만족스러운 명품 느낌의 펠트 재질로 — 꼭 이렇게」).
  집행급 렌더(입체 섬유·재굽기 반복·검수 통과)만 지면에 들어간다 — 납작한 단색 흉내·
  CSS 그림자로 때운 「펠트풍」은 펠트가 아니라 위반이다. 품질 축(§DESIGN.md 1
  「기능 좋고 디자인 트렌디한 명품」)의 재질판 집행이다.
- **PDF 로 나가는 소개서**는 §1 킷 색(08-18 무채 중성축)과 펠트 요소로 UI·UX 를 전부 꾸며 짓는다.
- **정보 판 재질은 ⏳판정 보류다**(유호님 08-20 「전부 다 제대로 만들고 나중에 결정」 —
  CSS 근사 시안에서는 패턴지와 유리가 같아 보였다 · 근사로는 못 가른다가 실측됐다).
  후보 = 반투명 판 **패턴지(재단지) vs 유리** · 카드·판 바닥 = **다린 무광 천**.
  셋 다 집행급(요소굽기)으로 지은 뒤 실물로 판정한다 — 판정 전까지 새 산출물이
  어느 쪽도 «확정»으로 인용하지 않는다. 패턴지 추천의 근거 셋(①글래시 모순 0
  ②한 세계 — 레진은 1점 액센트로 존속 ③위계 = 만든 것 펠트 / 받치는 것 종이·천)은 기록으로 남긴다.

## 1. 색 — 어디서 가져오나

**2026-08-04 개정 v2.0 — 브랜드 키트로 이관.** 발표물은 개편 전 인디고·골드 팔레트로
지어져 있었다(`#1A237E`·`#3D5AFE`·`#FFC400`·`#FF6D00`). 유호님 확정으로 **v10 Crew Dossier
19색**으로 옮긴다. 매핑 근거 = 디자인 컨셉 정본 **개정 이력 v1.3 행**의 구→신 대응
(현행판에 있다 — 본문 절이 아니라 이력 표다). ⚠단 그 행은 FIELD 색 축이라
`#1A237E`·`#FFC400` 두 색의 대응은 거기 없고, `#3D5AFE`의 대응(KC Cool Blue)은
v1.5에서 Part 6 전용으로 물러나 인쇄물에 못 들어온다 — 이 **세** 색의 발표물 접기
(`#1A237E`→Graphite 3 · `#FFC400`→Chalk(아래 🔴골드 문단) · `#3D5AFE`(주조 강조)→별색
승계 없이 잉크+웨이트, 골드와 같은 논리)는 **이 문서 §1이 정본**이다.
구 팔레트 전문은 git 이력에 있다.

| 역할 | 이름 | HEX | 대비·비고 |
|---|---|---|---|
| 바탕 | Paper | `#FBF7F0` | 순백 금지 |
| 잉크 | Graphite 3 | `#262626` | 순검정 금지 · Paper 위 **14.17** |
| 보조 잉크·괘선 | Graphite 4 | `#373737` | Paper 위 **11.15** |
| **반전면** | **Graphite** | **`#08090C`** | 표지·머리 띠·챕터 브레이크 |
| 반전면 잉크 | Chalk | `#E4E4E7` | Graphite 위 **15.69** |
| 반전면 보조 | Chalk 3 | `#D1D2D4` | Graphite 위 **13.16** |
| **신호** | **Coral** | **`#F96859`** | 다크 글자 **6.78** / 라이트는 **면만**(2.75) |
| 신호 — 라이트 글자 | Coral 3 | `#AE322A` | **5.98** — 본문 글자까지 ✅ |
| 신호 — 여린 면 | Coral Wash | `#FEF0E9` | 강조 패널 바닥 |

🔴 **구 팔레트에서 사라진 것은 「보상 골드」다.** 키트에 두 번째 액센트가 없다 —
보상 의미에 가장 가까운 Lime 은 **앱 전용**(성장·획득), KC 4색은 **Part 6 전용**이라
인쇄물에 못 들어온다. 그래서 골드는 되살리지 않고 **Chalk 으로 접는다.**
강조는 색이 아니라 **웨이트·크기**가 진다(디자인 컨셉 R2 — 위계는 밀도로 만든다).
로고의 `//` 가 잉크 저채도가 된 것도 같은 이유다.

- 한 지면의 색 = **바탕 + 잉크 + 신호 1점.** Coral 이 면적을 먹으면 신호가 아니라 배경이 된다 —
  한 지면에 강조 최대 2곳.
- ⚠ **라이트 배경에 Coral 「글자」 금지**(2.62). **크게 써도 안 된다** — 대형 텍스트 기준 3.0 에도
  미달한다. 글자면 Coral 3, 면이면 Coral(정본 v1.3.1).

## 2. 서체 — 건드리지 않는다

디자인 컨셉 정본 R3: **타이포는 절대 건드리지 않는다.** 위계는 폰트 수가 아니라
**밀도(크기·웨이트·여백)**로 만든다(R2).

| 용도 | 서체 | 폴백 |
|---|---|---|
| 한글 | SUIT Variable | 시스템 고딕 |
| 라틴·키릴(몽골어) | Inter Tight — 스택에서 SUIT보다 **앞** | system-ui |
| 시스템 라벨·숫자·판번호 | DM Mono | ui-monospace |

- 웨이트: 본문 500 · 헤드 800 (영문·몽골어 전용 헤드만 700).
- 🔑 **워드마크는 이 표가 정하지 않는다** — §3 도형 정본이 정한다(Inter Tight **600** · 자간 **−1** · 발표물 14슬롯 실측 08-22).
  폰트 정본(`docs/브랜드_폰트_정본.md` §1)도 08-18 에 같은 판정으로 고쳐졌다 — **실물이 규칙보다 뒤에 나왔으니 로고가 이긴다.**
- 🚫 **DM Mono에 한글·키릴 금지** — 글리프가 없어 넣는 순간 깨진다.
  로마자·숫자·기호 전용. (린트가 검사한다)
- 몽골어가 한국어와 대등하게 병기되는 자리는 몽골어 글자 크기에 **1.04배** 광학 보정.

## 3. 로고 — 2026-08-04 유호님 확정

**메인 = `W5`(축약) + `W1`(풀).** 나머지는 자리에 맞춰 꺼내 쓰는 보조 라이브러리다.
전체 후보와 근거 = `docs/발표물/로고/후보시트.html` (브라우저로 열면 됨).

🔑 **로고는 그림이 아니라 글자다 — 그리고 이제 진짜 넉 자다.** 워드마크 = **`synk`**
(k = Inter Tight SemiBold **실제 글리프** · 언제나 Coral). 심볼 락업 = `~ ^ ^ <` 유지.
예전 `syn<` 워드마크는 **은퇴**(유호 확정 08-24) — 처음 보는 눈에 「syn+화살표」로 오인돼
이름이 안 읽혔다. 브랜드명은 읽혀야 한다.

🔑 **층이 둘이다(유호 확정 08-24).** 워드마크는 하나고, `<` 는 워드마크에서 **독립한 기호**가 됐다:
- **이름 `synk`** — 이름을 «보여주는» 순간: 스플래시·대외 문서·간판·명함·스토어·SNS 프로필.
- **기호 `<`** — 이미 «안에 들어온» 자리: 앱 상단바·진행 눈금·로딩·도장·워터마크·불릿.
  이름을 반복하면 촌스러워지는 자리 전부가 기호의 자리다. ⚠앱 상단바 **왼쪽엔 두지 않는다**
  — 시스템 뒤로가기(‹) 자리라 오독된다(가운데·오른쪽·비내비 자리만).

**기본 표현 = 잉크 카드 «벡터 펠트»**(유호 확정 08-24 「이 스타일로 승격」+「한 단계 더 명품화」):
보풀 헤일로 · 손맛 실땀 · 두께 2톤 — 전부 벡터라 외부 이미지 0건, 자립형 1파일 그대로,
어느 기계에서나 선다(렌더판 PNG 는 git 밖이라 남의 화면에서 꺾쇠가 «사라진» 08-24 실사고가 근거다).
소형(≲60px)·1도 인쇄는 **민판**(필터 없는 순수 벡터)이 진다 — 크기가 재질을 정한다.

### 어느 자리에 무엇을 쓰나

| 자리 | 쓰는 것 | 왜 |
|---|---|---|
| 인쇄물 머리 띠 · 명함 · 표지 · 스플래시 | 워드마크 **`synk`** 펠트판(다크/라이트) | 이름을 보여주는 순간. 기본값이다 |
| 표지 · 배너 (가로 넉넉) | **`synk //`** 슬래시 락업 | 슬래시는 잉크의 저채도(아래 규칙 ②) |
| 소형(≲60px) · 복사·팩스·1도 인쇄 | 워드마크 **민판** | 필터 없는 순수 벡터 — 작은 자리·1도에서 정직하다 |
| 앱 상단바 · 진행 눈금 · 로딩 · 불릿 | 기호 **`<`** 자수판 | 이미 안에 들어온 자리 — 이름을 반복하지 않는다 |
| 도장(출석·칭찬) · 워터마크 | 기호 **`<`** (도장 = 배지 문법) | 08-24 승계 — 찍힌 자국 자체가 보상 |
| 정사각 자리 · 앱 아이콘 | S1(어두움) · S4(밝음) · 몽글 배지 | 심볼 단색 · 렌더판 |
| 가로로 긴 사인 · 이메일 서명 | L1 | 심볼 + 구분선 + 워드마크 |

### 색 규칙 — 두 줄

① **k(워드마크)와 기호 `<` 는 기본 Coral `#F96859`** — 로고에서 유일한 신호다. 배경을 안 탄다.
   **특별한 자리(기념일·이벤트·한정 굿즈)는 Pop `#E05C97`** 을 허용한다(유호 픽 08-24 — 상시로 쓰면 특별함이 닳는다).
   ⚠ 라이트에서 2.62는 **도형이라 기준 적용 제외**다. 코랄로 **글자**를 쓰는 자리라면 Coral 3 `#AE322A`.
② **`//`는 세 번째 색이 아니라 잉크의 저채도** — 다크 = Chalk 50%, 라이트 = Graphite 4 45%.
   구판은 배경 따라 골드↔블루를 갈아 끼웠는데, 그러면 로고가 3색이 되어 「2색 + 신호 1점」이 깨진다.
   신호를 `<` 하나로 남기니 **배경을 타는 문제 자체가 사라졌다.**

대비 실측(WCAG 상대휘도 · 후보시트가 페이지에서 직접 계산한다):

| | Graphite `#08090C` | Paper `#FBF7F0` |
|---|---|---|
| Chalk `#E4E4E7` | **15.69** ✅ | 1.19 ✗ 사라짐 |
| Coral `#F96859` | **6.78** ✅ | 2.75 — **면만**, 글자 ✗ |
| Coral 3 `#AE322A` | 3.12 — 24px↑ 만 | **5.98** ✅ 본문 글자까지 |
| Graphite 3 `#262626` | 1.32 ✗ 묻힘 | **14.17** ✅ |
| Graphite 4 `#373737` | 1.67 ✗ 묻힘 | **11.15** ✅ |

`syn` 글자색(08-24 · 2027 킷 정렬): 어두운 바탕 = **Paper `#FBF7F0`** / 밝은 바탕 = **Ink `#2B2320`**
(순백·순검정 금지의 그 하한들 — 구 Chalk·Graphite 3 은 퇴역 대기 무채라 로고에서 걷었다).

### 도형·표현의 실행 정본 — tools/lib/로고정본.js (복붙 금지 · 08-24)

로고는 이제 필터가 든 «레시피»라 md 에 복붙하면 그 순간 사본이 갈린다(사본 여덟 곳의 그 병).
**HTML 에 로고를 넣는 길은 소비자 통로뿐이다** — 손으로 붙이지 않는다:

```js
const 로고 = require('./tools/lib/로고정본.js');
로고.워드마크({ 판:'다크'|'라이트', 표현:'펠트'|'민', 슬래시:true })  // synk (+//)
로고.기호({ px, 펠트:true })      // < 자수판 — px<28 이면 실땀이 스스로 접힌다
로고.도장({ px })                 // 배지 문법(쿠션·실땀 링·광학 중심)
// 발표물 슬롯 = node tools/로고주입.js · 파이썬 소비자 = CLI --json
```

심볼 락업(S1 — 좌표는 08-04 확정 그대로 · 소비자가 옮겨 그리지 않는다):

```html
<!-- S1 심볼 ~^^< (= s·y·n·k) · 어두운 바탕. 밝은 바탕은 stroke 를 Ink #2B2320 로 -->
<svg viewBox="0 0 240 110" role="img" aria-label="SYNK">
  <g fill="none" stroke="#FBF7F0" stroke-width="11" stroke-linecap="butt" stroke-linejoin="miter">
    <path d="M10 62 C17 50, 29 50, 36 60 C41 67, 48 67, 52 58"/>
    <path d="M68 68 L88 34 L108 68"/>
    <path d="M124 68 L144 34 L164 68"/>
    <path d="M228 22 L186 52 L228 82"/>
  </g>
</svg>
```

⚠ **viewBox 여백을 깎지 말 것.** 심볼은 획 두께(11)의 절반이, 워드마크 펠트판은 보풀 헤일로가
여백에 들어간다. 줄이면 가장자리가 잘린다(실측으로 두 번 겪었다 — 로고정본.js 가 여백까지 안다).

### 남은 것

- ✅ **서체 의존은 08-24 에 끝났다** — `synk` 넉 자가 전부 path(글리프를 뜬 것)라
  Inter Tight 가 없는 기기에서도 자형이 같다. 기호·`//`·심볼도 순수 도형이다.
- ✅ 서체 정본과의 충돌은 **해소됐다**(08-18) — 폰트 정본 §1 이 「워드마크는 이 표가 정하지 않는다 ·
  `_브랜드킷.md` §3 을 따른다」로 고쳐졌고, 위 §2 도 08-22 에 같은 말로 맞췄다.

### 렌더판 — 펠트 실물 표현층 (유호 확정 2026-08-21 「이대로 확정」 · 결정.md)

로고 한 벌이 다섯 자리로 확정됐다 — **벡터 정본(위 도형)은 그대로 살고, 아래는 그 위에 앉는 표현층**이다:

| 자리 | 확정 | 실물 |
|---|---|---|
| 지면·인쇄 워드마크 | **벡터 펠트 `synk`**(08-24 승격 — B2 렌더판 꺾쇠는 은퇴) | `tools/lib/로고정본.js` |
| 심볼·워터마크(락업) | 고리 — 양모 실 두 가닥 | 렌더판 `로고고리` (08-22) |
| 도장·워터마크 표식 | **기호 `<`**(08-24 승계 — 도장 = 배지 문법) | `tools/lib/로고정본.js` |
| 앱 아이콘 | 몽글 배지 — 꺾쇠=입 옆모습 | 렌더판 `로고배지` |
| 앱 스플래시(다크) | 잉크 카드 — 양모 밤 · **벡터 펠트 synk**(08-24 개정 — 렌더판 의존 0) | 합성판 `로고/스플래시_잉크카드.html` |
| 모션·영상 | 고리가 꿰이는 순간 — 떨어짐→다가옴→엮임 | 시퀀스 `고리모션` 40컷 (08-22) |

- **렌더판의 통로는 하나다**: `node tools/룸굽기.js --굽기 로고꺾쇠` · `--굽기 로고배지` · `--굽기 로고고리`
  → `docs/Loom_자산/구움/<이름>_{몸,접지}.png` (구움은 git 밖 — 씨앗 고정이라 재현된다).
- 재질은 CSS 흉내가 아니라 **펠트 실물 사진 타일**(`docs/캐릭터/펠트패치_0815`)로 굽는다(Loom 헌법 ① 「결은 사진 픽셀에서만」).
- `로고꺾쇠`·`로고고리`는 **정면 직교**로 굽는다(판 부품과 같은 예외) — 벡터 정본과 외곽이 자리 그대로 맞아야 하는 합성 부품이다.
- **모션**(08-22 · 공명 기각 자리를 다시 채웠다): 형태가 아니라 **사건**을 움직인다 —
  「두 실이 서로를 잡는 순간」. 떨어져 있던 고리 둘이 다가와 겹치고, **겹친 뒤에야** 위아래로
  엮이며 꿰임이 성립한다(진폭 0 → 0.30). 꿈틀거림은 생물의 문법이라 벌레가 됐지만
  이동과 성립은 «연결»의 문법이다. **진행 1.0 = 확정 정지 화면**이라 마지막 컷이 곧 심볼이고,
  영상이 멈추는 자리에 로고를 겹쳐 넣거나 페이드로 바꿔치기할 필요가 없다.
  통로 = `node tools/룸굽기.js --굽기 로고고리 --시퀀스 40 --해상도 560 --샘플 96 --한장 --이름 고리모션`.
  · `--시퀀스` 눈금 신설 — 프레임마다 블렌더를 새로 띄우면 씬 셋업만 40번이라 굽기가 몇 배가 된다.
  · 움직이는 판에서는 **필름 그레인을 끈다**: 프레임마다 다른 그레인은 지글거리고, 같은 그레인은
    화면에 붙박인 얼룩이 된다(정지 판에서 «촬영물»을 만들던 층이 여기서는 손해다).
  · 🔴 실측: 첫 컷은 링이 1.30배 벌어져 최대 x 3.835 — 정적 구도(화면 반경 3.45)로는 프레임 밖으로
    나간다. 시퀀스일 때만 1.22배 넓힌다(정적 판의 구도는 한 눈금도 안 건드린다).
  · 쓰임: **WebP**(알파 유지 — 앱·웹의 어떤 바탕에도 얹힌다) + **GIF**(어디서나 열린다 ·
    알파가 1비트라 양모 밤 바탕을 미리 깔아 굽는다).
- **고리**(08-22): 확정 시안 좌표(반경 40 · 두께 11 · 중심 ±(24,18))를 1/20 로 옮겼다. 교점을 원-원 교차로
  계산하니 (-15.87, 21.17) — 시안의 clip 중심 (-15.9, 21.2) 과 일치했다(두 방법이 서로의 검산이다).
  꿰임은 **토러스로는 못 만든다**(한 평면에 있어 한쪽이 통째로 위에 얹힌다) — 가닥이 상대를 넘는 곳에서
  솟고 빠지는 곳에서 잠기는 z 흔들림이 꿰임의 전부다. 그리고 «실»은 매끈한 튜브가 아니라
  **두 겹이 꼬인 것**(플라이드 얀 · 피치 = 겹 지름의 1.9배)이다 — 꼬임이 없으면 고무로 읽힌다.
- 시안 전체 기록(20보드) = 시안 캔버스 `claude.ai/code/artifact/c4204ee1-ebf5-45db-8cc2-2f293eaf0bb2` · 픽 문서 = 세션 전달본.
- 상태 = **배지 확정**(유호 08-21 「3차 마감본으로 확정하자」 · 결정.md) — 배지 = 3차 마감판:
  옆모습(꺾쇠=입·구슬눈 1·홍조 1·뿔 12시)·스침 키·HDRI 0.32·3D 실땀·Stone 염색 오트·**수제 눌림 없음**
  (매끈한 쿠션이 확정 결 · 모따기 0.09/5단). 꺾쇠는 4차(수제 눌림 포함)가 현행.
  🔴 정면 얼굴 실험(큰 눈 둘·작은 중앙 입)은 **기각**(유호 08-21 「좋은 실험이었어」) — 코드는 git 11676697.
  ⚠잔털(파티클)은 7판에서 철회 — 이 스케일의 아플리케에선 사진 결을 덮는다(회색 카펫 실측 ·
  룸장면.py 주석). 잉크 카드 합성은 **08-21 착지**(위 표) — 렌더 몸판을 벡터 자리
  (직교 60×60 환산 · 중심 135.5,66.35)에 얹고 픽셀 실측 오차 0.3%.
  🔴 **공명 모션은 기각**(유호 08-21 「지렁이같이느껴져 · 아예 기각 삭제하자」 · 결정.md) —
  파일 삭제, 코드는 git a8dbd67b. 배운 것: 정지 시안에서 «직조»로 읽힌 두 가닥 사인파가
  움직이면 «벌레»가 된다 — 좋은 정지 시안을 그대로 시간축에 올리지 않는다.
  ⏳**실험 중(확정 아님)**: «>< 찡긋» 배지 — 얼굴 부품 0, 꺾쇠 둘이 감은 눈(유호 지시 08-21).
  아이콘 판 = `--굽기 로고쌍꺾쇠 --변주 버터|라피스|팝` · 요리 사진 판 = `--굽기 로고찡긋플레이팅`
  (유호 지시 08-21 「음식 사진으로 만들어보면」 — 다른 세션 플레이팅 트랙과 같은 규율). 유호 픽 대기.

## 4. 모티프 — 시냅스 노드

브랜드 고유 자산이다. 이름 SYNK 자체가 **동기화**다 —
그래서 여정선·관문·연결선을 **노드와 링크**로 그린다. 장식이 아니라 이름의 그림이다.

<!-- 08-14: 구 근거였던 「앱의 몬스터 7단계 진화가 이미 신경 계열이다」 절을 걷었다(유호 지시 「삭제」).
     모티프 판정은 안 바뀐다 — 근거가 둘에서 하나로 줄었을 뿐이고, 남은 하나(이름 SYNK = 동기화)가
     원래 더 세다. 성장 캐릭터는 마스코트 한 벌로 단일화됐다(.claude/skills/synk-brand). -->


🚫 **게르 천창(몽골 상징) 모티프는 쓰지 않는다.** 상업 사용 범위가 유호님 판단
대기 상태이고 원어민 검수가 선행 조건이다(디자인 컨셉 정본 미해결 3건 중 ①).
결정 나기 전까지 대외 인쇄물에 넣지 않는다.

## 5. 대상별 밀도 — 학부모물은 촘촘하게

디자인 컨셉 정본의 미해결 리스크: 몽골 학원 광고는 정보가 빽빽하고 화려해서,
흑백 여백 미학은 10~20대에겐 세련돼 보여도 **학부모에겐 「내용 없음」으로 읽힐 위험**이 있다.
처방은 규칙 3개는 유지하고 **밀도만 분리**하는 것이다.

- **학부모·상담 대면물(발표물 대부분)** = 밀도 높게. 여백으로 멋 부리지 않는다.
- SYNK FIELD 표현 모드(디더·리소 2도)는 이 인쇄물들에 **적용 제외**다
  (디자인 컨셉 정본 「적용 제외(불가침)」에 학부모 신뢰 문서 본문·몽골어 정보 화면이 명시).

## 6. 인쇄 규격 — 전 산출물 공통

- `@page { size: <용지>; margin: 0 }` — 여백 0으로 잠가 크롬이 머리글에 날짜·URL을 찍는 것을 차단.
- `print-color-adjust: exact` — 배경색을 인쇄에 살린다.
- 외부 링크·이미지·폰트 CDN **0건**(자립형 1파일).
- 흑백 복사 대비: 구분은 **색 + 패턴 이중 표기**. 색으로만 구분되는 정보를 만들지 않는다
  (몽골 현지에서 재복사될 것을 전제한다).
- 화면 편집용 표시(가이드선·설명)를 인쇄면에 남기지 않는다.
- 미확정은 지어내지 말고 「____」로 비우고, 산출물 끝에 「채워야 할 칸」으로 모은다.

## 7. 검사

```bash
node tools/발표물린트.js docs/발표물/*.html
```

금칙어 8종 · 폐기 표기 · 외부 자원 · 기간 약속 · 가격 수치 · 비노출 잠금 ·
DM Mono 한글 혼입 · 인쇄 규격 선언을 기계로 검사한다.
