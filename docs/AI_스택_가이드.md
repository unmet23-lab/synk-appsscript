# SYNK AI 스택 가이드 — 방향·세팅·역할·사용법

> **정본** · 2026-08-03 유호님 확정 · **2026-08-05 전면 개편 · 08-10 시기 표기 개정** — 로드맵 × 코어 엔진 × 네이티브 재구축(Glide 폐기·Supabase 스택·코딩 레인 분할)에 정렬. 판정 근거 전문 = memory `ai-stack-decision.md`(9차).
> 스택이 바뀌면 이 문서와 memory를 **같은 세션에서 함께** 갱신한다(개정 주체 = Claude). 한 장 지도 사본 = 바탕화면 `SYNK_지도`(신선도는 `node tools/지도대장.js`가 잰다).

---

## 0. 방향 — 모든 도구는 이것에 복무한다

방향 정본 = `docs/제품방향.md`(로드맵·핵심 등식) + `docs/코어엔진_설계.md`(내부 엔진). 스택은 그 실행 수단이다 — **어긋나는 도구는 층이 맞아도 안 들인다.**

> ⚠ **2026-08-10 시기 표기 개정** — 구 「1·2·3·4년차」는 죽었다(정본 표 = `docs/제품방향.md`).
> 특히 **「2년차 = 출시용 앱」은 폐기** — 출시는 **+6개월(2027-02)**로 당겨졌다.

| 시기 | 무엇 | 스택에 요구하는 것 |
|---|---|---|
| **지금 ~ +6개월** | 학원 운영 + 학생 접점 네이티브 재구축(SYNK-talk) | **수집** — 모든 숙제·발화·첨삭이 AI 학습 가능한 모양으로 쌓인다(핵심 등식) |
| **+6개월 (2027-02)** | **학원 앱 스토어 출시**(개원 02-25 전 · 쓰는 사람은 학원 관계자만) | EAS `production` 빌드·제출 · **같은 스키마 그대로**(호환이 목적이라 지금 계약 P0·L0으로 못박는다) |
| **개원+6개월 (2027-08 목표 · 유호 교정 08-14 — 구 「+18개월」 폐기)** | 개인 맞춤 비서형 AI 회화 챗봇 | 수집분을 봇에 학습 · 음성 파이프라인(STT는 실측 선정) · **몽골 1위** |
| **+24/28개월** | VR 시뮬레이터 → 타국 확장 · 진로별 AI 비서(시점 미정) | 코어 엔진 **E3 봇 팩토리** — 1호 = 유학 집약 봇(순서 불변) |

- **핵심 등식**: 매일 내는 숙제 = 전부 학습용 데이터화. 화면·게임화는 그 수집기를 매일 돌게 하는 엔진이지 목적이 아니다.
- **코어 엔진**(비출시·내부): 모든 SYNK 앱의 AI가 「무엇을 아는가(지식) × 이 학생을 아는가(개인화)」를 **엔진 하나**에서 꺼내 쓴다 — 엔진이 발전하면 앱들이 다같이 발전한다. 지금(E0)은 **계약 조항만, 엔진 전용 코드 0줄**.

---

## 0-1. 제품 스택 — 앱이 서는 곳 (2026-08-05 유호님 확정)

> 유호님 원문: **핵심 원본 데이터 = Supabase + Postgres** · **운영·리포트·수동 검토 = Google Sheets** · **자동화 = n8n 또는 Apps Script** · **앱 = Expo**.

| 층 | 부품 | 상태 · 요점 |
|---|---|---|
| **원본 데이터 (정본)** | **Supabase (Postgres)** | 🆕 필수 신규. 데이터 정본은 **여기 하나**. L0 데이터 계약 = Postgres DDL(스키마 감사 v9.187 산출물이 씨앗) · 엔진 이름공간 `engine.*` 예약 |
| ├ 인증 | Supabase Auth | **기본후보**(무료 5만 MAU) — Glide가 대신하던 로그인의 빈자리. ⚠유호님 명시 확정은 아직(3택: 자체 토큰/Supabase Auth/구글 계정) |
| └ 서버 함수 | Supabase Edge Functions | **AI 게이트웨이 권장 자리**(코어 엔진 A-2: 모델 호출은 서버측만 · 앱 번들에 API 키 금지) |
| **운영·리포트·수동 검토** | Google Sheets | 가동 중 · **파생 층**(정본 아님). 동기화는 단방향(Supabase→시트) — 시트→원본 되쓰기는 **L0 계약에 규격 있을 때만**(P0 계약 조항) |
| **자동화** | Apps Script · n8n | AS = 가동 중(엔진 23,746줄 자동화층으로 존속 · 실측 천장: 실행 6분·트리거 90분/일·메일 100/일) · **n8n = ⚠새 부품, 호스팅·비용 미확정**(설계 시 건별 확정) |
| **앱** | **Expo (SYNK-talk) + EAS** | repo `Documents\SYNK-talk`(PRIVATE) · CI 초록 · EAS projectId 연결·첫 빌드 트리거·`EXPO_TOKEN` 완비. **푸시 무료·초당 600건** → 알림 86곳 메일→푸시 이전(네이티브 전환의 숨은 배당) |
| **코어 엔진** | 4부품: 지식 그래프 · 학습자 모델 · 오류/교정 말뭉치 · AI 게이트웨이 | **E0(지금) = 계약 조항만** → E1 지식층+RAG(상담AI 로그 쌓인 뒤) → E2 학습자 모델 → E3 봇 팩토리(3~4년차). 정본 = `docs/코어엔진_설계.md` |

**제품 스택 원칙**
1. **데이터는 AI가 아니라 저장소에.** 모델·벤더는 갈아끼우고 데이터는 안 움직인다 — 3년차 봇의 훈련 재료는 지금 Supabase에 쌓이는 행이다.
2. **화면은 백엔드를 모른다** — `계약/` + 어댑터 1층으로 교체 비용 격리. C0 계약은 Codex **소비자 승인** 후 동결.
3. **전환은 단계식, 빅뱅 ✗.** 현행 AS+시트 운영은 새 앱 파일럿 게이트까지 그대로 돌고, Glide는 신규 투자 0의 비상 낙하산(실물 제거는 게이트 뒤 별도 승인).
4. 지식 주입 = **RAG**(파인튜닝 ✗ — 틀린 지식을 못 지운다) · 벡터 RAG ✗(지식 총량이 컨텍스트 상한 안일 동안 — 넘는 시점이 자동 재판정 조건).
5. **엔진이 죽어도 앱은 돈다** — AI 기능마다 강등 경로를 P0 기능표에 명시(단일 장애점 금지).

📌 ~~**빈자리 1건(제안 기록 — 미확정)**: 네이티브 앱의 런타임 오류 감시(Sentry류)가 스택에 없다~~ → ✅ **채워졌다(2026-08-15 · talk `4701bd6` 「기기 위의 관측 층 — Sentry + 자동 밟기(Maestro) 한 벌」 · 유호 지시)**: `@sentry/react-native`(talk `package.json` · `app.json` 플러그인) · 통로 `src/관측.js`(합성 vs 실사용 태그 분리 · 「DSN 이 없어서 안 도는 상태가 「켰다」의 얼굴을 하지 않게」 · 회귀 `tests/관측.test.js`) · 화면 `src/도착확인.js`. ⚠ 이 줄이 08-22까지 「빈자리」로 남아 08-20 엔진 전수 검토가 ⑧「앱 오류 감시」를 **신설**로 올렸다 — 문서가 낡아 실물이 두 번 지어질 뻔한 자리(실측 08-22 · 세션 기록 파기). **남은 것** = Sentry 웹 조회 auth 토큰(유호님 계정 몫 · `.maestro/README.md` ⏳) · 운영 DSN·릴리스 태깅·알림 임계(파일럿 전).

---

## 0-2. AI 작업 스택 — 누가 만드나 (한눈 지도)

```
                        유호님 (결정만 한다)
                           │ 폰
                           ▼
   ┌──── claude.ai/code (클라우드 세션 · ★PC 꺼져도 됨) ────┐
   │  계정1 허브: 기획·판정·정본·리뷰·병합·배포             │
   │  계정2 병렬 생산  ·  계정3(Pro) 예비                   │
   │  코딩 레인: L0 데이터계약 · L1 백엔드 · L3 콘텐츠      │
   │            · L4 통합/배포/CI                           │
   └──────┬──────────┬──────────────┬──────────────────────┘
          ▼          ▼              ▼
    [GPT 코덱스]  [제미나이 계열]   [옵시디언]
     L2 Expo 화면  결손 보전 6종    판정 이력을
     주력(발주는   + 몽골어 검문    눈으로(그래프)
     클로드 자동)  (API·모델정책.js)
     + 적대 검수    🔒+ 보안 리뷰(3.5 Flash Cyber — 공개 대기)
          │ 만든 것이 서는 곳
          ▼
   [Expo 앱] ↔ C0 계약 ↔ [Supabase Postgres 원본] ─단방향→ [시트 운영층]
                            ├ Edge Functions = AI 게이트웨이(코어 엔진)
                            └ 자동화 = Apps Script · n8n

  (결정큐 봇 = 08-20 소멸 · 지금은 결정.md 한 통로)
  [헤르메스] 폰 질문 창구 · 읽기 전용 · PC 켤 때만
  (오르카: SYNK-talk가 「일감 대기」가 되는 시점에 이종 병렬 레인)
```

**층이 5개다** — 도구를 늘릴 때 「어느 층인가」부터 묻는다. 같은 층에 둘을 두면 정본이 갈라진다.

| 층 | 하는 일 | 누구 |
|---|---|---|
| **지휘** | 유호님 ↔ 시스템 (결정만) | `결정.md` 한 통로(세션 첫머리 훅) · 헤르메스(PC 켤 때) — 🪦결정큐 봇은 08-20 소멸 |
| **두뇌** | 기획·판정·정본·배포 + 코딩 L0·L1·L3·L4 | 클로드 ×3 |
| **구현 파트너** | **L2 Expo 화면 주력**(닫힌 명세 발주 — 발주·리뷰·병합은 클로드) | GPT 코덱스 |
| **검사** | 백지 적대 리뷰(설계·코드) + 배포 검수 파이프라인 + 몽골어 검문 | GPT 코덱스 · 제미나이 API · 🔒**제미나이 3.5 Flash Cyber**(코드 보안 리뷰 전용 — 아직 못 켠다 §1-5) |
| **감각·정찰** | 밖에서 재료를 물어온다 · **클로드의 결손을 메운다** | 제미나이·제미나이LM |

**원칙 5 — 전부 여기서 나온다**
1. **만드는 곳은 병렬, 라이브는 한 줄.** 배포·정본(메모리·문서) 쓰기는 클로드 계정1만.
2. **이종(GPT)은 비교가 아니라 검사.** 결과 둘을 놓고 고르는 게 아니라, 기억·테스트·가드를 기준으로 공격시킨다. 구현을 나눠도 **심판은 기계 3겹**(pre-commit·CI·교차 검수)이다.
3. **지휘층(헤르메스)은 읽기 전용** — 프로즈가 아니라 **기계로** 그렇게 만들었다(§1-4).
4. **정찰층이 물어온 것은 재료지 판정이 아니다.** Spark·Deep Research가 물어온 경쟁사 표는 클로드가 기억·수치와 대조한 뒤에야 결정 재료가 된다.
5. **기억이 어디 사느냐가 배치를 정한다.** 기획·판정은 기억(재제안 금지 목록·실측치)이 사는 클로드에, 백지가 무기인 검증은 GPT에, 클로드가 원천적으로 못 하는 것만 제미나이에.

**월 비용**: 유료 = 클로드 맥스 ×2 + Pro ×1 + GPT **Plus($20)** + **Google AI Pro($19.99)**. **제품 스택 신규분은 전부 무료 시작** — Supabase Free·EAS Free·시트·AS 0원(n8n만 ⚠미정 — 셀프=서버비/클라우드=구독, 설계 시 확정). 유료 전환은 사용량이 실측으로 요구할 때만.

---

## 0-3. 코딩 레인 분할 (2026-08-05 유호님 채택 "나눠서 하자")

기획 = 유호님 1인 · 설계 = 정본 1곳·펜은 클로드·Codex 적대 검수·확정은 유호님 · 코딩 = 레인 분할. 실행 정본 = SYNK-talk `docs/개발_분업.md` **v3.2**(엔진 역할분배 v3 — 의미 펜=클로드·물리 펜=GPT·거부권 대칭 · §8-1-a). ⚠아래 레인 표의 「L2=Codex 주력」은 **F133 해소 후의 정상 상태**이고, **F133 은 2026-09-02 에 죽었다**(실측 · §1-2 ③) — 08-06 의 「클로드 구현→코덱스 검수」 뒤집기는 그 벽이 있던 동안의 임시 편성이었다. 다만 **실발주 1건이 아직 안 돌았다** — 복귀는 그 한 벌로 잰다.

| 레인 | 내용 | 담당 |
|---|---|---|
| L0 | 데이터 계약(Postgres DDL·수집_교정_계약) | 클로드 |
| L1 | 백엔드(Supabase·AS 연동) | 클로드 |
| **L2** | **Expo 화면** | **Codex 주력** — 공통 앱 기반(F0: 내비·상태·API층·토큰)은 Codex 단일 소유 선행, 토큰 값은 브랜드 킷 정본 구속 |
| L3 | 교정 프롬프트·콘텐츠 | 클로드 정본 + 제미나이 몽골어 검문(기존 배선) |
| L4 | 통합·배포·CI | 클로드 |

- 완성 단위 = 층이 아니라 **기능별 수직 슬라이스**(P0 제품계약 → F0 기반 1회 → C0 계약 동결 → S1~Sn 관통·즉시 통합). S1 = 실측 게이트 겸임.
- 검수는 전수가 아니라 **위험도 기반**(계약·인증·전역상태·배포설정만 전수 교차) · 발주서는 구현 전 Codex 검수(작성자·검수자 분리).
- **첫 사이클 = 실측 게이트**: 리뷰 반려 횟수·재작업 왕복·완성 시간·한도 잔량 → 레인 재배치. 같은 기능 2회 제작 ✗.
- ⛔ **라이브 repo(SYNK-appsscript)에 Codex 불입 금지 유지**(훅 무효 + 라이브 시트 접근). Codex의 일터는 SYNK-talk뿐(AGENTS.md·pre-commit 완비).
- ⚠ 「효율 2배」는 기대치 — 병렬 이득은 화면 구간 한정, 상류(계약·설계)는 직렬 + 이종 왕복 +5~8분/사이클 실측 · GPT 한도 정책 변동성 주의.

---

## 0-4. 로드맵 × 스택 — 무엇이 언제 켜지나

| 시기 | 켜지는 것 | 비고 |
|---|---|---|
| **지금 (~+6개월)** | 클로드 ×3 · Codex 레인 · 제미나이(구독+몽골어 검문 API) · 결정큐 봇 · **Supabase(P0·L0 착수와 함께)** · Expo/EAS | 현행 AS+시트는 파일럿 게이트까지 그대로 · Glide = 낙하산(투자 0) |
| **+6개월 — 스토어 출시 전** | **EAS `production` 빌드·`submit`** · Apple/Play 개발자 계정 · 개인정보처리방침 **공개 URL** · 심사용 데모 계정 | 앞의 셋은 코드가 아니라 **유호님 계정·결제**가 막는다 · `eas.json` `submit.production`이 아직 비었다 |
| **파일럿 게이트 뒤** | Glide 실물 제거(별도 승인) · 시트 = 파생 층으로 강등 완료 | 데이터 정본이 Supabase로 |
| **E1 (상담AI 로그 축적 뒤)** | 코어 엔진 지식층 + RAG(Edge Functions 게이트웨이) | 상담AI 지식층 합류는 되돌림 절차 먼저 |
| **음성 수집 착수 때** | **Gemini API 종량**(구독과 별개) · STT는 후보 4종(Gemini·Whisper·Google STT·ElevenLabs) **몽골 화자 한국어 샘플 20개로 실측 선정** | 원본 음성은 2TB에 **반드시 보관**(전사는 재실행 가능, 원본은 소급 불가) |
| **챗봇(2027-08 목표)** | 회화 봇 = E2 학습자 모델 + 게이트웨이 | 훈련 재료 = 지금부터 쌓인 스키마 그대로 |
| **+24개월~** | E3 봇 팩토리 — 직업 프로필 → 지식팩+프롬프트+평가셋 자동 조립 | 1호 = 유학 집약 봇(🚫일반 직업 봇 선행) |

---

## 1. 세팅 — 실측 상태

| 도구 | 상태 | 남은 세팅 |
|---|---|---|
| 클로드 ×3 | ✅ 가동 중 | 없음 — 계정 분담 규칙만 (§1-1) |
| GPT 코덱스 | ✅ Plus · **배포 검수 파이프라인 배선 완료**(읽기전용 3플래그·기본 luna high) | L2 구현 레인은 F0 착수 때부터 (§1-2) |
| 제미나이+제미나이LM | ✅ Google AI Pro 구독 + 자동 갱신 배선 + Gem·노트북 완비 | 없음 (§1-5) |
| 제미나이 **API**(몽골어 검문) | ✅ 키 작동 — 검문은 **공짜 몫 열쇠**(`SYNK_보안\제미나이_무료.txt` · 09-03 부터 「글」과 「돈」이 갈렸다 · 경로 정본 = `tools/모델정책.js`) · `tools/몽골어대조.js` 2겹 | ⚠무료 티어 — 학생 개인정보 금지 |
| 🔒 **제미나이 3.5 Flash Cyber**(보안 리뷰어) | 🔒 **못 켠다** — 정부·신뢰 파트너 한정 파일럿(CodeMender 경유). 유호님 실키 모델 목록에 **0건**(08-06 실측) | **세팅이 아니라 공개 대기** — 유호님이 손으로 하실 것도 없다 (§1-5) |
| **제미나이 Spark**(상시 수집) | ✅ 구독에 포함(별도 결제 없음) | **첫 스케줄 등록 = 유호님 손** (§1-5) · ⛔우리 자료·계정 연결 ✗ |
| 헤르메스 | ✅ 연결·자동 시작 (`@synk_yuho_bot`) | 없음 (§1-4) |
| 옵시디언 | ✅ 볼트=memory | 없음 (§1-3) |
| 오르카 | 🔒 봉인 | 트리거 = SYNK-talk 「일감 대기」 (§1-7) |
| **Supabase** | ⏳ **계정·프로젝트 생성 = 유호님 1회**(Apple·Google 개발자 계정과 한 묶음) | 그 뒤 L0 DDL 착수 (§1-9) |
| **Expo + EAS** | ✅ repo·CI 초록·첫 빌드·`EXPO_TOKEN` | ⏳빌드 눈 = Expo MCP 승인 (§1-9) |
| **n8n** | 미착수 | 호스팅(셀프/클라우드)·비용 = 설계 시 확정 (§1-9) |

### 1-1 클로드 계정 ×3 — 한도를 이어달리기로 쓴다

목적은 병렬이 아니라 **연속**이다. 유호님이 하루 20시간 작업을 원하는데 한 계정으로는 한도에 막힌다.

| 계정 | 등급 | 자리 |
|---|---|---|
| `unmet23@` | Max | **허브** — 라이브 repo·배포·정본을 만지는 유일한 곳 |
| `77yuhbs@` | Max | 병렬 생산 — 문서·콘텐츠·조사 |
| `unmet27@` | **Pro** | 예비 — 앞의 둘이 막혔을 때 가벼운 레인 |

- ☠️ **`unmet23` / `unmet27` — 한 글자 차이다.** 화면에서 거의 구별이 안 된다. **저장소가 안 보이면 코드보다 계정을 먼저 본다.**
- **공유되는 것**: 저장소 안의 전부(지침·가드·스킬·워크플로) · 배포 CI · GitHub 앱 설치. **계정별인 것**: 한도 · 대화 이력.
- 🚫 **계정2 해지·다운그레이드 재제안 금지**(08-04 유호님 확정 — "너무 막힌다"는 실사용 진술이 게이트의 통과 조건이었다). 계정을 **더 늘리는** 방향도 ✗ — 같은 병목을 돈으로 미루는 것이라, 남은 레버는 소모 절제다.

### 1-2 GPT 코덱스 — 역할 **셋**, 이름이 갈렸다 (08-06 유호님 지시)

- 실측: Codex CLI 설치·ChatGPT **Plus($20)** 로그인 완료. 추가 설치 0.
- 🔑 **셋 다 같은 명령(`codex exec`)으로 나가서 겉모습이 똑같다.** 그래서 이름을 갈랐다 — 08-06 유호님: *"왜 똑같은 걸 하는 것처럼 느껴지지?"*. 가르는 축은 **누가 쓰느냐**다: **①②는 클로드가 쓴 것을 GPT가 본다 · ③은 GPT가 직접 쓴다.**

| 이름 | GPT가 하는 일 | 대상 | 언제 | 유호님 시간 | 지금 |
|---|---|---|---|---|---|
| **① 배포 검수** | 적대 검토 | **코드** | 배포할 때마다 자동 | **0분** | ✅ 켜짐 — 편성은 `모델정책.js` |
| **② 설계 심문** | 적대 심문 | **설계 문서**(계약·L0·C0) | 문서가 자라면(드리프트 알림) | **0분**(백그라운드) | ✅ 켜짐 — **`luna/xhigh` ×1회**(유호 08-29) · ⚠08-20~29 즉사했었다(아래) |
| **③ 물리 작성** | **직접 구현** | L2 Expo 화면 · 물리층 DDL | 물리 구현 | 0분 | 🟡 **벽(F133)은 죽었다**(09-02 실측) · 실발주 0건 → 아래 |

**① 배포 검수** — ✅`/deploy` 4-B에 배선 완료(도구 = `tools/codex-review.js`). 읽기 전용은 **플래그 셋이 한 벌**(`--ignore-user-config` + `sandbox_mode="read-only"` + `approval_policy="never"` — 하나만 빼도 우회가 실측됐다). 픽 = **luna 기본 · sol 선택**(유호님 확정 "sol 너무 비싸다" · 🚫terra). 🔑 **모델·효력·회차 값은 여기 안 적는다 — 정본은 `tools/모델정책.js` 하나**이고 지금 값은 `node tools/모델정책.js` 가 낸다(이 줄이 08-15 판 「luna/high ×2」를 08-31 까지 말했는데 그 사이 편성이 세 번 바뀌었다). 3단(제안→판정→버그+기능전수).
- 🔑 **판단 패스는 2회 돈다**(유호님 확정 08-15 · 1단계 분석·기능체크 각 2회 → **합집합**). 같은 모델·같은 효력이어도 런마다 집는 게 달라서, 2회의 합이 1회보다 넓다. 합치는 건 **코드**가 한다(모델에게 시키면 떨어진 지적이 조용해진다). 대가는 **벽시계 시간뿐** — 급하면 `--회차 1`.
- 🔑 **지적마다 「합의 2/2·1/2」가 붙는다**(08-29). 같은 바이트를 회차 둘이 독립으로 보므로 그게 곧 **공짜 재현성 측정**이다 — 처분 순서를 정하는 신호. 근거: 원장 9일치에서 처분된 P1 의 **47%(9/19)가 기각**(오경보)이었는데 처분자가 쓸 재현성 재료가 없었다. ⚠**등급을 자동으로 깎지 않는다** — 08-12 실측에서 단독 3건이 전부 P0 였고 둘은 코드로 사실 확인됐다. 합의는 **순서지 면죄부가 아니다.**
- 🔑 **`--미처분`** — 도장(기각·채택수리·대상아님) 안 찍힌 지적을 등급별로 전수 조회한다. 08-29 첫 실측 **285건**(전체 지적 369 중 · P1 81·P2 74·P3 130) — 처분 이력은 다음 런의 재발 지적을 거르는 장치인데 **23%로만 돌고 있었다.** 막지 않는다(F103).
- 🔴 **`--저장소 <경로>` — 형제 저장소(SYNK-talk)도 이 통로로 검수한다**(08-31). 그전엔 도구가 형제 저장소를 **원리상** 못 봤다: `ROOT` 한 값이 「도구의 집」과 「재는 저장소」를 겸해서, talk 커밋 sha 를 `--commit` 으로 줘도 git 이 여기서 돌아 `fatal: ambiguous argument` 로 종료 2 였다(런 `검수-2026-08-31T0726-146d06`). ⚠**SYNK-talk 은 학생·강사가 쓰는 앱과 서버 전부인데 이종 검수를 한 번도 안 지났다** — 이 통로의 존재 이유가 「같은 벤더끼리는 블라인드스팟이 상관된다」인 만큼 그 자리가 통째로 비어 있었다.
  - `node tools/codex-review.js --저장소 ../SYNK-talk --commit <sha>` — git·codex 가 그쪽에서 돈다. ✅**실측 08-31**: 08-31 에 죽던 그 호출(`--commit 20d4455`)이 이제 talk 에서 **파일 12개**를 읽는다(`App.js`·`lib/관찰초안.js`·`src/관찰API.js` …). 같은 sha 를 집에서 대면 여전히 못 읽는다 — 그게 이 손잡이의 분모다.
    ⏭ **렌즈는 안 재봤다.** talk 에 `AGENTS.md` 가 있고 그게 `docs/이_저장소_규약.md`·`docs/공용지침.md` 를 가리키는 것까지는 **파일로 확인**했지만, codex 가 그걸 실제로 집는지는 **실런이 있어야 안다**(집에서의 근거는 「`AGENTS.md` 가 없을 때 `CLAUDE.md:45` 를 인용했다」 한 건뿐이라 우선순위를 추론한 것이다). 첫 talk 런이 그 답을 낸다.
  - 🔑 **장부는 여기 한 벌 그대로**(`docs/_ops/검수기록·검수기각·검수제안.jsonl`) — 저장소마다 두면 미처분 조회·런 목록·SessionStart 훅이 각자 반쪽 진실을 말한다. 대신 **행마다 `저장소` 를 찍고 읽을 때 가른다**: 지적 키가 `sha256(파일+제목)` 이라 저장소를 모르는데 두 저장소에 같은 상대 경로가 흔해서, 안 가르면 talk 에서 찍은 기각이 여기 지적을 조용히 걸러낸다(= 거짓 초록). 스탬프 없는 옛 행은 전부 「여기」다.
  - 🔴 **그 검수는 배포 게이트를 열지도 막지도 않는다.** `clasp-guard` 는 Apps Script 전용이고, talk 기록은 지문·범위가 비어 원리상 안 닿는다(회귀가 적대 픽스처로 민다 · `tests/검수타저장소.test.js`). **SYNK-talk 의 배포 게이트는 그쪽 `tools/원격배포.js` 의 배포판 대조**다 — 검수 초록을 배포 허가로 읽지 않는다.
  - ⚠ ②심문에는 안 쓴다 — 심문은 문서 경로를 그대로 받으므로 형제 저장소 문서도 경로 하나면 된다(겸용은 거절된다).
  - 🔴 **「아는 미완」 재료도 저장소마다 다르다**(08-31 · talk 첫 런이 스스로 낸 구멍). 그 첫 런의 지적 **3건이 전부** 「저장소가 스스로 미완이라 적어 둔 칸」이었는데, 재료가 `gh pr list` **하나**였기 때문이다 — 그건 이 집의 규약이고(유호 확정 08-15 「PR = 작업 목록」) talk 은 열린 PR 이 **0개**다. 블록이 통째로 안 실렸는데 로그는 「열린 트랙 0개」라 적어 **「미완이 없다」처럼** 보였다.
    - 재료를 목록으로 통일할 수 **없다**: talk 은 미완을 목록이 아니라 **산출물 안에 인라인으로** 선언한다(빈 측정란 · 「추정값」·「미측정」 주석 · 해소는 ~~취소선~~). 긁는 자는 실측으로 못 쓴다 — `남은 것 =` 4건(그중 3건이 이미 취소선) · `🔴` 342건(그냥 강조). 낡은 목록은 검수자를 **틀리게** 침묵시킨다.
    - → 재료를 두 **종류**로 갈랐다(`아는미완출처들`): **목록**(집 = 열린 PR) · **규약**(talk = README 「다음 관문」 + 「이 저장소는 미완을 이렇게 선언한다」는 읽는 법). 모르는 저장소는 빈 목록이 아니라 **«출처 없음»**이고, 셋(출처 모름 · 못 읽음 · 정말 0건) 다 **말은 한다** — 침묵이 「미완 없음」으로 읽히는 게 이 사고의 급소였다.
    - ⚠ 이 표는 talk 이 바뀌면 낡는다. 그래서 **낡으면 크게 운다** — 관문 절을 못 찾으면 조용한 0건이 아니라 「이 도구의 재료 표가 낡았다」 오류다(회귀가 못박는다). ⏭ 더 옳은 자리는 **저장소가 스스로 선언하는 것**이고, 그건 talk 커밋이 필요해 안 넣었다.

**② 설계 심문** — ✅**켜짐**(08-06 유호님 *"오케이. 키자."* — F121 종결). 도는 법 = **`node tools/codex-review.js --심문 <문서>`**(백그라운드로 던진다). 템플릿 정본 = `docs/_ops/동결심문_프롬프트.md`(옛 이름 「동결 심문」 = 같은 것) · 분업 정본 = SYNK-talk `docs/개발_분업.md` §8-2 · 장부 = `docs/_ops/심문기록.jsonl`(검수기록과 **분리** — 섞이면 배포 게이트가 오독한다).
- 🔑 **모델 = `gpt-5.6-luna` / `xhigh` · 1회**(유호님 확정 **08-29** — *"심문 루나 extra high 1회로 바꾸자"* · 정본 `tools/모델정책.js` 심문효력·심문편성). 08-15 의 `high ×2` 중 **심문 몫만** 먼저 대체했다 — 그때 ①검수는 `high ×2` 그대로였다(매 배포에 걸리는 비용이 다르다). ⚠**그 갈림은 08-31 에 닫혔다**: 유호님이 ①검수도 `xhigh ×1` 로 올리셔서 지금은 둘이 같다(정본 = `모델정책.js`). **효력이 역할별로 갈렸던 첫 자리** — 검수선택지의 「둘의 효력이 같아야 한다」는 같은 역할 안에서 sol↔luna 를 견줄 때의 조건이지 ①②를 묶는 규칙이 아니다.
  - 🔑 **회차를 내주고 깊이 한 칸을 샀다** — 08-15 의 정확히 반대 방향이다(그때는 `max×1` → `high×2`). 회차는 「런 간 편차」를, 효력은 「깊이」를 사는데 심문처럼 한 번이 무거운 자리에서는 후자가 값한다는 유호님 판정.
  - ⚠ **1회 런에는 합의 스탬프가 안 붙는다**(대조 상대가 없으면 지어내지 않는다) — 재현성 신호가 필요한 날은 `--회차 2`. 그날만 더 깊게는 `--효력 max`(luna 천장).
  - 🔴 **결과 파일 이름에 효력이 들어간다**(`…-luna-xhigh-1.md` · 08-29 수리). 안 그러면 **깊이를 올려도 옛 결과를 이어받는다** — 유호님이 xhigh 로 올린 그 자리에서 실측됐다(codex 호출 0회로 high 결과를 재사용). 조건이 이름에 없으면 이어받기는 조건 변화를 원리상 못 보고, 새는 방향은 언제나 「이미 돌았다」 쪽이다.
  - 옛 판(08-06 `sol/max` 1회)의 논리는 「되돌림 비용 최대 · 순수 판단 · 빈도 최소 → 비싸도 된다」였다. **비용이 0 이 되면서 전제가 바뀌었다** — 같은 시간에 두 번 돌리는 쪽이 낫다. 🚫「luna 가 유료가 됐다」는 실측 없이 sol 로 되돌리는 재제안 금지.
  - 🔴 **[2026-09-01] 「어느 문서를 심문하나」는 문서가 스스로 선언한다** — 유호님 「점검해줘」에 세어 보니 **심문받은 정본이 1벌**이었다(설계·계약·규격 중 본문이 스스로 「소급 불가」를 말하는 것만 13벌). 트리거가 「동결 직전」이라는 사람의 기억이라 그 순간이 안 왔던 것이다. 목록을 밖에 지으면 그게 둘째 정본이 되므로(`review-runs.js:191` 이 그래서 손을 뗐다), **`doc-graph` 의 「엣지는 항상 파생이 선언한다」를 그대로 빌려** 문서 아무 곳에 한 줄을 둔다:
    - `<!-- 심문: 소급불가 -->` = ②설계 심문 대상. 안 받았으면 `tools/ai스택점검.js` 가 운다.
    - `<!-- 심문: 면제 · 왜 면제인지 -->` = 대상이 아니다. **사유가 없으면 적색** — 사유 없는 면제는 「검토했다」와 「귀찮아서 껐다」가 같은 모양이다.
    - 🚫 자는 **세기만 한다.** 「이 문서가 소급 불가인가」는 판정이라 표식을 심는 것은 사람 몫이다.
    - ⚠ **표식은 백틱 없이 raw 로 놓는다** — 코드 표기(`` ` `` · 펜스) 안의 것은 문서가 «말하는» 것이지 «선언하는» 것이 아니라서 안 잡힌다. 바로 이 줄 위의 예시들이 그래서 안 잡힌다(09-01 실측: 이 가이드가 스스로 「미심문 대상」으로 잡혔고, 예외 목록을 짓는 대신 표기법으로 갈랐다).
    - ⚠ **표식은 문서 «맨 끝»에 심는다** — 머리에 끼우면 `doc-graph` 의 판번호 파서(머리 12줄만 읽는다)가 한 줄씩 밀린다. 13벌을 심을 때 대조로 확인했다(doc-graph 출력 268줄이 전후 완전 동일 · 09-01).
  - 🔑 **[2026-09-01] 문서를 개명하면 장부에 «개명 줄»을 얹는다** — `node tools/ai스택점검.js --개명 <옛경로> <새경로> --사유 "왜"`.
    - 왜: 장부는 append-only 이력이라 옛 이름이 영원히 남고, 그러면 두 자가 **서로 다르게** 틀린다 — `ai스택점검` 은 「추적 끊김」으로 **영원히 울고**, `review-runs` 는 파일이 없어 **영원히 침묵한다**(한쪽은 소음, 한쪽은 실명). 실물이 `docs/학생ID_종단_설계_v1.md` 였다: 장부의 **유일한** 심문 이력인데 개명 뒤 드리프트를 한 번도 못 냈다.
    - 🚫 **장부를 고쳐 쓰지 않는다.** 옛 줄은 그때 참이었으므로 남기고 사슬만 잇는다. 사슬 푸는 코드는 `tools/lib/심문장부.js` **하나**다(두 자가 각자 풀면 반드시 어긋난다).
    - 거절하는 자리 넷(강행 손잡이 없음): 값 부족 · 사유 없음 · 새 파일이 없다(오타) · 옛 파일이 아직 있다(그건 사본이다) · 장부가 그 옛 이름을 안 가리킨다.
  - 편성 이력: 08-09 `luna×2+sol×1` → **08-14 `max ×1`** → **08-15 `high ×2`** → **08-29 심문만 `xhigh ×1`** → **08-31 검수도 `xhigh ×1`**(지금 · 값의 정본은 `모델정책.js`). 08-15 는 **깊이 한 칸과 회차를 맞바꾼 한 벌**이다 — ⚠회차는 「런 간 편차」를 사지 「깊이」를 못 산다(효력이 모델보다 레버가 크다). 되돌림 비용이 특히 큰 자리는 `--효력 max` 로 그날만 올린다. 🔴**회차 «간» 대조는 여기서 또 끊긴다**(조건 두 칸이 동시에 갈렸다 · F281) — 처분표 §1 에 적는다.
  - ⚠ 잃는 것: **같은 모델 2회는 같은 맹점을 두 번 본다.** 관점 다양성이 필요한 자리면 `--검수 sol` 로 한 번 더 돌린다(sol 은 선택지에 그대로).
- 🔴 껐던 이유는 "가치 없음"이 아니라 **"유호님을 20분 세웠다"**(F121). 그래서 켜는 일의 본체는 심문이 아니라 **손 조립 제거**였다 — 이제 20분은 유호님 시간이 아니다.
- ☠️ **첫 실행은 거부당했고 종료코드는 0이었다**(08-06): 템플릿 **전문**을 보내는 바람에 GPT가 머리말의 ⏸꺼짐 표식을 읽고 실행을 거부했다(241바이트·겉보기 성공). 지시문과 운영 메모가 한 파일에 살면 모델은 둘을 안 가른다 → 도구가 첫 `---` 아래만 보내고, 결과에 등급도 「지적 0건」도 없으면 **확인 불가로 거절**한다.
- 실측 성적: 1패스에 **P0 11 · P1 7 · P2 1** · 전건 판정에서 채택·부분채택 15.
- 🔴 **08-29 — 이 표가 「✅켜짐」이라 적는 동안 심문은 9일간 즉사하고 있었다**(유호님 *"스킵되는 느낌이 강하게 들어"* — 맞았다). 원인 = **08-19 대철거(`e75fc7fc`)가 템플릿 `동결심문_프롬프트.md` 와 철학 추출기 `philosophy-card.js` 를 같이 걷었다.** 부르면 ENOENT 로 종료 2. ①검수는 매 배포마다 돌아 제 원장을 자가 재생했지만, ②심문은 **사람이 부르는 것**이라 아무도 그 즉사를 안 봤다 — 09일 산출물 0. 🔑 **「켜짐」은 배선의 사실이지 «돌고 있다»의 사실이 아니다.** 셋 다 복구(`84f65f09`) + 회귀 8벌 부활(266검사).
- 🔑 **트리거가 사람 기억에서 기계로 옮겨졌다**(08-29) — 「동결 직전」이라는 순간은 오지 않았다. 이제 심문받은 문서의 지문이 어긋나면 **세션을 열 때 `review-runs` 훅이 알린다**(0건이면 침묵). 심문한 적 없는 정본은 원리상 못 잡는다 — 그건 등재 목록이 없어서고, 지어내면 둘째 정본이 된다.

**③ 물리 작성** — 일터는 **SYNK-talk repo만**(AGENTS.md·pre-commit 완비). 발주서는 클로드가 자동 작성 + 구현 전 교차 검수. ⛔라이브 repo 불입 금지 유지.
- ✅ **F133 은 죽었다 — 2026-09-02 실측.** 코덱스 0.152.0 을 `-s workspace-write -c windows.sandbox="elevated"` 로 띄워 **셸 실행(`SPAWN_OK`)과 파일 쓰기(`WRITE_OK`) 둘 다 실물로 확인**했다(스크래치패드에 `probe.txt` 가 실제로 생겼다 · exit 0 이 아니라 **산출물**로 잰 것 — 08-06 이 「빈손 + 종료 0」이었으므로 종료코드는 이 자리에서 자가 아니다).
  - 옛 병(역사): 08-06 에 `CreateProcessAsUserW failed: 5` 로 **프로세스를 하나도 못 띄웠다**. 발주 2회 모두 빈손 + 종료 0. 뒤집힌 시점은 코덱스 **0.150.0** 이고(그 뒤 `elevated` 가 살아났다 · [[subscription-cli-review-lanes]]) **08-06~09-02 중 언제 죽었는지는 안 쟀다.**
  - ⏭ **아직 안 잰 것 — 「셸이 돈다」와 「발주 한 벌을 완주한다」는 다른 자다.** 프로브는 두 줄짜리였다. 실화면 발주 1건이 끝나야 「③이 실제로 돈다」가 참이 된다. **첫 발주가 그 답을 낸다**(그 전까지 ③은 「벽이 없다」이지 「돈다」가 아니다).
- → 08-06 유호님 확정 ②(**순서 뒤집기** — 클로드 구현 → 코덱스 검수)는 **F133 이 열려 있는 동안의** 임시 편성이었다. 정본 = talk `docs/개발_분업.md` §8-1-a(v3.2). 그 조건이 09-02 에 사라졌으므로 **표 원문(물리 펜=Codex)으로 복귀할 자리**다 — 복귀 실행은 첫 발주 1건으로 잰다.

- ❌ **커스텀 인스트럭션·프로젝트에 SYNK 지침을 심지 않는다.** ①②는 백지가 무기고, ③은 건별 발주서가 지침이다 — 심어둔 사본은 낡지만 일회용 발주서는 낡을 수 없다.
- ①②는 **매번 새 대화**. 기억이 남으면 백지 검증이 깨진다.

### 1-3 옵시디언 — 완료. 쓰는 법만

- 볼트 = 클로드 memory 폴더(판정 노트 전부 서로 링크됨). 08-03 연결 완료.
- `Ctrl+G` = 그래프 · `Ctrl+Shift+F` = 전문 검색 · 노트 하단 = 역링크(이 판정을 인용한 판정들).
- ❌ memory 폴더의 **이름·위치를 바꾸지 않는다**(클로드 기억 경로가 깨진다). 파일 정리·대량 편집도 금지 — 정본 작성자는 클로드다. 적고 싶은 게 생기면 클로드에게 말로.

### 1-4 헤르메스 — ✅ 08-03 연결 완료

- **봇 = `@synk_yuho_bot`(SYNK Commander)** · 홈 채널 지정 · 왕복 대화 확인. 자격증명 = `C:\Users\q1212\SYNK_보안\텔레그램.txt`.
- 확정 역할 = 폰 질문 창구. (결정 큐는 08-04 에 클라우드 봇으로 이관했다가 **08-20 에 통째로 소멸** — 아래.)
- 설정된 안전선: 유호님 ID **단독 화이트리스트**(`TELEGRAM_ALLOWED_USERS` · `GATEWAY_ALLOW_ALL_USERS=false`) · 작업 디렉터리 = 라이브 repo 밖 `C:\Users\q1212\hermes-work` · `home_mode: off`.
- 🛡 **읽기 전용은 이제 프로즈가 아니라 기계다** (08-03 실측으로 두 번 고쳤다):
  - ①`HERMES_WRITE_SAFE_ROOT=C:\Users\q1212\hermes-work` — 이 밖으로는 파일 도구가 **코드 수준에서 거부**한다.
  - ②텔레그램 툴셋에서 `terminal`·`file`·`code_execution`·`computer_use`·`delegation`·`browser` **비활성**.
  - 🔴 **왜 둘 다 필요했나**: ①만 걸고 실제로 시켜봤더니 봇이 **"터미널로 작성할게요"라며 우회해 repo에 파일을 실제로 썼다.** 게다가 헤르메스 자체 검증기는 「수정된 파일 없음」이라 **거짓 보고**했다 — 코드 독해나 봇의 말이 아니라 **파일 시스템을 직접 확인해야** 잡힌다.
  - 되돌리기: `hermes tools enable --platform telegram terminal file` (그 순간 경계는 다시 프로즈가 된다.)
- 🔴 남는 한계: ①**토큰이 새면 방어가 통째로 사라진다**(샜다 싶으면 `@BotFather` → `/revoke`) ②**메신저로 들어온 텍스트는 곧 명령이다** — 남이 보낸 글을 봇에게 그대로 넘기지 않는다.
- ✅ **자동 시작 켜짐**(시작프로그램 `Hermes_Gateway.vbs` · 끄기 = `hermes gateway uninstall`). **PC가 켜져 있어야 답한다.**

#### 🪦 결정 큐 클라우드 — **죽었다 (유호님 확정 2026-08-20 「소멸해줘」)**

- 08-04 에 세운 `@synk_queue_bot` 매일 9시 발송·30분 수거 통로는 **전량 걷혔다** — `tools/decision-queue.js` 삭제(`d1b77aa31`), 미결 87건도 그날 함께 소멸. `tools/telegram-answers-poll.js` 는 그때 안 걷힌 껍데기였는데 **09-01 에 걷었다**(⚠삭제됨 · 위임 3단계 재판정 ⓐ). 그날 원격도 재봤다 — `synk-memory` 의 워크플로 둘(`answers-poll`·`queue-push`)이 **`disabled_manually`** 라 부르는 통로 자체가 죽어 있었다.
- **지금 통로는 하나다**: 유호님이 정하시면 그 자리에서 `node tools/결정.js --추가` → 정본 `docs/_ops/결정.md` → 세션 첫머리에 훅이 요약을 띄운다. **폰으로 답하는 자리는 없다.**
- ⚠ 이 절을 지우지 않고 🪦로 남기는 이유: 「매일 9시에 답하면 된다」가 08-31 까지 이 문서 안 네 곳에 살아 있었다 — 죽은 통로는 지우기만 하면 다음 세션이 **다시 짓는다**.

### 1-5 제미나이 + 제미나이LM(구 NotebookLM) — ✅ Google AI Pro (08-04 유호님 결제)

🔑 **축 = 클로드가 「못 하는 것」만 메운다.** 능력 경쟁이 아니라 **결손 보전**이다. 클로드가 할 수 있는 일을 이쪽으로 옮기지 않는다 — 옮기는 순간 그 일이 기억에서 멀어진다.

| 딸려온 것 | SYNK에서의 자리 | 메우는 결손 |
|---|---|---|
| **시트·문서 속 제미나이** | 시트 우상단 `Ask Gemini`로 유호님이 **직접** 묻는다(✅실물 확인) | 🔑**클로드는 라이브 시트를 못 본다** |
| **제미나이LM (Plus)** | **클로드 한도가 찼을 때**의 조회 창구 — 출처를 찍어 답한다 | 한도. 이쪽은 클로드 한도를 **0** 쓴다 |
| **음성** | STT 실측·전사 | 클로드는 **오디오 입력을 못 받는다** |
| **Veo 3.1** (영상 생성) | 숏폼(리드크루 클립) 재료 | Sora 앱 종료(2026-04-26)로 비어 있던 자리 · ⚠API는 2026-09-24까지 살아 있다 |
| **Deep Research** | 넓은 1회성 조사 + **마누스 폐지로 빈 ㉠「밖의 사실 심문」 자리**(08-31 · 바로 아래 절) | ⚠정확도가 걸린 조사는 GPT 쪽이 앞선다(그래서 둘 다 쓴다) · ⚠**로그인 벽(FB·인스타) 뒤는 원리상 못 본다 — 그 자리는 지금 비어 있다**(§5) |
| **2TB 저장** | 학생 음성 **원본** 보관 | 원본은 소급 불가 — 반드시 남긴다(3년차 봇의 훈련 재료) |

- ✅ **자동 갱신 배선 완료**: `node tools/geminilm-drive.js`가 memory+docs를 이름 고정 PDF 5권으로 구워 `G:\내 드라이브\SYNK_제미나이LM`에(매일 21:00 예약 · `rot-check` 감시 · 왕복 2회 실증). 제미나이LM은 **API가 없다(불변)** — 자동 최신화 경로는 「드라이브 소스」뿐. (2026-08-22: 구글이 NotebookLM을 이 이름으로 바꿨다. 도구·예약 작업 이름은 전부 갈아탔고, 유호님 확정으로 드라이브 폴더 실물도 뒤이어 개명했다 — repo 파일명 `geminilm-*`, 예약 작업 `SYNK_GeminiLM`, 폴더 `SYNK_제미나이LM`. 폴더는 웹에서 **in-place 이름만** 바꿔 노트북 소스 바인딩[내부 ID 기반]을 지켰다 — 새 폴더를 만들어 옮긴 게 아니다.)
- ✅ **Gem 「SYNK 감각」**: 결손 보전 5가지만 · **결정 금지** · 정본 수치 지어내기 금지 · 학생 개인정보 거부 · 코드 금지. 심는 것은 느리게 변하는 것만(버전 붙는 정본 ✗ — 낡은 사본이 답하면 정본 분열).
- ⛔ **제미나이LM 조건 3**: 읽기 전용(답은 재료지 판정이 아니다) · 사본에 만든 날짜(생성기로만 제작) · 넣는 것 = memory+docs뿐.
- ❌ 셋 다(제미나이·제미나이LM·Veo) **학생 개인정보·학생 데이터 업로드 금지**(동의 범위 밖).
- 📌 **구독과 API는 별개다** — 구독은 유호님이 손으로 쓰는 것. 파이프라인에 들어가는 건 API 종량이고, 그 첫 실물이 몽골어 검문기다(아래).

#### 밖의 사실 심문 — 조사원 «둘»을 손으로 쓴다 (08-31 판정 · 마누스 폐지로 빈 ㉠ 자리)

🔑 **비었던 것은 하나뿐이다.** 마누스가 지던 축은 둘 — ㉠밖의 사실 적대 반박(법령·업종코드·FDI·상표·시세) · ㉡노트북 닫아도 도는 장시간 실행. **㉡은 하네스가 이미 진다**(claude.ai/code 클라우드 세션). **㉠만 진짜로 비었다** — codex 는 `read-only` 샌드박스에 네트워크가 없어 원리상 못 메운다(08-31 실측). 즉 「코드는 두 번 보는데 돈은 한 번도 안 본다」로 되돌아간 자리다.

🔑 **답 = 새 도구가 아니라 이미 결제 중인 조사원 둘.** 벤더가 달라서 «적대성»이 손으로도 산다 — 같은 물음을 둘에 던지고 **엇갈리는 지점만** 클로드가 판정한다.

| 조사원 | 어디 (추가 결제 0) | 부르는 때 |
|---|---|---|
| **제미나이 Deep Research** | Google AI Pro | 넓게 훑을 때 · 몽골 현지 소스가 걸릴 때 |
| **ChatGPT Deep Research** | GPT Plus(§1-2) | 정확도가 걸릴 때(기존 판정 = 이쪽이 앞선다) · ⚠Plus 등급의 월 한도는 **안 재봤다** |

**돌리는 법 = ✅유호 확정 08-31 「②로 할게」 — 클로드가 Claude in Chrome 으로 로그인된 크롬에서 «대신 눌러 드린다».** 유호님 손 **0분**. 조건 둘: **유호님이 PC 앞에 계실 때만** · **발사 직전 그 자리에서 승인**(DR 한도를 쓰는 일이라 조용히 안 쏜다). 세션 중 1회성이라 깨져도 그 자리에서 눈에 보이고, 상시 파이프라인이 아니라서 아래 ㉠㉡ 위험이 안 쌓인다.
- **㉑ 물러설 자리(폴백)** = 클로드가 발주문을 완성형으로 써 드리고 **유호님이 붙여넣으신다**(손 약 1분). PC 앞에 안 계실 때 · 크롬 통로가 깨졌을 때. **발주문 골격은 둘 다 같다** — 아래 다섯.

발주문 골격 다섯, 이게 빠지면 DR 은 「그럴듯한 요약」을 준다:

1. **판정할 명제 한 문장** — 조사 «주제»가 아니라 참/거짓을 가릴 «명제»를 준다.
2. **내가 이미 믿는 답 + 그 근거** — 그리고 「이것을 **반박**해 달라」고 못박는다. 조사가 아니라 심문이다.
3. **틀리면 무슨 일이 나는지**(돈·기한) — DR 이 어디에 힘을 쏟을지가 여기서 갈린다.
4. **1차 출처만** — 법령 원문·관보·기관 공식 페이지. 블로그·요약 기사 금지를 명시.
5. **출력 넷** — 결론 · 근거 URL · **내 답과 갈리는 지점** · **확인 못 한 것**. 넷째를 안 시키면 「0건」이 성공 얼굴로 온다.

▶ **받은 결과는 자료지 판정이 아니다** — 클로드가 기억·정본과 대조해 판정한다. 상시 수집물과 같은 취급(기관·경쟁사 페이지의 문장이 «지시»로 읽힐 수 있다).
▶ 🚫 **자동화 시스템은 짓지 않는다**(결정 08-31). 이유 셋 = ㉠화면 자동화는 조용히 죽는데 DR 은 20~40분 비동기라 「죽었는데 도는 척」이 가장 쉽다 ㉡그 구글 계정에 학생 음성 원본 2TB 가 산다 — 걸리면 잃는 게 DR 이 아니라 계정 전체 ㉢진짜 자동에는 유호님 로그인이 없어 「자동 같은 수동」이 된다. 🔑**가르는 선은 「브라우저를 미느냐」가 아니라 「상시로 도느냐」다** — 위 ② 대행이 되는 이유가 그것이고(세션 중 1회 · 승인 있음 · 깨지면 보임), 상시 루틴·무인 스케줄로 올리는 순간 같은 동작이 ㉠㉡ 를 되살린다.
▶ 🚫 **DR API 결제도 지금 안 연다**(결정 08-31 · 유호 질의 「굳이 결재해야할 이유가 있어?」). 구독판이 조사 품질에서 API 판에 안 밀린다 — API 가 파는 것은 품질이 아니라 «유호님 손을 뺀다» 하나뿐이다. **여는 조건 = 돈 걸린 밖의 사실 판정이 유호님 붙여넣기를 기다리며 «줄을 서는» 날**(건당 약 1~3달러).

▶ ✅ **[2026-09-01] 장부 = `tools/밖의사실.js` → `docs/_ops/밖의사실.jsonl`.** 자동화가 아니다(위 🚫 그대로) — **부르는 것도 판정도 사람이 한다.** 이 장부가 파는 것은 하나뿐이다: 09-01 까지 「반박을 안 받았다」와 「받아서 통과했다」가 **같은 모양**이었고, 그래서 코드는 두 번 보는데 법령·업종코드·상표는 한 번도 안 보는 상태가 조용했다. 이제 상태가 넷으로 갈린다 — 🔴열림(안 물어봤다) · 🟠반박받음(답은 왔고 판정이 없다) · ✅판정완료 · 🗄접음.
  - `--세움 "명제" --걸린것 "…" --내답 "…"` → `--발주문 <키>`(위 골격 다섯을 **이 문서에서 읽어** 채운 프롬프트를 낸다 · 복사본을 만들지 않는다) → `--반박 <키> --조사원 … --결과 …` → `--판정 <키> --결론 … --사유 …`
  - 열린 채로 남으면 `tools/ai스택점검.js` 가 세션 시작에 운다(기한을 적었으면 지난 것도 표시).

#### 제미나이 API — 몽골어 검문기 (✅08-05 배선 · 첫 API 파이프라인)

- `tools/몽골어대조.js` = **2겹**(역번역 대조 + 무맥락 문법 채점 · `--파일` 모드) · 키 = **공짜 몫 열쇠**(`SYNK_보안\제미나이_무료.txt` · git 밖 · 09-03 부터 「글」과 「돈」이 갈렸다 — 그림 한 번 구웠다고 검문까지 멈추지 않게. 경로를 아는 곳도 `tools/모델정책.js` 하나다).
  - 🔑 **모델·효력 값은 여기 안 적는다** — 정본은 `tools/모델정책.js` 하나이고, 지금 값은 `node tools/모델정책.js` 가 낸다. (08-31 까지 이 줄이 「3.6-flash」라 적고 있었는데 실물은 그 사이 3.7 로 올라가 있었다 — 한 값을 두 곳이 알면 갈리고, 문서 쪽이 언제나 옛 판이다. 09-03 에 3.8 로 또 올라갔고, 이 줄은 값을 안 적은 덕에 이번엔 안 낡았다.)
  - ✅ **[2026-09-01] 장부 = `docs/_ops/몽골어검문.jsonl`** — 매 실행이 한 줄(**통과·불통과 둘 다** · 통과만 남기면 「걸렸다」와 「안 했다」가 같은 모양이 된다).
    - 🔴 왜 생겼나: 그때까지 도장이 **사람이 쓴 문장**이었다. 도장 박힌 문서 4벌 중 **지문을 든 것 0벌** — 문안을 한 글자 고쳐도 도장이 그대로 남았고, 「이 문안이 검문을 지났나」를 셀 방법이 없었다. ①배포 검수는 이 병을 이미 고쳐 뒀는데(내용 지문이 바뀌면 옛 도장 무효) **같은 병을 한쪽만 고친 상태**였다.
    - 자는 ①배포 검수·심문 드리프트와 **같은 계산**이다(대상 파일 sha256 앞 12). `tools/ai스택점검.js` 가 「검문 뒤 바뀐 문안」을 센다.
    - ⚠ 인자 모드(문자열 둘)는 잴 파일이 없어 **드리프트를 원리상 못 잰다** — 문안은 `--파일` 로 검문한다.
- ⚠ **무료 티어 = 학생 개인정보 금지**(입력이 학습에 쓰일 수 있는 조건) — 콘텐츠·문안 검문 전용.
- 🚫 GPT 상시 몽골어 파이프라인 ✗ · 🚫3중 동시 검증 ✗(기존 판정).
- **음성 수집 착수 때**: Gemini API가 STT 후보 **5종**(Gemini·Whisper·Google STT·ElevenLabs·**SeamlessM4T**) 중 하나로 실측에 오른다 — **지금 고르지 않는다**(몽골 화자 한국어 샘플 20개로 실측 선정 · ⚠STT는 발음 오류를 「고쳐서」 전사한다 — 그 오류가 우리 데이터라 원본 음성 보관이 원칙).
  - 🆕 **SeamlessM4T = 후보 5번째로만 등재**(유호님 질의 2026-08-12 「하이브리드 파이프라인」에 대한 판정). 아키텍처로는 **기각** — 그 구조가 맡는 자리(몽골어 음성→한국어 텍스트 통역)는 우리 제품에서 **학생이 해야 할 일**이라, 기계가 대신하면 수집도 학습도 같이 사라진다. 우리가 받는 음성은 처음부터 **몽골 학생이 말한 한국어**다(`task_format` 7값 전부). 게다가 오픈소스 서빙은 GPU 인스턴스 상시 구동 = 새 상시 손일이라 「사람 손이 거의 안 가게」(철학 ③)와 「AI가 원격으로 짓고 고치고 검증」(개원에 바로 쓰게)을 둘 다 깬다. **다국어 음성에 강하다는 강점만** 실측 후보로 남긴다 — 아키텍처를 먼저 정하고 실측을 나중에 하는 것은 순서가 거꾸로다.

#### 제미나이 3.5 Flash Cyber — 코드 보안 리뷰어 (유호님 08-06 지시 · 🔒 아직 못 켠다)

- **자리 = 리뷰만.** 코드 취약점 발굴·검증·패치 제안 전용. **기존 제미나이 역할(결손 보전 6종·몽골어 검문)은 손대지 않는다** — 그 위에 얹는 게 아니라 별도 자리다. 실행은 유호님이 손으로(자동 파이프라인 아님).
- 🔓 이 등재로 「제미나이를 코드 검수자로 쓰지 않는다」(`tools/모델정책.js` 08-05 판정)는 **보안 취약점 리뷰 한정으로 좁혀졌다**. 일반 코드 리뷰 자리는 여전히 Codex.
- 🔒 **지금 켤 수 없다 — 세팅 문제가 아니다.** Google 공식 발표 문구: 「CodeMender를 통해 **정부와 신뢰 파트너에게만** 제공되는 제한 접근 파일럿」. AI Studio·Gemini API 일반 채널에 **없다**(같은 날 함께 나온 3.6 Flash·3.5 Flash-Lite는 AI Studio에 있다 — 셋을 한 발표로 묶어 읽으면 오판한다).
- **실측 3겹(2026-08-06)**: ①유호님 실키로 모델 목록 전량 조회 → `gemini-3.5-flash`·`gemini-3.6-flash`는 보이는데 **cyber는 0건** ②Google 공식 블로그 문구(위) ③**유호님 계정 AI Studio 모델 드롭다운을 원격으로 직접 열어 확인** — 3.6 Flash·3.5 Flash·3.1 Pro·3 Flash 등은 뜨는데 **Cyber는 한 줄도 없다**(읽기만 · 화면은 원상 복구).
- ▶ **열리는 날 할 일 1개**: 모델 ID를 `tools/모델정책.js` 제미나이사고 표에 등록 → 리뷰 전용 픽 추가. **지금은 코드 0줄**(있지도 않은 ID를 미리 적으면 폴백 금지 규칙에 걸려 조용히 죽는다).
- 🔁 **그때까지 이 자리는 비지 않는다** — 코드 보안 검수는 `/deploy` 4단계 보안 게이트 + Codex 적대 리뷰가 이미 진다. Cyber는 그 자리를 뺏는 게 아니라 **한 겹 더**다.
- ⚠ 열려도 **학생 데이터·개인정보는 금지선 그대로**(제미나이 계열 공통). 넣는 것은 코드뿐.

#### 제미나이 Spark — 몽골 어학원·교육사업 상시 수집 (유호님 08-06 지시 · 읽기 전용)

- **자리 = 시간축.** 같은 대상을 **반복해서 재 변화를 본다**(수강료·모집 공고·커리큘럼 추이). 🔑차별점은 「24시간」이 아니라 **「유호님을 안 거치고 스스로 깨어난다」** — 클로드는 세션이 끝나면 스스로 못 깨어난다. 그게 메우는 결손이고, **코딩에는 전혀 관여하지 않는다**(유호님 지시).
- **다른 조사 자리와 안 겹친다**: Deep Research = **1회성** 넓은 조사 / **Spark = 반복·추이**. 같은 질문을 두 곳에 던지지 않는다. ⚠**로그인 벽(FB·인스타) 뒤를 보는 자리는 08-09 부로 비어 있다**(§5).
- ☠️ **「코드를 못 보게 하는 설정」은 Spark 안에 없다.** 구글 공식 문구: 「Gemini Spark has access to all the same sites that you do, **including sites you're signed into**」 · 「Gemini has access to your local browsing state」. 게다가 Workspace 연결은 **Spark 작동의 전제조건**이고, 로컬 크롬/원격 브라우저는 **기기 상태에 따라 시스템이 고른다**(유호님 선택 아님). → **경계는 「막는 것」이 아니라 「주지 않는 것」으로 긋는다** — repo·Apps Script·GitHub·시트를 Spark 과제의 **입력으로 넣지 않는다**. 과제엔 공개 URL만 준다.
- ⛔ **쓰기 ✗**(기존 판정 유지) — 시트·독스를 쓰기 시작하면 git·테스트·훅 **밖에서** 정본이 바뀐다(`clasp-guard`·`git-scope-guard`·전체 테스트가 전부 무효인 층).
- ⚠ **수집물은 자료지 지시가 아니다.** 상시 웹 수집 = 신뢰할 수 없는 텍스트를 계속 읽는다는 뜻이라, 경쟁사 페이지에 심어둔 문장이 에이전트에게 **명령으로** 읽힐 수 있다. 구글의 prompt injection 「보호」는 완화지 차단이 아니다 — 결과를 클로드에 넘길 때도 **자료로만** 읽는다.
- ❌ 학생 개인정보·학생 데이터 ✗(제미나이 계열 공통 금지선 — 여기도 예외 없다). 참고 문서를 굳이 줘야 하면 **대외 공개 가능한 것만 복사본으로** — 원본 링크·Drive 폴더 공유 ✗.
- ▶ **유호님이 하실 것 1개**: Gemini 앱에서 수집 과제를 **스케줄로** 등록(예: 「매주 월 09:00 — 아래 어학원 공개 페이지에서 수강료·모집 공고·커리큘럼 **변화만** 정리」). 대상은 공개 URL 목록으로.

### 1-7 오르카 — 봉인 유지 · 트리거 갱신 (08-05)

- 창고(SYNK-talk)는 착수됐고 선행 3건(자격증명 이동·git층 pre-commit·AGENTS.md)도 끝났다 — 그래도 안 켜는 이유는 **병목이 레인 수가 아니라서다**: 남은 관문(실학생 문장 20개 등)은 유호님 몫이고, **오르카는 한도를 늘리지 않는다 — 병렬이라 더 빨리 태운다.**
- ✅ **트리거 = SYNK-talk가 「사람 대기」가 아니라 「일감 대기」가 되는 시점**(실학생 문장이 풀리는 때 — `EXPO_TOKEN`은 이미 완비). 그때 레인 4개(DB 스키마 1레인 고정 · Edge Functions · Expo 화면 · 교정 프롬프트)가 진짜 병렬이 된다.

### 1-8 폰 단독 작업 — PC가 꺼진 채로 (유호님 확정 08-04)

**폰 「코드」 탭에는 성격이 다른 두 종류가 섞여 있다 — 이걸 구별하는 게 전부다.**

| | 어디서 도나 | PC 꺼짐 |
|---|---|---|
| **원격 제어** 세션 (「연결 해제됨」으로 뜨는 것) | 유호님 데스크톱 | ❌ 안 됨 |
| **클라우드** 세션 (`+` → `☁ 기본값` + `레포 선택`) | Anthropic 서버 | ✅ **된다** |

- **폰에서 새 작업**: `+` → `레포 선택` `synk-appsscript` → 지시. ✅Claude GitHub 앱 설치 완료(범위 = `synk-appsscript`·`synk-memory` 2개만).
- ⚠ **폰 세션에서 `synk-memory`는 읽기만 — 커밋 금지**(그쪽은 `SYNK_MemoryPush`+Actions 트랙 — 세 번째 필자가 끼면 분기한다).
- **라이브 배포 = 커밋 「제목」이 `[배포]`로 시작**(제목 첫머리만 인정 — 본문에 써도 안 된다) → `deploy-live`가 대행(약 50초). PC에서는 종전대로 `/deploy`(보안 검토가 그 안에 있다).

### 1-9 제품 스택 신규 부품 — Supabase · Expo/EAS · n8n (08-05 신설)

- **Supabase**: ⏳ **계정·프로젝트 생성 = 유호님 1회**(polite: supabase.com → GitHub 로그인 → New project — Apple·Google 개발자 계정과 함께 SYNK-talk ⏳ 묶음). 그 뒤 클로드가 L0 DDL(Postgres)로 스키마를 세운다. **Free 티어로 시작**(DB 500MB·Auth 5만 MAU — 개발·설계 단계엔 충분) · **⚠유료(Pro $25/월) 전환 시점은 「사용량」이 아니라 「실학생 데이터가 처음 들어가는 날 전」**(2026-08-05 정정) — 근거는 용량이 아니라 **Free엔 다운로드 가능한 자동 백업이 없다**는 것이고, 학생 데이터는 날아가면 복구가 안 된다. 복구 시험 1회가 파일럿 게이트 조건(코어엔진 부록 A-9 · 지출 승인=유호님).
  - ⚠ **학생 데이터가 사는 곳이 된다** — 관할권·백업·동의(v18.9 개정 트랙)와 한 몸. RLS(행 단위 권한)는 L0 계약 항목.
- **Expo + EAS**: ✅ repo·CI 초록·EAS projectId·첫 빌드·`EXPO_TOKEN` 완비. ☠️「액세스 토큰」 화면이 둘(F112) · 로그인 없이 본 빌드 페이지를 실패로 읽지 말 것. ⏳빌드 눈 = Expo MCP 승인(유호님).
- **n8n**: 미착수 — **자동화가 AS 천장(실행 6분·트리거 90분/일)이나 외부 SaaS 연동 폭에 막힐 때** 건별로 켠다. 호스팅(셀프=서버비/클라우드=구독)·비용은 그 설계 세션에서 확정. **AS로 되는 일을 n8n으로 옮기지 않는다**(부품 수 = 유지비).

---

## 2. 역할 — 왜 이렇게 나눴는가

축은 **능력 비교가 아니라 "기억이 어디 사느냐"**다. 이 시스템의 기획·판정 품질은 모델 지능이 아니라 축적된 기억(재제안 금지 목록·실측치·판정 이력)에서 나오고, 그 기억은 전부 클로드 쪽에 산다.

| 일의 성질 | 가는 곳 | 이유 |
|---|---|---|
| 기억이 필요한 일 (기획·판정·정본·배포·데이터 계약) | 클로드 계정1 | 기억이 여기 산다 |
| 손이 많이 가는 병렬 생산 | 클로드 계정2 | 같은 기억·같은 하네스 |
| **코드**가 맞는지 배포 전에 재기 (**①배포 검수** · §1-2) | GPT 코덱스 | 기억이 **없어야** 잘하는 일 — 백지는 심문자의 무기 |
| **설계 문서**를 동결 전에 공격 (**②설계 심문** · §1-2) | GPT 코덱스 | 계약·스키마는 **소급 불가** — 동결 뒤엔 못 고친다 (✅켜짐 · `luna/xhigh` ×1 · 유호 08-29) |
| **닫힌 명세를 직접 구현** (**③물리 작성** = L2 화면·물리층 DDL · §1-2) | **GPT 코덱스** | 명세가 닫혀 있으면 기억이 필요 없다 — 발주서가 지침이다 (🟡F133 사망 09-02 · 실발주 0건) |
| 기억을 유호님 눈에 보이게 | 옵시디언 | 이미 옵시디언 문법(`[[링크]]` 노트) |
| 클로드가 **못 하는 것** (라이브 시트·오디오·영상 생성·한도) | 제미나이·제미나이LM | 능력 경쟁이 아니라 **결손 보전** |
| 몽골어가 맞는지 기계로 재기 | 제미나이 API (모델 = `모델정책.js`) | 유호님이 못 읽는 언어로 나가는 것 자체가 위협 모델 |
| 코드 **보안 취약점**만 따로 재기 | 🔒 제미나이 3.5 Flash Cyber (**공개 대기** — §1-5) | 취약점 전용 파인튜닝 = 일반 모델보다 이 한 겹에서 앞선다 · 열릴 때까지는 `/deploy` 보안 게이트 + Codex |
| 같은 것을 **반복해서 재 변화를 보기** (어학원·교육사업 추이) | **제미나이 Spark** (§1-5) | 클로드는 세션이 끝나면 **스스로 못 깨어난다** — 시간축이 결손 |
| 유호님 폰과 시스템 사이의 문 | 결정큐 봇·헤르메스 | 지휘층은 읽기 전용 |
| (조건부) 병렬 레인의 교통정리 | 오르카 | SYNK-talk 「일감 대기」부터 |
| **이 배치가 실제로 도는지 재기** | `tools/ai스택점검.js` | 아래 — 자가 셋인데 **자를 재는 자**가 없었다 |

#### 넷째 자 — 「배치대로 도는가」 (2026-09-01 · 유호님 「점검해줘」)

🔑 그날 처음 세어 보고 알았다. **검수는 코드를, 심문은 설계를, 정찰은 모델을 재는데 «역할 배치 자체»의 커버리지는 아무도 안 쟀다.** 그래서 아래 셋이 조용했다:

| 역할 | 그날 실측 | 무엇이었나 |
|---|---|---|
| ①배포 검수(Codex) | 런 **81건** / 08-20~08-31 · 훅까지 박힘 | 🟢 도는 자리 — 손댈 것 없다 |
| ②설계 심문(Codex) | **심문받은 정본 1벌** · 그마저 장부가 가리키는 파일이 개명돼 **추적 끊김** | 트리거가 사람의 기억이었다 |
| 몽골어 검문(Gemini) | 장부 **0줄** · 도장 4벌 중 지문 **0** | 「돌렸나」를 셀 자체가 없었다 |
| ㉠밖의 사실(DR 둘) | 장부 **0줄** | 「안 물어봤다」와 「통과」가 같은 모양 |

- `node tools/ai스택점검.js` = 한 장 전부 · `--훅` = 적색만·하루 1회(SessionStart 에 물려 있다) · `--json`.
- ⚠ **막지 않는다.** 알림이다 — 배포 게이트는 `clasp-guard` 하나면 족하다.
- 🚫 **심문 «드리프트»(심문 뒤 문서가 자란 것)는 여기서 안 센다** — 그 자는 `.claude/hooks/review-runs.js` 하나다. 같은 판정을 두 자로 재면 반드시 어긋난다.
- 🔴 이 자가 **자기 첫 실행에 자기 병을 밟았다**: 워크트리(`.claude/worktrees/…`)에서 돌리자 SKIP 의 `worktrees` 가 자기 문서 229벌을 통째로 걸러내고 「후보 0 · 걸린 것 없음」을 냈다. 절대경로에 자를 대면 저장소가 어디 놓였느냐로 답이 갈린다 — 회귀로 못 박았다(`tests/ai스택점검.test.js`). ⚠**같은 꼴의 SKIP 이 `tools/doc-graph.js` 에도 있다**(안 고쳤다 — 그쪽에서 재는 값은 워크트리에서 따로 의심한다).

**금지선 — 이것만 안 넘으면 시스템은 안 깨진다**
- ❌ GPT에 기획 **초안**을 맡기기 (기억이 없어 기각안을 재생산 → 큐만 증가)
- ❌ GPT에 상시 지침·커스텀 인스트럭션 심기 (사본은 스스로 낡는다)
- ❌ GPT 검증을 이어지던 대화에서 하기 → **매번 새 대화**
- ❌ Codex를 라이브 repo(SYNK-appsscript)에 들이기 (훅 무효 + 라이브 시트)
- ❌ 헤르메스에게 코드·파일 수정 시키기 (가드 0인 층)
- ❌ 제미나이·제미나이LM·Veo에 학생 데이터 넣기 · 무료 API 티어에 학생 개인정보
- ❌ **Spark 과제에 repo·Apps Script·GitHub·시트를 입력으로 넣기** (Spark 안엔 그걸 막는 설정이 없다 — 경계는 「주지 않는 것」뿐 · §1-5)
- ❌ **Spark에게 쓰기 시키기**(시트·독스 편집) — git·테스트·훅 **밖에서** 정본이 바뀐다
- ❌ **제미나이LM 답을 그대로 결정 근거로 쓰기** (사본은 낡는다 — 재료지 판정이 아니다)
- ❌ 옵시디언에서 memory 폴더 이동·개명·대량 편집
- ❌ **시트를 데이터 정본으로 되돌리기** (A안 기각 — 정본은 Supabase 하나, 시트는 파생 층)
- ❌ 시트→원본 역방향 쓰기를 계약 규격 없이 열기 (정본이 갈라진다 — P0 조항)

---

## 3. 사용법 — 유호님의 라우팅 규칙

**기본값: 무엇이든 일단 클로드.** 예외만 외우면 된다:

| 이런 순간 | 여는 곳 |
|---|---|
| **라이브 시트에 뭐가 들었는지 궁금하다** | **시트 안 `Ask Gemini`** — 클로드는 이걸 못 본다 |
| **클로드 한도가 찼는데 더 하고 싶다** | **제미나이LM**(조회) · 또는 계정을 바꾼다 |
| 유튜브·영상·이미지를 판독하고 싶다 | 제미나이 |
| 짧은 영상 소재가 필요하다 | 제미나이 **Veo** |
| 자료를 통독·듣고 싶다 (교재·정본) | 제미나이LM |
| 경쟁사가 뭘 하는지 알고 싶다 | **제미나이 Spark**(추이) ／ **Deep Research**(1회) — ⚠공개 페이지만. 로그인 벽 뒤는 지금 보는 자리가 없다(§5) |
| **어학원·교육사업이 뭐가 달라졌는지 계속 알고 싶다** | **제미나이 Spark 스케줄** (§1-5) — 1회 조사가 아니라 **추이**일 때만 |
| 넓고 빠른 웹 조사 | 제미나이 Deep Research ／ **정확도가 걸리면 GPT** |
| 몽골어 문안이 맞는지 재고 싶다 | 클로드에게 "몽골어 검문 돌려줘" (`tools/몽골어대조.js`) |
| SYNK-talk 화면을 만들고 싶다 | 클로드에게 말하면 된다 (유호님 몫 0) — **F133 사망(09-02)으로 Codex 직접 구현이 다시 가능하다**. 첫 발주 1건이 그걸 실측한다(§1-2 ③) |
| "예전에 뭐라고 정했더라?" | 옵시디언 `Ctrl+Shift+F` (또는 클로드에게 질문) |
| 폰으로 결정 처리 | 🪦 **없다** — 결정큐 봇은 08-20 소멸. 지금은 세션에서 말씀하시면 그 자리에서 `결정.md` 에 적힌다 |

### GPT 흐름 — 부르는 말이 셋 다 다르다 (08-06 이름 분리 · §1-2)

| 유호님이 하는 말 | 도는 것 | 유호님 몫 |
|---|---|---|
| (아무 말 안 해도) | **①배포 검수** — `/deploy`가 자동으로 부른다 | **0** |
| "이거 **설계 심문** 받자" | **②설계 심문** — 계약·설계 정본 동결 직전 | **0** — 클로드가 백그라운드로 돌린다 |
| "이 화면 만들어줘" | **③물리 작성** — 🟡F133 사망(09-02) · 첫 발주가 「정말 도는가」를 잰다 | **0** |

적용 급 = 되돌리기 어려운 결정·외부 공개물·계약/인증/전역상태/배포설정 코드. 공격은 클로드가 반영/기각으로 갈라 최종안을 낸다.

---

## 4. 말 한마디 명령표

| 하고 싶은 것 | 클로드에게 하실 말 |
|---|---|
| 폰으로 답한 결정을 정본에 반영 | "결정 답변 반영해줘" |
| 지금까지 정한 것 전체 보기 | "정한 거 전체 보여줘" (`node tools/결정.js`) |
| GPT 검증 | "이거 GPT 검증 받자" |
| 몽골어 검문 | "몽골어 검문 돌려줘" |
| **Supabase 착수** (계정 만드신 직후) | "Supabase 만들었어 — L0 착수해줘" |
| **P0 제품계약·첫 기능** | "P0 착수하자" |
| 음성 수집 시작 (제미나이 API) | "음성 수집 착수" |
| **STT 실측** (몽골 분 한국어 샘플 20개) | "음성 인식 실측하자" |
| 제미나이LM 묶음 재생성 | "제미나이LM 묶음 만들어줘" (`node tools/geminilm-export.js`) |
| 오르카 개봉 (talk가 일감 대기가 된 날) | "오르카 켜자" |
| **AI 역할이 배치대로 도는지 보기** | "AI 스택 점검해줘" (`node tools/ai스택점검.js`) |
| 밖의 사실에 반박 패스 걸기 | "이거 밖의 사실로 세워줘" (`node tools/밖의사실.js --세움 …`) |

---

## 5. 잠금 목록 (재제안 금지 — 해제 조건은 memory `ai-stack-decision.md`)

| 항목 | 상태 |
|---|---|
| Kimi K3 | ✅해지 완료(08-03 · 한 번도 안 썼음 실측) |
| Grok | ✗ (X에는 몽골 고객이 없다) |
| Manus | ✗ **폐지 — 유호님 지시 2026-08-31 「일단 삭제해줘」.** 08-16 에 「밖의 사실 적대 심문」 통로로 한 번 들였다가(`tools/마누스.js`) 걷었다. 실물 제거 완료: 도구·검수런 갈래·회귀 시험 3벌. 🔴 **비는 자리 = 법령·업종코드·FDI·상표·시세처럼 「틀리면 돈이 걸리는 밖의 사실」에 적대 반박 패스가 없어졌다** — 그 축은 지금 세션 안 검색 한 번으로 판정된다. 이력: 08-03 정찰 해제 → 08-09 잠금 → 08-16 재도입 → 08-31 폐지 |
| OpenClaw | ✗ (헤르메스와 같은 층 — 비서 둘 = 기억 분열) |
| OpenCode | ✗ (Max 로그인 미지원 = 별도 지갑) |
| 제미나이LM을 **자동화에** | ✗ API가 없다(불변) — 사람이 손으로 쓰는 조회 창구만 |
| 클로드 계정 추가·축소 | ✗ (늘리면 병목을 돈으로 미루고, 줄이면 막힌다 — 08-04 실측) |
| **시트 = 데이터 정본(A안)** | ✗ 기각(08-05) — 정본은 Supabase 하나 |
| **Google Workspace $7** | ✗ (F114 — Google AI Pro≠Workspace · 메일 쿼터 해법은 Expo 푸시 이전) |
| **Glide 신규 투자**(조립·수리·결제·GlideOS) | ✗ 폐기 확정(08-05) — 실물 제거만 파일럿 게이트 뒤 별도 승인 |
| 벡터 RAG | ✗ 조건부 — 지식 총량이 컨텍스트 상한을 넘는 시점에 자동 재판정 |
| 파인튜닝 프레임 · 멀티테넌트 | ✗ (기존 판정 유지) |
| 검수·심문 기본을 sol로 복귀 · terra | ✗ (유호님 확정 08-05·08-15·08-31 — 기본은 luna · sol은 `--검수 sol`로만 · 효력·회차는 `모델정책.js`) |
| 회차를 1로 내리기 · 상한 3 올리기 | ✗ (유호님 확정 08-15 — 급할 때 `--회차 1` 1회성만) |
| 효력을 max 로 되돌리기 | ✗ (유호님 확정 08-15 `high` — 그날만 `--효력 max`. 되돌리자는 제안은 high↔max **실측**을 들고 와야 한다 · 지금 0건) |
| GPT 상시 몽골어 파이프라인 · 3중 동시 검증 | ✗ (제미나이 검문기 판정) |
