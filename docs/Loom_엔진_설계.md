# Loom — SYNK 그래픽 엔진 설계 정본 (v1.3 · 2026-08-16)

> **유호 확정 08-16 (범위)**: 「**3D 초현실 · 펠트 · 3D 레진아트 · 글래시**한 느낌의 분위기를 혼합한
> **장점만**을 만들어서, **어느 프로젝트에 던져놔도** 원하는 분위기를 알아서 그래픽 문법으로 구현하는
> **AI 그래픽 봇 엔진**. 예를 들어 PPT 를 만들 때 규격을 알아서 분위기를 살려 **적재적소에 배치**한다.
> **싸구려 느낌이 아닌 진짜 비싸 보이는 느낌.**」
>
> **유호 지시 08-16 (자동 적용)**: 「캐러셀이든 문서 PDF 든 홈페이지든 Loom 이 추가되도록 시스템화한다.
> 내부용 문서마저 명품으로. 1번 2번조차 텍스트가 아니라 글래시한 재질로. 모든 여백에 이유가 있어야 한다.
> 기회비용 생각하지 말고 과감하게 — 기존 틀이 펠트·글래시·3D 초현실 구현을 방해하면 전부 무시해도 좋다.
> 확실히 가져갈 것은 마스코트 색뿐. 시스템 설계할 때 철학 정본만 신경 써라.」
>
> 값의 정본 = `docs/디자인_토큰.json`(색·재질·율·부품) · 통로 = `tools/lib/loom.js` ·
> **굽는 통로 = `tools/룸굽기.js`**(→ `룸장면.py` → `룸자산화.py` → `구운재질.json` · §3-6) ·
> 재질 파이프라인(펠트를 «만드는» 층) = `docs/펠트엔진_설계.md` — 이 문서는 그것을 «입히는» 층이다.

## 0. 한 문장

**던져놓으면 분위기를 판단해 배치하는 그래픽 봇.** 어느 지면(문서·캐러셀·인쇄물·PPT·홈페이지·앱)이든
네 재질의 장점만 섞은 한 분위기를 **자동으로** 입고, 부품이 제자리에 앉는다.
아무 생각 없이 만들어도 번호가 유리 원판으로 서고 여백이 스케일에 앉는다.
디테일을 사람이 매번 챙기면 그건 시스템이 아니다 —
철학 정본 「하지 않는 것 ㉡: 사람 손을 늘리는 해법을 쓰지 않는다」가 이 엔진의 존재 이유다.

**판정 축은 하나다 — 「싸구려로 읽히는가, 진짜 비싸 보이는가」.** 예쁜지가 아니다.
그리고 이 축은 취향이 아니라 **잴 수 있는 여섯 자**로 분해돼 있다(§3-4).

## 1. 왜 지었나 — 실측 진단 (2026-08-16)

유호님 진단(「브랜드킷이 글씨체·색밖에 없다」)이 정확했다. 다만 원인은 **Loom 이 빈약해서가 아니었다.**

| 재는 것 | 실측 |
|---|---|
| Loom 지면(`tools/펠트문서.js` 853줄)을 입은 HTML | **93개 중 6개** — 엔진 소개서 6벌뿐 |
| 캐러셀 | **0/8** — 전부 `:root{--ink:#171820…}` 손으로 박음 |
| `인쇄본빌드.js`·`발표물빌드.js`·`브랜드킷조립.py` | **0/3** — 각자 자기 CSS |
| `DESIGN.md`(= AI 가 시각 작업 전에 읽는 진입로)의 「재질」 절 | **0줄** |

**원인 둘.**
1. **스킨이 «문서 한 벌»로 통짜라 다른 지면이 조각을 못 가져간다.** 캐러셀은 레일도 표지도 필요 없고
   「번호와 불릿」만 필요한데, 통짜는 그걸 못 준다. → 부품을 조각으로 빼냈다(`tools/lib/loom.js`).
2. **진입로에 재질이 없으니 새로 만들 때 시야에 안 들어온다.** F472 그대로다 —
   정본에 값을 두는 것만으로는 안 되고 **누군가 그 값을 읽어야** 정본이 살아 있다.
   → `DESIGN.md` §2-d 에 재질·율·부품을 실었다(진입로가 자동 적용의 첫 다리다).

## 2. 층 구조

| 층 | 무엇 | 어디 | 상태 |
|---|---|---|---|
| **L0 값** | 색 23 · 재질 3 · 율 9단 · 부품 14 | `디자인_토큰.json` | ✅ 08-16 |
| **L1 재질** | 펠트(만들기) — 사진 기반 파이프라인 | `펠트엔진_설계.md` · `펠트패치.py` · `펠트색갈이.py` | ✅ |
| **L1b 굽기** | 유리·레진을 **광선추적으로 «찍는»** 층 — 「3D 초현실」이 사는 자리(§3-5) | `loom_장면.py`(스크래치패드) | 🔨 실증 · ▶`tools/` 승격 |
| **L2 부품** | 번호·불릿·칩·따옴·체크·오브 … 14종의 CSS | `tools/lib/loom.js` | ✅ 14/14 |
| **L3 율** | 여백 9단 + 호흡 5규칙 + 광학 6규칙 | 토큰 「율」 → `loom.js --율` | ✅ |
| **L4 지면** | 문서·캐러셀·인쇄·웹·부품만·인쇄부품·**밝은부품** — 프리셋 7 | `loom.js` 지면들 | ✅ 조립 / ✅전파 **23/23**(08-19) |
| **L5 강제** | «입어야 할 지면»을 등록하고, **어디에도 안 적힌 HTML 을 적색으로** 낸다 | `tools/lib/지면방.js` · `tests/지면방.test.js` | ✅ 등록층 08-16 |

## 3. 축 셋

### 3-1. 부품 — 「1번 2번조차 텍스트가 아니라 재질로」

🔑 **CSS `counter` 가 기본값이다.** 원고에 `<h2>제목</h2>` 만 써도 유리 원판 번호가 선다.
번호를 손으로 적게 두면 그 순간 지면마다 갈리고, 갈리면 시스템이 아니다.
`.번호` span 이 «있으면» 그것이 이긴다(옛 원고·접근성 호환 — `:has()` 로 갈랐다).

목록 14종이 곧 **분모**다: 토큰 목록에 있는데 CSS 가 없으면 그 부품은 아직 맨 텍스트로 남아 있다는 뜻이고,
그것을 세는 것이 `node tools/lib/loom.js --분모` 다. **목록에 없는 부품을 손으로 짓지 않는다.**

### 3-2. 율 — 「모든 여백에는 이유가 있다」

간격이 이름을 가지면 「여기 왜 24px?」에 **「블록 사이니까 `단`」**이라고 답할 수 있고,
답할 수 없는 간격은 애초에 안 쓰게 된다. 그게 이 축의 전부다.

`실 2 · 숨 4 · 틈 8 · 참 12 · 칸 16 · 단 24 · 켜 40 · 장 64 · 막 104`

4px 기반 ×1.5 근사인 이유: 선형(8·12·16·20)이면 인접 두 단이 눈에 안 갈려서 아무 데나 쓰게 된다.
**사다리** — `장 ≥ 켜×1.5 ≥ 단×2.5`. 절·문단무리·블록의 비율이고, 깨지면 절대값이 아무리 넉넉해도
문서가 한 덩어리로 읽힌다(`--율` 이 매번 재계산해 표시한다).

### 3-3. 재질 — 물성으로 위계를 낸다

> **유호님이 부른 넷을 이렇게 읽었다(설계 판단 · 다르면 교정 요청).** 원문의 네 낱말은
> 「3D 초현실 · 펠트 · 3D 레진아트 · 글래시」인데, 이 넷은 **같은 층이 아니다** —
> 펠트·레진·글래시(유리)는 **재질**이고, **3D 초현실은 그 셋을 «어떻게 만드는가»**다.
> 넷을 나란히 재질로 두면 위계가 안 선다(유리와 글래시가 같은 것이 되고, 「초현실」이 자리를 못 받는다).
> 그래서 **재질 3 + 굽는 방식 1**로 갈랐다 — 3D 초현실은 §3-5(굽는 층)가 통째로 그 답이다.
> 이렇게 갈라야 「장점만 혼합」이 성립한다: 재질끼리는 **직책**으로 안 섞이고,
> 초현실은 **셋 전부에 걸린다**.

셋은 취향이 아니라 **빛을 다루는 방식**으로 갈린다:

| 재질 | 빛 | 뜻 | 자리 | 개수 |
|---|---|---|---|---|
| **펠트** | 먹는다 (확산 .95) | 사람이 만든 것 | 브랜드 표식·표지 오브·마스코트 | 1~2점 |
| **유리** | 통과시킨다 (투과+반사) | 담는 것 — 뒤가 비쳐 층이 보인다 | 판·번호·태그·표 | 제한 없음 |
| **레진** | 가둔다 (두께+내부산란) | 여기가 제일 무겁다 | 히어로 숫자·핵심 표식 | **1점** |

한 지면에서 셋이 만나면 눈은 **먹는다 → 통과시킨다 → 가둔다** 순서로 읽는다.
이것이 `DESIGN.md` §1 「위계는 색이 아니라 밀도」의 확장이다 — **밀도 → 물성**.
그리고 **다른 회사가 못 베끼는 자리도 여기다**: 색으로 위계를 내는 것은 누구나 하지만,
물성으로 내려면 정본(값)과 측정(자)이 있어야 한다.

🔑 **유리·레진에 안료를 칠하지 않는다.** 유리의 색은 분산 림이 내고, 레진의 색은 «갇힌 빛»(코어 글로우)이 낸다.
칠하는 순간 「유리」가 아니라 「색 젤리」가 된다(유호 교정 08-15). 면이 아니라 빛이라
철칙 ④(바탕+잉크+신호 1점)의 «면» 조항에 안 걸린다 — 다만 **반복되는 부품의 갇힌 빛은 무채**여야 한다
(불릿 수십 개가 코랄이면 신호 1점이 죽는다). 코랄이 갇히는 곳은 히어로수 1점뿐이다.

### 3-4. 「비싸 보임」의 여섯 자 — 취향을 자로 바꾼다 (2026-08-16 실증)

유호 교정 08-16 「너무 싸구려 AI처럼」과 「아직도 퀄리티가 많이 부족해」는 **같은 축의 두 번**이다.
그런데 「비싸 보임」이 취향으로 남아 있으면 다음 판도 감으로 만들게 된다. 실증으로 여섯 갈래로 갈랐다 —
**전부 0원 레버이고, 하나라도 빠지면 그 자리에서 싸구려로 읽힌다.**

| # | 자 | 빠졌을 때의 모습 | 어디서 |
|---|---|---|---|
| ① | **환경** — 반사할 것이 있는가 | 단색 배경 = 반사할 게 조명뿐 → 흰 얼룩 몇 개가 떠 있는 그림 | 무대(환경 그라디언트·배경벽) |
| ② | **감광** — 검은 띠가 있는가 | 밝은 자리만 있으면 형태가 안 선다. **투명체의 윤곽은 어두운 띠가 만든다** | 감광판(빛이 아니라 «빛 아닌 것») |
| ③ | **뒤** — 투명체 뒤에 비칠 것이 있는가 | 알파로 오려낸 유리는 뒤를 잃는다 → 안 비치는 투명체 = **플라스틱** | 배경벽(카메라엔 안 보이고 굴절에만) |
| ④ | **불완전** — 기포·미세 요철·비대칭이 있는가 | 수학적으로 완벽한 면 = CG. 실물 레진에 기포 없는 판은 없다 | 셰이더 노이즈·기포 구·0.5% 찌그러짐 |
| ⑤ | **광학** — 심도·여백·오프센터가 있는가 | 정면·정중앙·꽉 참 = «도표»의 눈. 제품 사진은 살짝 옆에서, 여백을 두고 | 카메라(f/3.6 · 85mm · 여백) |
| ⑥ | **합주** — 부품이 서로를 아는가 | 따로 굽고 나중에 얹으면 서로 그림자도 반사도 없다 = **AI 콜라주** | 한 무대에서 함께 굽기 |

🔑 **⑥이 이번 회차의 도전안이자 가장 큰 발견이다.** 지금까지 부품은 하나씩 구워 알파로 얹었다.
그러면 아무리 잘 구워도 지면에서 「따로 만든 것들을 모아 놓은」 느낌이 남는다 —
원인은 부품 품질이 아니라 **부품들이 서로를 모른다**는 것이다.
실증 컷에서 셋을 한 무대에 세우자 유리 안에 레진의 붉은 기운이 담겼다. 그것이 «한 세계»의 증거다.

⚠ **대가**: ③⑥은 **그 지면의 배경을 알아야** 성립한다. 즉 부품 라이브러리를 한 번 구워 두고
아무 데나 붙이는 방식과 **원리적으로 충돌**한다. 화면(검은 무대)/인쇄(크림) 두 벌은 이미 있으니
당분간 두 벌로 굽고, 지면 배경이 셋 이상으로 늘면 그때 «지면이 굽기를 부르는» 배선으로 간다.

### 3-4b. 여섯 자 «위»의 층 — 더 잘하기 (v1.2 · 3차 실증)

§3-4 의 여섯 자는 **「망치는 것을 안 하기」**다. 전부 지켜도 «안 싸구려»까지고, 거기서 더 가려면
다른 성격의 층이 필요하다. 유호 질의 08-16 「더 발전시킬 방법이 없는지」에 대한 실측 답이다.

**A. 실증해서 들어간 것 (6)**

| # | 층 | 무엇이 달라지나 |
|---|---|---|
| ⑦ | 🥇**봉입 — 「가둔 것」** | 2차까지 우리 레진은 «빨간 유리공»이었다. **레진아트의 정체성은 가둔 것**인데 그게 없었다. 넣는 순간 깊이·스케일·손맛이 함께 선다(굴절이 확대·왜곡해 준다) |
| ⑧ | 🔴**`volume_bounces` = 0** | Blender 기본값이 0이다. **레진 속에서 빛이 한 번도 안 튀고 있었다** — 두께가 색을 만드는 재질인데 그 두께가 «필터 한 장»으로 납작했다. 8로 올리니 속이 살았다 |
| ⑨ | **볼륨 산란** | 흡수만 있으면 «색 필터»고, 산란이 있어야 «덩어리»다. ⚠2차에서 기각한 «볼륨 방출»과 다르다 — 방출은 흡수를 상쇄하지만 산란은 안 한다 |
| ⑩ | **박막 간섭** | 유리 표면의 아주 얇은 코팅이 각도마다 미세한 색을 낸다(비눗방울·보석 코팅·명품 유리의 서명). 안료가 아니라 **두께가 내는 색**이라 「유리에 안료를 칠하지 않는다」에 안 걸린다 |
| ⑪ | **코스틱 해방** | 코스틱을 «켜 놓고도» 안 보였다 — `sample_clamp_indirect`(간접광 클램프)와 `blur_glossy`(기본 1.0)가 잘라내고 뭉개고 있었다 |
| ⑫ | **필름 결** | 그레이딩(split toning) + 디더(8bit 밴딩 제거) + 그레인. **알아채면 진 것**인 층이다 |

**B. 실측으로 갈린 것 — 같은 층이 재질마다 다르다**

- 🔴 **박막을 레진에도 걸었더니 «보라 얼룩»이 떠서 갇힌 코랄을 오염시켰다.** 박막은 «맑은 유리»의 층이고
  레진의 표면은 매트하다. ⇒ **유리에만.**
- 🔴 **봉입물을 순금속(Metallic 1.0)으로 두니 검은 조각으로 묻혔다** — 순금속은 정반사만 하는데
  붉은 속에는 반사할 것이 없다. ⇒ 반금속 0.55(반사도 하고 확산도 한다).
- 🔴 **봉입물이 들어오면 흡수 밀도가 «두 일»을 한다** — 두께로 색을 만들고, 갇힌 것을 삼킨다.
  2.9 에서는 박편이 반사한 빛까지 흡수됐다. ⇒ 봉입 있으면 2.05 / 없으면 2.9.
- 🔴 **그레이딩 1회차가 과해 회색 하이라이트가 보라로 물들었다** ⇒ 세기 1/3.

**C. 4차에서 집행한 것 (유호 승인 2026-08-16 「전부다 승인할게」) — 자가 취향을 이겼다**

세 갈래 전부 실행했고, **둘은 승인 내용대로 섰고 하나는 실측이 기대를 깎았다.** 깎인 쪽을
「됐다」로 적지 않는 것이 이 절의 값이다.

| 갈래 | 실측 | 판정 |
|---|---|---|
| ⏳→✅ **HDRI 환경맵** | 유리구 A/B: 하이라이트 넓이 3.70%→**5.06%**(mono 0.9) · 그런데 국소 대비는 **-2.1%** | **작게 산다.** 「반사에 세상이 맺힌다」는 **과했다** — 우리 부품이 «구»라 환경이 저주파 얼룩으로 뭉개진다. 세기 **0.55** 로 고정(채우되 안 씻는 자리) |
| ▶→✅ **모따기** | 원판·판·칩에 폭 .030~.055 · 단 5~6 · `harden_normals` | **선다.** 모서리를 따라 하이라이트 «실선»이 생기고, 128px 로 줄여도 그 실선은 살아남는다 |
| ▶→🟡 **무대 안개** | 알파 덮임 59.4%→**59.5%**(Δ **+0.0%p**) · 그런데 몸에서의 변화가 **2.19/255** | **충돌은 풀렸고, 효과가 거의 없다.** 기본값 **끔**으로 둔다 |

🟡 **안개는 «충돌이 없다»가 «값이 있다»는 뜻이 아니다.** 처음엔 설계만 보고 「해결됨」이라고 적었는데,
   재 보니 **몸에서의 변화가 2.19/255** 였다. 안개가 살려면 물체 «사이»가 있어야 하는데
   단품 부품에는 사이가 없다 — 이 층은 **합주(여러 물체) 컷 전용**이고, 거기서도 작다.
   ⇒ 기본값 **끔**. `--안개` 로만 켠다. (설계로 판정하고 실측으로 뒤집힌 자리다.)

🔴 **HDRI 0.9 는 접지를 씻는다** — 알파 덮임 58.7%→**37.0%**. 환경이 밝아지면 그림자 캐처가
   받는 «가림»이 줄어 접지 그림자가 통째로 사라진다. 지면에 얹었을 때 물체가 **뜬다**.
   그래서 세기는 취향이 아니라 **접지가 살아 있는 상한**으로 정해진다.

🔴 **자가 나를 두 번 잡았다**(이 절이 실측으로 서 있는 이유):
   ① 첫 판에서 HDRI 를 켜면서 램프까지 같이 내렸다 — A/B 가 「환경 하나」가 아니라 「환경+조명 둘」이
      됐고, 결과는 «반사가 좋아진다면서 반사 구조가 -0.6%». 램프를 별도 눈금으로 분리했다(F045).
   ② 대조 **대상**을 레진으로 잡았다. 레진은 빛을 «가두는» 재질이라 환경이 속까지 못 들어간다.
      자를 옮기기 전에 대상을 옮겨야 했다(F183: 한 갈래만 재면 전체를 오단정한다).

🥇 **덤으로 잡힌 1~3차 전 판의 결함** — Cycles 램프는 기본값이 «카메라에 보임»이다. rim 소프트박스가
   피사체 뒤 위에 있어서 **매 판 실루엣을 가로지르는 흰 뚜껑**으로 찍혔다. 실물 제품 사진은
   소프트박스를 화면에 넣지 않는다. 끄니 16.56% 픽셀·평균 |Δ| 37.9/255 가 바뀌었고 **알파는 불변**
   (= 조명은 1도 안 건드렸다 · 반사·굴절엔 그대로 산다).

**C-2. 글자 봉입 — 「봉입물에 «의미»를 가둔다」(유호 승인 08-16)**

미검증이었던 질문은 **「작은 글자가 굴절 너머로 읽히는가」** 하나였다. → **읽힌다. 조건 셋을 지키면.**
①크게(구 반경의 **55%** 이상) ②앞쪽 얕게(굴절이 뒤쪽을 삼킨다) ③정면을 **비껴**
(정면 평행이면 굴절이 글자를 «판판한 스티커»로 만들어 갇힌 느낌이 죽는다).
⚠대비를 깎는 것은 코어 글로우다 — 자모 바로 뒤에서 배경이 밝아지니 흰 자모가 옅어진다.
그래서 **히어로 1점 전용**(킷 철칙 ④)이고, 본문 부품에는 안 쓴다.

**D. 원리적 한계 — 여기서 더 가면 성격이 바뀐다**

렌더 값으로 갈 수 있는 곳의 끝이 가깝다. 남은 가장 큰 지렛대는 **형태 자체**다 —
지금 우리 부품은 «구»이고, 명품이 명품인 이유의 절반은 형태가 남과 다르기 때문이다.
그건 값이 아니라 **디자인 결정**이라 유호님 몫이다(⏳).

### 3-5. 굽는 층 — 「3D 초현실」이 사는 자리 (v1.1 신설)

헌법 ①은 「결은 사진 픽셀에서만 · 프로시저럴은 매끈한 왁스로 읽혀 기각」이다. 그런데 v1.0 은
**펠트만 그것을 지켰다** — 유리·레진은 `linear-gradient`+`backdrop-filter`, 즉 5~6년 전 글래스모피즘이자
지금 AI 툴의 기본 산출이었다. 자기 헌법을 자기가 어긴 자리였고, 펠트 옆에 나란히 놓이자 둘이 가짜로 드러났다(F498).

🔑 **헌법 ①의 확장 — 「사진이 없는 재질의 «사진»은 렌더다」.**
펠트는 실물이 있으니 찍는다. 유리·레진은 실물을 살 수 없으니 **광선추적으로 «찍는다»** —
굴절·프레넬·분산·코스틱을 실제로 계산하면 그것은 흉내가 아니라 물리다.

| 재질 | 어디서 오나 | 통로 |
|---|---|---|
| **펠트** | 실물 사진의 울 패치(잘라 쓴다) | `펠트패치.py` · `펠트색갈이.py` |
| **유리·레진** | **Blender Cycles 렌더**(굴절·분산·기포·코스틱을 계산) | ✅ `tools/룸장면.py` |

**비용 = 0원.** Blender 는 무료이고, 이 노트북의 **Intel Arc B390 GPU** 가 실제로 잡힌다(실측).
1400px·420샘플 3구 합주 컷이 **1분 49초**, 900px·320샘플 단품이 **약 1분**.
API 결제·구독·다운로드가 **하나도 필요 없다** — 즉 이 축에서 돈은 병목이 아니다.

### 3-6. 굽는 통로 — 승격 (v1.3 · 2026-08-16 · 유호 승인 「전부다 승인할게」)

**승격이 전파보다 먼저인 이유**: 굽는 스크립트가 스크래치패드(= repo 밖)에 있으면 다음 세션은
그것을 못 보고 다시 CSS 로 짓는다. F498 은 「유리·레진이 흉내다」가 아니라 **「흉내 말고 다른 길이
저장소에 없다」**였다.

| 자리 | 무엇을 안다 | 왜 갈랐나 |
|---|---|---|
| `tools/룸굽기.js` | **통로 하나.** 부품표·해상도·씨앗·HDRI·분모 | 손으로 blender 를 부르면 판마다 값이 갈린다 |
| `tools/룸장면.py` | 장면만 — 재질·조명·부품 형상 | 값이 아니라 «무대»라 따로 산다 |
| `tools/룸자산화.py` | 구운 판 → 지면이 삼킬 것(잘라·줄여·WebP·base64) | 굽는 것과 «싣는 것»은 다른 일이다 |
| `tools/룸잰다.py` | 대조판을 **눈이 아니라 자로** 읽는다 | 「비싸 보임」이 취향으로 남으면 다음 판도 감이다 |
| `tools/lib/loom.js` §②-b | 구운 자산을 지면 CSS 에 얹는다 | 93개 지면이 지나는 **그 한 자리**다 |

```bash
node tools/룸굽기.js --자산      # HDRI 환경맵(CC0)
node tools/룸굽기.js --전량      # 부품 전량 굽기
python tools/룸자산화.py         # → docs/Loom_자산/구운재질.json
node tools/lib/loom.js --분모    # 「있나」 + 「무엇으로 그렸나」 두 축
```

🔑 **분모가 두 축이 됐다.** 「부품이 있나」와 「그 부품이 **무엇으로** 그려졌나」는 다른 질문이다.
앞의 축만 세면 **14/14 초록인 채로 재질 전부가 글래스모피즘인 상태**를 «완성»으로 읽는다 —
F498 이 정확히 그 상태였고, 부품 분모는 그때도 14/14 였다.

🔴 **회귀 장치 (실사고 08-16)** — `blender -b -P` 는 스크립트가 예외로 죽어도 **exit 0** 이다.
각진 부품 셋(원판·판·칩)이 `AttributeError` 로 통째로 안 구워졌는데 통로는 「⏱ 1.8초」를 찍고
다음으로 넘어갔다. ⇒ **시간·exit code·stderr 어느 것도 근거가 아니다.** 통로가 «판이 디스크에
있는지»를 확인하고 없으면 던진다. (원인은 내 `hasattr` 줄이 삼항이라 대입을 못 막은 것 —
hasattr 를 쓴 줄이 hasattr 의 보호를 못 받는 모양이었다.)

### 3-7. 🔴 방향 전환 — 재질 넷을 «섬유 하나»로 좁혔다 (2026-08-18 · 유호 지시)

유호 교정 08-18: 「지금 라이브판이랑 처방 적용이랑 둘 다 20년 전에 열심히 만든 듯한 느낌이야.
너무 많은 스타일을 혼합하고 있는 거라면 (글래시·3d초현실·펠트) 저 사진의 깔끔하고 정갈한
분위기·질감처럼 만드는 작업에만 집중해줘도 만족할 것 같아. 냉정하고 솔직하게 판단해줘.」

**판정: 좁히는 것이 맞다.** 근거는 취향이 아니라 셈이다 —
레퍼런스(털 아이콘)는 재질 1종·색 무채 1·오브젝트 1개에 밀도를 100% 쓴다. §3-3 의 우리 판은
재질 3종 × 부품 14종 × 색 23이라 **같은 노력이 14갈래로 쪼개진다.** 「장점만 혼합」은 실제로는
「어느 것도 최고가 아님」으로 끝났고, 그것이 「20년 전」의 정체였다.

🔑 **그리고 좁히는 것이 «손해»가 아닌 이유** — 유리·레진은 서로 남남이라 유리를 고쳐도 레진은
   1도 안 는다(개선이 3갈래로 갈린다). 반면 **펠트와 털은 같은 섬유의 두 상태**다:

   | | 길이 | 세움 | 밀도 |
   |---|---|---|---|
   | **펠트** | 짧다 | 눕는다 | 매우 높다 |
   | **털** | 길다 | 선다 | 중간 |

   숫자 셋만 움직이면 한 통로가 축 전체를 낸다 — **조명을 한 번 고치면 축 5단이 동시에 좋아진다.**
   실증 = 같은 코드·같은 형태·같은 씨앗으로 펠트(0.0) → 마스코트 자리(0.5) → 롱퍼(1.0) 를 뽑았다.

🔑 **마스코트는 그 축의 «중간»에 산다.** 유호님의 「펠트 느낌이지만 털 느낌이기도 하다」는 감이
   아니라 좌표였고, 이제 「결 0.50 · 길이 14 · 세움 0.72」로 못 박을 수 있다. 그전에는 판마다 갈렸다.
   ⚠이 확정은 08-14 「유호 확정 = 펠트」와 어긋나지 않는다 — **Loom 이 그 확정을 안 따라가고
   있었던 것**이고, 유리·레진은 그 뒤에 붙은 곁가지였다(「살아 움직이는」 느낌은 섬유만 낸다).

⇒ 유리·레진은 **버리지 않고 강등**한다: 표지·히어로 **1점 전용**, 문서 본문에서는 뺀다.

**헌법 ① 확장 (필수)** — 기존 조항은 「결은 사진 픽셀에서만 · 프로시저럴은 매끈한 왁스로 읽혀 기각」인데,
털 렌더러는 프로시저럴이지만 **실제 가닥 기하를 그려 빛을 계산**한다. 그 조항이 막으려던 것은
「그라디언트로 재질을 «흉내»내는 것」이지 「렌더」가 아니다. 안 적어 두면 다음 세션이 헌법 위반으로 지운다.
⇒ **결은 ①실물 사진 ②실제 기하를 그린 렌더 — 둘 중 하나. 그라디언트 흉내는 그대로 금지.**

**통로 둘** (둘 다 실물이 있고, 같은 수식의 형태를 쓴다):
- `tools/룸털.py` — Blender Cycles. 가닥 간 그림자·헤어 BSDF·전역 조명을 «계산»한다. 축 5개는 그 파일 머리말.
- ~~Canvas 판~~ — 🚫**폐기**(유호 확정 2026-08-19). 아래 §3-8 이 사유를 진다. 🚫재제안 금지.

⚠ **아직 못 한 것**: 레퍼런스의 «2겹 구조»(뒤 진한 회색 + 앞 밝은 흰색)가 Blender 판에 없다.
  그리고 「구멍 둘」은 **4연속 실패 후 접었다** — 방향 공간·3D 좌표계 둘 다에서 두 구멍이 «찢어진 상처»로
  합쳐졌고, A/B 로 보니 없는 쪽이 명백히 나았다. 구멍은 그 오브젝트의 «얼굴»이지 «재질»이 아니다.

### 3-8. 섬유 축을 «부품»까지 — 레시피 한 벌 + 재질 스위치 (2026-08-19 · 유호 확정 「1,2,3,4 전부」)

§3-7 이 방향을 정했고(재질 넷 → 섬유 하나), 여기는 그것을 **지면이 쓰는 부품**까지 끌고 온 자리다.

**🔑 레시피를 한 벌로 못 박았다** — `tools/lib/룸섬유.py`. 섬유를 굽는 자리가 둘이기 때문이다:
`룸털.py`(축을 재는 시험대) · `룸장면.py`(원판·판·칩을 굽는 곳). 두 벌로 적으면 갈라지고,
갈라진 날 **시험대만 좋아지고 지면의 부품은 옛 값으로 구워진다** — 새는 방향이 늘 「통과」인 자리다.
회귀 `tests/룸섬유.test.js` 5건이 «사본 0» 을 기계로 센다(서명 세 낱말이 모듈 밖에 있으면 적색).

**재질 스위치** — `blender -b -P tools/룸장면.py -- --부품 원판 --재질 털`.
⚠**기본값은 안 바꿨다**(`유리`). 바꾸는 순간 이미 구운 자산 8벌이 한 번에 갈리고, 그건 별도 트랙이다.

#### 🔴 재 보고 «틀린 것으로 판명된» 가설 — 톤은 레버가 아니다

「앞 덩어리가 흰색이 아니라 회색으로 읽힌다」의 범인이 AgX 톤 곡선이라 보고 A/B 했다. **아니었다**:

| 판 | 뒤 덩어리 | 앞(위) | 앞(중앙) |
|---|---|---|---|
| AgX · exposure −0.10 (기본) | 99.1 | 141.5 | 124.9 |
| Standard · 0.0 | 103.6 | **149.0** | 129.5 |
| AgX · **+0.90 EV** | 105.7 | 148.7 | 132.0 |

**+0.9 EV 를 줘도 앞이 7/255 밖에 안 오른다** ⇒ 한계는 조명이 아니라 **재질**이다(섬유 덩어리는
가닥 사이 다중산란으로 스스로를 어둡게 만든다 — 흰 털뭉치가 사진에서 0.5~0.6 으로 찍히는 그 이유).
「더 희게」의 자리는 톤이 아니라 광원 세기(축 ③)이고, 그건 명암을 먹는다. **다음 사람이 이 A/B 를
다시 하지 않도록 표째 남긴다.** 기본값은 그대로 AgX 다(Standard 이득 5% · 하이라이트 위험은 더 크다).

#### 🔴 그 A/B 를 «두 번» 돌리게 만든 것 — enum_items 가 동적이다

첫 A/B 는 통째로 헛돌았다. `view_transform`·`look` 열거자는 OCIO 에서 실행 시점에 채워져서
`bl_rna.properties[…].enum_items` 가 **`['None']` 하나만** 준다(Blender 4.2.9 실측). 그래서
「후보에 있으면 지정」 식 코드는 **한 번도 지정하지 않고** 기본값에 머무는데, 로그도 렌더도 멀쩡하다 —
두 판이 같은 톤인 채 「A/B 를 했다」가 된다. ⇒ **지정하고 되읽어 확인한다**(`톤지정()` · 안 먹으면 찍는다).

#### 실측 둘 — 밀도는 «부품 크기»로 다시 잡는다

- 시험대 값(24,000뿌리 × 26자식 = 624k 가닥)을 반경 1 원판에 그대로 심었더니 500px·64샘플이
  **11분을 넘겨 타임아웃**했다. 덩어리는 넓고 부품은 좁은데 밀도를 그대로 쓴 탓이다 ⇒ 3,200 × 10.
- 부품은 지면에서 **35~90px** 로 앉는다 — 그 이상의 가닥은 픽셀이 못 받는다.

#### 실측 셋 — 굽는 통로는 섰고, **다음 레버는 무대다**

| 무엇 | 값 | 뜻 |
|---|---|---|
| **섬유 렌더 프로파일** | 11분 초과 타임아웃 → **50초** | 유리 예산(투과 32튐·코스틱·간접 무제한)을 털에 그대로 쓴 것이 원인이었다. 털엔 투과가 없고 대신 «가닥 다중산란»이 있다 — 같은 돈으로 사는 게 없다 ⇒ `--재질 털` 일 때만 `max_bounces 8 · 투과 0 · 코스틱 끔 · 간접 클램프 10`. **유리 경로는 한 줄도 안 건드린다** |
| **길이 = 펠트/털을 가르는 축** | 0.105 → **0.030** | 첫 판은 원기둥 «면»에서 가닥이 방사로 뻗어 **민들레**로 읽혔다. 번호 원판은 손톱만 하게 앉는 부품이라 결이 «표면의 결»이어야지 «삐침»이면 안 된다 |
| 🔴 **그런데 어둡다** | 원판 몸 평균 **70.9/255** | 재질 밝기는 0.875 인데 판은 숯이다 |

🔑 **그리고 그 어둠의 자리가 어디인지 «잴 수 있었다»** — 같은 섬유 재질이 시험대(`룸털.py` 의 무대)에서는
**141/255**, 부품 무대(`룸장면.py`)에서는 **94.5/255**(털) · **70.9**(펠트)다. 재질도 톤도 아니고
**무대가 다르다**: `룸장면` 의 조명·감광판은 «투명체의 윤곽을 어두운 띠로 만드는» 유리 무대이고
(설계 §3-4 ②), 확산체인 섬유를 거기 세우면 그 띠가 그대로 «전체를 먹는다».

⇒ **다음 레버는 조명(축 ③)이고, 그건 «값 하나»가 아니라 «무대 한 벌»이다.** 섬유 부품은
`룸장면` 의 유리 무대가 아니라 **섬유 무대에서 구워야 한다**(시험대에 이미 서 있는 그것).
⚠그 무대를 여기서 손대지 않은 이유: 조명 세 줄은 4판을 돌려 굳힌 값이고, 「더하는 층만 얹는다」가
F503 의 처방이다(감광판을 더하면서 조명까지 같이 갈아엎어 유리가 하얗게 뜬 그 사고). **다음 트랙 몫.**

#### 축 ⑦ 형태 — 레버로 뺐고, **3판을 실제로 구웠다** (⏳유호 픽)

설계 §3-4b D 가 「남은 가장 큰 지렛대는 **형태 자체**다 — 값이 아니라 디자인 결정이라 유호님 몫」이라
적어 둔 자리다. 도구는 «고르게 해 주는 것»까지만 한다: `룸털.py --지수 N --축 a,b,c`.

값을 셋 구워 나란히 뒀다(2026-08-19 · 시험대 무대 · 500px·48샘플):

| `--지수` | 읽힘 |
|---|---|
| **2.0** | 순수 구 — 실루엣이 완전한 원. 「부품」보다 「오브젝트」로 읽힌다 |
| **4.2**(지금 판) | 모난 쿠션 — 위·아래가 눌린 «둥근 사각형」. 지금 부품(원판·판·칩)의 정체성 |
| **9.0** | 거의 정육면체 — 모서리가 뚜렷하고 «구운 비스킷」에 가깝다 |

⚠**렌더 비용이 지수를 따라 오른다** — 2.0·4.2 는 각 ~1분인데 9.0 은 그 두 배 넘게 걸렸다(같은 샘플·해상도).
날카로운 모서리 근처에서 노이즈가 늘어 디노이저가 더 오래 도는 탓으로 보인다 — **재지 않은 관찰**이니
9.0 근방을 실제로 쓰려면 샘플을 낮추거나 시간을 다시 잰다.
값을 바꿔 굽고 나란히 보면 된다 — 원본 3장은 이 세션의 스크래치패드에 있다(repo 밖).

#### 남은 것 (정직하게)

- 🚫 **Canvas 판 — 폐기**(유호 확정 2026-08-19 「폐기」). 두 가지가 겹쳤다:
  ①그 판은 죽은 세션의 스크래치패드(= repo 밖)에 있었고 컨테이너와 함께 **사라졌다**(복구 불가).
  ②**존재 이유가 없어졌다** — 그 판의 목적이 「Blender 가 없는 자리」였는데, 이제 클라우드 컨테이너에서
  Blender 가 실제로 돈다(공식 tarball · 4.2.9 · 이 트랙에서 실증). 굽는 층은 어차피 한 번 굽고
  base64 로 싣는 구조라, 「렌더러가 두 벌」인 상태의 값은 **갈라질 위험**뿐이었다.
  🚫**재제안 금지** — 되살릴 이유(예: 브라우저에서 실시간으로 결을 바꿔야 하는 화면)가 «먼저» 생기면
  그때 짓는 편이 싸다. 그 전에 다시 꺼내지 않는다.
- **부품 3종의 섬유 판 실물** — 원판·판·칩 **셋 다 구웠다**(50~70초/판 · 형태·결 전부 일관). 다만
  위 실측대로 **무대가 유리 무대라 셋 다 어둡다**(원판 70.9 · 판·칩 미측정이지만 같은 무대라 같은 값대).
  그대로 지면에 얹으면 「숯 부품」이 된다. ⇒ **섬유 무대로 옮기는 트랙이 먼저다** — 스위치·밀도·
  프로파일·형태 레버는 전부 섰으니, 그 트랙은 무대(조명 한 벌) 하나만 옮기면 끝난다.

## 4. 자동 적용 — 「Loom 이 추가되도록」

| 다리 | 무엇 | 상태 |
|---|---|---|
| ① 진입로 | `DESIGN.md` §2-d — AI 가 시각 작업 전에 반드시 읽는 자리에 재질·율·부품을 실었다 | ✅ |
| ② 통로 | `tools/lib/loom.js` 하나 — 값은 전부 토큰에서. **이 파일 밖에서 지면 CSS 를 손으로 쓰지 않는다** | ✅ |
| ③ 프리셋 | 지면별로 «필요한 조각만» 가져간다 — 캐러셀에 레일·표지를 억지로 안 준다 | ✅ |
| ④ 강제 | **분모를 등록하고** 어디에도 안 적힌 HTML 을 적색으로 낸다 — `지면방` | ✅ 08-16 |
| ⑤ 전파 | 기존 지면을 갈아 끼운다 | ✅ **23/23** (2026-08-19 · §5-2) |

```bash
node tools/lib/loom.js --css 캐러셀 > 그_지면.css
```

## 5. 전파 — 분모와 함께 (▶ 남은 일)

### 5-0. 🔑 분모가 세 판 연속 갈렸다 — 세는 법이 아니라 **분모가 없었다**

`6/93` → `1/87` → `7/99`. 세 번 다 「맞는 얼굴로 틀린 값」이었고, 원인은 하나다 —
**docs 안 HTML 을 통째로 세면 «애초에 입을 자리가 아닌 것»이 섞인다.** 아카이브(구판 보존)·
굽기 원고(원료)·일회성 판정 재료(하루 쓰고 죽는다)·자립형 파생본은 «아직 안 입은 지면»이 아니다.
분모가 틀리면 진척도 틀리고, 틀린 진척은 「아직 멀었다」로도 「거의 다 됐다」로도 읽힌다.

⇒ **등록층 `tools/lib/지면방.js`** (08-16). 추적 HTML **전건**에 판정을 붙인다:

| 판정 | 셈 | 뜻 |
|---|---|---|
| **입는다** | **21** | Loom 을 입어야 하는 살아있는 지면 — **이것이 전파의 분모다** |
| 보류 | 10 | 입을 지면이 맞는데 지금은 못 입는다. **조건을 반드시 적는다**(기계가 검사) |
| 제외 | 68 | 애초에 입을 자리가 아니다. **사유를 반드시 적는다** |
| 미분류 | **0** | ← 이것만이 적색이다 |

**전파 = 23/23** (2026-08-19 실측 `지면방 --분모` · 마커만 0 · 미분류 0). 남은 일감 0.
⚠ 이 숫자를 손으로 옮겨 적지 마라 — 참값은 문서가 아니라 그 명령이다(아래 ⚠).

### 5-1. 게이트는 «전파»가 아니라 «분류»에 걸린다

🔑 안 입은 지면이 있다고 빨개지지 **않는다.** 그건 일감이지 결함이 아니고, 주인 없는 적색은
남의 배포를 막는다. 빨개지는 것은 **미분류 하나** — 새 HTML 이 셋 중 어디에도 안 적힌 경우다.
이래야 「새 지면이 손 CSS 로 조용히 태어나는」 경로가 실제로 막힌다.

```bash
node tools/lib/지면방.js --분모     # 전파 N/M · 규칙별 포획 수 · 미분류
```

⚠**이 장치가 틀릴 때의 모습** = 너무 넓은 제외 패턴이 «입어야 할 새 지면»을 삼킨다
(미분류 0 = 초록인데 분모만 조용히 작아진다). ⇒ 닫은 것 = `docs/` **최상위는 와일드카드 제외 금지**
— 살아있는 새 지면이 거의 전부 거기서 태어나므로 그 층만 개별 등재를 강제하면 구멍이 구조적으로 닫힌다.
회귀 13 · **변이 8/8**(구멍 0 · 미적용 0 · `tools/변이.js` 공용 통로).

⚠**옛 셈을 인용하지 않는다** — 「6/93」은 «Loom 처럼 생긴 CSS 를 손으로 지닌 지면»을 센 것이고,
「1/87」·「7/99」는 분모에 아카이브·원고·판정 재료가 섞인 값이다.
🔴 **그리고 이 문서 자신이 그 함정에 빠졌다**(2026-08-18 실측) — 「7/21」을 세 곳에,
「10/21」을 두 곳에 적어 **한 문서가 두 값을 동시에 주장**했고, 실측은 셋 다 아닌 **10/22** 였다.
⇒ **참값은 문서가 아니라 `node tools/lib/지면방.js --분모` 다.** 인용할 일이 있으면 그 자리에서 돌린다.
그리고 이제 통로에 재질이 실렸으므로 **한 지면을 옮길 때마다 그 지면이 재질을 함께 얻는다**
(옮기기 전에는 옮겨 봐야 흉내였다).

### 5-2. 🔑 마지막 12벌이 안 입던 진짜 이유 — 「범위」와 「밝기」 둘 (2026-08-19)

`10/22 → 13/22` 에서 멈춰 있던 아홉 벌은 «어렵다»가 아니라 **두 구조적 구멍**에 걸려 있었다.
둘 다 새는 방향이 「통과」다 — 훅이 0 이니 안 얹고, 안 얹었으니 아무도 안 운다.

**㉠ 범위가 없으면 맨요소 훅은 «어디에도 안 닿는다».**
`부품만`·`인쇄부품` 은 `ol>li`·`ul>li`·`h2` 같은 맨요소 규칙을 `.룸` 안에 가둔다(F517 의 정당한 대가).
그래서 `.칩` 을 안 쓰는 지면은 목록·제목이 아무리 많아도 **훅 0** 이었다. 안 입은 12벌 중 10벌이
정확히 이 상태였다. ⇒ 지면에 `class="룸"` 컨테이너를 준다. 이게 이 트랙 작업의 절반이다.

**㉡ `낮` 이 통째로 `@media print` 안에 있었다 — 「밝은 화면」이라는 자리가 아예 없었다.**
🔴 실측(엔진 지도 · 렌더 전/후 대조): `인쇄부품` 을 밝은 화면 지면에 얹으면 **제목이 사라진다**
(Chalk `#E4E4E7` on Paper `#FAFAF9` = **1.21:1**). 그런데 원판·칩 같은 부품은 어두운 물체라 멀쩡히
보이므로 **「얹혔다」는 초록이 그대로 뜬다.** 맞는 얼굴로 틀린 값의 교과서적 형태다.
⇒ **`밝은부품` 프리셋 신설** = 인쇄부품 + `밝은글`(= `낮` 의 «글 색»만 `@media` 밖에서 한 번 더).
값은 두 벌로 안 적는다 — `밝은글자()` 하나에서 둘 다 읽는다(갈리면 한쪽만 안 보이게 된다).

**밝은글이 «안» 하는 것 둘** — 둘 다 실측으로 깎였다:
- **남의 맨 글자(`p`·`li`·`td`)는 안 만진다.** 밝은 지면 «안»의 어두운 섬(header 띠)까지 Ink 가 되어
  그 칸이 통째로 사라진다. 종이에서는 Loom 이 판 전체를 가지므로 `낮` 은 그대로 만진다.
- **인용을 안 흐린다.** 남의 blockquote 는 «판» 위에 앉을 수 있다(개인정보 게시본 = Chalk 판).
  거기에 Ash 2 를 얹으면 **4.46:1** 로 렌더린트가 문다. 판 색을 모르면 «흐림»으로 대비를 보장 못 한다.

**지면 갈래마다 다른 통로가 진다** — 손 HTML 은 생성기가 없어 한 번 얹으면 끝이라 F630 이 되풀이된다:

| 갈래 | 통로 | 몇 벌 |
|---|---|---|
| 손 HTML(생성기 없음) | **`tools/지면얹기.js`** — 등록층 + 프리셋 + 낡음 적색 | 6 |
| 발표물 | `발표물빌드.js` — 밝은 칸에만 `.룸`(휘도로 가른다) | 6 |
| 파이썬 생성기 | `브랜드킷조립.py` 가 조립 끝에 위 통로를 부른다(CSS 는 안 베낀다) | 1 |
| 조립 단계에서 CSS 를 품음 | 엔진 소개서·부품 시연·대장 3벌 | 9 |
| 캐러셀 | `지면얹기` — 단 **`.룸` 을 안 씌운다**(아래 🔑) | 1 |

🔑 **발표물의 `.룸` 은 «판 전체»가 아니라 «밝은 칸»에만 씌운다.** 덱에는 어두운 슬라이드가 섞여 있고
(01 = 밝은 12 + 어두운 5 · 03 = 표지 2), 낮 층은 그 안의 글까지 Ink 로 바꾼다 — 어두운 판 위 Ink = 사라짐.
어느 칸이 어두운지는 **이름(`dark`·`cover`)이 아니라 휘도로** 가른다(이름 규칙은 다음 덱에서 조용히 샌다).

🔴 **`.민` — 번호를 손으로 적은 `ol`**(`<li><span>1.</span>…`)에 counter 를 씌우면 «① 1. …» 두 번이다.
실물 = 01 덱의 `ol.q3` 하나. 기본 규칙은 **안 건드리고** 옵트아웃을 덧댔다 — 선택자를 고치면
`흠 검사`가 그 모양을 재고 있어 「순번 원판이 없다」로 거짓 적색을 낸다(실측).

🔑 **캐러셀은 `.룸` 을 «안» 씌운다 — 부품 클래스만 단다.** 이게 「1080 정사각이 깨진다」의 답이었다.
`.룸` 을 씌우면 맨요소 규칙(h2·p·ol)이 카드를 통째로 접수해 **절 번호 counter 가 카드마다 01 로 리셋되고**
제목·본문 조판이 문서 지면의 것으로 갈린다(실측 2026-08-19 렌더). 그런데 부품 클래스(`.번호`·`.칩`…)는
애초에 `.룸` 밖에 산다 — 즉 **범위를 안 주면 정확히 「재질만」 들어온다.** 새 프리셋이 필요 없었다.

⚠그래도 값 하나가 부딪힌다: Loom 의 `.번호` 는 `2.05em` 원판 규격과 `position:relative` 를 들고 오는데,
캐러셀 배지는 `cqw`(컨테이너 비율)로 잰 **절대 위치** 원판이다. 그대로 두면 배지가 모서리에서 본문 옆으로
흘러내린다(실측). ⇒ 지면이 `.card .badge{…}` 로 **특정도를 한 칸 올려** 자리·치수를 쥐고, 재질만 Loom 것을 쓴다.
🔑 그리고 **이름이 비슷하다고 같은 부품이 아니다** — 카드의 `.rule` 막대에 `실금` 을 씌웠더니 통째로 사라졌다.
토큰의 「구분선」은 «양끝이 옅어지는 절 구분선»이고, 카드의 막대는 «한 장의 신호 1점»이다. 안 씌운다.

**대가 · 안 닫은 것**: 배경을 자기 규칙이 아니라 **부모·이미지에서 받는** 칸은 밝다고 읽는다.
그 칸이 어두우면 글이 사라진다 ⇒ 빌드 로그가 밝음/어둠 셈을 찍는다(0 이면 눈에 띈다).
회귀 = `tests/지면얹기.test.js` 10건(픽스처 8 + 실저장소 거짓양성 2).

**측정**: 렌더린트 전/후 대조 — 만진 지면 11벌 전건에서 **대비 위반·킷 밖 색이 한 건도 안 늘었다**
(엔진 지도 16 · 컴퓨터 7 · 동반자층 15 · 운영 2 · 브랜드킷 26 은 **전부 선재**이고 ③ 중성축의 빚이다 —
`#676767 on #E4E4E7 = 4.46`. 아래 §9).

### 5-3. 갚는 순서

**이미 선 것 셋** — ✅굽는 층 승격(§3-6 · `tools/룸굽기.js`→`룸장면.py`→`룸자산화.py`) ·
✅`펠트문서.js` 배선(`d9fb41cf` — 같은 선택자 20개를 두 곳에서 정의하던 것을 `loom.js` 소비로 바꿔
소개서 6벌이 구운 재질을 입었다 · 전파 1→7) · ✅분모 등록(§5-0 — 이게 없으면 아래 어느 칸도
「몇 개 남았나」를 못 말한다).

🔴 **그리고 「캐러셀 8벌」도 같은 나쁜 분모였다**(08-16 실측 · 등록층이 서자 드러났다).
이름에 «캐러셀/carousel» 이 든 파일은 7벌인데 갈래가 이렇다:

| 갈래 | 셈 | 무엇 |
|---|---|---|
| 제외 | 5 | `docs/tools/` 하네스 2 · 체리젤리 시안 2 · 자립형 파생 1 — 지면이 아니다 |
| 보류 | 2 | 「괜찮아요」(발행 캐러셀 = 1080 정사각이라 문서 프리셋을 씌우면 깨진다) · 자개판(⏳유호 판정) |
| **지금 집을 수 있는 것** | **0** | ← 캐러셀은 다음 칸이 아니다 |

⇒ 순서를 바로잡는다 — 남은 **14벌**을 이 차례로 갚는다:

1. ~~**발표물 본판 6벌** — 차단자가 없다.~~ → ✅**3/6 착지 · 「차단자 0」은 틀렸다**(2026-08-16 · F517).
   재보니 차단자가 **넷**이었고 넷 다 「맞는 얼굴로 틀린 값」이라 세기 전엔 안 보였다:

   | 무엇 | 실측 | 닫은 방식 |
   |---|---|---|
   | ①**부품이 마크업에 없다** | 6벌 전부 부품 클래스 **0개** | `발표물빌드.js` 가 **훅이 있을 때만 얹는다** |
   | ②`부품만` 이 남의 판을 때린다 | 규칙 62 중 **28이 맨요소** · 6벌에 **34자리** | `css({범위})` — 맨요소만 `.룸` 안에 가둔다 |
   | ③종이에서 부품이 사라진다 | `부품만` 엔 `낮` 이 없다(Cream on 흰 = 1.13:1) | `인쇄부품` 프리셋 신설(부품만 + 낮) |
   | ④얹는 CSS 가 남의 규율을 어긴다 | `@page{margin:14mm}` 가 `size:A4;margin:0` 을 덮음 · KC Cool Blue(Part 6 전용) | `@page` 는 블록째 버림 · `기본분산={인쇄부품:false}` |

   🔑 **①이 제일 중요하다.** 전파 술어(`지면방` 의 `입은표시`)는 CSS 주석 마커 하나라,
   **얹기만 하면 부품이 0개여도 7/21 → 13/21 이 초록으로 켜진다.** F514 가 «무엇을 세는가»(분모)
   였다면 이건 «무엇을 세면 입은 것인가»(**술어**)다 — 분모를 고쳐도 술어가 헐거우면 진척은 다시 거짓이 된다.
   ✅**일반 해법 착지 2026-08-17** — 술어를 「마커가 있나」에서 **「닿는 훅이 있나」**로 바꿨다
   (`tools/lib/loom훅.js` 신설 · 빌드와 등록층이 **같은 함수**를 부른다 — 뿌리는 쪽과 세는 쪽이
   갈리면 조용히 통과하므로 재는 자를 하나로 뒀다). 선택자가 요구하는 클래스·태그가 **전부**
   마크업에 있어야 «닿는다»로 센다.
   - `:not()`·`:has()` 안은 요구가 아니고, `html`·`body`·`*`·`:root` 는 훅이 아니다 —
     `.글,article,body{…}` 의 `body` 하나 때문에 술어가 통째로 참이 되면 고치나 마나다.
   - 마커[i]→마커[i+1] **구간만** 읽는다. Loom CSS 의 끝을 텍스트로 못 찾아서(끝 표식이 없고
     마지막 마커 뒤로 재질층이 4.7만자 이어진다), 감싼 `</style>` 까지 긁으면 **원고 CSS 가
     훅으로 새어 들어와** 술어가 다시 헐거워진다.
   - 지면방 상태가 **셋**이 됐다: `안얹힘`(일감) · `마커만`(**결함 · 적색**) · `입음`.
     옛 판은 `!입음` 하나라 결함이 일감 사이에 섞여 원리적으로 안 보였다.
     적색이 하나 늘었다 — **미분류**(분모가 샌다 · F514) + **마커만**(술어가 샌다 · F517①).
   - 실측: 21벌 전건에서 옛 술어와 **한 건도 안 갈렸다**(전파 10/21 유지 · 거짓 강등 0 · 마커만 0).
     ⚠인계문의 「7벌은 부품 클래스가 아니라 맨 `h2`·`ol` 로 입었다」는 **반만 맞았다** —
     그 7벌은 부품 클래스도 18~33개 쓴다. 다만 «클래스만 세는» 술어로는 전역 프리셋 지면을
     원리적으로 못 읽는 것은 사실이라, 판정은 클래스가 아니라 **«닿나»**로 세웠다.

   🔑 **얹는 자리는 원고 CSS «뒤»다.** `.칩` vs `.tag` 는 특정도가 같아(0,1,0) 앞에 두면 원고가 이겨
   훅을 달아도 **아무 일이 안 일어난다** — 훅은 늘고 화면은 그대로인 자리다.

   ▶남은 3벌은 「자연스러운 부품 자리가 없다」 — 01·03 은 슬로건이 `<h1>`(칩이 아니다) · 02_mn 은
   `.lbl` 32개(언어 표식)라 한 지면 강조 상한과 부딪는다. **억지로 달면 장식이다.**
   진짜 자리는 **서수 번호**(`.n`·`.no`·`.num` 계열 **75자리**)이고, 지면마다 눈으로 봐야 한다.
2. **docs 최상위 상시 지면 6벌** — 브랜드킷·이해대장·운영 한눈에·자율주행 결정록·
   개인정보처리방침·「컴퓨터를 잃어버렸을 때」. 뒤 둘은 브랜드렌더린트 등록층이라 교차검사가 물고 있다.
   ⚠이해대장·결정록은 **자동 생성**이라 생성기(`tools/`) 쪽을 갈아야 한다 — 산출물을 손으로 고치면
   다음 생성이 exit 0 으로 되돌린다(`펠트문서.js --재현` 이 잡은 것과 같은 병).
3. **엔진 지도 1벌** — 소개서 6 은 이미 입었는데 지도만 남았다(같은 갈래인데 굽기 짝이 없다).
4. **캐러셀 프리셋** — 위 보류 2벌을 열려면 1080 정사각 프리셋이 먼저다. 그 뒤 「입는다」로 옮긴다.
5. **홈페이지 시안** — `시안굽기.js`. ⏳유호 픽(㉯/㉰) 뒤.

## 6. 대가 — 틀릴 때의 모습

장치는 «안 도는 쪽»보다 **«맞는 얼굴로 틀린 값»** 으로 더 자주 샌다(지침 v9.2 맹점 ④).
첫날에만 그 형태를 셋 잡았다 — 전부 렌더가 멀쩡해 보이는 것들이었다:

- 🔴 **`padding-bottom:1.5%`** — CSS 백분율 패딩은 **부모의 «너비»** 기준이라, 34.97px 원판에
  h2 폭(790px)의 1.5% = **11.86px** 이 들어갔다. → 실측해서 `.025em` 으로 고정(§아래).
- 🔴 **`position:relative` 가 호출부의 `absolute` 를 덮었다** — 원판 함수 안에 있어서 «나중에 오는 것이 이겼다».
  순번 원판이 제자리에 안 앉았는데 화면은 멀쩡했다. → 원판·림은 position 을 정하지 않는다.
- 🔴 **`em` 기준이 갈렸다** — `::before` 에는 `font-size:.72em` 이 걸리고 `::after`(림)에는 안 걸려서
  림이 원판보다 컸다(18.97 vs 26.34px). → 림에도 같은 font-size 를 준다.

**2차(굽는 층)에서 잡은 넷** — 전부 **exit 0 · 에러 0 · 렌더 정상**이라 로그로는 안 보였다.
어느 것도 「안 돌았다」로 드러나지 않았고, 넷 다 「통과」의 얼굴을 하고 있었다:

- 🔴 **`치우기()` 가 스튜디오 세트를 매 판 삭제했다** — 조명 판도 MESH 라서.
  조명 강도를 **8배 올려도 픽셀이 한 톨도 안 변해서야** 드러났다(그 전까지 「장면 레버는 효과가 없다」로 오독할 뻔했다).
  → 세트를 등록부에 담아 보호. 🔑**「값을 바꿨는데 결과가 같다」는 장치가 조용히 죽었다는 신호다.**
- 🔴 **컴포지터(5.x 노드그룹)를 붙이자 렌더가 통째로 흰 판이 됐다** — `NodeGroupInput` 이 렌더 결과를
  자동으로 물지 않는다(4.x 의 `RLayers→Composite` 배선이 사라졌고 `Composite` 노드 자체가 없다).
  2.4초에 끝났고 에러 0. → 컴포지터 기각(분산은 셰이더가 물리로 이미 낸다).
- 🔴 **스튜디오 판의 그림자를 안 끄면 알파에 사각 띠가 남는다** — 지면에 얹는 순간 회색 판때기로 드러난다.
  판은 빛을 «막는» 것이 아니라 «담기는» 것이다 → `visible_shadow = False`.
- 🔴 **Blender 는 상대경로를 blend 파일 기준(`//`)으로 읽는다** — `./출력` 을 넘겼더니 `C:\출력` 으로 샜다.
  성공 로그(`Saved:`)까지 정상으로 찍혔다.

그리고 **조건을 둘 이상 동시에 바꾼 대가**도 실측했다: 감광판을 더하면서 조명 3점까지 같이 갈아엎었더니
유리가 하얗게 떠서 헌법(「몸은 무대보다 어둡게」)이 **거꾸로** 깨졌다.
1차 세션이 4판을 돌려 잡은 값을 건드린 것이 원인이었고, 조명을 되돌리고 «내가 더하는 층»만 얹자 섰다.

닫지 못한 것 · 남는 위험:
- **부품 목록에만 늘고 CSS 가 안 따라오면 조용히 텍스트로 남는다.** → `--분모` 가 이름째 센다(구현 14/14).
  다만 마커는 주석이라 지워도 렌더는 멀쩡하다 — 그 자리는 회귀가 져야 한다(▶ L5).
- **`counter` 는 «가짜 번호»를 만든다** — HTML 에 없던 번호라 복사·스크린리더에 안 잡힌다.
  → `.번호` span 이 있으면 그것이 이기게 두었지만, 기본 경로는 여전히 counter 다. 대가를 안다.
- **낮 지면에서 레진의 «코스틱»(샌 빛)은 사라진다.** glow 가 종이에 안 실린다 —
  종이의 한계라 억지로 흉내내지 않는다. 코어는 «면»으로, 굴절밴드는 «선»으로 굳혀 구슬로 읽히게 했다.
- **광학 보정값은 폴백 서체(system-ui)로 잰 값이다.** Inter Tight 가 실린 지면에서 재확인이 남았다.
- **2차 실증에 남은 눈에 보이는 결함 둘**(정직하게 — 「비싸 보임」은 아직 완성이 아니다):
  ①**레진 코어가 여전히 «납작한 원판»에 가깝다** — 축을 어긋낸 덩어리로 바꿨지만 굴절이 그 변형을 상쇄한다.
  다음 층은 «형태 있는 봉입물»(꽃·조각·글자)이지 더 찌그러뜨리는 것이 아니다.
  ②**펠트가 이 조명에서 평평하다** — 펠트 헌법은 「재조명 최소 · 사진에 구워진 조명 보존」인데
  유리·레진용 스튜디오는 그것과 다른 빛이다. **한 무대에 셋을 세우면 조명이 재질마다 어긋난다** —
  ⑥합주의 대가이고, 아직 안 풀었다.

## 7. 실측 장부 — 광학 보정 (2026-08-16)

「원 안의 숫자를 위로 올린다」는 통설을 **재서** 값으로 바꿨다.

- 잰 것: canvas `actualBoundingBox`(잉크 경계) vs `fontBoundingBox` 중앙(line box 중앙).
- 결과: 숫자 잉크 중앙이 line box 중앙보다 **font-size 의 1.25%** 아래(40px 에서 0.5px).
- 집행: `padding-bottom:.025em` — **패딩은 중앙을 «절반»만 미므로** 이동량의 2배다.
- 검증: 3배 확대 렌더에서 이동 **0.39px** 측정 = 계산값 `0.2635 ÷ 2 × 3 = 0.395px`. 일치.
- 실물 크기에서는 **0.5px**. 짚어낼 수 없지만 알아챈다 — 그게 이 층의 전부다.

## 8. 도전안 — 「빈 자리」도 부품이다 (기록만 · 픽 대기)

지금 부품 14종은 전부 «놓는 것»이다. 그런데 명품 지면에서 가장 비싼 것은 종종 **아무것도 안 놓은 자리**다.
지면 한 칸을 통째로 비우는 것을 «여백»이 아니라 **부품(`.틈새`)** 으로 세우면,
비움에도 규격과 이유가 생긴다(지금은 여백이 «남은 것»이지 «놓은 것»이 아니다).
대가 = 부품이 하나 늘고, 남용하면 문서가 성겨진다. **유호님 픽 전에 짓지 않는다.**

## 9. ✅ 갚은 빚 — ③ 중성축의 대비 위반 51건 (2026-08-19 · 유호 확정 「진행」)

전파를 닫으면서 **만진 지면 12벌을 렌더 전/후로 대조**했다. 그 트랙이 낳은 위반은 0이었는데,
대조하다가 «이미 있던» 적색 51건이 드러났다 — `#676767`(Ash 2) on `#E4E4E7`(Chalk) = **4.46**(기준 4.5).
엔진 지도 16 · 동반자층 15 · 캐러셀 11 · 컴퓨터 7 · 운영 2.

### 왜 ③ 의 자가검사가 못 잡았나 — **잰 대상이 한 갈래였다**

`중성축.js` 는 「각 색이 **자기 바닥**에 대해 갖던 대비」를 보존했다(9/9 · 이탈 ≤0.07). 그건 맞게 잰 것이다.
그런데 지면에서 Ash 2 는 바닥(Paper)이 아니라 **Chalk 판 위**에도 앉는다. 구 Cream(`#F6F1E8`)이
새 Chalk(`#E4E4E7`)보다 밝아서 그 쌍이 통과했고, 무채로 옮기며 판이 어두워지자 4.5 를 **0.04 차이로**
못 넘게 됐다. 한 갈래만 재서 전체를 오단정한 전형(F183)이고, 빠진 자는 「색 × 색」이 아니라
**「그 색의 직책이 이름 댄 판 전부」**다.

🔑 **그 목록은 이미 있었다 — 산문 안에.** `직책` 칸이 「바닥은 Paper·Chalk·Chalk 2만」이라고 적고
있었는데, **산문이라 기계가 못 읽었다.** 그래서 고침의 절반은 값이 아니라 **`허용바닥` 데이터화**다.

### 고친 것 — 값이 아니라 **규칙**

`유도()` 뒤에 **바닥 게이트**를 달았다: 유도한 뒤, 그 색의 `허용바닥` **전부**에 대해 4.5 를 넘길
때까지 바닥에서 멀어지는 쪽으로 한 눈금씩 민다. 그 게이트가 두 색을 움직였다:

| 색 | 전 | 후 | 왜 | 크기 |
|---|---|---|---|---|
| **Ash 2** | `#676767` | **`#666666`** | Chalk 판 위 4.46 → **4.53** | **1/255** (ΔL\* 0.2) |
| **Ash** | `#8A8A8A` | **`#8C8C8C`** | Graphite 3 위 4.38 → **4.50** | **2/255** |

⚠**Ash 는 유호님이 고른 항목이 아니었다** — 같은 규칙이 잡은 곁가지다. 실지면에서는 아직 그 쌍이
안 나왔지만(우리 다크 지면이 Ash 를 Graphite 3 위에 안 올린다), **규칙을 고치면 같이 드러난다.**

### 대가 — 그리고 그 대가가 실제로 물었다

🔴 **옛 «유도값»은 두 그물에 다 안 걸린다.** `#676767` 은 구 킷 hex(`#5F657D`)도 DEAD 도 아니라
「한때 새 hex 였던 것」이다. 퇴역 map 은 구hex 를 찾고 DEAD 는 폐기 결정의 목록이니, 이미 그 값으로
구워진 지면 29벌이 그 틈으로 조용히 낡았을 것이다.
⇒ `tools/중성축.js` 에 **`옛유도` 표**를 세우고 `중성축_지면전파` 가 map 에 합친다. 회귀도 붙였다
(퇴역 11색 목록에는 **안 섞는다** — 그 목록은 「조항 ⓗ 가 갈아탄 개수」라는 분모를 지고 있다).

🔴 **그리고 `--적용` 이 «한 번 돈 뒤로는 아무것도 안 바꾸고 있었다».** 토큰을 고치는 함수가 킷을
**구 이름**(`Slate 2`)으로만 찾는데 토큰은 이미 `Ash 2` 로 개명돼 있었다. 그래서 0칸을 바꾸고도
`표.length` 를 돌려줘 화면에 **「11칸 반영」**이 찍혔다 — 값이 0인 판과 글자가 똑같다.
실제로 이 트랙에서 밟았다(토큰은 옛 hex, 지면만 새 hex → 렌더린트 「킷 밖 색 124건」).
⇒ 구·신 이름 둘 다로 찾고, **찾은 수가 아니라 «바뀐 수»를 돌려준다**(0이면 「이미 같다」라고 말한다).

### 실측 — 갚았다

| 지면 | 대비 위반 전 | 후 |
|---|---|---|
| 엔진 지도 | 16 | **0** |
| 마스코트 동반자층 | 15 | **0** |
| 컴퓨터를 잃어버렸을 때 | 7 | **0** |
| 운영 한눈에 | 2 | **0** |
| 캐러셀 「괜찮아요」 | 18 | **7** — 남은 7 은 **코랄 신호색**(`#E8543F` on Paper = 3.48)이고 이 트랙 전부터 있었다 |

킷 밖 색도 네 지면 전부 **0**. 펠트 `Ash` 패치는 새 값으로 **재염색**했다(ΔE2000 **0.07**) —
이름만 바꾸면 「Ash 라는 이름의 옛 회색 천」이 된다.

