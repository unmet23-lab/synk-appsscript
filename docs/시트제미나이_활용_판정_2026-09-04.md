# 시트 안 제미나이로 상담·앱 데이터를 살리는 길 — 판정 재료

<!-- 정본: v1 · 2026-09-04 -->
<!-- 유호 물음 09-04 「구글 라이브 시트 제미나이 이용해서 우리가 원래 쓰던 앱데이터나
     상담데이터(오프라인상담서) 활용해서 발전시킬 방법이 뭐가 있을지 고민해줘」 -->

> **시제** — 아래 「지금 있다」는 전부 2026-09-04 에 실물을 열어 잰 것이다.
> 갈래 ㉠~㉣ 은 **아직 아무것도 안 지은 제안**이다.
> **유호님이 읽으신 지면** = https://claude.ai/code/artifact/1bace26f-b07d-4fb5-a78c-d435b9a4fb0e (이 문서와 같은 내용)

---

## 0. 한 줄

> ✅ **2026-09-05 재봤다. 채용 판정 = 예.** 유호님 기준 「그대로면 안 써도 되는데 발전시킬 수 있다면 채용」에 대한 답이다.
> 실측 전문은 **§7**(맨 아래). 아래 §0~§6 은 재기 «전»에 쓴 것을 그대로 둔다 — 무엇을 보고 재러 갔는지가 남아야 한다.

**제미나이가 잘하는 일과 우리에게 빈 자리가 정확히 맞물린다 — 다만 그 자리는 「화면을 하나 더 만드는 것」이 아니다.**

우리는 **받을 그릇도, 보낼 배선도, 인쇄할 종이도 이미 다 지어 놨는데, 그 사이에서 «판단을 써 주는 자»만 없다.** 지금 그 자리는 사람 손이 채우거나 영원히 빈칸이다. 제미나이가 잘하는 일이 정확히 그 하나 — 많은 칸을 읽고 사람 말로 한 문단을 쓰는 것 — 이다.

반대로 제미나이가 가장 먼저 만들어 주는 것(숫자를 세는 화면)은 **이미 우리 시트가 수식으로 하고 있다.** 그건 두 번 만드는 일이다.

---

## 1. 실측 — 상담 스프레드시트를 통째로 열어 봤다

파일: `SYNK_LAB_상담데이터_입력시트_v18.3`(unmet23 소유 · 마지막 손댐 2026-09-01)
재는 법: 드라이브에서 엑셀로 내려받아 탭·행·수식을 직접 셌다.

| 탭 | 값이 든 줄 | 무엇 |
|---|---|---|
| 사용안내 | 36 | 시트 쓰는 법 |
| crew_errors | 1 | 접수 오류 장부(비어 있음) |
| crew_cards | 3 | 상담서 원본 접수(2건) |
| **대시보드** | 59 | **이미 수식으로 도는 경영 화면** |
| 학생카드 | 48 | 한 사람 보기 |
| 반별조회 | 44 | 반별 보기 |
| **상담데이터입력** | **4** | **머리 2줄 + 사람 2명**(김재헌 · 「ZZ 통로시험」) |
| 수강납입 | 504 | 거의 전부 빈 껍데기(수식만 깔려 있다) |
| 반설정 | 17 | 24반 정원 |

### 세 가지가 드러났다

**ⓐ 그릇은 크고, 담긴 것은 한 사람이다.**
상담데이터입력은 **칸이 104개**다(이름·연락처부터 「가장 걱정되는 것」·「10년 후 나의 무대」까지). 그런데 실제로 답이 들어온 사람은 **재헌님 한 명**이다. 앱 쪽도 마찬가지 — 명부에 13줄이 있는데 전부 시연용이다.

**ⓑ 경영 화면은 이미 있다.**
대시보드 탭이 수식으로 이미 세고 있다 — 재원생·이달 신규·이달 퇴소·순증감·누적 퇴소·이탈률·예상 월매출·미납 인원·미납액·목표 급수 분포·한국어 수준 분포·진학 시기 분포·반별 정원과 여석.
09-04 에 제미나이 캔버스가 2분 만에 만들어 준 것(재원생 수·월별 막대·반별 표)과 **같은 물건이다.**

**ⓒ 104칸을 묻고, 앱으로 흘러가는 것은 28칸이다.**
코드를 세어 봤다. 앱·판단으로 실제로 넘어가는 칸은 이름·연락처·반·한국어수준·목표 급수·취미 같은 **28칸 안팎**이고, **나머지 76칸은 시트에 적히고 거기서 끝난다.** 아무 코드도 그 칸을 읽지 않는다.

---

## 2. 발견 — 「다 지어 놨는데 채우는 자가 없는 자리」 셋

이것이 이 조사의 진짜 결론이다. 셋이 같은 모양이다.

### ① ⚠위험신호 칸 — 이름이 「자동」인데, 계산하는 코드가 0줄이다

상담시트 BI 칸의 이름은 **`⚠위험신호(자동)`** 이다. 그 값이 아침마다 앱 명부로 넘어가고, 원장 화면(원장만 보는 자리)에 뜨도록 **배선이 끝나 있다.**
그런데 저장소 전체를 뒤져도 **그 칸에 값을 쓰는 코드가 없다.** 읽는 코드만 있다.

⇒ 지금 상태로 12월을 맞으면 원장 화면의 위험신호는 **첫날부터 영원히 빈칸**이다.

**그런데 그 판단에 필요한 재료는 이미 다 받고 있다** — 상담서가 이렇게 묻는다:

| 묻고 있는 것 | 이탈을 알려 주는 이유 |
|---|---|
| 가족의 유학 지지도 (4택 중 「조건부 찬성」·「반대」) | 집에서 반대하면 두 달 안에 그만둔다 |
| 유학 예산 · 학비 조달 주체 | 돈이 끊기는 자리 |
| 가장 걱정되는 것 (10택 · 여러 개) | 「학비·생활비」와 「가족과 이별」은 결이 다르다 |
| 아르바이트 병행 계획 | 수업 시간과 부딪힌다 |
| 하루 학습 가능 시간 | 목표까지의 거리와 맞는가 |
| 목표 급수 · 목표 기한 · 지금 급수 | 「6개월에 3급 → 5급」은 못 지킬 약속이다 |

**이 여섯을 한 사람 안에서 겹쳐 읽어야 신호가 나온다.** 지금은 아무도 안 읽는다.

### ② 「상담 결과 요약」 A4 — 종이는 완성됐고, 밑줄은 손으로 적는다

`docs/발표물/10_상담결과_요약_A4.html` 이 이미 인쇄 가능한 상태로 서 있다(360KB · 8주 흐름·매달 가는 것·앱에서 겪는 것·사람이어야 하는 세 힘까지 다 들어 있다).
그 지면에 **손으로 채우는 밑줄이 정확히 10칸** 있다:

이름 · 상담일 · 상담한 사람 · 지금 자리(레벨) · **이 학생이 말한 목표(본인의 말 그대로, 28자)** · 추천 반 · 요일·시간 · **이 학생에게 특히 맞을 것** · 안내한 수강 조건 · **다음 단계(하나만, 날짜와 함께, 20자)**

이 10칸을 상담서 답으로 갈라 보면:

| 갈래 | 칸 | 무엇이 채우나 |
|---|---|---|
| 시트만으로 채워진다 | 이름 · 상담일 · 상담한 사람 · 요일·시간 | 칸끼리 이으면 끝(수식) |
| **읽어야 나온다** | **말한 목표 · 특히 맞을 것 · 추천 반 · 다음 단계** | **76칸을 겹쳐 읽고 사람 말로 써야 한다** ← 여기 |
| 원장이 정한다 | 지금 자리(레벨) · 안내한 수강 조건 | 사람 몫으로 둔다 |

굵게 표시한 넷이 **정확히 제미나이가 잘하는 일**이다.

### ③ 76칸 — 30분을 받아 적고, 아무도 안 읽는다

학생은 상담서 105문항에 답하느라 30분을 쓴다. 그런데 그중 76칸이 «적히고 끝»이다.
이대로 12월을 맞으면 **학생은 시간을 냈고 우리는 안 읽은 것**이 된다.

---

## 3. 그래서 제미나이가 설 자리 — 겹치는 것과 새로 되는 것

| | 제미나이 캔버스가 하는 일 | 우리 |
|---|---|---|
| 🚫 | 숫자를 세어 화면을 만든다 | **이미 있다**(대시보드 탭) — 두 번 만드는 일 |
| 🚫 | 점과 선으로 이은 관계 그림 | **못 그린다**(09-04 실측 · 새 탭 0개) |
| ✅ | **여러 칸을 겹쳐 읽고 한 문단으로 판단을 쓴다** | **없다** ← 위 셋 전부 |
| ✅ | 있는 표를 다른 말로 다시 쓴다(번역·요약·다듬기) | 몽골어 안내를 만들 때 |
| ✅ | 그럴듯한 시험용 자료를 만들어 준다 | 12월 전에 굴려 보는 데 쓴다(㉢) |

돈은 **0원**이다 — 유호님 제미나이 구독에 이미 들어 있다(09-04 실측).

---

## 4. 갈래 넷 — 값이 큰 순서

### ㉠ 상담 결과 요약 A4 를 기계가 채운다 · 값 1위 · 지금 시작할 수 있다

**바뀌는 장면.** 지금은 상담이 끝나면 원장이 A4 를 꺼내 밑줄 10칸을 손으로 적는다.
바꾸면 — 학생이 상담서를 내는 순간, **10칸이 채워진 A4 가 나온다.** 원장은 두 칸(레벨·수강 조건)만 고치고 그 자리에서 학생 손에 쥐여 준다.

**왜 값이 1위인가**
- **이미 9월 일감이다.** 개원 전 판매 설계 §④ 걸음 5 에 「진단 받은 사람에게 결과 + 제안 · 임자 = 기계」가 적혀 있다. 그 「결과」를 써 주는 자가 아직 없다.
- **파는 자리와 붙는다.** 미끼가 「무료 진단」인데, 진단을 받고 **손에 쥐는 것이 없으면** 그건 진단이 아니라 설문이다.
- **손 절차가 사라진다.** 유호님이 손으로 적던 열 칸이 없어진다.
- 학생이 가져가는 종이 통로가 이미 서 있다(증서·기록장) — 그 옆에 한 장이 더 붙는다.

**들어가는 것** — 시트 안 제미나이에게 「이 줄을 읽고 네 칸을 써라」를 시키는 자리 하나 + 그 값을 종이에 꽂는 배선.

### ㉡ ⚠위험신호를 실제로 채운다 · 값 2위 · 12월 전에

배선이 이미 끝나 있으므로 **만들 것은 「값을 쓰는 자」 하나뿐**이다.
여섯 칸을 겹쳐 읽고 → 한 줄로 → 원장 화면에.

🔴 **규율 하나를 같이 세운다: 사람에게 딱지를 붙이지 않는다.**
「이 학생은 위험군」이 아니라 **「집 지지가 조건부 + 예산 빠듯 → 첫 달에 학부모와 한 번 통화」** 처럼 **할 일**로 적는다. 이건 성향 뽑기 설계에서 이미 세운 규율과 같은 것이다.

### ㉢ 12월 전에 굴려 보기 — 가짜 학생 서른 명 · 값 3위 · 위험 0

제미나이에게 **105문항짜리 그럴듯한 몽골 학생 30명**을 시트 **사본**에 만들게 한다. 그러면:
- 대시보드·반 배정·앱이 **처음으로 진짜처럼 돈다** → 무엇이 비어 있는지 눈에 보인다
- 홍보물에 넣을 화면 사진이 생긴다(지금은 시연용 9명이라 화면이 텅 비어 보인다)
- ㉠㉡ 이 실제로 쓸 만한지 서른 번 재본다

🔴 **반드시 사본에서 한다.** 라이브에 가짜를 넣으면, 나중에 그 시트를 재는 모든 자가 내 가짜를 되읽어 준다.

### ㉣ 문항 다듬기 — 105문항 중 무엇을 쓸 것인가 · 되돌릴 수 없는 시한

㉠㉡ 을 하면 76칸 중 상당수가 «읽히는 칸»으로 바뀐다. 그러고도 남는 칸이 있으면 **묻기를 그만두는 것**도 답이다.
이건 첫 학생이 답을 내기 «전»에만 깨끗하게 할 수 있다.

---

## 5. 유호님이 정하실 것 — 둘

### 판정 1 · 어디부터 갈까

- **ⓐ ㉠부터** (내 추천) — 9월 일감에 이미 있고, 돈 0, 파는 자리와 바로 붙는다
- ⓑ ㉡부터 — 원장 화면의 빈칸을 먼저 메운다
- ⓒ ㉢부터 — 굴려 보고 나서 ㉠㉡ 을 정한다
- ⓓ 지금은 안 한다 — 홍보를 먼저 굴리고 12월에 연다

### 판정 2 · 제미나이에게 학생 답을 보여 줘도 되나

08-30 에 「구글 안은 안 된다」 갈래를 물리치신 적이 있다. 엄격히 읽으면 시트 속 제미나이도 «밖»이다.
지금은 실학생이 0명이라 위험이 0이지만, **2026-12-07 첫 학생부터 진짜로 걸린다.**

- **ⓐ 조건부로 연다**(내 추천) — 이름·연락처·SNS·이메일·보호자를 **빼고** 답변만 보여 준다.
  09-04 실측에서 「학생 이름을 화면에 띄우지 마」라는 조건을 제미나이가 **실제로 지켰다.**
- ⓑ 사본에서만 — 라이브는 절대 안 보여 준다
- ⓒ 아예 닫는다 — 그러면 ㉠㉡ 은 제미나이 대신 우리가 이미 쓰는 다른 AI 로 짓는다(돈이 든다)

---

## 6. 안 재본 것 — 정직하게

- **시트 칸 안에서 부르는 AI(`AI()` 함수)가 유호님 계정에서 도는지 안 재봤다.**
  09-02 에도 「메뉴에 보이더라」까지였고 지금도 그대로다. 웹 확인으로는 회사용 요금제는 중간 등급부터, 개인 계정은 유호님이 쓰시는 구독이면 열리는 것으로 보인다 — 다만 **한 번에 350칸·하루 상한**이 걸린다.
  🔑 **이 값이 갈래를 죽이지는 않는다.** 되면 시트 안에서 수식 하나로 끝나고, 안 되면 이미 도는 통로(스크립트 + 제미나이 부르기)로 같은 일을 한다.
- 앱데이터 쪽 백업 시트는 이번에 안 열었다(09-04 실측값을 그대로 썼다).
- 제미나이가 쓴 판단의 «품질»은 안 재봤다 — 한 사람 분량이라 잴 표본이 없다. ㉢ 을 하면 서른 번 잴 수 있다.

---

## 7. ✅ 실측 — 2026-09-05 새벽 · 상담시트 **사본**에서 (유호 지시 「발전시킬 수 있다면 채용」)

### 7-0. 재는 자를 먼저 못박았다

셋 중 하나라도 참이면 채용, 아니면 안 쓴다.

| | 자 | 결과 |
|---|---|---|
| ① | 지금 우리 시트·코드가 **못 내는 것**을 낸다(76칸을 겹쳐 읽은 판단 한 줄) | ✅ **통과** |
| ② | 유호님이 **손으로 하던 것**을 대신한다(상담 결과 종이 밑줄 열 칸) | ⏳ 안 쟀다 — ① 과 같은 능력이라 따로 안 재고 실제 만들 때 잰다 |
| ③ | 우리가 **못 만들던 재료**를 만든다(시험용 학생 여럿) | ✅ **통과** |

⚠ 재는 순서가 저절로 정해졌다 — 상담 자료가 두 줄뿐이라 ①을 시킬 표본이 없다. 그래서 ③으로 재료를 먼저 만들게 했다.

### 7-1. 판 — 라이브를 안 건드렸다

원본 상담 스프레드시트를 **사본**으로 떴고(파일→사본 만들기), 실제 사람이 적힌 두 줄(3~4행)을 먼저 지운 뒤 시작했다.
사본 = `137f0QkNMk6H5Iz1dsX5GcOf4eD4Ya5ZLuAUdnwRT4sc` (크롬 로그인 계정 소유 · 원본과 별개 파일).

### 7-2. 시험 ③ — 사람 열다섯 만들기 ✅

시킨 것: 「2행이 칸 이름이다. 3행부터 열다섯 줄에 가상 몽골 유학생을 채워라. 드롭다운 칸은 그 선택지 안에서 고르고, 가족지지·예산·걱정·학습시간·급수가 골고루 흩어지게 하고, 4~5명은 그만둘 위험이 실제로 보이게 하라.」

| 잰 것 | 값 |
|---|---|
| 채워진 칸 | **1,499 / 1,560** (15명 × 104칸 = **96%**) |
| 가족지지 갈래 | **3** (전폭지지 · 조건부찬성 · 설득필요) |
| 유학예산 갈래 | **3** · 학습가능시간 **4** · TOPIK급수 **6** · 한국어수준 **5** |
| 걸린 시간 | 약 4분 (승인 화면 없이 바로 시작) |

🔑 **머리 2행만 보고 104칸의 뜻을 알아냈다.** 규격 문서를 준 적이 없다.

### 7-3. 시험 ① — 위험신호 쓰기 ✅

시킨 것: 「열다섯 사람 각각 BI열에 한 줄. 여러 칸을 겹쳐 읽고, 사람에게 딱지를 붙이지 말고, 45자 안쪽, 걱정할 것이 없는 사람은 비워라.」

**결과 = 15명 중 5명만 쓰고 10명은 비웠다.** 억지로 안 채웠다(이게 가장 중요한 한 줄이다 — 전부 채우면 정보가 0이다).

| 사람 | 쓴 줄 |
|---|---|
| 졸자르갈 | 부모 반대와 학비 부족이 보이니 학부모 상담으로 지원 설득과 장학 대책을 세우라 |
| 뭉흐진 | D-4 비자 거절 이력이 보이니 재신청 전 잔고증명과 재정 서류를 직접 점검하라 |
| 알탄호약 | 기초 실력 대비 과도한 목표가 보이니 1:1 면담으로 현실적 단계별 목표를 세우라 |
| 체첵 | 야간 주 40시간 과다 근로가 보이니 근무시간 조정을 권고하고 출결을 관리하라 |
| 후슬렝 | 부모 반대와 학비 불투명이 보이니 학부모 상담으로 납입 의사와 분납을 협의하라 |

전부 「무엇이 보이니 무엇을 하라」 꼴이고 등급 표현이 0이다. 비운 열 명에 대해서도 왜 비웠는지 각각 댔고, 그 이유가 **구체적인 칸 값을 인용한다**(「아즈자르갈 — 서울에 유학 중인 친언니와 함께 생활」은 「한국연고」 칸이다).

### 7-4. 🔑 되읽기 시험 — 이것이 판정을 갈랐다

**의심**: 제미나이가 «자기가 심어 둔 답»을 되찾은 것 아닌가([[ledger-edit-is-not-measurement]] 의 그 자리). 실제로 자기 요약에 「고위험군 5명을 넣었다」고 스스로 적었다.

**그래서 자를 바꿨다** — 「사라」(문제없다고 판정돼 빈칸이던 사람)의 칸 셋을 **내가 손으로** 고쳤다:

| 칸 | 전 | 후 |
|---|---|---|
| 유학예산 | 여유있음 | **빠듯함** |
| 가족지지 | 전폭지지 | **설득필요** |
| 아르바이트계획 | 없음 | **직장 근무중** |

그리고 「BI열 열다섯 줄을 처음부터 다시 판단하라」고만 시켰다(무엇을 바꿨는지는 안 알려 줬다).

**결과**:
- 바뀐 칸 셋을 **정확히 읽어 냈다**(「유학예산: 빠듯함 · 가족지지: 설득필요 · 아르바이트계획: 직장 근무중」).
- 사라에게 새 줄이 붙었다 — 「빠듯한 예산과 가족 설득·직장 병행이 보이니 학부모 상담 및 장학 대책을 세우라」.
- **나머지 열넷은 한 글자도 안 흔들렸다**(빈칸 9명 그대로, 신호 5명 그대로).

⇒ **자기 기억이 아니라 «지금 칸 값»을 읽는다.** ①이 참으로 통과했다.

### 7-5. 🔴 실측에서 물린 함정 셋

1. **자기가 센 숫자가 틀린다.** 같은 문장을 한 번은 44자, 다음엔 39자라고 했다. 09-04 의 「탭 10개(실제 93)」와 같은 무늬다 — **제미나이가 낸 수는 그대로 안 쓴다.**
2. **드롭다운 칸에 손으로 타이핑하면 안 들어간다.** 자동완성이 가로채 세 칸이 전부 옛 값 그대로였다(내가 바꾼 줄 알고 넘어갈 뻔했다). 들어간 방법 = 수식으로 넣기(`="빠듯함"`). ⇒ **바꾼 뒤에는 반드시 되읽어 확인한다.**
3. **승인 화면 없이 바로 시트를 고친다.** 09-04 에 본 「계획 보여 주고 승인 버튼」이 이번엔 없었다. ⇒ **라이브에서는 절대 안 쓴다. 사본에서만.**

### 7-6. 그래서 무엇이 달라지나

- **㉡ 위험신호** — 배선은 이미 다 있고 계산하는 자만 없었는데, 그 자리를 제미나이가 채운다. 돈 0.
- **㉢ 리허설** — 12월 전에 시스템을 진짜처럼 굴려 볼 재료가 4분에 생긴다.
- **㉠ 상담 결과 종이** — 같은 능력이라 될 것으로 본다(안 쟀다).
- 남은 유호 판정 둘(§5)은 그대로다.

---

## 8. ✅ 집행 — 2026-09-05 · 유호 확정 둘

> 유호 확정 09-05 ① 「학생답을 제미나이에게 보여줬을때 보안관련 빼고 확실한 발전이 있으면 그렇게 해」
> ② 「그리고 상담 결과 종이 채우기도 해」 ⇒ §5 의 판정 둘이 **ⓐ·ⓐ 로 닫혔다.**

### 8-1. 학생 답 노출 — 조건부로 열렸다

08-30 확정 「구글 안은 안」은 폐기가 아니라 **조건으로 좁혀 열린다**:
① 사람을 알아보게 하는 칸(이름·연락처·SNS·이메일·보호자)은 **빼고 답변만** ② 확실한 발전이 있는 자리에만 ③ 라이브 아닌 사본에서.

🔑 **그래서 통로를 「AI 몫을 줄이는」 쪽으로 지었다** — 아래 §8-2 의 규칙 칸 넷이 그것이다.
AI 에게 보낼 것이 열 칸에서 **세 칸**으로 줄었고, 조건 ①이 통로 자체에 박혔다.

### 8-2. 상담 결과 종이 — 채우는 자가 섰다

새 도구 **`tools/상담결과조판.js`** 하나. 지면 원고를 읽어 값을 제자리에 꽂고, 굽기 «전»에 셋을 잰다.

| 무엇 | 어떻게 |
|---|---|
| 자리 찾기 | **순서가 아니라 라벨 이름**으로. 라벨이 없으면 **실패로 멈춘다**(조용히 건너뛰면 빈 종이가 채워진 얼굴로 나온다) |
| 글자 수 자 | **지면에서 읽는다**(말한목표 28 · 다음단계 20 · 길한줄 18). 코드에 베끼면 두 곳이 알게 된다 |
| 금칙 | **꽂은 값만** 센다. 지면 틀의 낱말까지 세면 매번 빨개져 사람이 자를 끈다 |
| 빈칸 | 「사람 몫」과 「빠뜨린 것」을 갈라 센다 |
| 넘침 | 헤드리스 크롬 실측 + **넘침시험**(일부러 넘치게 해서 자가 잡는지 되묻는다) |
| 새로 지은 것 | **이 파일 하나.** 크롬 찾기·측정 통로·폰트 임베드·금칙 목록은 증서조판·발표물린트에서 떼어 썼다 |

**규칙 칸 넷은 코드가 채운다**(`--상담행`) — 지금자리 · 요일시간 · 시작Lv · 목표급수 · 시즌수.
⇒ 열 칸 가운데 **AI 몫은 셋**(말한목표 · 맞을것 · 다음단계), **원장 몫은 하나**(안내한 수강 조건)만 남는다.

🔴 **이 도구가 처음 세우는 매핑 둘**(정본에 없었다 · 근거를 코드 주석에 붙였다):
- 한국어수준 → 시작 Lv: 수준 다섯(`PROFILE_LEVELS`)을 Lv 사다리 아래 다섯에 순서대로.
- 목표 급수 → **그 급수에 닿는 «가장 이른» Lv**(커리큘럼 정본 ⓪표). 부풀리면 종이가 학생에게 더 긴 길을 약속한다.
- 폭으로 적힌 목표(「3~4급」)는 **높은 쪽**을 과녁으로. 낮은 쪽으로 읽으면 학생의 목표를 깎아 적는 꼴이다.

**실측**: A4 210×297mm · 넘침 0 · **넘침시험 통과**(일부러 넘치게 하니 자가 잡았다).
최악 픽스처에서 글자 수 넘김 2건·칸 넘침 3개를 잡았고, 판단 셋을 빼면 **빠짐 3**을 정확히 짚었다.
첫 판이 자에서 두 번 틀렸고 고쳤다(남의 「10자」를 자기 한도로 물어 옴 · 쪽 요소를 `.page` 로 찾았는데 실물은 `.sheet`).

🔴 **곁가지로 드러난 어긋남**: 코드의 반 키 정본(주말 11·14)과 시트 「반설정」 탭(주말 11:00·15:30)이 갈렸다.
조판기는 **반 키**를 따른다(종이에 찍히는 것은 학생이 배정받은 그 반이다). 둘을 맞추는 것은 별건이다.

### 8-3. 아직 안 이은 것 — 다음 걸음

- **시트 → 종이 자동 통로.** 지금은 상담 행을 JSON 으로 손수 넘긴다. 원장이 시트에서 버튼 하나로 뽑는 자리는 아직 없다.
- **AI 몫 세 칸을 사본에서 열다섯 명분 돌려 보기** — 09-05 새벽에 시도했으나 브라우저 창이 접혀 지시가 안 들어갔다.
  ⚠ 능력 자체는 §7-3 에서 이미 증명됐다(위험신호 다섯 줄). 남은 것은 「종이 값 꼴로 한 번 더」다.
- **몽골어 판** — 학생·학부모가 가져가는 종이라면 다음 관문이다.

---

## 붙임 · 재는 법

```bash
# 상담 스프레드시트 통째로 재기 — 탭·줄·칸
# (드라이브에서 엑셀로 내려받아 파이썬으로 연다 · 기억 live-sheet-audit-path)
```

- 상담 스프레드시트 주소 = `Code.js:791` 의 `CONSULT_SHEET_ID`
- 상담서 105문항 정본 = `docs/크루카드/크루카드_스키마.json`
- 상담 결과 A4 = `docs/발표물/10_상담결과_요약_A4.html`(손으로 채우는 칸 = `class="fill"` 열 개)
- 개원 전 판매 설계 = `docs/개원전판매_설계_v1.md` §④
