# 현재 작업 인계

> 마지막 갱신: 2026-07-27
> 이 문서는 새 작업이 "이어가자"는 요청을 받을 때 읽는 단기 인계서다.

## 현재 주제

- **만족도팩 v9.70~73 + 디테일 팩 v9.74~75 — 커밋·배포 완료(2026-07-28)**: cowork 인계 절차 ①~④ 전부 완료(`c101801` 만족도팩 · `4eb6965` v9.74 · CI를 `node --test tests/*.test.js`로 확장). 라이브 6파일 반영됨. 배포 후 **v9.75로 감시 갭 2건 수리**: 설문 폼(v9.73)이 preflight 켜기 큐에서 누락돼 미생성 시 침묵하던 것 편입 + `enrollments` 공백(v9.72 만료 안내 무발송) 경고. **남은 유호님 ▶ 2건**: `createSurveyForm` 1회(만족도팩 실행지 v973) · `createAcademicForm` 1회(디테일 팩 실행지 v974). 그 외 유호님 몫은 Glide 클릭(실행지 v974 STEP 1~4)과 `enrollments` 수강 정보 입력.
- **조직계보 정본 v1 확정(2026-07-17) → v1.1 표기 전환(2026-07-29)**: 구성은 불변 — 브랜드 3(LAB·STUDIO·LIFE) + 레이어 2(IP·COMMON SYSTEM). **표기 축만 조직도 → 로드맵으로 바꿨다: 지금 여는 것(LAB) / 지금 쌓는 것(STUDIO·IP·COMMON SYSTEM) / 아직 아닌 것(LIFE).** 근거 = 1인 회사의 조직도는 실은 로드맵이고, 로드맵의 빈 칸은 결함이 아니라 순서(정본 §0-A). 정본 = `docs/조직계보_정본_v1.md`, 현행 문서 색인 = `docs/문서_지도.md`. 구 6브랜드 표기·Define/Build/Execute 용어는 폐기 — 발견 시 정본에 맞춰 고친다.
  - **유호님 결정 대기 2건(부록 2 D·E)**: ①LIFE를 브랜드→아이디어로 강등 ②IP·LIFE 폴더를 SYNK 폴더 하위로 이동. **둘 다 보류 상태 — 재제안 금지, 지시가 오면 실행만 한다.** STUDIO·IP를 LAB에 흡수하는 안은 2026-07-29 기각(정리축 파괴·2차 수익 경로 종속).
- Desktop도 같은 구도로 재편됨: `SYNK LAB`·`SYNK STUDIO`·`SYNK IP`·`SYNK LIFE`·`SYNK 보안` + `_archive_2026-07`(구본 보관). 브랜드 문서 리라이팅_v2는 `SYNK LAB\About Synk lab\` 아래 **단일본**만 유효.
- **Glide 조립(2026-07-19 v9.40 재리셋)**: v939 재조립도 기능 누락 다수(유호님 보고) → 라이브 재실측으로 원인 확정: 콘텐츠 절반 부재(숙제 0/210·연료 0/6·시즌 0/12·팁 0/30·문법 57/72)+월간 시트 5종 미존재+가이드가 코어만 커버. 코드 v9.40 배포 — **preflightGlide가 콘텐츠·시트·몽골어를 자동 복구**하도록 개조 + notices 스키마 실버그 수정 + 전 카드 폰트 통일(CARD_FONT). **조립 정본 = `docs/GLIDE_조립가이드_v940.md`** (v939 폐기 표기·§10에 기능→화면 전수 매트릭스). 남은 사람 몫: 상담시트 '반' 입력·강사 class_name·parent_of·P02 이메일(가이드 §1) → preflightGlide ⚠ 0 → 조립.

## 다음 행동

- 새 작업은 먼저 `AGENTS.md`, `docs/SYNK_CONTEXT.md`, 이 문서를 읽고 현재 요청과 관련된 부분만 사용한다.
- 새 작업 첫 답변에서 복원한 맥락을 장황하게 반복하지 말고 바로 유호님의 요청을 진행한다.
- 새로 확정된 장기 결정은 `docs/SYNK_CONTEXT.md`, 현재 상태 변화는 이 문서에 짧게 반영한다.

## 인계 형식

작업 담당이 바뀔 때 아래만 남긴다.

```text
목표:
현재 상태:
수정한 파일:
발생한 오류:
시도한 방법:
완료 기준:
```
