# SNS 검수형 자동발행 시스템 — 구현 기록

2026-09-10 KST. 콘텐츠 한 벌을 계정별로 변환·검사하고, 최종본 해시에 유호님 승인을 묶은 뒤, 공식 연결이 있는 채널만 발행하는 로컬 운영 도구를 만들었다. 글이나 영상이 바뀌면 기존 승인은 자동으로 무효가 된다.

## 이번에 실제로 만든 것

- `docs/마케팅/sns-계정.json`: 19개 계정의 회사·채널·발행 방식·필수 설정을 한곳에 둔다. 비밀번호와 토큰은 넣지 않는다.
- `tools/sns-자격.ps1`: 인증값을 평문 파일 대신 Windows 자격 증명 관리자에 `SYNK/SNS/...` 이름으로 저장·조회한다.
- `tools/sns-발행.js`, `tools/lib/sns-publisher.js`: 연결 점검, 발행 계획 생성, 해시 승인, 중복 방지, 발행 결과 기록을 맡는다.
- 직접 발행기: YouTube, Telegram, Pinterest, Facebook, Threads, LinkedIn, TikTok. Instagram은 승인된 HTTPS 미디어 호스트가 생기기 전까지 실패하도록 막았다.
- 수동 보조기: 네이버 블로그, 카카오톡 채널, Substack. 검수 파일을 정리하고 공식 편집 화면을 열어 최종 예약만 사람이 한다.

```powershell
npm run sns:연결점검
node tools/sns-발행.js --계획 docs/research/sns-upload-20260910/첫게시물_발행계획.json
node tools/sns-발행.js --승인 <계획파일> --항목 <항목ID>
node tools/sns-발행.js --발행 <계획파일> --항목 <항목ID>
```

승인·발행 명령은 계획 안의 오류가 0개이고 계정 연결이 현재 유효할 때만 성공한다. 예약·업로드 결과는 외부 플랫폼 응답과 함께 기록하며, 로컬 파일 생성만으로 게시됐다고 보고하지 않는다.

## 2026-09-10 연결 관측

| 상태 | 계정 |
|---|---|
| 직접 연결 확인 | SYNK LAB YouTube (`UCmZplYKuNHLPng4iL2AbLLg`) |
| 공식 편집기 준비 완료 | 네이버 블로그, 카카오톡 채널, Substack |
| 추가 인증·앱 연결 필요 | 나머지 15개 직접 발행 대상 |
| 의도적 차단 | SYNK 본체 Instagram 계정 확정 전, Instagram용 공개 HTTPS 미디어 호스트 확정 전 |

현재 발행 계획은 19개 항목, 검증 오류 0개, 승인 0개다. 따라서 이 구현 과정에서 외부 게시물은 발행하지 않았다.

## 유호님에게 남는 손

1. 게시하려는 최종본만 눈으로 확인하고 승인한다.
2. 처음 한 번만 각 플랫폼의 OAuth·본인 인증·앱 심사를 처리한다. 만료되거나 권한이 바뀐 때만 다시 한다.
3. 네이버·카카오·Substack은 공식 편집기에서 예약 시간과 공개 대상을 확인해 최종 예약한다.

그 밖의 계정별 변환, 파일명·비율·필수 필드 검사, 자막·썸네일 동봉, 승인 무효화, 중복 방지, 결과 기록은 도구가 맡는다.

## 티스토리 판단

지금은 만들지 않는다. 티스토리는 공식 Open API를 2024년 2월 말까지 순차 종료했고, 외부 API를 통한 글쓰기·수정·파일 첨부도 종료했다. 따라서 네이버와 같은 긴 글을 또 수동으로 관리하게 되어 “유호님 손 최소화” 목표와 충돌한다. [티스토리 공식 종료 공지](https://notice.tistory.com/2664), [공식 API 문서의 종료 안내](https://tistory.github.io/document-tistory-apis/)

검색용 독립 원문이 더 필요해지면 티스토리를 추가하기보다 `synk.im`에 정본 글을 두고 네이버·Pinterest·SNS가 그 글로 연결되게 한다. 티스토리는 네이버만으로 검색 유입이 부족하다는 실제 데이터와 유지 담당이 생긴 뒤 다시 검토한다.
