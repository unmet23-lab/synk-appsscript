# 음악 자산 장부 — 어떤 곡이 어디에 있고 어떻게 다시 만드나

> 정본. 곡을 굽거나 고를 때 여기부터 연다. (2026-09-04 신설 · 유호 「폴더 하나 만들어서 모아놓으면 관리가 편할 것 같은데」)

## 🗂 실물은 git 밖에 산다 · 장부만 git 이 쥔다

| | |
|---|---|
| 원본 곡 | 바탕화면 **`SYNK 자산\음악\<장르>\`**(OneDrive 밑 · [[desktop-path-onedrive]]) |
| 이 장부 | 저장소 안 (지금 이 파일) |

### 🔀 곡이 흐르는 차례 — 폴더 순서가 곧 공정이다

`SYNK 자산` 안에서 곡은 세 자리를 지난다. 유호님이 09-04 에 사진으로 보여 주신 실물 구조다.

| 자리 | 무엇이 | 크기(09-04) |
|---|---|---|
| **`음악\<장르>\`** | 만든 곡 **원본 창고** | 10벌 · 173MB |
| **`라디오팩\`** | 그중 방송에 뽑힌 곡 · **소리만**(mp3) | 9벌 · 118MB |
| **`라디오팩_ts\`** | 거기에 **화면을 박은 판** + `playlist.txt` | 9벌 · 334MB |

🔑 **`.ts` 가 2.8배 큰 까닭 = 화면이 곡 안에 들어 있다.** 인코딩 대본(`bots/송출/인코딩.sh`) 원문:
> 곡 이름에서 장르를 읽어 그 장르의 배경을 고른다 … 화면이 곡에 «박혀» 있어 송출은 `-c copy` 그대로다(재인코딩 0). 곡이 넘어가면 마스코트도 넘어간다.

미리 합쳐 두면 송출 서버가 실시간으로 소리·그림을 합칠 일이 없다. 24시간 켜 두는 방송이라 그 차이가 크다.
⚠ 그래서 **파일 이름의 장르 조각이 배경을 고르는 열쇠**다 — `synk-radio-01-**citypop**-air.mp3` → `citypop.png`.
우리 장르 폴더 이름을 이 규칙에 맞춘 것도 그 때문이다(다른 이름을 쓰면 배경이 안 붙는다).

**왜 갈랐나** — 곡 한 벌이 wav 로 **20MB** 다. 방송용 8시간치를 채우면 수 GB 이고, 물량은 다시 구울 때마다
또 생긴다. git 은 한번 담은 바이너리를 **이력에서 못 지운다** ⇒ 저장소가 되돌릴 수 없게 부푼다.
🔑 그래서 **곡은 밖에, 「그 곡을 다시 만드는 법」은 안에** 둔다. 장부가 있으면 원본이 사라져도 되살릴 수 있다.
⚠ 이미 `docs/캐릭터` 가 1.2GB 다 — 같은 실수를 소리 쪽에서 반복하지 않는다.

### 🗂 장르 폴더 셋 (유호 지시 09-04 「장르별로 세분화」)

이름은 **내가 지은 것이 아니라** 라디오 정본에서 가져왔다 — talk `lib/라디오곡차례.js` 의
`결ascii = { 시티팝: 'citypop', 차분: 'calm', 전자: 'house' }`. 그 로마자가 실제 방송 파일 이름에 박히므로
폴더 이름을 거기 맞춰야 곡을 편성에 넣을 때 그대로 이어진다.

| 폴더 | 지금 | 무엇이 들어가나 |
|---|---|---|
| `시티팝_citypop` | 7벌 · 132MB | 아침·저녁·낮 편성 |
| `차분_calm` | 1벌 · 11MB | 새벽 편성 |
| `전자_house` | 2벌 · 28MB | 낮·밤 편성 |

### 🔴 장르 이름이 세 곳에서 갈렸다 (09-04 발견 · 아직 안 고쳤다)

| 파일 | 셋째 장르를 뭐라 부르나 |
|---|---|
| talk `lib/라디오곡차례.js` — 곡을 **만들 때** | **차분**(`calm`) |
| talk `lib/곡장부.js` — 곡을 **적을 때** | **자연** |
| talk `lib/곡편성.js` — 곡을 **고를 때** | **자연**(새벽·밤) |

`곡편성.js` 는 곡의 장르 칸으로 거른다(`겨눔.장르.includes(c.장르)`).
⇒ **「차분」으로 구운 곡이 장부에 「자연」으로 안 적히면 새벽·밤에 영원히 안 잡힌다.**
⏳ 그 사이를 잇는 다리가 있는지는 **안 재봤다** — 곡을 실제로 편성에 넣는 날 이 줄부터 연다.
🔑 이건 [[constant-known-in-two-places]] 무늬다(트랙 §0-라디오 의 「결이 두 뜻으로 갈렸다」와는 **다른** 자리 —
그건 «결 = 시간대냐 장르냐»였고, 이건 «장르 이름 자체»가 어긋난 것이다).

## 🗑 지워도 되나 — **된다.** 다만 둘로 갈린다 (유호 09-04 「별로인 것들은 바로바로 삭제해도 돼?」)

| 이름 앞에 | 지우면 | 왜 |
|---|---|---|
| (표시 없음) | ✅ **다시 만들 수 있다** | 씨앗과 문면이 이 장부에 있다. 같은 곡이 그대로 나온다 |
| **`_씨앗없음_`** | 🔴 **영원히 사라진다** | 씨앗을 안 남겨서 되살릴 길이 없다. 그중 셋은 유호님이 좋다고 하신 판이다 |

정렬하면 `_씨앗없음_` 이 맨 위로 모이므로 폴더에서 바로 갈린다.
⚠ **`라디오팩`·`라디오팩_ts` 는 다른 자리다** — 거기는 방송에 실제로 나가는 판이라 지우면 송출이 끊긴다.
🔑 지우신 뒤 알려 주시면 이 장부의 표를 실물에 맞춘다(장부가 없는 곡을 가리키면 그때부터 거짓이 된다).

**🗑 09-04 에 지운 넷** (유호 「음질이 나쁜 것들 전부 일단 삭제해줘」). 전부 «손잡이를 어디까지 올리면
소리가 깨지나»를 재려고 만든 시험판이라, 답이 나온 지금은 쓸 데가 없다. 잰 값은 위 §만드는 통로가 쥔다.

| 지운 것 | 잘린 지점 |
|---|---|
| `B_여유크게_촘촘하게.wav` | 176곳 |
| `C_B에_맑은목소리요청.wav` | 31.5곳 |
| `D_기본값에_맑은목소리요청.wav` | 9곳 |
| `_씨앗없음_citypop_inst.wav` | 밀기 3 설정으로 구운 판 |

⚠ **`_씨앗없음_citypop_vocal.wav` 는 남겼다.** 음질로는 가장 나쁘지만(3,385곳) 유호님이 「훌륭한데?」
하신 스타일이고 씨앗이 없어 되찾을 길이 없다. 지우면 그 결이 영원히 사라지므로 내 해석으로 지우지 않는다.

## 🛠 만드는 통로 (2026-09-04 실측)

- 자리 = 허깅페이스 Space **`MiniMaxAI/MiniMax-Music3`**(gradio) · 부르는 자리 = **`/studio_generate`** 하나
  (🔴 09-04 까지 쓰던 `/compose_assist` → `/simple_generate` 는 **가사를 버린다** · 아래 §처방)
- 값 = **0원**(PRO 구독 안). 🔴 「구글 무료 크레딧으로는 못 만든다」고 적었던 앞 줄은 **09-05 에 뒤집혔다** —
  아래 §구글 크레딧 참조(`lyria-002` 를 `us-central1` 에서 실제로 구웠다)
- 걸리는 시간 = 2분짜리 한 곡에 **약 3분 반**
- 🔄 **밀기(guidance) 기본값 = 3** (09-06 에 `1.7` 에서 옮겼다 · 유호 지시 「그 씨앗 없는 곡처럼 똑같은 세팅으로」).
  나머지 둘은 그대로다 — `steps 30 · headroom 0`.
  🔴 **앞 판은 여기를 1.7 로 못 박고 「건드리지 않는다」고 적었다. 그 자는 «음질»이었고, 유호님 귀는 반대로 골랐다:**
  | 판 | 밀기 | 잘린 지점 | 유호 판정 |
  |---|---|---|---|
  | `_씨앗없음_citypop_vocal` | **3** | 3,385곳 | ✅ **「훌륭한데?」 · 「너무 마음에 들어」** |
  | `A_기본값그대로` | 1.7 | 2.5곳 | 🔴 **「별로」**(음질은 최고였다) |
  ⇒ 문면도 결도 같고 **손잡이만 갈린** 두 판이다. 음질을 지키느라 유호님이 좋다 하신 결을 도구가 못 내고 있었다.
  🔑 밀기가 하는 일 = 「문면을 얼마나 세게 따라가나」. 3 은 시티팝의 결(브라스·슬랩베이스·리버브)을 더 세게
  붙잡고, 그 대가로 소리가 커져 천장을 친다. 그 대가는 아래 §잘린 곡 고치기가 치른다(도구가 자동으로 한다).
  ⏳ **밀기 «만»이 그 판정을 갈랐는지는 안 재봤다** — 미니맥스는 같은 값으로도 매번 다른 곡을 내므로,
  「그 씨앗의 곡이 별로였을」 가능성이 남는다. 다만 재현하려면 그때 값을 그대로 쓰는 것이 원칙이다.
- 🔴 **씨앗을 반드시 남긴다.** 스타일은 씨앗이 정하므로, 안 적으면 좋은 곡을 다시 못 만든다(09-04 에 하나 잃었다).
- 📏 재는 자 = `ffmpeg astats`(Peak count · Flat factor) + `ebur128`. **귀 판정 «전»에 숫자가 먼저 답한다.**
  🔑 **「잘린 지점 N곳」의 자리는 astats 의 «Overall» 줄**(채널 평균)이다 — 장부의 `2.5곳`·`4.5곳` 같은
  소수가 거기서 나왔다(채널 합으로 세면 정수가 되어 옛 수와 안 맞는다 · 09-06 확인).
  🔴 그 값을 코드에서 읽을 때 `execFileSync` 를 쓰면 **언제나 빈손**이다 — 그것은 stdout 만 돌려주는데
  astats 는 stderr 로 쓴다. `spawnSync` 로 둘 다 받아야 한다(09-06 에 밟았다 · [[zero-is-a-success-face-taxonomy]]).

## 🔴🔴 가장 큰 함정 — 「작곡 도우미」가 **한국어 가사를 영어로 갈아치운다** (09-04 · 유호님이 잡으셨다)

`/compose_assist` 는 이름이 「도우미」인데 실제로는 **가사를 통째로 다시 쓴다.** 09-04 실측:

```
내가 넣은 것   → 파도 소리가 창을 두드려 / 그해 여름이 아직 여기 있어
도우미가 낸 것 → Sunlight on the highway, we're heading to the coast
```

🔴 **그래서 09-04 에 구운 곡은 전부 영어로 불렸다.** 나는 그것을 모른 채 「한국어 가사가 붙습니다」로
여러 번 보고했고, 유호님이 「지금 영어가사야」로 잡으셨다. `description`·`title` 은 그대로 두면서
**`lyrics` 만** 갈아치우므로 되돌아온 state 를 들여다보지 않으면 안 보인다.

### 🔴 09-04 처방은 틀렸다 — `/simple_generate` **도** 가사를 다시 쓴다 (09-05 소스 확인)

09-04 에 「state 를 dict 로 곧바로 넘기면 도우미 없이 그대로 된다」고 적었다. Space 소스를 읽어 보니 아니다.
`app.py` 1566~1577줄 원문:

```python
def simple_generate(state, duration, seed, randomize_seed, headroom, steps, guidance):
    state = _normalize_state(state)
    description = _composed_description(state)
    ...
    lyr, gm, vd, arr = compose_song(description, duration)   # ← 여기서 가사를 새로 짓는다
    state.update(lyrics=lyr, global_meta=gm, vocals=vd, arrangement=arr)
    for audio, status, wav, used_seed in generate(
        description, lyr, gm, vd, arr, duration, seed, randomize_seed, headroom, steps, guidance):
```

`state["lyrics"]` 를 **읽지도 않는다.** 우리가 넣은 가사는 버려지고, `compose_song` 이 설명(description)만 보고
가사를 새로 짓는다. 도우미 `/compose_assist` 는 건너뛴 것이 맞지만, **같은 일이 안쪽에서 한 번 더 일어난다.**

### ✅ 가사를 지키는 통로는 `/studio_generate` 하나다

같은 파일 1581~1589줄. `compose_song` 을 아예 안 부르고 state 의 다섯 칸을 그대로 넘긴다:

```python
def studio_generate(state, duration, seed, randomize_seed, headroom, steps, guidance):
    state = _normalize_state(state)
    for audio, status, wav, used_seed in generate(
        state["description"], state["lyrics"], state["global_meta"], state["vocals"],
        state["arrangement"], duration, seed, randomize_seed, headroom, steps, guidance):
```

⏳ **아직 안 재봤다** — 09-05 새벽에 만든 한국어 가사 곡들이 «우리 가사»로 불렸는지는 확인하지 않았다.
설명 칸에 한국어를 넣었으니 `compose_song` 이 한국어로 **다시** 지었을 수 있다(비슷하되 우리 것이 아닌 가사).
🔑 **곡을 받으면 가사가 내 것인지 먼저 확인한다.** 안 들어 보고 「한국어가 붙었다」고 말하지 않는다.

## 🔴 **씨앗만으로는 못 되만든다** — 그래서 통로를 갈랐다 (09-06 실측 · 이 장부에서 가장 비싼 자리)

유호님이 「노래 느낌은 이게 제일 나아」로 고르신 `씨앗8888_긴가사`(2분 59초)를 씨앗 8888 로 다시 구웠다.
**남남이 나왔다.** 견준 숫자(1 = 같은 곡 · 0 = 남남 · 자는 `ffmpeg` 로 소리를 펴서 잰 상관값):

| 견준 것 | 소리크기 곡선 | 파형 | 소리결 |
|---|---|---|---|
| 자기 자신끼리(자 눈금 맞추기) | 1.0000 | 1.0000 | 1.0000 |
| **원본 vs 씨앗 8888 로 다시 구운 것** | **0.150** | **0.077** | **0.331** |
| 다른 씨앗(777)끼리 = 남남 바닥값 | 0.013 | 0.016 | 0.326 |
| **같은 씨앗 8888 로 2분 사이에 두 번** | **0.114** | **0.070** | **0.463** |

🔑 맨 아랫줄이 결정적이다. 문면도 씨앗도 길이도 똑같이 두 번 구웠는데 남남이 나왔다.
⇒ 「문면을 잘못 짐작했나」의 문제가 아니다. **씨앗이 곡을 붙잡지 못한다.**

### 까닭 — 소스가 답한다 (`app.py` · 09-06 에 받아서 읽었다)

- 544~552줄 `compose_song` 이 굽기 «전»에 노랫말과 세 칸(`global_meta`·`vocals`·`arrangement`)을 **새로 쓴다.**
- 499~521줄 그 글을 짓는 것은 딥시크(남의 글쓰기 모델)이고, 부를 때 **씨앗도 온도도 안 넘긴다.**
  게다가 공급자 셋을 돌아가며 쓴다 ⇒ 같은 문면을 줘도 매번 다른 글이 나온다.
- 🔴 1110~1123줄 `generate` 는 **우리 문면(`description`)을 아예 안 본다.** 소리로 가는 것은 넷뿐이다 —
  노랫말과, 세 칸을 이어 붙인 한 덩이(`caption`). 씨앗은 그 넷이 정해진 «다음»에 `manual_seed` 로 박힌다.

⇒ 씨앗은 **재료가 같을 때만** 곡을 붙잡는다. `/simple_generate` 는 재료를 매번 새로 만든다.

### ✅ 되게 만든 법 — 「짓기」와 「굽기」를 갈랐다 (talk `tools/미니맥스곡생산.js` · 커밋 `394a36d`)

1. `compose_assist` 를 먼저 불러 네 칸을 **받아 적는다.** 🔑 이 문은 그래픽카드를 안 써서 **실행 몫이 0** 이다.
2. 그 넷을 `/studio_generate` 로 넘겨 굽는다. 그 문은 넷을 손대지 않는다.
3. 사이드카(`<곡이름>.json`)에 네 칸과 씨앗을 박는다 ⇒ `--되살리기` 가 그대로 다시 만든다.

실측 — 둘 다 sha256(파일 내용 도장)이 **한 바이트도 다르지 않다**:

| 무엇 | 결과 |
|---|---|
| 같은 넷 + 씨앗 8888 로 두 번(60초) | `091cb24583976d3c176f…` = `091cb24583976d3c176f…` |
| 짓고 굽기 → 사이드카로 되살리기(40초) | `6c1532de6f7ed87569f8…` = `6c1532de6f7ed87569f8…` |

🔑 소리의 결은 그대로다. 안쪽에서 짓던 것을 **밖으로 꺼냈을 뿐**이라 곡의 성격이 달라지지 않는다.

### 🔴 그래서 09-04~05 에 구운 곡은 **영원히 못 되살린다**

씨앗8888 을 포함해 그때 곡은 전부 `/simple_generate` 한 발로 구웠고, 그때 지어진 네 칸이 어디에도 안 남았다.
⇒ 그 곡들은 **파일 자체가 원본이다. 지우면 끝이다.** (옮기고 늘리는 것은 되지만 「다시 굽기」는 없다.)
⇒ 09-06 뒤에 구운 곡부터는 사이드카의 `되살아나나` 칸이 「된다 / 안 된다」를 스스로 말한다.

## 📏 곡 «길이»는 무엇이 정하나 — 씨앗이다 (09-04 실측 · 내 가설이 틀렸다)

`duration` 을 240 으로 줘도 그대로 안 나온다. 처음엔 「가사 분량이 정한다」로 읽었는데 **틀렸다**:

| 판 | 가사 | duration | 실제 길이 |
|---|---|---|---|
| 씨앗777 | 짧은 판 | 240 | 2분 44초 |
| **씨앗1234** | **같은 짧은 판** | 240 | **3분 24초** |
| 씨앗8888 | **두 배로 늘린 판**(절 2·후렴 3·브리지 1) | 240 | 2분 59초 |

⇒ 가사를 두 배로 늘려도 **15초**만 늘었는데, 같은 가사에 씨앗만 바꾸니 **40초**가 갈렸다.

🔴 **그런데 이 표는 자가 틀린 자리에서 쟀다** (09-05 확인). 셋 다 `/simple_generate` 로 구웠고, 위 절에서 보듯
그 통로는 **우리 가사를 버리고 스스로 짓는다.** 「가사를 두 배로 늘렸다」는 칸은 실제로 모델에 안 닿았다.
⇒ 「길이는 씨앗이 정한다」는 결론은 **아직 서지 않았다.** 가사가 실제로 닿는 `/studio_generate` 로 다시 재야 갈린다.

### 🔴 상한은 3분이 아니라 5분이다 (09-05 소스 확인 · 앞 판은 틀렸다)

`app.py` 1631줄이 길이 손잡이의 상한을 못 박는다:

```python
duration = gr.Slider(5, 300, value=60, step=5, label="Maximum song duration (seconds)")
```

**300초(5분)까지 받는다.** 09-04 에 셋 다 3분을 못 넘긴 것은 통로의 상한이 아니라 위의 «가사가 안 닿는» 문제로 보인다.

📏 가사 분량이 길이를 정한다는 규칙이 소스에 박혀 있다(`_LYRICS_RULES` · 481줄 원문):
> "Size the structure to the duration: <=30s: one verse + one chorus; ~60s: verse/pre-chorus/chorus/verse/chorus;
> **>=120s: full structure with bridge and outro**" · "Roughly **12-16 sung words per 10 seconds**"

⇒ 5분 곡을 노리면 가사를 **360~480 낱말**로 짜고 구역 표시를 충분히 넣는다.
쓸 수 있는 표시는 `[intro] [verse] [pre-chorus] [chorus] [post-chorus] [bridge] [instrumental] [solo] [outro]`
(각 표시는 **제 줄에 혼자** 있어야 하고, 같은 줄에 노랫말을 붙이면 안 된다).

⚠ `duration 290` 실패(`Expired ZeroGPU proxy token`)는 여전하다. 남의 실행 자리를 예약하는 시간이
소스(1101·1105줄)에는 `duration × (0.75 + 0.20 × steps/30) + 15` 초로 적혀 있다(상한 600초).
⇒ steps 를 30에서 낮추면 예약 시간이 줄어 더 긴 곡이 통과할 수 있다. ⏳ 안 재봤다(steps 는 음질도 건드린다).

🔴 **09-05 실측 — 서버가 실제로 요구하는 몫은 그 공식의 «정확히 2배»다.** 거절 메시지가 요구량을 말해 줘서 두 점으로 풀었다:

| 곡 길이 | 서버가 요구한 몫 | 소스 공식이 낸 값 |
|---:|---:|---:|
| 120초 | **258초** | 129초 |
| 60초 | **144초** | 72초 |

⇒ **몫(초) = 곡 길이 × 1.9 + 30** (steps 30 기준). 몫이 얼마 남았는지 셈할 때는 이 자를 쓴다.
🔑 거절 메시지가 «요구량 · 남은 양 · 회복 시각»을 다 준다(`258s requested vs. -55s left. Try again in 5:28:47`)
 ⇒ 굽기 전에 남은 몫을 따로 물을 자리가 없어도, 한 번 거절당하면 그 답이 전부 나온다(거절은 몫을 안 먹는다).

### ⇒ 4분 넘는 판은 «이어 붙여» 만든다

원래 라디오 공정이 그랬다(Lyria 때도 생성물 셋을 이었다 · `tools/라디오곡생산.js` 머리말).

```
ffmpeg -i A.wav -i A.wav -i A.wav \
  -filter_complex "[0][1]acrossfade=d=4:c1=tri:c2=tri[a];[a][2]acrossfade=d=4:c1=tri:c2=tri" \
  -ar 44100 -c:a pcm_s16le <긴판>
```

- 2분 곡 셋을 이으면 **5분 52초**(겹치는 4초씩 빠진다). 09-04 에 이 방식으로 긴 판을 만들었다.
- 🔑 **서로 다른 곡 셋을 이으면** 반복이 아예 안 들린다(같은 곡 반복보다 낫다).
- ⚠ 방송 판 규격이 **최소 360초**라 5분 52초(352초)는 8초 모자란다 — 넷을 잇거나 한 곡을 더 길게 고른다.
- 🔴 **같은 곡을 통째로 세 번 잇는 것은 티가 난다** (09-05 유호 「그냥 2분짜리 계속 반복만 하네」).
  씨앗이 없어 다시 못 굽는 곡은 아래 §마디 재배열로 늘린다.

## 💰 구글 크레딧으로 곡이 «된다» (09-05 · 09-04 판정을 뒤집었다)

유호님이 「크레딧으로 노래 만드는 건 안 되지?」 물으셔서 다시 쟀고, **09-04 의 「못 만든다」가 틀렸다.**

```
us-central1   모델 목록 HTTP 200 · 전체 133종
              음악: lyria-3-pro-preview, lyria-3-clip-preview, lyria-002
  lyria-002 :predict → HTTP 200 · 25.8초 만에 · 48kHz 스테레오 wav 6.0MB · 길이 32.8초
```

🔑 **09-04 에 「음악 모델 0개」가 나온 까닭 = 위치를 `global` 로 두고 쟀다.** 그 문은 목록 조회 자체가
404 라서 「없다」가 아니라 «못 물었다»였다. 곡 모델은 `us-central1` 에 있다.
⇒ memory `zero-is-a-success-face-taxonomy` 그대로다. **«0개»를 보면 자부터 의심하고 대조군을 세운다**
(같은 요청을 지역 넷에 던져 200 이 나오는 곳을 찾는 식).

| | |
|---|---|
| 부르는 곳 | `https://us-central1-aiplatform.googleapis.com/v1/projects/{proj}/locations/us-central1/publishers/google/models/lyria-002:predict` |
| 몸통 | `{"instances":[{"prompt":"<문면>"}],"parameters":{"sampleCount":1}}` |
| 돌아오는 것 | `predictions[0].bytesBase64Encoded` (48kHz 스테레오 wav) |
| 열쇠 | OAuth 토큰 = `tools/모델정책.js` 의 `벌텍스토큰()` (Vertex 는 API 키를 못 받는다) |
| 값 | 한 곡 $0.08 · 크레딧 ₩435,523 이면 물량 제한이 사실상 없다 (⏰ 시한 2026-11-13) |

🔴 **한계 넷** (전부 09-05 실측)

1. 🔴 **한 벌이 «32.768초» 고정이고 길이 손잡이가 없다.** `durationSeconds: 120` 을 주면
   **HTTP 200 을 주면서 그냥 무시한다**(받아 든 파일이 32.768초로 똑같았다).
   ⚠ 여기서 200 을 「통했다」로 읽으면 안 된다 — 모르는 칸을 조용히 버리는 무늬다.
   다른 이름 넷(`duration`·`lengthSeconds`·`audioLengthSeconds`·`sampleLengthSeconds`)은 400 도 안 나고 같이 무시된다.
2. 🔴 **`sampleCount` 도 안 듣는다** — 4 를 줘도 **1벌**만 온다. ⇒ 열두 벌이면 **열두 번 부른다.**
3. ~~`lyria-3-pro-preview` 는 `:predict` 가 404 — 접근 권한이 없다~~ → 🔴 **이 판정은 09-05 저녁에 뒤집혔다.**
   404 의 까닭은 권한이 아니라 **부르는 문이 다른 것**이었다(아래 §3세대). 크레딧으로도 잘 돈다.
4. 🔴 **Lyria 로는 «있는 곡을 늘리지» 못한다** — 소리를 입력으로 받는 칸이 없다. 새 곡만 만든다.
   ⇒ 여름 시티팝을 늘리는 일은 아래 ACE-Step 이 해야 한다.
   ⚠ 여기 붙어 있던 「가사도 안 붙는다(연주곡만)」도 **`lyria-002` 에만 참**이다 — 3세대는 노래를 부른다.

### ✅ 3세대 `lyria-3-pro-preview` — 09-05 저녁에 문을 찾았다 (유호 「lyria 3 pro preview로도 노래 몇개 만들어보자」)

위 «한계 넷»은 **전부 구세대 `lyria-002` 의 것**이다. 3세대는 다른 문을 쓰고 한계도 다르다.

| | `lyria-002`(구세대) | **`lyria-3-pro-preview`(3세대)** |
|---|---|---|
| 부르는 곳 | `us-central1-aiplatform…/…/lyria-002**:predict**` | `**aiplatform**.googleapis.com/v1beta1/projects/{p}/locations/**global**/**interactions**` |
| 몸통 | `{"instances":[{"prompt":…}]}` | `{"model":"lyria-3-pro-preview","input":"<문면>"}` |
| 돌아오는 것 | `predictions[0].bytesBase64Encoded` · wav | 응답 어딘가의 `{type:'audio',data}` · **mp3 44.1kHz 스테레오** |
| 길이 | **32.768초 고정** | **145~178초 실측**(길이 손잡이는 안 재봤다 · 문면 길이가 정하는 것으로 보인다) |
| 노래(가사) | 안 붙는다 | ✅ **붙는다**(유호 확인 09-05 「목소리는 잘 들어갔어」) |
| 걸리는 시간 | 25.8초 | 42~70초 |

🔴 **주소를 두 번 헛짚었다 — 자를 남긴다:**
| 던진 것 | 답 |
|---|---|
| `us-central1` 호스트 + `/locations/us-central1` | 400 `Unsupported location: us-central1. Supported locations are global, us, and eu` |
| `us-central1` 호스트 + `/locations/global` | 400 `Lyria 3 is only supported in the global location` |
| ✅ **전역 호스트 + `/locations/global`** | **200** |
⇒ **호스트와 경로가 «둘 다» global 이어야 한다.** 구세대와 정반대라(그쪽은 `us-central1`) 섞으면 조용히 404 다.

🔴 **09-05 첫 굽기가 「케이팝 같고 뻔하다」로 탈락했다 — 까닭은 모델이 아니라 «문면»이었다.**
내가 장부 §문면을 안 열고 새로 지었고, 거기서 **`80s`·`wide analog reverb`·`clean vintage mix` 가 빠지고**
`catchy singable chorus` 가 들어가 현대 팝 쪽으로 밀렸다.
🔑 **견줌은 문면을 글자 그대로 옮겨야 성립한다** — 아래 §문면이 그 정본이고, 새로 짓지 않는다.

#### 📏 방송판 한 벌(6분)을 만드는 실측값 — 09-05

| | |
|---|---|
| 몇 벌이 필요한가 | **12벌** (32.768초 × 12 = 393.2초) |
| 잇는 법 | `acrossfade=d=3:c1=tri:c2=tri` 를 **11번** 체인 ⇒ 393.2 − 33 = **360.2초** |
| 걸리는 시간 | **6.2분** (한 벌 25~35초 · 열두 번 부른다) |
| 값 | **$0.96** ($0.08 × 12) |
| 마무리 | `loudnorm=I=-14:TP=-1.5:LRA=11,aresample=48000` → mp3 320k |

🔑 **겹침을 3초로 잡은 까닭** = 방송판 규격이 최소 360초인데 4초로 겹치면 349초가 되어 11초 모자란다.
3초면 360.2초로 딱 걸린다.
🔑 **서로 다른 곡 열둘을 잇는 것이라 반복이 안 들린다** — 같은 곡을 되풀이하는 것보다 낫다(위 §이어 붙이기와 같은 원리).

### 🔴🔴 ACE-Step 으로 원곡을 늘리는 길은 **막혔다** — 여섯 판 실측 (09-06 밤 ~ 09-07 새벽)

> ⛔ **이 절 아래의 「이 길이 정답이다」는 09-07 에 뒤집혔다.** 여섯 번 다르게 시도해 여섯 번 다
> 원곡을 못 지켰다. **다시 이 길로 가지 마라** — 아래 실측이 그 값을 이미 치렀다.
> ✅ 지금의 답은 **Suno**(글로 노래를 만들어 주는 자리 · `결정.md` 09-07). 유호님이 직접 써 보시고 채택하셨다.

| 판 | 어떻게 했나 | 원곡 앞 20초 보존(파형) |
|---|---|---|
| 1 | `repaint` · 원곡 경로를 **글자로** 줌 | 0.132 |
| 2 | 파일 이름을 로마자로 | 0.107 |
| 3 | 원곡 뒤에 **무음**을 붙여 240초로 올림 | 0.109 · 🔴 뒤가 통째로 무음으로 돌아왔다 |
| 4 | 흔드는 값(`retake_variance`) 0 | 0.009 |
| 5 | 무음 대신 **원곡을 반복**해 240초로 | 0.096 |
| 6 | **`extend` 전용 자리**(`/extend_process_func`) | 0.081 |

🔑 **1이면 원곡 그대로 · 0.2 아래면 딴 곡.** 여섯 판 전부 딴 곡이 나왔다.
🔴 소스(`pipeline_ace_step.py` 1072~1078)는 원곡을 지키게 짜여 있다(`repaint_mask` 가 새로 그릴 자리만 1).
   그런데 실제로는 매번 새로 그린다 ⇒ **값의 문제가 아니라 그 서버에서 그 부분이 안 도는 것으로 보인다.**

**🔴 여기서 내가 밟은 함정 넷 — 다음이 또 밟지 않게 적는다**

1. **자리를 잘못 골랐다.** `repainting`(구간 다시 그리기)은 «원곡을 고쳐 쓰는» 자리다. 원곡이 안 남는 것이
   당연했다. `extend`(이어 붙이기)가 따로 있는 것을 **화면을 열고서야** 봤다(다섯 판을 헛되이 썼다).
   ⇒ 🔑 **부를 수 있는 자리 «이름 목록»을 눈으로 먼저 훑는다.** 내가 짠 자동 검색은 `extend` 를 안 잡았다.
2. **자를 잘못 골라 「성공」이라 보고했다.** 소리 크기 곡선으로 재서 0.857 이 나오자 「원곡이 살아 있다」고
   유호님께 말씀드렸다. 파형으로 재니 0.132(딴 곡)였다. ⇒ **곡이 같은지는 «파형»으로만 잰다.**
3. **무음을 붙여 놓고 확인 안 했다.** 원곡 뒤에 무음 2분을 붙여 「채워라」 했는데 그대로 무음이 돌아왔고,
   나는 그것을 잘라 붙여 보내 드렸다(유호 「2분 5초부터 아예 소리가 안 나와」).
   ⇒ **보내기 전에 20초마다 소리 크기를 잰다.** 몇 초면 되는 검사였다.
4. **«음악인지»를 재는 자를 두 번 만들었고 두 번 다 실패했다.** 대역 비율로 재니 유호님이 「외계인 소리」라
   하신 판이 81% 로 통과했다(정상 곡 94%). ⇒ 🔑 **소리가 음악으로 들리는지는 내가 못 잰다.**
   그 판정은 유호님 귀가 한다. 「좋게 들릴 것」이라고 미리 말하지 않는다.

**🔴 곁가지로 확인한 것 셋**
- `ACE-Step/Ace-Step-v1.5` Space 는 **넉 달째 고장**이다(Phase 1 에서 멈춘다 · 토론 게시판 2026-05-17
  「Having difficulty generating music, getting stuck at either phase one or more」). 화면에서도 곡이 안 나온다.
- **`acemusic.ai`**(같은 팀의 정식 서비스 · 무료)는 화면에서 곡이 나온다. 그런데 **늘리기 값을 화면이 막는다** —
  구간 끝 막대가 원곡 길이에서 묶이고, 길이 240초를 줘도 무시하고 2분을 낸다.
- ACE-Step 을 돌리는 다른 Space 아홉 곳을 훑었는데 **여덟 곳이 죽어 있었다**(RUNTIME_ERROR·PAUSED).

🔴 **미니맥스는 소리를 입력으로 «못» 받는다** — 부르는 항목 전량을 확인했고 오디오 칸이 하나도 없다.
⇒ 씨앗 없는 곡을 그 통로로 늘리는 길은 원리상 막혀 있다. 아래 §마디 재배열은 «덧대는» 것이라
**새 소리가 안 생긴다**(유호 09-05 「원곡을 중간에 짤라서 어거지로 삽입한거잖아」).

⚠ **원곡 뒤에 «다른 곡»을 이어 붙이는 것도 답이 아니다**(유호 09-07 「뒤에 뭘 이어붙여도 원곡 2분의
느낌이 아니지 않아? 다른 노래라는 거 아냐?」). 조성·멜로디·목소리가 달라 두 곡을 붙인 것으로 들린다.
⇒ **원곡을 «듣고» 그 연장을 그리는 것만이 답이다.**

<details><summary>아래는 09-05~06 에 적은 옛 판정이다(참고용 · 위 실측이 뒤집었다)</summary>

`ACE-Step/Ace-Step-v1.5`(허깅페이스 Space)는 소리를 받는다. 그래서 **이 길이 정답이다.**

| 갈래(`task_type`) | 무엇을 하나 |
|---|---|
| `text2music` | 글로 새 곡 (미니맥스가 하던 일) |
| **`cover`** | **원곡을 넣으면 그 결을 따라 다시 그린다** ← 씨앗 없는 곡을 늘리는 자리 |
| **`repaint`** | 곡의 지정한 구간만 다시 그린다 (`repainting_start`·`repainting_end`) |

- 부르는 곳 = `/generation_wrapper` · 항목 **49개**를 순서대로 준다. `generation_mode` 는
  `simple|custom|cover|repaint` 넷 중 하나이고 `task_type` 과 **따로** 준다(둘을 헷갈리면 거절당한다).
- 항목 순서는 `/capture_current_params` 의 이름 있는 목록과 **4번째부터 1:1로 맞는다**(09-05 대조 확인).
  요긴한 자리만: `0 selected_model · 1 generation_mode · 4 captions · 5 lyrics · 6 bpm · 9 vocal_language(ko 있다)
  · 10 inference_steps · 14 reference_audio · 15 audio_duration · 17 src_audio · 19·20 repainting_start·end
  · 22 audio_cover_strength · 23 task_type`
- 길이 = 소개글에 「짧은 반복구부터 **10분**까지」 · 한국어 노래를 지원 언어에 넣고 있다.
- 📜 라이선스 = Apache-2.0 (모델은 MIT). 미니맥스처럼 «화면에 이름 표시» 의무는 없다.

#### 🔴 걸린 것 = 남의 컴퓨터 몫(ZeroGPU)

```
You have exceeded your Pro ZeroGPU quota (180s requested vs. 74s left). Try again in 21:23:20.
```

🔴 **곡 길이와 «무관하게» 180초를 고정으로 잡는다** — 90초 곡으로 줄여도 요청은 그대로 180초였다.
미니맥스가 `duration × (0.75+0.20×steps/30)+15` 로 길이에 비례해 잡던 것과 다르다.
⇒ PRO 하루 몫으로 **몇 벌 못 굽는다.** 회복은 21시간 뒤 · 더 사려면 $1 에 10분.

🚫 **이 노트북에서는 돌리지 않는다**(유호 확정 09-05 · `결정.md`). 실제로 깔아 재본 결과는 그 확정에 붙어 있다.

### ✅ ACE-Step 으로 «진짜 늘리기»가 되는 자리를 찾았다 — 소스가 답한다 (09-06)

`handler.py` 1694~1700줄이 결정적이다. **repaint 의 끝 지점을 원곡보다 길게 주면 그만큼 빈 자리를
뒤에 붙이고 거기를 그린다.** 잘라 붙이는 것이 아니라 원곡을 듣고 이어서 «새 소리»를 만든다.

```python
right_padding_duration = max(0, actual_end - src_audio_duration)
```

⇒ 유호님이 물리신 「원곡을 중간에 짤라서 어거지로 삽입한거잖아」와 **다른 물건**이다.

넘길 값(항목 49개 중 요긴한 것 · 순서는 `/capture_current_params` 가 4번째부터 1:1로 알려 준다):

| 자리 | 이름 | 값 |
|---|---|---|
| 1 | `generation_mode` | `repaint` |
| 17 | `src_audio` | 올린 원곡 `{path, meta:{_type:'gradio.FileData'}}` |
| 19·20 | `repainting_start`·`end` | 원곡 끝 5초 앞 · **늘리고 싶은 길이** |
| 23 | `task_type` | `repaint` |
| 15 | `audio_duration` | 늘리고 싶은 길이 |
| 16 | `batch_size_input` | `1` (몫을 아낀다) |

🔴 **아직 한 벌도 못 구웠다** — 몫에 막혔다(아래).

### 🔴🔴 ACE-Step 은 몫이 모자라면 «까닭 없이» 죽는다 — 재는 자는 미니맥스다 (09-06 실측)

ACE-Step 의 `/generation_wrapper` 는 몫이 모자라면 이것만 돌려준다:

```
event: error
data: null
```

🔑 **값을 틀리게 줘도 똑같은 말이 온다**(항목을 44개만 보내도 같았다 · 09-06 에 판 여덟을 짚었다).
그래서 이 말만 보고는 「내 값이 틀렸나」와 「몫이 없나」를 못 가른다.

✅ **가르는 자 = 미니맥스에 큰 요청을 던진다.** 그쪽은 남은 양을 또박또박 말해 주고, **거절은 몫을 안 먹는다.**

```
You have exceeded your Pro ZeroGPU quota (600s requested vs. 156s left). Try again in 23:12:57.
```

| | 한 번에 얼마를 요구하나 | 거절할 때 |
|---|---|---|
| 미니맥스 | `0.95 × 곡길이 + 15` 초 (곡 길이에 비례) | **까닭과 남은 양을 말한다** |
| ACE-Step | **180초 고정** (곡 길이와 무관) | 🔴 `null` 만 온다 |

⇒ ACE-Step 을 한 번 던지려면 **180초가 통째로 있어야 한다.** 09-06 저녁에 156초가 남아 24초가 모자랐다.
⇒ 회복은 하루에 한 번이다. **ACE-Step 을 쓸 날은 미니맥스로 곡을 굽지 않는다.** 몫을 한 통에서 나눠 쓴다.

</details>

## 🎵 곡 만드는 자리 = **Suno** (2026-09-07 유호 확정 · 여기가 지금의 정본이다)

유호님이 직접 써 보시고 정하셨다 — 「진심으로 너무 좋은데? 수노 많이 써야겠다」.
🔑 오늘 새벽 ACE-Step 으로 여섯 번 막힌 끝에 이 자리로 왔다. 그 여섯 판의 값은 위 절이 쥔다.

| | |
|---|---|
| 자리 | `suno.com` |
| 무료 요금제 | 하루 10곡 · **8분 분량까지 소리 파일을 올릴 수 있다** · 🔴 상업 이용 금지 · 🔴 09-03 부터 곡 내려받기 없음 |
| **Pro** | **월 ₩12,000**(연 ₩36,000 묶어 결제) · 매달 500곡 · 월 20곡 내려받기 · ✅ 상업 이용 됨 |
| Premier | 월 ₩36,000 · 매달 2,000곡 · 월 60곡 내려받기 |

🔴 **학원에 쓰려면 Pro 이상이라야 한다.** 무료로 만든 곡은 라디오·홍보물에 못 쓴다.
⏳ **원곡 늘리기가 Suno 에서 되는지는 아직 안 재봤다** — 소리 파일을 올릴 수 있다는 것까지만 확인했다.

## 📀 음원을 세상에 내는 길 (2026-09-07 · 유호 확정 = 해외 유통사)

🔑 **유튜브 뮤직·스포티파이에는 개인이 직접 못 올린다.** 유통사(곡을 음악 가게들에 대신 넣어 주는 회사)를 거친다.

**유호 확정** — 「한국 유통사보다 해외 유통사를 이용하는 게 좋겠네」.
⇒ 유튜브 뮤직·스포티파이·애플 뮤직에 한 번에 들어간다. **멜론·지니·벅스는 포기한다**(그쪽은 한국 유통사가 따로 필요하고, 멜론은 개인이 직접 등록할 수 없다).

| 회사 | 값 | 특징 |
|---|---|---|
| **DistroKid** | **연 $24.99**(약 3만 5천원) | 곡 무제한. 값이 가장 싸다 |
| DistroKid Musician Plus | 연 $44.99 | 발매일 지정 · 음반사 이름 |
| TuneCore | 더 비쌈 | 정산이 빠르다 |
| CD Baby | 곡당 결제 | |
| Amuse | 무료 요금제 있음 | |
⏳ 어느 회사를 쓸지는 **아직 안 정했다**(실제로 낼 때 다시 견준다).

✅ **돈 받을 그릇이 없어도 지금 곡을 낼 수 있다** — [[no-korean-billing-vessel]] 이 막지 않는다.
   수익은 그 회사 «계정 안 통장»에 쌓이고, 받는 자리는 나중에 등록해도 그전 수익이 그대로 남는다
   (DistroKid 도움말 원문: "You won't need to enter any payout details until you have…").
   🔑 오히려 모아서 한 번에 빼는 편이 낫다 — 뺄 때마다 수수료가 붙는다.
⏳ **안 알아본 것 둘**: 미국 세금 서류(W-8BEN)를 안 내면 얼마가 미리 떼이는가 · 나중에 법인으로 명의를 옮길 수 있는가.

**낼 때 필요한 것** — 곡 파일(원본 음질) · **가로세로 3000픽셀 정사각형 표지 그림** · 아티스트 이름 · 곡 제목 · 발매일.

### 🧩 씨앗 없는 곡을 «마디를 안 깨고» 늘리는 법 (09-05 실측 · ⚠ 새 소리는 안 생긴다)

같은 곡을 반복해도 티가 안 나게 하려면 **덩이를 나눠 순서를 바꾸고 변주를 섞는다.**
`_씨앗없음_citypop_vocal_고친판` 으로 실제로 해서 2분 → **4분 17초**를 만들었다.

| 걸음 | 무엇을 | 값 |
|---|---|---|
| ① 마디 길이를 구한다 | `60 ÷ BPM × 4` | 112 BPM → 한 마디 **2.142857초** |
| ② 8마디 덩이로 자른다 | `17.142857초` | 120초 곡 = **정확히 7덩이** |
| ③ 덩이별 음량을 재 구조를 읽는다 | `volumedetect` | 큰 덩이 = 후렴 |
| ④ 변주 덩이를 만든다 | 아래 두 줄 | |
| ⑤ 순서를 짜 `concat` 으로 잇는다 | 마디가 맞으면 크로스페이드가 필요 없다 | |

```
# 반음 위로 (피치만 · 속도 유지) — R=1.059463
#   🔴 넉넉히 잘라 넣고 atrim 으로 못 박는다. 안 그러면 13ms 짧아져 마디가 밀린다
ffmpeg -ss <시작> -t 18.4 -i <원본> \
  -af "asetrate=44100*1.059463,atempo=1/1.059463,aresample=44100,atrim=0:17.142857,asetpts=N/SR/TB" <덩이>

# 보컬을 줄여 «연주 구간»처럼 (가운데 소리 빼기)
ffmpeg -i <덩이> -af "pan=stereo|c0=0.9*c0-0.55*c1|c1=0.9*c1-0.55*c0,volume=2.0" <덩이>
```

🔑 **마지막 후렴을 반음 올리는 것이 핵심이다** — 시티팝·J-POP 의 정석 수법이라 같은 소리인데 «전개»로 들린다.
실제로 쓴 차례 = `원곡 7덩이 → 연주 2덩이 → 절부터 3덩이 → 후렴 3덩이(반음 위)` = 15덩이 · 257초.

## 🩹 잘린 곡은 «고칠 수 있다» — 다시 못 굽는 곡을 살리는 길 (09-04 실측)

> ✅ **09-06 부터 이 절은 손으로 안 한다** — `tools/미니맥스곡생산.js` 가 굽고 나서 **자동으로** 한다
> (밀기 3 을 기본으로 삼았으니 그 대가를 도구가 치르는 것이 한 벌이다). 다듬은 판이 본 이름을 갖고,
> 구운 그대로는 옆에 `_원본.wav` 로 남는다 — 다듬기는 «추정»이라 되돌릴 길을 둔다.
> 🔑 아래 명령은 **도구 밖에서 옛 곡을 고칠 때** 그대로 쓴다(도구가 쓰는 값과 글자까지 같다).
> ⚠ `--안다듬기` 를 주면 건너뛴다 — 구운 소리 그대로를 견줄 때만 쓴다.

씨앗을 안 남긴 곡은 다시 못 만든다. 그런데 **음질만 나쁜 것이라면 그 파일 자체를 고칠 수 있다.**
유호님이 「너무 좋은데??」 하신 `_씨앗없음_citypop_vocal` 이 잘림 3,385곳짜리였고, 그것을 이렇게 살렸다:

```
ffmpeg -i <원본> -af "adeclip=window=55:overlap=75:arorder=8:threshold=10:hsize=1000:method=add,
                       loudnorm=I=-14:TP=-1.5:LRA=11" -ar 44100 -c:a pcm_s16le <고친판>
```

| | 원본 | 고친 판 |
|---|---|---|
| 꼭대기 | 0.000000(천장에 붙음) | **−1.48**(떨어졌다) |
| 천장 친 수 | **3,385곳** | **1곳** |
| 평평함(잘림) | **26.8** | **0.0** |
| 음량 | −11.4 LUFS | **−13.9 LUFS**(방송 규격 −14) |

- 🔑 `adeclip` 이 잘려 나간 파형을 앞뒤 소리로 **추정해 메운다**. ⚠ «되살리는» 것이 아니라 «추정»이므로
  숫자가 좋아져도 **귀로 한 번 더 확인**한다.
- 🔴 **`-ar 44100` 을 빼면 안 된다** — `loudnorm` 이 표본율을 192kHz 로 올려 파일이 4배가 된다(09-04 실측 88MB).
- 🔑 원본은 **지우지 않는다.** 고친 판은 원본에서 파생되므로, 원본이 사라지면 다시 고칠 수도 없다.

## 📜 쓸 때 지킬 것 — 표시 의무

MiniMax-Music3 COMMUNITY LICENSE 는 상업 사용을 허용하되 조건 둘을 단다.
1. 이 곡을 쓰는 **제품 화면에 「MiniMax-Music3」를 눈에 띄게 표시**한다 ⇒ 자리 = talk `bots/송출/크레딧.md` · 방송 설명란
2. 연매출 **$20M** 를 넘으면 별도 서면 승인(지금은 한참 아래)

## 🎵 첫 묶음 — 2026-09-04 (지금 **10벌 · 173MB** · 넷은 09-04 에 지웠다 · 장르 폴더 셋)

전부 `duration 120`(연주판은 모델이 더 짧게 닫았다) · 한국어 가사판은 아래 §가사 참조.
「어느 폴더」 칸이 지금 그 파일이 사는 자리다.

| 파일 | 씨앗 | 결 · 어느 폴더 | 손잡이 | 잘린 지점 / 음량 | 판정 |
|---|---|---|---|---|---|
| `_씨앗없음_ko_vocal.wav` | 🔴 **안 남겼다** | 담백한 한국어 시티팝 · 시티팝_citypop | 기본 | 안 쟀다 | 유호 「너무 좋은데?」 |
| `_씨앗없음_instrumental.wav` | 🔴 안 남겼다 | 로파이 공부 비트 · 차분_calm | 기본 | 안 쟀다 | 유호 「너무 좋은데?」 |
| `_씨앗없음_citypop_vocal.wav` | 🔴 안 남겼다 | 여름 시티팝 · 시티팝_citypop | 밀기 3 | **3,385곳** / −11.4 | 유호 「훌륭한데?」 · 🔴 지지직 · **고친 판의 원본이라 안 지운다** |
| `_씨앗없음_citypop_vocal_고친판.wav` | (위 파일에서 파생) | 〃 · 시티팝_citypop | 잘림 복원 + 음량 정리 | **1곳** / −13.9 | ✅ 유호 「너무 좋은데??」 판을 살린 것 |
| `_씨앗없음_frenchhouse_vocal.wav` | 🔴 안 남겼다 | 느린 그루브 · 전자_house | 밀기 3 | 안 쟀다 | |
| `_씨앗없음_frenchhouse_inst.wav` | 🔴 안 남겼다 | 느린 그루브(연주) · 전자_house | 밀기 3 | 안 쟀다 | |
| `A_기본값그대로.wav` | 20260904 | 여름 시티팝 · 시티팝_citypop | **기본** | **2.5곳** / −13.9 | 음질 최고 · 스타일은 유호 「별로」 |
| `씨앗11.wav` | **11** | 여름 시티팝 · 시티팝_citypop | 기본 | 14.5곳 / −13.3 | ⏳ 유호 판정 |
| `씨앗88.wav` | **88** | 〃 · 시티팝_citypop | 기본 | 5곳 / −13.9 | ⏳ 유호 판정 |
| `씨앗314.wav` | **314** | 〃 · 시티팝_citypop | 기본 | 4.5곳 / −13.6 | ⏳ 유호 판정 · 숫자상 가장 깨끗 |
| `씨앗2027.wav` | **2027** | 〃 · 시티팝_citypop | 기본 | 5곳 / −13.5 | ⏳ 유호 판정 |

### 결을 부르는 문면 (아티스트명·곡명은 넣지 않는다 — 필터 대상)

- **여름 시티팝**: `80s japanese city pop, bright summer seaside mood, sparkling electric piano, punchy slap bass, warm brass stabs, breezy airy female vocal, mid-up tempo around 112 bpm, wide analog reverb, nostalgic golden-hour warmth, clean vintage mix`
- **느린 그루브**: `french house downtempo, slow hypnotic groove around 92 bpm, warm rhodes electric piano, soft filtered bassline, intimate vocoder-tinted vocal, gentle side-chain pulse, late night romantic mood, vinyl texture, smooth analog filter sweeps`
- **담백한 한국어 시티팝**: `warm korean city pop, gentle female vocal, bright piano and soft synth, mid tempo, hopeful morning mood`
- **로파이 공부 비트**: `calm lo-fi study beat, soft rhodes piano, warm tape texture, steady gentle groove, no vocals`

### 가사

**「그 여름날」** — `파도 소리가 창을 두드려 / 그해 여름이 아직 여기 있어 / 눈을 감으면 다시 시작돼 /
기억해 그 여름날을 / 바람도 우리 편이었지 / 햇빛이 길게 누운 오후 / 아직 끝나지 않았어`

**「새벽 두 시」** — `말하지 않아도 알 것 같아 / 남아 있는 온기 하나 / 밤이 우리를 감싸고 / 아무 말도 필요 없어`

## ⏳ 아직 안 한 것

- 방송 판 규격(**최소 360초** · `SYNK-talk/lib/곡판규격.js`)에 맞추기 — 실측은 40~120초라 늘리거나 이어 붙여야 한다
- 8시간치 물량을 실제로 굽는 시간 · 남의 Space 를 대량으로 두드릴 때 막히는지
- 채택 곡 확정 뒤 talk `tools/라디오곡생산.js` 통로를 이쪽으로 옮기기(지금은 Lyria · AI Studio 문)
