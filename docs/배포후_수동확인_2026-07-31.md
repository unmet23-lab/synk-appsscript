# 배포 후 수동확인 12항 — 2026-07-31 (v9.82~84 3트랙 통합, 독립 리뷰어 처방)

> 자동 테스트가 못 보는 것들. 배포·▶ 실행 후 순서대로 확인. (괄호 = 실패 시 의미)

1. **profiles 열 지도 육안**: 1행에서 `DO1=랭킹보드HTML · DP1=출퇴근HTML · DQ1=결석신고HTML · DR/DS=빈칸(의도적 갭) · DT1=상담취향 … DX1=페이스라인`. 어긋나면 즉시 중단.
2. **syncProfiles ▶ 후 DT124~DX128**: 실학생 행에 값이 채워졌는지. 전부 빈칸 = 상담시트 헤더 이름 불일치(무경보 침묵 케이스).
3. **calcAll ▶ 후 DP120/DQ121**: 강사 행 DP에 카드·학부모 행 DQ에 카드·학생 행은 둘 다 빈칸. **교차 오염(학부모 행 DP에 값 등) = 슬라이스 사고 신호.**
4. **10분 스위프 왕복**: todayBoard_ ▶ 2회 연속 → DP/DQ 셀 수정 시각이 두 번째에 안 바뀌어야(바뀌면 재기입 루프).
5. **출퇴근 실물**: 강사 계정 출근 탭 → 10분 내 "근무 중"+출근 빅넘버 / 퇴근 탭 → "퇴근 완료"+근무시간 / 주간 스트립 오늘 칸 파란 ✓.
6. **결석 P-ACK**: 학부모 계정 폼 제출 → 10분 내 `✅ 접수됨 · Бүртгэгдсэн`(사유에 HTML 태그 노출 없이). 폼 "자녀"가 Choice(value=user_id)인지 + preflightGlide `결석 사전신고 student_id 전건 유효`.
7. **스토어**: contents에서 `싱크 과잠` 행 소멸 확인(남아 있으면 이중 지급 경로). setupStore ▶ 후 translateContents ▶ → 앱 스토어 몽골어 라벨 복구 확인.
8. **포인트 표기**: 학생 홈 숙제 카드가 `+10P`면 표기 수리 미반영. 랭킹 넛지 "숙제 n번"이 실제 필요 횟수와 일치하는지.
9. **레이드**: raidMonday ▶ 후 raid HP(정원×계수) vs class_stats 반주간데미지 — 데미지 < HP×0.7이면 계수 재조정(실재적 기준 격파 불가 경고).
10. **노션**: syncToNotion_ 실행 전 동의 게이트(상담동의=**v18.6**) 확인. 실행 후 크루 DB '상담서술'(rich_text) 내용은 유호님이 직접 읽고 내보내도 되는지 판단.
11. **KPI**: 주간 리포트에 "채널별 등록/상담" 줄 — 안 뜨면 상담시트 '인지채널' 헤더 부재/범위 밖.
12. **migrateConsentV186** *(구 V185를 대체 — 미실행 상태에서 확장됨)*: 메뉴에 없음 — 편집기에서 함수 선택 ▶. 실행 전 동의 문구(수집·용도·보관·철회)와 몽골어 병기 여부를 유호님이 확정. 실행 후 **checkFormMapping ▶로 `음성동의`가 열 매핑으로 뜨는지** 확인 — blob으로 떨어지면 거부자를 코드가 못 읽는다. 상세 = `docs/면접시뮬_준비_v990.md`.
13. ~~**Glide 버튼 포인트 값 3종(+1)**~~ → **✅ 08-01 원격 실측으로 해소 + 전제 정정**. 코드·배포로 안 바뀌는 값이라 확인이 필요했던 항목인데, 라이브를 열어 보니 **①🌟 MVP 왕관 버튼의 `points`는 이미 `5`**(PT.왕관과 정합 — 바꿀 것 없음) **②⚡ 시냅스 왕관·💝 칭찬 4버튼·개별 숙제 버튼은 애초에 조립돼 있지 않다**(학생 상세의 Button Block에 액션이 `Add row to point_logs`(MVP) + `Show notification`(Secondary) 2개뿐). 즉 "3종을 5·2로 낮춰야 한다"는 전제 자체가 라이브와 달랐고, **포인트 인플레의 Glide 잔존분은 0**이다.
    - ~~⚠ 대신 **새 갭**: 조립가이드 §8-1이 요구하는 왕관 2종·칭찬 4종이 라이브에 없다~~ → **✅ 08-01 01:50 조립 완료**. 학생 상세(반 › Details) Button Block에 6개 전량: 🌟 MVP 왕관·⚡ 시냅스 왕관(각 points **5**, reason `오늘의 MVP`/`오늘의 시냅스`) + 🗣 발음↑·🔥 열정·🤝 친구도움·🎯 집중력(각 points **2**, reason `칭찬·발음↑`/`칭찬·열정`/`칭찬·친구도움`/`칭찬·집중력`). 공통 = Table `point_logs` · student_id ← `user_id`(탭한 학생) · given_by ← `User / 이름` · 블록 전체 `Show component when role is teacher`. 6개 값 개별 재확인 완료.
    - **조립 중 잡은 라이브 결함 2건**(둘 다 이 항목을 열지 않았으면 못 봤다):
      ① **Visibility 비교값이 빈칸**이었다 — `role is` 까지만 설정되고 값이 없어 조건이 누구에게도 참이 아니었다. **강사에게도 버튼이 전부 안 보이던 상태.** `teacher` 입력으로 수리, 강사 프리뷰 노출 / 학생 프리뷰 소멸 양쪽 실측.
      ② 같은 화면에 **Glide 기본 더미 Button Block**이 조건 없이 남아 있었다(Primary = `Open link https://www.glideapps.com`). 학생이 누르면 앱 밖으로 나간다 → 삭제.
      추가로 버튼이 6개가 되며 Width `Wide`에서 라벨이 잘려 이모지만 보였다 → `Auto`로 전환(3줄로 접히며 전부 읽힘).
    - 남은 확인 1: 왕관 1회 지급 후 point_logs 행이 실제로 `+5`인지(첫 실사용 때).
14. **createInterviewLogForm** *(v9.90 신설)*: ▶ 1회 → 결과 메일의 배포 링크를 졸업생 그룹·상담 자리에 뿌린다. 주간 워치독의 `면접 기록 회수: N건`이 곧 VR 면접 시뮬 개발 준비도. 두 번 눌러도 안전(살아 있는 폼은 재생성하지 않음). 상세 = `docs/면접시뮬_준비_v990.md`.
