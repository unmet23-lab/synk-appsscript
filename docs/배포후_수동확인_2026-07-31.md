# 배포 후 수동확인 12항 — 2026-07-31 (v9.82~84 3트랙 통합, 독립 리뷰어 처방)

> 자동 테스트가 못 보는 것들. 배포·▶ 실행 후 순서대로 확인. (괄호 = 실패 시 의미)

1. **profiles 열 지도 육안**: 1행에서 `DO1=랭킹보드HTML · DP1=출퇴근HTML · DQ1=결석신고HTML · DR/DS=빈칸(의도적 갭) · DT1=상담취향 … DX1=페이스라인`. 어긋나면 즉시 중단.
2. **syncProfiles ▶ 후 DT124~DX128**: 실학생 행에 값이 채워졌는지. 전부 빈칸 = 상담시트 헤더 이름 불일치(무경보 침묵 케이스).
3. **calcAll ▶ 후 DP120/DQ121**: 강사 행 DP에 카드·학부모 행 DQ에 카드·학생 행은 둘 다 빈칸. **교차 오염(학부모 행 DP에 값 등) = 슬라이스 사고 신호.**
4. **10분 스위프 왕복**: todayBoard_ ▶ 2회 연속 → DP/DQ 셀 수정 시각이 두 번째에 안 바뀌어야(바뀌면 재기입 루프).
5. **출퇴근 실물**: 강사 계정 출근 탭 → 10분 내 "근무 중"+출근 빅넘버 / 퇴근 탭 → "퇴근 완료"+근무시간 / 주간 스트립 오늘 칸 파란 ✓.
6. **결석 P-ACK**: 학부모 계정 폼 제출 → 10분 내 `✅ 접수됨 · Бүртгэгдсэн`(사유에 HTML 태그 노출 없이). 폼 "자녀"가 Choice(value=user_id)인지 + preflightGlide `결석 사전신고 student_id 전건 유효`.
7. **스토어**: contents에서 `싱크 과잠` 행 소멸 확인(남아 있으면 이중 지급 경로). setupStore ▶ 후 translateContents ▶ → 앱 스토어 몽골어 라벨 복구 확인.
8. **포인트 표기**: 학생 홈 숙제 카드가 `+10P`면 표기 수리 미반영. 랭킹 넛지 "숙제 n번"이 실제 필요 횟수와 일치하는지.
9. **레이드**: raidMonday ▶ 후 raid HP(정원×계수) vs class_stats 반주간데미지 — 데미지 < HP×0.7이면 계수 재조정(실재적 기준 격파 불가 경고).
10. **노션**: syncToNotion_ 실행 전 동의 게이트(상담동의=v18.5) 확인. 실행 후 크루 DB '상담서술'(rich_text) 내용은 유호님이 직접 읽고 내보내도 되는지 판단.
11. **KPI**: 주간 리포트에 "채널별 등록/상담" 줄 — 안 뜨면 상담시트 '인지채널' 헤더 부재/범위 밖.
12. **migrateConsentV185**: 메뉴에 없음 — 편집기에서 함수 선택 ▶. 실행 전 동의 문구(수집·용도·보관·철회)와 몽골어 병기 여부를 유호님이 확정.
13. **Glide 버튼 포인트 값 3종(+1)** — 코드·배포로는 절대 안 바뀌는 값(Glide 액션 안에만 있음). 강사 학생 상세에서 **🌟 MVP·⚡ 시냅스 버튼 points가 5**, **💝 칭찬 4버튼이 2**, (쓰면) 개별 숙제 버튼 5인지 확인 → **왕관 1회 지급 후 point_logs 행이 +5인지 실측**. 안 바꾸면 이 세 지점만 인플레가 살아남아 왕관 1개=숙제 2회분(설계 의도 1회분)이 되고 월 소득이 시뮬 상한(310P)을 넘는다. 값·사유 = 조립가이드 §8-1.
