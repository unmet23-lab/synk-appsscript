# 학습 데이터 수집층 — 실행지

> **v9.139 · 2026-08-03 라이브.** **4단계 로드맵의 ①**(개원~1년 · 몽골 내 발음·학습 데이터 수집) — 여기 쌓이는 것이 **③**(2년 · 한국어 회화·TOPIK 학습 앱)의 재료다. 구 「개원 후 2년 축적 → AI 회화 앱」은 폐기가 아니라 ①~③으로 늘어난 것(로드맵 정본 = `docs/SYNK_CONTEXT.md` 「제품 축 — 4단계 로드맵」).
> 이 문서가 정본. 판정·근거는 memory `data-accumulation-2year-2026-08-03`.
> ⚠ **순서 뒤집기(08-03 유호 확정)**: 회화 앱은 2년 뒤가 아니라 **지금 착수됐다**(`C:\Users\q1212\Documents\SYNK-talk` · memory `platform-rebuild-decision-2026-08-03`). 이 수집층은 그대로 유효 — 오류태그 23종을 SYNK-talk이 그대로 이어받았고, talk_log가 회화 앱 v0의 씨앗이다. 동의 문구 현행 = **v18.9**(음성·AI 학습 필수 동의 · 몽골어 검수 발주 진행 중).

---

## 0. 왜 지금인가 — 급한 것과 안 급한 것

| 항목 | 소급 가능? | 기한 |
|---|---|---|
| **동의 문구 범위** | ❌ 절대 불가 | **첫 실학생 서명 전** |
| **퀴즈 「무엇을 골랐나」** | ❌ 절대 불가 | 매일 지나간다 |
| **숙제 문항 연결·재작성** | ❌ 절대 불가 | 매 제출마다 지나간다 |
| 첨삭 오류 태그 | ✅ 가능 (원문이 남음) | 급하지 않음 |
| 음성 전사 | ✅ 가능 (오디오가 남음) | 급하지 않음 |

**코드는 전부 라이브에 올라갔지만, 폼을 만들기 전까지는 아무것도 쌓이지 않습니다.**

---

## 1. 유호님이 누를 것 — 시트 메뉴 (5분)

Google 시트를 열면 상단에 **`SYNK`** 메뉴가 있습니다. 아래를 **위에서 아래 순서로** 누르세요.

| # | 메뉴 항목 | 무슨 일이 일어나나 | 다시 눌러도 되나 |
|---|---|---|---|
| 1 | `🧠 퀴즈 응답 폼 만들기(수집 1단계)` | 「SYNK 오늘의 퀴즈」 폼 생성 + `quiz_log` 시트 | ✅ 이미 있으면 그냥 넘어감 |
| 2 | `📝 숙제 폼에 수집 문항 넣기(수집 2단계)` | 기존 숙제 폼에 문항 2개 추가(숙제ID·재작성원본) | ✅ 멱등 |
| 3 | `🗣 한국어 대화 폼 만들기(수집 3단계)` | 「SYNK 한국어로 말 걸기」 폼 + `talk_log` 시트 | ✅ 멱등 |
| 4 | `🔄 전체 재계산` (calcAll) | 학생별 폼 링크가 profiles에 채워짐 | ✅ |
| 5 | `📊 수집 커버리지 보기` | 지금 무엇이 쌓였는지 리포트(읽기 전용) | ✅ 언제든 |

> ⚠️ **2번은 기존 숙제 폼을 고칩니다.** 이미 배포된 링크는 죽지 않습니다(두 문항 모두 선택 응답).
> ⚠️ 3번 대화 폼은 `CLAUDE_API_KEY`가 있어야 답장이 만들어집니다. 없으면 학생 문장만 쌓입니다(그것도 자산입니다).

---

## 2. Glide 조립 — 버튼 3개 (약 10클릭)

`calcAll` 실행 후 `profiles`에 새 열이 생깁니다.

| 열 | 이름 | 놓을 곳 | 컴포넌트 |
|---|---|---|---|
| **DS123** | `퀴즈폼URL` | 학생 홈 — 퀴즈 카드 **바로 아래** | Button · Action: **Open link** · Link: `퀴즈폼URL` |
| **DZ130** | `대화폼URL` | 학생 홈 | Button · Action: **Open link** · Link: `대화폼URL` |
| `hw_feedback` 시트 **다시쓰기URL** 열 | — | 첨삭 카드 상세 | Button · Action: **Open link** · Link: `다시쓰기URL` |

**클릭 순서 (퀴즈 버튼 예시)**
1. Glide 편집기 → 왼쪽 **Layout** → 학생 홈 화면 선택
2. 퀴즈 카드 컴포넌트를 클릭 → 오른쪽 패널 아래 **＋ Add component**
3. **Button** 선택
4. Title 칸에 `✍️ 답하기` 입력
5. **Action** → `Open link` 선택
6. **Link** → 오른쪽 데이터 아이콘 → `퀴즈폼URL` 선택
7. 대화 버튼은 3~6을 반복하되 Title `🗣 한국어로 말 걸기` · Link `대화폼URL`

> 💡 버튼 라벨을 짧게 유지하세요 — 학생 홈은 이미 버튼 3종이 한 줄입니다(memory `hw-quiz-wording-2026-07-31`).

**확인**: 학생 계정으로 앱을 열어 버튼을 누르면 폼이 **학생ID·퀴즈ID가 이미 채워진 채** 열려야 합니다. 빈칸이면 `calcAll`을 한 번 더 누르세요.

---

## 3. 동의 문구 — 가장 급한 것 ⚠️

### 3-1. 무엇이 바뀌었나

용도에 **「SYNK LAB과 그 관계·승계 사업체가 만드는 한국어 학습 서비스(앱·AI 학습 도구)의 개발과 개선」**을 넣었습니다.
넓힌 대가로 **「학습 서비스 개발에 쓸 때는 이름·연락처를 지운 형태로만 사용합니다」**를 함께 약속합니다.

**왜**: 구 문구는 용도가 학원 운영으로만 한정돼 있고 '제3자 제공 없음'이 붙어 있어서, 회화 앱을 SYNK LAB이 아닌 **별도 제품·법인으로 내는 순간 2년치를 통째로 못 씁니다.** 동의는 소급이 안 됩니다.

### 3-2. 라이브 폼에 반영하기

Apps Script 편집기에서 **`migrateConsentV186`** 함수를 ▶ 실행하세요(시트 메뉴에는 없습니다 — 상담 폼을 직접 고치는 함수라 의도적으로 편집기 전용).

> ⚠️ **이걸 실행하기 전까지 노션 상담서술 이관이 멈춥니다.** 일부러 그렇게 만들었습니다 — 구 문장으로 받은 동의를 새 용도로 쓰지 않기 위해서입니다. 실행하면 다시 열립니다.

### 3-3. 몽골어 병기 — 유호님 결정 대기 ⏳

번역 **초안이 코드에 들어가 있지만 아직 폼에 실리지 않습니다.**

- 초안 위치: `엔진_폼리포트.js`의 `CONSENT_HELP_A_MN` · `CONSENT_HELP_B_MN`
- 검수가 끝나면 같은 파일의 **`CONSENT_MN_APPROVED = false`를 `true`로** 바꾸고 배포 → `migrateConsentV186` ▶
- 검수 시 특히 볼 단어 3개: `залгамжлагч`(승계 사업체) · `нэр устгасан`(비식별) · `цуцлах`(철회)

**틀린 번역으로 받은 동의는 없느니만 못합니다.** 원어민 + 가능하면 법률 검토를 거치세요.

---

## 4. 무엇이 쌓이는가

| 시트 | 내용 | 회화 앱에서의 쓸모 |
|---|---|---|
| `hw_feedback` | 제출문 · **고친문장** · 오류태그 · **재작성원본** | 🟢 원문↔교정 병렬쌍 + 학습이 일어났는지 |
| `quiz_log` | 퀴즈ID · **고른 답** · 정답여부 · **확신도** | 🟢 오답의 내용(무엇과 헷갈리는가) |
| `talk_log` | 학생문 · AI답 · 턴 번호 | 🟢 **다회차 대화** — 다른 어디서도 안 나옴 |
| `voice_log` | 파일 · 전사 | 🟡 발음 오류(전사는 이미 붙어 있음) |

---

## 5. 6개월마다 할 일 (중요)

`📊 수집 커버리지 보기`를 눌러 **「아직 한 건도 못 잡은 오류 유형」** 줄을 보세요.

데이터의 값은 총량이 아니라 **유형 커버리지**입니다. 200명이 같은 조사 실수만 반복하면 3만 건이어도 유형 30개짜리 데이터입니다. 빈 유형이 보이면 **그 문법을 겨냥한 숙제·퀴즈를 다음 시즌에 넣으세요** — 안 나오는 오류는 안 쌓입니다.

그리고 그때까지 모인 것으로 **「몽골 학생이 가장 많이 틀리는 한국어 50」**을 실제로 뽑아보세요. 그 자체가 마케팅 자산·교재 개정 근거이면서, 무엇보다 **데이터 품질 검증**입니다 — 데이터의 결함은 쌓을 때가 아니라 **쓸 때** 드러납니다. 2년 뒤 처음 열어봤는데 형태가 틀렸으면 되돌릴 방법이 없습니다.

---

## 6. 아직 안 된 것 (정직한 한계)

- **실시간 음성 회화는 이 플랫폼에서 불가능합니다.** 3번의 대화 기능은 **비동기 펜팔**입니다(하루 1턴, 밤에 답장 생성). 웹앱 경로는 보안상 막혀 있고(v9.138), Glide는 월 500 update 상한이 있습니다. 실시간은 로드맵 **③의 별도 앱**(한국어 회화·TOPIK 학습 앱 — 착수본 SYNK-talk) 몫입니다.

  > 🔴 **나중에 회화 봇에 자체 화면을 붙일 때 반드시 읽을 것** (두뇌 트랙 08-03 통지).
  > 위험한 것은 `doGet`이 아니라 **HtmlService 페이지가 익명에게 한 번이라도 나가는 것**입니다.
  > 그 페이지를 받은 사람은 `google.script.run`으로 **이 프로젝트의 밑줄 없는 전역 함수 전부(실측 171개)**를
  > 원장 권한으로 부를 수 있습니다 — 학생 리포트카드 공개·학부모 메일 발송·음성 삭제가 전부 그 위에 있습니다.
  > **노출 단위가 「내 함수」가 아니라 「프로젝트 전체」**입니다. 지금의 폼/시트 경유 설계는 이 브릿지를 만들지 않아 안전합니다.
  > 화면을 붙이려면 **별도 Apps Script 프로젝트 분리를 먼저** 검토하세요(경위·되살리는 조건 3안 = `_보류_두뇌_웹화면.js` 머리말).
  > 라이브에 안 올릴 파일은 **`_` 접두 + 머리말에 「라이브 미배포」** 표기 — `safety.test.js`가 그 표기가 없으면 실수로 보고 막습니다.
- **퀴즈 응답 소요 시간(초)은 못 잽니다.** 폼으로는 열람 시각을 알 수 없어 '확신도' 3택으로 대체했습니다.
- **포인트 지급은 넣지 않았습니다.** 참여 유인이 필요하지만 포인트 경제는 유호님 확정 영역이라(지급 ÷2, memory `point-economy-rebalance-2026-07-31`) 임의로 건드리지 않았습니다. → **유호님 결정 필요.**
