# 엔진 전층 심문 — Fable 5 · 2026-08-22 (유호 지시 「fable 5로 이미 선 엔진 설계 심문해줘」)

> **성격**: 상태기반 처분표 회차가 **아니다** — 그 게이트(전건 처분)는 구현 착수용이었고 구현은 08-22 종결됐다.
> 이것은 **문서 사이·트랙 사이·활성 날**을 겨눈 전층 심문의 발견 원자료다. 처분 절차를 새로 세우지 않는다
> (CLAUDE.md 「고치고 끝낸다」) — 각 건은 ①수리 ②인계 ③유호 판정 ④기각 중 하나로 소화되면 그 줄을 지운다.
> **판정: 설계 골격은 이번에도 섰다. 갈아엎을 근거 0.** 격차 세 갈래 = ㉮활성 날의 미검증 경로 ㉯몸을 못 따라온 장부 ㉰트랙 사이 이음새.
> 조사 = 서브에이전트 2(talk 27건 · appsscript 22건) + 본세션 직접(정본 4벌 통독 · 급소 7건 실물 재확인). 라벨 = 각 건 끝(실측/추정).
> 경로 축약: `talk\` = `C:\Users\q1212\Documents\SYNK-talk\`.

## Ⅰ. 활성 날(신앱 생성 트랙 켜는 날) 사고 예약 — talk 코드 · 엔진 레인 몫

- **T1 🔴 B3 재호출이 자기 게이트에 죽는다** — `talk\supabase\functions\deliver-one\index.ts:300-307` 재호출 body `'{}'`(맥락 없음)인데 활성 뒤 deliver 는 맥락 누락 400(`deliver\index.ts:482-485`). cron 등록만 `맥락=배치`(활성조각:98). pg_net 발사-망각이라 「성공」 보고 뒤 실제 400. 배치 도중 사망 → 자가치유 0 · B4 재적재 영영 안 돎. [실측 — 본세션 재확인]
- **T2 🔴 학습자상태() 실패의 처분이 경로마다 정반대** — 현행 `한명()` 은 삼키고 배달(`상태없음` 표식), 생성 `학생조립()` catch 는 학생 전체를 `load_error` 로 접는다(`생성모드.ts:135-140`) → draft 없음 → 마감 스윕 착지 대상 없음 → 그날 그 학생 생성도 폴백도 0. [경로 실측 / 입력 실재 추정]
- **T3 vault 2항목('functions_base_url'·'service_role_key') 실재를 어느 층도 안 잰다** — 활성조각은 「이미 있어야 한다」 위임, cron.schedule 은 등록 때 평가 안 함, 왕복 A8 은 command 문자열 대조뿐. 미설정 채 활성 = 매밤 조용한 실패(net._http_response 에만). [부재 실측 / 운영 vault 상태 안 재봄]
- **T4 공갈 모델 장치가 코드에 없다 + 걷기를 잊으면 활성 날 전원 폴백** — `생성왕복시험.js:823-834` 주석이 사람 절차에 위임. 그리고 그날 **감시 7항은 전부 초록**(T6). [실측]
- **T5 워커 «성공» 경로를 어떤 시험도 통과 안 한다** — B3·C7 성공은 RPC 직접 주입, C4 는 성공 0 을 단언. §12-3 자인 「#6 뒤 첫 실행 몫」= 첫 실행이 곧 첫 시험. [시험 범위 실측]
- **T6 감시 7항 = 전부 큐 위생 · 산출 질 0항** — 전원 폴백의 날(키 만료·벤더 장애) 큐는 닫히므로 ①~⑥ 초록. 강등률은 응답 봉투·로그에만. [실측]
- **T7 밤당 생성 상한 = 3명×36회 = 108명** — 워커학생상한 3(§12-20 실측 후 걷기 예약) × generate-worker `*/10 16-21`. 재적 108 초과분은 매일 06:00 마감폴백인데 큐가 닫혀 감시 초록. 지금은 여유(정원 16). [수치 실측 / 산술 추정]
- **T8 새벽 앱 열기가 생성을 잡아먹는다(채택된 트레이드의 뒷면)** — 생성 창(00:05~06:00) 중 `/tasks` 조회가 «대기» job 을 구제 폴백으로 착지(왕복시험이 «현행 정상»으로 못박음). 새벽형 학생은 생성 살아 있어도 매일 강등 폴백. [기제 실측 / 설계 §3-1 채택 사항 — 재판정은 유호 자리]
- **T9 tasks 반쪽 DB catch → '없음' → 구앱 안내까지 침묵** — `tasks\index.ts:330-333`; 폴백 날은 `제출재료:null`이라 그날 발화가 서버에 안 남는다. [코드 실측 / 발생 추정]
- **T10 c12 신앱 「생성중」 어댑터·문구 미착지(§12-11 뒷단 자인)** — 활성 날 신앱이 구앱보다 나쁜 경험을 받을 수 있는 창. [실측 / 출시 순서 추정]
- **T11 빈 배정(배정 행만·submissions 0)은 멱등이 수리를 막는다** — deliver:950-955 자인 · tasks 조인이 그 행을 못 봄 · 구제는 duplicate 로 접힘. [실측(자인) / 빈도 추정(드묾)]
- **T12 동의 철회 잔존 job → 감시 ⑤ 매일 적색 → 늑대소년(F103)** — 왕복 B5 가 「영영 못 닫힘」을 스스로 단언하는데 감시 ⑤ 술어가 정확히 그 행을 센다. [기제 실측]

## Ⅱ. 거짓 초록 — 장부·게이트가 실물을 못 따라온다

- **G1 🔴 행동층 장부 칸이 낡은 거짓** — `talk\lib\이벤트검증.js:643` 「소비자 0줄」인데 `lib\과제요약.js`(121줄)+`생성모드.ts:97-104` 실재. 자기 처방(착지 커밋에서 상한 2→1)을 안 지킴 · `tests\이벤트검증.test.js:992` 가 `===2` 를 못박아 초록. [실측]
- **G2 🔴 §16-1 #6 게이트(A7)가 「파일 존재」만 잰다** — `생성왕복시험.js:288-292` `existsSync` → 미채점 79 인 채 4/4 · 「운영 붓기 차단」 문구 소멸. 진실은 채점 CLI 게이트 ③에만. [실측]
- **G3 §8-B 상태가 결과.md 에 0줄** — 80회 평가($0.28 · 미채점 79 + 검문탈락 0점 1)는 커밋 메시지·JSON 에만. 결과.md 는 correct 전용 장부. 곁: correct r2 61건 크레딧 전멸 2연 · r3 미실행 = 3연 다수결이 1.5회. [실측]
- **G4 「Ⅱ 닫혔다」의 21/21 은 문서 «내부» 낱말 대조다** — 설계문값대조·폐기어휘의 입력이 설계문 문자열 하나. 코드·talk 을 여는 줄 0. 설계문이 코드 대비 통째로 낡아도 초록. §12 상시 기계의 실체는 talk 커밋 계열인데 이 저장소 CI 는 형제를 못 본다(계약동기화.js:16-17 자인). [실측 — 트랙.md 「Ⅱ 닫혔다」 표현의 증거력 한정]
- **G7 복습스케줄 = 소비자 0 인 새 파생층인데 어느 래칫도 못 센다** — require 실측 tests 뿐. 행동층 래칫 커버리지 밖(사건도 부품도 아님). G1 과 같은 병이 장부 밖에서 한 번 더. [소비자 0 실측]
- **G8 엔진_두뇌.js(419줄) = 라이브 탑재 + 테스트 분모 밖** — .claspignore `!엔진_*` 로 올라가는데 ENGINE_FILES 7종에 빠져 전수 검사 밖. 호출자는 철회된 웹화면+수동 메뉴뿐 · AI 키 취급 코드 포함. [실측]
- **G9 mastery 판정관 4종이 조건부 트리거(목소리폼URL 헤더) 아래인데 도달 장부는 ✅** — 미개통이면 실행 0회 채 「닿았다」 상주. 라이브 트리거 실설치는 이 저장소에서 못 잼. [배선 실측 / 라이브 안 재봄]
- **G10 경로 B(회사 훈련) = 생산자 0 · 유일하게 끊긴 칸(⑥ data_use.granted)을 재는 자가 없다 · [자동] 4단의 실행 주체 무명** — 엔진도달:34·:150·:78-81 자인. [문서 자인 실측]
- **G11 하루 1회 질문 예산의 장부 = 화면층 한 줄** — `talk\lib\성향확인.js:59` `이력.오늘답함` 뿐. DB 유일 제약 0 · events 층 상한 0 · 멱등키는 답마다 새 uuid. 두 기기 = 같은 날 중복 표본. [실측 — 본세션 재확인]
- **G12 그 「하루」의 재료가 앱 시계(occurred_at)** — C0 「기기 시계는 못 믿는다」와 정면. 시계 앞선 기기 → 오늘 또 묻고 내일 억제. [코드 실측]
- **G13 부정 재노출 금지 키 형식이 두 곳(SQL 조립 ↔ lib 리터럴)** — 새 축이 서는 날 갈릴 수 있는 모양. [실측 / 갈릴 확률 추정]

## Ⅲ. 트랙 사이 이음새 — 설계 층 발견 (본세션)

- **S1 🔴 FSRS rating 재료의 구조 비대칭 — 태그 카드는 «맞았다» 신호를 거의 못 받는다.** 교정은 틀린 것만 태그를 내고(맞은 문장 = 그 카드에 무사건), 퀴즈는 정오·확신도가 있으나 태그 매핑 0. ⇒ 유일한 자연 발생 rating ≈ Again/Hard → stability 영구 저공 → **마스터한 카드가 due 목록에 영원히 남는다**(「맞는 것을 또 묻지 않는다」의 정반대 · due 목록은 늘 수만 있다). 파생 명세(§8 남는 일)가 풀어야 할 급소이고, 자연스러운 다리 = **재출제 문항에 태그를 스탬프**(그 답의 정오가 그 카드의 성공 리뷰가 된다). ⚠병행-후-교체 게이트가 실전 노출은 막고 있다. Ⅲ⑦(숙제 확신도)은 rating 의 «질»을 넓힐 뿐 성공 신호의 «존재»는 못 연다. [사실관계 실측(매핑 0·태그 발생 조건) / 귀결 추정]
- **S2 계산기의 정오 미상 처리 = Again** — `talk\lib\복습스케줄.js:60` `if (!정답)`. quiz.answered 에 정오가 없는(스냅샷 파생) 현 계약에서 파생층이 「대조 불가」를 null 로 넘기면 매번 lapse → 과재노출 + lapses 오염. 파생 명세에 「미상은 리뷰가 아니다(버림)」 명문 필요. [코드 실측 — 본세션 재확인]
- **S3 가둠이 due 만 자르고 S·D 는 그대로 다음 next 에 들어간다** — 지금 ts-fsrs 5.4.1 은 last_review 기준이라 무해하나 판올림 뒤 조용히 어긋날 수 있고 결정성 회귀는 초록 유지. [추정 — 본세션 재확인(코드)]
- **S4 FSRS due 가 저장소 경계를 건너는 통로 0줄** — 계산기 = talk lib · 1순위 소비자 = 이 저장소 `엔진_콘텐츠AI.js` aiStudioBatch_ ①(14일 창). 교체일의 다리(전송·형식·주체)가 설계 어디에도 없음 → 즉석 발명 = 두 정본 위험. [소재 실측 / 교체일 모양 추정]
- **S5 문법 급수 계약의 탐지층 소멸** — tests/계약·문법급수 08-19 삭제(e75fc7fc) · 남은 것 = 이 저장소 pre-commit 훅뿐(로컬 한정 · talk 커밋은 안 지남). 두 저장소가 각자 초록으로 표류 가능 = 「Lv3 에 1급 문항」 무증상 재현 경로. [삭제·훅 실측 / 표류 창 추정]
- **S6 실력 좌표계 이원화(알려짐 · 맥락 보강)** — 태그 23(AS) vs skills 30(talk) 매핑 0 은 E2 유호 자리로 봉인돼 있으나, S1·S4·G5 가 전부 이 갈림 위에 서 있다 — E2 판정이 열리는 날 함께 닫히는 묶음. [실측(자인)]
- **S7 명부 스윕 시차·급수 칸 공백** — profiles→learners 아침 07시 1회 · A:H 에 급수 0. 반 변경 당일 두 층이 하루 갈림. talk 의 급수 읽기 원천은 안 재봄. [스윕 실측 / talk 측 안 재봄]

## Ⅳ. 문서 낡음 — 기계가 못 잡는 층 (이 저장소 · 본세션 몫)

- ~~D1·D2·D3~~ ✅ **08-22 본세션 수리 완료** — 코어엔진 §5 시점 정정 · 철학 A-1 ㉡ 3칸 현행화 · 복습 문서 3벌 정합(제목 v0.2·머리말·문서_지도) · G5(도달 분모)·G6(성과회수 충돌)도 같은 커밋. ③운영·④완결 절 신설은 E1 착수 날 몫으로 코어엔진에 명문.
- ~~D4~~ ✅ **문서 몫 집행(08-23 · 유호 확정 「전부 진행해」)** — 트랙.md 규율에 `④모✗닿✗늘✗` 표기 신설 + 현행 4트랙(Ⅰ①·Ⅲ⑤⑥⑦) 적용. **잔여 = talk 장부 스키마의 「늘었나」 칸**(이벤트검증.js 값 스키마 2칸 → 3칸 — 엔진 레인 몫으로 인계).
- ~~D5~~ ✅ **집행(08-23)** — 단독 방패 감사(실측: 대체·v5.2 = 단독 방패 0줄 · 섞여 = 4946줄 하나·과거형)로 이스케이프를 좁히고(값대조: 정정→정정했·섞여→섞여 있었·갈려→갈려 있었 / 폐기어휘: 대체·v5.2 제거) 남용 방향 픽스처 2벌 추가 — 23/23 초록·실문서 그대로 초록.

## Ⅴ. 소화 분배 (제안 — 유호 확정 전까지 지시가 아니다)

| 몫 | 건 |
|---|---|
| **엔진 레인(talk)** | T1~T12 · G1·G7·G11~13 · S2·S3 (S1·S4 는 파생 명세 작업의 입력) |
| **이 저장소 문서 수리** | ✅**전건 집행** — G5·G6·D1~D3(08-22) + D4·D5(08-23 · 유호 확정 「전부 진행해」 = 결정.md 등재). D4 의 talk 장부 몫만 엔진 레인으로 |
| **유호님 몫(기존 그대로 — 새 요청 아님)** | §8-B 사람 채점 79건 · correct r3 크레딧(돈) · T8 트레이드 재판정 여부 |
| **판정 열리는 날 함께** | S5·S6(E2 태그↔skill) · G4(형제 CI — github-server-gate 트랙과 한 자리) |
