# SYNK 펠트 그래픽 엔진 — 설계 정본 (v0.1 · 2026-08-15)

> **유호 지시 08-15**: 펠트는 SYNK «회사 전용 재질»이다. 브랜드킷 등재를 넘어, 이 재질을 기능적으로
> 어느 회사보다 잘 구현하기 위한 그래픽 엔진을 세팅해 나간다. 앞으로 디자인할 것이 많다.
> 값의 정본 = `docs/디자인_토큰.json` 「재질」 축 · 실측 근거 = memory `mascot-felt-liveness` · `image-pipeline-api-switch`.

## 0. 한 문장

어느 지면(2D 컷 · 3D 모션 · UI 오브젝트 · 인쇄 · 영상)에서든 SYNK 펠트를
**같은 색(램프) · 같은 결(사진 섬유) · 같은 빛(조명 레시피)**으로 결정론 재현하는 **사진 기반 재질 시스템**.
게임 엔진이 아니라 «재질 파이프라인»이다 — 정본(값) + 통로(도구) + 검증(자)의 세 다리로 선다.

**스타일 좌표**(08-15 유호 방향 확인 · 근거=2026 「3D 하이퍼리얼」 조류 대조): 실물 참조·조명·오브젝트 중심은
조류와 같은 축이되, CGI 완벽주의는 흡수하지 않는다 — **수공 리얼리즘**(잔섬유·크림 스티치·수공의 미세 불균일)이
우리 구분자다. 실측 근거 = ㉡네이티브 재굽기 「잔섬유가 살아 결이 더 진짜」.

## 1. 헌법 — 실측이 세운 제약 (재론 금지 · 근거는 각 메모리)

1. **결은 사진 픽셀에서만.** 프로시저럴 노이즈는 매끈한 왁스로 읽혀 기각됐다(probe2 08-15).
   엔진의 섬유는 전부 정본 사진의 울 패치에서 잘라 온다 — 색·결이 자동 일치한다.
2. **색은 낱말이 아니라 참조·연산.** 프롬프트에 색 이름·hex 를 적으면 중간값에 착지한다(문장 ΔH 16 → 참조 ΔH 1.8).
   색 갈이는 재염색(선형광 채널 게인 · `tools/펠트색갈이.py`) 또는 참조 이미지가 진다.
3. **킷 hex 정확 일치는 불가능이 정상.** 결 있는 표면의 평균은 색역 끝(R 255)에 못 닿는다.
   코어색 숫자를 인용할 땐 **잰 자 이름**을 함께 적는다(F456 — 자간 ΔE 3.62 실측).
4. **생성 모델에 마스코트 통째 ✗ · 생성 텍스처 기각.** SynthID·재해석(코어 H8°→21°·섬유 소멸) 실측.
   생성은 «참조를 받는 굽기»(nano-banana 류)와 «형상만»(Hunyuan3D shape)으로 제한한다.
5. **조명 레시피 = 사진에 구워진 조명 보존.** 재조명 최소(키 120 · 필 35) + 방출 0.55 이 색을 진다.

## 2. 층 구조 — 있는 것 / 지을 것

| 층 | 무엇 | 상태 |
|---|---|---|
| **L0 재질 정본** | `디자인_토큰.json` 「재질」 축 — 헌법·정본사진·셰이딩·조명·울패치 값. 색은 마스코트 램프 «참조»(hex 중복 금지) | ✅ 08-15 등재 |
| **L1 2D 통로** | 정본 사진 + `펠트색갈이.py`(재염색 0원) + `lib/이미지굽기.js`(참조 굽기) | ✅ |
| **L2 3D 통로** | `마스코트인형리깅.py` make_material — 투영(마스코트 전용) + 울패치 하이브리드(공용 가능) | ✅ 마스코트 전용 |
| **L2-모듈** | make_material 에서 **펠트 셰이더 공용 모듈 추출**(`tools/lib/펠트재질.py`) — 임의 오브젝트가 입는다. 공용부=울패치 섬유+셰이딩+조명 / 마스코트 전용부=사진 투영 | ▶ 지을 것 |
| **L2b 패치 라이브러리** | 중립 펠트 패치 + 재염색으로 임의 색 파생 — 새 오브젝트가 목표색 펠트를 0원으로 입음 | ▶ 지을 것 |
| **L3 검증층** | `브랜드렌더린트.js`(바닥 계산·의상 합집합) · `마스코트램프.py`(램프 자·--검증 재현) · `상태컷정합.py` | ✅ 부분 |
| **L4 적용층** | `재질굽기.js` 슬롯(2D) · 리깅 anim(3D) · 첫 비마스코트 실증 = 녹음 오브 · **펠트 3D 타이포**(히어로 글자 1점 — 본문 타이포 불가침·킷 색 참조) · **캠페인 오브**(촬영 대체 — 웹·SNS·인쇄 일관) | ▶ 실증 대기 |

## 3. 짓는 순서 (막힘과 함께)

1. ✅ 설계 정본 + L0 토큰 등재 (08-15 이 커밋)
2. **L2-모듈 추출** — ⛔ `마스코트인형리깅.py` 는 `177ef8bf` 트랙(anim2 좌턴 미착지) 선언 파일. 착지 후.
   추출 때 **셰이딩 값을 토큰에서 읽게 배선**한다 — 그 전까지 값 개정은 토큰↔코드 **두 자리 동시 이동**.
3. **L2b 패치 라이브러리** — 신규 파일이라 자유. 모듈과 한 벌이 자연스러움.
4. **첫 비마스코트 실증 = 녹음 오브 펠트 3D** — 죽은 `1cfa648d` 트랙의 「UI 오브젝트 재질 슬롯」을 여기서 흡수.
5. **텍스처·멀티뷰 층(뒤태 해결)** — ⏳유호(HF PRO $9/월) 또는 무료 몫(하루 ~300초) 배분.
   ¾ 컷을 멀티뷰 입력으로(`--좌/--우/--뒤` · 누끼 선행) — 모델이 옆면을 상상 대신 «본다».

## 4. 도전안 — 실시간 웹 펠트 (Three.js · 시기 보류)

앱 안에서 마스코트가 **실시간으로** 산다(교대 mp4 가 아니라 연속 관절 — 「살아있는 캐릭터」 상시 지시의 종착).
사진 베이크 텍스처를 넘기면 헌법 1과 충돌 없다. **시기 = Expo 앱 화면 작업이 열릴 때** —
지금은 오프라인 엔진이 자산 생산의 병목이고, 실시간은 소비 지면이 서야 잰다. 지금 짓지 않는다.

## 5. AI 슬롯 지도 — 「AI가 오르면 엔진도 오른다」의 배선 (유호 방향 확인 08-15)

원리: **Blender·자·정본 = 결정론 불변 층** — AI 곡선을 타는 것은 그 주변의 «생성 슬롯»이다.
불변 층이 있어야 새 모델을 같은 자로 심판할 수 있다(더 나으면 교체 · 동점=기존 — 정찰 게이트 ㉠ 문법).
조종사도 슬롯이다 — 이 엔진의 셰이더·리깅 코드는 전부 AI가 썼으므로, 상위 모델 출시 시
전층 재검토 상시 지시(08-10)의 대상에 **이 엔진 코드 층이 포함**된다.

| 슬롯 | 지금 | AI가 오르면 | 재대조 자 |
|---|---|---|---|
| 형상 | Hunyuan3D-2.1(HF 무료) | 3.x API $0.16/생성 · 멀티뷰(¾ 컷 입력) | 정점 품질·워터타이트·눈 돌기 기하 |
| 텍스처 | ❌기각(색 13° 이탈·섬유 소멸) | **새 모델 출시 = 재개봉 조건** | 코어 색상각 이탈·섬유·스티치 보존 |
| 2D 굽기 | Gemini 참조 굽기 ✅ | 모델 라인 상승 = 자동 동승(배선 완) | 코어 ΔE·짝 정합(상태컷정합) |
| 모션 | 손 리깅(결정론 키) | **분업**: AI는 키프레임 «숫자»만, 픽셀은 Blender — 워터마크·재해석 원리적 0 | 루프 이음매 0·유호 눈검수 |
| 검수 | 결정론 자(ΔE·정합) | ▶VLM 눈검수 프리패스 — 고스트·심 등 «지각» 결함 앞단 필터(0원) | 유호 눈검수가 최종(대체 ✗) |

안 다는 것(실측 종결 — 재제안 금지): 마스코트 통째 생성 비디오(SynthID·차단 기계층 0) ·
가시 워터마크 모델(그록) · 프로시저럴 텍스처 애드온류(§1 헌법 1 위반) · 이미지 모델 재벤치(재개봉 🚫).

## 6. 독보성 전략 — AI 가 올라도 «이월되는» 넷 (유호 방향 08-15 · 릴 대조)

AI 발전은 «생성»을 흔하게 만든다 — 독보성은 생성이 아니라 이월 자산에 쌓인다. 투자 우선순위의 근거:
1. **정체성(IP)** — 램프 5단·상태 의상·표정 규격. 「펠트 캐릭터」는 누구나, «이 캐릭터»는 우리만.
2. **판정하는 자** — 천 장을 뽑아도 한 브랜드로 유지하는 능력(색차·정합·회귀). 생성이 흔해질수록 희소해진다.
3. **레시피 장부** — 기각 실측의 축적(§1 헌법·probe 기록). 새 모델은 슬롯에 꽂고 게이트는 그대로 심판.
4. **관계 자산** — 게임 단계의 진짜 해자: 그래픽은 시선을 얻고, «학생을 아는 엔진+정든 캐릭터»가 남게 한다.

재질 계열 확장: 펠트(현실감·수공)=1호 · **초현실 렌더(밀도·대기 원근·단일 광원·스케일 대비)=2호 후보** —
같은 «정본+측정+조명 레시피» 구조로 규격화한다. 개봉 조건 = 게임·VR 트랙 착수(앱 우선 확정 불변).
원리 대조 실측 08-15: 초현실감의 축=디테일 밀도(자개 「밀도=광택」과 같은 원리) — AI 기능이 아니라 공예 원리라 안 낡는다.

### 6-1. 게임 대비 준비 규율 (유호 버킷리스트 08-15 「몇 년 뒤 온라인 RPG」 — 지금은 규율만, 공사 0)

설계 3원칙: ①**재파생 가능성** — 정본=원본(최고해상 사진·형상 GLB)+레시피(토큰 파라미터), 픽셀 결과물=파생.
미래 렌더러 출시 = 전 자산 재굽기로 무료 업그레이드(재염색 바이트 재현이 증명) ②**열린 표준에만 정본**(glTF·PNG·JSON —
전용 포맷 감금 금지) ③**모듈 경계**(재질≠캐릭터≠렌더러 — L2 모듈이 집행).
수집 4(이미 하는 일 + 보존 규율): ①실물 사진 세트 아카이브 — 의미 있는 장소·순간을 다각도 20~30장(사진=시간 안 타는
정본 · 미래 AI 의 3D 원료 · 0원) ②모션·표정 어휘(규격을 정본에 남긴다) ③학생 놀이 데이터 = 교육 RPG 게임디자인
실측(어떤 메커니즘이 발화를 낳는가 — 유일 데이터) ④확정·기각 장부 = 취향 코퍼스(AI 가 유호님을 증폭하게 하는 재료).
**두 통 규칙(08-15 · 유호 질문 「인터넷 스크린샷·영상도 되나」)**: **영감통**=무제한(남의 작품 포함 — 방향 판정 전용 ·
파이프라인 반입 0) / **정본통**=셋만 — 직접 촬영 · 권리 열린 소스(Polyhaven·Unsplash·NASA 류) · **우리가 생성해
소유한 컷**(마스코트가 증명 — 존재하지 않는 세계의 정본은 생성→승격으로 만든다). 🚫남의 작품 스크린샷을 정본통에 —
파생 전체가 오염된다. 환상 세계에서도 실사의 역할은 소멸 아니라 이동(배경→**재질 원료**: 바위·얼음·구름의 결).
몽골 자연(고비·홉스굴·테렐지·별하늘)=미개발 세계관 원료 — 학생의 땅이 게임의 세계가 되는 관계 자산 한 몸.
**데이터 원칙 이월(08-15 판정)**: 「관찰 사실 ≠ AI 추론」 분리(사건출처 스키마 강제·AIQ 도장)는 RPG 에 그대로 +
확장 3 — ㉠NPC 기억=사실 로그와 해석 분리(모델 업그레이드마다 같은 역사에서 재해석 = 재파생 원칙과 동일)
㉡NPC 발화 게이트=관찰 로그 근거 있는 것만(구조적 무환각 — 성취감 「없는 성공 금지」의 확장) ㉢AI 생성
콘텐츠 출처 도장(AIQ 의 게임판). 온라인에선 관찰 층=서버 권위 진실이라 이 분리는 선택이 아니라 전제.
하지 않는 것: 게임 엔진 구매·넷코드·게임 트랙 착수(앱 우선 불변).
💡도전안(기록만 · 몇 년 뒤 픽): RPG 를 따로 짓지 않고 **앱이 서서히 RPG 가 되는 경로** — 미니게임→모험 지도→월드.
유저·데이터·세계관 단절 0. 수집=숙제=미니게임 철학의 연장선.

## 7. 대가 — 틀릴 때의 모습

- **정본 사진이 바뀌면 전 층이 낡는다** (사진 기반의 대가). 정본 교체 = 램프 재실측 + 패치 재절단 + `--검증` 재통과가 한 벌.
- **울패치 타일의 벽은 뒤통수(180°)** — 가로 스미어 실측. 3-5 텍스처 층이 닫을 때까지 큰 턴은 좌 +75° / 우 -28° 안.
- **토큰↔코드 값 이중 상주** (3-2 배선 전까지) — 갈라지면 「맞는 얼굴로 틀린 값」이 된다. 닫을 것 = 3-2 배선 그 자체.
