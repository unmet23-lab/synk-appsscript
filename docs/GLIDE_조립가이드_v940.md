# SYNK Glide 조립 가이드 — v9.40 완전판 (전 기능 무누락)

> **이 문서가 조립의 유일한 정본입니다.** 구 `GLIDE_조립가이드_v939.md`를 대체합니다(그 문서 상단에 폐기 표기).
> 작성 근거 = **2026-07-19 라이브 재실측**(시트 38탭 전수 + 코드 v9.40 전수 정독). 이 문서의 모든 시트명·열이름은 실측값이며, §10에 **기능→화면 전수 매트릭스**가 있어 "빠진 기능"이 구조적으로 없습니다.

---

## 0. 왜 또 다시 쓰나 — 이번엔 무엇이 다른가

지난 조립(v939)이 또 부족했던 이유를 재실측으로 확인했습니다:

| # | 실측된 원인 | v9.40에서 해결 |
|---|---|---|
| 1 | **콘텐츠 절반이 시트에 없었음** — 숙제 0/210 · 연료 0/6 · 시즌 0/12 · 팁 0/30 · 문법 57/72. "setupXxx를 실행하라"는 경고만 있었고 그 수동 실행이 누락됨 | `preflightGlide`가 **부족분을 자동 설치** + 몽골어 큐레이션 340행 자동 재주입 |
| 2 | **월간 시트 5종이 아예 없었음** (synk_stories·synk_cards·world_raid·academic_log 등) — 8월 1일에야 생겨서 Glide가 테이블로 잡을 수 없었음 | preflight가 헤더까지 **미리 생성** |
| 3 | **가이드가 코어만 다룸** — 숙제·퀴즈·팁·랭킹·업적·칭호·공지·전투리포트·스토리북·리그·월드레이드·경영계기판 등이 문서에 없었음 | 이 문서 §6~§9가 **역할별 전 기능** 커버, §10 매트릭스로 검증 |
| 4 | 반 배정·가족 연결이 여전히 공백 (사람 몫) | §1에 실명 기준으로 명시 |
| 5 | 스토어에서 잔액보다 비싼 구매 가능 (실측: 잔액 −35P 발견) | §6-3 구매 버튼에 **잔액 조건 필수** |

> 💡 v9.40 배포 후 첫 `preflightGlide` 실행이 위 1·2번을 전부 자동으로 채웁니다. 유호님 손이 필요한 건 §1(데이터 입력)과 §3 이후(Glide 화면 조립)뿐입니다.

---

## 1단계 · 데이터 준비 (앱 열기 전에 — ★사람만 할 수 있는 것)

앱은 시트를 비추는 거울입니다. 여기가 비면 아무리 조립해도 화면이 빕니다. **2026-07-19 실측 기준 남은 작업:**

### 1-1. 학생 반 배정 — 현재 학생 전원(SYNK-001 김재헌) 반 공백
- **상담 스프레드시트 → `상담데이터입력` 탭 → "반" 열**에 반 이름 입력.
- 값은 앱 시트 `schedule` 탭과 **글자까지 동일**: 현재 등록된 반 = `정규반1` ~ `정규반15`.
- 반영: 다음 날 아침 7시 자동. 지금 바로 = Apps Script에서 `syncProfiles` 실행.

### 1-2. 강사 담당 반 — SYNK-T01(김재헌) class_name 공백
- 앱 시트 `profiles` 탭 → 강사 행 **E열(class_name)**에 담당 반. 여러 반은 쉼표: `정규반1,정규반2`
- 안 하면: 수업 전 브리핑 메일·미등원 통보가 강사에게 안 감 + 강사 "우리 반" 필터 불가.

### 1-3. 학부모 ↔ 자녀 연결 — P01·P02 모두 공백
- `profiles` 학부모 행 **J열(parent_of)**에 자녀 user_id (예: `SYNK-001`).
- ⚠ **SYNK-P02 email이 `12`** — 이메일이 아니라 로그인 자체가 불가. 실제 이메일로 정정.

### 1-4. (개원 전) 강사 실제 이메일
- 현재 강사 이메일 = unmet23@gmail.com(원장 계정). 테스트 단계엔 그대로 OK.

---

## 2단계 · `preflightGlide` 1회 실행 — 나머지는 전부 자동

### 실행 (클릭 단위)
1. 앱 스프레드시트(SYNK_앱데이터) → **확장 프로그램 → Apps Script**
2. 상단 함수 드롭다운(▼) → **`preflightGlide`** → **▶ 실행**
3. 하단 실행 로그 확인: **✅**=자동 처리됨 · **⚠**=유호님 조치 필요(방법이 한 줄씩 적힘) · **ℹ**=참고
4. **⚠ 개수가 더 안 줄어들 때까지 2~3회 재실행**(전부 멱등·안전 — 특히 번역은 회당 60행이라 첫 실행에 다 안 끝날 수 있음) → **⚠ 0이면 조립 시작.**

### 실행 후 "이건 정상" 3가지 (놀라지 마세요)
- 📧 **경영 리포트 메일 1통 도착** — preflight가 콕핏 카드를 미리 만들면서 지난달 리포트를 발송(첫 1회만).
- ⏱️ 실행이 1~2분 걸림 — 콘텐츠 수백 행을 채우는 중. 6분 한도로 중간에 멈춰도 재실행하면 이어받음.
- 📝 **기존 공지(notices) 1행의 몽골어(title_mn/body_mn)가 이상하면** 그 두 칸만 지우세요 — 과거 버그로 오염된 행은 자동으로 안 고쳐지고, 비우면 10분 내 재번역됩니다.

### v9.40 preflight가 자동으로 해주는 것 (구버전과 달라진 핵심)
- **시트 골격 39종 보장** — 스토리북·월간카드·월드레이드·리그·학업로그 등 누락 시트를 헤더까지 생성
- **콘텐츠 자동 복구** — 유형별 개수가 기대와 다르면 해당 셋업 자동 실행 (숙제 210·연료 6·시즌 12·팁 30·문법 72+도달도 2 등) + 빈 이미지 플레이스홀더 + **몽골어 큐레이션 340행 재주입**
- 강사 폼용 시트·열 물리 생성 (weekly_topics F~L·attendance_batch·hw_batch·absence_notice·inquiries·teacher_checkins)
- 온보딩 카드 4역할 · 학업 로그(academic_log) 생성
- **월드보스 이번 달 소환** + **콕핏 월간 카드 2종**(경영리포트·여행지도 HTML) 생성
- 전체 계산 1회 — 이때 **오늘의숙제·주말의숙제·오늘의퀴즈·오늘의팁 키가 즉시 생성**(콜드스타트 시딩 — 21시까지 기다릴 필요 없음)
- app_state 중복 키 정리 · teacher_stats 8열 정규화 · system_manifest 실측 갱신
- 진단: 반 미배정·이메일 오류·자녀 미연결·시간표 불일치·**잔액 음수 학생**·트리거 생존

### ⚠ 여전히 직접 실행하면 안 되는 것
| 함수 | 이유 |
|---|---|
| **`bootstrapSynk`** | 내부 setupSchedule이 시간표를 기본값으로 리셋 → 유호님의 정규반1~15 커스텀이 날아감. 재해 복구 전용 |
| **`resetAllTriggers`** | preflight가 "트리거 누락"이라고 할 때만. 매월 1일 오전 금지 |
| `setupSchedule` | 시간표 리셋 — 절대 단독 실행 금지 |
| `seedMasteryForExisting` | 개원 무렵 실제 로스터 입장 후 1회 (재학생 문법 소급 인정) |

### 조립 중 즉시 검증용 수동 러너 (전부 안전·멱등)
| 함수 | 용도 |
|---|---|
| `runLessonExpandNow` | 마감폼 저장분을 밤까지 안 기다리고 즉시 전개+재계산 |
| `parentSweep` | 출석폼 전개·출결 보드·폼 접수를 즉시 1회 |
| `runExecReportNow` / `runTravelMapNow` | 콕핏 월간 카드 재생성 |
| `bizDashboardNow` | 경영계기판 즉시 갱신 |
| `syncNotionNow` | 노션 크루 DB 동기화 즉시 1회 |

---

## 3단계 · 앱 생성 + 로그인 + 권한

### 3-1. 생성
- **unmet23@gmail.com**으로 go.glideapps.com → New app → Google Sheets → **SYNK_앱데이터**.
- 시트는 절대 새로 만들지 않음 — 기존 시트가 엔진의 심장.

### 3-2. Users(로그인 매핑)
- Settings(⚙) → Users → User profiles table = **profiles**
- Email = `email` · Name = `이름` · Role = `role`

### 3-3. 권한 5건 (하나라도 빠지면 학생이 남의 정보를 봄)
1. Settings → Privacy → **"Users must sign in" ON**
2. Users table 매핑(3-2)
3. profiles `email` 열 = **Row Owner** — 각자 자기 행만 기기로 내려받음
4. 모든 탭·컴포넌트 Visibility = role 조건. 역할값 정확히 `student` / `parent` / `teacher` / **`director`** (admin 아님!)
5. 드라이브 **SYNK_리포트카드** 폴더 → 공유 → "링크가 있는 모든 사용자·뷰어"

### 3-4. 민감 3열 바인딩 금지
- **⚠상담위험(AZ·52)·핵심비전(BA·53)·리텐션신호(BK·63)** — 리텐션신호만 원장 콕핏에, 앞 2개는 앱에 아예 안 올림.

### 3-5. 테스트 계정 (실데이터)
| 로그인 | 행 | 화면 |
|---|---|---|
| unmet23@gmail.com | SYNK-T01 (teacher) | 강사 |
| yuh94@naver.com | SYNK-D01 (director) | 원장 |
| with3beat@naver.com | SYNK-001 (student) | 학생 |
| lalamerrose@naver.com | SYNK-P01 (parent) | 학부모 (1-3 연결 후) |

---

## 4단계 · 디자인 세팅 — "투박하지 않게" (★신규 장)

카드 HTML은 v9.40에서 **전 카드에 Noto Sans KR 폰트 스택·조인 자간**이 코드로 심어져 있습니다(소프트 글로우 축: 인디고-라벤더 그라디언트·라운드 18px·부드러운 그림자). 앱 껍데기(Glide 테마)를 카드와 한 톤으로 맞추는 게 이 장의 목표입니다.

> Glide 메뉴 이름은 업데이트로 조금씩 바뀝니다 — 같은 뜻의 항목을 찾으세요. 화면이 설명과 다르면 스크린샷으로 질문.

### 4-1. Appearance (Settings → Appearance)
| 항목 | 값 | 이유 |
|---|---|---|
| **Accent color** | `#4F46E5` (인디고) | 전 카드의 메인 컬러와 동일 — 버튼·링크·탭 하이라이트가 카드와 한 몸이 됨 |
| **Theme/Background** | **Light 고정** (System/Dark 금지) | 카드가 라이트 파스텔 배경이라 다크모드에서 흰 덩어리로 떠 보임 |
| **Font** | 기본(System) 또는 **Rounded 계열** | 카드 내부는 코드가 Noto Sans KR로 통일. 앱 메뉴는 System이 한글 최적 |
| Logo/Icon | SYNK 로고 (Canva의 SYNK LAB 브랜드 폴더) | 홈 화면 설치 시 아이콘 |

### 4-2. 탭 아이콘·이름 규칙 (센스의 8할)
- 탭 이름은 **2~4글자 한국어**로 짧게: `홈` `도감` `스토어` `소식` `우리 아이` `반` `콕핏`
- 아이콘은 **같은 스타일 계열**(Glide 아이콘 팩에서 라인이면 전부 라인, 필드면 전부 필드)로 통일. 탭마다 스타일이 섞이면 그게 "투박함"의 정체.
- 이모지 탭 이름(예: "🏠 홈")은 금지 — 아이콘과 중복돼 촌스러움. 이모지는 카드 안에만.

### 4-3. 컴포넌트 여백 리듬
- 카드(Rich Text) 사이에 **Separator를 넣지 않기** — 카드 자체에 그림자·여백이 있어 이중 구분선이 되면 투박해짐.
- 컬렉션 스타일: 학생·학부모 목록 = **Card** 스타일(이미지 크게), 데이터성 목록(출결 보드·리그) = **Compact/Table**.
- 목록 이미지가 없는 행이 섞이면 Glide가 회색 사각형을 그림 — 이미지 없는 컬렉션은 스타일에서 이미지 슬롯 자체를 끄기.

### 4-4. 색 사용 절제
- Accent 외 색은 **카드가 이미 들고 있음** — Glide 쪽에서 버튼 색·배경색을 개별 지정하지 말 것(통일감 파괴).
- 버튼은 기본 Accent 채움(Primary) 1종 + 아웃라인(Secondary) 1종만.

---

## 5단계 · 컬럼 타입 사전 (Data 모드에서 열 클릭 → 타입 지정)

### 5-A. profiles (82열) — 학생·학부모 화면의 원천
**절대 규칙**: 39·40열(헤더 빈 은퇴 열)·`(구)리포트확인월`(38)·`시냅스게이지`(31)·`게이지문구`(32) 바인딩 금지.

**Glide(앱)가 쓰는 열 = 5개뿐** (나머지 전부 스크립트 소유·읽기 전용):
| 열 | 이름 | 누가 |
|---|---|---|
| AK(37) | 착용칭호 | 학생 Choice |
| AO(41) | 몬스터이름 | 학생 Text Entry |
| AR(44) | 목표아이템 | 학생(스토어 찜) |
| BC(55) | 대표몬스터 | 학생(도감) |
| CB(80) | 드림한줄 | 학생 Text Entry |

**Rich Text로 지정할 열(HTML 카드)** — 액자HTML(BD56)·몬스터한마디(BF58)·기록실HTML(BG59)·플레이스타일(BH60)·시냅스케미(BI61)·매치업프리뷰(BJ62)·주간리포트MN(BL64)·대화카드MN(BM65)·축하배너MN(BN66)·학업추세HTML(BW75)·나의여정(BY77)
**Text 그대로**: 오늘의운세(BE57)·오늘의알림(BX76)·목표진행(BZ78)·게이트문구(CD82)·이달의스토리(AU47)·현재칭호(AC29)·대표칭호(AH34)·칭호등급(AI35)
**Number**: 누적잔액P(16)·월간포인트Q(17)·월간랭킹R(18)·진화진행률T(20)·연속출석U(21)·이번달출석V(22)·잔액AQ(43)·MVP누적AW(49)·시냅스누적AX(50)·현재급수BO(67)·남은문법수CC(81)·출석일당포인트CA(79)

### 5-B. contents — 열 이름이 직관과 다름 (주의 계속)
실측 헤더: **`content_id`·`type`·`value_ko`·`value_mn`·`image_url`·`threshold`·`🔒 Row ID`**
| 헤더 | 실제 의미 |
|---|---|
| type (B) | 유형 — 필터 기준: monster·store·grammar·reach·quiz·homework·braintip·fuel·boss·worldboss·lore·season·label·reason·cheer·cheermail |
| value_ko (C) | 이름/제목 |
| value_mn (D) | **설명/본문 (한국어 본문임!)** — 퀴즈는 `문제|정답` |
| image_url (E) | → **Image** 타입 |
| threshold (F) | 임계값/가격/순번 겸용 |
| 🔒 Row ID (G) | **절대 불가침** |

### 5-C. 그 외 Image·Rich Text 지정
| 시트.열 | 타입 |
|---|---|
| report_cards.image_url · hall_of_fame.사진URL · crew_projects.사진URL | Image |
| class_stats 9~13열(수업전브리핑·격파찬스·오늘체크·왕관밸런스·레이드카드HTML) | Rich Text |
| synk_cards.카드HTML | Rich Text |
| app_state.value 중 ~HTML 키 | (컬럼 타입이 아니라 **Rich Text 컴포넌트**로 표시 — §9-1 방식) |

> 💡 화면에서 `<div>` 원문이 보이면 100% Rich Text **컴포넌트**를 안 쓴 것. 열 타입이 아니라 화면 컴포넌트가 Rich Text여야 합니다.

---

## 6단계 · 학생 앱 (role = student) — 탭 5개

### 6-1. 탭 「홈」 (소스: profiles — 로그인 사용자 행)
위에서 아래 순서 (전부 읽기 전용, ⑬만 입력):
| # | 컴포넌트 | 소스 | 비고 |
|---|---|---|---|
| ① | 목록 1행 | onboarding (Filter role=student) | 첫 안내. (선택) 접이식 |
| ② | Text | 오늘의알림 BX | Visibility: is not empty — 왕관·진화·생일 날만 반짝 |
| ③ | Text | app_state `지난달의전당` (§9-1 방식) | Visibility: is not empty — 월초 자동 |
| ③b | Text | app_state `이달의시즌` (§9-1 방식) | 시즌 배너 "7월 시즌 · 한강 나이트 — …" (월초 자동 · is not empty) |
| ④ | **Rich Text** | 나의여정 BY | 대표 카드 (진화 스트립·기록·문법 게이트·드림 포함) |
| ⑤ | **Rich Text** | 액자HTML BD | 몬스터+진화 게이지 |
| ⑥ | Text | 게이트문구 CD | Visibility: is not empty — "문법 n개만 익히면 진화!" |
| ⑦ | **Rich Text** | 학업추세HTML BW | 실력 성장 |
| ⑧ | Progress | 진화진행률 T (0~100) | (선택) 이번달출석 V도 Progress 하나 더 — 최대값은 반유형별 수업일 수 |
| ⑨ | **Rich Text** | 몬스터한마디 BF | |
| ⑩ | **오늘의 숙제 카드** | app_state (아래 6-1a) | ★신규 — 지난 가이드 누락분 |
| ⑪ | **오늘의 퀴즈 카드** | app_state (아래 6-1b) | ★신규 |
| ⑫ | Rich Text ×4 | 기록실BG·플레이스타일BH·시냅스케미BI·매치업BJ(not empty) | |
| ⑬ | Text Entry | 드림한줄 CB | 학생 유일 입력칸 — "🌟 나의 목표" |
| ⑭ | Text | 오늘의운세 BE → 맨 아래 | |
| ⑮ | Text | app_state `오늘의팁` | 홈 최하단 한 줄 (뇌과학 팁) |

**6-1a. 오늘의 숙제 카드 (★상세)**
1. Data 모드 → app_state 테이블에 계산 컬럼 6개 생성 (§9-1의 Query→Single Value 방식):
   `오늘의숙제_v` `오늘의숙제유형_v` `오늘의숙제팁_v` `주말의숙제_v` `주말의숙제유형_v` `주말의숙제팁_v`
2. **profiles 테이블에** If-Then-Else 컬럼 `내숙제`: If `반유형(AJ36)` is `주말` → `주말의숙제_v` / Else → `오늘의숙제_v` (Single Value 컬럼을 profiles로 가져오려면: profiles에 Single Value 컬럼을 직접 만들면 됨 — 대상 시트 app_state)
3. 홈 화면에 Title 컴포넌트("📚 오늘의 숙제" + `내숙제유형`) + Text(`내숙제`) + 작은 Text(`내숙제팁`).
- 게시 시각: 매일 21시 자동(그날 배운 내용 기준). 금요일 숙제가 주말 내내 유지 — 정상.

**6-1b. 오늘의 퀴즈 카드 (★상세 — 정답 숨김 필수)**
1. app_state에 `퀴즈_v` = Single Value(key=`오늘의퀴즈_초급`). (선택) profiles.현재급수(BO)로 ITE 분기: 급수≥3 → `오늘의퀴즈_고급`, ≥1 → `_중급`, 그 외 `_초급`.
2. 값 형식 = `문제|정답` → **Split Text 컬럼**(구분자 `|`) → `퀴즈문제` `퀴즈정답`.
3. 화면: Text(`퀴즈문제`) + **정답 리빌**: Button "정답 보기" → Show Detail Screen(또는 Action: Show Notification에 `퀴즈정답`). ⚠ 언어정책: 정답을 문제와 같은 화면에 평문 노출 금지.

### 6-2. 탭 「도감」 (소스: contents, Filter `type is monster`)
- Collection(Card): Image=image_url · 제목=value_ko · 설명=value_mn.
- 상세 화면: "대표로 선택" Button → Set Column → profiles.대표몬스터(BC)=value_ko. 미도달 몬스터 선택은 다음 계산에서 자동 무효화(코드 검증) — 그래도 Visibility로 threshold ≤ 누적잔액P(16)일 때만 버튼 노출 권장.
- (재미) 학생의 몬스터이름(AO)을 Text Entry로 여기 배치해도 좋음 — "내 몬스터 이름 짓기".

### 6-3. 탭 「스토어」 (소스: contents, Filter `type is store`)
- 상단: profiles.잔액(AQ43) 크게 (Title 컴포넌트 "⚡ 내 포인트").
- Collection: Image+value_ko+가격(threshold).
- 상세 화면 버튼 2개:
  - **"🎯 찜하기"** → Set Column → profiles.목표아이템(AR)=value_ko → 홈 목표진행(BZ) 카드가 자동으로 "몇 P 남았는지" 계산.
  - **"교환하기"** → Add Row → point_logs: student_id=내 user_id · points=**음수 가격** · reason=**구매사유** · given_by=내 이름.
    - 준비(Data 모드 → contents에 계산 컬럼 2개): ① Math 컬럼 `음수가격` = `threshold * -1` ② Template 컬럼 `구매사유` = `스토어·{value_ko}`. Add Row에서 points=`음수가격`, reason=`구매사유` 바인딩.
  - ⚠ **교환 버튼 Visibility 필수**: 조건 = `threshold` ≤ **User Profile의 잔액(AQ)** (Glide는 조건 우변에서 User Profile 컬럼 선택 가능). 이게 없어서 잔액 −35P 사건 발생. 반대 조건으로 "포인트가 부족해요 🐣" Text를 함께 두면 완성.
  - 실제 상품 수령은 데스크에서 원장 확인(point_logs에 기록이 남음).

### 6-4. 탭 「소식」 (스토리·공지 통합)
- **공지**: notices — Collection(제목=`title_ko`·본문=`body_ko`·날짜=`created_at`) 최신순. ★열 이름이 title_ko/body_ko (실측). `notice_id` 열은 바인딩하지 말 것(자동 공지는 이 칸이 비어 있음 — 정상).
- **반 전투 리포트**: raid_story — Filter: `class_name is 내 반`(profiles→relation) or `전체`. 제목+스토리, 날짜 내림차순. 매일 밤 자동 발간(일일·리그중계·결산).
- **월간 스토리북**: synk_stories — 월별 그룹, 챕터 순. 제목=챕터제목, 본문=Rich Text(마크다운 *기울임* 포함). 8월 1일 첫 발간(그 전엔 빈 상태 정상 — Empty state 문구: "첫 이야기가 8월 1일에 발간돼요 📖").
- **이달의 카드**: synk_cards — Filter student_id=나 · 카드HTML=Rich Text. 매월 1일 생성.
- **명예의 전당**: hall_of_fame(졸업생) + league_history(월간 챔피언) 읽기 목록.

### 6-5. 탭 「랭킹」 (선택 — 소스: profiles, Filter role=student)
- Collection(Compact): 이름 · 월간포인트(Q) 내림차순 정렬 · 월간랭킹(R) 표시.
- 부지표: 출석일당포인트(CA79) — 주말반 공정 지표.
- **업적·칭호 섹션** (같은 탭 하단 또는 프로필 상세):
  - achievements — Filter student_id=나: 업적·등급·달성일.
  - 현재칭호(AC)=이번 달 보유 칭호 전부 · 대표칭호(AH)+칭호등급(AI)=배지.
  - **착용칭호(AK)**: Choice(소스: contents type=lore의 value_ko) → 착용한 칭호의 로어 표시 = relation(AK ↔ contents.value_ko) → Lookup으로 value_mn(로어 문구)·threshold열 뒤 등급 라벨.
- 왕관 컬렉션: MVP누적(AW)·시냅스누적(AX) — "🌟 ×n · ⚡ ×n" Template.

> 탭이 많으면 「랭킹」은 「소식」 하위 섹션으로 합쳐도 됨. 핵심: **기능을 빼지 말고 배치만 조정.**

---

## 7단계 · 학부모 앱 (role = parent) — 탭 4개

### 7-1. 탭 「우리 아이」
- 소스: profiles — **Relation**: 내 행 parent_of → 학생 user_id (Single 관계) → 자녀 행.
- 최상단 onboarding(role=parent — 안내MN 함께).
- 자녀 카드 (전부 Rich Text·Lookup으로): 축하배너MN(BN·not empty) → 주간리포트MN(BL) → 대화카드MN(BM) → 학업추세HTML(BW) → (선택) 액자HTML(BD)·이달의스토리(AU).
- 몽골어 우선 — 코드가 몽골어 템플릿으로 이미 생성함.

### 7-2. 탭 「성장 리포트」
- report_cards — Filter student_id=자녀ID: Image(image_url)+월+칭호+코멘트. 매월 1일 자동 생성.

### 7-3. 탭 「결석 신고」 + 「문의」 (폼 2개)
- **absence_notice** Form: student_id=자녀ID(자동) · 반=자녀 반 · 날짜=Date picker · 사유=Text · 등록시각=제출시각. → 그날 미출석 경보에서 자동 제외 + 강사 수업 브리핑에 반영.
- **inquiries** Form: student_id·이름(자동) · 문의내용=Text · 상태는 폼에 넣지 않음(기본 '접수') · 접수시각=제출시각. → 원장 아침 브리핑 + 주간 리포트에 뜸.

### 7-4. 탭 「소식」
- notices — 제목=`title_mn`·본문=`body_mn`(몽골어 열!) · KO는 보조로.

---

## 8단계 · 강사 앱 (role = teacher) — "수업 전 1분 · 수업 후 1분"

### 8-1. 탭 「반」 — 반 선택 네비게이션 (정본)
1. **반 목록**: class_stats — 반이름·학생수·반몬스터. (한 강사 여러 반이라 "로그인 강사 반 고정"이 아니라 **선택형**)
2. 반 탭하면 **반 상세 화면**:
   - Rich Text 5장: 수업전브리핑(9) → 오늘체크(11) → 격파찬스(10·not empty) → 레이드카드HTML(13) → 왕관밸런스(12)
   - **학생 리스트**: profiles Filter class_name=이 반 & role=student — 표시: 이름·**한국어수준(AY·51 — 강사 뷰 '레벨', 이 열이 강사용으로 설계됨)**·몬스터단계·진화진행률. 리텐션신호(BK)는 원장 전용이므로 미표시 권장.
   - (선택·재미) 반 상세 상단에 **Rollup 컬럼**: 이 반 학생들의 `단계번호(AP)−1` 합 = "🐲 우리 반 누적 진화 n회" — Data 모드에서 class_stats에 relation(반이름↔profiles.class_name)→Rollup(Sum).
   - 학생 상세 화면에 **왕관 버튼 2종** (Add Row → point_logs):
     - `🌟 오늘의 MVP` : points=10 · reason=**`오늘의 MVP`**(띄어쓰기 정확) · student_id=이 학생 · given_by=내 이름
     - `⚡ 오늘의 시냅스` : points=10 · reason=**`오늘의 시냅스`** · 나머지 동일
     - (선택) `📚 숙제완료` 개별 버튼: points=10 · reason=`숙제완료` — 일괄폼과 병행해도 중복지급 자동 차단
     - 하루 한도(왕관 반당 1명·숙제 1회)는 밤에 자동 정정 + 강사에게 통보 메일 — 안심하고 눌러도 됨
     - **생일축하 버튼은 만들지 않음** (아침 7시 자동 지급)
3. 반 상세 화면의 폼 버튼 3개:

**① 출석폼 (attendance_batch)** — 수업 시작 1탭
- 필드: 날짜=**오늘 자동** (★전개 판정 기준) / class_name=이 반 / 출석자목록=**Choice 멀티**(profiles·이 반 학생·value=user_id) / 입력자=내 이름. **처리상태는 폼에 넣지 않음.**
- 10분 내 attendance로 자동 전개. ⚠ 수업 당일에 저장(자정 넘기면 전개 안 됨 — 소급 오염 방지 설계).

**② 마감폼 (weekly_topics)** — 수업 끝 1탭 (진화 게이트의 심장)
- 폼 필드 = **A·B·F·G·H·I·J만**:
  - class_name(A)=이 반 / 배운내용(B)=Text
  - 문법태그(F)=**Choice 멀티** — 소스: contents `type is grammar` · display=value_ko · **value=content_id**(★G3xx ID로 저장돼야 게이트 매칭)
  - 전체도달도(G)=**Choice** — 소스: contents `type is reach` (`도달`/`더연습` 2행 — preflight가 생성) · value=value_ko
  - 예외학생(H)·숙제완료자(I)=**Choice 멀티**(이 반 학생·value=user_id) — I가 "숙제 멀티체크"
  - 연료미션(J)=**Choice**(소스: contents `type is fuel` · value=value_ko — 6종. 성공한 미션만 선택, 없으면 비움)
- created_at(D)=제출시각 자동. **E(번역)·K·L(처리상태)은 폼에 절대 넣지 않음.**
- 밤 22시 자동 전개: 숙제 +10P · 문법 도달(mastery) · 연료 → 진화 게이트·레이드 데미지가 움직임. 즉시 확인 = `runLessonExpandNow`.
- ⚠ 수업 당일 저장. 저장 후 그 행을 더 고칠 거면 그날은 러너 실행 금지(밤 배치에 맡기기).

**③ (선택) 숙제만 빠른 체크 (hw_batch)**: date=오늘·class_name·완료자목록 멀티·입력자. 마감폼 안 쓰는 날용.

### 8-2. 탭 「보드」
- today_board 읽기 — 유형·이름·반·시각·퇴근 (10분 갱신). 오늘 수업 반의 미등원자는 `—`.

### 8-3. 탭 「출퇴근」 (teacher_checkins Form)
- 필드 3개만: 이름=내 이름 · 구분=**Choice `출근`/`퇴근`** · 시각=제출시각 자동.
- 출근하면 today_board에 표시, 퇴근 5~15분 뒤 응원 메일 1통.

### 8-4. 탭 「수업 준비」 (선택 — 없어도 메일로 옴)
- 오늘의숙제 카드(6-1a 재사용 — 오늘 검사할 숙제) + 오늘의퀴즈(워밍업) + onboarding(teacher).
- **학업 기록 안내 카드**(Rich Text 고정 문구): "급수·모의점수는 월 1회 **시트 academic_log**에 직접 입력 — log_id·student_id·날짜·유형(level/mock)·값·비고·입력자. 앱 업데이트 소모 0." (+ 선택: student_errors에 약점 메모)

---

## 9단계 · 원장 앱 (role = director)

### 9-1. 탭 「콕핏」 — app_state 꺼내는 법 (★실증된 2단 방식)
key-value 세로 시트라 키마다 2단 계산 컬럼:
1. Data 모드 → app_state → **Query 컬럼**: `리텐션_q` = rows where key is `리텐션레이더HTML`
2. **Single Value 컬럼**: `리텐션_v` = First from `리텐션_q` → value
3. 화면에 **Rich Text** 컴포넌트 → 소스 = 그 Single Value. (**Lookup은 배열이라 안 붙음 — 반드시 Single Value**)

콕핏 구성 (위→아래): 리텐션레이더HTML → 케어사각HTML → 경영리포트HTML → 여행지도HTML → (Chart 컴포넌트) class_stats.이번달출석합 반별 막대. 전부 add/edit OFF.
- 4키 모두 preflight가 이미 생성해 둠. 퀴즈 3종 키(`오늘의퀴즈_초급/중급/고급`)도 같은 방식.

### 9-2. 탭 「출결」 — today_board (강사와 동일 뷰)

### 9-3. 탭 「운영」 (Collection 묶음)
- **리그 관리**: league_pairs — 이번 주(week 최신) 행 목록. **반A/반B 편집 허용**(Edit form — 이 2열만) = 원장 수동 매칭 오버라이드. 상태·결과는 편집 금지.
- **반 레이드 현황**: raid — 이번 주 행(week 최신): 반별 목표 HP·달성포인트·상태 읽기. (학생용 연출은 class_stats 레이드카드가 담당 — 이건 원장 숫자 뷰)
- **월드 레이드**: world_raid — 월·보스명·HP·누적데미지·상태 읽기. (연출용: 진행중 행을 Progress로 — 누적데미지/HP)
- **특별 칭호 부여**: manual_titles **Form** — student_id·칭호·부여자·날짜. 원장이 이벤트성 칭호를 직접 수여(다음 계산 때 현재칭호 AC에 합류).
- **보스 휴식주 지정**: 앱 아님 — 시트 `app_state`에 key=`보스휴식주`, value=그 주 아무 날짜(`2026-08-11`, 여러 주는 쉼표). 방학·나담 휴강 주에 보스 소환을 쉬게 함(🏖️ 휴식 스토리 자동).
- **시간표 변경**: 앱 아님 — 시트 `schedule` 탭에서 직접(반·요일유형·시작시간). ⚠ 반 이름을 바꾸면 profiles class_name도 같이 바꿔야 함.
- **공지 쓰기**: notices **Form** — `title_ko`·`body_ko`만 입력(몽골어는 10분 내 자동 번역). 학생·학부모 소식탭에 즉시 노출.
- **명예의 전당 등재**: hall_of_fame Form — 연도·이름·반·업적·한마디·사진URL.
- **크루 프로젝트**: crew_projects Form — 시즌·반·프로젝트명·한줄소개·결과물링크·사진URL·공개일·참여크루. (시즌 K-컬처 프로젝트 포트폴리오 — 학생 소식탭에도 읽기 노출 가능)
- **강사 지표**: teacher_stats 읽기 — 케어지수·지난달왕관·왕관편중%(60%↑면 편중 주의).
- **문의함**: inquiries — 상태 편집 허용('접수'→'처리완료').
- **퇴소 로그**: exit_log 읽기.

### 9-4. 탭 「경영」 (선택 — 시트 직접 관리와 병행)
- 경영계기판 시트 읽기(지표·값·상태 신호등) · kpi_metrics 읽기 · payments Form(매출 기입: student_id·이름·금액·납부일·방법) · leads Form(리드 기입: 날짜·이름·연락처·유입경로·체험참석·등록·권종·미등록사유·캠페인) · system_manifest 읽기(시스템 현황).
- 현금잔고·광고비 입력은 시트 `경영계기판` B2·B5에서 직접(월 1회).

---

## 10단계 · 기능→화면 전수 매트릭스 (빠뜨림 방지 장치)

코드에 존재하는 **모든** 사용자 대면 기능과 배치 지점. 조립 후 이 표를 체크리스트로 쓰세요.

| 기능(코드 근거) | 화면 | §
|---|---|---|
| 온보딩 4역할 (onboarding) | 각 역할 홈 최상단 | 6-1·7-1·8-4 |
| 오늘의알림 배너 (BX) | 학생 홈 ② | 6-1 |
| 지난달의전당 배너 (app_state) | 학생 홈 ③ | 6-1 |
| 나의여정 (BY) · 액자 (BD) · 게이트문구 (CD) | 학생 홈 ④⑤⑥ | 6-1 |
| 학업추세 (BW) · 진행바 (T) | 학생 홈 ⑦⑧ · 학부모 | 6-1·7-1 |
| 몬스터한마디 (BF) · 운세 (BE) · 팁 (app_state) | 학생 홈 ⑨⑭⑮ | 6-1 |
| **오늘의 숙제** (app_state 6키+AJ 분기) | 학생 홈 ⑩ · 강사 8-4 | 6-1a |
| **오늘의 퀴즈 3난이도** (Split·리빌) | 학생 홈 ⑪ · 강사 8-4 | 6-1b |
| 기록실 (BG) · 플레이스타일 (BH) · 케미 (BI) · 매치업 (BJ) | 학생 홈 ⑫ | 6-1 |
| 드림한줄 입력 (CB) | 학생 홈 ⑬ | 6-1 |
| 도감+대표몬스터 선택 (BC) · 몬스터이름 (AO) | 도감 탭 | 6-2 |
| 스토어+찜 (AR)+목표진행 (BZ)+**잔액 게이트 구매** | 스토어 탭 | 6-3 |
| 공지 (notices KO/MN) | 학생·학부모 소식 · 원장 폼 | 6-4·7-4·9-3 |
| 일일 전투·리그 중계·결산 (raid_story) | 학생 소식 | 6-4 |
| 월간 스토리북 (synk_stories) · 여행지도 | 학생 소식 · 콕핏 | 6-4·9-1 |
| 이달의 카드 (synk_cards) | 학생 소식 | 6-4 |
| 랭킹 (Q·R·CA) | 랭킹 탭 | 6-5 |
| 업적 (achievements·히든 포함) | 랭킹 탭 | 6-5 |
| 칭호 시스템 (AC·AH·AI·AK+lore) · 왕관 컬렉션 (AW·AX) | 랭킹 탭 | 6-5 |
| 명예의전당 (hall_of_fame) · 리그 역사 (league_history) | 소식 · 원장 폼 | 6-4·9-3 |
| 학부모 몽골어 4카드 (BL·BM·BN·BW) | 우리 아이 | 7-1 |
| 리포트카드 (report_cards) | 성장 리포트 | 7-2 |
| 결석 사전신고 (absence_notice) · 문의 (inquiries) | 학부모 폼 · 원장 문의함 | 7-3·9-3 |
| 반 브리핑·격파찬스·오늘체크·왕관밸런스·레이드카드 (class_stats 9~13) | 강사 반 상세 | 8-1 |
| 출석 1탭 (attendance_batch) · 마감폼 (weekly_topics F~L) · 숙제 일괄 (hw_batch) | 강사 폼 3종 | 8-1 |
| 왕관 2종+개별 숙제 버튼 (point_logs) | 강사 학생 상세 | 8-1 |
| 진화 게이트 (mastery·grammar·reach) | 마감폼 F·G + 학생 홈 ⑥ | 8-1·6-1 |
| 연료 미션 (fuel→class_fuel) | 마감폼 J | 8-1 |
| 출결 보드 (today_board) | 강사·원장 | 8-2·9-2 |
| 출퇴근 (teacher_checkins) | 강사 폼 | 8-3 |
| 학업 입력 (academic_log·월1회) | 시트 직접 (안내 카드) | 8-4 |
| 콕핏 4 HTML+퀴즈 키 (app_state) | 원장 콕핏 | 9-1 |
| 리그 오버라이드 (league_pairs) | 원장 운영 | 9-3 |
| 반 레이드 숫자 현황 (raid) | 원장 운영 | 9-3 |
| 월드 레이드 현황 (world_raid) | 원장 운영 | 9-3 |
| 크루 프로젝트 (crew_projects) | 원장 폼 | 9-3 |
| 특별 칭호 수여 (manual_titles) | 원장 폼 | 9-3 |
| 보스 휴식주 (app_state `보스휴식주`) | 시트 직접 | 9-3 |
| 시간표 관리 (schedule) | 시트 직접 | 9-3 |
| 이달의 시즌 배너 (app_state `이달의시즌`) | 학생 홈 ③b | 6-1 |
| 강사 학생 리스트의 레벨 표시 (한국어수준 AY) | 강사 반 상세 | 8-1 |
| 반 누적 진화 Rollup (단계번호 AP) | 강사 반 상세(선택) | 8-1 |
| 강사 지표 (teacher_stats 8열) | 원장 운영 | 9-3 |
| 경영계기판·KPI·leads·payments·exit_log·system_manifest | 원장 경영 | 9-4 |
| **앱에 안 올리는 것(의도)**: ⚠상담위험(AZ)·핵심비전(BA)·mastery_log·student_errors·carryover·monthly_snapshot·titles·story·form_responses·point_logs_archive — 내부 재료 | — | 3-4 |

### 10-B. 역할별 조립 완료 체크리스트 (조립하며 [ ]→[x])

**학생 (컴포넌트 21개)**
```
홈:    [ ]온보딩 [ ]알림배너BX [ ]전당배너 [ ]시즌배너 [ ]나의여정BY [ ]액자BD
       [ ]게이트문구CD [ ]학업추세BW [ ]진행바T [ ]몬스터한마디BF [ ]숙제카드 [ ]퀴즈카드(리빌)
       [ ]기록실BG [ ]플레이스타일BH [ ]케미BI [ ]매치업BJ [ ]드림한줄 입력CB [ ]운세BE [ ]팁
도감:  [ ]몬스터 목록 [ ]대표로 선택→BC [ ](선택)몬스터 이름짓기AO
스토어:[ ]잔액AQ 표시 [ ]상품 목록 [ ]찜하기→AR [ ]교환하기(잔액 조건!)→point_logs
소식:  [ ]공지(title_ko) [ ]반 전투리포트 [ ]스토리북 [ ]이달의카드 [ ]명예의전당 [ ]리그역사
랭킹:  [ ]월간랭킹 목록 [ ]업적 [ ]칭호(AC·AH·AI) [ ]착용칭호AK+로어 [ ]왕관컬렉션AW·AX
```

**학부모 (컴포넌트 12개)**
```
우리아이: [ ]온보딩MN [ ]축하배너BN [ ]주간리포트BL [ ]대화카드BM [ ]학업추세BW [ ](선택)액자·스토리AU
리포트:   [ ]report_cards 목록(자녀 필터)
폼:       [ ]결석 사전신고 [ ]문의하기
소식:     [ ]공지(title_mn 몽골어)
```

**강사 (컴포넌트 15개)**
```
반:     [ ]반 목록(class_stats) [ ]브리핑⑨ [ ]오늘체크⑪ [ ]격파찬스⑩ [ ]레이드카드⑬ [ ]왕관밸런스⑫
        [ ]학생 리스트(+레벨AY) [ ]왕관 버튼 2종(reason 정확!) [ ](선택)개별 숙제완료 버튼
폼:     [ ]출석폼(attendance_batch) [ ]마감폼(weekly_topics A·B·F·G·H·I·J만!) [ ](선택)hw_batch
그 외:  [ ]출결 보드 [ ]출퇴근 폼(3필드) [ ](선택)수업 준비 탭(숙제·퀴즈·학업입력 안내)
```

**원장 (컴포넌트 16개)**
```
콕핏:  [ ]리텐션레이더 [ ]케어사각 [ ]경영리포트 [ ]여행지도 [ ](선택)출석 Chart
운영:  [ ]출결 보드 [ ]리그 관리(반A/B 편집) [ ]레이드 현황 [ ]월드레이드 [ ]공지 쓰기 폼
       [ ]명예의전당 폼 [ ]크루 프로젝트 폼 [ ]특별 칭호 폼 [ ]강사 지표 [ ]문의함(상태 편집)
경영:  [ ](선택)계기판·KPI·leads·payments·exit_log·manifest
```

---

## 11단계 · 검증 시나리오 (하면 → 즉시 이렇게 돼야 정상)

| 하는 것 | 즉시 확인 방법 | 정상 결과 | 안 되면 |
|---|---|---|---|
| 학생 로그인 | — | 자기 카드만·타인 행 없음 | Row Owner(3-3) |
| 홈 스크롤 | — | 카드 15종 렌더·`<div>` 원문 없음 | Rich Text 컴포넌트 |
| 숙제·퀴즈·팁 카드 | — | 오늘 값 표시(빈칸 아님) | preflight 재실행(콜드스타트 시딩) |
| 퀴즈 "정답 보기" | — | 탭 전까지 정답 안 보임 | Split·리빌 재확인(6-1b) |
| 도감 "대표로 선택" | 시트 BC(55) 변경 | 다음 계산(14/22시) 후 액자 반영 | Set Column 대상 |
| 스토어 교환(잔액 부족 상품) | — | **버튼 자체가 안 보임** | Visibility 조건(6-3) |
| 스토어 교환(가능 상품) | point_logs에 음수 행 | 다음 계산 후 잔액 감소 | reason·points 부호 |
| 강사 출석폼 저장 | `parentSweep` 실행 | attendance에 행 생성 | 날짜=오늘? 출석자=ID? |
| 강사 마감폼 저장 | `runLessonExpandNow` | mastery_log 행+숙제 +10P+여정 카드 "문법 n/12" | F열이 G3xx ID로 저장됐는지 |
| 왕관 버튼 2회 연타 | 밤 22시 후 | 초과분 자동 정정+강사 메일 | reason 띄어쓰기 |
| 학부모 로그인 | — | 자녀 몽골어 카드 4종 | parent_of(1-3) |
| 결석 신고 제출 | — | 그날 미출석 메일 안 옴+강사 브리핑에 표시 | 날짜=오늘인지 |
| 원장 공지 폼 제출 | 10분 후 | 학생 소식탭+몽골어 자동 번역 | title_ko 열에 들어갔는지 |
| 원장 콕핏 | — | 카드 4장 렌더 | Single Value 방식(9-1) |
| 원장 계정으로 학생 탭 | — | **안 보임** | Visibility=director |

---

## 12단계 · 운영 원칙 (조립 후 불변)

1. **쿼터(월 500 update)**: 쓰기 = 강사 출석폼(1/수업)·마감폼(1/수업)·왕관 버튼·학생 가끔(대표몬스터·찜·드림·구매). 개별 토글·자동 쓰기·방문 카운터 금지. 시트→앱 sync는 변경 묶음당 1 — 계산 열은 writeIfChanged라 무변경 날 0.
2. **언어정책 잠금 4**: ①학습 카드에 몽골어 raw 병기 금지(리빌만) ②퀴즈 정답 숨김 ③학생 자유 언어 토글 금지 ④학습 텍스트↔메뉴 언어 격리. 문형의 한국어 표시는 정상.
3. **불가침**: 스크립트 계산열(P~CD 중 앱 소유 5열 제외)·처리상태(K·L)·🔒 Row ID·schedule 구조.
4. 백업: 매일 새벽 3시 자동 30일 — 실수해도 복구 가능. 복구 절차 = `docs/응급복구_런북.md`.

## 부록 A · 유호님 결정 대기 (이월)
1. 문법 커리큘럼 72종 검수(TOPIK 1~2급 초안) — 강사와.
2. 리텐션신호(BK)를 강사에게도 보여줄지 (기본: 원장 전용).
3. '오늘의 다짐' 셀프미션(+2P·R09) 도입 여부 — 코드는 준비됨(dailyGuard 1일 1회), 버튼만 달면 됨.
4. Glide Languages 설정 스크린샷 확인(몽골어 고착 우려).
5. SYNK-P02 실제 이메일.

## 부록 B · 이 문서가 폐기·대체한 것
- `GLIDE_조립가이드_v939.md` 전체 (콕핏 Single Value 방식 등 유효 내용은 본문에 승계).
- 루트 `GLIDE_조립_마스터보드.txt`의 체크리스트 — 역할값 `admin` 표기 등 낡음. 마스터보드는 참고용, **정본은 이 문서**.

---
*작성: 2026-07-19 · 코드 v9.40 + 라이브 38탭 재실측 기준. 조립 중 화면이 문서와 다르면 지어내지 말고 스크린샷으로 확인 요청.*
