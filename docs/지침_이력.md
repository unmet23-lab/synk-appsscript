# SYNK 공용 작업 지침 — 변경 이력

> **목적**: 각 조항이 *왜* 생겼는지(근거 사건)를 Claude 메모리 밖(Codex·cowork·GPT 세션)에서도 볼 수 있게 남긴다. 정착된 결정과 거절된 제안을 다시 논쟁하지 않기 위한 것 — 새로 참여하는 세션은 이 파일을 먼저 읽고 "이건 왜 이래?"를 여기서 확인한다.
> 정본 = repo `CLAUDE.md`. 개정 절차는 `/evolve` 스킬. 새 버전을 올릴 때마다 이 파일에 1줄을 추가한다.

## 버전별 요지

- **v3.0 (2026-07-10)** — 공용 작업 지침 최초 확정. 7섹션(역할분담·소통·냉철 진단·자기검증 루프·제안 동반·선제 보완·실행 원칙). 자기검증 루프는 처음엔 **5회**. 자기검증 루프 워크플로(유호 페르소나 + 프롬프트 엔지니어 2렌즈) 5라운드로 47건 이슈를 정제해 제작.
- **v3.1 (2026-07-10)** — 루프 **5→3회**로 축소(과잉적용 방지, 유호 직접 판단). 콘텐츠·기획안 산출물은 루프 대신 **forge 스킬** 신설(A/B 병렬 → 하드 게이트 → 병합). 하위 작업 **모델 라우팅** 조항 추가(코드=Opus급/콘텐츠=Sonnet급/기계적=Haiku급). + 멀티세션 규약·`/deploy` 스킬을 인프라로 도입.
- **v3.2 (2026-07-10)** — 46에이전트 적대적 감사가 잡은 치명 결함 4건 수정: ①루프 보고를 "자기검증을 거쳤다"(자기기만 유도) → **"실제로 잡아 고친 것 1개 명시"**로 교체 ②제안 반복 금지를 "같은 대화" → **"제안하는 순간 즉시 기록"**으로 확장(기억 단절로 반복이 새어 나옴) ③**Fable(모델명) 예외 삭제** — "검증이 가장 필요한 순간에 검증이 꺼지는" 역설 ④**버전 스탬프 도입**(사본 드리프트 탐지, forge 절차 정본은 SKILL.md 한 곳).
- **v3.3 (2026-07-10)** — 조항 감사 후 3개 추가: ①**분리 반박 패스**(비판을 초안 추론과 분리, "애매하면 통과" 기본값 금지 — 실제로 15건 중 0건 탈락하며 헛돈 사건) ②**판독 인용 + 자진 정정**(유호가 못 읽는 스크린샷·코드 근거는 원문 인용 + "~로 보인다", 오독 판명 시 첫머리 철회) ③**비가역 외부 실행 게이트**(메일·발행·대량삭제는 실행 직전 승인). **거절**: "무거운 작업 사전 규모 고지" = 유호가 예고를 소음으로 판단 → **재제안 금지**.
- **v3.4 (2026-07-10)** — **"검증 규모는 결정의 되돌림 비용에 비례시킨다"** 도입. 실측 근거: 지침 52줄을 20% 줄여야 360토큰인데 같은 날 심층감사 하나가 600만 토큰을 태움 — 진짜 낭비는 문서 길이가 아니라 과잉 검증 습관이었다. 가역(git 복구 가능)=가벼운 반박 1패스, 비가역(돈·계약·외부 발행)=심층 검증.
- **v3.5 (2026-07-10)** — "트렌디"의 정의를 지침에 명시: 유행을 **좇는** 게 아니라 **되는** 것(끊임없는 개선의 결과). 유호가 AI의 틀린 이분법("유행에 밝은 게 아니라 개선")을 바로잡은 데서 출발.
- **v3.5 후속 (2026-07-11, 버전 무범프)** — Codex CLI 패리티: `AGENTS.md` 신설(고유 섹션 "새 작업 컨텍스트 복원")·`.agents/skills` 스킬 미러링. ChatGPT용 지침 사본(v3.5-GPT) 제작·배포(ChatGPT Pro = "비서" 역할 확정과 함께).
- **v4.0 (2026-07-11)** — 진화 메커니즘을 지침 골격으로 승격. ①**'지침 진화' 섹션 신설** + `/evolve` 스킬(절차 정본) — 지침·스킬·메모리 전체를 진단→수정→압축→상호발전 사이클로 다룸(유호가 정의한 "트렌디"의 운영형) ②**사본 지도**를 헤더에 명시(정본 ↔ AGENTS.md·COWORK·GPT 사본 — 버전 범프 시 무엇을 같이 갱신할지 모든 세션이 알게, AGENTS.md의 "정본은 AGENTS.md" 자기참조 드리프트 해소) ③**마찰 신호 기록** 조항(유호의 교정·거절·불만·내 실수 중 **굵직하거나 반복되는 것**은 즉시 기록 = 다음 진화의 연료, 제안 기록과 대칭) ④**압축·재편**: 냉철 진단 → 판정·검증에 흡수, 제안 동반 + 선제 보완 병합, 실행 원칙 압축 — 전투검증 조항 10건 의미 전부 보존, "묶음당 1회" 배치 검증 규칙 유지 ⑤`docs/지침_이력.md` 신설(이 문서). 제작: forge(A/B 병렬 + 분리 게이트)로 도출, 게이트가 잡은 결함(외부 공개물의 루프/forge 이중 분류, 배치 규칙 유실, 마찰 기록 무한정 발동)을 수정해 확정.

- **v5.0 (2026-07-20)** — 유호님 지시 "최대한 압축"으로 **전면 압축**(의미·검증 뉘앙스 보존, 본문 분량 대폭 축소) + 마찰 신호 3건 조항화: ①**도전안 병기**(굵직한 판정·기획에 확실안+통념 깬 도전안 1개 탐색·라벨 병기 — "창의 부재·뻔한 대답" 지적) ②**신뢰성 섹션 신설**(반복 절차 오류는 회귀 장치를 그 자리에서 추가, 두 번 나오면 시스템 결함 — Glide 조립 v937→947 매회 신규 오류 실증) ③**원요청 대조**(제출 직전 요구 항목 누락 확인 — "원하는 대답 까먹지 말라"). + 07-20 지침 감사 채택분: 검증규모∝되돌림비용이 루프 규칙에 우선 명시, evolve 점화 기준(굵직한 마찰 2건=제안), 프로즈→기계 강제 원칙.

- **v5.0 후속 (2026-07-20, 버전 무범프)** — 신뢰성 조항("규칙을 기계로")의 첫 실행: **clasp-guard 훅** 도입(`.claude/hooks/clasp-guard.js` + settings.json PreToolUse). clasp push/deploy 시 4대 불변식(루트 .js 구문·tests/*.test.js·배포 파일 미커밋·GitHub 미push)을 실검사해 위반 시 자동 차단, 통과 시 기존 승인 흐름 유지. 회귀 점검 = `node tests/clasp-guard.check.js`. 검증된 임시 러너 절차만 `CLASP_GUARD_BYPASS=1` 우회. deploy 스킬 양쪽(.claude/.agents) 갱신 — Codex 창은 훅 미적용이라 절차 준수 의무 명시.

- **v5.1 (2026-07-27, 07-28 유호 확정본으로 개정)** — 모델 라우팅에 **독립성 축** 추가. 기존 라우팅은 등급(Opus/Sonnet/Haiku)만 나눠 **상관된 오류**(같은 지점에서 같이 틀림)를 막지 못했다.
  - 07-27 초안은 "검증자는 구현자와 **다른 계열**에서 고른다(Claude↔Codex↔Kimi K3) — 같은 계열은 검증이 아니다"였다. **07-28 유호님 판정으로 완화 확정**: ①**반례** — v9.74 세션에서 Claude 구현 → Claude 리뷰어가 배포 차단급 2건(선점 열 파괴·1899 재기입 루프)을 잡았다. 둘 다 "모델이 몰라서"가 아니라 "옆 파일을 안 봐서" 생긴 결함이라 상관 오류를 깬 축은 계열이 아니라 **컨텍스트 독립성**이었다. ②**상위 조항 충돌** — 계열 교차는 Codex 창 왕복(순차 핸드오프)을 요구해 가역물마다 강제하면 "검증 규모 ∝ 되돌림 비용"(스스로 우선순위를 선언한 조항)에 눌려 사문화된다. → **컨텍스트 독립을 기본, 계열 교차는 비가역(돈·계약·외부 발행)에 한정**. 계열 교차가 특히 값진 곳은 Claude가 구조적으로 약한 영역(화면 판독·영상·몽골어 감각)이라고 명시 — 이는 메모리 `ai-stack-decision`의 이종 벤더 3용도와 같은 축이다.
  - **Kimi K3는 조항에서 제외**(07-28 유호님: "아직 확정 아님 — 구상 중, 확정되면 알려줄게"). 미가동 도구 이름을 지침에 박지 않는다. 확정 통보가 오면 계열 목록에 추가.
  - 사본: `AGENTS.md` 동반 개정. `COWORK`·`GPT` 사본은 해당 없음 — 본문 무변경이라 각각 v5.0·v5.0-GPT 스탬프를 유지한다(스탬프를 올리면 붙여둔 사본이 영구 오탐을 낸다. **재복붙 불필요**).
  - 외부 진단(Cowork 세션) 6건 중 1건만 근거 게이트를 통과했고, 나머지 5건은 아래 재론 금지로 내렸다.

- **v5.2 (2026-07-28)** — **'한 수 더' 조항 신설**(제안·선제 보완 섹션). 근거 사건: 07-28 디자인 컨셉 세션에서 판을 바꾼 아이디어 3건이 **전부 유호님에게서 나왔다**(캐러셀 1장 몽골어 단독 / 오렌지 유성 낙하 / 블랙홀 오브제 + 유성이 빨려듦). 내 산출물은 요구는 충족했으나 "촌스럽다"·"운석 같다"·"뭘 표현한 건지 모르겠다"·"구성안으로는 못 고르겠다, 부족하다"는 평가를 연쇄로 받았다. **결정적 논거 = v5.0의 '도전안 병기'가 이미 있는데도 발생했다** — 같은 세션에서 도전안(라이트 반전·시즌 컬러 로테이션·한글 로고타입)은 매번 병기했으나 산출물 자체의 완성도 문제는 잡히지 않았다. 도전안은 **방향의 대안**이고, 이번 결함은 **정해진 방향 안의 실행 깊이**라 별개 축임이 실증됐다. 반박 패스에서 "일반론 조항 금지"(evolve 2단계 게이트)에 걸릴 위험이 지적돼 — "좋은 아이디어를 내라"는 행동을 안 바꾼다 — **찾는 렌즈 3종(기존 요소 재사용·기능을 미학으로·작은 요소로 서사)과 실제 사례를 조항에 박아** 적용 가능한 형태로 통과시켰다. 압축 후보는 검토했으나 근거 있는 삭제 대상 없음. 사본: `AGENTS.md`·`COWORK`·`GPT` 3종 동반 개정(본문 변경이므로 cowork·GPT는 **재복붙 필요**).

- **v6.0 (2026-07-31)** — 유호님 "지침서 제대로 분석" 지시 → 전수 분석(memory `directive-analysis-2026-07-31`) → 채택분 일괄 개정. ①**검증 결정표 신설** — "반박 1패스/단일 패스" 용어 혼동 + 순수 판정·진단의 티어 빈칸을 표 1개로 병합(기존 뉘앙스 전부 보존) ②**세션보드 선언화** — 보드 108줄·약 6.2만 토큰 비대(자기 규칙 "10줄 유지" 위반) → 줄=선언만, 보고는 메모리·docs로 + 옛 완료 줄 일괄 정리 ③**Code.js 구역 병행 조항** — 07-31 3세션 동시 선언·커밋 봉쇄 실사고 → 관행(함수 구역 명시+커밋 직전 재확인) 조항화 + 워크트리 경고(버전 동시 발번 실사고) ④**유호님 개별 확정 결정 > 지침 일반 조항** 우선순위 1줄 ⑤**메모리 인덱스·세션보드도 압축 대상**(시즌 1회 통합) ⑥**GPT·Codex 스택 하차**(유호님 07-31: "지금 GPT 안 쓰거든? GPT 관련 모든 것 삭제") — `AGENTS.md`·`GPT_지침사본`·`오케스트레이션_가이드`·`.agents/` 미러 삭제(복원은 git 이력), Codex 오케스트레이션 섹션·계열 교차 조항 제거, 사본 3종→1종(COWORK), 사본 감시 주체=Code 세션 명시(붙여넣은 사본은 스스로 낡음을 모른다 — 실드리프트: v5.2 재복붙 3일 미추적) ⑦**유호님 방향 조항** 신설(자동화 시스템 최선호·꼬리를 무는 집요한 최상 추구 — 07-31 직접 선언) ⑧전면 압축(유호님 지시 — "전면 압축은 명시 지시 시만" 단서를 evolve v1.1에 명문화).
- **v6.1 (2026-08-01)** — 유호님 질문("지침서가 토큰을 낭비하는 편이야?") → 상시 로드 실측(memory `context-budget-2026-07-31`). **판정: 지침서가 아니라 세션보드가 범인** — CLAUDE.md 4,005자 대 보드 38,700자(39줄, 상태 칸이 문단짜리 보고서)로 **9배**. v6.0의 '선언화' 조항이 **11일 만에 다시 무너진 재발**이므로 프로즈로 한 번 더 쓰는 대신 **기계로 옮겼다**(신뢰성 조항의 두 번째 실행): `board-guard` 훅(`.claude/hooks/board-guard.js`, PreToolUse Edit|Write|MultiEdit) — 칸 200자 초과·표 14줄 초과 편집을 차단, 회귀 테스트 7종(`tests/board-guard.test.js`, 실 보드 상태 비의존). 멀티 세션 규약에 **칸 200자·표 12줄 상한 + 아카이브 경로**를 명문화하고, 전문은 `docs/세션보드_아카이브.md`로 이관(보드 38,700→2,404자). 반박에서 "훅이 막는데 조항이 왜 또 필요한가"가 나왔으나 — 훅은 Edit/Write만 잡고 bash 쓰기·타 도구는 뚫리며, 훅은 "어디로 옮길지"를 알려주지 못한다 — 조항은 **숫자+아카이브 경로 한 문장**으로 최소화해 통과. 곁가지: 메모리 인덱스 9,199→8,036자(링크 문법이 76%라 절반 감량은 불가로 판명 — 🚫/⏳ 결정 플래그를 지우는 절감은 손해). ⚠ **`wc -m`은 이 환경에서 바이트를 센다** — 한글 문서 크기가 3배로 보인다(v6.0 항목의 "6.2만 토큰"도 같은 오차 위). 사본: COWORK는 멀티세션 규약을 애초에 뺀 포팅본이라 **본문 무변경 → 스탬프 유지·재복붙 불필요**.
- **v6.2 (2026-08-01)** — v6.1 당일 후속(유호님 "나머지도 전부 진행"). 근거 = v6.1의 **12줄 단일 상한이 반나절 만에 걸렸다**(하루에 세션 6개 이상이 각자 줄을 추가). 상한을 하나에서 둘로 쪼갬: **활성(완료 아닌) 12줄 · 전체 18줄** + 세션 종료 시 **내 완료 줄을 아카이브로 옮기는 것**을 규약화(`board-guard`가 양쪽을 기계 차단, 회귀 테스트 7→10종). 조율이 실제로 필요한 대상은 활성 줄뿐이고 완료 줄은 기록일 뿐이라 같은 자로 재면 둘 다 잘못 잰다. 곁가지로 `Code.js`의 `SYNK_VERSION` 주석 한 줄(10,328자·항목 41개)을 `docs/버전_이력.md`로 분리(상수 줄 107자) — 코드를 읽는 세션마다 딸려오던 비용 제거 + 버전 범프 diff가 1만 자 한 줄 충돌에서 짧은 줄 교체로 바뀐다. 사본 무변경.

- **v6.3 (2026-08-01)** — 유호님 `/evolve` 호출. 근거 = **조율 단위가 repo 파일이라 파일 밖 공유 자원에서 사고가 반복된다**는 신호 2계열. ①**버전 번호 동시 발번 5회**(v9.80 3중·v9.89·v9.94·v9.100·v9.106 선점 — memory `version-collision-multisession`, "규약에 배정 규칙만 없다"고 07-31부터 대기) ②**08-01 Glide 3중 편집**(세 트랙이 30분 안에 같은 Button Block·point_logs — 액션 소실로 불필요한 재생성 지시 발생, 서로의 변경을 결함으로 오인한 허위 보고 2건, **정상 행이 선택된 채 Delete 활성화**를 Cancel 1클릭 차로 회피). 반영 3건: **㉮파일 밖 공유 자원도 화면·컴포넌트 단위로 선언 + 열기 직전 `git log --oneline -10`**(보드는 세션이 끝나야 갱신돼 실시간성이 없다) **㉯버전은 커밋 직전 확정·재번호는 내 추가 라인만·문서 파일명에 버전 금지**(v9.106 실측 — 재번호가 rename까지 번짐) **㉰비가역 게이트에 '조준' 축 추가** — 승인은 *행위*에 대한 것이고 오늘 빈틈은 *대상 좌표가 승인 후 밀린 것*이라 별개 축(실행 직전 선택된 대상의 내용을 다시 읽어 대조). 반박에서 깎인 것: 초안의 "열기 전 `git log`면 막는다"는 **오늘 사고엔 무효**(내가 연 15:50엔 상대 커밋 16:01·16:09이 아직 없었다) → 효과를 "예방"이 아니라 **"조기 발각·가정"**으로 낮춰 통과. **기각**: "자원도 보드에 적어라" 단독 조항 — 오늘 세 트랙 모두 "Glide 라이브"라고 이미 적었고 못 막았다(구체 단위 요구로 흡수) · 세션보드에 **버전 선점 칸 신설**(보드 비대·워크트리 격리·실시간성 부재로 같은 실패, memory 후보 A) · 버전충돌 테스트를 **타 워크트리 미커밋본까지 확장**(실제 사고 패턴은 같은 워크트리 다중 세션이라 현행 테스트로 이미 유효 — 근거 대비 이득 낮음). 압축: 워크트리 경고 문장 1개를 ㉮㉯에 흡수(멀티세션 규약 6→8줄, 순증 2). 사본: ㉮㉯는 멀티세션 규약이라 COWORK 해당 없음(포팅본에서 제외된 절). 단 **㉰는 COWORK에도 있는 조항**이라 동반 개정했다(대상을 Notion 행·Canva 디자인·공유 시트로 포팅) → **본문 변경 · 스탬프 v6.0→v6.3 · ✅재복붙 완료(당일, 유호님)** — cowork 지침란은 이때 **처음** 채워졌다(v4.0부터 이어지던 재복붙 대기의 실체 = 붙여넣은 적이 없었던 것. 사본 감시는 "스탬프 비교"만으로는 빈 칸을 못 잡는다). ⚠ 초안에서 "COWORK 해당 없음"이라고 단정했다가 실제로 파일을 열어보고 뒤집었다 — **사본 영향 판정은 grep으로 확인한 뒤에 쓴다**.

- **v6.4 (2026-08-01)** — 유호님 `/evolve` 호출(v6.3 당일 후속). 수집한 신호 5건 중 **3건은 v6.3이 이미 흡수**(버전 배정·파일 밖 공유 자원·`git log` 완화책) → 재론하지 않고 남은 2건만 반영, **줄 수 순증 0**(신설 대신 기존 두 줄에 흡수). ①**조율 주체를 「유호님 경유」에서 「세션 간 직접 메시지」로.** 근거 = 08-01 6세션 동시 가동 중 **물어본 1곳만 사고가 없었고**(옆 세션 회신 "먼저 물어본 쪽이 옳았다 — 나머지 셋은 서로를 모른 채 들어갔다"), 내 메시지 4건이 중복 조립·중복 삽입을 실제로 차단했다. 반박에서 "세션끼리 합의하면 유호님이 모르는 결정이 생긴다"가 나와 **경계를 조항에 박아** 통과 — 오갈 수 있는 건 실행 조율(편집권·순서·중복 방지·인계 통지)뿐이고 **방향·판정·비가역 승인은 유호님**. 전달 불가 세션(스케줄·원격) 폴백 1구절 포함. 보강 근거 = memory `decision-queue-bottleneck-2026-07-31`(유호님 최대 병목 = 결정 대기 큐 15건+ — "조율은 유호님께"가 그 큐를 키운다). ②**git 밖 파일은 백업 대조.** 근거 = 08-01 급여 인센티브 정본을 옆 세션이 **11분 새 3회 저장**(07:30·07:40·07:41)하는 중에 편집했고, 백업 diff로 "순수 추가·삭제 0"을 확인해 덮어쓰기를 배제했다. 반박 3발 — "v6.3 ㉮(조기 발각)로 커버된다" → **아니다. ㉮는 *누가 만지는지*이고 이건 *무엇을 덮었는지*다**(내가 읽고 쓰는 사이 저장되면 조준 시점엔 정상이라 사전 게이트로 구조적으로 못 잡는다) · "비가역 게이트의 조준 대조와 중복" → 조준은 사전, 백업은 사후 검증 · "훅으로 기계화가 정석 아닌가"(신뢰성 조항) → **타당하나 Desktop 경로는 repo 훅 범위 밖**이고 모든 외부 쓰기에 걸려 오탐이 크다. v6.1 board-guard 전례대로 **프로즈 먼저 · 재발하면 훅**(예고 조항은 소음이라 문서엔 쓰지 않음). 압축: 두 후보를 신설 줄이 아니라 기존 줄에 흡수해 **멀티세션 규약 8줄 유지**. 사본: COWORK에 멀티세션 규약 **grep 0건 → 무영향 · 재복붙 불필요**(v6.3이 남긴 "사본 영향은 grep으로 확인한 뒤 쓴다" 교훈을 이번엔 선행 적용).
- **v6.5 (2026-08-01)** — v6.4 당일 후속(유호님 "그 한 줄 지금 붙여줘"). **v6.4 ①의 전제가 검증되지 않았다는 신호를 그 조항 안에 흡수**(신설 0줄, 한 문장 추가). 근거 = 08-01 실측 2건 — ㉠세션 간 조율에 **에이전트용 `SendMessage`를 써서 두 세션 모두 `No agent named ... is reachable`로 전량 실패**(맞는 도구는 `mcp__ccd_session_mgmt__send_message`) ㉡성공 응답조차 *"queued; processed after the in-flight turn **if that session stays healthy**"* — **읽힌 보장이 없다**. v6.4의 폴백은 "전달 불가 세션(스케줄·원격)이면 유호님께"로 **처음부터 불가인 경우만** 잡고, 진짜 위험인 **「보낸 줄 알고 편집에 들어가는 경우」**는 못 잡는다 — 조율을 아예 안 한 것보다 나쁘다(근거 없는 안심이 생겨 확인을 건너뛴다). 반영 = **조율했으면 그 사실을 커밋 메시지·보드 줄에 한 줄 남긴다** — 메시지는 유실돼도 `git log`는 남고, **v6.3 ㉮("열기 직전 `git log --oneline -10`")가 수신자를 같은 정보로 데려간다**(두 조항이 짝을 이뤄, 통보 채널이 죽어도 정보가 도달하는 이중화). 새 조항을 만들지 않고 ①의 폴백 문장에 붙인 이유 = v6.4가 세운 "순증 0" 기조와 같고, 별도 조항이면 ①을 읽는 사람이 그 한계를 못 본다. 자기 적용 실증: 이 개정 사실을 v6.4 개정 세션에 메시지로 통지하고 **그 통지 사실을 이 커밋 메시지에도 남겼다.** 사본: COWORK에 멀티세션 규약 **grep 0건 → 무영향·재복붙 불필요**(선행 확인).

- **v6.6 (2026-08-01)** — v6.4 ②의 **예시가 반나절 만에 사실과 어긋나 정정**(조항 취지는 불변). v6.4가 "git 밖 파일(Desktop 정본 등)"이라 쓴 직후, 그 조항의 근거 사고를 계기로 유호님이 이관을 지시해 **Desktop 정본 51종이 `docs/정본/`으로 들어갔다**(커밋 `7ea31be` — md5 전량 대조·`.claspignore` 허용목록이라 라이브 무영향). 대표 예시가 반례가 됐으므로 그대로 두면 다음 세션이 "정본은 git 밖"으로 오인한다. 수정: 예시를 **실제 잔여**(`Desktop\SYNK 보안\`·`SYNK STUDIO\`·최상위 PDF)로 교체하고 **"근본 해법은 백업이 아니라 git 안으로 옮기는 것"**을 명시 — 백업 절차가 정답처럼 굳어 이관을 미루는 것을 막는다. 조항 본문(편집 전 사본·편집 후 대조)은 유지(잔여 대상이 실재하고, 보안 폴더는 유호님이 직접 분류한 것이라 확인 없이 옮기지 않았다). 교훈 = **조항에 박은 구체 예시는 그 자체가 유지보수 대상이다.** 원칙은 안 늙어도 예시는 늙는다 — 사고를 근거로 쓴 조항일수록 그 사고가 해결되면 예시가 반례로 뒤집힌다. 사본: 멀티세션 규약이라 COWORK 무영향.

- **v6.7 (2026-08-01)** — 유호님 지시(강사 지표 트랙 제안 → 판정 위임). **충돌 해소 직후 `node --check`** 를 멀티세션 규약에 신설. 근거 = 08-01 실측 2회(v9.87·v9.113) — 두 세션이 파일 끝에 나란히 추가해 충돌 경계가 함수 중간을 갈랐고, 마커 3줄을 지울 때 닫는 `});`가 함께 사라져 **파일 전체가 로드 불가**가 됐다. 둘 다 **테스트 파일의 마지막 닫힘**이었다. **반박에서 살아남은 이유 = "이미 /deploy 2단계와 clasp-guard가 `node --check`를 한다"에 대한 답**: 그 둘은 **배포 시점**이고 충돌 해소는 배포와 무관하게 일어나므로, 그때까지 깨진 파일을 들고 다니게 된다. 더 결정적으로 **깨진 게 테스트 파일이면 그 테스트가 자기 파손을 못 잡는다** — 게이트는 "안전 테스트 실패"로 막지만 진짜 원인(충돌 해소 실수)을 가리키지 못해 진단이 엉뚱한 곳으로 간다. `safety.test.js`의 "Code.js 구문이 정상이다"도 같은 한계(자기 자신은 검사 못 함). 기계화 검토: PostToolUse 훅으로 `.js` 편집마다 검사하는 안은 **편집 중간 상태에서 오탐**이 나고 비용이 붙어 보류 — v6.1 board-guard·v6.6과 같은 순서(프로즈 먼저 · 재발하면 훅). 순증 1줄(멀티세션 규약 8→9): 억지 압축 대신 근거로 정당화 — 61행(git status·백업 대조)은 "내 작업만인가/남의 것을 덮었나"이고 이 조항은 "해소하다 내가 깨뜨렸나"로 실패 모드가 다르며, 61행은 v6.6에서 이미 4문장으로 과밀하다. 출처 표기: 사고 자체는 **강사 지표 트랙의 관측**이라 이미 수리돼 내가 직접 재현할 수 없으므로 조항에 보고 출처를 명시했다(검증 불가능한 숫자는 지어내지 않는다). 사본: COWORK grep 1건은 검증 결정표의 "다른 규칙과 충돌하면"이라 **무관 → 무영향·재복붙 불필요**.

- **v6.8 (2026-08-01)** — 유호님 지시로 **동시 발번 횟수 정정: 5회 → 9건**. 고치려고 대조해 보니 **강사 지표 트랙이 알려온 "6회"도 부분 집계**였다 — 그쪽 목록(v9.80·81·87·100·107·112)과 내 메모리 목록(v9.80 3중·89·94·100·106)이 겹치는 건 v9.80·v9.100 둘뿐이고, **합집합은 고유 버전 9개**(v9.80·81·87·89·94·100·106·107·112). 각 세션이 "내가 뒤로 밀린 건"만 세기 때문에 어느 쪽도 전체를 보지 못한다. 그래서 숫자에 **"두 트랙 관측 합집합이라 하한"**을 함께 박았다 — 커밋 메시지에는 재번호가 대부분 남지 않아(git 실측 명시 1건뿐: `907eb17`) 정확한 총계를 셀 방법이 없고, "검증 불가능한 숫자는 지어내지 않는다"에 따르면 **셀 수 없다는 사실 자체를 표기**하는 것이 정직하다. ⚠ 내가 직전에 "숫자는 행동을 안 바꾸니 다음 /evolve까지 두자"고 판단했으나 유호님이 정정을 지시했고, **그 지시가 옳았다** — 고치는 과정에서 6도 틀렸다는 것과 "세션마다 자기 것만 센다"는 구조적 맹점이 드러났다. 숫자를 고치는 일이 실제로는 계측 방식의 결함을 찾는 일이었다. 사본: 멀티세션 규약이라 COWORK 무영향·재복붙 불필요.

- **v6.9 (2026-08-01)** — v6.6이 고친 예시가 **같은 날 두 번째로 낡아** 재정정(조항 취지 불변). 유호님 승인으로 `Desktop\SYNK 보안\`의 사업 문서 5종도 `docs/정본/`으로 이관됐고(`cf694c2`), 그 폴더엔 이제 **자격증명 `로그인.txt` 하나만** 남았다 — 실측 확인. 예시를 갱신하면서 **자격증명 예외를 명문화**했다: 근본 해법은 git 안으로 옮기는 것이지만 **자격증명만은 옮기는 게 아니라 영원히 밖에 둔다**(비공개 repo여도). 이관 트랙이 `.gitignore`에 `**/로그인.txt`·`*비밀번호*`·`*password*`를 넣어 **기계로도 막아** 뒀다(`git check-ignore`로 차단 검증 후 테스트 사본 제거). **v6.6의 교훈이 하루에 두 번 증명됐다** — 「원칙은 안 늙어도 예시는 늙는다」. 사고를 근거로 쓴 조항일수록 그 사고가 해결되면 예시가 반례로 뒤집히고, **해결이 빠를수록 더 빨리 뒤집힌다**(v6.4 예시는 반나절, v6.6 예시는 두 시간). ⚠ 이 항목을 처음 기록할 때 **bash 백틱이 명령 치환으로 해석돼 경로·커밋해시가 전부 빈칸으로 커밋됐다**(같은 실수 당일 2회 — 메모리 갱신 때도 발생). 이력에 코드 인용을 넣을 땐 bash 인라인 대신 **스크립트 파일로 쓴다.** 사본: 멀티세션 규약이라 COWORK 무영향.

- **v6.10 (2026-08-02)** — 유호님 `/evolve` 호출. 근거 = 유호님 신고("주간 한도를 2~3일에 다 쓰고 남은 4일을 못 쓴다" = 가용의 **57% 손실**)를 실측으로 추적한 결과(memory `context-budget-2026-07-31` 2차 진단). ①**컨텍스트 비용 조항 신설**(실행 섹션) — 92개 세션 681.7MB 중 최대 세션이 **119MB·5,034이벤트**, 이벤트당 평균 24,872자(정상 대화 턴의 수십 배). 범인은 **누적분 두 갈래**였다: 스크린샷 448장(전체 이벤트의 8.9%인데 **용량의 93.2%**)과 최대 소스 파일 135만 자(≈38만 토큰) 전체 읽기. 둘 다 **유호님이 아니라 AI(나)의 도구 선택** — Glide 조작에서 `read_page`(텍스트, 훨씬 쌈) 대신 스크린샷으로 확인했고, 함수 하나가 필요할 때 파일을 통째로 읽었다. ②**"잴 수 있으면 추정하지 말고 재라 + 재기 전에 어느 층을 잴지 정하라"**를 판정·검증의 숫자 조항에 흡수(순증 0) — **v6.1이 같은 질문("지침서가 토큰을 낭비하나?")에 이미 답했는데 범인을 놓쳤다.** 그때는 "매 턴 상시 로드분"(지침·인덱스·보드)만 쟀고 그 층은 실제로 잘 관리되고 있었다(08-02 재실측: 합 ~17K 토큰). **층을 잘못 고르면 정확한 측정이 틀린 결론을 준다** — v6.1의 보드 압축은 옳았으나 그것으로 문제가 해결됐다고 믿은 것이 8일간 진짜 원인을 가렸다.
  - ⚠ **자기 적용 실패가 이 개정의 직접 근거다** — 유호님께 "절제로는 30~40% 절감이 한계"라고 **재보지도 않고 추정해 답했다가** 실측이 뒤집었다. 잴 수 있는 것이었다(트랜스크립트는 로컬에 있었다).
  - 반박이 잡은 부작용 1건: "세션을 작업 단위로 끊어라"는 **v6.2의 보드 줄 상한(활성 12·전체 18)과 정면 충돌**한다(하루 6세션이면 즉시 참). → 조항에 **"같은 트랙이면 보드 줄을 이어 쓴다"**를 함께 박아 통과.
  - **예시에 파일명을 박지 않았다**("`Code.js` 135만 자" ✗ → "최대 소스 135만 자" ○). v6.6·v6.9가 하루에 두 번 증명한 「원칙은 안 늙어도 예시는 늙는다」의 **선행 적용** — 분할이 끝나면 그 이름은 곧 반례가 된다.
  - 기계화(신뢰성 조항) **보류**: 스크린샷 횟수 훅은 좌표 클릭에 필요한 정당한 촬영까지 막고 임계값 근거가 없다. v6.1 board-guard·v6.6·v6.7과 같은 순서(프로즈 먼저 · 재발하면 훅).
  - 07-27 외부 진단 ④ "`Code.js` 크기 상한 조항"은 **기각 유지** — 이번에 실제 사건이 생겼으나 처방은 상한 조항이 아니라 **읽는 방식**(부분 읽기)과 **파일 분할**(유호님 승인·별건)이다.
  - 사본: COWORK 34행에 같은 숫자 조항 존재(grep 선행 확인) → **동반 개정 · 스탬프 v6.3→v6.10 · ⏳재복붙 필요.** ①은 Claude Code 전용 도구명이라 COWORK 무영향.

## 재론 금지 — 정착·거절된 것 (근거는 위 버전 항목)
- 자기검증 루프 **3회**(5회 아님), 모델명 기반 루프 생략 **금지**, "자기검증했다" 문구 대신 **잡아 고친 것 1개**.
- **"무거운 작업 사전 규모 고지"** 조항 — 거절됨(소음). 새 근거 없이 재제안 금지.
- 이 지침의 절차 정본은 **스킬 파일 한 곳**에만 둔다(forge=SKILL.md, 진화=evolve SKILL.md). 다른 문서엔 요약만 — 절차 본문 복제 금지.
- 마찰 신호 기록은 **굵직하거나 반복되는 것만** — 사소한 교정 1건마다 기록하는 무한정 발동은 v4.0 게이트에서 과잉적용으로 기각됨.
- **07-27 외부 진단 5건 — 기각(근거 게이트 미통과)**: ①`CLAUDE.md`에 '새 작업 컨텍스트 복원' 절 추가 — AGENTS.md 고유 섹션은 v3.5 후속의 **의도적 설계**이고 evolve SKILL이 보존을 명시, 실제 사건 근거 없음 ②응급복구 절차를 지침에 연결 — 런북 경로는 **월간 복구 리허설 메일**이 이미 안내(AI 조항보다 유호님께 직접 도달) ③사본 동기화 해시 검사 — 실측 드리프트 **0**(CLAUDE↔AGENTS 차이는 의도적 델타, 스킬 미러 차이는 줄바꿈), 예방 논거뿐 ④`Code.js` 크기 상한 조항 — 관찰(948KB)뿐이고 크기에 기인한 실제 사건 없음 ⑤docs 아카이브를 지침 조항으로 — `docs/문서_지도.md`가 정본/폐기를 이미 관리. 전부 **새 근거(실제 사건) 없이 재제안 금지**.

## v6.11 (2026-08-03) — 커밋 범위를 프로즈에서 훅으로

- **추가 · 커밋 범위 기계 강제**: `git commit -m "..." -- 경로들`을 1순위 형태로 명문화하고, `git add -A`·`git commit -a`를 **`.claude/hooks/git-scope-guard.js`(신설)**가 PreToolUse에서 차단. 근거 = 마찰 F006·F013·F014·F015 **동종 4건**(08-01~03). 인덱스는 저장소당 하나인데 세션은 5~6개가 같은 작업 트리를 공유해, `add`와 `commit`을 나눠 부르면 그 틈에 남의 스테이징이 얹힌다. 08-03 F015는 성격이 달랐다 — **경합이 아니라 `-A` 자체의 범위 과잉**으로 유호님의 git 밖 정본(사업문서 2종)을 push까지 보냈고, **같은 폴더의 자격증명이 안 나간 이유는 `.gitignore` 한 줄뿐**이었다. 그래서 조항에 「**git 밖에 두기로 한 것은 「추가하지 않기」로는 안 지켜진다 — `.gitignore`로 지킨다**」를 함께 박았다. 메모리 `uncommitted-swept-by-peer`에 대책이 **다섯 번** 적혀 있었는데도 반복됐다 = 프로즈 실패의 증명이라 기계로 옮긴 것. 회귀 `tests/git범위가드.test.js` 6건 — **통과해야 하는 목록을 차단 목록과 같은 무게로** 검사한다(과잉 차단은 BYPASS 습관을 만든다 · 커밋 메시지·heredoc 안의 문자열은 명령으로 세지 않는다 = clasp-guard 08-01 오탐 교훈 승계).
- **추가 · 「읽기를 좁히는 것과 검사를 자르는 것은 다르다」**(v6.10 컨텍스트 조항의 부작용 교정): F016 — `git diff --cached --stat | tail -5`로 잘라 봐서 앞쪽의 남의 파일 3건을 놓쳤다. 절약 대상(읽기)과 전량 대상(빠뜨리면 사고가 되는 목록)을 갈랐다.
- **추가 · 판정의 층**: 되돌림 비용이 큰 판정(권한·공개 범위·삭제)은 코드 독해에서 멈추지 말고 상태를 바꿔보고 되돌린다. 근거 = 08-03 리포트카드 실측(「폴더를 잠가도 안 깨진다」가 독해로는 옳아 보여 구현자·적대 리뷰어를 모두 통과했는데 실제로 잠그자 전제가 무너졌다 — 개별 공유는 몇 달간 실패 중이었고 catch가 「대체했다」로 로그해 실패를 성공처럼 보이게 했다). ⚠**반박 패스에서 잡아 완화한 것**: 이 조항이 그대로면 「바꿔보라」가 사고를 유발한다 — 실제로 그 실험은 학부모 화면을 깨진 채로 남겼다. 「되돌리는 절차를 먼저 정하고 실사용자가 닿는 시간대·경로는 피한다」를 붙였다.
- **압축**: 동시 발번 사고의 버전 번호 9개 나열 삭제(`bump-version.js` 채번 락으로 근본 해소됐고, 번호는 git 이력·메모리에 있다 — 지침에서 값어치 0). 이번 사이클은 **순증가**다 — 추가 3건이 전부 실사고 근거를 가졌고, 압축으로 뉘앙스를 깎을 만한 조항이 없었다.

## v6.12 (2026-08-03) — 장치를 만드는 것과 그 장치가 도는 것

유호님 `/evolve` 호출. 재료 = 마찰 장부 실측(전체 23건 · 살아있음 3) + 이번 사이클에서 새로 발견한 5건.

- **추가 · 「세 번째면 원인을 쓸 수 없게 만든다」**(신뢰성). 근거 = F010 월키 Date 오염 **네 번째**. 기존 조항은 "두 번이면 시스템 결함"까지만 말하고 **무엇을 다르게 하라**는 말이 없어서, 네 번 모두 「그 자리」에 회귀를 하나씩 더 붙이는 처방이 나왔다. 3회차부터는 호출부가 아니라 **통로**를 바꾼다(공용 헬퍼·타입·입력 차단 + 옛 통로를 테스트로 금지). ⚠ F010은 **닫지 않았다** — 이 조항은 코드 작업을 지시할 뿐 스스로 막지 못한다. 조항으로 신호를 닫으면 F020(엣지는 심었는데 낡음을 볼 눈이 없어 절반만 해소)을 그대로 반복하는 것이다.
- **추가 · 「장치와 트리거는 같은 커밋에서」**(신뢰성). 근거 = F019. 08-03 오전에 그래프 도구 3종을 만들고 **같은 날 오후에 재보니 아무도 돌리지 않았다** — `CLAUDE.md`는 그 도구들을 언급조차 안 했고(grep 0건), 마찰 3건이 `/evolve` 기준 2건을 넘긴 채 방치돼 있었다. 기존 조항("기계로 옮긴다")이 **만드는 데서 멈추게 하는 구조**였다. 스스로 발화하는 훅에는 적용하지 않는다(반박 패스에서 좁힘 — 안 좁히면 훅마다 불필요한 의례가 붙는다).
- **추가 · 「가드·회귀가 눈머는 두 가지」**(신뢰성). 근거 = F021·F023, 둘 다 이번 세션 실사고. ①`board-guard`가 `완료`만 알고 실제 표기 `**완료**`를 몰라 완료 줄을 전부 활성으로 셌다 — **테스트 16건이 통과하는 채로** 새 세션이 자기 줄을 못 넣고 막혔다(아무도 안 쓰는 표기로만 검사한 결과). ②낡은 인용 3건을 **실저장소에서 세는** 회귀를 썼다가 그 3건을 고치자마자 스스로 깨졌다 — 회귀가 「버그가 아직 있을 것」을 요구하면 다음 사람은 테스트를 고치는 게 아니라 끈다. 탐지 능력은 픽스처로, 실저장소는 거짓양성만.
- **기계화 · 메모리 인덱스 상한**(`.claude/hooks/memory-index-guard.js` 신설, 회귀 12건). 근거 = F012, 그리고 **프로즈 실패의 가장 선명한 사례**: "인덱스는 지도다. 한 줄에 판정 본문을 쓰지 않는다"가 **그 파일 맨 위에** 적혀 있는데도 107줄 중 25줄이 어겼다(>400자 17줄 · 최장 2,637자 = 한 줄이 인덱스의 9% · 상위 5줄 = 26%). 헤더가 스스로 "다이어트 역주행 +75%"라 적고도 계속 늘었다. 인덱스는 **매 세션 전량 로드**돼서 병행 6세션이면 하루 6번이라, 작업량과 무관하게 매번 새는 유일한 층이다.
  - **임계값을 추정하지 않고 쟀다** — v6.10이 "임계값 근거 없음"으로 스크린샷 훅을 보류한 전례를 따랐다. 실측 중앙 136자·**75분위 229자** → 상한 **250자** = 이미 4분의 3이 지키는 선. 새 규칙을 발명한 게 아니라 잘 쓰인 줄들의 실측 상한을 못박은 것.
  - **검사 대상은 이번 편집이 새로 넣는 줄뿐이다.** 전체를 검사하면 기존 25줄이 인질이 되어 인덱스를 아예 못 고치게 되고, 그러면 다음 사람은 규칙을 지키는 게 아니라 훅을 끈다(v6.11 「과잉 차단은 BYPASS 습관을 만든다」). Write 전문 교체 시에도 **디스크에 없던 줄만** 새 줄로 센다 — 압축·재배치가 훅에 걸리면 다이어트 자체가 불가능해진다. 회귀 2건이 이 성질을 [핵심]으로 못박았다.
  - ⚠ **자기 적용**: 이 조항을 쓰던 중 내가 같은 세션에 넣은 인덱스 줄이 **404자**임을 발견해 먼저 압축했다. 못 지킬 규칙을 추가하지 않는다.
- **압축·수리 · 「git 밖」 예시를 판별 규칙으로 교체**(멀티세션). 그 예시가 **네 번째로 낡았다**(v6.4 → v6.6 → v6.9 → 오늘). 실측: `SYNK STUDIO\`도 자격증명도 이제 Desktop이 아니라 `docs/정본/` **안**에 있고, 최상위 PDF는 존재하지 않는다 — 목록 3항 전부 반례. 나열을 버리고 **판별 기준(위치가 아니라 「git이 보느냐」) + 실측 명령(`git check-ignore -v`·`git status --ignored`)**으로 바꿨다. 규칙은 이관이 진행돼도 안 늙는다.
  - 🔴 **이 대조에서 새 위험 형태를 찾았다**: 「저장소 안에 있는데 무시되는 파일」이 가장 위험하다 — 나머지와 같은 자리에 있어 보호받는 것처럼 보이는데 git은 못 본다. 자격증명은 **차단이 확인됐다**(`.gitignore:17 **/로그인.txt` · 미추적 · 이력에 들어간 적 없음). 즉 사고가 아니라 **지침의 세계관이 낡은 것**이었다.
- **닫지 않은 신호 2건 — F010(월키 Date 4회) · F017(bump-version이 옆 세션 이력 줄을 덮는다).** 둘 다 처방이 코드고 이번 사이클은 문서다. 조항으로 닫으면 다음 사이클이 "해소됨"으로 보고 지나친다.
- 사본: 신뢰성·멀티세션은 COWORK가 **의도적으로 제외한 repo 전용 절**이라 본문 무변경 → **스탬프 유지(v6.11)**. 기존 ⏳재복붙 대기(v6.3 → v6.11)는 그대로.

## v7.0 (2026-08-03) — 전면 압축: 규칙은 남기고 서사는 이력으로

유호님 호출. 계기 = 「카파시가 지침에서 검증 루프 지침을 최대한 빼라는데 맞나? + 지침이 길다, 압축 최대한」(전면 압축은 유호님 명시 지시 시만 — 충족).

- **카파시 실측 대조(2026-08 웹 검색·원문 수집)**: 「검증 루프 지침을 빼라」는 발언은 **확인되지 않았다** — 카파시발로 유통되는 지침 정본(4원칙)의 4번이 오히려 *"Define success criteria. Loop until verified."*이고, 2026-06 확장판(10규칙 · 진위 미확인·본인은 공개 언급 회피)은 검증 규칙을 **추가**했다. 2026 「루프 엔지니어링」 담론의 실제 주장 = 검증 제거가 아니라 **「검증을 프로즈 지시문에서 구조(게이트·훅·별도 검증자·테스트)로 옮기고, 지침 파일은 짧게(상단=내구 판단 · 절차=스킬/잎사귀)」**. 이 저장소의 마찰 장부 실측(해소 26건의 절대다수가 프로즈 조항이 아니라 훅·도구)이 같은 방향을 독립적으로 증명해 왔다.
- **판정**: 검증 결정표·분리 반박·비가역 게이트는 빼는 게 아니라 **토큰 절약 장치다**(v3.4 실측 재확인: 지침 20% 감량=360토큰 vs 심층감사 1회=600만 토큰 — 결정표가 바로 그 남발을 막는 기준선. 빼면 검증 강도의 기본값이 사라져 과잉·과소 양쪽으로 샌다). 뺀 것은 **본문에 눌어붙은 사건 서사** — 각 조항의 근거는 이 파일에 버전별로 전부 남아 있어 정보 손실 0.
- **실측**: CLAUDE.md 8,290자/69줄 → **5,728자/66줄(–31%)**. 전문 보존 = `docs/_archive/CLAUDE_v6.12_압축전.md`.
- **신규 흡수 1건**: 「유호님이 배치한 파일·폴더·환경의 변경은 빈틈 보완이 아니라 방향 결정 — 반영하지 말고 제안으로」(제안·선제 보완 · 근거 = 마찰 F027 ARC 폴더 무단 이동, 이 조항으로 해소).
- **검증**: 분리 반박용 독립 리뷰어 에이전트가 세션 한도로 사망 → **기계 대조 + 수동 반박 1패스로 대체**: v6.12 규범 키워드 83종 전수 포함 확인(0건 누락) · 참조 파일·훅·스킬 13종 실존 확인. 잡아 고친 것 2건 = ①제안 기록 조항의 「수락·보류·거절 무관」 유실 ②인덱스·보드 「시즌 1회 통합」 주기 유실.
- **동반**: 메모리 인덱스 2차 압축(최장 4줄 → 250자 이하 · **18,584→16,023자**, 전문 `docs/_archive/MEMORY_인덱스_2026-08-03_압축전.md` · 압축 전 토픽 파일 4종에 본문 실재를 대조 확인) — 압축 중 `memory-index-guard` 훅이 내 초과 줄을 **4회 실제 차단**해 상한을 강제했다(v6.12 장치의 실증).
- 사본: COWORK 동반 압축 **v7.0**(4,758→4,119자 · 본문 변경).
- **⛔ 그리고 같은 날 그 사본을 동결했다** — 유호님 확인("cowork 아예 안 쓰고 있는데 붙여넣어야 하나?") → **붙이지 않고 대기를 닫는 것**으로 판정(A안). 근거 ①전례 = GPT 사본 폐기(v6.0, "지금 GPT 안 쓰거든")와 같은 축 ②같은 날 확정된 AI 스택 가이드에 cowork **grep 0건**(스택 밖) ③그 ⏳ 항목이 v6.3부터 **8판째** 인덱스·보드·이력 세 곳에 실려 매 세션 로드돼, 안 쓰는 도구를 위해 압축분을 도로 까먹고 있었다. **폐기가 아니라 동결**을 고른 이유 = 복원 비용 0 — 파일은 남기되 **머리말에 재개 3단계를 자기완결로** 박아 파일이 스스로 상태를 말하게 했다(v6.12 「장치와 트리거는 같은 커밋에서」의 적용). `/evolve` **5단계(사본 동기화)는 건너뛰도록 스킬 본문에 명문화** — 안 하면 다음 사이클이 재복붙을 또 대기로 올린다. 되살아나는 조건 = 유호님의 cowork 재사용 통보뿐.
  - 곁가지 실측: 카드뉴스 목적으로 cowork을 되살릴 필요가 없음을 확인했다 — **Claude Design은 cowork이 아니라 별도 창구**(`claude.ai/design`·데스크톱 사이드바 · Pro/Max/Team 베타 · 사용량만 공유). 이 저장소에는 `DesignSync` 도구가 있어 브랜드 키트 정본을 그 창구의 디자인 시스템으로 밀어넣을 수 있다.

## v7.1 (2026-08-04) — 미추적 파일 보호 + 재시도 실패의 번역 금지

마찰 F025·F029 도달(기준 2건) → 유호님 승인으로 실행.

- **F025**(옆 세션 정리에 미추적 새 파일·codex/ 소멸): ①기계 = `git-scope-guard`에 **`git clean` 차단** 추가(dry-run `-n`만 통과 · 경로를 좁혀도 남의 신작을 지우는 성질이 그대로라 커밋과 달리 경로 예외 없음 · 회귀 8건) ②프로즈 = 멀티 세션 규약에 「**새 파일은 미추적 상태로 오래 두지 않는다** — 미추적은 이력·stash·reflog 어디에도 없는 유일한 무보호 상태」. 한계 명시: 훅은 Claude 세션의 Bash만 본다 — 훅 밖 도구의 정리는 ②가 노출 시간을 줄여 막는다.
- **F029**(재시도 8회 실패를 「불가」로 접어 유호님께 넘김 → "게으른 판단" 교정 · 실제 원인은 30분 만에 특정): 판정·검증에 「**같은 방법의 재시도 실패를 「불가」로 번역하지 않는다** — 넘기기 전 층을 바꿔 근본원인 후보 1개 특정」. memory `no-impossible-verdicts`가 있었는데도 재발 → 지침 승격.
- 압축 후보: v7.0 전면 압축 + 같은 날 인덱스·보드 압축 직후라 **이번 사이클 없음**.

## v7.2 (2026-08-04) — 로컬 초록은 CI 초록이 아니다 + 조율 단위에 GitHub 설정

마찰 F031·F032·F035·F036·F037·F038·F039 (살아있는 10건 중 7건이 세 갈래로 묶였다) → 유호님 `/evolve` 호출.

- **F036·F039 (최대 신호 — 같은 날 두 세션이 각각 밟음 = 개인 실수 아닌 시스템 결함)**: 회귀가 repo 밖(`~/.claude/.../memory`) 경로를 읽어 CI를 깨뜨렸고, 그 **빨간 CI 위에서 라이브 배포가 나갔다**(로컬 「643건 fail 0」을 초록이라 보고). 같은 계열로 이 사이클 직전의 **CI 12연속 빨강**도 있었다 — 엔진이 `tz` 인자를 받아놓고 로컬 게터로 포맷해 UTC 러너에서만 날짜가 밀렸다(v9.150). 셋의 공통 원인은 하나다: **로컬 머신에는 CI에 없는 것이 있다**(로컬 시간대·홈의 정본·자격증명).
  - ①기계 = **`tools/test-ci.js` 신설** — 같은 스위트를 **빈 HOME·TZ=UTC**로 돌려 CI를 모사한다. **발동 조건을 같은 커밋에 담았다**: `/deploy` 2단계의 러너를 `node --test`에서 이것으로 교체(v6.12 「장치와 트리거는 같은 커밋에서」).
  - ②조항 = 신뢰성에 「repo 밖 환경에 기대는 검사는 CI에서 반드시 깨진다 / 실저장소 검사는 있을 때만 돌리되 **skip으로 드러낸다**(통과와 미실행이 같은 모양이면 안 된다) / **로컬 초록은 CI 초록이 아니다**」.
  - ⚠ **탐지 능력을 실험으로 못박았다** — 초록은 탐지의 증명이 아니므로(memory `guard-must-check-result`), 일회용 탐침 테스트로 **볼트가 있는 머신에서 평소 러너는 pass 1 / 같은 상황에서 `test-ci.js`는 fail 1**을 실측한 뒤 탐침을 제거했다. 이 실험이 없었으면 "돌려봤더니 초록"만 확인하고 끝났을 것이다.
  - 🔑 곁가지 발견: 볼트 경로가 **cwd 기준**이라 **워크트리에서도** 실저장소 검사가 조용히 skip된다(그래서 `/evolve` 1단계의 `memory-graph`도 이 워크트리에서 실패했다 — 개정 재료를 모으는 도구가 개정 사유를 재현했다). 그 검사는 메인 워크트리에서만 실제로 돈다.
- **F031·F032 (공유 시크릿 덮어쓰기)**: 조율 단위에 repo 밖 자원은 있었으나 **GitHub 설정(Secret·배포키)이 목록에 없어** 열기 직전 확인을 안 했고, 남의 배포키가 고아가 됐다. 멀티세션 규약에 **GitHub Secret·배포키를 조율 단위로 명시** + **덮기 전 `gh secret list`로 갱신 시각까지 본다**(존재 여부만 보면 덮은 게 안 보이고, 덮인 뒤 증상은 「조용함」뿐 — 토큰이었으면 발송·수거 봇이 갈라졌다). v6.3에서 기각된 "자원도 보드에 적어라"와 다른 것은 **열거가 아니라 판별 방법**을 넣었다는 점이다.
- **F035·F038 (`git status --short`가 rebase 진행 중을 감춘다)**: 기계는 이미 있었다(`git-scope-guard` 규칙④가 진행 중 커밋 차단). 남은 구멍은 **조항이 `--short`를 허용하는 것처럼 읽히는 것** — 범위를 확인하는 습관이 오히려 상태를 가려 커밋이 detached HEAD로 들어갔다. 「공유 파일 편집 전·커밋 전 `git status` **전문**」으로 정정하고 **일상 조회는 `--short` 무방**을 함께 박았다(컨텍스트 조항과의 충돌을 반박이 잡아냈다).
- **F037 (rebase --abort 스윕)**: 기존 조항은 **미추적** 파일만 보호했는데(F025), 추적 파일의 **미커밋 편집**은 남의 `rebase --abort`가 작업 트리를 되돌릴 때 함께 쓸린다. 두 실패 경로를 **한 문장으로 합쳐** 순증을 막았다(처방이 「즉시 커밋」으로 같다).
- **기각 2건**: **F030**(Glide 조립가이드는 지도지 현재 상태가 아니다) — 단발이고 처방이 도구별 조작 지식이라 memory `glide-remote-assembly` 행. **F033**(빠르게 변하는 제품은 검색 결과가 아니라 그 날짜를 본다) — 유호님 정정이 있었으나 단발이고, 「단정 금지」·「검증 불가능한 숫자는 지어내지 않는다」가 이미 인접 조항으로 존재. 새 근거(재발) 없이 재제안 금지.
- 압축: 신규 불릿 1개(신뢰성) 순증, 나머지 3건은 기존 줄 안에 삽입 — 그중 F037은 F025 문장과 **병합**해 별도 조항 신설을 피했다. 별도 압축 대상 없음 — 메모리 그래프 실측이 깨진 링크 0·고아 0·인덱스 불일치 0으로 건강했고, 세션보드는 같은 세션에서 이미 6줄→3줄로 정리했다.
- 사본: cowork **⛔동결**이라 5단계 건너뜀(v7.0 판정).
