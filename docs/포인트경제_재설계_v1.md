# 포인트 경제 재설계 v2 — 지급 ÷2 · 과잠 = 재원 12개월 자격
<!-- v2 = 경제 2세대(1차 인플레 수리) · 파일명 _v1 = 문서 판 번호 — 축이 다르다. 현행 수치 정본 = Code.js 상수 PT -->


> 2026-07-31 · 유호님 지시("포인트가 너무 후하다") → 당일 확정·구현 완료 **[v9.83]**.
> 확정 사항 4: ①지급 ÷2 ②**과잠 = 12개월 수강생 무료 지급**(스토어 하차) ③목표 카드 날짜화 ④보스 HP 재보정.
> 근거는 전부 `Code.js` 실측. 학생 0명(개원 2027-02) — **지금이 무비용 변경 시점**이었다.
>
> ⚠ **[v9.147에서 수치 일부 개정 — 2026-08-03]** 기능 압축(수집기 우선)으로 지급표가 바뀌었다: 첨삭확인 2→**1** · **재작성 3**(주 1회) 신설 · **퀴즈응답 1**(1일 1회·정답 무관) 신설 → 실측 295.4P/월(구 291.5P 대비 사실상 중립) · 보스 HP 추정식에 새 경로 반영. 현행 수치의 정본 = `Code.js` 상수 `PT` + memory `app-feature-compression-2026-08-03`. 이 문서의 **원칙**(지급 ÷2·과잠 자격·HP 종속변수)은 그대로 유효.
> ⚠ **리그·월드레이드는 v9.147부터 시즌 오프다**(`LEAGUE_ON`·`WORLD_RAID_ON = false` — 08-11 실측) — 위 295.4P 는 **둘(월 약 8.9P)을 뺀 실효값**이고 그 사실이 여기 안 적혀 있었다. 진화 앵커 대조: 실효 295.4P 로도 싱크마스터 2,400/295.4 = **8.1개월로 이미 설계 앵커(9개월)를 밑돌고**, §12-3 확정(유지·발전)대로 되살리는 날 304.4P → **7.9개월**로 더 벌어진다. ⏳**되살림과 단가 재판정을 같은 트랙으로 묶는다** — 재보정 없이 스위치만 켜지 않는다.

---

> 🔴 **2026-08-26 정정 — 이 문서의 «앵커 둘» 중 하나가 은퇴했다.**
> 이 문서는 포인트 단가를 **앵커 두 개**로 재정했다: ㉠과잠 1,700P ≈ 성실 5~6개월 · ㉡싱크마스터 2,400P ≈ 성실 9개월.
> 유호 확정(「7단계 캐릭터는 이제 없어」)으로 **㉡이 죽는다.** 뒤를 잇는 가이드 세 축은 재료가
> 포인트가 아니라 «자기 손으로 맞힌 문법 수 · 처음 해 본 일의 갈래 수 · 자기 목표 과제를 낸 날 수»라,
> **포인트 경제에 앵커를 하나도 주지 않는다.**
> ⇒ 소각 뒤 포인트 단가를 재는 자는 **㉠ 하나뿐이다.** 여기 적힌 판정(지급 ÷2)은 이미 집행됐고
> 그 근거는 그대로 유효하되, **다음번 단가 조정 때는 ㉡을 근거로 쓸 수 없다.**
> ⚠ `tests/safety.test.js:1923`(`2400 / hard >= 7`)은 **지금 고치지 않는다** — 임계값이 라이브에서
> 아직 돌기 때문이다. 그 줄은 **소각 커밋에서 같이 걷는다**(먼저 걷으면 도는 것을 지키는 가드가 사라진다).

---

## 1. 진단 — 설계 의도가 2배로 새 있었다

Code.js 헤더 §67 설계 앵커: **"성실 월 ~280P · 과잠 6개월(1,700P)"**
`docs/몬스터_진화_임계값_v1.md`: **"싱크마스터 2,400P = 성실 9개월"**

즉 **원래 설계가 이미 유호님 기준과 같았다.** 문제는 그 뒤로 지급 경로가 6개(첨삭확인·왕관 2종·레이드·리그·월드레이드·개근왕) 늘어나는 동안 **아무도 총합을 다시 안 봤다는 것**이다.

### 변경 전 실측 (평일반 · 월 21.7 수업일)

| 항목 | 단가 | 열심히 | 보통 |
|---|---:|---:|---:|
| 숙제완료 | 10P/일 | 217 | 130 |
| 첨삭확인 | 5P/일 | **109** | 65 |
| 레이드보상 | 20P/주 | 86 | 86 |
| 출석정산 | 3P/회 | 65 | 55 |
| 개근왕 칭호 | 30P/월 | 30 | 0 |
| 칭찬 | 3P/일 | 26 | 12 |
| 왕관(MVP·시냅스) | 10P | 22 | 15 |
| 리그승리 | 5P/주 | 11 | 11 |
| 월드레이드 | 10P/월 | 5 | 5 |
| 생일 | 20P/년 | 2 | 2 |
| **합계** | | **572P/월** | **381P/월** |

**과잠(1,700P) = 열심히 3.0개월 · 보통 4.5개월.** 목표의 정확히 **2배**. 같은 인플레가 진화에도 걸려 **싱크마스터 4.2개월**(설계 의도 9개월)이었다.

최대 범인은 **첨삭확인 +5P**다. "숙제완료 +10의 절반"으로 정했는데, **탭 한 번짜리 행동이 매일 반복**되는 걸 계산에 안 넣었다. 월 109P — 숙제 다음으로 큰 소득원이 손가락 한 번이었다.

---

## 2. ① 지급 ÷2 — 가격은 그대로

두 레버는 스토어만 놓고 보면 수학적으로 동일하다(간식 40P/일 26P나, 80P/일 52P나 3일). 갈리는 건 **스토어 밖**이다.

| | 지급 ÷2 | 가격 ×2 |
|---|---|---|
| 스토어 19종 | ✅ 무수정 | 19행 재기입 |
| 진화 임계 7단계 | ✅ 자동 복귀(8.2개월) | 별도 ×2(contents 시트+문서+게이트) |
| 반 보스 HP | ✅ 난이도 정상화 방향 | 그대로 무의미하게 쉬움 |
| 월드보스 HP | ✅ 같음 | 그대로 |

**지급을 낮추면 세 경제가 동시에 원래 설계로 돌아온다.** → 지급 인하.

### 신규 단가 (`Code.js` 상수 `PT` — 단일 소스)

| 항목 | 이전 | 신규 | 비고 |
|---|---:|---:|---|
| 숙제완료 | 10 | **5** | 여전히 최대 단일 소득원 |
| 출석정산(1회) | 3 | **2** | ÷2(1.5)보다 올림 — ~~출석은 바닥 보장~~ ⚠**08-08 확정(보상=학습 층만)과 충돌** — 아래 ⏳ 블록 |
| 첨삭확인 | 5 | **2** | ÷2보다 더 깎음 — 탭 1회가 제일 싸야 한다 |
| MVP 왕관 | 10 | **5** | ⚠ Glide 버튼(**값 이중 정본** · 이관처=전수분석 §11 학생상세 강2 — talk 미이식) |
| 시냅스 왕관 | 10 | **5** | ⚠ Glide 버튼(**값 이중 정본** · 이관처=전수분석 §11 학생상세 강2 — talk 미이식) |
| 칭찬(태그 4종) | 3 | **2** | ⚠ Glide 버튼(**값 이중 정본** · 이관처=전수분석 §11 학생상세 강2 — talk 미이식) |
| 레이드보상 | 20 | **10** | |
| 리그승리 | 5 | **3** | |
| 월드레이드 | 10 | **5** | |
| 개근왕 칭호 | 30 | **15** | ⚠출석일수 조건 — 출석정산과 같은 충돌(아래 ⏳ 블록) |
| 레이드영웅 칭호 | 20 | **10** | |
| 생일축하 | 20 | **10** | |

⚠ **08-08 「출석 2층 구조」 확정과의 충돌(08-11 실측 · 수치는 안 건드리고 표식만 남긴다)** — 출석정산·개근왕 두 경로(열심히 월 43.4+15=**58.4P** · 시뮬 하드 304.4P의 19.2%, 현 실효 295.4P 기준 **19.8%**)는 **운영 출석 일수**에 보상을 거는 자리다(`엔진_운영배치.js` 정산 실측). 「보상은 학습 층(숙제)에만」 확정이 없앤 축이 라이브 지급 코드에 그대로 산다. ⏳유호 택1: ㉠학습 출석(숙제 제출일) 기준으로 이관 ㉡폐지 후 다른 학습 경로로 재배분. ⚠어느 쪽이든 **문서·`PT`·`tests/safety.test.js`를 한 벌로** — 두 경로를 빼면 304.4−58.4=**246.0P** 로 하한 250P 검사가 먼저 깨진다.

### 결과 (스토어 가격 무수정)

| | 월 소득 | 진화 2,400P |
|---|---:|---:|
| 열심히 | **291P** | 8.2개월 |
| 보통 | **196P** | 12.2개월 |

### 🔒 재발 방지 — 이게 진짜 수리다

원인은 "누가 숫자를 잘못 썼다"가 아니라 **단가가 10곳에 흩어져 있어 아무도 총합을 볼 수 없었다**는 것이다. 그래서 값만 고치지 않고 구조를 고쳤다.

1. **`PT` 상수 = 유일한 지급 단가 소스.** 모든 지급 지점이 `PT.숙제`처럼 참조한다.
2. **`tests/safety.test.js`에 월간 소득 시뮬 3종 추가** — 단가 하나가 아니라 **총합**을 계산해 상한(310P)·과잠(≥5개월)·진화(≥7개월) 앵커와 대조한다. 지급 경로를 하나라도 추가하면 `PT` 항목 수 검사가 먼저 깨져 눈에 띈다.
3. **숫자 직기입 금지 검사** — 지급 행에 `[sid, 10, '숙제완료'` 같은 리터럴이 남아 있으면 테스트가 실패한다. 인플레가 되살아날 유일한 경로를 기계로 막았다.

---

## 3. ② 🧥 과잠 = 스토어 상품이 아니라 **재원 12개월 자격** (유호님 확정)

> 유호님 07-31: "과잠 지급은 **12개월**로 변경. 6개월은 비용이 너무 많이 들 것 같아. **12개월 수강생에게만 무료로 지급**."

### 왜 스토어에서 뺐나

과잠은 소속의 상징인데, 스토어에 놓는 순간 ①"포인트 주고 산 옷"이 되고 ②모으는 기간 내내 간식·굿즈(40~120P)를 **한 번도 못 사게** 만든다. 하위 티어 즉시보상 루프가 큰 상징에 통째로 잡아먹힌다.

코드는 이미 **획득 누계(P열)와 잔액(AQ열)이 분리**돼 있다(v7.1). 과잠만 누계 기준 자격으로 빼면 **학생은 간식을 사면서도 과잠 시계가 계속 돈다.**

### 규칙

| 조건 | 값 | 상수 |
|---|---|---|
| 재원 개월 | **12개월 이상** | `JACKET_TENURE_MONTHS` |
| 누적 획득 P | **1,200P 이상** | `JACKET_MIN_POINTS` |
| 지불 | **없음(무료)** — 잔액 무차감 | — |

**누계 하한 1,200P의 뜻** = "12개월 등록만 해두고 거의 안 나온 학생" 배제선. 신규 단가 기준 12개월 누계는 **열심히 3,498P · 보통 2,356P · 주말반 열심히 약 1,380P** 이므로 셋 다 통과하고 유령만 걸린다.

### 부수 효과 — 주말반 문제가 같이 풀린다

주말반은 토요일만이라 수업일이 평일반의 1/5(월 4.3일)이다. 누계 절대값(1,700P)을 기준으로 하면 주말반은 **14.8개월**이 걸려 같은 상품에 영영 못 닿았다. **재원 개월 기준으로 바꾸니 반 유형과 무관하게 같은 시점에 도달한다.**

### 구현

- `setupStore()`에서 **ST18 싱크 과잠 하차**(판매 중단, 나머지 19종 가격 불변)
- `jacketWatch_()` — 매일 아침(`morningJobs`) 자격 도달자 감지 → **`jacket_grants` 시트** 적재 + 원장 메일
- 멱등(이미 적재된 학생은 스킵) · role=student 필터 · 도달 0명이면 시트·메일 모두 무동작(무비용)
- 실물 전달 후 시트의 「지급상태」를 **지급완료**로 — 🔴 **08-11 실측: 이 열을 읽는 코드가 0곳이다**(헤더 정의 2곳 + 원장 안내문뿐). 목표 카드 분기(`Code.js:2988`)는 자격 도달이면 지급 여부와 무관하게 「🎉 자격 도달! 곧 전달해 드려요」를 **영구 표시**한다 — 12개월을 기다려 받은 순간이 화면에 아무 변화를 안 만들고, 받은 뒤에도 같은 문구가 계속 떠 안내가 거짓이 된다. 처방(경험 축): 카드 분기가 `jacket_grants` 지급상태를 읽어 「지급완료 → 받은 날짜·축하 기록」으로 갈리게 한다 — 구현은 별건.

---

## 4. ③ 💡 목표 진행 카드를 **날짜**로 (한 수 더)

> ⚠ **실행층 = 구 Glide/시트 층.** 네이티브 이관 시 이 절의 배선은 전수분석 학17(목표진행 BZ)이 인수한다 — talk 앱에는 아직 0줄(08-11 실측).

지급을 절반으로 깎으면 학생 체감은 "멀어졌다"이다. 그런데 **이미 계산 중이던 재료**(`records.p14`/`pPrev14` = 최근 28일 획득)로 그걸 뒤집을 수 있다.

**이전** (profiles BZ78):
> 🎯 목표: 싱크 텀블러 — 잔액 340P / 필요 450P · **110P** 더 모으면 교환 가능!

**이후**:
> 🎯 목표: 싱크 텀블러 — 잔액 340P / 필요 450P · **110P** 더 모으면 교환 가능!
> 📅 지금 페이스(하루 9.7P)면 **2026년 8월 12일** 도착 · ⚡ 하루 5P만 더 모으면 **2026년 8월 8일**

**찜한 상품이 없으면 🧥 과잠 진행으로 폴백**한다 — 과잠은 스토어에서 하차했으니 찜으로는 영영 뜨지 않는데, 모든 학생의 기본 장기 목표가 바로 그것이기 때문이다.

> 🧥 **싱크 과잠** — 재원 **7/12개월** · 누적 **1,840P**/1,200P ✅
> 포인트 조건은 이미 충족 — 남은 건 시간이에요. **2027년 3월 1일**에 자격 도달

**왜 장식이 아닌가**: 숫자가 작아진 게 아니라 **도착 예정일이 생긴 것**이 된다. 하락을 서사로 바꾼다. "하루 5P만 더"는 숙제 1회분이라 조건부 약속이 아니라 산수 그대로다 — 포인트가 아니라 **습관**을 판다. 추가 스캔 0, 새 열 0.

정직성 가드: 페이스 데이터가 없으면 날짜를 지어내지 않고 "며칠 더 모으면 도착 예정일이 계산돼요"로, 2년 넘는 예측은 아예 말하지 않는다.

---

## 5. ④ ⚔️ 보스 HP 재보정 — 지급의 종속 변수

반 보스 HP = 반 정원 × 계수, 데미지 = 그 반 학생들의 **주간 양수 포인트** 합. 즉 지급 단가를 바꾸면 난이도가 같은 비율로 따라 움직인다. 구 계수는 인플레 전 값이라 **실측 3.0배 오버킬**(격파가 사실상 자동)이었다.

| | 이전 | 신규 | 상수 |
|---|---:|---:|---|
| 평일반 계수 | 28 | **28**(유지) | `RAID_HP_PER.평일` |
| 주말반 계수 | 18 | **8** | `RAID_HP_PER.주말` |
| 월드보스 1인당 | 100 | **150** | `WORLD_HP_PER` |

- **1차 판정(평일 34·주말 14)은 독립 리뷰에서 기각됐다** — 주간 획득 추정에 레이드 보상 자체를 넣은 **순환 가정**이었다(격파의 결과를 격파의 원인으로 셈). 보상을 빼고 재계산한 1인 주간 획득은 평일 반 평균 **44.4P** · 주말 **12.7P**여서, 여유 1.6배 기준으로 평일은 **28 원복**·주말은 **8**이 맞다.
- **HP 산정에 실인원 반영** — `cap = min(정원, 실인원)`. 정원으로만 잡으면 개원 초 9명 반이 20명분 데미지를 내야 해 구조적으로 격파 불가였다. 보스가 파티 규모를 따라간다("매주 같은 HP"는 유지 — 로스터가 바뀔 때만 움직인다).
- 주말을 크게 낮춘 이유: 수업일이 평일의 1/5인데 계수는 평일의 64%(18/28)여서 원래부터 주말반 보스가 훨씬 어려웠다. 지급 ÷2 후에는 만실이어도 격파가 불가능해진다.
- ⚠ **학생 0명이라 추정치다. 개원 후 첫 달 실측으로 1회 재조정할 것** — `class_stats` 7열 '반주간데미지' 실값 ÷ 정원 = 실제 계수 상한. 그래서 하드코딩을 상수로 뽑았다.

---

## 6. 라이브 반영 상태 (2026-07-31 23:33 원격 실행)

> ⚠ **이 절의 실행층(Glide 버튼·시트)은 구층이다** — Glide 폐기·네이티브 재구축 확정. 칭찬 4태그+왕관의 이관처=전수분석 §11 학생 상세(강2), 스토어·찜·목표카드=학17. talk 앱에는 아직 0줄(08-11 실측) — 이관 전까지 이 절은 운영 기록으로 유효하다.

### ✅ 완료 — `setupStore` ▶ 원격 실행

Apps Script 편집기에서 원격 실행(23:27:03 시작 · 360초 · 완료됨). **실측 검증**(gviz로 시트 원문 직접 조회):

- **ST18 싱크 과잠 행 소멸 확인** → 리뷰 B2의 이중 경로(구매·무료지급) **해소**
- store 19종 잔존, 가격 전부 불변(40P 간식 ~ 2,000P 촬영)
- **전 타입 행 수 무결** — boss 12·braintip 30·cheer 7·cheermail 30·fuel 6·grammar 72·homework 210·label 15·lore 11·monster 7·quiz 100·reach 2·reason 9·season 12·worldboss 1 (전부 `CONTENT_EXPECT`와 일치)
  > ⚠ **위 수는 «이 검증을 돌린 날»의 값이다 — 고치지 않는다**(고치면 감사 기록이 거짓이 된다). 그 뒤 `CONTENT_EXPECT` 가 넷 움직였다: **grammar 72→91**(08-31 요목 Lv1~5 전건 편입) · **lore 11→4**(08-27) · **monster 7 은퇴**(함께한날 막7 · 시트 7행은 남되 기대에서 뺐다) · **guide 3 신설**(막1 가이드 셋). 지금 값을 알고 싶으면 `Code.js` 의 `CONTENT_EXPECT` 선언부가 정본이다.

> ⚠ **동시 실행 위험이 실제로 있었다.** 내 `setupStore`가 도는 6분 사이 다른 세션이 `setupHomework`·`setupQuiz`·`injectMongolianContents`를 같은 `contents` 시트에 실행했다. `replaceContentType`은 "전체 읽기 → 해당 type 제외 → 통째 재기입" 구조라, 스냅샷 이후 남이 쓴 행을 덮어쓸 수 있었다. 위 행 수 검증이 통과했으므로 **이번엔 유실 없음**. 앞으로 setup 계열 ▶는 **한 번에 하나씩**.

### 🕙 자동 처리 — 몽골어 복원

`replaceContentType`이 재기입한 store 행은 G열(몽골어)이 비워진다. 이건 **손으로 돌릴 필요가 없다** — `translateContents`가 `nightJobs`에 편입돼 **매일 밤 빈 번역 60행씩 자동 소진**한다(v9.41). store 19행 ≪ 60행이라 다음 야간 1회로 끝난다.
(오늘 야간 배치는 23:20에 이미 지나갔으므로 복원은 **다음 날 밤**. 그동안 몽골어 상품명만 공란 — 학생 0명이라 영향 없음.)

### ✅ 완료 — Glide 버튼 6종 (이미 조립·정확했음)

**2026-08-01 원격 전수 실측 결과, 왕관·칭찬 버튼은 이미 조립돼 있고 값도 전부 v9.83 사양과 일치한다.** 위치 = 반 → 반 상세 → 학생 리스트 → 학생 상세(「내 기록」)의 Button Block 1개.

| 버튼 | points | reason | student_id | given_by |
|---|---:|---|---|---|
| 🌟 오늘의 MVP 왕관 | **5** | `오늘의 MVP` | user_id | User/이름 |
| ⚡ 오늘의 시냅스 왕관 | **5** | `오늘의 시냅스` | user_id | User/이름 |
| 🗣 발음↑ | **2** | `칭찬·발음↑` | user_id | User/이름 |
| 🔥 열정 | **2** | `칭찬·열정` | user_id | User/이름 |
| 🤝 친구도움 | **2** | `칭찬·친구도움` | user_id | User/이름 |
| 🎯 집중력 | **2** | `칭찬·집중력` | user_id | User/이름 |

부가 배선도 정상 — `created_at` = Current date/time · `log_id` 공란(야간 calcAll이 채번) · MVP 액션에 **`Allow action when role is teacher`** 조건.

#### 08-01 마무리 — 조립 주체와 잔여 결함 처리

이 버튼들은 **「학생 역할 기능 검토」 세션이 07-31에 원격 조립**한 것이다(가이드 §8-1이 이미 5·2로 갱신돼 있어 값이 처음부터 정확했다). 그 세션은 실제로 버튼을 눌러보는 실전 테스트까지 했고, **눌러야만 드러나는 결함 1건**을 남겼다:

> 🔴 **칭찬 4개의 `created_at`에 학생 등록일이 찍힌다** — Glide가 Add Row 액션을 만들 때 이름이 같은 `profiles.created_at`을 자동 매핑한 것. point_logs의 `연월`(G열)은 `Code.js`가 **created_at 기준으로 자동 기입**하므로, 그 칭찬 2P가 **엉뚱한 달의 월간 포인트·랭킹·주간 레이드 데미지**로 굳고 `dailyGuard`의 칭찬 1일 1회 한도도 날짜 비교라 **무력화**된다.

**처리 결과(08-01, 이 트랙이 이어받아 마감)** — 6개 액션 전수 재확인:

| 액션 | points | reason | created_at |
|---|---:|---|---|
| 🌟 MVP · ⚡ 시냅스 | 5 | 오늘의 MVP / 오늘의 시냅스 | Current date/time |
| 🗣 발음↑ · 🔥 열정 · 🤝 친구도움 · 🎯 집중력 | 2 | `칭찬·{태그}` | **Current date/time**(교정 완료) |

추가로 이 트랙이 **잔재 2건을 발견·제거**했다:
- 🔥 열정 액션에만 **달력 아이콘이 잘못 설정**돼 있었다(다른 5개는 비어 있음 — created_at 교정 중 Icon 드롭다운을 건드린 것으로 보임) → Clear로 제거, 버튼 6개 표시가 균일해졌다.
- point_logs 마지막 행에 **테스트 행 1줄**(log_id·created_at 공란 · DEMO-01 · `칭찬·집중력` · 김재헌21)이 남아 있었다 → 삭제. 82행 → **81행**, 마지막이 정상 정산 행(PL020)임을 확인. 같은 화면의 78~81행은 시스템 출석정산이라 보존.

> ⚠ **내 오판 정정(2회).** 07-31에 "버튼 미조립"이라고 두 번 보고했는데 **틀렸다.** 원인은 학생 상세의 COMPONENTS 목록을 스크롤하다 Rich Text 7개에서 멈추고, **그 아래 Button Block을 못 본 채 "없다"고 단정**한 것이다. 목록의 끝을 확인하지 않고 부재를 단정하면 안 된다.
> 따라서 §2 시뮬의 **291P/월이 그대로 맞다**(한때 "실지급 263P"라고 적었던 것도 이 오판의 파생이라 철회).

---

## 7. 독립 리뷰 대응 (2026-07-31 · reviewer 서브에이전트)

3트랙 통합 커밋(9d1282e) 뒤 결합 diff 전체 독립 리뷰에서 **제 트랙에 블로커 3건**이 나왔고, 전부 타당해 수리했습니다.

| | 지적 | 판정 | 조치 |
|---|---|---|---|
| **B1** | 상수는 모았는데 **화면·메일 문자열 8곳이 옛 숫자** — 학생에게 "+10P"라 말하고 5P를 지급 | 타당. 제 누락 | 문구 14곳 `PT.*` 참조로 치환 + **"PT 미경유 `+숫자P` 리터럴 금지" 테스트** 추가 |
| **B2** | `setupStore` ▶ 전까지 라이브에 과잠 잔존 → 구매·무료지급 **이중 경로** + 과잠 찜 학생은 **죽은 카드**에 갇힘 | 타당 | 찜 값이 과잠이면 자격 카드로 흡수 + `jacketWatch_`에 **잔존 감시(dedup 메일)** 신설 + 배포 절차 명시 |
| **H1** | 계수 34는 **순환 가정**(격파 보상을 데미지 추정에 포함) + 실인원 9명 반은 격파 불가 | 타당 | 보상 제외 재계산 → 평일 **28 원복**·주말 **8** + `cap = min(정원, 실인원)` |

B1의 나머지 1건(랭킹 넛지의 숙제 환산)은 **v9.85 트랙이 먼저 더 나은 방식으로 해결**했습니다 — 빌더에 상수를 참조시키지 않고 호출부가 `meta.hwP: PT.숙제`를 주입해 순수성을 지켰습니다.

## 8. 검증

- `node --check Code.js` 통과
- `node --test tests/safety.test.js` — **109 tests / 104 pass / 0 fail**
- 실측 시뮬: 열심히 **291P/월** · 보통 **196P/월** · 12개월 누계 3,498P/2,356P(하한 1,200 통과)

**작업 중 잡아 고친 것 2**
1. `WORLD_HP_PER`를 상단 상수 블록으로 옮겼더니 기존 테스트 4건이 깨졌다 — 그 테스트들이 `const WORLD_HP_PER`를 코드 구간의 **끝 표식**으로 쓰고 있었다. 선언 위치를 원복하고 값만 바꿔 해결.
2. v9.81 리그 카드 테스트의 동점 픽스처가 S2를 `rank: 1`로 만들어 **동점 분기와 1위 분기를 겹쳐** 놓고 있었다 — v9.85가 "공동 1위도 왕좌"로 바꾸자 이 단언이 무고하게 깨졌다. 동점 검사를 공동 2위로 옮기고, 공동 1위=왕좌를 별도 단언으로 고정.
