# 저장소 정리 — 2026-09-09

필요 없는 실험·낡은 전송 사본은 복구 가능한 압축본으로 옮겼고, AI가 처음 읽는 안내는 줄였다. 회사 정본·브랜드 원본·진행 중 작업은 보존했다. 세 모델이 모든 파일을 자동으로 읽는 구조가 아니라, 같은 원본에서 이번 일에 필요한 부분을 찾아 읽는 구조다.

## 1. 조사 범위와 한계

2026-09-09 07:50 KST 목록 조사에서 파일 **121,956개/121,956개**, 디렉터리 11,603개의 이름·크기·위치를 조사했다. 접근 오류는 0건이며 정션·심볼릭 링크 9개는 중복 순회를 피하려고 따라 들어가지 않았다. 파일 크기 합계는 약 62.63GB로, 연결·공유 파일 때문에 실제 점유 디스크 용량과 같다고 보장하지 않는다.

Git 추적 Markdown 676개/676개를 정확한 내용 해시로 비교했고 완전히 같은 문서 묶음은 0개였다. 이것은 뜻이 겹치는 문서가 없다는 뜻이 아니다. 추적 경로 중 일반 파일로 잡히지 않은 102개는 Higgsfield 스킬의 정션 사본 경로이며 `.agents/skills`에 원본이 있다.

내용 검토는 AI 진입 지침, 공통 운영 원칙, 문서 지도, 과거 운영 규약, 실행 도구의 참조·의존 관계에 집중했다. **12만 파일의 모든 문장·그림·사실을 전부 검증한 것은 아니다.** 다른 작업도 같은 저장소를 사용 중이므로 파일 수는 조사 시점의 수치다.

## 2. ARC-AGI-3-Agents는 무엇인가

ARC-AGI-3는 새로운 상호작용 환경에서 AI의 문제 해결을 평가하는 시험이고, `ARC-AGI-3-Agents`는 그 시험용 에이전트 예제 코드다. [ARC 공식 설명](https://arcprize.org/arc-agi/3), [공식 코드 저장소](https://github.com/arcprize/ARC-AGI-3-Agents).

SYNK에서는 과거 `tools/bench`의 모델별 코딩 비교 과제에 쓰였다. 회사 엔진·학생 서비스·일상적인 GPT/Claude/Gemini 업무의 필수 부품은 아니다. 폴더만 남겨 두어도 모델 지능이 올라가지는 않는다. 필요할 때 다시 비교시험을 할 수 있는 선택 자료다.

로컬 사본은 별도 Git 저장소였고 보관 직전 수정·미추적·무시된 변경은 없었다. 상위 SYNK 저장소는 이 폴더를 무시하므로, SYNK 커밋만으로는 원본을 복구할 수 없었다. 따라서 별도 Git 이력까지 포함해 압축했다. 일부 예제는 OpenAI API를 직접 호출하므로 실행 비용을 개인 채팅 구독에 포함된다고 간주하지 않는다. 이번에는 해당 예제를 실행하지 않았다.

## 3. 실제로 보관·제거한 것

| 대상 | 보관한 파일 | 판단 |
|---|---:|---|
| `ARC-AGI-3-Agents` | 74개/74개 | 선택 시험 코드 46개와 별도 Git 파일 28개 |
| `올릴것` | 2개/2개 | 현재 정본과 다른 옛 전송 사본 |
| `upload_screens` | 2개/2개 | 현재 정본과 다른 옛 전송 사본; 실행 도구가 필요할 때 다시 생성 |

보관한 **78개/78개**를 ZIP에서 다시 읽어 원본과 SHA-256 해시·크기를 대조한 뒤 원래 세 폴더를 제거했다. 원본 합계 2,393,426바이트 → 압축본 1,201,566바이트다. 추가로 빈 `받을것_*` 폴더 14개와 빈 `output` 폴더 1개만 제거했다. 삭제 직전에도 파일 변경·폴더 포함 관계·실행 중 사용 여부를 다시 확인했다.

- 압축본: `C:/Users/q1212/Documents/SYNK-보관함/2026-09-09/저장소정리.zip`
- 압축본 SHA-256: `8aebb801399829c5e1611d59654fef6a85a91fcc9033c2f320b6c6aa152667e9`
- 복원: 압축본을 **빈 임시 폴더**에 먼저 풀어 필요한 폴더만 옮긴다. 현재 작업 파일 위에 통째로 덮어쓰지 않는다.

이 압축본은 공개 저장소 밖의 로컬 보관함에 있다. 클라우드 백업까지 완료한 것은 아니다. `tools/bench/README.md`에도 보관 사실과 복원 위치를 연결했다.

## 4. AI가 덜 헤매도록 바꾼 것

| 대상 | 정리 전 → 정리 후 | 바뀐 점 |
|---|---|---|
| `docs/문서_지도.md` | 약 12.9KB → 2.7KB | 회사별 출발점·핵심 정본만 남김 |
| 최상위 문서 목록 | 긴 지도 본문 → 별도 상세 색인 | 문서 이름과 원본 경로를 보존; 필요할 때만 더 읽음 |
| `.gitignore` | 약 22.1KB → 5.5KB | 오래된 설명 압축; 제외 규칙과 순서 보존 |
| `.gitattributes` | 약 7.9KB → 1.0KB | 오래된 설명 압축; 처리 규칙과 순서 보존 |
| `README.md` | 약 4.3KB → 3.5KB | 현재 진입 경로·폴더 역할·정리 보고서 안내 |

위 크기는 처음 조사한 작업 파일과 정리 후 파일을 비교한 근삿값이다. 줄바꿈 형식에 따라 Git 내부 바이트 수와 조금 다를 수 있으며, 파일 크기 감소율이 곧 토큰·속도 개선율은 아니다.

세부 변경:

- `docs/자료안내/상세_색인.md`에 최상위 문서별 경로를 유지했다. 문서 검사 도구도 지도에서 실제로 연결한 이 색인을 인식하도록 수정하고 시험을 추가했다. 하위 폴더 문서까지 모두 이 목록에 나열한 것은 아니다.
- 공통 운영 원칙에 모델별 진입, 관련 원문 읽기, 다른 모델에 작업을 넘길 때 필요한 자료와 확인 범위를 명시했다. 모든 정본 전문을 상주 지침에 복제하지 않는다.
- [과거 SYNK_CONTEXT](https://github.com/unmet23-lab/synk-appsscript/blob/a02539a2b930492b1cc57c21cca105d774ce8102/docs/SYNK_CONTEXT.md)의 낡은 역할 배치를 현재 원칙 링크로 바꾸고 회사 배경은 보존했다.
- 과거 작업 사본 규약은 역사 자료임을 표시했다. 예전 자동 병합·강제 삭제 예시를 현재 실행 절차처럼 읽지 않게 정리했다.
- 오래된 장부 규칙을 새로운 승인 장벽으로 오해하지 않게 고쳤고, 디자인 수치의 중복 기재는 실제 JSON 정본 참조로 바꿨다. 정본 수치 자체는 변경하지 않았다.
- 비활성 Git 훅은 역사·공용 코드임을 명시했다. 예전 설치기를 다시 실행하라는 안내를 제거했고, 아무 훅도 새로 켜지 않았다. 비어 있는 옛 브리핑 연결은 현재 상태와 대체 조회 명령을 안내한다.

## 5. 큰데도 남긴 것

큰 폴더가 곧 쓰레기는 아니다. 다음 항목은 이번 정리에서 지우지 않았다.

- **철학·엔진 7종·결정·작업 기록:** 요약만 남기면 계약·예외·확정 근거가 사라질 수 있다. 원문을 유지하고 관련 절을 찾도록 안내했다.
- **마스코트·Loom·영상·브랜드 원본:** 생성 원본, 승인된 결과물, 영상 제작 입력이 섞여 있다. Git에 없는 파일은 다시 만들 수 있다고 단정하지 않았다.
- **별도 작업 사본:** `.claude/worktrees`의 등록 사본 10개 모두 미커밋 설정이 있었고, 그중 하나에는 독자 커밋 3개, 다른 하나에는 엔진 수정이 있었다. 조사 당시 별도 `.worktrees` 사본 1개는 형제 저장소 소유였다. 삭제 안전성이 확인된 사본은 0개/11개였다. 조사 중 다른 작업이 없앤 사본을 이번 정리 성과로 세지 않는다.
- **의존 패키지·Python 환경·공용 함수:** 이름이 실험·훅이어도 현재 도구가 참조하는 것이 있다. `.venv-comet`의 다른 평가 도구 용도, `.venv-mn`의 사용 불확실성, 스킬 정션 연결 등을 구분해 보존했다.
- **`.git`과 다른 세션 변경:** Git 이력 압축·삭제, 다른 세션 미커밋 정리, 자동 병합은 하지 않았다.

따라서 이번 정리는 대용량 전체를 지워 공간을 크게 확보한 작업이 아니다. 불필요한 활성 사본을 치우고, 헷갈리는 안내와 처음 읽는 양을 줄인 작업이다.

## 6. Claude에서 시작하면 세 모델이 모든 정본을 읽는가

**아니다. 모델별 MD 파일은 각 AI의 출발 지침이지, 세 모델을 동시에 실행하거나 모든 연결 문서를 자동 독해하는 장치가 아니다.**

| 실제로 일하는 모델 | 이 저장소의 진입 지침 | 이후 읽을 범위 |
|---|---|---|
| GPT/Codex | `AGENTS.md` | 공통 운영 원칙과 이번 작업에 관련된 정본·코드 |
| Claude | `CLAUDE.md` | 가져온 공통 원칙과 이번 작업에 관련된 정본·코드 |
| Gemini/Antigravity | `GEMINI.md` 및 제품이 적용하는 규칙 | 공통 운영 원칙과 이번 작업에 관련된 정본·원자료 |

Claude의 `@파일경로`는 문서를 가져오는 문법이지만, 일반 Markdown 링크가 있다는 것만으로 대상 전체를 이미 읽은 것은 아니다. 실제 적용 범위는 하네스와 열린 작업 폴더에 달려 있다. [Claude 공식 메모리 안내](https://code.claude.com/docs/en/memory), [Codex 공식 AGENTS.md 안내](https://learn.chatgpt.com/docs/agent-configuration/agents-md), [Antigravity 공식 규칙 안내](https://antigravity.google/docs/rules-workflows).

예를 들어 Claude가 엔진 설계를 맡으면 철학·해당 엔진 계약·현재 구현을 확인한다. GPT에 구현을 넘기면 같은 작업 사본과 필요한 계약을 전달하고 GPT도 원문을 확인한다. Gemini에 외부 조사를 맡길 때는 필요한 배경과 최신 출처 확인 조건을 전달한다. 호출하지 않은 모델은 이 과정에 참여하지 않으며, 세 모델의 대화·기억이 자동으로 하나가 되는 것도 아니다.

핵심은 **모든 자료를 매번 전부 읽기**가 아니라 **필요한 원본을 빠뜨리지 않고 현재 판으로 확인하기**다. 문서 경로를 보냈다는 사실과 수신 모델이 실제로 읽었다는 증거를 구분한다. 이를 위해 새 강제 훅이나 매번 3사 호출 절차를 추가하지 않았다.

## 7. 검증과 남은 범위

- 관련 시험 **81개/81개 통과**: 문서 지도, 세션 최신 상태, 하네스 내보내기, 외부 사실, AI 스택 점검.
- 실행 명령: `node --test --test-concurrency=1 tests/문서지도.test.js tests/session-freshness.test.js tests/하네스내보내기.test.js tests/밖의사실.test.js tests/ai스택점검.test.js`.
- 앞선 병렬 실행은 77개 중 1개가 시간 제한에 걸렸다. 기준 커밋 후보를 각각 확인하던 반복 Git 조회를 묶어 해당 경로의 조회를 6회에서 2회로 줄였다. 기존 1.7초 제한·기준 가지 우선순위는 유지했고, 잘못된 참조를 제외하는 시험 2개를 더했다. 최종 결과는 위 순차 실행의 결과이며 모든 부하 조건을 보장하지 않는다.
- 독립 검토에서 이미지 표기와 이스케이프된 텍스트를 문서 링크로 오인하는 경우를 발견해 수정하고 시험 2개를 추가했다. 현재 지도에 쓰인 정상 링크의 동작은 유지한다.
- 수정한 JavaScript 도구 3개/3개 구문 검사 통과. 제외·속성 규칙 및 Git 훅 5개/5개 파일의 주석 외 내용과 순서가 동일하다.
- 변경 안내 문서 12개를 대상으로 로컬 Markdown 링크 존재 여부를 검사했다. 별도 최상위 색인 누락 검사와 기존 문서 참조 검사도 수행했다. 이 검사는 원문 사실의 정확성이나 모든 일반 링크를 보장하지 않는다.
- 로컬 저장을 막던 오래된 0바이트 `index.lock`은 Git 관련 실행 프로세스가 없고 파일이 점유·갱신되지 않음을 확인한 뒤 임시 보관함의 `synk-stale-index-lock-0909-64c9bc9afccb46d1aab3167df4f071c9.bak`으로 옮겼다. 실제 Git 인덱스·이력·작업 파일은 삭제하지 않았다.
- 공개 저장소로의 push는 앞선 승인 차단이 해결되지 않아 재시도하지 않았다. 이번 변경은 로컬 커밋으로 남기며 공개 업로드·라이브 배포와 구분한다.
- 이 작업에서는 유료 API 호출, 학생 서비스 변경, 새 예약 작업, Codex 훅 신뢰 승인 우회를 하지 않았다. 앞서 등록한 Codex 최신 상태 알림의 네이티브 신뢰 승인 대기 상태를 이번 문서 정리로 해결했다고 주장하지 않는다.

## 8. 종료 확인 — 2026-09-09

사용자의 종료·커밋 요청에 따라 현재 상태를 다시 확인했다. 정리 변경 `88138ef49`와 공개 반영 확인 기록 `fb8f772d9`는 현재 로컬 이력에 포함되어 있으며, 이번 작업 파일에는 추가 미커밋 변경이 없었다. 별도 작업 사본이나 이 작업의 열린 PR은 만들지 않았고, Apps Script·Supabase 라이브 코드를 변경하지 않아 이번 변경의 배포 단계는 없다.

승인 당시 9개 파일은 공개 원격 반영을 확인했다. 반면 위 두 후속 커밋은 종료 시점의 원격 `master`에 없었다. 원격 검사 도구도 `fb8f772d9`를 미게시·미검증으로 판정했다. 다른 작업의 커밋이 함께 쌓인 현재 `master` 전체를 이 요청으로 게시하지 않는다. 로컬 저장과 공개 게시를 구분한다.

종료 점검의 한계와 남은 확인:

- 기존 세션별 마찰 조회는 Git 호출 실패로 이 세션의 행을 판정하지 못했다. 별도의 열린 행 조회는 0건이었지만, 이것으로 실패한 세션별 조회가 통과했다고 하지 않는다.
- 라이브 조회와 외부 회차 장부 조회를 끈 로컬 점검에서 선언된 문서 참조의 끊김은 0건이었다. 다만 기억 색인의 없는 대상 1건, `.claude/hooks/philosophy-card.js`의 오래된 CLAUDE 문서 크기 설명 1건이 수리 항목으로 나왔다. 이번 종료에서 개인 기억을 새로 수정하거나 과거 검사기를 개편하지 않았다.
- Codex 자동 알림의 최초 신뢰 승인·실제 이벤트 실행은 앞선 보고서에서 미확인으로 남아 있으며, 이번 종료 확인도 그 상태를 완료로 바꾸지 않는다.
- 다른 세션의 미커밋·미추적 파일은 그대로 보존했다. 사용자의 새 기억 갱신 요청은 없어 이번 종료에서 개인 기억을 추가하지 않았다.

이번 정리와 게시 확인의 결과는 저장되어 있다. 전체 저장소의 모든 경고 해결이나 자동 알림의 완전한 활성화까지 완료된 것으로 확대하지 않는다.
