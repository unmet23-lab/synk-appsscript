# repo 대정리 — 삭제 판정안 v2 (2026-08-05 판정 · ✅2026-08-06 실행 완료)

> ✅ **실행 기록 (2026-08-06 · 유호님 답 "① 예 · ② 권고대로")** — 드라이런 재실행 FAIL 0 → 동반 수정 `9f90d20` → **① 24건 `cd8b453`** → **②ⓑ 16건 `0f38bd2`** → **②ⓓ synk-talk/ 삭제+.gitignore 걷기 `1d6375f`** → 워크트리 3(청정·master 포함 검증 후)·병합 브랜치 12·캐시 4곳 청소 → test-ci 초록·push·원격 run. **복원 = 각 삭제 커밋 revert 1회**(ⓓ만 git 밖 — 정본 `Documents\SYNK-talk` 별존). ⓐ노션_보존=**보류 존치** · ⓒCOWORK_지침=**유지** · ③Glide 게이트 예약분(§B)은 그날 이 문서의 드라이런을 다시 돌린다.

> **지시**: 유호님 "지금까지 준비한 모든 하네스랑 md 등등 전부 이 4개년계획+회사 뇌 엔진 개발에 도움이 안되는것들은 삭제해줘"
> **기준** = `docs/제품방향.md`(4개년+코어엔진+확정 스택) · **절차** = 독립 조사 2갈래 → **적대 반박 21건** → **기계 드라이런**(삭제 목록 전건의 저장소+메모리 전체 참조 검사 · FAIL 10건 적발) → 이 v2.
> v1→v2 변화: 즉시 삭제 30→**24건**(참조 실측으로 5건 제외·3건 게이트 이관·`.codex` 철회), 워크트리 날짜 오판 정정, 「revert 1회 복구」 과장 정정.

## 승인 문항 — 여기만 답하시면 됩니다

| 문항 | 내용 | 권고 |
|---|---|---|
| **①** | **즉시 삭제 24건**(git 추적 — 삭제 커밋 1개, 복원도 명령 1개) **+ 청소 3종**(git 밖: 캐시 ~5·고아 워크트리 3·병합 완료 브랜치 12 — 전부 재생성물·복제본이라 실손실 0) | **예** |
| **②** | 보류 4묶음 각각: ⓐ노션_보존 33 ⓑ정본 트리 _구본 16 ⓒCOWORK_지침 1 ⓓsynk-talk/ ~55 | ⓐ보류 ⓑ삭제 ⓒ유지 ⓓ삭제 |
| **③** | Glide 소멸 예약(파일럿 게이트 통과일에 v947 계열 ~7건 일괄) — **지금 답 불요** | 예약만 |

## 왜 「전부 삭제」가 아니라 24건인가 (조사 결론)

- **문서 ~318건 중 현행층의 죽은 문서는 사실상 없다** — 08-04 대이동으로 이미 격리 완료. 남은 삭제 여지는 `_archive/`(인용 금지 폴더)에 몰려 있고, 그마저 **라이브 엔진 코드 5개 파일이 「정본」으로 지목하는 문서 6건**, 교재 2과 전문의 유일 원본 1건, 재제안 금지 kill-list 2건이 섞여 있어 기계 검사로 걸러냈다.
- **하네스는 죽은 파일이 거의 없다** — 훅 18/18 등록·테스트 81 CI 가동·도구 36 중 기계 호출자 없는 것은 effort-audit 1쌍뿐(삭제 목록에 포함). 하네스는 「엔진 개발」에 직접 안 쓰여도 배포·데이터·멀티세션 사고를 막는 작업 OS로 4개년에 복무한다(각 가드에 F번호 실사고 이력).
- 삭제 실익은 용량(~0.5MB)이 아니라 **죽은 문서를 열어보는 노이즈 제거**다. 진짜 부피는 유호님 판단 묶음(②)에 있다.

### 이름만 보면 죽어 보이지만 실측은 산 것 (오판 방지 기록 — 🚫이 항목들 삭제 재제안 금지)

| 파일 | 실측 |
|---|---|
| tools/notebooklm-*.js 4종 | AI_스택_가이드 §1-5 현역 배선(매일 21:00 예약 · rot-check 감시). ⚠단 **마지막 실행(08-04 21:00)은 실패** — 배선은 살아있고 실행이 실패한 것(rot-check가 짖게 돼 있음) |
| **.codex/config.toml** | v1에서 「폐지 오케 잔재」로 오판 → **기계 검사가 잡음**: tools/모델정책.js(3곳)·codex-review.js·테스트 2종이 물고 있는 현행 검수 파이프라인 구성물 |
| tools/telegram-answers-poll.js | 결정큐 봇(@synk_queue_bot) = 유호님 폰 답변 수거 통로 |
| tools/memory-push.cmd | synk-memory 동기화(예약 작업 SYNK_MemoryPush 정상 가동) |
| _보류_두뇌_웹화면.js (루트) | 코어엔진_설계.md(작성 중인 뇌 정본)가 참조 — 열람 뷰 재료 후보 |
| 계약/수집_교정_계약.json | P0 데이터 계약 실물(tests/계약.test.js 가드) |
| tools/크루카드사본.js·브랜드렌더린트.js | 대외 표면(크루카드·접수폼) 품질 게이트 — 단 **하네스 밖 호출자가 얇은 순환쌍**임이 실측됨 → 삭제 아닌 「통합 후보」(도전안) |

## A. 즉시 삭제 24건 (승인 문항 ①) — 전량 git 추적 · 복원 = revert 1회

**A-1. `docs/_archive/` 15건** — 살아있는 참조 0을 기계로 확인(이력·색인 문서 제외):
안전개선_v930 · GLIDE_바인딩_지시서_v925 · 다듬기_v934_계획 · 다듬기_v934_감사요약.txt · 학습추적_설계_v934 · GLIDE_새앱_조립가이드_v937 · GLIDE_조립가이드_v939 · 반편성_정본_v1 · 조립후_AI기능_증분_v950 · 만족도팩_실행지_v973 · 조립후_디테일_증분_v974 · CURRENT_HANDOFF · 수업준비팩_v986 · 조립후_출퇴근_결석_증분_v982 · 배포후_수동확인_2026-07-31
- 동반 수정 3곳(같은 커밋): `.claude/agents/reviewer.md:13`·`docs/SYNK_CONTEXT.md:51`의 CURRENT_HANDOFF 참조 제거 · settings.local.json의 디테일_증분 낡은 허용줄 정리 · **문서_지도.md §_archive 절 재작성**(5줄+절 전체가 삭제 대상을 가리킴 — 손 편집 색인이라 자동 안 고쳐짐)

**A-2. 압축 전 스냅샷 6건**: MEMORY_인덱스\_압축전 5종 + CLAUDE_v6.12_압축전
- 동반 수정: MEMORY.md 머리말·지침_이력의 「압축 전 전문」 주소를 「git 이력」으로 갱신. **이력 기록물(세션보드_아카이브 등)의 옛 주소는 손대지 않는다**(이력은 그 시점의 사실)

**A-3. `GLIDE_조립_마스터보드.txt`**(루트 1건) — 조립 중단. 유일한 잔존 참조는 엔진_폼리포트.js:2194의 **역사 주석**(이력 인용이라 삭제 후에도 유효 — 코드 수정 없음)

**A-4. effort-audit 쌍 2건**: tools/effort-audit.js + docs/Claude_effort_레벨_실측.md — 테스트 0·훅/스킬/워크플로 호출 0·참조자가 서로뿐인 자기참조 섬. 결론(모델 라우팅 기본값)은 이미 메모리에 흡수됨 — 메모리 포인터 1줄 갱신 동반

**청소 3종 (git 밖 — revert 불가지만 전부 재생성물·복제본)**
- 캐시: `__pycache__` 2곳·`_fixture_recordings/`·빈 `.claude/commands/` 폴더 (실행 시 재생성)
- 고아 워크트리 3(`.claude/worktrees/elated-bhaskara·nifty-rosalind·zen-knuth`): HEAD 3개 전부 master에 포함 **실측 완료** · **실행 직전 각각 `git status` 청정 재확인** 후 `git worktree remove`+`prune`(디렉터리만 지우면 유령 등록이 남는다). ⚠v1의 「수 주 전 고아」는 브랜치를 잘못 읽은 오판 — elated-bhaskara HEAD는 오늘 01:01 「세션 종료」 커밋이다(그래서 청정 검사를 실행 시점에 다시 한다)
- 병합 완료 로컬 브랜치 12개 삭제. **미병합 브랜치 2개는 보호**(f076-델타 2커밋·claude/cranky-cartwright 1커밋 — 주인 확인 전 손대지 않음) · 남의 워크트리 2곳(orca/cabezon·임시 verify-head)은 불가침

## B. 게이트 예약 삭제 — Glide 낙하산 접히는 날(talk 파일럿 게이트) 일괄 ~7건

GLIDE_조립가이드_**v947**·GLIDE_조립_실행지_v947·glide_업데이트_실측설계 + **v940·조립후_리그반카드_v981·조립후_상담연동_증분_v984**(← v947이 참조해 지금 못 지움 — 기계 검사 적발) + Glide 전용 테스트·엔진 구역.
지금 못 지우는 이유: 낙하산 응급수리 유일 정본 + DESIGN.md 링크를 tests/디자인정본.test.js가 기계 검사(지우면 CI 적색). **실행일에 이 문서의 드라이런을 다시 돌린다.**

## C. 유호님 판단 4묶음 (승인 문항 ②)

| 묶음 | 건수 | 상황 | 권고 |
|---|---|---|---|
| ⓐ `_archive/노션_보존_2026-08-04/` | 33 | synk-content 스킬 2곳+이벤트_아이디어_v1이 참조(실측 확인) · csv 처분 ⏳미결 | **보류** — 참조 정리+csv 결정과 한 번에 |
| ⓑ `docs/정본/**/_구본_md/`+`보안/_구본/` | 16 | 유호님 배치 트리 안의 구판(현행 .txt판 별도 존재) | **삭제 가능** — 유호님 폴더라 확인 필요 |
| ⓒ `docs/COWORK_지침.md` | 1 | ⛔동결 — Cowork 앱에 유호님이 직접 붙여넣는 사본(⏳재복붙 대기) | **유지** |
| ⓓ `synk-talk/`(repo 안 오붙여넣기) | ~55 | 이미 승인 대기 · 순정 스캐폴드 실측(오늘 05:42 생성·사람 흔적 0) · 정본 `Documents\SYNK-talk` 실존 | **삭제** — ⚠git 밖이라 **복구 불가**(정본이 따로 있어 실손실 0) · .gitignore 블록도 같은 커밋에서 걷음(동거 회귀 — 로컬층 강제·CI는 skip) |

참고: `ARC-AGI-3-Agents/`는 유호님 배치("옮기지 말 것" 기록) — 목록만 올리고 권고하지 않는다. 연계된 tools/bench/(벤더 실측 하네스)는 이종 검수 실측에 쓰여 존치.

## D. 존치 — 요약과 근거

- **docs ~230건**: 불가침(지침·장부·보드·작성 중 문서) + 운영(수집층·교재·커리큘럼·크루카드·발표물·법무·정본 트리 — **학원이 곧 데이터 수집기**라 운영 실물은 1년차의 몸통). 재제안 금지 kill-list 정본은 면역계 — 지우면 죽인 제안이 좀비로 돌아온다.
- **하네스**: 훅 18(전부 등록·발동) · 도구 35(+스키마 3 — effort-audit 제외 전부 참조 실측) · 테스트 81(CI 1,499건) · 스킬 10(review-animations는 「자체앱 때 재고」로 **의도적 휴면** — 죽은 게 아니라 세워둔 것) · agents 1 · 워크플로 2 · **OS 예약 작업 3**(NotebookLM·MemoryPush 가동 / BootBrief는 아래 「발견」).
- 참조가 얇지만 존치한 것(정직 기록): SYNK_동기화_구조(시트=운영층 지도)·결석추적_실행지(⏳2026-12 배선 예약)·상담지_팔레트_판정(색 판정 기록) — 메모리가 근거를 물고 있다.

## 발견 2건 (삭제와 별개 — 이번 조사가 캐낸 것)

1. **SYNK_BootBrief 예약 작업이 한 번도 안 돌았다**(LastRun 1999 센티널·NextRun 공란) — 메모리엔 「부팅브리핑 라이브」로 기록돼 있어 **기록과 실물이 모순**. 헤르메스 트랙에서 수리 필요.
2. **SYNK_NotebookLM 마지막 실행(08-04 21:00) 실패** — 배선은 정상, rot-check가 주간 점검에서 짖게 돼 있음. 관찰만.

## 위험 자기지목 (v2 기준 잔여 위험)

1. **_archive 판정의 근거 깊이** — 기계 검사는 「파일명 언급」만 잡는다. 파일명 없이 내용만 재사용된 문서는 못 잡는다(반박자가 교재 2과 전문으로 실증 — 그 계열은 수동 대조로 걸렀지만 전수는 아님).
2. **정본 트리 44건 일괄 존치** — 내용 개별 검증 없이 「유호 배치+회사 실체」 원칙 적용.
3. **워크트리 제거** — 안 보이는 세션 위험(F079). HEAD가 master에 포함돼도 미커밋 작업본이 있을 수 있어 실행 직전 청정 검사가 마지막 방어선.
4. 2차 LLM 반박은 생략했다 — 근거: 반박 1패스가 잡은 오류 계열(참조 파손)을 **기계 드라이런이 전수 검사**로 대체했고(FAIL 10건 실적), 비가역 실행 앞에 유호 승인+실행 시점 게이트 재실행이 남아 있다.

## 도전안 (방향의 대안 1개)

파일 삭제 24건의 실효는 낮다. 유호님 피로(F115 "장치 증식")의 원천은 죽은 파일이 아니라 **산 장치의 가짓수**다(도구 36·훅 18·테스트 81 = 이름 135개). 통념(가드는 늘리기만 한다)을 깨고 **「하네스 통합 트랙」**을 별도 제안한다: 기능이 겹치는 계열을 병합해 이름 수 자체를 줄인다 — 세션 계열 5종→1, 노트북LM 4파일→1, 순환쌍(크루카드사본·브랜드렌더린트)→브랜드 린트 1로. 비용=대공사(회귀 재배선), 이득=외울 이름이 준다. 이번 삭제와 별건 — 원하시면 설계안부터.

**한 수 더**: 인계문 65건(대부분 TTL 12시간 만료)은 손으로 안 지운다 — `tools/인계문수거.js`에 「수거 완료 후 N일 지난 인계문 자동 정리」를 붙여 **앞으로도 안 쌓이게** 만드는 게 맞다(오류는 수정으로 끝내지 않는다). 승인 ①과 무관하게 다음 트랙으로 올린다.

## 실행 절차 (승인 후 — 전부 AI 몫, 유호님 클릭 0회)

1. 참조 드라이런 재실행(이 문서의 24건 — FAIL 0 확인) → 2. A-1 동반 수정+A-2 참조 갱신 커밋 → 3. 삭제 **한 커밋**(본문 = 전체 manifest · 복원 = revert 1회) → 4. 워크트리 청정 검사→remove→prune, 병합 브랜치 12 삭제 → 5. `node tools/test-ci.js` 전량 초록 → 6. push → 7. `gh run` 원격 초록 → 8. 보드·메모리·문서_지도 갱신. ②에서 답 주신 묶음도 같은 절차.

---
*v2 · 2026-08-05 · 세션 ca6bb355 · 반박 21건+기계 드라이런 FAIL 10건 반영 완료 · **문항 ①② 답 수신·2026-08-06 실행 완료(머리말 실행 기록)***
