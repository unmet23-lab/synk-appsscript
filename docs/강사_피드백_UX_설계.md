# 강사 피드백 UX — ⛔ 폐기 스텁 (정본은 다른 곳이다)

> **이 문서를 인용하지 않는다.**
> **정본 = `SYNK-talk/docs/강사_반단위_피드백_설계.md`** (세션 `5c3f51e1` · talk `e8b0631`).

## 왜 스텁인가

2026-08-12, **같은 🎫 하나(`955e8a16` 가 남긴 「㉡ 한 벌 + 선생님 피드백 UX」)를 두 세션이 10분 안에 나란히 설계했다**(F381).

- `1a1b47ad`(이 문서) — 19:48 보드 선언 → 19:56 설계 v1 커밋 `c7ed0d14`
- `5c3f51e1`(정본) — 19:58 보드 선언(설계는 그 전에 talk 저장소에서 이미 끝나 있었다)

보드 선언이 **먼저인 쪽이 나**였는데도 못 막았다 — 저쪽이 선언보다 **작업을 먼저** 했고, 그 사이 내 `board.js` 조회에는 그 줄이 없었다.

## 왜 저쪽이 정본인가 (내가 비켜난 이유)

1. 🔴 **이 문서의 ⓑ 가 틀렸다.** 「Glide 판 화면이 지금 어디에도 없다 → 유호님께 한 장면 여쭙는다」로 적었는데, 그 화면은 **저장소 안에 살아 있었다** — `Code.js:3336` 부근의 15·16열 `반카드요약`·`반몬스터이미지`, 주석에 유호님 07-31 원문(「강사 「반」 탭 첫 화면(반 리스트)이 후지다」)까지. **그래서 그 화면은 목록이 아니라 «카드»였고, 이 문서가 기본값으로 고른 「목록」은 근거 없는 선택이었다.** 유호님께 안 여쭤도 될 것을 ⏳로 올렸다(F376 과 같은 형태 — 묻기 전에 저장소를 먼저 뒤진다).
2. 저쪽은 🎫 **전부**를 덮는다 — ㉡ 서버 판정 + 「맞음」 자동 하강 + 피드백 UX 를 한 벌로. 이 문서는 피드백 UX 한 조각뿐이라 「따로 하면 각각 어색하다」는 정본 조건을 못 지킨다.
3. 저쪽 문서는 **코드 옆(talk)**에 산다. 구현이 talk 이라 정본이 거기 있는 편이 낡음이 덜하다.
4. 저쪽은 이 문서가 §7 에서 **추정으로** 적은 것을 실측으로 뒤집었다 — 「골든 큐에 반 축이 없다 · 자동 하강은 AI 앞단」.

## 여기서만 있었던 것 (정본으로 옮길 값이 있는 델타)

저쪽도 `actor_kind` 이름 충돌을 잡았다(그쪽 §1 말미 ⚠ — 「어느 조인도 역할을 `actor_kind` 로 가르지 않는다」). 다만 **무엇이 새는지의 좌표**는 여기에만 있으니 그것만 남긴다:

- `review/index.ts:572` — 검수 확정이 `'teacher'::engine.actor_kind` 로 **INSERT 된다**(그 값의 실제 뜻 = 「사람 직원」).
- `review/index.ts:490` — `supersedes` 대상 판정이 `c.actor_kind = 'teacher'` **하나로만** 가른다 → 강사 행이 정당한 재검수 대상이 되고, 거절 문구조차 사람 눈에 정상으로 보인다.
- `promotion_intent` CHECK(`= false or actor_kind = 'teacher'`, c9·c11) → 검수 안 거친 강사 한 마디가 훈련 승격 후보 자리에 설 수 있다.
- ⚠ 더 센 가설(강사 행이 `submissions.transcript_verified` 를 덮는다)은 열어 보니 **틀렸다** — 그 칸은 승인 트랜잭션이 직접 쓴다(`review/index.ts:582`). 검수 큐 이탈도 teacher 행 존재가 아니라 `pipeline_jobs.status` 다.

🚫 이 스텁을 지우지 않는다 — 지우면 F381 이 왜 생겼는지가 사라지고, 다음 세션이 `docs/` 에서 같은 이름을 다시 만든다.
