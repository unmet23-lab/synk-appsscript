# 13번 반딧불 노을 들판 — 실제 라디오 반영

2026-09-10 KST. 사용자 요청: “이번에 13번 까몽이로 라디오 배치를 지금 해줄래?”

## 반영

- 실제 방송 `eI-5vSy5mjY`에 13번 배경과 3개월 출석 잎망토 까몽이 적용. 기준 1920×1080에서 왼쪽455·위305·크기580. 사용자에게 설명한 왼쪽 구도를 유지했다.
- 배경은 기존 Seedance2.0 생성 원본을 15초 순환 영상으로 가공. 카메라 이동·지역별 흔들기·광학 흐름 없음. 이번 적용에서 새 생성 호출·구매 없음.
- 원본 잎망토 몸체를 공통으로 사용하고, 검수된 눈 영역 11,609픽셀 안에서만 기본·눈감음·눈웃음을 전환. 모든 사건 표정 9키는 이 3개 그림에 연결한다. 털·옷·공통 알파는 표정마다 바뀌지 않는다. 공통 테두리 보정과 접지 그림자 적용.
- 서버 설정 3개(마스코트 HTML·장면 선택·무대 목록)만 변경. 이전 파일 백업 보존. 기존 11곡 FLAC·곡 목록·방송 자격증명·송출 코드 등 보호12파일 SHA 동일. 방송 주소·제목 변경 없음.
- 수동 재시작 1회로 반영. 음악은 기존 11곡 묶음 시작부터 재생된다.

## 검증과 한계

- 로컬 실제 브라우저: 3표정 화면·배치·10초 배경 재생·자연 깜빡임 확인, 페이지 오류0. 원본6파일 지문 일치, 눈 바깥/옷/알파 변화0, 원본 메모리 변경0.
- 독립 검토: 배포4자산 SHA, 720p 15초450프레임, 배치·그림자 일치, 눈영역밖 변화0. 배포 드라이버의 복원·이미 적용된 상태 재시도 검증 누락을 수정 후 재검토.
- 실제 송출 Chromium 읽기 전용 36초306표본: 준비true, 까몽/13번, 9키 전부로드, 기본·깜빡·눈웃음·안도 관찰. 세 실제 그림과 고정 털 검사 통과. `서버실동작검증.json`.
- 반영 직후 자동 재연결2회: 18:16:53 UTC 방송 시각65.248초에서38초 정체, 18:20:13 방송 시각144.359초에서39초 정체. 기존 복구 장치가 처리했고 두번째 재시작18:20:19 PID154081. 명시적 FFmpeg 오류·메모리 부족 흔적은 없으며 최초 정체 원인은 확인 불가. 새 자산이나 네트워크 때문이라고 단정하지 않는다. 정체 시각은 정확한15초 반복 경계와 일치하지 않았다.
- 첫 유튜브 수신45초에는 송출 지연 때문에 이전 추석 화면과 새13번으로 바뀐 화면이 함께 포함됐다. 완전 디코드 통과·30ms이상 -45dB 아래 무음 검출0. 이 검사는 재연결 전후 전체가 끊김 없었다는 증거가 아니다.
- 복구 후 연속성·최종 상태는 아래 최종 확인에 기록한다. 사람의 장시간 편안함 평가·11곡 전체 회차 연속 청취·11번 실제 방송 반영은 이번 완료 범위가 아니다.

## 원본과 재현

- 시작 기준 HEAD `b031ed4ef460a09591ee61335e6415585506057e`; 반영 직전 기준 HEAD `5dbb16d2b80f5a254b868196e5ee4b449ef668ef`. 다른 세션의 미커밋 변경 보존.
- 운영 원문 `docs/AI_운영원칙.md` SHA `e062c7964da828ddc43f0e61a85864f668e2ec2539fb5283f4c451bcea6b5868`.
- 배경 원본 `docs/라디오/무대/dream_field.png`: `c3d6fb508a9244ca34e83de4631f5c1757b1f21b6045b06112dc469b71b10b1a`.
- 생성 영상 `13_생성원본.mp4`: `a599027b2d50eb32c02cb8f9681426378c715e1f0a212aa9932a9e753b6a5dce`. 기존 생성 결과이며 이번 새 호출 아님.
- 잎망토 공통 원본: `docs/Loom_자산/라디오차림/까몽/3개월출석잎망토_본체.webp`, 배포 복사본과 동일 SHA `48823c235151fb0d7859883205f7f6c3bd2040ebfe6b1f85631186b7563379c6`.
- 눈 공여 원본: `docs/Loom_자산/라디오차림/까몽/_후보/여름델+전설의팻말/65f92a753bb5416b/`의 본체·눈감음·눈웃음. 실제 서버 HTML 원본 SHA `64e3c25efc30375e999be983da7e6295de01108f8b7de0e676f44e32412da983`. 다른 세션의 차림선택 UI가 있는 로컬 HTML은 배포하지 않았다.
- 배포 파일 지문과 백업: `manifest.json`, `라이브반영.json`, `서버검사.json`. 재배포 입구 `../라이브반영.cjs --check|--upload|--apply`; 이미 같은 판이면 수동 재시작을 반복하지 않는다.
- 적용 참고: 사용자 “감나무가 덜덜” 피드백, 부분운동 멀미 기각, 털고정 요청, 13번 왼쪽 구도 수락. 전체 배경 영상과 고정된 재질로 반영했다. 과거 칭찬을 이번 영상의 최종 만족으로 확대하지 않는다.

## 최종 확인

- 첫 복구 후 별도35.016초 유튜브 실제 수신:1280×720/H.264/AAC, 수신 경고0·완전 디코드 오류0·30ms이상 -45dB 아래 무음 검출0. 새13번 배경과 잎망토 까몽이 화면 확인. `유튜브수신_복구후.json`, `실제방송_복구후.png`. 이 짧은 표본을 모든 시간의 무중단 보장으로 해석하지 않는다.
- 마지막 확인 2026-09-09 18:25:04 UTC(한국03:25:04): PID154081, active/running, NRestarts2 유지. 마지막 재연결 후4분45초 추가 재시작 없음. 최근3분 방송 시각113.848→173.709→233.593초 전진, 실시간비율0.997→0.998, 캡처 실패·버린 장·겹친 장0. TCP 연결정상·송신대기0. `안정성확인.json`. 현재 정상 송출 확인과 간헐 정체의 근본 원인 해결은 구분한다. 추가 예약 감시는 만들지 않았다.
- 기존 가장자리·표정리듬·송출진척 회귀 시험32개 통과. 로컬60초 규격화 파일은 비교용으로만 만들었으며, 원인 근거가 없어 서버에 적용하거나 추가 수동 재시작하지 않았다.
