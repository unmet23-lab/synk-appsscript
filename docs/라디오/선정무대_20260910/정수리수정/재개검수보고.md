# 정수리 수정본 — 재개 읽기 검수

2026-09-10 06:02:51–06:03:53 KST 확인. 서버 변경·재시작·페이지 조작 없음.

## 결과

- 수정 HTML, `radio13-06fdc6100afc.js`, `leaf-base-514d7886949b.png`가 배포 기대 SHA-256과 일치한다. 배경, 기존 11곡 FLAC, 음악 재생목록, 배경 목록도 이전 반영본 지문과 일치한다.
- 실제 ffmpeg 프로세스가 기대 배경과 기존 FLAC을 열고 있다.
- 실제 방송 Chromium을 36초 동안 읽어 285개 표정 상태를 확인했다. 기본·깜빡·집중·안도가 나타났으며, 실제 그림은 기본·눈감음·눈웃음 3종을 모두 관찰했다. 표정을 강제로 바꾸지 않았다.
- 9개 표정 키 모두 수정 leaf 원본 출처이고 1024×1024로 로드되어 있다. 서로 다른 실제 이미지 3개, 새 helper, `outsideEyeDifference=0`, `expressionAlphaEqual=true`를 확인했다.
- 배치는 1920×1080에서 x455/y305/580×580으로 유지된다.
- YouTube 공식 API: stream active/good, public live 방송 1개, 구성 문제 0개.

## 안정성 경계

- 확인 시 PID157610, active/running, 현재 systemd NRestarts=1. 실행 시작은 04:40:01 KST, 조회 시 약 82분49초 지속 중이었다.
- 마지막 정체 로그는 04:39:56 KST: 방송시각 00:02:49.227에서 35초 정체 후 기존 자동 복구가 동작했다.
- 03:00 KST 이후 확인한 정체는 총 4회다. 현재 NRestarts=1을 전체 누적 자동 복구 횟수로 해석하면 안 된다.
- 최근 3개 분당 로그는 실시간대비 1.0, 실패/버린장/겹친장 0, 방송시각은 매분 약 60초 진전했다. TCP 연결은 ESTAB이었다.
- 기존 정체의 근본 원인은 이번 읽기 검수로 확정하지 않았다. 이후 장시간 무중단 보장이나 공개 플레이어 영상·청취 검증으로 확대하지 않는다.

## 증거와 원본

- `재개상태검수.json`: 파일 지문 7개, 서비스·선별 로그·연결 상태.
- `재개서버검수.json`: 실제 방송 Chromium 36초 표정 및 수정본 출처 검증.
- `재개공개상태.json`: YouTube 공식 API의 받는 상태와 공개 방송 상태.
- `재개실행.cjs`: 기존 `../서버검수.cjs`를 읽고 인메모리로 검사항목을 보강한 읽기 전용 래퍼. 기존 증거 파일은 덮어쓰지 않았다.
- 재개 기준 HEAD `968b99517c88db3b5de4ea77c0d482dc32626c94`; 현재 AGENTS·AI 운영 원칙·대용량 자산 운영 원문을 읽었다. 사용 자산의 기대값은 이 폴더 `manifest.json` 및 `../라이브13/라이브반영.json`과 대조했다.

조회 과정에서 자격증명 값·전체 프로세스 인자·송출 URL은 출력하거나 저장하지 않았다.
