# 글라이드 이관 대장 — 사람 손이 쓰던 입력 칸 전수

> v1.0 · 2026-08-31 · 감사 S4-3 시공. **Glide 는 08-05 폐기됐다**(Code.js 「Glide 는 08-05 폐기라 버튼 계약도 죽었다」).
> 그날 이후 「Glide 소유」로 설계된 입력 칸들은 **쓰는 손이 사라진 채** 엔진에 남아 있다 — 읽는 코드는 살아서 영영 빈 칸을 기다린다.
> 이 대장은 그 칸 전부에 하나씩 판정을 붙인다. **주인 없는 칸을 「이관 예정」으로 포장하지 않는다 — 잇는 화면이 없으면 「끊김」으로 정직하게 적는다**(talk-migration-tail-rule).

## 자(무엇으로 셌나)

- **분모** = 엔진 7파일(`Code.js`·`엔진_수집.js`·`엔진_궤적.js`·`엔진_셋업확장.js`·`엔진_폼리포트.js`·`엔진_콘텐츠AI.js`·`엔진_운영배치.js`) `grep -i glide` **184줄**(08-31 실측) 중 «학생·학부모·강사·원장 손이 쓰던 입력 칸» 전수. 나머지 줄은 표시·바인딩·쿼터·역사 서술이라 입력 칸이 아니다.
- **신앱 대조** = SYNK-talk `src/*화면.js` **14벌**(08-31 ls 실측 — 앱감사_0831 의 「13벌」 + 그날 신설된 관찰화면) 머리말 정독 + `hw_feedback`·`학생확인`·`확인했어요`·`point_logs`·`teacher_gold`·`드림한줄`·`최애`·`애칭` grep(전부 0건 — src·lib·supabase).
- 사는 곳은 행 번호가 아니라 **고유 문자열**로 적는다(행은 밀린다).

## 판정 낱말

| 판정 | 뜻 |
|---|---|
| **폐기** | 칸의 «일» 자체가 죽었다(축 소각·대체 완료) — 잇지 않는다 |
| **신앱 이관** | 같은 «일»을 신앱 화면이 잇는다(원장이 달라졌으면 그 사실을 착지 칸에 적는다) |
| **끊김** | 일은 유효한데 잇는 손이 신앱에 없다 — 개원 전 판정 필요 목록 |
| **존치** | Glide 와 무관하게 유효 — 이름·문구만 손본다 |

## 이관 대장

### 학생 손

| 칸이름 | 사는 곳(파일:앵커 문자열) | Glide 시절 쓰는 손 | 판정 | 신앱 착지(화면·사건) | 근거 한 줄 |
|---|---|---|---|---|---|
| hw_feedback J열 학생확인 | 엔진_셋업확장.js 「J학생확인=Glide 전용(스크립트 불가침)」 · 정산 = 엔진_콘텐츠AI.js 「첨삭 '확인했어요' 정산」 | 학생이 첨삭 카드에 «확인했어요» 1탭 → +1P(첨삭확인) | **잇는다(축 통합)** | 없음 — 같은 «일»의 이웃은 답장화면(correction.viewed·responded)이나 **원장이 다르다**(Supabase corrections · hw_feedback 에 안 닿는다) | (09-01 확정) 첨삭 열람·확인을 신앱 답장화면 축(corrections 원장)으로 통합하는 설계 트랙 — 개원 전 필수 아님(첨삭은 무인 발행이 돌고, 그때까지 확인 신호는 없는 채 운영) |
| profiles CB80 드림한줄 | Code.js 「드림한줄(CB 80) — 학생이 Glide Set Column으로 직접 쓰는 사용자 소유 열」 | 학생이 나의여정 카드에서 목표 한 줄을 직접 적음 | **폐기(대체 완료)** | 없음 — 나침반화면은 «그날의 선언»을 담지 이 칸을 고치지 않는다 | (09-01 확정) 신앱 나침반 season_goal(시즌 목표 답) + 회고 「그때 스스로 말한 것」 재회(D3-4)가 같은 «일»(자기 선언 보관·재회)을 잇는다 — 시트 칸은 레거시 이력으로 굳힘 |
| profiles DA105 최애 | Code.js 「최애(DA 105) — 학생이 Glide Set Column으로 쓰는 사용자 소유 열」 | 학생이 최애(콘텐츠·주제)를 직접 적음 | **폐기(대체 예정)** | 없음 | (09-01 확정) 직접 입력 칸은 되살리지 않는다 — 성향 추출 설계(숙제 내용·M2 관찰)가 같은 일을 잇는다(disposition-extraction-design) |
| profiles BC55 대표몬스터 | Code.js 「BC(55)=대표몬스터: 도감에서 학생이 선택(Glide Set Column — 도달 단계만 버튼 노출)」 | 학생이 도감에서 대표 몬스터 선택 | **폐기** | — | v9.269 몬스터 축 소각(장면 사다리로 전환) — BC55 는 가이드 화이트리스트로 재용도됐다 |
| profiles AO 애칭(구 몬스터이름) | Code.js 「학생이 지은 이름 — 가이드 애칭으로 «승계»한다(설계 §4-1)」 | 학생이 제 몬스터의 이름을 지음 | **잇는다(신앱 소걸음)** | 없음 — 가이드 애칭 «승계» 설계(함께한날 §4-1)는 섰는데 신앱에 짓는 손이 없다 | (09-01 확정) 가이드 셋 선택(D4-1)이 섰으니 «가이드 애칭 짓기»가 자연 다음 걸음 — 첫등록 뒤 또는 설정 곁 한 칸 · 개원 전 목표(값 큼·비용 작음) |
| profiles AK 착용칭호 | Code.js 「학생이 고른 착용 칭호(profiles AK열, Glide 기록)」 | 학생이 얻은 칭호 중 하나를 골라 착용 | **보류** | 없음 | (09-01 확정) 성장 체계(함께한 날)가 자리 잡은 뒤 재판정 — 지금 잇지도 소각도 않는다(칭호 로어 표시는 무해하게 삶) |
| profiles AR44 목표아이템(스토어 찜) | Code.js 「AR = 사용자 기록 전용(스토어 목표 찜 — Glide Set Column, 스크립트는 값 안 씀)」 | 학생이 스토어에서 목표 상품을 찜 | **수기 대체(백로그)** | 없음 — 신앱에 스토어 화면 자체가 없다 | (09-01 확정) 스토어 축 전체(찜·구매·게이지)는 개원 수기 운영 — 신앱 스토어 화면은 개원 후 백로그 · 게이지(BZ78)는 그때까지 잠잠(오경보 없음) |
| 스토어 구매(point_logs 음수 Add Row) | Code.js 「잔액 음수 학생 진단(실측 -35 발견 — 구매 버튼 잔액 조건 필수 안내)」 | 학생이 Glide 구매 버튼으로 상품 구입(잔액 차감) | **수기 대체** | 없음 — 위와 동일(스토어 화면 부재) | (09-01 확정) 원장이 교실 실물 교환 시 시트 수기 차감(연 수십 건 규모 · point_logs 음수 행 직접 입력) — 잔액 회계는 그대로 산다 |
| GPS 셀프 출석(attendance Add Row) | Code.js 「학생 셀프 GPS 출석(Glide Form→attendance Add Row)」 | 학생이 앱에서 셀프 출석 | **폐기** | (구글 폼이 이미 대체) 앱 [출석] 버튼 → 출석 폼 1탭 | 엔진_폼리포트.js 「출석 폼 — 앱(Glide) 출석의 update-0 대체」(v9.49) — 폼 경로는 Glide 사망과 무관하게 산다 |

### 학부모 손

| 칸이름 | 사는 곳(파일:앵커 문자열) | Glide 시절 쓰는 손 | 판정 | 신앱 착지(화면·사건) | 근거 한 줄 |
|---|---|---|---|---|---|
| profiles AL 리포트확인월 | Code.js 「AK(착용칭호)·AL(리포트확인월)·AO(몬스터이름) … 값은 Glide(학생·학부모)만 기록」 | 학부모가 월간 리포트를 봤다고 기록 | **폐기** | 없음 — 신앱에 학부모 화면이 아직 없다 | (09-01 확정) 읽는 코드도 죽은 칸 — 학부모 열람 신호는 학부모 축이 서는 날 그 원장(Supabase)으로 새로 낳는다 |
| inquiries(학부모 문의) | 엔진_운영배치.js 「inquiries 시트(Glide "Add Row"로 학부모가 직접 씀)를 읽기만」 | 학부모가 앱에서 문의를 남김 | **수기 대체** | 없음 — 상담AI(입학 «전» 통로)와 별개로, 재원생 학부모의 인바운드 손이 없다 | (09-01 확정) 원장이 받은 문의(전화·메신저)를 시트에 수기 등재 — 기존 소비(브리핑·알림)가 그대로 산다 · 학부모 앱 축은 후순위 |
| absence_notice(결석 사전신고) | 엔진_운영배치.js 「결석 사전신고 — 학부모가 미리 알린 학생은 미출석 오경보에서 제외 (Glide "Add Row"로 직접 씀)」 | 학부모가 결석을 미리 알림 | **수기 대체(개원 절차 등재)** | 없음 | (09-01 확정) 학부모 메신저 신고 → 원장·강사가 시트에 적는 운영 절차로 오경보를 막는다 — 개원 운영 점검표에 한 줄 |

### 강사·원장 손

| 칸이름 | 사는 곳(파일:앵커 문자열) | Glide 시절 쓰는 손 | 판정 | 신앱 착지(화면·사건) | 근거 한 줄 |
|---|---|---|---|---|---|
| 칭찬 실지급 버튼 4종(point_logs Add Row) | Code.js 「실지급은 Glide Add row 버튼 4종이라 여기 값은 문서·시뮬용」 · 엔진_운영배치.js 「Glide 버튼은 reason='칭찬·발음↑' 등 4종 고정값으로 기록」 | 강사가 태그 버튼으로 개인 칭찬 +2P | **잇는다(M2 통합)** | 없음 — 관찰화면(한 줄 관찰)·반피드백화면(반 한 마디)은 말을 남기지 «포인트 지급»이 아니다 | (09-01 확정) M2 관찰 사건(observe/note)의 칭찬 태그에서 포인트 지급을 파생 — 별도 버튼이 아니라 «관찰이 곧 지급» · M2 다음 증분 트랙 |
| 인정 버튼(오늘의 도전·오늘의 성장) | Code.js 「Glide 는 08-05 폐기라 버튼 계약도 죽었다」(자백 그 줄) | 강사가 수업 중 도전·성장 인정 +5P | **잇는다(M2 통합)** | 없음 | (09-01 확정) 칭찬 4종과 같은 자리 — M2 관찰 태그 파생으로 통합(v9.258 기준 충족 전원 규약 유지) |
| teacher_gold 강사 4열(강사판정·강사교정·사유·강사) | 엔진_셋업확장.js 「강사판정·강사교정·사유·강사 4열은 **Glide가 채운다**」 | 강사가 주 5건 AI 교정에 판정을 붙임(정답 모음) | **신앱 이관** | 강사화면 — 골든 판정(M2 §7-1 · 「파인튜닝 쌍의 사람 라벨」). ⚠ **원장이 다르다**: 라벨은 Supabase 로 가고 teacher_gold 시트엔 안 닿는다(talk `teacher_gold` grep 0) | 같은 «일»(강사 라벨)은 이었고, 시트 쪽 표본 발행·픽스처 내보내기와의 접붙임은 별도 판정 |
| weekly_topics F~L(배운내용·문법태그·도달도·숙제완료자) | 엔진_셋업확장.js 「weekly_topics F~L(v9.36)은 … 수업 '내용' 로그이고 Glide 입력 경로다」 | 강사가 수업 후 배운 내용·문법 태그 입력 | **완료(09-02 · 폼 증설)** — `lessonCloseSpec_`·`lessonCloseExtraItems_`·`migrateLessonCloseForm0902`·`sweepLessonCloseForm_`(8열 창 → weekly_topics B·D·F·J) · 라이브 폼 증설은 배포 창에서 유호님이 시트 메뉴 「📋 차시 마감폼 만들기 → └ 증설」로 | 차시 마감폼(구글 폼) 문항 「배운내용」·「문법태그」(뱅크 파생 체크박스) → 10분 스위프가 weekly_topics 행으로(D = 응답 시각) → expandMasteryLog_·translateTopics_ 기존 그대로 | (09-01 확정) 차시 마감폼(구글 폼 — 강사 손에 이미 있음)에 문법태그·배운내용 칸 증설 → 엔진이 weekly_topics 로 전개 · 새 화면 0 · (09-02 시공) 도달도·예외학생·숙제완료자는 안 잇는다 — 도달은 masteryApply_ 한 문뿐(막2), 숙제 완료는 숙제 폼이 잇는다 |
| attendance_batch(수업 시작 출석 1탭) | 엔진_셋업확장.js 「수업 시작 출석 1탭(B안) → expandAttendanceBatch_가 attendance로 전개」 · 「출석 1탭만으로 Glide 월 500건의 57%다」 | 강사가 반 열기 60초 안에 출석자 1탭 | **완료(09-02 · 폼 신설)** — `createClassAttendanceForm`·`syncClassAttendanceForm_`·`extendClassAttendanceForm`(엔진_폼리포트.js) · `sweepClassAttendanceForm_`(엔진_콘텐츠AI.js → attendance_batch) → 기존 `expandAttendanceBatch_` 무변경 · 라이브 폼 생성은 배포 창에서 유호님이 시트 메뉴 「🙋 반 출석 폼 만들기」로 | 강사용 «반 출석 폼»(구글 폼 · 반을 고르면 그 반 명부 섹션으로 건너뛰어 결석자만 체크 · 섹션 없는 반은 이름 직접 입력) → attendance_batch 행(명부 − 결석자) → attendance | (09-01 확정) 강사용 «반 출석 폼»(구글 폼 · 반 고르고 결석자만 체크) → expandAttendanceBatch_ 접붙임 · 새 화면 0 |
| 레이드 연료 미션(class_fuel Add Row) | 엔진_콘텐츠AI.js 「레이드 연료 미션 — 이름이 Glide 폼 Choice와 정확히 일치해야 함 (raidFriday가 이름→P 매핑)」 | 강사가 수업 미션 달성 시 반 보너스 기록 | **완료(09-02 · 마감폼 동봉)** — 「연료미션」 드롭다운(선택지 = contents 시트 fuel 행 + 「없음」 · 아침 sync 가 갱신) → `sweepLessonCloseForm_` 가 weekly_topics J 열로 → 기존 `expandLessonLog_` 가 class_fuel 로(이름→P 매핑 그대로) | 차시 마감폼 「연료미션」 → weekly_topics J → class_fuel → raidFriday | (09-01 확정) 차시 마감폼 증설에 연료 미션 선택 칸 동봉(이름→P 매핑 그대로) |
| 강사 출퇴근 버튼(teacher_checkins Add Row) | Code.js 「Glide 출퇴근 버튼 Visibility 소스: 출근 버튼 = 오늘출근 is empty …」 | 강사가 출근·퇴근 1탭 | **완료(09-02 · 폼 신설)** — `createTeacherCheckinForm`·`syncTeacherCheckinForm_`(엔진_폼리포트.js) · `sweepTeacherCheckinForm_`(엔진_콘텐츠AI.js → teacher_checkins [이름,구분,시각] · 60초 연타 자 존중 · 수집표식 없음) · 라이브 폼 생성은 배포 창에서 유호님이 시트 메뉴 「⏱ 출퇴근 폼 만들기」로 | «출퇴근 폼»(구글 폼 · 강사 이름 드롭다운 = profiles 강사 · 구분 출근/퇴근) → teacher_checkins → 출결 보드·퇴근 응원·근태 지표 기존 그대로 | (09-01 확정) 출퇴근 1탭 구글 폼 신설(근태 지표 유지) — 강사 1~2인 규모라 폼이면 충분 |
| hall_of_fame(졸업생 명예의 전당) | Code.js 「졸업생 명예의 전당 — 원장이 Glide 폼으로 행 추가」 | 원장이 TOPIK 합격자 등재 | **수기 확정** | 없음 — 손이 원장 1인·연 수회라 **시트 수기가 실질 대체**(별도 화면 불요 후보) | (09-01 확정) 원장 1인·연 수회 — 시트 수기가 정본(화면 안 짓는다) · 첫 졸업생 전 절차 한 줄만 |

### 점검 함수

| 칸이름 | 사는 곳 | 판정 | 근거 한 줄 |
|---|---|---|---|
| preflightGlide(개원 전 점검·자동 복구) | 엔진_셋업확장.js `function preflightGlide()` 「원버튼 준비+자동복구+진단」 | **존치** — 시트 골격·콘텐츠·계산 점검은 Glide 와 무관하게 유효 | 이름과 로그 문구(「Glide 조립 사전점검」·「다음 단계 … Glide 연결」)가 죽은 앱을 가리킨다 — **개명·문구는 유호님 판정 대기**(코드 수정은 이 대장 밖 · S4-6 ①주석 스윕과 같은 세션 권장) |

## 대장 밖 — 이관이 필요 없는 입력 통로(전수의 나머지)

184줄 중 입력 칸이되 위 표에 없는 것들은 전부 **구글 폼·웹앱·시트 수기 경로**라 Glide 사망과 무관하게 산다:
출석 폼 · 숙제 제출 폼 · 퀴즈 응답 폼 · 대화 폼 · 강의 확인 폼 · 약점 메모 폼(student_errors) · 학업 기록 폼(academic_log 는 시트 직접 입력 겸용) · 차시 마감폼(lesson_close) · 결석 팔로업 폼 · 크루카드 웹앱 · crew_projects(시트 수기).
나머지 glide 줄은 표시(Rich Text·ITE·Visibility)·쿼터 설계·Row ID 보호·역사 서술이다 — 손이 아니다.

## 판정 확정 — 09-01 (유호 위임 「판정해줘」)

끊김 17칸 전건 판정 완료(각 행 판정 칸이 정본 · 근거 앞머리 「(09-01 확정)」):

| 갈래 | 수 | 칸 |
|---|---|---|
| **잇는다 — 폼 증설·신설**(새 화면 0 · 개원 전 필수) | 4 | weekly_topics(마감폼 증설) · attendance_batch(반 출석 폼) · 레이드 연료(마감폼 동봉) · 강사 출퇴근(1탭 폼) — **✅ 09-02 코드 완료**(회귀 `tests/폼넷.test.js` · 라이브 폼 넷은 배포 창에서 시트 메뉴 「SYNK」의 📋·🙋·⏱ 항목으로 — 각 행의 판정 칸에 함수 이름) |
| **잇는다 — M2 관찰 통합**(관찰이 곧 지급) | 2 | 칭찬 4종 · 인정 버튼 |
| **잇는다 — 신앱 소걸음·축 통합** | 2 | 애칭(가이드 애칭 짓기 · 개원 전 목표) · hw_feedback 학생확인(답장화면 축 통합 설계 · 급하지 않음) |
| **시트 수기 확정**(운영 절차로 산다) | 4 | 스토어 찜+구매 · inquiries · absence_notice(개원 절차 등재) · hall_of_fame |
| **폐기**(대체 완료·예정) | 3 | 드림한줄(나침반·회고 재회가 대체) · 최애(성향 추출이 대체) · 리포트확인월 |
| **보류** | 1 | 착용칭호(성장 체계 안착 뒤 재판정) |

- 시공 순서: **폼 넷이 먼저다**(개원 전 필수 · 전부 엔진 쪽 + 구글 폼이라 리드타임 짧음) → M2 통합 → 애칭 소걸음. 트랙.md 0-감사 절이 이 순서를 쥔다. — 폼 넷 ✅ 09-02 시공(코드 · 라이브 폼은 배포 창) · 다음은 M2 통합.
- 이 판정을 실행하는 세션은 **그 커밋에서 이 대장의 그 줄을 「완료」로 고친다**(안 고치면 대장이 곧 거짓이 된다).
