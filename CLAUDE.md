# SYNK — Claude Code 작업 지침

> v12.0 · 2026-09-09 — 네이티브 하네스 우선. 공통 운영은 아래 한 파일에서 읽는다.

@docs/AI_운영원칙.md

## Claude의 강점을 쓰는 자리

유호님이 직접 요청했으면 목적을 이해하고 결과 확인까지 맡는다. 기본 창구는 Codex지만 Claude를 단순 검수자나 제한된 글쓰기 도구로 가두지 않는다. 위임받았으면 목표·범위·결과 형식에 집중하고 통합 담당에게 완성된 재료와 증거를 돌려준다.

어려운 사업·제품·교육 설계, 철학과 고객 경험의 연결, 브랜드 말투·서사, 긴 자료의 모순, 깊은 원인 판단에 집중한다. 목표·정본·완료 조건을 기준으로 접근법을 선택한다. 상시 계획 승인·고정된 도구 순서·매번 3사 검토를 요구하지 않는다.

## 기본 기능 활용

탐색·편집·실행·서브에이전트·worktree·재개·압축·스킬·MCP·플러그인을 필요에 따라 사용한다. 큰 독립 작업을 병렬로 나누되 같은 파일에 동시에 쓰지 않는다. 지원 기능은 설치된 버전에서 확인한다. `--bare`나 진단용 안전 모드를 평소 실행에 써서 기억·지침·로그인까지 끄지 않는다.

코드·시험·계산·앱 조작에 Codex가 도움이 되면 설치된 공식 Codex 플러그인을 먼저 사용한다. 해당 환경에서 플러그인을 못 쓰면 공식 Codex CLI를 쓴다. 구체 목표를 전달하고 결과를 기다려 통합한다. 사용자에게 같은 말을 다른 모델에 다시 입력하도록 하지 않는다.

Google 자료·영상·음성·몽골어 대조는 사용 가능한 Gemini/Antigravity 공식 통로를 쓴다. 설치·인증·입력 기능을 확인하지 않은 것을 사용 가능하다고 말하지 않는다. 최상위 모델의 구독 포함량과 usage credits는 구분한다. 모델 목록을 손으로 제한하는 별도 목록을 만들지 않는다.

## 읽기와 제작

공통 운영 원칙의 작업별 경로를 따라 해당 자료만 읽는다. 철학 전문·기억 폴더 전체를 매번 싣지 않는다. 시각 결과는 SYNK 디자인 자산과 승인 실물을 출발점으로 삼고 학생 문안은 `synk-brand`를 먼저 읽는다. 회사마다 다른 약속과 고객을 섞지 않는다.

시험은 변경 위험에 맞게 수행한다. `Code.js`·`엔진_*.js`는 전역 이름공간을 공유한다. Apps Script 시험은 `node --test tests/<관련 파일>`. 형제 저장소는 그 자리의 지침을 읽는다.

배포가 요청에 포함되면 해당 deploy 스킬과 기존 도구로 완료한다. 이미 허용된 커밋·push·일반 로그인·설치·내부 편집을 단계마다 다시 묻지 않는다. 신규 지출, 외부 발송의 범위 확대, 본인 확인은 공통 원칙과 실제 하네스의 권한 규칙을 따른다.

## 검토와 마무리

검토만 요청받았으면 대상과 증거를 독립적으로 평가한다. 파일·위치·깨지는 동작·재현 입력을 제시하고 확인 못 한 것은 명시한다. 검토자 동의 수로 실제 결함을 덮지 않는다.

완료는 문서만 쓰는 것이 아니라 요청한 결과를 남기고 사용 가능함을 확인하는 것이다. 실제 수행·검증·잔여 문제를 구체적으로 보고한다. 상세 배치와 예시는 `docs/AI_워크플로우_아키텍처.md`.
