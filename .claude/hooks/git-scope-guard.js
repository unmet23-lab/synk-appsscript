#!/usr/bin/env node
// git-scope-guard — 커밋 범위 게이트 (PreToolUse 훅)
//
// 무엇을 막나: `git add -A` · `git add .` · `git commit -a` 처럼 **내가 지정하지 않은 것까지 담는** 명령.
//
// 왜 기계로 옮겼나 (마찰 장부 F006·F013·F014·F015 — 같은 원인 4건, 프로즈 대책은 5번 실패했다):
//   이 저장소는 세션 5~6개가 **같은 작업 트리와 같은 git 인덱스**를 공유한다. 그래서
//     · 남이 `git add` 해둔 파일이 내 인덱스에 얹혀 있고(F013·F014 — 경합)
//     · 유호님의 **git 밖 정본**(`docs/정본/SYNK STUDIO/`)과 **자격증명 폴더**가 같은 트리 안에 산다(F015 — 범위 과잉).
//   2026-08-03 F015 실사고: `git add -A` 가 유호님 사업문서 2종 + `.lnk` 를 담아 push까지 갔다.
//   같은 폴더에 `로그인.txt` 가 있었고, 안 나간 이유는 오직 `.gitignore` 한 줄이었다.
//   **「git 밖에 두기로 한 것」은 「추가하지 않기」로는 안 지켜진다 — 의도는 명령을 못 이긴다.**
//
// 무엇을 권하나: `git commit -m "..." -- 경로A 경로B`
//   경로를 주면 **인덱스를 통째로 무시**하고 그 파일만 커밋된다. 남이 스테이징해둔 것이 있어도 안 딸려온다.
//   (`git add A B && git commit -m ...` 도 허용 — 다만 두 명령을 **한 호출 안에서** 이어야 한다.
//    도구 호출 경계를 사이에 두면 그 틈에 피어가 커밋해 F014가 재발한다.)
//
// 통과 시 출력 없이 종료(기존 권한 흐름 유지), 위반 시 deny JSON.
// 의도적 예외는 명령에 GIT_SCOPE_BYPASS=1 을 붙여 의식적으로 우회한다.
'use strict';
const fs = require('fs');

function out(decision, reason) {
  process.stdout.write(JSON.stringify({
    hookSpecificOutput: {
      hookEventName: 'PreToolUse',
      permissionDecision: decision,
      permissionDecisionReason: reason,
    },
  }));
  process.exit(0);
}
const deny = (reason) => out('deny', reason);

let cmd = '';
try {
  const input = JSON.parse(fs.readFileSync(0, 'utf8'));
  cmd = String((input.tool_input && input.tool_input.command) || '');
} catch (_) {
  process.exit(0);
}

/* 실행되지 않는 텍스트를 먼저 걷어낸다 — clasp-guard의 2026-08-01 오탐 교훈 승계:
 * 커밋 메시지나 heredoc 본문에 `git add -A` 라고 **적기만 해도** 막히면,
 * 이 사고를 문서화하는 커밋 자체가 차단된다. 가드는 실행되는 명령을 봐야지 사람이 쓴 문장을 보면 안 된다. */
function stripNonExecutedText(s) {
  return s
    .replace(/<<-?\s*(['"]?)([A-Za-z_][A-Za-z0-9_]*)\1[\s\S]*?^\s*\2\s*$/gm, ' <<HEREDOC ')
    .replace(/(-m|--message)\s+(['"])[\s\S]*?\2/g, '$1 MSG');
}

const exec = stripNonExecutedText(cmd);
if (/GIT_SCOPE_BYPASS=1/.test(exec)) process.exit(0);

/* git 전역 옵션을 흡수하는 공용 접두사 — 아래 규칙은 **전부** 이것으로 시작해야 한다.
 * 왜: 규칙들이 `\bgit\s+add\b` 처럼 서브커맨드가 `git` 바로 뒤에 온다고 가정했는데,
 * 실제로는 `git -C <경로> add -A` 를 상시로 쓴다(settings.json 권한 목록에도 그 형태가 잔뜩 있다).
 * 2026-08-04 실측: 규칙 5개가 **하나도** 그 형태를 잡지 못했고, 못 잡는 방향은 「통과」였다.
 * 호출부마다 고치면 다음 규칙에서 또 틀리므로 상수 하나를 공유하고,
 * 회귀(tests/git범위가드)가 **모든 규칙이 -C 형태를 잡는지**를 검사한다.
 *   값을 따로 받는 옵션(-C <경로> · -c k=v · --git-dir <경로> …)과 값 없는 플래그(--no-pager)를 함께 흡수한다. */
const G = '\\bgit\\s+(?:(?:-[Cc]|--git-dir|--work-tree|--exec-path|--namespace)(?:=\\S+|\\s+\\S+)\\s+|--?[\\w-]+\\s+)*';
const re = (body, flags) => new RegExp(G + body, flags);

/* `-C <경로>`가 붙으면 그 저장소가 대상이다 — 아래 ④⑤는 git 상태를 실제로 읽으므로
 * cwd 대신 그 경로에서 물어야 한다(안 그러면 남의 저장소 상태로 판정한다). */
const cdir = /\bgit\s+(?:[^&|;]*?\s)?-C\s+(['"]?)([^'"\s]+)\1/.exec(exec);

/* …그리고 `-C` 없이 **셸이 옮겨간** 경우가 남아 있었다 (F134 · F137 과 같은 뿌리).
 *   이 훅의 `process.cwd()` 는 셸의 현재 위치가 아니라 **세션 프로젝트 폴더**다. 그래서
 *   `cd ../SYNK-talk && git merge …` 는 talk 를 향하는데 판정 재료는 appsscript 것이 된다.
 *   실측 F134: talk 워크트리(깨끗)를 향한 merge 가 **appsscript 의 인계문 2건**을 근거로 막혔다.
 *   그때는 거짓양성이라 안전했지만 **방향을 뒤집으면 통과다** — appsscript 가 깨끗하고 talk 에
 *   남의 미커밋이 있으면 ⑤ 되감기가 조용히 지나간다(가드가 지키려던 바로 그 경우).
 *   그래서 세그먼트를 왼쪽부터 훑어 git 을 만나기 직전의 위치를 쓴다. 못 읽는 경로면 안 옮긴다
 *   (파싱 실패가 판정을 엉뚱한 저장소로 보내면, 새는 방향은 또 「통과」다). */
function 셸이간곳() {
  const path = require('path');
  const 폴더인가 = (p) => { try { return fs.statSync(p).isDirectory(); } catch { return false; } };
  let 곳 = process.cwd();
  for (const seg of exec.split(/&&|\|\||[;\n]/)) {
    const m = /^\s*(?:cd|Set-Location|sl)\s+(?:-Path\s+)?(?:"([^"]+)"|'([^']+)'|(\S+))\s*$/.exec(seg);
    if (m) {
      const 다음 = path.resolve(곳, m[1] || m[2] || m[3]);
      if (폴더인가(다음)) 곳 = 다음;
      continue;
    }
    if (/\bgit\b/.test(seg)) break;   // git 이 실제로 도는 자리 = 지금까지 따라온 위치
  }
  return 곳;
}
const gitCwd = cdir ? cdir[2] : 셸이간곳();

const 대안 = '\n→ 대신: git commit -m "..." -- 경로A 경로B'
  + '\n   (경로를 주면 인덱스를 무시하고 그 파일만 커밋된다 — 남이 스테이징해둔 것이 있어도 안 딸려온다)'
  + '\n   묶어 쓰려면 한 호출 안에서: git add 경로A 경로B && git commit -m "..."'
  + '\n   의도적 예외라면 명령 앞에 GIT_SCOPE_BYPASS=1 을 붙인다.';

/* 🔑 발동한 **조각**을 돌려준다 — 발동 조건과 예외 조건을 **같은 조각**에서 본다 (F216·F218).
 *   둘을 각각 명령 **전체**에서 찾으면, 다른 조각의 예외가 통과권이 된다. 실측 3형이 전부
 *   「통과」로 샜다(가드가 존재하는 이유 그 자체를 뚫는 방향이다):
 *     · `git clean -n && git clean -fd`                  → ③ 미추적 삭제(복구 경로 0 · F025)
 *     · `git commit -- a.md && git commit -a -m x`       → ② 남의 스테이징 동승(F013·F014)
 *     · `git rebase --abort && git reset --hard origin/master` → ⑤ 남의 미커밋 소멸(F037)
 *   4번째라 호출부마다 고치지 않고 통로를 하나 둔다 — 새 규칙도 이걸 통해야 같은 구멍이 안 생긴다. */
const 걸린조각 = (발동, 예외) => exec.split(/&&|\|\||[;|\n]/).find((s) => 발동(s) && !(예외 && 예외(s)));

/* ① git add -A / --all / . — untracked까지 담는다(F015: 유호님 git 밖 정본·자격증명 폴더가 같은 트리에 있다) */
if (걸린조각((s) => re('add\\b[^&|;]*?(\\s-A\\b|\\s--all\\b|\\s\\.(\\s|$))').test(s))) {
  deny('[git-scope-guard] `git add -A` · `git add .` 차단 — 내가 지정하지 않은 파일까지 담는다.'
    + '\n이 저장소는 세션 여럿이 같은 작업 트리를 공유하고, 유호님의 git 밖 정본과 자격증명 폴더가 그 안에 있다.'
    + '\n2026-08-03 실사고(F015): 이 명령이 사업문서 2종을 커밋해 push까지 갔다.' + 대안);
}

/* ② git commit -a / --all — untracked는 안 담지만 **남의 tracked 수정**을 담는다(F013·F014 계열) */
if (걸린조각(
  (s) => re('commit\\b[^&|;]*?(\\s-a\\b|\\s--all\\b|\\s-[a-zA-Z]*a[a-zA-Z]*\\b)').test(s),
  (s) => re('commit\\b[^&|;]*?\\s--\\s').test(s))) {
  deny('[git-scope-guard] `git commit -a` 차단 — 추적 중인 **남의 미커밋 수정**까지 함께 커밋된다.'
    + '\n같은 작업 트리를 세션 5~6개가 공유한다(F013·F014 실사고).' + 대안);
}

/* ⑩ 훅 건너뛰기(`--no-verify`) — 프로즈로만 있던 금지를 기계로 옮긴다 (F473 · 2026-08-15).
 *
 * 실사고: 커밋 메시지의 회귀 숫자를 고치려고 `git commit --amend --no-verify` 를 썼다.
 *   CLAUDE.md 도 harness 도 「훅 건너뛰기는 유호님 명시 요청 때만」인데 **내 편의로** 썼다.
 *   `prepare-commit-msg` 는 `--no-verify` 가 안 막아 Session-Id 는 살았지만 그건 운이지 판단이 아니다.
 *
 * 🔴 이 저장소에서 특히 비싼 이유 — 이 한 플래그가 끄는 게이트가 **아홉**이다(실측 2026-08-15:
 *   pre-commit 여덟 = 계약동기화·계약동봉·이해대장·옛글자·인용·자리표·제어문자·대기열채번 + commit-msg 하나).
 *   그중 제어문자·옛글자는 **커밋되는 순간 이력에 영원히 남는** 축이라 다음 커밋으로 못 고친다.
 *
 * 🔑 급소는 `--no-verify` 가 아니라 **「안 재본 수를 메시지에 쓴 것」**이다 — F473 자신이 그렇게 판정했다.
 *   그 원인은 여기서 못 막는다. 다만 이 문을 닫으면 정정 경로가 「훅을 다시 지나는 amend」로 돌아오고,
 *   그건 몇 초짜리라 우회할 이유 자체가 사라진다.
 *
 * ⚠ `-n` 은 **commit 에서만** 잡는다 — `git push -n` 은 `--dry-run` 이라 오히려 안전한 쪽이고,
 *   그것까지 막으면 확인용 통로가 사라져 따를 수 없는 처방이 된다(F103). */
if (걸린조각((s) => re('commit\\b[^&|;]*?(\\s--no-verify\\b|\\s-[a-zA-Z]*n[a-zA-Z]*\\b)').test(s))
  || 걸린조각((s) => re('push\\b[^&|;]*?\\s--no-verify\\b').test(s))) {
  deny('[git-scope-guard] `--no-verify` 차단 — 훅 건너뛰기는 **유호님이 명시로 요청할 때만**이다 (F473).'
    + '\n이 한 플래그가 끄는 게이트가 아홉이다(pre-commit 여덟 + commit-msg 하나).'
    + '\n그중 제어문자·옛글자는 커밋되는 순간 **이력에 영원히 남아** 다음 커밋으로 못 고친다.'
    + '\n\n🔑 F473 이 난 자리: 메시지의 회귀 숫자가 틀려서 고치려고 붙였다. 급소는 이 플래그가 아니라'
    + '\n   **안 재본 수를 메시지에 쓴 것**이었다 — 숫자는 재고 나서 쓴다.'
    + '\n\n→ 메시지만 고칠 때:  git commit --amend -F <메시지파일>   (훅을 다시 지난다 · 몇 초다)'
    + '\n   훅이 실제로 틀렸으면 **그 훅을 고친다** — 끄는 것은 그 결함을 다음 세션에 넘기는 것이다.'
    + '\n   유호님이 명시로 요청한 자리라면: 명령 앞에 GIT_SCOPE_BYPASS=1');
}

/* ③ git clean — 미추적 파일을 지운다. 미추적은 이력·stash·reflog 어디에도 없어 **복구 경로가 0**이고,
 * 이 트리의 미추적 파일은 대개 **다른 세션이 방금 만든 작업물**이다(F025 실사고: 옆 세션의
 * 미커밋 테스트 파일과 codex/ 디렉터리가 정리 한 번에 소멸). dry-run(-n)만 통과 —
 * 커밋과 달리 범위를 좁혀도(경로 지정) 남의 신작을 지우는 성질이 그대로라 경로 예외를 두지 않는다. */
if (걸린조각(
  (s) => re('clean\\b').test(s),
  (s) => re('clean\\b[^&|;]*?(\\s-n\\b|\\s--dry-run\\b)').test(s))) {
  deny('[git-scope-guard] `git clean` 차단 — 미추적 파일은 git의 어떤 안전망에도 없어 복구가 불가능하고,'
    + '\n이 공유 트리의 미추적 파일은 대개 다른 세션이 방금 만든 작업물이다(F025 실사고).'
    + '\n→ 확인만 하려면: git clean -n (dry-run — 지우지 않고 목록만)'
    + '\n   특정 파일을 지우려면 rm 으로 그 파일만 명시 삭제한다.'
    + '\n   의도적 예외라면 명령 앞에 GIT_SCOPE_BYPASS=1 을 붙인다.');
}

/* ⑨ synk-memory 원격으로의 push — **세션은 커밋까지, push 는 스케줄러·Actions 몫** (F392).
 *   (번호는 추가순 — 자리는 성질순이라 exec 층 규칙들 옆에 둔다.)
 *   2026-08-13 실사고: 세션이 메모리 백업 저장소에서 push 를 직접 날렸다(e8909eb). fast-forward 라
 *   피해 0이었지만, 자동화(`auto: memory sync`)가 같은 원격에 동시에 쓰면 non-ff 거절이 나고 —
 *   그때 사람이 배우는 다음 수가 `--force` 다. 그 문이 열리기 전에 통로를 하나로 좁힌다.
 *   🔑 판정 재료는 **그 저장소의 원격 목록**이다(경로 무늬가 아니라) — 클론 위치가 바뀌어도 따라간다.
 *   원격 조회 실패는 「대상 아님」으로 접는다: 이 규칙의 새는 방향은 「통과」지만, 대상 아닌 저장소를
 *   막는 쪽으로 새면 BYPASS 습관을 가르친다(v6.11) — ③의 「경로 예외 없음」과 반대 성질의 자리다. */
{
  const 푸시조각 = 걸린조각((s) => re('push\\b').test(s));
  if (푸시조각) {
    let 원격들 = '';
    try {
      원격들 = require('child_process').execFileSync('git', ['-C', gitCwd, 'remote', '-v'],
        { encoding: 'utf8', stdio: ['ignore', 'pipe', 'pipe'] });
    } catch (_) { /* 저장소 아님·git 없음 — 이 규칙의 대상이 아니다 */ }
    if (/synk-memory/i.test(원격들) || /synk-memory/i.test(푸시조각)) {
      deny('[git-scope-guard] synk-memory 로의 `git push` 차단 — 그 원격은 스케줄러·Actions 가 쓴다(세션은 읽기·커밋까지 · F392).'
        + '\n커밋까지는 세션 몫이 맞다 — 남긴 커밋은 다음 `auto: memory sync` 가 실어 나른다.'
        + '\n같은 원격에 둘이 쓰면 non-ff 거절이 나고, 그 다음에 사람이 배우는 수가 --force 다.'
        + '\n→ 커밋에서 멈춘다. 자동화가 죽은 것을 **확인한** 경우에만 GIT_SCOPE_BYPASS=1 을 붙인다.');
    }
  }
  /* ⑨-b 처방문 되먹임 구멍(가드 맹점 ③): 위 사유의 「push 는 자동화 몫」을 읽은 세션이 그
   * 자동화 스크립트(tools/memory-push.cmd — Task Scheduler 가 매시 돌린다)를 **손으로 당기면**
   * 같은 push 가 이 가드 시야 밖에서 재현된다(.cmd 안의 git 은 훅을 안 지난다). 호출만 막고
   * 읽기는 자유다 — 조각의 **첫 낱말**이 그 스크립트일 때만 호출이다(cat·type 등 읽개가 첫
   * 낱말이면 언급일 뿐이다). 스케줄러 자신은 세션 훅 밖에서 돌므로 이 규칙에 안 닿는다. */
  const 메모리푸시호출 = 걸린조각((s) =>
    /^\s*(?:(?:call|start|cmd(?:\.exe)?\s+\/[ck]|&|\.)\s+)?["']?(?:[A-Za-z]:)?[\w.\/\\~-]*memory-push(?:\.cmd)?["']?(?:\s|$)/i.test(s));
  if (메모리푸시호출) {
    deny('[git-scope-guard] 스케줄러의 push 스크립트(tools/memory-push.cmd)를 세션이 직접 호출 — synk-memory push 와 같은 통로 위반이다(F392).'
      + '\n그 스크립트는 Task Scheduler(SYNK_MemoryPush)가 매시 돌린다 — 남긴 커밋은 다음 정시가 싣는다.'
      + '\n→ 커밋에서 멈춘다. 자동화가 죽은 것을 **확인한** 경우에만 GIT_SCOPE_BYPASS=1 을 붙인다.');
  }
}

/* ④ rebase·merge 진행 중의 commit — 2026-08-04 실사고(F038).
 * 옆 세션이 리베이스를 도는 동안 다른 세션이 `git commit -- 경로`를 했고, 그 커밋이
 * **detached HEAD 위에 얹혀** 리베이스 순서 안으로 들어갔다(HEAD가 리베이스 도중 전진해 인덱스가 꼬임).
 * 🔑범인은 부주의가 아니라 **도구가 상태를 안 보여준 것**이다 — `git status --short`는
 * 「rebase in progress」를 **한 글자도 표시하지 않는다**. 범위를 확인하는 습관(`--short`)이
 * 오히려 이 상태를 가린다. 그래서 사람의 주의가 아니라 훅이 본다.
 * 인덱스는 저장소당 하나인데 리베이스는 그 인덱스를 독점한다고 가정한다 — 이 트리에선 거짓이다. */
/* 🔑 2026-08-05 F103: 이 규칙이 **자기 처방을 막았다**. 안내문은 「마치거나(--continue) 되돌린 뒤(--abort)」
 *   라고 적어두고, 그 두 명령이 다시 동사 목록의 `merge` 에 걸려 deny 됐다(실측 재현: 둘 다 deny).
 *   남는 탈출구가 GIT_SCOPE_BYPASS 하나뿐이면 **우회가 정상 통로가 된다** — memory `guard-detection-layers`.
 *   ⚠ 장부가 적어둔 원인 후보 「env 접두어(GIT_EDITOR=)를 매처가 못 읽는다」는 **실측으로 반증됐다**:
 *      접두어를 떼도 똑같이 deny 다. `git rebase --continue` 가 통과하던 것도 처방이 맞아서가 아니라
 *      `rebase` 가 이 목록에 없어서 생긴 **우연**이었다(같은 축인데 한쪽만 막히는 비대칭이 증거다).
 *   그래서 둘을 가른다:
 *     · 진행 중인 작업을 **마치거나 되돌리는** 형태(--continue/--abort/--skip/--quit)
 *       → 새 커밋을 그 위에 얹는 게 아니라 그 작업 자체를 끝낸다. ④는 비켜선다.
 *       여기서 allow 로 조기 종료하지 않는다 — 그러면 ⑤(--abort 의 「더러운 트리」 검사)가 죽는다.
 *       ④가 비켜서도 --abort 는 ⑤가 계속 본다(보호는 그대로, 통로만 열린다).
 *     · 그 밖의 커밋 생성(F038 원형: 남의 rebase 중 `git commit -- 경로`) → 그대로 차단.
 *   그리고 **체인을 분해해서 본다** — `git add <해소한 파일> && git merge --continue` 가
 *   commit 계열 단어 하나 때문에 통째로 막히던 것이 F103 의 나머지 절반이다.
 *   ⚠ 이 자리에 「①②③은 `[^&|;]` 라 안전하고 ④만 그랬다」고 적혀 있었는데 **틀렸다**(F216·F218):
 *     발동 정규식이 구분자를 안 넘어도, **예외** 정규식을 명령 전체에서 찾으면 다른 조각의 예외가
 *     통과권이 된다. ②③⑤⑦이 전부 그 형태였다 — 지금은 넷 다 `걸린조각` 을 통한다. */
const 마침꼴 = '(merge|rebase|cherry-pick|revert)\\b[^&|;]*?\\s--(continue|abort|skip|quit)\\b';
const 커밋생성 = !!걸린조각(
  (s) => re('(commit|cherry-pick|revert|merge)\\b').test(s),
  (s) => re(마침꼴).test(s));
if (커밋생성) {
  const { execFileSync } = require('child_process');
  let gitDir = null;
  try {
    gitDir = execFileSync('git', ['rev-parse', '--absolute-git-dir'], { encoding: 'utf8', cwd: gitCwd }).trim();
  } catch { /* git이 없거나 저장소 밖 — 가드가 판단할 자리가 아니다 */ }
  if (gitDir) {
    const fs = require('fs');
    const p = require('path');
    const 진행 = [
      ['rebase-merge', 'rebase'], ['rebase-apply', 'rebase'],
      ['MERGE_HEAD', 'merge'], ['CHERRY_PICK_HEAD', 'cherry-pick'], ['REVERT_HEAD', 'revert'],
    ].find(([name]) => fs.existsSync(p.join(gitDir, name)));
    if (진행) {
      deny(`[git-scope-guard] ${진행[1]} 진행 중 — 새 커밋을 만들지 않는다.`
        + '\n지금 커밋하면 detached HEAD 위에 얹혀 그 작업의 순서 안으로 들어간다(2026-08-04 F038 실사고).'
        + '\n⚠ `git status --short`는 이 상태를 표시하지 않는다 — 범위만 확인하면 못 본다.'
        + '\n→ 먼저: git status (짧은 형식 말고 전체 — 진행 상태가 첫 줄에 나온다)'
        + `\n   **그 ${진행[1]} 가 내 것이면** 커밋으로 끝내지 말고 그 작업의 통로로 끝낸다:`
        + `\n     충돌 파일을 고친 뒤  git add <고친 파일> && git ${진행[1]} --continue`
        + `\n     되돌리려면        git ${진행[1]} --abort`
        + '\n     (이 두 형태는 이 규칙이 비켜선다 — BYPASS 를 붙일 필요가 없다. F103)'
        + '\n   다른 세션의 작업이면 **끝날 때까지 기다린다**(남의 병합을 대신 마치지 않는다).'
        + '\n   의도적 예외라면 명령 앞에 GIT_SCOPE_BYPASS=1 을 붙인다.');
    }
  }
}

/* ⑤ 작업 트리를 통째로 되감는 명령 — 2026-08-04 실사고(F037).
 * `git rebase --abort`가 작업 트리를 HEAD로 되돌리며 **옆 세션의 미커밋 편집 2파일을 쓸어냈다**
 * (abort를 한 것은 나고, 피해는 남이 봤다). ③ `git clean`이 미추적을 지우는 것과 같은 자리 —
 * 이쪽은 **추적 중인 남의 수정**을 지운다. 역시 reflog에도 stash에도 남지 않는다.
 * 🔑되감기 자체는 금지하지 않는다(정당한 필요가 있다 — 오늘 나도 abort가 필요했다).
 * 막는 것은 **트리에 미커밋 수정이 있는데 확인 없이 되감는 것**이고,
 * 깨끗한 트리에서는 조용히 통과한다(과잉 차단은 BYPASS 습관을 만든다 — ①~③과 같은 원칙). */
/* ⚠ ⑤의 `--abort` 는 **예외가 아니라 완화**다(아래 F103 주석). 그래서 조각을 고르는 순서가
 *   판정을 바꾼다 — 위험한 조각(abort 아닌 되감기)을 **먼저** 집고, 없을 때만 abort 조각을 쓴다.
 *   그냥 첫 조각을 쓰면 `git rebase --abort && git reset --hard` 가 abort 의 완화를 물려받는다. */
const 되감기꼴 = (s) => re('(rebase|merge|cherry-pick|revert)\\s+--abort\\b').test(s)
  || re('reset\\b[^&|;]*?\\s--hard\\b').test(s)
  || re('(checkout|restore)\\b[^&|;]*?\\s--\\s+\\.').test(s);
const 탈출구꼴 = (s) => re('(rebase|merge|cherry-pick|revert)\\s+--abort\\b').test(s);
const 되감기조각 = 걸린조각(되감기꼴, 탈출구꼴) || 걸린조각(되감기꼴);
if (되감기조각) {
  const { execFileSync } = require('child_process');
  let 더러운 = [];
  try {
    /* ⚠ core.quotepath=false 없으면 한글 경로가 8진 이스케이프로 나온다 — 이 저장소는 경로가 전부
     * 한글이라, 「무엇이 위험한지 보여주는」 바로 그 목록이 **읽을 수 없는 채로** 뜬다(⑦은 이미 이걸 붙였다). */
    더러운 = execFileSync('git', ['-c', 'core.quotepath=false', 'status', '--porcelain', '--untracked-files=no'],
      { encoding: 'utf8', cwd: gitCwd }).split('\n').filter((l) => l.trim());
  } catch { /* 저장소 밖 — 가드가 판단할 자리가 아니다 */ }

  /* 🔑 2026-08-05 F103 잔여 절반: `--abort` 는 ④를 비켜서게 해도 여기서 **영원히** 막혔다.
   *   충돌 난 merge·rebase 는 정의상 트리가 더럽다(충돌 파일이 unmerged 로 뜬다). 그래서
   *   「트리가 더러우면 되감기 금지」가 **그 작업의 유일한 탈출구**를 상시 차단한다 → 또 BYPASS.
   *   가르는 기준: unmerged 항목은 **그 작업 자신의 상태**지 남이 편집하던 것이 아니다.
   *   abort 가 그걸 버리는 건 사고가 아니라 abort 의 정의다. 그러니 위험 계산에서 뺀다.
   *   ⚠ 빼는 것은 **그 작업의 정식 탈출구(--abort)일 때만**이다 — reset --hard·checkout -- . 는
   *      그 작업과 무관한 뭉툭한 도구라 전부 그대로 센다(F037 보호는 거기서 온전하다).
   *   남의 tracked 수정(M·A·D…)은 여전히 센다 — 그게 F037 이 지키려던 바로 그것이다. */
  const 탈출구 = 탈출구꼴(되감기조각);   // 명령 전체가 아니라 **걸린 그 조각**을 본다(F218)
  const unmerged = (l) => /^(DD|AU|UD|UA|DU|AA|UU)\s/.test(l);
  const 충돌수 = 더러운.filter(unmerged).length;
  if (탈출구) 더러운 = 더러운.filter((l) => !unmerged(l));
  더러운 = 더러운.slice(0, 12);

  if (더러운.length) {
    deny('[git-scope-guard] 되감기 차단 — 작업 트리에 **미커밋 수정**이 있다.'
      + '\n이 트리는 세션 여럿이 공유하고, 그 수정은 대개 다른 세션이 지금 작업 중인 것이다.'
      + '\n되감으면 그 편집은 reflog·stash 어디에도 남지 않는다(2026-08-04 F037 실사고: 옆 세션 2파일 소멸).'
      + '\n\n지금 트리의 미커밋 수정:\n  ' + 더러운.join('\n  ')
      + (탈출구 && 충돌수
        ? `\n(충돌 중인 ${충돌수}개는 뺐다 — 그건 이 작업 자신의 상태라 abort 가 버리는 게 정상이다. 위는 그와 무관한 수정이다.)`
        : '')
      + '\n\n→ 남의 것이면: 그 세션에 알리고 커밋될 때까지 기다린다.'
      + '\n   내 것이면: 먼저 커밋하거나 `git stash push -- 경로`로 대피시킨 뒤 되감는다.'
      + '\n   의도적 예외라면 명령 앞에 GIT_SCOPE_BYPASS=1 을 붙인다.');
  }
}

/* ⑦ git stash — 규칙⑤(되감기)와 **같은 자리, 방향만 다르다**. 되감기는 남의 수정을 지우고,
 *   stash 는 남의 수정을 **어딘가로 옮긴다**. 둘 다 「내 트리인 줄 알았는데 공유 트리였다」가 원인.
 *
 *   2026-08-04 F066 실사고: 한 세션이 CI 실패 원인을 가리려고 `git stash` 를 걸었는데,
 *   그 안에 **다른 세션이 121줄 수정 중이던 clasp-guard.js** 가 들어갔다. 그 명령이 2분 타임아웃에
 *   걸려 pop 이 실행되지 않을 뻔했고, 실제로 그 사이 **원 세션이 「내 편집이 사라졌다」고 오판해
 *   복구를 시작하기 직전까지 갔다**(F068). 피해가 작았던 건 설계가 아니라 pop 이 제때 돈 덕이다.
 *
 *   🔑 stash 를 금지하지는 않는다 — 규칙⑤의 안내문이 「내 것이면 stash 로 대피시켜라」라고
 *      권하고 있고, 그건 여전히 옳다. 가르는 기준은 **범위**다:
 *        경로 미지정 → 트리 전체를 쓸어 담는다. 남의 것이 반드시 함께 간다 → 차단
 *        경로 지정   → 의도가 좁다. 통과시키되 그 경로의 미커밋을 보여준다(내 것이 맞는지 확인용)
 *      깨끗한 트리에서는 조용히 통과한다(과잉 차단은 BYPASS 습관을 만든다). */
/*   ⚠ **체인을 분해해서 본다**(F216 · ④가 F103 에서 받은 처방을 여기만 안 받고 있었다).
 *      명령 전체를 한 덩어리로 보면 **양방향으로** 틀린다 — 실측 둘 다:
 *        · `git stash list; git stash show` → `(\S*)` 가 구분자까지 삼켜 서브커맨드를 「list;」로
 *          잡고 **읽기 전용 조회를 차단**한다. 하필 그게 repo-staleness 훅이 시키는 진단이라
 *          통로가 BYPASS 밖에 없어진다(따를 수 없는 처방은 우회를 정상 통로로 만든다).
 *        · `git stash list && git stash` → **첫 번째 하나만** 보므로 뒤엣 맨 stash 가 통과한다.
 *          그게 트리를 통째로 쓸어담는 F066 사고 그 형태다. 새는 방향은 언제나 통과다. */
/*   🔴 2026-08-07 F212 잔여 — 「읽기전용」이라 이름 붙은 칸에 **파괴 하위명령이 5개** 있었다.
 *      list·show 옆에 pop·apply·drop·clear·branch 가 같이 앉아 전부 무사통과였고, 새는 방향은 통과다.
 *        · pop·apply  = 이 규칙이 막는 것의 **역방향**이다. 담는 대신 **쏟는다** — 공유 트리의
 *          남의 미커밋 위로 merge 가 돌고, 충돌하면 **남의 파일에 충돌 표시가 박힌다**.
 *          repo-staleness(F208)가 autostash 잔재를 알리며 「pop 은 대조 전 금지」라고 말하는데
 *          그 금지를 세는 층이 없었다 — 같은 판정이 두 곳에 적혀 갈라진 자리(CLAUDE.md 훅 맹점④).
 *        · drop·clear = ③ `git clean` 과 같은 자리다. autostash 잔재는 **어느 브랜치에도 없는
 *          유일본**이라(F208 실물: 남의 7파일) 지우면 복구 경로가 0이다. clear 는 그걸 전부 한다.
 *        · branch    = 조회처럼 생겼지만 성공하면 stash 를 **drop 한다**. 없애기 쪽에 둔다.
 *      가르는 기준은 ⑦의 원칙 그대로 **범위**다 — 잃을 것이 없으면(깨끗한 트리·빈 stash) 조용하다. */
{
  const { execFileSync } = require('child_process');
  const 하위 = (s) => { const m = re('stash\\b\\s*(\\S*)').exec(s); return m ? String(m[1] || '') : null; };
  const 조회 = /^(list|show|create|store)$/;   // 트리도 stash 목록도 안 줄인다
  const 쏟기 = /^(pop|apply)$/;
  const 없애기 = /^(drop|clear|branch)$/;
  const 조각 = (판정) => 걸린조각((s) => { const v = 하위(s); return v !== null && 판정(v); });
  const 미커밋 = () => {
    try {
      return execFileSync('git', ['-c', 'core.quotepath=false', 'status', '--porcelain', '--untracked-files=no'],
        { encoding: 'utf8', cwd: gitCwd }).split('\n').filter((l) => l.trim());
    } catch { return []; /* 저장소 밖 — 가드가 판단할 자리가 아니다 */ }
  };

  /* ⑦-c 없애기 — 트리 상태와 무관하다. 잴 것은 **지우면 되돌릴 데가 남는가** 하나다.
   *   🔑 통과 조건을 **보존**으로 뒀다(⑨와 같은 통로 · F103). 태그·브랜치가 그 stash 커밋을
   *   가리키면 조각을 지워도 내용은 이력에 남으니 조용히 통과한다 — 그래서 차단문이 시키는
   *   두 걸음(못 박고 → 지운다)이 **실제로 통한다**. 「어차피 못 지운다」로 두면 남은 출구가
   *   BYPASS(전 규칙 off) 뿐이고, 그게 F212 가 났던 자리다. 잔재를 영영 못 치우게 하지도 않는다.
   *   ⚠ repo-staleness 는 지금 잔재 정리를 `stash drop` 한 줄로 처방한다 — 그 줄은 여기서 막히므로
   *     그쪽 문구에 「못 박고 나서」가 붙어야 두 층이 다시 한 말을 한다(그 파일은 남이 편집 중이라
   *     안 건드렸다 · 조율은 커밋 메시지로 · `local_2909a862` 의 교차 회귀가 이 자리를 감시한다). */
  const 없애는조각 = 조각((v) => 없애기.test(v));
  if (없애는조각) {
    let 잔 = [];
    try {
      잔 = execFileSync('git', ['stash', 'list', '--format=%H %gd %gs'], { encoding: 'utf8', cwd: gitCwd })
        .split('\n').map((l) => /^([0-9a-f]{40}) (stash@\{\d+\}) (.*)$/.exec(l)).filter(Boolean)
        .map((m) => ({ sha: m[1], ref: m[2], 제목: m[3] }));
    } catch { /* 저장소 밖 — 가드가 판단할 자리가 아니다 */ }
    /* clear 는 전부, drop·branch 는 지목한 하나(생략하면 stash@{0}) */
    const 겨눈 = re('stash\\b[^&|;]*?(stash@\\{\\d+\\})').exec(없애는조각);
    const 겨냥 = /\bclear\b/.test(없애는조각) ? 잔 : 잔.filter((s) => s.ref === (겨눈 ? 겨눈[1] : 'stash@{0}'));
    /* 🔴 stash **바깥** ref 만 센다 — `refs/stash` 는 정의상 stash@{0} 을 가리키므로 그걸 세면
     *   맨 위 조각이 **언제나 못 박힌 것**으로 읽혀 규칙이 통째로 안 돈다(실 저장소 탐침이 잡았다:
     *   픽스처는 초록인데 라이브는 무음 통과 — 새는 방향은 여기서도 통과였다).
     *   못 재면 「못 박혔다」가 아니라 「모른다」다 — 모름은 차단 쪽에 둔다. */
    const 못박힘 = (sha) => {
      try {
        return execFileSync('git', ['for-each-ref', '--points-at', sha, '--format=%(refname)',
          'refs/tags', 'refs/heads', 'refs/remotes'], { encoding: 'utf8', cwd: gitCwd }).trim().length > 0;
      } catch { return false; }
    };
    const 잃을것 = 겨냥.filter((s) => !못박힘(s.sha)).slice(0, 12);
    if (잃을것.length) {
      const 첫 = 잃을것[0].ref;
      deny('[git-scope-guard] stash 를 **지우는** 명령 차단 — 이 조각을 가리키는 ref 가 하나도 없다(지우면 복구 경로 0).'
        + '\n이 트리는 세션 여럿이 공유하고, 여기 쌓인 것은 대개 **남의 rebase 가 걷어간 autostash 잔재**다'
        + '\n(F208 실물: 7파일이 갇힌 채 주인은 그게 어디 있는지도 모른다). ③ `git clean` 과 같은 자리다.'
        + '\n\n지울 뻔한 것:\n  ' + 잃을것.map((s) => `${s.ref}  ${s.제목}`).join('\n  ')
        + `\n\n→ 무엇이 들었나:  git stash show --stat ${첫}`
        + `\n   내 것만 꺼낸다: git checkout ${첫} -- 경로A 경로B   (지우지 않고 그 파일만 가져온다)`
        + `\n→ **정리가 맞다면 못 박고 나서 지운다** — 그러면 같은 명령이 그대로 통과한다:`
        + `\n     git tag 보관/autostash-0 ${첫}   그 뒤 원래 명령을 다시`
        + '\n   (태그가 그 커밋을 가리키면 stash 를 비워도 내용은 이력에 남는다)'
        + '\n   의도적 예외라면 명령 앞에 GIT_SCOPE_BYPASS=1 을 붙인다.');
    }
  }

  /* ⑦-b 쏟기 — pop·apply 는 공유 트리에 **쓴다**. 깨끗하면 정당한 복구라 조용히 지나간다. */
  if (조각((v) => 쏟기.test(v))) {
    const 더러운 = 미커밋().slice(0, 12);
    if (더러운.length) {
      deny('[git-scope-guard] `git stash pop`·`apply` 차단 — 지금 트리에 **미커밋 수정**이 있다.'
        + '\n이건 ⑦이 막는 것의 역방향이다: 담는 대신 쏟는다. 겹치면 merge 가 돌고, 충돌하면'
        + '\n**남의 파일에 충돌 표시가 박힌다**(pop 은 그 상태로 stash 를 남기고 멈춘다).'
        + '\nrepo-staleness 훅도 같은 말을 한다 — 「pop 은 대조 전 금지」. 이 층이 그걸 센다.'
        + '\n\n지금 트리의 미커밋 수정:\n  ' + 더러운.join('\n  ')
        + '\n\n→ 먼저 대조한다:    git stash show -p stash@{0}'
        + '\n   내 조각만 살린다: git checkout stash@{0} -- 경로A 경로B  (트리 전체에 쏟지 않는다)'
        + '\n   의도적 예외라면 명령 앞에 GIT_SCOPE_BYPASS=1 을 붙인다.');
    }
  }

  /* ⑦-a 쓸어담기 (원래 규칙) — bare `git stash` · `-u` · `push` · `save` */
  const 문제세그 = 조각((v) => !조회.test(v) && !쏟기.test(v) && !없애기.test(v));
  if (문제세그) {
    const 더러운 = 미커밋().slice(0, 12);
    const 경로지정 = /stash\b[^&|;]*?\s--\s+\S/.test(문제세그);
    if (더러운.length && !경로지정) {
      deny('[git-scope-guard] 경로 없는 `git stash` 차단 — 작업 트리를 **통째로** 쓸어 담는다.'
        + '\n이 트리는 세션 여럿이 공유하고, 지금 있는 미커밋은 대개 다른 세션이 작업 중인 것이다.'
        + '\n2026-08-04 F066 실사고: 진단하려고 건 stash 에 옆 세션의 121줄 편집이 딸려 들어갔고,'
        + '\npop 이 늦어 그 세션이 「내 편집이 사라졌다」고 오판해 복구를 시작하기 직전까지 갔다(F068).'
        + '\n\n지금 트리의 미커밋 수정:\n  ' + 더러운.join('\n  ')
        + '\n\n→ 내 것만 대피시키려면 경로를 준다: git stash push -- 경로A 경로B'
        + '\n   진단하려고 잠시 치우는 거라면 **치우지 말고 읽는다**: git show HEAD:경로'
        + '\n   (남의 미커밋 파일은 진단 목적이라도 stash·checkout 하지 않는다)'
        + '\n   의도적 예외라면 명령 앞에 GIT_SCOPE_BYPASS=1 을 붙인다.');
    }
    if (더러운.length && 경로지정) {
      out('allow', '[git-scope-guard] 경로를 준 stash — 그 파일이 **내 편집이 맞는지** 확인했나.'
        + ' 남의 것이면 진단 목적이라도 치우지 말고 `git show HEAD:경로` 로 읽는다(F066).'
        + '\n지금 트리의 미커밋:\n  ' + 더러운.join('\n  '));
    }
  }
}

/* ⑥ 커밋 메시지를 **셸 인용에 맡기는 것** — 같은 자리 3번째다(F054·F056·F060).
 *   ① F054: Bash 에 PowerShell here-string(@'…'@)을 보내 본문이 문자열로 새어 들어갔다.
 *   ② F056: 같은 함정으로 커밋 **제목 첫머리에 @** 가 붙었다(제목 표식을 쓰는 규약이라 더 나쁘다).
 *   ③ F060: 큰따옴표 안 백틱이 **명령 치환**으로 해석돼 인용한 코드가 통째로 사라졌다
 *           (bash: mono: command not found). 타 세션이 이미 push 한 뒤라 amend 도 못 했다.
 * 프로즈는 두 번 실패했다 — v7.11 「셸이 둘이다」 조항이 있는 상태에서 ③이 났고,
 * F056 이 「스크래치패드 파일 + -F 로 통일한다」고 처방까지 적어둔 뒤였다.
 * 지침 자체가 「3번째 = 원인을 쓸 수 없게 만든다」를 요구하므로 여기서 기계로 막는다.
 *
 * 🔑 원본 `cmd` 를 본다 — 위쪽 `exec` 는 stripNonExecutedText 가 `-m "…"` 을 `MSG` 로 지워버려
 *    **정확히 이 결함이 안 보인다**. 가드가 자기 전처리에 눈이 머는 자리다.
 *
 * 작은따옴표는 검사하지 않는다 — Bash·PowerShell 둘 다 리터럴이라 안전하다.
 * 이스케이프한 것(\` \$)도 통과시킨다: 위험한 건 「셸이 해석하는」 문자지 「글자로 쓴」 문자가 아니다. */
const 메시지인용 = /(?:-m|--message)[= ]\s*"((?:[^"\\]|\\.)*)"/g;
for (let m; (m = 메시지인용.exec(cmd)) !== null;) {
  const 본문 = m[1].replace(/\\./g, ''); // 이스케이프된 문자는 셸이 안 건드린다
  const hit = /`/.test(본문) ? '백틱(`)'
    : /\$\(/.test(본문) ? '$( )'
    : /\$\{|\$[A-Za-z_]/.test(본문) ? '$변수'
    : null;
  if (!hit) continue;
  deny(`[git-scope-guard] 커밋 메시지 안의 ${hit} — 셸이 해석해서 그 부분이 **사라지거나 딴것으로 바뀐다**.`
    + '\n2026-08-04 F060 실사고: 백틱으로 인용한 코드가 명령 치환으로 실행돼 메시지에서 통째로 없어졌고,'
    + '\n그때는 타 세션이 이미 push 한 뒤라 amend 도 불가능했다(공유 브랜치 force-push 는 문구보다 비싸다).'
    + '\n같은 자리 3번째다(F054 here-string 누출 · F056 제목 첫머리 @ · F060 백틱).'
    + '\n\n→ 대신 파일로 준다(셸을 아예 안 거친다):'
    + '\n   1) Write 도구로 스크래치패드에 메시지 파일을 쓴다'
    + '\n   2) git commit -F <그 파일> -- 경로A 경로B'
    + '\n   짧은 한 줄이면 작은따옴표도 안전하다: git commit -m \'제목\' -- 경로'
    + '\n   의도적 예외라면 명령 앞에 GIT_SCOPE_BYPASS=1 을 붙인다.');
}

/* ⑧ **범위 안의 내용** — 규칙 ①~⑦ 전부가 못 보던 자리 (F073 · F104 · 2026-08-05).
 *
 * 🔑 이 가드가 여섯 곳에서 권하는 `git commit -m "…" -- 경로` 는 **인덱스**로부터는 지켜주지만
 *    **파일 안**으로부터는 안 지켜준다. 경로를 못 박아도 커밋되는 건 그 경로의 **작업본 현재 상태**라,
 *    같은 파일에 남이 편집해 둔 줄이 있으면 통째로 함께 실린다. 실측 3건이 전부 이 형태다:
 *      · d64ad85 — 남이 되살린 Part 06 서체가 내 「주석 현행화」 커밋에 실려 push (F104 신고 원문)
 *      · b489f2e — 남의 「브랜드킷 재실측」 줄 편집이 내 보드 커밋에 실림
 *      · 6711ff2 — 같은 세션이 하루 안에 재발. 이번엔 남의 줄 **삭제**가 실림
 *    F073 까지 세면 **4번째**다. 지침이 「3번째 = 원인을 쓸 수 없게 만든다」를 요구하는 자리.
 *
 * 왜 프로즈로는 안 됐나: 규약에 「커밋 전 `git diff --stat` 전량 확인」이 이미 있다. 그런데
 *   `--stat` 은 **파일 이름만** 보여준다 — 셋 다 그 파일이 자기 범위인 걸 알고 있었고,
 *   모르던 건 **그 안에 남의 hunk 가 들어있다는 것**이다. 이름을 세는 검사는 원리상 못 본다.
 *
 * 무엇을 재나: 범위 안에서 **「내 것뿐」이라고 증명되지 않는** 파일. 판정층은 새로 만들지 않고
 *   `tools/작업본소유자.js` 하나에서 가져온다(같은 판정을 두 곳에 적으면 갈라진다 — 맹점 ④).
 *   증명된 내 것 = 종류가 `내것` **이고** `함께`(나 말고 만진 세션)가 비어 있을 때뿐이다.
 *   ⚠ 「모름」은 안전이 아니다 — Bash·스크립트가 쓴 파일은 아무 기록도 안 남는다(6711ff2 가 그 형태).
 *
 * 어떻게 막나 — **차단이 아니라 「보여주고 다시」**다. 안 본 diff 면 그 자리에서 **내용을 펼쳐** deny 하고,
 *   같은 명령을 그대로 재실행하면 통과한다(같은 내용이면 해시가 같다). 파일이 그 사이 바뀌면
 *   해시가 달라져 다시 펼친다. 🔑처방이 「같은 명령 재실행」이라 **가드가 자기 처방을 막을 수 없다**
 *   (맹점 ③ · F103 이 정확히 그걸로 났다). BYPASS 를 정상 통로로 만들지 않는 것이 설계 목표다.
 *
 * 🔴 그런데 그 처방이 **실행 불가능해지는 자리가 있다** (F141 · 2026-08-06 실측 4회 연속 재차단).
 *   여러 세션이 상시 덧쓰는 공유 파일(`세션보드.md`·`마찰신호.md`)은 매 시도마다 내용이 달라져
 *   「같은 내용」에 영영 못 닿는다 — 무한 차단이고, 남는 문은 BYPASS 하나뿐이라 F103 재현이다.
 *   그래서 두 가지를 바꿨다: ①해시를 **파일마다** 잡아 두 번째 창은 **그 사이 바뀐 것만** 싣는다
 *   ②같은 범위로 **세 번째면 통과**시키되 그 창에 바뀐 것을 실어 보여준다(안 보여주고 통과가 아니다).
 *   검사와 커밋 사이의 경합 창은 원리상 못 없앤다 — 무한 차단이 지키는 건 그 창이 아니라 인내심뿐이다. */
{
  const { execFileSync } = require('child_process');
  const p = require('path');

  /** 따옴표를 존중하는 최소 토크나이저 — 한글 경로엔 공백이 없지만 `docs/정본/SYNK STUDIO/` 는 있다. */
  const 토큰 = (s) => (s.match(/"[^"]*"|'[^']*'|\S+/g) || []).map((t) => t.replace(/^(['"])(.*)\1$/, '$2'));
  const 씻기 = (t) => t.replace(/\\/g, '/').replace(/^\.\//, '').replace(/\/+$/, '');

  /* 서브커맨드를 **토큰으로** 가른다.
   * ⚠ 이유를 정확히 적는다 — 처음엔 「정규식 G+commit 은 `git log --oneline commit` 같은 읽기 명령까지
   *   잡는다」고 적었는데 **실측으로 거짓이었다**: 흡수기 `--?[\w-]+\s+` 는 대시로 시작하는 토큰만 삼켜서,
   *   `log`·`branch`·`show` 처럼 대시 없는 서브커맨드가 앞에 오면 거기서 막힌다(8종 실측 · 오탐 0).
   *   틀린 근거를 주석에 남기면 다음 사람이 그걸 믿고 판단하므로 원문을 고쳐 둔다.
   * 그래서 진짜 이유는 둘이다:
   *   ① 아래 ⓑ가 `add` **뒤의 경로들**을 필요로 한다 — 정규식은 위치를 안 돌려준다.
   *   ② 값을 받는 옵션이 늘어나면 정규식은 조용히 어긋나지만(새는 방향은 통과), 토큰 판정은 목록 하나만 고친다. */
  const 값받는옵션 = /^(-[Cc]|--git-dir|--work-tree|--exec-path|--namespace)$/;
  function 서브커맨드(seg) {
    const ts = 토큰(seg);
    const gi = ts.findIndex((t) => t === 'git' || t.endsWith('/git') || t.endsWith('\\git.exe'));
    if (gi < 0) return null;
    for (let i = gi + 1; i < ts.length; i += 1) {
      const t = ts[i];
      if (값받는옵션.test(t)) { i += 1; continue; }
      if (t.startsWith('-')) continue;          // --no-pager · -c k=v · --git-dir=… 등 값을 붙여 쓴 꼴
      return { 이름: t, 뒤: ts.slice(i + 1) };
    }
    return null;
  }

  const 세그들 = exec.split(/&&|\|\||[;|\n]/);
  const 커밋세그 = 세그들.filter((seg) => {
    const s = 서브커맨드(seg);
    return s && s.이름 === 'commit' && !re(마침꼴).test(seg);
  });
  /* 저장소 밖이면 ⑧은 통째로 비켜선다 — ④⑤와 같은 자리다(「가드가 판단할 자리가 아니다」).
   * ⚠ 이것은 **판정층 고장과 다르다**: 저장소 밖에선 커밋 자체가 성립하지 않지만, 저장소 안에서
   *   소유 판정만 못 돌린 것은 「미커밋 0건」이 아니라 **모름**이고 그건 아래에서 펼친다. */
  let 저장소인가 = false;
  try {
    execFileSync('git', ['rev-parse', '--absolute-git-dir'], { encoding: 'utf8', cwd: gitCwd });
    저장소인가 = true;
  } catch { 저장소인가 = false; }
  if (커밋세그.length && 저장소인가) {

    /* 범위 = ⓐ `commit … -- 경로들` ⓑ 같은 호출의 `git add 경로들` ⓒ 둘 다 없으면 스테이징된 것.
     * ⓒ 를 빠뜨리면 `git add` 를 앞선 호출에서 한 형태가 통째로 검사 밖이 된다(새는 방향은 통과). */
    /* 🔑 셸 꼬리는 경로가 아니다 (F193 · 2026-08-07). `… -- 보드.md 2>&1` 의 `2>&1`,
     *   `… -- 보드.md > out.txt` 의 `>`·`out.txt` 가 그대로 범위로 들어갔다(실측 범위키
     *   `2>&1|docs/…`·`>|docs/…|out.txt`). 파이프·`&&` 는 세그가 이미 갈랐지만 리다이렉션은
     *   같은 세그에 남는다. 해는 두 겹 — ⓐ 회차 카운터의 키가 **명령 표기마다 달라져** F141 이
     *   세운 「3번째면 통과」 탈출구가 안 열린다(=BYPASS 가 정상 통로가 된다 · F103)
     *   ⓑ 리다이렉션 대상이 저장소 안 경로면 안 담길 파일이 범위로 셰인다.
     *   목록은 **한 곳에서만** 판정한다 — ⓐ·ⓑ 두 갈래에 따로 적으면 갈라진다. */
    const 꼬리부터버림 = (ts) => {
      const j = ts.findIndex((t) => /^\d*[<>]/.test(t));
      return j >= 0 ? ts.slice(0, j) : ts;
    };
    let 범위 = [];
    for (const seg of 커밋세그) {
      const ts = 토큰(seg);
      const i = ts.indexOf('--');
      if (i >= 0) 범위.push(...꼬리부터버림(ts.slice(i + 1)));
    }
    if (!범위.length) {
      for (const seg of 세그들) {
        const s = 서브커맨드(seg);
        if (s && s.이름 === 'add') 범위.push(...꼬리부터버림(s.뒤).filter((t) => !t.startsWith('-')));
      }
    }
    let 스테이징만 = false;
    if (!범위.length) {
      스테이징만 = true;
      try {
        범위 = execFileSync('git', ['-c', 'core.quotepath=false', 'diff', '--cached', '--name-only'],
          { encoding: 'utf8', cwd: gitCwd }).split('\n').filter(Boolean);
      } catch { /* 저장소 밖 — 가드가 판단할 자리가 아니다 */ }
    }
    범위 = 범위.map(씻기).filter(Boolean);

    /* 🔴 소유 판정이 볼 저장소를 **첫 require 보다 먼저** 못 박는다 (F193 · 2026-08-07).
     *   `작업본소유자.js` 의 ROOT 는 **모듈 최상단 const** 라 require 하는 순간 굳는다. 이 줄이
     *   아래 ⑨ 의 require 뒤에 있었을 때, gitCwd 가 형제 저장소(SYNK-talk)여도 조사()는 늘
     *   appsscript 를 봤다 — 그리고 ⑧ 은 형제 태그가 붙은 항목을 `!i.저장소` 로 걸러내므로
     *   **talk 커밋에서는 잴 것이 하나도 안 남았다.** 실측: 픽스처의 진짜 미커밋=조용히 통과,
     *   픽스처에 없는 appsscript 경로=차단(두 방향 다 틀렸고 흔한 쪽이 통과다).
     *   ⚠ 이 자리는 순서가 곧 정답이다 — 여기보다 위에서 저 모듈을 require 하면 그대로 재발한다.
     *   회귀 tests/git범위가드.test.js 「형제 저장소 커밋」이 그 순서를 못박는다. */
    if (!process.env.SYNK_OWNER_ROOT) process.env.SYNK_OWNER_ROOT = gitCwd;

    /* ⑨ **되돌림** — 범위 안의 파일이 남이 **이미 커밋한 판**보다 뒤로 가 있나 (F187 · F189 · 2026-08-07).
     *   판정은 「그 줄이 없나」가 아니라 「**내 판이 그 커밋의 부모인가**」다 — 아래 F189 주석이 그 이유다.
     *
     * 🔑 ⑧과 축이 반대다. ⑧은 「이 파일에 남의 **미커밋 편집**이 섞여 함께 실리나」를 본다.
     *    여기는 「남이 **커밋해 둔** 줄이 내 작업본에 **없나**」다. 트리는 하나인데 도구가 옛 내용을
     *    통째로 덮어쓰면 그 줄은 그냥 「삭제」로 커밋된다 — git 은 오류를 안 낸다(작업본 전체가 새
     *    스냅샷이라 의도한 삭제와 구별이 없다). 실사고: talk `tools/업로드왕복시험.js` 가 31f62d5 로
     *    커밋된 뒤에도 내 작업본은 옛 판이었고, 그대로 커밋했으면 **조용히 되돌아갔다**.
     *    기전은 도구다 — Edit 이 「file state is current」라며 **내가 처음 읽은 판**을 기준으로 치환해,
     *    그 사이 들어온 남의 줄을 조용히 옛 판으로 되돌린다. 사람이 「덮어쓰기」를 고른 적이 없다.
     *
     * 왜 ⑧으로는 안 잡히나 — 두 겹이다.
     *   ① 남의 **커밋**은 미커밋 목록에 안 나온다. 그 파일의 미커밋이 「내것 ∧ 함께 없음」이면
     *      ⑧은 통째로 비켜선다. 그래서 여기는 위험 목록이 아니라 **범위 전체**를 훑는다.
     *   ② ⑧이 펼쳐 보여주더라도 증상은 diff 의 「-」 줄 하나뿐이라 안 건드린 구역의 삭제를 **눈으로**
     *      세야 한다(F187 을 실제로 그렇게 잡았다). 규약의 `git diff --stat` 은 숫자만 보여줘 원리상 못 본다.
     *      처방 ① 이 요구한 것이 그 세기를 **기계로** 옮기는 것이다.
     *
     * 시간 경계 = **그 세션이 아직 살아 있나**. 옛 커밋의 줄을 지우는 건 평범한 작업이라 그것까지
     *   막으면 가드가 꺼진다. 살았나·세션 목록은 `작업본소유자` 하나에서 가져온다(두 곳에 적으면 갈라진다).
     *   ⚠ 세션 기록은 **연 저장소 키**에 쌓이는데 트랙 절반은 appsscript 를 열고 talk 파일을 만진다 —
     *     그래서 두 뿌리에서 모아 합집합으로 본다. 안 그러면 형제 저장소에서는 목록이 비어 영영 안 짖는다.
     *
     * 자기 처방(F103): 통과 조건이 **복원**이다 — 지운 줄이 돌아오면 같은 명령이 그대로 통과하고,
     *   같은 명령의 재실행만으로는 안 뚫린다(⑧과 일부러 다르게 뒀다 · 재실행은 되돌림을 안 고친다).
     *
     * ponytail: **마지막 커밋 하나만** 본다. 더 거슬러 올라가면 그 뒤 커밋이 정당하게 지운 줄까지
     *   「사라졌다」로 세어 거짓양성이 된다. 놓치는 변종(중간 커밋만 되돌리기)은 ⑧의 모름 칸이 받는다. */
    if (범위.length) {
      let 소유 = null;
      let 되돌림 = [];
      try {
        소유 = require(p.join(__dirname, '..', '..', 'tools', '작업본소유자.js'));
        const 나 = process.env.CLAUDE_CODE_HOST_SESSION_ID || '';
        const 메인 = p.join(__dirname, '..', '..');
        const 뿌리들 = [...new Set([gitCwd, 메인])];
        /* sid → {좌표: 그 세션이 마지막으로 만진 ms}. 두 뿌리의 기록을 **합친다** — 좌표계가 달라
         * (한쪽은 `docs/x`, 다른쪽은 `../SYNK-talk/docs/x`) 서로 덮지 않는다. 살았나 판정은 그대로. */
        const 산세션 = new Map();
        for (const r of 뿌리들) {
          for (const s of 소유.세션들(r)) {
            if (!소유.살았나(s)) continue;
            산세션.set(s.sid, { ...(산세션.get(s.sid) || {}), ...s.검사시각 });
          }
        }
        /* 🔑 F241 — 「남이 자기 줄을 **커밋한 뒤 제자리에서 고쳤다**」를 되돌림에서 뺀다 (2026-08-08).
         *   아래 F189 는 부모를 **그 커밋이 지운 줄**로 재는데, 순수 추가 커밋(지운 줄 0)엔 그 흔적이
         *   없어 「못 가르는 자리」로 막는 쪽에 남겼다. 그런데 **보드 선언 줄 추가가 정확히 그 형태다** —
         *   세션이 자기 줄 한 개를 더하고, 진행에 따라 그 줄을 고친다. 실측 08-08: `local_c8e8aa51` 이
         *   F239 선언 줄을 `7dd319a` 로 커밋한 뒤 같은 줄의 칸을 고쳤고, 그 다음 세션의 보드 커밋이
         *   2회 연속 차단됐다. 처방(「그 줄을 되살려라」)은 **남의 더 새로운 편집을 내 손으로 되돌리는 것**
         *   이라 F073 이 금지한 바로 그 행위다 = 따를 수 없는 처방, 남는 출구가 BYPASS 뿐(F103 축).
         *
         * 🔑 가르는 재료는 **줄 내용이 아니라 시계**다. 트리는 하나라, 그 세션이 커밋 뒤 그 파일을
         *   만졌으면 그 편집 결과가 지금 내 작업본에 그대로 있다 — 즉 내 판은 그 커밋의 부모가 아니라
         *   **후손**이고, 되돌림의 정의가 거짓이다. 재료는 `작업본소유자.세션들().검사시각`(track-collision
         *   이 경로별로 찍는다) 하나를 **읽기만** 한다 — 여기서 새로 만들면 두 곳이 갈라진다.
         *   ⚠ 반대 방향(`뒤늦은남의커밋`: 내가 만진 뒤 남이 커밋했나)과 같은 두 재료를 쓰지만 부등호가
         *     반대다. 별개 판정이라 통로를 합치지 않는다.
         *
         * 남는 맹점 하나: 그 세션이 고친 **뒤에** 내 도구가 다시 옛 판으로 덮으면 여기서 통과한다.
         *   그 경우 그 파일엔 그 세션의 미커밋이 반드시 있으므로 ⑧이 「함께 만졌다」로 받아 펼친다
         *   (회귀가 그 사각 없음을 못박는다). 못 재면(기록 없음·같은 초) 모름이라 그대로 막는다. */
        const 형제접두 = p.resolve(gitCwd) === p.resolve(메인) ? null : `../${p.basename(gitCwd)}/`;
        const 커밋뒤만졌나 = (who, 파일, 커밋초) => {
          const 기록 = 산세션.get(who);
          if (!기록) return false;
          const 때 = Math.max(0, ...[파일, 형제접두 && 형제접두 + 파일]
            .filter(Boolean).map((k) => Number(기록[k]) || 0));
          /* `%ct` 는 **초 절삭**이라 그 커밋의 실제 시각은 [ct, ct+1) 안 어디든이다. 같은 초는 「뒤」라고
           * 단정할 수 없으니 모름으로 두고 막는 쪽에 남긴다 — 새는 방향이 통과다. */
          return 때 > 0 && 때 >= (커밋초 + 1) * 1000;
        };
        const g = (a) => execFileSync('git', ['-c', 'core.quotepath=false', ...a],
          { encoding: 'utf8', cwd: gitCwd, maxBuffer: 8 << 20 });
        for (const f of g(['diff', 'HEAD', '--name-only', '--', ...범위]).split('\n').filter(Boolean).slice(0, 20)) {
          const [해시, ct, ...본문] = g(['log', '-1', '--format=%H%n%ct%n%B', '--', f]).split('\n');
          const sid = (본문.join('\n').match(/^Session-Id:\s*(\S+)/m) || [])[1];
          if (!해시 || !sid || sid === 나 || !산세션.has(sid)) continue;
          /* 🔑 F555 — **이름이 곧 주인인 파일**(인계문 조각)은 되돌림의 대상이 아니다 (2026-08-17).
           *   위 ⓑ 알리바이가 읽는 `검사시각` 은 track-collision 이 **Edit·Write 도구 호출에서만**
           *   찍는데, 인계문은 session-report 의 `원자쓰기` 가 fs 로 직접 쓴다 — 도구를 안 거치므로
           *   그 기록이 **원리상 한 줄도 안 남는다**(작업본소유자 273~282행이 같은 사실을 이미 적어
           *   뒀다: 「그 쓰기는 Edit·Write 를 안 거쳐서 `touched` 기록에 한 줄도 안 남는다」).
           *   그래서 ⓑ 가 절대 참이 될 수 없고, 생성기가 파일을 **통째로 다시 쓸 때마다**(append 가
           *   아니다) 옛 판의 줄이 「사라진」 것으로 읽혀 매번 막혔다.
           *   처방(「되살려라」)을 따르면 낡은 판으로 되돌아가고 다음 생성이 또 덮는다 = 따를 수 없는
           *   처방이라 BYPASS 가 정상 통로가 됐다(F103 · F555 신고 원문).
           * ⚠ 여는 범위를 두 겹으로 좁힌다 — 새는 방향은 언제나 「통과」다:
           *   ① 인계문 조각만. 보드 조각(`docs/_ops/보드/<지문>.md`)은 Write 도구로 쓰므로 ⓑ 가
           *      정상 작동한다 — 같이 열면 넓히는 쪽이다.
           *   ② **파일 이름의 주인 == 그 커밋의 주인**일 때만. 남이 그 인계문에 적어 둔 줄을 내가
           *      덮는 것은 생성기의 자기 갱신이 아니라 진짜 되돌림이다.
           *   판정은 새로 안 짓는다 — `작업본소유자.인계문지문` 하나를 부른다(두 곳에 적으면 갈라진다).
           *   못 부르면(`&&` 가 거짓) 그대로 막는 쪽에 남는다. */
          if (소유.인계문지문 && 소유.인계문지문(f) === 소유.짧게(sid)) continue;
          let 더한줄; let 지운줄;
          try {
            /* 부모가 없으면(최초 커밋) 잴 자리가 아니다 — 그 파일엔 「되돌릴 남의 줄」이라는 개념이 없다. */
            const 조각 = g(['diff', `${해시}^`, 해시, '--', f]).split('\n');
            const 살림 = (l) => l.slice(1).replace(/\r$/, '').trim();
            /* 짧은 줄(`}` · `});`)은 어디에나 있어 「있다」가 우연히 참이 된다 —
             * 새는 방향이 통과라 길이 하한을 둔다. 놓치면 ⑧이 받는다. */
            const 길다 = (l) => l.length >= 8;
            더한줄 = 조각.filter((l) => l.startsWith('+') && !l.startsWith('+++')).map(살림).filter(길다);
            지운줄 = 조각.filter((l) => l.startsWith('-') && !l.startsWith('---')).map(살림).filter(길다);
          } catch { continue; }
          if (!더한줄.length) continue;
          let 지금 = '';
          try { 지금 = fs.readFileSync(p.join(gitCwd, f), 'utf8'); } catch { continue; }
          /* 🔴 재는 단위가 판정 단위와 달랐다 — `지금.includes(줄)` 은 **줄 조각**도 참으로 읽는다.
           *   실측 08-07: 6536fd5 가 지운 이어쓰기 조각 `.split(/&&|…/)` 가 그 커밋이 새로 더한
           *   `걸린조각` 한 줄 **안에** 그대로 들어 있어, 옛 판이 살아 있는 것으로 읽혔다 →
           *   정당한 리팩터가 「되돌림」으로 차단됐고 처방(그 줄을 되살려라)은 이미 있는 줄을 가리켰다
           *   = 따를 수 없는 처방(F103·F212). 반대 방향도 같은 뿌리라 더 나쁘다 — 남이 커밋한 줄이
           *   다른 줄 **안에** 우연히 들어 있으면 「안 사라졌다」로 **통과**한다. 줄 집합으로 잰다. */
          const 지금줄 = new Set(지금.split('\n').map((l) => l.replace(/\r$/, '').trim()));
          const 사라진 = [...new Set(더한줄)].filter((l) => !지금줄.has(l));
          /* F189 — 「지웠다」와 **「그 줄을 제자리에서 고쳤다」**를 가른다.
           *   줄이 없다는 것만으로는 못 가른다: 수정하면 원문 문자열도 똑같이 사라진다.
           *   🔑 되돌림의 정의는 「없다」가 아니라 **「내 판이 그 커밋의 부모다」**이므로 부모를 잰다 —
           *   그 커밋이 **지운** 줄이 내 작업본에 아직 살아 있으면 내 판은 그 이전이다(=스테일).
           *   ⚠ 축이 파일 단위인 것이 맞다: 스테일은 줄이 아니라 **파일 판본**의 성질이라, 스테일이면
           *     그 옛 줄들이 통째로 남아 있다. 옛 줄도 남의 줄도 없으면 그 자리에 **새 내용**이 있는
           *     것이고 그건 사람이 고른 편집이다(그래도 지우는 게 맞나는 ⑧의 「모름」이 받는다).
           *   순수 추가 커밋(지운 줄 0)은 부모의 흔적이 없어 못 가르므로 옛 판정을 그대로 둔다 —
           *     새는 방향이 통과라, 못 가르는 자리는 막는 쪽에 남긴다.
           *   ⓐ 그 커밋 안에서 **자리만 옮긴 줄**(지웠다 다시 더함)은 부모의 증거가 아니다 — 뺀다. */
          const 더한집합 = new Set(더한줄);
          const 옛판줄 = 지운줄.filter((l) => !더한집합.has(l));
          if (사라진.length && (!옛판줄.length || 옛판줄.some((l) => 지금줄.has(l)))) {
            /* ⓑ 그 세션이 **커밋 뒤** 그 파일을 만졌으면 사라진 줄은 그 세션의 후속 편집이다 — F241.
             *   위 ⓐ(자리 이동)와 달리 이건 커밋 **밖**의 증거라 diff 로는 원리상 못 본다. */
            if (커밋뒤만졌나(sid, f, Number(ct))) continue;
            되돌림.push({ 파일: f, 해시: 해시.slice(0, 7), sid, 사라진 });
          }
        }
      } catch { 되돌림 = []; }
      /* ⚠ 못 쟀으면 여기서 조용히 통과한다 — 판정 불가를 통과로 번역하는 자리지만, 같은 파일을
       *   바로 아래 ⑧이 「모름」으로 받아 펼친다. 두 규칙 중 하나라도 서면 사각이 안 생긴다. */

      if (되돌림.length) {
        const 짧게 = (s) => (소유 ? 소유.짧게(s) : s.replace(/^local_/, '').slice(0, 8));
        const 총 = 되돌림.reduce((n, r) => n + r.사라진.length, 0);
        deny(`[git-scope-guard] 이 커밋은 **남이 방금 커밋한 ${총}줄을 지운다**(되돌림 · F187).`
          + '\n내 작업본이 그 커밋 **이전** 내용에서 갈라져 있다 — 트리는 하나인데 도구가 옛 내용을 통째로'
          + ' 덮어쓰면 남의 줄은 삭제로 실린다. git 은 오류를 안 내고 `--stat` 은 숫자만 보여줘 원리상 못 본다.'
          + `\n\n${되돌림.map((r) => `── ${r.파일}  ← ${r.해시} (${짧게(r.sid)} · 아직 도는 세션)\n`
            + r.사라진.slice(0, 6).map((l) => `   - ${l.slice(0, 120)}`).join('\n')
            + (r.사라진.length > 6 ? `\n   … ${r.사라진.length - 6}줄 더` : '')).join('\n\n')}`
          + '\n\n→ 되살려라: `git diff <해시>^ <해시> -- <파일>` 로 그 줄을 읽고 작업본에 다시 넣는다.'
          + '\n   (통째로 되감으려면 `git checkout <해시> -- <파일>` 뒤 내 편집을 다시 얹는다 — 내 편집이 날아가니 diff 를 먼저 읽어라)'
          + '\n   줄이 돌아오면 **같은 명령이 그대로 통과한다** — 재실행만으로는 안 뚫린다(통과 조건이 복원이다).'
          + '\n   정말 지우는 게 맞으면 그 세션에 알리고 GIT_SCOPE_BYPASS=1 을 붙인다.');
      }

      /* 판정층은 하나 — 작업본소유자를 **읽기만** 한다. 그게 못 돌면 「전부 모름」으로 내려간다:
       * 판정 불가를 통과로 번역하면 이 규칙은 있는 것보다 나쁘다(있는데 안심시킨다).
       * (뿌리 고정은 위로 올렸다 — F193. 여기서 하면 ⑨ 가 먼저 require 해 이미 늦다.) */
      let 항목 = null;
      try {
        const r = require(p.join(__dirname, '..', '..', 'tools', '작업본소유자.js')).조사();
        /* ⚠ `git못부름` 은 **빈 항목 배열과 함께** 온다 — `.항목` 만 꺼내 쓰면 「미커밋 0건」과
         *   구별이 안 되고, 그 0 은 조용한 통과가 된다. 판정 불가는 통과가 아니라 「전부 모름」이다. */
        항목 = r.git못부름 ? null : r.항목;
      } catch { 항목 = null; }

      const 안에드나 = (f) => 범위.some((r) => r === '.' || f === r || f.startsWith(`${r}/`));
      let 위험;
      if (항목 === null) {
        위험 = 범위.map((f) => ({ 파일: f, 왜: '소유 판정을 못 돌렸다(모름)' }));
      } else {
        /* 🔑 **형제 저장소 항목은 뺀다.** 이 판정은 `gitCwd` 의 커밋 범위를 보는 것이고, 형제 파일의
         *   경로는 그 저장소 기준이다. 안 빼면 범위가 `.` 인 순간 형제 미커밋 전부가 이 커밋에
         *   실릴 것처럼 걸리고, 펼치기는 `gitCwd` 에서 그 경로를 못 찾아 「내용을 못 읽었다」로 끝난다 —
         *   거짓 경보가 쌓이면 ⑧이 꺼진다(F134 확장이 항목에 형제를 넣으며 열린 자리다). */
        위험 = 항목.filter((i) => !i.트리 && !i.저장소 && 안에드나(i.파일))
          .filter((i) => !(i.종류 === '내것' && (!i.함께 || !i.함께.length)))
          .map((i) => ({
            파일: i.파일,
            왜: i.함께 && i.함께.length
              ? `나 말고 ${i.함께.map((s) => s.sid.replace(/^local_/, '').slice(0, 8)).join(', ')} 도 만졌다`
              : ({ 남의살아있는: '지금 도는 **남의** 작업본', 끝난세션: '끝난 세션이 남긴 것', 모름: '주인 **모름**(Bash·스크립트·외부 앱이 썼다)' }[i.종류] || i.종류),
          }));
      }

      if (위험.length) {
        /* 무엇이 실릴지 **펼쳐서** 보여준다. `--stat` 은 이름만 세서 이 사고를 원리상 못 본다. */
        const 조각 = [];
        const 미추적 = []; // F383 — 아래 처방문이 「add 부터」를 같이 말해야 하는 파일들
        for (const { 파일 } of 위험.slice(0, 8)) {
          let d = '';
          try {
            d = execFileSync('git', ['-c', 'core.quotepath=false', 'diff', 'HEAD', '--', 파일],
              { encoding: 'utf8', cwd: gitCwd, maxBuffer: 8 << 20 });
          } catch { d = ''; }
          if (!d.trim()) {
            // 미추적 신규 파일은 diff 가 비어 있다 — 비었다고 「변경 없음」으로 읽으면 통째로 사각이 된다.
            try {
              d = `(미추적 신규 파일 — 앞부분)\n${fs.readFileSync(p.join(gitCwd, 파일), 'utf8')}`;
              미추적.push(파일);
            } catch { d = '(내용을 못 읽었다)'; }
          }
          /* 🔴 자르기는 **줄 수와 바이트를 함께** 본다 (F316 · 2026-08-10 실측).
           *   줄 수만 세면 minify 된 HTML 한 줄이 54,293 바이트라 「40줄」이 곧 254만 바이트가 된다 —
           *   발표물 18건 커밋에서 실측 60만 토큰이 한 번에 유입돼 컨텍스트 창(200k)을 4배로 넘겼고,
           *   같은 트랙을 이어받은 세션 9개가 연쇄로 같은 자리에서 죽었다. 가드가 스스로 세션을 끝낸 것이다. */
          /* 세 값은 **역할이 다르다** — 최근 60커밋 diff 132건으로 조합을 재서 정했다(2026-08-10).
           *   · 줄당 300B  = 폭발을 막는 **유일한 방벽**. 사고 원인이 「한 줄 54,293자」였다.
           *                  이것만으로 최악이 54,357B → 363B 가 된다. 줄 수는 여기 아무 기여도 없다.
           *   · 파일당 12,000B = 최악의 **뚜껑**. 「모든 줄이 상한을 넘는」 diff 에서만 물린다.
           *                  이게 있어야 줄 수를 늘려도 최악이 안 늘어난다(8파일 96KB ≈ 32k 토큰 고정).
           *   · 줄 수 80   = **보이는 양을 정하는 손잡이**. 40줄일 때 원본의 52% 만 보였고,
           *                  80줄이면 71% 가 보이는데 최악은 32k 토큰 그대로다 — 공짜로 얻는 19%p.
           *
           * ⚠ 처음엔 파일당을 6,000B 로 잡고 「17% 가 여기 걸린다」고 판단했는데 **틀린 측정**이었다.
           *   diff 전체 크기가 6KB 를 넘는 비율을 쟀을 뿐, 훅이 실제로 자르는 방식을 재지 않았다.
           *   훅 자르기를 그대로 재현해 보니 파일당 상한이 물린 것은 **0건**이었다(6,000 이든 12,000 이든
           *   펼침 총량이 147,464B 로 동일). 실제로 자르고 있던 것은 40줄 제한(23%)과 줄당(59%)이다.
           *   → 값을 고칠 땐 「그 값이 실제로 물리는가」부터 재현해서 잰다. */
          const 줄수상한 = 80; const 줄당 = 300; const 파일당 = 12000;
          let 쓴바이트 = 0; let 바이트잘림 = false; const 보일줄 = [];
          for (const l of d.split('\n').slice(0, 줄수상한)) {
            if (쓴바이트 >= 파일당) { 바이트잘림 = true; break; }
            const 남은 = Math.min(줄당, 파일당 - 쓴바이트);
            if (l.length > 남은) { 보일줄.push(`${l.slice(0, 남은)}  …(이 줄 ${l.length - 남은}바이트 더)`); 바이트잘림 = true; }
            else 보일줄.push(l);
            쓴바이트 += Math.min(l.length, 남은) + 1;
          }
          const 줄수 = d.split('\n').length;
          const 꼬리 = [
            /* 상한은 위 한 곳에서만 정한다 — 같은 판정을 두 곳에 적으면 갈라진다(여기가 40 으로 굳어 있었다). */
            줄수 > 줄수상한 ? `… ${줄수 - 줄수상한}줄 잘림` : '',
            /* F103 — 처방은 실행 가능해야 한다. 여기서 `git diff` 전량을 시키면 그게 바로 컨텍스트를 터뜨린 그 명령이다. */
            바이트잘림 ? `⚠ 한 줄이 길어(최장 ${Math.max(...d.split('\n').map((l) => l.length))}B) 바이트로도 잘랐다 — 전량은 컨텍스트를 터뜨린다.\n   안전하게 보기: git diff HEAD --stat -- ${파일}  ·  줄 단위로 훑기: git diff HEAD -- ${파일} | cut -c1-200` : '',
          ].filter(Boolean).join('\n');
          const 글 = `── ${파일}\n${보일줄.join('\n')}${꼬리 ? `\n${꼬리}` : ''}`;
          /* 🔑 해시는 **파일마다** 잡는다 (F141). 통짜 해시는 파일 하나가 바뀌면 전부를 다시 펼치게 해서,
           *   두 번째 창이 「무엇이 그 사이 바뀌었나」를 못 보여주고 사람이 같은 200줄을 다시 훑는다.
           *   🔑 그리고 해시 재료는 **잘린 글이 아니라 원본 diff** 다 — 잘린 글로 잡으면 긴 줄 **안쪽**의
           *      변경이 해시를 안 흔들어 「아까 봤다」로 통과한다(자르기가 만든 새 사각). */
          조각.push({ 파일, 글, 해시: require('crypto').createHash('sha1').update(`${파일}\n${d}`).digest('hex') });
        }

        /* 「이 내용을 이미 펼쳐 봤나」를 해시로 기억한다. 시계·mtime 에 안 기댄다(CI·환경 의존 금지). */
        const store = require(p.join(__dirname, 'lib', 'handoff-store.js'));
        const 기억 = p.join(store.stateDir(),
          `commitscope-${store.projectKey(gitCwd)}-${store.safeId(process.env.CLAUDE_CODE_HOST_SESSION_ID || 'nosid')}.json`);
        let 상태 = { seen: [], 회차: {} };
        try { const j = JSON.parse(fs.readFileSync(기억, 'utf8')); 상태 = { seen: j.seen || [], 회차: j.회차 || {} }; } catch { /* 없으면 새로 */ }
        const 새것 = 조각.filter((c) => !상태.seen.includes(c.해시));

        if (새것.length) {
          /* 🔴 F141 — **처방이 실행 불가능해지는 자리**(F103 계열, 2026-08-06 실측 4회 연속 재차단).
           *   통과 조건이 「아까 본 것과 같은 내용」인데, `세션보드.md`·`마찰신호.md` 처럼 여러 세션이
           *   상시 덧쓰는 공유 파일에서는 매 시도마다 내용이 달라져 그 조건에 **영영 못 닿는다.**
           *   남는 탈출구가 BYPASS 하나뿐이면 우회가 정상 통로가 된다 — 그게 F103 이 남긴 교훈이다.
           *   그래서 **끝나게** 만든다: 같은 범위로 세 번째면 통과시키되, 그 창에 **그 사이 바뀐 것만**
           *   싣는다. 「안 보여주고 통과」가 아니라 「보여주면서 통과」다 — 어차피 검사와 커밋 사이의
           *   경합 창은 원리상 못 없앤다(커밋은 그 순간의 작업본을 싣는다). 무한 차단이 사는 것은
           *   그 창이 아니라 사람의 인내심뿐이다. */
          const 범위키 = `${범위.slice().sort().join('|')}\n${위험.map((w) => w.파일).sort().join('|')}`;
          const 회차 = (상태.회차[범위키] || 0) + 1;

          /* 🔴 F229 — 위 F141 탈출구는 **코드에만 있고 처방문이 안 가리켰다**(2026-08-08 실측).
           *   deny 문의 두 갈래가 이 두 파일에선 **둘 다 실행 불가능**하다:
           *     ①「위가 전부 내가 만든 것이면 다시 실행한다」 → 보드·장부는 정의상 남의 줄이 늘 섞여
           *        이 조건이 **영원히 거짓**이다. 규칙을 지키려는 쪽일수록 여기서 멈춘다.
           *     ②「그 파일을 범위에서 빼고 커밋한다」   → 보드가 유일 파일이면 **뺄 것이 없다**.
           *   그래서 신고자는 2회차에서 멈췄고(3번째면 열린다는 말이 어디에도 없다), 그 순간
           *   보드엔 종결 줄 7건이 미커밋으로 쌓여 있었다 — 새는 방향이 **인계 손실**이다.
           *
           * 🔑 고칠 자리는 문구가 아니라 **전제**다. ⑧이 지키는 것은 「내 커밋에 남의 미완성 편집이
           *   실려 나간다」인데, 이 두 파일에선 그 전제가 거짓이다: 섞이는 남의 줄은 그 세션이 자기
           *   트랙에 쓴 선언·종결이고 **함께 커밋되는 것이 이 파일의 관행이다**(board-guard 356행
           *   주석이 「보드를 상시 커밋하고 지나가는 세션이 남의 줄까지 동승시킨다」고 이미 관찰해 뒀다
           *   — 즉 관행과 가드가 서로 반대 말을 하고 있었다). 안 실리는 쪽이 손해다.
           *   ⑧이 여기서 잡을 수 있는 **진짜** 손해는 남의 줄 **삭제** 하나뿐이고 그건 ⑨의 자리다.
           *   그래서 회차를 안 세고 첫 시도부터 통과시키되 **diff 는 그대로 펼친다** — 3회차 allow 가
           *   이미 내린 판단(무한 차단은 BYPASS 를 정상 통로로 만든다)을 1회차로 앞당기는 것뿐이다.
           * ⚠ **전부** 공유판일 때만이다. 코드 경로가 하나라도 섞이면 그대로 차단한다(아래 안내문이
           *   「혼자 커밋한다」고 말하는 이유 — 묶으면 코드 쪽이 이 완화를 공짜로 물려받는다). */
          /* 🔴 F293 — 이 목록을 **여기 따로 적어** 판정이 두 벌이 됐고, 사본만 이주를 안 따라갔다
           *   (CLAUDE.md 가드 맹점 ④: 훅 안에서도 같은 판정을 두 곳에 적으면 갈라진다).
           *   적혀 있던 두 경로는 지금 **둘 다 옛 주소**다 — 보드는 세션별 파일로(F250), 장부는
           *   번호별 조각 파일로 갈렸다. 그래서 이 완화는 «아무도 안 쓰는 두 파일»에서만 열렸고,
           *   정작 살아 있는 공유판(보드 폴더·장부 폴더·대기열)은 첫 시도부터 막혀 매번 diff 를
           *   펼쳤다. 새는 방향이 여기선 「통과」가 아니라 **컨텍스트**다 — 그 펼침이 커밋 1회에
           *   60만 토큰을 태워 세션 9개를 죽인 자리다(F316).
           * 🔑 그래서 목록을 고치지 않고 **정본에서 파생한다** — `handoff-store` 의 `공용장부인가`
           *   하나가 track-collision·작업본소유자와 같은 판정을 이미 지고 있다(그 파일 머리말
           *   「판정은 여기 하나다」). 여기서 다시 적으면 세 번째 벌이 된다. */
          const 공유판뿐 = 위험.every((w) => store.공용장부인가(w.파일));
          try {
            fs.mkdirSync(p.dirname(기억), { recursive: true });
            fs.writeFileSync(기억, JSON.stringify({
              /* 🔴 F266 — `at` 은 장식이 아니라 **생존 조건**이다. `sweep()` 은 `at` 없는 파일을
               *   나이와 무관하게 지우고(handoff-store 309행), 그 sweep 은 세션이 하나 열리거나
               *   닫힐 때마다 돈다. 이 한 칸이 없어서 이 기억은 실사용에서 **한 번도 살아남지 못했고**,
               *   그 결과 아래 처방(「같은 명령을 그대로 다시 실행한다」)이 영영 안 열려
               *   남는 문이 BYPASS 하나뿐이었다 — F103 이 말한 「우회가 정상 통로가 되는」 자리다. */
              at: Date.now(),
              seen: [...새것.map((c) => c.해시), ...상태.seen].slice(0, 60),
              /* 회차는 **범위별**로 센다 — 다른 명령의 회차를 물려받아 조용히 통과하면 그게 구멍이다.
               * ⚠ 옛 항목을 먼저 지우고 새 값을 넣는다. `fromEntries` 는 **뒤에 온 것이 이겨서**,
               *   그냥 이어 붙이면 옛 회차가 새 회차를 덮어써 카운터가 영원히 1 이 된다(회귀가 잡았다). */
              회차: Object.fromEntries([[범위키, 회차],
                ...Object.entries(상태.회차).filter(([k]) => k !== 범위키)].slice(0, 20)),
            }));
          } catch { /* 기억을 못 써도 판정은 한다 — 다음에 한 번 더 펼쳐질 뿐이다(안전한 방향) */ }

          const 머리 = `[git-scope-guard] 커밋 범위 안에 **내 것이라고 증명되지 않는** 변경이 ${위험.length}건 있다.`
            + `\n\n${위험.map((w) => `  · ${w.파일}  ← ${w.왜}`).join('\n')}`
            + (스테이징만 ? '\n  (범위를 안 줬다 — 지금 **스테이징된 것**을 기준으로 쟀다)' : '');
          const 안본것 = 새것.map((c) => c.글).join('\n');
          const 그대로 = 조각.length - 새것.length;

          if (공유판뿐 || 회차 >= 3) {
            out('allow', `${머리}`
              + (공유판뿐
                ? '\n\n⚠ **이건 여러 세션이 상시 덧쓰는 선언판이다 — 남의 줄이 함께 실리는 것이 정상이다**(F229).'
                  + '\n   ⑧이 지키는 것은 「내 커밋에 남의 **미완성 편집**이 실려 나간다」인데 여기선 그 전제가 거짓이다:'
                  + '\n   섞이는 남의 줄은 그 세션이 **자기 트랙에 쓴 선언·종결**이고, 함께 커밋되는 것이 이 파일의 관행이다.'
                  + '\n   오히려 **안 실리는 쪽이 손해다** — 미커밋으로 남은 종결 줄은 계정을 갈아타면 통째로 사라진다(인계 3통로 중 하나).'
                  + '\n   그래서 차단하지 않는다. 대신 실려 갈 내용을 그대로 싣는다:'
                : `\n\n⚠ **같은 범위로 ${회차}번째다 — 매번 내용이 바뀌어 재차단이 끝나지 않는다**(공유 파일을 옆 세션이 덧쓰는 중이다).`
                  + '\n   무한 차단은 BYPASS 를 정상 통로로 만들 뿐이라 여기서 통과시킨다 — 대신 **그 사이 바뀐 것**을 싣는다:')
              + `\n\n${안본것}`
              + (공유판뿐
                ? '\n\n→ 볼 것은 하나다: **남의 줄이 `-` 로 사라지지 않았나**(더해진 남의 줄은 실려도 된다).'
                  + '\n   사라졌으면 그 줄을 작업본에 되살린 뒤 커밋한다(⑨가 산 세션의 최근 커밋만 보므로 그 밖은 여기서 눈으로 본다).'
                  + '\n   이 파일은 **혼자 커밋**한다 — 코드 경로와 묶으면 그쪽은 이 완화를 못 받고 통째로 차단된다.'
                : '\n\n→ 지금 읽어라. 남의 줄이 섞였으면 **커밋을 취소하지 말고**(이미 실렸다) 그 세션에 알리고, 다음엔 범위에서 뺀다.'
                  + '\n   장부·보드처럼 상시 덧쓰이는 파일은 **혼자 커밋**한다(다른 경로와 묶지 않는다 — 묶을수록 이 창이 길어진다).'));
          }
          deny(`${머리}`
            + '\n경로를 못 박아도 커밋되는 건 그 경로의 **작업본 현재 상태**다 — 같은 파일에 남이 편집해 둔 줄은 함께 실린다.'
            + '\n실측 3건이 전부 이 형태다(d64ad85 남의 서체 부활 · b489f2e·6711ff2 남의 보드 줄) — F073 까지 4번째.'
            + (회차 > 1
              ? `\n\n**${회차}번째다 — 그 사이 바뀐 것만 싣는다**(나머지 ${그대로}건은 아까 펼친 그대로다):\n${안본것}`
              : `\n\n지금 이 명령이 실어 갈 내용:\n${안본것}`)
            + '\n\n→ 위가 **전부 내가 만든 것이면** 같은 명령을 그대로 다시 실행한다(이제 통과한다).'
            /* F383 — 미추적 신규 파일은 「같은 명령 재실행」이 그대로 안 통한다: 차단은 실행 전에
             * 걸려 한 호출에 묶인 앞선 `git add` 도 함께 무효가 되므로, add 없이 커밋만 다시 치면
             * git 이 pathspec 오류로 죽는다(실측 08-12 한 세션 2회). 처방이 그 갈래를 같이 말한다. */
            + (미추적.length
              ? `\n   ⚠ 위엔 **미추적 신규 파일**이 있다(${미추적.join(' · ')}) — 재실행 명령엔 \`git add -- <경로>\` 를 **같은 호출 안에** 앞세운다.`
                + '\n     (차단은 실행 전이라 앞선 add 도 같이 무효다 — add 없이 커밋만 다시 치면 pathspec 오류로 죽는다 · F383)'
              : '')
            + '\n   남의 것이 섞였으면 그 파일을 범위에서 빼고 커밋한 뒤 그 세션에 알린다(고쳐서 대신 커밋하지 않는다).'
            + '\n   ⚠ 이 창은 「봤다」가 아니라 「보여줬다」만 기록한다 — 위 diff 를 실제로 읽는 것은 사람·모델 몫이다.'
            + '\n   누구 것인지: node tools/작업본소유자.js');
        }
      }
    }
  }
}

process.exit(0);
