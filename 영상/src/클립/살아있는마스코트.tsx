/**
 * 살아 있는 마스코트 — **4D 깊이층**을 릴에 옮긴 판 (유호 지시 08-26 「어제 만든 4d 버전 적용해서
 * 살아움직이는것처럼 만들어줄수있어? 막 옆도 보고 이런 느낌 있잖아」).
 *
 * 🔑 어디서 왔나: `docs/캐릭터/생명공방_0826/살아움직이는<캐릭터>.html` 의 4D 층.
 *   그 지면이 하는 일은 한 줄로 이렇다 — **그림 한 장에서 픽셀마다 깊이(z)를 재고,
 *   시점이 움직이면 z 만큼 반대로 민다.** 눈은 z 가 커서(+0.30) «먼저» 움직이고,
 *   그 어긋남이 사람 눈에 «고개를 돌렸다»로 읽힌다. 사진 한 장이 입체가 되는 자리다.
 *   ⚠ 그 층은 08-26 까지 **한 번도 돈 적이 없었다**(`v` 정의 한 줄이 없어 통째로 죽어 있었고,
 *     구조 검사 열 개는 «있나»만 봐서 못 잡았다). 실렌더가 잡아 고친 뒤의 판을 옮긴 것이다.
 *
 * 🔑 깊이는 `영상/깊이뽑기.js` 가 «미리» 뽑아 둔다(`public/깊이/<가이드>.json` · 29×29).
 *   Remotion 은 프레임마다 다시 그리므로, 여기서 256×256 픽셀을 매번 읽으면 900번 읽게 된다.
 *
 * 🔴 캔버스로 그린다 — `<Img>` 로는 못 한다. 깊이만큼 «부분마다 다르게» 밀어야 하는데
 *   CSS transform 은 그림 «통째»로만 움직인다. 그래서 격자 칸마다 따로 그린다.
 */
import React, { useEffect, useMemo, useRef, useState } from "react";
import {
  continueRender,
  delayRender,
  interpolate,
  spring,
  staticFile,
  useCurrentFrame,
  useVideoConfig,
} from "remotion";
import type { 가이드, 표정 } from "./타입";
import { 연출, 그늘, type 경계 } from "./연출";
/* 하이라이트 — 킷 다리의 Paper 틴트(아래 「빛」 주석). 이 파일의 지역 변수 `빛`(세기)과 이름이 겹쳐 별칭으로 든다 */
import { 빛 as 빛색 } from "../킷/색";
import { 몸짓표 } from "./몸짓";

/* ── «늘 도는» 움직임 — 전부 이 표 하나에 있다 ──────────────────────────────
 * 🔑 왜 한 표인가: 멀미는 «하나가 크다»가 아니라 **여럿이 동시에 돈다**에서 온다.
 *   숨(5.4초) · 부유(9.2초) · 가로시점(11초) · 세로시점(14초)이 서로 안 맞아떨어져
 *   캐릭터가 30초 내내 **같은 자리에 두 번 서지 않는다**. 값이 흩어져 있으면 그 총합을
 *   아무도 못 잰다 — 그래서 부유·숨까지 여기로 끌어올렸다(전엔 본문에 박힌 맨숫자였다).
 *   ⇒ 세기를 다시 만질 자리는 **이 표 하나**다.
 *
 * 🔴 08-26 유호님 「약간 멀미나는 느낌을 줄여줬으면」 ⇒ 세기를 내렸다.
 *   깎은 폭: 가로진폭 −36% · 세로진폭 −59% · 기울 −54% · 부유 −43% · 숨 −29%.
 *   **세로와 기울임(회전)이 멀미의 축**이라 거기를 더 깎았고, 가로 시차는
 *   «옆을 본다»로 읽히는 바로 그 축이라 가장 덜 깎았다(0.42 → 0.27).
 *
 * 🔑 **구운 영상에서 잰 값**(clip-01-1 · 540폭에서 재고 되돌림 · 14초 = 두 주기):
 *     머리 위 18% 가로 ±4.1px · 발 아래 20% 가로 ±2.1px · 무게중심 세로 ±15.9px
 *   ⚠ 나는 여기 처음에 «가로 ±13.7px» 이라고 **계산해서** 적었다가 실측에 반려됐다(3배 과대).
 *     까닭이 배울 만하다 — 가로는 z 에 곱해지는데 내가 z «최댓값»(1.30 = 눈의 봉우리)을 썼다.
 *     머리 «전체»의 평균 z 는 그보다 훨씬 낮다. 세로는 부유·숨이 z 와 무관해 계산이 맞았다
 *     (예고 ±15.7 · 실측 ±15.9). ⇒ **z 가 곱해지는 축은 계산하지 말고 구워서 잰다.**
 * 🔑 주기(11초·14초)는 안 건드렸다 — 원본 지면 `살아움직이는<캐릭터>.html` 과 같은 리듬이다.
 *   주기를 건드리면 지면과 릴의 «성격»이 갈린다. 세기만 릴에 맞춘다. */
const 입체 = {
  /** 픽셀의 z 만큼 반대로 미는 양 — 이게 «4D»다. 여기를 깎으면 입체가 사라지므로 안 깎았다. */
  시차: 0.075,
  가로진폭: 0.27,
  세로진폭: 0.09,
  가로주기: 11,
  세로주기: 14,
  빛세기: 0.5,
  /** 시점을 따라 몸이 «따라 기운다» — 고개만 돌고 몸이 안 움직이면 인형이 된다.
   *  기울임 = 회전이고, 회전은 흔들림 중에 멀미를 가장 잘 만든다. 그래서 반으로 깎았다. */
  기울: 0.013,
  /** 숨 — 크기가 아주 옅게 부풀었다 꺼진다. */
  숨폭: 0.01,
  숨주기: 26,
  /** 부유 — 위아래로 뜬다(px). 세로 흔들림 셋 중 가장 컸다. */
  부유: 8,
  부유주기: 44,
} as const;

const 컷들 = ["본체", "눈감음", "눈웃음"] as const;

/**
 * 두 컷이 «실제로» 다른 곳의 상자를 잰다(원본 픽셀 좌표). 한 번만 돈다.
 *
 * 🔴 왜 필요한가 — 08-26 실측이 시켰다. 깜빡임을 «메시 두 벌 겹쳐 그리기»로 녹였더니
 *   눈 아닌 띠까지 4배씩 뛰었다. 까닭이 원리적이다: 캔버스의 source-over 는
 *   **반투명 픽셀을 겹치면 알파가 더해진다** — 알파 0.5 인 털 끝에 같은 그림을 세기 a 로 덮으면
 *   0.5·a + 0.5·(1−0.5a) = 0.5 + 0.25a 가 된다. 깜빡일 때마다 **털 끝이 짙어지는** 것이다.
 *   그림을 아무리 맞춰 놔도(=`영상/컷맞추기.js`) 이건 그리는 쪽에서 나므로 안 없어진다.
 *
 * 🔑 그래서 겹쳐 그리는 범위를 **두 컷이 다른 칸**으로 좁힌다. 그 칸은 얼굴 «안»이라
 *   알파가 1 이고, 알파 1 인 곳에서는 겹쳐 그리기가 정확한 섞기가 된다(src·a + dst·(1−a)).
 *   상자를 파일로 안 받고 여기서 재는 까닭: 그림이 바뀌면 상자도 따라 바뀌어야 하는데,
 *   파일로 받으면 «둘 중 어느 게 현행인가»가 갈린다(이 저장소가 가장 자주 앓는 병이다).
 */
function 눈자리재기(a?: HTMLImageElement, b?: HTMLImageElement) {
  if (!a || !b || a.width !== b.width || a.height !== b.height) return null;
  const w = a.width, h = a.height;
  const cv = document.createElement("canvas");
  cv.width = w;
  cv.height = h;
  const ctx = cv.getContext("2d", { willReadFrequently: true });
  if (!ctx) return null;
  ctx.drawImage(a, 0, 0);
  const A = ctx.getImageData(0, 0, w, h).data;
  ctx.clearRect(0, 0, w, h);
  ctx.drawImage(b, 0, 0);
  const B = ctx.getImageData(0, 0, w, h).data;
  let x0 = w, y0 = h, x1 = -1, y1 = -1;
  for (let y = 0; y < h; y++) {
    for (let x = 0; x < w; x++) {
      const o = (y * w + x) * 4;
      if (A[o] !== B[o] || A[o + 1] !== B[o + 1] || A[o + 2] !== B[o + 2] || A[o + 3] !== B[o + 3]) {
        if (x < x0) x0 = x;
        if (x > x1) x1 = x;
        if (y < y0) y0 = y;
        if (y > y1) y1 = y;
      }
    }
  }
  return x1 < 0 ? null : { x0, y0, x1, y1 };
}

export const 살아있는마스코트: React.FC<{
  가이드: 가이드;
  경계표: 경계[];
  폭: number;
  말할때?: number[];
}> = ({ 가이드, 경계표, 폭, 말할때 }) => {
  const frame = useCurrentFrame();
  const { fps } = useVideoConfig();
  const 캔버스 = useRef<HTMLCanvasElement>(null);
  const [짐, 짐놓기] = useState<{
    그림: Record<string, HTMLImageElement>;
    z: number[];
    N: number;
    /** 두 컷이 «실제로» 다른 곳(원본 픽셀 좌표). 깜빡임을 여기 칸에만 겹쳐 그린다 — 아래 주석. */
    눈자리: { x0: number; y0: number; x1: number; y1: number } | null;
  } | null>(null);
  const [핸들] = useState(() => delayRender(`4D 마스코트 — ${가이드}`));

  /* 그림 셋 + 깊이 격자를 «한 번만» 싣는다. 다 실릴 때까지 렌더를 붙잡는다
     (안 붙잡으면 Remotion 이 빈 캔버스를 그대로 찍는다 — 조용히 새는 자리다). */
  useEffect(() => {
    let 살아있음 = true;
    const 그림불러오기 = (이름: string) =>
      new Promise<[string, HTMLImageElement]>((성공, 실패) => {
        const im = new Image();
        im.onload = () => 성공([이름, im]);
        im.onerror = () => 실패(new Error(`${가이드}/${이름}.png 를 못 실었다`));
        im.src = staticFile(`${가이드}/${이름}.png`);
      });

    Promise.all([
      Promise.all(컷들.map(그림불러오기)),
      fetch(staticFile(`깊이/${가이드}.json`)).then((r) => {
        if (!r.ok) throw new Error(`깊이/${가이드}.json 을 못 실었다 — 먼저 node 영상/깊이뽑기.js`);
        return r.json();
      }),
    ])
      .then(([그림쌍, 깊이]) => {
        if (!살아있음) return;
        const 그림 = Object.fromEntries(그림쌍);
        짐놓기({ 그림, z: 깊이.z, N: 깊이.N, 눈자리: 눈자리재기(그림["본체"], 그림["눈감음"]) });
        continueRender(핸들);
      })
      .catch((e) => {
        /* 조용히 넘어가지 않는다 — 4D 가 안 실렸는데 그림만 나오면 아무도 못 본다 */
        throw e;
      });
    return () => {
      살아있음 = false;
    };
  }, [가이드, 핸들]);

  /* ── 이 프레임의 «몸 상태» — 원본 지면의 격자() 와 같은 축들 ─────────────── */
  const 상태 = useMemo(() => {
    const 현 = 경계표.find((b) => frame >= b.시작 && frame < b.끝);
    const 표정: 표정 = 현?.표정 ?? "본체";

    const 등장 = spring({ frame: frame - (현?.시작 ?? 0), fps, config: 연출.스프링탄 });
    const 튐 = interpolate(등장, [0, 1], [0.92, 1]);
    const 숨 = 1 + Math.sin(frame / 입체.숨주기) * 입체.숨폭;
    const 부유 = Math.sin(frame / 입체.부유주기) * 입체.부유;

    const 최근말 = (말할때 || []).filter((f) => frame >= f && frame < f + 14).pop();
    const 말튐 = 최근말 === undefined ? 0 : Math.sin(((frame - 최근말) / 14) * Math.PI) * 0.055;

    /* ── 몸짓 — 이 장면의 «말»이 정한다(`몸짓.ts`) ──────────────────────────
       🔑 장면이 바뀌면 몸짓도 바뀌므로, 세기를 **장면 안에서 봉투**로 감싼다:
          들어와서 세지고, 중간에 유지되고, 나가면서 잦아든다. 안 감싸면 컷에서 뚝 끊겨
          「몸이 순간이동」한다(마스코트가 Sequence 밖에 사는 이 층에서 특히 티가 난다). */
    const 결 = 몸짓표[현?.몸짓 ?? "기본"];
    const 안 = frame - (현?.시작 ?? 0);
    const 길이 = (현?.끝 ?? 0) - (현?.시작 ?? 0) || 1;
    const 오름 = Math.min(1, 안 / (fps * 0.45));
    const 내림 = Math.min(1, ((현?.끝 ?? 0) - frame) / (fps * 0.4));
    const 세기 = Math.max(0, Math.min(오름, 내림));

    /* 바르르 — 좌우 고주파. 「매운 것을 먹은 몸」은 빠르고 폭이 작다(느리면 춤이 된다).
       🔴 전엔 `frame * 1.35` 였다 = 한 주기 4.65프레임 = 6.4Hz. 30fps 로는 한 주기를
         **4.7번밖에 못 찍는다** — 떨림이 아니라 «계단»으로 나고, 그 계단이 눈을 찌른다.
         0.9 로 낮추면 한 주기 7.0프레임 = 4.3Hz 라 30fps 가 제대로 담는다(08-26 유호 「멀미」). */
    const 떨림 = 결.떨림 * 세기 * Math.sin(frame * 0.9);
    /* 통통 — 세로 튐. 장면 안에서 두어 번만 튄다(계속 튀면 배경이 아니라 주연이 된다) */
    const 튐몸 = 결.튐 * 세기 * Math.max(0, Math.sin((안 / 길이) * Math.PI * 2.4));
    const 갸웃 = 결.갸웃 * 세기;
    const 늘임 = 결.늘임 * 세기 * Math.max(0, Math.sin((안 / 길이) * Math.PI * 1.6));
    /* 몸짓 크기 × 장면 크기배 — 둘 다 같은 봉투를 탄다.
       🔑 곱으로 겹친다(더하지 않는다): 몸짓은 «어떻게 움직이나», 크기배는 «얼마나 크게 있나»라
          축이 다르다. 더하면 표현 칸(쭉 1.035 + 배 1.12)이 두 축의 합으로 튀어 과해진다. */
    const 크기 = (1 + (결.크기 - 1) * 세기) * (1 + ((현?.크기배 ?? 1) - 1) * 세기);

    /* 깜빡임 (유호 지시 08-26 「눈도 조금만 더 깜빡이고」)
     *
     * 🔴 첫 판은 `sin(f/37) * sin(f/53) > 문턱` 이었는데, **문턱을 내려도 안 늘었다.**
     *   실측: 0.985 도 0.955 도 **30초에 1회**다. 까닭이 원리적이다 — 두 주기가
     *   232·333프레임(7.7초·11.1초)이라 둘이 «같이 꼭대기에 서는» 순간이 30초 안에 거의 없다.
     *   ⚠ 나는 이 자리에 「3회 → 11회」라고 주석을 먼저 적었다가 세어 보고 지웠다.
     *     세지 않은 수를 적으면 다음 사람이 그 수를 믿는다.
     *
     * 🔑 그래서 «주기»가 아니라 **일정**을 짠다: 3~5초 간격으로 자리를 미리 찍는다.
     *   간격을 해시로 흔들어 규칙적이지 않게 하되, 프레임만 알면 늘 같은 답이 나온다(재현 가능).
     * 🔑 한 번 감으면 4프레임 머문다 — 1프레임이면 30fps 에서 «안 보인다».
     * `frame > 8` 이 첫 프레임을 눈뜸으로 강제한다(릴 커버를 구하는 자리). */
    const 깜빡일정: number[] = [];
    for (let f = Math.round(fps * 1.2); f < 20000; ) {
      깜빡일정.push(f);
      const 흔들 = ((f * 2654435761) >>> 0) % 61; /* 0~60프레임 = 0~2초 */
      f += Math.round(fps * 3) + 흔들;
    }
    /* 🔴 08-26 유호님 「까몽이 눈 감을때 털이 변하는데 왜그런거야? 갑자기 털 위치가 좀 변해」
     *   — 원인은 코드가 아니라 **그림**에 있었다. 까몽 세 컷은 «따로 구운» Cycles 렌더다
     *   (PNG 메타 실측: 07:40 / 07:56 / 08:06 · 셋 다 samples 96 · 렌더 12:30 / 16:17 / 09:31).
     *   96샘플은 털처럼 잔 결에서 안 수렴해 **굽기마다 노이즈 무늬가 새로 난다.**
     *   재 봤다 — 머리 위 30%(눈은 뺐다)에서 본체↔눈감음 2.27% 어긋나는데
     *   눈감음↔눈웃음은 0.49%다(= 본체만 혼자 떨어져 있다). 그리고 부호 있는 평균차는
     *   −1.05/−1.51/−0.81 ≈ 0 인데 **절대값 평균차는 6.8** 이다 —
     *   「통째로 밝아졌다」가 아니라 「흩어졌다」, 곧 노이즈다.
     *   실루엣 자체는 0.00% 만 움직였으니 «위치»가 아니라 «결»이 바뀐 것이다.
     *
     * 🔑 그래서 컷을 **겹쳐 넘긴다.** 한 프레임에 탁 바꾸면 털 노이즈가 통째로 갈아끼워져
     *   「털이 변했다」로 읽힌다. 7프레임(233ms)에 걸쳐 녹이면 그 갈아끼움이
     *   «변화»가 아니라 «흐림»으로 읽힌다 — 사람 눈이 50~100ms 를 뭉쳐 보기 때문이다.
     *   ⚠ 이건 **덮는 것이지 고치는 것이 아니다.** 뿌리 고침 = 까몽을 더 높은 샘플로
     *     한 벌에 다시 굽기이고, 그건 굽기 레인 몫이라 `docs/_ops/트랙.md` 에 적었다.
     *
     * 결 값이 그대로 보이게 «표»로 둔다 — 오르내림 2프레임 · 다 감음 3프레임. */
    const 깜빡결 = [0.33, 0.67, 1, 1, 1, 0.67, 0.33];
    let 감김 = 0;
    if (표정 === "본체" && frame > 8) {
      for (const f of 깜빡일정) {
        if (frame >= f && frame < f + 깜빡결.length) {
          감김 = 깜빡결[frame - f];
          break;
        }
      }
    }
    /* 컷 — 몸짓이 「눈웃음」을 부르면 그것을 쓴다. 그림 셋 안에서만 고른다(없는 컷은 모듈이 던진다). */
    const 바탕컷 = 표정 === "눈웃음" ? "눈웃음" : 결.컷;

    /* 4D 시점 — 초 단위로 도는 두 사인. 주기가 11 과 14 라 서로 안 맞아떨어져
       같은 자리를 두 번 지나지 않는다(그게 «둘러본다»로 읽히는 까닭이다). */
    const t = frame / fps;
    const 시점x = Math.cos((t / 입체.가로주기) * Math.PI * 2) * 입체.가로진폭;
    const 시점y = Math.sin((t / 입체.세로주기) * Math.PI * 2) * 입체.세로진폭;

    return { 바탕컷, 감김, 튐, 숨, 부유, 말튐, 시점x, 시점y, 떨림, 튐몸, 갸웃, 늘임, 크기 };
  }, [frame, fps, 경계표, 말할때]);

  /* ── 그린다 — **삼각형 메시** ────────────────────────────────────────────
   * 🔴 첫 판은 «격자 칸마다 drawImage» 였는데 **찢어졌다**(실측: 캐릭터 위로 흰 줄이 그어졌다).
   *   까닭은 원리적이다 — 칸을 각자 밀면 이웃 사이에 **틈이 생긴다.** 실루엣 근처에서 z 가
   *   0 → 0.6 으로 뛰므로 이웃 칸의 이동량이 5px 씩 벌어지고, 그 5px 이 빈 채로 남는다.
   *   1px 씩 겹쳐 그려도 못 덮는다(겹침보다 틈이 크다).
   *
   * 🔑 삼각형 메시는 **꼭짓점을 공유**한다 — 이웃한 두 삼각형이 같은 점을 쓰므로 틈이
   *   «생길 수가 없다». 대신 삼각형마다 클립 + 아핀 변환이 필요하다(그래서 느리다).
   *   격자를 16 으로 낮춰 값을 치렀다: 512 삼각형/프레임. 깊이 격자(28)는 그대로 두고
   *   여기서만 성기게 읽는다 — 깊이는 정확하되 변형만 성기다.
   */
  useEffect(() => {
    const cv = 캔버스.current;
    if (!cv || !짐) return;
    const ctx = cv.getContext("2d");
    if (!ctx) return;
    const { 그림, z, N: zN, 눈자리 } = 짐;
    const 바탕 = 그림[상태.바탕컷];
    if (!바탕) return;

    /* 🔑 캔버스를 2배로 그려 CSS 로 반 줄인다 — 삼각형 «경계»의 반픽셀 겹침이 서브픽셀로 묻힌다.
       0.35px 부풀림만으로는 몸 위에 옅은 메시 선이 남았다(실측). 채우는 비용은 4배지만
       삼각형 수는 그대로라 실측 렌더 시간은 얼마 안 늘었다. */
    const 배율 = 2;
    const S = 폭 * 배율;
    const M = 16; /* 변형 격자 — 삼각형 512개 */
    ctx.clearRect(0, 0, S, S);

    /* 몸 전체 변형 — 등장튐 × 숨 × 옹알이튐 × **몸짓 크기** */
    const 배 = 상태.튐 * 상태.숨 * (1 + 상태.말튐) * 상태.크기;
    /* 세로만 늘이는 몸짓(쭉·움츠)은 가로와 갈라 잡는다 — 부피 보존의 흉내(젤리) */
    const 세로배 = 배 * (1 + 상태.늘임);
    const 가로배 = 배 * (1 - 상태.늘임 * 0.45);
    const 가운데 = S / 2;

    /** 깊이 격자에서 (u,v) 의 z 를 겹선형으로 읽는다. v=0 이 바닥이라 세로를 뒤집는다. */
    const z읽기 = (u: number, v: number) => {
      const fx = u * zN, fy = v * zN;
      const i0 = Math.min(zN - 1, Math.floor(fx)), j0 = Math.min(zN - 1, Math.floor(fy));
      const tx = fx - i0, ty = fy - j0;
      const g = (i: number, j: number) => z[j * (zN + 1) + i] ?? 0;
      return (
        g(i0, j0) * (1 - tx) * (1 - ty) +
        g(i0 + 1, j0) * tx * (1 - ty) +
        g(i0, j0 + 1) * (1 - tx) * ty +
        g(i0 + 1, j0 + 1) * tx * ty
      );
    };

    /* 꼭짓점 (M+1)² 를 먼저 다 구한다 — 삼각형들이 «같은 점»을 나눠 쓰게 하려는 것이다 */
    const 점 = new Float64Array((M + 1) * (M + 1) * 2);
    for (let j = 0; j <= M; j++) {
      for (let i = 0; i <= M; i++) {
        const u = i / M, vy = j / M;
        const v = 1 - vy; /* 0 = 바닥 · 1 = 머리 */
        const zv = z읽기(u, v);
        const h = Math.pow(v, 1.45); /* 위로 갈수록 크게 — 아래는 바닥에 붙어 있다 */
        const dx = 상태.시점x * zv * 입체.시차 * S * 0.5 + 상태.시점x * 입체.기울 * h * S;
        const dy = 상태.시점y * zv * 입체.시차 * S * 0.5;
        /* 갸웃 — 바닥을 축으로 몸을 기울인다. 위로 갈수록 크게(h) 돌아 고개가 기운 것으로 읽힌다 */
        let px = (u * S - 가운데) * 가로배 + 상태.떨림 * S * h;
        let py = (vy * S - 가운데) * 세로배;
        if (상태.갸웃) {
          const a2 = 상태.갸웃 * h;
          const s2 = Math.sin(a2), c2 = Math.cos(a2);
          const y0 = py + S / 2; /* 바닥을 회전축으로 */
          const nx = px * c2 - y0 * s2;
          const ny = px * s2 + y0 * c2;
          px = nx;
          py = ny - S / 2;
        }
        const bx = 가운데 + px;
        const by = 가운데 + py - 상태.튐몸 * S;
        const o = (j * (M + 1) + i) * 2;
        점[o] = bx + dx;
        점[o + 1] = by + dy;
      }
    }

    /** 원본 삼각형 → 화면 삼각형 아핀 변환 + 클립. 이음매가 원리상 안 생긴다. */
    const 삼각 = (
      im: HTMLImageElement,
      u0: number, v0: number, u1: number, v1: number, u2: number, v2: number,
      x0: number, y0: number, x1: number, y1: number, x2: number, y2: number,
    ) => {
      const d = (u1 - u0) * (v2 - v0) - (u2 - u0) * (v1 - v0);
      if (!d) return;
      const a = ((x1 - x0) * (v2 - v0) - (x2 - x0) * (v1 - v0)) / d;
      const b = ((y1 - y0) * (v2 - v0) - (y2 - y0) * (v1 - v0)) / d;
      const c = ((x2 - x0) * (u1 - u0) - (x1 - x0) * (u2 - u0)) / d;
      const e = ((y2 - y0) * (u1 - u0) - (y1 - y0) * (u2 - u0)) / d;
      ctx.save();
      ctx.beginPath();
      /* 무게중심에서 0.35px 씩 부풀린다 — 삼각형 «사이»의 반픽셀 실선(안티에일리어싱 틈)을 덮는다.
         꼭짓점을 공유하므로 틈 자체는 없고, 남는 것은 가장자리 부드러움뿐이다. */
      const cx = (x0 + x1 + x2) / 3, cy = (y0 + y1 + y2) / 3;
      const 불 = (x: number, y: number): [number, number] => {
        const l = Math.hypot(x - cx, y - cy) || 1;
        return [x + ((x - cx) / l) * 0.35, y + ((y - cy) / l) * 0.35];
      };
      const p0 = 불(x0, y0), p1 = 불(x1, y1), p2 = 불(x2, y2);
      ctx.moveTo(p0[0], p0[1]);
      ctx.lineTo(p1[0], p1[1]);
      ctx.lineTo(p2[0], p2[1]);
      ctx.closePath();
      ctx.clip();
      ctx.transform(a, b, c, e, x0 - a * u0 - c * v0, y0 - b * u0 - e * v0);
      ctx.drawImage(im, 0, 0);
      ctx.restore();
    };

    /** 메시 한 벌을 그린다. 꼭짓점(점)은 컷과 무관하므로 «한 번만» 구하고 여기서 나눠 쓴다.
     *  `상자` 를 주면 그 원본 영역에 걸친 칸만 그린다(깜빡임 겹쳐 그리기가 쓴다). */
    const 메시 = (
      im: HTMLImageElement,
      알파: number,
      상자?: { x0: number; y0: number; x1: number; y1: number } | null,
    ) => {
      const su = im.width / M, sv = im.height / M;
      ctx.save();
      ctx.globalAlpha = 알파;
      for (let j = 0; j < M; j++) {
        for (let i = 0; i < M; i++) {
          if (
            상자 &&
            ((i + 1) * su < 상자.x0 || i * su > 상자.x1 || (j + 1) * sv < 상자.y0 || j * sv > 상자.y1)
          ) continue;
          const o00 = (j * (M + 1) + i) * 2;
          const o10 = (j * (M + 1) + i + 1) * 2;
          const o01 = ((j + 1) * (M + 1) + i) * 2;
          const o11 = ((j + 1) * (M + 1) + i + 1) * 2;
          const u0 = i * su, v0 = j * sv, u1 = (i + 1) * su, v1 = (j + 1) * sv;
          삼각(im, u0, v0, u1, v0, u0, v1, 점[o00], 점[o00 + 1], 점[o10], 점[o10 + 1], 점[o01], 점[o01 + 1]);
          삼각(im, u1, v0, u1, v1, u0, v1, 점[o10], 점[o10 + 1], 점[o11], 점[o11 + 1], 점[o01], 점[o01 + 1]);
        }
      }
      ctx.restore();
    };

    메시(바탕, 1);
    /* 눈감음을 «위에 옅게» 겹친다 — 깜빡임이 컷 바꿈이 아니라 녹아드는 것이 된다.
       🔑 **눈자리에 걸친 칸만** 겹친다(`눈자리재기` 주석). 몸 전체를 겹치면 반투명한 털 끝의
          알파가 더해져 깜빡일 때마다 털이 짙어진다 — 실측으로 잡은 자리다.
       ⚠ 상자를 못 쟀으면(눈자리 null) 겹치지 않고 «탁» 바꾼다. 조용히 몸 전체를 겹치면
          그 순간 위의 그 병이 돌아오므로, 차라리 안 녹인다. */
    if (상태.감김 > 0) {
      const 감은그림 = 그림["눈감음"];
      if (감은그림) {
        if (눈자리) 메시(감은그림, 상태.감김, 눈자리);
        else if (상태.감김 >= 0.5) 메시(감은그림, 1);
      }
    }

    /* 빛 — 시점이 움직일 때만 변한다. 정지(시점 0)면 사진 그대로.
       사진에 이미 구워진 조명을 덮어쓰지 않으려는 절제다(원본 지면의 그 규율).
       🔑 «몸 전체»에 아주 옅은 기울기 한 겹으로 준다 — 칸마다 칠하면 그 칸 경계가 다시 보인다. */
    const 빛 = 상태.시점x * 입체.빛세기 * 0.10;
    if (Math.abs(빛) > 0.003) {
      ctx.save();
      ctx.globalCompositeOperation = "source-atop";
      const g = ctx.createLinearGradient(0, 0, S, 0);
      /* 🔑 빛도 그늘도 킷 다리를 지난다(09-02). 빛은 옛 `rgba(255,252,246,…)` 이 킷에 없어 다리를 안 만들었던
         자리인데, 발행검사 ③ 이 그것을 «킷 밖 색»으로 잡았다. 이제 킷의 가장 밝은 값(Paper · 철칙① 라이트 하한)
         틴트로 잇는다 — 알파 상한 0.0135 라 픽셀 차이는 8비트 한 단 아래다(근거·되돌리기 = `킷/색.ts` 「빛과 그늘」). */
      g.addColorStop(0, 빛색(Number(Math.max(0, 빛).toFixed(3))));
      g.addColorStop(0.5, 빛색(0));
      g.addColorStop(1, 그늘(Number(Math.max(0, 빛).toFixed(3))));
      ctx.fillStyle = g;
      ctx.fillRect(0, 0, S, S);
      ctx.restore();
    }
  }, [짐, 상태, 폭]);

  return (
    <canvas
      ref={캔버스}
      width={폭 * 2}
      height={폭 * 2}
      style={{
        width: 폭,
        height: 폭,
        transform: `translateY(${상태.부유 - 상태.말튐 * 80}px)`,
        filter: `drop-shadow(0 18px 26px ${그늘(0.16)})`,
      }}
    />
  );
};
