# 복원 과정·결말 HTTP 독립 검수

2026-09-10 · 별도 작업 사본 `codex/pulse-audio-20260910`. 제작 담당의 새 복원 계약을 읽고 실제 HTTP 요청과 응답으로 확인했습니다. `server.test.cjs`와 이 기록만 이번 검수 변경입니다. 서버·이야기·대화 구현은 수정하지 않았습니다.

## 결과

**HTTP 시험 10개와 기존 이야기 단위 시험 6개, 총 16개 통과.** 확인한 복원·투표·결말 문장 경계에 미해결 지적은 없습니다.

```text
node --test experiences/20260909/server.test.cjs experiences/20260909/server/story.test.cjs
```

각 시험은 임시 번호의 로컬 서버를 만들고 종료합니다. 실제 4399 서버를 종료하거나 원격으로 배포하지 않았습니다. AI 모델을 호출하지 않았습니다.

| 사용자 행동·경계 | 확인한 결과 |
|---|---|
| 단서 4개를 공유했다 | 아직 탐색 단계이며 투표 요청은 409. 공유 버튼 수집이 완료 조건을 대신하지 않는다. |
| 전차 연결부 `[3,1,2]`와 방송 96.4MHz를 복원했다 | 두 복원을 끝내면 투표 단계. 공유 버튼을 한 번도 누르지 않고도 진행할 수 있다. |
| 장소를 살펴보지 않고 조작했다 | 시작 전과 탐색 중 모두 409. 복원 기록은 바뀌지 않는다. |
| 허용 범위를 벗어난 연결부·회전, 잘못된 형식·범위·단위의 주파수를 보냈다 | 400이며 복원 기록과 변경 번호가 그대로다. |
| 값 형식은 맞지만 회로나 수신 위치가 틀렸다 | 탐색을 유지하고 피드백을 제공한다. 수신 전에는 복원 방송이 없다. 한쪽만 복원해도 투표가 열리지 않는다. |
| 기록원이 전차를, 정비사가 방송을 조작했다 | 각각 직접 살펴본 뒤 조작할 수 있고 실제 완료한 역할과 순서가 기록된다. |
| 두 명·세 명·네 명이 선택했다 | 참여자 전원이 같은 곳을 골라야 방장이 결말을 확정할 수 있다. 빠진 표·다른 선택은 409, 다른 참여자의 확정은 403. |
| 결말 뒤 다시 복원·투표를 시도했다 | 이미 끝난 단계의 조작을 거부한다. 복원 기록이 바뀌지 않는다. |
| 결말 전 한 문장을 남겼다 | 대기·탐색·투표 단계에서 모두 409. |
| 결말 뒤 한 문장을 남기고 수정했다 | 빈 값·120자 초과는 400. 역할별 한 문장을 보존하며 같은 역할의 재작성은 중복 추가 대신 수정한다. 동료의 상태 조회에도 같은 문장이 나타난다. |
| 다른 방을 새로 만들거나 같은 방을 초기화했다 | 새 방은 이전 방의 기록을 가져오지 않고 이전 방도 그대로다. 방장 초기화는 복원·선택·문장을 비우고 새 대기로 돌아간다. |
| 기존 인증·복구·연결 경계 | 방 분리·역할 중복·잘못된 자격·정적 비공개 파일 차단·SSE 역할별 전달·UTF-8 한글 조각·요청 본문 수신 중 역할 제거도 계속 통과. |

## 사용한 원본

기준 Git 커밋은 `a1d64fb04f8766dbef1cd3607c99577062f30f18`입니다. 아래 구현은 이후 root 작업본에서 복사한 미커밋 원문이므로 시험 당시 SHA-256을 남깁니다.

| 경로 | SHA-256 |
|---|---|
| `experiences/20260909/server.cjs` | `6235707e2bc634d6ac2934b7d7039690431ed3d036cef9a9b5a72eda6b3e63f0` |
| `experiences/20260909/server/story.cjs` | `d9f0e2732467e12455edd8fe2f606b794d9a1795a18a01d4791b26f29bc1ae85` |
| `experiences/20260909/server/story.test.cjs` | `027fcdd8d5d9d806337841060dc5069e3e59e89016ee5e1a2a9640e7dad38f2c` |
| `experiences/20260909/server/dialogue.cjs` | `3603adc6642fc1f54ff7c1478747c8f757fa8e1ab23c85dc80dabe041650a0e5` |

## 확인 범위

이 기록은 HTTP 서버의 행동과 데이터 경계를 검증합니다. 실제 화면에서 회로를 읽고 주파수를 맞추는 난이도·키보드와 휴대전화의 완주·소리의 청취 품질·AI 응답 품질은 별도 확인 대상입니다. 서버 응답에 문장이 존재한다는 확인을 실제 화면 표시 검증으로 확대하지 않습니다.

대화 모듈을 읽다가 공백만 있는 스트림 조각을 버리는 문제를 발견해 root에게 따로 전달했습니다. 그 모듈은 이번 소유·시험 범위 밖이므로 수정하지 않았으며 AI 대화 검증 완료를 주장하지 않습니다.
