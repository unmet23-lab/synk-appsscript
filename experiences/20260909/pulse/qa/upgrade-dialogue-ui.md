# PULSE 대화 UI 독립 검수 — 2026-09-10

검수자: `pulse_function_review`. 기능 생성자와 분리한 실제 Chrome 조작 검수다. 제품 파일은 변경하지 않았다. 최초 검수 기준은 root `b031ed4ef460a09591ee61335e6415585506057e`와 미커밋된 `ai-dialogue.js`·`ai-dialogue.css`·통합 `app.js`·`index.html`·`style.css`다.

## 수행 환경과 경계

- root `experiences/20260909/server.cjs`의 factory로 `127.0.0.1` 임시 포트에 별도 서버를 만들었다. 기존 4399 서버·사용자 브라우저·사용자 저장 상태는 조작하지 않았다.
- 설치된 Playwright와 Chrome의 독립 컨텍스트를 사용했다. 화면은 1440×960, 390×844, 320×844다.
- 대화 제공자는 명시적으로 넣은 **UI 검수용 스텁**이다. 미준비 응답과 `[UI 검수 모의 응답]`으로 시작하는 지연 스트림을 사용했다. 실제 AI 모델 호출·응답 품질·한국어 음성 품질을 확인한 결과가 아니다.
- 게임 방 만들기, 두 역할 전환, 기기 복원, 투표, 엔딩, 리셋은 실제 앱 UI와 서버를 사용했다. UI 재생성 이벤트를 직접 꾸며 기능 통과로 세지 않았다.
- 검사 코드와 스크린샷은 OS 임시 폴더 `C:/Users/q1212/AppData/Local/Temp/pulse-dialogue-review-20260910`에만 두었다. 인증 토큰·사용자 데이터는 출력하거나 증거 파일에 저장하지 않았다.

## 1차 결과

기능 확인 15개가 통과했다. 아래 2종 결함을 발견하여 주담당에게 전달했고, 주담당 수정 후 별도 재검수 9개도 통과했다. **이 검수에서 발견한 미해결 결함은 없다.** 동일 결함의 화면별 반복은 중복 집계하지 않았다.

1. **수정 확인 · P1 — 역할을 바꿔도 미전송 질문이 남음.** 최초 `pulse/ai-dialogue.js:102`의 `pulse:session` 처리에서 대화 로그만 비우고 입력값은 유지했다. 전차 정비사로 다온 창을 열어 `정비사만의 미전송 질문`을 입력 → 닫기 → 방송 기록원으로 전환 → 다온 창 열기 시 정비사의 미전송 문장이 그대로 나타났다. 1440·390·320px 모두 재현했고, 수정본의 같은 절차에서는 입력값이 빈 문자열임을 확인했다.
2. **수정 확인 · P2 — 모바일 대화 버튼이 현재 역할 표시를 가림.** 최초 `pulse/ai-dialogue.css:23`의 모바일 `top:80px`와 `pulse/style.css:28`의 역할 표시 `top:92px`가 충돌했다. 390·320px에서 버튼 영역 `(16,80,104.2×40)`이 역할 영역 `(20,92,82.3×42.5)`을 덮었다. 수정본은 다온 버튼을 우측으로 옮겨 실제 화면에서 두 요소가 분리됨을 확인했다.

## 통과한 동작

- `pulse/index.html:14`의 CSS 링크와 `pulse/index.html:138`의 모듈 스크립트가 존재한다. `#npc-dialogue`가 실제로 생성되고 대화 UI가 열린다.
- 미준비 상태에서 원인과 장소 힌트를 안내하며 전송은 비활성화된다. AI 없이 전차 조작을 계속할 수 있다.
- 열 때 입력창에 포커스가 가고, Escape로 닫으면 다온 버튼에 포커스가 돌아간다. 닫기 버튼으로 닫는 동작도 실제 역할 전환 과정에서 사용했다.
- 3개 화면에서 완료된 대화 로그는 역할별로 분리되며 이전 역할로 돌아가면 자기 기록만 복원된다.
- 3개 화면에서 모의 답변이 진행 중일 때 역할을 바꾸면 지연된 문장이 다른 역할 로그에 들어오지 않는다.
- 3개 화면에서 `이 밤 잠시 접어 두기`는 대화를 숨기고, 돌아오면 같은 역할의 대화를 복원한다.
- 실제 전원 연결과 방송 수신 → 두 역할 동의 → 방장 확정 → 엔딩 → `같은 동료와 다른 밤을 시작`을 실행했다. 로비에서 대화가 숨겨지고 다시 시작하면 로그와 입력이 비워진다.
- 두 독립 Chrome 참여자 중 한 사람이 라디오를 조작하는 동안, 다른 사람이 열어 둔 AI 입력창의 초안과 포커스는 보존된다.
- 모든 화면에서 가로 넘침이 없고, 열린 대화 UI의 axe WCAG 2 A/AA·2.1 AA 자동 검사 위반은 0건이었다.

## 화면 판단과 미확인 범위

데스크톱은 대화가 왼쪽, 게임 조작이 오른쪽에 남는다. 모바일에서는 열린 대화가 게임 패널 일부를 덮지만 닫기 버튼과 세로 스크롤을 사용할 수 있다. 수정 후에는 현재 역할이 왼쪽 위, 대화 버튼이 오른쪽 위에 분리되어 모두 읽힌다.

실제 휴대전화의 가상 키보드, 스크린 리더 수동 청취, 실제 AI 대사의 일관성·응답 지연·음성 읽어주기 품질, 새 3D 장면 완성도는 이번 검수에서 확인하지 않았다. 자동 접근성 0건은 이 범위의 수동 검증 완료를 뜻하지 않는다.

## 증거

- 검사 코드: `C:/Users/q1212/AppData/Local/Temp/pulse-dialogue-review-20260910.cjs`
- 결과 JSON: `C:/Users/q1212/AppData/Local/Temp/pulse-dialogue-review-20260910/results.json`
- 화면: 같은 증거 폴더의 `closed-1440.png`, `open-1440.png`, `closed-390.png`, `open-390.png`, `closed-320.png`, `open-320.png`

## 수정 후 재검수

주담당이 세션 변경 시 입력 초안을 비우고 모바일 대화 버튼을 우측에 배치한 뒤, 새 독립 서버와 Chrome 컨텍스트에서 두 결함을 좁혀 재검수했다.

- 1440·390·320px 모두 역할 전환 시 이전 역할의 미전송 초안이 남지 않는다.
- 1440·390·320px 모두 대화 버튼과 역할 표시가 겹치지 않는다. 모바일 버튼 왼쪽 좌표는 390px 화면에서 269.8px, 320px 화면에서 199.8px다. 역할 표시는 오른쪽 끝 102.3px 이내에 남는다.
- 3개 화면에서 열린 패널이 화면 안에 들어오고 가로 넘침이 없다. 입력 포커스와 Escape 닫기 후 버튼 포커스 복귀도 보존된다.
- 재검수 9개 통과. 수정 뒤에 전체 AI 품질 검사를 했다는 의미는 아니다.
- 재검수 코드: `C:/Users/q1212/AppData/Local/Temp/pulse-dialogue-recheck-20260910.cjs`
- 재검수 증거: 위 증거 폴더의 `recheck-results.json`, `recheck-closed-{1440,390,320}.png`, `recheck-open-{1440,390,320}.png`
- 재검수에 사용한 파일 SHA-256: `ai-dialogue.js` = `afd09aac7382ffa3885179b053ad574c3d6f70bbec89228667e58235023ef371`, `ai-dialogue.css` = `3d2814bbbd20fe7e0d16420ed2abf814406cf838cd73ddf245124b69ff2e7a81`.
