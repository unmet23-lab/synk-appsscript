#!/usr/bin/env node
/* 배너굽기 — 유튜브 채널 배너를 로고 정본 하나에서 굽는다. (유호 지시 2026-08-28)
 *
 * 🔑 «배너»와 «채널아트»는 같은 것이다 — 유튜브가 옛 이름(채널 아트)을 버리고
 *   지금은 고객센터에서도 「배너 이미지」 하나로 부른다. 파일도 하나만 올린다.
 *
 * 🔴 규격 — 2026-08-28 유튜브 고객센터 «원문 실측»(support.google.com/youtube/answer/10456525):
 *   · 업로드 최소: 16:9 의 **2048×1152**
 *   · 권장(특히 TV 화면을 염두에 둘 경우): **2560×1440**  ← 여기서 굽는 크기
 *   · **텍스트·로고가 잘리지 않는 최소 크기: 1235×338**  ← 이 파일의 존재 이유
 *   · 파일 크기 6MB 이하
 *   ⚠ 데스크톱·모바일이 «각각» 몇 픽셀을 보여주는지는 **공식 문서에 없다**(안 재봤다).
 *     흔히 도는 2560×423 은 출처를 못 찾았으므로 여기 쓰지 않는다 —
 *     확실한 것은 1235×338 하나뿐이고, 그 안에만 넣으면 어디서도 안 잘린다.
 *
 * 🔑 그래서 배너 디자인의 전부는 이것이다: **캔버스의 48%×23% 짜리 가운데 띠.**
 *   대부분의 채널이 이걸 몰라서 큰 화면에서만 멀쩡하고 폰에서 글자가 잘린다.
 *   바깥 영역은 «분위기»만 진다 — 글자도 로고도 두지 않는다.
 *
 * 사용:
 *   node tools/배너굽기.js               안 전부 + 시안 지면
 *   node tools/배너굽기.js --안 밤       한 안만
 *   node tools/배너굽기.js --안내선      안전 영역을 그려 넣은 «검수용» 판도 같이 굽는다
 *   출력 = docs/SHIFT/배너/<안>.png · 시안 = docs/SHIFT/배너/시안.html
 */
'use strict';
const fs = require('fs');
const path = require('path');
const { spawnSync } = require('child_process');
const { 색, 기호선, 워드마크, 도장 } = require('./lib/로고정본.js');
const 마스코트자산 = require('./lib/마스코트자산.js');
/* 브랜드 서체(SUIT)는 «굽기»가 지면 안에 싣는다 — 이름만 부르고 안 실으면 막힌 적이 없어
 * 경고도 안 뜨고 그냥 시스템 고딕으로 떨어진다(tests/지면폰트.test.js ⑦ · 정본 lib/브랜드폰트.js). */
const 브랜드폰트 = require('./lib/브랜드폰트.js');

const ROOT = path.resolve(__dirname, '..');
const 낼곳 = path.join(ROOT, 'docs/SHIFT/배너');

/* ══ 규격 — 소셜 대문은 «자리마다» 다른 곳이 잘린다 ═══════════════════════════
 * 🔑 **규격을 안(variant)에 붙인 까닭**: 펠트 필터·로고·몽글 누끼가 한 파일에 있어야
 *   「한 값을 두 곳이 안다」는 병이 안 생긴다. 커버 전용 파일을 새로 파면 펠트 필터가
 *   두 벌이 되고, 다음에 한쪽만 고쳐진다 — 마스코트 10벌 공존이 그렇게 났다.
 *
 * **유튜브 배너** — 고객센터 원문 실측(08-28): 권장 2560×1440 · 안 잘리는 띠 1235×338.
 *
 * **페이스북 페이지 커버** — 🔴 **09-02 에 실물로 재서 규격을 갈았다.**
 *   처음엔 흔히 도는 권장값 **1640×856**(1.92:1)으로 굽고 올렸다. 그랬더니 데스크톱이
 *   **아래 260px 을 잘라내** 몽글의 발과 이음 솔기가 통째로 사라졌다. 그 화면을 자로 재니
 *   데스크톱 표시 영역이 **1160×422 = 2.75:1** 이었다 — 1640 너비 기준 **596px** 만 보인다.
 *   ⇒ 그래서 **처음부터 1640×596 으로 굽는다.** 잘릴 것이 없으면 잘리지 않는다.
 *
 *   🔑 **두 가지가 «틀린 앎»이었다**:
 *   ① 권장 1640×856 은 «업로드 상한»이지 «보이는 판»이 아니다. 856 으로 구우면 30%가 죽는다.
 *   ② **프로필 사진은 커버와 안 겹친다** — 최신 페이스북은 커버 «아래»에 따로 놓는다.
 *     그래서 `올림`(왼쪽 아래를 비우려던 축)도 필요 없어졌다. 0 으로 되돌린다.
 *
 *   모바일은 여전히 좌우를 잘라낸다(더 세로로 본다) — 596 높이에 1.78:1 이면 너비 **1061px**
 *   만 남는다. 안전 영역 1180 은 그보다 넓으니 **글은 1061 안에** 있어야 한다(문안 실측 850).
 *
 * **링크드인 개인 프로필 배경** — 🔴 **09-06 에 실물 프로필(`/in/yuhobuilds`)을 자로 쟀다.**
 *   데스크톱이 배경 칸을 **792×198 = 정확히 4:1** 로 그린다 ⇒ **잘리는 곳이 없다.**
 *   그래서 권장값 **1584×396** 을 그대로 굽는다(페북 커버가 아래 30%를 먹던 것과 반대 결과다 —
 *   같은 자를 댔더니 이번엔 권장값이 맞았다. 재는 것과 믿는 것은 다르다).
 *   ⚠ **모바일 앱은 안 재봤다.** 데스크톱만 실측이다.
 *
 *   🔴 이 자리의 진짜 제약은 잘림이 아니라 **얼굴 사진**이다 — 프로필 사진이 배경의
 *   «왼쪽 아래»를 덮는다. 같은 날 실측: 화면에서 152×152 가 카드 기준 x28·y106 →
 *   1584 좌표로 **304×304 가 x 56~360 · y 212~396**. 배경의 왼쪽 23% × 아래 46% 다.
 *   ⇒ 그래서 이 판만 유일하게 문안이 «가운데»가 아니다(아래 `밀기`). */
const 규격들 = {
  유튜브:   { W: 2560, H: 1440, 안전W: 1235, 안전H: 338, 배: 1,    올림: 0,  몽글폭: 560, 몽글우: 186, 이음비: 0.875, 밀기: 0 },
  페북커버: { W: 1640, H: 596,  안전W: 1050, 안전H: 380, 배: 0.64, 올림: 0,  몽글폭: 300, 몽글우: 74, 이음비: 0.82, 밀기: 0 },
  링크드인: { W: 1584, H: 396,  안전W: 1000, 안전H: 300, 배: 0.62, 올림: 0,  몽글폭: 300, 몽글우: 74, 이음비: 0.805, 밀기: 190 },
};
let { W, H, 안전W, 안전H, 배, 올림, 몽글폭, 몽글우, 이음비, 밀기 } = 규격들.유튜브;
/** 안 하나를 굽기 직전에 그 안의 규격으로 갈아 끼운다. 안을 하나씩 굽는 도구라 안전하다.
 *  🔑 **적용한 규격을 돌려준다.** 위 전역은 «다음 안을 구울 때» 갈리므로, 그 안의 규격을
 *    나중까지 쓰려면(시안이 칸마다 제 비율로 그리려면) 여기서 받아 «떠 담아» 둬야 한다. */
function 규격적용(안) {
  const 규격 = 규격들[(안들[안] || {}).규격 || '유튜브'];
  ({ W, H, 안전W, 안전H, 배, 올림, 몽글폭, 몽글우, 이음비, 밀기 } = 규격);
  return 규격;
}
/** 규격에 비례한 px — 배너에서 눈으로 맞춘 값이 작은 판에서도 같은 «무게»로 읽히게 한다. */
const 재기 = (n) => Math.round(n * 배);

/* ══ 재질 — 명품화의 기본은 세밀한 재질 표현이다 (유호 지시 08-28) ═══════════════
 *
 * 문법은 «로고정본의 펠트 필터»(feTurbulence 2층 = 밝은 보풀 lf + 어두운 결 df)를 그대로 잇고,
 * 값만 배너 스케일로 다시 잰다. 🔑 왜 값을 다시 재나 — 필터의 주파수·변위는 «사용자 좌표» 단위라,
 * 로고(viewBox 181 을 216px 로 = 1.2px/단위)에서 좋던 결을 viewBox 74 짜리 꺾쇠를 1400px 로
 * 키워 쓰면(18.9px/단위) 보풀이 16배 굵은 «얼룩»이 된다. 섬유 굵기는 물체 크기와 무관하다 —
 * 큰 펠트 조각일수록 결이 상대적으로 잘아야 실물처럼 읽힌다.
 * ⇒ 배너 쪽 SVG 는 전부 «픽셀 좌표계»로 조립하고(1 단위 ≈ 1px), 필터 값은 로고정본의
 *   픽셀 환산값을 그대로 쓴다. 도형(기호선)은 transform=scale 로 키운다.
 *
 * 그리고 이 재질층이 «미학»만이 아니다 — 어두운 라디얼 그라디언트는 유튜브 재압축에서
 * 밴딩(띠 얼룩)이 생기는데, 전면의 미세 노이즈가 디더 역할을 해 그것을 깬다. */

/** 전면 양모 천 — 뭉침(저주파) 2층 + 섬유(고주파) 2층. 팔레트만 받아 라이트/다크 둘 다 굽는다. */
/* 🔑 크기를 인자로 받는다(기본값 = 배너 크기) — 형제 도구(라디오 배경굽기)가 720p 판을 빌려 간다.
 *   ⚠ 크기를 안 받고 viewBox 만 늘리면 `preserveAspectRatio="none"` 이 노이즈를 «늘여서»
 *     섬유 결이 가로로 뭉갠다. 재질은 늘리는 것이 아니라 그 크기로 다시 짜는 것이다. */
function 양모천svg({ 보풀, 그늘, 어둡나, w = W, h = H }) {
  const rgb = (hex) => {
    const n = parseInt(hex.slice(1), 16);
    return [(n >> 16 & 255) / 255, (n >> 8 & 255) / 255, (n & 255) / 255].map((v) => v.toFixed(4));
  };
  const [lr, lg, lb] = rgb(보풀), [dr, dg, db] = rgb(그늘);
  /* 알파 = 노이즈 밝기에서 유도(0 0 0 k b 행) — 로고정본과 같은 문법.
   *
   * 🔴 첫 판의 병(08-28 렌더 눈검증): 뭉침 두 층의 알파 기울기(0.5·0.34)가 너무 세서
   *   «천의 명암»이 아니라 «얼룩진 돌»이 됐다. 명품 천의 문법은 반대다 —
   *   멀리서는 거의 민면이고, 가까이서만 결이 산다. 그래서 뭉침은 겨우 보일 만큼(≤0.2),
   *   섬유(고주파)는 성기지만 또렷하게 — «세밀한」은 굵게가 아니라 «잘게»다. */
  /* 두 번째 눈검증(1:1 돋보기)의 처방 — 뭉침은 첫 판이 과했고 이 값이 맞았는데,
   * «섬유»는 1:1 에서 거의 안 보였다(임계 0.72 는 프랙탈 노이즈에서 드물다).
   * 잔털의 문턱을 낮춰 1:1 에서 또렷하고 축소에서 사라지는 자리(≈0.54)에 놓는다. */
  const 뭉침어둠알파 = 어둡나 ? '0.3 -0.19' : '0.2 -0.13';
  const 뭉침빛알파 = 어둡나 ? '0.26 -0.16' : '0.3 -0.2';
  const 보풀알파 = 어둡나 ? '0.95 -0.48' : '0.7 -0.44';
  const 그늘알파 = 어둡나 ? '0.6 -0.33' : '0.5 -0.33';
  return `<svg class="천" viewBox="0 0 ${w} ${h}" preserveAspectRatio="none" aria-hidden="true">
  <defs>
    <filter id="bn-wool" x="0" y="0" width="100%" height="100%" color-interpolation-filters="sRGB">
      <feFlood flood-color="#000" flood-opacity="0" result="base"/>
      <feTurbulence type="fractalNoise" baseFrequency="0.006 0.008" numOctaves="3" seed="41" result="m1"/>
      <feColorMatrix in="m1" type="matrix" values="0 0 0 0 ${dr}  0 0 0 0 ${dg}  0 0 0 0 ${db}  0 0 0 ${뭉침어둠알파}" result="뭉침어둠"/>
      <feTurbulence type="fractalNoise" baseFrequency="0.013 0.018" numOctaves="3" seed="53" result="m2"/>
      <feColorMatrix in="m2" type="matrix" values="0 0 0 0 ${lr}  0 0 0 0 ${lg}  0 0 0 0 ${lb}  0 0 0 ${뭉침빛알파}" result="뭉침빛"/>
      <feTurbulence type="fractalNoise" baseFrequency="1.05 0.52" numOctaves="2" seed="13" result="f1"/>
      <feColorMatrix in="f1" type="matrix" values="0 0 0 0 ${lr}  0 0 0 0 ${lg}  0 0 0 0 ${lb}  0 0 0 ${보풀알파}" result="잔털빛"/>
      <feTurbulence type="fractalNoise" baseFrequency="0.82 0.42" numOctaves="3" seed="29" result="f2"/>
      <feColorMatrix in="f2" type="matrix" values="0 0 0 0 ${dr}  0 0 0 0 ${dg}  0 0 0 0 ${db}  0 0 0 ${그늘알파}" result="잔털어둠"/>
      <feMerge>
        <feMergeNode in="base"/><feMergeNode in="뭉침어둠"/><feMergeNode in="뭉침빛"/>
        <feMergeNode in="잔털어둠"/><feMergeNode in="잔털빛"/>
      </feMerge>
    </filter>
  </defs>
  <rect width="${w}" height="${h}" filter="url(#bn-wool)"/>
</svg>`;
}

/* ══ 아플리케의 «실» — 채널마다 갈리는 유일한 값 (유호 지시 09-01 「잘 어울리게」) ═══════
 *
 * 🔑 구조는 셋이 같고 **실만 다르다.** 이것이 킷 원리 ②(「실은 부품, 램프가 정본」)의 집행이다 —
 *   세 채널이 한 가족으로 읽히는 까닭은 같은 로고를 써서가 아니라 «같은 재질 문법»을 써서다.
 *   잉크실 = 톤온톤(개인 · 기록) · 코랄실 = 브랜드 신호(정보) · 여린코랄 = 밝은 천 위(학생).
 * ⚠ 램프의 «세 자리»(보풀 빛 · 몸통 · 그늘)를 실마다 그대로 채운다 — 한 자리라도 다른 실에서
 *   빌려오면 펠트가 아니라 «색칠한 도형»이 된다. */
const 실 = {
  잉크: { 몸: 색['Ink'], 보풀: 색['Deep Wool'], 그늘: 색['Ink Deep'], 결어둠: '#080604', 땀: 색['Deep Wool'] },
  코랄: { 몸: 색['Coral'], 보풀: 색['Coral Soft'], 그늘: 색['Coral 3'], 결어둠: 색['Coral Rim'], 땀: 색['Coral 3'] },
  여린코랄: { 몸: 색['Coral Soft'], 보풀: '#FFD9CC', 그늘: 색['Coral 2'], 결어둠: 색['Coral 3'], 땀: 색['Coral 2'] },
};

/** hex → 0~1 삼원색. feColorMatrix 가 «비율»로 색을 받아서 필요하다. */
const rgb01 = (hex) => {
  const n = parseInt(String(hex).slice(1), 16);
  return [(n >> 16 & 255) / 255, (n >> 8 & 255) / 255, (n & 255) / 255].map((v) => v.toFixed(4));
};

/** 펠트 꺾쇠 — 펠트 조각을 천 위에 «꿰매 붙인» 아플리케.
 *  층은 로고정본 펠트 문법 그대로(접촉 그림자 → 겉보풀 fuzz 2층 → 잘린 단 → 몸 → 손땀),
 *  좌표계만 픽셀이다. 실이 잉크면 멀리서는 «무늬»고 가까이서만 «조각»이다 — 톤온톤의 문법.
 *  실을 코랄로 갈면 같은 조각이 «신호»가 된다(구조는 한 줄도 안 바뀐다). */
function 잉크펠트꺾쇠(px, 쓸실 = 실.잉크) {
  const [lr, lg, lb] = rgb01(쓸실.보풀);
  const [dr, dg, db] = rgb01(쓸실.결어둠);
  const S = px / 74;                        // 기호 펠트 viewBox 폭 74 → px
  const 몸 = (fill) => `<path d="${기호선}" fill="none" stroke="${fill}" stroke-width="13"
      stroke-linecap="round" stroke-linejoin="round"/>`;
  /* 손땀 — 실물 손바느질 땀은 5~8mm 로 «조각이 커져도 안 커진다».
   * dasharray 는 scale 안 좌표라, 원하는 픽셀 땀(≈26px)을 S 로 나눠 단위로 되돌린다. */
  const 땀길 = (24 / S).toFixed(2), 땀틈 = (18 / S).toFixed(2);
  /* 🔴 첫 판의 병 둘(08-28 렌더 눈검증):
   *   ① 잘린 단 변위가 30px 로 과해 윤곽이 «뭉텅한 덩어리»가 됐다 — 실물 펠트의 가위단은
   *      거의 곧고 «살짝만» 흔들린다. 변위를 12px 로 내리고 흔들림 주기를 잘게(0.016).
   *   ② 보풀(lf)의 알파 기울기 0.8 이 커서 조각의 몸통이 회갈색으로 떠 «톤온톤»이 죽었다 —
   *      멀리서 무늬·가까이서 조각이려면 보풀은 어둠 속에서 겨우 반짝여야 한다(0.5). */
  return `<svg class="아플리케" viewBox="0 0 ${px} ${px * (77 / 74)}" style="width:${px}px" aria-hidden="true">
  <defs>
    <filter id="bn-cut" x="-12%" y="-12%" width="124%" height="124%">
      <feTurbulence type="fractalNoise" baseFrequency="0.016" numOctaves="3" seed="7" result="w"/>
      <feDisplacementMap in="SourceGraphic" in2="w" scale="12"/>
    </filter>
    <filter id="bn-fuzz1" x="-16%" y="-16%" width="132%" height="132%">
      <feTurbulence type="fractalNoise" baseFrequency="0.5 0.68" numOctaves="2" seed="11" result="n"/>
      <feDisplacementMap in="SourceGraphic" in2="n" scale="9"/><feGaussianBlur stdDeviation="0.5"/>
    </filter>
    <filter id="bn-fuzz2" x="-20%" y="-20%" width="140%" height="140%">
      <feTurbulence type="fractalNoise" baseFrequency="0.24 0.36" numOctaves="2" seed="23" result="n"/>
      <feDisplacementMap in="SourceGraphic" in2="n" scale="20"/><feGaussianBlur stdDeviation="1.2"/>
    </filter>
    <filter id="bn-felt-ink" x="-10%" y="-10%" width="120%" height="120%" color-interpolation-filters="sRGB">
      <feTurbulence type="fractalNoise" baseFrequency="0.016" numOctaves="3" seed="7" result="w"/>
      <feDisplacementMap in="SourceGraphic" in2="w" scale="12" result="shape"/>
      <feTurbulence type="fractalNoise" baseFrequency="0.96 0.42" numOctaves="2" seed="13" result="n1"/>
      <feColorMatrix in="n1" type="matrix" values="0 0 0 0 ${lr}  0 0 0 0 ${lg}  0 0 0 0 ${lb}  0 0 0 0.5 -0.31" result="lf"/>
      <feComposite in="lf" in2="shape" operator="in" result="lfc"/>
      <feTurbulence type="fractalNoise" baseFrequency="0.66 0.38" numOctaves="3" seed="29" result="n2"/>
      <feColorMatrix in="n2" type="matrix" values="0 0 0 0 ${dr}  0 0 0 0 ${dg}  0 0 0 0 ${db}  0 0 0 0.72 -0.36" result="df"/>
      <feComposite in="df" in2="shape" operator="in" result="dfc"/>
      <feMerge><feMergeNode in="shape"/><feMergeNode in="dfc"/><feMergeNode in="lfc"/></feMerge>
    </filter>
    <filter id="bn-soft" x="-8%" y="-8%" width="116%" height="116%">
      <feTurbulence type="fractalNoise" baseFrequency="0.02" numOctaves="2" seed="5" result="w"/>
      <feDisplacementMap in="SourceGraphic" in2="w" scale="8"/>
    </filter>
  </defs>
  <g transform="scale(${S}) translate(-96,-28)">
    <g opacity="0.5" filter="url(#bn-soft)"><g transform="translate(1.0,2.3)">${몸(쓸실.그늘)}</g></g>
    <g opacity="0.1" filter="url(#bn-fuzz2)">${몸(쓸실.몸)}</g>
    <g opacity="0.22" filter="url(#bn-fuzz1)">${몸(쓸실.몸)}</g>
    <g filter="url(#bn-cut)"><g transform="translate(0,1.7)">${몸(쓸실.그늘)}</g></g>
    <g filter="url(#bn-felt-ink)">${몸(쓸실.몸)}</g>
    <g filter="url(#bn-soft)">
      <path d="${기호선}" fill="none" stroke="${쓸실.그늘}" stroke-width="${(4.6 / S).toFixed(3)}"
        stroke-dasharray="${땀길} ${땀틈}" stroke-dashoffset="${(9 / S).toFixed(2)}"
        stroke-linecap="round" stroke-linejoin="round" opacity="0.5"
        transform="translate(0,${(1.8 / S).toFixed(3)})"/>
      <path d="${기호선}" fill="none" stroke="${쓸실.땀}" stroke-width="${(3.4 / S).toFixed(3)}"
        stroke-dasharray="${땀길} ${땀틈}" stroke-linecap="round" stroke-linejoin="round" opacity="0.42"/>
    </g>
  </g>
</svg>`;
}

/** 이음 손땀 — 천 두 장을 이어 붙인 수평 솔기. 큰 화면에서만 보이는 자리의 «공예 단서». */
function 이음선svg(y, 실색, 투명) {
  return `<svg class="이음" viewBox="0 0 ${W} 60" style="top:${y}px" aria-hidden="true">
  <defs>
    <filter id="bn-seam" x="-2%" y="-200%" width="104%" height="500%">
      <feTurbulence type="fractalNoise" baseFrequency="0.012 0.3" numOctaves="2" seed="19" result="w"/>
      <feDisplacementMap in="SourceGraphic" in2="w" scale="7"/>
    </filter>
  </defs>
  <g filter="url(#bn-seam)" opacity="${투명}">
    <line x1="60" y1="30" x2="${W - 60}" y2="30" stroke="${실색}" stroke-width="3.4"
      stroke-dasharray="27 20 24 22 29 18" stroke-linecap="round"/>
    <line x1="60" y1="34" x2="${W - 60}" y2="34" stroke="${색['Ink Deep']}" stroke-width="2.6"
      stroke-dasharray="25 22 27 19 24 21" stroke-dashoffset="11" stroke-linecap="round" opacity="0.7"/>
  </g>
</svg>`;
}

/** 문안 — 공개선 12칸을 지난다.
 *  ⑫ 기본 시제 = 「짓고 있습니다」 · ⑩ 돈 ✗ · ⑪ 일의 숫자는 배너에 안 쓴다(안 바뀌는 자리라 낡는다).
 *  ⚠ 업로드 주기도 안 쓴다 — 아직 확정이 아니고(첫 3편 실측 뒤), 박으면 못 지키는 날 거짓말이 된다. */
const 큰줄 = 'AI로 교육 회사를 짓고 있습니다';
const 작은줄 = '학원 하나를 혼자 짓는 실황';

/** 링크드인 세 안이 함께 쓰는 빛 — 한 값을 세 곳이 알면 갈린다(⑦⑧⑨는 빛이 같아야 한 벌이다). */
const 링크드인빛 = { 키형: '46% 130% at 62% 47%', 비형: '112% 148% at 50% 50%', 키: 0.11, 비: 0.5 };

const 안들 = {
  밤: {
    이름: '① 밤 바탕',
    설명: '유튜브 기본이 다크라 화면에 이어 붙는다',
    바탕: 색['Ink Deep'],
    글자: 색['Paper'],
    보조: 색['Stone'],
    로고: () => 워드마크({ 판: '다크', 표현: '펠트', 색갈래: '코랄' }),
    천: { 보풀: 색['Deep Wool'], 그늘: '#000000', 어둡나: true },
  },
  양모: {
    이름: '② 양모 바탕',
    설명: '브랜드 지면과 같은 결. 다크 UI 위에서 카드처럼 뜬다',
    바탕: 색['Paper'],
    글자: 색['Ink'],
    보조: 색['Ash Wool'],
    로고: () => 워드마크({ 판: '라이트', 표현: '펠트', 색갈래: '코랄' }),
    천: { 보풀: '#FFFFFF', 그늘: 색['Oat'], 어둡나: false },
  },
  /* ══ 채널 셋 — 한 가족, 세 얼굴 (유호 지시 2026-09-01) ══════════════════════════
   * 구조(양모천 → 조명 → 아플리케 → 이음 손땀 → 로고 → 문안)는 셋이 **같다.**
   * 갈리는 것은 셋뿐: ①천의 밝기 ②아플리케의 실 ③문안. 그래서 나란히 놓으면 한 벌로 읽히고,
   * 따로 만나면 채널마다 다른 말을 한다. 🔑 청중이 다르면 «밝기»부터 갈라야 한다 —
   * 개인·정보 축은 한국어권이 밤에 보고, 학생 축은 낮에 밝은 화면에서 본다. */
  밤기호: {
    이름: '③ 밤 바탕 · 기호 — ✅ 확정 + 명품화 (08-28) · @yuhobuilds',
    설명: '톤온톤 잉크 펠트 아플리케 + 양모 천 결 + 이음 손땀 · 업로드본 = yuhobuilds_배너.png',
    바탕: 색['Ink Deep'],
    글자: 색['Paper'],
    보조: 색['Stone'],
    /* 로고 = «단색다크» (유호 지시 08-28 「배경이 검정이니 k를 흰색 재질로 · 저번의 차분한
     * 스티치 테두리 버전」) — k 가 syn 과 같은 Paper 펠트가 되고 실땀 테두리가 글자를 두른다.
     * 최종표 지면·프로필과 같은 판이라 SHIFT 지면끼리 로고가 갈리지 않게 되는 부수 효과도 있다. */
    로고: () => 워드마크({ 판: '다크', 표현: '펠트', 색갈래: '단색' }),
    천: { 보풀: 색['Deep Wool'], 그늘: '#000000', 어둡나: true },
    워터마크: true,
    이음: true,
    /* 🔑 «두 단어» 규칙(유호 확정 09-02 · 마케팅 정찰 C1) — Hermès·LOEWE·ROLEX·CHANEL·POP MART 배너 여덟 곳 실측:
     *   글자 0~2단어 · 히어로 하나 · 중앙. 문장 두 줄이던 것을 넉 자로 걷는다. 구조·재질은 그대로(08-28 확정판). */
    큰줄: '짓고 있습니다',
    작은줄: '',
    확정: 'yuhobuilds_배너',
  },

  synkbrief: {
    이름: '④ 밤 바탕 · 코랄 k — @synkbrief (SYNK 브리핑)',
    설명: '③과 한 벌이다(같은 유호님 축·같은 한국어권). 갈리는 것은 워드마크의 k 한 글자와 문안뿐 · 업로드본 = synkbrief_배너.png',
    바탕: 색['Ink Deep'],
    글자: 색['Paper'],
    보조: 색['Stone'],
    /* 🔴 **코랄 아플리케를 두 번 시도했다가 걷었다**(09-01 눈검증 · 0.30 → 0.16 둘 다 실패).
     *   밤 천 위의 코랄 #F96859 는 어떤 투명도에서도 «탁한 벽돌색»으로 읽힌다 —
     *   투명도가 아니라 «채도»의 문제라, 낮출수록 갈색에 가까워질 뿐 코랄로 안 돌아온다
     *   (펠트 필터의 그늘층이 Coral Rim #941F19 이라 더 탁해진다).
     *   🔑 **킷이 이미 아는 규칙이었다** — 코랄은 «밝은 면 위»가 제자리다(철칙 ②③의 거울).
     *   ⇒ 밤 판에서 코랄은 큰 면이 아니라 **한 점**으로만 산다: 워드마크의 k 한 글자.
     *   그것이 ③(단색 k)과 이 판을 가르고, 프로필의 코랄 도장과도 이어진다.
     *   ⚠ 다시 「코랄 배너」를 시도하려면 바탕부터 밝혀야 한다 — 실만 갈면 같은 자리에 온다. */
    로고: () => 워드마크({ 판: '다크', 표현: '펠트', 색갈래: '코랄' }),
    천: { 보풀: 색['Deep Wool'], 그늘: '#000000', 어둡나: true },
    워터마크: true,
    이음: true,
    /* 09-02 «두 단어» 규칙 — 이 판은 «워드마크만»(샤넬식 · 글자 0). 약속 문장(「오늘 나온 AI, 무슨 뜻인가 ·
     * 실제로 써 본 것만 · 매일 한 장」)은 채널 설명·인스타 소개가 진다 — 배너는 얼굴이지 설명서가 아니다. */
    큰줄: '',
    작은줄: '',
    확정: 'synkbrief_배너',
  },

  synklab: {
    이름: '⑤ 양모 바탕 · 몽글 — @synkkorean (SYNK LAB)',
    설명: '청중이 다르다(몽골 학생·학부모). 유일하게 밝은 판이고, 얼굴은 로고가 아니라 마스코트다 · 업로드본 = synklab_배너.png',
    바탕: 색['Paper'],
    글자: 색['Ink'],
    보조: 색['Ash Wool'],
    로고: () => 워드마크({ 판: '라이트', 표현: '펠트', 색갈래: '코랄' }),
    천: { 보풀: '#FFFFFF', 그늘: 색['Oat'], 어둡나: false },
    워터마크: true,
    워터실: 실.여린코랄,
    /* 🔴 첫 판(0.34)은 «분홍 얼룩»이 됐다 — 밝은 천 위에서는 같은 알파가 훨씬 세게 읽힌다.
     * 톤온톤의 문법은 «멀리서 무늬 · 가까이서 조각»인데 멀리서도 보이면 실패다(09-01 눈검증). */
    워터투명: 0.22,
    이음: true,
    /* 🔑 몽글은 «안전 영역 밖»에 앉는다 — 큰 화면에서만 보이는 덤이고, 폰에서 잘려도 뜻이 안 샌다.
     *   학생이 아는 얼굴이 채널 지면에 있다는 것 자체가 값이라 넣되, 뜻은 안 싣는다. */
    몽글: true,
    /* 이 채널이 «지금» 하는 일의 진실. 라디오 하나뿐이라 라디오를 말한다 —
     * 학원 콘텐츠가 붙는 날 이 두 줄을 갈면 된다(배너는 언제든 바뀐다). */
    /* ⚠ 작은줄에서 「SYNK LAB」을 뺐다 — 채널 이름이 바로 위에 뜨는데 배너가 또 말하면
     *   같은 값을 두 곳이 말하는 꼴이고, 좁은 안전 영역을 이름이 먹는다. */
    /* 09-02 «두 단어» 규칙 — 한 줄만. 「공부할 때 켜 두세요」는 채널 설명 첫 줄로 옮겼다(계정개설_실행지 §2 ③). */
    큰줄: '24시간 한국어 라디오',
    작은줄: '',
    확정: 'synklab_배너',
  },

  /* ══ ⑥ 페이스북 페이지 커버 — LAB (유호 지시 2026-09-02 「디테일하게 트렌디하게」) ═══
   * 배너 셋과 **같은 천·같은 아플리케·같은 손땀**이다. 갈리는 것은 셋:
   *
   * ① **규격** — 🔴 첫 판을 1640×856 으로 굽고 올렸다가 **아래 30%가 잘려** 몽글 발이 날아갔다.
   *   실물을 자로 재서 1640×596 으로 갈았다(위 `규격들` 주석). 안전 영역이 배너보다 «넓고 낮다».
   *
   * ② 🔴 **로고를 뺐다.** 페이스북은 커버 «바로 아래»에 프로필 사진 + 페이지 이름 「SYNK」를
   *   크게 띄운다. 커버가 로고를 또 말하면 한 화면에서 브랜드가 **네 번** 나온다(프로필·이름·
   *   소개·커버). 배너의 규약 「채널 이름이 위에 뜨는데 배너가 또 말하면 같은 값을 두 곳이
   *   말하는 꼴」을 그대로 잇는다 — 자리가 다르니 «빼는» 쪽으로 적용된다.
   *   ⇒ 그래서 문안을 키웠다(80 → 100). 빈 자리를 채우는 게 아니라 **여백을 남긴다.**
   *   ⚠ 모바일은 좌우를 잘라 **1061px** 만 남긴다 — 큰줄 실측 850px 이라 들어가지만,
   *     문안을 늘리면 이 셈을 다시 한다.
   *
   * ③ **몽골어다.** LAB 은 관객이 몽골 학생·학부모다(SHIFT 채널 셋은 한국어권).
   *   ⚠ **감수 전이다.** 표준 표현만 골라 위험을 낮췄고(`тоглоом шиг` = 게임처럼),
   *   날짜·급수 같은 «틀리면 손해인 숫자»는 넣지 않았다.
   *   같은 문안이 인스타 소개·페이지 소개에도 서 있다 — 검수자가 오면 **세 자리를 함께** 고친다. */
  synklab커버: {
    규격: '페북커버',
    이름: '⑥ 페이스북 커버 · 양모 바탕 — SYNK LAB 페이지',
    설명: '⑤ 라디오 배너와 한 벌(같은 천·아플리케·손땀). 로고를 빼고 문안을 키웠다 · 업로드본 = synklab_커버.png',
    바탕: 색['Paper'],
    글자: 색['Ink'],
    보조: 색['Ash Wool'],
    로고: () => 워드마크({ 판: '라이트', 표현: '펠트', 색갈래: '코랄' }),
    천: { 보풀: '#FFFFFF', 그늘: 색['Oat'], 어둡나: false },
    /* 🔴 큰 꺾쇠 워터마크를 **껐다**(09-02 · 유호 「< 균형이 안 맞는것같은데?」).
     *   1.7배를 옆으로 밀어도, 2.3배로 키워 중앙에 둬도 균형이 안 잡혔다 — 커버는 배너보다
     *   **낮고 넓어서** 큰 꺾쇠가 «천의 무늬»가 되기 전에 «화면을 먹는 덩어리»가 된다.
     *   ⇒ 덜어냈다. 브랜드 신호는 **도장 안의 꺾쇠**가 이미 진다 — 같은 기호를 두 번 말할
     *   까닭이 없다. 천이 비면 도장·문안·가이드 셋이 비로소 선명해진다. */
    워터마크: false,
    워터실: 실.여린코랄,
    /* 🔴 ⑤의 0.22 를 그대로 쓰다가 **너무 진했다**(09-02 눈검증). 까닭은 알파가 아니라 «면적»이다 —
     * 꺾쇠를 1.7배로 키우니 같은 알파가 훨씬 세게 읽힌다. 톤온톤의 문법은 «멀리서 무늬·가까이서 조각»인데
     * 멀리서도 보이면 실패다. ⇒ 0.14. 크기를 만지면 알파를 다시 잰다. */
    워터투명: 0.14,
    이음: true,
    몽글: true,
    /* 프로필 사진이 «눈웃음»이라 커버는 «눈감음»으로 간다 — 한 화면에 같은 얼굴을 두 번 두지 않는다. */
    몽글표정: '눈감음',
    /* 유호 채택 09-02 「괜찮은데?」 — 까몽을 왼쪽에 세운다. 자세가 몽글과 다른 것은 미결로 남는다. */
    까몽: '눈웃음',
    /* 🔑 «보이는 폭»을 몽글과 같게 맞췄는데도 까몽이 작아 보인다 — **털이 퍼진 실루엣**이라
     * 그림 폭의 상당 부분이 보풀이고 «몸통»은 훨씬 작기 때문이다. 눈으로 1.28배가 맞았다.
     * 올림은 코랄 조각(그림에서 가장 아래)이 이음 솔기에 «닿게» 하는 값이다 — 까몽은 뒹구는
     * 자세라 «발밑»이 없어서, 몽글처럼 발을 세우는 셈이 안 통한다. */
    까몽배: 1.28,
    까몽올림: 6,
    /* ══ 명품화 (09-02 · 유호 「조금만 더 트렌디하고 명품화 · 뭔가 약간 아쉬워」) ═══════
     * 아쉬움의 정체는 «평평함»이었다. 요소를 더하는 길은 막혀 있다 — 요소공방 156장이
     * 전부 **어두운 카드 규약**이라 밝은 양모 천에 올리면 검은 사각이 뜬다(자모·책 실측).
     * ⇒ 더하지 않고 **빛으로 세운다.** 넷을 고쳤다:
     *   ① 키라이트를 좁고 세게 — 문안 뒤가 밝아져 글이 «천에서 떠오른다»
     *   ② 비네트를 깊게(0.13 → 0.21) — 가장자리가 잠겨 흩어진 세 덩어리가 한 화면으로 모인다
     *   ③ 가이드 그림자를 두 층으로 — 접촉(짧고 진한) + 확산(길고 옅은). 한 층은 «뜨고» 두 층은 «놓인다»
     *   ④ 꺾쇠를 1.7배로 키우고 오른쪽 아래로 밀어 **화면 밖으로 잘리게** — 크기가 딱 맞으면
     *      «스티커»고, 잘려 나가면 «천에 짜인 무늬»가 된다. 문안과 겹치던 모서리도 이때 빠진다. */
    빛: { 키형: '44% 96% at 50% 43%', 비형: '102% 130% at 50% 50%', 키: 0.62, 비: 0.21 },
    그림자: 'drop-shadow(0 5px 7px rgba(43,35,32,0.30)) drop-shadow(0 22px 34px rgba(43,35,32,0.14))',
    /* 🔴 1.7배를 오른쪽 아래(57%·64%)로 밀었더니 **균형이 깨졌다**(유호 09-02 「< 균형이 안
     * 맞는것같은데?」). 한쪽으로 쏠린 꺾쇠는 «무늬»가 아니라 «흘러내린 조각»으로 읽힌다.
     * ⇒ 중앙으로 되돌리고 2.3배로 더 키운다 — 화면보다 커져 사방이 잘리면 위치가 사라지고
     *   비로소 «천에 짜인 무늬»가 된다. 문안과 겹치던 모서리 문제도 이때 함께 풀린다
     *   (겹치는 것이 아니라 «전면이 무늬»가 되므로). */
    워터배: 2.3,
    바닥띠: true,
    /* 도장 = 로고정본의 셋째 부품(펠트 원 + 쿠션 볼록 + **실땀 링** + 접지).
     * 유호 지시 09-02 「가운데 윗부분에 syn< 스티치한 버전」 — 기호 «<» 는 브랜드킷 §3 에서
     * 「이미 안에 들어온 자리」의 신호이고 도장이 그 대표 자리다. 워드마크 synk 를 안 쓰는 까닭은
     * 페이스북이 커버 아래에 페이지 이름을 이미 크게 띄우기 때문이다 — 이름은 한 번이면 된다. */
    /* 🔴 도장만 두면 **이름이 없다**(유호 09-02 「어떤 회사인지 보통 써있지 않나?」 — 맞는 지적이다).
     * 팔로워 0 의 신생 브랜드에서 꺾쇠는 «아는 사람»에게만 신호이고, 커버 이미지는 클릭·공유·캡처로
     * **페이스북 밖으로 나간다** — 그때 아래 페이지 이름은 안 따라간다.
     * 워드마크만 두는 판도 구워 봤는데 **문안과 같은 검정 굵은 글씨라 「제목이 둘」**이 됐다.
     * ⇒ 락업 — 도장(코랄 원)이 눈을 잡고 워드마크가 이름을 말한다. 색과 무게가 갈려 위계가 산다. */
    /* 🔴 락업(도장+워드마크 가로)을 걷었다 — **임팩트가 떨어졌다**(유호 09-02 눈검증).
     * 까닭 둘: ㉠가로로 나란히 놓으니 도장의 «점»이 «띠»로 늘어져 시선을 한 곳에 못 잡는다
     * ㉡락업에 맞추려 도장을 150→92 로 줄인 것이 그 힘을 더 깎았다.
     * ⇒ 유호님 제안대로 **도장은 위에서 크게 · 이름은 아래에 작게**. 세로 축 하나에
     *   강(도장) → 중(문안) → 약(이름) 이 흐르고, 이름도 잃지 않는다. */
    도장: true,
    도장크기: 150,
    아래로고: true,
    아래로고크기: 104,
    큰크기: 100,
    작은크기: 42,
    큰줄: 'Солонгос хэл — тоглоом шиг',
    /* 09-02 «두 단어» 규칙 — 둘째 줄(「Улаанбаатар · Тун удахгүй нээгдэнэ」)은 페이지 소개글이 진다. 커버는 한 줄. */
    작은줄: '',
    확정: 'synklab_커버',
  },

  /* ══ ⑦⑧ 링크드인 개인 프로필 배경 — @yuhobuilds (유호 지시 2026-09-06) ═════════════
   * ③ 유튜브 @yuhobuilds 배너와 **한 사람 축**이다 — 같은 밤 천 · 같은 이음 손땀 ·
   * 같은 워드마크(단색 k). 링크드인만 갈리는 것은 셋:
   *
   * ① **판이 아주 낮고 넓다**(4:1). 유튜브 16:9, 페북 커버 2.75:1 다음으로 가장 납작하다.
   * ② **큰 꺾쇠 워터마크를 끈다.** ⑥ 페북 커버에서 배운 그대로다 — 판이 낮을수록 큰 조각이
   *   «천의 무늬»가 되기 전에 «화면을 먹는 덩어리»가 된다. 4:1 은 그 병이 더 심하다.
   * ③ **문안이 가운데가 아니다**(밀기 190 = 폭의 62% 자리). 얼굴 사진이 왼쪽 아래를
   *   덮으므로, 가운데에 두면 «실제로 보이는 화면»에서 왼쪽으로 쏠린다. 캔버스만 보면
   *   치우쳐 보이는 것이 맞다 — 판정 자는 캔버스가 아니라 프로필 화면이다(페북 커버의 교훈).
   *
   * 🔑 문안이 **영어**인 까닭 — 링크드인 헤드라인이 이미 영어다(실행지 §3 「방법의 한 줄」).
   *   여기 관객은 한국·몽골의 일하는 사람들이고, 한 지면에서 언어가 갈리면 얼굴이 흔들린다.
   *
   * 🔴 **첫 판이 «평평한 검은 사각»이 됐다**(09-06 눈검증). ③ 배너의 기본 빛(키 0.045)을 그대로
   *   썼는데, ③은 큰 꺾쇠가 화면을 채워서 그 어둠이 «천»으로 읽혔다. 여기는 꺾쇠를 껐으니
   *   같은 어둠이 «빈 사각»이 된다 — 덜어낸 판은 빛이 형태를 대신 잡아야 한다.
   *   ⇒ 키라이트를 0.045 → 0.11 로 올리고 **광원을 문안 축(62%)으로 옮겼다.** 천 결이 살아나고
   *     글자가 천에서 떠오른다. 비네트도 함께 깊여(0.42 → 0.5) 4:1 의 넓은 좌우를 잠근다. */
  링크드인: {
    규격: '링크드인',
    이름: '⑦ 링크드인 · 워드마크만 — @yuhobuilds (샤넬식)',
    설명: '④ synkbrief 와 같은 판정 — 배너는 얼굴이지 설명서가 아니다. 말은 헤드라인이 진다 · 업로드본 = yuhobuilds_링크드인.png',
    바탕: 색['Ink Deep'],
    글자: 색['Paper'],
    보조: 색['Stone'],
    로고: () => 워드마크({ 판: '다크', 표현: '펠트', 색갈래: '단색' }),
    천: { 보풀: 색['Deep Wool'], 그늘: '#000000', 어둡나: true },
    워터마크: false,
    이음: true,
    빛: 링크드인빛,
    큰줄: '',
    작은줄: '',
  },

  링크드인한줄: {
    규격: '링크드인',
    이름: '⑧ 링크드인 · 워드마크 + 한 줄 — @yuhobuilds',
    설명: '⑦과 구조가 같고 아래에 영어 한 줄이 붙는다. 「어디서 무엇을」이 얼굴에 같이 선다',
    바탕: 색['Ink Deep'],
    글자: 색['Paper'],
    보조: 색['Stone'],
    로고: () => 워드마크({ 판: '다크', 표현: '펠트', 색갈래: '단색' }),
    천: { 보풀: 색['Deep Wool'], 그늘: '#000000', 어둡나: true },
    워터마크: false,
    이음: true,
    빛: 링크드인빛,
    /* ⑫ 기본 시제 「짓고 있습니다」의 영어 판. 장소를 말하는 까닭은 그것이 이 사람의 유일한
     * 대체 불가 조건이라서다(「서울에서 5,000km」와 같은 자리). 숫자·급수·날짜는 안 쓴다. */
    큰줄: '',
    작은줄: 'Building in Mongolia',
    작은크기: 68,
  },

  /* ⑨ = ⑧과 **한 글자만** 다르다 — 워드마크의 k 가 코랄이다.
   * 🔑 까닭은 ④ synkbrief 가 이미 세운 규칙이다: 「밤 판에서 코랄은 큰 면이 아니라 한 점으로만
   *   산다」. 이 판은 꺾쇠도 몽글도 없는 «가장 빈» 판이라, 그 한 점이 없으면 화면에 색이 0 이 된다.
   * ⚠ 대신 ③ 유튜브 @yuhobuilds 배너(단색 k)와 k 하나가 갈린다 — 유호님이 고르실 자리다. */
  링크드인코랄: {
    규격: '링크드인',
    이름: '⑨ 링크드인 · 코랄 k + 한 줄 — ✅ 확정 (유호 09-06) · @yuhobuilds',
    설명: '⑧과 한 글자만 다르다(k 가 코랄). 어두운 판에 색이 한 점 선다 · 업로드본 = yuhobuilds_링크드인.png',
    바탕: 색['Ink Deep'],
    글자: 색['Paper'],
    보조: 색['Stone'],
    로고: () => 워드마크({ 판: '다크', 표현: '펠트', 색갈래: '코랄' }),
    천: { 보풀: 색['Deep Wool'], 그늘: '#000000', 어둡나: true },
    워터마크: false,
    이음: true,
    빛: 링크드인빛,
    큰줄: '',
    작은줄: 'Building in Mongolia',
    작은크기: 68,
    확정: 'yuhobuilds_링크드인',
  },
};

/** 몽글 누끼 — 경로는 `lib/마스코트자산.js` 한 창구에서만 온다(마스코트 10벌 공존의 교훈).
 *  기본 표정은 «눈웃음» — 채널 첫 화면은 인사하는 얼굴이다(프로필굽기와 같은 판정).
 *
 *  🔑 **표정을 안이 고를 수 있게 열었다**(09-02). 까닭: 페이스북 페이지는 프로필 사진과 커버를
 *    **한 화면에 나란히** 띄운다. 둘 다 «눈웃음»이면 같은 얼굴이 두 번 나와 게을러 보인다.
 *    ⇒ 프로필은 눈웃음(인사), 커버는 **눈감음**(조용한 만족) — 한 캐릭터의 «다른 순간»이라
 *    브랜드에 살아있음이 생긴다.
 *  ⚠ **`본체`·`놀람` 은 눈이 유리 구슬이다** — 09-01 교정(「구 형태 은퇴」)에 걸릴 수 있어
 *    내가 고르지 않는다. 자수 눈인 `눈웃음`·`눈감음` 둘만 쓴다.
 *
 *  🔑 **액자 실측(09-01 · 알파 bbox)** — 몽글 누끼는 1024² 캔버스에 그림이 폭 57.3% · 높이 55.8%
 *    로 앉아 있고 **아래 여백이 22.0%** 다. 그래서 「발밑을 어디에 놓을까」는 요소의 bottom 이
 *    아니라 `bottom = 목표 − 폭×0.220` 으로 셈해야 맞는다(그냥 bottom:0 을 주면 122px 뜬다).
 *    ⚠ 같은 액자를 까몽에 기대면 안 된다 — 까몽 컷들은 폭 91.2% · 아래 여백 5.7% 로 규약이
 *    아예 다르다(트랙 §0-라디오 「액자 규약 통일」이 그 미결이다). */
/** 까몽 누끼 — 액자 규약이 몽글과 **아예 다르다**(09-01 알파 bbox 실측):
 *   · 몽글: 폭 57.3% · 아래 여백 22.0%
 *   · 까몽: 폭 91.2% · 아래 여백  5.7%
 *  그래서 같은 캔버스 px 을 주면 까몽이 **1.6배 커 보이고 발이 땅에 안 붙는다.**
 *  ⇒ 「보이는 그림 폭」을 맞춰 캔버스를 되계산하고, 발밑도 제 여백으로 되뺀다.
 *
 * 🔴 **밝은 커버 천에 세워 보고 내렸다 — 두 번째다** (09-02 · 유호 「까몽이를 써도 돼」).
 *   실제로 구워 눈으로 보니 걸린 것이 **셋**이고, 그 중 하나는 09-01 때 몰랐던 것이다:
 *   ① 🔑 **까몽 컷 여덟이 전부 «위에서 내려다본» 액자다** — 표정만 다르고 자세는 하나다.
 *      몽글은 정면으로 «선다». 같은 이음 솔기 위에 놓으면 **하나는 서 있고 하나는 뒹군다.**
 *      크기 셈을 아무리 맞춰도 이건 안 맞는다 — 액자 규약이 크기뿐 아니라 «자세»에도 갈린다.
 *   ② 09-01 판정이 여전히 참이다 — 밝은 천 위 검은 털이 시선을 문안에서 뺏는다.
 *   ③ **읽는 순서가 깨진다** — 커버는 문구가 먼저인데 왼쪽 아래 까몽이 먼저 잡힌다.
 *
 *   ⇒ **코드는 남긴다**(다시 시험할 때 바로 쓰라고). 켜는 법 = 안에 `까몽: '눈웃음'`.
 *   ⚠ 다음에 켜기 «전에» 둘 중 하나가 먼저다: ㉠**밤 바탕 판**을 만든다(까몽은 밤에 산다) 또는
 *     ㉡**까몽의 «서 있는» 컷을 굽는다**(트랙 §0-라디오 「액자 규약 통일」이 그 미결이다).
 *     그 둘 없이 그냥 켜면 09-01·09-02 와 같은 자리로 세 번째 돌아온다. */
function 까몽svg(표정) {
  const b64 = fs.readFileSync(마스코트자산.까몽경로(표정 || '눈웃음', { 누끼: true })).toString('base64');
  return `<img src="data:image/png;base64,${b64}" alt="" data-synk-kkamong>`;
}

function 몽글svg(표정) {
  const b64 = fs.readFileSync(마스코트자산.절대경로(표정 || '눈웃음', { 누끼: true })).toString('base64');
  return `<img src="data:image/png;base64,${b64}" alt="" data-synk-mascot>`;
}

/** 한 안의 자립형 HTML. `안내선` 이 참이면 안전 영역을 그린다(검수용 · 업로드본에는 안 넣는다).
 *
 * 층 순서(아래→위) — 실물 천 위에 실물 조각을 얹는 순서 그대로다:
 *   바닥색 → 양모 천 결 → 조명(키라이트 + 비네트) → 펠트 아플리케 → 이음 손땀 → 글·로고 → 안내선.
 * 조명의 광원은 50% 42% — 정중앙보다 «조금 위». 천은 늘 위에서 빛을 받는다. */
function 지면(안, 안내선) {
  규격적용(안);
  const a = 안들[안];
  const 천 = a.천 ? 양모천svg(a.천) : '';
  const 투명 = a.워터투명 == null ? 1 : a.워터투명;
  const 워터 = a.워터마크
    ? `<div class="워터" style="opacity:${투명}">${잉크펠트꺾쇠(Math.round(H * (a.워터배 || 1)), a.워터실 || 실.잉크)}</div>` : '';
  const 몽글 = a.몽글 ? `<div class="디제이 몽글">${몽글svg(a.몽글표정)}</div>` : '';
  const 까몽 = a.까몽 ? `<div class="디제이 까몽">${까몽svg(a.까몽)}</div>` : '';
  const 이음 = a.이음 ? 이음선svg(Math.round(H * 이음비), a.천 && !a.천.어둡나 ? 색['Ash Wool'] : 색['Deep Wool'], 0.3) : '';
  /* 🔑 빈 문자열 = «글자 없음»(유호 확정 09-02 «두 단어» 규칙 — 명품 배너 여덟 곳 실측: 글자 0~2단어 · 히어로 하나).
   *   `||` 로 두면 빈 줄이 기본 문안으로 «되살아나» 워드마크만 두는 판을 만들 수 없다 — null 만 기본으로 떨어진다. */
  const 큰 = a.큰줄 != null ? a.큰줄 : 큰줄;
  const 작은 = a.작은줄 != null ? a.작은줄 : 작은줄;
  const 선 = 안내선 ? `<div class="안전선"><span>안전 영역 ${안전W}×${안전H} — 여기 밖은 기기에 따라 잘린다</span></div>` : '';
  const 어둡 = a.천 ? a.천.어둡나 : true;
  /* ══ 빛 — 평평한 그림과 «무대»를 가르는 것 ═════════════════════════════════════
   * 배너 셋은 08-28 에 유호님 눈을 통과한 값이라 **손대지 않는다**. 아래는 전부
   * 「안이 주면 그 값, 안 주면 기존값」이라 기존 그림은 한 픽셀도 안 바뀐다. */
  const 빛 = a.빛 || {};
  const 키형 = 빛.키형 || '58% 74% at 50% 42%';
  const 비형 = 빛.비형 || '118% 148% at 50% 46%';
  const 키알 = 빛.키 != null ? 빛.키 : (어둡 ? 0.045 : 0.5);
  const 비알 = 빛.비 != null ? 빛.비 : (어둡 ? 0.42 : 0.13);
  const 키색 = 어둡 ? `rgba(251,247,240,${키알})` : `rgba(255,255,255,${키알})`;
  const 비색 = 어둡 ? `rgba(0,0,0,${비알})` : `rgba(43,35,32,${비알})`;
  /* 가이드 그림자 — 한 층이면 «떠 있고», 두 층이면 «놓인다».
   * 접촉(짧고 진한) + 확산(길고 옅은) = 실물 사진에서 늘 같이 오는 짝이다. */
  const 캐그림자 = a.그림자 || `drop-shadow(0 ${재기(18)}px ${재기(26)}px rgba(43,35,32,0.22))`;
  const 바닥 = a.바닥띠 ? `<div class="바닥"></div>` : '';
  return `<!doctype html><meta charset="utf-8">
<style>
  /* 낫표 「 」 교정 — 값의 정본 = docs/디자인_토큰.json 「서체.낫표교정」(유호 확정 08-31). 스택보다 먼저 선다. */
  @font-face{font-family:'SYNK Bracket';src:local('Malgun Gothic'),local('Apple SD Gothic Neo'),local('Noto Sans KR'),local('Noto Sans CJK KR');unicode-range:U+300C-300D;}
  html,body{margin:0;padding:0;background:${a.바탕};}
  .판{width:${W}px;height:${H}px;position:relative;overflow:hidden;background:${a.바탕};
    display:grid;place-items:center;
    font-family:'SYNK Bracket','Inter Tight','SUIT Variable',system-ui,'Malgun Gothic',sans-serif;}
  .천{position:absolute;inset:0;width:100%;height:100%;z-index:0;display:block;}
  /* 바깥 영역은 «분위기»만 — 글자도 로고도 두지 않는다. 키라이트가 위, 비네트가 가장자리. */
  .결{position:absolute;inset:0;z-index:1;
    background:
      radial-gradient(ellipse ${키형}, ${키색} 0%, transparent 62%),
      radial-gradient(ellipse ${비형}, transparent 0%, transparent 52%, ${비색} 100%);}
  /* 바닥 — 이음 솔기 아래를 아주 옅게 눌러 «지면»을 만든다. 선 하나만으로는 땅이 얇다. */
  .바닥{position:absolute;left:0;right:0;bottom:0;z-index:1;height:${Math.round(H * (1 - 이음비) + 30)}px;
    background:linear-gradient(to bottom, transparent 0%, rgba(43,35,32,0.035) 55%, rgba(43,35,32,0.075) 100%);}
  .워터{position:absolute;left:${a.워터x || '50%'};top:${a.워터y || '50%'};transform:translate(-50%,-50%);z-index:2;
    display:grid;place-items:center;
    filter:drop-shadow(0 26px 40px rgba(0,0,0,0.5));}
  .워터 svg{display:block;}
  /* 몽글 — «안전 영역 밖»의 덤. 오른쪽 여백은 (2560−1235)/2 = 662px 이고 그 안에서만 논다.
   * 발밑은 이음 솔기(H×0.875 + 선 오프셋 30 = 1290px)에 맞춘다: 누끼 캔버스의 아래 여백이
   * 폭의 22.0%(알파 bbox 실측 09-01)라 bottom = 150 − 560×0.220 ≈ 27.
   *
   * 🔴 **까몽을 왼쪽에 세우는 판을 지었다가 걷었다**(09-01 눈검증) — 라디오 DJ 가 몽글↔까몽
   *   교대라 둘이 함께 서면 좋겠다고 봤는데, **까몽은 검은 털 캐릭터라 밝은 양모 천 위에서
   *   «얼룩»이 된다.** 코랄 몽글은 같은 천에 부드럽게 앉는데 까몽만 대비가 튀어 시선을
   *   문안에서 뺏는다. ⇒ 밝은 판에 세울 가이드는 몽글 하나다(까몽은 밤 판에서 산다).
   *   ⚠ 다시 시도하려면 그 전에 «밤 바탕 판»을 만들어야지, 이 판에 넣는 것이 아니다. */
  .디제이{position:absolute;bottom:0;z-index:2;filter:${캐그림자};}
  .디제이 img{width:100%;height:auto;display:block;}
  /* 발밑 = 이음 솔기(H×0.875 + 선 오프셋 30). 누끼 캔버스의 아래 여백이 폭의 22.0%(알파 bbox
   * 실측 09-01)라 bottom 은 그만큼 되빼야 «땅에 선다» — 그냥 0 을 주면 뜬다. */
  .몽글{right:${몽글우}px;bottom:${Math.round(H - (H * 이음비 + 30) - 몽글폭 * 0.220)}px;width:${몽글폭}px;}
  /* 까몽 — 몽글과 «보이는 그림 폭»을 같게 맞춘 캔버스(몽글폭×0.573÷0.912)에, 발밑은 제 여백 5.7%로. */
  .까몽{left:${몽글우}px;bottom:${Math.round(H - (H * 이음비 + 30) - (몽글폭 * 0.573 / 0.912 * (a.까몽배 || 1)) * 0.057) + (a.까몽올림 || 0)}px;width:${Math.round(몽글폭 * 0.573 / 0.912 * (a.까몽배 || 1))}px;}
  .이음{position:absolute;left:0;width:100%;height:60px;z-index:2;display:block;}
  /* 안전 영역 — 모든 글자가 여기 «안»에 있다.
   * 🔴 overflow:hidden 이 이 파일의 안전장치다. 첫 판은 내용(로고 212 + 큰줄 109 + 작은줄 55 + 간격 68
   *    = 444px)이 338 을 넘겼는데, flex 는 넘친 것을 «바깥으로 밀어» 조용히 안전 영역 밖에 세웠다
   *    — 큰 화면에서는 멀쩡해 보이고 폰에서만 잘린다(08-28 실측 · 안내선 판에서 잡았다).
   *    이제 넘치면 그 자리에서 «잘려» 보이므로 눈검증이 반드시 잡는다.
   *    아래 크기는 합이 316px 로 22px 여유를 둔 값이다 — 문안을 늘리면 이 셈을 다시 한다. */
  /* 🔑 «밀기» — 문안 덩어리를 가로로 미는 유일한 자리(링크드인 전용 · 다른 규격은 0 이라 한 픽셀도
   *   안 바뀐다). 까닭은 미학이 아니라 «얼굴 사진»이다: 링크드인은 배경의 왼쪽 아래를 프로필
   *   사진이 덮으므로, 가운데에 두면 실제 화면에서 왼쪽으로 쏠려 보인다(위 규격 주석의 실측). */
  .속{position:relative;z-index:3;width:${안전W}px;height:${안전H}px;overflow:hidden;
    transform:translate(${밀기}px,-${올림}px);
    display:flex;flex-direction:column;align-items:center;justify-content:center;gap:${재기(22)}px;text-align:center;}
  /* 로고·글자가 천 위에 «앉는다» — 광원이 위(42%)니 그림자는 전부 아래로.
   * 글자의 이중 그림자 = 자수의 깊이: 아래로 부드러운 접촉 + 윗모서리에 실낱 하이라이트.
   * 폰트는 안 건드린다(킷 3규칙) — 이것은 활자가 아니라 «빛»의 층이다. */
  /* 락업 — 도장(기호)과 워드마크(이름)를 한 덩어리로. 「기호는 안에 들어온 자리, 이름은 보여주는
   * 순간」이라는 두 층이 **처음 오는 사람 앞에서는 동시에 필요하다** — 기호는 아는 사람에게만 신호다. */
  /* 아래 서명 — 도장은 위에서 «점»으로 강하게, 이름은 아래에서 작게. 세로 축 하나에
   * 강→중→약 이 흐른다. 락업(가로로 나란히)은 도장의 «점»을 띠로 늘여 임팩트를 깎았다. */
  .아래로고{width:${재기(a.아래로고크기 || 104)}px;margin-top:${재기(20)}px;opacity:.88;
    filter:drop-shadow(0 ${재기(4)}px ${재기(7)}px rgba(43,35,32,0.16));}
  .아래로고 svg{width:100%;height:auto;display:block;}
  .락업{display:flex;align-items:center;gap:${재기(30)}px;}
  .락업 .로고{width:${재기(150)}px;}
  .도장{display:grid;place-items:center;filter:drop-shadow(0 ${재기(6)}px ${재기(10)}px rgba(43,35,32,0.20));}
  .도장 svg{display:block;}
  /* 글자가 0 인 판(워드마크만 · 샤넬식)은 로고가 곧 히어로라 190 → 320 으로 키운다 — 안전 영역 338 높이 안이다. */
  .로고{width:${재기((큰 || 작은) ? 190 : 320)}px;filter:drop-shadow(0 ${재기(7)}px ${재기(13)}px rgba(0,0,0,${어둡 ? '0.55' : '0.18'}));}
  .로고 svg{width:100%;height:auto;display:block;}
  .큰{font-size:${재기(a.큰크기 || 80)}px;font-weight:800;letter-spacing:-0.035em;line-height:1.14;color:${a.글자};margin:0;
    text-shadow:0 3px 5px rgba(0,0,0,${어둡 ? '0.5' : '0.14'})${어둡 ? ', 0 -1px 0 rgba(251,247,240,0.07)' : ''};}
  .작은{font-size:${재기(a.작은크기 || 36)}px;font-weight:500;letter-spacing:-0.01em;color:${a.보조};margin:0;
    text-shadow:0 2px 3px rgba(0,0,0,${어둡 ? '0.4' : '0.1'});}
  .안전선{position:absolute;left:50%;top:50%;transform:translate(calc(-50% + ${밀기}px),calc(-50% - ${올림}px));z-index:9;
    width:${안전W}px;height:${안전H}px;box-sizing:border-box;
    border:3px dashed rgba(249,104,89,.85);border-radius:8px;}
  .안전선 span{position:absolute;left:0;top:-46px;font-size:26px;color:#F96859;white-space:nowrap;}
</style>
<div class="판">
  <!-- 이음(솔기)이 워터(아플리케)보다 먼저다 — 실물이라면 조각을 솔기 «위에» 꿰맨다. -->
  ${천}<div class="결"></div>${바닥}${이음}${워터}${몽글}${까몽}${선}
  <div class="속">
    ${a.락업
      ? `<div class="락업"><div class="도장">${도장({ px: 재기(a.도장크기 || 150) })}</div><div class="로고">${a.로고()}</div></div>`
      : (a.도장 ? `<div class="도장">${도장({ px: 재기(a.도장크기 || 150) })}</div>` : (a.로고없이 ? '' : `<div class="로고">${a.로고()}</div>`))}
    ${큰 ? `<p class="큰">${큰}</p>` : ''}
    ${작은 ? `<p class="작은">${작은}</p>` : ''}
    ${a.아래로고 ? `<div class="아래로고">${a.로고()}</div>` : ''}
  </div>
</div>`;
}

function 크롬() {
  const 후보 = ['C:/Program Files/Google/Chrome/Application/chrome.exe',
    'C:/Program Files (x86)/Google/Chrome/Application/chrome.exe'].filter(fs.existsSync);
  if (!후보.length) throw new Error('크롬을 못 찾았다 — 후보 2곳에 없다');
  return 후보[0];
}

function 굽기(안, 안내선) {
  /* 🔑 규격을 «여기서» 잡아 결과에 실어 보낸다 — 전역 W·H 는 다음 안을 구울 때 갈리므로,
   *   안을 다 구운 «뒤» 도는 시안()이 전역을 읽으면 지면 전체가 «마지막에 구운 안»의
   *   규격으로 그려진다(09-06 실사고: 유튜브 배너가 링크드인 4:1 로 찌그러졌다). */
  const 규격 = 규격적용(안);
  const 이름 = 안내선 ? `${안}_안내선` : 안;
  const src = path.join(낼곳, `_${이름}.html`);
  const png = path.join(낼곳, `${이름}.png`);
  fs.writeFileSync(src, 지면(안, 안내선), 'utf8');
  const r = spawnSync(크롬(), ['--headless=new', '--disable-gpu', '--force-device-scale-factor=1',
    '--hide-scrollbars', `--window-size=${W},${H}`, `--screenshot=${png}`,
    'file:///' + src.replace(/\\/g, '/')], { encoding: 'utf8' });
  if (!fs.existsSync(png)) throw new Error(`굽기 실패: ${이름}\n${r.stderr || ''}`);
  fs.unlinkSync(src);
  const 바이트 = fs.statSync(png).size;
  /* 6MB 는 유튜브가 거절하는 선이다 — 넘으면 조용히 두지 않고 그 자리에서 말한다. */
  const 넘음 = 바이트 > 6 * 1024 * 1024;
  return { 안: 이름, png, 바이트, 넘음, 규격 };
}

/** 시안 — 판정 자리. data URI 는 «한 번만» 적는다(프로필 시안이 13.7MB 로 부은 그 함정). */
function 시안(굽힌것) {
  /* 🔴 배너는 data URI 로 «안 품는다» — 프로필 시안과 반대 판정이다(08-28).
   *   섬유 노이즈는 PNG 압축이 안 먹혀 한 벌이 4MB 대이고, base64 ×1.33 ×3벌 = 13.2MB 지면이
   *   git 이력에 판마다 쌓인다(실측). 시안은 «옆 파일»을 상대 경로로 본다 — 후보 PNG 는
   *   같은 폴더의 굽기 산출물이라 시안을 여는 기계에는 늘 있고(없으면 `node tools/배너굽기.js` 한 번),
   *   git 이 쥐는 것은 확정본 하나뿐이다. */
  const 키 = {};
  굽힌것.forEach(({ 안 }, i) => { 키[안] = `--b${i + 1}`; });
  const 변수 = 굽힌것.map(({ 안, png }) =>
    `    ${키[안]}:url("./${path.basename(png)}");`).join('\n');

  /* 🔴 **비율은 «칸마다» 다르다** — 유튜브 16:9 · 페북 커버 2.75:1 · 링크드인 4:1.
   *   그래서 지면 CSS 에 한 벌을 박지 않고, 칸이 제 규격을 CSS 변수로 이고 간다.
   *   `--안전배` = 안전 영역만 보여 줄 때 그림을 몇 %로 키우나(W ÷ 안전W). */
  const 칸 = 굽힌것.map(({ 안, 바이트, 넘음, 규격 }) => {
    const a = 안들[안] || { 이름: 안, 설명: '' };
    const v = `background-image:var(${키[안]})`;
    const 자 = `--가로:${규격.W};--세로:${규격.H};--안전가로:${규격.안전W};--안전세로:${규격.안전H};`
      + `--안전배:${(규격.W / 규격.안전W * 100).toFixed(2)}%`;
    return `<section class="안" style="${자}">
  <h2>${a.이름}</h2>
  <p class="설명">${a.설명}</p>
  <div class="띠줄"><span class="라벨">전체 ${규격.W}×${규격.H} (큰 화면)</span><i class="배너" style="${v}"></i></div>
  <div class="띠줄"><span class="라벨">안전 영역만 ${규격.안전W}×${규격.안전H} (어디서도 안 잘리는 부분)</span>
    <i class="잘린띠" style="${v}"></i></div>
  <p class="파일"><code>docs/SHIFT/배너/${안}.png</code> · ${(바이트 / 1048576).toFixed(2)}MB
    ${넘음 ? '<b class="빨강">🔴 6MB 초과 — 유튜브가 거절한다</b>' : '<span class="초록">✅ 6MB 이하</span>'}</p>
</section>`;
  }).join('\n');

  /* 규격 줄은 `규격들` 이 만든다 — 숫자를 문안에 또 적으면 규격을 갈 때 한쪽만 낡는다
   *   (페북 커버를 1640×856 → 596 으로 간 09-02 가 그 자리였다). */
  const 규격줄 = Object.entries(규격들).map(([이름, g]) =>
    `<b>${이름}</b> ${g.W}×${g.H} (${(g.W / g.H).toFixed(2)} : 1) · 안 잘리는 곳 ${g.안전W}×${g.안전H}`).join('<br>');

  return `<!doctype html>
<html lang="ko"><head><meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>SYNK 소셜 대문 시안 — 유튜브 배너 · 페북 커버 · 링크드인 배경</title>
${브랜드폰트.블록()}
<style>
  /* 낫표 「 」 교정 — 값의 정본 = docs/디자인_토큰.json 「서체.낫표교정」(유호 확정 08-31). 스택보다 먼저 선다. */
  @font-face{font-family:'SYNK Bracket';src:local('Malgun Gothic'),local('Apple SD Gothic Neo'),local('Noto Sans KR'),local('Noto Sans CJK KR');unicode-range:U+300C-300D;}
  :root{--paper:${색['Paper']};--inkdeep:${색['Ink Deep']};--coral:${색['Coral']};
    --stone:${색['Stone']};--ash:${색['Ash Wool']};
${변수}
  }
  body{margin:0;padding:40px 24px 72px;background:var(--inkdeep);color:var(--paper);
    font-family:'SYNK Bracket','Inter Tight','SUIT Variable',system-ui,'Malgun Gothic',sans-serif;}
  .지면{max-width:1000px;margin:0 auto;}
  h1{font-size:1.9rem;font-weight:800;letter-spacing:-.03em;margin:0 0 8px;}
  .머리{color:var(--stone);font-size:.95rem;line-height:1.7;margin:0 0 36px;}
  .머리 b{color:var(--paper);}
  .안{padding:24px;margin:0 0 20px;border-radius:18px;
    background:rgba(228,228,231,.045);box-shadow:inset 0 0 0 1px rgba(228,228,231,.12);}
  .안 h2{font-size:1.05rem;font-weight:800;margin:0 0 4px;}
  .설명{color:var(--stone);font-size:.88rem;margin:0 0 18px;}
  .띠줄{margin:0 0 16px;}
  .라벨{display:block;font-size:.76rem;color:var(--ash);margin:0 0 6px;}
  /* 🔴 비율은 칸이 «CSS 변수»로 이고 온다 — 여기 숫자를 박으면 «마지막에 구운 안»의 규격으로
   *   지면 전체가 그려져, 유튜브 배너가 4:1 로 찌그러지고 「안전 영역만」이 엉뚱한 자리를 키운다.
   *   이 지면의 존재 이유가 「폰에서 잘리는 것을 미리 본다」이므로 그 판정이 통째로 거짓이 된다. */
  .배너{display:block;width:100%;aspect-ratio:var(--가로)/var(--세로);border-radius:10px;
    background-size:cover;background-position:center;}
  /* 안전 영역만 보여 주는 띠 — 배너를 확대해 그 규격의 안전 영역만 창에 남긴다. */
  .잘린띠{display:block;width:100%;aspect-ratio:var(--안전가로)/var(--안전세로);border-radius:10px;
    background-size:var(--안전배) auto;background-position:center;
    box-shadow:inset 0 0 0 2px rgba(249,104,89,.5);}
  .파일{margin:14px 0 0;font-size:.78rem;color:var(--ash);}
  .빨강{color:#FD9C87;} .초록{color:var(--stone);}
  code{font-family:'DM Mono',ui-monospace,Consolas,monospace;font-size:.92em;}
  .꼬리{margin-top:32px;color:var(--stone);font-size:.86rem;line-height:1.75;}
  .꼬리 b{color:var(--paper);}
</style></head>
<body><div class="지면">
<h1>SYNK 소셜 대문 시안 — ${굽힌것.length}안</h1>
<p class="머리"><b>이 지면에는 규격이 셋 섞여 있습니다</b> — 칸마다 제 규격으로 그리니 <b>칸끼리 모양이 다른 것이 정상</b>입니다:<br>
${규격줄}<br>
유튜브 규격은 2026-08-28 에 고객센터 원문으로, 페북 커버와 링크드인 배경은 09-02·09-06 에 실물 화면을 자로 재서 정했습니다. <b>배너와 채널아트는 같은 것입니다</b> — 유튜브가 옛 이름(채널 아트)을 버리고 지금은 「배너 이미지」 하나로 부릅니다. 올리는 파일도 하나입니다. 파일 크기는 6MB 이하여야 합니다.<br>
🔴 <b>판정은 아래 «안전 영역만» 줄로 하십시오</b> — 폰에서 보이는 것이 그것입니다.
다시 구우려면 <code>node tools/배너굽기.js</code>. ⚠ 이 지면은 옆 폴더의 후보 PNG 를 «상대 경로»로 봅니다 — 그림이 비어 보이면 그 명령 한 번이면 다시 채워집니다.</p>
${칸}
<p class="꼬리">
<b>바깥 영역에는 글자도 로고도 두지 않았습니다</b> — 큰 화면에서만 보이는 자리라 거기에 뜻을 실으면 폰에서 그 뜻이 사라집니다. 대신 «분위기»(가장자리로 갈수록 어두워지는 결)만 넣었고, ③은 꺾쇠를 크게 눕혀 큰 화면의 덤으로 뒀습니다.<br>
<b>업로드 주기는 안 적었습니다</b> — 아직 확정이 아니고(첫 3편 실측 뒤), 배너에 박으면 못 지키는 날 그것이 거짓말이 됩니다. 같은 이유로 「곧 나옵니다」류와 숫자도 뺐습니다(공개선 ⑫·⑪).
</p>
</div></body></html>`;
}

function main() {
  const argv = process.argv.slice(2);
  const 하나 = argv.includes('--안') ? argv[argv.indexOf('--안') + 1] : null;
  const 안내선 = argv.includes('--안내선');
  if (하나 && !안들[하나]) throw new Error(`모르는 안: ${하나} (있는 것: ${Object.keys(안들).join(' · ')})`);

  fs.mkdirSync(낼곳, { recursive: true });
  const 목록 = 하나 ? [하나] : Object.keys(안들);
  const 굽힌것 = [];
  for (const 안 of 목록) {
    const r = 굽기(안, false);
    console.log(`${r.넘음 ? '🔴' : '✅'} ${안.padEnd(8)} ${W}×${H} · ${(r.바이트 / 1048576).toFixed(2)}MB${r.넘음 ? ' — 6MB 초과!' : ''}`);
    굽힌것.push(r);
    if (안내선) {
      const g = 굽기(안, true);
      console.log(`   ↳ 검수용 안내선 판 · ${path.relative(ROOT, g.png)}`);
    }
  }

  /* 확정된 안은 «업로드본» 이름으로 한 벌 더 — 프로필굽기와 같은 규약.
   * 이 한 벌만 git 이 쥔다(후보·안내선 판은 .gitignore · 시안이 품는다). */
  /* 🔴 여기는 `find` 였다 — 확정이 «하나뿐»이라는 가정이 코드에 박혀 있었고,
   *    채널이 셋이 되자 그 가정이 조용히 두 벌을 버렸다(첫 벌만 복사되고 나머지는 후보로 남는다).
   *    확정은 채널 수만큼 있다 ⇒ 전부 복사한다. */
  const 확정들 = 굽힌것.filter(({ 안 }) => 안들[안] && 안들[안].확정);
  for (const c of 확정들) {
    const 업로드본 = path.join(낼곳, `${안들[c.안].확정}.png`);
    fs.copyFileSync(c.png, 업로드본);
    console.log(`🏷  확정 = ${path.relative(ROOT, 업로드본)} (← ${c.안})`);
  }

  if (!하나) {
    const s = path.join(낼곳, '시안.html');
    fs.writeFileSync(s, 시안(굽힌것), 'utf8');
    console.log(`📄 시안 = ${path.relative(ROOT, s)} — «안전 영역만» 줄로 고르십시오`);
  }
  console.log(`\n[배너굽기] ${굽힌것.length}벌 · ${path.relative(ROOT, 낼곳)}`);
}

/* 형제 도구가 «천»과 «아플리케»를 빌려 간다(`tools/라디오배경굽기.js`) — 같은 재질을 두 곳이
 * 각자 조립하면 그 자리에서 결이 갈린다. CLI 로 부를 때만 main 이 돈다. */
module.exports = { 양모천svg, 잉크펠트꺾쇠, 실, W, H, 안전W, 안전H };
if (require.main === module) main();
