#!/usr/bin/env node
/* board-move — 세션보드의 완료 줄을 아카이브로 **잃지 않고** 옮긴다.
 *
 * 왜 있나 (마찰 F046, 2026-08-04 실사고):
 *   손으로 짠 이관 스크립트가 한 번에 두 일을 했다 — ⓐ보드에서 줄 삭제(splice)
 *   ⓑ아카이브에 삽입(`replace('\n---\n', …)`). 그런데 아카이브가 **CRLF**라 앵커
 *   `\n---\n`가 실제 텍스트 `\r\n---\r\n`과 안 맞아 **ⓑ가 조용히 no-op**됐고,
 *   ⓐ만 성공해 줄이 통째로 사라졌다. 발각 단서는 `git status`에 아카이브가
 *   **안 뜬 것** 하나뿐이었다(성공했다면 2파일이 떠야 한다).
 *
 *   ⚠ 이 실패는 **훅으로 못 막는다.** 도구가 Edit이 아니라 Bash+fs.writeFileSync라
 *   PreToolUse(board-guard 계열)가 애초에 발화하지 않는다. 그래서 「막는 가드」가 아니라
 *   **잘못 쓸 수 없는 공용 통로**로 옮긴다(CLAUDE.md 신뢰성 「3번째」 조항).
 *
 * 설계 원칙 3:
 *   ① **줄끝은 실측한다** — 파일마다 따로. 앵커 문자열을 손으로 쓰지 않는다.
 *   ② **쓰기 전에 메모리에서 검증한다** — 옮길 줄이 새 아카이브에 있고 새 보드에 없으며,
 *      행 수가 각각 ±1인지. 하나라도 어긋나면 **아무것도 쓰지 않는다.**
 *   ③ **아카이브를 먼저 쓰고, 다시 읽어 확인한 뒤에 보드에서 지운다.**
 *      순서가 핵심이다 — 중간에 죽으면 결과는 「양쪽에 다 있음」(눈에 보이고 무해)이지
 *      「어디에도 없음」(조용하고 영구)이 아니다. 유실 대신 중복으로 실패하게 만든다.
 *   ④ **③을 파일 층에서 git 층으로 올린다**(마찰 F102, 2026-08-05 실사고).
 *      ③의 「유실 대신 중복」 보장은 **단일 세션 전제**였다. 이 저장소의 보장 경계는
 *      파일시스템이 아니라 **커밋**이라, 두 파일이 미커밋이면 보드 삭제만 **남의 커밋에
 *      실려 나가고**(F073) 아카이브 추가는 내 미커밋에 남아 — 파일시스템은 중복인데
 *      git 은 유실이다. 그래서 순서를 이렇게 못박는다:
 *        아카이브 쓰기 → **아카이브만 단독 커밋** → 확인 → 그 **뒤에야** 보드에서 지운다.
 *      보드 삭제를 작업본에 쓰는 것 자체가 위험한 행위다(그 순간부터 남이 실어갈 수 있다).
 *   ⑤ **보드는 깨끗할 때만 커밋한다.** `세션보드.md` 는 상시 여러 세션이 더럽히고
 *      (실측 08-05 01:04: 한 순간에 6개 세션) 자동 커밋이 곧 **남의 선언 수거**가 된다.
 *      안 해도 안전하다 — 아카이브가 이미 커밋됐으니 최악은 눈에 보이는 중복이다.
 *      아카이브가 더러우면 아예 **아무것도 쓰지 않고** 멈춘다(줄은 보드에 그대로).
 *   ⑥ **줄 주인이 살아 있으면 옮기지 않는다**(마찰 F146, 2026-08-07 실사고).
 *      ①~⑤는 전부 「옮기는 행위가 줄을 잃지 않는가」였다. ⑥은 앞의 질문이다 —
 *      **지금 옮겨도 되는 줄인가.** 「✅종결」은 **그 세션이 끝났다는 뜻이 아니다**:
 *      종결로 적힌 줄을 보드 정리로 옮겼는데 그 트랙 파일 3건을 **0분 전에 편집 중인**
 *      세션이 있었다(되돌림 revert 2건). 줄을 잃은 세션은 다음 턴에 자기 트랙을 못 찾고
 *      「트랙 없음」으로 접힌다(F144) — 아카이브에 멀쩡히 있어도 인계문은 보드만 본다.
 *      판정 재료는 줄에 적힌 **커밋 해시**뿐이다 — 「만지는 파일」 칸은 산문이라 못 믿는다.
 *
 * 사용:
 *   node tools/board-move.js "friction.js 채번 락"     # 이관 + 커밋(아카이브 먼저)
 *   node tools/board-move.js "..." --dry               # 계획만 보고 쓰지 않는다
 *
 *   ⛔ `--no-commit` 같은 탈출구는 두지 않는다 — 그 구멍이 F102 자체다.
 */
'use strict';
const fs = require('fs');
const path = require('path');
const { spawnSync } = require('child_process');
const 보드id = require(path.join(__dirname, '..', '.claude', 'hooks', 'lib', 'board-id.js'));

/* `SYNK_BOARD_ROOT` 는 `tools/board.js` 와 **같은 이름의 이음매**다 — 후보 찾기(아래 BOARD)를
 * 픽스처에서 재려면 이게 필요하다. `SYNK_BOARD` 는 파일을 곧장 주므로 후보 찾기를 건너뛴다:
 * 그 손잡이로만 시험하면 정작 「유령을 후보로 보는가」가 회귀에서 도달 불가가 된다. */
const ROOT = process.env.SYNK_BOARD_ROOT || path.resolve(__dirname, '..');
const ARCHIVE = process.env.SYNK_BOARD_ARCHIVE || path.join(ROOT, 'docs', '세션보드_아카이브.md');
const 보드lib = require(path.join(__dirname, 'lib', '보드.js'));
// 표 칸 가르기는 이 한 통로만 쓴다 — 날 `split('|')` 은 백틱 안 파이프에서 칸을 밀어낸다(F119).
const 표 = require(path.join(__dirname, 'lib', '표.js'));
/* 근거 후보 수집 — 원칙⑦ 거절이 「무엇을 열어야 하나」까지 내민다 (#Q93 ㉡ · 2026-08-16).
 * 여기서만 부른다: 이 거절문이 사람이 그 판단 앞에 **실제로 서 있는** 유일한 자리다. */
const 보드근거 = require(path.join(__dirname, 'lib', '보드근거.js'));

/* 「이 보드 **파일**의 주인은 누구인가」는 이 파일 안에서 **한 곳**에서만 판정한다 —
 * 원칙 ⑥(산 주인)과 원칙 ⑦(미완 이관)이 각자 적으면 갈라지고, 갈라진 쪽의 증상은 「통과」다.
 * `보드id.파일지문` 을 안 쓰는 이유는 그쪽이 `유물-` 접두를 「주인 없음」으로 떨어뜨리기 때문이다
 * — 수거된 세션 파일이야말로 이 두 원칙이 가장 필요한 자리다(F252 축). */
const 파일의주인지문 = (p) => (/^(?:유물-)?([0-9a-f]{8})$/i.exec(path.basename(p, '.md')) || [])[1] || '';

/** 줄끝을 실측한다 — 섞여 있으면 많은 쪽을 따른다(앵커를 손으로 쓰지 않는 이유). */
function eolOf(text) {
  const crlf = (text.match(/\r\n/g) || []).length;
  const lf = (text.match(/(^|[^\r])\n/g) || []).length;
  return crlf >= lf && crlf > 0 ? '\r\n' : '\n';
}
/* 「표 줄인가」는 `tools/lib/보드.js` 하나에서 온다 — 여기 다시 적었던 판이 조립기와 갈려
 * 유령 줄(규격 밖이라 표에 안 뜨는 줄)을 낳았다(F322·F330·F331 · 사연은 그 파일 머리말). */
const isRow = 보드lib.표줄;
const rowsIn = (text) => text.split(/\r?\n/).filter(isRow);

/* 「줄을 뺀 뒤 남은 게 상투구뿐인가」도 `tools/lib/보드.js` 하나에서 온다 — 같은 판정을
 * **이미 쌓인 것**에도 쓰는 `tools/보드껍데기정리.js` 가 있어서다(사연·판정 근거는 그 파일 머리말). */
const 껍데기인가 = 보드lib.껍데기인가;
/* 종료 코드는 **계약**이다: 0 옮겼다 · 1 못 옮겼다 · 6 원칙⑥(줄 주인이 아직 살아 있다) ·
 * 7 잔재를 닫았다(F572 — 요청한 줄을 «옮긴» 게 아니라 앞선 실행의 미커밋을 담았다).
 * 6 을 따로 판 이유(F278 · 2026-08-09): board-guard 가 「이 처방이 실제로 도는가」를 `--dry` 로
 * 물어보는데, 1 하나로는 **「board-move 가 거절했다」와 「board-move 가 아예 못 돌았다」**(아카이브
 * 없음·저장소 아님·경로 어긋남)를 못 가른다. 못 가르면 환경 고장이 「옮길 게 없다」로 읽혀
 * 상한이 조용히 풀린다 — 새는 방향이다. 글자(stderr 문구)로 가르지 않는 이유는 그게 곧
 * 두 번째 정의라서다(문구를 다듬는 순간 판정이 갈라진다). */
const die = (msg, code) => { console.error('[board-move] ' + msg); process.exit(code || 1); };
/* 7 = **잔재를 닫았다**(F572 · 아래 `잔재닫기`). 0 과 가르는 이유는 0 이 「요청한 그 줄을 옮겼다」는
 * 뜻이고 이쪽은 아니기 때문이다 — 뭉치면 호출자(`보드수거`)가 안 옮긴 줄을 「옮김」으로 센다. */
const 잔재닫음 = 7;

/* git 통로는 **needle 조회보다 위**에 둔다 — 아래 「못 찾았다」 자리에서 이미 git 이 필요하다(F572).
 * `root`·`rel`·`inRepo` 는 BOARD 가 정해진 뒤에만 뜻이 있어 그 자리에 그대로 남는다. */
const git = (args, cwd) => spawnSync('git', ['-c', 'core.quotePath=false', ...args], { cwd, encoding: 'utf8' });
const topOf = (p) => { const r = git(['rev-parse', '--show-toplevel'], path.dirname(p)); return r.status === 0 ? r.stdout.trim() : null; };

/** 다중집합 차집합 — 같은 줄이 두 번 있는 경우를 세면서 뺀다. 한 곳에만 둔다: 아래 `아카이브차분`
 *  과 `잔재닫기` 가 **같은 재료**로 「무엇이 더해졌나」를 봐야 한다(두 번 재면 갈라진다). */
const 행빼기 = (a, b) => {
  const m = new Map();
  for (const l of b) m.set(l, (m.get(l) || 0) + 1);
  return a.filter((l) => { const n = m.get(l) || 0; return n ? (m.set(l, n - 1), false) : true; });
};

const args = process.argv.slice(2);
const dry = args.includes('--dry');
/* `--미완확인 "사유"` = 원칙 ⑦(아래)의 유일한 탈출구. 사유를 **말로** 받는 이유는 F103 이다 —
 * 따를 수 없는 처방은 우회를 정상 통로로 만드니, 거절이 내미는 처방은 실행 가능해야 한다.
 *
 * 🔴 **주석만 그렇게 적혀 있고 구현이 안 따라왔다** (F407 · 2026-08-13 신고 · 여기서 닫는다).
 *   옛 판은 `includes('--미완확인')` 로 boolean 만 읽고, needle 을 「`--dry`·`--미완확인` 을 뺀
 *   **나머지 전부**」로 조립했다. 그래서 거절문이 시키는 대로 근거를 붙이면 그 근거까지 검색
 *   문구가 되어 **「줄을 못 찾았다」로 튕긴다** — 성공하는 유일한 통로가 근거를 버리는 쪽이었고,
 *   그래서 근거는 어디에도 안 남았다. 처방을 따르면 실패하고 어기면 되는 자리가 정확히 F103 이다.
 * 🔑 사유는 **필수**로 받는다(빈 문자열이면 거절). 선택이면 아무도 안 적고, 그러면 이 탈출구는
 *   「죽었으니 옮긴다」와 글자 하나 다르지 않다 — 원칙 ⑦ 이 막으려던 그 번역(F397)으로 되돌아간다.
 * ⚠ 정상 마감 경로(`/close` 의 `board-move "문구"`)는 이 깃발을 안 쓴다 — 여기 요구를 세워도
 *   도는 세션들의 마감 절차는 한 바이트도 안 흔들린다(F407 보류 사유였던 그 걱정의 실측 답). */
const 미완확인idx = args.findIndex((a) => a === '--미완확인' || a.startsWith('--미완확인='));
const 미완확인 = 미완확인idx !== -1;
/* 사유가 차지한 자리를 **인덱스로** 집어 needle 에서 뺀다 — 값으로 빼면 사유와 같은 글자가
 * 문구에 들어 있을 때 문구 쪽이 함께 지워진다(그 방향은 「못 찾았다」라 시끄럽지만, 조용히
 * 다른 줄을 집는 변형도 만들 수 있다). */
const 사유자리 = new Set();
let 미완사유 = '';
if (미완확인) {
  사유자리.add(미완확인idx);
  const 그것 = args[미완확인idx];
  if (그것.startsWith('--미완확인=')) {
    미완사유 = 그것.slice('--미완확인='.length).trim();
  } else if (미완확인idx + 1 < args.length && !args[미완확인idx + 1].startsWith('--')) {
    미완사유 = args[미완확인idx + 1].trim();
    사유자리.add(미완확인idx + 1);
  }
}
const needle = args.filter((a, i) => !사유자리.has(i) && a !== '--dry').join(' ').trim();
/* 이관 커밋 **본문**에 사유를 남기는 자리. 아카이브 «줄»이 아니라 커밋 메시지인 이유:
 * 옮기는 줄은 보드 원문과 바이트가 같아야 한다 — `headHas(ARCHIVE, row)` 착지 검사와
 * `내잔재뿐()` 의 「추가 1줄·삭제 0」 대조가 그 동일성 위에 서 있다. 커밋 본문은 그 불변식
 * 밖이면서 git 이력에 영구히 남는 자리다(「보존은 최신판+git 이력」).
 * ⚠ 제목은 안 건드린다 — 낡음 대조·수거 도구가 제목 꼴을 읽는다. */
const 사유본문 = () => (미완사유 ? ['', `미완확인 사유(원칙 ⑦ 탈출구 · F407): ${미완사유}`] : []);
if (!needle) die('옮길 줄을 식별할 문구를 달라. 예: node tools/board-move.js "채번 락"');
/* 사유 없는 `--미완확인` 은 거절한다 — 위 🔑. 처방을 **그대로 복사해 돌릴 수 있는 모양**으로
 * 내민다(따옴표까지): 여기서 「사유를 적어라」라고만 하면 그게 또 못 따를 처방이다(F103). */
if (미완확인 && !미완사유) {
  die('`--미완확인` 에는 **근거를 말로** 달아야 한다(F407) — 붙이지 않으면 「주인이 죽었으니 옮긴다」와\n' +
    '  같아지고, 그건 원칙 ⑦ 이 막으려던 번역이다(F397: 미완이 완료의 얼굴로 굳는다).\n' +
    '  **아무것도 쓰지 않았다.**\n' +
    `  ▶ 그대로 복사해 돌려라(무엇을 **직접 열어** 끝났다고 봤는지 적는다):\n` +
    `       node tools/board-move.js "${needle}" --미완확인 "커밋 <해시> 열어 잔여 0 확인"\n` +
    '  사유는 이관 커밋 메시지에 남는다 — 그게 다음 세션이 이 판정을 되짚는 유일한 재료다.');
}

/* ── 잔재 닫기 — 「옮김은 끝났고 커밋만 안 됐다」 (F572 · 2026-08-17 실사고) ──────────
 *
 * 원칙⑤ 거절문이 내미는 F433 처방(「잔재 줄부터 마저 옮겨라」)이 **실물로 튕겼다.** 이 파일이
 * 스스로 「남는 사각」이라 적어둔 판이 실재했다: 잔재를 남긴 세션이 **보드 삭제까지 작업본에
 * 쓰고** 죽으면 그 줄은 보드에 없어서 needle 이 못 찾는다. 그러면 유일한 출구가 못 따를 처방이
 * 되고, 잠기는 것은 신고자 한 명이 아니라 **보드 정리 전체**다(그날 완료 줄 5건 · 3시간).
 *
 * 🔑 **문을 하나 더 달지 않는다** — 처방이 가리키는 그 명령 자신이 이 상태를 알아보게 한다.
 *   `--잔재닫기` 같은 손잡이는 머리말이 이미 기각한 모양이고(「탈출구가 곧 우회다」), 무엇보다
 *   **아는 사람만 쓸 수 있는 출구는 없는 것과 같다**(F433 이 그 교훈으로 태어났다).
 *
 * 🔑 게이트는 **주인의 생사가 아니라 차분의 «모양»**이다. 여기서 재는 것은 「이 커밋이 남의
 *   것을 실어가나」이고, 그건 diff 를 보면 끝난다 — 원칙⑥(산 주인)은 «지금 옮겨도 되는 줄인가»를
 *   묻는 다른 질문이고, 이 자리엔 옮길 줄이 아예 없다(이미 옮겨져 있다). 모양 검사가 생사보다
 *   **더 세다**: 죽은 세션도 보드 파일에 딴 편집을 남길 수 있고, 그걸 실어가면 그게 F073 이다.
 *
 * ⚠ **틀릴 때의 모습**: 게이트가 새면 남의 보드 편집이 내 커밋에 실린다. 그래서 셋 다 요구한다 —
 *   ①아카이브에서 **빠진** 행 0(장부유실 방향이면 손을 안 댄다) ②needle 이 더해진 행 **정확히
 *   1줄**에 걸린다 ③함께 담을 보드 파일의 차분이 **그 행들의 삭제뿐**(추가 0·다른 삭제 0).
 *   하나라도 어긋나면 아무것도 쓰지 않고 무엇이 걸렸는지 이름을 댄다.
 * ⚠ 아카이브를 커밋하면 그 파일의 **더해진 행 전부**가 실린다 — 그래서 ③은 needle 이 걸린 행
 *   하나가 아니라 **더해진 행 전부**에 대해 본다. 「한 줄만 닫는다」는 원리상 불가능하다.
 * ⚠ 보드 줄이 **아직 남아 있는** 잔재 행은 그대로 둔다 — 아카이브만 커밋되고 보드에 줄이 남은
 *   상태는 F102 가 «유실 대신 중복»으로 일부러 고른 쪽이다. 다만 **조용히 두지 않고** 보고한다.
 */
/** @returns {null | {닫을행들:string[], 보드파일들:string[], 남는중복:string[]}} 못 닫을 이유는 `사유` 로. */
function 잔재조사(needle) {
  const r = topOf(ARCHIVE);
  if (!r) return { 사유: '저장소 밖이다 — 잔재를 닫을 층이 없다' };
  const 상대 = (p) => path.relative(r, p).split(path.sep).join('/');
  const head = git(['show', `HEAD:${상대(ARCHIVE)}`], r);
  if (head.status !== 0) return { 사유: '아카이브의 HEAD 판을 못 읽었다' };

  const 있던 = rowsIn(head.stdout).map((l) => l.trim());
  const 지금 = rowsIn(fs.readFileSync(ARCHIVE, 'utf8')).map((l) => l.trim());
  const 더해진 = 행빼기(지금, 있던);
  const 지워진 = 행빼기(있던, 지금);

  // ① 장부에서 빠진 행이 있으면 이건 잔재가 아니라 다른 사고다 — 손을 안 댄다.
  if (지워진.length) return { 사유: `아카이브에서 **빠진** 행이 ${지워진.length}건 있다 — 잔재 닫기가 아니라 장부유실 방향이다` };
  if (!더해진.length) return null;                                  // 잔재가 없다 = 정말 못 찾은 것
  // ② needle 이 더해진 행 하나에 걸려야 한다(0=딴 얘기 · 2+=모호).
  const 걸린것 = 더해진.filter((l) => l.includes(needle));
  if (걸린것.length !== 1) {
    return 걸린것.length === 0 ? null
      : { 사유: `그 문구가 아카이브의 잔재 ${걸린것.length}줄에 걸린다 — 더 구체적인 문구를 달라` };
  }

  // ③ 함께 담을 보드 파일의 차분이 **그 행들의 삭제뿐**인지 본다.
  const 폴더 = 보드lib.폴더(ROOT);
  const 더해진집합 = new Set(더해진);
  const 보드파일들 = []; const 걸림 = [];
  const st = git(['status', '--porcelain', '--', 상대(폴더)], r);
  for (const 줄 of (st.stdout || '').split(/\r?\n/)) {
    const 경로 = 줄.slice(3).trim().replace(/^"|"$/g, '');
    if (!경로 || !/\.md$/i.test(경로)) continue;
    if (줄.startsWith('??')) continue;            // 미추적 보드 파일엔 HEAD 판이 없다 = 삭제가 아니다
    const 절대 = path.join(r, 경로);
    const h = git(['show', `HEAD:${경로}`], r);
    if (h.status !== 0) continue;
    const 그전 = rowsIn(h.stdout).map((l) => l.trim());
    const 지금줄 = fs.existsSync(절대) ? rowsIn(fs.readFileSync(절대, 'utf8')).map((l) => l.trim()) : [];
    const 뺀것 = 행빼기(그전, 지금줄);
    const 넣은것 = 행빼기(지금줄, 그전);
    if (!뺀것.length && !넣은것.length) continue;  // 표 줄은 안 바뀌었다(머리말 등만 바뀜) — 안 담는다
    if (넣은것.length || 뺀것.some((l) => !더해진집합.has(l))) {
      걸림.push(`${경로} — 추가 ${넣은것.length}줄 · 잔재 아닌 삭제 ${뺀것.filter((l) => !더해진집합.has(l)).length}줄`);
      continue;
    }
    보드파일들.push(경로);
  }
  if (걸림.length) {
    return { 사유: '보드 파일에 **잔재 삭제가 아닌** 변경이 섞여 있다 — 담으면 남의 편집을 실어간다:\n       ' + 걸림.join('\n       ') };
  }

  /* 보드 줄이 아직 남아 있는 잔재 행 — 담긴 보드 파일들이 지운 것 말고 나머지다. */
  const 지워진행 = new Set(보드파일들.flatMap((경로) => {
    const h = git(['show', `HEAD:${경로}`], r);
    const 그전 = rowsIn(h.stdout).map((l) => l.trim());
    const 절대 = path.join(r, 경로);
    const 지금줄 = fs.existsSync(절대) ? rowsIn(fs.readFileSync(절대, 'utf8')).map((l) => l.trim()) : [];
    return 행빼기(그전, 지금줄);
  }));
  return {
    닫을행들: 더해진,
    보드파일들,
    남는중복: 더해진.filter((l) => !지워진행.has(l)),
    뿌리: r,
    상대,
  };
}

/** 「보드에서 못 찾았다」의 **단 하나의 자리**. 두 곳(파일 고르기 · 그 파일 안 검색)에서 같은
 *  판정을 내리므로 문구도 잔재 처리도 여기서만 적는다 — 나뉘면 한쪽만 잔재를 알아본다. */
function 못찾음(needle) {
  잔재닫기(needle);                                 // 닫았으면 여기서 끝난다(exit 7)
  die(`보드에서 "${needle}" 가 든 줄을 못 찾았다.`);
}

/** 잔재를 닫는다. 닫았으면 `process.exit` 로 끝내고, 못 닫으면 **돌아온다**(호출부가 원래 거절문을 낸다). */
function 잔재닫기(needle) {
  const j = 잔재조사(needle);
  if (!j) return;                                  // 잔재 없음 = 조용히 원래 거절문으로
  if (j.사유) {
    die(`보드에서 "${needle}" 가 든 줄을 못 찾았다 — 아카이브 잔재도 닫을 수 없다:\n` +
      `  ${j.사유}\n  **아무것도 쓰지 않았다.**`);
  }
  const 목록 = (행들) => 행들.map((l) => '       · ' + l.slice(0, 110)).join('\n');
  console.log(`[board-move] 그 줄은 **이미 아카이브에 옮겨져 있다** — 남은 것은 «커밋»이다(F572).`);
  console.log(`  닫을 잔재 ${j.닫을행들.length}줄 · 함께 담을 보드 파일 ${j.보드파일들.length}개`);
  console.log(목록(j.닫을행들));
  for (const p of j.보드파일들) console.log(`       ↳ 보드 삭제 동봉: ${p}`);
  if (j.남는중복.length) {
    console.log(`  ⚠ 보드 줄이 **아직 남아 있는** 잔재 ${j.남는중복.length}줄 — 아카이브·보드 양쪽에 보이게 남는다(F102 가 유실 대신 고른 쪽):`);
    console.log(목록(j.남는중복));
  }
  if (dry) { console.log('[board-move] --dry — 쓰지 않았다.'); process.exit(0); }

  const c = git(['commit',
    '-m', `docs: 중단된 board-move 의 원자 커밋을 닫는다 — 잔재 ${j.닫을행들.length}줄 (F572)\n\n` +
      '옮김은 작업본에서 이미 끝났고 커밋만 안 된 상태였다(주인이 커밋 전에 죽었다).\n' +
      '이 커밋이 실어가는 것은 아카이브의 더해진 행과 그 행들의 보드 삭제뿐이다 —\n' +
      '차분 모양으로 못박았다: 아카이브 삭제 0 · 보드 추가 0 · 잔재 아닌 보드 삭제 0.',
    '--', j.상대(ARCHIVE), ...j.보드파일들], j.뿌리);
  if (c.status !== 0) {
    die('잔재를 담는 커밋이 실패했다 — 아무것도 안 바뀌었다(작업본은 그대로다):\n  git: ' +
      ((c.stderr || '') + (c.stdout || '')).trim().split(/\r?\n/).slice(0, 4).join(' / '));
  }
  /* 착지 확인 — 「커밋됐나」는 작업본이 아니라 커밋된 내용으로 판정한다(F071 축). */
  const 안착 = git(['show', `HEAD:${j.상대(ARCHIVE)}`], j.뿌리);
  const 담긴것 = 안착.status === 0 ? rowsIn(안착.stdout).map((l) => l.trim()) : [];
  const 빠진것 = j.닫을행들.filter((l) => !담긴것.includes(l));
  if (빠진것.length) {
    die(`커밋은 됐는데 아카이브의 HEAD 판에 ${빠진것.length}줄이 없다 — 이 도구를 먼저 고쳐라:\n` + 목록(빠진것));
  }
  console.log(`[board-move] 완료 — 잔재 ${j.닫을행들.length}줄을 **한 커밋**으로 닫았다(F572 · F102 순서 유지).`);
  process.exit(잔재닫음);
}

/* 보드 정본은 `docs/_ops/보드/<지문>.md` 의 세션별 파일이다(F250) — 옮길 줄이 **어느 파일**에
 * 있는지부터 가른다. 아래 원칙 ①~⑥(줄끝 실측·아카이브 먼저·산주인 검사·깨끗할 때만 커밋)은
 * 그 파일 하나를 대상으로 그대로 돈다 — 파일이 좁아진 만큼 남의 줄을 건드릴 길이 사라졌다.
 * `SYNK_BOARD` 는 테스트 격리 이음매라 그대로 둔다(주면 그 파일 하나만 본다). */
const BOARD = process.env.SYNK_BOARD || (() => {
  /* 🔑 **유령 줄도 후보에 넣는다** (F322·F330·F331 · 2026-08-12). 옛 판은 `줄들`(엄격)만 봤고,
   * 그래서 규격 밖 줄은 이 도구가 **원리상 못 치웠다** — 그런데 그 줄이야말로 치워야 하는 것이다:
   * 표에 안 뜨니 주인 말고는 아무도 모르고, 주인이 죽으면 영원히 남는다. board-guard 가 만석일 때
   * 내미는 처방이 이 명령이라, 여기서 못 찾으면 처방이 못 따를 처방이 된다(F103).
   * 실측: `fe43f4b4.md:5`(✅종결·주인 사망)이 그 상태로 남아 있었다. */
  const 후보 = [...(보드lib.줄들(ROOT) || []), ...(보드lib.유령들(ROOT) || [])]
    .filter((r) => r.줄.includes(needle));
  if (!후보.length) 못찾음(needle);
  const 파일들 = [...new Set(후보.map((r) => r.파일))];
  if (파일들.length > 1) {
    die(`"${needle}" 가 세션 파일 ${파일들.length}개에 걸린다 — 더 구체적인 문구를 달라:\n  ` + 파일들.join('\n  '));
  }
  return path.join(보드lib.폴더(ROOT), 파일들[0]);
})();

const boardText = fs.readFileSync(BOARD, 'utf8');
const archiveText = fs.readFileSync(ARCHIVE, 'utf8');
const boardEol = eolOf(boardText);
const archiveEol = eolOf(archiveText);

const boardLines = boardText.split(/\r?\n/);
const hits = boardLines.map((l, i) => ({ l, i })).filter((x) => isRow(x.l) && x.l.includes(needle));
if (!hits.length) 못찾음(needle);
if (hits.length > 1) die(`"${needle}" 가 ${hits.length}줄에 걸린다 — 더 구체적인 문구를 달라:\n  ` +
  hits.map((h) => h.l.slice(0, 80)).join('\n  '));

const row = hits[0].l.replace(/\s+$/, '');

// ── 새 내용을 메모리에서 만든다 ────────────────────────────────────
const newBoard = boardLines.filter((_, i) => i !== hits[0].i).join(boardEol);

// 삽입 자리 = 첫 `---` 구분선 **다음**(아카이브 맨 위). 앵커는 실측한 줄끝으로 조립한다.
const archiveLines = archiveText.split(/\r?\n/);
const sep = archiveLines.findIndex((l) => /^\s*---\s*$/.test(l));
if (sep === -1) die('아카이브에서 `---` 구분선을 못 찾았다 — 구조가 바뀌었으면 이 도구를 먼저 고쳐라.');
const insertAt = archiveLines[sep + 1] === '' ? sep + 2 : sep + 1;
/* 🔴 F226 — **앞선 실행이 남긴 잔재를 다시 삽입하지 않는다.**
 * ①이 아카이브를 쓴 뒤 ②(단독 커밋)가 남의 `.git/index.lock` 에 막혀 죽으면 삽입만 된 채 남는다.
 * 그 상태로 재실행하면 아래 「행 수 +1」 검증은 **중복도 통과시킨다**(같은 줄이 두 번 실린다). */
const 이미있음 = archiveLines.some((l) => l.trim() === row.trim());
const newArchiveLines = archiveLines.slice();
if (!이미있음) newArchiveLines.splice(insertAt, 0, row);
const newArchive = newArchiveLines.join(archiveEol);

// ── 쓰기 전 검증 — 하나라도 어긋나면 아무것도 쓰지 않는다 ──────────
const problems = [];
if (!newArchive.includes(row)) problems.push('새 아카이브에 그 줄이 없다(삽입 실패 — F046이 바로 이것)');
if (newBoard.includes(row)) problems.push('새 보드에 그 줄이 남아 있다(삭제 실패)');
if (rowsIn(newArchive).length !== rowsIn(archiveText).length + (이미있음 ? 0 : 1)) {
  problems.push(`아카이브 행 수가 ${이미있음 ? '그대로가' : '+1이'} 아니다`);
}
if (rowsIn(newBoard).length !== rowsIn(boardText).length - 1) problems.push('보드 행 수가 -1이 아니다');
if (problems.length) die('검증 실패 — 아무것도 쓰지 않았다:\n- ' + problems.join('\n- '));

console.log(`  옮길 줄: ${row.slice(0, 90)}${row.length > 90 ? '…' : ''}`);
/* 유령을 치울 때는 **유령이었다고 말한다** — 조용히 옮기면 「보드에 있던 줄을 옮겼다」로 읽히고,
 * 규격 밖이었다는 사실(=다음 세션이 같은 모양을 베낄 위험)이 아무 데도 안 남는다.
 * 원문 그대로 옮긴다 — 아카이브는 역사라, 여기서 모양을 고치면 옛 기록을 개서하는 것이다. */
const 옮길유령사유 = 보드lib.유령사유(row);
if (옮길유령사유.length) {
  console.log('  ⚠ 이 줄은 **유령**이었다 — 표에는 안 뜨는데 파일엔 있던 줄이다(F322·F330·F331):');
  옮길유령사유.forEach((s) => console.log(`      ↳ ${s}`));
  console.log('    아카이브에는 **원문 그대로** 넣는다(역사를 개서하지 않는다).');
}
if (이미있음) console.log('  ↻ 아카이브에 그 줄이 **이미 있다**(앞선 실행의 잔재) — 다시 삽입하지 않는다(F226).');
console.log(`  보드 ${JSON.stringify(boardEol)} · 아카이브 ${JSON.stringify(archiveEol)} (실측)`);

/* ── git 자리를 **쓰기 전에** 잰다 ────────────────────────────────
 * 쓴 뒤에 재면 「내가 만든 더러움」과 「원래 있던 남의 미커밋」이 구분되지 않는다. */
/* ⚠ `core.quotePath=false` 없으면 git 이 한글 경로를 8진 이스케이프(`"\354\204\270…"`)로 뱉는다.
 * 이 저장소는 경로가 전부 한글이라, 빼면 아래 경고가 **읽을 수 없는 채로** 뜬다(F045 계열: 재는 층이 값을 깨뜨린다). */
/* `git`·`topOf` 는 위(needle 조회보다 앞)로 올렸다 — 그 자리에서 이미 필요하다(F572). */
const root = topOf(BOARD);
const inRepo = !!root && root === topOf(ARCHIVE);
const rel = (p) => path.relative(root, p).split(path.sep).join('/');
const dirtyOf = (p) => (git(['status', '--porcelain', '--', rel(p)], root).stdout || '').trim();
/** 「커밋됐나」는 작업본이 아니라 **커밋된 내용**으로 판정한다(F071: 조용한 no-op 이 난다). */
const headHas = (p, s) => { const r = git(['show', `HEAD:${rel(p)}`], root); return r.status === 0 && r.stdout.includes(s); };
const track = (row.match(/\*\*(.+?)\*\*/) || [null, needle])[1];
/** 경로를 **못 박아** 커밋한다. 여러 개를 받는 이유는 아카이브·보드를 한 커밋에 담기 위해서다
 *  (아래 「원자 커밋」 절) — `git add` 를 따로 하지 않는 것은 그 틈에 남의 스테이징이 얹히기 때문이다. */
const commit = (ps, subject, body) =>
  git(['commit', '-m', `${subject}\n\n${body}`, '--', ...[].concat(ps).map(rel)], root);
/** 그 경로를 git 이 아는가 — 안 지워 본 파일에만 의미가 있으니 **지우기 전에** 부른다. */
const 추적중 = (p) => git(['ls-files', '--error-unmatch', '--', rel(p)], root).status === 0;

/* ── 📤 「닿았나」 — 트랙을 닫는 순간에만 물어볼 수 있는 것 (F440) ─────────────
 *
 * 🔴 왜 여기인가: 2026-08-14 유호 신고 「자개 캐러셀이 아직 안온것같아」. 산출물 HTML 2벌이
 *   커밋되고 보드에 ✅ 로 적혔는데 반출도 직송도 0이라 유호님 화면엔 아무것도 안 갔다.
 *   커밋·렌더린트 초록·보드 ✅ 는 전부 «만들었다»만 증명한다 — «닿았다» 를 재는 자리가 **0**이었다.
 *   그리고 그것을 물어볼 수 있는 유일한 시점이 **트랙을 끝났다고 선언하는 이 자리**다:
 *   그 전엔 아직 만드는 중이라 안 닿은 게 정상이고, 그 뒤엔 줄이 아카이브로 내려가 아무도 안 본다.
 *
 * ⚠ **막지 않는다 · 이관을 되돌리지 않는다.** 알리기만 한다 — 반출은 유호님 화면을 바꾸는 일이라
 *   기계가 대신 누를 자리가 아니고, 여기서 막으면 「닫을 수 없는 트랙」이 생겨 우회가 정상 통로가
 *   된다(F103). 처방은 **그대로 붙여 실행되는 한 줄**로 낸다.
 * ⚠ 판정은 `운영자료.반출도달` **하나**가 진다. 여기서 폴더를 직접 훑으면 판정이 두 벌이 되고,
 *   갈라진 쪽의 증상은 언제나 「닿았다」(=조용한 통과)다(가드 맹점 ④).
 * ⚠ 실측 없는 것은 말하지 않는다 — 바탕화면 폴더를 못 찾으면 **❔ 못 쟀다**로 나가고 한 건도
 *   「안 닿았다」고 하지 않는다.
 *
 * 💸 대가(새 장치엔 함께 적는다):
 *   ① **비용** — 바로가기 대상 읽기가 캐시에 걸리면 ~150ms, 캐시가 찬물이면 ~10초다(실측
 *      2026-08-15: 9.8초 → 147ms). 트랙 닫을 때 1회라 감수하되, 찬물인 판에서는 눈에 띈다.
 *   ② **틀릴 때의 모습** — 새는 방향은 **「통과」**다. 보드 파일 칸이 경로를 안 적었거나
 *      (「스크래치패드」·「운영자료 41~43」 처럼 이름만 적은 줄이 실제로 있다) 확장자가 폴더에서
 *      도출한 집합 밖이면 **한 건도 안 뜬다.** 그래서 아래는 **본 경로 수를 분모로 함께** 낸다 —
 *      「0건」과 「안 봤다」가 같은 모양이면 이 장치는 있으나 마나다(F207).
 *   ③ **닫는 것** — 없다. 이 자리에서 닫을 옛 장치가 없다(«닿았나» 를 재던 것이 애초에 0개다).
 *      대신 이 장치는 스스로 조용해진다: 반출하면 다음 실행부터 안 뜬다. */
function 반출알림() {
  if (process.env.SYNK_반출알림 === '0' || !inRepo) return;
  try {
    const 운영자료 = require(path.join(__dirname, '운영자료.js'));
    const { 찾기: 바탕화면 } = require(path.join(__dirname, 'lib', '바탕화면.js'));
    // 뿌리 = 바탕화면 그 자체다(2026-08-17 재편 — 갈래 값이 「SYNK 코어」·「SYNK LAB\운영」을 들고 있다).
    const 폴더 = 바탕화면().경로;

    const 확장자 = 운영자료.화면확장자(폴더);
    if (!확장자) return;   // 폴더를 못 읽었다 = 도출할 정본이 없다. 조용히 둔다(아래 모름 갈래가 받는다)

    const 파일칸 = (표.칸나누기(row)[2] || '');
    const 후보 = 보드lib.산출물경로들(파일칸, 확장자)
      .filter((p) => { try { return fs.statSync(path.join(root, p)).isFile(); } catch { return false; } });
    if (!후보.length) return;   // 이 트랙은 유호님이 여는 산출물을 안 냈다

    const r = 운영자료.반출도달(폴더, 후보, { 뿌리: root });
    if (r.모름) {
      console.log(`  ❔ 「닿았나」는 못 쟀다 — ${r.사유} (안 닿았다는 뜻이 아니다)`);
      return;
    }
    const 안닿음 = r.항목.filter((x) => !x.닿음);
    if (!안닿음.length) {
      console.log(`  📤 유호님 화면 도달 ✅ ${후보.length}/${후보.length} (F440 축)`);
      return;
    }
    console.log(`  📤 **유호님 화면에 안 닿은 산출물 ${안닿음.length}건** — 커밋·보드 ✅ 는 「만들었다」만 증명한다(F440):`);
    안닿음.forEach((x) => console.log(`      · ${x.파일}`));
    console.log(`      본 경로 ${후보.length} = 닿음 ${후보.length - 안닿음.length} + 안닿음 ${안닿음.length}`
      + '  (경로를 안 적은 조각은 원리상 안 세진다 — 0건이 「안 봤다」일 수 있다)');
    console.log(`      보내기: node tools/운영자료.js ${안닿음.map((x) => `"${x.파일}"`).join(' ')} --갈래 운영`);
  } catch (e) {
    // 알림이 이관을 깨뜨리지 않는다 — 그래도 **침묵하지는 않는다**(조용한 실패가 이 파일의 주제다)
    console.log(`  ❔ 「닿았나」를 못 쟀다: ${e.message}`);
  }
}
/* 성공 종료(0)에서만 운다 — `die()` 는 1로 나가고, 이관이 안 된 트랙엔 물어볼 말이 없다.
 * 종료 자리가 셋(저장소 밖·선기록·원자)이라 **한 곳에 묶는다** — 세 곳에 적으면 한쪽만 낡는다. */
process.on('exit', (코드) => { if (코드 === 0) 반출알림(); });

/* ── ⛔ 줄 주인이 살아 있으면 아무것도 하지 않는다 (원칙 ⑥ · F146) ──────
 * 「살아있음」 판정은 **작업본소유자에서 그대로 가져온다** — 여기 다시 적으면 두 도구가
 * 서로 다른 생사를 보게 되고, 갈라진 쪽의 증상은 언제나 「통과」다(F111 이 같은 이유로 그 함수를 쓴다).
 * `--dry` 도 여기를 지난다 — 계획만 볼 때야말로 「옮겨도 되나」의 답이 필요하다.
 *
 * 🔴 재료가 **셋**이고 성질이 다르다 (F165 · F242 · F246). 여기서 새는 방향은 언제나
 *   「통과=남의 줄을 옮김」이므로, 인계문과 달리 **의심되면 멈춘다.** 다만 반대로 틀리면
 *   내 종결 줄이 영원히 안 옮겨져 보드 상한을 먹으므로, 재료를 늘리기 전에 **오염부터 뺀다.**
 *
 *   ① **파일 이름** (F246 · F250 이후). 보드 정본이 세션별 파일이 된 뒤로 「누가 이 줄을
 *      썼나」는 추론할 필요가 없다 — `docs/_ops/보드/<지문>.md` 의 이름이 곧 주인이다.
 *      셋 중 유일하게 **정확**하고, 커밋이 하나도 없는 트랙에도 있다.
 *      🔑 **그래서 ① 이 나를 가리키면 ②③ 은 아예 안 본다**(F288). 좌표가 이미 답을 냈는데
 *         추론을 덧대면 추론만 틀릴 수 있다 — 아래 「좌표가 추론을 이긴다」 절.
 *   ② **줄에 적힌 지문**(`local_302d8acd` · F165). 해시가 없는 트랙(양보·조사·판정안)의
 *      유일한 재료였다. 실측 2026-08-08: 보드·아카이브 333행 중 지문이 **상태 칸 밖에만**
 *      있는 행은 0 이라, 줄 전체를 봐도 오염이 없다 — 그대로 둔다.
 *   ③ **상태 칸의 커밋 해시**(F146·F242) — 줄 전체가 아니라 **상태 칸만** 본다(F246).
 *      줄 전체를 긁으면 원인 설명에 배경으로 인용한 남의 커밋까지 주인으로 센다. 실측:
 *      재료 있는 203행 중 **18행**이 그 형태였고, 처방이 「그 세션이 끝난 뒤 다시 돌려라」라
 *      내 세션에선 실행할 수 없다(F103 축). 작업 커밋이 적히는 자리는 규약상 상태 칸이다.
 *      ⚠ 좁히기는 **미탐 방향**이라 ① 이 그 자리를 메운다 — 옛 단일 파일 시절 행 4개는
 *        작업 해시를 「만지는 파일」 칸에 적었는데, 세션별 파일에서는 이름이 그걸 대신한다. */
/* 🔑 **IIFE 가 아니라 함수인 이유** (F433 · 2026-08-14). 같은 판정이 두 자리에서 필요해졌다 —
 *   ① 옮기려는 그 줄(원칙 ⑥ · 아래 호출) ② **아카이브에 남은 잔재 줄**(맨 아래 F433 절).
 *   ②를 그 자리에 다시 적으면 두 판정이 갈라지고, 갈라진 쪽의 증상은 이 파일에서 언제나 「통과」다.
 * ⚠ `파일지문칸` 은 **인자**다 — 아카이브는 세션별 파일이 아니라 그 좌표가 아예 없다. 보드 파일
 *   이름을 그대로 물려주면 「아카이브 잔재의 주인 = 내 보드 파일 주인」이라는 **거짓**이 선다.
 *   ⚠ 이 한 칸은 **회귀로 못 잰다**(변이를 심어도 안 빨개진다 — 실측). 원칙 ⑥ 이 먼저 돌아서,
 *   보드 파일 주인이 살아 있으면 아래 잔재 검사에 **도달하기 전에** exit 6 으로 끝나기 때문이다.
 *   즉 두 인자가 갈리는 조합이 원리상 도달 불가다. 못 재는 것을 잰 척하지 않고 여기 적어 둔다. */
const 산주인들 = (대상행, 파일지문칸) => {
  if (!inRepo) return [];                                    // 저장소 밖 픽스처엔 커밋도 세션도 없다
  // 칸을 못 가르면(형식이 바뀌면) 옛 재료인 줄 전체로 떨어진다 — 폴백은 언제나 더 많이 보는 쪽.
  const 상태칸 = 표.칸나누기(대상행)[3] || 대상행;
  const 해시들 = [...new Set(상태칸.match(/\b[0-9a-f]{7,40}\b/g) || [])];
  const 지문들 = 보드id.줄의지문(대상행);
  // `유물-` 은 수거된 세션 파일의 접두다(그 세션이 살아 있으면 여전히 그 세션 줄이다 · F252 축).
  const 파일지문 = 파일지문칸 || undefined;
  if (!해시들.length && !지문들.length && !파일지문) return [];
  let owner; let store;
  try {
    owner = require('./작업본소유자.js');
    store = require(path.join(__dirname, '..', '.claude', 'hooks', 'lib', 'handoff-store.js'));
  } catch (e) {
    /* 「검사를 못 했다」와 「통과했다」가 같은 모양이면 안 된다 — 조용히 넘기지 않고 말한다. */
    console.error(`  ⚠ 주인 생사 검사를 **못 했다**(${e.code || e.message}) — 원칙 ⑥이 이번엔 안 돌았다.`);
    return [];
  }
  const 나 = store.safeId(process.env.CLAUDE_CODE_HOST_SESSION_ID || '');
  /* `선언살았나` 지 `살았나` 가 아니다 (F304) — 심장박동만 보면 **승계 자리에서 언제나 참**이라
   * board-guard 가 처방하는 「죽은 세션의 줄이면 치운다」가 30분간 원리상 잠긴다. SessionEnd
   * 도장이 찍힌 세션은 여기서 산 주인이 아니다(도장이 없으면 지금까지와 똑같이 보수적이다). */
  const 산 = new Map(owner.세션들(root).filter(owner.선언살았나).map((s) => [s.sid, s]));
  if (!산.size) return [];
  /* 🔑 **좌표가 추론을 이긴다** (F288 · 2026-08-09). 보드 정본이 세션별 파일인 이상
   *   `보드/<내 지문>.md` 안의 줄은 **정의상 내 줄이다** — 남의 트랙은 거기 살 수 없다
   *   (`board-guard` 가 「내 파일에만 쓴다」를 기계로 막는다). 그러므로 이 파일의 상태 칸에
   *   적힌 남의 해시·지문은 소유 근거가 아니라 **인용**이고, ②③ 은 여기서 잴 것이 없다.
   *
   * ■ 무엇이 실제로 벌어졌나 — 세션 규약은 그 인용을 **시킨다**(「조율 사실을 커밋 메시지나
   *   보드에 1줄 남긴다」). 규약대로 남의 적색 커밋을 상태 칸에 적었더니 원칙 ⑥ 이 내 완료
   *   줄을 가뒀고(exit 6 · 「이 줄을 쓴 세션이 아직 살아 있다」), 처방은 「그 세션이 끝난 뒤
   *   다시 돌려라」라 내 세션에선 실행할 수 없다. 막힌 사람에게 남는 출구는 **근거를 지우는
   *   것**뿐이라 정보가 깎인다 — F103 그 모양이다(실제로 그렇게 우회했다).
   *   조건을 하나씩 바꿔 대조하니 물린 것은 **커밋 해시**였다: 해시만 지우자 통과했고,
   *   같은 줄에 남아 있던 지문 문자열은 영향이 0이었다.
   *
   * 🚫 **재제안 금지 — 「주인 표식에서 커밋 해시를 통째로 뺀다」**(F177·F242 가 각각 반박했다).
   *   옛 단일 보드(`세션보드.md`)와 형제 저장소 트랙은 그 재료로 산다. 여기서 끄는 것은
   *   **좌표가 이미 답을 준 파일 안에서만**이고, 좌표가 없는 줄은 ②③ 이 그대로 지킨다.
   * ⚠ **빈 id 가 「전부 내 파일」이 되면 안 된다.** 실측 2026-08-09 — 그것을 실제로 막는 것은
   *   아래 `내지문 &&` 가 **아니라** `safeId('')` = `'unknown'` 이다: 그 폴백은 hex 가 아니라,
   *   hex 8자리만 뽑는 `파일지문` 과 **원리적으로** 같아질 수 없다. 그래서 `내지문 &&` 는 지금
   *   도달 불가능한 벨트다 — 폴백이 언젠가 `''` 나 hex 로 바뀌면 그때 일한다. 그 전제
   *   (폴백 ≠ hex8)를 회귀가 직접 못박는다(`tests/보드이관.test.js` F288 셋째).
   *   🔴 여기 「빈 id 는 내지문이 falsy 라 안전하다」고 적으면 **거짓 근거**가 정본이 된다 —
   *      변이 M3(`|| !내지문`)이 회귀를 하나도 안 빨갛게 만든 것이 그 증거다. */
  const 내지문 = 보드id.지문(나);
  if (내지문 && 파일지문 === 내지문) return [];
  /* 🔴 해시는 **형제 저장소에서도** 찾는다 (F242 · 2026-08-08). 이 저장소에서만 `rev-parse` 하면
   *   talk 커밋 해시가 「해시처럼 생긴 딴 것」과 **같은 모양**(status!==0)이라 조용히 버려진다.
   *   이 프로젝트는 트랙 절반이 talk 작업이라 그 사각은 예외가 아니라 상시다 — 실측 2026-08-08:
   *   보드 「게임 4모듈」 줄은 지문 0 + 해시 전부 talk 이라 산주인 0 이었는데 주인은 25분 전 박동.
   *   F165 가 메운 사각(해시가 아예 없는 줄)의 옆 칸이고, 새는 방향은 여기서도 「통과」다.
   *   목록은 handoff-store 하나에서 파생시킨다(여기 좌표를 다시 적으면 갈라진다). */
  const 저장소들 = [{ 뿌리: root, 저장소: null }, ...store.siblings(root)];
  const 걸린것 = new Map();
  for (const h of 해시들) {
    for (const { 뿌리, 저장소 } of 저장소들) {
      if (git(['rev-parse', '--verify', '--quiet', `${h}^{commit}`], 뿌리).status !== 0) continue;  // 여기 것이 아니다
      const sid = ((git(['log', '-1', '--format=%(trailers:key=Session-Id,valueonly)', h], 뿌리).stdout || '')
        .trim().split(/\r?\n/)[0] || '').trim();
      /* 내 줄은 내가 옮긴다(/close 의 정상 경로) · 트레일러가 없으면 남이라고 단정하지 않는다. */
      if (sid && sid !== 나 && 산.has(sid) && !걸린것.has(sid)) {
        걸린것.set(sid, `${h}${저장소 ? `(${저장소})` : ''} ← ${owner.짧게(sid)}·${산.get(sid).분}분 전`);
      }
      break;                                                  // 이 저장소가 그 해시를 안다 — 더 뒤질 필요 없다
    }
  }
  /* 줄이 스스로 말하는 지문 — 커밋이 아직 하나도 없는 세션의 유일한 재료다. */
  const 산지문 = new Map([...산.values()].map((s) => [보드id.지문(s.sid), s]));
  /* ① 파일 이름 = 주인 (F246). 추론이 아니라 좌표라, 위 둘이 전부 비어도 답이 있다. */
  const 파일주인 = 파일지문 && 산지문.get(파일지문);
  if (파일주인 && 파일지문 !== 보드id.지문(나) && !걸린것.has(파일주인.sid)) {
    걸린것.set(파일주인.sid,
      `이 줄이 사는 파일 \`${path.basename(BOARD)}\` ← ${owner.짧게(파일주인.sid)}·${파일주인.분}분 전`);
  }
  for (const f of 지문들) {
    const s = 산지문.get(f);
    if (!s || 보드id.지문(s.sid) === 보드id.지문(나) || 걸린것.has(s.sid)) continue;
    걸린것.set(s.sid, `줄에 적힌 지문 \`${f}\` ← ${owner.짧게(s.sid)}·${s.분}분 전`);
  }
  return [...걸린것.values()];
};
/* ① 파일 이름 = 좌표(F246). 이 호출만 그 재료를 쓴다 — 그래서 위 `파일주인` 갈래의 문구가
 *   `path.basename(BOARD)` 를 가리켜도 참이다(아카이브 호출은 빈 지문이라 그 갈래에 못 닿는다). */
const 산주인 = 산주인들(row, 파일의주인지문(BOARD));
if (산주인.length) {
  die('이 줄을 쓴 세션이 **아직 살아 있다**(원칙 ⑥ · F146) — 옮기면 그 세션이 다음 턴에 자기 트랙을 잃는다.\n' +
    '  **아무것도 쓰지 않았다** — 줄은 보드에 그대로다.\n' +
    산주인.map((t) => '  · ' + t).join('\n') + '\n' +
    '  「✅종결」은 그 세션이 끝났다는 뜻이 아니다. 그 세션이 끝난 뒤 다시 돌려라.\n' +
    '  내 줄인데 막혔다면 환경변수 CLAUDE_CODE_HOST_SESSION_ID 가 비었는지 본다(비면 내 커밋도 남의 것으로 보인다).',
    6);
}

/* ── ⛔ 원칙 ⑦ — **남의 죽은 줄은 「끝났다」고 적혀 있을 때만 옮긴다** (F397 · 2026-08-13 실사고)
 * 원칙 ⑥ 은 「주인이 살아 있나」만 물었고, 죽었으면 그것으로 통과였다. 그래서 이 도구는
 * **「주인 프로세스가 죽었다」를 「일이 끝났다」로 번역한다** — 커밋 메시지까지 「완료 줄
 * 아카이브 이관」이라 적히므로 미완이 완료의 얼굴로 굳는다.
 *   실측: `18537b29` 가 `0ba9d4da` 의 줄을 옮겼는데 상태 칸은 `▶작업중 — /deploy 진입` 이었고,
 *   그 트랙이 밀고 있던 v9.228(숙제 서클 인쇄물)은 라이브에 한 바이트도 안 나간 상태였다
 *   (`contents_서클.js` 라이브 0). 아카이브로 들어간 미완 트랙은 보드에도 인계문에도 안 뜨니
 *   **다음 세션이 원리상 못 줍는다** — F240(라이브가 조용히 뒤처짐)의 새 유입 경로다.
 * 🔑 축이 F146·F165 와 다르다: 그건 「주인 표식이 없어 못 가른다」이고, 이건 **표식도 상태
 *   칸도 멀쩡한데 도구가 상태를 안 읽는다**. 새는 방향은 여기서도 「통과」다.
 * 🔑 **내 줄에는 안 건다.** 내 트랙의 상태는 내가 안다(`/close` 가 미완인 채 접는 정상 경로가
 *   있고, 거기까지 막으면 처방이 못 따를 처방이 된다). 좌표(파일 이름)가 나를 가리키면 통과.
 * 🔑 파일 이름이 지문이 아니면(옛 단일 보드·픽스처) 이 검사는 **안 돈다** — 주인을 못 가르는데
 *   막으면 미탐이 아니라 오탐이고, 오탐은 이 도구를 안 쓰게 만든다. */
if (!미완확인) {
  const 줄주인 = 파일의주인지문(BOARD);
  const 나의지문 = 보드id.지문(process.env.CLAUDE_CODE_HOST_SESSION_ID || '');
  /* ⚠ **둘 다 있어야** 「남의 것」이라고 말한다. 내 지문을 못 읽는 환경(변수 비었음·픽스처)에서
   *   「모름」을 「남의 것」으로 읽으면 이 도구가 **자기 줄에도 걸려** 통째로 못 쓰게 된다 —
   *   그러면 처방이 못 따를 처방이 되고, 우회가 정상 통로가 된다(F103). 미탐 방향을 택했다. */
  const 남의줄 = !!줄주인 && !!나의지문 && 줄주인 !== 나의지문;
  /* 🔑 「아직 안 끝났나」는 **여기서 다시 적지 않는다** — `tools/lib/보드.js` 의 `활성행` 이
   *   정본이고 `board-guard` 의 완료줄 판정도 같은 곳에서 온다. 그래서 두 성질이 공짜로 붙는다:
   *   ① 판정이 갈릴 자리가 없다(CLAUDE.md 신뢰성 ④) ② **board-guard 의 처방과 원리상 안 겹친다**
   *   — 그쪽은 `!활성행` 인 줄만 처방으로 내미니, 이 거절이 그 처방을 죽일 수 없다(F103·F278). */
  if (남의줄 && 보드lib.활성행(row)) {
    const 파일지문7 = 줄주인;
    const 상태칸7 = 표.칸나누기(row).slice(-1)[0] || row;
    /* 근거 캐기가 비싸서 이 처방이 안 쓰였다(#Q93 1차 실측) — 그 값을 여기서 미리 치른다.
     * 실패해도 거절문은 그대로 나간다: 수집이 죽어서 «처방 자체»가 사라지면 F103 이다. */
    let 근거글7 = '';
    try {
      근거글7 = '\n' + 보드근거.근거글(
        보드근거.근거후보(row, { root: ROOT, 지문: 파일지문7 }), { 들여쓰기: '  ' });
    } catch (e) { 근거글7 = `\n  ▣ 근거 후보 수집이 실패했다(${e.message}) — 손으로 캔다.`; }
    die('이 줄은 **남의 죽은 세션 줄인데 상태 칸이 「끝났다」고 말하지 않는다**(원칙 ⑦ · F397).\n' +
      '  **아무것도 쓰지 않았다** — 줄은 보드에 그대로다.\n' +
      `  · 줄 주인: \`${파일지문7}\` (파일 이름 = 좌표)\n` +
      `  · 상태 칸: ${상태칸7.trim().slice(0, 120)}\n` +
      '  주인이 죽은 것과 일이 끝난 것은 다르다 — 아카이브에 넣으면 그 트랙은 보드에도\n' +
      '  인계문에도 안 떠서 **다음 세션이 원리상 못 줍는다**(F240 유입 경로).\n' +
      근거글7 + '\n' +
      '  ▶ 셋 중 하나를 골라라:\n' +
      '    ① 남은 일이 있으면 **줍는다** — 내 보드 파일에 새 줄로 선언하고 이어서 한다\n' +
      '       (남의 줄은 고치지 않는다 · 이 줄은 그 트랙이 끝난 뒤 옮기면 된다).\n' +
      '    ② 남은 일이 없다고 **실측했으면** 그 근거를 달아 다시 돌려라:\n' +
      `       node tools/board-move.js "${needle}" --미완확인 "커밋 <해시> 열어 잔여 0 확인"\n` +
      '       (근거는 **필수**이고 이관 커밋 메시지에 남는다 — 옛 판은 근거를 붙이면 튕겼다 · F407)\n' +
      '    ③ 판단이 안 서면 `docs/_ops/작업대기열.md` 에 줄을 세워 두고 그 뒤 ②.');
  }
}

if (dry) { console.log('[board-move] --dry — 쓰지 않았다.'); process.exit(0); }

/* 이관 **전부터** 더러웠는지 잰다. 쓴 뒤에 재면 내가 만든 더러움과 구분되지 않는다. */
const archiveDirty = inRepo ? dirtyOf(ARCHIVE) : '';
const boardDirty = inRepo ? dirtyOf(BOARD) : '';

/* ⛔ 아카이브에 남의 미커밋이 있으면 **아무것도 쓰지 않고** 멈춘다.
 * 여기서 커밋하면 남의 삽입을 함께 실어간다(F073 역방향). 안 쓰고 멈추면 줄은 보드에 그대로라 안전하다.
 *
 * 🔴 단 **내 앞선 실행의 잔재는 예외다**(F226 · F103 축). ①이 쓴 뒤 ②(단독 커밋)가 남의
 * `.git/index.lock` 에 막혀 죽으면 삽입만 된 채 미커밋으로 남는데, 이 검사가 그것을 「남의 것」으로
 * 읽어 **이후 모든 재실행이 거부**됐다 — 즉 만석 처방(board-guard 가 내주는 이 명령)이 첫 실패 한 번에
 * 통째로 죽는다. 이 저장소는 세션 9~15개가 동시에 도는 게 일상이라 lock 경합은 예외가 아니다.
 * 가르는 재료는 HEAD 대비 차분이다 — **추가가 옮기려던 그 줄 하나, 삭제 0**(남의 삽입이면 딴 줄이 뜬다). */
/** HEAD 대비 아카이브 차분. 「내 잔재인가」와 「그 잔재의 주인이 살아 있나」가 **같은 재료**를
 *  보게 한 곳에서 판다 — 두 번 재면 갈라지고, 갈라진 쪽은 여기서도 「통과」 방향이다. */
const 아카이브차분 = () => {
  const r = git(['show', `HEAD:${rel(ARCHIVE)}`], root);
  if (r.status !== 0) return null;
  /* 다중집합 빼기는 위 `행빼기` 하나를 쓴다 — `잔재닫기` 도 같은 재료로 「무엇이 더해졌나」를 본다. */
  const 있던것 = r.stdout.split(/\r?\n/).map((l) => l.trim());
  const 지금것 = archiveText.split(/\r?\n/).map((l) => l.trim());
  return { 더해진것: 행빼기(지금것, 있던것), 지워진것: 행빼기(있던것, 지금것) };
};
/* **게을리** 부른다(+메모) — 옛 판은 `archiveDirty &&` 뒤에서만 돌았고, 여기서 눈치 없이 앞으로
 * 당기면 아카이브가 깨끗한 평상시 실행마다 `git show` 가 한 번 더 붙는다. 재료를 한 곳으로
 * 모으는 것과 그 재료를 언제 재는가는 다른 문제다. */
let 차분캐시;
const 차분 = () => (차분캐시 !== undefined ? 차분캐시 : (차분캐시 = inRepo ? 아카이브차분() : null));
const 내잔재뿐 = () => {
  const d = 차분();
  return !!d && d.지워진것.length === 0 && d.더해진것.length === 1 && d.더해진것[0] === row.trim();
};
if (archiveDirty && !내잔재뿐()) {
  /* 🔴 **처방이 실행 가능한지까지가 이 거절의 몫이다** (F433 · 2026-08-14 신고 · 여기서 닫는다).
   *   옛 판은 언제나 「그 세션이 커밋한 뒤 다시 돌려라」만 냈다. 그 잔재의 주인이 **죽었으면**
   *   그 문장은 영원히 실행 불가고, 잠기는 것은 신고자 한 명이 아니라 **보드 정리 전체**다
   *   (만석이 안 풀리면 board-guard 가 새 선언을 계속 막는다). 세션이 9~15개 도는 이 저장소에서
   *   lock 경합은 예외가 아니라 상시라, 이 잠금도 상시다 — 실측 2026-08-14 하루에 두 번 밟았다.
   *   ⇒ F103 그 모양이다: 따를 수 없는 처방은 우회(=근거를 지우거나 남의 미커밋을 만지는 것)를
   *   정상 통로로 만든다.
   * 🔑 **출구는 이미 있었는데 처방에 안 적혀 있었다** — 잔재 줄을 needle 로 한 번 더 돌리면
   *   `내잔재뿐()` 이 참이 되어 위 F226 경로로 이어서 커밋한다(실측으로 두 번 다 그렇게 풀었다).
   *   아는 사람만 풀 수 있는 출구는 없는 것과 같아서, 아는 것을 **거절문이 말하게** 옮겼다.
   * 🔑 **이 처방은 어떤 게이트도 열지 않는다** — 가리키는 명령이 이 도구 자신이라, 그 실행은
   *   원칙 ⑥(산 주인)·⑦(미완 이관)·F102 를 처음부터 다시 지난다. 늘어나는 것은 «출구의 가시성»뿐이다.
   * ⚠ **틀릴 때의 모습**: 잔재 줄에 재료(지문·상태 칸 해시)가 하나도 없으면 산 주인 0 이라
   *   **살아 있는 주인에게도** F226 문구가 나간다. 그래도 그 명령은 위 게이트에 다시 걸리므로
   *   최악이 「한 번 더 거절당한다」이지 남의 줄이 옮겨지는 것이 아니다 — 새는 방향이 통과가 아니다. */
  const 문구뽑기 = (l) => {
    const 굵은 = (l.match(/\*\*(.+?)\*\*/) || [])[1];
    /* `"` 앞까지만 쓴다 — needle 은 큰따옴표 안에 찍히므로 안에 `"` 가 있으면 복붙이 깨진다.
     * 잘라도 `includes` 검색이라 여전히 그 줄에 걸린다(여러 줄에 걸리면 도구가 스스로 되묻는다). */
    return String(굵은 || 표.칸나누기(l)[1] || l).split('"')[0].trim();
  };
  /* **내 줄은 뺀다** — 잔재에 내 것이 섞여 있어도 막고 있는 것은 «남의» 삽입이다. 안 빼면
   * 「지금 막힌 그 명령을 다시 돌려라」라는 원 없는 처방이 목록에 끼어 진짜 과녁을 가린다.
   * ✅ **그 사각은 닫혔다**(F572 · 2026-08-17). 옛 주석은 「잔재 줄의 보드 줄이 이미 사라진 판에선
   *   이 명령이 「못 찾았다」로 튕긴다 — F226 실물은 언제나 보드 줄이 남아 있는 모양이라 그쪽을
   *   기본으로 뒀다」였는데, 그 확률 판단이 **그날 반례를 만났다**(보드 삭제까지 쓰고 죽은 세션).
   *   이제 아래 명령이 그 상태를 알아보고 **잔재를 닫는다**(`잔재닫기` · 종료 코드 7) — 처방이
   *   가리키는 명령 «자신»이 도는 쪽으로 고쳤으니 이 문구는 그대로 둔다(문을 하나 더 달지 않는다). */
  const 잔재행들 = ((차분() || {}).더해진것 || []).filter(isRow).filter((l) => l !== row.trim());
  const 죽은잔재 = 잔재행들.filter((l) => !산주인들(l, '').length);
  const 처방 = 잔재행들.length && 죽은잔재.length === 잔재행들.length
    ? '  그 잔재 줄의 주인은 **죽었다**(생사 실측) — 기다리면 영원히 안 풀린다(F433).\n' +
      '  ▶ **그 줄부터 마저 옮겨라** — 잔재 줄을 문구로 주면 이어서 커밋한다(F226 경로):\n' +
      죽은잔재.map((l) => `       node tools/board-move.js "${문구뽑기(l)}"`).join('\n') + '\n' +
      '  그 뒤 이 명령을 다시 돌린다(줄은 보드에 그대로 있어 잃을 게 없다).'
    : '  그 세션이 커밋한 뒤 다시 돌려라(줄은 보드에 그대로 있어 잃을 게 없다).';
  die('아카이브에 이관 전부터 **남의** 미커밋이 있다 — 내 커밋이 그것을 실어가므로 **아무것도 쓰지 않았다**:\n' +
    '  ' + archiveDirty.split(/\r?\n/).join('\n  ') + '\n' + 처방);
}
if (archiveDirty) console.log('  ↻ 그 미커밋은 **내 앞선 실행의 잔재**다(추가 1줄·삭제 0) — 이어서 커밋한다(F226).');

/* ── ① 아카이브를 쓰고 **아카이브만 단독 커밋**한다 ────────────────
 * 순서가 곧 durability 다. 이 저장소의 보장 경계는 파일시스템이 아니라 **git 커밋**이고,
 * 보드 삭제를 작업본에 쓰는 순간 그 삭제는 **남의 커밋에 실려 나갈 수 있다**(F073).
 * 그래서 아카이브가 **커밋되기 전에는 보드를 건드리지 않는다** — 그 사이에 무슨 일이 나도
 * 최악이 「양쪽에 다 있음」(중복=보이고 무해)이지 「어디에도 없음」(조용하고 영구)이 아니다.
 * 원칙 ③(아카이브 먼저)을 파일 층에서 git 층으로 올린 것이다. */
fs.writeFileSync(ARCHIVE, newArchive, 'utf8');
if (!fs.readFileSync(ARCHIVE, 'utf8').includes(row)) {
  die('아카이브에 쓴 뒤 다시 읽었는데 그 줄이 없다 — 보드는 건드리지 않았다(줄은 안전하다).');
}

/* 보드 쓰기는 **한 통로**로 — 줄을 빼서 상투구만 남으면 파일째 지운다(`껍데기인가` 머리말).
 * 통로를 하나로 두는 이유는 갈래가 넷(저장소 밖·남의 미커밋·원자·되돌리기)이라서다 —
 * 네 자리에 각각 적으면 「어떤 갈래에서는 껍데기가 남는다」가 조용히 생긴다. */
const 껍데기 = 껍데기인가(newBoard);
const 보드추적중 = inRepo && 추적중(BOARD);
const 보드쓰기 = () => {
  if (!껍데기) return fs.writeFileSync(BOARD, newBoard, 'utf8');
  try { fs.unlinkSync(BOARD); } catch (e) { if (e.code !== 'ENOENT') throw e; }
  return undefined;
};
/** 원상복구 — 커밋이 실패했을 때 **재실행이 가능한 상태**로 되돌린다(아래 원자 커밋 절 ⚠). */
const 보드되돌리기 = () => fs.writeFileSync(BOARD, boardText, 'utf8');
if (껍데기) console.log('  🧹 이 줄이 마지막이라 **빈 보드 파일을 남기지 않고 지운다**(상투구만 남았다).');

if (!inRepo) {
  보드쓰기();
  console.log('[board-move] 완료(파일) · ⚠ git 저장소 밖이라 **커밋은 안 했다** — 픽스처가 아니라면 지금 직접 커밋해라(F102).');
  process.exit(0);
}

/* ── 보드에 **남의 미커밋**이 있으면 옛 2단 경로 그대로 간다 ────────────
 * 원자 커밋은 경로를 못 박아도 그 파일 **안의** 남의 바이트를 함께 싣는다(F073). 이 갈래에서는
 * 아카이브만 커밋하고 보드 삭제는 커밋하지 않는다 — 안 해도 안전하다(아카이브가 이미 커밋됐으니
 * 최악이 눈에 보이는 중복이다). 이쪽도 커밋은 **1개**라 아래 원자 경로와 값이 같다. */
if (boardDirty) {
  const ca = commit([ARCHIVE], `docs: 보드 아카이브 — 「${track}」 완료 줄 선기록`,
    ['board-move 자동 커밋(F102) — 보드에 남의 미커밋이 있어 아카이브만 먼저 못박는다.',
      '이 커밋이 있어야 보드 삭제가 남의 커밋에 실려 나가도 최악이 「중복」이지 유실이 아니다.']
      .concat(사유본문()).join('\n'));
  if (!headHas(ARCHIVE, row)) {
    die('아카이브 추가가 커밋되지 않았다 — **보드는 건드리지 않았다**(줄은 보드에 그대로라 안전하다):\n' +
      `  git commit -m "docs: 보드 아카이브 선기록" -- "${rel(ARCHIVE)}"\n` +
      '  git: ' + ((ca.stderr || '') + (ca.stdout || '')).trim().split(/\r?\n/).slice(0, 4).join(' / '));
  }
  보드쓰기();
  console.log('[board-move] 완료 — 아카이브 **선기록 커밋** 후 보드에서 제거했다.');
  console.log('  ⚠ 보드에 이관 전부터 남의 미커밋이 있어 **보드 삭제는 커밋하지 않았다**(남의 선언을 수거하지 않는다):');
  boardDirty.split(/\r?\n/).forEach((l) => console.log('    ' + l));
  console.log('  그대로 둬도 안전하다 — 아카이브가 이미 커밋돼 최악이 「중복」이다.');
  process.exit(0);
}

/* ── 원자 커밋 — 아카이브 삽입과 보드 삭제를 **한 커밋**에 담는다 (2026-08-12) ─────
 *
 * 옛 판은 커밋을 둘로 갈랐다(아카이브 선기록 → 보드 제거). 그 분리는 원칙 ④(F102)의 집행이었고
 * 전제가 바로 아랫줄에 적혀 있었다 — 「보드는 상시 여러 세션이 더럽히는 파일이다(실측 08-05
 * 01:04: 한 순간에 6개 세션)」. 보드 삭제를 작업본에 쓰는 순간 **남의 커밋에 실려 나갈 수
 * 있으니**(F073) 그 전에 아카이브를 못박아 두자는 것이었다.
 *
 * 🔑 그 전제는 **F250(2026-08-08)에서 죽었다.** 보드 정본이 `docs/_ops/보드/<지문>.md` 로 갈리면서
 *   대상이 내 파일 하나가 됐다(이 파일 머리말이 이미 「파일이 좁아진 만큼 남의 줄을 건드릴 길이
 *   사라졌다」고 적어 뒀다). 그런데도 커밋 분리만 남았다 — 2026-08-12 에 쓸어갈 통로를 실측했다:
 *     · `auto-commit` — `제외폴더` 에 `docs/_ops/보드/` 가 **통째로** 있다(그 훅 106행 · F248).
 *     · `git add -A` · `git commit -a` · `git clean` — `git-scope-guard` 가 차단한다.
 *     · 남의 세션 — 내 지문 파일을 **이름으로 집어** 커밋해야 하는데 그게 F073 이고 board-guard 가 막는다.
 *   「보드 삭제만 남이 실어간다」로 가는 길이 남아 있지 않다. 전제가 죽었는데 집행만 남으면 그 값은
 *   매 이관마다 **커밋 1개**로 계속 나간다(실측 08-12: 장부 커밋이 세션당 2.2 → 4.7).
 *
 * ⚠ durability 는 깎지 않는다. 한 커밋은 **원자**라 「보드 삭제는 커밋됐는데 아카이브 추가는
 *   아니다」가 원리상 만들어지지 않는다 — 옛 판은 그것을 *순서*로 막았고 이제는 *원자성*이 막는다
 *   (회귀 F102·F083 이 그대로 이 갈래를 겨눈다). 그리고 실패하면 **보드를 원문으로 되돌린다**:
 *   옛 판은 실패 자리에 「아카이브만 커밋된 중복」을 남겨 재실행이 F226 잔재 경로로만 살았는데,
 *   되돌리면 보드에 줄이 그대로 있어 재실행이 **정상 경로**로 간다. */
보드쓰기();
const c = commit(보드추적중 ? [ARCHIVE, BOARD] : [ARCHIVE],
  `docs: 보드 — 「${track}」 완료 줄 아카이브 이관`,
  ['board-move 자동 커밋(F102) — 아카이브 삽입과 보드 제거를 **한 커밋**에 담는다.',
    '원자라 「보드 삭제만 착지」가 원리상 없다. 실패하면 보드를 원문으로 되돌린다.']
    .concat(사유본문()).join('\n'));
if (!headHas(ARCHIVE, row) || headHas(BOARD, row)) {
  보드되돌리기();
  die('이관 커밋이 착지하지 않았다 — **보드를 원문으로 되돌렸다**(줄은 보드에 그대로라 안전하다).\n' +
    '  아카이브 삽입은 작업본에 남아 있다 — 다시 돌리면 그 잔재를 알아보고 이어간다(F226).\n' +
    `  손으로 하려면: git commit -m "docs: 보드 이관" -- "${rel(ARCHIVE)}" "${rel(BOARD)}"\n` +
    '  git: ' + ((c.stderr || '') + (c.stdout || '')).trim().split(/\r?\n/).slice(0, 4).join(' / '));
}
console.log(`[board-move] 완료 — 아카이브 삽입 + 보드 제거를 **한 커밋**으로 못박았다${껍데기 ? ' · 빈 파일은 지웠다' : ''}(F102).`);
