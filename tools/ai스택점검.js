#!/usr/bin/env node
'use strict';
/* AI 스택 점검 — 「세 벤더에 나눠 준 역할이 **실제로 도는가**」를 세는 한 장.
 *
 * ■ 왜 있나 (2026-09-01 · 유호님 「역할이 배치된 대로 잘 굴러가게 시스템화되어 있는지 점검해줘」)
 *   그날 처음 세어 보고 알았다 — 자가 셋 다 있는데 **자를 재는 자가 없었다**:
 *     · ①배포 검수(Codex)  = 훅까지 박혀 매 배포에 돈다(런 81건 / 08-20~08-31)
 *     · ②설계 심문(Codex)  = **정본 중 심문받은 것 1벌.** 트리거가 「동결 직전」이라는 사람의 기억
 *     · 몽골어 검문(Gemini) = **장부 0줄 · 도장에 지문 0.** 「돌렸나」를 셀 방법이 없었다
 *     · ㉠밖의 사실(DR 둘) = 장부 0줄. 「안 물어봤다」와 「물어봐서 통과」가 같은 모양
 *   검수는 코드를, 심문은 설계를, 정찰은 모델을 재는데 **배치 자체의 커버리지**는 아무도 안 쟀다.
 *   그래서 「1/35」가 두 주 동안 조용했다. 이 파일이 그 넷째 자다.
 *
 * ■ 하지 «않는» 것 — 자가 둘이면 반드시 갈린다(one-ruler-per-judgment)
 *   🚫 **심문 드리프트를 여기서 안 낸다** — 「심문 뒤 문서가 자랐다」는 `review-runs.js` 몫이다.
 *      여기는 그 자가 **원리상 못 보는 것**만 센다: ㉮한 번도 안 받은 것 ㉯장부가 가리키는데
 *      파일이 없는 것(그쪽은 `continue` 로 침묵한다 · :206).
 *   🚫 **목록을 짓지 않는다.** 「어느 문서가 소급 불가인가」는 판정이고, 여기 적으면 그게 둘째
 *      정본이 된다. 그래서 **문서가 자기 머리에 선언**하고(아래 표식) 이 자는 세기만 한다 —
 *      `doc-graph` 의 「엣지는 항상 파생이 선언한다」와 같은 정신이다.
 *   🚫 **막지 않는다.** 알림이다. 배포 게이트는 `clasp-guard` 하나면 족하다.
 *
 * ■ 표식 규약 — 문서 아무 곳에 한 줄
 *     <!-- 심문: 소급불가 -->            ← ②설계 심문 대상이다(안 받았으면 여기서 운다)
 *     <!-- 심문: 면제 · 왜 면제인지 -->   ← 대상이 아니다. **사유를 적어야** 면제가 된다
 *   면제에 사유를 강제하는 이유: 사유 없는 면제는 다음 사람에게 「검토했다」로 보이는데
 *   실제로는 「귀찮아서 껐다」와 같은 모양이다.
 *
 * 사용:
 *   node tools/ai스택점검.js            # 한 장 전부
 *   node tools/ai스택점검.js --훅        # 적색만 · 하루 1회(SessionStart 용)
 *   node tools/ai스택점검.js --훅 --force # 스로틀 무시
 *   node tools/ai스택점검.js --json
 *   node tools/ai스택점검.js --재보기   # 제미나이 키 생존을 캐시(6시간) 무시하고 지금 잰다
 *   node tools/ai스택점검.js --개명 <옛경로> <새경로> --사유 "왜"   # 심문 장부에 개명 사슬을 잇는다
 * 종료코드: 0=정상(적색이 있어도 0 — 알림이다) · 1=자 자신이 못 돈 것(«확인 불가»이지 「정상」이 아니다)
 *
 * ■ 개명 — 왜 «지우기»가 아니라 «얹기»인가 (2026-09-01)
 *   장부는 append-only 이력이라 문서를 개명하면 옛 이름이 영원히 남는다. 그러면 자가 둘로 갈려
 *   틀린다: 여기는 「추적 끊김」으로 **영원히 울고**, `review-runs` 는 파일이 없어 `continue` 로
 *   **영원히 침묵한다**(한쪽은 소음, 한쪽은 실명). 옛 줄은 그때 참이었으므로 고치지 않고,
 *   `{종류:'개명', 옛대상, 새대상, 사유}` 한 줄을 얹어 사슬을 잇는다.
 *   사슬 푸는 코드는 `tools/lib/심문장부.js` **하나**다 — 두 자가 각자 풀면 반드시 어긋난다.
 */

const fs = require('fs');
const os = require('os');
const path = require('path');
const crypto = require('crypto');
// 개명 사슬 해석은 여기 하나뿐이다(review-runs 도 같은 모듈을 쓴다).
const 장부해석 = require('./lib/심문장부.js');

/* 뿌리를 env 로 갈아끼울 수 있게 둔다 — **회귀가 픽스처 저장소를 세워 탐지력을 재기 위해서다.**
 * 실저장소에서는 대개 「걸린 것 0」이라 여기서 초록은 「깨끗하다」와 「자가 죽었다」를 못 가른다
 * (그 함정을 이 파일이 첫 실행에 정면으로 밟았다 — SKIP 머리말). 장부 경로 env 와 같은 관행이다. */
const ROOT = process.env.SYNK_AISTACK_ROOT || path.resolve(__dirname, '..');
const 심문장부 = () => path.join(ROOT, 'docs', '_ops', '심문기록.jsonl');
const 몽골장부 = () => process.env.SYNK_MN_LEDGER || path.join(ROOT, 'docs', '_ops', '몽골어검문.jsonl');
const 검수장부 = () => path.join(ROOT, 'docs', '_ops', '검수기록.jsonl');
const 정찰장부 = () => path.join(ROOT, 'docs', '_ops', '모델정찰.jsonl');
/* 도장은 git «디렉터리» 안에 둔다 — 작업본에 넣으면 세션마다 커밋 소음이 되고, `os.tmpdir()` 은
 * 재부팅으로 조용히 리셋된다(그럼 스로틀이 있는 척만 한다).
 *
 * 🔴 **`ROOT/.git` 을 폴더로 단정하면 안 된다** — 워크트리에서는 그게 `gitdir: …` 한 줄짜리
 *   **파일**이다. 09-01 이 자를 워크트리에서 처음 돌렸을 때 `mkdirSync` 가 ENOTDIR 로 죽고
 *   `도장쓰기` 의 catch 가 그걸 삼켜, 스로틀이 **있는 척만 하는 상태**로 매번 울 뻔했다
 *   (조용한 실패는 언제나 「통과」 쪽으로 샌다). 그래서 포인터를 따라간다. */
function git방() {
  const g = path.join(ROOT, '.git');
  try {
    if (fs.statSync(g).isDirectory()) return g;
    const m = /gitdir:\s*(.+)/.exec(fs.readFileSync(g, 'utf8'));
    if (m) {
      const 실제 = path.resolve(ROOT, m[1].trim());
      if (fs.statSync(실제).isDirectory()) return 실제;
    }
  } catch (_) { /* 아래 폴백 */ }
  return null;
}
const 도장경로 = () => process.env.SYNK_AISTACK_STAMP
  || path.join(git방() || os.tmpdir(), 'synk-ai스택도장.json');

const 표식꼴 = /<!--\s*심문\s*:\s*([^>]*?)\s*-->/;
const 소급꼴 = /소급\s*불가|소급불가/;
const 후보이름꼴 = /설계|계약|규격/;

/* ── 몽골어 «커버리지» 축 (2026-09-02 · 유호 「하자」) ────────────────────────
 * 왜: 앞의 몽골어 셈은 **장부에 든 것만** 본다 — 한 번도 검문 안 받은 문안은 **원리상 안 보인다.**
 *   09-02 실측이 그 구멍을 드러냈다: 자는 다섯 층으로 좋아졌는데 그 자를 «부르는 훅이 없어»
 *   내가 기억해야 돌았고, 실제로 그날 돌린 24건이 전부 임시 파일 대상이었다.
 *   ⇒ 「자가 좋은가」와 「자가 도는가」는 다른 물음이고, 후자를 세는 자가 없었다.
 * 규약은 심문 축과 «같다» — 🚫목록을 짓지 않는다. **문서가 제 머리에 선언**하고 이 자는 센다:
 *     `<!-- 몽골어검문: 대상 -->`              ← 우리가 내보내는 몽골어다(안 받았으면 여기서 운다)
 *     `<!-- 몽골어검문: 면제 · 왜 면제인지 -->` ← 대상이 아니다. **사유를 적어야** 면제가 된다
 *   (`.js` 파일이면 줄 주석 안에 그대로 둔다 — 꼴만 맞으면 된다.)
 * 🔑 **표식 없는 것은 «후보»로 개수만 낸다.** 첫날 31벌이 잡혔는데 목록을 매 세션 토해 내면
 *   그게 모래주머니다(자가 요란하면 사람이 자를 안 본다). 목록은 부를 때만: `--몽골후보`. */
const 몽골표식꼴 = /<!--\s*몽골어검문\s*:\s*([^>]*?)\s*-->/;
const 키릴꼴 = /[Ѐ-ӿ]/g;
/* 200자 = 「한 낱말 섞인 것」과 「문안이 들어 있는 것」을 가르는 선. 실측으로 골랐다(09-02):
 * 이 선 위가 31벌인데 그 아래는 러시아어 낱말 하나·상표 표기 같은 것들이라 후보가 아니다. */
const 키릴문턱 = 200;
/* 🔴 **ROOT 기준 상대경로에 댄다 — 절대경로에 대면 저장소가 어디 놓였느냐로 답이 갈린다.**
 *   2026-09-01 이 자를 짓던 워크트리(`.claude/worktrees/…`)에서 첫 실행이 **후보 0**을 냈다.
 *   절대경로에 `worktrees` 가 들어 있어 자기 문서 229벌을 통째로 걸러낸 것이다 — 그런데 출력은
 *   「후보 0 · 걸린 것 없음」이라 **성공 얼굴**이었다(zero-is-a-success-face). 자를 먼저 의심하라는
 *   그 원칙이 자기 자신에게 먼저 걸렸다. ⚠ 같은 꼴의 SKIP 이 `tools/doc-graph.js` 에도 있다 —
 *   거긴 이 세션이 안 만졌으니 그쪽에서 재는 값은 워크트리에서 따로 의심해야 한다. */
const SKIP = [/\/_archive\//, /\/worktrees\//, /\/_구본\//, /\/_ops\//];

function 지문(buf) {
  return crypto.createHash('sha256').update(buf).digest('hex').slice(0, 12);
}

function jsonl(경로) {
  let raw;
  try { raw = fs.readFileSync(경로, 'utf8'); }
  catch (_) { return null; }              // null = 장부가 없다(0줄과 갈린다)
  const 행 = [];
  for (const l of raw.split('\n')) {
    if (!l.trim()) continue;
    try { 행.push(JSON.parse(l)); } catch (_) { /* 못 읽은 줄은 셈에서 빠진다 — 아래가 분모로 말한다 */ }
  }
  return 행;
}

function 문서들() {
  const 것 = [];
  (function 걷기(디) {
    let 목록;
    try { 목록 = fs.readdirSync(디, { withFileTypes: true }); } catch (_) { return; }
    for (const e of 목록) {
      const p = path.join(디, e.name);
      // 앞에 '/' 를 붙여 첫 칸(`docs/`)도 `/…/` 꼴로 걸리게 한다.
      const 상대 = '/' + path.relative(ROOT, p).replace(/\\/g, '/');
      if (SKIP.some((r) => r.test(상대))) continue;
      if (e.isDirectory()) 걷기(p);
      else if (/\.md$/.test(e.name)) 것.push(p);
    }
  })(path.join(ROOT, 'docs'));
  return 것;
}

/* 🔴 **규약을 «설명»하는 것과 «선언»하는 것을 가른다** (2026-09-01 · 병합 직후 실측).
 *   표식 규약을 적어 둔 `docs/AI_스택_가이드.md` 가 스스로 「심문 대상」으로 잡혔다 — 그 문서는
 *   백틱 안에 표식을 **예시로** 실었을 뿐인데 파서가 raw 로 읽은 것이다. 코드 표기 안의 것은
 *   문서가 «말하는» 것이지 «선언하는» 것이 아니다. 예외 목록을 짓는 대신(그건 둘째 정본이 된다)
 *   표기법으로 가른다: 펜스 블록과 인라인 코드를 지운 뒤에 표식을 찾는다.
 *   ⇒ 진짜 선언은 백틱 없이 raw 로 놓는다. */
function 코드지운(본문) {
  return String(본문)
    .replace(/```[\s\S]*?```/g, '')
    .replace(/`[^`\n]*`/g, '');
}

/* ── ㉠ ②설계 심문 커버리지 ─────────────────────────────────────────────── */
function 심문커버리지() {
  const 적색 = [], 알림 = [];
  const 행들 = jsonl(심문장부());
  /* 개명 사슬은 `tools/lib/심문장부.js` 가 «혼자» 푼다 — review-runs 의 드리프트도 같은 모듈을
   * 쓴다. 두 자가 각자 풀면 한쪽은 쉬지 않고 울고 한쪽은 눈이 먼다(09-01 실물이 정확히 그것). */
  const 접힘 = 장부해석.요약(행들 || []);
  const 심문됨 = 접힘.심문됨;
  const 가리킨것 = 접힘.가리킨것;

  const 선언 = [], 면제 = [], 후보 = [];
  for (const abs of 문서들()) {
    let 본문;
    try { 본문 = fs.readFileSync(abs, 'utf8'); } catch (_) { continue; }
    const 상대 = path.relative(ROOT, abs).replace(/\\/g, '/');
    const 산문 = 코드지운(본문);
    const m = 표식꼴.exec(산문);
    if (m) {
      const 값 = m[1];
      if (/^면제/.test(값)) {
        const 사유 = 값.replace(/^면제\s*[·:\-]?\s*/, '').trim();
        면제.push({ 상대, 사유 });
        // 사유 없는 면제는 「검토했다」와 「껐다」가 같은 모양이 된다.
        if (!사유) 적색.push(`   · ${상대} — 면제인데 **사유가 없다**(표식: \`<!-- 심문: 면제 · 왜 -->\`)`);
      } else {
        선언.push(상대);
      }
      continue;
    }
    if (후보이름꼴.test(path.basename(상대)) && 소급꼴.test(본문)) 후보.push(상대);
  }

  const 미심문 = 선언.filter((p) => !심문됨.has(p));
  // 장부가 가리키는데 파일이 없다 = 개명·삭제. review-runs 는 여기서 `continue` 로 침묵한다(:206).
  const 끊김 = [...가리킨것].filter((p) => !fs.existsSync(path.join(ROOT, p)));

  if (미심문.length) {
    /* 🔑 「안 받았다」로 단정하지 않는다 — **이 자가 아는 것은 장부뿐이다.** 09-01 실측: 장부는
     *   08-29 이후만 알고, 그 전 심문 결과는 08-19 정리(e75fc7fc)로 걷혀 장부에 안 남았다
     *   (엔진도달 v1 은 P0 12건 동결 불가 판정까지 받았는데 장부엔 0줄이다). 자가 못 본 것을
     *   「없다」로 말하면 그게 [[measurement-needs-its-instrument]] 가 막으려는 바로 그 문장이다. */
    적색.push(`🔴 **심문 대상으로 선언됐는데 «장부에 심문 기록이 없는» 정본 ${미심문.length}벌** — 계약·스키마는 동결 뒤 못 고친다:`);
    for (const p of 미심문) 적색.push(`   · ${p}`);
    적색.push('   ⚠ 「안 받았다」가 아니라 「장부에 없다」다 — 장부는 08-29 이후만 안다. 문서가 스스로 심문 이력을 적고 있으면 그쪽이 더 오래된 진실이다.');
    적색.push('   심문: node tools/codex-review.js --심문 <문서> --던지기');
  }
  if (끊김.length) {
    적색.push(`🔴 **심문 장부가 가리키는 파일이 없다 ${끊김.length}벌** — 개명이거나 지워졌다. 「드리프트 없음」이 아니라 «추적 끊김»이다:`);
    for (const p of 끊김) 적색.push(`   · ${p} (장부에는 있는데 디스크에 없다)`);
    /* 처방을 «우는 자리»에 붙인다 — 09-01 까지 이 자리는 「다시 심문한다」고만 했는데, 그러면
     * 새 이름으로 줄이 하나 더 늘 뿐 **옛 줄은 그대로 울었다**(장부는 이력이라 안 고친다).
     * 개명 사건 줄이 그 사슬을 잇는다 — 옛 줄은 참인 채로 남고 자만 최종 이름을 본다. */
    적색.push('   개명이면 사슬을 잇는다(장부는 고치지 않고 «사건 줄»을 얹는다):');
    적색.push('     node tools/ai스택점검.js --개명 <옛경로> <새경로> --사유 "왜 바뀌었나"');
    적색.push('   진짜 지워진 것이면 그 트랙의 일이다 — 여기서 지어내지 않는다.');
  }
  /* 순환은 «조용히 한 칸만 가는» 대신 말한다 — 장부는 사람도 쓰는 자리라 A→B→A 가 실제로 가능하다. */
  const 순환들 = [...접힘.대상들.entries()].filter(([, v]) => v.순환).map(([k]) => k);
  if (순환들.length) {
    적색.push(`🔴 **개명 사슬이 돈다 ${순환들.length}벌** — 사슬 끝을 못 정한다(마지막으로 닿은 이름에서 멈췄다):`);
    for (const p of 순환들) 적색.push(`   · ${p}`);
    적색.push('   장부에 되돌리는 개명 줄이 들어간 것이다. 지금 이름으로 «앞으로 가는» 줄을 하나 더 얹어 끊는다.');
  }
  if (후보.length) {
    알림.push(`🟠 **표식이 없는 심문 후보 ${후보.length}벌** — 본문이 스스로 「소급 불가」를 말하는 설계·계약·규격 문서다.`);
    알림.push('   대상이면 `<!-- 심문: 소급불가 -->` · 아니면 `<!-- 심문: 면제 · 왜 -->` 를 그 문서에 심는다(판정은 사람이 한다).');
    for (const p of 후보) 알림.push(`   · ${p}`);
  }

  return {
    적색, 알림,
    셈: {
      선언: 선언.length, 면제: 면제.length, 미심문: 미심문.length, 후보: 후보.length,
      끊김: 끊김.length, 개명: 접힘.개명수, 순환: 순환들.length,
      장부: 행들 ? 행들.length : null,
    },
  };
}

/* 몽골어가 «들어 있는» 파일을 걷는다. 심문 축의 문서들()이 docs/*.md 만 보는 것과 달리,
 * 우리 몽골어는 캐러셀 원고·크루카드 html·엔진 js 문자열에도 산다. */
function 몽골파일들() {
  const 것 = [];
  (function 걷기(디) {
    let 목록;
    try { 목록 = fs.readdirSync(디, { withFileTypes: true }); } catch (_) { return; }
    for (const e of 목록) {
      if (e.name === 'node_modules' || e.name === '.git' || e.name.startsWith('.claude')) continue;
      const p = path.join(디, e.name);
      const 상대 = '/' + path.relative(ROOT, p).replace(/\\/g, '/');
      if (SKIP.some((r) => r.test(상대))) continue;
      if (e.isDirectory()) { 걷기(p); continue; }
      if (!/\.(md|html|txt|js)$/.test(e.name)) continue;
      것.push(p);
    }
  })(ROOT);
  return 것;
}

/** 선언·면제·후보를 센다. 반환의 각 배열은 ROOT 상대경로. */
function 몽골커버리지() {
  const 선언 = [], 면제 = [], 후보 = [], 사유없는면제 = [];
  for (const abs of 몽골파일들()) {
    let 본문;
    try { 본문 = fs.readFileSync(abs, 'utf8'); } catch (_) { continue; }
    const 상대 = path.relative(ROOT, abs).replace(/\\/g, '/');
    /* 코드 표기 안의 표식은 «설명»이지 «선언»이 아니다 — 심문 축이 이미 데인 자리라 같이 판다.
     * (규약을 적어 둔 문서가 스스로 대상으로 잡히는 병) */
    const m = 몽골표식꼴.exec(코드지운(본문));
    if (m) {
      const 값 = m[1];
      if (/^면제/.test(값)) {
        const 사유 = 값.replace(/^면제\s*[·:\-]?\s*/, '').trim();
        면제.push(상대);
        if (!사유) 사유없는면제.push(상대);
      } else 선언.push(상대);
      continue;
    }
    const n = (본문.match(키릴꼴) || []).length;
    if (n >= 키릴문턱) 후보.push({ 상대, 키릴: n });
  }
  후보.sort((a, b) => b.키릴 - a.키릴);
  return { 선언, 면제, 후보, 사유없는면제 };
}

/* 재검문 명령을 «그 파일에 맞는 모양»으로 낸다 — 복사해서 그대로 칠 수 있게.
 *
 * 🔴 왜 있나(2026-09-03 · 실물로 밟았다): 옛 판은 `--파일 <경로>` 하나만 냈다.
 *   그런데 캐러셀 원고는 **몽골어만** 들어 있고 한국어는 옆 파일(`<이름>.한국어.md`)에 따로 산다.
 *   그 안내대로 치면 도구가 「파일 형식이 아니다」로 죽는다(도구는 한국어/---/몽골어 «한 파일»을 기대한다).
 *   ⇒ 짝이 옆에 있으면 `--원문` 까지 박아 준다. 안내가 반쪽이면 그건 안내가 아니라 «막다른 길»이다.
 *   🔑 짝을 «추측»하지 않는다 — 실제로 파일이 있을 때만 붙인다(없는 경로를 박으면 또 다른 막다른 길이다). */
function 재기명령(상대) {
  const 짝 = 상대.replace(/\.md$/, '.한국어.md');
  const 있나 = 짝 !== 상대 && fs.existsSync(path.join(ROOT, 짝));
  return 있나
    ? `node tools/몽골어대조.js --파일 "${상대}" --원문 "${짝}"`
    : `node tools/몽골어대조.js --파일 "${상대}"`;
}

/* ── ㉡ 몽골어 검문 드리프트 + 커버리지 ─────────────────────────────────── */
function 몽골어검문() {
  const 적색 = [], 알림 = [];
  const 행들 = jsonl(몽골장부());
  const cov = 몽골커버리지();
  /* 장부가 아는 «검문된 경로» — 통과 여부와 무관하다. 여기서 세는 것은 「쟀나」이지 「좋나」가 아니다. */
  const 검문된 = new Set((행들 || [])
    .filter((r) => r && r.대상 && r.대상 !== '(인자)' && !r.대상.startsWith('../') && !path.isAbsolute(r.대상))
    .map((r) => r.대상));
  const 안받음 = cov.선언.filter((p) => !검문된.has(p));
  if (cov.사유없는면제.length) {
    적색.push(`🔴 **몽골어검문 면제인데 사유가 없는 것 ${cov.사유없는면제.length}벌** — 「검토했다」와 「껐다」가 같은 모양이 된다:`);
    /* 🔴 여기만 템플릿 문자열을 «안» 쓴다(2026-09-02 실측). 템플릿 안에서 백틱을 이스케이프하면
     *   (`\``) `코드지운()` 의 인라인 코드 정규식이 **엉뚱한 구간을 잘라** 표식이 raw 로 살아남고,
     *   그러면 **이 자가 자기 자신을 「면제 선언」으로 센다.** 심문 축에 같은 꼴이 있지만(:178)
     *   그쪽 `문서들()` 은 `docs/*.md` 만 훑어 `.js` 인 이 파일에 닿지 않아 드러나지 않았을 뿐이다.
     *   홑따옴표 문자열 안의 «맨» 백틱은 정상적으로 걷힌다. */
    for (const p of cov.사유없는면제) 적색.push('   · ' + p + '  (표식: `<!-- 몽골어검문: 면제 · 왜 -->`)');
  }
  if (안받음.length) {
    적색.push(`🔴 **「대상」으로 선언했는데 검문 장부에 없는 것 ${안받음.length}벌** — 대외로 나갈 몽골어가 한 번도 안 재졌다:`);
    for (const p of 안받음) 적색.push(`   · ${p}`);
    적색.push('   재기: node tools/몽골어대조.js --파일 <몽골어파일> --원문 <한국어파일>');
  }
  if (cov.후보.length) {
    알림.push(`🟠 표식 없는 몽골어 후보 ${cov.후보.length}벌(키릴 ${키릴문턱}자 이상) — 대상인지 면제인지 아직 아무도 안 정했다.`);
    알림.push('   목록: node tools/ai스택점검.js --몽골후보 · 정하면 그 파일에 한 줄: `<!-- 몽골어검문: 대상 -->` 또는 `<!-- 몽골어검문: 면제 · 왜 -->`');
  }
  if (행들 === null || !행들.length) {
    알림.push('🟠 **몽골어 검문 장부가 비어 있다** — 「검문을 다 지났다」가 아니라 **안 재봤다**이다.');
    알림.push('   문안을 검문할 때: node tools/몽골어대조.js --파일 <경로>  (한국어 / --- / 몽골어 가 한 파일에 있을 때)');
    알림.push('   몽골어«만» 든 파일이면 짝을 같이 준다: … --파일 <몽골어> --원문 <한국어짝>  (그 실행이 장부를 남긴다)');
    return { 적색, 알림, 셈: { 줄: 행들 ? 0 : null, 바뀜: 0, 끊김: 0,
      선언: cov.선언.length, 안받음: 안받음.length, 면제: cov.면제.length, 후보: cov.후보.length } };
  }
  /* 파일 대상만 드리프트를 잰다 — 인자 모드는 잴 파일이 없다(안 잰 것이지 통과가 아니다).
   *
   * 🔴 **저장소 «밖» 대상도 뺀다**(2026-09-01 · 장부의 첫 실줄이 가르쳐 준 것).
   *   옆 세션이 scratchpad 의 워밍업 파일을 검문했고, 장부에 `../../AppData/Local/Temp/…` 가 남았다.
   *   그런 파일은 곧 사라지므로 여기서 재면 **영원히 「추적 끊김」으로 운다** — 우리가 관리하지 않는
   *   바이트라 드리프트라는 말 자체가 안 선다. 다만 **0으로 위장하지 않고** 「바깥」으로 세어 적는다. */
  const 최신 = new Map();
  let 바깥 = 0;
  for (const r of 행들) {
    if (!r || !r.대상 || r.대상 === '(인자)' || !r.대상지문) continue;
    if (r.대상.startsWith('../') || path.isAbsolute(r.대상)) { 바깥++; continue; }
    최신.set(r.대상, r);
  }
  const 바뀜 = [], 끊김 = [];
  for (const [상대, r] of 최신) {
    const abs = path.join(ROOT, 상대);
    let 지금;
    try { 지금 = 지문(fs.readFileSync(abs)); } catch (_) { 끊김.push(상대); continue; }
    if (지금 !== r.대상지문) 바뀜.push({ 상대, 그때: r.대상지문, 지금, 통과: r.통과 });
  }
  if (바뀜.length) {
    적색.push(`🔴 **검문 뒤 문안이 바뀐 것 ${바뀜.length}벌** — 옛 도장은 이 바이트에 안 찍혔다:`);
    for (const b of 바뀜) 적색.push(`   · ${b.상대} (검문 때 ${b.그때} → 지금 ${b.지금}${b.통과 ? '' : ' · 그때도 불통과였다'})`);
    적색.push('   다시(복사해서 그대로 친다):');
    for (const b of 바뀜) 적색.push('     ' + 재기명령(b.상대));
  }
  if (끊김.length) {
    알림.push(`🟠 검문 장부가 가리키는 파일이 없다 ${끊김.length}벌 — ${끊김.join(' · ')}`);
  }
  const 인자만 = 행들.filter((r) => r && r.대상 === '(인자)').length;
  if ((인자만 || 바깥) && !최신.size) {
    알림.push(`🟠 검문 ${인자만 + 바깥}건이 전부 «잴 수 없는» 대상이다(인자 ${인자만} · 저장소 밖 ${바깥}) — **드리프트를 원리상 못 잰다.** 저장소 안 문안을 \`--파일\` 로 검문해야 자가 센다.`);
  } else if (바깥) {
    알림.push(`🟠 저장소 밖 대상 ${바깥}건은 드리프트에서 뺐다 — 우리가 관리하지 않는 바이트다(안 잰 것이지 통과가 아니다).`);
  }
  return { 적색, 알림, 셈: { 줄: 행들.length, 파일대상: 최신.size, 인자: 인자만, 바깥: 바깥,
    바뀜: 바뀜.length, 끊김: 끊김.length,
    // 커버리지 — 「장부에 든 것」이 아니라 「우리 몽골어 전부」를 분모로 잡는 축이다
    선언: cov.선언.length, 안받음: 안받음.length, 면제: cov.면제.length, 후보: cov.후보.length } };
}

/* ── ㉢ 밖의 사실 ───────────────────────────────────────────────────────── */
function 밖의사실축() {
  const 적색 = [], 알림 = [];
  let 요약;
  // 모듈은 **언제나 이 도구 옆**에서 읽는다(ROOT 는 픽스처로 갈릴 수 있다) — 장부만 env 로 갈린다.
  try { ({ 요약 } = require(path.join(__dirname, '밖의사실.js'))); }
  catch (e) { return { 적색: [`🔴 밖의 사실 자를 못 읽었다(${e.message}) — «확인 불가»다.`], 알림, 셈: {} }; }
  const s = 요약();
  if (!s.목록.length) {
    알림.push('🟠 **밖의 사실 장부가 비어 있다** — 법령·업종코드·FDI·상표·시세에 반박 패스가 **0건**이다.');
    알림.push('   세우기: node tools/밖의사실.js --세움 "명제" --걸린것 "무엇이 걸렸나" --내답 "지금 믿는 답"');
    return { 적색, 알림, 셈: { 합: 0, 열림: 0, 반박받음: 0, 판정완료: 0 } };
  }
  const 오늘 = new Date().toISOString().slice(0, 10);
  if (s.열림.length) {
    적색.push(`🔴 **반박을 안 받은 명제 ${s.열림.length}건** — 「통과」가 아니라 «안 물어봤다»다:`);
    for (const b of s.열림) 적색.push(`   · [${b.키}] ${b.명제}${b.기한 ? ` (기한 ${b.기한}${b.기한 < 오늘 ? ' · **지났다**' : ''})` : ''}`);
    적색.push('   발주문: node tools/밖의사실.js --발주문 <키>');
  }
  if (s.반박받음.length) {
    알림.push(`🟠 답은 왔는데 판정이 없는 것 ${s.반박받음.length}건 — DR 답은 자료지 판정이 아니다:`);
    for (const b of s.반박받음) 알림.push(`   · [${b.키}] ${b.명제}`);
  }
  return { 적색, 알림, 셈: { 합: s.목록.length, 열림: s.열림.length, 반박받음: s.반박받음.length, 판정완료: s.판정완료.length } };
}

/* ── ㉢ 제미나이 «키 생존» — 자가 도는가 앞에 「자가 살아 있는가」가 있다 (2026-09-03) ──
 *
 * ■ 왜 붙였나 (유호님 「제미나이가 역할을 잘 하고 있나?」에 세어 보다가 드러난 구멍)
 *   09-02 저녁 제미나이 키가 429(크레딧 소진)로 죽었다. 그 키 **하나**에 자리 셋이 매달려 있다 —
 *   몽골어 검문(①역번역 ②문법) · 검수 러너의 gemini 레인 · 이미지 굽기(나노바나나). 셋이 한꺼번에
 *   멈췄는데 **다음 날 세션 첫머리는 조용했다.** 이 자는 「장부에 몇 줄 있나」를 세는데, 장부는
 *   «부른 적이 있어야» 자라므로 아무도 안 부르면 조용한 것이 정상 얼굴이다.
 *   ⇒ 커버리지(도는가)와 생존(살았나)은 **다른 물음**이고, 뒤엣것을 재는 자가 없었다.
 *
 * ■ 규율 셋
 *   1. **셋으로 말한다** — 살았다 / 죽었다 / **못 물어봤다**. 네트워크 실패를 「정상」으로 접지 않는다.
 *   2. **판정을 캐시한다**(기본 6시간) — 세션 첫머리마다 네트워크를 때리면 그게 모래주머니다.
 *      캐시 파일은 `.claude/state/` (git 밖). `--재보기` 면 캐시를 무시하고 지금 잰다.
 *   3. **막지 않는다** — 이 자 전체의 규율 그대로. 알림이다.
 *   env: SYNK_GEMINI_PROBE=off 면 아예 안 묻는다(회귀가 픽스처로 두 방향을 다 재기 위해서다). */
const 생존캐시경로 = () => process.env.SYNK_GEMINI_STATE
  || path.join(ROOT, '.claude', 'state', '제미나이생존.json');
const 생존캐시수명 = 6 * 60 * 60 * 1000;

/* 🔑 09-03 부터 **열쇠가 둘**이다(유호 확정 「글은 공짜, 그림은 유료」) — 그래서 둘 다 잰다.
 *   하나만 재면 다른 하나의 죽음이 안 보인다. 캐시도 용도별로 따로 쥔다. */
const 용도들 = ['글', '돈'];

/** 캐시가 신선하면 그걸, 아니면 지금 물어본다. @returns {Promise<{[용도]:{때, 기록, 캐시}}>} */
async function 키생존기록({ 재보기 = false } = {}) {
  const 경로 = 생존캐시경로();
  let 캐시 = {};
  try { 캐시 = JSON.parse(fs.readFileSync(경로, 'utf8')) || {}; } catch (_) { /* 없음·깨짐 = 다시 잰다 */ }
  const 끔 = process.env.SYNK_GEMINI_PROBE === 'off';
  const 결과 = {};
  let 바뀜 = false;
  for (const 용도 of 용도들) {
    const c = 캐시[용도];
    if (!재보기 && c && c.때 && Date.now() - new Date(c.때).getTime() < 생존캐시수명) { 결과[용도] = { ...c, 캐시: true }; continue; }
    if (끔) { 결과[용도] = { 때: null, 기록: { 살았나: null, 용도, 종류: '안 재봤다', 사유: 'SYNK_GEMINI_PROBE=off' }, 캐시: false }; continue; }
    let 기록;
    // 모듈은 «언제나 이 도구 옆»에서 읽는다(ROOT 는 픽스처로 갈릴 수 있다).
    try { 기록 = await require(path.join(__dirname, '모델정책.js')).제미나이생존({ 용도 }); }
    catch (e) { 기록 = { 살았나: null, 용도, 종류: '자가 못 돌았다', 사유: String((e && e.message) || e).slice(0, 200) }; }
    결과[용도] = { 때: new Date().toISOString(), 기록, 캐시: false };
    캐시[용도] = { 때: 결과[용도].때, 기록 };
    바뀜 = true;
  }
  if (바뀜) {
    try {
      fs.mkdirSync(path.dirname(경로), { recursive: true });
      fs.writeFileSync(경로, JSON.stringify(캐시), 'utf8');
    } catch (_) { /* 캐시를 못 써도 판정은 그대로다 — 다음 번에 한 번 더 물을 뿐 */ }
  }
  return 결과;
}

/* 용도마다 «무엇이 매달려 있나»가 다르다 — 죽었을 때 무엇이 멈추는지 그 줄에서 알아야 한다.
 * (09-03 사용량 화면 실측으로 갈랐다: 돈을 태우는 것은 그림·음악이고, 글은 20% 안이었다.) */
const 매달린것 = {
  글: ['몽골어 검문 ①역번역 ②문법 — 대외로 나갈 몽골어가 **지금 아무 자도 안 지난다**',
      '검수 러너 `--벤더 둘` 의 gemini 회차'],
  돈: ['그림 굽기(나노바나나 프로) · 음악(Lyria) · 목소리·음성 실측',
      '이쪽은 공짜 몫이 **원리상 없다**(구글 가격표 「Not available」) — 크레딧이 있어야만 돈다'],
};

/** 순수 판정 — 용도별 기록 묶음을 받아 줄로 바꾼다(회귀가 두 방향을 다 잰다). */
function 키생존축(전체) {
  const 적색 = [], 알림 = [], 셈 = {};
  /* 열쇠 하나짜리 옛 모양({때,기록})으로 불러도 「글」로 읽는다 — 옛 캐시·옛 호출을 깨지 않는다. */
  const 묶음 = 전체 && 전체.기록 ? { 글: 전체 } : (전체 || {});
  for (const 용도 of 용도들) {
    const a = 한열쇠축(용도, 묶음[용도]);
    적색.push(...a.적색); 알림.push(...a.알림);
    Object.assign(셈, a.셈);
  }
  return { 적색, 알림, 셈 };
}

function 한열쇠축(용도, r) {
  const 적색 = [], 알림 = [];
  const g = (r && r.기록) || {};
  /* 🔑 «몇 분 전»으로 적는다 — 장부는 세계시(UTC)라 그대로 찍으면 방금 잰 것이 «어제»로 읽힌다
   *   (첫 실물이 그랬다: 09-03 00:20 에 잰 값이 「09-02 15:20」으로 나왔다). 상대시간은 시간대가
   *   없어 오독할 자리 자체가 없다 — 유호님이 읽는 줄에는 이쪽이 맞다. */
  const 언제 = (() => {
    if (!r || !r.때) return '안 재봤다';
    const 분 = Math.max(0, Math.round((Date.now() - new Date(r.때).getTime()) / 60000));
    const 말 = 분 < 60 ? `${분}분 전` : `${Math.round(분 / 60)}시간 전`;
    return `${말} 실측`;
  })();
  /* 상태 코드로도 가른다 — 09-05 이전에 캐시된 기록은 종류가 「기타」인 채 503 을 들고 있다(캐시는 잰 때의 낱말을 쥔다). */
  const 서버쪽 = g.종류 === '서버·일시' || (Number(g.상태) >= 500 && Number(g.상태) <= 599);
  if (g.살았나 === false && 서버쪽) {
    /* 🔑 5xx = 구글 쪽이 지금 못 받는 것 — 열쇠는 죽지 않았다. 「죽었다 · 새 열쇠」로 울리면 처방이
     *   틀리고(새 열쇠도 503 을 받는다) 유호님 손만 탄다(09-05 실측 · 503 「high demand」 한 발).
     *   그래서 적색이 아니라 알림이고, 처방은 «잠시 뒤 다시 재기» 하나다. 다만 «살았다»도 아니다 —
     *   그 사이 몽골어 검문이 돌면 죽으므로 매달린 것은 그대로 적는다. */
    알림.push(`🟠 제미나이 「${용도}」 문이 **잠시 막혔다**(${언제} · ${g.상태 || ''} 구글 쪽 과부하·장애) — 열쇠 문제가 아니다. 새 열쇠를 만들지 않는다.`);
    if (g.사유) 알림.push(`   · 구글이 한 말: ${String(g.사유).slice(0, 160)}`);
    알림.push(`   · 그 사이 안 도는 것: ${매달린것[용도][0]}`);
    알림.push('   처방: 잠시 뒤 다시 잰다 — node tools/모델정책.js --제미나이확인' + (용도 === '돈' ? ' 돈' : ''));
  } else if (g.살았나 === false) {
    /* 🔑 **용도마다 «우는 크기»가 다르다**(유호 지시 09-03 「항상 무료먼저 사용하게 해줘」).
     *   글 = 상시 도는 자리라 죽으면 대외로 나갈 몽골어가 아무 자도 안 지난다 → 적색.
     *   돈 = 평소 안 쓴다. 그림 굽기·음성 실측 둘뿐이고 **자동으로 도는 것이 하나도 없다**
     *        (사람이 그 도구를 부를 때만 쓴다) → 죽어 있어도 멈추는 것이 없으므로 **알림**.
     *   안 쓰기로 한 자리가 매 세션 빨갛게 울면 그게 모래주머니고, 그러면 진짜 적색까지 안 읽힌다. */
    const 담을곳 = 용도 === '돈' ? 알림 : 적색;
    담을곳.push(`${용도 === '돈' ? '🟠' : '🔴'} **제미나이 「${용도}」 열쇠가 죽어 있다** (${언제} · ${g.상태 || ''} ${g.종류 || ''}) — 여기 매달린 것:`);
    for (const 줄 of 매달린것[용도]) 담을곳.push('   · ' + 줄);
    if (g.사유) 담을곳.push(`   · 구글이 한 말: ${String(g.사유).slice(0, 160)}`);
    담을곳.push(g.종류 === '자격'
      ? '   처방: 키 교체(9월 Standard 키 거부 예고) — AI Studio 「Key Type」 확인 뒤 새 키를 같은 파일에.'
      : 용도 === '돈'
        ? '   **지금 멈추는 것은 없다** — 그림·음성을 실제로 구울 때 충전하면 된다(AI Studio 결제 · 유호님 손).'
        : '   처방: 공짜 몫 열쇠를 새 칸에서 하나 더 만든다(글은 공짜다). 다시 재기: node tools/모델정책.js --제미나이확인');
  } else if (g.살았나 !== true) {
    /* 「키없음」은 «아직 안 만든 것»이라 다음 수가 분명하다 — 그건 적색으로 부르고 만드는 법까지 준다.
     * 네트워크·타임아웃은 «못 물어본 것»이라 알림이다(우는 검사는 꺼진다). */
    if (g.종류 === '키없음') {
      적색.push(`🔴 **제미나이 「${용도}」 열쇠 파일이 없다** — 여기 매달린 것이 통째로 안 돈다:`);
      for (const 줄 of 매달린것[용도]) 적색.push('   · ' + 줄);
      for (const 줄 of String(g.안내 || g.사유 || '').split('\n')) if (줄.trim()) 적색.push('   ' + 줄.trim());
    } else {
      알림.push(`🟠 제미나이 「${용도}」 열쇠를 **못 물어봤다**(${g.종류 || '까닭 미상'}) — 「살았다」가 아니라 «안 재봤다»다.`);
      if (g.사유) 알림.push(`   · ${String(g.사유).slice(0, 160)}`);
    }
  }
  return { 적색, 알림, 셈: { [`${용도}열쇠`]: g.살았나 === true ? '예' : g.살았나 === false ? '**아니오**' : null, [`${용도}잰때`]: 언제 } };
}

/* ── ㉣ 이미 도는 것들의 「마지막이 언제였나」 — 판정하지 않는다. 사실만. ── */
function 신선도() {
  const 줄 = [];
  /* 🔑 `거를까` — 장부에 **검수행이 아닌 줄**이 섞여 있으면 세지 않는다(09-03 무효 표식 · `종류` 칸이 있다).
   *   안 거르면 표식 한 줄이 「①배포 검수 N건 · 마지막 …」의 건수와 «마지막 시각»을 동시에 부풀려,
   *   검수가 안 돈 날도 방금 돈 것처럼 읽힌다(수는 자와 함께 적는다). */
  const 잰다 = (이름, 경로, 꺼내기, 거를까) => {
    const 행들 = jsonl(경로).filter((r) => (거를까 ? 거를까(r) : true));
    if (!행들 || !행들.length) { 줄.push(`   ${이름}: 장부 없음/0줄 — «안 재봤다»`); return null; }
    const 마지막 = 꺼내기(행들[행들.length - 1]);
    const 일 = 마지막 ? Math.floor((Date.now() - new Date(마지막)) / 86400000) : null;
    줄.push(`   ${이름}: ${행들.length}건 · 마지막 ${마지막 ? String(마지막).slice(0, 16).replace('T', ' ') : '(시각 못 읽음)'}${일 == null ? '' : ` (${일}일 전)`}`);
    return 일;
  };
  잰다('①배포 검수(Codex)', 검수장부(), (r) => r.시각, (r) => r && !r.종류);
  잰다('모델 정찰(전 벤더)', 정찰장부(), (r) => r.때 || r.시각);
  return 줄;
}

/* ── 스로틀: 같은 적색은 하루 한 번(rot-check 원칙 4 — 우는 검사는 꺼진다) ── */
function 도장읽기() {
  try { return JSON.parse(fs.readFileSync(도장경로(), 'utf8')); } catch (_) { return {}; }
}
/** @returns {string|null} 실패 사유 — **삼키지 않는다.** 조용히 실패하면 스로틀이 「있는 척」이 된다. */
function 도장쓰기(v) {
  try {
    fs.mkdirSync(path.dirname(도장경로()), { recursive: true });
    fs.writeFileSync(도장경로(), JSON.stringify(v), 'utf8');
    return null;
  } catch (e) { return e.message; }
}

/* ── 개명 사건 줄 적기 (2026-09-01 · 유호님 「개명 사건 줄 받게 지어줘」)
 *
 * 🔑 **장부를 고쳐 쓰지 않는다.** 옛 줄은 그때 참이었으므로 그대로 두고, 사슬을 잇는 줄을 얹는다.
 * 🔑 **처방이 우는 자리에 붙어 있다** — 끊김을 보는 자가 곧 그것을 닫는 자다. 처방을 다른 도구에
 *   두면 「우는 곳」과 「고치는 곳」이 갈리고, 그 사이에서 사람이 잊는다.
 * ⚠ 거절하는 자리 넷: 값 없음 · 새 파일이 없다(오타) · 옛 파일이 아직 있다(개명이 아니라 사본이다)
 *   · 그 이름을 가리키는 심문 줄이 장부에 없다(이으면 «아무 데도 안 닿는» 줄이 된다).
 *   강행 손잡이는 두지 않는다 — 넷 다 「사실이 아니면 적지 않는다」 쪽이다. */
function 개명적기(argv) {
  const i = argv.indexOf('--개명');
  const 값들 = [argv[i + 1], argv[i + 2]];
  if (값들.some((v) => !v || /^--/.test(v))) {
    console.error('실행 오류: node tools/ai스택점검.js --개명 <옛경로> <새경로> --사유 "왜 바뀌었나"');
    return 1;
  }
  const 사유i = argv.indexOf('--사유');
  const 사유 = 사유i >= 0 ? argv[사유i + 1] : null;
  if (!사유 || /^--/.test(사유)) {
    console.error('실행 오류: --사유 가 필요하다 — 사유 없는 개명 줄은 다음 사람에게 「알 수 없음」이다');
    return 1;
  }
  const 상대 = (p) => path.relative(ROOT, path.resolve(ROOT, p)).replace(/\\/g, '/');
  const [옛, 새] = 값들.map(상대);

  if (!fs.existsSync(path.join(ROOT, 새))) {
    console.error(`실행 오류: 새 경로가 없다 — ${새} (오타이거나 아직 안 옮겼다)`);
    return 1;
  }
  if (fs.existsSync(path.join(ROOT, 옛))) {
    console.error(`실행 오류: 옛 경로가 아직 있다 — ${옛}. 둘 다 있으면 «개명»이 아니라 사본이다.`);
    return 1;
  }
  const 행들 = jsonl(심문장부()) || [];
  if (!행들.some((r) => r && r.종류 === '심문' && r.대상 === 옛)) {
    console.error(`실행 오류: 장부에 «${옛}» 을 가리키는 심문 줄이 없다 — 이으면 아무 데도 안 닿는 줄이 된다.`);
    console.error('  장부가 가리키는 것: node tools/ai스택점검.js --json');
    return 1;
  }

  const 줄 = 장부해석.개명줄(옛, 새, 사유);
  try {
    fs.mkdirSync(path.dirname(심문장부()), { recursive: true });
    fs.appendFileSync(심문장부(), JSON.stringify(줄) + '\n', 'utf8');
  } catch (e) {
    console.error(`실행 오류: 장부에 못 적었다 — ${e.message}`);
    return 1;
  }
  console.log(`✅ 개명 줄을 얹었다: ${옛} → ${새}`);
  console.log(`   사유: ${사유}`);
  console.log('   이제 자와 review-runs 가 «최종 이름»으로 본다 — 끊김은 닫히고, 심문 뒤 자란 것은 드리프트로 뜬다.');
  return 0;
}

async function main() {
  const argv = process.argv.slice(2);
  const 훅 = argv.includes('--훅') || argv.includes('--hook');
  const 강제 = argv.includes('--force');

  if (argv.includes('--개명')) return 개명적기(argv);

  /* 표식 없는 몽골어 후보 목록 — 매 세션에 토하면 모래주머니라 «부를 때만» 낸다. */
  if (argv.includes('--몽골후보')) {
    const cov = 몽골커버리지();
    console.log(`표식 없는 몽골어 후보 ${cov.후보.length}벌 (키릴 ${키릴문턱}자 이상 · 많은 순)`);
    console.log('선언 ' + cov.선언.length + ' · 면제 ' + cov.면제.length + '\n');
    for (const c of cov.후보) console.log(`  ${String(c.키릴).padStart(6)}자  ${c.상대}`);
    console.log('\n정하는 법 — 그 파일 아무 곳에 한 줄(코드 표기 «밖»에):');
    console.log('  `<!-- 몽골어검문: 대상 -->`              우리가 내보내는 몽골어다');
    console.log('  `<!-- 몽골어검문: 면제 · 왜 면제인지 -->` 대상이 아니다(사유를 적어야 면제가 된다)');
    return 0;
  }

  const 축 = [
    ['②설계 심문 (Codex)', 심문커버리지()],
    ['제미나이 키 생존', 키생존축(await 키생존기록({ 재보기: argv.includes('--재보기') }))],
    ['몽골어 검문 (Gemini)', 몽골어검문()],
    ['㉠밖의 사실 (DR 둘)', 밖의사실축()],
  ];
  const 적색 = 축.flatMap(([, a]) => a.적색);
  const 알림 = 축.flatMap(([, a]) => a.알림);

  if (argv.includes('--json')) {
    console.log(JSON.stringify({ 축: Object.fromEntries(축.map(([n, a]) => [n, a.셈])), 적색, 알림 }, null, 2));
    return 0;
  }

  if (훅) {
    if (!적색.length) return 0;                         // 알림만 있으면 훅에서는 침묵한다(상시 소음 금지)
    const 오늘 = new Date().toISOString().slice(0, 10);
    const 옛 = 도장읽기();
    const 새것 = {};
    const 낼것 = [];
    for (const 줄 of 적색) {
      // 열쇠 = 줄의 앞머리(경로·문면). 흔들리는 값(건수)이 뒤에 오도록 위에서 문면을 짰다.
      const 키 = 줄.trim().slice(0, 80);
      새것[키] = 오늘;
      if (강제 || 옛[키] !== 오늘) 낼것.push(줄);
    }
    const 도장실패 = 도장쓰기(새것);
    if (!낼것.length) return 0;                         // 오늘 이미 말했다
    console.log('🧭 [AI 스택 점검] 역할은 배치돼 있는데 **안 돌고 있는** 자리다(알림 · 막지 않는다):');
    console.log(낼것.join('\n'));
    if (도장실패) console.log(`   ⚠ 스로틀 도장을 못 썼다(${도장실패}) — 하루 1회가 아니라 **매 세션** 다시 운다.`);
    console.log('   전부 보기: node tools/ai스택점검.js');
    return 0;
  }

  console.log('🧭 AI 스택 점검 — 세 벤더에 나눠 준 역할이 실제로 도는가\n');
  for (const [이름, a] of 축) {
    const 셈 = Object.entries(a.셈).map(([k, v]) => `${k} ${v === null ? '(안 재봤다)' : v}`).join(' · ');
    console.log(`■ ${이름}${셈 ? ` — ${셈}` : ''}`);
    if (!a.적색.length && !a.알림.length) console.log('   ✅ 걸린 것 없음');
    for (const 줄 of a.적색) console.log(줄.startsWith('   ') ? 줄 : '  ' + 줄);
    for (const 줄 of a.알림) console.log(줄.startsWith('   ') ? 줄 : '  ' + 줄);
    console.log('');
  }
  console.log('■ 이미 도는 것 — 마지막이 언제였나(판정 아님)');
  for (const 줄 of 신선도()) console.log(줄);
  console.log(`\n합계: 적색 ${적색.length}줄 · 알림 ${알림.length}줄`);
  console.log('🚫 심문 «드리프트»(심문 뒤 문서가 자란 것)는 여기서 안 센다 — 그 자는 .claude/hooks/review-runs.js 하나다.');
  return 0;
}

module.exports = { 심문커버리지, 몽골어검문, 밖의사실축, 키생존축, 키생존기록, 표식꼴, 도장경로, 생존캐시경로 };

if (require.main === module) {
  main().then((c) => process.exit(c)).catch((e) => {
    // 자가 죽은 것은 「정상」이 아니라 «확인 불가»다 — 종료코드로 갈라 말한다.
    console.error('🔴 AI 스택 점검이 못 돌았다:', (e && e.message) || e);
    process.exit(1);
  });
}
