'use strict';
/* 공유 선언판 — 여러 세션의 조율 상태(보드 선언·마찰 장부·인계문)가 사는 자리. **목록 판정은 여기 한 벌뿐이다.**
 *
 * 왜 있나 (F357 · 2026-08-13 유호 확정 「새는 목록을 좁히는 것은 부활이 아니다」):
 *   「반입에서 통째 제외」(tools/폰작업반입.js)와 「충돌 시 손 merge 금지 안내」(tools/작업본소유자.js)가
 *   각자 목록을 들고 있었다 — 한쪽은 옛 보드 2경로뿐, 한쪽은 장부 접두뿐. F250(세션별 조각 이주) 뒤로
 *   두 목록 다 새 자리를 못 담았고, 새는 방향은 언제나 「통과」였다: 브랜치 반입 한 번이 남의 보드 줄을
 *   되살리고 장부 해소 칸을 갈라진 시점 판으로 되돌릴 자리. 같은 판정을 두 곳에 적으면 갈라진다
 *   (CLAUDE.md 가드 맹점 ④) — 소비자는 전부 여기서 파생한다.
 *
 * ⚠ 이 목록의 기준은 「덜 중요해서」가 아니라 **「브랜치 판은 갈라진 시점에서 이미 낡아서」**다 —
 *   새로 넣을 땐 그 기준으로 판단한다. */

const 선언판경로 = [
  'docs/세션보드.md',           // 스텁(안내문) — 표 행은 여기 안 온다(F250)
  'docs/세션보드_아카이브.md',  // 완료 줄 무덤 — 늘 뒤가 최신이라 브랜치 판은 원리상 낡았다
  'docs/_ops/인계문.md',        // 파생 목차 — 저장 정본은 세션별 조각
  'docs/_ops/마찰신호.md',      // 동결 아카이브 — 열린 행 닫기는 조각(그림자)으로만
];
const 보드폴더 = 'docs/_ops/보드/';      // 세션별 보드 조각 — 파일 이름이 곧 주인(F250)
const 장부폴더 = 'docs/_ops/장부/';      // 마찰 장부 조각 — 한 파일 = 한 행(해소 칸 포함)
const 인계문폴더 = 'docs/_ops/인계문/';  // 세션별 인계문 조각
const 선언판폴더 = [보드폴더, 장부폴더, 인계문폴더];

const 선언판인가 = (f) => {
  const s = String(f);
  return 선언판경로.includes(s) || 선언판폴더.some((p) => s.startsWith(p));
};

module.exports = { 선언판경로, 선언판폴더, 보드폴더, 장부폴더, 인계문폴더, 선언판인가 };
