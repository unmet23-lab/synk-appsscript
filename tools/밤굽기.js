/*
 * 밤굽기 — 긴 배치를 «주무실 때» 한 프로세스로 돌린다 (유호 방침 08-25 결정.md
 *   「다른 작업 하고 잘 때만 굽는 게 제일 효율적일 것 같아」 — 낮에는 GPU 를 비운다).
 *
 * 🔑 **세션에서 떼어 띄운다.** 08-24 밤 실측: 세션이 죽으면 그 세션이 띄운 굽기가 전부 같이
 *   죽는다(밤샘 222장이 그렇게 사라졌다). 띄우는 법 — PowerShell 에서:
 *     $p = Start-Process -FilePath (Get-Command node).Source `
 *            -ArgumentList "<이 파일>", "<대기분>" -WindowStyle Hidden `
 *            -RedirectStandardOutput "<out>" -RedirectStandardError "<err>" -PassThru
 *   그리고 **부모가 죽었는지 확인**한다(ParentProcessId 가 이미 없으면 고아화 성공 = claude 와 무관).
 * 🔑 그리고 «한 프로세스»로 끝까지 간다 — 감시가 감시를 기다리는 사슬을 안 만든다.
 * 🔑 **띄운 직후 트랙.md 에 「돌고 있음 + 로그 절대경로 + 내 소유 경로」를 적고 산 옆 세션에 알린다.**
 *   안 하면 옆 세션이 「주인 없는 미커밋」으로 읽고 진행 중인 것을 걷어 간다(08-25 실물).
 *
 * ■ 08-26 «이어굽기» — 이 판이 무엇의 뒷부분인가 (유호 지시 「① 굽기 작업 이어서 해」)
 *   07:27 에 띄운 밤 배치가 **13:20 에 끊겼다.** 버그가 아니라 **재부팅**이다 —
 *   시스템 이벤트 1074 「13:21:55 · 시작 메뉴에서 다시 시작」. 그때까지 난 것:
 *     ⓪까몽 누끼 3(38.9분 · 종료 0) · ⓪a누끼 검사(성한것 3/3) · ⓪a2워프 지면 · ⓪b감정 시안 3(3.2분)
 *     · ⓪c까몽 확정 3(100.3분) · ①화면 14+덧씌움 14(14.8분 · 지면은 `b90a3cc2a` 로 고친 뒤 10:06 에 났다)
 *     · ②NPC 몸색 재료 8 + 판정 지면(73.6분 · 종료 0)
 *   끊긴 자리 = **③ 요소 라이브러리 52 의 23장째**(퍼프로브+글자 22 완주 · 오브 26 은 미착수).
 *   ⇒ 이 판은 **그 뒤부터만** 돈다. 위 여덟 단계를 여기 남겨 두면 다시 부를 때 ≈4시간을 헛돈다
 *     (이 머리말이 「그때의 우선순위다 — 다음에 쓸 때 다시 판단해서 고친다」고 적어 둔 그 자리다).
 *     지운 단계의 전문은 git 이력이 쥔다(`556c2fed4` 이전 판).
 *
 * ■ 이 판의 순서
 *   ③요소 라이브러리 «남은 것» → ④NPC 20 → ⑤NPC 시트 → ⑥검사
 *   🔴 **남은 수를 여기 적지 않는다** — 08-26 18:08 에 유호님이 「가벼운 것만 지금」을 고르셔서
 *      오브 21색이 낮에 따로 돌았다. 여기 굳은 숫자를 두면 그런 날마다 이 주석이 거짓말을 한다.
 *      수는 `--이어` 가 그 자리에서 «파일 시각»으로 센다. 밤에 이 파일을 그냥 부르면 ③은
 *      「전부 기준 뒤에 났다」로 스스로 건너뛰고 ④NPC 부터 간다 — 고칠 것이 없다.
 *   🔑 ③은 `--이어` 로 «시각»을 기준 삼아 남은 것만 굽는다. 안 좁히면 `--라이브러리만` 이 언제나
 *      `--다시` 를 넘겨 **이미 난 22장을 통째로 또 굽는다**(≈2.5시간). 옛 판도 이미 1800px 이라
 *      «가로폭»으로는 못 가른다 — 판정 축은 시각이다(트랙 §0).
 *   ⏱ 실측 근거: 오브 26 = 08-24 판에서 18:27→21:43 ≈3.3시간 · NPC 20 = 799초/장 ≈4.4시간.
 *      퍼프로브 잔여 4 는 십수 분. 합계 ≈8시간.
 *   ⚠NPC 20 은 **현행 정본(몸색 Oat)** 으로 굽는다. 처방 ①(역마다 유채 몸)은 유호 미판정이라
 *     역형상 표를 안 움직인다 — 미확정을 전량에 박으면 반려 때 20장이 통째로 버려진다.
 *     그 판정 재료(몸색 8장)는 ②에서 이미 났다.
 *   ⚠**배치 도는 중에 `tools/요소굽기.py` 를 고치지 않는다** — 세트굽기가 항목마다 이 파일을
 *     새로 읽어, 고치면 앞 렌더와 뒤 렌더가 갈리고 갈린 세트는 나중에 못 알아본다.
 *
 * 쓰기: node tools/밤굽기.js [대기분]      (대기분 없으면 즉시 시작)
 */
'use strict';
const { spawnSync, execFileSync } = require('node:child_process');
const fs = require('node:fs');
const os = require('node:os');
const path = require('node:path');

const 루트 = path.resolve(__dirname, '..');
const 대기분 = Number(process.argv[2] || 0);
/* ④⑤ NPC 는 기본에서 뺐다 — 지금 부르면 16장이 헛불이다(아래 ④⑤ 절의 실측).
 * 자가 «앞모습까지» 움직인 날에만 사람이 이 깃발로 연다. */
const 인자NPC = process.argv.includes('--NPC');

/* ③ 이어굽기의 기준 시각 — «이것보다 옛 파일만 다시 굽는다».
 * 끊긴 배치가 ③에 들어간 순간(11:18:30)을 쓴다: 그 앞에 난 22장은 이 기준 뒤라 건너뛰고,
 * 08-24 에 난 30장만 걸린다. 손으로 목록을 적지 않는다 — 목록을 적으면 그 줄이 다음 낡은 참조가 된다. */
const 이어기준 = '2026-08-26T11:18';   // 공백 없는 자로 준다 — 공백이 들면 띄우개(PowerShell)에서 argv 둘로 쪼개진다

/** 블렌더 찾기 — NPC굽기.js·오브굽기.js 와 같은 순서(환경변수 → 설치 폴더 최신). */
function 블렌더() {
  for (const k of ['BLENDER_EXE', 'BLENDER']) {
    if (process.env[k] && fs.existsSync(process.env[k])) return process.env[k];
  }
  for (const b of ['C:\\Program Files\\Blender Foundation', 'C:\\Program Files (x86)\\Blender Foundation']) {
    if (!fs.existsSync(b)) continue;
    for (const d of fs.readdirSync(b).sort().reverse()) {
      const p = path.join(b, d, 'blender.exe');
      if (fs.existsSync(p)) return p;
    }
  }
  return null;
}

/* 로그는 저장소 밖(임시 폴더)에 쓴다 — 굽기 로그를 git 이 쥐면 커밋마다 딸려 들어간다.
 * 대신 «어디에 쓰는지»를 첫 줄에 찍는다: 경로를 모르면 로그는 없는 것과 같다(08-25 배움). */
const 로그길 = path.join(os.tmpdir(), '밤굽기.log');

/* ── 완주 도장 ──────────────────────────────────────────────────────────────
 * 무엇을 — `docs/_ops/밤굽기도장.json` 에 «이 배치가 끝까지 갔나»를 한 벌 남긴다.
 * 왜 — 지금 이 파일의 마지막 줄은 *"아침에 낼 것"* 이고, 알림 배선은 이 파일과
 *   `밤샘_*.js` 둘을 합쳐 **0건**이다(08-29 실측). 즉 「밤 배치가 완주했나」의 판정이
 *   통째로 «사람이 아침에 폴더를 연다»에 매달려 있다. 08-26 엔 07:27 에 띄운 배치가
 *   13:20 에 **재부팅으로** 끊겼고, 그걸 안 것은 시스템 이벤트 로그를 뒤진 뒤였다.
 *
 * 🔴 **죽었을 때도 찍힌다.** 착수하자마자 도장을 먼저 박고, 끝까지 가면 덮는다.
 *   완주했을 때만 찍으면 「없음」이 「안 돌렸다」와 「죽었다」를 못 가른다 — 이 저장소가
 *   여러 번 데인 무늬다(0건이 성공 얼굴).
 *
 * 🔴 **`완주:false` 하나로는 모자란다 — 그 값이 «돌고 있다»와 «죽었다» 둘 다다**(08-30 지적).
 *   읽는 쪽(`rot-check`)은 나이(시각)를 실어 사람이 가르게 했지만, **찍는 쪽은 그 둘을 안다** —
 *   착수할 때와 죽을 때는 서로 다른 자리다. 그래서 `상태` 를 같이 남긴다:
 *     상태:"돌는중" = 착수 도장. 신선하면 정상, 아침까지 그대로면 **잡을 수 없는 죽음**
 *                     (재부팅·강제 kill — 이건 어떤 훅으로도 못 잡는다)
 *     상태:"멈춤"   = 잡은 죽음(예외·신호·비정상 종료코드). 사유에 그 자취가 있다
 *     상태:"완주"   = 끝까지 갔다
 *     파일 없음      = 아예 안 돌렸다
 *   ⚠ **`완주` 는 그대로 불리언으로 둔다.** 읽는 쪽이 `typeof o.완주 !== 'boolean'` 이면
 *   「규격 아님」으로 떨어뜨린다(`tools/rot-check.js` 밤굽기Section 실측 08-30) — `완주:null`
 *   로 세 상태를 표현하면 읽는 쪽이 통째로 눈이 먼다. `상태` 는 **덧붙이는 열쇠**라 안 깨진다.
 *   ⚠ 지금 읽는 쪽은 `상태` 를 **안 본다**(그 파일은 다른 트랙이라 여기서 못 고친다).
 *   그때까지 이 열쇠는 사람이 JSON 을 열어 읽는 몫이다 — 넘길 문안은 보고에 적었다.
 *
 * ⚠ **경계** — 도장은 「완주했나·죽었나」만 잰다. **「잘 구워졌나(미학)」는 재지 않는다.**
 *   그건 유호님 눈이 판정 축이고, 기계가 대신 찍으면 반려를 통과로 바꾼다.
 *   그래서 단계가 0 아닌 코드로 끝나도 `완주` 는 그대로 true 다(끝까지 갔으니까) — 그 사실은
 *   `사유` 에 적어 사람이 본다. 완주 ≠ 합격.
 *
 * 🔑 읽는 자리는 `tools/rot-check.js` 가 진다 — 여기는 찍기만 한다.
 * 🔑 tmp→rename 으로 바꾼다: 쓰는 도중에 죽으면 반쪽 JSON 이 남아 읽는 쪽이 크래시한다.
 */
const 도장길 = path.join(루트, 'docs', '_ops', '밤굽기도장.json');
const 무엇 = `밤굽기.js${대기분 ? ` (${대기분}분 대기)` : ''}`;
let 도장끝 = false;   // 마지막 도장을 이미 찍었나 — exit 훅이 덮어쓰지 않게
function 도장(상태, 사유) {
  try {
    const 임시 = `${도장길}.${process.pid}.tmp`;
    fs.mkdirSync(path.dirname(도장길), { recursive: true });
    // `완주` 는 `상태` 에서 «파생»한다 — 두 곳에서 따로 정하면 반드시 갈린다(한 값을 두 곳이 앎).
    const 벌 = { 시각: new Date().toISOString(), 무엇, 상태, 완주: 상태 === '완주', 사유 };
    fs.writeFileSync(임시, `${JSON.stringify(벌, null, 2)}\n`, 'utf8');
    fs.renameSync(임시, 도장길);
  } catch (e) { /* 도장 실패로 굽기를 멈추지 않는다 — 굽기가 본체다 */ }
}
const 단계기록 = [];
/* 잡을 수 있는 죽음은 전부 여기로 모은다 = `상태:"멈춤"`.
 * 잡을 수 없는 것(재부팅·강제 kill)은 착수 도장 `상태:"돌는중"` 이 그대로 남아 그것으로 드러난다. */
process.on('exit', (코드) => { if (!도장끝) 도장('멈춤', `끝까지 못 갔다 — 종료코드 ${코드} · 지나온 단계: ${단계기록.join(' · ') || '없음'}`); });
for (const 신호 of ['SIGINT', 'SIGTERM', 'SIGHUP', 'SIGBREAK']) {
  process.on(신호, () => { 도장('멈춤', `신호 ${신호} 로 끊겼다 · 지나온 단계: ${단계기록.join(' · ') || '없음'}`); 도장끝 = true; process.exit(130); });
}
process.on('uncaughtException', (e) => { 도장('멈춤', `예외로 죽었다 — ${(e && e.message) || e} · 지나온 단계: ${단계기록.join(' · ') || '없음'}`); 도장끝 = true; process.exit(1); });
const 말 = (s) => {
  const 줄 = `[${new Date().toTimeString().slice(0, 8)}] ${s}`;
  console.log(줄);
  try { fs.appendFileSync(로그길, 줄 + '\n'); } catch (_) { /* 로그 실패로 굽기를 멈추지 않는다 */ }
};
const 분 = (ms) => (ms / 60000).toFixed(1);
const 잔다 = (초) => { try { execFileSync('powershell', ['-NoProfile', '-Command', `Start-Sleep -Seconds ${초}`]); } catch (_) { /* */ } };

/* 🔴 프로세스를 셀 때 `tasklist /FI` 를 쓰지 않는다 — 한글 로케일·MSYS 경로 변환 때문에 늘 0건이다
 *   (트랙 §0 이 금지한 통로 · 08-25 에 옆 세션이 그것으로 「굽기가 멈췄다」를 오진했다). */
function 블렌더수() {
  try {
    return parseInt(String(execFileSync('powershell', ['-NoProfile', '-Command',
      '@(Get-Process blender -ErrorAction SilentlyContinue).Count'], { encoding: 'utf8' })).trim(), 10) || 0;
  } catch (_) { return 0; }
}

function 단계(이름, 실행) {
  말(`■ ${이름}`);
  const t0 = Date.now();
  const 코드 = 실행();
  말(`■ ${이름} 끝 · ${분(Date.now() - t0)}분 · 종료코드 ${코드}`);
  단계기록.push(`${이름.split(' ')[0]}=${코드}`);   // 도장 사유에 실릴 «지나온 자취»
  return 코드;
}
const 노드 = (도구, 인자들) => spawnSync(process.execPath, [path.join(루트, 'tools', 도구), ...인자들],
  { cwd: 루트, stdio: 'inherit' }).status;

const BL = 블렌더();
if (!BL) { console.error('🔴 blender.exe 를 못 찾았다 — BLENDER_EXE 로 경로를 준다.'); process.exit(1); }

말(`■ 밤굽기(08-26 이어굽기) 착수 — ${대기분 ? `${대기분}분 뒤 시작` : '즉시 시작'} · 로그 ${로그길}`);
/* 착수 도장 — 재부팅·강제 kill 은 훅으로 못 잡는다. 이 줄이 아침까지 남아 있으면 «죽었다»다. */
도장('돌는중', `돌고 있다 — 아직 안 끝났다 (이 도장이 그대로면 도중에 죽은 것이다) · 로그 ${로그길}`);
for (let i = 0; i < 대기분; i += 1) {
  잔다(60);
  if ((i + 1) % 15 === 0) 말(`  … 대기 ${i + 1}/${대기분}분`);
}
/* GPU 양보 — 옆 세션이 굽는 중이면 최대 20분 기다렸다 들어간다.
 * 무한 대기는 안 한다: 아침에 「아무것도 안 됨」이 「좀 느림」보다 훨씬 나쁘다.
 * (GPU 는 Arc iGPU 하나뿐이라 두 벌이 물면 서로 절반 속도가 된다 — 08-25 실측 88초 → 285초.) */
for (let i = 0; i < 40; i += 1) {
  const n = 블렌더수();
  if (n === 0) break;
  if (i === 0) 말(`  ⏸ 블렌더 ${n}벌이 돈다 — 최대 20분 양보`);
  잔다(30);
}
말(`■ 시작 — 블렌더 ${블렌더수()}벌`);

/* ③ 요소 라이브러리 — 남은 것만.
 *   08-24 에 기계로 세니 3D 자산 중 52장이 옛 자(AgX)에 남아 있던 그 자리다(§2-F).
 *   `--이어` 가 그중 «아직 옛 것»만 골라 낸다 — 이미 난 것을 또 굽지 않는다.
 *   전부 났으면 이 단계는 「건너뛴다」를 찍고 몇 초 만에 끝난다. */
단계(`③ 요소 라이브러리 — ${이어기준} 보다 옛 것만(남은 수는 그 자리에서 센다)`,
  () => 노드('명품재굽기.js', ['--라이브러리만', '--이어', 이어기준]));

/* ④⑤ NPC — 🔴 **기본에서 뺐다(08-30). 지금 부르면 통째로 헛불이다.**
 *   §2-G 는 **08-28 에 닫혔다** — 20/20 을 구웠고 앱(talk `assets/npc/*.webp` 16장)까지 갔다.
 *   그런데 이 자리는 `노드('NPC굽기.js', [])` 로 «언제나 처음부터» 20장을 굽고 있었다 = 4.4시간.
 *   그 옛 주석(「이 통로에는 이어굽기가 없다」)은 08-26 판이었고, **08-27 에 `--이어` 가 신설**돼
 *   낡아 있었다.
 *   🔴 그런데 `--이어` 로 고쳐도 **헛불이 안 막힌다** — 08-30 에 세어 보니 20장 중 **16장이
 *     자보다 옛것**으로 잡힌다(자 = `NPC세트굽기.py` 08-27 16:48). 그 자 이동을 열어 보면
 *     바꾼 것은 `뒤통수`·`묶음` 색 두 줄뿐이고, 그 조각은 **«돌아섬(back)»에서만 보인다**
 *     (08-28 세션이 그것을 실측으로 적어 뒀다 — 「바뀐 것은 `*-back.webp` 넷뿐」).
 *     ⇒ calm·lean·win 열두 장과 무대 넷은 **같은 그림에 2~4시간**을 태운다.
 *   🔑 **이 저장소가 이 병으로 데인 것이 이걸로 셋째다**(203장 · 까몽 확정 3 · NPC 16장).
 *     뿌리는 하나다 — **`--이어` 계열이 「파일 mtime」으로 재는데, 물어야 할 것은 「렌더에 닿은
 *     마지막 커밋」이다.** mtime 은 「자가 움직였나」만 알고 「그 움직임이 이 그림에 닿나」를 모른다.
 *     ⇒ 자를 고치는 것은 이 파일의 몫이 아니라 굽기 레인의 다음 과녁이다(트랙 §0 에 적어 뒀다).
 *   ⇒ 그때까지 이 둘은 **`--NPC` 를 줘야만 돈다.** 자가 진짜로 «전량에 닿게» 움직인 날
 *     (몸색·형상·털처럼 앞모습을 바꾸는 변경) 사람이 그 깃발로 연다. */
if (인자NPC) {
  단계('④ NPC 20장 — 1800px · 샘플 256 · 현행 정본 · 이어굽기', () => 노드('NPC굽기.js', ['--이어']));
  단계('⑤ NPC 시안 시트', () => 노드('NPC시트.js', []));
} else {
  말('■ ④⑤ NPC — 건너뛴다(기본). §2-G 는 08-28 에 닫혔고, 지금 굽으면 16장이 헛불이다.');
  말('   정말 필요하면 `node tools/밤굽기.js [대기분] --NPC` — 까닭은 이 줄 위 주석에 있다.');
  단계기록.push('④⑤NPC=기본건너뜀');
}

단계('⑥ 기계 검사 — 전량이 새 자로 났나', () => 노드('명품재굽기.js', ['--검사', '2026-08-25 07:33']));

말('■ 밤굽기 끝 — 아침에 낼 것: 오브 시안 + NPC_시안.html');
/* 완주 도장. 「끝까지 갔다」만 뜻한다 — **잘 구워졌는지는 유호님 눈이 판정한다.** */
도장('완주', `끝까지 갔다 · 단계별 종료코드: ${단계기록.join(' · ')} · 로그 ${로그길} — 그림이 «좋은가»는 여기서 안 잰다(사람 몫)`);
도장끝 = true;
