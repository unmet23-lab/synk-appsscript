# SYNK — Codex 작업 지침

> 2026-09-10. 공통 작업 기준은 [AI 운영 원칙](docs/AI_운영원칙.md) 한 곳에서 유지한다.

## 0. 직접 받은 일

한국어로 결과부터 설명한다. 요청 이해·조사·설계·제작·검증·통합·완료 보고까지 맡고 이미 받은 권한을 단계마다 다시 묻지 않는다. 단순 검토 요청은 검토 범위를 따른다.

시작·재개·중요한 수정 직전에 공통 원칙의 「세션 사이 최신 상태」를 따른다. 커밋 뒤처짐 0을 내용 동일로 해석하지 않는다. 이번 일의 현재 원문·승인 자산·기존 결과가 사용한 원본을 실제로 비교한다.

## 1. 기준과 품질

업무별 기준은 공통 원칙의 표와 [문서 지도](docs/문서_지도.md)에서 찾는다. 시각 작업은 synk-design, 학생 접점 글은 synk-brand를 먼저 쓴다. [명품 기준](docs/명품_기준_v1.md)의 해당 항목을 실제 결과로 확인한다. 원문·실물·게시 반영을 구분하고 미확인 범위를 알린다.

현재 규칙을 과거 보고·후보·개인 기억과 혼동하지 않는다. 값은 담당 원천에서 가져오고, 결과에는 사용 기준·적용 위치·확인 증거·남은 일을 짧게 남긴다. 전체 문서 독해나 긴 사용자 검사표를 기본으로 강제하지 않는다.

## 2. 실행과 분업

공식 도구·설치된 스킬과 네이티브 서브에이전트·worktree·재개·예약을 우선한다. 독립 작업을 나누고 받은 결과를 통합한다. 사용자용 새 작업은 사용자가 요청했을 때 만든다. 새 강제 훅·중앙 큐·고정 모델 호출 순서를 만들지 않는다.

원본 학생 데이터·브랜드 자산·제품 계약과 다른 담당의 미커밋·스테이징을 보존한다. 자격증명·학생 식별정보를 출력하거나 인계하지 않는다. 필요한 인증·신규 지출·서비스 보호 절차는 공통 원칙을 따른다. 자기 변경만 경로 지정해 커밋·push한다.

## 3. 기존 도구의 기술 실행자

`tools/codex-build.js`가 명시적으로 기술 실행자로 호출했을 때만 발주서의 범위·시험·출력 형식·커밋 계약과 [GPT 정본](docs/GPT_정본.md)의 해당 역할을 따른다. 범위 확장이나 실패는 호출자에게 근거와 함께 반환한다. 일반 직접 대화의 권한을 이 제한으로 줄이지 않는다.

## 4. Code Review Rules

한국어로 대상 파일·실제 줄·깨지는 동작·재현 입력을 적는다. P0는 데이터·인증·라이브 손상, P1은 기능 오류, P2는 품질 문제, P3는 추가 확인이 필요한 의심이다. 대외 약속·보장·수치·돈·소급 불가 변경은 검토 결과를 명시한다.

읽은 범위와 미확인을 밝힌다. 대상 자체를 못 읽었으면 첫 줄에 「확인 불가」. 지적 없음만으로 검증 완료를 선언하지 않는다. 같은 결함을 중복 집계하거나 실제 문제 없이 지적을 만들지 않는다. 과거 기각을 뒤집는다면 지금 달라진 근거를 설명한다.

## 5. 저장소 실무

`Code.js`·`엔진_*.js`는 전역 이름공간을 공유한다. 콘텐츠는 `contents_*.js`, 도구는 `tools/`, 관련 시험은 `node --test tests/<관련 파일>`이다. 형제 SYNK-talk는 그 자리의 지침과 제품·데이터·API 계약을 읽는다.

요청 범위 배포는 해당 배포 스킬·기존 도구로 실제 반영까지 확인한다. 완료한 일과 폐기한 계획은 기존 트랙·결과에서 즉시 정리한다. 클릭 가능한 절대경로로 결과를 제공한다.
