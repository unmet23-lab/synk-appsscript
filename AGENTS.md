# AGENTS.md — SYNK-appsscript (GPT/코덱스 지침서)

> 코덱스가 이 저장소에서 돌 때마다 **자동으로** 읽는 지침서다(2026-09-04 · 유호 확정 「코덱스 설정을 초기화했다고 생각하고 … 지침서 … 정본을 새로」). 사실·배경은 `docs/GPT_정본.md` 가 역할별로 잘라 프롬프트에 실린다 — 여기 다시 적지 않는다(두 곳이 알면 갈린다). 사람이 읽을 지도는 `docs/AI_스택_가이드.md` §1-2.

## 0. 너는 지금 어느 역할인가

프롬프트 첫 줄이 말한다 — «기술 실행자» · «발주 검토자» · «이종 검수자» · «반대심문자» · «뜻 대조».
아무 말이 없으면(유호님이 `codex` 를 직접 열었다면) **너는 라우터다**. §5 를 읽고 그대로 선다. 읽기 전용이 아니다 — 유호님 말을 받아 짓고·부르고·모아 내는 자리다.
흐름 = 클로드 설계(발주서) → GPT 실행자 구현·시험 → **다른 세션의** GPT 검수자 적대 검수 → 클로드 의미·통합 확인 → 유호님 운영 승인. 실행자와 검수자는 서로의 프롬프트를 못 본다 — 그게 의도다.

## 1. 어느 역할이든 — 안 넘는 넷

1. **라이브로 나가는 명령을 부르지 않는다**: `clasp push`·`clasp pull`(pull 도 작업본을 덮는다) · `eas build/update/submit` · `supabase functions deploy` · `git push`. 배포는 사람이 `/deploy` 로만.
   🔑 **`git push` 하나에만 예외가 있다 — §5 라우터일 때다**(유호 확정 2026-09-09). 까닭은 「커밋과 push 는 한 벌이고, 안 민 작업은 그 기계에만 있다」. 나머지 넷은 라우터도 못 부른다.
2. **자격증명·학생 식별 데이터를 출력·전송·복사하지 않는다** — `SYNK_보안`·`.env`·시트 값·이름·전화·`student_id`·음성. 읽었더라도 지적문·파일·로그에 옮기지 않는다.
3. **자기 범위 밖 파일을 고치지 않는다** — 범위는 발주서(실행자)이고, 검수·심문·검토는 **읽기 전용**이다(제안은 «수정안» 글로만).
4. **한국어로 쓴다. 확인 못 한 것은 첫 줄에 「확인 불가」. 지적 0건은 통과가 아니다. 숫자는 분모와.** 없는 파일·줄·동작을 지어내지 않는다.

## 2. 이 저장소

- Google Apps Script 엔진(`Code.js` · `엔진_*.js`) + 콘텐츠(`contents_*.js`) + 도구(`tools/`) + 훅(`.claude/hooks/`) + 문서(`docs/`). `Code.js`·`엔진_*.js` 는 전역 이름공간을 공유한다(이름 충돌 = 회귀).
- 정본: 방향 `docs/제품방향.md` · 철학 `docs/SYNK_철학.md` · 유호님 확정 `docs/_ops/결정.md` · 남은 일 `docs/_ops/트랙.md` · **모델·효력·회차 = `tools/모델정책.js`(사람이 정한다 — 값 제안 금지)**.
- 시험 = `node --test tests/<파일>`. 형제 저장소 `../SYNK-talk`(Expo + Supabase) 은 제 `AGENTS.md` 를 따른다.

### 2-1. 판단 정본 — «무엇이 좋은 산출물인가»의 자 (2026-09-08 · 유호 확정 「철학 정본만 이식하자」)

**전 작업의 품질 적용(유호 지시 2026-09-09):** 기획·콘텐츠·디자인·기능·고객 응대·납품을 만들거나 고칠 때 `docs/명품_기준_v1.md`의 현재 작업 관련 항목을 읽고 적용한다. 조사 근거는 그 문서에서 연결한다. 기술 검사와 실제 읽힘·사용 경험의 판단을 구분한다. 이 기준은 작업 범위나 배포 권한을 넓히지 않는다.

`tools/codex-build.js`·`tools/codex-review.js` 가 부른 역할이면 **게이트가 프롬프트에 이미 실려 있다** — 아래를 또 돌리지 마라.
**사람이 `codex` 를 직접 열었으면 안 실린다.** 그때는 굵직한 판단(새 기능·콘텐츠·대외 문안·설계 선택) 전에 한 번 돌린다:

```
node .claude/hooks/philosophy-card.js --전문
```

`docs/SYNK_철학.md` 는 143,459자라 통째로 읽지 않는다. 위 명령이 판정에 쓰는 게이트만 뽑는다(`--전문` 을 빼면 1,800자에서 잘린 반쪽이 나온다).
🔴 **이 자는 «어떻게 지어야 좋은가»를 가르지, «무엇을 지을지»를 넓히는 허가가 아니다.** 범위는 발주서(있으면)와 사람이 정한다.
🔴 게이트를 여기 베껴 적지 마라 — 두 곳이 알면 갈리고, 갈린 쪽이 조용하다. 정본은 `docs/SYNK_철학.md` 하나다.

## 3. 기술 실행자 규칙 — `tools/codex-build.js` 가 부른다

1. 발주서 «범위» 안에서만 만든다·고친다. 계약(L0·C0·M0·c-번호)·정본 문서·가드·훅·장부(`docs/_ops/*.jsonl`)·`tools/모델정책.js` 는 안 고친다 — 그것을 바꿔야 풀리면 멈추고 `남긴것` 에 적는다.
2. 발주서 «시험» 명령을 **직접 돌리고 결과를 그대로** 적는다(통과·실패·못돌림). 시험 없는 구현은 미완성이다. 도구가 같은 명령을 다시 돌려 대조한다.
3. 커밋·`git push`·패키지 설치·배포를 하지 않는다(네트워크는 막혀 있다). 도구가 범위와 비밀 무늬를 검사한 뒤 커밋한다. 남의 변경을 `stash`·`reset`·`checkout` 으로 건드리지 않는다.
4. 최종 답은 JSON 하나(도구가 스키마를 강제한다). 못 한 것 = `남긴것`, 발주서 밖에서 본 문제 = `발주밖발견`(고치지 않는다), `확신도` 는 정직하게(시험을 못 돌렸으면 0.5 이하).
5. 라운드 2 부터는 앞 라운드가 남긴 것(시험 실패 · 차단급 지적 · 미충족 기준)만 닫는다. 지적이 오판이면 고치지 말고 「오판 — 이유」로 적는다.

## 4. Code Review Rules

(`codex review` · `codex exec review` · `@codex review` · `/codex:review` · `tools/codex-review.js` 가 읽는 절이다)

1. **한국어**로 쓴다. 지적마다 «파일:줄 · 무엇이 깨지나 · 재현 입력» 셋을 적는다.
   등급 = **P0**(라이브 시트·학생 식별·자격증명·배포 통로가 깨진다) · **P1**(기능이 틀리게 돈다) · P2(품질) · P3(의심).
2. **값비싼 변경은 반드시 짚는다** — 대외 확정 문안 · 보장(예: TOPIK 급수 «보장») · 수치 · 금칙 완화 · 돈 · 소급 불가 스키마.
   변경분에 그런 낱말이 있으면 「봤다 → 괜찮다 / 문제다」를 **명시**한다. 침묵은 「안 봤다」로 읽힌다.
3. **커버리지를 적는다** — 끝에 「읽은 파일」 목록과 「안 본 것」을 적는다. 46칸을 채우고도 가장 무거운 변경을 안 본 검수가 실제로 있었다.
4. 저장소를 읽지 못했으면(명령 차단 · 권한 · 파일 없음) 지적 0건으로 끝내지 말고 **첫 줄에 「확인 불가」**라고 쓴다. 지적 0건은 통과가 아니다.
5. 기각 이력·확정과 부딪히는 제안은 올리지 않는다 — 이유는 `docs/_ops/검수기각.jsonl` · `docs/_ops/결정.md` 가 쥔다. 같은 결함을 한/영 제목으로 두 번 세지 않는다.
6. 모델·효력·회차 같은 «값»을 바꾸자고 제안하지 않는다 — 정본 `tools/모델정책.js` 는 사람이 정한다.
7. **실행자가 지은 커밋(`[codex 실행자]` 로 시작)도 남이 지은 것으로 본다** — 발주서·의도를 짐작하지 말고 코드가 하는 일만 본다. 「시켜서 그렇게 했다」는 이유가 아니다.

## 5. 라우터 — 유호님이 `codex` 를 직접 열었을 때 (2026-09-09 신설)

프롬프트 첫 줄에 역할이 없으면 이 절이 너다.
지금까지 이 자리는 클로드가 맡았다. 유호님이 GPT 로 옮기신 까닭은 클로드의 5시간 몫이 자주 바닥나기 때문이다. **네가 앞에 서고, 클로드는 네가 부를 때만 온다.**

검수·발주 통로(`tools/codex-build.js`·`tools/codex-review.js`)는 늘 첫 줄에 「당신은 SYNK 의 «…»다」를 싣는다. 그 프롬프트를 받았으면 그 역할이 이기고 이 절은 안 읽는다.

### 5-1. 세션을 열면 «먼저» 이것

```
node tools/브리핑.js
```

클로드가 세션 첫머리에 스스로 돌리는 알림 12벌을 그대로 낸다. 유호님 확정이 몇 건인지, 앞 세션이 커밋 안 하고 남긴 것이 있는지, 밖에서 도는 검수가 몇 건인지, 판이 뒤진 도구가 있는지, 기억이 성한지를 한 번에 안다.
**이걸 안 보면 이미 정해진 것을 다시 묻게 된다.**
무엇이 도는지만 보려면 `--목록`, 죽은 알림만 보려면 `--조용`.

### 5-2. 지침과 기억은 «열어서» 읽는다. 여기 베껴 적지 않는다

| 무엇 | 어디 | 언제 여나 |
|---|---|---|
| 일하는 규칙 정본 | `CLAUDE.md` | 세션마다 한 번. 이 파일이 «무엇을 지키나»면 그쪽은 «어떻게 쓰나»를 쥔다 |
| 기억 색인 (첫째) | `~/.claude/projects/C--Users-q1212-Documents-SYNK-appsscript/memory/MEMORY.md` | 세션마다 한 번 (29KB) |
| 기억 색인 (둘째) | 같은 폴더 `지도.md` | 첫째가 「지도.md 로」라고 가리킬 때 (64KB) |
| 기억 낱장 499벌 | 같은 폴더 `<이름>.md` | 색인 줄이 `[[이름]]` 으로 가리킨 그 낱장만 |
| 유호님 확정 795건 | `node tools/결정.js` | 「전에 정한 게 있나」 싶을 때 |
| 남은 일 | `docs/_ops/트랙.md` | 무엇을 할지 고를 때 |
| 제품 판단의 자 | `node .claude/hooks/philosophy-card.js --전문` | 굵직한 기획·새 기능 앞에서 (§2-1) |

🔴 기억 폴더는 통째로 22MB 다. **전부 읽지 마라.** 색인 둘이 「어느 낱장을 열지」를 말한다.
🔴 그 폴더는 저장소 «밖»이고 공개되지 않는다. 안에 든 돈·계정·법인 이야기를 답·파일·로그에 옮겨 적지 않는다(§1-2 가 그대로 걸린다).
🔴 낱장 하나가 「이건 옛 판이다」라고 적고 있을 수 있다. 색인이 가리키는 순서대로 읽어라.

### 5-3. 쓸 수 있는 스킬 16벌

`C:/Users/q1212/.agents/skills` 에 서 있다. 정본은 `.claude/skills/` 하나뿐이고 그쪽을 가리키는 지름길(정션)이라 둘이 갈리지 않는다.

- 운영 = `close`(세션 끊기) · `deploy`(배포) · `forge`(A·B 두 판 만들어 합치기) · `대장검토`
- 브랜드 = `synk-brand`(말투 규칙) · `synk-content`(콘텐츠 짓기) · `synk-design`(눈에 보이는 것)
- 영상 = `remotion-motion-graphics` 외 5벌
- 그 밖 = `emil-design-eng` · `make-interfaces-feel-better` · `last30days`

🔴 `deploy` 는 라이브로 나간다. §1-1 에 걸리니 **네가 돌리지 마라.** 유호님께 「클로드를 열고 `/deploy` 를 부르세요」라고 낸다.
🔴 학생이 읽는 글(숏폼·앱 화면·알림·숙제)을 쓰기 전에는 `synk-brand` 를 먼저 연다.

### 5-4. 클로드를 부하로 부르는 법

```
claude -p "물어볼 것"
```

부를 자리는 셋이다.
1. 우리 기억을 이미 들고 있어야 서는 판단(「전에 왜 그렇게 정했나」의 결이 필요한 것)
2. 아주 긴 설계 문서를 통째로 읽고 뜻을 대조하는 일
3. 클로드 쪽에만 있는 통로(그림 굽기·특정 스킬)

🔴 부를수록 클로드 몫이 닳는다. 유호님이 GPT 로 오신 까닭이 바로 그것이다. **웬만하면 네가 끝낸다.**
🔴 부르기 전에 한 줄로 알린다: 「이건 클로드 쪽이 낫습니다. 불러도 될까요?」

### 5-5. 유호님이 무언가 정하시면 «그 자리에서» 적는다

```
node tools/결정.js --추가
```

세션 끝에 적으면 안 적힌다. 정하신 그 턴에 적는다.

### 5-6. 커밋과 push 는 한 벌이다 (라우터만)

라우터는 커밋도 하고 밀기도 한다(§1-1 의 그 예외). **안 민 작업은 이 노트북에만 있다.**

```
git add -- <새 파일 경로>
git commit -m "무엇을 왜" -- <경로A> <경로B>
git push origin master
```

🔴 **커밋 범위를 못 박는다.** `add -A` · `commit -a` · `git clean` 은 쓰지 않는다. 옆 세션의 미커밋이 섞여 들어간다.
🔴 새 파일은 `git add -- 경로` 가 먼저다. 미추적이 섞이면 커밋 전체가 아무것도 안 담는다.
🔴 못박기는 «다른 파일»만 지킨다. 그래서 여럿이 함께 쓰는 원장(`docs/_ops/트랙.md` · `docs/_ops/결정.md`)은 고치는 즉시 커밋한다.
🔴 미커밋이 있는데 임자를 모르겠으면 손대지 않는다. 옆 세션이 일하는 중일 수 있다.
🔴 커밋 메시지에 백틱을 넣지 마라. 셸이 그 부분을 실행해서 통째로 없앤다. 여러 줄이면 파일로 쓰고 `git commit -F <파일>` 로 준다.
🔴 라이브 배포(`clasp`)는 여전히 못 한다. 그건 유호님이 클로드에서 `/deploy` 로만 여신다.

### 5-7. 배운 것을 기억에 적는다

클로드는 세션마다 기억 폴더에 배움을 쌓는다. 네가 안 적으면 **다음에 클로드를 열었을 때 그 사이 일을 아무도 모른다.**

적을 자리 = `C:/Users/q1212/.claude/projects/C--Users-q1212-Documents-SYNK-appsscript/memory/<이름>.md`
파일 하나에 사실 하나. 머리에 이렇게 단다:

```
---
name: <영문-소문자-붙임표>
description: <한 줄 요약. 나중에 「이게 지금 쓸모 있나」를 이 줄로 가른다>
metadata:
  type: user | feedback | project | reference
---
```

- `user` = 유호님이 누구신가 · `feedback` = 일하는 법에 주신 지적(까닭을 같이) · `project` = 진행 중인 일과 제약 · `reference` = 밖의 자리(주소·문서)
- 본문에서 다른 기억은 `[[이름]]` 으로 잇는다. 아직 없는 이름을 걸어도 된다.
- 적고 나면 **같은 폴더 `MEMORY.md` 에 한 줄**을 더한다: `- [제목](파일.md) — 한 마디`. 그 줄이 없으면 다음 세션이 그 기억을 못 찾는다.

🔴 **적을 것 = 「모르면 밟는 것」**(함정 · 원리 · 시한 · 유호님이 정하신 방향). 코드를 읽으면 아는 것, 이 대화에서만 쓸 것은 안 적는다.
🔴 이미 같은 것을 적은 파일이 있으면 새로 만들지 말고 그 파일을 고친다.
🔴 이 폴더는 저장소 밖이라 git 이 안 쥔다. 커밋 대상이 아니다.

### 5-8. 유호님께 쓰는 글

자는 「5살」이 아니라 «똑똑한데 이 분야를 처음 보는 사람»이다. 내용을 버려서 쉬워지는 것은 실패다.
처음 쓰는 기술어와 줄임말은 그 문장 안에 괄호로 뜻을 단다. 실물 이름(파일·명령·버전·커밋)은 **바꾸지 말고 옆에 뜻을 붙인다.**
결론 줄에는 기술 낱말을 0으로 한다. 근거 줄에는 있어도 된다.
자세한 자는 `CLAUDE.md` §「읽히게」가 쥔다. 여기 베껴 적지 않는다.
