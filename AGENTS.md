# AGENTS.md — SYNK-appsscript (이종 에이전트용 포인터)

> 이 파일은 Codex·Gemini 계열 에이전트가 읽는 «포인터»다(2026-09-02 · 유호 「전부 진행해줘」).
> 규칙 본문은 `CLAUDE.md`(클로드 코드 전용 낱말이 섞여 있으나 규칙 자체는 벤더 무관)와
> `docs/AI_스택_가이드.md` 가 쥔다. 여기 다시 적지 않는다 — 두 곳이 알면 갈린다.

## 이 저장소

- Google Apps Script 엔진(`Code.js` · `엔진_*.js`) + 콘텐츠(`contents_*.js`) + 도구(`tools/`) + 훅(`.claude/hooks/`) + 문서(`docs/`).
- **라이브 배포는 사람이 `/deploy` 로만 한다.** 에이전트는 `clasp push` · `clasp pull` 을 절대 부르지 않는다(pull 도 작업본을 덮어쓴다).
- **자격증명 · 학생을 식별하는 데이터는 출력·전송하지 않는다.** 시트 값·이름·전화번호가 보이면 지적문에도 옮기지 않는다.
- 실행 통로는 읽기 전용이다 — 코드를 고치지 않는다. 제안은 «수정안»으로만 적는다.

## Code Review Rules

(`codex review` · `codex exec review` · `@codex review` · `/codex:review` 가 읽는 절이다)

1. **한국어**로 쓴다. 지적마다 «파일:줄 · 무엇이 깨지나 · 재현 입력» 셋을 적는다.
   등급 = **P0**(라이브 시트·학생 식별·자격증명·배포 통로가 깨진다) · **P1**(기능이 틀리게 돈다) · P2(품질) · P3(의심).
2. **값비싼 변경은 반드시 짚는다** — 대외 확정 문안 · 보장(예: TOPIK 급수 «보장») · 수치 · 금칙 완화 · 돈 · 소급 불가 스키마.
   변경분에 그런 낱말이 있으면 「봤다 → 괜찮다 / 문제다」를 **명시**한다. 침묵은 「안 봤다」로 읽힌다.
3. **커버리지를 적는다** — 끝에 「읽은 파일」 목록과 「안 본 것」을 적는다. 46칸을 채우고도 가장 무거운 변경을 안 본 검수가 실제로 있었다.
4. 저장소를 읽지 못했으면(명령 차단 · 권한 · 파일 없음) 지적 0건으로 끝내지 말고 **첫 줄에 「확인 불가」**라고 쓴다. 지적 0건은 통과가 아니다.
5. 기각 이력·확정과 부딪히는 제안은 올리지 않는다 — 이유는 `docs/_ops/검수기각.jsonl` · `docs/_ops/결정.md` 가 쥔다. 같은 결함을 한/영 제목으로 두 번 세지 않는다.
6. 모델·효력·회차 같은 «값»을 바꾸자고 제안하지 않는다 — 정본 `tools/모델정책.js` 는 사람이 정한다.
