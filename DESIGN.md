# SYNK DESIGN.md — 시각 산출물 공통 규칙 (AI 진입로)

> **상태**: ✅**확정** · 2026-08-09 — 판단 정본(브랜드 킷·폰트는 08-09 전면 개편에서도 「유지」로 확정).

> **눈에 보이는 것을 만들기 전에 이 파일부터 읽는다** — HTML·인쇄물·발표물·웹앱·앱 화면·SNS 이미지·지도/표 전부.
> **값의 정본은 각 링크 문서다.** 이 파일은 요약+진입로이고, 어긋나면 정본이 이긴다(지키던 회귀는 압축 08-20으로 철거 — 어긋남은 눈으로 잡는다).
> 외부 디자인 스킬(§6)과 충돌하면 **SYNK 킷이 항상 이긴다.** 스킬은 킷이 침묵하는 디테일만 채운다.

## 1. 정체성 — SYNK FIELD

컨셉 정본 = [디자인 컨셉 정본](docs/디자인_컨셉_정본_v1.md) — 전 산출물의 「왜」가 있다.

- 3규칙: **신호는 하나(Coral)** · **위계는 색이 아니라 밀도**(크기·웨이트·여백) · **타이포는 안 건드린다**.
- **품질 축(유호님 확정 08-05)**: 기능 0순위 · 디자인 0.1순위 — 사실상 동급. 어느 쪽도 깎아 다른 쪽을 사지 않는다. 지향 = 「기능 좋고 디자인 트렌디한 명품」.
- 🚫 **이모지 과다 금지(앱·웹앱 UI)** — 구 Glide처럼 이모지로 위계·장식·아이콘 대용을 하지 않는다. 위계·신호는 킷(색 1점·웨이트·여백)이 진다. 이모지는 콘텐츠 속 액센트 소량만(절제이지 전면 금지 아님 — 기존 라이브의 소급 제거는 유호님 승인부터).
- 모티프 = **시냅스 노드·링크** — 이름 SYNK(=동기화)의 그림이다. 🚫 게르 천창 모티프(유호님 판단 대기).
- 🚫 타사 DESIGN.md(Apple·Notion 등) 반입 금지 — 킷과 섞이면 정체성이 죽는다. 참고 열람만(§7).

## 2. 색 — 2027 킷 「내일 꾸러미」 (유호님 확정 2026-08-20)

값의 기계 원천 = `docs/디자인_토큰.json`(**42색** = 코랄 램프 5+Wash · 바탕 4(Paper·Ink·**Ink Deep**·Stitch)+양모 회색 4 · 2027 실 4꾸러미×3단 · KC 4 · ⏳퇴역 대기 12). 판정 재료 전문 = 「내일 꾸러미」 아티팩트(1~4차 시안 대조 · WGSN·Coloro 2027 키컬러 · 대비 실측 전량).

🔑 **두 원리**(이 킷의 문법 전부):
① **색이 아니라 램프다** — 유채색은 전부 3단(보풀 빛 Soft · 몸통 · 그늘 Deep). 펠트 섬유 결은 한 색 안의 밝음 폭이 보여준다(마스코트 램프가 5단인 이유). 몸통은 **면의 색**이고 글자는 Deep 이나 Ink 가 진다.
② **실은 부품, 램프가 정본** — 2027 실(청금석·메도우·버터·팝)은 유행이 지나면 그 실만 뽑아 바꾼다. 구조는 남는다.

| 층 | 이름 | HEX | 요점 |
|---|---|---|---|
| 바탕(라이트) | Paper | `#FBF7F0` | 양모 종이 — 순백 아님 |
| 잉크(라이트) | Ink | `#2B2320` | 따뜻한 먹(Paper 위 14.42) |
| **실땀** | **Stitch** | `#F0E3C8` | 모든 펠트 오브젝트의 테두리·구분선(운용 규칙 ②) |
| 양모 회색 1~4 | Oat·Stone·Ash Wool·Deep Wool | `#EDE7DC` `#C7BFB2` `#8D857A` `#575046` | 칩 바닥·비활성·보조 글자·보조 강조 |
| **주인공 = 코랄 램프 5단** | Coral Soft·2·**Coral**·3·Rim | `#FBB7A3` `#FD9C87` **`#F96859`** `#AE322A` `#941F19` | **마스코트 평상복 램프 그대로** — 행동·신호·오답(Coral 3) |
| 신호 여린 면 | Coral Wash | `#FEF0E9` | 코랄이 앉는 바닥 |
| 길잡이(안내·힌트·링크) | Lapis Soft·**Lapis**·Deep | `#C5D6F5` **`#3D6BC9`** `#24448C` | 2027 올해의 색 · ⚠**면 위 글자는 Paper**(Ink 3.04 미달) |
| 자람(정답·성장) | Meadow Soft·**Meadow**·Deep | `#D3E8BE` **`#7DB45A`** `#3F6B2E` | ⚠몸통은 **면 전용**(종이 위 글자 2.30) |
| 기쁨(별·보상) | Butter Soft·**Butter**·Deep | `#FFEBB0` **`#F5C445`** `#7E5A10` | 면 위 Ink 글자 9.43 |
| 특별함(기념·희귀) | Pop Soft·**Pop**·Deep | `#F8C7DE` **`#E05C97`** `#99295D` | 면은 큰 글자 전용(3.20) |
| K컬처 전용 4색 | KC Hot Pink·Pink 2·Sun·Cool Blue | `#FF3E88` `#FF6BA8` `#FFD447` `#4E7CFF` | Part 6 밖 반입 금지 · Pop 흡수 여부 후속 판정 |
| ⏳퇴역 대기 12 | Chalk 3종·Graphite 4종·Ash 2종·Lime 2종·Emerald | (토큰 참조) | 다크 시맨틱·소비처 재배선 후 삭제(다크 팔레트 트랙) — **새 산출물에 쓰지 않는다** |

**철칙 4** — (2027 판) ① 순백·순검정 금지(라이트 하한 = Paper `#FBF7F0` · Ink `#2B2320` / 다크 하한은 퇴역 대기 무채가 임시로 진다). ② 라이트 배경에 몸통색 **글자** 금지 — Coral·Meadow 는 면이고, 글자는 그 실의 Deep(코랄이면 Coral 3). ③ 몸통 면 위 글자는 실마다 정해져 있다: 코랄·버터 = **Ink** / 청금석 = **Paper** / 팝 = Ink(큰 글자) — 외우지 말고 토큰 직책을 읽는다. ④ **주연 1실 + 조연 1실** — 한 화면의 유채 실은 둘까지, 나머지 실은 그 화면에서 쉰다(독무대 정신 · KC 4색은 Part 6 전용 그대로).

구 조항 **ⓔ**(Slate→Ash)·**ⓕ**(Lime 2)·**ⓖ**(Emerald)는 퇴역 대기 12색과 함께 역사가 됐다 — 반려 이력은 컨셉 정본에 보존, 새 산출물은 위 표만 본다. 픽셀 집행 = `node tools/브랜드렌더린트.js`.

## 2-b. 마스코트 색 — 킷과 **한 몸이 됐다** (체리 퇴역 08-19 · 2027 킷 08-20)

체리(빨간 구판)는 **퇴역했다**(유호님 확정 2026-08-19 — 자산·배선 걷힘). ⚠**되살아남을 막는 회귀는 아직 없다**(실측 08-20: `tests/마스코트정본.test.js` 는 존재하지 않는다). 경로 정본 = `tools/lib/마스코트자산.js` 하나 · 렌더 층 판정 = `tools/브랜드렌더린트.js`(`MASCOT = {}`). 🔴토큰 `색.마스코트.램프`(체리 5단)는 **아직 남아 있다** — `tools/중성축.js`·`tools/펠트패치.py` 가 읽고 있어 지우면 깨진다(별건). 의상은 이제 **펠트 코랄 하나**고, 2027 킷이 그 램프를 킷 코랄 축으로 그대로 승격했다 —
`색.마스코트.평상복램프` 와 킷 `Coral Soft~Rim` 이 **같은 값**이다(마스코트 색 = UI 색 · 4차 확정의 핵심).

남는 규칙 둘:
① **바닥** — 코랄 짙은 면 위에 마스코트를 놓지 않는다(여린 단이 먹혀 윤곽이 뭉갠다). 판정은 목록이 아니라
**계산**이다(`tools/브랜드렌더린트.js` ΔE 임계 12 — 손 목록은 안 잰 색을 조용히 금지한다).
② **인라인하면 `data-synk-mascot`** — 린트는 `src` 경로로 그림을 찾아서, data URI 로 바꾸면 통째로 안 보인다.

## 2-c. 앱 모드 = **다크 단일** (유호님 확정 2026-08-14)

앱(SYNK-talk)은 다크 한 벌이다. **라이트 모드를 짓지 않는다** — 미룬 것이 아니라 킷이 못 짓게 되어 있다.
🔴 근거는 마스코트가 아니라 **답장 화면의 오류 태그(13px)** 다 — 이 앱에서 코랄이 «글자로» 서는 유일한
자리인데(`테마.신호자리.답장`), 라이트에서는 철칙 ②가 코랄 글자를 크기 불문 막고 대체값 `Coral 3` 은
24px↑/18.7px↑bold 한정이라 **놓을 킷 값이 0개**다. 라이트판 답장 화면은 신호 1점을 잃은 채로만 선다.
🚫 **「접근성 때문에 라이트가 필요하다」 재제안 금지** — 이 앱에선 반대가 실측됐다(라이트 번역이 *모든* 글자
층에서 대비가 낮다). 밝은 곳 대응은 폰의 화면 밝기가 진다.
🔑 **마스코트를 근거로 쓰지 않는다** — 다크·라이트 둘 다 임계를 넘어 모드를 못 가른다.
**대비 실측 전량**(층별 숫자·모드별 대조·집행 전후)은 git 이력의 판정 재료에 있다(`docs/앱_모드판정_라이트vs다크.html` — 압축 08-20으로 삭제) · 다시 재는 계측 = `node tools/모드대비계측.js`(값은 talk `src/테마.js` 원문 파생).

## 2-d. 앱 다크 = **「양모 밤」** (유호님 확정 2026-08-20 · 조항 ⓚ)

2027 킷(조항 ⓙ)이 라이트만 갈아입혀 **앱에 2027 킷이 안 닿아 있었다.** 그 빈칸을 이걸로 메운다.

**낮의 글자가 밤의 면이 된다** — 바탕 `Ink Deep`(신설) · 카드 **`Ink`**(라이트의 잉크) · 잉크 `Paper` ·
보조 `Oat` · 보조2 `Stone` · 실땀 `Stitch`. 한 벌의 양모를 낮과 밤에서 뒤집어 입는 구조다.

🔑 **다크에 새 «실»은 없다** — 원리 ①「색이 아니라 램프다」를 반대편에서 읽는다:
라이트는 면=몸통·글자=Deep, **다크는 면=Deep·글자=Soft.** 안내(청금석)·정답(메도우)·보상(버터)·
기념(팝)이 같은 램프로 밤을 산다. 성장은 `Meadow Soft` 가 진다(구 `Lime` 은 지면 층에만 남는다).

**면 2단은 그대로**(유호님 확정 08-14 「면 2단 보강」) — 판정 눈금은 대비비가 아니라 **ΔL\***다:
옛 카드 `Graphite 2` 가 8.3 이어서 「안 떠 있다」로 기각됐고, 확정된 `Graphite 3` 이 12.7 이다.
**`Ink` on `Ink Deep` = 12.8** 로 그 통과선 위에 세웠다.
🔴 **카드를 더 밝히지 않는다** — 밝힐수록 3층 글자(`Stone`)와 코랄 13px 이 동시에 후퇴한다.
사다리가 `Paper`→`Oat`→`Stone` 인 이유는 조항 ⓔ 다: **1·2층은 밀도가 가르고, 3층에서만 잉크가 물러선다.**

판정 재료(폰 목업 3안 + 대비 19칸 실측) = `docs/다크팔레트_판정_0820.html` · 굽기 `node tools/다크팔레트굽기.js`.

⚠ **형제 저장소(SYNK-talk)의 테마 값은 아직 무채다** — 저장소가 달라 여기 CI 가 안 보므로,
그쪽 `테마.js`·`테마면.test.js` 반영은 **별건**이고 이 조항이 그 작업의 근거다.
⏳ **Loom 지면 층(문서·소개서 화면)은 아직 무채다** — 구 무채 12색은 그 층 전용으로 살아 있고,
지면이 양모로 옮겨질 때 퇴역한다(`tools/lib/loom.js` 의 `쓰는색`).

## 2-d. 재질·율·부품 — Loom (유호님 지시 2026-08-16)

정본 = [Loom 엔진 설계](docs/Loom_엔진_설계.md) · 값 = `디자인_토큰.json` 「재질·율·부품」 ·
**통로 = `tools/lib/loom.js` 하나** (`node tools/lib/loom.js --css 캐러셀|문서|인쇄|웹|부품만`).
🚫 **이 통로 밖에서 지면 CSS 를 손으로 쓰지 않는다** — 손으로 쓰면 그 순간 지면마다 갈린다
(실측 08-16: 캐러셀 8/8·빌더 3/3 이 각자 CSS 를 지녀 Loom 도달이 **93 HTML 중 6**이었다).

🔴 **정보 판 재질 재심 중 — 2026-08-20 유호님**: UI·UX 장식 재질은 **펠트 단독**(하한 = 명품 ·
`docs/발표물/_브랜드킷.md` §0-b)이 확정이고, 정보 판(반투명 판·판 바닥)은 **패턴지 vs 유리 판정 보류** —
둘 다 집행급으로 구운 뒤 실물로 결정한다(유호님 「전부 다 제대로 만들고 나중에 결정」).
판정 전까지 아래 유리 조항을 «확정»으로 인용하지 않는다.

- **재질 3종은 «빛을 다루는 방식»으로 갈린다** — 색이 아니라 **물성**이 위계를 낸다(§1 「밀도」의 확장).
  **펠트**=빛을 먹는다(사람이 만든 것 · 표식·표지 오브 · 1~2점) → **유리**=통과시킨다(담는 것 · 판·번호·태그 · 제한 없음)
  → **레진**=가둔다(제일 무겁다 · 히어로 숫자 · **1점**). 눈은 이 순서로 읽는다.
- 🚫 **유리·레진에 안료를 칠하지 않는다** — 유리 색은 분산 림이, 레진 색은 «갇힌 빛»이 낸다.
  칠하면 「색 젤리」가 된다(유호 교정 08-15). **반복 부품의 갇힌 빛은 무채** — 불릿 수십 개가 코랄이면 신호 1점이 죽는다.
- **율 9단** `실2·숨4·틈8·참12·칸16·단24·켜40·장64·막104` — 이름 없는 px 을 지면에 적지 않는다.
  사다리 `장 ≥ 켜×1.5 ≥ 단×2.5`. 검사 = `node tools/lib/loom.js --율`.
- **부품 14종**(번호·순번·불릿·칩·따옴·체크·오브…)이 **분모**다 — `--분모` 가 미구현을 이름째 센다.
  🔑 번호는 CSS `counter` 가 «자동»으로 세운다(원고가 번호를 안 적어도 선다). 목록 밖 부품을 손으로 짓지 않는다.
- 🚫 **글자에는 재질을 안 입힌다** — 본문 타이포 불가침(§1 「타이포는 안 건드린다」).
- ⚠ **광학 보정을 `%` 로 주지 마라** — CSS 백분율 패딩은 부모의 «너비» 기준이라 원판에서 오작동한다
  (실측 08-16: 34.97px 원판에 11.86px). 원 안 숫자는 `padding-bottom:.025em`(= 실측 1.25%의 2배).

## 3. 타이포 — 3종 배타

정본 = [브랜드 폰트 정본](docs/브랜드_폰트_정본.md) (SUIT Variable · Inter Tight · DM Mono — 유호님 확정, 구 지정 폐기).

```css
--synk-font:      'Inter Tight','SUIT Variable',system-ui,-apple-system,'Apple SD Gothic Neo','Malgun Gothic',sans-serif;
--synk-font-mono: 'DM Mono',ui-monospace,SFMono-Regular,Consolas,monospace;
```

- **Inter Tight가 SUIT보다 앞** — 폴백이 글리프 단위라 이 순서가 3언어(라틴·키릴·한글)를 자동 분기한다. 순서를 바꾸지 말 것.
- 🚫 **DM Mono에 한글·키릴 금지**(글리프 없음 — 즉시 깨진다). `₮`·`№`·키릴은 **Inter Tight만**.
- 웨이트: 본문 500 · 헤드 800 · 태그라인 SUIT 900 / Inter Tight 500 소문자. 트래킹: 산세리프 음수 · 모노 양수.
- 몽골어가 한국어와 대등한 자리는 **1.04배** 광학 보정(`--synk-mn-scale`).

## 4. 로고 — `synk` (유호님 확정 2026-08-24 «승격»)

판정 정본 = [발표물 브랜드킷](docs/발표물/_브랜드킷.md) §3 · **도형·표현의 실행 정본 = `tools/lib/로고정본.js`**
(복붙 금지 — 필터가 든 레시피라 복붙하는 순간 사본이 갈린다. 소비자는 require 하거나 CLI `--json`).

- **워드마크 = `synk`** — k 는 Inter Tight SemiBold 실제 글리프 · **언제나 Coral**(특별 자리는 Pop).
  예전 `syn<` 워드마크는 **은퇴**(「syn+화살표」 오인 — 이름은 읽혀야 한다). 🚫새 산출물에 쓰지 않는다.
- **층이 둘** — 이름 `synk` 는 «보여주는 순간»(스플래시·대외·간판) · 기호 `<` 는 «이미 안에 들어온 자리»
  (상단바·진행·로딩·도장·워터마크·불릿 — ⚠상단바 왼쪽 금지 · 뒤로가기 자리).
- **기본 표현 = 잉크 카드 벡터 펠트** + 명품화 4레버(보풀 헤일로·손맛 실땀·두께 2톤·[카드]천 직조).
  소형(≲60px)·1도 인쇄 = 민판. 기호 `<` = 자수판(둥근 몸 13 · 60° · 28px↑ 실땀 — 작아지면 접힘).
  도장 = 배지 문법(쿠션·실땀 링·광학 중심). 렌더판 PNG 의존 0 — 어느 기계에서나 선다.
- `//`는 세 번째 색이 아니라 **잉크의 저채도**(3색 금지). 🚫 골드 복귀 금지 — 두 번째 액센트는 없다.
- 기호 `<` 벌림 **60.0°**(v2 그대로 — 날카로움은 의도 · 유호 08-18) · 🚫 기울임 변주(±6° = 삐딱함 · 08-21 실측).
- **워드마크 서체는 폰트 정본이 아니라 로고 정본이 정한다** — 그 충돌은 08-18 에 닫혔다.
- ⚠ 소비자가 일곱이다(로고주입·브랜드킷조립·색상조립·인쇄본빌드·교재읽기본·로고시트·스플래시).
  전부 `tools/lib/로고정본.js` 를 소비한다 — 새 소비자도 거기서 굽는다(사본을 만들지 않는다).

## 5. 산출물별 정본·통로

| 산출물 | 정본·검사 통로 |
|---|---|
| 발표물·인쇄물 10종 | [발표물 브랜드킷](docs/발표물/_브랜드킷.md) + `tools/발표물린트.js` + 빌드 `tools/발표물빌드.js`(폰트 임베드) |
| **지면 CSS 전량**(문서·캐러셀·인쇄·웹) | [Loom 엔진 설계](docs/Loom_엔진_설계.md) + **통로 `tools/lib/loom.js`**(§2-d) — 부품·율·재질. 시연 = `--시연` · 분모 = `--분모` · 🚫통로 밖 손 CSS |
| 디자인 토큰(색·서체 값의 기계 원천) | [디자인 토큰](docs/디자인_토큰.json) → `node tools/토큰빌드.js` → `docs/tools/synk-tokens.css` — **새 산출물은 토큰을 소비**(동결된 구 산출물 소급 개서 금지) |
| 브랜드 킷 완성본(화면 열람용 한 장) | [브랜드킷](docs/브랜드킷.html) = **조립 산출물**(손 편집 금지) — 값이 바뀌면 `python tools/브랜드킷조립.py` 재실행(토큰+역할 장부+로고 정본에서 조립 · 렌더린트 등록 ✗ — 색표 문서라 신호 1점이 원리상 위반) |
| 브랜드 킷 «색» 공유 한 벌(A4 · HTML+PDF 짝) | [브랜드킷_색상](docs/브랜드킷_색상.html)+`.pdf` = **조립 산출물**(손 편집 금지 · 유호 확정 08-24 승격) — 색이 바뀌면 `node tools/브랜드킷색상조립.js` 재실행(토큰 현행 30색 → Loom `인쇄` → 폰트 임베드 → 크롬 PDF · 렌더린트 등록 ✗ 위와 같은 사유 · PDF 폰트 감사는 이름이 아니라 Tf→Tj — memory `pdf-font-audit-type3-trap`) |
| 사운드킷(실음원 3종 · 유호님 확정) | [사운드킷 정본](docs/브랜드_사운드킷/_사운드킷.md) — 토큰 「사운드」 → `node tools/사운드킷빌드.js` → WAV 3종(손 편집 금지) |
| 인쇄 공통 규격 | `@page` margin 0 · `print-color-adjust: exact` · **자립형 1파일**(외부 자원 0) · 색+패턴 이중 표기(흑백 재복사 전제) |
| 바탕화면 지도·표 | 유호님 지시: PDF+HTML 쌍으로 바탕화면 `SYNK_지도`에 — PDF는 헤드리스 크롬, `@media print` 없으면 색이 안 찍힌다. **통로 = `node tools/지도대장.js --bake` 하나뿐** — 한컴·워드·캔바 등 통로 밖에서 구운 판은 바탕화면에 두지 않는다(근거 실측은 이 줄의 git 이력에). **정본 느낌 = 폴더 안 판**(유호님 확정 08-06 — 재조판 제안 금지) |
| 콘텐츠(보이스·금칙어) | `.claude/skills/synk-brand` — 시각이 아니라 말의 규칙 |
| 앱(Glide) | [GLIDE 조립가이드](docs/GLIDE_조립가이드_v947.md) — 「고쳤다」는 코드가 아니라 **렌더 픽셀**로 증명 |
| 앱(SYNK-talk·Expo) | talk repo `docs/이_저장소_규약.md` §5 — 킷을 테마 토큰으로 반입(UI 착수 시), §1 품질 축·이모지 절제 동일 적용 |
| 접수 폼(크루카드) | `crewcard/카드_kr.html`·`카드_mn.html` — 리터럴 hex(토큰 CSS 변수 비참조)지만 값은 2027 킷과 일치, `node tools/브랜드렌더린트.js` 재검증 대비·킷밖색·킷밖서체·구역위반 전부 0(2026-08-20). 이력 = memory `brand-kit-application-audit-2026-08-04` |

## 6. 완성도 스킬 — 킷 다음에, 킷 아래에서

`.claude/skills/`에 설치(2026-08-05 · MIT · 전문 검수 후 반입). **순서: 킷으로 골격 → 스킬로 디테일 → 린트/검수.**

> **스킬은 불러야 적용된다** — 항상 떠 있는 건 이름+설명 한 줄뿐이라, 안 부르면 규칙이 아예 없다.
> 자동 주입 훅(design-guard)은 압축 08-20으로 철거됐다 — **시각 파일을 만지기 전에 이 파일을 스스로 연다.**

| 스킬 | 언제 | 출처 |
|---|---|---|
| `make-interfaces-feel-better` | UI·HTML 디테일 제작+검수 — 동심원 모서리·광학 정렬·그림자·tabular-nums·히트 영역. **인쇄물엔 typography/surfaces만 해당** | jakubkrehel |
| `emil-design-eng` | 애니메이션·인터랙션 설계 판단 — 빈도→목적→이징→시간 결정 틀, 이징 커브 값 | emilkowalski |

- 미설치 판정(재제안 금지): apple-design(킷과 충돌)·pick-ui-library(라이브러리 없음)·prototype(/forge 중복)·improve-animations·find-animation-opportunities(자체앱 단계에 재고).
- **삭제 판정(재제안 금지 · 유호 판정 2026-08-09)**: `review-animations` — 0회 호출 실측은 훅 머리말에 있다(위 「두 곳에 안 적는다」 그대로). 모션 **설계** 판단은 `emil-design-eng`가 그대로 지고, 검수는 그 원칙으로 한다. 자체앱 모션이 실물로 서면 재반입 검토 — 그때까지 다시 제안하지 않는다.

## 7. 레퍼런스 — 필요할 때 브라우저로 연다 (상시 MCP 연결 금지 — 이미지 토큰)

- [lazyweb.com](https://www.lazyweb.com/) 실화면 25만+ · [namethatui.com](https://namethatui.com/) UI 요소 명칭 사전 · [motionsites.ai](https://motionsites.ai/) 모션 갤러리(프롬프트 복사) · [getdesign.md](https://getdesign.md/) 타사 시스템 분석(열람만)

## 8. 감각 — 모서리·그림자·모션·히트영역 (유호님 확정 2026-08-06)

값의 정본 = [디자인 토큰](docs/디자인_토큰.json) 「감각」. 근거 = 스킬(mifb·emil) vs 킷 실측 대조감사(memory `design-craft-audit-2026-08-06`).

- 감각 규칙: 동심원 모서리(바깥=안쪽+패딩) · 그림자=Graphite 틴트만·순검정 금지 · 고빈도·키보드=무모션·UI 300ms 상한·transform/opacity만·reduced-motion 존중 · 히트 44(터치)/40(밀집) · 숫자=tabular-nums · 눌림 scale 0.96 · 비활성=40% 불투명 · 포커스 링=다크 Coral/라이트 Coral 3 · 수치=가안(Expo 확정 전 인용 금지)
- **오류 = 빨강 신설 금지** — Coral 3+아이콘+리프레이밍 문구. 유호님: 「빨강은 코랄을 죽인다」 — 사운드킷 「실패음 없음」과 같은 축이다.
- **이미지 아웃라인 예외** — 1px 라이트 `rgba(0,0,0,.1)`/다크 `rgba(255,255,255,.1)`만. 철칙①(순백·순검정 금지)의 스코프는 **면·글자**다 — 틴트 아웃라인은 이미지 가장자리에서 때처럼 보인다.
- **수치는 가안** — 모서리 스케일·그림자 값·모션 duration·이징은 토큰 「가안」 블록에 있고 **Expo 첫 화면 실물에서 확정**한다. 확정 전까지 새 산출물이 가안 수치를 인용하지 않는다(동결 구 산출물 소급 개서 금지는 §5 그대로).
