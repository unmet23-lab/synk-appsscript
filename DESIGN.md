# SYNK DESIGN — 시각 제작의 현재 기준

> 2026-09-10 정리. 눈에 보이는 결과물을 만들거나 고칠 때 `synk-design`과 이 문서에서 시작한다. 현재 승인된 자산·규칙만 제작 기준으로 선택하고, 과거 사례는 읽힘과 판단의 비교에 사용한다. 최신 수정일이 승인 상태를 대신하지 않는다.

값은 아래 원천에서 가져온다. 외부 스킬은 SYNK 킷 안에서 디테일을 개선한다. 실행 책임은 [AI 운영 원칙](docs/AI_운영원칙.md), 품질은 [명품 기준](docs/명품_기준_v1.md) §3·§4를 따른다.

## 1. 원천과 표현 원칙

| 필요한 것 | 현재 원천 |
|---|---|
| 색·서체·재질·율·부품·감각 값 | [디자인 토큰](docs/디자인_토큰.json), 배포용 [토큰 CSS](docs/tools/synk-tokens.css) |
| 서체 역할·실제 폰트 로드 | [브랜드 폰트 정본](docs/브랜드_폰트_정본.md) |
| 대외 로고 선택·원본·조합 | [중립색 로고 정본](docs/로고_중립색_정본_v1.md), [현행 킷 실물](docs/홍보물/마케팅실행_20260909/브랜드킷/index.html) |
| 마스코트 파일 선택 | [마스코트 자산 코드](tools/lib/마스코트자산.js), [치수 정본](docs/캐릭터/마스코트_치수_정본_v1.md) |
| 지면의 재질·간격·부품 | [Loom 설계](docs/Loom_엔진_설계.md), [실행 코드](tools/lib/loom.js) |
| A 다크·B 타이포·C 겹침의 사용 범위 | [표현 모드](docs/디자인_컨셉_정본_v1.md) |
| 고객·제공물·회사별 역할 | [마케팅 정본](docs/마케팅_정본.md) |
| 승인된 읽힘·배치·마감 사례 | [명품 기준 §3-2](docs/명품_기준_v1.md#3-2-09-09-후속-승인-실물--다음-작업의-출발점), [대표 피드백](docs/명품_기준_v1.md#feedback-cases) |

- 기능과 디자인을 함께 완성한다. 위계는 크기·굵기·여백·정보 밀도로 만들고, 행동의 핵심 신호가 먼저 읽히게 한다.
- 모티프는 시냅스 노드·링크다. 미승인 게르 천창 모티프를 브랜드 문법으로 쓰지 않는다.
- 웹·앱에서 이모지를 위계나 아이콘 전체의 대용으로 쓰지 않는다. 콘텐츠의 소량 액센트는 가능하다.
- 타사 디자인 체계는 참고만 한다. 서체·색·재질 체계를 섞거나 본문 글자에 재질을 입히지 않는다.

## 2. 색·마스코트·재질

Paper·Ink·Ink Deep·Stitch와 양모 회색을 바탕으로, 유채색은 Soft·몸통·Deep의 역할로 고른다. 값·허용 범위는 토큰을 읽고 퇴역 대기 색은 새로 도입하지 않는다.

- 면·글자에 순백·순검정을 새로 쓰지 않는다. 라이트에서 Coral·Meadow 몸통은 면용이며 글자는 Deep/Ink 등 토큰의 직책을 따른다.
- 몸통 면의 글자: Coral·Butter에는 Ink, Lapis에는 Paper, Pop에는 큰 Ink 글자. 캐릭터 색 Moss·Chestnut은 면용이고 Indigo 면에는 Paper 글자다. 실제 사용 크기의 대비를 확인한다.
- 한 화면은 주연 1실+조연 1실로 묶는다. KC 4색은 Part 6 전용이다. 회사별 승인 로고와 캐릭터 고유색을 이 규칙 때문에 임의로 재칠하지 않는다.
- LAB·SHIFT·PULSE의 로고색은 §4를 따른다. 신호색을 하나로 모으는 원칙을 모든 사업 로고의 코랄 통일로 해석하지 않는다.
- 마스코트는 승인 원본을 보존한다. 평상복 코랄 램프와 UI 코랄의 연결을 유지하며, 코랄 짙은 면에서 윤곽이 묻히지 않게 한다. 인라인 이미지에는 `data-synk-mascot`를 유지해 기존 린트가 식별하게 한다.
- 표정만 바꿀 때 털·몸·의상과 공통 알파를 고정한다. [표정 전환 기준](docs/캐릭터/캐릭터_생명감_설계.md#1-a-표정-전환의-재질-고정)에 따라 좁은 변화 영역 밖 동일성과 실제 크기 반복 재생을 확인한다. 배치 변경을 캐릭터 재생성으로 해결하지 않는다.

**SYNK-talk는 다크 한 벌이다.** 바닥 Ink Deep, 카드 Ink, 글자 Paper→Oat→Stone, 실땀 Stitch를 쓴다. 역할색은 면=Deep·글자=Soft이며 카드 밝기를 임의로 높이지 않는다. 기업 웹에 다크 단일을 강제하지 않는다. 앱 반영은 현재 테마·화면으로 확인한다.

정보 판의 역할: 무광 천은 조용한 정보 바닥, 유리는 뒤가 보여야 하는 판, 패턴지는 도안·계획의 결. 다크 위 밝은 패턴지는 조정하고 실제 글자 대비를 잰다. UI 장식은 펠트, 앱의 CSS 재질 목업은 승인 실물 부품으로 교체할 때까지의 비계다.

Loom 지면은 `node tools/lib/loom.js --css 캐러셀|문서|인쇄|웹|부품만`에서 재질·율·부품을 가져온다. 별도 지면마다 이를 손으로 재정의하지 않는다. 펠트는 빛을 먹는 표식·표지 오브, 유리는 통과시키는 판·태그, 레진은 빛을 가두는 히어로 숫자 1점의 역할이다. 유리·레진을 안료처럼 칠하지 않고 반복 부품의 갇힌 빛은 무채로 둔다.

간격은 토큰의 율, 부품은 토큰 목록과 `--분모`를 따른다. 번호는 CSS counter로 세운다. 원 안 숫자의 광학 보정은 너비 기준 백분율 패딩을 피하고 Loom의 보정을 사용한다.

## 3. 타이포 — 역할과 실제 로드를 함께 확인

- 한글은 SUIT Variable, 라틴·키릴·몽골어는 Inter Tight, 숫자·코드 등 모노 역할은 DM Mono다. DM Mono에 한글·키릴·₮·№를 맡기지 않는다.
- 본문 스택은 `SYNK Bracket → Inter Tight → SUIT Variable → 시스템 폴백` 순서다. SYNK Bracket은 「 」 두 문자만 교정하는 별칭이며 새 본문 서체가 아니다.
- 굵기·자간·태그라인과 예외는 [폰트 정본](docs/브랜드_폰트_정본.md)을 따른다. 몽골어가 한국어와 대등한 자리는 정본의 1.04배 광학 보정을 적용한다.
- 폰트 이름을 CSS에 적은 것만으로 로드를 완료했다고 하지 않는다. 실제 표시 서체·글리프와 인쇄/PDF 임베드를 확인한다. 로고의 글자 형태는 본문 폰트로 다시 만들지 않는다.

## 4. 로고 — 현재 승인본을 용도에 연결

선택·색·비율의 담당 정본은 [중립색 로고 정본](docs/로고_중립색_정본_v1.md)이다.

| 자리 | 가져올 것 |
|---|---|
| 기업 웹·소개·홍보에서 브랜드 재질을 보여주는 로고 | [사진형 스티치 킷의 배치용 완성 조합](docs/홍보물/마케팅실행_20260909/브랜드킷/사용규칙.md). SYNK 또는 SYNK+사업명 전체를 사용 |
| 파비콘·작은 인쇄·정확한 도형 재현이 필요한 자리 | 현행 정밀 SVG. 실제 작은 크기의 철자·윤곽을 먼저 확인 |
| 앱·내부의 이름과 단독 표식 | `tools/lib/로고정본.js`의 `syn<`·기호·도장. 대외 이름은 `synk` |

대외 SYNK는 밝은 배경 Ink, 어두운 배경 Paper다. 사업명은 승인된 LAB 코랄·SHIFT 파랑·PULSE 베리를 유지한다. 내부 꺾쇠의 코랄 신호는 별도 용도다. 로고 파일의 형태·재질·사업명 조합을 보존하고 본문 글자로 사업명을 재조립하지 않는다.

벡터 생성기의 크기별 필터 규칙으로 사진형을 일괄 민판으로 바꾸지 않는다. 작은 사진형이 뭉개지면 실물 비교 후 정밀 SVG·적절한 크기·밝은 패널을 선택한다.

기호 `<`는 내부 진행·로딩·도장 등의 표식이며 뒤로가기 자리인 상단바 왼쪽에 놓지 않는다. 승인된 각도·형태를 임의로 기울이지 않는다. 특별판 알록은 실행 정본의 용도·최소 크기를 따르고 상시 기본 로고로 승격하지 않는다. `//`는 잉크의 저채도 표현이며 골드나 새 액센트를 추가하지 않는다.

## 5. 산출물별 제작 통로

| 작업 | 원천·실행·확인 |
|---|---|
| 디자인 토큰 갱신 | `docs/디자인_토큰.json` → `node tools/토큰빌드.js` → `docs/tools/synk-tokens.css`. 새 산출물은 이를 소비 |
| 발표·인쇄 | [발표물 킷](docs/발표물/_브랜드킷.md), `tools/발표물빌드.js`, `tools/발표물린트.js`. 폰트 임베드·실제 크기 확인 |
| 인쇄 공통 | `@page margin:0`, `print-color-adjust:exact`, 외부 자원 없는 자립형 1파일, 흑백 복사에 대비한 색+패턴 표기 |
| 킷 공유본 | `tools/브랜드킷조립.py`, `tools/브랜드킷색상조립.js`로 생성. 조립 결과 HTML/PDF를 손으로 수정하지 않음 |
| 마스코트 의상 | [의상 제작 정본](docs/캐릭터/의상제작_정본.md). 승인 몸·옷 동시 참조, 지정 모델과 표정 후처리, 기존 4K 원본 보존 |
| 앱의 실물 부품 | `tools/룸굽기.js` → 공방 자산, `tools/화면시안.js` → [한 지면 시안](docs/화면_시안.html). 개별 PNG만 보고 화면 완성을 판정하지 않음 |
| SYNK-talk 앱 | 해당 저장소 `docs/이_저장소_규약.md`와 현재 테마·화면. 이 문서의 갱신을 앱 반영으로 세지 않음 |
| 영상 | `영상/`의 Remotion, `영상/굽기.js`, `영상/프레임뽑기.js`. 색·서체는 `영상/src/킷/색.ts`·`폰트.ts`에서 소비 |
| 사운드 | [사운드킷](docs/브랜드_사운드킷/_사운드킷.md), `tools/사운드킷빌드.js`. 승인된 캐릭터 악기·목소리를 유지하고 실제 청취 |
| 학생 접점 문안 | `synk-brand` 다음 해당 콘텐츠 스킬. 현재 제공 범위와 독자의 언어를 확인 |

## 6. 완성도와 확인

`make-interfaces-feel-better`는 정렬·타이포·표면·상호작용, `emil-design-eng`는 목적·빈도에 맞는 모션에 적용한다. 인쇄에는 타이포·표면 원칙만 사용한다.

[토큰](docs/디자인_토큰.json)의 `감각.규칙`을 따르고 `감각.가안`을 확정값으로 쓰지 않는다. 동심원 모서리, 지정 틴트 그림자, 터치 영역, 숫자 폭, 눌림·비활성·포커스를 함께 확인한다. 고빈도·키보드 동작은 무모션, 모션은 transform/opacity와 reduced-motion을 존중한다. 오류에 별도 빨강을 신설하지 않는다. 이미지 가장자리의 저농도 순백·순검정 아웃라인 예외는 면·글자의 색 규칙과 구분한다.

기존 검사와 함께 화면은 실제 크기, 영상은 재생·프레임, 소리는 청취로 확인한다. 기술 통과를 배치·읽힘 전체의 통과로 세지 않는다. 결과에 **현재 원본 / 반영한 자리 / 실물·게시 확인과 남은 것**을 남긴다. 승인 원본을 보존하고 구 산출물은 요청 범위만 갱신한다.
